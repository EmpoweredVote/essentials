---
phase: 177-city-of-hillsboro-deep-seed
plan: 04
type: execute
wave: 3
depends_on: ["177-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1152_pace_stances.sql
  - C:/EV-Accounts/backend/migrations/1153_salgado_stances.sql
  - C:/EV-Accounts/backend/migrations/1154_anvery_stances.sql
  - C:/EV-Accounts/backend/migrations/1155_sinclair_stances.sql
  - C:/EV-Accounts/backend/migrations/1156_case_stances.sql
  - C:/EV-Accounts/backend/migrations/1157_alcaire_stances.sql
  - C:/EV-Accounts/backend/migrations/1158_harris_stances.sql
autonomous: false
requirements: [WASH-03]

must_haves:
  truths:
    - "Each of the 7 officials has evidence-only compass stances where a public record exists"
    - "Every stance is 100% cited (reasoning + sources in politician_context); ZERO default/neutral values"
    - "Topics with no evidence are omitted entirely (honest blank spokes), never defaulted"
    - "topic_id is resolved LIVE via JOIN on compass_topics.topic_key AND is_live=true (no hardcoded topic UUIDs)"
    - "No judicial-* topics are used (City Attorney is appointed, not elected)"
    - "Stance research was done one agent at a time (never parallel)"
    - "Implements CONTEXT decision D-11 (all live topics, one agent at a time, evidence-only, no defaults, skip judicial)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1152_pace_stances.sql"
      provides: "Mayor Beach Pace stances (audit-only)"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1158_harris_stances.sql"
      provides: "Councilor Rob Harris stances (audit-only)"
      contains: "politician_answers"
  key_links:
    - from: "each stance migration"
      to: "inform.compass_topics (is_live=true)"
      via: "JOIN on topic_key"
      pattern: "topic_key = s.topic_key AND ct.is_live = true"
    - from: "each stance migration"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\)"
---

<objective>
Research and author 7 audit-only stance migrations — one per Hillsboro official — populating
evidence-only compass stances. Research runs ONE AGENT AT A TIME (rate-limit rule), evidence-only,
100% cited, honest blank spokes where no public record exists, ZERO default values, and skips all
judicial-* topics (Hillsboro's City Attorney is appointed).

Purpose: Satisfy the stance portion of WASH-03 — evidence-only stances render on profiles.
Output: 7 audit-only migrations (`1152_pace_stances.sql` … `1158_harris_stances.sql`), NOT
registered in the ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-RESEARCH.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-01-SUMMARY.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor drives stance research (one agent at a time) and WRITES
the 7 `.sql` files. It has NO DB access. The INLINE ORCHESTRATOR applies each via `psql -f` and
commits them to the EV-Accounts repo. These are AUDIT-ONLY migrations — none writes a
schema_migrations ledger row; the on-disk file counter is authoritative. Migration numbers
1152–1158 assume plan 02 used 1150 + plan 03 used 1151; if Wave-0 shifted the base, renumber
consistently.

**STANCE RULES (non-negotiable — from memory + CONTEXT D-11):**
- ONE research agent at a time — never parallel (parallel burns rate-limit quota with no output).
- Evidence-only chairs model (integer 1–5); a "chair" is the position statement the evidence
  matches, NOT a polarity scale — pick the chair the evidence supports.
- 100% cited: every answer has reasoning + sources in politician_context.
- NO default values — a topic with no evidence is OMITTED entirely (blank spoke is honest).
- Skip all 8 judicial-* topics.
- Use the LIVE topic_key list captured in 177-01-SUMMARY.md; resolve topic_id via JOIN on
  compass_topics.topic_key AND is_live=true — never hardcode a topic UUID.
- Aim for 18–21+ stances where the record supports it; thin records get honest blanks.
- Pre-tenure attribution rule: do not attribute a vote/action to an official who was not seated
  at the time. Salgado was appointed in 2025 (verify per 177-01); Alcaire seated since 2017.
</context>

<interfaces>
inform.compass_topics    (id, topic_key, is_live)   -- 44 live topics; 8 are judicial-* (SKIP)
inform.politician_answers (politician_id, topic_id, value)                    -- ON CONFLICT (politician_id, topic_id) DO UPDATE SET value
inform.politician_context (politician_id, topic_id, reasoning, sources)       -- sources is text[]; ON CONFLICT DO UPDATE
-- politician UUIDs: resolve by external_id from 177-02-SUMMARY.md:
--   -4134101 Beach Pace | -4134102 Salgado | -4134103 Anvery | -4134104 Sinclair
--   -4134105 Case | -4134106 Alcaire | -4134107 Harris
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Research + author stance migrations for Mayor Pace and the 3 longest-tenured councilors (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1152_pace_stances.sql, C:/EV-Accounts/backend/migrations/1157_alcaire_stances.sql, C:/EV-Accounts/backend/migrations/1155_sinclair_stances.sql, C:/EV-Accounts/backend/migrations/1158_harris_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1133_beaty_stances.sql (48-line stance template — two-statement politician_answers + politician_context structure)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md §"<N+2>_pace_stances.sql through <N+8>_harris_stances.sql"
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-01-SUMMARY.md (live topic_key list)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Pace (-4134101), Alcaire (-4134106), Sinclair (-4134104), Harris (-4134107), IN
    SEQUENCE (one research agent at a time — do not spawn parallel research): research the official's
    documented positions across the live compass topics (skip judicial-*), gathering evidence with
    URLs. Then write the migration file using the two-statement structure from the 1133 analog:
    a WITH s(topic_key, val, reasoning, sources) VALUES block (one row per topic WITH evidence — omit
    topics with no evidence), an INSERT into inform.politician_answers … JOIN compass_topics ON
    topic_key AND is_live=true ON CONFLICT (politician_id, topic_id) DO UPDATE SET value, then the
    IDENTICAL VALUES block feeding INSERT into inform.politician_context … ON CONFLICT DO UPDATE SET
    reasoning, sources. Hardcode the politician UUID (from 177-02-SUMMARY.md) in each file. val is an
    integer 1–5 chair; never default; blank spoke = omit the row. Wrap each file in BEGIN…COMMIT with
    the audit-only header comment; NO ledger INSERT. Longest-tenured members (Alcaire since 2017,
    Sinclair since 2023) support deeper coverage; Harris/Pace seated Jan 2025 — thinner but real
    records. No fenced code in this action — 1133 + 177-PATTERNS.md are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics, ≥1 answer per official for those with any public record</automated>
  </verify>
  <acceptance_criteria>
    - 4 files authored: 1152_pace_stances.sql, 1157_alcaire_stances.sql, 1155_sinclair_stances.sql, 1158_harris_stances.sql
    - Each uses the two-statement politician_answers + politician_context structure
    - topic_id resolved via JOIN on topic_key AND is_live=true (no hardcoded topic UUIDs)
    - No judicial-* topic_key appears in any VALUES block
    - Every VALUES row has non-empty reasoning + at least one source URL
    - No default/neutral row for a topic lacking evidence (omitted instead)
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>4 stance migrations authored from one-at-a-time research; audit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Research + author stance migrations for the 3 remaining councilors (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1153_salgado_stances.sql, C:/EV-Accounts/backend/migrations/1154_anvery_stances.sql, C:/EV-Accounts/backend/migrations/1156_case_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1133_beaty_stances.sql
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md §"<N+2>_pace_stances.sql through <N+8>_harris_stances.sql"
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-01-SUMMARY.md (live topic_key list)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Salgado (-4134102), Anvery (-4134103), Case (-4134105), IN SEQUENCE (one agent at a
    time): research documented positions across live compass topics (skip judicial-*), then author
    the migration with the same two-statement structure as Task 1. Salgado was APPOINTED in 2025 to
    fill a vacancy — apply the pre-tenure attribution rule strictly (only attribute post-seating
    actions; his record is likely thin — honest blanks are correct, do NOT force stances). Anvery and
    Case were both elected Nov 2024 (seated Jan 2025) — recent, likely thinner records; use
    campaign-platform first-party statements as evidence where votes don't yet exist, cited. Never
    default. Audit-only header; NO ledger INSERT. UUIDs hardcoded from 177-02-SUMMARY.md.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 3 files authored: 1153_salgado_stances.sql, 1154_anvery_stances.sql, 1156_case_stances.sql
    - Same two-statement structure; topic_id via is_live JOIN
    - No judicial-* topics; every row cited; no defaults (thin records → honest blanks, especially Salgado)
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>3 stance migrations authored from one-at-a-time research; audit passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator applies all 7, runs stance audit, commits</name>
  <what-built>7 audit-only stance migrations. Orchestrator applies each and runs the citation/no-default audit.</what-built>
  <how-to-verify>
    1. Apply all 7 in order: `psql "$DATABASE_URL" -f <each stance file>`.
    2. Stance audit (run as SELECTs against the 7 Hillsboro politician UUIDs):
       a. Every politician_answers row has a matching politician_context row with non-null reasoning + non-empty sources (100% cited).
       b. ZERO rows where value is a defaulted/neutral placeholder for a topic lacking evidence (spot-check reasoning quality).
       c. ZERO judicial-* topics linked to any Hillsboro official.
       d. Each official has ≥1 stance if any public record exists; a genuinely empty official is documented, not padded.
    3. Commit all 7 in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/115{2,3,4,5,6,7,8}_*.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - All 7 stance migrations applied
    - Audit a–d all pass (100% cited, no defaults, no judicial, honest blanks documented)
    - All 7 committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official stance counts + any honest blanks, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Stance INSERTs applied to production inform schema via psql |
| research agent → public web | Evidence gathered from official/news/campaign sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-10 | Tampering | Uncited or fabricated stance value | mitigate | 100%-cited rule enforced by audit (every answer needs reasoning + sources); no-default rule; one-agent-at-a-time keeps evidence traceable |
| T-177-11 | Repudiation | Wrong attribution (pre-tenure vote) | mitigate | Pre-tenure attribution rule (Salgado appointed 2025; Pace/Anvery/Case/Harris seated Jan 2025); verify seating before attributing older actions |
| T-177-12 | Elevation of Privilege | Judicial-* topic seeded for a non-judicial official | mitigate | Skip-judicial rule; audit gate 2c asserts zero judicial-* topics |
| T-177-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- 7 stance migrations applied; audit confirms 100% cited, zero defaults, zero judicial topics,
  honest blanks documented; all committed.
</verification>

<success_criteria>
- Evidence-only stances for all 7 officials (where a record exists), 100% cited, no defaults,
  no judicial topics, researched one agent at a time.
</success_criteria>

<output>
Create `.planning/phases/177-city-of-hillsboro-deep-seed/177-04-SUMMARY.md` when done, recording
per-official stance counts and any honest blanks.
</output>
