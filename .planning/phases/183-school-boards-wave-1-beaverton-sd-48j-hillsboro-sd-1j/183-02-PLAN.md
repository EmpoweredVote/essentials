---
phase: 183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j
plan: 02
type: execute
wave: 2
depends_on: [183-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1203_or_westmetro_school_boards_wave1.sql
autonomous: false
requirements: [WSCH-01, WSCH-02]
user_setup: []

must_haves:
  truths:
    - "Two governments exist: 'Beaverton School District 48J, Oregon, US' (type='LOCAL', state='OR' uppercase, geo_id='4101920') and 'Hillsboro School District 1J, Oregon, US' (type='LOCAL', state='OR' uppercase, geo_id='4100023'), each inserted via a WHERE NOT EXISTS name guard (no unique constraint on governments)"
    - "Two chambers exist with VERBATIM per-district names (NOT the 254_or blanket 'Board of Education'): 'School Board' (official_count=7) under the Beaverton government and 'Board of Directors' (official_count=7) under the Hillsboro government — the generated slug/path column NEVER in the INSERT list"
    - "Exactly ONE SCHOOL district per government carries all 7 seats: district_type='SCHOOL', state='or' LOWERCASE, mtfcc='G5420', geo_id matching the EXISTING Phase-174 geofence (4101920 Beaverton / 4100023 Hillsboro) — single-shared-district pattern, NOT one district per seat, and NO new geofence loaded"
    - "7 Beaverton offices (ext_id -4101921..-4101927) titled 'Director, Zone 1'..'Director, Zone 7' with Chair on Zone 6 (Rajee → 'Director, Zone 6 (Chair)') and Vice Chair on Zone 3 (Potter → 'Director, Zone 3 (Vice Chair)'); all is_appointed=false, is_appointed_position=false, party=NULL, representing_state='OR' uppercase"
    - "7 Hillsboro offices (ext_id -4100024..-4100030) titled 'Director, Position 1'..'Director, Position 7' with Chair on Position 5 (Pantoja → 'Director, Position 5 (Chair)') and Vice Chair on Position 4 (Kim → 'Director, Position 4 (Vice Chair)'); all is_appointed=false, is_appointed_position=false, party=NULL, representing_state='OR' uppercase"
    - "All 14 politicians (7+7) are seeded is_active=true, is_incumbent=true, is_vacant=false, party=NULL (antipartisan); the 3 Hillsboro student representatives and Board Secretary Rose Roman are NOT seeded (D-R4); no Chair/Vice-Chair is a separate office row (14 offices total, not 16)"
    - "The in-migration post-verify DO block asserts (rolling back on any mismatch): exactly 1 gov each, exactly 7 offices on each SCHOOL district (BSD geo_id 4101920 / HSD geo_id 4100023, state='or'), and 0 section-split orphans for both G5420 geo_ids — and RAISES on failure"
    - "office_id back-fill is applied ONLY IF the freshest structural migration (182's 1196) still writes it — the executor checks 1196 before copying that UPDATE step; if 1196 does not back-fill office_id, this migration does not either"
    - "Migration 1203 registers in supabase_migrations.schema_migrations (version '1203', name 'or_westmetro_school_boards_wave1') OUTSIDE the transaction, after COMMIT — it is a STRUCTURAL migration"
    - "The file is saved UTF-8 (roster carries 'Vân Truong' + 'Karen Pérez'); verify-gate hygiene: the literal strings slug / schema_migrations (except the intended ledger INSERT) / photo_origin_url do not appear in comments"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1203_or_westmetro_school_boards_wave1.sql"
      provides: "Structural migration: 2 governments + 2 verbatim-named chambers + 2 single-shared SCHOOL districts (reusing existing Phase-174 geofences) + 14 politician/office CTEs (7 Beaverton 'Director, Zone N' + 7 Hillsboro 'Director, Position N', Chair/Vice-Chair as title-on-seat) + post-verify DO block (2 govs / 7+7 offices / 0 section-split) + ledger registration"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 200
  key_links:
    - from: "essentials.chambers ('School Board' / 'Board of Directors')"
      to: "essentials.governments (Beaverton/Hillsboro School District rows)"
      via: "government_id subquery WHERE name = the verbatim government name"
      pattern: "(Beaverton|Hillsboro) School District"
    - from: "essentials.offices (14 director seats)"
      to: "essentials.districts (2 shared SCHOOL districts, state='or', geo_id 4101920/4100023)"
      via: "CROSS JOIN ins_p on the SCHOOL district WHERE d.state='or'"
      pattern: "district_type = 'SCHOOL'"
    - from: "essentials.districts (SCHOOL, geo_id 4101920/4100023)"
      to: "essentials.geofence_boundaries (existing Phase-174 G5420 rows)"
      via: "geo_id match (no loader — geofence already present)"
      pattern: "'4101920'|'4100023'"
---

<objective>
Author the single structural migration that seeds both school-district governments end-to-end: 2
governments, 2 verbatim-named chambers, 2 single-shared SCHOOL districts on the EXISTING Phase-174 G5420
geofences, and 14 politician+office rows (7 Beaverton "Director, Zone N" + 7 Hillsboro "Director,
Position N", Chair/Vice-Chair as title-on-seat suffixes). The orchestrator then applies it and confirms
the post-verify gates and address routing.

Purpose: Satisfy the structural half of WSCH-01 + WSCH-02 in one file (D-P1 single-file precedent,
254_or shape). This is a verbatim copy of the proven 1107/254_or school-district pattern with two
deliberate deviations: (1) per-district verbatim chamber names ('School Board' / 'Board of Directors')
instead of 254_or's blanket 'Board of Education' (D-R1/R2); (2) no geofence loader — both G5420 rows
already exist (Phase 174) and must NOT be re-loaded. Both boards are whole-district at-large (Wave-0
confirmed), so each government gets exactly ONE shared SCHOOL district carrying all 7 seats — no
sub-zones, no per-seat districts.
Output: `1203_or_westmetro_school_boards_wave1.sql` (registered structural migration) + a recorded
post-verify pass and address-routing confirmation for downstream plans 03-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-RESEARCH.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-01-SUMMARY.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` migration file only. It has NO database
access and NO Supabase MCP. The INLINE ORCHESTRATOR applies it via `psql "$DATABASE_URL" -f <file>`
(DATABASE_URL from C:/EV-Accounts/backend/.env), reads the post-verify NOTICE, runs the SQL gates, and
re-runs the routing smoke test. C:/EV-Accounts is a SEPARATE git repo — never cd into it; use
`git -C "C:/EV-Accounts"` for any commit.

**CASING (the #1 silent-failure pitfall):** districts.state = LOWERCASE 'or' everywhere in JOIN/WHERE;
governments.state and offices.representing_state = UPPERCASE 'OR'. Wave-0 Probe F confirmed 'or'
lowercase is correct for the existing PPS SCHOOL district — use the SUMMARY's confirmed value.

**VERIFY-GATE HYGIENE:** keep the literal strings `slug`, `schema_migrations` (except the intended
ledger INSERT), and `photo_origin_url` out of comments. Save UTF-8 for 'Vân'/'Pérez'.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author structural migration 1203 for both school-district boards</name>
  <files>C:/EV-Accounts/backend/migrations/1203_or_westmetro_school_boards_wave1.sql</files>
  <read_first>
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-01-SUMMARY.md (Wave-0 confirmed: geo_ids, ext_id blocks, migration number 1203, lowercase 'or' casing, both 7-director rosters with Zone/Position + Chair/Vice-Chair, locked verbatim chamber + office-title strings)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-RESEARCH.md §"Pattern 2: Structural Migration", §"Pattern 3: Verified Rosters", §"Pattern 4: Office-title convention", §"Anti-Patterns to Avoid"
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md §"1203_or_westmetro_school_boards_wave1.sql" (header/government/chamber/district/CTE/post-verify/ledger patterns with exact analog line refs)
    - C:/EV-Accounts/backend/migrations/1107_ccsd_board_of_trustees.sql (PRIMARY analog — single-shared-district shape + post-verify DO block + ledger registration; read in full)
    - C:/EV-Accounts/backend/migrations/254_or_school_districts.sql (SECONDARY analog — OR-specific multi-district-in-one-file, lowercase 'or', G5420; note the office_id back-fill convention)
    - C:/EV-Accounts/backend/migrations/1196_cornelius_city_council.sql (freshest structural migration — check whether it still writes an office_id back-fill UPDATE and whether the ledger-register step matches; copy that decision)
  </read_first>
  <action>
    Write one structural migration seeding BOTH districts. Wrap the DDL in BEGIN; ... COMMIT; then
    register the ledger row AFTER commit. Header comment block: purpose + a `CRITICAL:` bullet list
    adapted from 1107 — governments has NO unique constraint (WHERE NOT EXISTS name guard); chambers
    generated slug/path column NEVER in the INSERT list; districts.state must be 'or' LOWERCASE (uppercase
    = 0 rows); governments.state / offices.representing_state = 'OR' UPPERCASE; district_type='SCHOOL'
    (not 'SCHOOL_DISTRICT'); party=NULL on all 14 (antipartisan); Chair/Vice-Chair are titles on existing
    seats NOT separate rows (14 offices, not 16); both G5420 geofences (4101920/4100023) ALREADY EXIST
    (Phase 174) — do NOT run any loader; save UTF-8 for 'Vân Truong' + 'Karen Pérez'.
    GOVERNMENTS: insert 'Beaverton School District 48J, Oregon, US' (type='LOCAL', state='OR',
    geo_id='4101920') and 'Hillsboro School District 1J, Oregon, US' (type='LOCAL', state='OR',
    geo_id='4100023'), each guarded by WHERE NOT EXISTS on the exact name.
    CHAMBERS: 'School Board' (name_formal 'Beaverton School District 48J School Board', official_count=7)
    under Beaverton; 'Board of Directors' (name_formal 'Hillsboro School District 1J Board of Directors',
    official_count=7) under Hillsboro — government_id via name subquery, NOT EXISTS guard, NO
    slug/path column in the column list.
    DISTRICTS: one SCHOOL district per government — (district_type='SCHOOL', state='or', geo_id='4101920',
    label='Beaverton School District 48J', mtfcc='G5420') and the 4100023/'Hillsboro School District 1J'
    twin — NOT EXISTS guard on (geo_id, district_type, state).
    OFFICES + POLITICIANS (14× the 1107 `WITH ins_p AS (INSERT...RETURNING id) INSERT INTO offices
    SELECT...` CTE): Beaverton ext_id -4101921..-4101927 for Zones 1-7 (Van Truong / Karen Pérez /
    Melissa Potter / Sunita Garg / Syed Qasim / Justice Rajee / Tammy Carpenter), titles 'Director, Zone
    1'..'Director, Zone 7' with 'Director, Zone 6 (Chair)' for Rajee and 'Director, Zone 3 (Vice Chair)'
    for Potter; Hillsboro ext_id -4100024..-4100030 for Positions 1-7 (Yessica Hardin Mercado / Mark
    Watson / Nancy Thomas / See Eun Kim / Ivette Pantoja / Katie Rhyne / Patrick Maguire), titles
    'Director, Position 1'..'Director, Position 7' with 'Director, Position 5 (Chair)' for Pantoja and
    'Director, Position 4 (Vice Chair)' for Kim. Every politician: party=NULL, is_active=true,
    is_appointed=false, is_vacant=false, is_incumbent=true, ON CONFLICT (external_id) DO NOTHING. Every
    office: representing_state='OR' uppercase, is_appointed_position=false, is_vacant=false,
    role_canonical=NULL, chamber_id via the correct government's chamber subquery, CROSS JOIN the SCHOOL
    district WHERE d.geo_id=<the district's geo_id> AND d.district_type='SCHOOL' AND d.state='or', guarded
    by NOT EXISTS on (district_id, politician_id). If 1196 still writes an office_id back-fill UPDATE,
    include the equivalent for both ext_id ranges; otherwise omit it.
    POST-VERIFY DO block (extend 1107's to the 2-district dual-gate form from RESEARCH Pattern 2): assert
    v_bsd_gov=1, v_hsd_gov=1; v_bsd_off=7 (offices JOIN districts WHERE geo_id='4101920' AND
    district_type='SCHOOL' AND state='or'), v_hsd_off=7 (geo_id='4100023'); v_split=0 (geofence_boundaries
    G5420 rows for both geo_ids that lack a matching SCHOOL district) — RAISE EXCEPTION on any mismatch,
    RAISE NOTICE on success. Then COMMIT; then INSERT INTO supabase_migrations.schema_migrations
    (version, name) VALUES ('1203','or_westmetro_school_boards_wave1') ON CONFLICT (version) DO NOTHING.
    No fenced code in this action — 1107 + RESEARCH Pattern 2 are the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator (Task 2) applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1203_or_westmetro_school_boards_wave1.sql` and confirms the post-verify NOTICE + SQL gates</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/1203_or_westmetro_school_boards_wave1.sql, wrapped in BEGIN;...COMMIT; with the ledger INSERT after COMMIT
    - Contains exactly 2 `INSERT INTO essentials.governments` guarded by WHERE NOT EXISTS on the verbatim names; both type='LOCAL', state='OR'
    - Contains 2 `INSERT INTO essentials.chambers` with names 'School Board' and 'Board of Directors' (grep finds neither 'Board of Education'), official_count=7 each, no slug/path column in the INSERT list
    - Contains 2 SCHOOL-district INSERTs with state='or' lowercase, mtfcc='G5420', geo_id '4101920'/'4100023'
    - Contains 14 `WITH ins_p AS` politician+office CTEs; grep confirms ext_ids -4101921..-4101927 and -4100024..-4100030 all present; all politician INSERTs have party NULL and ON CONFLICT (external_id) DO NOTHING
    - Office titles present: 'Director, Zone 6 (Chair)', 'Director, Zone 3 (Vice Chair)', 'Director, Position 5 (Chair)', 'Director, Position 4 (Vice Chair)', and the 10 plain 'Director, Zone N'/'Director, Position N' titles; grep confirms exactly 14 office INSERTs (no separate Chair/Vice-Chair rows)
    - Post-verify DO block asserts v_bsd_gov=1, v_hsd_gov=1, v_bsd_off=7, v_hsd_off=7, v_split=0 with RAISE EXCEPTION on mismatch
    - Ledger INSERT uses version '1203', name 'or_westmetro_school_boards_wave1'
    - File saved UTF-8 (grep finds 'Vân Truong' and 'Karen Pérez' intact); literal strings slug / photo_origin_url absent from comments
  </acceptance_criteria>
  <done>Migration 1203 authored with 2 govs, 2 verbatim chambers, 2 SCHOOL districts, 14 director CTEs, dual-district post-verify DO block, and ledger registration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies 1203, confirms post-verify + SQL gates + address routing</name>
  <what-built>Structural migration 1203. The orchestrator applies it, reads the post-verify NOTICE, runs the office-count/casing/section-split SQL gates, and re-runs the Phase-174 routing smoke test.</what-built>
  <how-to-verify>
    A. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1203_or_westmetro_school_boards_wave1.sql`.
       Confirm the run ends with the post-verify RAISE NOTICE (BSD gov=1/off=7, HSD gov=1/off=7,
       split_orphans=0) and NO RAISE EXCEPTION (any exception rolls back the whole migration — fix the
       migration and re-apply). Confirm the ledger row registered: `SELECT version, name FROM
       supabase_migrations.schema_migrations WHERE version='1203'` returns one row.
    B. SQL gates (independent of the in-migration DO block):
       - Beaverton offices = 7: `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON
         d.id=o.district_id WHERE d.geo_id='4101920' AND d.district_type='SCHOOL' AND d.state='or'` → 7.
       - Hillsboro offices = 7: same with geo_id='4100023' → 7.
       - Casing: `SELECT DISTINCT state FROM essentials.districts WHERE geo_id IN ('4101920','4100023') AND
         district_type='SCHOOL'` → only 'or'.
       - Section-split = 0 for both G5420 geo_ids (the DO-block gate re-run as a standalone SELECT).
       - Chamber names: `SELECT name FROM essentials.chambers c JOIN essentials.governments g ON
         g.id=c.government_id WHERE g.name LIKE '%School District%Oregon%'` → 'School Board' + 'Board of
         Directors' (NOT 'Board of Education').
    C. Address routing (reuse Phase 174's existing smoke test, no change needed):
       `cd C:/EV-Accounts/backend && npx tsx scripts/smoke-or-westmetro-school.ts` — confirm it still
       passes for both geo_ids (a Beaverton city-hall coordinate routes to 4101920, a Hillsboro one to
       4100023). This asserts the geofence→district linkage the new offices ride on.
    D. Record the 14 minted politician UUIDs by external_id for plan 03's headshot migration:
       `SELECT external_id, id FROM essentials.politicians WHERE external_id BETWEEN -4101927 AND -4101921
       OR external_id BETWEEN -4100030 AND -4100024 ORDER BY external_id`.
  </how-to-verify>
  <acceptance_criteria>
    - 1203 applied cleanly; post-verify NOTICE shows BSD gov=1/off=7, HSD gov=1/off=7, split=0; no exception
    - Ledger row version='1203' present
    - Both office-count gates = 7; casing gate returns only 'or'; section-split = 0 for both geo_ids
    - Chamber-name gate returns 'School Board' + 'Board of Directors' (no 'Board of Education')
    - smoke-or-westmetro-school.ts passes for both geo_ids (no routing regression)
    - The external_id → politician UUID map for all 14 directors is recorded in the SUMMARY for plan 03
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded post-verify + gate results + the 14 UUIDs, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Structural migration (INSERT-only DDL) applied by the orchestrator against production Supabase inside a transaction with a rollback-on-mismatch gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-06 | Tampering (data integrity) | migration re-run double-inserts govs/chambers/districts/offices | mitigate | Every INSERT uses WHERE NOT EXISTS / ON CONFLICT (external_id) DO NOTHING idempotency guards |
| T-183-07 | Tampering | uppercase d.state='OR' in office/district WHERE clauses silently links 0 offices | mitigate | Header CRITICAL note + Wave-0 Probe F confirmed 'or' lowercase; post-verify DO block RAISES if offices <> 7 (catches the silent-zero) |
| T-183-08 | Tampering | seeding onto the wrong geo_id would attach directors to a non-existent/other district | mitigate | Post-verify section-split gate asserts each G5420 geofence has a matching SCHOOL district; geo_ids confirmed in Wave-0 Probe A |
| T-183-09 | Repudiation | applying an unregistered structural migration loses ledger traceability | mitigate | Explicit INSERT INTO supabase_migrations.schema_migrations version '1203' after COMMIT |
| T-183-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — the smoke test uses the pre-existing tsx/node toolchain; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Post-verify DO block passes (2 govs, 7+7 offices, 0 section-split) — rolls back on any mismatch.
- Independent SQL gates confirm office counts, lowercase 'or' casing, verbatim chamber names.
- smoke-or-westmetro-school.ts confirms both districts still route.
- 0 compass stance rows remain the success state (no stance work in this plan).
</verification>

<success_criteria>
- Both governments + verbatim chambers + single-shared SCHOOL districts + 14 director offices seeded and
  applied; post-verify and all independent gates green; address routing confirmed for both geo_ids; the
  14 external_id→UUID map recorded for plan 03.
</success_criteria>

<output>
Create `.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-02-SUMMARY.md` when
done, recording: the applied migration number (1203), the post-verify + gate results, the verbatim
chamber names used, the confirmed office counts (7/7), the routing-smoke result, and the full external_id
→ politician UUID map for all 14 directors (needed to author the headshot migration in plan 03).
</output>
