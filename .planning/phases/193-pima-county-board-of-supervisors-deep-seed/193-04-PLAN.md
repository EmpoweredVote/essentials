---
phase: 193-pima-county-board-of-supervisors-deep-seed
plan: 04
type: execute
wave: 4
depends_on: ["193-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1290_pima_supervisor_1_stances.sql
  - C:/EV-Accounts/backend/migrations/1291_pima_supervisor_2_stances.sql
  - C:/EV-Accounts/backend/migrations/1292_pima_supervisor_3_stances.sql
  - C:/EV-Accounts/backend/migrations/1293_pima_supervisor_4_stances.sql
  - C:/EV-Accounts/backend/migrations/1294_pima_supervisor_5_stances.sql
autonomous: true
requirements: [PIMA-01]
must_haves:
  truths:
    - "Evidence-only compass stances are seeded for the 5 supervisors against the 36 non-judicial live topics (never the 8 judicial-* topics)"
    - "Every stance row is 100% cited (has a non-empty sources array); there are ZERO neutral-default rows — a topic with no evidence gets NO row at all (honest blank)"
    - "Stances are researched one supervisor at a time (quota); each supervisor's rows carry evidence-based reasoning"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1290_pima_supervisor_1_stances.sql"
      provides: "Rex Scott (D1, -4007001) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1294_pima_supervisor_5_stances.sql"
      provides: "Andrés Cano (D5, -4007005) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "same (politician_id, topic_id) — every answer has a cited context row"
      pattern: "politician_context"
    - from: "stance rows"
      to: "inform.compass_topics (36 non-judicial)"
      via: "topic_id references a non-judicial live topic UUID"
      pattern: "topic_id"
---

<objective>
Seed evidence-only compass stances for the 5 Pima County supervisors — the FIRST Arizona
jurisdiction to receive stances, setting the AZ template. One audit-only migration per supervisor
(1290-1294), researched ONE SUPERVISOR AT A TIME (quota), against the 36 NON-JUDICIAL live compass
topics (44 live topics minus the 8 `judicial-*` keys, which have no bearing on a county supervisor —
Pitfall 6). Every stance is 100% cited with evidence-based reasoning; a topic with no documented
record gets NO row at all (honest blank), never a neutral default (D-03 / global
`feedback_stance_no_default_value`). Discrete 1-5 "chairs", never a polarity scale.

Purpose: Satisfies ROADMAP success criterion #3 (evidence-only stances, 100% cited, no defaults,
honest blanks).
Output: 5 audit-only migrations `1290..1294_pima_supervisor_N_stances.sql` (unregistered), applied
so the supervisors' compasses populate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-02-SUMMARY.md

<execution_note>
Depends on Plan 02: the 5 politician UUIDs from 193-02-SUMMARY.md are REQUIRED (stance rows key on
the supervisor UUID). Executor authors the 5 `.sql` files (researching one supervisor at a time per
the `feedback_stance_research_one_at_a_time` quota rule — do NOT fan out parallel stance research).
The ORCHESTRATOR applies each via `psql -f` and asserts. Commit via `git -C "C:/EV-Accounts"`. These
are AUDIT-ONLY migrations — NOT registered in the ledger. Independent of Plan 03 (headshots) — both
depend only on Plan 02, so they may run in either order; kept in separate waves to honor the
one-supervisor-at-a-time quota and avoid a single overloaded context.
</execution_note>

<interfaces>
- inform.politician_answers (politician_id, topic_id, value): value = discrete 1.0-5.0 chair;
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value.
- inform.politician_context (politician_id, topic_id, reasoning, sources): reasoning = cited
  evidence prose ($$-quoted); sources = ARRAY[...] of source URLs (NEVER empty); ON CONFLICT
  (politician_id, topic_id) DO UPDATE.
- inform.compass_topics: 44 live topics; office_scope is empty/NULL (DB does NOT enforce judicial
  exclusion — it is a manual convention). The 8 EXCLUDED judicial keys: judicial-access-to-justice,
  judicial-bail-pretrial, judicial-criminal-justice, judicial-government-deference,
  judicial-interpretation, judicial-police-accountability, judicial-prosecution-priorities,
  judicial-transparency. Research against the remaining 36 only.
- Evidence sources for Pima supervisors (RESEARCH): Pima County Legistar, AZ Luminaria, AZPM, Tucson
  Sentinel, Arizona Daily Star / tucson.com.
- ext_ids: Scott -4007001, Heinz -4007002, Allen -4007003, Christy -4007004, Cano -4007005.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 per-supervisor evidence-only stance migrations (one supervisor at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1290_pima_supervisor_1_stances.sql, C:/EV-Accounts/backend/migrations/1291_pima_supervisor_2_stances.sql, C:/EV-Accounts/backend/migrations/1292_pima_supervisor_3_stances.sql, C:/EV-Accounts/backend/migrations/1293_pima_supervisor_4_stances.sql, C:/EV-Accounts/backend/migrations/1294_pima_supervisor_5_stances.sql</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md (lines 284-313 — the per-supervisor stance migration pattern: topic-UUID reference comment block, BEGIN/COMMIT, the two-table INSERT (politician_answers + politician_context) per topic, the 36/36 judicial exclusion, honest-blank omission, one file per supervisor)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Common Pitfall 6 — the 8 judicial keys to exclude; § Sources — the Pima evidence-source list; § user_constraints D-03)
    - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql (analog — read header + first stance block: topic-UUID table, BEGIN/COMMIT wrapper, two-table INSERT/ON CONFLICT shape, sources-array pattern)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1289 after Plan 03, or 1288 if Plan 03 not yet applied — either
    way name these disk-MAX+1 sequentially). Author 5 AUDIT-ONLY migrations, one per supervisor,
    numbered sequentially (provisionally 1290 Scott / 1291 Heinz / 1292 Allen / 1293 Christy /
    1294 Cano). NONE registered in the ledger.

    Research ONE SUPERVISOR AT A TIME (quota — never parallel). For each supervisor, mine the evidence
    sources (Pima County Legistar votes/motions, AZ Luminaria, AZPM, Tucson Sentinel, Arizona Daily
    Star/tucson.com) for documented positions across the 36 non-judicial live topics. For each topic
    WHERE clear evidence exists, emit BOTH: an INSERT into inform.politician_answers (politician_id =
    the supervisor UUID from the Plan 02 manifest, topic_id = the non-judicial topic UUID, value = a
    discrete 1-5 chair) AND an INSERT into inform.politician_context (same keys, reasoning =
    $$-quoted evidence prose naming the vote/statement/date, sources = ARRAY of the actual source
    URLs — NEVER an empty array). Both guarded ON CONFLICT (politician_id, topic_id) DO UPDATE.

    HARD RULES: (1) NEVER emit a row for a topic with no evidence — omit it entirely (honest blank),
    never a neutral/default value (D-03 / feedback_stance_no_default_value). (2) NEVER touch any of
    the 8 judicial-* topics — keep them in the reference comment block but write NO INSERT for them
    (Pitfall 6). (3) Every politician_answers row MUST have a matching politician_context row with a
    non-empty sources array (100% cited). (4) Values are discrete chairs (1-5), not a polarity scale
    (feedback_compass_chairs_not_polarity). (5) Attribute a vote only to supervisors who were seated
    at the time (no pre-tenure attribution).

    Copy the 44-topic UUID reference comment block from the analog into each file's header; the
    INSERTs skip the 8 judicial rows. Wrap each file in BEGIN/COMMIT. Do NOT register in the ledger.
  </action>
  <verify>
    <automated>for n in 1290 1291 1292 1293 1294; do f="C:/EV-Accounts/backend/migrations/${n}_pima_supervisor_$((n-1289))_stances.sql"; test -f "$f" && grep -qF "politician_answers" "$f" && grep -qF "politician_context" "$f" && ! grep -qiE "INSERT INTO inform.politician_answers[^;]*judicial-" "$f" && ! grep -qF "schema_migrations" "$f" || { echo "FAIL $f"; exit 1; }; done; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist (disk-MAX+1 sequential names, provisionally 1290-1294).
    - Each file contains both `inform.politician_answers` and `inform.politician_context` INSERTs.
    - NO INSERT references any `judicial-*` topic (the 8 judicial keys appear ONLY in the reference comment, never in an INSERT).
    - Every politician_answers INSERT has a matching politician_context INSERT with a non-empty `ARRAY[...]` sources value (no empty arrays).
    - NO neutral-default rows — every emitted row is evidence-backed; topics without evidence are absent (no row).
    - NONE of the 5 files registers in `schema_migrations` (audit-only).
    - Each file wrapped in BEGIN/COMMIT; each keyed to the correct supervisor UUID from the Plan 02 manifest.
  </acceptance_criteria>
  <done>5 audit-only stance migrations authored, one per supervisor, evidence-only against 36 non-judicial topics, 100% cited, honest blanks, discrete chairs, no defaults — unregistered.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the 5 stance migrations + assert evidence-only integrity (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator applies existing migrations + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md (Per-Task Verification Map — stances row: 100% cited, no default rows, honest blanks)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Common Pitfall 6 — 36 not 44; a "44 per politician" check would wrongly flag complete coverage)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). Apply the 5 migrations in order via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1290_pima_supervisor_1_stances.sql`
    (etc. through 1294). Then assert via `psql -tAc`, recording in 193-04-SUMMARY.md: (a) every
    politician_answers row for the 5 Pima UUIDs has a matching politician_context row (0 orphans);
    (b) 0 politician_context rows with a NULL or empty sources array (100% cited); (c) 0
    politician_answers rows on any judicial-* topic for the 5 supervisors; (d) all values are in
    [1.0, 5.0]; (e) per-supervisor stance counts (informational — expected to vary; honest blanks are
    fine, do NOT assert a fixed count). Any integrity failure (a-d) blocks the phase. Commit the 5
    migrations via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id BETWEEN -4007005 AND -4007001 AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id)) AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc JOIN essentials.politicians p ON p.id=pc.politician_id WHERE p.external_id BETWEEN -4007005 AND -4007001 AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL)) AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN inform.compass_topics t ON t.id=pa.topic_id WHERE p.external_id BETWEEN -4007005 AND -4007001 AND t.topic_key LIKE 'judicial-%')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 migrations apply cleanly.
    - 0 politician_answers rows without a matching politician_context row (every answer cited).
    - 0 politician_context rows with a NULL/empty sources array (100% cited).
    - 0 stance rows on any `judicial-*` topic for the 5 supervisors.
    - All stance values in [1.0, 5.0].
    - The combined boolean SELECT returns `t`.
    - Per-supervisor counts recorded in the SUMMARY (informational; honest blanks expected, no fixed count asserted).
    - 5 migrations committed to `C:/EV-Accounts`; none registered in the ledger.
  </acceptance_criteria>
  <done>Evidence-only stances live for the 5 supervisors: 100% cited, no defaults, no judicial rows, honest blanks — audit migrations applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stance migration files → production DB | Hand-authored evidence-only SQL crosses into live `inform` schema via `psql -f` |
| research sources → stance values | External news/Legistar evidence maps to a 1-5 chair; unsourced/mis-scoped values corrupt the compass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-CITE | Tampering | uncited / default stance rows | mitigate | Every answer requires a matching context row with a non-empty sources array; Task 2 asserts 0 orphans + 0 empty-sources; no-evidence topics omitted (honest blank), never defaulted |
| T-193-JUD | Tampering | judicial topics on a non-judicial office | mitigate | 8 judicial-* keys excluded from all INSERTs; Task 2 asserts 0 judicial rows for the 5 supervisors |
| T-193-TENURE | Spoofing | pre-tenure vote attribution | mitigate | Attribute a vote only to supervisors seated at the time; Cano (appointed Apr 2025) not credited with pre-appointment votes |
| T-193-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `.env`; applied by the inline orchestrator |
| T-193-SC | Tampering | package installs | accept | No new installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Every stance row 100% cited (matching context row + non-empty sources); 0 orphans.
- 0 judicial-* rows; 0 neutral-default rows; values in [1.0, 5.0].
- Researched one supervisor at a time; 5 audit migrations applied and unregistered.
</verification>

<success_criteria>
ROADMAP #3 (evidence-only compass stances, 100% cited, no defaults, honest blanks) is TRUE in
production for all 5 supervisors against the 36 non-judicial topics.
</success_criteria>

<output>
Create `.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-04-SUMMARY.md` when done —
include per-supervisor stance counts, notable honest blanks, and the evidence sources used.
</output>
