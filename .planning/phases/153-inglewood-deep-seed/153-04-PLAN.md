---
phase: 153-inglewood-deep-seed
plan: 04
type: execute
wave: 4
depends_on: ["153-03"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1021_inglewood_butts_stances.sql
  - C:/EV-Accounts/backend/migrations/1022_inglewood_padilla_stances.sql
  - C:/EV-Accounts/backend/migrations/1023_inglewood_morales_stances.sql
  - C:/EV-Accounts/backend/migrations/1024_inglewood_faulk_stances.sql
  - C:/EV-Accounts/backend/migrations/1025_inglewood_gray_stances.sql
autonomous: false
requirements: [INGL-01]

must_haves:
  truths:
    - "Each current official with a findable public record has at least one evidence-backed compass stance"
    - "Every stance answer has a paired context row with reasoning and real source URLs (100% citation)"
    - "No defaulted/neutral values; honest blank spokes where no evidence exists"
    - "No judicial-* topics and no retired topic IDs appear for any official"
    - "Stance values are CHAIRS (the chair the evidence matches), never a polarity axis"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1021_inglewood_butts_stances.sql"
      provides: "Mayor Butts evidence-only stances (richest record)"
      contains: "inform.politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1025_inglewood_gray_stances.sql"
      provides: "Gloria Gray stances (thin record / honest blanks acceptable)"
      contains: "inform.politician_answers"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired insert per (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\)"
    - from: "topic_id"
      to: "inform.compass_stances (live, non-judicial)"
      via: "queried at apply time, never hardcoded retired IDs"
      pattern: "compass_stances WHERE slug"
---

<objective>
Wave 4 of the Inglewood deep-seed: evidence-only compass stances for the 5 current officials, ONE
AUDIT-ONLY migration per official (`1021`-`1025`, raw SQL, NOT registered; ledger stays 1019).

CHAIRS model (value = the chair the evidence matches, never a polarity axis). 100% citation (paired
`inform.politician_answers` + `inform.politician_context` with reasoning + real source URLs). No
defaulted/neutral values — honest blank spokes where no evidence exists. ALL non-judicial live compass
topics researched; NO judicial-* topics (Inglewood is council-manager with an appointed City Attorney);
topic IDs queried live at apply time (never hardcode retired IDs). ONE research agent at a time
(rate-limit rule). Research order richest -> thinnest: Butts -> Padilla -> Morales Jr. -> Faulk -> Gray.

Unanimous-vote trap: the Inglewood council voted ~580/583 items unanimously — a unanimous vote anchors
all members at the same chair, so confirm the individual's name/position for genuine differentiation.

Purpose: trustworthy, fully-cited stance coverage with honest gaps.
Output: 5 stance migrations committed to EV-Accounts. This wave ENDS WITH A BLOCKING HUMAN-VERIFY CHECKPOINT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-inglewood-deep-seed/153-CONTEXT.md
@.planning/phases/153-inglewood-deep-seed/153-RESEARCH.md
@.planning/phases/153-inglewood-deep-seed/153-PATTERNS.md
@.planning/phases/153-inglewood-deep-seed/153-VALIDATION.md
@.planning/phases/153-inglewood-deep-seed/153-02-SUMMARY.md

<reference_data>
Current officials + pol UUIDs (resolve Padilla from Wave-2 summary):
  Mayor Butts   pol f5775ca1  ext_id -200740  (since 2011 — RICHEST: RSO permanence Apr 2023, crime/PLAs/housing)
  Padilla D2    pol/ext_id from Wave 2 (since 2013 — PLAs/union, immigration statement)
  Eloy D3       pol 6ed19c10  ext_id 666263    (since 2003 — Finance/Housing/Parking authorities, immigration statement)
  Faulk D4      pol 729bc539  ext_id 666264    (re-elected Nov 2024 — jobs/housing/PLAs, immigration statement)
  Gray D1       pol 7a04bf87  ext_id 666261    (seated 2023, health-excused since Dec 2025 — THIN, many honest blanks expected)
Topic IDs: query live `SELECT id, slug FROM inform.compass_stances WHERE slug=...` at apply time. Reference live UUIDs
  (confirm at apply): public-safety-approach e9ebefcd..., economic-development eb3d1247..., homelessness-response 6fbf39ae...,
  housing 669cac97..., taxes f7e5678d..., growth-and-development fb25c1ac... — NEVER use the 6 retired IDs (project_compass_live_topic_ids).
Evidence landscape: RESEARCH §CQ-5 (per-official source assessment) + §Common Pitfalls Pitfall 2 (unanimous != individual) + Pitfall 5 (Gray context).
NO judicial-* topics for anyone (council-manager city, appointed City Attorney).
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stance pre-flight — live topic IDs + per-official pol UUID confirmation</name>
  <files>(read-only; no file written this task)</files>
  <read_first>
    - .planning/phases/153-inglewood-deep-seed/153-RESEARCH.md §CQ-5 (per-official evidence) + §Common Pitfalls Pitfall 2 + Pitfall 5
    - .planning/phases/153-inglewood-deep-seed/153-PATTERNS.md §Pattern 21-23 (stance file header, paired answers+context block, thin-record honest-blanks, unanimous-vote guard) + §Shared Patterns (audit-only transaction shape)
    - .planning/phases/153-inglewood-deep-seed/153-02-SUMMARY.md (Padilla pol UUID + ext_id)
    - memory: project_compass_live_topic_ids (the 6 RETIRED IDs that must NOT be used), feedback_compass_chairs_not_polarity, feedback_stance_no_default_value, feedback_stance_research_all_topics, feedback_stance_research_one_at_a_time
  </read_first>
  <action>
    Via `mcp__supabase-local__execute_sql`:
    (a) Pull the live non-judicial topic set: `SELECT id, slug, label FROM inform.compass_stances WHERE slug NOT LIKE 'judicial-%' ORDER BY slug`. Record each slug -> live UUID. Confirm NONE of the 6 retired IDs (per project_compass_live_topic_ids) appear in what you will write.
    (b) Confirm each official's pol UUID + ext_id (Butts f5775ca1/-200740, Eloy 6ed19c10/666263, Faulk 729bc539/666264, Gray 7a04bf87/666261, Padilla from Wave-2 summary). Confirm all 5 currently have 0 stance rows: `SELECT politician_id, COUNT(*) FROM inform.politician_answers WHERE politician_id IN (...) GROUP BY politician_id`.
    Produce a topic-id map + per-official identifier block for use by the research+author tasks.
  </action>
  <verify>
    <automated>MISSING — read-only MCP queries; capture topic map in SUMMARY</automated>
  </verify>
  <acceptance_criteria>
    - live topic map captured: `SELECT COUNT(*) FROM inform.compass_stances WHERE slug LIKE 'judicial-%'` rows are EXCLUDED from the map; the map contains only non-judicial slugs with their live UUIDs
    - all 5 officials confirmed at 0 existing stances before authoring (greenfield)
    - Padilla pol UUID + ext_id resolved from Wave-2 summary
  </acceptance_criteria>
  <done>Live non-judicial topic-id map recorded (no retired IDs); 5 officials confirmed greenfield; all pol UUIDs/ext_ids resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Research + author + apply stances — ONE official at a time (Butts, Padilla, Morales, Faulk, Gray)</name>
  <files>C:/EV-Accounts/backend/migrations/1021_inglewood_butts_stances.sql, 1022_inglewood_padilla_stances.sql, 1023_inglewood_morales_stances.sql, 1024_inglewood_faulk_stances.sql, 1025_inglewood_gray_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1013_tony_wu_stances.sql (lines 1-71 — RICH record: audit-only header, per-topic paired answers+context block with $$ reasoning + ARRAY[...]::text[] sources, ON CONFLICT DO UPDATE on both)
    - C:/EV-Accounts/backend/migrations/1017_brian_gutierrez_stances.sql (lines 1-20 — THIN record: 3 well-cited stances + honest blanks, bulk VALUES syntax, NOT padded)
    - .planning/phases/153-inglewood-deep-seed/153-PATTERNS.md §Pattern 21-23 + §Shared Patterns
    - .planning/phases/153-inglewood-deep-seed/153-RESEARCH.md §CQ-5 (per-official evidence + cited sources) + Pitfall 2 (unanimous-vote trap) + Pitfall 5 (Gray)
    - Task 1 topic-id map + per-official identifier block
  </read_first>
  <action>
    Process the 5 officials STRICTLY ONE AT A TIME in order Butts -> Padilla -> Morales Jr. -> Faulk -> Gray (rate-limit rule; never run stance research agents in parallel). For EACH official:
    (1) Run a single stance-research pass against ALL non-judicial topics in the Task-1 map (not just local). Use only real public-record evidence (city minutes, official statements, campaign platforms with real URLs). Apply the CHAIRS model: pick the chair the evidence matches; if no evidence -> BLANK (omit the topic entirely; never default to neutral/likely).
    (2) Apply the unanimous-vote guard (Pitfall 2): a ~580/583-unanimous vote anchors all members identically — only place an individual in a chair when the evidence names that individual or otherwise distinguishes them; do not copy the same chair across all 5 on every topic.
    (3) Author the per-official AUDIT-ONLY migration (`BEGIN; ... COMMIT;`, NO schema_migrations registration; header notes ledger stays 1019, the CHAIRS model, 100% citation, honest blanks, NO judicial topics). Use the rich-record block syntax (1013) for Butts and the thin-record bulk syntax (1017) for sparse officials. Each stance = paired INSERT into `inform.politician_answers` (value) + `inform.politician_context` (reasoning in `$$...$$`, sources `ARRAY['url1','url2']::text[]` — never an empty array; omit the topic if no source), both with `ON CONFLICT (politician_id, topic_id) DO UPDATE`. Use the LIVE topic UUIDs from Task 1.
    (4) APPLY that official's migration live via `mcp__supabase-local__execute_sql`, then move to the next official. Gray is expected to be thin (health absence + newer member) — honest blanks are the correct outcome; note her context per Pitfall 5.
    After all 5: commit all files: `git -C "C:/EV-Accounts" add backend/migrations/1021_*.sql backend/migrations/1022_*.sql backend/migrations/1023_*.sql backend/migrations/1024_*.sql backend/migrations/1025_*.sql && git -C "C:/EV-Accounts" commit -m "feat(153): Inglewood evidence-only compass stances (5 officials, CHAIRS, 100% cited)"`.
  </action>
  <verify>
    <automated>MISSING — apply per official via MCP then run the acceptance assertion queries via mcp__supabase-local__execute_sql</automated>
  </verify>
  <acceptance_criteria>
    - every official with a findable record has >=1 stance: `SELECT politician_id, COUNT(*) FROM inform.politician_answers WHERE politician_id IN ('f5775ca1...','<padilla>','6ed19c10...','729bc539...','7a04bf87...') GROUP BY politician_id` (Gray may be the only one near 0 if genuinely no evidence — documented honest blank)
    - 100% citation: `SELECT COUNT(*) FROM inform.politician_answers a WHERE a.politician_id IN (...) AND NOT EXISTS (SELECT 1 FROM inform.politician_context c WHERE c.politician_id=a.politician_id AND c.topic_id=a.topic_id)` -> 0 (every answer paired with context)
    - no retired topic IDs: 0 rows among these officials reference any of the 6 retired IDs (project_compass_live_topic_ids)
    - no judicial topics: `SELECT COUNT(*) FROM inform.politician_answers a JOIN inform.compass_stances s ON s.id=a.topic_id WHERE a.politician_id IN (...) AND s.slug LIKE 'judicial-%'` -> 0
    - no defaulted/neutral padding: every present answer has a non-empty reasoning + >=1 source URL in its context row
    - ledger UNCHANGED: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` -> 1019; all 5 files committed to EV-Accounts
  </acceptance_criteria>
  <done>All 5 officials researched ONE at a time and applied; each stance fully cited (paired answer+context, real URLs); CHAIRS values, no defaults, honest blanks; no judicial/retired topics; 5 audit-only migrations committed; ledger 1019.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Evidence-only compass stances for the 5 current Inglewood officials (Butts richest; Gray thinnest with honest blanks), applied as 5 audit-only migrations. CHAIRS model, 100% citation (paired answers+context with real source URLs), no judicial topics, no defaulted values.</what-built>
  <how-to-verify>
    1. Visit https://essentials.empowered.vote/results?browse_geo_id=0636546&browse_mtfcc=G4110 and open each official's compass.
    2. Spot-check several stances per official: each has a real reasoning + a clickable/valid source URL; the value matches the chair the evidence describes (CHAIRS, not a polarity slider).
    3. Confirm NO neutral/defaulted spokes — blank spokes are honest gaps, not manufactured values.
    4. Confirm no judicial-* spokes appear (Inglewood is council-manager).
    5. Confirm Gray's thin record reads as honest blanks (health-excused, newer member) and was not padded.
    6. Confirm the unanimous-vote topics (e.g. RSO/rent) are not blindly identical across all 5 without individualized evidence.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 153, or list stances that need re-sourcing / re-chairing / removal.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web sources (untrusted) -> stance reasoning + values -> production DB | External evidence drives stance placement; surface is citation integrity / fabrication, not injection |
| executor SQL -> production Supabase | Audit-only static SQL inserting stance rows |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-13 | Tampering | Retired/wrong topic ID written | mitigate | Query live `inform.compass_stances` at apply time; never hardcode retired IDs; assert 0 retired-ID rows |
| T-153-14 | Repudiation | Uncited / defaulted stance (destroys trust) | mitigate | Every answer paired with a context row (reasoning + >=1 real URL); no neutral default; honest blank when no evidence; BLOCKING human-verify |
| T-153-15 | Integrity | Unanimous-vote trap (all 5 scored identically) | mitigate | Pitfall-2 guard: individualized evidence required before placing a member in a chair |
| T-153-16 | Integrity | Judicial topics on a council-manager city | mitigate | No judicial-* topics authored; assert 0 judicial rows for these officials |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Each official with a findable record has >=1 fully-cited stance; Gray's thin record honest-blanked.
- 100% answer/context pairing; 0 retired-topic rows; 0 judicial rows; no defaulted values — confirmed at the blocking human-verify checkpoint.
- schema_migrations MAX unchanged (1019); 5 stance files committed.
</verification>

<success_criteria>
- ROADMAP success criterion 4: evidence-only compass stances for officials with a findable public record; 100% citation; honest blank spokes elsewhere.
- ROADMAP success criterion 5 (complete with Wave 3): browse view renders roster + photos + stances; no duplicate/stale rows.
- INGL-01 fully satisfied across all 4 waves.
</success_criteria>

<output>
Create `.planning/phases/153-inglewood-deep-seed/153-04-SUMMARY.md` when done (per-official stance count + topics + values, source URLs, honest-blank notes for Gray, and checkpoint approval).
</output>
