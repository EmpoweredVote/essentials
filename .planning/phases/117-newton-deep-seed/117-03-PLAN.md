---
phase: 117-newton-deep-seed
plan: 03
type: execute
wave: 3
depends_on:
  - 117-01
  - 117-02
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-newton-headshots.py
  - C:/EV-Accounts/backend/migrations/580_newton_headshots.sql
autonomous: true
requirements:
  - NEWTON-02

must_haves:
  truths:
    - "All successfully-uploaded Newton officials have a type='default' row in essentials.politician_images"
    - "Every uploaded headshot is 600x750 pixels (4:5 ratio) processed Lanczos q90"
    - "Headshots are stored in the 'politician_photos' Supabase Storage bucket"
    - "Gap officials (no online source found) are documented in migration 580 comments — not blocking"
    - "No official has type='headshot' in politician_images (must be type='default')"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-newton-headshots.py"
      provides: "Python script — download, crop 4:5, resize 600x750 Lanczos q90, upload to politician_photos"
      contains: "politician_photos"
    - path: "C:/EV-Accounts/backend/migrations/580_newton_headshots.sql"
      provides: "politician_images INSERT rows for all uploaded officials"
      contains: "type = 'default'"
    - path: "essentials.politician_images (DB)"
      provides: "Headshot rows for uploaded Newton officials"
      contains: "type='default', politician_id IN (Newton external_id range)"
  key_links:
    - from: "_tmp-newton-headshots.py"
      to: "Supabase Storage bucket 'politician_photos'"
      via: "supabase-py client upload"
      pattern: "bucket='politician_photos'"
    - from: "migration 580"
      to: "essentials.politicians"
      via: "SELECT id FROM essentials.politicians WHERE external_id = -XXXX"
      pattern: "WHERE NOT EXISTS on politician_id"
---

<objective>
Upload headshots for all Newton officials (city + school committee) and record results in essentials.politician_images via migration 580. Produces a Python script (_tmp-newton-headshots.py) that downloads photos, crops to 4:5, resizes to 600x750 Lanczos q90, uploads to the 'politician_photos' bucket, and logs the resulting CDN URLs. Migration 580 inserts politician_images rows for all successful uploads.

Purpose: Satisfies NEWTON-02. newtonma.gov returns HTTP 403 on all programmatic requests — script must use browser User-Agent headers and fall back to alternate sources. Gaps (officials with no findable online photo) are acceptable and must be documented in migration 580 comments.

Output: _tmp-newton-headshots.py (run once, then archive); migration 580_newton_headshots.sql applied to production; best-effort headshot coverage documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-newton-deep-seed/117-RESEARCH.md
@.planning/phases/117-newton-deep-seed/117-01-SUMMARY.md
@.planning/phases/117-newton-deep-seed/117-02-SUMMARY.md

<interfaces>
<!-- Reference migrations the executor MUST read before writing the headshot script and migration 580. -->
<!-- Pattern source 1: MA Tier 2 headshots (migration 356) — nearest prior MA city headshot migration -->
C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql

<!-- Key conventions extracted from 356: -->
<!-- Storage bucket: 'politician_photos' (NOT 'politician-headshots') -->
<!-- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/ -->
<!-- politician_images columns: id, politician_id, url, type='default', photo_license='public_domain' -->
<!-- INSERT pattern: WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = ...) -->
<!-- No BEGIN/COMMIT — autocommit per block -->
<!-- Upload path: {politician_uuid}-headshot.jpg -->
<!-- Photo processing: crop to 4:5 FIRST, then resize 600x750 Lanczos q90 — never stretch directly -->
<!-- GAP documentation: comment in SQL with external_id and reason for gap -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run Newton headshot script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-newton-headshots.py</files>

  <read_first>
    C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql — review header comment for upload path pattern, bucket name, type='default' convention, and gap documentation format.
    .planning/phases/117-newton-deep-seed/117-RESEARCH.md — headshot sources section; newtonma.gov 403 issue; fallback sources list; expected 40-60% coverage.
    .planning/phases/117-newton-deep-seed/117-01-SUMMARY.md — confirms external_id ranges for city officials.
    .planning/phases/117-newton-deep-seed/117-02-SUMMARY.md — confirms external_id ranges for SC members.
  </read_first>

  <action>
Write C:/EV-Accounts/backend/scripts/_tmp-newton-headshots.py as a standalone Python script. Model it on the existing MA headshot scripts in the backend/scripts directory (e.g., the script that produced migration 356).

Script requirements:

1. IMPORTS: requests, PIL (Image, ImageOps), io, os, supabase (create_client), and a logging module for gap tracking. Read Supabase URL + anon key from environment variables (same pattern as prior scripts).

2. ROSTER: Define a list of dicts with all 33 Newton officials:
   - 25 city officials (external_id -2545560001 through -2545560025):
     - Mayor Marc C. Laredo (-2545560001)
     - 16 at-large councillors (-2545560002 through -2545560017, alphabetical by last name as assigned in migration 578)
     - 8 ward councillors (-2545560018 through -2545560025, alphabetical by last name as assigned in migration 578)
   - 8 SC members (external_id -2508610001 through -2508610008, Ward 1-8):
     Arrianna Proia, Linda Swain, Jason Bhardwaj, Tamika Olszewski,
     Ben Schlesinger, Jonathan Greene, Alicia Piedalue, Victor Lee
   Note: Mayor Laredo (external_id=-2545560001) is already in the city list; do NOT list him in the SC list.

3. HEADSHOT SOURCES — probe in order for each official:
   a. Try newtonma.gov with browser User-Agent header: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'
      - Mayor: https://www.newtonma.gov/government/mayor
      - Councillors: https://www.newtonma.gov/government/city-council
      - If response status 200, parse HTML with BeautifulSoup (or regex) to find img tags near the official's name.
   b. School Committee: try newton.k12.ma.us/school-committee (different domain — may not be blocked).
   c. Fallback: Wikimedia Commons search for "{first_name} {last_name} Newton Massachusetts politician".
   d. Fallback: campaign site (laredofornewton.com for Mayor; others based on name search).
   Note: Do NOT raise exceptions on 403/404 — log as GAP and continue.

4. IMAGE PROCESSING for each successfully downloaded image:
   - Open with PIL.Image.open(io.BytesIO(response.content))
   - Crop to 4:5 aspect ratio FIRST (center crop — take max dimensions fitting 4:5):
     target_ratio = 4/5
     current_ratio = width / height
     if current_ratio > target_ratio: new_width = int(height * target_ratio); crop((left, 0, left+new_width, height))
     else: new_height = int(width / target_ratio); crop((0, top, width, top+new_height))
   - THEN resize to 600x750 using Image.LANCZOS resampling.
   - Convert to RGB if needed (handles RGBA PNG source files).
   - Save to BytesIO buffer as JPEG with quality=90.

5. UPLOAD to Supabase Storage:
   - Bucket: 'politician_photos' (NOT 'politician-headshots')
   - First, query supabase.table('politicians').select('id').eq('external_id', eid).single() to get politician UUID.
   - Upload path: f"{politician_uuid}-headshot.jpg"
   - Upload method: supabase.storage.from_('politician_photos').upload(path, image_bytes, file_options={"content-type": "image/jpeg", "upsert": "true"})
   - CDN URL: f"https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{politician_uuid}-headshot.jpg"

6. OUTPUT: After each official, print one line:
   UPLOADED: {external_id} {full_name} -> {cdn_url}
   or
   GAP: {external_id} {full_name} -- {reason: "403 on newtonma.gov; no fallback found" or similar}

7. Final summary: print counts of UPLOADED and GAP.

After the script is written, run it (Python) and let it execute. Capture the printed output — the list of UPLOADED URLs and GAP officials is needed for Task 2.
  </action>

  <verify>
    <automated>
      Run the script and verify it completes without uncaught exceptions. Expected output format:
        UPLOADED: -2545560001 Marc C. Laredo -> https://kxsdzaojfaibhuzmclfq.storage.supabase.co/...
        GAP: -2545560002 Susan Albright -- 403 on newtonma.gov; no alternative found
        ...
        Summary: X uploaded, Y gaps

      Confirm uploaded files exist in Supabase Storage:
        SELECT COUNT(*) FROM storage.objects WHERE bucket_id = 'politician_photos' AND name LIKE '%-headshot.jpg' AND created_at > NOW() - INTERVAL '1 hour';
        -- Expected: at least 1 (ideally 10+; Newton 403 blocker means exact count varies)
    </automated>
  </verify>

  <done>
    Script ran to completion (no Python exceptions). At least one headshot uploaded to 'politician_photos' bucket. Full list of UPLOADED CDN URLs and GAP officials documented in script output — ready for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration 580 — Newton headshots</name>
  <files>C:/EV-Accounts/backend/migrations/580_newton_headshots.sql</files>

  <read_first>
    C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql — exact politician_images INSERT pattern (WHERE NOT EXISTS guard, type='default', photo_license='public_domain', url column).
    Script output from Task 1 — the printed UPLOADED/GAP log is the source of truth for which officials were uploaded and their CDN URLs.
  </read_first>

  <action>
Write C:/EV-Accounts/backend/migrations/580_newton_headshots.sql using the politician_images INSERT pattern from migration 356. Then apply via mcp__supabase-local.

HEADER COMMENT — include:
- Migration 580: Newton city government + school committee headshots (NEWTON-02)
- Storage bucket: politician_photos
- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/
- Total officials attempted: 33 (25 city + 8 SC; Mayor counted once)
- Uploaded count: X (from script output)
- Gap count: Y (from script output)
- newtonma.gov returns HTTP 403 on all programmatic requests — browser UA attempted; gaps expected
- Photo processing: crop 4:5 first, then resize 600x750 Lanczos q90
- type = 'default' (NOT 'headshot') — UI filter uses .find(img => img.type === 'default')
- DOCUMENTED GAPS: list each gap official with external_id, full_name, and reason

For each successfully uploaded official (from Task 1 UPLOADED output):
  INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license)
  SELECT gen_random_uuid(),
         (SELECT id FROM essentials.politicians WHERE external_id = {eid}),
         '{cdn_url}',
         'default', 'public_domain'
  WHERE NOT EXISTS (
    SELECT 1 FROM essentials.politician_images
    WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = {eid})
  );

Group inserts by section (CITY COUNCIL, SCHOOL COMMITTEE) with section divider comments.
Comment out gap officials with -- GAP: {external_id} {name} — {reason}.

POST-VERIFICATION DO block:
  v_img_count = SELECT COUNT(*) FROM essentials.politician_images pi
    JOIN essentials.politicians p ON p.id = pi.politician_id
    WHERE p.external_id BETWEEN -2545560025 AND -2545560001
      OR p.external_id BETWEEN -2508610008 AND -2508610001
    AND pi.type = 'default'
  v_wrong_type = SELECT COUNT(*) FROM essentials.politician_images pi
    JOIN essentials.politicians p ON p.id = pi.politician_id
    WHERE (p.external_id BETWEEN -2545560025 AND -2545560001
        OR p.external_id BETWEEN -2508610008 AND -2508610001)
      AND pi.type != 'default'
  If v_wrong_type > 0: RAISE EXCEPTION 'Newton headshots have wrong type (not default): % rows', v_wrong_type
  RAISE NOTICE 'Migration 580 post-verification PASSED: % headshots inserted (type=default), % gap officials documented', v_img_count, {gap_count}

LEDGER:
  INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('580') ON CONFLICT (version) DO NOTHING
  </action>

  <verify>
    <automated>
      After applying migration 580, run:
        SELECT COUNT(*) FROM essentials.politician_images pi
        JOIN essentials.politicians p ON p.id = pi.politician_id
        WHERE (p.external_id BETWEEN -2545560025 AND -2545560001
            OR p.external_id BETWEEN -2508610008 AND -2508610001)
          AND pi.type = 'default';
        -- Expected: matches UPLOADED count from Task 1 script output

        SELECT COUNT(*) FROM essentials.politician_images pi
        JOIN essentials.politicians p ON p.id = pi.politician_id
        WHERE (p.external_id BETWEEN -2545560025 AND -2545560001
            OR p.external_id BETWEEN -2508610008 AND -2508610001)
          AND pi.type != 'default';
        -- Expected: 0 (no wrong type values)

        SELECT COUNT(*) FROM supabase_migrations.schema_migrations WHERE version = '580';
        -- Expected: 1
    </automated>
  </verify>

  <done>
    Migration 580 applied without errors; all inserted politician_images rows have type='default'; gap officials documented in migration comments; ledger entry present; post-verification NOTICE emitted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python script → external HTTP sources | Fetches images from newtonma.gov, newton.k12.ma.us, and fallback sites |
| Python script → Supabase Storage | Uploads binary image data to politician_photos bucket |
| migration 580 → essentials.politician_images | Inserts CDN URLs as immutable reference data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-06 | Tampering | Image source substitution (wrong person's photo) | mitigate | Script logs source URL per official in UPLOADED output; human spot-check of at least 3 officials during migration 580 review |
| T-117-07 | Information Disclosure | Sensitive image content from unofficial sources | mitigate | Only public-domain official/campaign photos used; photo_license='public_domain' set in migration |
| T-117-08 | Tampering | Wrong bucket name ('politician-headshots' vs 'politician_photos') | mitigate | Script hardcodes bucket='politician_photos'; migration header CRITICAL comment warns against wrong name |
| T-117-09 | Tampering | type='headshot' instead of type='default' | mitigate | Post-verification gate checks v_wrong_type = 0; any non-'default' rows cause RAISE EXCEPTION |
| T-117-SC | Tampering | pip installs during script development | accept | No new pip installs — requests/PIL/supabase-py already present from prior phases |
</threat_model>

<verification>
After migration 580 applies successfully:

1. essentials.politician_images has rows for all uploaded Newton officials with type='default'
2. Zero rows with type != 'default' for Newton external_id ranges
3. All uploaded images are accessible at their CDN URLs (spot-check 2-3 in browser)
4. Image dimensions confirmed 600x750 for at least one uploaded photo (PIL verify or browser dev tools)
5. Gap officials documented in migration 580 comments with reason
6. Supabase Storage bucket 'politician_photos' contains new objects created this session
7. Ledger entry for '580' present in supabase_migrations.schema_migrations
</verification>

<success_criteria>
Migration 580 applied; at least one Newton official has a type='default' headshot in politician_images; zero type!='default' rows; gaps documented; NEWTON-02 closed at best-effort (40-60% coverage expected due to newtonma.gov 403 block). Phase 117 complete — Newton address returns populated LOCAL + SCHOOL sections with headshots.
</success_criteria>

<output>
Create .planning/phases/117-newton-deep-seed/117-03-SUMMARY.md when done. Include in SUMMARY: final UPLOADED count, gap count, list of gap officials with external_ids, and which sources were successfully used (newtonma.gov browser UA / newton.k12.ma.us / fallback).
</output>
