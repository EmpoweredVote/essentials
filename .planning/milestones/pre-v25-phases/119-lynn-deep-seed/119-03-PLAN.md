---
phase: 119-lynn-deep-seed
plan: 03
type: execute
wave: 3
depends_on:
  - 119-01
  - 119-02
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-lynn-headshots.py
  - C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql
autonomous: true
requirements:
  - LYNN-02

must_haves:
  truths:
    - "12 politician_images rows exist with type='default' for Lynn city officials (external_ids -2537490001..-2537490012)"
    - "0 politician_images rows exist for SC members (external_ids -2507110001..-2507110006) — all confirmed gaps"
    - "No politician_images rows with type != 'default' for any Lynn official"
    - "Mayor Nicholson headshot sourced from Wikipedia Commons (no lynnma.gov photo available)"
    - "Natasha Megie-Maddrey headshot downloaded from MegieMaddrey.png (no hyphen in CDN filename)"
    - "All 6 SC members documented as gaps in migration 586 with gap reason comment"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-lynn-headshots.py"
      provides: "Python script: downloads 12 city headshots, crops 4:5, resizes 600x750 Lanczos q90, uploads to politician_photos bucket"
      contains: "_tmp-lynn-headshots"
    - path: "C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql"
      provides: "politician_images INSERT rows for 12 city officials; gap comments for 6 SC members"
      contains: "586_lynn_headshots"
  key_links:
    - from: "_tmp-lynn-headshots.py ROSTER"
      to: "Supabase Storage politician_photos bucket"
      via: "upload_to_storage() function; path pattern {politician_uuid}-headshot.jpg"
      pattern: "BUCKET = 'politician_photos'"
    - from: "migration 586 politician_images INSERT"
      to: "essentials.politicians (external_id subquery)"
      via: "SELECT id FROM essentials.politicians WHERE external_id = N"
      pattern: "type = 'default'"
---

<objective>
Upload headshots for all 12 Lynn city officials and insert politician_images rows via migration 586. Document all 6 School Committee members as confirmed gaps.

Purpose: Satisfies LYNN-02 — 12/12 city officials have headshots at 600×750; SC gaps documented honestly per D-01.
Output: Python script uploads 12 images to politician_photos bucket; migration 586 inserts 12 politician_images rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-lynn-deep-seed/119-RESEARCH.md
@.planning/phases/119-lynn-deep-seed/119-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write _tmp-lynn-headshots.py Python upload script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-lynn-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-somerville-headshots.py (primary analog — accessible CMS pattern; copy all image processing functions verbatim; update only docstring, ROSTER, banner text, and Referer header)
    - .planning/phases/119-lynn-deep-seed/119-PATTERNS.md (complete ROSTER specification with all 18 entries, URL patterns, gap entries, and exact Python excerpts)
    - .planning/phases/119-lynn-deep-seed/119-RESEARCH.md (headshot sources section: CivicLive CDN URLs, Mayor Wikipedia URL, SC gaps, Pitfall 2 MegieMaddrey.png filename)
  </read_first>
  <action>
Create C:/EV-Accounts/backend/scripts/_tmp-lynn-headshots.py by adapting _tmp-somerville-headshots.py with these changes:

COPY VERBATIM (do not modify):
- All imports (os, io, sys, time, requests, PIL)
- .env config parsing block (SUPABASE_URL, SERVICE_KEY, DATABASE_URL, BUCKET, CDN_BASE, TARGET_SIZE, JPEG_QUALITY, RESAMPLE)
- crop_to_4_5() function
- resize_600x750() function
- download_image() function
- upload_to_storage() function
- resolve_politician_id() function
- process_member() function

MODIFY:
- Docstring: update to Phase 119 Lynn, migration 586, CivicLive CDN
- BROWSER_HEADERS Referer: 'https://www.lynnma.gov/' (not somervillema.gov)
- ROSTER: replace Somerville entries with 18 Lynn entries (12 city + 6 SC)

ROSTER — 18 entries in this order (from 119-PATTERNS.md ROSTER specification):

City officials (12 entries) — use CivicLive CDN base URL:
https://cdnsm5-hosted2.civiclive.com/UserFiles/Servers/Server_109726/Image/Council%20Photos/{LastName}.png

Mayor (external_id=-2537490001): source_url='https://upload.wikimedia.org/wikipedia/commons/7/7f/Jared_Nicholson_1.jpg', headers=BROWSER_HEADERS_DEFAULT (not CivicLive Referer; Wikipedia does not require it)
At-large (-2537490002 Field): source_url=CDN+Field.png
At-large (-2537490003 LaPierre): source_url=CDN+LaPierre.png
At-large (-2537490004 McClain): source_url=CDN+McClain.png
At-large (-2537490005 Net): source_url=CDN+Net.png
Ward 1 (-2537490006 Meaney): source_url=CDN+Meaney.png
Ward 2 (-2537490007 Matul): source_url=CDN+Matul.png
Ward 3 (-2537490008 Alinsug): source_url=CDN+Alinsug.png
Ward 4 (-2537490009 Megie-Maddrey): source_url=CDN+MegieMaddrey.png (CRITICAL: no hyphen in CDN filename per Pitfall 2; DB last_name has hyphen but CDN filename does not)
Ward 5 (-2537490010 Paez): source_url=CDN+Paez.png
Ward 6 (-2537490011 Hogan): source_url=CDN+Hogan.png
Ward 7 (-2537490012 Avery): source_url=CDN+Avery.png

SC gap entries (6 entries — source_url=None):
-2507110001 Brian K. Castellanos: gap_reason='No headshot on lynnschools.org (SchoolMessenger text-only page); no fallback per D-01'
-2507110002 Lorraine Gately: same gap_reason
-2507110003 Brenda Ortiz McGrath: same gap_reason
-2507110004 Lennin Peña: same gap_reason
-2507110005 Andrea L. Satterwhite: same gap_reason
-2507110006 Tristan J. Smith: same gap_reason

main() updates:
- Banner: 'Phase 119 — Lynn Headshots (migration 586)'
- ROSTER count: 18 (12 city + 6 SC)
- Summary note: 'CivicLive CDN accessible; 12 confirmed 200 (11 CDN + 1 Wikipedia Mayor); 6 SC gaps (SchoolMessenger text-only)'

Run the script and confirm it uploads 12 images and reports 6 gaps.
  </action>
  <verify>
    <automated>python C:/EV-Accounts/backend/scripts/_tmp-lynn-headshots.py — script must print summary showing 12 uploaded, 6 skipped/gap, 0 errors</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/_tmp-lynn-headshots.py exists
    - Script runs to completion without Python exceptions
    - Script output reports: 12 uploaded successfully
    - Script output reports: 6 gaps (SC members with no source URL)
    - Script output reports: 0 download errors
    - Supabase Storage politician_photos bucket contains files matching pattern {politician_uuid}-headshot.jpg for all 12 city official politician_ids
    - Megie-Maddrey image uploaded successfully (confirms MegieMaddrey.png filename was used, not Megie-Maddrey.png)
    - Mayor Nicholson image uploaded from Wikipedia Commons URL (not CivicLive CDN)
  </acceptance_criteria>
  <done>Python script uploaded 12 city official headshots to politician_photos bucket at 600x750; 6 SC gap entries logged; 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration 586_lynn_headshots.sql</name>
  <files>C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/583_somerville_headshots.sql (primary analog — accessible CMS pattern; copy INSERT block structure and post-verification pattern verbatim; update politician external_ids and CDN URL paths)
    - .planning/phases/119-lynn-deep-seed/119-PATTERNS.md (586 substitution table, politician_images INSERT pattern with exact SQL, gap comment pattern, post-verification DO block)
    - .planning/phases/119-lynn-deep-seed/119-RESEARCH.md (expected: 12 uploads city + 0 SC; headshot storage CDN base URL pattern)
  </read_first>
  <action>
Create C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql by adapting 583_somerville_headshots.sql with these substitutions (all in 119-PATTERNS.md):

External ID ranges:
- City officials: -2562535001..-2562535012 (Somerville) → -2537490001..-2537490012 (Lynn)
- SC officials: -2510890001..-2510890007 (Somerville) → -2507110001..-2507110006 (Lynn)

Migration ledger version: '583' → '586'

12 politician_images INSERT blocks (one per city official) using this pattern:
INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license)
SELECT gen_random_uuid(),
       (SELECT id FROM essentials.politicians WHERE external_id = {ext_id}),
       'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/' ||
         (SELECT id FROM essentials.politicians WHERE external_id = {ext_id}) || '-headshot.jpg',
       'default', 'public_domain'
WHERE NOT EXISTS (
  SELECT 1 FROM essentials.politician_images
  WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = {ext_id})
);

Insert blocks for all 12 city external_ids: -2537490001 through -2537490012.

6 gap comment lines (NO INSERT — just SQL comments, one per SC member):
-- GAP: -2507110001 Brian K. Castellanos — no headshot on lynnschools.org (SchoolMessenger text-only); no fallback per D-01
-- GAP: -2507110002 Lorraine Gately — no headshot on lynnschools.org (SchoolMessenger text-only); no fallback per D-01
-- GAP: -2507110003 Brenda Ortiz McGrath — no headshot on lynnschools.org (SchoolMessenger text-only); no fallback per D-01
-- GAP: -2507110004 Lennin Peña — no headshot on lynnschools.org (SchoolMessenger text-only); no fallback per D-01
-- GAP: -2507110005 Andrea L. Satterwhite — no headshot on lynnschools.org (SchoolMessenger text-only); no fallback per D-01
-- GAP: -2507110006 Tristan J. Smith — no headshot on lynnschools.org (SchoolMessenger text-only); no fallback per D-01

Post-verification DO block (copy from 583, substituting Lynn ranges):
- v_img_count: COUNT type='default' WHERE external_id BETWEEN -2537490012 AND -2537490001 OR BETWEEN -2507110006 AND -2507110001
- v_wrong_type: COUNT type != 'default' same range
- RAISE EXCEPTION if v_wrong_type > 0
- RAISE NOTICE: 'Migration 586 post-verification PASSED: % headshots inserted (type=default), 6 SC gap officials documented (SchoolMessenger text-only site)', v_img_count

Migration ledger:
INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('586') ON CONFLICT (version) DO NOTHING;
COMMIT;

Apply the migration via mcp__supabase-local after writing the file.
  </action>
  <verify>
    <automated>Run migration 586 via mcp__supabase-local. Post-verification RAISE NOTICE must report: 'Migration 586 post-verification PASSED: 12 headshots inserted (type=default), 6 SC gap officials documented'</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql exists
    - Migration applies without errors
    - Post-verification PASSED notice shows v_img_count = 12
    - Query: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE (p.external_id BETWEEN -2537490012 AND -2537490001 OR p.external_id BETWEEN -2507110006 AND -2507110001) AND pi.type = 'default' → 12
    - Query: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE (p.external_id BETWEEN -2537490012 AND -2537490001 OR p.external_id BETWEEN -2507110006 AND -2507110001) AND pi.type != 'default' → 0
    - No politician_images rows exist for SC external_ids -2507110001..-2507110006 (confirmed gaps, not inserted)
    - All 12 inserted politician_images rows have type='default' (not 'headshot')
    - LYNN-02 requirement satisfied: 12/12 city officials have headshots; 6 SC gaps documented
  </acceptance_criteria>
  <done>Migration 586 applied; 12 politician_images rows with type='default' inserted for Lynn city officials; 6 SC gaps documented in migration comments; post-verification PASSED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python script → external image CDN (CivicLive, Wikipedia) | Downloading from third-party URLs; image content is passively consumed |
| Python script → Supabase Storage | Service role key in .env file; uploads to politician_photos bucket |
| SQL migration → Supabase production DB | Storage CDN URL embedded in politician_images.url column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-03-01 | Information Disclosure | .env SERVICE_KEY | accept | Already in use in prior phases; key stays local; solo dev workflow |
| T-119-03-02 | Tampering | politician_images type field | mitigate | All INSERTs use type='default'; post-verification gate checks type != 'default' count = 0 |
| T-119-03-03 | Tampering | Schema migrations ledger | mitigate | ON CONFLICT (version) DO NOTHING |
| T-119-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; uses existing Pillow/requests/supabase-py from prior phases |
</threat_model>

<verification>
After running Python script and applying migration 586:

1. SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE (p.external_id BETWEEN -2537490012 AND -2537490001 OR p.external_id BETWEEN -2507110006 AND -2507110001) AND pi.type = 'default'; -- Expected: 12
2. SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE (p.external_id BETWEEN -2537490012 AND -2537490001 OR p.external_id BETWEEN -2507110006 AND -2507110001) AND pi.type != 'default'; -- Expected: 0
3. SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2507110006 AND -2507110001; -- Expected: 0 (all SC gaps, no uploads)
4. Section-split final check: SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('2537490', '2507110') AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id); -- Expected: 0
</verification>

<success_criteria>
- Python script: 12 uploaded, 6 gaps, 0 errors
- Migration 586: 12 politician_images rows type='default'; post-verification PASSED
- LYNN-02 satisfied: 12/12 city officials with headshots at 600×750; 6 SC gaps documented honestly per D-01
- Phase 119 complete: LYNN-01 + LYNN-02 both satisfied
</success_criteria>

<output>
Create `.planning/phases/119-lynn-deep-seed/119-03-SUMMARY.md` when done
</output>
