---
phase: 96-md-2026-elections-discovery-pipeline-landing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/278_md_2026_elections.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-278.ts
autonomous: true
requirements:
  - MD-ELECTIONS-01
tags:
  - elections
  - sql
  - maryland

must_haves:
  truths:
    - "essentials.elections contains 2 rows where state='MD' (1 primary 2026-06-23, 1 general 2026-11-03)"
    - "Re-applying migration 278 inserts zero additional rows"
    - "Migration 278 ledger entry exists in supabase_migrations.schema_migrations"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/278_md_2026_elections.sql
      provides: "Two INSERT statements for essentials.elections, idempotent via ON CONFLICT (name, election_date, state) DO NOTHING"
      contains: "2026-06-23"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-278.ts
      provides: "Node/pg script that applies migration 278 against DATABASE_URL"
      contains: "278_md_2026_elections.sql"
  key_links:
    - from: 278_md_2026_elections.sql
      to: essentials.elections
      via: "INSERT ... ON CONFLICT (name, election_date, state) DO NOTHING"
      pattern: "INSERT INTO essentials\\.elections"
---

<objective>
Seed Maryland's two 2026 election rows (primary + general) into `essentials.elections` via migration 278. This is the foundational migration — every race row in plan 96-02 references these elections by name+state subquery.

Per D-01 of CONTEXT.md, the primary row is bare (no race rows reference it); all races link to the general only. This plan covers MD-ELECTIONS-01 (election rows portion).

Purpose: Establish the FK target rows that migrations 279 and 280 need at apply time.
Output: Migration 278 SQL file + matching `_apply-migration-278.ts` script applied to production Supabase.

**Key research correction:** MD primary date is `2026-06-23` (verified at elections.maryland.gov). CONTEXT.md said July 14 — that is wrong. Use 2026-06-23 throughout this plan.

**ROADMAP deviation note (carry into 96-02):** ROADMAP says "198 total race rows" based on a one-row-per-seat model. Actual count per D-01 + UNIQUE constraint is 130 rows total (12 statewide + 47 senate + 71 SLDL geofences with seats=N). Plan 96-01 only inserts 2 election rows; the deviation is implemented in 96-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-CONTEXT.md
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-RESEARCH.md
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-PATTERNS.md
@C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql
@C:/EV-Accounts/backend/scripts/_apply-migration-277.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write migration 278 + apply script + apply to production</name>
  <files>C:/EV-Accounts/backend/migrations/278_md_2026_elections.sql, C:/EV-Accounts/backend/scripts/_apply-migration-278.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql (canonical analog — exact two-INSERT pattern to mirror)
    - C:/EV-Accounts/backend/scripts/_apply-migration-277.ts (closest apply-script analog — same Pool + readFileSync + pool.query shape)
    - .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-RESEARCH.md (Pattern 1 — exact MD values and ON CONFLICT clause)
    - .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-PATTERNS.md (Pattern Assignment for 278 — exact substitutions)
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/278_md_2026_elections.sql` mirroring migration 237's structure. Per D-01/D-02 and RESEARCH.md Critical Finding, use these EXACT values:

    - Primary row: `name='2026 Maryland State Primary'`, `election_date='2026-06-23'` (NOT July 14), `election_type='primary'`, `jurisdiction_level='state'`, `state='MD'`
    - General row: `name='2026 Maryland General Election'`, `election_date='2026-11-03'`, `election_type='general'`, `jurisdiction_level='state'`, `state='MD'`
    - Both INSERTs use `ON CONFLICT (name, election_date, state) DO NOTHING` (idempotency guard from PATTERNS.md "Idempotency Guards" table)

    Header comment lines must reference: "Migration 278: MD 2026 Elections — Phase 96 Plan 01"; cite D-02 bare-primary pattern; note that MD primary date verified at elections.maryland.gov as 2026-06-23 (correcting July 14 from CONTEXT.md). Do NOT include a ledger INSERT into supabase_migrations.schema_migrations inside the SQL — the apply script handles ledger tracking via the migration framework.

    Create `C:/EV-Accounts/backend/scripts/_apply-migration-278.ts` mirroring _apply-migration-277.ts exactly: import dotenv/config, pg Pool, fs/readFileSync, path; build pool from `process.env['DATABASE_URL']` with `ssl: { rejectUnauthorized: false }`; read SQL via `path.join(process.cwd(), 'migrations', '278_md_2026_elections.sql')`; call `await pool.query(sql)`; log success "Migration 278 applied successfully". Then run three verification queries inline (do not wrap as separate functions):

    1. Election count: `SELECT COUNT(*) as cnt FROM essentials.elections WHERE state = 'MD' AND election_date IN ('2026-06-23', '2026-11-03')` — log "(expected 2)"
    2. Primary date check: `SELECT election_date FROM essentials.elections WHERE name = '2026 Maryland State Primary' AND state = 'MD'` — log row value, expect 2026-06-23
    3. General date check: `SELECT election_date FROM essentials.elections WHERE name = '2026 Maryland General Election' AND state = 'MD'` — log row value, expect 2026-11-03

    Use the try/catch/finally pattern from _apply-migration-277.ts (process.exit(1) on error; pool.end() in finally). The ledger entry verification (`SELECT version FROM supabase_migrations.schema_migrations WHERE version = '278'`) should be the final smoke test logging "Ledger entry 278: PRESENT" if rows.length > 0.

    After both files exist, run the apply script: `cd C:/EV-Accounts/backend; npx tsx scripts/_apply-migration-278.ts`. Verify all three log lines show expected values. Re-run once to confirm idempotency (count should remain 2; "Migration 278 applied successfully" should still log — ON CONFLICT absorbs the duplicates).
  </action>
  <acceptance_criteria>
    - File exists: `C:/EV-Accounts/backend/migrations/278_md_2026_elections.sql`
    - File contains literal string `'2026-06-23'` (verified primary date)
    - File contains literal string `'2026 Maryland State Primary'`
    - File contains literal string `'2026 Maryland General Election'`
    - File contains literal string `ON CONFLICT (name, election_date, state) DO NOTHING` (exact text)
    - File does NOT contain the string `2026-07-14` (the wrong date from CONTEXT.md)
    - File exists: `C:/EV-Accounts/backend/scripts/_apply-migration-278.ts`
    - Apply script run output contains "Migration 278 applied successfully"
    - Apply script run output contains "(expected 2)" with cnt=2
    - SQL query `SELECT COUNT(*) FROM essentials.elections WHERE state='MD'` returns 2
    - SQL query `SELECT election_date FROM essentials.elections WHERE name = '2026 Maryland State Primary' AND state='MD'` returns date 2026-06-23
    - SQL query `SELECT election_date FROM essentials.elections WHERE name = '2026 Maryland General Election' AND state='MD'` returns date 2026-11-03
    - Re-running apply script keeps `SELECT COUNT(*) FROM essentials.elections WHERE state='MD'` at exactly 2 (idempotency)
    - Ledger row exists: `SELECT version FROM supabase_migrations.schema_migrations WHERE version='278'` returns 1 row
  </acceptance_criteria>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-278.ts</automated>
  </verify>
  <done>
    Migration 278 file exists, apply script exists, both have been run against production Supabase, election count for state='MD' is exactly 2, both rows have the correct election_date values, and re-running the apply script leaves the count at 2.
  </done>
</task>

</tasks>

<verification>
1. Run: `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-278.ts` — output must include "Migration 278 applied successfully" and "(expected 2)" with cnt=2.
2. SQL count gate: `SELECT COUNT(*) FROM essentials.elections WHERE state='MD'` returns exactly 2.
3. Date verification: both election_date values match (2026-06-23 and 2026-11-03).
4. Idempotency: re-running the apply script does NOT increase the count.
5. Ledger entry: `SELECT version FROM supabase_migrations.schema_migrations WHERE version='278'` returns 1 row.
</verification>

<success_criteria>
- 2 MD election rows in essentials.elections with verified dates (primary 2026-06-23, general 2026-11-03)
- Migration 278 applied to production Supabase via _apply-migration-278.ts
- Migration is idempotent (re-running yields zero additional rows)
- Ledger entry 278 present
- Plan 96-02 can now resolve `(SELECT id FROM essentials.elections WHERE name = '2026 Maryland General Election' AND state = 'MD')` to a non-null UUID
</success_criteria>

<output>
Create `.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-01-SUMMARY.md` when done. Capture:
- Confirmed election UUIDs (run a query to record them for downstream traceability)
- Idempotency re-run results
- Any deviations from PATTERNS.md
</output>
