---
phase: 104-va-headshots
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-va-senators-headshots.py
autonomous: true
requirements:
  - VA-GOV-06
must_haves:
  truths:
    - "40 VA state senator headshots exist in politician_photos Supabase Storage bucket at 600x750 JPEG q90"
    - "All 40 use the verified apps.senate.virginia.gov URL keys from 104-RESEARCH.md (no zero-padding, TitleCase, special-case names preserved verbatim)"
    - "Senators with anomalous filenames (Mulchi9, Williams Graves21, Carroll Foy33, VanValkenburg16, Bennett-Parker39, Craig19) all succeed via hardcoded keys"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-va-senators-headshots.py"
      provides: "Standalone download/crop/resize/upload script for 40 VA senators"
      contains: "SENATE_KEY_MAP with 40 entries SD-1 through SD-40"
    - path: "Supabase Storage politician_photos/{politician_id}-headshot.jpg"
      provides: "40 uploaded headshot JPEGs at 600x750"
  key_links:
    - from: "SENATE_KEY_MAP dict"
      to: "apps.senate.virginia.gov/Senator/images/member_photos/{Key}.jpg"
      via: "HTTP GET with browser User-Agent"
      pattern: "apps\\.senate\\.virginia\\.gov"
    - from: "SD-N ROSTER entry"
      to: "essentials.politicians.external_id = -5110000-N"
      via: "DB lookup at script run time"
      pattern: "WHERE external_id"
---

<objective>
Create and run `_tmp-va-senators-headshots.py` to download, crop to 4:5, resize to 600x750 Lanczos q90, and upload the headshots for all 40 VA state senators (external_ids -5110001 through -5110040) per D-01, D-02, D-05, D-12, D-13. Use the verified SENATE_KEY_MAP from 104-RESEARCH.md — do NOT re-probe URLs at runtime; the table was confirmed HTTP 200 on all 40 keys.

Purpose: Cover the senator portion of VA-GOV-06 (40 of 155 final politician_images rows).
Output: One Python script + 40 JPEG files in Supabase Storage, plus a deterministic SUCCESS/FAILED manifest block for Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-va-headshots/104-CONTEXT.md
@.planning/phases/104-va-headshots/104-RESEARCH.md
@C:/EV-Accounts/backend/scripts/_tmp-alexandria-headshots.py
@C:/EV-Accounts/backend/migrations/307_va_state_senators.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write _tmp-va-senators-headshots.py with verified SENATE_KEY_MAP for all 40 senators</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-va-senators-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-alexandria-headshots.py (template structure: env loader, PIL pipeline, BUCKET, manifest format)
    - .planning/phases/104-va-headshots/104-RESEARCH.md (§"Senate URL Pattern" full table of 40 keys + §"Verified Senate Script Pattern" with complete SENATE_KEY_MAP dict)
    - C:/EV-Accounts/backend/migrations/307_va_state_senators.sql (canonical senator names + external_ids)
    - .planning/phases/104-va-headshots/104-CONTEXT.md (D-01, D-05, D-06, D-11, D-13, D-14)
  </read_first>
  <action>
    Copy the Alexandria script structure exactly. Define module-level constants `SENATE_PHOTO_BASE = 'https://apps.senate.virginia.gov/Senator/images/member_photos/'` and `SENATE_KEY_MAP` as a dict mapping district number (int 1..40) to the verified filename key string from 104-RESEARCH.md §"Verified Senate Script Pattern" — copy that dict literal verbatim. Special-case strings MUST be preserved exactly: 'Mulchi9', 'Craig19', 'VanValkenburg16', 'DeSteph20', 'Williams Graves21' (with the space), 'McDougle26', 'McPike29', 'Carroll Foy33' (with the space), 'Bennett-Parker39' (with the hyphen). Generate ROSTER programmatically by iterating district numbers 1..40 and constructing each entry: `external_id = -5110000 - district` (so SD-1 = -5110001, SD-40 = -5110040), `full_name` pulled from a hardcoded NAMES dict (40 entries matching migration 307's order; copy the senator list from 104-RESEARCH.md §"State Senators (migration 307)"), `section_label='VA State Senate'`, `title=f'State Senator SD-{district}'`, `source_url = SENATE_PHOTO_BASE + SENATE_KEY_MAP[district] + '.jpg'`, `source_domain='apps.senate.virginia.gov'`, `is_required=True`. URL-quoting note: requests.get() handles the space in 'Williams Graves21' and 'Carroll Foy33' automatically (urlencodes to %20) — do not manually pre-encode. Resolve `politician_id` UUIDs at runtime via `SELECT id FROM essentials.politicians WHERE external_id = %s` for each entry (no hardcoded UUIDs). Manifest header: "=== VA SENATORS HEADSHOT MANIFEST ===" with one SUCCESS/FAILED line per senator and a "TOTALS: VA Senators {N}/40 succeeded" footer. Implement hard-gate: if any failure, sys.exit(1) — but if a single senator fails with HTTP 404 (signaling the verified key may have changed), the failure message must include the URL attempted so a fallback to D-06 sources can be planned in a gap-closure run.
  </action>
  <verify>
    <automated>python -c "import ast,sys; src=open('C:/EV-Accounts/backend/scripts/_tmp-va-senators-headshots.py').read(); assert 'Williams Graves21' in src and 'Carroll Foy33' in src and 'Mulchi9' in src and 'Bennett-Parker39' in src and 'VanValkenburg16' in src; print('keys-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/_tmp-va-senators-headshots.py exists
    - File contains string `SENATE_PHOTO_BASE = 'https://apps.senate.virginia.gov/Senator/images/member_photos/'`
    - File contains string `'Williams Graves21'` (space preserved)
    - File contains string `'Carroll Foy33'` (space preserved)
    - File contains string `'Bennett-Parker39'` (hyphen preserved)
    - File contains string `'VanValkenburg16'` (camelCase)
    - File contains string `'Mulchi9'` (final-segment-only)
    - File contains string `'Craig19'` (final-segment-only)
    - SENATE_KEY_MAP has exactly 40 entries (keys 1..40)
    - File contains string `BUCKET = 'politician_photos'`
    - File contains string `TARGET_SIZE = (600, 750)`
    - File contains string `SELECT id FROM` AND `WHERE external_id`
    - grep -v '^#' on the file returns 0 lines containing `storage_url`
  </acceptance_criteria>
  <done>
    Script exists with all 40 verified keys and runtime UUID resolution; Task 2 will execute it.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run _tmp-va-senators-headshots.py and verify all 40 senator headshots uploaded</name>
  <files>(no new files; runs the script written in Task 1)</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-va-senators-headshots.py (verify SENATE_KEY_MAP and ROSTER count)
    - .planning/phases/104-va-headshots/104-VALIDATION.md (post-run SQL verification)
  </read_first>
  <action>
    Run `python C:/EV-Accounts/backend/scripts/_tmp-va-senators-headshots.py` from C:/EV-Accounts/backend. Capture full stdout/stderr. Confirm the manifest section contains exactly 40 SUCCESS lines, one per senator SD-1 through SD-40. If any senator fails with HTTP 404 (signaling URL key change since 2026-06-08 research), record the senator + URL in the SUMMARY's "Gaps for Plan 05" section but continue processing the rest — only sys.exit(1) at end if total successes &lt; 40. If a senator's image is &lt; 200x200 px (below PIL min_dim), record the error and continue. Do NOT manually swap URLs or fall back to Wikipedia in this plan — fallbacks go in a follow-up gap plan. After successful run, HEAD-check 5 random senator CDN URLs to confirm HTTP 200. Save the manifest text into 104-02-SUMMARY.md.
  </action>
  <verify>
    <automated>python -c "import psycopg2; e={}; [e.update({k.strip():v.strip()}) for k,v in (l.split('=',1) for l in open('C:/EV-Accounts/backend/.env') if '=' in l and not l.strip().startswith('#'))]; c=psycopg2.connect(e['DATABASE_URL']+'?sslmode=require'); cur=c.cursor(); cur.execute('SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5110040 AND -5110001'); print('senators-found',cur.fetchone()[0])"</automated>
  </verify>
  <acceptance_criteria>
    - Script exit code 0
    - Captured stdout contains string `=== VA SENATORS HEADSHOT MANIFEST ===`
    - Captured stdout contains exactly 40 lines matching pattern `^SUCCESS: -51100`
    - Captured stdout contains 0 lines matching pattern `^FAILED: `
    - Captured stdout contains string `TOTALS: VA Senators 40/40 succeeded`
    - HEAD request to 5 sampled CDN URLs all return HTTP 200
    - 104-02-SUMMARY.md exists with verbatim manifest under heading `## Manifest (Plan 05 Input)`
  </acceptance_criteria>
  <done>
    40 senator headshots in Storage at 600x750 JPEG q90; manifest captured for Plan 05.
  </done>
</task>

</tasks>

<verification>
After both tasks: 40 JPEG objects in `politician_photos` bucket keyed by the politician UUID resolved from external_id -5110001..-5110040. Source images are ~262x269 px (small but above 200px PIL minimum); the Lanczos upscale to 600x750 should still be acceptable per the v7.0 California precedent. If quality is unacceptable on any senator, that is logged for a follow-up gap plan, NOT addressed in this plan.
</verification>

<success_criteria>
1. `_tmp-va-senators-headshots.py` exists with verified SENATE_KEY_MAP (40 entries, special-case names preserved verbatim)
2. Running the script produces 40 SUCCESS manifest lines and exit code 0
3. 40 Storage objects accessible at `{CDN_BASE}/{politician_id}-headshot.jpg`
4. Anomalous-name senators (Mulchi9, Williams Graves21, Carroll Foy33, Bennett-Parker39, VanValkenburg16, Craig19) all in SUCCESS
</success_criteria>

<output>
Create `.planning/phases/104-va-headshots/104-02-SUMMARY.md` when done, including:
- Verbatim manifest block (Plan 05 input)
- Per-senator source URL + original dimensions + crop dimensions (D-10 documentation feed for migration 315)
- Any senators flagged for quality follow-up
</output>
