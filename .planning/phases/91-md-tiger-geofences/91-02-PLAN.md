---
phase: 91-md-tiger-geofences
plan: 02
type: execute
wave: 2
depends_on:
  - 91-01
files_modified:
  - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts
  - C:/EV-Accounts/backend/scripts/smoke-md-geofences.ts
autonomous: true
requirements:
  - MD-GEO-01
  - MD-GEO-02
  - MD-GEO-03
  - MD-GEO-04
  - MD-GEO-05

must_haves:
  truths:
    - "The actual SLDL polygon count (from dry-run output) is recorded and replaces the 0 sentinel in EXPECTED_MD_MTFCC.sldl"
    - "The actual G4110 place count (from dry-run output) is recorded and replaces the 0 sentinel in EXPECTED_MD_MTFCC.place"
    - "expectedCounts in smoke-md-geofences.ts matches the confirmed dry-run counts for G4110 and G5220"
    - "A subsequent dry-run with confirmed counts passes all 5 layer assertions without MtfccAssertionError"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts"
      provides: "EXPECTED_MD_MTFCC with confirmed sldl and place counts (both non-zero)"
      contains: "sldl:"
    - path: "C:/EV-Accounts/backend/scripts/smoke-md-geofences.ts"
      provides: "expectedCounts with confirmed G4110 and G5220 values"
      contains: "G4110:"
  key_links:
    - from: "dry-run MtfccAssertionError output"
      to: "EXPECTED_MD_MTFCC.sldl and EXPECTED_MD_MTFCC.place"
      via: "'got N' in error message"
      pattern: "got \\d+"
    - from: "EXPECTED_MD_MTFCC confirmed counts"
      to: "smoke-md-geofences.ts expectedCounts"
      via: "matching G4110 and G5220 values"
      pattern: "G4110:|G5220:"
---

<objective>
Run a dry-run of all 5 MD TIGER layers to discover the actual SLDL and G4110 polygon counts, then update the loader and smoke test with confirmed values before the live load.

Purpose: D-04 and D-09 mandate that SLDL and place counts be confirmed by actual TIGER file inspection, not hardcoded from estimates. The MtfccAssertionError pattern in the loader surfaces the actual count without writing any data to the database. Plans 03 and 04 depend on confirmed counts being locked in.

Output: Updated EXPECTED_MD_MTFCC entries in load-state-tiger-boundaries.ts; updated expectedCounts in smoke-md-geofences.ts; no database rows written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-md-tiger-geofences/91-CONTEXT.md
@.planning/phases/91-md-tiger-geofences/91-RESEARCH.md
@.planning/phases/91-md-tiger-geofences/91-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run sldl and place layers — capture actual counts from MtfccAssertionError output</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Confirm that EXPECTED_MD_MTFCC.sldl and EXPECTED_MD_MTFCC.place are both 0 (sentinel values from Plan 01). Confirm the fipsArg === '24' block is present after the OR block.
    .planning/phases/91-md-tiger-geofences/91-RESEARCH.md — Section "Dry-Run Protocol" and "Counts Requiring Dry-Run Confirmation": expected ranges are sldl ~71, place ~311.
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the dry-run for sldl and place layers:

      npx tsx scripts/load-state-tiger-boundaries.ts --state MD --fips 24 --layers sldl,place --dry-run

    The loader will download the TIGER zip files (2.1MB for sldl, 2.6MB for place), stream the shapefiles, and throw MtfccAssertionError for each layer because the expected count (0) does not match actual. The error message format is:

      [MD MTFCC assertion] layer=sldl: expected 0 records, got N. TIGER file: ...
      [MD MTFCC assertion] layer=place: expected 0 records, got N. TIGER file: ...

    Record the actual N values from both error messages.

    Expected ranges from research: sldl ~71 (71 sub-district designations from MSA Maryland), place ~311 (TIGERweb ACS24 BAS25). If the actual counts differ from these expectations:
      - sldl: Any value between 60 and 80 is plausible; use the actual number
      - place: Any value between 240 and 400 is plausible; use the actual number
      - If sldl returns exactly 141: something is wrong — the TIGER file may be returning one polygon per delegate rather than one per sub-district; stop and investigate before proceeding

    Also run cd119 and sldu dry-runs to confirm they pass their already-locked assertions (8 and 47 respectively):

      npx tsx scripts/load-state-tiger-boundaries.ts --state MD --fips 24 --layers cd119,sldu --dry-run

    Both should print "MD MTFCC pre-flight assertion PASSED" — if either fails, record the actual count and update the EXPECTED_MD_MTFCC value before proceeding to live load.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/load-state-tiger-boundaries.ts --state MD --fips 24 --layers cd119,sldu --dry-run 2>&1 | grep -E "PASSED|assertion"</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run output for sldl contains "[MD MTFCC assertion] layer=sldl: expected 0 records, got N" — N is between 60 and 80 (or if outside this range, the executor investigates and documents the discrepancy)
    - Dry-run output for place contains "[MD MTFCC assertion] layer=place: expected 0 records, got N" — N is between 240 and 400
    - N is NOT 141 for sldl (if it is, stop — this indicates a counting error)
    - cd119 dry-run prints "PASSED: 8 records"
    - sldu dry-run prints "PASSED: 47 records"
    - The actual N values for sldl and place are recorded (in SUMMARY.md or noted for the next task)
  </acceptance_criteria>
  <done>Actual SLDL polygon count and actual G4110 place count are known from dry-run output and confirmed not to be 141 (SLDL) or obviously wrong (place).</done>
</task>

<task type="auto">
  <name>Task 2: Update confirmed counts in loader and smoke test</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Locate the EXPECTED_MD_MTFCC block (fipsArg === '24'); find sldl: 0 and place: 0 entries to update.
    C:/EV-Accounts/backend/scripts/smoke-md-geofences.ts — Locate expectedCounts block; find G4110: 0 and G5220: 0 entries to update.
  </read_first>
  <action>
    Update load-state-tiger-boundaries.ts EXPECTED_MD_MTFCC block:
      - Replace sldl: 0 with sldl: [actual count from Task 1], adding a comment: // confirmed via dry-run [date] — [N] MD SLDL sub-district polygons (NOT 141 delegates; NOT 47 senate districts)
      - Replace place: 0 with place: [actual count from Task 1], adding a comment: // confirmed via dry-run [date] — [N] MD G4110 incorporated places

    Update smoke-md-geofences.ts expectedCounts block:
      - Replace G4110: 0 with G4110: [actual place count from Task 1]
      - Replace G5220: 0 with G5220: [actual sldl count from Task 1]

    After both edits, run the full dry-run for all 5 layers to confirm all assertions now pass:

      npx tsx scripts/load-state-tiger-boundaries.ts --state MD --fips 24 --layers cd119,sldu,sldl,place,county --dry-run

    All 5 layers should print "MD MTFCC pre-flight assertion PASSED" with their expected counts. The county layer downloads tl_2024_us_county.zip (US-wide file) and filters to STATEFP='24' — expected count is 24.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/load-state-tiger-boundaries.ts --state MD --fips 24 --layers cd119,sldu,sldl,place,county --dry-run 2>&1 | grep "PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - EXPECTED_MD_MTFCC.sldl is a non-zero integer matching the dry-run "got N" output (not 0, not 141, not 47)
    - EXPECTED_MD_MTFCC.place is a non-zero integer matching the dry-run "got N" output (not 0)
    - smoke-md-geofences.ts G4110 matches EXPECTED_MD_MTFCC.place; G5220 matches EXPECTED_MD_MTFCC.sldl
    - Full 5-layer dry-run prints exactly 5 "MD MTFCC pre-flight assertion PASSED" lines (cd119|8, sldu|47, sldl|N, place|N, county|24)
    - No MtfccAssertionError in the full dry-run output
  </acceptance_criteria>
  <done>Both TBD sentinel values are replaced with confirmed counts; full 5-layer dry-run passes all assertions; loader and smoke test are in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov → EV-Accounts backend | TIGER zip files downloaded over HTTPS during dry-run (no DB writes) |
| dry-run output → code edits | Actual counts from MtfccAssertionError output flow into code as literal integers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04 | Tampering | TIGER zip files from census.gov | accept | census.gov is the authoritative US government source; files are read-only during dry-run; no DB writes occur |
| T-91-05 | Information Disclosure | DATABASE_URL during dry-run | mitigate | --dry-run flag skips all DB connections; dotenv.config() only reads local .env; no credentials transmitted |
| T-91-06 | Integrity | Wrong count written if sldl=141 not caught | mitigate | acceptance_criteria explicitly gate: "N is NOT 141 for sldl — if it is, stop"; executor must document and halt before proceeding |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan; loader runs existing dependencies |
</threat_model>

<verification>
After both tasks complete:
1. `grep -A1 "sldl:" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts | grep -v "0,"` — returns a non-zero integer
2. `grep -A1 "place:" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts | grep "confirmed via dry-run"` — comment is present
3. Full dry-run: `npx tsx scripts/load-state-tiger-boundaries.ts --state MD --fips 24 --layers cd119,sldu,sldl,place,county --dry-run` prints 5 PASSED lines, no errors
4. Smoke test expectedCounts G4110 and G5220 match the confirmed values
</verification>

<success_criteria>
- Actual SLDL polygon count confirmed from TIGER file (expected ~71; not 47, not 141)
- Actual G4110 incorporated place count confirmed from TIGER file (expected ~311)
- EXPECTED_MD_MTFCC.sldl and .place updated with confirmed values in loader
- smoke-md-geofences.ts expectedCounts.G4110 and .G5220 updated to match
- Full 5-layer dry-run passes all pre-flight assertions; no MtfccAssertionError
</success_criteria>

<output>
Create `.planning/phases/91-md-tiger-geofences/91-02-SUMMARY.md` when done. Record the confirmed counts (sldl=N, place=N) in the summary — Plans 03 and 04 reference these values.
</output>
