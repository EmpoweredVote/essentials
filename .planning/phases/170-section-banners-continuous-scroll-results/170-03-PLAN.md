---
phase: 170-section-banners-continuous-scroll-results
plan: 03
type: execute
wave: 2
depends_on: ["170-01", "170-02"]
files_modified:
  - src/pages/Results.jsx
autonomous: true
requirements: [BANR-02, BANR-03, NAV-01]
must_haves:
  truths:
    - "Results renders City → State → Federal in one continuous vertical scroll with a SectionBanner before each tier group (School folds under the City banner)."
    - "The correct banner image + label is selected per jurisdiction from buildingImageMap; a tier with no art shows the graceful gradient fallback (no broken image)."
    - "There is no tier sort control and no dead single-tier code path; selectedFilter is gone, the scroll-spy and the dead activeBuildingImage computation are removed."
    - "The Elected/Appointed Type filter and name search still work in the banner-divided layout."
  artifacts:
    - path: "src/pages/Results.jsx"
      provides: "Continuous-scroll tier layout with SectionBanner dividers; tier-filter logic removed"
      contains: "SectionBanner"
  key_links:
    - from: "src/pages/Results.jsx"
      to: "src/components/SectionBanner.jsx"
      via: "import + render before each tier group"
      pattern: "import SectionBanner|<SectionBanner"
    - from: "src/pages/Results.jsx"
      to: "buildingImageMap (getBuildingImages output)"
      via: "imageUrl prop per tier"
      pattern: "buildingImageMap\\.(Local|State|Federal)"
    - from: "src/pages/Results.jsx"
      to: "src/components/FilterBar.jsx"
      via: "FilterBar call site no longer passes selectedFilter/onFilterChange"
      pattern: "<FilterBar"
---

<objective>
Wire `SectionBanner` into `src/pages/Results.jsx` so the Results/Representatives page renders
City → State → Federal as one continuous scroll with a banner divider before each tier group
(BANR-02), location-aware per jurisdiction with graceful gradient fallback (BANR-03 wiring), and
reconcile all the now-constant `selectedFilter` logic left behind by removing the tier control
(NAV-01, page side).

Purpose: This is the visible payoff of the phase — the banners actually appear and the tier dropdown
is fully gone with no dead branches. Plans 170-01 (SectionBanner) and 170-02 (FilterBar) are
prerequisites; this plan consumes both contracts.

Output: modified `src/pages/Results.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/170-section-banners-continuous-scroll-results/170-CONTEXT.md
@.planning/phases/170-section-banners-continuous-scroll-results/170-UI-SPEC.md
@.planning/phases/170-section-banners-continuous-scroll-results/170-PATTERNS.md
@.planning/phases/170-section-banners-continuous-scroll-results/170-01-SUMMARY.md
@.planning/phases/170-section-banners-continuous-scroll-results/170-02-SUMMARY.md

@src/components/SectionBanner.jsx
@src/lib/buildingImages.js

<interfaces>
<!-- SectionBanner contract from Plan 170-01 (consume directly). -->
SectionBanner default export. Props:
  tier: 'city' | 'state' | 'federal'   (required)
  locationName: string                  (required)  → rendered after the coral pin
  imageUrl: string | null               (optional)  → null/absent ⇒ tier-tinted fallback gradient
  stats?, featureIcons?                  (optional)  → omit this phase (scaffolding only)

<!-- Existing Results.jsx values to use (extracted; line numbers approximate). -->
- buildingImageMap (L1076-1079) = getBuildingImages(representingCity, userState)
    → { Local: string|null, State: string|null, Federal: '/images/us-capitol.jpg' }
- representingCity (L1053-1067): string|null — city name from results list
- userState (L1071-1074): 2-letter abbrev|null — parsed from address
- STATE_NAMES (L92): map of abbrev → full state name; STATE_NAMES[userState] is the State title
- filteredHierarchy (L1197-1215): array of { tier, bodies }; tier ∈ 'Local'|'School'|'State'|'Federal'
- the tier render loop is `filteredHierarchy.map(({ tier, bodies }) => …)` (L1907-1973)
- the empty-state loop is `['Local','School','State'].map((tier) => …)` (L1876-1898)

<!-- After Plan 170-02, FilterBar no longer accepts selectedFilter/onFilterChange. -->
The <FilterBar …/> call site is at L1826-1836; it currently passes selectedFilter (L1827) and
onFilterChange (L1828) — both MUST be removed here.

<!-- NOTE: activeBuildingImage (L1232-1235) is already DEAD — computed but never rendered anywhere
     in the codebase (grep confirms only the declaration). Remove the computation, the scroll-spy
     useEffect (L1237-1259), and the scrollActiveTier state (L525). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the tier-filter logic, scroll-spy, dead building-image state, and per-tier eyebrows</name>
  <files>src/pages/Results.jsx</files>
  <read_first>
    - src/pages/Results.jsx lines 470-530 (selectedFilter state init L477-478, scrollActiveTier state L525)
    - src/pages/Results.jsx lines 1190-1260 (filteredHierarchy L1197, locationLabel L1218, activeBuildingImage L1232, scroll-spy useEffect L1237-1259)
    - src/pages/Results.jsx lines 1820-1840 (FilterBar call site)
    - src/pages/Results.jsx lines 1872-1973 (empty-state loop + tier render loop + per-tier eyebrows)
    - .planning/phases/170-section-banners-continuous-scroll-results/170-PATTERNS.md §Results.jsx (the exact before/after blocks for filteredHierarchy, locationLabel, empty-state, eyebrow removal)
    - .planning/phases/170-section-banners-continuous-scroll-results/170-CONTEXT.md D-05, D-11, D-12
  </read_first>
  <action>
    Make `selectedFilter` effectively always 'All' and delete every branch that depended on it being
    otherwise (D-12), so no dead single-tier path remains.

    1. Remove the `scrollActiveTier` useState (L525) and the dead `activeBuildingImage` computation
       (L1232-1235) and the IntersectionObserver scroll-spy `useEffect` that observes `[data-tier]`
       (L1237-1259). These are the retired scroll-spy (D-05); `activeBuildingImage` is already unused.
    2. Remove `selectedFilter`/`setSelectedFilter` state (L477-478) and the PostHog tier-filter capture.
       In `filteredHierarchy` (L1197-1215): change the early-return guard to `if (appointedFilter === 'All')
       return hierarchy;`, delete the `.filter(({ tier }) => selectedFilter === 'All' || tier === selectedFilter)`
       line, and update the dependency array to `[hierarchy, appointedFilter]`.
    3. In `locationLabel` (L1218-1229): delete the `if (selectedFilter === 'State') { … }` branch so it
       always returns `city && state ? \`${city}, ${state}\` : null`; update deps to `[list]`.
    4. In the empty-state loop (L1876-1898): delete the guard `if (selectedFilter !== 'All' && selectedFilter !== tier) return null;` (L1882) and remove the `{selectedFilter === 'All' && ( … eyebrow … )}` block (L1890-1894).
    5. In the tier render loop (L1907-1973): remove the `{selectedFilter === 'All' && ( … eyebrow … )}` block (L1914-1918) — the banner becomes the single per-tier label (D-02).
    6. At the FilterBar call site (L1826-1836): remove the `selectedFilter={…}` (L1827) and
       `onFilterChange={…}` (L1828) props — Plan 170-02 dropped them from FilterBar. Leave all other
       FilterBar props unchanged.
    7. Keep the `data-tier={tier}` attributes on the tier wrapper divs (harmless DOM anchors; may be
       used by scroll-restore) — only the IntersectionObserver consuming them is removed.

    Search the whole file afterward for any remaining `selectedFilter` / `scrollActiveTier` /
    `activeBuildingImage` reference (including the `tierFilter={selectedFilter}` prop at ~L2008 — replace
    with the constant `'All'` or remove if the consumer no longer needs it; check the consumer first) and
    resolve each so no undefined identifier remains. Do NOT change the Type filter, name search, compass,
    GovernmentBodySection/SubGroupSection rendering, or PoliticianCard.
  </action>
  <verify>
    <automated>grep -c "selectedFilter\|scrollActiveTier\|activeBuildingImage" src/pages/Results.jsx | grep -qx 0 && echo CLEAN ; npx eslint src/pages/Results.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectedFilter" src/pages/Results.jsx` returns 0.
    - `grep -c "scrollActiveTier" src/pages/Results.jsx` returns 0 and `grep -c "activeBuildingImage" src/pages/Results.jsx` returns 0.
    - `grep -c "IntersectionObserver" src/pages/Results.jsx` returns 0 (scroll-spy removed) OR any remaining IntersectionObserver is unrelated to `[data-tier]` (manual confirm).
    - No per-tier eyebrow `<span>` referencing `{tier}` remains in the two render loops (the eyebrow blocks at the former L1890/1914 are gone).
    - The `<FilterBar>` call site no longer contains `selectedFilter=` or `onFilterChange=`.
    - `npx eslint src/pages/Results.jsx` exits 0 (no undefined / unused-var errors).
  </acceptance_criteria>
  <done>All tier-filter, scroll-spy, dead building-image, and per-tier-eyebrow code is removed; selectedFilter no longer exists; the page lints clean with no dangling references.</done>
</task>

<task type="auto">
  <name>Task 2: Insert SectionBanner dividers before each tier group (School folds under City)</name>
  <files>src/pages/Results.jsx</files>
  <read_first>
    - src/pages/Results.jsx top imports (where to add `import SectionBanner from '../components/SectionBanner.jsx';`)
    - src/pages/Results.jsx lines 1907-1973 (the post-Task-1 tier render loop)
    - src/pages/Results.jsx lines 1053-1079 (representingCity, userState, buildingImageMap) + line 92 (STATE_NAMES)
    - src/components/SectionBanner.jsx (the prop contract: tier, locationName, imageUrl)
    - .planning/phases/170-section-banners-continuous-scroll-results/170-PATTERNS.md §Results.jsx change #3 (banner tier mapping) + 170-UI-SPEC.md §"Banner Placement in Results.jsx"
    - .planning/phases/170-section-banners-continuous-scroll-results/170-CONTEXT.md D-07 (School folds under City)
  </read_first>
  <action>
    Add `import SectionBanner from '../components/SectionBanner.jsx';` to the Results.jsx imports.

    In the `filteredHierarchy.map(({ tier, bodies }) => …)` loop, render a `<SectionBanner>` at the top
    of each tier wrapper based on `tier`, applying the D-07 School fold (3 banners only — City, State,
    Federal; School gets none):
      - tier === 'Local'   → `<SectionBanner tier="city" locationName={representingCity && userState ? \`${representingCity}, ${userState}\` : (representingCity || 'Your City')} imageUrl={buildingImageMap.Local} />`
      - tier === 'School'   → render NO banner (school sections continue beneath the City banner)
      - tier === 'State'    → `<SectionBanner tier="state" locationName={(userState && STATE_NAMES[userState]) || userState || 'Your State'} imageUrl={buildingImageMap.State} />`
      - tier === 'Federal'  → `<SectionBanner tier="federal" locationName="United States" imageUrl={buildingImageMap.Federal} />`

    Implement as a small per-iteration lookup (e.g. a `bannerFor(tier)` helper or an inline switch) that
    returns the `<SectionBanner>` element for Local/State/Federal and `null` for School, rendered just
    inside the tier wrapper `<div>` before the `bodies.map(...)`. Use the existing full-bleed wrapper
    already on that div (`-mx-6 md:-mx-12 px-6 md:px-12`) — the banner brings its own `-mx-6 md:-mx-12`
    so it sits edge-to-edge; if double negative margins cause horizontal overflow, render the banner as a
    sibling immediately before the tier `<div>` instead of nested inside it (executor's call after a quick
    visual check — the goal is one full-bleed band per tier group with no horizontal scrollbar).

    Do NOT add a banner to the empty-state loop (banners only precede populated tier groups). Do NOT pass
    `stats` or `featureIcons`. Confirm `buildingImageMap.State`/`.Local` may be `null` — that is expected
    and triggers SectionBanner's graceful fallback (BANR-03); do not add a generic-SVG fallback.
  </action>
  <verify>
    <automated>grep -c "import SectionBanner" src/pages/Results.jsx ; grep -c "tier=\"city\"\|tier=\"state\"\|tier=\"federal\"" src/pages/Results.jsx ; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import SectionBanner" src/pages/Results.jsx` returns 1.
    - `src/pages/Results.jsx` contains `tier="city"`, `tier="state"`, and `tier="federal"` (grep all three present).
    - The file contains `buildingImageMap.Local`, `buildingImageMap.State`, and `buildingImageMap.Federal` each passed as an `imageUrl` (grep all three present).
    - No `tier="school"` banner is rendered (grep returns 0).
    - The federal banner uses literal `locationName="United States"` (grep matches).
    - `npm run build` exits 0 (production build succeeds with the new import + JSX).
  </acceptance_criteria>
  <done>SectionBanner renders before City, State, and Federal tier groups with location-aware image + label and graceful fallback; School folds under City; the production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| address/results data → banner label | `representingCity` / `userState` / `STATE_NAMES` flow into the rendered banner title |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-04 | Tampering | banner `locationName` from city/state data | accept | React escapes text by default; `imageUrl` comes only from the repo-controlled `buildingImages.js` map — no user-supplied image URLs. |
| T-170-05 | Denial of Service | dropping the IntersectionObserver scroll-spy | accept | Removal reduces work; banners are static inline dividers (D-04, not sticky) — no new observers or listeners added. |
| T-170-SC | Tampering | npm installs | mitigate | No new packages this plan. No install task → no slopcheck gate needed. |
</threat_model>

<verification>
- `npm run build` exits 0.
- `npx eslint src/pages/Results.jsx` exits 0.
- `grep` confirms SectionBanner is imported, the three tier banners are present, School has none, and selectedFilter/scroll-spy are fully removed.
</verification>

<success_criteria>
- Results renders City → State → Federal in one continuous scroll with a SectionBanner before each tier group; School folds under City (BANR-02).
- Each banner is location-aware from buildingImageMap with graceful gradient fallback when art is null (BANR-03).
- The tier sort control and all selectedFilter-dependent code are gone with no dead branches; Type filter + name search preserved (NAV-01).
- Production build and lint pass.
</success_criteria>

<output>
Create `.planning/phases/170-section-banners-continuous-scroll-results/170-03-SUMMARY.md` when done
</output>
