---
phase: 73-or-government-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql
autonomous: true
requirements: []

must_haves:
  truths:
    - "7 chambers exist under State of Oregon government (geo_id='41'): Governor, Oregon Senate, Oregon House of Representatives, Attorney General, Secretary of State, State Treasurer, Labor Commissioner (per D-02)"
    - "All 7 chambers have is_appointed_position=false — all voter-elected (per D-03)"
    - "No new essentials.governments row inserted — State of Oregon already exists (per D-01)"
    - "All 7 chamber slugs are auto-generated by GENERATED ALWAYS — never inserted manually (per D-04)"
    - "Migration is idempotent — re-running produces no errors and no duplicate chamber rows"
    - "essentials.chambers count under State of Oregon government_id equals exactly 7 after migration applies (count gate)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql"
      provides: "Migration creating 7 OR chamber rows under the existing State of Oregon government"
      contains: "State of Oregon"
  key_links:
    - from: "essentials.chambers (7 new rows)"
      to: "essentials.governments (State of Oregon, pre-existing)"
      via: "government_id foreign key resolved via subquery by name"
      pattern: "SELECT id FROM essentials.governments WHERE name = 'State of Oregon'"
---

<objective>
Create migration 222 that seeds 7 chamber rows under the pre-existing State of Oregon government row (geo_id='41'). This is the foundation every subsequent OR phase (74 through 77) depends on — Phase 74 executives need Governor/AG/SoS/Treasurer/Labor Commissioner chambers; Phase 75 legislature needs Oregon Senate + Oregon House of Representatives chambers; Phase 77 city seeding references the OR government structure.

Per D-01, the State of Oregon government row already exists (Phase 72 confirmed geo_id='41' boundaries; the governments row was pre-seeded). Phase 73 creates chambers only — no government row INSERT, no politicians, no offices, no districts.

Per D-03, all 7 chambers are is_appointed_position=false (voter-elected statewide in Oregon — this differs from ME where AG/SoS/Treasurer were legislature-elected). The is_appointed_position field will be set on offices in Phase 74; chambers themselves don't carry this flag (only offices do), so this constraint is informational for the downstream phase.

Purpose: Phases 74-77 all need chamber_id references pointing at Oregon chamber rows. Nothing can be seeded until this migration runs.
Output: C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql — applied and verified.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-or-government-db/73-CONTEXT.md
@.planning/phases/50-me-government-db/50-01-PLAN.md
@.planning/phases/50-me-government-db/50-01-SUMMARY.md
@.planning/phases/72-portland-or/72-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 222 SQL (7 OR chambers under State of Oregon)</name>
  <files>C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql</files>
  <read_first>
    - C:/Transparent Motivations/essentials/.planning/phases/73-or-government-db/73-CONTEXT.md (locked decisions D-01..D-05)
    - C:/Transparent Motivations/essentials/.planning/phases/50-me-government-db/50-01-PLAN.md (analog ME chambers pattern — exact SQL structure to replicate)
    - C:/Transparent Motivations/essentials/.planning/phases/50-me-government-db/50-01-SUMMARY.md (confirmed pattern + idempotency outcomes)
    - C:/EV-Accounts/backend/migrations/ (directory listing — confirm 222 is next free number; 221_sj_stances.sql is already taken)
  </read_first>
  <action>
Create C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql containing 7 INSERT statements — ONE per chamber — that seed chambers under the existing State of Oregon government row. NO government row INSERT (per D-01). NO politicians, offices, or districts (those are Phases 74+).

CRITICAL CONSTRAINTS (replicate exactly from Phase 50 ME pattern):

1. The file MUST be wrapped in a single BEGIN; ... COMMIT; transaction.

2. Every INSERT into essentials.chambers MUST:
   - List columns exactly as: (id, name, name_formal, government_id) — NO slug column (slug is GENERATED ALWAYS; including it WILL error per D-04).
   - Use gen_random_uuid() for id.
   - Resolve government_id via subquery: (SELECT id FROM essentials.governments WHERE name = 'State of Oregon') — do NOT hardcode the UUID (per D-01 canonical pattern).
   - Be guarded by WHERE NOT EXISTS (...) clause keyed on (chamber name + government_id subquery resolving 'State of Oregon') for idempotency.

3. The 7 chamber rows to create (per D-02) — use these EXACT name AND name_formal values:
   - Governor (name='Governor', name_formal='Governor of Oregon')
   - Oregon Senate (name='Oregon Senate', name_formal='Oregon Senate')
   - Oregon House of Representatives (name='Oregon House of Representatives', name_formal='Oregon House of Representatives')
   - Attorney General (name='Attorney General', name_formal='Attorney General of Oregon')
   - Secretary of State (name='Secretary of State', name_formal='Oregon Secretary of State')
   - State Treasurer (name='State Treasurer', name_formal='Oregon State Treasurer')
   - Labor Commissioner (name='Labor Commissioner', name_formal='Oregon Labor Commissioner')

   Note: ME used the "Maine X" full-name pattern for the chamber name field (e.g. 'Maine Governor'), but CA used short names ('Governor', 'Attorney General') with name_formal carrying the state-qualified form. OR follows the CA short-name convention because CA is the most recent precedent (see STATE.md "CA chamber short names" entry). Use the short names above for the `name` column.

4. The migration must be IDEMPOTENT: re-running it MUST produce zero new rows and zero errors. The WHERE NOT EXISTS guard pattern from Phase 50 handles this — replicate exactly.

5. Add a SQL header comment block at the top of the file that documents:
   - Migration purpose (7 OR chambers under existing State of Oregon government row)
   - That State of Oregon row already exists (no government INSERT)
   - That slug is GENERATED ALWAYS — never include in INSERT
   - That essentials.governments has no unique constraint on geo_id, but Phase 73 does NOT insert into governments (only chambers)
   - That all 7 chambers will receive is_appointed_position=false on their downstream offices (per D-03)
   - That all INSERTs are idempotent via WHERE NOT EXISTS guards

6. Reference D-05 in the file header: the migration number 222 is used because the CONTEXT.md was written before migration 221_sj_stances.sql landed; 222 is the actual next free number as of plan creation (verified via directory listing).

DO NOT include any other migration content. DO NOT create essentials.governments INSERTs. DO NOT create politicians/offices/districts INSERTs.
  </action>
  <verify>
    <automated>powershell -Command "$f='C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql'; if (-not (Test-Path $f)) { exit 1 }; $c = Get-Content $f -Raw; $inserts = ([regex]::Matches($c, 'INSERT INTO essentials\.chambers')).Count; $govInserts = ([regex]::Matches($c, 'INSERT INTO essentials\.governments')).Count; $slug = ([regex]::Matches($c, '(?im)^[^-]*INSERT INTO essentials\.chambers[^;]*\bslug\b')).Count; $whereNotExists = ([regex]::Matches($c, 'WHERE NOT EXISTS')).Count; $stateOfOregon = ([regex]::Matches($c, ""State of Oregon"")).Count; if ($inserts -ne 7) { Write-Host 'FAIL chamber INSERT count'; exit 2 }; if ($govInserts -ne 0) { Write-Host 'FAIL no government INSERTs allowed'; exit 3 }; if ($slug -gt 0) { Write-Host 'FAIL slug column found in INSERT'; exit 4 }; if ($whereNotExists -lt 7) { Write-Host 'FAIL need 7 WHERE NOT EXISTS guards'; exit 5 }; if ($stateOfOregon -lt 14) { Write-Host 'FAIL State of Oregon refs (need at least 14: 7 INSERTs + 7 NOT EXISTS subqueries)'; exit 6 }; Write-Host 'PASS file structure'; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql exists on disk
    - File contains exactly 7 occurrences of "INSERT INTO essentials.chambers"
    - File contains exactly 0 occurrences of "INSERT INTO essentials.governments" (D-01: no government row INSERT)
    - File contains exactly 7 occurrences of "WHERE NOT EXISTS" (one per chamber INSERT for idempotency)
    - File contains the literal string "State of Oregon" at least 14 times (once per INSERT subquery + once per WHERE NOT EXISTS guard subquery × 7 chambers)
    - No INSERT INTO essentials.chambers statement contains a "slug" column reference (D-04: slug is GENERATED ALWAYS — including it will error)
    - File contains a BEGIN; ... COMMIT; transaction wrapper
    - All 7 chamber `name` values are present as exact-string matches: 'Governor', 'Oregon Senate', 'Oregon House of Representatives', 'Attorney General', 'Secretary of State', 'State Treasurer', 'Labor Commissioner'
    - All 7 `gen_random_uuid()` calls are present (one per chamber)
    - Subquery pattern "SELECT id FROM essentials.governments WHERE name = 'State of Oregon'" appears at least 14 times (once per INSERT government_id resolution + once per NOT EXISTS guard subquery)
  </acceptance_criteria>
  <done>File C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql exists with all 7 chamber INSERTs, no government INSERT, no slug column, all WHERE NOT EXISTS guards in place, all 7 exact chamber names present.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 222 and verify count gate + idempotency</name>
  <files>C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql (the file just written in Task 1)
    - C:/Transparent Motivations/essentials/.planning/phases/50-me-government-db/50-01-PLAN.md (Task 2 apply + verify pattern)
    - C:/EV-Accounts/backend/.env (location of DATABASE_URL; do NOT print or commit the contents)
  </read_first>
  <action>
Apply migration 222 to the live Supabase Postgres database and verify the chamber count gate. Run all psql commands from C:/EV-Accounts/backend (dotenv requires .env in cwd).

Step 1 — Pre-flight: confirm State of Oregon government row exists. If this query returns 0 rows, STOP and raise the issue — Phase 73 explicitly assumes the row already exists (D-01) and the migration will silently no-op for all 7 chambers because the subquery returns NULL:
  SELECT id, name, type, state, geo_id FROM essentials.governments WHERE name = 'State of Oregon';
  -- Expected: exactly 1 row, state='OR', geo_id='41', type='STATE'

Step 2 — Apply migration:
  cd C:/EV-Accounts/backend
  psql "$env:DATABASE_URL" -f migrations/222_or_government_chambers.sql
  (Use the DATABASE_URL from C:/EV-Accounts/backend/.env. If psql is not on PATH, use the supabase MCP tool with apply_migration.)

Step 3 — Verify the 7 chamber rows landed under State of Oregon:
  SELECT c.name, c.name_formal, c.slug
  FROM essentials.chambers c
  JOIN essentials.governments g ON c.government_id = g.id
  WHERE g.name = 'State of Oregon'
  ORDER BY c.name;
  -- Expected: exactly 7 rows with names ordered alphabetically:
  --   Attorney General
  --   Governor
  --   Labor Commissioner
  --   Oregon House of Representatives
  --   Oregon Senate
  --   Secretary of State
  --   State Treasurer
  -- All 7 slug columns must be non-NULL (auto-generated by GENERATED ALWAYS)

Step 4 — Count gate (the must_haves "count gate" assertion):
  SELECT COUNT(*) AS or_chamber_count
  FROM essentials.chambers c
  JOIN essentials.governments g ON c.government_id = g.id
  WHERE g.name = 'State of Oregon';
  -- Expected: exactly 7

Step 5 — Slug non-null check:
  SELECT c.name, c.slug FROM essentials.chambers c
  JOIN essentials.governments g ON c.government_id = g.id
  WHERE g.name = 'State of Oregon' AND c.slug IS NULL;
  -- Expected: 0 rows

Step 6 — Idempotency check: re-apply the migration. psql must exit 0. Then re-run the count gate query from Step 4 — must STILL return exactly 7 (no duplicates, no new rows).

Step 7 — Capture the resolved slugs for the SUMMARY (Phase 74+ will need to look up chambers by either name or slug):
  SELECT name, slug FROM essentials.chambers c
  JOIN essentials.governments g ON c.government_id = g.id
  WHERE g.name = 'State of Oregon' ORDER BY name;
  -- Record the auto-generated slugs in the SUMMARY (expected pattern: 'governor', 'oregon-senate', 'oregon-house-of-representatives', 'attorney-general', 'secretary-of-state', 'state-treasurer', 'labor-commissioner' — but the GENERATED ALWAYS expression is authoritative)

Do NOT attempt to set slug values, hardcode UUIDs, or modify the government row.
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/EV-Accounts/backend'; $env:PGPASSWORD = $null; $url = (Get-Content .env | Select-String '^DATABASE_URL=' | ForEach-Object { ($_ -split '=',2)[1] }); if (-not $url) { Write-Host 'FAIL no DATABASE_URL in .env'; exit 1 }; $count = & psql $url -t -A -c ""SELECT COUNT(*) FROM essentials.chambers c JOIN essentials.governments g ON c.government_id = g.id WHERE g.name = 'State of Oregon';"" 2>&1; if ($LASTEXITCODE -ne 0) { Write-Host ""FAIL psql error: $count""; exit 2 }; $count = ($count | Out-String).Trim(); if ($count -ne '7') { Write-Host ""FAIL expected 7 chambers, got: $count""; exit 3 }; Write-Host 'PASS 7 chambers under State of Oregon'; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight Step 1: query returns exactly 1 row with name='State of Oregon', state='OR', geo_id='41'
    - Step 2 (apply migration): psql exits 0, no SQL errors emitted
    - Step 3 (chamber list): query returns exactly 7 rows in alphabetical order: Attorney General, Governor, Labor Commissioner, Oregon House of Representatives, Oregon Senate, Secretary of State, State Treasurer
    - Step 4 (count gate): COUNT(*) of chambers under State of Oregon government_id returns exactly 7
    - Step 5 (slug non-null): query returns 0 rows (every chamber has an auto-generated non-null slug)
    - Step 6 (idempotency): re-running the migration exits 0 with no errors; count gate still returns exactly 7 (no duplicates)
    - Step 7: the 7 generated slugs are captured for the SUMMARY (string values)
    - No row inserted into essentials.governments by this migration (governments row count for name='State of Oregon' before and after = 1)
  </acceptance_criteria>
  <done>
Migration 222 is applied. The database contains exactly 7 chamber rows linked to the pre-existing State of Oregon government row. All 7 slugs are auto-generated and non-null. Re-running the migration produces no errors and no duplicates. The 7 generated slugs have been recorded for inclusion in the SUMMARY for Phase 74+ chamber lookups.
  </done>
</task>

</tasks>

<verification>
Run these final checks after both tasks complete:

```sql
-- VG1: Government row unchanged (still exactly 1, not 2)
SELECT COUNT(*) FROM essentials.governments WHERE name = 'State of Oregon';
-- Must return 1

-- VG2: Chamber count gate (must_haves count gate truth)
SELECT COUNT(*) FROM essentials.chambers c
JOIN essentials.governments g ON c.government_id = g.id
WHERE g.name = 'State of Oregon';
-- Must return 7

-- VG3: All 7 expected chamber names present
SELECT c.name FROM essentials.chambers c
JOIN essentials.governments g ON c.government_id = g.id
WHERE g.name = 'State of Oregon'
ORDER BY c.name;
-- Must return: Attorney General, Governor, Labor Commissioner,
--              Oregon House of Representatives, Oregon Senate,
--              Secretary of State, State Treasurer

-- VG4: All slugs auto-generated (non-null)
SELECT COUNT(*) FROM essentials.chambers c
JOIN essentials.governments g ON c.government_id = g.id
WHERE g.name = 'State of Oregon' AND c.slug IS NULL;
-- Must return 0
```

Confirm the migration file exists at C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql.
</verification>

<success_criteria>
1. essentials.chambers contains exactly 7 rows linked to the State of Oregon government (count gate per phase success criterion 3)
2. The 7 chamber names match D-02 exactly: Governor, Oregon Senate, Oregon House of Representatives, Attorney General, Secretary of State, State Treasurer, Labor Commissioner
3. All 7 chamber slugs are non-null (auto-generated by GENERATED ALWAYS per D-04)
4. essentials.governments row count for State of Oregon remains exactly 1 (no government INSERT performed per D-01)
5. Migration file 222_or_government_chambers.sql exists on disk at C:/EV-Accounts/backend/migrations/
6. Re-running the migration produces no errors and no duplicate rows (idempotency per D-04 pattern)
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local SQL file → live Supabase Postgres | Migration text is run by a developer against a production database; misformed SQL or a wrong subquery could corrupt the State of Oregon row or seed duplicate chambers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | essentials.governments row for State of Oregon | accept | Migration contains zero INSERT/UPDATE statements against essentials.governments (verified by acceptance criterion "no INSERT INTO essentials.governments"); the row cannot be modified by this migration |
| T-73-02 | Tampering | Duplicate chamber rows on re-run | mitigate | Every chamber INSERT is guarded by WHERE NOT EXISTS on (name + government_id subquery); idempotency verified by Step 6 in Task 2 |
| T-73-03 | Denial of Service | Wrong government_id resolution (subquery returns NULL) | mitigate | Pre-flight Step 1 in Task 2 confirms 'State of Oregon' row exists before applying the migration; if it doesn't, the operator stops and resolves before proceeding (rather than silently inserting 7 chambers with NULL government_id) |
| T-73-04 | Information Disclosure | DATABASE_URL leakage | mitigate | Task 2 explicitly instructs operator not to print or commit .env contents; psql connection string is read from .env at runtime |
</threat_model>

<output>
After completion, create `.planning/phases/73-or-government-db/73-01-SUMMARY.md` following the summary template at @C:/Users/Chris/.claude/get-shit-done/templates/summary.md.

Key items to capture in the summary:
- State of Oregon government UUID (SELECT id FROM essentials.governments WHERE name = 'State of Oregon') — Phase 74+ will need this
- The 7 chamber names and their auto-generated slugs (recorded in Step 7 of Task 2)
- Confirmed migration number applied: 222
- Confirmed next migration: 223
- Note that CONTEXT.md (and STATE.md) listed next migration as 221, but 221 was taken by 221_sj_stances.sql before Phase 73 started — 222 is the actual next free number used here; update STATE.md accordingly
- Idempotency confirmation (re-run produced no duplicates)
</output>
