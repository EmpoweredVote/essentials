---
phase: 142-long-beach-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/878_long_beach_reconcile.sql
autonomous: true
requirements: [LBCH-01]

must_haves:
  truths:
    - "Long Beach government row carries geo_id 0643000 (no longer NULL)"
    - "Exactly one chamber named 'Long Beach City Council' remains; the Mayor's chamber is named 'Mayor of Long Beach' (split-section risk eliminated)"
    - "Each of the 9 existing officials has exactly one politician_images row (no duplicates)"
    - "Rex Richardson's politicians.office_id is non-NULL (bidirectional Mayor link)"
    - "The 8 council district labels read 'District 1'…'District 9' instead of 'At-Large'"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/878_long_beach_reconcile.sql"
      provides: "Idempotent data-hygiene migration for Long Beach"
      contains: "WHERE id = '867f4e3f"
  key_links:
    - from: "essentials.governments (5e5c3e0b-5479-4759-ac7e-2ea0aecabd38)"
      to: "geo_id '0643000'"
      via: "UPDATE ... WHERE id = '5e5c3e0b-5479-4759-ac7e-2ea0aecabd38'"
      pattern: "geo_id\\s*=\\s*'0643000'"
    - from: "essentials.politicians (external_id -200813, Rex Richardson)"
      to: "essentials.offices (Mayor office 06c1def0)"
      via: "UPDATE politicians.office_id"
      pattern: "office_id\\s*="
---

<objective>
Reconcile the existing partial Long Beach seed and fix all data-hygiene defects in a single
idempotent migration (878). This is the foundation wave — every later wave (roster, headshots,
stances) assumes a clean, unambiguous structure.

Five fixes, all DB-verified against the live state on 2026-06-19:
1. Backfill `essentials.governments.geo_id = '0643000'` on the Long Beach gov row (currently NULL) — D-07
2. Rename the Mayor's duplicate-named chamber to 'Mayor of Long Beach' to kill the split-section risk — D-08
3. Dedupe the 3 officials that have 2 `politician_images` rows down to 1 each — D-09
4. Back-fill `politicians.office_id` for Rex Richardson (Mayor bidirectional link is NULL) — D-10
5. Relabel the 8 existing council districts from 'At-Large' to 'District 1'…'District 9' for display accuracy
   (resolves Open Question 2 — label-only UPDATE, no geofence change, honors flat-district D-06)

Purpose: A clean structural base. Duplicate chamber names cause split-section render bugs; NULL geo_id
breaks routing; duplicate images render twice; the missing office back-link orphans the Mayor.
Output: Migration file 878 applied to production; all 5 post-verification queries green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-long-beach-deep-seed/142-CONTEXT.md
@.planning/phases/142-long-beach-deep-seed/142-RESEARCH.md
@.planning/phases/142-long-beach-deep-seed/142-VALIDATION.md

<interfaces>
<!-- DB-verified live values (2026-06-19) the executor uses directly — no re-derivation needed,
     but Task 1 confirms them with a pre-flight query before any write. -->

Government row:
  id   = '5e5c3e0b-5479-4759-ac7e-2ea0aecabd38'
  name = 'City of Long Beach, California, US'
  geo_id = NULL  → set to '0643000'

Chambers (both currently named 'Long Beach City Council'):
  2109e716-127f-4d0d-8ec5-7bf77d503e03  → 8 council offices  → KEEP NAME
  867f4e3f-2be9-4d55-955d-796a3e5bc0a8  → 1 Mayor (LOCAL_EXEC) office → RENAME to 'Mayor of Long Beach'
  (chambers.slug is a GENERATED column — NEVER include slug in any write; it auto-recomputes on rename)

Mayor:
  Rex Richardson  external_id = -200813   politicians.office_id = NULL
  Mayor office UUID = '06c1def0-1f1b-4b9a-97a3-dc2903083edc'  (back-fill office_id to this)

Existing council politicians (external_id → district):
  665830 Mary Zendejas D1 · 665831 Cindy Allen D2 · 665833 Kristina Duggan D3 ·
  665834 Daryl Supernaw D4 · 665835 Megan Kerr D5 · 665838 Suely Saro D6 ·
  665839 Roberto Uranga D7 · 665842 Joni Ricks-Oddie D9
  (D8 is the missing seat — seated in Plan 02, NOT here)

Duplicate-image officials (2 rows each → delete the scraped_no_license one by its specific id):
  Cindy Allen 665831 · Megan Kerr 665835 · Roberto Uranga 665839

District labels (8 council LOCAL districts) currently 'At-Large' → set to 'District N' per the
district_id each office links to. Office→district mapping is read from essentials.offices.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state</name>
  <files>C:/EV-Accounts/backend/migrations/878_long_beach_reconcile.sql</files>
  <read_first>
    - .planning/phases/142-long-beach-deep-seed/142-RESEARCH.md (§Verified Existing DB State, §Common Pitfalls 1/6)
    - .planning/phases/142-long-beach-deep-seed/142-CONTEXT.md (§decisions D-07…D-10)
    - C:/EV-Accounts/backend/migrations/294_la_wave1_long_beach.sql (the direct predecessor — read first)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE, run read-only confirmation queries via mcp__supabase-local
    execute_sql (production) or psql -f using C:/EV-Accounts/backend/.env DATABASE_URL: (a) confirm
    governments id 5e5c3e0b-5479-4759-ac7e-2ea0aecabd38 has geo_id NULL; (b) confirm exactly two
    chambers under that government both named 'Long Beach City Council', with ids 2109e716… (8 offices)
    and 867f4e3f… (1 Mayor office); (c) confirm Rex Richardson external_id -200813 has office_id NULL
    and that office 06c1def0… is his Mayor office; (d) confirm external_ids 665831/665835/665839 each
    have 2 politician_images rows and capture the specific id + photo_license of each row; (e) read the
    office→district_id mapping for the 8 council offices so you know which district_id maps to which
    District number (D1–D9). If any precondition does not match RESEARCH.md, STOP and report the
    discrepancy — do not write a migration against a moved target.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id FROM essentials.governments WHERE id='5e5c3e0b-5479-4759-ac7e-2ea0aecabd38';" — returns one row, geo_id NULL (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - All 5 pre-flight queries executed; results captured in the executor's notes
    - Two chambers confirmed sharing the name 'Long Beach City Council' with the expected ids
    - The specific politician_images id to DELETE for each of 665831/665835/665839 is identified (the scraped_no_license row)
    - The office→District-number mapping for all 8 council offices is recorded
    - Any mismatch vs RESEARCH.md is surfaced and execution paused (no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches RESEARCH.md; exact ids for the dedupe DELETEs and the district relabels are known.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 878 (reconcile / data hygiene)</name>
  <files>C:/EV-Accounts/backend/migrations/878_long_beach_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/294_la_wave1_long_beach.sql (idempotent geo_id backfill pattern)
    - .planning/phases/142-long-beach-deep-seed/142-RESEARCH.md (§Pattern 4 chamber rename, §Code Examples dedupe + split-section, §Common Pitfalls 1/6/7)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA' uppercase)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/878_long_beach_reconcile.sql, all statements idempotent,
    using the captured ids from Task 1. (1) UPDATE essentials.governments SET geo_id='0643000' WHERE
    id='5e5c3e0b-5479-4759-ac7e-2ea0aecabd38' AND geo_id IS NULL (D-07). (2) UPDATE essentials.chambers
    SET name='Mayor of Long Beach', name_formal='Mayor of Long Beach' WHERE
    id='867f4e3f-2be9-4d55-955d-796a3e5bc0a8' — target by ID ONLY, never by name (Pitfall 1); do NOT
    touch the slug column, it is GENERATED (D-08). (3) For each of external_ids 665831, 665835, 665839:
    DELETE the duplicate politician_images row by its specific id captured in Task 1 (prefer deleting the
    scraped_no_license row, keep press_use); scope by id, never bulk-delete by politician_id (Pitfall 6)
    (D-09). (4) UPDATE essentials.politicians SET office_id='06c1def0-1f1b-4b9a-97a3-dc2903083edc' WHERE
    external_id=-200813 AND office_id IS NULL (D-10). (5) For the 8 council districts, UPDATE
    essentials.districts SET label='District N' (matching D1…D9 per the office→district mapping from
    Task 1) — label-only, no geo_id/geofence change, preserving flat-district D-06 (resolves Open Q2).
    End the file with the post-verification queries from RESEARCH §Code Examples (split-section check +
    chamber-name listing). Apply via mcp__supabase-local execute_sql or psql -f. This IS a structural
    migration — it registers in schema_migrations normally (it is NOT a stance migration).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id FROM essentials.governments WHERE id='5e5c3e0b-5479-4759-ac7e-2ea0aecabd38';" returns '0643000' AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.chambers WHERE government_id='5e5c3e0b-5479-4759-ac7e-2ea0aecabd38' AND name='Long Beach City Council';" returns 1</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='5e5c3e0b...'` → '0643000'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='5e5c3e0b...' AND name='Long Beach City Council'` → 1 (not 0, not 2)
    - A chamber named 'Mayor of Long Beach' exists under the government
    - `SELECT politician_id, COUNT(*) FROM essentials.politician_images WHERE politician_id IN (ids of 665831,665835,665839) GROUP BY 1 HAVING COUNT(*)>1` → 0 rows
    - `SELECT office_id FROM essentials.politicians WHERE external_id=-200813` → non-NULL (= 06c1def0…)
    - The 8 council districts read 'District 1'…'District 9' (no remaining 'At-Large' on a council district)
    - split-section check SQL from RESEARCH §Code Examples returns 0 rows
    - Migration is idempotent: a second apply changes 0 rows and raises no error
  </acceptance_criteria>
  <done>Migration 878 applied; all six post-verification queries return expected results; re-running the file is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator (psql/MCP) directly against production Supabase. No untrusted user input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering | Chamber rename targets wrong/both chambers | mitigate | UPDATE keyed on id '867f4e3f…' only, never on name (two chambers share the name); post-verify chamber count = 1 named 'Long Beach City Council' |
| T-142-02 | Tampering | Image dedupe deletes both rows for a person | mitigate | DELETE scoped to the specific captured row id; post-verify exactly 1 image row per affected official |
| T-142-03 | Tampering | Non-idempotent migration applied twice corrupts state | mitigate | All writes guarded (geo_id IS NULL, office_id IS NULL, id-scoped deletes); second apply = 0 rows changed |
| T-142-04 | Tampering | Accidental write to production without verifying pre-state | mitigate | Task 1 read-only pre-flight confirms live state matches RESEARCH before any write; STOP-on-mismatch |
| T-142-05 | Tampering | SQL injection via literal text | accept | No user input; all literals are operator-authored UUIDs/strings; not a realistic vector for a data-seed migration |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 878:
1. gov geo_id = '0643000'
2. exactly 1 chamber 'Long Beach City Council' + 1 chamber 'Mayor of Long Beach'
3. 0 officials with >1 image among 665831/665835/665839
4. Rex Richardson office_id non-NULL
5. council district labels = 'District 1'…'District 9'
6. feedback_section_split_check SQL → 0 rows
</verification>

<success_criteria>
All five data-hygiene defects fixed and independently verified; migration is idempotent; the Long
Beach structure is unambiguous and ready for roster completion in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/142-long-beach-deep-seed/142-01-SUMMARY.md` when done.
</output>
