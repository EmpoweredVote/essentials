---
phase: 164-north-las-vegas-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-north-las-vegas-ward-boundaries.ts
  - C:/EV-Accounts/backend/migrations/1093_north_las_vegas_city_council.sql
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [CLARK-04]
user_setup: []

must_haves:
  truths:
    - "Wave-0 BLOCKING probes confirm (before any write): live on-disk migration MAX (highest file +1, trust on-disk over the registered ledger — expected 1092 → next structural 1093, NOT 1091 from stale notes); external_id block -3207001..-3207005 has 0 existing rows; the ward MTFCC X0017 has 0 existing geofence_boundaries rows; NLV G4110 geo_id='3251800' + its geofence_boundaries casing (state='32' FIPS) [D-04, D-04b]"
    - "A Wave-0 operator checkpoint verifies the live roster + seat count (Mayor Goynes-Brown + Ward 1 Barrón / Ward 2 Garcia-Anderson / Ward 3 Black / Ward 4 Cherchio = 5 seats) before the structural migration applies [specifics]"
    - "4 NLV ward boundary polygons load into essentials.geofence_boundaries as X0017 rows (state='nv' lowercase, geo_id='north-las-vegas-nv-council-ward-1'..'-4') from the Clark County GISMO PoliticalBoundaries/MapServer/5 layer filtered where=PLACE=80, each ST_IsValid=true; Wards 1 and 2 (7 rings each) handled via ST_Multi(ST_MakeValid(...)) [D-01]"
    - "A standalone government 'City of North Las Vegas, Nevada, US' exists (type='City', state='NV' uppercase, geo_id='3251800'), NOT nested under State of Nevada geo_id='32' [D-04]"
    - "A chamber 'North Las Vegas City Council' (official_count=5) exists under that government [D-03]"
    - "Mayor Pamela Goynes-Brown (external_id -3207001) has a LOCAL_EXEC office on the city-wide G4110 district (geo_id='3251800'), title='Mayor', directly elected NOT rotational [D-03]"
    - "Each of the 4 ward council members has a LOCAL office on its per-ward X0017 district (geo_id='north-las-vegas-nv-council-ward-N'), title='Council Member, Ward 1..4' (ARABIC numerals — NLV convention, NOT Roman), external_id block -3207002..-3207005 [D-01]"
    - "Post-verify: exactly 1 LOCAL_EXEC office + exactly 4 LOCAL ward offices; office_id back-filled for all 5; zero section-split; Scott Black is_active=true is_incumbent=true (seated Ward 3 despite advancing to Nov 2026 mayoral runoff — the Carrie Cox parallel); Garcia-Anderson is_appointed=false (won full term Nov 2024); accent kept on 'Barrón'"
    - "City of North Las Vegas surfaces with the purple hasContext chip in src/lib/coverage.js COVERAGE_STATES (appended to the EXISTING Nevada block — LV + Henderson already present) [CLARK-04 SC#4]"
    - "D-01b single-city fallback NOT needed (clean ward polygons confirmed in RESEARCH) but kept as the documented safety net: if Wave-0 cannot source clean ward polygons, fall back to single-CITY-district model (all members on the one NLV G4110 geofence, ward-labeled), document ward-precise routing as deferred — phase still completes [D-01b]"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-north-las-vegas-ward-boundaries.ts"
      provides: "ArcGIS-JSON ward-polygon loader: fetch 4 NLV wards from maps.clarkcountynv.gov OpenData/PoliticalBoundaries MapServer Layer 5 (where=PLACE%3D80, outFields=WARD,NAME,PLACE, f=json, outSR=4326, resultRecordCount=100), convert rings→GeoJSON Polygon, INSERT geofence_boundaries as X0017 with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON(...),4326)), assert ST_IsValid, ST_MakeValid fallback, EXPECTED_COUNT=4"
      contains: "X0017"
    - path: "C:/EV-Accounts/backend/migrations/1093_north_las_vegas_city_council.sql"
      provides: "Structural migration: pre-flight (asserts 4 X0017 geofences) + 1 government + 1 chamber + 1 LOCAL_EXEC district (Mayor) + 4 LOCAL ward districts + 5 politician+office CTEs + office_id back-fill + post-verify DO block; registers as 1093 (or Wave-0-confirmed number)"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 200
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "COVERAGE_STATES Nevada block with North Las Vegas appended (browseGovernmentList:['3251800'], browseStateAbbrev:'NV', hasContext:true), LV + Henderson entries preserved"
      contains: "North Las Vegas"
  key_links:
    - from: "essentials.geofence_boundaries (4 X0017 ward polygons)"
      to: "essentials.districts (4 LOCAL ward rows, geo_id='north-las-vegas-nv-council-ward-N')"
      via: "shared geo_id; backend ST_Covers PIP + essentialsService.ts line 646 X% catchall routes X0017 to LOCAL (no backend edit)"
      pattern: "north-las-vegas-nv-council-ward-"
    - from: "essentials.chambers (North Las Vegas City Council)"
      to: "essentials.governments (City of North Las Vegas, Nevada, US, geo_id='3251800')"
      via: "government_id subquery WHERE name='City of North Las Vegas, Nevada, US'"
      pattern: "City of North Las Vegas, Nevada, US"
    - from: "essentials.offices (Mayor + 4 ward members)"
      to: "essentials.districts (1 LOCAL_EXEC at geo_id='3251800' + 4 LOCAL at X0017 wards, all state='nv')"
      via: "district join on geo_id + district_type + state='nv' lowercase"
      pattern: "state = 'nv'"
    - from: "src/lib/coverage.js COVERAGE_STATES Nevada block"
      to: "North Las Vegas browse (geo_id 3251800)"
      via: "browseGovernmentList array entry"
      pattern: "3251800"
---

<objective>
Seed the City of North Las Vegas structurally and make its 4 wards routable, mirroring the proven Phase 163 (Henderson) shape verbatim. First a Wave-0 BLOCKING probe confirms the live migration MAX (trust highest on-disk file +1, NOT the stale "1091" note — Phase 149 landed 1091/1092, so the next structural number is 1093; this is the v18.0-park drift the CONTEXT warned about, now materialized), the unused external_id block -3207001..-3207005, X0017 availability, and the NLV G4110 city geo_id='3251800' + its geofence casing. A Wave-0 operator checkpoint then verifies the live roster + seat count (5: Mayor Goynes-Brown + Ward 1 Barrón / Ward 2 Garcia-Anderson / Ward 3 Black / Ward 4 Cherchio) against cityofnorthlasvegas.com / Ballotpedia BEFORE any migration applies. Next, load the 4 NLV ward boundary polygons from the SHARED Clark County GISMO PoliticalBoundaries/MapServer/5 layer (filtered where=PLACE=80 — the layer holds all 3 valley cities' wards) into essentials.geofence_boundaries as X0017 rows (D-01). Then create a standalone government "City of North Las Vegas, Nevada, US" (NOT nested under State of Nevada geo_id='32' — D-04), one chamber "North Las Vegas City Council" (official_count=5 — D-03), one LOCAL_EXEC district for the directly-elected Mayor (Goynes-Brown) on the existing city-wide G4110 geofence (geo_id='3251800'), four LOCAL ward districts on the X0017 ward polygons, all 5 politician+office rows (ARABIC numeral ward titles — NOT Roman), and back-fill office_id. Finally append North Las Vegas to the EXISTING Nevada block in src/lib/coverage.js COVERAGE_STATES (LV + Henderson already present).

Purpose: Satisfies the structural half of CLARK-04 (Mayor + ward-routed council via the 4 ward polygons) plus the surfacing half (SC#4 — purple hasContext chip). Headshots are Plan 02; stances + final E2E verification are Plan 03.
Output: load-north-las-vegas-ward-boundaries.ts, 1093_north_las_vegas_city_council.sql (structural, registered), the coverage.js NV-block edit, plus the captured Wave-0 probe facts + the 5 external_id→UUID map recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and NO DB access):**
- The executor WRITES load-north-las-vegas-ward-boundaries.ts, the migration .sql, and the coverage.js edit to disk and documents the exact run/apply steps. The executor does NOT run the loader, run DB SELECTs, or apply migrations.
- The Wave-0 DB probes, the roster operator checkpoint, running the ward loader (`npx tsx scripts/load-north-las-vegas-ward-boundaries.ts` from the EV-Accounts repo root so it reads C:/EV-Accounts/backend/.env DATABASE_URL), applying mig 1093 (via `psql -f` using the EV-Accounts/backend/.env DATABASE_URL), and every post-apply audit SELECT are INLINE ORCHESTRATOR steps performed by execute-phase.
- Each task below marks which steps are executor-write vs inline-orchestrator-apply.
- The loader + migration live in the SEPARATE repo C:/EV-Accounts/backend/ (commit via `git -C "C:/EV-Accounts"`, always from the repo root — never cd into backend). The coverage.js edit lives in the essentials repo. SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/164-north-las-vegas-deep-seed/164-RESEARCH.md
@.planning/phases/164-north-las-vegas-deep-seed/164-PATTERNS.md
@.planning/phases/164-north-las-vegas-deep-seed/164-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
NLV city geo_id (TIGER G4110 place): '3251800' (confirmed Phase 158 smoke test)
Government: name='City of North Las Vegas, Nevada, US', type='City', state='NV' UPPERCASE, city=NULL, geo_id='3251800' (standalone, NOT under State of Nevada geo_id='32'). governments has NO unique constraint on geo_id or name — WHERE NOT EXISTS guard mandatory.
Chamber: name='North Las Vegas City Council', name_formal='North Las Vegas City Council', official_count=5
Ward geofence source (SHARED Clark County GISMO layer — NOT a per-city MapServer): https://maps.clarkcountynv.gov/arcgis/rest/services/OpenData/PoliticalBoundaries/MapServer/5/query?where=PLACE%3D80&outFields=WARD,NAME,PLACE&returnGeometry=true&f=json&outSR=4326&resultOffset=0&resultRecordCount=100 (ArcGIS JSON rings, NOT GeoJSON; outSR=4326 mandatory — default CRS is projected NV State Plane; where=PLACE%3D80 MANDATORY — the layer holds all 3 valley cities' wards: 60=Henderson, 65=LV, 80=NLV — without it 139 features incl. precinct fragments return)
Ward attribute parsing: WARD is esriFieldTypeSmallInteger 1..4 (already an int — parseInt(String(rawWard ?? ''),10) still safe); NAME = "1 - Isaac E. Barrón" etc. (informational only); use `Ward ${ward}` (ARABIC) as the geofence display name — NLV does NOT use Roman numerals like Henderson
Ward geofence rows: geofence_boundaries (id, geo_id, mtfcc, state, name, geometry, source); mtfcc='X0017' (Wave-0 confirms unclaimed; X0015=LV, X0016=Henderson; use next X-code if taken), state='nv' LOWERCASE, geo_id='north-las-vegas-nv-council-ward-N' (N=1..4, Arabic), source='clarkcountynv.gov-gismo-politicalboundaries-ward-place80-2026'; ON CONFLICT (geo_id, mtfcc) DO NOTHING
Ward ring counts (confirmed live): Ward 1=7 rings, Ward 2=7 rings, Ward 3=1, Ward 4=1 — wrap with ST_Multi; assert ST_IsValid=true in RETURNING (apply ST_MakeValid if false)
EXPECTED_COUNT=4 (NLV has 4 wards; Henderson had 4; LV had 6)
Mayor district: 1 LOCAL_EXEC row, state='nv' lowercase, geo_id='3251800', label='City of North Las Vegas', mtfcc='G4110' (uses EXISTING Phase 158 G4110 geofence; idempotent guard only — no new geofence row)
Ward districts: 4 LOCAL rows, state='nv' lowercase, geo_id='north-las-vegas-nv-council-ward-N', label='Ward 1'..'Ward 4' (ARABIC numerals — NLV official naming), mtfcc='X0017'
Office district join keys: Mayor → d.geo_id='3251800' AND d.district_type='LOCAL_EXEC' AND d.state='nv'; ward N → d.geo_id='north-las-vegas-nv-council-ward-N' AND d.district_type='LOCAL' AND d.state='nv' (uppercase 'NV' matches ZERO rows — silent no-op, the #1 failure mode)
offices.representing_state: 'NV' UPPERCASE (free-text label column, NOT the district join key)
Office NOT EXISTS guard: (district_id, politician_id) — one person per office
chambers auto-generated path column is GENERATED ALWAYS — never include it in the INSERT column list; name_formal must NOT be ''
Roster + external_ids (party='Non-Partisan' all — antipartisan, never displayed): -3207001 Pamela Goynes-Brown (Mayor, directly elected at-large, is_appointed=false, term-limited but seated through Nov 30 2026 — NOT rotational); -3207002 Isaac E. Barrón (Ward 1, KEEP accent); -3207003 Ruth Garcia-Anderson (Ward 2, is_appointed=false — won full term Nov 2024 by 9 votes); -3207004 Scott Black (Ward 3, is_active=true is_incumbent=true — seated despite advancing to Nov 2026 mayoral runoff, the Carrie Cox parallel); -3207005 Richard Cherchio (Ward 4, re-elected unopposed 2024)
Migration: structural number 1093 (on-disk MAX confirmed 1092 — Phase 149 landed 1091/1092 after the Henderson "next=1091" note; DO NOT use 1091/1092); registers in the ledger. Wave-0 re-verifies the live on-disk MAX and uses highest-file +1.
Backend routing: X0017 caught automatically by essentialsService.ts line 646 X% catchall (X0017 NOT in exclusion list X0001-X0004) — NO backend code change
Grep-gate forbidden tokens in .sql comments: paraphrase the auto-generated path column (→ "the auto-generated path column"), the removed image-origin column; the ledger table token is acceptable ONLY in the actual registration INSERT, not in comments
Casing convention: districts.state / geofence_boundaries.state = 'nv' lowercase (routing join key — 'NV' = 0 rows); governments.state / offices.representing_state = 'NV' uppercase. The G4110 TIGER place row the Mayor attaches to is state='32' (FIPS) per Phase 158; the LOCAL_EXEC district row is still state='nv'.
Browse link: essentials.empowered.vote/results?browse_geo_id=3251800&browse_mtfcc=G4110
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave-0 BLOCKING DB probes + roster/seat operator checkpoint (inline orchestrator)</name>
  <read_first>
    - .planning/phases/164-north-las-vegas-deep-seed/164-RESEARCH.md (§External_id Collision Analysis; §Custom MTFCC Registry; §Migration Counter — DRIFT MATERIALIZED; §North Las Vegas City geo_id; §Validation Architecture Wave 0 Gaps)
    - .planning/phases/164-north-las-vegas-deep-seed/164-VALIDATION.md (Wave 0 Requirements — the exact probe queries)
    - .planning/phases/163-henderson-deep-seed/163-01-SUMMARY.md (the Henderson Wave-0 drift lesson — on-disk MAX vs registered ledger)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor — these need DB/filesystem access). Run all four BLOCKING probes and the roster checkpoint BEFORE the executor's ward-loader/migration tasks proceed. Record every result in the SUMMARY.
(a) Live on-disk migration MAX: `ls C:/EV-Accounts/backend/migrations | grep -oE '^[0-9]+' | sort -n | tail -1` — trust the highest on-disk file +1 as the structural migration number (expected 1092 → 1093; DO NOT use the stale 1091 note — Phase 149 already landed 1091_seed_ca_2026_house_candidates.sql + 1092_phase149_dedup_redistricted_incumbents.sql). ALSO run `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` (expected lower — only structural migs register; the on-disk file number is the authority, NOT the registered ledger). If the on-disk MAX differs from 1092, renumber the structural migration (1093) and ALL downstream audit-only migrations (Plan 02 headshot 1094, Plan 03 stances 1095-1099) by the same offset and note it.
(b) external_id collision probe: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -3207005 AND -3207001` — expect 0. If non-zero, pick the next unused -3207xxx block and propagate to every downstream file.
(c) MTFCC existence probe: `SELECT DISTINCT mtfcc FROM essentials.geofence_boundaries WHERE mtfcc LIKE 'X%' ORDER BY mtfcc` — confirm X0017 is unclaimed (X0015 = LV, X0016 = Henderson). If X0017 is taken, use the next unclaimed X-code and propagate to the loader + migration + post-verify.
(d) NLV G4110 geo_id + casing: `SELECT geo_id, state FROM essentials.geofence_boundaries WHERE name ILIKE '%North Las Vegas%' AND mtfcc='G4110'` — expect geo_id='3251800', state='32' (TIGER FIPS string). Confirms the Mayor / single-city-fallback attach point exists.
Then read the CURRENT coverage.js NV block to confirm both Las Vegas (3240000) and Henderson (3231900) entries are present (Plan 01 Task 4 appends NLV after them).
Then the ROSTER OPERATOR CHECKPOINT: verify the live NLV roster + seat count against cityofnorthlasvegas.com (Akamai WAF-403 — use Ballotpedia / current news if the official site is blocked) — Mayor Pamela Goynes-Brown + Ward 1 Isaac E. Barrón + Ward 2 Ruth Garcia-Anderson + Ward 3 Scott Black + Ward 4 Richard Cherchio = 5 seats. Confirm Scott Black remains the seated Ward 3 incumbent (advanced to the Nov 2026 mayoral runoff but serves until the outcome/inauguration — the Carrie Cox parallel), and Garcia-Anderson is now ELECTED (won a full term Nov 2024, not merely appointed). Pause for operator confirmation before the migration applies. If the roster has shifted, adjust names/ext_ids before writing the migration.
This task GATES the structural migration (Task 3): do not apply mig 1093 until all four probes pass and the roster is operator-confirmed.
  </action>
  <verify><human-check>Operator confirms: on-disk migration MAX recorded (structural number = MAX+1, expected 1093 NOT 1091); external_id block -3207001..-3207005 returns 0 rows; X0017 returns 0 rows; NLV G4110 geo_id='3251800' state='32' confirmed; coverage.js NV block has LV + Henderson; and the 5-seat roster (Goynes-Brown + Barrón/Garcia-Anderson/Black/Cherchio) verified against the live source with Black still seated + Garcia-Anderson elected.</human-check></verify>
  <acceptance_criteria>
    - On-disk migration MAX recorded in SUMMARY; structural number set to MAX+1 (expected 1093); any drift offset applied to all downstream files.
    - external_id probe recorded = 0 for -3207005..-3207001 (or a new block chosen + propagated).
    - MTFCC probe recorded: X0017 unclaimed (or next X-code chosen + propagated to loader/migration/post-verify).
    - G4110 probe recorded: geo_id='3251800', state='32'.
    - coverage.js NV block confirmed to currently hold Las Vegas + Henderson.
    - Roster + seat count operator-verified against the live source = 5 seats; Black confirmed still seated; Garcia-Anderson confirmed elected (is_appointed=false).
    - This checkpoint is recorded as passed BEFORE mig 1093 applies.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded probe values + roster confirmation, or describe any collision (pick a new block/X-code), drift (renumber), or roster change (adjust names/ext_ids).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write + run the NLV ward-boundary loader (X0017, 4 wards, where=PLACE=80, D-01b fallback)</name>
  <files>C:/EV-Accounts/backend/scripts/load-north-las-vegas-ward-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-henderson-ward-boundaries.ts (EXACT ANALOG — copy nearly verbatim; only the constants block + the WARD/wardName parse change. fetchJson, arcgisRingsToGeoJson, the Pool setup, the INSERT, and the ST_MakeValid fallback copy unchanged)
    - .planning/phases/164-north-las-vegas-deep-seed/164-PATTERNS.md (section "load-north-las-vegas-ward-boundaries.ts" — the constants block, the SmallInteger WARD parsing, the INSERT pattern, the summary verify statement)
    - .planning/phases/164-north-las-vegas-deep-seed/164-RESEARCH.md (§Ward Geofence Source — endpoint + attribute schema; Pitfall 2 missing where=PLACE=80; Pitfall 8 multi-ring Wards 1+2 = 7 rings each)
  </read_first>
  <action>
EXECUTOR STEP: write `load-north-las-vegas-ward-boundaries.ts` by adapting `load-henderson-ward-boundaries.ts`. Copy verbatim: imports + Pool, fetchJson, arcgisRingsToGeoJson, the INSERT-with-ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON(...),4326)) + ON CONFLICT (geo_id, mtfcc) DO NOTHING + ST_IsValid RETURNING + ST_MakeValid fallback, the error handler. Replace the constants block with: NLV_WARD_URL = the maps.clarkcountynv.gov/arcgis/rest/services/OpenData/PoliticalBoundaries/MapServer/5/query URL (where=PLACE%3D80 — MANDATORY, the layer holds all 3 valley cities' wards keyed by PLACE 60/65/80; outFields=WARD,NAME,PLACE; returnGeometry=true; f=json — NOT geojson; outSR=4326 — MANDATORY, default CRS is projected NV State Plane; resultOffset=0; resultRecordCount=100); MTFCC='X0017' (or the Wave-0-confirmed code); STATE_CODE='nv'; SOURCE='clarkcountynv.gov-gismo-politicalboundaries-ward-place80-2026'; GEO_ID_PREFIX='north-las-vegas-nv-council-ward-'; EXPECTED_COUNT=4. Adapt the ward-attribute extraction: WARD is esriFieldTypeSmallInteger here (already an int) — use parseInt(String(rawWard ?? ''),10), guard isNaN + range 1..EXPECTED_COUNT; the geofence display name is `Ward ${ward}` using ARABIC numerals (NLV does NOT use Henderson's Roman "WARD I" form); geo_id uses Arabic numerals (north-las-vegas-nv-council-ward-1..4). Adapt the summary log to expect (4, true). Support --dry-run. Comment the where=PLACE=80 / outSR=4326 / f=json criticality inline.

INLINE ORCHESTRATOR STEP: run `npx tsx scripts/load-north-las-vegas-ward-boundaries.ts --dry-run` then without the flag, from the C:/EV-Accounts repo root (so it reads backend/.env DATABASE_URL). Confirm it reports exactly 4 wards inserted, all ST_IsValid=true. Then verify with `SELECT COUNT(*), bool_and(public.ST_IsValid(geometry)) FROM essentials.geofence_boundaries WHERE state='nv' AND mtfcc='X0017'` → expect (4, true). Record in SUMMARY.

D-01b FALLBACK (do NOT hard-block; NOT expected — clean polygons confirmed in RESEARCH): if the loader cannot source clean ward polygons (endpoint down, <4 valid features after ST_MakeValid) OR Wave-0 confirmed NLV elects purely at-large, SKIP the X0017 ward geofences and proceed with the single-CITY-district model — in Task 3, attach ALL 4 council members to the one NLV G4110 district (geo_id='3251800'), each ward-labeled, and document ward-precise routing as deferred. The phase still completes. Record which path was taken in the SUMMARY.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/load-north-las-vegas-ward-boundaries.ts"; test -f "$F" &amp;&amp; grep -q "X0017" "$F" &amp;&amp; grep -q "PLACE%3D80" "$F" &amp;&amp; grep -q "outSR=4326" "$F" &amp;&amp; grep -q "north-las-vegas-nv-council-ward-" "$F" &amp;&amp; grep -q "EXPECTED_COUNT" "$F" &amp;&amp; grep -q "ST_Multi" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - load-north-las-vegas-ward-boundaries.ts exists; copies the Henderson loader structure verbatim except the constants block + ward-attribute parsing.
    - URL includes where=PLACE%3D80, outSR=4326, f=json (NOT geojson), resultRecordCount=100, MapServer Layer 5 of OpenData/PoliticalBoundaries.
    - MTFCC='X0017' (or Wave-0 code); GEO_ID_PREFIX='north-las-vegas-nv-council-ward-'; EXPECTED_COUNT=4; STATE_CODE='nv'.
    - WARD parsed via parseInt(String(...)) with isNaN/range guard; display name `Ward ${ward}` (Arabic, NOT Roman).
    - INLINE: loader run → 4 X0017 ward rows in geofence_boundaries, `SELECT COUNT(*), bool_and(ST_IsValid)` = (4, true), recorded in SUMMARY.
    - Wards 1 and 2 (7 rings each) ST_IsValid=true (ST_MakeValid applied if needed).
    - OR (D-01b path): fallback documented in SUMMARY, single-city-district model selected, phase not blocked.
  </acceptance_criteria>
  <done>Ward loader written + run; 4 X0017 ward polygons loaded ST_IsValid (or D-01b fallback documented). Recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Write structural migration 1093 + apply inline + post-verify</name>
  <files>C:/EV-Accounts/backend/migrations/1093_north_las_vegas_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1084_henderson_city_council.sql (EXACT ANALOG — the 7-step skeleton: pre-flight → government → chamber → LOCAL_EXEC district → LOCAL ward districts → politician+office CTEs → office_id back-fill → post-verify DO block → ledger registration outside COMMIT)
    - .planning/phases/164-north-las-vegas-deep-seed/164-PATTERNS.md (section "1093_north_las_vegas_city_council.sql" — the complete Henderson→NLV substitution map + every step's adapted SQL + the roster table)
    - .planning/phases/164-north-las-vegas-deep-seed/164-RESEARCH.md (Pattern 2 structural migration; Code Examples — Mayor/Ward CTEs + Post-Verify DO block lines 678-723; Pitfalls 4 Black-incumbent + 5 Garcia-Anderson-elected + 6 Arabic numerals + 7 Barrón accent + 8 two-district-types/casing)
  </read_first>
  <action>
EXECUTOR STEP: write `1093_north_las_vegas_city_council.sql` modeled verbatim on 1084_henderson_city_council.sql, applying the Henderson→NLV substitution map from 164-PATTERNS.md. Steps:
- Pre-flight DO block: NOTICE if the NLV government already exists (idempotent re-run); RAISE EXCEPTION if fewer than 4 X0017 ward geofences exist (`< 4` ... mtfcc='X0017' AND state='nv') — "run load-north-las-vegas-ward-boundaries.ts first". (Under the D-01b single-city fallback, REMOVE this X0017 assertion and instead assert the G4110 geo_id='3251800' geofence exists.)
- Step 1 Government: INSERT 'City of North Las Vegas, Nevada, US', type='City', state='NV', city=NULL, geo_id='3251800' WHERE NOT EXISTS (mandatory — no unique constraint).
- Step 2 Chamber: INSERT 'North Las Vegas City Council' / name_formal='North Las Vegas City Council' / government_id subquery / official_count=5 WHERE NOT EXISTS. NEVER include the auto-generated path column in the INSERT list.
- Step 3a LOCAL_EXEC district (Mayor): INSERT district_type='LOCAL_EXEC', state='nv' lowercase, geo_id='3251800', label='City of North Las Vegas', mtfcc='G4110' WHERE NOT EXISTS (uses the existing Phase 158 G4110 geofence — no new geofence row).
- Step 3b LOCAL ward districts: 4 INSERTs, district_type='LOCAL', state='nv', geo_id='north-las-vegas-nv-council-ward-1..4', label='Ward 1'..'Ward 4' (ARABIC numerals — NLV official naming, NOT Henderson's Roman 'Ward I'), mtfcc='X0017', each WHERE NOT EXISTS. (Under D-01b: skip these; all 4 members attach to the city G4110 district instead per the 164-CONTEXT D-01b fallback model.)
- Step 4 politician+office CTEs (5 blocks): BLOCK 1 Mayor Goynes-Brown (-3207001) title='Mayor' on the LOCAL_EXEC district (geo_id='3251800', district_type='LOCAL_EXEC', state='nv'); BLOCKS 2-5 ward members on their LOCAL X0017 districts — Barrón (-3207002, KEEP accent in full_name='Isaac E. Barrón' / last_name='Barrón') 'Council Member, Ward 1'; Garcia-Anderson (-3207003, is_appointed=false — won full term Nov 2024) 'Council Member, Ward 2'; Black (-3207004, is_active=true is_incumbent=true — seated despite Nov 2026 mayoral runoff) 'Council Member, Ward 3'; Cherchio (-3207005, re-elected unopposed 2024) 'Council Member, Ward 4'. Each block: WITH ins_p INSERT politicians (party='Non-Partisan', is_active=true, is_vacant=false, is_incumbent=true, external_id) ON CONFLICT (external_id) DO NOTHING RETURNING id → INSERT offices (chamber_id subquery, representing_state='NV' uppercase, role_canonical=NULL) joined on the district by geo_id+district_type+state='nv' with NOT EXISTS (district_id, politician_id) guard. Mayor uses district_type='LOCAL_EXEC' (NOT 'LOCAL').
- Step 5 office_id back-fill: UPDATE politicians SET office_id=o.id WHERE external_id BETWEEN -3207005 AND -3207001 AND office_id IS NULL.
- Step 6 post-verify DO block (RAISE EXCEPTION on mismatch): exactly 1 NLV government row; exactly 1 LOCAL_EXEC office (Mayor, geo_id='3251800'); exactly 4 LOCAL X0017 ward offices (under D-01b: assert all 5 offices resolve to the G4110 district instead); section-split detector (0 orphan X0017 geofence rows lacking a matching LOCAL district). The exact DO-block shape is in 164-RESEARCH.md lines 678-715. COMMIT.
- Step 7 ledger registration (OUTSIDE the transaction COMMIT): INSERT the version '1093' (or Wave-0 number) + name 'north_las_vegas_city_council' ON CONFLICT (version) DO NOTHING.
GREP-GATE HYGIENE: keep the auto-generated path column literal out of comments (say "the auto-generated path column"); keep the removed image-origin column literal out (N/A here); the ledger table token appears only in the Step 7 INSERT, never in a comment.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1093_north_las_vegas_city_council.sql"; test -f "$F" &amp;&amp; grep -q "City of North Las Vegas, Nevada, US" "$F" &amp;&amp; grep -q "INSERT INTO essentials.chambers" "$F" &amp;&amp; grep -q "North Las Vegas City Council" "$F" &amp;&amp; grep -q "LOCAL_EXEC" "$F" &amp;&amp; grep -q -- "-3207001" "$F" &amp;&amp; grep -q -- "-3207005" "$F" &amp;&amp; grep -q "official_count" "$F" &amp;&amp; grep -q "X0017" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1093_north_las_vegas_city_council.sql exists, ≥200 lines, follows the 1084 7-step skeleton.
    - Pre-flight asserts ≥4 X0017 ward geofences (or, under D-01b, asserts the G4110 geofence).
    - Government 'City of North Las Vegas, Nevada, US' (state='NV', geo_id='3251800') with WHERE NOT EXISTS; standalone (NOT under geo_id='32').
    - Chamber 'North Las Vegas City Council' official_count=5; the auto-generated path column NOT in the INSERT list; name_formal non-empty.
    - 1 LOCAL_EXEC district (geo_id='3251800', state='nv') for the Mayor; 4 LOCAL X0017 ward districts (state='nv', labels 'Ward 1'..'Ward 4' Arabic).
    - 5 politician+office blocks: Mayor Goynes-Brown (-3207001, LOCAL_EXEC, title='Mayor') + 4 ward members (-3207002..-3207005) with Arabic-numeral titles; Black is_active=true is_incumbent=true; Garcia-Anderson is_appointed=false; Barrón accent preserved; all party='Non-Partisan'; representing_state='NV'.
    - office_id back-filled BETWEEN -3207005 AND -3207001.
    - Post-verify DO block asserts gov=1, exec=1, local=4, section-split=0.
    - Ledger registration line present OUTSIDE the COMMIT (version 1093 / 'north_las_vegas_city_council'); no forbidden tokens in comments.
    - INLINE: applied; post-verify PASSED; the 5 external_id→UUID map recorded in SUMMARY.
  </acceptance_criteria>
  <done>Structural migration written + applied; post-verify PASSED (gov=1, exec=1, local=4, split=0); external_id→UUID map captured in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 4: Append North Las Vegas to coverage.js Nevada block</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (lines ~183-189 — the existing Nevada block with the Las Vegas + Henderson entries; NOTE line ~124 is a DIFFERENT "Nevada" — a TX city — do NOT confuse it with the NV state block)
    - .planning/phases/164-north-las-vegas-deep-seed/164-PATTERNS.md (section "src/lib/coverage.js" — before/after of the NV block)
  </read_first>
  <action>
EXECUTOR STEP: edit src/lib/coverage.js — locate the existing Nevada block in COVERAGE_STATES (the one already containing the Las Vegas AND Henderson entries from Phases 162/163 — abbrev 'NV', NOT the TX city "Nevada" near line 124) and APPEND a North Las Vegas entry to its `areas` array, after Henderson: `{ label: 'North Las Vegas', browseGovernmentList: ['3251800'], browseStateAbbrev: 'NV', hasContext: true }`. Keep the Las Vegas (3240000) and Henderson (3231900) entries intact. browseGovernmentList uses the CITY geo_id ('3251800'), NOT the ward geo_ids. Do NOT create a second Nevada block and do NOT touch Landing.jsx (surfacing target is coverage.js).
  </action>
  <verify>
    <automated>F="C:/Transparent Motivations/essentials/src/lib/coverage.js"; test -f "$F" &amp;&amp; grep -q "North Las Vegas" "$F" &amp;&amp; grep -q "3251800" "$F" &amp;&amp; grep -q "Henderson" "$F" &amp;&amp; grep -q "3231900" "$F" &amp;&amp; grep -q "3240000" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - coverage.js Nevada block contains Las Vegas (3240000), Henderson (3231900), AND North Las Vegas (3251800) entries.
    - North Las Vegas entry: label 'North Las Vegas', browseGovernmentList ['3251800'], browseStateAbbrev 'NV', hasContext: true.
    - Exactly one NV state block (no duplicate); the TX "Nevada" city entry untouched; Landing.jsx untouched.
  </acceptance_criteria>
  <done>North Las Vegas appended to the existing coverage.js Nevada block with hasContext:true; LV + Henderson entries preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Clark County GISMO MapServer → loader | public regional GIS fetch crosses into DB geometry rows; validate content (where=PLACE=80 → exactly 4 features, ST_IsValid) before commit |
| generated .sql migration → live DB | service-role apply of seed SQL; only the inline orchestrator applies, after review |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Tampering | external_id / MTFCC collision corrupting existing rows | mitigate | Wave-0 BLOCKING probes (Task 1) confirm -3207001..-3207005 and X0017 are unclaimed before any write; ON CONFLICT DO NOTHING guards |
| T-164-02 | Tampering | wrong migration number (v18.0-park drift: 1091/1092 already landed) overwriting/skipping | mitigate | Wave-0 probe (a) trusts highest on-disk file +1 (1093), not the stale 1091 note or just schema_migrations; renumber + offset downstream if further drift |
| T-164-03 | Tampering | wrong-city or junk ward geometry (missing where=PLACE=80 returns all 3 cities + 125 precinct fragments) or invalid multi-ring (Wards 1+2 = 7 rings) | mitigate | where=PLACE%3D80 filter + EXPECTED_COUNT=4 assertion; ST_Multi(ST_MakeValid) + ST_IsValid RETURNING; D-01b single-city fallback if unsourceable |
| T-164-04 | Tampering | silent no-op from uppercase state='NV' on routing join | mitigate | districts/geofence state='nv' lowercase enforced; post-verify asserts 1 LOCAL_EXEC + 4 LOCAL offices exist (would be 0 on casing bug) |
| T-164-05 | Information disclosure | GIS endpoint serving unexpected content | accept | County-operated public open-data service, no auth/secrets; content validated (feature count + geometry) before insert |
| T-164-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit = N/A; all tooling pre-installed). No high-severity threats; data-seed phase. |
</threat_model>

<verification>
Structural + surfacing halves of CLARK-04:
- SC#1 (structural): 4 X0017 ward polygons ST_IsValid + 1 government + 1 chamber (official_count=5) + 1 LOCAL_EXEC Mayor office + 4 LOCAL ward offices, all state='nv', section-split=0 (Task 2 loader verify + Task 3 post-verify DO block). D-01b fallback path documented if needed.
- SC#4 (surfacing): North Las Vegas in coverage.js with hasContext:true alongside Las Vegas + Henderson (Task 4).
- Cross-cutting: Wave-0 probes confirm no collision/drift; structural migration registered at 1093 (or Wave-0 number); external_id→UUID map captured for Plans 02/03.
</verification>

<success_criteria>
- Wave-0 probes + roster checkpoint recorded and passed before any write; on-disk MAX → structural number set (expected 1093).
- load-north-las-vegas-ward-boundaries.ts written + run; 4 X0017 ward polygons ST_IsValid (or D-01b single-city fallback documented, phase not blocked).
- 1093_north_las_vegas_city_council.sql written + applied; post-verify PASSED (gov=1, exec=1 Mayor LOCAL_EXEC, local=4 ward, section-split=0); registered in the ledger.
- 5 external_id→UUID map recorded in SUMMARY (consumed by Plans 02/03).
- North Las Vegas surfaces in coverage.js Nevada block (hasContext:true), LV + Henderson preserved.
</success_criteria>

<output>
Create `.planning/phases/164-north-las-vegas-deep-seed/164-01-SUMMARY.md` when done (record the Wave-0 probe values + roster confirmation, which ward path was taken (X0017 vs D-01b), the post-verify results, the 5 external_id→UUID map, and the coverage.js edit).
</output>
