---
phase: 83-multnomah-county-government-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql
  - C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts
autonomous: true
requirements: [COUNTY-01, COUNTY-02, ROUTING-01]

must_haves:
  truths:
    - "A 'Multnomah County, Oregon, US' government row exists in essentials.governments with geo_id='41051', type='County', state='OR'"
    - "A 'Board of Commissioners' chamber exists under that government row"
    - "A COUNTY essentials.districts row exists with geo_id='41051', mtfcc='G4020', state='or' (lowercase)"
    - "All 5 commissioner officials (Chair + 4 district commissioners) are seeded with external_ids -410001, -410010, -410011, -410012, -410013 and have offices linked to the COUNTY district"
    - "A Portland City Hall coordinate (-122.6794, 45.5231) returns 5 COUNTY commissioner records via geofence_boundaries + districts + offices JOIN"
    - "An unincorporated Multnomah County coordinate (Corbett OR area) returns 5 COUNTY commissioner records and ZERO G4110 city boundary rows"
    - "Section-split check returns 0 orphan rows for geo_id='41051'"
    - "Migration is idempotent — re-running inserts 0 additional government, chamber, district, politician, or office rows"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql"
      provides: "Government + chamber + COUNTY district + 5 politicians + 5 offices + office_id back-fill + ledger entry"
      contains: "Multnomah County, Oregon, US"
    - path: "C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts"
      provides: "Routing smoke test verifying Portland + unincorporated coordinates"
      exports: ["main"]
  key_links:
    - from: "essentials.offices.district_id"
      to: "essentials.districts.id (geo_id='41051', mtfcc='G4020', state='or')"
      via: "FK join inside migration; verified by section-split detector"
      pattern: "JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = '41051'"
    - from: "essentials.geofence_boundaries (geo_id='41051', mtfcc='G4020', loaded Phase 72)"
      to: "essentials.districts (geo_id='41051', COUNTY, state='or')"
      via: "geo_id JOIN in districtQueryText"
      pattern: "JOIN essentials.districts d ON d.geo_id = gb.geo_id AND d.district_type = 'COUNTY'"
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id"
      via: "back-fill UPDATE at end of migration"
      pattern: "UPDATE essentials.politicians SET office_id = o.id ... WHERE p.external_id BETWEEN -410013 AND -410001"
---

<objective>
Seed the Multnomah County, Oregon government body — government row, Board of Commissioners chamber, COUNTY district, 5 elected officials (Chair Jessica Vega Pederson + 4 district commissioners Moyer/Singleton/Brim-Edwards/Jones-Dixon), and offices linked to the county geo_id — and verify routing works for both Portland (city + county) and unincorporated Multnomah County (county only, no LOCAL) addresses.

Purpose: COUNTY-01 (government body created), COUNTY-02 (5 commissioners + offices linked to geo_id=41051), and ROUTING-01 (unincorporated addresses see county→state→federal reps with no empty LOCAL city section) all depend on a single migration plus a smoke test. The G4020 geofence for geo_id=41051 was already loaded in Phase 72; this plan adds the essentials.districts row and government data so the existing routing code (essentialsService.ts districtQueryText) returns county commissioners for any Multnomah County address.

Output: Migration 244 applied to production Supabase via Supabase MCP; smoke test passes 4 success-criteria assertions.

Source artifact reconciliation: Roadmap success criteria text says "5 commissioners + chair = 6 seats" but RESEARCH.md (verified from multco.us on 2026-05-31) confirms the actual Board of Commissioners is 1 Chair + 4 District Commissioners = 5 total seats. Plan implements the verified 5-seat structure; the "6" in the roadmap is a count error from the requirements draft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/83-multnomah-county-government-routing/83-RESEARCH.md
@.planning/phases/83-multnomah-county-government-routing/83-PATTERNS.md

# Reference migrations (read first; these are the source-of-truth patterns)
@C:/EV-Accounts/backend/migrations/087_tx_schema_geo_id_state_county.sql
@C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql
@C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql
@C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql
@C:/EV-Accounts/backend/migrations/231_portland_officials.sql

# Reference smoke test (source-of-truth pattern)
@C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts
@C:/EV-Accounts/backend/scripts/smoke-portland-council-geofences.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 244 (government + chamber + COUNTY district + 5 officials + offices)</name>
  <files>C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/087_tx_schema_geo_id_state_county.sql (county government INSERT pattern, type='County')
    - C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql (OR chamber INSERT with WHERE NOT EXISTS guard; slug NEVER in column list)
    - C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql (district INSERT pattern; politician + office CTE block)
    - C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql (government row with WHERE NOT EXISTS; state casing convention)
    - C:/EV-Accounts/backend/migrations/231_portland_officials.sql (canonical 5-row politician/office CTE pattern; office_id back-fill; ledger entry; lines 40-69, 597-602, 607-610)
    - .planning/phases/83-multnomah-county-government-routing/83-RESEARCH.md (officials table, external_id scheme, pitfalls)
    - .planning/phases/83-multnomah-county-government-routing/83-PATTERNS.md (full inline SQL snippets to replicate verbatim)
  </read_first>
  <action>
    Create migration file 244_multnomah_county_government.sql at C:/EV-Accounts/backend/migrations/.

    Implements COUNTY-01 and COUNTY-02. Structure (single BEGIN/COMMIT transaction):

    1. File header comment: state "Migration 244: Multnomah County government + chamber + COUNTY district + 5 officials + offices" and document the 3 CRITICAL constraints (slug GENERATED ALWAYS; governments has no unique constraint on geo_id; districts.state='or' lowercase vs governments.state='OR' uppercase).

    2. Pre-flight DO block: RAISE NOTICE if government row 'Multnomah County, Oregon, US' already exists.

    3. Government INSERT (per 230_portland_government_structure.sql lines 39-46 pattern):
       INSERT INTO essentials.governments (id, name, type, state, city, geo_id)
       SELECT gen_random_uuid(), 'Multnomah County, Oregon, US', 'County', 'OR', NULL, '41051'
       WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name = 'Multnomah County, Oregon, US');

    4. Chamber INSERT (per 222_or_government_chambers.sql lines 54-64 pattern). NEVER include slug. Columns: id, name, name_formal, government_id. Values: gen_random_uuid(), 'Board of Commissioners', 'Multnomah County Board of Commissioners', subquery for government_id. WHERE NOT EXISTS guard on (name, government_id).

    5. COUNTY district INSERT (per 83-PATTERNS.md lines 105-115):
       INSERT INTO essentials.districts (id, district_type, state, geo_id, label, mtfcc)
       SELECT gen_random_uuid(), 'COUNTY', 'or', '41051', 'Multnomah County', 'G4020'
       WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE geo_id = '41051' AND district_type = 'COUNTY' AND state = 'or');
       state MUST be lowercase 'or' (per Pitfall 1). mtfcc='G4020' matches the existing geofence_boundaries row from Phase 72.

    6. Five politician + office CTE blocks (per 231_portland_officials.sql lines 40-69 pattern). One block per official:

       | external_id | full_name | first_name | last_name | office title |
       |-------------|-----------|------------|-----------|---------------|
       | -410001 | Jessica Vega Pederson | Jessica | Vega Pederson | County Chair |
       | -410010 | Meghan Moyer | Meghan | Moyer | Commissioner (District 1) |
       | -410011 | Shannon Singleton | Shannon | Singleton | Commissioner (District 2) |
       | -410012 | Julia Brim-Edwards | Julia | Brim-Edwards | Commissioner (District 3) |
       | -410013 | Vince Jones-Dixon | Vince | Jones-Dixon | Commissioner (District 4) |

       Each block:
       - politician INSERT: party=NULL (antipartisan), is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, ON CONFLICT (external_id) DO NOTHING RETURNING id
       - office INSERT FROM districts d CROSS JOIN ins_p p: district_id=d.id (WHERE geo_id='41051' AND district_type='COUNTY' AND state='or'), chamber_id=subquery for Board of Commissioners, politician_id=p.id, representing_state='OR' (uppercase per offices convention), is_appointed_position=false, is_vacant=false, role_canonical=NULL. WHERE NOT EXISTS guard on (district_id, politician_id) idempotency key.

    7. office_id back-fill (per 231_portland_officials.sql lines 597-602):
       UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o
       WHERE o.politician_id = p.id AND p.external_id BETWEEN -410013 AND -410001 AND p.office_id IS NULL;

    8. Post-verification DO block — must RAISE EXCEPTION on any failure. Three gates:
       (a) SELECT COUNT(*) FROM essentials.governments WHERE name = 'Multnomah County, Oregon, US' must equal 1.
       (b) SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON o.district_id=d.id WHERE d.geo_id='41051' AND d.district_type='COUNTY' AND d.state='or' must equal 5.
       (c) Section-split detector (RESEARCH lines 213-226): SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id='41051' AND gb.mtfcc='G4020' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.district_type='COUNTY' AND d.state='or') must equal 0.

    9. Supabase migration ledger entry:
       INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('244') ON CONFLICT (version) DO NOTHING;

    10. COMMIT;

    Pre-flight verification BEFORE applying: query SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-410001, -410010, -410011, -410012, -410013) — must return 0 (external_id range clear, per Assumption A3 in RESEARCH).

    Pre-flight verification BEFORE applying: query SELECT geo_id, name, mtfcc, state FROM essentials.geofence_boundaries WHERE geo_id = '41051' AND mtfcc = 'G4020' — must return 1 row with name like 'Multnomah County' and state='41' (confirms Phase 72 load is intact).
  </action>
  <verify>
    <automated>
      Apply migration via Supabase MCP, then run three SQL gates:
      (1) SELECT COUNT(*) FROM essentials.governments WHERE name = 'Multnomah County, Oregon, US' returns 1;
      (2) SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON o.district_id=d.id WHERE d.geo_id='41051' AND d.district_type='COUNTY' AND d.state='or' returns 5;
      (3) Section-split detector returns 0.
      All three gates must pass — the migration's embedded DO block already enforces them on apply.
    </automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql exists
    - File contains the string `'Multnomah County, Oregon, US'`
    - File contains the string `Board of Commissioners`
    - File contains exactly 5 occurrences of `essentials.politicians` INSERT statements with `external_id` values -410001, -410010, -410011, -410012, -410013 (one per official)
    - File contains the string `district_type, state, geo_id, label, mtfcc` and the literal `'COUNTY', 'or', '41051', 'Multnomah County', 'G4020'` (state must be lowercase 'or')
    - File contains `WHERE NOT EXISTS` guard on governments INSERT (no ON CONFLICT)
    - File does NOT include the column `slug` in any chamber INSERT column list
    - File contains `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('244')`
    - After apply: SELECT COUNT(*) FROM essentials.governments WHERE name='Multnomah County, Oregon, US' returns exactly 1
    - After apply: SELECT COUNT(*) FROM essentials.chambers WHERE name='Board of Commissioners' AND government_id=(SELECT id FROM essentials.governments WHERE name='Multnomah County, Oregon, US') returns exactly 1
    - After apply: SELECT COUNT(*) FROM essentials.districts WHERE geo_id='41051' AND district_type='COUNTY' AND state='or' returns exactly 1
    - After apply: SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-410001,-410010,-410011,-410012,-410013) returns exactly 5
    - After apply: SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON o.district_id=d.id WHERE d.geo_id='41051' AND d.district_type='COUNTY' returns exactly 5
    - After apply: SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-410001,-410010,-410011,-410012,-410013) AND office_id IS NOT NULL returns exactly 5 (office_id back-fill complete)
    - Section-split detector (RESEARCH lines 213-226 SQL) returns 0 rows
    - Re-running migration is a no-op: each of the 5 COUNT queries above stays unchanged (idempotency)
    - supabase_migrations.schema_migrations contains row with version='244'
  </acceptance_criteria>
  <done>
    Migration 244 file exists, was successfully applied to production Supabase, all post-apply SQL gates return the expected counts, section-split detector returns 0 rows, and idempotency confirmed by a second apply being a no-op.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Multnomah County routing smoke test</name>
  <files>C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts (canonical OR smoke test structure; AddressTest interface; geofence query; main() pattern; ALL ASSERTIONS PASSED output)
    - C:/EV-Accounts/backend/scripts/smoke-portland-council-geofences.ts (section-split check pattern; lines 119-140)
    - C:/EV-Accounts/backend/src/lib/essentialsService.ts (read districtQueryText to confirm G4020 → COUNTY mapping logic so the smoke test mirrors production routing)
    - .planning/phases/83-multnomah-county-government-routing/83-PATTERNS.md (lines 256-446 — full smoke test snippets including queryCountyOfficials, AddressTest data, final result block)
  </read_first>
  <action>
    Create smoke test file C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts. Implements ROUTING-01.

    Structure (mirror smoke-or-geofences.ts conventions):

    1. File header docstring stating the four assertions:
       - SC1: Portland City Hall (-122.6794, 45.5231) returns G4020 (41051) AND G4110 (4159000) boundaries
       - SC2: Portland City Hall returns exactly 5 COUNTY commissioner names via districts+offices+politicians JOIN
       - SC3: Unincorporated Multnomah County coordinate (Corbett OR area) returns G4020 (41051) but ZERO G4110 boundaries
       - SC4: Section-split check — geo_id='41051' has COUNTY district row (0 orphans)

    2. Imports: `import { Client } from 'pg'; import * as dotenv from 'dotenv'; dotenv.config();`

    3. AddressTest interface (per 83-PATTERNS.md lines 289-296).

    4. TEST_ADDRESSES array with 2 entries:
       - Portland City Hall: lon=-122.6794, lat=45.5231, expectedMtfcc=['G4020','G4110'], expectedGeoIds={G4020:'41051', G4110:'4159000'}
       - Corbett OR unincorporated: lon=-122.2, lat=45.5, expectedMtfcc=['G4020'], forbiddenMtfcc=['G4110']
       Both addresses must include label strings ('Portland OR City Hall', 'Corbett OR (unincorporated Multnomah County)').

    5. Helper queryBoundaries(client, lon, lat) returning array of {geo_id, name, mtfcc} — query essentials.geofence_boundaries WHERE state='41' AND ST_Covers(geometry, ST_SetSRID(ST_MakePoint($1,$2), 4326)) ORDER BY mtfcc. Verbatim from smoke-or-geofences.ts lines 63-77.

    6. Helper queryCountyOfficials(client, lon, lat) returning array of {full_name, district_type} — JOIN essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.geofence_boundaries gb ON gb.geo_id=d.geo_id WHERE gb.state='41' AND d.district_type='COUNTY' AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($1,$2), 4326)) ORDER BY p.full_name. (Pattern: 83-PATTERNS.md lines 349-369.)

    7. main():
       - Require DATABASE_URL; exit 1 with stderr message if not set.
       - Connect Client with ssl rejectUnauthorized:false.
       - Pre-flight: query the geofence_boundaries row for geo_id='41051' AND mtfcc='G4020' — exit 1 if not exactly 1 row (Phase 72 must be intact).
       - For Portland address: assert expectedMtfcc=['G4020','G4110'] all present; assert geo_ids match. Run queryCountyOfficials; assert returns exactly 5 rows; assert distinct full_name set matches {'Jessica Vega Pederson','Meghan Moyer','Shannon Singleton','Julia Brim-Edwards','Vince Jones-Dixon'}.
       - For Corbett address: assert G4020 present; assert no G4110 row in result. Run queryCountyOfficials; assert returns exactly 5 rows (same set as Portland).
       - SC4 block: run section-split detector SQL (verbatim from 83-PATTERNS.md lines 401-415); push error and set allPassed=false if any rows returned.
       - On any failure: collect into errors[]; print 'FAILED (N assertion(s))' with details; process.exit(1).
       - On success: print 'ALL ASSERTIONS PASSED' and the four SC labels; process.exit(0).
       - Use try/finally with client.end() to always close connection.

    8. Final pattern: `main().catch(err => { console.error('Smoke test error:', err); process.exit(1); });`

    Coordinate caveat: The Corbett coordinate (-122.2, 45.5) is marked [ASSUMED] in RESEARCH.md Assumption A1 and Open Question 3. The smoke test pre-flight MUST verify this coordinate actually returns G4020 (geo_id='41051') and not G4110. If the assumed coordinate falls inside any G4110 city boundary, fail fast with a clear error message and instruction to update TEST_ADDRESSES with a different unincorporated coordinate.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend ; npx tsx scripts/smoke-multnomah-county.ts</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts exists
    - File imports `Client` from `pg` and `dotenv`
    - File defines TEST_ADDRESSES array with at least 2 entries: one with label containing 'Portland' and coords (-122.6794, 45.5231); one with label containing 'Corbett' or 'unincorporated' and forbiddenMtfcc containing 'G4110'
    - File contains the function name `queryCountyOfficials` and that function returns rows from a JOIN of politicians, offices, districts, and geofence_boundaries filtered by district_type='COUNTY'
    - File contains an SC4 section-split check matching the SQL pattern (geo_id='41051', mtfcc='G4020', NOT EXISTS districts row)
    - Running `cd C:/EV-Accounts/backend ; npx tsx scripts/smoke-multnomah-county.ts` after migration 244 is applied exits with code 0
    - The script's stdout when passing contains the literal string `ALL ASSERTIONS PASSED`
    - The script's stdout when passing contains all four assertion labels (SC1, SC2, SC3, SC4) with `[PASS]` or equivalent
    - Running the smoke test BEFORE migration 244 is applied exits with non-zero code (proves the test actually detects the missing seed; can be confirmed mentally by reading the script — do not require literal pre-migration run)
    - The Portland coordinate assertion returns exactly 5 distinct commissioner names: Jessica Vega Pederson, Meghan Moyer, Shannon Singleton, Julia Brim-Edwards, Vince Jones-Dixon
    - The Corbett coordinate assertion confirms G4020 (geo_id='41051') is present and G4110 is absent
    - On any failure the script process.exit(1) with errors[] printed
  </acceptance_criteria>
  <done>
    Smoke test file exists, runs cleanly against production Supabase post-migration, exits 0, and prints all four SC labels as passing. The Corbett coordinate has been confirmed to fall in G4020 only (no G4110); if not, TEST_ADDRESSES has been updated with a verified unincorporated coordinate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → production Supabase | Migration writes to essentials schema with elevated DB privileges via Supabase MCP |
| public web → multco.us | Read-only fetch of public elected-officials data (no auth, no secrets); used by next plan, not this one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | migration 244 INSERT statements | mitigate | All INSERTs use WHERE NOT EXISTS or ON CONFLICT DO NOTHING; re-running migration is a no-op; section-split detector validates referential integrity inside the same transaction. |
| T-83-02 | Information disclosure | smoke test reading DATABASE_URL | accept | DATABASE_URL is a developer-local secret loaded via dotenv; smoke test never logs the connection string and exits non-zero if env var is unset. |
| T-83-03 | Denial of service | smoke test PostGIS ST_Covers query | accept | Two coordinates, two queries each; runtime ~10s; no loops, no large scans. Geofence_boundaries already indexed (state='41' filter pre-narrows). |
| T-83-04 | Elevation of privilege | direct DB write outside ledger | mitigate | Migration ledger entry inserted at end of transaction; if the migration fails, the ledger entry is also rolled back (single BEGIN/COMMIT). |
| T-83-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — only existing dependencies (`pg`, `dotenv`, `tsx`) already present in C:/EV-Accounts/backend/package.json. |
</threat_model>

<verification>
After Task 1 + Task 2 complete:

1. Migration ledger check: `SELECT * FROM supabase_migrations.schema_migrations WHERE version = '244'` returns 1 row.
2. Government, chamber, district, politician, office counts (see Task 1 acceptance criteria) all match expected values.
3. Section-split detector returns 0 rows for geo_id='41051'.
4. Smoke test passes: `cd C:/EV-Accounts/backend ; npx tsx scripts/smoke-multnomah-county.ts` exits 0 with all four SC labels marked PASS.
5. Re-apply migration 244 — all counts unchanged (idempotency).
</verification>

<success_criteria>
- COUNTY-01 satisfied: Multnomah County government row + Board of Commissioners chamber + COUNTY district row all exist in production essentials schema.
- COUNTY-02 satisfied: 5 politicians (external_ids -410001, -410010..-410013) seeded with offices linked to the COUNTY district (geo_id='41051').
- ROUTING-01 satisfied: smoke test confirms Portland City Hall returns G4020 + G4110 + 5 commissioners; unincorporated Multnomah coordinate returns G4020 + 5 commissioners + ZERO G4110 (no LOCAL city section).
- Phase 72 G4020 geofence boundary row remains intact (verified by pre-flight check).
- Migration is idempotent (re-running is no-op).
</success_criteria>

<output>
Create `.planning/phases/83-multnomah-county-government-routing/83-01-SUMMARY.md` when done. Include: migration number applied, all 5 politician_ids generated, all 5 office_ids, the COUNTY district_id, the government_id, the chamber_id, smoke test output (full stdout including ALL ASSERTIONS PASSED line), and any deviations from this plan (especially the Corbett coordinate if updated).
</output>
