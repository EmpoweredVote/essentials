---
phase: 22-compass-schema-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "STATE.md contains a 'Phase 22 Notes' section with all three audit findings documented"
    - "REQUIREMENTS.md shows AUDIT-01, AUDIT-02, and RETIRE-01 checked as complete"
    - "The retirement decision is explicitly stated as 'keep both' with reasoning in STATE.md"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Permanent record of scope mechanism, answer count, and retirement decision"
      contains: "compass_topic_roles"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 22 requirement checkboxes marked complete"
      contains: "[x] **AUDIT-01**"
  key_links:
    - from: ".planning/STATE.md Phase 22 Notes"
      to: "inform.compass_topic_roles"
      via: "documents the authoritative scope mechanism for Phase 23 to use"
      pattern: "compass_topic_roles"
    - from: ".planning/STATE.md Phase 22 Notes"
      to: "RETIRE-01 decision"
      via: "explicitly states 'keep both' with topic_id and count"
      pattern: "keep both"
---

<objective>
Write the Phase 22 audit findings to STATE.md and mark the three requirements complete in REQUIREMENTS.md.

Purpose: Phase 22 is a pure audit — all three questions were resolved definitively via live database queries and source code inspection (see 22-RESEARCH.md). The executor's only job is to write the known findings into the permanent project record. No migrations, no code changes.

Output: STATE.md updated with a "Phase 22 Notes" section; REQUIREMENTS.md with AUDIT-01, AUDIT-02, RETIRE-01 checked.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-compass-schema-audit/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 22 Notes to STATE.md</name>
  <files>.planning/STATE.md</files>
  <action>
Append a "### Phase 22 Notes" section to STATE.md (before the closing `---` separator line at the bottom, in the same pattern as other phase notes sections). Write the following findings verbatim — do not paraphrase or summarize further, these are the exact values Phase 23 and Phase 25 will rely on:

**AUDIT-01 — Scope Mechanism:**
- `inform.compass_stances` has NO scope/level/race_type column. The table holds only stance text content (id, topic_id, value, text, supporting_points, description, example_perspectives).
- Scope filtering lives in `inform.compass_topic_roles` — a join table with columns (topic_id UUID, role_scope TEXT, is_required BOOLEAN). Valid role_scope values: 'federal', 'state', 'local'. Constrained by CHECK (role_scope IN ('federal', 'state', 'local')) per migration 059.
- `inform.compass_topics.office_scope` (TEXT[]) exists but is informational only — never used as a render filter. All 26 live topics currently have office_scope = NULL.
- compassService.ts `getCompassTopics()` converts compass_topic_roles rows to three booleans (applies_federal, applies_state, applies_local) at query time. Topics with ZERO rows in compass_topic_roles default to all three tiers = true (cross-cutting behavior).
- Six live topics are currently cross-cutting (no compass_topic_roles rows): Affordable Housing, AI Oversight, Deportation Priorities, Healthcare Access, Immigration and Treatment, Taxation and Public Spending.
- The frontend (src/lib/compass.js) does NOT currently filter displayed topics by these boolean flags — the flags are returned in the API response but filtering is not enforced in the essentials UI.
- Pattern to add LOCAL scope to a new topic (from migration 063): INSERT INTO inform.compass_topic_roles (topic_id, role_scope, is_required) VALUES ('<uuid>', 'local', true) ON CONFLICT (topic_id, role_scope) DO NOTHING;

**AUDIT-02 — "Criminalization of Homelessness" Answer Count:**
- topic_id: 4938766b-b45a-46e3-93bd-b8b30651271a
- topic_key: homelessness
- is_live: true
- fc_community_slug: criminalization-of-homelessness
- tier roles: federal, state, local (3 rows in compass_topic_roles — all tiers)
- office_scope: NULL
- Politician answer count: 42 (confirmed via live COUNT(*) on inform.politician_answers)
- Value distribution: 3× value=1, 27× value=2, 6× value=3, 4× value=4, 1× value=5
- Politicians span LA council members, CA statewide officials, and Indiana politicians
- topic_rewrites: 0 rows (no active or historical rewrite for this topic)
- companion connect.communities row exists (slug: criminalization-of-homelessness); 0 members, 0 threads

**RETIRE-01 — Retirement Decision: KEEP BOTH**
- Reasoning: 42 politician answers is substantial data spanning real politicians with a diverse value distribution (1–5). Retiring would orphan all 42 politician_answers rows.
- "Criminalization of Homelessness" (rights/enforcement frame: should sleeping in public spaces be criminalized?) is complementary to, not duplicative of, the proposed Phase 23 "Homelessness Response" topic (service delivery frame: what is the primary strategy for addressing homelessness?).
- No retirement action is warranted. Both topics should remain live.
- Pure retirement pattern (for reference only): UPDATE inform.compass_topics SET is_live = false WHERE id = '<topic_id>'; — no topic_rewrites row needed for pure retirement.
- slug_history is a TEXT[] column on the connect.communities row itself (NOT a separate table — ROADMAP.md had this wrong).

Also add a timestamped update line at the bottom of STATE.md (in the same format as the existing update lines):
`*Updated: 2026-05-04 — Phase 22 complete (1/1 plans); compass schema audit done; AUDIT-01: scope lives in compass_topic_roles (not compass_stances); AUDIT-02: 42 politician answers for Criminalization of Homelessness; RETIRE-01: keep both (complementary framing); next migration is 111*`
  </action>
  <verify>Read .planning/STATE.md and confirm: (1) "Phase 22 Notes" section exists, (2) "compass_topic_roles" appears in the scope mechanism description, (3) "42" appears as the answer count, (4) "keep both" appears as the retirement decision.</verify>
  <done>STATE.md contains all three audit findings with the exact values listed above. The update timestamp line is present.</done>
</task>

<task type="auto">
  <name>Task 2: Mark AUDIT-01, AUDIT-02, RETIRE-01 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In REQUIREMENTS.md, find the three unchecked Phase 22 requirements and mark them complete. Change:

```
- [ ] **AUDIT-01**: Scope/level mechanism in `inform.compass_stances` identified — confirm if a scope column exists and how the compass filters questions by race type
- [ ] **AUDIT-02**: Politician answer count for "Criminalization of Homelessness" queried from `inform.politician_answers` — retirement decision documented
```

to:

```
- [x] **AUDIT-01**: Scope/level mechanism in `inform.compass_stances` identified — confirm if a scope column exists and how the compass filters questions by race type
- [x] **AUDIT-02**: Politician answer count for "Criminalization of Homelessness" queried from `inform.politician_answers` — retirement decision documented
```

And change:

```
- [ ] **RETIRE-01**: Retirement decision for "Criminalization of Homelessness" documented based on AUDIT-02 findings
```

to:

```
- [x] **RETIRE-01**: Retirement decision for "Criminalization of Homelessness" documented based on AUDIT-02 findings
```

Also update the requirements status table at the bottom of REQUIREMENTS.md. Change the three Phase 22 rows from `Pending` to `Complete`:

```
| AUDIT-01 | Phase 22 | Complete |
| AUDIT-02 | Phase 22 | Complete |
| RETIRE-01 | Phase 22 | Complete |
```
  </action>
  <verify>Read .planning/REQUIREMENTS.md and confirm: (1) AUDIT-01 shows `[x]`, (2) AUDIT-02 shows `[x]`, (3) RETIRE-01 shows `[x]`, (4) status table shows Complete for all three.</verify>
  <done>All three Phase 22 requirements are checked complete in both the checklist and the status table.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Read .planning/STATE.md — "Phase 22 Notes" section is present with compass_topic_roles, 42, and "keep both" all visible
2. Read .planning/REQUIREMENTS.md — AUDIT-01, AUDIT-02, RETIRE-01 all show [x] in checklist and Complete in status table
3. No migrations exist in C:\Focused Communities\supabase\migrations\ with a 111+ number (confirming no DB changes were made)
</verification>

<success_criteria>
- STATE.md has permanent record of all three audit findings with exact values (topic_id UUID, answer count 42, decision "keep both")
- REQUIREMENTS.md has AUDIT-01, AUDIT-02, RETIRE-01 all marked complete
- Phase 23 executor can read STATE.md Phase 22 Notes and immediately know: scope mechanism = compass_topic_roles, pattern for LOCAL scope insertion, retirement decision = keep both
- Zero database migrations created (this phase produces no schema changes)
</success_criteria>

<output>
After completion, create `.planning/phases/22-compass-schema-audit/22-01-SUMMARY.md` following the summary template.
</output>
