---
phase: 69-landing-elections-discovery
plan: 04
type: execute
wave: 2
depends_on:
  - 69-02
files_modified: []
autonomous: true
requirements:
  - ELECT-04

must_haves:
  truths:
    - "6 city discovery_jurisdictions rows exist for SF, SJ, SD, SAC, Fremont, Berkeley with future election_dates within the cron's 180-day horizon"
    - "1 CA statewide discovery_jurisdictions row exists with jurisdiction_geoid='06', jurisdiction_name='California Statewide', election_date='2026-11-03'"
    - "No duplicate LA city or LA County rows created (geo_id='0644000' and '06037' already exist)"
    - "All 7 new rows have non-null source_url pointing to the official city elections or CA SOS page"
    - "Cron smoke query returns all 7 new rows when filtered by state='CA' and election_date in 180-day horizon from 2026-05-28"
  artifacts:
    - path: "essentials.discovery_jurisdictions"
      provides: "7 discovery rows arming cron for 6 CA cities + 1 CA statewide"
  key_links:
    - from: "discoveryCron.ts sweep query"
      to: "essentials.discovery_jurisdictions rows"
      via: "WHERE election_date > now() AND election_date <= now() + INTERVAL '180 days'"
      pattern: "SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='CA' AND election_date > now() AND election_date <= now() + INTERVAL '180 days'"
---

<objective>
Insert 7 discovery_jurisdictions rows (6 CA cities + 1 CA statewide) and run a smoke test confirming all rows fall within the cron's 180-day sweep horizon.

Purpose: ELECT-04 requires discovery to be armed for all covered CA cities. "Armed" means having a row in discovery_jurisdictions with election_date in the future — the cron (discoveryCron.ts) automatically sweeps all rows within 180 days. No cron_active column exists or is needed.
Output: Migration 223 applied — 7 discovery_jurisdictions rows inserted; smoke test confirms cron would pick up all 7.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-landing-elections-discovery/69-RESEARCH.md
@.planning/phases/69-landing-elections-discovery/69-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert 7 discovery_jurisdictions rows (migration 223)</name>
  <files></files>

  <read_first>
    .planning/phases/69-landing-elections-discovery/69-RESEARCH.md — section "5. discovery_jurisdictions — Current CA State" to confirm which geo_ids already exist (LA city 0644000 and LA County 06037 already have rows — do NOT insert those). Also read the "Discovery Source URLs" section for the verified city clerk URLs and rationale for election date assignment (SF/SJ/SD/SAC = June 2 for active primary races; Fremont/Berkeley = Nov 3 for November-only elections).

    Pre-flight check before INSERT:
    SELECT jurisdiction_geoid, jurisdiction_name, election_date::text
    FROM essentials.discovery_jurisdictions
    WHERE jurisdiction_geoid IN ('0667000','0668000','0664000','0606000','0626000','0666000','06')
    ORDER BY jurisdiction_geoid;
    Expected: 0 rows (none of the 7 target geo_ids exist yet)

    Also confirm LA rows already exist (must NOT be duplicated):
    SELECT jurisdiction_geoid, jurisdiction_name FROM essentials.discovery_jurisdictions
    WHERE jurisdiction_geoid IN ('0644000', '06037');
    Expected: 2 rows (LA city cityclerk.lacity.org + LA County lavote.gov from migration 197)
  </read_first>

  <action>
    Apply migration 223 via mcp__supabase-local__execute_sql. Insert 7 rows into essentials.discovery_jurisdictions.

    Schema reference (verified 2026-05-28, NO cron_active column):
    - id uuid, jurisdiction_geoid text NOT NULL, jurisdiction_name text NOT NULL, state text NOT NULL, election_date date NOT NULL, source_url text, allowed_domains text[], created_at timestamptz, updated_at timestamptz

    The 7 rows to insert (all with gen_random_uuid() for id, ON CONFLICT DO NOTHING):

    Row 1 — San Francisco (June primary: active city council + school board races):
    - jurisdiction_geoid: '0667000'
    - jurisdiction_name: 'San Francisco'
    - state: 'CA'
    - election_date: '2026-06-02'
    - source_url: 'https://www.sf.gov/reports--candidates-june-2-2026-statewide-direct-primary-election'

    Row 2 — San Jose (June primary: active city council races):
    - jurisdiction_geoid: '0668000'
    - jurisdiction_name: 'San Jose'
    - state: 'CA'
    - election_date: '2026-06-02'
    - source_url: 'https://www.sanjoseca.gov/your-government/appointees/city-clerk/elections/2026-elections-primary-and-runoff'

    Row 3 — San Diego (June primary: active city council races):
    - jurisdiction_geoid: '0666000'
    - jurisdiction_name: 'San Diego'
    - state: 'CA'
    - election_date: '2026-06-02'
    - source_url: 'https://www.sandiego.gov/city-clerk/elections/city/electioninfo'

    Row 4 — Sacramento (June primary: active city council races):
    - jurisdiction_geoid: '0664000'
    - jurisdiction_name: 'Sacramento'
    - state: 'CA'
    - election_date: '2026-06-02'
    - source_url: 'https://www.cityofsacramento.gov/clerk/elections/candidate-information'

    Row 5 — Fremont (November general: Fremont has no June 2026 municipal races; filing deadline Aug 7):
    - jurisdiction_geoid: '0626000'
    - jurisdiction_name: 'Fremont'
    - state: 'CA'
    - election_date: '2026-11-03'
    - source_url: 'https://www.fremont.gov/government/election-information'

    Row 6 — Berkeley (November general: Berkeley has no June 2026 municipal races; Nov 3 confirmed):
    - jurisdiction_geoid: '0606000'
    - jurisdiction_name: 'Berkeley'
    - state: 'CA'
    - election_date: '2026-11-03'
    - source_url: 'https://berkeleyca.gov/your-government/elections/candidate-information'

    Row 7 — California Statewide (Governor + US House general; single row covers all statewide races):
    - jurisdiction_geoid: '06'
    - jurisdiction_name: 'California Statewide'
    - state: 'CA'
    - election_date: '2026-11-03'
    - source_url: 'https://www.sos.ca.gov/elections/upcoming-elections/primary-election-june-2-2026'

    CRITICAL: Do NOT insert rows for geo_id='0644000' (LA city) or '06037' (LA County) — those already exist per D-10.
    CRITICAL: There is NO cron_active column on this table. Do not include it in the INSERT.
    CRITICAL: Do NOT use November 4 or June 3 for any date — use '2026-11-03' and '2026-06-02' exactly.
  </action>

  <verify>
    <automated>
      SELECT jurisdiction_geoid, jurisdiction_name, election_date::text, source_url
      FROM essentials.discovery_jurisdictions
      WHERE jurisdiction_geoid IN ('0667000','0668000','0666000','0664000','0626000','0606000','06')
      ORDER BY election_date, jurisdiction_geoid;
    </automated>
  </verify>

  <acceptance_criteria>
    - Query above returns exactly 7 rows
    - Four rows with election_date='2026-06-02': SF (0667000), SJ (0668000), SD (0666000), SAC (0664000)
    - Three rows with election_date='2026-11-03': Fremont (0626000), Berkeley (0606000), CA Statewide (06)
    - No row has a NULL source_url
    - No SQL error on apply
    - LA rows unchanged: `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid IN ('0644000','06037')` still returns 2 (not 3 or 4)
  </acceptance_criteria>

  <done>7 discovery_jurisdictions rows inserted with correct election_dates and source URLs. No duplicate LA rows created.</done>
</task>

<task type="auto">
  <name>Task 2: Cron sweep smoke test</name>
  <files></files>

  <read_first>
    .planning/phases/69-landing-elections-discovery/69-RESEARCH.md — section "How Discovery Cron 'Arms' a Jurisdiction" to understand the exact cron query logic: `WHERE election_date > now() AND election_date <= now() + INTERVAL '180 days'`. From 2026-05-28, 180 days forward is ~2026-11-24 — both June 2 and November 3 are within scope.

    Also read section "Validation Architecture" for the full set of smoke queries to run at phase close.
  </read_first>

  <action>
    Run the following SQL smoke queries in order to verify the complete phase 69 outcome. These are read-only SELECT queries — no migration needed. Apply via mcp__supabase-local__execute_sql.

    Query 1 — Cron horizon check (primary verification of ELECT-04):
    SELECT jurisdiction_name, jurisdiction_geoid, election_date::text
    FROM essentials.discovery_jurisdictions
    WHERE state = 'CA'
      AND election_date > now()
      AND election_date <= now() + INTERVAL '180 days'
    ORDER BY election_date, jurisdiction_name;

    Expected result: At minimum the 7 new rows — SF/SJ/SD/SAC (June 2), Fremont/Berkeley/CA Statewide (Nov 3) — plus any pre-existing CA rows that still have future election_dates (LA city June 2, LA County June 2, Beverly Hills, etc. if their dates are in the future).

    Query 2 — New rows count check:
    SELECT COUNT(*) FROM essentials.discovery_jurisdictions
    WHERE jurisdiction_geoid IN ('0667000','0668000','0666000','0664000','0626000','0606000','06')
      AND election_date > now();
    Expected: 7

    Query 3 — CA elections completeness:
    SELECT name, election_date::text, jurisdiction_level
    FROM essentials.elections WHERE state = 'CA' ORDER BY election_date;
    Expected: at least 3 rows — LA County Primary (county, 2026-06-02), CA Statewide Primary (state, 2026-06-02), CA Statewide General (state, 2026-11-03)

    Query 4 — Governor race linkage:
    SELECT r.position_name, e.name AS election_name, r.office_id::text
    FROM essentials.races r
    JOIN essentials.elections e ON e.id = r.election_id
    WHERE r.id = 'bc936a36-287c-4ffd-abd8-5e4fd798bae5';
    Expected: election_name='CA 2026 Statewide General', office_id='08454462-a1f0-4d11-9f61-aba7a173a3de'

    Query 5 — US House race count:
    SELECT COUNT(*) FROM essentials.races r
    JOIN essentials.elections e ON e.id = r.election_id
    WHERE e.name = 'CA 2026 Statewide General'
      AND r.position_name LIKE 'U.S. Representative District%';
    Expected: 52

    Record results in the SUMMARY.md. If any query returns an unexpected value, investigate and fix before closing the phase.
  </action>

  <verify>
    <automated>
      SELECT COUNT(*) AS new_discovery_rows
      FROM essentials.discovery_jurisdictions
      WHERE jurisdiction_geoid IN ('0667000','0668000','0666000','0664000','0626000','0606000','06')
        AND election_date > now();
    </automated>
  </verify>

  <acceptance_criteria>
    - Query 1 (cron horizon): 7 new rows all appear in the result set
    - Query 2 (count): returns 7
    - Query 3 (elections): at least 3 CA election rows with the correct names, dates, jurisdiction_levels
    - Query 4 (Governor): election_name='CA 2026 Statewide General', office_id='08454462-a1f0-4d11-9f61-aba7a173a3de'
    - Query 5 (US House): returns 52
    - All 5 queries run without SQL errors
    - No unexpected values that require remediation
  </acceptance_criteria>

  <done>All 5 smoke queries pass. Phase 69 DB state is fully verified: Landing.jsx updated, CA elections seeded, Governor patched, 52 House races exist, 7 discovery rows armed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → essentials DB | Direct DB writes via Supabase MCP; jurisdiction_geoids and URLs are hardcoded constants from verified research — no user input |
| discovery_jurisdictions → discoveryCron.ts | Cron reads rows by election_date; source_url is passed to the discovery agent — confirm URLs are official city/state government domains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04-01 | Tampering | discovery_jurisdictions source_url | mitigate | All source_urls point to official government domains (sf.gov, sanjoseca.gov, sandiego.gov, cityofsacramento.gov, fremont.gov, berkeleyca.gov, sos.ca.gov); ON CONFLICT DO NOTHING prevents duplicate rows |
| T-69-04-02 | Elevation of Privilege | cron sweep scope | accept | discovery_jurisdictions rows can only expand cron scope, not reduce it; new rows are all official CA government jurisdictions |
| T-69-04-03 | Denial of Service | duplicate LA rows | mitigate | Pre-flight query confirms LA (0644000) and LA County (06037) rows exist; ON CONFLICT DO NOTHING prevents accidental duplication |
| T-69-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SQL only |
</threat_model>

<verification>
After migration 223 is applied, run all 5 smoke queries from Task 2. All must pass before creating the SUMMARY.md.

The full phase 69 verification set:
1. Cron horizon: 7 new CA discovery rows within 180 days
2. Count: exactly 7 new jurisdiction rows
3. Elections: CA Statewide Primary + General exist
4. Governor race: linked to CA 2026 Statewide General + office_id set
5. US House: 52 races under CA 2026 Statewide General
</verification>

<success_criteria>
- 7 discovery_jurisdictions rows inserted for SF, SJ, SD, SAC, Fremont, Berkeley, CA Statewide — all with future election_dates the cron will sweep
- No duplicate LA rows
- All 5 phase-level smoke queries return expected values
- ELECT-04 satisfied: cron is armed for all 6 covered CA cities + 1 statewide jurisdiction
</success_criteria>

<output>
Create `.planning/phases/69-landing-elections-discovery/69-04-SUMMARY.md` when done.
</output>
