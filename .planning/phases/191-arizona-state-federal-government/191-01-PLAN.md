---
phase: 191-arizona-state-federal-government
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1282_az_state_exec_gap.sql
  - C:/EV-Accounts/backend/migrations/1283_az_state_exec_headshots.sql
  - C:/EV-Accounts/backend/scripts/_tmp-az-state-exec-headshots.py
autonomous: true
requirements: [AZ-STATE-01]
user_setup: []

must_haves:
  truths:
    - "An AZ resident sees all 11 statewide elected officials (Governor + Secretary of State + Attorney General + Treasurer + Superintendent + Mine Inspector + 5 Corporation Commissioners) under State of Arizona"
    - "The Corporation Commission renders as ONE 5-member collegial body sharing one STATE_EXEC district"
    - "The State Mine Inspector office is elected-type (is_appointed_position=false) while holder Les Presmyk is flagged as an appointed holder (politicians.is_appointed=true)"
    - "The 7 net-new officials each have a 600x750 headshot with a recorded photo_license"
    - "No phantom Lieutenant Governor office/chamber/district is created"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1282_az_state_exec_gap.sql"
      provides: "Structural seed: 3 chambers + 3 STATE_EXEC districts + 7 politicians (-4004001..-4004007) + 7 offices + office_id back-fill; registers version 1282"
      contains: "az_state_exec_gap"
    - path: "C:/EV-Accounts/backend/migrations/1283_az_state_exec_headshots.sql"
      provides: "Audit-only politician_images INSERT for the 7 net-new officials (unregistered)"
      contains: "politician_images"
    - path: "C:/EV-Accounts/backend/scripts/_tmp-az-state-exec-headshots.py"
      provides: "Download -> crop-to-4:5 -> resize 600x750 -> upload pipeline for the 7 officials"
      contains: "crop_to_4_5"
  key_links:
    - from: "1282_az_state_exec_gap.sql offices rows"
      to: "State of Arizona government (geo_id='04')"
      via: "chamber.government_id + STATE_EXEC district state='AZ' geo_id='04'"
      pattern: "geo_id = '04'"
    - from: "1283 politician_images rows"
      to: "politicians -4004001..-4004007"
      via: "SELECT id FROM politicians WHERE external_id = ..."
      pattern: "external_id = -4004"
---

<objective>
Close the Arizona STATE_EXEC gap identified by RESEARCH: create the 3 missing chambers, 3 missing STATE_EXEC districts, 7 missing politicians, and 7 missing offices under the pre-existing State of Arizona government, then source and attach 600x750 headshots for all 7. This satisfies the STATE_EXEC half of AZ-STATE-01.

Purpose: Governor Hobbs + Secretary of State Fontes + Attorney General Mayes + Treasurer Yee already exist. This plan adds the remaining 7 statewide elected officials (Superintendent Tom Horne, State Mine Inspector Les Presmyk, and the 5-member Arizona Corporation Commission) so every AZ statewide-ballot office is represented.

Output:
- `1282_az_state_exec_gap.sql` (structural, REGISTERED as version 1282)
- `1283_az_state_exec_headshots.sql` (audit-only, UNREGISTERED)
- `_tmp-az-state-exec-headshots.py` (headshot pipeline)
- All three committed to the `C:/EV-Accounts` repo (branch `master`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Cross-repo + environment (READ — do not re-decide)
- ALL migration/script files live in the SEPARATE repo `C:/EV-Accounts` (default branch `master`; push deploys to Render). Commit ONLY via `git -C "C:/EV-Accounts" add/commit`. Do NOT stage these files in the essentials repo.
- `mcp__supabase-local` IS production and is NOT exposed to you. Apply every SQL statement and run every pre-check yourself via psql: `psql "$DATABASE_URL"` where `DATABASE_URL` lives in `C:/EV-Accounts/backend/.env` (psql binary: `C:/Program Files/PostgreSQL/18/bin/psql`, PostgreSQL 18.1). Load the var by reading that .env line, do not echo the secret.
- Structural migration 1282 ends with a `schema_migrations` registration footer OUTSIDE its BEGIN/COMMIT — applying the file via `psql -f` registers it. Audit-only 1283 has NO footer and MUST NOT register.
- Headshot upload uses `SUPABASE_SERVICE_ROLE_KEY` from the same `.env` (the Python script reads it). CDN base: `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/`.
</execution_context>

<context>
@.planning/phases/191-arizona-state-federal-government/191-CONTEXT.md
@.planning/phases/191-arizona-state-federal-government/191-RESEARCH.md
@.planning/phases/191-arizona-state-federal-government/191-PATTERNS.md

## Confirmed roster + external_id assignment (from RESEARCH — use verbatim)
| external_id | Official | Office / Chamber | party | flags | headshot license |
|---|---|---|---|---|---|
| -4004001 | Tom Horne | Superintendent of Public Instruction | Republican | is_appointed=false | cc_by_sa_2.0 (Wikimedia) |
| -4004002 | Les Presmyk | State Mine Inspector | Republican | **is_appointed=true** (holder appointed; OFFICE elected) | TBD via fallback chain |
| -4004003 | Nick Myers | Corporation Commission (Chair) | Republican | is_appointed=false | press_use (azcc.gov) |
| -4004004 | Rachel Walden | Corporation Commission (Vice Chair) | Republican | is_appointed=false | press_use (azcc.gov) |
| -4004005 | Lea Márquez Peterson | Corporation Commission | Republican | is_appointed=false | cc_by_2.0 (Wikimedia) |
| -4004006 | Kevin Thompson | Corporation Commission | Republican | is_appointed=false | press_use (azcc.gov) |
| -4004007 | René Lopez | Corporation Commission | Republican | is_appointed=false | press_use (azcc.gov) |

- State of Arizona government UUID: `15436f29-38d2-4cc0-8958-9e74ba60fabf`, geo_id `04`.
- Pre-existing (DO NOT re-create): Hobbs -400091, Mayes -400092, Fontes -400093, Yee -400094 + their 4 chambers/districts.
- Party stored on `politicians.party` for internal use ONLY — never displayed (antipartisan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB pre-check + collision gate (inline psql, NO writes)</name>
  <read_first>
    - .planning/phases/191-arizona-state-federal-government/191-RESEARCH.md — "Live Database Audit", "External ID Scheme", "Migration Counter" sections
    - C:/EV-Accounts/backend/.env — read the `DATABASE_URL` line only
  </read_first>
  <action>
    Run read-only psql assertions against production and ABORT the plan if any fails (all values from RESEARCH):
    (1) Exactly 1 State of Arizona government row: `SELECT count(*) FROM essentials.governments WHERE geo_id='04'` = 1.
    (2) The 4 pre-existing STATE_EXEC officials still present: `SELECT count(*) FROM essentials.politicians WHERE external_id IN (-400091,-400092,-400093,-400094)` = 4.
    (3) The 7 target external_ids are FREE (Pitfall 1 — the pool grows continuously): `SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -4004007 AND -4004001` = 0. If NOT 0, STOP and report — do not pick a new range silently.
    (4) No 'Arizona Corporation Commission' district yet: `SELECT count(*) FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='AZ' AND label='Arizona Corporation Commission'` = 0.
    (5) Migration-number gate (Pitfall 2): confirm files `1282`,`1283`,`1284` are absent on disk in `C:/EV-Accounts/backend/migrations/` AND that `SELECT max(version::int) FROM supabase_migrations.schema_migrations` < 1282. Use max(disk, ledger)+1 logic; both must clear 1282.
    (6) Baseline section-split (must already be 0): run the RESEARCH "Section-Split Check" query for state='AZ'.
    Record all 6 results in the SUMMARY. This task writes NOTHING to the DB.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE geo_id='04')=1 AND (SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -4004007 AND -4004001)=0"</automated>
  </verify>
  <acceptance_criteria>
    - Query in verify returns `t` (true).
    - All 6 assertions pass; results recorded in SUMMARY.
    - external_id range -4004001..-4004007 confirmed empty (count=0).
    - Migration numbers 1282/1283/1284 confirmed free on disk and above ledger MAX.
    - Baseline section-split for AZ = 0 rows.
  </acceptance_criteria>
  <done>All 6 pre-checks pass; if any fails the plan halts with a report rather than proceeding to writes.</done>
</task>

<task type="auto">
  <name>Task 2: Write + apply structural migration 1282_az_state_exec_gap.sql</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/270_md_state_executives.sql — PRIMARY analog: pre-flight assert + single-seat district/politician/office CTE block + Dereck Davis is_appointed politician block + office_id back-fill
    - C:/EV-Accounts/backend/migrations/1050_nv_controller.sql — pre-flight assert shape + registration footer OUTSIDE BEGIN/COMMIT
    - C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql — 5-officers-share-1-district collegial-body shape; NOTE its office guard uses `WHERE o.district_id=d.id AND o.politician_id=p.id` (the correct politician_id-aware guard — copy that, NOT a district_id+chamber_id-only guard)
    - .planning/phases/191-arizona-state-federal-government/191-PATTERNS.md — full block-by-block assignment for this file
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/1282_az_state_exec_gap.sql` by copy-adapting the analogs. Contents, in order:
    (a) Pre-flight `DO $$` assert: exactly 1 government with geo_id='04' (adapt 1050 lines 23-32) and a `COUNT(*)=4` assert on the 4 existing officials; header comment stating Hobbs/Mayes/Fontes/Yee MUST NOT be re-created.
    (b) Two single-seat blocks (mirror 270's Governor block): district `label='Arizona Superintendent of Public Instruction'` + chamber `name='Superintendent of Public Instruction'` + politician Tom Horne (-4004001, is_appointed=false) + office title 'Superintendent of Public Instruction'; and district `label='Arizona State Mine Inspector'` + chamber `name='State Mine Inspector'` + politician Les Presmyk (-4004002, **is_appointed=true**) + office title 'State Mine Inspector' with **is_appointed_position=false**. Add an inline comment on the Presmyk block per RESEARCH Pitfall 3: OFFICE is voter-elected, only this holder arrived by gubernatorial appointment — do not "fix" either flag.
    (c) Corporation Commission block: ONE district `label='Arizona Corporation Commission'` (state='AZ', geo_id='04', district_id='', mtfcc=''); ONE chamber `name='Corporation Commission'`, `name_formal='Arizona Corporation Commission'`, `official_count=5`; then 5 politician+office CTE blocks (Myers -4004003, Walden -4004004, Márquez Peterson -4004005, Thompson -4004006, Lopez -4004007), all offices titled 'Commissioner', all pointing at the SAME Corporation Commission district_id and chamber_id, each guarded by `NOT EXISTS (... WHERE o.district_id=d.id AND o.politician_id=p.id)` so all 5 insert (role_canonical NULL — Chair/Vice-Chair are display-only per Clark County precedent).
    All: districts.state='AZ' UPPERCASE, geo_id='04'; offices.representing_state='AZ'; politicians.party per roster table; is_active=true, is_incumbent=true, is_vacant=false. Never list the chambers GENERATED-ALWAYS column; chambers columns = (id,name,name_formal,government_id[,official_count]). Use `WHERE NOT EXISTS` on chambers/districts (no unique constraint) and `ON CONFLICT (external_id) DO NOTHING RETURNING id` on politicians.
    (d) office_id back-fill scoped `WHERE p.external_id BETWEEN -4004007 AND -4004001 AND p.office_id IS NULL`.
    (e) Registration footer OUTSIDE BEGIN/COMMIT: `INSERT INTO supabase_migrations.schema_migrations (version,name) VALUES ('1282','az_state_exec_gap') ON CONFLICT (version) DO NOTHING;`.
    Apply with `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1282_az_state_exec_gap.sql`. Then commit via `git -C "C:/EV-Accounts" add backend/migrations/1282_az_state_exec_gap.sql && git -C "C:/EV-Accounts" commit`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -4004007 AND -4004001)=7 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND d.state='AZ' AND d.label='Arizona Corporation Commission')=5 AND (SELECT max(version::int) FROM supabase_migrations.schema_migrations)=1282"</automated>
  </verify>
  <acceptance_criteria>
    - Verify query returns `t`: 7 new politicians exist, Corporation Commission district has exactly 5 offices, ledger MAX = 1282.
    - `SELECT count(*) FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id JOIN essentials.districts d ON d.id=o.district_id WHERE g.geo_id='04' AND d.district_type='STATE_EXEC'` = 11 (4 existing + 7 new).
    - Presmyk row: `SELECT is_appointed FROM politicians WHERE external_id=-4004002` = true AND his office `is_appointed_position` = false.
    - Exactly 1 district with label 'Arizona Corporation Commission'; that chamber has official_count=5.
    - Section-split query for AZ still returns 0 rows.
    - File committed to C:/EV-Accounts (git -C), NOT essentials.
  </acceptance_criteria>
  <done>11 AZ STATE_EXEC officials exist under State of Arizona; Corporation Commission modeled as 1 chamber/1 district with 5 offices; Presmyk flags correct; migration 1282 registered; 0 section-split.</done>
</task>

<task type="auto">
  <name>Task 3: Source 7 headshots + write/apply audit-only migration 1283</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py — env-load block, descriptive User-Agent, crop_to_4_5 + resize_600x750, runtime UUID lookup
    - C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py — ROSTER-loop + per-official failure isolation + manifest print + required-failure sys.exit(1) gate
    - C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql — audit-only politician_images INSERT shape (NO footer, NO photo_origin_url)
    - .planning/phases/191-arizona-state-federal-government/191-RESEARCH.md — "Headshot Sources" table (per-official source URLs + Pitfall 6 azcc.gov slugs) and Open Question 1 (Presmyk)
    - $HOME/.claude/commands/find-headshots.md — Presmyk fallback skill
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/scripts/_tmp-az-state-exec-headshots.py` as a 7-entry ROSTER loop (copy the config/UA/crop/resize blocks verbatim from `_tmp-nv-controller-headshot.py`; use the loop+manifest+exit-gate shape from `_tmp-nv-house-headshots.py`). ROSTER entries (external_id, source_url, photo_license, is_required):
    - Horne -4004001: Wikimedia `Tom_Horne_(52801743945)_(crop).jpg`, cc_by_sa_2.0, required.
    - Márquez Peterson -4004005: Wikimedia `Lea_Marquez_Peterson_by_Gage_Skidmore.jpg`, cc_by_2.0, required.
    - Myers -4004003: azcc.gov `/images/default-source/commissioners/2025-nick-myers.tmb-thumb200.jpg`, press_use, required.
    - Walden -4004004: azcc.gov `2025-rachel-walden.tmb-thumb200.jpg`, press_use, required.
    - Thompson -4004006: azcc.gov `2025-kevin-thompson.tmb-medium.jpg`, press_use, required.
    - Lopez -4004007: azcc.gov `lopez-headshota40372b9-eb5d-4c79-83b5-a3f7800dbe90.jpg`, press_use, required.
    - Presmyk -4004002: **is_required=False** — attempt `/find-headshots` skill / Ballotpedia / AZGOP press photo; asmi.az.gov is WAF-403, do NOT retry it. If a licensed source is found, upload it; if not, leave it for the Plan 03 human-verify checkpoint (do NOT block the other 6).
    Send the descriptive User-Agent (`EmpoweredVote-HeadshotBot/1.0 (https://empowered.vote; contact chris@empowered.vote)`) on Wikimedia/azcc.gov requests. Crop-first to 4:5 then resize 600x750 Lanczos q90; upload to `politician_photos/{uuid}-headshot.jpg` (uuid = each politician's actual UUID, resolved at runtime via `SELECT id FROM politicians WHERE external_id=%s`). Run the script; capture the printed UUID manifest.
    Then create `C:/EV-Accounts/backend/migrations/1283_az_state_exec_headshots.sql` = 7 `politician_images` INSERT blocks (mirror 1052, one per official), columns EXACTLY `(id, politician_id, url, type, photo_license)` — NO photo_origin_url (Pitfall 5), type='default', per-official photo_license from the roster, URL uses the captured UUID. Skip the Presmyk block ONLY if his upload failed. Header comment: audit-only, apply via psql not apply_migration, NO schema_migrations footer. Apply with `psql "$DATABASE_URL" -f`. Commit script + migration via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4004007 AND -4004001 AND pi.url IS NOT NULL" ; python -c "from PIL import Image; import sys; im=Image.open(sys.argv[1]); assert im.size==(600,750), im.size; print('600x750 OK')" $(ls -t /tmp/*headshot*.jpg 2>/dev/null | head -1) 2>/dev/null || echo "image-dim-check: verify at least one processed file is 600x750 in the script manifest"</automated>
  </verify>
  <acceptance_criteria>
    - `politician_images` rows for the 6 required officials (Horne, Márquez Peterson, Myers, Walden, Thompson, Lopez) exist with non-null url and correct photo_license; count of AZ STATE_EXEC gap images >= 6 (7 if Presmyk resolved).
    - Every uploaded image is exactly 600x750 (script manifest asserts dimensions).
    - Migration 1283 does NOT register (ledger MAX stays 1282).
    - photo_license is non-null on every inserted row; press_use for the 4 azcc.gov portraits, cc_by_sa_2.0 Horne, cc_by_2.0 Márquez Peterson.
    - Script + migration committed to C:/EV-Accounts.
    - If Presmyk unresolved: recorded in SUMMARY as a Plan 03 checkpoint item (not a silent omission).
  </acceptance_criteria>
  <done>At least 6/7 net-new officials have a 600x750 headshot with recorded license; migration 1283 applied audit-only; Presmyk either resolved or explicitly flagged for the Plan 03 checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| planner-authored SQL -> production DB | Hand-written INSERT values cross into live data (supabase-local IS prod) |
| external image host -> Storage bucket | Third-party portrait bytes + license metadata cross into public CDN |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-01 | Tampering | 1282 INSERT correctness | mitigate | Pre-flight `DO $$` asserts (Task 1 + in-file); `WHERE NOT EXISTS` / `ON CONFLICT DO NOTHING`; post-insert row-count asserts (=7 politicians, =5 CC offices, =11 STATE_EXEC) |
| T-191-02 | Tampering | Writing directly to production | mitigate | INSERT-only; never UPDATE/DELETE the 4 existing officials or their chambers/districts; section-split re-run = 0 as closing gate |
| T-191-03 | Tampering | external_id pool collision (Pitfall 1) | mitigate | Task 1 asserts -4004001..-4004007 empty immediately before write; post-insert asserts count=7 (ON CONFLICT silent no-op would show <7) |
| T-191-04 | Repudiation | image licensing provenance | mitigate | `photo_license` recorded per image (cc_by_sa_2.0 / cc_by_2.0 / press_use); no unlicensed source uploaded; Presmyk deferred rather than uploaded without a license |
| T-191-05 | Information disclosure | party affiliation | accept | Stored on politicians.party for internal use; never rendered on profiles (project-wide antipartisan control, no new exposure) |
| T-191-SC | Tampering | package installs | accept | No npm/pip installs in this phase; PIL/Pillow assumed present (ImageMagick fallback); no new dependency introduced |
</threat_model>

<verification>
- `psql` audit: 11 STATE_EXEC officials under geo_id='04'; Corporation Commission = 1 chamber (official_count=5) / 1 district / 5 offices.
- Presmyk: politicians.is_appointed=true, offices.is_appointed_position=false.
- >= 6 net-new officials with 600x750 headshots and non-null photo_license.
- Ledger MAX = 1282 (1283 unregistered). Section-split for AZ = 0.
- No 'Arizona Lieutenant Governor' row anywhere (confirm 0).
</verification>

<success_criteria>
- 7 net-new AZ statewide elected officials seeded (-4004001..-4004007), bringing the STATE_EXEC total under State of Arizona to 11.
- Corporation Commission modeled as one collegial 5-seat chamber on one shared STATE_EXEC district.
- >= 6/7 headshots uploaded at 600x750 with recorded licenses; Presmyk resolved or flagged for Plan 03.
- Migrations 1282 (registered) + 1283 (audit-only) applied and committed to C:/EV-Accounts.
</success_criteria>

<output>
Create `.planning/phases/191-arizona-state-federal-government/191-01-SUMMARY.md` when done. Record: all 6 pre-check results, the 7 captured politician UUIDs, per-official headshot outcome + license, Presmyk resolution status, and final ledger MAX.
</output>
