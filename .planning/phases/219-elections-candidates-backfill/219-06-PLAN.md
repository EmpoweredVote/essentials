---
phase: 219-elections-candidates-backfill
plan: 06
type: execute
wave: 2
depends_on: [219-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1397_longview_princeton_runoff_closure.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1397_longview_princeton_runoff_closure.ts
autonomous: false
requirements: [COLLIN-ELECT-02, COLLIN-ELECT-03]
user_setup: []

must_haves:
  truths:
    - "Longview (4843888) has its original May-2026 District 3 5-candidate GENERAL race seeded under the shared 2026-05-02 election row (the D3 June-13 runoff already exists in migration 187 — reused, not duplicated), plus Mayor + Districts 1,2,4,5,6 general races where held on the same May date (D-04)."
    - "Princeton (4859576) Place 4 June-2026 runoff (Rutledge beat Goria 293-245) is seeded as its OWN new race under its OWN new runoff election row, distinct from the already-seeded May-2026 Place 4 special (migration 100) (Pattern 5)."
    - "Every seeded race links to the correct existing office_id and renders on /results (COLLIN-ELECT-03)."
    - "The Longview D3 officeholder-seating gap (offices.politician_id still hold-over Wray Wade vs. confirmed runoff winner Brandon Smith) is flagged to the operator as an in-passing bonus fix or explicit out-of-scope note — never left as a silent inconsistency."
    - "Re-applying migration 1397 is net-zero (idempotent)."
  artifacts:
    - "C:/EV-Accounts/backend/migrations/1397_longview_princeton_runoff_closure.sql"
    - "C:/EV-Accounts/backend/scripts/_apply-migration-1397_longview_princeton_runoff_closure.ts"
  key_links:
    - "Longview D3 general race.election_id → shared 2026-05-02 row; Longview D3 runoff → existing migration-187 row (reused)."
    - "Princeton Place 4 runoff race.election_id → a NEW minted 'Princeton TX City Council Place 4 Runoff 2026' row."
  prohibitions:
    - statement: "No duplicate election row for Longview's D3 runoff (migration 187 already has it) — reuse by name/date/state."
      status: enforced
      verification: "apply-script gate: exactly one 'Longview TX City Council District 3 Runoff 2026' election row after apply."
    - statement: "candidate_status only ever uses a legal CHECK value (active/withdrawn/filed) — runoff winner via politician_id, never a status value."
      status: enforced
      verification: "apply-script gate: 0 candidate rows with candidate_status NOT IN ('active','withdrawn','filed')."
    - statement: "position_name always city-prefixed AND stage-disambiguated (general vs runoff produce distinct races for the same seat)."
      status: enforced
      verification: "apply-script gate: Princeton Place 4 special and Place 4 runoff are two distinct races; no ON CONFLICT collision."
    - statement: "primary_party stays NULL (D-06); no inform.* writes (D-07)."
      status: enforced
      verification: "0 non-NULL primary_party on seeded races; inform.* before/after unchanged."
    - statement: "No fabricated candidate; unresolved Longview/Princeton offices stay documented-open."
      status: enforced
      verification: "Every seeded candidate cited; open seats have a written reason."
  backstop:
    - statement: "Whether to also update Longview D3 offices.politician_id to Brandon Smith (an officeholder-seating action, Phase 218's kind of work) is an operator decision surfaced at the checkpoint; the elections/candidates seed itself is 219's job regardless."
      verification: backstop
---

<objective>
Close the two-stage-election gaps RESEARCH identified: Longview is missing its original May-2026 D3 general (only the runoff exists), and Princeton is missing its June-2026 Place 4 runoff (only the special exists). Seed the missing stage for each as its own race, reusing existing election rows where they already exist and minting a new one only for the Princeton runoff.

Purpose: Closes COLLIN-ELECT-02 for the two runoff-closure cities' full filed field (D-04). COLLIN-ELECT-03 for their render path.
Output: migration 1397 + gated apply-script, plus an explicit operator flag on the Longview D3 seating gap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-elections-candidates-backfill/219-RESEARCH.md
@.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md
@.planning/phases/219-elections-candidates-backfill/219-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock Longview (May-2026 general) + Princeton (June-2026 Place 4 runoff) cited rosters</name>
  <read_first>
    - .planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md (confirmed Longview D3 winner Brandon Smith 223-204; Princeton runoff final).
    - 219-RESEARCH.md §"Thin governments" Longview/Princeton rows, §Pitfall 5, §Pattern 5, §"Open Questions" #2 (Longview seating gap).
    - C:/EV-Accounts/backend/migrations/187_longview_tx_d3_runoff_election.sql (existing runoff row — reuse) and 100_collin_county_may2026_races.sql (existing Princeton Place 4 special race — do not duplicate).
    - Sources: longviewtexas.gov/3308/City-Election-Results + /2154/General-Election-Candidates; princetontx.gov/294/Elections; Princeton Herald; Longview News-Journal.
  </read_first>
  <action>
    Longview: assemble the original May-2026 District 3 5-candidate general roster + the Mayor and Districts 1,2,4,5,6 general fields where held on the same 2026-05-02 date (cited — D-04); these link under the shared election row. Do NOT re-create the D3 June-13 runoff (migration 187 has it) — only confirm its winner (Brandon Smith). Princeton: assemble the June-2026 Place 4 runoff pair (Rutledge beat Goria 293-245, certified 2026-06-23) for a NEW runoff race under a NEW election row; the May special (migration 100) stays as-is. Map winners to offices.politician_id where a politicians row exists; note that Longview/Princeton runoff winners are outside Phase 218's Collin-only scope, so a winner may need a new stub politicians row only if not already present (avoid the duplicate-officeholder bug — check first). is_incumbent explicit. Record the Longview D3 seating gap (Wade hold-over vs Smith) for the checkpoint. Escape apostrophes; preserve diacritics.
  </action>
  <verify>
    <automated>MISSING — roster verified by Task 2 apply-script gates.</automated>
  </verify>
  <acceptance_criteria>
    - Longview: cited May-2026 D3 general 5-candidate roster + any resolvable Mayor/District general fields; D3 runoff winner confirmed (not re-seeded).
    - Princeton: cited June-2026 Place 4 runoff pair with its runoff date.
    - Longview D3 seating gap documented for the operator.
  </acceptance_criteria>
  <done>Seed-ready cited rosters exist for both missing stages, with the seating gap flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 1397 (Longview general under shared row + new Princeton runoff row) + gated apply-script</name>
  <read_first>
    - 219-RESEARCH.md §Pattern 1/2/3/5.
    - C:/EV-Accounts/backend/migrations/187 (reuse runoff row by name/date/state), 1375 (ON CONFLICT idiom), 100 (existing Princeton special — leave intact).
    - C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts (gate shape).
    - Migration number from PREFLIGHT (research: 1397; re-confirm + shift block if drifted).
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1397_longview_princeton_runoff_closure.sql in BEGIN/COMMIT. Longview general races: resolve the shared 2026-05-02 election id by name/date/state; INSERT city-prefixed, stage-labeled position_name (e.g. 'Longview Council Member District 3' for the general, distinct from the runoff's own position_name) with correct office_id, primary_party NULL, ON CONFLICT position-name idiom. Do NOT INSERT a Longview D3 runoff election row (reuse migration 187's by name/date/state if any candidate attach is needed there). Princeton: INSERT a NEW election row (name e.g. 'Princeton TX City Council Place 4 Runoff 2026', the confirmed runoff date, TX) via ON CONFLICT (name, election_date, state) DO NOTHING, then a 'Princeton Council Member Place 4 Runoff' race under it (distinct from the migration-100 special race), + its 2 candidates. INSERT race_candidates WHERE NOT EXISTS; winners→politician_id (create a stub politicians row for a runoff winner ONLY if none exists — guard against the duplicate-officeholder bug), candidate_status='active', is_incumbent explicit, cited source. If seating Longview D3's Brandon Smith into offices.politician_id is approved at the checkpoint, include that UPDATE guarded by WHERE the office currently points at the hold-over; otherwise leave a header note. Write C:/EV-Accounts/backend/scripts/_apply-migration-1397_longview_princeton_runoff_closure.ts mirroring 1389 with gates: Longview + Princeton per-geo_id race count increased; every seeded race has ≥1 candidate; 0 illegal candidate_status; 0 non-NULL primary_party; exactly one Longview D3 runoff election row (no duplicate); Princeton Place 4 special AND runoff are two distinct races; inform.* unchanged; split-section 0; idempotent re-run net-zero.
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/_apply-migration-1397_longview_princeton_runoff_closure.ts 2>&1 | head; test -f migrations/1397_longview_princeton_runoff_closure.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; Longview general uses the shared row; Princeton runoff uses a NEW row minted by name/date/state; migration 187 runoff row is reused not duplicated.
    - Apply-script includes the no-duplicate-runoff-row gate and the two-distinct-Princeton-races gate plus the standard set.
  </acceptance_criteria>
  <done>Migration 1397 + gated apply-script authored and type-check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator applies 1397, decides Longview D3 seating, pushes, browse spot-check</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-1397_longview_princeton_runoff_closure.ts.
    - The Longview D3 seating-gap note from Task 1.
    - [[no_git_in_ev_accounts]] / [[backend_architecture]].
  </read_first>
  <action>Operator-run checkpoint: decide the Longview D3 seating question, apply migration 1397 via `npx tsx`, confirm every apply-script gate exits 0 (incl. no-duplicate-runoff-row and two-distinct-Princeton-races), commit + push to C:/EV-Accounts (Render auto-deploys), then browse-spot-check Princeton on /results — per the numbered how-to-verify steps.</action>
  <what-built>Executor has no DB access / prod push; operator applies + verifies, and decides the Longview D3 officeholder-seating question (in-scope bonus vs out-of-scope note).</what-built>
  <how-to-verify>
    1. Decide: seat Brandon Smith into Longview D3 offices.politician_id now (bonus fix) or record it as a Phase-220/follow-up note. Confirm the migration reflects the decision.
    2. `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-1397_longview_princeton_runoff_closure.ts` — all gates green, exit 0.
    3. `git -C "C:/EV-Accounts" add -A && git -C "C:/EV-Accounts" commit -m "feat(219): close Longview D3 general + Princeton Place 4 runoff" && git -C "C:/EV-Accounts" push origin master`.
    4. Browse spot-check `/results?browse_geo_id=4859576` (Princeton) — confirm both the Place 4 special AND the runoff render.
  </how-to-verify>
  <acceptance_criteria>
    - Apply-script exits 0, all gates green incl. no-duplicate-runoff-row and two-distinct-Princeton-races.
    - The Longview D3 seating decision is recorded (either applied or explicitly deferred) — no silent inconsistency.
    - Princeton browse shows both stages (backstop: human visual).
  </acceptance_criteria>
  <resume-signal>Type "approved (seated Smith)" or "approved (deferred seating)" once gates green + browse passes, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `C:/EV-Accounts/backend/migrations/1397_longview_princeton_runoff_closure.sql` (new)
- `C:/EV-Accounts/backend/scripts/_apply-migration-1397_longview_princeton_runoff_closure.ts` (new)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external news/canvass → race_candidates | Untrusted runoff-result claims; every row cited |
| migration SQL → production essentials schema | Idempotent, guarded writes; no-duplicate-row guard |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Tampering/Info disclosure | race_candidates (runoff winners) | high | mitigate | Evidence-only; winner matches PREFLIGHT citation |
| T-219-02 | Tampering (schema integrity) | candidate_status | high | mitigate | Only legal CHECK values; gate asserts 0 illegal |
| T-219-05 | Data integrity (silent loss / dup) | election-row + two-stage races | high | mitigate | Reuse migration-187 runoff row; new Princeton runoff row; distinct-races gate; duplicate-officeholder guard |
| T-219-04 | Integrity (scope violation) | inform.* tables | high | mitigate | before/after row-count assertion (D-07) |
| T-219-03 | Tampering (SQL literal) | apostrophe/diacritic names | medium | mitigate | Escape '→''; preserve UTF-8; type-check |
| T-219-SC | Tampering | npm/pip installs | low | accept | No package installs; legitimacy gate N/A |
</threat_model>

<verification>
- All apply-script gates green.
- Longview + Princeton race counts increased; exactly one D3 runoff election row; Princeton special + runoff both present; split-section 0; primary_party 0; inform.* net-zero.
</verification>

<success_criteria>
Longview D3 general + Princeton Place 4 runoff render as their own races on /results (alongside the pre-existing runoff/special); the Longview D3 seating gap is resolved or explicitly deferred; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/219-elections-candidates-backfill/219-06-SUMMARY.md` when done.
</output>
