---
phase: 202-palm-springs-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["202-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1328_palm_springs_city_council.sql  # NN placeholder — verify actual next migration number (DB-verified disk MAX+1) at execute time
autonomous: false
requirements: [CV-02]
must_haves:
  truths:
    - "A 'City of Palm Springs, California, US' government (type='City', state='CA', city='Palm Springs', geo_id='0655254') exists"
    - "One 'City Council' chamber (official_count=5) holds exactly 5 by-district council offices"
    - "Each of the 5 council offices links to its OWN LOCAL X0022 district (never the pre-existing G4110 0655254 city row)"
    - "Roster is Garner(D1)/Bernstein(D2)/deHarte(D3)/Soto(D4)/Ready(D5); all 5 net-new greenfield politicians; no directly-elected LOCAL_EXEC mayor row"
    - "The rotational Mayor (Soto, D4) and Mayor Pro Tem (Ready, D5) are TITLES set at INSERT on their district seats (title='Mayor' / 'Mayor Pro Tem'); the other three carry title='Councilmember'. Exactly 5 offices, no separate Mayor office/chamber/district. Re-verify current Mayor/MPT at execute time."
    - "The structural migration is applied to production and registered in the ledger; 5 politician UUIDs + ext_ids captured for Plans 03/04"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1328_palm_springs_city_council.sql"
      provides: "government + chamber + 5 LOCAL X0022 districts + 5 politicians + 5 offices (Soto Mayor, Ready Mayor Pro Tem) + office_id backfill + pre-flight + post-verify DO gates + ledger registration"
      contains: "City Council"
  key_links:
    - from: "essentials.offices (5 councilmembers)"
      to: "essentials.districts (X0022 LOCAL)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='X0022' AND state='ca'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'City Council'"
      to: "essentials.governments 'City of Palm Springs, California, US'"
      via: "government_id subquery scoped by name"
      pattern: "City of Palm Springs, California, US"
---

<objective>
Author the ONE structural migration that seeds the City of Palm Springs government + City Council
chamber + 5 LOCAL council-district rows (bound to the X0022 geofences from Plan 01) + 5 net-new
by-district councilmember politicians + their 5 offices, with the rotational Mayor (Soto, D4) and
Mayor Pro Tem (Ready, D5) set as TITLES on their seats at INSERT time. The orchestrator applies it via
`psql -f`, registers it in the ledger, and captures the 5 politician UUIDs + ext_ids for Plans 03/04.

Purpose: Satisfies ROADMAP success criteria #1 and #3 for CV-02 — the government/chamber/roster with
the by-district relabel (title-on-seat) pattern. Palm Springs is greenfield (no existing row), so all
5 members are created fresh — closer to Bellflower's create-as-Mayor-at-INSERT shape than West
Covina's UPDATE-an-existing-seat shape.
Output: `C:/EV-Accounts/backend/migrations/1328_palm_springs_city_council.sql` (verify actual next
migration number at execute time — disk MAX is already >=1328 as of this revision, e.g. 1328_tx_sd4_brett_ligon_seed.sql exists unrelated; interim phases may consume more. The literal 1328 here is an illustrative placeholder only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-palm-springs-deep-seed/202-CONTEXT.md
@.planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md
@.planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB access. Task 1 (author the migration SQL) is the only executor
file action. Task 2 is ORCHESTRATOR-RUN: the orchestrator DB-verifies the migration number + ext_id
block, applies the migration via `psql -f` against `C:/EV-Accounts/backend/.env` `DATABASE_URL`,
confirms the in-transaction post-verify gates passed, and captures the 5 politician UUIDs into
202-02-SUMMARY.md. Backend files live in the SEPARATE `C:/EV-Accounts` repo — commit via
`git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (from 202-PATTERNS.md structural-migration block + RESEARCH):
- Primary analog (copy overall shape verbatim): `C:/EV-Accounts/backend/migrations/1314_riverside_county_board_of_supervisors.sql`
  (government + chamber + pre-flight geofence assertion + 5 LOCAL district rows + 5×(politician+office)
  blocks + office_id back-fill + post-verify DO block + ledger registration).
- Secondary analog (Mayor/Mayor-Pro-Tem set at INSERT): `.planning/milestones/v17.0-phases/156-bellflower-deep-seed/156-02-PLAN.md`
  Task 2 (Santa Ines created + seated with title set directly in the office INSERT's SELECT list).
- Government: name `'City of Palm Springs, California, US'`, type `'City'`, state `'CA'` (uppercase
  label), city `'Palm Springs'`, geo_id `'0655254'`, WHERE NOT EXISTS guard.
- Chamber: name `'City Council'`, name_formal `'Palm Springs City Council'`, official_count `5`
  (the Mayor is one of the 5 by-district seats, NOT a 6th excluded seat).
- Districts: 5× district_type `'LOCAL'`, state `'ca'` (LOWERCASE — routing key), geo_id
  `'palm-springs-ca-council-district-1..5'`, mtfcc `'X0022'`.
- Roster + titles: D1 Garner 'Councilmember' / D2 Bernstein 'Councilmember' / D3 deHarte
  'Councilmember' / D4 Soto 'Mayor' / D5 Ready 'Mayor Pro Tem'. representing_state `'CA'` (uppercase).
- ext_id: next free collision-safe `-401Nxxx` block (Riverside used -4010001..-4010005) — DB-verify
  the actual free block at execute time, do NOT hardcode blindly.
- Casing (critical): districts.state='ca' lowercase; governments.state='CA' and
  offices.representing_state='CA' uppercase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Palm Springs City Council structural migration</name>
  <files>C:/EV-Accounts/backend/migrations/1328_palm_springs_city_council.sql</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 93-245 — the full structural-migration pattern: government+chamber INSERT, pre-flight geofence assertion, 5 LOCAL district INSERTs, 5 politician+office blocks with title-at-INSERT, office_id back-fill, post-verify DO block incl. Gate (f) exactly-one-Mayor + exactly-one-Mayor-Pro-Tem asserts, ledger registration; casing convention lines 381-389; idempotency convention lines 391-401)
    - .planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md (Pattern 1 rotational-Mayor title-on-seat; Pitfall 3 do-NOT-model-a-directly-elected-mayor; Pitfall 4 counter collisions)
    - C:/EV-Accounts/backend/migrations/1314_riverside_county_board_of_supervisors.sql (PRIMARY analog — read in FULL: the government+chamber+preflight+5-district+5-office+backfill+post-verify+ledger shape)
    - .planning/milestones/v17.0-phases/156-bellflower-deep-seed/156-02-PLAN.md (SECONDARY analog — read Task 2 for the Mayor-title-set-directly-in-the-office-INSERT pattern for a brand-new politician)
  </read_first>
  <action>
    Author `1328_palm_springs_city_council.sql` (VERIFY the actual next migration number at execute
    time — one past the true DB-verified disk MAX when the plan runs; disk MAX is already >=1328, so the 1328 in this filename is an ILLUSTRATIVE PLACEHOLDER only). Copy the Riverside 1314 shape
    section-by-section and substitute:

    (1) Government INSERT: `'City of Palm Springs, California, US'`, type `'City'`, state `'CA'`, city
    `'Palm Springs'`, geo_id `'0655254'`, guarded by WHERE NOT EXISTS on the name.

    (2) Chamber INSERT: name `'City Council'`, name_formal `'Palm Springs City Council'`,
    official_count `5`, government_id subquery scoped by the government name, guarded by WHERE NOT
    EXISTS on (name, government_id).

    (3) Pre-flight geofence assertion (DO block): RAISE EXCEPTION unless >= 5 rows exist in
    essentials.geofence_boundaries WHERE state='ca' AND mtfcc='X0022' — this refuses to apply until
    Plan 01's loader has run.

    (4) 5 LOCAL district INSERTs: district_type 'LOCAL', state 'ca' (LOWERCASE), geo_id
    'palm-springs-ca-council-district-1..5', label 'Palm Springs City Council District N', mtfcc
    'X0022', each guarded by WHERE NOT EXISTS on (geo_id, district_type, state).

    (5) 5 politician+office blocks (WITH ins_p AS (INSERT ... politicians ... ON CONFLICT (external_id)
    DO NOTHING RETURNING id) INSERT INTO offices ...). All 5 politicians are net-new greenfield —
    set the office `title` DIRECTLY in the INSERT's SELECT list per the Bellflower model, NOT a later
    UPDATE: D1 Grace Elena Garner → 'Councilmember'; D2 Jeffrey Bernstein → 'Councilmember'; D3 Ron
    deHarte → 'Councilmember'; D4 Naomi Soto → 'Mayor'; D5 David H. Ready → 'Mayor Pro Tem'. Each
    office links its politician to its OWN district via a join scoped by
    `d.geo_id='palm-springs-ca-council-district-N' AND d.district_type='LOCAL' AND d.state='ca' AND
    d.mtfcc='X0022'`, chamber_id via the City Council subquery, representing_state 'CA' (uppercase),
    is_appointed_position false, is_vacant false, role_canonical NULL, guarded by NOT EXISTS on
    (district_id, politician_id). Assign ext_ids from the next free collision-safe -401Nxxx block
    (DB-verify unclaimed at execute time — do NOT hardcode from the research doc). Do NOT create any
    separate LOCAL_EXEC Mayor office/chamber/district — Palm Springs is rotational (RESEARCH Pitfall 3;
    the directly-elected-mayor petition has NOT been adopted as of 2026-07-12).

    (6) office_id back-fill: copy Riverside's shape, adjust the `external_id BETWEEN` range to the
    actual assigned block.

    (7) Post-verify DO block (all inside the transaction, RAISE EXCEPTION on any failure): (a) exactly
    1 government named 'City of Palm Springs, California, US'; (b) exactly 5 offices on LOCAL X0022
    districts; (c) each of the 5 districts holds exactly 1 office; (d) appointed count = 0 (confirm at
    execute time); (e) section-split — 0 offices reachable under any non-Palm-Springs government;
    (f) exactly 1 office with title='Mayor' AND exactly 1 office with title='Mayor Pro Tem' across the
    X0022 council districts (replaces Riverside's single-Chair gate, generalized to the two rotational
    roles).

    (8) Ledger registration: INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES
    ('<actual-migration-number>', 'palm_springs_city_council') ON CONFLICT (version) DO NOTHING.
    CRITICAL: re-derive `<actual-migration-number>` from the REAL DB-verified disk MAX at apply time
    (query the migrations dir AND supabase_migrations.schema_migrations, take MAX(version)+1) — NEVER
    copy the placeholder `1328` from this plan. Disk MAX is already >=1328 (`1328_tx_sd4_brett_ligon_seed.sql`
    exists unrelated), so version 1328 is ALREADY registered; an `ON CONFLICT (version) DO NOTHING` against
    it would silently no-op the ledger row while the structural rows still land under their WHERE NOT EXISTS
    guards — leaving the migration unregistered. The literal `1328` anywhere in this plan is illustrative only.

    Never join/filter on a bare `geo_id='0655254'`; always scope the 5 new council districts by
    `district_type='LOCAL' AND mtfcc='X0022'`, and never let this migration touch the pre-existing
    G4110 city geofence row.
  </action>
  <verify>
    <automated>F=$(ls C:/EV-Accounts/backend/migrations/*_palm_springs_city_council.sql 2>/dev/null | head -1); test -n "$F" && test -f "$F" && grep -qF "City of Palm Springs, California, US" "$F" && grep -qF "City Council" "$F" && grep -qF "official_count" "$F" && grep -qF "X0022" "$F" && grep -qF "'Mayor'" "$F" && grep -qF "'Mayor Pro Tem'" "$F" && grep -qF "palm-springs-ca-council-district-" "$F" && grep -qcF "schema_migrations" "$F" && ! grep -qE "LOCAL_EXEC" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (migration number = one past disk MAX at execute time).
    - Government name/type/geo_id exactly 'City of Palm Springs, California, US'/'City'/'0655254'; WHERE NOT EXISTS guard present.
    - Chamber 'City Council' with official_count=5; WHERE NOT EXISTS guard present.
    - Pre-flight DO block asserts >=5 X0022 geofences (state='ca') before seeding.
    - Exactly 5 LOCAL district rows, state='ca' lowercase, mtfcc='X0022', geo_id prefix palm-springs-ca-council-district-.
    - 5 politician+office blocks; title set at INSERT: Soto='Mayor', Ready='Mayor Pro Tem', other 3='Councilmember'. NO 'LOCAL_EXEC' anywhere.
    - ext_ids from a DB-verified free -401Nxxx block; office_id back-fill present.
    - Post-verify DO block includes Gate (f): exactly-1-Mayor AND exactly-1-Mayor-Pro-Tem asserts, plus gates (a)-(e). Ledger registration present.
    - Idempotency: WHERE NOT EXISTS on gov/chamber/districts/offices; ON CONFLICT (external_id) DO NOTHING on politicians.
  </acceptance_criteria>
  <done>Structural migration authored: greenfield Palm Springs City gov + City Council (official_count=5) + 5 LOCAL X0022 districts + 5 by-district members with Soto=Mayor / Ready=Mayor Pro Tem titles-on-seat, idempotent, with pre-flight + post-verify gates + ledger registration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-verify roster + apply migration + capture UUIDs (ORCHESTRATOR-RUN + roster confirm)</name>
  <files>N/A — orchestrator applies the migration + read-only assertions; captures UUIDs into SUMMARY</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-CONTEXT.md (Roster table + Structure — rotational-mayor note + the early-2026 directly-elected-mayor discussion caveat)
    - .planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md (Assumption A5 roster-current-as-of-2026-07-12; Pitfall 3 re-verify at execute time; Sources — palmspringsca.gov term dates)
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 235-238 — re-verify current Mayor/MPT before writing; do NOT model a LOCAL_EXEC Mayor)
  </read_first>
  <what-built>
    The structural migration `1328_palm_springs_city_council.sql` authored in Task 1 — greenfield City
    of Palm Springs government + City Council (official_count=5) + 5 LOCAL X0022 districts + 5
    by-district councilmembers with Soto (D4)=Mayor and Ready (D5)=Mayor Pro Tem as titles-on-seat.
  </what-built>
  <how-to-verify>
    ORCHESTRATOR steps before/at apply:
    1. Re-verify the CURRENT roster + Mayor/Mayor-Pro-Tem against palmspringsca.gov/government/mayor-city-council
       + The Palm Springs Post + KESQ (the directly-elected-mayor petition is active but NOT adopted as
       of research — confirm still rotational; if a member changed or a measure passed, PAUSE and
       revise the migration before applying).
    2. DB-verify the next migration number (one past current disk/ledger MAX) and the next free -401Nxxx
       ext_id block; adjust the file if they shifted.
    3. Apply the migration file authored in Task 1 (using the disk-verified `*_palm_springs_city_council.sql`
       filename from step 2, NOT the illustrative `1328` placeholder) via
       `psql "$DATABASE_URL" -f backend/migrations/<verified>_palm_springs_city_council.sql`; confirm
       it commits with NO RAISE EXCEPTION (all 8 pre/post-verify gates passed).
    4. Assert via `psql -tAc`: 1 government row; 5 offices on LOCAL X0022 districts (1 per district);
       exactly 1 title='Mayor' + exactly 1 title='Mayor Pro Tem'; appointed=0; section-split=0; ledger
       has the new version with count=1.
    5. Capture the 5 politician UUIDs + ext_ids + geo_id→member mapping into 202-02-SUMMARY.md (the
       "Politician UUID Manifest" that Plans 03/04 consume).
    Confirm: roster current, all gates green, manifest recorded.
  </how-to-verify>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='City of Palm Springs, California, US')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022' AND d.district_type='LOCAL')=5 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022' AND o.title='Mayor')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022' AND o.title='Mayor Pro Tem')=1"</automated>
  </verify>
  <acceptance_criteria>
    - Roster + Mayor/MPT re-verified current against live sources (still rotational; no adopted directly-elected-mayor measure).
    - Migration applied and committed with no RAISE EXCEPTION; all 8 gates passed.
    - psql assertions: 1 government, 5 X0022 LOCAL offices (1/district), exactly 1 Mayor + exactly 1 Mayor Pro Tem, appointed=0, section-split=0.
    - Ledger registers the new version (count=1).
    - 5 politician UUIDs + ext_ids + geo_id→member manifest recorded in 202-02-SUMMARY.md.
    - Migration committed to `C:/EV-Accounts` via `git -C`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once roster is confirmed current, all gates green, and the UUID manifest is recorded — or describe the roster/gate discrepancy to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research/recon roster → migration SQL | Officeholder names + Mayor/MPT titles asserted from external sources cross into canonical DB rows |
| migration SQL → production DB | Service-role `psql -f` writes government/chamber/district/office rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-02 | Tampering | wrong-district / stale roster / wrong Mayor | mitigate | Cross-verify the X0022 FeatureServer CouncilName (Plan 01) + palmspringsca.gov/KESQ/Palm Springs Post at Task 2 before apply; live-browse routing checkpoint in Plan 06 |
| T-202-03 | Tampering | non-idempotent re-application | mitigate | WHERE NOT EXISTS on gov/chamber/districts/offices; ON CONFLICT (external_id) DO NOTHING on politicians; IS DISTINCT FROM where relevant |
| T-202-MAYOR | Tampering | modeling a directly-elected LOCAL_EXEC Mayor | mitigate | Title-on-seat only (Soto D4 / Ready D5); post-verify Gate (f) asserts exactly 1 Mayor + 1 Mayor Pro Tem; no LOCAL_EXEC row; re-verify petition NOT adopted at execute time |
| T-202-SPLIT | Tampering | section-split / cross-gov office leak | mitigate | Post-verify Gate (e) = 0 offices reachable under any non-Palm-Springs government; scope every district filter by district_type='LOCAL' AND mtfcc='X0022' |
</threat_model>

<verification>
- 1 City of Palm Springs government + 1 City Council chamber (official_count=5).
- 5 offices, 1 per X0022 LOCAL district; exactly 1 Mayor + 1 Mayor Pro Tem; appointed=0; section-split=0.
- Ledger registers the migration; UUID manifest captured for Plans 03/04.
</verification>

<success_criteria>
Greenfield Palm Springs City Council seeded with the by-district relabel (title-on-seat) pattern,
all post-verify gates green, roster re-verified current.
</success_criteria>

<output>
Create `.planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md` when done (MUST include the 5
politician UUID + ext_id + geo_id→member manifest for Plans 03/04, the actual migration number, and
the ext_id block used).
</output>
