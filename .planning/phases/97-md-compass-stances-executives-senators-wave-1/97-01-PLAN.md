---
phase: 97-md-compass-stances-executives-senators-wave-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/data/stance-research/compass-topics-reference.md
  - C:/EV-Accounts/backend/data/stance-research/gen_migration.py
  - C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-moore.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-miller.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-brown.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-lierman.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-davis.csv
  - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql
autonomous: true
requirements:
  - MD-STANCES-01
tags:
  - md
  - compass
  - stances
  - executives

must_haves:
  truths:
    - "Governor Wes Moore has >= 1 cited compass stance in inform.politician_answers + inform.politician_context"
    - "LG Aruna Miller has >= 1 cited compass stance in inform.politician_answers + inform.politician_context"
    - "AG Anthony G. Brown has >= 1 cited compass stance in inform.politician_answers + inform.politician_context"
    - "Comptroller Brooke Lierman has >= 1 cited compass stance in inform.politician_answers + inform.politician_context"
    - "Treasurer Dereck E. Davis is represented in migration 282 (stances or documented not-found note)"
    - "All ingested stance values are integers 1-5"
    - "Every stance row has a non-null source_url citation"
    - "All exec research ran sequentially, one agent at a time"
  artifacts:
    - path: "C:/EV-Accounts/backend/data/stance-research/compass-topics-reference.md"
      provides: "Authoritative compass topic question + 1-5 answer scale reference for research agents"
      contains: "abortion"
    - path: "C:/EV-Accounts/backend/data/stance-research/gen_migration.py"
      provides: "Migration generator with MD_EXEC_CANDIDATES + MD_EXEC_CSVS sections added"
      contains: "MD_EXEC_CANDIDATES"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-moore.csv"
      provides: "Wes Moore stance CSV (one row per researched topic)"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-miller.csv"
      provides: "Aruna Miller stance CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-brown.csv"
      provides: "Anthony G. Brown stance CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-lierman.csv"
      provides: "Brooke Lierman stance CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-davis.csv"
      provides: "Dereck E. Davis stance CSV (may be empty-with-header if not-found)"
    - path: "C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql"
      provides: "Idempotent SQL migration upserting exec stances + context"
      contains: "ON CONFLICT (politician_id, topic_id)"
  key_links:
    - from: "C:/EV-Accounts/backend/data/stance-research/gen_migration.py"
      to: "C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql"
      via: "generate_migration(migration_num=282, ...)"
      pattern: "migration_num=282"
    - from: "C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "mcp__supabase-local__apply_migration"
      pattern: "INSERT INTO inform\\.politician_(answers|context)"
    - from: "candidate_inventory full_name"
      to: "essentials.politicians.full_name"
      via: "exact string match (CRITICAL — mismatch silently drops rows)"
      pattern: "Wes Moore|Aruna Miller|Anthony G\\. Brown|Brooke Lierman|Dereck E\\. Davis"
---

<objective>
Research and ingest compass stances for all 5 MD constitutional officers — Governor Wes Moore (D-01), LG Aruna Miller (D-01), AG Anthony G. Brown (D-01), Comptroller Brooke Lierman (D-01), and Treasurer Dereck E. Davis (D-01, appointed). Stances are researched one agent at a time (per D-03 and the SEQUENTIAL EXECUTION constraint), accumulated into per-politician CSVs, bundled via `gen_migration.py` (D-07), and applied as migration 282 (D-06) to `inform.politician_answers` + `inform.politician_context`.

Purpose: Satisfy MD-STANCES-01 — Compass stances for Governor Moore + 3 constitutional officers (plus Davis per D-01 override of ROADMAP's "4 exec" count), cited from public record. Establish the reusable gen_migration.py MD-section pattern that plans 97-02/03/04 will copy.
Output: 5 exec CSVs + extended gen_migration.py `__main__` block + migration 282 applied + verification SELECTs returning expected counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-md-compass-stances-executives-senators-wave-1/97-CONTEXT.md
@.planning/phases/97-md-compass-stances-executives-senators-wave-1/97-RESEARCH.md
@.planning/phases/97-md-compass-stances-executives-senators-wave-1/97-PATTERNS.md

# Tooling and analog migrations
@C:/EV-Accounts/backend/data/stance-research/gen_migration.py
@C:/EV-Accounts/backend/migrations/216_sf_officials_stances.sql
@C:/EV-Accounts/backend/migrations/270_md_state_executives.sql
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Research agent → CSV file | Web-sourced text is written to local CSV; no user-supplied input |
| CSV → gen_migration.py | Python script reads researcher-authored CSVs; `dollar_quote()` handles `$$` escaping |
| gen_migration.py → 282_md_exec_stances.sql | Generated SQL with parameterized UUIDs + `$$` reasoning quoting + `ARRAY[...]::text[]` sources |
| 282_md_exec_stances.sql → Supabase | `mcp__supabase-local__apply_migration` applies hand-reviewed SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | CSV `full_name` mismatch silently drops rows | mitigate | Use exact `full_name` strings from RESEARCH.md Known Politician Data — Moore/Miller/Anthony G. Brown/Lierman/Dereck E. Davis. Acceptance criterion verifies per-candidate row count >= 1 (or documented not-found) for every UUID in candidate_inventory. |
| T-97-02 | Information disclosure | Reasoning text could contain quoted external content | accept | Reasoning is paraphrased prose from public record — no PII, no secrets. Hand-reviewed before apply. |
| T-97-03 | Tampering | data-centers/local-immigration/transportation-priorities topic_keys silently dropped | mitigate | EXCLUDED_TOPICS_FEDERAL passed to generate_migration(); researcher instructed not to include excluded keys. Verification gate inspects migration footer SELECTs for unexpected row counts. |

This phase has no authentication/authorization changes, no user-facing input, and no package installs. ASVS not applicable. Package Legitimacy Gate not applicable (no installs).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Create compass-topics-reference.md + extend gen_migration.py with MD exec section</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/compass-topics-reference.md,
    C:/EV-Accounts/backend/data/stance-research/gen_migration.py
  </files>
  <read_first>
    C:/EV-Accounts/backend/data/stance-research/gen_migration.py,
    .planning/phases/97-md-compass-stances-executives-senators-wave-1/97-RESEARCH.md,
    .planning/phases/97-md-compass-stances-executives-senators-wave-1/97-PATTERNS.md,
    C:/EV-Accounts/backend/migrations/270_md_state_executives.sql,
    C:/EV-Accounts/backend/migrations/216_sf_officials_stances.sql
  </read_first>
  <action>
    Wave 0 setup (per D-08 topic scope + RESEARCH.md Pitfall 2). Two file operations, executed in this order:

    (a) Verify Python: run `python3 --version` then `python3 C:/EV-Accounts/backend/data/stance-research/gen_migration.py --help 2>&1` (or fall back to plain `python` on Windows if `python3` is unavailable). Confirm the script imports without error. Record the exact Python command in the SUMMARY.

    (b) CREATE `C:/EV-Accounts/backend/data/stance-research/compass-topics-reference.md` (per RESEARCH.md Pitfall 2 — file missing). Source the 41 applicable topic keys from gen_migration.py TOPIC_UUIDS (lines 9–55) minus EXCLUDED_TOPICS_FEDERAL = {data-centers, local-immigration, transportation-priorities}. For each topic, write: the topic_key, a one-sentence policy question phrased neutrally, and a 1–5 answer scale where 1 = strong progressive/left stance, 5 = strong conservative/right stance, 3 = mixed/centrist (use existing CSV reasoning patterns from 2026-05-22-sf-lurie.csv as a calibration guide). The 41 topics MUST include: abortion, ai-regulation, campaign-finance, childcare, city-sanitation, civil-rights, climate-change, deportation, economic-development, fossil-fuels, growth-and-development, healthcare, homelessness, homelessness-response, housing, immigration, jail-capacity, judicial-access-to-justice, judicial-bail-pretrial, judicial-criminal-justice, judicial-government-deference, judicial-interpretation, judicial-police-accountability, judicial-prosecution-priorities, judicial-transparency, local-environment, medicare/aid, misinformation, public-safety-approach, redistricting, religious-freedom, rent-regulation, residential-zoning, same-sex-marriage, school-vouchers, social-security, tariffs, taxes, trans-athletes, ukraine-support, voting-rights. The file is read by exec + senator research agents in this phase and in Phase 98.

    (c) EXTEND `gen_migration.py` __main__ block (matching the existing SF_CANDIDATES / SF_CSVS pattern on lines 317–406). Append a new MD_EXEC section BEFORE the `if __name__ == '__main__':` closing — do not delete or alter any existing batch sections. The new section MUST define:
      - `MD_EXEC_CANDIDATES`: list of 5 tuples (full_name, politician_uuid). Use placeholder UUID `"REPLACE-WITH-DB-UUID"` for now; Task 2 fills these in after a DB lookup.
      - `MD_EXEC_CSVS`: list of 5 absolute paths matching the CSV filenames listed below.
      - One `generate_migration()` call with: `migration_num=282`, `batch_label="MD Executive Stances — 5 Constitutional Officers"`, `candidate_inventory=MD_EXEC_CANDIDATES`, `csv_files=MD_EXEC_CSVS`, `excluded_topics=EXCLUDED_TOPICS_FEDERAL`, `header_scope_note="Federal/state topics only; data-centers, local-immigration, transportation-priorities excluded."`, `outpath=os.path.join(base, "282_md_exec_stances.sql")`.

    CSV paths (use raw strings on Windows, exactly as in Pattern 1 of RESEARCH.md):
      - r"C:\EV-Accounts\backend\data\stance-research\2026-06-07-md-exec-moore.csv"
      - r"C:\EV-Accounts\backend\data\stance-research\2026-06-07-md-exec-miller.csv"
      - r"C:\EV-Accounts\backend\data\stance-research\2026-06-07-md-exec-brown.csv"
      - r"C:\EV-Accounts\backend\data\stance-research\2026-06-07-md-exec-lierman.csv"
      - r"C:\EV-Accounts\backend\data\stance-research\2026-06-07-md-exec-davis.csv"

    Full names (must match essentials.politicians exactly per migration 270):
      - "Wes Moore"
      - "Aruna Miller"
      - "Anthony G. Brown"
      - "Brooke Lierman"
      - "Dereck E. Davis"

    Do NOT run gen_migration.py yet — UUIDs are placeholders and CSVs do not exist.
  </action>
  <verify>
    <automated>
      python3 -c "import ast, sys; src = open(r'C:/EV-Accounts/backend/data/stance-research/gen_migration.py').read(); ast.parse(src); assert 'MD_EXEC_CANDIDATES' in src, 'MD_EXEC_CANDIDATES missing'; assert 'MD_EXEC_CSVS' in src, 'MD_EXEC_CSVS missing'; assert 'migration_num=282' in src, '282 generate_migration call missing'; assert 'Wes Moore' in src and 'Aruna Miller' in src and 'Anthony G. Brown' in src and 'Brooke Lierman' in src and 'Dereck E. Davis' in src, 'one or more exec full_names missing from gen_migration.py'; print('OK')"
    </automated>
    <automated>
      python3 -c "import os; p=r'C:/EV-Accounts/backend/data/stance-research/compass-topics-reference.md'; assert os.path.exists(p), 'compass-topics-reference.md missing'; t=open(p,encoding='utf-8').read(); required=['abortion','ai-regulation','campaign-finance','childcare','city-sanitation','civil-rights','climate-change','deportation','economic-development','fossil-fuels','growth-and-development','healthcare','homelessness','homelessness-response','housing','immigration','jail-capacity','judicial-access-to-justice','judicial-bail-pretrial','judicial-criminal-justice','judicial-government-deference','judicial-interpretation','judicial-police-accountability','judicial-prosecution-priorities','judicial-transparency','local-environment','medicare/aid','misinformation','public-safety-approach','redistricting','religious-freedom','rent-regulation','residential-zoning','same-sex-marriage','school-vouchers','social-security','tariffs','taxes','trans-athletes','ukraine-support','voting-rights']; missing=[k for k in required if k not in t]; assert not missing, f'missing topic keys: {missing}'; forbidden=['data-centers','local-immigration','transportation-priorities']; present=[k for k in forbidden if k in t]; assert not present, f'forbidden excluded keys present: {present}'; print('OK', len(required), 'topics present')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 --version` returns a Python 3.x version string (or `python --version` on Windows fallback); command exit 0
    - `C:/EV-Accounts/backend/data/stance-research/compass-topics-reference.md` exists and contains all 41 topic keys listed in the action body (every key present as a substring)
    - `compass-topics-reference.md` does NOT contain any of `data-centers`, `local-immigration`, `transportation-priorities`
    - `gen_migration.py` parses as valid Python (ast.parse succeeds)
    - `gen_migration.py` contains tokens `MD_EXEC_CANDIDATES`, `MD_EXEC_CSVS`, `migration_num=282`, and all 5 exec full_names as substrings
    - `gen_migration.py` retains all pre-existing batch sections (SF_CANDIDATES, BATCH2, etc.) — no deletions
  </acceptance_criteria>
  <done>
    Wave 0 setup files exist; researcher in Task 2 has a topic reference + a configured generator awaiting UUIDs + CSV data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sequential exec stance research (5 agents, one at a time) → 5 CSVs + UUID-filled gen_migration.py</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-moore.csv,
    C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-miller.csv,
    C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-brown.csv,
    C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-lierman.csv,
    C:/EV-Accounts/backend/data/stance-research/2026-06-07-md-exec-davis.csv,
    C:/EV-Accounts/backend/data/stance-research/gen_migration.py
  </files>
  <read_first>
    C:/EV-Accounts/backend/data/stance-research/gen_migration.py,
    C:/EV-Accounts/backend/data/stance-research/compass-topics-reference.md,
    .planning/phases/97-md-compass-stances-executives-senators-wave-1/97-RESEARCH.md,
    .planning/phases/97-md-compass-stances-executives-senators-wave-1/97-PATTERNS.md,
    C:/EV-Accounts/backend/data/stance-research/2026-05-22-sf-lurie.csv
  </read_first>
  <action>
    Sequential stance research (per D-02, D-03, and `feedback_stance_research_one_at_a_time.md`).

    (a) DB lookup — query Supabase for exec UUIDs and record them. Run via `mcp__supabase-local__execute_sql`:
      SELECT id, full_name, external_id
      FROM essentials.politicians
      WHERE external_id BETWEEN -240005 AND -240001
      ORDER BY external_id DESC;
    Expected: Moore=-240001, Miller=-240002, Brown=-240003, Lierman=-240004, Davis=-240005.
    Replace the placeholder UUIDs in `MD_EXEC_CANDIDATES` (gen_migration.py) with the returned UUIDs, in the order listed.

    (b) Sequential research — run ONE research agent at a time. Use the Task tool (general-purpose agent) once per politician, in this exact order: Wes Moore → Aruna Miller → Anthony G. Brown → Brooke Lierman → Dereck E. Davis. DO NOT issue more than one Task tool call in a single message. Wait for each agent's SUMMARY to return before starting the next agent. This is a hard rate-limit constraint (see threat T-97-01 and project memory `feedback_stance_research_one_at_a_time.md`).

    Each agent's brief:
      - Goal: research compass stances for one MD executive on as many of the 41 applicable topics in compass-topics-reference.md as possible (target 15–20+ per D-02). Evidence-only: every stance must be backed by a public source URL (mgaleg.maryland.gov, ballotpedia.org, ontheissues.org, governor.maryland.gov, marylandattorneygeneral.gov, marylandtaxes.gov, news outlets).
      - Output: write CSV at the path listed in <files> above for that politician.
      - CSV header (line 1, exact): `full_name,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3`
      - One data row per researched topic; `full_name` MUST exactly match `Wes Moore` / `Aruna Miller` / `Anthony G. Brown` / `Brooke Lierman` / `Dereck E. Davis` respectively (T-97-01 mitigation).
      - `topic_key` MUST be a key from compass-topics-reference.md; do NOT use `data-centers`, `local-immigration`, or `transportation-priorities` (T-97-03 mitigation).
      - `value` is an integer 1–5; `reasoning` is paraphrased prose (no `$$` markers); `source_url_1` non-empty.
      - For Treasurer Davis: he is the legislature-elected State Treasurer with a 30+ year House of Delegates record before 2021 — search mgaleg.maryland.gov voting record and marylandtaxes.gov. If no discoverable stances exist after thorough search, create the CSV with header row ONLY (no data rows) per RESEARCH.md Pitfall 6 — do NOT omit the file.

    (c) After all 5 CSVs exist, re-verify gen_migration.py has real UUIDs (no `REPLACE-WITH-DB-UUID` strings remain).
  </action>
  <verify>
    <automated>
      python3 -c "import os,csv; base=r'C:/EV-Accounts/backend/data/stance-research'; expected={'2026-06-07-md-exec-moore.csv':'Wes Moore','2026-06-07-md-exec-miller.csv':'Aruna Miller','2026-06-07-md-exec-brown.csv':'Anthony G. Brown','2026-06-07-md-exec-lierman.csv':'Brooke Lierman','2026-06-07-md-exec-davis.csv':'Dereck E. Davis'}; forbidden={'data-centers','local-immigration','transportation-priorities'}; total=0;
      for fname, name in expected.items():
        p=os.path.join(base,fname); assert os.path.exists(p), f'missing {p}';
        with open(p,encoding='utf-8',newline='') as f:
          rows=list(csv.DictReader(f)); 
          for r in rows:
            assert r['full_name']==name, f'{fname}: full_name mismatch {r[\"full_name\"]!r} vs {name!r}';
            assert r['topic_key'] not in forbidden, f'{fname}: forbidden topic {r[\"topic_key\"]}';
            v=int(r['value']); assert 1<=v<=5, f'{fname}: value {v} out of 1..5';
            assert r['source_url_1'].strip(), f'{fname}: source_url_1 empty for topic {r[\"topic_key\"]}';
          total += len(rows);
      print('OK',total,'total rows across 5 CSVs')"
    </automated>
    <automated>
      python3 -c "src=open(r'C:/EV-Accounts/backend/data/stance-research/gen_migration.py').read(); assert 'REPLACE-WITH-DB-UUID' not in src, 'placeholder UUIDs still present in gen_migration.py'; print('OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 5 exec CSV files exist at paths listed in <files>
    - Each CSV has the exact header `full_name,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3`
    - Moore + Miller + Brown + Lierman CSVs each have >= 10 data rows (per D-02 target 15-20; gate is >= 10 as proxy for "research happened")
    - Davis CSV either has >= 1 data row OR has header-only (zero data rows — gen_migration.py auto-emits not-found comment)
    - In every data row across all 5 CSVs: `full_name` matches the politician's canonical name; `topic_key` is not in {data-centers, local-immigration, transportation-priorities}; `value` is an integer 1-5; `source_url_1` is non-empty
    - `gen_migration.py` contains no remaining `REPLACE-WITH-DB-UUID` substring (real UUIDs filled in)
    - SUMMARY documents which agent ran when, total topics per exec, and any not-found notes (especially for Davis)
  </acceptance_criteria>
  <done>
    5 exec CSVs populated with evidence-only stances; gen_migration.py MD_EXEC_CANDIDATES has real UUIDs; ready to generate migration 282.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate + apply migration 282; run footer verification SELECTs</name>
  <files>
    C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql
  </files>
  <read_first>
    C:/EV-Accounts/backend/data/stance-research/gen_migration.py,
    C:/EV-Accounts/backend/migrations/216_sf_officials_stances.sql,
    .planning/phases/97-md-compass-stances-executives-senators-wave-1/97-PATTERNS.md
  </read_first>
  <action>
    (a) Run `python3 C:/EV-Accounts/backend/data/stance-research/gen_migration.py` to produce `C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql`. Confirm the script prints expected per-candidate counts and no `WARNING: Unknown topic_key` lines.

    (b) Read the generated SQL and confirm: header references "Migration 282" + "MD Executive Stances — 5 Constitutional Officers"; INSERT blocks present for all 5 execs OR `-- NOTE: No stances found in CSV` block for Davis if his CSV was header-only; verification footer SELECT queries are present.

    (c) Apply to production via `mcp__supabase-local__apply_migration` with `name="282_md_exec_stances"` and `query` set to the full file contents.

    (d) Run the footer verification SELECTs via `mcp__supabase-local__execute_sql`. Two queries (uncomment the SELECTs from the migration footer or run by hand):
      Q1 (per-candidate row count):
        SELECT p.full_name, COUNT(pa.topic_id) AS topic_count
        FROM essentials.politicians p
        LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
        WHERE p.external_id BETWEEN -240005 AND -240001
        GROUP BY p.id, p.full_name ORDER BY topic_count;
      Q2 (context pairing — must return 0):
        SELECT COUNT(*) FROM inform.politician_answers pa
        LEFT JOIN inform.politician_context pc
          ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
        WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -240005 AND -240001)
          AND pc.politician_id IS NULL;

    Record both query outputs in the SUMMARY. If Q2 > 0, FAIL the task and investigate (likely full_name mismatch — re-run gen_migration.py after fixing CSV).
  </action>
  <verify>
    <automated>
      python3 -c "import os; p=r'C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql'; assert os.path.exists(p),'282 SQL not generated'; t=open(p,encoding='utf-8').read(); assert 'Migration 282' in t,'header missing'; assert 'ON CONFLICT (politician_id, topic_id) DO UPDATE' in t,'idempotent upsert pattern missing'; assert 'inform.politician_answers' in t and 'inform.politician_context' in t,'target tables missing'; print('OK len=',len(t))"
    </automated>
    <automated>
      # Q2 context-pairing gate — must return 0; run via mcp__supabase-local__execute_sql and record output in SUMMARY
      echo "MANUAL — execute Q2 SQL above via mcp__supabase-local__execute_sql; expect COUNT = 0"
    </automated>
  </verify>
  <acceptance_criteria>
    - `C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql` exists and contains substring `Migration 282`
    - SQL file contains substring `ON CONFLICT (politician_id, topic_id) DO UPDATE` (idempotency)
    - SQL file contains INSERT INTO both `inform.politician_answers` and `inform.politician_context`
    - `mcp__supabase-local__apply_migration` returns success for name `282_md_exec_stances`
    - Q1 (per-candidate count) returns 5 rows (one per exec); Moore/Miller/Brown/Lierman each have COUNT >= 10; Davis row present (count may be 0 if not-found)
    - Q2 (context pairing) returns COUNT = 0 (no unpaired answer rows)
    - SUMMARY records Python command used, gen_migration.py stdout, Q1 + Q2 outputs verbatim
  </acceptance_criteria>
  <done>
    Migration 282 applied; MD-STANCES-01 satisfied for Moore/Miller/Brown/Lierman; Davis handled per evidence available; data integrity verified.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run via `mcp__supabase-local__execute_sql`:

```sql
-- A. All 5 execs appear with linked context rows (no orphan answers)
SELECT p.full_name, COUNT(DISTINCT pa.topic_id) AS answers, COUNT(DISTINCT pc.topic_id) AS contexts
FROM essentials.politicians p
LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
LEFT JOIN inform.politician_context pc ON pc.politician_id = p.id AND pc.topic_id = pa.topic_id
WHERE p.external_id BETWEEN -240005 AND -240001
GROUP BY p.full_name
ORDER BY p.full_name;
-- Expect: answers = contexts for every row.

-- B. Value range integrity (1-5 only)
SELECT MIN(value), MAX(value), COUNT(*) FILTER (WHERE value < 1 OR value > 5) AS out_of_range
FROM inform.politician_answers pa
JOIN essentials.politicians p ON p.id = pa.politician_id
WHERE p.external_id BETWEEN -240005 AND -240001;
-- Expect: out_of_range = 0; MIN >= 1; MAX <= 5.

-- C. Evidence-only check (every answer has a non-empty sources array)
SELECT COUNT(*) AS uncited
FROM inform.politician_answers pa
JOIN inform.politician_context pc USING (politician_id, topic_id)
JOIN essentials.politicians p ON p.id = pa.politician_id
WHERE p.external_id BETWEEN -240005 AND -240001
  AND (pc.sources IS NULL OR array_length(pc.sources, 1) IS NULL);
-- Expect: uncited = 0.
```
</verification>

<success_criteria>
- Migration 282 applied to production Supabase; all 5 MD execs covered (D-01)
- Moore/Miller/Brown/Lierman each have >= 10 cited stances in `inform.politician_answers`
- Davis is documented in migration 282 (stances or `-- NOTE: No stances found` comment per D-09)
- Every stance row has a corresponding context row (Q2 returns 0)
- Every stance value is 1-5; every context row has a non-empty sources[] array
- gen_migration.py extended with MD_EXEC section (reusable pattern for plans 97-02/03/04)
- compass-topics-reference.md created with 41 applicable topics (used by senator plans + Phase 98)
- All research ran sequentially, one agent at a time (per memory `feedback_stance_research_one_at_a_time.md`)
</success_criteria>

<output>
Create `.planning/phases/97-md-compass-stances-executives-senators-wave-1/97-01-SUMMARY.md` capturing: Python version + gen_migration.py invocation command, per-exec topic counts, Q1/Q2/B/C SQL outputs, any not-found notes for Davis, total elapsed wall-clock time.
</output>
