---
phase: 89-in-me-school-board-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts
  - C:/EV-Accounts/backend/scripts/smoke-phase89-in.ts
  - C:/EV-Accounts/backend/migrations/264_in_school_routing_fix.sql
autonomous: true
requirements:
  - IN-SCHOOL-01
  - IN-SCHOOL-02
tags:
  - in
  - school-boards
  - tiger
  - geofences
  - routing-fix
  - migration

must_haves:
  truths:
    - "G5420 geofence_boundaries rows exist for geo_id='1804770' (IPS) and geo_id='1800630' (MCCSC) with state='18', source='tiger_unsd_in_2024'"
    - "essentials.districts has SCHOOL rows with state='in' (LOWERCASE) for both IPS (geo_id='1804770') and MCCSC (geo_id='1800630'), each with mtfcc='G5420'"
    - "IPS D2 politician row reads full_name='Hasaan Rashid' (was 'Gayle Cosby') with first_name='Hasaan', last_name='Rashid'"
    - "IPS D3 politician 'Hope Duke Star' exists with external_id=-890001 and is linked via an office row to the IPS SCHOOL district"
    - "MCCSC D7 politician row reads full_name='Aja Jester' (was 'Brandon Shurr') with first_name='Aja', last_name='Jester'"
    - "All existing IPS offices (D1, D2, D3, D4, D5, At-Large×2) have non-NULL district_id pointing to the IPS SCHOOL district row"
    - "All existing MCCSC offices (D1-D7) have non-NULL district_id pointing to the MCCSC SCHOOL district row"
    - "Indianapolis coordinate (-86.1581, 39.7684) PostGIS ST_Covers query against G5420 returns the IPS G5420 geofence row and the SCHOOL routing JOIN returns at least 7 IPS commissioners"
    - "Bloomington coordinate (-86.5264, 39.1653) PostGIS ST_Covers query against G5420 returns the MCCSC G5420 geofence row and the SCHOOL routing JOIN returns 7 MCCSC trustees"
    - "Section-split check returns 0 orphans for the 2 IN GEOIDs (every G5420 row has a matching SCHOOL districts row with state='in')"
    - "Re-running migration 264 raises 'Migration 264 already applied' (idempotency via pre-flight)"
    - "supabase_migrations.schema_migrations contains version='264'"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts"
      provides: "TIGER UNSD downloader + shapefile parser + G5420 geofence_boundaries inserter for 2 IN school districts (IPS + MCCSC)"
      contains: "TARGET_GEOIDS"
      min_lines: 180
    - path: "C:/EV-Accounts/backend/scripts/smoke-phase89-in.ts"
      provides: "Phase 89 IN smoke test: G5420 count, SCHOOL district count, IPS routing (≥7 commissioners including Hope Duke Star + Hasaan Rashid), MCCSC routing (7 trustees including Aja Jester), section-split"
      contains: "smoke-phase89-in"
      min_lines: 120
    - path: "C:/EV-Accounts/backend/migrations/264_in_school_routing_fix.sql"
      provides: "IPS D3 INSERT + D2 UPDATE + MCCSC D7 UPDATE + 2 SCHOOL districts rows + office.district_id back-fill for all IPS + MCCSC offices + ledger entry"
      contains: "Migration 264"
      min_lines: 250
  key_links:
    - from: "essentials.geofence_boundaries (geo_id IN ('1804770','1800630'), mtfcc='G5420', state='18')"
      to: "essentials.districts (district_type='SCHOOL', state='in', same geo_ids)"
      via: "geo_id JOIN used by essentialsService.ts"
      pattern: "geo_id"
    - from: "essentials.offices.district_id (IPS + MCCSC offices)"
      to: "essentials.districts.id (district_type='SCHOOL', state='in')"
      via: "back-fill UPDATE inside migration 264"
      pattern: "UPDATE essentials.offices o SET district_id"
    - from: "Indianapolis address ST_Covers against G5420 (geo_id='1804770')"
      to: "7 IPS commissioner rows (D1 Thomas, D2 Rashid, D3 Hope Duke Star, D4 Impink, D5 Carey, AL Moore, AL Thompson)"
      via: "geofence_boundaries → districts → offices → politicians JOIN"
      pattern: "ST_Covers.*district_type = 'SCHOOL'"
---

<objective>
Fix Indiana school board routing by (1) loading G5420 TIGER UNSD geofences for Indianapolis Public Schools (GEOID='1804770') and Monroe County Community School Corporation (GEOID='1800630'), (2) applying corrective migration 264 that inserts the missing IPS D3 commissioner (Hope Duke Star), updates IPS D2 (Gayle Cosby → Hasaan Rashid), updates MCCSC D7 (Brandon Shurr → Aja Jester), inserts SCHOOL districts rows for both districts, and back-fills office.district_id on every IPS + MCCSC office so routing works, and (3) verifying with a smoke test.

CRITICAL CORRECTIONS — these MUST be applied verbatim (RESEARCH.md §CRITICAL GEOID CORRECTIONS):
- IPS TIGER GEOID is `1804770` (NOT 1800630 — that is MCCSC).
- MCCSC TIGER GEOID is `1800630` (NOT 1809480 — that is Richland Bean Borden, a different district in Ellettsville, IN).
- IPS has 5 geographic districts (D1-D5) + 2 At-Large = 7 commissioners total. **IPS has NO District 6.** The Phase 89 roadmap mention of "D6" is wrong.
- IPS D3 (Hope Duke Star) is missing entirely from the DB — must be INSERTed.
- IPS D2 currently shows Gayle Cosby (resigned March 2026); Hasaan Rashid was appointed April 2026 — must be UPDATEd.
- MCCSC D7 currently shows Brandon Shurr; Aja Jester replaced her in 2026 — must be UPDATEd.

This plan addresses IN-SCHOOL-01 (IPS 7 seats correctly seeded, routing wired) and IN-SCHOOL-02 (MCCSC board officials all 7 routable via the district).

Output: 2 G5420 rows in `essentials.geofence_boundaries`, 2 SCHOOL rows in `essentials.districts` with state='in', 1 new politician + office (Hope Duke Star), 2 UPDATEd politician rows (D2 IPS, D7 MCCSC), all IPS + MCCSC offices wired via district_id, migration 264 ledger entry, smoke test passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/89-in-me-school-board-completion/89-RESEARCH.md

# Canonical loader to copy (MUST READ — exact 5-constant substitution pattern):
@C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts

# Canonical OR loader as secondary reference:
@C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts

# Canonical migration patterns (corrective UPDATE + districts back-fill):
@C:/EV-Accounts/backend/migrations/254_or_school_districts.sql
@C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql

# Canonical smoke test pattern:
@C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write load-in-school-boundaries.ts (copy TX loader, swap to IN, 2 GEOIDs)</name>
  <files>C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts (canonical template — copy verbatim, change only the constants listed in the action below)
    - C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts (secondary reference; same overall shape — confirms helper functions downloadWithRedirects + extractZip + shapefile read loop)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§TIGER File Verification — Indiana row; §CRITICAL GEOID CORRECTIONS confirms 1804770 + 1800630)
  </read_first>
  <behavior>
    - Loader downloads `https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_18_unsd.zip` to `.tmp-in-school-unsd/` under cwd, extracts via AdmZip, opens the `.shp`/`.dbf` with the `shapefile` package, filters features to the 2 target GEOIDs (1804770 = IPS, 1800630 = MCCSC), and inserts each as a G5420 row into `essentials.geofence_boundaries` with state='18', source='tiger_unsd_in_2024', `ON CONFLICT (geo_id, mtfcc) DO NOTHING` for idempotency.
    - `--dry-run` mode prints `[dry-run] Would insert` lines and does NOT write to DB.
    - Diagnostic: logs the shapefile field names on the first feature and asserts 'GEOID' is present.
    - Asserts `districtMap.size === EXPECTED_COUNT` (2) after the shapefile loop — exits 1 with diagnostic if either GEOID is missing from the shapefile.
    - Post-insert verification COUNT against `state='18' AND mtfcc='G5420' AND source='tiger_unsd_in_2024'` returns 2 (warns if mismatched; does NOT fail the script if count is 2).
    - Cleanup removes the temp dir in `.finally()`.
    - If `process.env.DATABASE_URL` is missing, `console.error` and `process.exit(1)`.
  </behavior>
  <action>
    Create a new file at `C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts` by copying `C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts` verbatim and applying ONLY these substitutions (no other changes — preserve all imports, helpers, the PostGIS INSERT signature, error handling, and cleanup pattern):

    1. **Header docstring** — swap "Texas" / "tx" / "48" references for "Indiana" / "in" / "18" and update the example GEOID from `'4835100'` to `'1804770'`. Usage block becomes:
       `npx tsx scripts/load-in-school-boundaries.ts --dry-run`
       `npx tsx scripts/load-in-school-boundaries.ts`

    2. **Config block** — set exactly:
       - `TIGER_URL = 'https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_18_unsd.zip'`
       - `MTFCC = 'G5420'` (unchanged)
       - `STATE = '18'` (Indiana FIPS)
       - `SOURCE = 'tiger_unsd_in_2024'`
       - `EXPECTED_COUNT = 2`
       - `TARGET_GEOIDS = new Map<string, string>([['1804770', 'Indianapolis Public Schools'], ['1800630', 'Monroe County Community School Corporation']])`
       - `baseName = 'tl_2024_18_unsd'`
       - `tmpRoot = path.join(process.cwd(), '.tmp-in-school-unsd')`

    3. **Console log labels** — change `[load-tx-school-boundaries]` to `[load-in-school-boundaries]` everywhere (3 occurrences: startup log, fatal catch, final "Done" log).

    4. **Main() entry log** — change `'Fetching TX UNSD boundaries'` to `'Fetching IN UNSD boundaries'`.

    5. **Post-insert success log** — change `'All ${EXPECTED_COUNT} TX school district boundaries loaded successfully.'` to `'All ${EXPECTED_COUNT} IN school district boundaries loaded successfully.'`.

    Per RESEARCH.md §CRITICAL GEOID CORRECTIONS — verify the TARGET_GEOIDS map maps `'1804770'` to IPS and `'1800630'` to MCCSC (NOT the other way around — wrong mapping will route Indianapolis residents to Bloomington's board and vice versa). The 5-character GEOID prefix `18047` belongs to Marion County (Indianapolis); `18006` belongs to Monroe County (Bloomington).

    Do NOT add new logic. Do NOT change error handling. Do NOT change the cleanup pattern. Do NOT extend `load-state-tiger-boundaries.ts` — this is a standalone script following the same pattern as load-tx-school-boundaries.ts and load-or-school-boundaries.ts.

    Per memory `feedback_no_git_in_ev_accounts.md`: never run any git command in `C:/EV-Accounts/backend` (not a git repo).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/load-in-school-boundaries.ts --dry-run 2>&amp;1 | findstr /C:"All 2 target GEOIDs found"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts`
    - File contains literal string `TIGER_URL    = 'https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_18_unsd.zip'`
    - File contains literal string `STATE        = '18'`
    - File contains literal string `SOURCE       = 'tiger_unsd_in_2024'`
    - File contains literal string `EXPECTED_COUNT = 2`
    - File contains literal string `'1804770', 'Indianapolis Public Schools'`
    - File contains literal string `'1800630', 'Monroe County Community School Corporation'`
    - File contains literal string `baseName = 'tl_2024_18_unsd'`
    - File contains literal string `.tmp-in-school-unsd`
    - File contains literal string `[load-in-school-boundaries]` (and does NOT contain `[load-tx-school-boundaries]`)
    - File does NOT contain the literal string `'1809480'` (forbidden GEOID — Richland Bean Borden, would route Bloomington residents to the wrong district)
    - Dry-run command from `C:/EV-Accounts/backend` directory exits 0
    - Dry-run output identifies both target GEOIDs (e.g. logs "All 2 target GEOIDs found" or shows 2 "[dry-run] Would insert" lines for the 2 expected GEOIDs)
  </acceptance_criteria>
  <done>Loader file exists; dry-run succeeds without DB writes; shapefile contains both target GEOIDs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run load-in-school-boundaries.ts (live) — insert 2 G5420 rows</name>
  <files>essentials.geofence_boundaries (DB only — no source file modified)</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts (just authored in Task 1)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Architecture Patterns — System Architecture Diagram confirming G5420 insert with state='18')
  </read_first>
  <behavior>
    - Loader runs in live mode (no `--dry-run`), downloads the TIGER zip (or uses cache), and inserts 2 G5420 rows into `essentials.geofence_boundaries`.
    - Both rows have `state='18'`, `mtfcc='G5420'`, `source='tiger_unsd_in_2024'`, and a non-NULL PostGIS geometry.
    - Re-running the loader is a no-op (ON CONFLICT (geo_id, mtfcc) DO NOTHING).
  </behavior>
  <action>
    Execute the loader against production DB. Steps:

    1. Confirm working directory is `C:/EV-Accounts/backend` (NOT `C:/EV-Accounts`). dotenv lookup requires this exact cwd to find `.env` and `DATABASE_URL`. Established convention per STATE.md: "Run TIGER loader from C:/EV-Accounts/backend (not C:/EV-Accounts) — dotenv looks for .env in cwd".
    2. Confirm `DATABASE_URL` is set in `.env` (loader exits with error otherwise).
    3. Run: `npx tsx scripts/load-in-school-boundaries.ts` (no `--dry-run` flag).
    4. The loader logs each insert as `Inserted: <geoid> (<name>)` and the post-insert verification `All 2 IN school district boundaries loaded successfully.`
    5. If the run fails, do NOT manually edit `geofence_boundaries` — fix the loader, re-run.

    Per memory `feedback_no_git_in_ev_accounts.md`: do NOT run git commands in `C:/EV-Accounts` or `C:/EV-Accounts/backend`.
    Per memory `feedback_supabase_local_is_remote.md`: the DB targeted by `DATABASE_URL` is production — every INSERT is live.
  </action>
  <verify>
    <automated>node -e "const {Pool}=require('pg');require('dotenv').config({path:'C:/EV-Accounts/backend/.env'});const p=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});p.query(\"SELECT COUNT(*)::int AS cnt FROM essentials.geofence_boundaries WHERE state='18' AND mtfcc='G5420' AND source='tiger_unsd_in_2024'\").then(r=>{console.log('count='+r.rows[0].cnt);process.exit(r.rows[0].cnt===2?0:1)}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='18' AND mtfcc='G5420' AND source='tiger_unsd_in_2024'` returns exactly 2
    - `SELECT geo_id FROM essentials.geofence_boundaries WHERE state='18' AND mtfcc='G5420' ORDER BY geo_id` returns exactly these 2 values: `1800630`, `1804770` (no extras, no missing, no `1809480`)
    - `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id='1804770' AND mtfcc='G5420' AND geometry IS NOT NULL` returns 1
    - `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id='1800630' AND mtfcc='G5420' AND geometry IS NOT NULL` returns 1
    - PostGIS ST_Covers query at Indianapolis (-86.1581, 39.7684) against G5420 returns at least 1 row whose name matches `Indianapolis Public Schools`
    - PostGIS ST_Covers query at Bloomington (-86.5264, 39.1653) against G5420 returns at least 1 row whose name matches `Monroe County Community School Corporation`
  </acceptance_criteria>
  <done>2 G5420 rows persisted with valid PostGIS geometry; both target coordinates route to their correct ISD geofence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write smoke-phase89-in.ts smoke test (RED step before migration 264 applied)</name>
  <files>C:/EV-Accounts/backend/scripts/smoke-phase89-in.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts (canonical smoke test pattern — Client setup, AddressTest interface, error accumulator, finally cleanup, exit code logic)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Validation Architecture — Smoke Test Coordinates table for IPS + MCCSC; §Indianapolis Board Rosters — D3 Hope Duke Star, D2 Hasaan Rashid, D7 Aja Jester names that must appear)
  </read_first>
  <behavior>
    - Smoke test runs against production DB and verifies 6 assertions for IN routing.
    - All assertions run inside a single try/finally; client.end() in finally.
    - Errors accumulate into an array; assertions do NOT throw mid-test — final pass/fail block prints PASS/FAIL per SC and exits 0 (all pass) or 1 (any fail).
    - Pre-migration state: SC1 (G5420 count=2) passes after Task 2; SC2-SC6 must FAIL until migration 264 is applied. This proves the test would catch a real failure.
  </behavior>
  <action>
    Create a new TypeScript file at `C:/EV-Accounts/backend/scripts/smoke-phase89-in.ts` mirroring the structure of `smoke-multnomah-county.ts` (single-file Client-based smoke test with labeled assertions, error accumulator, exit-code path).

    REQUIRED IMPORTS: `import { Client } from 'pg'; import * as dotenv from 'dotenv'; dotenv.config();`

    REQUIRED main() flow:
    - Bail if `process.env['DATABASE_URL']` not set (console.error + process.exit(1)).
    - Create Client with `ssl: { rejectUnauthorized: false }`; `await client.connect()`.
    - try block runs the 6 assertions; finally calls `await client.end()`.
    - `let allPassed = true; const errors: string[] = [];` — push error strings on failure, set `allPassed=false`, do NOT throw.

    EXACT 6 ASSERTIONS (each must log `SC<N>: PASS` or push to `errors[]`):

    **SC1: 2 G5420 geofence_boundaries rows for IN.**
    Query: `SELECT COUNT(*)::int AS cnt FROM essentials.geofence_boundaries WHERE state='18' AND mtfcc='G5420' AND source='tiger_unsd_in_2024'`. Expected: 2.

    **SC2: 2 SCHOOL districts rows for IN (state='in' lowercase per RESEARCH.md Pitfall 5 equivalent).**
    Query: `SELECT COUNT(*)::int AS cnt FROM essentials.districts WHERE district_type='SCHOOL' AND state='in' AND geo_id IN ('1804770','1800630')`. Expected: 2.

    **SC3: IPS routing — Indianapolis coordinate returns at least 7 commissioners, AND list contains 'Hope Duke Star' AND 'Hasaan Rashid' AND does NOT contain 'Gayle Cosby'.**
    Query (parameters [-86.1581, 39.7684]):
    `SELECT p.full_name, d.geo_id FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.geofence_boundaries gb ON gb.geo_id=d.geo_id WHERE gb.state='18' AND d.district_type='SCHOOL' AND gb.mtfcc='G5420' AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($1,$2), 4326)) ORDER BY p.full_name`.
    Expected: `rows.length >= 7` AND every row has `d.geo_id === '1804770'` AND the set of `full_name` includes `'Hope Duke Star'` AND `'Hasaan Rashid'` AND does NOT include `'Gayle Cosby'`.

    **SC4: MCCSC routing — Bloomington coordinate returns 7 trustees, AND list contains 'Aja Jester' AND does NOT contain 'Brandon Shurr'.**
    Same query as SC3 with parameters [-86.5264, 39.1653]. Expected: `rows.length >= 7` AND every row has `d.geo_id === '1800630'` AND the set of `full_name` includes `'Aja Jester'` AND does NOT include `'Brandon Shurr'`.

    **SC5: Section-split check (per RESEARCH.md §Section-Split Check post-migration 264).**
    Query: `SELECT gb.geo_id FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('1804770','1800630') AND gb.mtfcc='G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.district_type='SCHOOL' AND d.state='in')`.
    Expected: `rows.length === 0`. If non-zero, list orphan geo_ids in the error.

    **SC6: Office back-fill — zero IPS or MCCSC offices have NULL district_id (per RESEARCH.md Pitfall 4).**
    Query: `SELECT COUNT(*)::int AS cnt FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE (g.name LIKE 'Indianapolis Public Schools%' OR g.name LIKE 'Monroe County Community School%') AND o.district_id IS NULL`.
    Expected: 0.

    FINAL OUTPUT BLOCK:
    - Log `=== Phase 89 IN Smoke Test Results ===` + one line per SC (PASS or FAIL with detail).
    - If `allPassed`: log `ALL ASSERTIONS PASSED`; `process.exit(0)`.
    - Else: log `FAILED (<N> assertion(s)):` then each error; `process.exit(1)`.
    - `main().catch(err => { console.error('Smoke test error:', err); process.exit(1); });`

    After writing the file, run `npx tsx scripts/smoke-phase89-in.ts` once. Before migration 264 has been applied, this MUST exit non-zero with SC2-SC6 failing (SC1 passes because Task 2 already loaded the geofences). This is the RED step proving the test would catch missing routing data.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-phase89-in.ts; if [ $? -ne 0 ]; then echo "RED-PHASE-PASS: smoke fails before migration 264 applied (expected)"; else echo "FAIL: smoke passed before migration applied — invalid initial state"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: smoke-phase89-in.ts exists
    - SOURCE: contains both GEOIDs `'1804770'` and `'1800630'`
    - SOURCE: does NOT contain GEOID `'1809480'`
    - SOURCE: contains `district_type='SCHOOL'` and `state='in'` (lowercase)
    - SOURCE: contains the Indianapolis coordinate `-86.1581` and `39.7684`
    - SOURCE: contains the Bloomington coordinate `-86.5264` and `39.1653`
    - SOURCE: contains both `Hope Duke Star` and `Hasaan Rashid` and `Aja Jester` (positive assertions)
    - SOURCE: contains both `Gayle Cosby` and `Brandon Shurr` (negative assertions — must NOT appear in routing results)
    - SOURCE: contains `ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($1, $2), 4326))` and `gb.mtfcc = 'G5420'`
    - SOURCE: contains a section-split NOT EXISTS query covering both IN GEOIDs
    - SOURCE: contains `process.exit(0)` on the all-pass path and `process.exit(1)` on the fail path
    - BEHAVIOR (RED step): `npx tsx scripts/smoke-phase89-in.ts` exits non-zero before migration 264 is applied; at minimum SC2 (SCHOOL districts count), SC3 (IPS routing), SC4 (MCCSC routing), SC6 (office back-fill) report failures
    - BEHAVIOR: stdout output identifies each SC as PASS or FAIL with a one-line explanation
  </acceptance_criteria>
  <done>smoke-phase89-in.ts authored; pre-migration RED run exits non-zero with the expected failures reported.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write migration 264 — IPS D3 INSERT + D2 UPDATE + MCCSC D7 UPDATE + 2 SCHOOL districts + office back-fill</name>
  <files>C:/EV-Accounts/backend/migrations/264_in_school_routing_fix.sql</files>
  <read_first>
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Migration 264 — IN Corrections — contains the canonical 8-step pattern; §Common Pitfalls 1-7; §Indianapolis Board Rosters for confirmed names and seat structure; §Open Questions 4 and 5 — must pre-query the DB before writing the SQL)
    - C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql (canonical pre-flight + post-verification DO block pattern)
    - C:/EV-Accounts/backend/migrations/254_or_school_districts.sql (canonical districts INSERT + office wiring pattern; canonical politician+office CTE block; office_id back-fill pattern)
    - .planning/STATE.md (entry `Next migration is 244` is outdated; actual last applied is 263 per RESEARCH.md §Migration Plan — migration 264 is correct next number)
  </read_first>
  <behavior>
    - Migration applies in a single `BEGIN; ... COMMIT;` transaction.
    - 4 pre-flight DO blocks abort with `RAISE EXCEPTION` if (a) the IPS or MCCSC government rows do not exist, (b) Hope Duke Star already exists in `essentials.politicians`, (c) external_id -890001 is occupied, (d) the 2 G5420 geofence_boundaries rows for IN do not exist (loader must have run first).
    - All 8 corrective steps (per RESEARCH.md §Migration 264 — IN Corrections) execute in order:
      Step 1: UPDATE IPS D2 (Gayle Cosby → Hasaan Rashid)
      Step 2: INSERT IPS SCHOOL districts row (state='in' lowercase, geo_id='1804770')
      Step 3: INSERT MCCSC SCHOOL districts row (state='in' lowercase, geo_id='1800630')
      Step 4: UPDATE all IPS offices to set district_id (back-fill for offices with district_id IS NULL)
      Step 5: UPDATE all MCCSC offices to set district_id (back-fill for offices with district_id IS NULL)
      Step 6: INSERT IPS D3 (Hope Duke Star) politician + office (CTE pattern)
      Step 7: UPDATE MCCSC D7 (Brandon Shurr → Aja Jester)
      Step 8: Back-fill politicians.office_id where external_id=-890001 AND office_id IS NULL
    - Post-verification DO block with 7 gates (RAISE EXCEPTION on any failure):
      (a) IPS SCHOOL districts row exists with state='in', geo_id='1804770'
      (b) MCCSC SCHOOL districts row exists with state='in', geo_id='1800630'
      (c) Hope Duke Star exists in politicians with external_id=-890001 and has a non-NULL office_id
      (d) No IPS politician has full_name='Gayle Cosby' (D2 must be Hasaan Rashid)
      (e) No MCCSC politician has full_name='Brandon Shurr' (D7 must be Aja Jester)
      (f) Zero IPS or MCCSC offices have NULL district_id
      (g) Section-split check: 0 orphan G5420 rows for the 2 IN GEOIDs
    - Ledger entry: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('264') ON CONFLICT (version) DO NOTHING;` before COMMIT.
    - File is UTF-8.
  </behavior>
  <action>
    Write `C:/EV-Accounts/backend/migrations/264_in_school_routing_fix.sql`. Follow the structure from `261_tx_collin_county_school_boards.sql` (pre-flight + transactional body + post-verification + ledger) but adapt the BODY to the corrective 8-step plan in RESEARCH.md §Migration 264 — IN Corrections (verbatim).

    **CRITICAL pre-execution discovery steps (per RESEARCH.md §Open Questions 4 and 5):**
    Before drafting the SQL, run these read-only queries against the DB and use the results to choose exact title and last_name values:
    1. `SELECT DISTINCT title FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE g.name LIKE 'Indianapolis Public Schools%' ORDER BY title` — capture the IPS title convention. If existing offices use `'Commissioner'`, the new D3 INSERT MUST use `'Commissioner'` for consistency. If they use `'Board Member'`, use `'Board Member'`. Pick whichever appears for the other 6 IPS seats.
    2. `SELECT full_name FROM essentials.politicians WHERE full_name LIKE '%Iruoje%'` — record the exact spelling in the DB for MCCSC D4. Do NOT change this name (per RESEARCH.md Open Question 5).
    3. `SELECT external_id, full_name FROM essentials.politicians WHERE full_name='Gayle Cosby'` — capture Gayle Cosby's external_id (it will be preserved through the UPDATE; only full_name/first_name/last_name change).
    4. `SELECT external_id, full_name FROM essentials.politicians WHERE full_name='Brandon Shurr'` — capture Brandon Shurr's external_id (preserved through UPDATE).

    Record these 4 query results in the SUMMARY (Task 5).

    **Header comment block:**
    ```
    -- Migration 264: Indiana school routing fix
    -- Corrects IPS + MCCSC routing per Phase 89 Plan 01.
    --
    -- Actions:
    --   1. UPDATE IPS D2 (Gayle Cosby → Hasaan Rashid; same external_id preserved)
    --   2. INSERT IPS SCHOOL districts row (geo_id='1804770', state='in' LOWERCASE)
    --   3. INSERT MCCSC SCHOOL districts row (geo_id='1800630', state='in' LOWERCASE)
    --   4. Back-fill district_id on all existing IPS offices
    --   5. Back-fill district_id on all existing MCCSC offices
    --   6. INSERT IPS D3 Hope Duke Star (external_id=-890001) + office
    --   7. UPDATE MCCSC D7 (Brandon Shurr → Aja Jester; same external_id preserved)
    --   8. Back-fill politicians.office_id on new D3 row
    --
    -- CRITICAL rules:
    --   - IPS GEOID = 1804770 (NOT 1800630 — that is MCCSC)
    --   - MCCSC GEOID = 1800630 (NOT 1809480 — that is Richland Bean Borden, wrong district)
    --   - IPS has 5 districts + 2 At-Large = 7 seats. NO District 6.
    --   - districts.state = 'in' LOWERCASE (Pitfall 5 in RESEARCH.md)
    --   - offices.representing_state = 'IN' UPPERCASE
    --   - district_type = 'SCHOOL' (NOT 'SCHOOL_DISTRICT')
    --   - G5420 geofence rows MUST exist before this migration (loader runs first)
    --   - slug column NEVER included in any chambers INSERT (chambers are not modified in this migration anyway)
    --
    -- Run: <YYYY-MM-DD>
    ```

    **Pre-flight DO blocks (4):**
    1. Both governments exist: `IF NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name LIKE 'Indianapolis Public Schools%')` RAISE EXCEPTION 'Pre-flight FAILED: IPS government not found — Phase 89 Plan 01 requires existing IPS seed'. Then same for `'Monroe County Community School%'`.
    2. Hope Duke Star does not already exist: `IF EXISTS (SELECT 1 FROM essentials.politicians WHERE full_name='Hope Duke Star')` RAISE EXCEPTION 'Pre-flight FAILED: Hope Duke Star already exists — migration already applied?'.
    3. external_id -890001 is unoccupied: `IF EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id=-890001)` RAISE EXCEPTION 'Pre-flight FAILED: external_id -890001 already occupied'.
    4. G5420 geofence rows exist: `SELECT COUNT(*) INTO v_count FROM essentials.geofence_boundaries WHERE geo_id IN ('1804770','1800630') AND mtfcc='G5420'; IF v_count <> 2 THEN RAISE EXCEPTION 'Pre-flight FAILED: expected 2 G5420 rows for IN (geo_ids 1804770 + 1800630), found %  — run load-in-school-boundaries.ts first', v_count; END IF;`

    **Body (8 steps — use exactly the SQL pattern from RESEARCH.md §Migration 264 — IN Corrections):**

    Step 1 — IPS D2 UPDATE: `UPDATE essentials.politicians SET full_name='Hasaan Rashid', first_name='Hasaan', last_name='Rashid' WHERE full_name='Gayle Cosby' AND EXISTS (SELECT 1 FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE o.politician_id=essentials.politicians.id AND g.name LIKE 'Indianapolis Public Schools%');`

    Step 2 — IPS SCHOOL district: `INSERT INTO essentials.districts (id, district_type, state, geo_id, label, mtfcc) SELECT gen_random_uuid(), 'SCHOOL', 'in', '1804770', 'Indianapolis Public Schools', 'G5420' WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE geo_id='1804770' AND district_type='SCHOOL' AND state='in');`

    Step 3 — MCCSC SCHOOL district: same as Step 2 with geo_id='1800630' and label='Monroe County Community School Corporation'.

    Step 4 — IPS office back-fill: `UPDATE essentials.offices o SET district_id=(SELECT d.id FROM essentials.districts d WHERE d.geo_id='1804770' AND d.district_type='SCHOOL' AND d.state='in') WHERE o.chamber_id IN (SELECT ch.id FROM essentials.chambers ch JOIN essentials.governments g ON g.id=ch.government_id WHERE g.name LIKE 'Indianapolis Public Schools%') AND o.district_id IS NULL;`

    Step 5 — MCCSC office back-fill: same as Step 4 with geo_id='1800630' and government name `'Monroe County Community School%'`.

    Step 6 — IPS D3 INSERT + office (CTE pattern from migration 254 lines 90-119): Insert politician (full_name='Hope Duke Star', first_name='Hope', last_name='Star', party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id=-890001), then INSERT office with title=<value from Discovery Step 1 above — `'Commissioner'` if existing IPS offices use Commissioner, else `'Board Member'`>, representing_state='IN', is_appointed_position=false, role_canonical=NULL, district_id from the IPS SCHOOL districts row, chamber_id from the IPS chamber, politician_id from the CTE. Use `ON CONFLICT (external_id) DO NOTHING` on the politician insert. The office uses a NOT EXISTS guard against (district_id, politician_id).

    Step 7 — MCCSC D7 UPDATE: `UPDATE essentials.politicians SET full_name='Aja Jester', first_name='Aja', last_name='Jester' WHERE full_name='Brandon Shurr' AND EXISTS (SELECT 1 FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE o.politician_id=essentials.politicians.id AND g.name LIKE 'Monroe County Community School%');`

    Step 8 — office_id back-fill: `UPDATE essentials.politicians SET office_id=o.id FROM essentials.offices o WHERE essentials.politicians.id=o.politician_id AND essentials.politicians.external_id=-890001 AND essentials.politicians.office_id IS NULL;`

    **Post-verification DO block (7 gates — RAISE EXCEPTION on any failure):**
    - (a) `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='in' AND geo_id='1804770'` = 1
    - (b) `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='in' AND geo_id='1800630'` = 1
    - (c) `SELECT COUNT(*) FROM essentials.politicians WHERE external_id=-890001 AND full_name='Hope Duke Star' AND office_id IS NOT NULL` = 1
    - (d) `SELECT COUNT(*) FROM essentials.politicians p WHERE p.full_name='Gayle Cosby'` = 0 (UPDATE step 1 must have replaced her name)
    - (e) `SELECT COUNT(*) FROM essentials.politicians p WHERE p.full_name='Brandon Shurr'` = 0 (UPDATE step 7 must have replaced her name)
    - (f) `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE (g.name LIKE 'Indianapolis Public Schools%' OR g.name LIKE 'Monroe County Community School%') AND o.district_id IS NULL` = 0
    - (g) Section-split: `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('1804770','1800630') AND gb.mtfcc='G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.district_type='SCHOOL' AND d.state='in')` = 0
    - On all-pass: `RAISE NOTICE 'Migration 264 post-verification PASSED: IPS D3 added, IPS D2 updated, MCCSC D7 updated, 2 SCHOOL districts inserted, all IPS+MCCSC offices wired, 0 orphans.'`

    **Ledger entry + COMMIT (last 4 lines):**
    `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('264') ON CONFLICT (version) DO NOTHING;`
    `COMMIT;`

    **File encoding:** UTF-8.

    Do NOT apply the migration in this task — Task 5 applies it via mcp__supabase-local.

    Per memory `feedback_no_git_in_ev_accounts.md`: never run git in `C:/EV-Accounts`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('C:/EV-Accounts/backend/migrations/264_in_school_routing_fix.sql','utf8');const needles=['Migration 264','1804770','1800630','Hope Duke Star','Hasaan Rashid','Aja Jester','Gayle Cosby','Brandon Shurr','district_type','SCHOOL','-890001','VALUES (\\u0027264\\u0027)'];const missing=needles.filter(n=>!c.includes(n));if(missing.length){console.error('MISSING:',missing);process.exit(1)}if(c.includes('1809480')){console.error('FAIL: contains forbidden GEOID 1809480 (Richland Bean Borden, wrong district)');process.exit(1)}if(c.includes('SCHOOL_DISTRICT')){console.error('FAIL: contains forbidden token SCHOOL_DISTRICT');process.exit(1)}if(/district_type\\s*=\\s*\\u0027SCHOOL\\u0027\\s+AND\\s+state\\s*=\\s*\\u0027IN\\u0027/i.test(c)){console.error('FAIL: districts.state must be \\u0027in\\u0027 LOWERCASE, found uppercase IN');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/264_in_school_routing_fix.sql`
    - File contains literal `Migration 264`
    - File contains literal `1804770` (IPS GEOID — RESEARCH.md §CRITICAL GEOID CORRECTIONS)
    - File contains literal `1800630` (MCCSC GEOID)
    - File contains literal `Hope Duke Star`, `Hasaan Rashid`, `Aja Jester` (new/updated names)
    - File contains literal `Gayle Cosby` and `Brandon Shurr` (in the WHERE clauses of the UPDATE statements only)
    - File contains literal `'SCHOOL'` for `district_type`
    - File contains literal `'in'` for districts.state (lowercase — per RESEARCH.md Pitfall 5 equivalent)
    - File does NOT contain literal `'1809480'` (Richland Bean Borden — forbidden, would route Bloomington to the wrong district)
    - File does NOT contain literal `'SCHOOL_DISTRICT'` (forbidden — would break routing per Phase 62 LAUSD precedent)
    - File does NOT contain any INSERT INTO essentials.chambers statement that includes a `slug` column (slug is GENERATED ALWAYS — would error if present; chambers are not modified in this migration so the pattern should not appear at all)
    - File contains literal `-890001` (Hope Duke Star external_id)
    - File contains literal `VALUES ('264')` (ledger entry)
    - File contains exactly 4 pre-flight DO blocks (governments exist, Hope Duke Star absent, external_id clear, G5420 geofences=2)
    - File contains exactly 7 post-verification gates inside the post-verification DO block
    - File does NOT include any `District 6` or `D6` reference for IPS (RESEARCH.md §Common Pitfalls Pitfall 2 — IPS has no D6)
  </acceptance_criteria>
  <done>Migration 264 SQL file authored with all 8 corrective steps, 4 pre-flight gates, 7 post-verification gates, UTF-8 encoded. Not yet applied — Task 5 applies it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Apply migration 264 to production DB and run smoke-phase89-in.ts (GREEN)</name>
  <files>
    - essentials.politicians (UPDATE D2 IPS, UPDATE D7 MCCSC, INSERT D3 IPS)
    - essentials.districts (INSERT 2 SCHOOL rows for IN)
    - essentials.offices (back-fill district_id for IPS + MCCSC offices, INSERT 1 office for Hope Duke Star)
    - supabase_migrations.schema_migrations (INSERT version='264')
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/264_in_school_routing_fix.sql (authored in Task 4)
    - C:/EV-Accounts/backend/scripts/smoke-phase89-in.ts (authored in Task 3)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Validation Architecture — Smoke Test Coordinates; §Common Pitfalls 1, 3, 4)
  </read_first>
  <behavior>
    - Migration 264 applies cleanly via the Supabase MCP server (mcp__supabase-local IS production per memory `feedback_supabase_local_is_remote.md`).
    - All 4 pre-flight DO blocks pass; all 8 steps execute; all 7 post-verification gates pass.
    - Ledger row for version='264' is present.
    - Smoke test `smoke-phase89-in.ts` exits 0 with all 6 SC PASS.
    - Re-applying migration 264 raises a pre-flight EXCEPTION (idempotency proof).
  </behavior>
  <action>
    Per RESEARCH.md §Environment Availability and memory `feedback_supabase_local_is_remote.md`:

    1. Apply migration 264 via `mcp__supabase-local__apply_migration`. Migration name: `264_in_school_routing_fix`. Pass the entire SQL content from Task 4.
    2. If the apply call surfaces any pre-flight `RAISE EXCEPTION` (e.g. "G5420 geofence rows not found"), do NOT bypass — go back to Task 2 (loader) and fix the underlying cause. Do NOT edit the SQL to skip pre-flights.
    3. After apply succeeds, verify the 7 in-migration post-verification gates have run (look for the final `RAISE NOTICE 'Migration 264 post-verification PASSED'` in the apply output).
    4. Run additional independent verification queries via `mcp__supabase-local__execute_sql`:
       - `SELECT version FROM supabase_migrations.schema_migrations WHERE version='264'` returns 1 row
       - `SELECT full_name FROM essentials.politicians WHERE external_id=-890001` returns 1 row with `Hope Duke Star`
       - `SELECT COUNT(*) FROM essentials.politicians WHERE full_name='Gayle Cosby'` returns 0
       - `SELECT COUNT(*) FROM essentials.politicians WHERE full_name='Brandon Shurr'` returns 0
       - `SELECT COUNT(*) FROM essentials.politicians WHERE full_name='Hasaan Rashid'` returns 1
       - `SELECT COUNT(*) FROM essentials.politicians WHERE full_name='Aja Jester'` returns 1
       - `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='in' AND geo_id IN ('1804770','1800630')` returns 2
       - `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE (g.name LIKE 'Indianapolis Public Schools%' OR g.name LIKE 'Monroe County Community School%') AND o.district_id IS NULL` returns 0
    5. Run smoke test: `cd /c/EV-Accounts/backend && npx tsx scripts/smoke-phase89-in.ts`. Must exit 0 with all 6 SC PASS and `ALL ASSERTIONS PASSED` printed.
    6. Idempotency check: attempt to apply migration 264 a second time via `mcp__supabase-local__apply_migration`. The pre-flight `RAISE EXCEPTION 'Pre-flight FAILED: Hope Duke Star already exists'` (or equivalent) must fire — no second apply should succeed.

    Per memory `feedback_no_git_in_ev_accounts.md`: never run git in `C:/EV-Accounts`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-phase89-in.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration 264 applied successfully via `mcp__supabase-local__apply_migration` (no exceptions raised by pre-flights or post-verification block; final `RAISE NOTICE 'Migration 264 post-verification PASSED'` visible in apply output)
    - `SELECT COUNT(*) FROM supabase_migrations.schema_migrations WHERE version='264'` returns 1
    - `SELECT full_name FROM essentials.politicians WHERE external_id=-890001` returns exactly `Hope Duke Star`
    - `SELECT COUNT(*) FROM essentials.politicians WHERE full_name='Gayle Cosby'` returns 0 (replaced by Hasaan Rashid)
    - `SELECT COUNT(*) FROM essentials.politicians WHERE full_name='Brandon Shurr'` returns 0 (replaced by Aja Jester)
    - `SELECT COUNT(*) FROM essentials.politicians WHERE full_name='Hasaan Rashid'` returns 1
    - `SELECT COUNT(*) FROM essentials.politicians WHERE full_name='Aja Jester'` returns 1
    - `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='in' AND geo_id IN ('1804770','1800630')` returns 2
    - `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE (g.name LIKE 'Indianapolis Public Schools%' OR g.name LIKE 'Monroe County Community School%') AND o.district_id IS NULL` returns 0
    - PostGIS routing query at Indianapolis (-86.1581, 39.7684) using the standard SCHOOL routing JOIN returns ≥ 7 commissioner rows, including `Hope Duke Star` and `Hasaan Rashid`
    - PostGIS routing query at Bloomington (-86.5264, 39.1653) returns 7 trustee rows, including `Aja Jester` and excluding `Brandon Shurr`
    - `npx tsx scripts/smoke-phase89-in.ts` exits 0 and stdout contains `ALL ASSERTIONS PASSED`
    - Re-applying migration 264 fails with a pre-flight `RAISE EXCEPTION` referencing either `Hope Duke Star already exists` or `external_id -890001 already occupied` (idempotency proof)
  </acceptance_criteria>
  <done>Migration 264 applied, all 7 post-verification gates green, smoke test passes with ALL ASSERTIONS PASSED, idempotency confirmed. IN-SCHOOL-01 and IN-SCHOOL-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov → loader → DB | Loader downloads TIGER UNSD zip over HTTPS and writes parsed PostGIS geometry to production DB. Source is U.S. Census Bureau (trusted public data); no user input crosses. |
| Migration file → DB | SQL applied via Supabase MCP server. No user input; pre-flight DO blocks abort on inconsistent state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering | TIGER zip integrity | accept | census.gov HTTPS; consistent with established loader pattern (load-tx/load-or). EXPECTED_COUNT=2 assertion catches malformed extracts. |
| T-89-02 | Repudiation | Migration ledger | mitigate | `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('264')` provides audit trail; pre-flight DO blocks prevent silent re-runs (4 different abort paths). |
| T-89-03 | Information Disclosure | Board member PII | accept | All seeded data is public record from official IPS + MCCSC websites; party=NULL per antipartisan design. |
| T-89-04 | Denial of Service | Migration locks production DB | mitigate | Single transaction with bounded scope (2 districts + 1 INSERT + 2 UPDATEs + 2 back-fill UPDATEs); typical apply time < 5s. |
| T-89-05 | Tampering | Wrong GEOID → wrong school district routing | mitigate | Hard-coded constants `'1804770'` (IPS) and `'1800630'` (MCCSC) in both loader and migration; explicit acceptance criteria forbid `'1809480'` (Richland Bean Borden, different district); RESEARCH.md §CRITICAL GEOID CORRECTIONS documents the historical confusion. |
| T-89-06 | Spoofing | Stale politician (Cosby/Shurr) shown to user | mitigate | Migration UPDATE statements use both `full_name` match and a chamber/government EXISTS guard so the wrong politician (with the same name elsewhere) cannot be updated; post-verification gates (d)/(e) abort if either old name still resolves. |
| T-89-SC | Tampering | npm package installs | accept | No new npm dependencies (pg, adm-zip, shapefile, dotenv, https already in package.json — same as Phase 86/88). |
</threat_model>

<verification>
After all 5 tasks complete:

1. `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='18' AND mtfcc='G5420' AND source='tiger_unsd_in_2024'` returns 2 (Task 2 outcome)
2. `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='in' AND geo_id IN ('1804770','1800630')` returns 2 (Task 5 outcome)
3. `SELECT full_name FROM essentials.politicians WHERE external_id=-890001` returns `Hope Duke Star` (Task 5 outcome)
4. No politician row exists for `Gayle Cosby` or `Brandon Shurr`
5. Indianapolis coordinate ST_Covers routing returns ≥ 7 commissioners including Hope Duke Star + Hasaan Rashid
6. Bloomington coordinate ST_Covers routing returns 7 trustees including Aja Jester
7. Section-split check returns 0 rows
8. `npx tsx scripts/smoke-phase89-in.ts` exits 0 with ALL ASSERTIONS PASSED
9. Re-running migration 264 raises a pre-flight EXCEPTION (idempotency)
</verification>

<success_criteria>
Plan 01 is COMPLETE when ALL of the following are true:

1. `C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts` exists; `--dry-run` reports both target GEOIDs found
2. Loader has run live: 2 G5420 rows present in `essentials.geofence_boundaries` (state='18', source='tiger_unsd_in_2024')
3. `C:/EV-Accounts/backend/scripts/smoke-phase89-in.ts` exists with 6 labeled assertions (SC1-SC6) and exited non-zero on the pre-migration RED run
4. `C:/EV-Accounts/backend/migrations/264_in_school_routing_fix.sql` exists, applied to production, ledger row for '264' present
5. All 7 post-verification gates inside migration 264 raised NOTICE (not EXCEPTION) — every gate green
6. `npx tsx scripts/smoke-phase89-in.ts` exits 0 with ALL ASSERTIONS PASSED (GREEN step)
7. Re-running migration 264 raises a pre-flight EXCEPTION (idempotency)
8. IN-SCHOOL-01 (IPS 7 seats correctly seeded incl. Hope Duke Star + Hasaan Rashid, routing wired) verifiable
9. IN-SCHOOL-02 (MCCSC 7 trustees incl. Aja Jester, routing wired) verifiable
</success_criteria>

<output>
Create `.planning/phases/89-in-me-school-board-completion/89-01-SUMMARY.md` when done. Required sections:
- Files modified (3 absolute paths)
- Loader run output: inserted vs skipped counts
- Migration 264 application output: post-verification NOTICE text
- Smoke test output: PASS/FAIL per SC plus the `ALL ASSERTIONS PASSED` line
- IPS title convention captured from pre-execution discovery (Commissioner vs Board Member); if Commissioner, confirmed used for Hope Duke Star's office
- MCCSC D4 full name as stored in DB (confirmation that no change was needed)
- Gayle Cosby and Brandon Shurr external_ids captured (UPDATEd, not re-INSERTed)
- Next migration after this plan: 265 (ME school board seed — Plan 02)
</output>
