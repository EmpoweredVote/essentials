---
phase: 169-dark-mode-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["169-01"]
files_modified:
  - src/index.css
  - src/components/Layout.jsx
  - src/components/FilterBar.jsx
  - src/pages/Results.jsx
autonomous: false
requirements: [DARK-02]

must_haves:
  truths:
    - "Results in dark mode reads as the Figma GitHub-dark treatment: page #0d1117, surfaces #161b22, teal #00c8d7 accents, hairline borders, no drop-shadows."
    - "The global header chrome (Layout + ev-ui Header) matches the dark page — no navy-blue mismatch, no light bleed."
    - "ev-ui GovernmentBodySection / SubGroupSection / PoliticianCard display correctly on dark via .dark .ev-* !important overrides."
    - "The PoliticianCard 4:5 tile is unchanged in shape and size — only re-colored."
    - "No faint-gray-on-dark: muted/inactive text floors at #8b949e on the in-scope surfaces."
    - "Light mode renders unchanged (no regression) — the html:not(.dark) WCAG blocks and light branches of every isDark ternary are untouched."
  artifacts:
    - path: "src/index.css"
      provides: ".dark .ev-* override blocks repointed to the new palette (PoliticianCard, gov-body-section eyebrow, header child hooks); html:not(.dark) blocks preserved"
      contains: "#161b22"
    - path: "src/components/Layout.jsx"
      provides: "Header dark style bg #0d1117"
      contains: "#0d1117"
    - path: "src/components/FilterBar.jsx"
      provides: "Minimal dark re-theme (dark-side ternary literals to new palette; light side + 44px preserved)"
    - path: "src/pages/Results.jsx"
      provides: "Card/compass/tier-eyebrow/link inline literals repointed to new palette; eyebrow gets Manrope D-06 type"
  key_links:
    - from: "src/index.css .dark .ev-politician-card"
      to: "ev-ui inline-styled PoliticianCard"
      via: "!important override"
      pattern: "\\.dark \\.ev-politician-card"
    - from: "src/components/Layout.jsx Header style prop"
      to: "ev-ui Header background"
      via: "isDark conditional style"
      pattern: "#0d1117"
    - from: "src/pages/Results.jsx tier eyebrow"
      to: "accent token / Manrope label"
      via: "isDark inline color"
      pattern: "#00c8d7"
---

<objective>
Apply the Plan-01 token palette to the in-scope dark surfaces: the `.dark .ev-*` override blocks in
`src/index.css` (PoliticianCard, government-body section eyebrow, header child hooks), the global
header bg in `Layout.jsx`, a minimal dark re-theme of `FilterBar.jsx`, and the inline-literal sweep +
eyebrow type on `Results.jsx`. Then verify the Figma dark treatment, header match, no faint-gray,
PoliticianCard geometry, and light-mode no-regression via the manual-visual checkpoint.

Purpose: DARK-02 — Results/Representatives + global header chrome render in the Figma dark treatment,
driven by the Plan-01 tokens, with no scattered inline literals on the in-scope surface and no light bleed.
Output: Repointed override blocks + component literals (dark-side only), Manrope eyebrow labels, and a
signed-off visual verification mapping 1:1 to the 5 ROADMAP success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-dark-mode-design-system-foundation/169-CONTEXT.md
@.planning/phases/169-dark-mode-design-system-foundation/169-RESEARCH.md
@.planning/phases/169-dark-mode-design-system-foundation/169-PATTERNS.md
@.planning/phases/169-dark-mode-design-system-foundation/169-UI-SPEC.md
@.planning/phases/169-dark-mode-design-system-foundation/169-VALIDATION.md
@.planning/phases/169-dark-mode-design-system-foundation/169-01-SUMMARY.md

<interfaces>
<!-- Established in-file override/ternary patterns the executor replicates (from RESEARCH + PATTERNS, line-verified). -->

src/index.css .dark .ev-* override blocks (the spine — !important is mandatory, ev-ui ships inline styles):
  PoliticianCard (canonical, L121-137): bg #1a2235→#161b22; hover #1e2a3a→#21262d (elevated token);
    h3 #f3f4f6→#e6edf3; p #d1d5db→#8b949e; border rgba(255,255,255,0.08) KEEP.
  gov-body-section eyebrow (L338-341): [role="button"] span color #59b0c4→#00c8d7 + Manrope 600 uppercase 1.2px (D-06).
  header child hooks (RESEARCH): .ev-header-secondary / .ev-header-nav / .ev-header-mobile-menu need .dark { color:#e6edf3 !important } overrides (ev-ui hardcodes old palette internally; <header> root has NO class).
  PRESERVE VERBATIM: html:not(.dark) light WCAG fixes (~L353-358) and @source not lines (L8-9).

src/components/Layout.jsx (L62-65): Header style={isDark ? { backgroundColor:'#020618' → '#0d1117', borderBottom:'1px solid rgba(255,255,255,0.08)' KEEP } : undefined}.

src/components/FilterBar.jsx (D-09 MINIMAL — swap ONLY dark side of each isDark ternary, keep light side + 44px):
  Dropdown L26-29: bg #1a2235→#161b22; active #59b0c4→#00c8d7; dark idle border #2d3f5a→#2d3748; dark idle text #d1d5db→#8b949e; chevron %2359b0c4→%2300c8d7.
  main L82-87: inputBg #1a2235→#161b22; inputBorder #2d3f5a→#2d3748; inputBorderFocus #59b0c4→#00c8d7; inputText #d1d5db→#8b949e; iconStroke #59b0c4→#00c8d7.
  checkbox accentColor (L159) #59b0c4→#00c8d7; <option> bg (L62) #1a2235→#161b22.

src/pages/Results.jsx inline-literal sites:
  compassBg (L1427): isDark #1a2235→#161b22 (candidate #fffef5/#fff KEEP).
  card style (L1438): isDark { backgroundColor:#1a2235→#161b22, borderColor:#2d3f5a→#2d3748 }; borderRadius:10 KEEP (control radius); candidate #fffef5/#fed12e KEEP.
  MiniCompass overlay bg (L1486/L1506): #1a2235→#161b22.
  tier eyebrow (L1892 AND L1916, identical): color isDark #59b0c4→#00c8d7 + apply Manrope/font-display per D-06; !isDark tierStyle.bg gating L1889/L1913 LIGHT-ONLY → DO NOT TOUCH.
  treasury link (L1939): dark:text-[#59b0c4]→dark:text-[#00c8d7]; KEEP dark:hover:text-[#7ec8d8].
  Auto-updating via tokens (verify only): page shell dark:bg-ev-navy (L1535/L1616), tab active dark:text-ev-teal-light/border (L1794/L1804), banners dark:bg-ev-navy-card (L1855).
  Faint-gray risks (lift toward #8b949e if borderline): tab inactive dark:text-gray-500 (L1795/L1805), empty-state dark:text-gray-400 (L1895/L1902).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint .dark .ev-* override blocks + header bg + header child overrides</name>
  <files>src/index.css, src/components/Layout.jsx</files>
  <read_first>
    - src/index.css (.dark .ev-* override blocks L120-348; html:not(.dark) WCAG blocks ~L353-358; @source not L8-9)
    - src/components/Layout.jsx (L53-66 — ev-ui Header isDark style prop)
    - .planning/phases/169-dark-mode-design-system-foundation/169-PATTERNS.md (§src/index.css Pattern B + C; §Layout.jsx; §Shared Patterns — ev-ui !important spine)
    - .planning/phases/169-dark-mode-design-system-foundation/169-RESEARCH.md (§ev-ui Header / Section dark theming — child class hooks; Pitfall 2, Pitfall 3)
    - .planning/phases/169-dark-mode-design-system-foundation/169-UI-SPEC.md (Color reserved-for list; PoliticianCard preserved; no drop-shadows)
  </read_first>
  <action>
    In src/index.css, repoint every in-scope `.dark .ev-*` override block to the new palette using the
    existing `.dark .ev-<component> { … !important }` pattern (Pattern B — !important is mandatory because
    ev-ui ships inline styles). PoliticianCard block (L121-137): bg #1a2235→#161b22, hover #1e2a3a→#21262d,
    h3 #f3f4f6→#e6edf3, p #d1d5db→#8b949e; KEEP the hairline border rgba(255,255,255,0.08); do NOT change
    any geometry/radius/padding (PoliticianCard 4:5 preserved, SC#4). Government-body section eyebrow
    (L338-341): `[role="button"] span` color #59b0c4→#00c8d7 and layer the D-06 eyebrow type
    (font-family var(--font-display), font-weight 600, text-transform uppercase, letter-spacing 1.2px).
    Sweep the remaining in-scope dark override literals in this file to the new palette: surface #1a2235→#161b22,
    elevated #1e2a3a→#21262d, primary text #f3f4f6→#e6edf3, muted #d1d5db→#8b949e, accent #59b0c4→#00c8d7,
    and any #9ca3af muted on in-scope (gov-body/subgroup/card/header) blocks → floor at #8b949e. Add
    `.dark .ev-header-secondary`, `.dark .ev-header-nav`, `.dark .ev-header-mobile-menu { color:#e6edf3 !important }`
    overrides (ev-ui hardcodes the OLD palette internally — Pitfall 2; <header> root has no class so its bg is
    owned by the Layout style prop). Add NO box-shadow (GitHub-dark = surface contrast + hairline only).
    In src/components/Layout.jsx (L62-65), change the Header dark `style` bg #020618→#0d1117; KEEP the
    borderBottom hairline. PRESERVE VERBATIM the `html:not(.dark)` light WCAG blocks (~L353-358; Pitfall 3)
    and the `@source not` lines (L8-9). Do NOT touch any light-scoped rule. No fenced code in this plan.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - build-command: `npm run build` exits 0.
    - source-assertion: `grep -c '#0d1117' src/components/Layout.jsx` ≥ 1 (header bg repointed); `grep -c '#020618' src/components/Layout.jsx` = 0 (old header bg gone).
    - source-assertion: in src/index.css, `grep -Ec '\.dark \.ev-politician-card' src/index.css` ≥ 1 and `grep -c '#1a2235' src/index.css` decreased vs pre-edit (in-scope override literals migrated); `grep -c 'html:not(.dark)' src/index.css` unchanged (light WCAG blocks preserved); `grep -c '@source not' src/index.css` = 2.
    - source-assertion: `grep -Ec 'ev-header-secondary|ev-header-nav|ev-header-mobile-menu' src/index.css` ≥ 1 (header child overrides added).
    - behavior: no `box-shadow` added to `.ev-politician-card` / header blocks.
  </acceptance_criteria>
  <done>ev-ui dark override blocks + header bg + header child text are on the new palette via !important; light WCAG + @source lines preserved; PoliticianCard geometry untouched; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep Results.jsx inline literals + eyebrow type, and minimal FilterBar dark re-theme</name>
  <files>src/pages/Results.jsx, src/components/FilterBar.jsx</files>
  <read_first>
    - src/pages/Results.jsx (L1420-1460 card/compass; L1480-1510 MiniCompass overlay; L1785-1830 tab strip; L1880-1949 tier eyebrows + empty state + treasury link)
    - src/components/FilterBar.jsx (L26-29 Dropdown palette; L62 option bg; L82-87 main palette; L159 checkbox accent)
    - .planning/phases/169-dark-mode-design-system-foundation/169-PATTERNS.md (§Results.jsx Patterns 1-3; §FilterBar.jsx dark-side ternary swap)
    - .planning/phases/169-dark-mode-design-system-foundation/169-RESEARCH.md (§Results.jsx theming touchpoints table; Pitfall 4 faint-gray; §FilterBar D-09)
    - .planning/phases/169-dark-mode-design-system-foundation/169-CONTEXT.md (D-06 eyebrow spec, D-09 minimal FilterBar)
    - .planning/phases/169-dark-mode-design-system-foundation/169-UI-SPEC.md (Typography eyebrow row; faint-gray floor #8b949e)
  </read_first>
  <action>
    Results.jsx — repoint dark-side inline literals to the new palette (swap ONLY the `isDark ?` branch,
    keep light branches per D-03): compassBg (L1427) #1a2235→#161b22 (KEEP candidate #fffef5/#fff);
    card style (L1438) backgroundColor #1a2235→#161b22 + borderColor #2d3f5a→#2d3748 (KEEP borderRadius:10
    control radius and candidate #fffef5/#fed12e); MiniCompass overlay bg (L1486/L1506) #1a2235→#161b22.
    Tier eyebrow labels (L1892 AND L1916, identical): dark color #59b0c4→#00c8d7 and apply D-06 type
    (font-family var(--font-display)/Manrope, weight 600, uppercase, letter-spacing 1.2px) — do NOT touch
    the `!isDark` tierStyle.bg gating at L1889/L1913 (light-only, D-03). Treasury link (L1939):
    dark:text-[#59b0c4]→dark:text-[#00c8d7], KEEP dark:hover:text-[#7ec8d8]. Faint-gray fix (Pitfall 4):
    lift tab-inactive dark:text-gray-500 (L1795/L1805) and empty-state dark:text-gray-400 (L1895/L1902)
    toward the #8b949e floor (use the muted token / a class that resolves ≥ #8b949e on #0d1117) — never
    fainter. The page-shell dark:bg-ev-navy and tab-active dark:text-ev-teal-light token classes auto-update
    from Plan 01 — verify legibility, no edit.
    FilterBar.jsx — D-09 MINIMAL re-theme: swap ONLY the dark side of each isDark ternary, keep light side
    and ALL geometry/44px/padding: Dropdown (L26-29) bg #1a2235→#161b22, active #59b0c4→#00c8d7, dark idle
    border #2d3f5a→#2d3748, dark idle text #d1d5db→#8b949e, chevron %2359b0c4→%2300c8d7; main (L82-87) inputBg
    #1a2235→#161b22, inputBorder #2d3f5a→#2d3748, inputBorderFocus #59b0c4→#00c8d7, inputText #d1d5db→#8b949e,
    iconStroke #59b0c4→#00c8d7; checkbox accentColor (L159) #59b0c4→#00c8d7; <option> bg (L62) #1a2235→#161b22.
    Do NOT restyle FilterBar layout (Phase 170 reworks it). No fenced code; follow 169-PATTERNS.md excerpts.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - build-command: `npm run build` exits 0.
    - source-assertion: `grep -c '#1a2235' src/pages/Results.jsx` = 0 and `grep -c '#1a2235' src/components/FilterBar.jsx` = 0 (old dark surface literal gone from both); `grep -c '#161b22' src/pages/Results.jsx` ≥ 1.
    - source-assertion: `grep -c '#59b0c4' src/pages/Results.jsx` = 0 and `grep -c '#59b0c4' src/components/FilterBar.jsx` = 0 (old dark accent gone); `grep -c '#00c8d7' src/pages/Results.jsx` ≥ 1.
    - source-assertion: `grep -c 'dark:text-gray-500' src/pages/Results.jsx` = 0 (faint-gray tab-inactive lifted) — or replaced by a class/token resolving ≥ #8b949e.
    - behavior: light-side ternary values (#fff, #d1d5db light branch, #00657c, #374151, #6b7280, #fffef5, #fed12e) and all 44px touch targets unchanged in FilterBar.
  </acceptance_criteria>
  <done>Results in-scope inline literals + eyebrow type and FilterBar dark-side palette are on the new tokens; light branches + 44px preserved; no faint-gray-on-dark; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual-visual verification — Figma dark match + light-mode no-regression</name>
  <read_first>
    - .planning/phases/169-dark-mode-design-system-foundation/169-VALIDATION.md (§Manual-Only Verifications — the 6 checks; §Validation Sign-Off)
    - .planning/phases/169-dark-mode-design-system-foundation/169-UI-SPEC.md (Color/Typography tables; reference screenshot path)
    - scratchpad/figma/essentials-design.png (Figma reference for node 3957:563)
  </read_first>
  <what-built>
    Plan 01 reconciled the dark tokens + fonts; this plan repointed the ev-ui overrides, header bg,
    Results inline literals + eyebrow type, and the minimal FilterBar re-theme. `npm run build` and
    `npm run test` are green. This checkpoint confirms visual fidelity + the core light-mode-no-regression
    risk that no automated test can cover.
  </what-built>
  <how-to-verify>
    1. Run `cd "C:/Transparent Motivations/essentials" && npm run build && npm run test` — both green.
    2. `npm run dev`, open the Results/Representatives page in DARK mode (toggle theme):
       a. Page reads as Figma GitHub-dark — page #0d1117, cards/tiles #161b22, teal #00c8d7 eyebrow labels,
          hairline borders, NO drop-shadows. Compare to scratchpad/figma/essentials-design.png. (SC#1)
       b. Global header chrome matches the dark page (not navy-blue, no light bleed). (SC#3, D-08)
       c. ev-ui section headers (GovernmentBodySection/SubGroupSection) + PoliticianCards render correctly
          on dark — no light-mode color bleed. (SC#3)
       d. No faint-gray-on-dark: inactive tab labels + empty-state copy are legible (≥ #8b949e). (SC#1)
       e. PoliticianCard 4:5 tile shape/size is identical to before — only colors changed. (SC#4)
    3. Toggle to LIGHT mode on Results + Landing + a politician profile — confirm IDENTICAL to pre-change;
       confirm light teal links (#00657c) and accents are unchanged (the two-#59b0c4 trap did not bleed). (SC#5)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issue (which surface, which color).</resume-signal>
  <acceptance_criteria>
    - build-command: `npm run build && npm run test` exit 0.
    - manual-visual: all 6 checks in 169-VALIDATION.md §Manual-Only pass, mapping 1:1 to the 5 ROADMAP success criteria (esp. SC#5 light no-regression).
    - behavior: human signs off that PoliticianCard geometry is unchanged and light mode is identical to pre-change.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only edits client-side CSS overrides + component inline-style literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-02-01 | (none) | CSS/JSX theming | accept | No new attack surface — client-side CSS/token/theming changes only; no untrusted input, no auth, no data paths, no crypto. The only correctness risk (light-mode regression) is mitigated by preserving the html:not(.dark) WCAG blocks + light ternary branches and verified by the Task 3 manual-visual checkpoint. |

Package install threat (T-169-SC) is handled in Plan 01; this plan installs nothing.
</threat_model>

<verification>
- `npm run build` green after each code task.
- `npm run build && npm run test` green before the checkpoint.
- Manual-visual: 6 checks in 169-VALIDATION.md, mapping 1:1 to the 5 ROADMAP success criteria.
- Source-structure: in-scope dark literals (#1a2235/#59b0c4) gone from Results.jsx + FilterBar.jsx; light branches + @source-not + html:not(.dark) preserved.
</verification>

<success_criteria>
- Results + global header render in the Figma dark treatment driven by Plan-01 tokens (SC#1, DARK-02).
- No scattered inline dark literals on the Results surface — values trace to tokens/new palette (SC#2).
- ev-ui components display correctly on dark via !important overrides, no light bleed (SC#3).
- PoliticianCard 4:5 tile unchanged in shape/size, re-colored only (SC#4).
- Light mode unchanged — no regression (SC#5).
</success_criteria>

<output>
Create `.planning/phases/169-dark-mode-design-system-foundation/169-02-SUMMARY.md` when done.
Record: which Results/FilterBar literals were migrated, the faint-gray lifts applied, the header child override selectors added, and the manual-visual sign-off result (esp. light-mode no-regression).
</output>
