---
phase: 128-carson-stances
plan: 03
type: execute
wave: 3
depends_on: [128-02]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CARSON-01]
must_haves:
  truths:
    - "Phase-wide Q1 query returns per-person stance counts for all 5 Carson target officials"
    - "Phase-wide Q2 (uncited contexts) returns 0 across all 5 Carson officials"
    - "Phase-wide Q3 (unpaired answers) returns 0 across all 5 Carson officials"
    - "Phase-wide Q4 (dead topic check, using ct.topic_key not ct.slug) returns 0 rows — no stances on retired topics"
    - "Phase-wide Q5 (excluded officials safety check) returns 0 — Bradshaw (-700305) and Cooper (-700306) have no stance rows"
    - "At least 3 of 5 officials have >= 1 stance row (CARSON-01 success criterion 1)"
    - "Compass render checkpoint passes on at least one Carson official's profile page"
    - "CARSON-01 is marked complete in REQUIREMENTS.md and ROADMAP.md"
    - "STATE.md reflects Phase 128 complete with next migration updated to 724"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CARSON-01 marked satisfied"
      contains: "CARSON-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 128 marked complete"
      contains: "Phase 128"
    - path: ".planning/STATE.md"
      provides: "Current position updated to Phase 129 ready, next migration 724"
      contains: "next migration"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "phase-wide Q2/Q3 citation checks (all 5 Carson officials)"
      pattern: "array_length\\(sources,1\\)"
---

<objective>
Run phase-wide closure verification across all 5 Carson officials (plus an excluded-officials safety check), confirm compass rendering on at least one official's profile, then update REQUIREMENTS.md, ROADMAP.md, and STATE.md to mark CARSON-01 complete.

Purpose: Confirm 100% citation rate across all 5 stance files, confirm excluded officials received zero stances, close CARSON-01, and prepare STATE.md for Phase 129 (Compton Stances).
Output: Phase 128 fully closed; planning documents updated and committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/128-carson-stances/128-02-SUMMARY.md
@.planning/phases/128-carson-stances/128-01-SUMMARY.md

Apply path: psql CLI with DATABASE_URL from C:/EV-Accounts/backend/.env. Do NOT run git commands in C:/EV-Accounts.

Carson target UUIDs (from 128-01-SUMMARY.md):
  -700300 Lula Davis-Holmes (Mayor)    → UUID in 128-01-SUMMARY.md
  -700301 Jawane Hilton (D1)           → UUID in 128-01-SUMMARY.md
  -700302 Jim Dear (D2)                → UUID in 128-01-SUMMARY.md
  -700303 Cedric L. Hicks Sr. (D3)    → UUID in 128-01-SUMMARY.md
  -700304 Arleen B. Rojas (D4)        → UUID in 128-01-SUMMARY.md
  -700305 Khaleah K. Bradshaw (EXCLUDED) → UUID in 128-01-SUMMARY.md
  -700306 Monica Cooper (EXCLUDED)     → UUID in 128-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-wide verification queries</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/128-carson-stances/128-01-SUMMARY.md (all 5 target UUIDs + exclusion UUIDs)
    - .planning/phases/128-carson-stances/128-02-SUMMARY.md (final stance counts)
    - C:/EV-Accounts/backend/.env (DATABASE_URL)
  </read_first>
  <action>
    Run all 5 phase-wide verification queries using the target UUIDs from 128-01-SUMMARY.md. Load DATABASE_URL from C:/EV-Accounts/backend/.env.

    Q1 — Per-person stance counts:
    `SELECT p.full_name, p.external_id, COUNT(pa.topic_id) AS stance_count
     FROM essentials.politicians p
     LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
     WHERE p.external_id IN (-700300, -700301, -700302, -700303, -700304)
     GROUP BY p.id, p.full_name, p.external_id
     ORDER BY p.external_id;`
    Expected: 5 rows, at least 3 with stance_count >= 1. Record all counts.

    Q2 — Uncited contexts (must return 0):
    `SELECT COUNT(*) FROM inform.politician_context pc
     WHERE pc.politician_id IN (
       SELECT id FROM essentials.politicians
       WHERE external_id IN (-700300, -700301, -700302, -700303, -700304)
     )
     AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1) = 0);`
    Must return 0. If non-zero, identify which rows and remediate before proceeding.

    Q3 — Unpaired answers (must return 0):
    `SELECT COUNT(*) FROM inform.politician_answers pa
     LEFT JOIN inform.politician_context pc
       ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
     WHERE pa.politician_id IN (
       SELECT id FROM essentials.politicians
       WHERE external_id IN (-700300, -700301, -700302, -700303, -700304)
     )
     AND pc.politician_id IS NULL;`
    Must return 0. If non-zero, identify and remediate.

    Q4 — Dead topic check (must return 0 rows, uses topic_key NOT slug):
    `SELECT pa.politician_id, ct.topic_key, pa.value
     FROM inform.politician_answers pa
     JOIN inform.compass_topics ct ON ct.id = pa.topic_id
     WHERE pa.politician_id IN (
       SELECT id FROM essentials.politicians
       WHERE external_id IN (-700300, -700301, -700302, -700303, -700304)
     )
     AND ct.is_active = false;`
    Must return 0 rows — no stances on retired/inactive topics.

    Q5 — Excluded officials safety check (must return 0):
    `SELECT COUNT(*) FROM inform.politician_answers
     WHERE politician_id IN (
       SELECT id FROM essentials.politicians
       WHERE external_id IN (-700305, -700306)
     );`
    Must return 0. Bradshaw and Cooper must have received zero stances.

    Record all results. If Q2, Q3, Q4, or Q5 return non-zero, STOP and remediate before updating planning documents.
  </action>
  <verify>
    <automated>Q2 = 0, Q3 = 0, Q4 = 0 rows, Q5 = 0. Q1 shows at least 3 of 5 officials with >= 1 stance.</automated>
  </verify>
  <acceptance_criteria>
    - Q1: per-person stance counts returned for all 5 officials; at least 3 have >= 1 stance
    - Q2: 0 uncited contexts across all 5 Carson target officials
    - Q3: 0 unpaired answers across all 5 Carson target officials
    - Q4: 0 rows on inactive/retired topics
    - Q5: 0 stances for excluded Bradshaw (-700305) and Cooper (-700306)
    - All results recorded in SUMMARY
  </acceptance_criteria>
  <done>All 5 phase-wide verification queries passed. Results recorded. Ready for compass render checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Compass render checkpoint</name>
  <files>(no files written — browser verification)</files>
  <read_first>
    - .planning/phases/128-carson-stances/128-01-SUMMARY.md (Davis-Holmes UUID for profile URL construction)
  </read_first>
  <action>
    Verify the compass renders correctly on at least one Carson official's profile page.

    Navigate to the Essentials app (essentials.empowered.vote) and load a profile page for a Carson official who received stance rows (Davis-Holmes preferred as Mayor). Check:
    1. Compass appears on the profile page
    2. At least one spoke is rendered (not all blank)
    3. No JavaScript errors in console
    4. The compass does not show default/fallback values for topics with no evidence (blank spokes should remain blank)

    If the profile URL follows the pattern `/politicians/{uuid}` or similar, use the UUID from 128-01-SUMMARY.md. If the app is not accessible from the executor environment, note this limitation and mark the checkpoint as "not verifiable in executor context" — the SUMMARY should record the expected URL for manual verification.
  </action>
  <verify>
    <automated>Compass renders with at least one spoke for a Carson official — OR — checkpoint marked as manually-verifiable with URL provided.</automated>
  </verify>
  <acceptance_criteria>
    - Compass render verified on at least one Carson official's profile, OR
    - Manual verification URL recorded in SUMMARY with note that executor context cannot reach the live app
  </acceptance_criteria>
  <done>Compass render checkpoint complete (verified or noted as manual).</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md — mark CARSON-01 complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find CARSON-01 entry)
  </read_first>
  <action>
    Update .planning/REQUIREMENTS.md to mark CARSON-01 as satisfied:
    - Change status from "Not started" or "In progress" to "Complete" / "✓" / "Satisfied" — match the existing file's convention
    - Add completion date: 2026-06-16
    - Reference migration numbers applied: 719–723
    - Note final stance counts from Q1 (total rows across all 5 officials)
  </action>
  <verify>
    <automated>REQUIREMENTS.md contains "CARSON-01" with a satisfied/complete status indicator.</automated>
  </verify>
  <acceptance_criteria>
    - CARSON-01 entry in REQUIREMENTS.md reflects completion with date and migration numbers
  </acceptance_criteria>
  <done>REQUIREMENTS.md updated: CARSON-01 marked satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md — mark Phase 128 complete</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (find Phase 128 row in milestone table)
  </read_first>
  <action>
    Update .planning/ROADMAP.md:
    - In the v15.0 milestone table, change Phase 128 status from "Not started" to "Complete (2026-06-16)"
    - Check the checkbox for Phase 128 in the roadmap checklist if one exists (change `- [ ]` to `- [x]`)
    - Do NOT modify any other phase rows
  </action>
  <verify>
    <automated>ROADMAP.md Phase 128 row shows "Complete" status.</automated>
  </verify>
  <acceptance_criteria>
    - Phase 128 row in ROADMAP.md milestone table shows complete status with date
    - No other phases modified
  </acceptance_criteria>
  <done>ROADMAP.md updated: Phase 128 marked complete.</done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md — advance to Phase 129</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (Current Position section — find Phase 128 references)
  </read_first>
  <action>
    Update .planning/STATE.md Current Position section:
    - Phase line: change from "Phase: 128 — Carson Stances (not yet started)" to "Phase: 129 — Compton Stances (not yet started)"
    - Plan line: reset to "—"
    - Status line: "Not started"
    - Last activity: "2026-06-16 — Phase 128 Carson Stances complete; CARSON-01 closed; [N] total stance rows across 5 officials; Bradshaw and Cooper confirmed excluded"
    - Next migration: update to 724 (migrations 719–723 were used in Phase 128)

    Preserve all other sections (v15.0 Roadmap Summary table, Key v15.0 Facts, Accumulated Context). In the Roadmap Summary table, update Phase 128 row to show "Complete (2026-06-16)" and Phase 129 row to show "Not started".
  </action>
  <verify>
    <automated>STATE.md Current Position shows Phase 129, next migration 724.</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md Current Position: Phase = 129, next migration = 724
    - Last activity line records Phase 128 completion with date 2026-06-16
    - Roadmap Summary table updated: Phase 128 = Complete, Phase 129 = Not started
    - No other content lost or corrupted
  </acceptance_criteria>
  <done>STATE.md updated: current position advanced to Phase 129, next migration 724.</done>
</task>

<task type="auto">
  <name>Task 6: Commit planning documents</name>
  <files>(git commit in C:/Transparent Motivations/essentials — NOT in C:/EV-Accounts)</files>
  <read_first>
    - .planning/STATE.md (verify updates from Tasks 3-5 are in place)
  </read_first>
  <action>
    Stage and commit the planning document updates in the essentials repo (C:/Transparent Motivations/essentials — the git repo). Do NOT run git commands in C:/EV-Accounts.

    Files to stage:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/128-carson-stances/128-01-SUMMARY.md
    - .planning/phases/128-carson-stances/128-02-SUMMARY.md
    - .planning/phases/128-carson-stances/128-03-SUMMARY.md (this plan's summary, created at end)

    Commit message format:
    `docs(phase-128): Carson Stances complete — CARSON-01 closed; migrations 719-723`

    Verify commit succeeds with `git log --oneline -1`.
  </action>
  <verify>
    <automated>git log shows the new commit with message referencing phase-128.</automated>
  </verify>
  <acceptance_criteria>
    - All 6 planning files staged and committed
    - Commit message includes "phase-128" and "CARSON-01"
    - git log confirms commit exists
  </acceptance_criteria>
  <done>Planning documents committed to essentials repo.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (Plan 03)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-P3-01 | Tampering | Q4 uses topic_key not slug | mitigate | Query explicitly uses ct.topic_key (Phase 126 schema confirmed) |
| T-128-P3-02 | Tampering | excluded officials check skipped | mitigate | Q5 explicitly checks Bradshaw + Cooper UUIDs before closing CARSON-01 |
| T-128-P3-03 | Tampering | git commit in wrong repo | mitigate | Commit in C:/Transparent Motivations/essentials only; explicit warning not to git in C:/EV-Accounts |
</threat_model>

<verification>
This plan IS the final verification step. All 5 queries must pass before REQUIREMENTS.md is updated. If any query fails, stop and remediate (re-examine the relevant plan's SUMMARY for the affected official, re-run the migration if needed).
</verification>

<success_criteria>
- Q1: all 5 officials have stance counts recorded; at least 3 have >= 1 row
- Q2: 0 uncited contexts across all 5 target officials
- Q3: 0 unpaired answers across all 5 target officials
- Q4: 0 stances on retired topics
- Q5: 0 stances for excluded Bradshaw and Cooper
- Compass render verified (or manual URL recorded)
- CARSON-01 marked complete in REQUIREMENTS.md
- Phase 128 marked complete in ROADMAP.md
- STATE.md advanced to Phase 129, next migration 724
- All planning documents committed to essentials repo
</success_criteria>

<output>
Create `.planning/phases/128-carson-stances/128-03-SUMMARY.md` when done. Record: all 5 query results (Q1 stance counts per official, Q2/Q3/Q4/Q5 = 0), compass render checkpoint result, REQUIREMENTS/ROADMAP/STATE update confirmation, and git commit hash.
</output>
