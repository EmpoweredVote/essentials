---
phase: 84-multnomah-smaller-cities
plan: "03"
type: execute
wave: 3
depends_on: ["84-01", "84-02"]
files_modified:
  - src/lib/groupHierarchy.js
  - C:/EV-Accounts/backend/src/lib/essentialsService.ts
autonomous: true
requirements: [CITIES-04, CITIES-05]
gap_closure: true
gaps_addressed:
  - "Wood Village LOCAL section shows Mayor (LOCAL_EXEC) after council sub-group instead of before"
  - "Maywood Park address autocorrects to Portland and returns Portland officials instead of Maywood Park officials"

must_haves:
  truths:
    - "Wood Village LOCAL section: Mayor Jairo Rios-Campos appears BEFORE 'Wood Village City Council' sub-group"
    - "Maywood Park address '10100 NE Marx St, Maywood Park, OR 97220' returns Jim Akers and 4 council members — not Portland leadership"
    - "No regression: Troutdale, Gresham, and Portland continue working correctly"
  artifacts:
    - path: "src/lib/groupHierarchy.js"
      provides: "subGroupOrderScore with district_type guard preventing LOCAL council from scoring as executive"
      contains: "district_type"
    - path: "C:/EV-Accounts/backend/src/lib/essentialsService.ts"
      provides: "Enclave-city alias map substituting Maywood Park centroid when input address contains 'Maywood Park' and geocoded city is 'Portland'"
      contains: "ENCLAVE_CITY_ALIASES"
  key_links:
    - from: "src/lib/groupHierarchy.js subGroupOrderScore"
      to: "district_type field on politician objects"
      via: "pols[0].district_type check before EXECUTIVE_KW score"
      pattern: "district_type.*LOCAL_EXEC"
    - from: "C:/EV-Accounts/backend/src/lib/essentialsService.ts getRepresentativesByAddress"
      to: "geocodeAddress result"
      via: "ENCLAVE_CITY_ALIASES lookup after geocodeAddress call"
      pattern: "ENCLAVE_CITY_ALIASES"
---

<objective>
Close two UAT gaps from Phase 84: (A) Mayor ordering bug in groupHierarchy.js where "Council President" title causes the council sub-group to score equal to the Mayor's sub-group, putting council before Mayor alphabetically; (B) Maywood Park enclave-city bug where Census TIGER returns city="PORTLAND" for all Maywood Park streets, so PostGIS resolves to Portland officials.

Purpose: CITIES-04 (Wood Village) and CITIES-05 (Maywood Park) are seeded correctly in the DB — the failures are display-layer and routing-layer bugs, not data bugs.

Output: (A) groupHierarchy.js emits LOCAL executives first for all 5 Multnomah cities regardless of whether council members hold titles containing "president". (B) essentialsService.ts substitutes the Maywood Park G4110 centroid when the address string contains "Maywood Park" and the geocoder returns city="Portland", routing the lookup to the correct G4110 boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/84-multnomah-smaller-cities/84-UAT.md
@.planning/phases/84-multnomah-smaller-cities/84-01-SUMMARY.md
@.planning/phases/84-multnomah-smaller-cities/84-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix groupHierarchy.js — guard EXECUTIVE_KW check against district_type</name>
  <files>src/lib/groupHierarchy.js</files>
  <action>
    In `subGroupOrderScore` (around line 382), the line:

      if (EXECUTIVE_KW.some(kw => lower.includes(kw) || titleLower.includes(kw))) return 10;

    fires for any sub-group whose label or first politician's office_title contains a word from EXECUTIVE_KW ('mayor', 'governor', 'president', 'trustee', 'executive'). The Wood Village and Maywood Park councils each have a "Council President" — so their council sub-group scores 10 (same as Mayor's score of 10), and the alphabetical tiebreaker ("C" < "M") puts council before Mayor.

    Fix: Add a district_type guard so that only LOCAL_EXEC sub-groups are eligible to score 10 via EXECUTIVE_KW. LOCAL sub-groups must be capped at 20 (legislative score) even when a politician's title contains "president".

    The check to add is: all politicians in the sub-group must have district_type='LOCAL_EXEC' (not 'LOCAL') before the EXECUTIVE_KW score of 10 can be returned.

    Change the existing line (line 382):

      if (EXECUTIVE_KW.some(kw => lower.includes(kw) || titleLower.includes(kw))) return 10;

    to:

      if (
        pols.length > 0 &&
        pols.every(p => p.district_type === 'LOCAL_EXEC') &&
        EXECUTIVE_KW.some(kw => lower.includes(kw) || titleLower.includes(kw))
      ) return 10;
      if (EXECUTIVE_KW.some(kw => lower.includes(kw) || titleLower.includes(kw))) return 20;

    Rationale: The first branch returns 10 only when every politician in the sub-group is LOCAL_EXEC (a genuine executive sub-group). The second branch catches the remaining EXECUTIVE_KW matches (LOCAL council members with titles like "Council President") and scores them 20 — the same as the LEGISLATIVE_KW path — so they never beat an actual mayor.

    The LEGISLATIVE_KW line that follows (return 20 for 'council', 'board', etc.) is unchanged; it still fires when no EXECUTIVE_KW word is present. The new fallthrough to 20 above handles the overlap case where EXECUTIVE_KW fires on a LOCAL sub-group.

    Do NOT change anything else in this function. Do NOT change EXECUTIVE_KW or LEGISLATIVE_KW arrays.
  </action>
  <verify>
    <automated>
      Verify the change is present:
      grep -n "district_type.*LOCAL_EXEC" src/lib/groupHierarchy.js

      Manual functional check (no automated test harness exists for this module):
      Load a Wood Village address (e.g. "2055 NE 238th Dr, Wood Village, OR 97060") in the app's Representatives tab. The LOCAL section must show Mayor Jairo Rios-Campos BEFORE the "Wood Village City Council" sub-group containing Dara Tan et al.

      Regression check: Load a Troutdale address ("219 E Columbia River Hwy, Troutdale, OR 97060"). Mayor David Ripma must still appear before council members (Troutdale has no "Council President" title, so this confirms the base case is not broken).
    </automated>
  </verify>
  <done>
    subGroupOrderScore in groupHierarchy.js contains a `pols.every(p => p.district_type === 'LOCAL_EXEC')` guard on the EXECUTIVE_KW return-10 branch. Wood Village's Mayor sub-group (LOCAL_EXEC, title "Mayor") scores 10; Wood Village's council sub-group (LOCAL, title "Council President") now scores 20 via the EXECUTIVE_KW fallthrough. Mayor appears first in the rendered LOCAL section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix essentialsService.ts — Maywood Park enclave-city centroid alias</name>
  <files>C:/EV-Accounts/backend/src/lib/essentialsService.ts</files>
  <action>
    Root cause: Census TIGER stores all Maywood Park streets with city="PORTLAND" (USPS city name for that ZIP). When a user types "10100 NE Marx St, Maywood Park, OR 97220", the Census Geocoder returns a match but with addressComponents.city="PORTLAND". The geocoded coordinates may then fall outside Maywood Park's small 0.34 sq mi G4110 polygon, causing PostGIS to return Portland officials.

    Fix: In `getRepresentativesByAddress` (around line 530 in essentialsService.ts), add an enclave-city alias map immediately after the `geocodeAddress` call. When the original address string contains "maywood park" (case-insensitive) and the geocoded city is "Portland" (or "PORTLAND"), substitute the Maywood Park G4110 centroid coordinates.

    Step 1 — Add the alias constant near the top of the file (after the imports block, before the first exported function). The constant should be a readonly object keyed by lowercase city name:

      /**
       * Enclave-city alias map.
       * Some cities are entirely enclosed within a larger USPS city boundary,
       * so Census TIGER / the geocoder returns the surrounding city name.
       * When the user's address string contains the enclave city name but the
       * geocoder returns the host city, substitute the enclave's G4110 centroid.
       *
       * Structure: { enclaveNameLower: { hostCityLower: string; lat: number; lng: number } }
       * Add new entries here as new enclave cities are onboarded.
       */
      const ENCLAVE_CITY_ALIASES: Record<string, { hostCity: string; lat: number; lng: number }> = {
        'maywood park': { hostCity: 'portland', lat: 45.5525170, lng: -122.5617782 },
      };

    Step 2 — In `getRepresentativesByAddress`, after the line:

      const { lat, lng, matchedAddress, state } = await geocodeAddress(address);

    add the alias override block. Use `let` bindings (or reassign) so the substitution only touches the coordinate variables, not `matchedAddress` or `state`:

      // Enclave-city alias override: some cities have streets stored under a
      // surrounding city's USPS name in Census TIGER. If the address string
      // names an enclave city but the geocoder returned its host city, substitute
      // the enclave's G4110 centroid so PostGIS hits the correct boundary.
      let resolvedLat = lat;
      let resolvedLng = lng;
      const addrLower = address.toLowerCase();
      for (const [enclaveName, alias] of Object.entries(ENCLAVE_CITY_ALIASES)) {
        if (addrLower.includes(enclaveName) && (matchedAddress.toLowerCase().includes(alias.hostCity) || state.toLowerCase() === 'or')) {
          resolvedLat = alias.lat;
          resolvedLng = alias.lng;
          break;
        }
      }

    Step 3 — Replace every use of `lat` and `lng` in the subsequent districtQueryText parameterization within `getRepresentativesByAddress` with `resolvedLat` and `resolvedLng`. Only the two variables passed as query parameters to ST_MakePoint need to change — do not rename them elsewhere in the function.

    The condition `addrLower.includes(enclaveName)` checks the raw user-supplied address string (before geocoding), which contains "Maywood Park". The `matchedAddress.toLowerCase().includes(alias.hostCity)` checks the geocoder's matched address (which will contain "PORTLAND" for Maywood Park streets). The `state.toLowerCase() === 'or'` acts as a safety valve so the substitution only fires in Oregon — it won't affect other states that happen to have a "Portland" (e.g. Portland, ME).

    Keep the change minimal. Do NOT refactor geocodeAddress, do NOT modify the cache logic, do NOT change the return signature of getRepresentativesByAddress.

    Note: C:/EV-Accounts/backend has its own git repo. After editing, commit in that repo:
      git -C "C:/EV-Accounts/backend" add src/lib/essentialsService.ts
      git -C "C:/EV-Accounts/backend" commit -m "fix(essentials): add Maywood Park enclave-city centroid alias"
    Do NOT run git commands in C:/EV-Accounts directly (not a git repo at that level — see memory note).
  </action>
  <verify>
    <automated>
      Verify the constant is present:
      grep -n "ENCLAVE_CITY_ALIASES" C:/EV-Accounts/backend/src/lib/essentialsService.ts

      Verify the alias coordinates are correct:
      grep -n "45.5525170\|maywood park" C:/EV-Accounts/backend/src/lib/essentialsService.ts

      Smoke test — confirm Maywood Park centroid still resolves correctly (the centroid is already the same coordinate as the alias, so the smoke test serves as an integration gate):
      cd C:/EV-Accounts/backend && npx tsx scripts/smoke-multnomah-cities.ts

      Functional verification: In the running app (or via a direct API call to the representatives endpoint with address "10100 NE Marx St, Maywood Park, OR 97220"), the LOCAL section must return Jim Akers (Mayor) and 4 council members (Kevin Bussema, Jeff Baltzell, Miriam Berman, Thomas Welander) — not Portland officials.
    </automated>
  </verify>
  <done>
    ENCLAVE_CITY_ALIASES constant exists in essentialsService.ts with Maywood Park entry (lat: 45.5525170, lng: -122.5617782, hostCity: 'portland'). The override block is present immediately after the geocodeAddress call in getRepresentativesByAddress. Searching "10100 NE Marx St, Maywood Park, OR 97220" returns Maywood Park officials (Jim Akers + 4 council members), not Portland officials. Smoke test still exits 0. Commit made in C:/EV-Accounts/backend repo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user address string → geocodeAddress | User input is the source of the enclave city name check; must not be trusted for coordinates — only for city-name matching |
| geocodeAddress result → PostGIS query | resolvedLat/resolvedLng are substituted coordinates; the override is only triggered when both the user address AND the geocoded host city match — dual-condition reduces false-positive risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-09 | Spoofing | ENCLAVE_CITY_ALIASES address string match | mitigate | Match requires `addrLower.includes(enclaveName)` AND geocoded result contains hostCity OR state='or' — an attacker cannot spoof Maywood Park coordinates for a non-Oregon address; dual-condition required. |
| T-84-10 | Tampering | groupHierarchy.js district_type guard | accept | The guard reads district_type from the politician object returned by the API; no user input path. Worst case: an incorrect district_type in DB returns wrong ordering (data bug, not security bug). |
| T-84-11 | Information disclosure | resolvedLat/resolvedLng in logs | accept | The same privacy contract from geocodeAddress applies — coordinates must not be logged. The alias substitution does not add any new log statements. |
| T-84-SC | Tampering | npm/pip/cargo installs | accept | No new packages; both files are edits to existing TypeScript/JS source. |
</threat_model>

<verification>
After both tasks complete:

1. groupHierarchy.js contains `pols.every(p => p.district_type === 'LOCAL_EXEC')` guard on line ~382.
2. Wood Village LOCAL section: Mayor Jairo Rios-Campos appears before "Wood Village City Council" in the rendered UI.
3. Maywood Park LOCAL section: Jim Akers and 4 council members appear when loading "10100 NE Marx St, Maywood Park, OR 97220".
4. Smoke test still passes: `cd C:/EV-Accounts/backend && npx tsx scripts/smoke-multnomah-cities.ts` exits 0 (SC1/SC2/SC3 all PASS).
5. No Portland regression: Loading a Portland address still returns Portland officials (alias override does NOT fire — "Portland" is not in ENCLAVE_CITY_ALIASES as an enclave name).
6. Troutdale regression check: Mayor David Ripma still appears before council members (non-"Council President" council title — confirms the base EXECUTIVE_KW path for LOCAL is not broken).
7. Commit exists in C:/EV-Accounts/backend git repo for the essentialsService.ts change.
8. Commit exists in the essentials git repo for the groupHierarchy.js change.
</verification>

<success_criteria>
- CITIES-04 gap closed: Wood Village LOCAL section displays Mayor (LOCAL_EXEC) before City Council sub-group (LOCAL) — "Council President" title no longer causes tiebreaker to put council first.
- CITIES-05 gap closed: "10100 NE Marx St, Maywood Park, OR 97220" returns Maywood Park officials — ENCLAVE_CITY_ALIASES centroid substitution routes the PostGIS lookup to the correct G4110 boundary.
- Smoke test exits 0 with all 5 cities passing SC1/SC2/SC3.
- No regression to Troutdale, Gresham, Fairview, or Portland routing.
</success_criteria>

<output>
Create `.planning/phases/84-multnomah-smaller-cities/84-03-SUMMARY.md` when done. Include: exact diff applied to groupHierarchy.js (the two changed lines), exact location of ENCLAVE_CITY_ALIASES constant in essentialsService.ts (line number), smoke test stdout, functional verification results for Wood Village and Maywood Park, and git commit SHAs for both repos.
</output>
