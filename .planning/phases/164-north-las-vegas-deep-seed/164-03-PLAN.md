---
phase: 164-north-las-vegas-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["164-01", "164-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1095_north_las_vegas_goynesbrown_stances.sql
  - C:/EV-Accounts/backend/migrations/1096_north_las_vegas_barron_stances.sql
  - C:/EV-Accounts/backend/migrations/1097_north_las_vegas_garciaanderson_stances.sql
  - C:/EV-Accounts/backend/migrations/1098_north_las_vegas_black_stances.sql
  - C:/EV-Accounts/backend/migrations/1099_north_las_vegas_cherchio_stances.sql
  - .planning/phases/164-north-las-vegas-deep-seed/164-03-SUMMARY.md
autonomous: false
requirements: [CLARK-04]
user_setup: []

must_haves:
  truths:
    - "Each council member with a public record has evidence-only compass stances across all live compass topics, researched ONE agent at a time, 100% cited, zero default values [D-05]"
    - "Topics with no city-level evidence are honest blank spokes (no row), never defaulted to Neutral/Likely [D-05]"
    - "Stances are CHAIRS (discrete positions the evidence matches, 1-5), not a directional polarity scale [D-05]"
    - "A North Las Vegas address returns the Mayor (sorted first) + the ONE correct ward council member via X0017 ward-precise routing — or, under D-01b, Mayor + all 4 ward-labeled members [CLARK-04 SC#1]"
    - "Mayor + City Council only — Municipal Court judges and non-elected offices (City Attorney/Manager) deferred to a future judicial-compass phase, so no judicial topics (council members are not judges) [D-02]; topic_id resolved live by topic_key WHERE is_live=true (no hardcoded topic UUIDs, no retired IDs)"
    - "Casing 'nv' lowercase; zero section-split; structural ledger stays at 1093 (all stance migrations audit-only, unregistered)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1095_north_las_vegas_goynesbrown_stances.sql"
      provides: "Audit-only evidence-only stance migration for Mayor Goynes-Brown (inform.politician_answers + inform.politician_context), topic_id resolved live by topic_key, NOT registered"
      contains: "inform.politician_answers"
    - path: ".planning/phases/164-north-las-vegas-deep-seed/164-03-SUMMARY.md"
      provides: "Recorded results for all 9 SQL/HTTP verification checks + the ward-routing/correct-person/stance human-verify outcome"
      contains: "CLARK-04"
  key_links:
    - from: "Mayor office (LOCAL_EXEC) + 4 ward offices (LOCAL X0017)"
      to: "districts (geo_id='3251800' + 'north-las-vegas-nv-council-ward-N', state='nv')"
      via: "address → /representatives/me ST_Covers PIP routing"
      pattern: "north-las-vegas-nv-council-ward-"
    - from: "5 council members (external_id -3207001..-3207005)"
      to: "inform.politician_answers + inform.politician_context"
      via: "topic_id resolved by topic_key WHERE is_live=true; ON CONFLICT (politician_id, topic_id)"
      pattern: "compass_topics"
---

<objective>
Research and install evidence-only compass stances for all 5 City of North Las Vegas council members — ONE research agent at a time (parallel agents burn rate-limit quota with no usable output — hard standing rule), all live compass topics, 100% cited, honest blank spokes where no public record exists, ZERO default values (chairs model, not polarity). Write one audit-only stance migration per member (1095-1099), apply each inline, then prove the full phase goal end-to-end via the 9 SQL/HTTP verification checks and a human checkpoint on ward-precise address routing + correct-person headshots + stance honesty. Mirrors Phase 163 (Henderson) Plan 03 exactly.

Purpose: Closes CLARK-04 by satisfying Success Criterion 3 (evidence-only stances, 100% cited, honest blanks, zero defaults) and confirming all 4 phase success criteria are TRUE.
Output: 5 per-member stance migrations (audit-only, unregistered) + 164-03-SUMMARY.md with recorded query results + the human-verify outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP):**
- Stance RESEARCH is run by research agents, ONE AT A TIME (memory feedback_stance_research_one_at_a_time — parallel agents burn quota with no output). The executor WRITES the 5 stance migration .sql files to disk from the researched evidence; the executor does NOT apply them or run DB SELECTs.
- Applying each stance migration (via `psql -f` using C:/EV-Accounts/backend/.env DATABASE_URL), and every verification SQL/HTTP/browse check, are INLINE ORCHESTRATOR steps performed by execute-phase.
- Stance migrations live in the SEPARATE repo C:/EV-Accounts/backend/migrations/ (commit via `git -C "C:/EV-Accounts"`). The SUMMARY.md goes in the essentials .planning phase dir.
- Each task marks executor-write / research-agent / inline-orchestrator-apply.
</execution_context>

<context>
@.planning/phases/164-north-las-vegas-deep-seed/164-RESEARCH.md
@.planning/phases/164-north-las-vegas-deep-seed/164-PATTERNS.md
@.planning/phases/164-north-las-vegas-deep-seed/164-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor + research agents need. Do NOT re-derive. -->
Government 'City of North Las Vegas, Nevada, US' geo_id='3251800'; chamber 'North Las Vegas City Council' (official_count=5)
Mayor district: geo_id='3251800', district_type='LOCAL_EXEC', state='nv' lowercase (Goynes-Brown sorts first via groupHierarchy.js LOCAL_EXEC-before-LOCAL ordering)
Ward districts: geo_id='north-las-vegas-nv-council-ward-N' (N=1..4), district_type='LOCAL', mtfcc='X0017', state='nv' lowercase (or, under D-01b, all members on the G4110 district)
5 members external_id -3207001 (Goynes-Brown/Mayor) .. -3207005 (Cherchio/Ward 4); the external_id→UUID map is in 164-01-SUMMARY.md
Stance tables (inform schema): inform.politician_answers (politician_id, topic_id, value); inform.politician_context (politician_id, topic_id, reasoning, sources); inform.compass_topics (topic_key, is_live)
topic_id MUST be resolved LIVE via JOIN inform.compass_topics ct ON ct.topic_key=s.topic_key AND ct.is_live=true — NEVER hardcode topic UUIDs; live/retired topic IDs in memory project_compass_live_topic_ids (retired UUIDs a9f53bc4, 45ca4740, f2a62698, 83eeb217, be60844f, c6957429 — do NOT use)
ON CONFLICT (politician_id, topic_id) DO UPDATE for both answers and context (idempotent)
Stances are CHAIRS (value = the discrete position the evidence matches, 1-5), NOT a directional polarity scale (memory feedback_compass_chairs_not_polarity)
No value for a topic = honest blank (no row); NEVER default to Neutral/Likely (memory feedback_stance_no_default_value)
No judicial topics (council members are not judges)
City-scope topics likely to have NLV evidence: homelessness, homelessness-response (NLV camping/shelter policy), housing (affordable housing, residential growth), public-safety-approach (NLVPD oversight/staffing), transportation-priorities (I-15 corridor, transit), economic-development (APEX Industrial Park / Faraday / manufacturing corridor — a DEFINING NLV issue), local-environment (water, desert climate, APEX air quality), growth-and-development (NLV historically fast-growing), taxes
Stance source tiers: cityofnorthlasvegas.com council agendas+minutes (WAF on the public site, but agenda/minutes PDFs may be reachable) → thenevadaindependent.com / Nevada Current → Las Vegas Review-Journal / KLAS 8NewsNow / Fox5Vegas / LV Sun → campaign sites/first-party statements
Realistic depth: Barrón (Ward 1, since 2013 — longest-serving, likely richest) + Cherchio (Ward 4, since 2015) + Black (Ward 3, since 2017) likely strong; Goynes-Brown (Mayor since 2022, prior Ward 2 councilwoman — high profile); Garcia-Anderson (Ward 2, appointed Dec 2022 / elected Nov 2024 — moderate record). Aim for realistic depth where documented; honest blanks for thin records.
Migration mapping (all AUDIT-ONLY, NOT registered): 1095 Goynes-Brown / 1096 Barrón / 1097 Garcia-Anderson / 1098 Black / 1099 Cherchio. Structural ledger stays at 1093. (If Wave-0 renumbered the structural migration, shift these by the same offset.)
Ward-routing address spot-check: pick a residential address in a known NLV ward → returns Mayor + that one ward member, NOT all 4 (under D-01b: Mayor + all 4 ward-labeled members is the documented expected result).
Browse link: essentials.empowered.vote/results?browse_geo_id=3251800&browse_mtfcc=G4110
Grep-gate forbidden tokens in .sql comments: paraphrase the auto-generated path column / the removed image-origin column; the ledger table token must NOT appear in comments (and no ledger-registration INSERT — these are audit-only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research stances (one agent at a time) + write 5 audit-only stance migrations 1095-1099</name>
  <files>C:/EV-Accounts/backend/migrations/1095_north_las_vegas_goynesbrown_stances.sql, C:/EV-Accounts/backend/migrations/1096_north_las_vegas_barron_stances.sql, C:/EV-Accounts/backend/migrations/1097_north_las_vegas_garciaanderson_stances.sql, C:/EV-Accounts/backend/migrations/1098_north_las_vegas_black_stances.sql, C:/EV-Accounts/backend/migrations/1099_north_las_vegas_cherchio_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1086_henderson_romero_stances.sql (EXACT ANALOG — the CTE shape: VALUES(topic_key,val,reasoning,sources) → JOIN compass_topics is_live → INSERT politician_answers ON CONFLICT → INSERT politician_context ON CONFLICT; audit-only, unregistered)
    - .planning/phases/164-north-las-vegas-deep-seed/164-PATTERNS.md (section "1095..1099_north_las_vegas_*_stances.sql" — full CTE pattern, per-file assignments, topic keys likely to have NLV evidence)
    - .planning/phases/164-north-las-vegas-deep-seed/164-RESEARCH.md (Pattern 4 stance CTE; source tiers; city-scope topics incl. APEX economic-development; realistic depth)
    - .planning/phases/164-north-las-vegas-deep-seed/164-01-SUMMARY.md (the 5 external_id→UUID map minted by mig 1093; confirmed ledger numbers)
  </read_first>
  <action>
RESEARCH (one agent at a time — HARD RULE, never parallel): for each member in turn (Goynes-Brown → Barrón → Garcia-Anderson → Black → Cherchio), dispatch a single stance-research agent that researches ALL live compass topics from public evidence (cityofnorthlasvegas.com council agendas/minutes → Nevada Independent / Nevada Current → LV Review-Journal/KLAS/Fox5/LV Sun → first-party/campaign statements). The agent returns, per topic with evidence: topic_key, the chair value (1-5 — the discrete position the evidence matches, NOT a polarity), an evidence-based reasoning string, and the source URL(s). Topics with no city-level evidence get NO entry (honest blank). ZERO defaults — never Neutral/Likely as a placeholder. No judicial topics. Expect strong APEX Industrial Park / Faraday economic-development + growth-and-development evidence (the defining NLV development story) and homelessness/housing/public-safety records; require a cited NLV-council statement/vote for each placed stance, not inference. For Garcia-Anderson (record begins Dec 2022 / elected Nov 2024), expect a thinner record and more honest blanks. Wait for each agent to finish and record its output before dispatching the next (avoids the rate-limit burn documented in memory). Honest blanks expected throughout.

EXECUTOR STEP (per member): write the stance migration modeled verbatim on 1086_henderson_romero_stances.sql. Header comment: file name, Phase 164 (CLARK-04), the member's name + external_id, AUDIT-ONLY (NOT registered, structural ledger stays 1093), idempotent, CHAIRS model, 100% citation, no defaults, honest blanks, topic_id resolved live by topic_key, stance sources. BEGIN; then a CTE: `WITH s(topic_key, val, reasoning, sources) AS (VALUES (<topic_key>, <1-5>, '<reasoning citing the specific vote/statement>', ARRAY['<url>',...]), ...)` covering ONLY the researched topics-with-evidence; `t AS (SELECT s.*, ct.id AS topic_id FROM s JOIN inform.compass_topics ct ON ct.topic_key=s.topic_key AND ct.is_live=true)`; `ins_ans AS (INSERT INTO inform.politician_answers (politician_id, topic_id, value) SELECT '<uuid>', topic_id, val FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value RETURNING 1)`; then `INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) SELECT '<uuid>', topic_id, reasoning, sources FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning=EXCLUDED.reasoning, sources=EXCLUDED.sources;` COMMIT;. Use the real per-member UUID from 164-01-SUMMARY.md. NEVER hardcode topic UUIDs (always JOIN on topic_key + is_live; none of the 6 retired UUIDs). Do NOT register any of these in the ledger. If a member genuinely has no sourceable city-level stance, write a migration with zero VALUES rows (or document the full blank) — no fabrication. GREP-GATE HYGIENE: keep the ledger table token out of comments.

Do NOT apply these migrations — that is the inline orchestrator step (Task 2).
  </action>
  <verify>
    <automated>P="C:/EV-Accounts/backend/migrations"; ok=1; for f in 1095_north_las_vegas_goynesbrown_stances 1096_north_las_vegas_barron_stances 1097_north_las_vegas_garciaanderson_stances 1098_north_las_vegas_black_stances 1099_north_las_vegas_cherchio_stances; do test -f "$P/$f.sql" || ok=0; done; F="$P/1095_north_las_vegas_goynesbrown_stances.sql"; grep -q "inform.politician_answers" "$F" &amp;&amp; grep -q "inform.politician_context" "$F" &amp;&amp; grep -q "compass_topics" "$F" &amp;&amp; grep -q "is_live = true" "$F" &amp;&amp; grep -q "ON CONFLICT (politician_id, topic_id)" "$F" &amp;&amp; [ "$ok" -eq 1 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 5 stance migrations (1095-1099) exist on disk, one per member in the correct name→file mapping.
    - Stances researched ONE agent at a time (no parallel dispatch); each agent covered all live compass topics.
    - Each migration uses the 1086 CTE shape: VALUES(topic_key, val, reasoning, sources) → JOIN inform.compass_topics ON topic_key AND is_live=true → INSERT politician_answers ON CONFLICT (politician_id, topic_id) DO UPDATE → INSERT politician_context ON CONFLICT (politician_id, topic_id) DO UPDATE.
    - topic_id resolved LIVE by topic_key + is_live=true — no hardcoded topic UUIDs; none of the 6 retired UUIDs appear.
    - Every placed stance has a non-empty reasoning string and ≥1 source URL (100% cited); values are discrete chairs (1-5), not a polarity scale.
    - No defaulted values (no Neutral/Likely placeholder); topics without evidence are simply absent (honest blank).
    - No judicial topics; each placed stance cites an NLV-council statement/vote (APEX/economic-development evidence expected but still must be cited, not inferred).
    - None of 1095-1099 contain a ledger-registration line (all audit-only).
    - Real per-member UUIDs (from 164-01-SUMMARY.md) used as politician_id.
  </acceptance_criteria>
  <done>5 evidence-only stance migrations written to disk, researched one agent at a time, chairs model, 100% cited, honest blanks, zero defaults, topic_id resolved live, all audit-only. Not yet applied.</done>
</task>

<task type="auto">
  <name>Task 2: Apply stance migrations inline + run the 9 SQL/HTTP verification checks</name>
  <files>.planning/phases/164-north-las-vegas-deep-seed/164-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/164-north-las-vegas-deep-seed/164-VALIDATION.md (Per-Task Verification Map — all checks + section-split SQL + ward-routing spot check)
    - .planning/phases/164-north-las-vegas-deep-seed/164-RESEARCH.md (Validation Architecture → Test Map; the exact SQL per check; Post-Verify DO block lines 678-715)
    - .planning/phases/164-north-las-vegas-deep-seed/164-02-SUMMARY.md (documented headshot gaps, if any, to subtract from the 5 count)
    - .planning/phases/164-north-las-vegas-deep-seed/164-01-SUMMARY.md (which ward path was taken — X0017 vs D-01b — and the structural migration number)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): apply each stance migration 1095-1099 via `psql -f`. After each apply, spot-count the answers for that member. Then run the full 9-check verification suite and record every result in 164-03-SUMMARY.md:
(1) Council office count — offices in chamber 'North Las Vegas City Council' under government 'City of North Las Vegas, Nevada, US' → expect 5 (1 Mayor + 4 wards). Record.
(2) District-type split — exactly 1 LOCAL_EXEC office (Mayor, geo_id='3251800') + exactly 4 LOCAL ward offices (mtfcc='X0017'), all state='nv'. (Under D-01b: all 5 offices resolve to the G4110 district — record that variant.) Record.
(3) Headshots present — politician_images (type='default') for external_id BETWEEN -3207005 AND -3207001 → expect 5 minus documented gaps (from 164-02-SUMMARY.md). Record + list gaps.
(4) Headshots serve — curl -sI each CDN url (the sourced members) → 200. Record.
(5) Evidence-only stances — `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -3207005 AND -3207001)` → expect ≥1 per sourced member; verify 100% have a paired politician_context row with non-null reasoning + sources (zero uncited), and zero defaulted/placeholder values. Record per-member counts + honest blanks.
(6) Section-split — run the LOCAL-tier section-split SQL (0 orphan X0017 geofence rows lacking a matching LOCAL district) → expect 0 rows. Record.
(7) Casing — DISTINCT state on districts linked to the North Las Vegas City Council chamber → expect only 'nv' lowercase. Record.
(8) Ledger — `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('1093','1094','1095','1096','1097','1098','1099')` → expect only 1093 registered (1094-1099 audit-only). Record.
(9) Ward-precise routing — pick a residential interior point inside a known NLV ward (per ward if possible) and run a /representatives/me-style probe or extend smoke-nv-geofences.ts → returns the Mayor + that ONE ward member, NOT all 4, and NOT a Strip/county-only result. (Under D-01b: returns Mayor + all 4 ward-labeled members — record that as the expected fallback result.) Record the ward → member resolution.
Plus the coverage.js check: confirm src/lib/coverage.js COVERAGE_STATES Nevada block contains the North Las Vegas entry (browseGovernmentList ['3251800'], hasContext:true) alongside Las Vegas + Henderson. Record.
Flag ANY deviation (office count off, a non-'nv' state, any uncited/defaulted stance, any extra ledger entry, a ward resolving to the wrong member or all 4) as a gap for a follow-up gap-closure plan.
  </action>
  <verify>
    <automated>F=".planning/phases/164-north-las-vegas-deep-seed/164-03-SUMMARY.md"; test -f "$F" &amp;&amp; grep -qi "office" "$F" &amp;&amp; grep -qi "headshot" "$F" &amp;&amp; grep -qi "stance" "$F" &amp;&amp; grep -qi "section-split" "$F" &amp;&amp; grep -qi "ward" "$F" &amp;&amp; grep -qi "ledger" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: all 5 stance migrations applied; per-member answer counts recorded.
    - Check (1) recorded: council office count = exactly 5 (1 Mayor + 4 wards).
    - Check (2) recorded: exactly 1 LOCAL_EXEC + exactly 4 LOCAL ward offices, all at state='nv' (or D-01b variant noted).
    - Check (3) recorded: politician_images count = 5 minus documented gaps; gaps listed.
    - Check (4) recorded: CDN headshots return HTTP 200 (sourced members spot-checked).
    - Check (5) recorded: ≥1 stance per sourced member; 100% cited (paired context with reasoning + sources); zero defaulted values; honest blanks noted.
    - Check (6) recorded: section-split query returns 0 rows.
    - Check (7) recorded: linked districts show only state='nv' lowercase.
    - Check (8) recorded: only ('1093') registered; 1094-1099 audit-only, unregistered.
    - Check (9) recorded: a known-ward address resolves to Mayor + that one ward member (ward-precise), not all 4 (or D-01b variant noted).
    - coverage.js Nevada block North Las Vegas entry confirmed (hasContext:true, ['3251800']) alongside Las Vegas + Henderson.
    - Any deviation flagged for gap closure.
  </acceptance_criteria>
  <done>All 5 stance migrations applied; all 9 checks + coverage check recorded in 164-03-SUMMARY.md with expected results; any deviation flagged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify ward-precise routing + correct-person headshots + stance honesty</name>
  <action>Pause for human verification. Present the what-built summary and the how-to-verify steps below; wait for the resume signal before marking the phase complete. No code or DB changes in this task.</action>
  <what-built>Seeded the City of North Las Vegas (Mayor + 4 ward council members) under a standalone city government, with custom non-TIGER ward geofences (4 X0017 ward polygons from the Clark County GISMO PLACE=80 layer) for ward-precise routing — or, under the D-01b fallback, the single-city-district model — the directly-elected at-large Mayor (Pamela Goynes-Brown) on a city-wide LOCAL_EXEC district (sorted first), 600x750 headshots (Goynes-Brown via Wikimedia public-domain; minus any documented genuine gap), and evidence-only compass stances (chairs model, 100% cited, honest blanks, zero defaults). North Las Vegas surfaces with the purple hasContext chip in coverage.js COVERAGE_STATES alongside Las Vegas + Henderson. All 9 SQL/HTTP audits passed inline (5 offices = 1 LOCAL_EXEC + 4 LOCAL, ward routing, headshots CDN-200 minus gaps, stances cited, casing 'nv', 0 section-split, structural ledger 1093).</what-built>
  <how-to-verify>
1. Ward-precise routing — open the app and enter a residential address in a KNOWN City of North Las Vegas ward (try one per ward if possible). Confirm the LOCAL section returns the Mayor + the ONE correct ward council member (not all 4), with NO empty LOCAL section and NO section-split. (Under D-01b the documented expectation is Mayor + all 4 ward-labeled members.) Use essentials.empowered.vote/results?browse_geo_id=3251800&browse_mtfcc=G4110 to see the full roster.
2. Mayor-first ordering + directly-elected model — confirm Mayor Pamela Goynes-Brown sorts FIRST (above the ward members), labeled 'Mayor', and that NO ward member is shown as the city's mayor (Goynes-Brown is the directly-elected at-large Mayor, not rotational; she is term-limited but seated through Nov 30 2026). If she does not sort first, check groupHierarchy.js covers LOCAL_EXEC-before-LOCAL ordering for the city tier.
3. Correct-person headshots — spot-check each profile. Confirm each photo is the right person, 4:5, no text/graphic overlay over the face (no "Re-Elect" banner etc.), not a placeholder. Confirm any documented gap renders cleanly without a photo. Confirm Scott Black (Ward 3) shows as a SEATED member (he advanced to the Nov 2026 mayoral runoff but serves until the outcome). Confirm the accent renders on "Barrón" (Ward 1).
4. Stance honesty — open 2-3 profiles (include the Mayor + the longest-serving Barrón). Confirm stances render with citations, blank spokes are genuinely blank (no defaulted Neutral), values look like documented positions (chairs, not a forced polarity), and the APEX/economic-development + growth-and-development stances reflect cited NLV-council positions. Party must NOT display anywhere (antipartisan — council races are nonpartisan).
5. Browse review — optionally review essentials.empowered.vote/results?browse_geo_id=3251800&browse_mtfcc=G4110.
  </how-to-verify>
  <verify><human-check>Operator confirms North Las Vegas ward addresses route to the Mayor + the one correct ward member (no all-4, no empty LOCAL, no section-split — or the documented D-01b variant), Goynes-Brown sorts first as the directly-elected Mayor, correct-person headshots (no overlay/placeholder, Black seated, Barrón accent), and evidence-only stances with honest blanks and no party display.</human-check></verify>
  <done>Human approves; or issues are logged and routed to a gap-closure plan.</done>
  <resume-signal>Type "approved" or describe any wrong-person photo, mis-routed ward (wrong member or all-4 shown), Mayor-ordering/model issue, Black-seating issue, Barrón-accent issue, defaulted stance, or party-display leak.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| research evidence → stance migration | researched chair values/citations cross into seed SQL; only evidence-backed values are written |
| migration file → live DB | service-role apply of seed SQL; only the inline orchestrator applies |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-10 | Tampering | fabricated/defaulted stance value (no evidence) | mitigate | Evidence-only chairs model; every stance has paired context with reasoning + source(s); check (5) asserts 0 uncited + 0 defaults; honest blanks where no record |
| T-164-11 | Tampering | APEX/development position assumed as a stance without a cited council vote | mitigate | Each placed stance requires a cited NLV-council statement/vote; human checkpoint step 4 confirms |
| T-164-12 | Spoofing | wrong topic via hardcoded topic UUID | mitigate | topic_id resolved live by topic_key + is_live=true (never hardcoded); 6 retired topic UUIDs excluded |
| T-164-13 | Repudiation | unverified claim of completion | mitigate | All 9 checks recorded with actual query results in SUMMARY; human checkpoint confirms ward routing + correct-person photos + stance honesty |
| T-164-14 | Tampering | data-integrity drift (section-split / casing / wrong-ward routing) | mitigate | Section-split SQL gate (0 rows) + casing check ('nv') + ward-precise routing probe (check 9) run inline; structural ledger asserted at 1093 |
| T-164-SC | Tampering | npm/pip/cargo installs | accept | No installs (stance SQL + verification only); RESEARCH Package Legitimacy Audit = N/A. No high-severity threats; data-seed phase. |
</threat_model>

<verification>
All four phase success criteria proven:
1. A City of North Las Vegas address returns the Mayor + the one correct ward council member via X0017 routing (or Mayor + all 4 under D-01b), no empty LOCAL section (checks 1+2+9 + browse step 1).
2. All 5 members render with 600x750 headshots, gaps documented (checks 3-4 + browse step 3).
3. Evidence-only stances render, 100% cited, honest blanks, zero defaults (check 5 — SC#3 + browse step 4).
4. North Las Vegas surfaces with the purple hasContext chip alongside Las Vegas + Henderson (coverage.js check + browse step 5).
Cross-cutting: casing 'nv' (check 7), 0 section-split (check 6), structural ledger 1093 (check 8), Mayor sorts first as directly-elected (browse step 2).
</verification>

<success_criteria>
- 5 per-member stance migrations (1095-1099) written, researched one agent at a time, applied; evidence-only, 100% cited, honest blanks, zero defaults, chairs model; each placed stance cites an NLV-council position.
- 164-03-SUMMARY.md records all 9 SQL/HTTP checks + the coverage check with expected results.
- Human checkpoint approves ward-precise routing, Mayor-first ordering (directly elected), correct-person headshots (no overlay, Black seated, Barrón accent), and stance honesty (no party display).
- All stance migrations audit-only (structural ledger stays 1093); any deviation routed to a gap-closure plan.
</success_criteria>

<output>
Create `.planning/phases/164-north-las-vegas-deep-seed/164-03-SUMMARY.md` when done (record per-member stance counts + honest blanks, the 9 verification results incl. ward-precise routing, and the human-verify outcome).
</output>
