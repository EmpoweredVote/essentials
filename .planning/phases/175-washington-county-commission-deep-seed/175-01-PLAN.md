---
phase: 175-washington-county-commission-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-washco-commissioner-boundaries.ts
  - C:/EV-Accounts/backend/migrations/1120_washco_commission.sql
autonomous: true
requirements: [WASH-01]

must_haves:
  truths:
    - "An unincorporated Washington County address returns the County Chair + exactly 1 district commissioner (no empty LOCAL section)"
    - "The standalone county government 'Washington County, Oregon, US' exists (geo_id 41067, not nested under State of Oregon)"
    - "The full Board roster (Chair + 4 district commissioners = 5 offices) is seeded with correct office structure"
    - "The 4 per-district geofences are loaded from the official Washington County GIS FeatureServer (gispub.co.washington.or.us), custom mtfcc X0018 — no Census/TIGER fallback (D-05)"
    - "The live roster (Chair + Districts 1-4) and exact body name 'Board of County Commissioners' are ground-truthed against washingtoncountyor.gov at execution, not hardcoded from memory (D-06)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-washco-commissioner-boundaries.ts"
      provides: "Loader that fetches 4 commissioner-district polygons from the WashCo FeatureServer (f=geojson, outSR=4326) into essentials.geofence_boundaries as X0018 / state='or'"
      contains: "gispub.co.washington.or.us"
    - path: "C:/EV-Accounts/backend/migrations/1120_washco_commission.sql"
      provides: "Structural migration: government + chamber + COUNTY district + 4 LOCAL X0018 districts + 5 offices (Chair on COUNTY, D1-D4 on LOCAL)"
      contains: "Washington County, Oregon, US"
  key_links:
    - from: "1120_washco_commission.sql Chair office"
      to: "essentials.districts COUNTY row geo_id='41067' state='or'"
      via: "office WHERE d.geo_id='41067' AND d.district_type='COUNTY' AND d.state='or'"
      pattern: "geo_id = '41067'"
    - from: "1120_washco_commission.sql commissioner offices D1-D4"
      to: "essentials.districts LOCAL rows geo_id='washco-or-commissioner-district-N'"
      via: "office WHERE d.geo_id='washco-or-commissioner-district-N' AND d.district_type='LOCAL' AND d.state='or'"
      pattern: "washco-or-commissioner-district-"
    - from: "1120 LOCAL district rows"
      to: "essentials.geofence_boundaries X0018 polygons loaded by the loader"
      via: "pre-flight assertion COUNT(*) X0018 >= 4"
      pattern: "mtfcc = 'X0018'"
---

<objective>
Build the Washington County, Oregon standalone county government, its Board of County Commissioners chamber, and the full 5-seat roster (directly-elected County Chair + 4 district commissioners), with per-district routing: the Chair routes county-wide via the existing COUNTY geofence (geo_id 41067) and each of the 4 commissioners routes via a custom X0018 per-district geofence loaded from the official Washington County GIS FeatureServer.

This is the structural foundation for WASH-01. Headshots (Plan 02) and stances + surfacing (Plan 03) build on the politician rows minted here.

Purpose: A WashCo resident gets the County Chair + their one specific district commissioner — no empty LOCAL section, no section-split (D-01, D-02, D-03, D-04).
Output: One loader script (4 polygons → geofence_boundaries) and one structural migration file (1120). The executor AUTHORS both files. The INLINE ORCHESTRATOR runs Wave-0 DB probes, runs the loader, then applies the migration via psql (executor has NO DB access).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Executor / Orchestrator split (read carefully — this phase has NO executor DB access)

- The **gsd-executor** writes the `.ts` loader and the `.sql` migration files ONLY. It cannot reach the database and must not attempt to.
- The **inline orchestrator** performs all DB-touching steps as numbered actions AFTER the executor returns:
  - **Wave-0 probes** (run BEFORE the executor authors the migration; results feed the file): see `<wave_0_probes>`.
  - Run the loader: `npx tsx C:/EV-Accounts/backend/scripts/load-washco-commissioner-boundaries.ts --dry-run` then live.
  - Apply the migration: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1120_washco_commission.sql` using DATABASE_URL from `C:/EV-Accounts/backend/.env`.
  - Run the DB-verification gates in `<verification>`.
- Commit migration + loader in the EV-Accounts repo via `git -C "C:/EV-Accounts"` (memory: feedback_no_git_in_ev_accounts).
</execution_context>

<context>
@.planning/phases/175-washington-county-commission-deep-seed/175-CONTEXT.md
@.planning/phases/175-washington-county-commission-deep-seed/175-RESEARCH.md
@.planning/phases/175-washington-county-commission-deep-seed/175-PATTERNS.md

<critical_facts>
- **Next migration number = 1120.** The CONTEXT/RESEARCH estimate of 1118 is STALE — migrations 1115-1119 (FL/PA/IL 2026 house candidates) landed after research. Highest on disk = 1119. Wave-0 confirms the DB ledger MAX; the structural file is `1120_washco_commission.sql`. Downstream files shift: headshots 1121, stances 1122-1126.
- **OR casing invariant:** `districts.state='or'` LOWERCASE (routing join key — uppercase silently zero-matches). `governments.state='OR'` and `offices.representing_state='OR'` UPPERCASE (display free-text).
- `essentials.chambers.slug` is GENERATED ALWAYS — NEVER include slug in the INSERT column list.
- `essentials.governments` has NO unique constraint on geo_id → WHERE NOT EXISTS guard on name is required.
- Office NOT EXISTS guard key = `(district_id, politician_id)`.
- **Verify-gate grep hygiene:** keep the literal tokens `slug`, `schema_migrations`, and `photo_origin_url` OUT of all comment text in the .sql/.ts files (grep gates scan whole files).
- Body name is EXACTLY `'Board of County Commissioners'` (verified washingtoncountyor.gov/bcc) — NOT Multnomah's "Board of Commissioners".
- External_id block: Chair `-410100`, D1 `-410110`, D2 `-410111`, D3 `-410112`, D4 `-410113` (Wave-0 confirms unused).
- mtfcc `X0018` for the 4 commissioner-district geofences (Wave-0 confirms unclaimed; X0017 is highest used).
- Seed CURRENTLY-SEATED incumbents: Chair Kathryn Harrington, D1 Nafisa Fai, D2 Pam Treece, D3 Jason Snider, D4 Jerry Willey. November-2026 runoff winners take office Jan 2027 (Phase 185's job, NOT this phase). Re-confirm roster against washingtoncountyor.gov at execution.
</critical_facts>

<wave_0_probes>
Inline orchestrator runs these BEFORE the executor authors 1120 (results parameterize the file). All against `C:/EV-Accounts/backend/.env` DATABASE_URL via psql:
1. Ledger MAX: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations;` — confirm next structural file number (expect 1120 region; if higher, bump all file numbers in this phase).
2. COUNTY district: `SELECT id, district_type, state FROM essentials.districts WHERE geo_id='41067';` — if a row exists with state='or' COUNTY, the WHERE NOT EXISTS guard is a no-op; if state='OR' (wrong casing), FLAG (must be 'or' to route).
3. External_id block free: `SELECT external_id FROM essentials.politicians WHERE external_id BETWEEN -410113 AND -410100 ORDER BY external_id;` — MUST return 0 rows.
4. X0018 unclaimed: `grep -r "X0018" C:/EV-Accounts/backend/migrations/ C:/EV-Accounts/backend/scripts/` — MUST return 0 lines.
5. FeatureServer live: fetch `https://gispub.co.washington.or.us/server/rest/services/BOC_CAO/CoCommissioners/FeatureServer/0/query?where=1%3D1&outFields=COMMDIST&returnGeometry=false&f=json` — MUST list 4 features with COMMDIST 1-4.
</wave_0_probes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the WashCo commissioner-district geofence loader</name>
  <files>C:/EV-Accounts/backend/scripts/load-washco-commissioner-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-henderson-ward-boundaries.ts (PRIMARY ANALOG — clone its DB pool setup, fetchJson helper, ST_MakeValid fallback, dry-run gate, and summary; lines cited in 175-PATTERNS.md "load-washco-commissioner-boundaries.ts" section)
    - .planning/phases/175-washington-county-commission-deep-seed/175-PATTERNS.md (the loader section — config block, feature-parsing divergence, INSERT shape)
    - .planning/phases/175-washington-county-commission-deep-seed/175-RESEARCH.md (SECTION C — FeatureServer URL, C2 loader pattern, C3 mtfcc, Pitfall 8 outSR)
  </read_first>
  <action>
    Clone load-henderson-ward-boundaries.ts. Swap the config constants: FeatureServer URL = `https://gispub.co.washington.or.us/server/rest/services/BOC_CAO/CoCommissioners/FeatureServer/0/query?where=1%3D1&outFields=COMMDIST,NAME,Lastname,Firstname&returnGeometry=true&f=geojson&outSR=4326&resultRecordCount=100`; mtfcc=`X0018`; state code=`or` (lowercase); source=`washingtoncountyor.gov-gis-commissioner-districts-2026`; geo_id prefix=`washco-or-commissioner-district-`; expected count=4. REMOVE the arcgisRingsToGeoJson helper — WashCo FeatureServer returns native GeoJSON via f=geojson, so feature.geometry is already a GeoJSON object; use JSON.stringify(feature.geometry) directly. Key district by the integer COMMDIST field; validate COMMDIST parses to integer 1-4 and SKIP/warn out-of-range (input-validation mitigation for threat T-175-01). Insert into essentials.geofence_boundaries (id, geo_id, mtfcc, state, name, geometry, source) wrapping geometry in public.ST_Multi(public.ST_SetSRID(public.ST_GeomFromGeoJSON($3), 4326)), ON CONFLICT (geo_id, mtfcc) DO NOTHING, RETURNING ST_GeometryType + ST_IsValid; copy the ST_MakeValid fallback (UPDATE + recheck) exactly from the Henderson analog. Keep the --dry-run gate and end-of-run summary. Do NOT put the tokens slug / photo_origin_url anywhere in the file.
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsx scripts/load-washco-commissioner-boundaries.ts --dry-run 2>&1 | grep -E "COMMDIST|district-[1-4]|4 (feature|district)" </automated>
  </verify>
  <acceptance_criteria>
    - File exists and contains the literal string `gispub.co.washington.or.us`.
    - File contains `X0018` and `washco-or-commissioner-district-`.
    - File contains `f=geojson` and `outSR=4326`.
    - File does NOT contain `arcgisRingsToGeoJson` (helper removed for native GeoJSON).
    - `--dry-run` reports exactly 4 districts (COMMDIST 1-4) and inserts 0 rows.
    - File does NOT contain the tokens `slug` or `photo_origin_url`.
  </acceptance_criteria>
  <done>Loader authored; --dry-run lists 4 commissioner-district polygons keyed COMMDIST 1-4 in WGS84, no rows written.</done>
</task>

<task type="auto">
  <name>Task 2: Author the structural migration 1120_washco_commission.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1120_washco_commission.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql (PRIMARY ANALOG — same-state OR county template: government row, chamber, COUNTY district, Chair-as-"County Chair" office with role_canonical NULL, office_id back-fill, post-verification DO block)
    - C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql (guard-key patterns: pre-flight geofence assertion, office NOT EXISTS on (district_id, politician_id), 2-column ledger INSERT form)
    - .planning/phases/175-washington-county-commission-deep-seed/175-PATTERNS.md (the 1118/1120 migration section — step-by-step with line citations into 244)
    - .planning/phases/175-washington-county-commission-deep-seed/175-RESEARCH.md (SECTION A — full guard SQL; Pitfalls 1, 2, 5)
  </read_first>
  <action>
    Author the structural migration as file `1120_washco_commission.sql` (NOT 1118 — that number is taken; Wave-0 confirms the exact next number). Wrap steps in BEGIN/COMMIT. Step 1: INSERT government `'Washington County, Oregon, US'` (type='County', state='OR' uppercase, city=NULL, geo_id='41067') WHERE NOT EXISTS on name. Step 2: INSERT chamber name=`'Board of County Commissioners'`, name_formal=`'Washington County Board of County Commissioners'`, government_id subselect, official_count=5 — NEVER list the generated column in the INSERT. Step 3a: INSERT COUNTY district (district_type='COUNTY', state='or' LOWERCASE, geo_id='41067', label='Washington County', mtfcc='G4020') WHERE NOT EXISTS. Step 3b: pre-flight DO block RAISE EXCEPTION if COUNT(*) of X0018 geofences (state='or') < 4 (proves the loader ran first), then INSERT 4 LOCAL districts geo_id='washco-or-commissioner-district-1'..4, district_type='LOCAL', state='or' LOWERCASE, mtfcc='X0018', label='Washington County Commissioner District N'. Step 4a: Chair office — WITH ins_p inserting politician 'Kathryn Harrington' (external_id -410100, party NULL, is_active true, is_incumbent true) ON CONFLICT (external_id) DO NOTHING; INSERT office title='County Chair', representing_state='OR', role_canonical NULL, joined to the COUNTY district (d.geo_id='41067' AND d.district_type='COUNTY' AND d.state='or'), NOT EXISTS guard on (district_id, politician_id). Step 4b: 4 commissioner office blocks — D1 Nafisa Fai (-410110), D2 Pam Treece (-410111), D3 Jason Snider (-410112), D4 Jerry Willey (-410113); each office title='Commissioner, District N', representing_state='OR', joined to its LOCAL district (d.geo_id='washco-or-commissioner-district-N' AND d.district_type='LOCAL' AND d.state='or'). Re-confirm these 5 names against washingtoncountyor.gov at execution before writing. Step 5: office_id back-fill UPDATE for external_id BETWEEN -410113 AND -410100 WHERE office_id IS NULL. Step 6: post-verification DO block with 4 gates — (a) gov count=1, (b) Chair offices on COUNTY district geo_id='41067'=1, (c) commissioner offices on LOCAL districts (geo_id LIKE 'washco-or-commissioner-district-%')=4, (d) section-split scan for the 41067 G4020 geofence=0 orphan rows; RAISE EXCEPTION on any mismatch, RAISE NOTICE on pass. Step 7 (AFTER COMMIT): register in the migration ledger using the 2-column form (version '1120', name 'washco_commission') ON CONFLICT DO NOTHING. Keep the tokens noted in critical_facts OUT of all comments.
  </action>
  <verify>
    <automated>grep -c "Washington County, Oregon, US" "C:/EV-Accounts/backend/migrations/1120_washco_commission.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File `1120_washco_commission.sql` exists and contains `'Washington County, Oregon, US'`.
    - File contains `'Board of County Commissioners'` and `'Washington County Board of County Commissioners'`.
    - File contains the COUNTY district insert with `'41067'`, `'COUNTY'`, and lowercase `'or'`.
    - File contains 4 LOCAL district inserts: `washco-or-commissioner-district-1` through `-4`, each with `'LOCAL'`, `'X0018'`, lowercase `'or'`.
    - File contains the 5 external_ids: `-410100`, `-410110`, `-410111`, `-410112`, `-410113`.
    - Chair office block filters `d.geo_id = '41067'` AND `d.district_type = 'COUNTY'`; commissioner blocks filter `washco-or-commissioner-district-` AND `'LOCAL'`.
    - File contains a pre-flight `RAISE EXCEPTION` keyed on `< 4` X0018 geofences.
    - File contains the ledger INSERT `VALUES ('1120', 'washco_commission')`.
    - File does NOT include the generated chamber column, nor the tokens `photo_origin_url`, in any line.
    - `grep -v '^--' 1120_washco_commission.sql | grep -ci "role_canonical) " ` confirms the offices INSERT lists role_canonical (set NULL for all 5).
  </acceptance_criteria>
  <done>Structural migration authored; idempotent guards on every INSERT; Chair on COUNTY / 4 commissioners on LOCAL X0018; in-migration 4-gate verification; ledger registration as 1120.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WashCo GIS FeatureServer → loader | Untrusted external GeoJSON (geometry + COMMDIST attribute) crosses into the DB |
| migration .sql → Postgres | Author-controlled, version-controlled SQL applied by orchestrator (trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-01 | Tampering | COMMDIST attribute from FeatureServer mapping to wrong geo_id | mitigate | Loader parses COMMDIST to integer and rejects/ skips anything outside 1-4 before forming geo_id; Wave-3 routing spot-check (Plan 03) confirms each district returns the expected commissioner |
| T-175-02 | Tampering | Malformed/self-intersecting polygon geometry ingested | mitigate | ST_IsValid asserted on insert; ST_MakeValid fallback + recheck (copied from Henderson analog); ST_SetSRID(...,4326) enforces CRS (outSR=4326 in URL) |
| T-175-03 | Injection | Field values (NAME) interpolated into SQL | mitigate | Parameterized inserts ($1..$4) for all data values; only the trusted constant mtfcc/state are string-interpolated (matches Henderson analog) |
| T-175-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: none) — supply-chain surface unchanged; no checkpoint required |

No HIGH-severity threats: the only external interaction is a read-only HTTPS GeoJSON fetch with no auth and no user input; all writes are parameterized, version-controlled SQL applied via psql.
</threat_model>

<verification>
Inline-orchestrator DB gates (psql against C:/EV-Accounts/backend/.env DATABASE_URL), run AFTER the loader runs live and 1120 is applied:

1. Geofences loaded: `SELECT COUNT(*), bool_and(public.ST_IsValid(geometry)) FROM essentials.geofence_boundaries WHERE state='or' AND mtfcc='X0018';` → expect (4, true).
2. Government row: `SELECT COUNT(*) FROM essentials.governments WHERE name='Washington County, Oregon, US';` → 1.
3. Roster = 5 offices: `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='Washington County, Oregon, US';` → 5.
4. Chair on COUNTY: `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='41067' AND d.district_type='COUNTY' AND d.state='or';` → 1.
5. Commissioners on LOCAL: `SELECT COUNT(*) FROM essentials.districts WHERE mtfcc='X0018' AND state='or';` → 4 (and 4 commissioner offices joined to them).
6. Section-split scan = 0: the in-migration gate (d) must have passed; re-run the 41067/G4020 orphan scan → 0 rows.
7. Ledger registered: `SELECT version FROM supabase_migrations.schema_migrations WHERE version='1120';` → 1 row.
</verification>

<success_criteria>
- Loader script authored; --dry-run lists 4 polygons; live run inserts 4 valid X0018 geofences (state='or').
- 1120_washco_commission.sql authored and applied: standalone government row (geo_id 41067, NOT under State of Oregon), Board of County Commissioners chamber (official_count 5), 5 offices (Chair on COUNTY / D1-D4 on LOCAL X0018).
- All in-migration gates pass; section-split = 0; ledger shows 1120.
- Satisfies ROADMAP success criteria #1 (standalone county + per-district routing) and the structural half of #2 (roster + correct office structure).
</success_criteria>

<output>
Create `.planning/phases/175-washington-county-commission-deep-seed/175-01-SUMMARY.md` when done. Record: confirmed next migration number, COUNTY-district pre-existence result, external_id block, X0018 confirmation, the 5 minted politician UUIDs (needed by Plans 02 and 03), and the verification gate outputs.
</output>
