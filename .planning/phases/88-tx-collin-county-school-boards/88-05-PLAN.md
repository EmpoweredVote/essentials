---
phase: 88-tx-collin-county-school-boards
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [TX-SCHOOL-05]
gap_closure: true
gaps_addressed:
  - "GAP 5: Richardson ISD 7 headshots are pre-circular-masked WordPress images with white corners"

must_haves:
  truths:
    - "All 7 Richardson ISD board members display clean rectangular headshot photos in the UI"
    - "No white corner artifacts visible on any Richardson ISD board member photo"
    - "Headshots are 600x750 JPEG at q90, 4:5 ratio, Lanczos — per project standard"
  artifacts:
    - path: "Supabase Storage: politician_photos bucket"
      provides: "7 replaced Richardson ISD headshots (external_ids -880029 to -880035)"
      contains: "clean rectangular photos, no circular masking"
  key_links:
    - from: "web.risd.org/board/[name-slug]/ bio pages"
      to: "Supabase Storage politician_photos bucket"
      via: "find-headshots skill → crop 4:5 → resize 600x750 → upload"
      pattern: "risd.org"
---

<objective>
Replace 7 Richardson ISD board member headshots that are pre-circular-masked WordPress images (white corners baked into JPEG). Source replacement rectangular photos from individual bio pages at web.risd.org/board/[name-slug]/ or RISD press/news articles, then process and upload to Supabase Storage per project headshot standards.

Purpose: GAP 5 — Richardson ISD headshots currently display with white corner artifacts due to WordPress circular masking on source images.
Output: 7 clean headshots uploaded to Supabase Storage, replacing the circular-masked originals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/88-tx-collin-county-school-boards/88-UAT.md
@.planning/phases/88-tx-collin-county-school-boards/88-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-source 7 Richardson ISD rectangular headshots from bio pages or press photos</name>
  <files></files>
  <action>
Query the DB to get the 7 Richardson ISD board members' politician_ids and office_titles so you know which person maps to which external_id:

  SELECT p.id, p.full_name, p.external_id, o.title AS office_title
  FROM essentials.politicians p
  JOIN essentials.offices o ON o.politician_id = p.id
  WHERE p.external_id BETWEEN -880035 AND -880029
  ORDER BY p.external_id DESC;

Richardson ISD board members (external_ids -880029 through -880035, Districts 1-5 + Places 6-7):
- -880029: District 1
- -880030: District 2
- -880031: District 3 (Debbie Rentería — accented character in name; confirmed passing UAT test 7)
- -880032: District 4
- -880033: District 5
- -880034: Place 6
- -880035: Place 7

For each board member, attempt to find a rectangular (non-circular) headshot in this order:
1. Individual bio page: web.risd.org/board/[firstname-lastname]/ — look for any img or background-image that is NOT the /wp-content/uploads/ circular source. Bio pages sometimes embed a different higher-resolution rectangular photo in the page body or sidebar.
2. RISD press releases / news at web.risd.org — search for the board member's name in news articles where they may appear in a non-circular photo.
3. LinkedIn public profile photo (if RISD doesn't have a rectangular source).
4. Local news articles (e.g., dallasnews.com, communityimpact.com, plano-star-courier.com) that covered board elections or meetings.

For each person: record the source URL found (or document as NO_RECTANGULAR_SOURCE if truly none found after checking all four sources).

Use the /find-headshots skill (@$HOME/.claude/commands/find-headshots.md) to guide the search. Do NOT use the existing web.risd.org/board/wp-content/uploads/ URLs — those are confirmed circular-masked.

Project headshot standards (mandatory):
- Crop to 4:5 ratio first (never stretch) — eyes at ~1/3 from top, full head + shoulders visible
- Resize to 600x750 using Lanczos, q90
- Save as JPEG to Supabase Storage politician_photos bucket at path: {politician_id}-headshot.jpg
- Insert into essentials.politician_images with type='default' (NOT 'headshot' — will be invisible)
- photo_license: use 'public_domain' if from official government/school district site; 'fair_use' if from news; 'assumed_public' if from LinkedIn/press release

For any member where NO rectangular source exists after exhausting all four sources: document as not-found with the sources checked. Do not upload a circular-masked image.
  </action>
  <verify>
    <automated>
SELECT p.full_name, p.external_id, pi.url, pi.type
FROM essentials.politicians p
LEFT JOIN essentials.politician_images pi ON pi.politician_id = p.id AND pi.type = 'default'
WHERE p.external_id BETWEEN -880035 AND -880029
ORDER BY p.external_id DESC;
    </automated>
  </verify>
  <done>
For each of the 7 board members: either (a) politician_images row exists with type='default' and url pointing to a new rectangular headshot in Supabase Storage, OR (b) the member is documented as NO_RECTANGULAR_SOURCE with sources checked listed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Re-sourced and uploaded replacement headshots for Richardson ISD board members. Up to 7 photos replaced with rectangular (non-circular) versions processed to 600x750 JPEG 4:5 at q90.
  </what-built>
  <how-to-verify>
1. Open ev-ui in browser and enter a Richardson TX address (e.g. 400 S. Greenville Ave, Richardson TX 75081).
2. Navigate to the Representatives tab.
3. Find the "Richardson Independent School District" section.
4. Inspect each of the 7 board member cards — confirm NO white corner artifacts are visible.
5. Check that photos are cropped to 4:5 portrait ratio with full head and shoulders showing.
6. For any member documented as NO_RECTANGULAR_SOURCE: confirm they display name-only (no broken image icon) — this is acceptable.
7. Confirm Debbie Rentería's name still renders with correct accented character (this was passing before; confirm no regression).
  </how-to-verify>
  <resume-signal>Type "approved" if all visible headshots show clean rectangular photos. Or describe which members still have issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External photo source → Supabase Storage | Photos sourced from public sites; stored in controlled bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-88-05-01 | Information Disclosure | politician photo PII | accept | Only photos from official/public sources; no private images |
| T-88-05-02 | Tampering | politician_images.type | mitigate | Always insert type='default' — type='headshot' causes silent UI invisibility |
</threat_model>

<verification>
Human visual check required (checkpoint:human-verify task above). Automated query confirms politician_images rows exist with type='default' for replaced members.
</verification>

<success_criteria>
- All 7 Richardson ISD board members either have clean rectangular headshots (no white corners) in Supabase Storage, or are documented as NO_RECTANGULAR_SOURCE
- politician_images rows for any new uploads use type='default'
- Human checkpoint approved in ev-ui
- GAP 5 closed (or documented as best-effort with no rectangular source available)
</success_criteria>

<output>
Create `.planning/phases/88-tx-collin-county-school-boards/88-05-SUMMARY.md` when done
</output>
