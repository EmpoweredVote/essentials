---
phase: 145-lancaster-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql
autonomous: true
requirements: [LANC-01]

must_haves:
  truths:
    - "D-01: this phase reconciles the EXISTING partial Lancaster seed (UPDATE/move-not-INSERT) — no greenfield rebuild, no duplicate offices/external_ids; all existing rows preserved"
    - "D-02: the Lancaster government row 'City of Lancaster, California, US' (id f6732517-76d8-4f5f-b528-e49d60f32a4c) carries geo_id '0640130' (no longer NULL), guarded WHERE geo_id IS NULL"
    - "D-03 (move-then-delete, UUID-targeted): the duplicate chamber a9be708e's single office afd045ec (Crist's, ext_id 686320) is MOVED into the survivor chamber 9b9014b4 FIRST, an inline assert proves a9be708e then has 0 offices, THEN a9be708e is DELETED — end state is ONE 'City Council' chamber (9b9014b4) with exactly 5 offices (1 Mayor + 4 Councilmember)"
    - "D-03b (bidirectional-link repair): politicians.office_id is set to match the office that already points at them (offices.politician_id) for the 3 CONTINUING members — Parris -200795 → office ed37230d, Hughes-Leslie -201279 → office 052a2e17, Mann -201281 → office 6e17ff80 (all were NULL); the forward pointer offices.politician_id was already correct — this repairs the reverse pointer only"
    - "D-06: the survivor chamber's Mayor office ed37230d points at district b59c2734 which already carries district_type='LOCAL_EXEC' (directly-elected Mayor, NOT a rotational/council seat) — CONFIRMED read-only, no change expected; district_type lives on essentials.districts NOT on offices (there is no offices.district_type column — do not write to it)"
    - "D-05: feedback_section_split_check SQL returns 0 rows for Lancaster after consolidation"
    - "D-04: this reconcile (geo_id backfill + chamber merge + bidirectional-link repair) is a STRUCTURAL migration (910) that registers in supabase_migrations.schema_migrations normally"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql"
      provides: "Idempotent geo_id backfill + move-office-afd045ec-then-delete-duplicate-chamber-a9be708e + reverse-pointer (politicians.office_id) repair for the 3 continuing members + official_count=5"
      contains: "a9be708e"
  key_links:
    - from: "essentials.governments (id f6732517-76d8-4f5f-b528-e49d60f32a4c)"
      to: "geo_id '0640130'"
      via: "UPDATE ... WHERE id = 'f6732517-76d8-4f5f-b528-e49d60f32a4c' AND geo_id IS NULL"
      pattern: "geo_id\\s*=\\s*'0640130'"
    - from: "essentials.offices (afd045ec, Crist's office in duplicate chamber a9be708e)"
      to: "survivor chamber 9b9014b4"
      via: "UPDATE offices SET chamber_id='9b9014b4...' WHERE id='afd045ec...'"
      pattern: "chamber_id\\s*=\\s*'9b9014b4"
    - from: "essentials.chambers (a9be708e, emptied duplicate)"
      to: "deleted (asserted 0 offices first)"
      via: "DO $$ assert 0 offices $$ then DELETE FROM essentials.chambers WHERE id = 'a9be708e...'"
      pattern: "DELETE FROM essentials\\.chambers"
    - from: "essentials.politicians (ext_id -200795/-201279/-201281, continuing members)"
      to: "their already-pointing office (offices.politician_id) — reverse pointer repaired"
      via: "UPDATE politicians SET office_id=<matching office> WHERE external_id=N AND office_id IS NULL"
      pattern: "office_id\\s*=\\s*'ed37230d"
---

<objective>
Reconcile the existing partial, duplicate-chamber Lancaster seed in a single idempotent structural
migration (910). This is the foundation wave — every later wave (roster, headshots, stances) assumes a
clean, unambiguous, single-chamber structure with consistent bidirectional office↔politician links.

⚠️ CORRECTED REALITY (DB-verified 2026-06-20, both link directions — see CONTEXT §db_precheck). The
earlier "survivor shells are EMPTY, reseat 3 office-unlinked members into empty shells" framing was a
WRONG-DIRECTION-JOIN artifact and is FALSE. The survivor chamber 9b9014b4's offices are ALREADY occupied
via offices.politician_id; what is broken is the REVERSE pointer politicians.office_id (NULL for
Parris/Hughes-Leslie/Mann/Malhi). This wave does NOT seat anyone into an empty shell — it merges the
duplicate chamber and REPAIRS the reverse pointer for the 3 continuing members. (Retiring Malhi+Crist and
creating White+Castellanos belongs to Wave 2 — this wave only does structure + link repair, same
structure/roster separation as Glendale's Najarian.)

Three structural fixes, all DB-verified against the live state on 2026-06-20:
1. Backfill `essentials.governments.geo_id = '0640130'` on the Lancaster gov row
   (id f6732517-76d8-4f5f-b528-e49d60f32a4c, currently NULL), guarded `WHERE geo_id IS NULL`
   (D-01 / D-02 / LANC-01).
2. MERGE the duplicate chamber: MOVE office afd045ec (Crist's, the only office under a9be708e) into the
   survivor chamber 9b9014b4 (UPDATE the office's chamber_id), then an inline assert confirms a9be708e
   now has 0 offices, then DELETE the duplicate chamber a9be708e. Target both chambers/the office by
   UUID ONLY — both chambers share the name 'City Council' and slug 'lancaster-city-council' (D-03).
   End state: ONE chamber (9b9014b4) with exactly 5 offices (1 Mayor + 4 Councilmember).
3. REPAIR the reverse pointer politicians.office_id for the 3 CONTINUING members so it matches the
   office that already points at them (D-03b): Parris -200795 → ed37230d, Hughes-Leslie -201279 →
   052a2e17, Mann -201281 → 6e17ff80. Currently all three are NULL. (Malhi -201280's office_id is also
   NULL but he is NOT repaired here — he is retired in Wave 2.)

The Mayor office ed37230d points at district b59c2734 which already carries district_type='LOCAL_EXEC'
(D-06 — directly-elected Mayor, the KEY contrast with Glendale's rotational mayor). This is CONFIRMED
read-only; NO change is expected. district_type is a column on essentials.districts, NOT on offices —
there is no offices.district_type column; do NOT attempt to UPDATE it. The 4 at-large Councilmember
offices point at the shared At-Large district 0097ca4d (D-08); leave them as-is.

Purpose: A clean structural base. Duplicate 'City Council' chamber names cause split-section render bugs;
NULL geo_id breaks routing; a desynced bidirectional link causes officials to render unseated or seats to
render empty depending on which pointer the query reads. Output: Migration 910 applied to production; all
post-verification queries green; split-section check returns 0 rows for Lancaster.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally (it is
NOT a stance/headshot audit-only migration). On-disk file counter is authoritative; next structural is
910 (D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-lancaster-deep-seed/145-CONTEXT.md
@.planning/phases/145-lancaster-deep-seed/145-RESEARCH.md
@.planning/phases/145-lancaster-deep-seed/145-VALIDATION.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- DB-verified live values (2026-06-20, BOTH link directions). Task 1 re-confirms them read-only
     before any write and STOPs on drift. -->

Government row:
  id     = f6732517-76d8-4f5f-b528-e49d60f32a4c   name = 'City of Lancaster, California, US'   type = LOCAL
  geo_id = NULL  -> set to '0640130'   (state already 'CA')

Two chambers, BOTH named 'City Council' / slug 'lancaster-city-council':
  KEEP   9b9014b4-0106-417f-a104-ac2055fc8134  (SURVIVOR — 4 offices, all OCCUPIED via offices.politician_id)
  DELETE a9be708e-1b42-42ac-92ec-e8e56f9c6474  (duplicate — 1 office afd045ec, holds Crist 686320)
  (chambers.slug is a GENERATED column — NEVER include slug in any write)

Survivor 9b9014b4 offices (occupied via offices.politician_id; politicians.office_id reverse pointer is NULL):
  ed37230d  Mayor            district b59c2734 (LOCAL_EXEC)  occupant R. Rex Parris        -200795
  052a2e17  Council Member   district 0097ca4d (At-Large)    occupant Lauren Hughes-Leslie -201279
  6e17ff80  Council Member   At-Large                        occupant Ken Mann             -201281
  03a0dae7  Council Member   At-Large                        occupant Raj Malhi (LOST)     -201280   (retire in Wave 2)
Duplicate a9be708e office (to MOVE into survivor, then delete the empty chamber):
  afd045ec  Council Member   district 9e9b89cd (At-Large)    occupant Marvin Crist (RETIRED) 686320  (retire in Wave 2)

Move-then-delete (D-03, SC 894 pattern — UUID-targeted):
  1. UPDATE essentials.offices SET chamber_id='9b9014b4-...' WHERE id='afd045ec-...'
     (moves Crist's single office into the survivor)
  2. DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='a9be708e-...') > 0
     THEN RAISE EXCEPTION 'Chamber a9be708e still has offices'; END IF; END $$;
  3. DELETE FROM essentials.chambers WHERE id='a9be708e-...'

Reverse-pointer repair (D-03b — set politicians.office_id where NULL, 3 continuing members only):
  -200795 Parris        -> office_id 'ed37230d-...'   (matches offices.politician_id already pointing at him)
  -201279 Hughes-Leslie -> office_id '052a2e17-...'
  -201281 Mann          -> office_id '6e17ff80-...'
  (DO NOT repair -201280 Malhi or 686320 Crist — they are retired in Wave 2.)

Schema note (CRITICAL): district_type lives on essentials.districts, NOT on offices. There is NO
  offices.district_type column. The Mayor district b59c2734 ALREADY has district_type='LOCAL_EXEC' —
  CONFIRM it read-only; do NOT attempt any UPDATE offices SET district_type=... (will error).

Crist (686320) + Malhi (-201280): their RETIREMENT is Wave 2, NOT here. In this wave only Crist's OFFICE
  (afd045ec) is moved (chamber_id change). Do NOT touch any politician's is_active/office_id here EXCEPT
  the 3 reverse-pointer repairs above.

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after
  consolidation; expect 0 rows for Lancaster. (5 OTHER LA-area cities have pre-existing split-section
  defects — OUT OF SCOPE here.)

Pattern migration to copy: C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql
  (geo_id backfill + FK-safe move/teardown + idempotent guards — the direct move-then-delete precedent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state (gov, two chambers, both link directions, Mayor district_type)</name>
  <files>C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql (the direct STRUCTURAL template — geo_id backfill + move-then-delete + idempotent guard patterns)
    - .planning/phases/143-santa-clarita-deep-seed/143-01-SUMMARY.md (exact move-then-delete reconcile shape + post-verification query set + the "live state differed from plan, reseat existing rows" finding)
    - .planning/phases/145-lancaster-deep-seed/145-CONTEXT.md (§db_precheck — LOCKED corrected state incl. both link directions, §decisions D-01..D-06, D-08, D-03b)
    - .planning/phases/145-lancaster-deep-seed/145-RESEARCH.md (§Live-Data Findings, §Common Pitfalls 1/3, §Validation Architecture Wave 1)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA' uppercase, district_type on districts)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE, run read-only confirmation queries via mcp__supabase-local
    execute_sql (= production) or psql -f using C:/EV-Accounts/backend/.env DATABASE_URL. Confirm EACH
    against the LOCKED CONTEXT §db_precheck:
    (a) gov: SELECT id, geo_id, state FROM essentials.governments WHERE
    id='f6732517-76d8-4f5f-b528-e49d60f32a4c' — confirm geo_id NULL, state 'CA';
    (b) two chambers under that gov both named 'City Council' / slug 'lancaster-city-council' —
    9b9014b4 (SURVIVOR) and a9be708e (DUPLICATE, 1 office);
    (c) survivor 9b9014b4 has exactly 4 offices: ed37230d Mayor, 052a2e17/6e17ff80/03a0dae7 Council
    Member — and confirm via offices.politician_id that they are OCCUPIED (Parris -200795 on ed37230d,
    Hughes-Leslie -201279 on 052a2e17, Mann -201281 on 6e17ff80, Malhi -201280 on 03a0dae7). This is the
    KEY correction: these offices are NOT empty shells;
    (d) a9be708e has exactly 1 office afd045ec occupied by Crist (ext_id 686320) — this is the MOVE
    target (Pitfall 1: do NOT delete the chamber before moving it);
    (e) confirm the REVERSE pointer state: SELECT external_id, office_id FROM essentials.politicians
    WHERE external_id IN (-200795,-201279,-201281,-201280,686320) — expect office_id NULL for
    Parris/Hughes-Leslie/Mann/Malhi and office_id set (afd045ec) for Crist (the desync this wave repairs
    for the 3 continuing members);
    (f) confirm the Mayor office ed37230d → district b59c2734, and that essentials.districts row
    b59c2734 already has district_type='LOCAL_EXEC' (D-06, read-only); confirm the 4 Council offices
    point at the At-Large district 0097ca4d (D-08).
    If ANY precondition does not match the LOCKED CONTEXT (a third chamber, a9be708e has 0 or >1 offices,
    an office is occupied by a different ext_id than locked, the Mayor district is not LOCAL_EXEC, or the
    reverse pointers are not as described), STOP and report the discrepancy — do NOT write a migration
    against a moved target. Do NOT reintroduce the "empty shells / reseat 3 unlinked" framing.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id, state FROM essentials.governments WHERE id='f6732517-76d8-4f5f-b528-e49d60f32a4c';" returns one row, geo_id NULL, state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - Gov f6732517 has geo_id NULL and state 'CA' (pre-state)
    - Two chambers confirmed sharing name 'City Council' / slug: 9b9014b4 (KEEP, 4 offices) and a9be708e (DELETE, 1 office afd045ec)
    - Survivor 9b9014b4 offices confirmed OCCUPIED via offices.politician_id (ed37230d=Parris -200795, 052a2e17=Hughes-Leslie -201279, 6e17ff80=Mann -201281, 03a0dae7=Malhi -201280) — NOT empty shells
    - a9be708e confirmed to have exactly 1 office afd045ec occupied by Crist (ext_id 686320) — the move target
    - Reverse pointer confirmed NULL for Parris/Hughes-Leslie/Mann/Malhi and set (afd045ec) for Crist — the desync to repair
    - Mayor office ed37230d → district b59c2734 with district_type='LOCAL_EXEC' (read-only, D-06); 4 Council offices → At-Large district 0097ca4d (D-08); no offices.district_type column touched
    - Any mismatch vs CONTEXT §db_precheck is surfaced and execution paused (STOP-on-drift; no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches the LOCKED CONTEXT §db_precheck on BOTH link directions; gov UUID, the move target office afd045ec, the 3 reverse-pointer repairs, and the LOCAL_EXEC Mayor district are all confirmed; the geo_id backfill, chamber merge, and reverse-pointer repair are safe to author.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 910 (geo_id backfill + merge duplicate chamber + repair reverse pointer for 3 continuing members)</name>
  <files>C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql (copy the idempotent geo_id backfill + office move + FK-safe chamber DELETE + inline-assert patterns; Lancaster is the move-then-delete case)
    - .planning/phases/143-santa-clarita-deep-seed/143-01-SUMMARY.md (exact post-verification query set + idempotency guard shapes)
    - .planning/phases/145-lancaster-deep-seed/145-CONTEXT.md (§db_precheck LOCKED ids, §decisions D-02..D-06, D-08, D-03b)
    - .planning/phases/145-lancaster-deep-seed/145-RESEARCH.md (§Architecture Patterns Wave 1 template, §Common Pitfalls 1/3, §Validation Architecture Wave 1 checks)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql wrapped in BEGIN/COMMIT, all
    statements idempotent, in this order:
    (1) UPDATE essentials.governments SET geo_id='0640130' WHERE
        id='f6732517-76d8-4f5f-b528-e49d60f32a4c' AND geo_id IS NULL (D-01 / D-02 / LANC-01).
    (2) MERGE — MOVE Crist's office into the survivor: UPDATE essentials.offices SET chamber_id=
        '9b9014b4-0106-417f-a104-ac2055fc8134' WHERE id='afd045ec-...' (D-03 step 1 — move FIRST,
        target by office UUID; Pitfall 1).
    (3) Inline assert the duplicate is now empty: a DO $$ block that RAISEs EXCEPTION if
        (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='a9be708e-1b42-42ac-92ec-e8e56f9c6474') > 0.
    (4) DELETE essentials.chambers WHERE id='a9be708e-1b42-42ac-92ec-e8e56f9c6474'. Target the duplicate
        by its UUID ONLY, never by name/slug (both chambers share 'City Council' / 'lancaster-city-council').
    (5) REPAIR the reverse pointer for the 3 CONTINUING members only (D-03b), each guarded by external_id
        AND office_id IS NULL: UPDATE essentials.politicians SET office_id='ed37230d-...' WHERE
        external_id=-200795 AND office_id IS NULL; SET office_id='052a2e17-...' WHERE external_id=-201279
        AND office_id IS NULL; SET office_id='6e17ff80-...' WHERE external_id=-201281 AND office_id IS NULL.
        Do NOT repair -201280 Malhi or 686320 Crist (retired in Wave 2). Do NOT alter offices.politician_id
        (the forward pointer is already correct).
    (6) Set survivor official_count: UPDATE essentials.chambers SET official_count=5 WHERE
        id='9b9014b4-...' AND official_count IS DISTINCT FROM 5 (final 5-seat roster after Wave 2).
    Do NOT touch the slug column anywhere (GENERATED). Do NOT write to offices.district_type (no such
    column — district_type is on essentials.districts; Mayor district b59c2734 is already LOCAL_EXEC, leave
    it). Do NOT add district_id to the at-large Council offices (D-08). Do NOT retire Crist/Malhi or create
    White/Castellanos here (Wave 2). End the file with post-verification queries: geo_id check + a chamber
    listing under gov f6732517 (expect exactly one 'City Council', the survivor 9b9014b4) + a count of
    offices under 9b9014b4 (expect 5) + a read-only Mayor-district district_type check via the
    offices→districts join (expect LOCAL_EXEC) + a reverse-pointer check (office_id non-NULL and matching
    offices.politician_id for -200795/-201279/-201281) + the feedback_section_split_check SQL (expect 0
    rows for Lancaster). Apply via mcp__supabase-local execute_sql or psql -f. This registers in
    schema_migrations normally (structural migration 910).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id FROM essentials.governments WHERE id='f6732517-76d8-4f5f-b528-e49d60f32a4c';" returns '0640130' AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='9b9014b4-0106-417f-a104-ac2055fc8134';" returns 5 AND psql "$DATABASE_URL" -c "SELECT office_id FROM essentials.politicians WHERE external_id=-200795;" returns 'ed37230d-...' (reverse pointer repaired)</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='f6732517-76d8-4f5f-b528-e49d60f32a4c'` → '0640130'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='f6732517-76d8-4f5f-b528-e49d60f32a4c' AND name='City Council'` → 1 (not 0, not 2); a9be708e deleted
    - `SELECT COUNT(*) FROM essentials.chambers WHERE id='a9be708e-1b42-42ac-92ec-e8e56f9c6474'` → 0 (duplicate deleted)
    - `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='9b9014b4-0106-417f-a104-ac2055fc8134'` → 5 (afd045ec moved in; 1 Mayor + 4 Councilmember)
    - politicians.office_id repaired: -200795→ed37230d, -201279→052a2e17, -201281→6e17ff80 (each matches its offices.politician_id); Malhi -201280 + Crist 686320 office_id UNCHANGED
    - Mayor office ed37230d → district b59c2734 still district_type='LOCAL_EXEC' (read-only confirm); no offices.district_type write attempted; 4 Council offices still At-Large (0097ca4d)
    - feedback_section_split_check SQL returns 0 rows for Lancaster (D-05)
    - No politician's is_active was modified; Crist/Malhi NOT retired here; White/Castellanos NOT created here
    - Migration 910 registered in supabase_migrations.schema_migrations (structural)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
  </acceptance_criteria>
  <done>Migration 910 applied; one 'City Council' chamber (9b9014b4) remains with 5 offices; geo_id 0640130 set; the 3 continuing members' reverse pointers repaired (office_id matches offices.politician_id); Mayor district confirmed LOCAL_EXEC; split-section check 0 rows; re-running the file is a no-op.</done>
</task>

</tasks>

<threat_model>
DB-seeding phase with NO new external attack surface. No untrusted user input crosses any boundary —
migration SQL is applied via service-role/operator (psql/MCP) directly against the internal production
Supabase DB. The real threat surface is accidental data corruption — deleting the duplicate chamber
before moving Crist's office (orphan), leaving the bidirectional link desynced, or writing district_type
to a non-existent offices column — mitigated by move-FIRST ordering, an inline empty-chamber assert,
UUID-scoped targeting, office_id IS NULL guards on the reverse-pointer repair, idempotent guards, and a
STOP-on-drift pre-flight that checks BOTH link directions.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator (psql/MCP) directly against production Supabase. No untrusted user input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Tampering | Chamber DELETE targets the wrong chamber (both share name 'City Council' / slug) | mitigate | DELETE keyed on UUID 'a9be708e…' ONLY, never on name/slug; post-verify exactly one 'City Council' chamber (9b9014b4) remains |
| T-145-02 | Tampering | Duplicate chamber deleted BEFORE office afd045ec is moved → orphans Crist's office | mitigate | Move-FIRST ordering (UPDATE office chamber_id before DELETE) + inline DO $$ assert RAISEs if a9be708e still has >0 offices before the DELETE (SC 894 pattern) |
| T-145-03 | Tampering | geo_id overwritten on a re-run or wrong gov | mitigate | UPDATE keyed on id f6732517 + WHERE geo_id IS NULL; second apply = 0 rows |
| T-145-26 | Tampering | Reverse-pointer repair desync persists, or repairs a departing member (Malhi) | mitigate | office_id set ONLY for the 3 continuing members (-200795/-201279/-201281), each guarded office_id IS NULL; post-verify office_id matches offices.politician_id; Malhi/Crist office_id left untouched |
| T-145-27 | Tampering | Write to non-existent offices.district_type column (over-copying old plan) → migration error | mitigate | district_type read-only via offices→districts join; Mayor district b59c2734 already LOCAL_EXEC; no UPDATE offices SET district_type anywhere |
| T-145-05 | Tampering | Non-idempotent migration applied twice corrupts state | mitigate | All writes guarded (geo_id IS NULL, id-scoped move/delete, office_id IS NULL, official_count IS DISTINCT FROM); second apply = 0 rows changed |
| T-145-06 | Tampering | Accidental write to production without verifying pre-state (drift since 2026-06-20) | mitigate | Task 1 read-only pre-flight confirms BOTH link directions match CONTEXT §db_precheck before any write; STOP-on-mismatch |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 910:
1. gov f6732517 geo_id = '0640130'
2. exactly 1 chamber 'City Council' under gov f6732517; duplicate a9be708e gone
3. survivor chamber 9b9014b4 has exactly 5 offices (1 Mayor + 4 Councilmember) and official_count=5
4. Mayor office ed37230d → district b59c2734 district_type='LOCAL_EXEC' (read-only); 4 Council offices → At-Large 0097ca4d
5. reverse pointer repaired: politicians.office_id non-NULL and matching offices.politician_id for -200795/-201279/-201281; Malhi/Crist office_id unchanged
6. feedback_section_split_check SQL → 0 rows for Lancaster
7. no is_active changes; Crist/Malhi not retired; White/Castellanos not created (those are Wave 2)
8. migration 910 registered in schema_migrations
</verification>

<success_criteria>
All structural defects fixed and independently verified: geo_id backfilled; the duplicate chamber merged
via move-then-delete (with an inline assert proving it was empty before deletion); the bidirectional
office↔politician link repaired for the 3 continuing members so both pointers agree; the directly-elected
Mayor district confirmed LOCAL_EXEC (read-only); migration 910 idempotent and registered; Lancaster is a
single unambiguous 'City Council' chamber with 5 offices, ready for roster turnover in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/145-lancaster-deep-seed/145-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
