---
phase: 197-sahuarita-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["197-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-sahuarita-headshots.py
  - C:/EV-Accounts/backend/migrations/1355_town_of_sahuarita_headshots.sql
autonomous: true
requirements: [SUB-03]
must_haves:
  truths:
    - "All 7 officials have a 600×750 (4:5, Lanczos q90) headshot uploaded to the politician_photos Storage bucket (type='default', press_use)"
    - "Each of the 7 politician_images rows binds to the CURRENT roster member seeded in Plan 01 (the execute-time-confirmed roster)"
    - "Because sahuaritaaz.gov (CivicPlus) is NOT WAF-blocked (HTTP 200) but its /ImageRepository/Document?documentID=N image endpoint SOFT-blocks non-browser clients (HTTP 200 with a 0-byte body), source portraits are retrieved via the /find-headshots Playwright flow (verifying byte-count > 0), with Ballotpedia/Wikimedia as fallback; photo_license is documented PER image (not a uniform value)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-sahuarita-headshots.py"
      provides: "crop_to_4_5 → resize 600×750 Lanczos q90 → Storage x-upsert pipeline for 7 officials; source URLs resolved via Playwright (soft-block fallback)"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1355_town_of_sahuarita_headshots.sql"
      provides: "7 audit-only politician_images rows (unregistered), UUIDs from Plan 01 manifest, per-image photo_license"
      contains: "politician_images"
  key_links:
    - from: "_tmp-sahuarita-headshots.py"
      to: "sahuaritaaz.gov (Playwright) / Ballotpedia / Wikimedia"
      via: "browser-rendered image bytes (byte-count > 0 verified), then requests/PIL pipeline"
      pattern: "crop_to_4_5"
    - from: "1355_town_of_sahuarita_headshots.sql"
      to: "essentials.politicians"
      via: "url binds to politician UUID resolved by external_id"
      pattern: "politician_images"
---

<objective>
Acquire and bind 7/7 headshots for the Sahuarita council. Author a gitignored pipeline script that
processes each official's portrait, crops to 4:5 (crop-first, never distort), resizes to 600×750 Lanczos
q90, and uploads to the `politician_photos` Storage bucket; plus an AUDIT-ONLY migration that inserts the 7
`politician_images` rows.

CRITICAL (Pitfall 4 — a DIFFERENT block signature than every prior AZ phase): the official host
sahuaritaaz.gov (CivicPlus, NOT Akamai) returns a clean HTTP 200 for both the Town Council roster page AND
the discovered `/ImageRepository/Document?documentID=N` image URLs — it is NOT WAF-blocked. BUT a direct
`curl` of that image endpoint returns HTTP 200 with an EMPTY body (`Content-Length: 0`, no `Content-Type`)
even with a cookie jar + Referer set — a SOFT block that serves a plausible-looking 200 to non-browser
clients. Do NOT trust a "direct fetch succeeded" claim on HTTP 200 alone — a downloaded 0-byte file is the
failure signature here (not a hard 403). Per D-05: attempt the direct sahuaritaaz.gov fetch FIRST, verify
byte-count > 0 AND that PIL can open it, and if it comes back empty use the `/find-headshots` skill's
Playwright browser-navigation flow (a real browser context correctly retrieves the image bytes, as proven
for the CivicPlus `/ImageRepository/Document` pattern in the Palmdale CA phase). Ballotpedia/Wikimedia are
the secondary fallback. Do NOT craft evasive headers to defeat the soft block. `photo_license` is
documented PER image (varies by source), NOT a uniform value.

Purpose: Satisfies SUB-03 / ROADMAP success criterion #2 (all seated officials have 600×750 headshots).
Output: `_tmp-sahuarita-headshots.py` (gitignored `backend/scripts/_*`) +
`1355_town_of_sahuarita_headshots.sql` (audit-only, unregistered), both applied so all 7 images serve from
the CDN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md
@.planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md
@.planning/phases/197-sahuarita-deep-seed/197-01-SUMMARY.md

<execution_note>
Depends on Plan 01: the 7 politician UUIDs (external_id → UUID → full_name) captured in 197-01-SUMMARY.md
are REQUIRED — the audit migration's CDN url values derive from the politician UUID
({uuid}-headshot.jpg), and the script resolves UUIDs at runtime via `SELECT id FROM essentials.politicians
WHERE external_id=%s`. Executor authors both files; the ORCHESTRATOR resolves the 7 source image URLs
(direct sahuaritaaz.gov first with a byte-count check, Playwright soft-block fallback, then
Ballotpedia/Wikimedia), runs the script, and applies the audit migration (executor has no
DB/Storage/web access). Commit the migration via `git -C "C:/EV-Accounts"`; the `_tmp-*` script is
gitignored and never committed. If the Task 2 roster changed any seat, source the CONFIRMED sitting member
— not the research-date name.
</execution_note>

<interfaces>
- Source hosts: sahuaritaaz.gov FIRST (CivicPlus, HTTP 200, NOT WAF-blocked) — the roster/staff pages
  expose `/ImageRepository/Document?documentID=N` URLs with descriptive alt text (e.g. Tom Murphy
  documentID=6097, alt="TOM-MURPHY-TOWN-COUNCIL-2020"), BUT a direct fetch of that image URL SOFT-blocks
  (200 + 0-byte body) → retrieve via the `/find-headshots` Playwright flow and VERIFY byte-count > 0.
  Ballotpedia candidate pages + Wikimedia are the secondary fallback. Do NOT spoof headers.
- essentials.politician_images columns: exactly (id, politician_id, url, type, photo_license) — NO
  photo_origin_url. type='default'; photo_license PER image (municipal government portrait / press_use, or
  Wikimedia/CC terms, or Ballotpedia press_use, or operator_supplied) — NOT a uniform value.
- Storage: bucket politician_photos; canonical path {politician_uuid}-headshot.jpg; x-upsert PUT; CDN base
  https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- ext_id block: -4014001 Murphy (Mayor), -4014002 Egbert (Vice Mayor), -4014003 Morales, -4014004
  Gillespie, -4014005 Priolo, -4014006 Lisk, -4014007 Lytle (subject to the Task 2 confirmed roster).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Sahuarita headshot pipeline script (soft-block Playwright fallback + byte-count guard)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-sahuarita-headshots.py</files>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md (§ headshot analog `1355_town_of_sahuarita_headshots.sql` mapping + the shared-pipeline note: reuse crop_to_4_5/resize_600x750/process_headshot_bytes/upload_to_storage/resolve_politician_id/dry_run_head_check/HEADERS verbatim from the Marana analog; ONLY ROSTER + source-acquisition change; sahuaritaaz.gov-first with a byte-count check then Playwright; mixed per-image license)
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Common Pitfall 4 — sahuaritaaz.gov soft-block signature: HTTP 200 + 0-byte body; verify byte-count > 0; Playwright fallback; per-image license discipline)
    - C:/EV-Accounts/backend/scripts/_tmp-marana-headshots.py (PRIMARY analog — read in full: reuse crop_to_4_5, resize_600x750, process_headshot_bytes, upload_to_storage, resolve_politician_id, env-load, dry_run_head_check, descriptive-UA HEADERS verbatim; only ROSTER + source-URL builder + count assertions change)
  </read_first>
  <action>
    Copy `_tmp-marana-headshots.py` and adapt. Reuse crop_to_4_5 / resize_600x750 / process_headshot_bytes
    (crop-FIRST then resize) / upload_to_storage (x-upsert PUT to `politician_photos/{uuid}-headshot.jpg`) /
    resolve_politician_id (parameterized `external_id=%s`, never hardcode a UUID) / the env-load block /
    dry_run_head_check / the descriptive User-Agent HEADERS — VERBATIM. Keep TARGET_SIZE=(600,750),
    JPEG_QUALITY=90, RESAMPLE=LANCZOS, MIN_DIM=100.

    Build a ROSTER list of 7 dicts (assert len(ROSTER)==7), each with: external_id (-4014001..-4014007),
    full_name, seat/title label, photo_url (initially the `_MISSING`/`TODO_` sentinel — the ORCHESTRATOR
    fills the resolved image URL per member in Task 3), photo_license (PER member — set from the actual
    source at fill time, NOT a uniform value), is_required=True (all 7). Add `source_url_for(member)`
    returning `member['photo_url']`.

    ADAPT the byte-count guard for this phase's soft-block signature: after any direct fetch, assert the
    response body length > 0 (and MIN_DIM) BEFORE treating it as a success — a 0-byte 200 is the
    sahuaritaaz.gov soft-block, not a real image. Keep the `_MISSING = 'TODO_...'` sentinel + `main()`
    fail-fast guard so a placeholder URL can NEVER reach a Storage write — if any ROSTER photo_url is still
    the sentinel, print the member and `sys.exit(1)`. Do a dry-run HEAD/GET per member first (verify 200
    AND non-zero bytes) before the real batch. On any required-member fetch/upload/empty-body failure, print
    the member + `sys.exit(1)`. PIL `convert('RGB')` + JPEG re-encode optimize=True (strips EXIF/embedded
    payloads). Print a {external_id, uuid, cdn_url, photo_license} manifest for the audit migration.

    Add a comment block documenting that sahuaritaaz.gov (CivicPlus) is NOT WAF-blocked but its
    /ImageRepository/Document image endpoint SOFT-blocks non-browser clients (HTTP 200 + 0-byte body), so
    source URLs are retrieved via the /find-headshots Playwright flow with a byte-count > 0 check, and
    Ballotpedia/Wikimedia as fallback (do NOT add header-spoofing logic).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-sahuarita-headshots.py"; test -f "$F" && grep -qF "crop_to_4_5" "$F" && grep -qF "external_id=%s" "$F" && grep -qE "len\(ROSTER\)\s*==\s*7" "$F" && grep -qF "sys.exit(1)" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/_tmp-sahuarita-headshots.py` (gitignored `backend/scripts/_*`).
    - Contains crop_to_4_5, resize_600x750, upload_to_storage; TARGET_SIZE (600, 750); LANCZOS; JPEG quality 90; MIN_DIM 100.
    - ROSTER has exactly 7 members (`assert len(ROSTER)==7`), ext_ids -4014001..-4014007.
    - politician_id resolved via parameterized `external_id=%s` (no f-string/`%`-format into SQL); PIL `convert('RGB')` present.
    - `_MISSING`/`TODO_` sentinel + fail-fast `sys.exit(1)` guard present so no placeholder URL reaches Storage; dry-run 200-AND-non-zero-bytes check present; byte-count > 0 guard for the soft-block; is_required=True on all 7.
    - photo_license is a PER-member field (not a single hardcoded uniform value).
    - No WAF/soft-block header-spoofing logic.
  </acceptance_criteria>
  <done>Pipeline script authored: 7-member ROSTER, crop-first 600×750 pipeline, runtime UUID resolution, sentinel + dry-run + byte-count-> 0 + required-failure guards, per-image license.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1355_town_of_sahuarita_headshots.sql</files>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md (§ `1355_town_of_sahuarita_headshots.sql` mapping — the audit-only politician_images pattern from the Marana 1346 analog: header, per-row INSERT ... WHERE NOT EXISTS, exact 5 columns, NO ledger registration, per-image mixed license, final `COMMIT;` only)
    - .planning/phases/197-sahuarita-deep-seed/197-01-SUMMARY.md (the 7 external_id → UUID → full_name manifest)
    - C:/EV-Accounts/backend/migrations/1346_town_of_marana_headshots.sql (analog — read the header + first few rows: audit-only, unregistered, WHERE NOT EXISTS idempotency, 5-column shape, mixed per-image license)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1354 after Plan 01); name this file disk-MAX+1 (provisionally
    `1355_town_of_sahuarita_headshots.sql`). Author an AUDIT-ONLY migration (NO ledger-registration INSERT
    into supabase_migrations, applied via `psql -f`). The header may note it is audit-only / unregistered,
    that the source is sahuaritaaz.gov (soft-blocked image endpoint) retrieved via Playwright, with
    Ballotpedia/Wikimedia fallback (Pitfall 4), and that photo_license VARIES per image.

    Emit 7 per-row INSERTs into essentials.politician_images (id, politician_id, url, type, photo_license)
    — columns EXACTLY these five, no photo_origin_url. Each row: id=gen_random_uuid(); politician_id =
    `(SELECT id FROM essentials.politicians WHERE external_id = -4014001)` (etc. through -4014007); url =
    the CDN path `{CDN_BASE}/{politician_uuid}-headshot.jpg` using the UUID from the Plan 01 manifest;
    type='default'; photo_license = the PER-IMAGE license value (from the Task 1 sourcing manifest). Guard
    each with WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (...same
    subselect...)) — idempotent. Cover all 7 ext_ids -4014001..-4014007. Do NOT add a ledger-registration
    INSERT.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1355_town_of_sahuarita_headshots.sql"; test -f "$F" && [ "$(grep -c 'INSERT INTO essentials.politician_images' "$F")" = "7" ] && ! grep -qF "photo_origin_url" "$F" && ! grep -qE "INSERT INTO supabase_migrations" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1355).
    - Grep count of `INSERT INTO essentials.politician_images` = 7.
    - Grep confirms all 7 ext_ids -4014001..-4014007 referenced.
    - Columns are exactly (id, politician_id, url, type, photo_license) — NO `photo_origin_url`.
    - NO `INSERT INTO supabase_migrations` line (audit-only, unregistered).
    - Every row guarded by `WHERE NOT EXISTS` on politician_id (idempotent).
    - photo_license set per row (may differ across the 7).
  </acceptance_criteria>
  <done>Audit-only migration authored with 7 idempotent politician_images rows bound to Plan 01 UUIDs, per-image license, unregistered.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Resolve source URLs (sahuaritaaz.gov-first + Playwright soft-block fallback) + run pipeline + apply audit migration + assert 7/7 (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator resolves URLs, runs an existing script, applies an existing migration; no files modified</files>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-VALIDATION.md (Per-Task Verification Map — headshots row: count=7 + PIL 600×750 + curl CDN 200; the byte-count check specific to this phase's soft-block)
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Common Pitfall 4 — soft-block 200+0-byte; Playwright fallback; verify byte-count; MIN_DIM floor; per-image license)
    - C:/Users/Chris/.claude/commands/find-headshots.md (the Playwright browser-navigation sourcing flow)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/Storage/web access). BLOCKING apply-and-assert: Plan 02 is not
    complete (and Plan 03 should not proceed) until 7/7 images serve from the CDN. First, for each of the 7
    CONFIRMED sitting officials (Task 2 roster), resolve a usable direct image URL: try the sahuaritaaz.gov
    `/ImageRepository/Document?documentID=N` URL FIRST and verify the retrieved bytes are non-zero (a 0-byte
    200 is the soft block); if empty, use the `/find-headshots` Playwright flow
    (`mcp__playwright__browser_navigate` + snapshot + image-URL extraction) which renders in a real browser
    and retrieves the actual bytes; fall back to Ballotpedia/Wikimedia if needed. Follow the project
    headshot rules (600×750 4:5, Lanczos, crop-only, eyes ~1/3 from top, no graphics on faces, no
    distortion). Do NOT spoof headers to defeat the soft block. Record the resolved URL + license per
    official, fill them into the ROSTER `photo_url` / `photo_license` fields, and finalize the matching
    photo_license values in migration 1355.

    Then run `py -3 C:/EV-Accounts/backend/scripts/_tmp-sahuarita-headshots.py` — it crops/resizes/uploads
    all 7 processed images to Storage and prints the {external_id, uuid, cdn_url, photo_license} manifest.
    If any REQUIRED member cannot be sourced (script exits 1), PAUSE for an operator-supplied file
    (photo_license='operator_supplied'). Then apply the audit migration via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1355_town_of_sahuarita_headshots.sql`.
    Assert: politician_images count for the 7 ext_ids = 7; `curl -sI` each CDN url → HTTP 200 AND non-zero
    Content-Length; PIL verify a sample (include Mayor Murphy and one council member) is exactly 600×750.
    Record results + final manifest (incl. per-image license) in 197-02-SUMMARY.md. Commit the audit
    migration via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (-4014001,-4014002,-4014003,-4014004,-4014005,-4014006,-4014007)"</automated>
  </verify>
  <acceptance_criteria>
    - Each official's source URL resolved (sahuaritaaz.gov-first with a byte-count > 0 check; Playwright soft-block fallback; Ballotpedia/Wikimedia secondary; no header-spoofing); per-image license recorded.
    - Script run exits 0 (all 7 required images fetched with non-zero bytes, processed, uploaded) — or an operator file was supplied for any exception.
    - `politician_images` count for the 7 Sahuarita ext_ids (-4014001..-4014007) = 7.
    - `curl -sI` on each of the 7 CDN URLs returns HTTP 200 with a non-zero Content-Length.
    - PIL dimension check on a sampled downloaded CDN image (incl. Murphy + one council member) = exactly 600×750.
    - Migration 1355's photo_license values match the actual sourced licenses; audit migration NOT registered in the ledger; committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <done>All 7 officials serve a 600×750 CDN headshot bound to the correct current politician row, sourced sahuaritaaz.gov-first (Playwright soft-block fallback) with per-image licenses; audit migration applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| image hosts (remote) → pipeline | Untrusted remote image bytes enter the pipeline over HTTP / via a Playwright browser context |
| pipeline → Storage/DB | Processed image + politician_images row cross into production |

## STRIDE Threat Register

Honest scope: an image-processing pipeline + audit-only INSERTs authored by the operator. Genuine threats
are payload-in-image, wrong-person binding, and a soft-block masquerading as success — not injection or
access control. No HIGH-severity threat; none fabricated. Mirrors the Marana 196 threat model.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-IMG | Tampering | remote image fetch | mitigate | PIL `convert('RGB')` + JPEG re-encode `optimize=True` strips EXIF/embedded/stego payloads; only `image/jpeg` written to Storage |
| T-197-SOFT | Tampering | soft-block 0-byte 200 masquerading as a valid image | mitigate | Byte-count > 0 (and MIN_DIM + PIL-open) guard before any Storage write; Playwright browser context retrieves real bytes when the direct endpoint soft-blocks; grep gate + dry-run check |
| T-197-BIND | Spoofing | politician_images ↔ politician binding | mitigate | Fetch ONLY the Task-2-confirmed current-roster URLs; runtime UUID resolution by external_id prevents wrong-person binding on retry |
| T-197-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; script itself gitignored (`backend/scripts/_*`); never in a committed migration; parameterized `external_id=%s` query |
| T-197-LIC | Repudiation | image license provenance | mitigate | photo_license recorded PER image (municipal portrait/press_use, Wikimedia/CC terms, Ballotpedia press_use, operator_supplied); documented in the migration header + SUMMARY manifest |
| T-197-WRONG | Spoofing | wrong-but-present image | transfer | Row-count cannot detect a wrong-but-present photo; deferred to a human identity spot-check at phase verification |
| T-197-SC | Tampering | package installs | accept | No new pip installs — reuses installed Pillow/requests/psycopg2 + already-available Playwright MCP; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 7/7 politician_images rows for the Sahuarita ext_id block, each 600×750 at HTTP 200 with non-zero bytes.
- Every image binds to the correct current official (Task 2 roster).
- Source URLs sahuaritaaz.gov-first with a byte-count > 0 guard (Playwright soft-block fallback); per-image license recorded; audit migration NOT registered.
</verification>

<success_criteria>
SUB-03 / ROADMAP #2 (all seated officials have 600×750 headshots) is TRUE in production; every image binds
to the correct current member; the audit migration is applied and left unregistered.
</success_criteria>

<output>
Create `.planning/phases/197-sahuarita-deep-seed/197-02-SUMMARY.md` when done — include the final
{external_id → UUID → cdn_url → photo_license} manifest, which officials required the Playwright soft-block
fallback, and any operator-supplied exceptions.
</output>
</content>
