---
phase: 152-west-covina-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1010_west_covina_reconcile.sql
autonomous: true
requirements: [WCOV-01]

must_haves:
  truths:
    - "This phase RECONCILES the EXISTING partial, dual-chamber West Covina seed (geo_id backfill + merge the two duplicate 'City Council' chambers + relabel the wrongly-'At-Large' council rows to 'District 1'..'District 5' + repair two one-directional links) — NOT a greenfield rebuild. West Covina is a CVRA by-district city (Ordinance No. 2310, Jan 17 2017 + Sanchez v. City of West Covina settlement; first district elections Nov 2018): FIVE single-member council districts (D1-D5) + a ROTATIONAL council-selected Mayor (a title on a seat, NOT a separate office). RESEARCH §D-02 Resolution OVERTURNS the CONTEXT.md At-Large default — the 5 At-Large labels are WRONG. A Wave-1 STOP-on-drift pre-flight re-confirms gov 1982a9fa geo_id NULL/empty, BOTH chamber UUIDs (12c9360a official_count 5 / 3 offices bidirectional-clean + b1a2c4cb official_count NULL / 2 offices one-directional), the 5 offices + members/ext_ids, BOTH link directions, the office->district map (whether the 5 At-Large offices share one district_id or have distinct rows — Pomona/Torrance shared-district defect check), the live schema_migrations MAX + on-disk MAX, and STOPs on drift"
    - "The West Covina government row 'City of West Covina, California, US' (id 1982a9fa-dc56-482d-83fc-27bf69458b22) carries geo_id '0684200' (no longer NULL/empty), guarded WHERE (geo_id IS NULL OR geo_id = ''); state already 'CA'. The same-name West Covina Unified School District gov 131e33d0 is OUT OF SCOPE and NEVER touched"
    - "move-then-delete (UUID-targeted): the two offices in the doomed chamber b1a2c4cb — Diaz -201107 (office abd27abb) + Gutierrez -201108 (office 0f3cce5f) — are MOVED into the survivor chamber 12c9360a (the cleaner bidirectional chamber, official_count 5) FIRST (UPDATE chamber_id WHERE chamber_id='b1a2c4cb…'), an inline assert proves b1a2c4cb then has 0 offices, THEN b1a2c4cb is DELETEd (target by UUID — both chambers share name 'City Council' / slug west-covina-city-council) — end state is ONE 'City Council' chamber (12c9360a) with exactly 5 offices"
    - "repair the two one-directional links (CONTEXT D-01b): the moved offices abd27abb (Diaz, offices.politician_id f5bf4ec4) + 0f3cce5f (Gutierrez, offices.politician_id 22fc2cdc) have politicians.office_id NULL — set politicians.office_id to match their offices.politician_id so all 5 survivor seats are bidirectional (the other 3 — Lopez-Viado/Cantos/Wu — are already bidirectional-clean)"
    - "by-district relabel (Palmdale 146 / Pasadena 149 / El Monte 151 pattern): the wrongly-'At-Large' council LOCAL district rows are relabeled to their occupant's RESEARCH-confirmed district — D1 Gutierrez (22fc2cdc, office 0f3cce5f), D2 Lopez-Viado (2872d7a4, office 4a8f2fd6), D3 Diaz (f5bf4ec4, office abd27abb), D4 Cantos (ecc57cd4, office 50471af9), D5 Wu (1bb5c062, office 65bf4e71) — resolved via the pre-flight office->district map (do NOT assume UUID order). If the 5 offices SHARE one At-Large district_id (Pomona/Torrance defect), create 5 distinct 'District N' rows (gen_random_uuid(), LOCAL, geo_id '0684200', state 'CA') and repoint each office; if they already have distinct rows, relabel in place. Keeping geo_id '0684200', state 'CA'"
    - "NO directly-elected Mayor office is created (RESEARCH §D-02 / §Anti-Patterns): West Covina's mayor is ROTATIONAL — the DB's lack of a Mayor office is CORRECT. Do NOT create a LOCAL_EXEC row. After this wave exactly 5 LOCAL district rows ('District 1'..'District 5') and ZERO LOCAL_EXEC rows exist under gov 1982a9fa (the Mayor/Mayor-Pro-Tem TITLES are set on the D2/D4 seats in Plan 02)"
    - "feedback_section_split_check SQL returns 0 rows for West Covina (geo_id '0684200') after consolidation (West Covina is NOT in the project_split_section_defects_5_cities set)"
    - "this reconcile (geo_id backfill + chamber merge + link repair + district relabel) is a STRUCTURAL migration (1010) that registers in supabase_migrations.schema_migrations normally; the migration file is COMMITTED to the EV-Accounts repo (feedback_no_git_in_ev_accounts)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1010_west_covina_reconcile.sql"
      provides: "Idempotent geo_id backfill + move-Diaz/Gutierrez-then-delete-chamber-b1a2c4cb + repair 2 one-directional links + relabel At-Large council rows to District 1-5 (split shared district_id if present)"
      contains: "b1a2c4cb"
  key_links:
    - from: "essentials.governments (id 1982a9fa-dc56-482d-83fc-27bf69458b22)"
      to: "geo_id '0684200'"
      via: "UPDATE ... WHERE id = '1982a9fa-dc56-482d-83fc-27bf69458b22' AND (geo_id IS NULL OR geo_id = '')"
      pattern: "geo_id\\s*=\\s*'0684200'"
    - from: "essentials.offices (Diaz abd27abb + Gutierrez 0f3cce5f, the doomed-chamber offices)"
      to: "survivor chamber 12c9360a"
      via: "UPDATE offices SET chamber_id='12c9360a...' WHERE chamber_id='b1a2c4cb...'"
      pattern: "chamber_id\\s*=\\s*'12c9360a"
    - from: "essentials.chambers (b1a2c4cb, emptied duplicate)"
      to: "deleted (asserted 0 offices first)"
      via: "DO $$ assert 0 offices $$ then DELETE FROM essentials.chambers WHERE id = 'b1a2c4cb...'"
      pattern: "DELETE FROM essentials\\.chambers"
    - from: "essentials.politicians (Diaz f5bf4ec4 + Gutierrez 22fc2cdc, one-directional links)"
      to: "their offices (back-pointer repaired)"
      via: "UPDATE politicians SET office_id=<office> WHERE id IN ('f5bf4ec4...','22fc2cdc...') AND office_id IS DISTINCT FROM <office>"
      pattern: "office_id\\s*=\\s*'abd27abb"
---

<objective>
Reconcile the existing partial, duplicate-chamber West Covina seed in a single idempotent structural migration
(1010). This is the foundation wave — every later wave (roster relabel/titles, headshots, stances) assumes a
clean, single-chamber, correctly-districted structure with consistent bidirectional links.

⚠️ RECONCILE, NOT GREENFIELD (DB-verified 2026-06-21 in CONTEXT.md §DB Pre-Check). The West Covina gov already
exists with geo_id NULL, TWO duplicate 'City Council' chambers (both slug west-covina-city-council), and 5
council offices (all mislabeled 'At-Large', no separate Mayor office). This wave (1) backfills geo_id, (2) merges
the duplicate chamber via move-then-delete, (3) repairs the two one-directional links, and (4) fixes the council
form of government from (wrong) At-Large to West Covina's real by-district structure (relabel D1-D5). The
Mayor/Mayor-Pro-Tem TITLES, official_count, and any roster verification are Plan 02 — this wave is STRUCTURE ONLY.

⚠️ WEST COVINA IS BY-DISTRICT WITH A ROTATIONAL MAYOR (RESEARCH §D-02 Resolution — OVERTURNS the CONTEXT.md
At-Large default). RESEARCH verified against westcovina.gov/172 + /177 + Ordinance No. 2310 (Jan 17 2017) +
Sanchez v. City of West Covina (CVRA settlement) + AB-1979: the council is FIVE by-district seats (D1-D5) and the
Mayor is ROTATIONAL (council members annually select one of their own — a title on a seat, NOT a separate
office). This is the Palmdale (146) / Pasadena (149) / El Monte (151) by-district relabel pattern for the council
seats AND the Palmdale (146) / Glendale (144) rotational-mayor-as-title model — NOT the El Monte (151) /
Lancaster (145) directly-elected-LOCAL_EXEC-Mayor model. Therefore:
- The 5 wrongly-'At-Large' council rows are relabeled to D1-D5 per the RESEARCH §D-02/§D-03 verified per-person
  map (D1 Gutierrez, D2 Lopez-Viado, D3 Diaz, D4 Cantos, D5 Wu).
- NO LOCAL_EXEC Mayor office is created — the DB's lack of a Mayor office is CORRECT (the Mayor title is set on
  a seat in Plan 02). (Anti-Pattern per RESEARCH §Anti-Patterns to Avoid: do NOT add a Mayor office.)

Four structural fixes, all DB-verified against the live state on 2026-06-21:
1. Backfill essentials.governments.geo_id = '0684200' on gov 1982a9fa-dc56-482d-83fc-27bf69458b22 (currently
   NULL), guarded WHERE (geo_id IS NULL OR geo_id = ''). state already 'CA'.
2. MERGE the duplicate chamber: MOVE both b1a2c4cb offices (Diaz abd27abb + Gutierrez 0f3cce5f) into the survivor
   chamber 12c9360a (the cleaner bidirectional chamber, official_count 5) FIRST (UPDATE chamber_id WHERE
   chamber_id='b1a2c4cb…'), then an inline assert confirms b1a2c4cb now has 0 offices, then DELETE the chamber
   b1a2c4cb. Target both chambers/offices by UUID ONLY — both share the name 'City Council'. End state: ONE
   chamber (12c9360a) with 5 offices.
3. REPAIR the two one-directional links (CONTEXT D-01b): the moved offices abd27abb (Diaz, offices.politician_id
   f5bf4ec4) and 0f3cce5f (Gutierrez, offices.politician_id 22fc2cdc) have politicians.office_id NULL — set
   politicians.office_id to match offices.politician_id so all 5 seats are bidirectional.
4. RELABEL the wrongly-'At-Large' council district rows to their occupant's verified district per the RESEARCH
   §D-02/§D-03 map (D1 Gutierrez, D2 Lopez-Viado, D3 Diaz, D4 Cantos, D5 Wu), resolving each At-Large UUID from
   the pre-flight office->district map. If the 5 offices SHARE one At-Large district_id (Pomona/Torrance defect),
   create 5 distinct 'District N' rows (gen_random_uuid()) and repoint each office; if distinct rows exist,
   relabel in place. Keep geo_id '0684200', state 'CA'. Do NOT create any LOCAL_EXEC Mayor row.

Purpose: A clean structural base. Duplicate 'City Council' chamber names cause split-section render bugs; NULL
geo_id breaks routing; the wrong At-Large labels misrepresent West Covina's real by-district form of government;
the one-directional links break roster integrity.
Output: Migration 1010 applied to production AND committed to the EV-Accounts repo; all post-verification queries
green; exactly 5 LOCAL district rows D1-D5 + ZERO LOCAL_EXEC rows; split-section check 0 rows.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally (NOT a
stance/headshot audit-only migration). On-disk file counter is authoritative; next structural is 1010 (RESEARCH
§D-01: on-disk MAX 1009 = 1009_cindy_galvan_stances.sql; live schema_migrations MAX = 999; pre-flight MUST
re-confirm BOTH the on-disk MAX and the live schema_migrations MAX before committing to 1010, since other
workstreams may have advanced the counter).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-west-covina-deep-seed/152-RESEARCH.md
@.planning/phases/152-west-covina-deep-seed/152-CONTEXT.md
@.planning/phases/152-west-covina-deep-seed/152-VALIDATION.md
@.planning/phases/151-el-monte-deep-seed/151-01-PLAN.md
@.planning/phases/146-palmdale-deep-seed/146-02-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- DB-verified live values (2026-06-21, CONTEXT.md §DB Pre-Check + RESEARCH §D-02 Resolution / §D-02/§D-03).
     Task 1 re-confirms them read-only before any write and STOPs on drift. -->

Government row:
  id     = 1982a9fa-dc56-482d-83fc-27bf69458b22   name = 'City of West Covina, California, US'
  geo_id = NULL  -> set to '0684200'   (state already 'CA')
  (OUT OF SCOPE same-name gov: 131e33d0-e40a-4aaf-bc8d-c665750d1b6d = West Covina Unified School District —
   NEVER touch it.)

Two chambers, BOTH named 'City Council' / slug 'west-covina-city-council' under gov 1982a9fa (MERGE into one):
  KEEP   12c9360a-60ac-476f-b2ac-055a26e891a0  (SURVIVOR — official_count 5, 3 offices, bidirectional-clean)
  DELETE b1a2c4cb-25b6-46c8-a3ab-852024e00f45  (DOOMED — official_count NULL, 2 offices: Diaz + Gutierrez, one-directional)
  (chambers.slug is a GENERATED column — NEVER include slug in any chamber write)
  (CONTEXT Discretion / RESEARCH §D-02/§D-03 survivor choice: 12c9360a is the cleaner bidirectional chamber — keep it.)

Survivor 12c9360a offices (3; bidirectional-clean; relabel their council districts):
  4a8f2fd6  Letty Lopez-Viado  (ext_id 687361, pol 2872d7a4, At-Large LOCAL -> relabel district 'District 2'; Mayor — title is Plan 02)
  50471af9  Ollie Cantos       (ext_id 687365, pol ecc57cd4, At-Large LOCAL -> relabel district 'District 4'; Mayor Pro Tem — title is Plan 02)
  65bf4e71  Tony Wu            (ext_id 687367, pol 1bb5c062, At-Large LOCAL -> relabel district 'District 5')

Doomed b1a2c4cb offices (MOVE both into survivor, repair back-pointers, then delete the empty chamber):
  abd27abb  Rosario Diaz       (ext_id -201107, pol f5bf4ec4, At-Large LOCAL -> relabel district 'District 3'; politicians.office_id NULL -> repair)
  0f3cce5f  Brian Gutierrez    (ext_id -201108, pol 22fc2cdc, At-Large LOCAL -> relabel district 'District 1'; politicians.office_id NULL -> repair)

Total = 5 offices (all council, all At-Large LOCAL). NO Mayor / LOCAL_EXEC office (correct — rotational mayor).
ALL 5 members are CURRENT (RESEARCH §D-02/§D-03 — no departures, no missing member, NO new politician this phase).

Move-then-delete (El Monte 1000 / Pasadena 946 / Palmdale 919 pattern — UUID-targeted, 2 offices):
  Step order: (1) move Diaz abd27abb + Gutierrez 0f3cce5f from b1a2c4cb to 12c9360a FIRST, (2) assert b1a2c4cb
  empty, (3) delete b1a2c4cb, (4) repair the 2 back-pointers, (5) THEN do the district relabel.
  1. UPDATE essentials.offices SET chamber_id='12c9360a-60ac-476f-b2ac-055a26e891a0'
     WHERE chamber_id='b1a2c4cb-25b6-46c8-a3ab-852024e00f45'   (moves Diaz + Gutierrez into the survivor)
  2. DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='b1a2c4cb-...') > 0
     THEN RAISE EXCEPTION 'Chamber b1a2c4cb still has offices'; END IF; END $$;
  3. DELETE FROM essentials.chambers WHERE id='b1a2c4cb-25b6-46c8-a3ab-852024e00f45'

Back-pointer repair (CONTEXT D-01b — the two one-directional links; the other 3 are already clean):
  UPDATE essentials.politicians SET office_id='abd27abb-...' WHERE id='f5bf4ec4-...' AND office_id IS DISTINCT FROM 'abd27abb-...'  (Diaz)
  UPDATE essentials.politicians SET office_id='0f3cce5f-...' WHERE id='22fc2cdc-...' AND office_id IS DISTINCT FROM '0f3cce5f-...'  (Gutierrez)
  (resolve the exact office UUIDs from the Task 1 office->district map — do NOT assume.)

District relabel (relabel label ONLY; geo_id/state stay as-is). The MANDATORY pre-flight office->district map
  (RESEARCH §Pitfall 2 / Open Q1 query over BOTH chambers) maps each office to its current district_id + label +
  district_type — use THOSE UUIDs (do NOT assume order):
  SELECT o.id AS office_id, p.external_id, p.last_name, o.title, d.id AS district_id, d.label, d.district_type
  FROM essentials.offices o
  LEFT JOIN essentials.politicians p ON p.id = o.politician_id
  LEFT JOIN essentials.districts d ON d.id = o.district_id
  WHERE o.chamber_id IN ('12c9360a-...','b1a2c4cb-...')
  ORDER BY d.district_type, d.label;
  Occupant -> district relabel map (RESEARCH §D-02/§D-03 — authoritative, verified against westcovina.gov/177):
    Gutierrez    (office 0f3cce5f, -201108) -> 'District 1'
    Lopez-Viado  (office 4a8f2fd6, 687361)  -> 'District 2'  (Mayor — title is Plan 02)
    Diaz         (office abd27abb, -201107) -> 'District 3'
    Cantos       (office 50471af9, 687365)  -> 'District 4'  (Mayor Pro Tem — title is Plan 02)
    Wu           (office 65bf4e71, 687367)  -> 'District 5'
  Each relabel guarded label IS DISTINCT FROM target.

  ⚠ SHARED-DISTRICT DEFECT (RESEARCH §Pitfall 2 / Open Q1 / Assumption A1 — Pomona 147 / Torrance 148): if the
  pre-flight JOIN shows 2+ offices pointing at the SAME district_id row, relabeling that one row would mislabel
  multiple seats. In that case, for each affected office create a NEW distinct district row
  (INSERT essentials.districts (id, label, district_type, geo_id, state) VALUES (gen_random_uuid(), 'District N',
  'LOCAL', '0684200', 'CA')) and UPDATE essentials.offices SET district_id=<new row> WHERE id=<office> (repoint),
  leaving each office on its OWN District N row. If the pre-flight shows 5 distinct rows already, relabel in place.

NO Mayor office (RESEARCH §Anti-Patterns / Palmdale 146 / Glendale 144 rotational model):
  Do NOT create a LOCAL_EXEC district or a separate Mayor office. The Mayor (Lopez-Viado) + Mayor Pro Tem (Cantos)
  are TITLES set on the D2/D4 seats in Plan 02. After this wave: exactly 5 LOCAL district rows, ZERO LOCAL_EXEC.

Schema notes (CRITICAL — CA convention, LOCATION-ONBOARDING §California Quick Reference):
  - chambers.slug is GENERATED — NEVER include slug in any chamber write
  - district_type + label live on essentials.districts, NOT on offices (no offices.district_type column)
  - districts.state is uppercase 'CA'; districts label column is `label` not `name`
  - West Covina's mayor is ROTATIONAL (Palmdale/Glendale model) — do NOT create a LOCAL_EXEC Mayor row
    (RESEARCH §Anti-Patterns); the DB's lack of a Mayor office is CORRECT
  - geo_id guard MUST include the empty-string case: WHERE (geo_id IS NULL OR geo_id = '')
  - This wave does NOT set official_count, set Mayor/Mayor-Pro-Tem titles, verify the live roster against
    westcovina.gov/177, or create any politician (all Plan 02)

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after consolidation;
  expect 0 rows for West Covina geo_id '0684200' (West Covina is NOT in the project_split_section_defects_5_cities
  set).

Pattern migrations to copy:
  C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql (the most recent dual-chamber-merge + At-Large->
    District relabel + idempotent guards + post-verification block + schema_migrations register — same defect
    shape as West Covina, minus the LOCAL_EXEC Mayor).
  C:/EV-Accounts/backend/migrations/919_palmdale_complete.sql (the rotational-mayor-as-title by-district pattern +
    bidirectional back-pointer repair shape; verify exact filename at apply time).

Migration ledger (RESEARCH §D-01): on-disk MAX = 1009 (1009_cindy_galvan_stances.sql); next STRUCTURAL is 1010.
  Pre-flight MUST re-confirm BOTH the on-disk MAX (ls of C:/EV-Accounts/backend/migrations/) AND the live
  SELECT MAX(version::int) FROM supabase_migrations.schema_migrations before numbering — other workstreams may
  have advanced the counter overnight. If the on-disk MAX is no longer 1009, number 1010_west_covina_reconcile.sql
  to the next free slot and STOP-report the drift.

EV-Accounts is a git repo on master (feedback_no_git_in_ev_accounts) — the migration FILE must be committed via
  `git -C "C:/EV-Accounts"` after the DB apply (always operate from the repo root via git -C, never cd into
  /backend).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state (gov, two chambers, both link directions, the office->district map incl. shared-district check, the current roster, migration counters) — STOP on drift</name>
  <files>C:/EV-Accounts/backend/migrations/1010_west_covina_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql (the DIRECT dual-chamber-merge + At-Large->District relabel + idempotent guards + post-verification block + schema_migrations register STRUCTURAL template — the same shape as West Covina, minus the LOCAL_EXEC Mayor)
    - C:/EV-Accounts/backend/migrations/919_palmdale_complete.sql (the rotational-mayor-as-title by-district + bidirectional back-pointer repair shape)
    - .planning/phases/151-el-monte-deep-seed/151-01-PLAN.md (the dual-chamber-merge + by-district relabel + STOP-on-drift pre-flight precedent this plan mirrors)
    - .planning/phases/146-palmdale-deep-seed/146-02-PLAN.md (the by-district relabel + rotational-mayor-as-title + shared-district-split precedent)
    - .planning/phases/152-west-covina-deep-seed/152-RESEARCH.md (§D-02 Resolution BY-DISTRICT verdict, §D-02/§D-03 roster + office-to-district map + shared-district pitfall, §D-01 Migration Ledger, §Open Questions 1, §Assumptions A1/A3/A4, §Validation Architecture WCOV-01a..01k, §Anti-Patterns)
    - .planning/phases/152-west-covina-deep-seed/152-CONTEXT.md (LOCKED decisions D-01/D-01b/D-02/D-03 + §DB Pre-Check live values)
    - .planning/phases/152-west-covina-deep-seed/152-VALIDATION.md (the per-wave verification SELECT contract — 152-01 reconcile row)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA', districts label column, district_type on districts)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE/INSERT, run read-only confirmation queries via mcp__supabase-local
    execute_sql (= production). Confirm EACH against the LOCKED CONTEXT §DB Pre-Check + RESEARCH §D-02/§D-03:
    (a) gov: SELECT id, geo_id, state, name FROM essentials.governments WHERE
    id='1982a9fa-dc56-482d-83fc-27bf69458b22' — confirm geo_id NULL/empty, state 'CA', name 'City of West Covina,
    California, US'; confirm the OUT-OF-SCOPE West Covina Unified gov 131e33d0 is a DIFFERENT id (do NOT touch it);
    (b) two chambers under that gov both named 'City Council' / slug 'west-covina-city-council' — 12c9360a
    (SURVIVOR, official_count 5, 3 offices, bidirectional-clean) and b1a2c4cb (DOOMED, official_count NULL, 2
    offices, one-directional); confirm NO third chamber;
    (c) survivor 12c9360a has exactly 3 offices occupied via offices.politician_id: 4a8f2fd6 (Lopez-Viado 687361,
    pol 2872d7a4), 50471af9 (Cantos 687365, pol ecc57cd4), 65bf4e71 (Wu 687367, pol 1bb5c062) — and confirm all 3
    have politicians.office_id set (bidirectional);
    (d) doomed b1a2c4cb has exactly 2 offices: abd27abb (Diaz -201107, pol f5bf4ec4) + 0f3cce5f (Gutierrez
    -201108, pol 22fc2cdc) — the MOVE targets — and confirm BOTH have offices.politician_id set but
    politicians.office_id NULL (the one-directional defect to repair, CONTEXT D-01b);
    (e) run the MANDATORY office->district map (RESEARCH §Pitfall 2 / Open Q1 query over BOTH chambers) — record
    each office's current district_id + label + district_type + title; confirm all 5 are 'At-Large' LOCAL; and
    ⚠ determine whether the 5 offices share ONE district_id row or each have a DISTINCT row (the Pomona 147 /
    Torrance 148 shared-district defect — Assumption A1 / Open Q1): record the exact distinct district_id count;
    (f) confirm there is NO LOCAL_EXEC district row under gov 1982a9fa (West Covina has no separate Mayor office —
    correct; RESEARCH §D-02);
    (g) confirm NO 'District 1'..'District 5' row already exists for geo_id 0684200 (idempotency check);
    (h) re-confirm the CURRENT by-district roster live against westcovina.gov/177 (RESEARCH §D-02/§D-03 / Assumption
    A3): D1 Gutierrez / D2 Lopez-Viado (Mayor) / D3 Diaz / D4 Cantos (Mayor Pro Tem) / D5 Wu — all 5 current, no
    departures, no missing member.
    Also confirm the migration ledger: SELECT MAX(version::int) FROM supabase_migrations.schema_migrations AND
    the on-disk MAX file in C:/EV-Accounts/backend/migrations/ (expect 1009 = 1009_cindy_galvan_stances.sql) —
    next structural is 1010. If ANY precondition does not match (a third chamber, b1a2c4cb has !=2 offices,
    12c9360a has !=3 offices, an office occupied by a different ext_id than locked, a LOCAL_EXEC row present, the
    on-disk MAX not 1009, or the current roster differs from RESEARCH §D-02/§D-03), STOP and report the
    discrepancy — do NOT write a migration against a moved target. Record the resolved district UUIDs, the
    shared-vs-distinct district finding, the two office UUIDs for the back-pointer repair, and the confirmed
    migration numbers in the SUMMARY for Plan 02.
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql "SELECT geo_id, state FROM essentials.governments WHERE id='1982a9fa-dc56-482d-83fc-27bf69458b22';" returns one row, geo_id NULL or '', state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - Gov 1982a9fa has geo_id NULL/empty and state 'CA' (pre-state); confirmed name 'City of West Covina, California, US'; the West Covina Unified gov 131e33d0 confirmed a DIFFERENT id (untouched)
    - Two chambers confirmed sharing name 'City Council': 12c9360a (KEEP, 3 offices, official_count 5, bidirectional-clean) and b1a2c4cb (DELETE, 2 offices, official_count NULL, one-directional); no third chamber
    - Survivor 12c9360a offices confirmed OCCUPIED + bidirectional via offices.politician_id/politicians.office_id (4a8f2fd6=Lopez-Viado 687361, 50471af9=Cantos 687365, 65bf4e71=Wu 687367)
    - Doomed b1a2c4cb confirmed to have exactly 2 offices: abd27abb (Diaz -201107) + 0f3cce5f (Gutierrez -201108) — the move targets — with offices.politician_id set but politicians.office_id NULL (the repair targets, CONTEXT D-01b)
    - Office->district map run; all 5 confirmed 'At-Large' LOCAL; the shared-vs-distinct district_id finding recorded (Pomona/Torrance defect check — Open Q1 / A1); any shared row surfaced for the create+repoint resolution
    - NO LOCAL_EXEC district row under gov 1982a9fa (no separate Mayor office — correct, RESEARCH §D-02); no pre-existing 'District 1'..'District 5' row for geo_id 0684200
    - Ledger confirmed: on-disk MAX 1009, live schema_migrations MAX recorded, next structural 1010
    - Current by-district roster re-confirmed live against westcovina.gov/177 = D1 Gutierrez / D2 Lopez-Viado / D3 Diaz / D4 Cantos / D5 Wu (all 5 current); STOP-and-report if changed
    - Any mismatch vs CONTEXT §DB Pre-Check / RESEARCH §D-02/§D-03 is surfaced and execution paused (STOP-on-drift; no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches CONTEXT §DB Pre-Check + RESEARCH §D-02/§D-03; gov UUID, the two move-target offices, both repair-target back-pointers, the office->district map (incl. shared-vs-distinct finding), and the migration numbers (1010 next) are all confirmed; the geo_id backfill, chamber merge, link repair, and district relabel are safe to author.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 1010 (geo_id backfill + merge duplicate chamber + repair 2 one-directional links + relabel At-Large->District 1-5, splitting shared district_id if present), then COMMIT the file to EV-Accounts</name>
  <files>C:/EV-Accounts/backend/migrations/1010_west_covina_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql (copy the idempotent geo_id backfill + office move + FK-safe chamber DELETE + inline-assert + At-Large->District relabel + the schema_migrations register + post-verification block)
    - C:/EV-Accounts/backend/migrations/919_palmdale_complete.sql (the bidirectional back-pointer repair shape + by-district handling)
    - .planning/phases/151-el-monte-deep-seed/151-01-PLAN.md (the exact authoring + post-verification query set this plan mirrors — minus the LOCAL_EXEC Mayor + create-District-6)
    - .planning/phases/146-palmdale-deep-seed/146-02-PLAN.md (the shared-district create+repoint + rotational-mayor-no-LOCAL_EXEC discipline)
    - .planning/phases/152-west-covina-deep-seed/152-RESEARCH.md (§Architecture Patterns 1+2 SQL templates, §D-02/§D-03 relabel map, §Pitfall 2 shared-district, §Anti-Patterns, §Validation Architecture WCOV-01a..01g/01k)
    - .planning/phases/152-west-covina-deep-seed/152-CONTEXT.md (LOCKED D-01/D-01b/D-02/D-03 + the EV-Accounts commit requirement)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA')
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1010_west_covina_reconcile.sql wrapped in BEGIN/COMMIT, all statements
    idempotent, in this order (mirror 1000 — do NOT inline full SQL here; copy the templates' shapes):
    (1) UPDATE essentials.governments SET geo_id='0684200' WHERE
        id='1982a9fa-dc56-482d-83fc-27bf69458b22' AND (geo_id IS NULL OR geo_id = '') (WCOV-01a, empty-string guard).
    (2) MERGE — MOVE both doomed-chamber offices into the survivor FIRST: UPDATE essentials.offices SET
        chamber_id='12c9360a-60ac-476f-b2ac-055a26e891a0' WHERE
        chamber_id='b1a2c4cb-25b6-46c8-a3ab-852024e00f45' (carries Diaz abd27abb + Gutierrez 0f3cce5f; target by
        the doomed UUID).
    (3) Inline assert the doomed chamber is now empty: a DO $$ block that RAISEs EXCEPTION if
        (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='b1a2c4cb-25b6-46c8-a3ab-852024e00f45') > 0.
    (4) DELETE essentials.chambers WHERE id='b1a2c4cb-25b6-46c8-a3ab-852024e00f45'. Target by UUID ONLY, never by
        name/slug (both chambers share 'City Council' / west-covina-city-council).
    (5) REPAIR the two one-directional links (CONTEXT D-01b): UPDATE essentials.politicians SET office_id=
        '<Diaz office abd27abb>' WHERE id='f5bf4ec4-...' AND office_id IS DISTINCT FROM '<abd27abb>'; and
        UPDATE essentials.politicians SET office_id='<Gutierrez office 0f3cce5f>' WHERE id='22fc2cdc-...' AND
        office_id IS DISTINCT FROM '<0f3cce5f>'. (The other 3 — Lopez-Viado/Cantos/Wu — are already bidirectional;
        leave them.) Use the office UUIDs resolved by the Task 1 office->district map.
    (6) RELABEL the wrongly-'At-Large' council district rows to their occupant's verified district, each guarded
        label IS DISTINCT FROM target, using the At-Large UUIDs resolved by the Task 1 office->district map:
        'District 1' (Gutierrez office 0f3cce5f), 'District 2' (Lopez-Viado office 4a8f2fd6), 'District 3' (Diaz
        office abd27abb), 'District 4' (Cantos office 50471af9), 'District 5' (Wu office 65bf4e71). Do NOT alter
        geo_id or state.
        ⚠ If Task 1 found a SHARED-district defect (2+ offices on one district_id — Pomona 147 / Torrance 148),
        do NOT relabel the shared row; instead create 5 distinct rows: INSERT essentials.districts (id, label,
        district_type, geo_id, state) VALUES (gen_random_uuid(), 'District N', 'LOCAL', '0684200', 'CA') for each
        and UPDATE essentials.offices SET district_id=<new row> WHERE id=<office> to repoint each office to its OWN
        District N row (guarded so a re-run does not create duplicate rows). If 5 distinct rows already exist,
        relabel in place.
    Do NOT touch the slug column anywhere (GENERATED). Do NOT write to offices.district_type (no such column).
    Do NOT create any LOCAL_EXEC Mayor row, do NOT set official_count, do NOT set Mayor/Mayor-Pro-Tem titles, do
    NOT create or seat any politician (all Plan 02). West Covina's mayor is rotational — no Mayor office is created.
    End the file with post-verification queries (mirror 1000): geo_id check + a chamber listing under gov 1982a9fa
    (expect exactly one 'City Council', the survivor 12c9360a) + a count of offices under 12c9360a (expect 5) +
    a confirm b1a2c4cb is gone + a confirm all 5 survivor offices are bidirectional (politicians.office_id matches
    offices.politician_id for all 5) + the district labels for geo_id 0684200 ('District 1'..'District 5' LOCAL,
    exactly 5 LOCAL, ZERO LOCAL_EXEC) + each office on a DISTINCT district_id (no shared row remains) + the
    feedback_section_split_check SQL (expect 0 rows for West Covina). Apply via mcp__supabase-local execute_sql,
    and register migration 1010 in supabase_migrations.schema_migrations (structural — INSERT ... ON CONFLICT
    (version) DO NOTHING, per 1000). Record the relabeled/created district UUIDs + the repaired link UUIDs in the
    SUMMARY for Plan 02. Then COMMIT the migration file to the EV-Accounts repo: git -C "C:/EV-Accounts" add
    backend/migrations/1010_west_covina_reconcile.sql && git -C "C:/EV-Accounts" commit -m "feat(152): West Covina
    reconcile — geo_id backfill + chamber merge + bidirectional link repair + by-district relabel D1-D5" (per
    feedback_no_git_in_ev_accounts; always operate from the repo root via git -C, never cd into /backend).
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql checks ALL of: SELECT geo_id FROM essentials.governments WHERE id='1982a9fa-dc56-482d-83fc-27bf69458b22' returns '0684200'; SELECT COUNT(*) FROM essentials.chambers WHERE government_id='1982a9fa-dc56-482d-83fc-27bf69458b22' AND name='City Council' returns 1; SELECT COUNT(*) FROM essentials.chambers WHERE id='b1a2c4cb-25b6-46c8-a3ab-852024e00f45' returns 0; SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='12c9360a-60ac-476f-b2ac-055a26e891a0' returns 5; SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.id=p.office_id AND o.politician_id=p.id WHERE o.chamber_id='12c9360a-60ac-476f-b2ac-055a26e891a0' returns 5 (all 5 bidirectional); SELECT COUNT(DISTINCT o.district_id) FROM essentials.offices o WHERE o.chamber_id='12c9360a-60ac-476f-b2ac-055a26e891a0' returns 5 (distinct district per seat); SELECT COUNT(*) FROM essentials.districts WHERE geo_id='0684200' AND district_type='LOCAL_EXEC' returns 0 (no Mayor office). Also: git -C "C:/EV-Accounts" log --oneline -1 -- backend/migrations/1010_west_covina_reconcile.sql shows the commit</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='1982a9fa-...'` -> '0684200'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='1982a9fa-...' AND name='City Council'` -> 1 (not 0, not 2); b1a2c4cb deleted
    - `SELECT COUNT(*) FROM essentials.chambers WHERE id='b1a2c4cb-25b6-46c8-a3ab-852024e00f45'` -> 0 (doomed deleted)
    - `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='12c9360a-60ac-476f-b2ac-055a26e891a0'` -> 5 (Diaz + Gutierrez moved in)
    - All 5 survivor offices bidirectional: Diaz (f5bf4ec4) + Gutierrez (22fc2cdc) back-pointers repaired (politicians.office_id matches offices.politician_id); the other 3 already clean
    - District relabel: rows for geo_id '0684200' = 'District 1'..'District 5' (all LOCAL) per the verified map (D1 Gutierrez, D2 Lopez-Viado, D3 Diaz, D4 Cantos, D5 Wu); each still geo_id '0684200', state 'CA'; each office on a DISTINCT district_id (shared-district defect split if it was present)
    - ZERO LOCAL_EXEC district rows under gov 1982a9fa (no Mayor office created — rotational mayor; correct)
    - No politician created/seated; official_count NOT set; Mayor/Mayor-Pro-Tem titles NOT set (all Plan 02)
    - feedback_section_split_check SQL returns 0 rows for West Covina (geo_id '0684200')
    - Migration 1010 registered in supabase_migrations.schema_migrations (structural)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
    - Migration file 1010_west_covina_reconcile.sql committed to the EV-Accounts repo (git -C "C:/EV-Accounts")
  </acceptance_criteria>
  <done>Migration 1010 applied + registered + committed to EV-Accounts; one 'City Council' chamber (12c9360a) remains with 5 bidirectionally-linked offices; geo_id 0684200 set; the two one-directional links repaired; council districts relabeled D1-D5 (shared row split if present, each office on a distinct district); NO Mayor office created (rotational); split-section check 0 rows; re-running the file is a no-op; relabeled/created district UUIDs + repaired link UUIDs recorded for Plan 02.</done>
</task>

</tasks>

<threat_model>
DB-seeding phase with NO new external attack surface. No untrusted user input crosses any boundary — migration
SQL is applied via service-role/operator (MCP) directly against the internal production Supabase DB. The real
threat surface is accidental data corruption — deleting the doomed chamber before moving Diaz/Gutierrez (orphaned
offices), relabeling the wrong district row (multiple similar At-Large UUIDs, or a SHARED At-Large district_id
that mislabels multiple seats), creating a LOCAL_EXEC Mayor office that does not belong (West Covina's mayor is
rotational), leaving the two one-directional links unrepaired, missing the empty-string geo_id case, writing to a
non-existent offices.district_type column, or touching the wrong gov (the West Covina Unified School District
131e33d0) — mitigated by move-FIRST ordering, an inline empty-chamber assert, UUID-scoped targeting of every
write, relabel-only/IS-DISTINCT-FROM guards, the shared-district create+repoint split, the NO-Mayor-office rule,
the bidirectional-repair guards, the empty-string geo_id guard, and a STOP-on-drift pre-flight that checks BOTH
link directions, the office->district map (incl. shared-vs-distinct), and the current roster.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> production DB | Migration SQL applied by a trusted operator (MCP) directly against production Supabase. No untrusted user input crosses here. |
| official source (westcovina.gov) -> roster confirmation | Live re-confirmation of the current 5 district occupants before any write; read-only here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01 | Tampering | Idempotent guarded writes — geo_id overwritten on re-run / wrong gov / empty-string case missed; chamber DELETE targets wrong chamber (both share name 'City Council' / slug west-covina-city-council) | mitigate | geo_id UPDATE keyed on id 1982a9fa + WHERE (geo_id IS NULL OR geo_id=''); pre-flight confirms it is the City of West Covina gov (NOT the West Covina Unified School District 131e33d0); chamber DELETE keyed on UUID 'b1a2c4cb' ONLY, never name/slug; doomed-chamber merge asserts 0 offices before DELETE; second apply = 0 rows |
| T-152-02 | Tampering | Doomed chamber deleted BEFORE Diaz/Gutierrez moved -> orphaned offices; wrong district relabeled (multiple similar At-Large UUIDs); a SHARED At-Large district_id mislabels multiple seats (Pomona/Torrance defect) | mitigate | Move-FIRST (UPDATE WHERE chamber_id='b1a2c4cb…') + inline DO $$ assert RAISEs if b1a2c4cb still has >0 offices before DELETE; each relabel UPDATE keyed on the exact At-Large UUID from the Task 1 office->district map; if a shared district_id is found, create 5 distinct rows + repoint (no single-row relabel); post-verify 5 distinct district_ids + labels D1-D5 |
| T-152-03 | Tampering | Wrong mayor model applied — a LOCAL_EXEC Mayor office created (El Monte/Lancaster directly-elected model misapplied where West Covina is rotational; RESEARCH §Anti-Patterns) | mitigate | NO LOCAL_EXEC row created; the Mayor is a title set on a seat in Plan 02; post-verify ZERO LOCAL_EXEC rows under gov 1982a9fa |
| T-152-04 | Tampering | One-directional links left unrepaired (Diaz f5bf4ec4 / Gutierrez 22fc2cdc politicians.office_id NULL); write to non-existent offices.district_type column | mitigate | Back-pointer UPDATEs set politicians.office_id for Diaz + Gutierrez guarded IS DISTINCT FROM; the relabel touches districts.label ONLY (no offices.district_type write); post-verify all 5 survivor offices bidirectional; second apply = 0 rows |
| T-152-05 | Tampering | Non-idempotent migration applied twice, or written without verifying pre-state (drift since 2026-06-21) | mitigate | All writes guarded (geo_id IS NULL/empty, id-scoped move/delete, IS DISTINCT FROM, NOT EXISTS); Task 1 read-only pre-flight confirms BOTH link directions + office->district map + the current roster before any write; STOP-on-mismatch; second apply = 0 rows |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable; existing Pillow + Supabase client only) |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 1010:
1. gov 1982a9fa geo_id = '0684200' (state 'CA'); West Covina Unified 131e33d0 untouched
2. exactly 1 chamber 'City Council' under gov 1982a9fa; doomed b1a2c4cb gone
3. survivor chamber 12c9360a has exactly 5 offices, all 5 bidirectional (politicians.office_id ↔ offices.politician_id)
4. district rows for geo_id 0684200: 'District 1'..'District 5' (all LOCAL, 5 rows, each office on a distinct district_id); ZERO LOCAL_EXEC; D1 Gutierrez / D2 Lopez-Viado / D3 Diaz / D4 Cantos / D5 Wu
5. NO Mayor office created (rotational); no shared-district defect remains; relabeled/created + repaired UUIDs recorded for Plan 02
6. feedback_section_split_check SQL -> 0 rows for West Covina
7. no politician created; official_count not set; Mayor/Mayor-Pro-Tem titles not set (all Plan 02)
8. migration 1010 registered in schema_migrations AND committed to EV-Accounts
</verification>

<success_criteria>
All structural defects fixed and independently verified: geo_id backfilled (empty-string-safe); the duplicate
chamber merged via move-then-delete of Diaz + Gutierrez (with an inline assert proving the doomed chamber was
empty before deletion); the two one-directional links repaired so all 5 survivor seats are bidirectional; the
council form of government corrected to West Covina's real by-district structure (rows relabeled D1-D5, shared
district_id split if present); NO LOCAL_EXEC Mayor office created (rotational mayor — title set in Plan 02);
migration 1010 idempotent, registered, and committed to EV-Accounts; West Covina is a single unambiguous 'City
Council' chamber with correctly-districted, bidirectionally-linked offices, ready for the roster relabel finalize
+ Mayor-title in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/152-west-covina-deep-seed/152-01-SUMMARY.md` when done (record the relabeled/created District 1-5 UUIDs, the two repaired back-pointer office UUIDs, the shared-vs-distinct district finding, confirmation no LOCAL_EXEC row exists, and the confirmed migration numbers for Plan 02).
</output>
</content>
