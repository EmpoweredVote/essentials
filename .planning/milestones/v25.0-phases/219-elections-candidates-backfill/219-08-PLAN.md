---
phase: 219-elections-candidates-backfill
plan: 08
type: execute
wave: 2
depends_on: [219-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1399_collin_thin_backfill_b.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1399_collin_thin_backfill_b.ts
autonomous: false
requirements: [COLLIN-ELECT-02, COLLIN-ELECT-03]
user_setup: []

must_haves:
  truths:
    - "Parker (4855152), Celina (4813684), Frisco (4827684), Fairview (4825224) and Lowry Crossing (4844308) each have their remaining offices' races backfilled where that seat had a known election, ADDING to the existing migration-100 races (COLLIN-ELECT-02)."
    - "Parker's existing migration-100 race_candidates rows for Pettle/Pilgrim/Barron are opportunistically UPDATEd to link their now-existing politician_id (D-05 photo-reuse), guarded so re-run is net-zero."
    - "Uncontested/declared-elected remaining seats are seeded as a race + the single candidate (D-03); seats past a reasonable research horizon stay documented-open (D-06)."
    - "Every seeded race links to the correct existing office_id and renders on /results (COLLIN-ELECT-03)."
  artifacts:
    - "C:/EV-Accounts/backend/migrations/1399_collin_thin_backfill_b.sql"
    - "C:/EV-Accounts/backend/scripts/_apply-migration-1399_collin_thin_backfill_b.ts"
  key_links:
    - "backfilled races.office_id → the previously race-less existing offices.id per city."
    - "Parker existing race_candidates.politician_id → offices.politician_id for the 218-seated Pettle/Pilgrim/Barron."
  prohibitions:
    - statement: "candidate_status only ever uses a legal CHECK value (active/withdrawn/filed)."
      status: enforced
      verification: "apply-script gate: 0 candidate rows with candidate_status NOT IN ('active','withdrawn','filed')."
    - statement: "position_name always city-prefixed; backfilled names never collide with existing migration-100 races (incl. Lowry Crossing's existing Ward 4 race, which is NOT re-seeded)."
      status: enforced
      verification: "apply-script gate: per-city race count = existing + newly-seeded, no ON CONFLICT swallow."
    - statement: "primary_party stays NULL (D-06); no inform.* writes (D-07)."
      status: enforced
      verification: "0 non-NULL primary_party on seeded races; inform.* before/after unchanged."
    - statement: "The Parker politician_id UPDATE only sets a currently-NULL politician_id (never overwrites), so re-run is net-zero."
      status: enforced
      verification: "apply-script gate: UPDATE ... WHERE politician_id IS NULL; second run affects 0 rows."
    - statement: "No fabricated candidate; a remaining office with no resolvable election stays race-less with a documented reason (Pitfall 4)."
      status: enforced
      verification: "Every seeded candidate cited; each still-race-less office has a written reason."
  backstop:
    - statement: "Parker publishes no official Place numbers (positional convention from 218) and Fairview uses 'Seat' not 'Place' — join offices by the stored title, do not re-map."
      verification: backstop
---

<objective>
Backfill the remaining race-less offices for five thin cities — Parker, Celina, Frisco, Fairview, Lowry Crossing — adding to the migration-100 races, plus the Parker bonus: link the now-existing politician_id onto Pettle/Pilgrim/Barron's existing candidate rows (D-05).

Purpose: Closes COLLIN-ELECT-02 for these five cities. COLLIN-ELECT-03 for their render path.
Output: migration 1399 + gated apply-script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-elections-candidates-backfill/219-RESEARCH.md
@.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md
@.planning/phases/219-elections-candidates-backfill/219-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify remaining offices + source rosters for Parker/Celina/Frisco/Fairview/Lowry Crossing</name>
  <read_first>
    - 219-RESEARCH.md §"Thin governments" rows for Parker/Celina/Frisco/Fairview/Lowry Crossing (existing races to REUSE, incl. Parker's politician_id bonus + Lowry Crossing Ward 4 first/second-seat assignment already documented — do not re-litigate), §Pitfall 4.
    - C:/EV-Accounts/backend/migrations/100_collin_county_may2026_races.sql (existing races; Parker At-Large Vote-For-2 uses Place 1 office seats=2; Lowry Crossing Ward 4 uses Place 4 office seats=2).
    - C:/EV-Accounts/backend/migrations/1389_collin_seat_cited_incumbents.sql (Parker Mayor/Place3/Place5 politician_id source for the bonus UPDATE).
    - Sources: parkertexas.us/76 + /87, celinatx.gov/government/city-council, friscotexas.gov, fairviewtexas.org, lowrycrossingtexas.org/operations/elections.php.
  </read_first>
  <action>
    For each city, enumerate offices with NO race (offices minus migration-100-covered seats). Source each remaining office's most-recent held election + full filed field (cited — D-04) or single declared-elected candidate (D-03); decide shared-row vs earlier-cycle per seat. Join offices by their STORED title (Parker positional 'Place', Fairview 'Seat', Lowry Crossing 'Ward'/'Place') — do not re-map. For the Parker bonus: identify the offices.politician_id for Pettle (Mayor), Pilgrim (Place 3), Barron (Place 5) to link onto their existing migration-100 race_candidates rows. Map other winners to politician_id where 218 seated them; is_incumbent explicit. Unresolvable seats stay documented-open. Escape apostrophes; preserve diacritics.
  </action>
  <verify>
    <automated>MISSING — roster verified by Task 2 apply-script gates.</automated>
  </verify>
  <acceptance_criteria>
    - Each city's race-less offices enumerated against migration 100.
    - Each backfilled seat has a cited roster + election-date decision; unresolvable seats documented-open.
    - Parker Pettle/Pilgrim/Barron politician_id values identified for the bonus UPDATE.
  </acceptance_criteria>
  <done>Seed-ready cited backfill rosters + the Parker link-up map exist for all five cities.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 1399 (backfill + Parker politician_id UPDATE) + gated apply-script</name>
  <read_first>
    - 219-RESEARCH.md §Pattern 1/2/3/4.
    - C:/EV-Accounts/backend/migrations/1375 (ON CONFLICT idiom); 100 (existing races to preserve).
    - C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts (gate shape).
    - Migration number from PREFLIGHT (research: 1399; re-confirm + shift block if drifted).
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1399_collin_thin_backfill_b.sql in BEGIN/COMMIT. Resolve shared/earlier-cycle election ids by name/date/state. INSERT backfill races (city-prefixed position_name distinct from existing races, correct office_id via stored title, primary_party NULL, ON CONFLICT position-name idiom) + race_candidates (WHERE NOT EXISTS; winners→politician_id; candidate_status='active'; is_incumbent explicit; cited source). Add the Parker bonus: UPDATE essentials.race_candidates SET politician_id = <the 218 office politician_id> WHERE full_name IN (Pettle/Pilgrim/Barron rows) AND politician_id IS NULL (the IS NULL guard makes re-run net-zero and prevents overwriting). Write C:/EV-Accounts/backend/scripts/_apply-migration-1399_collin_thin_backfill_b.ts mirroring 1389 with gates: per-geo_id race count increased by expected count; every seeded race has ≥1 candidate; 0 illegal candidate_status; 0 non-NULL primary_party; Parker Pettle/Pilgrim/Barron rows now have non-NULL politician_id; inform.* unchanged; split-section 0; idempotent re-run net-zero (incl. the Parker UPDATE affecting 0 rows on second pass).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/_apply-migration-1399_collin_thin_backfill_b.ts 2>&1 | head; test -f migrations/1399_collin_thin_backfill_b.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; backfill races use the ON CONFLICT position-name idiom; Parker UPDATE is guarded by politician_id IS NULL.
    - Apply-script asserts race-count increase, the Parker link-up, and net-zero idempotent re-run.
  </acceptance_criteria>
  <done>Migration 1399 + gated apply-script authored and type-check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator applies 1399, confirms gates green, pushes, browse spot-check</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-1399_collin_thin_backfill_b.ts.
    - [[no_git_in_ev_accounts]] / [[backend_architecture]].
  </read_first>
  <action>Operator-run checkpoint: apply migration 1399 via `npx tsx`, confirm every apply-script gate exits 0 (incl. race-count-increase, the Parker politician_id link-up, and net-zero re-run), commit + push to C:/EV-Accounts (Render auto-deploys), then browse-spot-check Parker on /results — per the numbered how-to-verify steps.</action>
  <what-built>Executor has no DB access / prod push; operator applies + verifies.</what-built>
  <how-to-verify>
    1. `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-1399_collin_thin_backfill_b.ts` — all gates green, exit 0.
    2. `git -C "C:/EV-Accounts" add -A && git -C "C:/EV-Accounts" commit -m "feat(219): backfill thin-city races B (Parker/Celina/Frisco/Fairview/Lowry Crossing) + Parker photo link" && git -C "C:/EV-Accounts" push origin master`.
    3. Browse spot-check `/results?browse_geo_id=4855152` (Parker) — confirm backfilled races render and Pettle/Pilgrim/Barron now show their headshot via the linked politician_id.
  </how-to-verify>
  <acceptance_criteria>
    - Apply-script exits 0, all gates green; race counts increased; Parker link-up confirmed.
    - Parker browse shows more races + the three linked headshots (backstop: human visual).
  </acceptance_criteria>
  <resume-signal>Type "approved" once gates green + browse passes, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `C:/EV-Accounts/backend/migrations/1399_collin_thin_backfill_b.sql` (new)
- `C:/EV-Accounts/backend/scripts/_apply-migration-1399_collin_thin_backfill_b.ts` (new)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external canvass/news → race_candidates | Untrusted civic claims; every row cited |
| migration SQL → production essentials schema | Idempotent, guarded ADD-only writes + guarded UPDATE |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Tampering/Info disclosure | race_candidates rows | high | mitigate | Evidence-only; still-race-less offices documented, never guessed |
| T-219-02 | Tampering (schema integrity) | candidate_status | high | mitigate | Only legal CHECK values; gate asserts 0 illegal |
| T-219-05 | Data integrity (silent loss / overwrite) | backfill + Parker UPDATE | high | mitigate | Distinct city-prefixed position_name; UPDATE guarded by politician_id IS NULL; count-increase gate; idempotent re-run |
| T-219-04 | Integrity (scope violation) | inform.* tables | high | mitigate | before/after row-count assertion (D-07) |
| T-219-03 | Tampering (SQL literal) | apostrophe/diacritic names | medium | mitigate | Escape '→''; preserve UTF-8; type-check |
| T-219-SC | Tampering | npm/pip installs | low | accept | No package installs; legitimacy gate N/A |
</threat_model>

<verification>
- All apply-script gates green.
- Per-city race count increased; Parker Pettle/Pilgrim/Barron politician_id linked; existing races untouched; split-section 0; primary_party 0; inform.* net-zero.
</verification>

<success_criteria>
Parker/Celina/Frisco/Fairview/Lowry Crossing render backfilled races on /results; Parker's three seated officials show their reused headshots; still-race-less offices documented; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/219-elections-candidates-backfill/219-08-SUMMARY.md` when done.
</output>
