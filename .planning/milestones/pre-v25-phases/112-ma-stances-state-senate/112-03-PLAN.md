---
phase: 112-ma-stances-state-senate
plan: 03
type: execute
wave: 3
depends_on:
  - 112-01
  - 112-02
files_modified: []
autonomous: false
requirements:
  - MA-STANCES-03
must_haves:
  truths:
    - "All 40 MA senators have been attempted (rows present or blank documented as intentional per D-01)"
    - "Phase-wide 100% citation rate: uncited_total = 0 across all 40 senators"
    - "Phase-wide pairing: unpaired_total = 0 across all 40 senators"
    - "Compass renders on at least one MA senator profile (ROADMAP success criterion #3)"
    - "MA-STANCES-03 closed"
  artifacts:
    - path: ".planning/phases/112-ma-stances-state-senate/112-03-SUMMARY.md"
      provides: "Final phase summary with MA-STANCES-03 closure metrics"
  key_links:
    - from: "src/lib/compass.js (computeDisplaySpokes)"
      to: "inform.politician_answers"
      via: "compass spoke selection reads politician_answers directly"
      pattern: "computeDisplaySpokes"
---

<objective>
Phase-wide verification for all 40 MA state senators. Run quality gates Q1/Q2/Q3 against production Supabase, verify compass renders on at least one senator profile (human checkpoint), and document MA-STANCES-03 closure.

Prerequisite: 112-01 and 112-02 both complete (all 40 senators researched and applied).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/112-ma-stances-state-senate/112-01-SUMMARY.md
@.planning/phases/112-ma-stances-state-senate/112-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-wide closure verification via mcp__supabase-local__execute_sql</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 112 success criteria)
    - .planning/REQUIREMENTS.md (MA-STANCES-03)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-10: citation rate)
  </read_first>
  <action>
    Q1 — Per-senator stance count (all 40):
      SELECT p.external_id, p.full_name, COUNT(pa.*) AS stance_count
      FROM essentials.politicians p
      LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
      WHERE p.external_id BETWEEN -210040 AND -210001
      GROUP BY p.external_id, p.full_name
      ORDER BY p.external_id DESC;
    Expected: 40 rows. Blank spokes (COUNT=0) are acceptable per D-01 if no public evidence was found.
    Record stance_count for each senator in the summary.

    Q2 — Phase-wide 100% citation rate (D-10):
      SELECT COUNT(*) AS uncited_total
      FROM inform.politician_context pc
      JOIN essentials.politicians p ON p.id = pc.politician_id
      WHERE p.external_id BETWEEN -210040 AND -210001
        AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL OR array_length(pc.sources,1)=0);
    Required: 0.

    Q3 — Phase-wide pairing check (unpaired = 0):
      SELECT COUNT(*) AS unpaired_total
      FROM inform.politician_answers pa
      JOIN essentials.politicians p ON p.id = pa.politician_id
      LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
      WHERE p.external_id BETWEEN -210040 AND -210001
        AND pc.politician_id IS NULL;
    Required: 0.

    If Q2 or Q3 fail (non-zero), identify offending politicians:
      -- For Q2 failures:
      SELECT p.full_name, COUNT(*) AS uncited
      FROM inform.politician_context pc JOIN essentials.politicians p ON p.id=pc.politician_id
      WHERE p.external_id BETWEEN -210040 AND -210001
        AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL OR array_length(pc.sources,1)=0)
      GROUP BY p.full_name;

      -- For Q3 failures:
      SELECT p.full_name, COUNT(*) AS unpaired
      FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id
      LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
      WHERE p.external_id BETWEEN -210040 AND -210001 AND pc.politician_id IS NULL
      GROUP BY p.full_name;
    Fix identified failures before proceeding to Task 2.
  </action>
  <verify>
    <automated>
      Q1: 40 rows returned. Q2: uncited_total = 0. Q3: unpaired_total = 0.
    </automated>
  </verify>
  <done>All 3 quality gates pass; MA-STANCES-03 closure conditions confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify compass renders on at least one MA senator profile</name>
  <what-built>
    Stance migrations 376–415 have been applied covering all 40 MA state senators. The compass reads from `inform.politician_answers` via `computeDisplaySpokes()` in `src/lib/compass.js`.

    ROADMAP Phase 112 success criterion #3 requires verifying the compass renders on at least one senator profile.
  </what-built>
  <how-to-verify>
    1. Resolve a senator slug (pick one with stances — e.g. Karen Spilka, external_id=-210013, or whichever had the highest stance count from Q1):
       SELECT slug FROM essentials.politicians WHERE external_id = -210013;
    2. Visit production profile: https://essentials.empowered.vote/politician/{slug}
    3. Confirm compass component renders with spokes for documented topics. Blank spokes for topics with no evidence are correct per D-01.
    4. Click into a spoke to confirm stance reasoning + sources display in accordion.

    Acceptable outcomes:
      - APPROVED: Compass renders with spokes; no UI errors.
      - ISSUES: Describe what is broken; orchestrator routes to gap-closure.
  </how-to-verify>
  <resume-signal>
    Type "approved" if compass renders correctly. Otherwise describe the issue.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write final phase summary and close MA-STANCES-03</name>
  <read_first>
    - .planning/phases/112-ma-stances-state-senate/112-01-SUMMARY.md
    - .planning/phases/112-ma-stances-state-senate/112-02-SUMMARY.md
  </read_first>
  <action>
    Create `.planning/phases/112-ma-stances-state-senate/112-03-SUMMARY.md` containing:
    1. Outcome: MA-STANCES-03 CLOSED.
    2. Full 40-senator table: external_id | name | district | migration | stance_count | status (OK if >=1, BLANK if 0 and documented).
    3. Quality gates: Q1 (40 rows), Q2 (uncited_total=0), Q3 (unpaired_total=0).
    4. Compass render checkpoint result (from Task 2).
    5. Senators with blank spokes (no evidence found) — list by name and district.
    6. Migrations applied: 376–415 (40 files).
    7. Next migration number: 416.
    8. Decisions honored: D-01 through D-10.
  </action>
  <verify>
    <automated>
      Test-Path ".planning/phases/112-ma-stances-state-senate/112-03-SUMMARY.md"
      (Select-String -Path ".planning/phases/112-ma-stances-state-senate/112-03-SUMMARY.md" -Pattern 'MA-STANCES-03').Count -ge 1
    </automated>
  </verify>
  <done>112-03-SUMMARY.md written with all tier tables, quality gate results, and requirement closure.</done>
</task>

</tasks>

<verification>
- Phase-wide Q1/Q2/Q3 all pass (40 senators, uncited_total=0, unpaired_total=0)
- Compass renders on at least one MA senator profile (human-verified)
- MA-STANCES-03 CLOSED
</verification>

<success_criteria>
- All 4 ROADMAP Phase 112 success criteria met:
  1. politician_answers rows present for all 40 MA senators (or blank documented)
  2. 100% citation rate — uncited_total = 0
  3. Compass renders on at least one senator profile (human-verified)
  4. Senators with no public evidence have blank spokes (not defaulted values)
- MA-STANCES-03 CLOSED
- Phase 112 ready for /gsd-verify-phase
</success_criteria>

<output>
Create `.planning/phases/112-ma-stances-state-senate/112-03-SUMMARY.md` per Task 3.
</output>
