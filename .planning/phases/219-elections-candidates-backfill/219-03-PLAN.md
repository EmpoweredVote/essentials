---
phase: 219-elections-candidates-backfill
plan: 03
type: execute
wave: 2
depends_on: [219-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1394_collin_offcycle_zero_race.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1394_collin_offcycle_zero_race.ts
autonomous: false
requirements: [COLLIN-ELECT-01, COLLIN-ELECT-03]
user_setup: []

must_haves:
  truths:
    - "Josephine (4838068), Lavon (4841800), Saint Paul (4864220) and Melissa (4847496) each have ≥1 seeded race with ≥1 candidate for whatever their PREFLIGHT-resolved reference cycle is, OR a documented legitimately-empty reason for every seat (COLLIN-ELECT-01)."
    - "Off-cycle cities that did not use the shared 2026-05-02 date get their own election row (minted by name/date/state, ON CONFLICT DO NOTHING) — not a wrong shared-row link (D-01)."
    - "Every seeded race links to the correct existing office_id and renders on /results (COLLIN-ELECT-03)."
    - "Re-applying migration 1394 is net-zero (idempotent)."
  artifacts:
    - "C:/EV-Accounts/backend/migrations/1394_collin_offcycle_zero_race.sql"
    - "C:/EV-Accounts/backend/scripts/_apply-migration-1394_collin_offcycle_zero_race.ts"
  key_links:
    - "races.office_id → existing offices.id per seat; races.election_id → shared row OR a city-specific minted row."
  prohibitions:
    - statement: "candidate_status only ever uses a legal CHECK value (active/withdrawn/filed)."
      status: enforced
      verification: "apply-script gate: 0 candidate rows with candidate_status NOT IN ('active','withdrawn','filed')."
    - statement: "position_name always city-prefixed."
      status: enforced
      verification: "apply-script gate: per-geo_id race count matches roster; every position_name starts with its city name."
    - statement: "primary_party stays NULL (D-06)."
      status: enforced
      verification: "0 seeded races with primary_party NOT NULL."
    - statement: "No inform.* writes (D-07)."
      status: enforced
      verification: "before/after row-count assertion unchanged."
    - statement: "No fabricated candidate — a city with no resolvable election (e.g. an [OPEN] Lavon/Saint Paul seat) stays documented-empty, never guessed (5 zero-source cities incl. Saint Paul stay blank)."
      status: enforced
      verification: "Every seeded candidate cited; each unresolved seat has a written reason in the migration header + SUMMARY."
  backstop:
    - statement: "Melissa's true ballot content (props-only vs candidate-race) was resolved in PREFLIGHT; if still ambiguous at execute, seed nothing for Melissa and document the open gap rather than guess."
      verification: backstop
---

<objective>
Seed races + candidates for the four off-cycle / previously-open zero-race cities — Josephine, Lavon, Saint Paul, Melissa — using each city's PREFLIGHT-resolved reference cycle. Cities that did not vote on 2026-05-02 get their own minted election row; cities with no resolvable election stay honestly empty.

Purpose: Closes COLLIN-ELECT-01 for the hardest-to-source zero-race cities without fabricating a race (D-06).
Output: migration 1394 + gated apply-script, committed to C:/EV-Accounts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-elections-candidates-backfill/219-RESEARCH.md
@.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md
@.planning/phases/219-elections-candidates-backfill/219-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm rosters + per-city reference cycle for Josephine, Lavon, Saint Paul, Melissa</name>
  <read_first>
    - .planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md (reference-cycle decision + [OPEN] markers + candidate_staging reuse for these 4 cities).
    - 219-RESEARCH.md §"Zero-race governments" rows for Josephine/Lavon/Saint Paul/Melissa, §"Assumptions Log" A2, §"Open Questions" #1.
    - City sources from migration 099: cityofjosephinetx.com, lavontx.gov/election-information, stpaultexas.us, cityofmelissa.com/287/Elections; Collin canvass archive.
  </read_first>
  <action>
    For each city, using the PREFLIGHT decision as the anchor and confirming with WebSearch/WebFetch + the Collin canvass PDF: determine per-seat the most-recent HELD election and its date, then assemble the full filed field (winners + losers, cited — D-04) or the single declared-elected candidate for uncontested seats (D-03). Decide election linkage per RESEARCH: shared 2026-05-02 row for any seat that truly voted that date, else a city-specific minted election row (name/date/state). Reuse Phase-218 winners via offices.politician_id (D-05); set is_incumbent explicitly. For Melissa: apply the PREFLIGHT resolution of the props-only-vs-candidate-race conflict; if genuinely unresolved, seed no Melissa race and document the open gap. Any seat with no resolvable election stays race-less with a written reason. Escape apostrophes; preserve diacritics.
  </action>
  <verify>
    <automated>MISSING — roster verified by Task 2's apply-script per-geo_id gates.</automated>
  </verify>
  <acceptance_criteria>
    - Each city has either a cited roster (≥1 seat) OR a documented legitimately-empty finding for every seat.
    - Election linkage decided per seat (shared vs minted own-row) with a citation for the date.
    - Melissa handled per its PREFLIGHT resolution (seeded if candidate-race confirmed; documented-open otherwise).
  </acceptance_criteria>
  <done>Seed-ready, cited per-seat plan exists for all four cities including honest-empty documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 1394 + gated apply-script (with own-election-row minting)</name>
  <read_first>
    - 219-RESEARCH.md §Pattern 1/2/3/4 (shared-row resolution, ON CONFLICT race insert, candidate attach, uncontested seat) and §Pattern 5 preamble (own election row minting by name/date/state).
    - C:/EV-Accounts/backend/migrations/1375_az_2026_local_races.sql (ON CONFLICT idiom) and 187 (own-election-row shape).
    - C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts (gate shape to mirror).
    - Migration number from PREFLIGHT (research: 1394; re-confirm + shift block preserving order if drifted).
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1394_collin_offcycle_zero_race.sql in BEGIN/COMMIT. For each off-cycle city needing its own election, INSERT INTO essentials.elections (name, election_date, state, election_type, jurisdiction_level) ... ON CONFLICT (name, election_date, state) DO NOTHING, then resolve that id by name/date/state. INSERT races with city-prefixed position_name, correct office_id (geo_id+title subquery), primary_party left NULL, using ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING. INSERT race_candidates with WHERE NOT EXISTS guard; winners via politician_id, all candidate_status='active', is_incumbent explicit, cited source. Then write C:/EV-Accounts/backend/scripts/_apply-migration-1394_collin_offcycle_zero_race.ts mirroring 1389: inform.politician_answers before-count; apply; gates (per-geo_id race count for every city that HAS a resolvable election; every seeded race has ≥1 candidate; 0 illegal candidate_status; 0 non-NULL primary_party on seeded races; inform.* unchanged; split-section 0 rows; idempotent re-run net-zero). For a documented-empty city, the gate asserts an explicit expected-0 with a comment citing the reason (not a silent skip).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/_apply-migration-1394_collin_offcycle_zero_race.ts 2>&1 | head; test -f migrations/1394_collin_offcycle_zero_race.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; own-election-row inserts use ON CONFLICT (name, election_date, state) DO NOTHING and are resolved by name/date/state (no hardcoded UUID target).
    - Every race INSERT uses the ON CONFLICT position-name idiom; candidates use WHERE NOT EXISTS.
    - Apply-script gates present incl. the documented-empty expected-0 assertion where applicable.
  </acceptance_criteria>
  <done>Migration 1394 + gated apply-script authored and type-check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator applies 1394, confirms gates green, pushes, browse spot-check</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-1394_collin_offcycle_zero_race.ts.
    - [[no_git_in_ev_accounts]] / [[backend_architecture]].
  </read_first>
  <action>Operator-run checkpoint: apply migration 1394 via `npx tsx`, confirm every apply-script gate exits 0, commit + push to C:/EV-Accounts (Render auto-deploys), then browse-spot-check a seeded city on /results — per the numbered how-to-verify steps.</action>
  <what-built>Executor has no DB access / prod push; operator applies + verifies.</what-built>
  <how-to-verify>
    1. `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-1394_collin_offcycle_zero_race.ts` — all gates green, exit 0.
    2. `git -C "C:/EV-Accounts" add -A && git -C "C:/EV-Accounts" commit -m "feat(219): seed off-cycle zero-race city races (Josephine/Lavon/Saint Paul/Melissa)" && git -C "C:/EV-Accounts" push origin master`.
    3. Browse spot-check one seeded city (e.g. `/results?browse_geo_id=4838068` Josephine) — Elections section shows the seeded race(s), or confirm a documented-empty city is correctly still empty.
  </how-to-verify>
  <acceptance_criteria>
    - Apply-script exits 0, all gates green.
    - Spot-checked city renders its seeded race (backstop: human visual); documented-empty cities remain intentionally empty with the reason recorded in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Type "approved" once gates green + browse check passes, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `C:/EV-Accounts/backend/migrations/1394_collin_offcycle_zero_race.sql` (new)
- `C:/EV-Accounts/backend/scripts/_apply-migration-1394_collin_offcycle_zero_race.ts` (new)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external canvass/news → race_candidates | Untrusted civic claims; every row cited or seat left documented-empty |
| migration SQL → production essentials schema | Idempotent, guarded writes |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Tampering/Info disclosure | race_candidates rows | high | mitigate | Evidence-only; unresolved seats documented-empty, never guessed (esp. Lavon/Saint Paul [OPEN]) |
| T-219-02 | Tampering (schema integrity) | candidate_status | high | mitigate | Only legal CHECK values; gate asserts 0 illegal |
| T-219-03 | Tampering (SQL literal) | apostrophe/diacritic names | medium | mitigate | Escape '→''; preserve UTF-8; type-check before apply |
| T-219-04 | Integrity (scope violation) | inform.* tables | high | mitigate | before/after row-count assertion (D-07) |
| T-219-05 | Data integrity (silent loss) | races via ON CONFLICT / own election rows | high | mitigate | City-prefixed position_name; election minted by name/date/state; per-geo_id race-count gate |
| T-219-SC | Tampering | npm/pip installs | low | accept | No package installs; legitimacy gate N/A |
</threat_model>

<verification>
- All apply-script gates green.
- Every city with a resolvable election has race count > 0; documented-empty cities have a recorded reason.
- Split-section 0 rows; 0 non-NULL primary_party; inform.* net-zero.
</verification>

<success_criteria>
Josephine/Lavon/Saint Paul/Melissa each render their real most-recent race on /results where one exists, and are honestly empty (documented) where none does; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/219-elections-candidates-backfill/219-03-SUMMARY.md` when done.
</output>
