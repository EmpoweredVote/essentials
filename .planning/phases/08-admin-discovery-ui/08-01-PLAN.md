---
phase: 08-admin-discovery-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/083_discovery_runs_auto_upserted.sql
  - C:/EV-Accounts/backend/src/lib/discoveryService.ts
autonomous: true

must_haves:
  truths:
    - "discovery_runs table has candidates_auto_upserted column"
    - "Completed discovery runs persist their autoUpserted count to that column"
    - "Existing rows backfill to 0 (no NULLs)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/083_discovery_runs_auto_upserted.sql"
      provides: "Adds candidates_auto_upserted INT NOT NULL DEFAULT 0 to essentials.discovery_runs"
      contains: "ALTER TABLE essentials.discovery_runs"
    - path: "C:/EV-Accounts/backend/src/lib/discoveryService.ts"
      provides: "Persists autoUpserted count via UPDATE on run completion"
      contains: "candidates_auto_upserted"
  key_links:
    - from: "discoveryService.ts UPDATE statement"
      to: "essentials.discovery_runs.candidates_auto_upserted"
      via: "parameterized UPDATE alongside candidates_new and candidates_withdrawn"
      pattern: "candidates_auto_upserted\\s*="
---

<objective>
Add the `candidates_auto_upserted` column to `essentials.discovery_runs` and update `discoveryService.ts` to persist the in-memory `autoUpserted` count when a run completes.

Purpose: Phase 7's auto-upsert branch tracks `autoUpserted` in memory but never writes it to the database. The Phase 8 admin dashboard needs this column to display per-run "Auto-Upserted" stats (DASH-02). Without this migration, the run history UI will show only 0s for that column.

Output: Migration 083 applied, discovery_runs has the new column, discoveryService.ts UPDATE statement persists the count, build passes, smoke test confirms the column is written on a real run.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-discovery-ui/08-CONTEXT.md
@.planning/phases/08-admin-discovery-ui/08-RESEARCH.md
@.planning/phases/07-cron-automation-auto-upsert/07-01-SUMMARY.md

# Existing service to modify
@C:/EV-Accounts/backend/src/lib/discoveryService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migration 083 for candidates_auto_upserted column</name>
  <files>C:/EV-Accounts/backend/migrations/083_discovery_runs_auto_upserted.sql</files>
  <action>
Create new migration file `083_discovery_runs_auto_upserted.sql` in `C:/EV-Accounts/backend/migrations/`.

The migration must:
- Wrap in `BEGIN;` / `COMMIT;`
- `ALTER TABLE essentials.discovery_runs ADD COLUMN IF NOT EXISTS candidates_auto_upserted INT NOT NULL DEFAULT 0;`
- Use `IF NOT EXISTS` for idempotency (matches Phase 5/6/7 migration style)
- No backfill needed beyond DEFAULT 0 — historical runs predate the auto-upsert feature so 0 is semantically correct

Numbering rationale: 082 is the current highest (`082_la_city_candidate_details.sql`), confirmed in research. Next is 083.

Do NOT modify any other table or column. This is a single-column additive migration.
  </action>
  <verify>
Run `cat C:/EV-Accounts/backend/migrations/083_discovery_runs_auto_upserted.sql` and confirm:
- File contains `BEGIN;` and `COMMIT;`
- Contains `ALTER TABLE essentials.discovery_runs`
- Contains `ADD COLUMN IF NOT EXISTS candidates_auto_upserted INT NOT NULL DEFAULT 0`

Apply migration locally via the project's standard psql / migration runner (whatever the existing migrations use — check for an apply script or run via `psql $DATABASE_URL -f migrations/083_*.sql`). After applying:
- `psql $DATABASE_URL -c "\d essentials.discovery_runs"` shows the new `candidates_auto_upserted` column with type integer, NOT NULL, default 0
  </verify>
  <done>Migration file exists, applies cleanly, and the column is present on `essentials.discovery_runs` with the correct type, nullability, and default.</done>
</task>

<task type="auto">
  <name>Task 2: Persist autoUpserted in discoveryService.ts run-completion UPDATE</name>
  <files>C:/EV-Accounts/backend/src/lib/discoveryService.ts</files>
  <action>
Update `C:/EV-Accounts/backend/src/lib/discoveryService.ts` so the run-completion UPDATE writes the in-memory `autoUpserted` count to the new `candidates_auto_upserted` column.

Steps:
1. Locate the success-path UPDATE near line 493 that currently sets `candidates_new = $3, candidates_withdrawn = $4` on `essentials.discovery_runs` (status='success' branch).
2. Add `candidates_auto_upserted = $N` to the SET clause (where $N is the next parameter index after current ones — adjust numbering to match existing positional params).
3. Pass the local `autoUpserted` variable (already tracked, see line 347 / 397) as the corresponding query parameter.
4. If there is a parallel failure-path UPDATE that writes counts, also include `candidates_auto_upserted` set to whatever the in-memory value is at that point (default 0 if not started counting). Inspect the file to confirm — only update branches that write the OTHER candidate count columns; do not invent new branches.
5. Do NOT change the public API of `runDiscoveryForJurisdiction()`. Do NOT change `DiscoveryRunSummary.autoUpserted` (already exists per Phase 7 SUMMARY, line 215).
6. Do NOT alter the auto-upsert eligibility logic, the staging insert path, the email suppression flag, or any other Phase 7 behavior.

Constraint: Use parameterized query placeholders (`$N`) consistent with the existing code style. No string concatenation of values into SQL.
  </action>
  <verify>
1. `cd C:/EV-Accounts/backend && npm run build` exits with 0 errors.
2. `grep -n "candidates_auto_upserted" C:/EV-Accounts/backend/src/lib/discoveryService.ts` returns at least one match in an UPDATE statement (not a comment).
3. Manually trigger a discovery run end-to-end against a test jurisdiction (or call `runDiscoveryForJurisdiction()` from a small ad-hoc script). Then:
   `psql $DATABASE_URL -c "SELECT id, candidates_new, candidates_withdrawn, candidates_auto_upserted FROM essentials.discovery_runs ORDER BY started_at DESC LIMIT 1"`
   Confirm the most recent row has a non-NULL `candidates_auto_upserted` value (0 is acceptable if no candidates qualified for auto-upsert).
  </verify>
  <done>`discoveryService.ts` writes `candidates_auto_upserted` on run completion. Build passes. A live test run produces a discovery_runs row with the column populated.</done>
</task>

</tasks>

<verification>
- Migration 083 file exists and applies idempotently
- `essentials.discovery_runs` schema includes `candidates_auto_upserted` column
- `discoveryService.ts` UPDATE writes the column
- Backend builds cleanly (`npm run build`)
- A real or simulated run produces a row with the column populated (not NULL)
</verification>

<success_criteria>
- [ ] `C:/EV-Accounts/backend/migrations/083_discovery_runs_auto_upserted.sql` exists with BEGIN/COMMIT and the additive ALTER
- [ ] Migration applied to local/dev database; column visible in `\d essentials.discovery_runs`
- [ ] `discoveryService.ts` references `candidates_auto_upserted` in at least one UPDATE
- [ ] `npm run build` in `C:/EV-Accounts/backend` exits 0
- [ ] Latest `discovery_runs` row after a run shows non-NULL `candidates_auto_upserted`
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-discovery-ui/08-01-SUMMARY.md` documenting:
- Migration number used and exact ALTER statement
- Lines changed in `discoveryService.ts`
- Confirmation that build passes and column is populated by a real run
</output>
