---
phase: 169-dark-mode-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/main.jsx
  - package.json
autonomous: false
requirements: [DARK-01]
user_setup: []

must_haves:
  truths:
    - "All dark @theme token VALUES in src/index.css match the Figma palette (page #0d1117, surface #161b22, accent #00c8d7, primary text #e6edf3, muted #8b949e); token NAMES are unchanged."
    - "Inter (body/UI) and Manrope (display/labels) are loaded and registered as --font-sans / --font-display tokens; the app default font resolves to Inter."
    - "Light mode is untouched — :root light vars and the html:not(.dark) WCAG blocks are byte-for-byte unchanged."
    - "npm run build compiles cleanly (Tailwind @theme + @source intact)."
  artifacts:
    - path: "src/index.css"
      provides: "Reconciled @theme dark token VALUES + new D-02 slate/text tokens + --font-sans/--font-display tokens; light :root and @source-not lines preserved"
      contains: "--color-ev-navy: #0d1117"
    - path: "src/main.jsx"
      provides: "Web-font load (self-host @fontsource OR confirmed CDN fallback) for Inter+Manrope"
  key_links:
    - from: "src/index.css @theme"
      to: "Tailwind utility classes (bg-ev-navy, text-ev-teal-light, font-sans, font-display)"
      via: "Tailwind v4 token compilation"
      pattern: "--color-ev-navy:\\s*#0d1117"
    - from: "src/main.jsx"
      to: "@fontsource/inter + @fontsource/manrope (or CDN @import)"
      via: "side-effect CSS import"
      pattern: "fontsource|googleapis"
---

<objective>
Reconcile the app-wide dark-mode design tokens in `src/index.css` to the Figma GitHub-dark palette
(node `3957:563`) as the single source of truth, and wire Inter + Manrope as `--font-*` type tokens.
This is the FOUNDATION wave — every downstream surface (Plan 02, and the out-of-scope pages that
inherit the global palette) reads from these tokens.

Purpose: DARK-01 — establish the single source of design tokens (color + type) so Results theming
(Plan 02) and the global app inherit the Figma dark treatment without scattered literals.
Output: Updated `@theme` dark token VALUES (names unchanged), new D-02 slate/text tokens, `--font-sans`
/ `--font-display` tokens, and a confirmed web-font load. Light mode untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-dark-mode-design-system-foundation/169-CONTEXT.md
@.planning/phases/169-dark-mode-design-system-foundation/169-RESEARCH.md
@.planning/phases/169-dark-mode-design-system-foundation/169-PATTERNS.md
@.planning/phases/169-dark-mode-design-system-foundation/169-UI-SPEC.md
@.planning/phases/169-dark-mode-design-system-foundation/169-VALIDATION.md

<interfaces>
<!-- Established in-file patterns the executor replicates. From src/index.css (read in full). -->

src/index.css current @theme dark block (L13-27) — KEEP NAMES, CHANGE VALUES:
  --color-ev-navy: #020618;        (page bg)
  --color-ev-navy-card: #1a2235;   (surface)
  --color-ev-navy-elevated: #1e2a3a;
  --color-ev-teal-light: #59b0c4;  (dark accent)
  --color-ev-coral: #ff5740;       (KEEP — D-04)

src/index.css :root LIGHT block (L29-36) — DO NOT TOUCH (D-03):
  --ev-teal: #00657c;        (light teal — forbidden to change)
  --ev-light-blue: #59b0c4;  (SEPARATE light token that shares the old dark hex — the #59b0c4 trap)

src/index.css L2: existing Google Fonts @import (Manrope wght@400;500;600;700) — current working load.
src/index.css L8-9: @source not "../.planning"; @source not "../**/*.md"; — build-safety, PRESERVE VERBATIM.
src/index.css L42: html, body { font-family: 'Manrope', sans-serif; } — global default to repoint to var(--font-sans).
src/main.jsx L6: import "./index.css"; — existing side-effect import; mirror this shape for @fontsource weights.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @fontsource package legitimacy before install (font-load path decision)</name>
  <read_first>
    - .planning/phases/169-dark-mode-design-system-foundation/169-RESEARCH.md (§Package Legitimacy Audit — both packages tagged [ASSUMED]; §Font Wiring — self-host vs CDN tradeoff)
    - .planning/phases/169-dark-mode-design-system-foundation/169-PATTERNS.md (src/main.jsx analog + CDN fallback note)
  </read_first>
  <what-built>
    No code yet — this is the package legitimacy gate (T-169-SC). The phase MAY install
    `@fontsource/inter` + `@fontsource/manrope` (self-host path), or take the known-working CDN
    `@import` fallback (installs nothing). Both packages are tagged [ASSUMED] in RESEARCH (slopcheck
    not run). Before any install, the executor runs `npm view @fontsource/inter version` and
    `npm view @fontsource/manrope version` to confirm both resolve on the npm registry.
  </what-built>
  <how-to-verify>
    1. Run `npm view @fontsource/inter version` and `npm view @fontsource/manrope version` — confirm
       both return a real version and the source repo is github.com/fontsource/fontsource (verify on
       npmjs.com/package/@fontsource/inter if any doubt).
    2. Decide the load path:
       - APPROVE SELF-HOST: install both and import weights in main.jsx (Task 3 self-host branch), OR
       - APPROVE CDN FALLBACK: skip install, extend the existing index.css @import to add Inter (Task 3 CDN branch).
    3. If either package looks suspicious/unresolvable, choose CDN fallback (no install).
  </how-to-verify>
  <resume-signal>Reply "self-host" or "cdn" (or describe a concern). This is a blocking legitimacy gate — not auto-approvable.</resume-signal>
  <acceptance_criteria>
    - behavior: both package names resolve on npm (or CDN fallback chosen); a load path is selected.
    - manual-visual: human confirms registry legitimacy before any install.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reconcile @theme dark token VALUES + add D-02 slate/text + font tokens (light untouched)</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (L1-43 — @theme dark block L13-27, :root light L29-36, @source-not L8-9, global font L42)
    - .planning/phases/169-dark-mode-design-system-foundation/169-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-07)
    - .planning/phases/169-dark-mode-design-system-foundation/169-UI-SPEC.md (Color + Typography tables — authoritative values)
    - .planning/phases/169-dark-mode-design-system-foundation/169-PATTERNS.md (§src/index.css Pattern A — keep-names/change-values)
    - .planning/phases/169-dark-mode-design-system-foundation/169-RESEARCH.md (§Token Inventory — Pitfall 1 the two-#59b0c4 trap)
  </read_first>
  <action>
    In the `@theme` block of src/index.css (L13-27), change ONLY the dark token VALUES, keeping names
    (D-01/D-02): `--color-ev-navy` #020618→#0d1117, `--color-ev-navy-card` #1a2235→#161b22,
    `--color-ev-teal-light` #59b0c4→#00c8d7, `--color-ev-text-primary` text #f3f4f6→#e6edf3 (add as a
    new token), `--color-ev-text-muted` #8b949e (add as a new token). KEEP `--color-ev-coral #ff5740`
    (D-04). For `--color-ev-navy-elevated` set #21262d (GitHub canvas.subtle, RESEARCH Open Q1 resolution
    for card hover). ADD into the same `@theme` block the D-02 divider slates `--color-ev-slate #2d3748`
    and `--color-ev-slate-strong #4a5568`. ADD the D-05 font tokens `--font-sans: "Inter", ui-sans-serif,
    system-ui, sans-serif;` and `--font-display: "Manrope", ui-sans-serif, system-ui, sans-serif;`.
    Update the global default at L42 `html, body { font-family: 'Manrope', sans-serif; }` to
    `font-family: var(--font-sans);` ONLY after Task 3 confirms Inter is loaded (do this swap in Task 3
    to avoid a font gap; leave L42 as-is in this task). Also update `.dark body` color (L46 region)
    primary text #f3f4f6→#e6edf3.
    DO NOT TOUCH the `:root` LIGHT block (L29-36) — specifically `--ev-teal #00657c` and `--ev-light-blue
    #59b0c4` stay byte-for-byte (D-03; the two-#59b0c4 trap — change only the dark `--color-ev-teal-light`).
    PRESERVE VERBATIM the `@source not "../.planning";` / `@source not "../**/*.md";` lines (L8-9) and the
    Manrope @import (L2) for now. No fenced code; follow Pattern A in 169-PATTERNS.md.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - build-command: `npm run build` exits 0 (Tailwind @theme + @source compile intact).
    - source-assertion: `grep -v '^\s*/\*' src/index.css | grep -c '#0d1117'` ≥ 1 (new page bg present); `grep -c -- '--color-ev-teal-light: #00c8d7' src/index.css` = 1; `grep -c -- '--ev-teal: #00657c' src/index.css` = 1 (light teal UNCHANGED); `grep -c -- '--ev-light-blue: #59b0c4' src/index.css` = 1 (light token UNCHANGED).
    - source-assertion: `grep -c '@source not' src/index.css` = 2 (build-safety lines preserved).
    - source-assertion: `grep -c -- '--font-sans' src/index.css` = 1 and `grep -c -- '--font-display' src/index.css` = 1.
    - behavior: `:root` light block (L29-36) is unchanged from pre-edit (only `@theme` dark values + new tokens changed).
  </acceptance_criteria>
  <done>@theme dark token values match the Figma palette, new slate/text/font tokens exist, light :root + @source-not lines untouched, build green.</done>
</task>

<task type="auto">
  <name>Task 3: Load Inter + Manrope per the approved path and switch the global default to var(--font-sans)</name>
  <files>src/main.jsx, src/index.css, package.json</files>
  <read_first>
    - src/main.jsx (full — L6 existing import "./index.css" side-effect pattern)
    - src/index.css (L2 existing Manrope @import; L42 global font default)
    - .planning/phases/169-dark-mode-design-system-foundation/169-PATTERNS.md (§src/main.jsx — self-host import shape + CDN alternative; "do not remove Manrope load until replacement confirmed")
    - .planning/phases/169-dark-mode-design-system-foundation/169-RESEARCH.md (§Font Wiring — weights 400/600 Inter, 600/700 Manrope)
  </read_first>
  <action>
    Implement the font load using the path approved in Task 1.
    SELF-HOST branch: install `@fontsource/inter @fontsource/manrope`, then add side-effect imports in
    src/main.jsx (mirroring the existing L6 `import "./index.css";` shape) for the weights actually used:
    Inter 400 + 600, Manrope 600 + 700. Remove the now-redundant Google Fonts Manrope @import from
    src/index.css L2 ONLY after confirming the self-hosted Manrope renders.
    CDN branch: do not install anything; extend the existing src/index.css L2 @import to add Inter
    (`Inter:wght@400;600`) alongside the existing Manrope @import; leave main.jsx unchanged.
    In BOTH branches, after the font is confirmed loaded, change src/index.css L42 `html, body`
    `font-family: 'Manrope', sans-serif;` → `font-family: var(--font-sans);` so the app default is Inter
    (D-05); the `.pac-container`/`.pac-item` Manrope rules (L51+) may stay as-is (out of scope). Keep the
    `@source not` lines untouched. No fenced code in this plan; follow the main.jsx analog in 169-PATTERNS.md.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - build-command: `npm run build` exits 0 with fonts bundled (self-host) or @import resolving (CDN).
    - source-assertion: `grep -Eic 'fontsource|Inter:wght' src/main.jsx src/index.css` ≥ 1 (Inter is loaded by one of the two paths).
    - source-assertion: `grep -c 'var(--font-sans)' src/index.css` ≥ 1 (global default repointed to Inter).
    - behavior: Manrope is still available (self-host import present OR @import still includes Manrope) — the app does not lose its current display font.
  </acceptance_criteria>
  <done>Inter + Manrope load via the approved path, the global default font is var(--font-sans) (Inter), Manrope remains available, build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New dev dependency (`@fontsource/*`) enters the build if the self-host path is chosen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-SC | Tampering | `@fontsource/inter` + `@fontsource/manrope` npm install | mitigate | Task 1 blocking-human legitimacy gate — `npm view` both packages + confirm github.com/fontsource/fontsource before install; CDN fallback installs nothing |

No other new threats: this is a client-side CSS/token/theming + font-loading change only. No untrusted input, no auth, no data paths, no crypto. Light-mode tokens and build-safety `@source` lines are explicitly preserved.
</threat_model>

<verification>
- `npm run build` green after each task (Tailwind @theme + @source compile intact).
- `npm run test` green (logic suite unaffected by token/font change).
- Light-mode no-regression guard: `:root` light vars and `--ev-teal #00657c` / `--ev-light-blue #59b0c4` unchanged.
</verification>

<success_criteria>
- Dark `@theme` token VALUES match the Figma palette; token NAMES unchanged (single source of truth — DARK-01 / SC#2).
- Inter + Manrope wired as `--font-*` tokens; app default is Inter (D-05).
- Light mode untouched (SC#5): no light token value changed, @source-not lines preserved.
- Build + existing tests green.
</success_criteria>

<output>
Create `.planning/phases/169-dark-mode-design-system-foundation/169-01-SUMMARY.md` when done.
Record: final font-load path chosen (self-host vs CDN), the exact token values written, and confirmation that the light `:root` block + `@source not` lines are unchanged.
</output>
