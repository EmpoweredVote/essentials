---
phase: 81-or-playbook-retrospective-v8-0-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LOCATION-ONBOARDING.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "LOCATION-ONBOARDING.md Cities Onboarded table has 2 new OR rows (Oregon state + Portland) appended after the existing CA rows"
    - "An 'Oregon Quick Reference' H2 section sits between the Cities Onboarded table and Step 1 listing 8 OR-specific traps with step pointers"
    - "All 9 OR-specific GOTCHAs (OR-1 through OR-9 from 81-RESEARCH.md) are placed inline in the correct steps using the [STATE-SPECIFIC: OR] tag pattern (or appended as OR annotations to existing GOTCHAs per the placement map)"
    - "Step 7 pitfall table has at least 4 new OR-specific rows (Portland council not in TIGER, Portland charter structure, portland.gov WAF headshots, PowerShell Unicode)"
    - "All existing Cambridge, Maine, and California content is preserved (no regression)"
  artifacts:
    - path: "LOCATION-ONBOARDING.md"
      provides: "Updated playbook with Oregon Quick Reference block, 2 new Cities Onboarded rows (Oregon state + Portland), 9 OR GOTCHAs inline across Steps 1-7, and 4+ new pitfall table rows"
      contains: "Oregon Quick Reference"
  key_links:
    - from: "LOCATION-ONBOARDING.md Cities Onboarded table"
      to: "Oregon Quick Reference block"
      via: "section placement: Quick Reference immediately follows the table (after the California Quick Reference block added in Phase 78)"
      pattern: "Oregon Quick Reference"
    - from: "Oregon Quick Reference table"
      to: "inline [STATE-SPECIFIC: OR] GOTCHA callouts"
      via: "step number pointers in 'See Step' column"
      pattern: "STATE-SPECIFIC: OR"
    - from: "Step 7 Common Pitfalls table"
      to: "OR-specific failure modes"
      via: "new pitfall rows for Portland council/TIGER, charter structure, WAF headshots, PowerShell Unicode"
      pattern: "PortlandMaps|portland.gov WAF|char.0x"
---

<objective>
Update LOCATION-ONBOARDING.md with all 9 OR-specific GOTCHAs discovered during v8.0 (Phases 72-80), plus a top-level "Oregon Quick Reference" summary block, plus 2 new rows in the Cities Onboarded table (Oregon state + Portland), plus 4+ new rows in the Step 7 pitfall table. Mirrors the Phase 78-01 (CA playbook retrospective) pattern exactly.

Purpose: Phase 81 is the v8.0 playbook retrospective. The playbook is the cold-start contract for the next state onboarding. 9 OR-specific traps were discovered during v8.0 — they currently live only in phase SUMMARY.md files and STATE.md context. Without this update, the next agent will re-discover the same traps the hard way. The "Oregon Quick Reference" block exists so a future agent can pre-flight scan all OR traps before starting any OR work (e.g., Eugene, Salem, Bend) the same way the California Quick Reference works for CA cities.

Output: Updated LOCATION-ONBOARDING.md (currently ~424 lines after Phase 78 update) with: (a) Cities Onboarded rows for Oregon (state) + Portland appended after the existing California rows; (b) a new "Oregon Quick Reference" H2 section between the Cities Onboarded table and the "California Quick Reference" block — or between the "California Quick Reference" block and "## Step 1" — read the file first to pick the correct insertion point; (c) 9 inline GOTCHAs added across Steps 1-7 (mix of new `> [GOTCHA]` callouts and annotations to existing GOTCHAs per 81-RESEARCH.md placement map); and (d) 4 new rows appended to the Step 7 Common Pitfalls table.
</objective>

<execution_context>
@C:\Users\Chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-or-playbook-retrospective-v8-0-close/81-RESEARCH.md

# Playbook file being updated — primary deliverable
@LOCATION-ONBOARDING.md

# Phase 78 analog: prior playbook retrospective with the exact same edit approach
@.planning/phases/78-ca-playbook-retrospective/78-01-PLAN.md
@.planning/phases/78-ca-playbook-retrospective/78-01-SUMMARY.md
@.planning/phases/78-ca-playbook-retrospective/78-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 2 OR rows to Cities Onboarded table and insert Oregon Quick Reference summary block</name>
  <files>LOCATION-ONBOARDING.md</files>
  <read_first>
    - LOCATION-ONBOARDING.md (the file being edited — read in full before any Edit; line 30 begins the Cities Onboarded table; line 45 (Berkeley) is the last existing row; the California Quick Reference H2 appears at line 49; Step 1 begins at line 69)
    - .planning/phases/81-or-playbook-retrospective-v8-0-close/81-RESEARCH.md (lines 186-208 contain the exact Oregon and Portland row content for the Cities Onboarded table; lines 264-290 contain the exact Oregon Quick Reference block markdown verbatim)
    - .planning/phases/78-ca-playbook-retrospective/78-01-SUMMARY.md (canonical Phase 78 execution log — shows how the California Quick Reference block was inserted; mirror that approach for Oregon)
  </read_first>
  <action>
Use the Edit tool to make two targeted insertions into LOCATION-ONBOARDING.md. Do NOT use Write — the file is ~424 lines of working content (Phase 78 baseline) that must be preserved.

**A. Append 2 new rows to the Cities Onboarded table.** The table's last existing row is the Berkeley CA row at line 45 (`| Berkeley | CA | 2026-05-22 | rcv ...`). Insert the 2 new OR rows immediately after the Berkeley row, BEFORE the `---` horizontal rule that ends the table (line 47).

Row content sources (use verbatim from 81-RESEARCH.md):
- Oregon (state) row: 81-RESEARCH.md lines 192-198 — the row content is the Field/Value table in the research file; assemble it into a single pipe-delimited markdown row with the 5 columns (City | State | Onboarded | Election method | Notable patterns) matching the existing table format
- Portland row: 81-RESEARCH.md lines 202-208 — same approach; assemble into a single pipe-delimited row

Order: Oregon (state) row first, then Portland row (chronological by onboarding date: 2026-05-30 → 2026-05-30; or alphabetical; either matches the Phase 78 chronological-onboarding pattern since both ship the same day).

Row content (assemble from 81-RESEARCH.md cells into a single pipe-delimited row each):

Oregon (state):
- City: `Oregon (state)`
- State: `OR`
- Onboarded: `2026-05-30`
- Election method: `plurality (state + federal); rcv (Portland City Council, Auditor)`
- Notable patterns: copy verbatim from 81-RESEARCH.md line 198 (covers constitutional officers all voter-elected unlike ME, cd119 TIGER key, sos.oregon.gov Blue Book headshot source, external_id schemes, 241 G4110 cities, oregonlegislature.gov MemberPhotos with disambiguation suffixes)

Portland:
- City: `Portland`
- State: `OR`
- Onboarded: `2026-05-30`
- Election method: `rcv (City Council 12 seats, City Auditor)`
- Notable patterns: copy verbatim from 81-RESEARCH.md line 208 (2024 charter reform 4 districts × 3 seats; PortlandMaps ArcGIS MapServer Layer 17 NOT TIGER, mtfcc=X0012, outSR=4326+ST_MakeValid; portland.gov WAF + 1_1_320w style URLs; gov name 'City of Portland, Oregon, US'; D3+D4+Auditor on 2026 ballot, Mayor+D1+D2 on 2028; ext_ids -690001..-690004 + -690010..-690021)

Both rows must match the existing 5-column pipe-delimited format used by Cambridge, Portland ME, Lewiston, Maine state, California state, Berkeley, etc. Do NOT shorten Notable patterns — the existing rows are also long. Do NOT collapse columns.

**B. Insert "Oregon Quick Reference" H2 section.** Per Phase 78 precedent, both Quick Reference blocks live between the Cities Onboarded table and Step 1. Read LOCATION-ONBOARDING.md lines 46-70 before editing to pick the correct insertion point — recommended placement is IMMEDIATELY AFTER the existing "California Quick Reference" block's trailing `---` rule (around line 66) and BEFORE the `## Step 1: Government Structure Research` H2 (line 69). This puts Oregon QR after California QR, both before Step 1, which matches the chronological "newer states added later" reading order.

The Oregon Quick Reference section must contain (copy from 81-RESEARCH.md lines 269-284 verbatim, then add trailing horizontal rule):

- An H2 heading: `## Oregon Quick Reference`
- A one-sentence introduction: `**Read this before starting any OR city or state work.** These traps are OR-specific — general playbook guidance above does not warn for them.`
- A markdown table with 3 columns: `Trap | See Step | One-Line Summary`
- 8 table rows, one per trap, using the exact wording from 81-RESEARCH.md lines 276-283
- A trailing `---` horizontal rule before `## Step 1` begins

The 8 trap rows (ordered as in 81-RESEARCH.md lines 276-283, do not reorder):
1. Portland council NOT in TIGER → Step 3
2. Portland 2024 charter reform → Step 1, 2
3. All OR constitutional officers voter-elected → Step 1, 5
4. portland.gov WAF blocks headshots → Step 4, 7
5. PowerShell Unicode mangling → Step 6
6. Federal officials may pre-exist → Step 5
7. Portland 2026 ballot: D3/D4/Auditor only → Step 2, 6
8. G4110 count needs dry-run confirmation → Step 3

Use copy-from-source for content, then verify by reading the inserted section back. Do NOT generate new wording for trap descriptions — 81-RESEARCH.md is canonical.

**Constraints:**
- Do NOT modify any rows above line 45 (existing Cambridge + Maine + California rows must remain unchanged)
- Do NOT change the table header row
- Do NOT modify the existing California Quick Reference block (Phase 78 deliverable — must remain intact)
- Insert the Oregon Quick Reference block AFTER the California Quick Reference's trailing `---` rule and BEFORE the `## Step 1` H2 — verify the boundary with Read after editing
- The H2 must be exactly `## Oregon Quick Reference` (no emoji, no period)
- After both edits, the Oregon Quick Reference section count is exactly 1 and the California Quick Reference section count is still exactly 1
  </action>
  <acceptance_criteria>
    - LOCATION-ONBOARDING.md contains the exact substring `| Oregon (state) | OR | 2026-05-30 |` (Cities Onboarded row 1 of 2)
    - LOCATION-ONBOARDING.md contains the exact substring `| Portland | OR | 2026-05-30 |` (Cities Onboarded row 2 of 2)
    - LOCATION-ONBOARDING.md contains exactly one occurrence of `## Oregon Quick Reference` (count via `grep -c "^## Oregon Quick Reference$"` returns 1)
    - LOCATION-ONBOARDING.md still contains exactly one occurrence of `## California Quick Reference` (Phase 78 block preserved)
    - LOCATION-ONBOARDING.md contains the Oregon Quick Reference introduction `Read this before starting any OR city or state work.`
    - The Oregon Quick Reference table contains all 8 trap labels: `Portland council NOT in TIGER`, `Portland 2024 charter reform`, `OR constitutional officers voter-elected` (or `All OR constitutional officers voter-elected`), `portland.gov WAF`, `PowerShell Unicode`, `Federal officials may pre-exist`, `Portland 2026 ballot`, `G4110 count needs dry-run` (each verifiable via grep returning at least 1)
    - The existing Cambridge row (`| Cambridge | MA | 2026-05-17 |`) is still present (no regression)
    - The existing Portland ME row (`| Portland | ME | 2026-05-19 | rcv |`) is still present (no regression — note: must distinguish ME Portland row from new OR Portland row by the state column)
    - The existing Maine state row (`| Maine (state) | ME | 2026-05-20 |`) is still present
    - The existing California state row (`| California (state) | CA | 2026-05-21 |`) is still present
    - The existing Berkeley row (`| Berkeley | CA | 2026-05-22 |`) is still present
    - The Oregon Quick Reference H2 sits between the existing California Quick Reference block and `## Step 1: Government Structure Research` (verify with line-number reads after edit)
    - File line count is greater than 424 (the Phase 78 baseline) — confirms additions without unintended deletions
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && [ "$(grep -c '^## Oregon Quick Reference$' LOCATION-ONBOARDING.md)" -eq 1 ] && [ "$(grep -c '^## California Quick Reference$' LOCATION-ONBOARDING.md)" -eq 1 ] && grep -q "| Oregon (state) | OR | 2026-05-30 |" LOCATION-ONBOARDING.md && grep -q "| Portland | OR | 2026-05-30 |" LOCATION-ONBOARDING.md && grep -q "Read this before starting any OR city or state work" LOCATION-ONBOARDING.md && grep -q "| Cambridge | MA | 2026-05-17 |" LOCATION-ONBOARDING.md && grep -q "| Portland | ME | 2026-05-19 | rcv |" LOCATION-ONBOARDING.md && grep -q "| California (state) | CA | 2026-05-21 |" LOCATION-ONBOARDING.md && grep -q "| Berkeley | CA | 2026-05-22 |" LOCATION-ONBOARDING.md && [ "$(wc -l < LOCATION-ONBOARDING.md)" -gt 424 ] && echo PASS</automated>
  </verify>
  <done>
LOCATION-ONBOARDING.md Cities Onboarded table has the existing 15 rows (Phase 78 baseline) plus 2 new OR rows (Oregon state + Portland), 17 total. A new H2 section "Oregon Quick Reference" sits between the existing California Quick Reference block and Step 1, containing the introduction sentence plus an 8-row trap → step → summary table. All 8 trap labels match 81-RESEARCH.md verbatim. No regressions to existing Cambridge, Maine, or California rows; California Quick Reference block unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert 9 inline OR-specific GOTCHAs across Steps 1-7 using [STATE-SPECIFIC: OR] tag pattern</name>
  <files>LOCATION-ONBOARDING.md</files>
  <read_first>
    - LOCATION-ONBOARDING.md (file being edited — Task 1 already added the Cities Onboarded rows and Oregon Quick Reference; this task adds inline GOTCHAs to Steps 1-7)
    - .planning/phases/81-or-playbook-retrospective-v8-0-close/81-RESEARCH.md (lines 23-131 contain the full text of all 9 OR GOTCHAs with problem, solution, and OR-specific example — this is the canonical source; lines 173-182 contain the inline placement map by Step)
    - .planning/phases/78-ca-playbook-retrospective/78-01-SUMMARY.md (canonical CA execution pattern — confirms format `> [GOTCHA] **[STATE-SPECIFIC: CA] Label:** problem... solution... CA example...`; mirror this pattern for OR)
  </read_first>
  <action>
Use the Edit tool to make 9 targeted insertions into LOCATION-ONBOARDING.md. Some insertions are new `> [GOTCHA]` blockquote lines; others are OR annotations appended to existing GOTCHAs (per the 81-RESEARCH.md placement map at lines 173-182).

**Format conventions (must match existing playbook style, including Phase 78 CA precedent):**
- New OR-specific GOTCHA line begins: `> [GOTCHA] **[STATE-SPECIFIC: OR] <short label>:** <problem>. <solution>. <OR example referencing phase>.`
- Annotation to existing GOTCHA: append a new sentence inside the existing blockquote line beginning with `**[STATE-SPECIFIC: OR]** <annotation>. <OR example>.`
- Reference the specific source phase in every example (Phase 72, 73, 74, 75, 76, 77, 79) so future agents can trace back if needed
- Use the exact body content from 81-RESEARCH.md lines 23-124 — paraphrase only for length, never invent new facts

**Placement map (from 81-RESEARCH.md lines 173-182):**

| GOTCHA | Step | Insertion approach | Format |
|--------|------|--------------------|--------|
| OR-2 (charter structure) | Step 1 — after the existing CA pre-existing seed GOTCHA at ~line 84 | New `> [GOTCHA]` | New |
| OR-5 (OR officers voter-elected) | Step 5 — annotate the existing "Legislature-elected offices" GOTCHA in Step 5 (around line 258) | Append OR annotation inside the existing GOTCHA blockquote | Annotation |
| OR-6 (federal pre-flight) | Step 5 — new GOTCHA after OR-5 annotation, before the existing CA external_id GOTCHA | New `> [GOTCHA]` | New |
| OR-1 (Portland council not in TIGER) | Step 3 — new GOTCHA placed after the existing CA "DataSF vs ArcGIS" GOTCHA (around line 205) | New `> [GOTCHA]` | New |
| OR-7 (G4110 dry-run required) | Step 3 — annotate the existing TIGER cd119 GOTCHA (around line 197) | Append OR annotation inside the existing GOTCHA blockquote | Annotation |
| OR-9 (staggered ballot terms) | Step 2 — new GOTCHA placed after the existing CA jungle primary GOTCHA (around line 126) | New `> [GOTCHA]` | New |
| OR-4 (PowerShell Unicode) | Step 6 — annotate the existing PowerShell bulk-seed generator GOTCHA (around line 310, `[PATTERN] PowerShell bulk-seed generator...`) | Append OR annotation directly within the existing PATTERN bullet describing the [char]0xNNNN escape sequence requirement | Annotation |
| OR-3 (portland.gov WAF / 1_1_320w) | Step 4 — new GOTCHA placed after the existing CA AEM/CQ5 GOTCHA (around line 238) | New `> [GOTCHA]` | New |
| OR-8 (Portland City Hall → D4) | Step 3 — short inline note appended inside the Portland row block of the Cities Onboarded section is acceptable; OR alternatively place a new `> [GOTCHA]` in Step 3 referencing "always use confirmed routing result, never assume". Choose ONE placement; mark which in the SUMMARY | New `> [GOTCHA]` or Annotation (executor's choice) | Either |

**Content sources (do NOT rewrite — copy from 81-RESEARCH.md verbatim or near-verbatim, condensed to single-line blockquote where natural):**
- OR-1 from 81-RESEARCH.md lines 27-33 (must include: PortlandMaps ArcGIS MapServer Layer 17, per-OBJECTID fetch, outSR=4326, ST_MakeValid, mtfcc=X0012, geo_ids portland-or-council-district-{1-4}, City Hall (-122.6794, 45.5231) → portland-or-council-district-4)
- OR-2 from 81-RESEARCH.md lines 38-42 (must include: 2024 charter reform, 4 districts × 3 seats = 12 council seats, RCV, authoritative roster from portland.gov/auditor/elections/elected-city-officials, government name 'City of Portland, Oregon, US')
- OR-3 from 81-RESEARCH.md lines 47-51 (must include: portland.gov WAF blocks /public/, use Drupal 1_1_320w style derivative URLs, extract itok token from profile page HTML, 320x320 WebP, center-crop to 4:5 then resize to 600x750)
- OR-4 from 81-RESEARCH.md lines 56-63 (must include: PowerShell 5.1 reads no-BOM as ANSI codepage, use [char]0xNNNN escape sequences in roster hashtable, [System.IO.File]::WriteAllLines with UTF8Encoding($false) to disable BOM, specific examples: HD-38 Nguyễn [char]0x1EBF, HD-45 Thuy Tran, HD-22 Munoz [char]0x00F1)
- OR-5 from 81-RESEARCH.md lines 67-72 (must include: unlike Maine, OR's 5 constitutional officers — Governor, AG, SoS, Treasurer, Labor Commissioner — are ALL voter-elected, is_appointed_position=false, race rows required for all 5; Labor Commissioner is OR's 5th office not present in Maine)
- OR-6 from 81-RESEARCH.md lines 77-83 (must include: pre-flight SELECT before INSERT, OR senators Wyden + Merkley pre-existed under -400065/-400066, UPDATE external_id pattern not INSERT, general rule: pre-flight by senator name)
- OR-7 from 81-RESEARCH.md lines 87-92 (must include: estimated 242 vs actual 241, MtfccAssertionError from dry-run gives actual count, update all 3 files: loader + verify SQL + smoke test)
- OR-8 from 81-RESEARCH.md lines 96-100 (must include: Portland City Hall (-122.6794, 45.5231) → portland-or-council-district-4, NOT District 1; always use confirmed routing from DB, never assume)
- OR-9 from 81-RESEARCH.md lines 105-109 (must include: 2024 charter staggered terms, D3/D4/Auditor on 2026 ballot, Mayor+D1+D2 on 2028 ballot 4-year terms, 7 race rows total, OFFSET 0/1/2 pattern for 3 distinct office_ids per district)

**Concrete behavior requirements:**
- OR-1 GOTCHA body must include the literal URL fragment `portlandmaps.com/arcgis/rest/services` AND the geo_id pattern `portland-or-council-district-`
- OR-2 GOTCHA body must include the literal authoritative roster URL `portland.gov/auditor/elections/elected-city-officials` AND the government name `City of Portland, Oregon, US`
- OR-3 GOTCHA body must include the literal Drupal style identifier `1_1_320w` AND the token name `itok`
- OR-4 GOTCHA body/annotation must include the literal escape sequence pattern `[char]0x` AND at least one Vietnamese-name example reference (Nguyễn or Thuy Tran)
- OR-9 GOTCHA body must include the literal phrase `charter reform` AND the term-stagger description (D3/D4/Auditor on 2026, Mayor+D1+D2 on 2028)
- Every OR GOTCHA must include `[STATE-SPECIFIC: OR]` in the bold label (for new GOTCHAs) or in the annotation prefix (for appended annotations)

**Constraints:**
- Use Edit (not Write) for every change — preserve all existing content
- Do NOT remove or alter any existing Cambridge, Maine, or California GOTCHA, example, or pitfall content
- Do NOT add OR examples inside Maine or California examples (do not mix state contexts within a single blockquote — OR gets its own callouts or appended-annotation lines)
- After all 9 edits, `grep -c "STATE-SPECIFIC: OR" LOCATION-ONBOARDING.md` returns at least 7 (one per new GOTCHA + each annotation; targeting ≥7 because some annotations may share a blockquote with the existing GOTCHA)
- After edits, the existing Maine `[STATE-SPECIFIC]` and `[STATE-SPECIFIC: Maine]` markers AND the 11 CA `[STATE-SPECIFIC: CA]` markers must still be present
  </action>
  <acceptance_criteria>
    - `grep -c "STATE-SPECIFIC: OR" LOCATION-ONBOARDING.md` returns >= 7 (one per OR GOTCHA — exact count depends on annotation vs new-GOTCHA placement; minimum 7 reflects 6 new GOTCHAs + at least 1 annotation marker line)
    - `grep -c "STATE-SPECIFIC: CA" LOCATION-ONBOARDING.md` returns >= 11 (Phase 78 baseline preserved)
    - `grep -c "cd119" LOCATION-ONBOARDING.md` still returns >= 1 (Maine TIGER cd119 GOTCHA not regressed)
    - OR-1 phrase present: `grep -q "PortlandMaps\|ArcGIS MapServer Layer 17" LOCATION-ONBOARDING.md` exits 0 AND `grep -q "portland-or-council-district-" LOCATION-ONBOARDING.md` exits 0 AND `grep -q "ST_MakeValid" LOCATION-ONBOARDING.md` exits 0
    - OR-2 phrase present: `grep -q "charter reform" LOCATION-ONBOARDING.md` exits 0 AND `grep -q "portland.gov/auditor/elections/elected-city-officials" LOCATION-ONBOARDING.md` exits 0 AND `grep -q "City of Portland, Oregon, US" LOCATION-ONBOARDING.md` exits 0
    - OR-3 phrase present: `grep -q "1_1_320w" LOCATION-ONBOARDING.md` exits 0 AND `grep -q "itok" LOCATION-ONBOARDING.md` exits 0
    - OR-3 portland.gov WAF mention: `grep -q "portland.gov WAF\|WAF blocks" LOCATION-ONBOARDING.md` exits 0
    - OR-4 phrase present: `grep -q "\[char\]0x" LOCATION-ONBOARDING.md` exits 0 (literal escape sequence form)
    - OR-4 Vietnamese-name reference: `grep -q "Nguy\|Thuy Tran" LOCATION-ONBOARDING.md` exits 0 (Unicode-aware grep; matches `Nguyễn` partial OR `Thuy Tran` literal)
    - OR-5 phrase present: `grep -q "all voter-elected\|all 5 voter-elected\|Labor Commissioner" LOCATION-ONBOARDING.md` exits 0
    - OR-6 phrase present: `grep -q "Wyden\|Merkley" LOCATION-ONBOARDING.md` exits 0 AND `grep -q "pre-flight" LOCATION-ONBOARDING.md` exits 0
    - OR-7 phrase present: `grep -q "MtfccAssertionError\|dry-run\|241" LOCATION-ONBOARDING.md` exits 0
    - OR-8 phrase present: `grep -q "portland-or-council-district-4" LOCATION-ONBOARDING.md` exits 0
    - OR-9 phrase present: `grep -q "D3.*D4.*Auditor\|staggered" LOCATION-ONBOARDING.md` exits 0
    - No existing Cambridge content removed: `grep -c "Cambridge" LOCATION-ONBOARDING.md` returns the same count as before edits (or higher)
    - No existing Maine content removed: `grep -c "Maine\|cd119" LOCATION-ONBOARDING.md` returns the same count as before edits (or higher) — note: "Portland" count is OK to grow because the OR Portland row was added in Task 1
    - No existing California content removed: `grep -c "STATE-SPECIFIC: CA" LOCATION-ONBOARDING.md` returns at least 11
    - File line count is GREATER than the Phase 78 baseline (~424 lines) — confirms additions without unintended deletions
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && [ "$(grep -c 'STATE-SPECIFIC: OR' LOCATION-ONBOARDING.md)" -ge 7 ] && [ "$(grep -c 'STATE-SPECIFIC: CA' LOCATION-ONBOARDING.md)" -ge 11 ] && grep -q "portland-or-council-district-" LOCATION-ONBOARDING.md && grep -q "ST_MakeValid" LOCATION-ONBOARDING.md && grep -q "charter reform" LOCATION-ONBOARDING.md && grep -q "portland.gov/auditor/elections/elected-city-officials" LOCATION-ONBOARDING.md && grep -q "City of Portland, Oregon, US" LOCATION-ONBOARDING.md && grep -q "1_1_320w" LOCATION-ONBOARDING.md && grep -q "itok" LOCATION-ONBOARDING.md && grep -qF "[char]0x" LOCATION-ONBOARDING.md && grep -qE "Nguy|Thuy Tran" LOCATION-ONBOARDING.md && grep -qE "Labor Commissioner|all voter-elected|all 5 voter-elected" LOCATION-ONBOARDING.md && grep -qE "Wyden|Merkley" LOCATION-ONBOARDING.md && grep -qE "MtfccAssertionError|dry-run" LOCATION-ONBOARDING.md && grep -q "portland-or-council-district-4" LOCATION-ONBOARDING.md && grep -qE "staggered|D3.*D4.*Auditor" LOCATION-ONBOARDING.md && [ "$(grep -c 'cd119' LOCATION-ONBOARDING.md)" -ge 1 ] && [ "$(wc -l < LOCATION-ONBOARDING.md)" -gt 424 ] && echo PASS</automated>
  </verify>
  <done>
LOCATION-ONBOARDING.md contains at least 7 `[STATE-SPECIFIC: OR]` markers across Steps 1-7. New `> [GOTCHA]` blockquotes added for OR-1, OR-2, OR-3, OR-6, OR-9 (and OR-8 if executor chose new-GOTCHA over annotation). OR-5 and OR-7 are annotations appended to existing Maine/general GOTCHAs. OR-4 is an annotation/extension of the existing PowerShell PATTERN bullet in Step 6 documenting the [char]0xNNNN escape sequence requirement. All required literal strings present: PortlandMaps, ST_MakeValid, charter reform, portland.gov/auditor/elections/elected-city-officials, 1_1_320w, itok, [char]0x, Vietnamese name reference, Labor Commissioner, Wyden/Merkley, MtfccAssertionError/dry-run, portland-or-council-district-4, staggered terms. All existing Cambridge, Maine, and California GOTCHAs preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 4 new OR-specific rows to Step 7 Common Pitfalls table</name>
  <files>LOCATION-ONBOARDING.md</files>
  <read_first>
    - LOCATION-ONBOARDING.md (the file being edited — Step 7 pitfall table header is at line 343; existing rows include the 14 (9 original + 5 CA added in Phase 78) rows ending around line 366)
    - .planning/phases/81-or-playbook-retrospective-v8-0-close/81-RESEARCH.md (lines 31-110 contain the Pitfall table entry strings inline within each GOTCHA's "Pitfall table entry:" line — these are the rows to add)
    - .planning/phases/78-ca-playbook-retrospective/78-01-SUMMARY.md (canonical Phase 78 pattern — confirms format `| <pitfall> | <how to catch it> |`)
  </read_first>
  <action>
Use the Edit tool to append 4 new rows to the Step 7 Common Pitfalls table in LOCATION-ONBOARDING.md.

The existing pitfall table starts at line 343 (`| Pitfall | How to Catch It |`) and runs through line 366 (last CA row `| CA external_id range collision | ...`). Append the 4 new OR rows BEFORE the blank line that ends the table (or the `---` rule that separates it from Step 8), so they extend the existing table rather than create a new one.

**Source rows (sourced from the "Pitfall table entry:" lines in 81-RESEARCH.md):**

Row 1 — from 81-RESEARCH.md line 33 (OR-1 pitfall entry):
```
| Portland council district boundaries not in TIGER | Source from PortlandMaps ArcGIS MapServer Layer 17 per-OBJECTID; always add outSR=4326 and ST_MakeValid |
```

Row 2 — from 81-RESEARCH.md line 42 (OR-2 pitfall entry):
```
| Portland council structure seeded from pre-2025 charter | 2024 charter reform: 4 districts × 3 seats = 12 council seats; official roster from portland.gov/auditor/elections/elected-city-officials not Wikipedia |
```

Row 3 — from 81-RESEARCH.md line 51 (OR-3 pitfall entry):
```
| portland.gov headshots not downloadable from /public/ direct paths | Use Drupal 1_1_320w style CDN URLs (extract itok token from profile page HTML); photo_origin_url records canonical path |
```

Row 4 — from 81-RESEARCH.md line 63 (OR-4 pitfall entry):
```
| PowerShell Unicode encoding: non-ASCII names mangled in PS 5.1 scripts | Use [char]0xNNNN escape sequences for all diacritical characters in .ps1 roster hashtables |
```

Each row must match the existing 2-column pipe-delimited format. Do NOT add a third column. Do NOT add OR examples to the existing Cambridge/Maine/CA pitfall rows (those rows describe general pitfalls; OR-specific rows go alongside as their own rows).

**Optional additional rows (executor may add up to 2 more from 81-RESEARCH.md if they fit naturally):**
- OR-5 pitfall entry from line 73: `| OR constitutional officers modeled as appointed (Maine pattern) | All 5 OR officers are voter-elected; is_appointed_position=false and race rows required for all 5 |`
- OR-9 pitfall entry from line 110: `| Portland 2026 races include all 12 council seats | Staggered terms: D3+D4+Auditor on 2026 ballot; Mayor+D1+D2 on 2028 ballot (4-year terms from 2025 charter reform) |`

Minimum required: 4 rows (OR-1, OR-2, OR-3, OR-4). The OR-5 and OR-9 rows are recommended but not strictly required — they reinforce inline GOTCHAs from Task 2.

**Constraints:**
- Use Edit (not Write)
- Do NOT modify the table header row at line 343
- Do NOT remove or alter any of the existing 14 pitfall rows (9 original + 5 CA from Phase 78)
- Append all 4 (or up to 6) rows as one Edit operation
- After edits, `grep` confirms each of the 4 required OR pitfall rows is present
  </action>
  <acceptance_criteria>
    - LOCATION-ONBOARDING.md contains the exact substring `| Portland council district boundaries not in TIGER |` (OR-1 pitfall)
    - LOCATION-ONBOARDING.md contains the exact substring `| Portland council structure seeded from pre-2025 charter |` (OR-2 pitfall)
    - LOCATION-ONBOARDING.md contains the exact substring `| portland.gov headshots not downloadable from /public/ direct paths |` (OR-3 pitfall)
    - LOCATION-ONBOARDING.md contains the exact substring `| PowerShell Unicode encoding: non-ASCII names mangled in PS 5.1 scripts |` (OR-4 pitfall)
    - All 4 new rows sit between the existing pitfall table header and the start of `## Step 8` (verify by reading the Step 7 section after edit; all 4 new rows appear within that range)
    - Existing pitfall rows preserved: `grep -q "Mayor modeled as LOCAL_EXEC when actually council-selected" LOCATION-ONBOARDING.md` matches
    - Existing pitfall rows preserved: `grep -q "Wrong geo_id (county FIPS instead of city place code)" LOCATION-ONBOARDING.md` matches
    - Existing CA pitfall rows preserved (Phase 78 baseline): `grep -q "CA jungle primary modeled as separate D/R primaries" LOCATION-ONBOARDING.md` matches AND `grep -q "AEM/CQ5 CMS headshots not extractable by WebFetch" LOCATION-ONBOARDING.md` matches
    - Total pitfall table row count increases by at least 4 (or up to 6 if executor included the optional OR-5 and OR-9 rows)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -q "| Portland council district boundaries not in TIGER |" LOCATION-ONBOARDING.md && grep -q "| Portland council structure seeded from pre-2025 charter |" LOCATION-ONBOARDING.md && grep -q "| portland.gov headshots not downloadable from /public/ direct paths |" LOCATION-ONBOARDING.md && grep -q "| PowerShell Unicode encoding: non-ASCII names mangled in PS 5.1 scripts |" LOCATION-ONBOARDING.md && grep -q "Mayor modeled as LOCAL_EXEC when actually council-selected" LOCATION-ONBOARDING.md && grep -q "Wrong geo_id (county FIPS instead of city place code)" LOCATION-ONBOARDING.md && grep -q "CA jungle primary modeled as separate D/R primaries" LOCATION-ONBOARDING.md && grep -q "AEM/CQ5 CMS headshots not extractable by WebFetch" LOCATION-ONBOARDING.md && echo PASS</automated>
  </verify>
  <done>
Step 7 Common Pitfalls table has all 14 original Phase-78-baseline rows plus 4 new OR-specific rows (18 total minimum, up to 20 if executor added optional OR-5/OR-9 rows). The 4 required new rows are: Portland council not in TIGER, Portland council pre-2025 charter, portland.gov headshots /public/ paths, PowerShell Unicode encoding. Format matches existing 2-column pipe-delimited rows.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run holistic playbook integrity check:

```bash
cd "C:/Transparent Motivations/essentials"

# 1. All 9 OR GOTCHAs landed (at least 7 [STATE-SPECIFIC: OR] markers, allowing for some annotations sharing a blockquote with the existing GOTCHA line)
test "$(grep -c 'STATE-SPECIFIC: OR' LOCATION-ONBOARDING.md)" -ge 7 || { echo "FAIL: <7 OR markers"; exit 1; }

# 2. Phase 78 CA baseline preserved
test "$(grep -c 'STATE-SPECIFIC: CA' LOCATION-ONBOARDING.md)" -ge 11 || { echo "FAIL: CA baseline regressed"; exit 1; }

# 3. Oregon Quick Reference block exists exactly once
test "$(grep -c '^## Oregon Quick Reference$' LOCATION-ONBOARDING.md)" -eq 1 || { echo "FAIL: OR Quick Ref count != 1"; exit 1; }

# 4. California Quick Reference block still exists exactly once
test "$(grep -c '^## California Quick Reference$' LOCATION-ONBOARDING.md)" -eq 1 || { echo "FAIL: CA Quick Ref count != 1"; exit 1; }

# 5. Both new Cities Onboarded rows present
grep -q "| Oregon (state) | OR | 2026-05-30 |" LOCATION-ONBOARDING.md || { echo "FAIL: missing Oregon state row"; exit 1; }
grep -q "| Portland | OR | 2026-05-30 |" LOCATION-ONBOARDING.md || { echo "FAIL: missing Portland OR row"; exit 1; }

# 6. All 4 required OR pitfall rows present
grep -q "Portland council district boundaries not in TIGER" LOCATION-ONBOARDING.md || { echo "FAIL: OR-1 pitfall"; exit 1; }
grep -q "Portland council structure seeded from pre-2025 charter" LOCATION-ONBOARDING.md || { echo "FAIL: OR-2 pitfall"; exit 1; }
grep -q "portland.gov headshots not downloadable" LOCATION-ONBOARDING.md || { echo "FAIL: OR-3 pitfall"; exit 1; }
grep -q "PowerShell Unicode encoding" LOCATION-ONBOARDING.md || { echo "FAIL: OR-4 pitfall"; exit 1; }

# 7. Required literal strings from inline GOTCHAs all present
grep -q "portland-or-council-district-" LOCATION-ONBOARDING.md || { echo "FAIL: portland-or-council-district- literal"; exit 1; }
grep -q "ST_MakeValid" LOCATION-ONBOARDING.md || { echo "FAIL: ST_MakeValid literal"; exit 1; }
grep -q "charter reform" LOCATION-ONBOARDING.md || { echo "FAIL: charter reform literal"; exit 1; }
grep -q "1_1_320w" LOCATION-ONBOARDING.md || { echo "FAIL: 1_1_320w literal"; exit 1; }
grep -qF "[char]0x" LOCATION-ONBOARDING.md || { echo "FAIL: [char]0x literal"; exit 1; }

# 8. No regression on Cambridge/Maine/CA content
grep -q "Cambridge" LOCATION-ONBOARDING.md && grep -q "cd119" LOCATION-ONBOARDING.md && grep -q "Frey\|Bellows" LOCATION-ONBOARDING.md || { echo "FAIL: Cambridge/Maine regression"; exit 1; }
grep -q "California Quick Reference" LOCATION-ONBOARDING.md && grep -q "sos.ca.gov" LOCATION-ONBOARDING.md || { echo "FAIL: CA Phase 78 baseline regression"; exit 1; }

# 9. File grew (no accidental truncation)
LINES=$(wc -l < LOCATION-ONBOARDING.md)
test "$LINES" -gt 424 || { echo "FAIL: file did not grow beyond Phase 78 baseline (was 424, now $LINES)"; exit 1; }

echo ALL_PASS
```
</verification>

<success_criteria>
- LOCATION-ONBOARDING.md Cities Onboarded table has 2 new OR rows (Oregon state + Portland) appended after the existing 15 rows
- "Oregon Quick Reference" H2 section exists between the California Quick Reference block and Step 1 with 8 trap rows
- All 9 OR GOTCHAs (OR-1 through OR-9 from 81-RESEARCH.md) are present inline at the correct steps with `[STATE-SPECIFIC: OR]` markers (new GOTCHAs or annotations as specified by placement map)
- Step 7 Common Pitfalls table has at least 4 new OR-specific rows (Portland council not in TIGER, charter structure, portland.gov WAF headshots, PowerShell Unicode)
- All existing Cambridge, Maine, and California content preserved (no regressions; California Quick Reference unchanged; 11 CA GOTCHAs unchanged; 5 CA pitfall rows unchanged)
- File line count increased from 424 (Phase 78 baseline) to a higher number reflecting the additions
- Quality bar: a future agent onboarding a new OR city (e.g., Eugene, Salem) can scan the Oregon Quick Reference block, click through to the inline GOTCHAs, and pre-flight all 8 traps before writing any migration
</success_criteria>

<output>
After completion, create `.planning/phases/81-or-playbook-retrospective-v8-0-close/81-01-SUMMARY.md` documenting:
- Final line count of LOCATION-ONBOARDING.md (delta from 424 baseline)
- Confirmation that 2 Cities Onboarded rows landed in the table (Oregon state + Portland)
- Confirmation that Oregon Quick Reference block landed between the California Quick Reference block and Step 1
- For each of OR-1 through OR-9: which step the inline GOTCHA landed in, and whether it was a new `> [GOTCHA]` or an annotation to an existing one
- For OR-8: which placement was chosen (new-GOTCHA or annotation)
- Count of new Step 7 pitfall rows (should be 4 minimum, up to 6 if optional OR-5/OR-9 rows added)
- Any deviations from the placement map (with rationale)
- Confirmation that no Cambridge, Maine, or California content was removed (grep counts before vs. after)
</output>
</content>
</invoke>