---
phase: 108-boston-deep-seed
plan: 03
type: execute
wave: 2
depends_on: ["108-01", "108-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/349_boston_headshots.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-349.ts
  - C:/EV-Accounts/backend/scripts/_boston-headshots-upload.py
autonomous: true
requirements: [MA-DEEP-02]
must_haves:
  truths:
    - "Mayor Wu + all 13 City Councillors have a headshot at 600×750 in the politician_photos bucket"
    - "Available School Committee members have headshots; genuinely unavailable photos are documented as gaps (best-effort)"
    - "Every uploaded headshot is cropped 4:5 then resized to 600×750 (Lanczos, q90) — never stretched"
    - "Every politician_images row has type='default'"
    - "D-21: Council headshots sourced from boston.gov/city-council; School Committee headshots from bostonschoolcommittee.org / bps.boston.gov"
    - "D-22: Each headshot is cropped 4:5 first then resized to 600×750 (Lanczos, q90), uploaded to the politician_photos bucket as {politician_id}-headshot.jpg"
    - "D-23: Best-effort for School Committee — genuinely unavailable SC photos are documented gaps, not blockers"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_boston-headshots-upload.py"
      provides: "Download + crop-4:5 + resize 600x750 Lanczos q90 + upload to politician_photos bucket"
      contains: "600"
    - path: "C:/EV-Accounts/backend/migrations/349_boston_headshots.sql"
      provides: "politician_images rows (type='default') for Boston officials"
      contains: "politician_images"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-349.ts"
      provides: "Apply + count-check runner for migration 349"
      contains: "349"
  key_links:
    - from: "politician_images.url"
      to: "Supabase Storage politician_photos bucket"
      via: "public CDN URL {politician-uuid}-headshot.jpg"
      pattern: "politician_photos/"
    - from: "politician_images.type"
      to: "UI headshot filter"
      via: ".find(img => img.type === 'default')"
      pattern: "'default'"
---

<objective>
Source, process, upload, and register headshots for all 21 Boston officials seeded in Plans 01 and 02: Mayor Wu + 13 City Councillors (boston.gov, verified URLs) + 7 School Committee members (best-effort). Per D-21, council photos come from boston.gov/city-council and School Committee photos from bostonschoolcommittee.org / bps.boston.gov. Per D-22, each photo is cropped to 4:5 first, resized to 600×750 (Lanczos, q90), uploaded to the politician_photos bucket as `{politician-uuid}-headshot.jpg`, and registered in essentials.politician_images with type='default', photo_license='public_domain'.

Purpose: Closes MA-DEEP-02 — all Boston city officials + School Committee have headshots at 600×750 in Supabase Storage.
Output: `_boston-headshots-upload.py` (download/process/upload), `349_boston_headshots.sql` (politician_images rows), `_apply-migration-349.ts` (apply runner).

Best-effort for School Committee (D-23): a genuinely unavailable SC photo is a documented gap, NOT a blocker. Council headshots (14) are non-negotiable — all URLs are verified in RESEARCH.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/108-boston-deep-seed/108-CONTEXT.md
@.planning/phases/108-boston-deep-seed/108-RESEARCH.md
@.planning/phases/108-boston-deep-seed/108-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download, crop 4:5, resize 600×750, and upload all Boston headshots to politician_photos</name>
  <files>C:/EV-Accounts/backend/scripts/_boston-headshots-upload.py</files>
  <read_first>
    - .planning/phases/108-boston-deep-seed/108-RESEARCH.md (§ Headshot URL Patterns — the 14 verified boston.gov URLs by person; § bostonpublicschools.org School Committee alternate sources; § Common Pitfalls 6 accent handling)
    - .planning/STATE.md (headshot conventions: politician_photos bucket, {politician_id}-headshot.jpg path, type='default', crop 4:5 first then resize, Lanczos q90; MD/VA precedent used Pillow)
    - C:/EV-Accounts/backend/scripts/_apply-migration-325.ts (DB connection env pattern: process.env.DATABASE_URL — mirror it in Python via os.environ for fetching politician UUIDs by external_id)
  </read_first>
  <action>
    Create a Python processing script using Pillow (used in prior MA/VA headshot phases — do NOT install ImageMagick if Pillow is present). For the 14 council officials, download from the verified RESEARCH.md URLs (D-21 — boston.gov/city-council): Mayor Wu = https://www.boston.gov/sites/default/files/img/library/photos/2021/11/wu-headshot-portrait.jpg ; Breadon (D9) breadon-headshot.jpg, Louijeune ruthzee-headshot.png (patterns-stg.boston.gov), Mejia mejia-headshot.png, Murphy murphy-headshot.png, Santana santana-headshot.jpg, Coletta Zapata coletta-headshot.png (patterns-stg.boston.gov), Flynn flynn-headshot.jpg (2018 path), FitzGerald fitzgerald-headshot.png, Worrell worrell-headshot.png, Pepén pepen-headshot.png, Weber weber-headshot.jpg, Culpepper culpepper-headshot.png, Durkan durkan-headshot.png — all under the 360x360 styles path documented in RESEARCH.md except Wu (full-res direct path). For the 7 School Committee members, attempt boston.gov appointment press releases and bostonpublicschools.org / bps.boston.gov member pages (D-21; RESEARCH.md alternate sources); if a photo cannot be sourced, record it in a gaps list and continue (best-effort, D-23). Processing per image (D-22): crop to 4:5 aspect ratio FIRST (center crop or eyes-at-~1/3 per project headshot rules — never change aspect by stretching), THEN resize to exactly 600×750 with Image.LANCZOS, save JPEG quality=90. Map each official to its politician UUID by querying `SELECT id FROM essentials.politicians WHERE external_id = ?` (council range -2507000001..-2507000014, SC range -2502790001..-2502790007) using DATABASE_URL from env. Upload each processed file to the Supabase Storage `politician_photos` bucket as `{politician_uuid}-headshot.jpg` (D-22; use the project's existing upload mechanism / Supabase storage API as in prior phases). Print a final manifest: for each official, external_id → UUID → uploaded URL or GAP. Strip the `itok` cache-busting query param from source URLs before download. C:/EV-Accounts is NOT a git repo — never run git there.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && python -c "from PIL import Image; print('Pillow OK')"</automated>
  </verify>
  <acceptance_criteria>
    - _boston-headshots-upload.py contains `600` and `750` (target dimensions) and `LANCZOS`
    - _boston-headshots-upload.py crops to 4:5 BEFORE resizing (crop logic precedes the resize-to-600x750 call)
    - _boston-headshots-upload.py references the politician_photos bucket and the `{uuid}-headshot.jpg` naming
    - _boston-headshots-upload.py maps officials by external_id in ranges -2507000001..-2507000014 and -2502790001..-2502790007
    - _boston-headshots-upload.py preserves the accented name for Pepén when matching/logging (no crash on é)
    - Running the script prints a manifest with all 14 council officials having an uploaded URL (no council GAPs), and SC members each marked uploaded or GAP
    - Spot-check: a downloaded+processed image opened via Pillow reports size (600, 750)
  </acceptance_criteria>
  <done>All 14 council headshots processed (4:5 crop → 600×750 Lanczos q90) and uploaded to politician_photos; SC headshots uploaded where available, gaps recorded; manifest printed mapping external_id → UUID → URL/GAP.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 349 — politician_images rows for all uploaded Boston headshots</name>
  <files>C:/EV-Accounts/backend/migrations/349_boston_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/315_va_headshots.sql (EXACT analog — per-politician politician_images INSERT with url/type/photo_license, WHERE NOT EXISTS guard on politician_id)
    - .planning/phases/108-boston-deep-seed/108-PATTERNS.md (§ 349_boston_headshots.sql — header block, per-politician INSERT pattern, optional count DO block, ledger)
    - C:/EV-Accounts/backend/scripts/_boston-headshots-upload.py (the manifest from Task 1 — use its uploaded URLs / UUIDs; only write INSERT blocks for officials that were actually uploaded)
  </read_first>
  <action>
    Create migration 349 following 315_va_headshots.sql. For each official that Task 1 successfully uploaded, write one INSERT block into essentials.politician_images (id=gen_random_uuid(), politician_id=subselect by external_id, url=the politician_photos CDN URL `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg`, type='default' (NOT 'headshot' — D-17/UI filter), photo_license='public_domain'), guarded with WHERE NOT EXISTS on politician_id (idempotent). Column is `url` (NOT storage_url). Cover all 14 council officials (external_id -2507000001..-2507000014) and every SC member (external_id -2502790001..-2502790007) that has an uploaded photo; SC members without a photo are intentionally omitted and listed in the migration header comment as documented gaps (D-23). Add a terminal DO block that RAISES NOTICE with the count of politician_images rows for external_id ranges -2507000014..-2507000001 OR -2502790007..-2502790001. Final ledger INSERT: version='349' ON CONFLICT DO NOTHING. Do NOT apply in this task — Task 3 applies.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/migrations/349_boston_headshots.sql','utf8'); const must=['politician_images',\"'default'\",'politician_photos/','-2507000001','-2507000014',\"VALUES ('349')\"]; const miss=must.filter(m=>!s.includes(m)); if(s.includes(\"'headshot'\")){console.error(\"type must be 'default' not 'headshot'\");process.exit(1)} if(miss.length){console.error('MISSING:',miss);process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - 349_boston_headshots.sql contains `politician_images` INSERTs with `'default'` type and `'public_domain'` license
    - 349_boston_headshots.sql uses column `url` (the CDN path contains `politician_photos/`) and never `'headshot'` as the type value
    - 349_boston_headshots.sql contains INSERT blocks for all 14 council external_ids -2507000001..-2507000014
    - 349_boston_headshots.sql header comment lists any School Committee members omitted as documented gaps
    - 349_boston_headshots.sql contains a terminal DO block raising a NOTICE with the inserted row count
    - 349_boston_headshots.sql contains `VALUES ('349')`
  </acceptance_criteria>
  <done>Migration file written: idempotent politician_images INSERT per uploaded official (type='default', public_domain), gaps documented in header, count NOTICE block, ledger entry 349.</done>
</task>

<task type="auto">
  <name>Task 3: Write apply runner and apply migration 349 to production</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-349.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-325.ts (EXACT analog — Pool, readFileSync, pool.query, smoke tests)
    - .planning/phases/108-boston-deep-seed/108-PATTERNS.md (§ _apply-migration-{347|348|349}.ts — 349 check: politician_images count 14 council + up to 7 SC)
    - C:/EV-Accounts/backend/migrations/349_boston_headshots.sql (the migration this runner executes — read to know expected row count)
  </read_first>
  <action>
    Create `_apply-migration-349.ts` copying `_apply-migration-325.ts`. readFileSync `migrations/349_boston_headshots.sql`, run via `pool.query(sql)`, then smoke tests logging expected values: (1) politician_images rows joined to politicians where external_id BETWEEN -2507000014 AND -2507000001 (expected 14 — all council); (2) politician_images rows where external_id BETWEEN -2502790007 AND -2502790001 (expected = number of SC members uploaded in Task 1, log actual vs uploaded-count, gaps acceptable per D-23); (3) assert every council image has type='default' (count of type<>'default' in council range must be 0); (4) ledger entry '349' PRESENT. Then RUN it from C:/EV-Accounts/backend via `npx tsx scripts/_apply-migration-349.ts`. Apply EITHER through this runner OR via Supabase MCP apply_migration — not both. C:/EV-Accounts is NOT a git repo — never run git there.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-349.ts</automated>
  </verify>
  <acceptance_criteria>
    - _apply-migration-349.ts contains `349_boston_headshots.sql`
    - Running the apply script prints council headshot count: 14 and a School Committee headshot count with its expected (best-effort) value
    - The script asserts/prints 0 council images with type<>'default'
    - The script prints Ledger entry 349: PRESENT
    - A verification query confirms all 14 council politicians (external_id -2507000014..-2507000001) have at least one type='default' politician_images row
  </acceptance_criteria>
  <done>Migration 349 applied to production; 14 council headshots registered with type='default'; SC headshots registered where available with gaps documented; ledger entry 349 present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boston.gov / bostonpublicschools.org → processing script | Untrusted external image bytes downloaded over HTTPS |
| Processing script → Supabase Storage | Uploaded binary objects |
| Apply runner → PostgreSQL | Fixed migration SQL executed verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-06 | Tampering | downloaded image bytes | mitigate | Decode + re-encode through Pillow (crop + resize + JPEG q90) — strips any embedded payload/metadata; only the re-encoded JPEG is uploaded, never raw source bytes |
| T-108-07 | Spoofing | source URL host (patterns-stg.boston.gov staging CDN) | accept | RESEARCH.md flags 2 staging-CDN URLs (Louijeune, Coletta Zapata) as MEDIUM confidence; accept residual risk — fallback is re-fetch from www.boston.gov profile pages; images are public official portraits, low-value target |
| T-108-08 | Information Disclosure | DATABASE_URL / storage credentials | mitigate | Credentials read from environment (process.env / os.environ), never hardcoded in the script — matches established _apply-migration pattern |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — Pillow, pg, tsx, dotenv already installed (RESEARCH.md Package Legitimacy Audit: N/A; Pillow used in prior MA/VA headshot phases). No install tasks in this plan. |
</threat_model>

<verification>
- All 14 council officials have a type='default' politician_images row pointing at politician_photos
- Each uploaded image is exactly 600×750 JPEG (4:5 crop → resize, never stretched)
- SC members have images where available; gaps documented in migration header (best-effort)
- Migration 349 in ledger; count NOTICE matches manifest
</verification>

<success_criteria>
- MA-DEEP-02 satisfied: all Boston city officials (Mayor + 13 councillors) have 600×750 headshots in the politician_photos bucket; School Committee best-effort with documented gaps
- Migration 349 in ledger
</success_criteria>

<output>
Create `.planning/phases/108-boston-deep-seed/108-03-SUMMARY.md` when done
</output>
</output>
