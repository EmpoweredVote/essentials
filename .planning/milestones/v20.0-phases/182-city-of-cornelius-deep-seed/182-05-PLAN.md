---
phase: 182-city-of-cornelius-deep-seed
plan: 05
type: execute
wave: 4
depends_on: ["182-03", "182-04"]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/buildingImages.js
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WASH-08]

must_haves:
  truths:
    - "A legally-licensed 1700×540 community banner for Cornelius is uploaded to Supabase Storage cities/cornelius.jpg (single-word slug per the pipeline naming convention)"
    - "buildingImages.js CURATED_LOCAL has a single-word 'cornelius' key in the post-WR-03 `{ state: 'OR', src }` format pointing at the banner CDN URL, with an attribution comment (verified license)"
    - "The 'cornelius' key correctly matches because getBuildingImages() does city.includes(key) against representing_city.toLowerCase() ('cornelius') — single word, no space-vs-hyphen trap; the state:'OR' field guards against same-name cross-state collisions"
    - "coverage.js Oregon block has a 'Cornelius' entry with browseGovernmentList ['4115550'] (CORRECTED — never 4115350), browseStateAbbrev 'OR', hasContext:true, alphabetically between 'Beaverton' and 'Fairview'"
    - "npm run build passes with both edits"
    - "The live browse link (browse_geo_id=4115550) surfaces Mayor Dalin first + the 3 filled councilors with headshots + stances, the community banner renders (not the tier gradient), the vacant seat renders acceptably (or is cleanly absent — no broken card, no placeholder person), and no party label appears"
    - "Render deploy is verified by bundle CONTENT (grep the served JS for geo_id 4115550 or the cities/cornelius.jpg asset path), NEVER by comparing build hashes (D-16 — Render's static-build hash ≠ local hash; a 45-min false wait in 181)"
    - "The banner subject honors D-14: the Cornelius Civic Center/Public Library (D-14's named alternate) or a street-level scene if a better Commons/Unsplash candidate surfaced at Wave-0 — NEVER a single-building roof crop, NEVER an aerial"
    - "Decision coverage — D-10: legally-licensed Wikimedia-first banner, no AI images, banner-asset-pipeline followed to cities/cornelius.jpg; D-11: banner asset work lives in this final surfacing plan; D-12: coverage.js Oregon block Cornelius entry with hasContext:true (geo_id 4115550); D-14: Civic Center/Library (or street-level alternate) subject honored, failure modes rejected; D-16: deploy verified by bundle content not hash + git status checked before commit; live browse link provided at completion (browse_geo_id=4115550&browse_mtfcc=G4110)"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "Cornelius purple hasContext chip in the Oregon COVERAGE_STATES block"
      contains: "Cornelius"
    - path: "C:/Transparent Motivations/essentials/src/lib/buildingImages.js"
      provides: "'cornelius' CURATED_LOCAL banner entry (post-WR-03 {state,src} format)"
      contains: "cornelius"
  key_links:
    - from: "coverage.js Oregon areas"
      to: "browse route (browse_geo_id=4115550)"
      via: "browseGovernmentList entry"
      pattern: "label: 'Cornelius'"
    - from: "buildingImages.js CURATED_LOCAL 'cornelius'"
      to: "offices.representing_city='Cornelius' (set inline in mig 1196)"
      via: "getBuildingImages() lowercased substring match"
      pattern: "cornelius:"
---

<objective>
Close the phase: (1) source, process, and upload a legally-licensed 1700×540 community banner for
Cornelius; (2) wire it via the single-word `'cornelius'` CURATED_LOCAL key (post-WR-03 `{ state: 'OR',
src }` format) in buildingImages.js; (3) surface Cornelius in the Oregon block of coverage.js with the
purple hasContext chip, using geo_id 4115550 (CORRECTED). The banner renders because mig 1196 set
`offices.representing_city='Cornelius'` inline (no backfill needed). Verify the Render deploy by bundle
CONTENT (grep for the geo_id / asset path), NEVER by hash (D-16).

Purpose: Satisfy WASH-08's purple hasContext chip criterion + the community banner standing constraint,
and verify the full roster end-to-end on the live app.
Output: banner in Storage (cities/cornelius.jpg) + two essentials-repo edits + passing build +
live-browse confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-03-SUMMARY.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-04-SUMMARY.md
@src/lib/coverage.js
@src/lib/buildingImages.js
@docs/banner-asset-pipeline.md

**This is the essentials repo** — coverage.js and buildingImages.js are committed HERE (`git`), NOT in
C:/EV-Accounts. The purple hasContext chip and the banner-render path are already built; only the data
row + banner asset + CURATED_LOCAL key are new. The banner upload uses the existing committed scripts
scripts/banners/process_banner.py + upload_banner.py — no new script file.

**TAILWIND HYGIENE:** do NOT introduce raw Windows `\` paths into any committed file (Tailwind v4 scans
.md/.js and a `\f...` sequence crashes the prod build). Use forward-slash CDN URLs only.

**SINGLE-WORD KEY (no trap here):** Cornelius is a single word, so the CURATED_LOCAL key is simply
`cornelius` — getBuildingImages() does `city.includes(key)` against
`offices.representing_city.toLowerCase()` = `'cornelius'`, which matches cleanly. Keep the post-WR-03
`{ state: 'OR', src }` format regardless (uniform convention; guards same-name cross-state collisions).
The Supabase Storage PATH is `cities/cornelius.jpg` (also single-word) — no asymmetry to manage.

**DEPLOY VERIFICATION (D-16):** verify the Render deploy by grepping the SERVED JS bundle for the new
geo_id (`4115550`) or asset path (`cities/cornelius.jpg`), NOT by comparing build hashes (Render's
static-build hash ≠ local hash — a 45-min false wait in 181). The Render API key at
C:/EV-Accounts/backend/.env reads deploy status for service srv-d7290ltm5p6s73ct3a2g (essentials-frontend).
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Orchestrator sources + processes + uploads the community banner</name>
  <decision>Which legally-licensed wide photo becomes the Cornelius community banner</decision>
  <context>
    Priority hint (D-14): a recognizable everyday street-level Cornelius scene (the Adair/Baseline
    OR-8/TV Highway main-street couplet) OR a true wide multi-roofline skyline; Cornelius Place /
    Cornelius Public Library is the named alternate. Cornelius has the SPARSEST Wikimedia Commons imagery
    of the seven WashCo cities — RESEARCH found NO ideal street-level or skyline candidate in
    Category:Cornelius, Oregon (25 files, all reviewed) as of 2026-07-03, and flagged Category:Scenic
    images of Washington County, Oregon + Category:Structures in Washington County, Oregon + Unsplash as
    an open deeper-search pass for Wave-0. The leading candidate (viewed/assessed at Wave-0 — see
    182-01-SUMMARY.md):
    - PRIMARY "File:Cornelius Civic Center - Oregon.JPG" (1679×1412, ~1.19:1) — the Public Library/City
      Hall, an EXACT match to D-14's named alternate; CC BY-SA 3.0 by M.O. Stevens (the same photographer
      as the Beaverton/Hillsboro/Tigard/Tualatin banners). Requires a MATERIALLY heavier crop to the
      3.15:1 target than any predecessor banner (from ~1.19:1) — try --vertical-anchor in the 0.4-0.55
      range and preview at 1700×540 before uploading.
    D-14 REJECTED failure modes (do NOT reconsider): "Cornelius Town Center - Oregon.JPG" (portrait,
    single-building with flagpole), "Grande Foods, Cornelius, Oregon.jpg" (single-building storefront,
    stale/uncertain tenant), any aerial. A clean license + crops-well-to-3.15:1 beats subject preference.
    Wikimedia Commons FIRST, Unsplash fallback. NO AI-generated images; no baked-in text/graphics.
    Follow docs/banner-asset-pipeline.md Stages 1-8. Run:
    `python scripts/banners/process_banner.py --input <raw> --output <processed_1700x540.jpg> --vertical-anchor <0.4-0.55>`
    then `python scripts/banners/upload_banner.py --file <processed_1700x540.jpg> --path cities/cornelius.jpg`.
    Record the final Title | Author | License (verified) for the attribution comment used in Task 2.
  </context>
  <options>
    <option id="civic-center">
      <name>Wikimedia "Cornelius Civic Center - Oregon.JPG" (D-14 named alternate, leading candidate)</name>
      <pros>License confirmed CC BY-SA 3.0 via imageinfo API; exact match to D-14's named alternate (Public Library/City Hall); M.O. Stevens photographer continuity across 4 prior WashCo banners; no identifiable faces</pros>
      <cons>Native ~1.19:1 needs the heaviest crop of the milestone to reach 3.15:1 — will lose most vertical building context; verify the 1700×540 crop before finalizing (assessment recorded at Wave-0)</cons>
    </option>
    <option id="street-level-alternate">
      <name>A street-level Adair/Baseline or Washington-County-subcategory candidate found at Wave-0</name>
      <pros>Directly matches D-14's top hint (everyday main-street scene); if found with a clean license + good 3.15:1 crop-fit it is preferred over the single-building Civic Center image</pros>
      <cons>RESEARCH found none in Category:Cornelius, Oregon; depends on the Wave-0 deeper Commons/Unsplash search actually surfacing one — do NOT accept a single-building roof crop or aerial to satisfy the hint</cons>
    </option>
    <option id="unsplash">
      <name>Unsplash fallback photo</name>
      <pros>Higher chance of a wide, crop-friendly street/skyline shot</pros>
      <cons>Attribution/license differs from the Wikimedia convention; confirm Unsplash license terms; must still satisfy D-14 (street-level or true skyline, no single-building crop, no aerial)</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen source + final Title | Author | License string (verified license), confirming cities/cornelius.jpg uploaded (CDN URL returns 200).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner in buildingImages.js and surface Cornelius in coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/buildingImages.js, C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - src/lib/buildingImages.js lines ~112-129 (CURATED_LOCAL block + attribution comments, incl. beaverton/hillsboro/tigard/tualatin/'forest grove'/sherwood entries in the post-WR-03 {state,src} format) + the getBuildingImages() city.includes(key) match loop (~lines 219-223)
    - src/lib/coverage.js lines ~98-109 (the Oregon block — Beaverton…Wood Village, incl. Fairview + Portland + Tigard)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md §"src/lib/coverage.js (config, request-response)" and §"src/lib/buildingImages.js (config, request-response)" (exact inserts + the single-word-key + post-WR-03 format notes)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md (confirmed geo_id 4115550) + the Task 1 attribution string
  </read_first>
  <action>
    Edit 1 — buildingImages.js: add a `cornelius` key (single word, lowercase) to CURATED_LOCAL in the
    post-WR-03 `{ state: 'OR', src }` format, src =
    `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/cornelius.jpg`,
    placed alongside the existing beaverton/hillsboro/tigard/tualatin/'forest grove'/sherwood entries.
    Add an attribution comment line above CURATED_LOCAL in the existing format `//   cornelius - <Title>
    | <Author> | <License>` using the string recorded in Task 1 (default per RESEARCH: `Cornelius Civic
    Center - Oregon.JPG (city hall / public library) | M.O. Stevens | CC BY-SA 3.0`, with the verified
    license). The single-word key resolves because getBuildingImages() lowercases
    offices.representing_city and does a city.includes(key) substring match; mig 1196 set
    representing_city='Cornelius' inline so 'cornelius' resolves at first browse. Keep the `state: 'OR'`
    field (post-WR-03 convention — guards same-name cross-state collisions). There is NO space/hyphen
    trap (single word) — both the key and the Storage path are 'cornelius'.
    Edit 2 — coverage.js: insert `{ label: 'Cornelius', browseGovernmentList: ['4115550'],
    browseStateAbbrev: 'OR', hasContext: true }` into the Oregon `areas` array, alphabetically BETWEEN
    'Beaverton' and 'Fairview' ("Be" < "Co" < "Fa"). Match the existing column alignment of surrounding
    rows. Use geo_id '4115550' (CORRECTED — NOT the stated 4115350, which is Coquille). Change nothing
    else in either file — no other state blocks, no other CURATED_LOCAL keys. No raw Windows `\` paths
    anywhere (forward-slash CDN URLs only).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has a `cornelius` key (single word, lowercase) in the post-WR-03 `{ state: 'OR', src }` format → the cities/cornelius.jpg CDN URL
    - An attribution comment `//   cornelius - ... | ... | ...` is present above CURATED_LOCAL with the verified license
    - coverage.js Oregon areas has 'Cornelius' between 'Beaverton' and 'Fairview'
    - Entry is exactly { label: 'Cornelius', browseGovernmentList: ['4115550'], browseStateAbbrev: 'OR', hasContext: true }
    - geo_id is 4115550 (CORRECTED), NOT 4115350
    - No other lines changed in either file; no raw `\` path introduced
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Banner wired via the single-word 'cornelius' key (post-WR-03 format) + Cornelius surfaced with geo_id 4115550; production build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy, live-browse end-to-end verification (bundle-content check), commit</name>
  <what-built>The Cornelius banner (Storage) + buildingImages.js 'cornelius' key + coverage.js purple chip.</what-built>
  <how-to-verify>
    1. Confirm `npm run build` passed (Task 2 gate).
    2. Commit both files in the essentials repo: `git add src/lib/coverage.js src/lib/buildingImages.js && git commit` (on a branch if the harness is on the default branch; end the message with the Co-Authored-By trailer), then push to trigger the Render deploy.
    3. VERIFY THE DEPLOY BY BUNDLE CONTENT (D-16): after the Render deploy completes (read status via the Render API key in C:/EV-Accounts/backend/.env, service srv-d7290ltm5p6s73ct3a2g), grep the SERVED JS bundle for `4115550` or `cities/cornelius.jpg` — do NOT compare build hashes (Render's static-build hash ≠ local hash; a 45-min false wait in 181). If the served bundle does NOT contain the new geo_id/asset path, the deploy has not propagated yet — wait and re-grep, do NOT chase a hash mismatch.
    4. Browse essentials.empowered.vote/results?browse_geo_id=4115550&browse_mtfcc=G4110 and confirm:
       - Mayor Jeffrey C. Dalin sorts FIRST, the 3 filled councilors follow (Godinez Valencia/Baker/López); no empty LOCAL section; no section-split. The vacant seat either renders acceptably or is cleanly absent — NO broken card, NO placeholder person.
       - Headshots render 600×750 with clean white backgrounds and no overlays (4/4 filled per plan 03); compass stances visible; NO party label (antipartisan).
       - The Local section shows the Cornelius community banner photo (NOT the tier gradient fallback) — this confirms representing_city='Cornelius' (mig 1196) + the single-word 'cornelius' CURATED_LOCAL key resolve. If it shows the gradient, the key or representing_city is wrong.
       - The purple hasContext chip appears for Cornelius.
    5. Optionally enter a real Cornelius address and confirm the same roster resolves via /representatives/me (and does NOT resolve to Coquille — a residual check on the geo_id correction).
  </how-to-verify>
  <acceptance_criteria>
    - Both files committed in the essentials repo; pushed; Render deploy verified by bundle CONTENT grep (4115550 / cities/cornelius.jpg present in served JS), NEVER by hash
    - Live browse shows Mayor Dalin first + 3 filled councilors, no empty LOCAL section, no section-split; vacant seat renders acceptably or is cleanly absent (no broken card, no placeholder person)
    - Headshots 600×750 clean white bg, no overlays (4/4 filled); stances visible; no party label; no wrong-person image
    - Community banner renders (not gradient fallback) — confirms the 'cornelius' key resolved
    - Purple hasContext chip shows for Cornelius
    - A real Cornelius address (if checked) resolves to Cornelius, not Coquille
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the live browse + banner + bundle-content deploy check, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Banner sourced from Wikimedia Commons / Unsplash |
| operator → Supabase Storage | Processed banner uploaded to public bucket cities/cornelius.jpg |
| build pipeline → prod | coverage.js + buildingImages.js edits shipped via the essentials build/Render deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-27 | Spoofing/IP | Unlicensed or AI-generated banner image | mitigate | Wikimedia-first with recorded Title/Author/License (Civic Center CC BY-SA 3.0 confirmed via imageinfo API); NO AI-generated images (pipeline forbids); decision checkpoint records the license string |
| T-182-28 | Tampering | Wrong CURATED_LOCAL key never matches → banner silently falls back to the tier gradient | mitigate | Single-word 'cornelius' key matches representing_city.toLowerCase() via city.includes(key); post-WR-03 state:'OR' guards same-name collisions; acceptance criteria assert the key; live-browse gate confirms the banner renders (not gradient) |
| T-182-29 | Tampering | Wrong geo_id (4115350/Coquille) surfaced in coverage.js → chip browses the wrong city | mitigate | Entry hardcodes browseGovernmentList ['4115550'] (CORRECTED); acceptance criteria assert 4115550 not 4115350; optional live address check confirms Cornelius (not Coquille) resolution |
| T-182-30 | Tampering | Malformed coverage.js/buildingImages.js breaks the prod build | mitigate | `npm run build` gate before commit; additive edits only |
| T-182-31 | Denial of Service | Windows `\` path in committed file crashes Tailwind scan | mitigate | Edits add only forward-slash CDN URLs + data rows; no raw backslash introduced |
| T-182-32 | Repudiation | Chasing a false hash mismatch wastes a deploy cycle (181 45-min lesson) | mitigate | D-16: verify the deploy by grepping the served bundle CONTENT for the geo_id/asset path, never by hash |
| T-182-33 | Spoofing | Banner that violates D-14 (single-building roof crop or aerial) misrepresents the city | mitigate | D-14 winning classes enforced (street-level or Civic Center/Library named alternate); documented failure-mode candidates rejected; decision checkpoint + Wave-0 crop assessment |
| T-182-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; banner scripts (process_banner.py/upload_banner.py) are pre-existing committed tooling |
</threat_model>

<verification>
- `npm run build` exits 0; banner uploaded + rendered (not gradient); Render deploy verified by bundle
  CONTENT (not hash); live browse surfaces Mayor-first roster with chip, stances, headshots, banner,
  vacant-seat handled cleanly, no section-split, no party label; both files committed.
</verification>

<success_criteria>
- Cornelius visible with the purple hasContext chip + community banner in the Oregon block (geo_id
  4115550, single-word 'cornelius' key, post-WR-03 format); build green; Render deploy confirmed by
  bundle content; live browse confirms the full filled roster + banner + clean vacant-seat handling
  end-to-end.
</success_criteria>

<output>
Create `.planning/phases/182-city-of-cornelius-deep-seed/182-05-SUMMARY.md` when done, recording the
banner source/license, the bundle-content deploy confirmation, and the live-browse confirmation. This is
the final plan — note WASH-08 is satisfied end-to-end.
</output>
</content>
