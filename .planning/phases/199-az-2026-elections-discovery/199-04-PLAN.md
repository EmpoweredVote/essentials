---
phase: 199-az-2026-elections-discovery
plan: 04
type: execute
wave: 3
depends_on:
  - 199-02
  - 199-03
files_modified:
  - C:/EV-Accounts/backend/migrations/1376_az_2026_discovery.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1376.ts
autonomous: true
requirements:
  - AZ-ELEC-01
tags:
  - discovery
  - sql
  - arizona
  - az-2026
user_setup: []
must_haves:
  truths:
    - "essentials.discovery_jurisdictions has 4 AZ rows: {geoid 04, geoid 04019} × {2026-07-21, 2026-11-03}"
    - "Each row has a 5-element allowed_domains array (azsos.gov, azcleanelections.gov, pima.gov, recorder.pima.gov, ballotpedia.org)"
    - "No row references a cron_active column (arming is date-window driven)"
    - "PHASE GATE: total races under the general e21f5757 = 83 (9 existing US House + 74 new); zero NULL office_id across all AZ 2026 shells; zero Pima BoS; zero Tucson city"
    - "Re-apply idempotent; ledger 1376 present"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/1376_az_2026_discovery.sql
      provides: "4 discovery_jurisdictions rows (curated 5-domain allowlist) + post-verify + ledger"
      contains: "essentials.discovery_jurisdictions"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-1376.ts
      provides: "Smoke tests: 4 rows, allowlist length=5, dates, no cron_active, PLUS phase gate (83 races, zero NULL office_id)"
      contains: "discovery_jurisdictions"
  key_links:
    - from: essentials.discovery_jurisdictions
      to: discovery cron (discoveryCron.ts / discoverySweep.ts)
      via: "date-window eligibility (no cron_active column) + allowed_domains bounds the scrape surface"
      pattern: "election_date"
---

<objective>
Arm the AZ discovery cron and gate phase completion:
1. Migration 1376 seeds 4 rows into essentials.discovery_jurisdictions — {statewide FIPS 04, Pima County 04019} × {primary 2026-07-21, general 2026-11-03} — each with the curated 5-domain AZ election-authority allowlist. No cron_active column (D-08: arming is date-window driven).
2. A phase gate verifies the whole phase: 83 total races under the general (9 existing + 74 new), zero NULL office_id across all AZ 2026 shells, zero Pima BoS, zero Tucson.

Purpose: AZ-ELEC-01 requires discovery armed with the AZ allowlist. This is the last wave — it runs after all race shells (Plans 02/03) so the 83-race phase gate is deterministic. The primary date (2026-07-21) is only ~4 days out at execute time: seed both primary discovery rows per the manifest, but confirm the cron tolerates a near-past election_date (low-stakes; see read_first).

Output: Migration 1376 SQL + apply script applied to production; 4 discovery rows verified; phase race-count gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/199-az-2026-elections-discovery/199-CONTEXT.md
@.planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md
@.planning/phases/105-va-2026-elections-discovery/105-03-PLAN.md
@supabase/migrations/241_or_discovery_jurisdictions.sql
@C:/EV-Accounts/backend/src/lib/discoveryCron.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 1376_az_2026_discovery.sql (4 rows) + apply script + apply</name>
  <files>
    C:/EV-Accounts/backend/migrations/1376_az_2026_discovery.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-1376.ts
  </files>
  <read_first>
    supabase/migrations/241_or_discovery_jurisdictions.sql — canonical discovery shape: (id, jurisdiction_geoid, jurisdiction_name, state, election_date, source_url, allowed_domains), gen_random_uuid(), WHERE NOT EXISTS idempotency, NO cron_active
    .planning/phases/105-va-2026-elections-discovery/105-03-PLAN.md Task 1 — the discovery apply-script smoke-test structure (count, dates, allowlist length, source_url, ledger)
    .planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md §7 (the 4-row table: geoids 04 + 04019, corrected dates, source_urls, 5-domain allowlist) + §10 (pitfalls: wrong primary date, cron_active token, verify-grep false positives)
    C:/EV-Accounts/backend/src/lib/discoveryCron.ts — confirm the date-window arming logic and that a near-past election_date (primary ~4 days out) does not throw; note the low-stakes finding in the SUMMARY
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/1376_az_2026_discovery.sql` following the OR migration 241 shape (4 INSERTs, each guarded by `WHERE NOT EXISTS (… jurisdiction_geoid = X AND election_date = Y)` OR `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING` — both valid per RESEARCH §6). Rows (RESEARCH §7):
    - (`04`, `State of Arizona`, `AZ`, `2026-07-21`, `https://azsos.gov/elections`, 5-domain allowlist)
    - (`04`, `State of Arizona`, `AZ`, `2026-11-03`, `https://azsos.gov/elections`, same 5)
    - (`04019`, `Pima County, Arizona`, `AZ`, `2026-07-21`, `https://www.pima.gov/394/Elections`, same 5)
    - (`04019`, `Pima County, Arizona`, `AZ`, `2026-11-03`, `https://www.pima.gov/394/Elections`, same 5)
    allowed_domains = `ARRAY['azsos.gov','azcleanelections.gov','pima.gov','recorder.pima.gov','ballotpedia.org']` (exactly 5, D-07). state column is char → pass `'AZ'`.

    Add a post-verify `DO $$`: assert 4 rows where jurisdiction_geoid IN ('04','04019'); each `array_length(allowed_domains,1)=5`; dates ∈ {2026-07-21, 2026-11-03}; RAISE EXCEPTION otherwise. Add ledger `VALUES ('1376')`. CRITICAL: the SQL (including comments) must NOT contain the token `cron_active` (Ph202 verify-grep-in-comments lesson; the column does not exist).

    Create `_apply-migration-1376.ts` (base on the 105-03 / migration 281 apply script). Smoke tests: AZ discovery row count = 4; allowed_domains length = 5; both dates present; no cron_active reference; ledger 1376 PRESENT. Orchestrator applies via `cd C:/EV-Accounts && npx tsx backend/scripts/_apply-migration-1376.ts`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-1376.ts 2>&amp;1 | tee /tmp/_1376.log; grep -q "AZ discovery rows: 4" /tmp/_1376.log &amp;&amp; grep -q "allowed_domains length: 5" /tmp/_1376.log &amp;&amp; grep -q "2026-07-21" /tmp/_1376.log &amp;&amp; grep -q "2026-11-03" /tmp/_1376.log &amp;&amp; grep -q "Ledger entry 1376: PRESENT" /tmp/_1376.log</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/1376_az_2026_discovery.sql exists
    - SQL contains jurisdiction_geoid values `'04'` and `'04019'`
    - SQL contains `'State of Arizona'` and `'Pima County, Arizona'`
    - SQL contains `'2026-07-21'` (twice) and `'2026-11-03'` (twice); does NOT contain `'2026-08-04'`
    - SQL contains `ARRAY['azsos.gov', 'azcleanelections.gov', 'pima.gov', 'recorder.pima.gov', 'ballotpedia.org']` (5 elements)
    - SQL contains an idempotency guard (`WHERE NOT EXISTS` on (jurisdiction_geoid, election_date) OR `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING`)
    - SQL does NOT contain the token `cron_active` (not even in comments)
    - SQL contains `VALUES ('1376')` (ledger)
    - Apply script prints `AZ discovery rows: 4 (expected 4)`, `allowed_domains length: 5`, both dates, `Ledger entry 1376: PRESENT`
    - Re-run keeps AZ discovery row count at 4 (idempotency)
    - Prod: `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid IN ('04','04019')` returns 4; every row `array_length(allowed_domains,1)=5`
  </acceptance_criteria>
  <done>
    4 AZ discovery_jurisdictions rows exist with the curated 5-domain allowlist and corrected dates; the cron arms both geoids by date window (no flag); migration 1376 in ledger.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase race-count gate — assert 83 total races + zero NULL office_id + zero Pima/Tucson</name>
  <files>
    (no file writes — phase-completion verification queries only; record results in the 199-04 SUMMARY)
  </files>
  <read_first>
    .planning/phases/199-az-2026-elections-discovery/199-VALIDATION.md — the "Race-count total" (83) and negative-scope success criteria
    .planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md §9 (success criterion → verification map)
  </read_first>
  <action>
    This task runs ONLY after Plans 02 and 03 have applied (Wave 3 gate). Run these production verification queries and record the results in the SUMMARY:
    1. Total races under the general: `SELECT COUNT(*) FROM essentials.races WHERE election_id=(SELECT id FROM essentials.elections WHERE name='AZ 2026 Statewide General' AND state='AZ')` — expect 83 (9 existing US House + 6 statewide + 1 corp + 30 senate + 30 house + 7 local).
    2. Zero NULL office_id across all AZ 2026 shells: `SELECT COUNT(*) FROM essentials.races WHERE election_id=(…general…) AND office_id IS NULL` — expect 0.
    3. Zero Pima BoS: count of position_name LIKE `%Supervisor%` under the general — expect 0.
    4. Zero City of Tucson: count of `Tucson Mayor` or LIKE `%Tucson Ward%` under the general — expect 0 (South Tucson City Council must NOT be counted).
    5. Zero hand-seeded candidates added this phase: confirm race_candidates count under the general is still 39 (the pre-existing US House candidates), i.e. no new candidates were seeded by Plans 01-04.

    If any assertion fails, STOP and surface which plan's shells are missing/miscounted — do not mark the phase complete. Fold the same 5 assertions into the 1376 apply-script output (or a small standalone verify script) so they run automatically as part of Task 1's apply, and print explicit lines the automated verify below can grep.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-1376.ts 2>&amp;1 | tee -a /tmp/_1376.log; grep -q "PHASE GATE total races: 83" /tmp/_1376.log &amp;&amp; grep -q "PHASE GATE NULL office_id: 0" /tmp/_1376.log &amp;&amp; grep -q "PHASE GATE Pima BoS: 0" /tmp/_1376.log &amp;&amp; grep -q "PHASE GATE Tucson city: 0" /tmp/_1376.log &amp;&amp; grep -q "PHASE GATE candidates unchanged: 39" /tmp/_1376.log</automated>
  </verify>
  <acceptance_criteria>
    - Prod: total races under the general = 83
    - Prod: NULL office_id count under the general = 0
    - Prod: `%Supervisor%` count under the general = 0
    - Prod: `Tucson Mayor`/`%Tucson Ward%` count under the general = 0 (South Tucson City Council excluded)
    - Prod: race_candidates count under the general = 39 (unchanged — no candidates hand-seeded this phase)
    - The 5 gate assertions are emitted by the 1376 apply script (or a standalone verify script) with grep-able `PHASE GATE …` lines
  </acceptance_criteria>
  <done>
    The phase gate is green: 83 races total, zero NULL office_id, zero Pima BoS, zero Tucson city, 39 candidates unchanged. AZ-ELEC-01 structural + discovery scope fully satisfied; a post-07-21 candidate reconcile remains DEFERRED to a future phase (not planned here).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovery_jurisdictions.allowed_domains → discovery cron scrape surface | The allowlist is the control that bounds which external domains the cron may fetch — an over-broad list lets the cron scrape untrusted sources |
| migration SQL → production Supabase | Privileged write; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-04-ALLOWLIST | Tampering / Elevation (cron scrapes untrusted domains) | allowed_domains | mitigate | Curated 5-domain AZ election-authority allowlist per row; post-verify asserts `array_length(allowed_domains,1)=5` on every row |
| T-199-04-WRONGDATE | Integrity | discovery election_date | mitigate | Corrected `2026-07-21`; acceptance criterion forbids `2026-08-04`; both dates asserted present |
| T-199-04-CRONFLAG | Integrity | cron_active token | mitigate | Column does not exist; SQL forbidden from containing `cron_active` even in comments (Ph202 grep lesson); arming is date-window driven |
| T-199-04-CORRUPT | Tampering | 1376 re-apply | mitigate | `WHERE NOT EXISTS`/`ON CONFLICT` idempotency + `DO $$` count assert (4 rows) |
| T-199-04-PHASEGATE | Integrity (wrong ballot reaching voters) | full-phase race set | mitigate | 83-race + zero-NULL + zero-Pima/Tucson + candidates-unchanged gate blocks phase close on any drift |
| T-199-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — existing `pg`/`tsx` only |
</threat_model>

<verification>
- 4 AZ discovery rows ({04, 04019} × {2026-07-21, 2026-11-03}), each 5-domain allowlist, no cron_active
- Phase gate: 83 races, zero NULL office_id, zero Pima BoS, zero Tucson, 39 candidates unchanged
- Idempotent re-apply; ledger 1376 PRESENT
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid IN ('04','04019')` returns 4
- Every AZ discovery row has a 5-element allowed_domains array; no cron_active reference
- Total AZ 2026 races under the general = 83; zero NULL office_id; zero Pima BoS; zero Tucson city
- AZ-ELEC-01 fully satisfied across Plans 01-04
</success_criteria>

<output>
Create `.planning/phases/199-az-2026-elections-discovery/199-04-SUMMARY.md` when done with: migration 1376 + apply-script paths; the 4 discovery rows; discoveryCron near-past-date finding; the phase-gate query results (83 / 0 / 0 / 0 / 39); acceptance-criteria checklist; the note that a post-07-21 candidate reconcile is DEFERRED to a future phase; Phase 199 closing checklist (AZ-ELEC-01 satisfied).
</output>
