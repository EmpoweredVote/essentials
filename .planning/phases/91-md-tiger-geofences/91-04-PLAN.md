---
phase: 91-md-tiger-geofences
plan: 04
type: execute
wave: 4
depends_on:
  - 91-03
files_modified: []
autonomous: true
requirements:
  - MD-GEO-01
  - MD-GEO-02
  - MD-GEO-03
  - MD-GEO-04
  - MD-GEO-05
  - MD-GEO-06

must_haves:
  truths:
    - "All 7 SQL verification gates pass (0 invalid geometries, 0 GeometryCollection types, correct counts, Baltimore City dual-tier, correct districts.state casing, St. Mary's County sentinel present, 0 section-split rows)"
    - "smoke-md-geofences.ts exits 0 with 'ALL ASSERTIONS PASSED'"
    - "Baltimore City Hall returns both G4110 (geo_id=2404000) and G4020 (geo_id=24510) from the spatial query"
    - "Garrett County rural coordinate does NOT return G4110 (unincorporated — no city boundary)"
    - "Leonardtown returns G4020 (St. Mary's County) + G4110 (incorporated town) — Phase 95 prerequisite confirmed"
    - "Section-split check returns 0 rows (D-10)"
    - "St. Mary's County G4020 row exists with geo_id='24037' — Phase 95 dependency satisfied"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/verify-md-tiger-import.sql"
      provides: "7-gate SQL verification — all gates pass"
      contains: "state = '24'"
    - path: "C:/EV-Accounts/backend/scripts/smoke-md-geofences.ts"
      provides: "3-address smoke test — exits 0"
      contains: "smoke-md-geofences"
  key_links:
    - from: "smoke-md-geofences.ts ST_Covers query"
      to: "geofence_boundaries.geometry (PostGIS)"
      via: "ST_Covers(geometry, ST_SetSRID(ST_MakePoint(lon, lat), 4326))"
      pattern: "ST_Covers"
    - from: "verify SQL Gate 7"
      to: "essentials.districts (state IN ('MD','md'))"
      via: "LEFT JOIN with geofence_boundaries — 0 rows means all districts have coverage"
      pattern: "0 rows"
---

<objective>
Run the full verification suite: 7-gate SQL verification script and 3-address TypeScript smoke test. Confirm all MD-GEO requirements are satisfied and the phase can be closed.

Purpose: This is the exit gate for Phase 91. Plans 92-96 can only proceed safely after all verification gates pass. The D-01 Baltimore City dual-tier invariant, D-10 section-split check, and the Phase 95 dependency (St. Mary's County G4020 row) are all confirmed here.

Output: All 7 SQL gates report expected results; smoke test exits 0 with ALL ASSERTIONS PASSED; phase is ready to close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-md-tiger-geofences/91-CONTEXT.md
@.planning/phases/91-md-tiger-geofences/91-RESEARCH.md
@.planning/phases/91-md-tiger-geofences/91-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run 7-gate SQL verification script</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/verify-md-tiger-import.sql — Read the full script before executing. Understand what each gate expects.
    .planning/phases/91-md-tiger-geofences/91-03-SUMMARY.md — Get the confirmed row counts from the live load (G4110 and G5220 counts to compare against Gate 3).
  </read_first>
  <action>
    Run all 7 gates from verify-md-tiger-import.sql against the production Supabase DB using mcp__supabase-local__execute_sql. Execute each gate as a separate query so results are individually readable. Record pass/fail for each.

    Gate 1 — Invalid geometries: SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='24' AND NOT ST_IsValid(geometry)
    Expected: 0. If non-zero, ST_MakeValid did not run or was ineffective for some rows — investigate before closing.

    Gate 2 — GeometryCollection types: SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='24' AND ST_GeometryType(geometry) NOT IN ('ST_Polygon', 'ST_MultiPolygon')
    Expected: 0.

    Gate 3 — Per-layer counts: SELECT mtfcc, COUNT(*) FROM essentials.geofence_boundaries WHERE state='24' GROUP BY mtfcc ORDER BY mtfcc
    Expected: G4020=24, G4110=[plan-02-place-count], G5200=8, G5210=47, G5220=[plan-02-sldl-count]. All 5 rows present.

    Gate 4 — Baltimore City dual-tier (D-01 invariant): SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='24' AND geo_id IN ('2404000', '24510') ORDER BY mtfcc
    Expected: exactly 2 rows — geo_id='24510'/G4020 and geo_id='2404000'/G4110, both name='Baltimore city'.

    Gate 5 — districts.state casing (D-07): SELECT state, district_type, COUNT(*) FROM essentials.districts WHERE state IN ('MD','md') GROUP BY state, district_type ORDER BY state, district_type
    Expected: md|COUNTY|24, md|STATE_LOWER|N, md|STATE_UPPER|47, MD|NATIONAL_LOWER|8. No rows with wrong casing.

    Gate 6 — St. Mary's County sentinel (Phase 95 prerequisite): SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='24' AND mtfcc='G4020' AND name LIKE '%Mary%'
    Expected: 1 row — name="St. Mary's County", geo_id='24037'.

    Gate 7 — Section-split check (D-10): SELECT d.geo_id, d.district_type, d.state FROM essentials.districts d WHERE d.state IN ('MD','md') AND d.geo_id NOT IN (SELECT geo_id FROM essentials.geofence_boundaries WHERE state='24') LIMIT 10
    Expected: 0 rows. If any rows returned, every district geo_id must have a matching geofence row — diagnose and fix before closing.

    For each gate that fails, diagnose root cause and determine if it can be fixed without re-running the live load (most issues can be fixed with a targeted UPDATE or re-run of a single layer).
  </action>
  <verify>
    <automated>Via mcp__supabase-local__execute_sql — run Gate 7 (section-split check) and Gate 4 (Baltimore City dual-tier) as the final automated checks: both must return expected results (0 rows for Gate 7, 2 rows for Gate 4)</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 returns 0 (no invalid geometries)
    - Gate 2 returns 0 (no GeometryCollection types)
    - Gate 3 returns 5 rows with counts matching confirmed values: G4020=24, G5200=8, G5210=47, G4110=plan-02-count, G5220=plan-02-count
    - Gate 4 returns 2 rows: geo_id='24510'/G4020 and geo_id='2404000'/G4110 (D-01 invariant confirmed)
    - Gate 5 returns 4 rows with correct state casing: md (COUNTY/STATE_LOWER/STATE_UPPER) and MD (NATIONAL_LOWER)
    - Gate 6 returns 1 row with name containing "Mary" and geo_id='24037' (Phase 95 prerequisite confirmed)
    - Gate 7 returns 0 rows (D-10 section-split check passes)
    - No gate fails; if any gate fails the executor diagnoses and resolves before marking done
  </acceptance_criteria>
  <done>All 7 SQL gates pass with expected results; section-split check = 0 rows (D-10); St. Mary's County present (Phase 95 dependency confirmed); Baltimore City dual-tier confirmed (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: Run smoke-md-geofences.ts and confirm phase success criteria</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/smoke-md-geofences.ts — Read the full script before running. Understand the 3 test addresses and their expectedMtfcc/forbiddenMtfcc values. Note that the Garrett County coordinate (-79.3, 39.53) may fall inside Oakland city — if G4110 is returned for that address, shift the coordinate to (-79.45, 39.65) per RESEARCH.md Pitfall 5.
    .planning/phases/91-md-tiger-geofences/91-RESEARCH.md — Pitfall 5 (Garrett County coordinate inside Oakland city) and the fallback coordinate.
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the smoke test:

      npx tsx scripts/smoke-md-geofences.ts

    The script exits 0 if all assertions pass, 1 if any fail. Monitor output for:

    SC3 (count check): Each MTFCC should match expectedCounts. If G4110 or G5220 show as 0, Plan 02 count updates were not applied to smoke-md-geofences.ts — update them and re-run.

    SC1 (Baltimore City Hall, D-01): Both G4110 geo_id='2404000' AND G4020 geo_id='24510' must be returned. If either is missing, the dual-tier load failed — investigate Gate 4 SQL result from Task 1 first.

    SC2 (Garrett County rural point): G4110 must NOT appear in the results. If G4110 is returned for coordinate (-79.3, 39.53), the point falls inside Oakland city (per Pitfall 5 in RESEARCH.md). In that case:
      - Update TEST_ADDRESSES[1].lon to -79.45 and .lat to 39.65 in smoke-md-geofences.ts
      - Re-run the smoke test
      - Confirm G4110 is still forbidden and G4020/G5200/G5210/G5220 are all returned

    SC4 (no NULL names): All rows returned for all 3 addresses must have non-null, non-empty name values.

    Also confirm the MD Target City lookup at the bottom of the output lists Baltimore city, Annapolis city, and Rockville city with their geo_ids — this is informational output for Phase 92+ planning.

    If the smoke test exits 0 with ALL ASSERTIONS PASSED, Phase 91 verification is complete.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-md-geofences.ts</automated>
  </verify>
  <acceptance_criteria>
    - Smoke test exits 0 with "ALL ASSERTIONS PASSED" in the output
    - SC1 line in output reads: "Baltimore City Hall returns G4110 (2404000) + G4020 (24510) + G5200 + G5210 + G5220 [PASS]"
    - SC2 line in output reads: "Garrett County rural returns G4020 + G5200 + G5210 + G5220; NO G4110 [PASS]" (if coordinate shift was needed, final coordinate is documented in SUMMARY.md)
    - SC3 line in output reads: "All 8 CD + 47 senate + N house + 24 counties + N cities present [PASS]"
    - SC4 line in output reads: "3 addresses each return non-NULL names across all tiers [PASS]"
    - MD Target City lookup outputs geo_ids for Baltimore city, Annapolis city, Rockville city — these geo_ids are recorded in SUMMARY.md for Phase 92 government seeding
  </acceptance_criteria>
  <done>smoke-md-geofences.ts exits 0; all 4 Phase 91 roadmap success criteria confirmed; MD geo_ids for key cities captured for downstream phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke test → Supabase production | Read-only SELECT queries via pg client; no writes |
| verify SQL → Supabase production | Read-only SELECT queries; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-11 | Integrity | Garrett County coordinate inside incorporated place (Pitfall 5) | mitigate | smoke test will detect G4110 return; executor shifts coordinate to (-79.45, 39.65) per RESEARCH.md instructions; documents final coordinate in SUMMARY |
| T-91-12 | Integrity | Section-split check non-zero (orphaned district rows) | mitigate | Gate 7 blocks phase close; executor must diagnose and fix orphaned rows before marking done; typically caused by a missing geofence layer that needs re-loading |
| T-91-13 | Integrity | Baltimore City dual-tier missing G4020 or G4110 | mitigate | Gate 4 SQL + SC1 smoke test both assert; if either fails, re-run the affected layer; ON CONFLICT DO NOTHING makes re-run safe |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No new packages; smoke test uses pre-existing pg + dotenv |
</threat_model>

<verification>
Phase 91 is complete when ALL of the following are true:
1. All 7 SQL gates in verify-md-tiger-import.sql return expected results (0/0/correct-counts/2-rows/4-casing-groups/1-sentinel/0)
2. smoke-md-geofences.ts exits 0 with "ALL ASSERTIONS PASSED"
3. Baltimore City geo_ids confirmed: G4110='2404000', G4020='24510'
4. Garrett County coordinate confirmed unincorporated (no G4110 returned)
5. Leonardtown (St. Mary's County) returns G4020 — Phase 95 can proceed
6. Section-split check = 0 rows (D-10)
</verification>

<success_criteria>
- All 7 SQL gates pass with expected values
- smoke-md-geofences.ts exits 0: SC1 (Baltimore dual-tier), SC2 (Garrett rural), SC3 (counts), SC4 (no null names) all PASS
- D-01 invariant confirmed: any Baltimore City coordinate returns both G4110 and G4020
- D-10 invariant confirmed: 0 section-split rows
- Phase 95 prerequisite confirmed: St. Mary's County G4020 geo_id='24037' present
- MD target city geo_ids documented in SUMMARY.md for Phase 92 (government row seeding)
</success_criteria>

<output>
Create `.planning/phases/91-md-tiger-geofences/91-04-SUMMARY.md` when done. Include:
- All 7 gate results (pass/fail + observed values)
- Smoke test exit code + any coordinate adjustments made
- Confirmed geo_ids for Baltimore city (G4110='2404000', G4020='24510'), Annapolis city, Rockville city
- Final confirmed counts: G4020=24, G4110=N, G5200=8, G5210=47, G5220=N
- Phase 91 status: COMPLETE
</output>
