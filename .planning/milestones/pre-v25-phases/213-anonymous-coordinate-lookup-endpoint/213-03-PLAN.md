---
phase: 213-anonymous-coordinate-lookup-endpoint
plan: 03
type: execute
wave: 3
depends_on: ["213-02"]
files_modified: []
autonomous: false
requirements: [RSLV-03]

must_haves:
  truths:
    - "The endpoint is live on accounts-api.empowered.vote after a Render deploy from master"
    - "A valid US point returns the exact single US House rep plus the state+federal floor (RSLV-05)"
    - "Each 422 rejection path (OUTSIDE_US_BOUNDS / SWAPPED_COORDINATES / INVALID_COORDINATES) returns its distinct code live"
    - "The lookup writes zero rows to any table"
    - "No raw coordinate appears in server-side handling (code path) or logs"
  artifacts: []
  key_links:
    - from: "live endpoint"
      to: "PostGIS ST_Covers officials"
      via: "curl POST against accounts-api.empowered.vote"
      pattern: "coordinate-lookup"
---

<objective>
Ship Plan 01+02 to production and prove the privacy + correctness contract against the LIVE endpoint (Criterion 1/2/3). Render auto-deploys on push to `master`, so the sequence is: commit/push in `C:/EV-Accounts` → wait for the Render deploy → live curl smoke test → operator sign-off.

This is the phase's [BLOCKING] verification. Because gsd-executor has NO Supabase MCP, all DB/zero-write checks run inline via psql/curl.

Purpose: Backend-before-frontend hard dependency — Phase 214 must not start until this endpoint is live and smoke-tested.
Output: A verified live endpoint + operator approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-CONTEXT.md
@.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-01-PLAN.md
@.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-02-PLAN.md

Live backend host: `accounts-api.empowered.vote`. Deploy = `git -C "C:/EV-Accounts" push origin master` -> Render. The DB is production (mcp__supabase-local IS production) — but this endpoint is read-only, so smoke traffic is safe. psql connection uses the existing accounts-api backend env (read the connection string from the C:/EV-Accounts backend env; never hardcode it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit + push to master, confirm Render deploy is live</name>
  <files>C:/EV-Accounts (git)</files>
  <read_first>
    - C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.ts (confirm the built route path before smoke-testing it)
    - C:/EV-Accounts/backend/src/index.ts (confirm the mount path)
  </read_first>
  <action>
    Ensure the full test suite + typecheck are green locally: `npx tsc --noEmit` and `npx vitest run src/lib/coordinateValidation.test.ts src/routes/essentialsCoordinateLookup.test.ts`. Stage the Plan 01+02 files and commit in the EV-Accounts repo via `git -C "C:/EV-Accounts" add ...` then `git -C "C:/EV-Accounts" commit` (message: `feat(213): anonymous coordinate-lookup endpoint`). Push to master via `git -C "C:/EV-Accounts" push origin master` — this triggers the Render auto-deploy. Poll an existing health route until the new deploy is serving, then curl the new route and confirm it is no longer 404. Do NOT proceed to Task 2 until the coordinate-lookup route responds (200 or 422 — anything but 404/502) from `accounts-api.empowered.vote`.
    Reminder (project GOTCHA): a Render build hash differs from local — verify against the LIVE host, not localhost.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -X POST https://accounts-api.empowered.vote/api/essentials/coordinate-lookup -H "Content-Type: application/json" -d '{}'</automated>
  </verify>
  <acceptance_criteria>
    - The curl above returns `422` (route is live and reachable; empty body -> INVALID_COORDINATES), NOT `404` or `502`.
    - `git -C "C:/EV-Accounts" log -1 --oneline` shows the phase-213 commit on master and the branch is not ahead of origin (pushed).
  </acceptance_criteria>
  <done>Phase-213 backend committed + pushed; Render deploy live; coordinate-lookup route reachable on accounts-api.empowered.vote.</done>
</task>

<task type="auto">
  <name>Task 2: Live smoke test — 200 correctness, three 422 codes, zero-write, no-coordinate-log</name>
  <files>C:/EV-Accounts (inline curl/psql only — no file edits)</files>
  <read_first>
    - C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.ts (confirm which logs, if any, the handler emits — for the no-coordinate-log source assertion)
    - C:/EV-Accounts/backend/src/lib/essentialsService.ts (confirm getRepresentativesByCoordinate issues only SELECTs — for the zero-write source assertion)
  </read_first>
  <action>
    Run four live checks against `https://accounts-api.empowered.vote/api/essentials/coordinate-lookup` (POST JSON body):
    (a) VALID POINT (Criterion 1 + RSLV-05): POST { "lat": 39.17, "lng": -86.52 } (Bloomington, IN). Assert 200; politicians non-empty; the response contains EXACTLY ONE `NATIONAL_LOWER` record — the single US House rep for the covering IN congressional district (count the occurrences; more than one means the nationwide roster leaked in, BLOCKER 1) — plus US Senators + Governor/state execs (the state-scoped floor); `matchedAddress` is `""`; the response JSON contains neither `39.17` nor `-86.52` (no coordinate echo).
    (b) REJECTIONS (Criterion 2): POST swapped { "lat": -86.52, "lng": 39.17 } -> 422 code `SWAPPED_COORDINATES`; POST { "lat": 51.5, "lng": -0.12 } (London) -> 422 code `OUTSIDE_US_BOUNDS`; POST { "lat": "abc" } -> 422 code `INVALID_COORDINATES`.
    (c) ZERO WRITE (Criterion 1): via psql against production, snapshot a row count sensitive to any inadvertent write (e.g. the analytics/events table if one exists, plus `SELECT count(*) FROM essentials.politicians`) BEFORE and AFTER issuing several valid lookups; assert delta 0. Also assert by source: a grep for `insert|update|delete` (case-insensitive) over the getRepresentativesByCoordinate code path returns no write statements (SELECT-only).
    (d) NO COORDINATE LEAK (Criterion 3): inspect the live Render logs for the smoke window (or, if log access is unavailable, the handler source) and assert no `39.17`/`-86.52`/raw-coordinate string was logged; confirm the handler emits no analytics event carrying the coordinate.
    Capture each curl's status + `code` / first-official for the SUMMARY and the checkpoint.
  </action>
  <verify>
    <automated>bash -c 'set -e; U="https://accounts-api.empowered.vote/api/essentials/coordinate-lookup"; V=$(curl -s -X POST "$U" -H "Content-Type: application/json" -d "{\"lat\":39.17,\"lng\":-86.52}"); N=$(echo "$V" | grep -o NATIONAL_LOWER | wc -l | tr -d " "); [ "$N" = "1" ] || { echo FAIL_HOUSE_REP_COUNT=$N; exit 1; }; echo "$V" | grep -q "39.17" && { echo FAIL_COORD_ECHO; exit 1; }; curl -s -X POST "$U" -H "Content-Type: application/json" -d "{\"lat\":-86.52,\"lng\":39.17}" | grep -q SWAPPED_COORDINATES || { echo FAIL_SWAP; exit 1; }; curl -s -X POST "$U" -H "Content-Type: application/json" -d "{\"lat\":51.5,\"lng\":-0.12}" | grep -q OUTSIDE_US_BOUNDS || { echo FAIL_OOB; exit 1; }; curl -s -X POST "$U" -H "Content-Type: application/json" -d "{\"lat\":\"abc\"}" | grep -q INVALID_COORDINATES || { echo FAIL_INVALID; exit 1; }; echo SMOKE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - Valid Bloomington point returns 200 with EXACTLY ONE `NATIONAL_LOWER` (US House) record (count === 1 — the BLOCKER 1 regression guard against the ~435-member nationwide roster) + the state-scoped floor (Senators + Governor/state execs), `matchedAddress === ""`, and no `39.17`/`-86.52` substring in the body.
    - Swapped -> `SWAPPED_COORDINATES`, London -> `OUTSIDE_US_BOUNDS`, `"abc"` -> `INVALID_COORDINATES` (each 422, distinct code).
    - psql before/after row-count delta == 0 across the sampled tables (zero writes).
    - No raw coordinate string present in the smoke-window logs / handler source; no coordinate-bearing analytics event.
    - The verify automated command prints `SMOKE_OK`.
  </acceptance_criteria>
  <done>Live endpoint proven: valid US point yields EXACTLY ONE US House rep + state-scoped floor with no coordinate echo; all three 422 codes distinct; zero writes; no coordinate logged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off on live coordinate-lookup endpoint</name>
  <what-built>An anonymous, unauthenticated POST /api/essentials/coordinate-lookup endpoint live on accounts-api.empowered.vote that resolves officials for raw lat/lng via PostGIS ST_Covers, with US bbox + swap-guard validation, the state+federal fallback floor, zero writes, and no coordinate echo/logging.</what-built>
  <how-to-verify>
    1. Confirm the Task 2 smoke output: Bloomington IN (39.17, -86.52) returned 200 with EXACTLY ONE US House rep (not the national roster) + Senators + Governor and matchedAddress "".
    2. Confirm the three rejection codes are correct and distinct: swapped -> SWAPPED_COORDINATES, London -> OUTSIDE_US_BOUNDS, "abc" -> INVALID_COORDINATES.
    3. Confirm the zero-write psql before/after delta was 0 and no raw coordinate appeared in logs.
    4. Optionally re-run a spot curl of your own choosing against the live host.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 213, or describe any issue (wrong House rep, missing floor, a coordinate leak, a wrong 422 code).</resume-signal>
  <files>none (verification checkpoint — no file edits)</files>
  <action>Present the Task 2 live smoke results to the operator and PAUSE. Do not close Phase 213 or start Phase 214 until the operator approves. No code changes in this task.</action>
  <verify><human-check>Operator reviews the live smoke output and replies "approved".</human-check></verify>
  <done>Operator has typed "approved" (or all raised issues are resolved and re-approved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → live endpoint | Anonymous, unauthenticated production HTTP surface |
| endpoint → production DB | Read-only ST_Covers; must perform no writes |
| endpoint → Render/app logs | Logs must contain no raw coordinate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-09 | Information Disclosure | live logs / responses | mitigate | live smoke asserts no coordinate string in body or logs; matchedAddress empty (D-06) |
| T-213-10 | Tampering | production data integrity | mitigate | before/after psql row-count delta 0 + SELECT-only source assertion (Criterion 1: no writes) |
| T-213-11 | Spoofing | swapped-coordinate wrong-location result | mitigate | live swap test proves SWAPPED_COORDINATES 422, never a silent swapped query (D-03) |
| T-213-SC | Tampering | supply chain — no new package installs this phase | accept | Phase 213 introduces no new npm dependency (express-rate-limit/zod already present); nothing to audit |
</threat_model>

<verification>
- Live route reachable on accounts-api.empowered.vote (not 404/502).
- Smoke: 200 correctness (House rep + floor, no echo), three distinct 422 codes, zero writes, no coordinate logged.
- Operator approval recorded at the blocking checkpoint.
</verification>

<success_criteria>
- All three ROADMAP Phase-213 success criteria proven against the live endpoint and signed off by the operator.
</success_criteria>

<output>
Create `.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-03-SUMMARY.md` when done.
</output>
