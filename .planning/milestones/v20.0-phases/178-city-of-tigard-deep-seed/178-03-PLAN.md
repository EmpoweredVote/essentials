---
phase: 178-city-of-tigard-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["178-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-tigard-headshots.py
  - C:/EV-Accounts/backend/migrations/1160_tigard_headshots.sql
autonomous: false
requirements: [WASH-04]

must_haves:
  truths:
    - "Each Tigard official with a usable found photo has a 600×750 headshot uploaded to Supabase Storage politician_photos/{uuid}-headshot.jpg"
    - "Every headshot was cropped to 4:5 FIRST, then resized to 600×750 Lanczos q90 (no distortion, no overlays)"
    - "Sources are per-official local-news / campaign / Ballotpedia images (tigard-or.gov is WAF-403 and NO CivicWeb/Legistar/Granicus bulk mirror exists — unlike Hillsboro)"
    - "One politician_images row per official WITH a found photo (type='default', photo_license='press_use'), guarded WHERE NOT EXISTS on politician_id"
    - "No image has superimposed text/graphics over the face; each is a valid non-zero JPEG (HTTP 200 from CDN)"
    - "Any genuine no-photo gap is documented honestly (a partial 5/7 or 6/7 outcome is acceptable per RESEARCH — NO fabricated rows, NO forced group-photo crops, NO hard-assert on 7/7)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-tigard-headshots.py"
      provides: "Download → composite → crop 4:5 → resize 600×750 → upload pipeline for up to 7 officials"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/1160_tigard_headshots.sql"
      provides: "politician_images INSERTs for each official with a found photo (audit-only, NOT registered in the ledger)"
      min_lines: 20
      contains: "politician_images"
  key_links:
    - from: "_tmp-tigard-headshots.py"
      to: "Supabase Storage politician_photos bucket"
      via: "PUT {uuid}-headshot.jpg x-upsert:true"
      pattern: "politician_photos"
    - from: "1160_tigard_headshots.sql"
      to: "essentials.politicians (by external_id)"
      via: "subselect politician_id per row"
      pattern: "external_id = -41736"
---

<objective>
Author the headshot pipeline script and the audit-only migration that records the Tigard officials'
portraits. Unlike Hillsboro (single CivicWeb portal), Tigard has NO bulk source — portraits are found
per-official on tigardlife.com / valleytimes.news / Ballotpedia / campaign sites, cropped 4:5 then
resized to 600×750 Lanczos q90, and mirrored to Supabase Storage. Genuine gaps are an expected,
acceptable outcome — do NOT force a 7/7 result.

Purpose: Satisfy the headshot portion of WASH-04 — officials render with 600×750 photos, genuine
gaps documented honestly.
Output: `_tmp-tigard-headshots.py` (gitignored helper, run by the orchestrator) + audit-only
migration `1160_tigard_headshots.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/178-city-of-tigard-deep-seed/178-RESEARCH.md
@.planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md
@.planning/phases/178-city-of-tigard-deep-seed/178-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES both files. It has NO DB/Storage access and NO
WebSearch. The INLINE ORCHESTRATOR searches per-official for a usable photo, populates/runs
`_tmp-tigard-headshots.py` (which resolves each politician UUID by external_id at runtime via
psycopg2, downloads, processes, and uploads), then applies the audit migration via `psql -f`. The
`.py` file is a gitignored `_tmp-*` helper — do NOT commit it. The `.sql` migration IS committed to
the EV-Accounts repo.

**AUDIT-ONLY MIGRATION:** `1160_tigard_headshots.sql` does NOT write a schema_migrations ledger row
(headshot/stance migrations bypass the ledger; on-disk counter is authoritative). Keep the literal
strings `slug`, `schema_migrations`, and `photo_origin_url` OUT of comments.
</context>

<interfaces>
essentials.politician_images  (id, politician_id, url, type, photo_license)  -- NO photo_origin_url column; type='default'
-- politician UUIDs: resolve by external_id from 178-02-SUMMARY.md:
--   -4173651 Yi-Kang Hu | -4173652 Tom Anderson | -4173653 Faraz Ghoddusi | -4173654 Heather Robbins
--   -4173655 Jake Schlack | -4173656 Jeanette Shaw | -4173657 Maureen Wolf
-- Spot-verified source (research): Jeanette Shaw (-4173656) →
--   https://tigardlife.com/wp-content/uploads/2024/09/Jeanette-Shaw-696x462.jpg (200, 36,280 bytes, 696×462)
-- Other 6 require per-official search at runtime: tigardlife.com / valleytimes.news article images
--   (Hu/Anderson/Wolf appointment & mayoral-transition articles most likely to carry individual photos),
--   Ballotpedia (coverage thin — Tigard outside its scheduled-update scope), campaign sites
--   (maureenwolf.com), Facebook official pages (facebook.com/YiForTigard — manual-only last resort).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot pipeline script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-tigard-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-hillsboro-headshots.py (the direct ~400-line template — copy structure verbatim, rename city strings; gitignored but present on disk)
    - .planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md §"_tmp-tigard-headshots.py"
    - .planning/phases/178-city-of-tigard-deep-seed/178-RESEARCH.md §"Headshot Sources"
    - .planning/phases/178-city-of-tigard-deep-seed/178-02-SUMMARY.md (minted UUIDs by external_id)
  </read_first>
  <action>
    Copy the Hillsboro script structure. Set an OFFICIALS list of dicts, each with ext_id, name, url
    (the per-official source found at runtime — seed Shaw's verified tigardlife.com URL from the
    <interfaces> block), and license ('press_use' for local-news photography). Assert ext_ids are
    unique but do NOT hard-assert len(OFFICIALS)==7 — a genuine partial outcome (5/7, 6/7) is
    acceptable per RESEARCH; contrast with Hillsboro's `assert len == 7`. Resolve each politician UUID
    at RUNTIME by external_id via psycopg2 (do NOT hardcode UUIDs — read them live). Keep the config
    block identical to Hillsboro: BUCKET='politician_photos', TARGET_SIZE=(600,750), JPEG_QUALITY=90,
    RESAMPLE=Image.Resampling.LANCZOS, DESCRIPTIVE_HEADERS (requests-based). Pipeline order per
    official: download (follow redirects), composite onto white if RGBA/PNG, CROP to 4:5 FIRST, RESIZE
    to 600×750 Lanczos, reject any image with superimposed text/graphics, upload to
    politician_photos/{uuid}-headshot.jpg via PUT with x-upsert:true, emit a manifest line
    (ext_id → uuid → CDN url → source). Add a TEST-DOWNLOAD guard: before the full loop, fetch one
    image and assert non-zero bytes + valid PIL open. Include the "gitignored _tmp helper, orchestrator
    runs it; expect genuine gaps — do not force 7/7" docstring note. No fenced code in this action —
    the analog + 178-PATTERNS.md are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `python C:/EV-Accounts/backend/scripts/_tmp-tigard-headshots.py`; the script validates non-zero JPEG per image and prints the CDN manifest (partial count acceptable)</automated>
  </verify>
  <acceptance_criteria>
    - OFFICIALS list entries use the exact ext_ids from -4173651..-4173657; Shaw's verified tigardlife.com URL seeded
    - Asserts unique ext_ids; does NOT hard-assert len==7 (genuine gaps acceptable)
    - UUIDs resolved at runtime by external_id (no hardcoded UUIDs)
    - TARGET_SIZE=(600,750), JPEG_QUALITY=90, Lanczos resample
    - Crop-to-4:5 happens BEFORE resize
    - Test-download guard present; rejects overlay/text images
    - Docstring marks it a gitignored _tmp helper run by the orchestrator, gaps expected
  </acceptance_criteria>
  <done>Pipeline script authored; runs and uploads found 600×750 portraits; documents genuine gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1160_tigard_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1151_hillsboro_headshots.sql (96-line template)
    - .planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md §"1160_tigard_headshots.sql"
  </read_first>
  <action>
    Single BEGIN…COMMIT. Header comment: "AUDIT-ONLY (not registered in the ledger)". One
    politician_images INSERT … SELECT statement per official WITH a found photo (OMIT the INSERT block
    entirely for any official with no usable photo — do NOT fabricate a row), columns EXACTLY
    (id, politician_id, url, type, photo_license) — NO photo_origin_url. politician_id resolved via
    subselect on external_id (within -4173651..-4173657). url is the final Supabase CDN path
    (politician_photos/{uuid}-headshot.jpg — the orchestrator fills each {uuid} from the script manifest
    before applying, or the executor templates them from 178-02-SUMMARY.md UUIDs). type='default'.
    photo_license='press_use' (local-news photography). Each INSERT guarded WHERE NOT EXISTS on
    politician_id (idempotent). NO ledger INSERT (audit-only). Keep `photo_origin_url`/`slug`/
    `schema_migrations` out of comments.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1160_tigard_headshots.sql` then runs `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -4173657 AND -4173651)` expecting the count of found photos (5–7 acceptable)</automated>
  </verify>
  <acceptance_criteria>
    - One politician_images INSERT per official WITH a found photo; columns exactly (id, politician_id, url, type, photo_license)
    - No photo_origin_url column anywhere
    - politician_id resolved via external_id subselect (within -4173651..-4173657)
    - type='default'; photo_license='press_use'; each guarded WHERE NOT EXISTS on politician_id
    - No fabricated row for an official with no found photo
    - No ledger INSERT (audit-only); `photo_origin_url` does NOT appear in comments
  </acceptance_criteria>
  <done>Audit migration authored; applies to record the found image rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs pipeline, applies migration, verifies identity + quality</name>
  <what-built>The headshot pipeline script + audit migration. Orchestrator searches per-official, runs the pipeline, and applies the migration.</what-built>
  <how-to-verify>
    1. Per-official search (tigardlife.com/valleytimes.news/Ballotpedia/campaign) for Hu, Anderson, Ghoddusi, Robbins, Schlack, Wolf (Shaw pre-verified); populate the OFFICIALS list. Run `python C:/EV-Accounts/backend/scripts/_tmp-tigard-headshots.py`; confirm each found image yields a valid non-zero upload (HTTP 200 on each CDN URL). Record per-official source + any genuine gap.
    2. Apply `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1160_tigard_headshots.sql`; confirm the politician_images row count matches the number of found photos.
    3. Visual identity + quality check on each portrait: correct person (Hu/Anderson/Ghoddusi/Robbins/Schlack/Shaw/Wolf), 600×750, 4:5 framing, NO superimposed text/graphics over the face.
    4. Commit the migration: `git -C "C:/EV-Accounts" add backend/migrations/1160_tigard_headshots.sql && git -C "C:/EV-Accounts" commit`. Do NOT commit the _tmp-*.py helper.
  </how-to-verify>
  <acceptance_criteria>
    - Found headshots uploaded (partial 5/7 or 6/7 acceptable; genuine gaps documented, not fabricated)
    - politician_images row count matches found-photo count
    - Each portrait is the correct person, 600×750, no overlays
    - Audit migration committed; _tmp .py NOT committed
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official source + any gap, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Portraits fetched from tigardlife.com / valleytimes.news / Ballotpedia / campaign sites |
| operator → Supabase Storage | Processed images uploaded to the public politician_photos bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-08 | Spoofing | Wrong-person or mislabeled headshot (higher risk — no official portal, per-official news search) | mitigate | Per-official dedicated profile/announcement article as source; Task 3 visual identity check per official before commit; reject group-photo crops |
| T-178-09 | Tampering | Corrupt/0-byte image or WAF "Access Denied" HTML saved as .jpg | mitigate | Test-download guard + valid-PIL-open assertion; requests DESCRIPTIVE_HEADERS + follow-redirects; never fetch WAF-403 tigard-or.gov |
| T-178-10 | Information Disclosure | Superimposed campaign text/graphics on portrait | mitigate | Pipeline rejects images with overlays; visual check confirms clean face |
| T-178-SC | Tampering | npm/pip/cargo installs (Pillow/psycopg2/requests) | mitigate | Pre-existing project deps used by all prior headshot phases (Hillsboro/Beaverton) — no NEW package install; if any pip install is needed, treat as [ASSUMED] and gate on a blocking human-verify before install |
</threat_model>

<verification>
- Found 600×750 portraits uploaded; politician_images rows match found count; identity/quality
  confirmed; genuine gaps documented; audit migration committed.
</verification>

<success_criteria>
- Every official with a usable found photo has a 600×750 local-news-sourced headshot with no
  overlays; genuine gaps documented honestly (no forced 7/7); audit migration recorded.
</success_criteria>

<output>
Create `.planning/phases/178-city-of-tigard-deep-seed/178-03-SUMMARY.md` when done, recording the
per-official image source and any documented gap.
</output>
