---
phase: 203-indio-deep-seed
plan: 06
type: execute
wave: 6
depends_on: ["203-01", "203-02", "203-03", "203-04", "203-05"]
files_modified: []
autonomous: false
requirements: [CV-03, BANR-01]
must_haves:
  truths:
    - "The full production audit block is all-green: 5 X0023 geofences, City of Indio government, 5 council offices (1 per district), exactly 1 Mayor (Holmes/D3) + 1 Mayor Pro Tem (Fermon/D2), 5/5 600×750 headshots, evidence-only cited stances, clean section-split (0), DB-honest coverage chip, banner CDN 200"
    - "A live address in each of the 5 districts routes to exactly 1 correct councilmember with the right headshot"
    - "The new Indio city banner renders via the coverage.js browse chip"
  artifacts:
    - path: ".planning/phases/203-indio-deep-seed/203-06-SUMMARY.md"
      provides: "Full audit results + live-browse sign-off; the phase verification record"
      contains: "audit"
  key_links:
    - from: "live address per district"
      to: "one Indio councilmember profile"
      via: "X0023 geofence PIP routing"
      pattern: "councilmember"
    - from: "coverage.js 'Indio' chip"
      to: "the new cities/indio.jpg banner render"
      via: "browse_label → representingCity → getBuildingImages"
      pattern: "indio"
---

<objective>
Final phase verification. Run the full production audit block covering every prior wave (geofences,
government/roster with Mayor/Mayor-Pro-Tem titles, headshots, stances, coverage chip, the NEW Indio
banner) and a blocking live-browse human-verify that confirms per-district address routing, correct
headshots, and the banner rendering via the coverage chip. Goal-backward gate for ROADMAP success
criteria #1-5 before `/gsd-verify-work`.

Purpose: Proves CV-03 + BANR-01 are TRUE end-to-end in production — a real Indio resident sees their one
councilmember with a full evidence-only compass, a correct headshot, and the new city banner.
Output: `.planning/phases/203-indio-deep-seed/203-06-SUMMARY.md` — the phase verification record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-indio-deep-seed/203-CONTEXT.md
@.planning/phases/203-indio-deep-seed/203-01-SUMMARY.md
@.planning/phases/203-indio-deep-seed/203-02-SUMMARY.md
@.planning/phases/203-indio-deep-seed/203-03-SUMMARY.md
@.planning/phases/203-indio-deep-seed/203-04-SUMMARY.md
@.planning/phases/203-indio-deep-seed/203-05-SUMMARY.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / live-browser access. Both tasks are ORCHESTRATOR-RUN /
human-verify: the inline orchestrator runs the `psql`/`curl` audit block, and the operator performs the
live browse on essentials.empowered.vote. No files are modified. Per
`feedback_no_playwright_on_user_live_browser`, the live browse is read-only + operator-driven — never
wipe localStorage on the live page.
</execution_note>

<interfaces>
- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- Banner (NEW this phase, Plan 05): .../politician_photos/cities/indio.jpg
- Live app: https://essentials.empowered.vote ; browse via the coverage chip (browse_label 'Indio',
  browseGovernmentList ['0636448']) or directly /results?browse_geo_id=0636448.
- ext_ids: -4012001..-4012005 assigned in Plan 02 (from 203-02-SUMMARY.md manifest).
- Section-split guard: no office reachable from the 5 X0023 LOCAL districts under any government other
  than City of Indio; the pre-existing G4110 0636448 city boundary row must NOT carry a council office.
- Title-on-seat: exactly 1 office title='Mayor' (Holmes D3, ext_id -4012003) + exactly 1 title='Mayor
  Pro Tem' (Fermon D2, ext_id -4012002); no LOCAL_EXEC row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full production audit block (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB/CDN assertions; no files modified</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (§ Section-split / district-collision guard lines 463-483 — X0023 LOCAL scoping; never bare geo_id='0636448'; standalone split-section check expect 0; Ph202 comment-grep GOTCHA)
    - .planning/phases/203-indio-deep-seed/203-02-SUMMARY.md (the roster + ext_id block + Mayor/MPT seats for the title assertion)
    - .planning/phases/203-indio-deep-seed/203-05-SUMMARY.md (coverage chip + new 'indio' banner key confirmation)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). Run the full audit via `psql "$DATABASE_URL" -tAc`
    + `curl -sI`, recording each result in 203-06-SUMMARY.md:
    (a) 5 geofences mtfcc='X0023' state='ca', geo_id indio-ca-council-district-1..5, all ST_IsValid;
    (b) exactly 1 government 'City of Indio, California, US' (geo_id 0636448, type City);
    (c) 5 offices under 'City Council', each LOCAL X0023 district holding exactly 1 office (GROUP BY
    HAVING count<>1 → 0); no LOCAL_EXEC Mayor office;
    (d) exactly 1 office title='Mayor' (on -4012003 Holmes) + exactly 1 office title='Mayor Pro Tem'
    (on -4012002 Fermon) + 3 title='Councilmember'; appointed count among the 5 = 0 (per recon);
    (e) 5/5 politician_images rows (type='default'); `curl -sI` each of the 5 CDN URLs → HTTP 200;
    PIL-verify a sample is 600×750;
    (f) stances: 0 uncited (every answer has a context row with non-empty sources), 0 judicial-* rows,
    all values in [1.0,5.0] (do NOT assert a fixed per-member count — honest blanks expected);
    (g) section-split: 0 offices reachable from the 5 X0023 districts under any non-Indio government;
    the pre-existing G4110 0636448 city row carries no council office (run the standalone split-section
    check from 203-PATTERNS.md lines 470-479 — expect 0);
    (h) coverage.js 'Indio' chip present with hasContext:true; buildingImages.js NEW 'indio' entry present
    → cities/indio.jpg; banner `curl -sI cities/indio.jpg` → HTTP 200.
    ANY red assertion blocks phase verification — surface it for gap closure, do NOT hand-wave.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0023' AND state='ca')=5 AND (SELECT count(*) FROM essentials.governments WHERE name='City of Indio, California, US' AND geo_id='0636448')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0023' AND d.district_type='LOCAL' AND d.state='ca')=5 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id=o.politician_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0023' AND o.title='Mayor' AND p.external_id=-4012003)=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id=o.politician_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0023' AND o.title='Mayor Pro Tem' AND p.external_id=-4012002)=1 AND (SELECT count(*) FROM essentials.politician_images pi JOIN essentials.offices o ON o.politician_id=pi.politician_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0023')=5 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.chambers c ON c.id=o.chamber_id WHERE d.geo_id LIKE 'indio-ca-council-district-%' AND d.district_type='LOCAL' AND d.state='ca' AND d.mtfcc='X0023' AND c.government_id <> (SELECT id FROM essentials.governments WHERE name='City of Indio, California, US'))=0 AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.offices o ON o.politician_id=pa.politician_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0023' AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id AND array_length(pc.sources,1) IS NOT NULL))"</automated>
  </verify>
  <acceptance_criteria>
    - Combined boolean SELECT returns `t` (5 X0023 geofences + 1 City of Indio gov + 5 council offices + exactly 1 Mayor on -4012003 + exactly 1 Mayor Pro Tem on -4012002 + 5 headshots + section-split=0 + 0 uncited stance rows).
    - Each X0023 district holds exactly 1 office (no HAVING count<>1 violation); no LOCAL_EXEC Mayor office.
    - 3 offices title='Councilmember'; appointed count = 0; standalone section-split query returns 0 rows; G4110 0636448 row carries no council office.
    - 0 judicial-* stance rows; all stance values in [1.0,5.0].
    - `curl -sI` on the 5 headshot CDN URLs and cities/indio.jpg all return HTTP 200; a sampled headshot is 600×750.
    - coverage.js 'Indio' hasContext:true chip present; buildingImages.js NEW 'indio' entry present → cities/indio.jpg.
    - All audit checks recorded green in 203-06-SUMMARY.md.
  </acceptance_criteria>
  <done>Full production audit is all-green across geofences, government/roster (Mayor/MPT titles), headshots, stances, section-split, coverage, and the new banner.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-browse per-district routing + banner render sign-off (OPERATOR)</name>
  <files>N/A — operator-driven read-only live browse; no files modified</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-01-SUMMARY.md (the 5 district centroids for choosing a probe address per district)
    - .planning/phases/203-indio-deep-seed/203-02-SUMMARY.md (the roster → district mapping to confirm the right member appears)
  </read_first>
  <what-built>
    The complete Indio deep-seed: 5 X0023 council-district geofences, City of Indio government + City
    Council, 5 by-district members (Holmes=Mayor D3, Fermon=Mayor Pro Tem D2), 5 headshots, evidence-only
    stances, a coverage.js chip, and the new city banner.
  </what-built>
  <how-to-verify>
    OPERATOR, read-only on https://essentials.empowered.vote (never wipe localStorage on the live page):
    1. Enter a full street address in EACH of the 5 council districts (one per district; use the Plan 01
       centroids as a guide). Confirm each routes to EXACTLY ONE Indio councilmember — the right person
       for that district — with their headshot and evidence-only compass. Confirm D3 shows the 'Mayor'
       title (Holmes) and D2 shows 'Mayor Pro Tem' (Fermon).
    2. Browse the coverage chip (or /results?browse_geo_id=0636448). Confirm the 'Indio' chip renders,
       lists the 5 members, and the NEW Indio city banner (Old Town / downtown street scene) displays —
       NOT a gradient, NOT the Palm Springs / Riverside / CV banner, NOT the state shot.
    3. Confirm no console errors and no party affiliation displayed on any profile (antipartisan).
    Report any mis-routing, wrong-person, missing headshot, wrong/missing banner, or stray title.
    Provide the live browse link (/results?browse_geo_id=0636448) at completion.
  </how-to-verify>
  <verify>
    <automated>MISSING — operator-driven live browse; no automated command (executor has no live-browser access). Verified via the 5 per-district routing confirmations + banner render recorded in 203-06-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 district probe addresses routes to exactly 1 correct councilmember with the right headshot + compass.
    - D3 shows 'Mayor' (Holmes), D2 shows 'Mayor Pro Tem' (Fermon); no duplicate/extra members.
    - The 'Indio' coverage chip renders and the NEW Indio city banner displays (not a gradient / Palm Springs / CV / state shot).
    - No console errors; no party affiliation displayed (antipartisan).
    - Operator sign-off + live browse link recorded in 203-06-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 5 districts route correctly, titles are right, and the new Indio city banner renders — or describe the routing/banner discrepancy for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live resident address → PIP routing | Untrusted address input resolves to a district/office via the geofence GIST query |
| production DB/CDN → operator browse | Rendered roster/headshot/banner/compass observed on the live app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-02 | Tampering | wrong-district routing / wrong member | mitigate | Task 2 operator probes all 5 districts and confirms exactly-one-correct-member routing; Task 1 asserts 1 office per X0023 district |
| T-203-SPLIT | Tampering | section-split / cross-gov office leak | mitigate | Task 1 gate (g) standalone check = 0 offices under any non-Indio government; G4110 0636448 row carries no office |
| T-203-BANNER | Information Disclosure | broken/wrong banner (e.g. Palm Springs/CV banner hijack) | mitigate | Task 1 `curl -sI` cities/indio.jpg → 200; Task 2 operator confirms the NEW Indio banner renders (not the CV/Palm Springs banner) |
| T-203-PARTY | Information Disclosure | party affiliation displayed | mitigate | Task 2 confirms no party shown on any profile (antipartisan, project-wide) |
</threat_model>

<verification>
- Full production audit all-green (geofences, gov/roster/titles, headshots, stances, section-split=0, coverage, banner CDN 200).
- 5 per-district addresses each route to exactly 1 correct member; the new banner renders via the chip.
- No console errors, no party display.
</verification>

<success_criteria>
CV-03 + BANR-01 are TRUE end-to-end: an Indio resident sees their one councilmember with a correct
headshot, evidence-only compass, and the new city banner.
</success_criteria>

<output>
Create `.planning/phases/203-indio-deep-seed/203-06-SUMMARY.md` when done (full audit results +
per-district routing confirmations + operator sign-off + live browse link — the phase verification
record).
</output>
</content>
