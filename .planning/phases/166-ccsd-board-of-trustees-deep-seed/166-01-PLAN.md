---
phase: 166-ccsd-board-of-trustees-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-ccsd-school-boundary.ts
  - C:/EV-Accounts/backend/migrations/1107_ccsd_board_of_trustees.sql
  - C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [CCSD-01]
user_setup: []

must_haves:
  truths:
    - "Wave-0 BLOCKING probes confirm (before any write): live on-disk migration MAX (highest file +1, trust on-disk over the registered ledger — expected 1106 → next structural 1107); external_id block -3209001..-3209011 has 0 existing rows; no pre-existing 'Clark County School District, Nevada, US' government; no pre-existing G5420 row for geo_id='3200060'; TS loader deps (adm-zip, shapefile) present in C:/EV-Accounts/backend/node_modules; loader --dry-run confirms GEOID 3200060 found in tl_2024_32_unsd.zip BEFORE the real run [D-04b, D-02a]"
    - "A Wave-0 operator checkpoint verifies the live 11-member composition against ccsd.net/trustees — 7 elected (Stevens A, Dominguez B, Henry C, Zamora D, Biassotti E, Bustamante Adams F, Cavazos G) + 4 appointed (Barron→North Las Vegas, Esparza-Stoffregen→Henderson, Johnson→Las Vegas, Satory→Clark County); exact spellings/diacritics + appointing jurisdictions locked; officers (Stevens President / Bustamante Adams VP / Dominguez Clerk) are title-on-seat NOT separate seats (11 seats, not 14) — before the structural migration applies [D-01, D-01a]"
    - "The CCSD G5420 TIGER UNSD boundary is LOADED this phase via load-ccsd-school-boundary.ts (a single-GEOID variant of load-or-school-boundaries.ts): tl_2024_32_unsd.zip, GEOID 3200060, state='32' FIPS, mtfcc='G5420', source='tiger_unsd_nv_2024', EXPECTED_COUNT=1, geometry inserted directly (NO geometry-copy migration) — and the loader runs BEFORE migration 1107 [D-02a]"
    - "A standalone government 'Clark County School District, Nevada, US' exists (type='LOCAL' per the 254_or school-district precedent, state='NV' uppercase, geo_id='3200060'), NOT nested under State of Nevada geo_id='32' [D-04]"
    - "A chamber 'Board of School Trustees' (official_count=11, name_formal non-empty, the auto-generated path column NEVER in the INSERT list) exists under that government [D-04a]"
    - "Exactly ONE SCHOOL district (district_type='SCHOOL', state='nv' lowercase, geo_id='3200060', mtfcc='G5420', label='Clark County School District') carries all 11 trustee offices — the single-shared-district pattern (SF/SD/Portland precedent), NOT one district per trustee [D-02, D-04a]"
    - "7 elected trustees (external_id -3209001..-3209007) have is_appointed=false + is_appointed_position=false, title='Trustee, District A'..'Trustee, District G'; 4 appointed trustees (-3209008..-3209011) have is_appointed=true + is_appointed_position=true, title='Trustee, Appointed – <Jurisdiction>' with NO fabricated district letter [D-01, D-01a]"
    - "Post-verify: exactly 1 government row; exactly 11 offices on the SCHOOL district; the 7/4 elected-appointed split (is_appointed_position false:7, true:4); office_id back-filled for all 11; zero section-split (the G5420 geo_id='3200060' geofence has a matching SCHOOL district); all 11 seeded is_active=true, is_incumbent=true, party=NULL (antipartisan, school-board precedent)"
    - "smoke-nv-geofences.ts is extended so the 5 existing Clark County test points (Strip + LV + Henderson + NLV + Boulder City) ALSO return G5420 geo_id=3200060 (CCSD covers the whole county); the PIP query is unchanged [VALIDATION smoke check]"
    - "Clark County School District surfaces with the purple hasContext chip in src/lib/coverage.js — appended to the EXISTING Nevada COVERAGE_STATES block (after Boulder City) using the geofence-keyed browseGeoId:'3200060' + browseMtfcc:'G5420' shape (NOT the city/county browseGovernmentList form), LV + Henderson + NLV + Boulder City preserved [CCSD-01 SC#4]"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-ccsd-school-boundary.ts"
      provides: "G5420 TIGER UNSD loader (single-GEOID variant of load-or-school-boundaries.ts): downloads tl_2024_32_unsd.zip, filters to GEOID 3200060, inserts one geofence_boundaries row with real geometry (ST_ForcePolygonCCW(ST_SetSRID(ST_Force2D(ST_GeomFromGeoJSON())))), ON CONFLICT (geo_id, mtfcc) DO NOTHING, EXPECTED_COUNT=1, --dry-run support"
      contains: "tiger_unsd_nv_2024"
      min_lines: 200
    - path: "C:/EV-Accounts/backend/migrations/1107_ccsd_board_of_trustees.sql"
      provides: "Structural migration: pre-flight (idempotent NOTICE if gov exists) + 1 government + 1 chamber + 1 shared SCHOOL district on geo_id='3200060' + 11 politician+office CTEs (7 elected A–G + 4 appointed by jurisdiction) + office_id back-fill + post-verify DO block (gov=1 / offices=11 / split=0 / 7-4 split) + ledger registration; registers as 1107 (or Wave-0-confirmed number)"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 280
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "COVERAGE_STATES Nevada block with Clark County School District appended (browseGeoId:'3200060', browseMtfcc:'G5420', browseStateAbbrev:'NV', hasContext:true); LV + Henderson + NLV + Boulder City preserved"
      contains: "Clark County School District"
  key_links:
    - from: "scripts/load-ccsd-school-boundary.ts"
      to: "essentials.geofence_boundaries (geo_id='3200060', mtfcc='G5420', state='32', source='tiger_unsd_nv_2024')"
      via: "TIGER UNSD shapefile filter on GEOID + ST_GeomFromGeoJSON insert, run BEFORE migration 1107"
      pattern: "3200060"
    - from: "essentials.chambers (Board of School Trustees)"
      to: "essentials.governments (Clark County School District, Nevada, US, geo_id='3200060')"
      via: "government_id subquery WHERE name='Clark County School District, Nevada, US'"
      pattern: "Clark County School District, Nevada, US"
    - from: "essentials.offices (11 trustees)"
      to: "essentials.districts (ONE shared SCHOOL district, geo_id='3200060', state='nv')"
      via: "district join on geo_id + district_type='SCHOOL' + state='nv' lowercase"
      pattern: "state = 'nv'"
    - from: "essentials.geofence_boundaries (G5420 geo_id='3200060')"
      to: "essentials.districts (SCHOOL, geo_id='3200060')"
      via: "shared geo_id; backend ST_Covers PIP resolves G5420 → SCHOOL district → all 11 trustees"
      pattern: "G5420"
    - from: "src/lib/coverage.js COVERAGE_STATES Nevada block"
      to: "CCSD browse (geo_id 3200060, mtfcc G5420)"
      via: "browseGeoId + browseMtfcc entry"
      pattern: "3200060"
---

<objective>
Deep-seed the structural + geofence + surfacing half of the Clark County School District. This is the only NV deep-seed that LOADS a new geofence: CCSD's G5420 TIGER UNSD boundary does not exist (Phase 158 skipped school districts), so this plan writes and runs a single-GEOID loader (`load-ccsd-school-boundary.ts`, GEOID 3200060, `tl_2024_32_unsd.zip`, `source='tiger_unsd_nv_2024'`) that inserts the real county-wide polygon directly — no geometry-copy migration. First a Wave-0 BLOCKING probe confirms the live migration MAX (trust the highest on-disk file +1 — expected 1106 → next structural 1107), the unused external_id block -3209001..-3209011, no pre-existing CCSD government, no pre-existing G5420 row for geo_id='3200060', the TS loader deps, and a loader `--dry-run` proving GEOID 3200060 is in the shapefile. A Wave-0 operator checkpoint then verifies the live 11-member roster (7 elected Districts A–G + 4 appointed by jurisdiction), exact spellings/diacritics, appointing jurisdictions, and that the board officers are title-on-seat (NOT extra seats) BEFORE any migration applies. Then run the loader (before the migration), create a standalone government "Clark County School District, Nevada, US" (type='LOCAL' per the 254_or school-district precedent — D-04, NOT nested under State of Nevada geo_id='32'), one chamber "Board of School Trustees" (official_count=11 — D-04a), ONE shared SCHOOL district on geo_id='3200060' (the SF/SD/Portland single-shared-district shape — NOT one district per trustee), all 11 politician+office rows (7 elected A–G with is_appointed=false; 4 appointed with is_appointed=true + jurisdiction-in-title, no fabricated district letter — D-01/D-01a), and back-fill office_id. Extend `smoke-nv-geofences.ts` so the 5 existing Clark County points also assert the new G5420 boundary, and append CCSD to the EXISTING Nevada block in `src/lib/coverage.js` COVERAGE_STATES (geofence-keyed browseGeoId + browseMtfcc — the first school-district coverage entry).

Purpose: Satisfies the structural + routing + surfacing half of CCSD-01 (G5420 boundary loaded with geometry; all 11 trustees resolve via the one SCHOOL district; SC#4 purple hasContext chip). Headshots are Plan 02; stances + final 10-check E2E verification are Plan 03.
Output: load-ccsd-school-boundary.ts (run before the migration), 1107_ccsd_board_of_trustees.sql (structural, registered), the smoke-test extension, the coverage.js NV-block edit, plus the captured Wave-0 probe facts + the 11 external_id→UUID map recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and NO DB access):**
- The executor WRITES the loader .ts, the migration .sql, the smoke-test extension, and the coverage.js edit to disk and documents the exact apply/run steps. The executor does NOT run DB SELECTs, run the loader, or apply migrations.
- The Wave-0 DB probes, the loader `--dry-run` + the real loader run (`npx tsx scripts/load-ccsd-school-boundary.ts` from C:/EV-Accounts/backend), the roster operator checkpoint, applying mig 1107 (via `psql -f` using the EV-Accounts/backend/.env DATABASE_URL), running the extended smoke test, and every post-apply audit SELECT are INLINE ORCHESTRATOR steps performed by execute-phase.
- Each task below marks which steps are executor-write vs inline-orchestrator-run/apply.
- The loader + migration + smoke test live in the SEPARATE repo C:/EV-Accounts/backend/ (commit via `git -C "C:/EV-Accounts"`, always from the repo root — never cd into backend). The coverage.js edit lives in the essentials repo. SUMMARY.md goes in the essentials .planning phase dir.
- RUN ORDER IS LOAD-BEFORE-MIGRATE: the G5420 loader MUST run before `psql -f 1107_*.sql` — the SCHOOL district INSERT and the section-split DO block both reference the geofence row by geo_id='3200060' AND mtfcc='G5420'.
</execution_context>

<context>
@.planning/phases/166-ccsd-board-of-trustees-deep-seed/166-RESEARCH.md
@.planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md
@.planning/phases/166-ccsd-board-of-trustees-deep-seed/166-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
CCSD UNSD GEOID: '3200060' (NCES 3200060 / Census sch3200060) — used for BOTH the G5420 geofence row AND the standalone government geo_id. County-wide unified district ≈ Clark County boundary.
G5420 loader: load-ccsd-school-boundary.ts is a single-GEOID copy of load-or-school-boundaries.ts. Change ONLY the config block: TIGER_URL='https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_32_unsd.zip' (NV FIPS 32; HTTP 200 / 403 KB confirmed), MTFCC='G5420', STATE='32', SOURCE='tiger_unsd_nv_2024', EXPECTED_COUNT=1, TARGET_GEOIDS=Map([['3200060','Clark County School District']]); rename baseName/tmpRoot (tl_2024_32_unsd, .tmp-nv-school-unsd). The download/redirect/cache, AdmZip extract, shapefile.open + GEOID-field filter, geometry insert (ON CONFLICT (geo_id, mtfcc) DO NOTHING), and post-insert count verification are copied verbatim. ocd_id = the GEOID (same as geo_id), matching the OR loader. NO geometry-copy migration (350-style shell backfill does NOT apply — the loader inserts the real polygon).
Government: name='Clark County School District, Nevada, US', type='LOCAL' (254_or school-district precedent — NOT 'City'/'County'), state='NV' UPPERCASE, city=NULL, geo_id='3200060' (standalone, NOT under State of Nevada geo_id='32'). governments has NO unique constraint + NO government_type column — WHERE NOT EXISTS guard mandatory.
Chamber: name='Board of School Trustees', name_formal='Clark County School District Board of School Trustees', official_count=11. The auto-generated path column is GENERATED ALWAYS — NEVER in the INSERT list; name_formal non-empty.
ONE shared SCHOOL district (the SF/SD/Portland single-shared-district pattern — NOT one per trustee):
  district_type='SCHOOL' (NOT 'SCHOOL_DISTRICT'), state='nv' LOWERCASE, geo_id='3200060', label='Clark County School District', mtfcc='G5420'. All 11 trustee offices attach here.
Office district join key: each trustee → d.geo_id='3200060' AND d.district_type='SCHOOL' AND d.state='nv' (uppercase 'NV' matches ZERO rows — silent no-op, the #1 NV failure mode).
offices.representing_state='NV' UPPERCASE (free-text label column, NOT the district join key).
Office NOT EXISTS guard: (district_id, politician_id) — one person per office.
Roster + external_ids (party=NULL all — antipartisan + school-board precedent 254_or; ALL is_active=true, is_incumbent=true, is_vacant=false):
  ELECTED (is_appointed=false, is_appointed_position=false, title='Trustee, District X'):
    -3209001 Emily Stevens (District A; President — officer title-on-seat, NOT a separate seat)
    -3209002 Lydia Dominguez (District B; Clerk — officer title-on-seat)
    -3209003 Tameka Henry (District C)
    -3209004 Brenda Zamora (District D)
    -3209005 Lorena Biassotti (District E)
    -3209006 Irene Bustamante Adams (District F; Vice President — officer title-on-seat)
    -3209007 Linda P. Cavazos (District G)
  APPOINTED (is_appointed=TRUE, is_appointed_position=TRUE, title='Trustee, Appointed – <Jurisdiction>', NO district letter):
    -3209008 Isaac Barron (Appointed – City of North Las Vegas)
    -3209009 Ramona Esparza-Stoffregen (Appointed – City of Henderson)
    -3209010 Adam Johnson (Appointed – City of Las Vegas)
    -3209011 Lisa Satory (Appointed – Clark County)
  Wave-0 locks exact spellings/diacritics + the 4 appointing jurisdictions against ccsd.net/trustees (file saved UTF-8). Officers (President/VP/Clerk) are titles on Stevens/Bustamante Adams/Dominguez — 11 seats, NOT 14. Planner's discretion exercised: officer roles are NOT surfaced as separate seats; do not add an officer annotation column this phase (keep title = the district/jurisdiction label).
Migration: structural number 1107 (on-disk MAX confirmed 1106 in research — re-verify live and use highest-file +1). Registers in the ledger. Only 1107 registers; the loader is a TS script (no migration number); Plan 02 headshot 1108 + Plan 03 stances 1109-1119 are audit-only.
Backend routing: the G5420 geofence is CREATED by the loader this phase; once present, backend ST_Covers PIP resolves a Clark County point → G5420 geo_id=3200060 → SCHOOL district → all 11 trustees. No backend code change.
coverage.js: NO existing school district is surfaced in coverage.js (CCSD is the first). Use the browseGeoId + browseMtfcc shape the coverageAreaToPath function already supports — NOT browseGovernmentList. Entry belongs in the NV COVERAGE_STATES block (after Boulder City), NOT COVERAGE_COUNTIES (which already has 'Clark County' → 32003 — a distinct county entry, leave untouched).
Grep-gate forbidden tokens in .sql comments: paraphrase the auto-generated path column (→ "the auto-generated path column"); keep slug / the removed image-origin column literal out; the ledger table token appears ONLY in the actual ledger-registration INSERT, never in a comment.
Casing convention: districts.state='nv' lowercase (routing join key — 'NV' = 0 rows); governments.state / offices.representing_state='NV' uppercase; the G5420 geofence row is state='32' (FIPS).
Browse link: essentials.empowered.vote/results?browse_geo_id=3200060&browse_mtfcc=G5420
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave-0 BLOCKING DB probes + loader dry-run + roster operator checkpoint (inline orchestrator)</name>
  <read_first>
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-VALIDATION.md (Wave 0 Requirements — the exact probe queries + the loader --dry-run + roster checkpoint)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-RESEARCH.md (§Phase Requirements UNSD GEOID; §Common Pitfalls 1/2/6; §Open Questions roster names/diacritics; §Environment Availability loader deps)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md (§load-ccsd-school-boundary.ts — the config block + dry-run note; the NV three-casing rule)
    - .planning/phases/165-boulder-city-deep-seed/165-01-PLAN.md (the prior NV deep-seed Wave-0 probe→roster checkpoint flow + the on-disk-MAX-vs-ledger drift lesson)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor — these need DB/filesystem access). Run all BLOCKING probes, the loader dry-run, and the roster checkpoint BEFORE the executor's loader/migration tasks proceed. Record every result in the SUMMARY.
(a) Live on-disk migration MAX: `ls C:/EV-Accounts/backend/migrations | grep -oE '^[0-9]+' | sort -n | tail -1` — trust the highest on-disk file +1 as the structural migration number (expected 1106 → 1107; the on-disk file number is the authority, NOT the registered ledger which is timestamp-versioned). If the on-disk MAX differs from 1106, renumber the structural migration (1107) and ALL downstream audit-only migrations (Plan 02 headshot 1108, Plan 03 stances 1109-1119) by the same offset and note it.
(b) external_id collision probe: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -3209011 AND -3209001` (more-negative bound first) — expect 0. If non-zero, pick the next unused -3209xxx block and propagate to every downstream file.
(c) No pre-existing government: `SELECT COUNT(*) FROM essentials.governments WHERE name='Clark County School District, Nevada, US'` — expect 0 (greenfield).
(d) No pre-existing G5420 row: `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id='3200060'` — expect 0 (confirms the greenfield G5420 load; NV has no school-district geofences).
(e) TS loader deps present: `ls C:/EV-Accounts/backend/node_modules/adm-zip C:/EV-Accounts/backend/node_modules/shapefile` — both must exist (the loader needs them).
(f) Loader --dry-run: AFTER Task 2 writes the loader, run `npx tsx scripts/load-ccsd-school-boundary.ts --dry-run` from C:/EV-Accounts/backend — it must log the found GEOIDs and confirm GEOID '3200060' is present in tl_2024_32_unsd.zip (the loader hard-fails EXPECTED_COUNT if absent). This dry-run GATES the real loader run. (If the loader is not yet written when this checkpoint first runs, defer step (f) until Task 2 produces the file, then re-confirm before Task 3 applies the migration.)
Then read the CURRENT coverage.js NV block to confirm Las Vegas (3240000), Henderson (3231900), North Las Vegas (3251800), AND Boulder City (3206500) entries are present (Task 5 appends CCSD after them), and re-read the exact line range (it shifted after the Phase 165 Boulder City append).
Then the ROSTER OPERATOR CHECKPOINT: verify the live CCSD board against ccsd.net/trustees (WAF-403 to curl but readable in a browser) — 7 elected: District A Emily Stevens (President), B Lydia Dominguez (Clerk), C Tameka Henry, D Brenda Zamora, E Lorena Biassotti, F Irene Bustamante Adams (Vice President), G Linda P. Cavazos; 4 appointed (AB175/2023): Isaac Barron→City of North Las Vegas, Ramona Esparza-Stoffregen→City of Henderson, Adam Johnson→City of Las Vegas, Lisa Satory→Clark County. Lock exact spellings/diacritics + the 4 appointing jurisdictions. Confirm the board officers (President/VP/Clerk) are titles on existing elected trustees, NOT separate seats (11 seats total, not 14). Confirm the appointed four are currently nonvoting (SB460 voting rights begin 2027) — this only affects stance evidence (Plan 03), not the structural seed. Pause for operator confirmation before the migration applies. If the roster has shifted (appointed-seat rotation), adjust names/ext_ids/titles before writing the migration.
This task GATES the loader run + the structural migration (Tasks 3+4): do not run the loader or apply mig 1107 until all probes pass, the dry-run confirms GEOID 3200060, and the roster is operator-confirmed.
  </action>
  <verify><human-check>Operator confirms: on-disk migration MAX recorded (structural number = MAX+1, expected 1107); external_id block -3209001..-3209011 returns 0 rows; no pre-existing CCSD government (0 rows); no pre-existing G5420 row for geo_id='3200060' (0 rows); adm-zip + shapefile present in node_modules; the loader --dry-run lists GEOID 3200060 found in tl_2024_32_unsd.zip; coverage.js NV block has LV + Henderson + NLV + Boulder City; and the 11-member roster (7 elected A–G + 4 appointed) verified against ccsd.net/trustees with exact spellings/jurisdictions locked and officers confirmed NOT to be separate seats (11 seats, not 14).</human-check></verify>
  <acceptance_criteria>
    - On-disk migration MAX recorded in SUMMARY; structural number set to MAX+1 (expected 1107); any drift offset applied to all downstream files.
    - external_id probe recorded = 0 for -3209011..-3209001 (or a new block chosen + propagated).
    - No-government probe recorded = 0 (greenfield).
    - No-G5420-row probe recorded = 0 for geo_id='3200060' (greenfield geofence load).
    - adm-zip + shapefile confirmed present in C:/EV-Accounts/backend/node_modules.
    - Loader --dry-run recorded: GEOID 3200060 found in tl_2024_32_unsd.zip (EXPECTED_COUNT satisfiable) — gates the real run.
    - coverage.js NV block confirmed to currently hold Las Vegas + Henderson + North Las Vegas + Boulder City; exact insertion line re-read.
    - 11-member roster operator-verified against ccsd.net/trustees: 7 elected A–G + 4 appointed; exact spellings/diacritics + 4 appointing jurisdictions locked; officers (President/VP/Clerk) confirmed as title-on-seat (11 seats, not 14); appointed-four nonvoting noted for Plan 03.
    - This checkpoint is recorded as passed BEFORE the loader runs + mig 1107 applies.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded probe values + the dry-run GEOID confirmation + roster confirmation, or describe any collision (pick a new block), drift (renumber), missing dep (block), shapefile GEOID miss (investigate FIPS/vintage), or roster change (adjust names/ext_ids/titles).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the G5420 TIGER UNSD loader load-ccsd-school-boundary.ts</name>
  <files>C:/EV-Accounts/backend/scripts/load-ccsd-school-boundary.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts (EXACT ANALOG — copy the file verbatim; change ONLY the config block at lines ~31-46 + the baseName/tmpRoot at ~50-53. The download/redirect/cache, AdmZip extract, shapefile.open + GEOID filter, the geometry insert at ~210-219, and post-insert count verification are reused unchanged.)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md (§load-ccsd-school-boundary.ts — the exact config block, the geometry-insert excerpt, the GEOID-field-filter note, the run-order note)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-RESEARCH.md (§Pattern 1; §Common Pitfalls 1+2; Assumptions A1; §Anti-Patterns — no geometry-less shell, no 350-style backfill)
  </read_first>
  <action>
EXECUTOR STEP: write `load-ccsd-school-boundary.ts` by copying `load-or-school-boundaries.ts` verbatim and changing ONLY the config block to the CCSD values: TIGER_URL='https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_32_unsd.zip' (NV FIPS 32), MTFCC='G5420', STATE='32', SOURCE='tiger_unsd_nv_2024', EXPECTED_COUNT=1, TARGET_GEOIDS=new Map([['3200060','Clark County School District']]); and rename baseName/tmpRoot to tl_2024_32_unsd / .tmp-nv-school-unsd. Keep verbatim: the redirect-following download + on-disk cache, the AdmZip extract, the shapefile.open + GEOID-field filter (the loader logs the first-feature field names and hard-fails if GEOID is missing), the geometry insert `INSERT INTO essentials.geofence_boundaries (geo_id, ocd_id, name, state, mtfcc, geometry, source, imported_at) VALUES ($1,$2,$3,$4,$5, public.ST_ForcePolygonCCW(public.ST_SetSRID(public.ST_Force2D(public.ST_GeomFromGeoJSON($6)),4326)), $7, now()) ON CONFLICT (geo_id, mtfcc) DO NOTHING` with params [geoId, geoId, name, STATE, MTFCC, geometryJson, SOURCE] (ocd_id = the GEOID, same as geo_id), and the post-insert count verification + the --dry-run branch (lists found GEOIDs without writing). Do NOT add a geometry-less shell path and do NOT add a 350-style geometry-copy step (the loader inserts the real polygon directly). DB connection reads C:/EV-Accounts/backend/.env DATABASE_URL exactly as the analog.

Do NOT run the loader in this task — running it (and the --dry-run) is the inline-orchestrator step (the dry-run is part of Task 1's gate; the real run is Task 3).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/load-ccsd-school-boundary.ts"; test -f "$F" &amp;&amp; grep -q "tl_2024_32_unsd.zip" "$F" &amp;&amp; grep -q "tiger_unsd_nv_2024" "$F" &amp;&amp; grep -q "G5420" "$F" &amp;&amp; grep -q "3200060" "$F" &amp;&amp; grep -q "ST_GeomFromGeoJSON" "$F" &amp;&amp; grep -q "ON CONFLICT (geo_id, mtfcc)" "$F" &amp;&amp; grep -q "dry-run\|dryRun\|DRY_RUN" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - load-ccsd-school-boundary.ts exists, ≥200 lines, structurally identical to load-or-school-boundaries.ts with only the config block + baseName/tmpRoot changed.
    - Config: TIGER_URL = the NV FIPS-32 UNSD zip; MTFCC='G5420'; STATE='32'; SOURCE='tiger_unsd_nv_2024'; EXPECTED_COUNT=1; TARGET_GEOIDS = single entry ['3200060','Clark County School District'].
    - Geometry insert copied verbatim (ST_ForcePolygonCCW(ST_SetSRID(ST_Force2D(ST_GeomFromGeoJSON())))), ON CONFLICT (geo_id, mtfcc) DO NOTHING, ocd_id = geoId.
    - --dry-run branch present (lists found GEOIDs without writing).
    - NO geometry-less shell path; NO 350-style geometry-copy logic.
    - Loader reads DATABASE_URL from C:/EV-Accounts/backend/.env (analog pattern).
  </acceptance_criteria>
  <done>load-ccsd-school-boundary.ts written as a single-GEOID variant of load-or-school-boundaries.ts (GEOID 3200060, tl_2024_32_unsd.zip, source='tiger_unsd_nv_2024', EXPECTED_COUNT=1), geometry inserted directly, --dry-run support. Not yet run.</done>
</task>

<task type="auto">
  <name>Task 3: Run the loader inline + write + apply structural migration 1107 + post-verify</name>
  <files>C:/EV-Accounts/backend/migrations/1107_ccsd_board_of_trustees.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/254_or_school_districts.sql (school-district shape: governments type='LOCAL' + WHERE NOT EXISTS guard, 'Board of …' chamber, district_type='SCHOOL' + mtfcc='G5420' + state lowercase, board-member CTE blocks all on one G5420 district, is_appointed=false on elected)
    - C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql (single-shared-district + ledger + post-verify analog: all members on ONE district, NV casing, office_id back-fill, post-verify DO block gov=1/office-count/section-split=0, out-of-transaction ledger registration)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md (§1107_ccsd_board_of_trustees.sql — every step's adapted SQL: government / chamber / SCHOOL district / 7 elected CTEs / 4 appointed CTEs / office_id back-fill / post-verify DO block / ledger line)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-RESEARCH.md (§Pattern 2; §Anti-Patterns; Pitfalls 1/4/5/6/7 — esp. Pitfall 4 appointed-mislabel, Pitfall 5 officers-not-extra-seats, Pitfall 6 lowercase 'nv')
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-01-... (this plan's <interfaces> roster + external_ids)
  </read_first>
  <action>
EXECUTOR STEP: write `1107_ccsd_board_of_trustees.sql` as a composite of 254_or (school-district shape) + 1055 (single-shared-district + ledger + post-verify). Steps:
- Pre-flight DO block: idempotent NOTICE if the CCSD government already exists (1055 re-runnable style). NO ward assertion (school district, no wards).
- Step 1 Government: INSERT 'Clark County School District, Nevada, US', type='LOCAL' (school precedent — NOT 'City'/'County'), state='NV' uppercase, city=NULL, geo_id='3200060' WHERE NOT EXISTS (mandatory — no unique constraint).
- Step 2 Chamber: INSERT 'Board of School Trustees' / name_formal='Clark County School District Board of School Trustees' / government_id subquery / official_count=11 WHERE NOT EXISTS. NEVER include the auto-generated path column in the INSERT list; name_formal non-empty.
- Step 3 ONE shared SCHOOL district: a SINGLE INSERT district_type='SCHOOL' (NOT 'SCHOOL_DISTRICT'), state='nv' LOWERCASE, geo_id='3200060', label='Clark County School District', mtfcc='G5420' WHERE NOT EXISTS. This is the SF/SD/Portland single-shared-district pattern — do NOT create 11 districts.
- Step 4 politician+office CTEs (11 blocks): 7 ELECTED (Stevens A -3209001, Dominguez B -3209002, Henry C -3209003, Zamora D -3209004, Biassotti E -3209005, Bustamante Adams F -3209006, Cavazos G -3209007) each WITH ins_p INSERT politicians (party=NULL, is_active=true, is_appointed=FALSE, is_vacant=false, is_incumbent=true, external_id) ON CONFLICT (external_id) DO NOTHING RETURNING id → INSERT offices (chamber_id subquery, title='Trustee, District X', representing_state='NV' uppercase, is_appointed_position=FALSE, is_vacant=false, role_canonical=NULL) joined on the SCHOOL district by geo_id='3200060'+district_type='SCHOOL'+state='nv' with NOT EXISTS (district_id, politician_id) guard. Then 4 APPOINTED (Barron -3209008, Esparza-Stoffregen -3209009, Johnson -3209010, Satory -3209011) — SAME skeleton but is_appointed=TRUE on the politician AND is_appointed_position=TRUE on the office, title='Trustee, Appointed – <Jurisdiction>' (North Las Vegas / Henderson / Las Vegas / Clark County respectively) with NO district letter. ALL 11 use the ONE SCHOOL district (no LOCAL_EXEC, no per-trustee district). Officers (President/VP/Clerk) are NOT seeded as extra seats and not as a separate annotation — keep title = the district/jurisdiction label (planner discretion: officer roles omitted).
- Step 5 office_id back-fill: UPDATE politicians SET office_id=o.id WHERE external_id BETWEEN -3209011 AND -3209001 (more-negative bound first) AND office_id IS NULL.
- Step 6 post-verify DO block (RAISE EXCEPTION on mismatch): exactly 1 CCSD government row; exactly 11 offices on the SCHOOL district (geo_id='3200060' AND district_type='SCHOOL' AND state='nv'); the 7/4 split (GROUP BY is_appointed_position → false:7, true:4); section-split detector (0 orphan rows — the G5420 geo_id='3200060' geofence must have a matching SCHOOL district state='nv'). The exact DO-block shape is in 166-PATTERNS.md "Post-verify DO block" + 166-RESEARCH.md §Pattern 2. COMMIT.
- Step 7 ledger registration (OUTSIDE the transaction COMMIT): INSERT version '1107' (or Wave-0 number) + name 'ccsd_board_of_trustees' ON CONFLICT (version) DO NOTHING.
GREP-GATE HYGIENE: keep the auto-generated path column literal out of comments (say "the auto-generated path column"); keep slug + the removed image-origin column literal out; the ledger table token appears only in the Step 7 INSERT, never in a comment.

INLINE ORCHESTRATOR STEP — RUN ORDER IS CRITICAL:
(1) FIRST run the loader: `npx tsx scripts/load-ccsd-school-boundary.ts` from C:/EV-Accounts/backend (after the Task 1 --dry-run gate passed). Then confirm `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id='3200060' AND mtfcc='G5420' AND geometry IS NOT NULL` = 1 AND source='tiger_unsd_nv_2024'. The geofence MUST exist with geometry before the migration (the SCHOOL district + section-split reference it).
(2) THEN apply 1107 via `psql -f` (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm the post-verify DO block prints PASSED (gov=1, offices=11, 7-4 split, split_orphans=0). Then run + record in SUMMARY:
- `SELECT COUNT(*) FROM essentials.districts WHERE geo_id='3200060' AND district_type='SCHOOL' AND state='nv'` → 1 (exactly one SCHOOL district).
- `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='3200060' AND d.district_type='SCHOOL' AND d.state='nv'` → 11.
- `SELECT is_appointed_position, COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='3200060' AND d.district_type='SCHOOL' AND d.state='nv' GROUP BY 1` → false:7, true:4.
- `SELECT official_count FROM essentials.chambers WHERE name='Board of School Trustees'` → 11.
- `SELECT DISTINCT state FROM essentials.districts WHERE geo_id='3200060'` → 'nv' only.
- Capture the 11 external_id→UUID map (`SELECT external_id, id FROM essentials.politicians WHERE external_id BETWEEN -3209011 AND -3209001`) for Plans 02/03.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1107_ccsd_board_of_trustees.sql"; test -f "$F" &amp;&amp; grep -q "Clark County School District, Nevada, US" "$F" &amp;&amp; grep -q "INSERT INTO essentials.chambers" "$F" &amp;&amp; grep -q "Board of School Trustees" "$F" &amp;&amp; grep -q "'SCHOOL'" "$F" &amp;&amp; grep -q "G5420" "$F" &amp;&amp; grep -q "Trustee, District A" "$F" &amp;&amp; grep -q "Appointed" "$F" &amp;&amp; grep -q -- "-3209001" "$F" &amp;&amp; grep -q -- "-3209011" "$F" &amp;&amp; grep -q "official_count" "$F" &amp;&amp; grep -q "3200060" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1107_ccsd_board_of_trustees.sql exists, ≥280 lines, composite of the 254_or school-district shape + the 1055 single-district/ledger/post-verify shape.
    - Pre-flight has NO ward assertion (only the idempotent gov-exists NOTICE).
    - Government 'Clark County School District, Nevada, US' (type='LOCAL', state='NV', geo_id='3200060') with WHERE NOT EXISTS; standalone (NOT under geo_id='32').
    - Chamber 'Board of School Trustees' official_count=11; the auto-generated path column NOT in the INSERT list; name_formal non-empty.
    - Exactly ONE SCHOOL district on geo_id='3200060' (district_type='SCHOOL', state='nv' lowercase, mtfcc='G5420') — NOT one per trustee, NOT 'SCHOOL_DISTRICT'.
    - 11 politician+office blocks: 7 elected (-3209001..-3209007, is_appointed=false, is_appointed_position=false, title 'Trustee, District A'..'G') + 4 appointed (-3209008..-3209011, is_appointed=true, is_appointed_position=true, title 'Trustee, Appointed – <Jurisdiction>' no district letter); all party=NULL, is_active=true, is_incumbent=true; representing_state='NV'; officers NOT seeded as extra seats.
    - office_id back-filled BETWEEN -3209011 AND -3209001.
    - Post-verify DO block asserts gov=1, offices=11, 7/4 split, section-split=0.
    - Ledger registration line present OUTSIDE the COMMIT (version 1107 / 'ccsd_board_of_trustees'); no forbidden tokens in comments.
    - INLINE: loader ran FIRST → G5420 geo_id='3200060' geometry IS NOT NULL = 1 row source='tiger_unsd_nv_2024'; THEN mig 1107 applied; post-verify PASSED; recorded counts (1 SCHOOL district, 11 offices, 7/4 split, official_count=11, state 'nv' only); the 11 external_id→UUID map recorded in SUMMARY.
  </acceptance_criteria>
  <done>G5420 boundary loaded (geometry present); structural migration 1107 written + applied AFTER the loader; post-verify PASSED (gov=1, offices=11, 7/4 split, section-split=0); exactly 1 SCHOOL district; 11 external_id→UUID map captured in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 4: Extend smoke-nv-geofences.ts with the G5420 CCSD assertion + run inline</name>
  <files>C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts (the address table lines ~33-91 + the PIP ST_Covers query lines ~99-106 — self-extend: add G5420 to each Clark County point's expected mtfcc set + add G5420:'3200060' to each expectedGeoIds map; the PIP query state='32' is unchanged)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md (§smoke-nv-geofences.ts — the exact extension)
  </read_first>
  <action>
EXECUTOR STEP: extend `smoke-nv-geofences.ts` so the 5 existing Clark County test points (the Strip point + the Las Vegas / Henderson / North Las Vegas / Boulder City city points) ALSO assert that ST_Covers now returns the new G5420 boundary geo_id='3200060' (CCSD's county-wide boundary covers the whole county). Add 'G5420' to each of those addresses' expectedMtfcc array and 'G5420': '3200060' to each expectedGeoIds map. Leave the PIP query (state='32', ST_Covers) unchanged and do NOT touch non-Clark-County test points. Do not add new addresses — only extend the existing Clark County expectations.

INLINE ORCHESTRATOR STEP: run `npx tsx scripts/smoke-nv-geofences.ts` from C:/EV-Accounts/backend (after the loader ran + mig 1107 applied in Task 3). Confirm all Clark County points now return G5420 geo_id='3200060' alongside their existing G4110/G4020/CD/SLDU/SLDL tiers, and the smoke test passes. Record the result in the SUMMARY.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts"; test -f "$F" &amp;&amp; grep -q "G5420" "$F" &amp;&amp; grep -q "3200060" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - smoke-nv-geofences.ts references G5420 + geo_id '3200060' in the Clark County point expectations.
    - The PIP ST_Covers query (state='32') is unchanged; non-Clark-County points untouched; no new addresses added.
    - INLINE: smoke test run AFTER the loader + migration; all Clark County points return G5420 geo_id='3200060'; result recorded in SUMMARY.
  </acceptance_criteria>
  <done>smoke-nv-geofences.ts extended so all 5 Clark County points assert the CCSD G5420 boundary; smoke test run inline and passing; recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 5: Append Clark County School District to coverage.js Nevada block</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the existing Nevada COVERAGE_STATES block — re-read the current line range; it shifted after the Phase 165 Boulder City append — with LV + Henderson + NLV + Boulder City entries; AND the coverageAreaToPath browseGeoId branch ~lines 306-313 which already supports browseGeoId + browseMtfcc + browseSchoolFilter)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md (§src/lib/coverage.js — the no-school-precedent finding, the browseGeoId-not-browseGovernmentList rule, the exact append line, the COVERAGE_STATES-not-COVERAGE_COUNTIES note)
  </read_first>
  <action>
EXECUTOR STEP: edit src/lib/coverage.js — locate the existing Nevada block in COVERAGE_STATES (abbrev 'NV', the one already containing Las Vegas 3240000 + Henderson 3231900 + North Las Vegas 3251800 + Boulder City 3206500; NOT the unrelated TX city "Nevada") and APPEND a Clark County School District entry to its `areas` array, after Boulder City: `{ label: 'Clark County School District', browseGeoId: '3200060', browseMtfcc: 'G5420', browseStateAbbrev: 'NV', hasContext: true }`. Use the GEOFENCE-keyed browseGeoId + browseMtfcc shape (the coverageAreaToPath function already supports it via the browseGeoId branch) — NOT browseGovernmentList (school districts are geofence-keyed, not government-list cities). Keep all four existing NV entries intact. Do NOT add the entry to COVERAGE_COUNTIES (which already has 'Clark County' → 32003 — a distinct county entry; leave it untouched). Do NOT create a second NV block and do NOT touch Landing.jsx (surfacing target is coverage.js). This is the first school-district coverage entry in the file.
  </action>
  <verify>
    <automated>F="C:/Transparent Motivations/essentials/src/lib/coverage.js"; test -f "$F" &amp;&amp; grep -q "Clark County School District" "$F" &amp;&amp; grep -q "G5420" "$F" &amp;&amp; grep -q "3200060" "$F" &amp;&amp; grep -q "Boulder City" "$F" &amp;&amp; grep -q "3206500" "$F" &amp;&amp; grep -q "3251800" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - coverage.js Nevada COVERAGE_STATES block contains Las Vegas (3240000), Henderson (3231900), North Las Vegas (3251800), Boulder City (3206500), AND Clark County School District (browseGeoId 3200060, browseMtfcc G5420) entries.
    - CCSD entry uses browseGeoId + browseMtfcc (geofence-keyed) — NOT browseGovernmentList; browseStateAbbrev 'NV', hasContext: true.
    - Exactly one NV state block (no duplicate); COVERAGE_COUNTIES 'Clark County' (32003) untouched; Landing.jsx untouched.
  </acceptance_criteria>
  <done>Clark County School District appended to the existing coverage.js Nevada block with browseGeoId+browseMtfcc+hasContext:true; LV + Henderson + NLV + Boulder City preserved; the first school-district coverage entry.</done>
</task>

</tasks>

<threat_model>
This is a server-side data-seed phase run by the operator via psql/tsx — no new user-input paths, no new endpoints. The realistic threat surface is low; the real risks are data-integrity (wrong-person routing, casing no-ops, fabricated structure) and supply-chain (npm installs — none here).

## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| census.gov TIGER UNSD shapefile → geofence_boundaries | external polygon crosses into routing geometry; loader validates GEOID + EXPECTED_COUNT before insert |
| generated .sql migration → live DB | service-role apply of seed SQL; only the inline orchestrator applies, after review |
| Wave-0 roster source (ccsd.net/trustees) → seeded roster | external roster claims cross into seed SQL; operator checkpoint verifies before write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-01 | Tampering | external_id collision (-3209001..-3209011) corrupting existing rows | mitigate | Wave-0 BLOCKING probe (Task 1b) confirms the block is unclaimed before any write; ON CONFLICT (external_id) DO NOTHING guards |
| T-166-02 | Tampering | wrong migration number overwriting/skipping a sibling migration | mitigate | Wave-0 probe (1a) trusts highest on-disk file +1 (1107), not the registered ledger; renumber + offset downstream if drift |
| T-166-03 | Tampering | wrong/empty G5420 geometry → all-county no-routing (no trustee resolves) | mitigate | Loader inserts real polygon (no shell); Wave-0 --dry-run confirms GEOID 3200060 in the shapefile; inline gate asserts geometry IS NOT NULL = 1 before the migration; post-verify section-split=0; smoke test asserts G5420 coverage |
| T-166-04 | Tampering | silent no-op from uppercase state='NV' on the district/office routing join | mitigate | districts.state='nv' lowercase enforced; post-verify asserts 11 offices on the SCHOOL district (would be 0 on a casing bug) |
| T-166-05 | Tampering | appointed trustees mislabeled (fabricated district letter) or officers seeded as extra seats (14 instead of 11) | mitigate | 4 appointed blocks set is_appointed/is_appointed_position=true + jurisdiction title (no letter); officers are title-on-seat; post-verify asserts offices=11 and the 7/4 split |
| T-166-06 | Tampering | wrong shapefile / TIGER vintage → GEOID 3200060 absent | mitigate | tl_2024_32_unsd.zip (NV FIPS 32) confirmed HTTP-200; loader hard-fails EXPECTED_COUNT; Wave-0 --dry-run lists found GEOIDs before the real run |
| T-166-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit = N/A; adm-zip/shapefile/pg/dotenv pre-installed, confirmed by Wave-0 ls). No high-severity threats; data-seed phase. |
</threat_model>

<verification>
Structural + routing + surfacing halves of CCSD-01:
- SC#1 (routing/structural): G5420 geo_id='3200060' loaded with geometry (loader + inline gate); 1 government + 1 chamber (official_count=11) + exactly 1 SCHOOL district on geo_id='3200060' carrying 11 offices (7 elected + 4 appointed), state='nv', section-split=0 (Task 3 post-verify DO block + inline counts); smoke test asserts all 5 Clark County points return G5420 (Task 4).
- SC#4 (surfacing): CCSD in coverage.js with hasContext:true alongside LV + Henderson + NLV + Boulder City (Task 5).
- Cross-cutting: Wave-0 probes + dry-run confirm no collision/drift/GEOID-miss; structural migration registered at 1107 (or Wave-0 number); 11 external_id→UUID map captured for Plans 02/03.
</verification>

<success_criteria>
- Wave-0 probes + loader dry-run + roster checkpoint recorded and passed before any write/load; on-disk MAX → structural number set (expected 1107).
- load-ccsd-school-boundary.ts written + run (before the migration); G5420 geo_id='3200060' geometry present (source='tiger_unsd_nv_2024').
- 1107_ccsd_board_of_trustees.sql written + applied AFTER the loader; post-verify PASSED (gov=1, offices=11, 7/4 elected/appointed split, section-split=0); exactly 1 SCHOOL district; registered in the ledger.
- smoke-nv-geofences.ts extended + run; all 5 Clark County points return G5420 geo_id='3200060'.
- 11 external_id→UUID map recorded in SUMMARY (consumed by Plans 02/03).
- Clark County School District surfaces in coverage.js Nevada block (browseGeoId+browseMtfcc, hasContext:true); LV + Henderson + NLV + Boulder City preserved.
</success_criteria>

<output>
Create `.planning/phases/166-ccsd-board-of-trustees-deep-seed/166-01-SUMMARY.md` when done (record the Wave-0 probe values + the dry-run GEOID confirmation + roster confirmation, the loader run + geometry check, the post-verify results, the district/office counts + 7/4 split, the 11 external_id→UUID map, the smoke-test result, and the coverage.js edit).
</output>
</content>
</invoke>
