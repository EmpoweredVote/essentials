---
phase: 79-or-landing-elections-discovery
plan: 4
type: execute
wave: 3
depends_on: [79-01, 79-02]
files_modified:
  - C:/EV-Accounts/backend/migrations/240_portland_city_races.sql
autonomous: true
requirements: []
tags: [oregon, portland, elections, city, races, migration]

must_haves:
  truths:
    - "essentials.races contains 7 Portland city race rows for the OR 2026 General election (3 D3 council seats + 3 D4 council seats + 1 City Auditor)"
    - "The 3 District 3 race rows have 3 DISTINCT office_ids (one per identical office in district 3)"
    - "The 3 District 4 race rows have 3 DISTINCT office_ids (one per identical office in district 4)"
    - "The City Auditor race links to office_id=a19813f9-ee4d-442d-b052-5c2f9f7db9c8 (Phase 77 City Auditor office)"
    - "All 7 rows have empty race_candidates (discovery fills via cron in Plan 79-05)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/240_portland_city_races.sql"
      provides: "7 Portland race rows: D3 Seats A/B/C + D4 Seats A/B/C + City Auditor"
      contains: "Portland City Council District 3 Seat A"
  key_links:
    - from: "migration 240 D3/D4 council race rows"
      to: "essentials.offices via geo_id portland-or-council-district-3 / -4"
      via: "ORDER BY o.id LIMIT 1 OFFSET {0,1,2} — enumerate 3 identical offices per district"
      pattern: "portland-or-council-district-[34]"
    - from: "migration 240 City Auditor row"
      to: "essentials.offices id a19813f9-ee4d-442d-b052-5c2f9f7db9c8"
      via: "hardcoded office_id from Phase 77 SUMMARY"
      pattern: "a19813f9-ee4d-442d-b052-5c2f9f7db9c8"
---

<objective>
Create the 7 Portland city race rows for the November 3, 2026 general election: 3 City Council seats per District 3, 3 City Council seats per District 4, and 1 City Auditor.

Purpose: Implements the Portland portion of D-07 and the race-row half of D-08. Per RESEARCH.md "Portland City Council 2026 Races", D-07's expectation of "no 2026 races" was WRONG — Districts 3 and 4 plus the City Auditor are on the 2026 ballot due to the 2024 charter staggered terms (2-year terms for D3/D4/Auditor, 4-year for D1/D2/Mayor). Mayor Wilson and Districts 1+2 are NOT up in 2026. Three council offices per district are identical (same title, same district_id); Seat A/B/C suffixes on position_name disambiguate them so the partial unique index `(election_id, position_name) WHERE primary_party IS NULL` works correctly (per Pitfall 6).

Output: One migration 240_portland_city_races.sql in C:/EV-Accounts/backend/migrations/, applied to live Supabase production DB. 7 new rows in essentials.races (total OR race count rises from 98 to 105).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-or-landing-elections-discovery/79-CONTEXT.md
@.planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md
@.planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md
@.planning/phases/79-or-landing-elections-discovery/79-01-SUMMARY.md
@.planning/phases/77-portland-city-structure/77-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 240_portland_city_races.sql (D-07 corrected per research)</name>
  <files>C:/EV-Accounts/backend/migrations/240_portland_city_races.sql</files>
  <read_first>
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (sections "Portland City Council 2026 Races", "Portland City Race Architecture", "Pitfall 6: Race position_name uniqueness for Portland council", "Portland Council Offices")
    - .planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md (section "migrations/240_portland_city_races.sql")
    - .planning/phases/77-portland-city-structure/77-02-SUMMARY.md (City Auditor office_id reference — should confirm a19813f9-ee4d-442d-b052-5c2f9f7db9c8)
  </read_first>
  <action>
    Create a new file at exactly `C:/EV-Accounts/backend/migrations/240_portland_city_races.sql` (UTF-8 without BOM via Write tool). Migration 240 is next-available after 239. Add header comments: `-- Migration 240: Portland City Races for OR 2026 General — Phase 79 Plan 04`, `-- D-07 corrected: Portland D3/D4/Auditor ARE on 2026 ballot (2-year terms from 2024 charter stagger)`, `-- Mayor Wilson + D1 + D2 NOT up in 2026 (4-year terms)`.

    Write a single `DO $$ ... $$;` block. Declare `v_general_id UUID;` at the top. First statement inside the block: `SELECT id INTO v_general_id FROM essentials.elections WHERE name = 'OR 2026 General' AND state = 'OR';`.

    Then 7 INSERT statements, all into `essentials.races (id, election_id, office_id, position_name, primary_party, seats)`, each with `gen_random_uuid()` for id, `v_general_id` for election_id, `NULL` for primary_party, `1` for seats, and ending with `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING;`.

    The 6 council INSERTs use this office_id subquery pattern (the 3 offices per district are identical so order by `o.id` and offset to enumerate them — per RESEARCH.md "Portland City Race Architecture"):
    - INSERT #1: position_name = `'Portland City Council District 3 Seat A'`, office_id subquery = `(SELECT o.id FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = 'portland-or-council-district-3' ORDER BY o.id LIMIT 1 OFFSET 0)`
    - INSERT #2: position_name = `'Portland City Council District 3 Seat B'`, same subquery with `OFFSET 1`
    - INSERT #3: position_name = `'Portland City Council District 3 Seat C'`, same subquery with `OFFSET 2`
    - INSERT #4: position_name = `'Portland City Council District 4 Seat A'`, geo_id = `'portland-or-council-district-4'`, `OFFSET 0`
    - INSERT #5: position_name = `'Portland City Council District 4 Seat B'`, same district, `OFFSET 1`
    - INSERT #6: position_name = `'Portland City Council District 4 Seat C'`, same district, `OFFSET 2`

    INSERT #7 (City Auditor): position_name = `'Portland City Auditor'`, office_id = hardcoded UUID literal `'a19813f9-ee4d-442d-b052-5c2f9f7db9c8'::uuid` (per RESEARCH.md "Portland Council Offices" — City Auditor office from Phase 77).

    Do NOT create rows for District 1, District 2, Mayor, City Administrator, or City Attorney (none up in 2026 per RESEARCH.md). Do NOT include `cron_active`, `election_name`, or any race_candidates inserts (D-11).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='C:/EV-Accounts/backend/migrations/240_portland_city_races.sql';const b=fs.readFileSync(p);if(b[0]===0xEF&&b[1]===0xBB&&b[2]===0xBF){console.error('FAIL: BOM');process.exit(1);}const s=b.toString('utf8');const must=[\"'Portland City Council District 3 Seat A'\",\"'Portland City Council District 3 Seat B'\",\"'Portland City Council District 3 Seat C'\",\"'Portland City Council District 4 Seat A'\",\"'Portland City Council District 4 Seat B'\",\"'Portland City Council District 4 Seat C'\",\"'Portland City Auditor'\",\"'portland-or-council-district-3'\",\"'portland-or-council-district-4'\",'a19813f9-ee4d-442d-b052-5c2f9f7db9c8','OFFSET 0','OFFSET 1','OFFSET 2','OR 2026 General','ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING'];for(const t of must){if(!s.includes(t))throw new Error('missing: '+t);}const forbid=[\"'Portland City Council District 1\",\"'Portland City Council District 2\",\"'Portland Mayor'\",\"'Portland City Administrator'\",\"'Portland City Attorney'\",'cron_active','election_name','race_candidates'];for(const t of forbid){if(s.includes(t))throw new Error('forbidden: '+t);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/240_portland_city_races.sql`
    - File is UTF-8 without BOM (first 3 bytes NOT 0xEF 0xBB 0xBF)
    - File contains all 7 position_name literals: `'Portland City Council District 3 Seat A'`, `'...Seat B'`, `'...Seat C'`, `'Portland City Council District 4 Seat A'`, `'...Seat B'`, `'...Seat C'`, `'Portland City Auditor'`
    - File contains literal `'portland-or-council-district-3'` and literal `'portland-or-council-district-4'`
    - File contains literal `OFFSET 0`, `OFFSET 1`, `OFFSET 2` (each used twice — once for D3 and once for D4)
    - File contains the City Auditor UUID literal `a19813f9-ee4d-442d-b052-5c2f9f7db9c8`
    - File contains `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`
    - File does NOT contain any of: `'Portland City Council District 1`, `'Portland City Council District 2`, `'Portland Mayor'`, `'Portland City Administrator'`, `'Portland City Attorney'` (none on 2026 ballot)
    - File does NOT contain `cron_active`, `election_name`, or `race_candidates`
  </acceptance_criteria>
  <done>Migration 240 file exists, NoBOM, contains 7 Portland race INSERTs with correct office_id subqueries and position_name disambiguation.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 240 to Supabase and verify (D-07 corrected)</name>
  <files>C:/EV-Accounts/backend/migrations/240_portland_city_races.sql (apply only)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/240_portland_city_races.sql (the file written in Task 1)
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (section "Validation Architecture — After Plan 04")
  </read_first>
  <action>
    Apply migration 240 to live Supabase production DB via mcp__supabase-local__execute_sql. After applying, run these verifications:
    1. Total OR race count: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR';` — expected 105 (8 + 90 + 7).
    2. Portland race count: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland %';` — expected 7.
    3. D3/D4/Auditor breakdown: `SELECT position_name, office_id FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland %' ORDER BY position_name;` — expected 7 rows with the exact position_names listed in Task 1.
    4. D3 office_id distinctness: `SELECT COUNT(DISTINCT office_id) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland City Council District 3 Seat %';` — expected 3 (three different office_ids — proves OFFSET 0/1/2 enumeration worked).
    5. D4 office_id distinctness: same query for District 4 — expected 3.
    6. City Auditor office_id: `SELECT office_id::text FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name = 'Portland City Auditor';` — expected `a19813f9-ee4d-442d-b052-5c2f9f7db9c8`.
    7. All 7 office_ids non-null: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland %' AND r.office_id IS NULL;` — expected 0.
    8. Idempotency: re-execute migration 240 — count remains 105.
    9. Section-split check: `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.state='41' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts WHERE state IN ('or','OR','41'));` — expected 0.
    10. race_candidates emptiness: `SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland %';` — expected 0 (D-11).
  </action>
  <verify>
    <automated>echo "Manual MCP verification — see acceptance_criteria for explicit queries via mcp__supabase-local__execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR'` returns exactly 105
    - `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland %'` returns exactly 7
    - The 7 Portland position_names returned by ordered SELECT match exactly: Portland City Auditor, Portland City Council District 3 Seat A, ...Seat B, ...Seat C, Portland City Council District 4 Seat A, ...Seat B, ...Seat C
    - `SELECT COUNT(DISTINCT office_id) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland City Council District 3 Seat %'` returns 3 (three distinct office_ids)
    - `SELECT COUNT(DISTINCT office_id) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland City Council District 4 Seat %'` returns 3
    - City Auditor race office_id equals `a19813f9-ee4d-442d-b052-5c2f9f7db9c8`
    - No Portland race row has NULL office_id
    - Re-applying migration 240 returns no error; total OR race count remains 105 (idempotent)
    - Section-split check returns 0
    - No race_candidates exist for Portland races yet (D-11)
  </acceptance_criteria>
  <done>Migration 240 applied; 7 Portland race rows present with distinct D3/D4 office_ids and correct Auditor link; total OR race count 105; idempotency confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → live Supabase production DB | Migration 240 applied via mcp__supabase-local__execute_sql |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-04-01 | Integrity | OFFSET-based office enumeration | mitigate | `ORDER BY o.id` is deterministic; 3 office rows per district verified in RESEARCH.md; Task 2 verification step 4/5 fails plan if distinctness count is not 3 |
| T-79-04-02 | Integrity | Hardcoded City Auditor UUID | mitigate | UUID `a19813f9-ee4d-442d-b052-5c2f9f7db9c8` verified in RESEARCH.md against live DB on 2026-05-30; Task 2 verification step 6 fails plan if office_id differs |
| T-79-04-03 | Tampering | scope creep risk (Mayor/D1/D2 inclusion) | mitigate | Task 1 verify command explicitly forbids `'Portland City Council District 1`, `District 2`, `Mayor`, `City Administrator`, `City Attorney` literals (none on 2026 ballot) |
| T-79-04-04 | Information Disclosure | race rows | accept | No PII; election data is public record |

Pure data migration. No auth, no API, no user input.
</threat_model>

<verification>
1. Total OR race count = 105
2. Portland race count = 7
3. D3 race rows have 3 distinct office_ids; D4 race rows have 3 distinct office_ids
4. City Auditor race office_id = a19813f9-ee4d-442d-b052-5c2f9f7db9c8
5. All 7 office_ids non-null
6. Idempotency confirmed
7. Section-split = 0
8. race_candidates for Portland = 0
</verification>

<success_criteria>
- 7 Portland race rows exist in essentials.races for OR 2026 General
- D3 and D4 each have 3 distinct office_id values (enumeration via OFFSET worked)
- City Auditor linked to Phase 77 Simone Rede office
- Plan 79-05 can register discovery jurisdictions to start filling race_candidates
</success_criteria>

<output>
Create `.planning/phases/79-or-landing-elections-discovery/79-04-SUMMARY.md` documenting:
- Final total OR race count (expected 105)
- 7 Portland race rows with their assigned office_ids (table format)
- D3 distinct office_ids list, D4 distinct office_ids list
- Confirmation of idempotency
- Confirmation of section-split check
- Migration 240 ledger entry confirmation
</output>
