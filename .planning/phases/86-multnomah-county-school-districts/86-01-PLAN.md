---
phase: 86-multnomah-county-school-districts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts
  - C:/EV-Accounts/backend/scripts/smoke-phase86.ts
  - C:/EV-Accounts/backend/migrations/254_or_school_districts.sql
autonomous: true
requirements: [OR-SCHOOL-01, OR-SCHOOL-02, OR-SCHOOL-03]

must_haves:
  truths:
    - "G5420 geofence_boundaries rows exist for all 6 Multnomah County school districts with geo_id IN ('4110040','4109480','4110520','4102800','4103940','4110560'), state='41', source='tiger_unsd_or_2024' (OR-SCHOOL-01 / D-02 / D-04 / D-05)"
    - "6 essentials.governments rows exist (one per district) with type='LOCAL', state='OR' (uppercase), correct geo_id per district (D-07)"
    - "6 essentials.chambers rows exist (one Board of Education chamber per government) with NO slug column included in INSERT (slug is GENERATED ALWAYS)"
    - "6 essentials.districts rows exist with district_type='SCHOOL' (NOT 'SCHOOL_DISTRICT'), state='or' (lowercase), mtfcc='G5420', geo_id matching the geofence_boundaries row (D-07, D-08, Pitfall 1)"
    - "38 essentials.politicians rows exist with external_ids in -860001..-860007 (PPS), -860011..-860015 (Parkrose), -860021..-860027 (Reynolds), -860031..-860037 (Centennial), -860041..-860047 (David Douglas), -860051..-860055 (Riverdale), each with party=NULL, is_appointed=false, is_incumbent=true (D-11, D-12, D-13)"
    - "38 essentials.offices rows exist linking each politician to its district's SCHOOL district row via district_id; titles follow per-district convention: PPS = 'Board Member (Zone N)', Parkrose/Reynolds/Centennial/David Douglas = 'Board Member (Position N)', Riverdale = 'Board Member (Seat N)' (D-10)"
    - "All 38 politicians have non-NULL office_id after the back-fill UPDATE (no orphan politicians in the -860001..-860055 range)"
    - "Section-split check returns 0 rows: every G5420 geofence_boundary with one of the 6 target geo_ids has a matching SCHOOL districts row (state='or')"
    - "Portland City Hall coordinate (-122.6794, 45.5231) returns the 7 PPS board members via the SCHOOL routing JOIN (geofence_boundaries → districts → offices → politicians filtered by district_type='SCHOOL' AND mtfcc='G5420')"
    - "Riverdale neighborhood coordinate (-122.6794, 45.4472) returns the 5 Riverdale board members and does NOT return PPS members (district isolation)"
    - "Migration 254 is idempotent — re-running inserts 0 additional rows in any of the 5 tables and the office_id back-fill UPDATE affects 0 rows"
    - "Smoke test exits 0 with all 5 assertions PASSED (SC1 g5420_count=6, SC2 school_districts_count=6, SC3 PPS officials >=7, SC4 Riverdale officials >=5 with no PPS, SC5 section-split=0)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts"
      provides: "TIGER UNSD downloader + shapefile parser + G5420 geofence_boundaries inserter for 6 OR school districts"
      exports: ["main"]
      min_lines: 200
    - path: "C:/EV-Accounts/backend/scripts/smoke-phase86.ts"
      provides: "Phase 86 smoke test verifying G5420 count, SCHOOL district count, PPS routing, Riverdale routing, section-split"
      exports: ["main"]
      min_lines: 150
    - path: "C:/EV-Accounts/backend/migrations/254_or_school_districts.sql"
      provides: "6 governments + 6 chambers + 6 SCHOOL districts + 38 politicians + 38 offices + office_id back-fill + post-verification DO block + ledger entry"
      contains: "tiger_unsd_or_2024"
      min_lines: 800
  key_links:
    - from: "C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts"
      to: "essentials.geofence_boundaries (mtfcc='G5420', state='41')"
      via: "INSERT with ON CONFLICT (geo_id, mtfcc) DO NOTHING"
      pattern: "ST_ForcePolygonCCW.*ST_SetSRID.*ST_Force2D.*ST_GeomFromGeoJSON"
    - from: "essentials.offices.district_id"
      to: "essentials.districts (district_type='SCHOOL', state='or', geo_id IN 6 GEOIDs)"
      via: "FK JOIN inside migration; verified by section-split detector and post-verification DO block"
      pattern: "JOIN essentials.districts d ON d.id = o.district_id WHERE d.district_type = 'SCHOOL'"
    - from: "essentials.geofence_boundaries (G5420, state='41')"
      to: "essentials.districts (SCHOOL, state='or')"
      via: "geo_id JOIN used by essentialsService.ts address lookup"
      pattern: "gb.mtfcc IN \\('G5400','G5410','G5420'\\) AND d.district_type = 'SCHOOL'"
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id"
      via: "back-fill UPDATE at end of migration"
      pattern: "UPDATE essentials.politicians SET office_id = o.id .* WHERE p.external_id BETWEEN -860055 AND -860001"
---

<objective>
Load G5420 geofences for all 6 Multnomah County school districts from TIGER UNSD (per D-02 / D-04), then seed 6 school district governments + chambers + SCHOOL districts + 38 board member politicians + 38 offices in a single migration (migration 254 per D-01), and verify routing with a smoke test.

The 6 districts are: Portland Public Schools (geo_id='4110040', 7 members), Parkrose School District 3 (geo_id='4109480', 5 members), Reynolds School District 7 (geo_id='4110520', 7 members), Centennial School District 28J (geo_id='4102800', 7 members), David Douglas School District 40 (geo_id='4103940', 7 members), and Riverdale School District 51J (geo_id='4110560', 5 members) per D-05 (Riverdale verified present in TIGER UNSD). Per D-06, the loader takes Riverdale's polygon directly from the TIGER UNSD shapefile — no manual polygon construction. Per D-14, no 2026 school board race rows are seeded in this plan (out of scope).

Purpose: OR-SCHOOL-01 (G5420 geofences loaded for all 6 districts), OR-SCHOOL-02 (school board government bodies with district_type='SCHOOL'), and OR-SCHOOL-03 (38 board members + offices seeded) all land in this plan. OR-SCHOOL-04 (headshots) is deferred to Plan 02 per D-01.

Output: New loader script applied (6 G5420 rows inserted to production); new smoke test exits 0; migration 254 applied to production Supabase via Supabase MCP with all post-verification gates green.

NOTE ON MIGRATION NUMBERING: RESEARCH.md and CONTEXT.md reference "migration 253". The filesystem now shows `253_fix_ca_legislature_orphan_context_rows.sql` already exists and is applied — so this plan uses migration **254** (and Plan 02 uses migration **255**). Confirmed via `ls C:/EV-Accounts/backend/migrations/` 2026-06-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-multnomah-county-school-districts/86-CONTEXT.md
@.planning/phases/86-multnomah-county-school-districts/86-RESEARCH.md
@.planning/phases/86-multnomah-county-school-districts/86-PATTERNS.md
@.planning/phases/86-multnomah-county-school-districts/86-VALIDATION.md

# Reference loader scripts (source-of-truth patterns)
@C:/EV-Accounts/backend/scripts/load-lausd-board-boundaries.ts
@C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts

# Reference migrations (source-of-truth seeding patterns)
@C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql
@C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql

# Reference smoke test (source-of-truth pattern)
@C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write load-or-school-boundaries.ts TIGER UNSD loader</name>
  <files>C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-lausd-board-boundaries.ts (LAUSD loader pattern — overall structure, DB pool setup, post-insert verification, main/finally shell — copy as base)
    - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts (zip download + adm-zip extract + shapefile.open pattern; see downloadWithRedirects and extractZip helpers at lines ~317-357, and the temp-dir + GEOID filter flow at ~577-600)
    - .planning/phases/86-multnomah-county-school-districts/86-PATTERNS.md (Pattern Map for load-or-school-boundaries.ts — sections "Imports pattern", "Config block pattern", "Zip download helper", "Zip extract helper", "Temp-dir + download + extract flow", "Shapefile-read + GEOID filter pattern", "PostGIS insert pattern", "Post-insert verification gate", "Main function shell")
    - .planning/phases/86-multnomah-county-school-districts/86-RESEARCH.md (D-04 TIGER URL, D-05 Riverdale GEOID, Pitfall 3, Pitfall 4, Pitfall 6, "Key Discovery" section about geoIdSource='GEOID')
  </read_first>
  <action>
Create a new standalone TypeScript loader script at C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts that downloads the Oregon TIGER UNSD shapefile, filters to the 6 target Multnomah County school districts by GEOID, and inserts G5420 geofence_boundaries rows. Do NOT extend load-state-tiger-boundaries.ts (D-03).

CONCRETE TARGETS (use exactly these values; do NOT abbreviate):
- TIGER_URL = 'https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_41_unsd.zip' (per D-04 verified)
- MTFCC = 'G5420'
- STATE = '41' (Oregon FIPS — geofence_boundaries.state convention)
- SOURCE = 'tiger_unsd_or_2024'
- EXPECTED_COUNT = 6
- DRY_RUN flag via process.argv.includes('--dry-run')
- Temp dir = path.join(process.cwd(), '.tmp-or-school-unsd')
- Base name = 'tl_2024_41_unsd'

TARGET_GEOIDS Map (6 entries, exact GEOIDs from RESEARCH.md):
- '4110040' → 'Portland Public Schools'
- '4109480' → 'Parkrose School District 3'
- '4110520' → 'Reynolds School District 7'
- '4102800' → 'Centennial School District 28J'
- '4103940' → 'David Douglas School District 40'
- '4110560' → 'Riverdale School District 51J'

REQUIRED IMPORTS: 'dotenv/config', Pool from 'pg', https, fs, path, AdmZip from 'adm-zip', shapefile (all already in C:/EV-Accounts/backend/package.json — used by load-state-tiger-boundaries.ts).

REQUIRED HELPERS (copy structure from load-state-tiger-boundaries.ts lines ~317-357):
- downloadWithRedirects(url, destPath): handles 301/302 redirects, caches downloaded zip if file exists, streams via https.get + fs.createWriteStream, rejects on non-200.
- extractZip(zipPath, destDir): uses AdmZip(zipPath).extractAllTo(destDir, true); returns { cleanup } closure that removes the temp dir only if it did not exist before.

REQUIRED MAIN FLOW:
1. Log "[load-or-school-boundaries] Fetching OR UNSD boundaries"; if DRY_RUN, log "DRY RUN — no DB writes".
2. mkdirSync the temp dir; build zipPath and destDir paths.
3. Call downloadWithRedirects(TIGER_URL, zipPath); log "Downloading <URL>".
4. Call extractZip(zipPath, destDir); log "Extracting <baseName>.zip".
5. Read destDir entries; find files ending '.shp' and '.dbf' (case-insensitive); throw if missing.
6. Open with shapefile.open(shpPath, dbfPath, { encoding: 'utf-8' }).
7. On the first feature only, log Object.keys(props) to confirm 'GEOID' field name is present (per Pitfall 6 / RESEARCH.md).
8. Iterate source.read() loop; coerce props['GEOID'] to String; skip if !TARGET_GEOIDS.has(geoid); accumulate to a districtMap<geoid,{name,geometryJson}> with JSON.stringify(feature.geometry).
9. After loop: assert districtMap.size === EXPECTED_COUNT — if not, console.error and process.exit(1) showing which GEOIDs were found vs expected.
10. For each (geoid, {name, geometryJson}) in districtMap, run the INSERT with ON CONFLICT (geo_id, mtfcc) DO NOTHING. Use ocd_id = geoId (same as LAUSD pattern when no formal OCD division exists). Use the exact PostGIS geometry expression: public.ST_ForcePolygonCCW(public.ST_SetSRID(public.ST_Force2D(public.ST_GeomFromGeoJSON($6)), 4326)). Parameters: [geoId, geoId, name, STATE, MTFCC, geometryJson, SOURCE].
11. Track inserted/skipped counts; log per district.
12. If not DRY_RUN, run post-insert verification query: SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='41' AND mtfcc='G5420' AND source='tiger_unsd_or_2024'; warn if != 6.
13. Call cleanup() in finally to remove the temp dir.
14. Wrap main() in .catch (process.exit(1)) and .finally (pool.end()).

CRITICAL CONSTRAINTS:
- geo_id field = the GEOID string directly (e.g. '4110040'), NOT a slug like 'oregon-school-district-N' (Pitfall 3).
- After the post-insert verification, NEVER exit non-zero if the count is 6 — only warn if mismatch.
- Pre-cache: if zipPath already exists on disk, skip the download (downloadWithRedirects handles this) — but always re-extract.

DEFENSIVE CHECKS:
- If process.env.DATABASE_URL is missing, console.error and process.exit(1).
- If shapefile field 'GEOID' is missing on the first feature, dump Object.keys and exit(1) with diagnostic.
- If districtMap.size < 6 after the loop, print which TARGET_GEOIDS were missing.

Per D-03: do NOT add 'unsd' to the STATE_LAYER_ALLOWLIST in load-state-tiger-boundaries.ts. This is a separate standalone script.

After writing the file, run `npx tsx scripts/load-or-school-boundaries.ts --dry-run` from C:/EV-Accounts/backend/ to verify the script compiles and downloads + extracts + finds all 6 GEOIDs without writing to the DB.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; npx tsx scripts/load-or-school-boundaries.ts --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: load-or-school-boundaries.ts exists and contains the string `'https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_41_unsd.zip'`
    - SOURCE: load-or-school-boundaries.ts contains `MTFCC` set to `'G5420'` and `STATE` set to `'41'` and `SOURCE` set to `'tiger_unsd_or_2024'`
    - SOURCE: load-or-school-boundaries.ts contains all 6 GEOIDs: `'4110040'`, `'4109480'`, `'4110520'`, `'4102800'`, `'4103940'`, `'4110560'`
    - SOURCE: load-or-school-boundaries.ts contains `downloadWithRedirects(` and `extractZip(` helper function definitions
    - SOURCE: load-or-school-boundaries.ts contains `ON CONFLICT (geo_id, mtfcc) DO NOTHING`
    - SOURCE: load-or-school-boundaries.ts contains `public.ST_ForcePolygonCCW(` and `public.ST_GeomFromGeoJSON($6)`
    - BEHAVIOR: `npx tsx scripts/load-or-school-boundaries.ts --dry-run` exits with code 0
    - BEHAVIOR: dry-run output includes 6 lines indicating each GEOID was matched (e.g. one per target district name)
    - BEHAVIOR: dry-run output indicates `districtMap.size === 6` (or equivalent "All 6 found" success message); does NOT report any missing GEOID
    - SOURCE: load-state-tiger-boundaries.ts state allowlist for Oregon is UNCHANGED (no 'unsd' added) — verified with: `grep -n "STATE_LAYER_ALLOWLIST" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` shows no OR/unsd addition (D-03)
  </acceptance_criteria>
  <done>
load-or-school-boundaries.ts is committed and runs `--dry-run` successfully, identifying all 6 target GEOIDs from the TIGER UNSD shapefile without writing to the DB. The script is ready to run live in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write smoke-phase86.ts smoke test</name>
  <files>C:/EV-Accounts/backend/scripts/smoke-phase86.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts (canonical smoke test pattern — Client setup, AddressTest interface, test address constants, error accumulator, finally cleanup, exit code logic; lines 14-27 imports, 28-51 test addresses, 98-110 client setup, 286-297 split check, 314-329 results)
    - .planning/phases/86-multnomah-county-school-districts/86-PATTERNS.md (smoke-multnomah-school.ts section — all 5 assertion query snippets including SC1 g5420 count, SC2 SCHOOL districts count, SC3 PPS officials query, section-split query, error accumulator)
    - .planning/phases/86-multnomah-county-school-districts/86-RESEARCH.md (Wave 0 Gaps section listing SC1-SC5 assertions; smoke test code example)
    - C:/EV-Accounts/backend/migrations/047_add_city_council_district_columns.sql (lines 126-137 — canonical SCHOOL routing JOIN condition that smoke test must mirror)
  </read_first>
  <action>
Create a new TypeScript smoke test at C:/EV-Accounts/backend/scripts/smoke-phase86.ts that verifies all Phase 86 success criteria. Per 86-VALIDATION.md the smoke runtime budget is ~30 seconds.

CONCRETE STRUCTURE (mirror smoke-multnomah-county.ts):
- import { Client } from 'pg'; import * as dotenv from 'dotenv'; dotenv.config();
- declare TEST_ADDRESSES: array of two AddressTest objects (label, lon, lat, expectedMtfcc, expectedGeoIds):
  1. { label: 'Portland City Hall (PPS zone)', lon: -122.6794, lat: 45.5231, expectedMtfcc: ['G5420'], expectedGeoIds: { G5420: '4110040' } }
  2. { label: 'Riverdale (SW Portland)', lon: -122.6794, lat: 45.4472, expectedMtfcc: ['G5420'], expectedGeoIds: { G5420: '4110560' } }
- main() function: bail out if process.env['DATABASE_URL'] not set; create Client with ssl: { rejectUnauthorized: false }; await client.connect().
- Use a single try/finally: all assertions inside try; await client.end() in finally.
- Use let allPassed = true; const errors: string[] = []; — push to errors[] and set allPassed=false on each failure; do NOT throw mid-test.

EXACT 5 ASSERTIONS (each must log "SC<N>: PASS" or push to errors[]):

SC1: 6 G5420 geofence_boundaries rows for OR.
```
SELECT COUNT(*) AS cnt FROM essentials.geofence_boundaries
WHERE state = '41' AND mtfcc = 'G5420' AND source = 'tiger_unsd_or_2024'
```
Expected: 6.

SC2: 6 SCHOOL district rows in essentials.districts.
```
SELECT COUNT(*) AS cnt FROM essentials.districts
WHERE district_type = 'SCHOOL' AND state = 'or'
  AND geo_id IN ('4110040','4109480','4110520','4102800','4103940','4110560')
```
Expected: 6.

SC3: Portland City Hall returns at least 7 PPS board members via the SCHOOL routing JOIN.
```
SELECT p.full_name, d.geo_id, d.district_type
FROM essentials.politicians p
JOIN essentials.offices o ON o.politician_id = p.id
JOIN essentials.districts d ON d.id = o.district_id
JOIN essentials.geofence_boundaries gb ON gb.geo_id = d.geo_id
WHERE gb.state = '41'
  AND d.district_type = 'SCHOOL'
  AND gb.mtfcc = 'G5420'
  AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($1, $2), 4326))
ORDER BY p.full_name
```
Parameters: [-122.6794, 45.5231]. Expected: rows.length >= 7 AND every row has d.geo_id === '4110040'.

SC4: Riverdale coordinate returns at least 5 board members AND none with geo_id='4110040' (district isolation).
Same query, parameters [-122.6794, 45.4472]. Expected: rows.length >= 5 AND every row has d.geo_id === '4110560'.

SC5: Section-split check — every G5420 geofence with one of the 6 target geo_ids has a SCHOOL district row.
```
SELECT gb.geo_id FROM essentials.geofence_boundaries gb
WHERE gb.geo_id IN ('4110040','4109480','4110520','4102800','4103940','4110560')
  AND gb.mtfcc = 'G5420'
  AND NOT EXISTS (
    SELECT 1 FROM essentials.districts d
    WHERE d.geo_id = gb.geo_id AND d.district_type = 'SCHOOL' AND d.state = 'or'
  )
```
Expected: rows.length === 0. If non-zero, list the orphan geo_ids in the error message.

FINAL OUTPUT BLOCK (copy from smoke-multnomah-county.ts lines 314-329):
- Log "\n=== Phase 86 Smoke Test Results ===" plus one line per SC (PASS or FAIL with detail).
- If allPassed: log "ALL ASSERTIONS PASSED"; process.exit(0).
- Else: log "FAILED (N assertion(s)):" then each error; process.exit(1).
- main().catch(err => { console.error('Smoke test error:', err); process.exit(1); });

Reminder: this file is referenced from 86-VALIDATION.md as `scripts/smoke-phase86.ts` (Wave 0 requirement). After writing the file, run `npx tsx scripts/smoke-phase86.ts` once — it MUST exit non-zero (FAIL) before Task 3 has applied the migration, because no rows exist yet. This is the RED step that proves the test would catch a real failure.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; npx tsx scripts/smoke-phase86.ts; if [ $? -ne 0 ]; then echo "RED-PHASE-PASS: smoke fails before migration applied (expected)"; else echo "FAIL: smoke passed before migration — invalid initial state"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: smoke-phase86.ts exists and contains all 6 GEOIDs: `'4110040'`, `'4109480'`, `'4110520'`, `'4102800'`, `'4103940'`, `'4110560'`
    - SOURCE: smoke-phase86.ts contains `district_type = 'SCHOOL'` and `state = 'or'` (lowercase)
    - SOURCE: smoke-phase86.ts contains both test coordinates: `-122.6794` (used for both Portland City Hall and Riverdale) and y-values `45.5231` and `45.4472`
    - SOURCE: smoke-phase86.ts contains `ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($1, $2), 4326))`
    - SOURCE: smoke-phase86.ts contains `gb.mtfcc = 'G5420'` and `gb.state = '41'`
    - SOURCE: smoke-phase86.ts contains a section-split NOT EXISTS query covering all 6 GEOIDs
    - SOURCE: smoke-phase86.ts contains `process.exit(0)` on the all-pass path and `process.exit(1)` on the fail path
    - BEHAVIOR (RED step): `npx tsx scripts/smoke-phase86.ts` exits non-zero with at least SC1, SC2, SC3, SC4 failing (no geofences or districts loaded yet)
    - BEHAVIOR: stderr/stdout output identifies which assertions failed (each SC labeled in output)
  </acceptance_criteria>
  <done>
smoke-phase86.ts is committed; running it before any data has been loaded exits non-zero with all SC1-SC4 reporting failures (proves the test would catch missing data). Section-split SC5 may pass or fail depending on whether geofences-only have been loaded — either is acceptable at this stage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write migration 254 (governments + chambers + SCHOOL districts + politicians + offices), then run loader + apply migration + run smoke test</name>
  <files>C:/EV-Accounts/backend/migrations/254_or_school_districts.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql (canonical single-government seed pattern — header comment, BEGIN, pre-flight DO block, government INSERT WHERE NOT EXISTS, chamber INSERT (no slug column), COUNTY district INSERT with state='or' lowercase, politician+office CTE per person, office_id back-fill UPDATE, post-verification DO block with multiple gates, ledger INSERT, COMMIT)
    - C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql (canonical multi-government seed pattern for 5 cities in one migration; mirror this structure for 6 school districts)
    - .planning/phases/86-multnomah-county-school-districts/86-PATTERNS.md (migrations/253_or_school_districts.sql section — file header pattern, pre-flight guard, government INSERT, chamber INSERT (no slug), SCHOOL district INSERT, politician+office CTE, office_id back-fill, post-verification DO block, ledger entry; note that PATTERNS.md says 253 but actual migration number is 254)
    - .planning/phases/86-multnomah-county-school-districts/86-RESEARCH.md (Board Rosters section — exact 38 names + zones/positions/seats; "Pattern 2" SCHOOL district seeding example; "External ID Assignment Scheme" table; Pitfall 1 state casing; Pitfall 5 Unicode names)
    - .planning/phases/86-multnomah-county-school-districts/86-CONTEXT.md (D-07 through D-13 — government structure decisions, antipartisan, external_id range, no school board races)
  </read_first>
  <action>
Write migration 254 at C:/EV-Accounts/backend/migrations/254_or_school_districts.sql that seeds all 6 Multnomah County school district governments, chambers, SCHOOL districts, 38 board member politicians, and 38 offices in a single transactional migration. Then run the loader live (Task 1's script without --dry-run), apply migration 254 to production Supabase via Supabase MCP, and run the smoke test.

MIGRATION NUMBER: 254 (NOT 253 — RESEARCH.md said 253 but filesystem confirms 253_fix_ca_legislature_orphan_context_rows.sql already exists per gsd planning state 2026-06-01).

MIGRATION FILE STRUCTURE (follow 246_multnomah_cities_government.sql template):

1. HEADER COMMENT BLOCK (lines 1-25 of file):
   - "-- Migration 254: OR school district government + chambers + SCHOOL districts + officials + offices"
   - List the 6 districts with their geo_ids and member counts (totals: 6 governments, 6 chambers, 6 districts, 38 politicians, 38 offices)
   - State the 5 CRITICAL rules: slug GENERATED ALWAYS, governments has no unique constraint on geo_id, districts.state='or' lowercase, governments.state='OR' uppercase, district_type='SCHOOL' (NOT 'SCHOOL_DISTRICT'), G5420 geofence rows must exist BEFORE this migration runs (loaded by loader script in Task 1).

2. BEGIN; then a pre-flight DO block that RAISE EXCEPTION 'Migration 254 already applied — aborting re-run' if any of the 6 government names already exist (use WHERE name IN (6 quoted names)).

3. SIX GOVERNMENT INSERTS (one per district). Government names (exactly as below for idempotency):
   - 'Portland Public Schools, Oregon, US', geo_id='4110040'
   - 'Parkrose School District 3, Oregon, US', geo_id='4109480'
   - 'Reynolds School District 7, Oregon, US', geo_id='4110520'
   - 'Centennial School District 28J, Oregon, US', geo_id='4102800'
   - 'David Douglas School District 40, Oregon, US', geo_id='4103940'
   - 'Riverdale School District 51J, Oregon, US', geo_id='4110560'
   Each uses: INSERT INTO essentials.governments (id, name, type, state, city, geo_id) SELECT gen_random_uuid(), '<name>', 'LOCAL', 'OR', NULL, '<geoid>' WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name = '<name>').

4. SIX CHAMBER INSERTS. Each chamber:
   - name='Board of Education'
   - name_formal='<District Name> Board of Education' (e.g. 'Portland Public Schools Board of Education')
   - government_id resolved via (SELECT id FROM essentials.governments WHERE name = '<full name with ", Oregon, US">')
   - DO NOT include slug in the column list (GENERATED ALWAYS)
   - WHERE NOT EXISTS guard on (name, government_id).

5. SIX SCHOOL DISTRICT INSERTS. For each district:
   - INSERT INTO essentials.districts (id, district_type, state, geo_id, label, mtfcc) SELECT gen_random_uuid(), 'SCHOOL', 'or', '<GEOID>', '<District Short Label>', 'G5420' WHERE NOT EXISTS (...).
   - state MUST be 'or' lowercase (Pitfall 1).
   - label values: 'Portland Public Schools', 'Parkrose School District', 'Reynolds School District', 'Centennial School District', 'David Douglas School District', 'Riverdale School District'.

6. THIRTY-EIGHT POLITICIAN + OFFICE BLOCKS. Each follows the CTE pattern from 244_multnomah_county_government.sql lines 90-119:
```
WITH ins_p AS (
  INSERT INTO essentials.politicians
    (id, full_name, first_name, last_name, party, is_active, is_appointed,
     is_vacant, is_incumbent, external_id)
  VALUES (gen_random_uuid(), '<full_name>', '<first>', '<last>', NULL,
          true, false, false, true, <external_id>)
  ON CONFLICT (external_id) DO NOTHING
  RETURNING id
)
INSERT INTO essentials.offices
  (id, district_id, chamber_id, politician_id, title, representing_state,
   is_appointed_position, is_vacant, role_canonical)
SELECT gen_random_uuid(), d.id, ch.id, p.id,
       '<title>', 'OR', false, false, NULL
FROM essentials.districts d
CROSS JOIN ins_p p
JOIN essentials.chambers ch ON ch.government_id = (
  SELECT id FROM essentials.governments WHERE name = '<gov name>'
)
WHERE d.geo_id = '<GEOID>'
  AND d.district_type = 'SCHOOL'
  AND d.state = 'or'
  AND ch.name = 'Board of Education'
  AND p.id IS NOT NULL
  AND NOT EXISTS (
    SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id
  );
```

   EXACT ROSTER (use these names verbatim — research-verified 2026-06-01; preserve Unicode):

   PPS — geo_id='4110040', govname='Portland Public Schools, Oregon, US' (D-09 verified):
   - -860001: Edward Wang (Chair, Zone 7) — title 'Board Member (Zone 7)'
   - -860002: Michelle DePass (Vice-Chair, Zone 2) — 'Board Member (Zone 2)'
   - -860003: Christy Splitt (Zone 1) — 'Board Member (Zone 1)'
   - -860004: Patte Sullivan (Zone 3) — 'Board Member (Zone 3)'
   - -860005: Rashelle Chase-Miller (Zone 4) — 'Board Member (Zone 4)'
   - -860006: Virginia La Forte (Zone 5) — 'Board Member (Zone 5)'
   - -860007: Stephanie Engelsman (Zone 6) — 'Board Member (Zone 6)'

   Parkrose — geo_id='4109480', govname='Parkrose School District 3, Oregon, US':
   - -860011: Paul Tabron Jr. (Position 1 Chair) — 'Board Member (Position 1)'
   - -860012: Brenda Rivas (Position 2 Vice Chair) — 'Board Member (Position 2)'
   - -860013: Joash Bullock (Position 3) — 'Board Member (Position 3)'
   - -860014: Adolfo Jimenez (Position 4) — 'Board Member (Position 4)'
   - -860015: Mariah Galaviz (Position 5) — 'Board Member (Position 5)'

   Reynolds — geo_id='4110520', govname='Reynolds School District 7, Oregon, US' (preserve Unicode ñ — Pitfall 5):
   - -860021: Aaron Muñoz (Position 1) — 'Board Member (Position 1)'
   - -860022: Joyce Rosenau (Position 2 Vice Chair) — 'Board Member (Position 2)'
   - -860023: Michael Reyes (Position 3 Chair) — 'Board Member (Position 3)'
   - -860024: Cayle Tern (Position 4) — 'Board Member (Position 4)'
   - -860025: Patty Carrera (Position 5) — 'Board Member (Position 5)'
   - -860026: Ana Gonzalez Muñoz (Position 6) — 'Board Member (Position 6)'
   - -860027: Francisco Ibarra (Position 7) — 'Board Member (Position 7)'

   Centennial — geo_id='4102800', govname='Centennial School District 28J, Oregon, US' (post-May-2025 roster):
   - -860031: David Linn (Position 1) — 'Board Member (Position 1)'
   - -860032: Ronald Hardin (Position 2) — 'Board Member (Position 2)' (note: nickname "Jess" — store as Ronald Hardin per LAUSD precedent of dropping informal nicknames)
   - -860033: Will Mohring (Position 3 Vice Chair) — 'Board Member (Position 3)'
   - -860034: Melissa Standley (Position 4) — 'Board Member (Position 4)'
   - -860035: Rose Solowski (Position 5 Chair) — 'Board Member (Position 5)'
   - -860036: Michael Newman (Position 6) — 'Board Member (Position 6)'
   - -860037: Pam Shields (Position 7) — 'Board Member (Position 7)'
   At execution time, executor MUST verify roster against csd28j.org/boardmembers (D-09; Open Question 1) — if names differ, use the live website roster and note discrepancy in SUMMARY.

   David Douglas — geo_id='4103940', govname='David Douglas School District 40, Oregon, US' (preserve Unicode é; strip credential suffix per Pitfall 5):
   - -860041: Althea Ender (Position 1) — 'Board Member (Position 1)'
   - -860042: Stephanie Stephens (Position 2) — 'Board Member (Position 2)' (full_name='Stephanie Stephens'; first='Stephanie'; last='Stephens'; note: drop the 'D.' middle initial per LAUSD precedent — A4 assumption)
   - -860043: Sara Epstein (Position 3) — 'Board Member (Position 3)' (strip ', MPH' suffix; full_name='Sara Epstein', last='Epstein')
   - -860044: Muriel Jordan (Position 4) — 'Board Member (Position 4)'
   - -860045: Thomas Stephenson (Position 5) — 'Board Member (Position 5)'
   - -860046: Heather Franklin (Position 6 Chair) — 'Board Member (Position 6)'
   - -860047: José Gamero-Georgeson (Position 7 Vice Chair) — 'Board Member (Position 7)' (full_name='José Gamero-Georgeson', first='José', last='Gamero-Georgeson')

   Riverdale — geo_id='4110560', govname='Riverdale School District 51J, Oregon, US':
   - -860051: Shaina Weinstein (Seat 1 Vice-Chair) — 'Board Member (Seat 1)'
   - -860052: Mina Stricklin (Seat 2 Chair) — 'Board Member (Seat 2)'
   - -860053: Michele Rosenbaum (Seat 3) — 'Board Member (Seat 3)'
   - -860054: Ali Lanenga (Seat 4) — 'Board Member (Seat 4)'
   - -860055: Milessa Lowrie (Seat 5) — 'Board Member (Seat 5)'

7. OFFICE_ID BACK-FILL (after all 38 blocks, before post-verify):
```
UPDATE essentials.politicians p
SET office_id = o.id
FROM essentials.offices o
WHERE o.politician_id = p.id
  AND p.external_id BETWEEN -860055 AND -860001
  AND p.office_id IS NULL;
```

8. POST-VERIFICATION DO BLOCK with the following gates (each RAISE EXCEPTION on failure):
   - Gate (a): COUNT essentials.governments WHERE name IN (6 names) = 6
   - Gate (b): COUNT essentials.chambers WHERE name = 'Board of Education' AND government_id IN (subquery for 6 govs) = 6
   - Gate (c): COUNT essentials.districts WHERE district_type='SCHOOL' AND state='or' AND geo_id IN (6 GEOIDs) = 6
   - Gate (d): COUNT essentials.politicians WHERE external_id BETWEEN -860055 AND -860001 = 38
   - Gate (e): COUNT essentials.offices o JOIN districts d ON d.id=o.district_id WHERE d.district_type='SCHOOL' AND d.geo_id IN (6 GEOIDs) = 38
   - Gate (f): Section-split — COUNT geofence_boundaries gb WHERE gb.geo_id IN (6 GEOIDs) AND gb.mtfcc='G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.district_type='SCHOOL' AND d.state='or') = 0
   - Gate (g): Office_id back-fill — COUNT essentials.politicians WHERE external_id BETWEEN -860055 AND -860001 AND office_id IS NULL = 0
   - On all-pass: RAISE NOTICE 'Migration 254 post-verification PASSED: 6 govs, 6 chambers, 6 SCHOOL districts, 38 politicians, 38 offices, section-split=0, office_id back-fill complete'

9. LEDGER ENTRY + COMMIT (end of file):
```
INSERT INTO supabase_migrations.schema_migrations (version)
VALUES ('254')
ON CONFLICT (version) DO NOTHING;
COMMIT;
```

CRITICAL CONSTRAINTS (re-stated for emphasis):
- governments.state = 'OR' UPPERCASE
- offices.representing_state = 'OR' UPPERCASE
- districts.state = 'or' LOWERCASE (Pitfall 1)
- geofence_boundaries.state = '41' (set by loader, not this migration)
- district_type = 'SCHOOL' NOT 'SCHOOL_DISTRICT' (D-08)
- party = NULL on all 38 politicians (D-11)
- is_appointed = false, is_appointed_position = false on all (D-12)
- is_incumbent = true on all (incumbents, not candidates)
- slug column NEVER included in chambers INSERT (GENERATED ALWAYS)
- File MUST be saved as UTF-8 to preserve ñ and é (Pitfall 5)
- Escape apostrophes by doubling (e.g. 'Paul Tabron Jr.' has no apostrophes; if any name had one it would be 'D''Angelo' style)

EXECUTION SEQUENCE (after writing the migration file):
a) Run the loader live first (Task 1 script without --dry-run): `cd /c/EV-Accounts/backend && npx tsx scripts/load-or-school-boundaries.ts`. Must report "All 6 OR school district boundaries loaded successfully" or "Inserted: 6 / Skipped: 0".
b) Apply migration 254 to production Supabase via the Supabase MCP `mcp__supabase-local__apply_migration` tool (the configured local MCP IS the remote production DB per memory feedback_supabase_local_is_remote). Pass the entire SQL file content; migration name 'or_school_districts'.
c) Confirm the ledger row exists: `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('254')`.
d) Run the smoke test: `cd /c/EV-Accounts/backend && npx tsx scripts/smoke-phase86.ts` — must exit 0 with all 5 assertions PASSED.

If smoke SC3 returns < 7 PPS officials but the Gates (d)/(e) inside the migration both passed, this indicates the Portland City Hall coordinate is outside the PPS G5420 polygon — diagnose with: `SELECT gb.geo_id, gb.name FROM essentials.geofence_boundaries gb WHERE gb.mtfcc='G5420' AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint(-122.6794, 45.5231), 4326))` and adjust the smoke test coordinate if needed. Do NOT widen the smoke test to be permissive.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; npx tsx scripts/smoke-phase86.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: migrations/254_or_school_districts.sql exists
    - SOURCE: migration 254 contains the header `-- Migration 254: OR school district government + chambers + SCHOOL districts + officials + offices`
    - SOURCE: migration 254 contains all 6 government names: `Portland Public Schools, Oregon, US`, `Parkrose School District 3, Oregon, US`, `Reynolds School District 7, Oregon, US`, `Centennial School District 28J, Oregon, US`, `David Douglas School District 40, Oregon, US`, `Riverdale School District 51J, Oregon, US`
    - SOURCE: migration 254 contains `district_type='SCHOOL'` (with single quotes) and `state='or'` (lowercase, single quotes)
    - SOURCE: migration 254 contains `'OR'` for governments.state and offices.representing_state (uppercase)
    - SOURCE: migration 254 contains `'G5420'` as mtfcc value
    - SOURCE: migration 254 contains `party=NULL` or `NULL` in the party column position of politician INSERTs
    - SOURCE: migration 254 contains exactly 38 distinct `external_id` integer literals in the range -860055..-860001 (verify with grep filtered to skip comments)
    - SOURCE: migration 254 contains UTF-8 Unicode characters `ñ` (in Aaron Muñoz, Ana Gonzalez Muñoz) and `é` (in José Gamero-Georgeson)
    - SOURCE: migration 254 contains `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('254')`
    - SOURCE: migration 254 does NOT contain the string `slug` inside the chambers INSERT column list — verify with: grep does NOT match `chambers \(.*slug` anywhere in the file
    - BEHAVIOR: `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='41' AND mtfcc='G5420' AND source='tiger_unsd_or_2024'` returns 6 (after loader run)
    - BEHAVIOR: `SELECT COUNT(*) FROM essentials.governments WHERE name IN (6 names)` returns 6 (after migration applied)
    - BEHAVIOR: `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='or' AND geo_id IN ('4110040','4109480','4110520','4102800','4103940','4110560')` returns 6
    - BEHAVIOR: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -860055 AND -860001` returns 38
    - BEHAVIOR: `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='SCHOOL' AND d.geo_id IN (6 GEOIDs)` returns 38
    - BEHAVIOR: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -860055 AND -860001 AND office_id IS NULL` returns 0
    - BEHAVIOR: `SELECT version FROM supabase_migrations.schema_migrations WHERE version='254'` returns 1 row
    - BEHAVIOR: `npx tsx scripts/smoke-phase86.ts` exits 0 and stdout contains `ALL ASSERTIONS PASSED`
    - BEHAVIOR: Re-running migration 254 raises exception `Migration 254 already applied — aborting re-run` (idempotency proof)
  </acceptance_criteria>
  <done>
Loader has been run live (6 G5420 rows inserted), migration 254 has been applied to production via Supabase MCP, all 7 post-verification gates inside the migration passed (RAISE NOTICE shown), and smoke-phase86.ts exits 0 with ALL ASSERTIONS PASSED. The phase Plan 02 (headshots) can now begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov → loader (HTTPS download) | External TLS download of TIGER UNSD zip into a temp dir on the backend host |
| Backend host → Supabase (DB writes) | DATABASE_URL connection from loader + migration tooling to production Supabase |
| Disk filesystem (temp .tmp-or-school-unsd/) | Untrusted ZIP contents extracted to a local temp path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | TIGER UNSD download bytes | mitigate | HTTPS-only fetch enforced (downloadWithRedirects uses `https.get`); reject non-200; reject if final downloaded zip cannot be opened by AdmZip (corruption detection); EXPECTED_COUNT=6 assertion will fail loudly if shapefile is malformed |
| T-86-02 | Tampering | Production DB rows (G5420 + governments + politicians) | mitigate | All INSERTs use parameterized `$N` placeholders (pg `Pool.query` arg array); SQL migration uses string literals only — no dynamic concatenation; idempotency guards (`ON CONFLICT DO NOTHING`, `WHERE NOT EXISTS`, pre-flight RAISE EXCEPTION) prevent silent duplicate writes |
| T-86-03 | Information Disclosure | DATABASE_URL leakage in logs | mitigate | Loader and smoke test do not log process.env contents; connection string only used to construct Pool/Client objects; no logging of raw env values |
| T-86-04 | Denial of Service | TIGER zip cached on disk | accept | tl_2024_41_unsd.zip is 2.9 MB; temp dir cleanup runs in finally; max single-run disk impact bounded; no recursive expansion attack surface from a known census.gov URL |
| T-86-05 | Repudiation | Migration application | mitigate | Single migration version '254' inserted into `supabase_migrations.schema_migrations` ledger; failed migrations roll back inside the BEGIN/COMMIT transaction; post-verification DO block raises and aborts on any gate failure |
| T-86-06 | Elevation of Privilege | None | accept | No new user-facing endpoints, no new auth surfaces, no role grants modified in this phase |
| T-86-07 | Spoofing | URL hijack via redirect | mitigate | `downloadWithRedirects` accepts redirects only via the `response.headers.location` field of an HTTP(S) response; final destination must return 200 and unzip cleanly; the GEOID-filter step rejects any feature outside the 6 known target GEOIDs (silent acceptance impossible) |
| T-86-SC | Tampering | npm package installs | accept | No new npm dependencies introduced — `pg`, `adm-zip`, `shapefile`, `dotenv`, `https` already present in package.json (verified via RESEARCH.md "Environment Availability"); no Package Legitimacy Audit required (no new installs) |
</threat_model>

<verification>
The verification gates for this plan run in three layers:

1. **Loader dry-run** (Task 1): proves the TIGER URL resolves, zip extracts, shapefile parses, and all 6 GEOIDs match before any DB write.
2. **Smoke RED step** (Task 2): proves the smoke test would catch a real failure — running it before data is loaded must exit non-zero on SC1–SC4.
3. **Migration post-verification DO block + smoke GREEN** (Task 3): seven internal gates inside migration 254 fire on apply; the smoke test then re-validates from the routing perspective (geofence → district → office → politician JOIN with ST_Covers).

End-state SQL gates (run by smoke-phase86.ts and by the migration's own post-verify DO block):
- COUNT(geofence_boundaries WHERE state='41' AND mtfcc='G5420' AND source='tiger_unsd_or_2024') = 6
- COUNT(districts WHERE district_type='SCHOOL' AND state='or' AND geo_id IN 6 GEOIDs) = 6
- COUNT(politicians WHERE external_id BETWEEN -860055 AND -860001) = 38
- COUNT(politicians WHERE external_id BETWEEN -860055 AND -860001 AND office_id IS NULL) = 0
- COUNT(offices o JOIN districts d ON d.id=o.district_id WHERE d.district_type='SCHOOL' AND d.geo_id IN 6 GEOIDs) = 38
- Section-split: COUNT(orphan G5420 geofences with no SCHOOL district row) = 0
- Routing: Portland City Hall (-122.6794, 45.5231) returns ≥ 7 PPS members
- Routing: Riverdale (-122.6794, 45.4472) returns ≥ 5 Riverdale members and 0 PPS members
- Idempotency: re-running migration 254 raises 'already applied'
</verification>

<success_criteria>
Plan 01 is COMPLETE when ALL of the following are true:

1. `C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts` exists and `--dry-run` reports all 6 target GEOIDs found
2. `C:/EV-Accounts/backend/scripts/smoke-phase86.ts` exists with 5 labeled assertions (SC1–SC5)
3. `C:/EV-Accounts/backend/migrations/254_or_school_districts.sql` exists, applied to production, ledger row for '254' present
4. Loader has run live: 6 G5420 rows present in `geofence_boundaries` with source='tiger_unsd_or_2024'
5. Migration's internal post-verification DO block raised NOTICE (not EXCEPTION) — all 7 gates green
6. `npx tsx scripts/smoke-phase86.ts` exits 0 with `ALL ASSERTIONS PASSED`
7. Re-running migration 254 raises 'Migration 254 already applied — aborting re-run' (idempotency)
8. OR-SCHOOL-01, OR-SCHOOL-02, OR-SCHOOL-03 marked verifiable in 86-VERIFICATION.md (created by execute-phase)
</success_criteria>

<output>
Create `.planning/phases/86-multnomah-county-school-districts/86-01-SUMMARY.md` when done.

Required sections in the SUMMARY:
- Files modified (with absolute paths + commit SHA)
- Loader run output (inserted vs skipped counts)
- Migration application output (post-verification NOTICE text)
- Smoke test output (ALL ASSERTIONS PASSED block)
- 38-politician roster confirmation table (external_id, full_name, district, title)
- Any deviation from RESEARCH.md (e.g. if Centennial roster differed from research, or PPS Finalsite UUIDs needed)
- Migration number discrepancy note: research said "253", actual is "254" — record reasoning
</output>
