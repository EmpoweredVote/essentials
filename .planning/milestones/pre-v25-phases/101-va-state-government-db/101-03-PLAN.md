---
phase: 101-va-state-government-db
plan: 03
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - C:/EV-Accounts/backend/migrations/generate_va_senate.ps1
  - C:/EV-Accounts/backend/migrations/302_va_state_senators.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-302.ts
autonomous: true
requirements:
  - VA-GOV-03
tags:
  - virginia
  - state-government
  - senators
  - sql-migration
  - powershell-generator

must_haves:
  truths:
    - "All 40 Virginia state senators (SD-1 through SD-40) are seeded in essentials.politicians with external_ids -5110001 through -5110040, party set per roster (21 Democrats, 19 Republicans), is_active=true, is_appointed=false, is_vacant=false"
    - "40 office rows are seeded, each linking a senator politician to the matching STATE_UPPER district via geo_id (51001-51040) under the Virginia Senate chamber of State of Virginia government"
    - "Every office row has title='Senator', representing_state='VA', is_appointed_position=false, is_vacant=false"
    - "All 40 senator politicians have office_id back-filled (NULL count = 0 after migration)"
    - "Migration is idempotent — re-running produces 0 new rows"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/generate_va_senate.ps1"
      provides: "PowerShell generator that emits 302_va_state_senators.sql with 40 CTE blocks + back-fill"
      contains: "function SenatorBlock"
    - path: "C:/EV-Accounts/backend/migrations/302_va_state_senators.sql"
      provides: "Generated 40-senator migration (BEGIN/COMMIT wrapped, 40 CTE blocks + 1 back-fill UPDATE)"
      contains: "WITH ins_p AS"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-302.ts"
      provides: "Apply script that reads 302_va_state_senators.sql and runs smoke tests"
      contains: "302_va_state_senators.sql"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts (STATE_UPPER, state='va' lowercase, geo_id='51001'..'51040')"
      via: "d.geo_id = '<gid>' AND d.district_type = 'STATE_UPPER' AND d.state = 'va'"
      pattern: "d.district_type = 'STATE_UPPER' AND d.state = 'va'"
    - from: "essentials.offices.chamber_id"
      to: "essentials.chambers (name='Virginia Senate' under State of Virginia)"
      via: "SELECT id FROM essentials.chambers WHERE name = 'Virginia Senate' AND government_id = (...) "
      pattern: "name = 'Virginia Senate' AND government_id = .SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA'"
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id"
      via: "back-fill UPDATE scoped to external_id BETWEEN -5110040 AND -5110001"
      pattern: "p.external_id BETWEEN -5110040 AND -5110001 AND p.office_id IS NULL"
---

<objective>
Generate, write, and apply migration 302 to seed all 40 Virginia state senators (SD-1 through SD-40) as politicians with offices linked to the STATE_UPPER districts under the Virginia Senate chamber. Implements VA-GOV-03.

Purpose: VA Senate is one of the two chambers seeded in Plan 01. The 40 senators are the full membership of the upper chamber and must each have an office row linking them to their SLDU district's geo_id (51001-51040).

Output:
- PowerShell generator: `C:/EV-Accounts/backend/migrations/generate_va_senate.ps1` (analog: generate_md_senate.ps1 with 7 VA substitutions)
- Generated SQL: `C:/EV-Accounts/backend/migrations/302_va_state_senators.sql` (40 CTE blocks + back-fill)
- Apply script: `C:/EV-Accounts/backend/scripts/_apply-migration-302.ts`
- DB state: 40 senator politician rows + 40 office rows + office_id back-filled
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-va-state-government-db/101-RESEARCH.md
@.planning/phases/101-va-state-government-db/101-PATTERNS.md
@C:/EV-Accounts/backend/migrations/generate_md_senate.ps1
@C:/EV-Accounts/backend/migrations/273_md_state_senators.sql
@C:/EV-Accounts/backend/scripts/_apply-migration-273.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write generate_va_senate.ps1 and emit 302_va_state_senators.sql</name>
  <files>
    C:/EV-Accounts/backend/migrations/generate_va_senate.ps1,
    C:/EV-Accounts/backend/migrations/302_va_state_senators.sql
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/generate_md_senate.ps1 (exact analog; 7 VA substitutions per PATTERNS.md)
    - .planning/phases/101-va-state-government-db/101-PATTERNS.md (`generate_va_senate.ps1` section lines 162-261; UTF-8 NoBOM write pattern; CTE count verification)
    - .planning/phases/101-va-state-government-db/101-RESEARCH.md (Complete Rosters > VA State Senators table lines 318-365 — full 40-row roster)
  </read_first>
  <behavior>
    - Generator script accepts optional `-Out` parameter (default `C:/EV-Accounts/backend/migrations/302_va_state_senators.sql`)
    - Generator hard-codes the 40-senator roster (district, full, first, last, party, ext_id) inside the script
    - Generator emits one CTE block per senator using the SenatorBlock function; each block follows the exact CTE shape from the MD senate analog with VA substitutions
    - Generator writes output file as UTF-8 without BOM (psql compatibility)
    - Generator prints `CTE blocks (senators): 40  (expected 40)` after writing
    - Output SQL: 1 BEGIN, 1 COMMIT, 40 `WITH ins_p AS` blocks, exactly 1 `UPDATE essentials.politicians` back-fill statement
    - Every CTE block specifies `d.district_type = 'STATE_UPPER' AND d.state = 'va'` (lowercase) in the WHERE clause — critical because geo_ids 51001-51040 overlap with STATE_LOWER
    - Every office row in output has chamber subquery `WHERE name = 'Virginia Senate' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA')`
    - Every office row has `representing_state='VA'`, `title='Senator'`, `is_appointed_position=false`, `is_vacant=false`
    - NOT EXISTS guard uses `(district_id, chamber_id)` — single-member upper chamber
    - office_id back-fill range is exactly `BETWEEN -5110040 AND -5110001`
    - Politician INSERT includes `AND p.id IS NOT NULL` guard in the cross-join (MD senate lesson)
  </behavior>
  <action>
    Create `C:/EV-Accounts/backend/migrations/generate_va_senate.ps1` by adapting `generate_md_senate.ps1` with these exact substitutions:

    1. `param([string]$Out = "C:/EV-Accounts/backend/migrations/302_va_state_senators.sql")`.
    2. Inside `SenatorBlock`, change FIPS prefix: `$gid = '51' + ([int]$r.dist).ToString().PadLeft(3, '0')` (not '24').
    3. Chamber name in office INSERT: `'Virginia Senate'` (replaces 'Maryland Senate' everywhere).
    4. Government subquery: `WHERE name = 'State of Virginia' AND state = 'VA'` (add `AND state = 'VA'` — MD has it, OR omits — VA must include per Pitfall 5 to avoid West Virginia collision).
    5. District JOIN: `d.state = 'va'` lowercase (TIGER casing for STATE_UPPER); MUST include `AND d.district_type = 'STATE_UPPER'` (critical because geo_ids 51001-51040 also exist in STATE_LOWER — omitting causes 2 offices per senator).
    6. `representing_state = 'VA'` on every office row.
    7. Title field: `'Senator'` (unchanged from MD).
    8. Keep `AND p.id IS NOT NULL` guard in the CROSS JOIN (MD senate pattern, line 4 of analog).
    9. NOT EXISTS guard: `(district_id, chamber_id)` — keep single-member single-member pattern from MD.
    10. Replace MD `$roster` array with the 40-row VA Senate roster from RESEARCH.md lines 324-363. Each row is `@{ dist=N; full='<full>'; first='<first>'; last='<last>'; party='<Democrat|Republican>'; ext_id=-(5110000+N) }`. Use exact names with periods, middle initials, and suffixes (e.g., 'William M. Stanley, Jr.', 'Luther H. Cifers, III', 'J.D. "Danny" Diggs'). For names with embedded quotes (Diggs has `"Danny"`), escape via EscSql which doubles single quotes — but the double-quotes inside the string are fine in PowerShell single-quoted strings; the SQL string will use single quotes so embedded `"` is literal. Verify by running: SELECT contains `'J.D. "Danny" Diggs'`.
    11. After foreach, append office_id back-fill block:
       ```
       -- ===== office_id back-fill =====
       UPDATE essentials.politicians p
       SET office_id = o.id
       FROM essentials.offices o
       WHERE o.politician_id = p.id
         AND p.external_id BETWEEN -5110040 AND -5110001
         AND p.office_id IS NULL;
       ```
    12. Write file via `[System.IO.File]::WriteAllText($Out, $sb.ToString(), [System.Text.UTF8Encoding]::new($false))` (UTF-8 NoBOM).
    13. After write, regex-count `WITH ins_p AS` matches and print `CTE blocks (senators): $cteCount  (expected 40)`.
    14. Header comment at top of generated SQL must cite source analogs (273_md_state_senators.sql, generate_md_senate.ps1), phase 101, requirement VA-GOV-03, plus the 3 CRITICAL warnings about state casing, district_type filter, and idempotency.

    Then EXECUTE the generator from PowerShell:
    `powershell -NoProfile -ExecutionPolicy Bypass -File C:/EV-Accounts/backend/migrations/generate_va_senate.ps1`

    This produces `C:/EV-Accounts/backend/migrations/302_va_state_senators.sql`. Confirm console output reports `CTE blocks (senators): 40  (expected 40)`.

    The 40-senator roster (from RESEARCH.md) with district, full_name, party, ext_id:
    SD-1 Timmy French R -5110001; SD-2 Mark D. Obenshain R -5110002; SD-3 Christopher T. Head R -5110003; SD-4 David R. Suetterlein R -5110004; SD-5 T. Travis Hackworth R -5110005; SD-6 Todd E. Pillion R -5110006; SD-7 William M. Stanley, Jr. R -5110007; SD-8 Mark J. Peake R -5110008; SD-9 Tammy Brankley Mulchi R -5110009; SD-10 Luther H. Cifers, III R -5110010; SD-11 R. Creigh Deeds D -5110011; SD-12 Glen H. Sturtevant, Jr. R -5110012; SD-13 Lashrecse D. Aird D -5110013; SD-14 Lamont Bagby D -5110014; SD-15 Michael J. Jones D -5110015; SD-16 Schuyler T. VanValkenburg D -5110016; SD-17 Emily M. Jordan R -5110017; SD-18 L. Louise Lucas D -5110018; SD-19 Christie New Craig R -5110019; SD-20 Bill DeSteph R -5110020; SD-21 Angelia Williams Graves D -5110021; SD-22 Aaron R. Rouse D -5110022; SD-23 Mamie E. Locke D -5110023; SD-24 J.D. "Danny" Diggs R -5110024; SD-25 Richard H. Stuart R -5110025; SD-26 Ryan T. McDougle R -5110026; SD-27 Tara A. Durant R -5110027; SD-28 Bryce E. Reeves R -5110028; SD-29 Jeremy S. McPike D -5110029; SD-30 Danica A. Roem D -5110030; SD-31 Russet W. Perry D -5110031; SD-32 Kannan Srinivasan D -5110032; SD-33 Jennifer D. Carroll Foy D -5110033; SD-34 Scott A. Surovell D -5110034; SD-35 David W. Marsden D -5110035; SD-36 Stella G. Pekarsky D -5110036; SD-37 Saddam Azlan Salim D -5110037; SD-38 Jennifer B. Boysko D -5110038; SD-39 Elizabeth B. Bennett-Parker D -5110039; SD-40 Barbara A. Favola D -5110040.

    Compute first/last from each full_name: first = tokens before last token excluding common suffixes (Jr., III); last = the final non-suffix token (or compound last name like 'Bennett-Parker', 'Carroll Foy'). Where ambiguous, follow MD precedent (MD compound last names use final word). For 'Carroll Foy' use 'Foy' as last (final word per MD compound rule); for 'Bennett-Parker' use 'Bennett-Parker' (hyphenated counted as one token). For 'William M. Stanley, Jr.' → first='William M.', last='Stanley' (drop suffix per MD pattern). Document each first/last in the script roster array.
  </action>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/generate_va_senate.ps1` exists
    - File `C:/EV-Accounts/backend/migrations/302_va_state_senators.sql` exists (produced by running the generator)
    - Generator script console output contains `CTE blocks (senators): 40  (expected 40)`
    - Generated SQL file contains exactly 40 occurrences of `WITH ins_p AS`
    - Generated SQL file contains exactly 1 `BEGIN;` and exactly 1 `COMMIT;`
    - Generated SQL file contains exactly 1 `UPDATE essentials.politicians` statement
    - Generated SQL file contains the literal substrings: `'Virginia Senate'`, `'Senator'`, `representing_state`, `'VA'`, `BETWEEN -5110040 AND -5110001`
    - Generated SQL file contains the literal substrings `d.district_type = 'STATE_UPPER'` AND `d.state = 'va'` (lowercase TIGER casing for district lookup)
    - Generated SQL file contains 40 distinct external_ids in range -5110001 to -5110040
    - Generated SQL contains zero occurrences of `'Maryland'`, `'OR'`, `'Oregon'`, `'md'` quoted as a state value
    - Generated SQL contains zero occurrences of `is_appointed = true` or `is_appointed_position = true`
    - Generated SQL contains the literal substring `'J.D. "Danny" Diggs'` (verifying embedded-quote handling for SD-24)
    - Generated SQL file is UTF-8 without BOM (first 3 bytes are NOT 0xEF 0xBB 0xBF)
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$g='C:/EV-Accounts/backend/migrations/generate_va_senate.ps1'; $s='C:/EV-Accounts/backend/migrations/302_va_state_senators.sql'; if (-not (Test-Path $g) -or -not (Test-Path $s)) { Write-Host 'FAIL: file missing'; exit 1 }; $c=Get-Content $s -Raw; $bytes=[System.IO.File]::ReadAllBytes($s); $errors=@(); if ($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF) { $errors += 'BOM present' }; $cte=([regex]::Matches($c, 'WITH ins_p AS')).Count; if ($cte -ne 40) { $errors += \"CTE count $cte != 40\" }; if (($c -split 'BEGIN;').Length - 1 -ne 1) { $errors += 'BEGIN count != 1' }; if (($c -split 'UPDATE essentials.politicians').Length - 1 -ne 1) { $errors += 'UPDATE count != 1' }; foreach ($n in @(\"'Virginia Senate'\",\"'Senator'\",\"BETWEEN -5110040 AND -5110001\",\"d.district_type = 'STATE_UPPER'\",\"d.state = 'va'\",\"'J.D. \"\"Danny\"\" Diggs'\")) { if (-not ($c.Contains($n))) { $errors += \"missing: $n\" } }; foreach ($f in @(\"'Maryland\",\"'OR'\",\"'Oregon\",\"is_appointed = true\",\"is_appointed_position = true\")) { if ($c.Contains($f)) { $errors += \"forbidden: $f\" } }; for ($i=1; $i -le 40; $i++) { $id = '-' + (5110000 + $i); if (-not ($c.Contains($id))) { $errors += \"missing ext_id $id\" } }; if ($errors.Count -gt 0) { Write-Host ('FAIL: ' + ($errors -join '; ')); exit 1 } else { Write-Host 'PASS: 302_va_state_senators.sql'; exit 0 }"</automated>
  </verify>
  <done>
    Generator script writes a syntactically clean 302_va_state_senators.sql with 40 CTE blocks, all VA-specific substitutions correct, lowercase state='va' on district lookups, STATE_UPPER district_type filter, no BOM, embedded-quote SD-24 name handled correctly.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write _apply-migration-302.ts and execute it</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-302.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-273.ts (exact analog with MD->VA substitutions)
    - .planning/phases/101-va-state-government-db/101-PATTERNS.md (`scripts/_apply-migration-302.ts` section lines 348-407; VA substitutions list)
  </read_first>
  <behavior>
    - Script reads `migrations/302_va_state_senators.sql` from process.cwd() and submits via `pg.Pool` using `process.env['DATABASE_URL']`
    - On success, prints `Migration 302 applied successfully`
    - Runs smoke test 1 (count): senator office count under Virginia Senate chamber = 40
    - Runs spot-check 1: SD-01 Timmy French (-5110001) — politician + office linked to district geo_id='51001' district_type='STATE_UPPER' state='va'
    - Runs spot-check 2: SD-20 Bill DeSteph (-5110020) — linked to geo_id='51020'
    - Runs spot-check 3: SD-40 Barbara A. Favola (-5110040) — linked to geo_id='51040'
    - Runs NULL office_id check: 0 politicians in range BETWEEN -5110040 AND -5110001 have office_id IS NULL
    - On any pg error, prints `Error applying migration 302:` and exits 1
  </behavior>
  <action>
    Create `C:/EV-Accounts/backend/scripts/_apply-migration-302.ts` by copying `_apply-migration-273.ts` and applying these substitutions:

    1. Migration filename: `'273_md_state_senators.sql'` → `'302_va_state_senators.sql'`.
    2. Chamber name in count query: `'Maryland Senate'` → `'Virginia Senate'`.
    3. `representing_state = 'MD'` → `representing_state = 'VA'`.
    4. external_id range in NULL check: `BETWEEN -2410047 AND -2410001` → `BETWEEN -5110040 AND -5110001`.
    5. Expected count: `(expected 47)` → `(expected 40)`.
    6. Spot-check queries: SD-01 (-5110001), SD-20 (-5110020), SD-40 (-5110040) — three SELECTs that JOIN essentials.politicians + essentials.offices + essentials.districts and print full_name, external_id, geo_id, district_type, state.
    7. Error message: `'Error applying migration 273:'` → `'Error applying migration 302:'`.
    8. `console.log('Migration 273 applied successfully')` → `console.log('Migration 302 applied successfully')`.

    Then execute the script via:
    `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-302.ts`

    Console output must show:
    - `Migration 302 applied successfully`
    - `Senator offices (Virginia Senate chamber): 40 (expected 40)`
    - 3 spot-check rows with correct geo_ids ('51001', '51020', '51040'), district_type='STATE_UPPER', state='va'
    - `NULL office_id count (expected 0): 0`

    If script fails, fix the issue and re-run. The migration is idempotent via ON CONFLICT/NOT EXISTS, so re-running after partial failure is safe.
  </action>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/_apply-migration-302.ts` exists
    - File contains the literal substring `'302_va_state_senators.sql'`
    - File contains the literal substring `'Virginia Senate'`
    - File contains the literal substring `BETWEEN -5110040 AND -5110001`
    - File contains exactly 0 occurrences of `'Maryland Senate'`, `'273_md_state_senators.sql'`, `-2410001`, `-2410047`
    - When executed via `npx tsx`, console contains:
      • `Migration 302 applied successfully`
      • `Senator offices (Virginia Senate chamber):` followed by `40` (and `(expected 40)`)
      • 3 spot-check rows for SD-1, SD-20, SD-40 each showing the correct geo_id
      • `NULL office_id count (expected 0): 0`
    - Post-apply Supabase query `SELECT COUNT(*)::int FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.governments g ON g.id=c.government_id WHERE c.name='Virginia Senate' AND g.name='State of Virginia' AND g.state='VA'` returns 40
    - Post-apply query `SELECT COUNT(*)::int FROM essentials.politicians WHERE external_id BETWEEN -5110040 AND -5110001` returns 40
    - Post-apply query `SELECT COUNT(*)::int FROM essentials.politicians WHERE external_id BETWEEN -5110040 AND -5110001 AND office_id IS NULL` returns 0
    - Section-split style sanity: `SELECT COUNT(*)::int FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE o.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -5110040 AND -5110001) AND (d.district_type != 'STATE_UPPER' OR d.state != 'va')` returns 0
  </acceptance_criteria>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/_apply-migration-302.ts 2>&1 | tee /tmp/302_apply.log && grep -q "Migration 302 applied successfully" /tmp/302_apply.log && grep -q "(expected 40)" /tmp/302_apply.log && grep -q "NULL office_id count (expected 0): 0" /tmp/302_apply.log</automated>
  </verify>
  <done>
    Apply script created, executed successfully, 40 senator offices seeded under Virginia Senate chamber, 0 NULL office_ids, 3 spot-checks confirm correct district linkage (STATE_UPPER + lowercase state='va' + correct geo_id), VA-GOV-03 satisfied at data layer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → production Supabase | Migration inserts 80 rows (40 politicians + 40 offices) via DATABASE_URL pg.Pool with postgres role |
| PowerShell generator output → SQL parser | Generated SQL must round-trip through psql/pg.Pool — UTF-8 BOM would break parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-10 | Tampering | District linkage correctness for senators | mitigate | Generator hard-codes `AND d.district_type = 'STATE_UPPER' AND d.state = 'va'`; 100% geo_id overlap with STATE_LOWER means omitting filter would CROSS JOIN to 2 rows per senator (80 offices instead of 40) — gated by post-apply count assertion = 40 |
| T-101-11 | Tampering | Chamber subquery matches wrong state | mitigate | Government subquery filters `AND state = 'VA'` to avoid West Virginia row collision (UUID 74564736, name 'State of West Virginia') per Pitfall 5 |
| T-101-12 | Tampering | Encoding corruption in generator output | mitigate | UTF-8 NoBOM write enforced via `[System.Text.UTF8Encoding]::new($false)`; embedded-quote name SD-24 ('J.D. "Danny" Diggs') verified via static substring check |
| T-101-13 | Information disclosure | Party data storage | accept | Party stored per-roster but UI is antipartisan; not displayed on profiles |
| T-101-SC | Tampering | npm/pip/cargo installs | mitigate | This plan uses pre-installed `pg` and `tsx` already in `C:/EV-Accounts/backend/package.json`; no new installs |
| T-101-14 | Tampering | Office over-seeding via duplicate apply | mitigate | ON CONFLICT (external_id) DO NOTHING on politicians + NOT EXISTS (district_id, chamber_id) on offices + office_id IS NULL guard on back-fill = full idempotency |
| T-101-15 | Repudiation | No per-row audit trail | accept | Consistent with project pattern; OK for public reference data |
</threat_model>

<verification>
After Task 2 completes:

1. `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5110040 AND -5110001` = 40
2. `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id WHERE c.name='Virginia Senate'` = 40
3. `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5110040 AND -5110001 AND office_id IS NULL` = 0
4. `SELECT party, COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5110040 AND -5110001 GROUP BY party ORDER BY party` → Democrat=21, Republican=19
5. Spot-check senators have correct (politician, geo_id, district_type, state) tuples for SD-01, SD-20, SD-40
6. Re-apply of migration 302 produces 0 new rows
</verification>

<success_criteria>
- VA-GOV-03 satisfied: 40 senators with offices linked to SLDU geofence districts
- Idempotency proven
- No regressions in MD/OR senator data (this migration touches only external_id range -5110040..-5110001 and Virginia Senate chamber)
</success_criteria>

<output>
Create `.planning/phases/101-va-state-government-db/101-03-SUMMARY.md` documenting:
- Generator script + generated SQL + apply script paths
- Generator console output (CTE count line)
- Apply script console output verbatim (count + spot-checks + NULL check)
- Post-apply verification query results (party split, total count, NULL count)
</output>
