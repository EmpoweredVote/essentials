---
phase: 197-sahuarita-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1354_town_of_sahuarita.sql
autonomous: false
requirements: [SUB-03]
must_haves:
  truths:
    - "A greenfield 'Town of Sahuarita, Arizona, US' government (type='Town', state='AZ', geo_id='0462140') exists — none exists today (RESEARCH-verified live 2026-07-16: 0 rows for '%sahuarita%')"
    - "One 'Town Council' chamber (official_count=7) holds exactly 7 at-large offices — no separate Mayor chamber/office (Mayor is a TITLE, not a seat)"
    - "Exactly ONE NEW essentials.districts row exists for geo_id='0462140' (district_type='LOCAL', mtfcc='G4110', state='az') — it did not exist before (Phase 190 wrote only the G4110 geofence; districts had 0 rows for 0462140, DB-confirmed). There is NO LOCAL_EXEC row (Pitfall 3 — no separately-elected Mayor office to represent)"
    - "ALL 7 offices link to the SAME single LOCAL/G4110/0462140 district (Marana/Oro-Valley shared-row precedent) — never a bare geo_id join (0462140 collides with the FIPS-keyed geofence_boundaries row)"
    - "Roster is Murphy(Mayor)/Egbert(Vice Mayor)/Morales/Gillespie/Priolo/Lisk/Lytle; party=NULL on all 7 (nonpartisan/antipartisan); is_appointed=false on all 7 politician rows"
    - "Mayor and Vice Mayor are TITLE annotations on 2 of the 7 at-large seats (title='Mayor' on Murphy, title='Vice Mayor' on Egbert — plain Palm-Springs/Indio style per RESEARCH A4); the other five carry plain 'Council Member'. Exactly 7 offices, no separate Mayor/Vice-Mayor seat. role_canonical NULL on all 7"
    - "The current roster is re-verified against a LIVE source at execute time given the July 21 2026 primary (5 days after research; may already be past) AND the separate post-canvass Mayor/Vice-Mayor title re-vote (Town Code 2.10.010) BEFORE the migration is applied; the structural migration is applied to production and registered in the ledger; 7 politician UUIDs captured for Plans 02/03"
    - "D-01: elected officials only (7 elected council members); no appointed staff (Town Manager, etc.); antipartisan — party=NULL on all 7, never displayed"
    - "D-02 (as resolved by RESEARCH): at-large + nonpartisan confirmed, but Mayor/Vice-Mayor are council-CHOSEN TITLES with NO separately-elected office — HYBRID of the Marana shared-LOCAL shape MINUS its LOCAL_EXEC half PLUS the Palm-Springs title-on-seat modeling (research overrode the CONTEXT default of a LOCAL_EXEC Mayor seat)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1354_town_of_sahuarita.sql"
      provides: "government + Town Council chamber + 1 NEW shared LOCAL district (NO LOCAL_EXEC) + 7 politicians (party NULL, is_appointed=false) + 7 offices (Murphy Mayor-titled, Egbert Vice-Mayor-titled, 5 plain Council Member) + office_id backfill + pre-flight geofence assertion + post-verify DO gates + ledger registration"
      contains: "Town Council"
  key_links:
    - from: "essentials.offices (all 7 incl. Mayor- and Vice-Mayor-titled)"
      to: "essentials.districts (ONE shared LOCAL / G4110 / 0462140)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='G4110' AND state='az'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'Town Council'"
      to: "essentials.governments 'Town of Sahuarita, Arizona, US'"
      via: "government_id subquery scoped by name"
      pattern: "Town of Sahuarita, Arizona, US"
---

<objective>
Author and apply the structural migration that seeds the Town of Sahuarita as a greenfield town
government (its own `essentials.governments` row at geo_id `0462140`), a single `Town Council` chamber
(`official_count=7`), the ONE district row all 7 offices attach to, and 7 politicians/offices (ext_id
`-4014001..-4014007`): a 7-member at-large nonpartisan council on which the Mayor and Vice Mayor are
TITLE annotations (not separate seats). Greenfield — no Town of Sahuarita government row exists today
(RESEARCH-verified live 2026-07-16: `SELECT ... WHERE name ILIKE '%sahuarita%'` => 0 rows).

CRITICAL STRUCTURAL DELTA (this OVERRIDES the CONTEXT D-02 default of a LOCAL_EXEC Mayor). RESEARCH
confirmed via 4 independent sources (Town Code Ch. 2.05/2.10, Wikipedia, sahuaritaaz.gov's own
Election-Information page, tucson.com composite) that Sahuarita's Mayor and Vice Mayor are NOT separately
elected — they are TITLES the 7-member council chooses from among its own membership, re-chosen every 2
years. Therefore this migration creates a HYBRID shape: the Marana/Oro-Valley "all at-large members share
ONE LOCAL district row" DB shape, MINUS Marana's LOCAL_EXEC half (there is no separately-elected Mayor
office), PLUS the Palm-Springs (1329)/Indio title-on-seat office modeling — Mayor/Vice-Mayor live purely
in the `title` string with `role_canonical` NULL. Pitfall 3: creating a `LOCAL_EXEC` row here would be a
phantom seat with no electoral basis — DO NOT create one. Sahuarita has NO wards and NO council districts:
NO geofence loader, NO ArcGIS query, NO new X00xx mtfcc. The Town's whole-town boundary geofence `0462140`
(G4110, FIPS 04-62140) is already live from Phase 190, but `essentials.districts` has ZERO rows for it
(DB-confirmed this session). This migration creates exactly ONE NEW `essentials.districts` row for
`0462140` (`district_type='LOCAL'`, `mtfcc='G4110'`, `state='az'` lowercase), shared by ALL 7 offices.

CRITICAL (nonpartisan / titles): (1) party=NULL on all 7 — Sahuarita runs NONPARTISAN elections
(confirmed via Ballotpedia's "Sahuarita Town Council At-large" candidate-page naming convention); do NOT
record a party. (2) Use PLAIN Palm-Springs-style titles — `'Mayor'` on Murphy, `'Vice Mayor'` on Egbert,
`'Council Member'` on the other five (RESEARCH A4: the plain style is the closer structural analog since
neither is a separately-elected office; either renders correctly — planner locks PLAIN). (3) Mayor and
Vice Mayor are NOT separate seats — exactly 7 offices total; `role_canonical` stays NULL on all 7.

CRITICAL (the #1 risk of this phase — the tightest research-to-election window in the milestone, layered):
Sahuarita's July 21, 2026 municipal primary is 5 days after the 2026-07-16 research date (the execute date
may land ON or AFTER it). Three seats are up (Murphy's, Egbert's, Morales's). Vice Mayor Kara Egbert is
CONFIRMED NOT seeking re-election (running for Precinct 7 Justice of the Peace) — her seat is guaranteed to
change hands. AND, uniquely to this phase, per Town Code 2.10.010 the Mayor and Vice Mayor TITLES are
re-chosen by the newly-seated council at the first meeting after the canvass — a SEPARATE, SCHEDULED event
from the election itself, so even a "no membership change" primary does not resolve who holds the
Mayor/Vice-Mayor titles. Task 2 is a SUBSTANTIVE, non-perfunctory BLOCKING roster-currency re-verification
that independently confirms BOTH the membership question AND the title question against a live source
BEFORE apply — a genuine two-stage decision point, not a copy-paste formality.

Purpose: Satisfies the government/roster half of SUB-03 and ROADMAP success criterion #1 (govt + council
roster seeded, election method verified). The existing G4110→LOCAL routing map in `essentialsService.ts`
covers all 7 at-large seats with NO code change (no LOCAL_EXEC join is exercised at all this phase — a
genuine simplification vs Marana/Oro Valley), so applying this migration makes every Sahuarita address
resolve to all 7 council members. Plans 02 (headshots) and 03 (stances) consume the 7 UUIDs captured here.
Output: `C:/EV-Accounts/backend/migrations/1354_town_of_sahuarita.sql` (structural, registered), applied
to production, with the 7 UUIDs recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md
@.planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md
@.planning/phases/197-sahuarita-deep-seed/197-CONTEXT.md
@.planning/phases/197-sahuarita-deep-seed/197-VALIDATION.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB access. Task 1 (author the `.sql` file) is the only executor file
action. Task 2 (SUBSTANTIVE BLOCKING roster-currency + title re-verify + apply + UUID capture) and Task 3
(post-apply assertions) are ORCHESTRATOR-RUN via `psql -f`/`psql -tAc` against
`C:/EV-Accounts/backend/.env` `DATABASE_URL`. A migration authored but not applied is a false positive —
the apply MUST happen (and its post-verify DO gate MUST pass) before Plans 02/03. All backend files live in
the SEPARATE `C:/EV-Accounts` repo (`master`→Render) — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (live-verified in 197-RESEARCH.md / 197-PATTERNS.md; the HYBRID analog is Marana
`1345_town_of_marana.sql` for the district/chamber/government shape MINUS its LOCAL_EXEC half, PLUS Palm
Springs `1329_palm_springs_city_council.sql` gate (f) for the title-on-seat modeling — no codebase
exploration needed):
- essentials.governments (id, name, type, state, city, geo_id): insert 'Town of Sahuarita, Arizona, US',
  type='Town' (confirm against the live Marana 1345 row convention at author time; note any deviation),
  state='AZ' (UPPERCASE free-text), city='Sahuarita', geo_id='0462140'. Greenfield. Idempotent WHERE NOT
  EXISTS on name. governments has NO unique constraint on geo_id — guard on name.
- essentials.chambers (id, name, name_formal, government_id, official_count): name='Town Council',
  name_formal='Sahuarita Town Council', official_count=7, government_id via name subquery. The
  auto-generated `slug` column must NEVER appear in the INSERT column list. Idempotent WHERE NOT EXISTS on
  (name, government_id).
- essentials.districts (id, district_type, state, geo_id, label, mtfcc) — exactly ONE NEW row reusing the
  ALREADY-LIVE Phase-190 geofence (NO geometry work): district_type='LOCAL', state='az' (lowercase),
  geo_id='0462140', label='Town of Sahuarita (At-Large)', mtfcc='G4110'. Idempotent WHERE NOT EXISTS on
  (geo_id, district_type, state). ALL 7 offices share THIS SINGLE row. NO LOCAL_EXEC row (Pitfall 3). Use
  `label`, NOT name_formal (no such column).
- essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed,
  is_vacant, is_incumbent, external_id, office_id): ext_id -4014001..-4014007 (DB-confirmed unused
  2026-07-16); external_id UNIQUE — use ON CONFLICT (external_id) DO NOTHING (greenfield — no pre-existing
  rows to update, unlike Marana's DO UPDATE). party=NULL on ALL 7 (nonpartisan). is_active=true,
  is_incumbent=true, is_vacant=false, is_appointed=false on all 7.
- essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state,
  is_appointed_position, is_vacant, role_canonical): representing_state='AZ' (UPPERCASE),
  is_appointed_position=false, role_canonical=NULL on all 7. Guard on (district_id, politician_id).
- ext_id → name → title (RESEARCH roster, 2026-07-16 — RE-VERIFY at Task 2, primary is imminent/past AND
  the title re-vote is a separate event):
    -4014001 Tom Murphy         title='Mayor'          (LOCAL / G4110 / 0462140)  [seat up 2026; Mayor title re-chosen post-canvass]
    -4014002 Kara Egbert        title='Vice Mayor'     (LOCAL / G4110 / 0462140)  [OPEN SEAT — confirmed NOT seeking re-election; confirm at Task 2]
    -4014003 Deborah Morales    title='Council Member' (LOCAL / G4110 / 0462140)  [seat up 2026 — running as incumbent]
    -4014004 Steven Gillespie   title='Council Member' (LOCAL / G4110 / 0462140)  [term to 2028]
    -4014005 Diane Priolo       title='Council Member' (LOCAL / G4110 / 0462140)  [term to 2028]
    -4014006 Kim Lisk           title='Council Member' (LOCAL / G4110 / 0462140)  [term to 2028]
    -4014007 Edgar Lytle        title='Council Member' (LOCAL / G4110 / 0462140)  [term to 2028]
- CRITICAL geo_id collision guard: 0462140 also appears in geofence_boundaries (state stored as FIPS '04',
  an unrelated convention). Every office↔district join MUST scope by district_type='LOCAL' + mtfcc='G4110'
  + state='az', never a bare geo_id.
- One-chamber-one-district precedent (live-applied at 1345 Marana LOCAL half + Torrance): all
  undifferentiated LOCAL offices sharing ONE LOCAL row in a single chamber. Palm Springs 1329 gate (f) is
  the copy-verbatim template for "exactly 1 office titled X bound to external_id Y" — re-scoped here to the
  single shared row (d.geo_id='0462140' AND d.mtfcc='G4110' AND d.state='az'), NOT a LIKE across per-seat
  districts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the structural Town of Sahuarita migration (hybrid: shared LOCAL, NO LOCAL_EXEC, Mayor/VM as titles)</name>
  <files>C:/EV-Accounts/backend/migrations/1354_town_of_sahuarita.sql</files>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md (the full HYBRID structural pattern: the Marana district/chamber/govt shape MINUS the LOCAL_EXEC half, the Palm-Springs title-on-seat modeling + gate (f) verbatim template re-scoped to the single shared row, the plain 'Mayor'/'Vice Mayor'/'Council Member' titles, ON CONFLICT DO NOTHING for greenfield, the required gate set a-g, and the CRITICAL header notes carried from both analogs — slug never in INSERT, no geo_id unique constraint, districts.state lowercase, every join scoped by district_type+mtfcc+state)
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Summary roster table; § Pattern 1 hybrid model + § Anti-Patterns to Avoid — NO LOCAL_EXEC, Egbert not stable VM, nonpartisan party NULL; § Common Pitfalls 3, 5, 6; § Code Examples — the greenfield/geofence/ext_id/ledger probes to re-run at execute time)
    - C:/EV-Accounts/backend/migrations/1345_town_of_marana.sql (PRIMARY district/chamber/govt analog — read in full: government/chamber/district/office INSERT shapes, pre-flight + post-verify DO gates, the office_id backfill, ledger footer. Copy the STRUCTURE, substituting Sahuarita facts, BUT DROP the entire LOCAL_EXEC district row + LOCAL_EXEC Mayor office block — Sahuarita has no separately-elected Mayor)
    - C:/EV-Accounts/backend/migrations/1329_palm_springs_city_council.sql (read gate (f) — the "exactly 1 office titled Mayor / exactly 1 titled Mayor Pro Tem bound to a specific external_id" post-verify gate; replicate that shape for title='Mayor' on -4014001 and title='Vice Mayor' on -4014002, re-scoped to the single shared LOCAL/G4110/0462140 row)
  </read_first>
  <action>
    First run `ls C:/EV-Accounts/backend/migrations | sort -n | tail -5` to confirm on-disk MAX (RESEARCH
    computed disk MAX=1353, ledger MAX=1345, next structural=1354 on 2026-07-16 — but Pitfall 5 documents a
    real ledger-vs-disk drift, so RE-CHECK now). Name this file actual-disk-MAX+1 (provisionally
    `1354_town_of_sahuarita.sql`). If disk MAX drifted, use actual disk-MAX+1 and note the deviation in a
    header comment and the SUMMARY. Also re-run the ext_id collision probe (`SELECT external_id FROM
    essentials.politicians WHERE external_id BETWEEN -4014010 AND -4014000` => expect 0 rows).

    Author a STRUCTURAL migration wrapped in BEGIN/COMMIT. Header comment discipline (copy 1345's shape,
    substitute Sahuarita facts): the geo_id-collision CRITICAL note with `'0462140'`; the districts.state
    must be 'az' lowercase note; governments.state='AZ' / offices.representing_state='AZ' uppercase note;
    at-large / one shared LOCAL row note; party=NULL nonpartisan note; PLUS a Sahuarita-specific
    CRITICAL/DEVIATION note that (a) there is NO LOCAL_EXEC row and NO separately-elected Mayor — Mayor and
    Vice Mayor are council-chosen TITLES modeled purely via the `title` string per Town Code 2.05/2.10
    (Palm-Springs precedent, RESEARCH Pattern 1), and (b) this OVERRODE the CONTEXT D-02 default expectation
    of a LOCAL_EXEC Mayor seat after RESEARCH's 4-source verification.

    Pre-flight DO block: assert the whole-town geofence exists — COUNT(*) >= 1 in
    essentials.geofence_boundaries WHERE geo_id='0462140' AND mtfcc='G4110' — RAISE EXCEPTION 'Sahuarita
    G4110 geofence missing — run Phase 190 first' otherwise. Add the standard idempotency hard-abort guard:
    RAISE EXCEPTION 'Migration 1354 already applied — aborting re-run' if the 'Town of Sahuarita, Arizona,
    US' government already exists.

    Insert the greenfield government 'Town of Sahuarita, Arizona, US' (type='Town', state='AZ' uppercase,
    city='Sahuarita', geo_id='0462140') idempotently (WHERE NOT EXISTS on name). Insert the 'Town Council'
    chamber (name_formal='Sahuarita Town Council', official_count=7) scoped to that government's id via name
    subquery, idempotent on (name, government_id). Do NOT list the auto-generated `slug` column.

    Insert exactly ONE NEW district row reusing the existing Phase-190 geofence (NO geometry work):
    district_type='LOCAL', state='az' lowercase, geo_id='0462140', label='Town of Sahuarita (At-Large)',
    mtfcc='G4110', idempotent WHERE NOT EXISTS on (geo_id, district_type, state). ALL 7 offices share THIS
    SINGLE row. DO NOT insert a LOCAL_EXEC row anywhere (Pitfall 3 — a phantom seat). CRITICAL:
    mtfcc='G4110'.

    Seed 7 officials using the WITH ins_p AS (INSERT INTO politicians ... ON CONFLICT (external_id) DO
    NOTHING RETURNING id) INSERT INTO offices ... FROM districts d CROSS JOIN ins_p p WHERE ... block, one
    per official. EVERY office joins the SAME single LOCAL row: d.geo_id='0462140' AND
    d.district_type='LOCAL' AND d.state='az' AND d.mtfcc='G4110'. Titles:
      - Murphy (-4014001) → title='Mayor'
      - Egbert (-4014002) → title='Vice Mayor'
      - Morales (-4014003) → title='Council Member'
      - Gillespie (-4014004) → title='Council Member'
      - Priolo (-4014005) → title='Council Member'
      - Lisk (-4014006) → title='Council Member'
      - Lytle (-4014007) → title='Council Member'
    All 7 offices: representing_state='AZ' uppercase, is_appointed_position=false, role_canonical=NULL. Each
    join scopes district_type='LOCAL' + mtfcc='G4110' + state='az' plus geo_id — NEVER a bare geo_id. Guard
    each office INSERT on NOT EXISTS (... o.district_id=d.id AND o.politician_id=p.id). Politicians:
    party=NULL on ALL 7 (nonpartisan — do NOT record a party value), is_active=true, is_incumbent=true,
    is_vacant=false, is_appointed=false on all 7. Because greenfield, use ON CONFLICT (external_id) DO
    NOTHING (NOT Marana's DO UPDATE).

    office_id backfill: UPDATE politicians SET office_id=o.id FROM offices o WHERE o.politician_id=p.id AND
    p.external_id IN (-4014001,-4014002,-4014003,-4014004,-4014005,-4014006,-4014007) AND p.office_id IS
    NULL.

    Post-verify DO gate (RAISE EXCEPTION → rollback unless ALL hold): (a) exactly 1 government named 'Town
    of Sahuarita, Arizona, US'; (b) exactly 7 offices under the 'Town Council' chamber, ALL on the ONE
    LOCAL/G4110/0462140 district (NOT a 1+6 split — no LOCAL_EXEC); (c) the LOCAL district holds exactly 7
    offices AND there is NO LOCAL_EXEC district for 0462140; (d) all 7 politicians have party IS NULL
    (nonpartisan gate) AND is_appointed=false; (e) EXACTLY 1 office has title='Mayor' AND its politician has
    external_id=-4014001 (Murphy) — no other office may carry it (Palm-Springs gate (f) shape); (f) EXACTLY
    1 office has title='Vice Mayor' AND its politician has external_id=-4014002 (Egbert) — no other office
    may carry it; (g) section-split: 0 offices attached under any government OTHER than Town of Sahuarita via
    this district; (h) office_id backfill: 0 NULLs remaining across the 7 external_ids.

    Register OUTSIDE BEGIN/COMMIT: INSERT INTO supabase_migrations.schema_migrations (version) VALUES
    ('1354') ON CONFLICT (version) DO NOTHING (substitute actual disk-MAX+1; check whether the live
    convention is the 1-column or 2-column form at author time). Do NOT invent politician UUIDs — offices
    resolve politician_id via the ins_p CTE at apply time.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1354_town_of_sahuarita.sql"; test -f "$F" && grep -qF "Town Council" "$F" && grep -qF "Town of Sahuarita, Arizona, US" "$F" && grep -qF "'LOCAL'" "$F" && ! (grep -v '^[[:space:]]*--' "$F" | grep -qF "'LOCAL_EXEC'") && grep -qF "G4110" "$F" && grep -qF "'0462140'" "$F" && grep -qF "'Mayor'" "$F" && grep -qF "'Vice Mayor'" "$F" && [ "$(grep -c "'Council Member'" "$F")" -ge 5 ] && ! grep -qiE "party[^a-z]*=[^a-z]*'(Democratic|Republican)" "$F" && [ "$(grep -c 'o.district_id = d.id AND o.politician_id = p.id' "$F")" = "7" ] && grep -qF "ON CONFLICT (external_id) DO NOTHING" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1354; deviation noted if disk MAX drifted).
    - Grep confirms `Town Council` and `Town of Sahuarita, Arizona, US` present; government type='Town' (or noted deviation), city='Sahuarita', geo_id='0462140'.
    - Grep confirms a NEW `LOCAL` districts INSERT with geo_id='0462140', mtfcc='G4110', state='az' — and NO `'LOCAL_EXEC'` district_type literal on any non-comment line (Pitfall 3 — the one row Phase 190 never wrote; exactly one district, not two). A header comment documenting the absence is fine — the gate strips `--` comment lines.
    - Grep count of the office↔district guard `o.district_id = d.id AND o.politician_id = p.id` is exactly 7; all 7 join the SAME LOCAL/G4110/0462140 row — never a bare geo_id.
    - Nonpartisan: NO politician row records a 'Democratic'/'Republican' party; `role_canonical` NULL for all 7; is_appointed=false on all 7 politician rows; ON CONFLICT (external_id) DO NOTHING (greenfield, not DO UPDATE).
    - Title modeling: exactly one office title='Mayor' (Murphy -4014001), exactly one title='Vice Mayor' (Egbert -4014002), five plain 'Council Member'; no separate Mayor/Vice-Mayor seat (7 offices total).
    - Header carries the Sahuarita-specific CRITICAL note that there is NO LOCAL_EXEC / no separately-elected Mayor (Mayor/VM are council-chosen titles), overriding the CONTEXT D-02 default.
    - Grep confirms 7 external_id literals -4014001..-4014007.
    - Pre-flight DO block asserts the G4110/0462140 geofence exists; post-verify DO gate present with assertions (a-h) — including LOCAL holds exactly 7, NO LOCAL_EXEC row, party-NULL gate, exactly-one-Mayor on -4014001, exactly-one-Vice-Mayor on -4014002.
    - Ledger-registration line `('1354'` (or actual number) present OUTSIDE COMMIT.
  </acceptance_criteria>
  <done>Structural migration authored: greenfield Town of Sahuarita government + Town Council chamber + 1 NEW shared LOCAL/G4110 district (NO LOCAL_EXEC) + 7 at-large offices/politicians (party NULL, is_appointed=false; Murphy carries the Mayor title, Egbert the Vice Mayor title, five plain 'Council Member'), backfill, pre-flight geofence + post-verify gates (incl. exactly-one-Mayor / exactly-one-Vice-Mayor / no-LOCAL_EXEC), ledger registration — ready to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] SUBSTANTIVE roster + Mayor/VM-title re-verification (July 21 2026 primary — may be past; titles re-voted post-canvass) + apply structural migration (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Summary — the full roster table + the council-chosen-title mechanism; § Common Pitfall 1 (5-day window, Egbert open seat), Pitfall 2 (winning re-election ≠ keeping the Mayor/VM title — Town Code 2.10.010); § Assumptions Log A1/A2; § Open Questions 1 & 3 — how to handle a primary that has already landed + the pending post-canvass title vote)
    - .planning/phases/197-sahuarita-deep-seed/197-VALIDATION.md (Manual-Only Verifications — the roster-currency test: BLOCKING re-verify of BOTH membership AND current Mayor/Vice-Mayor title holders vs sahuaritaaz.gov before apply)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. This is NOT a copy-paste formality — it is the genuine #1 risk of this phase and the shortest research-to-election window in the whole v22.0 milestone, layered with a scheduled post-canvass title re-vote. The operator re-confirms BOTH (a) the 7-person council membership AND (b) the current Mayor and Vice-Mayor title holders against a LIVE source, determines whether the July 21 2026 primary has occurred/been certified AND whether the post-canvass council organizational vote (Town Code 2.10.010) has re-assigned the Mayor/VM titles, and — if anything changed — explicitly decides with the operator whom to seed and which titles to place for each affected seat; only then does the ORCHESTRATOR apply migration 1354 (its in-transaction post-verify DO gate MUST pass — a RAISE EXCEPTION rolls the whole thing back) and capture the 7 UUIDs. See &lt;how-to-verify&gt;.</action>
  <what-built>
    Migration 1354 (Task 1) is authored but NOT applied. RESEARCH (2026-07-16) documented the current
    7-person roster (Murphy Mayor; Egbert Vice Mayor [OPEN SEAT — not seeking re-election]; Morales;
    Gillespie; Priolo; Lisk; Lytle) as valid for effectively 0–5 days because Sahuarita's July 21, 2026
    municipal primary is 5 days from the research date. Egbert's seat is guaranteed to change hands. AND —
    uniquely to this phase — the Mayor and Vice Mayor TITLES are re-chosen by the newly-seated council at
    the first meeting after the canvass (Town Code 2.10.010), a SEPARATE event from the election, so a
    certified primary does not by itself resolve who is Mayor/Vice Mayor. Applying to production (writes are
    LIVE prod) without a fresh two-stage re-check risks seeding a roster or a title assignment that a
    certified primary + post-canvass vote has already superseded (data-honesty threat).
  </what-built>
  <how-to-verify>
    1. Establish the authoritative facts from a LIVE source. sahuaritaaz.gov (CivicPlus) is NOT WAF-blocked
       — a fresh WebFetch/Playwright of sahuaritaaz.gov/274/Town-Council + /265/Election-Information returns
       the roster and dates. Corroborate against the confirmed-reachable Tucson-metro outlets (tucson.com,
       gvnews.com / Green Valley News, tucsonsentinel.com, KGUN9) plus Ballotpedia candidate pages. Confirm
       the July 21, 2026 primary date and the Nov 3, 2026 general date.
    2. Determine (a) whether the primary has OCCURRED and whether results are CERTIFIED as of the execute
       date (an AZ nonpartisan primary can produce an outright >50% winner with no Nov general), AND (b)
       whether the newly-seated council's post-canvass organizational meeting has HELD its Mayor/Vice-Mayor
       re-vote (Town Code 2.10.010). Given the 5-day window, treat "the primary has already happened" as the
       LIKELY branch; the title re-vote may still be pending even after certification.
    3. Decide the roster AND the Mayor/VM titles to seed WITH THE OPERATOR:
       - If the primary has NOT occurred / is not certified AND no new title vote has happened → seed the
         current 7-person roster (Murphy/Egbert/Morales/Gillespie/Priolo/Lisk/Lytle) with Murphy=Mayor,
         Egbert=Vice Mayor documented in RESEARCH — these represent residents today.
       - If a certified primary changed any officeholder (Egbert's open seat WILL resolve to someone new
         regardless) → explicitly decide with the operator whether to seed the outgoing (still-sitting until
         the term rolls over) or the newly-certified incoming member for each affected seat, and patch the
         affected politician rows in 1354 BEFORE apply.
       - If the post-canvass Mayor/VM re-vote has occurred and moved a title → move the `title='Mayor'` /
         `title='Vice Mayor'` annotation to the correct sitting member and UPDATE the post-verify gate's
         expected external_ids (gate (e)/(f)) accordingly. Never assume "Murphy stays Mayor" or "Egbert
         stays Vice Mayor" without confirming the actual vote.
    4. On approval, the ORCHESTRATOR applies via
       `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1354_town_of_sahuarita.sql`, confirms the
       in-transaction post-verify DO gate passed (COMMIT, no RAISE EXCEPTION), captures the 7 UUIDs
       (`SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id IN
       (-4014001,-4014002,-4014003,-4014004,-4014005,-4014006,-4014007) ORDER BY external_id`) into
       197-01-SUMMARY.md for Plans 02/03, records the execute-date roster + title decision (before/after
       primary, before/after the post-canvass title vote, what was seeded and why, per-seat), and commits
       via `git -C "C:/EV-Accounts"`.
  </action>
  <acceptance_criteria>
    - The authoritative primary date + general date + certification status AND the status of the post-canvass Mayor/VM title re-vote are established from a LIVE source at execute time (not assumed from RESEARCH), and recorded in 197-01-SUMMARY.md.
    - BOTH the seeded membership AND the Mayor/Vice-Mayor title holders are explicitly decided with the operator: current 7 + Murphy/Egbert titles if pre/uncertified, or the operator-chosen outgoing/incoming members + confirmed title holders per seat if a certified result and/or a post-canvass vote changed anything (Egbert's open seat resolved deliberately).
    - Migration applies with the in-transaction post-verify DO gate passing (no RAISE EXCEPTION → COMMIT), including party-NULL, LOCAL-holds-7, no-LOCAL_EXEC, exactly-one-Mayor and exactly-one-Vice-Mayor on the confirmed holders.
    - `psql -tAc "SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1354'"` returns 1 (structural registered; substitute actual number if disk MAX drifted).
    - The 7 politician UUIDs captured into 197-01-SUMMARY.md (external_id → UUID → full_name) for Plans 02/03; migration committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live roster AND the current Mayor/Vice-Mayor title holders + election dates + certification status + post-canvass-title-vote status are confirmed, the seed roster and title placements are explicitly decided per seat, and the migration is applied + gate-passed + UUIDs captured — or describe the roster/title delta to patch first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply structural assertions (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB assertions, no files modified</files>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-VALIDATION.md (Per-Task Verification Map + Wave 0 requirements — geo_id 0462140 resolves; greenfield; 1 govt + 1 chamber + 7 offices, party NULL, title annotations, scoped joins, 0 section-split)
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Common Pitfall 3 — the ONE NEW LOCAL row, NO LOCAL_EXEC; § Security Domain — section-split + wrong-district-attach + phantom-LOCAL_EXEC mitigations)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). After apply, run seed assertions via `psql "$DATABASE_URL"
    -tAc` and record in 197-01-SUMMARY.md. Assert: (a) exactly 1 government 'Town of Sahuarita, Arizona,
    US' at geo_id='0462140'; (b) exactly 7 offices under the 'Town Council' chamber of that government, ALL
    on the ONE LOCAL/G4110/0462140 district; (c) the LOCAL district holds exactly 7 offices AND there is NO
    LOCAL_EXEC district row for geo_id='0462140'; (d) exactly ONE new districts row for 0462140 (LOCAL,
    G4110, az); (e) all 7 politicians have party IS NULL AND is_appointed=false; (f) DISTINCT district state
    on the linked district = only 'az' (lowercase); (g) section-split: 0 offices reachable from this
    district under any government other than Town of Sahuarita; (h) exactly 1 office title='Mayor' and it is
    -4014001 (Murphy, or the Task-2-confirmed holder) AND exactly 1 office title='Vice Mayor' and it is
    -4014002 (Egbert, or the Task-2-confirmed holder), and no other office carries either title. Any
    mismatch blocks the phase — do NOT proceed to Plan 02.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='Town of Sahuarita, Arizona, US' AND geo_id='0462140')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0462140' AND d.state='az' AND d.mtfcc='G4110' AND d.district_type='LOCAL')=7 AND (SELECT count(*) FROM essentials.districts WHERE geo_id='0462140' AND district_type='LOCAL_EXEC')=0 AND (SELECT count(*) FROM essentials.districts WHERE geo_id='0462140' AND district_type='LOCAL' AND state='az' AND mtfcc='G4110')=1 AND NOT EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id IN (-4014001,-4014002,-4014003,-4014004,-4014005,-4014006,-4014007) AND (party IS NOT NULL OR is_appointed IS TRUE)) AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0462140' AND d.mtfcc='G4110' AND d.state='az' AND o.title='Mayor')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0462140' AND d.mtfcc='G4110' AND d.state='az' AND o.title='Vice Mayor')=1"</automated>
  </verify>
  <acceptance_criteria>
    - Greenfield Town of Sahuarita government count = 1 (geo_id 0462140).
    - 7 offices under 'Town Council', ALL on the ONE LOCAL/G4110/0462140 row.
    - LOCAL district holds exactly 7 offices; there are ZERO LOCAL_EXEC districts for 0462140 (Pitfall 3).
    - Exactly one new districts row for 0462140 (LOCAL/G4110/az).
    - party IS NULL AND is_appointed=false for all 7 politicians (nonpartisan).
    - DISTINCT linked-district state = only 'az'.
    - Section-split query returns 0 rows (no office leaked under a non-Sahuarita government).
    - Exactly 1 office title='Mayor' (Murphy -4014001 / Task-2 holder) and exactly 1 title='Vice Mayor' (Egbert -4014002 / Task-2 holder); no other office carries either.
    - The combined boolean SELECT returns `t`.
  </acceptance_criteria>
  <done>Live production confirms the greenfield Town of Sahuarita government, 7 at-large offices on the ONE shared LOCAL district (no LOCAL_EXEC), party NULL / is_appointed false, exactly one Mayor and one Vice Mayor title annotation, lowercase 'az' casing, clean section-split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | Hand-authored static seed SQL crosses into live `essentials` via `psql -f` (service-role) |
| Phase-190 G4110/0462140 geofence → new offices | The 7 new office↔district joins consume the existing whole-town geofence; wrong casing/type or a bare-geo_id collision (0462140 also exists FIPS-keyed) silently mislinks |
| active 2026 election + post-canvass title vote → seeded roster | The July 21 2026 primary (5 days from research; likely already past at execute) can supersede membership; 1 seat is a confirmed open seat; and the Mayor/VM titles are re-voted after the canvass (a separate event) |

## STRIDE Threat Register

Honest scope: a static data-seed migration authored by the operator — no new untrusted input, auth, or
network endpoint. Genuine threats are data-integrity (correct linkage / current roster / correct titles),
not injection or access control. No HIGH-severity threat exists; none is fabricated. Mirrors the Marana
196 threat model.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-01 | Tampering | office↔chamber↔government section-split | mitigate | Chamber scoped to the Sahuarita government id subquery; post-verify DO gate (g) + Task 3 section-split assertion → 0 rows or the transaction rolls back |
| T-197-02 | Tampering | office↔district WHERE vs the 0462140 multi-use geo_id | mitigate | Every join scopes district_type='LOCAL' + mtfcc='G4110' + state='az'; migration never joins a bare geo_id; post-verify asserts 7 LOCAL offices on the one row |
| T-197-03 | Tampering (data integrity) | phantom LOCAL_EXEC row (misrepresenting a non-existent separately-elected Mayor office) | mitigate | This migration creates NO LOCAL_EXEC row (Pitfall 3 / Pattern 1); post-verify gate (c) + Task 3 assert 0 LOCAL_EXEC districts for 0462140 and all 7 on the single LOCAL row |
| T-197-04 | Tampering (data-honesty) | seeding a roster or title assignment superseded by the July 21 2026 primary and/or the post-canvass Mayor/VM re-vote — the #1 risk | mitigate | Task 2 is a SUBSTANTIVE BLOCKING two-stage re-verify against a LIVE source (membership AND current Mayor/VM title holders) + certified-primary + post-canvass-vote check + explicit per-seat/per-title operator decision; execute-date decision recorded in the SUMMARY |
| T-197-TITLE | Tampering | Mayor/Vice-Mayor title modeling (double-count / mislabel / stale annotation) | mitigate | Title-only model: exactly 7 offices (no separate 8th/9th seat), title='Mayor' on Murphy's -4014001 seat and title='Vice Mayor' on Egbert's -4014002 seat ONLY (post-verify gate (e)/(f) + Task 3 assert exactly one each); Task 2 re-confirms both title holders before apply |
| T-197-PARTY | Information disclosure | recording a party on a nonpartisan office | mitigate | party=NULL on all 7 (antipartisan; Ballotpedia At-large naming confirms nonpartisan); post-verify gate (d) + Task 3 assert 0 non-NULL party |
| T-197-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `C:/EV-Accounts/backend/.env`; applied by the inline orchestrator; never hardcoded in the migration |
| T-197-SC | Tampering | package installs | accept | No new npm/pip/cargo installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 1 greenfield Town of Sahuarita government (geo_id 0462140).
- 1 'Town Council' chamber; 7 at-large offices ALL on the ONE shared LOCAL/G4110 district; NO LOCAL_EXEC row.
- party NULL / is_appointed false on all 7; Mayor + Vice Mayor as title annotations (exactly one each); five plain 'Council Member'; no separate Mayor/VM seat.
- Section-split 0 rows; casing lowercase 'az' on the district; structural migration registered.
- Roster AND Mayor/VM title holders re-verified against a live source at execute time (July 21 primary + post-canvass title vote decision recorded); 7 politician UUIDs captured for Plans 02/03.
</verification>

<success_criteria>
The government/roster half of SUB-03 and ROADMAP #1 is TRUE in production (7 at-large council members with
Mayor/Vice-Mayor as title annotations, election method verified, at-large routing enabled via the one new
LOCAL district row); the migration is applied + registered; the UUID manifest and execute-date roster +
title decision are recorded.
</success_criteria>

<output>
Create `.planning/phases/197-sahuarita-deep-seed/197-01-SUMMARY.md` when done — include the 7 external_id →
UUID → full_name manifest (required by Plans 02 and 03), the authoritative primary/general dates +
certification status + post-canvass-title-vote status established at execute time, the explicit per-seat
roster decision (esp. Egbert's open seat) and the confirmed Mayor/Vice-Mayor title holders.
</output>
</content>
</invoke>
