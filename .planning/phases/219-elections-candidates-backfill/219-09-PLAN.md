---
phase: 219-elections-candidates-backfill
plan: 09
type: execute
wave: 3
depends_on: [219-02, 219-03, 219-04, 219-05, 219-06, 219-07, 219-08]
files_modified:
  - C:/EV-Accounts/backend/scripts/_verify-219-coverage.ts
  - .planning/phases/219-elections-candidates-backfill/219-COVERAGE.md
autonomous: false
requirements: [COLLIN-ELECT-01, COLLIN-ELECT-02, COLLIN-ELECT-03]
user_setup: []

must_haves:
  truths:
    - "All 12 zero-race governments now have ≥1 seeded race with ≥1 candidate, or a documented legitimately-empty reason for every remaining race-less office (COLLIN-ELECT-01)."
    - "All 12 thin governments' race-vs-office coverage is reconciled against RESEARCH's per-city table, with a documented reason for any office left race-less (COLLIN-ELECT-02)."
    - "The split-section check returns 0 rows and no seeded race renders as a zero-candidate shell across all 24 governments (COLLIN-ELECT-03)."
    - "Antipartisan holds (0 non-NULL primary_party on any phase-seeded race) and no stances were written as a side effect (inform.* row counts unchanged across the whole phase)."
    - "A human operator browse spot-check of one government per tier (Blue Ridge / McKinney / Longview) confirms real races render."
  artifacts:
    - "C:/EV-Accounts/backend/scripts/_verify-219-coverage.ts"
    - ".planning/phases/219-elections-candidates-backfill/219-COVERAGE.md"
  key_links:
    - "The 24-government race-count-vs-office-count roll-up → the phase-gate pass/fail decision."
  prohibitions:
    - statement: "No new seeding in this plan — it is verification + documentation only."
      status: enforced
      verification: "This plan authors a read-only verify script + a markdown roll-up; no INSERT/UPDATE migration."
    - statement: "A documented legitimately-empty office (no election found in a reasonable cycle) is not counted as a defect."
      status: enforced
      verification: "COVERAGE.md lists each remaining race-less office with its cited reason; the gate treats those as expected."
    - statement: "candidate_status legality, city-prefixed position_name, and NULL primary_party hold phase-wide."
      status: enforced
      verification: "verify script asserts 0 illegal candidate_status, 0 non-NULL primary_party, and per-election position_name uniqueness across all phase election_ids."
  backstop:
    - statement: "Success Criterion #4 (a resident sees their actual current/next race) is confirmed by a human browse spot-check across all three tiers — not machine-verifiable."
      verification: backstop
---

<objective>
Prove Phase 219 is complete: run the 24-government coverage sweep across every tier's seeded data, confirm no split-section defects / zero-candidate shells / antipartisan or stance-side-effect regressions, document every legitimately-empty remaining office, and get a human browse spot-check across the three tiers.

Purpose: The phase gate for COLLIN-ELECT-01/02/03. Consolidates the per-migration gates into a single authoritative roll-up.
Output: `_verify-219-coverage.ts` (reusable roll-up) + `219-COVERAGE.md` (the documented per-government result).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-elections-candidates-backfill/219-RESEARCH.md
@.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md
@.planning/phases/219-elections-candidates-backfill/219-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 24-government coverage roll-up script + COVERAGE.md scaffold</name>
  <read_first>
    - 219-RESEARCH.md §"Validation Architecture" (the exact race-count, split-section, zero-candidate-shell, antipartisan, and no-stances-side-effect SQL) and §"Sampling Rate" phase-gate list.
    - 219-VALIDATION.md §"Wave 0 Requirements" (phase-close roll-up query) + §"Manual-Only Verifications".
    - C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts (the split-section query verbatim) and _verify-collin-218-completeness.ts (prior phase-completeness roll-up shape to mirror).
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/scripts/_verify-219-coverage.ts (read-only; no writes) that runs and reports, exiting non-zero on any hard failure: (a) for all 24 governments (12 zero-race + 12 thin geo_ids) a race-count-vs-office-count roll-up; (b) the project-standard split-section query (0 rows required); (c) zero-candidate-shell check — `SELECT r.id FROM essentials.races r LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE rc.id IS NULL AND r.election_id IN (<this phase's election_ids>)` must be 0 rows; (d) antipartisan — 0 non-NULL primary_party on any phase-seeded race; (e) candidate_status legality — 0 rows NOT IN ('active','withdrawn','filed') across phase races; (f) no-stances-side-effect — inform.politician_answers + inform.politician_context row counts match the values recorded before Wave 2 began (from the per-migration before/after captures). Then write a `.planning/phases/219-elections-candidates-backfill/219-COVERAGE.md` scaffold: one row per government with race count, office count, and a cited reason for any race-less office (legitimately-empty vs still-open). Do not seed any data.
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/_verify-219-coverage.ts 2>&1 | head; test -f "C:/Transparent Motivations/essentials/.planning/phases/219-elections-candidates-backfill/219-COVERAGE.md"</automated>
  </verify>
  <acceptance_criteria>
    - `_verify-219-coverage.ts` exists, type-checks, is read-only, and covers checks (a)-(f).
    - `219-COVERAGE.md` scaffold exists with a row per government.
  </acceptance_criteria>
  <done>The reusable coverage roll-up + documentation scaffold exist, ready for the operator to run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the coverage sweep + browse spot-check across all three tiers</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_verify-219-coverage.ts.
    - 219-VALIDATION.md §"Manual-Only Verifications".
  </read_first>
  <action>Operator-run checkpoint: run `_verify-219-coverage.ts`, confirm checks (a)-(f) exit 0, fill 219-COVERAGE.md with each remaining race-less office's cited reason, and browse-spot-check Blue Ridge/McKinney/Longview on /results — per the numbered how-to-verify steps.</action>
  <what-built>The final phase gate: the operator runs the read-only roll-up (executor has no DB access) and does the resident's-eye browse check the machine cannot.</what-built>
  <how-to-verify>
    1. `cd C:/EV-Accounts/backend && npx tsx scripts/_verify-219-coverage.ts` — confirm checks (a)-(f) all pass (exit 0). Paste the 24-government roll-up into 219-COVERAGE.md and fill each remaining race-less office's cited reason (legitimately-empty vs still-open).
    2. Browse spot-check one government per tier and confirm the Elections section shows real races + candidates:
       - Shared-election tier: `/results?browse_geo_id=4808872` (Blue Ridge)
       - Fallback-year tier: `/results?browse_geo_id=4845744` (McKinney)
       - Runoff-closure tier: `/results?browse_geo_id=4843888` (Longview)
    3. Confirm no government shows an empty Elections section unless COVERAGE.md documents it as legitimately-empty.
  </how-to-verify>
  <acceptance_criteria>
    - `_verify-219-coverage.ts` exits 0: all 12 zero-race govs have ≥1 race (or documented-empty); thin coverage reconciled; split-section 0 rows; 0 zero-candidate shells; 0 non-NULL primary_party; 0 illegal candidate_status; inform.* unchanged.
    - 219-COVERAGE.md completed with every race-less office's cited reason.
    - Browse spot-check of Blue Ridge, McKinney, Longview each shows real races (backstop: human visual).
  </acceptance_criteria>
  <resume-signal>Type "approved" once the roll-up is clean and all three browse checks pass, or describe the gap.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `C:/EV-Accounts/backend/scripts/_verify-219-coverage.ts` (new — reusable read-only phase-gate roll-up)
- `.planning/phases/219-elections-candidates-backfill/219-COVERAGE.md` (new — documented per-government coverage result)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| production essentials schema → verify script | Read-only; no writes cross back |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-05 | Data integrity (silent loss) | phase-wide race coverage | high | mitigate | 24-government roll-up catches any missing race the per-migration gates scoped-out |
| T-219-04 | Integrity (scope violation) | inform.* tables | high | mitigate | Phase-wide before/after row-count assertion (D-07) |
| T-219-01 | Information disclosure | documented-empty vs defect | medium | mitigate | Every race-less office carries a cited reason in COVERAGE.md; no fabricated fill |
| T-219-SC | Tampering | npm/pip installs | low | accept | No package installs; legitimacy gate N/A |
</threat_model>

<verification>
- `_verify-219-coverage.ts` exits 0 across checks (a)-(f).
- 219-COVERAGE.md documents every remaining race-less office with a cited reason.
- Human browse spot-check of Blue Ridge / McKinney / Longview approved.
</verification>

<success_criteria>
All four ROADMAP success criteria hold: 12 zero-race govs raced (or documented-empty), thin coverage backfilled, no split-section/zero-candidate-shell defects, and a resident browsing any of the 24 governments sees their real race.
</success_criteria>

<output>
Create `.planning/phases/219-elections-candidates-backfill/219-09-SUMMARY.md` when done.
</output>
