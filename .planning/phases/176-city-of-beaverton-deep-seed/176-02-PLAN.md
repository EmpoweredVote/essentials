---
phase: 176-city-of-beaverton-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["176-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1131_beaverton_city_council.sql
autonomous: false
requirements: [WASH-02]

must_haves:
  truths:
    - "A single 'City of Beaverton, Oregon, US' government row exists"
    - "A 'City Council' chamber exists with official_count=7"
    - "Exactly 1 LOCAL_EXEC + 1 LOCAL district exist for geo_id 4105350, state='or'"
    - "7 offices exist (Mayor on LOCAL_EXEC + 6 Councilors on LOCAL), each linked to a Beaverton politician"
    - "Section-split scan returns 0 rows for the Beaverton G4110 geofence"
    - "All 7 Beaverton politicians have a non-NULL office_id back-pointer"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1131_beaverton_city_council.sql"
      provides: "Structural seed: government + chamber + 2 districts + 7 offices + back-fill + post-verify DO block + ledger entry"
      contains: "City of Beaverton, Oregon, US"
      min_lines: 200
  key_links:
    - from: "offices"
      to: "districts (geo_id 4105350, state='or')"
      via: "district_id FK, guard on (district_id, politician_id)"
      pattern: "district_type IN \\('LOCAL', 'LOCAL_EXEC'\\)"
    - from: "politicians"
      to: "offices"
      via: "office_id back-fill UPDATE"
      pattern: "SET office_id = o.id"
---

<objective>
Author the single structural migration `1131_beaverton_city_council.sql` that seeds the City of
Beaverton government, its City Council chamber, the two at-large districts (Mayor LOCAL_EXEC +
councilors LOCAL), and all 7 offices (Mayor Lacey Beaty + Councilors Positions 1–6), with an
in-migration post-verification DO block and a ledger entry. This is a direct copy of the Gresham
block in migration 246 (RESEARCH confirmed Beaverton is structurally identical: council-manager,
directly-elected Mayor, 6 at-large numbered-position councilors, NO wards).

Purpose: Satisfy WASH-02 Success Criterion 1 (any Beaverton address returns Mayor + council) and
2 (full seated roster, correct office structure, seat count).
Output: `1131_beaverton_city_council.sql` (registered structural migration), applied and verified
by the orchestrator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-RESEARCH.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-01-SUMMARY.md

<interfaces>
<!-- Confirmed schema shapes — executor uses these directly, no codebase exploration needed. -->
essentials.governments  (id, name, type, state, city, geo_id)   -- slug is GENERATED — NEVER insert; NO unique constraint on geo_id → WHERE NOT EXISTS on name
essentials.chambers     (id, name, name_formal, government_id, official_count)  -- slug GENERATED — never insert
essentials.districts    (id, district_type, state, geo_id, label, mtfcc)        -- NO name_formal; government_id NULL for OR rows; NO unique constraint → WHERE NOT EXISTS
essentials.politicians  (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id, office_id)  -- ON CONFLICT (external_id)
essentials.offices      (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)  -- guard NOT EXISTS (district_id, politician_id)
supabase_migrations.schema_migrations (version)  -- ledger; structural migrations register here
</interfaces>

**EXECUTION ARCHITECTURE:** Executor WRITES `1131_...sql` only. Orchestrator applies via
`psql "$DATABASE_URL" -f migrations/1131_beaverton_city_council.sql` and commits it in the
C:/EV-Accounts repo (`git -C "C:/EV-Accounts"`). Executor has NO DB.

**VERIFY-GATE HYGIENE:** keep the literal `slug` and `photo_origin_url` strings out of comments.
The token `schema_migrations` legitimately appears in the ledger INSERT — that is allowed; do not
add it to prose comments elsewhere.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 1131_beaverton_city_council.sql (structural seed)</name>
  <files>C:/EV-Accounts/backend/migrations/1131_beaverton_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql — Gresham block (pre-flight guard, gov/chamber/districts/office INSERTs, office_id back-fill tail, post-verify DO block, ledger entry)
    - .planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md §"1131_beaverton_city_council.sql" (line-anchored excerpts for every block)
    - .planning/phases/176-city-of-beaverton-deep-seed/176-RESEARCH.md §"Live Roster — VERBATIM", §"Schema Shapes", §"OR Casing Rules"
    - .planning/phases/176-city-of-beaverton-deep-seed/176-01-SUMMARY.md (confirmed ext_id block + migration number)
  </read_first>
  <action>
    Copy the Gresham block from migration 246 and adapt to Beaverton. Wrap in BEGIN…COMMIT. Structure:
    (1) Pre-flight hard-abort DO block: RAISE EXCEPTION if a 'City of Beaverton, Oregon, US' government
    already exists. (2) Government INSERT: name='City of Beaverton, Oregon, US', type='LOCAL',
    state='OR' (UPPERCASE), city='Beaverton', geo_id='4105350', via WHERE NOT EXISTS on name — do NOT
    insert slug. (3) Chamber INSERT: name='City Council', name_formal='Beaverton City Council',
    government_id from subquery, official_count=7, WHERE NOT EXISTS on (name, government_id) — no slug.
    (4) Two district INSERTs, both state='or' (LOWERCASE), geo_id='4105350', mtfcc=NULL, WHERE NOT
    EXISTS on (geo_id, district_type, state): one LOCAL_EXEC label='Beaverton (Mayor, Citywide)', one
    LOCAL label='Beaverton (At-Large)'. (5) Mayor office block (CTE ins_p → offices): politician Lacey
    Beaty, ext_id -4105351, party NULL, is_active=true, is_appointed=false, is_incumbent=true, ON
    CONFLICT (external_id) DO UPDATE SET is_active; office on the LOCAL_EXEC district, title='Mayor',
    representing_state='OR' (UPPERCASE), is_appointed_position=false, role_canonical=NULL, office guard
    NOT EXISTS (district_id, politician_id). (6) Six councilor office blocks on the LOCAL district,
    title='Council Member (Position N)', representing_state='OR': Pos 1 Ashley Hartmeier-Prigg -4105352;
    Pos 2 Kevin Teater -4105353; Pos 3 Edward Kimmi -4105354 (Council President is a rotational title —
    ONE office row only, note in comment, do NOT create a second office); Pos 4 Allison Tivnon -4105355;
    Pos 5 John Dugger -4105356; Pos 6 Nadia Hasan -4105357. All party NULL. (7) office_id back-fill:
    UPDATE politicians SET office_id=o.id ... WHERE external_id IN (all 7) AND office_id IS NULL.
    (8) Post-verification DO block with four gates raising EXCEPTION on mismatch: gov_count=1;
    office_count=7 (join offices→districts on geo_id 4105350, district_type IN LOCAL/LOCAL_EXEC,
    state='or'); section-split=0 (G4110 geofence for 4105350 has matching LOCAL+LOCAL_EXEC districts);
    office_id NULLs=0 across the 7 ext_ids. (9) Ledger INSERT: version='1131' ON CONFLICT DO NOTHING.
    Use the confirmed ext_id block and migration number from 176-01-SUMMARY (default -4105351..-4105357
    and 1131 unless Wave-0 shifted them). Every casing value exactly as specified — wrong district casing
    silently breaks routing.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies with `psql "$DATABASE_URL" -f migrations/1131_beaverton_city_council.sql`; the in-migration post-verify DO block raises EXCEPTION on any gate failure (self-verifying apply)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/1131_beaverton_city_council.sql
    - Government INSERT uses WHERE NOT EXISTS on name; no slug column in gov or chamber INSERT
    - Chamber official_count=7, name='City Council', name_formal='Beaverton City Council'
    - Exactly 2 district INSERTs, both state='or' lowercase, mtfcc=NULL
    - 7 politician/office blocks with ext_ids -4105351..-4105357 (or Wave-0-shifted block); Mayor on LOCAL_EXEC, 6 councilors on LOCAL
    - representing_state='OR' uppercase on all offices; party NULL on all politicians
    - Exactly one office row for Kimmi (Position 3) — no separate Council President office
    - Post-verify DO block has 4 gates (gov=1, office=7, split=0, office_id nulls=0)
    - Ledger INSERT of version='1131' present; wrapped in BEGIN…COMMIT
    - Literal strings `slug` and `photo_origin_url` do NOT appear in comments
  </acceptance_criteria>
  <done>Structural migration authored; applies cleanly with post-verify DO block passing all 4 gates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies + audits + commits the structural migration</name>
  <what-built>1131_beaverton_city_council.sql — the full Beaverton structural seed with self-verifying post-verify DO block.</what-built>
  <how-to-verify>
    1. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1131_beaverton_city_council.sql`. The in-migration DO block must print "Post-verification PASSED" and raise NO exception.
    2. Independent audit: count offices joined to Beaverton chamber = 7; confirm Mayor Beaty on a LOCAL_EXEC district and 6 councilors on the LOCAL district; confirm districts.state='or' for both rows.
    3. Section-split scan: 0 rows for geo_id 4105350 G4110.
    4. Confirm ledger MAX is now 1131.
    5. Commit in the backend repo: `git -C "C:/EV-Accounts" add backend/migrations/1131_beaverton_city_council.sql && git -C "C:/EV-Accounts" commit` (deploys to Render on push to master).
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied with post-verify DO block passing (no exception)
    - Independent office count = 7; Mayor on LOCAL_EXEC; 6 councilors on LOCAL; both districts state='or'
    - Section-split = 0 rows
    - Ledger MAX = 1131
    - Migration committed in C:/EV-Accounts
  </acceptance_criteria>
  <resume-signal>Type "approved" with the audit counts, or describe the failing gate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Static structural SQL applied by the orchestrator against production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-01 | Tampering | Duplicate/corrupt seed on re-run | mitigate | Pre-flight hard-abort DO block + WHERE NOT EXISTS on gov/chamber/districts + office guard on (district_id, politician_id) + post-verify DO block with 4 gates |
| T-176-03 | Denial of Service | Wrong district casing silently drops routing | mitigate | districts.state='or' lowercase enforced per PATTERNS trap #1; post-verify section-split gate = 0 catches orphaned geofence |
| T-176-04 | Repudiation | Migration number collision / ledger drift | mitigate | Wave-0 probe D confirmed ledger MAX=1130; ledger INSERT version='1131' ON CONFLICT DO NOTHING |
| T-176-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- In-migration post-verify DO block passes (gov=1, offices=7, split=0, office_id nulls=0).
- Independent post-apply audit confirms 7 offices, correct district types, casing, section-split=0.
</verification>

<success_criteria>
- Single Beaverton government + City Council chamber (official_count=7) + 2 at-large districts +
  7 offices seeded; Mayor sorts first (LOCAL_EXEC); section-split=0; ledger MAX=1131; committed.
</success_criteria>

<output>
Create `.planning/phases/176-city-of-beaverton-deep-seed/176-02-SUMMARY.md` when done, recording
the minted politician UUIDs per ext_id (needed by plans 03 and 04).
</output>
