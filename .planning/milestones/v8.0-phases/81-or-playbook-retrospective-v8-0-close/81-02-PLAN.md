---
phase: 81-or-playbook-retrospective-v8-0-close
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "ROADMAP.md marks v8.0 Oregon as shipped (✅ emoji + shipped date) — visible at the top milestone list AND in the v8.0 details summary tag"
    - "ROADMAP.md Phase 81 progress table row shows 2/2 plans Complete with date 2026-05-31"
    - "ROADMAP.md Phase 81 plans list shows 81-01-PLAN.md AND 81-02-PLAN.md as [x] complete"
    - "STATE.md last_activity reflects Phase 81 completion and v8.0 milestone shipped"
    - "STATE.md Current Position section shows Phase 81 complete"
    - "PROJECT.md Validated list contains a v8.0 block of bullets summarizing OR TIGER, OR government DB, Portland deep seed, 2026 elections + discovery, compass stances, and playbook retrospective"
    - "PROJECT.md no longer shows v8.0 Oregon under 'Current Milestone' / 'Active' (the section is closed; next milestone heading placeholder or v9.0 reference takes its place)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "v8.0 milestone closed (✅ shipped 2026-05-31); Phase 81 marked Complete in progress table; both Phase 81 plans marked [x]; v8.0 <details> summary updated"
      contains: "shipped 2026-05-31"
    - path: ".planning/STATE.md"
      provides: "last_activity updated to reflect Phase 81 complete and v8.0 shipped; Current Position section refreshed"
      contains: "Phase 81 complete"
    - path: ".planning/PROJECT.md"
      provides: "v8.0 Oregon capabilities moved from 'Current Milestone / Active' section into the 'Validated' list as 5-6 bullets summarizing what shipped"
      contains: "— v8.0"
  key_links:
    - from: ".planning/ROADMAP.md milestone list (top)"
      to: ".planning/ROADMAP.md v8.0 details block"
      via: "both must show ✅ shipped state consistently"
      pattern: "v8.0 Oregon.*shipped"
    - from: ".planning/ROADMAP.md Phase 81 progress table row"
      to: ".planning/ROADMAP.md Phase 81 plans list"
      via: "both indicate Phase 81 2/2 complete on 2026-05-31"
      pattern: "81.*Complete.*2026-05-31|81-01-PLAN.md.*\\[x\\]"
    - from: ".planning/STATE.md last_activity"
      to: ".planning/STATE.md Current Position"
      via: "both reflect Phase 81 complete + v8.0 shipped"
      pattern: "Phase 81 complete"
    - from: ".planning/PROJECT.md Validated list"
      to: "LOCATION-ONBOARDING.md Oregon Quick Reference"
      via: "validated bullet references the playbook update delivered by Plan 01"
      pattern: "OR Playbook retrospective"
---

<objective>
Close the v8.0 Oregon milestone across the three planning files. This finalizes Phase 81 and the v8.0 milestone after Plan 01 lands the OR playbook content. Mirrors the Phase 78-02 (CA milestone close) execution pattern exactly.

Purpose: Plan 01 delivers the OR playbook deliverable; this plan reflects that delivery in the planning files so downstream tooling (STATE.md context loaders, ROADMAP-driven progress views, PROJECT.md validated-features lists) all show v8.0 as shipped. Without this plan, the playbook update would land but the planning files would still show v8.0 as in-progress — creating confusion about milestone state for the next planning session.

Output: Three planning files updated. ROADMAP.md has the v8.0 milestone marked shipped at both the top milestone list (line 15) and the v8.0 <details> summary tag (line 558), Phase 81 progress row updated (line 887), and both Phase 81 plan checkboxes marked. STATE.md has last_activity updated and Current Position section refreshed. PROJECT.md has the v8.0 Oregon section closed (Active checklist replaced or removed) with 5-6 bullets summarizing OR capabilities shipped appended to Validated.
</objective>

<execution_context>
@C:\Users\Chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-or-playbook-retrospective-v8-0-close/81-RESEARCH.md

# Phase 78 analog: the previous milestone-close used the same edit approach
@.planning/phases/78-ca-playbook-retrospective/78-02-PLAN.md
@.planning/phases/78-ca-playbook-retrospective/78-02-SUMMARY.md

# Plan 01 must have completed and produced this summary; reference for "playbook deliverable" wording in PROJECT.md
# @.planning/phases/81-or-playbook-retrospective-v8-0-close/81-01-SUMMARY.md (produced by Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark v8.0 Oregon milestone shipped in ROADMAP.md (4 edits)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (file being edited — line 15 has the milestone list entry; line 558 has the v8.0 &lt;details&gt; summary tag; line 887 has the Phase 81 progress table row; lines 754-764 have the Phase 81 section with `**Plans**: TBD`)
    - .planning/phases/81-or-playbook-retrospective-v8-0-close/81-RESEARCH.md (lines 216-224 describe the exact ROADMAP edits required — top milestone list, v8.0 details summary tag, Phase 81 plans, progress table row)
    - .planning/phases/78-ca-playbook-retrospective/78-02-PLAN.md (canonical pattern — confirms the 4-edit structure: top milestone, details summary, Phase plans list, progress table row)
    - .planning/phases/78-ca-playbook-retrospective/78-02-SUMMARY.md (canonical execution log — confirms the 4 edits applied successfully for CA)
  </read_first>
  <action>
Use the Edit tool to make 4 targeted edits to .planning/ROADMAP.md. Do NOT use Write.

**Edit 1: Top milestone list (line 15).** Replace the current v8.0 milestone line:
`- 🚧 **v8.0 Oregon** — Phases 72-81 (in progress)`
with:
`- ✅ **v8.0 Oregon** — Phases 72-81 (shipped 2026-05-31)`

The emoji change is from 🚧 (construction) to ✅ (white-heavy-check-mark). Use the exact Unicode emoji characters — match the existing v7.0 line at line 14 (`- ✅ **v7.0 California** — Phases 57-70, 78 (shipped 2026-05-29)`) for the format.

**Edit 2: v8.0 details summary tag (line 558).** Replace:
`<summary>🚧 v8.0 Oregon (Phases 72-81) — IN PROGRESS</summary>`
with:
`<summary>✅ v8.0 Oregon (Phases 72-81) — SHIPPED 2026-05-31</summary>`

**Edit 3: Phase 81 plans list (around line 763-764 in the Phase 81 section).** The current section reads:
```
### Phase 81: OR Playbook Retrospective + v8.0 Close
...
**Plans**: TBD
```

Replace `**Plans**: TBD` with `**Plans**: 2 plans` and add a Plans list block immediately after (matching the format used by Phase 78 at lines 545-553):
```
Plans:
**Wave 1**

- [x] 81-01-PLAN.md — Playbook update: 2 Cities Onboarded rows (Oregon state + Portland) + Oregon Quick Reference + 9 OR-specific GOTCHAs inline + 4+ Step 7 pitfall rows

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 81-02-PLAN.md — Close v8.0 Oregon milestone across ROADMAP.md, STATE.md, PROJECT.md
```

This mirrors the Phase 78 Wave 1 / Wave 2 split pattern and marks both Phase 81 plans complete.

**Edit 4: Phase 81 progress table row (line 887).** Replace:
`| 81. OR Playbook Retrospective + v8.0 Close | v8.0 | 0/TBD | Pending | - |`
with:
`| 81. OR Playbook Retrospective + v8.0 Close | v8.0 | 2/2 | Complete | 2026-05-31 |`

This count (2/2) is canonical — Phase 81 has exactly two plans (81-01-PLAN.md and 81-02-PLAN.md) per the file structure.

**Constraints:**
- Use Edit tool for all 4 edits; do NOT use Write
- Do NOT modify any other phase rows, milestone lines, or content in ROADMAP.md
- The ✅ and 🚧 emoji characters must be exact Unicode (U+2705 for ✅; U+1F6A7 for 🚧) — copy from the existing v7.0 shipped line to ensure correct encoding
- Do NOT modify the v7.0 California milestone line (Phase 78 baseline must be preserved)
- After all 4 edits, search confirms zero remaining instances of `🚧 **v8.0 Oregon` and zero remaining instances of `v8.0 Oregon.*in progress` or `Phase 81.*Pending`
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains the exact substring `- ✅ **v8.0 Oregon** — Phases 72-81 (shipped 2026-05-31)` (top milestone list)
    - `.planning/ROADMAP.md` contains the exact substring `<summary>✅ v8.0 Oregon (Phases 72-81) — SHIPPED 2026-05-31</summary>` (details tag)
    - `.planning/ROADMAP.md` contains the exact substring `- [x] 81-01-PLAN.md` (Plan 01 checkbox checked)
    - `.planning/ROADMAP.md` contains the exact substring `- [x] 81-02-PLAN.md` (Plan 02 checkbox checked)
    - `.planning/ROADMAP.md` contains the exact substring `| 81. OR Playbook Retrospective + v8.0 Close | v8.0 | 2/2 | Complete | 2026-05-31 |` (progress table row — 2/2 unconditional)
    - `.planning/ROADMAP.md` does NOT contain `- 🚧 **v8.0 Oregon** — Phases 72-81 (in progress)` (old top milestone line removed)
    - `.planning/ROADMAP.md` does NOT contain `<summary>🚧 v8.0 Oregon (Phases 72-81) — IN PROGRESS</summary>` (old details tag removed)
    - `.planning/ROADMAP.md` does NOT contain `| 81. OR Playbook Retrospective + v8.0 Close | v8.0 | 0/TBD | Pending | - |` (old progress row removed)
    - `.planning/ROADMAP.md` does NOT contain `**Plans**: TBD` in the Phase 81 section (replaced with `**Plans**: 2 plans` plus Plans list)
    - All other phase rows in the progress table are unchanged: `grep -c "| 80. " .planning/ROADMAP.md` returns 1 (Phase 80 row still present); `grep -c "| 78. CA Playbook Retrospective " .planning/ROADMAP.md` returns 1 (Phase 78 row still present); `grep -c "| 77. Portland City Structure + Officials " .planning/ROADMAP.md` returns 1 (Phase 77 row still present)
    - v7.0 milestone line preserved: `grep -c "v7.0 California.*shipped 2026-05-29" .planning/ROADMAP.md` returns at least 1
    - v6.0 milestone line preserved: `grep -c "v6.0 Maine Essentials.*shipped 2026-05-20" .planning/ROADMAP.md` returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qF "- ✅ **v8.0 Oregon** — Phases 72-81 (shipped 2026-05-31)" .planning/ROADMAP.md && grep -qF "<summary>✅ v8.0 Oregon (Phases 72-81) — SHIPPED 2026-05-31</summary>" .planning/ROADMAP.md && grep -qE "^- \[x\] 81-01-PLAN.md" .planning/ROADMAP.md && grep -qE "^- \[x\] 81-02-PLAN.md" .planning/ROADMAP.md && grep -qF "| 81. OR Playbook Retrospective + v8.0 Close | v8.0 | 2/2 | Complete | 2026-05-31 |" .planning/ROADMAP.md && ! grep -qF "- 🚧 **v8.0 Oregon** — Phases 72-81 (in progress)" .planning/ROADMAP.md && ! grep -qF "<summary>🚧 v8.0 Oregon (Phases 72-81) — IN PROGRESS</summary>" .planning/ROADMAP.md && ! grep -qF "| 81. OR Playbook Retrospective + v8.0 Close | v8.0 | 0/TBD | Pending | - |" .planning/ROADMAP.md && grep -q "v7.0 California.*shipped 2026-05-29" .planning/ROADMAP.md && grep -q "v6.0 Maine Essentials.*shipped 2026-05-20" .planning/ROADMAP.md && echo PASS</automated>
  </verify>
  <done>
ROADMAP.md shows v8.0 Oregon as ✅ shipped 2026-05-31 in all four locations: top milestone list, v8.0 details summary tag, Phase 81 plans list (now showing 81-01 and 81-02 both [x]), Phase 81 progress table row (2/2 Complete). v7.0 California and v6.0 Maine milestone lines untouched. No remaining 🚧 v8.0 / in-progress / Pending strings for Phase 81 or the v8.0 milestone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md last_activity and Current Position to reflect Phase 81 complete + v8.0 shipped</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (file being edited — YAML last_activity is at line 7; Current Position section is at lines 19-25; stopped_at is at line 14)
    - .planning/phases/81-or-playbook-retrospective-v8-0-close/81-RESEARCH.md (lines 226-232 describe the exact STATE.md edits required — last_activity, stopped_at, Current Position)
    - .planning/phases/78-ca-playbook-retrospective/78-02-PLAN.md (canonical pattern — confirms the 2-edit structure: last_activity + Current Position; YAML milestone field NOT touched)
  </read_first>
  <action>
Use the Edit tool to make 3 targeted edits to .planning/STATE.md. Do NOT use Write.

**Edit 1: YAML last_activity field (line 7).** Replace the current value:
`last_activity: 2026-05-31 -- Phase 80 execution started`
with:
`last_activity: 2026-05-31 -- Phase 81 complete — OR Playbook Retrospective; v8.0 Oregon milestone shipped`

**Edit 2: YAML stopped_at field (line 14).** Replace the current value:
`stopped_at: Phase 80 complete (4/4) — ready to discuss Phase 81`
with:
`stopped_at: Phase 81 complete (2/2) — v8.0 Oregon shipped`

**Edit 3: Current Position section body (lines 19-25).** The current block reads:
```
## Current Position

Phase: 81
Plan: Not started
Status: Ready to plan
Last activity: 2026-05-31
Next recommended run: /gsd-plan-phase 77.1
```

Replace with:
```
## Current Position

Phase: 81
Plan: All complete
Status: Complete
Last activity: 2026-05-31 -- Phase 81 complete; v8.0 Oregon milestone shipped
Next recommended run: /gsd-discuss-phase (next milestone — see ROADMAP.md backlog)
```

**Do NOT touch:**
- The YAML `milestone: v2.2` field (line 3) — this field tracks the parked v2.2 milestone, not the active v8.0 work (same pattern as Phase 78-02 honored)
- The YAML `milestone_name: Data Depth & Admin Tooling` field (line 4) — same rationale
- The `progress:` block at lines 8-13 — completed_phases counters live there but updating them is outside this plan's scope (would require recomputing total counts)
- The lines after Current Position that reference Phase 77, Phase 76, Phase 75, etc. (lines 27-35) — those are reference history notes, not status pointers
- The `## Accumulated Context` section — all Key Decisions remain valid
- The `### Parked from v2.2` section
- The `### Known Architecture` section
- The `## Session Continuity` block at the bottom

**Constraints:**
- Use Edit tool for all 3 edits; do NOT use Write
- The em-dash character `—` (U+2014) must be preserved exactly — copy from the existing line, do not retype with hyphen
- After edits, the file still parses as valid YAML at the top frontmatter block (the `---` delimiters at lines 1 and 15 are intact)
- The `milestone: v2.2` field MUST be preserved exactly (it is intentionally pinned to v2.2 per Phase 78 precedent and Open Question 2 in 78-RESEARCH.md)
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` contains the exact substring `last_activity: 2026-05-31 -- Phase 81 complete — OR Playbook Retrospective; v8.0 Oregon milestone shipped`
    - `.planning/STATE.md` contains the exact substring `stopped_at: Phase 81 complete (2/2) — v8.0 Oregon shipped`
    - `.planning/STATE.md` Current Position section contains `Phase: 81`
    - `.planning/STATE.md` Current Position section contains `Plan: All complete`
    - `.planning/STATE.md` Current Position section contains `Status: Complete`
    - `.planning/STATE.md` Current Position section contains `Last activity: 2026-05-31 -- Phase 81 complete; v8.0 Oregon milestone shipped`
    - `.planning/STATE.md` does NOT contain `last_activity: 2026-05-31 -- Phase 80 execution started` (old line removed)
    - `.planning/STATE.md` does NOT contain `stopped_at: Phase 80 complete (4/4) — ready to discuss Phase 81` (old line removed)
    - `.planning/STATE.md` does NOT contain `Plan: Not started` (old Current Position field removed)
    - `.planning/STATE.md` does NOT contain `Status: Ready to plan` (old Current Position field removed)
    - YAML frontmatter is intact: `head -n 15 .planning/STATE.md | grep -c "^---$"` returns 2 (opening and closing delimiters present)
    - `milestone: v2.2` field preserved (not changed)
    - Accumulated Context still present: `grep -c "## Accumulated Context" .planning/STATE.md` returns exactly 1
    - Parked v2.2 section preserved: `grep -c "Parked from v2.2" .planning/STATE.md` returns exactly 1
    - Known Architecture section preserved: `grep -c "### Known Architecture" .planning/STATE.md` returns exactly 1
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -q "last_activity: 2026-05-31 -- Phase 81 complete — OR Playbook Retrospective; v8.0 Oregon milestone shipped" .planning/STATE.md && grep -q "stopped_at: Phase 81 complete (2/2) — v8.0 Oregon shipped" .planning/STATE.md && grep -q "^Phase: 81$" .planning/STATE.md && grep -q "^Plan: All complete$" .planning/STATE.md && grep -q "^Status: Complete$" .planning/STATE.md && grep -q "Last activity: 2026-05-31 -- Phase 81 complete; v8.0 Oregon milestone shipped" .planning/STATE.md && ! grep -q "last_activity: 2026-05-31 -- Phase 80 execution started" .planning/STATE.md && ! grep -q "stopped_at: Phase 80 complete" .planning/STATE.md && ! grep -q "^Plan: Not started$" .planning/STATE.md && ! grep -q "^Status: Ready to plan$" .planning/STATE.md && grep -q "^milestone: v2.2$" .planning/STATE.md && [ "$(grep -c '## Accumulated Context' .planning/STATE.md)" -eq 1 ] && [ "$(grep -c 'Parked from v2.2' .planning/STATE.md)" -eq 1 ] && [ "$(grep -c '### Known Architecture' .planning/STATE.md)" -eq 1 ] && echo PASS</automated>
  </verify>
  <done>
STATE.md last_activity, stopped_at, and Current Position all updated to reflect Phase 81 complete + v8.0 shipped. Plan field set to "All complete"; Status set to "Complete". YAML frontmatter intact. `milestone: v2.2` field untouched (same pattern as Phase 78-02). Accumulated Context, Parked from v2.2, and Known Architecture sections all preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 5-6 v8.0 Oregon bullets to PROJECT.md Validated list and close the v8.0 Current Milestone section</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (file being edited — Validated list ends around line 97 with the last v7.0 bullet; 'Current Milestone: v8.0 Oregon' section spans lines 99-119; Active subsection lists v8.0 phase checkboxes 113-118; Out of Scope begins at line 120)
    - .planning/phases/81-or-playbook-retrospective-v8-0-close/81-RESEARCH.md (lines 236-242 contain the 5-6 v8.0 Validated bullets to add — copy verbatim)
    - .planning/phases/78-ca-playbook-retrospective/78-02-PLAN.md (canonical pattern — confirms the 2-edit structure: append v8.0 bullets after the last v7.0 bullet; replace Current Milestone section)
    - .planning/ROADMAP.md (v8.0 progress table rows are the source of truth for what shipped under v8.0)
  </read_first>
  <action>
Use the Edit tool to make 2 targeted edits to .planning/PROJECT.md. Do NOT use Write.

**Edit 1: Add 6 new v8.0 bullets at the end of the Validated list (after the last v7.0 bullet at line 97).** The last v7.0 bullet ends with:
`- ✓ CA Playbook retrospective — 11 CA-specific GOTCHAs added to LOCATION-ONBOARDING.md; California Quick Reference block added; 7 new rows in Cities Onboarded table; v7.0 milestone closed — v7.0`

After this line, insert these 6 new v8.0 bullets verbatim from 81-RESEARCH.md lines 237-242:

```
- ✓ Oregon TIGER geofences — 241 G4110 cities + 30 SLDU + 60 SLDL + 6 CD + 36 G4020 counties; Portland geo_id=4159000; any OR address routes to correct federal, state, and local representatives; cd119 TIGER key — v8.0
- ✓ Oregon state government DB — 5 voter-elected constitutional officers (Kotek/Rayfield/Read/Steiner/Stephenson) + 30 state senators + 60 house reps + 2 US Senators + 6 US House reps; all 90 legislators from oregonlegislature.gov with headshots at 600×750 — v8.0
- ✓ Portland deep seed — 2024 charter reform: 4-district × 3-seat RCV council (12 officials) + Mayor + City Auditor + 2 appointed (City Administrator, City Attorney); council district boundaries from PortlandMaps ArcGIS (not TIGER); 14 elected officials with headshots from portland.gov 1_1_320w style URLs — v8.0
- ✓ OR 2026 elections + discovery pipeline — 105 race rows (1 Governor + 1 US Senate + 6 US House + 30 OR Senate + 60 OR House + 7 Portland City); discovery_jurisdictions for OR statewide + Portland; armed via election_date 180-day cron window — v8.0
- ✓ 321 compass stances across 24 OR officials — Kotek 31, Rayfield 24, Bonamici 24, Bentz 21, Hoyle 20, Steiner 13, Salinas 18, Bynum 13, Read 12, Dexter 12, Stephenson 10, Wilson 10; all cited; compass renders on Kotek profile — v8.0
- ✓ OR Playbook retrospective — 9 OR-specific GOTCHAs added to LOCATION-ONBOARDING.md; Oregon Quick Reference block; 2 new Cities Onboarded rows (Oregon state + Portland); v8.0 milestone closed — v8.0
```

These bullets follow the existing v7.0/v6.0 pattern: `- ✓ <capability> — <key numbers/facts> — v8.0`. The bullets must appear consecutively (no blank lines between) immediately after the v7.0 CA Playbook retrospective bullet, BEFORE the `### Current Milestone: v8.0 Oregon` heading at line 99.

**Edit 2: Close the 'Current Milestone: v8.0 Oregon' section (lines 99-119).** The current block contains an H3 heading, Goal sentence, Target features list, and an Active checklist of v8.0 phases. Replace this entire block with a placeholder/transitional section indicating no current milestone is active.

Approach: REPLACE lines 99-119 (everything from `### Current Milestone: v8.0 Oregon` through the end of the `### Active` checklist) with:

```
### Current Milestone: Between Milestones

**Goal:** v8.0 Oregon shipped 2026-05-31. Next milestone (v9.0+) is pending discussion — see ROADMAP.md backlog and the parked v2.2 milestone (Phases 8-11) for candidate next steps.

### Active

(No phases currently in flight — awaiting next milestone scope decision.)
```

This preserves the `### Current Milestone` heading (referenced by downstream tooling) and the `### Active` subsection, but signals that no work is currently in flight. Mirrors the Phase 78 approach of replacing the section content rather than deleting it.

**Constraints:**
- Use Edit tool; do NOT use Write
- Do NOT modify the `### Validated` heading or any pre-v8.0 bullets (Cambridge, Maine, California, etc.)
- Do NOT modify the `### Out of Scope` section
- Do NOT modify the `## Context`, `## Constraints`, or `## Key Decisions` sections
- The `### Current Milestone` heading must always exist (it is referenced by downstream tooling); replace its contents, do not delete the heading
- All 6 v8.0 bullets must end with the literal suffix ` — v8.0` (em-dash + v8.0) — copy from 81-RESEARCH.md exactly to avoid hyphen-vs-em-dash drift
- After both edits, the v8.0 Oregon capabilities appear ONLY in the Validated section, NOT in the Current Milestone section
- The v7.0 California, v6.0 Maine, and v5.0 Cambridge sections of the Validated list must remain unchanged
  </action>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains all 6 v8.0 Validated bullets:
      - `grep -q "Oregon TIGER geofences — 241 G4110 cities" .planning/PROJECT.md` matches
      - `grep -q "Oregon state government DB — 5 voter-elected constitutional officers" .planning/PROJECT.md` matches
      - `grep -q "Portland deep seed — 2024 charter reform" .planning/PROJECT.md` matches
      - `grep -q "OR 2026 elections + discovery pipeline" .planning/PROJECT.md` matches
      - `grep -q "321 compass stances across 24 OR officials" .planning/PROJECT.md` matches
      - `grep -q "OR Playbook retrospective — 9 OR-specific GOTCHAs" .planning/PROJECT.md` matches
    - All 6 v8.0 bullets end with ` — v8.0` suffix: `grep -c " — v8.0$" .planning/PROJECT.md` returns at least 6
    - `### Current Milestone` heading still present exactly once: `grep -c "^### Current Milestone" .planning/PROJECT.md` returns 1
    - `### Current Milestone` line no longer references v8.0 Oregon as active: `grep -q "^### Current Milestone: v8.0 Oregon$" .planning/PROJECT.md` does NOT match (returns no result)
    - `### Current Milestone: Between Milestones` heading present: `grep -q "^### Current Milestone: Between Milestones$" .planning/PROJECT.md` matches
    - Old Active v8.0 phase checkboxes removed: `grep -q "^- \[ \] Phase 75: OR State Legislature$" .planning/PROJECT.md` does NOT match (was Phase 78's note that 75 was the only [ ]; now removed entirely)
    - The Active section no longer contains the v8.0 phase checkboxes: `grep -q "^- \[x\] Phase 72: OR Geofences$" .planning/PROJECT.md` does NOT match (removed; now in the Validated bullet)
    - Existing v7.0 Validated bullets preserved: `grep -q "CA TIGER geofences — 482 G4110 cities" .planning/PROJECT.md` matches; `grep -q "CA Playbook retrospective — 11 CA-specific GOTCHAs" .planning/PROJECT.md` matches
    - Existing v6.0 Validated bullets preserved: `grep -q "Maine TIGER geofences — 23 G4110 cities" .planning/PROJECT.md` matches
    - Existing v5.0 Validated bullets preserved: `grep -q "LOCATION-ONBOARDING.md.*cold-start playbook" .planning/PROJECT.md` matches
    - `### Out of Scope` section preserved: `grep -c "^### Out of Scope$" .planning/PROJECT.md` returns 1
    - `## Key Decisions` section preserved: `grep -c "^## Key Decisions$" .planning/PROJECT.md` returns 1
    - `## Context` section preserved: `grep -c "^## Context$" .planning/PROJECT.md` returns 1
    - `## Constraints` section preserved: `grep -c "^## Constraints$" .planning/PROJECT.md` returns 1
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -q "Oregon TIGER geofences — 241 G4110 cities" .planning/PROJECT.md && grep -q "Oregon state government DB — 5 voter-elected constitutional officers" .planning/PROJECT.md && grep -q "Portland deep seed — 2024 charter reform" .planning/PROJECT.md && grep -q "OR 2026 elections + discovery pipeline" .planning/PROJECT.md && grep -q "321 compass stances across 24 OR officials" .planning/PROJECT.md && grep -q "OR Playbook retrospective — 9 OR-specific GOTCHAs" .planning/PROJECT.md && [ "$(grep -c ' — v8.0$' .planning/PROJECT.md)" -ge 6 ] && grep -q "^### Current Milestone: Between Milestones$" .planning/PROJECT.md && ! grep -q "^### Current Milestone: v8.0 Oregon$" .planning/PROJECT.md && ! grep -q "^- \[ \] Phase 75: OR State Legislature$" .planning/PROJECT.md && ! grep -q "^- \[x\] Phase 72: OR Geofences$" .planning/PROJECT.md && grep -q "CA TIGER geofences — 482 G4110 cities" .planning/PROJECT.md && grep -q "CA Playbook retrospective — 11 CA-specific GOTCHAs" .planning/PROJECT.md && grep -q "Maine TIGER geofences — 23 G4110 cities" .planning/PROJECT.md && grep -q "LOCATION-ONBOARDING.md" .planning/PROJECT.md && [ "$(grep -c '^### Out of Scope$' .planning/PROJECT.md)" -eq 1 ] && [ "$(grep -c '^## Key Decisions$' .planning/PROJECT.md)" -eq 1 ] && echo PASS</automated>
  </verify>
  <done>
PROJECT.md has 6 new v8.0 Validated bullets appended after the v7.0 bullets, each ending with ` — v8.0`. The `### Current Milestone` heading now reads `Between Milestones` with a transitional Goal sentence pointing at ROADMAP.md backlog and v2.2 parked phases as candidate next steps. The `### Active` subsection is empty (placeholder note that no phases are currently in flight). All v7.0/v6.0/v5.0/earlier Validated bullets preserved. `### Out of Scope`, `## Key Decisions`, `## Context`, and `## Constraints` sections untouched.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run holistic milestone-closure integrity check:

```bash
cd "C:/Transparent Motivations/essentials"

# 1. ROADMAP.md shows v8.0 shipped at top milestone list AND in <details> tag
grep -qF "- ✅ **v8.0 Oregon** — Phases 72-81 (shipped 2026-05-31)" .planning/ROADMAP.md || { echo "FAIL: ROADMAP top v8.0 line"; exit 1; }
grep -qF "<summary>✅ v8.0 Oregon (Phases 72-81) — SHIPPED 2026-05-31</summary>" .planning/ROADMAP.md || { echo "FAIL: ROADMAP v8.0 details summary"; exit 1; }

# 2. ROADMAP.md Phase 81 progress row shows Complete with 2/2
grep -qF "| 81. OR Playbook Retrospective + v8.0 Close | v8.0 | 2/2 | Complete | 2026-05-31 |" .planning/ROADMAP.md || { echo "FAIL: ROADMAP Phase 81 progress row 2/2"; exit 1; }

# 3. STATE.md last_activity + Current Position reflect Phase 81 complete
grep -q "last_activity: 2026-05-31 -- Phase 81 complete" .planning/STATE.md || { echo "FAIL: STATE.md last_activity"; exit 1; }
grep -q "^Phase: 81$" .planning/STATE.md || { echo "FAIL: STATE.md Current Position phase"; exit 1; }
grep -q "^Plan: All complete$" .planning/STATE.md || { echo "FAIL: STATE.md Current Position plan"; exit 1; }
grep -q "^Status: Complete$" .planning/STATE.md || { echo "FAIL: STATE.md Current Position status"; exit 1; }

# 4. PROJECT.md Validated has all 6 v8.0 bullets
for marker in "Oregon TIGER geofences — 241 G4110 cities" "Oregon state government DB — 5 voter-elected" "Portland deep seed — 2024 charter reform" "OR 2026 elections + discovery pipeline" "321 compass stances across 24 OR officials" "OR Playbook retrospective — 9 OR-specific GOTCHAs"; do
  grep -q "$marker" .planning/PROJECT.md || { echo "FAIL: PROJECT.md missing v8.0 bullet: $marker"; exit 1; }
done

# 5. PROJECT.md Current Milestone no longer points at v8.0
! grep -q "^### Current Milestone: v8.0 Oregon$" .planning/PROJECT.md || { echo "FAIL: PROJECT.md still shows v8.0 Current Milestone"; exit 1; }
grep -q "^### Current Milestone: Between Milestones$" .planning/PROJECT.md || { echo "FAIL: PROJECT.md placeholder Current Milestone heading missing"; exit 1; }

# 6. PROJECT.md Active section no longer shows v8.0 phase checkboxes
! grep -q "^- \[x\] Phase 72: OR Geofences$" .planning/PROJECT.md || { echo "FAIL: PROJECT.md Phase 72 still in Active"; exit 1; }
! grep -q "^- \[ \] Phase 75: OR State Legislature$" .planning/PROJECT.md || { echo "FAIL: PROJECT.md Phase 75 still in Active"; exit 1; }

# 7. No regressions on pre-v8.0 content
grep -q "v7.0 California.*shipped 2026-05-29" .planning/ROADMAP.md || { echo "FAIL: v7.0 milestone line missing"; exit 1; }
grep -q "v6.0 Maine Essentials.*shipped 2026-05-20" .planning/ROADMAP.md || { echo "FAIL: v6.0 milestone line missing"; exit 1; }
grep -q "CA Playbook retrospective — 11 CA-specific GOTCHAs" .planning/PROJECT.md || { echo "FAIL: v7.0 CA Playbook bullet missing"; exit 1; }
grep -q "Maine TIGER geofences — 23 G4110 cities" .planning/PROJECT.md || { echo "FAIL: v6.0 PROJECT.md bullet missing"; exit 1; }

echo ALL_PASS
```
</verification>

<success_criteria>
- ROADMAP.md v8.0 Oregon shows ✅ shipped 2026-05-31 in BOTH the top milestone list and the v8.0 details summary tag
- ROADMAP.md Phase 81 progress table row shows 2/2 Complete with date 2026-05-31
- ROADMAP.md Phase 81 plans list shows both 81-01 and 81-02 as [x] complete with a Plans list block matching Phase 78 format
- STATE.md last_activity reflects Phase 81 complete + v8.0 shipped
- STATE.md stopped_at reflects Phase 81 complete (2/2)
- STATE.md Current Position shows Phase 81 complete with "Plan: All complete" and "Status: Complete"
- PROJECT.md Validated list contains 6 new v8.0 bullets covering Oregon TIGER, OR state government DB, Portland deep seed, 2026 elections+discovery, compass stances, and playbook retrospective
- PROJECT.md Current Milestone section now reads "Between Milestones" (signals no work currently in flight; pointer to ROADMAP.md backlog for next milestone)
- PROJECT.md v8.0 Active phase checkboxes removed (no longer shows Phase 72-77 as Active)
- No regressions: v7.0 California, v6.0 Maine, v5.0 Cambridge entries all preserved in Validated; Cambridge/Maine/CA Quick Reference blocks in LOCATION-ONBOARDING.md untouched; Key Decisions table preserved
- Plan 01 deliverable (LOCATION-ONBOARDING.md update) is accurately reflected in the PROJECT.md "OR Playbook retrospective" bullet
</success_criteria>

<output>
After completion, create `.planning/phases/81-or-playbook-retrospective-v8-0-close/81-02-SUMMARY.md` documenting:
- ROADMAP.md changes: 4 edits applied (top milestone, details summary, Phase 81 plans list with Wave 1/Wave 2 format, Phase 81 progress row 2/2)
- STATE.md changes: 3 edits applied (last_activity + stopped_at + Current Position with "Plan: All complete" + "Status: Complete")
- PROJECT.md changes: 2 edits applied (6 v8.0 Validated bullets added, Current Milestone section replaced with "Between Milestones" placeholder)
- Confirmation that all pre-v8.0 content preserved (count comparisons for v7.0/v6.0/v5.0 markers)
- Confirmation that PROJECT.md Active section is now empty (no v8.0 phase checkboxes; placeholder note that no phases are in flight)
- Any deviations from the planned edits with rationale
- v8.0 milestone close notification — Phase 81 closes the v8.0 milestone; project state now between milestones (awaiting next milestone scope discussion)
</output>
</content>
</invoke>