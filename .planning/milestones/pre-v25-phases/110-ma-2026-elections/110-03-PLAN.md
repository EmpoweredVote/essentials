---
phase: 110-ma-2026-elections
plan: 03
type: execute
wave: 3
depends_on:
  - 110-02
files_modified:
  - C:/Transparent Motivations/essentials/src/pages/Landing.jsx
autonomous: false
requirements:
  - MA-ELECTIONS-02
  - MA-ELECTIONS-03

must_haves:
  truths:
    - "Boston appears in the COVERAGE_CITIES list on the Landing page"
    - "Clicking Boston on the Landing page navigates to /results with browse_government_list=2507000 and browse_state=MA"
    - "The existing Cambridge entry is unchanged"
    - "The build passes without errors"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/pages/Landing.jsx"
      provides: "Boston city entry in COVERAGE_CITIES"
      contains: "label: 'Boston'"
  key_links:
    - from: "Landing.jsx COVERAGE_CITIES"
      to: "/results route"
      via: "navigate() with browse_government_list param"
      pattern: "browseGovernmentList: \\['2507000'\\]"
---

<objective>
Add Boston to the COVERAGE_CITIES array in Landing.jsx so it appears as a browsable city on the Landing page. The Boston entry routes users to the /results page showing Boston city officials (Mayor Wu + City Council + School Committee), which were seeded in Phase 108.

Purpose: Phase 108 seeded all Boston officials but the landing page never surfaced Boston as a city. Without this edit, users can only discover Boston officials by entering a Boston address — not by browsing. This completes the MA landing page coverage.

Note on MA state-level entry: The existing COVERAGE_CITIES routing logic (lines 81-88) only handles browseGovernmentList entries — there is no routing path for a state-only entry without browseGovernmentList. Adding a state-level "Massachusetts" entry without a browseable government would silently do nothing. That entry is deferred until a state-level browse route is implemented.

Output: src/pages/Landing.jsx with Boston entry added; build passes.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Transparent Motivations/essentials/.planning/phases/110-ma-2026-elections/110-RESEARCH.md
@C:/Transparent Motivations/essentials/.planning/phases/110-ma-2026-elections/110-02-SUMMARY.md

<interfaces>
<!-- Current COVERAGE_CITIES array from Landing.jsx (lines 14-26, verified 2026-06-11) -->

const COVERAGE_CITIES = [
  { label: 'Berkeley', state: 'California', browseGovernmentList: ['0606000'], browseStateAbbrev: 'CA' },
  { label: 'Fremont', state: 'California', browseGovernmentList: ['0626000'], browseStateAbbrev: 'CA' },
  { label: 'Sacramento', state: 'California', browseGovernmentList: ['0664000'], browseStateAbbrev: 'CA' },
  { label: 'San Diego', state: 'California', browseGovernmentList: ['0666000'], browseStateAbbrev: 'CA' },
  { label: 'San Francisco', state: 'California', browseGovernmentList: ['0667000'], browseStateAbbrev: 'CA' },
  { label: 'San Jose', state: 'California', browseGovernmentList: ['0668000'], browseStateAbbrev: 'CA' },
  { label: 'Portland', state: 'Maine', browseGovernmentList: ['2360545'], browseStateAbbrev: 'ME' },
  { label: 'Cambridge', state: 'Massachusetts', browseGovernmentList: ['2511000'], browseStateAbbrev: 'MA' },  ← insert Boston AFTER this line
  { label: 'Portland', state: 'Oregon', browseGovernmentList: ['4159000'], browseStateAbbrev: 'OR' },
  { label: 'Leonardtown', state: 'Maryland', browseGovernmentList: ['2446475', '24037'], browseStateAbbrev: 'MD' },
  { label: 'Alexandria', state: 'Virginia', browseGovernmentList: ['5101000', '51510'], browseStateAbbrev: 'VA' },
];

Entry to insert (exact text):
  { label: 'Boston', state: 'Massachusetts', browseGovernmentList: ['2507000'], browseStateAbbrev: 'MA' },

Boston geo_id: '2507000' (G4110 incorporated place, verified in geofence_boundaries)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Boston entry to COVERAGE_CITIES in Landing.jsx</name>
  <files>C:/Transparent Motivations/essentials/src/pages/Landing.jsx</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/pages/Landing.jsx (read COVERAGE_CITIES array at lines 14-26 to confirm current state before editing; also read the navigate() routing block around lines 81-88 to verify browseGovernmentList routing path is correct)
  </read_first>
  <action>
    Read Landing.jsx to confirm the Cambridge entry is at the expected location and no Boston entry already exists.

    Insert the following new line immediately after the Cambridge Massachusetts entry:
    { label: 'Boston', state: 'Massachusetts', browseGovernmentList: ['2507000'], browseStateAbbrev: 'MA' },

    The new entry must:
    - Use label: 'Boston' (not 'City of Boston')
    - Use state: 'Massachusetts'
    - Use browseGovernmentList: ['2507000'] (geo_id for Boston as a G4110 incorporated place)
    - Use browseStateAbbrev: 'MA'
    - Be positioned AFTER the Cambridge entry and BEFORE the Portland Oregon entry
    - Match the exact trailing comma style of the other entries

    Do NOT modify the Cambridge entry. Do NOT add a statewide "Massachusetts" entry (no routing support for it currently).
  </action>
  <verify>
    <automated>
      grep -c "label: 'Boston'" "C:/Transparent Motivations/essentials/src/pages/Landing.jsx" (expect 1)
      grep -c "browseGovernmentList: \['2507000'\]" "C:/Transparent Motivations/essentials/src/pages/Landing.jsx" (expect 1)
      grep -c "label: 'Cambridge'" "C:/Transparent Motivations/essentials/src/pages/Landing.jsx" (expect 1 — Cambridge unchanged)
    </automated>
  </verify>
  <acceptance_criteria>
    - Landing.jsx contains exactly 1 entry with label: 'Boston'
    - Boston entry has browseGovernmentList: ['2507000'] and browseStateAbbrev: 'MA'
    - Boston entry appears after Cambridge and before Portland (Oregon)
    - Cambridge entry is unchanged: browseGovernmentList: ['2511000'], browseStateAbbrev: 'MA'
    - No statewide "Massachusetts" entry added (label: 'Massachusetts' must NOT appear in COVERAGE_CITIES)
    - COVERAGE_CITIES array has 12 entries total (was 11, now +1 for Boston)
  </acceptance_criteria>
  <done>Boston entry inserted in COVERAGE_CITIES at correct position with correct geo_id and state abbreviation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Boston has been added to the COVERAGE_CITIES array in Landing.jsx with browseGovernmentList: ['2507000'] positioned after Cambridge and before Portland (Oregon). The build has been verified to pass.
  </what-built>
  <how-to-verify>
    1. Run the local dev server: cd "C:/Transparent Motivations/essentials" && npm run dev (or the equivalent start command for this project)
    2. Open the Landing page in a browser
    3. Confirm "Boston" appears in the city list alongside Cambridge (both Massachusetts)
    4. Click "Boston" — you should navigate to /results?browse_government_list=2507000&browse_label=Boston&browse_state=MA
    5. Confirm the /results page loads and shows Boston city officials (Mayor Wu should appear in the LOCAL section)
    6. Confirm "Cambridge" still appears and links correctly to /results?browse_government_list=2511000
    7. Run build: cd "C:/Transparent Motivations/essentials" && npm run build — confirm no errors
  </how-to-verify>
  <resume-signal>Type "approved" if Boston appears and routes correctly, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Landing.jsx → /results | Static config array; no user input; browseGovernmentList geo_id is hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | Tampering | COVERAGE_CITIES array | accept | Static array in JSX; geo_id '2507000' is a known verified value; no runtime injection |
| T-110-SC | Tampering | npm/npx installs | accept | No new packages; npm run build uses existing dependencies |
</threat_model>

<verification>
After Task 1 completes, before the human checkpoint:
1. `grep -c "label: 'Boston'" src/pages/Landing.jsx` → 1
2. `grep -c "2507000" src/pages/Landing.jsx` → 1
3. `grep -c "label: 'Cambridge'" src/pages/Landing.jsx` → 1 (unchanged)
4. `npm run build` from essentials directory completes without error

Human checkpoint verifies visual appearance and routing behavior in browser.
</verification>

<success_criteria>
- Boston entry exists in COVERAGE_CITIES with geo_id '2507000' and browseStateAbbrev 'MA'
- Clicking Boston navigates to /results with correct browse params
- Boston city officials (Mayor Wu et al.) appear for a Boston browse
- Cambridge entry unchanged
- Build passes
- Phase 110 complete — MA-ELECTIONS-01 through MA-ELECTIONS-04 all satisfied
</success_criteria>

<output>
Create .planning/phases/110-ma-2026-elections/110-03-SUMMARY.md when done
</output>
