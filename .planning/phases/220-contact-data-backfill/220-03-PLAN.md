---
phase: 220-contact-data-backfill
plan: 03
type: execute
wave: 2
depends_on: [220-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1406_collin_seat_alias_emails.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1406_collin_seat_alias_emails.ts
autonomous: true
requirements: [COLLIN-CONTACT-02]
user_setup: []

must_haves:
  truths:
    - "Migration 1406 seeds published seat/role-alias emails onto each seat's currently-seated official for Blue Ridge (mayor@ / council1..5@blueridgecity.com), Nevada (mayor@ / councilman1..5@cityofnevadatx.org), and Melissa (mayor@ / place1..4@ / place6@cityofmelissa.com, with Place 5 being the personal address cackerman@cityofmelissa.com) — seat aliases attach to the office and survive incumbent changes (D-02)."
    - "Each email is appended to essentials.politicians.email_addresses only if not already present, so re-applying migration 1406 is net-zero (idempotent, D-07)."
    - "No generic city-wide catch-all (info@ / cityhall@ / contact@ / bare council@ without a seat number) is ever seeded onto an individual official (D-02)."
    - "McKinney's already-present District#@ / mayor@ seat-alias pattern is the reference model and is NOT re-seeded here (baseline already complete)."
  artifacts:
    - "C:/EV-Accounts/backend/migrations/1406_collin_seat_alias_emails.sql"
    - "C:/EV-Accounts/backend/scripts/_apply-migration-1406_collin_seat_alias_emails.ts"
  key_links:
    - "Each seat alias → the office's currently-seated politician_id (governments.geo_id → chambers → offices → offices.politician_id), matched by seat title/place number."
  prohibitions:
    - statement: "No generic catch-all alias seeded onto any official (D-02)."
      status: enforced
      verification: "apply-script gate: 0 seeded email_addresses rows contain a generic catch-all local-part (the info / cityhall / contact / bare-council styles)."
    - statement: "No writes to inform.* (no compass-stance side effect, D-06/D-07)."
      status: enforced
      verification: "apply-script before/after row-count assertion on inform.politician_answers unchanged."
    - statement: "party is never touched (antipartisan — D-05)."
      status: enforced
      verification: "Migration UPDATEs only email_addresses; no party column in the SET list."
    - statement: "No fabricated email — every seeded address is transcribed verbatim from the RESEARCH Per-City table (D-05)."
      status: enforced
      verification: "Each seeded alias matches a RESEARCH Group-3 row exactly (domain and local-part)."
---

<objective>
Author migration 1406: seed the published seat/role-alias emails for the three cities whose council publishes office-attached aliases — Blue Ridge, Nevada, Melissa — onto each seat's currently-seated official (D-02). These aliases attach to the office, so they are safe to seed regardless of any future incumbent change.

Purpose: Closes COLLIN-CONTACT-02 for the seat-alias cities and satisfies Success Criterion #4 for their officials.
Output: migration 1406 + its gated apply-script, ready for the operator to apply in plan 220-06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-contact-data-backfill/220-RESEARCH.md
@.planning/phases/220-contact-data-backfill/220-CONTEXT.md
@.planning/phases/220-contact-data-backfill/220-PREFLIGHT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 1406 (seat-alias emails) + gated apply-script</name>
  <files>C:/EV-Accounts/backend/migrations/1406_collin_seat_alias_emails.sql, C:/EV-Accounts/backend/scripts/_apply-migration-1406_collin_seat_alias_emails.ts</files>
  <read_first>
    - .planning/phases/220-contact-data-backfill/220-RESEARCH.md §"Per-City Sourcing Table" Group 3 rows for Blue Ridge, Nevada, Melissa, and the McKinney reference row (the seat→email mapping + the note that Melissa Place 5 is a personal address, not a placeN@ alias).
    - .planning/phases/220-contact-data-backfill/220-CONTEXT.md D-02 (seat-alias yes / generic catch-all no; McKinney District#@ pattern is the model).
    - .planning/phases/220-contact-data-backfill/220-PREFLIGHT.md (the locked migration number for this plan — research: 1406).
    - C:/EV-Accounts/backend/scripts/_apply-migration-1393_collin_zero_race_shared_may2026.ts (gate shape to mirror).
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1406_collin_seat_alias_emails.sql wrapped in BEGIN/COMMIT. For each seat of Blue Ridge (4808872), Nevada (4850760), and Melissa (4847496), determine the currently-seated official's politician_id by joining essentials.offices→chambers→governments on geo_id + seat title/place number, then append the seat's published alias to that politician's email_addresses array ONLY IF the address is not already present (idempotency — e.g. WHERE NOT (email_addresses @> ARRAY[<addr>]::text[]), coalescing a NULL array to empty first). Transcribe each alias verbatim from the RESEARCH Group-3 rows (Blue Ridge: mayor@ + council1..5@blueridgecity.com; Nevada: mayor@ + councilman1..5@cityofnevadatx.org; Melissa: mayor@ + place1..4@ + place6@cityofmelissa.com, and Place 5 = the personal address cackerman@cityofmelissa.com which breaks the placeN@ shape — seed it as given). Do NOT seed any generic catch-all (the info / cityhall / contact / bare-council styles). Do NOT re-seed McKinney — its District#@/mayor@ aliases are already present (baseline); reference it in a header comment as the pattern model only. Escape apostrophes and preserve UTF-8 in any name comments; reference sources by URL/concept in header comments (do not paste a generic-alias literal). Then write C:/EV-Accounts/backend/scripts/_apply-migration-1406_collin_seat_alias_emails.ts mirroring 1393's structure: capture inform.politician_answers count before apply; apply the SQL; then gates incrementing a failure counter, exit non-zero on any failure — (g-a) each targeted seat's politician has its expected alias present in email_addresses (per-geo_id expected count met); (g-b) 0 seeded email_addresses across the 3 geo_ids contain a generic catch-all local-part (the info / cityhall / contact / bare-council styles) — the D-02 guard; (g-c) inform.politician_answers count unchanged (D-06/D-07); (g-d) split-section query returns 0 rows; (g-e) re-run the SQL a second time and assert net-zero new appends (idempotency, D-07).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/_apply-migration-1406_collin_seat_alias_emails.ts && test -f migrations/1406_collin_seat_alias_emails.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; the .ts references the correct .sql filename and mirrors 1393's gate pattern.
    - Blue Ridge (6), Nevada (6), Melissa (7) seat aliases are each mapped to the correct seat's politician_id via a geo_id + title join.
    - Melissa Place 5 is seeded as the personal address (not a placeN@ alias) exactly as RESEARCH gives it.
    - The append is idempotent (only when the address is absent); only email_addresses is touched.
    - The apply-script contains gates g-a..g-e including the generic-catch-all guard (g-b).
  </acceptance_criteria>
  <done>Migration 1406 and its gated apply-script are authored and type-check clean, ready for the operator to apply.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `C:/EV-Accounts/backend/migrations/1406_collin_seat_alias_emails.sql` (new)
- `C:/EV-Accounts/backend/scripts/_apply-migration-1406_collin_seat_alias_emails.ts` (new)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pre-sourced RESEARCH table → migration SQL | Each alias transcribed verbatim; no pattern-derivation |
| migration SQL → production essentials schema | Idempotent array-append, generic-catch-all-blocked writes only |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-220-06 | Info disclosure / policy violation | generic catch-all seeded onto an official | high | mitigate | D-02 guard gate g-b asserts 0 generic catch-all local-parts across the 3 geo_ids |
| T-220-03 | Tampering (wrong contact target) | seat alias → wrong politician_id | medium | mitigate | Seat matched by geo_id + title/place number; per-seat expected-alias gate g-a |
| T-220-05 | Integrity (stance side effect) | inform.* tables | high | mitigate | Before/after row-count assertion (gate g-c), D-06/D-07 |
| T-220-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; legitimacy gate N/A |
</threat_model>

<verification>
- apply-script gates g-a..g-e all green when the operator runs it in plan 220-06.
- Every Blue Ridge / Nevada / Melissa seat carries its published alias; 0 generic catch-alls; inform.* net-zero; split-section 0 rows; idempotent.
</verification>

<success_criteria>
Blue Ridge, Nevada, and Melissa officials each carry their published seat-alias email attached to the correct seat; no generic catch-all is seeded; the migration is idempotent.
</success_criteria>

<output>
Create `.planning/phases/220-contact-data-backfill/220-03-SUMMARY.md` when done.
</output>
