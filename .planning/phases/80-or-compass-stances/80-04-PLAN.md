---
phase: 80-or-compass-stances
plan: 04
type: execute
wave: 4
depends_on:
  - 80-03
files_modified: []
autonomous: false
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4

must_haves:
  truths:
    - "The full coverage summary SQL returns 25 rows (5 execs + 6 House reps + 13 Portland + Auditor, excluding 2 done senators and 2 appointed)"
    - "All 5 OR constitutional officers have stance_count > 0 (SC-1)"
    - "All 6 OR US House reps have stance_count > 0 (SC-1)"
    - "≥ 9 of 13 Portland officials have stance_count > 0 (SC-2)"
    - "All stance values are integers 1–5 (no decimals, no out-of-range) — D-07"
    - "Tina Kotek's profile renders the CompassCard widget in the browser (SC-4)"
    - "Every research run during Plans 01–03 was sequential — no parallel runs (SC-3)"
  artifacts:
    - path: ".planning/phases/80-or-compass-stances/80-04-SUMMARY.md"
      provides: "Verification summary documenting all 4 success criteria"
  key_links:
    - from: "essentials.politicians (Kotek UUID)"
      to: "inform.politician_answers (Kotek rows)"
      via: "politician_id FK"
      pattern: "inform.politician_answers.politician_id = essentials.politicians.id"
    - from: "GET /compass/politicians API"
      to: "CompassCard widget on profile page"
      via: "politicianIdsWithStances Set membership check"
      pattern: "politicianIdsWithStances.has(politicianId)"
---

<objective>
Verify that Phase 80 (OR Compass Stances) meets all four success criteria — no new ingestion, no code changes. This plan runs the full coverage summary SQL from 80-VALIDATION.md, the value-range integrity gate, the per-group Nyquist spot-checks, and a browser-side compass render check on the Tina Kotek profile. Then closes the phase.

Purpose: Closes SC-1 (executives + federal stances), SC-2 (Portland stances), SC-3 (one-at-a-time process audit), and SC-4 (compass renders on at least one OR official profile).

Output:
- `80-04-SUMMARY.md` documenting verification results
- Phase 80 marked complete in ROADMAP.md (plan count + Phases table entry)
- STATE.md updated with Phase 80 completion notes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-or-compass-stances/80-CONTEXT.md
@.planning/phases/80-or-compass-stances/80-RESEARCH.md
@.planning/phases/80-or-compass-stances/80-VALIDATION.md
@.planning/phases/80-or-compass-stances/80-01-SUMMARY.md
@.planning/phases/80-or-compass-stances/80-02-SUMMARY.md
@.planning/phases/80-or-compass-stances/80-03-SUMMARY.md
@.planning/phases/46-cambridge-compass-stances/46-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification SQL gates against Supabase</name>
  <files>
    .planning/phases/80-or-compass-stances/80-04-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/80-or-compass-stances/80-VALIDATION.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
    .planning/phases/80-or-compass-stances/80-01-SUMMARY.md
    .planning/phases/80-or-compass-stances/80-02-SUMMARY.md
    .planning/phases/80-or-compass-stances/80-03-SUMMARY.md
  </read_first>
  <action>
    Execute the four verification queries from `80-VALIDATION.md` against mcp__supabase-local. Capture the raw output of each into the SUMMARY for audit. Do NOT modify any data.

    Query A — Full coverage summary (SC-1 + SC-2):
    ```sql
    SELECT
      p.full_name,
      p.external_id,
      COUNT(pa.id) AS stance_count
    FROM essentials.politicians p
    LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
    WHERE (p.external_id BETWEEN -4102006 AND -4100001
        OR p.external_id BETWEEN -690020 AND -690001)
      AND p.external_id NOT IN (-690003, -690004, -4101001, -4101002)  -- skip appointed + already-done senators
      AND p.is_appointed = false
    GROUP BY p.full_name, p.external_id
    ORDER BY p.external_id;
    ```
    Expected: 25 rows. Gate: all 5 executives stance_count > 0 AND all 6 House reps stance_count > 0 AND ≥ 9 of 13 Portland stance_count > 0.

    Query B — Value range integrity (D-07):
    ```sql
    SELECT pa.value, COUNT(*)
    FROM inform.politician_answers pa
    JOIN essentials.politicians p ON p.id = pa.politician_id
    WHERE p.external_id BETWEEN -4102006 AND -690001
    GROUP BY pa.value
    ORDER BY pa.value;
    ```
    Expected: ONLY rows with value ∈ {1,2,3,4,5}. Gate: if any row has value outside 1–5, FAIL the phase.

    Query C — Citation coverage (D-06):
    ```sql
    SELECT COUNT(*) AS uncited_answers
    FROM inform.politician_answers pa
    JOIN essentials.politicians p ON p.id = pa.politician_id
    WHERE p.external_id BETWEEN -4102006 AND -690001
      AND NOT EXISTS (
        SELECT 1 FROM inform.politician_context pc
        WHERE pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
      );
    ```
    Expected: 0. Gate: any uncited answer row is a D-06 violation.

    Query D — Nyquist spot-check counts (from 80-VALIDATION.md):
    ```sql
    SELECT p.full_name, COUNT(pa.id) AS stances
    FROM essentials.politicians p
    LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
    WHERE p.id IN (
      '66c3bd97-94d1-4287-b1b8-86605a38cb97',  -- Kotek
      'f6202cef-4e46-4db5-a9c0-c69ac9a8eccd',  -- Hoyle
      '60fa9870-d984-46a7-a6ed-5f6fbebe72ce'   -- Ryan
    )
    GROUP BY p.full_name;
    ```
    Expected: Kotek ≥ 10; Hoyle ≥ 8; Ryan ≥ 3.

    Write the raw output of all 4 queries into `.planning/phases/80-or-compass-stances/80-04-SUMMARY.md` under a `## Verification Queries` section. Record PASS/FAIL for each gate.

    If any gate FAILS, halt and return failure context to the orchestrator — do NOT proceed to Task 2 until gaps are closed (likely via `/gsd-plan-phase --gaps`).

    Implements verification for SC-1, SC-2, D-06, D-07.
  </action>
  <verify>
    <automated>The four SQL queries (A, B, C, D) execute successfully and the SUMMARY.md file contains their raw output plus PASS/FAIL annotations.</automated>
  </verify>
  <acceptance_criteria>
    - `80-04-SUMMARY.md` exists at `.planning/phases/80-or-compass-stances/80-04-SUMMARY.md`
    - SUMMARY contains the raw output of all 4 queries (A, B, C, D)
    - SUMMARY records PASS/FAIL for each gate
    - Query A: 25 rows; all 5 execs > 0; all 6 House reps > 0; ≥ 9 Portland > 0
    - Query B: only values {1,2,3,4,5} appear
    - Query C: returns 0 (all answers cited)
    - Query D: Kotek ≥ 10, Hoyle ≥ 8, Ryan ≥ 3
  </acceptance_criteria>
  <done>All 4 automated SQL gates run and their results documented in SUMMARY with PASS/FAIL annotations; phase progression depends on all 4 PASSing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies compass renders on Tina Kotek's profile (SC-4)</name>
  <what-built>
    Tina Kotek (Governor of Oregon, politician UUID `66c3bd97-94d1-4287-b1b8-86605a38cb97`) has had ≥10 compass stances ingested into `inform.politician_answers` by Plan 80-01 Task 1. Per `src/components/CompassCard.jsx`, the CompassCard renders only when the politician's UUID is in `politicianIdsWithStances` returned from `GET /compass/politicians` — Kotek now qualifies.

    The compass widget should render on Kotek's profile page as a radar chart with stance values for the topics researched, plus a StanceAccordion listing the topics with values.
  </what-built>
  <how-to-verify>
    1. Open the Essentials app at its current URL (dev: http://localhost:5173, prod: production URL on Render).
    2. Navigate to `/politician/66c3bd97-94d1-4287-b1b8-86605a38cb97` (Tina Kotek profile).
    3. Confirm the CompassCard widget is visible on the profile (not null, not blank). The radar chart should render with multiple spokes populated.
    4. Confirm the StanceAccordion (or equivalent stance list) appears with at least 8 topics having values, including a citation/note for each (from `inform.politician_context`).
    5. Open browser DevTools → Console; confirm no errors related to compass rendering or missing data.
    6. (Optional) Repeat step 2 with the Dan Ryan profile (`/politician/60fa9870-d984-46a7-a6ed-5f6fbebe72ce`) to confirm compass also renders for a Portland council member.

    Report:
    - Whether the CompassCard rendered on Kotek's profile
    - How many topics appeared in the StanceAccordion
    - Any console errors observed
    - Whether the optional Ryan check (if performed) also rendered

    This satisfies SC-4: "The compass renders on politician profile pages for at least one OR official without errors."
  </how-to-verify>
  <resume-signal>Type "approved — kotek compass renders" if the compass widget displays correctly with stance topics. Otherwise describe what you saw (blank widget, console error, missing accordion, etc.) so a gap closure plan can be filed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Audit one-at-a-time process compliance (SC-3) and close the phase</name>
  <files>
    .planning/phases/80-or-compass-stances/80-04-SUMMARY.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/phases/80-or-compass-stances/80-01-SUMMARY.md
    .planning/phases/80-or-compass-stances/80-02-SUMMARY.md
    .planning/phases/80-or-compass-stances/80-03-SUMMARY.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </read_first>
  <action>
    Step 1 — Process audit for SC-3 (one-at-a-time rule from D-05):
    Read 80-01-SUMMARY.md, 80-02-SUMMARY.md, and 80-03-SUMMARY.md. Each must contain a one-at-a-time confirmation line. Sum the total number of sequential research runs across all three plans (expected: 5 + 6 + 13 = 24 research runs across 25 officials; Senators Wyden + Merkley were pre-completed, not researched in Phase 80).

    Append a `## SC-3 Process Audit` section to `80-04-SUMMARY.md` recording:
    - Total research runs executed during Phase 80 (target: 24)
    - Confirmation that each plan's SUMMARY documents sequential execution
    - PASS/FAIL for SC-3

    Step 2 — Append `## Success Criteria Final Status` table to `80-04-SUMMARY.md`:
    | ID | Criterion | Status | Evidence |
    | SC-1 | OR constitutional officers + federal officials with verifiable record have ≥1 stance | PASS/FAIL | Query A row counts |
    | SC-2 | Portland council officials with discoverable record have stances | PASS/FAIL | Query A Portland subset |
    | SC-3 | All ingestion ran one-at-a-time | PASS/FAIL | Process audit above |
    | SC-4 | Compass renders for ≥1 OR official | PASS/FAIL | Task 2 human-verify result |

    Step 3 — If all 4 success criteria PASS, update `.planning/ROADMAP.md`:
    - Change Phase 80 plan count from `0/TBD` to `4/4`
    - Change Phase 80 status from `Pending` to `Complete`
    - Set completion date to today (2026-05-30 or current date)
    - Update the Phase 80 Plans block to mark all 4 plans as `[x]`

    Step 4 — Update `.planning/STATE.md`:
    - Append Phase 80 completion entry to `## Accumulated Context` → `Roadmap Evolution`
    - Update `Current Position` block: Phase 80 → Complete; next is Phase 81
    - Update `last_updated` and `last_activity` timestamps in frontmatter

    Step 5 — If any success criterion FAILS, do NOT update ROADMAP/STATE — instead, write a `## Gaps Found` section in SUMMARY listing which criteria failed and what evidence is missing, then return failure to orchestrator so `/gsd-plan-phase --gaps` can be invoked.

    Implements SC-3 audit and phase closure for all 4 SCs.
  </action>
  <verify>
    <automated>Grep checks: `grep -c "Phase 80" .planning/ROADMAP.md` shows the phase entry remains; `grep -E "^\| 80\." .planning/ROADMAP.md` shows the Phases table row updated to `4/4 | Complete`; `grep -c "Phase 80" .planning/STATE.md` shows new accumulated-context entries present.</automated>
  </verify>
  <acceptance_criteria>
    - `80-04-SUMMARY.md` contains `## SC-3 Process Audit` section with total research-run count and PASS/FAIL
    - `80-04-SUMMARY.md` contains `## Success Criteria Final Status` table with all 4 SCs annotated
    - If all 4 SCs PASS: `.planning/ROADMAP.md` shows Phase 80 as `Complete` with `4/4` plans and a completion date; all 4 plan checkboxes marked `[x]`
    - If all 4 SCs PASS: `.planning/STATE.md` `last_updated` timestamp matches today; new `Roadmap Evolution` entry for Phase 80 completion present
    - If any SC FAILS: SUMMARY contains `## Gaps Found` section; ROADMAP/STATE NOT modified
  </acceptance_criteria>
  <done>Phase 80 is closed: SUMMARY documents all 4 success-criterion results; if PASS, ROADMAP + STATE reflect completion; if FAIL, gap-closure path is clearly documented.</done>
</task>

</tasks>

<verification>
The 3 tasks together constitute the verification of all 4 success criteria:
- Task 1 (automated SQL gates) verifies SC-1, SC-2, D-06, D-07
- Task 2 (human compass render) verifies SC-4
- Task 3 (process audit + roadmap update) verifies SC-3 and closes the phase

The final state: `80-04-SUMMARY.md` is a single source of truth showing all queries run, all gates PASS/FAIL, and final phase status. ROADMAP + STATE updated only if all gates PASS.
</verification>

<success_criteria>
- [ ] All 4 SQL gates from Task 1 run and recorded in SUMMARY with PASS/FAIL
- [ ] Human-verified compass render result captured (SC-4)
- [ ] One-at-a-time process audit completed and documented (SC-3)
- [ ] ROADMAP.md + STATE.md updated to Phase-80-Complete state (only if all 4 SCs PASS)
- [ ] SUMMARY contains a final SC table with status for all 4 criteria
</success_criteria>

<output>
`.planning/phases/80-or-compass-stances/80-04-SUMMARY.md` is the verification document. It must contain:
- `## Verification Queries` — raw output of Queries A, B, C, D and PASS/FAIL gates
- `## Human Compass Render Check` — outcome of Task 2 human-verify
- `## SC-3 Process Audit` — total research runs + one-at-a-time confirmation
- `## Success Criteria Final Status` — table with all 4 SCs annotated PASS/FAIL
- `## Gaps Found` (only if any SC fails) — what is missing and what to plan next
</output>
