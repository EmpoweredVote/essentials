---
phase: 208-educators-judges-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/Results.jsx]
autonomous: true
requirements: [TAB-01, TAB-02, TAB-03]
user_setup: []

must_haves:
  truths:
    - "A user can switch among Representatives, Educators, and Judges tabs alongside Elections (order: Representatives · Educators · Judges · Elections)"
    - "The Educators tab lists only classifyBucket(pol)==='educator' office-holders; the Judges tab lists only classifyBucket(pol)==='judge' office-holders"
    - "School-board and judicial office-holders no longer appear under Representatives (that list is decluttered)"
    - "An Educators or Judges tab button is HIDDEN entirely (not greyed/disabled) when the location has 0 office-holders of that bucket"
    - "Representatives tab always shows; Elections tab keeps its existing show/hide logic"
    - "A stale or in-session-emptied ?view=judges / ?view=educators (or any unknown ?view= value) falls back to Representatives without rendering a blank tab"
    - "The Elections tab label is plain 'Elections' (no suffix, no day-badge); the election summary + yellow 'N days away' pill render once in the location-header chip row, visible across all four tabs"
    - "The compass-mode control (compassTopSlot) renders and works on all three people-tabs"
  artifacts:
    - path: "src/pages/Results.jsx"
      provides: "Four-tab officials view with classifyBucket-driven partition, hide-when-empty tabs, fallback, and relocated election summary"
      contains: "classifyBucket"
  key_links:
    - from: "src/pages/Results.jsx"
      to: "src/lib/classify.js classifyBucket"
      via: "import + partition of deduped into three buckets"
      pattern: "classifyBucket"
    - from: "src/pages/Results.jsx tab buttons"
      to: "switchView"
      via: "onClick={() => switchView('educators'|'judges')}"
      pattern: "switchView\\('(educators|judges)'\\)"
    - from: "src/pages/Results.jsx render"
      to: "per-bucket filtered hierarchy"
      via: "renderPeopleTab called for representatives/educators/judges"
      pattern: "renderPeopleTab"
---

<objective>
Add Educators and Judges tabs to the officials view in `src/pages/Results.jsx`, beside the existing
Representatives and Elections tabs, routing every office-holder into its tab via `classifyBucket`
(Phase 207). School-board and judicial office-holders leave the Representatives list and surface only
under their own tab. Empty Educators/Judges tabs are hidden entirely; the active tab falls back to
Representatives when its bucket is empty (including stale `?view=`). The Elections tab label collapses
to plain "Elections" and its summary + day-pill relocate to the location-header row.

Purpose: Declutter Representatives (fixes "wade through every LA school-board district") and give
school-board/judicial office-holders first-class, own-tab surfacing — TAB-01, TAB-02, TAB-03.
Output: A single modified file, `src/pages/Results.jsx`, building clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/208-educators-judges-tabs/208-CONTEXT.md
@.planning/phases/208-educators-judges-tabs/208-PATTERNS.md

<interfaces>
<!-- The single source of truth for tab routing. Consumed read-only; do NOT re-implement. -->

From src/lib/classify.js (L306):
```
export function classifyBucket(pol) -> 'representative' | 'educator' | 'judge'
```
Null-safe: a null/missing pol returns 'representative'. Already exported. The module is already
imported in Results.jsx at line 5 as `import { computeVariant } from '../lib/classify';` — EXTEND
that existing import line to also import `classifyBucket`; do NOT add a second import statement.

From src/lib/groupHierarchy.js:
```
groupIntoHierarchy(politicians: Array) ->
  Array<{ tier, bodies: Array<{ key, title, url, subgroups: Array<{ key, label, url, pols }> }> }>
```
Runs its own internal dedup; safe to call on any subset. TIER_ORDER = ['Local','School','State','Federal'].

Existing Results.jsx anchors (verified zero-drift in 208-PATTERNS.md):
- :373  `const activeView = searchParams.get('view') || 'representatives';`
- :932  `switchView(view)` — already generic; fires posthog `essentials_tab_switched {from,to}`
- :1000 `matchesAppointedFilter(pol, filter)` — bucket-agnostic; reuse unchanged
- :1253 `electionsLabelSuffix` useMemo (produces e.g. "California General · Nov 3, 2026")
- :1302 `electionsDaysAway` useMemo
- :1340 `deduped` useMemo  → :1350 `hierarchy`  → :1355 `filteredHierarchy` (appointed-filter layer)
- :1742 collapsed location-chip header row (pin + address + Tribal-Land badge + edit pencil)
- :1931-1982 tab-row JSX (Representatives + Elections buttons + FilterBar)
- :1953-1967 in-tab Elections label + yellow day-badge (the block that moves)
- :1984-2138 Representatives render block; :2139-2173 Elections branch (`) : ( ... )`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bucket-partition data layer + empty-bucket flags + active-tab fallback</name>
  <files>src/pages/Results.jsx</files>
  <read_first>
    - src/pages/Results.jsx (whole file — read once; you will edit it across all 3 tasks)
    - src/lib/classify.js lines 280-326 (classifyBucket signature, return values, null-safety)
    - .planning/phases/208-educators-judges-tabs/208-PATTERNS.md sections 1 and 2 (tab mechanism + bucket partition, with verified line numbers and the exact useMemo shapes)
    - .planning/phases/208-educators-judges-tabs/208-CONTEXT.md D-06, D-08, D-09, D-11
  </read_first>
  <action>
    Extend the existing classify import at line 5 to `import { computeVariant, classifyBucket } from '../lib/classify';` (single line, no second import).

    Immediately after the `deduped` useMemo (ends ~:1348) and before `hierarchy` (:1350), add a
    `bucketed` useMemo that partitions `deduped` into `{ representative: [], educator: [], judge: [] }`
    by pushing each pol into `buckets[classifyBucket(pol)]`, keyed on `[deduped]`. This is the ONLY
    place classifyBucket is called — never add a parallel keyword check (per 207-D-06 / D-06:
    tab membership must not drift from list grouping).

    Rewrite the existing `hierarchy` useMemo to build from `bucketed.representative` instead of
    `deduped`, and add two sibling useMemos `educatorsHierarchy = groupIntoHierarchy(bucketed.educator)`
    and `judgesHierarchy = groupIntoHierarchy(bucketed.judge)`, each keyed on `[bucketed]`.

    Generalize the appointed-filter layer (the existing `filteredHierarchy` at :1355-1370, D-11 kept
    as-is) into a small module-scope helper `applyAppointedFilter(hierarchy, appointedFilter)` that
    contains the current map/filter body verbatim (reusing `matchesAppointedFilter` unchanged), then
    derive `filteredHierarchy`, `educatorsFilteredHierarchy`, `judgesFilteredHierarchy` as three
    useMemos each calling the helper on its bucket's hierarchy. Preserve the existing
    `appointedFilter === 'All'` short-circuit inside the helper.

    Add empty-bucket booleans computed from `bucketed` PRE-appointed-filter (per D-05: "0 office-holders
    for the location", not "0 after the appointed filter"): `const hasEducators = bucketed.educator.length > 0;`
    and `const hasJudges = bucketed.judge.length > 0;`.

    Add the D-08 fallback. Define the valid tab set as a constant (e.g. `const PEOPLE_TABS = ['representatives','educators','judges'];` plus 'elections') and derive `effectiveActiveView`
    AFTER `bucketed`: if `activeView` is 'educators' but `!hasEducators`, or 'judges' but `!hasJudges`,
    or `activeView` is not one of the four known values, resolve to 'representatives'. Use
    `effectiveActiveView` (not raw `activeView`) for all render-branch decisions in Tasks 2/3. This
    single rule covers both the in-session location change that empties the current tab AND a
    stale/bookmarked `?view=` value (D-08 security: never trust the raw param — validate against the
    known set, fall back to representatives).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (vite build clean; prebuild coverage gen runs)
    - `grep -n "classifyBucket" src/pages/Results.jsx` shows the import extension AND the partition useMemo (classifyBucket called in exactly the one partition site)
    - Source contains `educatorsHierarchy`, `judgesHierarchy`, `educatorsFilteredHierarchy`, `judgesFilteredHierarchy`, `hasEducators`, `hasJudges`, and `effectiveActiveView`
    - `hasEducators`/`hasJudges` are derived from `bucketed.*.length` (pre-appointed-filter), NOT from the filtered hierarchies
    - No parallel keyword/district_type check for bucket membership exists anywhere else in Results.jsx (grep for `school board`/`JUDICIAL` used for routing returns only pre-existing non-routing usages)
    - `effectiveActiveView` resolves any value not in {representatives, educators, judges, elections} to 'representatives'
  </acceptance_criteria>
  <done>
    Results.jsx builds clean with a classifyBucket-driven three-way partition of `deduped`, three per-bucket
    hierarchies and appointed-filtered hierarchies, hasEducators/hasJudges flags, and an effectiveActiveView
    that falls back to Representatives for empty or unknown tabs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract renderPeopleTab helper and render all three people-tabs with full parity</name>
  <files>src/pages/Results.jsx</files>
  <read_first>
    - src/pages/Results.jsx lines 1984-2173 (Representatives render block + Elections branch; the ternary to restructure)
    - .planning/phases/208-educators-judges-tabs/208-PATTERNS.md section 5 (render-block reuse, the per-tab scaffolding notes, and the recommended renderPeopleTab(hierarchy, fallbackListLength, viewName) shape)
    - .planning/phases/208-educators-judges-tabs/208-CONTEXT.md D-09, D-10
  </read_first>
  <action>
    Extract the Representatives render block (the `<>...</>` fragment currently at :1985-2138) into an
    in-component helper `renderPeopleTab(hier, fallbackListLength, viewName)` that returns the same JSX
    parameterized on the passed hierarchy. Inside it: use `hier` in place of `filteredHierarchy` for the
    tier `.map` (:2043), the tier-empty `.some` check (:2019-2034), and the filter-aware empty state
    (:2130-2135); use `fallbackListLength` in place of `federalFiltered.length` for the zero-results
    fallback (:2123). Keep `GovernmentBodySection`/`SubGroupSection`/`renderSeatGroup`/`SectionBanner`/
    `tierColors`/`buildBannerProps` usage byte-identical so all three tabs look and behave identically (D-09).

    D-10: `compassTopSlot` (:2004) must render inside every people-tab — keep it in the helper so the
    compass-mode control stays present and working on Educators/Judges too. (Do NOT do the per-tab
    default-lens shift — that is Phase 210.)

    Guard bucket-specific wording: the tier-empty message at :2027 says "{tier} representative data is
    not yet available" — that "representative" wording must NOT leak onto the Educators/Judges tabs.
    Either gate those tier-empty messages to `viewName === 'representatives'` only, or make the wording
    generic ("officials"). Since D-05 hides fully-empty Educators/Judges tabs, this message path is rare
    but must not display "representative data" on a Judges/Educators tab.

    Replace the binary `{activeView === 'representatives' ? (<>...</>) : (<div>elections</div>)}` ternary
    with per-tab conditional rendering driven by `effectiveActiveView` (from Task 1):
    representatives → `renderPeopleTab(filteredHierarchy, federalFiltered.length, 'representatives')`;
    educators → `renderPeopleTab(educatorsFilteredHierarchy, bucketed.educator.length, 'educators')`;
    judges → `renderPeopleTab(judgesFilteredHierarchy, bucketed.judge.length, 'judges')`;
    elections → the existing Elections `<div>` block (:2140-2172) unchanged. The location-level error
    banner (:1988) and precision banner (:1997) are representatives-appropriate only — keep them inside
    the representatives path (they are already shown once per location).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `npm run lint` reports no new errors in src/pages/Results.jsx
    - `grep -n "renderPeopleTab" src/pages/Results.jsx` shows the definition plus exactly three call sites (representatives, educators, judges)
    - The render branch keys off `effectiveActiveView`, not raw `activeView`
    - The Elections branch JSX (ElectionsView, VoterResourcesCard) is unchanged
    - No literal "representative data" string can render on the educators/judges tabs (message gated to representatives or reworded generically)
    - renderPeopleTab renders `compassTopSlot` (grep shows compassTopSlot inside the helper body)
  </acceptance_criteria>
  <done>
    A single renderPeopleTab helper drives Representatives, Educators, and Judges with identical pipeline
    behavior (tiers, banners, cards, compass slot), fed each tab's own filtered hierarchy; the Elections
    branch is untouched; the build and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Four-button tab row (hide-when-empty) + relocate election summary to header</name>
  <files>src/pages/Results.jsx</files>
  <read_first>
    - src/pages/Results.jsx lines 1742-1778 (location-header chip row — relocation destination) and 1931-1982 (tab row + Elections button/day-badge — source)
    - .planning/phases/208-educators-judges-tabs/208-PATTERNS.md sections 3 and 4 (tab-row extension + election-summary relocation, with the exact className boilerplate and badge markup)
    - .planning/phases/208-educators-judges-tabs/208-CONTEXT.md D-01, D-02, D-03, D-04, D-05
  </read_first>
  <action>
    D-01 order + D-05 hide-when-empty: in the tab-row `<div className="flex">` (:1934), insert an
    Educators button and a Judges button BETWEEN the Representatives button (ends :1944) and the
    Elections button (starts :1945). Wrap each new button in `{hasEducators && (...)}` /
    `{hasJudges && (...)}` — conditional render, NOT a `disabled` prop (D-06 supersedes the greyed-out
    wording; empty tabs are absent, never dead). Each button's onClick calls `switchView('educators')`
    / `switchView('judges')`; active state compares `effectiveActiveView === '...'`. This automatically
    extends the `essentials_tab_switched` posthog event (:933) to the new values — no analytics code
    change needed. Factor the repeated active/inactive className ternary (identical across all four
    buttons) into a small local helper `tabButtonClass(isActive)` to avoid 4x copy-paste drift.

    D-02: simplify the Elections button to a plain "Elections" label — remove the
    `electionsLabelSuffix ? \`Elections - ${...}\`` conditional and REMOVE the yellow day-badge JSX
    (:1957-1967) from the button entirely. Both the `sm:hidden` and `hidden sm:inline` spans collapse
    to plain "Elections".

    D-03: relocate the election summary into the collapsed location-header chip row (:1742-1777), placing
    a new block after the address `<span>` (:1750) — either before or after the Tribal-Land badge, before
    the edit-pencil button (:1763). Render `Elections - ${electionsLabelSuffix}` as text plus the yellow
    `{electionsDaysAway}` pill, copied verbatim from the moved badge markup (same #FED12E / #1a1a1a
    colors, same `Nd` mobile-compaction regex, same sm:hidden/hidden-sm:inline split). Guard the whole
    block on `electionsLabelSuffix` truthy (mirroring the old tab-button conditional). `electionsLabelSuffix`
    (:1253) and `electionsDaysAway` (:1302) are both defined above this JSX — no hook reordering needed.
    It renders once at location level and stays visible across all four tabs.

    D-04 mobile fit (~280-375px): mirror the existing Elections-button compact-label convention
    (`<span className="sm:hidden">` short label vs `<span className="hidden sm:inline">` full label) for
    the Educators/Judges buttons if labels are tight, and add `overflow-x-auto` to the tab-row flex
    container if four tabs still don't fit at 280px. All tabs must stay reachable. Exact short strings
    and where scroll kicks in are your discretion — tune against the real narrow render.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -n "switchView('educators')" src/pages/Results.jsx` and `grep -n "switchView('judges')" src/pages/Results.jsx` each return exactly one button
    - The Educators button is wrapped in `hasEducators &&` and the Judges button in `hasJudges &&` (conditional render); neither uses a `disabled` attribute
    - Tab DOM order is Representatives, Educators, Judges, Elections
    - The Elections button no longer contains `electionsLabelSuffix` or the `#FED12E` day-badge; grep for `#FED12E` now finds it only in the location-header chip block (around :1742-1777), not the tab row
    - The relocated election summary block is guarded on `electionsLabelSuffix` and references `electionsDaysAway`
    - `tabButtonClass` helper exists and is used by all four tab buttons
  </acceptance_criteria>
  <done>
    The tab row shows Representatives · Educators · Judges · Elections, with Educators/Judges hidden when
    empty; the Elections tab is a plain "Elections" label; the election summary + yellow day-pill now live
    in the location-header chip row and persist across all tabs; mobile labels/scroll keep every tab
    reachable at 280px; the build passes.
  </done>
</task>

</tasks>

<threat_model>
Frontend-only phase: no new backend, DB, API, or package installs. It re-partitions and re-renders data
already returned to the client. The only client-controlled surface is the `?view=` URL param.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?view=` param → tab render | User/bookmark-controlled string selects which tab renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-208-01 | Tampering | `?view=` param in Results.jsx (:373) | mitigate | Validate against the known tab set {representatives, educators, judges, elections}; any unknown/stale value resolves to 'representatives' via `effectiveActiveView` (D-08). Never render off the raw param. |
| T-208-02 | Denial of Service | stale `?view=judges` for a no-judges location | mitigate | Same fallback path renders Representatives rather than a blank/gone tab; empty people-buckets never crash (classifyBucket is null-safe, groupIntoHierarchy handles empty arrays) |
| T-208-03 | Information Disclosure | bucket partition | accept | No new data crosses the boundary — same office-holder list already returned to the client, only re-grouped client-side |
| T-208-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase adds zero dependencies; no install tasks, no package-legitimacy gate required |
</threat_model>

<verification>
- `npm run build` exits 0 after each task and at plan end.
- `npm run lint` reports no new errors in src/pages/Results.jsx.
- classifyBucket is called in exactly one partition site; no parallel routing logic.
- Tab order, hide-when-empty, plain Elections label, and relocated summary confirmed by the grep
  assertions above. Full visual/behavioral verification is Plan 208-02 (human-verify checkpoint).
</verification>

<success_criteria>
- TAB-01: Representatives, Educators, and Judges tabs are switchable alongside Elections.
- TAB-02: school-board (educator bucket) and judicial (judge bucket) office-holders no longer appear
  under Representatives.
- TAB-03 (as revised by D-06): Educators/Judges tabs are hidden entirely when their bucket is empty;
  the active tab falls back to Representatives for empty or unknown `?view=`.
- Elections label is plain "Elections"; the election summary + day-pill render in the location header.
</success_criteria>

<output>
Create `.planning/phases/208-educators-judges-tabs/208-01-SUMMARY.md` when done.
</output>
