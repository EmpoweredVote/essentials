---
phase: 181-city-of-sherwood-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["181-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-sherwood-headshots.py
  - C:/EV-Accounts/backend/migrations/1188_sherwood_headshots.sql
autonomous: false
requirements: [WASH-07]

must_haves:
  truths:
    - "Each of the 7 Sherwood officials has a 600×750 headshot uploaded to Supabase Storage politician_photos/{uuid}-headshot.jpg — RESEARCH confirmed 7/7 direct-download sourcing from sherwoodoregon.gov (the BEST in the milestone), so a full 7/7 is expected and any shortfall is a regression to investigate, not a planned gap"
    - "Every headshot was cropped to 4:5 FIRST, then resized to 600×750 Lanczos q90 (no distortion, no overlays)"
    - "The SQUARE-source crop nuance is handled correctly: all 7 sources are 600×600 (1:1), so the crop step trims WIDTH down (600→480 centered on the face) rather than the usual height trim, then upscales the 480×600 crop to 600×750"
    - "Sourcing is direct from sherwoodoregon.gov static <img> tags (wp-content/uploads/2025/02/<firstname>-<lastname>-600.jpg) — NO D-16 fallback chain is expected to be needed for any official"
    - "One politician_images row per official (type='default', photo_license='press_use' — uniform official-site studio portraits), columns exactly (id, politician_id, url, type, photo_license) — NO photo_origin_url; guarded WHERE NOT EXISTS on politician_id"
    - "No image has superimposed text/graphics over the face; each is a valid non-zero JPEG (HTTP 200 from CDN)"
    - "WR-01 (already shipped in the 180 template): the pipeline script main() EXITS NON-ZERO if ANY configured official failed to upload — a chained automation cannot apply the audit migration on a silently-partial run"
    - "D-15 WR-C: the headshot script's main() guards `if len(OFFICIALS) > 0:` before calling test_download_guard(OFFICIALS[0]) (the live _tmp-forest-grove-headshots.py had no length guard — do not repeat)"
    - "The headshot script hard-asserts len(OFFICIALS)==7 (RESEARCH confirmed 7/7 direct sourcing with no genuine gap expected) AND asserts ext_ids are unique"
    - "D-15 WR-A: the audit migration's ORCHESTRATOR NOTE text is kept in sync with the actual post-verify gate default (the live 1179 note drifted — said 'currently 0' while the applied gate read IF n <> 7; do not repeat)"
    - "The audit-only headshot migration post-verify asserts 7 politician_images rows whose url embeds the politician uuid"
    - "Decision coverage — D-09: headshots crop-to-4:5 then resize 600×750 Lanczos q90, {uuid}-headshot.jpg naming, photo_license='press_use' (government-hosted), no fabrication; D-15 WR-A (note-text sync) + WR-C (empty-roster guard); WR-01 non-zero exit already shipped"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-sherwood-headshots.py"
      provides: "Download → composite → crop 4:5 (width-trim on square source) → resize 600×750 → upload pipeline with the WR-01 non-zero-exit + WR-C empty-roster guard"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/1188_sherwood_headshots.sql"
      provides: "politician_images INSERTs for all 7 officials (audit-only, NOT registered in the ledger) with the WR-A-synced note + url-embeds-uuid count=7 gate"
      min_lines: 20
      contains: "politician_images"
  key_links:
    - from: "_tmp-sherwood-headshots.py"
      to: "Supabase Storage politician_photos bucket"
      via: "PUT {uuid}-headshot.jpg x-upsert:true"
      pattern: "politician_photos"
    - from: "1188_sherwood_headshots.sql"
      to: "essentials.politicians (by external_id)"
      via: "subselect politician_id per row"
      pattern: "external_id = -41671"
---

<objective>
Author the headshot pipeline script and the audit-only migration that records the Sherwood officials'
portraits. Sherwood has the BEST headshot sourcing in the milestone: all 7 portraits are static <img>
tags on sherwoodoregon.gov (no WAF, no JS/AJAX gate, no bulk-portal-absence gap) — expect a full 7/7,
NO D-16 fallback chain needed. One genuinely new pipeline nuance: all 7 source images are square
(600×600), so cropping to the 4:5 target trims WIDTH down (600→480 centered on the face) rather than
the usual height trim, then upscales to 600×750. Portraits are cropped 4:5 FIRST then resized. Carry
the already-shipped WR-01 fix (main() exits non-zero on ANY upload failure) and add the D-15 WR-C fix
(guard len(OFFICIALS)>0 before indexing) + the D-15 WR-A fix in the migration note.

Purpose: Satisfy the headshot portion of WASH-07 — all 7 officials render with 600×750 photos.
Output: `_tmp-sherwood-headshots.py` (gitignored helper, run by the orchestrator, WR-01 + WR-C fixed) +
audit-only migration `1188_sherwood_headshots.sql` (WR-A-synced note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES both files. It has NO DB/Storage access and NO
WebSearch. The INLINE ORCHESTRATOR runs `_tmp-sherwood-headshots.py` (which resolves each politician
UUID by external_id at runtime via psycopg2, downloads each portrait, processes, and uploads), then
applies the audit migration via `psql -f`. The `.py` file is a gitignored `_tmp-*` helper — do NOT
commit it. The `.sql` migration IS committed to the EV-Accounts repo (`git -C "C:/EV-Accounts"` — never
cd into it).

**AUDIT-ONLY MIGRATION:** `1188_sherwood_headshots.sql` does NOT write a schema_migrations ledger row
(headshot/stance migrations bypass the ledger; on-disk counter is authoritative). Keep the literal
strings `slug`, `schema_migrations`, and `photo_origin_url` OUT of comments.
</context>

<interfaces>
essentials.politician_images  (id, politician_id, url, type, photo_license)  -- NO photo_origin_url column; type='default'
-- politician UUIDs: resolve by external_id from 181-02-SUMMARY.md:
--   -4167101 Tim Rosener (Mayor) | -4167102 Kim Young (Council President) | -4167103 Renee Brouse
--   -4167104 Taylor Giles | -4167105 Keith Mays | -4167106 Doug Scott | -4167107 Dan Standke
-- Headshot sources (all confirmed HTTP 200, 600×600 JPEG, at RESEARCH time; re-confirmed at Wave-0):
--   tim-rosener-600.jpg | kim-young-600.jpg | renee-brouse-600.jpg | taylor-giles-600.jpg
--   keith-mays-600.jpg | doug-scott-600.jpg | daniel-standke-600.jpg
--   base: https://www.sherwoodoregon.gov/wp-content/uploads/2025/02/
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot pipeline script (WR-01 exit + WR-C empty-roster guard, square-source crop)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-sherwood-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-forest-grove-headshots.py (the ~500-line pipeline template — copy structure/functions verbatim, rename city strings; gitignored but present on disk. NOTE its main() already carries the WR-01 non-zero-exit fix; its line ~455 call test_download_guard(OFFICIALS[0]) has NO length guard — that is the WR-C defect to fix)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md §"_tmp-sherwood-headshots.py (utility, file-I/O)" (incl. the module docstring with the square-source nuance, the OFFICIALS roster with all 7 confirmed URLs, the WR-C-fixed main() guard, and the WR-01 exit gate)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md §"Headshot Sources — 7/7 CONFIRMED, NO FALLBACK CHAIN NEEDED"
    - .planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md (Wave-0 re-download confirmations + square-source note) + 181-02-SUMMARY.md (minted UUIDs by external_id)
  </read_first>
  <action>
    Copy the Forest Grove script structure (config block, crop_to_4_5, resize_600x750,
    process_headshot_bytes, transparent-composite guard, resolve_politician_id, download_image,
    upload_to_storage, process_member, manifest output, CDN_BASE-from-SUPABASE_URL derivation,
    descriptive User-Agent, test_download_guard). Set an OFFICIALS list of exactly 7 dicts, each with
    ext_id (-4167101..-4167107), name, url (the confirmed sherwoodoregon.gov URL:
    tim-rosener-600.jpg / kim-young-600.jpg / renee-brouse-600.jpg / taylor-giles-600.jpg /
    keith-mays-600.jpg / doug-scott-600.jpg / daniel-standke-600.jpg under base
    https://www.sherwoodoregon.gov/wp-content/uploads/2025/02/), and license='press_use' for ALL 7
    (uniform official-site studio portraits — government-hosted). Resolve each politician UUID at
    RUNTIME by external_id via psycopg2 (do NOT hardcode UUIDs). Keep the config block:
    BUCKET='politician_photos', TARGET_SIZE=(600,750), JPEG_QUALITY=90,
    RESAMPLE=Image.Resampling.LANCZOS. Pipeline order per official: download (follow redirects),
    composite onto white if RGBA/PNG (not expected for these JPEGs but keep the guard), CROP to 4:5
    FIRST — on these SQUARE 600×600 sources the crop trims WIDTH (600→480 centered on face), NOT the
    usual height trim; verify crop_to_4_5()'s width-trim branch handles the 1:1 input before running
    the full manifest — then RESIZE to 600×750 Lanczos (a 25% upscale, within acceptable precedent),
    reject any image with superimposed text/graphics, upload to
    politician_photos/{uuid}-headshot.jpg via PUT with x-upsert:true, emit a manifest line
    (ext_id → uuid → CDN url → source). HARD-ASSERT len(OFFICIALS)==7 (RESEARCH confirmed 7/7 direct
    sourcing — no genuine gap expected) AND assert ext_ids are unique. Keep the already-shipped WR-01
    exit gate: collect per-official results, print the manifest, and if ANY official failed to upload
    call sys.exit(1). APPLY THE D-15 WR-C FIX to main(): guard `if len(OFFICIALS) > 0:` before calling
    test_download_guard(OFFICIALS[0]) (the inherited script indexed [0] with no length check — an
    IndexError risk on an empty roster). Include a docstring SOURCE NOTE describing the best-in-milestone
    direct-download sourcing (no fallback chain), the square-source crop nuance, the WR-01 exit, and
    marking the file a gitignored _tmp helper the orchestrator runs. No fenced code in this action — the
    Forest Grove analog + 181-PATTERNS.md (which fully specifies the square-source docstring, OFFICIALS
    list, and the WR-C guard) are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `python C:/EV-Accounts/backend/scripts/_tmp-sherwood-headshots.py`; the script validates non-zero JPEG per image, prints the CDN manifest, and EXITS NON-ZERO if any configured upload failed (WR-01)</automated>
  </verify>
  <acceptance_criteria>
    - OFFICIALS list has exactly 7 entries (ext_ids -4167101..-4167107) with the confirmed sherwoodoregon.gov source URLs
    - license='press_use' for all 7 (government-hosted official portraits)
    - hard-asserts len(OFFICIALS)==7 AND asserts unique ext_ids
    - UUIDs resolved at runtime by external_id (no hardcoded UUIDs)
    - TARGET_SIZE=(600,750), JPEG_QUALITY=90, Lanczos resample; crop-to-4:5 happens BEFORE resize; the crop trims WIDTH on the square 600×600 source; rejects overlay/text images
    - main() calls sys.exit(1) when ANY configured official failed to upload (WR-01)
    - main() guards `if len(OFFICIALS) > 0:` before test_download_guard(OFFICIALS[0]) (D-15 WR-C)
    - Docstring SOURCE NOTE records the best-in-milestone direct sourcing (no fallback chain), the square-source crop nuance, and the WR-01 fix; marks it a gitignored _tmp helper run by the orchestrator
  </acceptance_criteria>
  <done>Pipeline script authored with the WR-01 exit + WR-C guard + square-source width-crop handling; runs and uploads 600×750 portraits for all 7 officials, exiting non-zero on any upload failure.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration (WR-A note-text sync)</name>
  <files>C:/EV-Accounts/backend/migrations/1188_sherwood_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1179_forest_grove_headshots.sql (the 141-line template — url-embeds-uuid post-verify gate; NOTE its ORCHESTRATOR NOTE drifted from the applied gate — the exact WR-A defect to fix)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md §"1188_sherwood_headshots.sql (audit-only headshot migration, CRUD)" (incl. the WR-A note-sync rule and the count=7 post-verify gate)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-03 headshot manifest (from Task 1 run — the per-official UUIDs and the actual sourced count, expected 7)
  </read_first>
  <action>
    Single BEGIN...COMMIT. Header comment: "AUDIT-ONLY (not registered in the ledger)" plus a source
    note stating the best-in-milestone direct-download sourcing from sherwoodoregon.gov static <img>
    tags (no WAF, no JS/AJAX gate, no D-16 fallback needed; expect 7/7). SEVEN politician_images
    INSERT ... SELECT blocks (one per official — expect all 7 sourced), columns EXACTLY
    (id, politician_id, url, type, photo_license) — NO photo_origin_url. politician_id resolved via
    subselect on external_id (within -4167101..-4167107). url is the final Supabase CDN path
    (politician_photos/{uuid}-headshot.jpg — the orchestrator fills each {uuid} from the script
    manifest before applying). type='default'. photo_license='press_use' for all 7 (government-hosted).
    Each INSERT guarded WHERE NOT EXISTS on politician_id (idempotent). Append the post-verify DO block
    from mig 1179: count politician_images rows JOINed to politicians WHERE external_id BETWEEN
    -4167107 AND -4167101 AND url LIKE '%' || politician_id::text || '%' — RAISE EXCEPTION if the count
    <> 7. APPLY THE D-15 WR-A FIX: write the ORCHESTRATOR NOTE's stated gate default to match whatever
    the post-verify gate literally says at the moment of authoring — if the gate reads `IF n <> 7`, the
    note must say the expected count is 7 (do NOT leave a stale "currently 0" note as the live 1179
    file did). If the executor writes the gate with a fail-closed 0 default first and edits it to 7,
    the note text MUST be edited in the same file. NO ledger INSERT (audit-only). Keep
    `photo_origin_url`/`slug`/`schema_migrations` out of comments.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1188_sherwood_headshots.sql` (the url-embeds-uuid DO block RAISEs unless 7 rows embed their politician uuid) then runs `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -4167107 AND -4167101)`</automated>
  </verify>
  <acceptance_criteria>
    - Seven politician_images INSERT blocks; columns exactly (id, politician_id, url, type, photo_license)
    - No photo_origin_url column anywhere
    - politician_id resolved via external_id subselect (within -4167101..-4167107)
    - type='default'; photo_license='press_use' for all 7; each guarded WHERE NOT EXISTS on politician_id
    - url-embeds-uuid DO block asserts count = 7
    - ORCHESTRATOR NOTE's stated expected-count matches the gate's actual code (D-15 WR-A — no drift)
    - No ledger INSERT (audit-only); `photo_origin_url` does NOT appear in comments
    - Header source note documents the best-in-milestone direct sourcing (no D-16 fallback)
  </acceptance_criteria>
  <done>Audit migration authored recording all 7 image rows with the WR-A-synced note and the count=7 url-embeds-uuid gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs pipeline, applies migration, verifies identity + quality</name>
  <what-built>The headshot pipeline script (WR-01 + WR-C) + audit migration (WR-A). Orchestrator runs the pipeline (direct sherwoodoregon.gov sourcing) and applies the migration.</what-built>
  <how-to-verify>
    1. Run `python C:/EV-Accounts/backend/scripts/_tmp-sherwood-headshots.py`; confirm each of the 7
       images downloads from sherwoodoregon.gov, crops WIDTH-first to 4:5 on the square source, resizes
       to 600×750, and uploads (HTTP 200 on each CDN URL). The script EXITS NON-ZERO if any upload
       failed (WR-01). A 7/7 outcome is expected — investigate any shortfall as a regression.
    2. Apply `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1188_sherwood_headshots.sql`;
       confirm the url-embeds-uuid DO block passes (count = 7) and the politician_images row count matches.
    3. Visual identity + quality check on each portrait: correct person (Rosener/Young/Brouse/Giles/
       Mays/Scott/Standke), 600×750, 4:5 framing (face centered after the width crop), NO superimposed
       text/graphics over the face. Confirm no wrong-person image.
    4. Commit the migration: `git -C "C:/EV-Accounts" add backend/migrations/1188_sherwood_headshots.sql && git -C "C:/EV-Accounts" commit`. Do NOT commit the _tmp-*.py helper.
  </how-to-verify>
  <acceptance_criteria>
    - Pipeline ran; WR-01 non-zero exit behaves correctly on any failure; all 7 headshots uploaded (7/7 expected)
    - politician_images row count = 7; url-embeds-uuid DO block passed
    - Each portrait is the correct person, 600×750, no overlays, face centered after the width crop
    - Audit migration committed; _tmp .py NOT committed
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official confirmation (7/7), or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Portraits sourced directly from sherwoodoregon.gov static <img> tags (no WAF, no fallback chain) |
| operator → Supabase Storage | Processed images uploaded to the public politician_photos bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-12 | Tampering | Partial-upload false success: a chained automation applies the audit migration after only some uploads succeeded | mitigate | WR-01 (already shipped) — main() calls sys.exit(1) when ANY configured official failed to upload; the audit migration's count=7 gate re-catches a mismatch |
| T-181-13 | Spoofing | Wrong-person or mislabeled headshot | mitigate | URLs are per-official confirmed at Wave-0; Task 3 visual identity check per official before commit; url-embeds-uuid gate ties each row to the right politician uuid |
| T-181-14 | Tampering | Corrupt/0-byte image, or a bad square-source crop that distorts/mis-centers the face | mitigate | Script validates non-zero bytes + valid PIL open; crop-to-4:5 (width-trim) verified against the 1:1 source before the full manifest; visual check confirms centered clean face |
| T-181-15 | Tampering | IndexError on an empty OFFICIALS list at test time | mitigate | D-15 WR-C guard: `if len(OFFICIALS) > 0:` before test_download_guard(OFFICIALS[0]) |
| T-181-16 | Information Disclosure | Superimposed text/graphics on portrait | mitigate | Pipeline rejects images with overlays; visual check confirms clean face |
| T-181-SC | Tampering | npm/pip/cargo installs (Pillow/psycopg2/requests) | mitigate | Pre-existing project deps used by all prior headshot phases — no NEW package install; if any pip install is needed, treat as [ASSUMED] and gate on a blocking human-verify before install |
</threat_model>

<verification>
- Pipeline runs with WR-01 non-zero exit on any failure + WR-C empty-roster guard; each of 7 600×750
  portraits uploaded (width-crop on square source); politician_images rows = 7; url-embeds-uuid gate
  passes (WR-A-synced note); identity/quality confirmed; audit migration committed.
</verification>

<success_criteria>
- All 7 officials have a 600×750 headshot with no overlays, face centered after the square-source
  width crop; the pipeline exits non-zero on any upload failure (WR-01) and guards the empty roster
  (WR-C); audit migration recorded with the count=7 url-embeds-uuid gate and a WR-A-synced note.
</success_criteria>

<output>
Create `.planning/phases/181-city-of-sherwood-deep-seed/181-03-SUMMARY.md` when done, recording the
per-official image source + the 7/7 coverage (or any documented regression).
</output>
