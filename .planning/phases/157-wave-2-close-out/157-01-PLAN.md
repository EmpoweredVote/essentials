---
phase: 157-wave-2-close-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v17.0-MILESTONE-AUDIT.md
autonomous: true
requirements: [LAC2-RETRO-01]

must_haves:
  truths:
    - "A reader can see, per city, the DB-verified gov/chamber/roster/headshot/stance counts for all 15 Wave-2 cities"
    - "Each city carries a structure-hard / data-soft verdict that distinguishes a structural defect (blocker) from an acceptable documented gap"
    - "The exact set of cities with >=1 seeded compass stance is recorded (drives the Wave-2 purple-chip decision)"
    - "The deferred split-section follow-up for the 5 known cities is recorded as a known issue, not a blocker"
    - "The shared groupHierarchy.js Mayor>Mayor-Pro-Tem fix is confirmed committed to main with its production-deploy status documented"
  artifacts:
    - path: ".planning/v17.0-MILESTONE-AUDIT.md"
      provides: "DB-verified per-city audit + verdicts + deferred items + shared-UI-fix note"
      contains: "v17.0"
  key_links:
    - from: ".planning/v17.0-MILESTONE-AUDIT.md"
      to: "production DB (mcp__supabase-local)"
      via: "read-only SELECT verification of every per-city count"
      pattern: "geo_id"
---

<objective>
Write `.planning/v17.0-MILESTONE-AUDIT.md` — a standalone, DB-verified per-city audit of all 15
LA County Wave-2 cities (phases 142–156), matching the format/location of the v16.0/v11.0/v10.0
audits (D-05).

Purpose: This audit is the source of truth for the milestone close. Its DB-verified per-city
stance count drives the Wave-2 purple-chip decision (D-02), and its verdicts gate the close
(structure-hard / data-soft, D-03/D-03a).

Output: `.planning/v17.0-MILESTONE-AUDIT.md` with a per-city count table, per-city verdicts,
the recorded set of >=1-stance cities, the deferred split-section follow-up, and the D-08
shared-UI-fix/deploy note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-wave-2-close-out/157-CONTEXT.md
@.planning/v16.0-MILESTONE-AUDIT.md
</context>

<key_facts>
- `mcp__supabase-local` IS production. ALL queries in this plan are READ-ONLY (SELECT only). No INSERT/UPDATE/DELETE.
- Authoritative tables (per memory `project_schema_key_tables`): `essentials.governments` (gov rows), `essentials.chambers`, `essentials.offices`, `essentials.politicians`, `essentials.districts`, `essentials.politician_images` (headshots), `inform.politician_answers` (stances).
- The 15 cities + geo_ids (CONTEXT.md D-01, use verbatim): Long Beach 0643000 · Santa Clarita 0669088 · Glendale 0630000 · Lancaster 0640130 · Palmdale 0655156 · Pomona 0658072 · Torrance 0680000 · Pasadena 0656000 · Downey 0619766 · El Monte 0622230 · West Covina 0684200 · Inglewood 0636546 · Burbank 0608954 · Norwalk 0652526 · Bellflower 0604982.
- `districts.government_id` is NULL across these rows — join districts via `geo_id`, NOT government_id (D-04a-a; confirmed wave-wide). Governments rows for these cities were bulk-imported with geo_id originally NULL then backfilled per phase; match the gov row by geo_id.
- Most Wave-2 cities were RECONCILES (not greenfield): duplicate-chamber merges + reseating happened. Count only ACTIVE offices on the SURVIVOR chamber; unlinked rows (office_id NULL / is_active=false) are intentionally kept and MUST NOT be counted as live roster (D-04a-b).
- Stance migrations were applied via raw SQL and do NOT register in `supabase_migrations.schema_migrations` — count stance rows from `inform.politician_answers`, not from the ledger.
</key_facts>

<tasks>

<task type="auto">
  <name>Task 1: DB-verify per-city counts for all 15 cities (gotcha-aware, read-only)</name>
  <read_first>
    - .planning/phases/157-wave-2-close-out/157-CONTEXT.md (D-04, D-04a — the five required query dimensions + the four baked-in gotchas)
    - .planning/v16.0-MILESTONE-AUDIT.md (the "Verified DB Metrics" + per-requirement evidence shape to mirror)
    - .planning/REQUIREMENTS.md (per-city geo_ids + per-phase reconcile notes for cross-checking expected rosters)
    - Memory file `feedback_section_split_check` (split-section detection SQL — run per government) at ~/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_section_split_check.md
  </read_first>
  <action>
    Run READ-ONLY (SELECT) queries against `mcp__supabase-local` for each of the 15 geo_ids
    (D-01 list, verbatim). For every city capture the five D-04 dimensions: (1) the
    `essentials.governments` row matched by geo_id; (2) its chamber(s) in `essentials.chambers`
    — flag any city still carrying >1 same-named "City Council" chamber as a structural defect;
    (3) live roster = count of ACTIVE `essentials.offices` on the survivor chamber with a linked
    politician, compared against the government's `official_count` — exclude unlinked/is_active=false
    rows (D-04a-b); (4) headshot count from `essentials.politician_images` for the seated roster;
    (5) stance count from `inform.politician_answers` joined to the seated roster (NOT from the
    migration ledger).
    Bake in the D-04a gotchas explicitly: (a) join `essentials.districts` to offices via geo_id,
    never via government_id (it is NULL); (b) after duplicate-chamber merges, verify you counted the
    SURVIVOR chamber and that unlinked/inactive rows are not miscounted as roster; (c) spot-check the
    three known wrong-person headshot fixes — Glendale, West Covina (Gutierrez), Pomona
    (Gonzalez/Torres) — confirm the current `politician_images` row resolves to the correct person;
    (d) run the per-government split-section detection query from `feedback_section_split_check`
    for each city and record the row count (zero = clean).
    Apply the D-03a roster-currency check: for each city confirm the seated roster reflects
    CURRENT post-June-2026 officeholders (count + structure + mayor type [rotational vs
    directly-elected/LOCAL_EXEC] + district-vs-at-large), cross-referencing the per-phase notes in
    REQUIREMENTS.md and the per-city STATE.md summaries. Any seat whose post-election occupant is
    genuinely uncertain from free sources is a DOCUMENTED GAP, not a blocker.
    Record raw numbers per city in a scratch note for Task 2. Do NOT write any row to the DB.
  </action>
  <verify>
    <automated>MISSING — verification is the read-only DB result set itself; Task 2 asserts the audit file captures it. Confirm 15 distinct geo_ids were queried and each returned a governments row.</automated>
  </verify>
  <acceptance_criteria>
    - All 15 geo_ids from D-01 were queried; each returned exactly one `essentials.governments` row.
    - For each city: chamber count, active-roster count vs official_count, headshot count, and stance count are recorded as concrete integers.
    - The split-section query ran per government and its row count (0 = clean) is recorded for each city.
    - The three wrong-person headshot spot-checks (Glendale, West Covina Gutierrez, Pomona Gonzalez/Torres) are each recorded as resolved-correct or flagged.
    - District joins used geo_id (no query relied on districts.government_id).
    - No write statement (INSERT/UPDATE/DELETE) was issued against the DB.
  </acceptance_criteria>
  <done>
    Raw per-city counts (gov, chamber, roster vs official_count, headshots, stances, split-section
    rows, wrong-person spot-checks, roster-currency verdict) captured for all 15 cities, ready to
    transcribe into the audit doc.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify the D-08 shared UI fix is committed + record its deploy status</name>
  <read_first>
    - src/lib/groupHierarchy.js (read-only — confirm the Mayor>Mayor-Pro-Tem ordering in execTitlePriority and the sort-then-label comment in groupIntoHierarchy)
    - .planning/phases/157-wave-2-close-out/157-CONTEXT.md (D-08 — verify-and-document only; NOT a deploy action, NOT a close blocker)
  </read_first>
  <action>
    Verify the Phase 156 shared-UI fix in `src/lib/groupHierarchy.js` (Mayor > Mayor-Pro-Tem
    ordering + label-from-lead-official) is committed to main: run `git log --oneline -- src/lib/groupHierarchy.js`
    and confirm the fix commit is present (commit subject references ordering Mayor before Mayor Pro
    Tem), and run `git status --short src/lib/groupHierarchy.js` to confirm no uncommitted local
    changes. Capture the commit hash. Then determine whether a production deploy is still pending:
    the fix landed on main during Phase 156 and `156-CONTEXT.md`/memory flagged it "needs deploy";
    this phase does NOT perform the deploy. Record the verified-committed status + the deploy-pending
    note as a DOCUMENTED ITEM for the audit (Task 3). Do NOT modify groupHierarchy.js and do NOT
    trigger any deploy.
  </action>
  <verify>
    <automated>git log --oneline -- src/lib/groupHierarchy.js</automated>
  </verify>
  <acceptance_criteria>
    - The groupHierarchy.js fix commit hash is captured and confirmed present on main.
    - `git status --short src/lib/groupHierarchy.js` shows no uncommitted changes.
    - The deploy-pending status (committed to main; production deploy not performed by this phase) is recorded for the audit.
    - groupHierarchy.js is unchanged by this plan (no edit, no deploy).
  </acceptance_criteria>
  <done>
    Shared-UI-fix commit verified on main with deploy status documented, ready for the audit's
    shared-UI-fix note.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write .planning/v17.0-MILESTONE-AUDIT.md</name>
  <read_first>
    - .planning/v16.0-MILESTONE-AUDIT.md (format/location precedent — frontmatter shape, Requirements Coverage table, Verified DB Metrics, Phase Summary, Carry-Forward sections to mirror)
    - .planning/phases/157-wave-2-close-out/157-CONTEXT.md (D-02 chip rule, D-03/D-03a verdict rules, D-05 required table dimensions, deferred split-section list)
    - .planning/REQUIREMENTS.md (LAC2-RETRO-01 text + per-phase mapping)
  </read_first>
  <action>
    Create `.planning/v17.0-MILESTONE-AUDIT.md` at the repo-root planning path (NOT
    `.planning/milestones/`), mirroring the v16.0 audit structure. Include: (1) frontmatter with
    `milestone: v17.0`, audited date, status, verified_against (production DB), and key metrics;
    (2) a per-city DB-verified count table with columns City | geo_id | gov | chamber(s) |
    roster (active vs official_count) | headshots | stances | split-section | verdict — populated
    from Task 1's numbers; (3) a per-city structure-hard / data-soft verdict (D-03/D-03a): missing
    gov/chamber/correct-current-roster = BLOCKER; headshot gaps + thin/blank stances + post-election
    uncertain seats = DOCUMENTED ACCEPTABLE GAP — phrase each so a reader can tell "incomplete on
    purpose" from "broken"; (4) an explicit recorded list of which cities have >=1 seeded compass
    stance (this is the Wave-2 purple-chip input, D-02) vs which are roster-only/0-stance; (5) a
    Known Issues / Deferred section naming the 5 split-section defect cities (Whittier, Compton,
    Carson, South El Monte, South Pasadena) as deferred follow-up — NOT fixed here, no data changes;
    (6) a Shared UI Fix note from Task 2 (groupHierarchy.js Mayor>Mayor-Pro-Tem fix committed to
    main at <hash>; production deploy still pending — documented item, not a blocker, no deploy
    performed). Tie the whole audit to requirement LAC2-RETRO-01. Use the Task 1 wrong-person
    headshot spot-check results and split-section row counts as evidence in the table/notes.
    Use the Write tool (no heredoc).
  </action>
  <verify>
    <automated>test -f .planning/v17.0-MILESTONE-AUDIT.md && grep -q "v17.0" .planning/v17.0-MILESTONE-AUDIT.md && for g in 0643000 0669088 0630000 0640130 0655156 0658072 0680000 0656000 0619766 0622230 0684200 0636546 0608954 0652526 0604982; do grep -q "$g" .planning/v17.0-MILESTONE-AUDIT.md || { echo "MISSING geo_id $g"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/v17.0-MILESTONE-AUDIT.md` (root planning dir, not the milestones/ subdir).
    - Frontmatter includes `milestone: v17.0` and `verified_against` referencing the production DB.
    - The per-city table contains all 15 geo_ids and the five D-04 dimensions per city.
    - Each city has an explicit structure-hard/data-soft verdict using BLOCKER vs DOCUMENTED-GAP language.
    - The set of >=1-stance cities is explicitly listed (the D-02 chip input).
    - The 5 split-section deferred cities are named in a Known Issues / Deferred section as not-fixed-here.
    - The D-08 groupHierarchy.js fix note records committed-to-main + deploy-pending status.
  </acceptance_criteria>
  <done>
    `.planning/v17.0-MILESTONE-AUDIT.md` written with the full per-city table, verdicts, chip input,
    deferred items, and shared-UI-fix note — ready to drive Wave 2 and Wave 3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB | Read-only SELECTs cross into the live Supabase DB |
| executor → committed planning docs | Audit markdown is committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-01 | Tampering | audit queries against production DB | mitigate | All DB access is SELECT-only; no INSERT/UPDATE/DELETE issued. Verified by acceptance criteria. |
| T-157-02 | Information Disclosure | committed audit markdown | mitigate | Audit records counts/verdicts only — no secrets, credentials, or PII beyond already-public officeholder names. |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (doc + read-only DB only). |

Realistic surface is minimal: read-only DB verification + a single committed markdown doc, no new
inputs/endpoints/auth/installs.
</threat_model>

<verification>
- `.planning/v17.0-MILESTONE-AUDIT.md` exists with all 15 geo_ids and per-city verdicts.
- Every DB interaction this plan performed was read-only.
- groupHierarchy.js commit verified present; file unchanged.
</verification>

<success_criteria>
- DB-verified per-city audit written (criterion 3, audit half) with the >=1-stance city set recorded for Wave 2.
- Structure-hard/data-soft verdicts present; deferred split-section + D-08 shared-UI-fix items documented.
</success_criteria>

<output>
Create `.planning/phases/157-wave-2-close-out/157-01-SUMMARY.md` when done — include the recorded
set of >=1-stance cities (Wave 2 needs it) and the audit verdict roll-up.
</output>
