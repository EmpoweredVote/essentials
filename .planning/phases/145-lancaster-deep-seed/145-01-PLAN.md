---
phase: 145-lancaster-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql
autonomous: true
requirements: [LANC-01]

must_haves:
  truths:
    - "D-01: this phase reconciles the EXISTING partial Lancaster seed (UPDATE/move-not-INSERT) — no greenfield rebuild, no duplicate offices/external_ids; existing rows preserved"
    - "D-02: the Lancaster government row 'City of Lancaster, California, US' carries geo_id '0640130' (no longer NULL), guarded WHERE geo_id IS NULL"
    - "D-03 (move-then-delete, NOT delete-empty): Crist's office is MOVED from the duplicate chamber a9be708e into the survivor chamber 9b9014b4 FIRST, THEN the now-emptied duplicate a9be708e is DELETED — end state is ONE 'City Council' chamber (9b9014b4) with exactly 5 offices (1 Mayor + 4 Councilmember)"
    - "D-06: the survivor chamber's Mayor office has district_type='LOCAL_EXEC' (directly-elected Mayor, NOT a rotational/council seat) — confirmed/set; the other 4 offices are at-large Councilmember (no district_id, D-08)"
    - "D-05: feedback_section_split_check SQL returns 0 rows for Lancaster after consolidation"
    - "D-04: this reconcile (geo_id backfill + office move + chamber delete) is a STRUCTURAL migration (910) that registers in supabase_migrations.schema_migrations normally"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql"
      provides: "Idempotent geo_id backfill + move-Crist-office-then-delete-duplicate-chamber + Mayor LOCAL_EXEC confirm for Lancaster"
      contains: "a9be708e"
  key_links:
    - from: "essentials.governments ('City of Lancaster, California, US')"
      to: "geo_id '0640130'"
      via: "UPDATE ... WHERE id = '<lancaster-gov-uuid>' AND geo_id IS NULL"
      pattern: "geo_id\\s*=\\s*'0640130'"
    - from: "essentials.offices (Crist's office in duplicate chamber a9be708e)"
      to: "survivor chamber 9b9014b4"
      via: "UPDATE offices SET chamber_id='9b9014b4...' WHERE chamber_id='a9be708e...'"
      pattern: "chamber_id\\s*=\\s*'9b9014b4"
    - from: "essentials.chambers (a9be708e, emptied duplicate)"
      to: "deleted (asserted 0 offices first)"
      via: "DO $$ assert 0 offices $$ then DELETE FROM essentials.chambers WHERE id = 'a9be708e...'"
      pattern: "DELETE FROM essentials\\.chambers"
---

<objective>
Reconcile the existing partial, duplicate-chamber Lancaster seed in a single idempotent structural
migration (910). This is the foundation wave — every later wave (roster, headshots, stances) assumes a
clean, unambiguous, single-chamber structure with the directly-elected Mayor office correctly typed.

⚠️ Lancaster's Wave 1 follows the SANTA CLARITA move-then-delete pattern, NOT the Glendale delete-empty
pattern. The duplicate chamber a9be708e is NOT empty — it holds the only currently-seated member
(Marvin Crist, ext_id 686320). His office MUST be moved into the survivor chamber FIRST, then the
emptied duplicate is deleted. (Crist's RETIREMENT belongs to Wave 2, not here — same separation as
Glendale's Najarian: this wave only does structure.)

Three structural fixes, all DB-verified against the live state on 2026-06-19:
1. Backfill `essentials.governments.geo_id = '0640130'` on the Lancaster gov row (currently NULL),
   guarded `WHERE geo_id IS NULL` (D-01 / D-02 / LANC-01).
2. MOVE Crist's office from the duplicate chamber a9be708e into the survivor chamber 9b9014b4
   (UPDATE the office's chamber_id), then an inline assert confirms a9be708e now has 0 offices, then
   DELETE the duplicate chamber a9be708e. Target both chambers by UUID ONLY — they share the name
   'City Council' and slug 'lancaster-city-council' (D-03). End state: ONE chamber (9b9014b4) with
   exactly 5 offices (1 Mayor + 4 Councilmember), official_count target 5.
3. Confirm/set the Mayor office in 9b9014b4 to district_type='LOCAL_EXEC' (directly-elected Mayor,
   D-06 — the KEY contrast with Glendale's rotational mayor). The other 4 offices remain at-large
   Councilmember with no district_id (D-08, confirmed at-large by RESEARCH).

Purpose: A clean structural base. Duplicate 'City Council' chamber names cause split-section render
bugs; NULL geo_id breaks routing; Lancaster's Mayor is a real directly-elected executive and must be
LOCAL_EXEC, not collapsed into a council seat.
Output: Migration 910 applied to production; all post-verification queries green; split-section check
returns 0 rows for Lancaster.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally (it is
NOT a stance/headshot audit-only migration). Ledger MAX is 909 (Glendale 902–909; 905–909 were
audit-only stance files that did not register); next structural is 910 (D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-lancaster-deep-seed/145-CONTEXT.md
@.planning/phases/145-lancaster-deep-seed/145-RESEARCH.md
@.planning/phases/145-lancaster-deep-seed/145-VALIDATION.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- DB-verified live values (2026-06-19). Task 1 confirms them read-only before any write. The
     Lancaster gov UUID is NOT in the source artifacts — resolve it by name in the pre-flight. -->

Government row (resolve UUID in pre-flight by name):
  name   = 'City of Lancaster, California, US'   type = LOCAL
  geo_id = NULL  -> set to '0640130'   (state already 'CA')

Two chambers, BOTH named 'City Council' / slug 'lancaster-city-council':
  KEEP   9b9014b4-0106-417f-a104-ac2055fc8134  (SURVIVOR — 4 offices: Mayor + 3 Councilmember, all empty)
  DELETE a9be708e-1b42-42ac-92ec-e8e56f9c6474  (duplicate — 1 Councilmember office, holds Crist)
  (chambers.slug is a GENERATED column — NEVER include slug in any write)

Move-then-delete (D-03, SC 894 pattern — NOT Glendale delete-empty):
  1. UPDATE essentials.offices SET chamber_id='9b9014b4-...' WHERE chamber_id='a9be708e-...'
     (moves Crist's single office into the survivor)
  2. DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='a9be708e-...') > 0
     THEN RAISE EXCEPTION 'Chamber a9be708e still has offices'; END IF; END $$;
  3. DELETE FROM essentials.chambers WHERE id='a9be708e-...'

Survivor chamber 9b9014b4 end state: 5 offices = 1 Mayor (district_type LOCAL_EXEC) + 4 Councilmember
  (at-large, no district_id). Mayor office: confirm/SET district_type='LOCAL_EXEC' (D-06). Do NOT
  collapse the Mayor into a council seat (Pitfall 3); do NOT create a NEW Mayor office (one already
  exists among the 4 survivor shells).

Crist (ext_id 686320): his OFFICE is moved here; he is NOT retired in this wave (retirement is Wave 2,
  same as Glendale's Najarian). Do NOT touch his is_active/office_id here beyond the chamber move.

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after
  consolidation; expect 0 rows for Lancaster. (5 OTHER LA-area cities have pre-existing split-section
  defects — OUT OF SCOPE here; record nothing about them.)

Pattern migration to copy: C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql
  (geo_id backfill + FK-safe move/teardown + idempotent guards — the direct move-then-delete precedent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state (gov, two chambers, Crist's office, Mayor office)</name>
  <files>C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql (the direct STRUCTURAL template — geo_id backfill + move-then-delete + idempotent guard patterns)
    - .planning/phases/143-santa-clarita-deep-seed/143-01-SUMMARY.md (exact move-then-delete reconcile shape + post-verification query set, esp. the "reseat existing rows / office-less transient" finding)
    - .planning/phases/145-lancaster-deep-seed/145-RESEARCH.md (§Live-Data Findings 7 DB State, §Common Pitfalls 1/3, §Validation Architecture Wave 1)
    - .planning/phases/145-lancaster-deep-seed/145-CONTEXT.md (§db_precheck, §decisions D-01..D-06, D-08)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA' uppercase)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE, run read-only confirmation queries via mcp__supabase-local
    execute_sql (= production) or psql -f using C:/EV-Accounts/backend/.env DATABASE_URL:
    (a) resolve the Lancaster gov UUID: SELECT id, geo_id, state FROM essentials.governments WHERE
    name='City of Lancaster, California, US' — confirm exactly one row, geo_id NULL, state 'CA'; capture
    the id for the migration;
    (b) confirm exactly two chambers under that government both named 'City Council' / slug
    'lancaster-city-council' — id 9b9014b4-0106-417f-a104-ac2055fc8134 (4 offices: Mayor + 3
    Councilmember, all empty, SURVIVOR) and id a9be708e-1b42-42ac-92ec-e8e56f9c6474 (1 Councilmember
    office holding Crist 686320, DUPLICATE);
    (c) confirm a9be708e has exactly 1 office and it links to Crist (ext_id 686320) — this office is the
    one to MOVE (Pitfall 1: do NOT delete the chamber before moving it);
    (d) confirm 9b9014b4 has 4 offices today and capture the Mayor office UUID + its current
    district_type (expect a Mayor office; note whether it is already LOCAL_EXEC or needs setting per
    D-06); confirm the other 3 are Councilmember with no district_id (at-large, D-08);
    (e) sanity-check the survivor's 4 office shells are all empty (politician_id NULL) per the pre-check.
    If ANY precondition does not match RESEARCH.md / CONTEXT (especially: a9be708e has 0 or >1 offices,
    a third chamber exists, the survivor lacks a Mayor office, or Crist's office is NOT in a9be708e),
    STOP and report the discrepancy — do not write a migration against a moved target.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id, state FROM essentials.governments WHERE name='City of Lancaster, California, US';" — returns one row, geo_id NULL, state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - Lancaster gov UUID resolved by name; row has geo_id NULL and state 'CA' (pre-state)
    - Two chambers confirmed sharing name 'City Council' / slug 'lancaster-city-council': 9b9014b4 (KEEP, 4 offices) and a9be708e (DELETE, 1 office)
    - a9be708e confirmed to have exactly 1 office and it belongs to Crist (ext_id 686320) — the move target
    - Survivor 9b9014b4 confirmed to have a Mayor office (UUID + district_type captured) + 3 at-large Councilmember offices (no district_id); all 4 shells empty
    - Any mismatch vs RESEARCH.md/CONTEXT is surfaced and execution paused (no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches the source artifacts; gov UUID captured; Crist's office in a9be708e confirmed as the move target; the survivor's Mayor office located; the geo_id backfill, office move, chamber delete, and Mayor-type confirm are safe to author.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 910 (geo_id backfill + move Crist's office + delete duplicate + Mayor LOCAL_EXEC)</name>
  <files>C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql (copy the idempotent geo_id backfill + office move + FK-safe chamber DELETE + inline-assert patterns; Lancaster is the move-then-delete case)
    - .planning/phases/143-santa-clarita-deep-seed/143-01-SUMMARY.md (exact post-verification query set + idempotency guard shapes)
    - .planning/phases/145-lancaster-deep-seed/145-RESEARCH.md (§Architecture Patterns Wave 1 template, §Common Pitfalls 1/3, §Validation Architecture Wave 1 checks)
    - .planning/phases/145-lancaster-deep-seed/145-CONTEXT.md (§db_precheck, §decisions D-02..D-06)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql wrapped in BEGIN/COMMIT, all
    statements idempotent, using the captured ids from Task 1, in this order:
    (1) UPDATE essentials.governments SET geo_id='0640130' WHERE id='&lt;lancaster-gov-uuid&gt;'
        AND geo_id IS NULL (D-01 / D-02 / LANC-01).
    (2) MOVE Crist's office into the survivor: UPDATE essentials.offices SET chamber_id=
        '9b9014b4-0106-417f-a104-ac2055fc8134' WHERE chamber_id='a9be708e-1b42-42ac-92ec-e8e56f9c6474'
        (D-03 step 1 — move FIRST, before delete; Pitfall 1).
    (3) Inline assert the duplicate is now empty: a DO $$ block that RAISEs EXCEPTION if
        (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='a9be708e-...') > 0.
    (4) DELETE essentials.chambers WHERE id='a9be708e-1b42-42ac-92ec-e8e56f9c6474'. Target the duplicate
        by its UUID ONLY, never by name/slug (both chambers share 'City Council' / 'lancaster-city-council').
    (5) Confirm/SET the Mayor office's type: UPDATE essentials.offices SET district_type='LOCAL_EXEC'
        WHERE id='&lt;survivor-mayor-office-uuid from Task 1&gt;' AND district_type IS DISTINCT FROM
        'LOCAL_EXEC' (D-06 — directly-elected Mayor). Do NOT create a new Mayor office; do NOT collapse it
        into a council seat (Pitfall 3); do NOT add district_id to the 4 at-large Councilmember offices (D-08).
    (6) Set survivor official_count: UPDATE essentials.chambers SET official_count=5 WHERE
        id='9b9014b4-...' AND official_count IS DISTINCT FROM 5 (final 5-seat roster after Wave 2).
    Do NOT touch the slug column anywhere (GENERATED). Do NOT touch Crist's is_active/office_id (his
    retirement is Wave 2). End the file with the post-verification queries: geo_id check + a chamber
    listing under the Lancaster gov (expect exactly one 'City Council', the survivor 9b9014b4) + a
    count of offices under 9b9014b4 (expect 5) + a Mayor-office district_type check (expect LOCAL_EXEC)
    + the feedback_section_split_check SQL (project memory; expect 0 rows for Lancaster). Apply via
    mcp__supabase-local execute_sql or psql -f. This registers in schema_migrations normally
    (structural migration 910).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id FROM essentials.governments WHERE name='City of Lancaster, California, US';" returns '0640130' AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='9b9014b4-0106-417f-a104-ac2055fc8134';" returns 5</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE name='City of Lancaster, California, US'` → '0640130'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='&lt;lancaster-gov&gt;' AND name='City Council'` → 1 (not 0, not 2); a9be708e deleted
    - `SELECT COUNT(*) FROM essentials.chambers WHERE id='a9be708e-1b42-42ac-92ec-e8e56f9c6474'` → 0 (duplicate deleted)
    - `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='9b9014b4-0106-417f-a104-ac2055fc8134'` → 5 (Crist's office moved in; 1 Mayor + 4 Councilmember)
    - Mayor office in 9b9014b4 has `district_type='LOCAL_EXEC'`; the 4 Councilmember offices have no district_id (at-large)
    - feedback_section_split_check SQL returns 0 rows for Lancaster (D-05)
    - Crist's is_active/office_id were NOT modified in this migration (only his office's chamber_id moved)
    - Migration 910 registered in supabase_migrations.schema_migrations (structural)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
  </acceptance_criteria>
  <done>Migration 910 applied; one 'City Council' chamber (9b9014b4) remains with 5 offices; geo_id 0640130 set; Mayor office typed LOCAL_EXEC; split-section check 0 rows; re-running the file is a no-op.</done>
</task>

</tasks>

<threat_model>
This is a DB-seeding phase with NO new external attack surface. No untrusted user input crosses any
boundary — migration SQL is applied via service-role/operator (psql/MCP) directly against the internal
production Supabase DB. The real threat surface is accidental data corruption — specifically deleting
the duplicate chamber before moving Crist's office (orphaning the only seated member), or mis-typing the
directly-elected Mayor — mitigated by the move-FIRST ordering, an inline empty-chamber assert,
UUID-scoped targeting, idempotent guards, and post-migration SQL verification.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator (psql/MCP) directly against production Supabase. No untrusted user input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Tampering | Chamber DELETE targets the wrong chamber (both share name 'City Council' / slug) | mitigate | DELETE keyed on UUID 'a9be708e…' ONLY, never on name/slug; post-verify exactly one 'City Council' chamber (9b9014b4) remains |
| T-145-02 | Tampering | Duplicate chamber deleted BEFORE Crist's office is moved → orphans/loses the only seated member | mitigate | Move-FIRST ordering (UPDATE office chamber_id before DELETE) + inline DO $$ assert RAISEs if a9be708e still has >0 offices before the DELETE (SC 894 pattern) |
| T-145-03 | Tampering | geo_id overwritten on a re-run or wrong gov | mitigate | UPDATE guarded by id-scope + WHERE geo_id IS NULL; second apply = 0 rows |
| T-145-04 | Tampering | Mayor mis-modeled as rotational/LOCAL council seat (Glendale pattern over-copied) | mitigate | Mayor office UUID set district_type='LOCAL_EXEC' guarded IS DISTINCT FROM; no new Mayor office; no district_id added to at-large seats (D-06/D-08) |
| T-145-05 | Tampering | Non-idempotent migration applied twice corrupts state | mitigate | All writes guarded (geo_id IS NULL, id-scoped move/delete, district_type/official_count IS DISTINCT FROM); second apply = 0 rows changed |
| T-145-06 | Tampering | Accidental write to production without verifying pre-state (data drift since 2026-06-19) | mitigate | Task 1 read-only pre-flight confirms live state matches the source artifacts before any write; STOP-on-mismatch |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 910:
1. gov geo_id = '0640130' on 'City of Lancaster, California, US'
2. exactly 1 chamber 'City Council' under the Lancaster gov; duplicate a9be708e gone
3. survivor chamber 9b9014b4 has exactly 5 offices (1 Mayor + 4 Councilmember) and official_count=5
4. Mayor office district_type = 'LOCAL_EXEC'; the 4 Councilmember offices have no district_id
5. feedback_section_split_check SQL → 0 rows for Lancaster
6. Crist (686320) is_active/office_id unchanged (only office chamber_id moved)
7. migration 910 registered in schema_migrations
</verification>

<success_criteria>
All three structural defects fixed and independently verified; Crist's office was moved into the
survivor BEFORE the duplicate was deleted (with an inline assert proving the duplicate was empty); the
directly-elected Mayor office is typed LOCAL_EXEC; migration 910 is idempotent and registered; Lancaster
is a single unambiguous 'City Council' chamber with 5 office shells ready for roster completion in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/145-lancaster-deep-seed/145-01-SUMMARY.md` when done.
</output>
