---
phase: 149-pasadena-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql
autonomous: true
requirements: [PASA-01]

must_haves:
  truths:
    - "This phase RECONCILES the EXISTING partial Pasadena seed (geo_id backfill + merge the two duplicate 'City Council' chambers + relabel the wrongly-'At-Large' council rows to 'District 1'..'District 7' + dedupe Lyon's extra image) — NOT a greenfield rebuild. Pasadena is BY-DISTRICT (7 single-member districts + a directly-elected Mayor), so this DOES relabel At-Large→District (unlike Torrance, which stayed At-Large). A Wave-1 pre-flight SELECT re-confirms gov d25619a9 geo_id NULL/empty, BOTH chamber UUIDs (survivor 2e7f01d0, doomed bdd1acad), the 8 offices + their members/ext_ids, BOTH link directions, the 6-vs-7 At-Large-row count, whether ext_id 657577 shows Rick Cole or Felicia Williams, Lyon's 2 image rows, and STOPs on drift"
    - "The Pasadena government row 'City of Pasadena, California, US' (id d25619a9-7276-4e8b-b7ae-8028e408aee0) carries geo_id '0656000' (no longer NULL/empty), guarded WHERE (geo_id IS NULL OR geo_id = ''); state already 'CA'"
    - "move-then-delete (UUID-targeted, 2 offices — NOT 1): the doomed chamber bdd1acad's TWO offices — Mayor/Gordo fc5e372a + Hampton 0c357b48 — are MOVED into the survivor chamber 2e7f01d0 (UPDATE chamber_id WHERE chamber_id='bdd1acad…') FIRST, an inline assert proves bdd1acad then has 0 offices, THEN bdd1acad is DELETEd (target by UUID — both chambers share the name 'City Council') — end state is ONE 'City Council' chamber (2e7f01d0) with exactly 8 offices (7 council + Mayor)"
    - "by-district relabel: the wrongly-'At-Large' council LOCAL district rows are relabeled to their occupant's real district per the RESEARCH §3 map — D1 Hampton (office 0c357b48), D2 Cole (7ab2730c), D3 Jones (e3617ff5), D4 Masuda (0bc62efd), D5 Rivas (7bdb4f77), D6 Madison (f2cb13dd), D7 Lyon (0cd97f4e) — keeping district_type='LOCAL', geo_id '0656000', state 'CA' unchanged; the pre-flight office→district map resolves each At-Large UUID before relabel"
    - "a 'District 7' (and any other missing district) row is CREATED guarded WHERE NOT EXISTS (LOCAL, geo_id '0656000', state 'CA') for Lyon if the pre-flight finds only 6 At-Large rows; after this wave exactly 8 district rows exist for geo_id '0656000': 'Pasadena Mayor' (LOCAL_EXEC, KEPT as-is) + 'District 1'..'District 7' (all LOCAL)"
    - "the 'Pasadena Mayor' LOCAL_EXEC district is KEPT exactly as-is — Gordo's directly-elected Mayor office fc5e372a is only MOVED to the survivor chamber here; NO new Mayor office/district created, NO rotational title flag (Lancaster/Pomona LOCAL_EXEC model)"
    - "cleanup: Jason Lyon's extra politician_images row (657582 has 2) is deduped to exactly one canonical row, and Jess Rivas (-700150, office 7bdb4f77, 0 images/0 stances) is confirmed correctly linked"
    - "feedback_section_split_check SQL returns 0 rows for Pasadena after consolidation"
    - "this reconcile (geo_id backfill + chamber merge + district relabel/create + Lyon image dedupe) is a STRUCTURAL migration (946) that registers in supabase_migrations.schema_migrations normally"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql"
      provides: "Idempotent geo_id backfill + move-2-offices-then-delete-doomed-chamber-bdd1acad + relabel At-Large council rows to District 1-7 + create any missing District row + Lyon image dedupe"
      contains: "bdd1acad"
  key_links:
    - from: "essentials.governments (id d25619a9-7276-4e8b-b7ae-8028e408aee0)"
      to: "geo_id '0656000'"
      via: "UPDATE ... WHERE id = 'd25619a9-7276-4e8b-b7ae-8028e408aee0' AND (geo_id IS NULL OR geo_id = '')"
      pattern: "geo_id\\s*=\\s*'0656000'"
    - from: "essentials.offices (fc5e372a Gordo/Mayor + 0c357b48 Hampton, the 2 doomed-chamber offices)"
      to: "survivor chamber 2e7f01d0"
      via: "UPDATE offices SET chamber_id='2e7f01d0...' WHERE chamber_id='bdd1acad...'"
      pattern: "chamber_id\\s*=\\s*'2e7f01d0"
    - from: "essentials.chambers (bdd1acad, emptied doomed duplicate)"
      to: "deleted (asserted 0 offices first)"
      via: "DO $$ assert 0 offices $$ then DELETE FROM essentials.chambers WHERE id = 'bdd1acad...'"
      pattern: "DELETE FROM essentials\\.chambers"
    - from: "essentials.districts (the wrongly-'At-Large' council rows)"
      to: "relabeled 'District 1'..'District 7' per occupant; missing rows created"
      via: "UPDATE districts SET label='District N' WHERE id='<At-Large UUID>'; INSERT 'District 7' WHERE NOT EXISTS"
      pattern: "label\\s*=\\s*'District 7'"
---

<objective>
Reconcile the existing partial, duplicate-chamber, structurally-defective Pasadena seed in a single
idempotent structural migration (946). This is the foundation wave — every later wave (roster link-repair,
headshots, stances) assumes a clean, single-chamber, correctly-districted structure.

⚠️ RECONCILE, NOT GREENFIELD (DB-verified 2026-06-20 in CONTEXT.md). The Pasadena gov already exists with
geo_id NULL, TWO duplicate 'City Council' chambers, and 8 offices (7 council + Mayor). This wave does NOT
create or reseat people — it (1) backfills geo_id, (2) merges the duplicate chamber, (3) fixes the form of
government from (wrong) At-Large to Pasadena's real 7-single-member-district structure, and (4) dedupes
Lyon's extra image. The bidirectional-link repairs, the Cole/Williams name confirmation, and official_count
are Plan 02 — this wave is STRUCTURE ONLY.

⚠️ PASADENA IS BY-DISTRICT (unlike Torrance). Pasadena is 7 single-member districts (D1-D7) + a
directly-elected citywide Mayor. The DB currently mislabels all council rows as 'At-Large'. Use the proven
Pomona (147) / Palmdale (146) by-district relabel pattern: relabel the existing wrongly-'At-Large' council
rows to 'District 1'..'District 7' per the RESEARCH §3 occupant map, and CREATE any missing district row
(D7 likely needed). Do NOT tear down and rebuild.

⚠️ PASADENA HAS THREE DISTINCTIVE FEATURES vs Pomona:
1. The doomed chamber has only 2 offices to move (Gordo/Mayor + Hampton), NOT 3 (Pitfall 5: move BOTH at
   once via a single UPDATE WHERE chamber_id='bdd1acad…'; missing one fails the assert).
2. NO shared-district defect is confirmed (Pomona's Garcia/Lustro shared a UUID). Assumptions A2/A3 say each
   At-Large row maps to one office — the pre-flight §5 query MUST confirm this; if a shared district is
   found, resolve it with the Pomona create-new-row-and-repoint pattern.
3. Pasadena's Mayor is DIRECTLY ELECTED (Lancaster/Pomona LOCAL_EXEC model) and is ALREADY correctly modeled
   — the 'Pasadena Mayor' LOCAL_EXEC district + Gordo's Mayor office fc5e372a already exist. Do NOT create a
   new Mayor district/office and do NOT use a rotational title flag (Pitfall 4). The only Mayor work in this
   wave is MOVING Gordo's office fc5e372a from the doomed chamber to the survivor.

Four structural fixes, all DB-verified against the live state on 2026-06-20:
1. Backfill essentials.governments.geo_id = '0656000' on gov d25619a9-7276-4e8b-b7ae-8028e408aee0 (currently
   NULL), guarded WHERE (geo_id IS NULL OR geo_id = '') (PASA-01, Pitfall 8 empty-string guard). state already 'CA'.
2. MERGE the duplicate chamber: MOVE both doomed-chamber offices (fc5e372a Gordo/Mayor, 0c357b48 Hampton)
   into the survivor chamber 2e7f01d0 (UPDATE chamber_id WHERE chamber_id='bdd1acad…'), then an inline assert
   confirms bdd1acad now has 0 offices, then DELETE the duplicate chamber bdd1acad. Target both
   chambers/offices by UUID ONLY — both share the name 'City Council'. End state: ONE chamber (2e7f01d0)
   with exactly 8 offices.
3. RELABEL the wrongly-'At-Large' council district rows to their occupant's REAL district per the RESEARCH §3
   map (D1 Hampton, D2 Cole, D3 Jones, D4 Masuda, D5 Rivas, D6 Madison, D7 Lyon), resolving each At-Large UUID
   from the pre-flight office→district map. Keep district_type='LOCAL', geo_id '0656000', state 'CA' (relabel
   ONLY). Then CREATE any missing district row (guarded WHERE NOT EXISTS — D7 likely needed for Lyon). KEEP
   the LOCAL_EXEC 'Pasadena Mayor' row exactly as-is.
4. CLEANUP: dedupe Lyon's extra politician_images row (657582 has 2 — keep exactly one canonical), and
   confirm Rivas (-700150, office 7bdb4f77) is correctly linked.

Purpose: A clean structural base. Duplicate 'City Council' chamber names cause split-section render bugs;
NULL geo_id breaks routing; the wrong At-Large labels misrepresent Pasadena's real by-district form of
government; Lyon's duplicate image would double-render. Output: Migration 946 applied to production; all
post-verification queries green; split-section check returns 0 rows for Pasadena.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally (NOT a
stance/headshot audit-only migration). On-disk file counter is authoritative; next structural is 946
(on-disk MAX is 945; ledger MAX is 937 with 938-945 audit-only — pre-flight must re-confirm).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-pasadena-deep-seed/149-RESEARCH.md
@.planning/phases/149-pasadena-deep-seed/149-CONTEXT.md
@.planning/phases/147-pomona-deep-seed/147-01-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- DB-verified live values (2026-06-20, CONTEXT.md + RESEARCH §3/§4/§5). Task 1 re-confirms them read-only
     before any write and STOPs on drift. -->

Government row:
  id     = d25619a9-7276-4e8b-b7ae-8028e408aee0   name = 'City of Pasadena, California, US'
  geo_id = NULL  -> set to '0656000'   (state already 'CA')

Two chambers, BOTH named 'City Council' under gov d25619a9 (MERGE into one):
  KEEP   2e7f01d0-69dd-4301-b24c-58d83eb19f47  (SURVIVOR — 6 offices, official_count 8)
  DELETE bdd1acad-f22d-4fa3-8ab5-667eed0e3d82  (doomed — 2 offices, official_count NULL)
  (chambers.slug is a GENERATED column — NEVER include slug in any write)

Survivor 2e7f01d0 offices (6, via offices.politician_id; KEEP — relabel their districts only):
  7ab2730c  Rick Cole       (ext_id 657577,  -> relabel district 'District 2'; CONFIRM name is Cole not Williams in Plan 02)
  e3617ff5  Justin Jones    (ext_id 657578,  -> relabel district 'District 3')
  0bc62efd  Gene Masuda     (ext_id 657579,  -> relabel district 'District 4')
  f2cb13dd  Steve Madison   (ext_id 657581,  -> relabel district 'District 6')
  7bdb4f77  Jess Rivas      (ext_id -700150, -> relabel district 'District 5'; 0 images/0 stances — confirm linked)
  0cd97f4e  Jason Lyon      (ext_id 657582,  -> relabel district 'District 7'; has 2 images -> DEDUPE)

Doomed bdd1acad offices (MOVE both into survivor, then delete the empty chamber):
  fc5e372a  Victor M. Gordo (ext_id -200901)  district 'Pasadena Mayor' LOCAL_EXEC — KEEP as-is; only MOVE office
  0c357b48  Tyron Hampton   (ext_id -201094)  At-Large -> relabel district 'District 1'

Total = 8 offices (7 council + Mayor). All council rows currently district_type='LOCAL' label 'At-Large'
  -> must become 'District 1'..'District 7'. 'Pasadena Mayor' row already LOCAL_EXEC (correct).

Move-then-delete (Pomona 926 / Palmdale 918 pattern — UUID-targeted, 2 offices not 3; Pitfall 5):
  1. UPDATE essentials.offices SET chamber_id='2e7f01d0-69dd-4301-b24c-58d83eb19f47'
     WHERE chamber_id='bdd1acad-f22d-4fa3-8ab5-667eed0e3d82'
     (moves BOTH doomed-chamber offices — Gordo fc5e372a + Hampton 0c357b48 — into the survivor at once)
  2. DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='bdd1acad-...') > 0
     THEN RAISE EXCEPTION 'Chamber bdd1acad still has offices'; END IF; END $$;
  3. DELETE FROM essentials.chambers WHERE id='bdd1acad-f22d-4fa3-8ab5-667eed0e3d82'

District relabel (relabel label ONLY; district_type/geo_id/state stay as-is). The MANDATORY pre-flight §5
  query maps each office in BOTH chambers to its current district_id + label — use THOSE UUIDs:
  SELECT o.id AS office_id, p.external_id, p.last_name, d.id AS district_id, d.label, d.district_type
  FROM essentials.offices o
  LEFT JOIN essentials.politicians p ON p.id = o.politician_id
  LEFT JOIN essentials.districts d ON d.id = o.district_id
  WHERE o.chamber_id IN ('2e7f01d0-...','bdd1acad-...')
  ORDER BY d.district_type, d.label;
  Occupant -> district relabel map (RESEARCH §3 — authoritative):
    Hampton (0c357b48) -> 'District 1'   (KEEP 'Pasadena Mayor' LOCAL_EXEC unchanged — Gordo, Pitfall 4)
    Cole    (7ab2730c) -> 'District 2'
    Jones   (e3617ff5) -> 'District 3'
    Masuda  (0bc62efd) -> 'District 4'
    Rivas   (7bdb4f77) -> 'District 5'
    Madison (f2cb13dd) -> 'District 6'
    Lyon    (0cd97f4e) -> 'District 7'

District CREATE (guarded NOT EXISTS — D7 likely needed for Lyon; Pitfall 7). Pre-flight determines whether
  6 or 7 At-Large rows exist. If only 6, Lyon's office has no At-Large row -> CREATE 'District 7':
  INSERT INTO essentials.districts (label, district_type, geo_id, state)
  SELECT 'District 7','LOCAL','0656000','CA'
  WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE label='District 7' AND geo_id='0656000');
  Then REPOINT Lyon's office to it if it currently points at NULL or a shared/wrong UUID:
  UPDATE essentials.offices SET district_id=(SELECT id FROM essentials.districts WHERE label='District 7' AND geo_id='0656000')
  WHERE id='0cd97f4e-...' AND district_id IS DISTINCT FROM (SELECT id FROM essentials.districts WHERE label='District 7' AND geo_id='0656000');
  (If the pre-flight finds a SHARED At-Large district — Assumptions A2/A3 — resolve with the same
   create-new-row-and-repoint pattern Pomona used for Lustro.)

Lyon image dedupe (Pitfall — keep one canonical):
  SELECT id, politician_id, url, type, created_at FROM essentials.politician_images
  WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = 657582) ORDER BY created_at;
  -- DELETE the stale row (the non-canonical / older / wrong-dimension one); keep exactly one.
  -- If neither is canonical, Wave 3 re-uploads from the official URL; deleting both stale rows is acceptable.

Schema notes (CRITICAL):
  - chambers.slug is GENERATED — NEVER include slug in any chamber write
  - district_type lives on essentials.districts, NOT on offices (no offices.district_type column)
  - districts.state is uppercase 'CA' (CA convention)
  - Pasadena's Mayor is the directly-elected Lancaster/Pomona LOCAL_EXEC model — 'Pasadena Mayor' row +
    Gordo office fc5e372a ALREADY EXIST. Do NOT create a new Mayor district/office; do NOT use a rotational
    title flag (Pitfall 4). The only Mayor work here is moving fc5e372a to the survivor.
  - geo_id guard MUST include the empty-string case: WHERE (geo_id IS NULL OR geo_id = '') (Pitfall 8)
  - This wave does NOT touch any politicians.is_active / office_id; the back-pointer repairs, the
    Cole/Williams name confirmation, and official_count are Plan 02

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after
  consolidation; expect 0 rows for Pasadena. (5 OTHER LA-area cities have pre-existing split-section defects —
  OUT OF SCOPE here, project_split_section_defects_5_cities.)

Pattern migrations to copy:
  C:/EV-Accounts/backend/migrations/926_pomona_reconcile.sql (geo_id backfill + move-then-delete + At-Large→
    District relabel + create-missing-district + idempotent guards — the DIRECT by-district precedent).
  C:/EV-Accounts/backend/migrations/936_torrance_reconcile.sql (most recent reconcile; geo_id empty-string
    guard + move-then-delete shape).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state (gov, two chambers, both link directions, the office→district map, 6-vs-7 At-Large rows, Cole-vs-Williams name, Lyon's 2 images, current roster) — STOP on drift</name>
  <files>C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/926_pomona_reconcile.sql (the DIRECT by-district STRUCTURAL template — geo_id backfill + move-then-delete + At-Large→District relabel + create-missing-district + idempotent guards)
    - C:/EV-Accounts/backend/migrations/936_torrance_reconcile.sql (geo_id empty-string guard + move-then-delete shape)
    - .planning/phases/147-pomona-deep-seed/147-01-PLAN.md (the exact pre-flight + STOP-on-drift shape this plan mirrors)
    - .planning/phases/149-pasadena-deep-seed/149-RESEARCH.md (§3 District→DB Member Mapping, §4 DB State, §5 District UUID Mapping incl. the mandatory office→district query, §Common Pitfalls 1-8, §Open Questions, §Validation Architecture Wave 1)
    - .planning/phases/149-pasadena-deep-seed/149-CONTEXT.md (LOCKED decisions D-01/D-02/D-05)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA', district_type on districts)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE/INSERT, run read-only confirmation queries via mcp__supabase-local
    execute_sql (= production) or psql -f using C:/EV-Accounts/backend/.env DATABASE_URL. Confirm EACH
    against the LOCKED RESEARCH §3/§4/§5 state:
    (a) gov: SELECT id, geo_id, state FROM essentials.governments WHERE
    id='d25619a9-7276-4e8b-b7ae-8028e408aee0' — confirm geo_id NULL/empty, state 'CA', name 'City of Pasadena,
    California, US';
    (b) two chambers under that gov both named 'City Council' — 2e7f01d0 (SURVIVOR, 6 offices, official_count
    8) and bdd1acad (DOOMED, 2 offices, official_count NULL); confirm NO third chamber;
    (c) survivor 2e7f01d0 has exactly 6 offices occupied via offices.politician_id: 7ab2730c (Cole 657577),
    e3617ff5 (Jones 657578), 0bc62efd (Masuda 657579), f2cb13dd (Madison 657581), 7bdb4f77 (Rivas -700150),
    0cd97f4e (Lyon 657582);
    (d) doomed bdd1acad has exactly 2 offices: fc5e372a (Gordo/Mayor -200901, district 'Pasadena Mayor'
    LOCAL_EXEC) and 0c357b48 (Hampton -201094, At-Large) — these are the MOVE targets (Pitfall 5: move BOTH,
    not 1);
    (e) run the MANDATORY office→district map (RESEARCH §5 query over BOTH chambers) — record each office's
    current district_id + label + district_type; determine whether exactly 6 or 7 At-Large LOCAL rows exist
    for geo_id 0656000, whether any 'District 7' row already exists, and whether Hampton's office has its own
    At-Large row or shares one (Open Q1 / Assumptions A2/A3);
    (f) confirm NO shared-district defect (no two offices pointing at the same district_id) — if found, note
    it for the Pomona-style create+repoint resolution;
    (g) Cole/Williams check (Pitfall 2 / Open Q2): SELECT external_id, first_name, last_name FROM
    essentials.politicians WHERE external_id=657577 — record whether it reads 'Rick Cole' or 'Felicia
    Williams' (the name correction, if needed, is Plan 02);
    (h) Lyon images (D-05): SELECT id, url, type, created_at FROM essentials.politician_images WHERE
    politician_id=(SELECT id FROM essentials.politicians WHERE external_id=657582) ORDER BY created_at —
    confirm exactly 2 rows and identify the stale one;
    (i) Rivas link (D-05): confirm -700150 office is 7bdb4f77 and Rivas has 0 images / 0 stances;
    (j) re-confirm the CURRENT ROSTER live (RESEARCH §2): Mayor Gordo + D1 Hampton / D2 Cole / D3 Jones /
    D4 Masuda / D5 Rivas / D6 Madison / D7 Lyon against cityofpasadena.net.
    Also confirm the migration ledger: on-disk MAX is 945 and schema_migrations MAX is 937 (next structural is
    946). If ANY precondition does not match (a third chamber, bdd1acad has !=2 offices, an office occupied by
    a different ext_id than locked, a shared-district defect not expected by §5, the LOCAL_EXEC Mayor row
    missing, Lyon does NOT have exactly 2 images, the on-disk MAX is not 945, or the current roster differs),
    STOP and report the discrepancy — do NOT write a migration against a moved target.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id, state FROM essentials.governments WHERE id='d25619a9-7276-4e8b-b7ae-8028e408aee0';" returns one row, geo_id NULL or '', state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - Gov d25619a9 has geo_id NULL/empty and state 'CA' (pre-state); confirmed name 'City of Pasadena, California, US'
    - Two chambers confirmed sharing name 'City Council': 2e7f01d0 (KEEP, 6 offices) and bdd1acad (DELETE, 2 offices); no third chamber
    - Survivor 2e7f01d0 offices confirmed OCCUPIED via offices.politician_id (7ab2730c=Cole 657577, e3617ff5=Jones 657578, 0bc62efd=Masuda 657579, f2cb13dd=Madison 657581, 7bdb4f77=Rivas -700150, 0cd97f4e=Lyon 657582)
    - Doomed bdd1acad confirmed to have exactly 2 offices: fc5e372a (Gordo -200901/Pasadena Mayor LOCAL_EXEC) and 0c357b48 (Hampton -201094/At-Large) — the move targets
    - Office→district map run; the count of At-Large LOCAL rows (6 vs 7), the existence of any 'District 7' row, and Hampton's district situation all resolved; any shared-district defect surfaced
    - Cole/Williams name recorded for ext_id 657577 (correction deferred to Plan 02 if needed)
    - Lyon (657582) confirmed to have exactly 2 politician_images rows; the stale one identified; Rivas (-700150) confirmed linked to office 7bdb4f77 with 0 images/0 stances
    - Ledger confirmed: on-disk MAX 945, schema_migrations MAX 937, next structural 946
    - Current roster re-confirmed live = Gordo Mayor + Hampton D1 / Cole D2 / Jones D3 / Masuda D4 / Rivas D5 / Madison D6 / Lyon D7; STOP-and-report if changed
    - Any mismatch vs RESEARCH §3/§4/§5 is surfaced and execution paused (STOP-on-drift; no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches RESEARCH §3/§4/§5; gov UUID, the 2 move-target offices, the office→district map, the 6-vs-7 At-Large row count, the Cole/Williams name, Lyon's 2 images, and the current roster are all confirmed; the geo_id backfill, 2-office chamber merge, district relabel/create, and Lyon dedupe are safe to author.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 946 (geo_id backfill + merge duplicate chamber [2 offices] + relabel At-Large→District 1-7 + create any missing district + Lyon image dedupe)</name>
  <files>C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/926_pomona_reconcile.sql (copy the idempotent geo_id backfill + office move + FK-safe chamber DELETE + inline-assert + At-Large→District relabel + create-missing-district patterns)
    - C:/EV-Accounts/backend/migrations/936_torrance_reconcile.sql (geo_id empty-string guard + move-then-delete shape)
    - .planning/phases/147-pomona-deep-seed/147-01-PLAN.md (the exact authoring shape + post-verification query set this plan mirrors)
    - .planning/phases/149-pasadena-deep-seed/149-RESEARCH.md (§Architecture Patterns Wave 1 SQL templates incl. move-then-delete + district relabel/create + Lyon dedupe, §Common Pitfalls 1-8, §Validation Architecture Wave 1 checks)
    - .planning/phases/149-pasadena-deep-seed/149-CONTEXT.md (LOCKED D-01/D-02/D-05)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA')
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql wrapped in BEGIN/COMMIT, all statements
    idempotent, in this order:
    (1) UPDATE essentials.governments SET geo_id='0656000' WHERE
        id='d25619a9-7276-4e8b-b7ae-8028e408aee0' AND (geo_id IS NULL OR geo_id = '') (PASA-01, Pitfall 8).
    (2) MERGE — MOVE both doomed-chamber offices into the survivor: UPDATE essentials.offices SET
        chamber_id='2e7f01d0-69dd-4301-b24c-58d83eb19f47' WHERE chamber_id='bdd1acad-f22d-4fa3-8ab5-667eed0e3d82'
        (Pitfall 5 — this carries Gordo fc5e372a + Hampton 0c357b48 at once; target by the doomed chamber UUID,
        move FIRST).
    (3) Inline assert the doomed chamber is now empty: a DO $$ block that RAISEs EXCEPTION if
        (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='bdd1acad-f22d-4fa3-8ab5-667eed0e3d82') > 0.
    (4) DELETE essentials.chambers WHERE id='bdd1acad-f22d-4fa3-8ab5-667eed0e3d82'. Target the duplicate by
        its UUID ONLY, never by name (both chambers share 'City Council').
    (5) RELABEL the wrongly-'At-Large' council district rows to their occupant's real district, each guarded
        label IS DISTINCT FROM target, using the At-Large UUIDs resolved by the Task 1 office→district map:
        'District 1' (Hampton's district, office 0c357b48), 'District 2' (Cole 7ab2730c), 'District 3' (Jones
        e3617ff5), 'District 4' (Masuda 0bc62efd), 'District 5' (Rivas 7bdb4f77), 'District 6' (Madison
        f2cb13dd), 'District 7' (Lyon 0cd97f4e). Do NOT alter district_type, geo_id, or state. Do NOT touch
        the 'Pasadena Mayor' LOCAL_EXEC row (Gordo, Pitfall 4).
    (6) CREATE any missing district row guarded WHERE NOT EXISTS (D7 likely needed if pre-flight found only 6
        At-Large rows): INSERT INTO essentials.districts (label, district_type, geo_id, state) SELECT
        'District 7','LOCAL','0656000','CA' WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE
        label='District 7' AND geo_id='0656000'). Then REPOINT Lyon's office 0cd97f4e to the District 7 row if
        it does not already point there (guarded IS DISTINCT FROM). (If the pre-flight found a shared-district
        defect, resolve it with the same create-new-row-and-repoint pattern.)
    (7) DEDUPE Lyon's images (D-05): DELETE the stale politician_images row identified in Task 1 for ext_id
        657582 by its exact image id — keep exactly one canonical row. (If neither is canonical, deleting both
        stale rows is acceptable — Wave 3 re-uploads.)
    Do NOT touch the slug column anywhere (GENERATED). Do NOT write to offices.district_type (no such column).
    Do NOT create a new Mayor district/office or flip a rotational title flag (Pitfall 4 — the directly-elected
    Mayor is already correctly modeled). Do NOT set chamber official_count here (Plan 02 sets it to 7). Do NOT
    repair the back-pointers, correct the Cole/Williams name, or reseat any office here (all Plan 02).
    End the file with post-verification queries: geo_id check + a chamber listing under gov d25619a9 (expect
    exactly one 'City Council', the survivor 2e7f01d0) + a count of offices under 2e7f01d0 (expect 8) + a
    confirm that bdd1acad is gone + the 8 district labels for geo_id 0656000 ('Pasadena Mayor' LOCAL_EXEC +
    'District 1'..'District 7' all LOCAL) + a confirm Lyon's office 0cd97f4e points at 'District 7' + a confirm
    Lyon has exactly 1 politician_images row + the feedback_section_split_check SQL (expect 0 rows for
    Pasadena). Apply via mcp__supabase-local execute_sql or psql -f, and register migration 946 in
    supabase_migrations.schema_migrations (structural). Record any newly-created district UUIDs in the SUMMARY
    for Plan 02.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id FROM essentials.governments WHERE id='d25619a9-7276-4e8b-b7ae-8028e408aee0';" returns '0656000' AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.chambers WHERE government_id='d25619a9-7276-4e8b-b7ae-8028e408aee0' AND name='City Council';" returns 1 AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='2e7f01d0-69dd-4301-b24c-58d83eb19f47';" returns 8 AND psql "$DATABASE_URL" -c "SELECT d.label FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE o.id='0cd97f4e-...';" returns 'District 7' AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=657582);" returns 1</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='d25619a9-...'` → '0656000'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='d25619a9-...' AND name='City Council'` → 1 (not 0, not 2); bdd1acad deleted
    - `SELECT COUNT(*) FROM essentials.chambers WHERE id='bdd1acad-f22d-4fa3-8ab5-667eed0e3d82'` → 0 (duplicate deleted)
    - `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='2e7f01d0-69dd-4301-b24c-58d83eb19f47'` → 8 (both Gordo + Hampton moved in)
    - District relabel: 8 rows for geo_id '0656000' — 'Pasadena Mayor' (LOCAL_EXEC) + 'District 1'..'District 7' (all LOCAL); each council row still geo_id '0656000', state 'CA' (unchanged); Hampton→D1, Cole→D2, Jones→D3, Masuda→D4, Rivas→D5, Madison→D6, Lyon→D7
    - Any missing district row created (D7 if needed); Lyon's office 0cd97f4e points at 'District 7'; no shared-district defect remains
    - 'Pasadena Mayor' LOCAL_EXEC row untouched; no new Mayor office/district created; no rotational title flag set
    - Lyon (657582) has exactly 1 politician_images row (deduped); Rivas (-700150) confirmed linked to 7bdb4f77
    - No politician's is_active/office_id modified; back-pointers NOT repaired; Cole/Williams name NOT corrected; official_count NOT set (all Plan 02)
    - feedback_section_split_check SQL returns 0 rows for Pasadena
    - Migration 946 registered in supabase_migrations.schema_migrations (structural)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
  </acceptance_criteria>
  <done>Migration 946 applied + registered; one 'City Council' chamber (2e7f01d0) remains with 8 offices; geo_id 0656000 set; the council districts relabeled D1-D7 (any missing row created), the LOCAL_EXEC Mayor row preserved; Lyon deduped to one image; Rivas confirmed linked; split-section check 0 rows; re-running the file is a no-op; new district UUIDs recorded for Plan 02.</done>
</task>

</tasks>

<threat_model>
DB-seeding phase with NO new external attack surface. No untrusted user input crosses any boundary —
migration SQL is applied via service-role/operator (psql/MCP) directly against the internal production
Supabase DB. The real threat surface is accidental data corruption — deleting the doomed chamber before
moving both offices (orphans), moving only 1 of the 2 offices, relabeling the wrong district row (multiple
similar At-Large UUIDs), creating a duplicate 'District 7', deleting the wrong Lyon image, writing to a
non-existent offices.district_type column, fabricating a duplicate Mayor row, missing the empty-string geo_id
case, or touching the wrong gov — mitigated by move-BOTH-FIRST ordering, an inline empty-chamber assert,
UUID-scoped targeting of every write, relabel-only guards, NOT EXISTS guards on the new district, the
empty-string geo_id guard, exact-id targeting of the Lyon dedupe, and a STOP-on-drift pre-flight that checks
BOTH link directions, the office→district map, the 6-vs-7 At-Large count, the Cole/Williams name, Lyon's 2
images, and the current roster.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator (psql/MCP) directly against production Supabase. No untrusted user input crosses here. |
| official city site (cityofpasadena.net) → roster confirmation | Live re-confirmation of the current Mayor + 7 district occupants before any write; read-only here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Tampering | Chamber DELETE targets the wrong chamber (both share name 'City Council') | mitigate | DELETE keyed on UUID 'bdd1acad…' ONLY, never on name; post-verify exactly one 'City Council' chamber (2e7f01d0) remains |
| T-149-02 | Tampering | Doomed chamber deleted BEFORE its 2 offices are moved, or only 1 of 2 moved → orphans (Pitfall 5) | mitigate | Move-BOTH-FIRST (UPDATE WHERE chamber_id='bdd1acad…' moves both at once) + inline DO $$ assert RAISEs if bdd1acad still has >0 offices before the DELETE; post-verify 8 offices in survivor |
| T-149-03 | Tampering | geo_id overwritten on a re-run or wrong gov, or empty-string case missed (Pitfall 8) | mitigate | UPDATE keyed on id d25619a9 + WHERE (geo_id IS NULL OR geo_id = ''); pre-flight confirms it is the City of Pasadena gov; second apply = 0 rows |
| T-149-04 | Tampering | Wrong district relabeled (multiple similar At-Large UUIDs; non-obvious occupant mapping; Madison=D6 not D2, Pitfall 3) | mitigate | UPDATE keyed on each exact At-Large UUID from the Task 1 office→district map per the locked occupant mapping; district_type/geo_id/state untouched; post-verify the labels D1-D7 |
| T-149-05 | Tampering | Duplicate 'District 7' row created on re-run, or a real shared-district defect missed (Pitfall 7 / A2/A3) | mitigate | INSERT guarded WHERE NOT EXISTS on (label, geo_id='0656000'); pre-flight §5 query confirms 6-vs-7 rows + any shared district; Lyon repoint guarded IS DISTINCT FROM; second apply = 0 rows |
| T-149-06 | Tampering | A new Mayor office/LOCAL_EXEC district fabricated, or rotational title flag wrongly set (Pitfall 4) | mitigate | No INSERT of any Mayor office/district; 'Pasadena Mayor' + office fc5e372a reused (only moved to survivor); post-verify no new LOCAL_EXEC row |
| T-149-07 | Tampering | Wrong Lyon image deleted, or both deleted leaving 0 (D-05) | mitigate | Dedupe targets the stale image id identified in Task 1 by exact id; post-verify Lyon has exactly 1 row (or a documented 0 to be re-uploaded in Wave 3) |
| T-149-08 | Tampering | Write to non-existent offices.district_type column, or altering district_type/geo_id on relabel | mitigate | District relabel touches label ONLY; no offices.district_type write anywhere; post-verify district_type still 'LOCAL', geo_id still '0656000' |
| T-149-09 | Tampering | Non-idempotent migration applied twice corrupts state | mitigate | All writes guarded (geo_id IS NULL/empty, id-scoped move/delete, label IS DISTINCT FROM, NOT EXISTS, district_id IS DISTINCT FROM, exact-id delete); second apply = 0 rows |
| T-149-10 | Tampering | Accidental write without verifying pre-state (drift since 2026-06-20) | mitigate | Task 1 read-only pre-flight confirms BOTH link directions + the office→district map + the current roster before any write; STOP-on-mismatch |
| T-149-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable; existing Pillow + Supabase client only) |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 946:
1. gov d25619a9 geo_id = '0656000' (state 'CA')
2. exactly 1 chamber 'City Council' under gov d25619a9; duplicate bdd1acad gone
3. survivor chamber 2e7f01d0 has exactly 8 offices (7 council + Mayor)
4. 8 district rows for geo_id 0656000: 'Pasadena Mayor' (LOCAL_EXEC) + 'District 1'..'District 7' (all LOCAL); each council relabel matches the occupant map (Hampton D1 / Cole D2 / Jones D3 / Masuda D4 / Rivas D5 / Madison D6 / Lyon D7)
5. any missing district row created (D7); Lyon's office 0cd97f4e points at 'District 7'; no shared-district defect; new UUIDs recorded for Plan 02
6. Lyon has exactly 1 politician_images row; Rivas confirmed linked to 7bdb4f77; 'Pasadena Mayor' LOCAL_EXEC unchanged
7. feedback_section_split_check SQL → 0 rows for Pasadena
8. no is_active/office_id changes; back-pointers not repaired; Cole/Williams name not corrected; official_count not set; no new Mayor row (all Plan 02 / already modeled)
9. migration 946 registered in schema_migrations
</verification>

<success_criteria>
All structural defects fixed and independently verified: geo_id backfilled (empty-string-safe); the duplicate
chamber merged via move-then-delete of both offices (with an inline assert proving it was empty before
deletion); the form of government corrected to Pasadena's real by-district structure (council rows relabeled
D1-D7 + any missing district created); the directly-elected Mayor row preserved (not duplicated); Lyon's
image deduped; migration 946 idempotent and registered; Pasadena is a single unambiguous 'City Council'
chamber with 8 correctly-districted offices, ready for the roster link-repair in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/149-pasadena-deep-seed/149-01-SUMMARY.md` when done (record any newly-created district UUIDs + the Cole/Williams name finding for Plan 02).
</output>
</content>
