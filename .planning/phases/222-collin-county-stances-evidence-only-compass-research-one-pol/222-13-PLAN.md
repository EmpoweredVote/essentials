---
phase: 222-collin-county-stances-evidence-only-compass-research-one-pol
plan: 13
type: execute
wave: 13
depends_on: ["222-12"]
files_modified:
  - "[EV-Accounts] C:/EV-Accounts/backend/migrations/<live-next-number>_222_parker_lucas_councils_stances.sql (NEW — number assigned live)"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md (APPEND)"
autonomous: false
requirements: [COLLIN-STANCE-01, COLLIN-STANCE-02]
must_haves:
  truths:
    - "Every un-stanced Parker and Lucas COUNCIL member on the 222-01 live worklist was individually attempted, ONE AT A TIME (D-01, D-03); both mayors were already covered in 222-08 and are not re-researched here"
    - "Every stance written carries a matching inform.politician_context row whose reasoning names a specific dated action, quote, vote, or questionnaire answer and whose sources array holds at least one real URL that was actually fetched (D-04, D-05)"
    - "Every Parker and Lucas council name appears in exactly one of {this plan's applied migration rows, 222-CONFIRMED-BLANK.md} — never neither, never both (COLLIN-STANCE-01)"
    - "A council member with no explicit on-topic evidence produces zero inform.politician_answers rows and exactly one blank-register entry naming the exact URLs checked; an all-blank council is a SUCCESS outcome (COLLIN-STANCE-01/empty, COLLIN-STANCE-02/empty)"
    - "This plan writes only (politician_id, topic_id) pairs that had no prior row per the 222-01 do-not-overwrite set (COLLIN-STANCE-01/adjacency, D-07)"
    - "Every value is a whole integer 1 through 5 — never fractional, never a defaulted middle value (COLLIN-STANCE-02/adjacency)"
    - "100% of any stance valued 1 or 5 was re-verified against its cited source before commit, plus at least a 20% random sample of the rest (222-VALIDATION.md resample contract for Tiers 2 to 4)"
    - "The migration is a single BEGIN/COMMIT transaction using ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables (COLLIN-STANCE-02/idempotency, COLLIN-STANCE-02/concurrency)"
    - "Parker is committed before Lucas research begins, so an interruption loses at most one government's un-committed work (COLLIN-STANCE-01/concurrency)"
    - "The migration has been APPLIED to production and the evidence-integrity gate returns zero rows for this plan's politician_ids — authored-but-unapplied SQL does not count as done"
  artifacts:
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
      provides: "The Parker and Lucas Council sections of the blank register (D-08)"
      contains: "Parker — Council"
  key_links:
    - "Stances attach by politician_id taken verbatim from 222-01-WORKLIST.md — never by name match"
    - "Register headings are `## {City} — Council`, distinct from the `## {City} — Mayor` sections written in 222-08, so 222-18 can reconcile both against the worklist"
  prohibitions:
    - "MUST NOT infer or assign any stance from party affiliation, endorsement, donor history, or pattern-matching to another official in the same city — only an explicit, on-topic, cited statement, recorded vote, or questionnaire answer may set a chair"
    - "MUST NOT assign a chair when the evidence is suggestive-but-not-explicit, and MUST NOT default to a middle value to avoid leaving a blank — a blank spoke is the correct, successful outcome"
    - "MUST NOT record a source URL that was not actually fetched and read, or that does not contain the cited quote, vote, or questionnaire answer"
    - "MUST NOT attribute a stance to an officeholder on the basis of a same-named person in a different city or state; the source must confirm the Texas city before the evidence is used"
    - "MUST NOT name, imply, or rely on a politician's party in reasoning text — party is never used for inference and never displayed"
---

<objective>
Evidence-only compass stance research for every un-stanced **Parker** (geo_id `4855152`) and **Lucas** (geo_id `4845012`) council member on the 222-01 live worklist — mayors excluded, already covered in 222-08 — one politician at a time, across all 11 canonical topics. Author, commit, and apply the migration and record every honest blank.

Purpose: Parker and Lucas are Low tier (RESEARCH.md section C) and neither carries a coverage chip today, so any sourced stance in either city flips a zero-coverage government and must be recorded for the 222-18 reconcile. Both cities had seats researched and seated in Phase 218, so several members may be recent with a thin public record — a legitimate blank, not a search failure.
Output: one AUDIT-ONLY migration in the EV-Accounts repo, Parker and Lucas Council register sections, and applied production rows.
</objective>

## Artifacts this phase produces

Phase 222 (all plans) produces data + planning artifacts only. No application code, no schema change, no packages.

- **[EV-Accounts repo]** `C:/EV-Accounts/backend/migrations/<N>_222_<slug>_stances.sql` — one numbered, git-committed, **AUDIT-ONLY** (NOT registered in `schema_migrations`) migration per research plan. Numbers assigned LIVE. This plan owns `<N>_222_parker_lucas_councils_stances.sql`.
- **[essentials repo]** `.planning/phases/222-.../222-01-WORKLIST.md` (222-01), `222-CONFIRMED-BLANK.md` (created 222-01, appended by every research plan), `222-NN-SUMMARY.md` per plan.
- **[essentials repo]** `.planning/REQUIREMENTS.md` — COLLIN-STANCE-01/02 + traceability (222-01).
- **[essentials repo]** `src/lib/coverage.js` — `hasContext: true` reconcile (222-18 only).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parker council — research one politician at a time, write the migration block, append the register, commit</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the Parker rows excluding the mayor are this task's exact scope, plus the live next-available migration number.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B is the SOURCE OF TRUTH for the verbatim question and five answer options of all 11 topics, OVERRIDING the researcher agent file for `housing`, `taxes`, and `healthcare` (Pitfall 1). Also read Pitfall 2, Pitfall 5 (Parker currently has no coverage chip), and section C (Low tier).
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — evidence hierarchy, quote-selection gates, inversion-trap table, de-identification protocol. Ignore its housing/taxes/healthcare scale text.
    - `C:/EV-Accounts/backend/migrations/1307_oro_valley_mayor_stances.sql` — the exact answers-plus-context upsert shape and AUDIT-ONLY header to copy.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format to mirror.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_parker_lucas_councils_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Never dispatch two research passes concurrently. Process the Parker council rows in 222-01-WORKLIST.md order, skipping the mayor row already handled in 222-08.

    For each person, run one research pass, preferring the `politician-stance-researcher` subagent and falling back to a single general-purpose research subagent seeded with that file's methodology. Pass the full verbatim question plus five answer options for all 11 topics from RESEARCH.md section B. WebFetch is the primary tool; a light WebSearch budget of a few calls per person is allowed to discover a URL, which matters here because Parker has no predictable candidate-page pattern.

    Parker source order (D-05 acceptable types): the official City of Parker council pages and the site's agenda and minutes archive, the most likely civic record for this town; local and regional news naming both the person and Parker, Texas; VOTE411 and League of Women Voters of Collin County; Community Impact Newspaper Collin County coverage; a campaign or personal site if one exists. Parker sits below Ballotpedia's stated coverage scope, so do not burn fetch cycles on Ballotpedia URL guesses; if a page resolves, read the body and confirm it names Parker, Texas before treating it as evidence (Pitfall 2).

    Assign a chair 1 through 5 ONLY where a cited source shows an explicit, on-topic position — a stated stance, a recorded council vote or motion, or a candidate-questionnaire answer (D-04). A routine procedural vote is not an on-topic position. Suggestive-but-not-explicit is a blank. No party inference, no pattern inference from a colleague or from the mayor's answers. Attempt all 8 canonical Local Lens topics (D-06) plus the 3 legacy-tail topics where explicit evidence exists (D-07). Skip any (politician_id, topic_id) pair in the 222-01 do-not-overwrite set.

    Self-audit before writing SQL: re-fetch and confirm 100% of any stance valued 1 or 5, plus at least a 20% random sample of the rest, checking that the cited source genuinely contains the claim, is on-topic for that specific compass topic, and points in the direction of the assigned chair per the inversion-trap table for that topic_key. Demote failures to blanks; demote generically-evaluative reasoning the same way.

    Re-read the live next-available migration number from 222-01-WORKLIST.md, confirm it is still free by listing `C:/EV-Accounts/backend/migrations`, and if anything is sourced create `C:/EV-Accounts/backend/migrations/<N>_222_parker_lucas_councils_stances.sql` in the EV-Accounts repo with an AUDIT-ONLY header naming Phase 222, the Parker and Lucas councils, and the date, stating it is deliberately not registered in `schema_migrations`. Use a single BEGIN/COMMIT transaction and write the Parker block: for each surviving (politician, topic) pair, an insert into `inform.politician_answers (politician_id, topic_id, value)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value`, immediately followed by an insert into `inform.politician_context (politician_id, topic_id, reasoning, sources)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources`. Dollar-quote all reasoning text, using a distinct tag on delimiter collision. Values are whole numbers 1 through 5.

    Append a `## Parker — Council` section to `222-CONFIRMED-BLANK.md` with an `Evidence:` line naming the exact URLs checked and one bullet per attempted person with no sourced position (full_name, office title, politician_id). Write the heading even when there are zero blanks. Update the running `Count:` line. If Parker sourced at least one stance, note in the SUMMARY that it moved from zero to non-zero coverage so 222-18 flips its chip.

    Commit both repos locally before starting Lucas. In EV-Accounts use `git -C "C:/EV-Accounts" add` then `git -C "C:/EV-Accounts" commit`; never `cd` into that repo and never `git push` it (a push deploys accounts-api to Render and carries every commit already ahead).
  </action>
  <verify>
    <automated>grep -q "^## Parker — Council" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - The number of Parker council entries researched equals the number of non-mayor Parker rows in 222-01-WORKLIST.md — no person skipped, none pre-declared hopeless.
    - The Parker mayor was not re-researched here.
    - The Parker register `Evidence:` line names specific URLs that were actually fetched.
    - 100% of value-1 and value-5 stances plus at least a 20% sample of the rest were re-fetched and confirmed; demotions are recorded with a reason.
    - Every value literal is a whole number 1 through 5, each answers insert is immediately followed by its matching context insert, and every `sources` array has at least one URL.
    - `222-CONFIRMED-BLANK.md` has a `## Parker — Council` section, and the union of Parker migration politician_ids and Parker Council register bullets equals the non-mayor Parker worklist rows with no overlap and no omission.
    - Whether Parker moved from zero to non-zero coverage is recorded explicitly.
    - Both repos are committed before Lucas research begins; `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>Parker's council is fully attempted with recorded dead ends, self-audited, written into the migration where sourced, registered where blank, and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Lucas council — research one politician at a time, extend the migration, append the register, commit</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the Lucas rows excluding the mayor are this task's exact scope.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B (verbatim topic text, overriding the agent file for housing, taxes, healthcare), Pitfall 2, Pitfall 5 (Lucas currently has no coverage chip), section C (Low tier).
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — methodology and inversion-trap table.
    - `C:/EV-Accounts/backend/migrations/<N>_222_parker_lucas_councils_stances.sql` — the file being extended (created in Task 1, or created here if Parker sourced nothing).
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format; Lucas Place 1 and Place 2 were among the seats disambiguated in Phase 218, so confirm the worklist person is the current officeholder before citing anything.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_parker_lucas_councils_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Process the Lucas council rows in 222-01-WORKLIST.md order, skipping the mayor row already handled in 222-08, using the same research method, the same RESEARCH.md section B topic text, and the same strict D-04 evidence bar as Task 1.

    Lucas source order (D-05 acceptable types): the official City of Lucas council pages and the site's agenda and minutes archive; local and regional news naming both the person and Lucas, Texas; VOTE411 and League of Women Voters of Collin County; Community Impact Newspaper Collin County coverage; a campaign or personal site if one exists. Lucas sits below Ballotpedia's stated coverage scope — confirm the Texas city in any page body before treating a hit as evidence (Pitfall 2).

    If a Lucas source names a different person for the seat than the worklist row does, record it as a flagged roster-currency finding and keep researching the worklist person — never attach one person's statements to another person's politician_id.

    Self-audit before extending the SQL: re-fetch and confirm 100% of any stance valued 1 or 5, plus at least a 20% random sample of the rest, checking presence, on-topic fit, and direction against the inversion-trap table for that topic_key. Demote failures to blanks.

    Extend `C:/EV-Accounts/backend/migrations/<N>_222_parker_lucas_councils_stances.sql` with the Lucas block inside the same single BEGIN/COMMIT transaction, using the identical answers-plus-context upsert pair, dollar-quoted reasoning, and whole 1-through-5 values. If the file does not exist because Parker sourced nothing, create it now with the same AUDIT-ONLY header. If neither council sources anything, do not create the file at all — the register alone carries the outcome, and that is a complete plan.

    Append a `## Lucas — Council` section to `222-CONFIRMED-BLANK.md` in the 221 format, with an `Evidence:` line naming the exact URLs checked and one bullet per attempted person with no sourced position. Write the heading even when there are zero blanks. Update the running `Count:` line. If Lucas sourced at least one stance, note in the SUMMARY that it moved from zero to non-zero coverage.

    Commit both repos locally. In EV-Accounts use `git -C "C:/EV-Accounts" add` then `git -C "C:/EV-Accounts" commit`; never `cd` into that repo and never `git push` it.
  </action>
  <verify>
    <automated>grep -q "^## Lucas — Council" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - The number of Lucas council entries researched equals the number of non-mayor Lucas rows in 222-01-WORKLIST.md — no person skipped.
    - The Lucas mayor was not re-researched here.
    - Any roster discrepancy is flagged rather than acted on.
    - The Lucas register `Evidence:` line names specific URLs that were actually fetched.
    - 100% of value-1 and value-5 stances plus at least a 20% sample of the rest were re-fetched and confirmed; demotions are recorded with a reason.
    - Every value literal is a whole number 1 through 5, each answers insert is immediately followed by its matching context insert, and every `sources` array has at least one URL.
    - `222-CONFIRMED-BLANK.md` has a `## Lucas — Council` section, and the union of this plan's migration politician_ids and its Council register bullets equals the non-mayor Parker + Lucas worklist rows with no overlap and no omission.
    - Whether Lucas moved from zero to non-zero coverage is recorded explicitly.
    - `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>Lucas's council is fully attempted with recorded dead ends, self-audited, written into the migration where sourced, registered where blank, and committed; nothing applied yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator applies the Parker/Lucas councils migration and runs the evidence-integrity gate</name>
  <read_first>
    - `C:/EV-Accounts/backend/migrations/<N>_222_parker_lucas_councils_stances.sql` — the SQL to apply, if it exists.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the evidence-integrity gate query.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the Parker and Lucas Council sections.
  </read_first>
  <what-built>
    A committed, AUDIT-ONLY migration covering the Parker and Lucas council members who yielded sourced positions, plus their Council register sections. The executor has no Supabase MCP access, so the SQL is authored and committed but NOT yet applied — the repository looks complete while production is unchanged until this checkpoint runs.
  </what-built>
  <action>
    Halt here and hand control to the operator. Do NOT attempt the apply step from this session — gsd-executor has no Supabase MCP binding, so the SQL is committed but production is still unchanged until the operator runs it. Present the how-to-verify steps below verbatim, wait for the resume signal, then record the operator's reported apply confirmation, evidence-integrity gate result, and reconcile result in this plan's SUMMARY before the phase advances. If any gate returns rows, do not mark this plan done.
  </action>
  <how-to-verify>
    1. If a migration file exists, apply it via `mcp__supabase-local__execute_sql` (this is production — the write is live). Do NOT register it in `schema_migrations`. If no file exists because both councils ended blank, confirm the register documents everyone and skip to step 3.
    2. Run the evidence-integrity gate from `222-VALIDATION.md` scoped to this plan's politician_ids: it must return zero rows.
    3. Run the exactly-one-bucket reconcile for the non-mayor Parker and Lucas rows against `222-01-WORKLIST.md` and the Council register sections — never both, never neither.
    4. Note whether Parker or Lucas moved from zero to at least one stance — neither has a coverage chip today and 222-18 will need to flip any that did.
  </how-to-verify>
  <acceptance_criteria>
    - Any migration file has been applied to production via `mcp__supabase-local__execute_sql` and the apply is confirmed, not assumed; or the absence of a file is confirmed as the correct all-blank outcome.
    - The evidence-integrity gate returns zero rows for this plan's politician_ids.
    - The Parker and Lucas council exactly-one-bucket reconcile passes.
    - Zero-to-non-zero status is recorded for both cities for the 222-18 coverage-chip reconcile.
    - No row was written to `schema_migrations`.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live (or confirmed unnecessary) and the gate returns zero rows, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped web text → SQL migration | Quotes and URLs harvested from the open web are embedded into a statement executed against production |
| research judgment → public civic profile | An assigned chair becomes a public claim about a real, named person |
| recently-seated members → thin or stale record | Both councils had seats disambiguated in Phase 218 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-04 | Tampering | migration SQL text | high | mitigate | Dollar-quote all scraped reasoning text; never concatenate scraped text into a bare SQL string |
| T-222-05 | Repudiation | fabricated or mismatched source URL | critical | mitigate | 100% re-fetch of value-1/value-5 stances plus a 20% sample; failing sources demote the stance to a blank |
| T-222-06 | Spoofing | wrong-person attribution (homonym or stale seat) | critical | mitigate | Confirm the Texas city in the page body; research the worklist politician_id only; flag roster discrepancies rather than switching subjects |
| T-222-07 | Denial of Service | false-positive completion (authored but unapplied SQL) | high | mitigate | Blocking apply-and-verify checkpoint; the gate runs against production, not the file |
| T-222-SC | Tampering | package installs | low | accept | This plan installs no packages |
</threat_model>

<verification>
- Every non-mayor Parker and Lucas worklist name individually attempted, sequentially; Parker committed before Lucas began.
- Every applied stance has a whole 1-5 value, a specific dated reasoning, and at least one re-fetched source URL at the Tier 2-4 sampling rate.
- Evidence-integrity gate returns zero rows for this plan's politician_ids after apply.
- Council exactly-one-bucket reconcile passes for both cities.
- EV-Accounts migration committed locally and not pushed.
</verification>

<success_criteria>
Parker and Lucas council members each carry evidence-cited compass chairs where a source explicitly documents a position, and an honest register entry naming the sources checked where none exists — with both cities' coverage-chip status recorded for close-out.
</success_criteria>

<output>
Create `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-13-SUMMARY.md` when done — record per-city attempted, sourced, stances applied, blanks, the migration number used, any stance demoted, any roster flag, and each city's zero-to-non-zero status.
</output>
