---
phase: 195-oro-valley-deep-seed
plan: 04
type: execute
wave: 4
depends_on: ["195-03"]
files_modified:
  - src/lib/buildingImages.js
  - src/lib/coverage.js
autonomous: false
requirements: [BANR-01, SUB-01]
must_haves:
  truths:
    - "A licensed Oro Valley community banner (real photo, no AI, no aerial) is processed to the banner spec and uploaded to Storage at cities/oro-valley.jpg"
    - "buildingImages.js CURATED_LOCAL has a state-scoped 'oro valley' entry pointing at the uploaded banner"
    - "coverage.js gets an 'Oro Valley' area APPENDED to the EXISTING Arizona COVERAGE_STATES block (created by Phase 194) — NOT a second Arizona block, and NOT an entry in COVERAGE_COUNTIES (where Pima County lives) — with a DB-honest chip (hasContext:true, since stances landed in Plan 03) browsing geo_id 0451600"
    - "The Oro Valley banner is visually distinct from Pima County's Catalina/Pusch-Ridge landscape banner, the Tucson downtown streetscape, and the AZ-state Phoenix skyline"
    - "A real Oro Valley street address, queried against the live candidates-search API, resolves to the seeded Town of Oro Valley Mayor + all 6 at-large Council Members (the phase-goal's resident-facing outcome, end-to-end)"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "CURATED_LOCAL 'oro valley' → uploaded banner URL"
      contains: "oro valley"
    - path: "src/lib/coverage.js"
      provides: "'Oro Valley' area appended to the EXISTING Arizona COVERAGE_STATES block, browseGovernmentList ['0451600'], hasContext:true"
      contains: "Oro Valley"
  key_links:
    - from: "coverage.js COVERAGE_STATES Arizona → 'Oro Valley'"
      to: "buildingImages.js CURATED_LOCAL 'oro valley'"
      via: "browse label 'Oro Valley' → representingCity → getBuildingImages substring match"
      pattern: "oro valley"
    - from: "buildingImages.js 'oro valley'"
      to: "Storage cities/oro-valley.jpg"
      via: "src URL (state-scoped 'AZ')"
      pattern: "cities/oro-valley.jpg"
---

<objective>
Give the Town of Oro Valley its licensed community banner (BANR-01) and its DB-honest coverage chip.
Source a real Oro Valley photo ONE AT A TIME (no AI, no aerial), process it to the banner spec via
`scripts/banners/process_banner.py`, upload to Storage at `cities/oro-valley.jpg` via
`upload_banner.py`, wire it into `src/lib/buildingImages.js` CURATED_LOCAL as a single state-scoped
`'oro valley'` entry, and surface the Town of Oro Valley in `src/lib/coverage.js`.

CRITICAL DELTA from Tucson (Pitfall 8): unlike Phase 194 (which created the FIRST Arizona
`COVERAGE_STATES` block), the Arizona block ALREADY EXISTS in `coverage.js` today — it contains one
`'Tucson'` area. This phase APPENDS one `'Oro Valley'` area object to that EXISTING block's `areas`
array. Do NOT create a second `{ name: 'Arizona', ... }` block (that would be a duplicate-block bug),
and do NOT add Oro Valley to `COVERAGE_COUNTIES` (where Pima County lives — untouched). Likewise
`buildingImages.js` is a plain APPEND of a new `'oro valley'` key.

CRITICAL (Open Question 2 — visual distinctiveness): 2 of the 3 shortlisted Wikimedia candidates
feature Pusch Ridge (part of the same Santa Catalina range Pima's already-shipped banner uses). The
one-at-a-time sourcing pass must deliberately weigh whether the chosen banner reads as too similar to
the Pima County banner side-by-side; a genuinely distinct non-mountain subject (Oro Valley Marketplace,
Steam Pump Ranch historic site, Oro Valley Community Center) is worth a look first. Not a hard blocker
(Pima's mountain banner was already approved), but a deliberate call per the project's
"visually distinct from sibling banners" convention.

Purpose: Satisfies SUB-01 / ROADMAP success criteria #4 (licensed banner sourced/processed/uploaded/
wired) and #5 (Oro Valley surfaced with a DB-honest chip). This is the 5th Tucson-metro deep-seed
banner and must stay visually distinct from Pima County (Catalina landscape), Tucson (downtown
streetscape), and the AZ-state Phoenix skyline.
Output: uploaded `cities/oro-valley.jpg` + edited `buildingImages.js` + `coverage.js` in the
`essentials` repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/195-oro-valley-deep-seed/195-RESEARCH.md
@.planning/phases/195-oro-valley-deep-seed/195-PATTERNS.md

<execution_note>
Frontend files (`src/lib/buildingImages.js`, `src/lib/coverage.js`) live in the CURRENT `essentials`
repo (commit here, not `C:/EV-Accounts`). The banner SOURCING + processing + Storage upload are
ORCHESTRATOR-RUN (executor has no network/Storage access): the inline orchestrator sources the photo
one-at-a-time, runs the banner toolchain, and uploads. Per project convention
(`feedback_banner_source_one_at_a_time`, `feedback_banner_prefer_photo_over_gradient`,
`feedback_banner_show_state_shot_on_conflict`) banner sourcing is sequential, prefers a real photo, and
shows the Pima Catalinas + Tucson streetscape + AZ-state Phoenix banners alongside during review
because there is a genuine visual-conflict risk with Pima's Catalina/Pusch-Ridge banner. The CDN URL is
deterministic (`cities/oro-valley.jpg`) so the frontend wiring can be authored regardless of upload
order. Depends on Plan 03 — hasContext:true is only honest because Plan 03 seeded ≥1 stance row.
</execution_note>

<interfaces>
- Banner spec: process via `scripts/banners/process_banner.py` (`--url <src> --output <path>
  --vertical-anchor` tuned per shot), upload via `scripts/banners/upload_banner.py --file <path>
  --dest cities/oro-valley.jpg` (needs SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
  Destination tier is `cities/` (banner TYPE, not government type). Storage is no-cache → overwrite =
  instant refresh.
- CDN URL: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/oro-valley.jpg
- buildingImages.js CURATED_LOCAL: add `'oro valley': { state: 'AZ', src: '<CDN URL>' }` (single-variant
  — 'oro valley' has no same-named-city collision in the covered set, so NOT the array-of-{state,src}
  form) with an attribution comment (subject | author | license) above it, matching the file's existing
  'tucson' entry convention. Key is SPACE-form ('oro valley') to match the coverage.js label; the
  Storage file is HYPHENATED (oro-valley.jpg). getBuildingImages() needs NO code change.
- coverage.js: APPEND to the EXISTING Arizona COVERAGE_STATES block's `areas` array (which already holds
  the 'Tucson' entry):
  `{ label: 'Oro Valley', browseGovernmentList: ['0451600'], browseStateAbbrev: 'AZ', hasContext: true }`.
  hasContext:true is honest because Plan 03 seeded ≥1 stance row. Do NOT create a second Arizona block
  (Pitfall 8); do NOT touch COVERAGE_COUNTIES (Pima County lives there — unrelated). Placement within
  the areas array is cosmetic.
- Shortlisted banner candidates (RESEARCH — visual selection still done at execute time):
  `Pusch_Ridge_from_Oro_Valley.jpg` (2304×1728, CC BY-SA 3.0/GFDL) and
  `Oro_Valley_Calle_Concordia.JPG` (1095×821, CC BY-SA 3.0) — BOTH feature Pusch Ridge (Catalina-range
  overlap with Pima's banner — weigh distinctiveness). Consider a non-mountain subject (Oro Valley
  Marketplace / Steam Pump Ranch / Community Center) if the mountain shots read too similar to Pima's.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Source + process + upload the Oro Valley banner, one at a time (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/195-oro-valley-deep-seed/195-RESEARCH.md (§ Standard Stack banner candidates; § Open Question 2 — Pusch-Ridge/Catalina visual overlap with the Pima banner; the one-at-a-time sourcing pass makes the final visual selection)
    - .planning/phases/195-oro-valley-deep-seed/195-PATTERNS.md (lines 358-392 — the CURATED_LOCAL 'oro valley' single-key entry + attribution comment convention; the process_banner.py / upload_banner.py commands + cities/<slug>.jpg destination)
    - C:/Transparent Motivations/essentials/scripts/banners/process_banner.py (the banner spec + --vertical-anchor)
    - C:/Transparent Motivations/essentials/scripts/banners/upload_banner.py (the Storage upload + --dest convention)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. The orchestrator sources a licensed Oro Valley photo ONE AT A TIME, processes it to the banner spec, uploads to cities/oro-valley.jpg, and the operator visually approves the banner — with explicit attention to distinctiveness from Pima's Catalina/Pusch-Ridge banner — before wiring. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A licensed community banner for the Town of Oro Valley — the 5th Tucson-metro deep-seed banner.
    RESEARCH shortlisted candidates (both mountain shots featuring Pusch Ridge, which overlaps Pima's
    Catalina-range banner) but visual selection is done at execute time. The orchestrator performs the
    one-at-a-time sourcing pass against commons.wikimedia.org (Category:Oro Valley, Arizona) — and,
    given Open Question 2, deliberately considers a non-mountain subject (Oro Valley Marketplace / Steam
    Pump Ranch / Community Center) if a mountain shot reads too similar to Pima's — processes via
    process_banner.py (--vertical-anchor tuned per shot), and uploads via upload_banner.py to
    cities/oro-valley.jpg. Human visual QA is required because "real photo, no AI, no aerial, distinct
    from the Pima Catalinas + Tucson streetscape + Phoenix skyline" is a judgment a row-count cannot make.
  </what-built>
  <how-to-verify>
    1. The orchestrator sources ONE candidate photo (licensed CC/public-domain, real photo, NOT
       AI-generated, NOT aerial/drone), records subject | author | license, runs
       `python scripts/banners/process_banner.py --url <src> --output /tmp/oro-valley.jpg
       --vertical-anchor 0.4` (tune anchor per shot), then
       `python scripts/banners/upload_banner.py --file /tmp/oro-valley.jpg --dest cities/oro-valley.jpg`
       (SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
    2. Operator visually confirms: reads as Oro Valley, a real photo (no AI artifacts, no aerial/drone
       shot), and visually DISTINCT from the Pima County Catalina/Pusch-Ridge landscape banner
       (highest-risk overlap), the Tucson downtown streetscape, AND the AZ-state Phoenix skyline — show
       all three alongside during review per feedback_banner_show_state_shot_on_conflict.
    3. If unusable or too similar to Pima's banner, the orchestrator re-sources another candidate ONE AT
       A TIME (prefer a real photo, ideally a non-mountain subject; gradient only if truly nothing
       usable exists).
  </how-to-verify>
  <acceptance_criteria>
    - `curl -sI https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/oro-valley.jpg` returns HTTP 200.
    - The uploaded image matches the banner spec (process_banner.py output dimensions) — PIL/curl-download dimension check.
    - Operator approves: real photo, Oro Valley subject, no AI, no aerial, distinct from the Pima Catalina/Pusch-Ridge banner AND the Tucson streetscape AND the AZ-state Phoenix skyline.
    - Subject | author | license recorded in 195-04-SUMMARY.md for the attribution comment.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the operator confirms the uploaded banner reads as Oro Valley, is a real non-aerial photo, and is distinct from the Pima County + Tucson + AZ-state banners — or describe what to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner into buildingImages.js + append Oro Valley to the EXISTING Arizona coverage.js block</name>
  <files>src/lib/buildingImages.js, src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/195-oro-valley-deep-seed/195-PATTERNS.md (lines 327-396 — the exact CURATED_LOCAL 'oro valley' single-variant entry + attribution comment, and the APPEND to the EXISTING Arizona COVERAGE_STATES areas array; the Pitfall 8 warning that the Arizona block already exists and Pima is in COVERAGE_COUNTIES; getBuildingImages needs NO change)
    - C:/Transparent Motivations/essentials/src/lib/buildingImages.js (the CURATED_LOCAL object + the existing single-key 'tucson' entry to mirror; getBuildingImages substring/state-scoping logic)
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the EXISTING Arizona COVERAGE_STATES block with its 'Tucson' area — append to its areas array; confirm NO second Arizona block is created; the separate COVERAGE_COUNTIES array holding Pima County — do NOT touch it)
    - .planning/phases/195-oro-valley-deep-seed/195-04-SUMMARY.md (the banner subject | author | license from Task 1 for the attribution comment)
  </read_first>
  <action>
    In `src/lib/buildingImages.js`, add ONE entry to the CURATED_LOCAL object:
    `'oro valley': { state: 'AZ', src: '<CDN URL for cities/oro-valley.jpg>' }` (single-variant, NOT the
    array-of-{state,src} form — 'oro valley' has no same-named-city collision), with an attribution
    comment above it (subject | author | license from Task 1) matching the file's existing 'tucson'
    convention. Note in the comment it is deliberately distinct from the Pima County Catalina landscape
    banner, the Tucson downtown streetscape, and the AZ-state Phoenix skyline. Make NO change to
    getBuildingImages() — its substring match + state-scoping already handle the label. Key is the
    SPACE-form 'oro valley'; the Storage file is HYPHENATED (oro-valley.jpg).

    In `src/lib/coverage.js`, APPEND one area object to the EXISTING Arizona COVERAGE_STATES block's
    `areas` array (the block that already contains 'Tucson'):
    `{ label: 'Oro Valley', browseGovernmentList: ['0451600'], browseStateAbbrev: 'AZ', hasContext: true }`.
    Do NOT create a second `{ name: 'Arizona', ... }` block (Pitfall 8), do NOT add Oro Valley to
    COVERAGE_COUNTIES (where Pima County lives), and do NOT broaden parseCityFromAddress. hasContext:true
    is honest because Plan 03 seeded ≥1 stance row (this plan depends on Plan 03). Placement within the
    areas array is cosmetic (file is not strictly A-Z).

    Commit both files to the `essentials` repo via git.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qiE "'oro valley':\s*\{" src/lib/buildingImages.js && grep -qF "cities/oro-valley.jpg" src/lib/buildingImages.js && grep -qF "'Oro Valley'" src/lib/coverage.js && grep -qF "'0451600'" src/lib/coverage.js && [ "$(grep -c "name: 'Arizona'" src/lib/coverage.js)" = "1" ] && node --input-type=module -e "import('./src/lib/coverage.js').then(()=>console.log('coverage-ok')).catch(e=>{console.error(e);process.exit(1)})" && node --input-type=module -e "import('./src/lib/buildingImages.js').then(()=>console.log('bi-ok')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has an `'oro valley'` key with `state: 'AZ'` and src = the cities/oro-valley.jpg CDN URL, plus an attribution comment (subject | author | license).
    - coverage.js Arizona COVERAGE_STATES block's areas array has a NEW `{ label: 'Oro Valley', browseGovernmentList: ['0451600'], browseStateAbbrev: 'AZ', hasContext: true }` entry.
    - There is EXACTLY ONE `name: 'Arizona'` block in coverage.js (no duplicate block created — Pitfall 8).
    - COVERAGE_COUNTIES is NOT modified (Pima County entry untouched); no Oro Valley entry added there.
    - Both files parse cleanly via `node --input-type=module -e "import(...)"` (exit 0).
    - getBuildingImages() and parseCityFromAddress unchanged.
    - `npx vitest run src/lib/buildingImages.test.js` stays green (established non-gap; run as a spot-check).
    - Both files committed to the `essentials` repo.
  </acceptance_criteria>
  <done>Oro Valley banner wired into CURATED_LOCAL and surfaced via a new area appended to the EXISTING Arizona COVERAGE_STATES block with a DB-honest hasContext:true chip; exactly one Arizona block; COVERAGE_COUNTIES untouched; both frontend files parse, buildingImages.test.js green, both committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the resident-facing outcome end-to-end — real Oro Valley address → Mayor + 6 council (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/195-oro-valley-deep-seed/195-01-SUMMARY.md (the 7 seeded officials + their offices/titles to expect back)
    - .planning/phases/195-oro-valley-deep-seed/195-RESEARCH.md (§ Architecture Patterns — "Address routing (NO CODE CHANGE)"; the existing G4110→(LOCAL,LOCAL_EXEC) mapping is expected to cover this with zero code change)
  </read_first>
  <action>ORCHESTRATOR-RUN closing gate — no executor file action. The whole phase goal is "Oro Valley residents can SEE their council with a compass." Every other plan verifies DB structure (row counts, joins) but not the live resident-facing result. The orchestrator POSTs a real Oro Valley street address (e.g. Town Hall, 11000 N La Cañada Dr, Oro Valley, AZ 85737) to the live candidates-search API (POST /candidates/search {query}, full street address required per the search contract) and confirms the response includes the seeded Mayor (LOCAL_EXEC) + all 6 at-large Council Members (LOCAL, all on geo_id 0451600). This exercises the geofence→district→office join that Plan 01 seeded, end-to-end, against the same shared-LOCAL-row shape that is already live in prod for Torrance. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A live end-to-end confirmation that the phase goal is actually TRUE for a resident — not just that
    the rows exist. Mirrors Phase 194 (Tucson)'s dedicated live-routing verification, scaled down to
    Oro Valley's simpler at-large shape (no per-ward differentiation to check — every Oro Valley address
    should return the SAME Mayor + 6 council, which IS the correct at-large behavior).
  </what-built>
  <how-to-verify>
    1. The orchestrator POSTs a real Oro Valley street address (Town Hall, 11000 N La Cañada Dr, Oro
       Valley, AZ 85737, or another verified-in-town address) to the live candidates-search API
       (accounts-api.empowered.vote, POST /candidates/search {query}).
    2. Confirm the response includes the Town of Oro Valley government with the Mayor (LOCAL_EXEC) + all
       6 at-large Council Members (LOCAL), matching the roster seeded in Plan 01 (party never displayed —
       antipartisan).
    3. If any council member is missing or an extra/wrong office appears, STOP — this indicates a
       district-attach or geofence-mapping defect from Plan 01 that must be fixed before the phase closes.
  </how-to-verify>
  <acceptance_criteria>
    - A real Oro Valley street address POSTed to the live candidates-search API returns the Town of Oro Valley with the Mayor + exactly 6 at-large Council Members (7 offices total), matching Plan 01's seeded roster.
    - No wrong-jurisdiction or duplicate Oro Valley office appears in the response (section-split / wrong-district-attach would surface here).
    - Result recorded in 195-04-SUMMARY.md (the address used + the offices returned).
  </acceptance_criteria>
  <resume-signal>Type "approved" once a real Oro Valley address returns the Mayor + all 6 Council Members from the live search API — or describe the routing discrepancy found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wikimedia (remote) → banner pipeline | Untrusted remote image bytes enter process_banner.py over HTTP |
| banner pipeline → Storage | Processed banner crosses into the production politician_photos bucket |
| frontend edits → live app | coverage.js / buildingImages.js changes ship to Render on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-BAN | Tampering | remote banner image fetch | mitigate | process_banner.py re-encodes to JPEG (strips embedded payloads); operator visual QA confirms a real non-AI non-aerial photo before wiring |
| T-195-LIC | Repudiation | banner license provenance | mitigate | Subject \| author \| license recorded in the SUMMARY + the attribution comment; only CC/public-domain sources used |
| T-195-COVLIE | Tampering | dishonest coverage chip | mitigate | hasContext:true only set because Plan 03 landed ≥1 stance row (depends_on 195-03); node parse-check confirms valid module |
| T-195-COVBLK | Tampering | wrong coverage array (duplicate AZ block / append to COUNTIES) | mitigate | APPEND to the EXISTING Arizona COVERAGE_STATES block (Pitfall 8); grep asserts exactly ONE `name: 'Arizona'` block; COVERAGE_COUNTIES Pima entry untouched |
| T-195-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; never in a committed frontend file |
| T-195-SC | Tampering | package installs | accept | No new installs — reuses installed PIL/requests banner toolchain; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- cities/oro-valley.jpg returns HTTP 200 at the banner spec; operator-approved as a real, distinct Oro Valley photo.
- CURATED_LOCAL 'oro valley' → banner URL; 'Oro Valley' area APPENDED to the EXISTING Arizona COVERAGE_STATES block (exactly one Arizona block); COVERAGE_COUNTIES untouched.
- Both frontend files parse, buildingImages.test.js green, both committed.
</verification>

<success_criteria>
SUB-01 / ROADMAP #4 (licensed banner sourced/processed/uploaded/wired) and #5 (Oro Valley surfaced with
a DB-honest chip appended to the existing Arizona block) are TRUE.
</success_criteria>

<output>
Create `.planning/phases/195-oro-valley-deep-seed/195-04-SUMMARY.md` when done — include the banner
subject | author | license and the CDN URL.
</output>
</content>
