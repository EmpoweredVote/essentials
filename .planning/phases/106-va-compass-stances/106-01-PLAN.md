---
phase: 106-va-compass-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql
autonomous: true
requirements:
  - VA-STANCES-01
must_haves:
  truths:
    - "Abigail Spanberger has >=15 stance rows in inform.politician_answers after migration 326 applies"
    - "Every Spanberger stance row in inform.politician_context has at least 1 URL in sources (100% citation rate per D-10)"
    - "Compass renders on Spanberger's profile page using the seeded stances"
    - "No defaulted/neutral fallback rows exist — only topics with documented evidence are inserted"
    - "D-05: per-individual SQL file 326_spanberger_stances.sql created following naming convention {N}_{firstname}_{lastname}_stances.sql, starting at 326"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql"
      provides: "Per-individual stance migration for Governor Abigail Spanberger"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "326_spanberger_stances.sql"
      to: "essentials.politicians (external_id=-510001)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
    - from: "inform.politician_answers"
      to: "computeDisplaySpokes() in src/lib/compass.js"
      via: "compass spoke selection reads politician_answers directly"
      pattern: "politician_answers.value"
---

<objective>
Research Abigail Spanberger's (Governor of Virginia, external_id=-510001) public record across all currently-active compass topics, write per-individual stance migration 326, apply it to production Supabase, and verify the stance row count.

Purpose: Satisfies VA-STANCES-01 (executives stances) for the highest-priority Virginia executive. Spanberger has a rich record as US House Rep VA-7 (2019-2025) and now Governor, so target 18-21+ stances per D-09.

Output: `326_spanberger_stances.sql` applied; >=15 stance rows in `inform.politician_answers` for Spanberger; >=15 paired rows in `inform.politician_context`, every row with at least 1 source URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-va-compass-stances/106-CONTEXT.md
@.planning/phases/106-va-compass-stances/106-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Spanberger UUID + active topic list, then research all topics</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (all decisions D-01 through D-10)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md (Section "Full Example" and "Per-Stance INSERT Pattern")
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120 — header, topic UUID reference block, BEGIN, first Brown section)
    - C:/EV-Accounts/backend/migrations/317_va_state_executives.sql (lines 94-110 to confirm Spanberger external_id=-510001)
  </read_first>
  <action>
    Step A (UUID + topic resolution, run via mcp__supabase-local__execute_sql):
      1. Resolve Spanberger UUID:
         SELECT id FROM essentials.politicians WHERE external_id = -510001;
         Record returned UUID as ${SPANBERGER_UUID}. If 0 rows, abort — migration 317 must be applied first.
      2. Resolve active compass topics:
         SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;
         Record full list as ${TOPIC_LIST}. The 282 migration ships 43 topics in its header comment; CONTEXT.md references "44" — use the live count from this query as ground truth.

    Step B (research, ONE agent only per D-08, NEVER in parallel):
      Launch a single web-research subagent (or perform web research yourself sequentially) to gather Spanberger's documented positions on EVERY topic in ${TOPIC_LIST}.
      Per D-01: attempt all topics. Per chair philosophy: omit topics with no evidence — do NOT default to neutral.
      Per D-09: target 18-21+ stances; ROADMAP minimum is >=15.
      For each topic where evidence is found, record: { topic_slug, value (1-5 integer per D-02), reasoning (2-5 sentences citing specific bills/votes/statements), sources (1+ URLs with paths, no bare domains) }.
      Spanberger has a documented record on: abortion, climate-change, healthcare (ACA defender), immigration (former CIA officer, moderate stance), gun-safety-adjacent topics, campaign-finance (anti-corruption advocate), voting-rights, redistricting (VA-7 redistricting commission supporter), tariffs, ukraine-support, social-security/medicare, taxes, civil-rights, deportation, public-safety-approach, judicial-* topics from House votes, fossil-fuels/economic-development, growth-and-development, transportation-priorities, AI-regulation. Local-only topics (city-sanitation, rent-regulation, residential-zoning, jail-capacity, local-environment, local-immigration) likely yield no Spanberger-specific evidence — omit if none found.

    Step C (do NOT write the migration file yet — that is Task 2):
      Save intermediate research output as `.planning/phases/106-va-compass-stances/_tmp-spanberger-research.json` with shape:
        [{ "topic_slug": "...", "topic_id": "uuid", "value": 1-5, "reasoning": "...", "sources": ["url1","url2"] }, ...]
      This file is the input to Task 2.
  </action>
  <verify>
    <automated>
      Run mcp__supabase-local__execute_sql with:
        SELECT id FROM essentials.politicians WHERE external_id = -510001;
      Must return exactly 1 row. Then verify temp research file exists and has >=15 entries:
        Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-spanberger-research.json"
        ((Get-Content "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-spanberger-research.json" | ConvertFrom-Json).Count -ge 15)
    </automated>
  </verify>
  <done>
    - Spanberger UUID resolved and recorded
    - Live active topic list resolved from inform.compass_topics
    - Research JSON file exists with >=15 entries, every entry has non-empty sources array
    - No defaulted neutral entries (per D-01: blank > neutral fallback)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 326_spanberger_stances.sql exactly matching the 282 pattern</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120 for header + first politician section structure)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1233-1251 for COMMIT + verification comment block)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md ("Full Example: Minimal Valid Per-Individual Stance Migration")
    - .planning/phases/106-va-compass-stances/_tmp-spanberger-research.json (created in Task 1)
  </read_first>
  <action>
    Create file `C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql` with this exact structure:

      1. Header comment block (per 282 lines 1-12):
         -- Migration 326: Abigail Spanberger Stances
         -- Purpose: Insert/upsert stance data for Abigail Spanberger (Governor of Virginia).
         -- Topic scope: All currently-active compass topics attempted; evidence-only — topics with no
         --   evidence are omitted entirely (no neutral defaults per D-01).
         -- Idempotency: ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables.
         -- Apply to remote Supabase via Supabase MCP (mcp__supabase-local is remote production per CLAUDE.md).

      2. Topic UUID reference block (copy verbatim from 282 lines 14-57, then append any extra rows present in the live active set from Task 1 Step A).

      3. `BEGIN;` then politician section header:
         -- ============================================================
         -- Abigail Spanberger
         -- ============================================================

      4. For each entry in _tmp-spanberger-research.json, emit a pair of INSERTs exactly matching 282 lines 65-79:

         -- ----- Abigail Spanberger / {topic-slug} -----
         INSERT INTO inform.politician_answers (politician_id, topic_id, value)
         VALUES ('${SPANBERGER_UUID}',
                 '{topic_uuid}',
                 {value}.0)
         ON CONFLICT (politician_id, topic_id)
         DO UPDATE SET value = EXCLUDED.value;

         INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
         VALUES ('${SPANBERGER_UUID}',
                 '{topic_uuid}',
                 $${reasoning text}$$,
                 ARRAY['url1', 'url2']::text[]::text[])
         ON CONFLICT (politician_id, topic_id)
         DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

      5. `COMMIT;`

      6. Verification comment block at end (per 282 lines 1235-1251), substituting ${SPANBERGER_UUID} and expected row count (the count from research JSON).

    Hard rules (verifiable):
      - File must start with `-- Migration 326:` on line 1
      - File must contain exactly one `BEGIN;` and exactly one `COMMIT;`
      - Every `INSERT INTO inform.politician_answers` must be paired with a following `INSERT INTO inform.politician_context` for the same topic_id
      - Every `politician_context` INSERT must contain `ARRAY[` followed by at least one quoted URL
      - All `value` literals are float form: `1.0`, `2.0`, `3.0`, `4.0`, or `5.0` — never bare integer, never quoted string
      - All `reasoning` blocks use `$$...$$` dollar quoting, never single-quote escaping
      - Do NOT apply the migration in this task — Task 3 applies it
  </action>
  <verify>
    <automated>
      Run all four checks (all must return 1 / true):
        Test-Path "C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql"
        (Select-String -Path "C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql" -Pattern '^BEGIN;$' -SimpleMatch).Count -eq 1
        (Select-String -Path "C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql" -Pattern '^COMMIT;$' -SimpleMatch).Count -eq 1
        # answers count == context count
        $a = (Select-String -Path "C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql" -Pattern 'INSERT INTO inform.politician_answers').Count
        $c = (Select-String -Path "C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql" -Pattern 'INSERT INTO inform.politician_context').Count
        ($a -eq $c) -and ($a -ge 15)
    </automated>
  </verify>
  <done>
    - Migration file exists at C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql
    - File begins with `BEGIN;` (post-comment-block) and ends with `COMMIT;` (pre-verification-comment-block)
    - Equal counts of `INSERT INTO inform.politician_answers` and `INSERT INTO inform.politician_context` (both >=15)
    - Topic UUID reference block present in header comments
    - Verification comment block at end with Spanberger UUID embedded
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 326 to production Supabase + verify counts</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql (the file written in Task 2)
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-06: apply immediately; D-07: format)
  </read_first>
  <action>
    Step A: Read the full content of `C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql` and pass it as the `query` argument to mcp__supabase-local__execute_sql. Per CLAUDE.md, mcp__supabase-local IS production. Per phase_specific_guidance: use execute_sql, NOT apply_migration (apply_migration uploads to migration history; execute_sql runs SQL directly).

    Step B (verification via mcp__supabase-local__execute_sql):
      Run this query — must return count >= 15:
        SELECT COUNT(*) AS n FROM inform.politician_answers
        WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510001);

      Run this query — must return 0 (every answer must have a context row):
        SELECT COUNT(*) AS unpaired
        FROM inform.politician_answers pa
        LEFT JOIN inform.politician_context pc
          ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
        WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510001)
          AND pc.politician_id IS NULL;

      Run this query — must return 0 (100% citation rate per D-10):
        SELECT COUNT(*) AS uncited
        FROM inform.politician_context
        WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510001)
          AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0);

    Step C: Delete the temp research file `.planning/phases/106-va-compass-stances/_tmp-spanberger-research.json` (no longer needed; migration is the source of truth).
  </action>
  <verify>
    <automated>
      Run via mcp__supabase-local__execute_sql:
        SELECT
          (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510001)) AS answer_count,
          (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510001) AND pc.politician_id IS NULL) AS unpaired,
          (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510001) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)) AS uncited;
      Expected: answer_count >= 15, unpaired = 0, uncited = 0.
    </automated>
  </verify>
  <done>
    - Migration 326 applied successfully (no SQL error)
    - inform.politician_answers row count for Spanberger >= 15
    - All Spanberger answer rows have paired context rows (unpaired = 0)
    - All Spanberger context rows have >=1 source URL (uncited = 0)
    - Temp research JSON deleted
  </done>
</task>

</tasks>

<verification>
- mcp__supabase-local__execute_sql query: stance count for Spanberger >= 15
- All paired (answer ↔ context) rows present, unpaired = 0
- 100% citation rate (uncited = 0)
- Migration file 326_spanberger_stances.sql committed to EV-Accounts repo (EV-Accounts auto-deploys on push to master; no Essentials commit needed here)
</verification>

<success_criteria>
- VA-STANCES-01 partially satisfied (Spanberger portion)
- ROADMAP Phase 106 success criterion #1 partially met (>=15 for Spanberger)
- D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-10 all honored
</success_criteria>

<output>
Create `.planning/phases/106-va-compass-stances/106-01-SUMMARY.md` documenting:
- Stance count delivered for Spanberger
- Topics with no evidence found (omitted) — list slugs
- Sources used (domain summary)
- Any deviations from D-01..D-10 (should be none)
</output>
