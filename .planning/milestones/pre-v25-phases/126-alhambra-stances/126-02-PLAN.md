---
phase: 126-alhambra-stances
plan: 02
type: execute
wave: 2
depends_on: [126-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/NNN+3_wang_stances.sql
  - C:/EV-Accounts/backend/migrations/NNN+4_andrade_stadler_stances.sql
autonomous: true
requirements: [ALHAMBRA-01]
must_haves:
  truths:
    - "Noya Wang (D4, external_id -700453) has evidence-only stance rows written, applied, and verified — searched as 'Noya Wang Alhambra Mayor' but migration uses her Council Member District 4 context; no Mayor office row created"
    - "Adele Andrade-Stadler (D5, external_id -700454) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
    - "No new essentials.offices or essentials.districts rows created for Alhambra"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/NNN+3_wang_stances.sql"
      provides: "Noya Wang stance migration (Council Member D4 context)"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/NNN+4_andrade_stadler_stances.sql"
      provides: "Adele Andrade-Stadler stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Research and apply evidence-only compass stances for the final 2 Alhambra officials (Noya Wang D4, Adele Andrade-Stadler D5), one politician at a time. Blocked on Plan 01 (requires confirmed migration numbers and all 5 UUIDs from 126-01-SUMMARY.md).

Purpose: Complete all 5 Alhambra officials' stance research, closing the data work for ALHAMBRA-01. Unblocks Plan 03 phase-wide closure.
Output: 2 per-individual stance migration files (NNN+3 and NNN+4), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-alhambra-stances/126-RESEARCH.md
@.planning/phases/126-alhambra-stances/126-01-SUMMARY.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 574_boston_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs (Phase 115 ledger-consistency precedent)
- CRITICAL: Do NOT write "Mayor Wang" in reasoning text without the "rotational" qualifier; use "Council Member Wang" or "Alhambra Council Member Wang (rotational Mayor 2025-26)"
</interfaces>

Alhambra roster — final 2 officials (UUIDs from 126-01-SUMMARY.md):
  -700453 Noya Wang (Council Member, District 4)       → NNN+3
  -700454 Adele Andrade-Stadler (Council Member, D5)   → NNN+4

NNN = confirmed in Plan 01 Wave 0 pre-flight; read from 126-01-SUMMARY.md.

CRITICAL ROTATIONAL MAYOR PITFALL (Pitfall 6 from 126-RESEARCH.md):
Noya Wang holds the rotational Mayor title for 2025-2026 — she is publicly called "Mayor Wang" in local press. Her DB seeded office is "Council Member (District 4)". Alhambra has NO separately elected Mayor. When researching Wang:
- SEARCH using: "Noya Wang Alhambra Mayor" and "Mayor Wang Alhambra" (to find press coverage)
- WRITE migration using: her District 4 council context only
- DO NOT add any new Mayor office row, LOCAL_EXEC district, or Mayor chamber
- Use "Council Member Wang" or "Alhambra Council Member Wang (rotational Mayor 2025-26)" in reasoning text
- WARNING SIGNS: migration contains INSERT INTO essentials.offices or essentials.districts

Out-of-scope guard: Do NOT write stances for any external_id outside -700454 to -700450. Do NOT create any new offices, districts, or chambers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Noya Wang (D4) stances — migration NNN+3</name>
  <files>C:/EV-Accounts/backend/migrations/NNN+3_wang_stances.sql</files>
  <read_first>
    - .planning/phases/126-alhambra-stances/126-01-SUMMARY.md (NNN confirmed value + Wang UUID from pre-flight)
    - .planning/phases/126-alhambra-stances/126-RESEARCH.md (Noya Wang section + Pitfall 6 rotational Mayor + Pitfall 8 reasoning text + Local Press Sources)
    - C:/EV-Accounts/backend/migrations/NNN_lee_stances.sql (in-phase sibling file as format reference, NNN = confirmed value)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process Noya Wang (external_id -700453, UUID from 126-01-SUMMARY.md, Council Member District 4) strictly after Plan 01 completes. Research, write, apply, and verify before proceeding to Task 2.

    CRITICAL: Wang currently holds the rotational Mayor title (2025-2026 rotation). Search using the Mayor title to find press coverage, but write the migration using her Council Member D4 context only. Do NOT create any Mayor-related DB rows.

    (a) Research: Search "Noya Wang Alhambra Mayor" and "Mayor Wang Alhambra" on sgvtribune.com, alhambraource.com, alhambraca.gov/agendas-minutes, patch.com/california/alhambra, latimes.com. As rotational Mayor she may have more public statements (press releases, council meeting presiding statements). Expected yield 4-9 stances — higher than the other members due to Mayor-period press visibility. High-probability topics: housing, local-immigration (Alhambra demographics), homelessness-response, public-safety-approach, growth-and-development. Also check for any statement or vote on local-environment (air quality near I-10 freeway). Search specifically for her District 4 council votes on Ballotpedia and alhambraca.gov meeting archives from her full council tenure (not just as Mayor).

    (b) Write migration NNN+3_wang_stances.sql:
    - Header: "Migration NNN+3: Noya Wang Stances"
    - Full 44-topic UUID reference block (from 126-RESEARCH.md, or live list from 126-01-SUMMARY.md if count differed)
    - BEGIN;
    - One politician_answers INSERT + one politician_context INSERT per evidenced topic
    - value = float literal (e.g. 2.0) — NEVER bare integer
    - sources = ARRAY[...]::text[]::text[] — double-cast, path-bearing URLs
    - reasoning = $$...$$ citing specific vote/statement/action
    - Reasoning text: use "Council Member Wang" or "Alhambra Council Member Wang (rotational Mayor 2025-26)" — NEVER bare "Mayor Wang" without context qualifier
    - COMMIT;
    - No INSERT INTO essentials.offices, essentials.districts, or essentials.chambers

    (c) Apply: mcp__supabase-local__execute_sql with full file content.

    (d) Verify immediately (before Task 2):
    Row count: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{wang_uuid}';`
    Unpaired check: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{wang_uuid}' AND pc.politician_id IS NULL;` — must return 0
    Citation check: `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{wang_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Wang's UUID. Citation check returns 0 for Wang's UUID. Migration file contains no INSERT INTO essentials.offices or essentials.districts.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file NNN+3_wang_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Wang's UUID
    - Uncited-context query returns 0 for Wang's UUID
    - File contains no INSERT INTO essentials.offices, essentials.districts, or essentials.chambers
    - Reasoning text does not contain bare "Mayor Wang" — uses "Council Member Wang" or includes "rotational Mayor" qualifier
    - No INSERT references external_ids outside -700454 to -700450 range
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration NNN+3_wang_stances.sql written, applied to production, and verified (0 unpaired, 0 uncited) for Noya Wang. No Mayor office row created.</done>
</task>

<task type="auto">
  <name>Task 2: Adele Andrade-Stadler (D5) stances — migration NNN+4</name>
  <files>C:/EV-Accounts/backend/migrations/NNN+4_andrade_stadler_stances.sql</files>
  <read_first>
    - .planning/phases/126-alhambra-stances/126-01-SUMMARY.md (NNN confirmed value + Andrade-Stadler UUID)
    - .planning/phases/126-alhambra-stances/126-RESEARCH.md (Adele Andrade-Stadler section + Local Press Sources)
    - C:/EV-Accounts/backend/migrations/NNN+3_wang_stances.sql (just-written sibling file as in-phase format reference)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process Adele Andrade-Stadler (external_id -700454, UUID from 126-01-SUMMARY.md, Council Member District 5) strictly after Task 1 completes. Research, write, apply, and verify.

    (a) Research: Search "Adele Andrade-Stadler Alhambra council" and "Andrade-Stadler Alhambra" on sgvtribune.com, alhambraource.com, alhambraca.gov/agendas-minutes, patch.com/california/alhambra, latimes.com, Ballotpedia. Andrade-Stadler has served multiple terms — compound Spanish-English surname pattern may indicate community advocacy background. Expected yield 2-5 stances. High-probability topics: housing, residential-zoning, growth-and-development, local-immigration, homelessness-response. If zero evidence found, write the ledger file with header + BEGIN;/COMMIT; + zero INSERTs.

    (b) Write migration NNN+4_andrade_stadler_stances.sql:
    - Header: "Migration NNN+4: Adele Andrade-Stadler Stances"
    - Full 44-topic UUID reference block
    - BEGIN;
    - One politician_answers INSERT + one politician_context INSERT per evidenced topic
    - value = float literal; sources = ARRAY[...]::text[]::text[] double-cast; reasoning = $$...$$ dollar-quoting
    - Reasoning text: use "Council Member Andrade-Stadler"
    - COMMIT;

    (c) Apply: mcp__supabase-local__execute_sql.

    (d) Verify:
    Row count: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{andrade_uuid}';`
    Unpaired check: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{andrade_uuid}' AND pc.politician_id IS NULL;` — must return 0
    Citation check: `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{andrade_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Andrade-Stadler's UUID. Citation check returns 0 for Andrade-Stadler's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file NNN+4_andrade_stadler_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Andrade-Stadler's UUID
    - Uncited-context query returns 0 for Andrade-Stadler's UUID
    - No INSERT references external_ids outside -700454 to -700450 range
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration NNN+4_andrade_stadler_stances.sql written, applied to production, and verified (0 unpaired, 0 uncited) for Adele Andrade-Stadler. All 5 Alhambra officials complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (mcp__supabase-local) | Writes go directly to live production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Tampering | stance value integrity | mitigate | Evidence-only rule: no INSERT without a direct cited source; per-person uncited-context query must return 0 |
| T-126-02 | Information Disclosure | reasoning text / sources | accept | All data is public-record political positions with public source URLs; no PII |
| T-126-03 | Repudiation | stance provenance | mitigate | Every value paired with a context row carrying path-bearing source URLs (100% citation rate) |
| T-126-04 | Tampering | rotational Mayor office creation for Wang | mitigate | Acceptance criteria assert no INSERT INTO essentials.offices or essentials.districts in Wang migration; Wave 0 (Plan 01) already confirmed no Mayor chamber exists |
| T-126-05 | Tampering | "Mayor Wang" in reasoning without qualifier | mitigate | Acceptance criteria require reasoning uses "Council Member Wang" or "rotational Mayor" qualifier |
| T-126-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
After both tasks, run the partial phase-wide citation check for this plan's officials:
`SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700453) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` must return 0.
</verification>

<success_criteria>
- Migrations NNN+3 and NNN+4 (Wang and Andrade-Stadler) written, applied, and verified
- 0 unpaired answers, 0 uncited contexts for both officials
- No Mayor office row created for Alhambra — Wang's migration uses Council Member D4 context exclusively
- Wang reasoning text uses "Council Member Wang" or includes "rotational Mayor" qualifier
- No external_ids outside -700454 to -700450 range touched
- Blank-spoke officials documented in SUMMARY (never defaulted to 3.0)
- All 5 Alhambra officials now complete; Plan 03 unblocked
</success_criteria>

<output>
Create `.planning/phases/126-alhambra-stances/126-02-SUMMARY.md` when done. Record: per-official stance counts for Wang and Andrade-Stadler, any blank-spoke officials, Wang Mayor-context handling note, and confirmation that no Mayor office was created.
</output>
