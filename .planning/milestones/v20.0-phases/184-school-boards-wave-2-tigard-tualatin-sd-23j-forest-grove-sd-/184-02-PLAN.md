---
phase: 184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd
plan: 02
type: execute
wave: 2
depends_on: [184-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1206_or_westmetro_school_boards_wave2.sql
autonomous: false
requirements: [WSCH-03, WSCH-04, WSCH-05]
user_setup: []

must_haves:
  truths:
    - "Three governments exist: 'Tigard-Tualatin School District 23J, Oregon, US' (type='LOCAL', state='OR' uppercase, geo_id='4112240'), 'Forest Grove School District 15, Oregon, US' (type='LOCAL', state='OR', geo_id='4105160'), and 'Sherwood School District 88J, Oregon, US' (type='LOCAL', state='OR', geo_id='4111290') — each inserted via a WHERE NOT EXISTS name guard (governments has NO unique constraint)"
    - "Three chambers exist with VERBATIM per-district names (Key Finding 5): 'School Board' (official_count=5) under the Tigard-Tualatin government, 'School Board' (official_count=5) under the Forest Grove government, and 'Board of Directors' (official_count=5) under the Sherwood government — the generated slug/path column NEVER in the INSERT list; official_count is 5 NOT 7 (all three boards are 5-seat — the single most important delta from Wave 1)"
    - "Exactly ONE SCHOOL district per government carries all 5 seats: district_type='SCHOOL', state='or' LOWERCASE, mtfcc='G5420', geo_id matching the EXISTING Phase-174 geofence (4112240 / 4105160 / 4111290) — single-shared-district pattern, NOT one district per seat, and NO new geofence loaded"
    - "15 offices total (5 TTSD + 5 FGSD + 5 SSD), each authored with the WR-01 FIX: the politician id resolves via a `pol AS (SELECT id FROM ins_p UNION SELECT id FROM essentials.politicians WHERE external_id = -N)` CTE and the office INSERT CROSS JOINs `pol` (NOT a raw `ins_p` CROSS JOIN), so the office NOT EXISTS guard is genuinely live/self-healing on a re-run"
    - "TTSD 5 offices (ext_id -4112241..-4112245) titled 'Director, Position 1'..'Director, Position 5' with Vice Chair on Position 3 (Irvin → 'Director, Position 3 (Vice Chair)') and Chair on Position 4 (Zurschmeide → 'Director, Position 4 (Chair)') — Chair/VC pairing RE-VERIFIED same-day against the live page per A3 before authoring; all is_appointed=false, is_appointed_position=false, party=NULL, representing_state='OR' uppercase"
    - "FGSD 5 offices (ext_id -4105161..-4105165) titled 'Director, Position 1'..'Director, Position 5' with Vice Chair on Position 3 (Lozano → 'Director, Position 3 (Vice Chair)') and Chair on Position 5 (Kottkey → 'Director, Position 5 (Chair)'); Linda Harrington (Position 4) is seeded as a seated director (D-R4 — confirmed June 23 2026 mid-term appointee); all is_appointed=false, party=NULL, representing_state='OR'"
    - "SSD 5 offices (ext_id -4111291..-4111295) using the district's HIGH-confidence VERBATIM title strings: Carson (Position 1) → 'Board Chair/Director, Position 1', Kaufman (Position 2) → 'Director, Position 2', Hawkins (Position 3) → 'Board Vice Chair/Director, Position 3', Moller (Position 4) → 'Director, Position 4', Thornton (Position 5) → 'Director, Position 5'; all is_appointed=false, party=NULL, representing_state='OR'"
    - "All 15 politicians are seeded is_active=true, is_incumbent=true, is_vacant=false, is_appointed=false, party=NULL (antipartisan); the FGSD non-voting Student Representative is NOT seeded (D-R4); no Chair/Vice-Chair is a separate office row (15 offices total, never 6 per district)"
    - "The in-migration post-verify DO block asserts (rolling back on any mismatch): exactly 1 gov each, exactly 5 offices on each SCHOOL district (TTSD 4112240 / FGSD 4105160 / SSD 4111290, state='or'), the WR-02 FIX gate (0 offices with a NULL chamber_id across all 3 geo_ids), 0 section-split orphans for all 3 G5420 geo_ids, and 0 directors still holding a NULL office_id after back-fill — RAISES on any failure"
    - "office_id back-fill is applied for all 15 ext_ids ONLY IF the freshest structural migration (183's 1203) still writes it — the executor confirms 1203's back-fill UPDATE shape before copying that step; if the freshest structural migration does not back-fill office_id, this migration does not either"
    - "Migration 1206 registers in supabase_migrations.schema_migrations (version '1206', name 'or_westmetro_school_boards_wave2') OUTSIDE the transaction, after COMMIT — it is a STRUCTURAL migration; the filename number is re-confirmed against on-disk `ls` MAX immediately before authoring (1204 was claimed by a concurrent workstream mid-183; collisions are real)"
    - "The file is saved UTF-8; verify-gate hygiene: the literal strings slug / schema_migrations (except the intended ledger INSERT) / photo_origin_url do not appear in comments"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1206_or_westmetro_school_boards_wave2.sql"
      provides: "Structural migration: 3 governments + 3 verbatim-named chambers (official_count=5) + 3 single-shared SCHOOL districts (reusing existing Phase-174 geofences) + 15 politician/office CTEs (WR-01 pol-union shape) + post-verify DO block (3 govs / 5+5+5 offices / WR-02 chamber_id-NOT-NULL / 0 section-split / 0 null office_id) + ledger registration"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 250
  key_links:
    - from: "essentials.chambers ('School Board' / 'School Board' / 'Board of Directors')"
      to: "essentials.governments (TTSD / FGSD / SSD School District rows)"
      via: "government_id subquery WHERE name = the verbatim government name"
      pattern: "(Tigard-Tualatin|Forest Grove|Sherwood) School District"
    - from: "essentials.offices (15 director seats)"
      to: "essentials.districts (3 shared SCHOOL districts, state='or', geo_id 4112240/4105160/4111290)"
      via: "CROSS JOIN pol on the SCHOOL district WHERE d.state='or'"
      pattern: "district_type = 'SCHOOL'"
    - from: "essentials.districts (SCHOOL, geo_id 4112240/4105160/4111290)"
      to: "essentials.geofence_boundaries (existing Phase-174 G5420 rows)"
      via: "geo_id match (no loader — geofence already present)"
      pattern: "'4112240'|'4105160'|'4111290'"
---

<objective>
Author the single structural migration that seeds all three school-district governments end-to-end: 3
governments, 3 verbatim-named chambers (official_count=5 each), 3 single-shared SCHOOL districts on the
EXISTING Phase-174 G5420 geofences, and 15 politician+office rows (5 TTSD + 5 FGSD + 5 SSD "Director,
Position N", Chair/Vice-Chair as title-on-seat suffixes), with all three 183-REVIEW fixes (WR-01/WR-02/WR-03
— WR-03 lives in plan 03) baked in from authoring, not patched after. The orchestrator then applies it and
confirms the post-verify gates and address routing.

Purpose: Satisfy the structural half of WSCH-03 + WSCH-04 + WSCH-05 in one file (D-P1 single-file
precedent, 1203 shape). This is the proven Wave-1 1203 pattern with three deliberate deltas: (1) THREE
governments at FIVE seats each (15 offices, not 14 — Key Finding 1); (2) the WR-01 `pol` union CTE +
WR-02 chamber_id-NOT-NULL gate applied from the first draft (D-F1, not cloned-then-patched); (3) no
geofence loader — all three G5420 rows already exist (Phase 174) and must NOT be re-loaded. All three
boards are whole-district at-large (Wave-0 confirmed), so each government gets exactly ONE shared SCHOOL
district carrying all 5 seats — no sub-zones, no per-seat districts.
Output: `1206_or_westmetro_school_boards_wave2.sql` (registered structural migration) + a recorded
post-verify pass and address-routing confirmation for downstream plans 03-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-RESEARCH.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-01-SUMMARY.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` migration file only. It has NO database
access and NO Supabase MCP. The INLINE ORCHESTRATOR applies it via `psql "$DATABASE_URL" -f <file>`
(DATABASE_URL from C:/EV-Accounts/backend/.env), reads the post-verify NOTICE, runs the SQL gates, and
re-runs the routing smoke test. C:/EV-Accounts is a SEPARATE git repo — never cd into it; use
`git -C "C:/EV-Accounts"` for any commit.

**CASING (the #1 silent-failure pitfall):** districts.state = LOWERCASE 'or' everywhere in JOIN/WHERE;
governments.state and offices.representing_state = UPPERCASE 'OR'. Wave-0 Probe F confirmed 'or'
lowercase is correct for the existing PPS SCHOOL district — use the 184-01-SUMMARY confirmed value.

**5-SEAT, NOT 7 (Key Finding 1):** every count literal — official_count, the post-verify expected-office
count, the ext_id block width — reads 5, never 7. Copying Wave 1's `official_count=7` / `expected 7
offices` gate literal is the #1 anti-pattern this phase.

**WR-01/WR-02 BAKED IN (D-F1):** do NOT clone 1203's office CTE or post-verify gate verbatim — 1203's
office NOT EXISTS guard is dead code (WR-01) and its post-verify has a chamber_id-NULL blind spot (WR-02).
Author the FIXED shapes directly, per 184-PATTERNS.md §"1206_..." and 184-RESEARCH.md §"Pattern 2".

**VERIFY-GATE HYGIENE:** keep the literal strings `slug`, `schema_migrations` (except the intended
ledger INSERT), and `photo_origin_url` out of comments. Save UTF-8.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author structural migration 1206 for all three school-district boards</name>
  <files>C:/EV-Accounts/backend/migrations/1206_or_westmetro_school_boards_wave2.sql</files>
  <read_first>
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-01-SUMMARY.md (Wave-0 confirmed: 3 geo_ids, three 5-wide ext_id blocks, migration number 1206, lowercase 'or' casing, all three 5-director rosters with Position + Chair/Vice-Chair, locked verbatim chamber + office-title strings, re-verified TTSD Chair/VC pairing)
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-RESEARCH.md §"Pattern 2: Structural Migration — WR-01/WR-02/WR-03 baked in", §"Pattern 3: Verified Rosters" (all three roster tables with exact ext_ids/titles), §"Anti-Patterns to Avoid", §"Pitfall 1" (5-not-7)
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md §"1206_or_westmetro_school_boards_wave2.sql" (header/government/chamber/district/WR-01 CTE/WR-02 post-verify/back-fill/ledger patterns with exact FIXED excerpts)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-REVIEW.md (WR-01/WR-02 fix SQL, verbatim — the authoritative source for the FIXED shapes)
    - C:/EV-Accounts/backend/migrations/1203_or_westmetro_school_boards_wave1.sql (PRIMARY analog — read in full for the government/chamber/district INSERT shape, header CRITICAL-block style, post-verify structure, and the office_id back-fill + ledger-register convention; do NOT copy its office CTE or post-verify gate verbatim — apply the WR-01/WR-02 fixes)
  </read_first>
  <action>
    Write one structural migration seeding all THREE districts. Wrap the DDL in BEGIN; ... COMMIT; then
    register the ledger row AFTER commit. Header CRITICAL-block adapted from 1203 with these facts:
    governments has NO unique constraint (WHERE NOT EXISTS name guard); chambers generated slug/path
    column NEVER in the INSERT list; districts.state must be 'or' LOWERCASE (uppercase = 0 rows);
    governments.state / offices.representing_state = 'OR' UPPERCASE; district_type='SCHOOL' (not
    'SCHOOL_DISTRICT'); party=NULL on all 15 (antipartisan); all 15 ELECTED (is_appointed=false,
    is_appointed_position=false); Chair/Vice-Chair are titles on existing seats NOT separate rows (15
    offices, never 6 per district); all three G5420 geofences (4112240/4105160/4111290) ALREADY EXIST
    (Phase 174) — do NOT run any loader; ALL THREE BOARDS ARE 5-SEAT (official_count=5, 15 offices — NOT
    7/21 like Wave 1); WR-01/WR-02 baked in from authoring per 183-REVIEW.
    GOVERNMENTS: insert 'Tigard-Tualatin School District 23J, Oregon, US' (geo_id='4112240'), 'Forest
    Grove School District 15, Oregon, US' (geo_id='4105160'), 'Sherwood School District 88J, Oregon, US'
    (geo_id='4111290') — all type='LOCAL', state='OR', each guarded by WHERE NOT EXISTS on the exact name.
    CHAMBERS: 'School Board' (name_formal 'Tigard-Tualatin School District 23J School Board',
    official_count=5) under TTSD; 'School Board' (name_formal 'Forest Grove School District 15 School
    Board', official_count=5) under FGSD; 'Board of Directors' (name_formal 'Sherwood School District 88J
    Board of Directors', official_count=5) under SSD — government_id via name subquery, NOT EXISTS guard,
    NO slug/path column in the column list.
    DISTRICTS: one SCHOOL district per government — (district_type='SCHOOL', state='or', geo_id='4112240',
    label='Tigard-Tualatin School District 23J', mtfcc='G5420'), the 4105160/'Forest Grove School District
    15' twin, and the 4111290/'Sherwood School District 88J' twin — NOT EXISTS guard on (geo_id,
    district_type, state).
    OFFICES + POLITICIANS (15× the WR-01-FIXED CTE from 184-PATTERNS §"Fixed shape for 1206"): each block
    is `WITH chamber AS (...), ins_p AS (INSERT...RETURNING id), pol AS (SELECT id FROM ins_p UNION SELECT
    id FROM essentials.politicians WHERE external_id = -N) INSERT INTO essentials.offices SELECT ..., p.id,
    ... FROM essentials.districts d CROSS JOIN pol p WHERE d.geo_id=<geo_id> AND d.district_type='SCHOOL'
    AND d.state='or' AND p.id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE
    o.district_id=d.id AND o.politician_id=p.id)`.
      TTSD ext_id -4112241..-4112245 for Positions 1-5 (David Jaimes / Kristen Miles / Tristan Irvin /
      Jill Zurschmeide / Crystal Weston), titles 'Director, Position 1'..'Director, Position 5' with
      'Director, Position 3 (Vice Chair)' for Irvin and 'Director, Position 4 (Chair)' for Zurschmeide.
      FGSD ext_id -4105161..-4105165 for Positions 1-5 (Brisa Franco / Pete Truax / Alma Lozano / Linda
      Harrington / Kristy Kottkey), titles 'Director, Position N' with 'Director, Position 3 (Vice Chair)'
      for Lozano and 'Director, Position 5 (Chair)' for Kottkey.
      SSD ext_id -4111291..-4111295 for Positions 1-5 (Harmony Carson / Matt Kaufman / Abby Hawkins / Hans
      Moller / Matt Thornton) using the district's VERBATIM literal title strings: 'Board Chair/Director,
      Position 1' (Carson), 'Director, Position 2' (Kaufman), 'Board Vice Chair/Director, Position 3'
      (Hawkins), 'Director, Position 4' (Moller), 'Director, Position 5' (Thornton).
    Every politician: party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true,
    ON CONFLICT (external_id) DO NOTHING. Every office: representing_state='OR' uppercase,
    is_appointed_position=false, is_vacant=false, role_canonical=NULL, chamber_id via that government's
    chamber subquery.
    OFFICE_ID BACK-FILL: if 1203 (freshest structural migration) still writes an office_id back-fill
    UPDATE, include the equivalent for all 15 ext_ids (-4112241..-4112245, -4105161..-4105165,
    -4111291..-4111295) WHERE office_id IS NULL; otherwise omit it.
    POST-VERIFY DO block (WR-02-FIXED, per 184-PATTERNS §"Post-verify DO block — WR-02 FIX APPLIED"):
    assert v_ttsd_gov=1, v_fgsd_gov=1, v_ssd_gov=1; v_ttsd_off=5, v_fgsd_off=5, v_ssd_off=5 (offices JOIN
    districts WHERE geo_id=<each> AND district_type='SCHOOL' AND state='or'); v_null_chamber=0 (WR-02:
    offices across all 3 geo_ids with a NULL chamber_id); v_split=0 (G5420 geofence rows for the 3 geo_ids
    that lack a matching SCHOOL district); v_null_oid=0 (the 15 directors still holding NULL office_id) —
    RAISE EXCEPTION on any mismatch, RAISE NOTICE on success. Then COMMIT; then INSERT INTO
    supabase_migrations.schema_migrations (version, name) VALUES ('1206','or_westmetro_school_boards_wave2')
    ON CONFLICT (version) DO NOTHING. No fenced code in this action — 1203 + 184-PATTERNS §"1206_..." +
    183-REVIEW WR-01/WR-02 are the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator (Task 2) applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1206_or_westmetro_school_boards_wave2.sql` and confirms the post-verify NOTICE + SQL gates</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/1206_or_westmetro_school_boards_wave2.sql, wrapped in BEGIN;...COMMIT; with the ledger INSERT after COMMIT
    - Contains exactly 3 `INSERT INTO essentials.governments` guarded by WHERE NOT EXISTS on the verbatim names; all type='LOCAL', state='OR'
    - Contains 3 `INSERT INTO essentials.chambers` with names 'School Board' (×2) and 'Board of Directors' (grep finds no 'Board of Education'), official_count=5 each (grep finds no official_count=7), no slug/path column in the INSERT list
    - Contains 3 SCHOOL-district INSERTs with state='or' lowercase, mtfcc='G5420', geo_id '4112240'/'4105160'/'4111290'
    - Contains 15 `WITH chamber AS ... ins_p AS ... pol AS (SELECT id FROM ins_p UNION SELECT id FROM essentials.politicians WHERE external_id = ...)` blocks (WR-01 shape); grep confirms `CROSS JOIN pol` (never `CROSS JOIN ins_p`); ext_ids -4112241..-4112245, -4105161..-4105165, -4111291..-4111295 all present; all politician INSERTs have party NULL and ON CONFLICT (external_id) DO NOTHING
    - Office titles present: 'Director, Position 3 (Vice Chair)' + 'Director, Position 4 (Chair)' (TTSD), 'Director, Position 3 (Vice Chair)' + 'Director, Position 5 (Chair)' (FGSD), 'Board Chair/Director, Position 1' + 'Board Vice Chair/Director, Position 3' (SSD), plus the remaining plain 'Director, Position N' titles; grep confirms exactly 15 office INSERTs (no separate Chair/Vice-Chair rows)
    - Post-verify DO block asserts all three govs=1, all three off=5 (grep finds no `<> 7`), v_null_chamber=0 (WR-02 gate present), v_split=0, v_null_oid=0, with RAISE EXCEPTION on mismatch
    - Ledger INSERT uses version '1206', name 'or_westmetro_school_boards_wave2'
    - File saved UTF-8; literal strings slug / photo_origin_url absent from comments; filename number re-confirmed against on-disk `ls` MAX at authoring time
  </acceptance_criteria>
  <done>Migration 1206 authored with 3 govs, 3 verbatim chambers (official_count=5), 3 SCHOOL districts, 15 WR-01-fixed director CTEs, WR-02-fixed post-verify DO block, and ledger registration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies 1206, confirms post-verify + SQL gates + address routing</name>
  <what-built>Structural migration 1206. The orchestrator applies it, reads the post-verify NOTICE, runs the office-count/casing/chamber-name/section-split SQL gates, and re-runs the Phase-174 routing smoke test.</what-built>
  <how-to-verify>
    A. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1206_or_westmetro_school_boards_wave2.sql`.
       Confirm the run ends with the post-verify RAISE NOTICE (TTSD gov=1/off=5, FGSD gov=1/off=5, SSD
       gov=1/off=5, null_chamber=0, split_orphans=0, office_id_nulls=0) and NO RAISE EXCEPTION (any
       exception rolls back the whole migration — fix the migration and re-apply). Confirm the ledger row
       registered: `SELECT version, name FROM supabase_migrations.schema_migrations WHERE version='1206'`
       returns one row.
    B. SQL gates (independent of the in-migration DO block):
       - TTSD offices = 5: `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON
         d.id=o.district_id WHERE d.geo_id='4112240' AND d.district_type='SCHOOL' AND d.state='or'` → 5.
       - FGSD offices = 5: same with geo_id='4105160' → 5.
       - SSD offices = 5: same with geo_id='4111290' → 5.
       - Casing: `SELECT DISTINCT state FROM essentials.districts WHERE geo_id IN ('4112240','4105160','4111290')
         AND district_type='SCHOOL'` → only 'or'.
       - Section-split = 0 for all three G5420 geo_ids (the DO-block gate re-run as a standalone SELECT).
       - Chamber names: `SELECT g.name AS gov, c.name AS chamber FROM essentials.chambers c JOIN
         essentials.governments g ON g.id=c.government_id WHERE g.name LIKE '%School District%Oregon%US'
         AND g.name IN ('Tigard-Tualatin School District 23J, Oregon, US','Forest Grove School District 15,
         Oregon, US','Sherwood School District 88J, Oregon, US')` → 'School Board' (TTSD), 'School Board'
         (FGSD), 'Board of Directors' (SSD) — NOT 'Board of Education'.
    C. Address routing (reuse Phase 174's existing smoke test, no change needed):
       `cd C:/EV-Accounts/backend && npx tsx scripts/smoke-or-westmetro-school.ts` — confirm it still
       passes for all three geo_ids (this wave's 3 are among the 5 west-metro districts the script covers).
    D. Record the 15 minted politician UUIDs by external_id for plan 03's headshot migration:
       `SELECT external_id, id FROM essentials.politicians WHERE external_id BETWEEN -4112245 AND -4112241
       OR external_id BETWEEN -4105165 AND -4105161 OR external_id BETWEEN -4111295 AND -4111291 ORDER BY
       external_id`.
  </how-to-verify>
  <acceptance_criteria>
    - 1206 applied cleanly; post-verify NOTICE shows all three gov=1/off=5, null_chamber=0, split=0, office_id_nulls=0; no exception
    - Ledger row version='1206' present
    - All three office-count gates = 5; casing gate returns only 'or'; section-split = 0 for all three geo_ids
    - Chamber-name gate returns 'School Board' / 'School Board' / 'Board of Directors' (no 'Board of Education')
    - smoke-or-westmetro-school.ts passes for all three geo_ids (no routing regression)
    - The external_id → politician UUID map for all 15 directors is recorded in the SUMMARY for plan 03
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded post-verify + gate results + the 15 UUIDs, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Structural migration (INSERT-only DDL) applied by the orchestrator against production Supabase inside a transaction with a rollback-on-mismatch gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-06 | Tampering (data integrity) | migration re-run double-inserts govs/chambers/districts/offices | mitigate | Every INSERT uses WHERE NOT EXISTS / ON CONFLICT (external_id) DO NOTHING; WR-01 `pol` union CTE makes the office guard genuinely live on re-run (not dead code) |
| T-184-07 | Tampering | uppercase d.state='OR' in office/district WHERE clauses silently links 0 offices | mitigate | Header CRITICAL note + Wave-0 Probe F confirmed 'or' lowercase; post-verify DO block RAISES if any board's offices <> 5 (catches the silent-zero) |
| T-184-08 | Tampering | a chamber-name string drift between the INSERT and the per-block subquery lands offices with a NULL chamber_id | mitigate | WR-02 fix: post-verify DO block asserts v_null_chamber=0 across all 3 geo_ids and RAISES on any |
| T-184-09 | Tampering | seeding a 7-seat count (Wave-1 muscle memory) would over/under-seat every board | mitigate | Key Finding 1 (all 5-seat) carried in header + must_haves; post-verify asserts exactly 5 per board; acceptance criteria grep for any `official_count=7`/`<> 7` |
| T-184-10 | Repudiation | applying an unregistered structural migration loses ledger traceability | mitigate | Explicit INSERT INTO supabase_migrations.schema_migrations version '1206' after COMMIT |
| T-184-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — the smoke test uses the pre-existing tsx/node toolchain; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Post-verify DO block passes (3 govs, 5+5+5 offices, 0 null chamber_id, 0 section-split, 0 null office_id) — rolls back on any mismatch.
- Independent SQL gates confirm office counts (5 each), lowercase 'or' casing, verbatim chamber names.
- smoke-or-westmetro-school.ts confirms all three districts still route.
- 0 compass stance rows remain the success state (no stance work in this plan).
</verification>

<success_criteria>
- All three governments + verbatim chambers + single-shared SCHOOL districts + 15 director offices seeded
  and applied; post-verify and all independent gates green; address routing confirmed for all three
  geo_ids; the 15 external_id→UUID map recorded for plan 03.
</success_criteria>

<output>
Create `.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-02-SUMMARY.md`
when done, recording: the applied migration number (1206), the post-verify + gate results, the verbatim
chamber names used, the confirmed office counts (5/5/5), the routing-smoke result, and the full external_id
→ politician UUID map for all 15 directors (needed to author the headshot migration in plan 03).
</output>
