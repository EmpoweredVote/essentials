---
phase: 115-boston-stances
plan: 03
type: execute
wave: 3
depends_on:
  - 115-02
files_modified:
  - C:/EV-Accounts/backend/migrations/582_gabriela_coletta_zapata_stances.sql
  - C:/EV-Accounts/backend/migrations/583_edward_flynn_stances.sql
  - C:/EV-Accounts/backend/migrations/584_john_fitzgerald_stances.sql
  - C:/EV-Accounts/backend/migrations/585_brian_worrell_stances.sql
  - C:/EV-Accounts/backend/migrations/586_enrique_pepen_stances.sql
autonomous: true
requirements:
  - MA-STANCES-05
must_haves:
  truths:
    - "All 5 district councillors D1–D5 (external_ids -2507000006 through -2507000010) have rows attempted in inform.politician_answers (blank spokes acceptable per D-01)"
    - "100% citation rate for this batch: uncited = 0 for all 5 politicians"
    - "Zero unpaired rows for this batch: unpaired = 0 for all 5 politicians"
    - "No defaulted stances: only evidence-backed topics inserted"
    - "Enrique J. Pepén full name stored as UTF-8 literal in SQL — é accent preserved; filename uses 586_enrique_pepen_stances.sql (accent stripped in filename only)"
    - "Migrations 582–586 applied one at a time (never in parallel)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/582_gabriela_coletta_zapata_stances.sql"
      provides: "Stance migration for Gabriela Coletta Zapata (D1, external_id -2507000006)"
    - path: "C:/EV-Accounts/backend/migrations/583_edward_flynn_stances.sql"
      provides: "Stance migration for Edward M. Flynn (D2, external_id -2507000007)"
    - path: "C:/EV-Accounts/backend/migrations/584_john_fitzgerald_stances.sql"
      provides: "Stance migration for John FitzGerald (D3, external_id -2507000008)"
    - path: "C:/EV-Accounts/backend/migrations/585_brian_worrell_stances.sql"
      provides: "Stance migration for Brian Worrell (D4, external_id -2507000009)"
    - path: "C:/EV-Accounts/backend/migrations/586_enrique_pepen_stances.sql"
      provides: "Stance migration for Enrique J. Pepén (D5, external_id -2507000010)"
  key_links:
    - from: "migrations 582–586"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE upsert"
      pattern: "INSERT INTO inform.politician_answers / INSERT INTO inform.politician_context"
---

<objective>
Evidence-only compass stances for Boston City Council District Councillors D1–D5: Gabriela Coletta Zapata (D1, Charlestown/East Boston/North End), Edward M. Flynn (D2, Chinatown/Downtown/South Boston/South End), John FitzGerald (D3, Dorchester north), Brian Worrell (D4, Dorchester south), and Enrique J. Pepén (D5, East Boston). External_ids -2507000006 through -2507000010.

Each councillor researched sequentially, one migration written and applied per councillor before starting the next. Immigration and local-immigration are first-tier topics for Coletta Zapata (D1 East Boston) and Pepén (D5 East Boston) given their immigrant-majority constituencies. Flynn (D2) is more moderate with law-enforcement family background — expect different public-safety stance. Worrell (D4) has strong housing and racial equity record.

Accent note: Enrique J. Pepén — the é must appear as UTF-8 literal in all SQL reasoning and comments; the filename uses the accented-stripped form `586_enrique_pepen_stances.sql` per project naming convention.

Output: Migrations 582–586 applied; per-politician unpaired=0 and uncited=0 for each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/115-boston-stances/115-RESEARCH.md
@.planning/phases/115-boston-stances/115-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gabriela Coletta Zapata (D1, external_id -2507000006) — migration 582</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + exact SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors table: Coletta Zapata — D1, Charlestown/East Boston/North End, immigration+housing focus; Research Source Guidance for City Councillors)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 2.

    Step A — Research (web search, ONE session per D-08):
      Politician: Gabriela Coletta Zapata, D1 District Councillor (external_id -2507000006).
      District covers Charlestown, East Boston, and North End — immigrant-majority neighborhoods.
      Sources: https://boston.gov/departments/city-council/gabriela-coletta, https://boston.legistar.com/, https://www.wbur.org, Boston Globe, ballotpedia.org.
      Priority topics: immigration, local-immigration, housing, rent-regulation, climate-change, local-environment, city-sanitation, transportation-priorities.
      Per D-01: omit topics with no public evidence; no neutral defaults.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000006;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/582_gabriela_coletta_zapata_stances.sql
      Header: "Migration 582: Gabriela Coletta Zapata Stances" (D1 District Councillor, external_id -2507000006).
      Copy topic UUID block from 416_christopher_flanagan_stances.sql lines 14–58; paste resolved UUID in header comment.
      per-topic: politician_answers INSERT first, politician_context INSERT second; both ON CONFLICT DO UPDATE.
      Reasoning: dollar-quoted $$...$$; sources: ARRAY[...]::text[]::text[] with full URLs.

    Step C — Apply:
      Pass full content of 582_gabriela_coletta_zapata_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify:
      SELECT COUNT(*) AS n FROM inform.politician_answers
        WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -2507000006);
      SELECT COUNT(*) AS unpaired FROM inform.politician_answers pa
        LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
        WHERE pa.politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000006)
          AND pc.politician_id IS NULL;
      SELECT COUNT(*) AS uncited FROM inform.politician_context
        WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000006)
          AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/582_gabriela_coletta_zapata_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000006.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 582_gabriela_coletta_zapata_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000006
    - Stance count recorded
  </acceptance_criteria>
  <done>Migration 582 applied; unpaired=0, uncited=0 for Coletta Zapata.</done>
</task>

<task type="auto">
  <name>Task 2: Edward M. Flynn (D2, external_id -2507000007) — migration 583</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors: Flynn — D2, more moderate, public safety + veteran affairs focus)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 3.

    Step A — Research (web search, ONE session):
      Politician: Edward M. Flynn, D2 District Councillor (external_id -2507000007).
      District covers Chinatown, Downtown, South Boston, South End.
      Sources: https://boston.gov/departments/city-council/ed-flynn, https://boston.legistar.com/, WBUR, Boston Globe, Boston Herald.
      Flynn is more moderate than progressive colleagues. Focus topics: public-safety-approach, homelessness, housing, economic-development, city-sanitation, immigration.
      Do NOT assume progressive values — research actual positions. Flynn may have more conservative public-safety and immigration stances than other councillors.
      Per D-01: no defaults; evidence only.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000007;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/583_edward_flynn_stances.sql
      Header: "Migration 583: Edward M. Flynn Stances" (D2 District Councillor, external_id -2507000007).

    Step C — Apply:
      Pass full content of 583_edward_flynn_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (three queries for external_id=-2507000007):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/583_edward_flynn_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000007.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 583_edward_flynn_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000007
    - Stance count recorded
  </acceptance_criteria>
  <done>Migration 583 applied; unpaired=0, uncited=0 for Flynn.</done>
</task>

<task type="auto">
  <name>Task 3: John FitzGerald (D3, external_id -2507000008) — migration 584</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors: FitzGerald — D3, Dorchester north, moderate, public safety + economic development focus)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 4.

    Step A — Research (web search, ONE session):
      Politician: John FitzGerald, D3 District Councillor (external_id -2507000008).
      District covers Dorchester north.
      Sources: https://boston.gov/departments/city-council/john-fitzgerald, https://boston.legistar.com/, WBUR, Boston Globe.
      Focus topics: public-safety-approach, economic-development, housing, homelessness, city-sanitation, growth-and-development.
      Per D-01: no defaults; blank spoke if no public evidence.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000008;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/584_john_fitzgerald_stances.sql
      Header: "Migration 584: John FitzGerald Stances" (D3 District Councillor, external_id -2507000008).

    Step C — Apply:
      Pass full content of 584_john_fitzgerald_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (three queries for external_id=-2507000008):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/584_john_fitzgerald_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000008.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 584_john_fitzgerald_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000008
    - Stance count recorded
  </acceptance_criteria>
  <done>Migration 584 applied; unpaired=0, uncited=0 for FitzGerald.</done>
</task>

<task type="auto">
  <name>Task 4: Brian Worrell (D4, external_id -2507000009) — migration 585</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors: Worrell — D4, Dorchester south, progressive, housing + racial equity focus)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 5.

    Step A — Research (web search, ONE session):
      Politician: Brian Worrell, D4 District Councillor (external_id -2507000009).
      District covers Dorchester south.
      Sources: https://boston.gov/departments/city-council/brian-worrell, https://boston.legistar.com/, WBUR, Boston Globe, ballotpedia.org.
      Focus topics: housing, rent-regulation, civil-rights, homelessness, public-safety-approach, city-sanitation, economic-development.
      Worrell has strong record on housing affordability and racial equity — expect evidence on housing, rent-regulation, civil-rights.
      Per D-01: no defaults; evidence only.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000009;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/585_brian_worrell_stances.sql
      Header: "Migration 585: Brian Worrell Stances" (D4 District Councillor, external_id -2507000009).

    Step C — Apply:
      Pass full content of 585_brian_worrell_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (three queries for external_id=-2507000009):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/585_brian_worrell_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000009.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 585_brian_worrell_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000009
    - Stance count recorded
  </acceptance_criteria>
  <done>Migration 585 applied; unpaired=0, uncited=0 for Worrell.</done>
</task>

<task type="auto">
  <name>Task 5: Enrique J. Pepén (D5, external_id -2507000010) — migration 586</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors: Pepén — D5, East Boston, progressive, immigration + local-immigration focus; Assumption A4 on é UTF-8 encoding)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D. This is the last task in Plan 03.

    ACCENT NOTE: The politician's legal name is Enrique J. Pepén. The é character (U+00E9) MUST appear as a UTF-8 literal in all SQL reasoning text and header comments. The filename uses the accent-stripped form: 586_enrique_pepen_stances.sql.

    Step A — Research (web search, ONE session):
      Politician: Enrique J. Pepén, D5 District Councillor (external_id -2507000010).
      District covers East Boston — large immigrant-majority neighborhood.
      Sources: https://boston.gov/departments/city-council/enrique-pepen, https://boston.legistar.com/, WBUR, Boston Globe, ballotpedia.org.
      Priority topics: immigration, local-immigration, housing, rent-regulation, transportation-priorities, city-sanitation, economic-development, climate-change.
      Pepén is a progressive with strong immigrant-rights record — expect evidence on immigration, local-immigration, housing.
      Per D-01: no defaults; evidence only.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000010;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/586_enrique_pepen_stances.sql (accent stripped in filename)
      Header comment: "Migration 586: Enrique J. Pepén Stances" — é as UTF-8 literal in comment only.
      Politician UUID comment: paste resolved UUID.

    Step C — Apply:
      Pass full content of 586_enrique_pepen_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (three queries for external_id=-2507000010):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/586_enrique_pepen_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000010.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 586_enrique_pepen_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000010
    - Stance count recorded
    - SQL header comment contains "Enrique J. Pepén" with é as UTF-8 literal (not HTML entity or escaped form)
  </acceptance_criteria>
  <done>Migration 586 applied; unpaired=0, uncited=0 for Pepén.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web research → SQL migration | Scraped reasoning text enters SQL dollar-quoted strings |
| mcp__supabase-local | All SQL executes against remote production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-07 | Tampering | reasoning text (5 politicians) | mitigate | Dollar-quoting ($$...$$) — never single-quote concatenation |
| T-115-08 | Spoofing | sources array URLs | mitigate | Real URLs only from pages actually consulted |
| T-115-09 | Tampering | UUID cross-contamination across 5 politicians | mitigate | Resolve each UUID fresh via WHERE external_id=N; verify Step D count matches expected politician |
</threat_model>

<verification>
After all 5 tasks complete, run batch check via mcp__supabase-local__execute_sql:

  SELECT p.external_id, p.full_name, COUNT(pa.*) AS stance_count
  FROM essentials.politicians p
  LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
  WHERE p.external_id BETWEEN -2507000010 AND -2507000006
  GROUP BY p.external_id, p.full_name
  ORDER BY p.external_id;
Expected: 5 rows; stance_count ≥0.

  SELECT COUNT(*) AS uncited_batch FROM inform.politician_context pc
  JOIN essentials.politicians p ON p.id=pc.politician_id
  WHERE p.external_id BETWEEN -2507000010 AND -2507000006
    AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL OR array_length(pc.sources,1)=0);
Expected: 0.

  SELECT COUNT(*) AS unpaired_batch FROM inform.politician_answers pa
  JOIN essentials.politicians p ON p.id=pa.politician_id
  LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
  WHERE p.external_id BETWEEN -2507000010 AND -2507000006 AND pc.politician_id IS NULL;
Expected: 0.
</verification>

<success_criteria>
- Migrations 582–586 created and applied (sequential per D-08)
- All 5 D1–D5 district councillors have rows attempted (blank documented as intentional per D-01)
- Batch uncited=0, unpaired=0 confirmed
- Pepén é accent preserved as UTF-8 in SQL header comments
- D-01 through D-10 honored throughout
</success_criteria>

<output>
Create `.planning/phases/115-boston-stances/115-03-SUMMARY.md` documenting:
- Stance count per councillor (table: external_id | name | district | migration | stance_count | status)
- Batch quality gate results (uncited=0, unpaired=0)
- Notable evidence patterns per district
- Next migration: 587
</output>
