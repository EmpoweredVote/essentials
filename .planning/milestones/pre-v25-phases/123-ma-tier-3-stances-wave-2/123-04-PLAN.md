---
phase: 123-ma-tier-3-stances-wave-2
plan: 04
type: execute
wave: 4
depends_on: [123-03]
files_modified:
  - C:/EV-Accounts/backend/migrations/653_choquette_stances.sql
  - C:/EV-Accounts/backend/migrations/654_pemberton_stances.sql
  - C:/EV-Accounts/backend/migrations/655_oliver_stances.sql
  - C:/EV-Accounts/backend/migrations/656_baptiste_stances.sql
  - C:/EV-Accounts/backend/migrations/657_lopes_stances.sql
  - C:/EV-Accounts/backend/migrations/658_pereira_stances.sql
autonomous: true
requirements: [NEWBED-03]
must_haves:
  truths:
    - "New Bedford Ward councillors Choquette (W1), Pemberton (W2), Oliver (W3), Baptiste (W4), Lopes (W5), Pereira (W6) each have evidence-only stance rows or documented blanks"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/653_choquette_stances.sql"
      provides: "Ward 1 councillor Choquette stance migration"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/658_pereira_stances.sql"
      provides: "Ward 6 councillor Pereira (Council President) stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Research and apply evidence-only compass stances for New Bedford Ward councillors Choquette (W1), Pemberton (W2), Oliver (W3), Baptiste (W4), Lopes (W5), Pereira (W6/Council President) — migrations 653–658, one politician at a time.

Purpose: Complete all 12 New Bedford officials, finishing NEWBED-03 stance coverage.
Output: 6 per-individual stance migration files (653–658), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-RESEARCH.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md
@.planning/phases/123-ma-tier-3-stances-wave-2/123-03-SUMMARY.md

<interfaces>
<!-- Canonical stance migration contract — identical to Plans 01-03. Source: 282 + 574. -->
INSERT INTO inform.politician_answers (politician_id, topic_id, value)
VALUES ('{uuid}', '{topic_uuid}', {N}.0)
ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$, ARRAY['https://host/path']::text[]::text[])
ON CONFLICT (politician_id, topic_id)
DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules: float value (not bare int), double-cast ::text[]::text[], BEGIN;/COMMIT;,
plain $$ dollar-quote, >= 1 path-bearing source, NO row for no-evidence topics (never 3.0 default).
Zero-INSERT file is valid (Phase 115 ledger-consistency precedent).
</interfaces>

New Bedford Ward roster slice (external_id → migration), VERIFIED from Phase 120 deep seed (migration 587):
  -2545000007 Leo Choquette (Ward 1)            → 653
  -2545000008 Scott Pemberton (Ward 2)          → 654
  -2545000009 Shawn Oliver (Ward 3)             → 655
  -2545000010 Derek Baptiste (Ward 4)           → 656
  -2545000011 Joseph Lopes (Ward 5)             → 657
  -2545000012 Ryan Pereira (Ward 6, President)  → 658

UUIDs for these 6 officials were resolved in Plan 03 Task 1 and recorded in 123-03-SUMMARY.md. Read the SUMMARY first — do not re-query if UUIDs are present.

Out-of-scope guard: Do NOT write stances for any external_id outside the -2545000001 to -2545000012 range.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ward councillors Choquette, Pemberton, Oliver (653–655)</name>
  <files>C:/EV-Accounts/backend/migrations/653_choquette_stances.sql, C:/EV-Accounts/backend/migrations/654_pemberton_stances.sql, C:/EV-Accounts/backend/migrations/655_oliver_stances.sql</files>
  <read_first>
    - .planning/phases/123-ma-tier-3-stances-wave-2/123-03-SUMMARY.md (all 12 New Bedford UUIDs — extract the three needed here before researching)
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format: BEGIN/COMMIT, float literals, dollar-quoting)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (authoritative on ::text[]::text[] double-cast)
  </read_first>
  <action>
    Read 123-03-SUMMARY.md and extract UUIDs for external_ids -2545000007, -2545000008, -2545000009. If UUIDs are missing from SUMMARY, run: `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id BETWEEN -2545000009 AND -2545000007 ORDER BY external_id DESC;` to resolve them. Then process ONE politician at a time, strictly sequentially — never run two research sessions in parallel. For each of Choquette (-2545000007, Ward 1), Pemberton (-2545000008, Ward 2), Oliver (-2545000009, Ward 3):
    (a) Research the official's public record across ALL active compass topics — council meeting records, local news (New Bedford Light, SouthCoast Today), newbedford-ma.gov, campaign materials.
    (b) Write the canonical migration file: value is a float literal (e.g. 2.0); sources is ARRAY[...]::text[]::text[]; wrap in BEGIN;/COMMIT;; reasoning in $$...$$ citing the specific source; include the 44-topic UUID reference block in the file header.
    (c) For any topic with NO direct evidence, write NO INSERT — never default to 3.0. Zero-INSERT file is valid.
    (d) Apply via mcp__supabase-local__execute_sql, then immediately run both per-person verification queries before moving to the next official.
    Ward councillors typically have thinner records than at-large or mayor — blank spokes are acceptable and honest. Do NOT touch any external_id outside -2545000001 to -2545000012.
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{uuid}' AND pc.politician_id IS NULL;` returns 0, AND `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - 3 migration files exist (653, 654, 655), each wrapped in BEGIN;/COMMIT;
    - Every value is a float literal with `.0`; every sources array double-cast ::text[]::text[]
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT references an external_id outside the -2545000001 to -2545000012 range
  </acceptance_criteria>
  <done>Migrations 653–655 written, applied, and verified for Choquette, Pemberton, Oliver.</done>
</task>

<task type="auto">
  <name>Task 2: Ward councillors Baptiste, Lopes, Pereira (656–658)</name>
  <files>C:/EV-Accounts/backend/migrations/656_baptiste_stances.sql, C:/EV-Accounts/backend/migrations/657_lopes_stances.sql, C:/EV-Accounts/backend/migrations/658_pereira_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
    - C:/EV-Accounts/backend/migrations/655_oliver_stances.sql (just-written sibling file as in-phase reference)
  </read_first>
  <action>
    Process ONE politician at a time, sequentially, for Baptiste (-2545000010, Ward 4), Lopes (-2545000011, Ward 5), Pereira (-2545000012, Ward 6/Council President 2026). Use UUIDs from 123-03-SUMMARY.md. Same procedure: research public record across all active topics, write canonical migration (float values, double-cast sources, BEGIN/COMMIT, $$ reasoning, 44-topic UUID header), omit no-evidence topics (never 3.0 default), apply, verify per-person before next. Ryan Pereira serves as 2026 Council President — may have a richer public record and stronger media profile than typical Ward 6 councillors. Do NOT touch any external_id outside -2545000001 to -2545000012.
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: unpaired-answer query returns 0 AND uncited-context query returns 0 (same two queries as Task 1).</automated>
  </verify>
  <acceptance_criteria>
    - 3 migration files exist (656, 657, 658), each wrapped in BEGIN;/COMMIT;
    - Every value float literal; every sources array double-cast ::text[]::text[]
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT references an external_id outside the -2545000001 to -2545000012 range
  </acceptance_criteria>
  <done>Migrations 656–658 written, applied, and verified for Baptiste, Lopes, Pereira.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Evidence converted to SQL; risk of fabricated/uncited claims |
| migration SQL → production DB | Writes go directly to live production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | stance value integrity | mitigate | Evidence-only rule; per-person uncited-context query must return 0 |
| T-123-02 | Information Disclosure | reasoning / sources | accept | Public-record positions with public URLs; no PII |
| T-123-03 | Repudiation | stance provenance | mitigate | Every value paired with a path-bearing source URL |
| T-123-04 | Tampering | out-of-scope external_id | mitigate | Acceptance criteria asserts no external_id outside -2545000001 to -2545000012 |
| T-123-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
Partial citation check for this plan's officials:
`SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2545000012 AND -2545000007) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` must return 0.
</verification>

<success_criteria>
- Migrations 653–658 (6 officials) written, applied to production, and verified
- 0 unpaired answers, 0 uncited contexts across all 6 officials
- No out-of-scope external_ids touched
- Blank-spoke officials documented in SUMMARY (not defaulted)
- All 12 New Bedford officials now have stance files (migrations 647–658 complete)
</success_criteria>

<output>
Create `.planning/phases/123-ma-tier-3-stances-wave-2/123-04-SUMMARY.md` when done. Record per-official stance counts for migrations 653–658, blank-spoke officials, and total stance row count for all 12 New Bedford officials.
</output>
