---
phase: 151-el-monte-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql
autonomous: true
requirements: [ELMN-01]

must_haves:
  truths:
    - "This phase RECONCILES the EXISTING partial, dual-chamber El Monte seed (geo_id backfill + merge the two duplicate 'City Council' chambers + relabel the wrongly-'At-Large' council rows to 'District 1'..'District 5' + create the 'District 6' row) — NOT a greenfield rebuild. El Monte is a CVRA by-district city (Ordinance No. 3010, Apr 5 2022): SIX single-member council districts (D1-D6) + a separately, directly-elected citywide Mayor modeled LOCAL_EXEC ('El Monte Mayor' district) = a 7-seat body. This OVERTURNS the CONTEXT.md At-Large default per RESEARCH §D-02 Resolution. A Wave-1 STOP-on-drift pre-flight re-confirms gov f5fe3651 geo_id NULL/empty, BOTH chamber UUIDs (5ca38f3a official_count NULL / 4 offices incl. the Mayor + b41e0065 official_count 7 / 2 offices), the 6 offices + members/ext_ids, BOTH link directions, the office->district map, the live schema_migrations MAX + on-disk MAX, and STOPs on drift"
    - "The El Monte government row 'City of El Monte, California, US' (id f5fe3651-75c2-4ede-86e2-c13fc008d545) carries geo_id '0622230' (no longer NULL/empty), guarded WHERE (geo_id IS NULL OR geo_id = ''); state already 'CA'"
    - "move-then-delete (UUID-targeted): the two offices in chamber b41e0065 — Longoria 657386 (3040818a) + Ruedas 657390 (06d458fe) — are MOVED into the survivor chamber 5ca38f3a (the chamber that holds the Mayor office) FIRST (UPDATE chamber_id WHERE chamber_id='b41e0065…'), an inline assert proves b41e0065 then has 0 offices, THEN b41e0065 is DELETEd (target by UUID — both chambers share name 'City Council' / slug el-monte-city-council) — end state is ONE 'City Council' chamber (5ca38f3a) with exactly 6 offices (Cortez D6 creation is Plan 02, bringing it to 7)"
    - "by-district relabel (Pasadena 149 / Pomona 147 pattern): the wrongly-'At-Large' council LOCAL district rows are relabeled to their occupant's verified district per RESEARCH §D-02/D-03 — D1 (Crippen-Thomas -201202, office 211af77a), D2 (Herrera -201204, office 7e9eac5e), D3 (Ruedas 657390, office 06d458fe), D4 (Longoria 657386, office 3040818a), D5 (Galvan -201203, office 3ffcb893) — resolved via the pre-flight office->district map (do NOT assume UUID order); keeping geo_id '0622230', state 'CA'"
    - "the directly-elected Mayor (Ancona -200669, office 57d646fc, district_type='LOCAL_EXEC', district 'El Monte Mayor') is KEPT EXACTLY AS-IS — Lancaster 145 / Pasadena 149 directly-elected-mayor model. Do NOT relabel it to a district, do NOT collapse it to a rotational title, do NOT change its district_type"
    - "the 'District 6' row (for Cortez, seated in Plan 02) is CREATED guarded WHERE NOT EXISTS (LOCAL, geo_id '0622230', state 'CA'); after this wave exactly 6 LOCAL district rows ('District 1'..'District 6') + exactly 1 LOCAL_EXEC row ('El Monte Mayor') exist under gov f5fe3651"
    - "feedback_section_split_check SQL returns 0 rows for El Monte (geo_id '0622230') after consolidation (El Monte proper is NOT in the project_split_section_defects_5_cities set)"
    - "this reconcile (geo_id backfill + chamber merge + district relabel + create-D6) is a STRUCTURAL migration (1000) that registers in supabase_migrations.schema_migrations normally; the migration file is COMMITTED to the EV-Accounts repo (feedback_no_git_in_ev_accounts)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql"
      provides: "Idempotent geo_id backfill + move-Longoria/Ruedas-then-delete-chamber-b41e0065 + relabel At-Large council rows to District 1-5 + create District 6 row + keep Mayor LOCAL_EXEC as-is"
      contains: "b41e0065"
  key_links:
    - from: "essentials.governments (id f5fe3651-75c2-4ede-86e2-c13fc008d545)"
      to: "geo_id '0622230'"
      via: "UPDATE ... WHERE id = 'f5fe3651-75c2-4ede-86e2-c13fc008d545' AND (geo_id IS NULL OR geo_id = '')"
      pattern: "geo_id\\s*=\\s*'0622230'"
    - from: "essentials.offices (Longoria 3040818a + Ruedas 06d458fe, the doomed-chamber offices)"
      to: "survivor chamber 5ca38f3a"
      via: "UPDATE offices SET chamber_id='5ca38f3a...' WHERE chamber_id='b41e0065...'"
      pattern: "chamber_id\\s*=\\s*'5ca38f3a"
    - from: "essentials.chambers (b41e0065, emptied duplicate)"
      to: "deleted (asserted 0 offices first)"
      via: "DO $$ assert 0 offices $$ then DELETE FROM essentials.chambers WHERE id = 'b41e0065...'"
      pattern: "DELETE FROM essentials\\.chambers"
    - from: "essentials.districts (new 'District 6' row for Cortez)"
      to: "created guarded NOT EXISTS (LOCAL, geo_id '0622230', state 'CA')"
      via: "INSERT ... SELECT 'District 6','LOCAL','0622230','CA' WHERE NOT EXISTS (...)"
      pattern: "'District 6'"
---

<objective>
Reconcile the existing partial, duplicate-chamber El Monte seed in a single idempotent structural migration
(1000). This is the foundation wave — every later wave (roster/Cortez creation, headshots, stances) assumes a
clean, single-chamber, correctly-districted structure with the directly-elected Mayor preserved as LOCAL_EXEC.

⚠️ RECONCILE, NOT GREENFIELD (DB-verified 2026-06-21 in CONTEXT.md §DB Pre-Check). The El Monte gov already
exists with geo_id NULL, TWO duplicate 'City Council' chambers (both slug el-monte-city-council), and 6 offices
(5 council ALL mislabeled 'At-Large' + 1 directly-elected Mayor). This wave does NOT create Cortez or unlink
anyone — it (1) backfills geo_id, (2) merges the duplicate chamber, (3) fixes the council form of government
from (wrong) At-Large to El Monte's real by-district structure (relabel D1-D5), and (4) creates the District 6
row that Cortez will occupy in Plan 02. The Cortez creation, the bidirectional back-pointer repairs,
official_count, and any unlink are Plan 02 — this wave is STRUCTURE ONLY.

⚠️ EL MONTE IS BY-DISTRICT WITH A DIRECTLY-ELECTED MAYOR (RESEARCH §D-02 Resolution — OVERTURNS the CONTEXT.md
At-Large default). RESEARCH verified against ci.el-monte.ca.us + Ordinance No. 3010 (Apr 5 2022, Map 117): the
council expanded from a 5-seat at-large body to SIX by-district seats (D1-D6) PLUS a separately, directly-elected
citywide Mayor. This is the Pasadena (149) / Pomona (147) by-district relabel pattern for the council seats AND
the Lancaster (145) / Pasadena (149) directly-elected-LOCAL_EXEC-Mayor pattern for Ancona — NOT the
Downey/Palmdale rotational-mayor model. Therefore:
- The 5 wrongly-'At-Large' council rows are relabeled to D1-D5 per the RESEARCH §D-02/D-03 verified per-person
  map (Crippen-Thomas D1, Herrera D2, Ruedas D3, Longoria D4, Galvan D5).
- A new 'District 6' row is created (for Cortez, seated Plan 02).
- Mayor Ancona (-200669, office 57d646fc, LOCAL_EXEC, district 'El Monte Mayor') is KEPT EXACTLY AS-IS — do NOT
  relabel, collapse, or change district_type. (Anti-Pattern per RESEARCH §Anti-Patterns to Avoid.)

Four structural fixes, all DB-verified against the live state on 2026-06-21:
1. Backfill essentials.governments.geo_id = '0622230' on gov f5fe3651-75c2-4ede-86e2-c13fc008d545 (currently
   NULL), guarded WHERE (geo_id IS NULL OR geo_id = ''). state already 'CA'.
2. MERGE the duplicate chamber: MOVE both b41e0065 offices (Longoria 3040818a + Ruedas 06d458fe) into the
   survivor chamber 5ca38f3a (the chamber holding the Mayor office 57d646fc) FIRST (UPDATE chamber_id WHERE
   chamber_id='b41e0065…'), then an inline assert confirms b41e0065 now has 0 offices, then DELETE the chamber
   b41e0065. Target both chambers/offices by UUID ONLY — both share the name 'City Council'. End state: ONE
   chamber (5ca38f3a) with 6 offices (Cortez D6 is Plan 02, → 7).
3. RELABEL the wrongly-'At-Large' council district rows to their occupant's verified district per the RESEARCH
   §D-02/D-03 map (D1 Crippen-Thomas, D2 Herrera, D3 Ruedas, D4 Longoria, D5 Galvan), resolving each At-Large
   UUID from the pre-flight office->district map. Keep geo_id '0622230', state 'CA'. Do NOT touch the Mayor's
   LOCAL_EXEC 'El Monte Mayor' row.
4. CREATE the missing 'District 6' row guarded WHERE NOT EXISTS (LOCAL, geo_id '0622230', state 'CA') for
   Cortez (created/seated in Plan 02; this wave only ensures her District 6 row exists).

Purpose: A clean structural base. Duplicate 'City Council' chamber names cause split-section render bugs; NULL
geo_id breaks routing; the wrong At-Large labels misrepresent El Monte's real by-district form of government.
Output: Migration 1000 applied to production AND committed to the EV-Accounts repo; all post-verification
queries green; exactly 6 LOCAL district rows D1-D6 + exactly 1 LOCAL_EXEC 'El Monte Mayor'; split-section check
0 rows.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally (NOT a
stance/headshot audit-only migration). On-disk file counter is authoritative; next structural is 1000 (RESEARCH
§D-01: on-disk MAX 999 = 999_downey_orphan_office_cleanup.sql; schema_migrations MAX must be re-confirmed at
apply time — pre-flight MUST re-confirm BOTH the on-disk MAX and the live schema_migrations MAX before
committing to 1000, since other workstreams may have advanced the counter).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-el-monte-deep-seed/151-RESEARCH.md
@.planning/phases/151-el-monte-deep-seed/151-CONTEXT.md
@.planning/phases/151-el-monte-deep-seed/151-VALIDATION.md
@.planning/phases/149-pasadena-deep-seed/149-01-PLAN.md
@.planning/phases/150-downey-deep-seed/150-01-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- DB-verified live values (2026-06-21, CONTEXT.md §DB Pre-Check + RESEARCH §D-02 Resolution / §D-03). Task 1
     re-confirms them read-only before any write and STOPs on drift. -->

Government row:
  id     = f5fe3651-75c2-4ede-86e2-c13fc008d545   name = 'City of El Monte, California, US'
  geo_id = NULL  -> set to '0622230'   (state already 'CA')
  (OUT OF SCOPE same-name govs: e46a6c1e = El Monte City School District; 71d17594 = South El Monte;
   0f1a5895 = El Monte Union High — NEVER touch these.)

Two chambers, BOTH named 'City Council' / slug 'el-monte-city-council' under gov f5fe3651 (MERGE into one):
  KEEP   5ca38f3a-ea2e-4160-abb5-f897702b6cb6  (SURVIVOR — holds the Mayor office; official_count NULL, 4 offices)
  DELETE b41e0065-40ed-4486-8ff1-6fe73e0c2532  (DOOMED — official_count 7, 2 offices: Longoria + Ruedas)
  (chambers.slug is a GENERATED column — NEVER include slug in any chamber write)
  (RESEARCH §D-02/D-03 survivor choice: 5ca38f3a holds the Mayor office 57d646fc — keep it as survivor.)

Survivor 5ca38f3a offices (4; relabel their council districts; KEEP the Mayor LOCAL_EXEC):
  211af77a  Sheila Crippen-Thomas (ext_id -201202, At-Large LOCAL     -> relabel district 'District 1')
  7e9eac5e  Martin Herrera        (ext_id -201204, At-Large LOCAL     -> relabel district 'District 2')
  3ffcb893  Cindy Galvan          (ext_id -201203, At-Large LOCAL     -> relabel district 'District 5')
  57d646fc  Jessica Ancona        (ext_id -200669, Mayor LOCAL_EXEC, district 'El Monte Mayor' -> KEEP AS-IS)

Doomed b41e0065 offices (MOVE both into survivor, then delete the empty chamber):
  3040818a  Viviana Longoria      (ext_id 657386,  At-Large LOCAL     -> relabel district 'District 4'; Mayor Pro Tem)
  06d458fe  Julia Ruedas          (ext_id 657390,  At-Large LOCAL     -> relabel district 'District 3')

Total = 6 offices (5 council At-Large + 1 Mayor LOCAL_EXEC). Cortez (D6) is MISSING — created in Plan 02.

Move-then-delete (Pasadena 946 / Downey 990 pattern — UUID-targeted, 2 offices):
  Step order: (1) move Longoria 3040818a + Ruedas 06d458fe from b41e0065 to 5ca38f3a FIRST, (2) assert
  b41e0065 empty, (3) delete b41e0065, (4) THEN do the district relabel + create District 6.
  1. UPDATE essentials.offices SET chamber_id='5ca38f3a-ea2e-4160-abb5-f897702b6cb6'
     WHERE chamber_id='b41e0065-40ed-4486-8ff1-6fe73e0c2532'   (moves Longoria + Ruedas into the survivor)
  2. DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='b41e0065-...') > 0
     THEN RAISE EXCEPTION 'Chamber b41e0065 still has offices'; END IF; END $$;
  3. DELETE FROM essentials.chambers WHERE id='b41e0065-40ed-4486-8ff1-6fe73e0c2532'

District relabel (relabel label ONLY; geo_id/state stay as-is). The MANDATORY pre-flight office->district map
  (RESEARCH §Pitfall 2 query over BOTH chambers) maps each office to its current district_id + label +
  district_type — use THOSE UUIDs (do NOT assume order):
  SELECT o.id AS office_id, p.external_id, p.last_name, o.title, d.id AS district_id, d.label, d.district_type
  FROM essentials.offices o
  LEFT JOIN essentials.politicians p ON p.id = o.politician_id
  LEFT JOIN essentials.districts d ON d.id = o.district_id
  WHERE o.chamber_id IN ('5ca38f3a-...','b41e0065-...')
  ORDER BY d.district_type, d.label;
  Occupant -> district relabel map (RESEARCH §D-02/D-03 — authoritative, verified against ci.el-monte.ca.us):
    Crippen-Thomas (211af77a, -201202) -> 'District 1'
    Herrera        (7e9eac5e, -201204) -> 'District 2'
    Ruedas         (06d458fe, 657390)  -> 'District 3'
    Longoria       (3040818a, 657386)  -> 'District 4'  (Mayor Pro Tem — title detail is Plan 02 / Discretion)
    Galvan         (3ffcb893, -201203) -> 'District 5'
    (D6 Cortez: create the 'District 6' row here; Cortez politician + office seat is Plan 02)
  Each relabel guarded label IS DISTINCT FROM target.

KEEP the Mayor (RESEARCH §Anti-Patterns / Lancaster 145 / Pasadena 149 Gordo precedent):
  Ancona's office 57d646fc + her LOCAL_EXEC district row ('El Monte Mayor') stay UNTOUCHED. Do NOT relabel,
  do NOT change district_type, do NOT collapse. El Monte directly elects its mayor citywide.

District CREATE (guarded NOT EXISTS — 'District 6' for Cortez; Pasadena 946 create-missing-district precedent):
  INSERT INTO essentials.districts (label, district_type, geo_id, state)
  SELECT 'District 6','LOCAL','0622230','CA'
  WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE label='District 6' AND geo_id='0622230');
  (Cortez's office seat + politician creation is Plan 02; this wave only ensures the District 6 row exists.)

Schema notes (CRITICAL — CA convention, LOCATION-ONBOARDING §California Quick Reference):
  - chambers.slug is GENERATED — NEVER include slug in any chamber write
  - district_type + label live on essentials.districts, NOT on offices (no offices.district_type column)
  - districts.state is uppercase 'CA'
  - El Monte's mayor is DIRECTLY-ELECTED (Lancaster/Pasadena LOCAL_EXEC model) — KEEP Ancona's LOCAL_EXEC row;
    do NOT apply the Downey/Palmdale rotational-mayor collapse (RESEARCH §Anti-Patterns)
  - geo_id guard MUST include the empty-string case: WHERE (geo_id IS NULL OR geo_id = '')
  - This wave does NOT create Cortez, set official_count, or repair back-pointers (all Plan 02)

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after
  consolidation; expect 0 rows for El Monte geo_id '0622230' (El Monte proper is NOT in the
  project_split_section_defects_5_cities set — South El Monte is, and is OUT OF SCOPE).

Pattern migrations to copy:
  C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql (the DIRECT dual-chamber-merge + At-Large->District
    relabel + create-missing-district + KEEP-LOCAL_EXEC-Mayor template — Pasadena had the same shape: duplicate
    chambers, by-district council, directly-elected Mayor kept as LOCAL_EXEC).
  C:/EV-Accounts/backend/migrations/990_downey_reconcile.sql (most recent move-then-delete + relabel + idempotent
    guards + post-verification block shape).

Migration ledger (RESEARCH §D-01): on-disk MAX = 999 (999_downey_orphan_office_cleanup.sql); next STRUCTURAL is
  1000. Pre-flight MUST re-confirm BOTH the on-disk MAX (ls of C:/EV-Accounts/backend/migrations/) AND the live
  SELECT MAX(version::int) FROM supabase_migrations.schema_migrations before numbering — other workstreams may
  have advanced the counter overnight. If the on-disk MAX is no longer 999, number 1000_elmonte_reconcile.sql to
  the next free slot and STOP-report the drift.

EV-Accounts is a git repo on master (feedback_no_git_in_ev_accounts) — the migration FILE must be committed via
  `git -C "C:/EV-Accounts"` after the DB apply (the apply is the push; the file still needs committing). The
  143-150 uncommitted-migration backlog must not recur.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state (gov, two chambers, both link directions, the office->district map, the At-Large/LOCAL_EXEC set, Ancona's Mayor LOCAL_EXEC, migration counters) — STOP on drift</name>
  <files>C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql (the DIRECT dual-chamber-merge + At-Large->District relabel + create-missing-district + KEEP-LOCAL_EXEC-Mayor STRUCTURAL template — the same shape as El Monte)
    - C:/EV-Accounts/backend/migrations/990_downey_reconcile.sql (most recent move-then-delete + relabel + idempotent guards + post-verification block shape)
    - .planning/phases/149-pasadena-deep-seed/149-01-PLAN.md (the directly-elected-Mayor-kept-as-LOCAL_EXEC + by-district relabel precedent this plan mirrors)
    - .planning/phases/151-el-monte-deep-seed/151-RESEARCH.md (§D-02 Resolution BY-DISTRICT verdict, §D-02/§D-03 roster + office-to-district map, §Common Pitfalls 1-7, §Open Questions, §D-01 Migration Ledger, §Validation Architecture ELMN-01a..01j, §Anti-Patterns)
    - .planning/phases/151-el-monte-deep-seed/151-CONTEXT.md (LOCKED decisions D-01/D-02/D-03 + §DB Pre-Check live values)
    - .planning/phases/151-el-monte-deep-seed/151-VALIDATION.md (the per-wave verification SELECT contract; note: VALIDATION says official_count=7 but RESEARCH §D-03 Pitfall 3 authoritatively sets official_count=6 (council only, Mayor excluded) — follow RESEARCH; official_count is a Plan 02 concern regardless)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA', district_type on districts)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE/INSERT, run read-only confirmation queries via mcp__supabase-local
    execute_sql (= production). Confirm EACH against the LOCKED CONTEXT §DB Pre-Check + RESEARCH §D-02/§D-03:
    (a) gov: SELECT id, geo_id, state, name FROM essentials.governments WHERE
    id='f5fe3651-75c2-4ede-86e2-c13fc008d545' — confirm geo_id NULL/empty, state 'CA', name 'City of El Monte,
    California, US';
    (b) two chambers under that gov both named 'City Council' / slug 'el-monte-city-council' — 5ca38f3a
    (SURVIVOR, official_count NULL, 4 offices) and b41e0065 (DOOMED, official_count 7, 2 offices); confirm NO
    third chamber;
    (c) survivor 5ca38f3a has exactly 4 offices occupied via offices.politician_id: 211af77a (Crippen-Thomas
    -201202), 7e9eac5e (Herrera -201204), 3ffcb893 (Galvan -201203), 57d646fc (Ancona -200669, the Mayor);
    (d) doomed b41e0065 has exactly 2 offices: 3040818a (Longoria 657386) + 06d458fe (Ruedas 657390) — the MOVE
    targets;
    (e) run the MANDATORY office->district map (RESEARCH §Pitfall 2 query over BOTH chambers) — record each
    office's current district_id + label + district_type + title; determine the count of 'At-Large' LOCAL rows,
    identify Ancona's LOCAL_EXEC 'El Monte Mayor' district UUID (office 57d646fc), and whether any 'District 1'
    ..'District 6' row already exists for geo_id 0622230;
    (f) confirm Ancona's office 57d646fc references the ONLY LOCAL_EXEC district row ('El Monte Mayor') under gov
    f5fe3651 — this row stays UNTOUCHED (RESEARCH §Anti-Patterns);
    (g) confirm NO shared-district defect (no two council offices pointing at the same district_id) — if found,
    note it for a create+repoint resolution (Pasadena 946 / Pomona pattern);
    (h) re-confirm the CURRENT by-district roster + Mayor live against ci.el-monte.ca.us (NOT elmonteca.gov —
    Pitfall 7): Mayor Ancona (citywide) + D1 Crippen-Thomas / D2 Herrera / D3 Ruedas / D4 Longoria / D5 Galvan /
    D6 Cortez (RESEARCH §D-02/D-03);
    (i) Cortez absence (Pitfall — RESEARCH §Open Q2 / Assumption A1): SELECT id, external_id, first_name,
    last_name FROM essentials.politicians WHERE first_name ILIKE '%marisol%' AND last_name ILIKE '%cortez%' —
    record whether a Cortez row already exists (Plan 02 needs this; if found, Plan 02 REUSES it).
    Also confirm the migration ledger: SELECT MAX(version::int) FROM supabase_migrations.schema_migrations AND
    the on-disk MAX file in C:/EV-Accounts/backend/migrations/ (expect 999 = 999_downey_orphan_office_cleanup.sql)
    — next structural is 1000. If ANY precondition does not match (a third chamber, b41e0065 has !=2 offices,
    5ca38f3a has !=4 offices, an office occupied by a different ext_id than locked, Ancona NOT LOCAL_EXEC, MORE
    than one LOCAL_EXEC row, a shared-district defect, the on-disk MAX not 999, or the current roster/mayor
    differs from RESEARCH §D-02/D-03), STOP and report the discrepancy — do NOT write a migration against a moved
    target. Record the resolved district UUIDs (incl. Ancona's LOCAL_EXEC UUID), the Cortez finding, and the
    confirmed migration numbers in the SUMMARY for Plan 02.
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql "SELECT geo_id, state FROM essentials.governments WHERE id='f5fe3651-75c2-4ede-86e2-c13fc008d545';" returns one row, geo_id NULL or '', state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - Gov f5fe3651 has geo_id NULL/empty and state 'CA' (pre-state); confirmed name 'City of El Monte, California, US'
    - Two chambers confirmed sharing name 'City Council': 5ca38f3a (KEEP, 4 offices incl. Mayor, official_count NULL) and b41e0065 (DELETE, 2 offices, official_count 7); no third chamber
    - Survivor 5ca38f3a offices confirmed OCCUPIED via offices.politician_id (211af77a=Crippen-Thomas -201202, 7e9eac5e=Herrera -201204, 3ffcb893=Galvan -201203, 57d646fc=Ancona -200669 Mayor)
    - Doomed b41e0065 confirmed to have exactly 2 offices: 3040818a (Longoria 657386) + 06d458fe (Ruedas 657390) — the move targets
    - Office->district map run; the At-Large LOCAL row count, Ancona's LOCAL_EXEC 'El Monte Mayor' district UUID (office 57d646fc), and the existence of any 'District 1'..'District 6' row all resolved; any shared-district defect surfaced
    - Ancona's office 57d646fc confirmed pointing at the ONLY LOCAL_EXEC district row ('El Monte Mayor') under gov f5fe3651 (the row to KEEP untouched)
    - Cortez absence/presence recorded for Plan 02 (SELECT by name — Assumption A1)
    - Ledger confirmed: on-disk MAX 999, live schema_migrations MAX recorded, next structural 1000
    - Current by-district roster + Mayor re-confirmed live against ci.el-monte.ca.us = Ancona Mayor + Crippen-Thomas D1 / Herrera D2 / Ruedas D3 / Longoria D4 / Galvan D5 / Cortez D6; STOP-and-report if changed
    - Any mismatch vs CONTEXT §DB Pre-Check / RESEARCH §D-02/§D-03 is surfaced and execution paused (STOP-on-drift; no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches CONTEXT §DB Pre-Check + RESEARCH §D-02/§D-03; gov UUID, the two move-target offices, the office->district map, Ancona's LOCAL_EXEC UUID, the Cortez finding, and the migration numbers (1000 next) are all confirmed; the geo_id backfill, chamber merge, district relabel, and create-District-6 are safe to author.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 1000 (geo_id backfill + merge duplicate chamber + relabel At-Large->District 1-5 + create District 6 + keep Mayor LOCAL_EXEC as-is), then COMMIT the file to EV-Accounts</name>
  <files>C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql (copy the idempotent geo_id backfill + office move + FK-safe chamber DELETE + inline-assert + At-Large->District relabel + create-missing-district + KEEP-LOCAL_EXEC-Mayor patterns + the schema_migrations register + post-verification block)
    - C:/EV-Accounts/backend/migrations/990_downey_reconcile.sql (the most recent move-then-delete authoring + idempotent-guard + post-verification shape)
    - .planning/phases/149-pasadena-deep-seed/149-01-PLAN.md (the exact authoring + post-verification query set this plan mirrors — directly-elected Mayor kept)
    - .planning/phases/151-el-monte-deep-seed/151-RESEARCH.md (§Architecture Patterns 1+2 SQL templates, §D-02/§D-03 relabel map, §Common Pitfalls 1/2/3/5/7, §Anti-Patterns, §Validation Architecture ELMN-01a..01g)
    - .planning/phases/151-el-monte-deep-seed/151-CONTEXT.md (LOCKED D-01/D-02/D-03 + the EV-Accounts commit requirement)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA')
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql wrapped in BEGIN/COMMIT, all statements
    idempotent, in this order (mirror 946/990 — do NOT inline full SQL here; copy the templates' shapes):
    (1) UPDATE essentials.governments SET geo_id='0622230' WHERE
        id='f5fe3651-75c2-4ede-86e2-c13fc008d545' AND (geo_id IS NULL OR geo_id = '') (ELMN-01a, empty-string guard).
    (2) MERGE — MOVE both doomed-chamber offices into the survivor FIRST (Pitfall 5 ordering): UPDATE
        essentials.offices SET chamber_id='5ca38f3a-ea2e-4160-abb5-f897702b6cb6' WHERE
        chamber_id='b41e0065-40ed-4486-8ff1-6fe73e0c2532' (carries Longoria 3040818a + Ruedas 06d458fe; target
        by the doomed UUID).
    (3) Inline assert the doomed chamber is now empty: a DO $$ block that RAISEs EXCEPTION if
        (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='b41e0065-40ed-4486-8ff1-6fe73e0c2532') > 0.
    (4) DELETE essentials.chambers WHERE id='b41e0065-40ed-4486-8ff1-6fe73e0c2532'. Target by UUID ONLY, never
        by name/slug (both chambers share 'City Council' / el-monte-city-council).
    (5) RELABEL the wrongly-'At-Large' council district rows to their occupant's verified district, each guarded
        label IS DISTINCT FROM target, using the At-Large UUIDs resolved by the Task 1 office->district map:
        'District 1' (Crippen-Thomas office 211af77a), 'District 2' (Herrera office 7e9eac5e), 'District 3'
        (Ruedas office 06d458fe), 'District 4' (Longoria office 3040818a), 'District 5' (Galvan office 3ffcb893).
        Do NOT alter geo_id or state. Do NOT touch Ancona's LOCAL_EXEC 'El Monte Mayor' row (office 57d646fc) —
        it stays exactly as-is (RESEARCH §Anti-Patterns).
    (6) CREATE the missing 'District 6' row guarded WHERE NOT EXISTS (for Cortez; seated in Plan 02): INSERT INTO
        essentials.districts (label, district_type, geo_id, state) SELECT 'District 6','LOCAL','0622230','CA'
        WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE label='District 6' AND geo_id='0622230').
        (If the pre-flight found a shared-district defect, resolve it with the Pasadena 946 create-new-row-and-
        repoint pattern.)
    Do NOT touch the slug column anywhere (GENERATED). Do NOT write to offices.district_type (no such column).
    Do NOT create Cortez, seat any office, set official_count, or repair back-pointers (all Plan 02). Do NOT
    collapse or relabel the Mayor (El Monte directly elects; Downey/Palmdale rotational collapse does NOT apply).
    End the file with post-verification queries (mirror 946/990): geo_id check + a chamber listing under gov
    f5fe3651 (expect exactly one 'City Council', the survivor 5ca38f3a) + a count of offices under 5ca38f3a
    (expect 6) + a confirm b41e0065 is gone + the district labels for geo_id 0622230 ('District 1'..'District 6'
    LOCAL + 'El Monte Mayor' LOCAL_EXEC; exactly 6 LOCAL + exactly 1 LOCAL_EXEC) + a confirm Ancona's office
    57d646fc still LOCAL_EXEC 'El Monte Mayor' untouched + the feedback_section_split_check SQL (expect 0 rows
    for El Monte). Apply via mcp__supabase-local execute_sql, and register migration 1000 in
    supabase_migrations.schema_migrations (structural — INSERT ... ON CONFLICT (version) DO NOTHING, per 946/990).
    Record the newly-created 'District 6' UUID + the relabeled district UUIDs in the SUMMARY for Plan 02. Then
    COMMIT the migration file to the EV-Accounts repo: git -C "C:/EV-Accounts" add backend/migrations/1000_elmonte_reconcile.sql
    && git -C "C:/EV-Accounts" commit -m "feat(151): El Monte reconcile — geo_id backfill + chamber merge + by-district relabel D1-D5 + create D6" (per feedback_no_git_in_ev_accounts; always operate from the repo root via git -C, never cd into /backend).
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql checks ALL of: SELECT geo_id FROM essentials.governments WHERE id='f5fe3651-75c2-4ede-86e2-c13fc008d545' returns '0622230'; SELECT COUNT(*) FROM essentials.chambers WHERE government_id='f5fe3651-75c2-4ede-86e2-c13fc008d545' AND name='City Council' returns 1; SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='5ca38f3a-ea2e-4160-abb5-f897702b6cb6' returns 6; SELECT COUNT(*) FROM essentials.districts WHERE geo_id='0622230' AND district_type='LOCAL' AND label IN ('District 1','District 2','District 3','District 4','District 5','District 6') returns 6; SELECT COUNT(*) FROM essentials.districts d JOIN essentials.offices o ON o.district_id=d.id WHERE o.id='57d646fc...' AND d.district_type='LOCAL_EXEC' returns 1 (Mayor untouched). Also: git -C "C:/EV-Accounts" log --oneline -1 -- backend/migrations/1000_elmonte_reconcile.sql shows the commit</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='f5fe3651-...'` -> '0622230'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='f5fe3651-...' AND name='City Council'` -> 1 (not 0, not 2); b41e0065 deleted
    - `SELECT COUNT(*) FROM essentials.chambers WHERE id='b41e0065-40ed-4486-8ff1-6fe73e0c2532'` -> 0 (doomed deleted)
    - `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='5ca38f3a-ea2e-4160-abb5-f897702b6cb6'` -> 6 (Longoria + Ruedas moved in; Cortez D6 -> 7 is Plan 02)
    - District relabel: rows for geo_id '0622230' = 'District 1'..'District 5' (all LOCAL) per the verified map (Crippen-Thomas D1, Herrera D2, Ruedas D3, Longoria D4, Galvan D5); each still geo_id '0622230', state 'CA'
    - 'District 6' row created (guarded NOT EXISTS, LOCAL, geo_id '0622230', state 'CA') for Cortez; exactly 6 LOCAL district rows D1-D6
    - Ancona's office 57d646fc + her 'El Monte Mayor' LOCAL_EXEC district row UNCHANGED (exactly 1 LOCAL_EXEC row remains under gov f5fe3651); no relabel/collapse applied
    - No politician created/seated; official_count NOT set; no back-pointers repaired (all Plan 02)
    - feedback_section_split_check SQL returns 0 rows for El Monte (geo_id '0622230')
    - Migration 1000 registered in supabase_migrations.schema_migrations (structural)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
    - Migration file 1000_elmonte_reconcile.sql committed to the EV-Accounts repo (git -C "C:/EV-Accounts")
  </acceptance_criteria>
  <done>Migration 1000 applied + registered + committed to EV-Accounts; one 'City Council' chamber (5ca38f3a) remains with 6 offices; geo_id 0622230 set; council districts relabeled D1-D5; District 6 row created for Cortez; the directly-elected Mayor (Ancona, LOCAL_EXEC 'El Monte Mayor') untouched; split-section check 0 rows; re-running the file is a no-op; new/relabeled district UUIDs recorded for Plan 02.</done>
</task>

</tasks>

<threat_model>
DB-seeding phase with NO new external attack surface. No untrusted user input crosses any boundary — migration
SQL is applied via service-role/operator (MCP) directly against the internal production Supabase DB. The real
threat surface is accidental data corruption — deleting the doomed chamber before moving Longoria/Ruedas
(orphaned offices), relabeling the wrong district row (multiple similar At-Large UUIDs), wrongly
collapsing/relabeling Ancona's directly-elected LOCAL_EXEC Mayor (applying the Downey rotational model where it
does not belong), creating a duplicate 'District 6', missing the empty-string geo_id case, writing to a
non-existent offices.district_type column, touching the wrong gov (the El Monte City School District e46a6c1e
or South El Monte 71d17594), or fetching headshots from the wrong host — mitigated by move-FIRST ordering, an
inline empty-chamber assert, UUID-scoped targeting of every write, relabel-only guards, the KEEP-Mayor-as-is
rule, a NOT EXISTS guard on District 6, the empty-string geo_id guard, and a STOP-on-drift pre-flight that
checks BOTH link directions, the office->district map, Ancona's LOCAL_EXEC UUID, and the current roster/mayor.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> production DB | Migration SQL applied by a trusted operator (MCP) directly against production Supabase. No untrusted user input crosses here. |
| official source (ci.el-monte.ca.us) -> roster confirmation | Live re-confirmation of the current directly-elected Mayor + 6 district occupants before any write; read-only here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Tampering | Idempotent guarded writes — geo_id overwritten on re-run / wrong gov / empty-string case missed; chamber DELETE targets wrong chamber (both share name 'City Council' / slug el-monte-city-council) | mitigate | geo_id UPDATE keyed on id f5fe3651 + WHERE (geo_id IS NULL OR geo_id=''); pre-flight confirms it is the City of El Monte gov (NOT the El Monte City School District e46a6c1e / South El Monte 71d17594); chamber DELETE keyed on UUID 'b41e0065' ONLY, never name/slug; doomed-chamber merge asserts 0 offices before DELETE; second apply = 0 rows |
| T-151-02 | Tampering | Doomed chamber deleted BEFORE Longoria/Ruedas moved -> orphaned offices (Pitfall 5); wrong district relabeled (multiple similar At-Large UUIDs) | mitigate | Move-FIRST (UPDATE WHERE chamber_id='b41e0065…') + inline DO $$ assert RAISEs if b41e0065 still has >0 offices before DELETE; each relabel UPDATE keyed on the exact At-Large UUID from the Task 1 office->district map per the locked RESEARCH §D-02/D-03 mapping; post-verify labels D1-D5 |
| T-151-03 | Tampering | Wrong mayor model applied — Ancona's directly-elected LOCAL_EXEC collapsed/relabeled (Downey rotational model misapplied; RESEARCH §Anti-Patterns) | mitigate | Ancona's office 57d646fc + her 'El Monte Mayor' LOCAL_EXEC district left UNTOUCHED; no relabel/collapse/district_type change; post-verify exactly 1 LOCAL_EXEC row remains under gov f5fe3651 |
| T-151-04 | Tampering | Duplicate 'District 6' row created on re-run, or a shared-district defect missed; write to non-existent offices.district_type column | mitigate | District 6 INSERT guarded WHERE NOT EXISTS on (label, geo_id='0622230'); pre-flight maps the office->district matrix + any shared district; non-mayor relabel touches label ONLY (no offices.district_type write); second apply = 0 rows |
| T-151-05 | Tampering | Non-idempotent migration applied twice, or written without verifying pre-state (drift since 2026-06-21) | mitigate | All writes guarded (geo_id IS NULL/empty, id-scoped move/delete, label IS DISTINCT FROM, NOT EXISTS); Task 1 read-only pre-flight confirms BOTH link directions + office->district map + Ancona's LOCAL_EXEC UUID + the current roster/mayor before any write; STOP-on-mismatch; second apply = 0 rows |
| T-151-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable; existing Pillow + Supabase client only) |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 1000:
1. gov f5fe3651 geo_id = '0622230' (state 'CA')
2. exactly 1 chamber 'City Council' under gov f5fe3651; doomed b41e0065 gone
3. survivor chamber 5ca38f3a has exactly 6 offices (Cortez D6 -> 7 is Plan 02)
4. district rows for geo_id 0622230: 'District 1'..'District 6' (all LOCAL, 6 rows) + 'El Monte Mayor' (LOCAL_EXEC, 1 row); Crippen-Thomas D1 / Herrera D2 / Ruedas D3 / Longoria D4 / Galvan D5
5. Ancona's office 57d646fc + 'El Monte Mayor' LOCAL_EXEC row UNTOUCHED; District 6 row created for Cortez; no shared-district defect; new/relabeled UUIDs recorded for Plan 02
6. feedback_section_split_check SQL -> 0 rows for El Monte
7. no Cortez created; official_count not set; back-pointers not repaired (all Plan 02)
8. migration 1000 registered in schema_migrations AND committed to EV-Accounts
</verification>

<success_criteria>
All structural defects fixed and independently verified: geo_id backfilled (empty-string-safe); the duplicate
chamber merged via move-then-delete of Longoria + Ruedas (with an inline assert proving the doomed chamber was
empty before deletion); the council form of government corrected to El Monte's real by-district structure
(rows relabeled D1-D5 + District 6 created for Cortez); the directly-elected Mayor (Ancona, LOCAL_EXEC 'El Monte
Mayor') preserved exactly as-is (Lancaster/Pasadena model, NOT the Downey rotational collapse); migration 1000
idempotent, registered, and committed to EV-Accounts; El Monte is a single unambiguous 'City Council' chamber
with correctly-districted offices, ready for the Cortez creation + roster reconcile in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/151-el-monte-deep-seed/151-01-SUMMARY.md` when done (record the newly-created 'District 6' UUID, the relabeled district UUIDs D1-D5, Ancona's untouched LOCAL_EXEC district UUID, the Cortez absence/presence finding, and the confirmed migration numbers for Plan 02).
</output>
</content>
