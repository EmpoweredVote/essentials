---
context: between-phases
phase: 114-complete / 115-ready
task: 1
total_tasks: 2
status: paused
last_updated: 2026-06-12T17:49:13Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stance research parallelism | Running multiple stance research agents in parallel burns rate-limit quota and produces no usable output | blocking | Always run stance research ONE agent at a time; confirm each migration applied before starting the next rep |

<current_state>
Phase 114 (MA-STANCES-04) is fully closed as of 2026-06-12. All 5 plans complete, migrations 496–573 applied, 1778 combined stances (Phase 113 Wave 1 + Phase 114 Wave 2), Q1/Q2/Q3 pass, compass render approved on Marjorie Decker HD-81.

A compass UI fix was committed in the same session (commit 89cd742) targeting `src/components/CompassCard.jsx`. The fix has NOT been visually browser-tested yet. Phase 115 (MA Playbook Retrospective) is ready to execute.
</current_state>

<completed_work>

- Phase 114 Plan 01 — HD-81–HD-100 stances (migrations 496–515, 289 stances)
- Phase 114 Plan 02 — HD-101–HD-120 stances (migrations 516–535, 138 DB rows)
- Phase 114 Plan 03 — HD-121–HD-140 stances (migrations 536–555)
- Phase 114 Plan 04 — HD-141–HD-158 stances (migrations 556–573, 182 DB rows)
- Phase 114 Plan 05 — Phase-wide verification: Q1=78 rows, Q2=0 uncited, Q3=0 unpaired, Q4=1778 combined; compass APPROVED on Decker HD-81; MA-STANCES-04 FULLY CLOSED
- Compass UI fix (commit 89cd742): removed blocking overlay div from CompassCard.jsx, widened layout, added maxLabelLines={3} for 3-line label wrapping
</completed_work>

<remaining_work>

- **[Optional, non-blocking]** Browser-test compass UI fix at essentials.empowered.vote on a MA house rep profile:
  - Visit `/politician/marjorie-decker` (or any HD-81+ rep with stances)
  - Confirm: spoke inversion works on click, min/max buttons work, no label truncation on "Economic Development Incentives" / "Local Immigration Enforcement"
- **Phase 115 — MA Playbook Retrospective (MA-RETRO-01)**: Run `/gsd-execute-phase 115`
</remaining_work>

<decisions_made>

- **Compass overlay removal**: Removed transparent `position:absolute inset:0 zIndex:10` overlay div rather than raising its z-index. Root cause: overlay was the last DOM child in the container, so same-z-index stacking placed it on top of everything, absorbing all mouse clicks. Fix: moved `onMouseMove`/`onMouseLeave` to container div, raised button z-index to 11.
- **maxLabelLines={3}**: With 2 lines, "Economic Development Incentives" → "Development Incentives" on one line = 24 chars → estimatedWidth 403px > 220px maxPadding cap → SVG viewBox clipped the label. With 3 lines: longest line ≈ 11 chars → 185px, within cap.
- **Grid column widened**: `minmax(360px,500px)` → `minmax(440px,600px)` (both logged-in and guest views) to give the compass more breathing room alongside the Stance Breakdown column.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current position (Phase 115 ready, next migration 574)
2. `.planning/ROADMAP.md` — Phase 115 objectives (MA-RETRO-01)
3. `src/components/CompassCard.jsx` — the compass UI fix changes (commit 89cd742) if compass issues resurface

## Critical Anti-Patterns (do NOT repeat these)

- **PARALLEL STANCE RESEARCH**: Never run stance research agents in parallel. Each one must complete (research → write migration → apply → verify) before the next begins. Reason: Claude API rate limits are exhausted immediately with no usable output.
- **Neutral defaulting**: Never assign a value when no evidence exists. `D-01` rule: blank spoke is honest; defaulted value destroys trust.

## Infrastructure State

- **Supabase (mcp__supabase-local)**: IS the remote/production DB — all writes are live
- **Next migration**: 574 (last applied was 573 — Kate Donaghue stances)
- **Migration files location**: `C:/EV-Accounts/backend/migrations/` (NOT in essentials git repo, NOT a git repo itself — never run git commands there)
- **ev-ui**: GitHub repo; `C:\ev-ui\ev-ui-main` has no `.git` — changes require clone; CI auto-publishes on tag push
- **Essentials app**: `C:\Transparent Motivations\essentials` — git repo, active branch: main

<context>
Phase 114 (MA House Wave 2) finished cleanly. The compass UI fix was driven by three user-reported issues after Phase 114 completed:
1. Clicking spokes did nothing (blocked by transparent overlay)
2. Min/max buttons did nothing (same cause — overlay absorbed clicks)
3. Long topic names ("Economic Development Incentives", "Local Immigration Enforcement") were truncated (2-line wrapLabel produced lines too wide for SVG viewBox)

All three were fixed in a single CompassCard.jsx edit session. The fix is committed and ready for visual QA.

Phase 115 is the v13.0 retrospective — it closes the milestone by documenting COUSUB gotchas, Boston patterns, and lessons learned. It's purely documentation/planning work with no migrations.
</context>

<next_action>
Start with: `/gsd-execute-phase 115` to run the MA Playbook Retrospective. Optionally browser-test the compass UI at essentials.empowered.vote/politician/marjorie-decker first to confirm spoke inversion + min/max work.
</next_action>
