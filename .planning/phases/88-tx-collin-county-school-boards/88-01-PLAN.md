---
phase: 88-tx-collin-county-school-boards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts
  - C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql
autonomous: true
requirements:
  - TX-SCHOOL-01
  - TX-SCHOOL-02
  - TX-SCHOOL-03
  - TX-SCHOOL-04
  - TX-SCHOOL-05
tags:
  - tx
  - school-boards
  - tiger
  - geofences
  - migration

must_haves:
  truths:
    - "A Plano resident lookup returns Plano ISD board members via G5420 geofence"
    - "A McKinney resident lookup returns McKinney ISD board members via G5420 geofence"
    - "An Allen resident lookup returns Allen ISD board members via G5420 geofence"
    - "A Frisco resident lookup returns Frisco ISD board members via G5420 geofence"
    - "A Richardson resident lookup returns Richardson ISD board members via G5420 geofence"
    - "Section-split check returns 0 rows after migration 261 applies (no orphaned G5420 geofences)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts"
      provides: "TIGER UNSD loader for 5 TX ISDs (G5420)"
      contains: "TARGET_GEOIDS"
    - path: "C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql"
      provides: "Seed migration: 5 governments + 5 chambers + 5 SCHOOL districts + 35 politicians + 35 offices"
      contains: "Migration 261"
  key_links:
    - from: "essentials.geofence_boundaries (geo_id IN ('4835100','4829850','4807890','4820010','4837020'), mtfcc='G5420')"
      to: "essentials.districts (district_type='SCHOOL', state='tx', same geo_ids)"
      via: "geo_id match"
      pattern: "geo_id"
    - from: "essentials.offices.district_id"
      to: "essentials.districts.id (district_type='SCHOOL')"
      via: "office FK"
      pattern: "district_id"
    - from: "essentials.offices.chamber_id"
      to: "essentials.chambers (name='Board of Trustees', government_id = ISD government)"
      via: "chamber FK"
      pattern: "chamber_id"
---

<objective>
Load G5420 TIGER UNSD geofences for the 5 Collin County ISDs (Plano, McKinney, Allen, Frisco, Richardson), then seed government bodies, chambers, SCHOOL districts, politicians, and offices in a single atomic migration. After this plan executes, a TX resident lookup against any of the 5 ISD coordinates returns the correct school board members alongside city council and state officials — closing requirements TX-SCHOOL-01 through TX-SCHOOL-05 (per D-04, D-05, D-15).

Purpose: Phase 88 has no automated test framework — verification is PostGIS smoke queries. The structural seed must be atomic (loader runs first, then migration 261 with pre-flight gates ensures geofences exist) and idempotent (`ON CONFLICT`/`WHERE NOT EXISTS` guards everywhere).

Output:
- `C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts` (new, copy of `load-or-school-boundaries.ts` with 5 substitutions)
- `C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql` (new, copy of `257_ca_city_school_boards.sql` structure with TX rosters)
- 5 G5420 rows in `essentials.geofence_boundaries` (state='48', source='tiger_unsd_tx_2024')
- 5 governments + 5 chambers + 5 SCHOOL districts + 35 politicians + 35 offices in `essentials`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-tx-collin-county-school-boards/88-CONTEXT.md
@.planning/phases/88-tx-collin-county-school-boards/88-RESEARCH.md
@.planning/phases/88-tx-collin-county-school-boards/88-PATTERNS.md
@.planning/phases/88-tx-collin-county-school-boards/88-VALIDATION.md

# Canonical analogs (MUST READ before any code/SQL writing — copy exact structure):
@C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts
@C:/EV-Accounts/backend/migrations/257_ca_city_school_boards.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write load-tx-school-boundaries.ts (copy OR loader, swap to TX)</name>
  <files>C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts (canonical template — copy verbatim, change only constants listed below)
    - .planning/phases/88-tx-collin-county-school-boards/88-PATTERNS.md (line-by-line substitution map for this file)
    - .planning/phases/88-tx-collin-county-school-boards/88-RESEARCH.md (TIGER GEOIDs verification section)
  </read_first>
  <behavior>
    - Loader downloads `https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_48_unsd.zip` to a temp directory, extracts, opens the shapefile, filters features to the 5 target GEOIDs, and inserts each as a G5420 row into essentials.geofence_boundaries via PostGIS ST_ForcePolygonCCW + ST_SetSRID + ST_Force2D + ST_GeomFromGeoJSON, with `ON CONFLICT (geo_id, mtfcc) DO NOTHING` for idempotency.
    - `--dry-run` mode prints "Would insert" lines and does NOT write to DB.
    - Asserts all 5 target GEOIDs are found in the shapefile; exits with error if any are missing.
    - Post-insert verification COUNT against `state='48' AND mtfcc='G5420' AND source='tiger_unsd_tx_2024'` returns 5.
    - Diagnostic: logs the shapefile field names on the first feature and asserts 'GEOID' is present (Pitfall 6 guard).
  </behavior>
  <action>
    Per D-01, D-02, D-03 — write a new file at `C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts` by copying `C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts` verbatim and applying ONLY these substitutions (no other changes):

    1. **Header docstring (lines 1-19)** — swap "Oregon" / "or" / "41" references for "Texas" / "tx" / "48" and update example GEOID from `'4110040'` to `'4835100'`. Usage block becomes:
       `npx tsx scripts/load-tx-school-boundaries.ts --dry-run`
       `npx tsx scripts/load-tx-school-boundaries.ts`

    2. **Config block (lines 29-53)** — set exactly:
       - `TIGER_URL = 'https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_48_unsd.zip'`
       - `MTFCC = 'G5420'` (unchanged)
       - `STATE = '48'` (Texas FIPS)
       - `SOURCE = 'tiger_unsd_tx_2024'`
       - `EXPECTED_COUNT = 5`
       - `TARGET_GEOIDS = new Map<string, string>([['4835100','Plano Independent School District'],['4829850','McKinney Independent School District'],['4807890','Allen Independent School District'],['4820010','Frisco Independent School District'],['4837020','Richardson Independent School District']])`
       - `baseName = 'tl_2024_48_unsd'`
       - `tmpRoot = path.join(process.cwd(), '.tmp-tx-school-unsd')`

    3. **Console log labels** — change `[load-or-school-boundaries]` to `[load-tx-school-boundaries]` everywhere (3 occurrences: the startup log, the fatal catch, and the final "Done" log).

    4. **Main() entry log (line 121)** — change `'Fetching OR UNSD boundaries'` to `'Fetching TX UNSD boundaries'`.

    5. **Post-insert success log (line 252)** — change `'All ${EXPECTED_COUNT} OR school district boundaries loaded successfully.'` to `'All ${EXPECTED_COUNT} TX school district boundaries loaded successfully.'`.

    All other code copies verbatim: imports, `downloadWithRedirects`, `extractZip`, the shapefile open/read loop, the GEOID field assertion, the districtMap.size assertion against EXPECTED_COUNT, the `pool.query` PostGIS INSERT with the exact 8-parameter signature `(geo_id, ocd_id, name, state, mtfcc, geometry, source, imported_at)`, the inserted/skipped counters, and the `.finally(() => void pool.end())` cleanup.

    Do NOT add any new logic. Do NOT change error handling. Do NOT change the cleanup pattern. Do NOT add any "improvements". This is a copy with 5 constant substitutions.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsx scripts/load-tx-school-boundaries.ts --dry-run 2>&amp;1 | findstr /C:"All 5 target GEOIDs found"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts`
    - File contains literal string `TIGER_URL    = 'https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_48_unsd.zip'`
    - File contains literal string `STATE        = '48'`
    - File contains literal string `SOURCE       = 'tiger_unsd_tx_2024'`
    - File contains literal string `EXPECTED_COUNT = 5`
    - File contains literal string `'4835100', 'Plano Independent School District'`
    - File contains literal string `'4829850', 'McKinney Independent School District'`
    - File contains literal string `'4807890', 'Allen Independent School District'`
    - File contains literal string `'4820010', 'Frisco Independent School District'`
    - File contains literal string `'4837020', 'Richardson Independent School District'`
    - File contains literal string `baseName = 'tl_2024_48_unsd'`
    - File contains literal string `.tmp-tx-school-unsd`
    - File contains literal string `[load-tx-school-boundaries]` (does NOT contain `[load-or-school-boundaries]`)
    - Dry-run command from `C:/EV-Accounts/backend` directory exits 0 and logs "All 5 target GEOIDs found"
    - Dry-run shows 5 "[dry-run] Would insert" lines for the 5 expected GEOIDs
  </acceptance_criteria>
  <done>The loader file exists, dry-run succeeds without DB writes, shapefile contains all 5 target GEOIDs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run load-tx-school-boundaries.ts (live) — insert 5 G5420 rows</name>
  <files>essentials.geofence_boundaries (DB only — no source file modified)</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts (just authored in Task 1)
    - .planning/phases/88-tx-collin-county-school-boards/88-RESEARCH.md (Production DB Pre-Flight Results — confirms 0 existing G5420 rows for state='48')
  </read_first>
  <behavior>
    - Loader runs in live mode, downloads the TIGER zip (or uses cache), and inserts 5 G5420 rows into `essentials.geofence_boundaries`.
    - All 5 rows have `state='48'`, `mtfcc='G5420'`, `source='tiger_unsd_tx_2024'`, and a non-NULL PostGIS geometry.
    - Re-running the loader is safe (ON CONFLICT (geo_id, mtfcc) DO NOTHING).
  </behavior>
  <action>
    Per D-02 — execute the loader against production DB. Steps:

    1. Confirm working directory is `C:/EV-Accounts/backend` (NOT `C:/EV-Accounts`) — the dotenv lookup requires this exact cwd to find `.env` and `DATABASE_URL`.
    2. Confirm `DATABASE_URL` is set in the environment (loader exits with error otherwise).
    3. Run: `npx tsx scripts/load-tx-school-boundaries.ts` (no `--dry-run` flag).
    4. The loader logs each insert as `Inserted: <geoid> (<name>)` and the post-insert verification `All 5 TX school district boundaries loaded successfully.`
    5. If the run fails for any reason, do NOT manually edit the geofence_boundaries table — fix the loader, re-run.

    Per memory `feedback_no_git_in_ev_accounts.md`: do NOT run any git commands in `C:/EV-Accounts` or `C:/EV-Accounts/backend` — that directory is not a git repo and git traversal will hijack the parent essentials repo.
  </action>
  <verify>
    <automated>node -e "const {Pool}=require('pg');require('dotenv').config({path:'C:/EV-Accounts/backend/.env'});const p=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});p.query(\"SELECT COUNT(*)::int AS cnt FROM essentials.geofence_boundaries WHERE state='48' AND mtfcc='G5420' AND source='tiger_unsd_tx_2024'\").then(r=>{console.log('count='+r.rows[0].cnt);process.exit(r.rows[0].cnt===5?0:1)}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='48' AND mtfcc='G5420' AND source='tiger_unsd_tx_2024'` returns exactly 5
    - `SELECT geo_id FROM essentials.geofence_boundaries WHERE state='48' AND mtfcc='G5420'` returns exactly these 5 values: `4835100`, `4829850`, `4807890`, `4820010`, `4837020` (no extras, no missing)
    - `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id='4835100' AND mtfcc='G5420' AND geometry IS NOT NULL` returns 1
    - PostGIS ST_Covers query against Plano centroid (-96.6989, 33.0198) returns at least 1 G5420 row whose name matches `Plano Independent School District`
  </acceptance_criteria>
  <done>5 G5420 rows persisted with valid PostGIS geometry; Plano coordinate routing returns the Plano ISD geofence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write migration 261 — seed governments, chambers, SCHOOL districts, politicians, offices</name>
  <files>C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/257_ca_city_school_boards.sql (canonical template — exact 7-step structure)
    - .planning/phases/88-tx-collin-county-school-boards/88-PATTERNS.md (substitution map, Richardson hybrid block example)
    - .planning/phases/88-tx-collin-county-school-boards/88-RESEARCH.md (board rosters per ISD, External ID Block Allocation Plan, Common Pitfalls 1-7, Sample SQL blocks)
    - .planning/phases/88-tx-collin-county-school-boards/88-CONTEXT.md (D-05 through D-21 decisions)
  </read_first>
  <behavior>
    - Migration applies in a single transaction with `BEGIN; ... COMMIT;`.
    - 3 pre-flight DO blocks abort the migration if (a) government names already exist, (b) external_id block -880001..-880035 is not clear, (c) the 5 G5420 geofence_boundaries rows do not exist.
    - 5 government INSERTs use `WHERE NOT EXISTS` (per D-07 — `governments.state='TX'` uppercase).
    - 5 chamber INSERTs use `WHERE NOT EXISTS` and DO NOT include the `slug` column (slug is GENERATED ALWAYS — including it raises an error).
    - 5 district INSERTs set `district_type='SCHOOL'` (NOT `'SCHOOL_DISTRICT'`) and `state='tx'` (lowercase per D-07).
    - 35 politician+office WITH CTE blocks (one per board member) — `party=NULL`, `is_appointed=false`, `is_active=true`, `is_incumbent=true`, `is_vacant=false`; office uses `representing_state='TX'` uppercase, `is_appointed_position=false`.
    - Richardson ISD blocks (-880029..-880033) use office title `'Board Member, District [N]'` (N=1..5); Richardson blocks -880034..-880035 use `'Board Member, Place [N]'` (N=6..7). All other 28 blocks (Plano/McKinney/Allen/Frisco) use `'Board Member, Place [N]'`.
    - office_id back-fill UPDATE for `external_id BETWEEN -880035 AND -880001 AND office_id IS NULL`.
    - Post-verification DO block with 7 gates: govs=5, chambers=5, SCHOOL districts=5, politicians=35, offices=35, section-split=0, office_id back-fill=0 NULLs.
    - Ledger entry: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('261')`.
    - File is saved as UTF-8 (preserves `é` in `Debbie Rentería`).
  </behavior>
  <action>
    Per D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-16, D-21 — write `C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql` by copying the 7-step structure of `C:/EV-Accounts/backend/migrations/257_ca_city_school_boards.sql` and substituting the following:

    **State casing (per D-07):** `governments.state='TX'` (uppercase), `districts.state='tx'` (LOWERCASE), `offices.representing_state='TX'` (uppercase). Wrong casing breaks essentialsService routing (Pitfall 3).

    **Government names (5 rows, type='LOCAL', state='TX', city=NULL, geo_id=GEOID):**
    - `'Plano Independent School District, Texas, US'` (geo_id='4835100')
    - `'McKinney Independent School District, Texas, US'` (geo_id='4829850')
    - `'Allen Independent School District, Texas, US'` (geo_id='4807890')
    - `'Frisco Independent School District, Texas, US'` (geo_id='4820010')
    - `'Richardson Independent School District, Texas, US'` (geo_id='4837020')

    **Chambers (5 rows, name='Board of Trustees', NO slug column — slug is GENERATED ALWAYS):**
    - name_formal = `'[ISD Name] Board of Trustees'` for each (e.g. `'Plano Independent School District Board of Trustees'`).

    **Districts (5 rows, district_type='SCHOOL', state='tx', mtfcc='G5420'):**
    - One row per GEOID with label = name without trailing `, Texas, US` (e.g. `'Plano Independent School District'`).

    **Politicians + Offices (35 WITH CTE blocks, external_id allocation per Research §External ID Block Allocation Plan):**

    Plano ISD (-880001..-880007, title='Board Member, Place [N]'):
    - -880001 Dr. Lauren Tyra, Place 1
    - -880002 Sam Johnson, Place 2
    - -880003 Nancy Humphrey, Place 3
    - -880004 Michael Cook, Place 4
    - -880005 Tarrah Lantz, Place 5
    - -880006 Elisa Klein, Place 6
    - -880007 Katherine Goodwin, Place 7

    McKinney ISD (-880008..-880014, title='Board Member, Place [N]'):
    - -880008 Harvey Oaxaca, Place 1
    - -880009 Kenneth Ussery, Place 2
    - -880010 Corey Homer, Place 3
    - -880011 Roxane Morrison, Place 4
    - -880012 Lynn Sperry, Place 5
    - -880013 Stephanie O'Dell, Place 6
    - -880014 Amy Dankel, Place 7

    Allen ISD (-880015..-880021, title='Board Member, Place [N]'):
    - -880015 Sarah Mitchell, Place 1
    - -880016 Veronica Yost, Place 2
    - -880017 John Holley, Place 3
    - -880018 Becca Kinnear, Place 4
    - -880019 Amanda Campbell, Place 5
    - -880020 Dr. Polly Montgomery, Place 6
    - -880021 Bill Parker, Place 7

    Frisco ISD (-880022..-880028, title='Board Member, Place [N]'):
    - -880022 Suresh Manduva, Place 1
    - -880023 Renee Sample, Place 2
    - -880024 Stephanie Elad, Place 3
    - -880025 Dynette Davis, Place 4
    - -880026 Mark Hill, Place 5
    - -880027 Sherrie Salas, Place 6
    - -880028 Keith Maddox, Place 7

    Richardson ISD (-880029..-880035 — HYBRID per D-09 and Pitfall 1):
    - -880029 Megan Timme, title=`'Board Member, District 1'`
    - -880030 Vanessa Pacheco, title=`'Board Member, District 2'`
    - -880031 Debbie Rentería, title=`'Board Member, District 3'`  (UTF-8 é character)
    - -880032 Regina Harris, title=`'Board Member, District 4'`
    - -880033 Rachel McGowan, title=`'Board Member, District 5'`
    - -880034 Eric Eager, title=`'Board Member, Place 6'`
    - -880035 Chris Poteet, title=`'Board Member, Place 7'`

    For each politician, set: `party=NULL`, `is_active=true`, `is_appointed=false` (per D-10, D-11), `is_vacant=false`, `is_incumbent=true`. Split full_name into first_name/last_name fields. For "Dr. Lauren Tyra": first_name='Dr. Lauren', last_name='Tyra' (match Phase 87 convention for honorifics). For "Dr. Polly Montgomery": first_name='Dr. Polly', last_name='Montgomery'.

    For each office: `is_appointed_position=false` (per D-11), `representing_state='TX'`, `role_canonical=NULL`. The office WHERE clause must match the district by `d.geo_id = '<ISD geo_id>' AND d.district_type = 'SCHOOL' AND d.state = 'tx'` (Pitfall 3 — wrong casing breaks routing).

    **Pre-flight DO blocks (per Research §Migration 261 Pre-flight):**
    1. Government names not already present (raises exception if any of the 5 names exist).
    2. external_id block -880001..-880035 is clear (raises exception if any row exists in range).
    3. 5 G5420 geofence_boundaries rows exist for the 5 GEOIDs (raises exception if count != 5 — loader must run first).

    **Post-verification DO block (per D-21 + Research §Post-Verification Gate, 7 gates):**
    - (a) 5 government rows with the 5 expected names
    - (b) 5 'Board of Trustees' chambers under those governments
    - (c) 5 SCHOOL districts with `state='tx'` and the 5 expected GEOIDs
    - (d) 35 politicians in external_id range -880035..-880001
    - (e) 35 offices linked to SCHOOL districts (`district_id IN (SELECT id FROM essentials.districts WHERE district_type='SCHOOL' AND state='tx' AND geo_id IN ('4835100','4829850','4807890','4820010','4837020'))`)
    - (f) Section-split check (per `feedback_section_split_check.md` and D-21): for each of the 5 GEOIDs, a SCHOOL districts row exists — count of orphans = 0.
    - (g) office_id back-fill: 0 NULL `office_id` values among the 35 politicians.

    **Coverage gap comment (per D-16):** Add header comment block:
    `-- Coverage gap note (D-16): Residents in smaller Collin County ISDs (Prosper, Wylie, Celina, Lovejoy, Princeton, etc.) will not see a SCHOOL section in Phase 88. A future phase could add these if needed.`

    **Header CRITICAL list (per Patterns §File header comment):**
    - slug is GENERATED ALWAYS on essentials.chambers — never include in INSERT column list
    - essentials.governments has NO unique constraint on geo_id — use WHERE NOT EXISTS guard
    - districts.state must be 'tx' (lowercase) to match routing queries
    - governments.state = 'TX' (uppercase). offices.representing_state = 'TX' (uppercase)
    - district_type='SCHOOL' (NOT 'SCHOOL_DISTRICT')
    - All 5 G5420 geofence_boundaries rows must exist before this migration (run loader first)
    - party=NULL on all 35 politicians (antipartisan)
    - is_appointed=false, is_appointed_position=false on all (elected board members)
    - Save this file as UTF-8 to preserve é character (Debbie Rentería — Richardson ISD)
    - Richardson ISD uses hybrid seat structure — Districts 1-5 + At-Large Places 6-7. Use 'Board Member, District [N]' for Districts 1-5 and 'Board Member, Place [N]' for At-Large 6-7.

    **Ledger entry (final statement before COMMIT):**
    `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('261') ON CONFLICT (version) DO NOTHING;`

    **File encoding:** Save the SQL file as UTF-8 (no BOM). The é in `Debbie Rentería` MUST be preserved. Verify by checking that the byte sequence `0xC3 0xA9` appears in the file (UTF-8 encoding of é).

    Do NOT apply the migration in this task — Task 4 applies it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql','utf8');const needles=['Migration 261','Plano Independent School District, Texas, US','McKinney Independent School District, Texas, US','Allen Independent School District, Texas, US','Frisco Independent School District, Texas, US','Richardson Independent School District, Texas, US','district_type','SCHOOL','state='tx'','external_id BETWEEN -880035 AND -880001','-880001','-880035','Debbie Rentería','Board Member, District 1','Board Member, District 5','Board Member, Place 6','Board Member, Place 7','Board of Trustees','VALUES ('261')'];const missing=needles.filter(n=>!c.includes(n));if(missing.length){console.error('MISSING:',missing);process.exit(1)}if(c.includes('SCHOOL_DISTRICT')){console.error('FAIL: contains forbidden token SCHOOL_DISTRICT');process.exit(1)}if(/INSERT INTO essentials\.chambers[^;]*\bslug\b/i.test(c)){console.error('FAIL: chambers INSERT includes forbidden slug column');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql`
    - File contains literal `Migration 261`
    - File contains all 5 ISD government names (e.g. `Plano Independent School District, Texas, US`)
    - File contains literal `state='tx'` (lowercase districts.state)
    - File contains literal `state, city, geo_id` after `INSERT INTO essentials.governments` with `'TX'` (uppercase) as the state value
    - File contains literal `representing_state` followed by `'TX'` (uppercase)
    - File contains literal `'SCHOOL'` for district_type
    - File does NOT contain literal `'SCHOOL_DISTRICT'` (forbidden — would break routing)
    - File contains literal `external_id BETWEEN -880035 AND -880001` (back-fill UPDATE and post-verification gate)
    - File contains literal `-880001` and `-880035` (block endpoints)
    - File contains literal `Debbie Rentería` (UTF-8 é preserved — byte check 0xC3 0xA9)
    - File contains literal `Board Member, District 1` and `Board Member, District 5` (Richardson hybrid districts)
    - File contains literal `Board Member, Place 6` and `Board Member, Place 7` (Richardson at-large)
    - File contains literal `Board of Trustees` (chamber name)
    - File contains literal `VALUES ('261')` (ledger entry)
    - File does NOT include the column `slug` in any `INSERT INTO essentials.chambers` statement (slug is GENERATED ALWAYS — including it raises error)
    - All 5 pre-flight checks present (government names, external_id block, geofence existence)
    - 7-gate post-verification DO block present
  </acceptance_criteria>
  <done>Migration 261 SQL file authored with all 5 ISDs, 35 board members, Richardson hybrid titles, UTF-8 encoded, pre-flight + post-verification gates in place. Not yet applied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Apply migration 261 to production DB + run section-split smoke verification</name>
  <files>essentials.governments, essentials.chambers, essentials.districts, essentials.politicians, essentials.offices (DB only)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql (authored in Task 3)
    - .planning/phases/88-tx-collin-county-school-boards/88-VALIDATION.md (Per-Task Verification Map — 5 PostGIS smoke queries)
    - .planning/phases/88-tx-collin-county-school-boards/88-RESEARCH.md (Suggested Test Coordinates table)
  </read_first>
  <behavior>
    - Migration 261 applies cleanly via the Supabase MCP server (mcp__supabase-local IS production per memory `feedback_supabase_local_is_remote.md`).
    - All 3 pre-flight DO blocks pass (governments names clear, external_id block clear, 5 geofences exist).
    - All 7 post-verification gates pass: 5 govs, 5 chambers, 5 SCHOOL districts, 35 politicians, 35 offices, section-split=0, 0 NULL office_id.
    - After apply, 5 PostGIS coordinate smoke queries (per VALIDATION.md) return the correct ISD name for each of Plano/McKinney/Allen/Frisco/Richardson centroids.
    - `supabase_migrations.schema_migrations` ledger has a row with `version='261'`.
  </behavior>
  <action>
    Per D-04, D-21:

    1. Apply migration 261 via `mcp__supabase-local__apply_migration` (the Supabase MCP server — production DB per memory). Pass the migration file's full SQL content. Use migration name `261_tx_collin_county_school_boards`.
    2. If the apply call surfaces any pre-flight `RAISE EXCEPTION` (e.g. "geofence rows not found" or "external_id block not clear"), do NOT bypass — go back to Task 1/2 and fix the underlying cause. Do NOT modify the SQL to skip pre-flights.
    3. After apply succeeds, run the 7 verification queries below directly against the DB (psql via node `pg` client OR via mcp__supabase-local execute_sql):
       - `SELECT COUNT(*) FROM essentials.governments WHERE name IN ('Plano Independent School District, Texas, US','McKinney Independent School District, Texas, US','Allen Independent School District, Texas, US','Frisco Independent School District, Texas, US','Richardson Independent School District, Texas, US')` returns 5
       - `SELECT COUNT(*) FROM essentials.chambers c JOIN essentials.governments g ON c.government_id=g.id WHERE c.name='Board of Trustees' AND g.geo_id IN ('4835100','4829850','4807890','4820010','4837020')` returns 5
       - `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='tx' AND geo_id IN ('4835100','4829850','4807890','4820010','4837020')` returns 5
       - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -880035 AND -880001` returns 35
       - `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id BETWEEN -880035 AND -880001` returns 35
       - Section-split check: `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('4835100','4829850','4807890','4820010','4837020') AND gb.mtfcc='G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.district_type='SCHOOL' AND d.state='tx')` returns 0
       - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -880035 AND -880001 AND office_id IS NULL` returns 0
    4. Run the 5 PostGIS coordinate smoke queries (per 88-VALIDATION.md Per-Task Verification Map and 88-RESEARCH.md §Suggested Test Coordinates):
       - Plano: `SELECT gb.name FROM essentials.geofence_boundaries gb WHERE ST_Covers(gb.geometry, ST_SetSRID(ST_Point(-96.6989, 33.0198), 4326)) AND gb.mtfcc='G5420'` returns `Plano Independent School District`
       - McKinney: same query with coords (-96.6155, 33.1976) returns `McKinney Independent School District`
       - Allen: (-96.6706, 33.1032) returns `Allen Independent School District`
       - Frisco: (-96.8236, 33.1501) returns `Frisco Independent School District`
       - Richardson: (-96.7298, 32.9482) returns `Richardson Independent School District`
    5. Verify ledger entry: `SELECT version FROM supabase_migrations.schema_migrations WHERE version='261'` returns 1 row.

    Per memory `feedback_no_git_in_ev_accounts.md`: do NOT run git in C:/EV-Accounts.
  </action>
  <verify>
    <automated>node -e "const {Pool}=require('pg');require('dotenv').config({path:'C:/EV-Accounts/backend/.env'});const p=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});(async()=>{const q=async(sql,exp)=>{const r=await p.query(sql);const got=parseInt(r.rows[0].n||r.rows[0].cnt||r.rows[0].count,10);if(got!==exp){console.error('FAIL '+sql+' expected '+exp+' got '+got);process.exit(1)}};await q(\"SELECT COUNT(*)::int AS n FROM essentials.governments WHERE name IN ('Plano Independent School District, Texas, US','McKinney Independent School District, Texas, US','Allen Independent School District, Texas, US','Frisco Independent School District, Texas, US','Richardson Independent School District, Texas, US')\",5);await q(\"SELECT COUNT(*)::int AS n FROM essentials.districts WHERE district_type='SCHOOL' AND state='tx' AND geo_id IN ('4835100','4829850','4807890','4820010','4837020')\",5);await q(\"SELECT COUNT(*)::int AS n FROM essentials.politicians WHERE external_id BETWEEN -880035 AND -880001\",35);await q(\"SELECT COUNT(*)::int AS n FROM essentials.offices o JOIN essentials.politicians pp ON o.politician_id=pp.id WHERE pp.external_id BETWEEN -880035 AND -880001\",35);await q(\"SELECT COUNT(*)::int AS n FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('4835100','4829850','4807890','4820010','4837020') AND gb.mtfcc='G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.district_type='SCHOOL' AND d.state='tx')\",0);await q(\"SELECT COUNT(*)::int AS n FROM essentials.politicians WHERE external_id BETWEEN -880035 AND -880001 AND office_id IS NULL\",0);await q(\"SELECT COUNT(*)::int AS n FROM supabase_migrations.schema_migrations WHERE version='261'\",1);const plano=await p.query(\"SELECT name FROM essentials.geofence_boundaries WHERE ST_Covers(geometry, ST_SetSRID(ST_Point(-96.6989, 33.0198), 4326)) AND mtfcc='G5420'\");if(!plano.rows.some(r=>r.name==='Plano Independent School District')){console.error('FAIL: Plano coord did not route to Plano ISD');process.exit(1)}console.log('ALL GATES PASS');process.exit(0)})().catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Migration 261 applied successfully via mcp__supabase-local (no exceptions raised by pre-flight or post-verification DO blocks)
    - `SELECT COUNT(*) FROM essentials.governments WHERE name IN (5 ISD names)` returns 5
    - `SELECT COUNT(*) FROM essentials.chambers WHERE name='Board of Trustees' AND government_id IN (5 ISD govs)` returns 5
    - `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='tx' AND geo_id IN ('4835100','4829850','4807890','4820010','4837020')` returns 5
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -880035 AND -880001` returns 35
    - `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id BETWEEN -880035 AND -880001` returns 35
    - Section-split check returns 0 rows
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -880035 AND -880001 AND office_id IS NULL` returns 0
    - All 5 PostGIS coordinate smoke queries route correctly: Plano (-96.6989, 33.0198) → Plano ISD; McKinney (-96.6155, 33.1976) → McKinney ISD; Allen (-96.6706, 33.1032) → Allen ISD; Frisco (-96.8236, 33.1501) → Frisco ISD; Richardson (-96.7298, 32.9482) → Richardson ISD
    - `SELECT version FROM supabase_migrations.schema_migrations WHERE version='261'` returns 1 row
    - Richardson ISD office title spot-check: `SELECT title FROM essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id=-880031` returns `'Board Member, District 3'` (Debbie Rentería)
    - Richardson ISD office title spot-check: `SELECT title FROM essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id=-880035` returns `'Board Member, Place 7'` (Chris Poteet)
  </acceptance_criteria>
  <done>All 5 TX ISDs seeded, all 7 verification gates pass, all 5 coordinate smoke queries route correctly, ledger entry present, requirements TX-SCHOOL-01 through TX-SCHOOL-05 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov → loader → DB | Loader downloads TIGER zip from census.gov (HTTPS) and writes parsed geometry to DB. Source is the U.S. Census Bureau — trusted public data; no user input crosses this boundary. |
| Migration file → DB | SQL applied via Supabase MCP server. No user input; pre-flight DO blocks abort on inconsistent state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Tampering | TIGER zip download integrity | accept | census.gov is HTTPS; risk is negligible (would require government source compromise). No signing layer exists in any prior phase; consistent with established loader pattern (load-or-school-boundaries.ts, load-state-tiger-boundaries.ts). |
| T-88-02 | Repudiation | Migration ledger | mitigate | `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('261')` provides audit trail of who applied what; pre-flight DO block prevents accidental re-runs. |
| T-88-03 | Information Disclosure | Board member PII | accept | All seeded data (names, office titles, ISD affiliation) is public record from official ISD websites — no PII beyond what is publicly published. party=NULL per antipartisan design (D-10). |
| T-88-04 | Denial of Service | Migration locks production DB | mitigate | Migration runs in a single transaction with bounded scope (5 governments + 35 politicians + 35 offices); typical apply time < 5s. No long-running ops. |
| T-88-05 | Elevation of Privilege | Loader DB credentials | mitigate | DATABASE_URL is the same Supabase service-role connection used by all v6.0+ loaders. No new privilege grants; loader uses INSERT-only on geofence_boundaries with `ON CONFLICT DO NOTHING`. |
</threat_model>

<verification>
After all 4 tasks complete:

1. `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5420' AND state='48'` returns 5
2. `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='tx' AND geo_id IN ('4835100','4829850','4807890','4820010','4837020')` returns 5
3. `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -880035 AND -880001` returns 35
4. Section-split check returns 0
5. All 5 PostGIS coordinate smoke queries (per 88-VALIDATION.md) route to the correct ISD
6. `supabase_migrations.schema_migrations` has version='261'
7. Richardson ISD office titles include both `'Board Member, District [1..5]'` and `'Board Member, Place [6..7]'` (hybrid structure correctly applied)
</verification>

<success_criteria>
- TX-SCHOOL-01: Plano ISD G5420 geofence + 7-member board seeded → CONFIRMED via Plano coord PostGIS smoke
- TX-SCHOOL-02: McKinney ISD G5420 geofence + 7-member board seeded → CONFIRMED via McKinney coord PostGIS smoke
- TX-SCHOOL-03: Allen ISD G5420 geofence + 7-member board seeded → CONFIRMED via Allen coord PostGIS smoke
- TX-SCHOOL-04: Frisco ISD G5420 geofence + 7-member board seeded → CONFIRMED via Frisco coord PostGIS smoke
- TX-SCHOOL-05: Richardson ISD G5420 geofence + 7-member board (hybrid Districts 1-5 + Places 6-7) seeded → CONFIRMED via Richardson coord PostGIS smoke + office title spot-checks
</success_criteria>

<output>
Create `.planning/phases/88-tx-collin-county-school-boards/88-01-SUMMARY.md` when done. Include:
- Migration 261 application status + timestamp
- Counts: 5 govs, 5 chambers, 5 SCHOOL districts, 35 politicians, 35 offices
- PostGIS coordinate smoke results (5 ISDs)
- Any assumption-log adjustments made during execution (e.g. Plano Place 4-7 confirmed names, McKinney Place 4 Roxane Morrison confirmed/corrected, Allen ISD roster confirmed/corrected per Pitfall 2 + Pitfall 7)
- Frisco ISD post-May-2026 roster confirmation (per Research Open Question 3)
- File: `C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts` (new)
- File: `C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql` (new)
- Next migration after this plan: 262 (headshots audit-only — Plan 02)
</output>
