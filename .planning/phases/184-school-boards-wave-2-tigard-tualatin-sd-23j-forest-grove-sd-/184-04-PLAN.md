---
phase: 184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd
plan: 04
type: execute
wave: 4
depends_on: [184-03]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WSCH-03, WSCH-04, WSCH-05]
user_setup: []

must_haves:
  truths:
    - "src/lib/coverage.js COVERAGE_SCHOOL_DISTRICTS carries three new PLAIN entries appended after the existing Hillsboro entry: { label: 'Tigard-Tualatin School District 23J', browseGeoId: '4112240', browseMtfcc: 'G5420', browseStateAbbrev: 'OR' }, { label: 'Forest Grove School District 15', browseGeoId: '4105160', browseMtfcc: 'G5420', browseStateAbbrev: 'OR' }, { label: 'Sherwood School District 88J', browseGeoId: '4111290', browseMtfcc: 'G5420', browseStateAbbrev: 'OR' } — NO hasContext key on any (0-stances-by-design is the honest state per [[feedback_school_districts_search_only]] + the Phase 173 CCSD chip-reconciliation lesson)"
    - "The existing CCSD + Beaverton + Hillsboro school-district entries and every other array in coverage.js are preserved unchanged; the file still parses (node import) and the three new entries flow into ALL_COVERAGE_AREAS as kind:'school district' automatically (no other file edited)"
    - "The frontend change is committed to THIS repo and pushed; the Render deploy is verified by grepping the DEPLOYED bundle CONTENT for the three new labels (never by comparing a build hash — Render hash != local hash is a known false-negative)"
    - "Full E2E SQL/HTTP gate suite passes: TTSD 5 offices on geo_id 4112240 SCHOOL/or; FGSD 5 offices on geo_id 4105160 SCHOOL/or; SSD 5 offices on geo_id 4111290 SCHOOL/or; casing 'or' only; 0 section-split for all 3 G5420 geo_ids; 0 offices with NULL chamber_id (WR-02 gate); headshot count = success count from plan 03; 0 compass stance rows for all 15 (success state); smoke-or-westmetro-school.ts passes"
    - "Live browse verified via Playwright on essentials.empowered.vote: /results?browse_geo_id=4112240&browse_mtfcc=G5420 shows all 5 TTSD directors with the 'Tigard-Tualatin School District 23J' card heading and their headshots + Chair(P4 Zurschmeide)/Vice-Chair(P3 Irvin) labels; /results?browse_geo_id=4105160&browse_mtfcc=G5420 shows all 5 FGSD directors with the 'Forest Grove School District 15' heading (Chair P5 Kottkey / Vice-Chair P3 Lozano); /results?browse_geo_id=4111290&browse_mtfcc=G5420 shows all 5 SSD directors with the 'Sherwood School District 88J' heading (Board Chair P1 Carson / Board Vice Chair P3 Hawkins) — no 6th row, no student rep"
    - "The card subtitle renders the correct district name via the getAccordionKey SCHOOL government_name fallback (no frontend code change needed — confirmed working live for both Wave-1 boards); each district chip renders PLAIN (no purple stance-count chip)"
    - "Address routing (SC-equivalent): a Tigard/Tualatin address returns the TTSD board, a Forest Grove address returns the FGSD board, a Sherwood address returns the SSD board (confirmed via the smoke test's coordinates and/or a live address lookup)"
    - "Human-verify checkpoint confirms correct-person headshots, exactly 5 directors per board (no student reps), plain chips, the district-name headings, and the Linda Harrington gap render (no broken image / no 'Coming Soon' placeholder) on the live deployed app"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "COVERAGE_SCHOOL_DISTRICTS with the three OR school-district entries appended (plain, no hasContext); CCSD + Beaverton + Hillsboro entries + all other arrays preserved"
      contains: "Tigard-Tualatin School District 23J"
  key_links:
    - from: "src/lib/coverage.js COVERAGE_SCHOOL_DISTRICTS"
      to: "ALL_COVERAGE_AREAS (search typeahead)"
      via: "COVERAGE_SCHOOL_DISTRICTS.map((d) => ({ ...d, kind: 'school district' }))"
      pattern: "COVERAGE_SCHOOL_DISTRICTS"
    - from: "browse /results?browse_geo_id=4112240&browse_mtfcc=G5420"
      to: "the 5 TTSD directors"
      via: "backend ST_Covers on the G5420 geofence → SCHOOL district → offices"
      pattern: "4112240|4105160|4111290"
---

<objective>
Surface all three school districts as plain search-only coverage entries, ship the frontend change, and
run the full same-session E2E live verification: SQL/HTTP gate suite, Render deploy verification by bundle
content, Playwright browse of all three G5420 links, and a human-verify checkpoint.

Purpose: Satisfy the address-routing + coverage-surfacing halves of WSCH-03/04/05 and close the phase. Per
D-B1 there is NO banner work and NO buildingImages.js edit; per [[feedback_school_districts_search_only]] +
the Phase 173 CCSD lesson the entries carry NO hasContext (0-stances-by-design is honest). The frontend
delta this phase is coverage.js only. The card subtitle already renders the correct district name via the
getAccordionKey SCHOOL fallback for existing PPS/Wave-1 data — no frontend code change is needed beyond the
coverage.js append.
Output: the coverage.js edit + a recorded full-E2E pass + live-browse confirmation, closing WSCH-03 +
WSCH-04 + WSCH-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-RESEARCH.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-03-SUMMARY.md
@src/lib/coverage.js

**EXECUTION ARCHITECTURE:** The gsd-executor EDITS src/lib/coverage.js only (this frontend repo — normal
git). It has NO database access, NO Supabase MCP, and does NOT push/deploy. The INLINE ORCHESTRATOR runs
the E2E SQL/HTTP gates via `psql`/`curl`, commits + pushes THIS repo (AFTER a `git fetch` + rebase onto
origin/main — a parallel Oregon workstream is active; never push a divergent branch), verifies the Render
deploy by grepping the DEPLOYED bundle CONTENT for the three new labels (never a hash), drives the
Playwright browse, and runs the human-verify checkpoint. C:/EV-Accounts is a SEPARATE repo and is NOT
touched in this plan.

**VERIFY-GATE HYGIENE / TAILWIND:** no raw Windows backslash paths in any committed file (Tailwind v4
scans .planning/*.md and a raw `\` path crashes the prod build — [[feedback_tailwind_scans_planning_md]]).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the three OR school-district entries to coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the COVERAGE_SCHOOL_DISTRICTS array ~line 253 with the existing CCSD + Beaverton + Hillsboro entries — the exact template; and ALL_COVERAGE_AREAS ~line 275 that consumes it)
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md §"src/lib/coverage.js" (the exact append shape, no hasContext — shows the current 3-entry state and the target 6-entry state)
  </read_first>
  <action>
    Append three entries to the COVERAGE_SCHOOL_DISTRICTS array, immediately after the existing Hillsboro
    School District 1J entry, matching that entry's shape EXACTLY (object key order label, browseGeoId,
    browseMtfcc, browseStateAbbrev) and adding NO hasContext key on any:
    { label: 'Tigard-Tualatin School District 23J', browseGeoId: '4112240', browseMtfcc: 'G5420',
    browseStateAbbrev: 'OR' } then { label: 'Forest Grove School District 15', browseGeoId: '4105160',
    browseMtfcc: 'G5420', browseStateAbbrev: 'OR' } then { label: 'Sherwood School District 88J',
    browseGeoId: '4111290', browseMtfcc: 'G5420', browseStateAbbrev: 'OR' }. Do NOT touch the CCSD /
    Beaverton / Hillsboro entries, the COVERAGE_STATES / COVERAGE_COUNTIES / COVERAGE_BROWSE_STATES arrays,
    or ALL_COVERAGE_AREAS. Keep the file's existing quoting/indentation style. No fenced code in this action
    — the existing school-district entries in the file are the source of truth for the shape.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && node -e "import('./src/lib/coverage.js').then(m => { const d = m.COVERAGE_SCHOOL_DISTRICTS; const ids = ['4112240','4105160','4111290']; const found = ids.map(id => d.find(x => x.browseGeoId === id)); if (found.some(x => !x)) { console.error('missing entries'); process.exit(1); } if (found.some(x => 'hasContext' in x)) { console.error('hasContext must be absent'); process.exit(1); } if (found.some(x => x.browseMtfcc !== 'G5420' || x.browseStateAbbrev !== 'OR')) { console.error('bad mtfcc/state'); process.exit(1); } console.log('coverage.js OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE_SCHOOL_DISTRICTS contains the TTSD ('4112240'), FGSD ('4105160'), and SSD ('4111290') entries, all browseMtfcc 'G5420', browseStateAbbrev 'OR'
    - None of the three new entries has a hasContext key (the node verify asserts this)
    - The existing CCSD + Beaverton + Hillsboro entries are unchanged; node import parses the file and prints "coverage.js OK"
    - No other array in coverage.js is modified; no raw Windows backslash path introduced
  </acceptance_criteria>
  <done>coverage.js has the three plain OR school-district entries appended; node import verify passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs full E2E, ships the frontend, verifies live browse, and confirms on the deployed app</name>
  <what-built>The coverage.js edit + the fully-seeded trio of school-district boards. The orchestrator now runs the full E2E gate suite, pushes the frontend, verifies the Render deploy by bundle content, drives Playwright, and confirms the live render.</what-built>
  <how-to-verify>
    A. Full E2E SQL/HTTP gate suite (via psql on DATABASE_URL from C:/EV-Accounts/backend/.env + curl):
       1. TTSD offices = 5: `... WHERE d.geo_id='4112240' AND d.district_type='SCHOOL' AND d.state='or'`.
       2. FGSD offices = 5: same with geo_id='4105160'.
       3. SSD offices = 5: same with geo_id='4111290'.
       4. Casing: `SELECT DISTINCT state FROM essentials.districts WHERE geo_id IN
          ('4112240','4105160','4111290') AND district_type='SCHOOL'` → only 'or'.
       5. Section-split = 0 for all three G5420 geo_ids; WR-02 gate: 0 offices with NULL chamber_id across
          the three geo_ids.
       6. Headshot count = the plan-03 SUCCESS count: `SELECT COUNT(*) FROM essentials.politician_images
          pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4112245
          AND -4112241 OR p.external_id BETWEEN -4105165 AND -4105161 OR p.external_id BETWEEN -4111295 AND
          -4111291` (target 15, or 14 if the Harrington gap stands).
       7. 0-stance success state: same JOIN against inform.politician_answers for the 15 ext_ids → 0.
       8. Routing: `cd C:/EV-Accounts/backend && npx tsx scripts/smoke-or-westmetro-school.ts` passes.
    B. Ship the frontend (THIS repo only): `git fetch` + rebase onto origin/main FIRST (parallel Oregon
       workstream active — avoid divergence), then commit the coverage.js change and push. After Render
       builds, verify the deploy by grepping the DEPLOYED bundle CONTENT for the new labels (fetch the live
       JS bundle and grep for "Tigard-Tualatin School District 23J" / "Forest Grove School District 15" /
       "Sherwood School District 88J") — do NOT compare a build hash (Render hash != local hash is a known
       false-negative).
    C. Playwright live browse on essentials.empowered.vote:
       - /results?browse_geo_id=4112240&browse_mtfcc=G5420 → 5 TTSD directors under a "Tigard-Tualatin
         School District 23J" heading, each with a headshot; confirm Zurschmeide shows "(Chair)" and Irvin
         "(Vice Chair)"; NO 6th row.
       - /results?browse_geo_id=4105160&browse_mtfcc=G5420 → 5 FGSD directors under a "Forest Grove School
         District 15" heading; confirm Kottkey "(Chair)" and Lozano "(Vice Chair)"; NO student-rep row;
         Linda Harrington (Position 4) renders without a broken image or a "Coming Soon" placeholder (either
         a real local-news photo or a clean no-image state per the plan-03 gap decision).
       - /results?browse_geo_id=4111290&browse_mtfcc=G5420 → 5 SSD directors under a "Sherwood School
         District 88J" heading; confirm Carson "Board Chair" and Hawkins "Board Vice Chair" render.
       - Confirm each district's coverage chip renders PLAIN (no purple stance-count chip).
    D. Address routing: confirm a Tigard/Tualatin address returns the TTSD board, a Forest Grove address the
       FGSD board, and a Sherwood address the SSD board (the smoke test's coordinates satisfy this; a live
       address lookup is a stronger confirmation if quick).
  </how-to-verify>
  <acceptance_criteria>
    - All E2E gates pass: 5/5/5 offices, casing 'or' only, 0 section-split all three geo_ids, 0 NULL chamber_id, headshot count = plan-03 success count, 0 stance rows, smoke test passes
    - coverage.js committed + pushed (after fetch/rebase); deploy verified by grepping the live bundle CONTENT for all three new labels (not by hash)
    - Playwright confirms 5 TTSD + 5 FGSD + 5 SSD directors under the correct headings with Chair/Vice-Chair labels and headshots; no student-rep rows; Harrington renders cleanly (no placeholder); all three chips plain (no purple)
    - A Tigard/Tualatin address routes to TTSD, a Forest Grove address to FGSD, a Sherwood address to SSD
    - Human confirms correct-person headshots and the exactly-5-per-board render on the live app
  </acceptance_criteria>
  <resume-signal>Type "approved" with the E2E + deploy + live-browse results, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only E2E gate SELECTs against production Supabase |
| operator → git/Render | Committing + pushing the frontend repo (after fetch/rebase) and verifying the public deploy |
| public → deployed app | Anonymous browse of the three G5420 district links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-17 | Tampering | a raw Windows backslash path committed in a .planning/*.md or coverage.js crashes the Tailwind v4 prod build | mitigate | Verify-gate hygiene: no raw `\` paths in committed files; the node import verify + deploy-content grep catch a broken build |
| T-184-18 | Spoofing | trusting a build-hash match that is a Render false-negative (hash != local) | mitigate | Deploy verification greps the DEPLOYED bundle CONTENT for the three new labels, never a hash |
| T-184-19 | Tampering (data integrity) | a stray hasContext key would render a false purple stance chip for a 0-stance board | mitigate | The node verify asserts hasContext is absent on all three new entries; Playwright confirms the plain chip |
| T-184-20 | Tampering | pushing a divergent branch on top of the parallel Oregon workstream's origin commits | mitigate | `git fetch` + rebase onto origin/main before the push; coordination note from the Accounts session |
| T-184-21 | Information Disclosure | E2E gate SELECTs read production data | accept | Read-only public-official data; no PII beyond published board rosters |
| T-184-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — Playwright/tsx already present; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Full E2E gate suite green (offices 5/5/5, casing 'or', 0 section-split, 0 NULL chamber_id, headshot count, 0 stances, routing).
- Deploy verified by bundle content; all three chips render plain (no hasContext).
- Playwright confirms all three boards render correctly (5 each, correct headings + Chair/Vice-Chair, no student reps, Harrington clean).
</verification>

<success_criteria>
- All three districts listed as plain search-only coverage entries (no hasContext); frontend shipped +
  deploy verified by content; a Tigard/Forest Grove/Sherwood address routes to the correct board; all three
  boards render 5 directors with headshots + district-name headings on the live app; 0 stance rows
  confirmed. WSCH-03 + WSCH-04 + WSCH-05 satisfied end-to-end.
</success_criteria>

<output>
Create `.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-04-SUMMARY.md`
when done, recording: the coverage.js entries added, the full E2E gate results, the deploy-content
verification, the Playwright live-browse confirmation for all three G5420 links (5 directors each, correct
headings, Chair/Vice-Chair, no student reps, Harrington gap render, plain chips), and the browse-link URLs
for the operator: essentials.empowered.vote/results?browse_geo_id=4112240&browse_mtfcc=G5420 ,
...browse_geo_id=4105160&browse_mtfcc=G5420 , and ...browse_geo_id=4111290&browse_mtfcc=G5420.
</output>
