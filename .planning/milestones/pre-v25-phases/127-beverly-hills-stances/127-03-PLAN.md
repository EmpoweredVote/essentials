---
phase: 127-beverly-hills-stances
plan: 03
type: execute
wave: 3
depends_on: [127-02]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [BEVHILLS-01]
must_haves:
  truths:
    - "Phase-wide Q1 query returns per-person stance counts for all 5 Beverly Hills target officials"
    - "Phase-wide Q2 (uncited contexts) returns 0 across all 5 Beverly Hills officials"
    - "Phase-wide Q3 (unpaired answers) returns 0 across all 5 Beverly Hills officials"
    - "Phase-wide Q4 (dead topic check, using ct.topic_key not ct.slug) returns 0 rows — no stances on retired topics"
    - "Phase-wide Q5 (Fisher safety check) returns 0 — Howard Fisher has no stance rows"
    - "At least 3 of 5 officials have >= 1 stance row (BEVHILLS-01 success criterion 1)"
    - "Compass render checkpoint passes on at least one Beverly Hills official's profile page"
    - "BEVHILLS-01 is marked complete in REQUIREMENTS.md and ROADMAP.md"
    - "STATE.md reflects Phase 127 complete with next migration updated to NNN+5"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BEVHILLS-01 marked satisfied"
      contains: "BEVHILLS-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 127 marked complete"
      contains: "Phase 127"
    - path: ".planning/STATE.md"
      provides: "Current position updated to Phase 128 ready, next migration NNN+5"
      contains: "next migration"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "phase-wide Q2/Q3 citation checks (all 5 BH officials)"
      pattern: "array_length\\(sources,1\\)"
---

<objective>
Run phase-wide closure verification across all 5 Beverly Hills officials (plus a Fisher exclusion safety check), confirm compass rendering on at least one official's profile, then update REQUIREMENTS.md, ROADMAP.md, and STATE.md to mark BEVHILLS-01 complete.

Purpose: Confirm 100% citation rate across all 5 stance files, confirm Fisher received zero stances, close BEVHILLS-01, and prepare STATE.md for Phase 128 (Carson Stances).
Output: Phase 127 fully closed; planning documents updated and committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/127-beverly-hills-stances/127-02-SUMMARY.md
@.planning/phases/127-beverly-hills-stances/127-01-SUMMARY.md

Beverly Hills target officials (all 5; non-contiguous external_ids — use IN lists, NOT BETWEEN):
  -200589 Lester Friedman (Mayor — directly elected, LOCAL_EXEC)
  -201153 John A. Mirisch (Council Member)
  -201154 Craig A. Corman (Council Member)
  -201155 Mary N. Wells (Council Member)
  -700010 Sharona R. Nazarian (Council Member)

EXCLUDED: -700011 Howard Fisher (City Treasurer) — must have ZERO stance rows (Q5 safety check).

UUIDs: resolved in Plan 01 pre-flight and recorded in 127-01-SUMMARY.md.
Migration range: NNN through NNN+4 (NNN confirmed in 127-01-SUMMARY.md; expected 714 → 714-718).
Next migration after phase = NNN+5 (expected 719).
Beverly Hills geo_id = 0606308.

Apply path: psql CLI with DATABASE_URL from C:/EV-Accounts/.env. mcp__supabase-local NOT callable in bash executor context (Phase 126). Do NOT run git commands in C:/EV-Accounts. The git commit in Task 2 covers .planning/ files only (this repo).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-wide closure verification (Q1–Q5 queries + compass render checkpoint)</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/127-beverly-hills-stances/127-02-SUMMARY.md (Mirisch + Nazarian + Wells stance counts)
    - .planning/phases/127-beverly-hills-stances/127-01-SUMMARY.md (Friedman + Corman counts + all 5 UUIDs + Fisher exclusion UUID + confirmed NNN)
    - .planning/phases/127-beverly-hills-stances/127-RESEARCH.md (Phase-Wide Closure Verification section with full Q1-Q5 query text + Pitfall 6 ct.topic_key + Pitfall 7 integer-cast filter)
    - C:/EV-Accounts/.env (read DATABASE_URL for psql CLI)
  </read_first>
  <action>
    Load DATABASE_URL from C:/EV-Accounts/.env, then run all 5 phase-wide SQL verification queries via `psql "$DATABASE_URL" -c "<query>"`, then verify compass rendering on at least one Beverly Hills official's profile.

    Q1 — Stance count per official (all 5 BH target officials):
    `SELECT p.full_name, p.external_id, COUNT(pa.topic_id) AS stances FROM essentials.politicians p LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id WHERE p.external_id IN (-200589, -201153, -201154, -201155, -700010) GROUP BY p.full_name, p.external_id ORDER BY p.external_id;`
    Expected: 5 rows, one per official. At least 3 of 5 must have count >= 1 (BEVHILLS-01 success criterion 1). Count may be 0 for a blank-spoke official — that is correct per evidence-only rule, but no more than 2 zero-count officials is expected given Beverly Hills' press footprint.

    Q2 — Uncited rows (must return 0):
    `SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-200589, -201153, -201154, -201155, -700010)) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0);`
    If non-zero: identify the specific unsourced rows via `SELECT pc.politician_id, pc.topic_id FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-200589, -201153, -201154, -201155, -700010)) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0);`, remediate by adding source URLs, and re-run Q2 before proceeding to Task 2.

    Q3 — Unpaired answers (must return 0):
    `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-200589, -201153, -201154, -201155, -700010)) AND pc.politician_id IS NULL;`
    If non-zero: identify the specific unpaired rows via `SELECT pa.politician_id, pa.topic_id FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-200589, -201153, -201154, -201155, -700010)) AND pc.politician_id IS NULL;`, insert the missing context rows, and re-run Q3.

    Q4 — Dead topic check (must return 0 rows — no stances on retired/inactive topics). CRITICAL: use ct.topic_key, NOT ct.slug (Pitfall 6 — slug does not exist on inform.compass_topics):
    `SELECT pa.topic_id, ct.topic_key, ct.is_active, COUNT(*) as rows FROM inform.politician_answers pa JOIN inform.compass_topics ct ON ct.id = pa.topic_id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-200589, -201153, -201154, -201155, -700010)) AND ct.is_active = false GROUP BY pa.topic_id, ct.topic_key, ct.is_active;`
    Expected: 0 rows. If any rows returned: these are stances on retired topics that must be deleted before closing.

    Q5 — Fisher exclusion safety check (must return 0 — Howard Fisher must have NO stance rows):
    `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -700011);`
    Expected: 0. If non-zero: a Fisher stance was written in error — DELETE those rows (`DELETE FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -700011); DELETE FROM inform.politician_context WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -700011);`) and re-run Q5 before proceeding.

    Compass render checkpoint: navigate to the Beverly Hills official with the most stances (determined from Q1 results — likely Mirisch or Friedman) on essentials.empowered.vote and confirm the compass chart renders with spokes for the evidenced topics. If no spokes appear despite stances in DB, note the issue in SUMMARY but do not block phase closure (DB data integrity Q1-Q5 is the hard gate).

    Record all query results in SUMMARY. Do not proceed to Task 2 if Q2, Q3, Q4, or Q5 return non-zero, or if fewer than 3 officials have >= 1 stance.
  </action>
  <verify>
    <automated>Q2 = 0 (uncited contexts). Q3 = 0 (unpaired answers). Q4 = 0 rows (no stances on inactive topics). Q5 = 0 (Fisher has no stance rows). Q1 shows >= 3 officials with >= 1 stance. Any failing result must be remediated before Task 2.</automated>
  </verify>
  <acceptance_criteria>
    - Q1 returns exactly 5 rows (one per BH target official); counts recorded in SUMMARY
    - Q1 shows at least 3 of 5 officials with stance count >= 1 (BEVHILLS-01 success criterion 1)
    - Q2 returns 0 (zero uncited context rows across all 5 officials)
    - Q3 returns 0 (zero unpaired answer rows across all 5 officials)
    - Q4 returns 0 rows (zero stances on inactive topics; query used ct.topic_key not ct.slug)
    - Q5 returns 0 (Howard Fisher has zero stance rows — exclusion confirmed)
    - Compass render verified on at least one BH official profile at essentials.empowered.vote; result documented in SUMMARY
    - Any non-zero results from Q2/Q3/Q4/Q5 were remediated before proceeding
    - Total stance count across all 5 officials recorded in SUMMARY
  </acceptance_criteria>
  <done>Phase-wide verification complete; Q2=0, Q3=0, Q4=0, Q5=0; >= 3 officials have stances; compass render verified; all counts recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Mark BEVHILLS-01 complete; update REQUIREMENTS.md, ROADMAP.md, STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find BEVHILLS-01 entry + Traceability table row — read the full section before editing)
    - .planning/ROADMAP.md (find Phase 127 entry in v15.0 section + the v15.0 Phases checklist + Progress Table — read before editing)
    - .planning/STATE.md (read Current Position + v15.0 Roadmap Summary + Decisions section before editing)
    - .planning/phases/126-alhambra-stances/126-03-SUMMARY.md (reference format for how Phase 126 closed its requirement)
  </read_first>
  <action>
    Update three planning documents in order. Read each file fully before editing.

    (1) REQUIREMENTS.md:
    - Find the BEVHILLS-01 entry under "## BEVHILLS — Beverly Hills Stances". Change `- [ ] **BEVHILLS-01:**` to `- [x] **BEVHILLS-01:**`. Do not alter any other requirement entries.
    - Find the Traceability table row `| BEVHILLS-01 | 127 | ⬜ |` and change `⬜` to `✅`.

    (2) ROADMAP.md, in the "# v15.0 LA City Stances" section:
    - In the "## Phases" checklist, change the Phase 127 line to: `- [x] **Phase 127: Beverly Hills Stances** — Mayor + 4 council (Treasurer excluded; [N] stance rows; BEVHILLS-01 closed [today's date])`
    - Under the "### Phase 127: Beverly Hills Stances" detail section, add a Plans block (mirroring how Phase 126's detail section lists its plans):
      **Plans:** 3 plans
      Plans:
      - [x] 127-01-PLAN.md — Wave 0 pre-flight + Lester Friedman (Mayor) + Craig A. Corman stances (migrations NNN through NNN+1)
      - [x] 127-02-PLAN.md — John A. Mirisch + Sharona R. Nazarian + Mary N. Wells stances (migrations NNN+2 through NNN+4)
      - [x] 127-03-PLAN.md — Phase-wide closure verification + close BEVHILLS-01
    - In the "## Progress Table" at the end of the v15.0 section, update the Phase 127 row to: `| 127. Beverly Hills Stances | 3/3 | Complete | [today's date] |`

    (3) STATE.md:
    - Update the "## v15.0 Roadmap Summary" table row for Phase 127 status from "Not started" to "Complete ([today's date])".
    - Update `## Current Position` to:
      - Phase: 128 — Carson Stances (not yet started)
      - Plan: —
      - Status: Not started
      - Last activity: [today's date] — Phase 127 Beverly Hills Stances complete; BEVHILLS-01 closed; [N] total stance rows across 5 officials; Fisher excluded (0 rows)
      - Next migration: NNN+5 (NNN confirmed in 127-01-SUMMARY.md; expected 719)
    - Update the "## Key v15.0 Facts" line "Next migration: 708" to "Next migration: NNN+5".
    - Add a Decisions entry for Phase 127 in the `## Decisions` section summarizing the phase outcome (confirmed NNN, total rows, per-official counts, blank-spoke count, Fisher exclusion confirmed, directly-elected-Mayor handling for Friedman, next migration NNN+5).

    Then commit the three planning files plus the 127-03-SUMMARY.md (and 127-01/127-02 SUMMARYs if not already committed) with the message:
    `docs(phase-127): Phase 127 Beverly Hills Stances complete — BEVHILLS-01 closed`

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

    (Run git only in the .planning repo working directory — NEVER in C:/EV-Accounts.)
  </action>
  <verify>
    <automated>Read REQUIREMENTS.md — BEVHILLS-01 entry shows `[x]` and Traceability row shows ✅. Read ROADMAP.md — Phase 127 shows complete with 3 plans listed and Progress Table row = Complete. Read STATE.md — next migration is NNN+5; Current Position is Phase 128; last activity updated to today's date.</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md: BEVHILLS-01 marked satisfied (`[x]`); Traceability table row for BEVHILLS-01 shows ✅
    - ROADMAP.md: Phase 127 checklist item marked `[x]`; Phase 127 detail section lists 3 plans all checked; Progress Table row 127 = Complete
    - STATE.md: v15.0 Roadmap Summary Phase 127 row = Complete; Current Position = Phase 128; next migration = NNN+5; Key v15.0 Facts next-migration line updated to NNN+5
    - STATE.md Decisions section includes a Phase 127 summary entry (total rows, per-official counts, Fisher exclusion confirmed, Friedman directly-elected handling, next migration NNN+5)
    - Git commit created covering REQUIREMENTS.md, ROADMAP.md, STATE.md, and the 127 SUMMARY files (committed only in the .planning repo, never C:/EV-Accounts)
  </acceptance_criteria>
  <done>BEVHILLS-01 marked complete in all planning documents; STATE.md updated to next migration NNN+5 and Phase 128 ready; commit created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification queries → production DB | Read-only queries against live Supabase via psql CLI; no write risk in Task 1 except remediations if Q2/Q3/Q4/Q5 non-zero |
| planning document edits → git | Updates to REQUIREMENTS.md / ROADMAP.md / STATE.md committed to the .planning repo (never C:/EV-Accounts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Tampering | stance value integrity | mitigate | Q2/Q3/Q4/Q5 must all return 0 before documents are updated; any non-zero result requires remediation first |
| T-127-02 | Information Disclosure | reasoning / sources | accept | Public-record positions; no PII |
| T-127-03 | Repudiation | requirement closure | mitigate | Closure requires Q2=0 + Q3=0 + Q4=0 + Q5=0 + >=3 officials with stances (verified in Task 1) before marking BEVHILLS-01 satisfied |
| T-127-04 | Tampering | Fisher (-700011) stance leakage | mitigate | Q5 safety check asserts Fisher has zero rows; remediation deletes any erroneous Fisher rows before closure |
| T-127-05 | Tampering | premature requirement closure | mitigate | Task 2 is explicitly blocked on Task 1 completing with all zero results |
| T-127-07 | Tampering | git run in wrong repo (C:/EV-Accounts) | mitigate | Commit instruction explicitly restricts git to the .planning repo; C:/EV-Accounts is not a git repo and git must never run there |
| T-127-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
Final phase-wide check results (all must pass before closing):
- Q2 (uncited contexts, all 5 officials) = 0
- Q3 (unpaired answers, all 5 officials) = 0
- Q4 (stances on inactive topics, using ct.topic_key) = 0 rows
- Q5 (Fisher stance rows) = 0
- Q1 shows >= 3 of 5 officials with >= 1 stance
- Compass render confirmed on at least one BH official profile
</verification>

<success_criteria>
- Q1: per-official stance counts for all 5 BH officials recorded; >= 3 have >= 1 stance
- Q2 = 0 (zero uncited context rows)
- Q3 = 0 (zero unpaired answer rows)
- Q4 = 0 rows (zero stances on inactive topics; ct.topic_key used)
- Q5 = 0 (Fisher exclusion confirmed)
- Compass render verified on at least one BH official at essentials.empowered.vote
- BEVHILLS-01 marked satisfied in REQUIREMENTS.md (entry + Traceability table)
- Phase 127 marked complete in ROADMAP.md with 3 plans listed and Progress Table updated
- STATE.md next migration = NNN+5; Current Position = Phase 128; last activity + Decisions updated
- Git commit created (in .planning repo only)
</success_criteria>

<output>
Create `.planning/phases/127-beverly-hills-stances/127-03-SUMMARY.md` when done. Record: Q1 per-official stance counts, Q2/Q3/Q4/Q5 results, total stance rows across all 5 officials, blank-spoke officials (names + reason), Fisher exclusion confirmation (Q5=0), compass render result, confirmed NNN+5 next migration value, and BEVHILLS-01 closure confirmation.
</output>
