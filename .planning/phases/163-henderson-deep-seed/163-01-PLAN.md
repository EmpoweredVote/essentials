---
phase: 163-henderson-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-henderson-ward-boundaries.ts
  - C:/EV-Accounts/backend/migrations/1084_henderson_city_council.sql
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [CLARK-03]
user_setup: []

must_haves:
  truths:
    - "Wave-0 BLOCKING probes confirm (before any write): live on-disk migration MAX (highest file +1, trust on-disk over schema_migrations); external_id block -3206001..-3206005 has 0 existing rows; the chosen ward MTFCC (X0016, or next unclaimed X-code) has 0 existing geofence_boundaries rows; Henderson G4110 geo_id='3231900' + its geofence_boundaries casing (state='32' FIPS) [D-04b]"
    - "A Wave-0 operator checkpoint verifies the live roster + seat count (Mayor Romero + Ward I Seebock / Ward II Larson / Ward III Cox / Ward IV Stewart = 5 seats) before the structural migration applies"
    - "4 Henderson ward boundary polygons load into essentials.geofence_boundaries as X0016 rows (state='nv' lowercase, geo_id='henderson-nv-council-ward-1'..'-4'), each ST_IsValid=true; Ward III (4 rings) handled via ST_Multi(ST_MakeValid(...)) [D-01]"
    - "A standalone government 'City of Henderson, Nevada, US' exists (type='City', state='NV' uppercase, geo_id='3231900'), NOT nested under State of Nevada geo_id='32' [D-04]"
    - "A chamber 'Henderson City Council' (official_count=5) exists under that government [D-03]"
    - "Mayor Michelle Romero (external_id -3206001) has a LOCAL_EXEC office on the city-wide G4110 district (geo_id='3231900'), title='Mayor', directly elected NOT rotational [D-03]"
    - "Each of the 4 ward council members has a LOCAL office on its per-ward X0016 district (geo_id='henderson-nv-council-ward-N'), title='Council Member, Ward I..IV' (Roman numerals), external_id block -3206002..-3206005 [D-01]"
    - "Post-verify: exactly 1 LOCAL_EXEC office + exactly 4 LOCAL ward offices; office_id back-filled for all 5; zero section-split; Carrie Cox is_active=true is_incumbent=true (seated despite June 2026 primary loss)"
    - "City of Henderson surfaces with the purple hasContext chip in src/lib/coverage.js COVERAGE_STATES (appended to the EXISTING Nevada block — LV already present) [CLARK-03 SC#4]"
    - "If Wave-0 cannot source clean ward polygons OR confirms pure at-large, fall back to single-CITY-district model (all members on the one Henderson G4110 geofence, ward-labeled), document ward-precise routing as deferred — phase still completes [D-01b]"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-henderson-ward-boundaries.ts"
      provides: "ArcGIS-JSON ward-polygon loader: fetch 4 wards from maps.cityofhenderson.com OpenDataAdministrativeBoundaries MapServer Layer 2 (f=json, outSR=4326, resultRecordCount=100), convert rings→GeoJSON Polygon, INSERT geofence_boundaries as X0016 with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON(...),4326)), assert ST_IsValid, EXPECTED_COUNT=4"
      contains: "X0016"
    - path: "C:/EV-Accounts/backend/migrations/1084_henderson_city_council.sql"
      provides: "Structural migration: pre-flight (asserts 4 X0016 geofences) + 1 government + 1 chamber + 1 LOCAL_EXEC district (Mayor) + 4 LOCAL ward districts + 5 politician+office CTEs + office_id back-fill + post-verify DO block; registers as 1084 (or Wave-0-confirmed number)"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 200
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "COVERAGE_STATES Nevada block with Henderson appended (browseGovernmentList:['3231900'], browseStateAbbrev:'NV', hasContext:true), LV entry preserved"
      contains: "Henderson"
  key_links:
    - from: "essentials.geofence_boundaries (4 X0016 ward polygons)"
      to: "essentials.districts (4 LOCAL ward rows, geo_id='henderson-nv-council-ward-N')"
      via: "shared geo_id; backend ST_Covers PIP + essentialsService.ts line 646 X% catchall routes X0016 to LOCAL (no backend edit)"
      pattern: "henderson-nv-council-ward-"
    - from: "essentials.chambers (Henderson City Council)"
      to: "essentials.governments (City of Henderson, Nevada, US, geo_id='3231900')"
      via: "government_id subquery WHERE name='City of Henderson, Nevada, US'"
      pattern: "City of Henderson, Nevada, US"
    - from: "essentials.offices (Mayor + 4 ward members)"
      to: "essentials.districts (1 LOCAL_EXEC at geo_id='3231900' + 4 LOCAL at X0016 wards, all state='nv')"
      via: "district join on geo_id + district_type + state='nv' lowercase"
      pattern: "state = 'nv'"
    - from: "src/lib/coverage.js COVERAGE_STATES Nevada block"
      to: "Henderson browse (geo_id 3231900)"
      via: "browseGovernmentList array entry"
      pattern: "3231900"
---

<objective>
Seed the City of Henderson structurally and make its 4 wards routable, mirroring the proven Phase 162 (Las Vegas) shape. First a Wave-0 BLOCKING probe confirms the live migration MAX (trust highest on-disk file +1, not just the registered ledger — v18.0-park drift previously pushed numbers past schema_migrations), the unused external_id block -3206001..-3206005, X0016 availability (or the next unclaimed X-code), and the Henderson G4110 city geo_id='3231900' + its geofence casing. A Wave-0 operator checkpoint then verifies the live roster + seat count (5: Mayor Romero + Ward I Seebock / Ward II Larson / Ward III Cox / Ward IV Stewart) against cityofhenderson.com BEFORE any migration applies. Next, load the 4 Henderson ward boundary polygons from the Henderson ArcGIS MapServer into essentials.geofence_boundaries as X0016 rows (D-01). Then create a standalone government "City of Henderson, Nevada, US" (NOT nested under State of Nevada geo_id='32' — D-04), one chamber "Henderson City Council" (official_count=5 — D-03), one LOCAL_EXEC district for the directly-elected Mayor (Michelle Romero) on the existing city-wide G4110 geofence (geo_id='3231900'), four LOCAL ward districts on the X0016 ward polygons, all 5 politician+office rows (Roman numeral ward titles), and back-fill office_id. Finally append Henderson to the EXISTING Nevada block in src/lib/coverage.js COVERAGE_STATES (LV already present).

Purpose: Satisfies the structural half of CLARK-03 (Mayor + ward-routed council via the 4 ward polygons) plus the surfacing half (SC#4 — purple hasContext chip). Headshots are Plan 02; stances + final E2E verification are Plan 03.
Output: load-henderson-ward-boundaries.ts, 1084_henderson_city_council.sql (structural, registered), the coverage.js NV-block edit, plus the captured Wave-0 probe facts + the 5 external_id→UUID map recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and NO DB access):**
- The executor WRITES load-henderson-ward-boundaries.ts, the migration .sql, and the coverage.js edit to disk and documents the exact run/apply steps. The executor does NOT run the loader, run DB SELECTs, or apply migrations.
- The Wave-0 DB probes, the roster operator checkpoint, running the ward loader (`npx tsx scripts/load-henderson-ward-boundaries.ts` from the EV-Accounts repo root so it reads C:/EV-Accounts/backend/.env DATABASE_URL), applying mig 1084 (via `psql -f` using the EV-Accounts/backend/.env DATABASE_URL, or mcp__supabase-local__apply_migration), and every post-apply audit SELECT are INLINE ORCHESTRATOR steps performed by execute-phase.
- Each task below marks which steps are executor-write vs inline-orchestrator-apply.
- The loader + migration live in the SEPARATE repo C:/EV-Accounts/backend/ (commit via `git -C "C:/EV-Accounts"`, always from the repo root — never cd into backend). The coverage.js edit lives in the essentials repo. SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/163-henderson-deep-seed/163-RESEARCH.md
@.planning/phases/163-henderson-deep-seed/163-PATTERNS.md
@.planning/phases/163-henderson-deep-seed/163-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
Henderson city geo_id (TIGER G4110 place): '3231900' (confirmed Phase 158 smoke test SC2)
Government: name='City of Henderson, Nevada, US', type='City', state='NV' UPPERCASE, city=NULL, geo_id='3231900' (standalone, NOT under State of Nevada geo_id='32'). governments has NO unique constraint on geo_id or name — WHERE NOT EXISTS guard mandatory.
Chamber: name='Henderson City Council', name_formal='Henderson City Council', official_count=5
Ward geofence source: https://maps.cityofhenderson.com/arcgis/rest/services/public/OpenDataAdministrativeBoundaries/MapServer/2/query?where=1%3D1&outFields=WARD,WARDNAME,COUNCILMAN&returnGeometry=true&f=json&outSR=4326&resultOffset=0&resultRecordCount=100 (ArcGIS JSON rings, NOT GeoJSON; outSR=4326 mandatory — default CRS is projected; resultRecordCount=100 MANDATORY — without it only 3 of 4 wards return)
Ward attribute parsing: WARD is esriFieldTypeString "1".."4" (parseInt, NOT integer like LV); WARDNAME = "WARD I".."WARD IV" (use as geofence display name); COUNCILMAN informational only
Ward geofence rows: geofence_boundaries (id, geo_id, mtfcc, state, name, geometry, source); mtfcc='X0016' (Wave-0 confirms unclaimed; use next X-code if taken), state='nv' LOWERCASE, geo_id='henderson-nv-council-ward-N' (N=1..4, Arabic), source='cityofhenderson.com-arcgis-opendata-admin-boundaries-ward-2026'; ON CONFLICT (geo_id, mtfcc) DO NOTHING
Ward ring counts (confirmed live): Ward 1=1, Ward 2=1, Ward 3=4 rings, Ward 4=1 — wrap with ST_Multi; assert ST_IsValid=true in RETURNING (apply ST_MakeValid if false)
EXPECTED_COUNT=4 (Henderson has 4 wards; LV had 6)
Mayor district: 1 LOCAL_EXEC row, state='nv' lowercase, geo_id='3231900', label='City of Henderson', mtfcc='G4110' (uses EXISTING Phase 158 G4110 geofence; idempotent guard only — no new geofence row)
Ward districts: 4 LOCAL rows, state='nv' lowercase, geo_id='henderson-nv-council-ward-N', label='Ward I'..'Ward IV' (Roman numerals — official naming), mtfcc='X0016'
Office district join keys: Mayor → d.geo_id='3231900' AND d.district_type='LOCAL_EXEC' AND d.state='nv'; ward N → d.geo_id='henderson-nv-council-ward-N' AND d.district_type='LOCAL' AND d.state='nv' (uppercase 'NV' matches ZERO rows — silent no-op, the #1 failure mode)
offices.representing_state: 'NV' UPPERCASE (free-text label column, NOT the district join key)
Office NOT EXISTS guard: (district_id, politician_id) — one person per office
chambers.slug is GENERATED ALWAYS — never include the auto-generated path column in INSERT column list; name_formal must NOT be ''
Roster + external_ids (party='Non-Partisan' all — antipartisan, never displayed): -3206001 Michelle Romero (Mayor, directly elected, is_appointed=false); -3206002 Jim Seebock (Ward I); -3206003 Monica Larson (Ward II, is_appointed=false elected Nov 2024); -3206004 Carrie Cox (Ward III, is_active=true is_incumbent=true — seated despite June 2026 primary loss, leaves Nov 2026); -3206005 Dan H. Stewart (Ward IV)
Migration: structural number 1084 (on-disk MAX confirmed 1083); registers in the ledger. Wave-0 re-verifies the live on-disk MAX and uses highest-file +1.
Backend routing: X0016 caught automatically by essentialsService.ts line 646 X% catchall (X0016 NOT in exclusion list X0001-X0004) — NO backend code change
Grep-gate forbidden tokens in .sql comments: paraphrase 'slug' (→ "the auto-generated path column"), 'photo_origin_url' (→ "the removed image-origin column"); the ledger table token is acceptable ONLY in the actual registration INSERT, not in comments
Browse link: essentials.empowered.vote/results?browse_geo_id=3231900&browse_mtfcc=G4110
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave-0 BLOCKING DB probes + roster/seat operator checkpoint (inline orchestrator)</name>
  <read_first>
    - .planning/phases/163-henderson-deep-seed/163-PATTERNS.md (section "Wave-0 DB Probes Required Before Any Migration Applies" — the 4 exact probe queries)
    - .planning/phases/163-henderson-deep-seed/163-RESEARCH.md (§External_id Collision Analysis; §Custom MTFCC Registry; §Migration Counter; §Henderson City geo_id; Wave 0 Gaps)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-01-SUMMARY.md (the LV Wave-0 drift lesson — on-disk MAX vs registered ledger)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor — these need DB/filesystem access). Run all four BLOCKING probes and the roster checkpoint BEFORE the executor's ward-loader/migration tasks proceed. Record every result in the SUMMARY.
(a) Live on-disk migration MAX: `ls C:/EV-Accounts/backend/migrations | tail` — trust the highest on-disk file +1 as the structural migration number (expected 1083 → 1084). ALSO run `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` (expected lower — only structural migs register). If the on-disk MAX differs from 1083, renumber the structural migration (1084) and ALL downstream audit-only migrations (Plan 02 headshots, Plan 03 stances) by the same offset and note it.
(b) external_id collision probe: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -3206005 AND -3206001` — expect 0. If non-zero, pick the next unused -3206xxx block and propagate to every downstream file.
(c) MTFCC existence probe: `SELECT DISTINCT mtfcc FROM essentials.geofence_boundaries WHERE mtfcc LIKE 'X%' ORDER BY mtfcc` — confirm X0016 is unclaimed (X0015 = LV). If X0016 is taken, use the next unclaimed X-code and propagate to the loader + migration + post-verify.
(d) Henderson G4110 geo_id + casing: `SELECT geo_id, state FROM essentials.geofence_boundaries WHERE name ILIKE '%Henderson%' AND mtfcc='G4110'` — expect geo_id='3231900', state='32' (TIGER FIPS string). Confirms the Mayor/single-city-fallback attach point exists.
Then the ROSTER OPERATOR CHECKPOINT: verify the live Henderson roster + seat count against cityofhenderson.com (or current news/Ballotpedia) — Mayor Michelle Romero + Ward I Jim Seebock + Ward II Monica Larson + Ward III Carrie Cox + Ward IV Dan H. Stewart = 5 seats. Confirm Cox remains the seated Ward III incumbent (lost the June 2026 primary but serves until the Nov 2026 general). Pause for operator confirmation before the migration applies. If the roster has shifted, adjust names/ext_ids before writing the migration.
This task GATES the structural migration (Task 3): do not apply mig 1084 until all four probes pass and the roster is operator-confirmed.
  </action>
  <verify><human-check>Operator confirms: on-disk migration MAX recorded (structural number = MAX+1); external_id block -3206001..-3206005 returns 0 rows; the ward MTFCC (X0016 or next unclaimed) returns 0 rows; Henderson G4110 geo_id='3231900' state='32' confirmed; and the 5-seat roster (Romero + Seebock/Larson/Cox/Stewart) verified against the live city source with Cox still seated.</human-check></verify>
  <acceptance_criteria>
    - On-disk migration MAX recorded in SUMMARY; structural number set to MAX+1 (expected 1084); any drift offset applied to all downstream files.
    - external_id probe recorded = 0 for -3206005..-3206001 (or a new block chosen + propagated).
    - MTFCC probe recorded: X0016 unclaimed (or next X-code chosen + propagated to loader/migration/post-verify).
    - G4110 probe recorded: geo_id='3231900', state='32'.
    - Roster + seat count operator-verified against the live city source = 5 seats; Cox confirmed still seated.
    - This checkpoint is recorded as passed BEFORE mig 1084 applies.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded probe values + roster confirmation, or describe any collision (pick a new block/X-code), drift (renumber), or roster change (adjust names/ext_ids).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write + run the Henderson ward-boundary loader (X0016, 4 wards, D-01b fallback)</name>
  <files>C:/EV-Accounts/backend/scripts/load-henderson-ward-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts (EXACT ANALOG — copy nearly verbatim; only the constants block + ward-attribute extraction change)
    - .planning/phases/163-henderson-deep-seed/163-PATTERNS.md (section "scripts/load-henderson-ward-boundaries.ts" — the constants block, the string-cast WARD parsing, the INSERT pattern, the summary verify statement)
    - .planning/phases/163-henderson-deep-seed/163-RESEARCH.md (§Ward Geofence Source — endpoint + attribute schema; Pitfall 1 pagination; Pitfall 2 string WARD; Pitfall 6 multi-ring Ward III)
  </read_first>
  <action>
EXECUTOR STEP: write `load-henderson-ward-boundaries.ts` by adapting `load-lv-ward-boundaries.ts`. Copy verbatim: imports + Pool, fetchJson, arcgisRingsToGeoJson, the INSERT-with-ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON(...),4326)) + ON CONFLICT (geo_id, mtfcc) DO NOTHING + ST_IsValid RETURNING + ST_MakeValid fallback, the error handler. Replace the constants block with: HENDERSON_WARD_URL = the maps.cityofhenderson.com OpenDataAdministrativeBoundaries MapServer Layer 2 query URL (where=1%3D1, outFields=WARD,WARDNAME,COUNCILMAN, returnGeometry=true, f=json, outSR=4326, resultOffset=0, resultRecordCount=100 — resultRecordCount=100 is MANDATORY or only 3 of 4 wards return); MTFCC='X0016' (or the Wave-0-confirmed code); STATE_CODE='nv'; SOURCE='cityofhenderson.com-arcgis-opendata-admin-boundaries-ward-2026'; GEO_ID_PREFIX='henderson-nv-council-ward-'; EXPECTED_COUNT=4. Adapt the ward-attribute extraction: WARD is a STRING in Henderson — use parseInt(String(rawWard ?? ''),10), guard isNaN + range 1..EXPECTED_COUNT; use WARDNAME ("WARD I".."WARD IV") as the geofence display name; geo_id uses Arabic numerals (henderson-nv-council-ward-1..4). Adapt the summary log to expect (4, true). Support --dry-run. Comment the resultRecordCount/outSR/f=json criticality inline.

INLINE ORCHESTRATOR STEP: run `npx tsx scripts/load-henderson-ward-boundaries.ts` from the C:/EV-Accounts repo root (so it reads backend/.env DATABASE_URL). Confirm it reports 4 wards inserted, all ST_IsValid=true. Then verify with `SELECT COUNT(*), bool_and(public.ST_IsValid(geometry)) FROM essentials.geofence_boundaries WHERE state='nv' AND mtfcc='X0016'` → expect (4, true). Record in SUMMARY.

D-01b FALLBACK (do NOT hard-block): if the loader cannot source clean ward polygons (endpoint down, <4 valid features after ST_MakeValid) OR Wave-0 confirmed Henderson elects purely at-large, SKIP the X0016 ward geofences and proceed with the single-CITY-district model — in Task 3, attach ALL 4 council members to the one Henderson G4110 district (geo_id='3231900'), each ward-labeled, and document ward-precise routing as deferred. The phase still completes. Record which path was taken in the SUMMARY.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/load-henderson-ward-boundaries.ts"; test -f "$F" &amp;&amp; grep -q "X0016" "$F" &amp;&amp; grep -q "resultRecordCount=100" "$F" &amp;&amp; grep -q "outSR=4326" "$F" &amp;&amp; grep -q "henderson-nv-council-ward-" "$F" &amp;&amp; grep -q "EXPECTED_COUNT" "$F" &amp;&amp; grep -q "ST_Multi" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - load-henderson-ward-boundaries.ts exists; copies the LV loader structure verbatim except the constants block + ward-attribute parsing.
    - URL includes resultRecordCount=100, outSR=4326, f=json (NOT geojson), MapServer Layer 2 of OpenDataAdministrativeBoundaries.
    - MTFCC='X0016' (or Wave-0 code); GEO_ID_PREFIX='henderson-nv-council-ward-'; EXPECTED_COUNT=4; STATE_CODE='nv'.
    - WARD parsed via parseInt(String(...)) with isNaN/range guard; WARDNAME used for display name.
    - INLINE: loader run → 4 X0016 ward rows in geofence_boundaries, `SELECT COUNT(*), bool_and(ST_IsValid)` = (4, true), recorded in SUMMARY.
    - Ward III (4 rings) ST_IsValid=true (ST_MakeValid applied if needed).
    - OR (D-01b path): fallback documented in SUMMARY, single-city-district model selected, phase not blocked.
  </acceptance_criteria>
  <done>Ward loader written + run; 4 X0016 ward polygons loaded ST_IsValid (or D-01b fallback documented). Recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Write structural migration 1084 + apply inline + post-verify</name>
  <files>C:/EV-Accounts/backend/migrations/1084_henderson_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1075_las_vegas_city_council.sql (EXACT ANALOG — the 7-step skeleton: pre-flight → government → chamber → LOCAL_EXEC district → LOCAL ward districts → politician+office CTEs → office_id back-fill → post-verify DO block → ledger registration outside COMMIT)
    - .planning/phases/163-henderson-deep-seed/163-PATTERNS.md (section "migrations/1084_henderson_city_council.sql" — the complete LV→Henderson substitution map + every step's adapted SQL)
    - .planning/phases/163-henderson-deep-seed/163-RESEARCH.md (Pattern 2 structural migration; Code Examples — Mayor/Ward CTEs + Post-Verify DO block; Pitfalls 4 Cox-incumbent + 5 Roman numerals + 6 two-district-types/casing)
  </read_first>
  <action>
EXECUTOR STEP: write `1084_henderson_city_council.sql` modeled verbatim on 1075_las_vegas_city_council.sql, applying the LV→Henderson substitution map from 163-PATTERNS.md. Steps:
- Pre-flight DO block: NOTICE if the Henderson government already exists (idempotent re-run); RAISE EXCEPTION if fewer than 4 X0016 ward geofences exist (`< 4` ... mtfcc='X0016' AND state='nv') — "run load-henderson-ward-boundaries.ts first". (Under the D-01b single-city fallback, REMOVE this X0016 assertion and instead assert the G4110 geo_id='3231900' geofence exists.)
- Step 1 Government: INSERT 'City of Henderson, Nevada, US', type='City', state='NV', city=NULL, geo_id='3231900' WHERE NOT EXISTS (mandatory — no unique constraint).
- Step 2 Chamber: INSERT 'Henderson City Council' / name_formal='Henderson City Council' / government_id subquery / official_count=5 WHERE NOT EXISTS. NEVER include the auto-generated path column in the INSERT list.
- Step 3a LOCAL_EXEC district (Mayor): INSERT district_type='LOCAL_EXEC', state='nv' lowercase, geo_id='3231900', label='City of Henderson', mtfcc='G4110' WHERE NOT EXISTS (uses the existing Phase 158 G4110 geofence — no new geofence row).
- Step 3b LOCAL ward districts: 4 INSERTs, district_type='LOCAL', state='nv', geo_id='henderson-nv-council-ward-1..4', label='Ward I'..'Ward IV' (Roman numerals), mtfcc='X0016', each WHERE NOT EXISTS. (Under D-01b: skip these; all 4 members attach to the LOCAL_EXEC G4110 district instead — or a single LOCAL G4110 district per the fallback model in 163-CONTEXT D-01b.)
- Step 4 politician+office CTEs (5 blocks): BLOCK 1 Mayor Romero (-3206001) title='Mayor' on the LOCAL_EXEC district (geo_id='3231900', district_type='LOCAL_EXEC', state='nv'); BLOCKS 2-5 ward members on their LOCAL X0016 districts — Seebock (-3206002) 'Council Member, Ward I'; Larson (-3206003, is_appointed=false) 'Council Member, Ward II'; Cox (-3206004, is_active=true is_incumbent=true) 'Council Member, Ward III'; Stewart (-3206005) 'Council Member, Ward IV'. Each block: WITH ins_p INSERT politicians (party='Non-Partisan', is_active=true, is_vacant=false, is_incumbent=true, external_id) ON CONFLICT (external_id) DO NOTHING RETURNING id → INSERT offices (chamber_id subquery, representing_state='NV' uppercase, role_canonical=NULL) joined on the district by geo_id+district_type+state='nv' with NOT EXISTS (district_id, politician_id) guard. Mayor uses district_type='LOCAL_EXEC' (NOT 'LOCAL').
- Step 5 office_id back-fill: UPDATE politicians SET office_id=o.id WHERE external_id BETWEEN -3206005 AND -3206001 AND office_id IS NULL.
- Step 6 post-verify DO block (RAISE EXCEPTION on mismatch): exactly 1 Henderson government row; exactly 1 LOCAL_EXEC office (Mayor); exactly 4 LOCAL X0016 ward offices (under D-01b: assert all 5 offices resolve to the G4110 district instead); section-split detector (0 orphan X0016 geofence rows lacking a matching LOCAL district). COMMIT.
- Step 7 ledger registration (OUTSIDE the transaction COMMIT): INSERT the version '1084' (or Wave-0 number) + name 'henderson_city_council' ON CONFLICT (version) DO NOTHING.
GREP-GATE HYGIENE: keep 'slug' out of comments (say "the auto-generated path column"); keep 'photo_origin_url' out (N/A here); the ledger table token appears only in the Step 7 INSERT, never in a comment.

INLINE ORCHESTRATOR STEP: apply 1084 via `psql -f` (DATABASE_URL from C:/EV-Accounts/backend/.env) or mcp__supabase-local__apply_migration. Confirm the post-verify DO block printed PASSED (gov=1, exec=1, local=4, split_orphans=0). Capture the 5 external_id→UUID map (`SELECT external_id, id FROM essentials.politicians WHERE external_id BETWEEN -3206005 AND -3206001`) and RECORD it in the SUMMARY — Plan 02 (headshots) + Plan 03 (stances) need these UUIDs.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1084_henderson_city_council.sql"; test -f "$F" &amp;&amp; grep -q "City of Henderson, Nevada, US" "$F" &amp;&amp; grep -q "INSERT INTO essentials.chambers" "$F" &amp;&amp; grep -q "Henderson City Council" "$F" &amp;&amp; grep -q "LOCAL_EXEC" "$F" &amp;&amp; grep -q -- "-3206001" "$F" &amp;&amp; grep -q -- "-3206005" "$F" &amp;&amp; grep -q "official_count" "$F" &amp;&amp; grep -vq "^[[:space:]]*--.*slug" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1084_henderson_city_council.sql exists, ≥200 lines, follows the 1075 7-step skeleton.
    - Pre-flight asserts ≥4 X0016 ward geofences (or, under D-01b, asserts the G4110 geofence).
    - Government 'City of Henderson, Nevada, US' (state='NV', geo_id='3231900') with WHERE NOT EXISTS; standalone (NOT under geo_id='32').
    - Chamber 'Henderson City Council' official_count=5; the auto-generated path column NOT in the INSERT list; name_formal non-empty.
    - 1 LOCAL_EXEC district (geo_id='3231900', state='nv') for the Mayor; 4 LOCAL X0016 ward districts (state='nv', labels 'Ward I'..'Ward IV').
    - 5 politician+office blocks: Mayor Romero (-3206001, LOCAL_EXEC, title='Mayor') + 4 ward members (-3206002..-3206005) with Roman-numeral titles; Cox is_active=true is_incumbent=true; all party='Non-Partisan'; representing_state='NV'.
    - office_id back-filled BETWEEN -3206005 AND -3206001.
    - Post-verify DO block asserts gov=1, exec=1, local=4, section-split=0.
    - Ledger registration line present OUTSIDE the COMMIT (version 1084 / 'henderson_city_council'); no forbidden tokens in comments.
    - INLINE: applied; post-verify PASSED; the 5 external_id→UUID map recorded in SUMMARY.
  </acceptance_criteria>
  <done>Structural migration written + applied; post-verify PASSED (gov=1, exec=1, local=4, split=0); external_id→UUID map captured in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 4: Append Henderson to coverage.js Nevada block</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (lines ~183-188 — the existing Nevada block with the Las Vegas entry)
    - .planning/phases/163-henderson-deep-seed/163-PATTERNS.md (section "src/lib/coverage.js" — before/after of the NV block)
  </read_first>
  <action>
EXECUTOR STEP: edit src/lib/coverage.js — locate the existing Nevada block in COVERAGE_STATES (the one already containing the Las Vegas entry from Phase 162) and APPEND a Henderson entry to its `areas` array: `{ label: 'Henderson', browseGovernmentList: ['3231900'], browseStateAbbrev: 'NV', hasContext: true }`. Keep the Las Vegas entry intact. browseGovernmentList uses the CITY geo_id ('3231900'), NOT the ward geo_ids. Do NOT create a second Nevada block and do NOT touch Landing.jsx (surfacing target is coverage.js).
  </action>
  <verify>
    <automated>F="C:/Transparent Motivations/essentials/src/lib/coverage.js"; test -f "$F" &amp;&amp; grep -q "Henderson" "$F" &amp;&amp; grep -q "3231900" "$F" &amp;&amp; grep -q "Las Vegas" "$F" &amp;&amp; grep -q "3240000" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - coverage.js Nevada block contains BOTH Las Vegas (3240000) and Henderson (3231900) entries.
    - Henderson entry: label 'Henderson', browseGovernmentList ['3231900'], browseStateAbbrev 'NV', hasContext: true.
    - Exactly one Nevada block (no duplicate); Landing.jsx untouched.
  </acceptance_criteria>
  <done>Henderson appended to the existing coverage.js Nevada block with hasContext:true; LV entry preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Henderson ArcGIS MapServer → loader | public GIS fetch crosses into DB geometry rows; validate content (4 features, ST_IsValid) before commit |
| generated .sql migration → live DB | service-role apply of seed SQL; only the inline orchestrator applies, after review |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | Tampering | external_id / MTFCC collision corrupting existing rows | mitigate | Wave-0 BLOCKING probes (Task 1) confirm -3206001..-3206005 and X0016 are unclaimed before any write; ON CONFLICT DO NOTHING guards |
| T-163-02 | Tampering | wrong migration number (v18.0-park drift) overwriting/skipping | mitigate | Wave-0 probe (a) trusts highest on-disk file +1, not just schema_migrations; renumber + offset downstream if drift |
| T-163-03 | Tampering | invalid/partial ward geometry (Ward III multi-ring) or 3-of-4 truncation | mitigate | resultRecordCount=100 + EXPECTED_COUNT=4 assertion; ST_Multi(ST_MakeValid) + ST_IsValid RETURNING; D-01b single-city fallback if unsourceable |
| T-163-04 | Tampering | silent no-op from uppercase state='NV' on routing join | mitigate | districts/geofence state='nv' lowercase enforced; post-verify asserts 1 LOCAL_EXEC + 4 LOCAL offices exist (would be 0 on casing bug) |
| T-163-05 | Information disclosure | GIS endpoint serving unexpected content | accept | City-operated public open-data service, no auth/secrets; content validated (feature count + geometry) before insert |
| T-163-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit = N/A; all tooling pre-installed) |
</threat_model>

<verification>
Structural + surfacing halves of CLARK-03:
- SC#1 (structural): 4 X0016 ward polygons ST_IsValid + 1 government + 1 chamber (official_count=5) + 1 LOCAL_EXEC Mayor office + 4 LOCAL ward offices, all state='nv', section-split=0 (Task 2 loader verify + Task 3 post-verify DO block). D-01b fallback path documented if needed.
- SC#4 (surfacing): Henderson in coverage.js with hasContext:true alongside Las Vegas (Task 4).
- Cross-cutting: Wave-0 probes confirm no collision/drift; structural migration registered at 1084 (or Wave-0 number); external_id→UUID map captured for Plans 02/03.
</verification>

<success_criteria>
- Wave-0 probes + roster checkpoint recorded and passed before any write; on-disk MAX → structural number set.
- load-henderson-ward-boundaries.ts written + run; 4 X0016 ward polygons ST_IsValid (or D-01b single-city fallback documented, phase not blocked).
- 1084_henderson_city_council.sql written + applied; post-verify PASSED (gov=1, exec=1 Mayor LOCAL_EXEC, local=4 ward, section-split=0); registered in the ledger.
- 5 external_id→UUID map recorded in SUMMARY (consumed by Plans 02/03).
- Henderson surfaces in coverage.js Nevada block (hasContext:true), LV preserved.
</success_criteria>

<output>
Create `.planning/phases/163-henderson-deep-seed/163-01-SUMMARY.md` when done (record the Wave-0 probe values + roster confirmation, which ward path was taken (X0016 vs D-01b), the post-verify results, the 5 external_id→UUID map, and the coverage.js edit).
</output>
