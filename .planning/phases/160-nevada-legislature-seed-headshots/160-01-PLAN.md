---
phase: 160-nevada-legislature-seed-headshots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql
autonomous: false
requirements: [NV-LEG-01, NV-LEG-02]
user_setup: []

must_haves:
  truths:
    - "Two chambers (Nevada State Senate + Nevada Assembly) exist under the State of Nevada government (geo_id='32')"
    - "21 State Senators have offices linked to the existing STATE_UPPER (SLDU) districts at state='nv'"
    - "42 Assembly members have offices linked to the existing STATE_LOWER (SLDL) districts at state='nv'"
    - "Each of the 63 legislators has office_id back-filled and a single office in its chamber"
    - "No legislator appears under two different governments (no section-split defect)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql"
      provides: "Structural migration: 2 chambers + 63 politicians + 63 offices linked to existing SLDU/SLDL districts + 2 office_id back-fills, idempotent, registers as 1053"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 200
  key_links:
    - from: "essentials.chambers (Senate + Assembly)"
      to: "essentials.governments (geo_id='32')"
      via: "government_id subquery WHERE geo_id='32'"
      pattern: "geo_id = '32'"
    - from: "essentials.offices (63 legislators)"
      to: "essentials.districts (STATE_UPPER/STATE_LOWER at state='nv')"
      via: "district join on geo_id/name_formal + district_type + state='nv'"
      pattern: "state = 'nv'"
    - from: "essentials.politicians (external_id -3203001..-3204042)"
      to: "essentials.offices"
      via: "office_id back-fill BETWEEN ranges"
      pattern: "BETWEEN -3203021 AND -3203001"
---

<objective>
Seed the full Nevada State Legislature structurally: create the two legislature chambers under State of Nevada (geo_id='32'), insert all 63 sitting legislators (21 Senate / 42 Assembly), link each to its PRE-EXISTING SLDU/SLDL district row (loaded by Phase 158 — NO new districts), and back-fill office_id. A Wave-0 DB probe confirms the district counts + exact Senate keying + unused external_id ranges BEFORE any write, and a roster operator-verification checkpoint precedes the migration apply.

Purpose: Satisfies the structural half of NV-LEG-01 (21 senators → SLDU) and NV-LEG-02 (42 assembly → SLDL). Headshots are Plan 02; final verification is Plan 03.
Output: 1053_nv_legislature.sql (structural, registered), plus the captured Wave-0 probe facts recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP):**
- The executor WRITES the migration .sql file to disk and documents the exact SQL. The executor does NOT apply migrations or run DB SELECTs.
- The Wave-0 DB probes, the `mcp__supabase-local__apply_migration` (1053), and every post-apply audit SELECT are INLINE ORCHESTRATOR steps performed by execute-phase, not by the executor.
- Each task below marks which steps are executor-write vs inline-orchestrator-apply.
- All migration files live in the SEPARATE repo C:/EV-Accounts/backend/. The SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/160-nevada-legislature-seed-headshots/160-RESEARCH.md
@.planning/phases/160-nevada-legislature-seed-headshots/160-PATTERNS.md
@.planning/phases/160-nevada-legislature-seed-headshots/160-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
State of Nevada government: id=9bb67edf-1081-4941-8f7d-2e791a5d28a1, geo_id='32'
Senate chamber: name='Nevada State Senate', name_formal='Nevada State Senate', under geo_id='32'
Assembly chamber: name='Nevada Assembly', name_formal='Nevada State Assembly', under geo_id='32'
Senate office title (D-04): 'State Senator'; Assembly office title (D-04): 'Assemblymember' (one word)
Senate external_id range: -3203001 (SD-1) .. -3203021 (SD-21)
Assembly external_id range: -3204001 (AD-1) .. -3204042 (AD-42)
District casing (legislature): state='nv' LOWERCASE — NOT uppercase 'NV' (that matches ZERO districts)
District type: 'STATE_UPPER' (Senate) / 'STATE_LOWER' (Assembly) — MANDATORY in every WHERE (SLDU/SLDL share geo_id space)
representing_state (office label column): 'NV' UPPERCASE (free-text, NOT the district join key)
Assembly district geo_ids: 32001-32042 (DB-confirmed), name_formal='Assembly District N'
Senate district geo_ids: UNCONFIRMED — Wave-0 probe required (key on name_formal if not simple form)
Next structural migration: 1053 (STATE.md + DB-confirmed; ledger MAX after 159 = 1052)
chambers.slug is GENERATED ALWAYS — never include in INSERT column list; name_formal must NOT be ''
The full 63-row roster (district → external_id → full_name → first/last → party) is in 160-RESEARCH.md "The Roster"
Diacritics to PRESERVE in full_name: Doñate (SD-10), González (AD-16), Zermeño (AD-11)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write structural migration 1053_nv_legislature.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/108_tx_state_legislature_chambers.sql (PRIMARY ANALOG — two-chamber idempotent INSERT, slug excluded)
    - C:/EV-Accounts/backend/migrations/227_or_state_house.sql (PRIMARY ANALOG — office→existing-TIGER-district CTE, lowercase state, mandatory district_type, NOT EXISTS guard)
    - C:/EV-Accounts/backend/migrations/319_va_delegates.sql (the "AND p.id IS NOT NULL" guard + vacancy handling)
    - C:/EV-Accounts/backend/migrations/109_tx_state_senate_officials.sql (office_id back-fill BETWEEN-range pattern)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-PATTERNS.md (section "1053_nv_legislature.sql" — the exact 4-step template + the 63-row substitution rules + structural-registration block)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-RESEARCH.md ("The Roster" — all 63 district/external_id/name/party rows; Common Pitfalls 1-8)
  </read_first>
  <action>
Write the structural migration following the 4-step template in 160-PATTERNS.md, using the full 63-row roster from 160-RESEARCH.md. Open with a header comment block stating: file is 1053_nv_legislature.sql, Phase 160 NV-LEG-01/02, STRUCTURAL (registers in the migration ledger), idempotent, State of Nevada government geo_id='32', Senate external_ids -3203001..-3203021, Assembly external_ids -3204001..-3204042, and the casing reminder that legislature districts use LOWERCASE state and the auto-generated chamber path column must never appear in the column list. Wrap STEPS 1-4 in a single BEGIN/COMMIT transaction.

Pre-flight: a DO block that RAISE EXCEPTION unless exactly 1 essentials.governments row has geo_id='32'.

STEP 1 — two chamber INSERTs (columns id, name, name_formal, government_id), Senate (name='Nevada State Senate', name_formal='Nevada State Senate') and Assembly (name='Nevada Assembly', name_formal='Nevada State Assembly'), government_id from a subquery WHERE geo_id='32', each guarded by WHERE NOT EXISTS on (name, government_id). Never list the auto-generated path column; name_formal is never empty.

STEP 2 — 63 politician+office CTEs (one per legislator), each a `WITH ins_p AS (INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id) VALUES (...) ON CONFLICT (external_id) DO NOTHING RETURNING id)` followed by an `INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant) SELECT ... FROM essentials.districts d CROSS JOIN ins_p p WHERE <district key> AND d.district_type=<type> AND d.state='nv' AND p.id IS NOT NULL AND NOT EXISTS (office on (district_id, chamber_id))`. Senate rows: district_type='STATE_UPPER', title='State Senator', chamber name='Nevada State Senate'. Assembly rows: district_type='STATE_LOWER', title='Assemblymember', chamber name='Nevada Assembly'. representing_state='NV' UPPERCASE on every office. Preserve diacritics in full_name (Doñate, González, Zermeño). For the `<district key>`: use the keying the Wave-0 probe (Task 2) confirms — if Senate+Assembly geo_ids are the simple 32001-32042 form, key on `d.geo_id='320NN'`; otherwise key on `d.name_formal='Senate District N'`/`'Assembly District N'`. The migration MUST be authored AFTER the Wave-0 probe result is known (Task 2 runs first inline; the executor reads the recorded keying from the orchestrator before writing the 63 WHERE clauses). All chamber_id subqueries scope to the geo_id='32' government.

STEP 3 — two office_id back-fills (109_tx pattern): UPDATE politicians SET office_id from offices WHERE external_id BETWEEN -3203021 AND -3203001 AND office_id IS NULL (Senate); repeat BETWEEN -3204042 AND -3204001 (Assembly). Note the more-negative bound comes first.

COMMIT. Then OUTSIDE the transaction, register the structural migration: INSERT the ('1053','nv_legislature') row into the migration ledger ON CONFLICT (version) DO NOTHING.

Do NOT apply this migration — that is an inline orchestrator step (Task 3). GREP-GATE HYGIENE: keep the literal tokens for the auto-generated path column and the ledger table OUT of explanatory comments (paraphrase, e.g. "the auto-generated path column"); the ledger INSERT keyword in STEP-registration is unavoidable but must be the only occurrence.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql"; test -f "$F" &amp;&amp; grep -q "INSERT INTO essentials.chambers" "$F" &amp;&amp; grep -q "Nevada State Senate" "$F" &amp;&amp; grep -q "Nevada Assembly" "$F" &amp;&amp; grep -q "geo_id = '32'" "$F" &amp;&amp; grep -q "STATE_UPPER" "$F" &amp;&amp; grep -q "STATE_LOWER" "$F" &amp;&amp; grep -q "state = 'nv'" "$F" &amp;&amp; grep -q -- "-3203001" "$F" &amp;&amp; grep -q -- "-3203021" "$F" &amp;&amp; grep -q -- "-3204001" "$F" &amp;&amp; grep -q -- "-3204042" "$F" &amp;&amp; [ "$(grep -c "ON CONFLICT (external_id) DO NOTHING" "$F")" -eq 63 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql exists.
    - Contains exactly two chamber INSERTs (Nevada State Senate + Nevada Assembly), both guarded by WHERE NOT EXISTS, both scoped to geo_id='32'.
    - Contains exactly 63 politician CTEs (`ON CONFLICT (external_id) DO NOTHING` appears 63 times) covering Senate -3203001..-3203021 and Assembly -3204001..-3204042.
    - Every office WHERE clause uses `d.state = 'nv'` LOWERCASE and includes `d.district_type` ('STATE_UPPER' for Senate titles 'State Senator', 'STATE_LOWER' for Assembly titles 'Assemblymember').
    - Every office row sets representing_state='NV' UPPERCASE; every office INSERT guarded by NOT EXISTS on (district_id, chamber_id) and `AND p.id IS NOT NULL`.
    - Diacritics preserved in full_name for Doñate (SD-10), González (AD-16), Zermeño (AD-11).
    - Two office_id back-fills present, one per chamber external_id range, each guarded by `office_id IS NULL`.
    - The migration-ledger registration line for ('1053','nv_legislature') appears OUTSIDE the BEGIN/COMMIT block; no headshot/audit rows in this file.
    - The auto-generated chamber path column does NOT appear in any chamber INSERT column list; no `photo_origin_url`; no compass/stance INSERT.
  </acceptance_criteria>
  <done>1053_nv_legislature.sql is written to disk against the Wave-0-confirmed keying and passes all acceptance criteria. Not yet applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Wave-0 DB probes (inline) + roster operator-verification checkpoint</name>
  <action>
INLINE ORCHESTRATOR STEPS (run BEFORE the executor finalizes Task 1's WHERE clauses, and BEFORE applying anything — gsd-executor has NO supabase MCP):
(P1) District counts + keying — run `SELECT district_type, geo_id, name_formal FROM essentials.districts WHERE district_type IN ('STATE_UPPER','STATE_LOWER') AND state='nv' ORDER BY 1,2;`. Assert exactly 21 STATE_UPPER + 42 STATE_LOWER rows. Capture the exact Senate geo_id values + name_formal (Assembly confirmed 32001-32042 / 'Assembly District N') so the executor can pick the district key (geo_id if simple, else name_formal). Record the chosen keying for Task 1.
(P2) external_id collision check — run `SELECT external_id FROM essentials.politicians WHERE external_id BETWEEN -3204042 AND -3203001 ORDER BY 1;`. Assert ZERO rows (ranges -3203001..-3203021 and -3204001..-3204042 are unused).
(P3) Migration counter — confirm the on-disk/DB ledger MAX so 1053 is the correct next structural number (resolve any counter drift like 159 hit).

Then PAUSE for the roster operator-verification checkpoint below. Present the what-built summary and how-to-verify; wait for the resume signal BEFORE the migration is applied (Task 3). No DB writes in this task.
  </action>
  <what-built>Wave-0 probes ran inline: STATE_UPPER/STATE_LOWER district counts (expect 21/42) and Senate keying captured; external_id ranges confirmed unused; migration counter confirmed at 1053. The structural migration 1053 is written (Task 1) but NOT yet applied. The 63-row roster (21 Senate / 42 Assembly, from leg.state.nv.us + Wikipedia cross-check, no vacancies) is staged for your sign-off.</what-built>
  <how-to-verify>
1. Probe results — confirm P1 returned exactly 21 STATE_UPPER + 42 STATE_LOWER districts at state='nv' (if not, STOP — the geofences from Phase 158 are incomplete). Confirm P2 returned 0 rows. Confirm the Senate district keying choice (geo_id vs name_formal) recorded for Task 1 looks right.
2. Roster currency — spot-check the 63-row roster in 160-RESEARCH.md "The Roster" against leg.state.nv.us "Current" Senate + Assembly listings. Pay attention to any seat that may have had a quiet mid-2026 appointment/resignation since research (RESEARCH A4): SD-11 Rogich and SD-18 Steinbeck are appointees — confirm they still hold the seats. Confirm there are still no vacancies.
3. Diacritics — confirm Doñate (SD-10), González (AD-16), Zermeño (AD-11) are spelled with the accents in the migration's full_name values.
4. If any seat changed: tell the executor the corrected district→name→party before approving (vacant seats are seeded is_vacant=true with no invented person, per D-07).
  </how-to-verify>
  <verify><human-check>Operator confirms the Wave-0 probe counts (21/42, 0 collisions), the Senate keying choice, and the 63-row roster currency against leg.state.nv.us before migration 1053 is applied.</human-check></verify>
  <done>Probes pass (21/42 districts, 0 external_id collisions, counter=1053), keying recorded for Task 1, and the operator approves the roster (or supplies corrections).</done>
  <resume-signal>Type "approved" or describe any roster change (new appointment, vacancy, spelling) or probe mismatch.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply migration 1053 inline + per-chamber count audit</name>
  <files>C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql</files>
  <read_first>
    - .planning/phases/160-nevada-legislature-seed-headshots/160-VALIDATION.md (Per-Task Verification Map — Senate=21, Assembly=42, district linkage)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-PATTERNS.md (Migration Counter Reference + Section-Split Verification SQL)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): apply C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql via mcp__supabase-local__apply_migration. Then run the per-chamber audits and record results for the SUMMARY:
(1) Senate count — offices joined to chamber 'Nevada State Senate' under geo_id='32' → expect 21.
(2) Assembly count — same with chamber 'Nevada Assembly' → expect 42.
(3) District linkage — offices→districts GROUP BY district_type for the two chambers → expect STATE_UPPER 21, STATE_LOWER 42.
(4) office_id back-fill — `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -3204042 AND -3203001 AND office_id IS NOT NULL` → expect 63.
(5) Section-split — run the exact query from 160-PATTERNS.md "Section-Split Verification SQL" (STATE_UPPER/STATE_LOWER) → expect 0 rows.
(6) Ledger — confirm ('1053','nv_legislature') is registered and ledger MAX = 1053.
Capture the per-member external_id → politician UUID map (`SELECT external_id, id FROM essentials.politicians WHERE external_id BETWEEN -3204042 AND -3203001 ORDER BY external_id`) — Plan 02's headshot script + migration 1054 consume this. Record any deviation (e.g. an office count below 21/42 implies a casing or keying miss — fix and re-apply idempotently).
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: migration 1053 applied successfully (no slug/column errors).
    - Senate office count = 21; Assembly office count = 42.
    - District linkage = STATE_UPPER 21 + STATE_LOWER 42 (all at state='nv').
    - office_id back-filled for all 63 legislators (count = 63).
    - Section-split query returns 0 rows.
    - Ledger registers ('1053','nv_legislature'); MAX version = 1053.
    - external_id → UUID map captured for all 63 legislators (handed to Plan 02).
  </acceptance_criteria>
  <done>Migration 1053 applied; 21 Senate + 42 Assembly offices linked to SLDU/SLDL at state='nv', office_id back-filled, 0 section-split, ledger MAX=1053, and the 63 UUIDs captured for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration file → live DB | service-role apply of seed SQL; only the inline orchestrator applies |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | Tampering | wrong-government section split (office attaches under non-NV chamber) | mitigate | Both chambers scoped to government_id WHERE geo_id='32'; section-split SQL gate must return 0 rows (Task 3 check 5) |
| T-160-02 | Tampering | silent zero-office no-op from uppercase state in WHERE | mitigate | Every office WHERE uses state='nv' lowercase + district_type; per-chamber count audit asserts 21/42 (Task 3 checks 1-2) |
| T-160-03 | Information disclosure | service-role key leakage | accept | Migrations applied via MCP by inline orchestrator only; no key in the .sql file; file committed to EV-Accounts repo |
| T-160-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (data-seed only); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
After all tasks (inline orchestrator):
1. Senate office count = 21; Assembly = 42 (per-chamber audit).
2. District linkage STATE_UPPER 21 + STATE_LOWER 42 at state='nv'.
3. office_id back-filled for 63 legislators.
4. Section-split SQL → 0 rows.
5. Ledger MAX = 1053; ('1053','nv_legislature') registered.
</verification>

<success_criteria>
- Two chambers (Nevada State Senate + Nevada Assembly) created under State of Nevada (geo_id='32').
- 21 Senators + 42 Assembly members seeded, each with a single office linked to its existing SLDU/SLDL district at state='nv', office_id back-filled.
- Zero section-split defects; migration ledger MAX = 1053.
- No stances, no new districts, no fabricated members; vacancies (if surfaced at checkpoint) seeded is_vacant=true with no invented person.
</success_criteria>

<output>
Create `.planning/phases/160-nevada-legislature-seed-headshots/160-01-SUMMARY.md` when done (record the Wave-0 probe results, the Senate keying choice, the 63 external_id→UUID map, and all Task-3 audit counts).
</output>
