---
phase: 222-collin-county-stances-evidence-only-compass-research-one-pol
plan: 09
type: execute
wave: 9
depends_on: ["222-08"]
files_modified:
  - "[EV-Accounts] C:/EV-Accounts/backend/migrations/<live-next-number>_222_collin_mayors_b_stances.sql (NEW — number assigned live)"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md (APPEND)"
autonomous: false
requirements: [COLLIN-STANCE-01, COLLIN-STANCE-02]
must_haves:
  truths:
    - "The mayor of every one of Weston, Blue Ridge, Josephine, Lavon, Lowry Crossing, Nevada, Saint Paul, and Van Alstyne that the 222-01 live worklist shows as un-stanced was individually attempted, ONE AT A TIME (D-01, D-02, D-03)"
    - "These towns being photo-poor in Phase 221 is NOT a reason to skip or shortcut them — a text-only official roster says nothing about whether council minutes, local news, or a questionnaire exist"
    - "Every stance written carries a matching inform.politician_context row whose reasoning names a specific dated action, quote, vote, or questionnaire answer and whose sources array holds at least one real URL that was actually fetched (D-04, D-05)"
    - "Every mayor in this plan's scope appears in exactly one of {this plan's applied migration rows, 222-CONFIRMED-BLANK.md} — never neither, never both (COLLIN-STANCE-01)"
    - "A sweep in which every mayor ends blank is a SUCCESS outcome fully recorded in the register, not a failed plan (COLLIN-STANCE-01/empty, COLLIN-STANCE-02/empty)"
    - "Any roster discrepancy found while researching — a town site naming a different person as mayor than the DB does — is recorded as a flagged finding and never silently used as the research subject"
    - "This plan writes only (politician_id, topic_id) pairs that had no prior row per the 222-01 do-not-overwrite set (COLLIN-STANCE-01/adjacency, D-07)"
    - "Every value is a whole integer 1 through 5 — never fractional, never a defaulted middle value (COLLIN-STANCE-02/adjacency)"
    - "100% of any stance valued 1 or 5 was re-verified against its cited source before commit, and at least a 20% random sample of the remaining stances was re-verified (222-VALIDATION.md resample contract for Tiers 2 to 4)"
    - "The migration is a single BEGIN/COMMIT transaction using ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables (COLLIN-STANCE-02/idempotency, COLLIN-STANCE-02/concurrency)"
    - "The migration has been APPLIED to production and the evidence-integrity gate returns zero rows for this plan's politician_ids — authored-but-unapplied SQL does not count as done"
  artifacts:
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
      provides: "Per-town Mayor sections of the blank register for the eight Tier 3/4 towns (D-08)"
      contains: "Blue Ridge — Mayor"
  key_links:
    - "Stances attach by politician_id taken verbatim from 222-01-WORKLIST.md — never by name match, which matters most in these towns where homonyms dominate search results"
    - "Register headings are written as `## {City} — Mayor` so the later council plans can append `## {City} — Council` without colliding"
  prohibitions:
    - "MUST NOT infer or assign any stance from party affiliation, endorsement, donor history, or pattern-matching to another official in the same city — only an explicit, on-topic, cited statement, recorded vote, or questionnaire answer may set a chair"
    - "MUST NOT assign a chair when the evidence is suggestive-but-not-explicit, and MUST NOT default to a middle value to avoid leaving a blank — a blank spoke is the correct, successful outcome"
    - "MUST NOT record a source URL that was not actually fetched and read, or that does not contain the cited quote, vote, or questionnaire answer"
    - "MUST NOT attribute a stance to an officeholder on the basis of a same-named person in a different city or state; the source must confirm the Texas town before the evidence is used"
    - "MUST NOT name, imply, or rely on a politician's party in reasoning text — party is never used for inference and never displayed"
---

<objective>
D-02 mayors sweep, part B: evidence-only compass stance research for the mayor of **Weston** (`4877740`), **Blue Ridge** (`4808872`), **Josephine** (`4838068`), **Lavon** (`4841800`), **Lowry Crossing** (`4844308`), **Nevada** (`4850760`), **Saint Paul** (`4864220`), and **Van Alstyne** (`4874924`) — one politician at a time, all 11 canonical topics — then author, commit, and apply one migration and record every honest blank.

Purpose: D-02 says every city's mayor goes in the first pass regardless of city size, precisely because a small-town mayor is the one person in that town who might have given an interview or delivered an address. RESEARCH.md expects a high blank rate here and that is the honest outcome — this plan exists to prove the attempt was genuinely made and to record exactly which dead ends were checked, not to produce stances.
Output: one AUDIT-ONLY migration in the EV-Accounts repo (only if anything is sourced), eight `## {City} — Mayor` register sections, and applied production rows.
</objective>

## Artifacts this phase produces

Phase 222 (all plans) produces data + planning artifacts only. No application code, no schema change, no packages.

- **[EV-Accounts repo]** `C:/EV-Accounts/backend/migrations/<N>_222_<slug>_stances.sql` — one numbered, git-committed, **AUDIT-ONLY** (NOT registered in `schema_migrations`) migration per research plan. Numbers assigned LIVE. This plan owns `<N>_222_collin_mayors_b_stances.sql`.
- **[essentials repo]** `.planning/phases/222-.../222-01-WORKLIST.md` (222-01), `222-CONFIRMED-BLANK.md` (created 222-01, appended by every research plan), `222-NN-SUMMARY.md` per plan.
- **[essentials repo]** `.planning/REQUIREMENTS.md` — COLLIN-STANCE-01/02 + traceability (222-01).
- **[essentials repo]** `src/lib/coverage.js` — `hasContext: true` reconcile (222-18 only).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mayors of Weston, Blue Ridge, Josephine, and Lavon — one at a time, then migration block, register, commit</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the mayor rows for Weston, Blue Ridge, Josephine, and Lavon are this task's exact scope, plus the live next-available migration number.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B is the SOURCE OF TRUTH for the verbatim question and five answer options of all 11 topics, OVERRIDING the researcher agent file for `housing`, `taxes`, and `healthcare` (Pitfall 1). Read Pitfall 2 in full — a Blue Ridge search returned only an unrelated Georgia homonym, which is the exact trap here. Also read section C (Low and Very-low tiers) and the anti-pattern warning against skipping photo-poor towns.
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — evidence hierarchy, quote-selection gates, inversion-trap table, de-identification protocol. Ignore its housing/taxes/healthcare scale text.
    - `C:/EV-Accounts/backend/migrations/1307_oro_valley_mayor_stances.sql` — the exact answers-plus-context upsert shape and AUDIT-ONLY header to copy.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format, plus the confirmed official-site URLs for these towns.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_collin_mayors_b_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Process in this order: Weston's mayor, Blue Ridge's mayor, Josephine's mayor, Lavon's mayor — taking each politician_id and full_name verbatim from 222-01-WORKLIST.md. Research only mayors here; council members belong to later plans.

    For each mayor, run one research pass, preferring the `politician-stance-researcher` subagent and falling back to a single general-purpose research subagent seeded with that file's methodology. Pass the full verbatim question plus five answer options for all 11 topics from RESEARCH.md section B. WebFetch is the primary tool; a light WebSearch budget of a few calls per person is allowed to discover a URL, which matters more here than anywhere else in the phase because these towns have no predictable URL patterns.

    Source order for each town (D-05 acceptable types): the official town site's council or elected-officials page and its agenda and minutes archive, often the only civic record that exists — blueridgecity.com/council is the confirmed Blue Ridge entry point; any State-of-the-Town address or recorded council statement; local and regional news coverage naming both the person and the town; VOTE411 and League of Women Voters of Collin County; a campaign or personal site if one exists. These towns sit below Ballotpedia's stated coverage scope, so do not spend fetch cycles on Ballotpedia URL guesses; if a Ballotpedia page does resolve, read the page body and confirm it names the Texas town before treating it as evidence.

    Assign a chair 1 through 5 ONLY where a cited source shows an explicit, on-topic position — a stated stance, a recorded council vote or motion, or a candidate-questionnaire answer (D-04). A routine procedural vote is not an on-topic position. Suggestive-but-not-explicit is a blank. No party inference, no pattern inference. Attempt all 8 canonical Local Lens topics (D-06) plus the 3 legacy-tail topics where explicit evidence exists (D-07). Skip any (politician_id, topic_id) pair in the 222-01 do-not-overwrite set.

    If the town's public roster names a different person as mayor than the worklist row does, record that as a flagged roster-currency finding in the SUMMARY and keep researching the worklist person — do not switch subjects, and never attach evidence about one person to another person's politician_id.

    A mayor who yields zero sourced topics is the expected, correct outcome for a Very-low-tier town. Confirming a genuine dead end quickly and recording the exact URLs checked is the deliverable.

    Self-audit before writing SQL: re-fetch and confirm 100% of any stance valued 1 or 5, plus at least a 20% random sample of the rest, checking that the cited source genuinely contains the claim, is on-topic for that specific compass topic, and points in the direction of the assigned chair per the inversion-trap table for that topic_key. Demote failures to blanks; demote generically-evaluative reasoning the same way.

    Re-read the live next-available migration number from 222-01-WORKLIST.md, confirm it is still free by listing `C:/EV-Accounts/backend/migrations`, and if anything is sourced create `C:/EV-Accounts/backend/migrations/<N>_222_collin_mayors_b_stances.sql` in the EV-Accounts repo with an AUDIT-ONLY header naming Phase 222 and the eight towns and stating it is deliberately not registered in `schema_migrations`. Use a single BEGIN/COMMIT transaction; for each surviving (politician, topic) pair write an insert into `inform.politician_answers (politician_id, topic_id, value)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value`, immediately followed by an insert into `inform.politician_context (politician_id, topic_id, reasoning, sources)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources`. Dollar-quote all reasoning text, using a distinct tag on delimiter collision. Values are whole numbers 1 through 5.

    Append a `## {City} — Mayor` section to `222-CONFIRMED-BLANK.md` for each of Weston, Blue Ridge, Josephine, and Lavon, with an `Evidence:` line naming the exact URLs checked rather than source families — a future phase must be able to see the dead end was real — and a bullet for the mayor if no position was sourced, carrying full_name, office title, and politician_id. Write the heading even when the mayor was sourced. Update the running `Count:` line.

    Commit both repos locally before starting Task 2. In EV-Accounts use `git -C "C:/EV-Accounts" add` then `git -C "C:/EV-Accounts" commit`; never `cd` into that repo and never `git push` it (a push deploys accounts-api to Render and carries every commit already ahead).
  </action>
  <verify>
    <automated>grep -q "^## Weston — Mayor" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; grep -q "^## Blue Ridge — Mayor" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - One research entry exists for each of the Weston, Blue Ridge, Josephine, and Lavon mayor rows the worklist shows as un-stanced — none skipped, none pre-declared hopeless.
    - Only mayors were researched; no council member of these towns appears in this task's output.
    - Each register `Evidence:` line names specific URLs that were actually fetched.
    - 100% of value-1 and value-5 stances plus at least a 20% sample of the rest were re-fetched and confirmed; demotions are recorded with a reason.
    - Every value literal is a whole number 1 through 5, each answers insert is immediately followed by its matching context insert, and every `sources` array has at least one URL.
    - Four `## {City} — Mayor` register sections exist, and each mayor appears in exactly one of {migration rows, register bullet}.
    - Any roster discrepancy is flagged rather than acted on.
    - Both repos are committed before Task 2 begins; `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>The Weston, Blue Ridge, Josephine, and Lavon mayors are fully attempted with recorded dead ends, self-audited, written into the migration where sourced, registered where blank, and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Mayors of Lowry Crossing, Nevada, Saint Paul, and Van Alstyne — one at a time, then extend the migration, register, commit</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the mayor rows for Lowry Crossing, Nevada, Saint Paul, and Van Alstyne are this task's exact scope.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B (verbatim topic text, overriding the agent file for housing, taxes, healthcare), Pitfall 2, section C (Very-low tier).
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — methodology and inversion-trap table.
    - `C:/EV-Accounts/backend/migrations/<N>_222_collin_mayors_b_stances.sql` — the file being extended (created in Task 1, or created here if Task 1 sourced nothing).
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — confirmed official URLs (lowrycrossingtexas.org, cityofnevadatx.org, stpaultexas.us, cityofvanalstyne.us/council) AND the recorded Saint Paul roster discrepancy: the town's public roster named a different mayor than the DB row, an open roster-currency item.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_collin_mayors_b_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Process in this order: Lowry Crossing's mayor, Nevada's mayor, Saint Paul's mayor, Van Alstyne's mayor — taking each politician_id and full_name verbatim from 222-01-WORKLIST.md. Research only mayors here.

    Use the same research method, the same RESEARCH.md section B topic text, and the same strict D-04 evidence bar as Task 1.

    Source order for each town (D-05 acceptable types): lowrycrossingtexas.org, cityofnevadatx.org, stpaultexas.us, and cityofvanalstyne.us/council respectively, plus each site's agenda and minutes archive; any recorded council statement or town address; local and regional news naming both the person and the town; VOTE411 and League of Women Voters of Collin County. Do not spend fetch cycles on Ballotpedia guesses for these towns; confirm the Texas town in the page body before accepting any hit as evidence.

    Saint Paul carries a known open roster-currency item: Phase 221 recorded that the town's public roster names a different mayor than the DB row does. Research the worklist person, and if the live town site again shows a different mayor, record it as a flagged finding in the SUMMARY and in the register section — this phase does not reseat anyone, and it must not attach one person's statements to another person's politician_id.

    Self-audit before extending the SQL: re-fetch and confirm 100% of any stance valued 1 or 5, plus at least a 20% random sample of the rest, checking presence, on-topic fit, and direction against the inversion-trap table for that topic_key. Demote failures to blanks.

    Extend `C:/EV-Accounts/backend/migrations/<N>_222_collin_mayors_b_stances.sql` with this task's blocks inside the same single BEGIN/COMMIT transaction, using the identical answers-plus-context upsert pair, dollar-quoted reasoning, and whole 1-through-5 values. If the file does not exist because Task 1 sourced nothing, create it now with the same AUDIT-ONLY header. If no mayor across all eight towns sources anything, do not create the file at all — the register alone carries the outcome, and that is a complete, successful plan.

    Append a `## {City} — Mayor` section to `222-CONFIRMED-BLANK.md` for each of Lowry Crossing, Nevada, Saint Paul, and Van Alstyne, with an `Evidence:` line naming the exact URLs checked and a bullet for the mayor if no position was sourced. Write the heading even when the mayor was sourced. Update the running `Count:` line.

    Commit both repos locally. In EV-Accounts use `git -C "C:/EV-Accounts" add` then `git -C "C:/EV-Accounts" commit`; never `cd` into that repo and never `git push` it.
  </action>
  <verify>
    <automated>grep -q "^## Lowry Crossing — Mayor" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; grep -q "^## Van Alstyne — Mayor" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - One research entry exists for each of the Lowry Crossing, Nevada, Saint Paul, and Van Alstyne mayor rows the worklist shows as un-stanced — none skipped.
    - Only mayors were researched; no council member of these towns appears in this task's output.
    - The Saint Paul roster-currency situation is explicitly reported as unchanged or changed, and no evidence was attached across people.
    - Each register `Evidence:` line names specific URLs that were actually fetched.
    - 100% of value-1 and value-5 stances plus at least a 20% sample of the rest were re-fetched and confirmed; demotions are recorded with a reason.
    - Every value literal is a whole number 1 through 5, each answers insert is immediately followed by its matching context insert, and every `sources` array has at least one URL.
    - Eight `## {City} — Mayor` register sections now exist across Tasks 1 and 2, and each mayor appears in exactly one of {migration rows, register bullet}.
    - `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>All eight Tier 3/4 mayors are fully attempted with recorded dead ends, self-audited, written into the migration where sourced, registered where blank, and committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator applies the mayors sweep B migration and runs the evidence-integrity gate</name>
  <read_first>
    - `C:/EV-Accounts/backend/migrations/<N>_222_collin_mayors_b_stances.sql` — the SQL to apply, if it exists.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the evidence-integrity gate query.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the eight Mayor sections.
  </read_first>
  <what-built>
    A committed, AUDIT-ONLY migration covering whichever of the eight Tier 3/4 town mayors yielded sourced positions, plus per-town Mayor register sections recording the exact dead ends checked for the rest. If no mayor sourced anything, there is no migration file and the register alone is the deliverable — that is a complete plan, not a failure.
  </what-built>
  <action>
    Halt here and hand control to the operator. Do NOT attempt the apply step from this session — gsd-executor has no Supabase MCP binding, so the SQL is committed but production is still unchanged until the operator runs it. Present the how-to-verify steps below verbatim, wait for the resume signal, then record the operator's reported apply confirmation, evidence-integrity gate result, and reconcile result in this plan's SUMMARY before the phase advances. If any gate returns rows, do not mark this plan done.
  </action>
  <how-to-verify>
    1. If a migration file exists, apply it via `mcp__supabase-local__execute_sql` (this is production — the write is live). Do NOT register it in `schema_migrations`. If no file exists because every mayor ended blank, confirm the register documents all eight and skip to step 3.
    2. Run the evidence-integrity gate from `222-VALIDATION.md` scoped to this plan's politician_ids: it must return zero rows.
    3. Run the exactly-one-bucket reconcile for the eight mayors against `222-01-WORKLIST.md` and the Mayor register sections — never both, never neither.
    4. Read two or three register `Evidence:` lines and confirm they name real, specific URLs rather than a generic statement that nothing was found.
    5. Note any town that gained its first-ever stance — those all currently lack a coverage chip and 222-18 will need to flip it.
  </how-to-verify>
  <acceptance_criteria>
    - Any migration file has been applied to production via `mcp__supabase-local__execute_sql` and the apply is confirmed, not assumed; or the absence of a file is confirmed as the correct all-blank outcome.
    - The evidence-integrity gate returns zero rows for this plan's politician_ids.
    - The eight-mayor exactly-one-bucket reconcile passes.
    - Register `Evidence:` lines name specific fetched URLs.
    - Any town moving from zero to at least one stance is recorded for the 222-18 coverage-chip reconcile.
    - No row was written to `schema_migrations`.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live (or confirmed unnecessary) and the gate returns zero rows, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped web text → SQL migration | Quotes and URLs harvested from the open web are embedded into a statement executed against production |
| research judgment → public civic profile | An assigned chair becomes a public claim about a real, named person |
| small-town name collisions | Below Ballotpedia's coverage scope, a same-named person in another state is the most likely search result |
| stale town roster → wrong subject | Saint Paul's public roster disagrees with the DB roster, an open item from Phase 221 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-04 | Tampering | migration SQL text | high | mitigate | Dollar-quote all scraped reasoning text; never concatenate scraped text into a bare SQL string |
| T-222-05 | Repudiation | fabricated or mismatched source URL | critical | mitigate | 100% re-fetch of value-1/value-5 stances plus a 20% sample; failing sources demote the stance to a blank; register Evidence lines carry specific URLs |
| T-222-06 | Spoofing | wrong-person attribution (homonym or stale roster) | critical | mitigate | Confirm the Texas town in the page body; research the worklist politician_id only; flag roster discrepancies rather than switching subjects |
| T-222-07 | Denial of Service | false-positive completion (authored but unapplied SQL) | high | mitigate | Blocking apply-and-verify checkpoint; the gate runs against production, not the file |
| T-222-SC | Tampering | package installs | low | accept | This plan installs no packages |
</threat_model>

<verification>
- All eight Tier 3/4 mayors individually attempted, sequentially, mayors only, with specific URLs recorded for every dead end.
- Every applied stance has a whole 1-5 value, a specific dated reasoning, and at least one re-fetched source URL at the Tier 2-4 sampling rate.
- Evidence-integrity gate returns zero rows for this plan's politician_ids after apply.
- Eight-mayor exactly-one-bucket reconcile passes.
- EV-Accounts migration committed locally and not pushed.
</verification>

<success_criteria>
D-02's "every city's mayor in the first pass" clause is fully satisfied across all 23 Collin governments: each mayor carries either evidence-cited chairs or a register entry naming the exact sources checked, and no town was skipped for being small or photo-poor.
</success_criteria>

<output>
Create `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-09-SUMMARY.md` when done — record per-town mayor attempted/sourced, stances applied, blanks, the migration number used (or that none was needed), any roster-currency flag, any stance demoted, and any town that moved from zero to at least one stance.
</output>
