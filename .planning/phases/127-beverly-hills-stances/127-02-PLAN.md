---
phase: 127-beverly-hills-stances
plan: 02
type: execute
wave: 2
depends_on: [127-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/NNN+2_mirisch_stances.sql
  - C:/EV-Accounts/backend/migrations/NNN+3_nazarian_stances.sql
  - C:/EV-Accounts/backend/migrations/NNN+4_wells_stances.sql
autonomous: true
requirements: [BEVHILLS-01]
must_haves:
  truths:
    - "John A. Mirisch (external_id -201153) has evidence-only stance rows written, applied, and verified — longest-tenured member, richest expected record"
    - "Sharona R. Nazarian (external_id -700010) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Mary N. Wells (external_id -201155) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
    - "Howard Fisher (external_id -700011) receives ZERO stance rows in any of these migrations"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/NNN+2_mirisch_stances.sql"
      provides: "John A. Mirisch stance migration"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/NNN+3_nazarian_stances.sql"
      provides: "Sharona R. Nazarian stance migration"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/NNN+4_wells_stances.sql"
      provides: "Mary N. Wells stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Research and apply evidence-only compass stances for the final 3 Beverly Hills officials (John A. Mirisch, Sharona R. Nazarian, Mary N. Wells), one politician at a time. Blocked on Plan 01 (requires confirmed migration numbers and all 5 UUIDs from 127-01-SUMMARY.md).

Purpose: Complete all 5 Beverly Hills officials' stance research, closing the data work for BEVHILLS-01. Unblocks Plan 03 phase-wide closure.
Output: 3 per-individual stance migration files (NNN+2 through NNN+4), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-beverly-hills-stances/127-RESEARCH.md
@.planning/phases/127-beverly-hills-stances/127-01-SUMMARY.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 703_lee_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs (Phase 115 ledger-consistency precedent)
- All 3 officials in this plan are Council Members — use "Council Member [Surname]" in reasoning text (none is the Mayor)
- CRITICAL: Howard Fisher (external_id -700011, City Treasurer) is EXCLUDED. NEVER write any INSERT with Fisher's UUID. NEVER research Fisher.
</interfaces>

Apply path: psql CLI with DATABASE_URL from C:/EV-Accounts/.env. Phase 126 confirmed mcp__supabase-local is NOT callable from the bash executor context — use psql CLI. Command form: `psql "$DATABASE_URL" -f <file>` to apply, `psql "$DATABASE_URL" -c "<query>"` for verification. Do NOT run git commands in C:/EV-Accounts.

Beverly Hills roster — final 3 officials (UUIDs from 127-01-SUMMARY.md):
  -201153 John A. Mirisch (Council Member, LOCAL)       → NNN+2
  -700010 Sharona R. Nazarian (Council Member, LOCAL)   → NNN+3
  -201155 Mary N. Wells (Council Member, LOCAL)         → NNN+4

NNN = confirmed in Plan 01 Wave 0 pre-flight; read from 127-01-SUMMARY.md (expected 714 → NNN+2 = 716, NNN+3 = 717, NNN+4 = 718).

Out-of-scope guard: Do NOT write stances for any external_id outside the target set {-200589, -201153, -201154, -201155, -700010}. NEVER write for -700011 (Fisher). Do NOT create any new offices, districts, or chambers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: John A. Mirisch (Council Member) stances — migration NNN+2</name>
  <files>C:/EV-Accounts/backend/migrations/NNN+2_mirisch_stances.sql</files>
  <read_first>
    - .planning/phases/127-beverly-hills-stances/127-01-SUMMARY.md (NNN confirmed value + Mirisch UUID from pre-flight + Fisher exclusion UUID)
    - .planning/phases/127-beverly-hills-stances/127-RESEARCH.md (John A. Mirisch section + Local Press Sources + Anti-Patterns + Pitfalls 1-5 + Pitfall 9 Fisher exclusion + Topic UUID Reference Block)
    - C:/EV-Accounts/backend/migrations/NNN_friedman_stances.sql (in-phase sibling file as format reference, NNN = confirmed value)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process John A. Mirisch (external_id -201153, UUID from 127-01-SUMMARY.md, Council Member) strictly after Plan 01 completes. Research, write, apply, and verify before proceeding to Task 2.

    (a) Research: Search "John Mirisch Beverly Hills council", "John Mirisch op-ed", and "John Mirisch Beverly Hills" on bhcourier.com, patch.com/california/beverlyhills, beverlyhills.org/government/city-council/meetings-agendas, latimes.com, ballotpedia.org. Mirisch is the longest-serving current council member (~2009-present) and an outspoken op-ed writer — VERY rich public record. Expected yield 10-15 stances (highest in the phase). Documented positions: campaign-finance (he supports local campaign contribution limits → progressive on that axis), housing/residential-zoning (has resisted RHNA density mandates → conservative on density), climate-change, growth-and-development, homelessness-response, public-safety-approach, local-environment, transportation-priorities, taxes. Search his op-eds in bhcourier.com archives for additional topic positions. Place each evidenced topic on the 1.0-5.0 scale per that topic's axis definition. No INSERT for any topic with no direct evidence.

    (b) Write migration NNN+2_mirisch_stances.sql (expected 716) using the canonical template:
    - Header: "Migration NNN+2: John A. Mirisch Stances"
    - Full 44-topic UUID reference block from 127-RESEARCH.md (or live list from 127-01-SUMMARY.md if count differed)
    - BEGIN;
    - paired politician_answers + politician_context INSERT per evidenced topic
    - value = float literal; sources = ARRAY[...]::text[]::text[] double-cast, path-bearing URLs; reasoning = $$...$$ dollar-quoting citing specific vote/op-ed/statement
    - Reasoning text uses "Council Member Mirisch"
    - COMMIT;

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/NNN+2_mirisch_stances.sql`.

    (d) Verify immediately (before Task 2), substituting Mirisch's UUID:
    Row count: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{mirisch_uuid}';"`
    Unpaired check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{mirisch_uuid}' AND pc.politician_id IS NULL;"` — must return 0
    Citation check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{mirisch_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Mirisch's UUID. Citation check returns 0 for Mirisch's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file NNN+2_mirisch_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Mirisch's UUID
    - Uncited-context query returns 0 for Mirisch's UUID
    - Reasoning text uses "Council Member Mirisch"
    - No INSERT references external_id -700011 (Fisher) or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration NNN+2_mirisch_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for John A. Mirisch.</done>
</task>

<task type="auto">
  <name>Task 2: Sharona R. Nazarian (Council Member) stances — migration NNN+3</name>
  <files>C:/EV-Accounts/backend/migrations/NNN+3_nazarian_stances.sql</files>
  <read_first>
    - .planning/phases/127-beverly-hills-stances/127-01-SUMMARY.md (NNN confirmed value + Nazarian UUID + exact stored full_name per Assumption A6 + Fisher exclusion UUID)
    - .planning/phases/127-beverly-hills-stances/127-RESEARCH.md (Sharona Nazarian section + Local Press Sources + Anti-Patterns + Pitfall 9 Fisher exclusion)
    - C:/EV-Accounts/backend/migrations/NNN+2_mirisch_stances.sql (just-written sibling file as in-phase format reference)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process Sharona R. Nazarian (external_id -700010, UUID from 127-01-SUMMARY.md, Council Member) strictly after Task 1 completes — never in parallel. Research, write, apply, and verify before proceeding to Task 3.

    (a) Research: Search "Sharona Nazarian Beverly Hills council" and "Sharona Nazarian Beverly Hills" on bhcourier.com, patch.com/california/beverlyhills, beverlyhills.org/government/city-council/meetings-agendas, latimes.com, ballotpedia.org. Nazarian was added in migration 301 as a gap-fill; she has served multiple terms and is notable for advocacy on civil rights and diversity. Expected yield 4-9 stances. High-probability topics: civil-rights, housing, residential-zoning, homelessness-response, growth-and-development, public-safety-approach, local-environment. Use the exact stored full_name from 127-01-SUMMARY.md in the header comment (Assumption A6 — DB may store "Sharona R. Nazarian"); the UUID is what matters in INSERTs. Place each evidenced topic on the 1.0-5.0 scale. No INSERT for any topic with no direct evidence. If zero evidence found, write the ledger file with header + BEGIN;/COMMIT; + zero INSERTs.

    (b) Write migration NNN+3_nazarian_stances.sql (expected 717):
    - Header: "Migration NNN+3: Sharona R. Nazarian Stances" (use exact stored full_name)
    - Full 44-topic UUID reference block
    - BEGIN;
    - paired INSERTs per evidenced topic; value = float literal; sources = ARRAY[...]::text[]::text[] double-cast; reasoning = $$...$$ dollar-quoting
    - Reasoning text uses "Council Member Nazarian"
    - COMMIT;

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/NNN+3_nazarian_stances.sql`.

    (d) Verify immediately (before Task 3), substituting Nazarian's UUID:
    Row count: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{nazarian_uuid}';"`
    Unpaired check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{nazarian_uuid}' AND pc.politician_id IS NULL;"` — must return 0
    Citation check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{nazarian_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Nazarian's UUID. Citation check returns 0 for Nazarian's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file NNN+3_nazarian_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Nazarian's UUID
    - Uncited-context query returns 0 for Nazarian's UUID
    - Reasoning text uses "Council Member Nazarian"
    - No INSERT references external_id -700011 (Fisher) or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration NNN+3_nazarian_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Sharona R. Nazarian.</done>
</task>

<task type="auto">
  <name>Task 3: Mary N. Wells (Council Member) stances — migration NNN+4</name>
  <files>C:/EV-Accounts/backend/migrations/NNN+4_wells_stances.sql</files>
  <read_first>
    - .planning/phases/127-beverly-hills-stances/127-01-SUMMARY.md (NNN confirmed value + Wells UUID + Fisher exclusion UUID)
    - .planning/phases/127-beverly-hills-stances/127-RESEARCH.md (Mary N. Wells section + Local Press Sources + Anti-Patterns + Pitfall 9 Fisher exclusion)
    - C:/EV-Accounts/backend/migrations/NNN+3_nazarian_stances.sql (just-written sibling file as in-phase format reference)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process Mary N. Wells (external_id -201155, UUID from 127-01-SUMMARY.md, Council Member) strictly after Task 2 completes — never in parallel. Research, write, apply, and verify.

    (a) Research: Search "Mary Wells Beverly Hills council" and "Mary N. Wells Beverly Hills" on bhcourier.com, patch.com/california/beverlyhills, beverlyhills.org/government/city-council/meetings-agendas, beverlyhillsweekly.com, ballotpedia.org. Wells is a council member with moderate tenure — expected yield 4-8 stances. High-probability topics: housing, residential-zoning, growth-and-development, homelessness-response, public-safety-approach, taxes, local-environment. Place each evidenced topic on the 1.0-5.0 scale per that topic's axis definition. No INSERT for any topic with no direct evidence. If zero evidence found, write the ledger file with header + BEGIN;/COMMIT; + zero INSERTs.

    (b) Write migration NNN+4_wells_stances.sql (expected 718):
    - Header: "Migration NNN+4: Mary N. Wells Stances"
    - Full 44-topic UUID reference block
    - BEGIN;
    - paired INSERTs per evidenced topic; value = float literal; sources = ARRAY[...]::text[]::text[] double-cast; reasoning = $$...$$ dollar-quoting citing specific vote/statement/action
    - Reasoning text uses "Council Member Wells"
    - COMMIT;

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/NNN+4_wells_stances.sql`.

    (d) Verify immediately, substituting Wells's UUID:
    Row count: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{wells_uuid}';"`
    Unpaired check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{wells_uuid}' AND pc.politician_id IS NULL;"` — must return 0
    Citation check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{wells_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Wells's UUID. Citation check returns 0 for Wells's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file NNN+4_wells_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Wells's UUID
    - Uncited-context query returns 0 for Wells's UUID
    - Reasoning text uses "Council Member Wells"
    - No INSERT references external_id -700011 (Fisher) or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration NNN+4_wells_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Mary N. Wells. All 5 Beverly Hills officials complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (psql CLI / live Supabase) | Writes go directly to live production Supabase via DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Tampering | stance value integrity | mitigate | Evidence-only rule: no INSERT without a direct cited source; per-person uncited-context query must return 0 |
| T-127-02 | Information Disclosure | reasoning text / sources | accept | All data is public-record political positions with public source URLs; no PII |
| T-127-03 | Repudiation | stance provenance | mitigate | Every value paired with a context row carrying path-bearing source URLs (100% citation rate) |
| T-127-04 | Tampering | writing stances for excluded Fisher (-700011) | mitigate | Acceptance criteria assert no INSERT references Fisher's UUID in any of the 3 migrations |
| T-127-06 | Tampering | unnecessary office/district creation | mitigate | All 3 officials' offices already exist; acceptance criteria assert no INSERT into essentials.offices/districts/chambers |
| T-127-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
After all three tasks, run the partial phase-wide citation check for this plan's officials (Mirisch + Nazarian + Wells):
`psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-201153, -700010, -201155)) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` must return 0.
</verification>

<success_criteria>
- Migrations NNN+2, NNN+3, NNN+4 (Mirisch, Nazarian, Wells) written, applied, and verified
- 0 unpaired answers, 0 uncited contexts across all 3 officials
- No INSERT references Fisher's UUID (-700011)
- No external_ids outside the 5-target set touched
- Blank-spoke topics documented in SUMMARY (never defaulted to 3.0)
- All 5 Beverly Hills officials now complete; Plan 03 unblocked
</success_criteria>

<output>
Create `.planning/phases/127-beverly-hills-stances/127-02-SUMMARY.md` when done. Record: per-official stance counts for Mirisch, Nazarian, and Wells, any blank-spoke topics, and confirmation that no Fisher stances and no office/district rows were created.
</output>
