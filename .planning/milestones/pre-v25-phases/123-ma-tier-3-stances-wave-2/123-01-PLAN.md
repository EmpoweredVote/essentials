---
phase: 123-ma-tier-3-stances-wave-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/635_nicholson_stances.sql
  - C:/EV-Accounts/backend/migrations/636_field_stances.sql
  - C:/EV-Accounts/backend/migrations/637_lapierre_stances.sql
  - C:/EV-Accounts/backend/migrations/638_mcclain_stances.sql
  - C:/EV-Accounts/backend/migrations/639_net_stances.sql
autonomous: true
requirements: [LYNN-03]
must_haves:
  truths:
    - "Wave 0 pre-flight confirms next migration is 635, active topic count is 44, and Lynn UUID mapping resolves for all 12 officials before any stance is written"
    - "Lynn Mayor Nicholson has evidence-only stance rows (or documented blank if no public record)"
    - "Lynn At-Large councillors Field, LaPierre, McClain, Net each have evidence-only stance rows or documented blanks"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/635_nicholson_stances.sql"
      provides: "Mayor Nicholson stance migration (or zero-stance ledger file)"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/639_net_stances.sql"
      provides: "At-Large councillor Net stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Run Wave 0 pre-flight verification, then research and apply evidence-only compass stances for the first 5 Lynn officials (Mayor Nicholson + At-Large councillors Field, LaPierre, McClain, Net), one politician at a time.

Purpose: Establish the verified migration starting point and topic reference for the entire phase, and begin closing LYNN-03.
Output: 5 per-individual stance migration files (635–639), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-RESEARCH.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 574_boston_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs (Phase 115 ledger-consistency precedent)
</interfaces>

Lynn roster (external_id → migration), from Phase 119 deep seed (migration 584):
  -2537490001 Jared Nicholson (Mayor)           → 635
  -2537490002 Brian M. Field (At-Large)         → 636
  -2537490003 Brian P. LaPierre (At-Large)      → 637
  -2537490004 Nicole D. McClain (At-Large)      → 638
  -2537490005 Hong L. Net (At-Large)            → 639
  -2537490006 Peter Meaney (Ward 1)             → 640
  -2537490007 Obed A. Matul (Ward 2)            → 641
  -2537490008 Constantino Alinsug (Ward 3)      → 642
  -2537490009 Natasha S. Megie-Maddrey (Ward 4) → 643
  -2537490010 Cardeliz Paez (Ward 5)            → 644
  -2537490011 Frederick W. Hogan (Ward 6)       → 645
  -2537490012 Jordan T. Avery (Ward 7)          → 646

Out-of-scope guard: Do NOT write stances for any external_id outside the -2537490001 to -2537490012 range. LYNN-03 covers Mayor + City Council only; no school committee.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 pre-flight verification</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md (Wave 0 Pre-Flight Queries section and Topic UUID Reference Block)
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-RESEARCH.md (established research approach)
  </read_first>
  <action>
    Run four verification queries via mcp__supabase-local__execute_sql before writing any migration:
    (1) `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations;` — confirm the highest applied migration. Expected: 634 (Phase 122 last migration was 634_hardt_stances.sql). If MAX is 634, the next migration is 635. If MAX is higher, use MAX+1 and record the deviation in SUMMARY.
    (2) `SELECT COUNT(*) FROM inform.compass_topics WHERE is_active = true;` — expected 44. If different, run `SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;` and use that live list as the topic reference (do NOT trust the cached block in PATTERNS.md if count differs).
    (3) `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id BETWEEN -2537490012 AND -2537490001 ORDER BY external_id DESC;` — resolve UUIDs for ALL 12 Lynn officials and confirm name-to-external_id mapping matches the roster table above. Capture all 12 UUIDs for use in this plan and Plans 02-05.
    (4) `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2537490012 AND -2537490001);` — informational; upsert handles pre-existing rows.
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql with the active-topic count query returns a single integer; the MAX migration query returns 634 (or documents the actual value). UUIDs for all 12 Lynn officials are captured.</automated>
  </verify>
  <acceptance_criteria>
    - Starting migration number confirmed as 635 (or higher if DB shows a later applied migration)
    - Active compass topic count confirmed (44 expected; if not, live slug→id list captured)
    - UUIDs resolved for ALL 12 external_ids (-2537490001 through -2537490012) with names matching the roster
    - All 12 UUIDs recorded in SUMMARY for use in subsequent plans
  </acceptance_criteria>
  <done>Pre-flight queries run; starting migration number, topic count, and all 12 Lynn UUIDs confirmed and recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Mayor Nicholson + At-Large Field, LaPierre (635–637)</name>
  <files>C:/EV-Accounts/backend/migrations/635_nicholson_stances.sql, C:/EV-Accounts/backend/migrations/636_field_stances.sql, C:/EV-Accounts/backend/migrations/637_lapierre_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format: BEGIN/COMMIT, float literals, dollar-quoting, per-person section header)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (authoritative on ::text[]::text[] double-cast)
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md (Full Minimal Valid Migration Template, lines 291-351)
  </read_first>
  <action>
    Process ONE politician at a time, strictly sequentially — never run two research sessions in parallel (rate-limit rule, feedback_stance_research_one_at_a_time.md). For each of Nicholson (-2537490001, Mayor), Field (-2537490002, At-Large), LaPierre (-2537490003, At-Large):
    (a) Research the official's public record across ALL active compass topics — campaign statements, votes, lynn.ma.us, local news (Lynn Journal, Daily Item), state legislature records where applicable. Place each topic with direct evidence on the 1.0–5.0 scale (1.0 most progressive/left, 5.0 most conservative/right).
    (b) Write the migration file using the canonical template: value is a float literal (e.g. 2.0); sources is ARRAY[...]::text[]::text[]; wrap in BEGIN;/COMMIT;; reasoning in $$...$$ citing the specific vote/statement/source; include the 44-topic UUID reference block in the file header.
    (c) For any topic with NO direct evidence, write NO INSERT — never default to 3.0. If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; and zero INSERT blocks (Phase 115 ledger-consistency precedent), and note in SUMMARY.
    (d) Apply via mcp__supabase-local__execute_sql, then immediately run both per-person verification queries before moving to the next official.
    Mayor Nicholson is a relatively new mayor (inaugurated 2022, served as school committee before) — expect 5–12 stances; Field and LaPierre are At-Large councillors with multi-year records. Key Lynn topics: housing, residential-zoning, local-environment, growth-and-development, public-safety-approach, transportation-priorities, homelessness, economic-development. Do NOT touch any external_id outside the -2537490001 to -2537490012 range.
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{uuid}' AND pc.politician_id IS NULL;` returns 0, AND `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - 3 migration files exist (635, 636, 637), each wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`; no bare-integer value lines
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT references an external_id outside the -2537490001 to -2537490012 range
  </acceptance_criteria>
  <done>Migrations 635–637 written, applied, and verified (0 unpaired, 0 uncited) for Nicholson, Field, LaPierre.</done>
</task>

<task type="auto">
  <name>Task 3: At-Large McClain, Net (638–639)</name>
  <files>C:/EV-Accounts/backend/migrations/638_mcclain_stances.sql, C:/EV-Accounts/backend/migrations/639_net_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
    - C:/EV-Accounts/backend/migrations/635_nicholson_stances.sql (just-written sibling file as in-phase reference)
  </read_first>
  <action>
    Process ONE politician at a time, sequentially, for McClain (-2537490004, At-Large) then Net (-2537490005, At-Large). Same procedure as Task 2: research public record across all active topics, write canonical migration (float values, double-cast sources, BEGIN/COMMIT, dollar-quoted reasoning, 44-topic UUID header), omit topics with no evidence (no 3.0 defaults), apply, then run per-person verification before the next official. Hong L. Net may have a thin record (newer councillor — blank spokes are acceptable and honest). Do NOT touch any external_id outside the -2537490001 to -2537490012 range.
  </action>
  <verify>
    <automated>For each of the 2 UUIDs: unpaired-answer query returns 0 AND uncited-context query returns 0 (same two queries as Task 2).</automated>
  </verify>
  <acceptance_criteria>
    - 2 migration files exist (638, 639), each wrapped in BEGIN;/COMMIT;
    - Every value is a float literal; every sources array double-cast ::text[]::text[]
    - Unpaired-answer query returns 0 for both UUIDs
    - Uncited-context query returns 0 for both UUIDs
    - No INSERT references an external_id outside the -2537490001 to -2537490012 range
  </acceptance_criteria>
  <done>Migrations 638–639 written, applied, and verified for McClain, Net.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (mcp__supabase-local) | Writes go directly to live production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | stance value integrity | mitigate | Evidence-only rule: no INSERT without a direct cited source; per-person uncited-context query must return 0 |
| T-123-02 | Information Disclosure | reasoning text / sources | accept | All data is public-record political positions with public source URLs; no PII |
| T-123-03 | Repudiation | stance provenance | mitigate | Every value paired with a context row carrying path-bearing source URLs (100% citation rate) |
| T-123-04 | Tampering | out-of-scope external_id | mitigate | Acceptance criteria asserts no external_id outside -2537490001 to -2537490012 appears in any migration |
| T-123-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
After all three tasks, run the partial phase-wide citation check for this plan's officials:
`SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2537490005 AND -2537490001) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` must return 0.
</verification>

<success_criteria>
- Wave 0 pre-flight complete; starting migration number (635) and topic count (44) confirmed
- Migrations 635–639 (5 officials) written, applied to production, and verified
- 0 unpaired answers, 0 uncited contexts across all 5 officials
- No out-of-scope external_ids touched
- Blank-spoke officials documented in SUMMARY (not defaulted to 3.0)
</success_criteria>

<output>
Create `.planning/phases/123-ma-tier-3-stances-wave-2/123-01-SUMMARY.md` when done. Record: confirmed starting migration number, active topic count, all 12 Lynn UUIDs (for Plans 02-05), per-official stance counts, and any blank-spoke officials.
</output>
