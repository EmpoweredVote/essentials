---
phase: 123-ma-tier-3-stances-wave-2
plan: 03
type: execute
wave: 3
depends_on: [123-02]
files_modified:
  - C:/EV-Accounts/backend/migrations/647_mitchell_stances.sql
  - C:/EV-Accounts/backend/migrations/648_abreu_stances.sql
  - C:/EV-Accounts/backend/migrations/649_burgo_stances.sql
  - C:/EV-Accounts/backend/migrations/650_carney_stances.sql
  - C:/EV-Accounts/backend/migrations/651_gomes_stances.sql
  - C:/EV-Accounts/backend/migrations/652_roy_stances.sql
autonomous: true
requirements: [NEWBED-03]
must_haves:
  truths:
    - "New Bedford Mayor Mitchell has evidence-only stance rows (or documented blank if no public record)"
    - "New Bedford At-Large councillors Abreu, Burgo, Carney, Gomes, Roy each have evidence-only stance rows or documented blanks"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
    - "Wave 0 pre-flight for New Bedford resolves all 12 New Bedford UUIDs before any stance is written"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/647_mitchell_stances.sql"
      provides: "Mayor Mitchell stance migration (or zero-stance ledger file)"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/652_roy_stances.sql"
      provides: "At-Large councillor Roy stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Resolve New Bedford UUIDs, then research and apply evidence-only compass stances for New Bedford Mayor Mitchell + At-Large councillors Abreu, Burgo, Carney, Gomes, Roy — migrations 647–652, one politician at a time.

Purpose: Begin closing NEWBED-03 after Lynn is fully complete.
Output: 6 per-individual stance migration files (647–652), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-RESEARCH.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md
@.planning/phases/123-ma-tier-3-stances-wave-2/123-02-SUMMARY.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 574_boston_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs
</interfaces>

New Bedford full roster (external_id → migration), from Phase 120 deep seed (migration 587):
  -2545000001 Jon Mitchell (Mayor)              → 647
  -2545000002 Ian Abreu (At-Large)              → 648
  -2545000003 Shane Burgo (At-Large)            → 649
  -2545000004 Naomi Carney (At-Large)           → 650
  -2545000005 Brian Gomes (At-Large)            → 651
  -2545000006 James Roy (At-Large)              → 652
  -2545000007 Leo Choquette (Ward 1)            → 653
  -2545000008 Scott Pemberton (Ward 2)          → 654
  -2545000009 Shawn Oliver (Ward 3)             → 655
  -2545000010 Derek Baptiste (Ward 4)           → 656
  -2545000011 Joseph Lopes (Ward 5)             → 657
  -2545000012 Ryan Pereira (Ward 6, President)  → 658

Out-of-scope guard: Do NOT write stances for any external_id outside the -2545000001 to -2545000012 range. NEWBED-03 covers Mayor + City Council only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: New Bedford UUID pre-flight + Mayor Mitchell + At-Large Abreu, Burgo (647–649)</name>
  <files>C:/EV-Accounts/backend/migrations/647_mitchell_stances.sql, C:/EV-Accounts/backend/migrations/648_abreu_stances.sql, C:/EV-Accounts/backend/migrations/649_burgo_stances.sql</files>
  <read_first>
    - .planning/phases/123-ma-tier-3-stances-wave-2/123-02-SUMMARY.md (total Lynn row count, confirmed applied migration number after 646)
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format: BEGIN/COMMIT, float literals, dollar-quoting)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (authoritative on ::text[]::text[] double-cast)
  </read_first>
  <action>
    First, resolve all 12 New Bedford UUIDs via: `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id BETWEEN -2545000012 AND -2545000001 ORDER BY external_id DESC;` — confirm name-to-external_id mapping against the roster table above and capture all 12 UUIDs for Plans 03 and 04. Also confirm next migration is 647: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations;` — expected 646; if higher, use MAX+1 and record in SUMMARY.

    Then process ONE politician at a time, strictly sequentially — never run two research sessions in parallel. For each of Mitchell (-2545000001, Mayor), Abreu (-2545000002, At-Large), Burgo (-2545000003, At-Large):
    (a) Research the official's public record across ALL active compass topics — campaign statements, votes, newbedford-ma.gov, local news (New Bedford Light, SouthCoast Today), state legislature records where applicable. Place each topic with direct evidence on the 1.0–5.0 scale.
    (b) Write the migration file using the canonical template: value is a float literal (e.g. 2.0); sources is ARRAY[...]::text[]::text[]; wrap in BEGIN;/COMMIT;; reasoning in $$...$$ citing the specific source; include the 44-topic UUID reference block in the file header.
    (c) For any topic with NO direct evidence, write NO INSERT — never default to 3.0.
    (d) Apply via mcp__supabase-local__execute_sql, then immediately run both per-person verification queries.
    Mayor Mitchell is a long-serving mayor (since 2012) — expect a richer record (10–18 stances); Abreu and Burgo are At-Large councillors with multi-year records. Key New Bedford topics: housing, economic-development, public-safety-approach, local-environment, growth-and-development, transportation-priorities, homelessness. Do NOT touch any external_id outside -2545000001 to -2545000012.
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{uuid}' AND pc.politician_id IS NULL;` returns 0, AND `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - All 12 New Bedford UUIDs resolved and recorded for use in Plans 03 and 04
    - Next migration number confirmed as 647 (or higher if DB shows later applied migration)
    - 3 migration files exist (647, 648, 649), each wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`; no bare-integer value lines
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT references an external_id outside the -2545000001 to -2545000012 range
  </acceptance_criteria>
  <done>New Bedford UUIDs resolved; migrations 647–649 written, applied, and verified (0 unpaired, 0 uncited) for Mitchell, Abreu, Burgo.</done>
</task>

<task type="auto">
  <name>Task 2: At-Large Carney, Gomes, Roy (650–652)</name>
  <files>C:/EV-Accounts/backend/migrations/650_carney_stances.sql, C:/EV-Accounts/backend/migrations/651_gomes_stances.sql, C:/EV-Accounts/backend/migrations/652_roy_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
    - C:/EV-Accounts/backend/migrations/647_mitchell_stances.sql (just-written sibling file as in-phase reference)
  </read_first>
  <action>
    Process ONE politician at a time, sequentially, for Carney (-2545000004, At-Large), Gomes (-2545000005, At-Large), Roy (-2545000006, At-Large). Use UUIDs resolved in Task 1. Same procedure: research public record across all active topics, write canonical migration (float values, double-cast sources, BEGIN/COMMIT, $$ reasoning, 44-topic UUID header), omit topics with no evidence (no 3.0 defaults), apply, verify per-person before next. Brian Gomes is a long-serving at-large councillor — expect a richer record; Carney and Roy may be thinner. Do NOT touch any external_id outside -2545000001 to -2545000012.
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: unpaired-answer query returns 0 AND uncited-context query returns 0 (same two queries as Task 1).</automated>
  </verify>
  <acceptance_criteria>
    - 3 migration files exist (650, 651, 652), each wrapped in BEGIN;/COMMIT;
    - Every value float literal; every sources array double-cast ::text[]::text[]
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT references an external_id outside the -2545000001 to -2545000012 range
  </acceptance_criteria>
  <done>Migrations 650–652 written, applied, and verified for Carney, Gomes, Roy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Evidence converted to SQL; risk of fabricated/uncited claims |
| migration SQL → production DB | Writes go directly to live production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | stance value integrity | mitigate | Evidence-only rule; per-person uncited-context query must return 0 |
| T-123-02 | Information Disclosure | reasoning / sources | accept | Public-record positions with public URLs; no PII |
| T-123-03 | Repudiation | stance provenance | mitigate | Every value paired with a path-bearing source URL |
| T-123-04 | Tampering | out-of-scope external_id | mitigate | Acceptance criteria asserts no external_id outside -2545000001 to -2545000012 |
| T-123-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
Partial citation check for this plan's officials:
`SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2545000006 AND -2545000001) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` must return 0.
</verification>

<success_criteria>
- All 12 New Bedford UUIDs resolved and recorded for Plans 03-05
- Migrations 647–652 (6 officials) written, applied to production, and verified
- 0 unpaired answers, 0 uncited contexts across all 6 officials
- No out-of-scope external_ids touched
- Blank-spoke officials documented in SUMMARY (not defaulted to 3.0)
</success_criteria>

<output>
Create `.planning/phases/123-ma-tier-3-stances-wave-2/123-03-SUMMARY.md` when done. Record: all 12 New Bedford UUIDs, per-official stance counts for migrations 647–652, and any blank-spoke officials.
</output>
