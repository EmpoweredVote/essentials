---
phase: 90-post-election-follow-up-minicompass-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx
  - C:/ev-ui/ev-ui-main/package.json
autonomous: true
requirements:
  - UI-01
user_setup: []

must_haves:
  truths:
    - "Local ev-ui clone at C:/ev-ui/ev-ui-main is at 0.9.2 codebase or later (NOT the stale 0.8.12 state)"
    - "RadarChartCore.jsx accepts a new dotRadius prop with default 5 that preserves prior visual behavior"
    - "The dot <circle> r= attributes in RadarChartCore.jsx are derived from the dotRadius prop, not hardcoded literals"
    - "A new ev-ui patch version (≥0.9.3) is published to npm via the v*.*.* tag push workflow"
    - "After publish, npm view @empoweredvote/ev-ui version returns the new published version (≥0.9.3)"
  artifacts:
    - path: "C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx"
      provides: "RadarChartCore component with dotRadius prop"
      contains: "dotRadius"
    - path: "C:/ev-ui/ev-ui-main/package.json"
      provides: "Version bump to 0.9.3 (or higher patch)"
      contains: "\"version\""
  key_links:
    - from: "C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx"
      to: "<circle r= attribute on user dot and compare dot>"
      via: "dotRadius prop"
      pattern: "r=\\{.*dotRadius"
    - from: "git tag v0.9.3 push"
      to: "GitHub Actions publish.yml (OIDC trusted publishing)"
      via: "tags: 'v*.*.*' trigger"
      pattern: "v0\\.9\\.[3-9]"
---

<objective>
Patch the project-owned `@empoweredvote/ev-ui` library so that `RadarChartCore` exposes a configurable `dotRadius` prop, then publish the patched version to npm. This unblocks Plan 90-02, which will consume the new prop from `MiniCompass.jsx` to satisfy UI-01.

Purpose: ev-ui 0.9.2 ships `showLabels={false}` (which already solves UI-02 path) but its dot circles are hardcoded at `r=7` (and `r=8` for matched dots), with NO prop to override. UI-01 requires dots ~50% smaller than current. The only correct fix is a prop addition in the library — not a CSS hack, not a custom overlay layer.

Output:
- Local `C:/ev-ui/ev-ui-main` refreshed from GitHub to 0.9.2 codebase (current local clone is stale at 0.8.12)
- `RadarChartCore.jsx` modified to accept `dotRadius` (default 5) and use it for both user and compare dot circles
- `package.json` bumped to next patch (0.9.3 or higher)
- Tagged commit `v0.9.3` pushed to GitHub — CI auto-publishes to npm via OIDC trusted publishing (no OTP needed)
- `npm view @empoweredvote/ev-ui version` reports the new version
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-CONTEXT.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-RESEARCH.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh local ev-ui clone to 0.9.2 codebase</name>
  <files>C:/ev-ui/ev-ui-main/</files>
  <read_first>
    - C:/ev-ui/ev-ui-main/package.json (current local version — should be 0.8.12 per RESEARCH.md Pitfall 5)
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-RESEARCH.md (Pitfall 5 — stale local clone; A4 in Assumptions Log)
    - C:/Users/Chris/.claude/projects/C--Transparent-Motivations-essentials/memory/project_evui_workflow.md (project memory — ev-ui repo workflow; local clone has no .git, must re-clone)
  </read_first>
  <action>
    Bring the local C:/ev-ui/ev-ui-main directory up to the 0.9.2 published source so subsequent edits in this plan layer on top of `showLabels` and current dot rendering — NOT the stale 0.8.12 baseline.

    Per RESEARCH.md A4 + Pitfall 5 + project memory `project_evui_workflow.md`: the local clone has no .git, so a normal `git pull` is impossible. Recover via fresh clone:

    1. Verify staleness with the current package.json (per `read_first`) — expect `"version": "0.8.12"`. If it already shows 0.9.2 or higher, skip the re-clone and proceed to task 2.
    2. Move the existing `C:/ev-ui/ev-ui-main/` aside (rename to `C:/ev-ui/ev-ui-main.stale-0.8.12.bak/`) to preserve any local-only files for inspection.
    3. `git clone https://github.com/EmpoweredVote/ev-ui.git C:/ev-ui/ev-ui-main` (per `project_evui_workflow.md` clone URL).
    4. Verify the refreshed `package.json` reports `0.9.2` (or whatever is current on `main`).
    5. Confirm `src/RadarChartCore.jsx` now contains the `showLabels` prop (it must — per RESEARCH.md `showLabels` was added in 0.9.2). If not, the clone is wrong — stop and surface to user.
    6. Run `npm ci` and `npm run build` inside the refreshed clone to confirm the project builds cleanly before any edits.

    Do NOT edit RadarChartCore.jsx in this task. This task is solely about refreshing the working tree.
  </action>
  <verify>
    <automated>node -p "require('C:/ev-ui/ev-ui-main/package.json').version" — output must satisfy semver ^0.9.2 (i.e., 0.9.2 or higher pre-publish bump); grep "showLabels" C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx — must return at least one match; (cd C:/ev-ui/ev-ui-main && npm run build) must exit 0</automated>
  </verify>
  <acceptance_criteria>
    - C:/ev-ui/ev-ui-main/package.json `version` field reads "0.9.2" (or higher, if the npm registry has advanced since 2026-06-04)
    - C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx contains the literal token `showLabels`
    - C:/ev-ui/ev-ui-main/.git directory exists (proves it was re-cloned, not just renamed)
    - `npm run build` in C:/ev-ui/ev-ui-main exits 0
    - C:/ev-ui/ev-ui-main.stale-0.8.12.bak/ exists and contains the prior local files (no destructive loss)
  </acceptance_criteria>
  <done>Refreshed clone is at 0.9.2 source, contains showLabels prop, builds cleanly, and the stale 0.8.12 working tree has been preserved as a .bak directory.</done>
</task>

<task type="auto">
  <name>Task 2: Add dotRadius prop to RadarChartCore and bump version</name>
  <files>C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx, C:/ev-ui/ev-ui-main/package.json</files>
  <read_first>
    - C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx (refreshed 0.9.2 source from Task 1 — current dot rendering uses hardcoded r=7/r=8 per RESEARCH.md Code Examples section "Verified 0.9.2 dot sizes")
    - C:/ev-ui/ev-ui-main/package.json (current version, must bump)
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-PATTERNS.md (RadarChartCore section — exact prop signature shape and dot rendering analog)
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-RESEARCH.md (Pattern 3: dotRadius prop — default=5; user/compare dot rendering pattern)
  </read_first>
  <action>
    Add a configurable `dotRadius` prop to RadarChartCore so consumers can shrink the dots. This addresses UI-01 once Plan 90-02 wires `dotRadius={2.5}` from MiniCompass.

    Concrete changes in `C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx`:
    - Add `dotRadius = 5` to the destructured props signature (alongside the existing `size`, `labelFontSize`, `padding`, `showLabels`, etc. — find the same destructure block referenced in PATTERNS.md "Props signature" subsection of the RadarChartCore section).
    - Replace BOTH hardcoded `r={…}` literals on the dot `<circle>` elements (the user dot AND the compare dot — both currently render at the same hardcoded values per RESEARCH.md). The new attribute value must derive from `dotRadius` while preserving the matched/unmatched ratio currently present in 0.9.2 source: i.e., if 0.9.2 renders `r={matches ? 8 : 7}`, the new attribute must be `r={matches ? dotRadius * (8/7) : dotRadius}`. If 0.9.2 already collapsed both to a single literal, use `r={dotRadius}` for both. Planner discretion per 90-RESEARCH.md is to use either formula — match the structure already present in the refreshed source.
    - Do NOT touch `strokeWidth`, fill colors, `pointerEvents`, or any other dot styling.
    - Do NOT touch the `showLabels` / `padding` / `minPadding` logic — it is correct as-is in 0.9.2.

    Then bump the version in `C:/ev-ui/ev-ui-main/package.json`:
    - Change `"version"` from the current value to the next patch (`"0.9.3"` if currently `"0.9.2"`). If the registry has advanced past 0.9.2 since 2026-06-04, bump to the next available patch beyond the current `main` version.

    Verify the build still passes: `npm run build` inside C:/ev-ui/ev-ui-main. Confirm via `grep` that the built `dist/index.mjs` contains the new `dotRadius` token (CI will rebuild on tag push too, but local build proves source compiles).

    Do NOT commit or push in this task — Task 3 handles commit + tag push so failures here don't pollute git history.
  </action>
  <verify>
    <automated>grep -c "dotRadius" C:/ev-ui/ev-ui-main/src/RadarChartCore.jsx — must return ≥ 3 (prop default, user dot r=, compare dot r=); node -p "require('C:/ev-ui/ev-ui-main/package.json').version" — must report a version strictly greater than the value before this task (e.g., 0.9.3); (cd C:/ev-ui/ev-ui-main && npm run build) must exit 0; grep -c "dotRadius" C:/ev-ui/ev-ui-main/dist/index.mjs — must return ≥ 1 after build</automated>
  </verify>
  <acceptance_criteria>
    - src/RadarChartCore.jsx props destructure includes `dotRadius = 5` (or `dotRadius=5`)
    - Both `<circle>` dot rendering blocks reference `dotRadius` in their `r={…}` attribute — no literal `r={7}`, `r={8}`, `r={5}`, or `r={6}` remains on user/compare dot elements
    - package.json `version` strictly greater than its value at the start of this task (e.g., `0.9.2` → `0.9.3`)
    - `npm run build` exits 0 and dist/index.mjs contains the string `dotRadius`
    - No changes to label/padding/showLabels logic — `git diff` shows changes ONLY to the dot `<circle>` r= attributes, the destructured props block, and package.json version
  </acceptance_criteria>
  <done>RadarChartCore.jsx accepts dotRadius prop (default 5), both user and compare dots derive their r from the prop, package.json is bumped to next patch, and the library builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Commit, tag, and push to publish via CI</name>
  <files>C:/ev-ui/ev-ui-main/ (git working tree)</files>
  <read_first>
    - C:/ev-ui/ev-ui-main/.github/workflows/publish.yml (CI workflow — triggers on tag `v*.*.*` push; uses OIDC trusted publishing; verifies package.json version matches tag)
    - C:/Users/Chris/.claude/projects/C--Transparent-Motivations-essentials/memory/project_evui_workflow.md (project memory — CI auto-publishes on tag push; no OTP required)
    - C:/ev-ui/ev-ui-main/package.json (must read current bumped version to construct matching tag name)
  </read_first>
  <action>
    Publish the dotRadius patch to npm by pushing a matching git tag. CI does the rest.

    Per publish.yml: the workflow runs on `push` to tags matching `v*.*.*`. It checks out, validates that `package.json` version matches the tag name (without the `v` prefix), runs `npm ci` + `npm run build`, then `npm publish --access public` via OIDC trusted publishing. No npm token or OTP is needed locally.

    Steps:
    1. From inside `C:/ev-ui/ev-ui-main`: `git add src/RadarChartCore.jsx package.json` (do not bulk-add; the only files changed in Task 2 are these two).
    2. Commit with message: `feat(RadarChartCore): add dotRadius prop for compact contexts (MiniCompass)` followed by a brief body referencing Phase 90 UI-01.
    3. Read the bumped version from package.json (e.g., `0.9.3`) and create a matching annotated tag: `git tag -a v<version> -m "ev-ui v<version> — dotRadius prop"`. The tag MUST be exactly `v` + the package.json version (the CI step "Verify package.json version matches tag" will fail otherwise).
    4. `git push origin main` to push the commit, then `git push origin v<version>` to push the tag.
    5. Wait for the GitHub Actions publish workflow to complete. Either poll with `gh run list --repo EmpoweredVote/ev-ui --workflow publish.yml --limit 1` and `gh run watch <id>`, or wait up to ~5 minutes and verify via `npm view @empoweredvote/ev-ui version` returning the new version.

    Do NOT modify `C:/Transparent Motivations/essentials/package.json` or run `npm install` here — Plan 90-02 owns those changes.
    Do NOT skip hooks or sign-bypass any commit (no `--no-verify`).
    Do NOT force-push, do NOT reset.
  </action>
  <verify>
    <automated>cd C:/ev-ui/ev-ui-main && git log -1 --pretty=%s — must contain "dotRadius"; git tag --list "v0.9.*" — must list the new vX.Y.Z matching package.json version; npm view @empoweredvote/ev-ui version — must return a version that matches the bumped package.json (note: there is a CI propagation delay; verification may need a retry within 5 minutes of tag push)</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1` in C:/ev-ui/ev-ui-main shows the new commit with `dotRadius` in the subject line, authored by the current user
    - `git tag --list` includes a `vX.Y.Z` tag whose `Z` matches the bumped package.json patch number
    - `git ls-remote origin refs/tags/vX.Y.Z` returns a non-empty result (tag is pushed)
    - `npm view @empoweredvote/ev-ui version` returns the bumped version (within ~5 minutes of the tag push completing CI)
    - GitHub Actions "Publish to npm and dispatch to consumers" run for this tag completes with status `success` (verify via `gh run list --workflow publish.yml --repo EmpoweredVote/ev-ui --limit 1`)
  </acceptance_criteria>
  <done>The dotRadius patch is committed, tagged, pushed, and successfully published to npm under @empoweredvote/ev-ui — verified by `npm view`. Plan 90-02 can now run `npm install` and consume the new version.</done>
</task>

</tasks>

<verification>
- Local clone at C:/ev-ui/ev-ui-main is on the 0.9.2 (or newer) `main` branch with a real .git directory.
- RadarChartCore.jsx has a `dotRadius` prop (default 5), used for both user and compare dot `<circle>` r= attributes.
- package.json version is strictly greater than 0.9.2 (e.g., 0.9.3).
- A `vX.Y.Z` git tag matching the package.json version is pushed to origin.
- `npm view @empoweredvote/ev-ui version` returns the new published version.
- GitHub Actions publish workflow for this tag has status `success`.
- No edits to `showLabels` / `padding` / `minPadding` logic — those already work as-shipped in 0.9.2.
</verification>

<success_criteria>
- UI-01 prop is in place at the library level. Consumers can pass `dotRadius={N}` to RadarChartCore.
- The newly published ev-ui patch is available for Plan 90-02 to install in the essentials repo.
- No regression in the existing showLabels/padding/label behavior introduced by 0.9.2.
</success_criteria>

<output>
Create `.planning/phases/90-post-election-follow-up-minicompass-ui/90-01-SUMMARY.md` when done. Include: actual published version, git commit SHA, tag name, npm view confirmation timestamp, and any unexpected build/CI surprises.
</output>
