---
phase: 160-nevada-legislature-seed-headshots
plan: 03
type: execute
wave: 3
depends_on: ["160-01", "160-02"]
files_modified:
  - .planning/phases/160-nevada-legislature-seed-headshots/160-03-SUMMARY.md
autonomous: false
requirements: [NV-LEG-01, NV-LEG-02]
user_setup: []

must_haves:
  truths:
    - "21 State Senators route from SLDU addresses; 42 Assembly members route from SLDL addresses"
    - "All 63 legislators (minus documented gaps) have a CDN-served 600x750 headshot"
    - "No compass stances exist for any of the 63 legislators (deferred — verified absent)"
    - "Legislature districts are state='nv' lowercase; zero section-split defects; ledger MAX=1053"
  artifacts:
    - path: ".planning/phases/160-nevada-legislature-seed-headshots/160-03-SUMMARY.md"
      provides: "Recorded results for all 9 SQL/HTTP verification checks + the address-routing/correct-person human-verify outcome"
      contains: "NV-LEG"
  key_links:
    - from: "63 legislator offices"
      to: "STATE_UPPER/STATE_LOWER districts at state='nv'"
      via: "address → /representatives/me routing"
      pattern: "state = 'nv'"
    - from: "63 legislators"
      to: "inform.politician_answers"
      via: "stance-absence assertion (expect 0)"
      pattern: "politician_answers"
---

<objective>
Prove the full phase goal end-to-end: any NV address returns its correct State Senator + Assemblymember, each with a headshot, with zero stances and zero section-split. This plan creates NO data — it runs the 9 SQL/HTTP verification checks from 160-VALIDATION.md inline and records the results, then a human checkpoint confirms address routing + correct-person headshots.

Purpose: Closes NV-LEG-01 + NV-LEG-02 by confirming all 4 phase success criteria are TRUE.
Output: 160-03-SUMMARY.md with recorded query results + the human-verify outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — this is an INLINE-ORCHESTRATOR verification plan.** gsd-executor has NO supabase MCP, so every SQL query and browse/curl check below is performed inline by execute-phase, not delegated to an executor subagent. The "executor" deliverable is the recorded SUMMARY of results.
</execution_context>

<context>
@.planning/phases/160-nevada-legislature-seed-headshots/160-RESEARCH.md
@.planning/phases/160-nevada-legislature-seed-headshots/160-PATTERNS.md
@.planning/phases/160-nevada-legislature-seed-headshots/160-VALIDATION.md

<interfaces>
<!-- Identifiers for the verification queries. -->
State of Nevada government geo_id='32'
Senate chamber name='Nevada State Senate'; Assembly chamber name='Nevada Assembly'
Senate external_ids -3203001..-3203021; Assembly external_ids -3204001..-3204042 (full range -3204042..-3203001)
Legislature district casing: state='nv' lowercase; district_type STATE_UPPER (21) / STATE_LOWER (42)
Stance table: inform.politician_answers
CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
Browse link (surfacing review): essentials.empowered.vote/results?browse_state_officials=NV
Section-Split + per-check SQL: 160-VALIDATION.md "Per-Task Verification Map" + "Section-Split Verification SQL"
Documented headshot gaps (if any): see 160-02-SUMMARY.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the 9 SQL/HTTP verification checks inline and record results</name>
  <files>.planning/phases/160-nevada-legislature-seed-headshots/160-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/160-nevada-legislature-seed-headshots/160-VALIDATION.md (Per-Task Verification Map — all 9 checks + Section-Split SQL + address-routing spot check)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; the exact SQL per check)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-02-SUMMARY.md (documented headshot gaps, if any, to subtract from the 63 count)
  </read_first>
  <action>
INLINE ORCHESTRATOR — run each check via mcp__supabase-local__execute_sql (or curl for HTTP) and record the result in 160-03-SUMMARY.md:
(1) Senate count — offices joined to chamber 'Nevada State Senate' under geo_id='32' → expect 21. Record.
(2) Assembly count — same with chamber 'Nevada Assembly' → expect 42. Record.
(3) District linkage — offices→districts GROUP BY district_type for the two chambers → expect STATE_UPPER 21, STATE_LOWER 42. Record.
(4) Headshots present — politician_images (type='default') for external_ids BETWEEN -3204042 AND -3203001 → expect 63 minus documented gaps (from 160-02-SUMMARY.md). Record count + any gaps.
(5) Headshots serve — curl -sI each CDN url (or a representative spot-check across both chambers) → 200. Record.
(6) Stances absent — `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -3204042 AND -3203001)` → expect 0 (SC#4). Record.
(7) Casing correct — DISTINCT state on districts linked to the two chambers → expect only 'nv'. Record.
(8) Section-split — run the exact STATE_UPPER/STATE_LOWER section-split SQL from 160-VALIDATION.md → expect 0 rows. Record.
(9) Ledger — `SELECT version FROM the migration ledger WHERE version IN ('1053','1054')` → expect only 1053 (1054 NOT registered); MAX=1053. Record.
Flag ANY deviation (count below 21/42, a non-'nv' state, any stance row, any extra ledger entry) as a gap for a follow-up gap-closure plan.
  </action>
  <verify>
    <automated>F=".planning/phases/160-nevada-legislature-seed-headshots/160-03-SUMMARY.md"; test -f "$F" &amp;&amp; grep -q "Senate" "$F" &amp;&amp; grep -q "Assembly" "$F" &amp;&amp; grep -qi "section-split" "$F" &amp;&amp; grep -qi "stance" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Check (1) recorded: Senate office count = 21.
    - Check (2) recorded: Assembly office count = 42.
    - Check (3) recorded: STATE_UPPER 21 + STATE_LOWER 42 linkage.
    - Check (4) recorded: politician_images count = 63 minus documented gaps; gaps (if any) listed.
    - Check (5) recorded: CDN headshots return HTTP 200 (spot-check across both chambers).
    - Check (6) recorded: 0 stance rows for the 63 legislators (SC#4 — verified absent).
    - Check (7) recorded: linked districts show only state='nv' lowercase.
    - Check (8) recorded: section-split query returns 0 rows.
    - Check (9) recorded: only ('1053') registered; ledger MAX=1053 (1054 not registered).
    - Any deviation flagged for gap closure.
  </acceptance_criteria>
  <done>All 9 checks recorded in 160-03-SUMMARY.md with expected results; any deviation flagged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify NV legislators route by address with correct-person headshots</name>
  <action>Pause for human verification. Present the what-built summary and the how-to-verify steps below; wait for the resume signal before marking the phase complete. No code or DB changes in this task.</action>
  <what-built>Seeded all 63 NV legislators (21 Senate / 42 Assembly) under State of Nevada with offices linked to the existing SLDU/SLDL districts, plus 600x750 headshots (minus any documented genuine gaps). All 9 SQL/HTTP audits passed inline (21/42 counts, district linkage, 63 headshots CDN-200, 0 stances, casing 'nv', 0 section-split, ledger MAX=1053).</what-built>
  <how-to-verify>
1. Address routing (Las Vegas) — open the app and enter a Las Vegas address. Confirm the returned State Senator + Assemblymember match the SLDU/SLDL the address falls in, each with a headshot. Legislators are address-routed (not in the statewide list).
2. Address routing (Reno / rural) — enter a Reno address and a rural northern-NV address (e.g. Elko). Confirm the correct senator + assembly member resolve in each, with headshots.
3. Correct-person headshots — spot-check a sample of senators + assembly members (include Doñate SD-10, González AD-16, Zermeño AD-11 to confirm diacritics + correct person). Confirm each photo is the right person, 4:5, no text/graphic overlay, not a placeholder.
4. Documented gaps — confirm any legislator listed as a genuine headshot gap in 160-02-SUMMARY.md is genuinely sourceless (not a pipeline miss), and renders cleanly without a photo.
5. Surfacing — optionally review essentials.empowered.vote/results?browse_state_officials=NV.
  </how-to-verify>
  <verify><human-check>Operator confirms correct-person headshots (no overlay/placeholder) and correct address routing for Las Vegas + Reno + a rural address per the steps above.</human-check></verify>
  <done>Human approves; or issues are logged and routed to a gap-closure plan.</done>
  <resume-signal>Type "approved" or describe any wrong-person photo, missing/mis-routed legislator, or routing issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Read-only verification plan; no writes, no external input crosses a boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-07 | Repudiation | unverified claim of completion | mitigate | All 9 checks recorded with actual query results in SUMMARY; human checkpoint confirms routing + correct-person photos |
| T-160-08 | Tampering | data-integrity drift (section-split / stray stance) | mitigate | Section-split SQL gate (0 rows) + stance-absence assertion (0 rows) run inline |
| T-160-SC | Tampering | npm/pip/cargo installs | accept | No installs (verification-only); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
All four phase success criteria proven:
1. 21 Senators route from SLDU addresses (check 1 + check 3 + browse step 1-2).
2. 42 Assembly members route from SLDL addresses (check 2 + check 3 + browse step 1-2).
3. 63 legislators have CDN-served 600x750 headshots, gaps documented (checks 4-5 + browse step 3-4).
4. 0 compass stances for legislators (check 6 — SC#4 verified absent).
Cross-cutting: casing 'nv' (check 7), 0 section-split (check 8), ledger MAX=1053 (check 9).
</verification>

<success_criteria>
- 160-03-SUMMARY.md records all 9 SQL/HTTP checks with expected results.
- Human checkpoint approves correct-person headshots (no overlay) and address routing for Las Vegas + Reno + a rural address.
- Any deviation routed to a gap-closure plan.
</success_criteria>

<output>
Create `.planning/phases/160-nevada-legislature-seed-headshots/160-03-SUMMARY.md` when done.
</output>
