---
phase: 153-inglewood-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1018_inglewood_reconcile.sql
autonomous: true
requirements: [INGL-01]

must_haves:
  truths:
    - "City of Inglewood government carries geo_id 0636546 so the browse route resolves it"
    - "Exactly one 'City Council' chamber exists under the Inglewood government"
    - "Every office under the survivor chamber is bidirectionally linked to its politician"
    - "The duplicate Eloy Morales record is unlinked (not deleted) with its photo migrated to the survivor"
    - "The four council seats carry District 1-4 labels (not 'At-Large') and the Mayor stays a directly-elected LOCAL_EXEC office"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1018_inglewood_reconcile.sql"
      provides: "geo_id backfill + dual-chamber merge + link repair + Eloy dedup + by-district relabel"
      contains: "inglewood_reconcile"
  key_links:
    - from: "essentials.governments af811c4b"
      to: "browse route geo_id 0636546"
      via: "geo_id backfill"
      pattern: "geo_id = '0636546'"
    - from: "essentials.offices (survivor chamber a25a6dea)"
      to: "essentials.politicians"
      via: "bidirectional office_id <-> politician_id"
      pattern: "office_id IS DISTINCT FROM"
---

<objective>
Wave 1 of the Inglewood deep-seed: reconcile the structural defects on the City of Inglewood
government (`af811c4b-e4da-4f30-ac33-9a7fe7d434ba`) in ONE structural migration
(`1018_inglewood_reconcile.sql`, registers in `supabase_migrations.schema_migrations`).

This is the messiest reconcile in the 142-152 series: NULL geo_id, TWO duplicate 'City Council'
chambers, three one-directional links, a same-person duplicate (Eloy Morales seeded twice), and
At-Large labels that must become District 1-4 while keeping the directly-elected Mayor as-is.

Purpose: a single clean government + chamber + correctly-linked roster skeleton that Wave 2 finalizes.
Output: `1018_inglewood_reconcile.sql` applied live via Supabase MCP, committed to EV-Accounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-inglewood-deep-seed/153-CONTEXT.md
@.planning/phases/153-inglewood-deep-seed/153-RESEARCH.md
@.planning/phases/153-inglewood-deep-seed/153-PATTERNS.md
@.planning/phases/153-inglewood-deep-seed/153-VALIDATION.md

<reference_data>
Gov UUID: af811c4b-e4da-4f30-ac33-9a7fe7d434ba | geo_id: 0636546 | state: CA
SURVIVOR chamber: a25a6dea-7f26-4f5e-bc6a-2a5d321063d5 (official_count 5, 3 bidirectional offices)
DOOMED   chamber: 8b99bcf0-813d-459a-b7e1-f82e12080ffc (official_count NULL, 3 one-directional offices)
Both chambers share name 'City Council' / slug 'inglewood-city-council' — TARGET BY UUID ONLY.

SURVIVOR offices (bidirectional-clean):
  Eloy Morales 666263  pol 6ed19c10-7b34-47f0-8705-0d154271e362  office ddcd280b  -> District 3 (dedup survivor)
  Dionne Faulk 666264  pol 729bc539-3175-4e5d-96ba-c18768890e1e  office 35b92278  -> District 4
  Gloria Gray  666261  pol 7a04bf87...                            office 8e9b0c61  -> District 1

DOOMED offices (one-directional: offices.politician_id set, politicians.office_id NULL):
  James Butts Jr. -200740  pol f5775ca1...  office 90121859  -> Mayor LOCAL_EXEC (KEEP as-is)
  Eloy Morales Jr. -201081 pol ff97a6bb...  office 7fd55592  -> DEDUP dup (migrate photo to 666263, then unlink)
  George Dotson  -201082   pol 3e73448b...  office 6b20a733  -> suspected departed (UNLINK in Wave 2, NOT here)

Eloy dedup confirmed SAME person (RESEARCH CQ-3): keep 666263, migrate -201081 photo, unlink-not-delete -201081.
OUT OF SCOPE (never touch): Inglewood Unified School District gov 3c4c8dca-894e-403f-8935-272989bc3c46.
Migration counter: on-disk MAX 1017 (authoritative) / live schema_migrations MAX 1011 -> next = 1018.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STOP-on-drift pre-flight — re-confirm every Wave-1 invariant + locate Padilla + both migration counters</name>
  <files>(read-only; no file written this task)</files>
  <read_first>
    - .planning/phases/153-inglewood-deep-seed/153-CONTEXT.md §code_context (the live DB pre-check, AUTHORITATIVE on UUIDs/ext_ids)
    - .planning/phases/153-inglewood-deep-seed/153-RESEARCH.md §Common Pitfalls Pitfall 1 (Padilla absent from 6-office list) + §Open Questions 1-3
    - .planning/phases/153-inglewood-deep-seed/153-VALIDATION.md §Wave 0 Requirements (STOP-on-drift list)
  </read_first>
  <action>
    Via `mcp__supabase-local__execute_sql`, run these read-only queries and STOP (raise to operator, do not mutate) on ANY drift from CONTEXT.md §code_context:
    (a) Confirm gov: `SELECT id, geo_id, state FROM essentials.governments WHERE id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba'` — expect geo_id NULL/empty, state CA.
    (b) Confirm both chambers: `SELECT id, name, slug, official_count FROM essentials.chambers WHERE government_id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba'` — expect exactly the two UUIDs a25a6dea + 8b99bcf0, both name 'City Council'.
    (c) Surface ALL offices under the gov (NOT filtered by chamber — this is how Padilla is located, per Pitfall 1): `SELECT o.id office_id, o.chamber_id, o.title, o.district_id, o.district_type, o.politician_id, p.external_id, p.full_name, p.office_id pol_office_id FROM essentials.offices o LEFT JOIN essentials.politicians p ON p.id=o.politician_id WHERE o.government_id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba' ORDER BY o.chamber_id`. Record each office UUID, district_id, link direction, ext_id.
    (d) Locate Padilla explicitly: `SELECT id, external_id, full_name, office_id FROM essentials.politicians WHERE full_name ILIKE '%padilla%'` AND scan the office scan above for a District-2 / Padilla row. Record verdict: PADILLA-PRESENT (with ext_id + office UUID + chamber) or PADILLA-ABSENT. This verdict feeds Wave 2.
    (e) Confirm Eloy duplicate: both -201081 (pol ff97a6bb, office 7fd55592, 1 image) and 666263 (pol 6ed19c10, office ddcd280b, 0 image) resolve as listed.
    (f) Shared-district check on the survivor chamber (Pomona/WC defect): `SELECT district_id, COUNT(*) FROM essentials.offices WHERE chamber_id='a25a6dea-7f26-4f5e-bc6a-2a5d321063d5' GROUP BY district_id HAVING COUNT(*)>1`. ALSO run it across all offices being merged. Record any shared district_id — it changes the relabel SQL in Task 2.
    (g) Resolve the 3 DOOMED office UUIDs concretely (90121859 Butts, 7fd55592 Eloy-Jr, 6b20a733 Dotson) — copy full UUIDs from the scan; PATTERNS lists truncated forms.
    (h) Migration counters: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` (expect 1011) AND list `ls C:/EV-Accounts/backend/migrations/ | tail` for on-disk MAX (expect 1017). If on-disk MAX != 1017, the next number is on-disk MAX + 1 — STOP and report.
    Produce a written pre-flight findings block (every UUID/ext_id/district_id resolved, Padilla verdict, shared-district verdict, both counters). Do NOT proceed to Task 2 if any invariant drifted — surface to operator.
  </action>
  <verify>
    <automated>MISSING — read-only assertion queries via MCP; capture results in the SUMMARY pre-flight block</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba'` returns NULL or '' (un-backfilled).
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba' AND name='City Council'` returns 2.
    - The all-offices scan (c) returns the 6 documented offices; Padilla verdict recorded as PRESENT(ext_id,office) or ABSENT.
    - Shared-district query (f) result recorded (0 rows = no defect; >0 rows = split required in Task 2).
    - `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` returns 1011; on-disk MAX = 1017; next migration confirmed = 1018.
  </acceptance_criteria>
  <done>All Wave-1 invariants re-confirmed against CONTEXT.md; Padilla located-or-absent recorded; shared-district verdict recorded; both counters confirm next=1018; no drift (or drift surfaced to operator and STOP).</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply 1018_inglewood_reconcile.sql (geo_id + chamber merge + link repair + Eloy dedup + by-district relabel)</name>
  <files>C:/EV-Accounts/backend/migrations/1018_inglewood_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1010_west_covina_reconcile.sql (lines 1-128 — THE primary template: header, geo_id backfill 43-47, chamber move-assert-delete 49-64, one-directional link repair 66-75, by-district relabel + shared-district split 77-99, ledger registration 110-113, post-verification 115-128)
    - C:/EV-Accounts/backend/migrations/1000_elmonte_reconcile.sql (lines 28-29, 93-94 — directly-elected Mayor kept-as-is "untouched" comment)
    - .planning/phases/153-inglewood-deep-seed/153-PATTERNS.md §Pattern 1-9 (the per-section mapping with Inglewood UUIDs) + §Pattern 5 (Eloy dedup — NEW, no file precedent; the assembled shape) + §Shared Patterns (IS DISTINCT FROM, districts.label not name, chambers.slug GENERATED, transaction shape)
    - Task 1 pre-flight findings block (resolved office UUIDs, shared-district verdict, Padilla verdict)
  </read_first>
  <action>
    Using the Write tool, author `C:/EV-Accounts/backend/migrations/1018_inglewood_reconcile.sql` following 1010 verbatim in structure, substituting Inglewood identifiers. Operations in order inside a single `BEGIN; ... COMMIT;`:
    (1) geo_id backfill: `UPDATE essentials.governments SET geo_id='0636546' WHERE id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba' AND (geo_id IS NULL OR geo_id='')`.
    (2) Repair the 3 one-directional back-pointers BEFORE the chamber move (so both link directions are live when offices move): `UPDATE essentials.politicians SET office_id='<office>' WHERE id='<pol>' AND office_id IS DISTINCT FROM '<office>'` for Butts (pol f5775ca1 -> office 90121859), Eloy-Jr (pol ff97a6bb -> office 7fd55592), Dotson (pol 3e73448b -> office 6b20a733). Use the FULL office UUIDs resolved in Task 1.
    (3) Eloy dedup (PATTERNS Pattern 5): FIRST migrate the image — `UPDATE essentials.politician_images SET politician_id=(SELECT id FROM essentials.politicians WHERE external_id=666263) WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-201081)`. THEN unlink the dup both directions: `UPDATE essentials.offices SET politician_id=NULL WHERE id='7fd55592...' AND politician_id IS NOT NULL` and `UPDATE essentials.politicians SET office_id=NULL WHERE external_id=-201081 AND office_id IS NOT NULL`. Do NOT delete the dup politician or image row.
    (4) Chamber merge — move the REMAINING doomed offices (Butts office 90121859; Dotson office 6b20a733 — the now-empty 7fd55592 also moves harmlessly) into the survivor by UUID: `UPDATE essentials.offices SET chamber_id='a25a6dea-7f26-4f5e-bc6a-2a5d321063d5' WHERE chamber_id='8b99bcf0-813d-459a-b7e1-f82e12080ffc'`. Then the empty-assert DO-block (RAISE EXCEPTION if doomed still has offices) and `DELETE FROM essentials.chambers WHERE id='8b99bcf0-813d-459a-b7e1-f82e12080ffc'`.
    (5) By-district relabel per the Task-1 district map (Gray office 8e9b0c61 -> District 1, Eloy office ddcd280b -> District 3, Faulk office 35b92278 -> District 4). Use `UPDATE essentials.districts SET label='District N' WHERE id='<district_uuid>' AND label IS DISTINCT FROM 'District N'`, resolving each district_id from Task 1. IF Task 1 found a shared district_id, split it first per 1010 lines 79-88 (INSERT new districts row label/district_type='LOCAL'/geo_id='0636546'/state='CA' guarded NOT EXISTS, repoint the displaced office). District 2 (Padilla) is handled in Wave 2 — do NOT relabel a D2 row here unless Task 1 found Padilla already seated on a survivor-chamber office.
    (6) Keep Mayor Butts as-is: add an explicit comment mirroring El Monte's "untouched" line — office 90121859 stays LOCAL_EXEC title 'Inglewood Mayor', no district_type/label change.
    AFTER `COMMIT;`, register OUTSIDE the transaction: `INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('1018','inglewood_reconcile') ON CONFLICT (version) DO NOTHING`. Append a post-verification comment block adapting 1010 lines 115-128 to Inglewood UUIDs + geo_id 0636546 + the split-section check.
    Then APPLY the migration live via `mcp__supabase-local__apply_migration` (or execute_sql for the whole file). Then commit the file: `git -C "C:/EV-Accounts" add backend/migrations/1018_inglewood_reconcile.sql && git -C "C:/EV-Accounts" commit -m "feat(153): Inglewood reconcile — geo_id + dual-chamber merge + Eloy dedup + by-district relabel"`.
  </action>
  <verify>
    <automated>MISSING — apply via MCP then run the post-apply assertion queries in acceptance_criteria via mcp__supabase-local__execute_sql</automated>
  </verify>
  <acceptance_criteria>
    - geo_id: `SELECT geo_id FROM essentials.governments WHERE id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba'` -> '0636546'
    - one chamber: `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba' AND name='City Council'` -> 1
    - doomed gone: `SELECT COUNT(*) FROM essentials.chambers WHERE id='8b99bcf0-813d-459a-b7e1-f82e12080ffc'` -> 0
    - survivor holds the moved offices: `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='a25a6dea-7f26-4f5e-bc6a-2a5d321063d5'` -> 5 (Gray, Eloy, Faulk, Butts/Mayor, Dotson-still-present-but-Wave2-unlinks; the emptied Eloy-Jr office 7fd55592 also moved). Record the exact count.
    - Eloy dup unlinked: `SELECT office_id FROM essentials.politicians WHERE external_id=-201081` -> NULL; `SELECT politician_id FROM essentials.offices WHERE id='7fd55592...'` -> NULL
    - Eloy survivor has the migrated image: `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id='6ed19c10-7b34-47f0-8705-0d154271e362'` -> 1
    - bidirectional integrity for the three known current survivors: `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id=o.politician_id WHERE o.chamber_id='a25a6dea-7f26-4f5e-bc6a-2a5d321063d5' AND p.external_id IN (666263,666264,666261,-200740) AND p.office_id<>o.id` -> 0
    - district labels: Gray/Eloy/Faulk offices' districts read 'District 1'/'District 3'/'District 4' (no 'At-Large'); shared-district query returns 0 rows
    - split-section check (RESEARCH §Validation): returns 0 rows for gov af811c4b
    - ledger: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` -> 1018; migration file committed to EV-Accounts (git log shows the commit)
  </acceptance_criteria>
  <done>1018_inglewood_reconcile.sql authored, applied live, committed; all acceptance assertions pass; single 'City Council' chamber with the directly-elected Mayor kept as LOCAL_EXEC; Eloy dedup complete; District 1/3/4 relabeled; D2 deferred to Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor SQL -> production Supabase (`mcp__supabase-local` IS production) | Static migration SQL mutates live rows; no external/user input — the surface is data integrity, not injection |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-01 | Tampering | Wrong-row mutation/deletion (two chambers share name+slug) | mitigate | Target chambers/offices/politicians by UUID ONLY (never by name/slug); empty-assert DO-block raises EXCEPTION before chamber DELETE |
| T-153-02 | Tampering | Cross-government contamination (Inglewood Unified 3c4c8dca) | mitigate | Every statement scoped to gov af811c4b or specific UUIDs; school-district gov never referenced |
| T-153-03 | Denial of Service | Re-run destroying/duplicating data | mitigate | Idempotent guards: `geo_id IS NULL OR geo_id=''`, `IS DISTINCT FROM`, `IS NOT NULL`, `ON CONFLICT DO NOTHING`; move-then-assert-then-delete in one transaction |
| T-153-04 | Tampering | Eloy dedup loses the photo or hard-deletes the person | mitigate | Migrate `politician_images.politician_id` BEFORE unlink; unlink-not-delete (null both pointers, KEEP politician + image rows) |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Wave-1 health one-liner (RESEARCH §Quick Run Command): geo_id='0636546', chamber_count=1.
- Split-section check returns 0 rows for gov af811c4b.
- All survivor-chamber offices for current members bidirectional; Eloy -201081 unlinked; Eloy 666263 has 1 image.
- Migration 1018 registered in schema_migrations; file committed to EV-Accounts.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (partial): government + single chamber exist; current officials linked to geo_id 0636546.
- ROADMAP success criterion 2 (partial): by-district structure (D1/D3/D4 relabeled; Mayor LOCAL_EXEC kept) — D2 + official_count finalized in Wave 2.
- INGL-01 structural foundation in place; no duplicate/stale chamber rows.
</success_criteria>

<output>
Create `.planning/phases/153-inglewood-deep-seed/153-01-SUMMARY.md` when done (include the Task-1 pre-flight findings block, the Padilla verdict, the exact post-apply office count, and the shared-district verdict).
</output>
