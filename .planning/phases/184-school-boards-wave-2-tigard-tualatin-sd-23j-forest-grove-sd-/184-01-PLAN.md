---
phase: 184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-probe.sql
autonomous: false
requirements: [WSCH-03, WSCH-04, WSCH-05]
user_setup: []

must_haves:
  truths:
    - "Orchestrator has re-confirmed all THREE G5420 geofences exist with valid geometry and correct names: geo_id 4112240 = 'Tigard-Tualatin School District 23J', geo_id 4105160 = 'Forest Grove School District 15', geo_id 4111290 = 'Sherwood School District 88J' (source='tiger_unsd_or_2024_westmetro', mtfcc='G5420', ST_IsValid=true) — loaded by Phase 174, NOT re-loaded this phase"
    - "Orchestrator has confirmed greenfield: 0 essentials.governments rows named for any of the 3 districts (verbatim names OR ILIKE) AND 0 SCHOOL districts on any of the 3 geo_ids"
    - "Orchestrator has confirmed all THREE 5-wide ext_id blocks are collision-free: TTSD -4112241..-4112245, FGSD -4105161..-4105165, SSD -4111291..-4111295 (probed with margin on BOTH bounds per IN-03: -4112250..-4112238, -4105170..-4105158, -4111300..-4111288)"
    - "Orchestrator has confirmed the on-disk migration MAX so the next STRUCTURAL migration number is safe (research snapshot: on-disk MAX 1205 → next structural 1206, headshots 1207; the DB ledger MAX is a known TRAP — on-disk `ls` counter authoritative; 1204 was consumed by a concurrent workstream mid-Phase-183, so filename collisions are real — re-confirm immediately before each authoring step)"
    - "Orchestrator has confirmed districts.state casing for an EXISTING OR SCHOOL district (Portland Public Schools, geo_id 4110040) returns lowercase 'or' — the casing the structural migration WHERE/JOIN clauses must use"
    - "All three boards are re-confirmed 5-MEMBER (NOT 7 like Wave 1) and WHOLE-DISTRICT AT-LARGE via direct district-site fetch: TTSD from ttsdschools.org/about-us/school-board, FGSD from www.fgsdk12.org (the guessed fgsd.k12.or.us domain does NOT resolve — 000), SSD from sherwood.k12.or.us/board/board-members/ — all HTTP 200, no WAF; the D-Z1 zone-voted branch does NOT fire for any of the 3 and D-Z2 fallback is moot (NO sub-zone geofences)"
    - "The three verified rosters are locked (all 5/5, live 2026-07-04): TTSD P1 David Jaimes / P2 Kristen Miles / P3 Tristan Irvin (Vice Chair) / P4 Jill Zurschmeide (Chair) / P5 Crystal Weston — Chair/Vice-Chair pairing RE-VERIFIED same-day at execution per A3 (possible annual rotation); FGSD P1 Brisa Franco / P2 Pete Truax / P3 Alma Lozano (Vice Chair) / P4 Linda Harrington / P5 Kristy Kottkey (Chair); SSD P1 Harmony Carson (Board Chair) / P2 Matt Kaufman / P3 Abby Hawkins (Board Vice Chair) / P4 Hans Moller / P5 Matt Thornton — no vacancies; the FGSD Student Representative seat is confirmed NON-VOTING and EXCLUDED (D-R4)"
    - "Verbatim chamber names locked before plan 02: TTSD = 'School Board', FGSD = 'School Board', SSD = 'Board of Directors' (per-district verbatim, D-R1/D-R2). Office-title conventions locked: all three 'Director, Position N'; Sherwood's Chair/Vice-Chair use the district's HIGH-confidence literal strings 'Board Chair/Director, Position 1' (Carson) and 'Board Vice Chair/Director, Position 3' (Hawkins); FGSD title is MEDIUM confidence (no literal string in HTML — optionally reconfirm via a Washington County candidate-filing PDF, non-blocking per Open Question 1)"
    - "Headshot sourcing re-confirmed per the 3 distinct CDN paths (15 URLs): TTSD 5/5 finalsite untransformed originals HTTP 200 (small circular, upscale-with-caveat per A5); FGSD 4/5 Edlio JPEGs HTTP 200 + Position 4 Linda Harrington confirmed a 'Coming Soon' placeholder (documented gap, D-R5 — attempt a local-news photo fetch first, else honest gap); SSD 5/5 recovered via the WP REST API /wp-json/wp/v2/media/{id} → media_details.sizes.large.source_url (NOT the on-page fly-images pre-cropped URL — transform trap); Matt Kaufman near-square 1831x1694 needs an ordinary center-crop"
    - "Decision coverage — D-Z1: WHO-VOTES routing re-verified whole-district at-large for all 3 (single G5420 district each); D-Z2: confirmed moot; D-R1/R2: verbatim body + office-title names locked; D-R4: FGSD student rep excluded, Harrington counts as seated; D-R5: headshot sources + Harrington gap noted; D-F1/D-F2: WR-01/02/03 + IN-01..04 fixes flagged for plans 02/03; D-B1: no banner work confirmed"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-probe.sql"
      provides: "The read-only Wave-0 probe queries the orchestrator runs before any structural write"
      min_lines: 30
  key_links:
    - from: "probe.sql"
      to: "essentials.geofence_boundaries / essentials.governments / essentials.districts / essentials.politicians / supabase_migrations.schema_migrations / inform.politician_answers"
      via: "read-only SELECT probes"
      pattern: "essentials\\.(geofence_boundaries|governments|districts|politicians)"
---

<objective>
Author the Wave-0 verification probe file for the three-district school-board deep-seed and drive the
orchestrator-run DB + roster + headshot checks that MUST pass before any structural write. This plan
gates the entire phase: no government, chamber, district, office, or headshot migration may be authored
or applied until every probe below returns its expected result.

Purpose: Lock the load-bearing facts before writing SQL. RESEARCH (2026-07-04) already live-verified
everything at HIGH confidence, but roster/site/CDN state can drift between research and execution, so
Wave-0 re-confirms: (1) all three Phase-174 G5420 geofences still exist, valid, correctly named (do NOT
re-load); (2) greenfield status + collision-free ext_id blocks (5-wide each, margin on both bounds per
IN-03); (3) the next structural migration number via on-disk `ls` (ledger MAX is a trap — on-disk 1205
→ next 1206; 1204 was claimed mid-183 by another workstream, collisions are real); (4) lowercase 'or'
districts casing; (5) all three rosters are 5/5 (NOT 7 like Wave 1) and whole-district at-large (D-Z1
zone branch does NOT fire — no sub-zone geofences); (6) the 15 headshot URLs across THREE CDNs still
resolve (finalsite / Edlio / Sherwood WP REST API), with the Linda Harrington placeholder gap
re-checked; (7) the verbatim chamber and office-title strings are locked. The Forest Grove domain is
www.fgsdk12.org (the guessed fgsd.k12.or.us does NOT resolve). The FGSD Student Representative is
re-confirmed excluded (D-R4).
Output: `_tmp-westmetro-school-wave2-probe.sql` (a gitignored `_tmp-*` orchestrator-run helper) + a
recorded pass/fail for each Wave-0 gate, saved in the SUMMARY for downstream plans 02-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-RESEARCH.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-CONTEXT.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO database
access, NO WebSearch, and NO Supabase MCP. The INLINE ORCHESTRATOR (main context) runs the probes via
`psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`, re-fetches all
three rosters (ttsdschools.org / www.fgsdk12.org / sherwood.k12.or.us — NO WAF, plain curl returns 200),
and re-checks the 15 headshot URLs across the three CDNs. This probe file is a gitignored `_tmp-*`
helper — do NOT commit it. C:/EV-Accounts is a SEPARATE git repo — never cd into it; use
`git -C "C:/EV-Accounts"` if a commit is ever needed.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except where Probe E genuinely reads that table), and `photo_origin_url` out of
comments in any file authored this phase. Save the file as UTF-8.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-probe.sql</files>
  <read_first>
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md §"_tmp-westmetro-school-wave2-probe.sql" (Probes A-G with exact literal values + the IN-03 nits)
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-RESEARCH.md §"External_id collision probe", §"Migration ledger check", §"Domain verification", §"Pattern 3: Verified Rosters", §"Wave 0 Gaps"
    - C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-probe.sql (the direct one-phase-old analog — copy the labeled `\echo`-per-probe shape verbatim, adapting to 3 districts / 5-wide ext_id blocks; gitignored but present on disk)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-01-PLAN.md (the prior-phase probe-plan analog for probe structure)
  </read_first>
  <action>
    Write labeled read-only SELECT probes, each preceded by a `\echo` label line so the orchestrator
    reads results unambiguously. Do NOT wrap in a transaction (read-only); no DDL — SELECT/\echo only.
    Header comment: gitignored orchestrator-run read-only helper; all three G5420 geofences (4112240
    TTSD, 4105160 FGSD, 4111290 SSD) pre-loaded and smoke-tested by Phase 174 — must NOT be re-loaded;
    all three boards confirmed WHOLE-DISTRICT at-large so D-Z1 does not fire and D-Z2 is moot; all three
    boards are 5-seat (NOT 7 like Wave 1) so every ext_id block and count literal reflects 5.
    Probe A (GEOFENCE EXISTENCE-AND-NAME, all 3): select geo_id, mtfcc, name, source,
    ST_IsValid(geometry) from essentials.geofence_boundaries WHERE geo_id IN
    ('4112240','4105160','4111290') AND mtfcc='G5420' (EXPECT 3 rows with the three verbatim district
    names, all source='tiger_unsd_or_2024_westmetro', all ST_IsValid=true).
    Probe B (GREENFIELD governments): count essentials.governments WHERE name IN the three verbatim names
    ('Tigard-Tualatin School District 23J, Oregon, US', 'Forest Grove School District 15, Oregon, US',
    'Sherwood School District 88J, Oregon, US') OR name ILIKE '%tigard-tualatin school%' OR name ILIKE
    '%forest grove school%' OR name ILIKE '%sherwood school%' (EXPECT 0).
    Probe C (GREENFIELD districts): count essentials.districts WHERE geo_id IN the 3 geo_ids AND
    district_type='SCHOOL' (EXPECT 0).
    Probe D (EXT_ID collision, all 3 blocks with margin on BOTH bounds — IN-03): select external_id,
    full_name from essentials.politicians WHERE external_id BETWEEN -4112250 AND -4112238 OR external_id
    BETWEEN -4105170 AND -4105158 OR external_id BETWEEN -4111300 AND -4111288 ORDER BY external_id
    (EXPECT 0 rows).
    Probe E (MIGRATION LEDGER): select MAX(version::bigint) from supabase_migrations.schema_migrations
    WHERE version ~ '^[0-9]{1,6}$' (record it; header comment names the REAL expected numbers — on-disk
    MAX 1205 → next structural 1206, next headshot migration 1207 — and states on-disk `ls` is
    authoritative because the ledger MAX is a trap and 1204 was claimed by a concurrent workstream).
    Probe F (CASING): select distinct state from essentials.districts WHERE geo_id='4110040' AND
    district_type='SCHOOL' (Portland Public Schools existing OR precedent; EXPECT 'or' lowercase).
    Probe G (0-STANCE BASELINE): count inform.politician_answers pa JOIN essentials.politicians p ON
    pa.politician_id=p.id WHERE p.external_id BETWEEN -4112245 AND -4112241 OR p.external_id BETWEEN
    -4105165 AND -4105161 OR p.external_id BETWEEN -4111295 AND -4111291 (EXPECT 0 — documents the
    0-by-design baseline). No fenced code in this action — the wave1-probe.sql analog is the structural
    source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator (Task 2) runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-probe.sql` and records each result</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-probe.sql with labeled probes A-G, each prefixed by a `\echo` line
    - Probe A selects geo_id, mtfcc, name, source, ST_IsValid for geo_id IN ('4112240','4105160','4111290') AND mtfcc='G5420' (expects 3 rows with the three verbatim district names)
    - Probe B queries governments by all three verbatim names AND the three ILIKE terms ('%tigard-tualatin school%', '%forest grove school%', '%sherwood school%')
    - Probe C queries districts WHERE geo_id IN the 3 geo_ids AND district_type='SCHOOL'
    - Probe D queries external_id BETWEEN -4112250 AND -4112238 OR BETWEEN -4105170 AND -4105158 OR BETWEEN -4111300 AND -4111288 (5-wide blocks with margin both sides)
    - Probe E reads MAX(version::bigint) from the migration ledger with a numeric-version filter; a header comment names the real next numbers (1206 structural / 1207 headshots) and flags on-disk `ls` as authoritative
    - Probe F reads distinct districts.state for geo_id='4110040' AND district_type='SCHOOL' (expect 'or' lowercase)
    - Probe G counts inform.politician_answers for all three ext_id ranges (expect 0 baseline)
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE); not wrapped in a transaction; saved UTF-8; no `slug`/`photo_origin_url` literals in comments
  </acceptance_criteria>
  <done>Probe file authored with all 7 labeled probes and correct literal values (geo_ids 4112240/4105160/4111290, three 5-wide ext_id blocks with both-bound margin, PPS casing geo_id 4110040, real migration numbers 1206/1207 in the ledger comment).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes, re-fetches all 3 rosters, re-checks 15 headshots, confirms all gates</name>
  <what-built>The Wave-0 probe file. The orchestrator now executes it against the live DB, re-fetches all three live rosters directly from the district sites (no WAF), and re-checks the 15 headshot sources across the three CDNs (finalsite / Edlio / Sherwood WP REST API).</what-built>
  <how-to-verify>
    A. Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-probe.sql`
       (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
       1. Probe A returns 3 rows: 4112240='Tigard-Tualatin School District 23J', 4105160='Forest Grove
          School District 15', 4111290='Sherwood School District 88J', all
          source='tiger_unsd_or_2024_westmetro', all ST_IsValid=true. If any row is missing, mis-named,
          or invalid geometry → STOP (Phase 174 dependency broken; do NOT re-load — investigate).
       2. Probe B returns 0. If >0 → STOP, a district is already seeded — switch to reconcile.
       3. Probe C returns 0. If >0 → STOP, SCHOOL districts already exist.
       4. Probe D returns 0 rows. If any row → shift the affected ext_id block and record the new range
          for plans 02/03.
       5. Probe E: cross-check against `ls C:/EV-Accounts/backend/migrations | grep -oE '^[0-9]+' | sort -n | tail`.
          Next STRUCTURAL migration = on-disk file MAX + 1 (research snapshot: on-disk 1205 → 1206;
          headshots = 1207). The DB ledger MAX is a TRAP — do NOT use it. Re-confirm on-disk MAX again
          immediately before authoring EACH migration file (1204 was claimed by a concurrent workstream
          mid-183). Record 1206/1207 for plans 02/03.
       6. Probe F returns 'or' (lowercase) — the casing plans 02/03 must use for SCHOOL districts.
       7. Probe G returns 0 — the 0-stance-by-design baseline for the phase gate.
    B. Re-fetch all three rosters directly (NO WAF — plain curl returns 200). Confirm each is 5/5, no
       vacancies, and the CONTEXT-guessed count of 7 is WRONG (all three are 5-seat):
       - ttsdschools.org/about-us/school-board — TTSD 5 directors: P1 David Jaimes, P2 Kristen Miles,
         P3 Tristan Irvin (Vice Chair), P4 Jill Zurschmeide (Chair), P5 Crystal Weston. RE-VERIFY the
         Chair/Vice-Chair pairing same-day (A3 — a stale prior-year minutes PDF shows Irvin=Chair; the
         live current-page table is authoritative). Re-confirm the district's own language "The
         five-member Tigard-Tualatin School Board is elected by the voters within the cities of Tigard,
         Tualatin, Durham, and King City" (WHOLE-DISTRICT at-large — D-Z1 does NOT fire; no sub-zone
         geofences).
       - www.fgsdk12.org board-members page — FGSD 5 directors: P1 Brisa Franco, P2 Pete Truax, P3 Alma
         Lozano (Vice Chair), P4 Linda Harrington, P5 Kristy Kottkey (Chair). Do NOT scrape
         fgsd.k12.or.us — it returns 000 (does not resolve). SPECIAL EXCLUSION CHECK (D-R4): the Student
         Representative seat on the same page is NON-VOTING and MUST be excluded (confirm exactly 5
         elected directors). Linda Harrington (P4) counts as seated — she is a confirmed June 23, 2026
         mid-term appointee filling the Kate Grandusky vacancy.
       - sherwood.k12.or.us/board/board-members/ — SSD 5 directors: P1 Harmony Carson (Board Chair),
         P2 Matt Kaufman, P3 Abby Hawkins (Board Vice Chair), P4 Hans Moller, P5 Matt Thornton;
         chamber name literal "Board of Directors"; no zone/sub-district language.
       - If ANY roster changed (new director, vacancy, name/position change, or a Chair/VC rotation on
         TTSD) → STOP and re-plan that district's roster in plan 02.
    C. Headshot retrievability across THREE CDNs (15 URLs, verbatim in 184-RESEARCH.md §"Code Examples"):
       - TTSD (5 finalsite untransformed originals): confirm HTTP 200 + non-empty body for all 5; note
         they are small circular renditions (~3.3-27KB) — plan 03 uses the untransformed original and
         upscales with a documented caveat (A5), same class as Hillsboro Wave 1.
       - FGSD (Edlio): confirm the 4 genuine JPEGs (Franco/Truax/Lozano/Kottkey, 200-292KB) HTTP 200;
         Position 4 Linda Harrington's on-page image is a confirmed "Coming Soon" placeholder (4.3KB
         gray PNG) — attempt a targeted local-news photo fetch (forestgrovenewstimes.com /
         newsinthegrove.com June-2026 appointment coverage references a contributed district photo);
         if none usable, document an honest gap per D-R5 (do NOT ship the placeholder).
       - SSD (WP REST API): for each of the 5 attachment ids (10012, 10127, 10011, 10010, 10013) query
         `GET https://sherwood.k12.or.us/wp-json/wp/v2/media/{id}` and read
         media_details.sizes.large.source_url (do NOT use the on-page fly-images pre-cropped URL — it is
         a transform trap). Confirm 4/5 are native 2400x3000 (exact 4:5, no upscale) and 1/5 (Matt
         Kaufman) is near-square 1831x1694 needing an ordinary center-crop.
    D. Lock the verbatim naming for plans 02/03: chamber names TTSD='School Board', FGSD='School Board',
       SSD='Board of Directors' (per-district verbatim, D-R1/R2); office titles all 'Director, Position
       N', with Sherwood's Chair/VC using the district's HIGH-confidence literal strings 'Board
       Chair/Director, Position 1' (Carson) and 'Board Vice Chair/Director, Position 3' (Hawkins).
       FGSD title is MEDIUM confidence (no literal string in HTML) — optionally reconfirm via a
       Washington County candidate-filing PDF (non-blocking, Open Question 1); proceed with 'Director,
       Position N' if none found quickly. Chair/Vice-Chair are title-on-seat suffixes, NOT separate rows
       (5 offices per district, 15 total — never 6 per district).
  </how-to-verify>
  <acceptance_criteria>
    - All 7 probe gates recorded with pass/fail; A=3 correctly-named valid geofences, B/C/D=0, E cross-checked (next structural 1206 / headshots 1207, ledger recognized as trap), F='or', G=0
    - All three 5-director rosters re-confirmed unchanged via direct district-site fetch; all three confirmed 5-SEAT (not 7) and WHOLE-DISTRICT at-large (no sub-zone geofences); D-Z1 confirmed NOT firing for any
    - FGSD Student Representative confirmed excluded (exactly 5 elected directors); Linda Harrington confirmed as a seated mid-term appointee
    - TTSD Chair/Vice-Chair pairing re-verified same-day against the live page (A3 rotation guard)
    - 15 headshot sources re-checked: TTSD 5/5 finalsite 200 (small, upscale-with-caveat flagged); FGSD 4/5 Edlio 200 + Harrington placeholder gap decision recorded (local-news photo attempted, else honest gap); SSD 5/5 recovered via WP REST API media_details.sizes.large (fly-images trap avoided), Kaufman center-crop flagged
    - Verbatim chamber names ('School Board'/'School Board'/'Board of Directors') and office-title conventions ('Director, Position N', with Sherwood's literal Chair/VC strings) locked; Chair/VC confirmed as title-on-seat suffixes
    - Confirmed ext_id blocks (TTSD -4112241..-4112245, FGSD -4105161..-4105165, SSD -4111291..-4111295 unless probe D forced a shift) and next migration numbers (1206/1207) recorded for plans 02/03
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |
| operator → external web | Roster re-fetch + headshot re-check from ttsdschools.org / www.fgsdk12.org / sherwood.k12.or.us + resources.finalsite.net / 3.files.edl.io / sherwood.k12.or.us/wp-json (no WAF) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-01 | Tampering | probe.sql executed on prod DB | accept | Probes are read-only SELECTs; no write path; no user input |
| T-184-02 | Spoofing | acting on the stale CONTEXT.md assumption that boards are 7-seat (or that FGSD lives at fgsd.k12.or.us) would seed wrong counts / scrape a dead domain | mitigate | Wave-0 re-fetches all three live pages, confirms 5/5 counts and the www.fgsdk12.org domain before any write |
| T-184-03 | Spoofing | seeding the FGSD non-voting Student Representative as a director (its markup sits alongside the 5 directors) | mitigate | Wave-0 re-confirms exactly 5 elected directors per board and names the excluded FGSD student rep (D-R4) |
| T-184-04 | Spoofing | stale roster or a TTSD Chair/Vice-Chair annual rotation since the 2026-07-04 research pull | mitigate | Direct district-site re-fetch of all three rosters + same-day TTSD Chair/VC re-verify (A3); STOP-and-re-plan on any change |
| T-184-05 | Tampering (data integrity) | wrong migration number from trusting the DB ledger MAX (a known trap; 1204 was claimed mid-183) | mitigate | Probe E cross-checked against on-disk `ls` — on-disk MAX authoritative (1205 → 1206/1207), re-confirmed before each authoring step |
| T-184-06 | Spoofing | shipping Linda Harrington's "Coming Soon" placeholder as a real headshot | mitigate | Wave-0 confirms the placeholder (4.3KB gray PNG) and records a document-gap-not-fabricate decision (D-R5) |
| T-184-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- All three G5420 geofences confirmed present, valid, correctly named before any structural write.
- All three boards confirmed 5-seat + whole-district at-large (no sub-zone geofences); D-Z2 moot.
- Any failed gate blocks plans 02-04 until resolved.
</verification>

<success_criteria>
- All three geofences confirmed present + name-matched + valid; greenfield (0 govs, 0 SCHOOL districts);
  three 5-wide ext_id blocks free; next migration numbers confirmed (1206 structural / 1207 headshots,
  ledger recognized as trap); lowercase 'or' casing confirmed; all three 5/5 rosters re-confirmed via
  direct fetch; all three whole-district at-large; FGSD student rep excluded; 15 headshot sources checked
  across three CDNs with the Harrington gap decided; verbatim chamber + office-title strings locked;
  0-stance baseline recorded.
</success_criteria>

<output>
Create `.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-01-SUMMARY.md`
when done, recording: all three confirmed geo_ids + names, greenfield status, confirmed 5-wide ext_id
blocks, next migration numbers (1206/1207), lowercase 'or' casing, all three re-confirmed 5-director
rosters (with Position numbers + Chair/Vice-Chair), the whole-district-at-large confirmation, the
excluded FGSD student rep, per-CDN headshot check results + the Harrington gap decision, and the locked
verbatim chamber + office-title strings — for downstream plans 02-04.
</output>
</output>
