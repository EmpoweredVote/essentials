---
phase: 212-backend-place-name-resolver-national-fallback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.ts
  - C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.test.ts
autonomous: true
requirements: [RSLV-02]
must_haves:
  truths:
    - "An idempotent, re-runnable build-time script ingests the Census Gazetteer Places (incl. CDPs) + Counties files into gazetteer_places / gazetteer_counties"
    - "Re-running the ingest produces zero net-new rows (upsert / ON CONFLICT), matching D-11"
    - "County subdivisions / townships / MCDs are excluded (D-09)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.ts"
      provides: "Idempotent Gazetteer download+parse+upsert ingest (D-08/D-10/D-11)"
      min_lines: 60
    - path: "C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.test.ts"
      provides: "Pipe-delimited parsing + idempotency (ON CONFLICT) unit coverage"
  key_links:
    - from: "ingest script column mapping"
      to: "migration 1378 gazetteer table columns (geo_id/name/state/lsad/aland_sqmi/intptlat/intptlong)"
      via: "INSERT ... ON CONFLICT (geo_id) DO UPDATE"
      pattern: "ON CONFLICT.*geo_id.*DO UPDATE"
---

<objective>
Author the idempotent, build-time Census Gazetteer ingest script (Places + Counties) that populates the nationwide reference tables defined in migration 1378, giving place-name coverage far beyond the ~20 curated deep-seeded states. Authoring + parsing unit test only — the live run against the production DB happens in Plan 03 ([BLOCKING]).

Purpose: RSLV-02 nationwide fallback coverage; catches unincorporated communities people actually search (CDPs like "Sun City", "Paradise", "East Los Angeles") that no curated roster covers.
Output: `ingest-gazetteer-places-counties.ts` + its parsing/idempotency test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md

<interfaces>
<!-- Canonical Gazetteer table columns (defined by Plan 01 migration 1378 — script MUST match). -->
gazetteer_places:   geo_id(PK) | name | state | lsad | aland_sqmi | intptlat | intptlong
gazetteer_counties: geo_id(PK) | name | state | aland_sqmi | intptlat | intptlong

<!-- Census Gazetteer file facts (RESEARCH.md Pitfall 2, Code Examples, Assumptions Log A2): -->
- Gazetteer files DO ship as single nationwide zips (unlike TIGER cd/place):
  2024_Gaz_place_national.zip , 2024_Gaz_counties_national.zip (www2.census.gov). Confirm the live vintage/URL at execute time; match to the TIGER vintage already used for geofences (D-10, ~2023/2024 cd119 set).
- Pipe-delimited, one header row. Places columns (order): USPS|GEOID|ANSICODE|NAME|LSAD|FUNCSTAT|ALAND|AWATER|ALAND_SQMI|AWATER_SQMI|INTPTLAT|INTPTLONG. Counties: same minus LSAD|FUNCSTAT. Verify against the downloaded file's header row before finalizing column indexes (A2/A3 risk).
- USPS column = the 2-letter state abbrev → gazetteer.state. GEOID → geo_id. NAME → name.

<!-- Existing ingest shape to model on (download/extract/stream/upsert), minus shapefile parsing: -->
C:/EV-Accounts/backend/scripts/load-national-house-districts.ts — uses adm-zip (^0.5.16, existing dep) + pg pool; documents "Census publishes no single nationwide file" for TIGER (does NOT apply to Gazetteer — Gazetteer national files exist).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author idempotent Gazetteer Places+Counties ingest script</name>
  <files>C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-national-house-districts.ts (download → adm-zip extract → stream → DB write shape to mirror)
    - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts (Census URL construction + adm-zip usage conventions)
    - C:/EV-Accounts/backend/src/lib/db.js import (the shared `pool`)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md §"Pattern 2" trade-off note, §"Pitfall 2", §"Common Pitfalls", §"Code Examples" (record layout), Open Question 3 (verify header at execute time)
  </read_first>
  <behavior>
    - Parses a pipe-delimited Gazetteer Places line into {geo_id, name, state(USPS), lsad, aland_sqmi, intptlat, intptlong}, trimming each field
    - Parses a Counties line into {geo_id, name, state, aland_sqmi, intptlat, intptlong}
    - Skips the header row and blank lines; does NOT ingest any county-subdivision/MCD file (D-09 — that file is never downloaded)
    - Upsert is idempotent: INSERT ... ON CONFLICT (geo_id) DO UPDATE — a second identical run changes zero row counts
  </behavior>
  <action>
    Create C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.ts. It (1) downloads the nationwide Gazetteer Places zip + Counties zip (D-08) from www2.census.gov at the vintage matched to the loaded TIGER geofences (D-10) — verify the exact current-vintage URL + header layout at run time (RESEARCH Open Question 3), never hardcode a URL for an unverified vintage; (2) extracts via `adm-zip` (existing dep — no new package); (3) parses each line with `line.split('|').map(s => s.trim())` mapping columns by verified header index; (4) upserts into essentials.gazetteer_places / essentials.gazetteer_counties via parameterized INSERT ... ON CONFLICT (geo_id) DO UPDATE SET name=..., state=..., ... (D-11 idempotent — support truncate+reload OR upsert, but the re-run MUST be net-zero-new). Do NOT ingest the county-subdivisions/MCD file (D-09). Export a `main()` / runnable entry so Plan 03 can invoke it. Batch inserts (do not issue one round-trip per row — ~30k+ places). Use the shared `pool` from ./lib/db.js. Log final inserted/updated counts.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npm run test:unit -- scripts/ingest-gazetteer-places-counties.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Script file exists and exports a runnable entry (main or equivalent)
    - Upsert statements use ON CONFLICT (geo_id) DO UPDATE (grep confirms) and parameterized placeholders ($1..) — never string-interpolated field values
    - No `npm install` / no new dependency added to package.json (adm-zip/pg already present)
    - No download or reference to a county-subdivision/MCD ("cousub"/"county_sub"/"mcd") file (grep -vi excludes it)
  </acceptance_criteria>
  <done>Idempotent Places+Counties ingest authored, MCD file excluded, upsert parameterized, no new deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parsing + idempotency unit test (RSLV-02, D-11)</name>
  <files>C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.test.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.ts (the script under test, from Task 1)
    - C:/EV-Accounts/backend/vitest.config.ts (test runner config) and any existing scripts/*.test.ts for the project's test conventions
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md §"Validation Architecture" (RSLV-02 row → "re-run produces 0 new inserts")
  </read_first>
  <behavior>
    - Given a 3-line Places fixture (header + 2 data rows), the parser yields exactly 2 records with correctly split geo_id/name/state/lsad/intptlat/intptlong
    - The header row is not emitted as a record
    - A record whose USPS field is "IL" maps to state "IL" (verbatim USPS, never inferred from name)
    - The generated upsert SQL string contains ON CONFLICT (geo_id) DO UPDATE (idempotency guaranteed by construction)
  </behavior>
  <action>
    Create C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.test.ts with Vitest. Extract/export the pure parse function from Task 1's script so it is unit-testable without a DB or network. Assert the fixture-parsing behaviors above. For idempotency, assert the upsert SQL uses ON CONFLICT ... DO UPDATE (construction-level guarantee) — the live net-zero re-run is verified against the production DB in Plan 03. Do NOT hit the network or a real DB in this unit test.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npm run test:unit -- scripts/ingest-gazetteer-places-counties.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists; runs green with no network/DB access
    - Asserts header-skip, 2-record parse, USPS→state fidelity, and ON CONFLICT DO UPDATE presence
  </acceptance_criteria>
  <done>Green Vitest file covering RSLV-02 parsing + idempotency-by-construction, no live dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Census download → ingest script | External public file (no auth) parsed and written to the DB |
| ingest script → live Postgres | Bulk writes to the new reference tables (Plan 03 run) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-03 | Tampering | Gazetteer line → SQL | mitigate | Parameterized INSERT (no field value interpolated into SQL); column mapping by verified header index |
| T-212-04 | Denial of Service | one round-trip per row on ~30k+ rows | mitigate | Batched inserts, single connection from shared pool |
| T-212-05 | Spoofing | wrong-vintage / 404 URL | mitigate | Verify current-vintage URL + header layout at run time (Open Question 3); never hardcode an unverified vintage |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (adm-zip/pg already deps); no install, no slopcheck needed |
</threat_model>

<verification>
- `npm run test:unit -- scripts/ingest-gazetteer-places-counties.test.ts` green
- grep confirms ON CONFLICT DO UPDATE + no MCD file reference + no package.json change
</verification>

<success_criteria>
- Idempotent Places+Counties ingest authored and unit-tested (parsing + idempotency-by-construction)
- Ready for the Plan 03 live run against production
</success_criteria>

<output>
Create `.planning/phases/212-backend-place-name-resolver-national-fallback/212-02-SUMMARY.md` when done.
</output>
