---
phase: 176-city-of-beaverton-deep-seed
plan: 04
type: execute
wave: 3
depends_on: ["176-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1133_beaty_stances.sql
  - C:/EV-Accounts/backend/migrations/1134_hartmeier_prigg_stances.sql
  - C:/EV-Accounts/backend/migrations/1135_teater_stances.sql
  - C:/EV-Accounts/backend/migrations/1136_kimmi_stances.sql
  - C:/EV-Accounts/backend/migrations/1137_tivnon_stances.sql
  - C:/EV-Accounts/backend/migrations/1138_dugger_stances.sql
  - C:/EV-Accounts/backend/migrations/1139_hasan_stances.sql
autonomous: false
requirements: [WASH-02]

must_haves:
  truths:
    - "Each of the 7 officials has evidence-only compass stances (or an honest near-empty record)"
    - "Every stance carries reasoning + at least one source URL — 100% cited"
    - "ZERO default/neutral values — a topic with no evidence is omitted entirely (blank spoke)"
    - "Stance values are discrete chairs (integer 1-5), not a polarity scale"
    - "topic_id resolved LIVE via JOIN on compass_topics.topic_key AND is_live=true — no hardcoded topic UUIDs"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1133_beaty_stances.sql"
      provides: "Mayor Lacey Beaty stances (audit-only)"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1139_hasan_stances.sql"
      provides: "Councilor Nadia Hasan stances (audit-only)"
      contains: "politician_context"
  key_links:
    - from: "1133..1139 stance migrations"
      to: "inform.politician_answers + inform.politician_context"
      via: "CTE with topic_key→topic_id JOIN, ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "inform\\.compass_topics ct[\\s\\S]*is_live = true"
---

<objective>
Research and author 7 audit-only stance migrations (1133–1139), one per Beaverton official, using
evidence-only compass research — one research agent at a time (hard rate-limit rule), all live
compass topics, 100% cited, honest blank spokes, ZERO default values, chairs model. Migrations
1133–1139 apply via raw SQL and do NOT register in the ledger (on-disk counter authoritative).

Purpose: Satisfy WASH-02 Success Criterion 3 (evidence-only stances render — 100% cited, honest
blanks, no defaults).
Output: 7 stance migration files (one per official), each following the WashCo Harrington analog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-RESEARCH.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-01-SUMMARY.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-02-SUMMARY.md

<interfaces>
inform.politician_answers  (politician_id, topic_id, value)             -- ON CONFLICT (politician_id, topic_id) DO UPDATE SET value
inform.politician_context  (politician_id, topic_id, reasoning, sources) -- ON CONFLICT (politician_id, topic_id) DO UPDATE
inform.compass_topics      (id, topic_key, is_live)                      -- topic_id resolved by JOIN on topic_key AND is_live=true; NEVER hardcode topic UUIDs
-- value is an integer 1-5 (CHAIRS model, not polarity). Blank spoke = omit the row entirely.
</interfaces>

**EXECUTION ARCHITECTURE:** Executor WRITES the 7 `.sql` files (with the politician UUID from
176-02-SUMMARY hardcoded, and the VALUES rows populated from the stance research). Stance research
runs ONE agent at a time (feedback_stance_research_one_at_a_time — parallel burns rate-limit quota
with no usable output). The ORCHESTRATOR applies each via `psql "$DATABASE_URL" -f ...` and commits
them in C:/EV-Accounts. Stance migrations do NOT register in schema_migrations.

**HARD RULES (non-negotiable):** all live compass topics per official; evidence-only, 100% cited;
NO default/neutral values (feedback_stance_no_default_value); values are chairs, not polarity
(feedback_compass_chairs_not_polarity); aim for 18–21+ depth across officials; honest blank spokes;
antipartisan (party never referenced). Use ONLY the live topic_key list captured in 176-01-SUMMARY.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Research + author stance migrations for Mayor + first 3 councilors (1133–1136)</name>
  <files>C:/EV-Accounts/backend/migrations/1133_beaty_stances.sql, C:/EV-Accounts/backend/migrations/1134_hartmeier_prigg_stances.sql, C:/EV-Accounts/backend/migrations/1135_teater_stances.sql, C:/EV-Accounts/backend/migrations/1136_kimmi_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1122_washco_harrington_stances.sql — entire file (header, CTE s(topic_key,val,reasoning,sources), topic_key→topic_id JOIN, dual ON CONFLICT into politician_answers + politician_context)
    - .planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md §"1133_beaty_stances.sql through 1139" + §"Stance Value Model"
    - .planning/phases/176-city-of-beaverton-deep-seed/176-01-SUMMARY.md (live topic_key list) + 176-02-SUMMARY.md (politician UUID per ext_id)
  </read_first>
  <action>
    For EACH official in order (Mayor Beaty 1133; Hartmeier-Prigg Pos 1 1134; Teater Pos 2 1135; Kimmi
    Pos 3 1136), run ONE stance-research agent at a time against ALL live compass topics from
    176-01-SUMMARY. Author one migration per official copying the 1122 CTE body: header comment
    (audit-only, NOT registered), BEGIN…COMMIT, WITH s(topic_key, val, reasoning, sources) VALUES rows —
    one row per topic that has cited evidence, omit topics with no evidence entirely (blank spoke).
    val is an integer 1–5 matching the discrete chair the evidence supports (NOT a polarity), reasoning
    is the evidence summary, sources is a text[] of 1+ real URLs. JOIN inform.compass_topics ON
    topic_key AND is_live=true to resolve topic_id (never hardcode a topic UUID). Hardcode the
    politician UUID from 176-02-SUMMARY. Dual ON CONFLICT (politician_id, topic_id) DO UPDATE into
    politician_answers (value) and politician_context (reasoning, sources). Kimmi's Council President
    title is display-only — do NOT let it influence stance research or add a synthetic stance. NEVER
    default a value; a thin record yields an honest near-empty file. No fenced code in this action.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each file then runs the stance audit: SELECT count of uncited answers (politician_answers without a matching politician_context row) must be 0; no NULL/defaulted values</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist (1133–1136), one per official, correct filename per PATTERNS
    - Each uses the topic_key→topic_id JOIN with is_live=true (no hardcoded topic UUIDs)
    - Each row has non-empty reasoning + a sources array with ≥1 URL (100% cited)
    - No row has a defaulted/neutral value inserted for a topic lacking evidence
    - Politician UUIDs match 176-02-SUMMARY; topic_keys drawn from 176-01-SUMMARY live list
    - No schema_migrations ledger INSERT (audit-only); no `slug`/`schema_migrations`/`photo_origin_url` in comments
  </acceptance_criteria>
  <done>Stance migrations for Mayor + Positions 1–3 authored from one-at-a-time evidence research.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Research + author stance migrations for councilors Positions 4–6 (1137–1139)</name>
  <files>C:/EV-Accounts/backend/migrations/1137_tivnon_stances.sql, C:/EV-Accounts/backend/migrations/1138_dugger_stances.sql, C:/EV-Accounts/backend/migrations/1139_hasan_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1122_washco_harrington_stances.sql — entire file (same CTE analog)
    - .planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md §"Stance Value Model"
    - .planning/phases/176-city-of-beaverton-deep-seed/176-01-SUMMARY.md (live topic_key list) + 176-02-SUMMARY.md (UUIDs)
  </read_first>
  <action>
    Same one-agent-at-a-time evidence-only process as Task 1, for Tivnon Pos 4 (1137), Dugger Pos 5
    (1138), Hasan Pos 6 (1139). Same 1122 CTE structure, chairs model, 100% cited, honest blanks, no
    defaults, topic_key→topic_id is_live JOIN, hardcoded UUID from 176-02-SUMMARY, dual ON CONFLICT.
    Audit-only — no ledger INSERT. No fenced code in this action.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each file then runs the stance audit (0 uncited, 0 defaulted values)</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist (1137–1139), one per official, correct filenames
    - Each uses topic_key→topic_id JOIN with is_live=true; 100% cited; no defaulted values
    - Politician UUIDs match 176-02-SUMMARY
    - No ledger INSERT; forbidden literal strings absent from comments
  </acceptance_criteria>
  <done>Stance migrations for Positions 4–6 authored from evidence research.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator applies 1133–1139, audits citations + no-defaults, commits</name>
  <what-built>7 audit-only stance migrations (one per official), evidence-only, chairs model.</what-built>
  <how-to-verify>
    1. Apply each: `psql "$DATABASE_URL" -f migrations/1133_beaty_stances.sql` … through 1139.
    2. Citation audit: every inform.politician_answers row for the 7 officials has a matching politician_context row with non-null reasoning + non-empty sources — 0 uncited.
    3. No-default audit: confirm no value was inserted for a topic lacking evidence (spot-check honest blanks against research notes); confirm all values are 1–5.
    4. Coverage: each official has ≥1 stance OR a documented honest near-empty record; total across 7 aims for the 18–21+ depth of prior deep-seeds.
    5. Confirm none of 1133–1139 registered in schema_migrations (disk counter authoritative; next after 1139 = 1140).
    6. Commit 1133–1139 in C:/EV-Accounts.
  </how-to-verify>
  <acceptance_criteria>
    - All 7 stance migrations applied; 0 uncited answers; all values 1–5
    - Every official has ≥1 stance or a documented honest near-empty record
    - None of 1133–1139 appear in schema_migrations
    - 1133–1139 committed in C:/EV-Accounts
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official stance counts, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research sources → DB | Cited evidence (public voting records/statements) transcribed into stance rows |
| operator → live DB | Audit-only stance INSERTs applied by the orchestrator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-08 | Tampering | Fabricated / uncited / defaulted stance destroys trust | mitigate | Evidence-only rule; 100%-cited audit (answers ⋈ context); no-default rule; honest blank spokes; one-agent-at-a-time research |
| T-176-09 | Tampering | Wrong topic_id (stale/retired topic) | mitigate | topic_id resolved LIVE via is_live=true JOIN on topic_key — retired topics excluded |
| T-176-10 | Repudiation | Duplicate stance on re-run | mitigate | ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables (idempotent) |
| T-176-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- 0 uncited answers; 0 defaulted values; all values 1–5; each official ≥1 stance or documented
  honest near-empty; none of 1133–1139 in the ledger.
</verification>

<success_criteria>
- 7 evidence-only stance migrations applied; 100% cited; no defaults; chairs model; committed;
  aiming for 18–21+ total stance depth across the roster.
</success_criteria>

<output>
Create `.planning/phases/176-city-of-beaverton-deep-seed/176-04-SUMMARY.md` when done, recording
per-official stance counts + notable honest blanks.
</output>
