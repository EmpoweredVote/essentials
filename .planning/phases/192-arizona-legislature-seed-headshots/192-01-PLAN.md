---
phase: 192-arizona-legislature-seed-headshots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1286_az_legislature.sql
autonomous: false
requirements: [AZ-LEG-01]
must_haves:
  truths:
    - "30 Arizona state senators seeded, 1 per SLDU district (LD 1-30), under the State Senate chamber @ geo_id='04'"
    - "60 Arizona state house reps seeded, exactly 2 per SLDL district (30 districts), under the House of Representatives chamber @ geo_id='04'"
    - "Every legislative office links to its district via lowercase state='az' AND explicit district_type (STATE_UPPER for senators, STATE_LOWER for reps)"
    - "The 3 mid-term successors (Sears SD-9, Reim HD-3, Allen HD-7) carry is_appointed=true on the politician + is_appointed_position=false on the office; the 3 departed members are NOT seeded"
    - "The structural migration is applied to production and registered in the ledger"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1286_az_legislature.sql"
      provides: "2 chambers + 90 politicians + 90 offices + office_id backfill + post-verify DO gate + ledger registration"
      contains: "State Senate"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts"
      via: "district_id join scoped by state='az' + district_type"
      pattern: "district_type\\s*=\\s*'STATE_(UPPER|LOWER)'"
    - from: "essentials.offices (60 House)"
      to: "essentials.politicians"
      via: "NOT EXISTS guard keyed on (district_id, politician_id)"
      pattern: "o\\.district_id = d\\.id AND o\\.politician_id = p\\.id"
---

<objective>
Author and apply the structural migration that seeds the full 90-member 57th Arizona Legislature
(the CURRENT sitting biennium, 2025-26 — see below) under the existing State of Arizona
government (geo_id `04`): 2 net-new chambers, 90 politicians, 90 offices linked to the Phase 190
SLDU/SLDL geofences. Greenfield — 0 legislative chambers/offices exist today.

Purpose: Satisfies ROADMAP success criteria #1 (30 senators on SLDU) and #2 (60 reps on SLDL,
2 per district). Downstream Phase 199 (2026 elections) and the deferred stance-research pass both
consume these offices.
Output: `C:/EV-Accounts/backend/migrations/1286_az_legislature.sql` (structural, registered),
applied to production, with the 90 politician UUIDs captured into the SUMMARY for Plan 02.

⚠ ROSTER SOURCE OF TRUTH: seed the **57th Legislature** roster in 192-RESEARCH.md § "The Roster"
verbatim (30 Senate + 60 House). The "56th" label in CONTEXT.md D-02 is a stale off-by-one — D-02's
intent is "seed the current sitting members," which is the 57th. Do NOT re-derive the roster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-arizona-legislature-seed-headshots/192-CONTEXT.md
@.planning/phases/192-arizona-legislature-seed-headshots/192-RESEARCH.md
@.planning/phases/192-arizona-legislature-seed-headshots/192-PATTERNS.md
@.planning/phases/192-arizona-legislature-seed-headshots/192-VALIDATION.md

<execution_note>
`gsd-executor` has NO Supabase MCP. Executor TASKS author the `.sql` file only. Tasks marked
ORCHESTRATOR-RUN (the roster checkpoint + apply, and the post-apply assertions) are executed by
the inline execute-phase orchestrator using `psql -f` against `DATABASE_URL` in
`C:/EV-Accounts/backend/.env` (or the supabase MCP). A migration authored but not applied is a
false positive — the apply MUST happen before Plan 03 verification.
</execution_note>

<interfaces>
Key schema contracts (live-verified in 192-RESEARCH.md — no exploration needed):
- essentials.governments: State of Arizona row exists at geo_id='04', government_id
  15436f29-38d2-4cc0-8958-9e74ba60fabf.
- essentials.chambers (id, name, name_formal, government_id) — `id` is GENERATED path column; do
  NOT insert into path. Idempotent INSERT ... WHERE NOT EXISTS on (name, government_id).
- essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state,
  is_appointed_position, is_vacant, role_canonical, normalized_position_name). NO unique constraint
  on (district_id, chamber_id) — only offices_pkey on id (live-verified). role_canonical +
  normalized_position_name left NULL (matches NV precedent).
- essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed,
  is_vacant, is_incumbent, external_id, office_id). external_id has a UNIQUE constraint (ON CONFLICT
  (external_id) DO NOTHING).
- essentials.districts: SLDU = 30 rows (district_type='STATE_UPPER', state='az', geo_id
  04001..04030); SLDL = 30 rows (district_type='STATE_LOWER', state='az', geo_id 04001..04030).
  SLDU and SLDL SHARE the geo_id space — district_type is MANDATORY in every WHERE. NO name_formal
  column — use `label`.
- ext_id block (live-verified clear): Senate -4005001..-4005030 (LD-NN = -40050NN);
  House -4006001..-4006060 (LD-N reps = -4006(2N-1) and -4006(2N), no seat ordering implied).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the structural legislature migration</name>
  <files>C:/EV-Accounts/backend/migrations/1286_az_legislature.sql</files>
  <read_first>
    - .planning/phases/192-arizona-legislature-seed-headshots/192-PATTERNS.md (lines 23-243 — the full structural migration pattern: header/pre-flight, chamber INSERT, per-legislator Senate CTE shape, the CRITICAL House 2-per-district guard divergence, appointed-holder flags, office_id backfill, post-verify DO gate, ledger registration)
    - .planning/phases/192-arizona-legislature-seed-headshots/192-RESEARCH.md (§ "The Roster" — the 90 names/districts/party; § "ext_id range"; § "Common Pitfalls" 1, 3, 4)
    - C:/EV-Accounts/backend/migrations/1053_nv_legislature.sql (primary analog — 2-chamber legislature seed shape)
    - C:/EV-Accounts/backend/migrations/1282_az_state_exec_gap.sql (AZ conventions + the collegial-body (district_id, politician_id) guard from the 5-seat Corporation Commission)
  </read_first>
  <action>
    First run `ls C:/EV-Accounts/backend/migrations | sort` to confirm the on-disk MAX (research saw
    1285); name the file disk-MAX+1 (provisionally `1286_az_legislature.sql`). If disk MAX drifted,
    use the actual disk-MAX+1 and note the deviation.

    Author a STRUCTURAL migration wrapped in BEGIN/COMMIT. Pre-flight DO block: assert exactly 1
    government at geo_id='04', and assert greenfield (0 chambers matching name ILIKE '%senate%' OR
    '%house%' OR '%representative%' under government geo_id='04'; RAISE EXCEPTION otherwise).

    Create 2 chambers under geo_id='04' (idempotent INSERT ... WHERE NOT EXISTS on (name,
    government_id), per D-04): Senate → name='State Senate', name_formal='Arizona State Senate';
    House → name='House of Representatives', name_formal='Arizona House of Representatives'.

    Seed 30 Senate offices — 1 per SLDU district, using the 1053 SD-1 CTE shape (PATTERNS.md lines
    87-124): title='State Senator', representing_state='AZ', ext_id -40050NN for LD-NN, WHERE
    d.geo_id='040NN' AND d.district_type='STATE_UPPER' AND d.state='az' (lowercase). Guard on
    (district_id, chamber_id) is acceptable for Senate (1 per district).

    Seed 60 House offices — 2 per SLDL district, using the AZ 191 Corporation Commission collegial
    guard (PATTERNS.md lines 126-165). CRITICAL (D-01): each House office INSERT NOT EXISTS guard
    MUST key on `o.district_id = d.id AND o.politician_id = p.id` — NOT (district_id, chamber_id) —
    otherwise the 2nd rep of each LD silently no-ops. Both reps: title='State Representative' (no
    Seat A/B), representing_state='AZ', WHERE d.geo_id='040NN' AND d.district_type='STATE_LOWER' AND
    d.state='az'. ext_ids -4006(2N-1) and -4006(2N) for LD-N (no seat ordering meaning).

    Mid-term successors (Sears SD-9, Reim HD-3, Allen HD-7 — per RESEARCH § The Roster): on the
    politician row set is_appointed=true; on the office row keep is_appointed_position=false;
    is_incumbent=true, is_active=true (PATTERNS.md lines 167-176). Do NOT seed or keep the 3 departed
    members (Chaplik HD-3, Marshall HD-7, Burch SD-9) — zero genuine vacancies exist.

    Two office_id backfills (PATTERNS.md lines 178-188): Senate range -4005030..-4005001, House range
    -4006060..-4006001.

    Post-verify DO gate (PATTERNS.md lines 190-231) that RAISE EXCEPTIONs (rolling back the whole
    transaction) unless ALL hold: 90 new politicians in ext_id -4006060..-4005001; 30 Senate offices;
    60 House offices; House `GROUP BY district_id HAVING count(*) <> 2` returns 0 rows; STATE_UPPER
    linkage = 30 and STATE_LOWER linkage = 60 on 30 districts at state='az'; the section-split query
    (160-VALIDATION.md shape, lowercase d.state='az', district_type IN ('STATE_UPPER','STATE_LOWER'))
    returns 0 rows.

    Register the structural migration OUTSIDE BEGIN/COMMIT: INSERT INTO
    supabase_migrations.schema_migrations (version, name) VALUES ('1286','az_legislature') ON CONFLICT
    (version) DO NOTHING (substitute the actual disk-MAX+1 number).

    Do NOT invent politician UUIDs — offices resolve politician_id via the ins_p CTE at apply time.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1286_az_legislature.sql" && grep -qF "State Senate" "C:/EV-Accounts/backend/migrations/1286_az_legislature.sql" && grep -qF "House of Representatives" "C:/EV-Accounts/backend/migrations/1286_az_legislature.sql" && grep -c "o.district_id = d.id AND o.politician_id = p.id" "C:/EV-Accounts/backend/migrations/1286_az_legislature.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/1286_az_legislature.sql` exists (disk-MAX+1 name).
    - Grep confirms both chamber names present: `State Senate` AND `House of Representatives`.
    - Grep count of `o.district_id = d.id AND o.politician_id = p.id` (the House collegial guard key) is exactly 60 (once per House office) — the (district_id, chamber_id) key must NOT appear in any House office INSERT.
    - Grep confirms `district_type = 'STATE_UPPER'` and `district_type = 'STATE_LOWER'` both present; `state = 'az'` (lowercase) present; no uppercase `state = 'AZ'` in any office↔district WHERE.
    - Grep confirms 90 distinct external_id literals in ranges -4005001..-4005030 and -4006001..-4006060; none of Chaplik/Marshall/Burch surnames present in any INSERT.
    - Grep confirms `is_appointed` set true exactly 3 times (Sears/Reim/Allen) and each of those offices keeps `is_appointed_position` false.
    - Grep confirms the ledger-registration line `('1286'` (or actual number) present OUTSIDE the COMMIT.
    - Post-verify `DO $$ ... RAISE EXCEPTION` gate present with the 6 assertions listed in the action.
  </acceptance_criteria>
  <done>Structural migration authored with 2 chambers, 90 offices/politicians, correct guards, appointed flags, backfills, post-verify gate, and ledger registration — ready to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Roster currency re-verification + apply structural migration (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/192-arizona-legislature-seed-headshots/192-RESEARCH.md (§ "The Roster" + "Genuine vacancies found: 0" + Assumption A1 — the residual same-day-change risk this checkpoint mitigates)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor file action. The operator re-confirms the live 90-member roster; on approval the ORCHESTRATOR applies the structural migration via `psql -f` and captures the 90 politician UUIDs. See &lt;how-to-verify&gt; for the exact sequence.</action>
  <what-built>
    The structural migration 1286_az_legislature.sql (Task 1) is authored but NOT yet applied. This
    checkpoint confirms the 90-member roster is still current before the orchestrator applies it to
    production. Research (2026-07-08) confirmed 0 genuine vacancies and 3 resolved mid-term
    successions (Burch→Sears SD-9, Chaplik→Reim HD-3, Marshall→Allen HD-7); legislative sessions can
    see further mid-term appointments at any time (research "valid until ~14 days for the roster").
  </what-built>
  <how-to-verify>
    1. Open https://www.azleg.gov/memberroster/ and confirm the current Senate (30) + House (60)
       roster still matches 192-RESEARCH.md § "The Roster" — especially the 3 mid-term seats
       (SD-9 Sears, HD-3 Reim, HD-7 Allen).
    2. Confirm no NEW resignation/appointment has landed since 2026-07-08 for any of the 90 seats.
    3. On approval, the ORCHESTRATOR applies the migration via
       `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1286_az_legislature.sql`
       (executor has no DB access), then captures the 90 politician UUIDs
       (`SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id BETWEEN -4006060 AND -4005001 ORDER BY external_id`)
       into 192-01-SUMMARY.md for Plan 02's headshot script/migration to consume.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the live azleg.gov roster matches the seeded roster (or supplies the exact delta to patch into the migration before apply).
    - Migration applies with the in-transaction post-verify DO gate passing (no RAISE EXCEPTION → transaction COMMITs).
    - `psql -tAc "SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1286'"` returns 1 (structural registered).
    - The 90 politician UUIDs are captured into 192-01-SUMMARY.md (external_id → UUID → full_name), because Plan 02's audit migration cannot hardcode UUIDs ahead of apply.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live roster is confirmed and the migration is applied + UUIDs captured, or describe the roster delta to patch first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply structural assertions (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/192-arizona-legislature-seed-headshots/192-VALIDATION.md (Per-Task Verification Map — seed-senate, seed-house, district-linkage rows)
    - .planning/phases/192-arizona-legislature-seed-headshots/192-PATTERNS.md (lines 390-432 — the compact single-query audit style)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). After apply, run the seed assertions via
    `psql "$DATABASE_URL" -tAc` and record results in 192-01-SUMMARY.md. Assert: (a) 30 offices under
    chamber 'State Senate' @ geo_id='04'; (b) 60 offices under chamber 'House of Representatives' @
    geo_id='04'; (c) House offices `GROUP BY district_id HAVING count(*) <> 2` → 0 rows; (d) linked
    districts GROUP BY district_type at state='az' → STATE_UPPER 30, STATE_LOWER 60 links on 30
    districts; (e) DISTINCT state on linked districts → only 'az' (lowercase). Any mismatch blocks
    the phase — do NOT proceed to Plan 02.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.governments g ON g.id=c.government_id WHERE g.geo_id='04' AND c.name='State Senate')=30 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.governments g ON g.id=c.government_id WHERE g.geo_id='04' AND c.name='House of Representatives')=60 AND NOT EXISTS (SELECT 1 FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id WHERE c.name='House of Representatives' GROUP BY o.district_id HAVING count(*)<>2)"</automated>
  </verify>
  <acceptance_criteria>
    - State Senate offices count = 30 (SQL output true).
    - House of Representatives offices count = 60 (SQL output true).
    - House offices GROUP BY district_id HAVING count(*) <> 2 → 0 rows (no NOT EXISTS violation → boolean true).
    - Linked-district audit: STATE_UPPER = 30 and STATE_LOWER = 30 distinct districts (60 house links) at state='az'.
    - DISTINCT state on all 90 linked districts = only 'az'.
    - The combined boolean SELECT returns `t`.
  </acceptance_criteria>
  <done>Live production DB confirms 30 Senate + 60 House offices, exactly 2 reps per SLDL district, correct STATE_UPPER/STATE_LOWER linkage, lowercase 'az' casing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | Hand-authored static SQL crosses into the live `essentials` schema via `psql -f` (service-role) |
| Phase 190 geofences → new offices | Office↔district joins consume pre-existing SLDU/SLDL rows; wrong casing/type silently mislinks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-01 | Tampering | office↔chamber↔government joins | mitigate | Scope every chamber/office subquery to government geo_id='04'; in-transaction section-split DO gate must return 0 rows or the transaction rolls back |
| T-192-02 | Tampering | office↔district WHERE (casing/type) + House 2nd-rep guard | mitigate | Every office↔district WHERE uses lowercase state='az' + explicit district_type; House INSERT guard keyed on (district_id, politician_id) NOT (district_id, chamber_id); post-verify asserts 30/60 and HAVING count<>2 = 0 rows |
| T-192-04 | Spoofing | mid-term successor rows | mitigate | Seed only the 3 current successors (Sears/Reim/Allen) with is_appointed=true; the 3 departed members (Chaplik/Marshall/Burch) are never inserted; Task 2 human roster check |
| T-192-05 | Tampering | (deferred) stance rows | accept | 0 stances is the required end-state; this plan writes no `inform.politician_answers` rows — asserted in Plan 03 (stances-zero) |
| T-192-SC | Tampering | package installs | accept | No new npm/pip/cargo installs this phase; reuses installed psql/psycopg2 — RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 30 Senate offices + 60 House offices under geo_id='04'; House exactly 2/district.
- STATE_UPPER 30 / STATE_LOWER 30 district linkage at lowercase state='az'.
- Section-split (STATE_UPPER/STATE_LOWER) = 0 rows.
- Structural migration registered (version present in ledger); 90 politician UUIDs captured for Plan 02.
</verification>

<success_criteria>
ROADMAP #1 (30 senators on SLDU) + #2 (60 reps on SLDL, 2/district) are TRUE in production; the
structural migration is applied and registered; UUID manifest recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/192-arizona-legislature-seed-headshots/192-01-SUMMARY.md` when done —
include the 90 external_id → politician UUID → full_name manifest (required by Plan 02).
</output>
