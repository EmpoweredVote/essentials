---
phase: 117-newton-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/578_newton_city_government.sql
autonomous: true
requirements:
  - NEWTON-01

must_haves:
  truths:
    - "A Newton, MA address returns a LOCAL section with Mayor Laredo + 24 City Councillors"
    - "Newton city government row exists in essentials.governments"
    - "Newton section-split check returns 0 orphan rows (geo_id=2545560, mtfcc=G4110)"
    - "All 25 city officials (Mayor + 24 councillors) have non-NULL office_id"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/578_newton_city_government.sql"
      provides: "Newton city government seed — Mayor + 24 City Councillors"
      contains: "City of Newton, Massachusetts, US"
    - path: "essentials.governments (DB)"
      provides: "Newton government row"
      contains: "name = 'City of Newton, Massachusetts, US'"
    - path: "essentials.politicians (DB)"
      provides: "25 Newton city officials"
      contains: "external_id BETWEEN -2545560025 AND -2545560001"
    - path: "essentials.offices (DB)"
      provides: "25 offices — 1 LOCAL_EXEC (Mayor) + 24 LOCAL (councillors)"
      contains: "district linked to geo_id='2545560'"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts"
      via: "district_id JOIN on geo_id='2545560'"
      pattern: "district_type IN ('LOCAL_EXEC', 'LOCAL')"
    - from: "essentials.politicians"
      to: "essentials.offices"
      via: "office_id back-fill UPDATE"
      pattern: "external_id BETWEEN -2545560025 AND -2545560001"
---

<objective>
Seed City of Newton, Massachusetts city government: Mayor Marc Laredo (LOCAL_EXEC) + 24 City Councillors (16 at-large + 8 ward, all LOCAL). Migration 578 is the complete deliverable.

Purpose: Establishes the Newton LOCAL section so any Newton address returns Mayor + full council via the existing routing query. No UI changes needed — routing uses the geo_id=2545560 G4110 geofence already present from v5.0.

Output: migration 578_newton_city_government.sql applied to production; 25 politicians, 25 offices, 2 districts, 1 chamber, 1 government row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-newton-deep-seed/117-RESEARCH.md

<interfaces>
<!-- Reference migrations the executor MUST read before writing migration 578. -->
<!-- Pattern source 1: Worcester flat-district pattern (Tier 2 — 11 officials) -->
C:/EV-Accounts/backend/migrations/351_worcester_government.sql

<!-- Key structural conventions extracted from 351: -->
<!-- governments(id, name, type='LOCAL', state='MA', city, geo_id) -->
<!-- chambers(id, name, name_formal, government_id)  — NO slug column (GENERATED ALWAYS) -->
<!-- districts: LOCAL_EXEC label='Worcester (Citywide)', LOCAL label='Worcester', mtfcc=NULL, state='ma' -->
<!-- politicians: party=NULL, is_appointed=false, ON CONFLICT (external_id) DO NOTHING -->
<!-- offices: WITH ins_p AS (INSERT...RETURNING id) INSERT...CROSS JOIN ins_p WHERE NOT EXISTS -->
<!-- office_id back-fill: UPDATE WHERE o.politician_id = p.id AND p.office_id IS NULL -->
<!-- Post-verification DO block checks: gov=1, chambers=1, districts=2, politicians=N, offices=N, split=0, null_ids=0 -->
<!-- Ledger: INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('351') ON CONFLICT DO NOTHING -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 578 — Newton city government</name>
  <files>C:/EV-Accounts/backend/migrations/578_newton_city_government.sql</files>

  <read_first>
    C:/EV-Accounts/backend/migrations/351_worcester_government.sql — primary structural pattern (flat-district, at-large + ward titles, office_id back-fill, post-verification DO block).
    .planning/phases/117-newton-deep-seed/117-RESEARCH.md — full roster, external_id assignments, title conventions, pitfall list.
  </read_first>

  <action>
Create C:/EV-Accounts/backend/migrations/578_newton_city_government.sql modeled on 351_worcester_government.sql with the following Newton-specific values:

HEADER COMMENT — include:
- Migration 578: City of Newton, Massachusetts government (NEWTON-01)
- government name: 'City of Newton, Massachusetts, US'
- geo_id: '2545560' (G4110, v5.0)
- 25 politicians: Mayor Laredo (-2545560001) + 24 City Councillors (-2545560002..-2545560025)
- All 24 councillors use title 'City Councilor' (at-large) or 'City Councilor (Ward N)' (ward seat)
- NOTE: 'City Councilor' NOT 'City Councillor' (American spelling, Newton charter)
- parties all NULL; is_appointed=false for all 25

PRE-FLIGHT BLOCK 1 — RAISE EXCEPTION if government already exists (abort on double-apply; matches Plan 02 pattern):
  SELECT COUNT(*) FROM essentials.governments WHERE name = 'City of Newton, Massachusetts, US'

PRE-FLIGHT BLOCK 2 — RAISE EXCEPTION if Newton G4110 geofence absent:
  SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id = '2545560' AND mtfcc = 'G4110'

PRE-FLIGHT BLOCK 3 — RAISE EXCEPTION if external_id block not clear:
  SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2545560025 AND -2545560001

Wrap Steps 1-7 in BEGIN; ... COMMIT;

STEP 1 — Government row:
  INSERT (id, name, type, state, city, geo_id)
  VALUES (gen_random_uuid(), 'City of Newton, Massachusetts, US', 'LOCAL', 'MA', 'Newton', '2545560')
  WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name = 'City of Newton, Massachusetts, US')

STEP 2 — City Council chamber:
  INSERT (id, name, name_formal, government_id)
  name = 'City Council', name_formal = 'Newton City Council'
  WHERE NOT EXISTS on (name, government_id)
  NEVER include slug column.

STEP 3a — LOCAL_EXEC district (Mayor):
  district_type='LOCAL_EXEC', state='ma', geo_id='2545560', label='Newton (Citywide)', mtfcc=NULL
  WHERE NOT EXISTS on (geo_id, district_type, state)

STEP 3b — LOCAL district (all 24 councillors):
  district_type='LOCAL', state='ma', geo_id='2545560', label='Newton', mtfcc=NULL
  WHERE NOT EXISTS on (geo_id, district_type, state)

STEP 4 — 25 politician+office blocks (25 WITH ins_p blocks):

BLOCK 1 — Mayor Marc C. Laredo, external_id=-2545560001
  full_name='Marc C. Laredo', first_name='Marc', last_name='Laredo'
  is_appointed=false; links to LOCAL_EXEC district; title='Mayor'

BLOCKS 2-17 — 16 at-large councillors, all link to LOCAL district, title='City Councilor':
  -2545560002: Susan Albright (W2 AL), first='Susan', last='Albright'
  -2545560003: Brittany Hume Charm (W5 AL), first='Brittany', last='Hume Charm'
  -2545560004: Cyrus Dahmubed (W4 AL), first='Cyrus', last='Dahmubed'
  -2545560005: Rena Getz (W5 AL), first='Rena', last='Getz'
  -2545560006: Brian Golden (W7 AL), first='Brian', last='Golden'
  -2545560007: Lisa Gordon (W6 AL), first='Lisa', last='Gordon'
  -2545560008: Becky Grossman (W7 AL), first='Becky', last='Grossman'
  -2545560009: David Kalis (W8 AL), first='David', last='Kalis'
  -2545560010: Andrea Kelley (W3 AL), first='Andrea', last='Kelley'
  -2545560011: Josh Krintzman (W4 AL), first='Josh', last='Krintzman'
  -2545560012: Allison Leary (W1 AL), first='Allison', last='Leary'
  -2545560013: Tarik Lucas (W2 AL), first='Tarik', last='Lucas'
  -2545560014: John Oliver (W1 AL), first='John', last='Oliver'
  -2545560015: Sean Roche (W6 AL), first='Sean', last='Roche'
  -2545560016: Jacob Silber (W8 AL), first='Jacob', last='Silber'
  -2545560017: Pamela Wright (W3 AL), first='Pamela', last='Wright'

BLOCKS 18-25 — 8 ward councillors, all link to LOCAL district, title='City Councilor (Ward N)':
  -2545560018: R. Lisle Baker (W7), full_name='R. Lisle Baker', first='Lisle', last='Baker', title='City Councilor (Ward 7)'
  -2545560019: Martha Bixby (W6), first='Martha', last='Bixby', title='City Councilor (Ward 6)'
  -2545560020: Randy Block (W4), first='Randy', last='Block', title='City Councilor (Ward 4)'
  -2545560021: Stephen Farrell (W8), first='Stephen', last='Farrell', title='City Councilor (Ward 8)'
  -2545560022: Maria S. Greenberg (W1), full_name='Maria S. Greenberg', first='Maria', last='Greenberg', title='City Councilor (Ward 1)'
  -2545560023: Julie Irish (W5), first='Julie', last='Irish', title='City Councilor (Ward 5)'
  -2545560024: Julia Malakie (W3), first='Julia', last='Malakie', title='City Councilor (Ward 3)'
  -2545560025: David Micley (W2), first='David', last='Micley', title='City Councilor (Ward 2)'

Each politician+office block uses the Worcester 351 pattern:
  WITH ins_p AS (INSERT INTO essentials.politicians (...) VALUES (...) ON CONFLICT (external_id) DO NOTHING RETURNING id)
  INSERT INTO essentials.offices (...) SELECT gen_random_uuid(), d.id, chamber_subquery, p.id, 'title', 'MA', false, false, NULL
  FROM essentials.districts d CROSS JOIN ins_p p
  WHERE d.geo_id = '2545560' AND d.district_type = '[LOCAL_EXEC|LOCAL]' AND d.state = 'ma'
    AND p.id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id)

The chamber_subquery in each block:
  (SELECT id FROM essentials.chambers WHERE name = 'City Council'
   AND government_id = (SELECT id FROM essentials.governments WHERE name = 'City of Newton, Massachusetts, US'))

STEP 5 — office_id back-fill:
  UPDATE essentials.politicians p SET office_id = o.id
  FROM essentials.offices o
  WHERE o.politician_id = p.id
    AND p.external_id BETWEEN -2545560025 AND -2545560001
    AND p.office_id IS NULL

STEP 6 — Post-verification DO block with 7 gates:
  Gate (a): governments WHERE name='City of Newton, Massachusetts, US' = 1
  Gate (b): chambers WHERE name='City Council' AND government_id=(Newton gov) = 1
  Gate (c): districts WHERE state='ma' AND geo_id='2545560' AND district_type IN ('LOCAL_EXEC','LOCAL') = 2
  Gate (d): politicians WHERE external_id BETWEEN -2545560025 AND -2545560001 = 25
  Gate (e): offices JOIN districts WHERE state='ma' AND geo_id='2545560' AND district_type IN ('LOCAL_EXEC','LOCAL') = 25
  Gate (f): section-split: geofence_boundaries WHERE geo_id='2545560' AND mtfcc='G4110' AND NOT EXISTS (districts WHERE geo_id='2545560' AND state='ma') = 0
  Gate (g): politicians WHERE external_id BETWEEN -2545560025 AND -2545560001 AND office_id IS NULL = 0
  On all failures: RAISE EXCEPTION with count; on pass: RAISE NOTICE with all counts.

STEP 7 — Ledger entry:
  INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('578') ON CONFLICT (version) DO NOTHING
  </action>

  <verify>
    <automated>
      Apply migration via mcp__supabase-local execute_sql. The post-verification DO block in the migration itself raises EXCEPTION on any gate failure. Migration succeeds = all 7 gates passed.

      Spot-check queries after apply:
        SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2545560025 AND -2545560001;
        -- Expected: 25

        SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = '2545560' AND d.state = 'ma';
        -- Expected: 25

        SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id = '2545560' AND gb.mtfcc = 'G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.state = 'ma');
        -- Expected: 0 (section-split clean)

        SELECT title FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id = -2545560001;
        -- Expected: 'Mayor'

        SELECT title FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id = -2545560018;
        -- Expected: 'City Councilor (Ward 7)'
    </automated>
  </verify>

  <done>
    migration 578 applied without errors; post-verification DO block emitted PASSED notice; 25 politicians + 25 offices in DB; section-split returns 0; no NULL office_ids; title 'City Councilor' (not 'City Councillor') confirmed on at least one at-large councillor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → Supabase production | Data inserted directly via mcp__supabase-local; no user input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | migration SQL — government/politician rows | mitigate | WHERE NOT EXISTS + ON CONFLICT DO NOTHING guards prevent duplicate rows; pre-flight EXCEPTION blocks abort on dirty state |
| T-117-02 | Tampering | office_id back-fill UPDATE | mitigate | WHERE office_id IS NULL ensures idempotent update; no risk of overwriting prior valid office_id |
| T-117-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; existing project dependencies only |
</threat_model>

<verification>
After migration 578 applies successfully:

1. Newton government row in essentials.governments (name = 'City of Newton, Massachusetts, US')
2. Newton City Council chamber in essentials.chambers (name_formal = 'Newton City Council')
3. 2 district rows: LOCAL_EXEC (Newton (Citywide)) + LOCAL (Newton), both geo_id='2545560', state='ma'
4. 25 politicians in external_id range -2545560025 to -2545560001
5. 25 offices: 1 linked to LOCAL_EXEC + 24 linked to LOCAL
6. Section-split check: 0 rows (geo_id='2545560', mtfcc='G4110' geofence has matching district)
7. 0 NULL office_ids in external_id range
8. Title spelling confirmed: 'City Councilor' (not 'City Councillor')
</verification>

<success_criteria>
Migration 578 applied without error; post-verification DO block emits NOTICE with gov=1, chambers=1, districts=2, politicians=25, offices=25, split_orphans=0, null_office_ids=0. Newton city LOCAL section is reachable from any Newton, MA address via existing routing.
</success_criteria>

<output>
Create .planning/phases/117-newton-deep-seed/117-01-SUMMARY.md when done.
</output>
