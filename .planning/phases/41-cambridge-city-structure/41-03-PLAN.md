---
phase: 41-cambridge-city-structure
plan: 03
type: execute
wave: 3
depends_on:
  - 41-01
  - 41-02
files_modified:
  - C:/EV-Accounts/backend/migrations/159_cambridge_incumbents.sql
  - src/pages/Landing.jsx

autonomous: true

must_haves:
  truths:
    - "All 9 City Councillor office rows have a politician_id assigned (no unassigned councillor offices)"
    - "All 6 School Committee Member office rows have a politician_id assigned"
    - "Mayor office has politician_id = Siddiqui's id AND is_appointed_position=true"
    - "City Manager office has politician_id = Yi-An Huang's id AND is_appointed_position=true"
    - "Siddiqui's politicians.office_id points to the Mayor office (primary display title)"
    - "Siddiqui's City Councillor office row also has politician_id = Siddiqui's id (dual-office)"
    - "All 16 politicians have at least one URL in urls[] (bio page)"
    - "Cambridge appears in Landing.jsx COVERAGE_AREAS with browseGovernmentList: ['2511000']"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/159_cambridge_incumbents.sql"
      provides: "All 16 Cambridge incumbents seeded with contact data"
      contains: "Siddiqui.*Mayor.*City Councillor|dual.*office"
    - path: "src/pages/Landing.jsx"
      provides: "Cambridge browse entry in COVERAGE_AREAS"
      contains: "Cambridge.*2511000|2511000.*Cambridge"
  key_links:
    - from: "essentials.politicians"
      to: "essentials.offices"
      via: "office_id + offices.politician_id (bidirectional)"
      pattern: "UPDATE essentials.offices SET politician_id"
---

<objective>
Seed all 16 Cambridge incumbents via migration 159 and add Cambridge to the Landing page COVERAGE_AREAS.

Purpose: This completes Phase 41. After this plan, every Cambridge office has a named incumbent with contact data, and Cambridge is discoverable from the landing page.
Output: Migration 159 applied; Landing.jsx updated with Cambridge entry.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-cambridge-city-structure/41-RESEARCH.md
@.planning/phases/41-cambridge-city-structure/41-02-SUMMARY.md

Reference migrations for pattern:
@C:/EV-Accounts/backend/migrations/091_plano_politicians.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 159 — all Cambridge incumbents</name>
  <files>C:/EV-Accounts/backend/migrations/159_cambridge_incumbents.sql</files>
  <action>
Create migration file that seeds all 16 Cambridge incumbents. Key rules:

**MAYOR IDENTITY: Sumbul Siddiqui — NOT McGovern.**
Research confirmed Siddiqui was unanimously elected Mayor by the council on January 5, 2026 (third term).
McGovern is a regular City Councillor only.

**Siddiqui dual-office pattern:**
- Siddiqui holds BOTH a City Councillor office (elected) AND the Mayor office (appointed)
- Her `politicians.office_id` = the Mayor office UUID (Mayor is primary display title)
- Her City Councillor office row ALSO has `politician_id = siddiqui.id`
- This means she appears in the councillor listing AND has "Mayor" as her profile title

**Per-politician patterns:**
- `party = NULL, party_short_name = NULL` — antipartisan design
- `is_active = true, is_incumbent = true, is_vacant = false`
- `valid_from = '2026-01-05'` (January 5, 2026 inauguration)
- `valid_to = '2028-01-01', term_date_precision = 'month'` (2-year terms)
- `data_source = 'cambridgema.gov'` for councillors / `'cpsd.us'` for school committee
- No `external_id` (local city pattern — TX migrations 088-091 do not use external_id for city politicians)
- `is_appointed = false` for elected councillors/school committee; `true` for Mayor and City Manager

**Al-Zubi email:** If uncertain about hyphen format at execution time, use NULL for email_addresses and populate bio URL only. The official site shows `aal-zubi@cambridgema.gov` — use this if confirmed, otherwise NULL.

**City Manager bio URL:** No individual bio page exists — use department page: `https://www.cambridgema.gov/Departments/citymanagersoffice`

**School Committee emails:** All from cpsd.us (better individual contact data per CONTEXT.md).
Richard Harding Jr. uses a Gmail: `harding4cambridge@gmail.com` — this is publicly listed, use it.

Migration structure: One DO block per politician (follow migration 091 pattern exactly).

```sql
-- =============================================================================
-- Migration 159: Cambridge, MA — all incumbents seeded with contact data
--
-- 16 politicians total:
--   9 City Councillors (including Siddiqui who also holds Mayor office)
--   1 Mayor (Siddiqui — dual-office, is_appointed=true)
--   1 City Manager (Yi-An Huang, is_appointed=true)
--   6 School Committee Members
--
-- MAYOR IS SIDDIQUI, NOT McGOVERN.
-- Siddiqui: politicians.office_id = Mayor office (primary display)
--           City Councillor office also has politician_id = Siddiqui's id
--
-- Spelling: "Councillor" (double-L) — Cambridge British spelling
-- Depends on: migrations 157 and 158
-- =============================================================================

BEGIN;

-- ─────────────────────────────────────────────────────────────────────────────
-- SIDDIQUI — City Councillor + Mayor (dual-office)
-- office_id = Mayor (primary display title is "Mayor" on her profile)
-- City Councillor office also wired to her id
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_mayor_office_id      UUID;
  v_councillor_office_id UUID;
  v_politician_id        UUID;
BEGIN
  SELECT o.id INTO v_mayor_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000' AND o.title = 'Mayor';

  SELECT o.id INTO v_councillor_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Sumbul', 'Siddiqui', 'Sumbul', 'Sumbul Siddiqui',
    NULL, NULL,
    true, true, false, true,
    v_mayor_office_id,
    '2026-01-05', '2028-01-01', 'month',
    ARRAY['mayor@cambridgema.gov', 'ssiddiqui@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/sumbulsiddiqui'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_mayor_office_id;
  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_councillor_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- AZEEM — City Councillor (Vice Mayor title, but NOT a separate DB office)
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Burhan', 'Azeem', 'Burhan', 'Burhan Azeem',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['bazeem@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/burhanazeem'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- FLAHERTY — City Councillor (new Jan 2026)
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Tim', 'Flaherty', 'Tim', 'Tim Flaherty',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['tflaherty@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/timflaherty'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- McGOVERN — City Councillor (NOT Mayor — Mayor is Siddiqui)
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Marc', 'McGovern', 'Marc', 'Marc C. McGovern',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['mmcgovern@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/marcmcgovern'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- NOLAN — City Councillor
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Patricia', 'Nolan', 'Patricia', 'Patricia M. Nolan',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['pnolan@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/patricianolan'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- SIMMONS — City Councillor
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'E. Denise', 'Simmons', 'Denise', 'E. Denise Simmons',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['dsimmons@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/denisesimmons'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- SOBRINHO-WHEELER — City Councillor
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Jivan', 'Sobrinho-Wheeler', 'Jivan', 'Jivan Sobrinho-Wheeler',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['jsobrinhowheeler@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/jivansobrinhowheeler'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- AL-ZUBI — City Councillor (new Jan 2026)
-- Note: Email aal-zubi@cambridgema.gov uses a hyphen — verify at execution time.
-- If hyphen format unconfirmed, use NULL for email_addresses.
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Ayah', 'Al-Zubi', 'Ayah', 'Ayah A. Al-Zubi',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['aal-zubi@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/ayahaalzubi'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- ZUSY — City Councillor
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'City Councillor'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Catherine', 'Zusy', 'Catherine', 'Catherine Zusy',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['czusy@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citycouncil/members/catherinezusy'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- CITY MANAGER: Yi-An Huang (appointed, not elected)
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000' AND o.title = 'City Manager';

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Yi-An', 'Huang', 'Yi-An', 'Yi-An Huang',
    NULL, NULL,
    true, true, false, true,
    v_office_id, '2022-01-01', NULL, 'year',
    ARRAY['citymanager@cambridgema.gov'],
    ARRAY['https://www.cambridgema.gov/Departments/citymanagersoffice'],
    'cambridgema.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- SCHOOL COMMITTEE — 6 members (cpsd.us is authoritative for emails)
-- ─────────────────────────────────────────────────────────────────────────────
DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'School Committee Member'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'David', 'Weinstein', 'David', 'David Weinstein',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['dweinstein@cpsd.us'],
    ARRAY['https://www.cpsd.us/school-committee/school-committee-members-subcommittees'],
    'cpsd.us'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'School Committee Member'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Caitlin', 'Dube', 'Caitlin', 'Caitlin Dube',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['cadube@cpsd.us'],
    ARRAY['https://www.cpsd.us/school-committee/school-committee-members-subcommittees'],
    'cpsd.us'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'School Committee Member'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Luisa', 'de Paula Santos', 'Luisa', 'Luisa de Paula Santos',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['ldepaulasantos@cpsd.us'],
    ARRAY['https://www.cpsd.us/school-committee/school-committee-members-subcommittees'],
    'cpsd.us'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'School Committee Member'
    AND o.politician_id IS NULL
  LIMIT 1;

  -- Richard Harding Jr. uses a Gmail address — publicly listed on cpsd.us
  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Richard', 'Harding', 'Richard', 'Richard Harding, Jr.',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['harding4cambridge@gmail.com'],
    ARRAY['https://www.cpsd.us/school-committee/school-committee-members-subcommittees'],
    'cpsd.us'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'School Committee Member'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Elizabeth', 'Hudson', 'Elizabeth', 'Elizabeth Hudson',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['ehudson@cpsd.us'],
    ARRAY['https://www.cpsd.us/school-committee/school-committee-members-subcommittees'],
    'cpsd.us'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

DO $$
DECLARE
  v_office_id     UUID;
  v_politician_id UUID;
BEGIN
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '2511000'
    AND o.title = 'School Committee Member'
    AND o.politician_id IS NULL
  LIMIT 1;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id, valid_from, valid_to, term_date_precision,
    email_addresses, urls, data_source
  ) VALUES (
    'Arjun', 'Jaikumar', 'Arjun', 'Arjun Jaikumar',
    NULL, NULL,
    true, true, false, false,
    v_office_id, '2026-01-05', '2028-01-01', 'month',
    ARRAY['ajaikumar@cpsd.us'],
    ARRAY['https://www.cpsd.us/school-committee/school-committee-members-subcommittees'],
    'cpsd.us'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

COMMIT;
```
  </action>
  <verify>
Apply the migration:
```bash
psql $DATABASE_URL -f C:/EV-Accounts/backend/migrations/159_cambridge_incumbents.sql
```

Verify all offices now have politicians assigned:
```bash
psql $DATABASE_URL -c "
SELECT o.title, p.full_name, p.email_addresses[1], p.is_appointed
FROM essentials.offices o
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
LEFT JOIN essentials.politicians p ON p.id = o.politician_id
WHERE g.geo_id = '2511000'
ORDER BY o.title, p.last_name;"
```

Verify no unassigned offices remain:
```bash
psql $DATABASE_URL -c "
SELECT COUNT(*) AS unassigned
FROM essentials.offices o
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
WHERE g.geo_id = '2511000' AND o.politician_id IS NULL;"
```
Expected: 0

Verify Siddiqui dual-office:
```bash
psql $DATABASE_URL -c "
SELECT p.full_name, o.title AS primary_office,
       (SELECT o2.title FROM essentials.offices o2
        WHERE o2.politician_id = p.id AND o2.id != p.office_id
        LIMIT 1) AS secondary_office
FROM essentials.politicians p
JOIN essentials.offices o ON o.id = p.office_id
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
WHERE g.geo_id = '2511000' AND p.full_name = 'Sumbul Siddiqui';"
```
Expected: primary_office = 'Mayor', secondary_office = 'City Councillor'
  </verify>
  <done>
- Migration file exists at C:/EV-Accounts/backend/migrations/159_cambridge_incumbents.sql
- psql apply exits with no errors
- 0 unassigned office rows for geo_id='2511000'
- Siddiqui has primary office = Mayor; Councillor office also wired to her id
- Yi-An Huang assigned to City Manager office with is_appointed=true
- All 16 politicians have at least 1 URL populated
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Cambridge to Landing.jsx COVERAGE_AREAS</name>
  <files>src/pages/Landing.jsx</files>
  <action>
Read the current COVERAGE_AREAS array in Landing.jsx and add the Cambridge entry.

The current array has 3 entries (Monroe County, Los Angeles County, Collin County).
Add Cambridge as the 4th entry at the end of the array.

**Cambridge entry to add:**
```javascript
{ county: 'Cambridge', state: 'Massachusetts', browseGovernmentList: ['2511000'], browseStateAbbrev: 'MA' }
```

No `address` field (Cambridge uses browseGovernmentList, not address-based lookup).
No `browseCountyGeoId` (Cambridge is a city, not a county — no congressional intersection needed).

The `county` field renders as the primary label ("Cambridge") and `state` renders as the secondary label ("Massachusetts") in the existing UI component — this matches the visual pattern for other entries.

After adding the entry, verify the array has exactly 4 entries and Cambridge renders correctly.
  </action>
  <verify>
```bash
grep -A2 "Cambridge" src/pages/Landing.jsx
```
Should show the new COVERAGE_AREAS entry.

Also check that the COVERAGE_AREAS array has exactly 4 entries:
```bash
grep -c "county:" src/pages/Landing.jsx
```
Expected: 4
  </verify>
  <done>
- Landing.jsx has Cambridge entry with browseGovernmentList: ['2511000'] and browseStateAbbrev: 'MA'
- COVERAGE_AREAS array has exactly 4 entries
- No syntax errors introduced
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end — Cambridge officials returned for a Cambridge address</name>
  <files></files>
  <action>
Run a point-query smoke test using the production database to confirm Cambridge officials are returned for a Cambridge address.

Use the essentialsService pattern — query geofence_boundaries for geo_id='2511000' and verify politicians are returned:

```bash
psql $DATABASE_URL -c "
SELECT p.full_name, o.title, ch.name AS chamber
FROM essentials.politicians p
JOIN essentials.offices o ON o.id = p.office_id
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
WHERE g.geo_id = '2511000'
  AND p.is_active = true
ORDER BY ch.name, o.title, p.last_name;"
```

Expected: 16 rows — 9 City Councillors (including Siddiqui appearing as Mayor), 6 School Committee Members, 1 City Manager.

Note: Siddiqui's row will show title = 'Mayor' (her primary office_id). Her City Councillor office also has her politician_id set, but the SELECT on politicians.office_id returns only her primary office.

Also verify the geofence boundary exists:
```bash
psql $DATABASE_URL -c "
SELECT geo_id, mtfcc, state, ST_IsValid(geom) as valid
FROM essentials.geofence_boundaries
WHERE geo_id = '2511000';"
```
Expected: 1 row (loaded in Phase 38).
  </action>
  <verify>
```bash
psql $DATABASE_URL -c "
SELECT COUNT(*) FROM essentials.politicians p
JOIN essentials.offices o ON o.id = p.office_id
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
WHERE g.geo_id = '2511000' AND p.is_active = true;"
```
Returns 16.
  </verify>
  <done>
- 16 active politicians confirmed for Cambridge (geo_id='2511000')
- Geofence boundary exists and is valid (geom IS NOT NULL, ST_IsValid=true)
- Siddiqui appears as Mayor (primary office)
- Yi-An Huang appears as City Manager
- All 6 School Committee members present
  </done>
</task>

</tasks>

<verification>
```bash
psql $DATABASE_URL -c "
SELECT o.title, COUNT(*) as cnt, COUNT(o.politician_id) as assigned
FROM essentials.offices o
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
WHERE g.geo_id = '2511000'
GROUP BY o.title ORDER BY o.title;"
```

Expected (cnt = assigned for all rows):
```
City Councillor          | 9 | 9
City Manager             | 1 | 1
Mayor                    | 1 | 1
School Committee Member  | 6 | 6
```

```bash
grep "Cambridge" src/pages/Landing.jsx
```
Expected: COVERAGE_AREAS entry with browseGovernmentList: ['2511000']
</verification>

<success_criteria>
- CAMB-04: Yi-An Huang seeded as City Manager incumbent with is_appointed_position=true
- CAMB-05: Siddiqui linked to Mayor office (is_appointed_position=true); Mayor is NOT local_exec
- CAMB-06: All 9 Councillor offices + 6 School Committee offices have January 2026 incumbents
- CAMB-07: Contact data populated — all councillors have cambridgema.gov emails + bio URLs; all school committee members have cpsd.us emails + URL
- LAND-01: Cambridge appears in Landing.jsx COVERAGE_AREAS with browseGovernmentList: ['2511000'] and browseStateAbbrev: 'MA'
- 0 unassigned offices for Cambridge
- Siddiqui dual-office verified: Mayor (primary) + City Councillor (secondary)
</success_criteria>

<output>
After completion, create `.planning/phases/41-cambridge-city-structure/41-03-SUMMARY.md` using the summary template.
Include:
- All 16 politicians confirmed with names
- Siddiqui dual-office verified
- Cambridge COVERAGE_AREAS entry confirmed in Landing.jsx
- Confirmation migrations 157-159 all applied
- Note: next migration is 160
</output>
