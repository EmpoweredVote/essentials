---
phase: 89-in-me-school-board-completion
plan: 03
type: execute
wave: 3
depends_on:
  - 89-01
  - 89-02
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py
  - C:/EV-Accounts/backend/migrations/266_me_in_school_headshots.sql
  - "Supabase Storage: politician_photos/{politician_id}-headshot.jpg (live uploads for available photos)"
  - "essentials.politician_images (live INSERTs for available photos)"
autonomous: false
requirements:
  - IN-SCHOOL-01
  - IN-SCHOOL-02
  - ME-SCHOOL-01
  - ME-SCHOOL-02
  - ME-SCHOOL-03
tags:
  - in
  - me
  - school-boards
  - headshots
  - supabase-storage
  - audit-only

must_haves:
  truths:
    - "Every available board member headshot (across IPS D2/D3, MCCSC D7 + 6 unchanged MCCSC, and 37 new ME members) uploaded to Supabase Storage at 600x750 JPEG (Lanczos q90, 4:5 crop first)"
    - "politician_images row exists for each uploaded headshot with type='default' (UI filter requirement per memory project_phase17_headshots.md and Phase 62 LAUSD lesson)"
    - "No politician_images row has type<>'default' for any Phase 89 politician (silent UI invisibility prevented)"
    - "Migration 266 (audit-only SQL) documents every Phase 89 politician's photo source URL OR 'No photo found' status — 40 documented entries minimum (Hope Duke Star + Hasaan Rashid + Aja Jester + 37 ME members) and optionally includes IPS D1/D4/D5/At-Large×2 + MCCSC D1-D6 if they did not already have photos"
    - "Migration 266 has the AUDIT-ONLY safety guard at top that raises an EXCEPTION if anyone applies it"
    - "Migration 266 does NOT contain an `INSERT INTO supabase_migrations.schema_migrations` entry (audit-only — never applied to ledger)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py"
      provides: "Python PIL script for downloading + cropping to 4:5 + resizing to 600x750 JPEG q90 + uploading to Supabase Storage + INSERTing politician_images rows"
      contains: "_tmp-in-me-school-headshots"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/266_me_in_school_headshots.sql"
      provides: "Audit-only SQL documenting every Phase 89 official's headshot status with source URL or 'no photo found' comment"
      contains: "Migration 266"
      min_lines: 150
  key_links:
    - from: "essentials.politician_images.type='default'"
      to: "UI filter `.find(img => img.type === 'default')` in Profile.jsx / Results.jsx"
      via: "type column"
      pattern: "type='default'"
    - from: "essentials.politician_images.politician_id"
      to: "essentials.politicians.id (Phase 89 external_ids: -890001, -890011..-890018, -890021..-890027, -890031..-890037, -890041..-890048, -890051..-890057, plus updated IPS D2 and MCCSC D7 rows)"
      via: "FK"
      pattern: "politician_id"
    - from: "Supabase Storage politician_photos/{politician_id}-headshot.jpg"
      to: "politician_images.url"
      via: "public storage URL composed at INSERT time"
      pattern: "kxsdzaojfaibhuzmclfq.storage.supabase.co"
---

<objective>
Source, process, and upload headshots for every Phase 89 board member where a public online photo exists on the official school district website. Process every photo to 600x750 JPEG (Lanczos q90, 4:5 crop first per memory `feedback_headshot_resize_no_distort.md`). Insert a `politician_images` row with `type='default'` for each upload. Document every Phase 89 official's photo status in audit-only migration 266 (SQL only — never applied to Supabase ledger; same pattern as migrations 255, 258, 262).

Scope (per RESEARCH.md §Headshot Availability Summary):
- **IPS:** D3 (Hope Duke Star, new) + D2 (Hasaan Rashid, updated) need photos. RESEARCH.md confirms myips.org shows transparent GIF placeholders for ALL members → "No photo found on official IPS website" for all IPS members. Wikipedia / local news photos may exist but are out of scope per memory `feedback_headshot_no_graphics.md` and the established "official-site-only" policy.
- **MCCSC:** D7 (Aja Jester, updated) — check mccsc.edu/board. Optionally re-check D1-D6 photos if they were never uploaded; this is a backfill opportunity but not required by IN-SCHOOL-02.
- **ME:** All 37 new board members (Lewiston 8, Bangor 7, South Portland 7, Auburn 8, Biddeford 7) — check official district websites per district URL conventions in RESEARCH.md §Maine Board Rosters.

This is a Wave 3 plan that depends on Plan 01 (IPS D3 + MCCSC D7 politicians must exist) and Plan 02 (37 ME politicians must exist) so the `politician_id` UUIDs are stable.

Output:
- Live uploads to Supabase Storage at `politician_photos/{politician_id}-headshot.jpg` for each available photo
- Live `essentials.politician_images` rows (one per upload, `type='default'`)
- `C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py` (new helper script for the live upload pipeline)
- `C:/EV-Accounts/backend/migrations/266_me_in_school_headshots.sql` (new, AUDIT-ONLY, never applied) capturing every INSERT performed live plus a "No photo found" comment block for every official without an upload
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/89-in-me-school-board-completion/89-RESEARCH.md
@.planning/phases/89-in-me-school-board-completion/89-01-SUMMARY.md
@.planning/phases/89-in-me-school-board-completion/89-02-SUMMARY.md

# Canonical audit-only headshot migration (mirror this structure):
@C:/EV-Accounts/backend/migrations/258_ca_city_school_headshots.sql
@C:/EV-Accounts/backend/migrations/262_tx_collin_county_school_headshots.sql

# Canonical headshot processing script (Python PIL pattern — read for the crop+resize pipeline):
@C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write _tmp-in-me-school-headshots.py — headshot download/crop/resize/upload helper</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py (canonical Python PIL helper for OR school headshots — copy the download + crop + resize + Supabase Storage upload + politician_images INSERT pipeline)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Maine Board Rosters per-district sections for source URL conventions; §Headshot Availability Summary)
    - .planning/phases/89-in-me-school-board-completion/89-01-SUMMARY.md (Hope Duke Star politician_id UUID; Hasaan Rashid politician_id UUID — IPS D2 existing row; Aja Jester politician_id UUID — MCCSC D7 existing row)
    - .planning/phases/89-in-me-school-board-completion/89-02-SUMMARY.md (37 ME politician_id UUIDs)
  </read_first>
  <behavior>
    - Helper script accepts a list of `{politician_id, full_name, source_url, district_label}` entries either from a hard-coded ROSTER constant or from a queryable DB function that pulls all Phase 89 politicians.
    - For each entry: downloads the source image (curl with `User-Agent: Mozilla/5.0` if needed — per Sacramento AEM/CQ5 precedent in STATE.md), crops to 4:5 aspect ratio FIRST (center-crop wide / top-crop tall — NEVER stretch per memory `feedback_headshot_resize_no_distort.md`), then resizes to 600x750 using Lanczos (PIL `LANCZOS` resampling) at JPEG quality 90.
    - Uploads processed JPEG to Supabase Storage bucket `politician_photos` with object path `{politician_id}-headshot.jpg`.
    - INSERTs one row into `essentials.politician_images` per upload with `type='default'`, `photo_license='public_domain'`, `url` pointing to the public Storage URL, `politician_id` FK.
    - For each official without a photo: prints the URL checked and the "no photo" status to a log file (used as input for Task 3's audit migration).
    - Photos with superimposed text/graphics ("Re-Elect" banners, etc.) are REJECTED per memory `feedback_headshot_no_graphics.md` — recorded as "no photo".
    - Re-running the script for the same politician_id is a no-op (Storage upload uses upsert; politician_images INSERT uses `WHERE NOT EXISTS`).
  </behavior>
  <action>
    Create `C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py` by copying the structure of `_tmp-or-school-headshots.py` and adapting it for the Phase 89 rosters.

    REQUIRED IMPORTS: `from PIL import Image`, `import requests`, `import psycopg2`, `import os`, `from dotenv import load_dotenv`, `from io import BytesIO`. All available per RESEARCH.md §Environment Availability.

    REQUIRED FUNCTIONS (mirror the OR script):
    - `load_env()` — loads `C:/EV-Accounts/backend/.env` via dotenv.
    - `connect_db()` — returns a psycopg2 connection via DATABASE_URL with SSL.
    - `download_image(url, user_agent='Mozilla/5.0 ...')` — fetches the image bytes via requests; raises if status != 200.
    - `crop_to_45(img)` — center-crops horizontally if wider than 4:5 ratio (1.25 tall), top-crops vertically if taller than 4:5. NEVER stretch. Per memory `feedback_headshot_cropping.md`: eyes at ~1/3 from top for top-cropped images — i.e. crop bottom, not top, for tall sources.
    - `resize_600_750(img)` — uses `img.resize((600, 750), Image.LANCZOS)`.
    - `save_jpeg_q90(img, path)` — saves with `quality=90, optimize=True`.
    - `upload_to_storage(politician_id, jpeg_bytes)` — uploads to Supabase Storage `politician_photos` bucket via the Storage REST API (use `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` from `.env`; upsert=true).
    - `insert_politician_image(politician_id)` — SQL: `INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) VALUES (gen_random_uuid(), %s, %s, 'default', 'public_domain') ON CONFLICT DO NOTHING` with `WHERE NOT EXISTS` guard on `(politician_id)` to prevent duplicate rows for the same person.
    - `main()` — iterates the ROSTER list, for each entry calls download → crop → resize → save → upload → insert. Logs progress and final summary (uploaded count vs no-photo count).

    REQUIRED ROSTER CONSTANT (use the politician_ids captured in Plan 01 SUMMARY and Plan 02 SUMMARY, plus the source URL patterns from RESEARCH.md):
    - **IPS (3 entries):** D3 Hope Duke Star (external_id=-890001), D2 Hasaan Rashid (D2 existing external_id from Plan 01), MCCSC D7 Aja Jester. Source URL = `https://myips.org/district-school-board/school-board/` for IPS, `https://www.mccsc.edu/board` for MCCSC. Per RESEARCH.md, IPS shows transparent GIFs only → all 3 IPS officials will likely fall to "no photo found" status; document the URL checked.
    - **Lewiston (8 entries):** -890011..-890018 with politician_ids from Plan 02 SUMMARY. Source URL = `https://www.lewistonpublicschools.org/en-US/school-committee-bc9f3846/school-committee-members-ab24c81b`. RESEARCH.md notes Thrillshare CMS — photos may exist; check each profile.
    - **Bangor (7 entries):** -890021..-890027. Source URL = `https://www.bangorschools.net/page/school-committee`. Thrillshare CMS.
    - **South Portland (7 entries):** -890031..-890037. Source URL = `https://www.spsd.org/board/members-of-the-board` or `https://www.spsdme.org/board`. JS-heavy CMS — may require curl with browser UA.
    - **Auburn (8 entries):** -890041..-890048. Source URL = `https://auburnschl.edu` (locate School Committee page).
    - **Biddeford (7 entries):** -890051..-890057. Source URL = `https://biddefordschools.me` (locate School Committee page).

    Total: 3 IN + 37 ME = 40 ROSTER entries. The script attempts each; on failure (404, blocked, no photo on page), it logs "no photo" and continues.

    OUTPUT LOG: `C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt` — one line per official:
    `<external_id>|<full_name>|<district>|UPLOADED OR NO_PHOTO|<source_url_or_url_checked>|<dimensions_and_crop_notes>`
    This log is the source-of-truth input for Task 3's audit migration.

    REJECT photos with superimposed text/graphics per memory `feedback_headshot_no_graphics.md` — if a downloaded image visually contains "Re-Elect" or similar overlay, log as NO_PHOTO with note `rejected: overlay graphic`.

    Per memory `feedback_no_git_in_ev_accounts.md`: do NOT run git in `C:/EV-Accounts`.
    Per memory `feedback_supabase_local_is_remote.md`: every Storage upload + DB INSERT is live production.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py','utf8');const needles=['from PIL import Image','LANCZOS','600','750','quality=90','politician_photos','politician_images','type','default','public_domain','myips.org','lewistonpublicschools.org','bangorschools.net','spsd.org','auburnschl.edu','biddefordschools.me'];const missing=needles.filter(n=>!c.includes(n));if(missing.length){console.error('MISSING:',missing);process.exit(1)}if(/type\\s*=\\s*[\\u0027\"](?!default)/.test(c)){console.error('FAIL: politician_images.type literal must be default');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py`
    - File imports PIL `Image`, `requests`, `psycopg2`, `dotenv`
    - File contains literal `LANCZOS` (resampling algorithm — per memory `feedback_headshot_image_sizing.md`)
    - File contains both `600` and `750` literals (final dimensions per memory)
    - File contains `quality=90` (JPEG quality)
    - File contains `politician_photos` (Storage bucket)
    - File contains `politician_images` (DB table)
    - File contains `'default'` (politician_images.type — UI filter requirement)
    - File contains `'public_domain'` (photo_license)
    - File contains all 6 source URL patterns: `myips.org`, `lewistonpublicschools.org`, `bangorschools.net`, `spsd.org`, `auburnschl.edu`, `biddefordschools.me`
    - File does NOT contain any politician_images.type value other than 'default' (no `type='headshot'` or similar — would cause silent UI invisibility per project memory)
    - ROSTER constant contains 40 entries (3 IN + 37 ME) OR the script dynamically queries them from the DB via Phase 89 external_id ranges
    - Output log file written to `C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt` with one line per official
  </acceptance_criteria>
  <done>Helper script authored with the headshot processing pipeline, ready to run live in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run _tmp-in-me-school-headshots.py — live source/process/upload for all 40 Phase 89 officials, district-by-district</name>
  <files>
    - Supabase Storage: politician_photos bucket (up to 40 new files, one per uploaded photo)
    - essentials.politician_images (live INSERTs, one row per uploaded photo)
    - C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt (working log used as input for Task 3)
  </files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py (just authored)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Headshot Availability Summary — confirms IPS has transparent placeholders only; all other districts unknown)
    - .planning/phases/89-in-me-school-board-completion/89-01-SUMMARY.md (politician_ids for IPS D3 + IPS D2 + MCCSC D7)
    - .planning/phases/89-in-me-school-board-completion/89-02-SUMMARY.md (politician_ids for 37 ME officials)
  </read_first>
  <behavior>
    - Script runs through each district in order (IPS, MCCSC, Lewiston, Bangor, South Portland, Auburn, Biddeford), attempting one download+process+upload per official.
    - For uploaded photos: Storage object exists at `politician_photos/{politician_id}-headshot.jpg` (600x750 JPEG q90), politician_images row exists with `type='default'`.
    - For "no photo" officials: working log entry records the URL checked + reason (404, no image element on page, image is a transparent placeholder, image is a generic silhouette, overlay text rejection per memory).
    - Re-running the script for the same official is a no-op (Storage upsert; politician_images NOT EXISTS guard).
    - Photos with superimposed text/graphics ("Re-Elect" overlays, watermarks) are rejected per memory `feedback_headshot_no_graphics.md`.
    - Photos with wrong aspect ratio are processed correctly (crop to 4:5 FIRST per memory `feedback_headshot_resize_no_distort.md`, then resize to 600x750 — never stretch).
  </behavior>
  <action>
    Execute the script district-by-district (do NOT run all 7 districts in one shot — bounded context use; commit progress to the working log after each district).

    Per RESEARCH.md §Headshot Availability Summary and project memory `feedback_supabase_local_is_remote.md`:

    1. **IPS (3 officials: -890001 Hope Duke Star + D2 Hasaan Rashid + MCCSC D7 Aja Jester):**
       - Confirm via `WebFetch` or curl that `https://myips.org/district-school-board/school-board/` still shows transparent GIF placeholders for all members. If still true → all 3 IPS officials get NO_PHOTO status with the URL checked logged.
       - If photos are now present (rare — check anyway): download each, crop 4:5, resize 600x750 LANCZOS q90, upload to Storage, INSERT politician_images row with type='default'.

    2. **MCCSC (1 official to attempt: D7 Aja Jester at minimum):**
       - Check `https://www.mccsc.edu/board` for Aja Jester's photo. Process + upload + INSERT if found. Document URL checked if not found.
       - Backfill opportunity: if D1-D6 MCCSC members do not have photos in `politician_images`, optionally process their photos as well (NOT required for IN-SCHOOL-02 completion — but document in the audit migration).

    3. **Lewiston (8 officials, external_ids -890011..-890018):**
       - Visit `https://www.lewistonpublicschools.org/en-US/school-committee-bc9f3846/school-committee-members-ab24c81b`.
       - For each of the 8 members, locate the photo URL (Thrillshare CMS may host them under `https://cdn.thrillshare.com/...`).
       - Process + upload + INSERT for each found photo. Document URL checked for any not found.
       - Lewiston Ward 5 may be vacant (per Plan 02 Task 3 reconciliation) → if so, skip (no politician_id to attach to is_vacant placeholder rows; document in audit).

    4. **Bangor (7 officials, -890021..-890027):**
       - Visit `https://www.bangorschools.net/page/school-committee`. Same processing pipeline as Lewiston.

    5. **South Portland (7 officials, -890031..-890037):**
       - Visit `https://www.spsd.org/board/members-of-the-board` (JS-rendered — may need curl with `User-Agent: Mozilla/5.0`).
       - Same processing pipeline. If site is unreachable, log "site blocked" for each official.

    6. **Auburn (8 officials, -890041..-890048):**
       - Visit `https://auburnschl.edu` (locate School Committee page). Same pipeline.

    7. **Biddeford (7 officials, -890051..-890057):**
       - Visit `https://biddefordschools.me` (locate School Committee page). Same pipeline.

    For every official: append one line to `C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt`:
    `<external_id>|<full_name>|<district>|UPLOADED|<source_url>|<dimensions> -> 4:5 crop -> 600x750 q90`
    OR
    `<external_id>|<full_name>|<district>|NO_PHOTO|<url_checked>|<reason: 404 / transparent placeholder / silhouette / overlay rejected / vacant>`

    REJECT any photo with superimposed "Re-Elect" or other overlay graphics per memory `feedback_headshot_no_graphics.md` — log as NO_PHOTO with reason `rejected: overlay graphic`.

    Per memory `feedback_no_git_in_ev_accounts.md`: do NOT run git in `C:/EV-Accounts`.

    After all 7 districts processed (or attempted), the working log is the source-of-truth input for Task 3's audit migration.
  </action>
  <verify>
    <automated>node -e "const {Pool}=require('pg');require('dotenv').config({path:'C:/EV-Accounts/backend/.env'});const p=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});(async()=>{const r=await p.query(\"SELECT COUNT(*)::int AS n FROM essentials.politician_images pi JOIN essentials.politicians pp ON pi.politician_id=pp.id WHERE (pp.external_id=-890001 OR pp.external_id BETWEEN -890057 AND -890011) AND pi.type='default'\");console.log('uploaded='+r.rows[0].n);const bad=await p.query(\"SELECT COUNT(*)::int AS n FROM essentials.politician_images pi JOIN essentials.politicians pp ON pi.politician_id=pp.id WHERE (pp.external_id=-890001 OR pp.external_id BETWEEN -890057 AND -890011) AND pi.type<>'default'\");if(bad.rows[0].n > 0){console.error('FAIL: '+bad.rows[0].n+' politician_images rows with type<>default — UI silently invisible');process.exit(1)}const fs=require('fs');if(!fs.existsSync('C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt')){console.error('FAIL: working log not written');process.exit(1)}console.log('OK');process.exit(0)})().catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - For every uploaded photo: Supabase Storage object exists at `politician_photos/{politician_id}-headshot.jpg`
    - For every uploaded photo: `essentials.politician_images` row exists with `type='default'`, `photo_license='public_domain'`, `url` pointing to the public Storage URL
    - `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians pp ON pi.politician_id=pp.id WHERE (pp.external_id=-890001 OR pp.external_id BETWEEN -890057 AND -890011) AND pi.type<>'default'` returns 0 (no UI-invisible rows)
    - For every upload: image binary at the Supabase Storage URL is exactly 600x750 JPEG (sample-check via ImageMagick `identify` or PIL `.size`)
    - No uploaded image contains superimposed text/graphics over the face
    - Working log `C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt` exists with one line per Phase 89 official (minimum 40 entries: 3 IN + 37 ME) — each marked UPLOADED or NO_PHOTO with source URL
    - For each NO_PHOTO entry: a clear reason recorded (404 / transparent placeholder / silhouette / overlay rejected / vacant)
  </acceptance_criteria>
  <done>All available headshots uploaded; working log captured covering all 40 Phase 89 officials; no UI-invisible type rows exist; no overlay-text photos uploaded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN VERIFY — Phase 89 photos render correctly in UI; Hasaan Rashid + Hope Duke Star + Aja Jester display</name>
  <files>None modified — visual UI inspection only</files>
  <read_first>
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Validation Architecture — Smoke Test Coordinates per district)
    - C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt (Task 2 working log)
  </read_first>
  <what-built>
    - Plan 01 (Wave 1): IPS D3 INSERT (Hope Duke Star) + D2 UPDATE (→ Hasaan Rashid) + MCCSC D7 UPDATE (→ Aja Jester) + 2 SCHOOL districts wired + office back-fill
    - Plan 02 (Wave 2): 5 ME governments + 37 ME politicians + 37 ME offices wired to 5 SCHOOL districts
    - Plan 03 Task 1+2 (this wave): live headshot uploads + politician_images rows for whichever Phase 89 officials had photos on official sites
  </what-built>
  <how-to-verify>
    Open the ev-ui app (local or deployed) and verify by entering addresses inside each district. Use the coordinates from RESEARCH.md §Smoke Test Coordinates as guidance, or real street addresses.

    1. **Indianapolis (IPS)** — Enter an Indianapolis address near (-86.1581, 39.7684). Confirm:
       - SCHOOL section appears in the elected officials view
       - 7 commissioners listed (D1 Ashley Thomas, D2 Hasaan Rashid, D3 Hope Duke Star, D4 Allissa Impink, D5 Dr. Nicole Carey, AL Angelia Moore, AL Deandra Thompson)
       - **D2 must display as Hasaan Rashid** (NOT Gayle Cosby — Plan 01 UPDATE must show through to the UI)
       - **D3 must display as Hope Duke Star** (newly inserted)
       - Photos render where uploaded; placeholders acceptable for IPS members (RESEARCH.md confirmed transparent GIFs only on official site)
       - Office title for D3 matches the IPS convention captured in Plan 01 SUMMARY (Commissioner vs Board Member)

    2. **Bloomington (MCCSC)** — Enter a Bloomington address near (-86.5264, 39.1653). Confirm:
       - SCHOOL section appears with 7 trustees
       - **D7 displays as Aja Jester** (NOT Brandon Shurr)
       - Photos render where uploaded (D7 Aja Jester specifically should be checked for presence/absence)

    3. **Lewiston (ME)** — Enter a Lewiston address near (-70.2140, 44.0978). Confirm:
       - 8 School Committee members listed (Wards 1-7 + At-Large)
       - Ward 5 displays correctly per Plan 02 Task 3 reconciliation (either the verified appointee name OR a "Vacant" indicator)
       - Office titles read 'School Committee Member (Ward N)' for wards and 'School Committee Member (At-Large)' for at-large
       - Photos render where uploaded

    4. **Bangor (ME)** — Enter a Bangor address near (-68.7772, 44.8012). Confirm:
       - 7 School Committee members listed
       - Office titles read 'School Committee Member' (no qualifier — all at-large per RESEARCH.md)
       - 7th-member identity matches Plan 02 Task 3 reconciliation
       - Photos render where uploaded

    5. **South Portland (ME)** — Enter a South Portland address near (-70.2788, 43.6415). Confirm:
       - 7 Board of Education members listed (Districts 1-5 + 2 At-Large)
       - Office titles read 'Board Member (District N)' or 'Board Member' per RESEARCH.md
       - D5 disposition matches Plan 02 Task 3 (filled or vacant)
       - Photos render where uploaded

    6. **Auburn (ME)** — Enter an Auburn address near (-70.2312, 44.0978). Confirm:
       - 8 School Committee members listed (Wards 1-5 + 3 At-Large)
       - Office titles read 'School Committee Member (Ward N)' for wards and 'School Committee Member (At-Large)' for at-large
       - Photos render where uploaded

    7. **Biddeford (ME)** — Enter a Biddeford address near (-70.4520, 43.4909). Confirm:
       - 7 School Committee members listed
       - Office titles read 'School Committee Member' (no qualifier — all at-large per RESEARCH.md)
       - Photos render where uploaded

    8. **Cross-tier check (at one ME address)** — Confirm the SCHOOL section appears alongside LOCAL (city council, e.g. Lewiston city), STATE (Maine legislature), and NATIONAL (Congress) sections — no empty sections, no routing errors.

    9. **Antipartisan check** — Confirm NO party label displays on any Phase 89 official's card (party=NULL per Plan 01 and Plan 02 INSERTs; per memory `feedback_antipartisan_display.md` party must never display on politician profiles).

    If any of these are wrong (members missing, wrong office titles, no photos rendering where uploaded, UTF-8 mojibake, party label visible, Gayle Cosby still showing for IPS D2, Brandon Shurr still showing for MCCSC D7), respond with "issues" and describe the problem. The plan will not advance until issues are resolved.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all 7 districts render correctly with Hope Duke Star, Hasaan Rashid, and Aja Jester displayed properly. Or describe issues to be addressed.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write migration 266 (audit-only) — document all 40 Phase 89 headshot statuses</name>
  <files>C:/EV-Accounts/backend/migrations/266_me_in_school_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/258_ca_city_school_headshots.sql (canonical audit-only template — read fully)
    - C:/EV-Accounts/backend/migrations/262_tx_collin_county_school_headshots.sql (Phase 88 audit-only — recent sibling, also follows the same pattern)
    - C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt (Task 2 working log — exhaustive list of every official's photo status + source URL)
    - .planning/phases/89-in-me-school-board-completion/89-01-SUMMARY.md (IPS D2 + IPS D3 + MCCSC D7 external_ids and politician_ids)
    - .planning/phases/89-in-me-school-board-completion/89-02-SUMMARY.md (37 ME official external_ids and politician_ids)
  </read_first>
  <behavior>
    - File documents every politician_images INSERT performed live in Task 2 (one block per uploaded photo) in the canonical pattern from migration 258.
    - For officials with no photo: a comment block records the official's name, external_id, district, URL(s) checked, and the reason no photo was uploaded — no INSERT.
    - Safety guard `RAISE EXCEPTION` at the top aborts any attempt to apply migration 266 via Supabase ledger.
    - Header summarizes per-district upload counts.
    - File saved as UTF-8 (preserve any accented characters in ME names).
    - File NOT applied via Supabase ledger — committed to git only.
  </behavior>
  <action>
    Write `C:/EV-Accounts/backend/migrations/266_me_in_school_headshots.sql` by copying the structure of `C:/EV-Accounts/backend/migrations/262_tx_collin_county_school_headshots.sql` (Phase 88 audit-only, most recent precedent). Adapt counts and content per the Task 2 working log.

    **Header:**
    ```
    -- Migration 266: Phase 89 IN + ME School Board Headshots
    -- AUDIT-ONLY: captures the live politician_images INSERTs performed during Phase 89 Plan 03
    -- execution on <YYYY-MM-DD>.
    -- DO NOT apply via Supabase ledger -- actual DB writes happened live via _tmp-in-me-school-headshots.py.
    -- Pattern matches 258_ca_city_school_headshots.sql / 262_tx_collin_county_school_headshots.sql.
    --
    -- 40 officials documented:
    --   Indianapolis Public Schools (IPS) — 3 entries (D2 Hasaan Rashid, D3 Hope Duke Star, optionally D1/D4/D5/AL members not previously uploaded) — <X>/3 uploaded
    --   Monroe County Community School Corporation (MCCSC) — at minimum D7 Aja Jester, optionally D1-D6 — <Y>/<N> uploaded
    --   Lewiston Public Schools (ME)            (8 officials, -890011..-890018) — <A>/8 uploaded
    --   Bangor School Department (ME)           (7 officials, -890021..-890027) — <B>/7 uploaded
    --   South Portland Public Schools (ME)      (7 officials, -890031..-890037) — <C>/7 uploaded
    --   Auburn Public Schools (ME)              (8 officials, -890041..-890048) — <D>/8 uploaded
    --   Biddeford Public Schools (ME)           (7 officials, -890051..-890057) — <E>/7 uploaded
    --
    -- Photo processing: crop to 4:5 ratio (center-crop wide / top-crop tall), then resize 600x750 Lanczos q90.
    -- Storage bucket: politician_photos; path: {politician_id}-headshot.jpg.
    -- politician_images.type = 'default' (UI filter: .find(img => img.type === 'default')).
    --
    -- Run: <YYYY-MM-DD>
    ```

    **Safety guard (mandatory — copy from migration 262 lines 26-29):**
    ```
    -- Safety guard: this file is AUDIT-ONLY. Abort if applied directly.
    DO $$
    BEGIN
      RAISE EXCEPTION 'Migration 266 is AUDIT-ONLY and must not be applied. Actual DB writes happened live via _tmp-in-me-school-headshots.py during Phase 89 Plan 03.';
    END $$;
    ```

    **Per-district section pattern:** Use section header comments per district (e.g. `-- ============= LEWISTON PUBLIC SCHOOLS =============`). For each official in that district, emit either an INSERT block (if photo uploaded) or a comment-only block (if not).

    **INSERT block per uploaded photo (per migration 258/262 lines 38-47):**
    ```
    -- <Full Name> (external_id=<ext_id>) — <district>, <title>
    -- source: <original URL from district website>
    -- original: <dimensions> JPEG <orientation> (<crop description> -> 4:5, resize 600x750)
    INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license)
    SELECT gen_random_uuid(),
           (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>),
           'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/' ||
             (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>)::text || '-headshot.jpg',
           'default', 'public_domain'
    WHERE NOT EXISTS (
      SELECT 1 FROM essentials.politician_images
      WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>)
    );
    ```

    **No-photo comment per official without photo:**
    ```
    -- <Full Name> (external_id=<ext_id>): No photo found.
    -- URL(s) checked: <URL>
    -- Reason: <404 / transparent placeholder / silhouette / overlay rejected / vacant seat>
    ```

    **40 entries minimum** — one per external_id covering at least Hope Duke Star (-890001), Hasaan Rashid (existing IPS D2 external_id from Plan 01 SUMMARY), Aja Jester (existing MCCSC D7 external_id from Plan 01 SUMMARY), plus all 37 ME external_ids (-890011..-890018, -890021..-890027, -890031..-890037, -890041..-890048, -890051..-890057). Every external_id MUST appear exactly once as either an INSERT or a no-photo comment.

    **For IPS — Hope Duke Star (-890001), Hasaan Rashid, and any IPS member NOT previously uploaded:** Per RESEARCH.md, myips.org shows transparent GIF placeholders only — every IPS entry should be a NO_PHOTO comment with `URL(s) checked: https://myips.org/district-school-board/school-board/` and `Reason: official site shows transparent GIF placeholders; no photos available`.

    **For MCCSC — Aja Jester (D7):** Either INSERT (if uploaded successfully) or NO_PHOTO with `URL(s) checked: https://www.mccsc.edu/board`.

    **NO ledger entry at the bottom** — this is AUDIT-ONLY. Do NOT include `INSERT INTO supabase_migrations.schema_migrations`.

    **No COMMIT statement** — the safety guard above prevents apply; no transaction wrapping needed.

    **File encoding:** UTF-8.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('C:/EV-Accounts/backend/migrations/266_me_in_school_headshots.sql','utf8');const needles=['Migration 266','AUDIT-ONLY','RAISE EXCEPTION','LEWISTON','BANGOR','SOUTH PORTLAND','AUBURN','BIDDEFORD','politician_photos','Hope Duke Star','Hasaan Rashid','Aja Jester'];const missing=needles.filter(n=>!c.toUpperCase().includes(n.toUpperCase()));if(missing.length){console.error('MISSING:',missing);process.exit(1)}const meIds=[];for(let r of [[11,18],[21,27],[31,37],[41,48],[51,57]]){for(let i=r[0];i<=r[1];i++){const n=String(i).padStart(3,'0');meIds.push('-890'+n)}}meIds.push('-890001');const missingIds=meIds.filter(e=>!c.includes(e));if(missingIds.length){console.error('MISSING external_ids:',missingIds);process.exit(1)}if(c.includes('schema_migrations')){console.error('FAIL: audit-only must NOT include schema_migrations ledger insert');process.exit(1)}if(/type\\s*=\\s*\\u0027(?!default)/.test(c)){console.error('FAIL: politician_images.type must be default everywhere');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/266_me_in_school_headshots.sql`
    - File contains literal `Migration 266`
    - File contains literal `AUDIT-ONLY`
    - File contains the safety-guard `RAISE EXCEPTION 'Migration 266 is AUDIT-ONLY` block at top
    - File contains all 5 ME district section headers (`LEWISTON`, `BANGOR`, `SOUTH PORTLAND`, `AUBURN`, `BIDDEFORD`) plus an IPS + MCCSC section
    - File contains all 38 unique Phase 89 external_id references: `-890001` (Hope Duke Star) plus each of `-890011..-890018`, `-890021..-890027`, `-890031..-890037`, `-890041..-890048`, `-890051..-890057` (37 ME ids)
    - File contains literal `Hope Duke Star`, `Hasaan Rashid`, and `Aja Jester`
    - Every `politician_images` INSERT in the file uses `'default'` for type (no other type values appear — UI filter requirement)
    - File does NOT contain `INSERT INTO supabase_migrations.schema_migrations` (audit-only — no ledger entry)
    - File does NOT contain `COMMIT;` (no transaction wrapping needed — safety guard at top blocks apply)
    - File contains literal `politician_photos` (Supabase Storage bucket name)
    - For every external_id covered: EITHER an `INSERT INTO essentials.politician_images` block exists OR a `No photo found` comment exists — never both, never neither
    - For all IPS officials (D2 Hasaan Rashid + D3 Hope Duke Star + any other IPS member documented): records `No photo found` with reason citing the transparent GIF placeholders on myips.org (per RESEARCH.md confirmation)
  </acceptance_criteria>
  <done>Migration 266 audit file authored documenting all Phase 89 headshot statuses, UTF-8 preserved, safety guard in place, no ledger entry, ready to commit to git but never apply to Supabase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official school district website → local processing → Supabase Storage | Headshot images downloaded from public ISD websites; processed locally via PIL (re-encoded JPEG); uploaded to Supabase Storage. Untrusted external content (image binary) crosses this boundary. |
| Supabase Storage → ev-ui frontend | Uploaded images served via public Supabase Storage CDN URL embedded in `politician_images.url`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-13 | Tampering | Image content (downloaded JPEG/PNG) | mitigate | PIL re-encodes to JPEG q90 at 600x750 — strips EXIF, neutralizes any embedded format payloads. Crop-to-4:5 followed by Lanczos resize normalizes all inputs. |
| T-89-14 | Information Disclosure | Board member photos | accept | All photos sourced from public school district websites — no PII beyond what districts publish themselves. License recorded as `public_domain` per established Phase 86/87/88 conventions. |
| T-89-15 | Spoofing | Wrong photo attached to wrong politician | mitigate | Storage object key uses `{politician_id}-headshot.jpg` (UUID, not external_id, not name) — collision-resistant. politician_images.politician_id FK anchors row to the correct politician UUID. Task 3 human verify gate catches visible misattributions. |
| T-89-16 | Repudiation | Live DB writes without ledger entry | mitigate | Migration 266 audit file documents every INSERT with source URL + dimensions — committed to git as the audit trail (same pattern as 258, 262). |
| T-89-17 | Denial of Service | Supabase Storage upload rate | accept | 40 photos max — trivial scale. |
| T-89-18 | Elevation of Privilege | Photo with superimposed political messaging used as official portrait | mitigate | Per memory `feedback_headshot_no_graphics.md`, photos with "Re-Elect" banners or other superimposed graphics are rejected at sourcing time (Task 2 acceptance criterion enforces; Task 1 helper script logs the rejection). |
| T-89-19 | Information Disclosure | Wrong-type politician_images row hides photo from UI | mitigate | Per memory `project_phase17_headshots.md` and Phase 62 LAUSD lesson: all politician_images rows MUST use `type='default'`. Task 1 source assertion + Task 2 SQL gate (`type<>'default'`=0) + Task 4 audit file gate (`type<>'default'`=0) enforce this at three layers. |
| T-89-SC | Tampering | npm package installs | accept | No new npm dependencies. Python deps (PIL, requests, psycopg2, dotenv) are already established per Phase 86 _tmp-or-school-headshots.py. |
</threat_model>

<verification>
After all 4 tasks complete:

1. `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians pp ON pi.politician_id=pp.id WHERE (pp.external_id=-890001 OR pp.external_id BETWEEN -890057 AND -890011) AND pi.type='default'` returns the count of uploaded photos matching the Task 2 working log
2. `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians pp ON pi.politician_id=pp.id WHERE (pp.external_id=-890001 OR pp.external_id BETWEEN -890057 AND -890011) AND pi.type<>'default'` returns 0 (no UI-invisible rows)
3. Every uploaded image at the Supabase Storage public URL is exactly 600x750 JPEG (sample-check via ImageMagick `identify`)
4. Migration 266 SQL file exists, contains all 38 Phase 89 external_ids, has audit-only safety guard, never applied to Supabase ledger
5. Human-verify (Task 3): all 7 districts (IPS + MCCSC + Lewiston + Bangor + South Portland + Auburn + Biddeford) render correctly in UI; Hope Duke Star + Hasaan Rashid + Aja Jester displayed properly; no Gayle Cosby or Brandon Shurr visible
</verification>

<success_criteria>
Plan 03 is COMPLETE when ALL of the following are true:

1. `C:/EV-Accounts/backend/scripts/_tmp-in-me-school-headshots.py` exists with the full crop+resize+upload+INSERT pipeline
2. Task 2 working log `C:/EV-Accounts/backend/scripts/.tmp-phase89-headshot-log.txt` covers all 40 Phase 89 officials (UPLOADED or NO_PHOTO per official)
3. For every uploaded photo: Storage object at `politician_photos/{politician_id}-headshot.jpg` and politician_images row with `type='default'`
4. No politician_images row with `type<>'default'` exists for any Phase 89 politician
5. Human-verify (Task 3): IPS shows Hope Duke Star (D3) + Hasaan Rashid (D2); MCCSC shows Aja Jester (D7); all 5 ME districts show correct rosters and titles; no party labels (antipartisan); no UI-invisible photos
6. `C:/EV-Accounts/backend/migrations/266_me_in_school_headshots.sql` exists, audit-only, covers every Phase 89 external_id, never applied to ledger
7. Phase 89 success criteria IN-SCHOOL-01 (IPS), IN-SCHOOL-02 (MCCSC), ME-SCHOOL-01 (Lewiston), ME-SCHOOL-02 (Bangor), ME-SCHOOL-03 (South Portland/Auburn/Biddeford) all marked complete (closure of the headshot dimension of each requirement)
</success_criteria>

<output>
Create `.planning/phases/89-in-me-school-board-completion/89-03-SUMMARY.md` when done. Required sections:
- Files modified (2 absolute paths + Storage uploads list)
- Per-district upload counts: IPS X/3, MCCSC Y/<N>, Lewiston A/8, Bangor B/7, South Portland C/7, Auburn D/8, Biddeford E/7
- Total uploaded vs no-photo counts
- For each NO_PHOTO official: name + external_id + URL(s) checked + reason
- Notable findings (e.g. district CMS quirks, JS-only sites needing curl-with-UA, overlay-graphic rejections)
- Human-verify (Task 3) result: which districts passed, any UI issues addressed
- Migration 266 file path + confirmation it was NOT applied to ledger
- v10.0 milestone status (Phase 89 = final phase; check ROADMAP.md and STATE.md for milestone closure)
- Reconciliation against Research §Assumption Log (A3 Lewiston Ward 5, A4-A6 South Portland, A7 Auburn, A8 Bangor — final dispositions of each assumption)
</output>
