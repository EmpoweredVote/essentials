---
phase: 115-boston-stances
plan: 04
type: execute
wave: 4
depends_on:
  - 115-03
files_modified:
  - C:/EV-Accounts/backend/migrations/587_benjamin_weber_stances.sql
  - C:/EV-Accounts/backend/migrations/588_miniard_culpepper_stances.sql
  - C:/EV-Accounts/backend/migrations/589_sharon_durkan_stances.sql
  - C:/EV-Accounts/backend/migrations/590_liz_breadon_stances.sql
autonomous: true
requirements:
  - MA-STANCES-05
must_haves:
  truths:
    - "All 4 district councillors D6–D9 (external_ids -2507000011 through -2507000014) have rows attempted in inform.politician_answers (blank spokes acceptable per D-01)"
    - "100% citation rate for this batch: uncited = 0 for all 4 politicians"
    - "Zero unpaired rows for this batch: unpaired = 0 for all 4 politicians"
    - "No defaulted stances: only evidence-backed topics inserted"
    - "Migrations 587–590 applied one at a time (never in parallel)"
    - "Liz Breadon noted as Council President in migration header"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/587_benjamin_weber_stances.sql"
      provides: "Stance migration for Benjamin J. Weber (D6, external_id -2507000011)"
    - path: "C:/EV-Accounts/backend/migrations/588_miniard_culpepper_stances.sql"
      provides: "Stance migration for Miniard Culpepper (D7, external_id -2507000012)"
    - path: "C:/EV-Accounts/backend/migrations/589_sharon_durkan_stances.sql"
      provides: "Stance migration for Sharon Durkan (D8, external_id -2507000013)"
    - path: "C:/EV-Accounts/backend/migrations/590_liz_breadon_stances.sql"
      provides: "Stance migration for Liz Breadon, Council President (D9, external_id -2507000014)"
  key_links:
    - from: "migrations 587–590"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE upsert"
      pattern: "INSERT INTO inform.politician_answers / INSERT INTO inform.politician_context"
---

<objective>
Evidence-only compass stances for Boston City Council District Councillors D6–D9: Benjamin J. Weber (D6, Jamaica Plain/Mission Hill), Miniard Culpepper (D7, Roxbury), Sharon Durkan (D8, Back Bay/Beacon Hill/Fenway/Allston-Brighton), and Liz Breadon (D9, Allston-Brighton; Council President). External_ids -2507000011 through -2507000014.

Weber and Breadon are newer progressives with housing and transit focus. Culpepper is a newer member focused on public safety and economic development in Roxbury. Durkan is more moderate with development and transit focus. Each researched and applied sequentially.

Output: Migrations 587–590 applied; per-politician unpaired=0 and uncited=0 for each. After this plan, all 14 Boston council officials (Mayor + 13 councillors) have stances attempted. Plan 05 covers School Committee + phase-wide verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/115-boston-stances/115-RESEARCH.md
@.planning/phases/115-boston-stances/115-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benjamin J. Weber (D6, external_id -2507000011) — migration 587</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + exact SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors: Weber D6 — Jamaica Plain/Mission Hill, newer progressive, housing+transit focus)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 2.

    Step A — Research (web search, ONE session per D-08):
      Politician: Benjamin J. Weber, D6 District Councillor (external_id -2507000011).
      District covers Jamaica Plain and Mission Hill.
      Sources: https://boston.gov/departments/city-council/ben-weber, https://boston.legistar.com/, WBUR, Boston Globe, ballotpedia.org.
      Focus topics: housing, rent-regulation, residential-zoning, transportation-priorities, climate-change, local-environment, city-sanitation, homelessness.
      Weber is a newer progressive; expect evidence on housing and transit; thinner record on national topics.
      Per D-01: omit topics with no public evidence; no neutral defaults.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000011;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/587_benjamin_weber_stances.sql
      Header: "Migration 587: Benjamin J. Weber Stances" (D6 District Councillor, external_id -2507000011).
      Copy topic UUID block from 416_christopher_flanagan_stances.sql lines 14–58.
      politician_answers INSERT first, politician_context INSERT second; both ON CONFLICT DO UPDATE.
      Reasoning: $$...$$ dollar-quoting; sources: ARRAY[...]::text[]::text[] with full URLs.

    Step C — Apply:
      Pass full content of 587_benjamin_weber_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify:
      SELECT COUNT(*) AS n FROM inform.politician_answers
        WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -2507000011);
      SELECT COUNT(*) AS unpaired FROM inform.politician_answers pa
        LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
        WHERE pa.politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000011)
          AND pc.politician_id IS NULL;
      SELECT COUNT(*) AS uncited FROM inform.politician_context
        WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000011)
          AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/587_benjamin_weber_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000011.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 587_benjamin_weber_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000011
    - Stance count recorded (0 acceptable for newer member with thin record)
  </acceptance_criteria>
  <done>Migration 587 applied; unpaired=0, uncited=0 for Weber.</done>
</task>

<task type="auto">
  <name>Task 2: Miniard Culpepper (D7, external_id -2507000012) — migration 588</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors: Culpepper D7 — Roxbury, newer member, public safety + economic development)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 3.

    Step A — Research (web search, ONE session):
      Politician: Miniard Culpepper, D7 District Councillor (external_id -2507000012).
      District covers Roxbury.
      Sources: https://boston.gov/departments/city-council/miniard-culpepper, https://boston.legistar.com/, WBUR, Boston Globe.
      Focus topics: public-safety-approach, economic-development, housing, homelessness, city-sanitation.
      Culpepper is a newer member; expect thinner record. Blank spokes are correct per D-01 if no evidence found.
      Per D-01: no neutral defaults.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000012;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/588_miniard_culpepper_stances.sql
      Header: "Migration 588: Miniard Culpepper Stances" (D7 District Councillor, external_id -2507000012).

    Step C — Apply:
      Pass full content of 588_miniard_culpepper_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (three queries for external_id=-2507000012):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/588_miniard_culpepper_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000012.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 588_miniard_culpepper_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000012
    - Stance count recorded (0 acceptable and expected for thin-record newer member)
  </acceptance_criteria>
  <done>Migration 588 applied; unpaired=0, uncited=0 for Culpepper.</done>
</task>

<task type="auto">
  <name>Task 3: Sharon Durkan (D8, external_id -2507000013) — migration 589</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors: Durkan D8 — Back Bay/Beacon Hill/Fenway/Allston-Brighton, moderate, development+transit)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 4.

    Step A — Research (web search, ONE session):
      Politician: Sharon Durkan, D8 District Councillor (external_id -2507000013).
      District covers Back Bay, Beacon Hill, Fenway, Allston-Brighton.
      Sources: https://boston.gov/departments/city-council/sharon-durkan, https://boston.legistar.com/, WBUR, Boston Globe, ballotpedia.org.
      Focus topics: growth-and-development, residential-zoning, transportation-priorities, housing, city-sanitation, economic-development.
      Durkan is more moderate than progressive colleagues; expect development-friendly stances and mixed housing positions.
      Per D-01: no defaults; evidence only.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000013;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/589_sharon_durkan_stances.sql
      Header: "Migration 589: Sharon Durkan Stances" (D8 District Councillor, external_id -2507000013).

    Step C — Apply:
      Pass full content of 589_sharon_durkan_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (three queries for external_id=-2507000013):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/589_sharon_durkan_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000013.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 589_sharon_durkan_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000013
    - Stance count recorded
  </acceptance_criteria>
  <done>Migration 589 applied; unpaired=0, uncited=0 for Durkan.</done>
</task>

<task type="auto">
  <name>Task 4: Liz Breadon, Council President (D9, external_id -2507000014) — migration 590</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (District Councillors: Breadon D9 — Allston-Brighton, progressive, Council President, transportation+housing focus)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D. This is the last task in Plan 04.

    Step A — Research (web search, ONE session):
      Politician: Liz Breadon, D9 District Councillor and Council President (external_id -2507000014).
      District covers Allston-Brighton.
      Sources: https://boston.gov/departments/city-council/liz-breadon, https://boston.legistar.com/, WBUR, Boston Globe, ballotpedia.org.
      Focus topics: transportation-priorities, housing, rent-regulation, residential-zoning, city-sanitation, climate-change, local-environment.
      Breadon is progressive and has a strong transit record. As Council President she has a longer leadership record to draw from.
      Per D-01: evidence only; no defaults.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000014;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/590_liz_breadon_stances.sql
      Header: "Migration 590: Liz Breadon Stances" (D9 District Councillor, Council President, external_id -2507000014).

    Step C — Apply:
      Pass full content of 590_liz_breadon_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (three queries for external_id=-2507000014):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/590_liz_breadon_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000014.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 590_liz_breadon_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000014
    - Stance count recorded
  </acceptance_criteria>
  <done>Migration 590 applied; unpaired=0, uncited=0 for Breadon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web research → SQL migration | Scraped reasoning text enters SQL dollar-quoted strings |
| mcp__supabase-local | All SQL executes against remote production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-10 | Tampering | reasoning text (4 politicians) | mitigate | Dollar-quoting ($$...$$) — never single-quote concatenation |
| T-115-11 | Spoofing | sources array URLs | mitigate | Real URLs only from pages actually consulted |
| T-115-12 | Tampering | UUID cross-contamination | mitigate | Resolve each UUID fresh via WHERE external_id=N |
</threat_model>

<verification>
After all 4 tasks complete, run batch check via mcp__supabase-local__execute_sql:

  SELECT p.external_id, p.full_name, COUNT(pa.*) AS stance_count
  FROM essentials.politicians p
  LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
  WHERE p.external_id BETWEEN -2507000014 AND -2507000011
  GROUP BY p.external_id, p.full_name
  ORDER BY p.external_id;
Expected: 4 rows; stance_count ≥0.

  SELECT COUNT(*) AS uncited_batch FROM inform.politician_context pc
  JOIN essentials.politicians p ON p.id=pc.politician_id
  WHERE p.external_id BETWEEN -2507000014 AND -2507000011
    AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL OR array_length(pc.sources,1)=0);
Expected: 0.

  SELECT COUNT(*) AS unpaired_batch FROM inform.politician_answers pa
  JOIN essentials.politicians p ON p.id=pa.politician_id
  LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
  WHERE p.external_id BETWEEN -2507000014 AND -2507000011 AND pc.politician_id IS NULL;
Expected: 0.
</verification>

<success_criteria>
- Migrations 587–590 created and applied (sequential per D-08)
- All 4 D6–D9 district councillors have rows attempted (blank documented as intentional per D-01)
- Batch uncited=0, unpaired=0 confirmed
- All 14 Boston council officials (Mayor Wu + 13 councillors) now have stances attempted across Plans 01–04
- D-01 through D-10 honored throughout
</success_criteria>

<output>
Create `.planning/phases/115-boston-stances/115-04-SUMMARY.md` documenting:
- Stance count per councillor (table: external_id | name | district | migration | stance_count | status)
- Batch quality gate results (uncited=0, unpaired=0)
- Combined council total stance count (all 14 council officials, Plans 01–04)
- Next migration: 591
</output>
