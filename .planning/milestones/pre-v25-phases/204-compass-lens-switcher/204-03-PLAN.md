---
phase: 204-compass-lens-switcher
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LensChipRow.jsx
autonomous: true
requirements: [LENS-01]

must_haves:
  truths:
    - "The switcher renders one pill per lens in the order given, Best Match first"
    - "The active chip is filled with that lens's own color and white text"
    - "A needs-calibration chip renders grey/muted with a ~1.5px purple ring and dimmed label, and is not aria-pressed"
    - "Hovering (desktop) or first-tapping (mobile) a purple chip reveals a 'Calibrate this lens?' prompt; a LIT chip does not"
    - "Clicking a LIT chip calls onSelectLens(key); clicking a purple chip (or its prompt / second tap) calls onCalibrate(key)"
  artifacts:
    - path: "src/components/LensChipRow.jsx"
      provides: "Data-driven N-lens chip row with LIT/active/needs-calibration states, per-lens icons, hover+tap-to-prompt affordance"
      exports: ["default"]
      min_lines: 60
  key_links:
    - from: "LensChipRow chip onClick"
      to: "parent handlers"
      via: "onSelectLens for LIT, onCalibrate for needs-calibration"
      pattern: "onCalibrate"
    - from: "chip label/title"
      to: "lens.name / lens.description"
      via: "text + title attribute (never innerHTML)"
      pattern: "title="
---

<objective>
Build `src/components/LensChipRow.jsx` — the data-driven, presentational lens-switcher row. It maps
over a caller-supplied lens list (Best Match synthesized first by the parent), rendering each as a
`stance-btn` pill with three visual states (active = lens-color fill; LIT-inactive = outlined;
needs-calibration = grey + purple rim + dimmed), per-lens icons mirrored from EV-CompassV2, and a
hover (desktop) / tap-to-prompt (mobile) calibration affordance.

Purpose: Isolating the chip sub-tree keeps CompassControlsBar a thin bar and gives the switcher a
clean, testable prop surface (D-01..D-11). All navigation/persistence/analytics stay in the parent
(Plan 04), so this component is pure presentation + local hover/tap state.
Output: New `src/components/LensChipRow.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/204-compass-lens-switcher/204-SPEC.md
@.planning/phases/204-compass-lens-switcher/204-CONTEXT.md
@.planning/phases/204-compass-lens-switcher/204-PATTERNS.md

<interfaces>
Props contract this component MUST accept (flat props, ev-context convention):
- lenses: Array<{ key, name, description, color, icon?, calibrated: boolean, topicCount: number }>
    // parent synthesizes and prepends the Best Match entry: { key:'custom', name:'Best Match', color:'#FF5740', calibrated:true }
- activeLensKey: string   // currently selected key
- onSelectLens: (key) => void    // called for a LIT chip
- onCalibrate: (key) => void     // called for a needs-calibration chip (after prompt confirm)
- isDesktop: boolean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LensChipRow component with three chip states, icons, and hover/tap prompt</name>
  <read_first>
    - src/components/CompassControlsBar.jsx — L44-63 (the `stance-btn` pill + coral-active inline-style-spread idiom to copy) and the viewfinder SVG at L58-61 (reuse for Best Match)
    - C:/EV-CompassV2/src/pages/CombinedPage.jsx — renderLensIcon (~L1325-1345: Capitol dome=federal, gavel=judicial, house=local) and the lens-row .map (~L1576-1593: active = { background: lens.color, color:'#fff', borderColor: lens.color } vs outlined)
    - src/components/CompassCard.jsx — L8 + L303-306 (useTheme isDark inline-ternary idiom for any dark-mode color tweak)
    - .planning/phases/204-compass-lens-switcher/204-CONTEXT.md — D-01 (pills), D-02 (own-color fill), D-03 (grey+purple-rim+dimmed), D-04 (per-lens icon+label), D-05/D-06/D-07 (Best Match framing), D-11 (mobile two-tap)
  </read_first>
  <action>
    Create a default-export React component `LensChipRow` taking the props in <interfaces>. Render an
    inline-flex row that maps `lenses` in the given order (parent puts Best Match first — do not
    re-sort). Each lens is a `<button className="stance-btn">` reusing the existing pill class; vary
    ONLY inline `style` per state via the `...(cond ? {...} : {})` spread idiom:
    - Active (`activeLensKey === lens.key`): `background: lens.color, borderColor: lens.color, color:'#fff'`, `aria-pressed` true.
    - LIT-inactive (`lens.calibrated === true`, not active): transparent background, `color`/`borderColor` = lens.color.
    - Needs-calibration (`lens.calibrated === false`, always excludes 'custom'): grey/muted fill, `border: '1.5px solid #7C3AED'` (purple), dimmed label (reduced opacity/muted color); omit `aria-pressed`.
    Add a local `renderLensIcon(lens)` returning inline SVGs (width/height 15, stroke/fill currentColor
    per the two CombinedPage conventions): Capitol dome for `federal`, gavel for `judicial`, house for
    `local`, the viewfinder from CompassControlsBar for `custom`, and a neutral fallback dot for any
    future key. Label text = `lens.name`. Set `title = lens.description || lens.name` (text only; never
    dangerouslySetInnerHTML — T-204-03).
    Hover/tap affordance (D-06/D-11): keep local `useState` for `hoveredKey` (desktop) and `tappedKey`
    (mobile). On a needs-calibration chip: desktop hover (onMouseEnter/Leave) OR mobile first tap
    reveals an inline "Calibrate this lens?" prompt next to/under that chip; clicking a LIT chip calls
    `onSelectLens(lens.key)`; clicking a needs-calibration chip when its prompt is showing (desktop) or
    on the second tap / prompt click (mobile) calls `onCalibrate(lens.key)`. A LIT chip never shows the
    prompt. Use `isDark` from useTheme only if a color needs a dark-mode contrast tweak (D-02 discretion).
    Do NOT own the wrap-vs-scroll container — CompassControlsBar (Plan 04) provides it.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/LensChipRow.jsx default-exports a component and renders one `stance-btn` button per `lenses` entry in the given order
    - The chip whose key === activeLensKey has `background`/`borderColor` = its `lens.color` and white text and `aria-pressed`
    - A chip with `calibrated: false` renders a 1.5px purple ring + muted fill + dimmed label and has no `aria-pressed`
    - Hovering (or first-tapping) a `calibrated:false` chip surfaces a "Calibrate this lens?" prompt; a `calibrated:true` chip does not surface it
    - Clicking a LIT chip invokes `onSelectLens` with its key; confirming a purple chip invokes `onCalibrate` with its key
    - `lens.description` reaches the DOM only via the `title` attribute (no HTML injection)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The switcher renders all three chip states + icons and routes clicks to onSelectLens/onCalibrate per the affordance rules; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lens props -> rendered chip | `name`/`description`/`color` originate from the API and are rendered as label/title/inline-style |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-204-02 | Injection | chip inline background style | mitigate | `color` is already sanitized to a hex by normalizeApiLens (Plan 01) before it reaches this component; consume as-is, add no unsanitized style values |
| T-204-03 | Injection | chip label + prompt | mitigate | Render `name`/`description` as text nodes / `title` attribute only; never dangerouslySetInnerHTML |
| T-204-SC | Tampering | dependencies | accept | No new packages introduced |
</threat_model>

<verification>
- `npm run build` succeeds
- All five `must_haves.truths` observable by rendering the component with mock props
</verification>

<success_criteria>
A self-contained, data-driven chip row honoring D-01 through D-11, with navigation/analytics left to
the parent.
</success_criteria>

<output>
Create `.planning/phases/204-compass-lens-switcher/204-03-SUMMARY.md` when done.
</output>
