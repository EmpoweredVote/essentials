---
phase: 126-alhambra-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/NNN_lee_stances.sql
  - C:/EV-Accounts/backend/migrations/NNN+1_maza_stances.sql
  - C:/EV-Accounts/backend/migrations/NNN+2_maloney_stances.sql
autonomous: true
requirements: [ALHAMBRA-01]
must_haves:
  truths:
    - "Wave 0 pre-flight [BLOCKING] confirms actual next migration number (expected 703 but MUST verify), resolves all 5 Alhambra UUIDs, confirms active topic count is 44, confirms no pre-existing Alhambra stances, and confirms no Mayor chamber exists for Alhambra geo_id=0600884"
    - "Katherine Lee (D1, external_id -700450) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Ross J. Maza (D2, external_id -700451) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Jeff Maloney (D3, external_id -700452) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/NNN_lee_stances.sql"
      provides: "Katherine Lee stance migration (NNN determined by Wave 0)"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/NNN+1_maza_stances.sql"
      provides: "Ross J. Maza stance migration"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/NNN+2_maloney_stances.sql"
      provides: "Jeff Maloney stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Run Wave 0 pre-flight verification [BLOCKING], then research and apply evidence-only compass stances for the first 3 Alhambra officials (Katherine Lee D1, Ross J. Maza D2, Jeff Maloney D3), one politician at a time.

Purpose: Establish the verified migration starting point, active topic count, and all 5 Alhambra UUIDs for the entire phase; begin closing ALHAMBRA-01.
Output: 3 per-individual stance migration files (NNN through NNN+2), each applied to production and verified. All 5 UUIDs recorded in SUMMARY for Plans 02-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-alhambra-stances/126-RESEARCH.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 574_boston_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs (Phase 115 ledger-consistency precedent)
- NEVER write "Mayor [Surname]" in reasoning text without the "rotational" qualifier — DB title is always "Council Member (District N)"
</interfaces>

Alhambra roster (external_id → expected migration), from migration 307 [VERIFIED]:
  -700450 Katherine Lee (Council Member, District 1)   → NNN
  -700451 Ross J. Maza (Council Member, District 2)    → NNN+1
  -700452 Jeff Maloney (Council Member, District 3)    → NNN+2
  -700453 Noya Wang (Council Member, District 4)       → NNN+3 [Plan 02]
  -700454 Adele Andrade-Stadler (Council Member, D5)   → NNN+4 [Plan 02]

NNN = actual next migration confirmed at Wave 0. Expected 703 (STATE.md=699 is stale; 699-702 exist on disk), but Wave 0 MUST verify.

Out-of-scope guard: Do NOT write stances for any external_id outside the -700454 to -700450 range. Do NOT create any new offices, districts, or chambers for Alhambra. Alhambra has NO separately elected Mayor.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 pre-flight verification [BLOCKING]</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/126-alhambra-stances/126-RESEARCH.md (Migration Numbering section + Wave 0 Pre-Flight Queries section + Pitfall 6 + Pitfall 7)
    - .planning/STATE.md (Current Position section — confirms STATE.md says "next migration: 699" but this is stale)
  </read_first>
  <action>
    Run all 6 Wave 0 pre-flight queries via mcp__supabase-local__execute_sql before writing any migration. Do not skip any query — this task is BLOCKING for all subsequent tasks.

    Query 1 — Confirm highest applied migration (determines actual starting number):
    `SELECT MAX(version::int) AS max_applied FROM supabase_migrations.schema_migrations;`
    If MAX = 702, next migration = 703. If MAX is higher, use MAX+1. Record the confirmed starting number.

    Query 2 — Confirm 699-702 are specifically applied:
    `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('699', '700', '701', '702') ORDER BY version;`
    Expected: 4 rows. If fewer rows, note which migrations are missing — this changes the starting number.

    Query 3 — Confirm active topic count (must be 44):
    `SELECT COUNT(*) FROM inform.compass_topics WHERE is_active = true;`
    If count differs from 44, run `SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;` and capture the live list — do NOT use the cached UUID reference block from 126-RESEARCH.md if the count differs.

    Query 4 — Resolve all 5 Alhambra UUIDs:
    `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700450 ORDER BY external_id DESC;`
    Expected 5 rows: -700450 Katherine Lee, -700451 Ross J. Maza, -700452 Jeff Maloney, -700453 Noya Wang, -700454 Adele Andrade-Stadler. Capture all 5 UUIDs for use in this plan and Plans 02-03.

    Query 5 — Check for pre-existing Alhambra stance rows (informational; upsert handles them):
    `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700450);`

    Query 6 — Confirm NO Mayor chamber or LOCAL_EXEC district for Alhambra (Alhambra has rotational Mayor only — no separate Mayor office):
    `SELECT c.name FROM essentials.chambers c JOIN essentials.governments g ON g.id = c.government_id WHERE g.geo_id = '0600884';`
    Expected: exactly 1 row — "City Council" only. If a Mayor chamber exists, DO NOT create any Mayor office rows — document the finding and proceed with council-only stances.

    Record all query results in the plan SUMMARY. The confirmed migration starting number (NNN) is the authoritative value for all 5 migration filenames in this phase.
  </action>
  <verify>
    <automated>All 6 queries return results via mcp__supabase-local__execute_sql. Starting migration number recorded (expected 703). Topic count confirmed. All 5 Alhambra UUIDs resolved. City Council check returns exactly 1 chamber row.</automated>
  </verify>
  <acceptance_criteria>
    - MAX(version::int) query returns a value >= 702; starting migration number confirmed and recorded
    - Queries 1+2 results are consistent (if 699-702 all applied, MAX=702 and 4 rows returned)
    - Active topic count is confirmed (44 expected; if different, live slug→id list captured)
    - All 5 UUIDs resolved: -700450 Katherine Lee, -700451 Ross J. Maza, -700452 Jeff Maloney, -700453 Noya Wang, -700454 Adele Andrade-Stadler — names and external_ids match roster
    - Pre-existing stance count recorded (informational)
    - Alhambra chamber query returns exactly "City Council" (no Mayor chamber)
    - All 5 UUIDs recorded in SUMMARY for Plans 02-03
  </acceptance_criteria>
  <done>Pre-flight queries run; actual starting migration number confirmed, topic count confirmed, all 5 Alhambra UUIDs resolved and recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Katherine Lee (D1) stances — migration NNN</name>
  <files>C:/EV-Accounts/backend/migrations/NNN_lee_stances.sql</files>
  <read_first>
    - .planning/phases/126-alhambra-stances/126-RESEARCH.md (Katherine Lee section + Local Press Sources + Anti-Patterns + Pitfalls 1-5 + Pitfall 8)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (canonical format: BEGIN/COMMIT, float literals, dollar-quoting, double-cast sources)
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical per-local-official format)
  </read_first>
  <action>
    Process Katherine Lee (external_id -700450, UUID resolved in Task 1, Council Member District 1) strictly after Task 1 completes. Research her public record across all 44 active compass topics, then write, apply, and verify her migration before any other official is processed.

    (a) Research: Search "Katherine Lee Alhambra council" across alhambraca.gov/agendas-minutes, sgvtribune.com, alhambraource.com, patch.com/california/alhambra, latimes.com. Lee is the longest-serving council member (since ~2010) — expected yield 4-8 stances. High-probability topics: housing (RHNA compliance votes), residential-zoning (growth/preservation), local-immigration (Alhambra ~50% Asian-American), local-environment, public-safety-approach, growth-and-development. Place each topic with direct evidence on the 1.0-5.0 scale per that topic's axis definition. No INSERT for any topic with no direct evidence.

    (b) Write migration NNN_lee_stances.sql (NNN = confirmed number from Task 1) using the canonical template:
    - Header comment block: "Migration NNN: Katherine Lee Stances"
    - Full 44-topic UUID reference block from 126-RESEARCH.md (or live list if count differed)
    - BEGIN;
    - One politician_answers INSERT + one politician_context INSERT per evidenced topic
    - value = float literal (e.g. 2.0) — NEVER bare integer or string
    - sources = ARRAY['https://host/path/specific-article']::text[]::text[] — double-cast, path-bearing URLs
    - reasoning = $$....$$ dollar-quoting, 2-5 sentences citing specific vote/statement/action
    - Reasoning text uses "Council Member Lee" — never "Mayor Lee" (Lee is not the current rotational Mayor; rotational title is ceremonial and not in her DB seeding)
    - COMMIT;

    (c) Apply: mcp__supabase-local__execute_sql with full file content.

    (d) Verify immediately (before Task 3):
    Row count: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{lee_uuid}';`
    Unpaired check: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{lee_uuid}' AND pc.politician_id IS NULL;` — must return 0
    Citation check: `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{lee_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Lee's UUID. Citation check returns 0 for Lee's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file NNN_lee_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0` (grep: no `, [1-5]\)` without `.0`)
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Lee's UUID
    - Uncited-context query returns 0 for Lee's UUID
    - Reasoning text contains "Council Member Lee" (not "Mayor Lee" without "rotational" qualifier)
    - No INSERT references external_ids outside the -700454 to -700450 range
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration NNN_lee_stances.sql written, applied to production, and verified (0 unpaired, 0 uncited) for Katherine Lee.</done>
</task>

<task type="auto">
  <name>Task 3: Ross J. Maza (D2) and Jeff Maloney (D3) stances — migrations NNN+1 and NNN+2</name>
  <files>C:/EV-Accounts/backend/migrations/NNN+1_maza_stances.sql, C:/EV-Accounts/backend/migrations/NNN+2_maloney_stances.sql</files>
  <read_first>
    - .planning/phases/126-alhambra-stances/126-RESEARCH.md (Ross J. Maza section + Jeff Maloney section + Anti-Patterns + Assumption A6 re: "Ross J. Maza" middle initial)
    - C:/EV-Accounts/backend/migrations/NNN_lee_stances.sql (just-written sibling file as in-phase format reference)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process ONE politician at a time, strictly sequentially. Maza first, then Maloney — never in parallel (rate-limit rule). Apply and verify each migration before researching the next.

    Ross J. Maza (external_id -700451, UUID from Task 1, Council Member District 2):
    (a) Research: Search "Ross Maza Alhambra council" and "Ross J. Maza Alhambra" on sgvtribune.com, alhambraca.gov/agendas-minutes, alhambraource.com, patch.com/california/alhambra. Maza has served since ~2020; expected yield 2-6 stances. High-probability topics: zoning, homelessness, development. NOTE: Assumption A6 — verify the DB full_name from Task 1 UUID query; if DB shows "Ross Maza" (no middle initial), use the DB name in the file header comment only. The politician_id (UUID) is what matters in INSERT statements.
    (b) Write migration NNN+1_maza_stances.sql. Same canonical format as Task 2. Use "Council Member Maza" in reasoning text.
    (c) Apply via mcp__supabase-local__execute_sql.
    (d) Verify: run row count + unpaired check + citation check for Maza's UUID before proceeding to Maloney.

    Jeff Maloney (external_id -700452, UUID from Task 1, Council Member District 3):
    (a) Research: Search "Jeff Maloney Alhambra council" on sgvtribune.com, alhambraca.gov/agendas-minutes, alhambraource.com, patch.com/california/alhambra. Expected yield 2-6 stances. Check for SGV city coalition positions and San Gabriel Valley Tribune coverage of local development and public safety.
    (b) Write migration NNN+2_maloney_stances.sql. Same canonical format. Use "Council Member Maloney" in reasoning text.
    (c) Apply via mcp__supabase-local__execute_sql.
    (d) Verify: run row count + unpaired check + citation check for Maloney's UUID.

    For either official: if zero evidence found, still write the migration file with header + BEGIN;/COMMIT; + zero INSERT blocks (ledger-consistency precedent), note in SUMMARY.
  </action>
  <verify>
    <automated>For each of Maza and Maloney UUIDs: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{uuid}' AND pc.politician_id IS NULL;` returns 0, AND `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - 2 migration files exist: NNN+1_maza_stances.sql and NNN+2_maloney_stances.sql
    - Both files wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Maza's UUID
    - Uncited-context query returns 0 for Maza's UUID
    - Unpaired-answer query returns 0 for Maloney's UUID
    - Uncited-context query returns 0 for Maloney's UUID
    - Reasoning text uses "Council Member Maza" and "Council Member Maloney"
    - No INSERT references external_ids outside -700454 to -700450 range
    - Row counts for both officials recorded in SUMMARY
  </acceptance_criteria>
  <done>Migrations NNN+1 and NNN+2 written, applied to production, and verified (0 unpaired, 0 uncited) for Maza and Maloney.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (mcp__supabase-local) | Writes go directly to live production Supabase (mcp__supabase-local IS the remote DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Tampering | stance value integrity | mitigate | Evidence-only rule: no INSERT without a direct cited source; per-person uncited-context query must return 0 |
| T-126-02 | Information Disclosure | reasoning text / sources | accept | All data is public-record political positions with public source URLs; no PII |
| T-126-03 | Repudiation | stance provenance | mitigate | Every value paired with a context row carrying path-bearing source URLs (100% citation rate) |
| T-126-04 | Tampering | rotational Mayor office creation | mitigate | Wave 0 Query 6 confirms no Mayor chamber; acceptance criteria assert no INSERT into essentials.offices or essentials.districts |
| T-126-05 | Tampering | stale migration number collision | mitigate | Wave 0 Query 1+2 confirms actual MAX applied migration before any file is written |
| T-126-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After all three tasks, run the partial phase-wide citation check for this plan's officials:
`SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -700452 AND -700450) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` must return 0.
</verification>

<success_criteria>
- Wave 0 pre-flight complete; actual starting migration number confirmed (BLOCKING gate passed)
- Active topic count confirmed as 44 (or live list captured)
- All 5 Alhambra UUIDs resolved and recorded in SUMMARY for Plans 02-03
- No Mayor chamber found for Alhambra (or deviation documented)
- Migrations NNN, NNN+1, NNN+2 (3 officials: Lee, Maza, Maloney) written, applied, and verified
- 0 unpaired answers, 0 uncited contexts across all 3 officials
- No external_ids outside -700454 to -700450 range touched
- Blank-spoke officials documented in SUMMARY (never defaulted to 3.0)
</success_criteria>

<output>
Create `.planning/phases/126-alhambra-stances/126-01-SUMMARY.md` when done. Record: confirmed starting migration number (NNN), active topic count, all 5 Alhambra UUIDs (for Plans 02-03), per-official stance counts for Lee/Maza/Maloney, and any blank-spoke officials.
</output>
