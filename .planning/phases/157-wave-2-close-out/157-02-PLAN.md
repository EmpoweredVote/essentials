---
phase: 157-wave-2-close-out
plan: 02
type: execute
wave: 2
depends_on: ["157-01"]
files_modified:
  - src/lib/coverage.js
  - LOCATION-ONBOARDING.md
autonomous: true
requirements: [LAC2-RETRO-01]

must_haves:
  truths:
    - "All 15 Wave-2 cities are present and browsable in the CA block of COVERAGE_STATES with the correct geo_id and browseStateAbbrev:'CA'"
    - "hasContext:true (purple chip) is set ONLY for cities the Wave-1 audit verified with >=1 seeded compass stance; 0-stance cities are listed but plain"
    - "LOCATION-ONBOARDING.md has one Cities Onboarded row per Wave-2 city"
    - "LOCATION-ONBOARDING.md captures the net-new LA-County-Wave-2 GOTCHAs surfaced across 142–156"
  artifacts:
    - path: "src/lib/coverage.js"
      provides: "COVERAGE_STATES CA-block entries for all 15 Wave-2 cities with audit-correct hasContext flags"
      contains: "0604982"
    - path: "LOCATION-ONBOARDING.md"
      provides: "15 Cities Onboarded rows + Wave-2 GOTCHAs"
      contains: "Wave 2"
  key_links:
    - from: "src/pages/Landing.jsx"
      to: "src/lib/coverage.js"
      via: "import { COVERAGE_STATES } from '../lib/coverage'"
      pattern: "COVERAGE_STATES"
---

<objective>
Surface all 15 LA County Wave-2 cities on the landing page and capture the wave's learnings.

Purpose: Make every seeded city browsable from the landing grid with an honest purple chip (chip
only where the Wave-1 audit verified >=1 stance, D-02), and record the wave's reusable patterns in
the onboarding playbook (D-07).

Output: edited `src/lib/coverage.js` (COVERAGE_STATES CA block) + edited `LOCATION-ONBOARDING.md`
(15 Cities Onboarded rows + net-new GOTCHAs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-wave-2-close-out/157-CONTEXT.md
@.planning/phases/157-wave-2-close-out/157-01-SUMMARY.md
@.planning/v17.0-MILESTONE-AUDIT.md
</context>

<interfaces>
The landing page surfacing target is `src/lib/coverage.js`, NOT Landing.jsx directly. Landing.jsx
imports `COVERAGE_STATES` from `../lib/coverage` (Landing.jsx line 8); the locality typeahead and
browse routing also derive from it (coverageAreaToPath, searchCoverageAreas). Edit the CA block of
`COVERAGE_STATES` in coverage.js.

CA-block entry shape (existing pattern in coverage.js):
  { label: 'City Name', browseGovernmentList: ['<geo_id>'], browseStateAbbrev: 'CA', hasContext: true }
- `hasContext: true` renders the purple chip = "city has compass stances seeded" (coverage.js
  header comment, line ~4). Omit the `hasContext` key entirely for a 0-stance city (see the ME/OR
  plain entries for the no-chip shape).
- CA block is maintained ALPHABETICAL by label.

IMPORTANT pre-existing state (verify, do not assume): all 15 Wave-2 cities already appear in the CA
block, currently ALL carrying `hasContext: true` (Bellflower 0604982, Burbank 0608954,
Downey 0619766, Glendale 0630000, Inglewood 0636546, Lancaster 0640130, Long Beach 0643000,
Norwalk 0652526, Palmdale 0655156, Pasadena 0656000, Pomona 0658072, Santa Clarita 0669088,
Torrance 0680000, West Covina 0684200, El Monte 0622230). This task is therefore VERIFY-AND-RECONCILE:
confirm each entry exists with the correct geo_id, add any genuinely missing entry alphabetically,
and bring each city's `hasContext` into agreement with the Wave-1 audit's actual stance count.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the 15 Wave-2 cities in coverage.js COVERAGE_STATES (CA block)</name>
  <read_first>
    - src/lib/coverage.js (the file being modified — the CA block + the header comment defining hasContext semantics)
    - .planning/v17.0-MILESTONE-AUDIT.md (the recorded set of >=1-stance cities — the authoritative hasContext input, D-02)
    - .planning/phases/157-wave-2-close-out/157-CONTEXT.md (D-01 geo_ids verbatim; D-02 chip rule)
  </read_first>
  <action>
    In `src/lib/coverage.js`, reconcile the CA block of `COVERAGE_STATES` so all 15 Wave-2 cities
    are present, browsable, and alphabetically placed, each as
    `{ label, browseGovernmentList: ['<geo_id>'], browseStateAbbrev: 'CA' }` with the D-01 geo_ids
    verbatim: Bellflower 0604982 · Burbank 0608954 · Downey 0619766 · El Monte 0622230 ·
    Glendale 0630000 · Inglewood 0636546 · Lancaster 0640130 · Long Beach 0643000 · Norwalk 0652526 ·
    Palmdale 0655156 · Pasadena 0656000 · Pomona 0658072 · Santa Clarita 0669088 · Torrance 0680000 ·
    West Covina 0684200.
    For EACH city set `hasContext: true` ONLY if the Wave-1 audit (`v17.0-MILESTONE-AUDIT.md`)
    verified >=1 seeded compass stance for that city; for any city the audit recorded as
    roster-only / 0-stance, REMOVE the `hasContext` key so the entry renders plain (D-02). Add any
    genuinely-missing entry in alphabetical position; correct any wrong geo_id. Do not touch
    non-Wave-2 CA entries, the IN/ME/MD/MA/OR/TX/UT/VA blocks, COVERAGE_COUNTIES, or the helper
    functions. No fenced code: edit the array entries in place.
  </action>
  <verify>
    <automated>node -e "const {COVERAGE_STATES}=require('./src/lib/coverage.js'); const ca=COVERAGE_STATES.find(s=>s.abbrev==='CA').areas; const need=['0604982','0608954','0619766','0622230','0630000','0636546','0640130','0643000','0652526','0655156','0656000','0658072','0669088','0680000','0684200']; const have=new Set(ca.flatMap(a=>a.browseGovernmentList||[])); const missing=need.filter(g=>!have.has(g)); if(missing.length){console.error('MISSING',missing);process.exit(1)} console.log('all 15 present')"</automated>
  </verify>
  <acceptance_criteria>
    - All 15 Wave-2 geo_ids are present in the CA block of COVERAGE_STATES with browseStateAbbrev:'CA'.
    - Each city's `hasContext` flag matches the Wave-1 audit: present+true only for >=1-stance cities, absent for 0-stance cities.
    - CA block remains alphabetical by label.
    - No non-Wave-2 entries, other state blocks, COVERAGE_COUNTIES, or helper functions were modified.
    - coverage.js still parses (the node verify command exits 0).
  </acceptance_criteria>
  <done>
    All 15 Wave-2 cities browsable in coverage.js with audit-honest purple-chip flags; landing grid
    and locality typeahead pick them up via the existing COVERAGE_STATES import.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 15 Cities Onboarded rows + net-new Wave-2 GOTCHAs to LOCATION-ONBOARDING.md</name>
  <read_first>
    - LOCATION-ONBOARDING.md (the file being modified — the "Cities Onboarded" 5-column table ~line 29 and the v15.0 LA-area rows that show the row shape to mirror; the "LA-Area City Stances (v15.0) Quick Reference" GOTCHA block format)
    - .planning/v17.0-MILESTONE-AUDIT.md (per-city verified counts + verdicts to ground each row's Notable patterns)
    - .planning/phases/157-wave-2-close-out/157-CONTEXT.md (D-07 — the 6 required GOTCHA patterns: a–f)
    - .planning/REQUIREMENTS.md (per-phase reconcile notes feeding the Notable patterns cells)
  </read_first>
  <action>
    In `LOCATION-ONBOARDING.md` add one "Cities Onboarded" table row per Wave-2 city (15 rows),
    using the existing 5-column format: City | State | Onboarded | Election method | Notable patterns.
    Fill each Notable-patterns cell with the city-specific detail synthesized from the Wave-1 audit
    and the per-phase notes (geo_id, district-vs-at-large, mayor type, ext_id ranges where known,
    headshot source pattern incl. WAF/NO-WAF, stance count, any documented gap). Then add a net-new
    "LA County Wave-2 (v17.0)" GOTCHA / Quick Reference block mirroring the v15.0 LA-area reference
    block format, capturing the recurring wave patterns required by D-07: (a) reconcile-vs-greenfield
    default — most cities were already partially seeded, DB pre-check every city by NAME (geo_id was
    NULL); (b) June-2026 election turnover requiring reseating; (c) duplicate-chamber merge/reseating
    (count the survivor chamber); (d) `districts.government_id` NULL → link via geo_id; (e) wrong-person
    headshot pitfalls (Glendale, West Covina Gutierrez, Pomona Gonzalez/Torres); (f) WAF-403 vs NO-WAF
    source patterns per city. Do not duplicate GOTCHAs already present from earlier waves — add only
    net-new wave-2 patterns. Use the Edit/Write tools (no heredoc).
  </action>
  <verify>
    <automated>grep -c "| CA |" LOCATION-ONBOARDING.md; grep -qi "Wave.2" LOCATION-ONBOARDING.md && echo "wave-2 block present"; for c in Bellflower Burbank Downey "El Monte" Glendale Inglewood Lancaster "Long Beach" Norwalk Palmdale Pasadena Pomona "Santa Clarita" Torrance "West Covina"; do grep -q "$c" LOCATION-ONBOARDING.md || { echo "MISSING ROW: $c"; exit 1; }; done && echo "all 15 rows present"</automated>
  </verify>
  <acceptance_criteria>
    - 15 new Cities Onboarded rows exist, one per Wave-2 city, each in the 5-column format with a populated Notable-patterns cell.
    - A net-new LA County Wave-2 (v17.0) GOTCHA/reference block exists covering D-07 patterns (a)–(f).
    - All 15 city names appear in the file (verify command lists none missing).
    - No earlier-wave GOTCHA content was deleted or duplicated.
  </acceptance_criteria>
  <done>
    Onboarding playbook updated — 15 city rows + the net-new Wave-2 GOTCHA block, ready for the next
    LA County wave.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → committed frontend data | A static JS data-array (coverage.js) is edited and committed |
| executor → committed playbook doc | LOCATION-ONBOARDING.md markdown is edited and committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-03 | Tampering | coverage.js static data array | mitigate | Edit is data-only (labels + literal geo_ids + boolean flags); node parse-check in verify confirms the module still loads. No new inputs/endpoints/auth. |
| T-157-04 | Information Disclosure | LOCATION-ONBOARDING.md | accept | Playbook records public officeholder/source patterns only; no secrets or credentials written. |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |

Realistic surface is minimal: static data-array edit + markdown edit, no runtime input handling.
</threat_model>

<verification>
- coverage.js parses and contains all 15 Wave-2 geo_ids with audit-correct hasContext flags.
- LOCATION-ONBOARDING.md contains 15 new city rows + a net-new Wave-2 GOTCHA block.
</verification>

<success_criteria>
- Criterion 1: all 15 cities surfaced with correct browse wiring and honest purple-chip state.
- Criterion 2: onboarding playbook updated with Wave-2 GOTCHAs + 15 Cities Onboarded rows.
</success_criteria>

<output>
Create `.planning/phases/157-wave-2-close-out/157-02-SUMMARY.md` when done — note which cities got
the purple chip vs which stayed plain, and the count of net-new GOTCHAs added.
</output>
