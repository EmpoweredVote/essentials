---
phase: 188-location-stats-strip
plan: 03
type: execute
wave: 3
depends_on: ["188-02"]
files_modified:
  - src/components/SectionBanner.jsx
  - src/components/SectionBanner.test.js
  - src/pages/Results.jsx
  - src/components/ElectionsView.jsx
autonomous: false
requirements: [STAT-01, STAT-03]

must_haves:
  truths:
    - "A city banner renders a top-right scrim with a POPULATION label + grouped number when stats.value is a positive number"
    - "State-tier and federal-tier banners render the population stat (D-04/D-06 always resolve)"
    - "A banner whose stats prop is null/0/NaN/undefined renders NO stat block at all (STAT-03)"
    - "Both Results.jsx and ElectionsView.jsx compute/pass resolved population stats to their SectionBanner calls with no page-specific divergence"
  artifacts:
    - path: "src/components/SectionBanner.jsx"
      provides: "Top-right population scrim (replaces the inert sr-only stats slot) + shouldRenderStat predicate"
      contains: "data-slot=\"stats\""
    - path: "src/pages/Results.jsx"
      provides: "populationMap useMemo passed to all three banners and to ElectionsView"
      contains: "resolvePopulation"
    - path: "src/components/ElectionsView.jsx"
      provides: "populationMap prop passed through to its three banners"
      contains: "populationMap"
  key_links:
    - from: "src/pages/Results.jsx populationMap"
      to: "SectionBanner stats prop"
      via: "resolvePopulation per tier -> {label:'POPULATION', value} | null -> stats={populationMap.Local/State/Federal}"
      pattern: "stats=\\{populationMap"
    - from: "src/pages/Results.jsx"
      to: "src/components/ElectionsView.jsx"
      via: "populationMap prop alongside buildingImageMap/featureIconMap"
      pattern: "populationMap=\\{populationMap\\}"
---

<objective>
Render the population stat and wire it end-to-end. Replace SectionBanner's inert `stats` sr-only slot
(line 214) with the top-right navy-scrim block per the locked UI-SPEC, resolve population in the parent
(Results.jsx) via a `populationMap` useMemo mirroring the shipped `buildingImageMap`/`featureIconMap`
pattern, and thread it through ElectionsView so both pages behave identically. This closes STAT-01
(user sees a legible population fact) and the render half of STAT-03 (omit cleanly on miss).

Purpose: Delivers the visible feature and keeps SectionBanner purely presentational (receives resolved
`stats` data, never resolves) — the exact posture Phase 189's shared-component consolidation needs.
Output: SectionBanner scrim render, Results.jsx populationMap + banner/ElectionsView wiring,
ElectionsView populationMap pass-through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/188-location-stats-strip/188-CONTEXT.md
@.planning/phases/188-location-stats-strip/188-RESEARCH.md
@.planning/phases/188-location-stats-strip/188-PATTERNS.md
@.planning/phases/188-location-stats-strip/188-UI-SPEC.md

<interfaces>
<!-- Contracts consumed (Plan 02) + the exact call sites to extend. -->
Consumes — src/lib/population.js (Plan 02):
  export function resolvePopulation({ tier, geoId, city, stateAbbrev }): number | null

SectionBanner.jsx anchors (VERIFIED line numbers):
  :39   export const FALLBACK_GRADIENTS = { ... }        // exported-pure-const test precedent
  :214  {stats && <div className="sr-only" data-slot="stats" />}   // REPLACE this inert slot
  :219  {featureIcons?.length > 0 && ( ... bottom:16 right:16 flex row ... )}  // conditional idiom + scrim family to mirror
  :89-101  FeatureIconChip scrim: background 'rgba(13, 17, 23, 0.55)', backdropFilter 'blur(2px)'

Results.jsx anchors (VERIFIED):
  :33    import { resolveFeatureIcons } from '../lib/featureIcons';
  :386,:823  searchParams.get('browse_geo_id')  // 7-digit place FIPS in browse mode (string)
  :1140  const buildingImageMap = useMemo(() => getBuildingImages(representingCity, userState), [...]);
  :1145  const featureIconMap = useMemo(() => resolveFeatureIcons({representingCity, userState, treasuryCities}), [...]);
  :1951-1969  three <SectionBanner> tier branches (imageUrl/featureIcons props to sit beside)
  :2070-2086  <ElectionsView ... buildingImageMap={} featureIconMap={} representingCity={} userState={} stateNames={} />

ElectionsView.jsx anchors (VERIFIED):
  :277  export default function ElectionsView({ ... buildingImageMap={}, featureIconMap={}, representingCity, userState, stateNames={} })
  :577-598  three <SectionBanner> tier branches receiving buildingImageMap?.Local etc.

UI-SPEC locked values (do NOT re-derive — cite 188-UI-SPEC.md):
  scrim: position absolute; top:16px right:16px; flex column; align-items flex-end; gap 4px;
         background rgba(13,17,23,0.55); backdropFilter blur(2px); borderRadius 10px; padding 4px 12px
  label: font var(--font-sans); 11px/600; lineHeight 13px; letterSpacing 1px; uppercase; color var(--color-ev-text-muted)
  number: font var(--font-sans); 20px/700; lineHeight 22px; color var(--color-ev-text-primary); value.toLocaleString()
  render only when: typeof stats?.value === 'number' && stats.value > 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the population scrim in SectionBanner (+ shouldRenderStat predicate)</name>
  <files>src/components/SectionBanner.jsx, src/components/SectionBanner.test.js</files>
  <read_first>
    - src/components/SectionBanner.jsx (full — need line 214 inert slot, the :219 featureIcons conditional idiom, the :89-101 FeatureIconChip scrim literals, the :39 FALLBACK_GRADIENTS export precedent, the :140-ish stats props docblock, and the :148-153 imageFailed graceful posture)
    - src/components/SectionBanner.test.js (the "test a named pure export, never render the DOM" convention)
    - .planning/phases/188-location-stats-strip/188-UI-SPEC.md §"Layout Contract", §"Typography", §"Color", §"Data / Rendering Contract" (binding values)
    - .planning/phases/188-location-stats-strip/188-PATTERNS.md §"src/components/SectionBanner.jsx"
  </read_first>
  <action>
    Replace the inert slot at SectionBanner.jsx:214 (`{stats && <div className="sr-only" data-slot="stats" />}`)
    with a guarded top-right stat block, placed in the same sibling group as the title and feature-icon
    row (after the image/gradient layer; NO z-index — follow existing DOM-order convention per UI-SPEC).
    Guard: render only when `typeof stats?.value === 'number' && stats.value > 0` — treat null/undefined/
    0/NaN/non-number identically and render nothing (STAT-03; mirror the `featureIcons?.length > 0 &&`
    idiom). The block is a `<div data-slot="stats">` styled per the locked UI-SPEC values quoted in
    <interfaces>: absolute top:16px right:16px, flex column, align-items flex-end, gap 4px, background
    `rgba(13,17,23,0.55)` (verbatim, matching FeatureIconChip — D-11), backdropFilter `blur(2px)`,
    borderRadius 10px, padding 4px 12px. First child span = the label (`{stats.label}`) at 11px/600,
    lineHeight 13px, letterSpacing 1px, textTransform uppercase, color var(--color-ev-text-muted),
    font var(--font-sans). Second child span = `{stats.value.toLocaleString()}` at 20px/700, lineHeight
    22px, color var(--color-ev-text-primary), font var(--font-sans). D-09: label comes from the prop
    (do NOT hardcode 'POPULATION' inside the component), number via toLocaleString (no abbrev/suffix).
    D-13: tokens only, dark-mode only, no `!important`. Do NOT change font sizes/padding (the 47px content
    fits the 49px mobile budget per UI-SPEC non-collision proof).

    Update the `stats` prop docblock (~line 140) from "inert scaffolding" to the real
    `{ label: string, value: number } | null` contract.

    Extract the guard as an exported pure predicate `export function shouldRenderStat(stats)` returning
    `typeof stats?.value === 'number' && stats.value > 0`, and use it in the render guard — mirroring how
    FALLBACK_GRADIENTS is an exported testable const (keeps repo's no-jsdom test convention viable).

    Add a `describe('shouldRenderStat')` block to SectionBanner.test.js asserting: true for {value:652503};
    false for null, undefined, {value:0}, {value:NaN}, {value:'5'}, {} — pure-logic only, no render.
  </action>
  <verify>
    <automated>npx vitest run src/components/SectionBanner.test.js</automated>
  </verify>
  <acceptance_criteria>
    - SectionBanner.jsx no longer contains the `sr-only` stats slot; it renders a `<div data-slot="stats">` guarded by `shouldRenderStat(stats)` (equivalently `typeof stats?.value === 'number' && stats.value > 0`).
    - The scrim inline styles match UI-SPEC exactly: top:16px, right:16px, background rgba(13,17,23,0.55), backdropFilter blur(2px), borderRadius 10px, padding 4px 12px; label 11px/600 uppercase var(--color-ev-text-muted); number 20px/700 var(--color-ev-text-primary) rendered via `.toLocaleString()`.
    - No `z-index` and no `!important` introduced anywhere in SectionBanner.jsx; label text is `{stats.label}` (not a hardcoded string).
    - `shouldRenderStat` is exported; `npx vitest run src/components/SectionBanner.test.js` exits 0 with the new predicate cases passing.
  </acceptance_criteria>
  <done>SectionBanner renders the locked top-right population scrim when given a positive numeric stat, omits entirely otherwise, and stays presentational (label supplied via prop).</done>
</task>

<task type="auto">
  <name>Task 2: Resolve population in Results.jsx and thread it through ElectionsView</name>
  <files>src/pages/Results.jsx, src/components/ElectionsView.jsx</files>
  <read_first>
    - src/pages/Results.jsx around :33 (imports), :386/:823 (browse_geo_id read), :1071-1148 (representingCity/userState derivation + buildingImageMap/featureIconMap useMemos), :1951-1969 (three SectionBanner tier branches), :2070-2086 (ElectionsView hand-off)
    - src/components/ElectionsView.jsx :277-290 (signature/destructure) and :577-598 (three SectionBanner tier branches)
    - .planning/phases/188-location-stats-strip/188-RESEARCH.md §"Geo-id Resolution Across Modes" and §"Where the Resolver Lives"
    - .planning/phases/188-location-stats-strip/188-PATTERNS.md §"src/pages/Results.jsx" and §"src/components/ElectionsView.jsx"
  </read_first>
  <action>
    Results.jsx: add `import { resolvePopulation } from '../lib/population';` alongside the existing lib
    imports (near :16/:33). Add a `populationMap` useMemo mirroring the buildingImageMap/featureIconMap
    blocks at :1140-1148. It computes three values with the already-derived identity variables
    (representingCity :1071-1100, userState :1104-1138) and `searchParams.get('browse_geo_id')` (D-05):
    cityPop = resolvePopulation({ tier:'city', geoId: searchParams.get('browse_geo_id'), city:
    representingCity, stateAbbrev: userState }); statePop = resolvePopulation({ tier:'state', stateAbbrev:
    userState }) (D-04); federalPop = resolvePopulation({ tier:'federal' }) (D-06). Return
    `{ Local: cityPop != null ? { label:'POPULATION', value: cityPop } : null, State: statePop != null ?
    { label:'POPULATION', value: statePop } : null, Federal: federalPop != null ? { label:'POPULATION',
    value: federalPop } : null }`. Dependency array: [representingCity, userState, searchParams]. The `!=
    null` gate keeps null flowing through so STAT-03 omit stays intact.
    Add `stats={populationMap.Local}` / `stats={populationMap.State}` / `stats={populationMap.Federal}` to
    the three <SectionBanner> tier branches at :1951-1969 (beside imageUrl/featureIcons). Add
    `populationMap={populationMap}` to the <ElectionsView> hand-off at :2082-2086.

    ElectionsView.jsx: add `populationMap = {}` to the destructured props (:277-290). Add
    `stats={populationMap?.Local}` / `stats={populationMap?.State}` / `stats={populationMap?.Federal}` to
    the three <SectionBanner> tier branches (:577-598). ElectionsView must NOT read browse_geo_id or the
    router — it stays a pure pass-through (per PATTERNS note), so both pages resolve identically via the
    single Results-computed map (Phase 189 alignment).
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Results.jsx imports `resolvePopulation`, has a `populationMap` useMemo returning `{Local, State, Federal}` of `{label:'POPULATION', value} | null`, with `searchParams` in the dependency array and `browse_geo_id` supplied to the city tier.
    - All three Results.jsx <SectionBanner> branches receive `stats={populationMap.Local|State|Federal}`; the <ElectionsView> receives `populationMap={populationMap}`.
    - ElectionsView.jsx destructures `populationMap = {}` and passes `stats={populationMap?.Local|State|Federal}` to its three banners; it does NOT read browse_geo_id/router.
    - `npm run lint` reports no new errors for the edited files; `npm test` (full suite) exits 0.
  </acceptance_criteria>
  <done>Population is resolved once in Results.jsx and rendered identically on both the Results and Elections banners across all three tiers, with null-on-miss preserved end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live visual verification across tiers + omit case</name>
  <what-built>The top-right population scrim now renders on city/state/federal banners on both the Results and Elections pages, sourced from the committed Census bundle via resolvePopulation, and omits cleanly when a location does not resolve.</what-built>
  <read_first>
    - .planning/phases/188-location-stats-strip/188-UI-SPEC.md §"Layout Contract" and §"Non-collision proof" (the visual acceptance targets)
    - project memory: feedback_provide_city_browse_links (use /results?browse_geo_id=... to reach a specific city)
  </read_first>
  <how-to-verify>
    1. Run `npm run dev` and open the app (essentials.empowered.vote local build) in dark mode.
    2. Load a browse-by-area city with a known FIPS, e.g. `/results?browse_geo_id=4858016` (Plano, TX) and `/results?browse_geo_id=0644000` (Los Angeles, CA). Confirm the CITY banner shows a top-right `POPULATION` label over a grouped number (e.g. `285,494` / `3,898,747`), on a rounded semi-transparent navy scrim, right-aligned, clear of the top-right corner, never overlapping the bottom-left title or the bottom-right feature-icon row — at BOTH mobile (120px) and desktop (180px) widths.
    3. Confirm the STATE banner shows the state population and the FEDERAL banner shows the US national total (`UNITED STATES` title + `POPULATION` + national figure).
    4. Load an address-search city (no browse_geo_id) for a known place and confirm the city stat resolves via the name+state index.
    5. Navigate to the Elections page for the same location and confirm the identical stat appears on each tier (no divergence).
    6. Confirm the omit case: a city that does not resolve (e.g. a government-list browse with no matching place) shows NO stat block — no "0", "undefined", empty scrim, or broken label — and the banner otherwise renders normally. Check the browser console for zero new errors/warnings.
  </how-to-verify>
  <acceptance_criteria>
    - City, state, and federal banners each display a correctly formatted `POPULATION` + grouped number where data resolves, on both Results and Elections pages, positioned per UI-SPEC (top-right, clear of corner, no title/icon collision) at both breakpoints.
    - An unresolved location renders no stat block at all (STAT-03) with no console errors.
    - Operator confirms parity between the Results and Elections banners.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any visual/positioning/omit issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved stats data → SectionBanner render | A `{label, value}` object (or null) from a trusted static bundle is rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-06 | (V5) Input Validation | `stats.value.toLocaleString()` in SectionBanner | accept | `value` is a number from the static committed bundle; toLocaleString on a number has no injection surface (no HTML, no user input). Render guard rejects non-numbers first. |
| T-188-07 | Denial of Service (UI) | Broken/empty scrim on unresolved data | mitigate | `shouldRenderStat` guard (typeof number && >0) omits the entire block for null/0/NaN/undefined — no empty container, no layout shift (STAT-03, Phase 189 empty-state parity). |
| T-188-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- SectionBanner renders the locked scrim only when stats.value is a positive number; predicate unit-tested.
- Results.jsx resolves population once and passes it to both its banners and ElectionsView; ElectionsView passes through.
- Full suite + lint green; live checkpoint confirms all tiers, both pages, and the omit case.
</verification>

<success_criteria>
- STAT-01: user sees a legible population fact on city + state banners (and federal), positioned without collision.
- STAT-03: unavailable stat is omitted cleanly — no zeros/nulls/undefined/broken labels; banner renders normally.
- Both pages behave identically (no page-specific divergence), setting up Phase 189 consolidation.
</success_criteria>

<output>
Create `.planning/phases/188-location-stats-strip/188-03-SUMMARY.md` when done.
</output>
