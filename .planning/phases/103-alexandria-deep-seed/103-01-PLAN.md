---
phase: 103-alexandria-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/312_alexandria_government.sql
autonomous: true
requirements:
  - VA-DEEP-01
tags:
  - virginia
  - alexandria
  - city-government
  - migration
must_haves:
  truths:
    - "Migration 312 file exists at C:/EV-Accounts/backend/migrations/312_alexandria_government.sql"
    - "Migration 312 applied successfully via mcp__supabase-local__apply_migration (post-verification DO block passes all gates)"
    - "essentials.governments has exactly 1 row with name='City of Alexandria, Virginia, US', state='VA', city='Alexandria', geo_id='5101000', type='LOCAL'"
    - "essentials.chambers has exactly 1 row with name='City Council', name_formal='Alexandria City Council' linked to the Alexandria government"
    - "essentials.districts has 1 LOCAL_EXEC row (geo_id='5101000', state='va', mtfcc=NULL, label='Alexandria (Citywide)') and 1 LOCAL row (geo_id='5101000', state='va', mtfcc=NULL, label='Alexandria (At-Large)')"
    - "essentials.politicians has 7 rows with external_id BETWEEN -5101000007 AND -5101000001 (Mayor Gaskins + 6 council), all party=NULL, is_appointed=false"
    - "essentials.offices has exactly 7 rows linked to Alexandria districts: 1 with title='Mayor' (LOCAL_EXEC), 6 with title='Council Member' (LOCAL); all representing_state='VA', is_appointed_position=false"
    - "All 7 politicians have office_id populated (no NULLs in external_id BETWEEN -5101000007 AND -5101000001)"
    - "Section-split detector returns 0 rows for geo_id='5101000' / mtfcc='G4110' / state='va'"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/312_alexandria_government.sql"
      provides: "Alexandria city government + Mayor + 6 council migration"
      contains: "City of Alexandria, Virginia, US"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts (geo_id='5101000')"
      via: "district_id foreign key"
      pattern: "geo_id = '5101000' AND state = 'va'"
    - from: "essentials.politicians"
      to: "essentials.offices"
      via: "office_id back-fill"
      pattern: "office_id IS NOT NULL WHERE external_id BETWEEN -5101000007 AND -5101000001"
---

<objective>
Seed the Alexandria, Virginia city government — Mayor Alyia Gaskins (LOCAL_EXEC) and 6 at-large council members (LOCAL) — by creating and applying migration 312 (VA-DEEP-01). Follow the exact structure of migration 277 (Leonardtown), substituting Alexandria identifiers (geo_id='5101000', state='va'/'VA', 7 officials instead of 6).

Purpose: Establishes the city government tier so any Alexandria address routes to the LOCAL section with Mayor + 6 council. Required for subsequent ACPS plan (Plan 02) and headshots (Plan 03) because both depend on the politician UUIDs created here.

Output: One SQL migration file (312_alexandria_government.sql) applied to the production Supabase database. After apply, essentials.governments has the Alexandria row; essentials.districts has LOCAL_EXEC + LOCAL rows for geo_id='5101000'; 7 politicians + 7 offices are created and linked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/103-alexandria-deep-seed/103-CONTEXT.md
@.planning/phases/103-alexandria-deep-seed/103-PATTERNS.md
@C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Alexandria geofence + roster from alexandriava.gov</name>
  <files>
    (read-only verification; no files modified)
  </files>
  <read_first>
    - C:/Transparent Motivations/essentials/.planning/phases/103-alexandria-deep-seed/103-CONTEXT.md (D-05, D-07, D-19, roster names)
    - C:/Transparent Motivations/essentials/.planning/ROADMAP.md §Phase 103 (roster: Bagley, Aguirre, Chapman, Elnoubi, Greene, Marks)
  </read_first>
  <action>
    Confirm two facts before writing migration 312:

    (1) Geofence check — run via `mcp__supabase-local__execute_sql`:
        SELECT geo_id, mtfcc, state FROM essentials.geofence_boundaries
        WHERE geo_id = '5101000' AND mtfcc = 'G4110' AND state = '51';
        Expected: exactly 1 row (per D-05). If 0 rows, STOP and report — Phase 100 geofence loader did not load Alexandria G4110.

    (2) Roster verification — fetch https://www.alexandriava.gov/city-council to confirm the 6 current at-large council member full names and the Mayor's full name. The roster currently expected per ROADMAP/CONTEXT.md is:
        Mayor: Alyia Gaskins
        Council (6 at-large): Bagley, Aguirre, Chapman, Elnoubi, Greene, Marks
        Use the live site to expand each to FULL legal/preferred names (first + last) and confirm titles (Mayor vs Vice Mayor vs Council Member). If Alexandria designates one council member as "Vice Mayor," still use title='Council Member' in offices (Mayor is LOCAL_EXEC; all six council seats are LOCAL with title='Council Member' — Vice Mayor is a procedural role, not a separate office, per Leonardtown convention).

    (3) Pre-existing data check — run via `mcp__supabase-local__execute_sql`:
        SELECT COUNT(*) FROM essentials.governments
          WHERE name = 'City of Alexandria, Virginia, US';
        SELECT COUNT(*) FROM essentials.politicians
          WHERE external_id BETWEEN -5101000007 AND -5101000001;
        Both must return 0 (per D-07). If non-zero, STOP and report — migration 312 may already be partially applied.

    Record the 7 verified full names (Mayor + 6 council) in your task notes for use in Task 2.

    Per D-19, the external_id assignment is: -5101000001 = Mayor Gaskins, -5101000002..-5101000007 = 6 council members in alphabetical order by last name (Aguirre, Bagley, Chapman, Elnoubi, Greene, Marks — assign sequentially after verifying exact spelling on alexandriava.gov).
  </action>
  <verify>
    <automated>
      SELECT statements above return: G4110 row count=1, governments=0, politicians=0; alexandriava.gov fetch returns 7 confirmed full names.
    </automated>
  </verify>
  <acceptance_criteria>
    - geofence_boundaries query for geo_id='5101000' AND mtfcc='G4110' AND state='51' returns exactly 1 row
    - governments query for name='City of Alexandria, Virginia, US' returns 0
    - politicians query for external_id range returns 0
    - alexandriava.gov roster yields 7 full names (Mayor + 6 council) recorded in task notes
    - Council members alphabetically ordered by last name with external_id assignments -5101000002..-5101000007
    - Task notes explicitly record Mayor external_id = -5101000001
  </acceptance_criteria>
  <done>
    Geofence + government + politicians pre-state confirmed; 7 verified full names recorded with external_id assignments for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 312_alexandria_government.sql</name>
  <files>
    C:/EV-Accounts/backend/migrations/312_alexandria_government.sql
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql (full file — this is the structural template)
    - C:/Transparent Motivations/essentials/.planning/phases/103-alexandria-deep-seed/103-PATTERNS.md (§312 section, lines 19-223; documents every adaptation)
    - C:/Transparent Motivations/essentials/.planning/phases/103-alexandria-deep-seed/103-CONTEXT.md (decisions D-04..D-19)
  </read_first>
  <action>
    Create file C:/EV-Accounts/backend/migrations/312_alexandria_government.sql by copying the full structure of 277_leonardtown_government.sql and applying the following exact substitutions (per D-04..D-19 and PATTERNS.md §312):

    Header / ledger:
    - Header comment: "Migration 312: Alexandria, Virginia city government (VA-DEEP-01)"
    - Migration ledger version: '312'

    Identifier substitutions (replace every occurrence in the analog):
    - Government name: 'Town of Leonardtown, Maryland, US' -> 'City of Alexandria, Virginia, US'
    - Government type: 'LOCAL' (unchanged)
    - Government state: 'MD' -> 'VA' (uppercase, per D-11)
    - Government city: 'Leonardtown' -> 'Alexandria'
    - Government geo_id: '2446475' -> '5101000' (per D-05)
    - Chamber name: 'Town Council' -> 'City Council'
    - Chamber name_formal: 'Leonardtown Town Council' -> 'Alexandria City Council'
    - Chamber slug column: OMIT (slug is GENERATED ALWAYS per D-14)
    - District state: 'md' -> 'va' (lowercase, per D-10)
    - District LOCAL_EXEC label: 'Leonardtown (Townwide)' -> 'Alexandria (Citywide)'
    - District LOCAL label: 'Leonardtown (At-Large)' -> 'Alexandria (At-Large)'
    - District mtfcc: NULL (unchanged, per D-13)
    - District tiger_geoid: OMIT (defaults NULL per D-08)
    - Office representing_state: 'MD' -> 'VA' (uppercase, per D-12)
    - Office title for Mayor row: 'Mayor' (unchanged)
    - Office title for council rows: 'Council Member' (unchanged)
    - is_appointed: false, is_appointed_position: false, party: NULL (D-16, D-17)

    Politician rows (7 total, in this exact order):
    - Mayor: Alyia Gaskins (first='Alyia', last='Gaskins', external_id=-5101000001) — LOCAL_EXEC office with title='Mayor'
    - Council 1..6 in alphabetical order by last name (use Task 1 verified full names), external_ids -5101000002..-5101000007 — each LOCAL office with title='Council Member'

    Each politician+office uses the WITH ins_p CTE pattern from PATTERNS.md §312 (8 CTE blocks total: 1 Mayor + 6 council + 0 extras). Council count in analog is 5; THIS MIGRATION HAS 6 COUNCIL BLOCKS — add one additional CTE block beyond the analog's 5.

    office_id back-fill UPDATE: range BETWEEN -5101000007 AND -5101000001 (per PATTERNS.md §312, "Adapt range" instruction).

    Post-verification DO block — keep the 277 pattern but adapt all three gates plus add Gate (d) for office_id NULL check (per PATTERNS.md §312 instruction "Also add Gate (d) for office_id back-fill nulls"):
    - Gate (a): v_gov_count <> 1 -> EXCEPTION "expected 1 City of Alexandria government row, found %"
    - Gate (b): v_office_count <> 7 -> EXCEPTION "expected 7 offices linked to geo_id=5101000 districts, found %" (NOTE: 7, not 6 — 1 Mayor + 6 council)
    - Gate (c): section-split detector with geo_id='5101000', mtfcc='G4110', state='va' -> EXCEPTION on > 0 orphans
    - Gate (d): NEW — count politicians in external_id BETWEEN -5101000007 AND -5101000001 with office_id IS NULL; EXCEPTION on > 0 NULLs
    - Final RAISE NOTICE with all four counters

    Do NOT include BEGIN/COMMIT wrapping the migration body — supabase apply_migration handles transaction boundaries (confirmed by migration 277 structure).

    File MUST end with the migration ledger INSERT for version '312'.
  </action>
  <verify>
    <automated>
      File C:/EV-Accounts/backend/migrations/312_alexandria_government.sql exists; `grep -c "City of Alexandria, Virginia, US" C:/EV-Accounts/backend/migrations/312_alexandria_government.sql` returns >= 6 (governments INSERT + idempotency check + chamber subqueries + post-verification gate); `grep -c "external_id" C:/EV-Accounts/backend/migrations/312_alexandria_government.sql` returns >= 14 (7 politician INSERTs + 7 ON CONFLICT/range references); `grep "v_office_count <> 7" C:/EV-Accounts/backend/migrations/312_alexandria_government.sql` returns exactly 1 match.
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/312_alexandria_government.sql
    - Contains exactly 7 ON CONFLICT (external_id) DO NOTHING clauses (1 Mayor + 6 council)
    - Contains exactly 6 occurrences of "title='Council Member'" or "'Council Member'" inside INSERT...SELECT blocks
    - Contains exactly 1 occurrence of "'Mayor'" as office title
    - All district inserts use state = 'va' (lowercase, per D-10)
    - All government and office inserts use 'VA' (uppercase, per D-11, D-12)
    - Mayor LOCAL_EXEC district row uses label='Alexandria (Citywide)' and mtfcc IS NULL
    - LOCAL district row uses label='Alexandria (At-Large)' and mtfcc IS NULL
    - office_id back-fill UPDATE statement uses range BETWEEN -5101000007 AND -5101000001
    - Post-verification DO block has 4 gates: gov_count=1, office_count=7, split_count=0, null_office_id_count=0
    - Section-split detector inside post-verification uses geo_id='5101000', mtfcc='G4110', state='va'
    - File ends with `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('312') ON CONFLICT (version) DO NOTHING;`
    - File does NOT contain `slug` in any chamber INSERT column list (D-14)
    - File does NOT contain `tiger_geoid` in any district INSERT column list (D-08)
  </acceptance_criteria>
  <done>
    Migration 312 SQL file written and validated against PATTERNS.md adaptation rules.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 312 + spot-check verification</name>
  <files>
    (no new files; applies migration via MCP tool)
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/312_alexandria_government.sql (the file written in Task 2 — read full contents to feed into apply_migration query parameter)
    - C:/Transparent Motivations/essentials/.planning/phases/103-alexandria-deep-seed/103-CONTEXT.md (D-05, D-07 — expected post-state)
  </read_first>
  <action>
    (1) Apply migration via `mcp__supabase-local__apply_migration`:
        name: "312_alexandria_government"
        query: <full contents of 312_alexandria_government.sql>
        The migration's embedded DO block runs the 4-gate post-verification during apply. If the apply call returns an error containing "Post-verification FAILED" or "EXCEPTION", STOP and report — do not retry without diagnosing.

    (2) Run spot-check queries via `mcp__supabase-local__execute_sql` and confirm each returns the expected value:

        Query A — government row:
          SELECT COUNT(*) FROM essentials.governments
          WHERE name = 'City of Alexandria, Virginia, US';
          Expected: 1

        Query B — district rows:
          SELECT district_type, label, state, geo_id, mtfcc
          FROM essentials.districts
          WHERE geo_id = '5101000' AND state = 'va'
          ORDER BY district_type;
          Expected: 2 rows — LOCAL (Alexandria (At-Large), mtfcc=NULL), LOCAL_EXEC (Alexandria (Citywide), mtfcc=NULL)

        Query C — politicians + offices:
          SELECT p.external_id, p.full_name, o.title, d.district_type
          FROM essentials.politicians p
          JOIN essentials.offices o ON o.politician_id = p.id
          JOIN essentials.districts d ON d.id = o.district_id
          WHERE p.external_id BETWEEN -5101000007 AND -5101000001
          ORDER BY p.external_id DESC;
          Expected: 7 rows — Mayor Gaskins at -5101000001 with title='Mayor' / district_type='LOCAL_EXEC', and 6 council members -5101000002..-5101000007 with title='Council Member' / district_type='LOCAL'

        Query D — office_id back-fill:
          SELECT COUNT(*) FROM essentials.politicians
          WHERE external_id BETWEEN -5101000007 AND -5101000001
            AND office_id IS NULL;
          Expected: 0

        Query E — section-split independent verification:
          SELECT COUNT(*) FROM essentials.geofence_boundaries gb
          WHERE gb.geo_id = '5101000' AND gb.mtfcc = 'G4110'
            AND NOT EXISTS (
              SELECT 1 FROM essentials.districts d
              WHERE d.geo_id = gb.geo_id AND d.state = 'va'
            );
          Expected: 0

    (3) Update C:/Transparent Motivations/essentials/.planning/STATE.md "Next migration" counter: 312 -> 313 (migration 312 has been applied). Record application in STATE.md "Accumulated Context" with a one-line entry "Migration 312 applied: Alexandria city government (Mayor Gaskins + 6 council); 7 offices linked to geo_id=5101000".

    If any spot-check query returns an unexpected value, STOP and report. The migration's own post-verification block should have prevented this, but the spot-checks confirm production state.
  </action>
  <verify>
    <automated>
      mcp__supabase-local__apply_migration returns success (no exception). Queries A=1, B=2 rows with expected district_types, C=7 rows with Mayor+6 council, D=0, E=0.
    </automated>
  </verify>
  <acceptance_criteria>
    - mcp__supabase-local__apply_migration completes without raising "Post-verification FAILED"
    - Query A returns exactly 1
    - Query B returns 2 rows (one LOCAL_EXEC with label='Alexandria (Citywide)', one LOCAL with label='Alexandria (At-Large)'), both with mtfcc IS NULL and state='va'
    - Query C returns exactly 7 rows; the row at external_id=-5101000001 has title='Mayor' and district_type='LOCAL_EXEC'; the other 6 rows have title='Council Member' and district_type='LOCAL'
    - Query D returns 0
    - Query E returns 0
    - STATE.md "Next migration" line updated from 312 to 313
    - STATE.md gains one line under Accumulated Context recording migration 312 application
  </acceptance_criteria>
  <done>
    Migration 312 applied to production Supabase; spot-checks confirm 1 government + 2 districts + 7 politicians/offices + 0 orphans + 0 NULL office_ids; STATE.md updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner -> Migration SQL | Static SQL file; no runtime user input — boundary is between author intent and DB executor |
| MCP tool -> Production Supabase | apply_migration crosses into production essentials.* schema |
| alexandriava.gov -> Planner (Task 1) | Untrusted external website content read for name verification only — never embedded as code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01-01 | Tampering | essentials.governments / districts / politicians / offices | mitigate | All INSERTs use WHERE NOT EXISTS or ON CONFLICT DO NOTHING; post-verification DO block raises EXCEPTION on count mismatch, rolling back the apply_migration transaction |
| T-103-01-02 | Tampering | essentials.offices.politician_id linkage | mitigate | CTE NOT EXISTS guard on (district_id, politician_id) prevents duplicate office rows on re-run; Gate (d) in post-verification verifies office_id back-fill is total |
| T-103-01-03 | Information Disclosure | external_id collision with other states | accept | external_id range -5101000001..-5101000007 is geo_id-based (5101000); collision impossible because no other US place shares this geo_id |
| T-103-01-04 | Denial of Service | Section-split orphan blocking address routing | mitigate | Gate (c) post-verification refuses to commit if geofence_boundaries has a G4110 row at geo_id=5101000 without a matching district row |
| T-103-01-05 | Tampering | Name spelling errors (Bagley/Aguirre/etc.) | mitigate | Task 1 verifies live alexandriava.gov roster before SQL is written; PATTERNS.md flags the ROADMAP names as needing verification |
| T-103-01-06 | Elevation of Privilege | is_appointed_position misuse | mitigate | All 7 officials are voter-elected per D-17; INSERTs hard-code is_appointed=false, is_appointed_position=false |
| T-103-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SQL-only migration; supply-chain surface = zero |
</threat_model>

<verification>
After all 3 tasks complete:
1. Migration 312 file exists in C:/EV-Accounts/backend/migrations/
2. supabase_migrations.schema_migrations has version '312'
3. Spot-check Queries A-E from Task 3 all match expected values
4. STATE.md "Next migration" counter reads 313
</verification>

<success_criteria>
- Migration 312 applied to production Supabase without raising EXCEPTION
- essentials.governments has exactly 1 City of Alexandria row
- essentials.districts has exactly 1 LOCAL_EXEC + 1 LOCAL row for geo_id='5101000', state='va'
- essentials.politicians has 7 rows (Mayor + 6 council) with external_id BETWEEN -5101000007 AND -5101000001
- essentials.offices has 7 rows: 1 Mayor (LOCAL_EXEC), 6 Council Member (LOCAL); all representing_state='VA'
- All 7 politicians have office_id populated (0 NULLs)
- Section-split detector returns 0 for geo_id='5101000' / mtfcc='G4110' / state='va'
- STATE.md counter advanced from 312 to 313
</success_criteria>

<output>
Create `.planning/phases/103-alexandria-deep-seed/103-01-SUMMARY.md` when done. Include: politician UUIDs for all 7 officials (needed by Plan 03), confirmation of all 5 spot-check query results, link to applied migration 312 row.
</output>
