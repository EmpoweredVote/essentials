---
phase: 93-md-legislature-federal-officials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/272_md_legislative_chambers.sql
autonomous: true
requirements:
  - MD-GOV-03
  - MD-GOV-04
tags:
  - md
  - chambers
  - migration

must_haves:
  truths:
    - "essentials.chambers contains 'Maryland Senate' under State of Maryland government per D-07"
    - "essentials.chambers contains 'Maryland House of Delegates' under State of Maryland government per D-07"
    - "Pre-flight assertions fail loudly if State of Maryland row missing or chambers already exist per D-08"
    - "Migration is idempotent — re-run inserts 0 additional rows"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/272_md_legislative_chambers.sql"
      provides: "MD legislative chambers migration"
      contains: "Maryland Senate"
  key_links:
    - from: "essentials.chambers (Maryland Senate)"
      to: "essentials.governments (State of Maryland, state='MD')"
      via: "government_id FK via name+state subquery"
      pattern: "WHERE name = 'State of Maryland' AND state = 'MD'"
    - from: "essentials.chambers (Maryland House of Delegates)"
      to: "essentials.governments (State of Maryland, state='MD')"
      via: "government_id FK via name+state subquery"
      pattern: "WHERE name = 'State of Maryland' AND state = 'MD'"
---

<objective>
Create migration 272 to seed the two Maryland legislative chambers (Maryland Senate and Maryland House of Delegates) under the existing State of Maryland government row (geo_id='24'), implementing D-07 (4-migration sequence starts with chambers-only) and D-08 (pre-flight assertions).

Purpose: Phase 92 seeded MD executive chambers but not legislative ones. Migrations 273 and 274 will reference these chambers via name lookup — they must exist before any official-seeding migration runs. RESEARCH.md confirms D-02 resolution: every A/B parent district elects 3 delegates via the existing subdistrict rows, so NO parent STATE_LOWER district rows are needed in this migration (per D-08 conditional which is now resolved to "not required"). This migration is chambers-only.

Output: One new SQL migration file `272_md_legislative_chambers.sql` that adds exactly 2 rows to `essentials.chambers` under the State of Maryland government_id, applied to production via mcp__supabase-local__apply_migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/93-md-legislature-federal-officials/93-CONTEXT.md
@.planning/phases/93-md-legislature-federal-officials/93-RESEARCH.md
@.planning/phases/93-md-legislature-federal-officials/93-PATTERNS.md

# Canonical analog — copy this file's pattern (same state, chambers-only)
@C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql

# Wider chambers analog (Oregon precedent for legislative chambers)
@C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write migration 272 — MD legislative chambers</name>
  <files>C:/EV-Accounts/backend/migrations/272_md_legislative_chambers.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/272_md_legislative_chambers.sql (target file - confirm not present yet to avoid overwrite)
    - C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql (exact analog: same state, same WHERE NOT EXISTS pattern, same government subquery)
    - C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql (additional chamber-naming precedent)
    - .planning/phases/93-md-legislature-federal-officials/93-PATTERNS.md (specific patterns and pre-flight blocks listed for this file)
    - .planning/phases/93-md-legislature-federal-officials/93-RESEARCH.md (Pattern 1 reference for chamber insert)
    - .planning/phases/93-md-legislature-federal-officials/93-CONTEXT.md (D-07, D-08, naming convention discretion)
  </read_first>
  <behavior>
    - Pre-flight assert 1: exactly 1 row exists in essentials.governments WHERE name='State of Maryland' AND state='MD' (RAISE EXCEPTION if not)
    - Pre-flight assert 2: zero existing essentials.chambers rows with name IN ('Maryland Senate','Maryland House of Delegates') under State of Maryland government_id (RAISE EXCEPTION if any exist)
    - Insert exactly 1 chamber row with name='Maryland Senate', name_formal='Maryland State Senate', government_id resolved by subquery (WHERE NOT EXISTS guard)
    - Insert exactly 1 chamber row with name='Maryland House of Delegates', name_formal='Maryland House of Delegates', government_id resolved by subquery (WHERE NOT EXISTS guard)
    - Wrapped in BEGIN/COMMIT
    - Idempotent: re-running the file inserts 0 additional rows (both INSERTs guarded by WHERE NOT EXISTS)
  </behavior>
  <action>
    **Pre-flight (STATE.md counter check):** Before applying migration 272, verify `.planning/STATE.md` shows `Next migration: 272` (a known counter discrepancy was documented in project memory: 268 -> 272). If STATE.md shows a different value, update STATE.md to `Next migration: 272` first, then proceed. Do NOT skip this check -- a stale counter causes future migrations to be mis-numbered.

    Create `C:/EV-Accounts/backend/migrations/272_md_legislative_chambers.sql` mirroring `269_md_government_chambers.sql` structure. Copy the file header style from 269 and adapt: change migration number to 272 and document the 2 legislative chambers added. Use BEGIN/COMMIT transaction wrapper.

    Block A — Pre-flight assertion on State of Maryland government row (copy lines 35-46 of 269 verbatim): `DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD') <> 1 THEN RAISE EXCEPTION ... END IF; END $$;`.

    Block B — New pre-flight assertion for legislative chamber non-existence (per D-08): `DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.chambers c JOIN essentials.governments g ON g.id = c.government_id WHERE g.name = 'State of Maryland' AND c.name IN ('Maryland Senate','Maryland House of Delegates')) <> 0 THEN RAISE EXCEPTION 'Pre-flight failed: MD legislative chambers already exist'; END IF; END $$;`.

    Block C — Insert 'Maryland Senate' with name_formal='Maryland State Senate' per CONTEXT.md Claude's Discretion. Use INSERT ... SELECT ... WHERE NOT EXISTS guard on (name + government_id). Resolve government_id via subquery `(SELECT id FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD')`. Column list: (id, name, name_formal, government_id). DO NOT include `slug` — it is GENERATED ALWAYS (Pitfall 6 in RESEARCH.md).

    Block D — Insert 'Maryland House of Delegates' with name_formal='Maryland House of Delegates' (legislative chamber names are self-qualifying — same convention as OR house in 222). Same INSERT ... SELECT ... WHERE NOT EXISTS pattern as Block C.

    Block E — Migration ledger entry insert if 269 includes one (check the source file). End with COMMIT.

    Do NOT add parent STATE_LOWER district rows — RESEARCH.md confirmed D-02 resolution: all 12 A/B parent districts elect 3 delegates from existing subdistrict rows (the 71 existing SLDL rows cover all 141 delegate positions).

    Apply the migration to production via mcp__supabase-local__apply_migration with name `272_md_legislative_chambers`.
  </action>
  <verify>
    <automated>
SELECT COUNT(*) FROM essentials.chambers c
JOIN essentials.governments g ON g.id = c.government_id
WHERE g.name = 'State of Maryland' AND g.state = 'MD'
  AND c.name IN ('Maryland Senate','Maryland House of Delegates');
-- Expected: 2
    </automated>
    <automated>
SELECT name, name_formal FROM essentials.chambers c
JOIN essentials.governments g ON g.id = c.government_id
WHERE g.name = 'State of Maryland' AND c.name IN ('Maryland Senate','Maryland House of Delegates')
ORDER BY name;
-- Expected rows:
--   'Maryland House of Delegates', 'Maryland House of Delegates'
--   'Maryland Senate',             'Maryland State Senate'
    </automated>
  </verify>
  <done>
    File `272_md_legislative_chambers.sql` exists; migration applied; SQL count query returns exactly 2; `Maryland Senate` has `name_formal='Maryland State Senate'`; `Maryland House of Delegates` has `name_formal='Maryland House of Delegates'`; idempotent re-apply inserts 0 additional rows.
  </done>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/272_md_legislative_chambers.sql`
    - File contains text 'Maryland Senate' AND 'Maryland House of Delegates' AND 'Maryland State Senate'
    - File contains text 'State of Maryland' AND 'state = ''MD'''
    - File does NOT contain 'slug' column in any INSERT statement
    - File contains 2 INSERT statements into essentials.chambers
    - File contains 2 DO $$ BEGIN ... RAISE EXCEPTION blocks (pre-flight assertions)
    - SQL: `SELECT COUNT(*) FROM essentials.chambers c JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='State of Maryland' AND g.state='MD' AND c.name IN ('Maryland Senate','Maryland House of Delegates')` returns 2
    - SQL: `SELECT name_formal FROM essentials.chambers WHERE name='Maryland Senate'` returns 'Maryland State Senate'
    - SQL: `SELECT name_formal FROM essentials.chambers WHERE name='Maryland House of Delegates'` returns 'Maryland House of Delegates'
    - SQL: Re-applying migration produces no errors and no new rows (idempotency)
    - SQL: `SELECT COUNT(*) FROM essentials.districts WHERE district_type='STATE_LOWER' AND state='md'` returns 71 (unchanged — no parent rows added)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration apply → live Supabase DB | Migrations are direct DDL/DML to production; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-01 | Tampering | Migration file integrity | mitigate | File is checked into git in `C:/EV-Accounts/backend/migrations/`; reviewed by author before apply; idempotent guards prevent partial-apply corruption |
| T-93-01-02 | Repudiation | Migration ledger | mitigate | Migration name `272_md_legislative_chambers` recorded by mcp__supabase-local__apply_migration; STATE.md migration counter updated |
| T-93-01-03 | Denial of Service | Pre-flight assertions on wrong DB state | accept | Pre-flight assertions RAISE EXCEPTION on bad state — this is the intended fail-fast behavior |
</threat_model>

<verification>
After migration apply, run all SQL queries listed in `<verify>` and `<acceptance_criteria>`. Confirm idempotent re-run produces no new rows. Update STATE.md `Next migration: 273`.
</verification>

<success_criteria>
- File `C:/EV-Accounts/backend/migrations/272_md_legislative_chambers.sql` exists and follows the 269 pattern exactly
- Migration applied to production via mcp__supabase-local__apply_migration without error
- 2 new rows exist in essentials.chambers under State of Maryland government_id
- Idempotent: re-apply inserts 0 additional rows
- STATE.md `Next migration` advances to 273
</success_criteria>

<output>
Create `.planning/phases/93-md-legislature-federal-officials/93-01-SUMMARY.md` when done with:
- Migration file path
- Apply timestamp
- Final chamber row count (must be 2)
- Government_id resolved for State of Maryland
- Next migration counter advanced to 273
</output>
