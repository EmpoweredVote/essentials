---
phase: 222-collin-county-stances-evidence-only-compass-research-one-pol
plan: 04
type: execute
wave: 4
depends_on: ["222-03"]
files_modified:
  - "[EV-Accounts] C:/EV-Accounts/backend/migrations/<live-next-number>_222_plano_mckinney_gapfill_stances.sql (NEW — number assigned live)"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md (APPEND)"
autonomous: false
requirements: [COLLIN-STANCE-01, COLLIN-STANCE-02]
must_haves:
  truths:
    - "The gap list is re-derived LIVE at execution, not taken from the 222-01 numbers, because plan 222-02's Class A deletions change it (Downs drops to 1 topic held, Tu to 4, Muns to 5, Quintanilla to 4). The plan states the ~93-slot figure as a pre-deletion estimate superseded at execution (COLLIN-STANCE-01)"
    - "Only (politician_id, topic_id) pairs with NO existing answer row are written; pairs that already hold a stance are left byte-identical (D-07)"
    - "A pair deleted by 222-02 is eligible for a NEW evidence-backed stance here, but only on genuine new evidence — re-writing a deleted chair at the same value with no new sourcing is forbidden"
    - "All 15 people (Plano's 8 + McKinney's 7) are attempted across all 11 topics, one politician at a time (D-03)"
    - "Both cities are expected 0/N on `growth-and-development` and `healthcare`, and McKinney additionally expected 0/7 on `civil-rights`, `taxes`, and `local-immigration` — the plan treats a high blank rate on these topics as a correct, successful outcome, not a research failure"
    - "Every applied stance is a whole integer 1-5 with a paired `politician_context` row carrying reasoning that cites a specific dated action, quote, or vote plus at least one real, fetched source URL (D-04, D-05)"
    - "Every attempted-but-unsourced (person, topic) pair is appended to `222-CONFIRMED-BLANK.md` (D-08)"
  artifacts:
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
      provides: "The Plano + McKinney topic-gap-fill section of the per-person blank register (D-08)"
      contains: "## Plano + McKinney topic-gap fill"
  key_links:
    - "Stances attach by politician_id taken verbatim from the live-supplied gap list — never by name match, and never by geo_id (there is no unique geo_id)"
    - "topic_id values come from the 11 canonical UUIDs verified live in 222-01; question and answer wording comes from 222-RESEARCH.md section B, which overrides the researcher agent file for housing, taxes, and healthcare"
  prohibitions:
    - "MUST NOT infer or assign any stance from party affiliation, endorsement, donor history, or pattern-matching to another official in the same city — only an explicit, on-topic, cited statement, recorded vote, or questionnaire answer may set a chair"
    - "MUST NOT assign a chair when the evidence is suggestive-but-not-explicit, and MUST NOT default to a middle value to avoid leaving a blank — a blank spoke is the correct, successful outcome"
    - "MUST NOT record a source URL that was not actually fetched and read, or that does not contain the cited quote, vote, or questionnaire answer"
    - "MUST NOT attribute a stance to an officeholder on the basis of a same-named person in a different city or state; the source must confirm the Texas city before the evidence is used"
    - "MUST NOT name, imply, or rely on a politician's party in reasoning text — party is never used for inference and never displayed"
    - "MUST NOT modify or re-reason any pre-existing stance row that 222-02 did not delete"
---

<objective>
Fill the unanswered compass topics for **Plano**'s 8 and **McKinney**'s 7 seated officeholders — evidence-only, one politician at a time, across all 11 canonical topics — so the two largest cities in scope stop reading as "covered" while each currently holds only 3–6 of 11 topics per person. This is an operator-approved narrow exception to D-07 (which otherwise leaves the already-stanced cohort untouched), covering exactly these two cities.

Purpose: at the 222-01 Task 3 blocking checkpoint the operator declined to close this plan as a McKinney no-op and instead asked Plano and McKinney to be double-checked, since both were stanced in an early pass before this phase's evidence bar was written down. The resulting audit (`222-01-INTEGRITY-AUDIT.md`) found that neither city is actually complete — Plano holds 51 of 88 possible topic slots unfilled, McKinney 42 of 77 — and assigned the gap-fill to this plan.
Output: one AUDIT-ONLY gap-fill migration in the EV-Accounts repo, appended blank-register entries for every attempted-but-unsourced pair, and applied production rows.
</objective>

## Artifacts this phase produces

Phase 222 (all plans) produces data + planning artifacts only. No application code, no schema change, no packages.

- **[EV-Accounts repo]** `C:/EV-Accounts/backend/migrations/<N>_222_<slug>.sql` — one numbered, git-committed, **AUDIT-ONLY** (NOT registered in `schema_migrations`) migration per research/remediation plan. Numbers assigned LIVE. This plan owns `<N>_222_plano_mckinney_gapfill_stances.sql`.
- **[essentials repo]** `.planning/phases/222-.../222-01-WORKLIST.md` (222-01), `222-01-INTEGRITY-AUDIT.md` (222-01) — the pre-deletion planning snapshot this plan supersedes with a live re-derivation — `222-CONFIRMED-BLANK.md` (created 222-01, appended by every research/remediation plan), `222-NN-SUMMARY.md` per plan.
- **[essentials repo]** `.planning/REQUIREMENTS.md` — COLLIN-STANCE-01/02 + traceability (222-01).
- **[essentials repo]** `src/lib/coverage.js` — `hasContext: true` reconcile (222-18 only).

## Environment Constraints (read before executing)

These are hard facts about the execution environment discovered during 222-01. They govern how every task below must be written and executed — the wave-1 executor stalled on some of these before they were written down, so read this before starting Task 1:

1. **No DB access in this session for building the gap list, and none for applying.** `mcp__supabase-local__execute_sql` is NOT bound in subagent/executor sessions. The live per-person, per-topic gap list this plan researches against is supplied by the orchestrator at execution start (re-derived fresh — see Task 1) — no task in this plan may query the DB directly to build or verify that list itself, and the migration apply is the Task 3 operator checkpoint, never this session.
2. **This plan DOES require a web-capable agent.** Task 1 needs WebSearch/WebFetch against Ballotpedia, VOTE411, Community Impact, Star Local Media, council minutes, and official city bios (D-05). `gsd-executor` itself has no such binding, so Task 1 must be dispatched to the `politician-stance-researcher` subagent, or a `general-purpose` subagent seeded with the same methodology if that project-scoped agent does not resolve — never attempted directly by the orchestrating executor.
3. **Migrations live in the separate `C:/EV-Accounts` repo** (branch `master`), are **AUDIT-ONLY** (never registered in `schema_migrations`), and numbers are assigned LIVE at execution. The highest on-disk prefix was **1415** on 2026-07-25 → next free **1416**, but this plan must re-check the directory listing at its own execution time — 222-02's remediation migration and any concurrent milestone work may have already claimed numbers in between.
4. **Every migration is a single BEGIN/COMMIT transaction.**
5. **Join on `politician_id`.** There is no unique constraint on `geo_id`.
6. **Party affiliation must never appear in reasoning text and is never displayed.**
7. **Blank-register contract is (person, topic) granularity.** A person may legitimately have some topics land in this plan's applied migration and other topics land in this plan's register entries in the very same research pass — that is expected, not a defect, per the 222-01 operator's granularity ruling.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-INTEGRITY-AUDIT.md
</context>

## Multi-Source Coverage Audit

| Source | Confirms | Verified |
|---|---|---|
| `222-01-INTEGRITY-AUDIT.md` "Plano + McKinney topic-gap detail" | The pre-deletion per-person topic holdings for all 15 people, the two city-wide zero-coverage topics, and the explicit caveat that 222-02's deletions shrink several per-person counts before this plan runs | 2026-07-25, orchestrator, run against production |
| `222-01-WORKLIST.md` "Operator decisions required" §1 | The operator's 2026-07-25 re-scope of this plan from "McKinney, 0 un-stanced, no-op" to a Plano + McKinney topic-gap fill, approved at the 222-01 Task 3 blocking checkpoint | 2026-07-25, operator approval — **SOURCE for this plan's re-scoped shape** |
| `222-RESEARCH.md` §B | Verbatim question/answer wording for all 11 topics, overriding the researcher agent's embedded housing/taxes/healthcare scale text | Verified live 2026-07-25, no divergence found |
| `221-CONFIRMED-BLANK.md` | The register-entry format this plan's blank-register section mirrors | Precedent, Phase 221 |

<tasks>

<task type="auto">
  <name>Task 1: Research the live Plano + McKinney gap list, one politician at a time</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-INTEGRITY-AUDIT.md` — the "Plano + McKinney topic-gap detail" section is this task's starting point, but read its caveat carefully: the ~93-slot figure and the per-person held-topic counts it quotes are a **pre-deletion snapshot**, explicitly superseded once 222-02 runs.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the Plano and McKinney government entries confirming 0 un-stanced but not-complete status, and the 222-02/222-04 assignment table.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B is the SOURCE OF TRUTH for the verbatim question and five answer options of all 11 topics. It OVERRIDES the embedded scale text in the researcher agent file for `housing`, `taxes`, and `healthcare` (Pitfall 1). Also read Pitfall 2 (homonym trap).
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — reuse its evidence hierarchy, quote-selection gates, inversion-trap table, and de-identification protocol. Ignore its housing/taxes/healthcare scale text.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md` — D-03, D-04, D-05, D-07, D-08.
  </read_first>
  <files>(no file writes this task — research output is held for Task 2)</files>
  <action>
    First, re-derive the live gap list rather than trusting the audit's pre-deletion ~93-slot figure (Environment Constraint 1). This plan `depends_on: ["222-03"]` and runs in wave 4, after 222-02 (wave 2) has applied its remediation deletions — so the orchestrator supplies, at this task's start, each of Plano's 8 and McKinney's 7 officeholders' CURRENT per-topic holdings (post-222-02-deletion). Expect this to differ from the audit's pre-deletion snapshot exactly as the audit's own caveat predicts: Chris Krupa Downs holding 1 topic (not 3), Maria Tu holding 4 (not 5), John B. Muns holding 5 (not 6), Vidal Quintanilla holding 4 (not 6). Treat the audit's per-person counts as superseded planning color, never as this task's operational gap list.

    Work strictly ONE POLITICIAN AT A TIME across all 15 people (Plano's 8, then McKinney's 7, or whatever order the orchestrator's live-supplied list presents them in), never two research passes concurrently — parallel fan-outs burn API quota and this is a standing user rule (D-03). This task REQUIRES a web-capable agent (Environment Constraint 2): dispatch the `politician-stance-researcher` subagent if it resolves from this session, or a single `general-purpose` subagent seeded with the methodology read from `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` otherwise. `gsd-executor` itself has no WebSearch/WebFetch and must not attempt this research directly.

    For each person, attempt every one of the 11 canonical topics that the live-supplied gap list shows as unfilled for that person — do not re-attempt a topic that already holds a stance (D-07); a pre-existing pair must be left byte-identical by this plan. Pass the full verbatim question plus five answer options for each attempted topic from RESEARCH.md section B — do not let the agent fall back to its own embedded wording. Use WebFetch as the primary tool; a light WebSearch budget of a few calls per person is allowed only to discover a URL when direct fetch patterns fail.

    Plano source order (D-05 acceptable types): the official City of Plano council pages and searchable council agendas/minutes for recorded votes and motions; Ballotpedia individual candidate pages; VOTE411 and League of Women Voters of Collin County questionnaires; Community Impact Newspaper and Star Local Media candidate Q&As and coverage; the officeholder's campaign or official site.

    McKinney source order (D-05 acceptable types): mckinneytexas.org council member pages and the searchable agenda and minutes archive for recorded votes and motions; Ballotpedia individual candidate pages; Community Impact Newspaper's McKinney bureau candidate Q&As and coverage; VOTE411 and League of Women Voters of Collin County; Star Local Media and DFW broadcast coverage; the officeholder's campaign site.

    Confirm the page names Plano or McKinney, Texas before accepting any source (Pitfall 2) — a same-named person in another city or state is not evidence.

    Assign a chair 1 through 5 ONLY where a cited source shows an explicit, on-topic position: a stated stance, a recorded council vote or motion, or a candidate-questionnaire answer (D-04). Suggestive-but-not-explicit is a blank. No party inference, no pattern inference from a colleague. Expect and treat as a normal, correct outcome a high blank rate on: both cities' `growth-and-development` and `healthcare` topics (0/N today), and additionally McKinney's `civil-rights`, `taxes`, and `local-immigration` (0/7 today) — these are known, expected zero-coverage topics per the audit, not evidence the research step is failing.

    A (politician_id, topic_id) pair that 222-02 deleted is eligible for a NEW evidence-backed stance here, but ONLY on genuine new evidence found during this task's own research pass — do not re-write a deleted chair at the same value on the same (lack of) sourcing that got it deleted in the first place; that would simply re-commit the defect 222-02 just removed.

    Record per person, in a structured working note carried into Task 2: politician_id, full_name, office title, city, and for each attempted topic either {value 1-5, one-to-three-sentence reasoning citing a specific dated action/quote/vote, exact source URLs actually fetched} or `BLANK`; plus the list of source URLs actually checked for that person.

    A person who yields zero newly-sourced topics is a normal, correct outcome. Do not lower the evidence bar to raise a count.
  </action>
  <verify>
    <automated>The working note lists one entry per person (15 total: 8 Plano + 7 McKinney), each carrying results for every topic the live-supplied gap list marked as unfilled for that person, with no already-held topic re-attempted. Entry count equals 15.</automated>
  </verify>
  <acceptance_criteria>
    - The live gap list was supplied by the orchestrator (post-222-02) at this task's start, not assumed from the audit's pre-deletion ~93-slot figure.
    - All 15 people (Plano's 8 + McKinney's 7) were attempted across every topic their live gap list shows as unfilled.
    - No topic already holding a pre-existing stance was re-attempted or overwritten.
    - Every assigned value is a whole integer 1-5 with reasoning naming a specific dated action, quote, vote, or questionnaire answer and at least one fetched source URL.
    - Every person carries a sources-checked list regardless of hit rate.
    - Research passes were sequential — the working note records them one at a time.
    - Any newly-sourced stance on a topic 222-02 deleted cites genuinely new evidence, never a repeat of the deleted reasoning.
  </acceptance_criteria>
  <done>Every one of Plano's 8 and McKinney's 7 officeholders has been individually attempted against their live-derived gap-list topics under the strict evidence bar.</done>
</task>

<task type="auto">
  <name>Task 2: Author the gap-fill migration, self-audit, append the blank register, and commit</name>
  <read_first>
    - The Task 1 working note.
    - `C:/EV-Accounts/backend/migrations/1307_oro_valley_mayor_stances.sql` — the exact answers-plus-context upsert shape to copy, including the AUDIT-ONLY header comment and dollar-quoting for reasoning text.
    - `C:/EV-Accounts/backend/migrations/998_horacio_ortiz_stances.sql` — the alternate topic_key-joined style, useful for a multi-topic single-politician block.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format to mirror.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to (note the "Phase 222 integrity remediation" section 222-02 may have already added — append after it, do not overwrite it).
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — for the live next-available migration number starting point; re-verify live per Environment Constraint 3.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_plano_mckinney_gapfill_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Run the self-audit gate before finalizing anything for commit: confirm every candidate stance is a whole integer 1-5; every answer has a matching context row; every context row's `sources` array has at least one real, fetched URL; no reasoning contains party language; and no reasoning reads as or resembles "no record found" / "no public statement" — that exact pattern is what created the Class A defects 222-02 just deleted, and a value must never be written alongside an admission that no evidence exists. Any candidate that fails this self-audit is demoted to a blank-register bullet, not written to the migration.

    Re-read the live next-available migration number from `222-01-WORKLIST.md`, confirm it is still free by listing `C:/EV-Accounts/backend/migrations` (222-02's remediation migration, and any concurrent wave-3 Frisco migration or other milestone work, may have already claimed numbers), and take the next free number. Create `C:/EV-Accounts/backend/migrations/<N>_222_plano_mckinney_gapfill_stances.sql` in the EV-Accounts repo — this file does NOT live in the essentials repo.

    The file opens with a header comment marking it AUDIT-ONLY and stating it is deliberately not registered in `schema_migrations`, names Phase 222 and "Plano + McKinney topic-gap fill", and records the date. The body is a single BEGIN/COMMIT transaction. For each surviving (politician, topic) pair, write the pair of upserts: an insert into `inform.politician_answers (politician_id, topic_id, value)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value`, immediately followed by an insert into `inform.politician_context (politician_id, topic_id, reasoning, sources)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources`. The `ON CONFLICT DO UPDATE` shape exists so re-running this migration itself is idempotent — it must NOT be used as license to include any pair that already held a stance before this plan ran; the pairs written must be exactly the ones Task 1 identified as having no prior row. Never write an answers row without its matching context row. Dollar-quote all reasoning text, using a distinct tag on delimiter collision. Values are whole numbers 1 through 5. topic_id values are the canonical UUIDs verified in 222-01. If a city yields zero newly-sourced stances across every person, do not create an empty INSERT block for it — record that outcome and proceed.

    Then append a `## Plano + McKinney topic-gap fill (2026-07-25)` section to `222-CONFIRMED-BLANK.md` in the essentials repo, AFTER any existing "Phase 222 integrity remediation" section 222-02 already added (append, never overwrite). Under this heading, add a `### Plano` and a `### McKinney` subsection, each mirroring the 221 format: a one-line `Evidence:` note naming the source families actually checked for that city, then one bullet per attempted-but-unsourced (person, topic) pair carrying full_name, office title, politician_id, and topic_key. A person may appear here for some topics and simultaneously hold new applied stances from this same migration for other topics — that is expected, not a defect, per the 222-01 operator's per-(person,topic) granularity ruling. Update the running `Count:` line at the top of the register.

    Commit both repos separately and locally. In EV-Accounts use `git -C "C:/EV-Accounts" add backend/migrations/<N>_222_plano_mckinney_gapfill_stances.sql` then `git -C "C:/EV-Accounts" commit` with a message naming phase 222 and "Plano + McKinney topic-gap fill" — never `cd` into that repo. Do NOT run `git push` against EV-Accounts: a push deploys accounts-api to Render and carries every commit already ahead. In essentials, commit the register change normally.

    Report: pairs attempted, pairs sourced/applied, pairs blanked, per city — and confirm applied-plus-blanked equals attempted for both Plano and McKinney.
  </action>
  <verify>
    <automated>grep -q "^## Plano + McKinney topic-gap fill" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - The self-audit gate ran before commit: every applied stance is a whole 1-5 value with a matching context row, at least one fetched source URL, no party language, and no "no record found" pattern; failures were demoted to the register instead of written to the migration.
    - The migration file (if any stance survived) exists under `C:/EV-Accounts/backend/migrations/` with a numeric prefix taken live this session, an AUDIT-ONLY header, and a single BEGIN/COMMIT transaction.
    - Every inserted (politician_id, topic_id) pair had NO prior row — no pre-existing pair is present anywhere in the migration.
    - Every `inform.politician_answers` insert is immediately followed by a matching `inform.politician_context` insert for the same politician_id and topic_id.
    - Every value literal is a whole number 1 through 5; no fractional value appears.
    - Every `sources` array has at least one URL.
    - `222-CONFIRMED-BLANK.md` has a `## Plano + McKinney topic-gap fill (2026-07-25)` section with `### Plano` and `### McKinney` subsections, and for each city the applied-pair count plus register-bullet count equals the attempted-pair count.
    - `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending — the file is committed, not dangling.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>The gap-fill migration is authored, self-audited, committed locally in EV-Accounts, and the Plano + McKinney topic-gap-fill section of the blank register is written and committed in essentials. Nothing has been applied to production yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator applies the gap-fill migration</name>
  <read_first>
    - `C:/EV-Accounts/backend/migrations/<N>_222_plano_mckinney_gapfill_stances.sql` — the SQL to apply.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the evidence-integrity gate query.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the "Plano + McKinney topic-gap fill" section, for the attempted/applied/blanked reconcile.
  </read_first>
  <what-built>
    A committed, AUDIT-ONLY Plano + McKinney topic-gap-fill migration plus the matching blank-register section. The executor has no Supabase MCP access, so the SQL is authored and committed but NOT yet applied. Until it is applied, the repository looks complete while production is unchanged — this checkpoint exists specifically to close that gap.
  </what-built>
  <action>
    Halt here and hand control to the operator. Do NOT attempt the apply step from this session — gsd-executor has no Supabase MCP binding, so the SQL is committed but production is still unchanged until the operator runs it. Present the how-to-verify steps below verbatim, wait for the resume signal, then record the operator's reported apply confirmation and gate results in this plan's SUMMARY before the phase advances. If any check fails, do not mark this plan done.
  </action>
  <how-to-verify>
    1. Spot-check 3 of the applied stances against their cited source URLs — confirm the quote, vote, or questionnaire answer is genuinely present and on-topic for that specific compass topic.
    2. Confirm no pre-existing (politician_id, topic_id) pair was overwritten — diff the migration's pairs against Plano's and McKinney's live per-person holdings recorded in `222-01-INTEGRITY-AUDIT.md` (adjusted for any 222-02 deletions already applied).
    3. Confirm the blank-register bullet count plus the migration's applied-pair count equals the total attempted-pair count reported in this plan's SUMMARY, for both Plano and McKinney.
    4. Apply the migration via `mcp__supabase-local__execute_sql` (this is production — the write is live). Do NOT register it in `schema_migrations`.
    5. Run the evidence-integrity gate from `222-VALIDATION.md` scoped to Plano's and McKinney's politician_ids: it must return zero rows.
    6. Confirm coverage moved: re-query Plano's and McKinney's per-person topic counts and confirm each increased from its pre-plan baseline (3-6 of 11) without exceeding 11 for anyone.
  </how-to-verify>
  <acceptance_criteria>
    - The migration has been applied to production via `mcp__supabase-local__execute_sql` and the apply is confirmed, not assumed.
    - 3 spot-checked stances are confirmed against their cited source URLs.
    - No pre-existing (politician_id, topic_id) pair was overwritten.
    - Blank count plus applied count equals attempted pairs for both cities.
    - The evidence-integrity gate returns zero rows for Plano's and McKinney's politician_ids.
    - Per-person topic coverage measurably increased for both cities without exceeding 11 for anyone.
    - No row was written to `schema_migrations`.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live and the gate returns zero rows, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped web text → SQL migration | Quotes and URLs harvested from the open web are embedded into a statement executed against production |
| research judgment → public civic profile | An assigned chair becomes a public claim about a real, named person |
| pre-deletion audit snapshot → stale gap scope | The audit's ~93-slot figure is planning color from before 222-02's deletions land; using it as-is would mis-scope this plan's research |
| authored SQL → production DB | The executor cannot apply; a file on disk is not a live row |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-04 | Tampering | migration SQL text | high | mitigate | Dollar-quote all scraped reasoning text; never concatenate scraped text into a bare SQL string |
| T-222-05 | Repudiation | fabricated or mismatched source URL | critical | mitigate | Self-audit gate before commit; operator spot-checks 3 stances against cited URLs at the checkpoint |
| T-222-06 | Spoofing | wrong-person attribution (homonym) | high | mitigate | Confirm the Texas city named in the source before accepting evidence (Pitfall 2); prohibition against same-named officeholders in other cities/states |
| T-222-07 | Denial of Service | false-positive completion (authored but unapplied SQL) | high | mitigate | Blocking apply-and-verify checkpoint; the gate runs against production, not the file |
| T-222-09 | Tampering | stale gap-list scope (pre-222-02 snapshot used instead of live post-deletion state) | medium | mitigate | Task 1 requires the orchestrator's live-supplied, post-222-02 gap list rather than the audit's pre-deletion figures |
| T-222-10 | Tampering | accidental overwrite of a pre-existing stance (D-07 violation) | high | mitigate | Task 1 only attempts topics the live gap list marks unfilled; Task 2's acceptance criteria require every inserted pair to have had no prior row; operator diffs pairs against the pre-plan baseline at the checkpoint |
| T-222-SC | Tampering | package installs | low | accept | This plan installs no packages; nothing to audit |
</threat_model>

<verification>
- All 15 people (Plano's 8 + McKinney's 7) individually attempted, sequentially, against their live-derived gap-list topics.
- Every applied stance has a whole 1-5 value, a specific dated reasoning, and at least one re-fetched source URL; no pre-existing pair touched.
- Evidence-integrity gate returns zero rows for Plano's and McKinney's politician_ids after apply.
- Blank count plus applied count equals attempted pairs for both cities.
- EV-Accounts migration committed locally and not pushed.
</verification>

<success_criteria>
Plano's and McKinney's officeholders show materially more compass coverage than the 3-6-of-11 baseline, with every new chair evidence-cited and every remaining gap honestly blank-registered — applied live, gated at zero defects, and with no pre-existing stance touched.
</success_criteria>

<output>
Create `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-04-SUMMARY.md` when done — record pairs attempted, applied, and blanked per city, the migration number used, any self-audit demotions, and the operator's checkpoint confirmation.
</output>
