---
phase: 106-va-compass-stances
plan: 04
type: execute
wave: 4
depends_on:
  - 106-03
files_modified:
  - C:/EV-Accounts/backend/migrations/329_warner_stances.sql
autonomous: true
requirements:
  - VA-STANCES-02
must_haves:
  truths:
    - "Mark Warner has >=15 stance rows in inform.politician_answers after migration 329 applies"
    - "Every Warner context row has >=1 URL in sources (100% citation rate per D-10)"
    - "No defaulted/neutral fallback rows exist"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/329_warner_stances.sql"
      provides: "Per-individual stance migration for US Senator Mark Warner"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "329_warner_stances.sql"
      to: "essentials.politicians (external_id=-400080)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research Mark Warner's (US Senator from Virginia, external_id=-400080) public record across all currently-active compass topics, write migration 329, apply, and verify.

Purpose: Begins VA-STANCES-02 (federal senators). Warner has served in the US Senate since 2009 (17+ years) and was VA Governor 2002-2006 — extensive voting record. Target >=15 stances per ROADMAP; aim 18-21+ per D-09.

Output: `329_warner_stances.sql` applied; >=15 stance rows for Warner; all paired with cited context rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/106-va-compass-stances/106-CONTEXT.md
@.planning/phases/106-va-compass-stances/106-PATTERNS.md
@.planning/phases/106-va-compass-stances/106-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Warner UUID, then research all active topics</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-01..D-10)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120)
    - C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql (lines 76-87 for Warner external_id=-400080)
  </read_first>
  <action>
    Step A: Run via mcp__supabase-local__execute_sql:
      SELECT id FROM essentials.politicians WHERE external_id = -400080;
    Record as ${WARNER_UUID}.

    Step B: Run ONE web-research subagent (per D-08). Warner's documented Senate record covers nearly every federal topic:
      - Chair, Senate Intelligence Committee (2021-2024): ai-regulation, misinformation (TikTok regulation, deepfake bills)
      - Banking Committee senior member: economic-development, taxes, campaign-finance
      - Coal-country VA Governor → moderated fossil-fuels position; climate-change: voted for IRA
      - Healthcare: voted for ACA, opposed repeal; medicare/aid: supported
      - Foreign policy votes: ukraine-support (strong yes), tariffs (mixed on China)
      - Voting-rights: cosponsor of Freedom to Vote Act
      - Immigration: 2013 Gang of Eight comprehensive reform supporter, deportation positions
      - Abortion: pro-choice voting record, voted yes Women's Health Protection Act
      - Civil-rights, same-sex-marriage (voted for Respect for Marriage Act)
      - Social-security: defender, no privatization
      - Religious-freedom, trans-athletes, school-vouchers: Senate votes available
      - Judicial-* topics: Senate confirmation votes on SCOTUS nominees provide evidence
      - Local-only topics (city-sanitation, rent-regulation, residential-zoning, jail-capacity, local-environment, local-immigration): likely no Senate record — OMIT

    Target: 18-21+ stances per D-09; ROADMAP minimum >=15.

    Use sources like ontheissues.org/Senate/Mark_Warner.htm, ballotpedia.org, govtrack.us, congress.gov votes, warner.senate.gov press releases.

    Save research as `.planning/phases/106-va-compass-stances/_tmp-warner-research.json`.
  </action>
  <verify>
    <automated>
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-warner-research.json"
      ((Get-Content "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-warner-research.json" | ConvertFrom-Json).Count -ge 15)
    </automated>
  </verify>
  <done>
    - Warner UUID resolved
    - Research JSON file exists with >=15 entries, every entry cited
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 329_warner_stances.sql matching 282 pattern exactly</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251)
    - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql (VA exec analog)
    - .planning/phases/106-va-compass-stances/_tmp-warner-research.json
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/329_warner_stances.sql` following exact 326 structure with substitutions:
      - Header: "Migration 329: Mark Warner Stances" / "US Senator from Virginia"
      - Section header: "Mark Warner"
      - politician_id literal: ${WARNER_UUID}
      - One INSERT pair per topic in _tmp-warner-research.json

    Same hard rules: single BEGIN/COMMIT, paired INSERTs (>=15 each), float values, dollar quoting, ARRAY cast, topic reference block.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/329_warner_stances.sql"
      (Select-String -Path "C:/EV-Accounts/backend/migrations/329_warner_stances.sql" -Pattern '^BEGIN;$' -SimpleMatch).Count -eq 1
      (Select-String -Path "C:/EV-Accounts/backend/migrations/329_warner_stances.sql" -Pattern '^COMMIT;$' -SimpleMatch).Count -eq 1
      $a = (Select-String -Path "C:/EV-Accounts/backend/migrations/329_warner_stances.sql" -Pattern 'INSERT INTO inform.politician_answers').Count
      $c = (Select-String -Path "C:/EV-Accounts/backend/migrations/329_warner_stances.sql" -Pattern 'INSERT INTO inform.politician_context').Count
      ($a -eq $c) -and ($a -ge 15)
    </automated>
  </verify>
  <done>
    - Migration file exists with valid structure
    - Equal answers/context INSERTs (>=15 each)
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 329 + verify</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/329_warner_stances.sql
  </read_first>
  <action>
    Step A: Read migration 329 SQL, pass to mcp__supabase-local__execute_sql.

    Step B (verification):
      SELECT
        (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -400080)) AS answer_count,
        (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -400080) AND pc.politician_id IS NULL) AS unpaired,
        (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -400080) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)) AS uncited;
    Required: answer_count >= 15, unpaired = 0, uncited = 0.

    Step C: Delete `.planning/phases/106-va-compass-stances/_tmp-warner-research.json`.
  </action>
  <verify>
    <automated>
      Via mcp__supabase-local__execute_sql: answer_count >= 15, unpaired = 0, uncited = 0.
    </automated>
  </verify>
  <done>
    - Migration 329 applied
    - Warner stance count >= 15
    - 100% citation rate
  </done>
</task>

</tasks>

<verification>
- mcp__supabase-local__execute_sql confirms Warner stance count >= 15
- All paired and cited
</verification>

<success_criteria>
- VA-STANCES-02 advanced (Warner portion)
- ROADMAP Phase 106 success criterion #2 partially met (>=15 for Warner)
</success_criteria>

<output>
Create `.planning/phases/106-va-compass-stances/106-04-SUMMARY.md` documenting Warner stance count, omitted topics, source domain summary.
</output>
