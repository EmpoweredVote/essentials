---
phase: 194-city-of-tucson-deep-seed
plan: 05
type: execute
wave: 5
depends_on: ["194-04"]
files_modified:
  - src/lib/buildingImages.js
  - src/lib/coverage.js
autonomous: false
requirements: [BANR-01, TUC-01]
must_haves:
  truths:
    - "A licensed downtown-Tucson streetscape banner (real photo, no AI, no aerial) is processed to the banner spec and uploaded to Storage at cities/tucson.jpg"
    - "buildingImages.js CURATED_LOCAL has a state-scoped 'tucson' entry pointing at the uploaded banner"
    - "coverage.js gets a BRAND-NEW Arizona block in COVERAGE_STATES (none exists today — Pima County lives in the separate COVERAGE_COUNTIES array, which is NOT touched) with a DB-honest 'Tucson' chip (hasContext:true, since stances landed in Plan 04) browsing geo_id 0477000"
    - "The Tucson banner is visually distinct from Pima County's Catalinas/Saguaro landscape banner and the AZ-state Phoenix skyline"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "CURATED_LOCAL 'tucson' → uploaded banner URL"
      contains: "tucson"
    - path: "src/lib/coverage.js"
      provides: "NEW Arizona COVERAGE_STATES block with a 'Tucson' area, browseGovernmentList ['0477000'], hasContext:true"
      contains: "Arizona"
  key_links:
    - from: "coverage.js COVERAGE_STATES Arizona → 'Tucson'"
      to: "buildingImages.js CURATED_LOCAL 'tucson'"
      via: "browse_label 'Tucson' → representingCity → getBuildingImages substring match"
      pattern: "tucson"
    - from: "buildingImages.js 'tucson'"
      to: "Storage cities/tucson.jpg"
      via: "src URL (state-scoped 'AZ')"
      pattern: "cities/tucson.jpg"
---

<objective>
Give the City of Tucson its licensed community banner (BANR-01) and its DB-honest coverage chip. Source
a real downtown-Tucson streetscape photo ONE AT A TIME (Congress St / historic district, ideally with a
Sentinel Peak / Catalina backdrop; no AI, no aerial — D-07), process it to the banner spec via
`scripts/banners/process_banner.py`, upload to Storage at `cities/tucson.jpg` via `upload_banner.py`,
wire it into `src/lib/buildingImages.js` CURATED_LOCAL as a single state-scoped `'tucson'` entry, and
surface the City of Tucson in `src/lib/coverage.js`.

CRITICAL DELTA from Pima (Pitfall 6): `coverage.js` has NO Arizona block in `COVERAGE_STATES` today —
grep confirms zero `name: 'Arizona'` matches. Pima County (Phase 193) lives in the SEPARATE
`COVERAGE_COUNTIES` array, which this phase does NOT touch. Tucson is the FIRST Arizona CITY, so add a
brand-new `{ name: 'Arizona', abbrev: 'AZ', areas: [...] }` block modeled on the existing Nevada block —
do NOT search for a non-existent Arizona block to append to, and do NOT add Tucson to COVERAGE_COUNTIES.

Purpose: Satisfies ROADMAP success criteria #4 (licensed banner sourced/processed/uploaded/wired) and
#5 (City of Tucson surfaced with a DB-honest chip). The banner must read "Tucson the city" (downtown
streetscape) and stay visually distinct from Pima County's Catalinas/Saguaro landscape banner and the
live AZ-state Phoenix skyline (Pima deliberately took the natural scenery so Tucson could take the
streetscape). This city unit is the flagship template for the 4 suburb deep-seeds (195-198).
Output: uploaded `cities/tucson.jpg` + edited `buildingImages.js` + `coverage.js` in the `essentials` repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md
@.planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md

<execution_note>
Frontend files (`src/lib/buildingImages.js`, `src/lib/coverage.js`) live in the CURRENT `essentials`
repo (commit here, not `C:/EV-Accounts`). The banner SOURCING + processing + Storage upload are
ORCHESTRATOR-RUN (executor has no network/Storage access): the inline orchestrator sources the photo
one-at-a-time, runs the banner toolchain, and uploads. Per project convention
(`feedback_banner_source_one_at_a_time`, `feedback_banner_prefer_photo_over_gradient`,
`feedback_banner_show_state_shot_on_conflict`) banner sourcing is sequential, prefers a real photo, and
shows the AZ-state Phoenix + Pima Catalinas banners alongside during review if there is any risk of
visual conflict. The CDN URL is deterministic (`cities/tucson.jpg`) so the frontend wiring can be
authored regardless of upload order.
</execution_note>

<interfaces>
- Banner spec: process via `scripts/banners/process_banner.py` (`--url <src> --output <path>
  --vertical-anchor` tuned per shot), upload via `scripts/banners/upload_banner.py --file <path>
  --dest cities/tucson.jpg` (needs SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
  Destination tier is `cities/` (banner TYPE, not government type). Storage is no-cache → overwrite =
  instant refresh.
- CDN URL: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/tucson.jpg
- buildingImages.js CURATED_LOCAL: add `tucson: { state: 'AZ', src: '<CDN URL>' }` (single-variant —
  'tucson' has no same-named-city collision in the covered set, so NOT the array-of-{state,src} form)
  with an attribution comment (title | author | license) above it, matching the file's existing
  convention (e.g. the single-key 'pima county' entry). getBuildingImages() needs NO code change.
- coverage.js: NEW block in COVERAGE_STATES modeled on Nevada's:
  `{ name: 'Arizona', abbrev: 'AZ', areas: [{ label: 'Tucson', browseGovernmentList: ['0477000'],
  browseStateAbbrev: 'AZ', hasContext: true }] }`. hasContext:true is honest because Plan 04 seeded ≥1
  stance row. Do NOT touch COVERAGE_COUNTIES (Pima County lives there — unrelated). File is not in
  strict A-Z order; placement is cosmetic.
- Shortlisted banner candidates (RESEARCH Tertiary — visual selection still done at execute time):
  `Tucson - Downtown (7176890768).jpg` (5760×3840, CC BY 2.0, Bill Morrow — strongest), plus
  `Downtown Tucson 2013.JPG`, `FOX Theater AZ Tucson NRHP 03000905.jpg`, others in
  Category:Downtown Tucson / Category:Congress Street (Tucson).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Source + process + upload the Tucson downtown-streetscape banner (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Sources Tertiary — the shortlisted Wikimedia downtown-Tucson candidates; the one-at-a-time sourcing pass makes the final visual selection; § user_constraints D-07)
    - .planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md (lines 380-388 — the process_banner.py / upload_banner.py commands + cities/<slug>.jpg destination convention)
    - C:/Transparent Motivations/essentials/scripts/banners/process_banner.py (the banner spec + --vertical-anchor)
    - C:/Transparent Motivations/essentials/scripts/banners/upload_banner.py (the Storage upload + --dest convention)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. The orchestrator sources a licensed downtown-Tucson streetscape photo ONE AT A TIME, processes it to the banner spec, uploads to cities/tucson.jpg, and the operator visually approves the banner before wiring. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A licensed downtown-Tucson streetscape banner for the City of Tucson — the first AZ CITY-tier
    banner. RESEARCH shortlisted candidates (strongest: `Tucson - Downtown (7176890768).jpg`, CC BY 2.0)
    but visual selection is done at execute time. The orchestrator performs the one-at-a-time sourcing
    pass against commons.wikimedia.org (Category:Downtown Tucson / Category:Congress Street (Tucson,
    Arizona)), processes via process_banner.py (--vertical-anchor tuned per shot), and uploads via
    upload_banner.py to cities/tucson.jpg. Human visual QA is required because "real photo, no AI, no
    aerial, distinct from Pima's Catalinas + Phoenix skyline" is a judgment a row-count cannot make.
  </what-built>
  <how-to-verify>
    1. The orchestrator sources ONE candidate photo (licensed CC/public-domain, real photo, NOT
       AI-generated, NOT aerial, a downtown streetscape — Congress St / historic district preferred),
       records title | author | license, runs
       `python scripts/banners/process_banner.py --url <src> --output /tmp/tucson.jpg
       --vertical-anchor 0.4` (tune anchor per shot), then
       `python scripts/banners/upload_banner.py --file /tmp/tucson.jpg --dest cities/tucson.jpg`
       (SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
    2. Operator visually confirms: reads as Tucson the CITY (downtown streetscape), a real photo (no AI
       artifacts, no aerial/drone shot), and visually DISTINCT from BOTH the Pima County
       Catalinas/Saguaro landscape banner (already live) AND the AZ-state Phoenix skyline (already live)
       — show both alongside during review per feedback_banner_show_state_shot_on_conflict.
    3. If unusable, the orchestrator re-sources another candidate ONE AT A TIME (prefer a real photo;
       gradient only if truly nothing usable exists).
  </how-to-verify>
  <acceptance_criteria>
    - `curl -sI https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/tucson.jpg` returns HTTP 200.
    - The uploaded image matches the banner spec (process_banner.py output dimensions) — PIL/curl-download dimension check.
    - Operator approves: real photo, downtown-Tucson streetscape subject, no AI, no aerial, distinct from the Pima Catalinas banner AND the AZ-state Phoenix skyline.
    - Title | author | license recorded in 194-05-SUMMARY.md for the attribution comment.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the operator confirms the uploaded banner reads as downtown Tucson, is a real non-aerial photo, and is distinct from the Pima County + AZ-state banners — or describe what to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner into buildingImages.js + surface Tucson via a NEW Arizona coverage.js block</name>
  <files>src/lib/buildingImages.js, src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md (lines 355-432 — the exact CURATED_LOCAL 'tucson' single-variant entry + attribution comment, and the NEW Arizona COVERAGE_STATES block modeled on Nevada; the Pitfall 6 warning that no Arizona block exists and Pima is in COVERAGE_COUNTIES; getBuildingImages needs NO change)
    - C:/Transparent Motivations/essentials/src/lib/buildingImages.js (the CURATED_LOCAL object + the single-key 'pima county' entry to mirror; getBuildingImages substring/state-scoping logic)
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the COVERAGE_STATES array + the Nevada block to mirror; confirm NO Arizona block exists; the separate COVERAGE_COUNTIES array holding Pima County — do NOT touch it)
    - .planning/phases/194-city-of-tucson-deep-seed/194-05-SUMMARY.md (the banner title | author | license from Task 1 for the attribution comment)
  </read_first>
  <action>
    In `src/lib/buildingImages.js`, add ONE entry to the CURATED_LOCAL object:
    `tucson: { state: 'AZ', src: '<CDN URL for cities/tucson.jpg>' }` (single-variant, NOT the
    array-of-{state,src} form — 'tucson' has no same-named-city collision), with an attribution comment
    above it (title | author | license from Task 1) matching the file's existing convention. Note in the
    comment it is a downtown streetscape, deliberately distinct from the Pima County landscape banner
    and the AZ-state Phoenix skyline. Make NO change to getBuildingImages() — its substring match +
    state-scoping already handle the label.

    In `src/lib/coverage.js`, add a BRAND-NEW block to the COVERAGE_STATES array (NOT COVERAGE_COUNTIES):
    `{ name: 'Arizona', abbrev: 'AZ', areas: [{ label: 'Tucson', browseGovernmentList: ['0477000'],
    browseStateAbbrev: 'AZ', hasContext: true }] }`, modeled exactly on the existing Nevada block. No
    Arizona block exists today (Pitfall 6) — this is the first AZ city entry; do NOT append Tucson to
    COVERAGE_COUNTIES (where Pima County lives) and do NOT broaden parseCityFromAddress. hasContext:true
    is honest because Plan 04 seeded ≥1 stance row (this plan depends on Plan 04). Placement in the
    array is cosmetic (file is not strictly A-Z).

    Commit both files to the `essentials` repo via git.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qiE "tucson:\s*\{" src/lib/buildingImages.js && grep -qF "cities/tucson.jpg" src/lib/buildingImages.js && grep -qF "name: 'Arizona'" src/lib/coverage.js && grep -qF "'0477000'" src/lib/coverage.js && node --input-type=module -e "import('./src/lib/coverage.js').then(()=>console.log('coverage-ok')).catch(e=>{console.error(e);process.exit(1)})" && node --input-type=module -e "import('./src/lib/buildingImages.js').then(()=>console.log('bi-ok')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has a `tucson` key with `state: 'AZ'` and src = the cities/tucson.jpg CDN URL, plus an attribution comment (title | author | license).
    - coverage.js COVERAGE_STATES has a NEW `{ name: 'Arizona', abbrev: 'AZ', areas: [{ label: 'Tucson', browseGovernmentList: ['0477000'], browseStateAbbrev: 'AZ', hasContext: true }] }` block.
    - COVERAGE_COUNTIES is NOT modified (Pima County entry untouched); no Tucson entry added there.
    - Both files parse cleanly via `node --input-type=module -e "import(...)"` (exit 0) — matches the Phase 162/193 coverage.js parse-check convention (no dedicated test file exists).
    - getBuildingImages() and parseCityFromAddress unchanged.
    - `npx vitest run src/lib/buildingImages.test.js` stays green (established non-gap; run as a spot-check).
    - Both files committed to the `essentials` repo.
  </acceptance_criteria>
  <done>Tucson banner wired into CURATED_LOCAL and surfaced via a brand-new Arizona COVERAGE_STATES block with a DB-honest hasContext:true chip; both frontend files parse, buildingImages.test.js green, both committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wikimedia (remote) → banner pipeline | Untrusted remote image bytes enter process_banner.py over HTTP |
| banner pipeline → Storage | Processed banner crosses into the production politician_photos bucket |
| frontend edits → live app | coverage.js / buildingImages.js changes ship to Render on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-BAN | Tampering | remote banner image fetch | mitigate | process_banner.py re-encodes to JPEG (strips embedded payloads); operator visual QA confirms a real non-AI non-aerial photo before wiring |
| T-194-LIC | Repudiation | banner license provenance | mitigate | Title \| author \| license recorded in the SUMMARY + the attribution comment; only CC/public-domain sources used |
| T-194-COVLIE | Tampering | dishonest coverage chip | mitigate | hasContext:true only set because Plan 04 landed ≥1 stance row (depends_on 194-04); node parse-check confirms valid module |
| T-194-COVBLK | Tampering | wrong coverage array (append to COUNTIES / duplicate AZ block) | mitigate | Add a NEW COVERAGE_STATES Arizona block (Pitfall 6); grep confirms `name: 'Arizona'` newly present; COVERAGE_COUNTIES Pima entry untouched |
| T-194-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; never in a committed frontend file |
| T-194-SC | Tampering | package installs | accept | No new installs — reuses installed PIL/requests banner toolchain; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- cities/tucson.jpg returns HTTP 200 at the banner spec; operator-approved as a real, distinct downtown-Tucson streetscape.
- CURATED_LOCAL 'tucson' → banner URL; NEW Arizona COVERAGE_STATES block with a 'Tucson' hasContext:true chip; COVERAGE_COUNTIES untouched.
- Both frontend files parse, buildingImages.test.js green, both committed.
</verification>

<success_criteria>
ROADMAP #4 (licensed banner sourced/processed/uploaded/wired) and #5 (City of Tucson surfaced with a
DB-honest chip in a new Arizona block) are TRUE.
</success_criteria>

<output>
Create `.planning/phases/194-city-of-tucson-deep-seed/194-05-SUMMARY.md` when done — include the banner
title | author | license and the CDN URL.
</output>
</content>
