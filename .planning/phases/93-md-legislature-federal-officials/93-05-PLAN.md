---
phase: 93-md-legislature-federal-officials
plan: 05
type: execute
wave: 3
depends_on:
  - 93-02
files_modified:
  - scripts/md_senators_headshots.py
autonomous: true
# Note: MD-GOV-06 (100% headshot coverage) is OWNED by Phase 94, not Phase 93.
# This plan attempts best-effort ingestion only (floor >= 1 per D-05); Phase 94 enforces coverage.
requirements: []
tags:
  - md
  - senate
  - headshots
  - best-effort

must_haves:
  truths:
    - "Best-effort headshot ingestion attempted for all 47 MD state senators seeded by Plan 93-02 per D-05"
    - "Each successfully sourced headshot is cropped to 4:5 then resized to exactly 600x750 (Lanczos, q90) per LOCATION-ONBOARDING.md and feedback_headshot_image_sizing.md"
    - "Each processed headshot is uploaded to Supabase Storage bucket 'politician-headshots' under prefix 'md-senators/' per D-06"
    - "Each successful upload inserts an idempotent essentials.politician_images row (type='default', photo_license='public_domain')"
    - "Senators whose headshot cannot be sourced or processed are SKIPPED (logged) — script does NOT halt; Phase 94 closes gaps per D-05"
    - "Script is idempotent — re-running uploads zero additional files and inserts zero additional rows for previously-processed senators"
  artifacts:
    - path: "scripts/md_senators_headshots.py"
      provides: "Python ingestion script for 47 MD senator headshots from mgaleg.maryland.gov"
      contains: "OFFICIALS"
  key_links:
    - from: "scripts/md_senators_headshots.py OFFICIALS list"
      to: "essentials.politicians (external_id BETWEEN -2410047 AND -2410001)"
      via: "politician_id UUID resolved from DB at script construction time"
      pattern: "external_id mapping to UUID"
    - from: "scripts/md_senators_headshots.py download step"
      to: "https://mgaleg.maryland.gov/2026RS/images/[lastname][01|02|nn].jpg"
      via: "HTTPS GET, no auth required"
      pattern: "lowercase lastname + optional 2-digit suffix"
    - from: "scripts/md_senators_headshots.py upload step"
      to: "Supabase Storage bucket 'politician-headshots'"
      via: "Storage REST API with SERVICE_ROLE key"
      pattern: "PUT /storage/v1/object/politician-headshots/md-senators/{politician_id}.jpg"
    - from: "scripts/md_senators_headshots.py DB insert step"
      to: "essentials.politician_images"
      via: "psycopg2 INSERT ... WHERE NOT EXISTS"
      pattern: "type='default', photo_license='public_domain'"
---

<objective>
Build `scripts/md_senators_headshots.py` to source, process, and ingest headshots for the 47 Maryland state senators seeded in Plan 93-02. This is **best-effort inline ingestion per D-05/D-06**: senators whose headshot cannot be sourced or processed are skipped (logged) — the script proceeds without halting. Phase 94 enforces 100% coverage; Phase 93 completes when ingestion has been *attempted*.

Purpose: Implements MD-GOV-06 for the senate chamber. Establishes the same Maryland headshot script template used by Plan 93-06 (delegates). Follows `scripts/md_executives_headshots.py` exactly except for the OFFICIALS list (47 senators), TMP_DIR path, and source URL pattern (`mgaleg.maryland.gov/2026RS/images/[lastname][nn].jpg`).

Output: 1 Python script + best-effort headshot rows in `essentials.politician_images` for as many of the 47 senators as the script can successfully process.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/93-md-legislature-federal-officials/93-CONTEXT.md
@.planning/phases/93-md-legislature-federal-officials/93-RESEARCH.md
@.planning/phases/93-md-legislature-federal-officials/93-PATTERNS.md
@.planning/phases/93-md-legislature-federal-officials/93-02-SUMMARY.md

# Canonical analog — exact template (same state, same Supabase, same PIL flow, same psycopg2 pattern)
@scripts/md_executives_headshots.py

# Project memory on headshot processing rules
@$HOME/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_headshot_image_sizing.md
@$HOME/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_headshot_cropping.md
@$HOME/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_headshot_resize_no_distort.md
@$HOME/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_headshot_no_graphics.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Resolve UUIDs + source URLs for all 47 MD senators</name>
  <files>(no file changes — discovery only; intermediate notes feed Task 2 OFFICIALS list)</files>
  <read_first>
    - .planning/phases/93-md-legislature-federal-officials/93-RESEARCH.md (Current Incumbents senator table; Headshot URL Pattern section)
    - .planning/phases/93-md-legislature-federal-officials/93-PATTERNS.md (scripts/md_senate_headshots.py section — OFFICIALS list structure + mgaleg URL pattern)
    - .planning/phases/93-md-legislature-federal-officials/93-02-SUMMARY.md (confirms 47 senators seeded with external_id range -2410047..-2410001)
    - scripts/md_executives_headshots.py (lines 80-116 — OFFICIALS list shape)
  </read_first>
  <behavior>
    - Query essentials.politicians for all 47 rows in external_id range -2410047..-2410001, returning id (UUID), full_name, last_name
    - For each senator, derive the candidate mgaleg.maryland.gov image URL: `https://mgaleg.maryland.gov/2026RS/images/[lowercase lastname][optional 2-digit suffix].jpg`
      * First attempt: lowercase lastname with no suffix (e.g., 'jennings.jpg')
      * Disambiguation: if the no-suffix URL 404s, try suffix '01' (e.g., 'ferguson01.jpg'), then '02', etc., up to '03'
      * For hyphenated/multi-word last names, strip non-alphabetic characters (e.g., 'Lewis Young' → 'lewisyoung', 'St. John' → 'stjohn')
    - For mgaleg roster (https://mgaleg.maryland.gov/mgawebsite/Members/Index/senate) the canonical image URL is rendered inline on the senator's roster row — if HEAD probing fails, fetch the senator's member page to scrape the actual `/2026RS/images/...jpg` URL as fallback
    - Record one URL per senator. Senators with no resolvable URL (after all retries) remain in the OFFICIALS list with `source_url=None` so the runtime loop logs "SKIP: no source URL" and moves on — Phase 94 will close those gaps
    - Sanity check: politicians table returns exactly 47 rows; no UUID is null
  </behavior>
  <action>
    Use mcp__supabase-local (production DB) to fetch the 47 senator rows:
    ```sql
    SELECT id, external_id, full_name, last_name
    FROM essentials.politicians
    WHERE external_id BETWEEN -2410047 AND -2410001
    ORDER BY external_id DESC;  -- SD-01 McKay first
    ```
    Expect exactly 47 rows from Plan 93-02. Record each (UUID, external_id, full_name, last_name) tuple.

    For each senator, derive the candidate mgaleg.maryland.gov image URL:
    1. Lowercase the last name and strip non-alphabetic characters (apostrophes, hyphens, spaces, periods): e.g., "McKay" → "mckay", "Lewis Young" → "lewisyoung", "Hershey, Jr." → "hershey".
    2. Attempt URL `https://mgaleg.maryland.gov/2026RS/images/{normalized}.jpg` first.
    3. If that URL returns HTTP 404 (HEAD probe via requests.head, follow redirects, 5s timeout), try `{normalized}01.jpg`, then `{normalized}02.jpg`, up to `{normalized}03.jpg`.
    4. If no candidate URL resolves to HTTP 200, fall back to fetching the senator's mgaleg member roster page (https://mgaleg.maryland.gov/mgawebsite/Members/Index/senate) once, parsing the HTML for the senator's row, and extracting the actual `<img src="/2026RS/images/...">` attribute.
    5. If every probe fails, record `source_url=None` for that senator — best-effort per D-05, the script later logs SKIP and continues.

    Record the (politician_id UUID, external_id, full_name, source_url) tuples in a working list ready to be embedded directly into Task 2's OFFICIALS constant in `scripts/md_senators_headshots.py`. Do not write the script in this task — only compute the data.

    Document in the Task 2 OFFICIALS list any URLs that required suffix disambiguation (e.g., `ferguson01.jpg` rather than `ferguson.jpg`) so future maintainers can re-derive them.
  </action>
  <verify>
    <automated>
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -2410047 AND -2410001;
-- Expected: 47 (confirms Plan 93-02 prerequisites)
    </automated>
    <automated>
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -2410047 AND -2410001 AND id IS NULL;
-- Expected: 0 (no null UUIDs)
    </automated>
  </verify>
  <done>
    47 (politician_id, external_id, full_name, source_url) tuples resolved. Each source_url either points to a verified HTTP 200 mgaleg.maryland.gov image, or is null (best-effort SKIP). Tuple list ready to embed in Task 2.
  </done>
  <acceptance_criteria>
    - SQL `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2410047 AND -2410001` returns 47
    - All 47 politician UUIDs collected
    - For each senator: either a candidate source_url has been HEAD-probed and returned HTTP 200, or source_url is explicitly null (best-effort skip)
    - Tuple list documents any suffix disambiguation needed (e.g., 'ferguson01.jpg' vs 'ferguson.jpg')
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create scripts/md_senators_headshots.py from md_executives_headshots.py template</name>
  <files>scripts/md_senators_headshots.py</files>
  <read_first>
    - scripts/md_senators_headshots.py (target — confirm not present)
    - scripts/md_executives_headshots.py (exact analog — copy structure verbatim)
    - .planning/phases/93-md-legislature-federal-officials/93-PATTERNS.md (scripts/md_senate_headshots.py section — lists every block to copy from the analog with line numbers, plus required TMP_DIR change and OFFICIALS list shape)
    - .planning/phases/93-md-legislature-federal-officials/93-RESEARCH.md (Headshot URL Pattern; Architecture Patterns System Architecture Diagram; Standard Stack section confirming PIL + psycopg2 already installed)
  </read_first>
  <behavior>
    - Script header docstring identifies it as the senator headshot ingestion for Phase 93, references D-05/D-06 (best-effort, skip on failure)
    - Constants copied verbatim from md_executives_headshots.py:
      * SUPABASE_URL, SERVICE_KEY (with JWT-shape validation), BUCKET='politician-headshots', STORAGE_BASE, TARGET_W=600, TARGET_H=750
      * Only differences: TMP_DIR = Path("C:/Transparent Motivations/essentials/scripts/tmp_md_senators_headshots"), STORAGE_PREFIX = 'md-senators'
    - OFFICIALS list contains 47 tuples (politician_id_uuid_str, external_id_int, full_name, source_url_or_None) from Task 1
    - Functions copied verbatim from md_executives_headshots.py (do not modify): download_image, crop_and_resize (4:5 crop then 600x750 Lanczos resize @ q90), process_image, upload_to_storage, check_image_exists, insert_politician_image
    - main() loop iterates OFFICIALS, and for each entry:
      1. If source_url is None → log "SKIP: no source URL — Phase 94 will close" and continue
      2. If check_image_exists(politician_id) returns True → log "SKIP: already ingested" and continue
      3. Else attempt download → process → upload → insert_politician_image
      4. Any exception during the per-senator block is caught, logged as "SKIP: <reason>", and execution continues to the next senator (best-effort)
    - End-of-run summary prints counts: processed, skipped (no URL), skipped (already exists), failed (download/process/upload error), total OFFICIALS=47
    - Script exits 0 even if individual senators failed (best-effort) — only fails if SERVICE_KEY missing/invalid or DB connection unreachable
  </behavior>
  <action>
    Copy `scripts/md_executives_headshots.py` to `scripts/md_senators_headshots.py` and apply these targeted edits per PATTERNS.md:

    1. **Docstring (lines 1-26):** Replace with senator-specific header. Reference Phase 93 Plan 05, requirement MD-GOV-06, decisions D-05 (best-effort) and D-06 (split by chamber). Cite source: https://mgaleg.maryland.gov/2026RS/images/[lastname][nn].jpg.

    2. **Imports + constants (lines 49-66):** Keep verbatim except:
       - `TMP_DIR = Path("C:/Transparent Motivations/essentials/scripts/tmp_md_senators_headshots")`
       - Add `STORAGE_PREFIX = "md-senators"` constant (used in storage upload path)

    3. **OFFICIALS list (lines 80-116):** Replace with the 47-tuple list from Task 1. Tuple shape: `(politician_id_str, external_id_int, full_name_str, source_url_or_None)`. Preserve display order SD-01..SD-47.

    4. **Function bodies (lines 122-278):** Copy verbatim. The `download_image`, `crop_and_resize`, `process_image`, `upload_to_storage`, `check_image_exists`, and `insert_politician_image` functions need zero changes — they are state-agnostic.

    5. **Storage upload path:** Verify the upload_to_storage function uses `f"{STORAGE_BASE}/{BUCKET}/{STORAGE_PREFIX}/{politician_id}.jpg"` for the destination object key. If md_executives_headshots.py hardcoded `md-executives` as the prefix, parametrize it to STORAGE_PREFIX. The local file path remains under TMP_DIR.

    6. **main() loop (lines 283-364):** Adapt per the behavior block. Critical change from analog: each per-senator block must be wrapped in `try/except Exception as e` so that a single failure logs a SKIP and continues — Phase 94 closes any gaps per D-05. Treat `source_url is None` and `check_image_exists True` as informational skips (not errors).

    7. **Final summary:** Print breakdown `OFFICIALS=47, processed=X, skipped_no_url=Y, skipped_exists=Z, failed=W`. The script exits 0 unless SERVICE_KEY is missing/invalid (raise SystemExit with descriptive message) or DB connection fails fast at startup.

    Confirm `TMP_DIR` is created via `Path.mkdir(parents=True, exist_ok=True)` early in main(). Do NOT delete TMP_DIR contents on success — leave processed JPEGs in place for human spot-check before Phase 94.

    Verify the storage upload object path matches: `politician-headshots/md-senators/{politician_id}.jpg` (NOT a generic `md-headshots` or `headshots` prefix).

    Do NOT run the script yet — Task 3 handles execution.
  </action>
  <verify>
    <automated>
# Confirm file exists and key strings present
python -c "import pathlib; p=pathlib.Path('scripts/md_senators_headshots.py'); content=p.read_text(encoding='utf-8'); assert 'md-senators' in content, 'missing storage prefix'; assert 'tmp_md_senators_headshots' in content, 'missing TMP_DIR'; assert content.count('(\"') > 40 or content.count(\"('\") > 40 or content.count('(') > 50, 'OFFICIALS list too short'; print('OK: scripts/md_senators_headshots.py structural check passed')"
    </automated>
    <automated>
# Confirm Python syntax is valid (no execution side-effects)
python -m py_compile scripts/md_senators_headshots.py
echo "Syntax OK"
    </automated>
  </verify>
  <done>
    File `scripts/md_senators_headshots.py` exists; passes `python -m py_compile`; OFFICIALS list contains 47 tuples; STORAGE_PREFIX='md-senators'; TMP_DIR points to scripts/tmp_md_senators_headshots; per-senator try/except wraps the processing block.
  </done>
  <acceptance_criteria>
    - File `scripts/md_senators_headshots.py` exists
    - `python -m py_compile scripts/md_senators_headshots.py` exits 0
    - File contains text 'md-senators' (storage prefix)
    - File contains text 'tmp_md_senators_headshots' (TMP_DIR)
    - File contains text 'politician-headshots' (BUCKET)
    - File contains text 'mgaleg.maryland.gov' or imports OFFICIALS list whose URLs contain it
    - File contains text 'public_domain' (photo_license)
    - File contains text "type = 'default'" or `type='default'` (politician_images type)
    - File contains text 'TARGET_W' and the value 600 AND 'TARGET_H' and the value 750
    - File contains text 'Lanczos' or 'LANCZOS' (PIL resampling)
    - File contains text 'try' and 'except' in the per-senator loop (best-effort skip-on-failure)
    - OFFICIALS list has at least 47 tuple entries (count of `(` characters at column 1 indentation, or count tuples directly)
    - File does NOT contain text 'md-executives' as STORAGE_PREFIX literal (would be a copy/paste bug)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run scripts/md_senators_headshots.py and verify best-effort outcome</name>
  <files>(no file changes — runs the script; outputs go to Supabase Storage + DB)</files>
  <read_first>
    - scripts/md_senators_headshots.py (the script produced by Task 2)
    - .planning/phases/93-md-legislature-federal-officials/93-CONTEXT.md (D-05 — best-effort, Phase 94 closes gaps)
    - .planning/phases/93-md-legislature-federal-officials/93-RESEARCH.md (Validation Architecture section — MD-GOV-06 verification)
  </read_first>
  <behavior>
    - Execute `python scripts/md_senators_headshots.py` (or `py scripts/md_senators_headshots.py` on Windows) with SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY environment variables set
    - Script downloads, processes (crop 4:5 then resize 600×750 Lanczos q90), and uploads each sourcable senator headshot
    - Script logs per-senator outcome: processed | skipped (no URL) | skipped (already exists) | failed (with reason)
    - End-of-run summary prints counts; script exits 0
    - Supabase Storage bucket `politician-headshots` now contains files at path `md-senators/{politician_id}.jpg` for each processed senator
    - essentials.politician_images table has one new row per processed senator (type='default', photo_license='public_domain', url pointing to storage)
    - At least 1 senator processed successfully (sanity floor — if zero, something is fundamentally broken)
    - Re-run is idempotent: 2nd execution prints "SKIP: already ingested" for every previously-processed senator and exits 0 with 0 new uploads/inserts
  </behavior>
  <action>
    Step 1: Ensure env vars are set in the shell session:
    ```
    $env:SUPABASE_URL = "<production URL>"   # from .env or 1Password
    $env:SUPABASE_SERVICE_ROLE_KEY = "<service role JWT>"
    $env:SUPABASE_DB_URL = "<postgres connection string>"
    ```
    If any are missing, surface a clear error to the operator BEFORE attempting the run.

    Step 2: Execute `python scripts/md_senators_headshots.py`. Capture stdout/stderr. The run is best-effort — per-senator failures are SKIP-logged, not fatal.

    Step 3: Read the end-of-run summary. Record counts: processed, skipped_no_url, skipped_exists, failed. Total must equal 47.

    Step 4: Run the verification SQL queries listed in `<verify>` to confirm DB state matches script output.

    Step 5: Spot-check Supabase Storage via the dashboard or REST API (`GET /storage/v1/object/list/politician-headshots?prefix=md-senators/`) — confirm at least 1 file matches each processed senator's politician_id.

    Step 6: Re-run the script once. Confirm 2nd run logs "SKIP: already ingested" for every previously-processed senator and inserts zero new rows. This proves idempotency.

    Step 7: Document any senators in the SUMMARY.md whose headshot could NOT be sourced/processed. These names feed Phase 94's gap-closure scope.
  </action>
  <verify>
    <automated>
SELECT COUNT(*) AS senator_image_count
FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id BETWEEN -2410047 AND -2410001
  AND pi.type = 'default';
-- Expected: >= 1 (best-effort floor; ideally close to 47)
    </automated>
    <automated>
-- Confirm photo_license is public_domain on every new row (no nulls, no wrong values)
SELECT COUNT(*) FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id BETWEEN -2410047 AND -2410001
  AND pi.type = 'default'
  AND (pi.photo_license IS NULL OR pi.photo_license <> 'public_domain');
-- Expected: 0
    </automated>
    <automated>
-- Confirm URL points at Supabase Storage bucket politician-headshots and md-senators prefix
SELECT COUNT(*) FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id BETWEEN -2410047 AND -2410001
  AND pi.type = 'default'
  AND pi.url NOT LIKE '%politician-headshots/md-senators/%';
-- Expected: 0 (every URL contains the correct storage path)
    </automated>
    <automated>
-- Confirm no duplicate rows per senator (idempotency check)
SELECT politician_id, COUNT(*) AS row_count
FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id BETWEEN -2410047 AND -2410001
  AND pi.type = 'default'
GROUP BY politician_id
HAVING COUNT(*) > 1;
-- Expected: 0 rows
    </automated>
  </verify>
  <done>
    Script ran to completion exit 0; at least 1 senator processed; politician_images rows match script output; storage bucket contains corresponding files; idempotent re-run produced zero new rows; remaining-gap list documented in SUMMARY for Phase 94.
  </done>
  <acceptance_criteria>
    - `python scripts/md_senators_headshots.py` exits with code 0
    - End-of-run summary line printed showing processed/skipped/failed counts that sum to 47
    - SQL: senator_image_count is >= 1 (best-effort floor)
    - SQL: 0 rows with NULL or wrong photo_license among new senator images
    - SQL: 0 rows whose url does NOT contain `politician-headshots/md-senators/`
    - SQL: 0 senators with > 1 type='default' image row (no dupes)
    - Re-running script produces 0 net new rows (idempotency)
    - SUMMARY.md lists the names of any senators whose headshot could not be sourced — empty list is acceptable
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mgaleg.maryland.gov → local TMP_DIR | External HTTP fetch; images may be missing, redirected, or replaced |
| Local TMP_DIR → Supabase Storage | Authenticated PUT via SERVICE_ROLE key; mutates live storage |
| Local script → essentials.politician_images | psycopg2 direct connection; INSERT with WHERE NOT EXISTS guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-05-01 | Tampering | Source URL substitution (mgaleg image swapped) | accept | Public-domain official roster; if image is wrong, Phase 94 manual review catches it; risk is cosmetic, not security |
| T-93-05-02 | Information Disclosure | SERVICE_ROLE key in env | mitigate | Env var only — never committed to repo; JWT shape validated at script startup before any network call |
| T-93-05-03 | Tampering | Wrong politician_id mapping (UUID drift between Task 1 and Task 3) | mitigate | Task 1 resolves UUIDs from production DB at script-build time; Task 2 embeds them as literals in OFFICIALS so they cannot drift mid-run |
| T-93-05-04 | Denial of Service | Network failure on individual senator download | mitigate | Per-senator try/except — single download failure logs SKIP and continues; Phase 94 closes the gap |
| T-93-05-05 | Tampering | Image not resized correctly (wrong aspect ratio, missing crop) | mitigate | crop_and_resize copied verbatim from md_executives_headshots.py which already passed feedback_headshot_image_sizing / cropping / resize_no_distort checks |
| T-93-05-06 | Tampering | Duplicate politician_images rows on re-run | mitigate | INSERT ... WHERE NOT EXISTS guard; check_image_exists short-circuits before download; idempotency verified by Task 3 acceptance criteria |
| T-93-05-07 | Repudiation | Headshot with superimposed graphics ("Re-Elect" banners) per feedback_headshot_no_graphics | accept | mgaleg.maryland.gov roster images are official portraits with no campaign overlays; if a violation is found, Phase 94 manual review replaces it |
</threat_model>

<verification>
After all three tasks: rerun the verification SQL queries (counts, license validation, URL prefix validation, no-dupes). Confirm the SUMMARY.md lists per-senator outcome and documents any unsourced senators for Phase 94 follow-up.
</verification>

<success_criteria>
- File `scripts/md_senators_headshots.py` exists with OFFICIALS list of 47 senators
- Script passes `python -m py_compile`
- Script runs to completion exit 0
- At least 1 senator headshot successfully ingested (best-effort floor per D-05; ideal is 47)
- All ingested rows: type='default', photo_license='public_domain', url contains `politician-headshots/md-senators/`
- No duplicate type='default' rows per senator
- Idempotent re-run produces 0 new rows
- Any unsourced senators documented in SUMMARY.md for Phase 94 follow-up
</success_criteria>

<output>
Create `.planning/phases/93-md-legislature-federal-officials/93-05-SUMMARY.md` when done with:
- Script file path
- End-of-run summary counts (processed / skipped_no_url / skipped_exists / failed; total = 47)
- List of senator names whose headshot was NOT sourced (Phase 94 scope)
- 1-2 sentence note on any URL disambiguation patterns discovered (e.g., 'ferguson01.jpg' rather than 'ferguson.jpg')
- Confirmation of idempotent re-run
- Storage bucket path: politician-headshots/md-senators/{politician_id}.jpg
</output>
