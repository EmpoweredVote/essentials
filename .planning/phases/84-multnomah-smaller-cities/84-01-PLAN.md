---
phase: 84-multnomah-smaller-cities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql
  - C:/EV-Accounts/backend/scripts/smoke-multnomah-cities.ts
autonomous: true
requirements: [CITIES-01, CITIES-02, CITIES-03, CITIES-04, CITIES-05]
tags: [phase-84, multnomah, oregon, cities, government-seeding, routing]

must_haves:
  truths:
    - "Government rows for 5 cities exist in essentials.governments with type='LOCAL', state='OR', geo_id values 4131250, 4174850, 4124250, 4183950, 4146730 — implements CITIES-01..05 per D-01"
    - "5 'City Council' chambers exist (one per city, each linked to its government_id) — per D-07"
    - "10 districts exist (5 LOCAL_EXEC + 5 LOCAL), all state='or' (lowercase per D-11), labels follow pattern '{City} (Citywide)' for LOCAL_EXEC and '{City} (At-Large)' for LOCAL — per D-04, D-06"
    - "31 politicians seeded with party=NULL (per D-12) across external_id ranges -4131251..-4131257, -4174851..-4174857, -4124251..-4124257, -4183951..-4183955, -4146731..-4146735"
    - "31 offices exist with representing_state='OR' (uppercase per D-11); is_appointed_position=true on Wood Village Mayor (-4183951) and Maywood Park Mayor (-4146731) only; all 29 other offices is_appointed_position=false"
    - "All 31 politicians have non-null office_id after migration 246 back-fill UPDATE (per D-14)"
    - "Smoke test smoke-multnomah-cities.ts exits 0; all 5 centroid coordinates resolve to their expected G4110 geo_id; all 5 centroids return LOCAL + LOCAL_EXEC officials matching the seeded names"
    - "Section-split check returns 0 rows for the 5 G4110 geo_ids (each has both LOCAL and LOCAL_EXEC district rows under state='or')"
    - "Migration is idempotent — re-apply inserts 0 additional government/chamber/district/politician/office rows per D-13"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql"
      provides: "Single BEGIN/COMMIT transaction seeding 5 governments + 5 chambers + 10 districts + 31 politicians + 31 offices + office_id back-fill + ledger entry, per D-02"
      contains: "City of Gresham, Oregon, US"
    - path: "C:/EV-Accounts/backend/scripts/smoke-multnomah-cities.ts"
      provides: "Routing smoke test validating SC1 (G4110 match), SC2 (LOCAL/LOCAL_EXEC officials), SC3 (section-split) for 5 city centroids per D-03"
      exports: ["main"]
  key_links:
    - from: "essentials.offices.district_id (31 rows)"
      to: "essentials.districts.id (10 rows: 5 LOCAL_EXEC + 5 LOCAL across 5 geo_ids)"
      via: "FK join validated by post-verification DO block inside migration"
      pattern: "JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id IN ('4131250','4174850','4124250','4183950','4146730') AND d.state = 'or'"
    - from: "essentials.geofence_boundaries (5 G4110 rows from Phase 72, state='41')"
      to: "essentials.districts (10 new rows, state='or')"
      via: "geo_id JOIN in districtQueryText (routing query)"
      pattern: "JOIN essentials.districts d ON d.geo_id = gb.geo_id AND d.district_type IN ('LOCAL','LOCAL_EXEC')"
    - from: "essentials.politicians.office_id (31 rows)"
      to: "essentials.offices.id (31 rows)"
      via: "back-fill UPDATE at end of migration (per D-14)"
      pattern: "UPDATE essentials.politicians SET office_id = o.id ... WHERE p.external_id IN (<31 ids>)"
---

<objective>
Seed government bodies, City Council chambers, LOCAL_EXEC + LOCAL at-large districts, and all 31 elected officials (with offices) for the 5 incorporated Multnomah County cities outside Portland — Gresham (7), Troutdale (7), Fairview (7), Wood Village (5), Maywood Park (5) — via a single idempotent migration, then verify routing with a 5-coordinate smoke test.

Purpose: CITIES-01..05 each require a city council government body + elected officials seeded with offices linked correctly to the city's G4110 geo_id. Per D-01 and D-02, all 5 cities ship in one migration (246) inside one BEGIN/COMMIT transaction. Per D-03, one smoke test (smoke-multnomah-cities.ts) verifies G4110 boundary routing and LOCAL/LOCAL_EXEC officials return correctly for all 5 cities.

Output: Migration 246 applied to production Supabase via mcp__supabase-local (same pattern as Phase 83 migration 244); smoke test passes all 3 success-criteria classes (SC1 boundary, SC2 officials, SC3 section-split) across 5 cities.

Critical research override: CONTEXT.md D-05 said Gresham council was ward-based (X0013-X0018 MTFCCs). RESEARCH.md proves Gresham is at-large Position 1-6 (greshamoregon.gov + Charter Review Committee final report confirms no ward map adopted). This plan implements Gresham identically to the other 4 cities — single LOCAL district under geo_id='4131250'. No ward geofences are loaded. D-05 is OVERRIDDEN by research; D-06 pattern applies to all 5 cities.

Wood Village + Maywood Park Mayor exception: Both Mayors are council-selected (chosen by council from among elected councilors), not directly elected. Per RESEARCH.md Pattern 2, the Mayor office row gets is_appointed_position=true and the politician row gets is_appointed=true. The Mayor still uses district_type=LOCAL_EXEC (it is the executive seat regardless of how chosen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/84-multnomah-smaller-cities/84-CONTEXT.md
@.planning/phases/84-multnomah-smaller-cities/84-RESEARCH.md
@.planning/phases/84-multnomah-smaller-cities/84-PATTERNS.md
@.planning/phases/83-multnomah-county-government-routing/83-01-SUMMARY.md

# Source-of-truth migration analog (read verbatim — Phase 83 immediate predecessor)
@C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql

# Additional pattern sources
@C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql
@C:/EV-Accounts/backend/migrations/231_portland_officials.sql

# Smoke test analog (near-copy)
@C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts

# Routing query source (must mirror SQL in queryLocalOfficials)
@C:/EV-Accounts/backend/src/lib/essentialsService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 246 (5 city governments + chambers + districts + 31 officials + offices + back-fill)</name>
  <files>C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql (Phase 83 canonical analog — copy file header, pre-flight DO block, government INSERT, chamber INSERT, district INSERT, WITH ins_p CTE, office_id back-fill UPDATE, post-verification DO block, ledger entry, COMMIT structure verbatim and repeat 5x for cities)
    - C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql (Mayor LOCAL_EXEC district pattern with mtfcc=NULL and label '{City} (Citywide)')
    - C:/EV-Accounts/backend/migrations/231_portland_officials.sql (WITH ins_p CTE canonical source for politician + office atomic insert; office_id back-fill at lines 597-602)
    - .planning/phases/84-multnomah-smaller-cities/84-PATTERNS.md (full inline SQL templates for governments INSERT, chambers INSERT, LOCAL_EXEC + LOCAL district INSERT, WITH ins_p CTE for directly-elected Mayor, WITH ins_p CTE for council-appointed Mayor, councilor CTE, office_id back-fill, post-verification DO block, ledger entry)
    - .planning/phases/84-multnomah-smaller-cities/84-RESEARCH.md (Verified Incumbent Roster section for all 31 names + external_ids + titles; Critical Research Correction section for Gresham at-large override; Office Title Conventions table for per-city council title format)
    - .planning/phases/84-multnomah-smaller-cities/84-CONTEXT.md (D-01..D-14 locked decisions)
    - The file being created itself (verify it does not already exist before writing; if it does, halt — Phase 84 has not been started)
  </read_first>
  <action>
    Create file C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql. Implements CITIES-01..05 per D-01 (2-plan structure), D-02 (single migration 246 covering all 5 cities in one BEGIN/COMMIT), D-04 (LOCAL_EXEC for Mayors with geo_id matching city G4110), D-06 (LOCAL at-large for all councils — D-05 OVERRIDDEN by research), D-07 (Mayor + City Council per city), D-11 (state casing OR/or/OR), D-12 (party=NULL), D-13 (idempotency), D-14 (office_id back-fill).

    Structure (single BEGIN/COMMIT transaction; ledger entry version='246' at end):

    SECTION A — File header (8-12 line comment block):
    - "Migration 246: Multnomah smaller cities government + chambers + districts + officials + offices"
    - Purpose statement listing all 5 cities with geo_ids: Gresham '4131250', Troutdale '4174850', Fairview '4124250', Wood Village '4183950', Maywood Park '4146730'
    - Per city: 1 government + 1 chamber + 1 LOCAL_EXEC district + 1 LOCAL at-large district + N officials + N offices
    - Totals: 5 governments, 5 chambers, 10 districts, 31 politicians, 31 offices
    - CRITICAL constraints stated verbatim:
      * "slug is GENERATED ALWAYS on essentials.chambers — never include in INSERT column list"
      * "essentials.governments has NO unique constraint on geo_id — use WHERE NOT EXISTS guard"
      * "districts.state must be 'or' (lowercase) to match routing queries; governments.state='OR' uppercase; offices.representing_state='OR' uppercase (per D-11)"
      * "district_type='LOCAL_EXEC' for Mayor (per D-04); district_type='LOCAL' for council (per D-06); never 'COUNTY' or 'SCHOOL'"
      * "Gresham is at-large Position 1-6 (research override of CONTEXT.md D-05); NO ward geofences"

    SECTION B — BEGIN; followed by pre-flight DO block:
    - DO block: RAISE NOTICE if any of the 5 government names already exist (use IN clause covering all 5 names). Non-fatal; logs idempotent re-run notice only.

    SECTION C — Per-city seed block, repeated 5x in this order: GRESHAM, TROUTDALE, FAIRVIEW, WOOD VILLAGE, MAYWOOD PARK. Each city block contains:

    Step 1 — Government INSERT (per 244 lines 39-46 pattern):
      INSERT INTO essentials.governments (id, name, type, state, city, geo_id)
      SELECT gen_random_uuid(), 'City of {City}, Oregon, US', 'LOCAL', 'OR', '{City}', '{geo_id}'
      WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name = 'City of {City}, Oregon, US');
      Note: type='LOCAL' for city governments (NOT 'County' — that value is reserved for the Multnomah County row from Phase 83).

    Step 2 — Chamber INSERT (per 244 lines 53-63 pattern):
      INSERT INTO essentials.chambers (id, name, name_formal, government_id)
      SELECT gen_random_uuid(), 'City Council', '{City} City Council',
             (SELECT id FROM essentials.governments WHERE name = 'City of {City}, Oregon, US')
      WHERE NOT EXISTS (SELECT 1 FROM essentials.chambers WHERE name = 'City Council' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'City of {City}, Oregon, US'));
      Column list: id, name, name_formal, government_id. NEVER include slug.

    Step 3 — LOCAL_EXEC district INSERT (Mayor, citywide):
      INSERT INTO essentials.districts (id, district_type, state, geo_id, label, mtfcc)
      SELECT gen_random_uuid(), 'LOCAL_EXEC', 'or', '{geo_id}', '{City} (Citywide)', NULL
      WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE geo_id = '{geo_id}' AND district_type = 'LOCAL_EXEC' AND state = 'or');
      state MUST be lowercase 'or'; mtfcc MUST be NULL.

    Step 4 — LOCAL at-large district INSERT (council):
      INSERT INTO essentials.districts (id, district_type, state, geo_id, label, mtfcc)
      SELECT gen_random_uuid(), 'LOCAL', 'or', '{geo_id}', '{City} (At-Large)', NULL
      WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE geo_id = '{geo_id}' AND district_type = 'LOCAL' AND state = 'or');

    Steps 5..N — WITH ins_p CTE blocks (one per official). Per-city official roster:

    GRESHAM (7 officials, geo_id='4131250'):
    | external_id | full_name | first_name | last_name | title | is_appointed | is_appointed_position | district_type |
    |-4131251 | Travis Stovall | Travis | Stovall | Mayor | false | false | LOCAL_EXEC |
    |-4131252 | Kayla Brown | Kayla | Brown | Council Member (Position 1) | false | false | LOCAL |
    |-4131253 | Eddy Morales | Eddy | Morales | Council Member (Position 2) | false | false | LOCAL |
    |-4131254 | Cathy Keathley | Cathy | Keathley | Council Member (Position 3) | false | false | LOCAL |
    |-4131255 | Jerry Hinton | Jerry | Hinton | Council Member (Position 4) | false | false | LOCAL |
    |-4131256 | Sue Piazza | Sue | Piazza | Council Member (Position 5) | false | false | LOCAL |
    |-4131257 | Janine Gladfelter | Janine | Gladfelter | Council Member (Position 6) | false | false | LOCAL |

    TROUTDALE (7 officials, geo_id='4174850'):
    |-4174851 | David Ripma | David | Ripma | Mayor | false | false | LOCAL_EXEC |
    |-4174852 | Carol Allen | Carol | Allen | City Councilor | false | false | LOCAL |
    |-4174853 | Jesse Davidson | Jesse | Davidson | City Councilor | false | false | LOCAL |
    |-4174854 | John Leamy | John | Leamy | City Councilor | false | false | LOCAL |
    |-4174855 | Glenn White | Glenn | White | City Councilor | false | false | LOCAL |
    |-4174856 | Geoffrey Wunn | Geoffrey | Wunn | City Councilor | false | false | LOCAL |
    |-4174857 | Zach Andrews | Zach | Andrews | City Councilor | false | false | LOCAL |

    FAIRVIEW (7 officials, geo_id='4124250'):
    |-4124251 | Keith Kudrna | Keith | Kudrna | Mayor | false | false | LOCAL_EXEC |
    |-4124252 | Jeff Dennerline | Jeff | Dennerline | Council Member (Position 1) | false | false | LOCAL |
    |-4124253 | Steve Marker | Steve | Marker | Council Member (Position 2) | false | false | LOCAL |
    |-4124254 | E'an Todd | E'an | Todd | Council Member (Position 3) | false | false | LOCAL |
    |-4124255 | Jenni Weber | Jenni | Weber | Council Member (Position 4) | false | false | LOCAL |
    |-4124256 | Steve Owen | Steve | Owen | Council Member (Position 5) | false | false | LOCAL |
    |-4124257 | Paul Copeland | Paul | Copeland | Council Member (Position 6) | false | false | LOCAL |

    Special-case for E'an Todd (-4124254): the apostrophe in 'E'an' MUST be escaped as a doubled single-quote inside the SQL string literal — write the VALUES tuple as `'E''an Todd', 'E''an', 'Todd', ...`. Use standard ASCII apostrophe (U+0027), not the Unicode RIGHT SINGLE QUOTATION MARK U+2019. RESEARCH.md Pitfall 7 covers this.

    WOOD VILLAGE (5 officials, geo_id='4183950'):
    |-4183951 | Jairo Rios-Campos | Jairo | Rios-Campos | Mayor | true | true | LOCAL_EXEC |
    |-4183952 | Dara Tan | Dara | Tan | Council President | false | false | LOCAL |
    |-4183953 | John Miner | John | Miner | City Councilor | false | false | LOCAL |
    |-4183954 | Charlene Gothard | Charlene | Gothard | City Councilor | true | false | LOCAL |
    |-4183955 | Patricia Smith | Patricia | Smith | City Councilor | false | false | LOCAL |

    MAYWOOD PARK (5 officials, geo_id='4146730'):
    |-4146731 | Jim Akers | Jim | Akers | Mayor | true | true | LOCAL_EXEC |
    |-4146732 | Kevin Bussema | Kevin | Bussema | Council President | false | false | LOCAL |
    |-4146733 | Jeff Baltzell | Jeff | Baltzell | City Councilor | false | false | LOCAL |
    |-4146734 | Miriam Berman | Miriam | Berman | City Councilor | false | false | LOCAL |
    |-4146735 | Thomas Welander | Thomas | Welander | City Councilor | false | false | LOCAL |

    For each official, write a WITH ins_p CTE block (per 231_portland_officials.sql lines 40-69 pattern):
      WITH ins_p AS (
        INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id)
        VALUES (gen_random_uuid(), '{full_name}', '{first_name}', '{last_name}', NULL, true, {is_appointed_value}, false, true, {external_id})
        ON CONFLICT (external_id) DO NOTHING
        RETURNING id
      )
      INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)
      SELECT gen_random_uuid(), d.id,
             (SELECT id FROM essentials.chambers WHERE name = 'City Council' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'City of {City}, Oregon, US')),
             p.id, '{title}', 'OR', {is_appointed_position_value}, false, NULL
      FROM essentials.districts d
      CROSS JOIN ins_p p
      WHERE d.geo_id = '{geo_id}' AND d.district_type = '{LOCAL or LOCAL_EXEC}' AND d.state = 'or'
        AND p.id IS NOT NULL
        AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id);

    Notes:
    - party always NULL (per D-12)
    - is_active always true; is_vacant always false; is_incumbent always true
    - representing_state always 'OR' (uppercase per D-11)
    - role_canonical always NULL
    - For Mayors: d.district_type = 'LOCAL_EXEC'
    - For councilors: d.district_type = 'LOCAL'

    SECTION D — office_id back-fill (per 244 lines 255-260 pattern; per D-14):
      UPDATE essentials.politicians p
      SET office_id = o.id
      FROM essentials.offices o
      WHERE o.politician_id = p.id
        AND p.external_id IN (
          -4131251,-4131252,-4131253,-4131254,-4131255,-4131256,-4131257,
          -4174851,-4174852,-4174853,-4174854,-4174855,-4174856,-4174857,
          -4124251,-4124252,-4124253,-4124254,-4124255,-4124256,-4124257,
          -4183951,-4183952,-4183953,-4183954,-4183955,
          -4146731,-4146732,-4146733,-4146734,-4146735
        )
        AND p.office_id IS NULL;
    Use explicit IN list — NOT BETWEEN — because the 5 ranges are non-contiguous.

    SECTION E — Post-verification DO block (RAISE EXCEPTION on any failure). Required gates:
    - Per-city government count gate: SELECT COUNT(*) FROM essentials.governments WHERE name = 'City of {City}, Oregon, US' must equal 1 (repeat for all 5 cities).
    - Per-city office-linked count gate: SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = '{geo_id}' AND d.district_type IN ('LOCAL','LOCAL_EXEC') AND d.state = 'or' must equal 7 (Gresham/Troutdale/Fairview) or 5 (Wood Village/Maywood Park).
    - Section-split gate (verbatim from PATTERNS.md): SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('4131250','4174850','4124250','4183950','4146730') AND gb.mtfcc = 'G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.district_type IN ('LOCAL', 'LOCAL_EXEC') AND d.state = 'or') must equal 0.
    - office_id back-fill gate: SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (<31 external ids>) AND office_id IS NULL must equal 0 (all 31 back-filled).
    - On success, RAISE NOTICE 'Post-verification PASSED'.

    SECTION F — Ledger entry + COMMIT:
      INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('246') ON CONFLICT (version) DO NOTHING;
      COMMIT;

    Pre-flight verification BEFORE applying (run as separate SQL — not part of the migration file):
    1. SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -4183959 AND -4131240 — must return 0 (range clear; per RESEARCH External ID Scheme).
    2. SELECT geo_id, name, mtfcc, state FROM essentials.geofence_boundaries WHERE geo_id IN ('4131250','4174850','4124250','4183950','4146730') AND mtfcc = 'G4110' — must return 5 rows with state='41' (confirms Phase 72 load is intact).
    3. SELECT version FROM supabase_migrations.schema_migrations WHERE version = '246' — must return 0 rows (migration not yet applied).

    Apply migration via mcp__supabase-local__execute_sql (same pattern Phase 83 used for migration 244). Do NOT use the loader scripts — the migration is a single transaction applied directly.

    After apply, run section-split check SQL (verbatim from RESEARCH Section-Split Check SQL block) BEFORE running the smoke test. Expected: 0 rows.
  </action>
  <verify>
    <automated>
      Apply migration via mcp__supabase-local__execute_sql, then run SQL gates (the embedded post-verification DO block already enforces these on apply):
      (1) SELECT COUNT(*) FROM essentials.governments WHERE name IN ('City of Gresham, Oregon, US','City of Troutdale, Oregon, US','City of Fairview, Oregon, US','City of Wood Village, Oregon, US','City of Maywood Park, Oregon, US') — returns 5.
      (2) SELECT COUNT(*) FROM essentials.chambers WHERE name = 'City Council' AND government_id IN (SELECT id FROM essentials.governments WHERE name LIKE 'City of % Oregon, US') — returns 5.
      (3) SELECT COUNT(*) FROM essentials.districts WHERE geo_id IN ('4131250','4174850','4124250','4183950','4146730') AND state = 'or' AND district_type IN ('LOCAL','LOCAL_EXEC') — returns 10.
      (4) SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-4131251,-4131252,-4131253,-4131254,-4131255,-4131256,-4131257,-4174851,-4174852,-4174853,-4174854,-4174855,-4174856,-4174857,-4124251,-4124252,-4124253,-4124254,-4124255,-4124256,-4124257,-4183951,-4183952,-4183953,-4183954,-4183955,-4146731,-4146732,-4146733,-4146734,-4146735) — returns 31.
      (5) SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON o.district_id = d.id WHERE d.geo_id IN ('4131250','4174850','4124250','4183950','4146730') — returns 31.
      (6) SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (<31 ids>) AND office_id IS NULL — returns 0.
      (7) Section-split detector SQL — returns 0 rows.
      (8) SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id IN (-4183951, -4146731) AND o.is_appointed_position = true — returns 2 (Wood Village + Maywood Park Mayors only).
      (9) SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id IN (-4183951, -4146731) AND o.is_appointed_position = false — returns 0 (sanity check).
      (10) SELECT version FROM supabase_migrations.schema_migrations WHERE version = '246' — returns 1 row.
    </automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql exists
    - File begins with BEGIN; and ends with COMMIT;
    - File contains exactly these 5 government name literals: 'City of Gresham, Oregon, US', 'City of Troutdale, Oregon, US', 'City of Fairview, Oregon, US', 'City of Wood Village, Oregon, US', 'City of Maywood Park, Oregon, US'
    - File contains exactly 5 INSERT INTO essentials.governments statements (5 cities) with type='LOCAL' and state='OR'
    - File contains exactly 5 INSERT INTO essentials.chambers statements; column list of every chamber INSERT is exactly `(id, name, name_formal, government_id)` — slug MUST NOT appear in any chamber INSERT
    - File contains 10 INSERT INTO essentials.districts statements: 5 with district_type='LOCAL_EXEC' and 5 with district_type='LOCAL'; all 10 have state='or' (lowercase) and mtfcc=NULL
    - File contains exactly 31 INSERT INTO essentials.politicians statements (each inside a WITH ins_p CTE) with the 31 external_ids listed above
    - File contains exactly 31 INSERT INTO essentials.offices statements with representing_state='OR' (uppercase)
    - Offices for external_ids -4183951 and -4146731 set is_appointed_position=true; ALL 29 other offices set is_appointed_position=false
    - Politicians for external_ids -4183951, -4146731, -4183954 set is_appointed=true; ALL 28 other politicians set is_appointed=false
    - File contains the literal string `'E''an Todd'` (escaped ASCII apostrophe — Pitfall 7) and does NOT contain the U+2019 Unicode RIGHT SINGLE QUOTATION MARK character
    - File contains the office_id back-fill UPDATE with all 31 external_ids in an explicit IN list (per D-14)
    - File contains a post-verification DO block with RAISE EXCEPTION on failure for: per-city government counts (5), per-city office counts (7/7/7/5/5), section-split (0), office_id back-fill (0 nulls)
    - File contains `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('246') ON CONFLICT (version) DO NOTHING;`
    - File does NOT contain `district_type='COUNTY'` or `district_type='SCHOOL'` anywhere
    - File does NOT contain `state = 'OR'` inside any districts INSERT (only governments and offices use uppercase OR)
    - File does NOT contain any reference to MTFCC values X0013 through X0018 (no Gresham ward geofences; D-05 OVERRIDDEN by research)
    - After apply: all 10 SQL gates in the <verify><automated> block return their expected counts (5, 5, 10, 31, 31, 0, 0, 2, 0, 1)
    - After apply: re-running the migration is a no-op — repeated SELECT COUNTs above stay unchanged (idempotency per D-13)
    - After apply: supabase_migrations.schema_migrations contains exactly one row with version='246'
  </acceptance_criteria>
  <done>
    Migration 246 file exists, was successfully applied to production Supabase via mcp__supabase-local__execute_sql, all 10 post-apply SQL gates return the expected counts, section-split detector returns 0 rows, and idempotency is confirmed by a second apply being a no-op (all counts unchanged).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write smoke-multnomah-cities.ts (5-coordinate routing smoke test)</name>
  <files>C:/EV-Accounts/backend/scripts/smoke-multnomah-cities.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts (Phase 83 analog — read in full; copy imports, AddressTest interface, queryBoundaries, Client setup, error-collection pattern, ALL ASSERTIONS PASSED stdout, process.exit branches)
    - C:/EV-Accounts/backend/src/lib/essentialsService.ts (read districtQueryText to confirm the queryLocalOfficials SQL mirrors production routing: G4110 → LOCAL/LOCAL_EXEC mapping via geofence_boundaries.geo_id → districts.geo_id JOIN)
    - .planning/phases/84-multnomah-smaller-cities/84-PATTERNS.md (lines 398-606 — full smoke test template including AddressTest interface, TEST_ADDRESSES with 5 verified centroids, queryBoundaries verbatim, queryLocalOfficials adapted, SC_SPLIT block, final ALL ASSERTIONS PASSED output)
    - .planning/phases/84-multnomah-smaller-cities/84-RESEARCH.md (Verified Geo_IDs and Test Coordinates table — all 5 centroids confirmed via production DB ST_Covers query; Verified Incumbent Roster — for the expected name sets per city)
  </read_first>
  <action>
    Create file C:/EV-Accounts/backend/scripts/smoke-multnomah-cities.ts. Implements D-03 (one smoke test for all 5 cities) and provides the automated verification used by VALIDATION.md row 84-01-02.

    Structure (mirror smoke-multnomah-county.ts conventions; near-copy with these changes):

    1. File header docstring stating 3 success-criteria classes (SC1, SC2, SC3) and per-city test labels:
       - SC1 — Each city centroid returns G4110 with the expected geo_id (per D-03 boundary match)
       - SC2 — Each city centroid returns LOCAL + LOCAL_EXEC officials matching the seeded name set
       - SC3 — Section-split check across all 5 G4110 geo_ids returns 0 orphans
       - Usage line: `npx tsx scripts/smoke-multnomah-cities.ts` (run from C:/EV-Accounts/backend)

    2. Imports: `import { Client } from 'pg'; import * as dotenv from 'dotenv'; dotenv.config();`

    3. AddressTest interface (verbatim from smoke-multnomah-county.ts):
       interface AddressTest { label: string; lon: number; lat: number; expectedMtfcc: string[]; forbiddenMtfcc?: string[]; expectedGeoIds?: Record<string, string>; }

    4. TEST_ADDRESSES array with 5 entries — coordinates VERBATIM from RESEARCH.md Verified Geo_IDs table (each centroid was confirmed via ST_Covers against production DB):
       - { label: 'Gresham (centroid)', lon: -122.441364519028, lat: 45.5021166610009, expectedMtfcc: ['G4110'], expectedGeoIds: { G4110: '4131250' } }
       - { label: 'Troutdale (centroid)', lon: -122.395436661508, lat: 45.5372271419675, expectedMtfcc: ['G4110'], expectedGeoIds: { G4110: '4174850' } }
       - { label: 'Fairview (centroid)', lon: -122.438921112803, lat: 45.5469083700965, expectedMtfcc: ['G4110'], expectedGeoIds: { G4110: '4124250' } }
       - { label: 'Wood Village (centroid)', lon: -122.420492816904, lat: 45.5357895342016, expectedMtfcc: ['G4110'], expectedGeoIds: { G4110: '4183950' } }
       - { label: 'Maywood Park (centroid)', lon: -122.56177821613, lat: 45.5525170048598, expectedMtfcc: ['G4110'], expectedGeoIds: { G4110: '4146730' } }

    5. EXPECTED_NAMES_BY_GEO_ID constant — maps each city's geo_id to a string array of expected full_name values for SC2 comparison. Values verbatim from RESEARCH.md Verified Incumbent Roster:
       - '4131250' (Gresham, 7): ['Travis Stovall', 'Kayla Brown', 'Eddy Morales', 'Cathy Keathley', 'Jerry Hinton', 'Sue Piazza', 'Janine Gladfelter']
       - '4174850' (Troutdale, 7): ['David Ripma', 'Carol Allen', 'Jesse Davidson', 'John Leamy', 'Glenn White', 'Geoffrey Wunn', 'Zach Andrews']
       - '4124250' (Fairview, 7): ['Keith Kudrna', 'Jeff Dennerline', 'Steve Marker', "E'an Todd", 'Jenni Weber', 'Steve Owen', 'Paul Copeland']
       - '4183950' (Wood Village, 5): ['Jairo Rios-Campos', 'Dara Tan', 'John Miner', 'Charlene Gothard', 'Patricia Smith']
       - '4146730' (Maywood Park, 5): ['Jim Akers', 'Kevin Bussema', 'Jeff Baltzell', 'Miriam Berman', 'Thomas Welander']

    6. queryBoundaries(client, lon, lat) — verbatim from smoke-multnomah-county.ts lines 61-75 (returns geo_id, name, mtfcc rows from ST_Covers query against essentials.geofence_boundaries WHERE state='41').

    7. queryLocalOfficials(client, lon, lat) — adapt smoke-multnomah-county.ts queryCountyOfficials by changing the district_type filter from 'COUNTY' to IN ('LOCAL', 'LOCAL_EXEC'). Signature: returns Array<{full_name: string; district_type: string}>. Query joins politicians → offices → districts → geofence_boundaries via geo_id, filtered by state='41' AND d.district_type IN ('LOCAL','LOCAL_EXEC') AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($1,$2), 4326)). ORDER BY p.full_name.

    8. main() function. Pre-flight + per-address SC1/SC2 loop + SC3 section-split:
       - Require DATABASE_URL from env; exit 1 with stderr message if missing.
       - Connect new Client with ssl: { rejectUnauthorized: false }.
       - try { ... } finally { await client.end(); }
       - allPassed = true; errors: string[] = [].
       - Pre-flight: query SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id IN ('4131250','4174850','4124250','4183950','4146730') AND mtfcc = 'G4110' — must equal 5; otherwise push 'Pre-flight FAIL: not all 5 G4110 city boundaries loaded — Phase 72 must be re-applied' and exit 1 immediately.
       - For each addr in TEST_ADDRESSES:
         * Print `=== ${addr.label} ===`.
         * boundaries = await queryBoundaries(client, addr.lon, addr.lat).
         * SC1: assert every mtfcc in addr.expectedMtfcc is present in boundaries; assert each geo_id in addr.expectedGeoIds matches the boundary row of that mtfcc. Push specific error message + set allPassed=false on mismatch.
         * officials = await queryLocalOfficials(client, addr.lon, addr.lat).
         * SC2: expected = EXPECTED_NAMES_BY_GEO_ID[expectedGeoIds.G4110]; assert officials.length === expected.length; assert sorted(officials.map(o => o.full_name)) deep-equals sorted(expected). Push detailed mismatch error (showing missing and unexpected names) on failure.
       - SC3: run section-split SQL (verbatim from PATTERNS.md lines 555-579):
         SELECT gb.geo_id, gb.name
         FROM essentials.geofence_boundaries gb
         WHERE gb.geo_id IN ('4131250','4174850','4124250','4183950','4146730')
           AND gb.mtfcc = 'G4110'
           AND NOT EXISTS (
             SELECT 1 FROM essentials.districts d
             WHERE d.geo_id = gb.geo_id
               AND d.district_type IN ('LOCAL', 'LOCAL_EXEC')
               AND d.state = 'or'
           )
         For each returned row, push `SC_SPLIT FAIL: no LOCAL/LOCAL_EXEC district for geo_id='${row.geo_id}' (${row.name})`; set allPassed=false.
       - Final block:
         * If allPassed: print 'ALL ASSERTIONS PASSED' and 3 lines listing SC1/SC2/SC3 each with [PASS]; process.exit(0).
         * Else: print `FAILED (${errors.length} assertion(s))` then each error; process.exit(1).
    9. Final main().catch wrapper: `main().catch((err) => { console.error('Smoke test error:', err); process.exit(1); });`

    Idempotency / safety: Script makes no writes. All queries are read-only ST_Covers + COUNT + SELECT. Safe to run repeatedly.

    Single-quote handling: When writing the TypeScript string literal for the name "E'an Todd", use double-quoted JS string: `"E'an Todd"`. Do not escape — TS literal handles standard ASCII apostrophe natively. Verify the character in the source is the same standard apostrophe written into the SQL migration (Task 1) — both must use U+0027.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend ; npx tsx scripts/smoke-multnomah-cities.ts</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/smoke-multnomah-cities.ts exists
    - File imports `Client` from `pg` and `dotenv` and calls `dotenv.config()`
    - File defines TEST_ADDRESSES with exactly 5 entries; labels contain 'Gresham', 'Troutdale', 'Fairview', 'Wood Village', and 'Maywood Park' respectively
    - The 5 expectedGeoIds.G4110 values are exactly '4131250', '4174850', '4124250', '4183950', '4146730'
    - The 5 centroid coordinates exactly match RESEARCH.md Verified Geo_IDs table (lon/lat to 14 decimal places where given)
    - File defines `queryLocalOfficials` (function name literally present) which uses district_type IN ('LOCAL','LOCAL_EXEC') in its SQL — NOT district_type = 'COUNTY'
    - File contains an SC3 / section-split SQL query whose WHERE clause includes mtfcc = 'G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.district_type IN ('LOCAL', 'LOCAL_EXEC') AND d.state = 'or')
    - File contains the EXPECTED_NAMES_BY_GEO_ID constant (or equivalent named map) with the 31 verbatim full_name strings listed in the action above
    - File contains the literal string "E'an Todd" (standard ASCII apostrophe inside a double-quoted JS string)
    - Running `cd C:/EV-Accounts/backend ; npx tsx scripts/smoke-multnomah-cities.ts` after migration 246 is applied exits with code 0
    - When passing, stdout contains the literal string `ALL ASSERTIONS PASSED` and the three labels `SC1`, `SC2`, `SC3` each followed by `[PASS]` (or equivalent passing marker)
    - When passing, queryLocalOfficials results return exactly 7 rows for Gresham/Troutdale/Fairview centroids and exactly 5 rows for Wood Village/Maywood Park centroids
    - When passing, the SC3 section-split query returns 0 rows
    - Script makes only read-only queries (grep test: no INSERT/UPDATE/DELETE/CREATE/DROP keywords in the file)
    - Script process.exit(1) on any assertion failure with errors[] printed to stdout
  </acceptance_criteria>
  <done>
    Smoke test file exists, runs cleanly against production Supabase post-migration, exits 0, and prints SC1/SC2/SC3 as passing for all 5 cities. All 5 city centroids correctly resolve to their G4110 geo_id; queryLocalOfficials returns the expected 31 distinct full_name values across the 5 cities; section-split returns 0 orphans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → production Supabase | Migration 246 writes to essentials schema with elevated DB privileges via mcp__supabase-local__execute_sql; single BEGIN/COMMIT transaction is the only privileged surface |
| smoke test → production Supabase | Read-only SQL queries via service role connection string (DATABASE_URL); no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering | migration 246 INSERT statements (5 govts, 5 chambers, 10 districts, 31 politicians, 31 offices) | mitigate | All INSERTs use WHERE NOT EXISTS or ON CONFLICT (external_id) DO NOTHING (per D-13); re-running migration is a no-op; section-split detector inside the same transaction validates referential integrity before COMMIT. |
| T-84-02 | Tampering | per-row idempotency under partial failure | mitigate | Single BEGIN/COMMIT (per D-02); if any statement fails, the entire transaction rolls back, including the ledger entry — no partial state is persisted. |
| T-84-03 | Information disclosure | DATABASE_URL exposure in smoke test | accept | DATABASE_URL is a developer-local secret loaded via dotenv; smoke test never logs the connection string; only public names + counts are printed. |
| T-84-04 | Denial of service | smoke test PostGIS ST_Covers queries | accept | 5 coordinate queries, each runs in <50ms against indexed geofence_boundaries.geometry; total runtime ~10s. No loops, no large scans. |
| T-84-05 | Elevation of privilege | direct DB write outside migration ledger | mitigate | Migration ledger entry (`INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('246')`) sits inside the same BEGIN/COMMIT — if the migration fails, ledger entry is also rolled back. |
| T-84-06 | Tampering | wrong district_type (COUNTY vs LOCAL) causes city officials to surface in wrong section of UI | mitigate | Acceptance criteria explicitly check absence of `district_type='COUNTY'` and `district_type='SCHOOL'` strings in migration source; post-verification DO block validates 31 offices are linked to LOCAL/LOCAL_EXEC districts at correct geo_ids. |
| T-84-07 | Tampering | wrong districts.state casing ('OR' uppercase) silently breaks routing | mitigate | Acceptance criteria explicitly check that districts INSERTs use lowercase 'or' (per D-11). Smoke test queryLocalOfficials uses d.state = 'or' filter — would return 0 rows under wrong casing and trigger SC2 failure immediately. |
| T-84-08 | Repudiation | live writes without audit trail | mitigate | Migration ledger entry version='246' records the apply; the migration file itself is committed to git as the durable record. |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — `pg`, `dotenv`, `tsx` already present in C:/EV-Accounts/backend/package.json (used by Phase 83 smoke-multnomah-county.ts). |
</threat_model>

<verification>
After Task 1 + Task 2 complete:

1. Migration ledger check: `SELECT * FROM supabase_migrations.schema_migrations WHERE version = '246'` returns 1 row.
2. Government / chamber / district / politician / office counts (5 / 5 / 10 / 31 / 31) match expected values (see Task 1 acceptance criteria gates 1-5).
3. office_id back-fill complete: 0 NULL office_ids across the 31 new politicians.
4. Section-split detector returns 0 rows for all 5 G4110 geo_ids.
5. Wood Village + Maywood Park Mayor offices have is_appointed_position=true (count = 2); all other 29 offices have is_appointed_position=false.
6. Smoke test passes: `cd C:/EV-Accounts/backend ; npx tsx scripts/smoke-multnomah-cities.ts` exits 0 with SC1, SC2, SC3 all marked [PASS] for all 5 cities.
7. Re-apply migration 246 — all SQL gate counts unchanged (idempotency per D-13).
8. Phase 83 county routing remains intact: re-run `npx tsx scripts/smoke-multnomah-county.ts` from Phase 83 (must still exit 0 — confirms no regression).
</verification>

<success_criteria>
- CITIES-01 satisfied: City of Gresham government + Council chamber + LOCAL_EXEC + LOCAL districts + 7 officials + 7 offices exist; Gresham centroid resolves to correct G4110 geo_id 4131250 and returns 7 expected officials.
- CITIES-02 satisfied: City of Troutdale government + Council chamber + districts + 7 officials + 7 offices exist; Troutdale centroid resolves correctly.
- CITIES-03 satisfied: City of Fairview government + Council chamber + districts + 7 officials (including E'an Todd with escaped apostrophe) + 7 offices exist; Fairview centroid resolves correctly.
- CITIES-04 satisfied: City of Wood Village government + Council chamber + districts + 5 officials (Mayor Rios-Campos with is_appointed=true, is_appointed_position=true; Gothard with is_appointed=true) + 5 offices exist; Wood Village centroid resolves correctly.
- CITIES-05 satisfied: City of Maywood Park government + Council chamber + districts + 5 officials (Mayor Akers with is_appointed=true, is_appointed_position=true) + 5 offices exist; Maywood Park centroid resolves correctly.
- Migration 246 is idempotent (re-apply is a no-op per D-13).
- Smoke test exits 0 with ALL ASSERTIONS PASSED across SC1, SC2, SC3.
- No Gresham ward geofences (X0013-X0018) loaded — D-05 OVERRIDDEN by RESEARCH.md.
- Phase 83 routing is not regressed.
</success_criteria>

<output>
Create `.planning/phases/84-multnomah-smaller-cities/84-01-SUMMARY.md` when done. Include: migration number applied (246), all 31 generated politician_ids (mapped by external_id), all 31 office_ids, the 10 district_ids (5 LOCAL_EXEC + 5 LOCAL), the 5 government_ids, the 5 chamber_ids, smoke test full stdout (must include ALL ASSERTIONS PASSED line), pre-flight gate results, the actual character used for E'an Todd's apostrophe, and any deviations from this plan.
</output>
