---
phase: 215-header-declutter
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/classify.js
  - src/lib/classify.test.js
autonomous: true
requirements: [HDR-01, HDR-02]
must_haves:
  truths:
    - "The per-tab type-filter default is a single source of truth exported from classify.js (D-05)"
    - "An appointed judge passes the Appointed filter; an elected rep passes the Elected filter; a retention-vote judge passes the Elected filter"
  artifacts:
    - "src/lib/classify.js exports TAB_TYPE_DEFAULTS"
    - "src/lib/classify.js exports resolveIsAppointed and matchesAppointedFilter"
    - "src/lib/classify.test.js has a TAB_TYPE_DEFAULTS describe block"
  key_links:
    - "TAB_TYPE_DEFAULTS.judges === 'Appointed' is the exact value that keeps the Judges tab non-empty under the Elected default (HDR-02)"
---

<objective>
Establish the single source of truth for Phase 215's per-tab type-filter policy: add the
`TAB_TYPE_DEFAULTS` constant and move the two pure filter functions (`resolveIsAppointed`,
`matchesAppointedFilter`) out of `Results.jsx` into `src/lib/classify.js` so they are directly
unit-testable. This closes the HDR-02 risk (the Judges tab silently emptying) with an automated
test rather than code inspection alone.

Purpose: HDR-01/HDR-02 require an Elected-by-default filter with a Judges=Appointed exception (D-05).
Encoding that matrix as a named constant map — alongside the existing `FEDERAL_ORDER`/`STATE_ORDER`
constants in the same file — gives Plan 02 a stable import and gives the phase an automated proof of
the exact values.

Output: `TAB_TYPE_DEFAULTS`, `resolveIsAppointed`, `matchesAppointedFilter` exported from classify.js;
a new `describe` block in classify.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-header-declutter-elected-default-compass-icons-search-by-nam/215-CONTEXT.md
@.planning/phases/215-header-declutter-elected-default-compass-icons-search-by-nam/215-RESEARCH.md
</context>

<artifacts_this_phase_produces>
- `TAB_TYPE_DEFAULTS` — exported const map in `src/lib/classify.js`: `{ representatives: 'Elected', educators: 'Elected', judges: 'Appointed' }`
- `resolveIsAppointed(pol)` — exported pure function moved verbatim from `Results.jsx:1096-1101`
- `matchesAppointedFilter(pol, filter)` — exported pure function moved verbatim from `Results.jsx:1104-1114`
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add TAB_TYPE_DEFAULTS + extract filter functions to classify.js (RED then GREEN)</name>
  <files>src/lib/classify.js, src/lib/classify.test.js</files>
  <read_first>
    - src/lib/classify.js (existing constant-map convention: FEDERAL_ORDER, STATE_ORDER, LOCAL_ORDER at top of file)
    - src/lib/classify.test.js (existing vitest describe/it convention, imports from './classify.js')
    - src/pages/Results.jsx lines 1095-1114 (the current inline resolveIsAppointed + matchesAppointedFilter to move verbatim)
  </read_first>
  <behavior>
    - TAB_TYPE_DEFAULTS.representatives === 'Elected'
    - TAB_TYPE_DEFAULTS.educators === 'Elected'
    - TAB_TYPE_DEFAULTS.judges === 'Appointed'  (HDR-02: the value that keeps the Judges tab populated)
    - resolveIsAppointed({ is_appointed: true }) === true (individual override wins)
    - resolveIsAppointed({ is_elected: true }) === false
    - resolveIsAppointed({ is_elected: false }) === true
    - matchesAppointedFilter({ is_elected: true }, 'Elected') === true
    - matchesAppointedFilter({ is_elected: false }, 'Elected') === false
    - matchesAppointedFilter({ is_elected: false, faces_retention_vote: true }, 'Elected') === true (retention judges surface under Elected)
    - matchesAppointedFilter({ is_elected: false }, 'Appointed') === true (appointed judge passes Appointed)
    - matchesAppointedFilter({ is_elected: true }, 'Appointed') === false
    - matchesAppointedFilter(anyPol, 'All') === true (kept general even though the dropdown no longer emits 'All')
  </behavior>
  <action>
    RED first: add a `describe('TAB_TYPE_DEFAULTS + appointed-filter logic')` block to
    src/lib/classify.test.js covering every case in the behavior list above; import the three new
    symbols from './classify.js'. Run the test and confirm it fails (symbols not yet exported).

    GREEN: in src/lib/classify.js, alongside the existing FEDERAL_ORDER/STATE_ORDER/LOCAL_ORDER
    constant exports at the top of the file, add `export const TAB_TYPE_DEFAULTS` with keys
    representatives/educators/judges mapping to 'Elected'/'Elected'/'Appointed' (per D-05). Then move
    `resolveIsAppointed` and `matchesAppointedFilter` out of Results.jsx (they are currently plain
    function declarations inside the component, lines 1096-1114) into classify.js as named exports,
    byte-for-byte identical logic — do not change the elected/appointed/retention resolution, only its
    location. Keep the `filter === 'All'` branch intact so the function stays general. A brief head
    comment may reference decision D-05; do NOT reproduce any dropdown option label as a literal.

    NOTE: this task only ADDS the exports to classify.js and its test. Removing the now-duplicated inline
    definitions from Results.jsx and switching Results.jsx to import them is Plan 02's job (Plan 02
    depends on this plan) — leaving them briefly duplicated keeps this plan independently green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/classify.test.js -t "TAB_TYPE_DEFAULTS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const TAB_TYPE_DEFAULTS" src/lib/classify.js` returns exactly one hit
    - `grep -n "export function matchesAppointedFilter\|export const matchesAppointedFilter" src/lib/classify.js` returns one hit
    - `grep -n "export function resolveIsAppointed\|export const resolveIsAppointed" src/lib/classify.js` returns one hit
    - `npx vitest run src/lib/classify.test.js -t "TAB_TYPE_DEFAULTS"` passes with all behavior cases green
    - `npm run lint` reports no new errors in classify.js / classify.test.js
  </acceptance_criteria>
  <done>
    classify.js exports TAB_TYPE_DEFAULTS, resolveIsAppointed, matchesAppointedFilter; the new test
    block is green; judges default value is asserted to be 'Appointed'.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure client-side constant + pure functions over already-fetched politician records; no input, no I/O, no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-01 | Tampering | classify.js filter values | low | accept | Display-only default; appointed non-judge officials remain queryable via the API (D-06). Not an authorization boundary. No mitigation needed. |
</threat_model>

<verification>
- `npx vitest run src/lib/classify.test.js` fully green (no regression to existing classify tests)
- `npm run lint` clean
</verification>

<success_criteria>
TAB_TYPE_DEFAULTS + the two filter functions are exported and unit-tested; the Judges=Appointed value
is proven by an automated assertion.
</success_criteria>

<output>
Create `.planning/phases/215-header-declutter-elected-default-compass-icons-search-by-nam/215-01-SUMMARY.md` when done
</output>
