---
phase: 178-city-of-tigard-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["178-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql
autonomous: false
requirements: [WASH-04]

must_haves:
  truths:
    - "One 'City of Tigard, Oregon, US' government row exists (geo_id 4173650)"
    - "One 'City Council' chamber exists under it with official_count=7"
    - "Exactly one LOCAL_EXEC district and one LOCAL district exist on geo_id 4173650, state='or' lowercase"
    - "7 offices exist (Mayor + 6 councilors), each with representing_city='Tigard' set INLINE"
    - "Office titles are plain 'Mayor' / 'Councilor' — NO ward or numbered-position text on any seat"
    - "Mayor Yi-Kang Hu and Councilor Tom Anderson carry is_appointed=true (politician) + is_appointed_position=true (office); the other 5 carry false/false"
    - "All 7 politicians have office_id back-filled (no NULL office_id)"
    - "Section-split scan returns 0 rows for geo_id 4173650 (via the WR-01 independent canonical query, not the dead same-transaction gate)"
    - "NO ward/X00xx geofences or districts were created (pure at-large model)"
    - "The Youth Councilor is NOT seeded — exactly 7 offices, official_count=7"
    - "Maureen Wolf is one councilor office (Council President title-on-seat), not a separate office row"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql"
      provides: "Structural seed: government + chamber + 2 districts + 7 offices + office_id backfill + WR-01-fixed post-verify DO block + ledger row"
      min_lines: 250
      contains: "City of Tigard, Oregon, US"
  key_links:
    - from: "offices"
      to: "districts (geo_id 4173650)"
      via: "district_id FK on LOCAL_EXEC + LOCAL rows"
      pattern: "geo_id = '4173650'"
    - from: "politicians.office_id"
      to: "offices.id"
      via: "back-fill UPDATE keyed on external_id IN (-4173651..-4173657)"
      pattern: "office_id = o.id"
---

<objective>
Author the single structural migration that seeds the City of Tigard government, City Council
chamber, two citywide districts (LOCAL_EXEC for the Mayor + LOCAL at-large for all 6 councilors),
and the 7 officials — mirroring the Hillsboro migration 1150 template. Tigard is the SIMPLEST
at-large shape in the milestone: no wards, no numbered positions, plain 'Mayor'/'Councilor' titles.
Two seats (Mayor Hu, Councilor Anderson) are council-appointed and require the appointed-seat flag
pattern (Las Vegas mig 1075 precedent). Set `representing_city='Tigard'` INLINE on every office
INSERT so the community banner derives correctly at first browse (no backfill migration).

Purpose: Satisfy the roster/structure portion of WASH-04 with an idempotent, self-verifying
migration that produces no section-split and no empty LOCAL section.
Output: `1159_tigard_city_council.sql` (registered in schema_migrations), applied by the
orchestrator via psql, with its in-migration DO-block gates passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/178-city-of-tigard-deep-seed/178-RESEARCH.md
@.planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md
@.planning/phases/178-city-of-tigard-deep-seed/178-01-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES the `.sql` file to C:/EV-Accounts/backend/migrations/.
It has NO DB access. The INLINE ORCHESTRATOR applies it via `psql "$DATABASE_URL" -f <file>`
(DATABASE_URL from C:/EV-Accounts/backend/.env) and commits it in the C:/EV-Accounts repo
(`git -C "C:/EV-Accounts"`). Use the exact migration number confirmed in 178-01-SUMMARY.md (default
1159) — if Wave-0 recorded a different next number, rename the file and update the ledger INSERT.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug` and
`photo_origin_url` OUT of comments in this file. The ledger INSERT into
supabase_migrations.schema_migrations is a genuine table write (expected), not a comment.
</context>

<interfaces>
<!-- Confirmed schema shapes — executor uses these directly, no exploration needed. -->

essentials.governments  (id, name, type, state, city, geo_id)   -- slug GENERATED; NO geo_id unique constraint; INSERT via WHERE NOT EXISTS on name
essentials.chambers     (id, name, name_formal, government_id, official_count)  -- slug GENERATED ALWAYS — NEVER include in INSERT column list
essentials.districts    (id, district_type, state, geo_id, label, mtfcc)  -- NO name_formal; government_id NULL; state='or' LOWERCASE for LOCAL/LOCAL_EXEC
essentials.politicians  (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id, office_id)  -- party ALWAYS NULL (antipartisan)
essentials.offices      (id, district_id, chamber_id, politician_id, title, representing_state, representing_city, is_appointed_position, is_vacant, role_canonical)
</interfaces>

<roster>
<!-- Seed exactly these 7 (re-confirmed at Wave-0). ext_id / title / district_type / appointed: -->
| ext_id    | full_name       | title     | district_type | is_appointed |
|-----------|-----------------|-----------|---------------|--------------|
| -4173651  | Yi-Kang Hu      | Mayor     | LOCAL_EXEC    | TRUE  (appointed Oct 2025, term through Dec 31 2026) |
| -4173652  | Tom Anderson    | Councilor | LOCAL         | TRUE  (appointed interim Dec 2025, term through Dec 31 2026) |
| -4173653  | Faraz Ghoddusi  | Councilor | LOCAL         | FALSE (elected Nov 2024, 2-yr term through Dec 31 2026) |
| -4173654  | Heather Robbins | Councilor | LOCAL         | FALSE (elected Nov 2024, 2-yr term through Dec 31 2026) |
| -4173655  | Jake Schlack    | Councilor | LOCAL         | FALSE (elected Nov 2024, 4-yr term through Dec 31 2028) |
| -4173656  | Jeanette Shaw   | Councilor | LOCAL         | FALSE (elected Nov 2024, 4-yr term through Dec 31 2028) |
| -4173657  | Maureen Wolf    | Councilor | LOCAL         | FALSE (elected Nov 2024, 4-yr term; holds Council President title-on-seat) |
<!-- Titles stay PLAIN 'Mayor' / 'Councilor' — NO ward or "Position N" text (charter Ch.3 has none).
     Mayor Hu on LOCAL_EXEC; all 6 councilors on the shared LOCAL district.
     party=NULL, representing_state='OR', representing_city='Tigard' on all 7.
     Wolf's "Council President" is a title-on-seat — note ONLY in a migration comment; do NOT create
     a separate office row and do NOT alter her title. Do NOT seed the Youth Councilor. -->
</roster>

<tasks>

<task type="auto">
  <name>Task 1: Author the Tigard structural migration</name>
  <files>C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1150_hillsboro_city_council.sql (the near-1:1 template — entire file)
    - C:/EV-Accounts/backend/migrations/1075_las_vegas_city_council.sql lines 218–244 (Kara Kelley block — the appointed-seat is_appointed/is_appointed_position pattern for Hu + Anderson)
    - .planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md §"1159_tigard_city_council.sql (structural migration)" (every code block, adapted values)
    - .planning/phases/178-city-of-tigard-deep-seed/178-01-SUMMARY.md (confirmed geo_id, ext_id block, migration number)
    - .planning/phases/178-city-of-tigard-deep-seed/178-RESEARCH.md §"Schema Shapes", §"OR Casing Rules", §"Migration Template Fixes to Carry Forward"
  </read_first>
  <action>
    Wrap everything in a single BEGIN…COMMIT transaction. Structure exactly as Hillsboro mig 1150,
    adapting values:
    (1) Pre-flight hard-abort DO block: RAISE EXCEPTION if a 'City of Tigard, Oregon, US' government
    already exists (message references migration 1159).
    (2) Government INSERT … SELECT (name='City of Tigard, Oregon, US', type='LOCAL', state='OR'
    UPPERCASE, city='Tigard', geo_id='4173650') guarded WHERE NOT EXISTS on name — NEVER insert slug.
    (3) Chamber INSERT (name='City Council', name_formal='Tigard City Council', government_id
    subselect, official_count=7) guarded WHERE NOT EXISTS on (name, government_id) — NEVER insert slug.
    (4) TWO district INSERTs, both state='or' LOWERCASE, geo_id='4173650', mtfcc=NULL: one
    district_type='LOCAL_EXEC' label='Tigard (Mayor, Citywide)', one district_type='LOCAL'
    label='Tigard (At-Large)', each guarded WHERE NOT EXISTS on (geo_id, district_type, state).
    Create NO other districts — no ward/X00xx rows (pure at-large model).
    (5) SEVEN office blocks (the CTE pattern: INSERT politician ON CONFLICT (external_id) DO UPDATE
    SET is_active, then INSERT office CROSS JOIN the correct district guarded by NOT EXISTS on
    (district_id, politician_id)). Use the exact 7 rows in the <roster> table above: Mayor Hu on the
    LOCAL_EXEC district; the 6 councilors on the shared LOCAL district. Titles PLAIN 'Mayor' /
    'Councilor' (NO ward/position text). party=NULL, role_canonical=NULL, representing_state='OR'
    UPPERCASE, representing_city='Tigard' INLINE on every office. CRITICAL appointed-seat handling:
    for Hu (-4173651) AND Anderson (-4173652), set is_appointed=true on the politician row AND
    is_appointed_position=true on the office row (the LV mig 1075 Kelley pattern); for the other 5,
    is_appointed=false / is_appointed_position=false (Hillsboro's uniform pattern). Add a comment above
    Hu's and Anderson's blocks noting their appointment + Dec 31 2026 term-end; add a comment above
    Wolf's block noting she holds the Council President title (title-on-seat only, no separate row).
    (6) office_id back-fill UPDATE with an explicit external_id IN (-4173651,…,-4173657) list, guarded
    AND p.office_id IS NULL.
    (7) Post-verification DO block WITH THE WR-01 FIX: gates gov_count=1, office_count=7 (JOIN
    districts on geo_id 4173650 + district_type IN ('LOCAL','LOCAL_EXEC') + state 'or') — message must
    note "not 8 — no Youth Councilor"; an INDEPENDENT geofence-presence assertion (COUNT(*) FROM
    essentials.geofence_boundaries WHERE geo_id='4173650' AND mtfcc='G4110' must be ≥1) instead of the
    inherited dead same-transaction split gate; the canonical section-split query (GROUP BY
    o.district_id HAVING COUNT(DISTINCT o.chamber_id) > 1 over offices JOIN districts on geo_id
    4173650) = 0; office_id NULLs=0; representing_city='Tigard' count=7. RAISE EXCEPTION on any
    mismatch; RAISE NOTICE on success.
    (8) Ledger INSERT into supabase_migrations.schema_migrations (version) VALUES ('1159') ON CONFLICT
    DO NOTHING (STRUCTURAL migrations register — contrast with headshot/stance migrations).
    Do NOT place fenced code blocks in this action — the SQL itself lives in the file; the analog
    blocks in 178-PATTERNS.md are the source of truth for exact text.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql`; the in-migration WR-01-fixed post-verify DO block RAISEs on any gate failure (all gates must emit the success NOTICE)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the confirmed migration number path (default 1159_tigard_city_council.sql)
    - Single BEGIN…COMMIT; pre-flight abort guard present
    - governments INSERT uses geo_id='4173650', state='OR', WHERE NOT EXISTS on name; no slug column
    - chambers INSERT has official_count=7; no slug column
    - Exactly 2 district INSERTs (LOCAL_EXEC + LOCAL), both state='or' lowercase, geo_id='4173650'; NO ward/X00xx districts
    - 7 office blocks with the exact ext_ids -4173651..-4173657; titles are plain 'Mayor'/'Councilor' with NO ward/position text
    - Hu (-4173651) AND Anderson (-4173652) have is_appointed=true + is_appointed_position=true; the other 5 have false/false
    - representing_city='Tigard' appears on all 7 office INSERTs (inline, no backfill migration)
    - office_id back-fill UPDATE lists all 7 external_ids
    - Post-verify DO block uses the WR-01 independent geofence assertion + canonical section-split query (not the dead same-transaction gate); asserts gov=1, offices=7, section-split=0, office_id nulls=0, representing_city=7
    - Ledger INSERT VALUES ('1159') present (structural migration registers)
    - Literal strings `slug` and `photo_origin_url` do NOT appear in comments
  </acceptance_criteria>
  <done>Structural migration authored; applies cleanly; WR-01-fixed post-verify DO block emits its success NOTICE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies the migration, runs the E2E structural gate, commits</name>
  <what-built>The Tigard structural migration. The orchestrator applies it and runs the standalone verification gate.</what-built>
  <how-to-verify>
    1. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql`. The in-migration DO block must emit "Post-verification PASSED". If it RAISEs, the transaction rolls back — fix and re-apply.
    2. Independent E2E gate (run as separate SELECTs):
       a. governments count for name='City of Tigard, Oregon, US' = 1.
       b. chambers row exists, official_count=7.
       c. districts: exactly 1 LOCAL_EXEC + 1 LOCAL for geo_id='4173650' state='or'; ZERO ward/X00xx rows for Tigard.
       d. offices count = 7 (NOT 8 — no Youth Councilor) for the Tigard chamber; representing_city='Tigard' on all 7; titles are plain 'Mayor'/'Councilor'.
       e. Section-split scan returns 0 rows for geo_id 4173650.
       f. All 7 politicians have non-NULL office_id.
       g. Hu (-4173651) and Anderson (-4173652) have is_appointed=true + is_appointed_position=true; Wolf (-4173657) has exactly ONE office row.
    3. Commit the migration in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/1159_tigard_city_council.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied; "Post-verification PASSED" NOTICE emitted
    - Independent E2E gate a–g all pass
    - Zero ward/X00xx districts for Tigard; exactly 7 offices (no Youth Councilor)
    - Hu + Anderson appointed flags set; Wolf single office row
    - Migration committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with the gate results, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Structural INSERTs applied to production Supabase via psql |
| migration file → schema | Idempotency + WR-01-fixed post-verify gates gate the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-04 | Tampering | Duplicate/partial re-seed on prod | mitigate | Pre-flight hard-abort DO block + WHERE NOT EXISTS on gov/chamber/districts + office guard on (district_id, politician_id); single transaction rolls back on any post-verify failure |
| T-178-05 | Tampering | Dead post-verify gate lets a real section-split ship silently | mitigate | WR-01 fix: independent geofence-presence assertion + canonical GROUP BY/HAVING section-split query, not the same-transaction "create-then-check-absence" gate |
| T-178-06 | Repudiation | Untracked structural change | mitigate | Migration registered in schema_migrations ledger; committed to EV-Accounts git |
| T-178-07 | Elevation of Privilege | Ward/position labels or a Youth Councilor row hide/inflate the real roster | mitigate | Pure at-large enforced (2 citywide districts, plain titles); E2E gate 2c/2d assert zero ward rows + exactly 7 offices |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- In-migration WR-01-fixed post-verify DO block passes; independent E2E gate a–g passes; migration committed.
</verification>

<success_criteria>
- 1 government + 1 chamber (official_count=7) + 2 citywide districts + 7 offices (plain titles, no
  Youth Councilor), all with representing_city='Tigard', Hu+Anderson appointed, office_id back-filled,
  section-split=0, no ward geofences.
</success_criteria>

<output>
Create `.planning/phases/178-city-of-tigard-deep-seed/178-02-SUMMARY.md` when done, recording the
minted politician UUIDs (by external_id) for plans 03 (headshots) and 04 (stances).
</output>
