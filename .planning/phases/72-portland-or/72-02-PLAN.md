---
phase: 72-portland-or
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql
  - C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts
autonomous: true
requirements:
  - GEO-OR-01
  - GEO-OR-02
  - GEO-OR-03
  - GEO-OR-04
  - GEO-OR-05
  - GEO-OR-06

must_haves:
  truths:
    - "All 7 SQL verification gates pass for OR state='41' boundaries"
    - "smoke-or-geofences.ts exits 0 with ALL ASSERTIONS PASSED printed to stdout"
    - "Portland OR address (City Hall) returns G4110 geo_id='4159000' (or corrected actual), G4020 geo_id='41051', plus G5200/G5210/G5220 rows"
    - "Bend OR address returns no G4110 row (forbiddenMtfcc assertion passes)"
    - "Salem OR address returns G4110 + G4020 + G5200 + G5210 + G5220 rows"
    - "Section-split check (Gate 7) returns 0 rows — no orphaned geofence_boundaries without districts rows"
    - "districts.state = 'or' (lowercase) for all 132 OR district rows — no uppercase 'OR' rows"
    - "Portland geo_id and Multnomah County geo_id confirmed from DB and documented in SUMMARY"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql"
      provides: "All 7 gates pass against production DB"
      contains: "state = '41'"
    - path: "C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts"
      provides: "Address smoke test exits 0"
      contains: "ALL ASSERTIONS PASSED"
  key_links:
    - from: "smoke-or-geofences.ts Portland test"
      to: "essentials.geofence_boundaries"
      via: "ST_Covers(geometry, ST_SetSRID(ST_MakePoint($1,$2),4326)) WHERE state='41'"
      pattern: "4159000"
    - from: "verify-or-tiger-import.sql Gate 7"
      to: "essentials.districts"
      via: "geo_id NOT IN (SELECT geo_id FROM essentials.districts)"
      pattern: "0 rows"
---

<objective>
Run all verification gates and the smoke test against the Oregon TIGER data loaded in Plan 01. Confirm all 6 GEO-OR requirements pass. Document actual geo_ids in the phase SUMMARY so future OR phases have confirmed reference values.

Purpose: Gate the data quality before any downstream phase (government DB, state legislature, Portland city seed) relies on these boundaries. A bad geometry or missing district row here causes silent routing failures in production.

Output: All 7 SQL gates green. Smoke test exits 0. 72-02-SUMMARY.md records confirmed Portland geo_id, Multnomah County geo_id, and districts.state casing.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Transparent Motivations/essentials/.planning/PROJECT.md
@C:/Transparent Motivations/essentials/.planning/ROADMAP.md
@C:/Transparent Motivations/essentials/.planning/STATE.md
@C:/Transparent Motivations/essentials/.planning/phases/72-portland-or/72-RESEARCH.md
@C:/Transparent Motivations/essentials/.planning/phases/72-portland-or/72-01-SUMMARY.md

# Verification scripts created in Plan 01
@C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql
@C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 7 SQL verification gates</name>
  <files>C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql — read full file to confirm expected values before running; update Gate 3 and Gate 4 expected comments if the dry-run in Plan 01 revealed a different G4110 count or a different Portland geo_id
  </read_first>
  <action>
Run the SQL verification file against the production database. DATABASE_URL is in C:/EV-Accounts/backend/.env — run from C:/EV-Accounts/backend.

  cd C:/EV-Accounts/backend
  psql $DATABASE_URL -f scripts/verify-or-tiger-import.sql

Read each gate result carefully:

Gate 1 — invalid_geometry_count: MUST be 0. If non-zero, note which geo_ids are invalid (they will need ST_MakeValid re-run).
Gate 2 — geometry_collection_count: MUST be 0. If non-zero, the layer needs ST_MakeValid.
Gate 3 — per-layer counts: MUST show G4020|36, G4110|N (N = dry-run confirmed count from Plan 01), G5200|6, G5210|30, G5220|60. If any count is wrong, the corresponding layer must be re-run from Plan 01.
Gate 4 — Portland sentinel: MUST return 1 row with mtfcc='G4110'. Record the actual geo_id returned. If geo_id is not '4159000', update smoke-or-geofences.ts expectedGeoIds.G4110 and the Gate 4 comment with the correct value.
Gate 5 — districts counts: MUST show COUNTY|36, NATIONAL_LOWER|6, STATE_LOWER|60, STATE_UPPER|30. All under state='or' (lowercase). Run SELECT DISTINCT state FROM essentials.districts WHERE state ILIKE 'or' to confirm — should return only 'or', never 'OR'.
Gate 6 — Multnomah County: MUST return 1 row. Record the actual geo_id returned (expected '41051').
Gate 7 — section-split check: MUST return 0 rows. If any rows are returned, they represent geofence_boundaries records missing a corresponding districts row — diagnose and re-run the failing layer before proceeding.

If any gate fails:
  - Gates 1/2 non-zero: re-run the affected layer with --dry-run, check for geometry issues, re-run live
  - Gate 3 count mismatch: re-run the specific layer (cd119/sldu/sldl/place/county as appropriate)
  - Gate 5 count mismatch or uppercase state: check districts rows with SELECT DISTINCT state, district_type, COUNT(*) FROM essentials.districts WHERE state ILIKE 'or' GROUP BY state, district_type
  - Gate 7 non-zero rows: each returned geo_id/mtfcc identifies the missing districts row; re-run the parent layer

Do not proceed to Task 2 until all 7 gates pass.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && psql $DATABASE_URL -f scripts/verify-or-tiger-import.sql</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 returns invalid_geometry_count = 0
    - Gate 2 returns geometry_collection_count = 0
    - Gate 3 returns exactly 5 rows: G4020|36, G4110|N, G5200|6, G5210|30, G5220|60 (N matches Plan 01 dry-run count)
    - Gate 4 returns exactly 1 row for Portland city with mtfcc='G4110'
    - Gate 5 returns exactly 4 rows: COUNTY|36, NATIONAL_LOWER|6, STATE_LOWER|60, STATE_UPPER|30 — all under state='or' lowercase
    - Gate 6 returns exactly 1 row for geo_id='41051', name='Multnomah County'
    - Gate 7 returns 0 rows (no orphaned geofence_boundaries without districts rows)
    - SELECT DISTINCT state FROM essentials.districts WHERE state ILIKE 'or' returns only lowercase 'or', no uppercase 'OR'
  </acceptance_criteria>
  <done>All 7 SQL gates pass. 0 invalid geometries, 0 GeometryCollections, correct per-layer counts, Portland sentinel present, districts counts match, Multnomah County present, section-split check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run smoke test and confirm address routing</name>
  <files>C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts — read full file; if Task 1 Gate 4 revealed a different Portland geo_id than '4159000', update expectedGeoIds.G4110 before running
  </read_first>
  <action>
If the Portland geo_id from Gate 4 (Task 1) is not '4159000', update smoke-or-geofences.ts:
  - TEST_ADDRESSES[0].expectedGeoIds.G4110 = '<actual geo_id from Gate 4>'
  - Run npx tsc --noEmit from C:/EV-Accounts after the edit

Then run the smoke test from C:/EV-Accounts/backend:
  cd C:/EV-Accounts/backend
  npx tsx scripts/smoke-or-geofences.ts

Read the output:
  - Portland City Hall test: must return G4020, G4110, G5200, G5210, G5220 — 5 rows. The G4110 row must be 'Portland city'. The G4020 row must be 'Multnomah County' with geo_id='41051'. A G5200 row must appear (congressional district, OR-03 expected — verify name).
  - Bend OR test: must NOT return G4110 (forbiddenMtfcc check). Must return G4020 (Deschutes County), G5200, G5210, G5220.
  - Salem OR test: must return G4020, G4110 (Salem city), G5200, G5210, G5220.
  - Layer count check: must show G4020|36, G4110|N, G5200|6, G5210|30, G5220|60.

If the smoke test exits with non-zero code:
  - "forbidden mtfcc G4110 found for Bend OR": the Bend coordinate falls inside an incorporated city boundary — change to lon: -121.4, lat: 44.12 (confirmed rural Deschutes County, no G4110)
  - "expected mtfcc G4110 not found for Portland OR": place layer did not load or Portland geo_id mismatch — re-run place layer
  - "geo_id mismatch for G4110": update expectedGeoIds.G4110 from the actual DB value (printed in error)

Record in SUMMARY:
  - Actual Portland geo_id (from test output or Gate 4)
  - Actual Multnomah County geo_id (from test output or Gate 6)
  - Oregon congressional district for Portland City Hall (from G5200 row in smoke test)
  - State senate district for Portland City Hall (from G5220 row)
  - State house district for Portland City Hall (from G5210 row)
  - districts.state = 'or' (lowercase) confirmed
  - Final G4110 count (from dry-run confirmed value in Plan 01)
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsx scripts/smoke-or-geofences.ts</automated>
  </verify>
  <acceptance_criteria>
    - smoke-or-geofences.ts exits 0
    - stdout contains "ALL ASSERTIONS PASSED"
    - Portland City Hall test: returns 5 MTFCC rows including G4110 name='Portland city' and G4020 name='Multnomah County'
    - Bend OR test: forbiddenMtfcc G4110 assertion passes (no G4110 row returned for this unincorporated address)
    - Bend OR test: returns G4020 (Deschutes County) + G5200 + G5210 + G5220
    - Salem OR test: returns G4110 (Salem city) + G4020 + G5200 + G5210 + G5220
    - Layer count check: G4020|36, G4110|N (dry-run confirmed), G5200|6, G5210|30, G5220|60 all match
    - No assertion failures printed
  </acceptance_criteria>
  <done>smoke-or-geofences.ts exits 0. "ALL ASSERTIONS PASSED" in output. All 3 address tests pass including the Bend forbiddenMtfcc check. Layer counts match expected values. Portland geo_id and congressional district recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend scripts → PostGIS | Read-only SQL queries against production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | TIGER download URL manipulation | mitigate | Already mitigated in Plan 01; Plan 02 is read-only verification — no new download surface |
| T-72-02 | Tampering | Pre-flight assertion bypass (wrong counts accepted) | mitigate | verify-or-tiger-import.sql Gate 3 independently confirms counts post-load; Gate 7 section-split check catches any geofence rows without matching districts rows; smoke test confirms routing works end-to-end |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | No new packages; Plan 02 runs npx tsx against already-installed node_modules |
</threat_model>

<verification>
1. psql $DATABASE_URL -f scripts/verify-or-tiger-import.sql — all 7 gates return expected values
2. npx tsx scripts/smoke-or-geofences.ts — exits 0, prints "ALL ASSERTIONS PASSED"
3. SELECT DISTINCT state FROM essentials.districts WHERE state ILIKE 'or' returns only lowercase 'or'
4. Gate 7 section-split check returns 0 rows
5. Portland City Hall returns G4110 Portland city + G4020 Multnomah County + G5200 + G5210 + G5220
6. Bend OR returns no G4110 row
</verification>

<success_criteria>
- All 7 SQL verification gates pass (GEO-OR-01, GEO-OR-05, GEO-OR-06)
- smoke-or-geofences.ts exits 0 — all 3 address tests pass (GEO-OR-02, GEO-OR-03, GEO-OR-04)
- Portland geo_id confirmed and documented in SUMMARY
- Multnomah County geo_id confirmed as '41051' in SUMMARY
- districts.state = 'or' (lowercase consistently) confirmed in SUMMARY
- OR congressional district name for Portland documented (expected OR-3 or equivalent TIGER label)
- Phase 72 success criteria met: any OR address routes to correct federal/state/local boundaries
</success_criteria>

<output>
After completion, create .planning/phases/72-portland-or/72-02-SUMMARY.md

Include these values in the SUMMARY (source from actual test output):
- Portland city geo_id: <actual from Gate 4>
- Multnomah County geo_id: <actual from Gate 6>
- G4110 place count (TIGER 2024 actual): <dry-run confirmed count from Plan 01>
- districts.state casing: 'or' (lowercase, all 132 rows)
- Portland City Hall congressional district: <from smoke test G5200 row name>
- Portland City Hall state senate district: <from smoke test G5220 row name>
- Portland City Hall state house district: <from smoke test G5210 row name>
- Next migration number: 221 (no DB migration in Phase 72 — loader only; verify before Phase 73)
</output>
