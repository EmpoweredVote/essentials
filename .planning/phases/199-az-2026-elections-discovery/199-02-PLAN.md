---
phase: 199-az-2026-elections-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 199-01
files_modified:
  - C:/EV-Accounts/backend/migrations/1373_az_2026_statewide_races.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1373.ts
  - C:/EV-Accounts/backend/migrations/1374_az_2026_legislative_races.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1374.ts
autonomous: true
requirements:
  - AZ-ELEC-01
tags:
  - races
  - sql
  - arizona
  - az-2026
  - legislative
user_setup: []
must_haves:
  truths:
    - "6 statewide single-winner race shells exist (Governor, Secretary of State, Attorney General, Treasurer, Superintendent of Public Instruction, State Mine Inspector), each with a non-NULL office_id and seats=1"
    - "1 Arizona Corporation Commission race shell exists with seats=2 and a non-NULL STATE_EXEC office_id (2-of-5 at-large, not 5 shells)"
    - "The Corp Commission race SURFACES via fetchStatewideRaceRows — its anchored office's district → district_type is STATE_EXEC (non-NULL office_id alone is NOT sufficient; a non-STATE_EXEC anchor makes the seats=2 race invisible)"
    - "30 'State Senate District N' shells exist with seats=1, all non-NULL office_id"
    - "30 'State House District N' shells exist with seats=2 (30 races modelling 60 members), all non-NULL office_id"
    - "All 67 new shells anchor to the general election e21f5757 (primary_party NULL); re-apply idempotent; ledgers 1373+1374 present"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/1373_az_2026_statewide_races.sql
      provides: "6 statewide + 1 Corp Commission (seats=2, STATE_EXEC-anchored) race shells + post-verify + ledger"
      contains: "ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING"
    - path: C:/EV-Accounts/backend/migrations/1374_az_2026_legislative_races.sql
      provides: "30 Senate (seats=1) + 30 House (seats=2) race shells via per-district office_id subquery + post-verify + ledger"
      contains: "ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-1373.ts
      provides: "Smoke tests: 6 statewide non-NULL office_id, corp seats=2, corp district_type=STATE_EXEC, ledger"
      contains: "essentials.races"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-1374.ts
      provides: "Smoke tests: 30 Senate + 30 House, House seats=2, zero NULL office_id, ledger"
      contains: "essentials.races"
  key_links:
    - from: essentials.races.election_id
      to: essentials.elections (general, e21f5757)
      via: "subquery WHERE name='AZ 2026 Statewide General' AND state='AZ'"
      pattern: "WHERE name = 'AZ 2026 Statewide General'"
    - from: essentials.races.office_id
      to: essentials.offices
      via: "deterministic non-NULL anchor: statewide/corp = known office_id or STATE_EXEC subquery; legislative = per-district ORDER BY o.id LIMIT 1 (OR migration 239 pattern)"
      pattern: "ORDER BY o.id LIMIT 1"
    - from: essentials.races.office_id (Corp Commission)
      to: essentials.districts.district_type = 'STATE_EXEC'
      via: "office → district join; fetchStatewideRaceRows surfaces the race ONLY when district_type ∈ {STATE_EXEC, NATIONAL_UPPER, NATIONAL_EXEC} (electionService.ts ~118-121)"
      pattern: "STATE_EXEC"
---

<objective>
Seed the statewide and legislative 2026 race shells against the pre-existing general election e21f5757:
1. Migration 1373 — 6 statewide single-winner shells (Governor, SoS, AG, Treasurer, Superintendent, Mine Inspector) + 1 Arizona Corporation Commission race with `seats=2` (2-of-5 at-large, NOT 5 shells), anchored to a STATE_EXEC office so it actually surfaces.
2. Migration 1374 — 30 `State Senate District N` (seats=1) + 30 `State House District N` (seats=2, 30 races for 60 members — the partial-unique index forbids 60 identical shells).

Purpose: AZ-ELEC-01 requires statewide + all-90 legislative race shells so any AZ resident sees their 2026 ballot. This phase is PURE STRUCTURE — NO candidates are hand-seeded (post-research D-01: primary is 4 days out; the discovery cron + a future post-07-21 reconcile fill nominees). Every shell carries primary_party NULL and a deterministic non-NULL office_id. For the statewide feed, non-NULL is necessary but NOT sufficient — the Corp Commission anchor must resolve to a STATE_EXEC office or the seats=2 race never reaches voters.

Output: Migrations 1373 + 1374 SQL + apply scripts applied to production; 67 new race shells verified with zero NULL office_ids and a STATE_EXEC-anchored (therefore visible) Corp race.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/199-az-2026-elections-discovery/199-CONTEXT.md
@.planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md
@.planning/phases/105-va-2026-elections-discovery/105-02-PLAN.md
@C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql
@C:/EV-Accounts/backend/src/lib/electionService.ts

<interfaces>
<!-- office_id anchoring rule — RESOLVED from electionService.ts (do not re-litigate). -->
<!-- VISIBILITY (not just non-NULL): fetchStatewideRaceRows (lines ~106-128) surfaces a race when
     r.office_id IS NULL OR its office → district has district_type ∈ STATEWIDE_DISTRICT_TYPES
     (STATE_EXEC, NATIONAL_UPPER, NATIONAL_EXEC — see the ANY($2) filter at lines ~118-121).
     A non-NULL anchor that is NOT one of these types is INVISIBLE in the statewide feed. So the
     Corp Commission anchor MUST resolve to a STATE_EXEC office (RESEARCH §4b: all AZ statewide
     execs incl. Corp Commission are STATE_EXEC, geo_id='04'). -->
<!-- Legislative: fetchDistrictRaceRows INNER-JOINs essentials.offices ON o.id = r.office_id
     then districts ON d.id = o.district_id. A NULL office_id makes the race INVISIBLE.
     Therefore legislative shells MUST have a non-NULL office_id. For AZ House (2 offices
     per district) ORDER BY o.id LIMIT 1 is SAFE because both offices share the same
     district geo_id/district_type — the geography match is identical either way. -->
<!-- seats is never used as a filter in any electionService fetch path → seats=2 does
     not break the elections feed (candidates surface via rc.race_id regardless of seats). -->

Statewide office_ids (RESEARCH §4b — verify each is non-NULL AND STATE_EXEC at execute time):
  Governor                              4d870c55-7937-41ba-8716-7a30da7e3e06
  Secretary of State                    520719e6-cb1e-46a2-a99d-b83cc4d16d38
  Attorney General                      2214a2e6-e96b-4055-8d58-471a55e89922
  Treasurer                             21f57932-e817-42a2-a5d6-55430990adfc
  Superintendent of Public Instruction  ba26bb00-515a-4445-8cc5-b24f28107663
  State Mine Inspector                  73481f59-8969-4734-bd69-cdf6d97df7a3

Legislative subquery shape (per district geo_id 04001..04030; OR migration 239 pattern):
  Senate: chambers.name='State Senate', districts.district_type='STATE_UPPER', d.state='az'
  House:  chambers.name='House of Representatives', districts.district_type='STATE_LOWER', d.state='az'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 1373_az_2026_statewide_races.sql (6 statewide + Corp Commission seats=2, STATE_EXEC-anchored) + apply script + apply</name>
  <files>
    C:/EV-Accounts/backend/migrations/1373_az_2026_statewide_races.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-1373.ts
  </files>
  <read_first>
    .planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md §4b (6 statewide office_ids), §4c (Corp Commission 2-of-5 seats=2 + the 5 commissioner office_ids), §10 (pitfall: do NOT seed 5 Corp shells)
    .planning/phases/105-va-2026-elections-discovery/105-02-PLAN.md — the WITH gen_elec CTE + VALUES + ON CONFLICT + scoped post-verify pattern
    C:/EV-Accounts/backend/src/lib/electionService.ts lines 84-128 (fetchStatewideRaceRows), ESPECIALLY the STATE_EXEC branch at lines ~118-121 (`d.district_type = ANY($2::text[])` where $2 = STATEWIDE_DISTRICT_TYPES). This is the visibility source of truth: a non-NULL Corp office_id surfaces ONLY IF its district → district_type is STATE_EXEC. A non-STATE_EXEC (or district-less) anchor makes the seats=2 corp race INVISIBLE despite being non-NULL.
  </read_first>
  <action>
    First verify against production that all 6 statewide office_ids in <interfaces> resolve to a live essentials.offices row whose district → district_type is STATE_EXEC (representing_state AZ). If any UUID does not resolve or is not STATE_EXEC, re-resolve by chamber name + representing_state and record the correction in the SUMMARY; do NOT proceed with a NULL or non-STATE_EXEC statewide office_id.

    Create `C:/EV-Accounts/backend/migrations/1373_az_2026_statewide_races.sql` following the 105-02 WITH gen_elec pattern. Resolve election_id via `(SELECT id FROM essentials.elections WHERE name='AZ 2026 Statewide General' AND state='AZ')` — do NOT hardcode e21f5757. Insert 6 single-winner shells (`seats=1`, `primary_party=NULL`) with position_names exactly: `Governor`, `Secretary of State`, `Attorney General`, `Treasurer`, `Superintendent of Public Instruction`, `State Mine Inspector`, each carrying its verified office_id literal from <interfaces>.

    Add ONE Corporation Commission race: position_name `Arizona Corporation Commission`, `seats=2`, `primary_party=NULL`, anchored to a non-NULL **STATE_EXEC** Corporation Commission office_id resolved deterministically. Use a subquery that JOINs districts and REQUIRES STATE_EXEC so the pick is guaranteed visible: `SELECT o.id FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.districts d ON d.id=o.district_id WHERE c.name='Corporation Commission' AND o.representing_state IN ('AZ','Arizona') AND d.district_type='STATE_EXEC' ORDER BY o.id LIMIT 1`. If this returns 0 rows (Corp offices are not STATE_EXEC or lack a district), STOP and surface the gap — do NOT fall back to a non-STATE_EXEC anchor, because per <interfaces> the seats=2 corp race would then be invisible in fetchStatewideRaceRows. This is 1 race (2 of 5 seats up, at-large), NOT 5 shells.

    Guard all inserts with `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`. Add a scoped post-verify `DO $$` block asserting: exactly the 6 statewide position_names exist with non-NULL office_id; the Corp race exists with seats=2; AND the Corp race's office → district → district_type = 'STATE_EXEC' (so the statewide feed picks it up); RAISE EXCEPTION otherwise. Add ledger `VALUES ('1373')`. Keep comments free of the token `cron_active`.

    Create `_apply-migration-1373.ts` (copy a recent apply-script structure). Smoke tests: statewide shell count = 6 (by the 6 position_names); NULL office_id count among the 7 = 0; Corp race seats = 2; Corp race office → district district_type = STATE_EXEC (join and print it); ledger 1373 PRESENT. Orchestrator applies via `cd C:/EV-Accounts && npx tsx backend/scripts/_apply-migration-1373.ts`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-1373.ts 2>&amp;1 | tee /tmp/_1373.log; grep -q "Statewide shells: 6" /tmp/_1373.log &amp;&amp; grep -q "NULL office_id count: 0" /tmp/_1373.log &amp;&amp; grep -q "Corp Commission seats: 2" /tmp/_1373.log &amp;&amp; grep -q "Corp district_type: STATE_EXEC" /tmp/_1373.log &amp;&amp; grep -q "Ledger entry 1373: PRESENT" /tmp/_1373.log</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/1373_az_2026_statewide_races.sql exists
    - SQL contains `WHERE name = 'AZ 2026 Statewide General' AND state = 'AZ'` (no hardcoded e21f5757)
    - SQL contains all 6 literals: `'Governor'`, `'Secretary of State'`, `'Attorney General'`, `'Treasurer'`, `'Superintendent of Public Instruction'`, `'State Mine Inspector'`
    - SQL contains `'Arizona Corporation Commission'` with `seats` value `2`
    - SQL Corp anchor subquery JOINs `essentials.districts` and requires `d.district_type='STATE_EXEC'` (visibility guard — not just a bare non-NULL anchor)
    - SQL does NOT create 5 separate Corporation Commission race rows (exactly 1 corp race)
    - SQL contains `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`
    - SQL post-verify asserts the Corp race's office → district → district_type = 'STATE_EXEC'
    - SQL contains `VALUES ('1373')` and does NOT contain `cron_active`
    - Apply script prints `Statewide shells: 6 (expected 6)`, `NULL office_id count: 0 (expected 0)`, `Corp Commission seats: 2`, `Corp district_type: STATE_EXEC`, `Ledger entry 1373: PRESENT`
    - Re-run keeps counts unchanged (idempotency)
    - Prod: `COUNT(*)` of the 6 statewide position_names under the general = 6, all office_id NOT NULL; the corp race has seats=2, non-NULL office_id, and its office → district district_type = STATE_EXEC (so fetchStatewideRaceRows surfaces it)
    - Zero candidates inserted by this migration (pure structure — no race_candidates writes)
  </acceptance_criteria>
  <done>
    7 statewide/corp race shells exist under the AZ general with non-NULL office_ids; Corp Commission is a single seats=2 race anchored to a STATE_EXEC office (verified to surface via fetchStatewideRaceRows); migration 1373 in ledger.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 1374_az_2026_legislative_races.sql (30 Senate seats=1 + 30 House seats=2) + apply script + apply</name>
  <files>
    C:/EV-Accounts/backend/migrations/1374_az_2026_legislative_races.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-1374.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql — the per-district DO $$ + office_id subquery + ON CONFLICT pattern (AZ uses the SAME shape; substitute state/chamber names and geo_ids)
    .planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md §4d (geo_ids 04001–04030; Senate STATE_UPPER seats=1; House STATE_LOWER seats=2; chamber names 'State Senate' / 'House of Representatives') + §10 (pitfall: model House as 30 seats=2, NOT 60)
    C:/EV-Accounts/backend/src/lib/electionService.ts lines 55-82 (fetchDistrictRaceRows — confirms office_id MUST be non-NULL; district-matched by geo_id/mtfcc; see <interfaces>)
  </read_first>
  <action>
    First confirm the exact `districts.label` text for AZ STATE_UPPER and STATE_LOWER districts against production (e.g. `SELECT DISTINCT district_type, label FROM essentials.districts WHERE state='az' AND district_type IN ('STATE_UPPER','STATE_LOWER') ORDER BY 1,2 LIMIT 5`), so position_name matches the app's expected form. Per RESEARCH §4d and VALIDATION the target position_names are `State Senate District N` and `State House District N` (N = 1..30, matching the label values). Use the verified label form.

    Create `C:/EV-Accounts/backend/migrations/1374_az_2026_legislative_races.sql` mirroring OR migration 239: one idempotent INSERT per district (a DO $$ block per district, or a single DO $$ loop over geo_ids 04001..04030 — a PowerShell/JS generator like `generate_or_legislative_races.ps1` is acceptable, but the emitted SQL must be committed). For each of the 30 Senate districts: resolve election_id via the general-election name subquery, office_id via `SELECT o.id FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.districts d ON d.id=o.district_id WHERE c.name='State Senate' AND d.district_type='STATE_UPPER' AND d.state='az' AND d.geo_id='040NN' ORDER BY o.id LIMIT 1`, position_name `State Senate District N`, `seats=1`, `primary_party=NULL`. For each of the 30 House districts: same shape with chamber `House of Representatives`, district_type `STATE_LOWER`, position_name `State House District N`, `seats=2` (ORDER BY o.id LIMIT 1 deterministically picks one of the 2 offices — safe, both share the district geo_id).

    Guard every insert with `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`. Add a scoped post-verify `DO $$` asserting: 30 rows LIKE `State Senate District %` and 30 rows LIKE `State House District %` under the general; ALL House rows seats=2; zero NULL office_id among the 60; RAISE EXCEPTION otherwise. Add ledger `VALUES ('1374')`. Keep comments free of `cron_active`.

    Create `_apply-migration-1374.ts`. Smoke tests: Senate shells = 30; House shells = 30; House seats all = 2; legislative NULL office_id count = 0; ledger 1374 PRESENT. Orchestrator applies via `cd C:/EV-Accounts && npx tsx backend/scripts/_apply-migration-1374.ts`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-1374.ts 2>&amp;1 | tee /tmp/_1374.log; grep -q "Senate shells: 30" /tmp/_1374.log &amp;&amp; grep -q "House shells: 30" /tmp/_1374.log &amp;&amp; grep -q "House seats!=2 count: 0" /tmp/_1374.log &amp;&amp; grep -q "Legislative NULL office_id count: 0" /tmp/_1374.log &amp;&amp; grep -q "Ledger entry 1374: PRESENT" /tmp/_1374.log</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/1374_az_2026_legislative_races.sql exists
    - SQL resolves election_id via `WHERE name = 'AZ 2026 Statewide General'` (no hardcoded UUID)
    - SQL contains 30 distinct `State Senate District` position_names (1..30) and 30 distinct `State House District` position_names (1..30)
    - Every House insert sets `seats` to `2`; every Senate insert sets `seats` to `1`
    - SQL creates 30 House races (NOT 60) — count of `State House District %` rows = 30
    - Every office_id anchor uses `ORDER BY o.id LIMIT 1` (deterministic, non-NULL)
    - SQL contains `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`
    - SQL contains `VALUES ('1374')` and does NOT contain `cron_active`
    - Apply script prints `Senate shells: 30`, `House shells: 30`, `House seats!=2 count: 0`, `Legislative NULL office_id count: 0`, `Ledger entry 1374: PRESENT`
    - Re-run keeps counts unchanged (idempotency)
    - Prod: 30 `State Senate District %` + 30 `State House District %` rows under the general; all 60 office_id NOT NULL
    - Zero candidates inserted by this migration
  </acceptance_criteria>
  <done>
    60 legislative race shells (30 Senate seats=1 + 30 House seats=2) exist under the AZ general with non-NULL office_ids; migration 1374 in ledger; the discovery cron can attach candidates by race_id.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → production Supabase (`essentials.races`) | Privileged write of civic reference shells; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-02-CORRUPT | Tampering | 1373/1374 re-apply | mitigate | `ON CONFLICT … WHERE primary_party IS NULL DO NOTHING` + scoped `DO $$` count asserts make re-apply a verified no-op |
| T-199-02-PHANTOM | Integrity | Corp Commission / House shell shape | mitigate | Corp = exactly 1 seats=2 race (not 5); House = 30 seats=2 races (not 60); post-verify asserts counts |
| T-199-02-INVISIBLE | Information Disclosure (data not reaching voters) | office_id anchoring + statewide visibility | mitigate | Legislative: deterministic non-NULL `ORDER BY o.id LIMIT 1` anchor; NULL-office_id count asserted 0 (matches fetchDistrictRaceRows INNER JOIN). Corp: STATE_EXEC-required anchor + post-verify assert district_type='STATE_EXEC' (matches fetchStatewideRaceRows lines ~118-121) — a non-NULL-but-non-STATE_EXEC anchor would be silently invisible, so the anchor STOPs rather than falls back |
| T-199-02-LEDGER | Repudiation | schema_migrations | mitigate | Mandatory `VALUES ('1373')`/`('1374')`; apply scripts RAISE if missing |
| T-199-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — existing `pg`/`tsx` only |
</threat_model>

<verification>
- 6 statewide + 1 Corp (seats=2, STATE_EXEC-anchored) + 30 Senate + 30 House (seats=2) = 67 new shells under the general
- Zero NULL office_ids; Corp anchor verified STATE_EXEC (surfaces in statewide feed); House modelled as 30 seats=2 (not 60); Corp as 1 seats=2 (not 5)
- Idempotent re-apply; ledgers 1373 + 1374 PRESENT
- Zero candidates hand-seeded (pure structure)
</verification>

<success_criteria>
- Prod count of the 6 statewide position_names + 1 corp under the general = 7, all non-NULL office_id
- The Corp race's office → district district_type = STATE_EXEC (so fetchStatewideRaceRows surfaces the seats=2 race)
- Prod count `State Senate District %` = 30 and `State House District %` = 30, all non-NULL office_id, House seats=2
- AZ-ELEC-01 (statewide + legislative shell portion) satisfied
</success_criteria>

<output>
Create `.planning/phases/199-az-2026-elections-discovery/199-02-SUMMARY.md` when done with: migration 1373/1374 + apply-script paths; the resolved statewide + corp office_ids; confirmation the Corp anchor is STATE_EXEC; the verified legislative position_name label form; smoke-test output; acceptance-criteria checklist; deviations (esp. any office_id re-resolution).
</output>
</output>
