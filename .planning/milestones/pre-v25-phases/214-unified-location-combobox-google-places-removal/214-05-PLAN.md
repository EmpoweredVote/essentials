---
phase: 214-unified-location-combobox-google-places-removal
plan: 05
type: execute
wave: 4
depends_on: [214-03, 214-04]
files_modified:
  - src/hooks/useGooglePlacesAutocomplete.js
  - src/components/LocationBrowser.jsx
  - src/components/LocalityMatches.jsx
  - src/index.css
  - src/lib/coverage.js
  - package.json
  - package-lock.json
autonomous: true
requirements: [SRCH-08]
user_setup: []

must_haves:
  truths:
    - "useGooglePlacesAutocomplete.js, LocationBrowser.jsx, and LocalityMatches.jsx no longer exist"
    - "The .pac-container/.pac-item* CSS block is gone from index.css but the unrelated .ev-candidate-enter animation survives"
    - "@googlemaps/js-api-loader is absent from package.json AND package-lock.json"
    - "The scoped acceptance grep for pac-container/window.google/@googlemaps returns zero hits"
    - "The only remaining 'google' hits are the unrelated Google Civic Information API (voter-info) feature"
    - "The app still builds and the full test suite is green after all deletions"
  artifacts:
    - path: "src/index.css"
      provides: "Google-free stylesheet retaining ev-candidate-enter animation"
      contains: "ev-candidate-enter"
  key_links:
    - from: "package.json"
      to: "(removed)"
      via: "npm uninstall"
      pattern: "@googlemaps"
---

<objective>
Complete the D-09 Google Places retirement and run the SRCH-08 acceptance gate. Delete the now-orphaned Google modules, remove the non-contiguous `.pac-container` CSS block, reword the stale `coverage.js` comment, uninstall `@googlemaps/js-api-loader`, and prove zero remaining Google-Places references with a correctly-SCOPED grep that does not false-fail against the unrelated Google Civic Information API.

Purpose: SRCH-08 — "own the search stack, drop Google" is only true once the dependency and dead code are provably gone.
Output: Three deleted files, edited `index.css`/`coverage.js`, uninstalled dependency, passing scoped grep gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-CONTEXT.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-RESEARCH.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned Google modules, remove .pac CSS block, reword coverage.js comment, uninstall dependency</name>
  <read_first>
    - 214-RESEARCH.md "Google Places Removal Surface" files table (lines ~566-587), "False positives — DO NOT touch" (lines ~580-587), LocationBrowser consumer check (lines ~617-622)
    - 214-PATTERNS.md src/index.css section (lines ~346-361) — NON-CONTIGUOUS delete gotcha: the .pac block is ~60-102 AND ~120-123 with an UNRELATED .ev-candidate-enter animation (~104-118) sandwiched between; delete in two edits, never one range
    - src/lib/coverage.js — re-grep for the stale "Google address autocomplete owns that path" comment (~line 319) inside searchCoverageAreas
    - 214-CONTEXT.md D-09
  </read_first>
  <action>
    Re-grep `useGooglePlacesAutocomplete`, `LocationBrowser`, and `LocalityMatches` across `src/` first to confirm only self-references remain after Plans 03/04. Delete `src/hooks/useGooglePlacesAutocomplete.js`, `src/components/LocationBrowser.jsx`, and `src/components/LocalityMatches.jsx`. In `src/index.css`, re-grep for `.pac-container` and `.pac-item-query` to get current line ranges, then remove the `.pac-container` / `.pac-item` / `.pac-item:first-child` / `.pac-item:hover` / `.pac-item-selected` / `.pac-matched` / `.pac-icon` rules AND the separate `.pac-item-query` rule as TWO non-contiguous edits (or rule-by-rule) — the `.ev-candidate-enter` keyframes/class between them MUST survive. In `src/lib/coverage.js`, reword the stale comment that references "Google address autocomplete" so it accurately describes the leading-digit skip behavior without naming Google (do not change function logic). Run `npm uninstall @googlemaps/js-api-loader` (not a manual package.json edit) so `package-lock.json` regenerates cleanly.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The three files no longer exist (`ls` on each path errors with "No such file").
    - `grep -n "ev-candidate-enter" src/index.css` still returns a hit (animation preserved); `grep -nE "pac-container|pac-item|pac-icon|pac-matched" src/index.css` returns ZERO.
    - `grep -in "google" src/lib/coverage.js` returns ZERO (stale comment reworded).
    - `npm run build` succeeds (no dangling imports).
  </acceptance_criteria>
  <done>Orphaned Google modules deleted; .pac CSS block gone with the animation intact; coverage.js comment reworded; @googlemaps uninstalled; build green.</done>
</task>

<task type="auto">
  <name>Task 2: SRCH-08 scoped grep gate + full-suite regression</name>
  <read_first>
    - 214-RESEARCH.md "Recommended acceptance grep (scoped correctly)" (lines ~589-615) + Pitfall 1 (a naive `grep -ri google` false-fails against the Google Civic Information API)
    - phase_specific_guidance in the planning brief: the gate MUST be scoped to pac-container / pac-item / window.google / @googlemaps — NOT a blanket `google` match
  </read_first>
  <action>
    Run the SCOPED acceptance gate and confirm zero hits across `src/`, `package.json`, and `package-lock.json` for the Places-autocomplete surface (`pac-container`, `pac-item`, `window.google`, `@googlemaps`). Then run the SECONDARY sanity grep (bare-word `google`, case-insensitive, across `src/`) and confirm the ONLY remaining hits are the documented, in-scope-to-KEEP files: `src/components/VoterResourcesCard.jsx` and `src/lib/voterResourceLinks.js` (Google Civic Information API — a different feature), plus possibly the address-format comment in `src/lib/buildingImages.js`. Any OTHER file appearing here is a real regression to fix before proceeding. Run the full test suite. Record both grep outputs verbatim in the SUMMARY as the SRCH-08 evidence.
  </action>
  <verify>
    <automated>grep -rnE "pac-container|pac-item|window[.]google|@googlemaps" src/ package.json package-lock.json || echo "SRCH-08 SCOPED GATE: PASS (zero hits)"; npm test</automated>
  </verify>
  <acceptance_criteria>
    - The scoped grep across `src/` + `package.json` + `package-lock.json` for `pac-container|pac-item|window.google|@googlemaps` produces zero matching lines.
    - The secondary bare-word `grep -rin google src/` lists ONLY VoterResourcesCard.jsx, voterResourceLinks.js, and (optionally) buildingImages.js — no other file.
    - `npm test` exits 0 (full suite green).
    - SUMMARY contains both grep outputs verbatim as SRCH-08 evidence.
  </acceptance_criteria>
  <done>Scoped SRCH-08 gate returns zero hits; only the unrelated Civic API files retain the word "google"; full suite green; evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package manager → dependency tree | `npm uninstall` mutates package.json/lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-SC | Tampering (supply chain) | npm uninstall @googlemaps/js-api-loader | accept | This phase performs NO package install — only a removal of an existing, legitimate Google-maintained package (RESEARCH Package Legitimacy Audit); no new dependency enters the tree, so no slopcheck/legitimacy checkpoint applies |
| T-214-08 | Denial of Service (accidental over-delete) | index.css non-contiguous .pac block | mitigate | Two-edit deletion with an explicit acceptance grep that `.ev-candidate-enter` survives, preventing removal of the unrelated candidate-card animation |
</threat_model>

<verification>
- `npm run build` succeeds after deletions; `npm test` green.
- Scoped SRCH-08 grep gate returns zero hits; secondary sanity grep matches only the documented Civic API files.
</verification>

<success_criteria>
Google Places is fully retired: the three modules are deleted, the .pac CSS block is gone (animation intact), @googlemaps is uninstalled from package.json and lockfile, the scoped grep gate passes with zero hits, and the app builds with a green test suite.
</success_criteria>

<output>
Create `.planning/phases/214-unified-location-combobox-google-places-removal/214-05-SUMMARY.md` when done, embedding both grep outputs as SRCH-08 acceptance evidence.
</output>
