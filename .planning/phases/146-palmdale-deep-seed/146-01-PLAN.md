---
phase: 146-palmdale-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/918_palmdale_reconcile.sql
autonomous: true
requirements: [PLMD-01]

must_haves:
  truths:
    - "D-01: this phase reconciles the EXISTING partial Palmdale seed (UPDATE/move/relabel-not-INSERT) — no greenfield rebuild, no duplicate offices/external_ids; all existing politician rows preserved. A Wave-1 pre-flight SELECT re-confirms BOTH link directions + the current mayor and STOPs on drift"
    - "D-03: the Palmdale government row 'City of Palmdale, California, US' (id 4f59ebad-631b-4340-91f0-091a6cecb3bb) carries geo_id '0655156' (no longer NULL), guarded WHERE geo_id IS NULL; state already 'CA'"
    - "D-04 (move-then-delete, UUID-targeted): the duplicate chamber c8e8d31e's single office 198661de (Bishop's, ext_id -201331) is MOVED into the survivor chamber 000d672d FIRST, an inline assert proves c8e8d31e then has 0 offices, THEN c8e8d31e is DELETED — end state is ONE 'City Council' chamber (000d672d) with exactly 4 offices (Bettencourt's 5th is added in Plan 02)"
    - "D-02 (NEW — by-district relabel): the 4 occupied At-Large district rows are relabeled to match their occupant's real district — f61fd139→'District 1' (Bishop), 6ad1e005→'District 2' (Loa), a1d3e3bf→'District 4' (Ohlsen), 7fe09a06→'District 5' (Alarcón) — keeping district_type='LOCAL', geo_id='0655156', state='CA' UNCHANGED; and one NEW district row 'District 3' (LOCAL, geo_id '0655156', state 'CA') is CREATED for Bettencourt (her office is seated in Plan 02)"
    - "D-07: feedback_section_split_check SQL returns 0 rows for Palmdale after consolidation"
    - "D-06: this reconcile (geo_id backfill + chamber merge + district relabel/create) is a STRUCTURAL migration (918) that registers in supabase_migrations.schema_migrations normally"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/918_palmdale_reconcile.sql"
      provides: "Idempotent geo_id backfill + move-office-198661de-then-delete-duplicate-chamber-c8e8d31e + relabel 4 At-Large districts to D1/D2/D4/D5 + create new District 3 row"
      contains: "c8e8d31e"
  key_links:
    - from: "essentials.governments (id 4f59ebad-631b-4340-91f0-091a6cecb3bb)"
      to: "geo_id '0655156'"
      via: "UPDATE ... WHERE id = '4f59ebad-631b-4340-91f0-091a6cecb3bb' AND geo_id IS NULL"
      pattern: "geo_id\\s*=\\s*'0655156'"
    - from: "essentials.offices (198661de, Bishop's office in duplicate chamber c8e8d31e)"
      to: "survivor chamber 000d672d"
      via: "UPDATE offices SET chamber_id='000d672d...' WHERE id='198661de...'"
      pattern: "chamber_id\\s*=\\s*'000d672d"
    - from: "essentials.chambers (c8e8d31e, emptied duplicate)"
      to: "deleted (asserted 0 offices first)"
      via: "DO $$ assert 0 offices $$ then DELETE FROM essentials.chambers WHERE id = 'c8e8d31e...'"
      pattern: "DELETE FROM essentials\\.chambers"
    - from: "essentials.districts (f61fd139/6ad1e005/a1d3e3bf/7fe09a06, the 4 At-Large rows)"
      to: "real district labels District 1/2/4/5"
      via: "UPDATE districts SET label='District N' WHERE id='<uuid>' (district_type/geo_id/state UNCHANGED)"
      pattern: "label\\s*=\\s*'District 1'"
---

<objective>
Reconcile the existing partial, duplicate-chamber, wrong-form-of-government Palmdale seed in a single
idempotent structural migration (918). This is the foundation wave — every later wave (roster, headshots,
stances) assumes a clean, single-chamber, correctly-districted structure.

⚠️ RECONCILE, NOT GREENFIELD (DB-verified 2026-06-20, BOTH link directions — see CONTEXT §db_precheck,
re-confirmed by RESEARCH §5). The survivor chamber 000d672d already holds 3 occupied offices (Alarcón,
Loa, Ohlsen via offices.politician_id) and the duplicate chamber c8e8d31e holds 1 (Bishop). This wave
does NOT create or reseat people — it (1) backfills geo_id, (2) merges the duplicate chamber, and (3)
fixes the form of government from (wrong) At-Large to Palmdale's real 5-single-member-district structure.
Bishop's bidirectional-link repair, the Bettencourt CREATE, and the Mayor flag are Plan 02 — this wave is
STRUCTURE ONLY.

Three structural fixes, all DB-verified against the live state on 2026-06-20:
1. Backfill essentials.governments.geo_id = '0655156' on gov 4f59ebad-631b-4340-91f0-091a6cecb3bb
   (currently NULL), guarded WHERE geo_id IS NULL (D-01 / D-03 / PLMD-01). state already 'CA'.
2. MERGE the duplicate chamber: MOVE Bishop's office 198661de (the only office under c8e8d31e) into the
   survivor chamber 000d672d (UPDATE the office's chamber_id), then an inline assert confirms c8e8d31e now
   has 0 offices, then DELETE the duplicate chamber c8e8d31e. Target both chambers/the office by UUID ONLY
   — both chambers share the name 'City Council' and slug 'palmdale-city-council' (D-04). End state: ONE
   chamber (000d672d) with exactly 4 offices (Bettencourt's 5th comes in Plan 02).
3. RELABEL the 4 occupied At-Large district rows to their occupant's REAL district (D-02 — NEW by-district
   pattern): f61fd139→'District 1' (Bishop), 6ad1e005→'District 2' (Loa), a1d3e3bf→'District 4' (Ohlsen),
   7fe09a06→'District 5' (Alarcón). Keep district_type='LOCAL', geo_id='0655156', state='CA' on each
   (relabel ONLY). Then CREATE one new district row 'District 3' (LOCAL, geo_id '0655156', state 'CA') for
   Bettencourt — her office is seated into the survivor chamber in Plan 02.

Purpose: A clean structural base. Duplicate 'City Council' chamber names cause split-section render bugs;
NULL geo_id breaks routing; the wrong At-Large labels misrepresent Palmdale's real by-district form of
government (CVRA — Jauregui v. Palmdale). Output: Migration 918 applied to production; all post-verification
queries green; split-section check returns 0 rows for Palmdale.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally (NOT a
stance/headshot audit-only migration). On-disk file counter is authoritative; next structural is 918 (D-06,
on-disk MAX is 917).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-palmdale-deep-seed/146-CONTEXT.md
@.planning/phases/146-palmdale-deep-seed/146-RESEARCH.md
@.planning/phases/146-palmdale-deep-seed/146-VALIDATION.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- DB-verified live values (2026-06-20, BOTH link directions). Task 1 re-confirms them read-only
     before any write and STOPs on drift. -->

Government row:
  id     = 4f59ebad-631b-4340-91f0-091a6cecb3bb   name = 'City of Palmdale, California, US'   type = LOCAL
  geo_id = NULL  -> set to '0655156'   (state already 'CA')
  (Separate gov 'Palmdale Elementary' cc407856 — OUT OF SCOPE, do NOT touch.)

Two chambers, BOTH named 'City Council' / slug 'palmdale-city-council':
  KEEP   000d672d-97f1-4f1f-af61-9eb6f008c4fd  (SURVIVOR — 3 occupied offices)
  DELETE c8e8d31e-c9f2-4d50-9a94-66be603a5c45  (duplicate — 1 office 198661de, holds Bishop -201331)
  (chambers.slug is a GENERATED column — NEVER include slug in any write)

Survivor 000d672d offices (occupied via offices.politician_id):
  6ca2f775-a206-42f4-817e-bfb8d0f865aa  Councilmember   district 7fe09a06  occupant Andrea Alarcón -201? (ext_id 692518)
  2e584cbd-f6e9-4213-8a7d-0ed319ad6c55  Councilmember   district 6ad1e005  occupant Richard J. Loa  (ext_id 692504)
  a67a975e-9743-435b-a44c-1badb47866c3  Councilmember   district a1d3e3bf  occupant Eric Ohlsen      (ext_id 692516)  [Mayor flag in Plan 02]
Duplicate c8e8d31e office (to MOVE into survivor, then delete the empty chamber):
  198661de-2850-46a1-954e-ef502122a40c  Council Member  district f61fd139  occupant Austin Bishop    (ext_id -201331)

Move-then-delete (D-04, Lancaster 910 pattern — UUID-targeted):
  1. UPDATE essentials.offices SET chamber_id='000d672d-...' WHERE chamber_id='c8e8d31e-...'
     (moves Bishop's single office into the survivor)
  2. DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='c8e8d31e-...') > 0
     THEN RAISE EXCEPTION 'Chamber c8e8d31e still has offices'; END IF; END $$;
  3. DELETE FROM essentials.chambers WHERE id='c8e8d31e-...'

District relabel (D-02 — relabel ONLY; district_type/geo_id/state stay as-is). The 4 At-Large rows by
occupant (the mapping is non-obvious — Pitfall 2; use ONLY these UUIDs):
  f61fd139-b621-... -> 'District 1'  (Bishop's district)
  6ad1e005-cf20-... -> 'District 2'  (Loa's district)
  a1d3e3bf-...      -> 'District 4'  (Ohlsen's district)
  7fe09a06-620f-... -> 'District 5'  (Alarcón's district)
  (Note: NO District 3 is being relabeled — it is CREATED fresh below. Districts 3/4/5 mapping is
  intentionally non-sequential because the existing rows map to the occupants above.)

District CREATE (Bettencourt's District 3 — her office is seated in Plan 02, not here):
  INSERT INTO essentials.districts (label, district_type, geo_id, state)
  VALUES ('District 3', 'LOCAL', '0655156', 'CA')  — guarded NOT EXISTS on (label,geo_id) so re-run is a no-op.
  Capture/record the new District 3 UUID for Plan 02 (Bettencourt's office references it).

Schema notes (CRITICAL):
  - chambers.slug is GENERATED — NEVER include slug in any chamber write
  - district_type lives on essentials.districts, NOT on offices (no offices.district_type column)
  - districts.state is uppercase 'CA' (CA convention)
  - This wave does NOT touch any politicians.is_active / office_id; Bishop's NULL back-pointer repair,
    Bettencourt CREATE, and the Mayor flag are Plan 02

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after
  consolidation; expect 0 rows for Palmdale. (5 OTHER LA-area cities have pre-existing split-section
  defects — OUT OF SCOPE here, project_split_section_defects_5_cities.)

Pattern migration to copy: C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql
  (geo_id backfill + move-then-delete duplicate chamber + idempotent guards — the direct precedent;
  Palmdale ADDS the district relabel/create which Lancaster did not have).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state (gov, two chambers, both link directions, 4 At-Large districts, current mayor)</name>
  <files>C:/EV-Accounts/backend/migrations/918_palmdale_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql (the direct STRUCTURAL template — geo_id backfill + move-then-delete + idempotent guard patterns)
    - .planning/phases/145-lancaster-deep-seed/145-01-PLAN.md (the exact pre-flight + STOP-on-drift shape this plan mirrors)
    - .planning/phases/146-palmdale-deep-seed/146-CONTEXT.md (§db_precheck — LOCKED state incl. both link directions, §decisions D-01..D-07, D-02)
    - .planning/phases/146-palmdale-deep-seed/146-RESEARCH.md (§Live-Data Findings 2/5, §Common Pitfalls 2, §Validation Architecture Wave 1)
    - .planning/phases/146-palmdale-deep-seed/146-VALIDATION.md (Wave 1 SQL assertions)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA', district_type on districts)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE/INSERT, run read-only confirmation queries via mcp__supabase-local
    execute_sql (= production) or psql -f using C:/EV-Accounts/backend/.env DATABASE_URL. Confirm EACH
    against the LOCKED CONTEXT §db_precheck:
    (a) gov: SELECT id, geo_id, state FROM essentials.governments WHERE
    id='4f59ebad-631b-4340-91f0-091a6cecb3bb' — confirm geo_id NULL, state 'CA' (and that this is the
    'City of Palmdale, California, US' row, NOT the Palmdale Elementary gov cc407856);
    (b) two chambers under that gov both named 'City Council' / slug 'palmdale-city-council' —
    000d672d (SURVIVOR, 3 offices) and c8e8d31e (DUPLICATE, 1 office);
    (c) survivor 000d672d has exactly 3 offices occupied via offices.politician_id: 6ca2f775 (Alarcón
    692518, district 7fe09a06), 2e584cbd (Loa 692504, district 6ad1e005), a67a975e (Ohlsen 692516,
    district a1d3e3bf);
    (d) c8e8d31e has exactly 1 office 198661de occupied by Bishop (ext_id -201331, district f61fd139) —
    this is the MOVE target (Pitfall: do NOT delete the chamber before moving the office);
    (e) confirm all 4 of those district rows (f61fd139, 6ad1e005, a1d3e3bf, 7fe09a06) currently have
    label='At-Large', district_type='LOCAL', geo_id='0655156', state='CA' — and that NO 'District 3'
    row already exists for geo_id 0655156;
    (f) re-confirm the CURRENT MAYOR live (rotational, changed April 2025; re-selected Dec 2 2025): check
    cityofpalmdaleca.gov/304/City-Council + /307 and AV Press that Eric Ohlsen (D4) currently holds the
    Mayor title and Bishop is Pro Tem. (This wave does NOT flag the mayor — Plan 02 does — but re-confirm
    here so Plan 02 has no surprises.)
    If ANY precondition does not match the LOCKED CONTEXT (a third chamber, c8e8d31e has 0 or >1 offices,
    an office is occupied by a different ext_id than locked, a district is not At-Large/LOCAL, a District 3
    row already exists, or the current mayor is NOT Ohlsen), STOP and report the discrepancy — do NOT write
    a migration against a moved target.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id, state FROM essentials.governments WHERE id='4f59ebad-631b-4340-91f0-091a6cecb3bb';" returns one row, geo_id NULL, state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - Gov 4f59ebad has geo_id NULL and state 'CA' (pre-state); confirmed NOT the Palmdale Elementary gov cc407856
    - Two chambers confirmed sharing name 'City Council' / slug 'palmdale-city-council': 000d672d (KEEP, 3 offices) and c8e8d31e (DELETE, 1 office 198661de)
    - Survivor 000d672d offices confirmed OCCUPIED via offices.politician_id (6ca2f775=Alarcón 692518/7fe09a06, 2e584cbd=Loa 692504/6ad1e005, a67a975e=Ohlsen 692516/a1d3e3bf)
    - c8e8d31e confirmed to have exactly 1 office 198661de occupied by Bishop (ext_id -201331, district f61fd139) — the move target
    - All 4 occupied district rows (f61fd139, 6ad1e005, a1d3e3bf, 7fe09a06) confirmed label='At-Large', district_type='LOCAL', geo_id='0655156', state='CA'; no 'District 3' row exists yet for 0655156
    - Current Mayor re-confirmed live = Eric Ohlsen (D4), Bishop Pro Tem (for Plan 02's flag); STOP-and-report if changed
    - Any mismatch vs CONTEXT §db_precheck is surfaced and execution paused (STOP-on-drift; no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches the LOCKED CONTEXT §db_precheck on BOTH link directions; gov UUID, the move target office 198661de, the 4 At-Large district UUIDs, and the current mayor are all confirmed; the geo_id backfill, chamber merge, and district relabel/create are safe to author.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 918 (geo_id backfill + merge duplicate chamber + relabel 4 districts + create District 3)</name>
  <files>C:/EV-Accounts/backend/migrations/918_palmdale_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/910_lancaster_reconcile.sql (copy the idempotent geo_id backfill + office move + FK-safe chamber DELETE + inline-assert patterns)
    - .planning/phases/145-lancaster-deep-seed/145-01-PLAN.md (the exact authoring shape + post-verification query set this plan mirrors)
    - .planning/phases/146-palmdale-deep-seed/146-CONTEXT.md (§db_precheck LOCKED ids, §decisions D-02..D-04, D-06, D-07)
    - .planning/phases/146-palmdale-deep-seed/146-RESEARCH.md (§Architecture Patterns Wave 1 template incl. district relabel SQL, §Common Pitfalls 2, §Validation Architecture Wave 1 checks)
    - .planning/phases/146-palmdale-deep-seed/146-VALIDATION.md (Wave 1 SQL assertions)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA')
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/918_palmdale_reconcile.sql wrapped in BEGIN/COMMIT, all
    statements idempotent, in this order:
    (1) UPDATE essentials.governments SET geo_id='0655156' WHERE
        id='4f59ebad-631b-4340-91f0-091a6cecb3bb' AND geo_id IS NULL (D-01 / D-03 / PLMD-01).
    (2) MERGE — MOVE Bishop's office into the survivor: UPDATE essentials.offices SET chamber_id=
        '000d672d-97f1-4f1f-af61-9eb6f008c4fd' WHERE chamber_id='c8e8d31e-c9f2-4d50-9a94-66be603a5c45'
        (D-04 step 1 — move FIRST, target by the duplicate chamber UUID; this carries office 198661de).
    (3) Inline assert the duplicate is now empty: a DO $$ block that RAISEs EXCEPTION if
        (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='c8e8d31e-c9f2-4d50-9a94-66be603a5c45') > 0.
    (4) DELETE essentials.chambers WHERE id='c8e8d31e-c9f2-4d50-9a94-66be603a5c45'. Target the duplicate
        by its UUID ONLY, never by name/slug (both chambers share 'City Council' / 'palmdale-city-council').
    (5) RELABEL the 4 occupied At-Large district rows (D-02), each guarded label IS DISTINCT FROM target:
        UPDATE essentials.districts SET label='District 1' WHERE id='f61fd139-...' (Bishop);
        SET label='District 2' WHERE id='6ad1e005-...' (Loa);
        SET label='District 4' WHERE id='a1d3e3bf-...' (Ohlsen);
        SET label='District 5' WHERE id='7fe09a06-...' (Alarcón). Use ONLY these UUIDs (Pitfall 2). Do
        NOT alter district_type, geo_id, or state on any of them (relabel only).
    (6) CREATE the new District 3 row for Bettencourt, guarded so re-run is a no-op: INSERT INTO
        essentials.districts (label, district_type, geo_id, state) SELECT 'District 3','LOCAL','0655156','CA'
        WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE label='District 3' AND geo_id='0655156').
    Do NOT touch the slug column anywhere (GENERATED). Do NOT write to offices.district_type (no such
    column). Do NOT set chamber official_count to 5 yet (Bettencourt's 5th seat lands in Plan 02; this
    chamber has 4 offices after this wave — set official_count=4 here, guarded IS DISTINCT FROM 4, or leave
    it for Plan 02 to set to 5 — prefer setting 4 here for accuracy). Do NOT repair Bishop's back-pointer,
    create Bettencourt, seat any office into District 3, or flag the Mayor here (Plan 02).
    End the file with post-verification queries: geo_id check + a chamber listing under gov 4f59ebad
    (expect exactly one 'City Council', the survivor 000d672d) + a count of offices under 000d672d (expect
    4) + a confirm that c8e8d31e is gone + the 5 district labels (the 4 relabeled D1/D2/D4/D5 + the new D3,
    all LOCAL / geo_id 0655156) + the feedback_section_split_check SQL (expect 0 rows for Palmdale). Apply
    via mcp__supabase-local execute_sql or psql -f, and register migration 918 in
    supabase_migrations.schema_migrations (structural). Record the new District 3 UUID in the SUMMARY for
    Plan 02.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id FROM essentials.governments WHERE id='4f59ebad-631b-4340-91f0-091a6cecb3bb';" returns '0655156' AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.chambers WHERE government_id='4f59ebad-631b-4340-91f0-091a6cecb3bb' AND name='City Council';" returns 1 AND psql "$DATABASE_URL" -c "SELECT label FROM essentials.districts WHERE id IN ('f61fd139-...','6ad1e005-...','a1d3e3bf-...','7fe09a06-...') ORDER BY label;" returns District 1/2/4/5</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='4f59ebad-631b-4340-91f0-091a6cecb3bb'` → '0655156'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='4f59ebad-...' AND name='City Council'` → 1 (not 0, not 2); c8e8d31e deleted
    - `SELECT COUNT(*) FROM essentials.chambers WHERE id='c8e8d31e-c9f2-4d50-9a94-66be603a5c45'` → 0 (duplicate deleted)
    - `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='000d672d-97f1-4f1f-af61-9eb6f008c4fd'` → 4 (198661de moved in; Bettencourt's 5th is Plan 02)
    - District relabel: f61fd139→'District 1', 6ad1e005→'District 2', a1d3e3bf→'District 4', 7fe09a06→'District 5'; each still district_type='LOCAL', geo_id='0655156', state='CA' (unchanged)
    - New 'District 3' row exists for geo_id '0655156' with district_type='LOCAL', state='CA'; its UUID recorded for Plan 02
    - No politician's is_active/office_id modified; Bishop back-pointer NOT repaired here; Bettencourt NOT created; no office seated into District 3; Mayor NOT flagged (all Plan 02)
    - feedback_section_split_check SQL returns 0 rows for Palmdale (D-07)
    - Migration 918 registered in supabase_migrations.schema_migrations (structural)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
  </acceptance_criteria>
  <done>Migration 918 applied + registered; one 'City Council' chamber (000d672d) remains with 4 offices; geo_id 0655156 set; the 4 At-Large districts relabeled D1/D2/D4/D5 and a new District 3 row created (UUID recorded for Plan 02); split-section check 0 rows; re-running the file is a no-op.</done>
</task>

</tasks>

<threat_model>
DB-seeding phase with NO new external attack surface. No untrusted user input crosses any boundary —
migration SQL is applied via service-role/operator (psql/MCP) directly against the internal production
Supabase DB. The real threat surface is accidental data corruption — deleting the duplicate chamber before
moving Bishop's office (orphan), relabeling the wrong district row (4 similar UUIDs), writing to a
non-existent offices.district_type column, or touching the Palmdale Elementary gov — mitigated by
move-FIRST ordering, an inline empty-chamber assert, UUID-scoped targeting of every write, relabel-only
guards (district_type/geo_id/state untouched), a NOT EXISTS guard on the new District 3, idempotent guards,
and a STOP-on-drift pre-flight that checks BOTH link directions and the current mayor.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator (psql/MCP) directly against production Supabase. No untrusted user input crosses here. |
| official city site / AV Press → mayor confirmation | Live re-confirmation of the rotational mayor (changed 4/2025; re-selected 12/2/2025) before Plan 02 flags the title; read-only here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Tampering | Chamber DELETE targets the wrong chamber (both share name 'City Council' / slug) | mitigate | DELETE keyed on UUID 'c8e8d31e…' ONLY, never on name/slug; post-verify exactly one 'City Council' chamber (000d672d) remains |
| T-146-02 | Tampering | Duplicate chamber deleted BEFORE office 198661de is moved → orphans Bishop's office | mitigate | Move-FIRST ordering (UPDATE office chamber_id before DELETE) + inline DO $$ assert RAISEs if c8e8d31e still has >0 offices before the DELETE (Lancaster 910 pattern) |
| T-146-03 | Tampering | geo_id overwritten on a re-run or wrong gov (e.g. Palmdale Elementary cc407856) | mitigate | UPDATE keyed on id 4f59ebad + WHERE geo_id IS NULL; pre-flight confirms it is the City of Palmdale gov; second apply = 0 rows |
| T-146-04 | Tampering | Wrong district relabeled (4 similar At-Large UUIDs; non-obvious occupant mapping) | mitigate | UPDATE keyed on each exact UUID per the locked occupant mapping (f61fd139→D1, 6ad1e005→D2, a1d3e3bf→D4, 7fe09a06→D5); district_type/geo_id/state untouched; post-verify the 4 labels |
| T-146-05 | Tampering | Write to non-existent offices.district_type column, or altering district_type/geo_id on relabel | mitigate | district relabel touches label ONLY; no offices.district_type write anywhere; post-verify district_type still 'LOCAL', geo_id still '0655156' |
| T-146-06 | Tampering | Duplicate 'District 3' row created on re-run | mitigate | INSERT guarded WHERE NOT EXISTS on (label='District 3', geo_id='0655156'); second apply = 0 rows |
| T-146-07 | Tampering | Non-idempotent migration applied twice corrupts state | mitigate | All writes guarded (geo_id IS NULL, id-scoped move/delete, label IS DISTINCT FROM, NOT EXISTS, official_count IS DISTINCT FROM); second apply = 0 rows |
| T-146-08 | Tampering | Accidental write without verifying pre-state (drift since 2026-06-20) | mitigate | Task 1 read-only pre-flight confirms BOTH link directions + the current mayor match CONTEXT §db_precheck before any write; STOP-on-mismatch |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable; existing Pillow + Supabase client only) |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 918:
1. gov 4f59ebad geo_id = '0655156' (state 'CA')
2. exactly 1 chamber 'City Council' under gov 4f59ebad; duplicate c8e8d31e gone
3. survivor chamber 000d672d has exactly 4 offices (Bettencourt's 5th is Plan 02)
4. the 4 occupied districts relabeled District 1/2/4/5 (f61fd139/6ad1e005/a1d3e3bf/7fe09a06), each still LOCAL / geo_id 0655156 / state CA
5. new 'District 3' row exists (LOCAL / 0655156 / CA); UUID recorded for Plan 02
6. feedback_section_split_check SQL → 0 rows for Palmdale
7. no is_active/office_id changes; Bishop not repaired; Bettencourt not created; Mayor not flagged (all Plan 02)
8. migration 918 registered in schema_migrations
</verification>

<success_criteria>
All structural defects fixed and independently verified: geo_id backfilled; the duplicate chamber merged
via move-then-delete (with an inline assert proving it was empty before deletion); the form of government
corrected to Palmdale's real by-district structure (4 At-Large rows relabeled D1/D2/D4/D5 + a new District
3 row); migration 918 idempotent and registered; Palmdale is a single unambiguous 'City Council' chamber
with 4 correctly-districted offices, ready for the roster completion in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/146-palmdale-deep-seed/146-01-SUMMARY.md` when done (record the new District 3 UUID for Plan 02).
</output>
</content>
