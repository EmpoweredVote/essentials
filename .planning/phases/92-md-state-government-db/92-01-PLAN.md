---
phase: 92-md-state-government-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql
autonomous: true
requirements:
  - MD-GOV-01
tags:
  - maryland
  - state-government
  - chambers
  - migration

must_haves:
  truths:
    - "State of Maryland government row exists exactly once (pre-existing from migration 174, NOT inserted by this plan) per D-01 chamber scaffolding"
    - "5 chambers exist under State of Maryland: Governor, Lieutenant Governor, Attorney General, Comptroller, State Treasurer (per D-01: LG is standalone, not under Governor)"
    - "Each chamber uses the short-name + state-qualified name_formal convention from OR migration 222 (e.g., name='Governor', name_formal='Governor of Maryland'); State Treasurer uses name_formal='Maryland State Treasurer'"
    - "Migration 269 is idempotent — re-running inserts 0 additional rows"
    - "Migration 269 fails fast with RAISE EXCEPTION if the State of Maryland government row count != 1"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql"
      provides: "Migration A: pre-flight assertion + 5 chamber INSERTs"
      contains: "DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD') <> 1"
  key_links:
    - from: "essentials.chambers"
      to: "essentials.governments"
      via: "government_id FK"
      pattern: "government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD')"
---

<objective>
Create and apply migration 269 (`269_md_government_chambers.sql`) which seeds 5 constitutional officer chamber scaffolds (Governor, Lieutenant Governor, Attorney General, Comptroller, State Treasurer) under the pre-existing State of Maryland government row. Implements requirement MD-GOV-01 and decision D-01 (LG gets its own standalone chamber — 5 chambers total, not 4).

Purpose: Phase 93 (legislature) and Phase 96 (elections) cannot link offices/races without these chamber rows. This is the foundational migration of Phase 92.

Output: One SQL migration file applied via `mcp__supabase-local__apply_migration`, plus verification queries confirming 5 chamber rows + zero duplicates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-md-state-government-db/92-CONTEXT.md
@.planning/phases/92-md-state-government-db/92-RESEARCH.md
@.planning/phases/92-md-state-government-db/92-PATTERNS.md
@C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql
@C:/EV-Accounts/backend/migrations/168_me_government_chambers.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 269_md_government_chambers.sql</name>
  <files>C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql (primary template — copy structure including BEGIN/COMMIT wrapper, pre-flight DO block, and per-chamber WHERE NOT EXISTS guards)
    - C:/EV-Accounts/backend/migrations/168_me_government_chambers.sql (secondary reference for ME state-prefixed naming exception we are NOT using)
    - .planning/phases/92-md-state-government-db/92-RESEARCH.md (verify next migration number = 269, chamber naming table, and pre-flight pattern)
    - .planning/phases/92-md-state-government-db/92-PATTERNS.md (verified chamber name vs name_formal table for all 5 MD chambers)
  </read_first>
  <action>
    Create the file at C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql. Use the OR 222 template structure exactly with these MD-specific substitutions:

    Header comment block: state purpose ("5 MD chambers under existing State of Maryland row"), reference migration 174 as the source of the pre-existing government row, document D-01 (LG standalone), call out that slug is GENERATED ALWAYS on essentials.chambers (never include in INSERT column list), and call out essentials.governments has no unique constraint on geo_id (resolve government_id via name+state subquery).

    Wrap the entire migration in BEGIN; ... COMMIT;.

    Pre-flight DO $$ BEGIN ... END $$ block: IF (SELECT COUNT(*) FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD') <> 1 THEN RAISE EXCEPTION 'Pre-flight failed: expected exactly 1 State of Maryland government row; found %', (same subquery). Do NOT INSERT into essentials.governments — migration 174 already seeded the row (verified by research via psql 2026-06-05).

    Five INSERT statements, one per chamber, using INSERT INTO essentials.chambers (id, name, name_formal, government_id) SELECT gen_random_uuid(), '<name>', '<name_formal>', (SELECT id FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD') WHERE NOT EXISTS (SELECT 1 FROM essentials.chambers WHERE name = '<name>' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD')). Never include slug in the column list (GENERATED ALWAYS).

    The 5 chamber rows (exact values, per D-01 and research):
    - name='Governor', name_formal='Governor of Maryland'
    - name='Lieutenant Governor', name_formal='Lieutenant Governor of Maryland' (D-01: standalone, NOT under Governor)
    - name='Attorney General', name_formal='Attorney General of Maryland'
    - name='Comptroller', name_formal='Comptroller of Maryland'
    - name='State Treasurer', name_formal='Maryland State Treasurer' (asymmetric formal name per OR 222 precedent for the same chamber)

    Do not include any politician, district, office, or politician_images rows in this migration — those are Plan 02 / Migration 270 scope.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql','utf8');const checks=[/BEGIN;/,/COMMIT;/,/State of Maryland/,/RAISE EXCEPTION/,/'Governor'/,/'Lieutenant Governor'/,/'Attorney General'/,/'Comptroller'/,/'State Treasurer'/,/'Governor of Maryland'/,/'Lieutenant Governor of Maryland'/,/'Attorney General of Maryland'/,/'Comptroller of Maryland'/,/'Maryland State Treasurer'/,/gen_random_uuid\(\)/,/WHERE NOT EXISTS/];const missing=checks.filter(r=>!r.test(t));if(missing.length){console.error('MISSING:',missing.map(r=>r.toString()).join(', '));process.exit(1);}if(/INSERT INTO essentials\.governments/i.test(t)){console.error('FORBIDDEN: file inserts into essentials.governments — should only assert');process.exit(1);}if(/\bslug\b/i.test(t.replace(/--.*$/gm,'').replace(/\/\*[\s\S]*?\*\//g,''))){console.error('FORBIDDEN: slug appears in non-comment code (GENERATED ALWAYS column)');process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql
    - File contains exactly 5 `INSERT INTO essentials.chambers` statements (one per chamber)
    - File contains zero `INSERT INTO essentials.governments` statements (pre-existing row asserted only)
    - File contains the literal string `'State of Maryland'` AND `state = 'MD'` in the pre-flight DO block
    - File contains the literal string `RAISE EXCEPTION` for pre-flight failure
    - File contains all 5 chamber name literals: `'Governor'`, `'Lieutenant Governor'`, `'Attorney General'`, `'Comptroller'`, `'State Treasurer'`
    - File contains all 5 name_formal literals: `'Governor of Maryland'`, `'Lieutenant Governor of Maryland'`, `'Attorney General of Maryland'`, `'Comptroller of Maryland'`, `'Maryland State Treasurer'`
    - File does NOT contain the token `slug` outside SQL comments (GENERATED ALWAYS column)
    - File begins with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>The migration file exists, contains the pre-flight assertion + 5 chamber INSERTs with correct name/name_formal pairs per D-01, and the automated grep checks all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 269 and verify chamber row counts</name>
  <files>(no file modifications — DB writes via MCP)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql (the migration just created in Task 1 — read it to confirm contents match plan)
    - .planning/phases/92-md-state-government-db/92-VALIDATION.md (per-task verification map — uses these exact queries)
  </read_first>
  <action>
    Apply migration 269 to the live Supabase production DB using `mcp__supabase-local__apply_migration` with name `269_md_government_chambers` and the SQL body of the file from Task 1.

    After application, run three verification queries via `mcp__supabase-local__execute_sql`:

    Query A (gov row sanity): `SELECT COUNT(*) AS gov_count FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD'` — expect `gov_count = 1`.

    Query B (chamber count): `SELECT COUNT(*) AS chamber_count FROM essentials.chambers c JOIN essentials.governments g ON g.id = c.government_id WHERE g.name = 'State of Maryland' AND g.state = 'MD'` — expect `chamber_count = 5`.

    Query C (chamber roster): `SELECT name, name_formal FROM essentials.chambers c JOIN essentials.governments g ON g.id = c.government_id WHERE g.name = 'State of Maryland' AND g.state = 'MD' ORDER BY name` — expect 5 rows with exact name/name_formal pairs matching D-01: Attorney General / Attorney General of Maryland; Comptroller / Comptroller of Maryland; Governor / Governor of Maryland; Lieutenant Governor / Lieutenant Governor of Maryland; State Treasurer / Maryland State Treasurer.

    Re-apply the same migration a second time (idempotency check). Re-run Query B — expect `chamber_count = 5` still (no duplicates).

    If any verification fails, do NOT proceed to Plan 02. Capture the failing query output and surface for resolution.
  </action>
  <verify>
    <automated>echo "Verification is performed via MCP tools mcp__supabase-local__apply_migration + mcp__supabase-local__execute_sql during execution. Acceptance criteria below define the SQL gates that must return the expected counts."</automated>
  </verify>
  <acceptance_criteria>
    - `mcp__supabase-local__apply_migration` with name=`269_md_government_chambers` returns success (no SQL errors, no RAISE EXCEPTION fired)
    - SQL: `SELECT COUNT(*) FROM essentials.governments WHERE name='State of Maryland' AND state='MD'` returns exactly `1`
    - SQL: `SELECT COUNT(*) FROM essentials.chambers c JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='State of Maryland' AND g.state='MD'` returns exactly `5`
    - SQL: `SELECT name FROM essentials.chambers c JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='State of Maryland' AND g.state='MD' ORDER BY name` returns rows: `Attorney General`, `Comptroller`, `Governor`, `Lieutenant Governor`, `State Treasurer` (alphabetical, exactly 5)
    - SQL: `SELECT name_formal FROM essentials.chambers c JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='State of Maryland' AND g.state='MD' AND c.name='State Treasurer'` returns exactly `Maryland State Treasurer`
    - SQL: `SELECT name_formal FROM essentials.chambers c JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='State of Maryland' AND g.state='MD' AND c.name='Lieutenant Governor'` returns exactly `Lieutenant Governor of Maryland` (D-01: standalone chamber)
    - Idempotency: re-applying migration 269 a second time produces no additional rows — chamber count still equals 5
  </acceptance_criteria>
  <done>Migration 269 applied successfully; all 5 chambers exist with correct names and name_formals; idempotency confirmed. Phase 92 Plan 02 is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → production DB | SQL written locally is applied directly to live Supabase production (mcp__supabase-local IS production per project memory). Pre-flight DO block is the validation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01-01 | Tampering | essentials.governments (assert vs insert) | mitigate | Pre-flight DO $$ asserts COUNT=1 with RAISE EXCEPTION; migration aborts and rolls back via BEGIN/COMMIT wrapper if the State of Maryland row is missing or duplicated. No INSERT INTO essentials.governments anywhere in the migration. |
| T-92-01-02 | Tampering | essentials.chambers (duplicate inserts on re-run) | mitigate | Every chamber INSERT uses WHERE NOT EXISTS guard on (name + government_id); re-running migration 269 inserts 0 additional rows. |
| T-92-01-03 | Denial of Service | Migration apply path | accept | Migration is small (5 INSERTs + 1 assert); execution time < 100ms. No DoS risk. |
| T-92-01-04 | Information Disclosure | Chamber data | accept | All data is public record (constitutional officer titles). No PII. |
| T-92-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure SQL migration. T-92-01-SC marked not applicable. |
</threat_model>

<verification>
- All acceptance criteria SQL gates return expected counts
- Migration 269 file passes the grep-based structural check in Task 1's verify block
- Migration applied successfully via `mcp__supabase-local__apply_migration` with no errors
- Re-applying the same migration produces zero new rows (idempotency)
- No INSERT INTO essentials.governments statements exist anywhere in the migration file
</verification>

<success_criteria>
1. `269_md_government_chambers.sql` exists in `C:/EV-Accounts/backend/migrations/` with the structure documented in `<action>`
2. SQL: `SELECT COUNT(*) FROM essentials.chambers c JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='State of Maryland' AND g.state='MD'` returns exactly 5
3. The Lieutenant Governor chamber exists as a standalone chamber row (D-01 satisfied) — `SELECT COUNT(*) FROM essentials.chambers c JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='State of Maryland' AND c.name='Lieutenant Governor'` returns exactly 1
4. State Treasurer chamber uses asymmetric name_formal `Maryland State Treasurer` (research-verified per OR 222 precedent)
5. Migration is idempotent — second application produces zero additional rows
</success_criteria>

<output>
Create `.planning/phases/92-md-state-government-db/92-01-SUMMARY.md` when done. Include:
- Migration 269 file path + SHA-256 (optional)
- Apply status (success / failure with error)
- Final chamber roster (5 rows, name + name_formal pairs)
- Idempotency confirmation
- Any deviations from this plan
</output>
