---
phase: 193-pima-county-board-of-supervisors-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["193-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py
  - C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql
autonomous: true
requirements: [PIMA-01]
must_haves:
  truths:
    - "All 5 supervisors have a 600×750 (4:5, Lanczos q90) headshot uploaded to the politician_photos Storage bucket"
    - "Each of the 5 politician_images rows binds to the CURRENT roster member (never a departed member's photo)"
    - "The accented name (Andrés Cano) is handled without corruption; all 5 source portraits fetch from pima.gov CivicPlus (no WAF)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py"
      provides: "CivicPlus fetch → crop_to_4_5 → resize 600×750 Lanczos q90 → Storage x-upsert pipeline for 5 supervisors"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql"
      provides: "5 audit-only politician_images rows (unregistered), UUIDs from Plan 02 manifest"
      contains: "politician_images"
  key_links:
    - from: "_tmp-pima-supervisors-headshots.py"
      to: "content.civicplus.com/api/assets/az-pimacounty"
      via: "requests.get per-supervisor uuid-keyed asset URL with descriptive User-Agent"
      pattern: "civicplus.com"
    - from: "1289_pima_county_headshots.sql"
      to: "essentials.politicians"
      via: "url binds to politician UUID resolved by external_id"
      pattern: "politician_images"
---

<objective>
Acquire and bind 5/5 supervisor headshots. Author a gitignored pipeline script that fetches each
supervisor's official portrait from pima.gov's CivicPlus CMS asset host
(`content.civicplus.com/api/assets/az-pimacounty/{uuid}`), crops to 4:5 (crop-first, never distort),
resizes to 600×750 Lanczos q90, and uploads to the `politician_photos` Storage bucket; plus an
AUDIT-ONLY migration that inserts the 5 `politician_images` rows. RESEARCH verified all 5 CivicPlus
assets are reachable, no WAF, HTTP 200, at usable resolutions (300×450 to 733×1100).

Purpose: Satisfies ROADMAP success criterion #2 (5/5 supervisors have 600×750 headshots).
Output: `_tmp-pima-supervisors-headshots.py` (gitignored `backend/scripts/_*`) +
`1289_pima_county_headshots.sql` (audit-only, unregistered), both applied so all 5 images serve
from the CDN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-02-SUMMARY.md

<execution_note>
Depends on Plan 02: the 5 politician UUIDs (external_id → UUID → full_name) captured in
193-02-SUMMARY.md are REQUIRED — the audit migration cannot hardcode UUIDs ahead of the structural
apply, and the script resolves UUIDs at runtime via `SELECT id FROM essentials.politicians WHERE
external_id=%s`. Executor authors both files; the ORCHESTRATOR runs the script and applies the audit
migration (executor has no DB/Storage access). Commit the migration via `git -C "C:/EV-Accounts"`;
the `_tmp-*` script is gitignored and never committed.
</execution_note>

<interfaces>
- CivicPlus asset URL: `https://content.civicplus.com/api/assets/az-pimacounty/{uuid}?cache=1800` —
  per-supervisor uuid-keyed (NOT a filename pattern), so the 5 URLs are hardcoded per member from the
  pima.gov Board-of-Supervisors page (RESEARCH § Code Examples confirmed all 5 reachable, no WAF).
  Example: Rex Scott = `.../az-pimacounty/2aa1d923-aee8-451d-a8a6-5aa66abf415b?cache=1800` (733×1100).
- essentials.politician_images columns: exactly (id, politician_id, url, type, photo_license) — NO
  photo_origin_url. type='default', photo_license='us_government_work'.
- Storage: bucket politician_photos; canonical path {politician_uuid}-headshot.jpg; x-upsert PUT;
  CDN base https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- ext_id block: Scott -4007001, Heinz -4007002, Allen -4007003, Christy -4007004, Cano -4007005.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the supervisor headshot pipeline script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md (lines 221-254 — the pipeline pattern: reuse crop_to_4_5/resize_600x750/process_headshot_bytes/upload_to_storage/resolve_politician_id/dry_run_head_check/HEADERS verbatim; only ROSTER + source_url_for change)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Code Examples — the CivicPlus URL shape + no-WAF confirmation)
    - C:/EV-Accounts/backend/scripts/_tmp-az-legislature-headshots.py (analog — read in full: reuse crop_to_4_5, resize_600x750, upload_to_storage, resolve_politician_id, env-load, dry_run_head_check, descriptive-UA HEADERS verbatim; only the ROSTER + source-URL builder + count assertions change)
  </read_first>
  <action>
    Copy `_tmp-az-legislature-headshots.py` and adapt. Reuse crop_to_4_5 / resize_600x750 /
    process_headshot_bytes (crop-FIRST then resize) / upload_to_storage (x-upsert PUT to
    `politician_photos/{uuid}-headshot.jpg`) / resolve_politician_id (parameterized `external_id=%s`,
    never hardcode a UUID) / the env-load block / dry_run_head_check / the descriptive User-Agent
    HEADERS — VERBATIM. Keep TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=LANCZOS, MIN_DIM=100.

    Build a ROSTER list of 5 dicts (assert len(ROSTER)==5), each with: external_id
    (-4007001..-4007005), full_name, chamber label, photo_url = the exact per-supervisor CivicPlus
    asset URL from the pima.gov Board page (hardcoded per member — uuid-keyed, not a pattern),
    photo_license='us_government_work', is_required=True (all 5). Add `source_url_for(member)` that
    returns `member['photo_url']`.

    Percent-encode nothing in the CivicPlus URL itself (asset UUIDs are ASCII); Andrés Cano's
    ACCENTED NAME appears only in the human-readable full_name field (stored in the earlier migration,
    not in the fetch URL), so no URL encoding is needed — but confirm the fetched bytes for all 5 open
    as valid images. Resolve politician_id at runtime via the parameterized query. PIL
    `convert('RGB')` + JPEG re-encode optimize=True (strips EXIF/embedded payloads, T-193-IMG).

    Do a dry-run HEAD per member first (verify 200) before the real batch. On any required-member
    fetch/upload failure, print the member + `sys.exit(1)` so the orchestrator catches a partial run
    before the audit migration is written (fallback: `/find-headshots` skill or operator-supplied
    file with photo_license='operator_supplied'). Print a {external_id, uuid, cdn_url} manifest for
    the audit migration.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py" && grep -qF "crop_to_4_5" "C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py" && grep -qF "civicplus.com" "C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py" && grep -qF "external_id=%s" "C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py" && grep -qE "len\(ROSTER\)\s*==\s*5" "C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py` (gitignored `backend/scripts/_*`).
    - Contains crop_to_4_5, resize_600x750, upload_to_storage; TARGET_SIZE (600, 750); LANCZOS; JPEG quality 90; MIN_DIM 100.
    - ROSTER has exactly 5 members (`assert len(ROSTER)==5`), ext_ids -4007001..-4007005; each has a hardcoded content.civicplus.com/api/assets/az-pimacounty asset URL.
    - politician_id resolved via parameterized `external_id=%s` (no f-string/`%`-format into SQL); PIL `convert('RGB')` present.
    - is_required=True on all 5; dry-run HEAD check present; `sys.exit(1)` on required-failure path present.
    - photo_license='us_government_work' on all 5.
  </acceptance_criteria>
  <done>Pipeline script authored: 5-member ROSTER, CivicPlus crop-first 600×750 pipeline, runtime UUID resolution, dry-run HEAD gate, required-failure PAUSE.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md (lines 258-281 — the audit-only politician_images pattern: header, per-row INSERT ... WHERE NOT EXISTS, exact 5 columns, NO ledger registration)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-02-SUMMARY.md (the 5 external_id → UUID → full_name manifest)
    - C:/EV-Accounts/backend/migrations/1287_az_legislature_headshots.sql (analog — read the header + first few rows: audit-only, unregistered, WHERE NOT EXISTS idempotency, 5-column shape)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1288 after Plan 02); name this file disk-MAX+1 (provisionally
    `1289_pima_county_headshots.sql`). Author an AUDIT-ONLY migration (NO ledger footer, NOT
    registered in schema_migrations, applied via `psql -f`). Header must state the source is pima.gov
    CivicPlus (`content.civicplus.com/api/assets/az-pimacounty/{uuid}`), that images are crop-first
    600×750, and that this is AUDIT-ONLY / unregistered.

    Emit 5 per-row INSERTs into essentials.politician_images (id, politician_id, url, type,
    photo_license) — columns EXACTLY these five, no photo_origin_url. Each row: id=gen_random_uuid();
    politician_id = `(SELECT id FROM essentials.politicians WHERE external_id = -4007001)` (etc.);
    url = the CDN path `{CDN_BASE}/{politician_uuid}-headshot.jpg` using the UUID from the Plan 02
    manifest; type='default'; photo_license='us_government_work'. Guard each with WHERE NOT EXISTS
    (SELECT 1 FROM essentials.politician_images WHERE politician_id = (...same subselect...)) —
    idempotent. Cover all 5 ext_ids -4007001..-4007005. Do NOT register in the ledger.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql" && grep -c "INSERT INTO essentials.politician_images" "C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql" && ! grep -qF "schema_migrations" "C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql" && ! grep -qF "photo_origin_url" "C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1289).
    - Grep count of `INSERT INTO essentials.politician_images` = 5.
    - Grep confirms all 5 ext_ids -4007001..-4007005 referenced.
    - Columns are exactly (id, politician_id, url, type, photo_license) — NO `photo_origin_url`.
    - NO `schema_migrations` registration line (audit-only, unregistered).
    - Every row guarded by `WHERE NOT EXISTS` on politician_id (idempotent).
  </acceptance_criteria>
  <done>Audit-only migration authored with 5 idempotent politician_images rows bound to Plan 02 UUIDs, unregistered.</done>
</task>

<task type="auto">
  <name>Task 3: Run pipeline + apply audit migration + assert 5/5 (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator runs an existing script + applies an existing migration; no files modified</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md (Per-Task Verification Map — headshots row: count=5 + PIL 600×750 + curl CDN 200)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Code Examples — CivicPlus source resolutions, MIN_DIM=100 floor)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/Storage access). Run
    `py -3 C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py` — it uploads all 5
    processed images to Storage and prints the {external_id, uuid, cdn_url} manifest. If any REQUIRED
    member fails to source (script exits 1), PAUSE for an operator-supplied file
    (photo_license='operator_supplied') — research reports 5/5 sourceable so this is not expected.
    Then apply the audit migration via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql`.
    Assert: politician_images count for the 5 ext_ids = 5; `curl -sI` each CDN url → HTTP 200; PIL
    verify a sample (include Cano D5 appointee) is exactly 600×750. Record results + final manifest in
    193-03-SUMMARY.md. Commit the audit migration via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4007005 AND -4007001"</automated>
  </verify>
  <acceptance_criteria>
    - Script run exits 0 (all 5 required images fetched, processed, uploaded) — or an operator file was supplied for any exception.
    - `politician_images` count for the 5 Pima ext_ids (-4007005..-4007001) = 5.
    - `curl -sI` on each of the 5 CDN URLs returns HTTP 200.
    - PIL dimension check on a sampled downloaded CDN image (incl. Cano D5) = exactly 600×750.
    - Audit migration NOT registered in the ledger; committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <done>All 5 supervisors serve a 600×750 CDN headshot bound to the correct current politician row; audit migration applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CivicPlus (remote) → pipeline | Untrusted remote image bytes enter the pipeline over HTTP |
| pipeline → Storage/DB | Processed image + politician_images row cross into production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-IMG | Tampering | remote CivicPlus image fetch | mitigate | PIL `convert('RGB')` + JPEG re-encode `optimize=True` strips EXIF/embedded/stego payloads; only `image/jpeg` written to Storage |
| T-193-BIND | Spoofing | politician_images ↔ politician binding | mitigate | Fetch ONLY current-roster URLs; runtime UUID resolution by external_id prevents wrong-person binding on retry; Plan 06 human identity spot-check |
| T-193-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; script itself gitignored (`backend/scripts/_*`); never in a committed migration; parameterized `external_id=%s` query (no injection) |
| T-193-WRONG | Spoofing | wrong-but-present image | transfer | Row-count cannot detect a wrong-but-present photo; deferred to Plan 06 human-verify spot-check |
| T-193-SC | Tampering | package installs | accept | No new pip installs — reuses installed Pillow/requests/psycopg2; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 5/5 politician_images rows for the Pima ext_id block, each 600×750 at HTTP 200.
- Every image binds to the correct current supervisor; no departed member.
- Audit migration NOT registered in the ledger.
</verification>

<success_criteria>
ROADMAP #2 (5/5 supervisors have 600×750 headshots) is TRUE in production; every image binds to the
correct current member; the audit migration is applied and left unregistered.
</success_criteria>

<output>
Create `.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-03-SUMMARY.md` when done —
include the final {external_id → UUID → cdn_url} manifest and any operator-supplied exceptions.
</output>
