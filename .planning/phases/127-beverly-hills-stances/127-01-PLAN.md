---
phase: 127-beverly-hills-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/NNN_friedman_stances.sql
  - C:/EV-Accounts/backend/migrations/NNN+1_corman_stances.sql
autonomous: true
requirements: [BEVHILLS-01]
must_haves:
  truths:
    - "Wave 0 pre-flight [BLOCKING] confirms actual next migration number (expected 714 but MUST verify against DB-applied MAX), resolves all 5 Beverly Hills target UUIDs, confirms active topic count is 44, confirms Howard Fisher's UUID for exclusion, confirms Friedman holds a LOCAL_EXEC Mayor district, and records any pre-existing BH stance counts"
    - "Lester Friedman (Mayor, external_id -200589) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Craig A. Corman (Council Member, external_id -201154) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
    - "Howard Fisher (external_id -700011) receives ZERO stance rows — explicitly excluded per BEVHILLS-01"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/NNN_friedman_stances.sql"
      provides: "Lester Friedman stance migration (NNN determined by Wave 0)"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/NNN+1_corman_stances.sql"
      provides: "Craig A. Corman stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Run Wave 0 pre-flight verification [BLOCKING], then research and apply evidence-only compass stances for the first 2 Beverly Hills officials (Mayor Lester Friedman, Council Member Craig A. Corman), one politician at a time.

Purpose: Establish the verified migration starting point, active topic count, all 5 Beverly Hills target UUIDs, and Fisher's exclusion UUID for the entire phase; begin closing BEVHILLS-01.
Output: 2 per-individual stance migration files (NNN through NNN+1), each applied to production and verified. All 5 target UUIDs + Fisher's UUID recorded in SUMMARY for Plans 02-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-beverly-hills-stances/127-RESEARCH.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 703_lee_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs (Phase 115 ledger-consistency precedent)
- CRITICAL (Beverly Hills, INVERTED from Alhambra): Friedman IS the directly-elected Mayor (LOCAL_EXEC office, -200589). "Mayor Friedman" is CORRECT in reasoning text — do NOT add any "rotational" qualifier. The Alhambra rotational-Mayor warning does NOT apply here (RESEARCH.md Pitfall 8).
- CRITICAL: Howard Fisher (external_id -700011, City Treasurer) is EXCLUDED. NEVER write any INSERT with Fisher's UUID. NEVER research Fisher.
</interfaces>

Apply path: psql CLI with DATABASE_URL from C:/EV-Accounts/.env. Phase 126 confirmed mcp__supabase-local is NOT callable from the bash executor context — use psql CLI as the primary apply and query path. Command form: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/NNN_name_stances.sql` to apply, and `psql "$DATABASE_URL" -c "<query>"` for verification queries. Load DATABASE_URL from C:/EV-Accounts/.env first (do NOT run git commands in C:/EV-Accounts — it is not a git repo).

Beverly Hills roster (external_id → expected migration), from migrations 300-301 [VERIFIED]:
  -200589 Lester Friedman (Mayor, LOCAL_EXEC — directly elected)  → NNN     [this plan]
  -201154 Craig A. Corman (Council Member, LOCAL)                 → NNN+1   [this plan]
  -201153 John A. Mirisch (Council Member, LOCAL)                 → NNN+2   [Plan 02]
  -700010 Sharona R. Nazarian (Council Member, LOCAL)            → NNN+3   [Plan 02]
  -201155 Mary N. Wells (Council Member, LOCAL)                  → NNN+4   [Plan 02]
  -700011 Howard Fisher (City Treasurer)                         → EXCLUDED — no stances ever

NNN = actual next migration confirmed at Wave 0. Expected 714 (713_alhambra_dedup.sql is highest on disk; STATE.md says next migration 714), but Wave 0 MUST verify the DB-applied MAX.

Out-of-scope guard: Do NOT write stances for any external_id outside the target set {-200589, -201153, -201154, -201155, -700010}. NEVER write for -700011 (Fisher). Do NOT create any new offices, districts, or chambers for Beverly Hills — Friedman's Mayor office already exists.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 pre-flight verification [BLOCKING]</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/127-beverly-hills-stances/127-RESEARCH.md (Migration Numbering section + Wave 0 Pre-Flight Queries section + Pitfall 6 topic_key + Pitfall 7 integer-cast filter + Pitfall 8 directly-elected Mayor + Pitfall 9 Fisher exclusion)
    - .planning/STATE.md (Current Position section — confirms "next migration: 714")
    - C:/EV-Accounts/.env (read DATABASE_URL for psql CLI; do NOT run git in C:/EV-Accounts)
  </read_first>
  <action>
    Load DATABASE_URL from C:/EV-Accounts/.env, then run all 8 Wave 0 pre-flight queries via `psql "$DATABASE_URL" -c "<query>"` before writing any migration. Do not skip any query — this task is BLOCKING for all subsequent tasks.

    Query 1 — Confirm highest applied integer migration (determines actual starting number). MUST include the integer-format filter (Pitfall 7 — bare version::int overflows on timestamp-format versions):
    `SELECT MAX(version::int) AS max_applied FROM supabase_migrations.schema_migrations WHERE version ~ '^[0-9]{3}$';`
    If MAX = 713, next migration = 714. If MAX is higher, use MAX+1. Record the confirmed starting number (NNN). NOTE: 706 and 707 appear twice on disk (stances + geofencing); rely on the DB-applied MAX, not the disk listing.

    Query 2 — Confirm 710-713 are specifically applied:
    `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('710', '711', '712', '713') AND version ~ '^[0-9]{3}$' ORDER BY version;`
    Expected: 4 rows. If fewer rows, note which migrations are missing — this changes the starting number.

    Query 3 — Confirm active topic count (must be 44):
    `SELECT COUNT(*) FROM inform.compass_topics WHERE is_active = true;`
    If count differs from 44, run `SELECT topic_key, id FROM inform.compass_topics WHERE is_active = true ORDER BY topic_key;` and capture the live list — do NOT use the cached UUID reference block from 127-RESEARCH.md if the count differs. NOTE: column is topic_key, NOT slug (Pitfall 6).

    Query 4 — Resolve all 5 Beverly Hills target UUIDs (non-contiguous external_ids — use an IN list, NOT a BETWEEN range):
    `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id IN (-200589, -201153, -201154, -201155, -700010) ORDER BY external_id;`
    Expected 5 rows: -700010 Sharona R. Nazarian, -201155 Mary N. Wells, -201154 Craig A. Corman, -201153 John A. Mirisch, -200589 Lester Friedman (ascending order). Capture all 5 UUIDs and the exact stored full_name for each (Nazarian middle-initial check, Assumption A6) for use in this plan and Plans 02-03.

    Query 5 — Confirm Howard Fisher's UUID (informational — to EXCLUDE from all stances):
    `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id = -700011;`
    Expected: 1 row, Howard Fisher. Record Fisher's UUID in SUMMARY explicitly as the DO-NOT-WRITE UUID. Fisher must receive ZERO stance rows in this phase.

    Query 6 — Check for pre-existing BH stance rows (informational; upsert handles them):
    `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-200589, -201153, -201154, -201155, -700010));`

    Query 7 — Confirm Beverly Hills Mayor office exists as LOCAL_EXEC (Friedman directly elected — confirms Pitfall 8: NOT rotational):
    `SELECT d.district_type, o.title, p.full_name FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id = -200589;`
    Expected: district_type=LOCAL_EXEC, title='Mayor', full_name=Lester Friedman. This confirms "Mayor Friedman" is correct (directly elected, not rotational). If district_type is not LOCAL_EXEC, document the deviation but proceed (the UUID is what matters for INSERTs).

    Query 8 — Confirm BH City Council chamber:
    `SELECT c.name FROM essentials.chambers c JOIN essentials.governments g ON g.id = c.government_id WHERE g.geo_id = '0606308' AND c.name != 'City Treasurer';`
    Expected: at least 1 row 'City Council' (a separate City Treasurer chamber also exists — that is fine; the filter excludes it).

    Record all query results in the plan SUMMARY. The confirmed migration starting number (NNN) is the authoritative value for all 5 migration filenames in this phase. Record Fisher's UUID explicitly as the exclusion UUID.
  </action>
  <verify>
    <automated>All 8 queries return results via psql CLI. Starting migration number recorded (expected 714). Topic count confirmed. All 5 BH target UUIDs resolved. Fisher's UUID recorded as exclusion. LOCAL_EXEC Mayor district confirmed for Friedman. City Council chamber returns at least 1 row.</automated>
  </verify>
  <acceptance_criteria>
    - MAX(version::int) query (with `WHERE version ~ '^[0-9]{3}$'` filter) returns a value >= 713; starting migration number (NNN) confirmed and recorded
    - Queries 1+2 results are consistent (if 710-713 all applied, MAX=713 and 4 rows returned)
    - Active topic count is confirmed (44 expected; if different, live topic_key→id list captured)
    - All 5 target UUIDs resolved: -200589 Friedman, -201153 Mirisch, -201154 Corman, -201155 Wells, -700010 Nazarian — names and external_ids match roster; exact stored full_names recorded
    - Fisher's UUID (external_id -700011) recorded in SUMMARY as the explicit DO-NOT-WRITE exclusion UUID
    - Pre-existing stance count recorded (informational)
    - Friedman's Mayor district confirmed as LOCAL_EXEC with title 'Mayor' (or deviation documented)
    - BH City Council chamber query returns at least 1 row
    - All 5 target UUIDs + Fisher's exclusion UUID recorded in SUMMARY for Plans 02-03
  </acceptance_criteria>
  <done>Pre-flight queries run via psql CLI; actual starting migration number (NNN) confirmed, topic count confirmed, all 5 BH target UUIDs + Fisher's exclusion UUID resolved and recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Lester Friedman (Mayor) stances — migration NNN</name>
  <files>C:/EV-Accounts/backend/migrations/NNN_friedman_stances.sql</files>
  <read_first>
    - .planning/phases/127-beverly-hills-stances/127-RESEARCH.md (Lester Friedman section + Local Press Sources + Anti-Patterns + Pitfalls 1-5 + Pitfall 8 directly-elected Mayor + Pitfall 9 Fisher exclusion + Topic UUID Reference Block)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (canonical format: BEGIN/COMMIT, float literals, dollar-quoting, double-cast sources)
    - C:/EV-Accounts/backend/migrations/703_lee_stances.sql (most recent canonical per-local-official format, same city tier)
  </read_first>
  <action>
    Process Lester Friedman (external_id -200589, UUID resolved in Task 1, directly-elected Mayor, LOCAL_EXEC office) strictly after Task 1 completes. Research his public record across all 44 active compass topics, then write, apply, and verify his migration before any other official is processed.

    (a) Research: Search "Lester Friedman Beverly Hills Mayor" and "Lester Friedman Beverly Hills council" across bhcourier.com, beverlyhills.org/government/city-council/meetings-agendas, patch.com/california/beverlyhills, latimes.com, ballotpedia.org. Friedman has served on the council since ~2013 and holds the directly-elected Mayor role — expected yield 7-12 stances. High-probability topics for Beverly Hills (RESEARCH.md "Topics with highest evidence probability"): housing (RHNA resistance), residential-zoning (anti-density), homelessness-response (encampment enforcement in a wealthy enclave), growth-and-development (Rodeo Drive / Beverly Hilton), public-safety-approach (independent BHPD), local-environment, taxes (low-tax municipality), transportation-priorities (Purple Line debates), local-immigration (BH is NOT a sanctuary city). Place each topic with direct evidence on the 1.0-5.0 scale per that topic's axis definition (1=most progressive, 5=most conservative; Beverly Hills positions skew moderate-to-conservative). No INSERT for any topic with no direct evidence — low-probability topics (ukraine-support, judicial-*, tariffs, same-sex-marriage, school-vouchers, redistricting) will most likely be blank.

    (b) Write migration NNN_friedman_stances.sql (NNN = confirmed number from Task 1, expected 714) using the canonical template:
    - Header comment block: "Migration NNN: Lester Friedman Stances"
    - Full 44-topic UUID reference block from 127-RESEARCH.md Topic UUID Reference Block (or live list if Task 1 count differed)
    - BEGIN;
    - One politician_answers INSERT + one politician_context INSERT per evidenced topic, using exactly the paired-INSERT contract from <interfaces>
    - value = float literal (e.g. 4.0) — NEVER bare integer or string
    - sources = ARRAY['https://bhcourier.com/path/specific-article']::text[]::text[] — double-cast, path-bearing URLs (no bare domains)
    - reasoning = $$....$$ dollar-quoting, 2-5 sentences citing specific vote/statement/action
    - Reasoning text uses "Mayor Friedman" — this is CORRECT (directly elected, not rotational; do NOT add any "rotational" qualifier — Pitfall 8)
    - COMMIT;
    - No INSERT INTO essentials.offices, essentials.districts, or essentials.chambers (Friedman's Mayor office already exists)

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/NNN_friedman_stances.sql` (DATABASE_URL from C:/EV-Accounts/.env).

    (d) Verify immediately (before Task 3), substituting Friedman's UUID from Task 1:
    Row count: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{friedman_uuid}';"`
    Unpaired check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{friedman_uuid}' AND pc.politician_id IS NULL;"` — must return 0
    Citation check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{friedman_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Friedman's UUID. Citation check returns 0 for Friedman's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file NNN_friedman_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0` (grep: no `, [1-5]\)` without `.0`)
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Friedman's UUID
    - Uncited-context query returns 0 for Friedman's UUID
    - Reasoning text contains "Mayor Friedman" with NO "rotational" qualifier (he is directly elected)
    - File contains no INSERT INTO essentials.offices, essentials.districts, or essentials.chambers
    - No INSERT references external_id -700011 (Fisher) or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration NNN_friedman_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Mayor Lester Friedman.</done>
</task>

<task type="auto">
  <name>Task 3: Craig A. Corman (Council Member) stances — migration NNN+1</name>
  <files>C:/EV-Accounts/backend/migrations/NNN+1_corman_stances.sql</files>
  <read_first>
    - .planning/phases/127-beverly-hills-stances/127-RESEARCH.md (Craig A. Corman section + Anti-Patterns + Pitfalls 1-5 + Pitfall 9 Fisher exclusion)
    - C:/EV-Accounts/backend/migrations/NNN_friedman_stances.sql (just-written sibling file as in-phase format reference)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process Craig A. Corman (external_id -201154, UUID from Task 1, Council Member) strictly after Task 2 completes — never in parallel (rate-limit rule). Research, write, apply, and verify.

    (a) Research: Search "Craig Corman Beverly Hills council" and "Craig A. Corman Beverly Hills" on bhcourier.com, beverlyhills.org/government/city-council/meetings-agendas, patch.com/california/beverlyhills, beverlyhillsweekly.com, ballotpedia.org. Corman is a long-tenured council member — expected yield 5-9 stances. High-probability topics: housing (housing element compliance votes), residential-zoning, homelessness-response, growth-and-development, public-safety-approach, taxes. Place each evidenced topic on the 1.0-5.0 scale per that topic's axis definition. No INSERT for any topic with no direct evidence. If zero evidence found, still write the migration file with header + BEGIN;/COMMIT; + zero INSERT blocks (ledger-consistency precedent) and note in SUMMARY.

    (b) Write migration NNN+1_corman_stances.sql (expected 715). Same canonical format as Task 2:
    - Header: "Migration NNN+1: Craig A. Corman Stances"
    - Full 44-topic UUID reference block
    - BEGIN;
    - paired politician_answers + politician_context INSERT per evidenced topic
    - value = float literal; sources = ARRAY[...]::text[]::text[] double-cast, path-bearing URLs; reasoning = $$...$$ dollar-quoting citing specific vote/statement/action
    - Reasoning text uses "Council Member Corman" (he is a council member, not the Mayor)
    - COMMIT;

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/NNN+1_corman_stances.sql`.

    (d) Verify immediately, substituting Corman's UUID:
    Row count: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{corman_uuid}';"`
    Unpaired check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{corman_uuid}' AND pc.politician_id IS NULL;"` — must return 0
    Citation check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{corman_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` — must return 0
  </action>
  <verify>
    <automated>For Corman's UUID: unpaired check returns 0 AND citation check returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file NNN+1_corman_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Corman's UUID
    - Uncited-context query returns 0 for Corman's UUID
    - Reasoning text uses "Council Member Corman"
    - No INSERT references external_id -700011 (Fisher) or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration NNN+1_corman_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Craig A. Corman.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (psql CLI / live Supabase) | Writes go directly to live production Supabase via DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Tampering | stance value integrity | mitigate | Evidence-only rule: no INSERT without a direct cited source; per-person uncited-context query must return 0 |
| T-127-02 | Information Disclosure | reasoning text / sources | accept | All data is public-record political positions with public source URLs; no PII |
| T-127-03 | Repudiation | stance provenance | mitigate | Every value paired with a context row carrying path-bearing source URLs (100% citation rate) |
| T-127-04 | Tampering | writing stances for excluded Fisher (-700011) | mitigate | Acceptance criteria assert no INSERT references Fisher's UUID; Wave 0 records Fisher's UUID explicitly as the exclusion target |
| T-127-05 | Tampering | stale migration number collision | mitigate | Wave 0 Query 1+2 confirms actual DB-applied MAX (with integer-format filter) before any file is written; disk has duplicate 706/707 — rely on DB MAX |
| T-127-06 | Tampering | unnecessary Mayor office creation | mitigate | Friedman's LOCAL_EXEC Mayor office already exists (Wave 0 Query 7); acceptance criteria assert no INSERT into essentials.offices/districts/chambers |
| T-127-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After all three tasks, run the partial phase-wide citation check for this plan's officials (Friedman + Corman):
`psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-200589, -201154)) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` must return 0.
</verification>

<success_criteria>
- Wave 0 pre-flight complete; actual starting migration number (NNN) confirmed (BLOCKING gate passed)
- Active topic count confirmed as 44 (or live list captured)
- All 5 BH target UUIDs + Fisher's exclusion UUID resolved and recorded in SUMMARY for Plans 02-03
- Friedman's LOCAL_EXEC Mayor district confirmed (directly elected — "Mayor Friedman" correct)
- Migrations NNN, NNN+1 (2 officials: Friedman, Corman) written, applied, and verified
- 0 unpaired answers, 0 uncited contexts across both officials
- No INSERT references Fisher's UUID (-700011)
- No external_ids outside the 5-target set touched
- Blank-spoke topics documented in SUMMARY (never defaulted to 3.0)
</success_criteria>

<output>
Create `.planning/phases/127-beverly-hills-stances/127-01-SUMMARY.md` when done. Record: confirmed starting migration number (NNN), active topic count, all 5 BH target UUIDs + exact stored full_names (for Plans 02-03), Fisher's exclusion UUID, per-official stance counts for Friedman/Corman, and any blank-spoke topics.
</output>
