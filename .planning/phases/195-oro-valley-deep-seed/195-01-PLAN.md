---
phase: 195-oro-valley-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1305_town_of_oro_valley.sql
autonomous: false
requirements: [SUB-01]
must_haves:
  truths:
    - "A greenfield 'Town of Oro Valley, Arizona, US' government (type='Town', state='AZ', geo_id='0451600') exists"
    - "One 'Town Council' chamber (official_count=7) holds exactly 7 offices: an at-large Mayor + 6 undifferentiated at-large council members"
    - "TWO NEW essentials.districts rows exist for geo_id='0451600' (mtfcc='G4110', state='az'): ONE LOCAL_EXEC (Mayor) + ONE LOCAL (shared by all 6 council offices) — neither existed before this phase (Phase 190 wrote only the geofence)"
    - "The Mayor office links to the LOCAL_EXEC/G4110/0451600 district; ALL 6 council offices link to the SAME single LOCAL/G4110/0451600 district (Torrance precedent) — never a bare geo_id join"
    - "Roster is Winfield(Mayor)/Barrett(VM)/Jones-Ivey/Nicolson/Greene/Murphy/Robb; party=NULL on all 7 (nonpartisan); the stale Bohen/Solomon names are NOT seeded"
    - "The Vice Mayor is a TITLE ANNOTATION on Barrett's council seat (title='Council Member (Vice Mayor)'); the other five council members carry plain 'Council Member'. Exactly 7 offices, no separate Vice Mayor seat"
    - "The current roster is re-verified against a live source at execute time given the active 2026 election BEFORE the migration is applied; the structural migration is applied to production and registered in the ledger; 7 politician UUIDs captured for Plans 02/03"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1305_town_of_oro_valley.sql"
      provides: "government + Town Council chamber + 1 NEW LOCAL_EXEC Mayor district + 1 NEW shared LOCAL council district + 7 politicians (party NULL) + 7 offices (Barrett Vice-Mayor-annotated) + office_id backfill + pre-flight geofence assertion + post-verify DO gates + ledger registration"
      contains: "Town Council"
  key_links:
    - from: "essentials.offices (Mayor)"
      to: "essentials.districts (LOCAL_EXEC / G4110 / 0451600)"
      via: "district_id join scoped by district_type='LOCAL_EXEC' AND mtfcc='G4110' AND state='az'"
      pattern: "LOCAL_EXEC"
    - from: "essentials.offices (6 council members)"
      to: "essentials.districts (ONE shared LOCAL / G4110 / 0451600)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='G4110' AND state='az'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'Town Council'"
      to: "essentials.governments 'Town of Oro Valley, Arizona, US'"
      via: "government_id subquery scoped by name"
      pattern: "Town of Oro Valley, Arizona, US"
---

<objective>
Author and apply the structural migration that seeds the Town of Oro Valley as a greenfield town
government (its own `essentials.governments` row at geo_id `0451600`), a single `Town Council` chamber
(`official_count=7`), the TWO district rows the 7 offices attach to, and 7 politicians/offices (ext_id
`-4009001..-4009007`): an at-large **Mayor** (LOCAL_EXEC) plus 6 undifferentiated at-large council
members (all sharing ONE LOCAL district). Greenfield — no Town of Oro Valley government row exists
today (RESEARCH-verified live 2026-07-10).

CRITICAL DELTA from Tucson/Pima (Pitfall 4): Oro Valley has NO wards and NO council districts — there
is NO geofence loader, NO ArcGIS query, and NO new X00xx mtfcc for this phase. The Town's whole-town
boundary geofence `0451600` (G4110) is already live from Phase 190, but `essentials.districts` has ZERO
rows for it. This migration creates exactly TWO NEW `essentials.districts` rows for `0451600`
(`mtfcc='G4110'`, `state='az'` lowercase): ONE `LOCAL_EXEC` for the Mayor and ONE `LOCAL` shared by
ALL 6 council offices (Torrance precedent — not 6 separate rows). No geometry work.

CRITICAL (RESEARCH divergences from Tucson): (1) party=NULL on all 7 — Oro Valley runs NONPARTISAN
elections (Beaverton precedent), NOT partisan like Tucson; do NOT record a party. (2) Council-seat
titles are plain `'Council Member'` (Torrance block-voting shape), NOT Beaverton's `'(Position N)'`
numbering — unless the Task 2 checkpoint independently confirms formal seat numbering. (3) The Vice
Mayor (Melanie Barrett) is a TITLE ANNOTATION on her council seat
(`title='Council Member (Vice Mayor)'`), NOT a separate office; `role_canonical` stays NULL on all 7.

CRITICAL (Pitfall 2 — the #1 risk of this phase): Oro Valley is mid-way through an actively contested
2026 election (Mayor + 3 council seats up; primary date itself unresolved — July 21 vs Aug 4 2026;
general Nov 3 2026; sources conflict on incumbent re-election intent). The current roster is valid for
`≤7 days` from the 2026-07-10 research date. Task 2 is a SUBSTANTIVE, non-perfunctory BLOCKING
roster-currency re-verification against a live source BEFORE apply — this is a genuine decision point,
not a copy-paste formality.

Purpose: Satisfies the government/roster half of SUB-01 and ROADMAP success criterion #1 (govt +
council roster seeded, election method verified). The existing G4110→(LOCAL,LOCAL_EXEC) routing map in
`essentialsService.ts` covers both the Mayor and the 6 at-large council members with NO code change, so
applying this migration makes every Oro Valley address resolve to the at-large Mayor + all 6 council
members. Plans 02 (headshots) and 03 (stances) consume the 7 politician UUIDs captured here.
Output: `C:/EV-Accounts/backend/migrations/1305_town_of_oro_valley.sql` (structural, registered),
applied to production, with the 7 UUIDs recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/195-oro-valley-deep-seed/195-RESEARCH.md
@.planning/phases/195-oro-valley-deep-seed/195-PATTERNS.md
@.planning/phases/195-oro-valley-deep-seed/195-VALIDATION.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB access. Task 1 (author the `.sql` file) is the only executor
file action. Task 2 (SUBSTANTIVE BLOCKING roster-currency re-verify + apply + UUID capture) and Task 3
(post-apply assertions) are ORCHESTRATOR-RUN via `psql -f`/`psql -tAc` against
`C:/EV-Accounts/backend/.env` `DATABASE_URL`. A migration authored but not applied is a false positive —
the apply MUST happen before Plans 02/03. All backend files live in the SEPARATE `C:/EV-Accounts` repo
(`master`→Render) — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (live-verified in 195-RESEARCH.md / 195-PATTERNS.md — no exploration needed):
- essentials.governments (id, name, type, state, city, geo_id): insert 'Town of Oro Valley, Arizona,
  US', type='Town' (per RESEARCH Architecture diagram — confirm this matches the governments.type
  convention used by existing rows at author time; Beaverton's own file used the generic 'LOCAL' tier
  tag, so verify which value the AZ/nonpartisan precedent expects and note any deviation), state='AZ'
  (UPPERCASE free-text), city='Oro Valley', geo_id='0451600'. Greenfield. Idempotent WHERE NOT EXISTS
  on name.
- essentials.chambers (id, name, name_formal, government_id, official_count): name='Town Council',
  name_formal='Oro Valley Town Council', official_count=7 (Mayor + 6 council), government_id via name
  subquery. The auto-generated `slug` column must NEVER appear in the INSERT column list. Idempotent
  WHERE NOT EXISTS on (name, government_id).
- essentials.districts (id, district_type, state, geo_id, label, mtfcc) — TWO NEW rows, BOTH reusing
  the ALREADY-LIVE Phase-190 geofence (NO geometry work):
  (1) 1 row district_type='LOCAL_EXEC', state='az' (lowercase), geo_id='0451600',
      label='Town of Oro Valley (Mayor)', mtfcc='G4110'. Idempotent WHERE NOT EXISTS on
      (geo_id, district_type, state).
  (2) 1 row district_type='LOCAL', state='az' (lowercase), geo_id='0451600',
      label='Town of Oro Valley (At-Large)', mtfcc='G4110'. Idempotent WHERE NOT EXISTS on
      (geo_id, district_type, state). ALL 6 council offices share this SINGLE row (Torrance precedent).
  Use `label`, NOT name_formal (no such column). Do NOT copy Beaverton's mtfcc=NULL — use 'G4110'.
- essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed,
  is_vacant, is_incumbent, external_id, office_id): ext_id -4009001..-4009007; external_id UNIQUE
  (ON CONFLICT (external_id) DO UPDATE SET is_active=EXCLUDED.is_active). party=NULL on ALL 7
  (nonpartisan — do NOT record a party value). is_active=true, is_incumbent=true, is_vacant=false,
  is_appointed=false on all 7.
- essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state,
  is_appointed_position, is_vacant, role_canonical): representing_state='AZ' (UPPERCASE),
  is_appointed_position=false, role_canonical=NULL on all 7. Guard on (district_id, politician_id).
- ext_id → seat → title (RESEARCH roster, 2026-07-10 — re-verify at Task 2):
    -4009001 Joseph "Joe" Winfield        Mayor            title='Mayor'                        (LOCAL_EXEC / G4110 / 0451600)
    -4009002 Melanie Barrett              Council (VM)     title='Council Member (Vice Mayor)'  (LOCAL / G4110 / 0451600)
    -4009003 Joyce Jones-Ivey             Council          title='Council Member'               (LOCAL / G4110 / 0451600)
    -4009004 Josh Nicolson                Council          title='Council Member'               (LOCAL / G4110 / 0451600)
    -4009005 Dr. Harry "Mo" Greene II     Council          title='Council Member'               (LOCAL / G4110 / 0451600)
    -4009006 Mary Murphy                  Council          title='Council Member'               (LOCAL / G4110 / 0451600)
    -4009007 Elizabeth Robb               Council          title='Council Member'               (LOCAL / G4110 / 0451600)
- CRITICAL geo_id collision guard: 0451600 also appears in geofence_boundaries (state stored as FIPS
  '04', an unrelated convention). Every office↔district join MUST scope by district_type + mtfcc +
  state='az', never a bare geo_id. Mayor join scopes LOCAL_EXEC/G4110/az; each council join scopes
  LOCAL/G4110/az.
- One-chamber-two-district-types precedent (live-confirmed): Torrance/Beaverton hold an at-large
  LOCAL_EXEC Mayor + LOCAL council offices in a single chamber. Torrance's 6 undifferentiated
  'Council Member' offices ALL share ONE LOCAL row — copy that shape, not Beaverton's numbered titles.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the structural Town of Oro Valley migration</name>
  <files>C:/EV-Accounts/backend/migrations/1305_town_of_oro_valley.sql</files>
  <read_first>
    - .planning/phases/195-oro-valley-deep-seed/195-PATTERNS.md (lines 21-228 — the full structural pattern: header/warning-comment discipline, pre-flight hard-abort guard, government/chamber INSERT, the TWO NEW district rows with the CRITICAL mtfcc='G4110' delta from Beaverton's NULL, the WITH ins_p...INSERT INTO offices per-official block, the plain 'Council Member' title delta from Beaverton's numbered '(Position N)', the Vice Mayor title annotation on Barrett, office_id backfill, the four post-verify DO gates + the added Vice-Mayor-annotation gate, ledger footer, and the Shared Patterns section lines 398-450)
    - .planning/phases/195-oro-valley-deep-seed/195-RESEARCH.md (§ Summary roster table; § Architecture Patterns 1 & 2; § Common Pitfalls 3, 4, 6, 7; § Code Examples — the Torrance precedent + external_id/ledger probes)
    - C:/EV-Accounts/backend/migrations/1131_beaverton_city_council.sql (PRIMARY analog — read in full: government/chamber/district/office INSERT shapes, pre-flight + post-verify DO gates, the office_id backfill, ledger footer; copy the file STRUCTURE verbatim while OVERRIDING two of Beaverton's own choices — mtfcc must be 'G4110' not NULL, and council titles must be plain 'Council Member' not '(Position N)')
    - C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql (read only the '(Chair)' title-annotation gate — replicate that gate shape for '(Vice Mayor)' on Barrett's -4009002 seat, asserting exactly 1 matching office; Beaverton has no equivalent gate)
  </read_first>
  <action>
    First run `ls C:/EV-Accounts/backend/migrations | sort` to confirm on-disk MAX (RESEARCH computed
    disk MAX=1304, ledger MAX=1296, next structural=1305 on 2026-07-10 — but Pitfall 6 documents a real
    same-day 1296 collision, so re-check now). Name this file actual-disk-MAX+1 (provisionally
    `1305_town_of_oro_valley.sql`). If disk MAX drifted, use actual disk-MAX+1 and note the deviation.

    Author a STRUCTURAL migration wrapped in BEGIN/COMMIT. Pre-flight DO block: assert the whole-town
    geofence exists — COUNT(*) >= 1 in essentials.geofence_boundaries WHERE geo_id='0451600' AND
    mtfcc='G4110' — RAISE EXCEPTION 'Oro Valley G4110 geofence missing — run Phase 190 first' otherwise
    (codifies Pitfall 4). Also add the standard idempotency hard-abort guard: RAISE EXCEPTION if the
    'Town of Oro Valley, Arizona, US' government already exists.

    Insert the greenfield government 'Town of Oro Valley, Arizona, US' (type='Town', state='AZ'
    uppercase, city='Oro Valley', geo_id='0451600') idempotently (WHERE NOT EXISTS on name). Confirm the
    governments.type value against the existing-rows convention at author time (RESEARCH diagram calls
    for 'Town'; note any deviation in a comment). Insert the 'Town Council' chamber
    (name_formal='Oro Valley Town Council', official_count=7) scoped to that government's id via name
    subquery, idempotent on (name, government_id). Do NOT list the auto-generated `slug` column.

    Insert TWO NEW district rows, BOTH reusing the existing Phase-190 geofence (NO geometry work):
      (1) ONE LOCAL_EXEC Mayor row (district_type='LOCAL_EXEC', state='az' lowercase, geo_id='0451600',
          label='Town of Oro Valley (Mayor)', mtfcc='G4110'), idempotent WHERE NOT EXISTS on
          (geo_id, district_type, state).
      (2) ONE LOCAL at-large row (district_type='LOCAL', state='az' lowercase, geo_id='0451600',
          label='Town of Oro Valley (At-Large)', mtfcc='G4110'), idempotent WHERE NOT EXISTS on
          (geo_id, district_type, state). ALL 6 council offices share THIS SINGLE row.
    CRITICAL: mtfcc='G4110' on BOTH rows — do NOT copy Beaverton's NULL. Pitfall 4: without these rows
    the offices attach to nothing.

    Seed 7 officials using the WITH ins_p AS (INSERT INTO politicians ... RETURNING id) INSERT INTO
    offices ... FROM districts d CROSS JOIN ins_p p WHERE ... block, one per official. Scoping:
      - Mayor Winfield (-4009001) → title='Mayor'; office joins the LOCAL_EXEC row:
        d.geo_id='0451600' AND d.district_type='LOCAL_EXEC' AND d.state='az' AND d.mtfcc='G4110'.
      - Barrett (-4009002) → title='Council Member (Vice Mayor)'  ← Vice Mayor title annotation; office
        joins d.geo_id='0451600' AND d.district_type='LOCAL' AND d.state='az' AND d.mtfcc='G4110'.
      - Jones-Ivey (-4009003) → 'Council Member' (same LOCAL join)
      - Nicolson (-4009004) → 'Council Member' (same LOCAL join)
      - Greene (-4009005) → 'Council Member' (same LOCAL join)
      - Murphy (-4009006) → 'Council Member' (same LOCAL join)
      - Robb (-4009007) → 'Council Member' (same LOCAL join)
    All 6 council offices join the SAME single LOCAL/G4110/0451600 row (Torrance precedent). Each join
    scopes district_type + mtfcc + state='az' plus geo_id — NEVER a bare geo_id. All 7 offices:
    representing_state='AZ' uppercase, is_appointed_position=false, role_canonical=NULL. Only Barrett's
    office carries the `(Vice Mayor)` marker — the other five council members and the Mayor carry NO
    `(Vice Mayor)` suffix, and the five plain council seats use the exact title 'Council Member' (NOT
    '(Position N)' — Torrance shape, Pitfall 3). Guard each office INSERT on NOT EXISTS (...
    o.district_id=d.id AND o.politician_id=p.id). Politicians: party=NULL on ALL 7 (nonpartisan — do NOT
    record a party value; Beaverton precedent), is_active=true, is_incumbent=true, is_vacant=false,
    is_appointed=false on all 7.

    office_id backfill: UPDATE politicians SET office_id=o.id FROM offices o WHERE o.politician_id=p.id
    AND p.external_id IN (-4009001,-4009002,-4009003,-4009004,-4009005,-4009006,-4009007) AND
    p.office_id IS NULL.

    Post-verify DO gate (RAISE EXCEPTION → rollback unless ALL hold): (a) exactly 1 government named
    'Town of Oro Valley, Arizona, US'; (b) exactly 7 offices under the 'Town Council' chamber — 1 on the
    LOCAL_EXEC/G4110/0451600 district and 6 on the ONE LOCAL/G4110/0451600 district; (c) the LOCAL_EXEC
    district holds exactly 1 office AND the LOCAL district holds exactly 6 offices (NOTE: unlike Tucson's
    "each district holds exactly 1" — here all 6 council share ONE LOCAL row, so assert count=6 on it);
    (d) all 7 politicians have party IS NULL (nonpartisan gate); (e) EXACTLY 1 office carries a
    Vice-Mayor annotation (o.title LIKE '%(Vice Mayor)%') AND that office's politician has
    external_id=-4009002 (Barrett) — no other office may carry it; (f) section-split: 0 offices attached
    under any government OTHER than Town of Oro Valley via these 2 districts; (g) office_id backfill: 0
    NULLs remaining across the 7 external_ids.

    Register OUTSIDE BEGIN/COMMIT: INSERT INTO supabase_migrations.schema_migrations (version) VALUES
    ('1305') ON CONFLICT (version) DO NOTHING (substitute actual disk-MAX+1). Do NOT invent politician
    UUIDs — offices resolve politician_id via the ins_p CTE at apply time.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1305_town_of_oro_valley.sql"; test -f "$F" && grep -qF "Town Council" "$F" && grep -qF "Town of Oro Valley, Arizona, US" "$F" && grep -qF "'LOCAL_EXEC'" "$F" && grep -qF "G4110" "$F" && grep -qF "'0451600'" "$F" && grep -qF "Council Member (Vice Mayor)" "$F" && [ "$(grep -c "'Council Member'" "$F")" -ge 5 ] && ! grep -qF "Council Member (Position" "$F" && ! grep -qiE "party[^a-z]*=[^a-z]*'(Democratic|Republican)" "$F" && [ "$(grep -c 'o.district_id = d.id AND o.politician_id = p.id' "$F")" = "7" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1305).
    - Grep confirms `Town Council` and `Town of Oro Valley, Arizona, US` present; government type='Town' (or noted deviation), geo_id='0451600'.
    - Grep confirms a NEW `LOCAL_EXEC` districts INSERT with geo_id='0451600', mtfcc='G4110', state='az' AND a NEW `LOCAL` districts INSERT with geo_id='0451600', mtfcc='G4110', state='az' (Pitfall 4 — the two rows Phase 190 never wrote; mtfcc='G4110' NOT NULL).
    - Grep count of the office↔district guard `o.district_id = d.id AND o.politician_id = p.id` is exactly 7 (no 8th office).
    - Mayor office joins LOCAL_EXEC/G4110/0451600; all 6 council offices join the SAME LOCAL/G4110/0451600 row — never a bare geo_id.
    - Nonpartisan (Beaverton delta): NO office title contains `(Position` (plain 'Council Member', Torrance shape); NO politician row records a 'Democratic'/'Republican' party; `role_canonical` NULL for all 7.
    - Vice Mayor modeling: Barrett's office title='Council Member (Vice Mayor)'; the other five council seats + Mayor carry NO `(Vice Mayor)` marker.
    - Grep confirms 7 external_id literals -4009001..-4009007.
    - Pre-flight DO block asserts the G4110/0451600 geofence exists; post-verify DO gate present with assertions (a-g) — including LOCAL district holds exactly 6, party-NULL gate, and exactly-one-Vice-Mayor on -4009002.
    - Ledger-registration line `('1305'` (or actual number) present OUTSIDE COMMIT.
  </acceptance_criteria>
  <done>Structural migration authored: greenfield Town of Oro Valley government + Town Council chamber + 1 NEW LOCAL_EXEC/G4110 Mayor district + 1 NEW shared LOCAL/G4110 council district + 7 offices/politicians (party NULL; Barrett carries the Vice Mayor title annotation; five plain 'Council Member'), backfill, pre-flight geofence + post-verify gates, ledger registration — ready to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SUBSTANTIVE BLOCKING roster-currency re-verification during the active 2026 election + apply structural migration (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/195-oro-valley-deep-seed/195-RESEARCH.md (§ Summary + § Common Pitfall 1 (stale Bohen/Solomon) + § Common Pitfall 2 (active 2026 election, unresolved primary date) + § Assumptions Log A1/A4 + § Open Question 3 + § Metadata "Valid until: 7 days OR LESS for roster-currency")
    - .planning/phases/195-oro-valley-deep-seed/195-VALIDATION.md (Manual-Only Verifications — the roster-currency test instructions: fresh Playwright fetch OR fresh news search; certified-primary check; outgoing-vs-incoming decision with the operator)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. This is NOT a copy-paste formality — it is the genuine #1 risk of this phase (an active, contested election with an imminent, date-uncertain primary). The operator re-confirms the roster against a LIVE source, checks whether the primary has been certified, and — if it has — explicitly decides whether to seed the outgoing or incoming officeholders; only then does the ORCHESTRATOR apply migration 1305 and capture the 7 UUIDs. See &lt;how-to-verify&gt;.</action>
  <what-built>
    Migration 1305 (Task 1) is authored but NOT applied. RESEARCH (2026-07-10) documented the current
    7-person roster (Winfield Mayor; Barrett VM / running for Mayor; Jones-Ivey; Nicolson; Greene;
    Murphy; Robb) as valid for `≤7 days` because Oro Valley is in an actively contested 2026 election:
    Mayor Winfield confirmed NOT seeking re-election, 3 council seats up, sources conflicting on whether
    Barrett/Jones-Ivey/Nicolson are running again, and the PRIMARY DATE itself unresolved (July 21 vs
    Aug 4 2026; general Nov 3 2026). RESEARCH also caught a persistent STALE WebSearch composite naming
    Bohen/Solomon (replaced in July 2024) — those two must NOT be seeded. Applying to production
    (mcp__supabase-local IS live prod) without a fresh re-check risks seeding a roster that a certified
    primary has already superseded (T-195-04 data-honesty threat).
  </what-built>
  <how-to-verify>
    1. Establish the authoritative election dates + current candidate/officeholder facts from a LIVE
       source — attempt a fresh Playwright fetch of the WAF-blocked orovalleyaz.gov Town-Clerk /
       elections page (mcp__playwright__browser_navigate, since curl/WebFetch return 403), OR a fresh
       news/civic search (iloveov.com, Tucson Local Media/Explorer News, tucson.com, AZPM). Resolve the
       real primary date (July 21 vs Aug 4 2026) and confirm the Nov 3 2026 general.
    2. Determine whether the primary has occurred AND whether results are CERTIFIED as of the execute
       date. A nonpartisan AZ primary can produce an outright >50% winner (no Nov general needed).
    3. Decide the roster to seed WITH THE OPERATOR:
       - If the primary has NOT yet occurred / is not certified → seed the current 7-person roster
         (Winfield/Barrett/Jones-Ivey/Nicolson/Greene/Murphy/Robb) documented in RESEARCH — these are
         who represents residents today.
       - If a primary result IS certified and has changed any officeholder → explicitly decide with the
         operator whether to seed the outgoing (still-sitting until Nov/January) or the newly-certified
         incoming officeholder(s), and patch the affected politician rows in 1305 BEFORE apply. Never
         silently assume "no change since research." Never seed Bohen/Solomon.
    4. Confirm the seat-title convention: default is plain 'Council Member' (Torrance block-voting,
       Open Question 1). If the live source shows Oro Valley formally numbers its seats ('Position N'),
       switch the five plain titles to the Beaverton '(Position N)' convention before apply (all still
       share the ONE LOCAL district row). Confirm Barrett is still the sitting Vice Mayor (annotation on
       her seat); if the VM changed, move the '(Vice Mayor)' suffix to the correct sitting member.
    5. On approval, the ORCHESTRATOR applies via
       `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1305_town_of_oro_valley.sql`, captures
       the 7 UUIDs (`SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id
       IN (-4009001,-4009002,-4009003,-4009004,-4009005,-4009006,-4009007) ORDER BY external_id`) into
       195-01-SUMMARY.md for Plans 02/03, records the execute-date roster decision (before/after primary,
       what was seeded and why), and commits via `git -C "C:/EV-Accounts"`.
  </action>
  <acceptance_criteria>
    - The authoritative primary date + general date + certification status are established from a LIVE source at execute time (not assumed from RESEARCH), and recorded in 195-01-SUMMARY.md.
    - The seeded roster is explicitly decided with the operator: current 7 if pre/uncertified primary, or the operator-chosen outgoing/incoming set if a certified result changed a seat. Bohen/Solomon NOT seeded.
    - Seat-title convention confirmed (plain 'Council Member' default, or '(Position N)' if a live source shows formal numbering); Barrett confirmed as sitting Vice Mayor (or the annotation moved to the correct member).
    - Migration applies with the in-transaction post-verify DO gate passing (no RAISE EXCEPTION → COMMIT), including party-NULL, LOCAL-holds-6, and exactly-one-Vice-Mayor on the confirmed holder.
    - `psql -tAc "SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1305'"` returns 1 (structural registered).
    - The 7 politician UUIDs captured into 195-01-SUMMARY.md (external_id → UUID → full_name) for Plans 02/03; migration committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live roster + election dates + certification status are confirmed, the seed roster is explicitly decided (outgoing vs incoming if applicable), and the migration is applied + UUIDs captured — or describe the roster/title/Vice-Mayor delta to patch first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply structural assertions (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB assertions, no files modified</files>
  <read_first>
    - .planning/phases/195-oro-valley-deep-seed/195-VALIDATION.md (Per-Task Verification Map — 195-01 row: 1 govt + 1 chamber + 7 offices, party NULL, Vice Mayor annotation, scoped joins, 0 section-split)
    - .planning/phases/195-oro-valley-deep-seed/195-RESEARCH.md (§ Common Pitfall 4 — the two NEW districts rows; § Security Domain — section-split + wrong-district-attach mitigations)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). After apply, run seed assertions via
    `psql "$DATABASE_URL" -tAc` and record in 195-01-SUMMARY.md. Assert: (a) exactly 1 government
    'Town of Oro Valley, Arizona, US' at geo_id='0451600'; (b) exactly 7 offices under the 'Town
    Council' chamber of that government — 1 on the LOCAL_EXEC/G4110/0451600 district and 6 on the ONE
    LOCAL/G4110/0451600 district; (c) the LOCAL_EXEC district holds exactly 1 office AND the LOCAL
    district holds exactly 6 offices; (d) both new districts rows exist (LOCAL_EXEC + LOCAL,
    geo_id='0451600', mtfcc='G4110', state='az'); (e) all 7 politicians have party IS NULL; (f)
    DISTINCT district state on the 2 linked districts = only 'az' (lowercase); (g) section-split: 0
    offices reachable from these 2 districts under any government other than Town of Oro Valley; (h)
    Vice Mayor annotation: exactly 1 of the 7 offices has title LIKE '%(Vice Mayor)%' AND it is the
    office whose politician external_id=-4009002 (Barrett), and no other office carries it. Any mismatch
    blocks the phase — do NOT proceed to Plan 02.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='Town of Oro Valley, Arizona, US' AND geo_id='0451600')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0451600' AND d.state='az' AND d.mtfcc='G4110' AND d.district_type IN ('LOCAL','LOCAL_EXEC'))=7 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0451600' AND d.district_type='LOCAL_EXEC' AND d.state='az' AND d.mtfcc='G4110')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0451600' AND d.district_type='LOCAL' AND d.state='az' AND d.mtfcc='G4110')=6 AND NOT EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id IN (-4009001,-4009002,-4009003,-4009004,-4009005,-4009006,-4009007) AND party IS NOT NULL) AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0451600' AND d.mtfcc='G4110' AND d.state='az' AND o.title LIKE '%(Vice Mayor)%')=1 AND (SELECT p.external_id FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.politicians p ON p.id=o.politician_id WHERE d.geo_id='0451600' AND d.mtfcc='G4110' AND o.title LIKE '%(Vice Mayor)%')=-4009002"</automated>
  </verify>
  <acceptance_criteria>
    - Greenfield Town of Oro Valley government count = 1 (geo_id 0451600).
    - 7 offices under 'Town Council' — 1 on LOCAL_EXEC/G4110/0451600, 6 on the ONE LOCAL/G4110/0451600 row.
    - LOCAL_EXEC district holds exactly 1 office; LOCAL district holds exactly 6 offices.
    - Both new districts rows (LOCAL_EXEC + LOCAL, 0451600/G4110/az) exist (Pitfall 4).
    - party IS NULL for all 7 politicians (nonpartisan).
    - DISTINCT linked-district state = only 'az'.
    - Section-split query returns 0 rows (no office leaked under a non-Oro-Valley government).
    - Vice Mayor annotation: exactly 1 office carries `(Vice Mayor)` and it is Barrett's (-4009002) seat.
    - The combined boolean SELECT returns `t`.
  </acceptance_criteria>
  <done>Live production confirms the greenfield Town of Oro Valley government, 7 offices (at-large Mayor on the new LOCAL_EXEC district + 6 council members on the ONE shared LOCAL district), party NULL, the Vice Mayor title annotation on Barrett's seat only, lowercase 'az' casing, clean section-split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | Hand-authored static SQL crosses into live `essentials` via `psql -f` (service-role) |
| Phase-190 G4110/0451600 geofence → new offices | The 2 new office↔district joins consume the existing whole-town geofence; wrong casing/type or a bare-geo_id collision silently mislinks |
| active 2026 election → seeded roster | An imminent, date-uncertain primary can supersede the roster between research and execute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-01 | Tampering | office↔chamber↔government section-split | mitigate | Chamber scoped to the Oro Valley government id subquery; post-verify DO gate (f) + Task 3 section-split assertion → 0 rows or the transaction rolls back |
| T-195-02 | Tampering | office↔district WHERE vs the 0451600 multi-use geo_id | mitigate | Every join scopes district_type + mtfcc='G4110' + state='az' (Mayor: LOCAL_EXEC; council: the ONE LOCAL row); migration never joins bare geo_id; post-verify asserts 1 LOCAL_EXEC + 6 LOCAL offices on the correct rows |
| T-195-03 | Tampering | missing LOCAL/LOCAL_EXEC districts rows (Pitfall 4) | mitigate | Migration creates BOTH new rows (0451600/G4110/az); pre-flight asserts the geofence exists; post-verify gate (d) + Task 3 assert both rows exist and hold exactly the Mayor / 6 council offices |
| T-195-04 | Tampering | seeding a roster superseded by the active 2026 election (data-honesty) | mitigate | Task 2 is a SUBSTANTIVE BLOCKING roster-currency re-verify against a LIVE source + certified-primary check + explicit outgoing/incoming decision with the operator; stale Bohen/Solomon never seeded; execute-date roster decision recorded in the SUMMARY |
| T-195-VM | Tampering | Vice Mayor modeling (double-count / mislabel / stale annotation) | mitigate | Title-annotation model: exactly 7 offices (no separate 8th seat), the `(Vice Mayor)` suffix on Barrett's -4009002 seat ONLY (post-verify gate (e) + Task 3 assert exactly one, on -4009002); Task 2 re-confirms the sitting VM before apply |
| T-195-PARTY | Information disclosure | recording a party on a nonpartisan office | mitigate | party=NULL on all 7 (Beaverton nonpartisan convention); post-verify gate (d) + Task 3 assert 0 non-NULL party; diverges deliberately from Tucson |
| T-195-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `C:/EV-Accounts/backend/.env`; applied by the inline orchestrator; never hardcoded in the migration |
| T-195-SC | Tampering | package installs | accept | No new npm/pip/cargo installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 1 greenfield Town of Oro Valley government (geo_id 0451600).
- 1 'Town Council' chamber; 7 offices — at-large Mayor on a NEW LOCAL_EXEC/G4110 district + 6 council members on ONE shared NEW LOCAL/G4110 district.
- party NULL on all 7 (nonpartisan); Vice Mayor = title annotation on Barrett's seat; exactly one `(Vice Mayor)`; no separate 8th office.
- Section-split 0 rows; casing lowercase 'az' on districts; structural migration registered.
- Roster re-verified against a live source at execute time (active-election decision recorded); 7 politician UUIDs captured for Plans 02/03.
</verification>

<success_criteria>
The government/roster half of SUB-01 and ROADMAP #1 is TRUE in production (Mayor + 6 at-large council
members, election method verified, at-large routing enabled via the two new districts rows); the
migration is applied + registered; the UUID manifest and execute-date roster decision are recorded.
</success_criteria>

<output>
Create `.planning/phases/195-oro-valley-deep-seed/195-01-SUMMARY.md` when done — include the 7
external_id → UUID → full_name manifest (required by Plans 02 and 03), the authoritative primary/general
dates + certification status established at execute time, the explicit roster decision (current vs
outgoing/incoming), and record Barrett as Vice Mayor surfaced via the `title` annotation.
</output>
</content>
