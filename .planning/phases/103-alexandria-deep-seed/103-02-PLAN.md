---
phase: 103-alexandria-deep-seed
plan: 02
type: execute
wave: 2
depends_on:
  - 103-01
files_modified:
  - C:/EV-Accounts/backend/migrations/313_acps_school_board.sql
autonomous: true
requirements:
  - VA-DEEP-02
tags:
  - virginia
  - alexandria
  - school-board
  - migration
  - g5420
must_haves:
  truths:
    - "Alexandria City Public Schools (ACPS) TIGER UNSD geo_id is identified and recorded"
    - "essentials.geofence_boundaries has 1 row for ACPS geo_id with mtfcc='G5420' and state='51'"
    - "essentials.governments has exactly 1 row with name='Alexandria City Public Schools, Virginia, US', state='VA', geo_id=<ACPS_geo_id>, city=NULL, type='LOCAL'"
    - "essentials.chambers has exactly 1 row with name='School Board', name_formal='Alexandria City Public Schools Board' linked to the ACPS government"
    - "essentials.districts has exactly 1 SCHOOL row with geo_id=<ACPS_geo_id>, state='va', mtfcc='G5420', label='Alexandria City Public Schools'"
    - "essentials.politicians has 9 rows with external_id BETWEEN -<ACPS_geo_id>009 AND -<ACPS_geo_id>001, all party=NULL, is_appointed=false"
    - "essentials.offices has 9 rows linked to the ACPS SCHOOL district, all title='School Board Member' (or chair/vc title variant), representing_state='VA', is_appointed_position=false"
    - "All 9 ACPS politicians have office_id populated"
    - "Section-split detector returns 0 rows for ACPS geo_id / mtfcc='G5420' / state='va'"
    - "Migration 313 applied with all 7 post-verification gates passing"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/313_acps_school_board.sql"
      provides: "ACPS school board migration with G5420 geofence + 9 board members"
      contains: "Alexandria City Public Schools, Virginia, US"
  key_links:
    - from: "essentials.geofence_boundaries"
      to: "essentials.districts (SCHOOL)"
      via: "geo_id match"
      pattern: "mtfcc='G5420' AND state='va' AND district_type='SCHOOL'"
    - from: "essentials.offices"
      to: "essentials.districts (ACPS SCHOOL)"
      via: "district_id"
      pattern: "9 offices linked to the single SCHOOL district"
---

<objective>
Seed the Alexandria City Public Schools (ACPS) school board — 9 members under a single TIGER UNSD SCHOOL district — by creating and applying migration 313 (VA-DEEP-02). Because no Virginia G5420 (school district) rows are currently loaded in geofence_boundaries, this migration must INSERT the ACPS G5420 row directly (per D-03) before creating the SCHOOL district / government / chamber / politicians / offices. Follows migration 254 (OR school districts) structure, collapsed to a single district.

Purpose: Allows any Alexandria resident to see all 9 ACPS board members in the SCHOOL section of their representatives view. Completes VA-DEEP-02 — required for VA-DEEP-03 headshots (Plan 03) which needs the politician UUIDs from this migration.

Output: One SQL migration file (313_acps_school_board.sql) applied to production Supabase. After apply: 1 G5420 geofence row + 1 ACPS government + 1 School Board chamber + 1 SCHOOL district + 9 politicians + 9 offices, all idempotent and section-split-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/103-alexandria-deep-seed/103-CONTEXT.md
@.planning/phases/103-alexandria-deep-seed/103-PATTERNS.md
@C:/EV-Accounts/backend/migrations/254_or_school_districts.sql
@C:/EV-Accounts/backend/migrations/257_ca_city_school_boards.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Look up ACPS TIGER UNSD geo_id + verify board roster from acps.k12.va.us</name>
  <files>
    (read-only research; no files modified)
  </files>
  <read_first>
    - C:/Transparent Motivations/essentials/.planning/phases/103-alexandria-deep-seed/103-CONTEXT.md (D-01, D-02, D-20)
  </read_first>
  <action>
    The Alexandria City Public Schools (ACPS) TIGER UNSD (G5420 "Unified School District") geo_id is UNKNOWN at plan time. Find it via the following ordered fallback:

    (1) PRIMARY — query the live Supabase DB via `mcp__supabase-local__execute_sql`:
        SELECT geo_id, mtfcc, state FROM essentials.geofence_boundaries
        WHERE mtfcc IN ('G5420', 'G5400', 'G5410')
          AND state = '51';
        Expected: 0 rows (per D-03). If a row already exists matching Alexandria City SD, use that geo_id and skip the geofence INSERT in Task 2.

    (2) SECONDARY — query Census TIGER metadata. ACPS is a Unified School District covering the city of Alexandria. Census GEOID format for school districts is `{STATE_FIPS}{LDU_ID}` — Virginia FIPS=51. For Alexandria City Public Schools (UNSD), the LEAID is 5100090 (Virginia LEAIDs start with 51), so the TIGER GEOID is 5100090. Confirm by fetching the TIGER 2024 UNSD layer at https://www2.census.gov/geo/tiger/TIGER2024/UNSD/ and checking the VA UNSD shapefile metadata, OR by querying the NCES Common Core of Data which lists Alexandria City Public Schools with LEAID 5100090.

    (3) TERTIARY — if (2) cannot be confirmed, ask user before proceeding (this is a blocking research output).

    Record the discovered ACPS_GEOID for use in Task 2. Throughout the rest of this plan, the literal string `{ACPS_GEOID}` will be replaced by the actual digit string (e.g., '5100090').

    (4) ROSTER VERIFICATION — fetch https://www.acps.k12.va.us/school-board/members-of-the-school-board to confirm the 9 current board members and their official titles. Expected per CONTEXT.md:
        Chair: Michelle Rief
        Vice Chair: Christopher Harris
        Members: Abdalla, Beaty, Carmichael Booz, Kenley, Reyna, Scioscia, Simpson Baird
        Confirm full first+last names and which member is Chair vs Vice Chair vs Member. Office titles to use:
        - Chair: 'School Board Chair'
        - Vice Chair: 'School Board Vice Chair'
        - Members: 'School Board Member'

    (5) PRE-EXISTING DATA CHECK — assuming ACPS_GEOID is known, run:
        SELECT COUNT(*) FROM essentials.governments
          WHERE name = 'Alexandria City Public Schools, Virginia, US';
        SELECT COUNT(*) FROM essentials.politicians
          WHERE external_id BETWEEN -({ACPS_GEOID}::bigint * 1000 + 9) AND -({ACPS_GEOID}::bigint * 1000 + 1);
        (e.g., for ACPS_GEOID=5100090 the range is BETWEEN -5100090009 AND -5100090001)
        Both must return 0 (per D-07). If non-zero, STOP and report.

    Record in task notes: ACPS_GEOID value, 9 verified names with role assignments, and the computed external_id range.
  </action>
  <verify>
    <automated>
      mcp__supabase-local__execute_sql confirms 0 pre-existing VA G5420 rows; ACPS_GEOID identified (e.g., 5100090); acps.k12.va.us fetch returns 9 board member names with Chair/VC/Member role labels; pre-existing government and politician counts return 0.
    </automated>
  </verify>
  <acceptance_criteria>
    - Task notes record a specific numeric ACPS_GEOID value (digits only, no spaces)
    - Task notes record 9 full names with role classifications (1 Chair + 1 Vice Chair + 7 Members)
    - external_id assignment is recorded: -ACPS_GEOID001 (Chair) through -ACPS_GEOID009 (last member alphabetically)
    - Pre-existing government count query returns 0
    - Pre-existing politician range count returns 0
    - VA G5420 row count in geofence_boundaries query returns 0
  </acceptance_criteria>
  <done>
    ACPS_GEOID value confirmed; 9-member roster with role labels and external_id assignments recorded for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 313_acps_school_board.sql</name>
  <files>
    C:/EV-Accounts/backend/migrations/313_acps_school_board.sql
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/254_or_school_districts.sql (full file — primary structural template)
    - C:/EV-Accounts/backend/migrations/257_ca_city_school_boards.sql (secondary — pre-flight external_id clear check pattern)
    - C:/Transparent Motivations/essentials/.planning/phases/103-alexandria-deep-seed/103-PATTERNS.md (§313 section)
    - C:/Transparent Motivations/essentials/.planning/phases/103-alexandria-deep-seed/103-CONTEXT.md (D-01, D-02, D-03, D-20)
  </read_first>
  <action>
    Create file C:/EV-Accounts/backend/migrations/313_acps_school_board.sql using the structure of 254_or_school_districts.sql collapsed to a single district + 9-member board, with these specifications (per PATTERNS.md §313 and D-01..D-20):

    Header / ledger:
    - Header comment: "Migration 313: Alexandria City Public Schools (ACPS) school board (VA-DEEP-02)"
    - Migration ledger version: '313'

    Section order (top to bottom):

    (1) Pre-flight idempotency check — DO block raising EXCEPTION if government 'Alexandria City Public Schools, Virginia, US' already exists. Text: 'Migration 313 already applied — aborting re-run'.

    (2) Pre-flight external_id block clear — DO block per PATTERNS.md §313, range BETWEEN -<ACPS_GEOID>009 AND -<ACPS_GEOID>001; EXCEPTION if any rows present.

    (3) G5420 geofence INSERT (NEW STEP — not present in analog 254 because OR rows pre-existed):
        INSERT INTO essentials.geofence_boundaries (geo_id, mtfcc, state)
        SELECT '<ACPS_GEOID>', 'G5420', '51'
        WHERE NOT EXISTS (
          SELECT 1 FROM essentials.geofence_boundaries
          WHERE geo_id = '<ACPS_GEOID>' AND mtfcc = 'G5420'
        );
        state='51' is the FIPS numeric string for Virginia (matches geofence_boundaries convention).

    (4) Government INSERT — name='Alexandria City Public Schools, Virginia, US', type='LOCAL', state='VA', city=NULL, geo_id='<ACPS_GEOID>'; WHERE NOT EXISTS on name.

    (5) Chamber INSERT — name='School Board', name_formal='Alexandria City Public Schools Board', government_id = subquery on government name; WHERE NOT EXISTS on (name, government_id). Do NOT include slug column (D-14).

    (6) SCHOOL district INSERT — district_type='SCHOOL', state='va', geo_id='<ACPS_GEOID>', label='Alexandria City Public Schools', mtfcc='G5420'; WHERE NOT EXISTS on (geo_id, district_type, state). Do NOT include tiger_geoid column (D-08).

    (7) Nine politician + office CTE blocks (one per board member), each using the WITH ins_p AS (INSERT...ON CONFLICT (external_id) DO NOTHING RETURNING id) INSERT INTO offices SELECT FROM districts CROSS JOIN ins_p WHERE ... NOT EXISTS pattern from PATTERNS.md §313:

        Block 1 — Chair Michelle Rief, external_id=-<ACPS_GEOID>001, office title='School Board Chair'
        Block 2 — Vice Chair Christopher Harris, external_id=-<ACPS_GEOID>002, office title='School Board Vice Chair'
        Blocks 3-9 — 7 remaining members in alphabetical order by last name, external_ids -<ACPS_GEOID>003..-<ACPS_GEOID>009, office title='School Board Member'

        Each block:
        - politician fields: party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true
        - office fields: chamber_id = subquery on (name='School Board', government_id subquery on 'Alexandria City Public Schools, Virginia, US'); representing_state='VA'; is_appointed_position=false; is_vacant=false; role_canonical=NULL
        - district CROSS JOIN filter: d.geo_id='<ACPS_GEOID>' AND d.district_type='SCHOOL' AND d.state='va'
        - NOT EXISTS guard on (district_id, politician_id)

    (8) office_id back-fill UPDATE — range BETWEEN -<ACPS_GEOID>009 AND -<ACPS_GEOID>001 with office_id IS NULL.

    (9) Post-verification DO block with 7 gates (per PATTERNS.md §313):
        Gate (a): v_gov_count = 1 ('Alexandria City Public Schools, Virginia, US')
        Gate (b): v_chamber_count = 1 (School Board for that government)
        Gate (c): v_dist_count = 1 (SCHOOL district for geo_id=<ACPS_GEOID>, state='va')
        Gate (d): v_pol_count = 9 (politicians in external_id range)
        Gate (e): v_off_count = 9 (offices linked to that SCHOOL district)
        Gate (f): v_split_count = 0 — section-split detector:
            SELECT COUNT(*) FROM essentials.geofence_boundaries gb
            WHERE gb.geo_id = '<ACPS_GEOID>' AND gb.mtfcc = 'G5420'
              AND NOT EXISTS (
                SELECT 1 FROM essentials.districts d
                WHERE d.geo_id = gb.geo_id AND d.state = 'va'
                  AND d.district_type = 'SCHOOL'
              );
        Gate (g): v_null_count = 0 (politicians in range with office_id IS NULL)
        Each gate raises EXCEPTION on mismatch with descriptive message.
        Final RAISE NOTICE summarizing all 7 counters.

    (10) Migration ledger INSERT: VALUES ('313') ON CONFLICT DO NOTHING.

    Throughout the file, substitute the literal ACPS_GEOID digit string for every '<ACPS_GEOID>' placeholder (do NOT leave any angle-bracket placeholder text in the final SQL).

    Do NOT include BEGIN/COMMIT — supabase apply_migration handles transaction boundaries.
  </action>
  <verify>
    <automated>
      File C:/EV-Accounts/backend/migrations/313_acps_school_board.sql exists; `grep -c "Alexandria City Public Schools, Virginia, US" C:/EV-Accounts/backend/migrations/313_acps_school_board.sql` returns >= 8 (gov INSERT, idempotency check, multiple chamber subqueries across 9 CTE blocks, post-verification gate); `grep -c "ON CONFLICT (external_id) DO NOTHING" C:/EV-Accounts/backend/migrations/313_acps_school_board.sql` returns exactly 9; `grep -c "'G5420'" C:/EV-Accounts/backend/migrations/313_acps_school_board.sql` returns >= 3 (geofence INSERT, district INSERT, post-verification gate (f)); no occurrences of literal "<ACPS_GEOID>" or "ACPS_GEOID" placeholder remain in the file.
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/313_acps_school_board.sql
    - Contains exactly 9 ON CONFLICT (external_id) DO NOTHING clauses
    - Contains 1 'School Board Chair' office title, 1 'School Board Vice Chair' office title, 7 'School Board Member' office titles
    - Contains a geofence_boundaries INSERT with mtfcc='G5420' and state='51'
    - Contains a districts INSERT with district_type='SCHOOL', state='va', mtfcc='G5420'
    - office_id back-fill UPDATE statement uses BETWEEN -<ACPS_GEOID>009 AND -<ACPS_GEOID>001 (with actual digits substituted)
    - Post-verification DO block has 7 distinct gates labeled (a) through (g)
    - All district filters use state='va' (lowercase, per D-10)
    - All government/office filters use 'VA' (uppercase, per D-11, D-12)
    - No `slug` in chamber INSERT column list (D-14)
    - No `tiger_geoid` in district INSERT column list (D-08)
    - No occurrences of literal text "<ACPS_GEOID>" or "{ACPS_GEOID}" or "ACPS_GEOID" placeholder remain
    - File ends with `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('313') ON CONFLICT (version) DO NOTHING;`
  </acceptance_criteria>
  <done>
    Migration 313 SQL file written with all placeholder substitutions resolved and 7-gate post-verification block.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 313 + spot-check verification</name>
  <files>
    (no new files; applies migration via MCP tool)
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/313_acps_school_board.sql (the file written in Task 2)
    - C:/Transparent Motivations/essentials/.planning/phases/103-alexandria-deep-seed/103-CONTEXT.md (D-01, D-03)
  </read_first>
  <action>
    (1) Apply migration via `mcp__supabase-local__apply_migration`:
        name: "313_acps_school_board"
        query: <full contents of 313_acps_school_board.sql>
        Post-verification DO block runs all 7 gates during apply. Any "Post-verification FAILED" -> STOP and report without retry.

    (2) Spot-check queries via `mcp__supabase-local__execute_sql` (substitute the actual ACPS_GEOID digit string):

        Query A — G5420 geofence:
          SELECT COUNT(*) FROM essentials.geofence_boundaries
          WHERE geo_id = '<ACPS_GEOID>' AND mtfcc = 'G5420' AND state = '51';
          Expected: 1

        Query B — government:
          SELECT COUNT(*) FROM essentials.governments
          WHERE name = 'Alexandria City Public Schools, Virginia, US';
          Expected: 1

        Query C — district:
          SELECT district_type, label, state, geo_id, mtfcc FROM essentials.districts
          WHERE geo_id = '<ACPS_GEOID>' AND state = 'va';
          Expected: 1 row, district_type='SCHOOL', label='Alexandria City Public Schools', mtfcc='G5420'

        Query D — politicians + offices:
          SELECT p.external_id, p.full_name, o.title
          FROM essentials.politicians p
          JOIN essentials.offices o ON o.politician_id = p.id
          JOIN essentials.districts d ON d.id = o.district_id
          WHERE d.geo_id = '<ACPS_GEOID>' AND d.state = 'va'
          ORDER BY p.external_id DESC;
          Expected: 9 rows; the row at external_id=-<ACPS_GEOID>001 has title='School Board Chair'; the row at -<ACPS_GEOID>002 has title='School Board Vice Chair'; the remaining 7 rows have title='School Board Member'

        Query E — office_id back-fill check:
          SELECT COUNT(*) FROM essentials.politicians
          WHERE external_id BETWEEN -(<ACPS_GEOID>::bigint * 1000 + 9) AND -(<ACPS_GEOID>::bigint * 1000 + 1)
            AND office_id IS NULL;
          Expected: 0

        Query F — section-split independent verification:
          SELECT COUNT(*) FROM essentials.geofence_boundaries gb
          WHERE gb.geo_id = '<ACPS_GEOID>' AND gb.mtfcc = 'G5420'
            AND NOT EXISTS (
              SELECT 1 FROM essentials.districts d
              WHERE d.geo_id = gb.geo_id AND d.state = 'va' AND d.district_type = 'SCHOOL'
            );
          Expected: 0

    (3) Update C:/Transparent Motivations/essentials/.planning/STATE.md: advance "Next migration" counter from 313 to 314. Record one-line entry under Accumulated Context: "Migration 313 applied: ACPS school board (9 members under SCHOOL district geo_id=<ACPS_GEOID>); G5420 geofence inserted directly per D-03".
  </action>
  <verify>
    <automated>
      apply_migration completes without raising EXCEPTION. Query A=1, B=1, C=1 row with expected values, D=9 rows with title distribution (1 Chair / 1 VC / 7 Members), E=0, F=0.
    </automated>
  </verify>
  <acceptance_criteria>
    - mcp__supabase-local__apply_migration completes without raising "Post-verification FAILED"
    - Query A returns 1
    - Query B returns 1
    - Query C returns 1 row with district_type='SCHOOL', label='Alexandria City Public Schools', mtfcc='G5420', state='va'
    - Query D returns 9 rows with title distribution: 1 'School Board Chair', 1 'School Board Vice Chair', 7 'School Board Member'
    - Query E returns 0
    - Query F returns 0
    - STATE.md "Next migration" line updated from 313 to 314
    - STATE.md gains one line under Accumulated Context recording migration 313 application and the chosen ACPS_GEOID
  </acceptance_criteria>
  <done>
    Migration 313 applied; spot-checks confirm 1 G5420 geofence + 1 government + 1 chamber + 1 SCHOOL district + 9 politicians/offices + 0 orphans + 0 NULL office_ids; STATE.md updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Census TIGER metadata -> Planner (Task 1) | External authoritative source for geo_id lookup; if wrong, downstream routing fails silently |
| acps.k12.va.us -> Planner (Task 1) | Untrusted website content; names extracted manually, never embedded as code |
| MCP tool -> Production Supabase | apply_migration crosses into production; G5420 geofence INSERT is the first VA G5420 row ever |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02-01 | Tampering | essentials.geofence_boundaries (G5420 INSERT) | mitigate | WHERE NOT EXISTS guard on (geo_id, mtfcc); only one G5420 row possible; post-verification Gate (f) confirms district links to it |
| T-103-02-02 | Spoofing | Wrong ACPS_GEOID used | mitigate | Task 1 has three-step fallback (DB query -> Census TIGER -> ask user); explicit recording in task notes before SQL written; section-split detector would fail loudly on mismatch |
| T-103-02-03 | Tampering | external_id collision with Alexandria city officials | mitigate | external_id range derives from ACPS_GEOID (e.g., -5100090001..-5100090009) vs city -5101000001..-5101000007 — different geo_id base means no overlap; Task 1 pre-flight verifies range is clear |
| T-103-02-04 | Denial of Service | Section-split orphan blocks ACPS routing | mitigate | Gate (f) post-verification confirms G5420 row has matching SCHOOL district before commit |
| T-103-02-05 | Tampering | Chair vs Member title swap | mitigate | Task 1 explicit role verification; spot-check Query D in Task 3 confirms exactly 1 Chair + 1 VC + 7 Members |
| T-103-02-06 | Tampering | tiger_geoid column accidentally populated | mitigate | D-08 explicit; acceptance criteria forbids tiger_geoid in column list; column defaults NULL |
| T-103-02-07 | Elevation of Privilege | is_appointed_position misuse on appointed board | accept | ACPS board members are voter-elected per CONTEXT.md D-17 (citywide elections); hard-coded false |
| T-103-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SQL-only migration |
</threat_model>

<verification>
After all 3 tasks complete:
1. Migration 313 file exists in C:/EV-Accounts/backend/migrations/
2. supabase_migrations.schema_migrations has version '313'
3. Spot-check Queries A-F from Task 3 all match expected values
4. STATE.md "Next migration" counter reads 314
</verification>

<success_criteria>
- Migration 313 applied without raising EXCEPTION
- VA G5420 row exists in geofence_boundaries for ACPS_GEOID
- ACPS government + School Board chamber + SCHOOL district seeded
- 9 ACPS board members exist with correct title distribution (1 Chair + 1 VC + 7 Members)
- All 9 politicians have office_id populated
- Section-split detector returns 0 for ACPS geo_id / G5420 / state='va'
- STATE.md counter advanced from 313 to 314
</success_criteria>

<output>
Create `.planning/phases/103-alexandria-deep-seed/103-02-SUMMARY.md` when done. Include: chosen ACPS_GEOID value (e.g., 5100090), confirmation source (Census TIGER or DB query), 9 politician UUIDs (needed by Plan 03), all spot-check query results, link to applied migration 313 row.
</output>
