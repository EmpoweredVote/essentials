---
phase: 111-ma-stances-execs-federal
plan: 04
type: execute
wave: 4
depends_on:
  - 111-03
files_modified:
  - C:/EV-Accounts/backend/migrations/366_markey_stances.sql
autonomous: true
requirements:
  - MA-STANCES-02
must_haves:
  truths:
    - "Edward J. Markey has >=15 stance rows in inform.politician_answers after migration 366 applies"
    - "Every Markey stance row in inform.politician_context has at least 1 URL in sources (100% citation rate per D-10)"
    - "No defaulted/neutral fallback rows exist"
    - "D-05: per-individual SQL file 366_markey_stances.sql created"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/366_markey_stances.sql"
      provides: "Per-individual stance migration for US Senator Edward J. Markey"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "366_markey_stances.sql"
      to: "essentials.politicians (external_id=-200102)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research Edward J. Markey's (US Senator MA, external_id=-200102) public record across all currently-active compass topics, write per-individual stance migration 366, apply it to production Supabase, and verify stance row count.

Purpose: Partially satisfies MA-STANCES-02. Markey has 40+ year Congressional record (US House 1976–2013, Senate 2013–present) — one of the longest-serving members. Target 18–21+ stances per D-09; ROADMAP minimum ≥15.

Output: `366_markey_stances.sql` applied; ≥15 stance rows with 100% citation rate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md
@.planning/phases/111-ma-stances-execs-federal/111-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Markey UUID + active topics + research all topics</name>
  <read_first>
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01 through D-10)
    - .planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120)
    - C:/EV-Accounts/backend/migrations/155_ma_us_senators.sql (lines 46-60 to confirm Markey external_id=-200102)
  </read_first>
  <action>
    Step A (UUID + topic resolution via mcp__supabase-local__execute_sql):
      SELECT id FROM essentials.politicians WHERE external_id = -200102;
      Record as ${MARKEY_UUID}. If 0 rows, abort.
      SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;

    Step B (research — ONE agent only, NEVER parallel per D-08):
      Research Markey's documented positions on EVERY topic in ${TOPIC_LIST}.
      Target: 18–21+ stances; ROADMAP minimum ≥15.
      Markey's documented record includes:
        - climate-change: Green New Deal co-author (Senate side); decades of energy/climate legislation
        - fossil-fuels: strong decarbonization advocate; opposed fossil fuel leases
        - ai-regulation: AI legislation pioneer; algorithmic accountability
        - campaign-finance: DISCLOSE Act co-sponsor; Citizens United opposition
        - healthcare: Medicare for All supporter; ACA defender; children's health (CHIP)
        - social-security: expansion advocate
        - abortion: Women's Health Protection Act supporter
        - immigration: DREAM Act co-author (original); DACA champion
        - tariffs: mixed record — has supported targeted tariffs
        - ukraine-support: consistent support
        - civil-rights: strong LGBTQ+ record; voting rights
        - trans-athletes: LGBTQ+ support
        - redistricting: democracy reform
        - voting-rights: For the People Act
        - taxes: wealth tax / corporate tax supporter
        - economic-development: MA innovation economy
        - nuclear weapons / national security: anti-proliferation record
        - misinformation: social media regulation
        - judicial-*: court reform; judicial independence
        - childcare: early childhood education legislation
        - housing: housing legislation
        - deportation: opposed mass deportation
      Omit topics with no evidence — no neutral defaults.
      For each topic found: record { topic_slug, topic_id, value (1-5), reasoning (2-5 sentences citing specific bills/votes), sources }.

    Step C: Save to `.planning/phases/111-ma-stances-execs-federal/_tmp-markey-research.json`.
  </action>
  <verify>
    <automated>
      SELECT id FROM essentials.politicians WHERE external_id = -200102; -- must return 1 row
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/111-ma-stances-execs-federal/_tmp-markey-research.json"
      ((Get-Content ".../_tmp-markey-research.json" | ConvertFrom-Json).Count -ge 15)
    </automated>
  </verify>
  <done>Markey UUID resolved; research JSON ≥15 entries with non-empty sources.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 366_markey_stances.sql</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251)
    - .planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md
    - .planning/phases/111-ma-stances-execs-federal/_tmp-markey-research.json
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/366_markey_stances.sql` following 282 pattern exactly:
    Header comment (Migration 366: Edward J. Markey Stances / US Senator MA) → Topic UUID reference block → BEGIN; → section header → INSERT pairs per research JSON → COMMIT; → Verification comment block with ${MARKEY_UUID}.
    Hard rules: exactly one BEGIN; and COMMIT;, all values as float (1.0..5.0), $$...$$ dollar-quoting, ARRAY[...]::text[]::text[], paired inserts, ≥15 paired entries.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/366_markey_stances.sql"
      (Select-String -Path "..." -Pattern '^BEGIN;$' -SimpleMatch).Count -eq 1
      (Select-String -Path "..." -Pattern '^COMMIT;$' -SimpleMatch).Count -eq 1
      $a=(Select-String -Path "..." -Pattern 'INSERT INTO inform.politician_answers').Count; $c=(Select-String -Path "..." -Pattern 'INSERT INTO inform.politician_context').Count; ($a -eq $c) -and ($a -ge 15)
    </automated>
  </verify>
  <done>Migration file exists; BEGIN/COMMIT paired; ≥15 answer+context INSERT pairs.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 366 + verify + cleanup</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/366_markey_stances.sql
  </read_first>
  <action>
    Step A: Pass full file content to mcp__supabase-local__execute_sql.
    Step B: Verify via single query:
      SELECT (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-200102)) AS answer_count,
             (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-200102) AND pc.politician_id IS NULL) AS unpaired,
             (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-200102) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0)) AS uncited;
      Expected: answer_count>=15, unpaired=0, uncited=0.
    Step C: Delete `_tmp-markey-research.json`.
  </action>
  <verify>
    <automated>answer_count>=15, unpaired=0, uncited=0.</automated>
  </verify>
  <done>Migration 366 applied; row count ≥15; 0 unpaired; 0 uncited; temp file deleted.</done>
</task>

</tasks>

<verification>
- Stance count for Markey >=15, unpaired=0, uncited=0
- Migration 366_markey_stances.sql committed to EV-Accounts repo
</verification>

<success_criteria>
- MA-STANCES-02 further satisfied (Markey portion)
- ROADMAP Phase 111 success criterion #1 met for Markey (≥15)
- D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-10 all honored
</success_criteria>

<output>
Create `.planning/phases/111-ma-stances-execs-federal/111-04-SUMMARY.md` documenting stance count for Markey, topics omitted, sources used.
</output>
