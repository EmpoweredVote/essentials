---
phase: 100-va-tiger-geofences
plan: 02
type: execute
wave: 2
depends_on:
  - 100-01
files_modified: []
autonomous: true
requirements:
  - VA-GEO-01
  - VA-GEO-02
  - VA-GEO-03

must_haves:
  truths:
    - "geofence_boundaries rows exist for state='51' with all 5 MTFCC types (G4020, G4110, G5200, G5210, G5220)"
    - "geofence_boundaries has exactly 133 rows with state='51' AND mtfcc='G4020' (95 counties + 38 independent cities)"
    - "geofence_boundaries has exactly 11 rows with state='51' AND mtfcc='G5200' (already existed; idempotent re-load logs already_exists:11)"
    - "geofence_boundaries has exactly 40 rows with state='51' AND mtfcc='G5210' (40 VA Senate districts)"
    - "geofence_boundaries has N rows with state='51' AND mtfcc='G5220' where N matches EXPECTED_VA_MTFCC.sldl from Plan 01 Task 3"
    - "geofence_boundaries has N rows with state='51' AND mtfcc='G4110' where N matches EXPECTED_VA_MTFCC.place from Plan 01 Task 3"
    - "Alexandria dual-tier present: geo_id='5101000' (G4110) AND geo_id='51510' (G4020) — both named 'Alexandria city' (VA-GEO-02 invariant)"
    - "Fairfax County (geo_id='51059') AND Fairfax city (geo_id='51600') both present as separate G4020 rows (independent city pattern)"
    - "districts rows exist for Virginia with state IN ('VA','va') at 4 district_types: va|COUNTY|133, va|STATE_UPPER|40, va|STATE_LOWER|N, VA|NATIONAL_LOWER|11"
    - "verify-va-tiger-import.sql Gate 7 returns 0 rows (no section-split — every G5200/G5210/G5220/G4020 geofence has a matching districts row)"
    - "smoke-va-geofences.ts exits 0 — all 3 VA addresses route correctly (Alexandria dual-tier; rural Shenandoah no G4110; Richmond dual-tier)"
  artifacts:
    - path: "essentials.geofence_boundaries (Supabase production)"
      provides: "5 layers of VA TIGER boundaries"
      contains: "state='51' rows for G4020, G4110, G5200, G5210, G5220"
    - path: "essentials.districts (Supabase production)"
      provides: "VA district rows with correct state casing"
      contains: "state='va' for COUNTY/STATE tiers; state='VA' for NATIONAL_LOWER"
  key_links:
    - from: "TIGER PLACE layer (tl_2024_51_place.zip)"
      to: "geofence_boundaries G4110 rows"
      via: "upsertGeofence() with ST_MakeValid + state='51'; G4110-only MTFCC filter"
      pattern: "mtfcc='G4110' AND state='51'"
    - from: "TIGER COUNTY layer (tl_2024_us_county.zip filtered STATEFP='51')"
      to: "geofence_boundaries G4020 rows (including Alexandria geo_id='51510')"
      via: "filterByStatefp=true in processLayer()"
      pattern: "mtfcc='G4020' AND state='51'"
    - from: "TIGER SLDU/SLDL layers"
      to: "districts table"
      via: "insertDistrictIfMissing() with abbrev='va' (lowercase)"
      pattern: "state='va' AND district_type IN ('STATE_UPPER','STATE_LOWER')"
    - from: "TIGER CD119 layer"
      to: "districts table NATIONAL_LOWER rows"
      via: "insertDistrictIfMissing() with abbrevUpper='VA' (uppercase)"
      pattern: "state='VA' AND district_type='NATIONAL_LOWER'"
---

<objective>
Live-load all 5 VA TIGER layers into geofence_boundaries and districts using the confirmed EXPECTED_VA_MTFCC counts from Plan 01, then verify via the 7-gate SQL script and the 3-address smoke test.

Purpose: This is the primary data write for Phase 100. After this plan completes, all VA address routing is possible end-to-end. Phases 101-106 (state government, federal officials, Alexandria deep seed, elections, stances) all depend on these geofence and district rows existing for state='51'/'va'/'VA'.

Output: geofence_boundaries and districts rows for state='51' (all 5 layers, idempotent for cd119 which already has 11 rows); verify SQL all 7 gates green; smoke test exits 0 for 3 VA addresses (Alexandria dual-tier, rural Shenandoah unincorporated, Richmond independent city).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-va-tiger-geofences/100-RESEARCH.md
@.planning/phases/100-va-tiger-geofences/100-PATTERNS.md
@.planning/phases/100-va-tiger-geofences/100-VALIDATION.md
@.planning/phases/100-va-tiger-geofences/100-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load all 5 VA TIGER layers into geofence_boundaries and districts</name>
  <read_first>
    .planning/phases/100-va-tiger-geofences/100-01-SUMMARY.md — Confirm the actual sldl count (N) and place count (N) recorded from Plan 01 Task 3 dry-run. The loader EXPECTED_VA_MTFCC must contain these confirmed values before running live.
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Verify EXPECTED_VA_MTFCC has non-zero sldl and place values (confirm Plan 01 edits are still in place); confirm STATE_LAYER_ALLOWLIST/STATE_CITY_ASSERTIONS/STATE_RUN_MAKEVALID all contain VA entries.
    .planning/phases/100-va-tiger-geofences/100-RESEARCH.md — Pre-Existing State section: 11 G5200 rows already exist; cd119 live load logs `already_exists:11` (correct, not an error). Pitfall 5 explains this.
  </read_first>
  <action>
    Confirm Plan 01 is complete before proceeding: EXPECTED_VA_MTFCC.sldl and EXPECTED_VA_MTFCC.place must both be non-zero in load-state-tiger-boundaries.ts. If either is still 0, stop — Plan 01 Task 3 must be re-run first; this task cannot proceed with sentinel values in place.

    From C:/EV-Accounts/backend, run the live load command for all 5 VA layers:

      npx tsx scripts/load-state-tiger-boundaries.ts --state VA --fips 51 --layers cd119,sldu,sldl,place,county

    No --dry-run flag. The loader will:
      1. Download all 5 TIGER zip files from census.gov to the local cache (cd119 1.7MB, sldu 3.1MB, sldl 4.8MB, place 3.6MB, county 80MB national)
      2. Run the EXPECTED_VA_MTFCC pre-flight assertion for each layer (must pass before any DB write — values were confirmed in Plan 01)
      3. Run the STATE_CITY_ASSERTIONS check for the place layer (confirm 'Alexandria city' is present in NAMELSAD values)
      4. Stream each shapefile and upsert rows into essentials.geofence_boundaries with ON CONFLICT (geo_id, mtfcc) DO NOTHING; ST_MakeValid applied per STATE_RUN_MAKEVALID
      5. Insert corresponding district rows into essentials.districts via insertDistrictIfMissing() (WHERE NOT EXISTS guard) — using abbrev='va' (lowercase) for county/sldu/sldl, abbrevUpper='VA' (uppercase) for cd119 NATIONAL_LOWER

    Monitor console output for these expected results per layer:
      - cd119: pre-flight PASSED 11 records; upsert logs `inserted_boundary:0 + already_exists:11` (correct — 11 G5200 rows existed before Phase 100 from a prior TIGER run); district insertion guarded by WHERE NOT EXISTS (the 11 existing NATIONAL_LOWER rows are skipped)
      - sldu: pre-flight PASSED 40 records; upsert inserts 40 G5210 boundary rows + 40 va|STATE_UPPER district rows
      - sldl: pre-flight PASSED N records (where N matches Plan 01 confirmed count, expected ~100); upsert inserts N G5220 boundary rows + N va|STATE_LOWER district rows (Virginia uses single-member districts — one polygon per delegate, unlike MD multi-member)
      - place: pre-flight PASSED N records (where N matches Plan 01 confirmed count); upsert inserts N G4110 boundary rows; NOTE the place layer does NOT write district rows (G4110 places are city geofences only, not legislative districts)
      - county: pre-flight PASSED 133 records; the county shapefile is national (tl_2024_us_county.zip), filtered by STATEFP='51'; upsert inserts 133 G4020 boundary rows + 133 va|COUNTY district rows (including Alexandria geo_id='51510' and Fairfax City geo_id='51600' as independent cities)

    Watch for these failure modes and stop if any occur:
      - Any `MtfccAssertionError` — would mean EXPECTED_VA_MTFCC value drifted from actual; re-run Plan 01 Task 3 to refresh
      - `STATE_CITY_ASSERTIONS gate FAILED` — 'Alexandria city' not found in NAMELSAD; investigate casing and stop
      - Any non-zero ERROR line that is NOT `already_exists` — investigate before retrying
      - Loader exits non-zero — read the error message; ON CONFLICT DO NOTHING makes re-running safe so a retry after a fix is acceptable

    Do not abort on `already_exists:11` for cd119 — this is the documented expected behavior per RESEARCH.md (Pre-Existing State section). The EXPECTED_VA_MTFCC.cd119=11 pre-flight assertion already validated the shapefile records; the upsert correctly skips the 11 pre-existing rows.

    Do not write a migration file for this load; the loader script IS the data-writing mechanism for TIGER geofences (established pattern from ME/OR/MD/CA/TX/UT/IN/MA).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx -e "require('dotenv').config(); const {Client} = require('pg'); const c = new Client({connectionString: process.env.DATABASE_URL, ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(\"SELECT mtfcc, COUNT(*) AS n FROM essentials.geofence_boundaries WHERE state='51' GROUP BY mtfcc ORDER BY mtfcc\")).then(r=>{r.rows.forEach(row=>console.log(row.mtfcc,row.n)); c.end()})"</automated>
  </verify>
  <acceptance_criteria>
    - Loader exits 0 (no non-zero error exit)
    - cd119 layer logs `already_exists:11` (not an error — confirms pre-existing rows are intact)
    - sldu layer logs 40 G5210 boundary rows upserted
    - sldl layer logs N G5220 rows upserted (N = EXPECTED_VA_MTFCC.sldl confirmed in Plan 01)
    - place layer logs N G4110 rows upserted (N = EXPECTED_VA_MTFCC.place confirmed in Plan 01)
    - county layer logs 133 G4020 rows upserted
    - `SELECT mtfcc, COUNT(*) FROM essentials.geofence_boundaries WHERE state='51' GROUP BY mtfcc ORDER BY mtfcc` returns rows for G4020 (133), G4110 (N), G5200 (11), G5210 (40), G5220 (N) — with G4110 and G5220 matching Plan 01 confirmed counts
    - No MtfccAssertionError in loader output
    - STATE_CITY_ASSERTIONS gate passes for 'Alexandria city'
  </acceptance_criteria>
  <done>All 5 VA TIGER layers loaded into geofence_boundaries and districts. geofence_boundaries has rows for all 5 MTFCC types at state='51'. districts has rows for va|COUNTY|133, va|STATE_UPPER|40, va|STATE_LOWER|N, VA|NATIONAL_LOWER|11.</done>
</task>

<task type="auto">
  <name>Task 2: Run verify-va-tiger-import.sql — all 7 gates must pass</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/verify-va-tiger-import.sql — The 7-gate verification script created in Plan 01. Read it to understand expected values for each gate before running.
    .planning/phases/100-va-tiger-geofences/100-01-SUMMARY.md — Actual sldl and place counts from dry-run; match against Gate 3 and Gate 5 expected counts.
    .planning/phases/100-va-tiger-geofences/100-RESEARCH.md — Gate 7 section explains why OR-direction is required (NATIONAL_UPPER geo_id='51' false-positive risk under MD direction).
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the verification SQL:

      psql $DATABASE_URL -f scripts/verify-va-tiger-import.sql

    Alternatively if psql is unavailable, run each gate individually via the Supabase MCP execute_sql tool. Check each gate's result against the expected values documented in the SQL file comments.

    Expected gate results:
      - Gate 1 (invalid geometry): 0 rows — no invalid geometries for state='51'
      - Gate 2 (GeometryCollection): 0 rows — no GeometryCollection types for state='51'
      - Gate 3 (per-layer counts): G4020=133, G4110=N (Plan 01 place count), G5200=11, G5210=40, G5220=N (Plan 01 sldl count)
      - Gate 4 (Alexandria dual-tier): exactly 2 rows — geo_id='51510' name='Alexandria city' mtfcc='G4020' AND geo_id='5101000' name='Alexandria city' mtfcc='G4110'
      - Gate 5 (districts table): va|COUNTY|133, va|STATE_LOWER|N, va|STATE_UPPER|40, VA|NATIONAL_LOWER|11
      - Gate 6 (Fairfax sentinel): exactly 2 rows — geo_id='51059' 'Fairfax County' G4020 AND geo_id='51600' 'Fairfax city' G4020
      - Gate 7 (section-split OR-direction): 0 rows — every G5200/G5210/G5220/G4020 geofence for state='51' has a matching districts row

    Stop and investigate if any gate returns unexpected results:
      - Gate 1/2 non-zero: invalid/mixed geometry in DB — the loader's ST_MakeValid may have failed on some records; check which MTFCC types are affected
      - Gate 3 count mismatch: a layer loaded fewer rows than expected; check for duplicate ON CONFLICT skips vs actual missing records
      - Gate 4 returns 0 or 1 row: Alexandria dual-tier failed — check geo_id values in geofence_boundaries for name='Alexandria city'
      - Gate 6 returns only 1 row: Fairfax County and Fairfax city are separate independent-city entities in VA; both must be present
      - Gate 7 returns any rows: section-split bug — a legislative or county geofence is missing its districts row; the loader's insertDistrictIfMissing() skipped it
  </action>
  <verify>
    <automated>psql $DATABASE_URL -c "SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='51' AND geo_id IN ('5101000', '51510') ORDER BY mtfcc;" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - verify-va-tiger-import.sql runs to completion with no psql error exit
    - Gate 1 returns 0 rows (SELECT ... WHERE NOT ST_IsValid: 0 invalid geometries)
    - Gate 2 returns 0 rows (SELECT ... WHERE ST_GeometryType = 'ST_GeometryCollection': 0)
    - Gate 3 shows G4020=133, G5200=11, G5210=40; G4110 and G5220 match confirmed counts from Plan 01
    - Gate 4 returns exactly 2 rows: geo_id='51510' G4020 AND geo_id='5101000' G4110 (VA-GEO-02 satisfied)
    - Gate 5 includes rows: va|COUNTY|133, va|STATE_UPPER|40, VA|NATIONAL_LOWER|11
    - Gate 6 returns exactly 2 rows: Fairfax County (geo_id='51059') AND Fairfax city (geo_id='51600')
    - Gate 7 returns 0 rows (no section-split — OR-direction check passes)
  </acceptance_criteria>
  <done>All 7 SQL gates pass. Alexandria dual-tier (VA-GEO-02) confirmed. Fairfax independent-city pattern confirmed. Section-split check clean. Database state is consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Run smoke-va-geofences.ts — all 3 VA addresses must route correctly</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts — The smoke test script created in Plan 01. Confirm that expectedCounts.G4110 and expectedCounts.G5220 match the confirmed Plan 01 dry-run counts (should have been updated in Plan 01 Task 3 Edit B).
    .planning/phases/100-va-tiger-geofences/100-01-SUMMARY.md — Confirmed sldl count (G5220) and place count (G4110) for cross-checking smoke test expectedCounts.
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the smoke test:

      npx tsx scripts/smoke-va-geofences.ts

    The test queries production via DATABASE_URL (dotenv loaded from .env). It runs 3 address assertions:

      Address 1 (Alexandria VA City Hall — lon=-77.0469, lat=38.8048):
        - expectedMtfcc: G4020, G4110, G5200, G5210, G5220 (all 5 tiers)
        - expectedGeoIds: G4110='5101000', G4020='51510', G5200='5108' (VA-8 Don Beyer)
        - This is the VA-GEO-02 dual-tier invariant: Alexandria must return BOTH G4110 AND G4020

      Address 2 (Rural Shenandoah County VA — lon=-78.6, lat=38.9):
        - expectedMtfcc: G4020, G5200, G5210, G5220 (4 tiers — county + legislative)
        - forbiddenMtfcc: G4110 (unincorporated — must NOT return an incorporated place)

      Address 3 (Richmond VA City Hall — lon=-77.4360, lat=37.5407):
        - expectedMtfcc: G4020, G4110, G5200, G5210, G5220 (all 5 tiers — Richmond is an independent city)

    Watch for:
      - Any 'EXPECTED MTFCC MISSING' error — a tier is absent for a test address; check which layer failed to load (most likely sldl or place since they had sentinel 0s)
      - Any 'FORBIDDEN MTFCC PRESENT' error for Address 2 — rural coordinate is inside an incorporated place; shift coordinate and document
      - Any 'EXPECTED GEO_ID MISMATCH' for G4110='5101000' or G4020='51510' — Alexandria dual-tier geo_ids are wrong; investigate geofence_boundaries rows for name='Alexandria city'
      - SC3 expectedCounts mismatch — if G4110 or G5220 count in DB differs from smoke test expectedCounts; re-check Plan 01 Task 3 Edit B

    If smoke-va-geofences.ts exits 1, read the console output for which SC failed and investigate the specific geofence_boundaries query for that address.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-va-geofences.ts; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/smoke-va-geofences.ts` exits 0
    - Console output includes SC1 pass: Alexandria returns all 5 MTFCC tiers including G4110='5101000' AND G4020='51510'
    - Console output includes SC2 pass: rural Shenandoah returns 4 MTFCC tiers with NO G4110 (forbiddenMtfcc satisfied)
    - Console output includes Richmond returning all 5 MTFCC tiers including G4110 and G4020 (Richmond independent city dual-tier)
    - Console output shows SC3 pass: total counts for state='51' match expected (G4020=133, G5200=11, G5210=40, G4110/G5220=confirmed counts from Plan 01)
    - Console output shows SC4 pass: no NULL name rows for any state='51' geofence tier
    - No 'EXPECTED MTFCC MISSING', 'FORBIDDEN MTFCC PRESENT', or 'EXPECTED GEO_ID MISMATCH' lines in output
    - smoke-va-geofences.ts exit code is 0 (not 1)
  </acceptance_criteria>
  <done>All 3 VA address assertions pass. VA-GEO-03 satisfied: any VA address returns correct federal, state, and city representatives via PostGIS routing. VA-GEO-02 satisfied end-to-end via smoke test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader -> Supabase production | Live writes to essentials.geofence_boundaries and essentials.districts — this is the only plan that writes data |
| loader -> census.gov | HTTPS download of TIGER zip files for live load (same as dry-run in Plan 01) |
| smoke test -> Supabase production | Read-only spatial queries via DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-06 | Integrity | Live load with stale sentinel 0 in EXPECTED_VA_MTFCC | mitigate | Task 1 requires executor to confirm Plan 01 completion; EXPECTED_VA_MTFCC.sldl/place must be non-zero before running live; loader pre-flight assertion blocks DB write if count mismatch |
| T-100-07 | Integrity | ON CONFLICT DO NOTHING silently drops rows | accept | Established loader pattern; the EXPECTED_VA_MTFCC pre-flight assertion validates shapefile count before upsert; post-load Gate 3 SQL verifies DB row counts match expected |
| T-100-08 | Integrity | Wrong district state casing (VA vs va) | mitigate | Gate 5 SQL verifies both `state='va'` (STATE/COUNTY) and `state='VA'` (NATIONAL_LOWER); mismatch caught before phase is marked complete |
| T-100-09 | Integrity | Alexandria dual-tier failure (missing G4020 or G4110) | mitigate | Gate 4 SQL + smoke test SC1 both assert both rows present; two independent checks |
| T-100-10 | Availability | 80MB national county zip causes timeout/OOM on large download | accept | tl_2024_us_county.zip is the national file; already used by OR and MD phases without issue; filtered by STATEFP='51' before upsert |
</threat_model>

<verification>
After all 3 tasks complete:
1. `SELECT mtfcc, COUNT(*) FROM essentials.geofence_boundaries WHERE state='51' GROUP BY mtfcc ORDER BY mtfcc` — returns 5 rows: G4020/133, G4110/N, G5200/11, G5210/40, G5220/N (N from Plan 01 confirmed counts)
2. `SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='51' AND geo_id IN ('5101000', '51510') ORDER BY mtfcc` — returns exactly 2 rows (Alexandria dual-tier, VA-GEO-02)
3. `SELECT state, district_type, COUNT(*) FROM essentials.districts WHERE state IN ('VA','va') GROUP BY state, district_type ORDER BY state, district_type` — returns va|COUNTY|133, va|STATE_LOWER|N, va|STATE_UPPER|40, VA|NATIONAL_LOWER|11
4. `psql $DATABASE_URL -f scripts/verify-va-tiger-import.sql` — all 7 gates pass (Gates 1/2 = 0 rows; Gate 4 = 2 rows; Gate 6 = 2 rows; Gate 7 = 0 rows)
5. `npx tsx scripts/smoke-va-geofences.ts` exits 0
</verification>

<success_criteria>
- VA-GEO-01: geofence_boundaries rows exist for state='51' with all 5 MTFCC types (G4020/133, G4110/N, G5200/11, G5210/40, G5220/N)
- VA-GEO-02: Alexandria appears in geofence_boundaries with BOTH geo_id='5101000' (G4110) AND geo_id='51510' (G4020)
- VA-GEO-03: smoke-va-geofences.ts exits 0 for all 3 VA test addresses — Alexandria dual-tier, rural Shenandoah county-only, Richmond dual-tier
- districts table has correct state casing: lowercase 'va' for COUNTY/STATE tiers, uppercase 'VA' for NATIONAL_LOWER
- verify-va-tiger-import.sql Gate 7 returns 0 rows (no section-split)
- No sentinel 0 values remain in any file (EXPECTED_VA_MTFCC.sldl/place and smoke expectedCounts.G4110/G5220 all confirmed non-zero integers)
</success_criteria>

<output>
Create `.planning/phases/100-va-tiger-geofences/100-02-SUMMARY.md` when done. Record final row counts for all 5 MTFCC types, confirmation of Alexandria dual-tier, Gate 7 result, and smoke test exit code. Note next phase: Phase 101 (VA Chambers).
</output>