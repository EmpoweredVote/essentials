---
phase: 105-va-2026-elections-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - 105-02
files_modified:
  - C:/EV-Accounts/backend/migrations/325_va_2026_discovery.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-325.ts
  - src/pages/Landing.jsx
autonomous: true
requirements:
  - VA-ELECTIONS-03
tags:
  - discovery
  - landing
  - jsx
  - sql
  - virginia
  - va-2026
must_haves:
  truths:
    - "essentials.discovery_jurisdictions has 2 VA rows (one per election_date: 2026-08-04 + 2026-11-03)"
    - "Both rows have a real source_url (ballotpedia.org or similar VA federal source) and 4-element allowed_domains array"
    - "Migration 325 ledger entry is present"
    - "src/pages/Landing.jsx COVERAGE_CITIES contains a Virginia (Alexandria) entry with browseGovernmentList=['5101000','51510']"
    - "npm run build passes for the essentials repo"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/325_va_2026_discovery.sql
      provides: "2 VA discovery_jurisdictions rows + post-verify + ledger"
      contains: "essentials.discovery_jurisdictions"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-325.ts
      provides: "Apply script with smoke tests: count=2, dates, source_url, ledger"
      contains: "discovery_jurisdictions"
    - path: src/pages/Landing.jsx
      provides: "Virginia entry in COVERAGE_CITIES"
      contains: "Alexandria"
  key_links:
    - from: essentials.discovery_jurisdictions
      to: discovery cron agent (reads rows within 180-day window)
      via: "date-based eligibility (no cron_active column per MD Phase 96 D-03)"
      pattern: "election_date BETWEEN now\\(\\) AND now\\(\\) \\+ interval '180 days'"
    - from: src/pages/Landing.jsx
      to: /results browse flow
      via: "browseGovernmentList query params"
      pattern: "browse_government_list"
---

<objective>
Arm Virginia's federal discovery cron and add Virginia to the Landing.jsx coverage list:
1. Migration 325 seeds 2 rows into essentials.discovery_jurisdictions (one per 2026 VA election date), with VA-appropriate source_url and allowed_domains.
2. src/pages/Landing.jsx gets a new COVERAGE_CITIES entry for Alexandria (dual-tier geo_ids 5101000 + 51510, browseStateAbbrev='VA').

Purpose: VA-ELECTIONS-03 requires both discovery armed + Landing entry. This is the final integration of v12.0 — Alexandria becomes visible on the public landing page, and the discovery agent picks up VA federal races within its 180-day eligibility window.

Output: Migration 325 SQL + apply script applied to production; Landing.jsx committed; npm run build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-03-SUMMARY.md
@C:/EV-Accounts/backend/migrations/281_md_2026_discovery.sql
@C:/EV-Accounts/backend/scripts/_apply-migration-281.ts
@src/pages/Landing.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 325_va_2026_discovery.sql + apply script + apply to production</name>
  <files>
    C:/EV-Accounts/backend/migrations/325_va_2026_discovery.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-325.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/migrations/281_md_2026_discovery.sql (verbatim pattern for 2-row INSERT + post-verify + ledger)
    C:/EV-Accounts/backend/scripts/_apply-migration-281.ts (5 smoke tests: count, dates, allowed_domains length, source_url, ledger)
    .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-03-SUMMARY.md (D-03 no cron_active, ledger-required lesson, idempotency check)
  </read_first>
  <action>
    Create the migration SQL file at `C:/EV-Accounts/backend/migrations/325_va_2026_discovery.sql` following the MD migration 281 pattern. Substitute VA-specific values:

    - Header comment block: "Migration 325: VA 2026 Discovery Jurisdictions — Phase 105 Plan 03". Note D-03 (no cron_active column — date-based eligibility, MD Phase 96 lesson). Note both VA election dates (2026-08-04 primary + 2026-11-03 general) are within the 180-day cron window as of 2026-06-09 (primary ~56 days out, general ~147 days out).
    - Two INSERT statements into `essentials.discovery_jurisdictions (jurisdiction_geoid, jurisdiction_name, state, election_date, source_url, allowed_domains)`:
      - Row 1: jurisdiction_geoid='51', jurisdiction_name='Commonwealth of Virginia', state='VA', election_date='2026-08-04', source_url='https://ballotpedia.org/United_States_Congress_elections_in_Virginia,_2026', allowed_domains=ARRAY['ballotpedia.org','vpap.org','elections.virginia.gov','virginia.gov']
      - Row 2: same as Row 1 but election_date='2026-11-03'
      - Rationale: VA federal races (1 Senate + 11 House) are best discovered via ballotpedia (canonical multi-race index) and vpap.org (Virginia Public Access Project, the VA-equivalent of mgaleg.maryland.gov for finance/candidate data). elections.virginia.gov is the official SBE site. virginia.gov is the catch-all.
    - Both INSERTs guarded by `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING`.
    - Post-verify `DO $$` block: `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='VA';` RAISE EXCEPTION 'Expected 2 VA discovery_jurisdictions rows' if count != 2.
    - Ledger INSERT: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('325') ON CONFLICT (version) DO NOTHING;` (mandatory).

    Create the apply script at `C:/EV-Accounts/backend/scripts/_apply-migration-325.ts` by copying `_apply-migration-281.ts` and substituting '281' -> '325', '281_md_2026_discovery.sql' -> '325_va_2026_discovery.sql', state 'MD' -> 'VA', election_date '2026-06-23' -> '2026-08-04' (general '2026-11-03' unchanged), source_url updated to the ballotpedia URL above, allowed_domains length still 4. Keep all 5 smoke tests: count, both dates ordered, allowed_domains length, source_url, ledger. Rename the script's log label from "MD election dates:" to "VA election dates:" so it matches the acceptance criteria below.

    Apply via `cd C:/EV-Accounts && npx tsx backend/scripts/_apply-migration-325.ts`.

    Note: jurisdiction_geoid='51' is the VA FIPS state code (matches geofence_boundaries.state='51' per ROADMAP). Do not use '5100000' or any other variant; the MD pattern uses bare 2-digit state FIPS ('24') and we follow it.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-325.ts 2>&amp;1 | tee /tmp/_325.log; grep -q "VA discovery_jurisdictions rows: 2" /tmp/_325.log &amp;&amp; grep -q "2026-08-04" /tmp/_325.log &amp;&amp; grep -q "2026-11-03" /tmp/_325.log &amp;&amp; grep -q "allowed_domains length (primary): 4" /tmp/_325.log &amp;&amp; grep -q "Ledger entry 325: PRESENT" /tmp/_325.log</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/325_va_2026_discovery.sql exists
    - SQL file contains `jurisdiction_geoid` value `'51'`
    - SQL file contains `'Commonwealth of Virginia'`
    - SQL file contains `'2026-08-04'`
    - SQL file contains `'2026-11-03'`
    - SQL file contains `'https://ballotpedia.org/United_States_Congress_elections_in_Virginia,_2026'`
    - SQL file contains `ARRAY['ballotpedia.org', 'vpap.org', 'elections.virginia.gov', 'virginia.gov']`
    - SQL file contains `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING`
    - SQL file does NOT contain `cron_active` (column does not exist; D-03 from MD Phase 96)
    - SQL file contains `RAISE EXCEPTION 'Expected 2 VA discovery_jurisdictions rows`
    - SQL file contains `VALUES ('325')` (ledger entry)
    - File C:/EV-Accounts/backend/scripts/_apply-migration-325.ts exists and references `325_va_2026_discovery.sql`
    - Apply script prints `VA discovery_jurisdictions rows: 2 (expected 2)`
    - Apply script prints `VA election dates:` followed by `[ '2026-08-04', '2026-11-03' ]`
    - Apply script prints `allowed_domains length (primary): 4 (expected 4)`
    - Apply script prints `Ledger entry 325: PRESENT`
    - Re-running apply script keeps row count at 2 (idempotency)
    - Production DB query `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='VA'` returns 2
  </acceptance_criteria>
  <done>
    Two VA discovery_jurisdictions rows exist in production; the cron agent will pick up VA federal race discovery within its 180-day window; migration 325 is in the ledger.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Virginia (Alexandria) entry to src/pages/Landing.jsx COVERAGE_CITIES + verify build</name>
  <files>
    src/pages/Landing.jsx
  </files>
  <read_first>
    src/pages/Landing.jsx (read the full COVERAGE_CITIES array — lines 14-25 in current state)
    .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-03-SUMMARY.md (D-04 verbatim object pattern; Maryland entry inserted at line 24 after Portland Oregon)
  </read_first>
  <action>
    Edit `src/pages/Landing.jsx` to add a Virginia entry to the `COVERAGE_CITIES` array. Insert the new entry immediately AFTER the existing Leonardtown/Maryland line (current line 24) and BEFORE the closing `];` of COVERAGE_CITIES.

    New entry (exact object literal — VA-DEEP-01 confirmed Alexandria's dual-tier as `5101000` G4110 + `51510` G4020 per ROADMAP, and the Maryland entry's bundled-list pattern is the model):

    `  { label: 'Alexandria', state: 'Virginia', browseGovernmentList: ['5101000', '51510'], browseStateAbbrev: 'VA' },`

    Rationale for dual geo_id: Alexandria is an independent city in VA (no county layer above it), but it has both a G4110 incorporated-place geofence (5101000) and a G4020 county-equivalent geofence (51510). Including BOTH in browseGovernmentList ensures the /results browse flow returns all officials linked to either tier — same logic Leonardtown uses for its town + St. Mary's County pair.

    Do NOT modify any other line in Landing.jsx. Do NOT reorder the existing entries. Do NOT add an entry to COVERAGE_COUNTIES (Alexandria is a city not a county here).

    After the edit, run `cd "C:/Transparent Motivations/essentials" && npm run build` and confirm zero new errors (pre-existing chunk size warning is acceptable per MD Phase 96 Plan 03 precedent).

    Commit message: `feat(105-03): add Virginia (Alexandria) to Landing.jsx COVERAGE_CITIES` — commit to the essentials repo only (NOT to C:/EV-Accounts which is not a git repo per project memory).
  </action>
  <verify>
    <automated>cd "/c/Transparent Motivations/essentials" &amp;&amp; grep -q "label: 'Alexandria', state: 'Virginia'" src/pages/Landing.jsx &amp;&amp; grep -q "browseGovernmentList: \['5101000', '51510'\]" src/pages/Landing.jsx &amp;&amp; grep -q "browseStateAbbrev: 'VA'" src/pages/Landing.jsx &amp;&amp; npm run build 2>&amp;1 | tail -20 | grep -E "(built in|✓ built)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/pages/Landing.jsx contains `label: 'Alexandria'`
    - File src/pages/Landing.jsx contains `state: 'Virginia'`
    - File src/pages/Landing.jsx contains `browseGovernmentList: ['5101000', '51510']`
    - File src/pages/Landing.jsx contains `browseStateAbbrev: 'VA'`
    - The Alexandria entry appears AFTER the Leonardtown/Maryland entry in COVERAGE_CITIES (line-number check: grep -n 'Alexandria' line > grep -n 'Leonardtown' line)
    - All 10 prior COVERAGE_CITIES entries unchanged (verify by grep -c for 'label:' inside COVERAGE_CITIES still increments by exactly 1)
    - COVERAGE_COUNTIES unchanged
    - `npm run build` exits with status 0
    - `npm run build` output contains "built in" or "✓ built" (vite success token)
    - Git commit `feat(105-03): add Virginia (Alexandria) to Landing.jsx COVERAGE_CITIES` exists on current branch
  </acceptance_criteria>
  <done>
    src/pages/Landing.jsx now shows Alexandria, Virginia as the 11th Coverage City; clicking it routes to /results with browse_government_list=5101000,51510 and browse_state=VA; build passes; commit on main.
  </done>
</task>

</tasks>

<verification>
- 2 VA discovery_jurisdictions rows in production (idempotent)
- Migration 325 in ledger
- Landing.jsx Alexandria entry visible after npm run dev / build
- All 3 phase requirements (VA-ELECTIONS-01/02/03) satisfied across Plans 01/02/03
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='VA'` returns 2
- src/pages/Landing.jsx has Alexandria/Virginia entry in COVERAGE_CITIES
- npm run build passes
- VA-ELECTIONS-03 satisfied
</success_criteria>

<output>
Create `.planning/phases/105-va-2026-elections-discovery/105-03-SUMMARY.md` when done with:
- Migration 325 file path + apply script path
- Apply script smoke test output (all 5 checks)
- Landing.jsx diff (the new line + its line number)
- Build output success token
- Acceptance criteria checklist
- Any deviations
- Phase 105 closing checklist: all 3 requirement IDs marked satisfied
</output>
