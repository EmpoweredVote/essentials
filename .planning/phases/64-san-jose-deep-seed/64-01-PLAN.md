---
phase: 64-san-jose-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-sj-council-boundaries.ts
  - C:/EV-Accounts/backend/scripts/smoke-sj-geofences.ts
  - C:/EV-Accounts/backend/migrations/217_sj_government_structure.sql
autonomous: true

must_haves:
  truths:
    - "10 San Jose council district rows exist in geofence_boundaries with mtfcc='X0010' and state='06'"
    - "San Jose City Hall (lon=-121.88, lat=37.335) resolves to exactly 1 council district (District 3)"
    - "Oakland (lon=-122.2711, lat=37.8044) returns 0 SJ council district rows (no false positive)"
    - "essentials.governments has a 'City of San Jose' row with geo_id='0668000', state='CA'"
    - "2 chambers exist under the SJ government: 'Mayor' and 'City Council'"
    - "11 essentials.districts rows exist: 10 LOCAL (sj-council-district-1..10) + 1 LOCAL_EXEC (geo_id='0668000')"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-sj-council-boundaries.ts"
      provides: "ArcGIS loader for SJ council district geofences"
      contains: "X0010"
    - path: "C:/EV-Accounts/backend/scripts/smoke-sj-geofences.ts"
      provides: "Smoke test verifying 3 SCs"
      contains: "sj-council-district"
    - path: "C:/EV-Accounts/backend/migrations/217_sj_government_structure.sql"
      provides: "Government row + 2 chambers + 11 districts"
      contains: "City of San Jose"
  key_links:
    - from: "load-sj-council-boundaries.ts"
      to: "essentials.geofence_boundaries"
      via: "INSERT ON CONFLICT (geo_id, mtfcc) DO NOTHING"
      pattern: "sj-council-district"
    - from: "smoke-sj-geofences.ts"
      to: "essentials.geofence_boundaries"
      via: "ST_Covers with geo_id LIKE 'sj-council-district-%'"
      pattern: "ST_Covers"
    - from: "217_sj_government_structure.sql"
      to: "essentials.districts"
      via: "WHERE NOT EXISTS guards (no unique constraint)"
      pattern: "WHERE NOT EXISTS"
---

<objective>
Load San Jose council district geofences and create the government structure DB scaffold (government row, chambers, districts) so the SJ government is query-ready before officials are seeded in Plan 64-02.

Purpose: Plan 64-02 requires essentials.districts rows to link office rows to districts. The loader script must run first so PostGIS data is present for smoke test routing verification.
Output: 10 geofence_boundaries rows (X0010), migration 217 applied (1 government + 2 chambers + 11 districts), smoke test green.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-san-jose-deep-seed/64-CONTEXT.md
@.planning/phases/64-san-jose-deep-seed/64-RESEARCH.md

# Reference loader (identical ArcGIS pattern — adapt for SJ)
@C:/EV-Accounts/backend/scripts/load-fremont-council-boundaries.ts

# Reference smoke test (adapt for SJ coords and X0010)
@C:/EV-Accounts/backend/scripts/smoke-fremont-geofences.ts

# Reference government structure migration (adapt for SJ)
@C:/EV-Accounts/backend/migrations/213_berkeley_government_structure.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run load-sj-council-boundaries.ts + smoke-sj-geofences.ts</name>
  <files>C:/EV-Accounts/backend/scripts/load-sj-council-boundaries.ts</files>
  <files>C:/EV-Accounts/backend/scripts/smoke-sj-geofences.ts</files>
  <action>
Create load-sj-council-boundaries.ts by copying load-fremont-council-boundaries.ts and applying these exact changes:

Config constants:
  ARCGIS_URL = 'https://geo.sanjoseca.gov/server/rest/services/OPN/OPN_OpenDataService/MapServer/120/query?where=1%3D1&outFields=DISTRICTINT%2CCOUNCILMEMBER&outSR=4326&f=geojson'
  MTFCC = 'X0010'
  STATE = '06'
  SOURCE = 'sj_city_council_districts_2022'
  EXPECTED_COUNT = 10
  MAX_DISTRICT = 10

Field parsing change: The SJ field is DISTRICTINT (not DISTRICT). Change:
  const rawDistrict = props['DISTRICTINT'];  // was props['DISTRICT']

geo_id pattern: 'sj-council-district-{N}' (was 'fremont-council-district-{N}')

Pre-flight check: Assert X0010 is unclaimed or only has sj-council-district rows (same guard pattern as X0008/X0009 checks in prior loaders).

CRITICAL: outSR=4326 is already in the URL — do NOT remove it. The native CRS is WKID 102643 (State Plane CA Zone 3, feet). Without outSR=4326, coordinates will be ~6 million feet and ST_Covers will always return 0 rows.

Do NOT use the COUNCILMEMBER field for the boundary name. Store name='District N' derived from DISTRICTINT only.

Run from C:/EV-Accounts/backend (not C:/EV-Accounts — dotenv looks for .env in cwd):
  npx tsx scripts/load-sj-council-boundaries.ts --dry-run
  npx tsx scripts/load-sj-council-boundaries.ts

Then create smoke-sj-geofences.ts by copying smoke-fremont-geofences.ts and applying:
  - geo_id LIKE pattern: 'sj-council-district-%'
  - mtfcc: 'X0010'
  - EXPECTED_COUNT: 10
  - SC2 coordinate: SJ City Hall lon=-121.88, lat=37.335 (confirmed District 3 via ArcGIS spatial query)
  - SC3 coordinate: Oakland lon=-122.2711, lat=37.8044 (same negative test)
  - Log comments updated to reference SJ (not Fremont)

Run: npx tsx scripts/smoke-sj-geofences.ts
  </action>
  <verify>
Run from C:/EV-Accounts/backend:
  npx tsx scripts/load-sj-council-boundaries.ts --dry-run
  (dry-run logs "Would insert: geo_id=sj-council-district-{1..10}" — 10 lines)
  npx tsx scripts/load-sj-council-boundaries.ts
  (logs "Inserted: District N (sj-council-district-N)" × 10 and "All 10 SJ council districts loaded successfully")
  npx tsx scripts/smoke-sj-geofences.ts
  (logs SC1 PASS, SC2 PASS — District 3, SC3 PASS)
  </verify>
  <done>
smoke-sj-geofences.ts exits 0 with "ALL ASSERTIONS PASSED":
- SC1: 10 rows with geo_id LIKE 'sj-council-district-%' AND mtfcc='X0010' AND state='06'
- SC2: SJ City Hall (-121.88, 37.335) returns exactly 1 district (sj-council-district-3)
- SC3: Oakland (-122.2711, 37.8044) returns 0 rows
  </done>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration 217_sj_government_structure.sql</name>
  <files>C:/EV-Accounts/backend/migrations/217_sj_government_structure.sql</files>
  <action>
Create 217_sj_government_structure.sql following the exact pattern of 213_berkeley_government_structure.sql with these San Jose specifics:

Government:
  name = 'City of San Jose', type = 'LOCAL', state = 'CA', city = 'San Jose', geo_id = '0668000'
  Guard: WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name='City of San Jose' AND state='CA')

Chambers (2 total — NOT 3 like Berkeley):
  1. Mayor: name='Mayor', name_formal='Mayor of San Jose'
     -- TODO Phase 69: set election_method='RCV' on Mayor chamber (SJ uses RCV)
  2. City Council: name='City Council', name_formal='San Jose City Council'
     -- TODO Phase 69: set election_method='RCV' on City Council chamber (SJ uses RCV)
  DO NOT create a City Attorney or City Auditor chamber — both are APPOINTED by City Council per the San Jose City Charter.
  slug is a GENERATED column — never include in INSERT columns.

Local council districts (10 rows):
  geo_ids: 'sj-council-district-1' through 'sj-council-district-10'
  district_type='LOCAL', state='CA'
  labels: 'District 1' through 'District 10'
  Guard: WHERE NOT EXISTS per row (no unique constraint on districts — use same multi-row VALUES pattern as migration 213)

LOCAL_EXEC district (1 row):
  geo_id='0668000', district_type='LOCAL_EXEC', label='San Jose (Citywide)', state='CA'
  Guard: WHERE NOT EXISTS (... WHERE geo_id='0668000' AND state='CA' AND district_type IN ('LOCAL','LOCAL_EXEC'))

Wrap entire migration in BEGIN; ... COMMIT;

Apply via Supabase MCP (mcp__supabase-local__execute_sql) — NOT psql CLI.
Verify with:
  SELECT name, type, state, geo_id FROM essentials.governments WHERE name='City of San Jose';
  SELECT name FROM essentials.chambers WHERE government_id=(SELECT id FROM essentials.governments WHERE name='City of San Jose' AND state='CA');
  SELECT COUNT(*) FROM essentials.districts WHERE geo_id LIKE 'sj-council-district-%' AND state='CA';
  SELECT geo_id, district_type FROM essentials.districts WHERE geo_id='0668000' AND district_type='LOCAL_EXEC' AND state='CA';
  </action>
  <verify>
Via Supabase MCP queries after applying:
  1. SELECT returns 1 row: name='City of San Jose', type='LOCAL', state='CA', geo_id='0668000'
  2. SELECT returns 2 chambers: 'Mayor' and 'City Council' (NOT City Attorney/Auditor)
  3. COUNT(*) returns 10 for sj-council-district-% rows
  4. SELECT returns 1 row: geo_id='0668000', district_type='LOCAL_EXEC'
  </verify>
  <done>
Migration 217 applied idempotently:
- 1 government row: City of San Jose (geo_id='0668000', state='CA')
- 2 chamber rows: Mayor + City Council (no City Attorney, no City Auditor)
- 10 LOCAL district rows: sj-council-district-1 through sj-council-district-10 (state='CA')
- 1 LOCAL_EXEC district row: geo_id='0668000' (San Jose Citywide)
  </done>
</task>

</tasks>

<verification>
Run section-split detector (not expected to find anything yet — no offices seeded — but confirms the government setup is clean before officials migration):

```sql
SELECT ch.name_formal, COUNT(DISTINCT COALESCE(gb.display_name,'')) AS distinct_section_names,
  array_agg(DISTINCT COALESCE(gb.display_name, '(none)') ORDER BY COALESCE(gb.display_name, '(none)')) AS section_names,
  COUNT(DISTINCT o.id) AS office_count
FROM essentials.offices o
JOIN essentials.chambers ch ON ch.id = o.chamber_id
LEFT JOIN essentials.districts d ON d.id = o.district_id
LEFT JOIN essentials.government_bodies gb
  ON gb.geo_id = d.geo_id
  AND gb.state = d.state
  AND gb.body_key = COALESCE(NULLIF(ch.name_formal,''), ch.name)
WHERE ch.name_formal != ''
  AND ch.government_id = (SELECT id FROM essentials.governments WHERE name='City of San Jose' AND state='CA')
GROUP BY ch.name_formal
HAVING COUNT(DISTINCT COALESCE(gb.display_name,'')) > 1
ORDER BY office_count DESC;
-- Expected: 0 rows
```
</verification>

<success_criteria>
1. load-sj-council-boundaries.ts runs cleanly and inserts 10 rows
2. smoke-sj-geofences.ts exits 0 (SC1/SC2/SC3 all PASS)
3. Migration 217 applied: 1 government + 2 chambers + 10 LOCAL districts + 1 LOCAL_EXEC district
4. Section-split detector returns 0 rows
</success_criteria>

<output>
After completion, create `.planning/phases/64-san-jose-deep-seed/64-01-SUMMARY.md`
</output>
