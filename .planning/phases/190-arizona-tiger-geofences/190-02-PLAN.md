---
phase: 190-arizona-tiger-geofences
plan: 02
type: execute
wave: 2
depends_on:
  - 190-01
files_modified: []
autonomous: true
requirements:
  - AZ-GEO-01

must_haves:
  truths:
    - "geofence_boundaries rows exist for state='04' with all 5 MTFCC types (G4020, G4110, G5200, G5210, G5220)"
    - "geofence_boundaries has exactly 15 rows with state='04' AND mtfcc='G4020' (15 AZ counties, no independent cities)"
    - "geofence_boundaries has exactly 9 rows with state='04' AND mtfcc='G5200' (9 AZ congressional districts)"
    - "geofence_boundaries has exactly 30 rows with state='04' AND mtfcc='G5210' (30 AZ legislative-district senate polygons)"
    - "geofence_boundaries has exactly 30 rows with state='04' AND mtfcc='G5220' (30 AZ SLDL polygons — D-04: shared by 2 house seats, NOT 60)"
    - "geofence_boundaries has N rows with state='04' AND mtfcc='G4110' where N matches EXPECTED_AZ_MTFCC.place from Plan 01 (~91 municipalities)"
    - "Tucson, Oro Valley, Marana, Sahuarita, and South Tucson all present as distinct G4110 rows"
    - "Pima County (geo_id='04019') present as a G4020 row"
    - "districts rows exist for Arizona with state IN ('AZ','az') at 4 district_types: az|COUNTY|15, az|STATE_UPPER|30, az|STATE_LOWER|30, AZ|NATIONAL_LOWER|9"
    - "The Catalina Foothills probe returns Pima County G4020 + legislative tiers but NO G4110 and NO G4040 — unincorporated (headline correctness check)"
    - "Section-split gate returns 0 rows — every G5200/G5210/G5220/G4020 geofence has a matching districts row (success criterion #5)"
    - "smoke-az-geofences.ts exits 0 — Catalina Foothills returns no G4110; all 5 municipalities return G4110 + county/CD/SLDU/SLDL"
  artifacts:
    - path: "essentials.geofence_boundaries (Supabase production)"
      provides: "5 layers of AZ TIGER boundaries"
      contains: "state='04' rows for G4020, G4110, G5200, G5210, G5220"
    - path: "essentials.districts (Supabase production)"
      provides: "AZ district rows with correct state casing"
      contains: "state='az' for COUNTY/STATE tiers; state='AZ' for NATIONAL_LOWER"
  key_links:
    - from: "TIGER PLACE layer (tl_2024_04_place.zip)"
      to: "geofence_boundaries G4110 rows"
      via: "upsertGeofence() with ST_MakeValid + state='04'; G4110-only MTFCC filter"
      pattern: "mtfcc='G4110' AND state='04'"
    - from: "TIGER COUNTY layer (tl_2024_us_county.zip filtered STATEFP='04')"
      to: "geofence_boundaries G4020 rows (including Pima County geo_id='04019')"
      via: "filterByStatefp=true in processLayer()"
      pattern: "mtfcc='G4020' AND state='04'"
    - from: "TIGER SLDU/SLDL layers"
      to: "districts table"
      via: "insertDistrictIfMissing() with abbrev='az' (lowercase)"
      pattern: "state='az' AND district_type IN ('STATE_UPPER','STATE_LOWER')"
    - from: "TIGER CD119 layer"
      to: "districts table NATIONAL_LOWER rows (PRE-EXISTING — from a prior seed, uppercase 'AZ')"
      via: "insertDistrictIfMissing() writes lowercase 'az' but all 9 CD geo_ids already exist as uppercase 'AZ' from a prior seed, so the WHERE NOT EXISTS guard SKIPS them — the uppercase Gate 5 rows come from that seed, not this load"
      pattern: "state='AZ' AND district_type='NATIONAL_LOWER'"
---

<objective>
Live-load all 5 AZ TIGER layers into geofence_boundaries and districts using the confirmed EXPECTED_AZ_MTFCC counts from Plan 01, then verify via the SQL gate script and the 6-address smoke test. This is the data-write that makes Arizona geographically routable.

Purpose: This is the primary data write for Phase 190. After this plan completes, all AZ address routing is possible end-to-end. Phases 191-199 (state government, federal officials, legislature, Pima County + Tucson-metro deep-seeds, elections) all depend on these geofence and district rows existing for state='04'/'az'/'AZ'. The headline correctness check is Catalina Foothills resolving to Pima County with NO city (the AZ analog of NV's Strip check).

Output: geofence_boundaries and districts rows for state='04' (all 5 layers); verify SQL all gates green (especially the unincorporated-Pima Gate 4 and the section-split Gate 7); smoke test exits 0 for 6 AZ addresses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/190-arizona-tiger-geofences/190-CONTEXT.md
@.planning/phases/190-arizona-tiger-geofences/190-01-PLAN.md
@.planning/phases/190-arizona-tiger-geofences/190-01-SUMMARY.md
@.planning/milestones/v18.0-phases/158-nevada-tiger-geofences/158-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load all 5 AZ TIGER layers into geofence_boundaries and districts</name>
  <read_first>
    .planning/phases/190-arizona-tiger-geofences/190-01-SUMMARY.md — Confirm the actual sldl count (must be 30 per D-04) and place count (N) recorded from Plan 01 Task 3 dry-run, the verified cd119 URL filename, and the pre-existing-row check result (greenfield or stray-row inventory). The loader EXPECTED_AZ_MTFCC must contain these confirmed values before running live.
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Verify EXPECTED_AZ_MTFCC has sldl=30 and a non-zero place value (Plan 01 edits still in place); confirm STATE_LAYER_ALLOWLIST/STATE_CITY_ASSERTIONS/STATE_RUN_MAKEVALID all contain AZ entries.
    .planning/phases/190-arizona-tiger-geofences/190-CONTEXT.md — Do NOT touch any pre-existing AZ officials/government data (there is none per roadmap greenfield assertion; Phases 191+ own that). This plan writes ONLY geofence_boundaries + districts rows.
  </read_first>
  <action>
    Confirm Plan 01 is complete before proceeding: EXPECTED_AZ_MTFCC.sldl must be 30 and EXPECTED_AZ_MTFCC.place must be non-zero in load-state-tiger-boundaries.ts. If sldl is still 0 (or 60) or place is 0, STOP — Plan 01 Task 3 must be completed/corrected first; this task cannot proceed with sentinel or wrong values (the pre-flight assertion would abort).

    From C:/EV-Accounts/backend, run the live load command for all 5 AZ layers (NO --dry-run flag):

      npx tsx scripts/load-state-tiger-boundaries.ts --state AZ --fips 04 --layers cd119,sldu,sldl,place,county

    Before the run, confirm the loader prints a State+FIPS banner reading AZ (FIPS 04) — this guards against a wrong-state clobber. The loader will:
      1. Download all 5 TIGER zip files from census.gov (cached from Plan 01 dry-run; county is the ~80MB national file)
      2. Run the EXPECTED_AZ_MTFCC pre-flight assertion for each layer (must pass before any DB write)
      3. Run the STATE_CITY_ASSERTIONS check for the place layer (confirm all 5 AZ municipalities present in NAMELSAD)
      4. Stream each shapefile and upsert rows into essentials.geofence_boundaries with ON CONFLICT (geo_id, mtfcc) DO NOTHING; ST_MakeValid applied per STATE_RUN_MAKEVALID
      5. Insert corresponding district rows into essentials.districts via insertDistrictIfMissing() (WHERE NOT EXISTS guard). The loader writes state='az' (LOWERCASE) at every call site — there is no abbrevUpper branch. county/sldu/sldl are new and land as lowercase 'az'. cd119 NATIONAL_LOWER inserts are SKIPPED by the guard because all 9 CD geo_ids already exist as uppercase 'AZ' from a prior seed (verified in Plan 01 STEP A). Gate 5's uppercase AZ|NATIONAL_LOWER|9 therefore reflects the pre-existing seed rows, NOT new writes.

    Monitor console output for these expected results per layer:
      - cd119: pre-flight PASSED 9 records; upsert inserts 9 G5200 boundary rows. District-row inserts: the 9 NATIONAL_LOWER rows already exist (uppercase 'AZ', prior seed — see Plan 01 STEP A), so insertDistrictIfMissing logs already_exists for all 9 and writes NO new district rows. If Plan 01 found the NATIONAL_LOWER rows ABSENT, STOP (the loader would write lowercase 'az' and break Gate 5) — refer to the SUMMARY.
      - sldu: pre-flight PASSED 30 records; upsert inserts 30 G5210 boundary rows + 30 az|STATE_UPPER district rows
      - sldl: pre-flight PASSED 30 records; upsert inserts 30 G5220 boundary rows + 30 az|STATE_LOWER district rows (D-04: one polygon per legislative district, shared by 2 house seats)
      - place: pre-flight PASSED N records (N from Plan 01, ~91); upsert inserts N G4110 boundary rows; the place layer does NOT write district rows
      - county: pre-flight PASSED 15 records; the county shapefile is national (tl_2024_us_county.zip), filtered by STATEFP='04'; upsert inserts 15 G4020 boundary rows + 15 az|COUNTY district rows (including Pima County geo_id='04019')

    Watch for these failure modes and STOP if any occur:
      - Any MtfccAssertionError — EXPECTED_AZ_MTFCC value drifted from actual; re-run Plan 01 Task 3 to refresh
      - STATE_CITY_ASSERTIONS gate FAILED — an AZ municipality not found in NAMELSAD; investigate casing/LSAD and stop
      - sldl inserts 60 rows instead of 30 — the D-04 assumption is broken; stop and escalate (phase 192 seeding depends on 30)
      - Any non-zero ERROR line that is NOT already_exists — investigate before retrying
      - Loader exits non-zero — read the error; ON CONFLICT DO NOTHING makes re-running safe so a retry after a fix is acceptable

    Do NOT write a migration file for this load; the loader script IS the data-writing mechanism for TIGER geofences (established pattern from NV/VA/ME/OR/MD/CA/TX/UT/IN/MA). The on-disk migration counter is NOT advanced by this load.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx -e "require('dotenv').config(); const {Client}=require('pg'); const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(\"SELECT mtfcc, COUNT(*) AS n FROM essentials.geofence_boundaries WHERE state='04' GROUP BY mtfcc ORDER BY mtfcc\")).then(r=>{r.rows.forEach(x=>console.log(x.mtfcc,x.n)); return c.end()})"</automated>
  </verify>
  <acceptance_criteria>
    - Loader exits 0 (no non-zero error exit)
    - The State+FIPS banner reads AZ (FIPS 04) before any write
    - sldu layer logs 30 G5210 boundary rows upserted
    - sldl layer logs 30 G5220 rows upserted (D-04 — NOT 60)
    - cd119 layer logs 9 G5200 rows (inserted or already_exists per Plan 01 pre-existing-row check)
    - place layer logs N G4110 rows upserted (N = EXPECTED_AZ_MTFCC.place confirmed in Plan 01)
    - county layer logs 15 G4020 rows upserted
    - The mtfcc count query returns G4020=15, G4110=N, G5200=9, G5210=30, G5220=30 — G4110 matching Plan 01 confirmed count
    - No MtfccAssertionError in loader output
    - STATE_CITY_ASSERTIONS gate passes for all 5 AZ municipalities
  </acceptance_criteria>
  <done>All 5 AZ TIGER layers loaded into geofence_boundaries and districts. geofence_boundaries has rows for all 5 MTFCC types at state='04' (G5220=30 per D-04). districts has rows for az|COUNTY|15, az|STATE_UPPER|30, az|STATE_LOWER|30, AZ|NATIONAL_LOWER|9.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the live load — geometry gates, casing, unincorporated-Pima probe, section-split gate</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/verify-az-tiger-import.sql — The gate verification script created in Plan 01. Read it to understand expected values for each gate before running.
    .planning/phases/190-arizona-tiger-geofences/190-01-SUMMARY.md — Actual sldl (30) and place (N) counts; match against Gate 3 expected counts.
    .planning/phases/190-arizona-tiger-geofences/190-CONTEXT.md — D-03 (Catalina Foothills is unincorporated — Gate 4 must show NO G4110) and the D-00 districts casing rule (uppercase 'AZ' for NATIONAL, lowercase 'az' for state-subdivision tiers).
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the verification SQL:

      psql $DATABASE_URL -f scripts/verify-az-tiger-import.sql

    If psql is unavailable (gsd-executor has no Supabase MCP per milestone conventions), run each gate inline via the same npx tsx -e inline-pg pattern used in Task 1's verify command, substituting each gate query. Check each gate result against the expected values in the SQL file comments.

    Expected gate results:
      - Gate 1 (invalid geometry): 0 rows — no invalid geometries for state='04'
      - Gate 2 (GeometryCollection): 0 rows — no GeometryCollection types for state='04'
      - Gate 3 (per-layer counts): G4020=15, G4110=N (Plan 01 place count), G5200=9, G5210=30, G5220=30
      - Gate 4 (unincorporated-Pima invariant — HEADLINE CHECK): the spatial probe at the Catalina Foothills coordinate returns a G4020 row named 'Pima County' (geo_id='04019') plus the G5200/G5210/G5220 legislative tiers, and NO G4110 row and NO G4040 row. If a G4110 row IS returned, Catalina Foothills is being falsely matched to a city — STOP and investigate (pick a coordinate clearly inside unincorporated Catalina Foothills, outside Tucson/Oro Valley city limits)
      - Gate 5 (districts casing): az|COUNTY|15, az|STATE_UPPER|30, az|STATE_LOWER|30, AZ|NATIONAL_LOWER|9 — confirm lowercase 'az' for county/sldu/sldl and uppercase 'AZ' for cd119
      - Gate 6 (Pima County sentinel): exactly 1 row — geo_id='04019' 'Pima County' G4020
      - Gate 7 (section-split OR-direction): 0 rows — every G5200/G5210/G5220/G4020 geofence for state='04' has a matching districts row (success criterion #5)

    Stop and investigate if any gate returns unexpected results:
      - Gate 1/2 non-zero: invalid/mixed geometry in DB — the loader's ST_MakeValid may have failed on some records; check which MTFCC types are affected
      - Gate 3 count mismatch: a layer loaded fewer rows than expected; check for ON CONFLICT skips vs actual missing records. G5220 MUST be 30 (D-04)
      - Gate 4 returns a G4110 row: the unincorporated-Pima invariant is violated — the headline correctness check fails; investigate the probe coordinate and the place polygons covering it
      - Gate 5 casing wrong (e.g. 'AZ' for county, or 'az' for cd119): the D-00 casing rule is violated; backend queries matching exact casing would miss those tiers
      - Gate 7 returns any rows: section-split bug — a legislative or county geofence is missing its districts row; insertDistrictIfMissing() skipped it
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx -e "require('dotenv').config(); const {Client}=require('pg'); const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(\"SELECT mtfcc FROM essentials.geofence_boundaries WHERE state='04' AND ST_Covers(geometry, ST_SetSRID(ST_MakePoint(-110.9210,32.3130),4326)) ORDER BY mtfcc\")).then(r=>{const m=r.rows.map(x=>x.mtfcc); console.log('Catalina Foothills tiers:',m.join(',')); console.log('G4110_present:',m.includes('G4110')); console.log('G4040_present:',m.includes('G4040')); return c.end()})"</automated>
  </verify>
  <acceptance_criteria>
    - verify-az-tiger-import.sql runs to completion with no error exit (or all gates run inline with no error)
    - Gate 1 returns 0 rows (no invalid geometries for state='04')
    - Gate 2 returns 0 rows (no GeometryCollection types)
    - Gate 3 shows G4020=15, G5200=9, G5210=30, G5220=30; G4110 matches confirmed count from Plan 01
    - Gate 4 (Catalina Foothills probe): the verify command above prints G4110_present: false and G4040_present: false, and the tier list includes G4020 (Pima County) plus G5200/G5210/G5220 — headline check satisfied
    - Gate 5 includes rows: az|COUNTY|15, az|STATE_UPPER|30, az|STATE_LOWER|30, AZ|NATIONAL_LOWER|9 with correct casing
    - Gate 6 returns exactly 1 row: Pima County (geo_id='04019')
    - Gate 7 returns 0 rows (no section-split — OR-direction check passes; success criterion #5)
  </acceptance_criteria>
  <done>All SQL gates pass. Catalina Foothills resolves to Pima County with NO city (headline check). Districts casing correct (az/AZ per tier). Section-split gate clean (success criterion #5). Database state consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Run smoke-az-geofences.ts — all 6 AZ addresses must route correctly</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/smoke-az-geofences.ts — The smoke test created in Plan 01. Confirm expectedCounts.G4110 and expectedCounts.G5220 match the confirmed Plan 01 dry-run counts (G5220 must be 30; updated in Plan 01 Task 3 Edit B).
    .planning/phases/190-arizona-tiger-geofences/190-01-SUMMARY.md — Confirmed sldl count (G5220=30) and place count (G4110) for cross-checking smoke test expectedCounts.
    .planning/phases/190-arizona-tiger-geofences/190-CONTEXT.md — The 6 verification addresses (Catalina Foothills + 5 municipalities) and the Catalina-Foothills-forbids-G4110 invariant.
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the smoke test:

      npx tsx scripts/smoke-az-geofences.ts

    The test queries production via DATABASE_URL (dotenv loaded from .env). It runs 6 address assertions:

      Address 1 (Catalina Foothills — unincorporated Pima County):
        - expectedMtfcc: G4020, G5200, G5210, G5220 (county + legislative tiers)
        - forbiddenMtfcc: G4110, G4040 (unincorporated — must NOT return a city or township)
        - expectedGeoIds: G4020='04019' (Pima County)
        - This is the headline correctness check

      Addresses 2-6 (Tucson, Oro Valley, Marana, Sahuarita, South Tucson):
        - expectedMtfcc: G4020, G4110, G5200, G5210, G5220 (all 5 tiers — incorporated municipality)
        - expectedGeoIds: G4020='04019' (all in Pima County; Marana Town Hall is Pima-side)
        - These verify each municipality routes to its own G4110 tier. Note South Tucson must return 'South Tucson city', not 'Tucson city'

    Watch for:
      - Any EXPECTED MTFCC MISSING error — a tier is absent for a test address; check which layer failed to load (most likely place or sldl if their sentinels were not updated)
      - Any FORBIDDEN MTFCC PRESENT error for Address 1 (Catalina Foothills) — the coordinate is inside an incorporated place or township; headline-check failure — shift the coordinate clearly into unincorporated Foothills and document
      - Any EXPECTED GEO_ID MISMATCH for G4020='04019' — the Pima County geo_id is wrong, OR (for Marana) the coordinate landed in Pinal County (04021); refine the coordinate or adjust and document
      - SC3 expectedCounts mismatch — if G4110 or G5220 count in DB differs from smoke test expectedCounts; re-check Plan 01 Task 3 Edit B

    If smoke-az-geofences.ts exits 1, read the console output for which assertion failed and investigate the specific geofence_boundaries query for that address. Coordinates may be refined to land cleanly inside the intended boundary; the criterion is correct tier resolution, not the exact coordinate.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-az-geofences.ts; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsx scripts/smoke-az-geofences.ts exits 0
    - Console output: Catalina Foothills (Address 1) returns G4020 (Pima County, geo_id='04019') + G5200/G5210/G5220 and NO G4110/G4040 (forbiddenMtfcc satisfied — headline check)
    - Console output: Tucson, Oro Valley, Marana, Sahuarita, and South Tucson each return all 5 MTFCC tiers including G4110 + county/CD/SLDU/SLDL
    - Console output shows SC3 pass: total counts for state='04' match expected (G4020=15, G5200=9, G5210=30, G5220=30, G4110=confirmed count from Plan 01)
    - Console output shows SC4 pass: no NULL/empty name rows for any state='04' geofence tier returned
    - No EXPECTED MTFCC MISSING, FORBIDDEN MTFCC PRESENT, or EXPECTED GEO_ID MISMATCH lines in output
    - smoke-az-geofences.ts exit code is 0 (not 1)
  </acceptance_criteria>
  <done>All 6 AZ address assertions pass. AZ-GEO-01 satisfied end-to-end: Catalina Foothills routes to Pima County with no city; the 5 incorporated municipalities route to their G4110 tier plus county/CD/SLDU/SLDL; all tiers loaded (G5220=30 per D-04); section-split clean (via Task 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader -> Supabase production | Live writes to essentials.geofence_boundaries and essentials.districts — this is the only plan that writes data (mcp__supabase-local IS the production DB) |
| loader -> census.gov | HTTPS download of TIGER zip files for the live load (same files cached from Plan 01) |
| smoke test / verify -> Supabase production | Read-only spatial queries via DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-08 | Integrity | Accidental wrong-state clobber (--fips 04 with a typo'd --state) writing AZ rows under another state's FIPS | mitigate | main() round-trip check rejects mismatched --state/--fips; the FIPS arg is the literal state column value; loader prints State+FIPS banner before any write; executor confirms the banner reads AZ (FIPS 04) |
| T-190-09 | Integrity | Live load with stale/wrong sentinel in EXPECTED_AZ_MTFCC (sldl 0 or 60, place 0) | mitigate | Task 1 requires executor to confirm Plan 01 completion; EXPECTED_AZ_MTFCC.sldl must be 30 and place non-zero before running live; loader pre-flight assertion blocks DB write on count mismatch |
| T-190-10 | Integrity | SLDL written as 60 rows (D-04 violation) breaking phase 192 seeding | mitigate | Task 1 acceptance requires sldl=30; Gate 3 SQL re-verifies G5220=30; two independent checks |
| T-190-11 | Integrity | ON CONFLICT DO NOTHING silently drops rows | accept | Established loader pattern; the EXPECTED_AZ_MTFCC pre-flight assertion validates shapefile count before upsert; post-load Gate 3 SQL verifies DB row counts match expected |
| T-190-12 | Integrity | Wrong district state casing (AZ vs az) breaks backend tier matching | mitigate | Gate 5 SQL verifies both state='az' (COUNTY/STATE) and state='AZ' (NATIONAL_LOWER); mismatch caught before the phase is marked complete |
| T-190-13 | Integrity | Catalina Foothills falsely matched to a city geofence (headline-check failure) | mitigate | Gate 4 SQL probe + smoke test Address 1 forbiddenMtfcc both assert NO G4110/G4040 at Catalina Foothills; two independent checks |
| T-190-14 | Availability | 80MB national county zip causes timeout/OOM | accept | tl_2024_us_county.zip already used by NV/VA/OR/MD phases without issue; filtered by STATEFP='04' before upsert; cached from Plan 01 |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; all deps pre-existing in EV-Accounts/backend (pg, dotenv, shapefile, adm-zip) |
</threat_model>

<verification>
After all 3 tasks complete:
1. mtfcc count query for state='04' returns 5 rows: G4020/15, G4110/N, G5200/9, G5210/30, G5220/30 (N from Plan 01 confirmed count) — success criteria #1-#4
2. Catalina Foothills probe (-110.9210, 32.3130): G4110_present=false, G4040_present=false, tier list includes G4020 (Pima County) + G5200/G5210/G5220 — headline check
3. districts casing query: state IN ('AZ','az') returns az|COUNTY|15, az|STATE_UPPER|30, az|STATE_LOWER|30, AZ|NATIONAL_LOWER|9
4. Section-split gate (G5200/G5210/G5220/G4020 geofences with no matching districts row): 0 rows — success criterion #5
5. npx tsx scripts/smoke-az-geofences.ts exits 0 — end-to-end routing verified
</verification>

<success_criteria>
- AZ-GEO-01: geofence_boundaries rows exist for state='04' with all 5 MTFCC types (G4020/15, G4110/N, G5200/9, G5210/30, G5220/30) — loader keys verified from actual TIGER zip filenames (roadmap SC1-SC4)
- G4110 incorporated-place boundaries loaded for all AZ municipalities including Tucson, Oro Valley, Marana, Sahuarita, South Tucson (SC1)
- G4020 county boundaries for all 15 AZ counties including Pima County geo_id='04019' (SC2)
- CD boundaries for AZ's 9 US House districts (SC3); SLDU 30 + SLDL 30 (2-seat, D-04) (SC4)
- Section-split scan returns 0 defects (SC5) — verified by Gate 7 OR-direction
- Headline check: a Catalina Foothills address returns Pima County tiers with NO city (no false G4110 match) — verified by Gate 4 probe and smoke test Address 1 forbiddenMtfcc
- districts table has correct state casing: lowercase 'az' for COUNTY/STATE tiers, uppercase 'AZ' for NATIONAL_LOWER (D-00)
- smoke-az-geofences.ts exits 0 for all 6 addresses
</success_criteria>

<output>
Create `.planning/phases/190-arizona-tiger-geofences/190-02-SUMMARY.md` when done. Record final row counts for all 5 MTFCC types (confirm G5220=30 per D-04), confirmation of the Catalina-Foothills-unincorporated result (no G4110/G4040), districts casing, Gate 7 result, and smoke test exit code. Note next phase: Phase 191 (Arizona State & Federal Government).
</output>
