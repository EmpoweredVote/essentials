---
phase: 165-boulder-city-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["165-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-boulder-city-council-headshots.py
  - C:/EV-Accounts/backend/migrations/1101_boulder_city_council_headshots.sql
  - .planning/phases/165-boulder-city-deep-seed/165-02-SUMMARY.md
autonomous: false
requirements: [CLARK-05]
user_setup: []

must_haves:
  truths:
    - "Each of the 5 Boulder City council members (Mayor Hardy + Jorgensen / Booth / Walton / Ashurst) has a 600×750 (4:5) headshot mirrored to Supabase Storage politician_photos/{uuid}-headshot.jpg; 0 gaps expected (all 5 flybouldercity.com sources confirmed HTTP 200), any genuine gap documented (no fabrication) [D-06]"
    - "Headshots are crop-to-4:5 FIRST then resize to 600×750 Lanczos q90 — never stretch; no text/graphic overlays over the face; each visually spot-checked for correct-person before insert [D-06]"
    - "flybouldercity.com is CLEAN (no WAF) — all 5 individual portraits served via /ImageRepository/Document?documentId={10964,9459,10924,10899,14763}; standard Chrome UA sufficient; NO Wikimedia descriptive UA needed; documentId param is lowercase on flybouldercity.com [D-06]"
    - "Migration 1101 inserts politician_images rows (id, politician_id, url, type='default', photo_license) resolved by external_id — AUDIT-ONLY, NOT registered, structural ledger stays at 1100 (or Wave-0 number) [D-06]"
    - "politician_images has NO image-origin-url column; columns are exactly (id, politician_id, url, type, photo_license); photo_license set per member from the actual source (flybouldercity.com official city portal → 'us_government_work' unless the operator determines otherwise at execution) [D-06]"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-boulder-city-council-headshots.py"
      provides: "Headshot pipeline: per-member flybouldercity.com ImageRepository URL → download → crop 4:5 FIRST → resize 600×750 Lanczos q90 → upload to Storage politician_photos/{uuid}-headshot.jpg; UUID resolved at runtime from external_id via psycopg2; emits a manifest (SUCCESS ext_id→uuid→cdn_url→license, FAILED for any gap)"
      contains: "600"
    - path: "C:/EV-Accounts/backend/migrations/1101_boulder_city_council_headshots.sql"
      provides: "Audit-only headshot migration: one INSERT block per member (external_id -3208001..-3208005), type='default', photo_license from manifest, idempotent NOT EXISTS, NOT registered"
      contains: "essentials.politician_images"
  key_links:
    - from: "5 council members (external_id -3208001..-3208005)"
      to: "essentials.politician_images (url → Storage CDN politician_photos/{uuid}-headshot.jpg)"
      via: "politician_id resolved by external_id; type='default'"
      pattern: "politician_images"
    - from: "_tmp-boulder-city-council-headshots.py manifest (cdn_url + license per member)"
      to: "migration 1101 INSERT blocks"
      via: "manifest SUCCESS line → migration url + photo_license values"
      pattern: "headshot.jpg"
---

<objective>
Source, process, and install 600×750 headshots for all 5 City of Boulder City council members. Unlike Henderson and North Las Vegas (Akamai WAF-403), flybouldercity.com is CLEAN — all 5 individual council portraits are served directly via the ImageRepository endpoint with no WAF (HTTP 200 confirmed in research, all high-resolution 1733×2042 to 2080×2600). DocumentIds are pre-confirmed: Hardy 10964, Jorgensen 9459, Booth 10924, Walton 10899, Ashurst 14763. Standard Chrome UA is sufficient (no descriptive Wikimedia UA needed). Crop to 4:5 FIRST then resize to 600×750 Lanczos q90, reject any image with text/graphic overlays over the face, visually spot-check each for correct-person, mirror to Storage politician_photos/{uuid}-headshot.jpg, and write the audit-only migration 1101 to insert politician_images rows. 0 gaps expected; document any genuine gap honestly — no fabrication.

Purpose: Satisfies CLARK-05 SC#2 (all seated officials render with 600×750 headshots; genuine gaps documented).
Output: _tmp-boulder-city-council-headshots.py + the headshot manifest, 1101_boulder_city_council_headshots.sql (audit-only), recorded results + the correct-person spot-check + any documented gaps in 165-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and NO DB access):**
- The executor WRITES _tmp-boulder-city-council-headshots.py and 1101_boulder_city_council_headshots.sql to disk. The executor does NOT run the script, run DB SELECTs, or apply migrations.
- Running the headshot script (`python scripts/_tmp-boulder-city-council-headshots.py` from C:/EV-Accounts so it reads backend/.env for DATABASE_URL + SUPABASE_SERVICE_ROLE_KEY), the visual correct-person spot-check, applying mig 1101, and every verification SELECT/curl are INLINE ORCHESTRATOR / operator steps.
- The 5 source URLs are pre-confirmed (flybouldercity.com ImageRepository DocumentIds) — no per-member sourcing research is needed at execution time unless a URL 404s.
- The script + migration live in the SEPARATE repo C:/EV-Accounts/backend/ (commit via `git -C "C:/EV-Accounts"`; the _tmp-* script is gitignored). SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/165-boulder-city-deep-seed/165-RESEARCH.md
@.planning/phases/165-boulder-city-deep-seed/165-PATTERNS.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
The 5 external_id→UUID map is in 165-01-SUMMARY.md (minted by mig 1100). UUID resolved at runtime from external_id via psycopg2 — never hardcode a UUID in the script.
Bucket: politician_photos (NOT politician-headshots). Storage path: {politician_uuid}-headshot.jpg. CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
politician_images columns: (id, politician_id, url, type, photo_license) — NO image-origin-url column (removed). type='default' on all headshot rows.
Pipeline invariants: crop to 4:5 ratio FIRST, THEN resize 600×750 Lanczos q90, optimize=True strips EXIF; RGBA/transparent → white-composite (no black halo); MIN_DIM=100. Never stretch/resize before cropping (memory feedback_headshot_resize_no_distort).
Source: flybouldercity.com is CLEAN (no WAF, HTTP 200 confirmed all 5). Standard Chrome UA (BROWSER_HEADERS) sufficient. NO descriptive Wikimedia UA needed (no Wikimedia sources for Boulder City). documentId param is LOWERCASE on flybouldercity.com (vs uppercase documentID on bcnv.org) — use the flybouldercity.com domain + lowercase param.
Roster + confirmed sources (all flybouldercity.com/ImageRepository/Document?documentId=N; all license='us_government_work' unless operator revises at execution):
  -3208001 Joe Hardy (Mayor): documentId=10964 (2080×2499 RGB, HTTP 200)
  -3208002 Sherri Jorgensen: documentId=9459 (2080×2600 RGB, HTTP 200)
  -3208003 Cokie Booth: documentId=10924 (1857×2600 RGB, HTTP 200)
  -3208004 Steve Walton: documentId=10899 (2080×2600 RGB, HTTP 200)
  -3208005 Denise E. Ashurst: documentId=14763 (1733×2042 RGB, HTTP 200)
All 5 are well above 600×750 — pure downscale, no upscaling artifacts; all RGB (the RGBA→white-composite guard is harmless and stays in).
photo_license guidance: flybouldercity.com is an official city portal → 'us_government_work'. Operator may revise per actual source at execution. Reject any image with campaign banners / "Re-Elect" text / graphic overlays over the face (memory feedback_headshot_no_graphics).
Gap handling: if a URL 404s at execution (small CMS-refresh risk), fall back to bcnv.org / Ballotpedia, OR emit a FAILED manifest line and document the gap in SUMMARY — do NOT fabricate or insert a placeholder image. 0 gaps expected.
Migration 1101 is AUDIT-ONLY: NOT registered; structural ledger stays at 1100 (or Wave-0 number). If Wave-0 renumbered the structural migration, shift 1101 by the same offset.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the headshot pipeline script + run it + correct-person spot-check</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-boulder-city-council-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-north-las-vegas-council-headshots.py (EXACT ANALOG — copy ALL functions verbatim; only the docstring, OFFICIALS roster, guard assertions, and manifest label change)
    - .planning/phases/165-boulder-city-deep-seed/165-PATTERNS.md (section "_tmp-boulder-city-council-headshots.py" — the OFFICIALS config block, the guard range, the list of functions to copy verbatim, the Wikimedia-UA-not-needed note, the manifest label)
    - .planning/phases/165-boulder-city-deep-seed/165-RESEARCH.md (§Headshot Sources — the 5 documentIds + sizes + photo_license guidance; Pitfall 3 flybouldercity.com-vs-bcnv.org URL/param case)
    - .planning/phases/165-boulder-city-deep-seed/165-01-SUMMARY.md (the 5 external_id→UUID map minted by mig 1100 + the confirmed structural migration number)
  </read_first>
  <action>
EXECUTOR STEP: write `_tmp-boulder-city-council-headshots.py` by copying `_tmp-north-las-vegas-council-headshots.py` verbatim for all functions — resolve_politician_id, download_image, crop_to_4_5, resize_600x750, upload_to_storage, process_headshot_bytes (RGBA→white-composite, MIN_DIM=100, q90, optimize=True), process_member, main — plus the env-load + DB-connect block (reads C:/EV-Accounts/backend/.env: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL; BUCKET='politician_photos'; CDN_BASE as in interfaces; TARGET_SIZE=(600,750); JPEG_QUALITY=90; RESAMPLE=LANCZOS; BROWSER_HEADERS Chrome UA). Replace the config block with the 5-member OFFICIALS roster (ext_id, name, url=flybouldercity.com/ImageRepository/Document?documentId=N with the lowercase documentId param, license='us_government_work') from the interfaces section — all 5 URLs are pre-confirmed (no per-member sourcing research). Add the guard assertions: len(OFFICIALS)==5, unique ext_ids, all ext_ids in -3208005..-3208001. Update the docstring: city=Boulder City, Phase 165 (CLARK-05), note flybouldercity.com has NO WAF (Chrome UA sufficient) and that documentId is lowercase on this domain; remove the Akamai-WAF / NLV-cityofnorthlasvegas.com note and the Wikimedia-descriptive-UA caveat (no Wikimedia sources — the WIKIMEDIA_HEADERS dict + the 'wikimedia.org' download branch may stay in as a harmless no-op, or be removed). Adapt the manifest label to "=== BOULDER CITY CITY COUNCIL HEADSHOT MANIFEST ===" and "{successes}/5 uploaded, {gaps} gaps", each SUCCESS line carrying ext_id → uuid → cdn_url → license (consumed by migration 1101). Keep crop-4:5-FIRST then resize-600×750.

INLINE ORCHESTRATOR STEP: run `python scripts/_tmp-boulder-city-council-headshots.py` from C:/EV-Accounts. Confirm it processes 5 members, crops 4:5 → 600×750, uploads to Storage, and prints the manifest. Then VISUALLY SPOT-CHECK each uploaded headshot for correct-person (right individual, head + shoulders, no text/graphic overlay over the face — reject + document if any image is wrong-person or overlaid). Record the manifest (SUCCESS + any FAILED lines) + the spot-check outcome in the SUMMARY. If a flybouldercity.com URL 404s, fall back to bcnv.org / Ballotpedia or emit a FAILED line + document the gap (no fabrication).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-boulder-city-council-headshots.py"; test -f "$F" &amp;&amp; grep -q "600" "$F" &amp;&amp; grep -q "750" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; grep -q "flybouldercity.com" "$F" &amp;&amp; grep -q -- "-3208001" "$F" &amp;&amp; grep -q -- "-3208005" "$F" &amp;&amp; grep -q "external_id" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-boulder-city-council-headshots.py exists; all pipeline functions copied verbatim from the NLV script; crop-4:5-FIRST then resize-600×750 preserved.
    - OFFICIALS roster has 5 members (-3208001..-3208005) with the 5 confirmed flybouldercity.com documentId URLs (lowercase documentId param) + license='us_government_work'; guard assertions present (count 5, unique, in range).
    - Source is flybouldercity.com (NOT bcnv.org for the individual portraits); no Akamai/Wikimedia-UA handling required.
    - UUID resolved at runtime from external_id (psycopg2) — no hardcoded UUID.
    - INLINE: script run; manifest recorded in SUMMARY (SUCCESS ext_id→uuid→cdn_url→license; any FAILED line); correct-person spot-check recorded.
    - Any image with a text/graphic overlay over the face is rejected; gaps documented, never fabricated. 0 gaps expected.
  </acceptance_criteria>
  <done>Headshot script written + run; 5 flybouldercity.com portraits cropped 4:5 → 600×750 → Storage; correct-person spot-check + manifest recorded; 0 gaps expected, any gap documented.</done>
</task>

<task type="auto">
  <name>Task 2: Write audit-only migration 1101 + apply inline + verify</name>
  <files>C:/EV-Accounts/backend/migrations/1101_boulder_city_council_headshots.sql, .planning/phases/165-boulder-city-deep-seed/165-02-SUMMARY.md</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1094_north_las_vegas_city_council_headshots.sql (EXACT ANALOG — one INSERT block per member, type='default', NOT EXISTS idempotency, audit-only / unregistered)
    - .planning/phases/165-boulder-city-deep-seed/165-PATTERNS.md (section "1101_boulder_city_council_headshots.sql" — the per-member INSERT block family + CDN URL format + the forbidden-token paraphrase rules + the all-us_government_work license note)
  </read_first>
  <action>
EXECUTOR STEP: write `1101_boulder_city_council_headshots.sql` modeled on 1094. Header comment: file name, Phase 165 (CLARK-05), AUDIT-ONLY (NOT registered, structural ledger stays at 1100 / Wave-0 number), columns exactly (id, politician_id, url, type, photo_license), idempotent via NOT EXISTS, type='default'. Paraphrase the removed image-origin column note ("the removed image-origin column is intentionally absent") — do NOT write the literal removed-column token. BEGIN; then one INSERT block per member (external_id -3208001..-3208005, only the members with a SUCCESS manifest line): `INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) SELECT gen_random_uuid(), (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>), '<cdn_url_from_manifest>', 'default', '<license_from_manifest>' WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>))`. CDN URL format: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg. photo_license per member from the manifest (all 5 expected 'us_government_work'). COMMIT. NO ledger-registration line (audit-only). For any FAILED member, OMIT the block and document the gap in SUMMARY (no placeholder row).

INLINE ORCHESTRATOR STEP: apply 1101 via `psql -f` (DATABASE_URL from C:/EV-Accounts/backend/.env). Then verify + record in SUMMARY:
- `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -3208005 AND -3208001` → target 5 minus documented gaps (0 expected); list any gaps.
- `curl -sI` each CDN url (the SUCCESS members) → 200.
- `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '1101'` → expect 0 rows (audit-only, ledger stays at 1100).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1101_boulder_city_council_headshots.sql"; S=".planning/phases/165-boulder-city-deep-seed/165-02-SUMMARY.md"; test -f "$F" &amp;&amp; grep -q "essentials.politician_images" "$F" &amp;&amp; grep -q "'default'" "$F" &amp;&amp; grep -q "NOT EXISTS" "$F" &amp;&amp; test -f "$S" &amp;&amp; grep -qi "headshot" "$S" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1101_boulder_city_council_headshots.sql exists; one INSERT block per SUCCESS member; type='default'; NOT EXISTS idempotency; photo_license per member from manifest (expected all 'us_government_work').
    - CDN URL format matches politician_photos/{uuid}-headshot.jpg.
    - No ledger-registration line (audit-only); no forbidden tokens in comments (image-origin column paraphrased).
    - INLINE: applied; politician_images count recorded (target 5 minus gaps, 0 expected); CDN urls return 200; any gap listed in SUMMARY.
    - Ledger check recorded: 1101 NOT registered (structural ledger stays at 1100).
  </acceptance_criteria>
  <done>Migration 1101 written + applied; headshot rows present for all sourced members (CDN-200); 0 gaps expected (any documented); ledger stays at 1100; results recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external image source (flybouldercity.com) → headshot pipeline | fetched image crosses into Storage + DB; validate it is a real portrait, no overlay, correct person |
| manifest → migration 1101 | manifest cdn_url/license cross into seed SQL; only SUCCESS lines become INSERT blocks |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-06 | Tampering | wrong-person or overlay-laden headshot inserted (or wrong documentId → group photo) | mitigate | Use flybouldercity.com individual-portrait documentIds (not the bcnv.org group photo); reject text/graphic overlays; INLINE visual correct-person spot-check before insert; Plan 03 human-verify reconfirms |
| T-165-07 | Tampering | fabricated/placeholder image for a member whose URL 404s | mitigate | No fabrication rule; fall back to bcnv.org/Ballotpedia, else FAILED manifest line + documented gap instead of a placeholder row |
| T-165-08 | Spoofing | server returns an error body saved as an image | mitigate | download_image validates content-type / decodes via PIL before processing; sizes confirmed > MIN_DIM |
| T-165-09 | Information disclosure | mis-set photo_license | mitigate | photo_license set per member from the actual source (flybouldercity.com official portal = us_government_work; operator revises if a different source is used) |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages (Pillow/psycopg2/requests pre-installed; RESEARCH audit = N/A). No high-severity threats; data-seed phase. |
</threat_model>

<verification>
CLARK-05 SC#2: all 5 seated officials render with 600×750 headshots, genuine gaps documented.
- Pipeline crops 4:5 first then 600×750; mirrored to politician_photos/{uuid}-headshot.jpg; correct-person spot-checked (Task 1).
- politician_images rows present (target 5, 0 gaps expected); CDN urls 200 (Task 2).
- Migration 1101 audit-only — structural ledger stays at 1100.
</verification>

<success_criteria>
- _tmp-boulder-city-council-headshots.py written + run; all 5 flybouldercity.com portraits sourced (no WAF, Chrome UA); 600×750 4:5 crops mirrored to Storage; correct-person spot-check + manifest recorded.
- 1101_boulder_city_council_headshots.sql written + applied (audit-only); politician_images rows for all 5 members; CDN-200; any gap documented (no fabrication).
- Ledger stays at 1100; results + any gaps recorded in 165-02-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/165-boulder-city-deep-seed/165-02-SUMMARY.md` when done (record the per-member manifest, the politician_images count + CDN-200 results, the correct-person spot-check, any documented gaps, photo_license per member, and confirmation the ledger stayed at 1100).
</output>
