---
phase: 176-city-of-beaverton-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["176-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-beaverton-headshots.py
  - C:/EV-Accounts/backend/migrations/1132_beaverton_headshots.sql
autonomous: false
requirements: [WASH-02]

must_haves:
  truths:
    - "Each of the 7 Beaverton officials has a 600x750 headshot in Supabase Storage (genuine gaps documented)"
    - "Each headshot is cropped to 4:5 THEN resized to 600x750 Lanczos q90 — never stretched"
    - "No headshot has superimposed text or graphics over the face"
    - "politician_images rows exist (id, politician_id, url, type, photo_license) with photo_license per actual source"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-beaverton-headshots.py"
      provides: "Download → composite → crop-4:5 → resize-600x750 → upload pipeline for 7 officials"
      contains: "OFFICIALS"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/1132_beaverton_headshots.sql"
      provides: "Audit-only politician_images INSERTs capturing the uploaded headshots"
      contains: "politician_images"
      min_lines: 30
  key_links:
    - from: "_tmp-beaverton-headshots.py"
      to: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      via: "PUT x-upsert upload"
      pattern: "politician_photos"
    - from: "1132_beaverton_headshots.sql"
      to: "essentials.politician_images"
      via: "INSERT ... WHERE NOT EXISTS per official"
      pattern: "politician_images \\(id, politician_id, url, type, photo_license\\)"
---

<objective>
Produce 600x750 headshots for all 7 Beaverton officials by cloning the WashCo headshot pipeline,
and author the audit-only migration `1132_beaverton_headshots.sql` that records the resulting
politician_images rows. Mayor Beaty has a ready Wikimedia CC BY 2.0 source; the other 6 need
CivicPlus/campaign/Ballotpedia sourcing (RESEARCH §Headshot Sources). Genuine gaps are documented,
never fabricated.

Purpose: Satisfy WASH-02 Success Criterion 2 (all officials render with 600x750 headshots).
Output: `_tmp-beaverton-headshots.py` (gitignored) + `1132_beaverton_headshots.sql` (audit-only,
NOT registered in the ledger).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-RESEARCH.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-02-SUMMARY.md
@.claude/skills/find-headshots

<interfaces>
essentials.politician_images (id, politician_id, url, type, photo_license)  -- NO photo_origin_url column
-- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
-- Storage path: politician_photos/{uuid}-headshot.jpg
</interfaces>

**EXECUTION ARCHITECTURE:** Executor WRITES both files. The ORCHESTRATOR runs the python script
(`python C:/EV-Accounts/backend/scripts/_tmp-beaverton-headshots.py` using .env creds), reads the
emitted manifest (uuid per ext_id), fills the {uuid} placeholders in 1132, applies it via
`psql "$DATABASE_URL" -f migrations/1132_beaverton_headshots.sql`, and commits ONLY the .sql in
C:/EV-Accounts (the `_tmp-*` script is gitignored — do NOT commit it).

**VERIFY-GATE HYGIENE:** keep the literal `slug`, `schema_migrations`, `photo_origin_url` strings
out of comments. 1132 is audit-only — it must NOT contain a schema_migrations ledger INSERT.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the WashCo headshot pipeline for Beaverton's 7 officials</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-beaverton-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-washco-headshots.py — entire file (docstring, OFFICIALS list, .env config block, crop_to_4_5, resize_600x750, process_headshot_bytes transparent-composite, upload, manifest print)
    - .planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md §"_tmp-beaverton-headshots.py"
    - .planning/phases/176-city-of-beaverton-deep-seed/176-RESEARCH.md §"Headshot Sources" (per-official source table + license notes) and §"Common Pitfalls" Pitfall 5 (JS-rendered CivicPlus portraits)
    - .claude/skills/find-headshots (skill for locating portrait sources)
  </read_first>
  <action>
    Copy _tmp-washco-headshots.py verbatim and change ONLY: the module docstring (Beaverton, Phase 176,
    7 officials), the OFFICIALS list, the assertion count (==7), and the manifest header strings
    ("BEAVERTON CITY COUNCIL HEADSHOT MANIFEST", "/7 uploaded"). OFFICIALS entries (ext_id / name /
    url / license) using the confirmed ext_id block from 176-02-SUMMARY: -4105351 Lacey Beaty
    url=https://commons.wikimedia.org/wiki/Special:FilePath/Mayor_Lacey_Beaty_crop.jpg license='cc_by_2.0';
    -4105352 Ashley Hartmeier-Prigg; -4105353 Kevin Teater; -4105354 Edward Kimmi; -4105355 Allison
    Tivnon; -4105356 John Dugger; -4105357 Nadia Hasan — the 6 non-Mayor url values start empty and are
    filled at execution from beavertonoregon.gov council-position pages (CivicPlus portraits are
    JS-rendered — flag in a comment that the orchestrator resolves each per-member portrait UUID via
    the find-headshots skill / browser inspection, with campaign-site and Ballotpedia fallbacks per the
    RESEARCH source table), license='press_use' unless the actual source dictates otherwise. Keep the
    crop-4:5-FIRST-then-resize-600x750 order, the transparent→white composite guard, the descriptive
    User-Agent header (Wikimedia 429s on browser UA), the runtime UUID-by-external_id resolution, and
    the PUT x-upsert upload to politician_photos/{uuid}-headshot.jpg. Reject any image with text/graphic
    overlay over the face. Do NOT change the .env-parsing config block. No fenced code in this action —
    the python file itself is the artifact.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `python C:/EV-Accounts/backend/scripts/_tmp-beaverton-headshots.py` and reads the manifest (expect up to 7 SUCCESS lines with uuids)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-beaverton-headshots.py
    - OFFICIALS list has exactly 7 entries with ext_ids -4105351..-4105357 (or Wave-0-shifted block)
    - assert len(OFFICIALS)==7 present; ext_id uniqueness assertion present
    - Mayor Beaty entry uses the Wikimedia FilePath URL + license='cc_by_2.0'
    - Pipeline crops to 4:5 BEFORE resizing to 600x750 Lanczos q90
    - Transparent→white composite guard retained; descriptive User-Agent retained
    - Upload targets politician_photos/{uuid}-headshot.jpg with x-upsert
  </acceptance_criteria>
  <done>Headshot script authored; pipeline order and 7-official roster correct.</done>
</task>

<task type="auto">
  <name>Task 2: Author audit-only migration 1132_beaverton_headshots.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1132_beaverton_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1121_washco_commission_headshots.sql — entire file (header, per-official INSERT pattern, audit-only note)
    - .planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md §"1132_beaverton_headshots.sql"
  </read_first>
  <action>
    Copy 1121 and adapt for Beaverton's 7 officials. Header comment: "Migration 1132: City of Beaverton
    City Council Headshots — AUDIT-ONLY (not registered in the ledger)". One INSERT block per official
    into essentials.politician_images (id, politician_id, url, type, photo_license) — id=gen_random_uuid(),
    politician_id resolved by subquery on external_id, url = the CDN URL with the {uuid} placeholder that
    the orchestrator fills from the script manifest, type='default', photo_license per actual source
    (Mayor Beaty 'cc_by_2.0'; city-portal/campaign 'press_use'), each guarded by WHERE NOT EXISTS on
    politician_id. Omit any official whose script run reported a genuine GAP (document the gap in the
    plan SUMMARY instead). Do NOT include a schema_migrations ledger INSERT (audit-only). Do NOT include
    a photo_origin_url column. No fenced code in this action.
  </action>
  <verify>
    <automated>MISSING — orchestrator fills {uuid}s, applies `psql "$DATABASE_URL" -f migrations/1132_beaverton_headshots.sql`, then counts politician_images for the 7 ext_ids</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/1132_beaverton_headshots.sql
    - One INSERT block per successfully-uploaded official (up to 7)
    - Columns exactly (id, politician_id, url, type, photo_license) — no photo_origin_url
    - Each block guarded by WHERE NOT EXISTS on politician_id
    - Mayor Beaty photo_license='cc_by_2.0'; type='default' on all
    - NO schema_migrations ledger INSERT (audit-only)
    - Literal strings `slug`, `schema_migrations`, `photo_origin_url` absent from comments
  </acceptance_criteria>
  <done>Audit-only headshot migration authored, ready for the orchestrator to fill uuids and apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs pipeline, applies 1132, verifies 600x750 + no overlays</name>
  <what-built>The Beaverton headshot pipeline script + audit-only politician_images migration.</what-built>
  <how-to-verify>
    1. Run `python C:/EV-Accounts/backend/scripts/_tmp-beaverton-headshots.py`; read the manifest — record uuid per ext_id and any GAP.
    2. Fill the {uuid} placeholders in 1132, then `psql "$DATABASE_URL" -f migrations/1132_beaverton_headshots.sql`.
    3. Audit: count politician_images rows for the 7 Beaverton ext_ids (expect 7 minus documented gaps).
    4. HTTP 200 spot-check a sample CDN URL; confirm images are 600x750, correctly cropped (eyes ~1/3 from top), and have NO text/graphic overlay over the face (feedback_headshot_no_graphics).
    5. Commit ONLY the .sql in C:/EV-Accounts (the _tmp-* script stays gitignored).
  </how-to-verify>
  <acceptance_criteria>
    - politician_images row count = 7 minus any documented genuine gaps
    - Sampled CDN URLs return HTTP 200; images are 600x750, no overlays
    - Any gap documented in the SUMMARY (no fabricated images)
    - 1132 committed in C:/EV-Accounts; _tmp-* script NOT committed
  </acceptance_criteria>
  <resume-signal>Type "approved" with the headshot count and any documented gaps, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image sources → Storage | Portrait images fetched from Wikimedia/city/campaign sites, processed, uploaded to Supabase Storage |
| operator → live DB | Audit-only politician_images INSERTs applied by the orchestrator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-05 | Spoofing | Wrong-person or overlay-defaced headshot | mitigate | Per-official source verification against RESEARCH source table + human-verify checkpoint (identity + no overlay per feedback_headshot_no_graphics) |
| T-176-06 | Tampering | Duplicate image rows on re-run | mitigate | WHERE NOT EXISTS guard on politician_id per row |
| T-176-07 | Information Disclosure | Storage bucket exposure | accept | Bucket is public read by design (public official headshots); service key only in gitignored .env |
| T-176-SC | Tampering | pip installs (Pillow/psycopg2/requests) | mitigate | Pipeline reuses the already-vetted WashCo script dependencies — no NEW packages introduced; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- politician_images count = 7 minus documented gaps; sampled CDN URLs HTTP 200 at 600x750 with no overlays.
- Genuine gaps documented, not fabricated.
</verification>

<success_criteria>
- All 7 officials (minus documented genuine gaps) have 600x750 press_use/cc_by_2.0 headshots mirrored
  to Storage and recorded via audit-only migration 1132.
</success_criteria>

<output>
Create `.planning/phases/176-city-of-beaverton-deep-seed/176-03-SUMMARY.md` when done, recording
uuid per ext_id, photo_license per official, and any documented headshot gaps.
</output>
