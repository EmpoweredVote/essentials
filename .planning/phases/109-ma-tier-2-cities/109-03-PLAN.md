---
phase: 109-ma-tier-2-cities
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/353_lowell_government.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-353.ts
autonomous: true
requirements: [MA-TIER2-02]
must_haves:
  truths:
    - "A Lowell address returns a LOCAL section listing City Manager Golden, Mayor Gitschier, and 10 city councillors"
    - "Lowell has NO LOCAL_EXEC district — all 12 officials are in a single LOCAL district (council-manager / Plan E model)"
    - "City Manager Golden and Mayor Gitschier are is_appointed=true; the 10 councillors are is_appointed=false"
    - "All 12 Lowell officials have a non-NULL office_id"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/353_lowell_government.sql"
      provides: "Lowell government + chamber + 1 LOCAL district + 12 politicians + 12 offices (council-manager model, per MA-TIER2-02)"
      contains: "City of Lowell, Massachusetts, US"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-353.ts"
      provides: "Apply + smoke-test harness for migration 353"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts (geo_id=2537000, LOCAL only)"
      via: "district_id FK"
      pattern: "geo_id = '2537000'"
    - from: "essentials.politicians (office_id)"
      to: "essentials.offices"
      via: "office_id back-fill"
      pattern: "office_id IS NULL"
---

<objective>
Seed the City of Lowell government using the Plan E (council-manager) model so any Lowell address returns a populated LOCAL section with City Manager Thomas A. Golden Jr., council-elected Mayor Erik R. Gitschier, and the 10 directly-elected councillors (2 at-large + 8 district). Implements the Lowell portion of MA-TIER2-02.

Purpose: Lowell uses Plan E government — the Mayor is elected by the council (ceremonial), not by voters, and the City Manager is appointed. This mirrors Cambridge (Siddiqui + Huang, both is_appointed=true). Lowell's G4110 geofence (geo_id='2537000') is already loaded from v5.0.
Output: Migration `353_lowell_government.sql` (council-manager variant — NO LOCAL_EXEC district) + apply harness `_apply-migration-353.ts`, applied to production via Supabase MCP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-ma-tier-2-cities/109-RESEARCH.md
@.planning/phases/109-ma-tier-2-cities/109-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 353 — Lowell government (council-manager, no LOCAL_EXEC)</name>
  <files>C:/EV-Accounts/backend/migrations/353_lowell_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/347_boston_government.sql (structural analog: 7-step pattern)
    - .planning/phases/109-ma-tier-2-cities/109-PATTERNS.md (lines 304-377: Lowell council-manager differences + external_id table with is_appointed flags)
    - .planning/phases/109-ma-tier-2-cities/109-RESEARCH.md (lines 116-139: Lowell roster + DB model notes; lines 356-377: Pattern 2; lines 503-507: Pitfall 2 — no LOCAL_EXEC)
  </read_first>
  <action>
    Create migration 353 following the migration 351 structure BUT with two critical council-manager differences (Pitfall 2 + Pattern 2):
      1. NO LOCAL_EXEC district — Lowell has no popularly-elected Mayor. Insert ONLY a single LOCAL district. (Cambridge precedent.)
      2. City Manager and Mayor are is_appointed=true; all 10 councillors are is_appointed=false.

    Concrete values:
      - Government: name='City of Lowell, Massachusetts, US', type='LOCAL', state='MA', city='Lowell', geo_id='2537000'
      - Chamber: name='City Council', name_formal='Lowell City Council', NO slug
      - Districts: ONE row only — LOCAL: 'ma', '2537000', label='Lowell', mtfcc=NULL. Do NOT insert a LOCAL_EXEC district.

    12 politician+office WITH ins_p blocks (party=NULL, is_active=true, is_vacant=false, is_incumbent=true; ON CONFLICT (external_id) DO NOTHING; office NOT EXISTS guard on (district_id, politician_id)). ALL offices link to the single LOCAL district (geo_id='2537000'):
      - Thomas A. Golden, Jr., -253700001, is_appointed=true, title='City Manager', is_appointed_position=true
      - Erik R. Gitschier, -253700002, is_appointed=true, title='Mayor', is_appointed_position=true (council-elected; spelling 'Gitschier' per official site — A1)
      - Rita Mercier, -253700003, is_appointed=false, title='City Councilor', is_appointed_position=false
      - Vesna Nuon, -253700004, is_appointed=false, title='City Councilor'
      - Daniel Rourke, -253700005, is_appointed=false, title='City Councilor (District 1)'
      - Corey Robinson, -253700006, is_appointed=false, title='City Councilor (District 2)'
      - Belinda M. Juran, -253700007, is_appointed=false, title='City Councilor (District 3)'
      - Sean McDonough, -253700008, is_appointed=false, title='City Councilor (District 4)'
      - Kimberly Scott, -253700009, is_appointed=false, title='City Councilor (District 5)'
      - Sokhary Chau, -253700010, is_appointed=false, title='City Councilor (District 6)'
      - Sidney L. Liang, -253700011, is_appointed=false, title='City Councilor (District 7)'
      - John Descoteaux, -253700012, is_appointed=false, title='City Councilor (District 8)'
      For 'Thomas A. Golden, Jr.', set first_name='Thomas', last_name='Golden'.

    Header CRITICAL rules identical to migration 351, PLUS a note: "Lowell is Plan E (council-manager) — NO LOCAL_EXEC district; City Manager + Mayor are is_appointed=true (Cambridge precedent)."
    Step 6 office_id back-fill: external_id BETWEEN -253700012 AND -253700001.
    Step 7 post-verification gates: 1 gov, 1 chamber, **1** district (LOCAL only for geo_id='2537000'), 12 politicians, 12 offices, 0 section-split orphans for geo_id='2537000', 0 NULL office_ids.
    Step 8 ledger: version '353'.
    Use exact string '2537000'. No per-district geofences. Do NOT create a LOCAL_EXEC district.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/migrations/353_lowell_government.sql','utf8'); let miss=[]; for(let i=1;i<=12;i++){const id='-2537000'+String(i).padStart(2,'0'); if(!s.includes(id))miss.push(id)} if(miss.length){console.error('MISSING:',miss);process.exit(1)} if(s.includes('LOCAL_EXEC')){console.error('LOCAL_EXEC must NOT appear for Lowell');process.exit(1)} if(!s.includes('City Manager')){console.error('missing City Manager');process.exit(1)} if(!s.includes(\"VALUES ('353')\")){process.exit(1)} console.log('OK 12 ids, no LOCAL_EXEC, City Manager, ledger')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/353_lowell_government.sql
    - Contains exact string 'City of Lowell, Massachusetts, US'
    - All 12 external_ids -253700001 through -253700012 appear
    - String 'LOCAL_EXEC' does NOT appear anywhere (council-manager city — only LOCAL district)
    - Golden (-253700001) office title='City Manager', is_appointed_position=true
    - Gitschier (-253700002) office title='Mayor', is_appointed=true (council-elected)
    - All 10 councillor offices use is_appointed_position=false
    - Post-verification district gate expects 1 (not 2)
    - Ledger INSERT version '353' present
  </acceptance_criteria>
  <done>Migration 353 SQL written with council-manager model: 1 LOCAL district, City Manager + council-elected Mayor (is_appointed=true), 10 elected councillors, gates expecting 1 district, ledger entry.</done>
</task>

<task type="auto">
  <name>Task 2: Write apply harness _apply-migration-353.ts</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-353.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-347.ts (apply-harness analog)
    - .planning/phases/109-ma-tier-2-cities/109-PATTERNS.md (lines 611-690: apply-script pattern; lines 681-690: Lowell district smoke test expects 1)
  </read_first>
  <action>
    Create _apply-migration-353.ts by adapting _apply-migration-347.ts. Read+execute migrations/353_lowell_government.sql via pg Pool (DATABASE_URL, ssl rejectUnauthorized:false). Smoke tests:
      1. government rows name='City of Lowell, Massachusetts, US' → expect 1
      2. politicians external_id BETWEEN -253700012 AND -253700001 → expect 12
      3. districts state='ma' AND geo_id='2537000' AND district_type='LOCAL' → expect 1 (NO LOCAL_EXEC for council-manager city — query district_type='LOCAL' only)
      4. politicians in range with office_id IS NULL → expect 0
      5. ledger version='353' → PRESENT/MISSING
    Error → exit(1); finally pool.end(). Fallback harness; authoritative apply is Supabase MCP (Task 3).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/scripts/_apply-migration-353.ts','utf8'); if(!s.includes('353_lowell_government.sql')||!s.includes('-253700012')||!s.includes('2537000')){process.exit(1)} console.log('OK apply-353')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_apply-migration-353.ts
    - References migrations/353_lowell_government.sql
    - District smoke test queries district_type='LOCAL' only and expects 1
    - Politician smoke test expects 12; NULL office_id check expects 0
    - Reads DATABASE_URL from process.env (no hardcoded credentials)
  </acceptance_criteria>
  <done>Apply harness written for migration 353 with Lowell-specific smoke tests (1 district, 12 politicians).</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply migration 353 to production via Supabase MCP</name>
  <files>C:/EV-Accounts/backend/migrations/353_lowell_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/353_lowell_government.sql (read full file before executing)
  </read_first>
  <action>
    Apply migration 353 to production using `mcp__supabase-local__execute_sql` (NOT the .ts harness, NOT supabase db push). Pass the full SQL file contents. The Step 7 post-verification DO block raises an exception on any gate failure (note: district gate expects 1, not 2).

    After applying, run the scoped MA section-split query via execute_sql:
      SELECT COUNT(*) AS orphan_count FROM essentials.geofence_boundaries g WHERE g.state='25' AND g.mtfcc='G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=g.geo_id);
    The invariant: Lowell's geo_id 2537000 is no longer in the orphan set (decremented by 1 from the pre-353 count).

    Also confirm the council-manager model applied correctly:
      SELECT COUNT(*) FROM essentials.districts WHERE state='ma' AND geo_id='2537000' AND district_type='LOCAL_EXEC';  -- expect 0

    feedback_supabase_local_is_remote: this is the live production DB.
  </action>
  <verify>
    <automated>MISSING — verified via Supabase MCP execute_sql at runtime: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -253700012 AND -253700001 returns 12; SELECT COUNT(*) FROM essentials.districts WHERE state='ma' AND geo_id='2537000' AND district_type='LOCAL_EXEC' returns 0</automated>
  </verify>
  <acceptance_criteria>
    - mcp__supabase-local__execute_sql applied migration 353 without exception (Step 7 gates passed, district gate=1)
    - SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -253700012 AND -253700001 returns 12
    - SELECT COUNT(*) FROM essentials.districts WHERE state='ma' AND geo_id='2537000' AND district_type='LOCAL_EXEC' returns 0 (no LOCAL_EXEC for Lowell)
    - 0 of the 12 politicians have NULL office_id
    - geo_id 2537000 is not present in the MA G4110 section-split orphan set
    - Ledger row version='353' present
  </acceptance_criteria>
  <done>Migration 353 applied to production; council-manager model confirmed (0 LOCAL_EXEC districts); all gates passed; Lowell geo_id no longer a section-split orphan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | SQL executed via Supabase MCP against live production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-07 | Tampering | migration 353 re-run | mitigate | WHERE NOT EXISTS on government; ON CONFLICT (external_id) DO NOTHING; NOT EXISTS guard on (district_id, politician_id) — idempotent |
| T-109-08 | Tampering | accidental LOCAL_EXEC district for council-manager city | mitigate | Action forbids LOCAL_EXEC; Task 1 verify greps for absence; Task 3 confirms 0 LOCAL_EXEC rows post-apply |
| T-109-09 | Information Disclosure | seeded politician records | accept | Public elected/appointed officials from official city websites; no non-public PII |
</threat_model>

<verification>
- Migration 353 applied without exception (district gate expects 1)
- 12 Lowell officials present (external_id -253700012..-253700001), 0 NULL office_id
- 0 LOCAL_EXEC districts for geo_id 2537000 (council-manager model intact)
- Lowell geo_id 2537000 no longer a section-split orphan
</verification>

<success_criteria>
A Lowell address routes (via geo_id='2537000') to a LOCAL section listing City Manager Golden, Mayor Gitschier, and 10 councillors, all with non-NULL office_ids. The council-manager model is represented correctly (no LOCAL_EXEC district). Lowell portion of MA-TIER2-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/109-ma-tier-2-cities/109-03-SUMMARY.md` when done
</output>
