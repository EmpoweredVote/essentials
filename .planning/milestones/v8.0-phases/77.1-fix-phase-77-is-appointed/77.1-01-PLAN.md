---
phase: 77.1-fix-phase-77-is-appointed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql
autonomous: true
requirements: []
tags:
  - postgres
  - migration
  - essentials.politicians
  - portland
  - data-fix

must_haves:
  truths:
    - "essentials.politicians row for Raymond C. Lee III (external_id=-690003) has is_appointed=true"
    - "essentials.politicians row for Robert L. Taylor (external_id=-690004) has is_appointed=true"
    - "supabase_migrations.schema_migrations contains a row with version='235'"
    - "essentials.offices rows for both politicians remain unchanged (is_appointed_position=true preserved)"
    - "Re-applying the migration is a no-op (UPDATE affects 0 rows the second time)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql"
      provides: "Surgical UPDATE of is_appointed for Lee III and Taylor + ledger entry"
      contains: "UPDATE essentials.politicians"
  key_links:
    - from: "C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql"
      to: "essentials.politicians.is_appointed"
      via: "WHERE external_id IN (-690003, -690004)"
      pattern: "external_id IN \\(-690003, -690004\\)"
    - from: "C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql"
      to: "supabase_migrations.schema_migrations"
      via: "INSERT ... VALUES ('235') ON CONFLICT DO NOTHING"
      pattern: "schema_migrations.*VALUES \\('235'\\)"
---

<objective>
Fix Phase 77 data omission: set `essentials.politicians.is_appointed = true` for Raymond C. Lee III (City Administrator, external_id=-690003) and Robert L. Taylor (City Attorney, external_id=-690004). Both were inserted by migration 231 with `is_appointed=false`, even though their offices rows correctly have `is_appointed_position=true`.

Purpose: Restore parity between `politicians.is_appointed` and `offices.is_appointed_position` for Portland's appointed officials. The backend reads `p.is_appointed` directly in `essentialsService.ts` and `essentialsBrowseService.ts` and surfaces it in API responses; the current value of `false` is incorrect for these two appointed officials.

Output: One applied migration (235), two politicians rows updated, one ledger entry, idempotency confirmed via second UPDATE returning 0 rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/77.1-fix-phase-77-is-appointed/77.1-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write migration 235 SQL file</name>
  <files>C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/223a_or_executive_district_fix.sql (reference pattern — minimal BEGIN/UPDATE/INSERT-ledger/COMMIT)
    - C:/EV-Accounts/backend/migrations/231_portland_officials.sql (confirm the politicians rows for external_id=-690003 and -690004 exist with is_appointed=false; confirm the offices rows for these politicians have is_appointed_position=true so we do NOT touch offices)
    - C:/EV-Accounts/backend/migrations/ (directory listing — confirm 234_ca_state_senate_stances.sql is the last applied migration before writing 235; if a 235 file already exists, STOP and surface the conflict — do not overwrite)
  </read_first>
  <behavior>
    - The migration file exists at the exact path C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql
    - File contents are valid PostgreSQL wrapped in BEGIN; ... COMMIT;
    - UPDATE statement scopes to essentials.politicians (NOT essentials.offices)
    - UPDATE WHERE clause matches by external_id IN (-690003, -690004) — never by UUID
    - SET clause sets is_appointed = true (only — no other columns)
    - Ledger INSERT writes version '235' to supabase_migrations.schema_migrations with ON CONFLICT (version) DO NOTHING
    - File contains a leading SQL comment describing the fix (what + why + reference to Phase 77 omission)
    - No DROP, ALTER, or DDL statements anywhere in the file
  </behavior>
  <action>
    Write a new file at C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql containing exactly one transaction that performs a surgical data fix.

    The file must contain, in order:

    1. A header comment block (SQL line comments using `--`) explaining:
       - Migration 235 purpose: fix Phase 77 omission where politicians.is_appointed was left false for two appointed Portland officials
       - Target rows: external_id=-690003 (Raymond C. Lee III, City Administrator) and external_id=-690004 (Robert L. Taylor, City Attorney)
       - Cross-reference: migration 231 created both rows with is_appointed=false; offices.is_appointed_position is already true and is NOT modified by this migration
       - Idempotency: re-running is safe (UPDATE on already-true rows is a no-op; ledger INSERT uses ON CONFLICT DO NOTHING)

    2. `BEGIN;`

    3. The UPDATE statement:
       - Target table: `essentials.politicians`
       - SET clause: `is_appointed = true`
       - WHERE clause: `external_id IN (-690003, -690004)`
       - No JOIN, no subquery, no other predicates

    4. The ledger INSERT statement:
       - Target table: `supabase_migrations.schema_migrations`
       - Columns: `(version)`
       - VALUES: `('235')` — string literal, not integer
       - Conflict clause: `ON CONFLICT (version) DO NOTHING`

    5. `COMMIT;`

    Use the format established in C:/EV-Accounts/backend/migrations/223a_or_executive_district_fix.sql. Do NOT add any other UPDATE, INSERT, or DDL statements. Do NOT modify essentials.offices in this migration. Do NOT include explanatory RAISE NOTICE or DO blocks unless they exactly mirror the 223a pattern. Use canonical migration ledger format per D-99 of project conventions: column is `version` (text), table is `supabase_migrations.schema_migrations`.

    Before writing: list the migrations directory and confirm 234_ca_state_senate_stances.sql is the highest numbered file and that no file named 235_* already exists. If a 235_* file already exists, STOP and report the conflict — do not overwrite.
  </action>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql exists.
    - File contains the exact string `BEGIN;` on its own line.
    - File contains the exact string `COMMIT;` on its own line.
    - File contains the exact string `UPDATE essentials.politicians` (case-sensitive).
    - File contains the exact string `SET is_appointed = true` (case-sensitive).
    - File contains the exact substring `external_id IN (-690003, -690004)` (case-sensitive).
    - File contains the exact substring `INSERT INTO supabase_migrations.schema_migrations` (case-sensitive).
    - File contains the exact substring `VALUES ('235')` (case-sensitive).
    - File contains the exact substring `ON CONFLICT (version) DO NOTHING` (case-sensitive).
    - File does NOT contain the substring `essentials.offices` anywhere (the migration must not touch offices).
    - File does NOT contain the substrings `DROP`, `ALTER TABLE`, or `CREATE TABLE` (no DDL).
    - File does NOT contain the substring `is_appointed_position` (this column belongs to offices and must not appear).
    - Header comment references both external_ids (-690003 and -690004) and both full names (Raymond C. Lee III, Robert L. Taylor).
    - `ls C:/EV-Accounts/backend/migrations/235_*.sql` returns exactly one file (no conflicting 235_* file present).
  </acceptance_criteria>
  <verify>
    <automated>grep -E "UPDATE essentials.politicians|SET is_appointed = true|external_id IN \(-690003, -690004\)|VALUES \('235'\)|ON CONFLICT \(version\) DO NOTHING|BEGIN;|COMMIT;" "C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql" | wc -l | awk '{ if ($1 < 7) { print "FAIL: missing required strings"; exit 1 } else { print "PASS" } }' && ! grep -E "essentials\.offices|is_appointed_position|DROP |ALTER TABLE|CREATE TABLE" "C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql" && echo "PASS: no forbidden strings"</automated>
  </verify>
  <done>
    Migration file exists at the canonical path, contains exactly one transaction with one UPDATE (essentials.politicians) and one ledger INSERT (version '235'), and contains no references to essentials.offices or is_appointed_position. Idempotent by construction (both UPDATE and INSERT are safe to re-run).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply migration 235 to Supabase and verify both gates</name>
  <files>C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql (the file created in Task 1; confirm its contents before applying)
    - .planning/phases/77.1-fix-phase-77-is-appointed/77.1-RESEARCH.md (Validation Architecture section: the two SQL gates to run after applying)
  </read_first>
  <behavior>
    - Before applying: SELECT confirms both target rows currently have is_appointed=false
    - Migration is applied to the live Supabase database via mcp__supabase-local
    - After applying: Gate 1 SELECT returns 2 rows, both with is_appointed=true
    - After applying: Gate 2 SELECT returns 1 row from schema_migrations with version='235'
    - Idempotency check: re-running the migration body (UPDATE statement only) returns 0 rows affected
    - essentials.offices rows for both politicians remain unchanged (is_appointed_position=true preserved; spot-check)
  </behavior>
  <action>
    Apply migration 235 to the live Supabase database using the mcp__supabase-local MCP tool (NOTE: per project memory feedback_supabase_local_is_remote.md, mcp__supabase-local IS the remote production DB — treat this as a live write).

    Sequence:

    1. PRE-FLIGHT — run this SELECT first and confirm both rows currently have is_appointed=false:
       ```
       SELECT external_id, full_name, is_appointed
       FROM essentials.politicians
       WHERE external_id IN (-690003, -690004)
       ORDER BY external_id DESC;
       ```
       Expected: 2 rows. -690003 = "Raymond C. Lee III", is_appointed=false. -690004 = "Robert L. Taylor", is_appointed=false. If either row is missing OR if either already has is_appointed=true, STOP and report — do not apply.

    2. APPLY — execute the contents of C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql against the database. The BEGIN/COMMIT wrapper means either the whole transaction succeeds or rolls back.

    3. GATE 1 — re-run the same SELECT from step 1 and confirm both rows now have is_appointed=true:
       ```
       SELECT external_id, full_name, is_appointed
       FROM essentials.politicians
       WHERE external_id IN (-690003, -690004)
       ORDER BY external_id DESC;
       ```
       Expected: 2 rows, both is_appointed=true. If 0, 1, or 3+ rows, or if any is_appointed=false, FAIL and report.

    4. GATE 2 — confirm ledger entry present:
       ```
       SELECT version FROM supabase_migrations.schema_migrations WHERE version = '235';
       ```
       Expected: exactly 1 row with version='235'.

    5. OFFICES SPOT-CHECK — confirm offices were NOT modified (defensive check):
       ```
       SELECT o.is_appointed_position, p.external_id, p.full_name
       FROM essentials.offices o
       JOIN essentials.politicians p ON p.id = o.politician_id
       WHERE p.external_id IN (-690003, -690004);
       ```
       Expected: 2 rows, both is_appointed_position=true (unchanged from Phase 77).

    6. IDEMPOTENCY CHECK — re-run the UPDATE statement alone (not the full migration; the ledger INSERT would conflict harmlessly anyway):
       ```
       UPDATE essentials.politicians
       SET is_appointed = true
       WHERE external_id IN (-690003, -690004) AND is_appointed = false;
       ```
       Expected: 0 rows affected (because both are already true after step 2).

    Do NOT modify any other politicians rows, any offices rows, or any other tables. If any gate fails, STOP and report the exact query, expected result, and actual result — do not attempt remediation without surfacing the failure.
  </action>
  <acceptance_criteria>
    - Pre-flight SELECT returned 2 rows, both is_appointed=false (recorded in task output).
    - After APPLY, Gate 1 SELECT returns exactly 2 rows: (-690003, "Raymond C. Lee III", true) and (-690004, "Robert L. Taylor", true).
    - Gate 2 SELECT returns exactly 1 row: version='235'.
    - Offices spot-check returns 2 rows, both is_appointed_position=true (unchanged).
    - Idempotency UPDATE returns 0 rows affected.
    - No other rows in essentials.politicians have is_appointed changed (verifiable by a follow-up sanity SELECT: `SELECT COUNT(*) FROM essentials.politicians WHERE is_appointed = true AND external_id NOT IN (-690003, -690004)` returns the same count as before the migration — record the pre-flight count for comparison if you want extra confidence, but the targeted WHERE clause already guarantees this).
    - All four SELECT results and the IDEMPOTENCY UPDATE result are captured in the task output verbatim (paste raw rows / "0 rows affected").
  </acceptance_criteria>
  <verify>
    <automated>echo "Manual verification via mcp__supabase-local: Gate 1 SELECT returns 2 rows with is_appointed=true; Gate 2 returns 1 row with version='235'; idempotency UPDATE returns 0 rows. All query outputs must be captured in the task output."</automated>
  </verify>
  <done>
    Migration 235 is applied to the live Supabase database. essentials.politicians rows for external_id=-690003 (Raymond C. Lee III) and external_id=-690004 (Robert L. Taylor) both have is_appointed=true. supabase_migrations.schema_migrations contains version='235'. essentials.offices rows for both politicians are unchanged (is_appointed_position=true preserved). The migration is idempotent (re-running affects 0 rows). All four verification queries' outputs are captured in the task summary.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, the overall phase passes when all five truths in `must_haves.truths` are demonstrably true via the captured query output from Task 2:

1. Run `SELECT external_id, full_name, is_appointed FROM essentials.politicians WHERE external_id IN (-690003, -690004) ORDER BY external_id DESC;` — must return 2 rows, both `is_appointed=true`, names matching Raymond C. Lee III and Robert L. Taylor.
2. Run `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '235';` — must return 1 row.
3. Run `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id IN (-690003, -690004) AND o.is_appointed_position = true;` — must return 2 (offices still flagged appointed; we did not touch them).
4. Re-run the UPDATE from migration 235 directly — must report 0 rows affected (idempotency).
5. The migration file exists at the canonical path with the required structural strings (covered by Task 1 acceptance criteria).
</verification>

<success_criteria>
Phase 77.1 is complete when:

- [ ] Migration file `C:/EV-Accounts/backend/migrations/235_fix_portland_officials_is_appointed.sql` exists with valid BEGIN/UPDATE/INSERT-ledger/COMMIT structure.
- [ ] Migration 235 has been applied to the live Supabase database (essentials.politicians rows updated; ledger entry inserted).
- [ ] `essentials.politicians.is_appointed = true` for external_id=-690003 (Raymond C. Lee III).
- [ ] `essentials.politicians.is_appointed = true` for external_id=-690004 (Robert L. Taylor).
- [ ] `supabase_migrations.schema_migrations` contains a row with version='235'.
- [ ] `essentials.offices.is_appointed_position` for both officials remains `true` (unchanged from Phase 77).
- [ ] Idempotency confirmed: re-running the UPDATE returns 0 rows affected.
- [ ] All verification query outputs are captured in the Task 2 output and the eventual SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/77.1-fix-phase-77-is-appointed/77.1-01-SUMMARY.md` when done. The SUMMARY must include:

- Migration filename and full path applied
- Pre-flight query output (showing is_appointed=false before)
- Gate 1 output (showing is_appointed=true after)
- Gate 2 output (showing ledger row version='235')
- Offices spot-check output (showing is_appointed_position=true preserved)
- Idempotency UPDATE result (0 rows affected)
- Next migration number for downstream phases (236)
</output>
