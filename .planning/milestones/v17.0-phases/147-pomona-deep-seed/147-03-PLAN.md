---
phase: 147-pomona-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["147-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql
autonomous: false
requirements: [POMO-01]

must_haves:
  truths:
    - "the exact image gap/quality set is determined by a pre-flight image-coverage + dimension query over the 7 current members, NOT hardcoded; the 3 missing (Garcia, Lustro, Sandoval), the 1 new (Ontiveros-Cole), and the quality-suspect existing rows (Canales 469×469 may be Torres; Preciado 150×150 too small) are all surfaced from live data"
    - "pomonaca.gov is FULLY WAF-403 — NO direct city-site curl is attempted (Pitfall 7); confirmed alt sources are used: Pomona Choice Energy 2020 CivicPlus photos (HTTP 200) for Sandoval/Garcia/Lustro/Ontiveros-Cole; the existing good-quality DB photo (1280×1700) for Martin; campaign/Ballotpedia for Canales; the /find-headshots skill may be used to discover/verify alternative sources"
    - "the stale PCE-2025 photos are REJECTED for Martin AND Canales (alt text shows Rubio Gonzalez / Robert Torres — wrong people; any URL containing /2025/02/638723568 is forbidden, Pitfall 5)"
    - "every uploaded portrait is the correct verified person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 FIRST (never stretched) then resized 600×750 Lanczos q90 JPEG, uploaded to Supabase Storage politician_photos at the canonical path {uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), x-upsert:true; politician_images type='default', photo_origin_url set"
    - "no fabricated photos — where no acceptable verified portrait is obtainable (e.g. Preciado if PCE 2020 unusable, or Canales if no verified non-Torres photo found), an honest gap is documented rather than uploading a bad/wrong-person/overlaid image as type='default'"
    - "migration 928 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 927; on-disk counter authoritative)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql"
      provides: "Audit-only headshot INSERT/replace (Sandoval/Garcia/Lustro/Ontiveros-Cole + Martin reprocess + Preciado/Canales if obtainable) + photo_origin_url backfill + full-roster image-coverage audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos ({uuid}-headshot.jpg objects)"
      to: "essentials.politician_images.url (type='default', press_use)"
      via: "INSERT politician_images scoped by politician_id, guarded NOT EXISTS"
      pattern: "'default'"
---

<objective>
Give the current 7-member Pomona roster clean, correct official headshots and confirm single canonical-image
coverage. After Plan 02 the survivor chamber ddabfccc holds Sandoval (Mayor) + Martin D1 + Preciado D2 +
Garcia D3 + Ontiveros-Cole D4 + Lustro D5 + Canales D6, all bidirectionally linked. This is the HARDEST
headshot environment of the LA-County Wave-2 cities so far. The EXACT gap/quality set is resolved by a
pre-flight image-coverage + dimension query, not assumed.

⚠️ pomonaca.gov is FULLY WAF-403 — HTTP 403 on ALL paths (member pages, ShowPublishedDocument,
ShowPublishedImage, ImageRepository). Do NOT copy the Palmdale non-WAF curl assumption; do NOT attempt any
direct curl to www.pomonaca.gov (Pitfall 7). Go directly to the confirmed alternative sources. You MAY use
the /find-headshots skill (~/.claude/commands/find-headshots.md) to discover and verify alternative sources
for any member.

Per RESEARCH §6 the state and source plan is:
- Sandoval (-200916), Garcia (-201350), Lustro (-201352), Ontiveros-Cole (-700658): MISSING/new — source
  from Pomona Choice Energy 2020 CivicPlus photos (172×230, HTTP 200, correct alt text confirmed) at
  https://pomonachoiceenergy.org/wp-content/uploads/2020/06/{filename}.jpg. For Sandoval try
  timsandoval.com/about first; for Garcia try noraforpomona.com/about first; PCE 2020 is the reliable
  fallback for all four. Ballotpedia/LinkedIn are further fallbacks.
- Martin (675752): existing DB photo is GOOD (1280×1700 JPEG, press_use) — REPROCESS it (crop 4:5 →
  600×750) to the canonical path; VERIFY it is Martin (A1), not a prior D1 occupant.
- Preciado (675753): existing DB photo is 150×150 (too small to upscale). Try the PCE 2020 photo (172×230,
  alt='Victor Preciado' confirmed) — better than 150×150. If no acceptable source, document an honest gap.
- Canales (675765): existing DB photo is 469×469 and MAY BE Robert Torres (the PCE HTML alt for the
  'Canales' slot reads 'Robert S. Torres'; A2). VERIFY identity. If it is Torres or unverifiable, source a
  verified non-Torres photo (campaign site / Ballotpedia / news) — and do NOT use the PCE 2025-upload photo.

⚠️ REJECT THE STALE PCE-2025 PHOTOS (Pitfall 5). The Pomona Choice Energy "2025 new member" images for
Martin and Canales show the WRONG people (alt text 'Rubio R. Gonzalez' for Martin, 'Robert S. Torres' for
Canales). Any photo URL containing /2025/02/638723568 (e.g. /2025/02/638723568891930000.png or
.../638723568911570000) is FORBIDDEN. Use Martin's existing good DB photo; find a verified photo for Canales.

Processing (NON-NEGOTIABLE per project memory): for each member — visually confirm the CORRECT person →
crop to 4:5 FIRST (never stretch — crop only, eyes ~1/3 from top, full head+shoulders) → resize 600×750
Lanczos q90 JPEG (Python Pillow) → upload to the Supabase Storage politician_photos bucket at the canonical
path {uuid}-headshot.jpg using SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env) with header
x-upsert:true → INSERT politician_images (type='default', photo_license='press_use'); set photo_origin_url
to the source page. Reject any source with superimposed text/graphics over the face — document a gap honestly
rather than upload a bad photo. Resolve each member's UUID by external_id at apply time.

Purpose: A voter sees the right face next to each official. Wrong-person (Torres-as-Canales,
Gonzalez-as-Martin), text-overlaid, or stretched photos destroy trust. Output: Migration 928 (audit-only —
NOT registered in schema_migrations) + verified portraits for every member with an obtainable correct photo
+ a human checkpoint confirming the correct person + correct crop for each; any genuine gap documented
honestly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/147-pomona-deep-seed/147-RESEARCH.md
@.planning/phases/147-pomona-deep-seed/147-VALIDATION.md
@.planning/phases/147-pomona-deep-seed/147-02-SUMMARY.md
@.planning/phases/146-palmdale-deep-seed/146-03-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §6). pomonaca.gov IS WAF-403 — alt sources only.
     Pre-flight resolves the exact gap/quality set; /find-headshots skill available. -->

Current 7 members (resolve UUID by external_id; pre-flight a coverage + dimension query):
  -200916 Sandoval (Mayor) · 675752 Martin (D1) · 675753 Preciado (D2) · -201350 Garcia (D3) ·
  -700658 Ontiveros-Cole (D4, NEW) · -201352 Lustro (D5) · 675765 Canales (D6)

Source plan (RESEARCH §6 — pomonaca.gov WAF-403, alt sources only; /find-headshots may assist):
  Sandoval (-200916): timsandoval.com/about → fallback PCE 2020 → Ballotpedia
  Garcia (-201350): noraforpomona.com/about → fallback PCE 2020 (172×230)
  Lustro (-201352): PCE 2020 (172×230) → Ballotpedia/LinkedIn
  Ontiveros-Cole (-700658): PCE 2020 (172×230, alt='Elizabeth Ontiveros-Cole' confirmed) → Ballotpedia
  Martin (675752): REPROCESS existing DB 1280×1700 JPEG (good, press_use) → crop 4:5 → 600×750; VERIFY identity (A1)
  Preciado (675753): existing 150×150 too small → PCE 2020 (172×230, alt='Victor Preciado') → else honest gap
  Canales (675765): existing 469×469 MAY BE Torres (A2) → VERIFY; if Torres/unverifiable, find verified non-Torres photo

  PCE 2020 base: https://pomonachoiceenergy.org/wp-content/uploads/2020/06/{filename}.jpg (HTTP 200, correct alt text)
  FORBIDDEN (Pitfall 5 — wrong people): any URL containing /2025/02/638723568 (PCE-2025 stale uploads)
  FORBIDDEN: any direct curl to www.pomonaca.gov (WAF-403, Pitfall 7)

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face

Storage + DB:
  - Bucket: politician_photos · Path: canonical {uuid}-headshot.jpg (project_palmdale_no_waf canonical convention)
  - Upload via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), header x-upsert:true
  - politician_images: type='default', photo_license='press_use'
  - UPDATE politicians.photo_origin_url to the source page where NULL
  - Each INSERT guarded WHERE NOT EXISTS on politician_id → exactly ONE type='default' row per member

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql or psql -f). It does NOT
register in schema_migrations (same convention as Palmdale 920 / Lancaster 912). On-disk counter
authoritative; ledger stays at 927 (Plan 02).

Pattern migration: C:/EV-Accounts/backend/migrations/920_palmdale_headshots.sql (audit-only INSERT +
photo_origin_url backfill + Storage URL shape). For WAF-blocked sourcing precedent see
912_lancaster_headshots.sql (Lancaster was also WAF-blocked — alt sources + checkpoint).
/find-headshots skill: ~/.claude/commands/find-headshots.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight image-coverage/dimension check; source + verify portraits for the 4 missing/new members (Sandoval/Garcia/Lustro/Ontiveros-Cole) from alt sources (NO city-site curl); crop 4:5 → 600×750 → upload</name>
  <files>C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/920_palmdale_headshots.sql (audit-only INSERT + download/crop/upload pipeline + Storage URL shape)
    - C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql (WAF-blocked alt-source sourcing + checkpoint precedent)
    - .planning/phases/146-palmdale-deep-seed/146-03-PLAN.md (the exact pre-flight-coverage + crop/resize + checkpoint shape this plan mirrors — but Pomona IS WAF-blocked)
    - .planning/phases/147-pomona-deep-seed/147-RESEARCH.md (§6 headshot sources/status, §Common Pitfalls 5/7, §Assumptions A1/A2/A6)
    - .planning/phases/147-pomona-deep-seed/147-VALIDATION.md (Wave 3 SQL assertions)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots — 4:5 crop FIRST → 600×750 Lanczos q90)
    - ~/.claude/commands/find-headshots.md (skill for discovering/verifying alternative headshot sources)
  </read_first>
  <action>
    First run a pre-flight image-coverage + dimension query: for the 7 current members (-200916/675752/
    675753/-201350/-700658/-201352/675765), LEFT JOIN essentials.politician_images (type='default') and list
    who has a type='default' image, its url + dimensions where known — confirm the missing set
    (Sandoval/Garcia/Lustro/Ontiveros-Cole), Martin's good existing photo, Preciado's too-small (150×150)
    photo, and Canales's suspect (469×469, possible Torres) photo. Do NOT attempt any curl to
    www.pomonaca.gov (WAF-403, Pitfall 7). For the 4 missing/new members, source from the confirmed alt
    sources (Sandoval: timsandoval.com/about → PCE 2020; Garcia: noraforpomona.com/about → PCE 2020; Lustro:
    PCE 2020; Ontiveros-Cole: PCE 2020), using the /find-headshots skill to discover/verify where helpful.
    For EACH, visually confirm it is the correct named person and has NO superimposed text/graphics; REJECT
    any URL containing /2025/02/638723568 (stale wrong-person PCE-2025, Pitfall 5). Then crop to 4:5 (crop
    only — never stretch; eyes ~1/3 from top, full head+shoulders), resize to 600×750 Lanczos q90 JPEG
    (Pillow), resolve each UUID by external_id, and upload to politician_photos at {uuid}-headshot.jpg via
    SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env) with x-upsert:true. Record each public Storage
    URL + source page for Task 2. If no acceptable verified portrait is obtainable for a member, document the
    genuine gap (do NOT upload a campaign/overlaid/wrong-person image as type='default').
  </action>
  <verify>
    <automated>For each sourced member the processed file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('{member}.jpg').size)"` → (600, 750)) and the uploaded Storage object at {uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight image-coverage + dimension query run; the missing/suspect set determined from live data (Sandoval/Garcia/Lustro/Ontiveros-Cole missing; Martin good; Preciado 150×150; Canales 469×469 possible-Torres)
    - NO curl to www.pomonaca.gov attempted (WAF-403, Pitfall 7); the 4 missing/new portraits sourced from confirmed alt sources (campaign sites / PCE 2020); /find-headshots used where helpful
    - Each of the 4 portraits is the correct verified person, no superimposed text/graphics, processed to exactly 600×750, 4:5, not stretched, eyes ~1/3 from top, uploaded to {uuid}-headshot.jpg (HTTP 200)
    - NO forbidden /2025/02/638723568 PCE-2025 URL used for any member (Pitfall 5)
    - Public Storage URL + source page recorded for Task 2 for each uploaded member
    - If any of the 4 has no acceptable portrait, an honest gap documented (NOT a wrong-person/overlaid image faked as type='default')
  </acceptance_criteria>
  <done>The 4 missing/new members (Sandoval/Garcia/Lustro/Ontiveros-Cole) have verified portraits sourced from alt sources (no city-site curl), each confirmed correct + clean, processed to 600×750 4:5, uploaded to Storage at {uuid}-headshot.jpg; Storage URLs + sources recorded for Task 2; any genuine gap documented.</done>
</task>

<task type="auto">
  <name>Task 2: Reprocess Martin's good DB photo; verify/replace Canales (reject Torres); upgrade Preciado from 150×150 if obtainable; author + apply migration 928 (audit-only) + full-roster image audit</name>
  <files>C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/920_palmdale_headshots.sql (INSERT politician_images + photo_origin_url backfill + audit-only apply, ledger-unchanged check)
    - C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql (full-roster coverage audit + documented-gap handling)
    - .planning/phases/147-pomona-deep-seed/147-RESEARCH.md (§6 existing-headshot audit incl. Martin good / Preciado small / Canales possible-Torres, §Common Pitfalls 5, §Validation Architecture Wave 3, §Assumptions A1/A2)
    - .planning/phases/147-pomona-deep-seed/147-VALIDATION.md (Wave 3 SQL assertions)
    - ~/.claude/commands/find-headshots.md (skill for sourcing a verified non-Torres Canales photo)
  </read_first>
  <action>
    Handle the 3 existing-photo members. Martin (675752): download her existing DB 1280×1700 JPEG, VERIFY it
    is Debra Martin (A1, not a prior D1 occupant), crop 4:5 → 600×750 Lanczos q90, upload to her
    {uuid}-headshot.jpg with x-upsert:true (canonical path). Canales (675765): VERIFY the existing 469×469
    photo's identity — if it is Robert Torres or cannot be verified as Lorraine Canales (A2), source a
    verified non-Torres photo (campaign site / Ballotpedia / news via /find-headshots), crop/resize, upload;
    if no verified Canales photo is obtainable, document an honest gap and leave her existing row as-is rather
    than asserting a Torres photo as Canales. NEVER use any /2025/02/638723568 PCE-2025 URL (Pitfall 5).
    Preciado (675753): the existing 150×150 is too small — if the PCE 2020 photo (172×230, alt='Victor
    Preciado') is verified correct, crop/resize/upload to replace it; else document an honest gap (do not
    upscale 150×150 into a blurry image).
    Then write C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql: for every member uploaded in Task
    1 or this task, INSERT a politician_images row (resolved by external_id) url = the Storage public URL,
    type='default', photo_license='press_use', guarded WHERE NOT EXISTS on politician_id (or replace an
    existing too-small/wrong row by its exact image id/url for Preciado/Canales); UPDATE
    essentials.politicians SET photo_origin_url = the source page WHERE external_id=<id> AND photo_origin_url
    IS NULL. Apply via raw SQL (mcp__supabase-local execute_sql or psql -f) — do NOT run through the
    schema_migrations ledger (audit-only). Then run the full-roster coverage audit: LEFT JOIN the 7 current
    Pomona officials (-200916/675752/675753/-201350/-700658/-201352/675765) to politician_images
    (type='default') and report who has exactly one type='default' row vs a documented gap; confirm NO row's
    url contains /2025/02/638723568. Verify MAX(version) in schema_migrations is UNCHANGED by this file
    (stays 927).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (-200916,675752,675753,-201350,-700658,-201352,675765) AND pi.url LIKE '%/2025/02/638723568%';" returns 0 (no stale wrong-person PCE-2025 photo) AND psql "$DATABASE_URL" -c "SELECT p.external_id, COUNT(pi.*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (-200916,675752,675753,-201350,-700658,-201352,675765) GROUP BY p.external_id;" shows each member with COUNT 0 (documented gap) or 1 (no member has >1)</automated>
  </verify>
  <acceptance_criteria>
    - Martin (675752): her good DB photo reprocessed to 600×750 4:5 at the canonical path; identity confirmed Debra Martin (A1); exactly one type='default' press_use row
    - Canales (675765): identity verified; if existing photo was Torres/unverifiable, replaced with a verified non-Torres photo OR documented as an honest gap (NOT a Torres photo asserted as Canales)
    - Preciado (675753): upgraded from 150×150 via a verified PCE 2020 photo to 600×750, OR documented as an honest gap (no blurry upscale)
    - NO politician_images row for any Pomona member has a url containing /2025/02/638723568 (Pitfall 5)
    - Each member with a verified portrait has exactly one type='default' press_use row pointing at {uuid}-headshot.jpg; photo_origin_url populated; no member has >1 type='default' row
    - Members with no obtainable verified portrait are documented as honest gaps (not faked)
    - schema_migrations MAX(version) unchanged by migration 928 (audit-only confirmed; stays 927)
    - Migration is idempotent (re-apply = 0 rows changed)
  </acceptance_criteria>
  <done>928 applied (audit-only); Martin reprocessed; Canales verified/replaced-or-gapped (never Torres); Preciado upgraded-or-gapped; no stale PCE-2025 photo anywhere; full-roster image audit shows each current member with exactly one default image or a documented honest gap; ledger untouched (stays 927).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify every Pomona headshot (correct person, no Torres/Gonzalez, clean crop)</name>
  <action>Pause for the user to visually verify each Pomona portrait per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>Verified official headshots for the 7 current Pomona members, sourced from confirmed alt sources (pomonaca.gov is WAF-403 — no city-site curl): PCE 2020 / campaign sites for Sandoval/Garcia/Lustro/Ontiveros-Cole, reprocessed DB photo for Martin, verified-or-gapped for Preciado/Canales; all processed to 600×750 4:5 and live in Supabase Storage + politician_images (type='default', press_use). Stale PCE-2025 wrong-person photos rejected. Any honest gaps documented.</what-built>
  <how-to-verify>
    1. Confirm each member's photo is the CORRECT person (cross-check Sandoval/Garcia/Lustro/Ontiveros-Cole against PCE/campaign sources; Martin against a known Debra Martin image; Preciado against a known image).
    2. CRITICAL: confirm Canales is NOT Robert Torres and Martin is NOT Rubio Gonzalez — these are the known wrong-person stale-photo traps.
    3. Confirm no superimposed text/graphics over any face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    4. Confirm each member shows exactly one canonical image (no duplicate type='default' rows) on the Pomona browse view; any documented gap is genuinely imageless (not faked).
    5. Confirm no photo URL contains /2025/02/638723568 (forbidden stale PCE-2025).
    6. Confirm the on-disk counter is authoritative: schema_migrations MAX(version) is unchanged (still 927; the headshot file did not register).
  </how-to-verify>
  <resume-signal>Type "approved" or name which photo(s) need re-sourcing / re-cropping / identity-fix.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with WAF-blocked official image source. No untrusted user input reaches the DB — uploads +
image-row writes use service-role/operator against production. Threat surface = wrong-person photo (the
known Torres-as-Canales / Gonzalez-as-Martin stale-photo traps), a distorted/overlaid photo, a fabricated/
campaign photo filling a real gap, an attempted curl to the WAF-blocked city site, re-sourcing over Martin's
good image, or accidental ledger registration — mitigated by the pre-flight coverage/dimension check, the
explicit forbidden-URL rule (/2025/02/638723568), the no-city-curl rule, the crop-then-resize/no-stretch
rule, a blocking human-verify checkpoint that specifically checks for Torres/Gonzalez, the honest-gap rule,
and the audit-only raw-SQL apply.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pomonaca.gov (WAF-403) | Official source UNAVAILABLE — never curled (Pitfall 7); alt sources only. |
| Pomona Choice Energy / campaign sites / Ballotpedia → local processing | Alt image sources; PCE-2020 verified correct, PCE-2025 known wrong-person — human-verified before upload. |
| operator → Supabase Storage + production DB | Uploads + image-row INSERT against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-19 | Spoofing | Wrong-person headshot (Torres-as-Canales / Gonzalez-as-Martin stale PCE-2025 photos) | mitigate | FORBID any url containing /2025/02/638723568; use Martin's good DB photo; verify Canales identity; blocking human-verify specifically checks Canales≠Torres, Martin≠Gonzalez (Pitfall 5) |
| T-147-20 | Tampering | Attempted curl to WAF-blocked pomonaca.gov wastes effort / marks false gaps (Pitfall 7) | mitigate | No curl to www.pomonaca.gov; alt sources (PCE 2020, campaign sites, /find-headshots) used directly |
| T-147-21 | Tampering | Image stretched / aspect-distorted / text-overlaid, or 150×150 upscaled into a blurry image | mitigate | Crop-to-4:5-FIRST-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; Preciado gapped rather than blurry-upscaled; human checkpoint |
| T-147-22 | Spoofing | Fabricated / campaign / wrong photo uploaded to fill a gap (Preciado/Canales) | mitigate | Honest-gap rule — if no acceptable verified portrait, leave imageless and document; never seed a wrong-person/overlaid photo as type='default' |
| T-147-23 | Tampering | Re-sourcing over a good image, or duplicate image row | mitigate | Pre-flight coverage query targets only the missing/too-small/suspect set; each INSERT guarded WHERE NOT EXISTS on politician_id; replacements target exact id/url; post-audit confirms ≤1 type='default' per official |
| T-147-24 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged (stays 927) before/after |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | No package installs (existing Pillow + Supabase client; RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After 928 + checkpoint:
1. each current member with a verified portrait has exactly one type='default' press_use image at {uuid}-headshot.jpg; photo_origin_url set
2. full-roster coverage: every member is either imaged (exactly one default) or a documented honest gap; no member has >1
3. NO politician_images url contains /2025/02/638723568 (no stale wrong-person photo)
4. Canales is verified not-Torres (or honestly gapped); Martin is verified not-Gonzalez
5. schema_migrations MAX(version) unchanged by 928 (stays 927)
6. human checkpoint approved (correct person each, no overlays, correct crop)
</verification>

<success_criteria>
Every current Pomona member with an obtainable correct photo has a single 600×750 verified image at the
canonical path; portraits are sourced from confirmed alt sources (pomonaca.gov WAF-403 never curled); the
known wrong-person stale photos (Torres-as-Canales, Gonzalez-as-Martin) are rejected; no
wrong-person/overlaid/stretched/blurry photos; genuine gaps documented honestly; the ledger counter is
preserved (stays 927).
</success_criteria>

<output>
Create `.planning/phases/147-pomona-deep-seed/147-03-SUMMARY.md` when done (record any honest image gaps).
</output>
</content>
