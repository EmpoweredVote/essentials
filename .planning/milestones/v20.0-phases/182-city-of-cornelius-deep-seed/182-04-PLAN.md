---
phase: 182-city-of-cornelius-deep-seed
plan: 04
type: execute
wave: 3
depends_on: ["182-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1198_dalin_stances.sql
  - C:/EV-Accounts/backend/migrations/1199_godinez_valencia_stances.sql
  - C:/EV-Accounts/backend/migrations/1200_baker_stances.sql
  - C:/EV-Accounts/backend/migrations/1201_lopez_stances.sql
autonomous: false
requirements: [WASH-08]

must_haves:
  truths:
    - "Each of the 4 FILLED officials has evidence-only compass stances where a public record exists (the vacant 5th seat yields zero stances by definition)"
    - "Every stance is 100% cited (reasoning + sources in politician_context); ZERO default/neutral values"
    - "Topics with no evidence are omitted entirely (honest blank spokes), never defaulted — expect a THINNER-than-average yield: Baker was appointed only weeks ago (June 2026) and López (appointed April 2023) may have a modest record; Mayor Dalin (continuously seated since Nov 2011) plausibly has the deepest record"
    - "topic_id is resolved LIVE via JOIN on compass_topics.topic_key AND is_live=true (no hardcoded topic UUIDs)"
    - "Each stance migration ends with the strengthened FOUR-GATE DO block (WR-01 identity + answers-count + WR-03 context-parity + WR-04 content-correspondence bidirectional set-equality with non-empty reasoning/sources) from the Sherwood 1189-1195 template"
    - "No judicial-* topics are used (Cornelius council-manager form — City Attorney/Municipal Court Judge appointed, confirmed at Wave-0)"
    - "Stance research was done one agent at a time (never parallel), on model=sonnet, and agents author their own migration files directly"
    - "Spanish-language sources are ADMITTED as evidence (D-15 — Cornelius is Oregon's most heavily Latino city): cite the ORIGINAL Spanish-language URL, write the reasoning in English faithfully summarizing (never machine-translate-and-embellish); a source the agent cannot actually read is not evidence"
    - "The Nov 17, 2025 council-minutes local-immigration/Equity Corps of Oregon anchor is attributed to Mayor Dalin + Council President Godinez Valencia only after re-fetching the actual minutes PDF via curl+pdftotext (WebFetch OCR fails on the scanned Agenda Center PDFs) and citing the original document URL"
    - "Pamplin / Forest Grove News-Times content is recovered via search-index extraction (pamplinmedia.com TLS-fails all fetchers), cited to the original article URL; never fabricated"
    - "No 2026-election candidate position is seeded as a current stance (currently-seated roster only; Phase 185 owns 2026 candidate data)"
    - "Decision coverage — D-08: all live compass topics per official, one research agent at a time on model=sonnet, evidence-only, 100% cited, honest blank spokes, zero default values, judicial-* topics skipped, agents author their own migrations; D-15: Spanish-language sources admitted, cited to original URLs with faithful English reasoning; D-16: Pamplin search-index extraction + curl+pdftotext for scanned minutes PDFs"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1198_dalin_stances.sql"
      provides: "Mayor Jeffrey C. Dalin stances (audit-only)"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1199_godinez_valencia_stances.sql"
      provides: "Councilor / Council President Angeles Godinez Valencia stances (audit-only)"
      contains: "politician_answers"
  key_links:
    - from: "each stance migration"
      to: "inform.compass_topics (is_live=true)"
      via: "JOIN on topic_key"
      pattern: "topic_key = s.topic_key AND ct.is_live = true"
    - from: "each stance migration"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\)"
---

<objective>
Research and author 4 audit-only stance migrations — one per FILLED Cornelius official — populating
evidence-only compass stances. Research runs ONE AGENT AT A TIME (rate-limit rule) on model=sonnet,
evidence-only, 100% cited, honest blank spokes where no public record exists, ZERO default values, and
skips all judicial-* topics (Cornelius's council-manager form means the City Attorney + Municipal Court
Judge are appointed, confirmed at Wave-0). Every file carries the strengthened FOUR-GATE DO block
(WR-01 identity + answers-count + WR-03 context-parity + WR-04 content-correspondence) from the Sherwood
1189-1195 template. Spanish-language sources are admitted per D-15 (Cornelius is Oregon's most heavily
Latino city) — cite the original Spanish URL with faithful English reasoning. The strong shared anchor
is the Nov 17, 2025 council-minutes discussion of federal immigration enforcement + the Equity Corps of
Oregon (Mayor Dalin + Council President Godinez Valencia), attributable only after re-fetching the
actual minutes PDF via curl+pdftotext. Expect a thinner-than-average yield given 2 recent appointees.

Purpose: Satisfy the stance portion of WASH-08 — evidence-only stances render on profiles.
Output: 4 audit-only migrations (`1198_dalin_stances.sql`, `1199_godinez_valencia_stances.sql`,
`1200_baker_stances.sql`, `1201_lopez_stances.sql`), NOT registered in the ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** stance research runs via ORCHESTRATOR-spawned general-purpose agents (one at
a time, model=sonnet — gsd-executor has NO WebSearch and NO Supabase MCP). The agents author the 4
`.sql` files directly from the researched evidence; the INLINE ORCHESTRATOR applies each via `psql -f`
and commits them to the EV-Accounts repo (`git -C "C:/EV-Accounts"` — never cd into it; check
`git -C "C:/EV-Accounts" status` for pre-staged files before committing, per D-16). These are AUDIT-ONLY
migrations — none writes a schema_migrations ledger row; the on-disk file counter is authoritative.
Migration numbers 1198-1201 assume plan 02 used 1196 + plan 03 used 1197; if Wave-0 shifted the base,
renumber consistently. Files carrying the accented 'López' literal must be saved UTF-8 without a BOM.

**STANCE RULES (non-negotiable — from operator conventions + memory):**
- ONE research agent at a time — never parallel (parallel burns rate-limit quota with no output);
  model=sonnet (181 quota lesson — worked fine); time-box each agent (~20-25 web ops).
- Evidence-only chairs model (integer 1-5); a "chair" is the position statement the evidence matches,
  NOT a polarity scale — pick the chair the evidence supports.
- 100% cited: every answer has reasoning + sources in politician_context.
- NO default values — a topic with no evidence is OMITTED entirely (blank spoke is honest).
- Skip all judicial-* topics (Wave-0 confirmed: council-manager form → City Attorney/Municipal Court
  Judge appointed).
- Use the LIVE topic_key list captured in 182-01-SUMMARY.md; resolve topic_id via JOIN on
  compass_topics.topic_key AND is_live=true — never hardcode a topic UUID.
- Aim for as many cited stances as the record supports; thin records get honest blanks. Expect a LOWER
  total than most predecessor cities: Baker seated June 2026 (weeks old), López appointed April 2023
  (modest record likely); Mayor Dalin (seated since Nov 2011) plausibly the deepest.
- D-15 SPANISH-LANGUAGE SOURCES ADMITTED: Cornelius is Oregon's most heavily Latino city and
  communicates bilingually. A Spanish-only article, city communication, or candidate statement is
  evidence under the same triple-rigor rule — cite the ORIGINAL Spanish URL in the sources array, write
  the reasoning in English faithfully summarizing (never machine-translate-and-embellish). If the agent
  cannot actually read the source content, it is NOT evidence. Actively search Spanish-language primary
  sources, not just English.
- NOV 17 2025 IMMIGRATION ANCHOR: the council minutes record Council President Godinez Valencia
  pointing residents to immigration resources + the Equity Corps of Oregon, and Mayor Dalin reporting
  metro-mayor coordination on federal immigration-enforcement activity + community-safety guidance.
  This is directly on-topic for local-immigration. Re-fetch and READ the actual minutes PDF via
  curl+pdftotext (WebFetch OCR fails on the scanned Agenda Center PDFs) before citing it; cite the
  original document URL. Attribute per official only where the minutes actually record that official.
- D-16 PAMPLIN: pamplinmedia.com (Forest Grove News-Times, shared west-metro desk) TLS-fails for ALL
  fetchers — recover coverage via search-index extraction, cite the ORIGINAL article URL, stay strictly
  evidence-only. Never fabricate content the index does not actually show.
- Do NOT seed any 2026-election candidate position as a current stance — this is the currently-seated
  roster only (Phase 185 owns 2026 candidate data).
- Appointed-seat context: Baker + López are seated by APPOINTMENT (not election) — do NOT describe them
  as "elected at-large"; anchor their evidence to their actual appointed-seat record.
</context>

<interfaces>
inform.compass_topics    (id, topic_key, is_live)   -- resolve topic_id via JOIN; judicial-* topics SKIPPED
inform.politician_answers (politician_id, topic_id, value)                    -- ON CONFLICT (politician_id, topic_id) DO UPDATE SET value
inform.politician_context (politician_id, topic_id, reasoning, sources)       -- sources is text[]; ON CONFLICT DO UPDATE
-- politician UUIDs: resolve by external_id from 182-02-SUMMARY.md (4 FILLED seats only):
--   -4115551 Jeffrey C. Dalin (Mayor) | -4115552 Angeles Godinez Valencia (Council President)
--   -4115553 Edgar Baker (appointed) | -4115554 Edén López (appointed)
-- File→official mapping: 1198=Dalin, 1199=Godinez Valencia, 1200=Baker, 1201=López
-- The vacant 5th seat (-4115555) has no politician and gets no stance migration.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Research + author stance migrations for Dalin and Godinez Valencia (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1198_dalin_stances.sql, C:/EV-Accounts/backend/migrations/1199_godinez_valencia_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1189_rosener_stances.sql (the strengthened stance template — two-statement politician_answers + politician_context structure + the FOUR-GATE DO block: WR-01 identity + answers-count + WR-03 context-parity + WR-04 content-correspondence)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md §"Stance migrations 1198…120x_<lastname>_stances.sql" (the two-statement CTE shape + four-gate block + the D-15 bilingual-evidence note + the model/quota note)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md §"Stance Evidence Landscape" (Nov 17 2025 immigration anchor; bilingual-evidence expectation; Pamplin fallback) + §"Common Pitfalls" (Pitfall 2 appointed seats)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md (live topic_key list + Nov-17-minutes prerequisite) + 182-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Dalin (-4115551) then Godinez Valencia (-4115552), IN SEQUENCE (one research agent at a
    time, model=sonnet — do not spawn parallel research): research the official's documented positions
    across the live compass topics (skip judicial-*), gathering evidence with URLs. Consult the city
    Agenda Center meeting-packet/minutes PDFs (via curl+pdftotext — WebFetch OCR fails on the scanned
    PDFs), corneliusor.gov communications, and Spanish-language primary sources (D-15 — cite the original
    Spanish URL, English reasoning), using D-16 search-index extraction for any pamplinmedia.com / Forest
    Grove News-Times content and citing the original article URL. Re-fetch and READ the Nov 17, 2025
    council minutes PDF and attribute the local-immigration / Equity Corps of Oregon discussion to
    whichever of these two the minutes actually record (RESEARCH: both Dalin and Godinez Valencia).
    Then write each migration file using the two-statement structure from the 1189 analog: a
    WITH s(topic_key, val, reasoning, sources) VALUES block (one row per topic WITH evidence — omit
    topics with no evidence), an INSERT into inform.politician_answers ... JOIN compass_topics ON
    topic_key AND is_live=true ON CONFLICT (politician_id, topic_id) DO UPDATE SET value, then the
    IDENTICAL VALUES block feeding INSERT into inform.politician_context ... ON CONFLICT DO UPDATE SET
    reasoning, sources. THEN append the FOUR-GATE DO block from 1189: (1) WR-01 identity gate — resolve
    external_id from essentials.politicians WHERE id='<uuid>' and RAISE EXCEPTION if it IS DISTINCT FROM
    the expected ext_id; (2) answers-count gate — SELECT COUNT(*) from inform.politician_answers for the
    uuid and RAISE if <> expected authored count; (3) WR-03 context-parity — SELECT COUNT(*) from
    inform.politician_context for the uuid and RAISE if <> expected count; (4) WR-04
    content-correspondence — bidirectional set-equality between politician_answers and politician_context
    on topic_id PLUS a non-empty-reasoning/non-empty-sources assertion on every context row. Hardcode
    the politician UUID (from 182-02-SUMMARY.md) and its expected ext_id in each file. val is an integer
    1-5 chair; never default; blank spoke = omit the row. Wrap each file in BEGIN...COMMIT with the
    audit-only header comment; NO ledger INSERT. DALIN has the deepest plausible record (Mayor since Nov
    2011) — mine Agenda Center minutes and city communications accordingly. Save any file containing the
    accented 'López' in a comment as UTF-8 no BOM (not applicable to these two, but a standing rule). No
    fenced code in this action — 1189 + 182-PATTERNS.md are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (the four-gate DO block RAISEs on an identity/count/parity/correspondence mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 2 files authored: 1198_dalin_stances.sql, 1199_godinez_valencia_stances.sql
    - Each uses the two-statement politician_answers + politician_context structure
    - Each ends with the FOUR-GATE DO block (WR-01 identity + answers-count + WR-03 context-parity + WR-04 content-correspondence), expected counts matching authored rows
    - topic_id resolved via JOIN on topic_key AND is_live=true (no hardcoded topic UUIDs)
    - No judicial-* topic_key appears in any VALUES block
    - Every VALUES row has non-empty reasoning + at least one source URL; Spanish-language sources (if used) cite the ORIGINAL Spanish URL with faithful English reasoning
    - No default/neutral row for a topic lacking evidence (omitted instead)
    - Nov 17 2025 immigration/Equity Corps anchor attributed only where the re-fetched minutes PDF records that official, cited to the original document URL
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>2 stance migrations authored from one-at-a-time sonnet research; four-gate assertions present; Nov-17 immigration anchor + bilingual-evidence handling correct; audit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Research + author stance migrations for Baker and López (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1200_baker_stances.sql, C:/EV-Accounts/backend/migrations/1201_lopez_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1189_rosener_stances.sql (same strengthened four-gate template)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md §"Stance migrations 1198…120x_<lastname>_stances.sql"
    - .planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md §"Stance Evidence Landscape" (appointee-heavy roster → expect low yield; Baker seated June 2026; López appointed April 2023) + §"Common Pitfalls" (Pitfall 2 appointed seats)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md (live topic_key list) + 182-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Baker (-4115553) then López (-4115554), IN SEQUENCE (one agent at a time, model=sonnet):
    research documented positions across live compass topics (skip judicial-*), then author the
    migration with the same two-statement structure + FOUR-GATE DO block as Task 1. These two are
    APPOINTED (not elected) — Baker seated only weeks ago (June 2026), López since April 2023 — so expect
    a THIN record and MANY honest blanks; do NOT pad, do NOT default, and do NOT describe them as
    "elected at-large." Anchor evidence to their actual appointed-seat record: Agenda Center
    minutes/votes since seating, corneliusor.gov communications, and Spanish-language primary sources
    (D-15 — cite the original Spanish URL, English reasoning; especially relevant for López given the
    city's bilingual communications). Use D-16 search-index extraction for any pamplinmedia.com content,
    citing original URLs. If the Nov 17 2025 minutes (or any dated record) attribute a position to Baker
    or López, cite it via curl+pdftotext to the original document URL. val is an integer 1-5 chair; never
    default; blank spoke = omit the row. Audit-only header; NO ledger INSERT. UUIDs + expected ext_ids
    hardcoded from 182-02-SUMMARY.md. SAVE 1201_lopez_stances.sql AS UTF-8 WITHOUT A BOM — the file will
    carry the accented 'Edén López' in its header comment and reasoning. No fenced code in this action.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (four-gate DO block RAISEs on mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 2 files authored: 1200_baker_stances.sql, 1201_lopez_stances.sql (1201 saved UTF-8 no BOM)
    - Same two-statement structure + FOUR-GATE DO block (WR-01 identity + answers-count + WR-03 context-parity + WR-04 content-correspondence); topic_id via is_live JOIN
    - No judicial-* topics; every row cited; no defaults (thin appointee records → honest blanks, not padding)
    - Baker/López anchored to their appointed-seat record (not described as "elected at-large")
    - Spanish-language sources (if used) cite the ORIGINAL Spanish URL with faithful English reasoning
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>2 stance migrations authored from one-at-a-time sonnet research; four-gate assertions present; thin appointee records honored with honest blanks (no padding); López file UTF-8 no BOM; audit passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator applies all 4, runs stance audit, commits</name>
  <what-built>4 audit-only stance migrations. Orchestrator applies each and runs the citation/no-default audit.</what-built>
  <how-to-verify>
    1. Apply all 4 in order: `psql "$DATABASE_URL" -f <each stance file>`. Each four-gate DO block must pass (no identity/count/parity/correspondence RAISE).
    2. Stance audit (run as SELECTs against the 4 Cornelius filled politician UUIDs):
       a. Every politician_answers row has a matching politician_context row with non-null reasoning + non-empty sources (100% cited).
       b. ZERO rows where value is a defaulted/neutral placeholder for a topic lacking evidence (spot-check reasoning quality).
       c. ZERO judicial-* topics linked to any Cornelius official.
       d. Each official has >=1 stance where the record supports it; honest blanks (a 0-count) acceptable where the record is genuinely thin (especially Baker, seated weeks ago) — document rather than force. A 0-count for Mayor Dalin specifically would warrant a re-check given his long tenure.
       e. Spot-check the Nov 17 2025 immigration/Equity Corps anchor: attributed only to officials the minutes actually record (Dalin + Godinez Valencia), cited to the original document URL; any Spanish-language source cites the original Spanish URL with faithful (non-embellished) English reasoning.
    3. Check `git -C "C:/EV-Accounts" status` for pre-staged files (D-16); stage ONLY these 4 migrations. Commit them in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/1198_*.sql backend/migrations/1199_*.sql backend/migrations/1200_*.sql backend/migrations/1201_*.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - All 4 stance migrations applied; all four-gate assertions passed
    - Audit a-e all pass (100% cited, no defaults, no judicial, honest blanks documented, immigration anchor + bilingual sourcing attributed correctly)
    - `git -C "C:/EV-Accounts" status` checked; all 4 committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official stance counts + any honest blanks, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Stance INSERTs applied to production inform schema via psql |
| research agent → public web | Evidence gathered from official/Agenda-Center/news sources (incl. Spanish-language sources per D-15 and D-16 search-index extraction for Pamplin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-21 | Tampering | Uncited or fabricated stance value (incl. fabricating Pamplin content the index does not show, or machine-translating a Spanish source into an embellished claim) | mitigate | 100%-cited rule enforced by audit (every answer needs reasoning + sources); no-default rule; D-15 requires the original Spanish URL + faithful English reasoning (a source the agent cannot read is not evidence); D-16 search-index extraction cites the original article URL; one-agent-at-a-time keeps evidence traceable |
| T-182-22 | Tampering | topic_key typo silently drops authored rows | mitigate | answers-count + context-parity (WR-03) + content-correspondence (WR-04) assertions in every stance file RAISE on a count/set mismatch |
| T-182-23 | Repudiation | Wrong-UUID misattribution (a valid-but-wrong politician UUID) | mitigate | WR-01 identity gate resolves external_id from the hardcoded UUID and RAISEs unless it matches the intended official's ext_id |
| T-182-24 | Spoofing | Nov 17 2025 immigration anchor attributed to an official the minutes do not record, or an appointee (Baker/López) padded with unsupported stances | mitigate | Re-fetch the actual minutes PDF via curl+pdftotext before attributing; no-default rule + audit gate 2d/2e enforce honest blanks for thin appointee records |
| T-182-25 | Elevation of Privilege | Judicial-* topic seeded for a non-judicial official | mitigate | Skip-judicial rule (Wave-0 confirmed council-manager form); audit gate 2c asserts zero judicial-* topics |
| T-182-26 | Tampering (data integrity) | Mojibake corruption of 'Edén López' in 1201 | mitigate | 1201_lopez_stances.sql saved UTF-8 without a BOM |
| T-182-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- 4 stance migrations applied; four-gate assertions pass; audit confirms 100% cited, zero defaults,
  zero judicial topics, honest blanks documented, immigration anchor + bilingual sourcing attributed
  correctly; all committed after a clean git status check.
</verification>

<success_criteria>
- Evidence-only stances for all 4 filled officials, 100% cited, no defaults, no judicial topics,
  four-gate assertions present, researched one agent at a time on sonnet, thin appointee records honored
  with honest blanks, Spanish-language sources admitted with faithful English reasoning, Nov-17
  immigration anchor attributed correctly.
</success_criteria>

<output>
Create `.planning/phases/182-city-of-cornelius-deep-seed/182-04-SUMMARY.md` when done, recording
per-official stance counts and any honest blanks (expect a thinner-than-average total).
</output>
</content>
