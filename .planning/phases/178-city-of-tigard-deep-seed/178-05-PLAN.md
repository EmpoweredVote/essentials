---
phase: 178-city-of-tigard-deep-seed
plan: 05
type: execute
wave: 4
depends_on: ["178-03", "178-04"]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/buildingImages.js
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WASH-04]

must_haves:
  truths:
    - "A legally-licensed 1700×540 community banner for Tigard is uploaded to Supabase Storage cities/tigard.jpg"
    - "buildingImages.js CURATED_LOCAL has a lowercase 'tigard' key pointing at the banner CDN URL, with an attribution comment (verified CC BY-SA version)"
    - "coverage.js Oregon block has a 'Tigard' entry with browseGovernmentList ['4173650'], browseStateAbbrev 'OR', hasContext:true, alphabetically between Portland and Troutdale"
    - "npm run build passes with both edits"
    - "The live browse link surfaces Mayor Hu first + all 6 councilors with headshots + stances, the community banner renders (not the tier gradient), and no party label appears"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "Tigard purple hasContext chip in the Oregon COVERAGE_STATES block"
      contains: "Tigard"
    - path: "C:/Transparent Motivations/essentials/src/lib/buildingImages.js"
      provides: "tigard CURATED_LOCAL banner entry"
      contains: "tigard"
  key_links:
    - from: "coverage.js Oregon areas"
      to: "browse route (browse_geo_id=4173650)"
      via: "browseGovernmentList entry"
      pattern: "label: 'Tigard'"
    - from: "buildingImages.js CURATED_LOCAL 'tigard'"
      to: "offices.representing_city='Tigard' (set inline in mig 1159)"
      via: "getBuildingImages() lowercased substring match"
      pattern: "tigard:"
---

<objective>
Close the phase: (1) source, process, and upload a legally-licensed 1700×540 community banner for
Tigard; (2) wire it via the `tigard` CURATED_LOCAL key in buildingImages.js; (3) surface Tigard in
the Oregon block of coverage.js with the purple hasContext chip. The banner renders because mig 1159
set `offices.representing_city='Tigard'` inline (no backfill needed).

Purpose: Satisfy WASH-04 Success Criterion 4 (purple hasContext chip) + the community banner standing
constraint, and verify the full roster end-to-end on the live app.
Output: banner in Storage (cities/tigard.jpg) + two essentials-repo edits + passing build +
live-browse confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md
@.planning/phases/178-city-of-tigard-deep-seed/178-RESEARCH.md
@.planning/phases/178-city-of-tigard-deep-seed/178-03-SUMMARY.md
@.planning/phases/178-city-of-tigard-deep-seed/178-04-SUMMARY.md
@src/lib/coverage.js
@src/lib/buildingImages.js
@docs/banner-asset-pipeline.md

**This is the essentials repo** — coverage.js and buildingImages.js are committed HERE (`git`), NOT
in C:/EV-Accounts. The purple hasContext chip and the banner-render path are already built; only the
data row + banner asset + CURATED_LOCAL key are new. The banner upload uses the existing committed
scripts scripts/banners/process_banner.py + upload_banner.py — no new script file.

**TAILWIND HYGIENE:** do NOT introduce raw Windows `\` paths into any committed file (Tailwind v4
scans .md/.js and a `\f...` sequence crashes the prod build). Use forward-slash CDN URLs only.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Orchestrator sources + processes + uploads the community banner</name>
  <decision>Which legally-licensed wide photo becomes the Tigard community banner</decision>
  <context>
    Priority hint: downtown/civic horizon. RESEARCH identified a strong candidate —
    commons.wikimedia.org "File:Downtown Tigard Oregon.JPG" (Aboutmovies, 2009, CC BY-SA — VERIFY the
    exact version string at execution per Assumption A10; 960×660, wide street-level civic shot); the
    "Tigard, Oregon" Commons category has ~24 files if a better landscape/civic shot is preferred
    (e.g. "Tigard Public Library entrance - Oregon.JPG"). A clean license + crops-well-to-3.15:1
    (1700×540) beats subject preference. Wikimedia Commons FIRST, Unsplash fallback. NO AI-generated
    images; no baked-in text/graphics. This is a decision checkpoint because the subject/source/license
    is a human-judgment call that then drives the attribution comment. Follow
    docs/banner-asset-pipeline.md Stages 1–8. Run:
    `python scripts/banners/process_banner.py --input <raw> --output <processed_1700x540.jpg>` then
    `python scripts/banners/upload_banner.py --file <processed_1700x540.jpg> --path cities/tigard.jpg`.
    Record the final Title | Author | License (with the verified CC BY-SA version) for the attribution
    comment used in Task 2.
  </context>
  <options>
    <option id="wikimedia-downtown">
      <name>Wikimedia "Downtown Tigard Oregon.JPG" (research-identified, preferred)</name>
      <pros>Already located; wide street-level civic shot; CC BY-SA matches Hillsboro precedent (Steve Morgan CC BY-SA 4.0)</pros>
      <cons>2009 upload at 960×660 — verify it crops cleanly to 3.15:1 without quality loss; confirm exact CC BY-SA version</cons>
    </option>
    <option id="wikimedia-alt">
      <name>Alternate Wikimedia Commons "Tigard, Oregon" category photo</name>
      <pros>~24 files available; may offer a wider/higher-res civic shot (e.g. Tigard Public Library)</pros>
      <cons>Requires per-file license + croppability check</cons>
    </option>
    <option id="unsplash">
      <name>Unsplash fallback photo</name>
      <pros>Higher chance of a wide, crop-friendly horizon shot</pros>
      <cons>Attribution/license differs from Wikimedia convention; confirm Unsplash license terms</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen source + final Title | Author | License string (verified CC BY-SA version), confirming cities/tigard.jpg uploaded (CDN URL returns 200).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner in buildingImages.js and surface Tigard in coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/buildingImages.js, C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - src/lib/buildingImages.js lines 107–113 (CURATED_LOCAL block + attribution comments, incl. beaverton + hillsboro entries)
    - src/lib/coverage.js lines 96–105 (the Oregon block — Beaverton…Wood Village, incl. Portland + Troutdale)
    - .planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md §"src/lib/buildingImages.js" and §"src/lib/coverage.js" (exact inserts)
  </read_first>
  <action>
    Edit 1 — buildingImages.js: add a lowercase `tigard` key to CURATED_LOCAL pointing at
    `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/tigard.jpg`,
    placed alongside the existing `beaverton` + `hillsboro` entries. Add an attribution comment line
    above CURATED_LOCAL in the existing format `//   tigard - <Title> | <Author> | <License>` using the
    string recorded in Task 1 (with the verified CC BY-SA version). The lowercase key is required
    because getBuildingImages() lowercases offices.representing_city and does a substring match; mig
    1159 set representing_city='Tigard' inline so this resolves at first browse.
    Edit 2 — coverage.js: insert `{ label: 'Tigard', browseGovernmentList: ['4173650'],
    browseStateAbbrev: 'OR', hasContext: true }` into the Oregon `areas` array, alphabetically BETWEEN
    'Portland' and 'Troutdale' ("Ti" < "Tr"). Match the existing column alignment of surrounding rows.
    Use geo_id '4173650'. Change nothing else in either file — no other state blocks, no other
    CURATED_LOCAL keys. No raw Windows `\` paths anywhere (forward-slash CDN URLs only).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has `tigard:` → the cities/tigard.jpg CDN URL
    - An attribution comment `//   tigard - ... | ... | ...` is present above CURATED_LOCAL with the verified CC BY-SA version
    - coverage.js Oregon areas has 'Tigard' between 'Portland' and 'Troutdale'
    - Entry is exactly { label: 'Tigard', browseGovernmentList: ['4173650'], browseStateAbbrev: 'OR', hasContext: true }
    - No other lines changed in either file; no raw `\` path introduced
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Banner wired + Tigard surfaced; production build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-browse end-to-end verification + commit</name>
  <what-built>The Tigard banner (Storage) + buildingImages.js key + coverage.js purple chip.</what-built>
  <how-to-verify>
    1. Confirm `npm run build` passed (Task 2 gate).
    2. Browse essentials.empowered.vote/results?browse_geo_id=4173650&browse_mtfcc=G4110 and confirm:
       - Mayor Yi-Kang Hu sorts FIRST, all 6 councilors follow; no empty LOCAL section; no section-split.
       - Headshots render 600×750 with no overlays (partial coverage acceptable per documented gaps); compass stances visible; NO party label (antipartisan — despite Anderson/Ghoddusi being characterized by party in news coverage).
       - The Local section shows the Tigard community banner photo (NOT the tier gradient fallback) — this confirms representing_city='Tigard' (mig 1159) + the lowercase CURATED_LOCAL key resolve correctly.
       - The purple hasContext chip appears for Tigard.
    3. Optionally enter a real Tigard address and confirm the same roster resolves via /representatives/me.
    4. Commit both files in the essentials repo: `git add src/lib/coverage.js src/lib/buildingImages.js && git commit` (on a branch if the harness is on the default branch; end the message with the Co-Authored-By trailer).
  </how-to-verify>
  <acceptance_criteria>
    - Live browse shows Mayor Hu first + 6 councilors, no empty LOCAL section, no section-split
    - Headshots 600×750 no overlays (documented gaps acceptable); stances visible; no party label
    - Community banner renders (not gradient fallback)
    - Purple hasContext chip shows for Tigard
    - coverage.js + buildingImages.js committed in the essentials repo
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the live browse + banner, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Banner sourced from Wikimedia Commons / Unsplash |
| operator → Supabase Storage | Processed banner uploaded to public bucket cities/tigard.jpg |
| build pipeline → prod | coverage.js + buildingImages.js edits shipped via the essentials build/deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-15 | Spoofing/IP | Unlicensed or AI-generated banner image | mitigate | Wikimedia-first with recorded Title/Author/License (verified CC BY-SA version per A10); NO AI-generated images (pipeline forbids); decision checkpoint records the license string |
| T-178-16 | Tampering | Malformed coverage.js/buildingImages.js breaks the prod build | mitigate | `npm run build` gate before commit; additive edits only |
| T-178-17 | Denial of Service | Windows `\` path in committed file crashes Tailwind scan | mitigate | Edits add only forward-slash CDN URLs + data rows; no raw backslash introduced |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; banner scripts (process_banner.py/upload_banner.py) are pre-existing committed tooling |
</threat_model>

<verification>
- `npm run build` exits 0; banner uploaded + rendered; live browse surfaces Mayor-first roster with
  chip, stances, headshots, banner, no section-split, no party label; both files committed.
</verification>

<success_criteria>
- Tigard visible with the purple hasContext chip + community banner in the Oregon block; build green;
  live browse confirms the full roster + banner end-to-end.
</success_criteria>

<output>
Create `.planning/phases/178-city-of-tigard-deep-seed/178-05-SUMMARY.md` when done, recording the
banner source/license and the live-browse confirmation. This is the final plan — note WASH-04 is
satisfied end-to-end.
</output>
