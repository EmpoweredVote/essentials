---
phase: 121-fall-river-medford-waltham-deep-seeds
plan: 05
type: execute
wave: 3
depends_on:
  - 121-01
  - 121-02
  - 121-03
  - 121-04
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-fall-river-headshots.py
  - C:/EV-Accounts/backend/migrations/594_fall_river_headshots.sql
  - C:/EV-Accounts/backend/scripts/_tmp-medford-headshots.py
  - C:/EV-Accounts/backend/migrations/595_medford_headshots.sql
  - C:/EV-Accounts/backend/scripts/_tmp-waltham-headshots.py
  - C:/EV-Accounts/backend/migrations/596_waltham_headshots.sql
autonomous: true
requirements:
  - FALLRIV-02
  - MEDFORD-02
  - WALTHAM-02

must_haves:
  truths:
    - "Python scripts for all three cities run to completion without exceptions"
    - "No politician_images rows with type != 'default' for any Fall River, Medford, or Waltham official"
    - "All gaps documented in migration comments with specific HTTP error code or block reason (never 'unknown')"
    - "All successfully uploaded images are 600x750 (crop 4:5 first, then resize Lanczos q90)"
    - "All uploads go to politician_photos bucket (NOT 'politician-headshots')"
    - "Post-verification PASSED for migrations 594, 595, and 596"
    - "Section-split check returns 0 for all three geo_ids after headshot migrations applied"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-fall-river-headshots.py"
      provides: "Python upload script for Fall River officials"
      contains: "_tmp-fall-river-headshots"
    - path: "C:/EV-Accounts/backend/migrations/594_fall_river_headshots.sql"
      provides: "politician_images INSERTs for Fall River; gap comments"
      contains: "594_fall_river_headshots"
    - path: "C:/EV-Accounts/backend/scripts/_tmp-medford-headshots.py"
      provides: "Python upload script for Medford officials (city + SC)"
      contains: "_tmp-medford-headshots"
    - path: "C:/EV-Accounts/backend/migrations/595_medford_headshots.sql"
      provides: "politician_images INSERTs for Medford; gap comments"
      contains: "595_medford_headshots"
    - path: "C:/EV-Accounts/backend/scripts/_tmp-waltham-headshots.py"
      provides: "Python upload script for Waltham officials"
      contains: "_tmp-waltham-headshots"
    - path: "C:/EV-Accounts/backend/migrations/596_waltham_headshots.sql"
      provides: "politician_images INSERTs for Waltham; gap comments"
      contains: "596_waltham_headshots"
  key_links:
    - from: "_tmp-fall-river-headshots.py ROSTER"
      to: "Supabase Storage politician_photos bucket"
      via: "upload_to_storage(); path = {politician_uuid}-headshot.jpg"
      pattern: "BUCKET = 'politician_photos'"
    - from: "migration 594/595/596 politician_images INSERT"
      to: "essentials.politicians (external_id subquery)"
      via: "SELECT id FROM essentials.politicians WHERE external_id = N"
      pattern: "type = 'default'"
---

<objective>
Upload best-effort headshots for all Fall River, Medford, and Waltham city officials; insert politician_images rows via migrations 594, 595, 596. All gaps documented with specific reasons.

Purpose: Satisfies FALLRIV-02, MEDFORD-02, WALTHAM-02 — each city's elected officials have headshots at 600x750 in Supabase Storage; gaps documented honestly.
Output: 3 Python scripts + 3 SQL migrations; all successfully processed images at 600x750 in politician_photos bucket; post-verification PASSED for all three migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-fall-river-medford-waltham-deep-seeds/121-01-SUMMARY.md
@.planning/phases/121-fall-river-medford-waltham-deep-seeds/121-02-SUMMARY.md
@.planning/phases/121-fall-river-medford-waltham-deep-seeds/121-03-SUMMARY.md
@.planning/phases/121-fall-river-medford-waltham-deep-seeds/121-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fall River headshots — script + migration 594</name>
  <files>
    C:/EV-Accounts/backend/scripts/_tmp-fall-river-headshots.py
    C:/EV-Accounts/backend/migrations/594_fall_river_headshots.sql
  </files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-lynn-headshots.py (primary analog — copy ALL image processing functions verbatim; update only ROSTER, docstring, banner, and Referer header)
    - C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql (primary analog for migration structure — copy WHERE NOT EXISTS guard, post-verification DO block structure)
    - .planning/phases/121-fall-river-medford-waltham-deep-seeds/121-01-SUMMARY.md (confirms politician UUIDs and external_ids loaded in migration 590)
    - .planning/STATE.md (key schema: type='default' requirement, politician_photos bucket name)
  </read_first>
  <action>
PART A: Write C:/EV-Accounts/backend/scripts/_tmp-fall-river-headshots.py

Copy ALL image processing code verbatim from _tmp-lynn-headshots.py:
- All imports (os, io, sys, time, requests, PIL)
- .env config block (SUPABASE_URL, SERVICE_KEY, DATABASE_URL, BUCKET, CDN_BASE, TARGET_SIZE, JPEG_QUALITY, RESAMPLE)
- crop_to_4_5() function
- resize_600x750() function
- download_image() function
- upload_to_storage() function
- resolve_politician_id() function
- process_member() function

MODIFY only:
- Docstring: update to Phase 121 Fall River, migration 594
- BROWSER_HEADERS Referer: 'https://www.fallriverma.org'
- ROSTER: all Fall River officials from migration 590 external_ids (use 121-01 SUMMARY for confirmed external_ids and names)
- main() banner: 'Phase 121 — Fall River Headshots (migration 594)'

HEADSHOT SOURCE STRATEGY for Fall River:
Primary source: https://www.fallriverma.org/city-government/mayor/ (Mayor Coogan headshot)
City Council pages: https://www.fallriverma.org/city-government/city-council/ (per-councilor bio pages, if available)

Source investigation order per official:
1. Official fallriverma.org bio page — check HTML for img src (not a CivicLive/Revize CMS that blocks bots)
2. Wikipedia Commons — check for individual Wikipedia article with Wikimedia Commons image
3. Campaign or official social media (official government photo only — no editorial news photos)
4. If no accessible source: source_url=None

IMPORTANT PITFALL: If fallriverma.org uses a CivicLive/Revize CMS (same pattern as Quincy/Newton which returned HTTP 403), treat all fallriverma.org bio page images as gaps and use Wikipedia/alternative sources only. Test the first councilor URL before attempting the full batch — if it 403s, mark all as gaps immediately.

IMPORTANT PITFALL: Wikipedia Commons requires Wikimedia User-Agent header (Chrome UA returns 429):
WIKIMEDIA_HEADERS = {
    'User-Agent': 'EmpoweredVoteBot/1.0 (https://empowered.vote; research@empowered.vote) Python/requests',
    'Accept': 'text/html,application/xhtml+xml'
}
Use this header only for en.wikipedia.org and commons.wikimedia.org URLs.

For each gap: set gap_reason to the specific HTTP error code or block reason. Never 'unknown'.

Run the script after writing.

PART B: Write C:/EV-Accounts/backend/migrations/594_fall_river_headshots.sql

Use the actual upload results from Part A to write the migration.

Migration structure (copy from 586_lynn_headshots.sql):

Header comment block:
-- Migration 594: Fall River city officials headshots (FALLRIV-02)
-- Storage bucket: politician_photos
-- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/
-- Total officials attempted: N (all Fall River officials from migration 590)
-- Uploaded: [actual count]
-- Gap count: [actual gap count]
-- Photo processing: crop 4:5 first, then resize 600x750 Lanczos q90
-- type = 'default' (NOT 'headshot')

For each uploaded official: INSERT INTO essentials.politician_images ... WHERE NOT EXISTS guard (copy exact pattern from 586).
For each gap official: SQL comment with specific reason.

Post-verification DO block:
- Count type='default' rows for external_id range -2522640030..-2522640001 → v_img_count
- Count type!='default' rows → v_wrong_type; RAISE EXCEPTION if > 0
- RAISE NOTICE 'Migration 594 post-verification PASSED: % headshots (type=default), % gaps'

Migration ledger: INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('594') ON CONFLICT (version) DO NOTHING;
COMMIT;

Apply via mcp__supabase-local__execute_sql after writing.
  </action>
  <verify>
    <automated>python C:/EV-Accounts/backend/scripts/_tmp-fall-river-headshots.py — must run to completion without Python exceptions; then migration 594 must emit: 'Migration 594 post-verification PASSED'</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-fall-river-headshots.py runs to completion (0 Python exceptions)
    - Script shows per-official result for all Fall River officials (uploaded OR gap with specific reason)
    - Script summary: X uploaded + Y gaps = total Fall River officials, 0 Python errors
    - 594_fall_river_headshots.sql exists and applies without errors
    - Migration 594 post-verification PASSED: v_wrong_type = 0
    - Query: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2522640030 AND -2522640001 AND pi.type != 'default' → 0
    - Query: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2522640030 AND -2522640001 AND pi.type = 'default' → matches script upload count
    - Every gap official has SQL comment in 594 with specific reason (not 'unknown')
    - FALLRIV-02 satisfied: best-effort headshots at 600x750; gaps documented
  </acceptance_criteria>
  <done>Fall River headshot script ran; migration 594 applied; all uploads type='default'; all gaps documented with specific reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Medford headshots — script + migration 595</name>
  <files>
    C:/EV-Accounts/backend/scripts/_tmp-medford-headshots.py
    C:/EV-Accounts/backend/migrations/595_medford_headshots.sql
  </files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-fall-river-headshots.py (just written in Task 1 — copy identical structure; update ROSTER + docstring + banner + Referer)
    - C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql (migration structure analog)
    - .planning/phases/121-fall-river-medford-waltham-deep-seeds/121-02-SUMMARY.md (city gov external_ids from migration 591)
    - .planning/phases/121-fall-river-medford-waltham-deep-seeds/121-04-SUMMARY.md (SC external_ids from migration 593)
    - .planning/STATE.md (SCHOOL section headshot scope — SC members included if headshots available)
  </read_first>
  <action>
PART A: Write C:/EV-Accounts/backend/scripts/_tmp-medford-headshots.py

Copy ALL image processing code from _tmp-fall-river-headshots.py (just written).

MODIFY:
- Docstring: Phase 121 Medford, migration 595
- BROWSER_HEADERS Referer: 'https://www.medfordma.org'
- ROSTER: ALL Medford officials — city council officials (from 121-02 SUMMARY, migration 591 external_ids) PLUS SC elected members (from 121-04 SUMMARY, migration 593 external_ids).
  Include Mayor Lungo-Koehn once (city gov external_id -2540115001 only — her SC ex-officio office does not need a separate headshot row).
- main() banner: 'Phase 121 — Medford Headshots (migration 595)'

HEADSHOT SOURCE STRATEGY for Medford:
City officials: https://www.medfordma.org/departments/office-of-the-mayor/ (Mayor Lungo-Koehn)
City Council: https://www.medfordma.org/departments/city-council/ (per-councilor bio pages)
SC members: https://www.medfordschools.org/o/mps/page/school-committee (per-member photos if available)

Source investigation order per official:
1. Official medfordma.org or medfordschools.org bio page
2. Wikipedia Commons (Wikimedia UA headers required)
3. Campaign or official social (no editorial news photos)
4. source_url=None if no accessible source

Same Wikipedia UA header pattern as Task 1 if needed.

Run after writing.

PART B: Write C:/EV-Accounts/backend/migrations/595_medford_headshots.sql

Use actual upload results from Part A.

Header comment:
-- Migration 595: Medford city officials + School Committee headshots (MEDFORD-02)
-- Officials scope: city council (migration 591) + SC elected members (migration 593)
-- Mayor Lungo-Koehn appears once (city external_id only)
-- type = 'default' (NOT 'headshot')

Post-verification covers ALL Medford officials:
- external_id range for city officials: -2540115001 through -2540115NNN
- external_id range for SC members: -<LEAID>001 through -<LEAID>NNN
Use two separate COUNT queries joined by addition, or run as two DO block checks.

Post-verification DO block:
- Count type='default' for all Medford city + SC external_ids → v_img_count
- Count type!='default' → v_wrong_type; RAISE EXCEPTION if > 0
- RAISE NOTICE 'Migration 595 post-verification PASSED: % headshots (type=default), % gaps'

Migration ledger: INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('595') ON CONFLICT (version) DO NOTHING;
COMMIT;

Apply via mcp__supabase-local__execute_sql.
  </action>
  <verify>
    <automated>python C:/EV-Accounts/backend/scripts/_tmp-medford-headshots.py — must run to completion; then migration 595 must emit: 'Migration 595 post-verification PASSED'</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-medford-headshots.py runs to completion (0 Python exceptions)
    - Script shows per-official result for all Medford city + SC officials (uploaded OR gap with specific reason)
    - 595_medford_headshots.sql exists and applies without errors
    - Migration 595 post-verification PASSED: v_wrong_type = 0 for all Medford external_ids
    - Query: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2540115020 AND -2540115001 AND pi.type != 'default' → 0
    - Query for SC: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -<LEAID>N AND -<LEAID>001 AND pi.type != 'default' → 0
    - Every gap has SQL comment with specific reason in 595
    - MEDFORD-02 satisfied: best-effort headshots; gaps documented
  </acceptance_criteria>
  <done>Medford headshot script ran; migration 595 applied; all uploads type='default'; gaps documented.</done>
</task>

<task type="auto">
  <name>Task 3: Waltham headshots + phase-wide section-split verification — script + migration 596</name>
  <files>
    C:/EV-Accounts/backend/scripts/_tmp-waltham-headshots.py
    C:/EV-Accounts/backend/migrations/596_waltham_headshots.sql
  </files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-fall-river-headshots.py (copy structure; update ROSTER + docstring + banner + Referer)
    - C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql (migration structure analog)
    - .planning/phases/121-fall-river-medford-waltham-deep-seeds/121-03-SUMMARY.md (Waltham external_ids from migration 592)
    - .planning/STATE.md (geo_ids for Fall River, Medford, Waltham for section-split check)
  </read_first>
  <action>
PART A: Write C:/EV-Accounts/backend/scripts/_tmp-waltham-headshots.py

Copy ALL image processing code from _tmp-fall-river-headshots.py (Task 1).

MODIFY:
- Docstring: Phase 121 Waltham, migration 596
- BROWSER_HEADERS Referer: 'https://www.city.waltham.ma.us'
- ROSTER: all Waltham officials from 121-03 SUMMARY (migration 592 external_ids)
- main() banner: 'Phase 121 — Waltham Headshots (migration 596)'

HEADSHOT SOURCE STRATEGY for Waltham:
Mayor: https://mayor.waltham.ma.us/ or https://www.city.waltham.ma.us/mayor
City Council: https://www.city.waltham.ma.us/city-council (per-councilor bio pages if available)

Source investigation order per official:
1. Official city.waltham.ma.us bio page
2. Wikipedia Commons (Wikimedia UA headers)
3. Campaign or official social (no editorial news photos)
4. source_url=None if no accessible source

Same Wikipedia UA header pattern if needed.

Run after writing.

PART B: Write C:/EV-Accounts/backend/migrations/596_waltham_headshots.sql

Use actual upload results from Part A.

Header comment:
-- Migration 596: Waltham city officials headshots (WALTHAM-02)
-- type = 'default' (NOT 'headshot')

Post-verification DO block (covers Waltham only):
- Count type='default' → v_img_count; count type!='default' → v_wrong_type
- RAISE EXCEPTION if v_wrong_type > 0
- RAISE NOTICE 'Migration 596 post-verification PASSED: % headshots (type=default), % gaps'

Migration ledger: INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('596') ON CONFLICT (version) DO NOTHING;
COMMIT;

Apply via mcp__supabase-local__execute_sql.

PART C: Phase-wide section-split verification (run after migration 596 is applied)

Run the section-split check for all three cities to confirm 0 orphan rows:

-- Fall River section-split
SELECT COUNT(*) FROM essentials.geofence_boundaries gb
WHERE gb.geo_id = '2522640' AND gb.mtfcc = 'G4110'
  AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.state = 'ma');
-- Expected: 0

-- Medford section-split
SELECT COUNT(*) FROM essentials.geofence_boundaries gb
WHERE gb.geo_id = '2540115' AND gb.mtfcc = 'G4110'
  AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.state = 'ma');
-- Expected: 0

-- Waltham section-split
SELECT COUNT(*) FROM essentials.geofence_boundaries gb
WHERE gb.geo_id = '2573440' AND gb.mtfcc = 'G4110'
  AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.state = 'ma');
-- Expected: 0

-- Medford SC section-split
SELECT COUNT(*) FROM essentials.geofence_boundaries gb
WHERE gb.mtfcc = 'G5420' AND gb.geo_id LIKE '250660%'
  AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.district_type = 'SCHOOL' AND d.state = 'ma');
-- Expected: 0

Document all four results in the SUMMARY.
  </action>
  <verify>
    <automated>python C:/EV-Accounts/backend/scripts/_tmp-waltham-headshots.py — must complete without exceptions; migration 596 must emit: 'Migration 596 post-verification PASSED'; all four section-split checks must return 0</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-waltham-headshots.py runs to completion (0 Python exceptions)
    - 596_waltham_headshots.sql exists and applies without errors
    - Migration 596 post-verification PASSED: v_wrong_type = 0
    - Query: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2573440020 AND -2573440001 AND pi.type != 'default' → 0
    - Every gap has SQL comment with specific reason in 596
    - Section-split check for Fall River geo_id='2522640' → 0
    - Section-split check for Medford geo_id='2540115' → 0
    - Section-split check for Waltham geo_id='2573440' → 0
    - Section-split check for Medford SC G5420 geo_id → 0
    - WALTHAM-02 satisfied: best-effort headshots at 600x750; gaps documented
    - Phase 121 success criteria 5 satisfied: section-split returns 0 rows after all three cities seeded
  </acceptance_criteria>
  <done>Waltham headshot script ran; migration 596 applied; all four section-split checks pass 0; WALTHAM-02 satisfied; Phase 121 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python scripts → external image sources (official city sites, Wikipedia Commons) | Downloading from third-party URLs; image content passively consumed |
| Python scripts → Supabase Storage | Service role key in .env; uploads to politician_photos bucket |
| SQL migrations → Supabase production DB | Storage CDN URLs embedded in politician_images.url column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-05-01 | Information Disclosure | .env SERVICE_KEY | accept | Key stays local; same pattern used in prior phases 119/120 |
| T-121-05-02 | Tampering | politician_images type field | mitigate | All INSERTs use type='default'; post-verification gate checks type != 'default' count = 0; RAISE EXCEPTION on any wrong-type row in each migration |
| T-121-05-03 | Tampering | Schema migrations ledger | mitigate | ON CONFLICT (version) DO NOTHING on all three migrations |
| T-121-05-04 | Spoofing | Image source authenticity | accept | Sources limited to official city sites + Wikipedia Commons; editorial news photos excluded; solo review at execution |
| T-121-05-05 | Information Integrity | Medford SC headshots scope | mitigate | ROSTER for Medford script includes SC elected members; Mayor Lungo-Koehn included once (city external_id only); acceptance criteria confirms no duplicate rows |
| T-121-05-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; reuses Pillow/requests from prior phases |
</threat_model>

<verification>
After all three scripts + migrations applied:

Fall River:
1. SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2522640030 AND -2522640001 AND pi.type = 'default'; -- Expected: upload count from Task 1
2. SELECT COUNT(*) ... AND pi.type != 'default'; -- Expected: 0

Medford:
3. SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2540115020 AND -2540115001 AND pi.type = 'default'; -- Expected: upload count
4. SELECT COUNT(*) ... AND pi.type != 'default'; -- Expected: 0

Waltham:
5. SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2573440020 AND -2573440001 AND pi.type = 'default'; -- Expected: upload count
6. SELECT COUNT(*) ... AND pi.type != 'default'; -- Expected: 0

Section-splits (all cities + Medford SC):
7. Fall River split: 0
8. Medford split: 0
9. Waltham split: 0
10. Medford SC G5420 split: 0
</verification>

<success_criteria>
- All three Python scripts ran to completion without Python exceptions
- Migrations 594, 595, 596 all applied; all post-verifications PASSED
- Zero wrong-type headshots (type != 'default') for any official across all three cities
- All gaps documented with specific HTTP error codes or block reasons
- Section-split returns 0 for all four geofences (Fall River, Medford, Waltham G4110; Medford SC G5420)
- FALLRIV-02, MEDFORD-02, WALTHAM-02 all satisfied
- Phase 121 complete: FALLRIV-01/02, MEDFORD-01/02, WALTHAM-01/02 all closed
</success_criteria>

<output>
Create `.planning/phases/121-fall-river-medford-waltham-deep-seeds/121-05-SUMMARY.md` when done
</output>
