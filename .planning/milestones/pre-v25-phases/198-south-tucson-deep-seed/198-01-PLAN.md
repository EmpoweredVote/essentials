---
phase: 198-south-tucson-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1363_city_of_south_tucson.sql
autonomous: false
requirements: [SUB-04]
must_haves:
  truths:
    - "A greenfield 'City of South Tucson, Arizona, US' government (type='City' — South Tucson is legally a City, matching Tucson 0477000, NOT a Town like Sahuarita/Oro-Valley/Marana; state='AZ', geo_id='0468850') exists — none exists today (RESEARCH-verified live 2026-07-17: 0 rows for '%south tucson%')"
    - "One 'City Council' chamber (official_count=7) holds exactly 7 at-large offices — no separate Mayor chamber/office (Mayor/Vice Mayor/Acting Mayor are TITLES, not seats)"
    - "Exactly ONE NEW essentials.districts row exists for geo_id='0468850' (district_type='LOCAL', mtfcc='G4110', state='az') — it did not exist before (Phase 190 wrote only the G4110 geofence; districts had 0 rows for 0468850, DB-confirmed). There is NO LOCAL_EXEC row (Pitfall 3 — no separately-elected Mayor office to represent)"
    - "ALL 7 offices link to the SAME single LOCAL/G4110/0468850 district (Sahuarita shared-row precedent) — never a bare geo_id join (0468850 collides with the FIPS-keyed geofence_boundaries row)"
    - "D-03 (BLOCKING enclave routing, RESOLVED live 2026-07-17): South Tucson's polygon does NOT overlap Tucson's polygon in AREA (ST_Area(ST_Intersection(0468850, 0477000)) = 0 km² — a true donut hole; ST_Intersects=true is only shared boundary edges) AND a point inside South Tucson is covered EXCLUSIVELY by 0468850, never Tucson's 0477000. A pre-flight DO block in the migration re-asserts the 0-overlap invariant before seeding; the ST_Covers point probe is re-run at execute time"
    - "Roster is Valenzuela(Mayor)/Brown-Dominguez(Vice Mayor)/Robles(Acting Mayor)/Jimenez/Diaz/Flagg/Aguirre; party=NULL on all 7 (nonpartisan/antipartisan); is_appointed=false on all 7 politician rows"
    - "THREE titles are TITLE annotations on 3 of the 7 at-large seats (title='Mayor' on Valenzuela -4015001, title='Vice Mayor' on Brown-Dominguez -4015002, title='Acting Mayor' on Robles -4015003 — plain Sahuarita/Palm-Springs style); the other four carry plain 'Council Member'. Exactly 7 offices, no separate titled seat. role_canonical NULL on all 7"
    - "The current roster is re-verified against a LIVE source at execute time given the July 21 2026 primary (4 days after research; the sitting Mayor Valenzuela is herself an incumbent candidate) AND the separate post-canvass Mayor/Vice-Mayor/Acting-Mayor title re-vote BEFORE the migration is applied; the structural migration is applied to production and registered in the ledger; 7 politician UUIDs captured for Plans 02/03"
    - "D-01: elected officials only (7 elected council members); no appointed staff (City Manager, etc.); antipartisan — party=NULL on all 7, never displayed"
    - "D-02 (as resolved by RESEARCH): at-large + nonpartisan confirmed, but Mayor/Vice-Mayor/Acting-Mayor are council-CHOSEN TITLES with NO separately-elected office — mirrors Sahuarita's shared-LOCAL/no-LOCAL_EXEC shape EXACTLY, extended to a THIRD title gate; type='City' not 'Town' (research overrode the CONTEXT default of a LOCAL_EXEC Mayor seat)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1363_city_of_south_tucson.sql"
      provides: "government (type='City') + City Council chamber + 1 NEW shared LOCAL district (NO LOCAL_EXEC) + 7 politicians (party NULL, is_appointed=false) + 7 offices (Valenzuela Mayor-titled, Brown-Dominguez Vice-Mayor-titled, Robles Acting-Mayor-titled, 4 plain Council Member) + office_id backfill + D-03 enclave pre-flight + geofence pre-flight + post-verify DO gates (three title gates) + ledger registration"
      contains: "City Council"
  key_links:
    - from: "essentials.offices (all 7 incl. the 3 titled)"
      to: "essentials.districts (ONE shared LOCAL / G4110 / 0468850)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='G4110' AND state='az'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'City Council'"
      to: "essentials.governments 'City of South Tucson, Arizona, US'"
      via: "government_id subquery scoped by name"
      pattern: "City of South Tucson, Arizona, US"
---

<objective>
Author and apply the structural migration that seeds the City of South Tucson as a greenfield city
government (its own `essentials.governments` row at geo_id `0468850`, type='City'), a single `City Council`
chamber (`official_count=7`), the ONE district row all 7 offices attach to, and 7 politicians/offices
(ext_id `-4015001..-4015007`): a 7-member at-large nonpartisan council on which the Mayor, Vice Mayor, AND
Acting Mayor are TITLE annotations (not separate seats). Greenfield — no City of South Tucson government row
exists today (RESEARCH-verified live 2026-07-17: `SELECT ... WHERE name ILIKE '%south tucson%'` => 0 rows).

CRITICAL STRUCTURAL SHAPE (mirrors Sahuarita 197 EXACTLY, extended by one title). RESEARCH confirmed (3+
independent WebSearch composites tracing to southtucsonaz.gov's own Elections/City-Council pages, plus
Tucson Spotlight Nov-2024 swearing-in + AZ Luminaria Jun-2026 voter guide) that South Tucson's Mayor, Vice
Mayor, AND Acting Mayor are NOT separately elected — they are TITLES the 7-member council chooses from among
its own membership. Therefore this migration reuses Sahuarita's `1354_town_of_sahuarita.sql` STRUCTURE
near-verbatim: the "all at-large members share ONE LOCAL district row" DB shape, with NO LOCAL_EXEC row (no
separately-elected office), PLUS the Palm-Springs/Sahuarita title-on-seat modeling — the three titles live
purely in the `title` string with `role_canonical` NULL. Exactly TWO deltas from Sahuarita: (1) a THIRD
title-assertion gate (Acting Mayor) copied verbatim from the Mayor/Vice-Mayor gate shape; (2)
`governments.type='City'` not `'Town'` (South Tucson is legally a City — every source + the live Tucson
0477000 `type='City'` row). Pitfall 3: creating a `LOCAL_EXEC` row here would be a phantom seat with no
electoral basis — DO NOT create one. South Tucson has NO wards and NO council districts: NO geofence loader,
NO ArcGIS query, NO new X00xx mtfcc. The whole-city boundary geofence `0468850` (G4110, FIPS 04-68850) is
already live from Phase 190, but `essentials.districts` has ZERO rows for it (DB-confirmed this session).
This migration creates exactly ONE NEW `essentials.districts` row for `0468850` (`district_type='LOCAL'`,
`mtfcc='G4110'`, `state='az'` lowercase), shared by ALL 7 offices.

CRITICAL (D-03 enclave routing — the FIRST enclave/wholly-surrounded jurisdiction in this milestone): South
Tucson sits entirely inside the City of Tucson's footprint. RESEARCH resolved this BLOCKING check live
2026-07-17 with HIGH confidence: `ST_Area(ST_Intersection(0468850, 0477000))` = 0 km² (a true topological
donut hole — Tucson's polygon has South Tucson cut out of it, not drawn over it; `ST_Intersects=true` is
merely shared boundary edges, the CORRECT safe signature), and a `ST_Covers` point test on the South Tucson
centroid AND the approximate City-Hall-area point is covered EXCLUSIVELY by the South Tucson geofence,
never by Tucson's. This migration must carry a pre-flight DO block that RE-ASSERTS the 0-overlap invariant
(RAISE EXCEPTION if intersection area > ~0.001 km²) BEFORE seeding, and Task 3 re-runs the ST_Covers point
probe. A non-zero overlap would indicate a TIGER data change requiring escalation. Do NOT use the mailing
address city string ("Tucson, AZ 85713" — South Tucson has no distinct USPS post office) as a jurisdiction
proxy; the geo_id/geofence is authoritative.

CRITICAL (nonpartisan / titles): (1) party=NULL on all 7 — South Tucson runs NONPARTISAN elections (AZ
Luminaria: "South Tucson elections are nonpartisan"); do NOT record a party. (2) Use PLAIN Sahuarita-style
titles — `'Mayor'` on Valenzuela, `'Vice Mayor'` on Brown-Dominguez, `'Acting Mayor'` on Robles, `'Council
Member'` on the other four. (3) None of the three titles is a separate seat — exactly 7 offices total;
`role_canonical` stays NULL on all 7.

CRITICAL (the #1 risk of this phase — the tightest research-to-election window in the milestone, layered
and sharper than Sahuarita's): South Tucson's July 21, 2026 municipal primary is 4 days after the
2026-07-17 research date (the execute date may land ON or AFTER it). Three seats are up (Valenzuela's,
Flagg's, Aguirre's) — and uniquely, the sitting MAYOR (Roxanna Valenzuela) is herself one of the three
incumbent candidates. Even a "no membership change" primary does not resolve who holds the
Mayor/Vice-Mayor/Acting-Mayor titles — those are a SEPARATE, SUBSEQUENT council organizational vote after
the canvass. Task 2 is a SUBSTANTIVE, non-perfunctory BLOCKING roster-currency re-verification that
independently confirms BOTH the membership question AND all THREE title-holder questions against a live
source BEFORE apply — a genuine multi-stage decision point, not a copy-paste formality.

Purpose: Satisfies the government/roster half of SUB-04 and ROADMAP success criterion #1 (govt + council
roster seeded, election method verified). The existing G4110→LOCAL routing map in `essentialsService.ts`
covers all 7 at-large seats with NO code change (no LOCAL_EXEC join is exercised at all this phase, and the
enclave donut-hole geometry precludes any double-match with Tucson), so applying this migration makes every
South Tucson address resolve to all 7 council members. Plans 02 (headshots) and 03 (stances) consume the 7
UUIDs captured here.
Output: `C:/EV-Accounts/backend/migrations/1363_city_of_south_tucson.sql` (structural, registered), applied
to production, with the 7 UUIDs recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md
@.planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md
@.planning/phases/198-south-tucson-deep-seed/198-CONTEXT.md
@.planning/phases/198-south-tucson-deep-seed/198-VALIDATION.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB access. Task 1 (author the `.sql` file) is the only executor file
action. Task 2 (SUBSTANTIVE BLOCKING roster-currency + three-title re-verify + enclave ST_Covers re-probe +
apply + UUID capture) and Task 3 (post-apply assertions) are ORCHESTRATOR-RUN via `psql -f`/`psql -tAc`
against `C:/EV-Accounts/backend/.env` `DATABASE_URL`. A migration authored but not applied is a false
positive — the apply MUST happen (and its post-verify DO gate MUST pass) before Plans 02/03. All backend
files live in the SEPARATE `C:/EV-Accounts` repo (`master`→Render) — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (live-verified in 198-RESEARCH.md / 198-PATTERNS.md; the closest analog is Sahuarita's OWN
applied migration `1354_town_of_sahuarita.sql` — a near-verbatim structural copy, extended to a 3rd title
gate + type='City'; no codebase exploration needed):
- essentials.governments (id, name, type, state, city, geo_id): insert 'City of South Tucson, Arizona, US',
  type='City' (South Tucson is legally a City — confirm against the live Tucson 0477000 `type='City'` row at
  author time; this is the delta from Sahuarita's 'Town'), state='AZ' (UPPERCASE free-text), city='South
  Tucson', geo_id='0468850'. Greenfield. Idempotent WHERE NOT EXISTS on name. governments has NO unique
  constraint on geo_id — guard on name.
- essentials.chambers (id, name, name_formal, government_id, official_count): name='City Council',
  name_formal='South Tucson City Council', official_count=7, government_id via name subquery. The
  auto-generated `slug` column must NEVER appear in the INSERT column list. Idempotent WHERE NOT EXISTS on
  (name, government_id).
- essentials.districts (id, district_type, state, geo_id, label, mtfcc) — exactly ONE NEW row reusing the
  ALREADY-LIVE Phase-190 geofence (NO geometry work): district_type='LOCAL', state='az' (lowercase),
  geo_id='0468850', label='City of South Tucson (At-Large)', mtfcc='G4110'. Idempotent WHERE NOT EXISTS on
  (geo_id, district_type, state). ALL 7 offices share THIS SINGLE row. NO LOCAL_EXEC row (Pitfall 3). Use
  `label`, NOT name_formal (no such column).
- essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed,
  is_vacant, is_incumbent, external_id, office_id): ext_id -4015001..-4015007 (DB-confirmed unused
  2026-07-17: `-4015010..-4015000` = 0 rows); external_id UNIQUE — use ON CONFLICT (external_id) DO NOTHING
  (greenfield — no pre-existing rows to update, unlike Marana's DO UPDATE). party=NULL on ALL 7
  (nonpartisan). is_active=true, is_incumbent=true, is_vacant=false, is_appointed=false on all 7.
- essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state,
  is_appointed_position, is_vacant, role_canonical): representing_state='AZ' (UPPERCASE),
  is_appointed_position=false, role_canonical=NULL on all 7. Guard on (district_id, politician_id).
- ext_id → name → title (RESEARCH roster, 2026-07-17 — RE-VERIFY at Task 2; primary 4 days out, sitting
  Mayor is a candidate, title re-vote is a separate event):
    -4015001 Roxanna Valenzuela       title='Mayor'          (LOCAL / G4110 / 0468850)  [seat up 2026; incumbent candidate; Mayor title re-chosen post-canvass]
    -4015002 Melissa Brown-Dominguez  title='Vice Mayor'     (LOCAL / G4110 / 0468850)  [term to 2028; not up]
    -4015003 Pablo Robles             title='Acting Mayor'   (LOCAL / G4110 / 0468850)  [term to 2028; not up]
    -4015004 Dulce Jimenez            title='Council Member' (LOCAL / G4110 / 0468850)  [term to 2028]
    -4015005 Paul Diaz                title='Council Member' (LOCAL / G4110 / 0468850)  [term to 2028; former Mayor pre-Nov-2024]
    -4015006 Brian Flagg              title='Council Member' (LOCAL / G4110 / 0468850)  [seat up 2026; incumbent candidate]
    -4015007 Cesar Aguirre            title='Council Member' (LOCAL / G4110 / 0468850)  [seat up 2026; incumbent candidate]
- CRITICAL geo_id collision guard: 0468850 also appears in geofence_boundaries (state stored as FIPS '04',
  an unrelated convention). Every office↔district join MUST scope by district_type='LOCAL' + mtfcc='G4110'
  + state='az', never a bare geo_id.
- D-03 enclave invariant (RESOLVED live 2026-07-17; re-assert): `ST_Area(ST_Intersection(st.geometry,
  tuc.geometry)::geography)/1e6` for st.geo_id='0468850' and tuc.geo_id='0477000' = 0 (~0). A `ST_Covers`
  point test on the South Tucson centroid (approx -110.9687, 32.1948) resolves to 'South Tucson city' and
  NULL for Tucson.
- Sahuarita 1354 is the copy-verbatim template for "exactly 1 office titled X bound to external_id Y" — its
  gates (e)/(f) triplicated here (Mayor -4015001, Vice Mayor -4015002, Acting Mayor -4015003), re-scoped to
  the single shared row (d.geo_id='0468850' AND d.mtfcc='G4110' AND d.state='az').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the structural City of South Tucson migration (shared LOCAL, NO LOCAL_EXEC, three titles as annotations, type='City', enclave pre-flight)</name>
  <files>C:/EV-Accounts/backend/migrations/1363_city_of_south_tucson.sql</files>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md (the full structural pattern: the near-verbatim Sahuarita 1354 copy with exactly two deltas — a THIRD title gate (Acting Mayor) + type='City'; the header/CRITICAL-note block to copy and adapt; the D-03 enclave pre-flight DO block sourced from RESEARCH Pattern 2; the government/chamber/district/office/backfill/gate/ledger shapes; the plain 'Mayor'/'Vice Mayor'/'Acting Mayor'/'Council Member' titles; ON CONFLICT DO NOTHING for greenfield)
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Summary roster table; § Pattern 1 three-title model + § Anti-Patterns to Avoid — NO LOCAL_EXEC, nonpartisan party NULL, type='City' not 'Town', Historic-4th-Ave vs South-4th-Ave confusion; § Common Pitfalls 3, 5, 6; § Pattern 2 enclave ST_Area(ST_Intersection) check; § Code Examples — the greenfield/geofence/ext_id/ledger/enclave probes to re-run at execute time)
    - C:/EV-Accounts/backend/migrations/1354_town_of_sahuarita.sql (PRIMARY analog — read in full: government/chamber/district/office INSERT shapes, pre-flight + post-verify DO gates incl. the exactly-one-Mayor and exactly-one-Vice-Mayor gates (e)/(f), the office_id backfill, ledger footer. Copy the STRUCTURE near-verbatim, substituting South Tucson facts, and TRIPLICATE gate (e)/(f) to a third gate for title='Acting Mayor' on -4015003)
  </read_first>
  <action>
    First run `ls C:/EV-Accounts/backend/migrations | sort -n | tail -5` to confirm on-disk MAX (RESEARCH
    computed disk MAX=1362, ledger MAX=1354, next structural=1363 on 2026-07-17 — Pitfall 5 documents a real
    ledger-vs-disk drift, so RE-CHECK now). Name this file actual-disk-MAX+1 (provisionally
    `1363_city_of_south_tucson.sql`). If disk MAX drifted, use actual disk-MAX+1 and note the deviation in a
    header comment and the SUMMARY. Also re-run the ext_id collision probe (`SELECT external_id FROM
    essentials.politicians WHERE external_id BETWEEN -4015010 AND -4015000` => expect 0 rows).

    Author a STRUCTURAL migration wrapped in BEGIN/COMMIT. Header comment discipline (copy 1354's shape,
    substitute South Tucson facts): the geo_id-collision CRITICAL note with `'0468850'`; the districts.state
    must be 'az' lowercase note; governments.state='AZ' / offices.representing_state='AZ' uppercase note;
    at-large / one shared LOCAL row note; party=NULL nonpartisan note; a CRITICAL/DEVIATION note that (a)
    there is NO LOCAL_EXEC row and NO separately-elected Mayor — Mayor, Vice Mayor, AND Acting Mayor are
    council-chosen TITLES modeled purely via the `title` string (Sahuarita Pattern 1, extended to a third
    title), (b) this OVERRODE the CONTEXT D-02 default expectation of a LOCAL_EXEC Mayor seat, and (c)
    governments.type='City' (NOT 'Town') because South Tucson is legally incorporated as a City — confirm
    against the live Tucson 0477000 row at author time.

    D-03 ENCLAVE PRE-FLIGHT DO block (novel this phase — copy from RESEARCH Pattern 2 / Code Examples;
    place it BEFORE the geofence-existence pre-flight): SELECT
    `ST_Area(ST_Intersection(st.geometry, tuc.geometry)::geography)/1e6` INTO a numeric var for
    st.geo_id='0468850' and tuc.geo_id='0477000'; RAISE EXCEPTION 'D-03 BLOCKING enclave check FAILED:
    South Tucson/Tucson intersection area = % sqkm (expected ~0, a clean donut hole) — do NOT proceed' if
    the value IS NULL OR > 0.001. Document inline that this is the milestone's first enclave check.

    Geofence pre-flight DO block: assert the whole-city geofence exists — COUNT(*) >= 1 in
    essentials.geofence_boundaries WHERE geo_id='0468850' AND mtfcc='G4110' — RAISE EXCEPTION 'South Tucson
    G4110 geofence missing — run Phase 190 first' otherwise. Add the standard idempotency hard-abort guard:
    RAISE EXCEPTION 'Migration 1363 already applied — aborting re-run' if the 'City of South Tucson, Arizona,
    US' government already exists.

    Insert the greenfield government 'City of South Tucson, Arizona, US' (type='City', state='AZ' uppercase,
    city='South Tucson', geo_id='0468850') idempotently (WHERE NOT EXISTS on name). Insert the 'City Council'
    chamber (name_formal='South Tucson City Council', official_count=7) scoped to that government's id via
    name subquery, idempotent on (name, government_id). Do NOT list the auto-generated `slug` column.

    Insert exactly ONE NEW district row reusing the existing Phase-190 geofence (NO geometry work):
    district_type='LOCAL', state='az' lowercase, geo_id='0468850', label='City of South Tucson (At-Large)',
    mtfcc='G4110', idempotent WHERE NOT EXISTS on (geo_id, district_type, state). ALL 7 offices share THIS
    SINGLE row. DO NOT insert a LOCAL_EXEC row anywhere (Pitfall 3 — a phantom seat). CRITICAL:
    mtfcc='G4110'.

    Seed 7 officials using the WITH ins_p AS (INSERT INTO politicians ... ON CONFLICT (external_id) DO
    NOTHING RETURNING id) INSERT INTO offices ... FROM districts d CROSS JOIN ins_p p WHERE ... block, one
    per official. EVERY office joins the SAME single LOCAL row: d.geo_id='0468850' AND
    d.district_type='LOCAL' AND d.state='az' AND d.mtfcc='G4110'. Titles:
      - Valenzuela (-4015001) → title='Mayor'
      - Brown-Dominguez (-4015002) → title='Vice Mayor'
      - Robles (-4015003) → title='Acting Mayor'
      - Jimenez (-4015004) → title='Council Member'
      - Diaz (-4015005) → title='Council Member'
      - Flagg (-4015006) → title='Council Member'
      - Aguirre (-4015007) → title='Council Member'
    All 7 offices: representing_state='AZ' uppercase, is_appointed_position=false, role_canonical=NULL. Each
    join scopes district_type='LOCAL' + mtfcc='G4110' + state='az' plus geo_id — NEVER a bare geo_id. Guard
    each office INSERT on NOT EXISTS (... o.district_id=d.id AND o.politician_id=p.id). Politicians:
    party=NULL on ALL 7 (nonpartisan — do NOT record a party value), is_active=true, is_incumbent=true,
    is_vacant=false, is_appointed=false on all 7. Because greenfield, use ON CONFLICT (external_id) DO
    NOTHING (NOT Marana's DO UPDATE).

    office_id backfill: UPDATE politicians SET office_id=o.id FROM offices o WHERE o.politician_id=p.id AND
    p.external_id IN (-4015001,-4015002,-4015003,-4015004,-4015005,-4015006,-4015007) AND p.office_id IS
    NULL.

    Post-verify DO gate (RAISE EXCEPTION → rollback unless ALL hold): (a) exactly 1 government named 'City of
    South Tucson, Arizona, US'; (b) exactly 7 offices under the 'City Council' chamber, ALL on the ONE
    LOCAL/G4110/0468850 district; (c) the LOCAL district holds exactly 7 offices AND there is NO LOCAL_EXEC
    district for 0468850; (d) all 7 politicians have party IS NULL (nonpartisan gate) AND is_appointed=false;
    (e) EXACTLY 1 office has title='Mayor' AND its politician has external_id=-4015001 (Valenzuela); (f)
    EXACTLY 1 office has title='Vice Mayor' AND its politician has external_id=-4015002 (Brown-Dominguez);
    (i) [NEW third gate] EXACTLY 1 office has title='Acting Mayor' AND its politician has
    external_id=-4015003 (Robles) — no other office may carry any of the three titles; (g) section-split: 0
    offices attached under any government OTHER than City of South Tucson via this district; (h) office_id
    backfill: 0 NULLs remaining across the 7 external_ids.

    Register OUTSIDE BEGIN/COMMIT: INSERT INTO supabase_migrations.schema_migrations (version) VALUES
    ('1363') ON CONFLICT (version) DO NOTHING (substitute actual disk-MAX+1; check whether the live
    convention is the 1-column or 2-column form at author time). Do NOT invent politician UUIDs — offices
    resolve politician_id via the ins_p CTE at apply time.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1363_city_of_south_tucson.sql"; test -f "$F" && grep -qF "City Council" "$F" && grep -qF "City of South Tucson, Arizona, US" "$F" && grep -qF "'City'" "$F" && grep -qF "'LOCAL'" "$F" && ! (grep -v '^[[:space:]]*--' "$F" | grep -qF "'LOCAL_EXEC'") && grep -qF "G4110" "$F" && grep -qF "'0468850'" "$F" && grep -qF "'0477000'" "$F" && grep -qF "ST_Intersection" "$F" && grep -qF "'Mayor'" "$F" && grep -qF "'Vice Mayor'" "$F" && grep -qF "'Acting Mayor'" "$F" && [ "$(grep -c "'Council Member'" "$F")" -ge 4 ] && ! grep -qiE "party[^a-z]*=[^a-z]*'(Democratic|Republican)" "$F" && [ "$(grep -c 'o.district_id = d.id AND o.politician_id = p.id' "$F")" = "7" ] && grep -qF "ON CONFLICT (external_id) DO NOTHING" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1363; deviation noted if disk MAX drifted).
    - Grep confirms `City Council` and `City of South Tucson, Arizona, US` present; government type='City' (the delta from Sahuarita — confirmed against live Tucson 0477000), city='South Tucson', geo_id='0468850'.
    - D-03 enclave pre-flight present: `ST_Intersection` between 0468850 and 0477000 with a RAISE EXCEPTION when overlap area > ~0 (both geo_ids referenced).
    - Grep confirms a NEW `LOCAL` districts INSERT with geo_id='0468850', mtfcc='G4110', state='az' — and NO `'LOCAL_EXEC'` district_type literal on any non-comment line (Pitfall 3; the gate strips `--` comment lines so a header note documenting the absence is fine).
    - Grep count of the office↔district guard `o.district_id = d.id AND o.politician_id = p.id` is exactly 7; all 7 join the SAME LOCAL/G4110/0468850 row — never a bare geo_id.
    - Nonpartisan: NO politician row records a 'Democratic'/'Republican' party; `role_canonical` NULL for all 7; is_appointed=false on all 7 politician rows; ON CONFLICT (external_id) DO NOTHING (greenfield, not DO UPDATE).
    - Title modeling: exactly one office title='Mayor' (Valenzuela -4015001), one title='Vice Mayor' (Brown-Dominguez -4015002), one title='Acting Mayor' (Robles -4015003), four plain 'Council Member'; no separate titled seat (7 offices total).
    - Header carries the CRITICAL notes: NO LOCAL_EXEC / no separately-elected Mayor (three council-chosen titles), overriding CONTEXT D-02; and type='City' rationale.
    - Grep confirms 7 external_id literals -4015001..-4015007.
    - Geofence pre-flight DO block asserts the G4110/0468850 geofence exists; post-verify DO gate present with assertions (a-i) — including LOCAL holds exactly 7, NO LOCAL_EXEC row, party-NULL gate, exactly-one-Mayor on -4015001, exactly-one-Vice-Mayor on -4015002, exactly-one-Acting-Mayor on -4015003.
    - Ledger-registration line `('1363'` (or actual number) present OUTSIDE COMMIT.
  </acceptance_criteria>
  <done>Structural migration authored: greenfield City of South Tucson government (type='City') + City Council chamber + 1 NEW shared LOCAL/G4110 district (NO LOCAL_EXEC) + 7 at-large offices/politicians (party NULL, is_appointed=false; Valenzuela Mayor-titled, Brown-Dominguez Vice-Mayor-titled, Robles Acting-Mayor-titled, four plain 'Council Member'), backfill, D-03 enclave + geofence pre-flights, post-verify gates (incl. three exactly-one-title gates + no-LOCAL_EXEC), ledger registration — ready to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] SUBSTANTIVE roster + Mayor/Vice-Mayor/Acting-Mayor title re-verification (July 21 2026 primary — 4 days out, sitting Mayor is a candidate; titles re-voted post-canvass) + enclave ST_Covers re-probe + apply structural migration (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Summary — the full roster table + the three-title council-chosen mechanism; § Common Pitfall 1 (4-day window, sitting Mayor a candidate), Pitfall 2 (majority-vs-plurality win-threshold unresolved), Pitfall 4 (southtucsonaz.gov Cloudflare block — use Playwright to render the roster/elections page); § Assumptions Log A1/A2; § Open Questions 1 & 2; § Code Examples — the enclave ST_Covers probe to re-run)
    - .planning/phases/198-south-tucson-deep-seed/198-VALIDATION.md (Manual-Only Verifications — the roster-currency test: BLOCKING re-verify of BOTH membership AND all three current title holders; and the enclave-routing probe as a BLOCKING pre-seed check)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. This is NOT a copy-paste formality — it is the genuine #1 risk of this phase and the tightest research-to-election window in the milestone, layered with a scheduled post-canvass THREE-title re-vote and the unique fact that the sitting Mayor is herself on the ballot. The operator: (1) re-runs the D-03 enclave ST_Covers point probe (a known in-South-Tucson address routes to South Tucson EXCLUSIVELY, not Tucson) — because southtucsonaz.gov is Cloudflare-JS-challenge-blocked, use Playwright (not curl/WebFetch) to render the roster/elections page; (2) re-confirms BOTH the 7-person council membership AND the current Mayor / Vice Mayor / Acting Mayor title holders against a LIVE source, determines whether the July 21 2026 primary has occurred/been certified AND whether the post-canvass council organizational vote has re-assigned the three titles, and — if anything changed — explicitly decides with the operator whom to seed and which titles to place for each affected seat; only then does the ORCHESTRATOR apply migration 1363 (its in-transaction post-verify DO gate MUST pass — a RAISE EXCEPTION rolls the whole thing back) and capture the 7 UUIDs. See &lt;how-to-verify&gt;.</action>
  <what-built>
    Migration 1363 (Task 1) is authored but NOT applied. RESEARCH (2026-07-17) documented the current
    7-person roster (Valenzuela Mayor; Brown-Dominguez Vice Mayor; Robles Acting Mayor; Jimenez; Diaz;
    Flagg; Aguirre) as valid for effectively 0-4 days because South Tucson's July 21, 2026 municipal primary
    is 4 days from the research date. Three seats are up (Valenzuela, Flagg, Aguirre), and the sitting MAYOR
    (Valenzuela) is herself an incumbent candidate. AND the three TITLES (Mayor/Vice Mayor/Acting Mayor) are
    re-chosen by the newly-seated council at the first meeting after the canvass — a SEPARATE event from the
    election. Applying to production (writes are LIVE prod) without a fresh multi-stage re-check risks
    seeding a roster or title assignment that a certified primary + post-canvass vote has already superseded
    (data-honesty threat). Additionally, this is the milestone's first enclave jurisdiction — the D-03
    ST_Covers routing probe is re-run here as a live pre-apply confirmation.
  </what-built>
  <how-to-verify>
    1. Re-run the D-03 enclave routing probe against live PostGIS (RESEARCH Code Examples): confirm
       `ST_Area(ST_Intersection(0468850, 0477000))` is ~0 AND `ST_Covers` on a known in-South-Tucson point
       (the centroid approx -110.9687, 32.1948, or the City-Hall-area point) resolves to 'South Tucson city'
       EXCLUSIVELY and NULL for Tucson (0477000). If overlap is non-zero or the point double-matches, STOP —
       a TIGER data change needs escalation before seeding.
    2. Establish the authoritative roster facts from a LIVE source. southtucsonaz.gov IS Cloudflare-JS-
       challenge-blocked to curl/WebFetch (HTTP 403 "Just a moment...") — use the Playwright browser flow to
       render `/citycouncil` (and `/directory` / the elections page) and read the actual roster + title
       language. Corroborate against the confirmed-reachable outlets (azluminaria.org, tucsonspotlight.org,
       tucson.com, tucsonsentinel.com, kgun9.com) plus Ballotpedia candidate pages. Confirm the July 21, 2026
       primary date and whether a November general applies.
    3. Determine (a) whether the primary has OCCURRED and whether results are CERTIFIED as of the execute
       date, AND (b) whether the newly-seated council's post-canvass organizational meeting has HELD its
       Mayor/Vice-Mayor/Acting-Mayor re-vote. Given the 4-day window, treat "the primary has already
       happened" as the LIKELY branch; the three-title re-vote may still be pending even after certification.
    4. Decide the roster AND the three titles to seed WITH THE OPERATOR:
       - If the primary has NOT occurred / is not certified AND no new title vote has happened → seed the
         current 7-person roster with Valenzuela=Mayor, Brown-Dominguez=Vice Mayor, Robles=Acting Mayor as
         documented in RESEARCH — these represent residents today.
       - If a certified primary changed any officeholder (Valenzuela's, Flagg's, and/or Aguirre's seats
         could each change) → explicitly decide with the operator whether to seed the outgoing
         (still-sitting until the term rolls over) or the newly-certified incoming member for each affected
         seat, and patch the affected politician rows in 1363 BEFORE apply.
       - If the post-canvass title re-vote has occurred and moved any of the three titles → move the
         `title='Mayor'` / `title='Vice Mayor'` / `title='Acting Mayor'` annotation to the correct sitting
         member and UPDATE the post-verify gate's expected external_ids (gates (e)/(f)/(i)) accordingly.
         Never assume "Valenzuela stays Mayor" without confirming the actual vote — she is on the ballot.
    5. On approval, the ORCHESTRATOR applies via
       `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1363_city_of_south_tucson.sql`, confirms
       the in-transaction post-verify DO gate passed (COMMIT, no RAISE EXCEPTION), captures the 7 UUIDs
       (`SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id IN
       (-4015001,-4015002,-4015003,-4015004,-4015005,-4015006,-4015007) ORDER BY external_id`) into
       198-01-SUMMARY.md for Plans 02/03, records the execute-date roster + three-title decision (before/after
       primary, before/after the post-canvass title vote, what was seeded and why, per-seat), and commits
       via `git -C "C:/EV-Accounts"`.
  </how-to-verify>
  <acceptance_criteria>
    - D-03 enclave routing re-confirmed at execute time: intersection area ~0 AND the ST_Covers point probe resolves EXCLUSIVELY to South Tucson (0468850), NULL for Tucson (0477000); recorded in 198-01-SUMMARY.md.
    - The authoritative primary date + certification status AND the status of the post-canvass three-title re-vote are established from a LIVE source (Playwright-rendered southtucsonaz.gov + corroborating outlets), not assumed from RESEARCH, and recorded in 198-01-SUMMARY.md.
    - BOTH the seeded membership AND all three title holders (Mayor/Vice Mayor/Acting Mayor) are explicitly decided with the operator: current 7 + Valenzuela/Brown-Dominguez/Robles titles if pre/uncertified, or the operator-chosen members + confirmed title holders per seat if a certified result and/or post-canvass vote changed anything.
    - Migration applies with the in-transaction post-verify DO gate passing (no RAISE EXCEPTION → COMMIT), including party-NULL, LOCAL-holds-7, no-LOCAL_EXEC, and exactly-one-Mayor / exactly-one-Vice-Mayor / exactly-one-Acting-Mayor on the confirmed holders.
    - `psql -tAc "SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1363'"` returns 1 (structural registered; substitute actual number if disk MAX drifted).
    - The 7 politician UUIDs captured into 198-01-SUMMARY.md (external_id → UUID → full_name) for Plans 02/03; migration committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the enclave routing is re-confirmed, the live roster AND all three title holders + election dates + certification status + post-canvass-title-vote status are confirmed, the seed roster and three title placements are explicitly decided per seat, and the migration is applied + gate-passed + UUIDs captured — or describe the roster/title/enclave delta to patch first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply structural assertions (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB assertions, no files modified</files>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-VALIDATION.md (Per-Task Verification Map + Wave 0 requirements — geo_id 0468850 resolves; greenfield; 1 govt + 1 chamber + 7 offices, party NULL, three title annotations, scoped joins, 0 section-split, enclave ST_Covers)
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Common Pitfall 3 — the ONE NEW LOCAL row, NO LOCAL_EXEC; § Security Domain — section-split + wrong-district-attach + phantom-LOCAL_EXEC + enclave-double-match mitigations)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). After apply, run seed assertions via `psql "$DATABASE_URL"
    -tAc` and record in 198-01-SUMMARY.md. Assert: (a) exactly 1 government 'City of South Tucson, Arizona,
    US' at geo_id='0468850' with type='City'; (b) exactly 7 offices under the 'City Council' chamber of that
    government, ALL on the ONE LOCAL/G4110/0468850 district; (c) the LOCAL district holds exactly 7 offices
    AND there is NO LOCAL_EXEC district row for geo_id='0468850'; (d) exactly ONE new districts row for
    0468850 (LOCAL, G4110, az); (e) all 7 politicians have party IS NULL AND is_appointed=false; (f) DISTINCT
    district state on the linked district = only 'az' (lowercase); (g) section-split: 0 offices reachable
    from this district under any government other than City of South Tucson; (h) exactly 1 office
    title='Mayor' (-4015001 / Task-2 holder), exactly 1 title='Vice Mayor' (-4015002 / Task-2 holder), and
    exactly 1 title='Acting Mayor' (-4015003 / Task-2 holder), and no other office carries any of the three
    titles; (i) enclave: ST_Area(ST_Intersection(0468850, 0477000)) ~0 AND a known South Tucson point is
    covered by 0468850 only. Any mismatch blocks the phase — do NOT proceed to Plan 02.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='City of South Tucson, Arizona, US' AND geo_id='0468850' AND type='City')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0468850' AND d.state='az' AND d.mtfcc='G4110' AND d.district_type='LOCAL')=7 AND (SELECT count(*) FROM essentials.districts WHERE geo_id='0468850' AND district_type='LOCAL_EXEC')=0 AND (SELECT count(*) FROM essentials.districts WHERE geo_id='0468850' AND district_type='LOCAL' AND state='az' AND mtfcc='G4110')=1 AND NOT EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id IN (-4015001,-4015002,-4015003,-4015004,-4015005,-4015006,-4015007) AND (party IS NOT NULL OR is_appointed IS TRUE)) AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0468850' AND d.mtfcc='G4110' AND d.state='az' AND o.title='Mayor')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0468850' AND d.mtfcc='G4110' AND d.state='az' AND o.title='Vice Mayor')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0468850' AND d.mtfcc='G4110' AND d.state='az' AND o.title='Acting Mayor')=1 AND (SELECT ST_Area(ST_Intersection(st.geometry, tuc.geometry)::geography)/1e6 FROM essentials.geofence_boundaries st, essentials.geofence_boundaries tuc WHERE st.geo_id='0468850' AND tuc.geo_id='0477000') < 0.001"</automated>
  </verify>
  <acceptance_criteria>
    - Greenfield City of South Tucson government count = 1 (geo_id 0468850, type='City').
    - 7 offices under 'City Council', ALL on the ONE LOCAL/G4110/0468850 row.
    - LOCAL district holds exactly 7 offices; there are ZERO LOCAL_EXEC districts for 0468850 (Pitfall 3).
    - Exactly one new districts row for 0468850 (LOCAL/G4110/az).
    - party IS NULL AND is_appointed=false for all 7 politicians (nonpartisan).
    - DISTINCT linked-district state = only 'az'.
    - Section-split query returns 0 rows (no office leaked under a non-South-Tucson government).
    - Exactly 1 office title='Mayor' (-4015001 / Task-2 holder), 1 title='Vice Mayor' (-4015002 / Task-2 holder), 1 title='Acting Mayor' (-4015003 / Task-2 holder); no other office carries any of the three.
    - Enclave invariant holds: ST_Area(ST_Intersection(0468850, 0477000)) < 0.001 km².
    - The combined boolean SELECT returns `t`.
  </acceptance_criteria>
  <done>Live production confirms the greenfield City of South Tucson government (type='City'), 7 at-large offices on the ONE shared LOCAL district (no LOCAL_EXEC), party NULL / is_appointed false, exactly one Mayor + one Vice Mayor + one Acting Mayor title annotation, lowercase 'az' casing, clean section-split, and the enclave donut-hole invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | Hand-authored static seed SQL crosses into live `essentials` via `psql -f` (service-role) |
| Phase-190 G4110/0468850 geofence → new offices | The 7 new office↔district joins consume the existing whole-city geofence; wrong casing/type or a bare-geo_id collision (0468850 also exists FIPS-keyed) silently mislinks |
| Tucson (0477000) enclosing geofence → South Tucson routing | South Tucson is wholly inside Tucson; an enclave overlap would let a South Tucson address double-match or be swallowed by Tucson's council |
| active 2026 election + post-canvass three-title vote → seeded roster | The July 21 2026 primary (4 days from research; likely past at execute) can supersede membership; the sitting Mayor is a candidate; the three titles are re-voted after the canvass (a separate event) |

## STRIDE Threat Register

Honest scope: a static data-seed migration authored by the operator — no new untrusted input, auth, or
network endpoint (data-seeding phase, no runtime/request surface). Genuine threats are data-integrity
(correct linkage / current roster / correct titles / clean enclave routing), not injection or access
control. No HIGH-severity threat exists; none is fabricated. Mirrors the Sahuarita 197 threat model,
extended with one enclave-specific integrity threat.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-01 | Tampering | office↔chamber↔government section-split | mitigate | Chamber scoped to the South Tucson government id subquery; post-verify DO gate (g) + Task 3 section-split assertion → 0 rows or the transaction rolls back |
| T-198-02 | Tampering | office↔district WHERE vs the 0468850 multi-use geo_id | mitigate | Every join scopes district_type='LOCAL' + mtfcc='G4110' + state='az'; migration never joins a bare geo_id; post-verify asserts 7 LOCAL offices on the one row |
| T-198-03 | Tampering (data integrity) | phantom LOCAL_EXEC row (misrepresenting a non-existent separately-elected Mayor office) | mitigate | This migration creates NO LOCAL_EXEC row (Pitfall 3 / Pattern 1); post-verify gate (c) + Task 3 assert 0 LOCAL_EXEC districts for 0468850 and all 7 on the single LOCAL row |
| T-198-ENCLAVE | Tampering (data integrity) | enclave double-match — a South Tucson address silently resolving to Tucson's council instead of/in addition to South Tucson's | mitigate | D-03 BLOCKING pre-flight DO block asserts ST_Area(ST_Intersection(0468850, 0477000))=0 before seeding; Task 2 re-runs the ST_Covers point probe; Task 3 re-asserts the 0-overlap invariant |
| T-198-04 | Tampering (data-honesty) | seeding a roster or title assignment superseded by the July 21 2026 primary and/or the post-canvass three-title re-vote — the #1 risk | mitigate | Task 2 is a SUBSTANTIVE BLOCKING multi-stage re-verify against a LIVE source (membership AND all three current title holders) + certified-primary + post-canvass-vote check + explicit per-seat/per-title operator decision; execute-date decision recorded in the SUMMARY |
| T-198-TITLE | Tampering | Mayor/Vice-Mayor/Acting-Mayor title modeling (double-count / mislabel / stale annotation) | mitigate | Title-only model: exactly 7 offices (no separate seat), title='Mayor' on -4015001, 'Vice Mayor' on -4015002, 'Acting Mayor' on -4015003 ONLY (post-verify gates (e)/(f)/(i) + Task 3 assert exactly one each); Task 2 re-confirms all three holders before apply |
| T-198-PARTY | Information disclosure | recording a party on a nonpartisan office | mitigate | party=NULL on all 7 (antipartisan; AZ Luminaria confirms nonpartisan); post-verify gate (d) + Task 3 assert 0 non-NULL party |
| T-198-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `C:/EV-Accounts/backend/.env`; applied by the inline orchestrator; never hardcoded in the migration |
| T-198-SC | Tampering | package installs | accept | No new npm/pip/cargo installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 1 greenfield City of South Tucson government (geo_id 0468850, type='City').
- 1 'City Council' chamber; 7 at-large offices ALL on the ONE shared LOCAL/G4110 district; NO LOCAL_EXEC row.
- party NULL / is_appointed false on all 7; Mayor + Vice Mayor + Acting Mayor as title annotations (exactly one each); four plain 'Council Member'; no separate titled seat.
- D-03 enclave invariant: ST_Area(ST_Intersection(0468850, 0477000)) ~0; a South Tucson point covered exclusively by 0468850.
- Section-split 0 rows; casing lowercase 'az' on the district; structural migration registered.
- Roster AND all three title holders re-verified against a live source at execute time (July 21 primary + post-canvass title vote decision recorded); 7 politician UUIDs captured for Plans 02/03.
</verification>

<success_criteria>
The government/roster half of SUB-04 and ROADMAP #1 is TRUE in production (7 at-large council members with
Mayor/Vice-Mayor/Acting-Mayor as title annotations, election method verified, at-large routing enabled via
the one new LOCAL district row, enclave routing confirmed clean); the migration is applied + registered; the
UUID manifest and execute-date roster + three-title decision are recorded.
</success_criteria>

<output>
Create `.planning/phases/198-south-tucson-deep-seed/198-01-SUMMARY.md` when done — include the 7 external_id →
UUID → full_name manifest (required by Plans 02 and 03), the authoritative primary date + certification
status + post-canvass-title-vote status established at execute time, the enclave re-check result, the
explicit per-seat roster decision, and the confirmed Mayor/Vice-Mayor/Acting-Mayor title holders.
</output>
</content>
