---
phase: 148-torrance-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["148-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/938_torrance_headshots.sql
autonomous: false

requirements: [TORR-01]

must_haves:
  truths:
    - "the exact image gap/quality set is determined by a pre-flight image-coverage + dimension query over the 7 CURRENT members (Chen Mayor + Gerson/Kaji/Kalani/Bridgett-Lewis/Mattucci/Sheikh per the ROSTER OVERRIDE), NOT hardcoded; who already has a canonical 600×750 image (Mattucci 2b4b35a8, Sheikh 9ac3ac10), who has an OLD-path image to reprocess (Chen 3dfd7349 at …/default.jpeg), and who is missing (Kaji, Kalani, Gerson, Bridgett-Lewis) are all surfaced from live data"
    - "torranceca.gov has NO WAF — portraits are curl'd DIRECTLY (no Palmdale/Lancaster WAF workaround needed); Kaji (politician e9af3b91, photo_origin torranceca.gov/home/showpublishedimage/16860/637925631132900000) + Kalani (politician 0695e308, …/13180/637305200693530000) source from those city URLs; Gerson (d8767eea) + Bridgett-Lewis (9e24181e) source from torranceca.gov council member pages; Chen (3dfd7349) reprocess from his existing …/default.jpeg to the canonical path"
    - "every uploaded portrait is the correct verified person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 FIRST (never stretched) then resized 600×750 Lanczos q90 JPEG, uploaded to Supabase Storage politician_photos at the canonical path {politician_uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), x-upsert:true; politician_images type='default', photo_license='press_use', photo_origin_url set"
    - "Mattucci (2b4b35a8) and Sheikh (9ac3ac10) already have canonical 600×750 images — VERIFY dimensions + identity + canonical path; leave as-is if compliant, reprocess only if non-canonical/wrong-size. Chen (3dfd7349) is the CURRENTLY-SEATED Mayor (ROSTER OVERRIDE — NOT retired) so he DOES get a proper canonical headshot (reprocess his old …/default.jpeg, or re-source from torranceca.gov if better)"
    - "no fabricated photos — where no acceptable verified portrait is obtainable for a member, an honest gap is documented rather than uploading a bad/wrong-person/overlaid image as type='default'; each member ends with exactly ONE type='default' row or a documented gap"
    - "migration 938 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 937; on-disk counter authoritative)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/938_torrance_headshots.sql"
      provides: "Audit-only headshot INSERT/replace (Kaji/Kalani/Gerson/Bridgett-Lewis source + Chen reprocess + Mattucci/Sheikh verify) + photo_origin_url backfill + full-roster image-coverage audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos ({politician_uuid}-headshot.jpg objects)"
      to: "essentials.politician_images.url (type='default', press_use)"
      via: "INSERT politician_images scoped by politician_id, guarded NOT EXISTS"
      pattern: "'default'"
---

<objective>
Give the current 7-member Torrance roster clean, correct official headshots and confirm single
canonical-image coverage. After Plan 02 the survivor chamber f6fcb0ba holds the CURRENTLY-SEATED council
(ROSTER OVERRIDE): Chen (Mayor) + Gerson + Kaji + Kalani + Bridgett Lewis + Mattucci + Sheikh, all
bidirectionally linked. This is the EASIEST headshot environment of the LA-County Wave-2 cities so far —
torranceca.gov has NO WAF, so portraits curl directly. The EXACT gap/quality set is resolved by a pre-flight
image-coverage + dimension query, not assumed.

⚠️ ROSTER OVERRIDE — headshot ALL 7 CURRENT members, including Chen. The RESEARCH §7 treats Chen as a
"retiring" Mayor (skip his photo); the user override seats Chen as the CURRENTLY-SEATED Mayor, so Chen DOES
get a proper canonical headshot. Mattucci is the CURRENTLY-SEATED At-Large member (not "retiring"), and Betty
Lieu / Kalani-as-Mayor do NOT exist in this phase. Target exactly these 7 politician UUIDs:
  Chen 3dfd7349 (Mayor) · Gerson d8767eea · Kaji e9af3b91 · Kalani 0695e308 · Bridgett-Lewis 9e24181e ·
  Mattucci 2b4b35a8 · Sheikh 9ac3ac10
(All have 0 stances; that is Plan 04. This wave is photos only.)

⚠️ torranceca.gov has NO WAF — curl directly (do NOT apply the Pomona/Lancaster WAF workaround). Per the live
DB state (2026-06-20):
- Kaji (e9af3b91): source from his city photo_origin_url torranceca.gov/home/showpublishedimage/16860/637925631132900000
- Kalani (0695e308): source from torranceca.gov/home/showpublishedimage/13180/637305200693530000
- Gerson (d8767eea) + Bridgett Lewis (9e24181e): source from their torranceca.gov council member pages
  (RESEARCH §7 lists /files/assets/city/v/2|3/... portrait paths; you may use the /find-headshots skill to
  discover/verify the exact current URL)
- Chen (3dfd7349): has an OLD-path image (…/3dfd7349…/default.jpeg). REPROCESS it to the canonical
  {uuid}-headshot.jpg path (crop 4:5 → 600×750); if the old image is too small/poor, re-source from
  torranceca.gov (his official Mayor portrait). NOTE: do NOT carry over a cc_by_sa license onto a new
  press_use upload — set photo_license to match the actual source (press_use for the official city portrait).
- Mattucci (2b4b35a8) + Sheikh (9ac3ac10): already have canonical 600×750 images. VERIFY dimensions +
  identity + canonical path; leave as-is if compliant; reprocess only if non-canonical or wrong-size.

Processing (NON-NEGOTIABLE per project memory): for each member — visually confirm the CORRECT person →
crop to 4:5 FIRST (never stretch — crop only, eyes ~1/3 from top, full head+shoulders) → resize 600×750
Lanczos q90 JPEG (Python Pillow) → upload to the Supabase Storage politician_photos bucket at the canonical
path {politician_uuid}-headshot.jpg using SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env) with header
x-upsert:true → INSERT politician_images (type='default', photo_license='press_use'); set photo_origin_url to
the source page. Reject any source with superimposed text/graphics over the face — document a gap honestly
rather than upload a bad photo. Resolve each member's UUID by external_id at apply time.

Purpose: A voter sees the right face next to each official. Wrong-person, text-overlaid, or stretched photos
destroy trust. Output: Migration 938 (audit-only — NOT registered in schema_migrations) + verified portraits
for every member with an obtainable correct photo + a human checkpoint confirming the correct person +
correct crop for each; any genuine gap documented honestly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/148-torrance-deep-seed/148-RESEARCH.md
@.planning/phases/148-torrance-deep-seed/148-VALIDATION.md
@.planning/phases/148-torrance-deep-seed/148-02-SUMMARY.md
@.planning/phases/147-pomona-deep-seed/147-03-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §7). torranceca.gov is NO-WAF — curl directly.
     Pre-flight resolves the exact gap/quality set; /find-headshots skill available. ⚠ ROSTER OVERRIDE:
     7 current members incl. Chen as seated Mayor; NO Betty Lieu, NO Kalani-as-Mayor. -->

Current 7 members (resolve UUID by external_id; pre-flight a coverage + dimension query):
  -201036 Chen (Mayor, politician 3dfd7349) · 683376 Gerson (d8767eea) · 683364 Kaji (e9af3b91) ·
  683370 Kalani (0695e308) · 683366 Bridgett Lewis (9e24181e) · -201103 Mattucci (2b4b35a8) ·
  -201102 Sheikh (9ac3ac10)

Source plan (torranceca.gov NO WAF — curl directly; /find-headshots may assist):
  Kaji (e9af3b91):  torranceca.gov/home/showpublishedimage/16860/637925631132900000
  Kalani (0695e308): torranceca.gov/home/showpublishedimage/13180/637305200693530000
  Gerson (d8767eea): torranceca.gov council member page (RESEARCH §7 lists …/files/assets/city/v/2|3/… portrait paths)
  Bridgett Lewis (9e24181e): torranceca.gov council member page (RESEARCH §7 portrait path; png ~2.3MB)
  Chen (3dfd7349): REPROCESS existing …/3dfd7349…/default.jpeg → canonical; or re-source torranceca.gov official Mayor portrait
  Mattucci (2b4b35a8): ALREADY canonical 600×750 — verify identity + dimensions + path; leave as-is if compliant
  Sheikh (9ac3ac10): ALREADY canonical 600×750 — verify identity + dimensions + path; leave as-is if compliant

  torranceca.gov: NO WAF, HTTP 200 on /files/assets/city/… portrait paths AND /home/showpublishedimage/… (verified 2026-06-20)
  Do NOT carry a cc_by_sa license onto a press_use upload — set photo_license to match the actual source

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face

Storage + DB:
  - Bucket: politician_photos · Path: canonical {politician_uuid}-headshot.jpg (project_palmdale_no_waf canonical convention)
  - Upload via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), header x-upsert:true
  - politician_images: type='default', photo_license='press_use'
  - UPDATE politicians.photo_origin_url to the source page where NULL
  - Each INSERT guarded WHERE NOT EXISTS on politician_id → exactly ONE type='default' row per member

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql or psql -f). It does NOT
register in schema_migrations (same convention as Pomona 928 / Palmdale 920 / Lancaster 912). On-disk counter
authoritative; ledger stays at 937 (Plan 02).

Pattern migration: C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql (audit-only INSERT +
photo_origin_url backfill + Storage URL shape + full-roster coverage audit). For NO-WAF direct-curl sourcing
see the Palmdale precedent (project_palmdale_no_waf).
/find-headshots skill: ~/.claude/commands/find-headshots.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight image-coverage/dimension check; source + verify portraits for the missing/new members (Kaji/Kalani/Gerson/Bridgett-Lewis) from torranceca.gov (NO-WAF direct curl); crop 4:5 → 600×750 → upload</name>
  <files>C:/EV-Accounts/backend/migrations/938_torrance_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql (audit-only INSERT + download/crop/upload pipeline + Storage URL shape)
    - .planning/phases/147-pomona-deep-seed/147-03-PLAN.md (the exact pre-flight-coverage + crop/resize + checkpoint shape this plan mirrors — but Torrance is NO-WAF, curl directly)
    - .planning/phases/148-torrance-deep-seed/148-RESEARCH.md (§7 headshot sources/status — apply the ROSTER OVERRIDE: Chen is seated Mayor [gets a headshot], Mattucci is seated [verify], NO Betty Lieu)
    - .planning/phases/148-torrance-deep-seed/148-VALIDATION.md (Wave 3 SQL assertions)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots — 4:5 crop FIRST → 600×750 Lanczos q90)
    - ~/.claude/commands/find-headshots.md (skill for discovering/verifying torranceca.gov portrait URLs)
  </read_first>
  <action>
    First run a pre-flight image-coverage + dimension query: for the 7 current members (3dfd7349 Chen /
    d8767eea Gerson / e9af3b91 Kaji / 0695e308 Kalani / 9e24181e Bridgett-Lewis / 2b4b35a8 Mattucci /
    9ac3ac10 Sheikh), LEFT JOIN essentials.politician_images (type='default') and list who has a
    type='default' image, its url + dimensions where known — confirm the missing set (Kaji/Kalani/Gerson/
    Bridgett-Lewis), Chen's old-path …/default.jpeg, and Mattucci's + Sheikh's already-canonical images.
    torranceca.gov has NO WAF — curl directly. For the 4 missing members, source from torranceca.gov:
    Kaji from torranceca.gov/home/showpublishedimage/16860/637925631132900000; Kalani from
    …/home/showpublishedimage/13180/637305200693530000; Gerson + Bridgett-Lewis from their council member
    pages (RESEARCH §7 /files/assets/city/v/2|3/… portrait paths), using the /find-headshots skill to
    discover/verify the exact current URL where helpful. For EACH, visually confirm it is the correct named
    person and has NO superimposed text/graphics. Then crop to 4:5 (crop only — never stretch; eyes ~1/3 from
    top, full head+shoulders), resize to 600×750 Lanczos q90 JPEG (Pillow), resolve each UUID by external_id,
    and upload to politician_photos at {politician_uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY
    (C:/EV-Accounts/backend/.env) with x-upsert:true. Record each public Storage URL + source page for Task 2.
    If no acceptable verified portrait is obtainable for a member, document the genuine gap (do NOT upload an
    overlaid/wrong-person image as type='default').
  </action>
  <verify>
    <automated>For each sourced member the processed file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('{member}.jpg').size)"` → (600, 750)) and the uploaded Storage object at {politician_uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight image-coverage + dimension query run; the missing/old set determined from live data (Kaji/Kalani/Gerson/Bridgett-Lewis missing; Chen old-path default.jpeg; Mattucci/Sheikh already canonical)
    - torranceca.gov curl'd directly (NO WAF workaround); the 4 missing portraits sourced from city URLs (Kaji/Kalani showpublishedimage; Gerson/Bridgett-Lewis council pages); /find-headshots used where helpful
    - Each of the 4 portraits is the correct verified person, no superimposed text/graphics, processed to exactly 600×750, 4:5, not stretched, eyes ~1/3 from top, uploaded to {politician_uuid}-headshot.jpg (HTTP 200)
    - Public Storage URL + source page recorded for Task 2 for each uploaded member
    - If any of the 4 has no acceptable portrait, an honest gap documented (NOT a wrong-person/overlaid image faked as type='default')
  </acceptance_criteria>
  <done>The 4 missing members (Kaji/Kalani/Gerson/Bridgett-Lewis) have verified portraits sourced directly from torranceca.gov (no WAF), each confirmed correct + clean, processed to 600×750 4:5, uploaded to Storage at {politician_uuid}-headshot.jpg; Storage URLs + sources recorded for Task 2; any genuine gap documented.</done>
</task>

<task type="auto">
  <name>Task 2: Reprocess Chen's seated-Mayor portrait to canonical; verify Mattucci/Sheikh existing canonical images; author + apply migration 938 (audit-only) + full-roster image audit</name>
  <files>C:/EV-Accounts/backend/migrations/938_torrance_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql (INSERT politician_images + photo_origin_url backfill + audit-only apply, ledger-unchanged check + full-roster coverage audit)
    - .planning/phases/148-torrance-deep-seed/148-RESEARCH.md (§7 existing-headshot audit — but Chen is the SEATED Mayor per ROSTER OVERRIDE [gets a canonical headshot], Mattucci is SEATED [verify])
    - .planning/phases/148-torrance-deep-seed/148-VALIDATION.md (Wave 3 SQL assertions)
    - ~/.claude/commands/find-headshots.md (skill for sourcing Chen's official Mayor portrait if the old default.jpeg is too small)
  </read_first>
  <action>
    Handle the 3 existing-photo members. Chen (3dfd7349, SEATED Mayor per ROSTER OVERRIDE): download his
    existing …/3dfd7349…/default.jpeg, VERIFY it is George Chen, crop 4:5 → 600×750 Lanczos q90, upload to his
    {uuid}-headshot.jpg with x-upsert:true (canonical path); if the old image is too small/poor quality,
    re-source his official Mayor portrait from torranceca.gov (NO WAF) instead. Set photo_license to match the
    actual source (press_use for the official city portrait) — do NOT carry a cc_by_sa license onto a
    press_use row. Mattucci (2b4b35a8) + Sheikh (9ac3ac10): VERIFY their existing images are exactly 600×750,
    the correct person, at the canonical {uuid}-headshot.jpg path with type='default'/press_use — if
    compliant, leave as-is (no re-upload); if non-canonical path or wrong size, reprocess + re-upload.
    Then write C:/EV-Accounts/backend/migrations/938_torrance_headshots.sql: for every member uploaded in Task
    1 or this task, INSERT a politician_images row (resolved by external_id) url = the Storage public URL,
    type='default', photo_license='press_use', guarded WHERE NOT EXISTS on politician_id (or replace an
    existing old-path/wrong-size row by its exact image id/url for Chen); UPDATE essentials.politicians SET
    photo_origin_url = the source page WHERE external_id=<id> AND photo_origin_url IS NULL. Apply via raw SQL
    (mcp__supabase-local execute_sql or psql -f) — do NOT run through the schema_migrations ledger
    (audit-only). Then run the full-roster coverage audit: LEFT JOIN the 7 current Torrance officials
    (-201036/683376/683364/683370/683366/-201103/-201102) to politician_images (type='default') and report
    who has exactly one type='default' row vs a documented gap; confirm no member has >1 default row. Verify
    MAX(version) in schema_migrations is UNCHANGED by this file (stays 937).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT p.external_id, COUNT(pi.*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (-201036,683376,683364,683370,683366,-201103,-201102) GROUP BY p.external_id;" shows each member with COUNT 0 (documented gap) or 1 (no member has >1) AND psql "$DATABASE_URL" -c "SELECT MAX(version) FROM supabase_migrations.schema_migrations;" is unchanged (stays 937)</automated>
  </verify>
  <acceptance_criteria>
    - Chen (3dfd7349, seated Mayor): canonical 600×750 4:5 headshot at {uuid}-headshot.jpg; identity confirmed George Chen; photo_license matches the actual source (press_use for city portrait, not carried-over cc_by_sa); exactly one type='default' row
    - Mattucci (2b4b35a8) + Sheikh (9ac3ac10): existing canonical 600×750 images verified (correct person, canonical path, type='default'/press_use); left as-is if compliant, reprocessed only if non-canonical/wrong-size
    - Each member with a verified portrait has exactly one type='default' press_use row pointing at {politician_uuid}-headshot.jpg; photo_origin_url populated; no member has >1 type='default' row
    - Members with no obtainable verified portrait are documented as honest gaps (not faked)
    - schema_migrations MAX(version) unchanged by migration 938 (audit-only confirmed; stays 937)
    - Migration is idempotent (re-apply = 0 rows changed)
  </acceptance_criteria>
  <done>938 applied (audit-only); Chen reprocessed to a canonical seated-Mayor portrait; Mattucci/Sheikh existing canonical images verified; full-roster image audit shows each current member with exactly one default image or a documented honest gap; ledger untouched (stays 937).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify every Torrance headshot (correct person, clean crop, all 7 current members)</name>
  <action>Pause for the user to visually verify each Torrance portrait per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>Verified official headshots for the 7 CURRENT Torrance members (ROSTER OVERRIDE — Chen Mayor + Gerson/Kaji/Kalani/Bridgett-Lewis/Mattucci/Sheikh), sourced directly from torranceca.gov (NO WAF): Kaji/Kalani from showpublishedimage URLs, Gerson/Bridgett-Lewis from council pages, Chen reprocessed to a canonical seated-Mayor portrait, Mattucci/Sheikh existing canonical images verified; all processed to 600×750 4:5 and live in Supabase Storage + politician_images (type='default', press_use). Any honest gaps documented.</what-built>
  <how-to-verify>
    1. Confirm each member's photo is the CORRECT person (cross-check Kaji/Kalani/Gerson/Bridgett-Lewis/Chen against torranceca.gov; Mattucci/Sheikh against known images).
    2. CRITICAL: confirm the real Bridgett Lewis (683366) is shown — NOT confused with the deleted Brigitte-Lewis duplicate; confirm Chen (seated Mayor) has a proper canonical portrait, not the old tiny default.jpeg.
    3. Confirm no superimposed text/graphics over any face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    4. Confirm each member shows exactly one canonical image (no duplicate type='default' rows) on the Torrance browse view; any documented gap is genuinely imageless (not faked).
    5. Confirm photo_license values are honest (press_use for city portraits; no cc_by_sa carried onto a press_use upload).
    6. Confirm the on-disk counter is authoritative: schema_migrations MAX(version) is unchanged (still 937; the headshot file did not register).
  </how-to-verify>
  <resume-signal>Type "approved" or name which photo(s) need re-sourcing / re-cropping / identity-fix.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with a NO-WAF official image source. No untrusted user input reaches the DB — uploads +
image-row writes use service-role/operator against production. Threat surface = wrong-person photo (e.g.
showing the deleted Brigitte-Lewis duplicate instead of the real Bridgett, or a wrong council portrait), a
distorted/overlaid photo, a fabricated photo filling a real gap, carrying a cc_by_sa license onto a press_use
row, re-sourcing over a good canonical image (Mattucci/Sheikh), or accidental ledger registration —
mitigated by the pre-flight coverage/dimension check, the correct-person verification, the
crop-then-resize/no-stretch rule, a blocking human-verify checkpoint, the honest-gap rule, the
license-matches-source rule, and the audit-only raw-SQL apply.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| torranceca.gov (NO WAF) | Official source AVAILABLE — curled directly (HTTP 200 on /files/assets and /home/showpublishedimage paths). |
| campaign sites / Ballotpedia / TUSD → local processing | Fallback image sources if a city portrait is missing; human-verified before upload. |
| operator → Supabase Storage + production DB | Uploads + image-row INSERT against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-18 | Spoofing | Wrong-person headshot (e.g. the deleted Brigitte-Lewis duplicate shown for Bridgett, or a swapped council portrait) | mitigate | Resolve UUID by external_id (Bridgett 683366 / 9e24181e only); visually confirm each portrait; blocking human-verify specifically checks the real Bridgett + each member's identity |
| T-148-19 | Spoofing | Chen (seated Mayor) left with the old tiny default.jpeg instead of a canonical portrait (RESEARCH "retiring" framing) | mitigate | ROSTER OVERRIDE: Chen gets a canonical reprocess/re-source; post-audit confirms a 600×750 type='default' row for 3dfd7349; checkpoint confirms it is not the old tiny image |
| T-148-20 | Tampering | Image stretched / aspect-distorted / text-overlaid | mitigate | Crop-to-4:5-FIRST-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; human checkpoint |
| T-148-21 | Spoofing | Fabricated / campaign / wrong photo uploaded to fill a gap | mitigate | Honest-gap rule — if no acceptable verified portrait, leave imageless and document; never seed a wrong-person/overlaid photo as type='default' |
| T-148-22 | Repudiation | cc_by_sa license carried onto a press_use upload (license misrepresentation) | mitigate | photo_license set to match the actual source (press_use for official city portraits); do not copy a prior cc_by_sa value onto a new row |
| T-148-23 | Tampering | Re-sourcing over a good canonical image (Mattucci/Sheikh), or duplicate image row | mitigate | Pre-flight coverage query targets only the missing/old set; Mattucci/Sheikh verified-and-left-as-is when compliant; each INSERT guarded WHERE NOT EXISTS on politician_id; post-audit confirms ≤1 type='default' per official |
| T-148-24 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged (stays 937) before/after |
| T-148-SC | Tampering | npm/pip/cargo installs | accept | No package installs (existing Pillow + Supabase client; RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After 938 + checkpoint:
1. each current member with a verified portrait has exactly one type='default' press_use image at {politician_uuid}-headshot.jpg; photo_origin_url set
2. full-roster coverage: every member is either imaged (exactly one default) or a documented honest gap; no member has >1
3. the real Bridgett Lewis (683366) is shown (not the deleted Brigitte duplicate); Chen (seated Mayor) has a canonical portrait
4. photo_license values honest (no cc_by_sa carried onto a press_use row); no stretched/overlaid photos
5. schema_migrations MAX(version) unchanged by 938 (stays 937)
6. human checkpoint approved (correct person each, no overlays, correct crop)
</verification>

<success_criteria>
Every current Torrance member with an obtainable correct photo has a single 600×750 verified image at the
canonical path; portraits are sourced directly from torranceca.gov (NO WAF); Chen is given a canonical
seated-Mayor portrait (not the old tiny image); the real Bridgett Lewis is shown (not the deleted duplicate);
no wrong-person/overlaid/stretched photos; licenses honest; genuine gaps documented honestly; the ledger
counter is preserved (stays 937).
</success_criteria>

<output>
Create `.planning/phases/148-torrance-deep-seed/148-03-SUMMARY.md` when done (record any honest image gaps).
</output>
</content>
