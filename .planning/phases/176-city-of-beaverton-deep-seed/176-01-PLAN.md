---
phase: 176-city-of-beaverton-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-beaverton-wave0-probe.sql
autonomous: false
requirements: [WASH-02]

must_haves:
  truths:
    - "Orchestrator has confirmed the ext_id block -4105351..-4105357 is free of collisions"
    - "Orchestrator has confirmed no City of Beaverton government row already exists"
    - "Orchestrator has confirmed city geo_id 4105350 G4110 geofence exists"
    - "Orchestrator has confirmed the on-disk migration MAX (1130) matches the DB ledger MAX so next=1131 is safe"
    - "Position 1 councilor (Hartmeier-Prigg) is confirmed still seated (not vacated early)"
    - "The live compass topic_key list (is_live=true) is captured for stance ingestion"
    - "Wave-0 checkpoint confirms CONTEXT decisions D-01, D-02, D-06 (form-of-government/routing resolution + seated roster ground-truth) before any write"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-beaverton-wave0-probe.sql"
      provides: "The 6 Wave-0 probe queries the orchestrator runs before any write"
      min_lines: 20
  key_links:
    - from: "probe.sql"
      to: "essentials.governments / essentials.politicians / essentials.geofence_boundaries / supabase_migrations.schema_migrations / inform.compass_topics"
      via: "SELECT probes"
      pattern: "essentials\\.(governments|politicians|geofence_boundaries)"
---

<objective>
Author the Wave-0 verification probe file for the City of Beaverton deep-seed and drive the
orchestrator-run DB checks that MUST pass before any structural write. This plan gates the
entire phase: no government, roster, headshot, or stance migration may be authored or applied
until every probe below returns its expected result.

Purpose: Prevent duplicate seeds, ext_id collisions, wrong migration numbering, and seeding a
non-seated councilor — the four highest-risk failure modes for this phase (RESEARCH Pitfalls 1 & 4,
Assumptions A2/A4).
Output: `_tmp-beaverton-wave0-probe.sql` (gitignored `_tmp-*` helper) + a recorded pass/fail for
each of the 6 Wave-0 gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-RESEARCH.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-VALIDATION.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO
database access. The INLINE ORCHESTRATOR (main context) runs the probes via
`psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`.
This file is a gitignored `_tmp-*` helper — do NOT commit it.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except where a query genuinely reads that table — see probe 4), and
`photo_origin_url` out of comments.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 6-query Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-beaverton-wave0-probe.sql</files>
  <read_first>
    - .planning/phases/176-city-of-beaverton-deep-seed/176-RESEARCH.md §"Geofence Pre-Check", §"Ext_id Block", §"Wave 0 Gaps"
    - .planning/phases/176-city-of-beaverton-deep-seed/176-VALIDATION.md §"Wave 0 Requirements"
    - .planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md §"Key Casing / Structural Traps"
  </read_first>
  <action>
    Write six labeled SELECT probes (each preceded by a `\echo` label line so the orchestrator can
    read results unambiguously). Probe A: count rows in essentials.governments WHERE name='City of
    Beaverton, Oregon, US' (expect 0). Probe B: select external_id from essentials.politicians WHERE
    external_id BETWEEN -4105357 AND -4105351 (expect 0 rows). Probe C: count geofence rows in
    essentials.geofence_boundaries WHERE geo_id='4105350' AND mtfcc='G4110' (expect 1), and separately
    count essentials.districts WHERE geo_id='4105350' (expect 0). Probe D: select MAX(version::int)
    from supabase_migrations.schema_migrations (expect 1130 to match on-disk MAX so next structural =
    1131). Probe E: spot-check districts.state casing — select distinct state from essentials.districts
    WHERE geo_id='4159000' (Portland) to confirm lowercase 'or' is the correct casing for LOCAL rows.
    Probe F: select topic_key from inform.compass_topics WHERE is_live=true ORDER BY topic_key — this
    is the authoritative live-topic list for the stance plan (04). Include a header comment noting the
    file is a gitignored orchestrator-run helper. Do NOT wrap in a transaction (read-only). No fenced
    code, no DDL — SELECT/\echo only.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-beaverton-wave0-probe.sql` and records the 6 results</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-beaverton-wave0-probe.sql with 6 labeled probes (A–F)
    - Probe A queries governments by exact name 'City of Beaverton, Oregon, US'
    - Probe B queries the exact ext_id range -4105357..-4105351
    - Probe C queries geo_id='4105350' with mtfcc='G4110' AND a separate districts count
    - Probe D reads MAX(version::int) from the migration ledger
    - Probe F selects topic_key WHERE is_live=true
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE)
  </acceptance_criteria>
  <done>Probe file authored with all 6 queries and correct literal values.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes and confirms all 6 gates</name>
  <what-built>The 6-query Wave-0 probe file. The orchestrator now executes it against the live DB.</what-built>
  <how-to-verify>
    Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-beaverton-wave0-probe.sql`
    (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
    1. Probe A returns 0 (no existing Beaverton gov). If >0 → STOP, phase already partly seeded.
    2. Probe B returns 0 rows (ext_id block free). If any row → shift range to -4105401..-4105407 and record the new block for plans 02/03/04.
    3. Probe C returns geofence=1 AND districts=0. If geofence≠1 → STOP, city geofence missing. If districts>0 → STOP, districts already exist.
    4. Probe D returns 1130. If not → reconcile; next structural migration number = (ledger MAX + 1); record it for plan 02.
    5. Probe E returns 'or' (lowercase) — confirms the casing plan 02 must use for LOCAL/LOCAL_EXEC districts.
    6. Probe F returns the live topic_key list — save it verbatim for plan 04's stance research.
    Separately verify at beavertonoregon.gov/council-position-1 that Ashley Hartmeier-Prigg is still the seated Position 1 holder (RESEARCH Pitfall 1 / A2). If she has vacated early → STOP and re-plan the roster.
  </how-to-verify>
  <acceptance_criteria>
    - All 6 probe gates recorded with pass/fail
    - Confirmed ext_id block (default -4105351..-4105357 unless probe B forced a shift)
    - Confirmed next structural migration number (default 1131)
    - Confirmed live topic_key list captured for plan 04
    - Hartmeier-Prigg confirmed seated (Position 1)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-01 | Tampering | probe.sql executed on prod | accept | Probes are read-only SELECTs; no write path; no user input |
| T-176-02 | Information Disclosure | topic_key list from inform.compass_topics | accept | Public compass topic keys; no PII |
| T-176-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All 6 Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- Any failed gate blocks plans 02–05 until resolved.
</verification>

<success_criteria>
- ext_id block confirmed free; no duplicate Beaverton gov; geofence present, districts absent;
  next migration number confirmed; live topic_key list captured; Position 1 incumbent confirmed.
</success_criteria>

<output>
Create `.planning/phases/176-city-of-beaverton-deep-seed/176-01-SUMMARY.md` when done, recording
the confirmed ext_id block, next migration number, and live topic_key list for downstream plans.
</output>
