---
phase: 109-ma-tier-2-cities
plan: 06
type: execute
wave: 2
depends_on: ["109-01", "109-02", "109-03", "109-04", "109-05"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-ma-tier2-headshots.py
  - C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql
autonomous: true
requirements: [MA-TIER2-01, MA-TIER2-02]
must_haves:
  truths:
    - "Available MA Tier 2 officials have headshots at 600x750 in the politician_photos Supabase Storage bucket"
    - "Each uploaded headshot has a politician_images row with type='default' so the UI renders it"
    - "Gaps (officials with no accessible official photo) are documented, not blocking — Tier 2 is best-effort"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-ma-tier2-headshots.py"
      provides: "Download + crop 4:5 + resize 600x750 Lanczos q90 + upload + politician_images insert across all 5 cities (per MA-TIER2-01, MA-TIER2-02)"
    - path: "C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql"
      provides: "Idempotent politician_images INSERT blocks for all uploaded officials + best-effort count NOTICE"
      contains: "Migration 356"
  key_links:
    - from: "Supabase Storage politician_photos bucket"
      to: "essentials.politician_images.url"
      via: "CDN URL written by upload script"
      pattern: "politician_photos"
    - from: "essentials.politician_images.type"
      to: "UI .find(img => img.type === 'default')"
      via: "type='default' literal"
      pattern: "'default'"
---

<objective>
Source best-effort headshots for the 59 MA Tier 2 officials across Worcester, Springfield, Lowell, Brockton, and Quincy; crop to 4:5, resize to 600x750 (Lanczos, q90), upload to the politician_photos bucket, and write politician_images rows (type='default'). Closes the headshot portion of MA-TIER2-01 and MA-TIER2-02.

Purpose: Headshots make the LOCAL section visually complete. Tier 2 is best-effort — Worcester has a verified URL pattern; Brockton's Mayor has a verified URL; Springfield/Lowell/Quincy require per-page source inspection. Officials with no accessible official photo are documented as gaps, not blockers.
Output: Python upload script `_tmp-ma-tier2-headshots.py` (runs to download/process/upload/insert) + migration `356_ma_tier2_headshots.sql` (idempotent record of the inserted rows + post-verification NOTICE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-ma-tier-2-cities/109-RESEARCH.md
@.planning/phases/109-ma-tier-2-cities/109-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write headshot upload script _tmp-ma-tier2-headshots.py</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-ma-tier2-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_boston-headshots-upload.py (exact-match analog: .env config block, ROSTER structure, crop_to_4_5, insert_politician_images_row parameterized query, process_member pipeline)
    - .planning/phases/109-ma-tier-2-cities/109-PATTERNS.md (lines 503-607: full script pattern with concrete config/roster/functions; lines 761-785: parameterized-SQL + .env-only shared patterns)
    - .planning/phases/109-ma-tier-2-cities/109-RESEARCH.md (lines 453-491: headshot sources per city; lines 594-623: assumptions A4/A5 + open questions on photo accessibility)
  </read_first>
  <action>
    Create _tmp-ma-tier2-headshots.py by adapting _boston-headshots-upload.py. Copy verbatim: the .env loader (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL — never hardcode), BUCKET='politician_photos', CDN_BASE, TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS, the crop_to_4_5 function, the insert_politician_images_row parameterized query (%s::uuid placeholders — NEVER f-string into SQL), and the process_member pipeline (download → verify → crop 4:5 → resize 600x750 → re-encode JPEG q90 → upload → DB insert).

    Build a ROSTER list of all 59 officials grouped by city, each entry: external_id, full_name, city, politician_id=None (resolved at runtime by querying essentials.politicians WHERE external_id=...), source_url, mandatory=False (Tier 2 = best-effort).

    Source URLs:
      - Worcester (geo_id 2582000, external_ids -258200001..-258200011): try `https://www.worcesterma.gov/media/council/{lastname}-headshot.jpg` first, fall back to `https://www.worcesterma.gov/media/council/{lastname}.jpg`. Set Referer header 'https://www.worcesterma.gov/'.
      - Brockton Mayor Rodrigues (-250900001): `https://brockton.ma.us/wp-content/uploads/2026/01/MRodrigues-300x300.jpeg`. Brockton council (-250900002..-250900012): inspect individual pages under brockton.ma.us/government/city-council/ for WordPress wp-content photo URLs; populate source_url where found, leave entry with source_url=None where not found (script skips and logs gap).
      - Springfield (-256700001..-256700014): inspect springfield-ma.gov/cos/city-council-members member pages; prefer stable `/cos/fileadmin/user_upload/` images over `_processed_` cache URLs; populate where found, gap-skip otherwise.
      - Lowell (-253700001..-253700012): inspect lowellma.gov/533/Meet-the-City-Council member pages (CivicPlus CMS); populate where found, gap-skip otherwise.
      - Quincy (-255574501..-255574510): inspect quincyma.gov city council member pages; populate where found, gap-skip otherwise. New Jan 2026 council may lack official photos for some.

    Resolve politician_id at runtime: SELECT id FROM essentials.politicians WHERE external_id = %s (parameterized). For each roster entry with a source_url, run the full pipeline; on download failure or missing source_url, log "GAP: {full_name} ({city}) — no accessible photo" and continue (do not abort). At end, print a summary: uploaded count + gap list.

    feedback_headshot_resize_no_distort: crop to 4:5 FIRST, then resize to 600x750 — never stretch directly.
    feedback_headshot_no_graphics: skip any source image with superimposed text/graphics over the face.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/scripts/_tmp-ma-tier2-headshots.py','utf8'); if(!s.includes('crop_to_4_5')){console.error('missing crop_to_4_5');process.exit(1)} if(!s.includes('(600, 750)')&&!s.includes('(600,750)')){console.error('missing 600x750');process.exit(1)} if(!s.includes('%s::uuid')){console.error('must use parameterized query');process.exit(1)} if(/INSERT INTO essentials.politician_images.*f[\"']/.test(s)){console.error('f-string SQL forbidden');process.exit(1)} if(!s.includes('-258200001')||!s.includes('-255574510')){console.error('roster incomplete');process.exit(1)} console.log('OK script: crop, 600x750, parameterized, roster spans Worcester..Quincy')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-ma-tier2-headshots.py
    - Contains crop_to_4_5 function and TARGET_SIZE (600, 750)
    - DB insert uses parameterized %s::uuid placeholders (no f-string interpolation into SQL)
    - Credentials read from .env (no hardcoded SUPABASE_SERVICE_ROLE_KEY / DATABASE_URL)
    - ROSTER spans all 5 cities (external_ids -258200001 through -255574510 represented)
    - Worcester URLs try '-headshot.jpg' then plain '.jpg'; Brockton Mayor uses the verified MRodrigues URL
    - Missing-photo entries are gap-skipped and logged, not fatal
  </acceptance_criteria>
  <done>Headshot upload script written: best-effort across all 5 cities, crop-4:5-then-resize-600x750, parameterized inserts, gap-tolerant.</done>
</task>

<task type="auto">
  <name>Task 2: Run the upload script, then write + apply migration 356 recording the inserted rows</name>
  <files>C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/349_boston_headshots.sql (exact-match analog: header, per-official INSERT block with WHERE NOT EXISTS guard, post-verification NOTICE)
    - .planning/phases/109-ma-tier-2-cities/109-PATTERNS.md (lines 442-499: migration 356 header + INSERT-block + post-verification patterns)
    - C:/EV-Accounts/backend/scripts/_tmp-ma-tier2-headshots.py (the script that produces the uploaded CDN URLs — read to confirm which officials uploaded)
  </read_first>
  <action>
    First, run the upload script:
      cd C:/EV-Accounts/backend && python scripts/_tmp-ma-tier2-headshots.py
    (feedback_no_git_in_ev_accounts: never run git in C:/EV-Accounts.) The script downloads, processes, uploads to the politician_photos bucket, and inserts politician_images rows directly (parameterized). Capture its summary output (uploaded count + gap list).

    Then write migration 356_ma_tier2_headshots.sql as the idempotent record of what was inserted, following 349_boston_headshots.sql:
      - Header: title "Migration 356: MA Tier 2 headshots (MA-TIER2-01, MA-TIER2-02)"; note bucket=politician_photos, CDN base, type='default' (NOT 'headshot'; UI filters .find(img => img.type === 'default')), url column (NOT storage_url), photo_license='public_domain', no BEGIN/COMMIT (autocommit per-INSERT like 349).
      - One INSERT block per official that was actually uploaded: INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) SELECT gen_random_uuid(), (SELECT id FROM essentials.politicians WHERE external_id = -NNNNNNNNN), '<actual CDN url from upload>', 'default', 'public_domain' WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -NNNNNNNNN)). Fill the real CDN URLs produced by the script (UUIDs come from bucket filenames).
      - Comment out (or omit) officials that were gap-skipped, with a "-- GAP: {name} — no accessible photo" line so the migration documents coverage.
      - Post-verification DO block: COUNT politician_images joined to politicians across all 5 external_id ranges (-258200011..-258200001 OR -256700014..-256700001 OR -253700012..-253700001 OR -250900012..-250900001 OR -255574510..-255574501); RAISE NOTICE with the count (best-effort — no hard gate).
      - Ledger: INSERT version '356' ON CONFLICT DO NOTHING.

    Apply migration 356 via `mcp__supabase-local__execute_sql` (the WHERE NOT EXISTS guards make it idempotent even though the script already inserted the rows — re-running inserts nothing).

    feedback_supabase_local_is_remote: this is the live production DB.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql','utf8'); if(!s.includes('Migration 356')){process.exit(1)} if(!s.includes(\"'default'\")){console.error('type must be default');process.exit(1)} if(s.includes('storage_url')){console.error('use url column not storage_url');process.exit(1)} if(!s.includes(\"VALUES ('356')\")){process.exit(1)} console.log('OK migration 356: default type, url column, ledger')"</automated>
  </verify>
  <acceptance_criteria>
    - Upload script ran; summary printed uploaded count + gap list
    - File exists at C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql
    - All INSERT blocks use type='default' and the url column (NOT storage_url); photo_license='public_domain'
    - Each INSERT block has a WHERE NOT EXISTS guard on politician_id (idempotent)
    - Gap-skipped officials are documented as '-- GAP:' comments
    - Migration 356 applied via mcp__supabase-local__execute_sql without exception; ledger version '356' present
    - Verified at runtime: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -258200011 AND -258200001 OR p.external_id BETWEEN -256700014 AND -256700001 OR p.external_id BETWEEN -253700012 AND -253700001 OR p.external_id BETWEEN -250900012 AND -250900001 OR p.external_id BETWEEN -255574510 AND -255574501 returns the uploaded count (≥1, best-effort)
  </acceptance_criteria>
  <done>Upload script run; headshots uploaded to politician_photos bucket at 600x750; migration 356 written + applied recording all uploaded rows with type='default'; gaps documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external city websites → upload script | untrusted image bytes downloaded over HTTP |
| upload script → production DB + Storage | writes politician_images rows + bucket objects to live production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-16 | Tampering | SQL injection via image metadata / names in script | mitigate | All DB inserts use parameterized %s::uuid placeholders; never f-string into SQL (Task 1 verify enforces) |
| T-109-17 | Tampering | re-running script or migration 356 duplicating images | mitigate | WHERE NOT EXISTS guard on politician_id in both script insert and migration 356 |
| T-109-18 | Spoofing | malicious image substituted for official photo | accept | Sources are official city .gov / .ma.us domains; best-effort sourcing; manual no-graphics check per feedback_headshot_no_graphics |
| T-109-19 | Information Disclosure | credentials hardcoded in script | mitigate | Credentials loaded from .env only; Task 1 verify enforces no hardcoded keys |
</threat_model>

<verification>
- Upload script ran and reported uploaded count + gap list
- Migration 356 applied without exception; ledger version '356' present
- politician_images rows present (type='default') for all uploaded officials across the 5 cities
- Gaps documented in migration comments
</verification>

<success_criteria>
Available MA Tier 2 officials have 600x750 headshots in the politician_photos bucket, each with a type='default' politician_images row so the UI renders them. Officials with no accessible official photo are documented as gaps. Headshot portion of MA-TIER2-01 and MA-TIER2-02 is satisfied (best-effort standard).
</success_criteria>

<output>
Create `.planning/phases/109-ma-tier-2-cities/109-06-SUMMARY.md` when done
</output>
