---
phase: 221-collin-county-headshots-source-and-attach-photos-for-officia
plan: 04
type: execute
wave: 4
depends_on: ["221-02", "221-03"]
files_modified:
  - .planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md
autonomous: false
requirements: [COLLIN-HEADSHOT-01, COLLIN-HEADSHOT-02]
must_haves:
  truths:
    - "The AFTER coverage counts are measured against 221-01's BEFORE counts, giving a documented coverage delta = number of headshots newly attached this phase (COLLIN-HEADSHOT-01)"
    - "A sample of newly-attached images is machine-verified to be exactly 600x750 px (4:5), proving the crop-then-resize spec held (COLLIN-HEADSHOT-02)"
    - "A 221-CONFIRMED-BLANK.md register lists every officeholder intentionally left without a photo, with per-person search evidence, so a later phase does not re-litigate them"
    - "Brandon Smith and Gopal Ponangi each have a final disposition (headshot attached OR confirmed-blank with evidence) recorded"
  artifacts:
    - ".planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md"
  key_links:
    - "The AFTER query reuses the exact 221-01 Collin scope so BEFORE/AFTER counts are comparable; each new politician_images row still links to a real seated politician_id"
  prohibitions:
    - "Never record a person as confirmed-blank without documented search evidence — blank is a terminal state only after a genuine search returned no usable public photo"
---

<objective>
Close the phase: measure the coverage delta (AFTER vs 221-01 BEFORE), machine-verify the attached images meet the 600x750 4:5 spec, and write the 221-CONFIRMED-BLANK.md register of officeholders intentionally left without a photo (COLLIN-HEADSHOT-01, COLLIN-HEADSHOT-02). This plan writes no photo assets and no image rows — it verifies and documents what 221-02/03 produced.

Purpose: Prove the phase goal held — every officeholder with a public photo now has a conforming headshot, everyone else is a documented honest blank — and leave a durable blank register so Phase 222 (and beyond) does not re-source the same dead ends.
Output: `221-CONFIRMED-BLANK.md` + the verified coverage delta.
</objective>

## Artifacts this phase produces

Recap (full list in 221-01): new `essentials.politician_images` rows (`type='default'`, `photo_license`), 600×750 JPEGs in Supabase Storage bucket `politician_photos`, `221-01-WORKLIST.md`, and — from this plan — `221-CONFIRMED-BLANK.md`. No code, schema, or migrations.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-WORKLIST.md
@.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-02-SUMMARY.md
@.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AFTER audit — coverage delta + image-dimension spec check</name>
  <read_first>
    - `221-01-WORKLIST.md` — the BEFORE counts and the full in-scope politician_id set (the exact scope to re-query).
    - `221-02-SUMMARY.md` and `221-03-SUMMARY.md` — the reported found counts to reconcile against.
    - `~/.claude/commands/find-headshots.md` — for the storage CDN base and the {politician_id}-headshot.jpg naming used to fetch images.
  </read_first>
  <files>(read-only DB query via mcp__supabase-local__execute_sql + read-only image fetches; no writes this task)</files>
  <action>
    Re-run the 221-01 scoped audit query (same Collin County scope, pi.id IS NULL test) to get AFTER counts: total in-scope, WITH image, WITHOUT image. Compute the coverage delta = AFTER-with-image minus BEFORE-with-image, and assert it equals the total "found" count reported across 221-02 and 221-03 SUMMARYs (reconcile any mismatch before closing). Assert every politician_images row created this phase is type='default' with a non-null photo_license and links to an in-scope politician_id. Then run a dimension spot-check: for at least 3 newly-attached images (more if fewer than 3 were found, use all), fetch the object from the politician_photos CDN base and open it with Python PIL, asserting Image.size == (600, 750). Record the AFTER counts, delta, and dimension-check results. Confirm Brandon Smith and Gopal Ponangi each resolve to a final disposition (has image row OR is on the blank list).
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql returns AFTER counts; a Python PIL check over >=3 fetched newly-attached images asserts each is exactly (600, 750); coverage delta equals the summed found count from 221-02/03.</automated>
  </verify>
  <acceptance_criteria>
    - AFTER counts recorded; coverage delta computed and equal to the summed 221-02/03 found count.
    - Every new politician_images row: type='default', photo_license non-null, links to an in-scope politician_id.
    - >=3 newly-attached images (or all, if fewer) verified exactly 600x750 via PIL.
    - Brandon Smith and Gopal Ponangi each have a final disposition.
  </acceptance_criteria>
  <done>The coverage delta is measured and reconciled to the sourcing SUMMARYs, and attached images are machine-verified at 600x750.</done>
</task>

<task type="auto">
  <name>Task 2: Write the confirmed-blank register (221-CONFIRMED-BLANK.md)</name>
  <read_first>
    - `221-02-SUMMARY.md` and `221-03-SUMMARY.md` — the per-person honest-blank dispositions + search evidence.
    - `221-01-WORKLIST.md` — the full in-scope set, to confirm every person is accounted for (found OR blank).
  </read_first>
  <files>.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md</files>
  <action>
    Write 221-CONFIRMED-BLANK.md listing every in-scope officeholder intentionally left without a photo: politician_id, full_name, city, office, and the search evidence from the sourcing SUMMARYs (what sources were checked, why nothing usable was found). Group by city; call out the five zero-known-source cities (Blue Ridge, Farmersville, Lowry Crossing, Nevada, Saint Paul). Cross-check against the AFTER audit: every in-scope person from 221-01 must be either found (has an image row) or on this blank list — no person unaccounted for. State explicitly that these blanks are the correct terminal state (no public photo exists) and should NOT be re-litigated by a later phase without new source information.
  </action>
  <verify>
    <automated>test -f .planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md && grep -q "politician_id" .planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md</automated>
  </verify>
  <acceptance_criteria>
    - 221-CONFIRMED-BLANK.md exists, listing every intentionally-blank person with politician_id + city + office + search evidence.
    - Found-count + blank-count equals the 221-01 in-scope total (every person accounted for).
    - The five zero-source cities are explicitly called out.
  </acceptance_criteria>
  <done>A durable confirmed-blank register exists that accounts for every in-scope person and documents the evidence so blanks are not re-litigated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-close end-to-end spec + coverage spot-check</name>
  <action>Human-verify checkpoint — the operator reviews the coverage delta, spot-checks attached images end-to-end against the phase spec, and skims the confirmed-blank register. No code or DB rows are written in this task; it gates phase close.</action>
  <what-built>Coverage delta measured, images machine-verified at 600x750, and a confirmed-blank register written accounting for every in-scope officeholder.</what-built>
  <how-to-verify>
    1. Review the AFTER coverage counts + delta and confirm the delta matches the total headshots you approved across 221-02/03.
    2. Open 3+ attached images across both batches and confirm the phase spec end-to-end: real photo of the correct person, 4:5 crop (not stretched), eyes ~1/3 from top, head+shoulders, no text/graphics — and no placeholder or AI-generated face anywhere.
    3. Skim 221-CONFIRMED-BLANK.md and confirm every blank has real search evidence and that Brandon Smith + Gopal Ponangi each carry a final disposition.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe what fails.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sourcing SUMMARY claims → phase-close record | Reported found/blank counts must reconcile against the live DB, not be trusted as-is |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-221-07 | Repudiation | unverified coverage claim | medium | mitigate | AFTER audit re-queries the live DB and reconciles the delta to the SUMMARY found counts; every in-scope person must be found OR on the blank register |
| T-221-08 | Tampering | off-spec image slipping through | medium | mitigate | Python PIL dimension gate asserts 600x750 on a sample; blocking human spec spot-check across both batches |
</threat_model>

<verification>
- AFTER counts + delta reconciled to sourcing SUMMARYs; >=3 images verified 600x750 via PIL; confirmed-blank register accounts for every in-scope person; blocking human end-to-end spec spot-check.
</verification>

<success_criteria>
The coverage delta is documented and reconciled, attached images are verified at 600x750 4:5, and a confirmed-blank register accounts for every in-scope Collin County officeholder with evidence — proving the phase goal: real headshots where a public photo exists, documented honest blanks everywhere else.
</success_criteria>

<output>
Create `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-04-SUMMARY.md` when done — record BEFORE/AFTER counts, the coverage delta, the dimension-check results, and a pointer to 221-CONFIRMED-BLANK.md.
</output>
