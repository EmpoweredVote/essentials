---
phase: 104-va-headshots
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py
autonomous: true
requirements:
  - VA-GOV-06
must_haves:
  truths:
    - "99 VA House delegate headshots exist in politician_photos Supabase Storage bucket at 600x750 JPEG q90"
    - "HD-20 (external_id -5120020) is NEVER attempted (skipped entirely per D-04, D-17 — is_vacant=true)"
    - "Script uses house.vga.virginia.gov (not vga.virginia.gov which is dead) per 104-RESEARCH.md correction to D-03"
    - "Script uses full-res /delegate_photos/{H####}.jpg (NOT /thumbs/) to get 2500x2500 source images"
    - "DELEGATE_HID_MAP has exactly 99 entries (HD-1..HD-100 minus HD-20)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py"
      provides: "Standalone download/crop/resize/upload script for 99 VA delegates"
      contains: "DELEGATE_HID_MAP from 104-RESEARCH.md with HD-20 absent"
    - path: "Supabase Storage politician_photos/{politician_id}-headshot.jpg"
      provides: "99 uploaded headshot JPEGs at 600x750"
  key_links:
    - from: "DELEGATE_HID_MAP[district]"
      to: "house.vga.virginia.gov/delegate_photos/{H####}.jpg"
      via: "HTTP GET with browser User-Agent"
      pattern: "house\\.vga\\.virginia\\.gov/delegate_photos/H"
    - from: "HD-N ROSTER entry"
      to: "essentials.politicians.external_id = -5120000-N"
      via: "DB lookup at script run time"
      pattern: "WHERE external_id"
---

<objective>
Create and run `_tmp-va-delegates-headshots.py` to download, crop to 4:5, resize to 600x750 Lanczos q90, and upload headshots for 99 active VA House delegates (external_ids -5120001 through -5120100 EXCLUDING -5120020 which is vacant) per D-01, D-02, D-03 (corrected by RESEARCH), D-04, D-12, D-13, D-17.

**Critical correction from CONTEXT.md D-03:** the documented URL `vga.virginia.gov/delegate_photos/{H0000}.jpg` is dead (404). The verified working URL is `house.vga.virginia.gov/delegate_photos/{H####}.jpg` where `{H####}` is the **VGA internal member ID** (NOT the district number). Use the complete HD→H-ID mapping from 104-RESEARCH.md verbatim — HD-1 is H0219, NOT H0001.

Purpose: Cover the delegate portion of VA-GOV-06 (99 of 155 final politician_images rows).
Output: One Python script + 99 JPEG files in Supabase Storage, plus a deterministic SUCCESS/FAILED manifest block for Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-va-headshots/104-CONTEXT.md
@.planning/phases/104-va-headshots/104-RESEARCH.md
@C:/EV-Accounts/backend/scripts/_tmp-alexandria-headshots.py
@C:/EV-Accounts/backend/migrations/308_va_delegates.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write _tmp-va-delegates-headshots.py with verified DELEGATE_HID_MAP (99 entries, HD-20 absent)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-alexandria-headshots.py (template structure)
    - .planning/phases/104-va-headshots/104-RESEARCH.md (§"Delegate URL Pattern" complete 99-entry mapping + §"Verified Delegate Script Pattern" Python literal for DELEGATE_HID_MAP)
    - C:/EV-Accounts/backend/migrations/308_va_delegates.sql (canonical delegate names + external_ids; HD-20 confirmed vacant)
    - .planning/phases/104-va-headshots/104-CONTEXT.md (D-03 ORIGINAL — note research overrides; D-04 HD-20 skip; D-15 WHERE NOT EXISTS; D-17 vacant skip)
  </read_first>
  <action>
    Copy the Alexandria script structure exactly. Define `DELEGATE_PHOTO_BASE = 'https://house.vga.virginia.gov/delegate_photos/'` (note: `house.vga`, NOT bare `vga`). Define `DELEGATE_HID_MAP` as a Python dict mapping district int (1..100) to the H-ID string from 104-RESEARCH.md §"Verified Delegate Script Pattern" — copy that dict literal verbatim. The dict MUST have exactly 99 entries: keys 1..19, 21..100. Key 20 MUST be absent (NOT mapped to None, NOT mapped to a placeholder — physically missing from the dict). Generate ROSTER by iterating `sorted(DELEGATE_HID_MAP.keys())` (yielding 99 districts in order), constructing for each: `external_id = -5120000 - district` (so HD-1 = -5120001, HD-100 = -5120100, HD-20 absent because key 20 is absent from the dict), `full_name` pulled from a hardcoded NAMES dict copied from migration 308 (HD-1 Patrick A. Hope through HD-100 Robert S. Bloxom Jr.; HD-20 entry MUST be absent from NAMES too — DO NOT include "Michelle Maldonado" placeholder), `section_label='VA House of Delegates'`, `title=f'Delegate HD-{district}'`, `source_url = DELEGATE_PHOTO_BASE + DELEGATE_HID_MAP[district] + '.jpg'`, `source_domain='house.vga.virginia.gov'`, `is_required=True`. Resolve `politician_id` at runtime via DB lookup on external_id. Manifest header: "=== VA DELEGATES HEADSHOT MANIFEST ===". Hard-gate: if total successes &lt; 99, sys.exit(1) with the failing district list. Polite throttling: `time.sleep(0.5)` between downloads is already in the Alexandria template — keep it (99 downloads x 0.5s = ~50s overhead, acceptable). Add an explicit assert at script start: `assert 20 not in DELEGATE_HID_MAP, 'HD-20 must be absent from DELEGATE_HID_MAP per D-04'` and `assert len(DELEGATE_HID_MAP) == 99, f'Expected 99 delegate entries, got {len(DELEGATE_HID_MAP)}'`.
  </action>
  <verify>
    <automated>python -c "import importlib.util; spec=importlib.util.spec_from_file_location('s','C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py'); m=importlib.util.module_from_spec(spec); import sys; sys.modules['s']=m; exec(open('C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py').read().split('def main')[0]); assert len(DELEGATE_HID_MAP)==99; assert 20 not in DELEGATE_HID_MAP; assert DELEGATE_HID_MAP[1]=='H0219'; assert DELEGATE_HID_MAP[100]=='H0267'; print('hid-map-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py exists
    - File contains string `DELEGATE_PHOTO_BASE = 'https://house.vga.virginia.gov/delegate_photos/'`
    - File does NOT contain string `vga.virginia.gov/delegate_photos` UNLESS preceded by `house.` (the dead URL pattern must not appear standalone)
    - File does NOT contain string `/thumbs/` (full-res only)
    - File contains string `'H0219'` (HD-1 mapping)
    - File contains string `'H0267'` (HD-100 mapping)
    - File contains string `assert 20 not in DELEGATE_HID_MAP`
    - File contains string `assert len(DELEGATE_HID_MAP) == 99`
    - Loaded module has `len(DELEGATE_HID_MAP) == 99` and `20 not in DELEGATE_HID_MAP`
    - File contains string `BUCKET = 'politician_photos'`
    - File contains string `SELECT id FROM` AND `WHERE external_id`
  </acceptance_criteria>
  <done>
    Script exists, HID map is exactly 99 entries with HD-20 absent, structural assertions are in source. Task 2 runs the script.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run _tmp-va-delegates-headshots.py and verify all 99 delegate headshots uploaded</name>
  <files>(no new files; runs the script written in Task 1)</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py (verify ROSTER count = 99)
    - .planning/phases/104-va-headshots/104-VALIDATION.md (post-run verification protocol)
  </read_first>
  <action>
    Run `python C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py` from C:/EV-Accounts/backend. Capture full stdout/stderr (will be long — 99 entries plus throttling). Confirm manifest contains exactly 99 SUCCESS lines and that HD-20 is referenced nowhere (no SUCCESS line for -5120020, no FAILED line for -5120020). If any delegate returns HTTP 500 (signaling H-ID is for a former member — see 104-RESEARCH.md Pitfall 2), record the district + H-ID in the SUMMARY "Gaps for Plan 05" section and continue processing; sys.exit(1) only if total successes &lt; 99. After successful run, HEAD-check 10 random delegate CDN URLs to confirm HTTP 200. Save manifest text into 104-03-SUMMARY.md. Also run a sanity SQL query: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id = -5120020 AND is_vacant = true` — must return 1, confirming HD-20 is still vacant at script time (no need to re-add it if it returns 0; just note the change).
  </action>
  <verify>
    <automated>python -c "import psycopg2; e={}; [e.update({k.strip():v.strip()}) for k,v in (l.split('=',1) for l in open('C:/EV-Accounts/backend/.env') if '=' in l and not l.strip().startswith('#'))]; c=psycopg2.connect(e['DATABASE_URL']+'?sslmode=require'); cur=c.cursor(); cur.execute('SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5120100 AND -5120001 AND external_id != -5120020'); print('delegates-found',cur.fetchone()[0])"</automated>
  </verify>
  <acceptance_criteria>
    - Script exit code 0
    - Captured stdout contains string `=== VA DELEGATES HEADSHOT MANIFEST ===`
    - Captured stdout contains exactly 99 lines matching pattern `^SUCCESS: -5120`
    - Captured stdout contains 0 lines containing `-5120020` (no attempt for HD-20)
    - Captured stdout contains 0 lines matching pattern `^FAILED: `
    - Captured stdout contains string `TOTALS: VA Delegates 99/99 succeeded`
    - HEAD request to 10 sampled CDN URLs all return HTTP 200
    - SQL `SELECT COUNT(*) FROM essentials.politicians WHERE external_id = -5120020 AND is_vacant = true` returns 1
    - 104-03-SUMMARY.md exists with verbatim manifest under heading `## Manifest (Plan 05 Input)`
  </acceptance_criteria>
  <done>
    99 delegate headshots in Storage; HD-20 untouched; manifest captured for Plan 05.
  </done>
</task>

</tasks>

<verification>
After both tasks: 99 JPEG objects in `politician_photos` bucket keyed by politician UUIDs resolved from external_id -5120001..-5120100 (excluding -5120020). Source images are full-res 2500x2500 from house.vga.virginia.gov — excellent quality, no upscale needed. HD-20 has zero presence in Storage, manifest, or SUMMARY.
</verification>

<success_criteria>
1. `_tmp-va-delegates-headshots.py` exists with `DELEGATE_HID_MAP` of exactly 99 entries (HD-20 absent)
2. Script uses `house.vga.virginia.gov` (not `vga.virginia.gov`)
3. Running the script produces 99 SUCCESS lines, 0 references to -5120020, and exit code 0
4. 99 Storage objects accessible at `{CDN_BASE}/{politician_id}-headshot.jpg`
</success_criteria>

<output>
Create `.planning/phases/104-va-headshots/104-03-SUMMARY.md` when done, including:
- Verbatim manifest block (Plan 05 input)
- Per-delegate source URL + H-ID + original dimensions + crop dimensions
- HD-20 confirmation (still is_vacant=true at script run time; no row attempted)
- Any delegates with HTTP 500 or other failures (gap input for follow-up)
</output>
