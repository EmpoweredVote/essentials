---
phase: 145-lancaster-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["145-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql
autonomous: false
requirements: [LANC-01]

must_haves:
  truths:
    - "R. Rex Parris (external_id -200795) has a 600×750 headshot in Supabase Storage politician_photos + a type='default' politician_images row (press_use), sourced from Wikimedia Commons (HTTP 200 with WIKIMEDIA_HEADERS — bot UA, NOT Chrome UA)"
    - "Hughes-Leslie (-201279), Mann (-201281), White (-700655), Castellanos (-700656) each get an official 600×750 headshot where a portrait is obtainable — cityoflancasterca.org is Akamai/WAF-403 so these are sourced via checkpoint:human-verify (browser); AVAQMD CDN (Referer header) for Mann/Hughes-Leslie and Ballotpedia for White/Castellanos are documented fallbacks"
    - "Each new headshot shows the correct person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 FIRST (not stretched), then resized 600×750 Lanczos q90 JPEG, uploaded to politician_photos/{uuid}-headshot.jpg; photo_origin_url set"
    - "Genuine gaps (members with no obtainable official portrait) are documented honestly — NO fabricated photos, NO campaign photo uploaded as type='default'"
    - "Crist (686320, retired) needs no new headshot; his existing image is left as-is (re-source only if a quality pass improves it — Claude's discretion)"
    - "Migration 912 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 911)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql"
      provides: "Audit-only headshot INSERT rows (per member with an obtainable portrait) + photo_origin_url backfill + full-roster image audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      to: "essentials.politician_images.url (type='default', press_use)"
      via: "INSERT politician_images scoped by politician_id, guarded NOT EXISTS"
      pattern: "'default'"
---

<objective>
Give the current 5-member Lancaster roster clean official headshots and confirm single canonical-image
coverage. After Plan 02 the survivor chamber holds Parris (Mayor) + Hughes-Leslie, Mann, White,
Castellanos (Councilmembers). Image state (DB-verified 2026-06-19):
- Parris (-200795): 0 images — SOURCE required (Wikimedia Commons — automated, bot UA)
- Hughes-Leslie (-201279): 0 images — SOURCE required (cityoflancasterca.org WAF; AVAQMD CDN fallback)
- Mann (-201281): 0 images — SOURCE required (cityoflancasterca.org WAF; AVAQMD CDN fallback)
- White (-700655): 0 images — SOURCE required (cityoflancasterca.org WAF; Ballotpedia fallback)
- Castellanos (-700656): 0 images — SOURCE required (cityoflancasterca.org WAF; Ballotpedia fallback)
- Crist (686320, retired): 1 existing image — no new headshot needed (discretionary re-source only)

⚠️ cityoflancasterca.org is Akamai/WAF-blocked — curl AND WebFetch return HTTP 403 (Pitfall 4; same WAF
as Glendale's glendaleca.gov in Phase 144). The official councilmember-page portraits for Hughes-Leslie,
Mann, White, and Castellanos CANNOT be fetched programmatically. They MUST be sourced via a
checkpoint:human-verify — the executor (or user) opens each page in a browser and downloads the official
portrait. Parris is the one automated source: Wikimedia Commons returns HTTP 200 but ONLY with
WIKIMEDIA_HEADERS (bot UA — a Chrome UA returns 429). Documented fallbacks before going to the browser:
AVAQMD CDN imgix URLs (try Referer: https://www.avaqmd.ca.gov/) for Mann + Hughes-Leslie; Ballotpedia
candidate photos for White + Castellanos.

Processing (NON-NEGOTIABLE per project memory): download portrait → crop to 4:5 FIRST (never stretch —
crop only, eyes ~1/3 from top, full head+shoulders) → resize 600×750 Lanczos q90 JPEG (Python Pillow) →
upload to Supabase Storage politician_photos bucket as {politician_uuid}-headshot.jpg (service-role key
in C:/EV-Accounts/backend/.env) → INSERT politician_images (type='default', photo_license='press_use');
set photo_origin_url. Reject any source with superimposed text/graphics over the face — document a gap
honestly rather than upload a bad photo.

Purpose: A voter sees the right face next to each official. Wrong-person, text-overlaid, or stretched
photos destroy trust. Output: Migration 912 (audit-only — NOT registered in schema_migrations) + each
obtainable portrait sourced + a human checkpoint confirming the correct person + correct crop; honest
gaps documented (White/Castellanos may have no official portrait yet — just sworn in April 28 2026).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/145-lancaster-deep-seed/145-RESEARCH.md
@.planning/phases/145-lancaster-deep-seed/145-02-SUMMARY.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §Headshot Sources). cityoflancasterca.org is WAF-blocked. -->

Parris (-200795) — AUTOMATED (Wikimedia Commons, bot UA required):
  https://upload.wikimedia.org/wikipedia/commons/2/2d/R._Rex_Parris%2C_mayor_of_Lancaster%2C_CA.jpg
  HTTP 200 with WIKIMEDIA_HEADERS (bot UA); Chrome UA → 429. 1800×2283 source. CC-BY-SA → press_use OK.

WAF-blocked (cityoflancasterca.org — Akamai 403 to all automation; require BROWSER download):
  Hughes-Leslie (-201279): cityoflancasterca.org councilmember page
    fallback: AVAQMD board page — try CDN https://streamline.imgix.net/.../Lauren%20Hughes-Leslie.jpg
             with header Referer: https://www.avaqmd.ca.gov/  (CDN 403 without Referer)
  Mann (-201281): cityoflancasterca.org councilmember page
    fallback: AVAQMD CDN https://streamline.imgix.net/.../KenMann.jpg with Referer header
  White (-700655): https://www.cityoflancasterca.org/government/city-officials/councilmember-white (WAF)
    fallback: Ballotpedia candidate photo (Cedric White, Lancaster City Council At-large 2026)
  Castellanos (-700656): cityoflancasterca.org/government/city-officials/councilmember-rocio-castellanos (WAF)
    fallback: Ballotpedia candidate photo; candidate filing statement PDF may have a photo
  (White + Castellanos just sworn in April 28 2026 — official portrait may not exist yet → honest gap OK)

Crist (686320, retired): 1 existing image — leave as-is; re-source only if a quality pass improves it.

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face (reject "Re-Elect"-style banners)

Storage + DB:
  - Bucket: politician_photos  ·  Path: {politician_uuid}-headshot.jpg  ·  service-role key in C:/EV-Accounts/backend/.env
  - politician_images: type='default', photo_license='press_use'
  - UPDATE politicians.photo_origin_url to the source page (Wikimedia for Parris; cityoflancasterca.org/AVAQMD/Ballotpedia for others) where NULL
  - Each INSERT guarded WHERE NOT EXISTS on politician_id → exactly ONE type='default' row per member, no duplicate

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql or psql -f). It does
NOT register in schema_migrations (same convention as SC 896 / Glendale 904). On-disk counter
authoritative; ledger stays at whatever Plan 02 left it (911).

New official UUIDs resolved by external_id at apply time — do not hardcode.

Pattern migration: C:/EV-Accounts/backend/migrations/904_glendale_headshots.sql (audit-only INSERT +
WAF-browser-source flow; see 144-03-SUMMARY.md). Also 896_santa_clarita_headshots.sql.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source Parris (Wikimedia, automated) + obtain the 4 WAF/fallback portraits; crop/resize/upload</name>
  <files>C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/904_glendale_headshots.sql (audit-only INSERT + WAF-browser-source + Wikimedia/bot-UA flow; download/crop/upload pipeline)
    - .planning/phases/144-glendale-deep-seed/144-03-SUMMARY.md (§What was done — Parris-style Wikimedia source + WAF browser flow + re-source-existing-image pattern)
    - .planning/phases/145-lancaster-deep-seed/145-RESEARCH.md (§Headshot Sources — exact URLs, WAF note, AVAQMD Referer trick, Ballotpedia fallbacks, White/Castellanos gap; §Common Pitfalls 4)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots)
  </read_first>
  <action>
    Parris (-200795) is the one automated source: download the Wikimedia Commons portrait
    (https://upload.wikimedia.org/wikipedia/commons/2/2d/R._Rex_Parris%2C_mayor_of_Lancaster%2C_CA.jpg)
    using WIKIMEDIA_HEADERS (bot UA — NOT Chrome UA, which returns 429). For Hughes-Leslie (-201279) and
    Mann (-201281): first TRY the AVAQMD imgix CDN URLs with header Referer: https://www.avaqmd.ca.gov/
    (RESEARCH §Headshot Sources); if the CDN still 403s, fall through to the browser flow in Task 3. For
    White (-700655) and Castellanos (-700656): try Ballotpedia candidate photos; cityoflancasterca.org is
    WAF-403 so the official portrait needs the browser flow — and these two just got sworn in (April 28
    2026), so an official portrait may not exist yet (honest gap is acceptable, do NOT upload a campaign
    photo as type='default'). For EVERY obtained portrait: crop to 4:5 (crop only — never stretch; eyes
    ~1/3 from top, full head+shoulders), then resize to 600×750 Lanczos q90 JPEG (Pillow). Reject any
    image with superimposed text/graphics over the face — document a gap honestly. Resolve each politician
    UUID by external_id, then upload to Supabase Storage bucket politician_photos as {uuid}-headshot.jpg
    (service-role key in C:/EV-Accounts/backend/.env). Record each public Storage URL + its source page
    for Task 2. Crist (686320, retired): no new headshot (leave the existing image; re-source only if a
    quality pass clearly improves it — Claude's discretion). Document every genuine gap (which member,
    why no portrait) for the summary.
  </action>
  <verify>
    <automated>For each sourced member: the processed local file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('parris.jpg').size)"` → (600, 750)) and the Storage object {uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - Parris portrait obtained from Wikimedia (bot UA / WIKIMEDIA_HEADERS, not Chrome UA), processed to exactly 600×750, 4:5, not stretched, no superimposed text/graphics, uploaded to politician_photos/{uuid}-headshot.jpg (HTTP 200)
    - Mann + Hughes-Leslie attempted via AVAQMD CDN (Referer header); if blocked, deferred to the Task 3 browser flow — documented either way
    - White + Castellanos attempted via Ballotpedia; if no obtainable official portrait, documented as an honest gap (NOT a campaign photo uploaded as type='default')
    - Every uploaded portrait is the correct person, 600×750, 4:5, no overlays
    - Public Storage URLs + source pages recorded for Task 2; all genuine gaps documented
  </acceptance_criteria>
  <done>Parris sourced from Wikimedia (automated); Mann/Hughes-Leslie/White/Castellanos sourced via CDN/Ballotpedia where obtainable (else deferred to browser / documented as gaps); processed images live in Storage at canonical paths.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 912 (headshot rows) and audit full-roster image coverage</name>
  <files>C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/904_glendale_headshots.sql (INSERT politician_images + photo_origin_url backfill + roster image audit; audit-only apply, ledger-unchanged check)
    - .planning/phases/144-glendale-deep-seed/144-03-SUMMARY.md (full-roster coverage audit query + audit-only ledger check + documented-gap handling)
    - .planning/phases/145-lancaster-deep-seed/145-RESEARCH.md (§Validation Architecture Wave 3 checks, §Common Pitfalls 4)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql. For each member with an obtained
    portrait (resolved by external_id), INSERT a politician_images row (url = the Storage public URL from
    Task 1, type='default', photo_license='press_use') guarded WHERE NOT EXISTS on politician_id, and
    UPDATE politicians.photo_origin_url to the source page where NULL. Do NOT insert rows for members
    documented as genuine gaps (e.g. White/Castellanos if no official portrait yet) — leave them imageless
    and record the gap. Do NOT touch Crist's existing image (retired). Apply via raw SQL
    (mcp__supabase-local execute_sql or psql -f) — do NOT run through the schema_migrations ledger
    (audit-only). Then run the full-roster coverage audit: LEFT JOIN the 5 current Lancaster officials
    (-200795/-201279/-201281/-700655/-700656) to politician_images and confirm only the documented gaps
    are missing an image, and confirm no current official has >1 type='default' row. Verify MAX(version)
    in schema_migrations is UNCHANGED by this file (stays at 911).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT p.external_id, COUNT(pi.*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (-200795,-201279,-201281,-700655,-700656) GROUP BY p.external_id HAVING COUNT(pi.*) > 1;" returns 0 rows (no current official has >1 default image) AND psql "$DATABASE_URL" -c "SELECT photo_license FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id=-200795 AND pi.type='default';" returns press_use (Parris image present)</automated>
  </verify>
  <acceptance_criteria>
    - Parris (-200795) has exactly one type='default' politician_images row pointing at his Storage URL, photo_license='press_use'
    - Each other current member with an obtained portrait has exactly one type='default' press_use row; documented-gap members have 0 (honest, not faked)
    - photo_origin_url populated for every sourced member
    - Full-roster LEFT JOIN missing-image query returns only the documented gaps (e.g. White/Castellanos if no portrait yet)
    - No current Lancaster official has >1 type='default' image row
    - Crist's existing image untouched (retired)
    - schema_migrations MAX(version) unchanged by migration 912 (audit-only confirmed; stays 911)
    - Migration is idempotent (re-apply = 0 rows changed)
  </acceptance_criteria>
  <done>912 applied; Parris + each obtainable member has a single press_use headshot row; documented gaps left honestly imageless; full-roster image audit clean (≤1 default each); ledger untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-source the WAF-blocked portraits + visually verify the new headshots</name>
  <action>Pause for the user to (a) download the WAF-blocked cityoflancasterca.org official portraits for any member not obtained via Wikimedia/AVAQMD/Ballotpedia in a browser and hand them to the executor for processing, and (b) visually verify all new portraits per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>New official headshots (Parris via Wikimedia; Mann/Hughes-Leslie via AVAQMD CDN where it worked; White/Castellanos via Ballotpedia where available) processed to 600×750 and live in Supabase Storage + politician_images; documented gaps recorded; Crist (retired) left as-is. cityoflancasterca.org is Akamai/WAF-blocked so the remaining official portraits require manual browser download.</what-built>
  <how-to-verify>
    1. For any member still missing a portrait, open their cityoflancasterca.org councilmember page in a browser, download the official portrait, and provide it to the executor (curl/WebFetch will 403).
    2. After processing, confirm each photo is the CORRECT person (cross-check against the official page / Ballotpedia).
    3. Confirm no superimposed text/graphics over the face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    4. Confirm each current member shows exactly one canonical image (no duplicate type='default' rows).
    5. Confirm White/Castellanos either have a correct official portrait or are documented as an honest gap (no campaign photo faked as type='default'); confirm Crist (retired) is untouched.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which photo(s) need re-sourcing / re-cropping.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with a limited external image source. No untrusted user input reaches the DB —
uploads + image-row writes use service-role/operator against production. Threat surface = wrong-person
or distorted/overlaid photos, a fabricated/campaign photo filling a real gap, or accidental ledger
registration — mitigated by the crop-then-resize/no-stretch rule, official/authoritative sources, a
blocking human-verify checkpoint, the honest-gap rule, and the audit-only raw-SQL apply.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cityoflancasterca.org (WAF-blocked) → browser → local processing | External image source; WAF blocks automation so a human downloads; could be wrong-person or text-overlaid. |
| Wikimedia / AVAQMD CDN / Ballotpedia → local processing | External fallback image sources (Parris automated; Mann/Hughes-Leslie/White/Castellanos fallbacks). |
| operator → Supabase Storage + production DB | Uploads + image-row INSERT against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-13 | Spoofing | Wrong-person headshot uploaded for a member | mitigate | Portraits sourced from official cityoflancasterca.org pages / Wikimedia / authoritative fallbacks; blocking human-verify checkpoint cross-checks each face |
| T-145-14 | Tampering | Image stretched / aspect-distorted / text-overlaid | mitigate | Crop-to-4:5-FIRST-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; human checkpoint |
| T-145-15 | Spoofing | Fabricated / campaign photo uploaded for White/Castellanos to fill a gap | mitigate | Honest-gap rule — if no official portrait yet, leave imageless and document; never seed a campaign photo as type='default' |
| T-145-16 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged (stays 911) before/after |
| T-145-17 | Tampering | Duplicate image rows introduced for a member | mitigate | Each INSERT guarded WHERE NOT EXISTS on politician_id; post-audit confirms ≤1 type='default' per official |
| T-145-18 | DoS | Repeated automated hits to the WAF / 429 from Wikimedia | mitigate | Use WIKIMEDIA_HEADERS (bot UA) for Wikimedia; do NOT hammer cityoflancasterca.org with curl (it 403s) — go straight to the browser flow |
</threat_model>

<verification>
After 912 + checkpoint:
1. Parris (-200795) has exactly one type='default' press_use image; each other obtainable member likewise
2. full-roster LEFT JOIN missing-image query (5 current officials) returns only the documented gaps
3. no current official has >1 type='default' image
4. Crist's existing image untouched (retired)
5. schema_migrations MAX(version) unchanged by 912 (stays 911)
6. human checkpoint approved (correct person, correct crop, no overlays; gaps documented, not faked)
</verification>

<success_criteria>
Parris has a correct 600×750 Wikimedia portrait; each current member with an obtainable official photo
has a single press_use canonical image; genuine gaps (notably newly-sworn White/Castellanos) are
honestly documented and left imageless rather than faked; no wrong-person/overlaid/stretched photos; the
ledger counter is preserved.
</success_criteria>

<output>
Create `.planning/phases/145-lancaster-deep-seed/145-03-SUMMARY.md` when done.
</output>
