---
phase: 172-elections-page-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ElectionsView.jsx
  - src/pages/Results.jsx
autonomous: false
requirements: [DARK-03, BANR-05]

must_haves:
  truths:
    - "A user viewing the Elections tab in dark mode sees the same Figma dark treatment as the Representatives (Results) tab — same page background, section areas, tiles, and accent color, with no faint-gray-on-dark legibility failures."
    - "Scrolling the Elections page shows a SectionBanner divider before each tier (City -> State -> Federal), location-aware per jurisdiction, with the same graceful gradient fallback when no art exists."
    - "The Elections city/state banner labels match the Representatives tab for the same search (no divergence) because the banner inputs are threaded from the parent, not re-derived in the child."
    - "Existing Elections behaviors are preserved unchanged: seeded per-session candidate ordering, 'Running Unopposed' / 'No candidates have filed' rendering, elections/me Connected auto-load, and the Compass / MiniCompass overlay."
    - "Light mode renders exactly as before (no light-branch regression)."
  artifacts:
    - path: "src/components/ElectionsView.jsx"
      provides: "Re-themed dark palette + SectionBanner tier dividers + new banner-input props"
      contains: "SectionBanner"
    - path: "src/pages/Results.jsx"
      provides: "ElectionsView call site passing buildingImageMap / representingCity / userState / stateNames"
      contains: "buildingImageMap={buildingImageMap}"
  key_links:
    - from: "src/pages/Results.jsx (<ElectionsView> call site)"
      to: "src/components/ElectionsView.jsx (prop destructure)"
      via: "props: buildingImageMap, representingCity, userState, stateNames"
      pattern: "buildingImageMap=\\{buildingImageMap\\}"
    - from: "src/components/ElectionsView.jsx (tier .map)"
      to: "src/components/SectionBanner.jsx"
      via: "<SectionBanner tier=... locationName=... imageUrl=... /> per tier"
      pattern: "<SectionBanner"
---

<objective>
Bring `src/components/ElectionsView.jsx` to full parity with the already-shipped Results
(Representatives) page: the same Phase-169 dark palette (DARK-03) and the same Phase-170
`SectionBanner` tier dividers (BANR-05). ElectionsView was out of Phase 169/170 file scope, so it
still carries stale dark literals and the old `<span>` tier eyebrow.

Purpose: Elections and Representatives are two tabs of the same page; today they look subtly
mismatched on dark (old navy `#1a2235` vs new `#161b22`, old accent `#59b0c4` vs new `#00c8d7`) and
Elections still shows a plain tier eyebrow instead of the location-aware banner divider.

Output: ElectionsView re-themed to the new tokens and rendering `SectionBanner` per tier, fed by
banner inputs threaded down from Results.jsx (the single source of truth — never re-derived in the
child). All four preserved behaviors untouched (color-only changes). Verified by build gate +
human visual-parity sign-off.

This is PARITY work — `src/pages/Results.jsx` is the canonical, in-production template for every
change. The risk is divergence, not invention: copy the parent's proven values exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/172-elections-page-parity/172-RESEARCH.md
@.planning/phases/172-elections-page-parity/172-PATTERNS.md
@.planning/phases/172-elections-page-parity/172-VALIDATION.md

@src/components/ElectionsView.jsx
@src/pages/Results.jsx
@src/components/SectionBanner.jsx
@src/lib/buildingImages.js

<interfaces>
<!-- SectionBanner contract (src/components/SectionBanner.jsx) — reuse verbatim, NO signature change. -->
SectionBanner props:
  tier          'city' | 'state' | 'federal'   (NOTE: Local maps to 'city')
  locationName  string                          (text shown over the banner)
  imageUrl      string | null                   (null/undefined => tier-tinted gradient fallback, handled internally)
  stats, featureIcons  optional scaffolding slots (render nothing) — do NOT pass

<!-- Banner inputs already computed in Results.jsx — pass DOWN, do NOT recompute in the child. -->
Results.jsx:
  STATE_NAMES        module const  L93   (abbrev -> full name map; lives in Results.jsx, not a shared lib)
  representingCity   useMemo       L1045
  userState          useMemo       L1070
  buildingImageMap   useMemo       L1083 (= getBuildingImages(representingCity, userState) -> { Local, State, Federal })
  <ElectionsView>    call site     L1975 (currently passes 6 props)

<!-- ElectionsView.jsx current prop destructure: L247-255 (elections, loading, tierFilter, hideWithdrawn, compassMode, onCandidateClick, isDark). -->

<!-- Exact Results.jsx banner-selection precedent to MIRROR (L1864-1882):
  tier === 'Local'   -> <SectionBanner tier="city"    locationName={representingCity && userState ? `${representingCity}, ${userState}` : (representingCity || 'Your City')} imageUrl={buildingImageMap.Local} />
  tier === 'State'   -> <SectionBanner tier="state"   locationName={(userState && STATE_NAMES[userState]) || userState || 'Your State'}                                      imageUrl={buildingImageMap.State} />
  tier === 'Federal' -> <SectionBanner tier="federal" locationName="United States"                                                                                          imageUrl={buildingImageMap.Federal} />
  else null -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dark-token parity swap (DARK-03) — literals, loading skeleton, empty-state box</name>
  <files>src/components/ElectionsView.jsx</files>
  <read_first>
    - src/components/ElectionsView.jsx (the file being edited; current dark literals at L510, 537, 568, 576, 710, 742, 775; loading skeleton L477-501; "No candidates have filed" box L635-650)
    - .planning/phases/172-elections-page-parity/172-PATTERNS.md (Change 1, 1b, 1c — exact token map + the Results SkeletonCard `dark:bg-gray-700` precedent + the isDark empty-box target)
    - src/pages/Results.jsx (SkeletonCard/SkeletonSection L221-243 for the `bg-gray-200 dark:bg-gray-700` skeleton precedent; new-palette literals used throughout)
  </read_first>
  <action>
    Swap the stale Phase-169 dark literals to the Phase-169 canonical tokens at the cited sites in
    `src/components/ElectionsView.jsx`, in DARK branches only — never alter the light branch of any
    `isDark ?` ternary:
    - L510: `dark:text-[#59b0c4]` -> `dark:text-[#00c8d7]` (empty-state heading).
    - L537: tier eyebrow `<span>` `color: isDark ? '#59b0c4'` -> `'#00c8d7'`. (This `<span>` is
      replaced wholesale by the banner in Task 2; update the value here so the file is clean if the
      span survives in any path, but Task 2 owns its removal.)
    - L568: branch-header label `color: isDark ? '#59b0c4'` -> `'#00c8d7'`.
    - L576: branch-header divider line `backgroundColor: isDark ? '#59b0c4'` -> `'#00c8d7'`.
    - L710: PoliticianCard inline `isDark ? { backgroundColor: '#1a2235', borderColor: '#2d3f5a' }`
      -> `{ backgroundColor: '#161b22', borderColor: '#2d3748' }`.
    - L742 and L775: MiniCompass overlay panel `backgroundColor: isDark ? '#1a2235'` -> `'#161b22'`
      (stacked-mobile panel and desktop side-overlay panel respectively).
    Leave L535 as-is (`backgroundColor: isDark ? 'transparent' : tierStyle.bg`) — the banner owns the
    dark tier visual; keep the light band path.
    Loading skeleton (L477-501): add `dark:bg-gray-700` to every `bg-gray-200` block, matching the
    Results SkeletonCard convention (do not change the existing `bg-gray-200` light class).
    "No candidates have filed" box (L635-650): wrap each hardcoded color in an `isDark ?` ternary —
    dark surface `#161b22` (light keeps `pillars.empower.light`); left border `#00c8d7` (light keeps
    `pillars.empower.textColor`); title color `#e6edf3` (light keeps `#1C1C1C`); subtext color
    `#8b949e` (light keeps `#6B7280`). The `#8b949e` muted floor is the FAINTEST allowed on dark —
    never fainter. Leave the unopposed/withdrawn translucent overlay badges (L715-724) untouched —
    their `rgba(0,0,0,*)` overlays read on both modes.
    Do NOT touch any seeded-shuffle, dedup, tier/branch ordering, filter, or compass logic — this
    task is color-only.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -nE '1a2235|2d3f5a|59b0c4' src/components/ElectionsView.jsx; test -z "$(grep -nE '1a2235|2d3f5a|59b0c4' src/components/ElectionsView.jsx)" && echo PASS_NO_STALE_LITERALS || echo FAIL_STALE_LITERALS_REMAIN</automated>
    <automated>cd "C:/Transparent Motivations/essentials" && npx vitest run && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ElectionsView.jsx` contains zero occurrences of `1a2235`, `2d3f5a`, or `59b0c4`
      (grep returns no matches).
    - `src/components/ElectionsView.jsx` contains `#00c8d7`, `#161b22`, and `#2d3748` (the new tokens).
    - The loading skeleton blocks each carry `dark:bg-gray-700` alongside `bg-gray-200`.
    - The "No candidates have filed" box wraps surface/border/title/subtext colors in `isDark ?`
      ternaries; no color on dark is fainter than `#8b949e`.
    - Every edited site keeps its original light-branch value unchanged.
    - `npx vitest run && npx vite build` exits 0 (existing `src/lib/buildingImages.test.js` still passes).
  </acceptance_criteria>
  <done>
    ElectionsView renders the new Phase-169 dark palette at every previously-stale site; loading
    skeleton and empty-state box are dark-legible; build + lib tests green; light mode untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Thread banner inputs + insert SectionBanner per tier (BANR-05)</name>
  <files>src/pages/Results.jsx, src/components/ElectionsView.jsx</files>
  <read_first>
    - src/pages/Results.jsx (the `<ElectionsView>` call site L1975-1987; the banner-selection precedent L1864-1882; the derivations `STATE_NAMES` L93, `representingCity` L1045, `userState` L1070, `buildingImageMap` L1083 — pass these down, do not move them)
    - src/components/ElectionsView.jsx (prop destructure L247-255; import block L1-13; the tier loop L527-538 where the `<span>` eyebrow at L536-538 is replaced)
    - src/components/SectionBanner.jsx (the component contract — tier/locationName/imageUrl; reuse verbatim)
    - .planning/phases/172-elections-page-parity/172-PATTERNS.md (Change 2 + Change 3 — exact banner shape, tier-name mapping Local->city, placement-inside-the-loop, and the prop-threading anti-pattern)
  </read_first>
  <action>
    Thread the banner inputs from parent to child, then render `SectionBanner` per tier — mirroring
    Results.jsx exactly.
    In `src/pages/Results.jsx` at the `<ElectionsView>` call site (L1975-1987), ADD four props after
    the existing six (do not remove or reorder the existing ones):
    `buildingImageMap={buildingImageMap}`, `representingCity={representingCity}`,
    `userState={userState}`, `stateNames={STATE_NAMES}`. Use the already-computed values at L93 /
    L1045 / L1070 / L1083 — do NOT recompute and do NOT refactor `STATE_NAMES` into a shared lib
    (out of scope).
    In `src/components/ElectionsView.jsx`:
    - Add the import (mirror Results' import, but `./SectionBanner.jsx` because this file lives in
      `src/components/`): import the default export `SectionBanner` from `./SectionBanner.jsx`.
    - Extend the prop destructure (L247-255) to accept the four new props with safe defaults:
      `buildingImageMap = {}`, `representingCity = null`, `userState = null`, `stateNames = {}`.
    - Inside the existing tier loop `election.hierarchy.filter(...).map(({ tier, bodies }) => { ... })`
      (L527 onward), build a `banner` element keyed off `tier` exactly as Results does (Local->`city`,
      State->`state`, Federal->`federal`, anything else -> `null`):
        Local:   tier="city",    locationName = representingCity && userState ? `${representingCity}, ${userState}` : (representingCity || 'Your City'), imageUrl = buildingImageMap?.Local
        State:   tier="state",   locationName = (userState && stateNames?.[userState]) || userState || 'Your State',                                     imageUrl = buildingImageMap?.State
        Federal: tier="federal", locationName = "United States",                                                                                          imageUrl = buildingImageMap?.Federal
      Use `stateNames` (the threaded `STATE_NAMES` map) where Results uses its local `STATE_NAMES`.
    - Render `{banner}` immediately before the tier's body content, REPLACING the `<span>` tier
      eyebrow at L536-538 (remove that `<span>` and its wrapping `<div className="mb-3">` if it then
      holds nothing). Render the banner once per tier INSIDE the `.map(...)` (not outside the
      `processedElections.map` election loop) so banners do not double-render. Keep the outer tier
      `<div>` with `backgroundColor: isDark ? 'transparent' : tierStyle.bg` (light band preserved).
      The banner renders in both light and dark for true parity with Results (SectionBanner's dark
      styling applies regardless) — the light/dark banner-parity choice is confirmed at the Task 3
      checkpoint.
    Do NOT call `getBuildingImages`, `parseCityFromAddress`, `parseStateFromAddress`, or `useTheme()`
    inside ElectionsView (single source of truth = the parent). Do NOT change SectionBanner's
    signature. Do NOT touch seeded-shuffle, dedup, ordering, withdrawn/unopposed/empty logic, the
    MiniCompass overlay, or the `elections/me` auto-load (which lives in the parent, not here).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -nE 'buildingImageMap=\{buildingImageMap\}|representingCity=\{representingCity\}|userState=\{userState\}|stateNames=\{STATE_NAMES\}' src/pages/Results.jsx</automated>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -nE "import SectionBanner from './SectionBanner" src/components/ElectionsView.jsx && grep -cE '<SectionBanner' src/components/ElectionsView.jsx</automated>
    <automated>cd "C:/Transparent Motivations/essentials" && npx vitest run && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Results.jsx` `<ElectionsView>` call site passes all four new props:
      `buildingImageMap={buildingImageMap}`, `representingCity={representingCity}`,
      `userState={userState}`, `stateNames={STATE_NAMES}` (existing six props unchanged).
    - `src/components/ElectionsView.jsx` imports `SectionBanner` from `./SectionBanner.jsx`.
    - `src/components/ElectionsView.jsx` renders `<SectionBanner` inside the per-tier `.map(...)`,
      with tier mapping Local->"city", State->"state", Federal->"federal".
    - The old `<span>` tier eyebrow at the former L536-538 no longer renders that tier label band.
    - ElectionsView does NOT contain a call to `getBuildingImages(`, `parseCityFromAddress(`,
      `parseStateFromAddress(`, or `useTheme(`.
    - `npx vitest run && npx vite build` exits 0.
  </acceptance_criteria>
  <done>
    The Elections page renders the location-aware `SectionBanner` divider before each City/State/
    Federal tier, fed by props threaded from Results; banner labels match the Representatives tab;
    build + lib tests green; no preserved behavior touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual-parity sign-off (dark + banners + light/dark banner decision + behavior regression)</name>
  <read_first>
    - .planning/phases/172-elections-page-parity/172-VALIDATION.md (Manual-Only Verifications table + Sign-Off checklist)
    - .planning/phases/172-elections-page-parity/172-RESEARCH.md (Open Question 2 — SectionBanner is documented dark-only but Results renders it in BOTH modes; "match Results" = render in both)
  </read_first>
  <what-built>
    ElectionsView re-themed to the new Phase-169 dark palette (DARK-03) and rendering the Phase-170
    `SectionBanner` divider before each City -> State -> Federal tier (BANR-05), fed by
    buildingImageMap / representingCity / userState / stateNames threaded from Results.jsx. Loading
    skeleton, empty state, and "No candidates have filed" box are now dark-legible. `npx vitest run
    && npx vite build` is green.
  </what-built>
  <how-to-verify>
    Start the dev server (`npm run dev`) and, in DARK mode unless noted:
    1. DARK-03 parity: Open the Elections tab next to the Representatives (Results) tab. Confirm the
       page background, section areas, tiles, and accent color match — no faint-gray-on-dark, no
       old-navy/old-accent mismatch.
    2. BANR-05 banners: Scroll Elections for a CURATED jurisdiction (e.g. an LA address — expect
       LA / California / US banner art) and confirm a banner appears before each tier with the correct
       city/state label. Then browse an ART-LESS jurisdiction and confirm the graceful gradient
       fallback (no broken image).
    3. Label match: Confirm the Elections city/state banner labels are identical to the Representatives
       tab for the same search (no divergence).
    4. LIGHT/DARK BANNER-PARITY DECISION (research Open Question 2): SectionBanner is documented
       dark-only, but Results renders it in BOTH light and dark, so this plan renders it in both for
       true parity. Toggle to LIGHT mode on Elections and confirm the banner-in-light treatment is
       acceptable, OR direct that the banner should be dark-only (which would require a follow-up
       change). State your decision explicitly.
    5. Preserved behaviors:
       - Seeded ordering: reload Elections (same candidate order); then open a new session / clear
         `ev:election-seed` in sessionStorage (order reshuffles).
       - Unopposed / no-candidate: find a "Running Unopposed" race and a "No candidates have filed"
         body; confirm both render correctly in light AND dark.
       - elections/me auto-load: as a Connected user with a saved location, open the Elections tab and
         confirm it prefills.
       - MiniCompass overlay: enable compass mode; check the side (desktop) and stacked (mobile)
         panels are dark-legible.
    6. Light mode: confirm no light-mode regression on Elections.
  </how-to-verify>
  <resume-signal>Type "approved" (and state the light/dark banner-parity decision), or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent -> child React props | `Results.jsx` passes already-fetched, already-trusted in-app values (buildingImageMap / representingCity / userState / stateNames) into `ElectionsView`. No new untrusted input crosses any boundary. |

This phase changes presentation only — no auth, no input validation, no data handling, no crypto,
no new network calls. There is no meaningful attacker-facing surface (authenticated frontend
re-theme + prop threading on an existing page).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-01 | Information Disclosure | Dark-mode color overrides on ElectionsView | mitigate | Legibility/accessibility: ev-ui inline dark overrides require `!important`; never render faint-gray-on-dark — the `#8b949e` muted floor is the faintest allowed. (Task 1 enforces; SectionBanner is first-party and needs no `!important`.) |
| T-172-02 | Tampering | Tailwind v4 build (scans committed `.md`) | mitigate | Do NOT introduce raw Windows backslash paths in any committed file — they parse as CSS hex escapes and crash the prod build. Use forward slashes only; keep the existing `@source not` guards in `src/index.css`. (Build gate `npx vite build` catches a regression.) |
| T-172-03 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (no `npm install` step; all code is in-repo or already installed). RESEARCH Package Legitimacy Audit = "Not applicable." |
</threat_model>

<verification>
- `npx vitest run` green after each task (lib units, incl. `src/lib/buildingImages.test.js`).
- `npx vitest run && npx vite build` green after the wave.
- `grep -nE '1a2235|2d3f5a|59b0c4' src/components/ElectionsView.jsx` returns zero matches.
- `<ElectionsView>` call site in `src/pages/Results.jsx` passes the four new props.
- `src/components/ElectionsView.jsx` renders `<SectionBanner` inside the per-tier `.map(...)`.
- Human visual-parity checkpoint approved (dark parity + banners + light/dark banner decision + all
  four preserved behaviors confirmed; no light-mode regression).
</verification>

<success_criteria>
- DARK-03: Elections page renders the same Figma dark treatment as Results (background, chrome,
  section areas, tiles), no stale literals, no faint-gray-on-dark.
- BANR-05: same `SectionBanner` dividers between City -> State -> Federal, location-aware with the
  same graceful fallback, labels matching the Representatives tab.
- Preserved: seeded per-session ordering, unopposed / no-candidate handling, `elections/me`
  auto-load, MiniCompass overlay — all unchanged in behavior (color-only re-theme).
- Light mode unchanged (no regression).
- `npx vitest run && npx vite build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/172-elections-page-parity/172-01-SUMMARY.md` when done.
</output>
