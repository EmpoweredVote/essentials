---
phase: 158-nevada-tiger-geofences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts
  - C:/EV-Accounts/backend/scripts/verify-nv-tiger-import.sql
  - C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts
autonomous: true
requirements:
  - NV-GEO-01

must_haves:
  truths:
    - "Running the loader with --state NV --fips 32 --dry-run does not throw 'layer not in allowlist' for any of cd119,sldu,sldl,place,county"
    - "The verify SQL script exists at C:/EV-Accounts/backend/scripts/verify-nv-tiger-import.sql with state='32' filters and an OR-direction section-split gate"
    - "The smoke test script exists at C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts with 5 NV addresses (Strip/unincorporated, City of Las Vegas, Henderson, North Las Vegas, Boulder City)"
    - "The Strip test address forbids G4110 (and any city/township tier) — the unincorporated-Strip invariant (success criterion #1)"
    - "EXPECTED_NV_MTFCC.sldl and EXPECTED_NV_MTFCC.place are non-zero confirmed counts (from dry-run) after this plan completes"
    - "smoke-nv-geofences.ts expectedCounts.G5220 matches EXPECTED_NV_MTFCC.sldl and expectedCounts.G4110 matches EXPECTED_NV_MTFCC.place"
    - "Full 5-layer dry-run prints exactly 5 'NV MTFCC pre-flight assertion PASSED' lines with no MtfccAssertionError"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts"
      provides: "NV entries in STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, STATE_RUN_MAKEVALID, plus EXPECTED_NV_MTFCC block in processLayer() with confirmed non-zero counts for all 5 layers"
      contains: "NV: new Set(['cd119', 'sldu', 'sldl', 'place', 'county'])"
    - path: "C:/EV-Accounts/backend/scripts/verify-nv-tiger-import.sql"
      provides: "SQL verification gates for state='32'; per-layer counts; districts casing; Strip-unincorporated sentinel; OR-direction section-split gate"
      contains: "state = '32'"
    - path: "C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts"
      provides: "5-address TypeScript smoke test (Strip/LV/Henderson/N.LV/Boulder City) exiting 0/1"
      contains: "smoke-nv-geofences"
  key_links:
    - from: "load-state-tiger-boundaries.ts STATE_LAYER_ALLOWLIST"
      to: "processLayer() EXPECTED_NV_MTFCC block"
      via: "fipsArg === '32' guard"
      pattern: "fipsArg === '32'"
    - from: "smoke-nv-geofences.ts Strip TEST_ADDRESS"
      to: "forbiddenMtfcc assertion"
      via: "forbiddenMtfcc: ['G4110', 'G4040']"
      pattern: "forbiddenMtfcc"
    - from: "Plan 01 Task 3 dry-run MtfccAssertionError 'got N' output"
      to: "EXPECTED_NV_MTFCC.sldl and EXPECTED_NV_MTFCC.place final values"
      via: "manual edit replacing sentinel 0 with confirmed N"
      pattern: "got \\d+"
---

<objective>
Add Nevada (FIPS 32) to the generalized TIGER loader, create the verification and smoke test scripts, and discover the actual SLDL and G4110 polygon counts via dry-run — leaving the scaffold fully armed for the live load in Plan 02. No database rows are written in this plan.

Purpose: Plan 02 (the live data load) requires (a) the loader to recognize NV across all four inline allowlists, (b) the verify+smoke scripts to exist, and (c) the EXPECTED_NV_MTFCC pre-flight counts to match the actual TIGER 2024 file contents. The sentinel-0 dry-run trick surfaces the actual sldl and place counts without writing any data. This phase mirrors the proven VA/MD/OR/ME 5-layer pattern (cd119 key, no cousub).

Output: 4 additive edits to load-state-tiger-boundaries.ts (with confirmed non-zero counts for all 5 layers); 2 new scripts (verify-nv-tiger-import.sql, smoke-nv-geofences.ts) with matching confirmed counts; no database rows written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-nevada-tiger-geofences/158-CONTEXT.md
@.planning/phases/100-va-tiger-geofences/100-01-PLAN.md
@.planning/phases/100-va-tiger-geofences/100-01-SUMMARY.md

<interfaces>
<!-- Key contracts in load-state-tiger-boundaries.ts the executor edits. Extracted from the live file. -->
<!-- These are ADDITIVE insertion points — no existing state block (CA/TX/UT/IN/MA/ME/OR/MD/VA/DC) is removed. -->

FIPS_TO_STATE already contains `'32': 'nv'` — NO edit needed there. The main()
round-trip check (`expectedAbbrev !== args.state`) therefore accepts `--state NV --fips 32`.

The four NV insertion points (mirror the VA entries directly above DC):
  1. STATE_LAYER_ALLOWLIST: `NV: new Set([...])` after the VA entry, before DC.
  2. STATE_CITY_ASSERTIONS: `NV: [...]` after the VA entry, before the closing brace.
  3. STATE_RUN_MAKEVALID: `NV: new Set([...])` after the VA entry, before DC.
  4. EXPECTED_NV_MTFCC block in processLayer(): a `if (fipsArg === '32') { ... }`
     block modeled VERBATIM on the existing VA block (`if (fipsArg === '51')`).

cd119 dispatch entry URL template = `tl_${v}_us_cd119.zip` (vintage 2024, congress 119).
That `cd119` key is the verified one — NOT `cd`. The `cd` key uses the SAME url template
but is the legacy alias; using `cd` for NV would still resolve a file but is the wrong
established key for TIGER 2024 — use `cd119` per D-01.

place layer: G4110-only MTFCC hard filter (line ~1029); geo_id = GEOID (7-char STATEFP+PLACEFP).
county layer: national file `tl_2024_us_county.zip`, filtered STATEFP='32'; geo_id = GEOID (5-char).
sldu/sldl: state-scoped files `tl_2024_32_sldu.zip` / `tl_2024_32_sldl.zip`; filterByStatefp=false.
districts casing: insertDistrictIfMissing receives `abbrev` (lowercase 'nv') for county/sldu/sldl
  and `abbrevUpper` ('NV') for cd119 NATIONAL_LOWER. This is the ME/OR casing rule.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NV to TIGER loader (4 additive edits — allowlist, city-assert, makevalid, EXPECTED_NV_MTFCC block)</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Read before editing. The 4 insertion points mirror the VA entries (the most recent 5-layer state, exact analog):
      (1) STATE_LAYER_ALLOWLIST (~line 43) — insert after the `VA: new Set([...])` entry, before the `DC:` entry;
      (2) STATE_CITY_ASSERTIONS (~line 95) — insert after the `VA: ['Alexandria city']` entry, before the closing brace;
      (3) STATE_RUN_MAKEVALID (~line 109) — insert after the `VA:` entry, before the `DC:` entry;
      (4) EXPECTED_NV_MTFCC block in processLayer() — insert after the VA block (the `if (fipsArg === '51') { ... }` block ending ~line 968), before the DC block (`if (fipsArg === '11')` ~line 971).
    FIPS_TO_STATE already has '32': 'nv' — no change needed there (confirm by reading the map ~line 128).
    .planning/phases/100-va-tiger-geofences/100-01-PLAN.md — Task 1 is the structural template; NV substitutes 51->32, VA->NV, the city-assertion strings, and the per-key counts.
  </read_first>
  <action>
    Make exactly 4 additive edits to load-state-tiger-boundaries.ts. No existing lines are removed; all existing state blocks (CA/TX/UT/IN/MA/ME/OR/MD/VA/DC) are preserved verbatim. Each edit is placed immediately adjacent to the corresponding VA entry to keep the diff reviewable.

    Edit 1 — STATE_LAYER_ALLOWLIST (insert after the VA entry, before the DC entry):
      NV: new Set(['cd119', 'sldu', 'sldl', 'place', 'county']),
    Note: cd119 key (NOT cd) per D-01 — this is the Maine/Oregon silent-no-op trap; the wrong key would still resolve a file but is the wrong established TIGER 2024 dispatch key. No cousub per D-03 (Nevada unincorporated towns are advisory-only — county-level governance only). No aiannh per D-02 (tribal lands skipped this milestone).

    Edit 2 — STATE_CITY_ASSERTIONS (insert after the VA entry, before the closing brace of the const):
      NV: ['Las Vegas city', 'Henderson city', 'North Las Vegas city', 'Boulder City city'],
    These are case-insensitive substrings matched against TIGER NAMELSAD (the gate uses `.toLowerCase().includes(...)`). The expected NAMELSAD for Boulder City is the literal `'Boulder City city'` (TIGER appends the LSAD token 'city' even though the place name already contains 'City') — Task 3 dry-run confirms the exact NAMELSAD. If dry-run shows different strings, fail the task and report the actual NAMELSAD values seen; do NOT silently mutate the strings (this is the STATE_CITY_ASSERTIONS vintage gate, D-04).

    Edit 3 — STATE_RUN_MAKEVALID (insert after the VA entry, before the DC entry):
      NV: new Set(['cd119', 'sldu', 'sldl', 'place', 'county']),
    All 5 NV layers get ST_MakeValid — identical to ME, OR, MD, VA.

    Edit 4 — EXPECTED_NV_MTFCC block (insert after the VA block closing brace, before the DC block opening `if (fipsArg === '11')`). The block must have:
      - Guard: if (fipsArg === '32') { ... }
      - Record literal EXPECTED_NV_MTFCC with all 5 keys:
          cd119: 4   (4 NV congressional districts — already exist as G5200 rows from the pre-existing partial seed; assertion validates the shapefile count)
          sldu: 21   (21 NV State Senate districts, single-member)
          sldl: 0    (SENTINEL — Task 3 dry-run reveals actual count; NV Assembly is 42 single-member districts, so expect ~42)
          place: 0   (SENTINEL — Task 3 dry-run reveals actual count; expect ~19 G4110 incorporated cities per D-04, but the G4110-only filter count is unknown until dry-run)
          county: 17 (16 NV counties + Carson City as an independent city-county = 17 county-equivalents)
      - if (layer in EXPECTED_NV_MTFCC) dispatch that re-streams the shapefile with IDENTICAL filter logic to the upsert pass (filterByStatefp, place MTFCC===G4110 guard, districtNumField skipDistrictCodes). Copy this filter body verbatim from the VA block; NV has no cousub layer — do NOT add a FUNCSTAT guard.
      - Named MtfccAssertionError thrown when actualCount !== expected, with message: `[NV MTFCC assertion] layer=${layer}: expected ${expected} records, got ${actualCount}. TIGER file: ${url}. Aborting before any DB write — verify TIGER 2024 FIPS 32 file is correct.`
      - console.log on PASS: `  [${layer}] NV MTFCC pre-flight assertion PASSED: ${actualCount} records (expected ${expected}).`

    Copy the structural skeleton from the VA block verbatim. Only change: fipsArg guard value (32), Record name (EXPECTED_NV_MTFCC), error message label ([NV MTFCC assertion]), and the per-key counts above.

    The sldl: 0 and place: 0 values are intentional integer sentinels (not a TBD keyword, which would break TypeScript). Task 3 dry-run will fire MtfccAssertionError revealing the actual N. Do NOT skip Task 3 — Plan 02's live load depends on confirmed non-zero values being in place.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit --skipLibCheck scripts/load-state-tiger-boundaries.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - load-state-tiger-boundaries.ts STATE_LAYER_ALLOWLIST contains literal `NV: new Set(['cd119', 'sldu', 'sldl', 'place', 'county'])`
    - load-state-tiger-boundaries.ts STATE_CITY_ASSERTIONS contains literal `NV: ['Las Vegas city', 'Henderson city', 'North Las Vegas city', 'Boulder City city']`
    - load-state-tiger-boundaries.ts STATE_RUN_MAKEVALID contains literal `NV: new Set(['cd119', 'sldu', 'sldl', 'place', 'county'])`
    - The file contains exactly one occurrence of `if (fipsArg === '32')` introducing the EXPECTED_NV_MTFCC block
    - The EXPECTED_NV_MTFCC Record contains all 5 keys: cd119, sldu, sldl, place, county with integer values 4, 21, 0, 0, 17 respectively
    - The error thrown inside the block has name 'MtfccAssertionError' and message containing '[NV MTFCC assertion]'
    - The string `cd` does NOT appear as an NV allowlist key (grep for `NV: new Set` shows `cd119`, never bare `cd`)
    - `npx tsc --noEmit --skipLibCheck scripts/load-state-tiger-boundaries.ts` exits 0 (no TypeScript errors)
    - Existing VA/MD/OR/ME blocks are still present (grep for `EXPECTED_VA_MTFCC`, `EXPECTED_MD_MTFCC`, `EXPECTED_OR_MTFCC` still returns matches — no regressions)
  </acceptance_criteria>
  <done>NV is registered in the 3 config blocks and the EXPECTED_NV_MTFCC pre-flight assertion exists with sentinel 0s for sldl/place. TypeScript compiles cleanly. Task 3 will replace the sentinel 0s with confirmed counts.</done>
</task>

<task type="auto">
  <name>Task 2: Create verify-nv-tiger-import.sql and smoke-nv-geofences.ts (adapt from VA analogs)</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts — Full template for smoke-nv-geofences.ts. Copy structure verbatim (imports from 'pg' and 'dotenv', AddressTest interface, queryBoundaries function, main() flow, expectedMtfcc/forbiddenMtfcc/expectedGeoIds/NULL-name checks, error accumulation, exit 0/1).
    C:/EV-Accounts/backend/scripts/verify-va-tiger-import.sql — Reference for gate structure and the OR-direction section-split gate (geofence -> districts). NV must use the OR/VA direction (NOT the inverse) so the 4 pre-existing G5200 NATIONAL_UPPER/CD rows do not produce a false positive.
    .planning/phases/158-nevada-tiger-geofences/158-CONTEXT.md — D-01..D-04 and the Strip-unincorporated invariant; success criteria #1-#4.
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/scripts/verify-nv-tiger-import.sql by adapting verify-va-tiger-import.sql with these substitutions (every gate present; no gate omitted):
      - Header: `-- Phase 158 NV TIGER import verification` and `Run via: psql $DATABASE_URL -f backend/scripts/verify-nv-tiger-import.sql`
      - All `state = '51'` filters changed to `state = '32'`; remove any leftover VA-specific Alexandria/Fairfax dual-tier gates (Nevada has NO independent-city dual-tier — counties and cities are separate normal tiers)
      - Gate 1 (invalid geometry): count of rows for state='32' WHERE NOT ST_IsValid(geometry); expected 0
      - Gate 2 (GeometryCollection): count for state='32' WHERE ST_GeometryType = 'ST_GeometryCollection'; expected 0
      - Gate 3 (per-layer row counts): `SELECT mtfcc, COUNT(*) FROM essentials.geofence_boundaries WHERE state='32' GROUP BY mtfcc ORDER BY mtfcc;` with an expected comment `-- Expected: G4020|17, G4110|[DRY-RUN-COUNT], G5200|4, G5210|21, G5220|[DRY-RUN-COUNT]` — leave [DRY-RUN-COUNT] as a literal comment placeholder (verify SQL runs after the live load in Plan 02; the executor compares actual DB rows against EXPECTED_NV_MTFCC)
      - Gate 4 (Strip-unincorporated invariant — success criterion #1): a comment-documented spatial probe at a known Las Vegas Strip coordinate (lon=-115.1728, lat=36.1147 — near the Bellagio) returning the covering boundaries: `SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='32' AND ST_Covers(geometry, ST_SetSRID(ST_MakePoint(-115.1728, 36.1147), 4326)) ORDER BY mtfcc;` — expected: a G4020 row named 'Clark County' (geo_id='32003') and the G5200/G5210/G5220 tiers, but NO G4110 row (the Strip is unincorporated)
      - Gate 5 (districts table casing): `SELECT state, district_type, COUNT(*) FROM essentials.districts WHERE state IN ('NV','nv') GROUP BY state, district_type ORDER BY state, district_type;` — expected: nv|COUNTY|17, nv|STATE_UPPER|21, nv|STATE_LOWER|[DRY-RUN], NV|NATIONAL_LOWER|4 (cd119 uses uppercase 'NV' per abbrevUpper; county/sldu/sldl use lowercase 'nv')
      - Gate 6 (Clark County sentinel — confirms county layer): `SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='32' AND mtfcc='G4020' AND name LIKE '%Clark County%';` — expected 1 row: geo_id='32003' 'Clark County'
      - Gate 7 (section-split — MUST use OR/VA-direction): `SELECT gb.geo_id, gb.name, gb.mtfcc FROM essentials.geofence_boundaries gb WHERE gb.mtfcc IN ('G5200','G5210','G5220','G4020') AND gb.state='32' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts) LIMIT 10;` — expected 0 rows. Include an SQL comment explaining the OR-direction choice: a pre-existing NATIONAL_UPPER row (no polygon) would produce a false positive under the inverse (districts -> geofence) query.

    Create C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts by adapting smoke-va-geofences.ts in full. Copy verbatim (no NV-specific change): the imports; the AddressTest interface; the main() structure with client.connect/try/finally/client.end; the per-address loop (expectedMtfcc check, forbiddenMtfcc check, expectedGeoIds check, NULL-name check); the error accumulation; the final exit 0/1 branching. Apply these NV-specific substitutions:
      - File header comment block: `smoke-nv-geofences.ts` / `Phase 158: Nevada geofences smoke test` / Usage `npx tsx scripts/smoke-nv-geofences.ts` from C:/EV-Accounts/backend; SC1-SC4 describe Strip-unincorporated (no G4110), the 4 incorporated cities each returning G4110 + county/CD/SLDU/SLDL, layer counts, and non-NULL names
      - queryBoundaries function: `state = '32'` (not '51')
      - TEST_ADDRESSES array — replace VA addresses entirely with these 5 NV addresses (the executor MAY refine coordinates to land cleanly inside each boundary; the criterion is correct tier resolution per success criteria #1-#2):
          Address 1: label='Las Vegas Strip (unincorporated Clark County)', lon=-115.1728, lat=36.1147, expectedMtfcc=['G4020','G5200','G5210','G5220'], forbiddenMtfcc=['G4110','G4040'], expectedGeoIds={G4020:'32003'}  // Clark County; NO city, NO township
          Address 2: label='City of Las Vegas (City Hall)', lon=-115.1497, lat=36.1716, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'32003'}
          Address 3: label='Henderson NV (City Hall)', lon=-114.9817, lat=36.0397, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'32003'}
          Address 4: label='North Las Vegas NV (City Hall)', lon=-115.1175, lat=36.1989, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'32003'}
          Address 5: label='Boulder City NV (City Hall)', lon=-114.8330, lat=35.9786, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'32003'}
      - SC3 expectedCounts block (state='32' query): G4020:17, G4110:0, G5200:4, G5210:21, G5220:0. The 0 values for G4110 and G5220 are intentional placeholders — Task 3 dry-run reveals actual counts and updates these here. With 0 as expected, SC3 fails loudly until updated (prevents silent skip)
      - Target city lookup block: query for NV cities `'Las Vegas city', 'Henderson city', 'North Las Vegas city', 'Boulder City city'` in state='32' G4110 rows
      - Final result console.log block: reference Phase 158 success criteria SC1-SC4 using Strip/Las Vegas/Henderson/North Las Vegas/Boulder City language

    Do NOT inline the loader file contents into the smoke test. The smoke test only queries Postgres and asserts results.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit --skipLibCheck scripts/smoke-nv-geofences.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/verify-nv-tiger-import.sql` exists
    - verify-nv-tiger-import.sql contains `state = '32'` (multiple gates) and contains NO `state = '51'` or `state = '24'` (no leftover VA/MD text)
    - verify-nv-tiger-import.sql Gate 4 (Strip) probes coordinate (-115.1728, 36.1147) and the comment states the expected result has NO G4110 row (Strip is unincorporated)
    - verify-nv-tiger-import.sql Gate 6 contains `name LIKE '%Clark County%'` and expects 1 row (geo_id='32003')
    - verify-nv-tiger-import.sql Gate 7 uses OR-direction: `FROM essentials.geofence_boundaries gb ... WHERE ... AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts)` (NOT the inverse direction)
    - File `C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts` exists
    - smoke-nv-geofences.ts contains `state = '32'` in queryBoundaries and SC3
    - smoke-nv-geofences.ts TEST_ADDRESSES[0] (Strip) has `forbiddenMtfcc: ['G4110', 'G4040']` and expectedGeoIds `G4020: '32003'`
    - smoke-nv-geofences.ts contains 5 TEST_ADDRESSES entries (Strip + 4 incorporated cities)
    - smoke-nv-geofences.ts imports from 'pg' and 'dotenv' (same as VA analog)
    - `npx tsc --noEmit --skipLibCheck scripts/smoke-nv-geofences.ts` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>verify-nv-tiger-import.sql and smoke-nv-geofences.ts both exist; TypeScript compiles cleanly; SQL contains state='32' with the Strip-unincorporated Gate 4 and OR-direction Gate 7; smoke test asserts the Strip forbids G4110/G4040 and each of the 4 cities returns G4110 + all tiers.</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run all 5 NV TIGER layers; record actual sldl/place counts; update sentinels in loader and smoke test</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Confirm EXPECTED_NV_MTFCC.sldl=0 and EXPECTED_NV_MTFCC.place=0 sentinels from Task 1 are present; confirm the `fipsArg === '32'` guard is in place.
    C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts — Confirm expectedCounts.G4110=0 and expectedCounts.G5220=0 sentinels from Task 2 are present.
    .planning/phases/158-nevada-tiger-geofences/158-CONTEXT.md — D-01 (cd119 not cd — verify the actual TIGER zip filename in the loader output) and D-04 (~19 G4110 cities expected).
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the dry-run command for all 5 NV layers:

      npx tsx scripts/load-state-tiger-boundaries.ts --state NV --fips 32 --layers cd119,sldu,sldl,place,county --dry-run

    The loader will:
      1. Print the resolved URL for each layer — CONFIRM the cd119 URL is `.../CD/tl_2024_us_cd119.zip` (this is the D-01 loader-key verification: the filename must contain `cd119`, not `cd`). If the URL contains a different key, STOP — the wrong key silently loads zero rows.
      2. Download all 5 TIGER zip files from census.gov to the local cache (cd119, sldu, sldl, place state-scoped; county is the ~80MB national file)
      3. Run the STATE_CITY_ASSERTIONS gate for the place layer — confirm all 4 NV cities ('Las Vegas city', 'Henderson city', 'North Las Vegas city', 'Boulder City city') are found in the NAMELSAD values seen
      4. Run the EXPECTED_NV_MTFCC pre-flight assertion for each layer
      5. NOT write to the database (--dry-run flag)

    Expected outcomes per layer:
      - cd119: PASSES with `[cd119] NV MTFCC pre-flight assertion PASSED: 4 records (expected 4)`
      - sldu:  PASSES with `[sldu] NV MTFCC pre-flight assertion PASSED: 21 records (expected 21)`
      - sldl:  THROWS `MtfccAssertionError [NV MTFCC assertion] layer=sldl: expected 0 records, got N`
      - place: THROWS `MtfccAssertionError [NV MTFCC assertion] layer=place: expected 0 records, got N`
      - county: PASSES with `[county] NV MTFCC pre-flight assertion PASSED: 17 records (expected 17)`

    Record the actual N values from the sldl and place MtfccAssertionError messages. Expected ranges:
      - sldl: ~42 (Nevada Assembly has 42 single-member districts). Stop and investigate if N is NOT in the range [40, 44].
      - place: ~19 (Nevada has ~19 incorporated cities per D-04). Stop and investigate if N is below 15 or above 30.

    Stop and investigate if any of these red flags appear:
      - cd119 throws MtfccAssertionError with `got 0` — the loader key is wrong (the silent-no-op trap); re-verify the URL contains `cd119`
      - sldu actual count is NOT 21 (Nevada has exactly 21 single-member Senate districts)
      - county throws MtfccAssertionError (would mean 17 is wrong — should be 16 counties + Carson City = 17)
      - STATE_CITY_ASSERTIONS gate fails for any of the 4 NV cities (casing/NAMELSAD mismatch — record the actual NAMELSAD values seen and stop; do not mutate the assertion strings silently)

    After confirming the actual counts, apply two follow-up edits:

      Edit A (load-state-tiger-boundaries.ts): In the EXPECTED_NV_MTFCC Record, replace `sldl: 0,` with `sldl: N,` and `place: 0,` with `place: N,` (N = actual dry-run counts). Add a line comment next to each: `// confirmed via dry-run YYYY-MM-DD — N NV G5220 single-member Assembly polygons` and `// confirmed via dry-run YYYY-MM-DD — N NV G4110 incorporated places`.

      Edit B (smoke-nv-geofences.ts): In the expectedCounts Record (SC3 block), replace `G4110: 0,` with `G4110: N,` (matching EXPECTED_NV_MTFCC.place) and `G5220: 0,` with `G5220: N,` (matching EXPECTED_NV_MTFCC.sldl). The two files must stay in sync — they reference the same underlying TIGER counts.

    After both edits, re-run the full 5-layer dry-run to confirm all 5 assertions now PASS:

      npx tsx scripts/load-state-tiger-boundaries.ts --state NV --fips 32 --layers cd119,sldu,sldl,place,county --dry-run

    Expect 5 "NV MTFCC pre-flight assertion PASSED" lines with no MtfccAssertionError. If any layer fails, the recorded counts are wrong — re-read the error message and re-apply Edit A.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/load-state-tiger-boundaries.ts --state NV --fips 32 --layers cd119,sldu,sldl,place,county --dry-run 2>&1 | grep -c "NV MTFCC pre-flight assertion PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - The dry-run output shows the cd119 resolved URL ending in `tl_2024_us_cd119.zip` (loader-key D-01 verification — NOT a bare `cd` filename)
    - First dry-run (with sentinel 0s) prints MtfccAssertionError for sldl with `got N` where N is in range [40, 44]
    - First dry-run prints MtfccAssertionError for place with `got N` where N is between 15 and 30
    - First dry-run prints PASSED for cd119 (4 records), sldu (21 records), county (17 records)
    - First dry-run shows the STATE_CITY_ASSERTIONS gate PASSED for all 4 NV cities (or, if it fails, the executor stops and records the actual NAMELSAD values)
    - load-state-tiger-boundaries.ts EXPECTED_NV_MTFCC.sldl is now a non-zero integer (the recorded N); place is now a non-zero integer (the recorded N); each entry has a `// confirmed via dry-run` line comment
    - smoke-nv-geofences.ts expectedCounts.G4110 equals EXPECTED_NV_MTFCC.place; expectedCounts.G5220 equals EXPECTED_NV_MTFCC.sldl
    - Re-run full 5-layer dry-run output contains exactly 5 occurrences of "NV MTFCC pre-flight assertion PASSED" and 0 occurrences of "MtfccAssertionError"
    - The verify command above prints exactly the integer 5
  </acceptance_criteria>
  <done>EXPECTED_NV_MTFCC has confirmed non-zero counts for all 5 layers (no sentinel 0 remaining); smoke-nv-geofences.ts expectedCounts matches; full 5-layer dry-run passes all pre-flight assertions with zero MtfccAssertionErrors; the cd119 loader key is verified from the actual zip filename. Plan 02 live load is now unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TIGER scripts -> filesystem | New script files written to EV-Accounts/backend/scripts/ |
| loader -> census.gov | HTTPS download of TIGER zip files during dry-run (read-only; no DB writes) |
| dry-run MtfccAssertionError output -> code edits | Actual sldl/place counts surfaced in error messages flow into code as literal integers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-01 | Tampering | load-state-tiger-boundaries.ts edits | mitigate | 4 additive inserts only; no deletions; CA/TX/UT/IN/MA/ME/OR/MD/VA/DC blocks preserved verbatim; TypeScript compile check (npx tsc --noEmit) gates Task 1 acceptance; regression grep for EXPECTED_VA/MD/OR_MTFCC |
| T-158-02 | Integrity | Wrong loader key (`cd` instead of `cd119`) silently loads zero CD rows | mitigate | D-01: Task 3 asserts the resolved cd119 URL ends in `tl_2024_us_cd119.zip`; cd119 pre-flight assertion expects 4 records — `got 0` halts before any DB write |
| T-158-03 | Integrity | Live load with stale sentinel 0 in EXPECTED_NV_MTFCC | mitigate | Task 3 gates: 5-layer dry-run must print 5 PASSED lines before Plan 01 is done; sentinel 0 left in place fails Plan 02 immediately on the first affected layer |
| T-158-04 | Integrity | Wrong sldl/place count written if outside expected range | mitigate | Task 3 acceptance criteria gate: N must be in [40,44] for sldl and [15,30] for place; executor halts and documents discrepancy before edit |
| T-158-05 | Tampering | TIGER zip files from census.gov | accept | census.gov is the authoritative US government source; files are read-only during dry-run; no DB writes; HTTPS download with redirect following is the established pattern |
| T-158-06 | Information Disclosure | DATABASE_URL in smoke test | accept | DATABASE_URL already used in existing VA/MD/OR smoke tests; dotenv.config() pattern; no new exposure surface; Plan 01 does not run the smoke test against production |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; all deps pre-existing in EV-Accounts/backend (pg, dotenv, shapefile, adm-zip). RESEARCH.md Package Legitimacy audit not required — no installs in this phase |
</threat_model>

<verification>
After all 3 tasks complete:
1. `grep -c "NV: new Set" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns 2 (STATE_LAYER_ALLOWLIST + STATE_RUN_MAKEVALID)
2. `grep -c "Boulder City city" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns >= 1 (STATE_CITY_ASSERTIONS NV entry)
3. `grep -c "fipsArg === '32'" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns 1
4. `grep -n "sldl:" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts | grep "confirmed via dry-run"` — the EXPECTED_NV_MTFCC.sldl line carries a confirmed non-zero integer with a dry-run comment
5. `ls C:/EV-Accounts/backend/scripts/verify-nv-tiger-import.sql` — file exists
6. `ls C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts` — file exists
7. `npx tsc --noEmit --skipLibCheck scripts/smoke-nv-geofences.ts` from C:/EV-Accounts/backend — exits 0
8. Full 5-layer dry-run: `npx tsx scripts/load-state-tiger-boundaries.ts --state NV --fips 32 --layers cd119,sldu,sldl,place,county --dry-run` prints exactly 5 "PASSED" lines and zero "MtfccAssertionError" lines
</verification>

<success_criteria>
- NV is registered in STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, STATE_RUN_MAKEVALID with the correct 5-layer set (cd119 key per D-01; no cousub per D-03; no aiannh per D-02)
- EXPECTED_NV_MTFCC block inserted after the VA block; all 5 keys present (cd119, sldu, sldl, place, county); ALL values are confirmed non-zero integers after Task 3
- verify-nv-tiger-import.sql created with all gates: geometry checks for state='32'; per-layer counts; Strip-unincorporated probe (Gate 4); districts casing (Gate 5); Clark County sentinel (Gate 6); OR-direction section-split (Gate 7)
- smoke-nv-geofences.ts created and TypeScript-clean; Strip address forbids G4110/G4040; each of the 4 cities asserts G4110 + county/CD/SLDU/SLDL
- Full 5-layer dry-run passes all pre-flight assertions (no MtfccAssertionError); the cd119 loader key is verified from the actual zip filename; loader and smoke test expectedCounts are in sync for G4110/G5220
- No database rows written in Plan 01 (dry-run only)
</success_criteria>

<output>
Create `.planning/phases/158-nevada-tiger-geofences/158-01-SUMMARY.md` when done. Record the confirmed sldl and place counts (sldl=N, place=N), the verified cd119 URL filename, and the 4 confirmed NV city NAMELSAD strings — Plan 02 references these when checking the live load row counts.
</output>
