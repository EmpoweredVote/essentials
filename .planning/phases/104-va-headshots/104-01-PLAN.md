---
phase: 104-va-headshots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-va-execs-headshots.py
autonomous: true
requirements:
  - VA-GOV-06
must_haves:
  truths:
    - "3 VA state executive headshots exist in politician_photos Supabase Storage bucket at 600x750 JPEG q90"
    - "Each headshot is reachable via the CDN URL {CDN_BASE}/{politician_id}-headshot.jpg"
    - "Spanberger, Hashmi, and Jones all produce a 'SUCCESS' line in the script manifest"
    - "Jay Jones output has been visually spot-checked since the source is landscape 425x283px and the result may look blurry after upscale"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-va-execs-headshots.py"
      provides: "Standalone download/crop/resize/upload script for 3 VA execs"
      contains: "ROSTER list of 3 entries with external_ids -510001, -510002, -510003"
    - path: "Supabase Storage politician_photos/{politician_id}-headshot.jpg"
      provides: "3 uploaded headshot JPEGs at 600x750"
  key_links:
    - from: "_tmp-va-execs-headshots.py ROSTER"
      to: "essentials.politicians.external_id IN (-510001, -510002, -510003)"
      via: "DB lookup at script run time"
      pattern: "SELECT id FROM .* politicians WHERE external_id"
    - from: "Local PIL crop+resize"
      to: "Supabase Storage politician_photos bucket"
      via: "PUT with x-upsert header and Bearer SERVICE_KEY"
      pattern: "x-upsert"
---

<objective>
Create and run `_tmp-va-execs-headshots.py` to download, crop to 4:5, resize to 600x750 Lanczos q90, and upload to Supabase Storage the headshots for the 3 VA state executives — Spanberger (-510001), Hashmi (-510002), Jones (-510003) — per D-01, D-02, D-12, D-13.

Purpose: Cover the executive portion of VA-GOV-06. Produces 3 of the 155 final politician_images rows that Plan 05 (migration 315) will audit.
Output: One Python script + 3 JPEG files in Supabase Storage at politician_photos/{politician_id}-headshot.jpg, plus a deterministic SUCCESS/FAILED manifest block printed to stdout that Plan 05 will copy into migration 315 comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-va-headshots/104-CONTEXT.md
@.planning/phases/104-va-headshots/104-RESEARCH.md
@C:/EV-Accounts/backend/scripts/_tmp-alexandria-headshots.py
@C:/EV-Accounts/backend/migrations/306_va_state_executives.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write _tmp-va-execs-headshots.py from the Alexandria template with the 3-exec ROSTER</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-va-execs-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-alexandria-headshots.py (copy structure verbatim: .env loader, BUCKET='politician_photos', CDN_BASE, TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS, BROWSER_HEADERS, download_image, crop_to_4_5, resize_600x750, upload_to_storage, process_member, main)
    - .planning/phases/104-va-headshots/104-RESEARCH.md (§"VA State Executives — Source URLs" section: 3 verified URLs and dimensions)
    - .planning/phases/104-va-headshots/104-CONTEXT.md (D-01 through D-17 pipeline rules)
  </read_first>
  <action>
    Copy the entire structure of `_tmp-alexandria-headshots.py` and replace the ROSTER with exactly 3 entries for VA executives. Each ROSTER entry MUST include: `external_id`, `full_name`, `section_label='VA State Executives'`, `title`, `politician_id` (resolved at runtime via DB lookup, see below), `source_url`, `source_domain`, `is_required=True`. Use these exact source URLs and external_ids from 104-RESEARCH.md §"VA State Executives": Spanberger external_id=-510001, source_url='https://www.governor.virginia.gov/media/governorvirginiagov/governor-of-virginia/images/Governor-Spanberger-Official-Portrait.jpg', source_domain='governor.virginia.gov'; Hashmi external_id=-510002, source_url='https://www.ltgov.virginia.gov/media/governorvirginiagov/lieutenant-governor/Portrait-LT-Governor-Ghazala-Hashmi.jpg', source_domain='ltgov.virginia.gov'; Jones external_id=-510003, source_url='https://www.ag.virginia.gov/images/Jones-headshot-20260320.jpg', source_domain='ag.virginia.gov'. Unlike the Alexandria script, DO NOT hardcode `politician_id` UUIDs — instead modify `main()` to query each official's UUID via `SELECT id FROM essentials.politicians WHERE external_id = %s` BEFORE the processing loop, populating `member['politician_id']` dynamically (per RESEARCH Open Question 1). Keep BUCKET='politician_photos', `type='default'` semantics (only relevant when migration 315 runs — this script does not write to politician_images), CDN_BASE pointing to kxsdzaojfaibhuzmclfq.storage.supabase.co. Reuse the manifest format verbatim — emit "=== VA EXECS HEADSHOT MANIFEST ===" with "SUCCESS: {external_id} {full_name} {politician_id} -> {cdn_url}" lines per official and "TOTALS: VA Execs {N}/3 succeeded" at the end. Implement a hard-gate: if any of the 3 fail, sys.exit(1) (per is_required=True). For Jay Jones specifically, print a "WARNING: source is landscape 425x283 — verify quality visually" line after his SUCCESS to surface the risk noted in 104-RESEARCH.md Pitfall 8.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend &amp;&amp; python scripts/_tmp-va-execs-headshots.py 2>&amp;1 | grep -c "^SUCCESS: -51000"</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/_tmp-va-execs-headshots.py exists
    - File contains string `BUCKET = 'politician_photos'`
    - File contains string `TARGET_SIZE = (600, 750)`
    - File contains string `Image.Resampling.LANCZOS`
    - File contains string `'external_id': -510001` (Spanberger)
    - File contains string `'external_id': -510002` (Hashmi)
    - File contains string `'external_id': -510003` (Jones)
    - File contains string `Governor-Spanberger-Official-Portrait.jpg`
    - File contains string `Portrait-LT-Governor-Ghazala-Hashmi.jpg`
    - File contains string `Jones-headshot-20260320.jpg`
    - File contains string `SELECT id FROM` AND `politicians WHERE external_id` (runtime UUID resolution, no hardcoded UUIDs)
    - grep -v '^#' on the file returns 0 lines containing 'storage_url' (column name is `url`, not `storage_url`, even in comments-stripped form)
    - File does NOT contain string `politician-headshots` (wrong bucket name)
  </acceptance_criteria>
  <done>
    The script file exists with the structural patterns above. Implementation alone is not "done" — Task 2 runs it.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run _tmp-va-execs-headshots.py and verify all 3 execs uploaded</name>
  <files>(no new files; runs the script written in Task 1 and verifies Storage state)</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-va-execs-headshots.py (verify the manifest format Task 1 produced)
    - C:/EV-Accounts/backend/.env (confirm SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL all populated)
    - .planning/phases/104-va-headshots/104-VALIDATION.md (post-run verification protocol)
  </read_first>
  <action>
    Run `python C:/EV-Accounts/backend/scripts/_tmp-va-execs-headshots.py` from the C:/EV-Accounts/backend working directory. Capture full stdout/stderr to a string. Confirm the manifest section "=== VA EXECS HEADSHOT MANIFEST ===" is present and contains exactly 3 "SUCCESS: " lines, one per exec. If Jay Jones shows the landscape WARNING but still SUCCESS, that is acceptable — record visual quality risk for the SUMMARY but do not fail the task. If any of the 3 fails OR the script exit code is nonzero, STOP and report the exact failure mode (HTTP status, PIL error, Storage upload error) — DO NOT attempt to swap source URLs or re-run with modifications until reporting back. After successful run, query Supabase Storage HEAD on each of the 3 CDN URLs produced to confirm HTTP 200. Save the captured manifest text into the Plan 05 input (record it in the SUMMARY so migration 315 can quote the per-official URL+dimensions metadata per D-10).
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend &amp;&amp; python -c "import psycopg2, os; e={}; [e.update({k.strip():v.strip()}) for k,v in (l.split('=',1) for l in open('.env') if '=' in l and not l.strip().startswith('#'))]; c=psycopg2.connect(e['DATABASE_URL']+'?sslmode=require'); cur=c.cursor(); cur.execute(\"SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-510001,-510002,-510003)\"); print('found',cur.fetchone()[0])"</automated>
  </verify>
  <acceptance_criteria>
    - Script exit code 0
    - Captured stdout contains string `=== VA EXECS HEADSHOT MANIFEST ===`
    - Captured stdout contains exactly 3 lines matching pattern `^SUCCESS: -51000[123] `
    - Captured stdout contains 0 lines matching pattern `^FAILED: `
    - Captured stdout contains string `TOTALS: VA Execs 3/3 succeeded`
    - HEAD request to each of the 3 CDN URLs (resolved from the SUCCESS lines) returns HTTP 200
    - Captured manifest text recorded in 104-01-SUMMARY.md under heading `## Manifest (Plan 05 Input)`
  </acceptance_criteria>
  <done>
    Spanberger, Hashmi, Jones headshots live in Supabase Storage at 600x750 JPEG q90; manifest captured for Plan 05.
  </done>
</task>

</tasks>

<verification>
After both tasks: 3 JPEG objects exist in `politician_photos` bucket, one per exec UUID resolved by external_id (-510001, -510002, -510003). The deterministic manifest block in the Plan 01 SUMMARY is what Plan 05 (migration 315) will quote for D-10 documentation. No DB row is inserted by this plan; that is migration 315's job.
</verification>

<success_criteria>
1. `_tmp-va-execs-headshots.py` exists and follows the Alexandria template structure
2. Running the script produces 3 SUCCESS manifest lines and exit code 0
3. 3 Storage objects accessible at `{CDN_BASE}/{politician_id}-headshot.jpg` for politicians with external_id IN (-510001, -510002, -510003)
4. Jay Jones quality risk explicitly noted in SUMMARY for human spot-check before Plan 05 finalization
</success_criteria>

<output>
Create `.planning/phases/104-va-headshots/104-01-SUMMARY.md` when done, including:
- The exact manifest block printed by the script (verbatim copy)
- Source URL, original dimensions, crop dimensions for each of the 3 execs (Plan 05 input per D-10)
- Jay Jones visual-quality observation (acceptable / blurry / fall back to Wikipedia)
</output>
