---
phase: 202-palm-springs-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["202-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-palmsprings-headshots.py
  - C:/EV-Accounts/backend/migrations/1329_palm_springs_headshots.sql  # NN placeholder — verify actual next migration number (DB-verified disk MAX+1) at execute time
autonomous: false
requirements: [CV-02]
must_haves:
  truths:
    - "All 5 councilmembers have a 600×750 (4:5 crop-FIRST, Lanczos q90) headshot uploaded to the politician_photos Storage bucket"
    - "Each of the 5 politician_images rows binds to the CURRENT roster member via runtime external_id → UUID resolution (never an invented UUID)"
    - "Source is per-member (palmspringsca.gov is Akamai WAF-403 to bots) from Ballotpedia / Wikimedia / thepalmspringspost.com / campaign sites; photo_license matches each image's actual source (never defaulted to us_government_work for a non-.gov source)"
    - "type='default' on every row (the UI filters on it)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-palmsprings-headshots.py"
      provides: "per-member fetch → to_rgb_white_background → crop_to_4_5 → resize 600×750 Lanczos q90 → Storage x-upsert pipeline for 5 councilmembers (gitignored, orchestrator-run)"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1329_palm_springs_headshots.sql"
      provides: "5 audit-only (unregistered) politician_images rows, UUIDs from Plan 02 manifest"
      contains: "politician_images"
  key_links:
    - from: "_tmp-palmsprings-headshots.py"
      to: "Ballotpedia / Wikimedia / thepalmspringspost.com / campaign sites"
      via: "requests.get per-member hardcoded source URL with descriptive User-Agent, after per-domain WAF/reachability probe"
      pattern: "resolve_politician_id"
    - from: "1329_palm_springs_headshots.sql"
      to: "essentials.politicians"
      via: "SELECT id WHERE external_id = <member ext_id> subquery"
      pattern: "politician_images"
---

<objective>
Author the headshot ETL pipeline + the audit-only migration that gives all 5 Palm Springs
councilmembers a 600×750 (4:5 crop-first) headshot in Storage, each `politician_images` row bound to
the current roster member by runtime external_id→UUID resolution. Because palmspringsca.gov is Akamai
WAF-403 to bots, sources are per-member (Ballotpedia / Wikimedia / The Palm Springs Post / campaign
sites), probed individually — no single CMS pattern works for all 5.

Purpose: Satisfies the headshot half of ROADMAP success criterion #3 for CV-02.
Output: `_tmp-palmsprings-headshots.py` (gitignored, orchestrator-run) + `1329_palm_springs_headshots.sql`
(audit-only, unregistered — verify actual number at execute time).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-palm-springs-deep-seed/202-CONTEXT.md
@.planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md
@.planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md
@.planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md

<execution_note>
`gsd-executor` has NO Storage/DB access and CANNOT run the Python pipeline. Task 1 (author the Python
script) and Task 3 (author the audit migration) are executor file actions. Task 2 is ORCHESTRATOR-RUN:
the orchestrator runs the pipeline (PIL/requests/psycopg2 + Storage live only in its shell), captures
the emitted UUID→URL manifest, then the executor's audit migration is filled with those UUIDs and the
orchestrator applies it via `psql -f`. `_tmp-*` scripts are gitignored — never committed.
</execution_note>

<interfaces>
Key contracts (from 202-PATTERNS.md headshot block + 202-02-SUMMARY.md manifest):
- Primary analog (reuse functions VERBATIM): `C:/EV-Accounts/backend/scripts/_tmp-riverside-supervisors-headshots.py`
  — reuse `to_rgb_white_background()` (RGBA→white composite, never naive convert('RGB')),
  `crop_to_4_5()` (crop FIRST, never stretch), `resize_600x750()` (Lanczos), `upload_to_storage()`
  (x-upsert PUT to politician_photos/{uuid}-headshot.jpg), `resolve_politician_id()` (parameterized
  external_id=%s), `head_check()` (dry-run every roster URL first), the env-load block, and the
  descriptive HEADERS User-Agent.
- What changes: the ROSTER list (5 entries — external_id from the Plan 02 manifest, per-member
  hardcoded photo_url after individual reachability probes), the guard assertions
  (assert len(ROSTER)==5, ext_id uniqueness), and source_url_for() (returns member['photo_url']).
- Audit migration analog: `C:/EV-Accounts/backend/migrations/1315_riverside_county_headshots.sql`
  — columns exactly (id, politician_id, url, type, photo_license); WHERE NOT EXISTS idempotency;
  type MUST be 'default'; AUDIT-ONLY (NOT registered in schema_migrations).
- 5 members + ext_ids + UUIDs: from 202-02-SUMMARY.md manifest (D1 Garner / D2 Bernstein / D3 deHarte
  / D4 Soto / D5 Ready). photo_license = 'press_use' for Ballotpedia/press, a correct Wikimedia
  Commons license string for Wikimedia, 'us_government_work' ONLY if the final source is a .gov page
  (unlikely here — do NOT default it for non-.gov sources).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Palm Springs headshot pipeline (Python, gitignored)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-palmsprings-headshots.py</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 249-284 — the headshot-pipeline reuse spec: which functions to copy verbatim, what changes, the orchestration/gitignore note)
    - .planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md (the 5 politician ext_ids + geo_id→member mapping for the ROSTER)
    - C:/EV-Accounts/backend/scripts/_tmp-riverside-supervisors-headshots.py (PRIMARY analog — read in FULL: to_rgb_white_background / crop_to_4_5 / resize_600x750 / upload_to_storage / resolve_politician_id / head_check / env-load / HEADERS — copy verbatim, only ROSTER + source_url_for change)
  </read_first>
  <action>
    Copy `_tmp-riverside-supervisors-headshots.py` verbatim and change only: (1) the ROSTER list — 5
    entries, each with the member name, external_id (from 202-02-SUMMARY.md manifest), district, and a
    per-member hardcoded photo_url; (2) source_url_for(member) returns member['photo_url']; (3) guard
    assertions assert len(ROSTER)==5 and ext_id uniqueness. Keep `to_rgb_white_background()`,
    `crop_to_4_5()` (crop 4:5 FIRST, then resize — never stretch), `resize_600x750()` (Lanczos q90),
    `upload_to_storage()` (x-upsert PUT to politician_photos/{uuid}-headshot.jpg), `resolve_politician_id()`
    (parameterized external_id=%s), `head_check()` (dry-run every URL first), the env-load block, and
    the descriptive User-Agent HEADERS all verbatim.

    Per-member source: leave photo_url as a clearly-marked TODO placeholder per member with the probe
    order to follow at execute time (palmspringsca.gov is WAF-403 — do NOT rely on it): try Wikimedia
    Commons category → Ballotpedia candidate page (2022/2024 cycle) → thepalmspringspost.com photo
    archive → individual campaign site (e.g. naomisoto.com for Soto, rondeharte.com for deHarte). Set
    each member's photo_license to match the ACTUAL final source ('press_use' for Ballotpedia/press,
    the correct Wikimedia license string for Wikimedia) — never default 'us_government_work' for a
    non-.gov source. The script must be gitignored (`backend/scripts/_*`) and never committed.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-palmsprings-headshots.py"; test -f "$F" && grep -qF "crop_to_4_5" "$F" && grep -qF "resize_600x750" "$F" && grep -qF "to_rgb_white_background" "$F" && grep -qF "resolve_politician_id" "$F" && grep -qF "head_check" "$F" && grep -qE "len\(ROSTER\)\s*==\s*5" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File `_tmp-palmsprings-headshots.py` exists and reuses crop_to_4_5 / resize_600x750 / to_rgb_white_background / upload_to_storage / resolve_politician_id / head_check verbatim.
    - ROSTER has 5 entries with ext_ids matching the Plan 02 manifest; assert len(ROSTER)==5 present.
    - source_url_for returns member['photo_url']; per-member probe order documented (Wikimedia/Ballotpedia/Palm Springs Post/campaign); palmspringsca.gov NOT relied on.
    - photo_license per member set to the actual source (no blanket us_government_work default).
    - Script path matches the gitignored `_*` convention.
  </acceptance_criteria>
  <done>Headshot pipeline authored: reuses the Riverside crop-first-then-resize-600×750 functions verbatim; only ROSTER (5 members, manifest ext_ids) + per-member source change; gitignored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Probe sources + run pipeline + visual-QA (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator probes sources, runs the Python pipeline, uploads to Storage, emits UUID→URL manifest</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md (Open Question 2 — per-member source probing; Assumption A4 — thin Ballotpedia coverage)
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 281-284 — orchestration note: executor writes, orchestrator runs)
  </read_first>
  <what-built>
    The `_tmp-palmsprings-headshots.py` pipeline from Task 1 — reuses the verbatim crop-first→600×750
    Lanczos functions; ROSTER = the 5 councilmembers with manifest ext_ids.
  </what-built>
  <how-to-verify>
    ORCHESTRATOR steps: (1) for EACH of the 5 members, probe sources in order (Wikimedia Commons →
    Ballotpedia → thepalmspringspost.com → campaign site) with a browser-UA/headless fetch, confirm a
    reachable ≥600px-ish image, and fill that member's photo_url + photo_license in the script;
    (2) run `python backend/scripts/_tmp-palmsprings-headshots.py` — head_check dry-run first, then
    process (4:5 crop FIRST → 600×750 Lanczos q90 → x-upsert to politician_photos/{uuid}-headshot.jpg);
    (3) confirm all 5 upload with HTTP 200 and open each rendered image to visually confirm: correct
    person (identity match), 4:5 framing, eyes ~1/3 from top, head+shoulders, no distortion, no
    text/graphics on the face. Record the 5 UUID→URL→license manifest into 202-03-SUMMARY.md for Task 3.
    If any source is unreachable or any image is low-quality/wrong-person, PAUSE and re-source that one.
  </how-to-verify>
  <verify>
    <automated>MISSING — orchestrator-run visual-QA; no automated command (executor has no Storage/browser access). Verified via the 5 HTTP-200 upload confirmations + visual-QA artifact recorded in 202-03-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - All 5 members have a reachable per-member source (palmspringsca.gov WAF-403 avoided).
    - Pipeline runs; 5 images uploaded to politician_photos/{uuid}-headshot.jpg, each HTTP 200.
    - Visual QA per image: correct identity, 4:5, eyes ~1/3 from top, head+shoulders, no distortion, no graphics on face.
    - 5 UUID→URL→photo_license manifest recorded in 202-03-SUMMARY.md (license matches actual source per member).
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 5 headshots are uploaded (HTTP 200) and visually confirmed correct — or name the member(s) to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the audit-only headshots migration</name>
  <files>C:/EV-Accounts/backend/migrations/1329_palm_springs_headshots.sql</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 288-309 — the audit-only headshots migration shape: exact columns, WHERE NOT EXISTS, type='default', NOT registered)
    - .planning/phases/202-palm-springs-deep-seed/202-03-SUMMARY.md (the 5 UUID→URL→license manifest from Task 2)
    - C:/EV-Accounts/backend/migrations/1315_riverside_county_headshots.sql (PRIMARY analog — read in FULL: per-row shape, header comment, WHERE NOT EXISTS idempotency)
  </read_first>
  <action>
    Author `1329_palm_springs_headshots.sql` (verify actual next number at execute time). Copy the
    Riverside 1315 per-row shape, 5 rows — one per councilmember. Each row: INSERT INTO
    essentials.politician_images (id, politician_id, url, type, photo_license) SELECT
    gen_random_uuid(), (SELECT id FROM essentials.politicians WHERE external_id = <member ext_id>),
    '<Storage URL from Task 2 manifest>', 'default', '<license from Task 2 manifest>' WHERE NOT EXISTS
    (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id FROM
    essentials.politicians WHERE external_id = <member ext_id>)). Use the ACTUAL UUIDs/URLs/licenses
    from the 202-03-SUMMARY.md manifest — do NOT invent them. `type` MUST be 'default' on every row
    (the UI filters on it). Header comment must state this is AUDIT-ONLY, NOT registered in the
    migration ledger, applied via `psql -f` AFTER the Python pipeline uploads to Storage. The
    orchestrator applies it and confirms 5/5 politician_images rows resolve to a live Storage URL.
  </action>
  <verify>
    <automated>F=$(ls C:/EV-Accounts/backend/migrations/*_palm_springs_headshots.sql 2>/dev/null | head -1); test -n "$F" && test -f "$F" && grep -qF "politician_images" "$F" && grep -qcF "'default'" "$F" && grep -qF "WHERE NOT EXISTS" "$F" && [ "$(grep -c 'INSERT INTO essentials.politician_images' "$F")" -ge 5 ] && ! grep -qiE "schema_migrations" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; 5 INSERT INTO essentials.politician_images rows.
    - Columns exactly (id, politician_id, url, type, photo_license); type='default' on all 5.
    - politician_id resolved via SELECT ... WHERE external_id = <manifest ext_id>; WHERE NOT EXISTS idempotency guard on each.
    - URLs + licenses are the actual Task 2 manifest values (no invented UUIDs).
    - Header states AUDIT-ONLY / NOT registered; no schema_migrations INSERT.
    - Orchestrator confirms 5/5 rows resolve to a live Storage URL after `psql -f` apply.
  </acceptance_criteria>
  <done>Audit-only headshots migration authored (5 rows, type='default', WHERE NOT EXISTS, unregistered) using the real Task 2 manifest; applied by the orchestrator; 5/5 images live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image hosts (remote) → pipeline | Untrusted image bytes enter the pipeline over HTTP |
| pipeline → Supabase Storage + politician_images | Processed images + DB pointer rows written with service-role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-IMG | Spoofing | wrong-person / mislabeled photo | mitigate | Per-member identity confirmed at the Task 2 visual-QA blocking checkpoint before the audit migration binds it |
| T-202-LIC | Repudiation | wrong image license | mitigate | photo_license set per-member to the ACTUAL source; never defaulted to us_government_work for a non-.gov source |
| T-202-03 | Tampering | non-idempotent re-application | mitigate | WHERE NOT EXISTS (politician_id) guard on every politician_images row; audit-only (unregistered) so ledger untouched |
| T-202-SC | Tampering | Python package installs | accept | Zero new installs — reuses already-installed PIL/requests/psycopg2 |
</threat_model>

<verification>
- 5/5 councilmembers have a 600×750 4:5 crop-first headshot in Storage (HTTP 200), correct identity.
- 5 politician_images rows, type='default', bound by external_id→UUID, license matches source.
- Migration is audit-only (not in the ledger).
</verification>

<success_criteria>
All 5 Palm Springs councilmembers carry a correct, correctly-licensed 600×750 headshot.
</success_criteria>

<output>
Create `.planning/phases/202-palm-springs-deep-seed/202-03-SUMMARY.md` when done (record the 5
UUID→URL→photo_license manifest + per-member source used).
</output>
