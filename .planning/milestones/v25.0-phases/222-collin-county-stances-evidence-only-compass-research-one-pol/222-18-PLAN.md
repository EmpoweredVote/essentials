---
phase: 222-collin-county-stances-evidence-only-compass-research-one-pol
plan: 18
type: execute
wave: 18
depends_on: ["222-17"]
files_modified:
  - "[essentials] src/lib/coverage.js"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-18-CLOSEOUT.md"
  - "[essentials] .planning/REQUIREMENTS.md"
autonomous: false
requirements: [COLLIN-STANCE-01, COLLIN-STANCE-02]
must_haves:
  truths:
    - "An AFTER coverage snapshot is recorded live and diffed against 222-01's BEFORE snapshot, both in aggregate and grouped per government — the delta is reported as people-with-at-least-one-stance, never as a target that had to be hit (RESEARCH.md section F)"
    - "Every name on the 222-01 live worklist appears in exactly one of {applied inform.politician_answers rows written by this phase, an entry in 222-CONFIRMED-BLANK.md} — never neither, never both (COLLIN-STANCE-01)"
    - "A government that ends the phase entirely blank is reported as a complete, successful outcome, and its coverage chip correctly stays absent (COLLIN-STANCE-01/empty, COLLIN-STANCE-02/empty)"
    - "The evidence-integrity gate returns zero rows across every politician_id this phase touched — no NULL value, no value outside 1 to 5, no fractional value, no answers row missing its context row, no empty sources array, no topic_id outside the 11 canonical UUIDs (COLLIN-STANCE-02)"
    - "The split-section check returns zero rows, per standing milestone convention"
    - "Every Texas entry in src/lib/coverage.js that moved from zero to at least one stance carries `hasContext: true`, and every entry still at zero does not — the chip is DB-honest in both directions (RESEARCH.md Pitfall 5)"
    - "The coverage.js Texas block still has exactly the same 24 entries with the same geo_ids after the edit — only `hasContext` flags changed (COLLIN-STANCE-02/adjacency)"
    - "Re-running the close-out queries changes nothing; the AFTER snapshot is a read-only derivation (COLLIN-STANCE-01/idempotency)"
    - "The close-out is reported per government so an interrupted phase can be resumed at the exact government that is missing, rather than re-run wholesale (COLLIN-STANCE-01/concurrency, COLLIN-STANCE-01/ordering)"
    - "Browse spot-check screenshots exist for Frisco and for one government this phase flipped from zero to non-zero — captured as images, because the compass renders inside an ev-ui iframe and a text snapshot cannot prove it drew"
    - "COLLIN-STANCE-01 and COLLIN-STANCE-02 traceability rows in REQUIREMENTS.md are updated from Pending to their true end state based on the gates, not on optimism"
  artifacts:
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-18-CLOSEOUT.md"
      provides: "BEFORE/AFTER delta, per-government table, gate results, completeness reconciliation, chip reconcile record, spot-check evidence"
      contains: "AFTER snapshot"
    - path: "src/lib/coverage.js"
      provides: "hasContext chips reconciled against live stance coverage for the Texas block"
      contains: "browseStateAbbrev: 'TX'"
  key_links:
    - "222-01-WORKLIST.md is the completeness contract's left-hand side; applied inform.politician_answers rows plus 222-CONFIRMED-BLANK.md entries are the right-hand side — this plan proves the two sides match exactly"
    - "The per-government AFTER query drives the coverage.js chip edit: with_stance greater than zero and no chip means add the chip; zero and a chip means the chip is wrong and must be reported"
  prohibitions:
    - "MUST NOT add `hasContext: true` to a government that did not actually gain a stance — the chip must remain DB-honest, and an aspirational chip advertises coverage that does not exist"
    - "MUST NOT close the phase by marking a requirement Met while any gate returns rows or any worklist name is unaccounted for"
    - "MUST NOT report the phase outcome as a stance-count achievement; the measure is that every person was genuinely attempted and every applied stance is evidence-backed"
    - "MUST NOT resolve a completeness gap by deleting the person from the worklist or by back-filling a stance — the only correct resolutions are researching them or recording an honest blank"
    - "MUST NOT push the EV-Accounts repository; a push deploys accounts-api to Render and carries every commit already ahead"
---

<objective>
Close Phase 222: take the live AFTER coverage snapshot and diff it against 222-01's BEFORE, prove the exactly-one-bucket completeness contract across every worklist name, run the phase-wide evidence-integrity and split-section gates, reconcile the `src/lib/coverage.js` `hasContext` chips against real DB coverage, capture browse spot-check screenshots, and record the honest end state in REQUIREMENTS.md.

Purpose: This phase's two silent failure modes are a person who was never actually attempted and a stance that has no real evidence behind it. Neither is visible from the repository — both are only visible from production plus the register. This plan is where they surface, and where the coverage chip stops lying in either direction.
Output: `222-18-CLOSEOUT.md`, reconciled `src/lib/coverage.js`, finalized `222-CONFIRMED-BLANK.md`, updated REQUIREMENTS.md traceability, and spot-check screenshots.
</objective>

## Artifacts this phase produces

Phase 222 (all plans) produces data + planning artifacts only. No application code, no schema change, no packages. This close-out plan makes the single non-planning source edit of the phase: the `hasContext` flags in `src/lib/coverage.js`.

- **[EV-Accounts repo]** `C:/EV-Accounts/backend/migrations/<N>_222_<slug>_stances.sql` — the numbered, git-committed, **AUDIT-ONLY** (NOT registered in `schema_migrations`) migrations authored by plans 222-02 through 222-17.
- **[essentials repo]** `.planning/phases/222-.../222-01-WORKLIST.md` — live worklist, BEFORE snapshot, per-plan assignment table (222-01).
- **[essentials repo]** `.planning/phases/222-.../222-CONFIRMED-BLANK.md` — the per-person blank register (D-08), appended per government, finalized here.
- **[essentials repo]** `.planning/phases/222-.../222-18-CLOSEOUT.md` — AFTER snapshot, per-government delta, gate results, completeness reconciliation, chip record, spot-check evidence (this plan).
- **[essentials repo]** `.planning/REQUIREMENTS.md` — COLLIN-STANCE-01/02 entries and traceability rows (created 222-01, status finalized here).
- **[essentials repo]** `src/lib/coverage.js` — `hasContext: true` added to any Texas entry that moved from zero to at least one stance (this plan).
- **[essentials repo]** `.planning/phases/222-.../222-NN-SUMMARY.md` — one per plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AFTER snapshot, per-government delta, and the phase-wide gates</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the BEFORE snapshot (both count variants), the full worklist, and the per-plan assignment table this close-out reconciles against.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section F for the BEFORE/AFTER coverage query, the per-government breakdown query, and the split-section check; section A for the `collin_scope` CTE to re-materialize.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the evidence-integrity gate query and its six failure conditions.
    - The 222-02 through 222-17 SUMMARY files — for the list of politician_ids this phase touched and the per-plan attempted/sourced/blank counts.
  </read_first>
  <files>(read-only: production SQL via mcp__supabase-local__execute_sql; no writes this task)</files>
  <action>
    Re-materialize the `collin_scope` CTE from RESEARCH.md section A exactly as 222-01 did, including the `(p.is_vacant = false OR p.is_vacant IS NULL)` filter and the same 24 Texas geo_ids. Never use `p.is_vacant = false` alone.

    Run the AFTER coverage query in both count variants 222-01 recorded — any-topic and restricted to the 11 canonical topic UUIDs — producing total in-scope, count with at least one stance, and count without. Diff each against 222-01's BEFORE numbers and express the result as a delta in people-with-at-least-one-stance. Report the remaining without-stance count plainly; it is the honest residue of an evidence-only phase, not a shortfall to explain away.

    Run the per-government breakdown from RESEARCH.md section F: government_name, officeholder count, and count with at least one stance, grouped and ordered by government. This table drives Task 2's coverage.js edit.

    Run the evidence-integrity gate from 222-VALIDATION.md scoped to the union of every politician_id this phase touched, collected from the 222-02 through 222-17 SUMMARY files. It must return zero rows. It fails on a NULL value, a value outside 1 to 5, a value that is not equal to its own rounding, an answers row with no matching `inform.politician_context` row, a NULL or empty `sources` array, or a topic_id outside the 11 canonical UUIDs from RESEARCH.md section B. If it returns any row, that is a phase defect — record each offending row with its politician_id and topic_id and stop rather than proceeding to close.

    Run the split-section check from RESEARCH.md section F verbatim. Expect zero rows. This phase writes only to `inform.politician_answers` and `inform.politician_context`, so a non-zero result indicates an unrelated regression — record it and flag it rather than attributing it to this phase.

    Build the completeness reconciliation input: for every name on the 222-01 worklist, determine whether it has at least one `inform.politician_answers` row written by this phase and whether it appears as an entry in `222-CONFIRMED-BLANK.md`. Produce three lists — in-exactly-one-bucket, in-both, in-neither. The second and third lists must be empty.

    If `mcp__supabase-local__execute_sql` is not bound in this session, STOP and emit the exact SQL to the operator as a blocking request. Do not estimate the AFTER numbers and do not infer gate results from the SUMMARY files.
  </action>
  <verify>
    <automated>The MCP session returns, in one pass: both AFTER count variants; the per-government breakdown; the evidence-integrity gate result set; the split-section check result set; and the three completeness lists (exactly-one, both, neither) with their row counts.</automated>
  </verify>
  <acceptance_criteria>
    - The AFTER query text contains `is_vacant IS NULL` (the OR-NULL form) and both count variants are recorded as live numbers.
    - The BEFORE-to-AFTER delta is stated as people-with-at-least-one-stance, alongside the remaining without-stance count.
    - A per-government table exists with government_name, officeholder count, and with-stance count for all 24 Texas entries.
    - The evidence-integrity gate result is recorded; if it is not zero rows, every offending politician_id and topic_id is listed and the plan halts.
    - The split-section check result is recorded; a non-zero result is flagged as an unrelated regression rather than silently absorbed.
    - The in-both list is empty and the in-neither list is empty; if either is non-empty, every name in it is listed.
  </acceptance_criteria>
  <done>Live AFTER truth exists: the coverage delta, the per-government table, both phase-wide gate results, and the three completeness lists. Nothing has been written.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the coverage.js chips, finalize the register, write the close-out, and update requirement status</name>
  <read_first>
    - The Task 1 query output — especially the per-government with-stance table and the three completeness lists.
    - `src/lib/coverage.js` (essentials repo) — the file being modified; read the full Texas block and note which of the 24 entries currently carry `hasContext: true` (Allen, Anna, Celina, Frisco, Longview, McKinney, Melissa, Murphy, Plano, Princeton, Prosper, Richardson) and which carry none.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — Pitfall 5, which describes this exact chip-drift failure and why nothing forces the edit.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being finalized; confirm every government has a section and the running Count line matches the bullet total.
    - `.planning/REQUIREMENTS.md` (essentials repo) — the file being modified: the `### Compass stances (COLLIN-STANCE)` section and the `## Traceability` table added in 222-01.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the close-out shape 221 used, including its post-close addendum convention for late findings.
  </read_first>
  <files>src/lib/coverage.js, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-18-CLOSEOUT.md, .planning/REQUIREMENTS.md</files>
  <action>
    Reconcile the coverage chips in `src/lib/coverage.js` — this is the essentials repo, not EV-Accounts. Walk the Task 1 per-government table against the Texas block. For every entry whose live with-stance count is now greater than zero but which carries no `hasContext` flag, add `hasContext: true` to that entry using a scoped edit. For every entry whose live with-stance count is zero, leave it without the flag — an all-blank small town ending without a chip is the correct, honest result of an evidence-only phase, not an omission to paper over. If any entry already carries `hasContext: true` but shows a live with-stance count of zero, do not remove it silently; record it in the close-out as a pre-existing chip discrepancy for the operator to decide on, since it predates this phase. Change only `hasContext` flags: the Texas block must still contain exactly the same 24 entries with the same labels and the same `browseGovernmentList` geo_ids afterward.

    Finalize `222-CONFIRMED-BLANK.md`: confirm every government from the 222-01 assignment table has at least one section, set the final `Count:` line to the true total of registered blank people, and add a closing paragraph stating the completeness contract result — the worklist total, the number of people who received at least one stance, the number recorded as genuine blanks, and an explicit statement that the in-both and in-neither lists are empty. Mirror 221's convention: if a late finding emerges after this line is written, append it as a dated addendum rather than editing the body.

    Write `222-18-CLOSEOUT.md` containing: the BEFORE numbers copied from 222-01 and the AFTER numbers from Task 1, in both count variants, with the delta; the per-government table; the evidence-integrity gate result; the split-section check result; the completeness reconciliation with all three list sizes; the list of governments that moved from zero to non-zero and the exact chip edits made; any pre-existing chip discrepancy found; the total migrations authored with their live numbers; and a short honest-outcome paragraph framing the residual blanks as the correct product of the strict D-04 evidence bar rather than as unfinished work.

    Update `.planning/REQUIREMENTS.md` with scoped edits only, never a whole-file rewrite. Set the `COLLIN-STANCE-01` and `COLLIN-STANCE-02` traceability rows to their true end state: Complete only if the evidence-integrity gate returned zero rows, the in-both and in-neither lists are both empty, and every government carries a register section; otherwise leave the row Pending and name the blocking condition in the close-out. Tick the corresponding requirement checkboxes only under the same condition. Do not touch the COLLIN-BROWSE, COLLIN-ELECT, COLLIN-CONTACT, or COLLIN-PEOPLE sections or their rows.

    Commit the essentials repo normally. Do not run `git push` against `C:/EV-Accounts` at any point in this plan — every migration was already committed locally by its own research plan, and pushing that repo deploys accounts-api to Render carrying every commit already ahead.
  </action>
  <verify>
    <automated>test -f .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-18-CLOSEOUT.md &amp;&amp; [ "$(grep -c "browseStateAbbrev: 'TX'" src/lib/coverage.js)" = "24" ] &amp;&amp; grep -q "COLLIN-STANCE-01" .planning/REQUIREMENTS.md &amp;&amp; grep -q "COLLIN-STANCE-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Every government whose live with-stance count is greater than zero has `hasContext: true` in the `src/lib/coverage.js` Texas block, and every government at zero does not.
    - The Texas block still contains exactly 24 entries with unchanged labels and unchanged `browseGovernmentList` geo_ids; only `hasContext` flags differ from the pre-edit file.
    - Any pre-existing chip that is not DB-honest is reported in the close-out rather than silently removed.
    - `222-CONFIRMED-BLANK.md` has a section for every government in the 222-01 assignment table, a final `Count:` matching its bullet total, and a closing completeness paragraph stating the worklist total, the sourced count, the blank count, and that the in-both and in-neither lists are empty.
    - `222-18-CLOSEOUT.md` exists and contains the AFTER snapshot in both variants, the BEFORE-to-AFTER delta, the per-government table, both gate results, the completeness reconciliation, the chip edits made, and the honest-outcome paragraph.
    - The COLLIN-STANCE-01 and COLLIN-STANCE-02 traceability rows reflect the gate results — marked Complete only when the gate returned zero rows, both completeness lists are empty, and every government has a register section.
    - The COLLIN-BROWSE, COLLIN-ELECT, COLLIN-CONTACT, and COLLIN-PEOPLE sections and traceability rows in REQUIREMENTS.md are byte-identical to before.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>The chips are DB-honest, the register is finalized with its completeness statement, the close-out document records every gate and delta, and the requirement rows tell the truth about the outcome.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator browse spot-check and final phase sign-off</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-18-CLOSEOUT.md` — the document under review, including which governments flipped from zero to non-zero.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the finalized register and its completeness paragraph.
    - `src/lib/coverage.js` — the reconciled Texas block.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the Validation Sign-Off checklist.
  </read_first>
  <what-built>
    A closed-out phase: live AFTER snapshot and per-government delta against 222-01's BEFORE, a zero-row evidence-integrity gate across every touched politician, a zero-row split-section check, a proven exactly-one-bucket completeness contract, DB-honest coverage chips, a finalized blank register, and updated requirement traceability. What remains is the one thing no query can prove — that the compass actually renders for a newly-stanced officeholder in the live app.
  </what-built>
  <action>
    Halt here and hand control to the operator. Present the how-to-verify steps below verbatim and wait for the resume signal. The remaining checks are the ones no query can perform — that the compass visibly renders in the live ev-ui iframe for a newly-stanced officeholder, that no party is displayed, and that sampled register entries genuinely have no applied rows. Do NOT mark Phase 222 complete on gate results alone; record the operator's screenshots and checklist confirmations in this plan's SUMMARY first.
  </action>
  <how-to-verify>
    1. Open the live Frisco browse view at `?browse_government_list=4827684&browse_label=Frisco&browse_state=TX` and confirm a newly-stanced Frisco officeholder's compass spokes render. Capture a **screenshot** — the widget renders inside an ev-ui iframe, so a Playwright text snapshot cannot prove the compass drew.
    2. Pick one government the close-out lists as flipped from zero to non-zero and open its browse view using the same pattern with that government's geo_id and label. Confirm the compass renders and capture a second screenshot. If the phase flipped no government from zero to non-zero, say so explicitly and substitute a second Tier-1 city — do not skip the second screenshot silently.
    3. Open a profile for one officeholder who received a stance and confirm the reasoning and source render alongside the spoke, and that no party affiliation is displayed anywhere on the profile.
    4. Spot-check two register entries against the live database: confirm each named person genuinely has no `inform.politician_answers` rows from this phase. A person in the register who also has applied rows is a completeness-contract violation.
    5. Review the close-out's honest-outcome paragraph. Confirm it reports attempt coverage and evidence integrity rather than a stance count, and that residual blanks are framed as the correct result of the strict evidence bar.
    6. Walk the Validation Sign-Off checklist in `222-VALIDATION.md` and confirm each line, then set `nyquist_compliant: true` in that file's frontmatter if every line holds.
  </how-to-verify>
  <acceptance_criteria>
    - Two browse screenshots are captured: Frisco, plus one government flipped zero-to-non-zero (or an explicitly-stated substitute if none flipped).
    - A newly-stanced officeholder's compass spokes are visibly rendered in both screenshots.
    - A profile check confirms reasoning and source render with the spoke and that no party affiliation appears.
    - Two register entries are confirmed against production to have no applied rows from this phase.
    - The close-out's outcome paragraph is confirmed to report attempt coverage and evidence integrity, not stance count.
    - Every line of the `222-VALIDATION.md` Validation Sign-Off checklist is confirmed, and `nyquist_compliant: true` is set only if all lines hold.
    - The operator confirms no EV-Accounts push occurred during the phase.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 222, or describe what must be corrected first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository state → claimed completion | Migrations on disk and a full-looking register can both exist while production is unchanged |
| hand-maintained chip → advertised coverage | `coverage.js` is not derived at runtime, so its chip can advertise coverage the database does not have |
| gate results → requirement status | Marking a requirement Met is a durable claim that outlives the session that made it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-07 | Denial of Service | false-positive completion | high | mitigate | AFTER snapshot and both gates run against production, not against the repository; requirement status is conditioned on zero-row gate results |
| T-222-10 | Information Disclosure | chip advertises coverage that does not exist | high | mitigate | Chip edits are driven strictly by the live per-government with-stance table; a zero-count government keeps no chip; pre-existing discrepancies are reported, not silently reversed |
| T-222-11 | Repudiation | a person silently unaccounted for | critical | mitigate | Three-list completeness reconciliation (exactly-one / both / neither); the in-both and in-neither lists must be empty and are named in the close-out if not |
| T-222-12 | Tampering | broad edit to a shared source file | medium | mitigate | Scoped `hasContext` edits only; the 24-entry Texas block structure and geo_ids must be unchanged after the edit |
| T-222-SC | Tampering | package installs | low | accept | This plan installs no packages |
</threat_model>

<verification>
- AFTER snapshot recorded live in both count variants and diffed against 222-01's BEFORE, in aggregate and per government.
- Evidence-integrity gate returns zero rows across every politician_id this phase touched.
- Split-section check returns zero rows.
- Exactly-one-bucket completeness contract proven: in-both and in-neither lists both empty.
- `src/lib/coverage.js` chips reconciled DB-honestly with the Texas block otherwise unchanged.
- Browse spot-check screenshots captured for Frisco and one flipped government.
- REQUIREMENTS.md traceability reflects the true outcome.
</verification>

<success_criteria>
Phase 222 closes with proof rather than assertion: every in-scope Collin County officeholder was genuinely attempted and lands in exactly one of {evidence-cited stances, honest blank register}, every applied stance survives the evidence-integrity gate, the coverage chips tell the truth in both directions, and the compass is visibly rendering for newly-stanced officeholders on the live site.
</success_criteria>

<output>
Create `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-18-SUMMARY.md` when done — record the BEFORE/AFTER delta in both variants, the gate results, the completeness list sizes, the governments flipped and chips edited, any pre-existing chip discrepancy, the screenshots captured, and the final COLLIN-STANCE-01/02 status.
</output>
