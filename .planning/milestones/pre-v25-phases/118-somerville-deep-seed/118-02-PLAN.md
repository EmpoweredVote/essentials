---
phase: 118-somerville-deep-seed
plan: 02
type: execute
wave: 2
depends_on:
  - 118-01
files_modified:
  - C:/EV-Accounts/backend/migrations/582_somerville_school_committee.sql
autonomous: true
requirements:
  - SOMERVILLE-01

must_haves:
  truths:
    - "A Somerville, MA address returns a SCHOOL section with 7 elected School Committee members + Mayor Wilson (ex officio) + Council President Davis (ex officio) = 9 total"
    - "Somerville Public Schools government row exists in essentials.governments"
    - "G5420 geofence for geo_id='2510890' exists in geofence_boundaries with state='25'"
    - "Somerville school section-split returns 0 (geo_id=2510890, mtfcc=G5420)"
    - "Mayor Wilson has TWO offices: LOCAL_EXEC from migration 581 AND SCHOOL ex-officio from migration 582"
    - "Council President Davis has TWO offices: LOCAL from migration 581 AND SCHOOL ex-officio from migration 582"
    - "Mayor Wilson's office_id still points to his LOCAL_EXEC office (NOT overwritten by migration 582 back-fill)"
    - "Council President Davis's office_id still points to his LOCAL office (NOT overwritten by migration 582 back-fill)"
    - "7 new SC politicians seeded (external_ids -2510890001 through -2510890007); Mayor and Davis NOT re-seeded"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/582_somerville_school_committee.sql"
      provides: "Somerville School Committee seed — 7 elected + 2 ex-officio"
      contains: "Somerville Public Schools, Massachusetts, US"
    - path: "essentials.geofence_boundaries (DB)"
      provides: "G5420 geofence for Somerville school district"
      contains: "geo_id='2510890', mtfcc='G5420', state='25'"
    - path: "essentials.politicians (DB)"
      provides: "7 new SC members"
      contains: "external_id BETWEEN -2510890007 AND -2510890001"
    - path: "essentials.offices (DB)"
      provides: "9 SCHOOL offices (7 elected + 2 ex-officio)"
      contains: "district linked to geo_id='2510890'"
  key_links:
    - from: "essentials.offices (Mayor ex-officio)"
      to: "essentials.politicians (external_id=-2562535001)"
      via: "subquery SELECT id FROM essentials.politicians WHERE external_id = -2562535001"
      pattern: "Cross-join existing politician; no new INSERT"
    - from: "essentials.offices (Council President ex-officio)"
      to: "essentials.politicians (external_id=-2562535011)"
      via: "subquery SELECT id FROM essentials.politicians WHERE external_id = -2562535011"
      pattern: "Cross-join existing politician; no new INSERT"
    - from: "essentials.offices"
      to: "essentials.districts"
      via: "district_id JOIN on geo_id='2510890', district_type='SCHOOL'"
      pattern: "district_type = 'SCHOOL' (NOT 'SCHOOL_DISTRICT')"
---

<objective>
Seed Somerville Public Schools school committee: G5420 geofence + government + chamber + SCHOOL district + 7 elected members (new politicians) + Mayor Wilson ex-officio linkage + City Council President Davis ex-officio linkage (no new politician rows for either — reuse external_ids from migration 581). Migration 582 is the complete deliverable.

Purpose: Establishes the Somerville SCHOOL section so any Somerville address returns the full 9-member School Committee. CRITICAL DIFFERENCE from Newton: Somerville has TWO ex-officio members (Mayor + City Council President). The back-fill UPDATE must exclude BOTH external_ids to preserve their city-government office_ids.

Output: migration 582_somerville_school_committee.sql applied to production; G5420 geofence + 1 government + 1 chamber + 1 SCHOOL district + 7 new politicians + 9 offices (7 elected + 2 ex-officio); Mayor's and Davis's office_ids unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-somerville-deep-seed/118-RESEARCH.md
@.planning/phases/118-somerville-deep-seed/118-01-SUMMARY.md

<interfaces>
<!-- Reference migrations the executor MUST read before writing migration 582. -->
<!-- Pattern source 1: Newton school committee (migration 579) — same milestone; single ex-officio variant -->
C:/EV-Accounts/backend/migrations/579_newton_school_committee.sql

<!-- Key structural conventions from 579 / 348 Boston lineage: -->
<!-- Step 0: INSERT geofence_boundaries (geo_id, mtfcc='G5420', state='25') WHERE NOT EXISTS -->
<!-- Step 1: governments WHERE NOT EXISTS on name -->
<!-- Step 2: chambers — NO slug column (GENERATED ALWAYS) -->
<!-- Step 3: districts (district_type='SCHOOL', NOT 'SCHOOL_DISTRICT', state='ma', geo_id, label, mtfcc='G5420') -->
<!-- Step 4: politician+office WITH ins_p pattern for 7 elected members; is_appointed=false -->
<!-- Step 4b: Mayor ex-officio INSERT (subquery on external_id=-2562535001 — NO new politician) -->
<!-- Step 4c: Council President ex-officio INSERT (subquery on external_id=-2562535011 — NO new politician) -->
<!-- Step 5: office_id back-fill — ONLY for -2510890001 through -2510890007 (excludes -2562535001 AND -2562535011) -->
<!-- Step 6: Post-verification DO block with 10 gates -->
<!-- Step 7: schema_migrations ledger entry -->

<!-- CRITICAL DIFFERENCE FROM NEWTON (579): Somerville has TWO ex-officio members, not one. -->
<!-- Both ex-officio blocks use subquery on existing external_ids. Back-fill must exclude BOTH. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 582 — Somerville School Committee</name>
  <files>C:/EV-Accounts/backend/migrations/582_somerville_school_committee.sql</files>

  <read_first>
    C:/EV-Accounts/backend/migrations/579_newton_school_committee.sql — primary pattern for G5420 geofence, SCHOOL district, chamber, politician+office blocks, and Mayor ex-officio pattern. Somerville extends this with a SECOND ex-officio block.
    .planning/phases/118-somerville-deep-seed/118-RESEARCH.md — school committee roster, SC external_id assignments, TWO ex-officio SQL patterns (lines 284-314), Pitfalls 2/4/5/6/7/9.
    .planning/phases/118-somerville-deep-seed/118-01-SUMMARY.md — confirms Mayor Wilson (external_id=-2562535001) and Council President Davis (external_id=-2562535011) are seeded; both needed for ex-officio subqueries.
  </read_first>

  <action>
Create C:/EV-Accounts/backend/migrations/582_somerville_school_committee.sql modeled on 579_newton_school_committee.sql with the following Somerville-specific values:

HEADER COMMENT — include:
- Migration 582: Somerville Public Schools school committee (SOMERVILLE-01)
- geo_id='2510890' (NCES LEAID for Somerville Public Schools, MA FIPS=25)
- G5420 geofence inserted directly (no MA G5420 TIGER loader)
- 7 new politicians (-2510890001..-2510890007, is_appointed=false, elected, Ward 1-7)
- Mayor Wilson (external_id=-2562535001) linked via subquery — NO new politician INSERT
- Council President Davis (external_id=-2562535011) linked via subquery — NO new politician INSERT
- 9 offices total in SCHOOL district (7 elected + 2 ex-officio)
- office_id back-fill excludes BOTH -2562535001 (Mayor) AND -2562535011 (Davis) — back-fill range: -2510890001 through -2510890007 only
- CRITICAL: district_type='SCHOOL' (NOT 'SCHOOL_DISTRICT')
- CRITICAL: districts.state='ma' lowercase; governments.state='MA' uppercase; offices.representing_state='MA' uppercase
- CRITICAL: slug is GENERATED ALWAYS on chambers — never include in INSERT column list
- CRITICAL: essentials.governments has NO unique constraint — WHERE NOT EXISTS guard on name
- CRITICAL: SC geo_id is '2510890' (NCES LEAID) — NOT '2562535' (city geo_id)

PRE-FLIGHT BLOCK 1 — RAISE EXCEPTION if government already exists:
  SELECT COUNT(*) FROM essentials.governments WHERE name = 'Somerville Public Schools, Massachusetts, US'
  If > 0: RAISE EXCEPTION 'Migration 582 already applied — aborting re-run'

PRE-FLIGHT BLOCK 2 — RAISE EXCEPTION if SC external_id block not clear:
  SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2510890007 AND -2510890001
  If > 0: RAISE EXCEPTION with count

PRE-FLIGHT BLOCK 3 — RAISE EXCEPTION if Mayor Wilson (external_id=-2562535001) is NOT found:
  SELECT COUNT(*) FROM essentials.politicians WHERE external_id = -2562535001
  If = 0: RAISE EXCEPTION 'Pre-flight FAILED: Mayor Wilson (external_id=-2562535001) not found — run migration 581 first'

PRE-FLIGHT BLOCK 4 — RAISE EXCEPTION if Council President Davis (external_id=-2562535011) is NOT found:
  SELECT COUNT(*) FROM essentials.politicians WHERE external_id = -2562535011
  If = 0: RAISE EXCEPTION 'Pre-flight FAILED: Council President Davis (external_id=-2562535011) not found — run migration 581 first'

Do NOT wrap in BEGIN/COMMIT (matching 579 pattern — autocommit).

STEP 0 — Insert G5420 geofence:
  INSERT INTO essentials.geofence_boundaries (geo_id, mtfcc, state)
  SELECT '2510890', 'G5420', '25'
  WHERE NOT EXISTS (SELECT 1 FROM essentials.geofence_boundaries WHERE geo_id = '2510890' AND mtfcc = 'G5420')

STEP 1 — Government row:
  INSERT (id, name, type, state, city, geo_id)
  VALUES (gen_random_uuid(), 'Somerville Public Schools, Massachusetts, US', 'LOCAL', 'MA', NULL, '2510890')
  WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name = 'Somerville Public Schools, Massachusetts, US')

STEP 2 — School Committee chamber:
  INSERT (id, name, name_formal, government_id)
  name='School Committee', name_formal='Somerville School Committee'
  government_id = (SELECT id FROM essentials.governments WHERE name = 'Somerville Public Schools, Massachusetts, US')
  WHERE NOT EXISTS on (name, government_id)
  NEVER include slug.

STEP 3 — SCHOOL district row:
  INSERT (id, district_type, state, geo_id, label, mtfcc)
  district_type='SCHOOL', state='ma', geo_id='2510890', label='Somerville Public Schools', mtfcc='G5420'
  WHERE NOT EXISTS on (geo_id, district_type, state)

STEP 4 — 7 elected politician+office blocks (Ward 1-7, is_appointed=false):

The chamber_subquery used in every block:
  (SELECT id FROM essentials.chambers WHERE name = 'School Committee'
   AND government_id = (SELECT id FROM essentials.governments WHERE name = 'Somerville Public Schools, Massachusetts, US'))

BLOCK 1: Emily Ackman (Ward 1, Chair) — external_id=-2510890001
  full_name='Emily Ackman', first_name='Emily', last_name='Ackman'
  is_appointed=false; title='School Committee Chair'
  d.geo_id = '2510890' AND d.district_type = 'SCHOOL' AND d.state = 'ma'

BLOCK 2: Elizabeth Eldridge (Ward 2) — external_id=-2510890002
  full_name='Elizabeth Eldridge', first_name='Elizabeth', last_name='Eldridge'
  is_appointed=false; title='School Committee Member'

BLOCK 3: Michele Lippens (Ward 3) — external_id=-2510890003
  full_name='Michele Lippens', first_name='Michele', last_name='Lippens'
  is_appointed=false; title='School Committee Member'
  NOTE: 'Michele' with ONE 'l' per official somerville.k12.ma.us page — NOT 'Michelle'

BLOCK 4: Andre L. Green (Ward 4) — external_id=-2510890004
  full_name='Andre L. Green', first_name='Andre', last_name='Green'
  is_appointed=false; title='School Committee Member'

BLOCK 5: Laura Pitone (Ward 5) — external_id=-2510890005
  full_name='Laura Pitone', first_name='Laura', last_name='Pitone'
  is_appointed=false; title='School Committee Member'

BLOCK 6: Emma Stellman (Ward 6) — external_id=-2510890006
  full_name='Emma Stellman', first_name='Emma', last_name='Stellman'
  is_appointed=false; title='School Committee Member'

BLOCK 7: Leiran Biton (Ward 7, Vice Chair) — external_id=-2510890007
  full_name='Leiran Biton', first_name='Leiran', last_name='Biton'
  is_appointed=false; title='School Committee Vice Chair'

Each of Blocks 1-7 uses the standard WITH ins_p pattern from 579:
  WITH ins_p AS (
    INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id)
    VALUES (gen_random_uuid(), ..., NULL, true, false, false, true, -XXXX)
    ON CONFLICT (external_id) DO NOTHING RETURNING id
  )
  INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)
  SELECT gen_random_uuid(), d.id, (chamber_subquery), p.id, 'title', 'MA', false, false, NULL
  FROM essentials.districts d CROSS JOIN ins_p p
  WHERE d.geo_id = '2510890' AND d.district_type = 'SCHOOL' AND d.state = 'ma'
    AND p.id IS NOT NULL
    AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id)

BLOCK 8 — Mayor Wilson ex-officio — DO NOT INSERT a new politician row:
  INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)
  SELECT gen_random_uuid(), d.id, (chamber_subquery), p.id, 'Mayor (ex officio)', 'MA', false, false, NULL
  FROM essentials.districts d
  CROSS JOIN (SELECT id FROM essentials.politicians WHERE external_id = -2562535001) p
  WHERE d.geo_id = '2510890' AND d.district_type = 'SCHOOL' AND d.state = 'ma'
    AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id);

BLOCK 9 — Council President Davis ex-officio — DO NOT INSERT a new politician row:
  INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)
  SELECT gen_random_uuid(), d.id, (chamber_subquery), p.id, 'City Council President (ex officio)', 'MA', false, false, NULL
  FROM essentials.districts d
  CROSS JOIN (SELECT id FROM essentials.politicians WHERE external_id = -2562535011) p
  WHERE d.geo_id = '2510890' AND d.district_type = 'SCHOOL' AND d.state = 'ma'
    AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id);

STEP 5 — office_id back-fill (7 elected SC members ONLY — EXCLUDE both ex-officio external_ids):
  UPDATE essentials.politicians p SET office_id = o.id
  FROM essentials.offices o
  WHERE o.politician_id = p.id
    AND p.external_id BETWEEN -2510890007 AND -2510890001
    AND p.office_id IS NULL
  NOTE: -2562535001 (Mayor Wilson) and -2562535011 (Davis) are intentionally excluded from this range.
  Both must continue pointing to their LOCAL_EXEC / LOCAL office_ids from migration 581.

STEP 6 — Post-verification DO block with 10 gates:
  Gate (a): governments WHERE name='Somerville Public Schools, Massachusetts, US' = 1
  Gate (b): chambers WHERE name='School Committee' AND government_id=(SPS gov) = 1
  Gate (c): districts WHERE district_type='SCHOOL' AND state='ma' AND geo_id='2510890' = 1
  Gate (d): politicians WHERE external_id BETWEEN -2510890007 AND -2510890001 = 7
  Gate (e): offices JOIN districts WHERE district_type='SCHOOL' AND geo_id='2510890' AND state='ma' = 9
  Gate (f): section-split: geofence_boundaries WHERE geo_id='2510890' AND mtfcc='G5420'
    AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = '2510890' AND d.district_type = 'SCHOOL' AND d.state = 'ma') = 0
  Gate (g): politicians WHERE external_id BETWEEN -2510890007 AND -2510890001 AND office_id IS NULL = 0
  Gate (h): geofence_boundaries WHERE geo_id='2510890' AND mtfcc='G5420' AND state='25' = 1
  Gate (i): Mayor Wilson's office_id still points to LOCAL_EXEC district (geo_id='2562535'):
    SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.id = p.office_id
    JOIN essentials.districts d ON d.id = o.district_id
    WHERE p.external_id = -2562535001 AND d.district_type = 'LOCAL_EXEC' AND d.geo_id = '2562535'
    Expected: 1 (if <> 1, RAISE EXCEPTION 'Mayor Wilson office_id back-fill overwrote LOCAL_EXEC — CRITICAL BUG')
  Gate (j): Council President Davis's office_id still points to LOCAL district (geo_id='2562535'):
    SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.id = p.office_id
    JOIN essentials.districts d ON d.id = o.district_id
    WHERE p.external_id = -2562535011 AND d.district_type = 'LOCAL' AND d.geo_id = '2562535'
    Expected: 1 (if <> 1, RAISE EXCEPTION 'Council President Davis office_id back-fill overwrote LOCAL — CRITICAL BUG')
  On all failures: RAISE EXCEPTION with diagnostic; on pass: RAISE NOTICE with all counts.

STEP 7 — Ledger entry:
  INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('582') ON CONFLICT (version) DO NOTHING
  </action>

  <verify>
    <automated>
      Apply migration via mcp__supabase-local execute_sql. Post-verification DO block raises EXCEPTION on any of 10 gate failures. Migration succeeds = all 10 gates passed.

      Critical spot-checks after apply:
        SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = '2510890' AND d.district_type = 'SCHOOL' AND d.state = 'ma';
        -- Expected: 9 (7 elected + 2 ex-officio)

        SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2510890007 AND -2510890001;
        -- Expected: 7 (Mayor and Davis NOT in this range)

        SELECT o.title FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id JOIN essentials.politicians p ON p.id = o.politician_id WHERE d.geo_id = '2510890' AND d.district_type = 'SCHOOL' AND p.external_id IN (-2562535001, -2562535011) ORDER BY o.title;
        -- Expected: 'City Council President (ex officio)', 'Mayor (ex officio)'

        SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-2562535001, -2562535011);
        -- Expected: 2 (both seeded exactly once total — not re-seeded in this migration)

        SELECT p.full_name FROM essentials.politicians p JOIN essentials.offices o ON o.id = p.office_id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id = -2562535001 AND d.district_type = 'LOCAL_EXEC';
        -- Expected: 'Jake Wilson' (confirms Mayor's office_id was NOT overwritten)

        SELECT p.full_name FROM essentials.politicians p JOIN essentials.offices o ON o.id = p.office_id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id = -2562535011 AND d.district_type = 'LOCAL';
        -- Expected: 'Lance L. Davis' (confirms Davis's office_id was NOT overwritten)

        SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id = '2510890' AND gb.mtfcc = 'G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.district_type = 'SCHOOL' AND d.state = 'ma');
        -- Expected: 0 (section-split clean)
    </automated>
  </verify>

  <acceptance_criteria>
    - migration 582 applied without errors; post-verification DO block emitted PASSED notice
    - 7 new SC politicians in external_id range -2510890007 to -2510890001
    - 9 offices linked to SCHOOL district geo_id='2510890'
    - Both ex-officio offices present: 'Mayor (ex officio)' (external_id=-2562535001) and 'City Council President (ex officio)' (external_id=-2562535011)
    - Mayor Wilson seeded exactly once across both migrations (not re-seeded in 582)
    - Council President Davis seeded exactly once across both migrations (not re-seeded in 582)
    - Mayor Wilson's office_id confirmed pointing to LOCAL_EXEC district (geo_id='2562535')
    - Council President Davis's office_id confirmed pointing to LOCAL district (geo_id='2562535')
    - G5420 geofence present (geo_id='2510890', state='25')
    - Section-split returns 0 for geo_id='2510890', mtfcc='G5420'
    - Michele Lippens spelled with ONE 'l' — NOT 'Michelle'
    - Ledger entry for '582' present in supabase_migrations.schema_migrations
  </acceptance_criteria>

  <done>
    Migration 582 applied without errors; post-verification emits PASSED notice with all 10 gates; 7 new SC politicians; 9 SCHOOL offices (7 elected + 2 ex-officio); Mayor and Davis each seeded once total; both office_ids confirmed pointing to their original city offices; G5420 geofence present with state='25'; section-split returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → Supabase production | Data inserted directly via mcp__supabase-local; no user input path |
| Migration 582 → migration 581 state | Depends on external_ids -2562535001 (Mayor) and -2562535011 (Davis) existing; pre-flight gates 3+4 enforce this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-04 | Tampering | Mayor Wilson dual-insert risk | mitigate | Pre-flight gate 3 asserts external_id=-2562535001 exists; Block 8 uses subquery (not INSERT); total politician count stays 19 across both migrations |
| T-118-05 | Tampering | Council President Davis dual-insert risk | mitigate | Pre-flight gate 4 asserts external_id=-2562535011 exists; Block 9 uses subquery (not INSERT); post-verification gate checks total count = 2 for both external_ids |
| T-118-06 | Tampering | office_id overwrite — back-fill must skip BOTH ex-officio members | mitigate | UPDATE range explicitly -2510890001 through -2510890007 only; gates (i) and (j) verify both office_ids unchanged |
| T-118-07 | Tampering | Wrong geo_id on SCHOOL district (2562535 vs 2510890) | mitigate | Pre-flight + header comment + post-verification gate (c) explicitly check geo_id='2510890'; city geo_id never used in SC steps |
| T-118-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After migration 582 applies successfully:

1. Somerville Public Schools government row exists (name = 'Somerville Public Schools, Massachusetts, US')
2. School Committee chamber exists (name_formal = 'Somerville School Committee')
3. G5420 geofence present (geo_id='2510890', mtfcc='G5420', state='25')
4. 1 SCHOOL district row (geo_id='2510890', state='ma', district_type='SCHOOL')
5. 7 new politicians in external_id range -2510890007 to -2510890001
6. 9 offices linked to SCHOOL district: 7 elected + 2 ex-officio (Mayor + Council President)
7. Section-split check: 0 rows for geo_id='2510890', mtfcc='G5420'
8. Mayor Wilson (external_id=-2562535001) still has office_id pointing to LOCAL_EXEC district
9. Council President Davis (external_id=-2562535011) still has office_id pointing to LOCAL district
10. Mayor and Davis each appear in politicians table exactly once (not re-seeded)
11. Both ex-officio titles confirmed: 'Mayor (ex officio)' and 'City Council President (ex officio)'
</verification>

<success_criteria>
Migration 582 applied without error; post-verification DO block emits NOTICE with gov=1, chambers=1, districts=1, sc_politicians=7, total_school_offices=9, split_orphans=0, null_sc_office_ids=0, geo_count=1, mayor_local_exec_intact=1, davis_local_intact=1. Somerville SCHOOL section is populated with 9 members (7 elected + 2 ex-officio); both ex-officio members' city office_ids undisturbed.
</success_criteria>

<output>
Create .planning/phases/118-somerville-deep-seed/118-02-SUMMARY.md when done.
</output>
