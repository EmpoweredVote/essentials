---
phase: 194-city-of-tucson-deep-seed
plan: 04
type: execute
# WAVE NOTE (deliberate serialization): wave 4 sits behind wave 3 (headshots) even though both depend
# ONLY on Plan 02 — this is an INTENTIONAL serialization, not a computed dependency inconsistency.
# Stance research must run ONE OFFICIAL AT A TIME (feedback_stance_research_one_at_a_time: parallel
# stance fan-out burns the session quota), so this plan is kept in its own later wave to avoid a
# parallel stance pass and to keep each execution context small.
wave: 4
depends_on: ["194-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1298_tucson_mayor_stances.sql
  - C:/EV-Accounts/backend/migrations/1299_tucson_ward_1_stances.sql
  - C:/EV-Accounts/backend/migrations/1300_tucson_ward_2_stances.sql
  - C:/EV-Accounts/backend/migrations/1301_tucson_ward_3_stances.sql
  - C:/EV-Accounts/backend/migrations/1302_tucson_ward_4_stances.sql
  - C:/EV-Accounts/backend/migrations/1303_tucson_ward_5_stances.sql
  - C:/EV-Accounts/backend/migrations/1304_tucson_ward_6_stances.sql
autonomous: true
requirements: [TUC-01]
must_haves:
  truths:
    - "Evidence-only compass stances are seeded for the 7 officials (Mayor + 6 ward members) against the 36 non-judicial live topics (never the 8 judicial-* topics)"
    - "Every stance row is 100% cited (has a non-empty sources array); there are ZERO neutral-default rows — a topic with no evidence gets NO row at all (honest blank)"
    - "Stances are researched one official at a time (quota); citations are from non-WAF news sources (AZ Luminaria / Tucson Sentinel / AZPM / tucson.com), never an unfetched tucsonaz.gov agenda URL"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1298_tucson_mayor_stances.sql"
      provides: "Mayor Regina Romero (-4008001) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1304_tucson_ward_6_stances.sql"
      provides: "Ward 6 Miranda Schubert (-4008007) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "same (politician_id, topic_id) — every answer has a cited context row"
      pattern: "politician_context"
    - from: "stance rows"
      to: "inform.compass_topics (36 non-judicial)"
      via: "topic_id references a non-judicial live topic UUID"
      pattern: "topic_id"
---

<objective>
Seed evidence-only compass stances for the 7 City of Tucson officials — the Mayor + 6 ward members.
One audit-only migration per official (1298 Mayor, 1299-1304 Wards 1-6), researched ONE OFFICIAL AT A
TIME (quota), against the 36 NON-JUDICIAL live compass topics (44 live topics minus the 8 `judicial-*`
keys, which have no bearing on a mayor or council member — Pitfall 8). Every stance is 100% cited with
evidence-based reasoning; a topic with no documented record gets NO row at all (honest blank), never a
neutral default (D-06 / global `feedback_stance_no_default_value`). Discrete 1-5 "chairs", never a
polarity scale.

CRITICAL (Pitfall 4): tucsonaz.gov Mayor & Council agendas/minutes are on the SAME Akamai WAF as the
headshot host (Tucson does NOT use Legistar) — do NOT cite an unfetched tucsonaz.gov agenda URL. Cite
the reachable news-outlet coverage proven for the AZ metro: AZ Luminaria, Tucson Sentinel, AZPM,
Tucson Spotlight, tucson.com / Arizona Daily Star.

Wave placement (deliberate): this plan is wave 4, behind wave 3 (headshots), even though BOTH depend
only on Plan 02. That is an INTENTIONAL serialization to honor the one-official-at-a-time
stance-research quota (`feedback_stance_research_one_at_a_time`) and to keep each execution context
small — NOT a computed dependency inconsistency. Within this plan, work each official's migration one
at a time (author → apply → next), 1298 through 1304, so a context that degrades partway is
recoverable at a clean per-official boundary.

Purpose: Satisfies ROADMAP success criterion #3 (evidence-only stances for all 7 officials, 100%
cited, no defaults, honest blanks).
Output: 7 audit-only migrations `1298_tucson_mayor_stances.sql` + `1299..1304_tucson_ward_N_stances.sql`
(unregistered), applied so the officials' compasses populate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md
@.planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md
@.planning/phases/194-city-of-tucson-deep-seed/194-02-SUMMARY.md

<execution_note>
Depends on Plan 02: the 7 politician UUIDs from 194-02-SUMMARY.md are REQUIRED (stance rows key on the
official UUID). Executor authors the 7 `.sql` files (researching one official at a time per the
`feedback_stance_research_one_at_a_time` quota rule — do NOT fan out parallel stance research). The
ORCHESTRATOR applies each via `psql -f` and asserts. Commit via `git -C "C:/EV-Accounts"`. These are
AUDIT-ONLY migrations — NOT registered in the ledger. Independent of Plan 03 (headshots) — both depend
only on Plan 02, so they COULD run in either order; they are kept in separate, later waves DELIBERATELY
to honor the one-official-at-a-time quota and avoid a single overloaded context.
</execution_note>

<interfaces>
- inform.politician_answers (politician_id, topic_id, value): value = discrete 1.0-5.0 chair;
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value.
- inform.politician_context (politician_id, topic_id, reasoning, sources): reasoning = cited evidence
  prose ($$-quoted); sources = ARRAY[...] of source URLs (NEVER empty); ON CONFLICT (politician_id,
  topic_id) DO UPDATE.
- inform.compass_topics: 44 live topics; office_scope is empty/NULL (DB does NOT enforce judicial
  exclusion — manual convention). The 8 EXCLUDED judicial keys: judicial-access-to-justice,
  judicial-bail-pretrial, judicial-criminal-justice, judicial-government-deference,
  judicial-interpretation, judicial-police-accountability, judicial-prosecution-priorities,
  judicial-transparency. Research against the remaining 36 only.
- Evidence sources (RESEARCH Pitfall 4 — non-WAF, reachable): AZ Luminaria, Tucson Sentinel, AZPM,
  Tucson Spotlight, tucson.com / Arizona Daily Star. NOT tucsonaz.gov agenda PDFs (WAF-blocked).
- ext_ids → file: -4008001 Romero → 1298 (Mayor); -4008002 Santa Cruz → 1299 (Ward 1);
  -4008003 Cunningham → 1300 (Ward 2); -4008004 Dahl → 1301 (Ward 3); -4008005 Lee → 1302 (Ward 4);
  -4008006 Barajas → 1303 (Ward 5); -4008007 Schubert → 1304 (Ward 6).
- Tenure caveat: Barajas (Ward 5) and Schubert (Ward 6) were seated Dec 2025 — attribute only
  documented positions/statements from their tenure or campaign record, no pre-tenure vote attribution.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 7 per-official evidence-only stance migrations (one official at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1298_tucson_mayor_stances.sql, C:/EV-Accounts/backend/migrations/1299_tucson_ward_1_stances.sql, C:/EV-Accounts/backend/migrations/1300_tucson_ward_2_stances.sql, C:/EV-Accounts/backend/migrations/1301_tucson_ward_3_stances.sql, C:/EV-Accounts/backend/migrations/1302_tucson_ward_4_stances.sql, C:/EV-Accounts/backend/migrations/1303_tucson_ward_5_stances.sql, C:/EV-Accounts/backend/migrations/1304_tucson_ward_6_stances.sql</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md (lines 268-300 — the per-official stance migration pattern: topic-UUID reference comment block, BEGIN/COMMIT, the two-table INSERT (politician_answers + politician_context) per topic, the 36/36 judicial exclusion, honest-blank omission, one file per official; cite non-WAF sources)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfall 8 — the 8 judicial keys to exclude; § Common Pitfall 4 — do not cite unfetched tucsonaz.gov agendas; § Sources — the reachable AZ metro news list; § user_constraints D-06)
    - C:/EV-Accounts/backend/migrations/1290_pima_supervisor_1_stances.sql (analog — read header + first stance block: topic-UUID table, BEGIN/COMMIT wrapper, two-table INSERT/ON CONFLICT shape, sources-array pattern)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1297 after Plan 03, or 1296 if Plan 03 not yet applied — either way
    name these sequentially disk-MAX+1). Author 7 AUDIT-ONLY migrations, one per official, numbered
    1298 Mayor / 1299 Ward 1 / 1300 Ward 2 / 1301 Ward 3 / 1302 Ward 4 / 1303 Ward 5 / 1304 Ward 6.
    NONE registered in the ledger.

    PER-OFFICIAL CADENCE (save-point / recoverability): work strictly ONE OFFICIAL AT A TIME and treat
    each as an independent save-point. For official N: (1) research N's documented positions, (2)
    author the file, (3) hand off for the orchestrator to apply + assert that ONE file (Task 2 asserts
    per file), then (4) move to the next — 1298 → 1299 → ... → 1304, in order. Do NOT research or
    author the whole batch in one pass. This keeps the quota honored (one at a time) AND makes a
    mid-plan context degradation recoverable at a clean per-official boundary. Record in the SUMMARY
    which officials are complete so a resumed pass knows where to restart.

    Research ONE OFFICIAL AT A TIME (quota — never parallel). For each official, mine the reachable
    non-WAF evidence sources (AZ Luminaria, Tucson Sentinel, AZPM, Tucson Spotlight, tucson.com /
    Arizona Daily Star) for documented positions across the 36 non-judicial live topics. For each topic
    WHERE clear evidence exists, emit BOTH: an INSERT into inform.politician_answers (politician_id =
    the official UUID from the Plan 02 manifest, topic_id = the non-judicial topic UUID, value = a
    discrete 1-5 chair) AND an INSERT into inform.politician_context (same keys, reasoning = $$-quoted
    evidence prose naming the vote/statement/date, sources = ARRAY of the actual non-WAF source URLs —
    NEVER an empty array, NEVER a tucsonaz.gov agenda URL that was not actually fetched). Both guarded
    ON CONFLICT (politician_id, topic_id) DO UPDATE.

    HARD RULES: (1) NEVER emit a row for a topic with no evidence — omit it entirely (honest blank),
    never a neutral/default value (D-06 / feedback_stance_no_default_value). (2) NEVER touch any of the
    8 judicial-* topics — keep them in the reference comment block but write NO INSERT for them
    (Pitfall 8). (3) Every politician_answers row MUST have a matching politician_context row with a
    non-empty sources array (100% cited). (4) Values are discrete chairs (1-5), not a polarity scale
    (feedback_compass_chairs_not_polarity). (5) Attribute a vote/position only to officials in office
    at the time (no pre-tenure attribution — esp. Barajas W5 / Schubert W6, seated Dec 2025).

    Copy the 44-topic UUID reference comment block from the analog into each file's header; the INSERTs
    skip the 8 judicial rows. Wrap each file in BEGIN/COMMIT. Do NOT add a ledger-registration INSERT.
  </action>
  <verify>
    <automated>for f in 1298_tucson_mayor 1299_tucson_ward_1 1300_tucson_ward_2 1301_tucson_ward_3 1302_tucson_ward_4 1303_tucson_ward_5 1304_tucson_ward_6; do F="C:/EV-Accounts/backend/migrations/${f}_stances.sql"; test -f "$F" && grep -qF "politician_answers" "$F" && grep -qF "politician_context" "$F" && ! grep -qiE "INSERT INTO inform.politician_answers[^;]*judicial-" "$F" && ! grep -qE "INSERT INTO supabase_migrations" "$F" || { echo "FAIL $F"; exit 1; }; done; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist (1298 Mayor + 1299-1304 Wards 1-6, disk-MAX+1 sequential names).
    - Each file contains both `inform.politician_answers` and `inform.politician_context` INSERTs.
    - NO INSERT references any `judicial-*` topic (the 8 judicial keys appear ONLY in the reference comment, never in an INSERT).
    - Every politician_answers INSERT has a matching politician_context INSERT with a non-empty `ARRAY[...]` sources value (no empty arrays); no tucsonaz.gov agenda URL cited as evidence.
    - NO neutral-default rows — every emitted row is evidence-backed; topics without evidence are absent (no row).
    - NONE of the 7 files adds an `INSERT INTO supabase_migrations` line (audit-only).
    - Each file wrapped in BEGIN/COMMIT; each keyed to the correct official UUID from the Plan 02 manifest.
    - Files authored one official at a time (1298→1304), each a recoverable save-point; SUMMARY records per-official completion order.
  </acceptance_criteria>
  <done>7 audit-only stance migrations authored, one per official (1298→1304 in sequence, each a recoverable save-point), evidence-only against 36 non-judicial topics, 100% cited from non-WAF sources, honest blanks, discrete chairs, no defaults — unregistered.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the 7 stance migrations + assert evidence-only integrity (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator applies existing migrations + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md (Per-Task Verification Map — stances row: 100% cited, no default rows, honest blanks)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfall 8 — 36 not 44; a "44 per official" check would wrongly flag complete coverage)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). Apply the 7 migrations IN ORDER as each official's
    file lands (per the per-official cadence in Task 1) via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1298_tucson_mayor_stances.sql` (etc.
    through 1304) — applying + asserting each file before the next official keeps the per-official
    save-point clean. Then assert the aggregate via `psql -tAc`, recording in 194-04-SUMMARY.md: (a)
    every politician_answers row for the 7 Tucson UUIDs has a matching politician_context row (0
    orphans); (b) 0 politician_context rows with a NULL or empty sources array (100% cited); (c) 0
    politician_answers rows on any judicial-* topic for the 7 officials; (d) all values in [1.0, 5.0];
    (e) per-official stance counts (informational — expected to vary; honest blanks are fine, do NOT
    assert a fixed count — esp. the two Dec-2025 new members may have thinner records). Any integrity
    failure (a-d) blocks the phase. Commit the 7 migrations via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id BETWEEN -4008007 AND -4008001 AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id)) AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc JOIN essentials.politicians p ON p.id=pc.politician_id WHERE p.external_id BETWEEN -4008007 AND -4008001 AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL)) AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN inform.compass_topics t ON t.id=pa.topic_id WHERE p.external_id BETWEEN -4008007 AND -4008001 AND t.topic_key LIKE 'judicial-%') AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id BETWEEN -4008007 AND -4008001 AND (pa.value < 1.0 OR pa.value > 5.0))"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 migrations apply cleanly.
    - 0 politician_answers rows without a matching politician_context row (every answer cited).
    - 0 politician_context rows with a NULL/empty sources array (100% cited).
    - 0 stance rows on any `judicial-*` topic for the 7 officials.
    - All stance values in [1.0, 5.0].
    - The combined boolean SELECT returns `t`.
    - Per-official counts recorded in the SUMMARY (informational; honest blanks expected, no fixed count asserted).
    - 7 migrations committed to `C:/EV-Accounts`; none registered in the ledger.
  </acceptance_criteria>
  <done>Evidence-only stances live for the 7 officials: 100% cited from non-WAF sources, no defaults, no judicial rows, honest blanks — audit migrations applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stance migration files → production DB | Hand-authored evidence-only SQL crosses into live `inform` schema via `psql -f` |
| research sources → stance values | External news evidence maps to a 1-5 chair; unsourced/mis-scoped values corrupt the compass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-CITE | Tampering | uncited / default stance rows | mitigate | Every answer requires a matching context row with a non-empty sources array; Task 2 asserts 0 orphans + 0 empty-sources; no-evidence topics omitted (honest blank), never defaulted |
| T-194-JUD | Tampering | judicial topics on a non-judicial office | mitigate | 8 judicial-* keys excluded from all INSERTs; Task 2 asserts 0 judicial rows for the 7 officials |
| T-194-SRC | Repudiation | citing an unfetched WAF-blocked agenda URL | mitigate | Cite only reachable non-WAF news outlets (AZ Luminaria/Tucson Sentinel/AZPM/tucson.com), never an unverified tucsonaz.gov agenda PDF (Pitfall 4) |
| T-194-TENURE | Spoofing | pre-tenure vote attribution | mitigate | Attribute a position only to officials seated at the time; Barajas (W5) / Schubert (W6), seated Dec 2025, not credited with pre-tenure votes |
| T-194-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `.env`; applied by the inline orchestrator |
| T-194-SC | Tampering | package installs | accept | No new installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Every stance row 100% cited (matching context row + non-empty sources); 0 orphans.
- 0 judicial-* rows; 0 neutral-default rows; values in [1.0, 5.0].
- Researched one official at a time (1298→1304, per-official save-points); 7 audit migrations applied and unregistered.
</verification>

<success_criteria>
ROADMAP #3 (evidence-only compass stances, 100% cited, no defaults, honest blanks) is TRUE in
production for all 7 officials against the 36 non-judicial topics.
</success_criteria>

<output>
Create `.planning/phases/194-city-of-tucson-deep-seed/194-04-SUMMARY.md` when done — include
per-official stance counts, notable honest blanks, the per-official completion order (save-point
trail), and the evidence sources used.
</output>
</content>
