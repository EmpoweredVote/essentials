---
phase: 89-in-me-school-board-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 89-01
files_modified:
  - C:/EV-Accounts/backend/scripts/load-me-school-boundaries.ts
  - C:/EV-Accounts/backend/scripts/smoke-phase89-me.ts
  - C:/EV-Accounts/backend/migrations/265_me_city_school_boards.sql
autonomous: true
requirements:
  - ME-SCHOOL-01
  - ME-SCHOOL-02
  - ME-SCHOOL-03
tags:
  - me
  - school-boards
  - tiger
  - geofences
  - greenfield-seed
  - migration

must_haves:
  truths:
    - "G5420 geofence_boundaries rows exist for all 5 ME school districts (geo_id IN ('2307320','2302820','2312330','2302610','2303150')) with state='23', source='tiger_unsd_me_2024'"
    - "5 essentials.governments rows exist (one per district) with type='LOCAL', state='ME' (uppercase), correct geo_id per district"
    - "5 essentials.chambers rows exist (one School Committee or Board of Education chamber per government) with NO slug column included in INSERT"
    - "5 essentials.districts rows exist with district_type='SCHOOL', state='me' (LOWERCASE per RESEARCH.md Pitfall 5), mtfcc='G5420', geo_id matching each geofence_boundaries row"
    - "37 essentials.politicians rows exist with external_ids in -890011..-890018 (Lewiston 8), -890021..-890027 (Bangor 7), -890031..-890037 (South Portland 7), -890041..-890048 (Auburn 8), -890051..-890057 (Biddeford 7) — each with party=NULL, is_appointed=false, is_incumbent=true"
    - "37 essentials.offices rows exist linking each politician to its district's SCHOOL district row via district_id; titles follow per-district convention: Lewiston/Auburn use 'School Committee Member (Ward N)' or 'School Committee Member (At-Large)'; Bangor/Biddeford use 'School Committee Member' (at-large only); South Portland uses 'Board Member (District N)' or 'Board Member' for at-large"
    - "Lewiston Ward 5 row is present with either the verified appointee name (if confirmed at execution time) OR with is_vacant=true (placeholder) — RESEARCH.md Open Question 1"
    - "All 37 politicians have non-NULL office_id after the back-fill UPDATE (no orphan politicians in -890011..-890057)"
    - "Section-split check returns 0 rows: every G5420 row with one of the 5 target ME geo_ids has a matching SCHOOL districts row (state='me')"
    - "Lewiston coordinate (-70.2140, 44.0978) returns ≥ 7 Lewiston School Committee members via the SCHOOL routing JOIN"
    - "Bangor coordinate (-68.7772, 44.8012) returns 7 Bangor School Committee members"
    - "South Portland coordinate (-70.2788, 43.6415), Auburn coordinate (-70.2312, 44.0978), Biddeford coordinate (-70.4520, 43.4909) each return their own board members and do NOT cross-pollinate (district isolation)"
    - "Migration 265 is idempotent — re-running raises a pre-flight EXCEPTION"
    - "supabase_migrations.schema_migrations contains version='265'"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-me-school-boundaries.ts"
      provides: "TIGER UNSD downloader + shapefile parser + G5420 geofence_boundaries inserter for 5 ME school districts"
      contains: "TARGET_GEOIDS"
      min_lines: 180
    - path: "C:/EV-Accounts/backend/scripts/smoke-phase89-me.ts"
      provides: "Phase 89 ME smoke test: G5420 count=5, SCHOOL districts count=5, 5 coordinate routing tests, section-split=0"
      contains: "smoke-phase89-me"
      min_lines: 150
    - path: "C:/EV-Accounts/backend/migrations/265_me_city_school_boards.sql"
      provides: "5 governments + 5 chambers + 5 SCHOOL districts + 37 politicians + 37 offices + office_id back-fill + post-verification + ledger entry"
      contains: "Migration 265"
      min_lines: 600
  key_links:
    - from: "essentials.geofence_boundaries (geo_id IN (5 ME GEOIDs), mtfcc='G5420', state='23')"
      to: "essentials.districts (district_type='SCHOOL', state='me', same geo_ids)"
      via: "geo_id JOIN used by essentialsService.ts"
      pattern: "geo_id"
    - from: "essentials.offices.district_id"
      to: "essentials.districts.id (district_type='SCHOOL', state='me')"
      via: "FK established at INSERT time for each office"
      pattern: "district_id"
    - from: "Lewiston address ST_Covers against G5420 (geo_id='2307320')"
      to: "Lewiston School Committee members"
      via: "geofence_boundaries → districts → offices → politicians JOIN with district_type='SCHOOL'"
      pattern: "ST_Covers.*district_type = 'SCHOOL'"
---

<objective>
Seed Maine Tier 2 city school boards (greenfield — no existing rows). For each of Lewiston, Bangor, South Portland, Auburn, and Biddeford:
1. Load G5420 TIGER UNSD geofence (state='23').
2. INSERT government + chamber + SCHOOL district + politicians + offices in migration 265.
3. Verify routing with a smoke test.

Per RESEARCH.md §TIGER File Verification (Maine):
- Lewiston Public Schools — GEOID `2307320` — 8 members (7 wards + 1 at-large)
- Bangor School Department — GEOID `2302820` — 7 at-large members
- South Portland Public Schools — GEOID `2312330` — 7 members (district + at-large mix)
- Auburn Public Schools — GEOID `2302610` — 8 members (5 wards + 3 at-large)
- Biddeford Public Schools — GEOID `2303150` — 7 at-large members

Total new artifacts: 5 G5420 geofence rows + 5 governments + 5 chambers + 5 SCHOOL districts + 37 politicians + 37 offices.

External ID block: -890011..-890057 (per RESEARCH.md §External ID Allocation; clear per the known DB state). External_id -890001 was consumed by Plan 01 (Hope Duke Star).

This plan addresses ME-SCHOOL-01 (Lewiston), ME-SCHOOL-02 (Bangor), and ME-SCHOOL-03 (South Portland + Auburn + Biddeford).

CRITICAL — pre-execution discovery for roster verification (RESEARCH.md §Open Questions 1, 2, 3):
- Lewiston Ward 5: appointee name unknown. Implementer MUST check `lewistonpublicschools.org/en-US/school-committee-bc9f3846/school-committee-members-ab24c81b` at execution time. If still vacant, INSERT a placeholder politician row with `is_vacant=true` and `full_name='VACANT - Ward 5'`.
- South Portland: 2 [ASSUMED] names and 1 vacancy at the time of research. Implementer MUST verify the full current roster at `spsd.org/board/members-of-the-board` or `spsdme.org` before drafting the South Portland SQL block. If the page is JS-rendered and blocks server-side fetch, use the headshot script's existing HTTP client (or curl with browser User-Agent) to retrieve the rendered HTML.
- Bangor: Sara Luciano's current status uncertain. Implementer MUST verify at `bangorschools.net/page/school-committee`.
- Auburn: Nov 2025 winners confirmed but prior-cycle holdovers not in research. Implementer MUST check `auburnschl.edu` for the full current roster.

Output: 5 G5420 rows + 5 governments + 5 chambers + 5 SCHOOL districts + 37 politicians + 37 offices, migration 265 ledger entry, smoke test passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/89-in-me-school-board-completion/89-RESEARCH.md
@.planning/phases/89-in-me-school-board-completion/89-01-SUMMARY.md

# Canonical loaders (copy structure verbatim):
@C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts
@C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts

# Loader authored in Plan 01 (analog for Plan 02):
@C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts

# Canonical greenfield multi-government SCHOOL board migration (mirror this structure):
@C:/EV-Accounts/backend/migrations/254_or_school_districts.sql
@C:/EV-Accounts/backend/migrations/257_ca_city_school_boards.sql
@C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql

# Canonical smoke test pattern:
@C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts
@C:/EV-Accounts/backend/scripts/smoke-phase89-in.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write load-me-school-boundaries.ts (copy TX/IN loader, swap to ME, 5 GEOIDs)</name>
  <files>C:/EV-Accounts/backend/scripts/load-me-school-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts (canonical template — copy verbatim)
    - C:/EV-Accounts/backend/scripts/load-in-school-boundaries.ts (sibling script from Plan 01 — confirms the substitution pattern)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§TIGER File Verification — Maine row with all 5 GEOIDs)
  </read_first>
  <behavior>
    - Loader downloads `https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_23_unsd.zip` to `.tmp-me-school-unsd/`, extracts, opens shapefile, filters to the 5 target GEOIDs, and INSERTs G5420 rows into `essentials.geofence_boundaries` with state='23', source='tiger_unsd_me_2024'.
    - `--dry-run` mode prints `[dry-run] Would insert` lines and does NOT write to DB.
    - Asserts `districtMap.size === 5` after the shapefile loop.
    - Post-insert verification COUNT against `state='23' AND mtfcc='G5420' AND source='tiger_unsd_me_2024'` returns 5.
  </behavior>
  <action>
    Create a new file at `C:/EV-Accounts/backend/scripts/load-me-school-boundaries.ts` by copying `C:/EV-Accounts/backend/scripts/load-tx-school-boundaries.ts` verbatim and applying ONLY these substitutions:

    1. **Header docstring** — swap "Texas" / "tx" / "48" references for "Maine" / "me" / "23" and update example GEOID from `'4835100'` to `'2307320'`. Usage block:
       `npx tsx scripts/load-me-school-boundaries.ts --dry-run`
       `npx tsx scripts/load-me-school-boundaries.ts`

    2. **Config block** — set exactly:
       - `TIGER_URL = 'https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_23_unsd.zip'`
       - `MTFCC = 'G5420'`
       - `STATE = '23'` (Maine FIPS)
       - `SOURCE = 'tiger_unsd_me_2024'`
       - `EXPECTED_COUNT = 5`
       - `TARGET_GEOIDS = new Map<string, string>([['2307320','Lewiston Public Schools'],['2302820','Bangor School Department'],['2312330','South Portland Public Schools'],['2302610','Auburn Public Schools'],['2303150','Biddeford Public Schools']])`
       - `baseName = 'tl_2024_23_unsd'`
       - `tmpRoot = path.join(process.cwd(), '.tmp-me-school-unsd')`

    3. **Console log labels** — change `[load-tx-school-boundaries]` to `[load-me-school-boundaries]` everywhere (3 occurrences).

    4. **Main() entry log** — change `'Fetching TX UNSD boundaries'` to `'Fetching ME UNSD boundaries'`.

    5. **Post-insert success log** — change `'All ${EXPECTED_COUNT} TX school district boundaries loaded successfully.'` to `'All ${EXPECTED_COUNT} ME school district boundaries loaded successfully.'`.

    All other code copies verbatim: imports, `downloadWithRedirects`, `extractZip`, shapefile open/read loop, GEOID field assertion, districtMap.size assertion against EXPECTED_COUNT, the `pool.query` PostGIS INSERT with 8 parameters, inserted/skipped counters, `.finally(() => void pool.end())` cleanup.

    Do NOT add new logic. Do NOT extend `load-state-tiger-boundaries.ts`. This is standalone, like the IN and TX siblings.

    Per memory `feedback_no_git_in_ev_accounts.md`: never run git in `C:/EV-Accounts/backend`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/load-me-school-boundaries.ts --dry-run 2>&amp;1 | findstr /C:"All 5 target GEOIDs found"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/load-me-school-boundaries.ts`
    - File contains literal `TIGER_URL    = 'https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_23_unsd.zip'`
    - File contains literal `STATE        = '23'`
    - File contains literal `SOURCE       = 'tiger_unsd_me_2024'`
    - File contains literal `EXPECTED_COUNT = 5`
    - File contains literal `'2307320', 'Lewiston Public Schools'`
    - File contains literal `'2302820', 'Bangor School Department'`
    - File contains literal `'2312330', 'South Portland Public Schools'`
    - File contains literal `'2302610', 'Auburn Public Schools'`
    - File contains literal `'2303150', 'Biddeford Public Schools'`
    - File contains literal `baseName = 'tl_2024_23_unsd'`
    - File contains literal `.tmp-me-school-unsd`
    - File contains literal `[load-me-school-boundaries]` (does NOT contain `[load-tx-school-boundaries]` or `[load-in-school-boundaries]`)
    - Dry-run from `C:/EV-Accounts/backend` exits 0 and shows 5 `[dry-run] Would insert` lines for the 5 expected GEOIDs
  </acceptance_criteria>
  <done>Loader file exists; dry-run succeeds without DB writes; shapefile contains all 5 ME target GEOIDs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run load-me-school-boundaries.ts (live) — insert 5 G5420 rows</name>
  <files>essentials.geofence_boundaries (DB only — no source file modified)</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-me-school-boundaries.ts (just authored)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Environment Availability — confirms tl_2024_23_unsd.zip is 2.6 MB; §Section-Split Check post-migration 265 — gates expected after this task)
  </read_first>
  <behavior>
    - Loader runs in live mode, downloads the TIGER zip (or uses cache), and inserts 5 G5420 rows into `essentials.geofence_boundaries`.
    - All 5 rows have state='23', mtfcc='G5420', source='tiger_unsd_me_2024', non-NULL PostGIS geometry.
    - Re-running is a no-op via `ON CONFLICT (geo_id, mtfcc) DO NOTHING`.
  </behavior>
  <action>
    Execute the loader against production DB:
    1. Confirm cwd is `C:/EV-Accounts/backend` (dotenv looks there for `.env`).
    2. Confirm `DATABASE_URL` is set.
    3. Run: `npx tsx scripts/load-me-school-boundaries.ts` (no `--dry-run`).
    4. Loader logs each insert as `Inserted: <geoid> (<name>)` and final `All 5 ME school district boundaries loaded successfully.`
    5. If the run fails, fix the loader — do not manually patch the DB.

    Per memory `feedback_no_git_in_ev_accounts.md`: do NOT run git in `C:/EV-Accounts`.
    Per memory `feedback_supabase_local_is_remote.md`: every write is live production.
  </action>
  <verify>
    <automated>node -e "const {Pool}=require('pg');require('dotenv').config({path:'C:/EV-Accounts/backend/.env'});const p=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});p.query(\"SELECT COUNT(*)::int AS cnt FROM essentials.geofence_boundaries WHERE state='23' AND mtfcc='G5420' AND source='tiger_unsd_me_2024'\").then(r=>{console.log('count='+r.rows[0].cnt);process.exit(r.rows[0].cnt===5?0:1)}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='23' AND mtfcc='G5420' AND source='tiger_unsd_me_2024'` returns 5
    - `SELECT geo_id FROM essentials.geofence_boundaries WHERE state='23' AND mtfcc='G5420' ORDER BY geo_id` returns exactly `2302610`, `2302820`, `2303150`, `2307320`, `2312330` (no extras, no missing)
    - PostGIS ST_Covers query at Lewiston (-70.2140, 44.0978) returns a G5420 row with name matching `Lewiston Public Schools`
    - PostGIS ST_Covers query at Bangor (-68.7772, 44.8012) returns a G5420 row with name matching `Bangor School Department`
    - PostGIS ST_Covers query at South Portland (-70.2788, 43.6415) returns a G5420 row with name matching `South Portland Public Schools`
    - PostGIS ST_Covers query at Auburn (-70.2312, 44.0978) returns a G5420 row with name matching `Auburn Public Schools`
    - PostGIS ST_Covers query at Biddeford (-70.4520, 43.4909) returns a G5420 row with name matching `Biddeford Public Schools`
  </acceptance_criteria>
  <done>5 G5420 rows persisted with valid PostGIS geometry; all 5 ME target coordinates route to their correct district geofence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Resolve roster open questions — pre-execution DB + website verification for Lewiston/South Portland/Bangor/Auburn rosters</name>
  <files>None (read-only verification, results captured into Task 5's migration draft and into the Plan 02 SUMMARY)</files>
  <read_first>
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Maine Board Rosters — full per-district sections; §Open Questions 1, 2, 3; §Assumptions Log A3-A10; §Common Pitfalls 6 and 7)
  </read_first>
  <behavior>
    - 4 verification steps run independently and produce a single Roster Reconciliation Table (committed to SUMMARY) before Task 5 writes any SQL.
    - For each [ASSUMED] or open-question item, the table records: official source URL checked, name found (or "still vacant" / "not on page"), and decision (use research name / use new name / mark vacant).
    - If a website is unreachable (JS-rendered + WebFetch fails), use curl with `User-Agent: Mozilla/5.0` and log the response; if still no result, follow RESEARCH.md guidance: use the research roster + `is_vacant=true` for unverified seats.
  </behavior>
  <action>
    Execute these 4 verification steps in order. Do NOT proceed to Task 5 (migration drafting) until all 4 are complete and captured.

    **Step 1 — Lewiston Ward 5 appointee (Open Question 1, Pitfall 7):**
    - Fetch `https://www.lewistonpublicschools.org/en-US/school-committee-bc9f3846/school-committee-members-ab24c81b`.
    - Identify the current Ward 5 member's name.
    - Decision matrix:
      - If a name is listed → use that name in the migration (UTF-8 if any accents). Record exact source URL + name.
      - If "Vacant" or no Ward 5 entry → INSERT a placeholder with `full_name='VACANT - Ward 5'`, `first_name=NULL`, `last_name=NULL` (use single-quote NULL via SQL `NULL` literal, not the string 'NULL'), `is_vacant=true`, `is_appointed=true` (appointee process). external_id=-890015.

    **Step 2 — South Portland complete roster (Open Question 2, Pitfall 6):**
    - Fetch `https://www.spsd.org/board/members-of-the-board` (and `https://www.spsdme.org/board` as a fallback).
    - For each of the 7 seats (D1, D2, D3, D4, D5, At-Large×2), record the current officeholder's full name and seat label.
    - Compare against the RESEARCH.md §South Portland Public Schools table. If RESEARCH names match → use them. If different → use the website roster and document the discrepancy in the SUMMARY's Roster Reconciliation Table.
    - If D5 is still vacant: INSERT placeholder with `is_vacant=true` (same convention as Lewiston Ward 5).

    **Step 3 — Bangor Sara Luciano status (Open Question, A8):**
    - Fetch `https://www.bangorschools.net/page/school-committee`.
    - Determine whether Sara Luciano is still a committee member.
    - If yes → include her as the 7th member with title 'School Committee Member' (no qualifier).
    - If no → use whichever name is listed in her place on the official roster.

    **Step 4 — Auburn full roster including holdovers (Open Question 3, A7):**
    - Fetch `https://auburnschl.edu` and locate the School Committee roster page.
    - Confirm: Ward 1 Korin McGuigan, Ward 2 Misty Edgecomb, Ward 3 Patricia Gautier, Ward 4 Lydia Chapman, Ward 5 Daniel F. Poisson Sr. (5 ward members from Nov 2025 winners).
    - For the 3 At-Large seats: confirm Pamela Albert, Olivia Jaye Rich, Nancy Pulk all currently serve. If any holdover incumbents from prior cycles still serve (not on the Nov 2025 ballot), document and decide on the full 8-member roster.

    **Output of this task:** A "Roster Reconciliation Table" with one row per district summarizing: source URL fetched, names confirmed/changed/vacant, and the final 37-row name list. This table goes into Task 5's pre-flight comment block in the migration AND into the Plan 02 SUMMARY's deviations section.

    Per memory `feedback_no_git_in_ev_accounts.md`: do NOT run git in `C:/EV-Accounts`.
    Per memory `feedback_headshot_image_sizing.md` and `feedback_headshot_no_graphics.md`: do NOT process or upload any headshots in this task — that is Plan 03.
  </action>
  <verify>
    <human-check>Reconciliation table exists with one row per district covering source URLs fetched + final 37-row roster + any deviations from RESEARCH.md. The table is captured in the Plan 02 SUMMARY working draft.</human-check>
  </verify>
  <acceptance_criteria>
    - Roster Reconciliation Table covers all 4 open questions (Lewiston Ward 5, South Portland full roster, Bangor 7th member, Auburn full roster including holdovers)
    - Lewiston Ward 5 row records: either a confirmed appointee name + source URL, OR the placeholder decision `is_vacant=true` with explanation
    - South Portland section records: each of the 7 seats' confirmed officeholder, OR a documented vacancy + source URL
    - Bangor section records: Sara Luciano's confirmed status (still member / replaced / departed) + the 7th member's name + source URL
    - Auburn section records: all 8 members (5 wards from Nov 2025 + 3 at-large) confirmed against the official site + any holdovers
    - Final roster has exactly 37 names (8 Lewiston + 7 Bangor + 7 South Portland + 8 Auburn + 7 Biddeford)
    - Roster table captured in the Plan 02 working SUMMARY for re-use by Task 5
  </acceptance_criteria>
  <done>All 4 open-question rosters verified; final 37-name roster captured for use by Task 5.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write smoke-phase89-me.ts smoke test (RED step before migration 265 applied)</name>
  <files>C:/EV-Accounts/backend/scripts/smoke-phase89-me.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts (canonical smoke test pattern)
    - C:/EV-Accounts/backend/scripts/smoke-phase89-in.ts (sibling smoke from Plan 01 — mirror its structure)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Validation Architecture — Smoke Test Coordinates table for all 5 ME districts)
  </read_first>
  <behavior>
    - Smoke test verifies 8 assertions: G5420 count, SCHOOL districts count, 5 coordinate routing tests, section-split check, no NULL office_id.
    - All assertions in single try/finally; errors accumulate; exit 0 if all pass else 1.
    - Pre-migration RED state: SC1 (G5420=5) passes after Task 2; SC2-SC8 must FAIL until migration 265 applies.
  </behavior>
  <action>
    Create `C:/EV-Accounts/backend/scripts/smoke-phase89-me.ts` mirroring `smoke-phase89-in.ts` structure. Use the same Client + try/finally + error accumulator pattern.

    EXACT 8 ASSERTIONS (each must log `SC<N>: PASS` or push to errors[]):

    **SC1: 5 G5420 geofence_boundaries rows for ME.**
    Query: `SELECT COUNT(*)::int AS cnt FROM essentials.geofence_boundaries WHERE state='23' AND mtfcc='G5420' AND source='tiger_unsd_me_2024'`. Expected: 5.

    **SC2: 5 SCHOOL districts rows for ME (state='me' lowercase per RESEARCH.md Pitfall 5).**
    Query: `SELECT COUNT(*)::int AS cnt FROM essentials.districts WHERE district_type='SCHOOL' AND state='me' AND geo_id IN ('2307320','2302820','2312330','2302610','2303150')`. Expected: 5.

    **SC3-SC7: One routing assertion per ME district.** Each runs the standard SCHOOL routing JOIN with ST_Covers and the district's coordinate. For each: assert `rows.length >= <expected_min>` AND every returned row has `d.geo_id === <district_geo_id>` AND at least one confirmed-high-confidence name from that district appears in `full_name` fields (name-level validation, mirrors SC3/SC4 in smoke-phase89-in.ts).

    - SC3 Lewiston: parameters [-70.2140, 44.0978]; expected_min=7 (8 members, at least 7 if Ward 5 is vacant); expected geo_id='2307320'; name_assert: at least one row has full_name='Luke Jensen' (At-Large, election-confirmed HIGH confidence)
    - SC4 Bangor: parameters [-68.7772, 44.8012]; expected_min=7; expected geo_id='2302820'; name_assert: at least one row has full_name='Tim Surrette' (Chair, election-confirmed HIGH confidence)
    - SC5 South Portland: parameters [-70.2788, 43.6415]; expected_min=6 (7 members, at least 6 if D5 is vacant); expected geo_id='2312330'; name_assert: at least one row has full_name='Rosemarie De Angelis' (D3 Chair, confirmed HIGH confidence)
    - SC6 Auburn: parameters [-70.2312, 44.0978]; expected_min=8; expected geo_id='2302610'; name_assert: at least one row has full_name='Korin McGuigan' (Ward 1, election-confirmed HIGH confidence)
    - SC7 Biddeford: parameters [-70.4520, 43.4909]; expected_min=7; expected geo_id='2303150'; name_assert: at least one row has full_name='Meagan Desjardins' (highest vote-getter Nov 2025, HIGH confidence)

    Routing JOIN (use this exact pattern):
    ```
    SELECT p.full_name, d.geo_id
    FROM essentials.politicians p
    JOIN essentials.offices o ON o.politician_id = p.id
    JOIN essentials.districts d ON d.id = o.district_id
    JOIN essentials.geofence_boundaries gb ON gb.geo_id = d.geo_id
    WHERE gb.state = '23'
      AND d.district_type = 'SCHOOL'
      AND gb.mtfcc = 'G5420'
      AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($1, $2), 4326))
    ORDER BY p.full_name
    ```

    **SC8: Section-split check (per RESEARCH.md §Section-Split Check post-migration 265).**
    Query: `SELECT gb.geo_id FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('2307320','2302820','2312330','2302610','2303150') AND gb.mtfcc='G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.district_type='SCHOOL' AND d.state='me')`. Expected: 0 rows.

    FINAL OUTPUT BLOCK: `=== Phase 89 ME Smoke Test Results ===` + per-SC PASS/FAIL lines + `ALL ASSERTIONS PASSED` or `FAILED (<N>)` summary + `process.exit(0 or 1)`.

    After writing, run `npx tsx scripts/smoke-phase89-me.ts` once. Pre-migration-265 state: this MUST exit non-zero with SC2-SC8 failing (SC1 passes from Task 2's loader run). This is the RED proof.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-phase89-me.ts; if [ $? -ne 0 ]; then echo "RED-PHASE-PASS: smoke fails before migration 265 applied (expected)"; else echo "FAIL: smoke passed before migration applied — invalid initial state"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: smoke-phase89-me.ts exists
    - SOURCE: contains all 5 ME GEOIDs: `'2307320'`, `'2302820'`, `'2312330'`, `'2302610'`, `'2303150'`
    - SOURCE: contains `district_type = 'SCHOOL'` and `state = 'me'` (lowercase)
    - SOURCE: contains `gb.state = '23'` and `gb.mtfcc = 'G5420'`
    - SOURCE: contains all 5 coordinate pairs from RESEARCH.md §Smoke Test Coordinates
    - SOURCE: contains `ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($1, $2), 4326))`
    - SOURCE: contains a section-split NOT EXISTS query covering all 5 ME GEOIDs
    - SOURCE: contains `process.exit(0)` on all-pass and `process.exit(1)` on any-fail
    - BEHAVIOR (RED step): `npx tsx scripts/smoke-phase89-me.ts` exits non-zero before migration 265 applies; SC2-SC8 fail; SC1 passes
    - BEHAVIOR: stdout output identifies each SC as PASS or FAIL with explanation
  </acceptance_criteria>
  <done>smoke-phase89-me.ts authored; pre-migration RED run exits non-zero with the expected failures.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Write migration 265 — 5 governments + chambers + SCHOOL districts + 37 politicians + 37 offices</name>
  <files>C:/EV-Accounts/backend/migrations/265_me_city_school_boards.sql</files>
  <read_first>
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Maine Board Rosters; §Migration 265 — ME School Board Seed Pattern; §External ID Allocation; §Common Pitfalls 5, 6, 7)
    - Task 3 Roster Reconciliation Table (37-name final roster + any deviations from RESEARCH.md)
    - C:/EV-Accounts/backend/migrations/254_or_school_districts.sql (canonical 6-government greenfield school board migration — use this as the template; OR pattern is the closest match because it includes multiple districts with mixed seat types)
    - C:/EV-Accounts/backend/migrations/261_tx_collin_county_school_boards.sql (secondary reference — pre-flight + post-verification DO block structure)
  </read_first>
  <behavior>
    - Migration applies in a single `BEGIN; ... COMMIT;` transaction.
    - 3 pre-flight DO blocks abort with RAISE EXCEPTION if (a) any of the 5 government names already exist (re-run guard), (b) external_id block -890011..-890057 is not clear, (c) the 5 G5420 geofence_boundaries rows do not exist.
    - 5 government INSERTs use WHERE NOT EXISTS; `state='ME'` UPPERCASE; `type='LOCAL'`; `city=NULL`; geo_id=district GEOID.
    - 5 chamber INSERTs use WHERE NOT EXISTS; chamber `name` = 'School Committee' for Lewiston/Bangor/Auburn/Biddeford; 'Board of Education' for South Portland (per RESEARCH.md per-district sections); NO slug column included.
    - 5 SCHOOL districts INSERTs: `district_type='SCHOOL'`, `state='me'` LOWERCASE, `mtfcc='G5420'`, geo_id+label per district.
    - 37 politician+office CTE blocks, one per board member: `party=NULL`, `is_appointed=false` (except Lewiston Ward 5 if appointee/vacant per Task 3 reconciliation), `is_active=true`, `is_incumbent=true`, `is_vacant=false` (except verified vacancies); office uses `representing_state='ME'` UPPERCASE, `is_appointed_position=false` (unless Lewiston Ward 5 = appointee), title per RESEARCH.md per-district convention.
    - office_id back-fill UPDATE for `external_id BETWEEN -890057 AND -890011 AND office_id IS NULL`.
    - Post-verification DO block with 8 gates (govs=5, chambers=5, SCHOOL districts=5, politicians=37, offices=37, section-split=0, NULL office_id=0, name spot-check for each district).
    - Ledger entry: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('265') ON CONFLICT (version) DO NOTHING;` before COMMIT.
    - File saved as UTF-8 (preserves any accented characters in ME names).
  </behavior>
  <action>
    Write `C:/EV-Accounts/backend/migrations/265_me_city_school_boards.sql` mirroring the structure of `254_or_school_districts.sql` but with ME data per RESEARCH.md §Maine Board Rosters and the Task 3 Roster Reconciliation Table.

    **State casing (per RESEARCH.md Pitfall 5):** `governments.state='ME'` UPPERCASE, `districts.state='me'` LOWERCASE, `offices.representing_state='ME'` UPPERCASE. Wrong casing breaks routing.

    **Header comment block:** Match the structure of migration 254's header:
    ```
    -- Migration 265: Maine city school board seed (Lewiston + Bangor + South Portland + Auburn + Biddeford)
    -- Phase 89 Plan 02. Greenfield — no existing rows for these 5 districts.
    --
    -- Districts (per RESEARCH.md §TIGER File Verification):
    --   Lewiston Public Schools       (geo_id='2307320', 8 members: 7 wards + 1 at-large)
    --   Bangor School Department      (geo_id='2302820', 7 at-large members)
    --   South Portland Public Schools (geo_id='2312330', 7 members — district + at-large mix)
    --   Auburn Public Schools         (geo_id='2302610', 8 members: 5 wards + 3 at-large)
    --   Biddeford Public Schools      (geo_id='2303150', 7 at-large members)
    --
    -- Totals: 5 govs, 5 chambers, 5 SCHOOL districts, 37 politicians, 37 offices.
    --
    -- CRITICAL rules (per RESEARCH.md §Common Pitfalls):
    --   - districts.state = 'me' LOWERCASE (Pitfall 5; matches essentialsService.ts routing)
    --   - governments.state = 'ME' UPPERCASE
    --   - offices.representing_state = 'ME' UPPERCASE
    --   - district_type = 'SCHOOL' (NOT 'SCHOOL_DISTRICT')
    --   - slug NEVER included in chambers INSERT (GENERATED ALWAYS)
    --   - All 5 G5420 geofence_boundaries rows MUST exist before this migration (run load-me-school-boundaries.ts first)
    --
    -- Roster reconciliation (per Task 3 verification of Open Questions 1, 2, 3):
    --   <one line per district summarizing: source URL fetched + names confirmed/changed/vacant>
    --
    -- Run: <YYYY-MM-DD>
    ```

    **Pre-flight DO blocks (3):**
    1. Government names not yet present: `IF EXISTS (SELECT 1 FROM essentials.governments WHERE name IN ('Lewiston Public Schools, Maine, US','Bangor School Department, Maine, US','South Portland Public Schools, Maine, US','Auburn Public Schools, Maine, US','Biddeford Public Schools, Maine, US'))` RAISE EXCEPTION 'Migration 265 already applied — aborting re-run'.
    2. external_id block clear: `IF EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id BETWEEN -890057 AND -890011)` RAISE EXCEPTION 'Pre-flight FAILED: external_id block -890011..-890057 not clear'.
    3. G5420 rows exist: `SELECT COUNT(*) INTO v_count FROM essentials.geofence_boundaries WHERE geo_id IN ('2307320','2302820','2312330','2302610','2303150') AND mtfcc='G5420'; IF v_count <> 5 THEN RAISE EXCEPTION 'Pre-flight FAILED: expected 5 G5420 rows for ME, found % — run load-me-school-boundaries.ts first', v_count; END IF;`

    **5 government INSERTs (WHERE NOT EXISTS):**
    - 'Lewiston Public Schools, Maine, US', geo_id='2307320'
    - 'Bangor School Department, Maine, US', geo_id='2302820'
    - 'South Portland Public Schools, Maine, US', geo_id='2312330'
    - 'Auburn Public Schools, Maine, US', geo_id='2302610'
    - 'Biddeford Public Schools, Maine, US', geo_id='2303150'
    Pattern: `INSERT INTO essentials.governments (id, name, type, state, city, geo_id) SELECT gen_random_uuid(), '<name>', 'LOCAL', 'ME', NULL, '<geoid>' WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name='<name>');`

    **5 chamber INSERTs (WHERE NOT EXISTS, NO slug column):**
    Per RESEARCH.md per-district chamber names:
    - Lewiston: name='School Committee', name_formal='Lewiston Public Schools School Committee'
    - Bangor: name='School Committee', name_formal='Bangor School Department School Committee'
    - South Portland: name='Board of Education', name_formal='South Portland Public Schools Board of Education'
    - Auburn: name='School Committee', name_formal='Auburn Public Schools School Committee'
    - Biddeford: name='School Committee', name_formal='Biddeford Public Schools School Committee'
    Pattern: `INSERT INTO essentials.chambers (id, government_id, name, name_formal) SELECT gen_random_uuid(), (SELECT id FROM essentials.governments WHERE name='<gov name>'), '<chamber name>', '<name_formal>' WHERE NOT EXISTS (SELECT 1 FROM essentials.chambers WHERE name='<chamber name>' AND government_id=(SELECT id FROM essentials.governments WHERE name='<gov name>'));`

    **5 SCHOOL district INSERTs:**
    - Lewiston: district_type='SCHOOL', state='me', geo_id='2307320', label='Lewiston Public Schools', mtfcc='G5420'
    - Bangor: same pattern with geo_id='2302820', label='Bangor School Department'
    - South Portland: same pattern with geo_id='2312330', label='South Portland Public Schools'
    - Auburn: same pattern with geo_id='2302610', label='Auburn Public Schools'
    - Biddeford: same pattern with geo_id='2303150', label='Biddeford Public Schools'

    **37 politician + office WITH-CTE blocks** — use the canonical pattern from migration 254 lines 90-119. For each politician:
    - `INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id) VALUES (gen_random_uuid(), '<full>', '<first>', '<last>', NULL, true, <is_appointed>, <is_vacant>, true, <ext_id>) ON CONFLICT (external_id) DO NOTHING RETURNING id`
    - `INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical) SELECT gen_random_uuid(), d.id, ch.id, p.id, '<title>', 'ME', <is_appointed_pos>, <is_vacant>, NULL FROM essentials.districts d CROSS JOIN ins_p p JOIN essentials.chambers ch ON ch.government_id=(SELECT id FROM essentials.governments WHERE name='<gov name>') WHERE d.geo_id='<GEOID>' AND d.district_type='SCHOOL' AND d.state='me' AND ch.name='<chamber name>' AND p.id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id=d.id AND o.politician_id=p.id);`

    **External_id ranges + names (use the Task 3 Roster Reconciliation Table for the final names):**

    Lewiston (-890011..-890018, gov='Lewiston Public Schools, Maine, US', chamber='School Committee', geo_id='2307320'):
    - -890011: Phoenix McLaughlin — title 'School Committee Member (Ward 1)'
    - -890012: Janet Beaudoin — 'School Committee Member (Ward 2)'
    - -890013: Elizabeth Eames — 'School Committee Member (Ward 3)'
    - -890014: Julia Harper — 'School Committee Member (Ward 4)'
    - -890015: <Ward 5 per Task 3 verification — name OR 'VACANT - Ward 5'> — 'School Committee Member (Ward 5)' (if vacant: is_vacant=true on politician + office, is_appointed=true if appointee; if confirmed name: is_appointed=true on politician, is_appointed_position=true on office since the seat is filled by mayoral appointment after the original holder forfeited)
    - -890016: Meghan Hird — 'School Committee Member (Ward 6)'
    - -890017: Donna Gallant — 'School Committee Member (Ward 7)'
    - -890018: Luke Jensen — 'School Committee Member (At-Large)'

    Bangor (-890021..-890027, gov='Bangor School Department, Maine, US', chamber='School Committee', geo_id='2302820'):
    All 7 at-large, title = 'School Committee Member' (no qualifier per RESEARCH.md):
    - -890021: Tim Surrette
    - -890022: Katie Brydon
    - -890023: Mallory Cook
    - -890024: Ben Speed
    - -890025: Ben Sprague
    - -890026: Shelly Okere
    - -890027: <Sara Luciano OR Task 3 replacement>

    South Portland (-890031..-890037, gov='South Portland Public Schools, Maine, US', chamber='Board of Education', geo_id='2312330'):
    Use the Task 3 verified roster. Title convention: 'Board Member (District N)' for D1-D5; 'Board Member' for at-large seats.
    - -890031: <D1 name from Task 3> — 'Board Member (District 1)'
    - -890032: Tyler Smith — 'Board Member (District 2)'
    - -890033: Rosemarie De Angelis — 'Board Member (District 3)'
    - -890034: George Risch — 'Board Member (District 4)'
    - -890035: <D5 name from Task 3 OR vacant placeholder> — 'Board Member (District 5)' (if vacant: is_vacant=true on politician + office)
    - -890036: <At-Large 1 from Task 3> — 'Board Member'
    - -890037: Eleni Richardson — 'Board Member'

    Auburn (-890041..-890048, gov='Auburn Public Schools, Maine, US', chamber='School Committee', geo_id='2302610'):
    - -890041: Korin McGuigan — 'School Committee Member (Ward 1)'
    - -890042: Misty Edgecomb — 'School Committee Member (Ward 2)'
    - -890043: Patricia Gautier — 'School Committee Member (Ward 3)'
    - -890044: Lydia Chapman — 'School Committee Member (Ward 4)'
    - -890045: Daniel F. Poisson Sr. — 'School Committee Member (Ward 5)' (note: 'Sr.' is part of last_name OR full_name; split per existing project convention from Sacramento's 'Rick Jennings II' precedent — last_name='Poisson Sr.' or per Task 3 verification)
    - -890046: Pamela Albert — 'School Committee Member (At-Large)'
    - -890047: Olivia Jaye Rich — 'School Committee Member (At-Large)'
    - -890048: Nancy Pulk — 'School Committee Member (At-Large)'

    Biddeford (-890051..-890057, gov='Biddeford Public Schools, Maine, US', chamber='School Committee', geo_id='2303150'):
    All 7 at-large, title = 'School Committee Member' (no qualifier):
    - -890051: Amy Clearwater
    - -890052: Meagan Desjardins
    - -890053: Michele Landry
    - -890054: Marie Potvin
    - -890055: Timothy Stebbins
    - -890056: Karen Ruel
    - -890057: Emily Henley

    **office_id back-fill (after all 37 blocks):**
    `UPDATE essentials.politicians p SET office_id=o.id FROM essentials.offices o WHERE o.politician_id=p.id AND p.external_id BETWEEN -890057 AND -890011 AND p.office_id IS NULL;`

    **Post-verification DO block (8 gates — RAISE EXCEPTION on any failure):**
    - (a) COUNT essentials.governments WHERE name IN (5 names) = 5
    - (b) COUNT essentials.chambers WHERE government_id IN (5 govs subquery) AND name IN ('School Committee','Board of Education') = 5
    - (c) COUNT essentials.districts WHERE district_type='SCHOOL' AND state='me' AND geo_id IN (5 GEOIDs) = 5
    - (d) COUNT essentials.politicians WHERE external_id BETWEEN -890057 AND -890011 = 37
    - (e) COUNT essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id BETWEEN -890057 AND -890011 = 37
    - (f) Section-split: `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('2307320','2302820','2312330','2302610','2303150') AND gb.mtfcc='G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.district_type='SCHOOL' AND d.state='me')` = 0
    - (g) office_id back-fill: COUNT essentials.politicians WHERE external_id BETWEEN -890057 AND -890011 AND office_id IS NULL = 0
    - (h) Per-district roster size spot-check (5 sub-checks):
      - Lewiston offices = 8 (`SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE g.name='Lewiston Public Schools, Maine, US'` = 8)
      - Bangor = 7, South Portland = 7, Auburn = 8, Biddeford = 7
    - On all-pass: `RAISE NOTICE 'Migration 265 post-verification PASSED: 5 govs, 5 chambers, 5 SCHOOL districts, 37 politicians, 37 offices, section-split=0, office_id back-fill complete.'`

    **Ledger entry + COMMIT:**
    `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('265') ON CONFLICT (version) DO NOTHING;`
    `COMMIT;`

    **File encoding:** UTF-8. Preserve any accented characters (e.g. if Auburn `Olivia Jaye Rich` contains diacritics in the live source, preserve them).

    Do NOT apply the migration in this task — Task 6 applies it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('C:/EV-Accounts/backend/migrations/265_me_city_school_boards.sql','utf8');const needles=['Migration 265','Lewiston Public Schools, Maine, US','Bangor School Department, Maine, US','South Portland Public Schools, Maine, US','Auburn Public Schools, Maine, US','Biddeford Public Schools, Maine, US','district_type','SCHOOL','2307320','2302820','2312330','2302610','2303150','-890011','-890057','VALUES (\\u0027265\\u0027)'];const missing=needles.filter(n=>!c.includes(n));if(missing.length){console.error('MISSING:',missing);process.exit(1)}if(c.includes('SCHOOL_DISTRICT')){console.error('FAIL: contains forbidden SCHOOL_DISTRICT');process.exit(1)}if(/INSERT\\s+INTO\\s+essentials\\.chambers[^;]*\\bslug\\b/i.test(c)){console.error('FAIL: chambers INSERT includes forbidden slug column');process.exit(1)}if(/district_type\\s*=\\s*\\u0027SCHOOL\\u0027\\s+AND\\s+state\\s*=\\s*\\u0027ME\\u0027/i.test(c)){console.error('FAIL: districts.state must be lowercase me, found uppercase ME');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/265_me_city_school_boards.sql`
    - File contains literal `Migration 265`
    - File contains all 5 ME government names (`Lewiston Public Schools, Maine, US`, `Bangor School Department, Maine, US`, `South Portland Public Schools, Maine, US`, `Auburn Public Schools, Maine, US`, `Biddeford Public Schools, Maine, US`)
    - File contains all 5 ME GEOIDs: `2307320`, `2302820`, `2312330`, `2302610`, `2303150`
    - File contains `state='me'` (lowercase, for districts.state — RESEARCH.md Pitfall 5)
    - File contains `'ME'` UPPERCASE for governments.state and offices.representing_state
    - File contains `'SCHOOL'` for district_type and does NOT contain `'SCHOOL_DISTRICT'`
    - File contains both chamber names: `School Committee` (4 districts) and `Board of Education` (South Portland)
    - File contains external_id literals for the full range: at least `-890011` (Lewiston start) and `-890057` (Biddeford end)
    - File contains 37 distinct external_id integers (-890011..-890018, -890021..-890027, -890031..-890037, -890041..-890048, -890051..-890057) — verify each appears exactly once in a politician INSERT
    - File contains `VALUES ('265')` (ledger entry)
    - File does NOT include the column `slug` in any `INSERT INTO essentials.chambers` statement
    - 3 pre-flight DO blocks present (government names check, external_id block check, geofence existence check)
    - 8-gate post-verification DO block present
    - office_id back-fill UPDATE present, scoped to `external_id BETWEEN -890057 AND -890011`
  </acceptance_criteria>
  <done>Migration 265 SQL file authored with all 5 ME districts, 37 board members, full pre-flight + post-verification gates, UTF-8 encoded, ready for apply.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Apply migration 265 to production DB and run smoke-phase89-me.ts (GREEN)</name>
  <files>
    - essentials.governments (5 new LOCAL school district rows)
    - essentials.chambers (5 new School Committee/Board of Education rows)
    - essentials.districts (5 new SCHOOL rows for ME)
    - essentials.politicians (37 new rows with external_ids -890011..-890057)
    - essentials.offices (37 new rows with representing_state='ME', linked to SCHOOL districts)
    - supabase_migrations.schema_migrations (INSERT version='265')
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/265_me_city_school_boards.sql (authored in Task 5)
    - C:/EV-Accounts/backend/scripts/smoke-phase89-me.ts (authored in Task 4)
    - .planning/phases/89-in-me-school-board-completion/89-RESEARCH.md (§Validation Architecture — Smoke Test Coordinates)
  </read_first>
  <behavior>
    - Migration 265 applies via `mcp__supabase-local__apply_migration` (production DB per memory `feedback_supabase_local_is_remote.md`).
    - All 3 pre-flight DO blocks pass; all 8 post-verification gates pass.
    - Ledger row for version='265' present.
    - Smoke test `smoke-phase89-me.ts` exits 0 with all 8 SC PASS.
    - Re-applying migration 265 raises the pre-flight EXCEPTION 'Migration 265 already applied' (idempotency).
  </behavior>
  <action>
    1. Apply migration 265 via `mcp__supabase-local__apply_migration`. Migration name: `265_me_city_school_boards`. Pass the entire SQL content.
    2. If a pre-flight `RAISE EXCEPTION` fires (e.g. "5 G5420 rows not found"), do NOT bypass — fix the underlying cause and re-run.
    3. Confirm `RAISE NOTICE 'Migration 265 post-verification PASSED'` appears in the apply output.
    4. Run independent verification queries via `mcp__supabase-local__execute_sql`:
       - `SELECT COUNT(*) FROM essentials.governments WHERE name IN (5 ME school gov names)` returns 5
       - `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='me' AND geo_id IN ('2307320','2302820','2312330','2302610','2303150')` returns 5
       - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -890057 AND -890011` returns 37
       - `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id BETWEEN -890057 AND -890011` returns 37
       - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -890057 AND -890011 AND office_id IS NULL` returns 0
       - `SELECT version FROM supabase_migrations.schema_migrations WHERE version='265'` returns 1
    5. Run smoke test: `cd /c/EV-Accounts/backend && npx tsx scripts/smoke-phase89-me.ts`. Must exit 0 with `ALL ASSERTIONS PASSED` printed.
    6. Idempotency check: attempt to re-apply migration 265. The pre-flight `RAISE EXCEPTION 'Migration 265 already applied — aborting re-run'` must fire.

    Per memory `feedback_no_git_in_ev_accounts.md`: do NOT run git in `C:/EV-Accounts`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-phase89-me.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration 265 applied via `mcp__supabase-local__apply_migration` (final `RAISE NOTICE 'Migration 265 post-verification PASSED'` visible)
    - `SELECT COUNT(*) FROM supabase_migrations.schema_migrations WHERE version='265'` returns 1
    - `SELECT COUNT(*) FROM essentials.governments WHERE name IN (5 ME school gov names)` returns 5
    - `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='me' AND geo_id IN ('2307320','2302820','2312330','2302610','2303150')` returns 5
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -890057 AND -890011` returns 37
    - `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id BETWEEN -890057 AND -890011` returns 37
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -890057 AND -890011 AND office_id IS NULL` returns 0
    - PostGIS routing query at Lewiston (-70.2140, 44.0978) returns ≥ 7 rows all with geo_id='2307320'
    - PostGIS routing query at Bangor (-68.7772, 44.8012) returns 7 rows all with geo_id='2302820'
    - PostGIS routing query at South Portland (-70.2788, 43.6415) returns ≥ 6 rows all with geo_id='2312330'
    - PostGIS routing query at Auburn (-70.2312, 44.0978) returns 8 rows all with geo_id='2302610'
    - PostGIS routing query at Biddeford (-70.4520, 43.4909) returns 7 rows all with geo_id='2303150'
    - `npx tsx scripts/smoke-phase89-me.ts` exits 0 and stdout contains `ALL ASSERTIONS PASSED`
    - Re-applying migration 265 fails with pre-flight `RAISE EXCEPTION 'Migration 265 already applied — aborting re-run'`
  </acceptance_criteria>
  <done>Migration 265 applied, all 8 post-verification gates green, smoke test passes with ALL ASSERTIONS PASSED, idempotency confirmed. ME-SCHOOL-01, ME-SCHOOL-02, ME-SCHOOL-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov → loader → DB | TIGER UNSD download (HTTPS) and PostGIS insert to production DB. Trusted public data. |
| Official ME school district websites → Task 3 verification | Untrusted HTTP fetches for roster verification. No data is written from these fetches; output is recorded in SUMMARY and used to draft SQL constants. |
| Migration file → DB | SQL applied via Supabase MCP server. No user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-07 | Tampering | TIGER zip integrity | accept | census.gov HTTPS; EXPECTED_COUNT=5 assertion catches malformed extracts. |
| T-89-08 | Tampering | Stale or incorrect ME school board roster | mitigate | Task 3 explicit pre-execution verification of every [ASSUMED] name against official district websites; placeholder `is_vacant=true` for unconfirmed seats; Roster Reconciliation Table captured in SUMMARY for audit trail. |
| T-89-09 | Repudiation | Migration ledger | mitigate | `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('265')`; 3 pre-flight EXCEPTION paths prevent silent re-runs. |
| T-89-10 | Information Disclosure | Board member PII | accept | All seeded data is public record from official ME school district websites. |
| T-89-11 | Denial of Service | Migration locks production DB | mitigate | Single transaction with bounded scope (5 govs + 37 politicians + 37 offices); apply time typically < 10s. |
| T-89-12 | Tampering | Cross-district pollution (e.g. Bangor address routes to Lewiston) | mitigate | Each office's WHERE clause requires `d.geo_id='<this district GEOID>' AND d.district_type='SCHOOL' AND d.state='me' AND ch.name='<this chamber name>'` — no ambiguous match possible; smoke SC3-SC7 verify district isolation by checking every returned row has the expected geo_id. |
| T-89-SC | Tampering | npm package installs | accept | No new dependencies — pg, adm-zip, shapefile, dotenv, https already present. |
</threat_model>

<verification>
After all 6 tasks complete:

1. `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='23' AND mtfcc='G5420' AND source='tiger_unsd_me_2024'` returns 5
2. `SELECT COUNT(*) FROM essentials.districts WHERE district_type='SCHOOL' AND state='me' AND geo_id IN (5 ME GEOIDs)` returns 5
3. `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -890057 AND -890011` returns 37
4. `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id BETWEEN -890057 AND -890011` returns 37
5. All 5 PostGIS coordinate smoke queries route to their correct ME district
6. Section-split check returns 0 rows for the 5 ME GEOIDs
7. `npx tsx scripts/smoke-phase89-me.ts` exits 0 with ALL ASSERTIONS PASSED
8. Re-running migration 265 raises 'already applied — aborting re-run'
9. supabase_migrations.schema_migrations contains version='265'
</verification>

<success_criteria>
Plan 02 is COMPLETE when ALL of the following are true:

1. `C:/EV-Accounts/backend/scripts/load-me-school-boundaries.ts` exists; dry-run reports all 5 target GEOIDs found
2. Loader has run live: 5 G5420 rows present in `essentials.geofence_boundaries`
3. Task 3 Roster Reconciliation Table captured for all 4 open questions (Lewiston Ward 5, South Portland full roster, Bangor 7th member, Auburn full roster)
4. `C:/EV-Accounts/backend/scripts/smoke-phase89-me.ts` exists with 8 labeled assertions and exited non-zero on the pre-migration RED run
5. `C:/EV-Accounts/backend/migrations/265_me_city_school_boards.sql` exists, applied to production, ledger row for '265' present
6. All 8 post-verification gates inside migration 265 raised NOTICE (not EXCEPTION)
7. `npx tsx scripts/smoke-phase89-me.ts` exits 0 with ALL ASSERTIONS PASSED (GREEN step)
8. Re-running migration 265 raises 'Migration 265 already applied' (idempotency)
9. ME-SCHOOL-01 (Lewiston), ME-SCHOOL-02 (Bangor), ME-SCHOOL-03 (South Portland + Auburn + Biddeford) all verifiable
</success_criteria>

<output>
Create `.planning/phases/89-in-me-school-board-completion/89-02-SUMMARY.md` when done. Required sections:
- Files modified (3 absolute paths)
- Loader run output (5 inserted)
- Task 3 Roster Reconciliation Table (4 sections — Lewiston, South Portland, Bangor, Auburn — with source URLs + final names + any deviations from RESEARCH.md)
- Migration 265 application output (RAISE NOTICE text)
- Smoke test output (per-SC PASS/FAIL + ALL ASSERTIONS PASSED)
- Final 37-politician roster confirmation table (external_id, full_name, district, title, is_vacant flag if applicable)
- Lewiston Ward 5 final disposition (appointee name OR vacant placeholder)
- South Portland D5 final disposition
- Bangor 7th-member final identity (Sara Luciano OR replacement)
- Any new external_ids consumed and updated `Next external_id` pointer (after -890057, the next ME external_id starts at -890060+)
- Next migration after this plan: 266 (headshots audit — Plan 03)
</output>
