---
context: phase
phase: 106-va-compass-stances
task: 1
total_tasks: 8
status: in_progress
last_updated: 2026-06-09T19:28:38.894Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: HALLUCINATED-SOURCE-URLS — AI research agents generate politician press-release URL slugs that look real but cannot be verified. They know the URL pattern + the politician's record, and construct citations without fetching the actual pages.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Hallucinated press-release URLs | Agent correctly identifies what the politician did (votes, sponsorships) then generates a plausible-looking `politician.gov/news/press-releases/politician-action-name` slug without fetching it. The slug matches the URL pattern but may not exist. Discovered in migration 326 post-execution. | blocking | Use ONLY aggregation indexes as sources: `ballotpedia.org/{Name}`, `ontheissues.org/{state}/{Name}.htm`, `congress.gov/bill/...` (for named bill numbers), real news articles with date-based paths. Never use politician.house.gov or politician.senate.gov press-release slug URLs. |
| OnTheIssues /House/ path stale | `/House/Abigail_Spanberger.htm` returns 404 — she's now Governor. Path moved to `/VA/`. | advisory | For politicians who changed roles, verify the correct OnTheIssues path before using. Pattern: `/VA/` for VA statewide, `/Senate/` for US senators, `/House/` only if still in House. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 106 (VA Compass Stances) is in progress. Wave 1 (Plan 106-01, Spanberger) is complete:
- Migration 326 applied to production Supabase (32 stances, all passing verification)
- Sources corrected post-execution: hallucinated press-release URLs stripped, ontheissues.org/House/ fixed to /VA/, DB updated via MCP
- The corrected migration file is at C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql

Paused before Wave 2 (Plan 106-02, Hashmi). All 7 remaining plans are not started.
</current_state>

<completed_work>

- **Plan 106-01 (Spanberger):** COMPLETE
  - Resolved UUID `46c6ebb0-137a-46aa-b6fa-17af31aa4ef1` from `essentials.politicians WHERE external_id=-510001`
  - Researched 44 compass topics, found evidence on 32
  - Wrote migration 326 following 282 pattern (BEGIN/COMMIT, ON CONFLICT upsert, dollar-quoted reasoning)
  - Applied migration via mcp__supabase-local__execute_sql — 32 rows, 0 unpaired, 0 uncited
  - **Post-session correction:** Stripped hallucinated press-release slugs from all sources; replaced with ballotpedia + ontheissues.org/VA/ + congress.gov + one real Politico article. Committed corrected file to disk; DB updated via batch UPDATE.
</completed_work>

<remaining_work>

- **Plan 106-02:** Ghazala Hashmi (Lt. Governor, external_id=-510002) — migration 327, ≥10 stances
- **Plan 106-03:** Jay Jones (Attorney General, external_id=-510003) — migration 328, ≥10 stances
- **Plan 106-04:** Mark Warner (US Senator, external_id=-400080) — migration 329, ≥15 stances
- **Plan 106-05:** Tim Kaine (US Senator, external_id=-400079) — migration 330, ≥15 stances
- **Plan 106-06:** Alexandria city council 7 members (Mayor Gaskins + 6 council, external_ids -5101000001..-5101000007) — migrations 331-337, D-03 5-minute cap applies
- **Plan 106-07:** ACPS school board 9 members (from migration 313) — migrations 338-346, D-03/D-04 5-minute cap applies
- **Plan 106-08:** Phase-wide quality gate + compass render verification (checkpoint, not autonomous)
</remaining_work>

<decisions_made>

- **D-08 (from CONTEXT.md):** ONE research agent per politician, never in parallel. Still applies.
- **Source restriction (new this session):** Aggregation indexes only — ballotpedia.org, ontheissues.org with verified path, congress.gov for bill numbers, real news with date-based paths. NO politician press-release slug URLs.
- **OnTheIssues path:** Use `/VA/` for Virginia statewide officials (Governor, Lt. Gov, AG). Use `/Senate/` for US senators. Verify path if unsure.
- **VPAP candidate IDs:** Unverified — omit from plans 02-07 until manually confirmed in browser.
- **Migration numbering:** Plans 01-05 are migrations 326-330. Plans 06-07 are migrations 331-346 (one per person). Next migration after phase = 347.
</decisions_made>

<blockers>
None — clean pause point between waves.
</blockers>

## Required Reading (in order)
1. `.planning/phases/106-va-compass-stances/106-CONTEXT.md` — decisions D-01 through D-10, especially D-08 (one agent, never parallel) and D-03/D-04 (5-min cap for Alexandria officials)
2. `.planning/phases/106-va-compass-stances/106-PATTERNS.md` — SQL migration structure to follow
3. `.planning/phases/106-va-compass-stances/106-01-SUMMARY.md` — Spanberger reference for migration structure
4. `C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql` — the corrected migration as the canonical pattern for all remaining plans
5. `C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql` — original structural template

## Critical Anti-Patterns (do NOT repeat these)
- **HALLUCINATED-PRESS-RELEASE-SLUGS:** Never include `politician.house.gov/news/press-releases/slug`, `politician.senate.gov/...`, `governor.state.gov/...` or similar politician-specific press release URLs. The AI generates these from URL pattern knowledge + factual knowledge of votes — the slug itself is fabricated. Use ballotpedia / ontheissues / congress.gov / major news (with date-based paths) only.
- **WRONG ONTHEISSUES PATH:** Don't assume `/House/`. Check the politician's current role: statewide VA → `/VA/`, US Senate → `/Senate/`, still in House → `/House/`.

## Infrastructure State
- `mcp__supabase-local` IS production Supabase — all execute_sql calls write to live DB
- C:/EV-Accounts/backend/migrations/ — migration files go here (no git in this directory)
- Spanberger UUID: `46c6ebb0-137a-46aa-b6fa-17af31aa4ef1` (already resolved, reuse)
- Next migration number: 327 (Hashmi)

<context>
Clean pause between Wave 1 (complete) and Wave 2 (not started). The source URL issue was caught and corrected — it's a systemic issue with how AI research agents generate citations, not a data quality issue with the stances themselves. The stance values and reasoning for Spanberger are accurate; only the specific press-release URL slugs were fabricated. The correction model (aggregation indexes + congress.gov + real dated news) is established and must be applied to all remaining plans.

The remaining 7 plans follow the exact same 3-task structure as Plan 01: (1) resolve UUID + research, (2) write migration, (3) apply + verify. Plans 06-07 have the 5-minute research cap per person.
</context>

<next_action>
Resume with: `/gsd-execute-phase 106 --wave 2`

The executor for Plan 106-02 must be briefed with the source restriction constraint (no press-release slugs). The `.continue-here.md` blocking constraint table will be parsed automatically by execute-phase and the agent must demonstrate understanding before proceeding.
</next_action>
