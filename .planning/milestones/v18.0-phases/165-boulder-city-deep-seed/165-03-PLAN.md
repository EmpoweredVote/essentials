---
phase: 165-boulder-city-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["165-01", "165-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1102_boulder_city_hardy_stances.sql
  - C:/EV-Accounts/backend/migrations/1103_boulder_city_jorgensen_stances.sql
  - C:/EV-Accounts/backend/migrations/1104_boulder_city_booth_stances.sql
  - C:/EV-Accounts/backend/migrations/1105_boulder_city_walton_stances.sql
  - C:/EV-Accounts/backend/migrations/1106_boulder_city_ashurst_stances.sql
  - .planning/phases/165-boulder-city-deep-seed/165-03-SUMMARY.md
autonomous: false
requirements: [CLARK-05]
user_setup: []

must_haves:
  truths:
    - "Each council member with a public record has evidence-only compass stances across all live compass topics, researched ONE agent at a time, 100% cited, zero default values [D-05]"
    - "Topics with no Boulder-City-council-level evidence are honest blank spokes (no row), never defaulted to Neutral/Likely; honest blanks especially expected for Ashurst (shortest record, Nov 2024 onward) [D-05, RESEARCH honest-blank expectation]"
    - "Stances are CHAIRS (discrete positions the evidence matches, 1-5), not a directional polarity scale; each placed stance cites a Boulder-City-council statement/vote, not inference [D-05]"
    - "Stance research leads with the Boulder City emphasis topics (growth-and-development via the Controlled Growth Ordinance 120-unit/year cap; data-centers via the Feb 24 2026 ballot vote; economic-development via the solar lease revenue ~$13.7M/33% of FY25 budget; homelessness-response via the May 2025 5-0 camping ban; taxes; local-environment) while sweeping ALL live topics; the no-gambling charter is background context, NOT a citable stance (do NOT infer a value from it) [D-07]"
    - "A Boulder City address returns the Mayor (sorted first) + all 4 at-large council members (no ward routing — correct for an at-large city), with no empty LOCAL section and no section-split [CLARK-05 SC#1]"
    - "Mayor + City Council only — Municipal Court judge and non-elected offices deferred, so NO judicial topics (council members are not judges) [D-03]; topic_id resolved live by topic_key WHERE is_live=true (no hardcoded topic UUIDs, no retired IDs)"
    - "Casing 'nv' lowercase; zero section-split; structural ledger stays at 1100 (all stance migrations 1102-1106 audit-only, unregistered)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1102_boulder_city_hardy_stances.sql"
      provides: "Audit-only evidence-only stance migration for Mayor Hardy (inform.politician_answers + inform.politician_context), topic_id resolved live by topic_key, NOT registered"
      contains: "inform.politician_answers"
    - path: ".planning/phases/165-boulder-city-deep-seed/165-03-SUMMARY.md"
      provides: "Recorded results for all 8 SQL/HTTP verification checks + the at-large-routing/correct-person/stance human-verify outcome"
      contains: "CLARK-05"
  key_links:
    - from: "Mayor office (LOCAL_EXEC) + 4 at-large council offices (LOCAL)"
      to: "districts (1 LOCAL_EXEC + 1 shared LOCAL, both geo_id='3206500', state='nv')"
      via: "address → /representatives/me ST_Covers PIP routing (G4110 → both districts)"
      pattern: "3206500"
    - from: "5 council members (external_id -3208001..-3208005)"
      to: "inform.politician_answers + inform.politician_context"
      via: "topic_id resolved by topic_key WHERE is_live=true; ON CONFLICT (politician_id, topic_id)"
      pattern: "compass_topics"
---

<objective>
Research and install evidence-only compass stances for all 5 City of Boulder City council members — ONE research agent at a time (parallel agents burn rate-limit quota with no usable output — hard standing rule), all live compass topics, 100% cited, honest blank spokes where no public record exists, ZERO default values (chairs model, not polarity). Lead with the Boulder City emphasis topics (Controlled Growth Ordinance, the Feb 2026 data-center ballot vote, solar lease revenue, the May 2025 camping ban) while sweeping all live topics. Write one audit-only stance migration per member (1102-1106), apply each inline, then prove the full phase goal end-to-end via the 8 SQL/HTTP verification checks (the at-large adaptation of the Phase 164 9-check template — ward-routing checks dropped) and a human checkpoint on at-large address routing + correct-person headshots + stance honesty. Mirrors Phase 164 (NLV) Plan 03, MINUS the ward-precise routing checks.

Purpose: Closes CLARK-05 by satisfying Success Criterion 3 (evidence-only stances, 100% cited, honest blanks, zero defaults) and confirming all 4 phase success criteria are TRUE.
Output: 5 per-member stance migrations (audit-only, unregistered) + 165-03-SUMMARY.md with recorded query results + the human-verify outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP):**
- Stance RESEARCH is run by research agents, ONE AT A TIME (memory feedback_stance_research_one_at_a_time — parallel agents burn quota with no output). The executor WRITES the 5 stance migration .sql files to disk from the researched evidence; the executor does NOT apply them or run DB SELECTs.
- Applying each stance migration (via `psql -f` using C:/EV-Accounts/backend/.env DATABASE_URL), and every verification SQL/HTTP/browse check, are INLINE ORCHESTRATOR steps performed by execute-phase.
- Stance migrations live in the SEPARATE repo C:/EV-Accounts/backend/migrations/ (commit via `git -C "C:/EV-Accounts"`). The SUMMARY.md goes in the essentials .planning phase dir.
- Each task marks executor-write / research-agent / inline-orchestrator-apply.
</execution_context>

<context>
@.planning/phases/165-boulder-city-deep-seed/165-RESEARCH.md
@.planning/phases/165-boulder-city-deep-seed/165-PATTERNS.md
@.planning/phases/165-boulder-city-deep-seed/165-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor + research agents need. Do NOT re-derive. -->
Government 'City of Boulder City, Nevada, US' geo_id='3206500'; chamber 'Boulder City City Council' (official_count=5)
Mayor district: geo_id='3206500', district_type='LOCAL_EXEC', state='nv' lowercase (Hardy sorts first via groupHierarchy.js LOCAL_EXEC-before-LOCAL ordering)
Council district: ONE shared LOCAL district geo_id='3206500', district_type='LOCAL', state='nv' lowercase (all 4 at-large members attach here — no wards, no per-member districts)
5 members external_id -3208001 (Hardy/Mayor) .. -3208005 (Ashurst); the external_id→UUID map is in 165-01-SUMMARY.md
Stance tables (inform schema): inform.politician_answers (politician_id, topic_id, value); inform.politician_context (politician_id, topic_id, reasoning, sources); inform.compass_topics (topic_key, is_live)
topic_id MUST be resolved LIVE via JOIN inform.compass_topics ct ON ct.topic_key=s.topic_key AND ct.is_live=true — NEVER hardcode topic UUIDs; live/retired topic IDs in memory project_compass_live_topic_ids (retired UUIDs a9f53bc4, 45ca4740, f2a62698, 83eeb217, be60844f, c6957429 — do NOT use)
ON CONFLICT (politician_id, topic_id) DO UPDATE for both answers and context (idempotent)
Stances are CHAIRS (value = the discrete position the evidence matches, 1-5), NOT a directional polarity scale (memory feedback_compass_chairs_not_polarity)
No value for a topic = honest blank (no row); NEVER default to Neutral/Likely (memory feedback_stance_no_default_value)
No judicial topics (council members are not judges)
Boulder City emphasis topics + the cited evidence (D-07; lead with these, then sweep all live topics):
  growth-and-development — Controlled Growth Ordinance (120 residential units/year cap, ~45 yrs); council votes on subdivision allotments (Jorgensen documented votes denying a subdivision plan). bouldercityreview.com.
  data-centers — Feb 24 2026 council vote placing the Eldorado Valley data-center question on the Nov 2026 ballot; charter Section 144 requires voter approval. reviewjournal.com / hoodline.com. (A2: confirm whether the vote was unanimous; adjust individual values.)
  economic-development — solar lease revenue ~$13.7M = 33% of FY25 budget; Dec 2024 council denied a Boulder Flats Solar extension; 2024 council approved a solar lease rewrite. bcnv.org FY25 ACFR / bouldercityreview.com.
  taxes — lowest property tax in NV, solar-funded budget displaces property-tax revenue. bouldercityreview.com budget coverage.
  local-environment — Eldorado Valley land-use charter restrictions; solar rezoning. bcnv.org / bouldercityreview.com.
  homelessness-response — May 2025 5-0 council vote to outlaw camping/sleeping/storing property on public land. bouldercityreview.com. (A3: confirm Jorgensen with the 5-0 majority.)
  Lower-evidence: public-safety-approach (BCPD), housing (CGO doubles as a housing constraint), transportation-priorities, cannabis-regulation.
  The no-gambling charter is BACKGROUND CONTEXT (a settled structural fact), NOT a citable stance — do NOT infer a value from the charter's existence.
Stance source tiers: bcnv.org council agendas/minutes + bouldercityreview.com (the primary local source) → reviewjournal.com / Nevada Independent / Nevada Current → first-party/campaign statements
Realistic depth (RESEARCH honest-blank expectation): Hardy 6-10 topics (richest record — former state senator, mayor since 2022); Jorgensen 4-7; Booth/Walton 3-5; Ashurst 2-4 (only Nov 2024 onward — most honest blanks).
Migration mapping (all AUDIT-ONLY, NOT registered): 1102 Hardy / 1103 Jorgensen / 1104 Booth / 1105 Walton / 1106 Ashurst. Structural ledger stays at 1100. (If Wave-0 renumbered the structural migration, shift these by the same offset.)
At-large address spot-check: a Boulder City residential address → returns Mayor + all 4 at-large council members (correct — no ward routing), Mayor sorted first, no empty LOCAL section, no section-split.
Browse link: essentials.empowered.vote/results?browse_geo_id=3206500&browse_mtfcc=G4110
Grep-gate forbidden tokens in .sql comments: paraphrase the auto-generated path column / the removed image-origin column; the ledger table token must NOT appear in comments (and no ledger-registration INSERT — these are audit-only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research stances (one agent at a time) + write 5 audit-only stance migrations 1102-1106</name>
  <files>C:/EV-Accounts/backend/migrations/1102_boulder_city_hardy_stances.sql, C:/EV-Accounts/backend/migrations/1103_boulder_city_jorgensen_stances.sql, C:/EV-Accounts/backend/migrations/1104_boulder_city_booth_stances.sql, C:/EV-Accounts/backend/migrations/1105_boulder_city_walton_stances.sql, C:/EV-Accounts/backend/migrations/1106_boulder_city_ashurst_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1095_north_las_vegas_goynesbrown_stances.sql (EXACT ANALOG — the CTE shape: VALUES(topic_key,val,reasoning,sources) → JOIN compass_topics is_live → INSERT politician_answers ON CONFLICT → INSERT politician_context ON CONFLICT; audit-only, unregistered)
    - .planning/phases/165-boulder-city-deep-seed/165-PATTERNS.md (section "1102..1106_boulder_city_*_stances.sql" — full CTE pattern, per-file assignments, the D-07 emphasis topics, expected per-member counts)
    - .planning/phases/165-boulder-city-deep-seed/165-RESEARCH.md (Pattern 3 stance CTE; §Stance Evidence Richness table; §Stance Evidence — D-07 Boulder City Topics in Detail; Assumptions A2/A3; the honest-blank expectation per member)
    - .planning/phases/165-boulder-city-deep-seed/165-01-SUMMARY.md (the 5 external_id→UUID map minted by mig 1100; confirmed ledger numbers)
  </read_first>
  <action>
RESEARCH (one agent at a time — HARD RULE, never parallel): for each member in turn (Hardy → Jorgensen → Booth → Walton → Ashurst), dispatch a single stance-research agent that researches ALL live compass topics from public evidence, LEADING with the Boulder City emphasis topics (growth-and-development via the Controlled Growth Ordinance; data-centers via the Feb 24 2026 ballot vote; economic-development via solar lease revenue decisions; homelessness-response via the May 2025 5-0 camping ban; taxes; local-environment) then sweeping the rest. Source tiers: bcnv.org agendas/minutes + bouldercityreview.com → reviewjournal.com / Nevada Independent / Nevada Current → first-party/campaign statements. The agent returns, per topic with evidence: topic_key, the chair value (1-5 — the discrete position the evidence matches, NOT a polarity), an evidence-based reasoning string, and the source URL(s). Topics with no Boulder-City-council-level evidence get NO entry (honest blank). ZERO defaults — never Neutral/Likely as a placeholder. No judicial topics. The no-gambling charter is background context, NOT a citable stance — do NOT infer a value from it. Require a cited Boulder-City-council statement/vote for each placed stance, not inference; confirm the data-center vote's individual breakdown (A2) and Jorgensen's camping-ban vote (A3) before attributing those values. Expect honest blanks throughout, especially for Ashurst (Nov 2024 onward — thinnest record). Wait for each agent to finish and record its output before dispatching the next (avoids the rate-limit burn documented in memory).

EXECUTOR STEP (per member): write the stance migration modeled verbatim on 1095_north_las_vegas_goynesbrown_stances.sql. Header comment: file name, Phase 165 (CLARK-05), the member's name + external_id, AUDIT-ONLY (NOT registered, structural ledger stays 1100), idempotent, CHAIRS model, 100% citation, no defaults, honest blanks, topic_id resolved live by topic_key, stance sources. BEGIN; then a CTE: `WITH s(topic_key, val, reasoning, sources) AS (VALUES (<topic_key>, <1-5>, '<reasoning citing the specific Boulder City vote/statement>', ARRAY['<url>',...]), ...)` covering ONLY the researched topics-with-evidence; `t AS (SELECT s.*, ct.id AS topic_id FROM s JOIN inform.compass_topics ct ON ct.topic_key=s.topic_key AND ct.is_live=true)`; `ins_ans AS (INSERT INTO inform.politician_answers (politician_id, topic_id, value) SELECT '<uuid>', topic_id, val FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value RETURNING 1)`; then `INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) SELECT '<uuid>', topic_id, reasoning, sources FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning=EXCLUDED.reasoning, sources=EXCLUDED.sources;` COMMIT;. Use the real per-member UUID from 165-01-SUMMARY.md. NEVER hardcode topic UUIDs (always JOIN on topic_key + is_live; none of the 6 retired UUIDs). Do NOT register any of these in the ledger. If a member genuinely has no sourceable city-level stance, write a migration with zero VALUES rows (or document the full blank) — no fabrication. GREP-GATE HYGIENE: keep the ledger table token out of comments.

Do NOT apply these migrations — that is the inline orchestrator step (Task 2).
  </action>
  <verify>
    <automated>P="C:/EV-Accounts/backend/migrations"; ok=1; for f in 1102_boulder_city_hardy_stances 1103_boulder_city_jorgensen_stances 1104_boulder_city_booth_stances 1105_boulder_city_walton_stances 1106_boulder_city_ashurst_stances; do test -f "$P/$f.sql" || ok=0; done; F="$P/1102_boulder_city_hardy_stances.sql"; grep -q "inform.politician_answers" "$F" &amp;&amp; grep -q "inform.politician_context" "$F" &amp;&amp; grep -q "compass_topics" "$F" &amp;&amp; grep -q "is_live = true" "$F" &amp;&amp; grep -q "ON CONFLICT (politician_id, topic_id)" "$F" &amp;&amp; [ "$ok" -eq 1 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 5 stance migrations (1102-1106) exist on disk, one per member in the correct name→file mapping.
    - Stances researched ONE agent at a time (no parallel dispatch); each agent covered all live compass topics, leading with the D-07 emphasis topics.
    - Each migration uses the 1095 CTE shape: VALUES(topic_key, val, reasoning, sources) → JOIN inform.compass_topics ON topic_key AND is_live=true → INSERT politician_answers ON CONFLICT (politician_id, topic_id) DO UPDATE → INSERT politician_context ON CONFLICT (politician_id, topic_id) DO UPDATE.
    - topic_id resolved LIVE by topic_key + is_live=true — no hardcoded topic UUIDs; none of the 6 retired UUIDs appear.
    - Every placed stance has a non-empty reasoning string and ≥1 source URL (100% cited); values are discrete chairs (1-5), not a polarity scale.
    - No defaulted values (no Neutral/Likely placeholder); topics without evidence are simply absent (honest blank); the no-gambling charter is NOT used as a stance.
    - No judicial topics; each placed stance cites a Boulder-City-council statement/vote (CGO / data-center / solar / camping-ban evidence expected but still must be cited, not inferred); A2/A3 individual-vote breakdowns confirmed before attribution.
    - None of 1102-1106 contain a ledger-registration line (all audit-only).
    - Real per-member UUIDs (from 165-01-SUMMARY.md) used as politician_id.
  </acceptance_criteria>
  <done>5 evidence-only stance migrations written to disk, researched one agent at a time, chairs model, 100% cited, honest blanks, zero defaults, topic_id resolved live, all audit-only. Not yet applied.</done>
</task>

<task type="auto">
  <name>Task 2: Apply stance migrations inline + run the 8 SQL/HTTP verification checks</name>
  <files>.planning/phases/165-boulder-city-deep-seed/165-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/165-boulder-city-deep-seed/165-VALIDATION.md (Per-Task Verification Map — all checks + section-split SQL + the at-large routing expectation)
    - .planning/phases/165-boulder-city-deep-seed/165-RESEARCH.md (§Validation Architecture → 9-Check E2E adapted at-large; the exact SQL per check; the post-verify shape)
    - .planning/phases/165-boulder-city-deep-seed/165-02-SUMMARY.md (documented headshot gaps, if any, to subtract from the 5 count)
    - .planning/phases/165-boulder-city-deep-seed/165-01-SUMMARY.md (the structural migration number + the district/office counts)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): apply each stance migration 1102-1106 via `psql -f`. After each apply, spot-count the answers for that member. Then run the full 8-check verification suite (the at-large adaptation of the Phase 164 template — ward-routing checks dropped, at-large checks substituted) and record every result in 165-03-SUMMARY.md:
(1) Government + chamber — government 'City of Boulder City, Nevada, US' = 1 row; `SELECT official_count FROM essentials.chambers WHERE name ILIKE '%Boulder City%Council%'` = 5. Record.
(2) Two districts on 3206500 — `SELECT district_type FROM essentials.districts WHERE geo_id='3206500' AND state='nv'` → exactly [LOCAL_EXEC, LOCAL] (2 rows, NOT 4 LOCAL). Record.
(3) 5 offices split — `SELECT d.district_type, COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='3206500' AND d.state='nv' GROUP BY d.district_type` → LOCAL_EXEC: 1, LOCAL: 4. Record.
(4) Headshots present — politician_images (type='default') for external_id BETWEEN -3208005 AND -3208001 → expect 5 minus documented gaps (0 expected, from 165-02-SUMMARY.md). Record + list any gaps. Also `curl -sI` each CDN url → 200.
(5) Evidence-only stances — `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -3208005 AND -3208001)` → expect ≥1 per sourced member; verify 100% have a paired politician_context row with non-null reasoning + sources (zero uncited), and zero defaulted/placeholder values. Record per-member counts + honest blanks (Ashurst expected thinnest).
(6) Section-split — `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id='3206500' AND gb.mtfcc='G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=gb.geo_id AND d.state='nv')` → expect 0 rows. Record.
(7) Casing — `SELECT DISTINCT state FROM essentials.districts WHERE geo_id='3206500'` → expect only 'nv' lowercase. Record.
(8) Ledger — `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('1100','1101','1102','1103','1104','1105','1106')` → expect only 1100 registered (1101-1106 audit-only). Record.
Plus the at-large routing spot-check: pick a Boulder City residential interior point and run a /representatives/me-style probe or extend smoke-nv-geofences.ts → returns the Mayor + all 4 at-large council members (correct for an at-large city — NOT a ward subset, NOT a county-only result); expected mtfccs include G4110 (geo_id 3206500) with NO X-tier. Record the resolution.
Plus the coverage.js check: confirm src/lib/coverage.js COVERAGE_STATES Nevada block contains the Boulder City entry (browseGovernmentList ['3206500'], hasContext:true) alongside Las Vegas + Henderson + North Las Vegas. Record.
Flag ANY deviation (office count off, 4 LOCAL districts instead of 1 shared, a non-'nv' state, any uncited/defaulted stance, any extra ledger entry, a routing result missing a council member or showing an X-tier) as a gap for a follow-up gap-closure plan.
  </action>
  <verify>
    <automated>F=".planning/phases/165-boulder-city-deep-seed/165-03-SUMMARY.md"; test -f "$F" &amp;&amp; grep -qi "office" "$F" &amp;&amp; grep -qi "headshot" "$F" &amp;&amp; grep -qi "stance" "$F" &amp;&amp; grep -qi "section-split" "$F" &amp;&amp; grep -qi "ledger" "$F" &amp;&amp; grep -qi "coverage" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: all 5 stance migrations applied; per-member answer counts recorded.
    - Check (1) recorded: government = 1 row; chamber official_count = 5.
    - Check (2) recorded: exactly 2 districts on 3206500 ([LOCAL_EXEC, LOCAL]), NOT 4 LOCAL.
    - Check (3) recorded: 1 LOCAL_EXEC office + 4 LOCAL offices, all at state='nv'.
    - Check (4) recorded: politician_images count = 5 minus documented gaps (0 expected); CDN urls 200; gaps listed.
    - Check (5) recorded: ≥1 stance per sourced member; 100% cited (paired context with reasoning + sources); zero defaulted values; honest blanks noted (Ashurst thinnest).
    - Check (6) recorded: section-split query returns 0 rows.
    - Check (7) recorded: linked districts show only state='nv' lowercase.
    - Check (8) recorded: only ('1100') registered; 1101-1106 audit-only, unregistered.
    - At-large routing spot-check recorded: a Boulder City address resolves to Mayor + all 4 council members (no ward subset, no X-tier).
    - coverage.js Nevada block Boulder City entry confirmed (hasContext:true, ['3206500']) alongside LV + Henderson + North Las Vegas.
    - Any deviation flagged for gap closure.
  </acceptance_criteria>
  <done>All 5 stance migrations applied; all 8 checks + at-large routing + coverage check recorded in 165-03-SUMMARY.md with expected results; any deviation flagged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify at-large routing + correct-person headshots + stance honesty</name>
  <action>Pause for human verification. Present the what-built summary and the how-to-verify steps below; wait for the resume signal before marking the phase complete. No code or DB changes in this task.</action>
  <what-built>Seeded the City of Boulder City (Mayor + 4 at-large council members) under a standalone city government, with NO ward geofences (Boulder City is at-large — all 5 attach to the one existing G4110 city geofence geo_id=3206500). The directly-elected at-large Mayor (Joe Hardy) is on a city-wide LOCAL_EXEC district (sorted first); the 4 at-large council members (Jorgensen, Booth, Walton, Ashurst) are on ONE shared LOCAL district on the same geo_id, titled 'Council Member' (no ward number; Jorgensen's Mayor Pro Tem is an internal designation, not a separate seat). 600×750 headshots for all 5 (flybouldercity.com, no WAF; minus any documented genuine gap, 0 expected), and evidence-only compass stances (chairs model, 100% cited, honest blanks, zero defaults; led by the Controlled Growth Ordinance, the Feb 2026 data-center ballot vote, solar lease revenue, and the May 2025 camping ban). Boulder City surfaces with the purple hasContext chip in coverage.js COVERAGE_STATES alongside Las Vegas + Henderson + North Las Vegas. All 8 SQL/HTTP audits passed inline (5 offices = 1 LOCAL_EXEC + 4 LOCAL, exactly 2 districts on 3206500, headshots CDN-200 minus gaps, stances cited, casing 'nv', 0 section-split, structural ledger 1100).</what-built>
  <how-to-verify>
1. At-large routing — open the app and enter a residential address in the City of Boulder City. Confirm the LOCAL section returns the Mayor + ALL 4 at-large council members (correct for an at-large city — NOT a ward subset, NOT all-county-only), with NO empty LOCAL section and NO section-split. Use essentials.empowered.vote/results?browse_geo_id=3206500&browse_mtfcc=G4110 to see the full roster.
2. Mayor-first ordering + directly-elected model — confirm Mayor Joe Hardy sorts FIRST (above the council members), labeled 'Mayor', and that NO council member is shown as the city's mayor (Hardy is the directly-elected at-large Mayor, not rotational; he is on the 2026 ballot but seated). Confirm Jorgensen displays as 'Council Member' (not as a separate Mayor Pro Tem seat). If Hardy does not sort first, check groupHierarchy.js covers LOCAL_EXEC-before-LOCAL ordering.
3. Correct-person headshots — spot-check each profile. Confirm each photo is the right person, 4:5, no text/graphic overlay over the face (no "Re-Elect" banner etc.), not a placeholder. Confirm any documented gap renders cleanly without a photo. Confirm Hardy/Booth/Walton (terms expiring 2026) show as SEATED members (on the ballot but serving until inauguration). Confirm the accent/spelling renders on "Denise E. Ashurst".
4. Stance honesty — open 2-3 profiles (include the Mayor + Jorgensen). Confirm stances render with citations, blank spokes are genuinely blank (no defaulted Neutral), values look like documented positions (chairs, not a forced polarity), and the growth-and-development / data-centers / economic-development stances reflect cited Boulder-City-council positions (CGO, Feb 2026 ballot vote, solar leases). Confirm the no-gambling charter did NOT produce an inferred stance. Confirm Ashurst's profile shows honest blanks for her thin record. Party must NOT display anywhere (antipartisan — council races are nonpartisan).
5. Browse review — optionally review essentials.empowered.vote/results?browse_geo_id=3206500&browse_mtfcc=G4110.
  </how-to-verify>
  <verify><human-check>Operator confirms a Boulder City address routes to the Mayor + all 4 at-large council members (no ward subset, no empty LOCAL, no section-split), Hardy sorts first as the directly-elected Mayor with Jorgensen shown as 'Council Member' (not a separate MPT seat), correct-person headshots (no overlay/placeholder, Hardy/Booth/Walton seated, Ashurst name correct), and evidence-only stances with honest blanks, no inferred no-gambling stance, and no party display.</human-check></verify>
  <done>Human approves; or issues are logged and routed to a gap-closure plan.</done>
  <resume-signal>Type "approved" or describe any wrong-person photo, mis-routed result (missing council member or showing wards/X-tier), Mayor-ordering/model issue, Jorgensen-shown-as-separate-MPT-seat issue, seating issue, Ashurst-name issue, defaulted/inferred stance, or party-display leak.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| research evidence → stance migration | researched chair values/citations cross into seed SQL; only evidence-backed values are written |
| migration file → live DB | service-role apply of seed SQL; only the inline orchestrator applies |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-10 | Tampering | fabricated/defaulted stance value (no evidence) | mitigate | Evidence-only chairs model; every stance has paired context with reasoning + source(s); check (5) asserts 0 uncited + 0 defaults; honest blanks where no record (Ashurst thinnest) |
| T-165-11 | Tampering | no-gambling charter or development fact assumed as a stance without a cited council vote | mitigate | Each placed stance requires a cited Boulder-City-council statement/vote; the no-gambling charter is explicitly background-only; A2/A3 individual-vote breakdowns confirmed; human checkpoint step 4 confirms |
| T-165-12 | Spoofing | wrong topic via hardcoded topic UUID | mitigate | topic_id resolved live by topic_key + is_live=true (never hardcoded); 6 retired topic UUIDs excluded |
| T-165-13 | Repudiation | unverified claim of completion | mitigate | All 8 checks recorded with actual query results in SUMMARY; human checkpoint confirms at-large routing + correct-person photos + stance honesty |
| T-165-14 | Tampering | data-integrity drift (section-split / casing / 4-LOCAL-districts) | mitigate | Section-split SQL gate (0 rows) + casing check ('nv') + exactly-2-districts check run inline; structural ledger asserted at 1100 |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No installs (stance SQL + verification only); RESEARCH Package Legitimacy Audit = N/A. No high-severity threats; data-seed phase. |
</threat_model>

<verification>
All four phase success criteria proven:
1. A City of Boulder City address returns the Mayor + all 4 at-large council members (no ward routing — correct for an at-large city), no empty LOCAL section (checks 1+2+3 + at-large routing + browse step 1).
2. All 5 members render with 600×750 headshots, gaps documented (check 4 + browse step 3).
3. Evidence-only stances render, 100% cited, honest blanks, zero defaults (check 5 — SC#3 + browse step 4).
4. Boulder City surfaces with the purple hasContext chip alongside LV + Henderson + North Las Vegas (coverage.js check + browse step 5).
Cross-cutting: casing 'nv' (check 7), 0 section-split (check 6), structural ledger 1100 (check 8), Mayor sorts first as directly-elected (browse step 2).
</verification>

<success_criteria>
- 5 per-member stance migrations (1102-1106) written, researched one agent at a time, applied; evidence-only, 100% cited, honest blanks, zero defaults, chairs model; each placed stance cites a Boulder-City-council position; no inferred no-gambling stance.
- 165-03-SUMMARY.md records all 8 SQL/HTTP checks + the at-large routing + coverage check with expected results.
- Human checkpoint approves at-large routing, Mayor-first ordering (directly elected; Jorgensen as Council Member), correct-person headshots (no overlay, Hardy/Booth/Walton seated, Ashurst name correct), and stance honesty (no party display, no inferred charter stance).
- All stance migrations audit-only (structural ledger stays 1100); any deviation routed to a gap-closure plan.
</success_criteria>

<output>
Create `.planning/phases/165-boulder-city-deep-seed/165-03-SUMMARY.md` when done (record per-member stance counts + honest blanks, the 8 verification results incl. at-large routing, and the human-verify outcome).
</output>
