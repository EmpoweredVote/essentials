---
phase: 180-city-of-forest-grove-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-forest-grove-wave0-probe.sql
autonomous: false
requirements: [WASH-06]

must_haves:
  truths:
    - "Orchestrator has re-confirmed geo_id 4126200 has exactly 1 G4110 geofence row (RESEARCH found it CORRECT — the first WashCo city this milestone to match the ROADMAP-stated value on the first check; re-verify regardless because 2 of the prior stated geo_ids were wrong) — this probe runs FIRST, before any other"
    - "Orchestrator has confirmed the school-district row 4105160 (G5420) is a distinct, non-colliding geofence (relevant to future Phase 184, not this phase)"
    - "Orchestrator has confirmed no City of Forest Grove government/chamber row already exists (greenfield) by geo_id 4126200 AND by name ILIKE '%forest grove%'"
    - "Orchestrator has confirmed the ext_id block -4126201..-4126207 is free of collisions (probed with margin -4126210..-4126195)"
    - "Orchestrator has confirmed the on-disk migration MAX so the next STRUCTURAL migration number is safe (research snapshot: on-disk MAX 1177 → next 1178; ledger MAX 1169 is a TRAP — on-disk counter authoritative)"
    - "The current seated 7-member roster is re-confirmed live from forestgrove-or.gov/611/Meet-the-Council (Mayor Malynda Wenzl + Councilors Marshall/Martinez/Valenzuela/Gustafson/Falconer/Schimmel; Valenzuela holds Council President title-on-seat; ALL 7 currently held by ELECTION — zero appointed, zero vacant seats), with special re-confirmation of the close Schimmel/Truax 2024 seat"
    - "forestgrove-or.gov WAF status confirmed: NO-WAF for text/HTML, but the headshot photo widget is JS/AJAX-loaded and NOT curl-visible — a JS-capable fetch is attempted for all 7 officials BEFORE invoking the D-16 external fallback chain"
    - "The A7 assumption is resolved: City Attorney / Municipal Court Judge confirmed appointed (not elected) for Forest Grove specifically, gating the skip-all-judicial-* stance rule"
    - "The live compass topic_key list (is_live=true) is captured for stance ingestion (research: 44 live, 8 judicial-*)"
    - "The banner candidate images (3 Old College Hall angles + Downtown Forest Grove) are viewed directly and a compositional call on the 3.15:1 crop is recorded for plan 05"
    - "The CURATED_LOCAL two-word-key requirement is confirmed against the live getBuildingImages() match loop: the key MUST be 'forest grove' (space), never 'forest-grove'"
    - "Wave-0 checkpoint confirms the PURE AT-LARGE / directly-elected-Mayor / PLAIN-TITLE (no positions, no wards, no X00xx geofences) model and the MYAC non-council-seat finding (nothing to exclude) before any write"
    - "Decision coverage — D-01: council structure ground-truthed from forestgrove-or.gov, not memory; D-02: WHO-VOTES tie-breaker applied (whole city votes all seats → at-large branch, no custom geofences); D-05: Mayor's directly-elected citywide role re-confirmed; D-07: seated roster + exact body name verbatim + WAF status captured; D-16: JS-capable fetch attempted before external fallback; D-17: no non-voting dais seat exists to exclude"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-forest-grove-wave0-probe.sql"
      provides: "The Wave-0 probe queries the orchestrator runs before any write"
      min_lines: 20
  key_links:
    - from: "probe.sql"
      to: "essentials.governments / essentials.politicians / essentials.geofence_boundaries / supabase_migrations.schema_migrations / inform.compass_topics"
      via: "SELECT probes"
      pattern: "essentials\\.(governments|politicians|geofence_boundaries)"
---

<objective>
Author the Wave-0 verification probe file for the City of Forest Grove deep-seed and drive the
orchestrator-run DB + roster + web checks that MUST pass before any structural write. This plan gates
the entire phase: no government, roster, headshot, or stance migration may be authored or applied
until every probe below returns its expected result.

Purpose: Prevent the highest-risk failure modes for this phase. Unlike Hillsboro/Tualatin, RESEARCH
found geo_id 4126200 CORRECT — but the geofence-existence probe still runs FIRST per standing
practice (2 of the prior stated WashCo geo_ids were wrong; the habit must not lapse just because it
passed). The other load-bearing checks: confirm the greenfield status + ext_id block + migration
counter (ledger 1169 is a trap; on-disk MAX 1177 is authoritative); re-confirm the live 7-member
roster (with special attention to the extremely close Schimmel/Truax 2024 seat — RESEARCH Pitfall 2);
attempt a JS-capable headshot fetch before the D-16 fallback chain (the photo widget is JS-rendered,
not WAF-blocked — RESEARCH Pitfall 3); confirm the A7 assumption (City Attorney/Municipal Judge
appointed) before skipping judicial-* topics; and resolve the two-word CURATED_LOCAL key question
(RESEARCH Pitfall 4) definitively against the live match loop (RESEARCH Pitfalls 1-5; Assumptions
A1-A12; Open Questions 1-4).
Output: `_tmp-forest-grove-wave0-probe.sql` (gitignored `_tmp-*` helper) + a recorded pass/fail for
each Wave-0 gate, recorded in the SUMMARY for downstream plans 02-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO database
access, NO WebSearch, and NO Supabase MCP. The INLINE ORCHESTRATOR (main context) runs the probes via
`psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`, re-fetches
the roster from forestgrove-or.gov (NO WAF for text — direct curl works), attempts a JS-capable fetch
of the headshot photo widget, confirms the A7 judge-appointment assumption, and views the banner
candidate images. This probe file is a gitignored `_tmp-*` helper — do NOT commit it. C:/EV-Accounts
is a SEPARATE git repo — never cd into it; use `git -C "C:/EV-Accounts"` if a commit is ever needed.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except in Probe E where a query genuinely reads that table), and
`photo_origin_url` out of comments in any file authored this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-forest-grove-wave0-probe.sql</files>
  <read_first>
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md §"geo_id Verification", §"Ext_id Block", §"Next Migration Number", §"Live Compass Topics", §"Wave 0 Gaps"
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md §"_tmp-forest-grove-wave0-probe.sql (utility, request-response)" and §"Key Casing / Structural Traps"
    - C:/EV-Accounts/backend/scripts/_tmp-tualatin-wave0-probe.sql (the direct prior-phase probe-file analog — copy the 7-probe A-G shape verbatim, adapting values; gitignored but present on disk)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-01-PLAN.md (the prior-phase probe-plan analog)
  </read_first>
  <action>
    Write labeled SELECT probes, each preceded by a `\echo` label line so the orchestrator reads
    results unambiguously. Do NOT wrap in a transaction (read-only); no DDL — SELECT/\echo only.
    Probe A (GEO_ID EXISTENCE — RUN FIRST): count rows in essentials.geofence_boundaries WHERE
    geo_id='4126200' AND mtfcc='G4110' (EXPECT 1 — RESEARCH confirmed this value is CORRECT, unlike
    Tualatin there is NO wrong value to also check for 0; a single EXPECT-1 probe suffices). Add a
    second part A2: count rows WHERE geo_id='4105160' AND mtfcc='G5420' (EXPECT 1 — confirms the Forest
    Grove School District 15 row is a distinct, non-colliding geofence for future Phase 184, not a
    collision risk here). Probe B: count essentials.districts WHERE geo_id='4126200' (expect 0 — no
    city districts yet). Probe C: count essentials.governments WHERE geo_id='4126200' OR name ILIKE
    '%forest grove%' (expect 0 — greenfield). Probe D: select external_id from essentials.politicians
    WHERE external_id BETWEEN -4126210 AND -4126195 (expect 0 rows — ext_id block -4126201..-4126207
    free, probed with margin). Probe E: select MAX(version::bigint) from
    supabase_migrations.schema_migrations WHERE version ~ '^[0-9]{1,6}$' (record it; the research
    snapshot is ledger MAX 1169 which is a TRAP — the next STRUCTURAL migration number = on-disk file
    MAX + 1, research snapshot on-disk MAX 1177 → next 1178; the orchestrator cross-checks with an `ls`
    of the migrations dir). Probe F: select distinct state from essentials.districts WHERE
    geo_id='4159000' (Portland) to confirm lowercase 'or' is the correct casing for LOCAL/LOCAL_EXEC
    rows. Probe G: select topic_key from inform.compass_topics WHERE is_live=true ORDER BY topic_key —
    the authoritative live-topic list for the stance plan (04); also count how many begin with
    'judicial-' (expect 8, all skipped). Include a header comment noting the file is a gitignored
    orchestrator-run helper. No fenced code in this action — the _tmp-tualatin-wave0-probe.sql analog
    is the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-forest-grove-wave0-probe.sql` and records each result</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-forest-grove-wave0-probe.sql with labeled probes A-G
    - Probe A asserts geofence_boundaries geo_id='4126200' AND mtfcc='G4110' returns 1 (single EXPECT-1 probe — no wrong-value check needed); A2 confirms geo_id='4105160' G5420 school-district row is distinct
    - Probe C queries governments by geo_id='4126200' OR name ILIKE '%forest grove%'
    - Probe D queries the exact ext_id range -4126210..-4126195
    - Probe E reads MAX(version::bigint) from the migration ledger with a numeric-version filter
    - Probe F reads distinct districts.state for geo_id='4159000' (expect 'or' lowercase)
    - Probe G selects topic_key WHERE is_live=true and counts judicial-* topics
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE); not wrapped in a transaction
  </acceptance_criteria>
  <done>Probe file authored with all queries and correct literal values (confirmed geo_id 4126200, ext_id range -4126210..-4126195).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes, re-fetches roster, tests headshots, confirms all gates</name>
  <what-built>The Wave-0 probe file. The orchestrator now executes it against the live DB, re-fetches the live roster directly from forestgrove-or.gov (no WAF for text), attempts a JS-capable fetch of the headshot photo widget, confirms the A7 judge-appointment assumption, and views the banner candidate images.</what-built>
  <how-to-verify>
    A. Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-forest-grove-wave0-probe.sql`
       (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
       1. Probe A returns geofence(4126200, G4110)=1. If ≠ 1 → STOP, city geofence missing/mislabeled —
          re-run a name search before proceeding. A2 confirms the 4105160 school-district row is a
          separate G5420 row (not a collision).
       2. Probe B returns districts=0. If >0 → STOP, districts already exist (not greenfield).
       3. Probe C returns 0 (no existing Forest Grove gov). If >0 → STOP, phase already partly seeded —
          switch to reconcile.
       4. Probe D returns 0 rows (ext_id block free). If any row → shift the block and record the new
          range for plans 02/03/04.
       5. Probe E: cross-check against `ls C:/EV-Accounts/backend/migrations | grep -E '^[0-9]+_' | sort -t_ -k1 -n | tail`.
          Next STRUCTURAL migration = on-disk file MAX + 1 (research snapshot: on-disk 1177 → 1178;
          ledger MAX 1169 is a TRAP — do NOT use it). Record the confirmed number for plan 02.
       6. Probe F returns 'or' (lowercase) — the casing plan 02 must use for LOCAL/LOCAL_EXEC districts.
       7. Probe G returns the live topic_key list (research: 44 live, 8 judicial-*) — save it verbatim for plan 04.
    B. Re-fetch the current roster directly from forestgrove-or.gov/611/Meet-the-Council (NO WAF for
       text — plain curl or curl with a Chrome UA both return 200). Confirm the 7-member roster is
       unchanged since 2026-07-03: Mayor Malynda Wenzl (directly elected, term through Dec 2026);
       Councilor Michael Marshall; Councilor Karen Martinez; Councilor Mariana Valenzuela (Council
       President); Councilor Donna Gustafson; Councilor Angel Falconer; Councilor Brian Schimmel.
       Confirm ALL 7 are currently held by ELECTION (zero appointed, zero vacant) — Valenzuela's
       original 2020 appointment was superseded by the Nov 2022 election, so is_appointed=false is
       correct for her present seating (same treatment class as Tualatin's Pratt). SPECIAL CHECK
       (RESEARCH Pitfall 2 / A6): re-confirm the current roster still shows Brian Schimmel (NOT Peter
       Truax) in the third 2024-elected seat — the 2024 race was extremely close. Run a FRESH WebSearch
       for any Forest Grove council resignation/appointment after 2026-07-03. If any member changed →
       re-plan the roster in plan 02.
    C. Headshot retrievability (RESEARCH §"Headshot Sources"): forestgrove-or.gov photos are JS/AJAX
       loaded, NOT curl-visible (a different failure mode than a WAF block). FIRST attempt a JS-capable
       fetch (browser automation / Playwright MCP tool if available in the execution environment) of
       forestgrove-or.gov/directory.aspx?EID=<n> and/or the rendered /611/Meet-the-Council page for the
       7 officials, to determine whether real portraits are recoverable. Record, per official, whether a
       usable photo URL was found via JS fetch. Only if the JS fetch fails, fall to the D-16 chain
       (Ballotpedia → Wikimedia → local news: forestgrovenewstimes.com / newsinthegrove.com). Do NOT
       treat 0 curl-visible URLs as a genuine no-photo finding without the JS attempt (Pitfall 3).
       Record the sourcing findings for plan 03 — a partial N/7 outcome is an acceptable honest result.
    D. Confirm the structural decision holds (RESEARCH Form-of-Government): PURE AT-LARGE with a
       directly-elected Mayor + 6 Councilors, every seat voted citywide → Beaverton/Tualatin shape (1
       LOCAL_EXEC Mayor district + 1 LOCAL at-large district on geo_id 4126200), NO ward/X00xx
       geofences, PLAIN 'Mayor'/'Councilor' titles (Tigard convention — NOT Tualatin's numbered
       'Council Member (Position N)'). Confirm the Mayor's Youth Advisory Council (MYAC) is a SEPARATE
       advisory board (not a council-dais seat) — nothing to exclude from the 7-member roster (D-17
       satisfied cleanly). CONFIRM THE A7 ASSUMPTION: do a fast forestgrove-or.gov / WebSearch check
       that City Attorney and Municipal Court Judge are APPOINTED (council-appointed), not elected —
       this gates the skip-all-8-judicial-* stance rule for plan 04. If any of these is overturned →
       STOP and re-plan.
    E. View the banner candidate images directly (RESEARCH §"Community Banner" / A12 / Wave-0 Gap):
       the 3 Old College Hall Pacific University angles (front/back/side, CC BY 3.0) and Downtown Forest
       Grove, Oregon (Public Domain). Record a compositional call on which crops acceptably to 3.15:1
       (Old College Hall front is the D-15 top-priority hint; Downtown FG is the alternate) for plan 05.
    F. Confirm the CURATED_LOCAL two-word-key requirement (RESEARCH Pitfall 4): read src/lib/buildingImages.js
       getBuildingImages() match loop live and confirm the key MUST be the literal 'forest grove'
       (space), never 'forest-grove' — the pre-existing 'los angeles'/'west covina' keys are the proof
       pattern. Record for plan 05. (This is a live source read, not a SQL probe.)
  </how-to-verify>
  <acceptance_criteria>
    - All probe gates recorded with pass/fail
    - geo_id confirmed: 4126200 (G4110) returns 1; 4105160 (G5420) is a distinct non-colliding row
    - Confirmed ext_id block (default -4126201..-4126207 unless probe D forced a shift)
    - Confirmed next structural migration number (default 1178) via ledger + on-disk `ls` cross-check (ledger 1169 recognized as a trap)
    - Live topic_key list captured for plan 04; 8 judicial-* noted for exclusion
    - Live roster re-confirmed (7 members; ALL is_appointed=false; Valenzuela Council President title-on-seat; Schimmel not Truax in the 3rd 2024 seat) via direct forestgrove-or.gov fetch + fresh WebSearch
    - Headshot JS-capable fetch attempted per official BEFORE the D-16 fallback chain; per-official sourcing findings recorded (partial N/7 acceptable)
    - A7 confirmed: City Attorney/Municipal Court Judge appointed → skip all 8 judicial-* topics
    - Pure at-large / directly-elected-Mayor / PLAIN-title decision confirmed; MYAC non-seat finding confirmed; no X00xx geofences
    - Banner candidate images viewed; 3.15:1 crop call recorded for plan 05
    - CURATED_LOCAL key format confirmed as 'forest grove' (space) against the live getBuildingImages() match loop
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |
| operator → external web | Roster re-fetch, JS-capable headshot fetch, and banner-image viewing from forestgrove-or.gov / Wikimedia (no WAF for text) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-01 | Tampering | probe.sql executed on prod DB | accept | Probes are read-only SELECTs; no write path; no user input |
| T-180-02 | Spoofing | trusting the stated geo_id without a check would risk a phantom government (2 of the prior WashCo stated geo_ids were wrong) | mitigate | Probe A runs FIRST and asserts 4126200 (G4110) returns exactly 1 before any write — the standing habit that catches the phantom-seed class even though RESEARCH found this value correct |
| T-180-03 | Spoofing | stale roster (member change since research) — esp. the close Schimmel/Truax 2024 seat | mitigate | Direct forestgrove-or.gov re-fetch (no WAF) + fresh WebSearch + explicit Schimmel-not-Truax re-confirmation before seed |
| T-180-04 | Spoofing | Falconer's Milwaukie, OR history misattributed to Forest Grove | mitigate | Wave-0 records the roster with the explicit Milwaukie pitfall flag carried into plans 02/04 |
| T-180-05 | Information Disclosure | topic_key list from inform.compass_topics | accept | Public compass topic keys; no PII |
| T-180-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- geo_id 4126200 (G4110) existence confirmed before any structural write is authored.
- The A7 judge-appointment assumption is resolved before the judicial-* skip rule is locked.
- Any failed gate blocks plans 02-05 until resolved.
</verification>

<success_criteria>
- geo_id 4126200 confirmed present; no duplicate Forest Grove gov; districts absent; ext_id block
  free; next migration number confirmed (1178, ledger 1169 recognized as trap); live topic_key list
  captured; roster re-confirmed via direct fetch (Schimmel not Truax); JS-capable headshot fetch
  attempted; A7 judge-appointment confirmed; pure at-large + directly-elected-Mayor + plain-title +
  MYAC-non-seat decisions confirmed; banner candidates viewed; CURATED_LOCAL 'forest grove' key
  format confirmed.
</success_criteria>

<output>
Create `.planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md` when done, recording
the confirmed geo_id (4126200), ext_id block, next structural migration number, live topic_key list,
re-confirmed 7-member roster (all elected, Valenzuela Council President, Schimmel not Truax),
per-official headshot-source findings (JS fetch results + which need D-16 fallback), the A7
judge-appointment confirmation, the banner crop call, and the confirmed 'forest grove' CURATED_LOCAL
key format for downstream plans 02-05.
</output>
