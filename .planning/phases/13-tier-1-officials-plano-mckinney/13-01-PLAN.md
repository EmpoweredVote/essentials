---
phase: 13-tier-1-officials-plano-mckinney
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-tier-1-officials-plano-mckinney/staging/plano-politicians.md
  - C:/EV-Accounts/backend/migrations/091_plano_politicians.sql
autonomous: false

must_haves:
  truths:
    - "essentials.politicians has 8 active incumbent rows for Plano (Mayor + Place 1-5, 7-8)"
    - "Every Plano politician row has office_id linking to a valid essentials.offices row from migration 088"
    - "Every essentials.offices row for Plano has its politician_id back-pointer populated"
    - "Email and bio URL contact fields populated for >=80% of Plano incumbents (target: 100% per research)"
    - "All Plano politician rows have is_active=true, is_incumbent=true, party=NULL"
  artifacts:
    - path: ".planning/phases/13-tier-1-officials-plano-mckinney/staging/plano-politicians.md"
      provides: "Human-readable, human-reviewable politician staging table"
      contains: "| city | office_title | full_name |"
    - path: "C:/EV-Accounts/backend/migrations/091_plano_politicians.sql"
      provides: "Idempotent migration inserting 8 Plano politician rows + back-linking offices.politician_id"
      contains: "INSERT INTO essentials.politicians"
  key_links:
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id (Plano rows)"
      via: "FK lookup by chamber→government geo_id='4863000' AND office.title match"
      pattern: "WHERE g.geo_id = '4863000'"
    - from: "essentials.offices.politician_id"
      to: "essentials.politicians.id (just-inserted Plano rows)"
      via: "UPDATE offices SET politician_id = v_politician_id after each INSERT"
      pattern: "UPDATE essentials.offices SET politician_id"
---

<objective>
Seed all 8 current Plano incumbent politicians (Mayor + 7 council seats) into `essentials.politicians`, linked to the office rows created in Phase 12 migration 088, with full contact information from plano.gov bio pages.

Purpose: Plano is the first of two Tier 1 Texas cities receiving incumbent officials in v3.0. Without politician rows linked to offices, the Elections page cannot show "current incumbent" labels next to seats, and the office_id ↔ politician_id back-link cannot be exercised end-to-end.

Output:
- `staging/plano-politicians.md` — human-reviewed staging table for all 8 Plano incumbents
- Migration `091_plano_politicians.sql` — applied to production Supabase, inserts 8 politicians + updates 8 offices' politician_id
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-tier-1-officials-plano-mckinney/13-CONTEXT.md
@.planning/phases/13-tier-1-officials-plano-mckinney/13-RESEARCH.md
@.planning/phases/12-tx-db-foundation/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research Plano roster + write staging file</name>
  <files>
    .planning/phases/13-tier-1-officials-plano-mckinney/staging/plano-politicians.md
  </files>
  <action>
Step 1 — Verify Plano roster against current sources.

Use WebFetch on https://www.plano.gov/1166/Mayor-and-Council to read the live Plano City Council page. Cross-check each name, place, and term-end against the roster in `13-RESEARCH.md`. Also fetch the individual bio URLs listed below to confirm name spelling and capture official email addresses.

If the live page and the research disagree on any name, place number, term date, or bio URL — STOP and surface the discrepancy in the staging file's `notes` column rather than guessing. Do NOT modify the schema or insert anything yet.

Confirmed roster (from `13-RESEARCH.md`, post-May 2025 + Jan 2026 special election; no May 2026 race for Plano):

| Place | Title | Name | Took Office | Term End | Bio URL |
|-------|-------|------|-------------|----------|---------|
| Mayor | Mayor | John B. Muns | May 2021 (re-elected May 2025) | May 2029 | https://www.plano.gov/1349/Mayor-John-B-Muns |
| 1 | Council Member | Maria Tu | May 2019 (re-elected May 2023) | May 2027 | https://www.plano.gov/1355/Mayor-Pro-Tem-Maria-Tu |
| 2 | Council Member | Bob Kehr | May 2025 | May 2029 | https://www.plano.gov/1354/Councilmember-Bob-Kehr |
| 3 | Council Member | Rick Horne | May 2023 | May 2027 | https://www.plano.gov/1356/Councilmember-Rick-Horne |
| 4 | Council Member | Chris Krupa Downs | May 2025 | May 2029 | https://www.plano.gov/1353/Councilmember-Chris-Krupa-Downs |
| 5 | Council Member | Steve Lavine | May 2025 | May 2029 | https://www.plano.gov/1357/Councilmember-Steve-Lavine |
| 7 | Council Member | Shun Thomas | Feb 9, 2026 (special elected Jan 31, 2026) | May 2027 | https://www.plano.gov/1358/Councilmember-Shun-Thomas |
| 8 | Council Member | Vidal Quintanilla | May 2025 | May 2029 | https://www.plano.gov/1359/Councilmember-Vidal-Quintanilla |

Email pattern (Plano): `{firstname}{lastname}@plano.gov` lowercased, no spaces. Mayor uses `mayor@plano.gov`. Confirmed addresses:
mayor@plano.gov, mariatu@plano.gov, bobkehr@plano.gov, rickhorne@plano.gov, chrisdowns@plano.gov, stevelavine@plano.gov, shunthomas@plano.gov, vidalquintanilla@plano.gov.

Name field handling (CRITICAL — pitfall from research):
- Chris Krupa Downs → first_name='Chris', last_name='Krupa Downs', preferred_name='Chris', full_name='Chris Krupa Downs', email='chrisdowns@plano.gov' (Plano dropped the middle name in their email).
- John B. Muns → first_name='John', last_name='Muns', preferred_name='John', full_name='John B. Muns'.
- All others: first_name = first token, last_name = last token, preferred_name = first token, full_name = "First Last".

Step 2 — Write the staging file at `.planning/phases/13-tier-1-officials-plano-mckinney/staging/plano-politicians.md`.

Required structure:

```markdown
# Plano Incumbent Politicians — Staging

**Source date:** 2026-05-01
**Status:** Awaiting human review before migration 091 is written.
**Total seats:** 9 (Mayor + Place 1-8) — Place 6 is the Mayor seat (Plano numbering quirk; do NOT seed a separate Place 6).
**Total incumbents to insert:** 8

## Roster

| city | office_title | full_name | first_name | last_name | preferred_name | email | bio_url | valid_from | valid_to | term_date_precision | citation_url | notes |
|------|--------------|-----------|------------|-----------|----------------|-------|---------|------------|----------|---------------------|--------------|-------|
| Plano | Mayor | John B. Muns | John | Muns | John | mayor@plano.gov | https://www.plano.gov/1349/Mayor-John-B-Muns | 2025-05-01 | 2029-05-01 | month | https://www.plano.gov/1349/Mayor-John-B-Muns | Re-elected May 2025; original term started May 2021 |
| Plano | Council Member Place 1 | Maria Tu | ... | ... | ... | mariatu@plano.gov | ... | 2023-05-01 | 2027-05-01 | month | ... | Re-elected May 2023 |
| ... (one row per remaining incumbent — Place 2, 3, 4, 5, 7, 8) ... |

## Verification Notes

- Plano City Council has 9 seats but only 8 distinct holders: the Mayor occupies what is internally Place 6. The Phase 12 office rows do NOT include "Council Member Place 6" — confirmed in 12-02-SUMMARY.md (Plano has 9 offices total = Mayor + Place 1-8 by title; the Place 6 collision is handled by titling the mayor seat "Mayor" only).
- No May 2026 race for Plano — current roster IS the post-May 2026 roster.
- Place 7 was filled by Shun Thomas via Jan 31, 2026 special election (Julie Holmer resigned). Term completes the original Place 7 term ending May 2027.
- All party fields will be NULL in the migration (Texas municipal = nonpartisan).
```

The `valid_from` value is the **most recent term start** (re-election or special election win) for each politician. The `valid_to` is the term-end month per the table above. Use `term_date_precision = 'month'` everywhere — exact swearing-in dates are not consistently published.

Notes column should flag any discrepancies found between the live plano.gov page and the research roster. If a discrepancy is found, do NOT proceed to Task 2; surface it for human review.
  </action>
  <verify>
- `.planning/phases/13-tier-1-officials-plano-mckinney/staging/plano-politicians.md` exists
- File has exactly 8 data rows in the roster table
- Every row has a non-empty `citation_url` (no citation = no insert per CONTEXT.md)
- `office_title` values exactly match Phase 12 office titles: 'Mayor', 'Council Member Place 1', 'Council Member Place 2', 'Council Member Place 3', 'Council Member Place 4', 'Council Member Place 5', 'Council Member Place 7', 'Council Member Place 8' (no Place 6 row)
- Run: `wc -l .planning/phases/13-tier-1-officials-plano-mckinney/staging/plano-politicians.md` — should be >= 30 lines
  </verify>
  <done>
Staging file exists with 8 fully-populated incumbent rows, every field cross-checked against either the live plano.gov page or the research document, every row has a citation URL, and any discrepancies between live source and research are flagged in the `notes` column.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plano incumbent staging file at `.planning/phases/13-tier-1-officials-plano-mckinney/staging/plano-politicians.md` — 8 rows covering Mayor + Place 1-5, 7-8 with names, emails, bio URLs, term dates, and citation URLs.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/13-tier-1-officials-plano-mckinney/staging/plano-politicians.md`.
2. Spot-check 2-3 rows against https://www.plano.gov/1166/Mayor-and-Council in your browser — do names match? Does the place number match the seat?
3. Confirm the email pattern looks right (e.g., mayor@plano.gov for Muns, chrisdowns@plano.gov for Krupa Downs — note the email DROPS "krupa").
4. Read the `notes` column — any discrepancies flagged that need resolving before the migration?
5. Confirm Place 7 holder is Shun Thomas (NOT Julie Holmer — she resigned).

If anything looks wrong, describe the issue. Otherwise approve to proceed to migration 091.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write + apply migration 091 (Plano politicians)</name>
  <files>
    C:/EV-Accounts/backend/migrations/091_plano_politicians.sql
  </files>
  <action>
Write `C:/EV-Accounts/backend/migrations/091_plano_politicians.sql` translating the approved staging file into INSERT statements + offices.politician_id back-links.

Migration structure — one DO block per politician, all wrapped in a single transaction:

```sql
-- Migration 091: Seed Plano incumbent politicians (post-May 2025 + Jan 2026 special)
-- Depends on migration 088 (Plano government + chamber + 8 offices)
-- 8 incumbents: Mayor + Council Member Place 1-5, 7-8 (Place 6 is internal to Mayor seat)

BEGIN;

DO $$
DECLARE
  v_office_id UUID;
  v_politician_id UUID;
BEGIN
  -- Mayor — John B. Muns
  SELECT o.id INTO v_office_id
  FROM essentials.offices o
  JOIN essentials.chambers ch ON ch.id = o.chamber_id
  JOIN essentials.governments g ON g.id = ch.government_id
  WHERE g.geo_id = '4863000' AND o.title = 'Mayor';

  IF v_office_id IS NULL THEN
    RAISE EXCEPTION 'Plano Mayor office not found — migration 088 must run first';
  END IF;

  INSERT INTO essentials.politicians (
    first_name, last_name, preferred_name, full_name,
    party, party_short_name,
    is_active, is_incumbent, is_vacant, is_appointed,
    office_id,
    valid_from, valid_to, term_date_precision,
    email_addresses, urls,
    data_source
  ) VALUES (
    'John', 'Muns', 'John', 'John B. Muns',
    NULL, NULL,
    true, true, false, false,
    v_office_id,
    '2025-05-01', '2029-05-01', 'month',
    ARRAY['mayor@plano.gov'],
    ARRAY['https://www.plano.gov/1349/Mayor-John-B-Muns'],
    'plano.gov'
  ) RETURNING id INTO v_politician_id;

  UPDATE essentials.offices SET politician_id = v_politician_id WHERE id = v_office_id;
END $$;

-- Repeat one DO block per remaining incumbent (Place 1, 2, 3, 4, 5, 7, 8) ...

COMMIT;
```

CRITICAL pitfalls from research (do not repeat):
1. **ALWAYS update `essentials.offices.politician_id` after each politician INSERT** — the service layer JOINs both directions; missing back-link breaks the API.
2. **`party = NULL` (not the string 'nonpartisan')** — Texas municipal is nonpartisan; NULL is the schema convention.
3. **`valid_from` and `valid_to` are TEXT columns**, not DATE — insert as ISO strings like `'2025-05-01'`.
4. **`term_date_precision = 'month'`** — exact swearing-in dates aren't consistently published.
5. **`is_appointed = false` for ALL Plano** — Shun Thomas was elected (Jan 31, 2026 special election), not appointed.
6. **No `slug` column on essentials.politicians** — politicians table does NOT have a generated slug like chambers does. Verify by inspecting schema before assuming.
7. **`data_source = 'plano.gov'`** for all 8 rows.
8. **Use `RAISE EXCEPTION` if office lookup returns NULL** — protects against silent FK orphans if migration 088 is missing.
9. **Do NOT hardcode any UUIDs** — always look up office_id by `g.geo_id = '4863000'` + `o.title = '...'` exact match.

After writing the migration file:

1. Apply via `cd /c/EV-Accounts && supabase db query --linked --file backend/migrations/091_plano_politicians.sql` (per STATE.md, this is the preferred method — bypasses the connection-pooler max-clients issue and Docker-not-running issue).
2. If apply fails: read the error carefully. If it's a schema mismatch (e.g., a column you assumed exists doesn't), inspect `essentials.politicians` columns first via `supabase db query --linked` with a SELECT against `information_schema.columns WHERE table_schema='essentials' AND table_name='politicians'`, fix the migration, and re-run.
3. After successful apply, run the verification query from `13-RESEARCH.md`:

```sql
SELECT g.name AS city, o.title AS office, p.full_name, p.is_active, p.is_incumbent,
  p.office_id IS NOT NULL AS has_office_id_on_politician,
  o.politician_id IS NOT NULL AS has_politician_id_on_office,
  CASE WHEN p.email_addresses IS NOT NULL THEN 'email' ELSE 'null' END AS contact_email_status,
  CASE WHEN p.urls IS NOT NULL THEN 'bio' ELSE 'null' END AS bio_url_status
FROM essentials.politicians p
JOIN essentials.offices o ON o.id = p.office_id
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
WHERE g.geo_id = '4863000'
  AND p.is_active = true
ORDER BY o.title;
```

Expected: 8 rows, all with `has_office_id_on_politician = true`, `has_politician_id_on_office = true`, `contact_email_status = 'email'`, `bio_url_status = 'bio'`.

Commit the migration file with message: `feat(13-01): migration 091 seed Plano incumbent politicians (8 rows)`.
  </action>
  <verify>
- `C:/EV-Accounts/backend/migrations/091_plano_politicians.sql` exists with 8 DO blocks (one per incumbent) + BEGIN/COMMIT wrapper
- `supabase db query --linked --file backend/migrations/091_plano_politicians.sql` exits 0
- Verification SELECT returns exactly 8 rows for `g.geo_id = '4863000'` with all four boolean/contact columns showing the expected values
- Each of the 8 expected office titles ('Mayor', 'Council Member Place 1' through 'Council Member Place 5', 'Council Member Place 7', 'Council Member Place 8') appears exactly once
  </verify>
  <done>
Migration 091 applied cleanly. 8 Plano politicians exist in essentials.politicians, each with office_id populated and is_active=true / is_incumbent=true / party=NULL. Each of the 8 corresponding essentials.offices rows has politician_id populated. All 8 rows have email_addresses[] and urls[] populated (100% contact coverage — well above the 80% phase target).
  </done>
</task>

</tasks>

<verification>
Run after Task 2 completes:

```bash
# Confirm migration committed
cd "C:/EV-Accounts" && git log --oneline backend/migrations/091_plano_politicians.sql

# Confirm DB state via supabase CLI
cd /c/EV-Accounts && supabase db query --linked --query "
SELECT COUNT(*) AS plano_active_incumbents
FROM essentials.politicians p
JOIN essentials.offices o ON o.id = p.office_id
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
WHERE g.geo_id = '4863000' AND p.is_active = true AND p.is_incumbent = true;
"
# Expected: 8

# Confirm offices.politician_id back-link
cd /c/EV-Accounts && supabase db query --linked --query "
SELECT COUNT(*) AS plano_offices_with_politician
FROM essentials.offices o
JOIN essentials.chambers ch ON ch.id = o.chamber_id
JOIN essentials.governments g ON g.id = ch.government_id
WHERE g.geo_id = '4863000' AND o.politician_id IS NOT NULL;
"
# Expected: 8
```
</verification>

<success_criteria>
- [ ] `staging/plano-politicians.md` exists with 8 reviewed incumbent rows + citation URLs
- [ ] Human review checkpoint approved
- [ ] Migration `091_plano_politicians.sql` exists and applied to production Supabase
- [ ] `essentials.politicians` has 8 active Plano incumbent rows (Mayor + 7 council)
- [ ] All 8 Plano politicians have `office_id` linking to a valid `essentials.offices` row
- [ ] All 8 Plano `essentials.offices` rows have `politician_id` populated (back-link)
- [ ] All 8 rows: `is_active=true`, `is_incumbent=true`, `is_vacant=false`, `is_appointed=false`, `party=NULL`
- [ ] >=80% (target: 100%) have `email_addresses` populated; >=80% (target: 100%) have `urls` populated
- [ ] `data_source='plano.gov'` on all rows
- [ ] Migration committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/13-tier-1-officials-plano-mckinney/13-01-SUMMARY.md` capturing: migration applied, 8 incumbents seeded, contact coverage stats, any deviations from staging file, and any pitfalls encountered.
</output>
