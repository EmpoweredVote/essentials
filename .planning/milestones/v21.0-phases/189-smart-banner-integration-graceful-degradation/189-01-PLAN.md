---
phase: 189-smart-banner-integration-graceful-degradation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/bannerProps.js
  - src/lib/bannerProps.test.js
  - src/components/SectionBanner.jsx
  - src/components/SectionBanner.test.js
autonomous: true
requirements: [SBAN-03, SBAN-04]
tags: [banner, refactor, frontend]

must_haves:
  truths:
    - "buildBannerProps(tier, ctx) is a single pure function that assembles tier -> SectionBanner props (imageUrl/featureIcons/stats/locationName) with zero page-specific logic"
    - "The population stat scrim renders mid-left (desktop centered, mobile upper-left) instead of top-right, left-aligned to the title margin"
    - "shouldRenderStat and shouldRenderIcons are exported, unit-tested predicates that omit their block for null/0/NaN/[]/undefined"
  artifacts:
    - path: "src/lib/bannerProps.js"
      provides: "Pure buildBannerProps(tier, ctx) prop-assembly helper (SBAN-03)"
      exports: ["buildBannerProps"]
      contains: "TIER_TO_MAP_KEY"
    - path: "src/lib/bannerProps.test.js"
      provides: "Fixture-injected unit matrix for buildBannerProps (all 3 tiers + locationName fold-in + empty-ctx)"
      min_lines: 30
    - path: "src/components/SectionBanner.jsx"
      provides: "D-05 mid-left stat reposition + exported shouldRenderIcons predicate"
      exports: ["shouldRenderStat", "shouldRenderIcons"]
      contains: "md:top-1/2 md:-translate-y-1/2"
    - path: "src/components/SectionBanner.test.js"
      provides: "shouldRenderIcons unit cases (true for [{...}], false for []/null/undefined/non-array)"
  key_links:
    - from: "src/lib/bannerProps.js"
      to: "SectionBanner prop contract"
      via: "returns {tier, locationName, imageUrl, featureIcons, stats}"
      pattern: "TIER_TO_MAP_KEY"
    - from: "src/components/SectionBanner.jsx"
      to: "population stat block"
      via: "shouldRenderStat guard + repositioned wrapper"
      pattern: "shouldRenderStat\\(stats\\)"
---

<objective>
Build the single-source-of-truth prop-assembly helper (`buildBannerProps`) and apply the D-05 stat
reposition + extract the `shouldRenderIcons` guard — the two pure/presentational units that Wave 2's
page wiring will consume.

Purpose: Close SBAN-03 (one function assembles every banner's props, including the currently-duplicated
`locationName` logic) and strengthen SBAN-04 (both omit guards are named, exported, and directly unit-tested).
Also lands the D-05 mid-left reposition (orthogonal to the helper — props unchanged, only render position moves).
Output: `src/lib/bannerProps.js` + tests; `SectionBanner.jsx` reposition + `shouldRenderIcons`; extended `SectionBanner.test.js`.

Deferred (D-03): the population-bundle dynamic-import split is NOT in this phase — RESEARCH confirmed it is
not a near-1-line change (converting `Results.jsx`'s synchronous `populationMap` useMemo to async state).
The ready-made follow-on design lives in `189-RESEARCH.md` §"D-03 Bundle-Size Assessment". Do not add it here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-smart-banner-integration-graceful-degradation/189-RESEARCH.md
@.planning/phases/189-smart-banner-integration-graceful-degradation/189-CONTEXT.md
@.planning/phases/188-location-stats-strip/188-UI-SPEC.md

<interfaces>
<!-- SectionBanner's UNCHANGED prop contract (src/components/SectionBanner.jsx:158) — buildBannerProps must return exactly these keys. -->
SectionBanner({ tier, locationName, imageUrl, stats, featureIcons })
  tier          : 'city' | 'state' | 'federal'   (lowercase)
  locationName  : string
  imageUrl      : string | null
  stats         : { label: string, value: number } | null    (omitted by shouldRenderStat when not a positive finite number)
  featureIcons  : Array<{ key, href, label, iconSrc }>        (omitted when empty)

<!-- The three already-resolved maps are keyed CAPITALIZED (existing shipped shape) — buildBannerProps must translate: -->
buildingImageMap / featureIconMap / populationMap  are keyed { Local, State, Federal }
SectionBanner.tier                                  is lowercase { city, state, federal }
TIER_TO_MAP_KEY = { city: 'Local', state: 'State', federal: 'Federal' }   <-- bridge ONCE, here (Pitfall 1)

<!-- Existing exported predicate to mirror (src/components/SectionBanner.jsx:155): -->
export function shouldRenderStat(stats) { return typeof stats?.value === 'number' && stats.value > 0; }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the pure buildBannerProps helper + unit tests</name>
  <files>src/lib/bannerProps.js, src/lib/bannerProps.test.js</files>
  <read_first>
    - .planning/phases/189-smart-banner-integration-graceful-degradation/189-RESEARCH.md (§"Pattern 1: buildBannerProps" reference implementation + §"Code Examples" test skeleton + Pitfalls 1 & 2)
    - src/lib/population.js (pure-resolver convention: plain function, no React import, injectable maps)
    - src/lib/featureIcons.test.js (existing test-file convention this new test must mirror)
    - src/components/SectionBanner.jsx (prop contract lines 133-158; map-key vs tier-string mismatch)
  </read_first>
  <behavior>
    - buildBannerProps('city', CTX) returns { tier:'city', locationName:'Plano, TX', imageUrl:<Local>, featureIcons:<Local>, stats:<Local> } for a fully-populated ctx
    - buildBannerProps('state', {...CTX, stateNames:{}}) locationName falls back to the raw abbreviation ('TX')
    - buildBannerProps('federal', CTX) locationName is always 'United States' regardless of city/state
    - A tier whose map entries are null/[]/null returns imageUrl:null, featureIcons:[], stats:null (never undefined) — SBAN-04 precondition
    - buildBannerProps('city', {}) does not throw; returns locationName:'Your City', imageUrl:null, featureIcons:[], stats:null
  </behavior>
  <action>
    Create `src/lib/bannerProps.js` exporting a pure `buildBannerProps(tier, ctx = {})` per D-01/D-01a/D-02.
    No `import ... from 'react'` — plain function only (mirrors resolvePopulation/resolveFeatureIcons).
    Define module-scope `const TIER_TO_MAP_KEY = { city: 'Local', state: 'State', federal: 'Federal' }` and use it to
    bridge the lowercase `tier` string to the capitalized map keys ONCE (Pitfall 1 — never index a map by the raw tier).
    Destructure ctx with explicit defaults: `representingCity = null, userState = null, stateNames = {},
    buildingImageMap = {}, featureIconMap = {}, populationMap = {}`. Compute `locationName` in this one place
    (folding the STATE_NAMES-vs-stateNames divergence per SBAN-03): city -> `${representingCity}, ${userState}` when
    both present, else `representingCity || 'Your City'`; state -> `(userState && stateNames[userState]) || userState || 'Your State'`;
    federal -> `'United States'`. Return `{ tier, locationName, imageUrl: buildingImageMap[mapKey] ?? null,
    featureIcons: featureIconMap[mapKey] ?? [], stats: populationMap[mapKey] ?? null }` — the `?? null`/`?? []` defaults
    are the explicit self-consistent contract (Pitfall 2); never return `undefined` for any of the three.
    Do NOT re-resolve/re-fetch any data — pure assembly over already-resolved maps only.
    Then create `src/lib/bannerProps.test.js` (Vitest, no jsdom/React import, mirroring featureIcons.test.js) with the
    five behavior cases above using a shared CTX fixture matching 189-RESEARCH.md's skeleton.
  </action>
  <verify>
    <automated>npx vitest run src/lib/bannerProps.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/bannerProps.js` exists and exports `buildBannerProps`
    - The file contains zero `from 'react'` import (grep: no match for `from 'react'`)
    - The file contains the literal `TIER_TO_MAP_KEY` bridge object
    - `src/lib/bannerProps.test.js` has at least 5 `it(...)` cases covering the five behaviors above
    - `npx vitest run src/lib/bannerProps.test.js` exits 0 with all tests passing
  </acceptance_criteria>
  <done>buildBannerProps is a pure, exported, fully-tested prop-assembler; every tier returns null/[]/null (never undefined) on a miss.</done>
</task>

<task type="auto">
  <name>Task 2: Reposition stat scrim to mid-left (D-05) + extract shouldRenderIcons predicate</name>
  <files>src/components/SectionBanner.jsx, src/components/SectionBanner.test.js</files>
  <read_first>
    - src/components/SectionBanner.jsx (stat block lines 225-270; feature-icon guard line 275; shouldRenderStat line 155)
    - .planning/phases/189-smart-banner-integration-graceful-degradation/189-RESEARCH.md (§"D-05 reposition — exact JSX diff shape" + Pitfall 4)
    - .planning/phases/189-smart-banner-integration-graceful-degradation/189-CONTEXT.md (D-05 exact prototype approach, D-06 no separate UI-SPEC)
    - .planning/phases/188-location-stats-strip/188-UI-SPEC.md (scrim tokens/type that MUST stay unchanged)
    - src/components/SectionBanner.test.js (existing test conventions to extend)
  </read_first>
  <action>
    (a) D-05 reposition (SUPERSEDES 188 D-11/D-12 top-right). Wrap the existing `shouldRenderStat(stats) && (...)` stat
    block in an OUTER positioning div with the EXACT className string `px-6 md:px-12 absolute left-0 top-4 md:top-1/2 md:-translate-y-1/2`
    (this is the only responsive-vertical-anchor mechanism; inline style cannot express a `md:` breakpoint). Move the inner
    `data-slot="stats"` div INSIDE that wrapper and REMOVE its `position:'absolute'`, `top:'16px'`, `right:'16px'` (positioning now
    lives on the wrapper). Change the inner block's `alignItems` from `'flex-end'` to `'flex-start'` (Pitfall 4 — left-align the
    label/number to match the title). Keep the 188 tokens UNCHANGED: `background:'rgba(13,17,23,0.55)'`, `backdropFilter:'blur(2px)'`,
    `borderRadius:'10px'`, `padding:'4px 12px'`, `display:'flex'`, `flexDirection:'column'`, `gap:'4px'`; keep label 11px/600 uppercase
    `--color-ev-text-muted`, number 14px/700 `--color-ev-text-primary` via `.toLocaleString()`. Keep the `shouldRenderStat(stats)`
    guard intact (STAT-03). Introduce NO `z-index` and NO `!important` (188 D-13). Do not touch the title block or feature-icon row layout.
    (b) Extract `shouldRenderIcons(featureIcons)` as an exported pure predicate next to `shouldRenderStat` (with the same
    `// eslint-disable-next-line react-refresh/only-export-components` line), returning `Array.isArray(featureIcons) && featureIcons.length > 0`.
    Replace the inline `featureIcons?.length > 0 &&` render guard with `shouldRenderIcons(featureIcons) &&`.
    (c) Extend `src/components/SectionBanner.test.js` with a `describe('shouldRenderIcons')` block: true for `[{ key:'x' }]`;
    false for `[]`, `null`, `undefined`, and a non-array (`'x'`).
  </action>
  <verify>
    <automated>npx vitest run src/components/SectionBanner.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `SectionBanner.jsx` exports `shouldRenderIcons`; the icon-row render guard uses `shouldRenderIcons(featureIcons)` (grep confirms no remaining bare `featureIcons?.length > 0 &&` render guard)
    - The stat wrapper className is EXACTLY `px-6 md:px-12 absolute left-0 top-4 md:top-1/2 md:-translate-y-1/2`
    - The inner stats block uses `alignItems: 'flex-start'`; grep shows no `top: '16px'` / `right: '16px'` remaining on the stats block
    - Scrim tokens unchanged: file still contains `rgba(13,17,23,0.55)`, `blur(2px)`, `borderRadius: '10px'`, `padding: '4px 12px'`
    - File contains no `!important` and no new `zIndex`/`z-index` on the stat block
    - `SectionBanner.test.js` has a `shouldRenderIcons` describe block with the 5 cases above; `npx vitest run src/components/SectionBanner.test.js` exits 0
  </acceptance_criteria>
  <done>Stat scrim floats responsive mid-left, left-aligned to the title; shouldRenderIcons is an exported, tested twin of shouldRenderStat; scrim tokens and no-!important constraint preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed static bundle -> React render | Population/icon/image data originate from first-party committed bundles + one already-shipped Phase 187 fetch; no new untrusted input crosses into this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-01 | Information Disclosure | `stats.value.toLocaleString()` in SectionBanner | accept | Value is a trusted positive integer from the committed Census bundle; already public; `shouldRenderStat` gates non-numbers. No injection surface (no dangerouslySetInnerHTML, no URL construction here). |
| T-189-02 | Tampering | `buildBannerProps` prop assembly | accept | Pure function over in-repo maps; no network, no eval, no user-controlled key indexing (fixed `TIER_TO_MAP_KEY`). |
| T-189-SC | Tampering | package installs | accept | This phase installs ZERO npm packages (RESEARCH §"Package Legitimacy Audit" — N/A); no supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/lib/bannerProps.test.js src/components/SectionBanner.test.js` — both green.
- `npm test` — full suite still green (no regression to the 109-test baseline).
- Code inspection: `bannerProps.js` has no React import; SectionBanner stat block sits mid-left with `alignItems:'flex-start'`.
</verification>

<success_criteria>
- buildBannerProps assembles all three tiers' props (incl. locationName) from ctx maps with the TIER_TO_MAP_KEY bridge (SBAN-03).
- shouldRenderStat + shouldRenderIcons are exported and directly unit-tested for the omit cases (SBAN-04).
- Stat scrim renders responsive mid-left per D-05, tokens unchanged, no !important, no z-index.
</success_criteria>

<output>
Create `.planning/phases/189-smart-banner-integration-graceful-degradation/189-01-SUMMARY.md` when done.
</output>
