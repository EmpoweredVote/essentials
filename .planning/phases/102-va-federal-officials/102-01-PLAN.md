---
phase: 102-va-federal-officials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql
autonomous: true
requirements:
  - VA-FED-01
  - VA-FED-02
tags:
  - migration
  - virginia
  - federal-officials
  - postgres

must_haves:
  truths:
    - "An Alexandria, VA address returns Don Beyer (VA-8) as the user's US House representative via the live API."
    - "An address in each of the 11 VA congressional districts returns the correct US House rep listed in CONTEXT.md D-04."
    - "Warner and Kaine remain seeded with their existing external_ids (-400080 / -400079) and office_id values — no row mutation."
    - "Re-running migration 311 produces no new rows (idempotent via ON CONFLICT + NOT EXISTS guards)."
    - "All 11 new VA House rep politicians have a non-null office_id pointing to their NATIONAL_LOWER office."
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql"
      provides: "Idempotent SQL migration that asserts senators and seeds 11 VA House reps"
      contains: "BEGIN; ... COMMIT;"
      min_lines: 250
  key_links:
    - from: "essentials.politicians (external_id in -5102001..-5102011)"
      to: "essentials.offices (chamber_id=c2facc31-7b13-428c-b7b9-32d0d3b95f76)"
      via: "politician_id FK"
      pattern: "CTE ins_p + INSERT INTO essentials.offices ... CROSS JOIN ins_p p"
    - from: "essentials.offices.district_id"
      to: "essentials.districts (district_type='NATIONAL_LOWER', state='VA', geo_id in 5101..5111)"
      via: "FK to pre-existing TIGER district rows from Phase 100"
      pattern: "WHERE d.geo_id = '51XX' AND d.district_type = 'NATIONAL_LOWER' AND d.state = 'VA'"
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id"
      via: "STEP 4 back-fill UPDATE"
      pattern: "UPDATE essentials.politicians p SET office_id = o.id ... WHERE p.external_id BETWEEN -5102011 AND -5102001 AND p.office_id IS NULL"
---

<objective>
Seed the 11 Virginia US House representatives into essentials.politicians and essentials.offices, linked to the pre-existing NATIONAL_LOWER CD geofence districts (geo_ids 5101–5111). Assert (do NOT mutate) the already-seeded VA US Senators Warner (-400080) and Kaine (-400079).

Purpose: Complete the VA federal delegation in production so that any VA address returns its US Senators + US House rep on the Representatives tab. This unblocks Phase 105 (VA 2026 elections — Warner Senate race + 11 House races) and Phase 104 (VA federal headshots).

Output: One SQL migration file `C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql` applied via the Supabase MCP `apply_migration` tool per D-09 and D-10.

Implements: VA-FED-01 (Warner + Kaine assertions per D-01/D-02/D-03), VA-FED-02 (11 House reps per D-04..D-08 with roster D-11 corrections — VA-5=Cline, VA-9=McGuire, VA-11=Walkinshaw).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-va-federal-officials/102-CONTEXT.md
@.planning/phases/102-va-federal-officials/102-PATTERNS.md
@C:/EV-Accounts/backend/migrations/275_md_federal_officials.sql
@C:/EV-Accounts/backend/migrations/224_or_federal_officials.sql
</context>

<source_audit>
Coverage of all four artifact types:

| Source | Item | Plan | Status |
|--------|------|------|--------|
| GOAL (ROADMAP §Phase 102) | Warner + Kaine + 11 House reps seeded | 102-01 (Task 1+2) | COVERED |
| REQ | VA-FED-01 (2 US Senators with NATIONAL_UPPER districts) | 102-01 (Task 1 pre-flight + Task 2 verification) | COVERED |
| REQ | VA-FED-02 (11 US House reps with NATIONAL_LOWER linked to VA CD geofences) | 102-01 (Task 1 CTE blocks + Task 2 verification) | COVERED |
| CONTEXT D-01..D-03 | Warner/Kaine assertion-only — no INSERT/UPDATE | 102-01 Task 1 STEP 2 pre-flight assertion | COVERED |
| CONTEXT D-04 | external_id scheme -5102001..-5102011 | 102-01 Task 1 per-rep CTE blocks | COVERED |
| CONTEXT D-05 | 11 NATIONAL_LOWER districts pre-exist (geo_ids 5101–5111) | 102-01 Task 1 STEP 1 pre-flight + WHERE filters | COVERED |
| CONTEXT D-06 | House chamber UUID c2facc31-7b13-428c-b7b9-32d0d3b95f76 hardcoded | 102-01 Task 1 office INSERTs + NOT EXISTS guards | COVERED |
| CONTEXT D-07 | representing_state='VA', is_appointed_position=false | 102-01 Task 1 office INSERT column values | COVERED |
| CONTEXT D-08 | NOT EXISTS guard on (district_id, chamber_id) for single-member CDs | 102-01 Task 1 per-rep NOT EXISTS clause | COVERED |
| CONTEXT D-09 | Single migration file | 102-01 (single plan, single file) | COVERED |
| CONTEXT D-10 | No TypeScript apply script | 102-01 Task 2 uses mcp__supabase-local__apply_migration | COVERED |
| CONTEXT D-11 | Migration number = 311; roster VA-5=Cline, VA-9=McGuire, VA-11=Walkinshaw | 102-01 filename + Task 1 roster | COVERED |
| RESEARCH (102-PATTERNS Pattern 1-7) | Reuse MD-275 structure with VA substitutions | 102-01 Task 1 follows pattern map exactly | COVERED |

No items MISSING. No items deferred. No items out of scope.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 311_va_federal_officials.sql</name>
  <files>C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/275_md_federal_officials.sql (primary analog — copy CTE structure, replace state codes/geo_ids/external_ids)
    - C:/EV-Accounts/backend/migrations/224_or_federal_officials.sql (secondary analog — richer pre-flight with NATIONAL_LOWER count + chamber assertions, use this style per Pattern 4)
    - .planning/phases/102-va-federal-officials/102-CONTEXT.md (locked decisions D-01..D-11 and corrected roster — VA-5=Cline, VA-9=McGuire, VA-11=Walkinshaw)
    - .planning/phases/102-va-federal-officials/102-PATTERNS.md (Patterns 1–7, especially Pattern 5 House rep CTE block and Pattern 4 OR-style pre-flight)
  </read_first>
  <action>
    Create file `C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql`. Use the BEGIN ... COMMIT transaction wrapper from Pattern 7.

    Header comment block (Pattern 1): title "Migration 311: VA Federal Officials (11 US House Reps)"; document Warner=-400080 and Kaine=-400079 as ALREADY SEEDED; list both shared federal chamber UUIDs (Senate=7cbe07bc-84b8-433b-952b-540e7de18a92, House=c2facc31-7b13-428c-b7b9-32d0d3b95f76); state the three CRITICAL notes from MD-275 lines 10–12 adapted to VA; list the 11-rep roster `VA-01 Rob Wittman (R) — geo_id 5101` through `VA-11 James Walkinshaw (D) — geo_id 5111` with corrections from D-11: VA-5 = Ben Cline (R), VA-9 = John McGuire (R), VA-11 = James Walkinshaw (D), VA-7 = Eugene Vindman (D), VA-10 = Suhas Subramanyam (D), VA-4 = Jennifer McClellan (D).

    STEP 1 + STEP 2 pre-flight: use the richer OR-style block from Pattern 4 (single `DO $$ DECLARE ... BEGIN ... END $$`). Declare INT variables v_national_upper_count, v_national_lower_count, v_senate_count, v_house_count. Assertions in order: (a) exactly 1 row in essentials.districts with district_type='NATIONAL_UPPER' AND state='VA' AND geo_id='51'; (b) exactly 11 rows in essentials.districts with district_type='NATIONAL_LOWER' AND state='VA' AND geo_id IN ('5101','5102','5103','5104','5105','5106','5107','5108','5109','5110','5111'); (c) exactly 1 chamber row WHERE name='U.S. Senate'; (d) exactly 1 chamber row WHERE name='U.S. House of Representatives'; (e) exactly 2 rows in essentials.politicians WHERE external_id IN (-400080, -400079). Each failure RAISES EXCEPTION with the count interpolated. End with a single RAISE NOTICE summarizing the four counts.

    STEP 3: emit 11 House rep CTE blocks following Pattern 5 exactly. Per-rep substitutions in lockstep (geo_id last 2 digits = CD number; external_id last 3 digits = CD number zero-padded):
      - VA-01: 'Rob Wittman', 'Rob', 'Wittman', 'Republican', external_id=-5102001, geo_id='5101'
      - VA-02: 'Jen Kiggans', 'Jen', 'Kiggans', 'Republican', external_id=-5102002, geo_id='5102'
      - VA-03: 'Bobby Scott', 'Bobby', 'Scott', 'Democrat', external_id=-5102003, geo_id='5103'
      - VA-04: 'Jennifer McClellan', 'Jennifer', 'McClellan', 'Democrat', external_id=-5102004, geo_id='5104'
      - VA-05: 'Ben Cline', 'Ben', 'Cline', 'Republican', external_id=-5102005, geo_id='5105' (per D-11 correction — NOT Bob Good)
      - VA-06: 'Morgan Griffith', 'Morgan', 'Griffith', 'Republican', external_id=-5102006, geo_id='5106'
      - VA-07: 'Eugene Vindman', 'Eugene', 'Vindman', 'Democrat', external_id=-5102007, geo_id='5107'
      - VA-08: 'Don Beyer', 'Don', 'Beyer', 'Democrat', external_id=-5102008, geo_id='5108'
      - VA-09: 'John McGuire', 'John', 'McGuire', 'Republican', external_id=-5102009, geo_id='5109' (per D-11)
      - VA-10: 'Suhas Subramanyam', 'Suhas', 'Subramanyam', 'Democrat', external_id=-5102010, geo_id='5110'
      - VA-11: 'James Walkinshaw', 'James', 'Walkinshaw', 'Democrat', external_id=-5102011, geo_id='5111' (per D-11)

    Every CTE block follows the exact Pattern 5 shape: `WITH ins_p AS (INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id) VALUES (gen_random_uuid(), ..., true, false, false, true, <ext_id>) ON CONFLICT (external_id) DO NOTHING RETURNING id)` followed by `INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical) SELECT gen_random_uuid(), d.id, 'c2facc31-7b13-428c-b7b9-32d0d3b95f76', p.id, 'U.S. Representative', 'VA', false, false, NULL FROM essentials.districts d CROSS JOIN ins_p p WHERE d.geo_id='<geo_id>' AND d.district_type='NATIONAL_LOWER' AND d.state='VA' AND p.id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.chamber_id = 'c2facc31-7b13-428c-b7b9-32d0d3b95f76')`.

    Hardcode the chamber UUID at every site (MD style, not OR subselect style) per Pattern 5 note. state must be uppercase `'VA'` everywhere it appears in district WHERE clauses and as `representing_state` in office INSERTs (Pitfall 5).

    STEP 4 office_id back-fill (Pattern 6): `UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -5102011 AND -5102001 AND p.office_id IS NULL`. Comment that the BETWEEN range scopes only the 11 new VA House reps and that senators are excluded by both the range and the IS NULL guard.

    Do NOT include any INSERT/UPDATE against Warner or Kaine. Do NOT include any district INSERTs. Do NOT include a chamber INSERT. Do NOT add a TypeScript apply script per D-10.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql'; if(!(Test-Path $f)){throw 'missing'}; $c=Get-Content $f -Raw; $checks=@(); if($c -notmatch 'BEGIN;'){$checks+='no BEGIN'}; if($c -notmatch 'COMMIT;'){$checks+='no COMMIT'}; if(($c | Select-String -Pattern '-5102001|-5102002|-5102003|-5102004|-5102005|-5102006|-5102007|-5102008|-5102009|-5102010|-5102011' -AllMatches).Matches.Count -lt 11){$checks+='missing ext_ids'}; foreach($g in 5101..5111){if($c -notmatch \"geo_id\s*=\s*'$g'\"){$checks+=\"missing geo $g\"}}; if($c -notmatch 'NATIONAL_UPPER'){$checks+='no NATIONAL_UPPER assertion'}; if($c -notmatch 'NATIONAL_LOWER'){$checks+='no NATIONAL_LOWER filter'}; if($c -notmatch \"state\s*=\s*'VA'\"){$checks+='no state=VA filter'}; if($c -notmatch '\-400080'){$checks+='missing Warner ext'}; if($c -notmatch '\-400079'){$checks+='missing Kaine ext'}; if($c -notmatch 'c2facc31-7b13-428c-b7b9-32d0d3b95f76'){$checks+='missing House chamber UUID'}; if($c -notmatch 'BETWEEN -5102011 AND -5102001'){$checks+='missing office_id backfill range'}; if($c -match 'INSERT INTO essentials\.politicians[\s\S]{0,200}-400079'){$checks+='Kaine MUST NOT be re-inserted'}; if($c -match 'INSERT INTO essentials\.politicians[\s\S]{0,200}-400080'){$checks+='Warner MUST NOT be re-inserted'}; if($c -match 'INSERT INTO essentials\.districts'){$checks+='no district INSERTs allowed'}; if($c -match 'INSERT INTO essentials\.chambers'){$checks+='no chamber INSERTs allowed'}; if($checks.Count -gt 0){throw ($checks -join '; ')}; Write-Host 'OK'"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql` exists and is wrapped in a single `BEGIN; ... COMMIT;` transaction.
    - File contains exactly 11 `INSERT INTO essentials.politicians` statements (the per-rep CTEs) — verified by counting `ON CONFLICT (external_id) DO NOTHING` occurrences = 11.
    - File contains exactly 11 `INSERT INTO essentials.offices` statements — verified by counting `'U.S. Representative'` literal occurrences = 11.
    - All 11 external_ids -5102001 through -5102011 are present, each exactly once.
    - All 11 geo_ids '5101' through '5111' are present in WHERE clauses, each exactly once.
    - The literal string `'c2facc31-7b13-428c-b7b9-32d0d3b95f76'` appears at least 22 times (11 INSERT site + 11 NOT EXISTS site).
    - The literal string `state = 'VA'` (uppercase) appears in every district WHERE clause; no occurrence of `state = 'va'` (lowercase) anywhere in the file.
    - Pre-flight assertions exist for: (a) NATIONAL_UPPER count=1 with geo_id='51', (b) NATIONAL_LOWER count=11 with geo_ids 5101-5111, (c) U.S. Senate chamber count=1, (d) U.S. House chamber count=1, (e) senators -400080 and -400079 both exist.
    - File contains the back-fill `UPDATE essentials.politicians p SET office_id = o.id` with `BETWEEN -5102011 AND -5102001 AND p.office_id IS NULL`.
    - File contains ZERO `INSERT INTO essentials.districts` statements.
    - File contains ZERO `INSERT INTO essentials.chambers` statements.
    - File contains ZERO INSERT statements that touch external_id -400080 or -400079 (Warner/Kaine are assertion-only per D-01/D-02).
    - No TypeScript `_apply-migration-311.ts` file is created (D-10).
    - VA-5 row uses 'Ben Cline' / 'Cline' (NOT 'Bob Good'); VA-9 uses 'John McGuire'; VA-11 uses 'James Walkinshaw' (per D-11 corrections).
  </acceptance_criteria>
  <done>Migration file present, transaction-wrapped, all 11 House reps encoded with correct roster, pre-flight assertions cover the four invariants from Pattern 4, idempotency guards in place, and PowerShell verification command exits with `OK`.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 311 and verify VA federal delegation in production DB</name>
  <files>(no new files — applies migration via Supabase MCP and queries DB)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql (the file to apply — read entire contents to copy into apply_migration call)
    - .planning/phases/102-va-federal-officials/102-CONTEXT.md (D-09 mandates apply via `mcp__supabase-local__apply_migration`, NOT `supabase db push`)
    - .planning/STATE.md (current migration counter context — note STATE.md says 309 but 309/310 are taken per D-11; new counter after this plan is 312)
  </read_first>
  <action>
    Apply migration 311 to the production Supabase DB via the MCP tool `mcp__supabase-local__apply_migration` per D-09 (the supabase-local MCP IS the live remote DB per project memory). Migration name parameter: `311_va_federal_officials`. Query parameter: the full SQL contents of `C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql`.

    After apply succeeds, run the following verification SQL queries via `mcp__supabase-local__execute_sql` and capture results:

    Query V1 (politician count): `SELECT COUNT(*) AS n FROM essentials.politicians WHERE external_id BETWEEN -5102011 AND -5102001;` — expect n=11.

    Query V2 (office count): `SELECT COUNT(*) AS n FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id BETWEEN -5102011 AND -5102001;` — expect n=11.

    Query V3 (office_id backfill): `SELECT COUNT(*) AS n FROM essentials.politicians WHERE external_id BETWEEN -5102011 AND -5102001 AND office_id IS NULL;` — expect n=0.

    Query V4 (roster + district join, ordered by CD): `SELECT p.full_name, p.party, p.external_id, d.geo_id, d.district_type, o.representing_state FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id BETWEEN -5102011 AND -5102001 ORDER BY d.geo_id;` — expect 11 rows, all with district_type='NATIONAL_LOWER' and representing_state='VA', and these exact (geo_id, full_name) pairs: ('5101','Rob Wittman'), ('5102','Jen Kiggans'), ('5103','Bobby Scott'), ('5104','Jennifer McClellan'), ('5105','Ben Cline'), ('5106','Morgan Griffith'), ('5107','Eugene Vindman'), ('5108','Don Beyer'), ('5109','John McGuire'), ('5110','Suhas Subramanyam'), ('5111','James Walkinshaw').

    Query V5 (Warner/Kaine untouched assertion): `SELECT external_id, full_name, office_id IS NOT NULL AS has_office FROM essentials.politicians WHERE external_id IN (-400080, -400079) ORDER BY external_id;` — expect 2 rows, both has_office=true, names 'Mark Warner' and 'Tim Kaine'.

    Query V6 (Alexandria address routing — VA-8 = Beyer success criterion from ROADMAP): `SELECT p.full_name FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id='5108' AND d.district_type='NATIONAL_LOWER' AND d.state='VA';` — expect single row 'Don Beyer'.

    Query V7 (idempotency dry-run check): Re-apply the migration a second time via `mcp__supabase-local__apply_migration` with the same name — Supabase will skip it (already-applied tracking). If it does re-execute (unusual), Query V1 must still return n=11 (CTE guards ensure no duplicates).

    Query V8 (no orphan offices): `SELECT COUNT(*) AS n FROM essentials.offices o LEFT JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id BETWEEN -5102011 AND -5102001 AND p.id IS NULL;` — expect n=0.

    Record the full results of V1–V8 in the summary file produced by execute-plan. Update STATE.md migration counter note (currently says "Next migration: 309") to reflect 312 as the next migration in the SUMMARY only — do NOT edit STATE.md from this task (the orchestrator handles STATE.md updates).
  </action>
  <verify>
    <automated>
    Verification is performed by the apply step itself and queries V1–V8 above. Each query has an expected exact result documented in the action. Task fails if any query returns a mismatch. Manual review of MCP tool output is required because apply_migration runs through Claude tool surface, not shell.
    </automated>
  </verify>
  <acceptance_criteria>
    - `mcp__supabase-local__apply_migration` returns success for migration name `311_va_federal_officials`.
    - Query V1 returns `n=11` exactly.
    - Query V2 returns `n=11` exactly.
    - Query V3 returns `n=0` exactly (office_id backfill 100% complete for all 11 reps).
    - Query V4 returns exactly 11 rows in geo_id order with the (geo_id, full_name) pairs listed in the action — VA-5 MUST be 'Ben Cline', VA-9 MUST be 'John McGuire', VA-11 MUST be 'James Walkinshaw'.
    - All 11 rows in V4 have `district_type='NATIONAL_LOWER'` and `representing_state='VA'`.
    - Query V5 returns 2 rows, both with `has_office=true`, names 'Mark Warner' and 'Tim Kaine' — confirming senators were not mutated.
    - Query V6 returns exactly one row, full_name='Don Beyer' (Alexandria=VA-8 routing success criterion from ROADMAP §Phase 102).
    - Query V8 returns `n=0` (no orphan offices).
    - Section-split check query `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE o.representing_state='VA' AND d.district_type='NATIONAL_LOWER' AND d.geo_id NOT IN ('5101','5102','5103','5104','5105','5106','5107','5108','5109','5110','5111')` returns 0 (no VA House offices linked to wrong geofences).
    - SUMMARY.md records actual returned counts and the full V4 result set as the audit trail.
  </acceptance_criteria>
  <done>Migration applied successfully via Supabase MCP; all eight verification queries return expected results; Warner and Kaine offices unmutated; Alexandria-to-Beyer routing confirmed at the SQL layer; results recorded in the plan SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration author → production DB | SQL written locally is applied to live Supabase production DB via supabase-local MCP (project memory: supabase-local IS production). |
| TIGER geofence data → office routing | Pre-existing district rows from Phase 100 (state-supplied shapes) are joined to determine which CD each politician serves; any geofence misload would mis-route reps to wrong constituents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | Migration SQL (wrong external_id collision with existing politician) | mitigate | `ON CONFLICT (external_id) DO NOTHING` returns NULL id; downstream office INSERT then no-ops via `AND p.id IS NOT NULL` predicate. Range -5102001..-5102011 is reserved per D-04 (FIPS 51 + "02" House marker + CD#) and confirmed unused before this migration. |
| T-102-02 | Tampering | Senator rows (-400080 Warner, -400079 Kaine) accidentally mutated | mitigate | Migration contains ZERO INSERT/UPDATE against external_ids -400080 or -400079. Pre-flight STEP 2 only SELECTs them. Acceptance criteria includes a grep assertion against any insert touching these IDs (Task 1) and a post-apply V5 query confirming they retain office_id (Task 2). |
| T-102-03 | Information Disclosure | Incorrect party label displayed publicly | mitigate | Party is stored on DB but never rendered (project memory: antipartisan UI). Even if party were misencoded, no public surface displays it; risk is data-quality only, not disclosure. Roster verified against ROADMAP.md and CONTEXT.md D-11 corrections (VA-5=Cline, VA-9=McGuire, VA-11=Walkinshaw). |
| T-102-04 | Denial of Service | Migration locks `essentials.politicians` table during 11 single-row inserts | accept | Each CTE block is a single-row INSERT with `ON CONFLICT DO NOTHING`; lock duration is sub-millisecond. Transaction-wrapped so partial commit is impossible. Production write throughput unaffected. |
| T-102-05 | Repudiation | Wrong politician seeded for a district (e.g., outdated VA-5 = Bob Good instead of Cline) leads to silently wrong representation displayed to constituents | mitigate | Roster is explicitly corrected in D-11 and confirmed in the system instructions for this planning call; acceptance criteria in Task 2 Query V4 asserts each (geo_id, full_name) pair exactly. Task 1 acceptance includes an explicit "not 'Bob Good'" assertion for VA-5. |
| T-102-06 | Elevation of Privilege | Migration grants implicit privileges via new offices to unintended chamber | mitigate | All 11 offices are hardcoded to chamber_id `c2facc31-7b13-428c-b7b9-32d0d3b95f76` (U.S. House) per D-06. Pre-flight STEP 1 (d) confirms this chamber row exists and has the expected name. NOT EXISTS guard on (district_id, chamber_id) prevents office duplication across chambers. |
| T-102-07 | Tampering | Idempotency failure on re-apply (duplicate rows) | mitigate | Three layers of idempotency: (a) `ON CONFLICT (external_id) DO NOTHING` on politicians; (b) `AND p.id IS NOT NULL` on office INSERT after a no-op politician insert; (c) `NOT EXISTS` guard on (district_id, chamber_id) for offices. Task 2 Query V7 explicitly tests re-application. |
| T-102-SC | Tampering | npm/pip/cargo installs | N/A | This phase installs ZERO packages — pure SQL migration applied via existing Supabase MCP tool. No supply-chain surface. |
</threat_model>

<verification>
1. Task 1 verify command (PowerShell static analysis of the SQL file) returns `OK`.
2. Task 2 queries V1–V8 all return their expected exact results.
3. Section-split check (acceptance criterion in Task 2) returns 0 rows.
4. Live API spot-check (manual, post-execution but encouraged): a GET to the representatives endpoint with an Alexandria, VA address returns Don Beyer in the US House slot.
</verification>

<success_criteria>
1. `C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql` exists, is transaction-wrapped, encodes the corrected D-11 roster, and contains the four-invariant pre-flight block (OR Pattern 4 style).
2. Migration applied successfully to production via `mcp__supabase-local__apply_migration`.
3. Production DB contains exactly 11 new rows in `essentials.politicians` with external_id in [-5102011, -5102001], each with non-null `office_id` and a corresponding office in `essentials.offices` linked to a NATIONAL_LOWER VA district.
4. Warner (-400080) and Kaine (-400079) are unmutated and retain their existing offices (assertion-only per D-01/D-02).
5. Alexandria-to-Beyer routing is satisfied at the SQL layer: `geo_id='5108' AND district_type='NATIONAL_LOWER' AND state='VA'` yields exactly one office whose politician is Don Beyer.
6. VA-FED-01 and VA-FED-02 are fully satisfied — verifiable by the verification queries above.
</success_criteria>

<output>
Create `.planning/phases/102-va-federal-officials/102-01-SUMMARY.md` recording:
- Migration filename and full apply timestamp.
- V1–V8 query results inline (paste actual returned rows for V4 and V5 as audit evidence).
- The next-migration counter (312) for STATE.md update by the orchestrator.
- Any deviations from the locked roster, with explanation (should be none).
</output>
</content>
</invoke>