---
phase: 154-burbank-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1026_burbank_reconcile.sql
autonomous: true
requirements: [BURB-01]

must_haves:
  truths:
    - "City of Burbank government carries geo_id 0608954 so the browse route resolves it"
    - "Exactly one 'City Council' chamber exists under the Burbank government"
    - "All 5 offices sit in the survivor chamber 73422d25, each bidirectionally linked to its politician"
    - "Anthony (-201161) and Mullins (-201162) one-directional links are repaired and they point at the surviving At-Large district 15458750"
    - "The doomed chamber 6a72dbe8 and the orphaned doomed At-Large district 809bbb35 are deleted; the 5 seats stay At-Large (no relabel)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1026_burbank_reconcile.sql"
      provides: "geo_id backfill + dual-chamber merge + one-directional link repair + At-Large district consolidation (STRUCTURAL, registered)"
      contains: "burbank_reconcile"
  key_links:
    - from: "essentials.governments 3e3deaea"
      to: "browse route geo_id 0608954"
      via: "geo_id backfill"
      pattern: "geo_id = '0608954'"
    - from: "essentials.offices (survivor chamber 73422d25)"
      to: "essentials.politicians"
      via: "bidirectional office_id <-> politician_id"
      pattern: "office_id IS DISTINCT FROM"
---

<objective>
Wave 1 of the Burbank deep-seed: reconcile the structural defects on the City of Burbank government
(`3e3deaea-c5f4-4a68-b3ae-a79589f544ea`) in ONE structural migration
(`1026_burbank_reconcile.sql`, registers in `supabase_migrations.schema_migrations`).

This is a simpler reconcile than Inglewood (153): NULL geo_id, TWO duplicate 'City Council' chambers,
and two one-directional links (Anthony + Mullins). NO person-dedup (5 distinct people, RESEARCH-confirmed).
NO by-district relabel (Burbank is at-large; CVRA ballot has not passed — the 5 'At-Large' labels are CORRECT).
NO LOCAL_EXEC Mayor (rotational mayor handled in Wave 2 as a title on a seat).

Purpose: a single clean government + chamber + correctly-linked at-large roster skeleton that Wave 2 finalizes.
Output: `1026_burbank_reconcile.sql` applied live via Supabase MCP, committed to EV-Accounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-burbank-deep-seed/154-CONTEXT.md
@.planning/phases/154-burbank-deep-seed/154-RESEARCH.md
@.planning/phases/154-burbank-deep-seed/154-PATTERNS.md
@.planning/phases/154-burbank-deep-seed/154-VALIDATION.md

<reference_data>
Gov UUID: 3e3deaea-c5f4-4a68-b3ae-a79589f544ea | geo_id: 0608954 | state: CA (already set)
SURVIVOR chamber: 73422d25-c0a6-477a-b74f-2b38b94b6389 (official_count 5, 3 bidirectional-clean offices)
  surviving At-Large district: 15458750-78aa-4b9a-ade4-247e28bc25c2 (CA, 3 offices)
DOOMED   chamber: 6a72dbe8-06fa-4148-9152-1c8e2f11b30e (official_count NULL, 2 one-directional offices)
  doomed At-Large district: 809bbb35-8d84-4e51-aef8-44547b32d063 (CA, 2 offices — drop after move)
Both chambers share name 'City Council' / slug 'burbank-city-council' — TARGET BY UUID ONLY.

SURVIVOR offices (bidirectional-clean — leave linked, just consolidate):
  Nikki Perez       663414   pol 96f91743-def6-436c-9537-a4b836c1b3eb  office f205911b  At-Large
  Christopher Rizzotti 663419 pol a83a63a8-3e0f-4a2e-9226-8c0cd26a1349  office caea9243  At-Large
  Tamala Takahashi  663418   pol ea6f7109-6067-4a48-bbdf-2a8b9cffe05f  office 70e56076  At-Large

DOOMED offices (one-directional: offices.politician_id set, politicians.office_id NULL — repair + move):
  Konstantine Anthony -201161 pol 6c4c7919-3e7f-41fa-8b1b-1c8b421fe4a7  office 1294961c  At-Large
  Zizette Mullins     -201162 pol f933bd87-d397-4ef1-873b-57559b629000  office 9969febe  At-Large

RESEARCH RESOLUTIONS (do NOT re-litigate):
  - At-large + rotational mayor CONFIRMED (RESEARCH §Form of Government Verdict) — do NOT relabel At-Large to districts.
  - Mullins is a LEGITIMATE current council member (Vice Mayor), NOT the City Clerk — do NOT unlink her.
  - All 5 DB rows are current council members — no person-dedup, no unlinking, no new member.
OUT OF SCOPE (never touch): Burbank Unified School District gov d5ffbb65-f0db-41ad-8d11-278b8fb9aedc.
Migration counter: on-disk MAX 1025 (authoritative) / live schema_migrations MAX 999 -> next = 1026.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STOP-on-drift pre-flight — re-confirm every Wave-1 invariant + both migration counters</name>
  <files>(read-only; no file written this task)</files>
  <read_first>
    - .planning/phases/154-burbank-deep-seed/154-CONTEXT.md §code_context (the live DB pre-check, AUTHORITATIVE on UUIDs/ext_ids)
    - .planning/phases/154-burbank-deep-seed/154-RESEARCH.md §Roster Verdict (all 5 current; Mullins is Vice Mayor not Clerk) + §Form of Government Verdict (at-large, do not relabel)
    - .planning/phases/154-burbank-deep-seed/154-VALIDATION.md §Wave 0 Requirements (STOP-on-drift list) + §Per-Wave Verification Map Wave 1 rows
  </read_first>
  <action>
    Via `mcp__supabase-local__execute_sql`, run these read-only queries and STOP (raise to operator, do not mutate) on ANY drift from CONTEXT.md §code_context:
    (a) Confirm gov: `SELECT id, geo_id, state FROM essentials.governments WHERE id='3e3deaea-c5f4-4a68-b3ae-a79589f544ea'` — expect geo_id NULL/empty, state CA.
    (b) Confirm both chambers: `SELECT id, name, slug, official_count FROM essentials.chambers WHERE government_id='3e3deaea-c5f4-4a68-b3ae-a79589f544ea'` — expect exactly the two UUIDs 73422d25 (official_count 5) + 6a72dbe8 (official_count NULL), both name 'City Council'.
    (c) Surface ALL offices under the gov: `SELECT o.id office_id, o.chamber_id, o.title, o.district_id, o.district_type, o.politician_id, p.external_id, p.full_name, p.office_id pol_office_id FROM essentials.offices o LEFT JOIN essentials.politicians p ON p.id=o.politician_id WHERE o.government_id='3e3deaea-c5f4-4a68-b3ae-a79589f544ea' ORDER BY o.chamber_id`. Record each office UUID, district_id, link direction (pol_office_id NULL = one-directional), ext_id. Confirm 5 offices / 5 distinct people; confirm Anthony (-201161) + Mullins (-201162) are the two one-directional rows in the DOOMED chamber.
    (d) Confirm the two At-Large district UUIDs: survivor `15458750-78aa-4b9a-ade4-247e28bc25c2` (3 offices) and doomed `809bbb35-8d84-4e51-aef8-44547b32d063` (2 offices). Confirm all 5 office district labels are 'At-Large' (NO relabel will happen).
    (e) Resolve the 2 DOOMED office UUIDs concretely from the scan: Anthony office (1294961c...) and Mullins office (9969febe...). Copy the FULL UUIDs — PATTERNS lists truncated forms.
    (f) Migration counters: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` (expect 999) AND list `ls C:/EV-Accounts/backend/migrations/ | tail` for on-disk MAX (expect 1025). On-disk is authoritative: if on-disk MAX != 1025, the next number is on-disk MAX + 1 — STOP and report.
    Produce a written pre-flight findings block (every UUID/ext_id/district_id resolved, both link directions, both At-Large district UUIDs, both counters). Do NOT proceed to Task 2 if any invariant drifted — surface to operator.
  </action>
  <verify>
    <automated>MISSING — read-only assertion queries via MCP; capture results in the SUMMARY pre-flight block</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='3e3deaea-c5f4-4a68-b3ae-a79589f544ea'` returns NULL or '' (un-backfilled).
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='3e3deaea-c5f4-4a68-b3ae-a79589f544ea' AND name='City Council'` returns 2.
    - The all-offices scan (c) returns exactly 5 offices / 5 distinct people; Anthony (-201161) + Mullins (-201162) confirmed one-directional (pol.office_id NULL) in DOOMED chamber 6a72dbe8.
    - Both At-Large district UUIDs confirmed (survivor 15458750 = 3 offices; doomed 809bbb35 = 2 offices); all 5 labels = 'At-Large'.
    - `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` returns 999; on-disk MAX = 1025; next migration confirmed = 1026.
  </acceptance_criteria>
  <done>All Wave-1 invariants re-confirmed against CONTEXT.md; the 5 offices / 5 people scan recorded; both one-directional links + both At-Large district UUIDs resolved; both counters confirm next=1026; no drift (or drift surfaced to operator and STOP).</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply 1026_burbank_reconcile.sql (geo_id + chamber merge + link repair + At-Large district consolidation)</name>
  <files>C:/EV-Accounts/backend/migrations/1026_burbank_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1018_inglewood_reconcile.sql (THE primary template: header, geo_id backfill, one-directional link repair, chamber move-assert-delete, ledger registration, post-verification — OMIT its Eloy-dedup and by-district relabel steps; Burbank has neither)
    - C:/EV-Accounts/backend/migrations/1010_west_covina_reconcile.sql (secondary: geo_id backfill + chamber move-assert-delete for an at-large city — no relabel)
    - .planning/phases/154-burbank-deep-seed/154-PATTERNS.md §`1026_burbank_reconcile.sql` (per-section SQL excerpts: geo_id guard, IS DISTINCT FROM link repair, move-assert-delete, district repoint, orphan-district delete, registration) + §KEY DIFFERENCES FROM INGLEWOOD (omit dedup + relabel) + §Shared Patterns
    - Task 1 pre-flight findings block (resolved office UUIDs, both At-Large district UUIDs, both counters)
  </read_first>
  <action>
    Using the Write tool, author `C:/EV-Accounts/backend/migrations/1026_burbank_reconcile.sql` following 1018 in structure but OMITTING the Eloy-dedup and by-district relabel blocks. Operations in order inside a single `BEGIN; ... COMMIT;`:
    (1) geo_id backfill (empty-string-safe guard): `UPDATE essentials.governments SET geo_id='0608954' WHERE id='3e3deaea-c5f4-4a68-b3ae-a79589f544ea' AND (geo_id IS NULL OR geo_id='')`.
    (2) Repair the 2 one-directional back-pointers BEFORE the chamber move: `UPDATE essentials.politicians SET office_id='<office>' WHERE id='<pol>' AND office_id IS DISTINCT FROM '<office>'` for Anthony (pol 6c4c7919 -> office 1294961c) and Mullins (pol f933bd87 -> office 9969febe). Use the FULL office UUIDs resolved in Task 1.
    (3) Chamber merge — move BOTH doomed-chamber offices into the survivor by UUID ONLY (both chambers share name/slug): `UPDATE essentials.offices SET chamber_id='73422d25-c0a6-477a-b74f-2b38b94b6389' WHERE chamber_id='6a72dbe8-06fa-4148-9152-1c8e2f11b30e'`. Then the empty-assert DO-block (`RAISE EXCEPTION` if chamber 6a72dbe8 still has offices) and `DELETE FROM essentials.chambers WHERE id='6a72dbe8-06fa-4148-9152-1c8e2f11b30e'`.
    (4) Re-point the 2 moved offices (Anthony 1294961c + Mullins 9969febe) from the doomed At-Large district to the surviving one: `UPDATE essentials.offices SET district_id='15458750-78aa-4b9a-ade4-247e28bc25c2' WHERE id IN ('1294961c...','9969febe...') AND district_id IS DISTINCT FROM '15458750-78aa-4b9a-ade4-247e28bc25c2'`.
    (5) Delete the now-orphaned doomed At-Large district guarded by NOT EXISTS: `DELETE FROM essentials.districts d WHERE d.id='809bbb35-8d84-4e51-aef8-44547b32d063' AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id=d.id)`.
    Do NOT relabel any 'At-Large' district (Burbank stays at-large per RESEARCH). Do NOT create a LOCAL_EXEC Mayor (Wave 2 handles rotational titles). Do NOT touch the school-district gov d5ffbb65.
    AFTER `COMMIT;`, register OUTSIDE the transaction: `INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('1026','burbank_reconcile') ON CONFLICT (version) DO NOTHING`. Append a post-verification comment block adapting 1018's to Burbank UUIDs + geo_id 0608954 + the split-section check.
    Then APPLY the migration live via `mcp__supabase-local__apply_migration` (or execute_sql for the whole file). Then commit the file: `git -C "C:/EV-Accounts" add backend/migrations/1026_burbank_reconcile.sql && git -C "C:/EV-Accounts" commit -m "feat(154): Burbank reconcile — geo_id + dual-chamber merge + one-directional link repair + At-Large consolidation"`.
  </action>
  <verify>
    <automated>MISSING — apply via MCP then run the post-apply assertion queries in acceptance_criteria via mcp__supabase-local__execute_sql</automated>
  </verify>
  <acceptance_criteria>
    - geo_id (VALIDATION Wave-1): `SELECT geo_id FROM essentials.governments WHERE id='3e3deaea-c5f4-4a68-b3ae-a79589f544ea'` -> '0608954'
    - one chamber (VALIDATION Wave-1): `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='3e3deaea-c5f4-4a68-b3ae-a79589f544ea' AND name='City Council'` -> 1
    - doomed gone (VALIDATION Wave-1): `SELECT COUNT(*) FROM essentials.chambers WHERE id='6a72dbe8-06fa-4148-9152-1c8e2f11b30e'` -> 0
    - all 5 offices in survivor (VALIDATION Wave-1): `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='73422d25-c0a6-477a-b74f-2b38b94b6389'` -> 5
    - all 5 bidirectional (VALIDATION Wave-1): `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id=o.politician_id WHERE o.chamber_id='73422d25-c0a6-477a-b74f-2b38b94b6389' AND p.office_id<>o.id` -> 0
    - Anthony + Mullins links repaired (VALIDATION Wave-1): `SELECT office_id FROM essentials.politicians WHERE external_id=-201161` -> NOT NULL; `SELECT office_id FROM essentials.politicians WHERE external_id=-201162` -> NOT NULL
    - doomed At-Large district deleted (VALIDATION Wave-1): `SELECT COUNT(*) FROM essentials.districts WHERE id='809bbb35-8d84-4e51-aef8-44547b32d063'` -> 0
    - At-Large preserved (no relabel): `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE o.chamber_id='73422d25-c0a6-477a-b74f-2b38b94b6389' AND d.label='At-Large'` -> 5
    - split-section check (RESEARCH §Validation Architecture): returns 0 rows for gov 3e3deaea
    - ledger: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` -> 1026; migration file committed to EV-Accounts (git log shows the commit)
  </acceptance_criteria>
  <done>1026_burbank_reconcile.sql authored, applied live, committed; all acceptance assertions pass; single 'City Council' chamber holding all 5 At-Large offices bidirectionally; Anthony + Mullins repaired and consolidated to district 15458750; doomed chamber + doomed district deleted; no relabel, no dedup, no LOCAL_EXEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor SQL -> production Supabase (`mcp__supabase-local` IS production) | Static migration SQL mutates live rows; no external/user input — the surface is data integrity, not injection |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-01 | Tampering | Wrong-row mutation/deletion (two chambers share name+slug 'City Council'/'burbank-city-council') | mitigate | Target chambers/offices/districts by UUID ONLY (never by name/slug); empty-assert DO-block raises EXCEPTION before chamber DELETE; orphan-district DELETE guarded by NOT EXISTS |
| T-154-02 | Tampering | Cross-government contamination (Burbank Unified School District d5ffbb65) | mitigate | Every statement scoped to gov 3e3deaea or specific UUIDs; school-district gov never referenced |
| T-154-03 | Denial of Service | Re-run destroying/duplicating data | mitigate | Idempotent guards: `geo_id IS NULL OR geo_id=''`, `IS DISTINCT FROM`, `NOT EXISTS`, `ON CONFLICT DO NOTHING`; move-then-assert-then-delete in one transaction |
| T-154-04 | Tampering | Accidental At-Large->District relabel or wrongful Mullins unlink (pattern-recognition misfire from prior by-district phases) | mitigate | Plan explicitly omits relabel + unlink; acceptance assertion requires 5 'At-Large' labels intact; RESEARCH Verdicts authoritative |
| T-154-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Wave-1 health one-liner (RESEARCH §Quick Run Command): geo_id='0608954', chamber_count=1, official_count=5.
- Split-section check returns 0 rows for gov 3e3deaea.
- All 5 survivor-chamber offices bidirectional; Anthony + Mullins repaired and on district 15458750; doomed chamber + district gone; 5 'At-Large' labels intact.
- Migration 1026 registered in schema_migrations; file committed to EV-Accounts.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (partial): government + single chamber exist; current officials linked to geo_id 0608954.
- ROADMAP success criterion 2 (partial): at-large structure preserved (no relabel); rotational-mayor titles + official_count finalized in Wave 2.
- ROADMAP success criterion 5 (partial): no duplicate/stale chamber or district rows.
- BURB-01 structural foundation in place.
</success_criteria>

<output>
Create `.planning/phases/154-burbank-deep-seed/154-01-SUMMARY.md` when done (include the Task-1 pre-flight findings block, the resolved office UUIDs, the post-apply office count, and the split-section verdict).
</output>
