---
phase: 199-az-2026-elections-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1372_az_2026_primary_election.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1372.ts
autonomous: true
requirements:
  - AZ-ELEC-01
tags:
  - elections
  - sql
  - arizona
  - az-2026
user_setup: []
must_haves:
  truths:
    - "essentials.elections has an 'AZ 2026 Statewide Primary' row dated 2026-07-21 (state='AZ')"
    - "AZ election count = 2 (pre-existing general + new primary)"
    - "The pre-existing general election e21f5757-071e-4851-9c06-83520d96460e is confirmed present (FK anchor for all downstream race shells)"
    - "Re-apply is idempotent (AZ election count stays 2)"
    - "Migration 1372 ledger entry present in supabase_migrations.schema_migrations"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/1372_az_2026_primary_election.sql
      provides: "Idempotent INSERT for the bare AZ 2026 primary election row + ledger entry"
      contains: "ON CONFLICT (name, election_date, state) DO NOTHING"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-1372.ts
      provides: "Apply script with smoke tests: primary date, AZ count=2, general present, ledger"
      contains: "essentials.elections"
  key_links:
    - from: essentials.elections
      to: essentials.races
      via: "The pre-existing general election (e21f5757…) is the FK target Plans 02/03 resolve via name subquery; the new primary row stays bare (no races link to it)"
      pattern: "WHERE name = 'AZ 2026 Statewide General' AND state = 'AZ'"
---

<objective>
Seed the bare AZ 2026 Statewide Primary election row (2026-07-21) into essentials.elections via migration 1372, and confirm the pre-existing AZ 2026 Statewide General row (e21f5757-071e-4851-9c06-83520d96460e) is present as the FK anchor that Plans 02/03 will resolve by name.

Purpose: D-05 (corrected) requires the primary election row so the discovery date-window covers both dates and the phase mirrors the VA/MD elections pattern. Per the VA Plan 01 lesson, the primary row is bare — ALL race shells anchor to the general, not the primary. This plan is the safety gate that proves the general FK anchor exists before 74 race shells are seeded downstream.

Output: Migration 1372 SQL + apply script applied to production; AZ elections count = 2 verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/199-az-2026-elections-discovery/199-CONTEXT.md
@.planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md
@.planning/phases/105-va-2026-elections-discovery/105-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 1372_az_2026_primary_election.sql + apply script + apply to production</name>
  <files>
    C:/EV-Accounts/backend/migrations/1372_az_2026_primary_election.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-1372.ts
  </files>
  <read_first>
    .planning/phases/199-az-2026-elections-discovery/199-CONTEXT.md — the `<post_research_updates>` block (D-05 CORRECTED: primary date is 2026-07-21, NOT 2026-08-04)
    .planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md §4a (elections row manifest) + §6 (migration numbering) + §10 (pitfall: wrong primary date)
    .planning/phases/105-va-2026-elections-discovery/105-01-PLAN.md — the elections-row seed pattern (bare primary + general; ON CONFLICT idempotency; ledger row; apply-script smoke tests)
    C:/EV-Accounts/backend/scripts/_apply-migration-1216.ts OR _apply-migration-1213.ts — a recent AZ/cross-repo apply-script structure to copy (pg pool via DATABASE_URL, runs the .sql, then smoke queries)
  </read_first>
  <action>
    First re-confirm the migration number is free (RESEARCH §6, disk-authoritative, Ph191 lesson): list `C:/EV-Accounts/backend/migrations/` sorted; the current max is `1371` so the next free number is `1372`. If the disk max is NOT 1371, use the next free number and shift the whole 199 series (1372→1376) up by the same offset, keeping them sequential; record the offset in the SUMMARY.

    Create `C:/EV-Accounts/backend/migrations/1372_az_2026_primary_election.sql` following the VA 105-01 / MD 278 elections-row pattern. Values from RESEARCH §4a: a single INSERT into `essentials.elections (name, election_date, election_type, jurisdiction_level, state)` with name `'AZ 2026 Statewide Primary'`, election_date `'2026-07-21'` (the corrected HB 2022 date — NEVER 2026-08-04), election_type `'primary'`, jurisdiction_level `'state'`, state `'AZ'`. Match the naming style of the existing `'AZ 2026 Statewide General'`. Guard with `ON CONFLICT (name, election_date, state) DO NOTHING`. Header comment must note the primary row is BARE (no races link to it — all shells anchor to the general) and that the date is HB 2022's 2026-07-21. Keep the comment free of the token `cron_active` (Ph202 verify-grep lesson).

    Add a post-verify `DO $$ … RAISE EXCEPTION` block that asserts (a) the primary row exists at 2026-07-21, and (b) the general election `'AZ 2026 Statewide General'` still exists (the downstream FK anchor). Add the mandatory ledger row `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('1372') ON CONFLICT (version) DO NOTHING;`.

    Create `C:/EV-Accounts/backend/scripts/_apply-migration-1372.ts` by copying a recent apply-script structure and substituting the 1372 filename. Smoke tests to print: primary election_date = 2026-07-21; AZ election count = 2; general election e21f5757 present (by name); ledger entry 1372 PRESENT.

    Apply per the AZ 191–198 executor-authors / orchestrator-applies split: the executor authors the SQL + apply script; the orchestrator runs `cd C:/EV-Accounts && npx tsx backend/scripts/_apply-migration-1372.ts`. Do NOT run git in C:/EV-Accounts (Render deploys from master; project memory). The ledger row lives inside the SQL — do not write it separately.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-1372.ts 2>&amp;1 | tee /tmp/_1372.log; grep -q "Primary election_date: 2026-07-21" /tmp/_1372.log &amp;&amp; grep -q "AZ election rows: 2" /tmp/_1372.log &amp;&amp; grep -q "General election present" /tmp/_1372.log &amp;&amp; grep -q "Ledger entry 1372: PRESENT" /tmp/_1372.log</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/1372_az_2026_primary_election.sql exists
    - SQL contains the literal `'AZ 2026 Statewide Primary'`
    - SQL contains the literal `'2026-07-21'` and does NOT contain `'2026-08-04'`
    - SQL contains `ON CONFLICT (name, election_date, state) DO NOTHING`
    - SQL contains `VALUES ('1372')` (ledger entry)
    - SQL does NOT contain the token `cron_active` (not even in comments)
    - File C:/EV-Accounts/backend/scripts/_apply-migration-1372.ts exists and references `1372_az_2026_primary_election.sql`
    - Apply script prints `Primary election_date: 2026-07-21`
    - Apply script prints `AZ election rows: 2 (expected 2)`
    - Apply script prints `General election present` (name='AZ 2026 Statewide General')
    - Apply script prints `Ledger entry 1372: PRESENT`
    - Re-running the apply script keeps AZ election count at 2 (idempotency)
    - Prod query `SELECT election_date FROM essentials.elections WHERE name='AZ 2026 Statewide Primary' AND state='AZ'` returns 2026-07-21
    - Prod query `SELECT COUNT(*) FROM essentials.elections WHERE state='AZ'` returns 2
  </acceptance_criteria>
  <done>
    The AZ 2026 Statewide Primary row exists (2026-07-21); the general election FK anchor is confirmed present; migration 1372 is in the ledger; Plans 02/03 can resolve `(SELECT id FROM essentials.elections WHERE name='AZ 2026 Statewide General' AND state='AZ')` to a non-null UUID.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → production Supabase (`essentials` schema) | Privileged write against live civic reference data; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-01-CORRUPT | Tampering | 1372 INSERT re-apply | mitigate | `ON CONFLICT (name, election_date, state) DO NOTHING` + `DO $$` count assert (AZ count=2) makes re-apply a verified no-op |
| T-199-01-WRONGDATE | Integrity | primary election_date | mitigate | Hardcode corrected `2026-07-21`; acceptance criterion forbids `2026-08-04`; apply-script asserts exact date |
| T-199-01-LEDGER | Repudiation | schema_migrations | mitigate | Mandatory `VALUES ('1372')` ledger row; apply script RAISEs if missing |
| T-199-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — uses existing `pg`/`tsx` in C:/EV-Accounts/backend node_modules |
</threat_model>

<verification>
- AZ 2026 Statewide Primary row exists with date 2026-07-21 (bare — no races link to it)
- Pre-existing general election confirmed present (FK anchor)
- AZ election count = 2, idempotent on re-apply
- Migration 1372 in ledger
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM essentials.elections WHERE state='AZ'` returns 2
- `SELECT election_date FROM essentials.elections WHERE name='AZ 2026 Statewide Primary' AND state='AZ'` returns 2026-07-21
- General election e21f5757-071e-4851-9c06-83520d96460e confirmed present
- AZ-ELEC-01 (elections-row portion) satisfied
</success_criteria>

<output>
Create `.planning/phases/199-az-2026-elections-discovery/199-01-SUMMARY.md` when done with: migration 1372 + apply-script paths; verified primary UUID + date; confirmation the general FK anchor is present; any migration-number offset applied; acceptance-criteria checklist; deviations.
</output>
