---
phase: 143-santa-clarita-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["143-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql
autonomous: true
requirements: [SCLR-01]

must_haves:
  truths:
    - "Marsha McLean is seated as a Santa Clarita Councilmember in the surviving Chamber B (eeabd028, external_id 11243) — she was NOT in the DB before this wave"
    - "Bill Miranda is seated as a Santa Clarita Councilmember in Chamber B — he was NOT in the DB before this wave"
    - "Both new members use reserved external_ids in the SC -700xxx range (recommended -700181 McLean, -700182 Miranda) confirmed free by a pre-flight uniqueness query"
    - "The current rotational Mayor (Laurene Weste, external_id 665693) is flagged by title on her existing Councilmember seat — NO separate Mayor row/chamber/office is created (D-05)"
    - "All 5 current councilmembers (Weste/Ayala/Gibbs/McLean/Miranda) are seated in Chamber B with non-NULL politicians.office_id back-links; Cameron Smyth remains retired (not among the 5)"
    - "Chamber B official_count=5; all 5 seats share the flat single-geo_id 0669088 LOCAL district (no per-seat geofences) — D-10"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql"
      provides: "Roster completion: INSERT McLean + Miranda, mayor title on Weste's seat"
      contains: "-700181"
  key_links:
    - from: "essentials.politicians (external_id -700181 McLean, -700182 Miranda)"
      to: "essentials.offices in Chamber B (eeabd028) on the LOCAL district geo_id 0669088"
      via: "CTE INSERT politician → INSERT office → back-fill office_id"
      pattern: "eeabd028"
    - from: "essentials.offices (Weste's seat, politician external_id 665693)"
      to: "title flagged 'Mayor' (rotational role on a council seat)"
      via: "UPDATE offices.title WHERE politician_id = Weste.id"
      pattern: "title\\s*=\\s*'Mayor"
---

<objective>
Complete the Santa Clarita council to its full, current 5-member roster and flag the rotational Mayor.
This wave runs only after Plan 01 has consolidated the structure to one 'City Council' chamber and
retired Smyth.

⚠️ The two MISSING current members are Marsha McLean and Bill Miranda — NOT Cameron Smyth (departed,
retired in Plan 01). The DB after Plan 01 has Weste/Ayala/Gibbs (Chamber B). The true current council
is Weste/Ayala/Gibbs + McLean + Miranda. Any task that re-seats Smyth or lists only the 3 existing
members as "the roster" is wrong (Pitfall 1, Pitfall 7).

Part A — INSERT Marsha McLean (D2/at-large holdover, term ends 2026):
- external_id -700181 (recommended; pre-flight confirms free), full_name 'Marsha McLean',
  first 'Marsha', last 'McLean', is_active true, is_appointed false, is_incumbent true, is_vacant false,
  source 'santaclarita.gov'. Office in Chamber B (eeabd028), title 'Councilmember', on the LOCAL
  district sharing geo_id '0669088'. Back-fill politicians.office_id.

Part B — INSERT Bill Miranda (D5/at-large holdover, term ends 2026):
- external_id -700182 (recommended; pre-flight confirms free), full_name 'Bill Miranda', first 'Bill',
  last 'Miranda', same flags/source. Office in Chamber B, title 'Councilmember', same LOCAL district.
  Back-fill politicians.office_id.

Part C — Flag the current rotational Mayor on Weste's existing seat:
- UPDATE the office title for Laurene Weste (external_id 665693) to 'Mayor' (planner's discretion; the
  rotational designation goes ON her council seat). Do NOT create a separate LOCAL_EXEC district /
  chamber / Mayor office — SC is council-manager with a rotational mayor (D-05, Pitfall 4). Weste was
  sworn in as Mayor Dec 9 2025 (7th term).

Part D — Confirm Chamber B official_count=5.

Purpose: A complete, accurate, current roster is the prerequisite for headshots (Plan 03) and stances
(Plan 04). Output: Migration 895 applied; 5 current councilmembers seated + office-linked; Smyth still retired.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-santa-clarita-deep-seed/143-CONTEXT.md
@.planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md
@.planning/phases/143-santa-clarita-deep-seed/143-01-SUMMARY.md

<interfaces>
<!-- DB-verified live values (2026-06-19). Pre-flight confirms the external_id range before assigning. -->

Government: 42164a8f-2e0a-4786-9099-ce36f3f97101  geo_id '0669088'  state 'CA'
Surviving council chamber (Chamber B): eeabd028-35b3-4aae-97ec-0fba4c829c00  external_id 11243

Existing Chamber B members (do NOT renumber — real source IDs):
  665693 Laurene Weste (current rotational Mayor → title to 'Mayor')
  665689 Patsy Ayala (Mayor Pro Tem, District 1)
  665692 Jason Gibbs (District 3)

NEW members to INSERT (reserved -700xxx range; pre-flight confirms free):
  Marsha McLean  external_id -700181  first 'Marsha'  last 'McLean'  title 'Councilmember'  (D2 holdover)
  Bill Miranda   external_id -700182  first 'Bill'    last 'Miranda' title 'Councilmember'  (D5 holdover)
  (Cameron Smyth -700180 is RETIRED — already detached in Plan 01; never the 5th member)

Reserved-range pre-flight (Pitfall 5) — run BEFORE assigning:
  SELECT external_id, full_name FROM essentials.politicians
  WHERE external_id BETWEEN -700199 AND -700100 ORDER BY external_id;
  Expect: -700180 = Cameron Smyth (existing, retired); -700181 and -700182 EMPTY. If either is taken,
  pick the next free slot in the range and STOP-report the deviation.
  Also (Assumption A4): SELECT full_name FROM essentials.politicians
  WHERE full_name ILIKE '%McLean%' OR (full_name ILIKE '%Miranda%' AND full_name ILIKE '%Bill%') — confirm
  no pre-existing McLean/Miranda politician row elsewhere (state/federal) to avoid a dup person.

LOCAL district for the council (flat single-geo_id, D-10 — NO per-seat geofences):
  Pre-flight: SELECT id, label, district_type FROM essentials.districts
  WHERE geo_id='0669088' AND state='CA' — reuse the existing LOCAL district row(s) the Chamber B
  offices already link to. Do NOT create new districts/geofences or any LOCAL_EXEC row (Pitfall 3, Pitfall 4).

Schema rules (CA convention):
  - chambers.slug is GENERATED — NEVER include slug in any write
  - governments/districts have NO unique constraint on geo_id — guard with WHERE NOT EXISTS, never ON CONFLICT(geo_id)
  - districts.state = 'CA' (UPPERCASE)
  - politicians.external_id is unique → ON CONFLICT (external_id) DO NOTHING is safe
  - mayor is rotational → NO separate Mayor row/chamber/office; title goes on Weste's existing seat (D-05)

Pattern migrations to copy:
  220_sacramento_officials.sql — CTE INSERT politician → INSERT office → back-fill office_id
  879_long_beach_complete.sql — reserved -700xxx INSERT + office_id back-fill (direct precedent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight uniqueness + district check, then author migration 895 Part A/B (seat McLean + Miranda)</name>
  <files>C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/220_sacramento_officials.sql (politician+office CTE, office_id back-fill)
    - C:/EV-Accounts/backend/migrations/879_long_beach_complete.sql (reserved -700xxx INSERT precedent)
    - .planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md (§Pattern 2, §Migration 895 Part A/B, §Common Pitfalls 1/3/5/7, KEY RESEARCH QUESTION 1)
    - .planning/phases/143-santa-clarita-deep-seed/143-CONTEXT.md (§decisions D-06, D-07, D-08, D-10)
  </read_first>
  <action>
    First run the pre-flight: (a) SELECT external_id, full_name FROM essentials.politicians WHERE
    external_id BETWEEN -700199 AND -700100 — expect -700180 = Cameron Smyth only; -700181 and -700182
    free. If either target is taken, choose the next free slot and STOP-report the deviation (Pitfall 5).
    (b) SELECT for any pre-existing 'McLean' / 'Bill Miranda' politician row (Assumption A4) — if a real
    duplicate person exists, STOP and report rather than creating a second row. (c) SELECT id, label,
    district_type FROM essentials.districts WHERE geo_id='0669088' AND state='CA' to capture the existing
    LOCAL district_id the Chamber B offices already use — reuse it (D-10; do NOT create new
    districts/geofences/LOCAL_EXEC; Pitfall 3/4). Then begin
    C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql with Part A and Part B: for each of
    McLean (-700181) and Miranda (-700182), a CTE that INSERTs the politician (names/flags per interfaces,
    source 'santaclarita.gov', is_active true, is_appointed false, is_incumbent true, is_vacant false) with
    ON CONFLICT (external_id) DO NOTHING RETURNING id, then INSERTs an office into chamber
    eeabd028-35b3-4aae-97ec-0fba4c829c00 on the captured LOCAL district_id, title exactly 'Councilmember',
    representing_state 'CA', is_appointed_position false, guarded by NOT EXISTS on (chamber_id, politician_id).
    Immediately back-fill politicians.office_id to the new office id. Do NOT include slug anywhere. This is a
    structural migration (registers in schema_migrations normally).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT p.full_name, o.title FROM essentials.politicians p JOIN essentials.offices o ON o.id=p.office_id WHERE p.external_id IN (-700181,-700182) ORDER BY p.external_id;" returns Marsha McLean / Councilmember and Bill Miranda / Councilmember</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: -700181 and -700182 confirmed free (or alternate free slots chosen + deviation reported); no pre-existing McLean/Miranda dup person
    - McLean (-700181) and Miranda (-700182) politician rows exist with correct names/flags, source 'santaclarita.gov'
    - Each has an office in chamber eeabd028…, on the shared geo_id 0669088 LOCAL district, title exactly 'Councilmember', is_appointed_position=false
    - Both politicians.office_id back-links are non-NULL
    - No new essentials.districts / geofence_boundaries / LOCAL_EXEC rows were created (flat single-geo_id, D-10)
  </acceptance_criteria>
  <done>McLean + Miranda seated in Chamber B on the shared LOCAL district; office_id back-links set; no per-seat geofences created.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 895 Part C/D — flag rotational Mayor on Weste's seat + set official_count, then roster audit</name>
  <files>C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql</files>
  <read_first>
    - .planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md (§KEY RESEARCH QUESTION 2 rotational mayor, §Migration 895 Part C/D, §Common Pitfalls 4)
    - .planning/phases/143-santa-clarita-deep-seed/143-CONTEXT.md (§decisions D-05)
    - LOCATION-ONBOARDING.md (§LA-Area City Stances Quick Reference — rotational vs directly-elected Mayor)
  </read_first>
  <action>
    Append Part C and Part D to migration 895. Part C: UPDATE essentials.offices SET title='Mayor' WHERE
    politician_id = (SELECT id FROM essentials.politicians WHERE external_id=665693) — flag Laurene Weste's
    EXISTING council seat as the current rotational Mayor (planner's discretion: 'Mayor'). Do NOT create a
    separate LOCAL_EXEC district, chamber, or Mayor office; do NOT add a second row for Weste (Pitfall 4,
    D-05). Part D: UPDATE essentials.chambers SET official_count=5 WHERE
    id='eeabd028-35b3-4aae-97ec-0fba4c829c00' if not already 5. End the file with a roster-count
    post-verification query: all 5 current councilmembers (665693/665689/665692/-700181/-700182) seated in
    Chamber B with office_id non-NULL; exactly one office titled 'Mayor'; Smyth (-700180) NOT among them
    (still retired). Apply via mcp__supabase-local execute_sql or psql -f.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id WHERE o.chamber_id='eeabd028-35b3-4aae-97ec-0fba4c829c00' AND p.is_active=true;" returns 5 AND psql "$DATABASE_URL" -c "SELECT o.title FROM essentials.offices o JOIN essentials.politicians p ON o.politician_id=p.id WHERE p.external_id=665693;" returns 'Mayor'</automated>
  </verify>
  <acceptance_criteria>
    - Weste's (665693) office title = 'Mayor'; exactly one office under Chamber B is titled 'Mayor'
    - No separate LOCAL_EXEC district / Mayor chamber / Mayor office was created; Weste has exactly one office row
    - Chamber B (eeabd028…) official_count=5
    - Full roster query returns the 5 current councilmembers (Weste/Ayala/Gibbs/McLean/Miranda) all office-linked
    - Smyth (-700180) is NOT in the active roster (is_incumbent=false, is_active=false from Plan 01)
    - Migration 895 is idempotent: second apply changes 0 rows, no error
  </acceptance_criteria>
  <done>Rotational Mayor flagged on Weste's seat; official_count=5; full current roster of 5 seated and office-linked; migration idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator directly against production Supabase. No untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-07 | Tampering | New external_id (-700181/-700182) collides with an existing CA politician | mitigate | Pre-flight SELECT on range -700199…-700100 before any INSERT; ON CONFLICT (external_id) DO NOTHING belt-and-suspenders; STOP-report on collision |
| T-143-08 | Spoofing | Wrong people seated / Smyth re-seated as the 5th member (stale roster) | mitigate | Roster verified against santaclarita.gov 2026-06-19 (Weste/Ayala/Gibbs/McLean/Miranda); Smyth retired in Plan 01 and excluded here; Pitfall 1/7 enforced |
| T-143-09 | Tampering | Duplicate McLean/Miranda person row already exists elsewhere | mitigate | Assumption A4 pre-flight name SELECT; STOP-report if a real dup person is found rather than INSERT |
| T-143-10 | Tampering | Rotational mayor modeled as a separate LOCAL_EXEC row (double-counts Weste) | mitigate | D-05 enforced: title-only UPDATE on Weste's existing seat; post-verify Weste has exactly one office and there is exactly one 'Mayor' title; no LOCAL_EXEC created (Pitfall 4) |
| T-143-11 | Tampering | Per-seat geofence/district explosion from CVRA district numbers | mitigate | D-10 flat single-geo_id; reuse existing geo_id 0669088 LOCAL district; no geofence_boundaries / new district INSERTs (Pitfall 3) |
| T-143-12 | Tampering | chambers.slug included in INSERT → error; or non-idempotent re-apply duplicates rows | mitigate | slug omitted (GENERATED); all writes guarded by ON CONFLICT / NOT EXISTS / IS NULL; second apply = 0 rows |
</threat_model>

<verification>
After applying 895:
1. -700181 (McLean) and -700182 (Miranda) each have a politician row + linked Chamber B office, office_id non-NULL
2. all 5 current councilmembers (665693/665689/665692/-700181/-700182) seated in Chamber B
3. Weste's office title = 'Mayor'; exactly one 'Mayor' title; no separate Mayor row/chamber/LOCAL_EXEC
4. Chamber B official_count=5; no new geofence/district rows (flat single-geo_id)
5. Smyth (-700180) still retired, not in active roster
6. feedback_section_split_check SQL → still 0 rows
</verification>

<success_criteria>
Santa Clarita's elected council is complete and current: 5 at-large councilmembers
(Weste/Ayala/Gibbs/McLean/Miranda) all office-linked in one chamber, with the rotational Mayor flagged
on Weste's seat and Smyth correctly excluded — ready for headshots in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/143-santa-clarita-deep-seed/143-02-SUMMARY.md` when done.
</output>
