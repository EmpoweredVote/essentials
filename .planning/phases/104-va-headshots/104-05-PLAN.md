---
phase: 104-va-headshots
plan: 05
type: execute
wave: 2
depends_on:
  - 104-01
  - 104-02
  - 104-03
  - 104-04
files_modified:
  - C:/EV-Accounts/backend/migrations/315_va_headshots.sql
autonomous: false
requirements:
  - VA-GOV-06
must_haves:
  truths:
    - "Migration 315_va_headshots.sql exists as a single AUDIT-ONLY SQL file documenting 155 politician_images INSERTs"
    - "Each INSERT uses the WHERE NOT EXISTS guard pattern from migration 314 (idempotent)"
    - "Each INSERT references an external_id subquery (no hardcoded UUIDs per RESEARCH Open Question 2 recommendation)"
    - "Header block documents per-official source URL, original dimensions, crop dimensions, resize dimensions per D-10"
    - "Migration is NOT applied via Supabase MCP — scripts already wrote politician_images rows live (or will, see below) and migration 315 is the audit record"
    - "Final SQL count returns exactly 155 (3 execs + 40 senators + 99 delegates + 13 federal)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/315_va_headshots.sql"
      provides: "AUDIT-ONLY migration recording all 155 VA headshot politician_images INSERTs"
      contains: "155 WHERE NOT EXISTS INSERT statements grouped by section (execs, senators, delegates, federal)"
  key_links:
    - from: "migration 315 INSERT subquery"
      to: "essentials.politicians.external_id"
      via: "SELECT id FROM essentials.politicians WHERE external_id = N"
      pattern: "WHERE external_id = -[0-9]+"
    - from: "migration 315 url column"
      to: "Supabase Storage politician_photos bucket"
      via: "CDN URL kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg"
      pattern: "kxsdzaojfaibhuzmclfq.*politician_photos"
---

<objective>
Two-part finalization for Phase 104:

1. Apply the politician_images INSERTs for all 155 VA officials (scripts in Plans 01-04 ONLY upload to Storage — they do not insert DB rows; see _tmp-alexandria-headshots.py and migration 314 — the scripts populate Storage and the migration writes politician_images rows). This plan executes the INSERTs via psql/SQL editor (NOT via Supabase MCP per D-09).

2. Write `migration 315_va_headshots.sql` as the AUDIT-ONLY record of those INSERTs with full D-10 documentation (per-official source URL, original dimensions, crop dimensions). Note: prior phase 103's migration 314 was NOT marked AUDIT-ONLY — Phase 104 follows the CONTEXT.md D-09 explicit instruction to make 315 AUDIT-ONLY.

Purpose: Close VA-GOV-06 by establishing the canonical politician_images record for all 155 VA officials and an auditable migration file.
Output: One SQL migration file in C:/EV-Accounts/backend/migrations/ + 155 confirmed politician_images rows in essentials.politician_images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-va-headshots/104-CONTEXT.md
@.planning/phases/104-va-headshots/104-RESEARCH.md
@.planning/phases/104-va-headshots/104-VALIDATION.md
@.planning/phases/104-va-headshots/104-01-SUMMARY.md
@.planning/phases/104-va-headshots/104-02-SUMMARY.md
@.planning/phases/104-va-headshots/104-03-SUMMARY.md
@.planning/phases/104-va-headshots/104-04-SUMMARY.md
@C:/EV-Accounts/backend/migrations/314_alexandria_headshots.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate migration 315_va_headshots.sql with 155 AUDIT-ONLY INSERT statements</name>
  <files>C:/EV-Accounts/backend/migrations/315_va_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/314_alexandria_headshots.sql (header style, INSERT pattern, comment style — Phase 104 D-10 says "matching the documentation style of 314_alexandria_headshots.sql")
    - .planning/phases/104-va-headshots/104-01-SUMMARY.md (exec manifest + per-official source URL + dimensions)
    - .planning/phases/104-va-headshots/104-02-SUMMARY.md (senator manifest + per-official source URL + dimensions)
    - .planning/phases/104-va-headshots/104-03-SUMMARY.md (delegate manifest + per-official source URL + dimensions; HD-20 absent)
    - .planning/phases/104-va-headshots/104-04-SUMMARY.md (federal manifest + per-official source URL + dimensions; Walkinshaw house.gov noted)
    - .planning/phases/104-va-headshots/104-CONTEXT.md (D-09 AUDIT-ONLY; D-10 documentation requirements; D-11 type='default'; D-14 url column; D-15 NOT EXISTS guard; D-16 photo_license='public_domain')
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/315_va_headshots.sql`. Use the migration 314 header block as the template but ADD AUDIT-ONLY language per D-09 and the 104-RESEARCH Pattern 3 section. Structure the file with these sections in order:

    HEADER COMMENT BLOCK including: title "Migration 315: VA Headshots (VA-GOV-06)"; counts ("3 execs + 40 senators + 99 delegates + 13 federal = 155 rows"); HD-20 vacant-skip note; storage bucket name `politician_photos`; CDN base URL; per-official sources subsection grouped by section header — quote the exact `source_url -> original {WxH} -> crop {WxH} -> resize 600x750` line for each of the 155 officials, copied from the four SUMMARY files; explicit AUDIT-ONLY warning ("Do NOT apply via Supabase MCP ledger. Scripts in scripts/_tmp-va-*-headshots.py do the live writes; this migration records them and supplies the politician_images rows when applied via psql or Supabase SQL editor."); special-case callouts (Jay Jones landscape source; Walkinshaw house.gov fallback; senate anomalous names Mulchi9/Williams Graves21/Carroll Foy33/VanValkenburg16/Bennett-Parker39/Craig19).

    INSERT BLOCKS in 4 sections, each preceded by a section comment banner (`-- ============================================================` + `-- VA STATE EXECUTIVES (3 officials)` etc.). Each INSERT MUST use this exact pattern (copied verbatim from migration 314):

    ```
    -- {full_name} ({external_id}) — {title}
    INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license)
    SELECT gen_random_uuid(),
           (SELECT id FROM essentials.politicians WHERE external_id = {external_id}),
           '{CDN_BASE}/{politician_uuid}-headshot.jpg',
           'default', 'public_domain'
    WHERE NOT EXISTS (
      SELECT 1 FROM essentials.politician_images
      WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = {external_id})
    );
    ```

    Use `{politician_uuid}` resolved from the SUMMARY manifests (the scripts emit `SUCCESS: {external_id} {full_name} {politician_uuid} -> {cdn_url}` lines — copy the CDN URLs verbatim from those lines). HD-20 (external_id -5120020) MUST NOT appear anywhere in the file (no INSERT, no comment line that references it as a row to insert; OK to mention in the header note that HD-20 is intentionally skipped).

    No BEGIN/COMMIT wrapper per migration 314 pattern. No DROP/CREATE statements. The INSERTs are idempotent via WHERE NOT EXISTS.
  </action>
  <verify>
    <automated>grep -c "^INSERT INTO essentials.politician_images" C:/EV-Accounts/backend/migrations/315_va_headshots.sql</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/315_va_headshots.sql exists
    - `grep -c "^INSERT INTO essentials.politician_images" 315_va_headshots.sql` returns exactly 155
    - `grep -c "^WHERE NOT EXISTS" 315_va_headshots.sql` returns exactly 155
    - `grep -c "type = 'default'" 315_va_headshots.sql` PLUS `grep -c "'default', 'public_domain'" 315_va_headshots.sql` totals ≥ 155 (every INSERT classifies as default + public_domain)
    - `grep -c "external_id = -5120020" 315_va_headshots.sql` returns 0 (HD-20 absent)
    - `grep -c "external_id = -510001\\b" 315_va_headshots.sql` returns ≥ 2 (1 in INSERT subquery + 1 in WHERE NOT EXISTS subquery; Spanberger present)
    - `grep -c "external_id = -5120100" 315_va_headshots.sql` returns ≥ 2 (HD-100 Bloxom present)
    - File contains string `AUDIT-ONLY` in header block
    - File contains string `Do NOT apply via Supabase MCP`
    - File contains string `kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/` (CDN URLs present)
    - File does NOT contain string `storage_url` (column is `url`)
    - File does NOT contain string `politician-headshots` (wrong bucket name)
  </acceptance_criteria>
  <done>
    Migration 315 exists, has exactly 155 INSERT/WHERE NOT EXISTS pairs, HD-20 is absent, AUDIT-ONLY documentation is in place. Task 2 applies it via psql.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply migration 315 via psql (NOT Supabase MCP) and verify 155 politician_images rows</name>
  <files>(no new files; applies migration 315 to production DB via psql, then runs verification SQL)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/315_va_headshots.sql (the migration written in Task 1)
    - .planning/phases/104-va-headshots/104-VALIDATION.md (Full Post-Run Verification Query section)
    - C:/EV-Accounts/backend/.env (DATABASE_URL credentials)
  </read_first>
  <action>
    Apply migration 315 by running it directly against the production database via psql (NOT through the Supabase MCP `apply_migration` tool per D-09). Use the DATABASE_URL from `C:/EV-Accounts/backend/.env`. Example invocation: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/315_va_headshots.sql` (or equivalent Python psycopg2 script reading the SQL file and executing it). The migration is idempotent via WHERE NOT EXISTS so re-running is safe. After applying, run the Full Post-Run Verification Query from 104-VALIDATION.md and confirm the result is exactly 155. Also run the secondary check: `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id = -5120020` and confirm it returns 0 (HD-20 absent). Also confirm `SELECT COUNT(DISTINCT politician_id) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (the 155-element list from VALIDATION.md))` returns 155 (no duplicate politician_id values).

    Capture the verification query outputs and record them in 104-05-SUMMARY.md. DO NOT update STATE.md migration counter to 316 — that happens after `/gsd-complete-phase`, not here.
  </action>
  <verify>
    <automated>python -c "import psycopg2; e={}; [e.update({k.strip():v.strip()}) for k,v in (l.split('=',1) for l in open('C:/EV-Accounts/backend/.env') if '=' in l and not l.strip().startswith('#'))]; c=psycopg2.connect(e['DATABASE_URL']+'?sslmode=require'); cur=c.cursor(); cur.execute(\"SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (-510001,-510002,-510003) OR p.external_id BETWEEN -5110040 AND -5110001 OR (p.external_id BETWEEN -5120100 AND -5120001 AND p.external_id != -5120020) OR p.external_id IN (-400079,-400080,-5102001,-5102002,-5102003,-5102004,-5102005,-5102006,-5102007,-5102008,-5102009,-5102010,-5102011)\"); print('count',cur.fetchone()[0])"</automated>
  </verify>
  <acceptance_criteria>
    - Migration 315 applied without SQL errors (psql exit code 0)
    - Full verification query from 104-VALIDATION.md returns exactly 155
    - HD-20 verification query returns 0 (no politician_image row for external_id -5120020)
    - All 155 politician_images rows have `type = 'default'` (no 'headshot' or other values)
    - All 155 rows have `photo_license = 'public_domain'`
    - All 155 rows have URLs starting with `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/`
    - 104-05-SUMMARY.md contains the verification query outputs verbatim
    - 104-05-SUMMARY.md notes that migration 315 was applied via psql, NOT via Supabase MCP
  </acceptance_criteria>
  <done>
    155 politician_images rows live in production; HD-20 has zero rows; migration 315 is canonical audit record.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human spot-check of headshot quality on 5 risk-flagged officials</name>
  <what-built>
    155 VA official headshots in production at 600x750 JPEG, served via Supabase Storage CDN URLs in essentials.politician_images.
  </what-built>
  <how-to-verify>
    Open the production frontend (or visit the Storage URLs directly) and visually inspect these 5 risk-flagged officials. For each, confirm the headshot is recognizable, not blurry, not stretched, and shows the person's face clearly without graphics/text superimposed.

    1. **Jay Jones (AG, external_id=-510003)** — source was landscape 425x283; check that the cropped-then-upscaled portrait at 600x750 looks acceptable. URL: `{CDN_BASE}/{uuid}-headshot.jpg` (UUID in 104-01-SUMMARY.md).
    2. **Tammy Brankley Mulchi (SD-9, external_id=-5110009)** — anomalous name 'Mulchi9'; confirm it's actually her photo (not a different senator's).
    3. **Christie New Craig (SD-19, external_id=-5110019)** — anomalous name 'Craig19'; confirm correct identity.
    4. **James Walkinshaw (US Rep VA-11, external_id=-5102011)** — only federal sourced from walkinshaw.house.gov; confirm quality matches the other 12 (which use the unitedstates.github.io mirror).
    5. **One random delegate** — pick any HD-N at random; confirm the H-ID mapping returned the right person (not a former member with the same H-ID).

    Also spot-check at least 5 random senator headshots given the small source images (~262x269); confirm Lanczos upscale to 600x750 produced acceptable (not blurry) results.

    If any of the 5 is unacceptable, type "issue: {official name} — {problem}" and a follow-up gap-closure plan can be drafted.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 (plus 5 random senators) look acceptable, or "issue: {official} — {problem}" for any that need rework.</resume-signal>
</task>

</tasks>

<verification>
After all 3 tasks: 155 politician_images rows exist in essentials.politician_images with type='default' and photo_license='public_domain'; HD-20 is absent; migration 315 is on disk as the audit record; risk-flagged officials have been visually approved by the human.
</verification>

<success_criteria>
1. `315_va_headshots.sql` exists with exactly 155 INSERT statements (3 execs + 40 senators + 99 delegates + 13 federal)
2. Migration applied via psql (NOT Supabase MCP per D-09); production verification query returns 155
3. HD-20 verification query returns 0
4. All 5 risk-flagged officials pass human visual spot-check
5. VA-GOV-06 satisfied; phase ready for `/gsd-complete-phase`
</success_criteria>

<output>
Create `.planning/phases/104-va-headshots/104-05-SUMMARY.md` when done, including:
- Full verification SQL output (155 expected, actual count)
- HD-20 absence confirmation
- Path to migration 315 file + line count
- Human spot-check result (approved / issues list)
- Note that migration counter in STATE.md should advance to 316 on `/gsd-complete-phase`
</output>
