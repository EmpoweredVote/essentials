---
phase: 199-az-2026-elections-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - 199-01
files_modified:
  - C:/EV-Accounts/backend/migrations/1375_az_2026_local_races.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1375.ts
autonomous: true
requirements:
  - AZ-ELEC-01
tags:
  - races
  - sql
  - arizona
  - az-2026
  - local
user_setup: []
must_haves:
  truths:
    - "Exactly 7 Tucson-metro local race shells exist under the AZ general: South Tucson City Council (seats=3), Oro Valley Mayor (seats=1), Oro Valley Town Council (seats=3), Marana Mayor (seats=1), Marana Town Council (seats=4), Sahuarita Town Council (seats=3)"
    - "ZERO Pima County Board of Supervisors shells (not on the 2026 ballot)"
    - "ZERO City of Tucson (ward/mayor) shells (odd-year cycle, no 2026 race)"
    - "Both mayor shells are single-winner with non-NULL office_id; every at-large council shell has a non-NULL office_id via deterministic LIMIT 1 anchor"
    - "Re-apply idempotent; ledger 1375 present"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/1375_az_2026_local_races.sql
      provides: "7 local race shells + negative-scope post-verify (zero Pima BoS, zero Tucson) + ledger"
      contains: "ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-1375.ts
      provides: "Smoke tests: 7 local shells, zero Pima BoS, zero Tucson, zero NULL office_id, ledger"
      contains: "essentials.races"
  key_links:
    - from: essentials.races.office_id
      to: essentials.offices (via chamber → government geo_id)
      via: "at-large council anchor = ORDER BY o.id LIMIT 1 over the body's council offices; fetchGovernmentRaceRows matches by government geo_id so any council office is correct"
      pattern: "ORDER BY o.id LIMIT 1"
    - from: essentials.races.election_id
      to: essentials.elections (general, e21f5757)
      via: "subquery WHERE name='AZ 2026 Statewide General' AND state='AZ'"
      pattern: "WHERE name = 'AZ 2026 Statewide General'"
---

<objective>
Seed exactly the 7 confirmed Tucson-metro local 2026 race shells (migration 1375) against the pre-existing general election e21f5757, and prove the two negative facts: ZERO Pima County Board of Supervisors shells and ZERO City of Tucson shells.

Purpose: D-03 (corrected) — seed local shells ONLY where the 2026 cycle is confirmed. Research confirmed: South Tucson (3 council seats), Oro Valley (mayor + 3 council), Marana (mayor + 4 council), Sahuarita (3 council). Pima BoS is a presidential-cycle body (2024→2028) and Tucson city runs odd-year — both get zero shells. Pure structure: NO candidates hand-seeded (discovery cron fills). Every shell carries primary_party NULL and a non-NULL office_id.

Output: Migration 1375 SQL + apply script applied to production; exactly 7 local shells verified; zero Pima/Tucson confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/199-az-2026-elections-discovery/199-CONTEXT.md
@.planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md
@.planning/phases/105-va-2026-elections-discovery/105-02-PLAN.md
@C:/EV-Accounts/backend/src/lib/electionService.ts

<interfaces>
<!-- At-large local council office_id anchoring — RESOLVED from electionService.ts. -->
<!-- fetchGovernmentRaceRows (lines 130-155) INNER-JOINs offices o ON o.id = r.office_id,
     then chambers → governments, matching by g.geo_id. A NULL office_id makes the local
     race INVISIBLE. So each council shell MUST have a non-NULL office_id. Any one of the
     body's council offices is correct because they all share the same chamber→government;
     ORDER BY o.id LIMIT 1 gives a deterministic, safe pick. Mayor offices are unambiguous
     singletons (already non-NULL). -->

Mayor office_ids (RESEARCH §4e — verify non-NULL at execute time):
  Oro Valley Mayor   b3c8f75c-e8f9-4097-ab14-5790e380f9df
  Marana Mayor       ac3daf57-c50a-4056-af79-acdda23551df

Council government geo_ids (anchor via chamber → government):
  South Tucson  0468850  (seat count 3)
  Oro Valley    0451600  (seat count 3)
  Marana        0444270  (seat count 4)
  Sahuarita     0462140  (seat count 3, NO mayor race — council appoints mayor)

Confirmed seed set (7 races) — RESEARCH §4e:
  'South Tucson City Council'  seats=3   (at-large)
  'Oro Valley Mayor'           seats=1   (office b3c8f75c)
  'Oro Valley Town Council'    seats=3   (at-large)
  'Marana Mayor'               seats=1   (office ac3daf57)
  'Marana Town Council'        seats=4   (at-large)
  'Sahuarita Town Council'     seats=3   (at-large)
Do NOT seed: any Pima BoS / County Supervisor race; any City of Tucson ward or mayor race.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the 6 local office anchors against production and confirm exact council chamber/government names</name>
  <files>
    (no file writes — query-only; record results in the migration header scratch + SUMMARY)
  </files>
  <read_first>
    .planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md §4e (local manifest: seat counts, mayor office_ids, government geo_ids)
    C:/EV-Accounts/backend/src/lib/electionService.ts lines 130-155 (fetchGovernmentRaceRows — the office_id-required, government-geo-matched convention already summarised in <interfaces>)
  </read_first>
  <action>
    Query production to resolve each local anchor. For the two mayors, confirm the office_ids in <interfaces> resolve to live essentials.offices rows (representing_city Oro Valley / Marana). For the four at-large councils, resolve a deterministic council office_id per body via the chamber → government join, e.g. `SELECT o.id FROM essentials.offices o JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.governments g ON g.id=ch.government_id WHERE g.geo_id='0468850' AND ch.name ILIKE '%council%' ORDER BY o.id LIMIT 1` (repeat for 0451600, 0444270, 0462140). Record the exact chamber names so the migration subqueries match production.

    Verify each of the 6 anchors returns a non-NULL office_id. If any returns 0 rows (e.g. a council chamber name mismatch), STOP and surface the gap — do not seed a NULL local office_id (it would make the race invisible per <interfaces>). Confirm no City-of-Tucson or Pima-BoS office is accidentally in scope.
  </action>
  <verify>
    <automated>Run the 6 resolution queries; confirm all 6 return exactly one non-NULL office_id before Task 2. Record the 6 UUIDs + chamber names in the migration file header comment.</automated>
  </verify>
  <acceptance_criteria>
    - Oro Valley Mayor + Marana Mayor office_ids resolve to exactly 1 live office each (representing_city matches)
    - Each of the 4 council anchors (geo_ids 0468850, 0451600, 0444270, 0462140) resolves to exactly 1 non-NULL office_id via chamber→government
    - The exact council chamber names are recorded for use in Task 2 subqueries
    - No City of Tucson (0477000) or Pima BoS office is among the resolved anchors
  </acceptance_criteria>
  <done>
    All 6 local office anchors confirmed non-NULL against production; Task 2 can write the migration with correct subqueries/literals.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 1375_az_2026_local_races.sql (7 shells) + apply script + apply</name>
  <files>
    C:/EV-Accounts/backend/migrations/1375_az_2026_local_races.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-1375.ts
  </files>
  <read_first>
    .planning/phases/105-va-2026-elections-discovery/105-02-PLAN.md — WITH gen_elec CTE + scoped post-verify pattern
    C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql — per-race office_id subquery + ON CONFLICT shape
    .planning/phases/199-az-2026-elections-discovery/199-RESEARCH.md §4e + §10 (pitfalls: zero Pima BoS, zero Tucson)
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/1375_az_2026_local_races.sql`. Resolve election_id via the general-election name subquery (no hardcoded UUID). Insert exactly the 7 races in <interfaces>, all `primary_party=NULL`:
    - `Oro Valley Mayor` seats=1 → office b3c8f75c (verified in Task 1); `Marana Mayor` seats=1 → office ac3daf57.
    - `South Tucson City Council` seats=3, `Oro Valley Town Council` seats=3, `Marana Town Council` seats=4, `Sahuarita Town Council` seats=3 → each anchored to its Task-1-resolved council office via the chamber→government `ORDER BY o.id LIMIT 1` subquery (deterministic, non-NULL).

    Guard with `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`. Add a post-verify `DO $$` that asserts: exactly the 7 named local shells exist with non-NULL office_id; AND a NEGATIVE assert that NO race under the AZ general has position_name LIKE `%Supervisor%` (zero Pima BoS) and NO race matches `Tucson Mayor` or LIKE `%Tucson Ward%` (zero City of Tucson) — note `South Tucson City Council` is legitimately present and must NOT trip these negatives, so scope the Tucson negatives to `Tucson Mayor` / `%Tucson Ward%` exactly (do NOT use a bare `%Tucson%`). RAISE EXCEPTION on any violation. Add ledger `VALUES ('1375')`. Keep comments free of `cron_active`.

    Create `_apply-migration-1375.ts`. Smoke tests: local shell count = 7; Pima BoS count = 0; City-of-Tucson (Tucson Mayor / Tucson Ward) count = 0; local NULL office_id count = 0; ledger 1375 PRESENT. Orchestrator applies via `cd C:/EV-Accounts && npx tsx backend/scripts/_apply-migration-1375.ts`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-1375.ts 2>&amp;1 | tee /tmp/_1375.log; grep -q "Local shells: 7" /tmp/_1375.log &amp;&amp; grep -q "Pima BoS shells: 0" /tmp/_1375.log &amp;&amp; grep -q "Tucson city shells: 0" /tmp/_1375.log &amp;&amp; grep -q "Local NULL office_id count: 0" /tmp/_1375.log &amp;&amp; grep -q "Ledger entry 1375: PRESENT" /tmp/_1375.log</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/1375_az_2026_local_races.sql exists
    - SQL resolves election_id via `WHERE name = 'AZ 2026 Statewide General'` (no hardcoded UUID)
    - SQL contains exactly these 6 position_name literals: `'South Tucson City Council'`, `'Oro Valley Mayor'`, `'Oro Valley Town Council'`, `'Marana Mayor'`, `'Marana Town Council'`, `'Sahuarita Town Council'` (7 races: South Tucson seats=3, OV Mayor seats=1, OV Council seats=3, Marana Mayor seats=1, Marana Council seats=4, Sahuarita Council seats=3)
    - SQL contains NO Pima County / Board of Supervisors / Supervisor race
    - SQL contains NO City of Tucson ward or Tucson Mayor race
    - Seat counts exactly: South Tucson=3, Oro Valley Council=3, Marana Council=4, Sahuarita Council=3, both mayors=1
    - SQL contains `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`
    - SQL post-verify includes a negative assert for `%Supervisor%` = 0 and `Tucson Mayor`/`%Tucson Ward%` = 0 (scoped so `South Tucson City Council` does NOT trip it)
    - SQL contains `VALUES ('1375')` and does NOT contain `cron_active`
    - Apply script prints `Local shells: 7`, `Pima BoS shells: 0`, `Tucson city shells: 0`, `Local NULL office_id count: 0`, `Ledger entry 1375: PRESENT`
    - Re-run keeps counts unchanged (idempotency)
    - Zero candidates inserted by this migration
  </acceptance_criteria>
  <done>
    Exactly 7 Tucson-metro local shells exist under the AZ general with non-NULL office_ids; zero Pima BoS and zero City-of-Tucson shells proven; migration 1375 in ledger.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → production Supabase (`essentials.races`) | Privileged write of local civic reference shells; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-03-PHANTOM | Integrity (wrong data reaching voters) | Pima BoS / Tucson phantom races | mitigate | Negative post-verify asserts `%Supervisor%`=0 and `Tucson Mayor`/`%Tucson Ward%`=0; only the 7 confirmed shells inserted |
| T-199-03-INVISIBLE | Information Disclosure | at-large council office_id anchoring | mitigate | Deterministic non-NULL `ORDER BY o.id LIMIT 1` council anchor (fetchGovernmentRaceRows INNER JOIN requires non-NULL); NULL-office_id count asserted 0 |
| T-199-03-CORRUPT | Tampering | 1375 re-apply | mitigate | `ON CONFLICT … WHERE primary_party IS NULL DO NOTHING` + scoped `DO $$` count assert |
| T-199-03-LEDGER | Repudiation | schema_migrations | mitigate | Mandatory `VALUES ('1375')`; apply script RAISEs if missing |
| T-199-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — existing `pg`/`tsx` only |
</threat_model>

<verification>
- Exactly 7 local shells (South Tucson 3, OV mayor + council, Marana mayor + council, Sahuarita council)
- Zero Pima BoS; zero City of Tucson; zero NULL office_id
- Idempotent re-apply; ledger 1375 PRESENT
- Zero candidates hand-seeded
</verification>

<success_criteria>
- Prod: exactly 7 named local shells under the general, all non-NULL office_id
- Prod: `%Supervisor%` count = 0 and `Tucson Mayor`/`%Tucson Ward%` count = 0 under the general
- AZ-ELEC-01 (local shell portion) satisfied
</success_criteria>

<output>
Create `.planning/phases/199-az-2026-elections-discovery/199-03-SUMMARY.md` when done with: migration 1375 + apply-script paths; the 6 resolved local office anchors (position_name + office_id + chamber name); smoke-test output; acceptance-criteria checklist; deviations.
</output>
