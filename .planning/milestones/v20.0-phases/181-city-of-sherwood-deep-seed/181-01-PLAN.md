---
phase: 181-city-of-sherwood-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-sherwood-wave0-probe.sql
autonomous: false
requirements: [WASH-07]

must_haves:
  truths:
    - "Orchestrator has re-confirmed geo_id 4167100 has exactly 1 G4110 geofence row — RESEARCH CORRECTED the ROADMAP/CONTEXT-stated 4167450 (0 rows, does not exist at all) to 4167100 (sole G4110 'Sherwood city' row); this probe runs FIRST, before any other, and must ALSO confirm 4167450 returns 0 rows"
    - "Orchestrator has confirmed the school-district row 4111290 (G5420 'Sherwood School District 88J') is a distinct, non-colliding geofence (relevant to future Phase 184, not this phase)"
    - "Orchestrator has confirmed no City of Sherwood government/chamber row already exists (greenfield) by geo_id 4167100 AND by name ILIKE '%sherwood%'"
    - "Orchestrator has confirmed the ext_id block -4167101..-4167107 is free of collisions (probed with margin -4167110..-4167095)"
    - "Orchestrator has confirmed the on-disk migration MAX so the next STRUCTURAL migration number is safe (research snapshot: on-disk MAX 1186 → next 1187; ledger MAX 1178 is a TRAP — on-disk counter authoritative)"
    - "The current seated 7-member roster is re-confirmed live from sherwoodoregon.gov/government/city-council/ (Mayor Tim Rosener + Councilors Young/Brouse/Giles/Mays/Scott/Standke; Kim Young holds Council President title-on-seat; ALL 7 currently held by ELECTION — zero appointed, zero vacant seats), with special re-confirmation that Keith Mays is a PLAIN Councilor (a former Mayor/Council President) and Rosener is the current Mayor"
    - "sherwoodoregon.gov WAF status confirmed: NO-WAF for text AND headshots — all 7 portraits are static <img> tags directly curl-retrievable (600x600 JPEG each); the D-16 fallback chain is NOT expected to be needed for any official"
    - "The A4 assumption is resolved: City Attorney / Municipal Court Judge confirmed council-appointed (not elected) for Sherwood specifically (Dec-2024 org chart), gating the skip-all-judicial-* stance rule"
    - "The live compass topic_key list (is_live=true) is captured for stance ingestion (research: 44 live, 8 judicial-*)"
    - "All 7 headshot source URLs are re-downloaded to confirm continued availability (research found 7/7 HTTP 200, 600x600 JPEG); the SQUARE-source crop nuance (crop WIDTH down to 4:5) is noted for plan 03"
    - "The banner candidate images (Railroad St primary + Downtown alternate) are viewed directly and a compositional call on the 3.15:1 crop (native 4:3 → substantial vertical crop) is recorded for plan 05"
    - "Wave-0 checkpoint confirms the PURE AT-LARGE / directly-elected-Mayor (2-YEAR term) / PLAIN-TITLE (no positions, no wards, no X00xx geofences) model and the Youth-Advisory-Board non-council-seat finding (nothing to exclude) before any write"
    - "Open Question 2 preparation: note that the Oct 28 2025 seated-roster-on-vote-date must be confirmed before the housing-charter vote is attributed per-official in plan 04"
    - "Decision coverage — D-01: council structure ground-truthed from sherwoodoregon.gov, not memory; D-02: WHO-VOTES tie-breaker applied (whole city votes all seats → at-large branch, no custom geofences); D-05: Mayor's directly-elected citywide role (2-year term) re-confirmed; D-07: seated roster + exact body name verbatim + WAF/photo status captured; D-13: no non-voting dais seat exists to exclude"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-sherwood-wave0-probe.sql"
      provides: "The Wave-0 probe queries the orchestrator runs before any write"
      min_lines: 20
  key_links:
    - from: "probe.sql"
      to: "essentials.governments / essentials.politicians / essentials.geofence_boundaries / supabase_migrations.schema_migrations / inform.compass_topics"
      via: "SELECT probes"
      pattern: "essentials\\.(governments|politicians|geofence_boundaries)"
---

<objective>
Author the Wave-0 verification probe file for the City of Sherwood deep-seed and drive the
orchestrator-run DB + roster + web checks that MUST pass before any structural write. This plan gates
the entire phase: no government, roster, headshot, or stance migration may be authored or applied
until every probe below returns its expected result.

Purpose: Prevent the highest-risk failure modes for this phase. RESEARCH found the ROADMAP/CONTEXT
geo_id 4167450 is WRONG (0 rows — does not exist at all, a MORE severe failure mode than
Hillsboro/Tualatin's wrong-but-existing corrections) and the correct value is 4167100 — the
geofence-existence probe runs FIRST and asserts BOTH (4167100=1, 4167450=0). Other load-bearing
checks: confirm the greenfield status + ext_id block + migration counter (ledger 1178 is a trap;
on-disk MAX 1186 → next 1187 is authoritative); re-confirm the live 7-member roster (special attention
to Keith Mays being a PLAIN Councilor despite his former-Mayor history — RESEARCH Pitfall 3);
re-download all 7 headshot URLs (RESEARCH found the BEST sourcing in the milestone — 7/7 static <img>
tags, no WAF, no D-16 fallback needed) and note the SQUARE-source crop nuance; confirm the A4
assumption (City Attorney/Municipal Judge council-appointed) before skipping judicial-* topics; view
the banner candidates; and note the Open Question 2 seated-roster-on-vote-date check for plan 04
(RESEARCH Pitfalls 1-5; Assumptions A1-A10; Open Questions 1-4).
Output: `_tmp-sherwood-wave0-probe.sql` (gitignored `_tmp-*` helper) + a recorded pass/fail for each
Wave-0 gate, recorded in the SUMMARY for downstream plans 02-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO database
access, NO WebSearch, and NO Supabase MCP. The INLINE ORCHESTRATOR (main context) runs the probes via
`psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`, re-fetches
the roster from sherwoodoregon.gov (NO WAF — direct curl works), re-downloads the 7 headshot URLs,
confirms the A4 judge-appointment assumption, and views the banner candidate images. This probe file
is a gitignored `_tmp-*` helper — do NOT commit it. C:/EV-Accounts is a SEPARATE git repo — never cd
into it; use `git -C "C:/EV-Accounts"` if a commit is ever needed.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except in Probe E where a query genuinely reads that table), and
`photo_origin_url` out of comments in any file authored this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-sherwood-wave0-probe.sql</files>
  <read_first>
    - .planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md §"geo_id Verification — CORRECTED", §"Ext_id Block", §"Next Migration Number", §"Live Compass Topics", §"Wave 0 Gaps"
    - .planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md §"Key Casing / Structural Traps" and the geo_id note at the top
    - C:/EV-Accounts/backend/scripts/_tmp-forest-grove-wave0-probe.sql (the direct prior-phase probe-file analog — copy the labeled-probe A-G shape verbatim, adapting values; gitignored but present on disk)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-01-PLAN.md (the prior-phase probe-plan analog)
  </read_first>
  <action>
    Write labeled SELECT probes, each preceded by a `\echo` label line so the orchestrator reads
    results unambiguously. Do NOT wrap in a transaction (read-only); no DDL — SELECT/\echo only.
    Probe A (GEO_ID EXISTENCE — RUN FIRST): count rows in essentials.geofence_boundaries WHERE
    geo_id='4167100' AND mtfcc='G4110' (EXPECT 1 — the RESEARCH-CORRECTED value). Add a part A2: count
    rows WHERE geo_id='4167450' AND mtfcc='G4110' (EXPECT 0 — proves the ROADMAP/CONTEXT-stated value
    does not exist; if this returns >0 STOP and reconcile which is correct). Add a part A3: count rows
    WHERE geo_id='4111290' AND mtfcc='G5420' (EXPECT 1 — confirms the Sherwood School District 88J row
    is a distinct, non-colliding geofence for future Phase 184, not a collision risk here). Probe B:
    count essentials.districts WHERE geo_id='4167100' (expect 0 — no city districts yet). Probe C:
    count essentials.governments WHERE geo_id='4167100' OR name ILIKE '%sherwood%' (expect 0 —
    greenfield). Probe D: select external_id from essentials.politicians WHERE external_id BETWEEN
    -4167110 AND -4167095 (expect 0 rows — ext_id block -4167101..-4167107 free, probed with margin);
    add a second name-based scan selecting full_name WHERE full_name ILIKE ANY of the 7 roster names
    (Rosener/Young/Brouse/Giles/Mays/Scott/Standke — expect 0, confirms no legacy partial-seed by
    name). Probe E: select MAX(version::bigint) from supabase_migrations.schema_migrations WHERE
    version ~ '^[0-9]{1,6}$' (record it; the research snapshot is ledger MAX 1178 which is a TRAP — the
    next STRUCTURAL migration number = on-disk file MAX + 1, research snapshot on-disk MAX 1186 → next
    1187; the orchestrator cross-checks with an `ls` of the migrations dir). Probe F: select distinct
    state from essentials.districts WHERE geo_id='4159000' (Portland) to confirm lowercase 'or' is the
    correct casing for LOCAL/LOCAL_EXEC rows. Probe G: select topic_key from inform.compass_topics
    WHERE is_live=true ORDER BY topic_key — the authoritative live-topic list for the stance plan (04);
    also count how many begin with 'judicial-' (expect 8, all skipped). Include a header comment noting
    the file is a gitignored orchestrator-run helper. No fenced code in this action — the
    _tmp-forest-grove-wave0-probe.sql analog is the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-sherwood-wave0-probe.sql` and records each result</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-sherwood-wave0-probe.sql with labeled probes A-G
    - Probe A asserts geofence_boundaries geo_id='4167100' AND mtfcc='G4110' returns 1; A2 asserts geo_id='4167450' G4110 returns 0 (the wrong stated value); A3 confirms geo_id='4111290' G5420 school-district row is distinct
    - Probe C queries governments by geo_id='4167100' OR name ILIKE '%sherwood%'
    - Probe D queries the exact ext_id range -4167110..-4167095 AND a name-based scan for the 7 roster names
    - Probe E reads MAX(version::bigint) from the migration ledger with a numeric-version filter
    - Probe F reads distinct districts.state for geo_id='4159000' (expect 'or' lowercase)
    - Probe G selects topic_key WHERE is_live=true and counts judicial-* topics
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE); not wrapped in a transaction
  </acceptance_criteria>
  <done>Probe file authored with all queries and correct literal values (CORRECTED geo_id 4167100, wrong-value check 4167450, ext_id range -4167110..-4167095).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes, re-fetches roster, re-downloads headshots, confirms all gates</name>
  <what-built>The Wave-0 probe file. The orchestrator now executes it against the live DB, re-fetches the live roster directly from sherwoodoregon.gov (no WAF), re-downloads all 7 headshot URLs, confirms the A4 judge-appointment assumption, and views the banner candidate images.</what-built>
  <how-to-verify>
    A. Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-sherwood-wave0-probe.sql`
       (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
       1. Probe A returns geofence(4167100, G4110)=1; A2 returns geofence(4167450, G4110)=0. If A≠1 →
          STOP, re-run a name search before proceeding. If A2>0 → STOP, reconcile which geo_id is
          correct. A3 confirms the 4111290 school-district row is a separate G5420 row (not a collision).
       2. Probe B returns districts=0. If >0 → STOP, districts already exist (not greenfield).
       3. Probe C returns 0 (no existing Sherwood gov). If >0 → STOP, phase already partly seeded —
          switch to reconcile.
       4. Probe D returns 0 rows on BOTH the range scan and the name scan (ext_id block free, no legacy
          rows). If any row → shift the block and record the new range for plans 02/03/04.
       5. Probe E: cross-check against `ls C:/EV-Accounts/backend/migrations | grep -E '^[0-9]+_' | sort -t_ -k1 -n | tail`.
          Next STRUCTURAL migration = on-disk file MAX + 1 (research snapshot: on-disk 1186 → 1187;
          ledger MAX 1178 is a TRAP — do NOT use it). Record the confirmed number for plan 02.
       6. Probe F returns 'or' (lowercase) — the casing plan 02 must use for LOCAL/LOCAL_EXEC districts.
       7. Probe G returns the live topic_key list (research: 44 live, 8 judicial-*) — save it verbatim for plan 04.
    B. Re-fetch the current roster directly from sherwoodoregon.gov/government/city-council/ (NO WAF —
       plain curl returns 200). Confirm the 7-member roster is unchanged since 2026-07-03: Mayor Tim
       Rosener (directly elected citywide, 2-year term, expires Jan 2027); Councilor Kim Young (Council
       President); Councilor Renee Brouse; Councilor Taylor Giles; Councilor Keith Mays; Councilor Doug
       Scott; Councilor Dan Standke. Confirm ALL 7 are currently held by ELECTION (zero appointed, zero
       vacant). SPECIAL CHECK (RESEARCH Pitfall 3): re-confirm Keith Mays is a PLAIN Councilor today
       (NOT Mayor, NOT Council President) despite his former-Mayor 2018-2022/2005-2012 and former
       Council-President 2001-2004 history; Rosener is the current Mayor and Young is the current
       Council President. Run a FRESH WebSearch for any Sherwood council resignation/appointment after
       2026-07-03. If any member changed → re-plan the roster in plan 02.
    C. Headshot retrievability (RESEARCH §"Headshot Sources — 7/7 CONFIRMED"): sherwoodoregon.gov's
       council page embeds all 7 headshots as static <img> tags directly retrievable via plain curl.
       Re-download all 7 URLs (wp-content/uploads/2025/02/<firstname>-<lastname>-600.jpg for
       tim-rosener, kim-young, renee-brouse, taylor-giles, keith-mays, doug-scott, daniel-standke) and
       confirm each returns HTTP 200 as a valid 600x600 JPEG. Note the SQUARE-source crop nuance for
       plan 03: crop WIDTH down to 4:5 (600→480 centered on face), not the usual height crop, then
       upscale to 600x750. If any URL now 404s, note it and fall to the D-16 chain for that official
       only — but a full 7/7 is expected. Record per-official confirmations for plan 03.
    D. Confirm the structural decision holds (RESEARCH Form-of-Government): PURE AT-LARGE with a
       directly-elected Mayor + 6 Councilors, every seat voted citywide → Beaverton/Tualatin/Forest
       Grove shape (1 LOCAL_EXEC Mayor district + 1 LOCAL at-large district on geo_id 4167100), NO
       ward/X00xx geofences, PLAIN 'Mayor'/'Councilor' titles (Tigard/Forest Grove convention — NOT
       numbered positions, NOT wards). NOTE THE 2-YEAR MAYOR TERM (new this milestone — every other
       WashCo Mayor is 4-year; affects the migration comment/label only, not the shape). Confirm the
       Youth Advisory Board is a SEPARATE advisory board (not a council-dais seat) — nothing to exclude
       from the 7-member roster (D-13 satisfied cleanly). CONFIRM THE A4 ASSUMPTION: the Dec-2024 org
       chart shows City Attorney (Sebastian Tapia, Interim) and Municipal Court Judge (Jack Morris)
       connecting directly to Council — both council-APPOINTED, not elected — gating the
       skip-all-8-judicial-* stance rule for plan 04. If any of these is overturned → STOP and re-plan.
    E. View the banner candidate images directly (RESEARCH §"Community Banner" / A9 / Wave-0 Gap): the
       primary "Railroad St - panoramio.jpg" (Old Town commercial street, CC BY 3.0, dreid1987) and the
       alternate "Downtown - panoramio - dreid1987.jpg" (quieter historic-cottage street, CC BY 3.0).
       Both are native 4:3 (~1.33:1) needing a substantial (~58%) vertical crop to the pipeline's
       3.15:1 target — record a compositional call (which crops acceptably; try --vertical-anchor
       0.4-0.55 to preserve storefronts/crosswalk) for plan 05. Reject the documented failure-mode
       candidates (aerials Sherwood DJIMini 1/2, single-building Library close-up, generic Sunset Blvd
       overlook, no-mill-in-frame Old mill, too-low-res coffee company) per D-14.
    F. Note Open Question 2 for plan 04: before attributing the Oct 28 2025 unanimous housing-charter
       vote per-official, the exact seated roster on that date must be confirmed (all 7 were very likely
       seated by then — Young/Brouse/Standke since Jan 2025 — but the meeting minutes/packet PDF should
       be checked). Record this as a plan-04 prerequisite.
  </how-to-verify>
  <acceptance_criteria>
    - All probe gates recorded with pass/fail
    - geo_id confirmed: 4167100 (G4110) returns 1; 4167450 (G4110) returns 0 (wrong stated value); 4111290 (G5420) is a distinct non-colliding row
    - Confirmed ext_id block (default -4167101..-4167107 unless probe D forced a shift); no legacy rows by name
    - Confirmed next structural migration number (default 1187) via ledger + on-disk `ls` cross-check (ledger 1178 recognized as a trap)
    - Live topic_key list captured for plan 04; 8 judicial-* noted for exclusion
    - Live roster re-confirmed (7 members; ALL is_appointed=false; Young Council President title-on-seat; Mays a PLAIN Councilor not Mayor/CP; Rosener the Mayor) via direct sherwoodoregon.gov fetch + fresh WebSearch
    - All 7 headshot URLs re-downloaded HTTP 200 as 600x600 JPEG; square-source crop nuance noted for plan 03 (partial N/7 would be an unexpected regression, not a planned gap)
    - A4 confirmed: City Attorney/Municipal Court Judge council-appointed → skip all 8 judicial-* topics
    - Pure at-large / directly-elected-Mayor (2-YEAR term) / PLAIN-title decision confirmed; Youth Advisory Board non-seat finding confirmed; no X00xx geofences
    - Banner candidate images viewed; 3.15:1 crop call recorded for plan 05 (Railroad St primary, Downtown alternate)
    - Open Question 2 seated-roster-on-vote-date check recorded as a plan-04 prerequisite
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |
| operator → external web | Roster re-fetch, headshot re-download, and banner-image viewing from sherwoodoregon.gov / Wikimedia (no WAF) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-01 | Tampering | probe.sql executed on prod DB | accept | Probes are read-only SELECTs; no write path; no user input |
| T-181-02 | Spoofing | trusting the stated geo_id 4167450 (which does NOT exist) would seed a phantom government routing nothing | mitigate | Probe A runs FIRST and asserts 4167100 (G4110) returns exactly 1 AND 4167450 returns 0 before any write — the standing habit that catches the phantom-seed class (third wrong stated geo_id this milestone) |
| T-181-03 | Spoofing | stale roster (member change since research) | mitigate | Direct sherwoodoregon.gov re-fetch (no WAF) + fresh WebSearch before seed |
| T-181-04 | Spoofing | Keith Mays's former Mayor/Council-President titles misattributed to his current PLAIN-Councilor seat | mitigate | Wave-0 explicitly re-confirms Mays is a plain Councilor today; the pitfall flag is carried into plans 02/04 |
| T-181-05 | Information Disclosure | topic_key list from inform.compass_topics | accept | Public compass topic keys; no PII |
| T-181-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- geo_id 4167100 (G4110) existence confirmed AND 4167450 confirmed absent before any structural write is authored.
- The A4 judge-appointment assumption is resolved before the judicial-* skip rule is locked.
- Any failed gate blocks plans 02-05 until resolved.
</verification>

<success_criteria>
- geo_id 4167100 confirmed present (4167450 confirmed absent); no duplicate Sherwood gov; districts
  absent; ext_id block free; next migration number confirmed (1187, ledger 1178 recognized as trap);
  live topic_key list captured; roster re-confirmed via direct fetch (Mays plain Councilor); all 7
  headshot URLs re-downloaded HTTP 200 (square-source nuance noted); A4 judge-appointment confirmed;
  pure at-large + directly-elected-Mayor (2-year) + plain-title + Youth-Board-non-seat decisions
  confirmed; banner candidates viewed; Open Question 2 recorded for plan 04.
</success_criteria>

<output>
Create `.planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md` when done, recording the
confirmed geo_id (4167100), ext_id block, next structural migration number, live topic_key list,
re-confirmed 7-member roster (all elected, Young Council President, Mays plain Councilor, Rosener
Mayor), per-official headshot-download confirmations, the A4 judge-appointment confirmation, the banner
crop call, and the Open Question 2 plan-04 prerequisite for downstream plans 02-05.
</output>
