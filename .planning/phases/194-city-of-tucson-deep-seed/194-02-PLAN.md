---
phase: 194-city-of-tucson-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["194-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1296_city_of_tucson.sql
autonomous: false
requirements: [TUC-01]
must_haves:
  truths:
    - "A greenfield 'City of Tucson, Arizona, US' government (type='City', state='AZ', geo_id='0477000') exists"
    - "One 'City Council' chamber (official_count=7) holds exactly 7 offices: an at-large Mayor + 6 by-ward council members"
    - "A NEW LOCAL_EXEC districts row (geo_id='0477000', mtfcc='G4110', state='az') is created for the Mayor — it did NOT exist before this phase (Phase 190 wrote only the geofence)"
    - "The Mayor office links to the new LOCAL_EXEC/G4110/0477000 district; each ward office links to its OWN LOCAL X0020 district — never a bare geo_id join"
    - "Roster is Romero(Mayor)/Santa Cruz(W1)/Cunningham(W2)/Dahl(W3)/Lee(W4)/Barajas(W5)/Schubert(W6), all Democratic (party stored, never displayed); no departed member (Fimbres/Kozachik) is seeded"
    - "The Vice Mayor is a TITLE ANNOTATION on the Ward 1 seat (title='Council Member, Ward 1 (Vice Mayor)'); the other five wards carry plain 'Council Member, Ward N'. Exactly 7 offices, no separate Vice Mayor seat"
    - "The structural migration is applied to production and registered in the ledger; 7 politician UUIDs captured for Plans 03/04"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1296_city_of_tucson.sql"
      provides: "government + City Council chamber + 6 LOCAL ward districts + 1 NEW LOCAL_EXEC Mayor district + 7 politicians + 7 offices (W1 Vice-Mayor-annotated) + office_id backfill + pre-flight + post-verify DO gates + ledger registration"
      contains: "City Council"
  key_links:
    - from: "essentials.offices (Mayor)"
      to: "essentials.districts (LOCAL_EXEC / G4110 / 0477000)"
      via: "district_id join scoped by district_type='LOCAL_EXEC' AND mtfcc='G4110' AND state='az'"
      pattern: "LOCAL_EXEC"
    - from: "essentials.offices (6 ward members)"
      to: "essentials.districts (X0020 LOCAL)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='X0020' AND state='az'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'City Council'"
      to: "essentials.governments 'City of Tucson, Arizona, US'"
      via: "government_id subquery scoped by name"
      pattern: "City of Tucson, Arizona, US"
---

<objective>
Author and apply the structural migration that seeds the City of Tucson as a greenfield city
government (its own `essentials.governments` row at geo_id `0477000`), a single `City Council`
chamber, the district rows the offices attach to, and 7 politicians/offices (ext_id
`-4008001..-4008007`): an at-large **Mayor** (LOCAL_EXEC) plus 6 by-ward council members (LOCAL).
Greenfield — no City of Tucson government row exists today.

CRITICAL DELTA from Pima (Pitfall 5): the Mayor's at-large boundary geofence `0477000` (G4110) is
live from Phase 190, but `essentials.districts` has ZERO rows for it. This migration must create ONE
NEW `LOCAL_EXEC` districts row (geo_id='0477000', mtfcc='G4110', state='az') in addition to the 6 new
`LOCAL` ward districts (X0020) — the Mayor's office cannot attach to anything otherwise. No
loader/geometry work is needed for the Mayor; only this one districts INSERT reusing the existing
Phase-190 geofence.

CRITICAL (D-05): exactly 7 offices — no 8th "Vice Mayor" office. The Vice Mayor (research: Lane Santa
Cruz, Ward 1) is surfaced as a TITLE ANNOTATION on her existing Ward 1 seat
(`title='Council Member, Ward 1 (Vice Mayor)'`), the SAME locked shape Phase 193 used for the Pima
Chair. `role_canonical` stays NULL on all 7 offices — the annotation lives in `title`. The Vice Mayor
rotates annually (council-selected each December) — re-confirm the current holder at the Task 2 roster
checkpoint before applying. D-04: all 7 are recorded as Democratic in `politicians.party` (Tucson runs
partisan municipal elections) but party is NEVER displayed (antipartisan, no special-casing).

Purpose: Satisfies the government/roster half of ROADMAP success criterion #1 (Mayor + 6 ward members,
election method verified). The G4110→(LOCAL,LOCAL_EXEC) mapping + generic X% catch-all in
`essentialsService.ts` already route both the Mayor and the 6 wards with NO code change, so applying
this migration makes every Tucson address resolve to its ONE ward member + the city-wide Mayor. Plans
03 (headshots) and 04 (stances) consume the 7 politician UUIDs captured here.
Output: `C:/EV-Accounts/backend/migrations/1296_city_of_tucson.sql` (structural, registered), applied
to production, with the 7 UUIDs recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-city-of-tucson-deep-seed/194-CONTEXT.md
@.planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md
@.planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md
@.planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md
@.planning/phases/194-city-of-tucson-deep-seed/194-01-SUMMARY.md

<execution_note>
Depends on Plan 01: the 6 X0020 geofences MUST be live (the migration's pre-flight DO block RAISE
EXCEPTIONs if <6 exist). `gsd-executor` authors the `.sql` file only. Task 2 (BLOCKING roster-currency
checkpoint + apply + UUID capture) and Task 3 (post-apply assertions) are ORCHESTRATOR-RUN via
`psql -f` against `C:/EV-Accounts/backend/.env` `DATABASE_URL`. A migration authored but not applied is
a false positive — the apply MUST happen before Plans 03/04. Commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (live-verified in 194-RESEARCH.md / 194-PATTERNS.md):
- essentials.governments (id, name, type, state, city, geo_id): insert 'City of Tucson, Arizona, US',
  type='City', state='AZ' (UPPERCASE free-text), city=NULL, geo_id='0477000'. Greenfield. Idempotent
  WHERE NOT EXISTS on name.
- essentials.chambers (id, name, name_formal, government_id, official_count): name='City Council',
  name_formal='Tucson City Council', official_count=7 (Mayor + 6 wards), government_id via name
  subquery. The auto-generated `slug` column must NEVER appear in the INSERT column list. Idempotent
  WHERE NOT EXISTS on (name, government_id).
- essentials.districts (id, district_type, state, geo_id, label, mtfcc) — TWO kinds:
  (1) 6 rows district_type='LOCAL', state='az' (lowercase), geo_id='tucson-az-ward-1..6',
      label='Tucson Ward N', mtfcc='X0020'. Idempotent WHERE NOT EXISTS on (geo_id, district_type, state).
  (2) 1 NEW row district_type='LOCAL_EXEC', state='az' (lowercase), geo_id='0477000',
      label='City of Tucson (Mayor)', mtfcc='G4110'. Idempotent WHERE NOT EXISTS on
      (geo_id, district_type, state). Reuses the ALREADY-LIVE Phase-190 geofence — no geometry work.
      Use `label`, NOT name_formal (no such column).
- essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed,
  is_vacant, is_incumbent, external_id, office_id): ext_id -4008001..-4008007; external_id UNIQUE
  (ON CONFLICT (external_id) DO NOTHING). party='Democratic' on all 7 (stored, NEVER displayed —
  antipartisan). is_active=true, is_incumbent=true, is_vacant=false, is_appointed=false on all 7.
- essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state,
  is_appointed_position, is_vacant, role_canonical): representing_state='AZ' (UPPERCASE),
  is_appointed_position=false, role_canonical=NULL on all 7. Guard on (district_id, politician_id).
- ext_id → seat → title (RESEARCH roster, 2026-07-10):
    -4008001 Regina Romero      → Mayor          → title='Mayor'                              (LOCAL_EXEC / G4110 / 0477000)
    -4008002 Lane Santa Cruz    → Ward 1 (VM)    → title='Council Member, Ward 1 (Vice Mayor)' (LOCAL / X0020 / tucson-az-ward-1)
    -4008003 Paul Cunningham    → Ward 2         → title='Council Member, Ward 2'             (tucson-az-ward-2)
    -4008004 Kevin Dahl         → Ward 3         → title='Council Member, Ward 3'             (tucson-az-ward-3)
    -4008005 Nikki Lee          → Ward 4         → title='Council Member, Ward 4'             (tucson-az-ward-4)
    -4008006 Selina Barajas     → Ward 5         → title='Council Member, Ward 5'             (tucson-az-ward-5)
    -4008007 Miranda Schubert   → Ward 6         → title='Council Member, Ward 6'             (tucson-az-ward-6)
- CRITICAL geo_id collision guard (Pitfall 7-adjacent): 0477000 also appears in geofence_boundaries
  (state stored as FIPS '04', an unrelated convention). Every office↔district join MUST scope by
  district_type + mtfcc + state='az', never a bare geo_id. The Mayor join scopes
  LOCAL_EXEC/G4110/az; each ward join scopes LOCAL/X0020/az.
- One-chamber-two-district-types precedent (live-confirmed): Beaverton/La Verne hold an at-large
  LOCAL_EXEC Mayor + LOCAL council offices in a single 'City Council' chamber.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the structural City of Tucson migration</name>
  <files>C:/EV-Accounts/backend/migrations/1296_city_of_tucson.sql</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md (lines 93-236 — the full structural pattern: government/chamber INSERT, pre-flight geofence assertion, the 6× LOCAL ward district INSERT, the NEW LOCAL_EXEC Mayor district INSERT (Pitfall 5, no Pima equivalent), the WITH ins_p...INSERT INTO offices per-official block, the Vice Mayor title-annotation on Ward 1, office_id backfill, post-verify DO gate, ledger registration)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Summary roster; § Architecture Patterns 1 & 2; § Common Pitfalls 5, 7, 9; § Assumptions Log A1/A4)
    - .planning/phases/194-city-of-tucson-deep-seed/194-CONTEXT.md (D-02 Mayor at-large; D-04 partisan stored-never-shown; D-05 Vice Mayor = title annotation on the sitting Ward 1 member)
    - C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql (PRIMARY analog — read in full: government/chamber/district/office INSERT shapes, pre-flight + post-verify DO gates, the '(Chair)' title-annotation gate (f) at lines 427-453 — replicate for '(Vice Mayor)' on Ward 1; ADD the new LOCAL_EXEC district block Pima never needed)
  </read_first>
  <action>
    First run `ls C:/EV-Accounts/backend/migrations | sort` to confirm on-disk MAX (research saw 1295,
    the ut_sd18 cull). Name this file disk-MAX+1 (provisionally `1296_city_of_tucson.sql`). If disk MAX
    drifted, use actual disk-MAX+1 and note the deviation (Pitfall 9: disk-MAX authoritative, not
    ledger-MAX which is 1288).

    Author a STRUCTURAL migration wrapped in BEGIN/COMMIT. Pre-flight DO block: assert
    `COUNT(*) >= 6` in essentials.geofence_boundaries WHERE state='az' AND mtfcc='X0020' — RAISE
    EXCEPTION 'run load-tucson-ward-boundaries.ts first' otherwise (gates on Plan 01).

    Insert the greenfield government 'City of Tucson, Arizona, US' (type='City', state='AZ' uppercase,
    city=NULL, geo_id='0477000') idempotently (WHERE NOT EXISTS on name). Insert the 'City Council'
    chamber (name_formal='Tucson City Council', official_count=7) scoped to that government's id via
    name subquery, idempotent on (name, government_id). Do NOT list the auto-generated `slug` column.

    Insert district rows — TWO kinds:
      (1) 6 LOCAL ward rows (district_type='LOCAL', state='az' lowercase, geo_id='tucson-az-ward-1..6',
          label='Tucson Ward N', mtfcc='X0020'), each idempotent WHERE NOT EXISTS on
          (geo_id, district_type, state).
      (2) ONE NEW LOCAL_EXEC Mayor row (district_type='LOCAL_EXEC', state='az' lowercase,
          geo_id='0477000', label='City of Tucson (Mayor)', mtfcc='G4110'), idempotent WHERE NOT EXISTS
          on (geo_id, district_type, state). This reuses the existing Phase-190 geofence — NO geometry
          work. Pitfall 5: without this row the Mayor's office attaches to nothing.

    Seed 7 officials using the `WITH ins_p AS (INSERT INTO politicians ... RETURNING id) INSERT INTO
    offices ... FROM districts d CROSS JOIN ins_p p WHERE ...` block, one per official. Scoping:
      - Mayor Romero (-4008001) → title='Mayor'; office joins the LOCAL_EXEC row:
        `d.geo_id='0477000' AND d.district_type='LOCAL_EXEC' AND d.state='az' AND d.mtfcc='G4110'`.
      - Ward 1 Santa Cruz (-4008002) → title='Council Member, Ward 1 (Vice Mayor)'  ← Vice Mayor
        title annotation (D-05); office joins `d.geo_id='tucson-az-ward-1' AND d.district_type='LOCAL'
        AND d.state='az' AND d.mtfcc='X0020'`.
      - Ward 2 Cunningham (-4008003) → 'Council Member, Ward 2' (tucson-az-ward-2)
      - Ward 3 Dahl (-4008004) → 'Council Member, Ward 3' (tucson-az-ward-3)
      - Ward 4 Lee (-4008005) → 'Council Member, Ward 4' (tucson-az-ward-4)
      - Ward 5 Barajas (-4008006) → 'Council Member, Ward 5' (tucson-az-ward-5)
      - Ward 6 Schubert (-4008007) → 'Council Member, Ward 6' (tucson-az-ward-6)
    Each ward office join scopes district_type='LOCAL' AND mtfcc='X0020' AND state='az' plus its
    geo_id — NEVER a bare geo_id. All 7 offices: representing_state='AZ' uppercase,
    is_appointed_position=false, role_canonical=NULL (the Vice Mayor distinction lives in the Ward 1
    `title` string). Guard each office INSERT on `NOT EXISTS (... o.district_id=d.id AND
    o.politician_id=p.id)`. Politicians: party='Democratic' (all 7 — stored, never displayed, D-04),
    is_active=true, is_incumbent=true, is_vacant=false, is_appointed=false on all 7.

    Only the Ward 1 office carries the `(Vice Mayor)` marker — the other five wards and the Mayor carry
    NO `(Vice Mayor)` suffix.

    office_id backfill: UPDATE politicians SET office_id=o.id FROM offices o WHERE o.politician_id=p.id
    AND p.external_id BETWEEN -4008007 AND -4008001 AND p.office_id IS NULL.

    Post-verify DO gate (RAISE EXCEPTION → rollback unless ALL hold): (a) exactly 1 government named
    'City of Tucson, Arizona, US'; (b) exactly 7 offices under the 'City Council' chamber — 1 on the
    LOCAL_EXEC/G4110/0477000 district and 6 on the LOCAL/X0020 ward districts; (c) each of those 7
    districts holds exactly 1 office (GROUP BY district_id HAVING count(*)<>1 → 0 rows); (d) EXACTLY 1
    office carries a Vice-Mayor annotation (`o.title LIKE '%(Vice Mayor)%'`) AND that office's
    politician has external_id=-4008002 (Santa Cruz, Ward 1) — no other office may carry it; (e)
    section-split: 0 offices attached under any government OTHER than City of Tucson via these 7
    districts. (Pima's is_appointed gate does NOT apply — all 7 Tucson officials are elected, not
    appointed — omit it.)

    Register OUTSIDE BEGIN/COMMIT: INSERT INTO supabase_migrations.schema_migrations (version, name)
    VALUES ('1296','city_of_tucson') ON CONFLICT (version) DO NOTHING (substitute actual disk-MAX+1).
    Do NOT invent politician UUIDs — offices resolve politician_id via the ins_p CTE at apply time.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1296_city_of_tucson.sql"; test -f "$F" && grep -qF "City Council" "$F" && grep -qF "City of Tucson, Arizona, US" "$F" && grep -qF "'LOCAL_EXEC'" "$F" && grep -qF "G4110" "$F" && grep -qF "'0477000'" "$F" && grep -qF "Council Member, Ward 1 (Vice Mayor)" "$F" && grep -qF "Council Member, Ward 2" "$F" && grep -qF "Council Member, Ward 6" "$F" && grep -qF "title = 'Mayor'" "$F" || grep -qF "'Mayor'" "$F" && ! grep -qF "Council Member, Ward 2 (Vice Mayor)" "$F" && ! grep -qF "Council Member, Ward 6 (Vice Mayor)" "$F" && ! grep -qiE "role_canonical\s*=\s*'" "$F" && [ "$(grep -c 'o.district_id = d.id AND o.politician_id = p.id' "$F")" = "7" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1296).
    - Grep confirms `City Council` and `City of Tucson, Arizona, US` present; government type='City', geo_id='0477000'.
    - Grep confirms a NEW `LOCAL_EXEC` districts INSERT with geo_id='0477000', mtfcc='G4110', state='az' (Pitfall 5 — the row Pima never needed).
    - Grep confirms 6 LOCAL ward district rows (geo_id tucson-az-ward-1..6, mtfcc='X0020', lowercase state='az').
    - Grep count of the office↔district guard `o.district_id = d.id AND o.politician_id = p.id` is exactly 7 (no 8th office).
    - Mayor office joins LOCAL_EXEC/G4110/0477000; each ward office joins LOCAL/X0020/tucson-az-ward-N — never a bare geo_id.
    - Vice Mayor modeling (D-05): Ward 1 office title='Council Member, Ward 1 (Vice Mayor)'; the other five wards + Mayor carry NO `(Vice Mayor)` marker; `role_canonical` NULL for all 7.
    - Grep confirms 7 external_id literals -4008001..-4008007; party='Democratic' on all 7 (stored, never displayed).
    - Pre-flight DO block asserts >=6 X0020 geofences; post-verify DO gate present with assertions (a-e) — including exactly-one-Vice-Mayor-annotation on the -4008002 (Ward 1) seat.
    - Ledger-registration line `('1296'` (or actual number) present OUTSIDE COMMIT.
  </acceptance_criteria>
  <done>Structural migration authored: greenfield City of Tucson government + City Council chamber + 6 LOCAL X0020 ward districts + 1 NEW LOCAL_EXEC/G4110 Mayor district + 7 offices/politicians (Ward 1 carries the Vice Mayor title annotation), backfill, pre-flight + post-verify gates (incl. exactly-one-Vice-Mayor on Ward 1), ledger registration — ready to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING roster-currency re-verification + apply structural migration (Wave-0 gate #2, ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Summary roster table + § Assumptions Log A1/A4 — party + Vice-Mayor-rotation residual risk; "valid until 7 days for roster-currency"; § Common Pitfall 2 — the Vice Mayor conflict resolved to Santa Cruz/Ward 1)
    - .planning/phases/194-city-of-tucson-deep-seed/194-CONTEXT.md (Claude's Discretion — blocking roster-currency human-verify before seeding; D-03 election-method confirmation; vacant seat → vacant office, never a departed member)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor file action. The operator re-confirms the current Mayor + 6 ward members + who holds Vice Mayor (the Vice Mayor drives the Ward-1-vs-other title annotation); on approval the ORCHESTRATOR applies migration 1296 via `psql -f` and captures the 7 politician UUIDs. This is the second of the two locked pre-seed gates. See &lt;how-to-verify&gt;.</action>
  <what-built>
    Migration 1296 (Task 1) is authored but NOT applied. This checkpoint confirms the 7-member roster
    + current Vice Mayor are still current before applying to production (mcp__supabase-local IS live
    prod). RESEARCH (2026-07-10) confirmed Romero (Mayor) + Santa Cruz/Cunningham/Dahl/Lee/Barajas/
    Schubert (Wards 1-6) with Santa Cruz as Vice Mayor (2nd consecutive year, re-voted Dec 2025), and
    Barajas/Schubert newly seated Dec 2025 (replacing Fimbres/Kozachik — who must NOT be seeded). The
    Vice Mayor rotates annually (council-selected each December, "valid until ~7 days for roster") — a
    rotation since the research date would move the `(Vice Mayor)` suffix to a different ward seat, so
    confirm the current Vice Mayor here so the annotation lands on the right seat. The live GIS `NAME`
    attribute is a free authoritative cross-check.
  </what-built>
  <how-to-verify>
    1. Cross-check the current roster against the live Tucson-wards GIS `NAME` field (one curl:
       `https://gisdata.pima.gov/arcgis1/rest/services/GISOpenData/Boundaries2/MapServer/3/query?where=1%3D1&outFields=WARD,NAME&returnGeometry=false&f=json`)
       plus a current City of Tucson / news source, confirming: Mayor Regina Romero; Ward 1 Lane Santa
       Cruz (Vice Mayor); Ward 2 Paul Cunningham; Ward 3 Kevin Dahl; Ward 4 Nikki Lee; Ward 5 Selina
       Barajas; Ward 6 Miranda Schubert.
    2. Confirm no resignation/appointment/Vice-Mayor re-vote has landed since 2026-07-10. If the Vice
       Mayor rotated to a different ward member, move the `(Vice Mayor)` annotation to THAT ward's seat
       before apply (only one office may carry it). If a seat is genuinely vacant, seed a vacant office
       (is_vacant=true), never a departed member (Fimbres/Kozachik are gone — do not seed them).
    3. Confirm the D-03 election-method characterization still holds (partisan ward primary → partisan
       citywide general; Mayor directly elected at-large) — informational, does not change the seed shape.
    4. On approval, the ORCHESTRATOR applies via
       `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1296_city_of_tucson.sql`, captures the
       7 UUIDs (`SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id
       BETWEEN -4008007 AND -4008001 ORDER BY external_id`) into 194-02-SUMMARY.md for Plans 03/04, and
       commits the migration via `git -C "C:/EV-Accounts"`.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the live roster + Vice Mayor match the seeded roster (or supplies the exact delta to patch before apply — including moving the Vice Mayor annotation if it rotated).
    - Migration applies with the in-transaction post-verify DO gate passing (no RAISE EXCEPTION → COMMIT), including exactly-one-Vice-Mayor annotation on the confirmed holder's ward seat.
    - `psql -tAc "SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1296'"` returns 1 (structural registered).
    - The 7 politician UUIDs captured into 194-02-SUMMARY.md (external_id → UUID → full_name) for Plans 03/04.
    - Migration committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live roster + Vice Mayor are confirmed and the migration is applied + UUIDs captured, or describe the roster/Vice-Mayor delta to patch first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply structural assertions (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB assertions, no files modified</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md (Per-Task Verification Map — government+roster + routing rows: 1 govt + 1 chamber + 7 offices, Vice Mayor annotation, scoped joins)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfall 5 — the new LOCAL_EXEC row; § Common Pitfall 7 — the 0477000 casing/collision the scoped join guards against)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). After apply, run seed assertions via
    `psql "$DATABASE_URL" -tAc` and record in 194-02-SUMMARY.md. Assert: (a) exactly 1 government
    'City of Tucson, Arizona, US' at geo_id='0477000', type='City'; (b) exactly 7 offices under the
    'City Council' chamber of that government — 1 on the LOCAL_EXEC/G4110/0477000 district and 6 on the
    LOCAL/X0020 ward districts; (c) each of the 7 districts holds exactly 1 office (GROUP BY
    district_id HAVING count(*)<>1 → 0 rows); (d) the new LOCAL_EXEC/G4110/0477000/az districts row
    exists (Pitfall 5); (e) DISTINCT district state on the 7 linked districts = only 'az' (lowercase);
    (f) section-split: 0 offices reachable from these 7 districts under any government other than City
    of Tucson; (g) Vice Mayor annotation: exactly 1 of the 7 offices has `title LIKE '%(Vice Mayor)%'`
    AND it is the office whose politician external_id=-4008002 (Santa Cruz, Ward 1), and no other
    office carries it. Any mismatch blocks the phase — do NOT proceed to Plan 03.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='City of Tucson, Arizona, US' AND geo_id='0477000' AND type='City')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE (d.geo_id LIKE 'tucson-az-ward-%' AND d.district_type='LOCAL' AND d.state='az' AND d.mtfcc='X0020') OR (d.geo_id='0477000' AND d.district_type='LOCAL_EXEC' AND d.state='az' AND d.mtfcc='G4110'))=7 AND (SELECT count(*) FROM essentials.districts WHERE geo_id='0477000' AND district_type='LOCAL_EXEC' AND state='az' AND mtfcc='G4110')=1 AND NOT EXISTS (SELECT 1 FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE (d.mtfcc='X0020' OR (d.geo_id='0477000' AND d.mtfcc='G4110')) GROUP BY o.district_id HAVING count(*)<>1) AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc IN ('X0020','G4110') AND o.title LIKE '%(Vice Mayor)%')=1 AND (SELECT p.external_id FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.politicians p ON p.id=o.politician_id WHERE o.title LIKE '%(Vice Mayor)%')=-4008002"</automated>
  </verify>
  <acceptance_criteria>
    - Greenfield City of Tucson government count = 1 (geo_id 0477000, type City).
    - 7 offices under 'City Council' — 1 on LOCAL_EXEC/G4110/0477000, 6 on LOCAL/X0020 wards.
    - New LOCAL_EXEC/G4110/0477000/az districts row exists (Pitfall 5).
    - Each of the 7 districts holds exactly 1 office (no HAVING count<>1 violation).
    - DISTINCT linked-district state = only 'az'.
    - Section-split query returns 0 rows (no office leaked under a non-Tucson government).
    - Vice Mayor annotation: exactly 1 office carries `(Vice Mayor)` and it is the Ward-1/-4008002 (Santa Cruz) seat; no other office carries it.
    - The combined boolean SELECT returns `t`.
  </acceptance_criteria>
  <done>Live production confirms the greenfield City of Tucson government, 7 offices (at-large Mayor on the new LOCAL_EXEC district + 6 ward members on LOCAL X0020 districts, 1 per district), the Vice Mayor title annotation on the Ward-1/Santa Cruz seat only, correct casing, clean section-split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | Hand-authored static SQL crosses into live `essentials` via `psql -f` (service-role) |
| Plan 01 X0020 geofences + Phase-190 G4110 geofence → new offices | Office↔district joins consume the ward + city geofences; wrong casing/type or a bare-geo_id collision silently mislinks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-COLL | Tampering | office↔district WHERE vs the 0477000 multi-use geo_id | mitigate | Every join scopes district_type + mtfcc + state='az' (Mayor: LOCAL_EXEC/G4110; wards: LOCAL/X0020); migration never joins bare geo_id; post-verify asserts 7 offices on the correct districts only |
| T-194-EXEC | Tampering | missing LOCAL_EXEC districts row (Pitfall 5) | mitigate | Migration creates the NEW LOCAL_EXEC/G4110/0477000/az row; post-verify gate (d) + Task 3 assert it exists and holds exactly the Mayor office |
| T-194-SPLIT | Tampering | office↔chamber↔government section-split | mitigate | Chamber scoped to the Tucson government id subquery; post-verify DO gate section-split assertion → 0 rows or the transaction rolls back; Plan 01 already confirmed clean geofence nesting |
| T-194-ROST | Spoofing | roster rows (departed member / stale seat) | mitigate | Seed only the 7 current officials; departed Fimbres/Kozachik never inserted; Task 2 BLOCKING human roster-currency check + live GIS NAME cross-check |
| T-194-VM | Tampering | Vice Mayor modeling (double-count / mislabel / stale-annotation) | mitigate | Title-annotation model (D-05): exactly 7 offices (no separate 8th seat), the `(Vice Mayor)` suffix on the Ward-1/-4008002 seat ONLY (post-verify gate + Task 3 assert exactly one, on -4008002); rotates annually so Task 2 re-confirms before apply |
| T-194-PARTY | Information disclosure | partisan data (D-04) surfacing on a profile | mitigate | party='Democratic' stored per the antipartisan convention but NEVER selected/displayed by any profile query — no special-casing, identical to every prior phase |
| T-194-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `.env`; applied by the inline orchestrator; never hardcoded |
| T-194-SC | Tampering | package installs | accept | No new installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 1 greenfield City of Tucson government (geo_id 0477000, type City).
- 1 'City Council' chamber; 7 offices — at-large Mayor on a NEW LOCAL_EXEC/G4110 district + 6 ward members on LOCAL X0020 districts, 1 per district.
- Vice Mayor = title annotation on the Ward-1/Santa Cruz seat; exactly one office carries `(Vice Mayor)`; no separate 8th office.
- Section-split 0 rows; casing lowercase 'az' on districts; party stored (Democratic) never displayed; structural migration registered.
- 7 politician UUIDs captured for Plans 03/04.
</verification>

<success_criteria>
The government/roster half of ROADMAP #1 is TRUE in production (Mayor + 6 ward members, election
method verified, per-ward routing enabled via the new districts rows); the migration is applied +
registered; the UUID manifest is recorded.
</success_criteria>

<output>
Create `.planning/phases/194-city-of-tucson-deep-seed/194-02-SUMMARY.md` when done — include the 7
external_id → UUID → full_name manifest (required by Plans 03 and 04) and record Santa Cruz (Ward 1)
as Vice Mayor surfaced via the `title` annotation, noting the annotation rotates annually
(informational).
</output>
</content>
