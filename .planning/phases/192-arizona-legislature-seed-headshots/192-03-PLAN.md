---
phase: 192-arizona-legislature-seed-headshots
plan: 03
type: execute
wave: 3
depends_on: ["192-01", "192-02"]
files_modified: []
autonomous: false
requirements: [AZ-LEG-01]
must_haves:
  truths:
    - "The full phase audit (30 Senate / 60 House / 2-per-district / linkage / 90 headshots / 0 stances / casing / section-split / ledger) is all-green in production"
    - "A resident in a known LD sees 1 senator + 2 representatives, all labeled that district"
    - "Every seeded headshot depicts the RIGHT current member — including the 3 mid-term appointees (Sears/Reim/Allen); none of the 3 departed members appear"
    - "0 compass stances exist for the 90 AZ legislators — confirmed deferred by design, NOT a gap"
  artifacts:
    - path: ".planning/phases/192-arizona-legislature-seed-headshots/192-03-SUMMARY.md"
      provides: "Recorded full-audit results + human-verify sign-off"
      contains: "AZ-LEG-01"
  key_links:
    - from: "inform.politician_answers"
      to: "the 90 AZ-leg politician ids"
      via: "count query = 0 (deferred by design)"
      pattern: "politician_answers"
---

<objective>
Prove the phase goal in production with a full SQL/HTTP audit and a human-verify checkpoint.
No new DB writes — this plan is read-only assertions + a live browse spot-check. The 0-stances
assertion is a REQUIRED end-state labeled *deferred by design* (NV v18.0 pattern), not a gap.

Purpose: Satisfies ROADMAP success criterion #4 (0 compass stances, confirmed deferred) and
end-to-end validates #1–#3.
Output: `192-03-SUMMARY.md` with the recorded audit block and the human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-arizona-legislature-seed-headshots/192-VALIDATION.md
@.planning/phases/192-arizona-legislature-seed-headshots/192-PATTERNS.md
@.planning/phases/192-arizona-legislature-seed-headshots/192-01-SUMMARY.md
@.planning/phases/192-arizona-legislature-seed-headshots/192-02-SUMMARY.md

<execution_note>
ORCHESTRATOR-RUN (executor has no DB access). All assertions run inline via `psql -tAc` / `curl`.
No files are modified in the backend repo; results are recorded in 192-03-SUMMARY.md.
</execution_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full phase audit (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/192-arizona-legislature-seed-headshots/192-VALIDATION.md (Per-Task Verification Map — full check list)
    - .planning/phases/192-arizona-legislature-seed-headshots/192-PATTERNS.md (lines 390-432 — section-split SQL + compact single-query audit style; the Per-Task Verification Map table)
    - .planning/phases/192-arizona-legislature-seed-headshots/192-RESEARCH.md (§ Validation Architecture → Test Map)
  </read_first>
  <action>
    ORCHESTRATOR-RUN. Run the full audit via `psql "$DATABASE_URL" -tAc` and record each result in
    192-03-SUMMARY.md as a Per-Task Verification Map table (Check / Expected / Actual / status).
    Assertions:
    (1) Senate offices under 'State Senate' @ geo_id='04' = 30.
    (2) House offices under 'House of Representatives' @ geo_id='04' = 60.
    (3) House offices GROUP BY district_id HAVING count(*) <> 2 → 0 rows.
    (4) Linked districts: STATE_UPPER 30 + STATE_LOWER 30 at lowercase state='az', geo_id 04001..04030.
    (5) politician_images for the 90 ext_ids (-4006060..-4005001) = 90.
    (6) Stances: `SELECT count(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -4006060 AND -4005001)` → 0 (LABEL in the SUMMARY: *deferred by design — NV v18.0 pattern, NOT a gap*).
    (7) DISTINCT state on all 90 linked districts = only 'az'.
    (8) Section-split query (160-VALIDATION.md shape, lowercase d.state='az', district_type IN ('STATE_UPPER','STATE_LOWER'), HAVING COUNT(DISTINCT ch.government_id) > 1) → 0 rows.
    (9) Ledger: version '1286' (structural) registered = 1; version '1287' (headshots) registered = 0 (audit-only stays unregistered). Use `count(*) WHERE version='NNNN'` — NEVER max(version::int).
    Any red assertion blocks the phase and routes to gap closure.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.governments g ON g.id=c.government_id WHERE g.geo_id='04' AND c.name='State Senate')=30 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.governments g ON g.id=c.government_id WHERE g.geo_id='04' AND c.name='House of Representatives')=60 AND (SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4006060 AND -4005001)=90 AND (SELECT count(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -4006060 AND -4005001))=0 AND (SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1286')=1 AND (SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1287')=0"</automated>
  </verify>
  <acceptance_criteria>
    - State Senate offices = 30; House offices = 60.
    - House offices GROUP BY district_id HAVING count(*) <> 2 → 0 rows.
    - STATE_UPPER 30 + STATE_LOWER 30 linked districts at state='az'.
    - politician_images for the 90 ext_ids = 90.
    - `inform.politician_answers` for the 90 AZ-leg ids = 0 (recorded and labeled *deferred by design*).
    - DISTINCT state on linked districts = only 'az'; section-split query → 0 rows.
    - Ledger: version 1286 registered (=1), version 1287 unregistered (=0).
    - The combined boolean SELECT returns `t`; full table recorded in 192-03-SUMMARY.md.
  </acceptance_criteria>
  <done>Every automated phase assertion is green in production and recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live browse — address routing + correct-person photo spot-check</name>
  <read_first>
    - .planning/phases/192-arizona-legislature-seed-headshots/192-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/192-arizona-legislature-seed-headshots/192-RESEARCH.md (§ "The Roster" — the 3 mid-term appointees; § Security Domain — the 3 departed filenames still hosted)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor file action. The operator visually verifies address routing and correct-person headshots on the live app. See &lt;how-to-verify&gt; for the steps.</action>
  <what-built>
    All 90 legislators are seeded with offices linked to their SLDU/SLDL geofence and a 600×750
    headshot, and the full SQL audit (Task 1) is green including 0 stances. A DB row-count confirms an
    image EXISTS but cannot confirm WHO it depicts, and address→officeholder rendering is UI-resolved
    — both need a live visual check.
  </what-built>
  <how-to-verify>
    1. On https://essentials.empowered.vote, enter a full street address in a known Arizona
       legislative district. Confirm the result shows exactly 1 State Senator + 2 State
       Representatives, all labeled that same District N (AZ senate/house districts are coterminous —
       this is correct, not a bug), each with a headshot.
    2. Spot-check the 3 mid-term appointees depict the RIGHT current person: Kiana Sears (SD-9),
       Cody Reim (HD-3), Sylvia Allen (HD-7) — NOT their departed predecessors (Burch/Chaplik/
       Marshall).
    3. Spot-check 2 accented-surname members render a photo (Peña, Gabaldón, Márquez, or Luna-Nájera).
    4. Confirm no profile shows a blank/placeholder headshot across a sample of ~10 members spanning
       both chambers.
  </how-to-verify>
  <acceptance_criteria>
    - A known-LD address returns 1 senator + 2 reps, all labeled the correct District N, each with a headshot.
    - The 3 mid-term appointees (Sears/Reim/Allen) show the correct current person, not the departed predecessor.
    - The sampled accented-surname members render a photo (no broken image).
    - No blank/placeholder headshots in the sampled set.
    - Operator sign-off recorded in 192-03-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once address routing and the correct-person photo spot-check pass, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production DB → audit | Read-only assertions over the seeded rows |
| live app → operator | UI-rendered address routing + headshot identity crosses to human judgment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-05 | Tampering | inform.politician_answers | mitigate | Assert count = 0 for the 90 AZ-leg ids; record as *deferred by design* (NV v18.0), so a future stance pass is not mistaken for a gap here |
| T-192-01 | Tampering | office↔government linkage | mitigate | Section-split query (STATE_UPPER/STATE_LOWER, state='az') asserted 0 rows |
| T-192-06 | Spoofing | headshot identity | mitigate | Human-verify checkpoint spot-checks the RIGHT current member incl. the 3 mid-term appointees; confirms none of the 3 departed members appear |
| T-192-02 | Tampering | address→officeholder routing | mitigate | Live browse confirms 1 senator + 2 reps at the correct District N (validates district_type/casing end-to-end) |
</threat_model>

<verification>
- Full audit all-green (30/60/2-per-district/linkage/90 headshots/0 stances/casing/section-split/ledger).
- Live routing returns 1 senator + 2 reps per LD.
- Correct-person photos incl. the 3 mid-term appointees; 0 departed members.
</verification>

<success_criteria>
ROADMAP #4 (0 compass stances, confirmed deferred by design) is TRUE and recorded; #1–#3 are
end-to-end validated in production and by live operator sign-off.
</success_criteria>

<output>
Create `.planning/phases/192-arizona-legislature-seed-headshots/192-03-SUMMARY.md` when done —
include the full Per-Task Verification Map (all assertions with actual values) and the operator
sign-off.
</output>
