---
phase: 204-compass-lens-switcher
plan: 02
type: execute
wave: 2
depends_on: ["204-01"]
files_modified:
  - src/contexts/CompassContext.jsx
autonomous: true
requirements: [LENS-01]

must_haves:
  truths:
    - "The context exposes a global activeLensKey (default 'custom') and a setActiveLens setter that persists to ev:compassLens"
    - "The fallback lenses array carries name + description (not just key/color/topicIds)"
    - "API-fetched lenses are passed through normalizeApiLens before entering context state"
    - "On return from calibration, a pending lens that is now calibrated is auto-selected and its marker cleared (D-12)"
    - "getEffectiveLens / getEffectiveLensKey / toggleLens still exist and function for out-of-scope profile + elections consumers"
  artifacts:
    - path: "src/contexts/CompassContext.jsx"
      provides: "Global persisted activeLensKey state, setActiveLens, lens metadata fallback, normalizeApiLens hydration, calibration-return auto-select"
      contains: "activeLensKey"
  key_links:
    - from: "CompassContext value/memo"
      to: "consumers (Results, LensChipRow via CompassControlsBar)"
      via: "activeLensKey + setActiveLens exports added to memo object AND deps array"
      pattern: "setActiveLens"
    - from: "loadCompassData hydration"
      to: "GET /compass/lenses rows"
      via: "normalizeApiLens map before setLenses"
      pattern: "normalizeApiLens"
---

<objective>
Turn the latent lens data model into an explicit, persisted, global selection in
`CompassContext`: add `activeLensKey` (persisted via `ev:compassLens`, default `'custom'`),
`setActiveLens`, name/description on the fallback lenses, `normalizeApiLens` hydration, and the
auto-select-on-return-from-calibration behavior (D-12) — while leaving the existing per-office
helpers intact as functional shims for the out-of-scope profile `CompassCard` and `ElectionsView`.

Purpose: The grid needs ONE global lens key that survives reload and applies to every card;
retiring the per-office auto-lensing must not regress the profile/elections consumers that still
call `getEffectiveLens`.
Output: Updated `src/contexts/CompassContext.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/204-compass-lens-switcher/204-SPEC.md
@.planning/phases/204-compass-lens-switcher/204-CONTEXT.md
@.planning/phases/204-compass-lens-switcher/204-PATTERNS.md

<interfaces>
From src/lib/compass.js (added in Plan 01 — use directly, do not re-implement):
- normalizeApiLens(l) -> { key, name, description, color, icon, topicIds, autoDistrictTypes }
- isLensCalibrated(lens, userAnswers) -> boolean  // >= min(8, lens.topicIds.length)
- LENS_SELECTION_KEY = 'ev:compassLens'
- saveLensSelection(key) / loadLensSelection(knownKeys) -> string  // 'custom' when unknown
- loadLensPending() / clearLensPending()
- LENS_FALLBACKS  // array with name/description for the offline fallback
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global activeLensKey state, setActiveLens, metadata fallback, normalized hydration</name>
  <read_first>
    - src/contexts/CompassContext.jsx — L59-102 (lensOverride + getEffectiveLens/getEffectiveLensKey/toggleLens), L69-73 (fallback lenses array to enrich), L117-120 (fetchLenses hydration), L460-525 (value useMemo object + deps array ordering)
    - src/lib/compass.js — the Plan 01 exports listed in <interfaces>
    - C:/EV-CompassV2/src/lib/lenses.js — LENSES shape parity
  </read_first>
  <action>
    Import normalizeApiLens, isLensCalibrated, LENS_SELECTION_KEY, saveLensSelection, loadLensSelection,
    loadLensPending, clearLensPending from '../lib/compass' (flat named imports, matching the existing
    import block).
    (a) Enrich the fallback `lenses` array (L69-73) so each entry also carries `name` and `description`
    (values verbatim from EV-CompassV2 / Plan 01's LENS_FALLBACKS); keep key/color/topicIds/autoDistrictTypes.
    (b) In the hydration block (L118-120), map fetched rows through `normalizeApiLens` before
    `setLenses(rows.map(normalizeApiLens))` so name/description/icon are guaranteed and color is sanitized.
    (c) Add `const [activeLensKey, setActiveLensKey] = useState(() => loadLensSelection(['custom', ...fallbackKeys]))`
    defaulting to `'custom'`; add `setActiveLens = useCallback((key) => { setActiveLensKey(key); saveLensSelection(key); }, [])`.
    (Fire PostHog from the UI layer, not here — context has no posthog.)
    (d) Add `activeLensKey`, `setActiveLens`, and `isLensCalibrated` (re-exported for the grid) to BOTH
    the value useMemo object and its dependency array, following the existing state-first-then-callbacks
    ordering.
    (e) RETIRE per-office auto-lensing only for the GRID: leave getEffectiveLens, getEffectiveLensKey,
    toggleLens, toggleLocalLens, setLocalLens exported and functional so the out-of-scope profile
    CompassCard (L52-69/L297) and ElectionsView (L293/L758) do not regress. Add a code comment noting
    the grid no longer consumes them (Results switches to activeLensKey in Plan 04) per SPEC Req 8.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Context value exposes `activeLensKey` (initial value `'custom'` when nothing persisted) and `setActiveLens`
    - `setActiveLens('federal')` writes `'federal'` to `localStorage['ev:compassLens']`
    - Fallback lenses entries each contain a `name` and `description` string
    - Hydration calls `normalizeApiLens` on fetched rows before `setLenses`
    - getEffectiveLens, getEffectiveLensKey, toggleLens remain in the exported context value
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Global lens selection state is live and persisted; profile/elections shims still export; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-select the lens on return from calibration (D-12)</name>
  <read_first>
    - src/contexts/CompassContext.jsx — loadCompassData (L105-261) and where compassDataLoaded is set (L256); the mount effect (L264-397)
    - src/lib/compass.js — loadLensPending / clearLensPending / isLensCalibrated
    - .planning/phases/204-compass-lens-switcher/204-CONTEXT.md — D-12 (returning user's calibrated lens auto-selects) and the RESOLVED calibrate-URL flag
  </read_first>
  <action>
    Add a useEffect keyed on `compassDataLoaded` + `userAnswers` + `lenses` that runs after compass
    data has loaded: read `loadLensPending()`. If a pending key exists, find its lens in `lenses` and,
    when `isLensCalibrated(lens, userAnswers)` is now true, call `setActiveLens(pendingKey)` then
    `clearLensPending()`. If the pending lens is still not calibrated, leave the marker for a later
    visit (do not auto-select). Guard so it only fires once per pending marker (clear-on-apply is the
    guard). The marker itself is written by the grid's onCalibrate handler in Plan 04.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - After compass data loads, if `localStorage['ev:compassLensPending']` names a lens that is now calibrated, activeLensKey becomes that key and the pending marker is removed
    - With no pending marker the effect is a no-op (activeLensKey unchanged)
    - The effect does not loop (marker cleared once applied)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Returning from calibration auto-selects and applies the just-calibrated lens exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> context state | `ev:compassLens` / `ev:compassLensPending` read on mount and applied as the active lens |
| GET /compass/lenses -> context state | API rows become the rendered lens registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-204-01 | Tampering | activeLensKey hydration | mitigate | `loadLensSelection(knownKeys)` (Plan 01) rejects any persisted value not in the live lens-key set before it becomes active |
| T-204-02 | Injection | lens hydration | mitigate | `normalizeApiLens` sanitizes `color` and shapes name/description before state |
| T-204-05 | Elevation | pending-lens auto-select | mitigate | Auto-select only applies a pending key that resolves to a real lens AND passes isLensCalibrated; unknown keys are ignored |
| T-204-SC | Tampering | dependencies | accept | No new packages introduced |
</threat_model>

<verification>
- `npm run build` succeeds
- activeLensKey persists across reload; unknown persisted values degrade to 'custom'
- Profile CompassCard + ElectionsView continue to import getEffectiveLens without error
</verification>

<success_criteria>
Global persisted lens selection + normalized hydration + calibration-return auto-select all live,
with zero regression to out-of-scope consumers.
</success_criteria>

<output>
Create `.planning/phases/204-compass-lens-switcher/204-02-SUMMARY.md` when done.
</output>
