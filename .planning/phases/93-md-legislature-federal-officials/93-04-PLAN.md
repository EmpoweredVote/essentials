---
phase: 93-md-legislature-federal-officials
plan: 04
type: execute
wave: 2
depends_on:
  - 93-01
files_modified:
  - C:/EV-Accounts/backend/migrations/275_md_federal_officials.sql
autonomous: true
requirements:
  - MD-GOV-05
tags:
  - md
  - federal
  - migration

must_haves:
  truths:
    - "Both pre-existing US senators (Van Hollen ext=-400033, Alsobrooks ext=-400034) verified present by pre-flight assertion per D-09/D-10"
    - "8 essentials.politicians rows exist with external_id in -2440001..-2440008 (one per US House district 2401..2408)"
    - "8 essentials.offices rows exist with title='U.S. Representative', representing_state='MD', chamber=U.S. House of Representatives (UUID c2facc31-7b13-428c-b7b9-32d0d3b95f76)"
    - "Each new House rep's office.district_id resolves to NATIONAL_LOWER district with state='MD' (uppercase) per Pitfall 5"
    - "Each new House rep politician.office_id is back-filled (NOT NULL) post-migration"
    - "Migration is idempotent — re-run inserts 0 additional rows; senators are NOT re-inserted"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/275_md_federal_officials.sql"
      provides: "MD federal officials migration: pre-flight US senators + 8 US House reps + office back-fill"
      contains: "STEP 1: Assert MD NATIONAL_UPPER"
  key_links:
    - from: "essentials.offices (8 MD US House reps)"
      to: "essentials.chambers (U.S. House of Representatives, UUID c2facc31-7b13-428c-b7b9-32d0d3b95f76)"
      via: "hardcoded chamber UUID per RESEARCH.md Federal Chamber UUIDs (verified in production)"
      pattern: "'c2facc31-7b13-428c-b7b9-32d0d3b95f76'"
    - from: "essentials.offices (8 MD US House reps)"
      to: "essentials.districts (8 NATIONAL_LOWER rows, state='MD')"
      via: "district_id resolved by geo_id + district_type + state"
      pattern: "d.geo_id IN ('2401','2402','2403','2404','2405','2406','2407','2408') AND d.district_type='NATIONAL_LOWER' AND d.state='MD'"
---

<objective>
Create migration 275 to seed 8 Maryland US House representatives with offices linked to NATIONAL_LOWER districts (geo_ids 2401..2408) under the shared U.S. House of Representatives chamber. The 2 US senators (Van Hollen ext=-400033, Alsobrooks ext=-400034) are ALREADY SEEDED per RESEARCH.md — migration 275 asserts their existence via pre-flight check and does NOT re-insert them.

Purpose: Implements MD-GOV-05. Critical deviation from prior federal migrations (e.g., ME 170) is the pre-flight senator assertion replacing the senator INSERT, plus the upper-case state filter (`state='MD'`) because NATIONAL districts use uppercase per Pitfall 5. The 8 House reps include 7 Democrats + 1 Republican (Andy Harris MD-01).

Output: One new SQL migration file `275_md_federal_officials.sql` applied to production via mcp__supabase-local__apply_migration. 8 new politicians + 8 new offices + 8 office_id back-fills.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/93-md-legislature-federal-officials/93-CONTEXT.md
@.planning/phases/93-md-legislature-federal-officials/93-RESEARCH.md
@.planning/phases/93-md-legislature-federal-officials/93-PATTERNS.md
@.planning/phases/93-md-legislature-federal-officials/93-01-SUMMARY.md

# Canonical analog (closest match for federal officials seeding)
@C:/EV-Accounts/backend/migrations/170_me_federal_officials.sql

# MD state-casing precedent (uppercase MD for NATIONAL)
@C:/EV-Accounts/backend/migrations/270_md_state_executives.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify Steny Hoyer (MD-05) and other House rep incumbencies, write migration 275</name>
  <files>C:/EV-Accounts/backend/migrations/275_md_federal_officials.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/275_md_federal_officials.sql (target — confirm not present)
    - C:/EV-Accounts/backend/migrations/170_me_federal_officials.sql (exact analog — adapt by replacing senator INSERTs with pre-flight assertion)
    - .planning/phases/93-md-legislature-federal-officials/93-PATTERNS.md (275_md_federal_officials.sql section — exact pattern with House rep CTE block)
    - .planning/phases/93-md-legislature-federal-officials/93-RESEARCH.md (Current Incumbents 8 MD US House Representatives table; Pre-Existing US Senators section; Federal Chamber UUIDs section; NATIONAL_LOWER geo_ids; Open Questions on Steny Hoyer)
    - .planning/phases/93-md-legislature-federal-officials/93-CONTEXT.md (D-09 shared federal chambers, D-10 Van Hollen + Alsobrooks)
  </read_first>
  <behavior>
    - Pre-execution verification: confirm Steny Hoyer (MD-05) is still the seated MD-05 representative as of execution date (RESEARCH.md Open Questions note: announced no re-election in 2026 but should still be incumbent through end of current term)
    - Pre-flight assert 1: 1 row exists in essentials.districts WHERE district_type='NATIONAL_UPPER' AND state='MD' (RAISE EXCEPTION if not)
    - Pre-flight assert 2: exactly 2 rows exist in essentials.politicians WHERE external_id IN (-400033, -400034) — Van Hollen + Alsobrooks already seeded (RAISE EXCEPTION if not)
    - For each of 8 House reps, emit one CTE block with politician INSERT + office INSERT
    - politician INSERT: ON CONFLICT (external_id) DO NOTHING RETURNING id; values is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, party from RESEARCH.md table
    - office INSERT: chamber_id hardcoded as `'c2facc31-7b13-428c-b7b9-32d0d3b95f76'` (U.S. House of Representatives, verified in production); title='U.S. Representative'; representing_state='MD'; is_appointed_position=false; is_vacant=false; role_canonical=NULL (170 includes this column — include for consistency)
    - district WHERE clause: `d.geo_id = 'NNNN' AND d.district_type = 'NATIONAL_LOWER' AND d.state = 'MD'` (UPPERCASE 'MD')
    - CROSS JOIN includes `AND p.id IS NOT NULL`
    - NOT EXISTS guard: `(o.district_id = d.id AND o.chamber_id = 'c2facc31-7b13-428c-b7b9-32d0d3b95f76')` (single-member federal, chamber_id guard fine)
    - external_id range -2440001 (CD-01 Harris) through -2440008 (CD-08 Raskin)
    - Office back-fill UPDATE scoped to `external_id BETWEEN -2440008 AND -2440001`
    - Wrapped in BEGIN/COMMIT
    - Idempotent: re-running inserts 0 additional rows; pre-flight assertion still passes (senators remain present)
  </behavior>
  <action>
    Step 1 — Verify Steny Hoyer (MD-05) incumbency: WebFetch `https://hoyer.house.gov/` or `https://www.house.gov/representatives/find-your-representative` for MD-05. If Hoyer is no longer the seated MD-05 rep (e.g., resigned mid-term), identify replacement and update RESEARCH.md Open Questions resolution before proceeding. If Hoyer is still seated, proceed.

    Step 2 — Verify other 7 House reps (Harris MD-01, Olszewski MD-02, Elfreth MD-03, Ivey MD-04, McClain Delaney MD-06, Mfume MD-07, Raskin MD-08) via spot-check WebFetch of 1-2 official .house.gov sites. If any has changed since RESEARCH.md verification (2026-06-05), update the roster accordingly.

    Step 3 — Create `C:/EV-Accounts/backend/migrations/275_md_federal_officials.sql` following the 170 pattern with these deviations:
    a) Replace ME senator INSERT blocks with a pre-flight assertion on Van Hollen (-400033) + Alsobrooks (-400034). Use `DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-400033, -400034)) <> 2 THEN RAISE EXCEPTION 'Pre-flight failed: MD US senators not found (ext -400033/-400034)'; END IF; END $$;`.
    b) Add NATIONAL_UPPER district pre-flight assertion: `DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.districts WHERE district_type = 'NATIONAL_UPPER' AND state = 'MD') <> 1 THEN RAISE EXCEPTION 'Pre-flight failed: MD NATIONAL_UPPER district not found'; END IF; END $$;`.
    c) Replace ME House rep CTE blocks (lines ~101-154 of 170) with 8 MD House rep CTE blocks. Use this exact roster from RESEARCH.md Current Incumbents table:
       - MD-01: Andy Harris, Republican, ext=-2440001, geo_id='2401'
       - MD-02: Johnny Olszewski, Democrat, ext=-2440002, geo_id='2402'
       - MD-03: Sarah Elfreth, Democrat, ext=-2440003, geo_id='2403'
       - MD-04: Glenn Ivey, Democrat, ext=-2440004, geo_id='2404'
       - MD-05: Steny Hoyer, Democrat, ext=-2440005, geo_id='2405'  (only if Step 1 confirmed; else use replacement)
       - MD-06: April McClain Delaney, Democrat, ext=-2440006, geo_id='2406'
       - MD-07: Kweisi Mfume, Democrat, ext=-2440007, geo_id='2407'
       - MD-08: Jamie Raskin, Democrat, ext=-2440008, geo_id='2408'
    d) Each CTE block follows the 170 House rep template but with title `'U.S. Representative'` (not `'Representative'`), representing_state `'MD'`, state filter `d.state = 'MD'` (uppercase) per Pitfall 5, hardcoded chamber UUID `'c2facc31-7b13-428c-b7b9-32d0d3b95f76'`. Include `role_canonical` column as NULL for schema consistency (170 has it).
    e) Append office_id back-fill at end: `UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -2440008 AND -2440001 AND p.office_id IS NULL;`.
    f) Wrap in BEGIN; ... COMMIT;.

    Step 4 — Apply migration via `mcp__supabase-local__apply_migration` with name `275_md_federal_officials` and the SQL body.

    Step 5 — Run verification queries listed in `<verify>`. Confirm 8 House rep offices + 2 pre-existing senator offices are intact (senators must still have their offices linked to NATIONAL_UPPER).

    Step 6 — Re-apply to confirm idempotency.
  </action>
  <verify>
    <automated>
SELECT COUNT(*) FROM essentials.offices
WHERE chamber_id = 'c2facc31-7b13-428c-b7b9-32d0d3b95f76'
  AND representing_state = 'MD';
-- Expected: 8
    </automated>
    <automated>
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -2440008 AND -2440001;
-- Expected: 8
    </automated>
    <automated>
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -2440008 AND -2440001 AND office_id IS NOT NULL;
-- Expected: 8 (all back-filled)
    </automated>
    <automated>
-- District linkage gate: every new House rep office linked to a NATIONAL_LOWER district with state='MD'
SELECT COUNT(*) FROM essentials.offices o
JOIN essentials.politicians p ON p.id = o.politician_id
JOIN essentials.districts d ON d.id = o.district_id
WHERE p.external_id BETWEEN -2440008 AND -2440001
  AND d.district_type = 'NATIONAL_LOWER'
  AND d.state = 'MD';
-- Expected: 8
    </automated>
    <automated>
-- Pre-existing senators unchanged: Van Hollen + Alsobrooks still have offices linked to NATIONAL_UPPER
SELECT COUNT(*) FROM essentials.offices o
JOIN essentials.politicians p ON p.id = o.politician_id
JOIN essentials.districts d ON d.id = o.district_id
WHERE p.external_id IN (-400033, -400034)
  AND d.district_type = 'NATIONAL_UPPER'
  AND d.state = 'MD';
-- Expected: 2
    </automated>
    <automated>
-- Per-district House rep coverage: each CD-01..CD-08 has exactly 1 rep
SELECT d.geo_id, COUNT(*) AS office_count
FROM essentials.offices o
JOIN essentials.districts d ON d.id = o.district_id
WHERE o.chamber_id = 'c2facc31-7b13-428c-b7b9-32d0d3b95f76'
  AND o.representing_state = 'MD'
  AND d.geo_id IN ('2401','2402','2403','2404','2405','2406','2407','2408')
GROUP BY d.geo_id
HAVING COUNT(*) <> 1;
-- Expected: 0 rows (each CD has exactly 1 rep)
    </automated>
  </verify>
  <done>
    Migration 275 applied; 8 House reps + offices seeded with correct chamber UUID and uppercase state casing; pre-flight assertions passed; senator offices unchanged; back-fill complete; idempotent re-apply produced 0 new rows.
  </done>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/275_md_federal_officials.sql` exists
    - File contains text 'Pre-flight failed: MD US senators not found' (senator existence assertion)
    - File contains text "external_id IN (-400033, -400034)" (asserting on Van Hollen + Alsobrooks)
    - File does NOT contain text "INSERT INTO essentials.politicians ... VALUES (gen_random_uuid(), 'Chris Van Hollen'" (do not re-insert senators) — verify by checking no INSERT block contains external_id -400033 or -400034
    - File contains exactly 8 occurrences of `WITH ins_p AS` (8 House rep CTE blocks)
    - File contains text `'c2facc31-7b13-428c-b7b9-32d0d3b95f76'` (hardcoded U.S. House chamber UUID)
    - File contains text `'U.S. Representative'` (title — note the periods)
    - File contains text "d.state = 'MD'" (uppercase MD for NATIONAL)
    - File contains text "d.district_type = 'NATIONAL_LOWER'"
    - File contains text "BETWEEN -2440008 AND -2440001" (back-fill range)
    - File begins with `BEGIN;` and ends with `COMMIT;`
    - SQL: `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='c2facc31-7b13-428c-b7b9-32d0d3b95f76' AND representing_state='MD'` returns 8
    - SQL: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2440008 AND -2440001 AND office_id IS NULL` returns 0
    - SQL: Per-district House rep gate (HAVING COUNT(*) <> 1) returns 0 rows
    - SQL: Pre-existing senator offices still resolve to NATIONAL_UPPER (COUNT = 2)
    - SQL: Re-applying migration produces no errors; senator and House rep counts unchanged (idempotency)
    - STATE.md `Next migration` advances to 276
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration apply → live Supabase DB | Direct DDL/DML to production |
| External fetch (house.gov) → House rep verification | External source; verify before seeding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-04-01 | Tampering | Pre-flight senator assertion | mitigate | Hard assertion on (external_id IN (-400033, -400034)) count = 2; migration aborts if senators missing |
| T-93-04-02 | Tampering | State casing for NATIONAL districts | mitigate | `d.state = 'MD'` (uppercase) per Pitfall 5; SQL gate verifies all 8 offices link to NATIONAL_LOWER with state='MD' |
| T-93-04-03 | Tampering | Chamber UUID hardcoding | accept | Federal chamber UUIDs verified stable in production DB (RESEARCH.md Federal Chamber UUIDs section); hardcoding is the established pattern from 170 |
| T-93-04-04 | Information Disclosure | Steny Hoyer (MD-05) incumbency drift | mitigate | Task 1 Step 1 re-verifies Hoyer at execution time via WebFetch; if no longer incumbent, identifies replacement before proceeding |
| T-93-04-05 | Denial of Service | Title mismatch ('Representative' vs 'U.S. Representative') | mitigate | Acceptance criteria explicitly require `'U.S. Representative'` (CONTEXT.md D-09 canonical title) |
</threat_model>

<verification>
After task: run all SQL queries listed in `<verify>` and `<acceptance_criteria>`. Confirm STATE.md migration counter advances to 276. Spot-check 2 House reps (Andy Harris CD-01 Republican, Jamie Raskin CD-08 Democrat) match the seeded roster.
</verification>

<success_criteria>
- Steny Hoyer (MD-05) incumbency confirmed at execution time (or replacement identified and substituted)
- File `275_md_federal_officials.sql` exists with pre-flight senator assertion + 8 House rep CTE blocks + back-fill
- Migration applied to production successfully
- 8 new politicians (ext -2440001..-2440008) and 8 new offices seeded
- Office back-fill complete (no NULL office_id in the new range)
- Per-district gate passes (exactly 1 rep per CD-01..CD-08)
- Senator offices unchanged (Van Hollen + Alsobrooks still linked to NATIONAL_UPPER)
- Idempotent re-apply produces 0 additional rows
- STATE.md `Next migration` advances to 276
</success_criteria>

<output>
Create `.planning/phases/93-md-legislature-federal-officials/93-04-SUMMARY.md` when done with:
- Steny Hoyer (MD-05) incumbency verification timestamp + result
- Migration file path + apply timestamp
- Final counts: 8 new House rep politicians, 8 new offices, 8 back-filled office_ids
- Pre-flight assertion results (senators verified, NATIONAL_UPPER district verified)
- Per-district coverage spot-check (CD-01 through CD-08 each have exactly 1 rep)
- Confirmation of idempotency
- Next migration counter advanced to 276
</output>
