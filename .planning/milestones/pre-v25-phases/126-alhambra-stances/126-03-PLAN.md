---
phase: 126-alhambra-stances
plan: 03
type: execute
wave: 3
depends_on: [126-02]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [ALHAMBRA-01]
must_haves:
  truths:
    - "Phase-wide Q1 query returns per-person stance counts for all 5 Alhambra officials"
    - "Phase-wide Q2 (uncited contexts) returns 0 across all 5 Alhambra officials"
    - "Phase-wide Q3 (unpaired answers) returns 0 across all 5 Alhambra officials"
    - "Phase-wide Q4 (dead topic check) returns 0 rows — no stances on retired topics"
    - "Compass render checkpoint passes on at least one Alhambra official's profile page"
    - "ALHAMBRA-01 is marked complete in REQUIREMENTS.md and ROADMAP.md"
    - "STATE.md reflects Phase 126 complete with next migration updated to NNN+5"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ALHAMBRA-01 marked satisfied"
      contains: "ALHAMBRA-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 126 marked complete"
      contains: "Phase 126"
    - path: ".planning/STATE.md"
      provides: "Current position updated to Phase 127 ready, next migration NNN+5"
      contains: "next migration"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "phase-wide Q2/Q3 citation checks (all 5 Alhambra officials)"
      pattern: "array_length\\(sources,1\\)"
---

<objective>
Run phase-wide closure verification across all 5 Alhambra officials, confirm compass rendering on at least one official's profile, then update REQUIREMENTS.md, ROADMAP.md, and STATE.md to mark ALHAMBRA-01 complete.

Purpose: Confirm 100% citation rate across all 5 stance files, close ALHAMBRA-01, and prepare STATE.md for Phase 127 (Beverly Hills Stances).
Output: Phase 126 fully closed; planning documents updated and committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/126-alhambra-stances/126-02-SUMMARY.md
@.planning/phases/126-alhambra-stances/126-01-SUMMARY.md

Alhambra officials (all 5, external_id range -700454 to -700450):
  -700450 Katherine Lee (Council Member, D1)
  -700451 Ross J. Maza (Council Member, D2)
  -700452 Jeff Maloney (Council Member, D3)
  -700453 Noya Wang (Council Member, D4 — rotational Mayor 2025-26)
  -700454 Adele Andrade-Stadler (Council Member, D5)

UUIDs: resolved in Plan 01 pre-flight and recorded in 126-01-SUMMARY.md.
Migration range: NNN through NNN+4 (NNN confirmed in 126-01-SUMMARY.md).
Next migration after phase = NNN+5.
Alhambra geo_id = 0600884.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-wide closure verification (Q1–Q4 queries + compass render checkpoint)</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/126-alhambra-stances/126-02-SUMMARY.md (Wang + Andrade-Stadler stance counts)
    - .planning/phases/126-alhambra-stances/126-01-SUMMARY.md (Lee + Maza + Maloney counts + all 5 UUIDs + confirmed NNN)
    - .planning/phases/126-alhambra-stances/126-RESEARCH.md (Phase-Wide Closure Verification section with full Q1-Q4 query text)
  </read_first>
  <action>
    Run all 4 phase-wide SQL verification queries via mcp__supabase-local__execute_sql, then verify compass rendering on at least one Alhambra official's profile.

    Q1 — Stance count per official (all 5 Alhambra members):
    `SELECT p.full_name, p.external_id, COUNT(pa.topic_id) AS stances FROM essentials.politicians p LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id WHERE p.external_id BETWEEN -700454 AND -700450 GROUP BY p.full_name, p.external_id ORDER BY p.external_id DESC;`
    Expected: 5 rows, one per official. Count may be 0 for blank-spoke officials — that is correct per evidence-only rule.

    Q2 — Uncited rows (must return 0):
    `SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700450) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0);`
    If non-zero: identify the specific unsourced rows via `SELECT pc.politician_id, pc.topic_id FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700450) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0);`, remediate by adding source URLs, and re-run Q2 before proceeding to Task 2.

    Q3 — Unpaired answers (must return 0):
    `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700450) AND pc.politician_id IS NULL;`
    If non-zero: identify the specific unpaired rows via `SELECT pa.politician_id, pa.topic_id FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700450) AND pc.politician_id IS NULL;`, insert the missing context rows, and re-run Q3.

    Q4 — Dead topic check (must return 0 rows — no stances on retired/inactive topics):
    `SELECT pa.topic_id, ct.slug, ct.is_active, COUNT(*) as rows FROM inform.politician_answers pa JOIN inform.compass_topics ct ON ct.id = pa.topic_id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700450) AND ct.is_active = false GROUP BY pa.topic_id, ct.slug, ct.is_active;`
    Expected: 0 rows. If any rows returned: these are stances on retired topics that must be deleted before closing.

    Compass render checkpoint: navigate to the Alhambra official with the most stances (determined from Q1 results) on essentials.empowered.vote and confirm the compass chart renders with spokes for the evidenced topics. If no spokes appear despite stances in DB, note the issue in SUMMARY but do not block phase closure (DB data integrity Q1-Q4 is the hard gate).

    Record all query results in SUMMARY. Do not proceed to Task 2 if Q2, Q3, or Q4 return non-zero.
  </action>
  <verify>
    <automated>Q2 = 0 (uncited contexts). Q3 = 0 (unpaired answers). Q4 = 0 rows (no stances on inactive topics). Any non-zero result from Q2/Q3/Q4 must be remediated before Task 2.</automated>
  </verify>
  <acceptance_criteria>
    - Q1 returns exactly 5 rows (one per Alhambra official); counts recorded in SUMMARY
    - Q2 returns 0 (zero uncited context rows across all 5 officials)
    - Q3 returns 0 (zero unpaired answer rows across all 5 officials)
    - Q4 returns 0 rows (zero stances on inactive topics)
    - Compass render verified on at least one Alhambra official profile at essentials.empowered.vote; result documented in SUMMARY
    - Any non-zero results from Q2/Q3/Q4 were remediated before proceeding
    - Total stance count across all 5 officials recorded in SUMMARY
  </acceptance_criteria>
  <done>Phase-wide citation check complete; Q2=0, Q3=0, Q4=0; compass render verified; all counts recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Mark ALHAMBRA-01 complete; update REQUIREMENTS.md, ROADMAP.md, STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find ALHAMBRA-01 entry — read the full section before editing)
    - .planning/ROADMAP.md (find Phase 126 entry — read before editing)
    - .planning/STATE.md (read Current Position + v15.0 Roadmap Summary before editing)
    - .planning/phases/124-ma-tier3-stances-wave3/124-05-SUMMARY.md (reference format for how Phase 124 closed three requirements)
  </read_first>
  <action>
    Update three planning documents in order. Read each file fully before editing.

    (1) REQUIREMENTS.md: Find the ALHAMBRA-01 entry under "## ALHAMBRA — Alhambra Stances". Change `- [ ] **ALHAMBRA-01:**` to `- [x] **ALHAMBRA-01:**`. Do not alter any other requirement entries.

    (2) ROADMAP.md: Find the Phase 126 entry in the v15.0 section. Mark the phase complete — update the status indicator. Update plan count to "**Plans:** 3 plans". Add the plan list:
    Plans:
    - [x] 126-01-PLAN.md — Wave 0 pre-flight + Katherine Lee (D1) + Ross J. Maza (D2) + Jeff Maloney (D3) stances (migrations NNN through NNN+2)
    - [x] 126-02-PLAN.md — Noya Wang (D4) + Adele Andrade-Stadler (D5) stances (migrations NNN+3 through NNN+4)
    - [x] 126-03-PLAN.md — Phase-wide closure verification + close ALHAMBRA-01

    (3) STATE.md: Update `## Current Position` to:
    - Phase: 127 — Beverly Hills Stances (not yet started)
    - Plan: —
    - Status: Not started
    - Last activity: [today's date] — Phase 126 complete; ALHAMBRA-01 closed; [N] total stance rows across 5 officials
    - Next migration: NNN+5 (NNN confirmed in 126-01-SUMMARY.md)

    Also add a Decisions entry for Phase 126 in the `## Decisions` section summarizing the phase outcome (blank-spoke count, total rows, rotational Mayor handling).

    Then commit all three files plus the 126-03-SUMMARY.md with the message:
    `docs(phase-126): Phase 126 Alhambra Stances complete — ALHAMBRA-01 closed`
  </action>
  <verify>
    <automated>Read REQUIREMENTS.md — ALHAMBRA-01 entry shows `[x]`. Read ROADMAP.md — Phase 126 shows complete with 3 plans listed. Read STATE.md — next migration is NNN+5; last activity updated to today's date.</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md: ALHAMBRA-01 marked satisfied (`[x]`)
    - ROADMAP.md: Phase 126 marked complete; plan list shows 3 plans all checked
    - STATE.md: next migration = NNN+5; last activity updated to today's date with Phase 126 complete note
    - STATE.md Decisions section includes Phase 126 summary entry
    - Git commit created covering REQUIREMENTS.md, ROADMAP.md, STATE.md, and 126-03-SUMMARY.md
  </acceptance_criteria>
  <done>ALHAMBRA-01 marked complete in all planning documents; STATE.md updated to next migration NNN+5 and Phase 127 ready; commit created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification queries → production DB | Read-only queries against live Supabase; no write risk in Task 1 (except remediations if Q2/Q3/Q4 non-zero) |
| planning document edits → git | Updates to REQUIREMENTS.md / ROADMAP.md / STATE.md committed to repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Tampering | stance value integrity | mitigate | Q2/Q3/Q4 must all return 0 before documents are updated; any non-zero result requires remediation first |
| T-126-02 | Information Disclosure | reasoning / sources | accept | Public-record positions; no PII |
| T-126-03 | Repudiation | requirement closure | mitigate | Closure requires Q2=0 + Q3=0 + Q4=0 (verified in Task 1) before marking ALHAMBRA-01 satisfied |
| T-126-04 | Tampering | premature requirement closure | mitigate | Task 2 is explicitly blocked on Task 1 completing with all zero results |
| T-126-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
Final phase-wide check results (all must be zero before closing):
- Q2 (uncited contexts, all 5 officials) = 0
- Q3 (unpaired answers, all 5 officials) = 0
- Q4 (stances on inactive topics) = 0 rows
- Compass render confirmed on at least one Alhambra official profile
</verification>

<success_criteria>
- Q1: per-official stance counts for all 5 Alhambra officials recorded
- Q2 = 0 (zero uncited context rows)
- Q3 = 0 (zero unpaired answer rows)
- Q4 = 0 rows (zero stances on inactive topics)
- Compass render verified on at least one Alhambra official at essentials.empowered.vote
- ALHAMBRA-01 marked satisfied in REQUIREMENTS.md
- Phase 126 marked complete in ROADMAP.md with 3 plans listed
- STATE.md next migration = NNN+5; last activity updated; Phase 127 ready
- Git commit created
</success_criteria>

<output>
Create `.planning/phases/126-alhambra-stances/126-03-SUMMARY.md` when done. Record: Q1 per-official stance counts, Q2/Q3/Q4 results, total stance rows across all 5 officials, blank-spoke officials (names + reason), compass render result, confirmed NNN+5 next migration value, and ALHAMBRA-01 closure confirmation.
</output>
