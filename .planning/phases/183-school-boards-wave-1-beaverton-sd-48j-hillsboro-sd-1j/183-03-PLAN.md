---
phase: 183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j
plan: 03
type: execute
wave: 3
depends_on: [183-02]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-headshots.py
  - C:/EV-Accounts/backend/migrations/1204_or_westmetro_school_boards_wave1_headshots.sql
autonomous: false
requirements: [WSCH-01, WSCH-02]
user_setup: []

must_haves:
  truths:
    - "A headshot pipeline script _tmp-westmetro-school-wave1-headshots.py exists that downloads all 14 director headshots from their verbatim finalsite URLs, crops to 4:5 FIRST then resizes to 600×750 (Lanczos q90), composites any RGBA/circle-cutout source onto white, uploads each to Supabase Storage politician_photos/{uuid}-headshot.jpg (x-upsert), and resolves the politician UUID at runtime by external_id (NOT hardcoded)"
    - "The script carries the standing WR-01/WR-02/WR-C fixes verbatim: main() exits non-zero if ANY official fails (so a chained apply cannot run on a silently-partial upload); the test-download guard returns bytes reused by the first official; the OFFICIALS>0 guard protects the index access"
    - "The script hard-fails fast on a malformed roster: 14 unique ext_ids, every official carries a license string, MIN_DIM=100 reject; it derives the Storage CDN base from SUPABASE_URL (never a hardcoded project ref) and uses the `py` launcher (bare python/python3 are non-functional stubs on this machine)"
    - "The orchestrator ran the script via `py` and captured a per-official manifest (external_id → UUID → CDN url + license + source); a full 14/14 SUCCESS is expected, and any genuine low-resolution Hillsboro gap (Kim/Pantoja/Watson/Thomas per A3) is documented (no fabrication, no over-upscale) rather than shipped blurry"
    - "An AUDIT-ONLY migration 1204_or_westmetro_school_boards_wave1_headshots.sql exists with one INSERT INTO essentials.politician_images block per SUCCESS director — columns exactly (id, politician_id, url, type, photo_license); type='default'; photo_license per actual source; url = the project Storage CDN base + the director's minted UUID + '-headshot.jpg'; idempotent via WHERE NOT EXISTS on politician_id"
    - "1204 is AUDIT-ONLY: it does NOT write a supabase_migrations.schema_migrations ledger row (matching the freshest headshot migration 1197_cornelius_headshots.sql); any director whose upload failed has its entire INSERT block removed with a documented-gap comment"
    - "The orchestrator applied 1204 and the headshot-count gate confirms the number of politician_images rows for the 14 ext_ids equals the number of successful uploads (target 14 minus any documented gap)"
    - "0 compass stance rows remain for all 14 directors (0-by-design success state — verified unchanged); no student-rep or secretary image is inserted (D-R4)"
    - "The file is saved UTF-8; verify-gate hygiene: literal strings slug / schema_migrations / photo_origin_url do not appear in 1204 (politician_images has NO photo_origin_url column)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-headshots.py"
      provides: "Headshot ETL: download 14 finalsite images → crop-4:5-first → 600×750 Lanczos q90 → white-composite RGBA → upload to Storage {uuid}-headshot.jpg → print SUCCESS/FAILED manifest; WR-01/02/C fixes; non-zero exit on any failure"
      contains: "resolve_politician_id"
      min_lines: 200
    - path: "C:/EV-Accounts/backend/migrations/1204_or_westmetro_school_boards_wave1_headshots.sql"
      provides: "Audit-only politician_images rows (one per success director, columns id/politician_id/url/type/photo_license, type='default', idempotent NOT EXISTS); NO ledger registration"
      contains: "INSERT INTO essentials.politician_images"
      min_lines: 40
  key_links:
    - from: "_tmp-westmetro-school-wave1-headshots.py"
      to: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      via: "requests.put with x-upsert, CDN base derived from SUPABASE_URL"
      pattern: "x-upsert"
    - from: "essentials.politician_images (14 rows)"
      to: "essentials.politicians (ext_id -4101921..-4101927 / -4100024..-4100030)"
      via: "politician_id subquery WHERE external_id = -N"
      pattern: "external_id = -41019|external_id = -41000"
---

<objective>
Source, process, and record 600×750 headshots for all 14 directors: author the ETL pipeline script, run
it to upload the images to Supabase Storage, then author and apply the audit-only politician_images
migration for every successfully-uploaded director. Genuine low-resolution gaps are documented, never
fabricated (D-R5).

Purpose: Satisfy the headshot half of WSCH-01 + WSCH-02. RESEARCH confirmed all 14 finalsite URLs return
HTTP 200 directly (the cleanest sourcing in the milestone — no WAF, no fallback chain needed), so a full
14/14 is expected; the only risk is a few small Hillsboro source images (A3), handled by trying the
largest CDN rendition first and documenting any residual gap rather than over-upscaling. The script is a
gitignored `_tmp-*` helper; 1204 is an audit-only migration (no ledger row, like 1197).
Output: `_tmp-westmetro-school-wave1-headshots.py` + `1204_or_westmetro_school_boards_wave1_headshots.sql`
+ recorded upload manifest and headshot-count gate for downstream plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-RESEARCH.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.py` script and the `.sql` migration. It has NO
database access, NO Storage access, and NO Supabase MCP. The INLINE ORCHESTRATOR runs the script via
`py C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-headshots.py` (bare `python`/`python3` are
non-functional Windows Store stubs — MUST use `py`), reads the SUCCESS/FAILED manifest, then applies 1204
via `psql "$DATABASE_URL" -f <file>`. The `.py` script is a gitignored `_tmp-*` helper — do NOT commit it.
C:/EV-Accounts is a SEPARATE git repo — never cd into it for commits; use `git -C "C:/EV-Accounts"`.

**VERIFY-GATE HYGIENE:** keep the literal strings `slug`, `schema_migrations`, and `photo_origin_url`
out of 1204 (politician_images is exactly `(id, politician_id, url, type, photo_license)` — there is no
photo_origin_url column). Save both files UTF-8.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot ETL pipeline script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-headshots.py</files>
  <read_first>
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-02-SUMMARY.md (the 14 external_id → politician UUID map; roster names + Zone/Position)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-RESEARCH.md §"Code Examples" (the 14 verbatim finalsite URLs + the low-res A3 note), §"Environment Availability" (py launcher, Pillow/psycopg2/requests versions)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md §"_tmp-westmetro-school-wave1-headshots.py" (config/roster/guard/UUID-resolve/download/process/upload/WR-fix/manifest patterns with exact analog line refs)
    - C:/EV-Accounts/backend/scripts/_tmp-cornelius-headshots.py (PRIMARY analog — freshest WR-01/WR-02/WR-C fixes, SUPABASE_URL-derived CDN base, crop-4:5-first, RGBA→white composite; read in full)
    - C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py (SECONDARY analog — school-board roster-dict shape + type='default' insert convention; do NOT copy its hardcoded CDN base or its verify=False SSL-bypass hack)
  </read_first>
  <action>
    Adapt _tmp-cornelius-headshots.py into a 14-official school-board pipeline. Config: derive CDN_BASE
    from SUPABASE_URL (split the project ref out of the https host — never hardcode the ref), BUCKET
    'politician_photos', TARGET_SIZE (600,750), JPEG_QUALITY 90, RESAMPLE Lanczos, DESCRIPTIVE_HEADERS
    with the EmpoweredVote UA, DOWNLOAD_TIMEOUT 30, MIN_DIM 100. OFFICIALS list: 14 dicts, each
    {'ext_id', 'name', 'url', 'license'} — 7 Beaverton (ext_id -4101921..-4101927, the verbatim
    finalsite URLs from RESEARCH §Code Examples) + 7 Hillsboro (ext_id -4100024..-4100030); for Hillsboro
    prefer the largest `f_auto,q_auto` (no explicit size-suffix) rendition first per A3. Do NOT hardcode
    politician_id — resolve it at runtime by external_id (resolve_politician_id via psycopg2, cornelius
    shape). photo_license per actual source (district finalsite CDN → 'press_use' unless the freshest
    phase used a different string — confirm against 1197). Guard-check before any network call: 14 unique
    ext_ids, len(OFFICIALS)==14, every official has a license string. Functions copied verbatim from
    cornelius: download_image (status + empty-body checks, descriptive UA, verify=True — NO SSL bypass),
    crop_to_4_5, resize_600x750, process_headshot_bytes (RGBA/circle-cutout→white-composite branch kept
    intact even though finalsite sources are plain opaque JPEGs; MIN_DIM reject; assert final size ==
    (600,750)), upload_to_storage (x-upsert PUT, CDN url from the SUPABASE_URL-derived base). WR-01:
    main() collects results and sys.exit(1) if any official failed. WR-C: guard `if len(OFFICIALS) > 0`
    before indexing OFFICIALS[0] in the test-download guard. WR-02: test_download_guard returns the
    verified bytes so the first official reuses them (no redundant second HTTP round-trip). Final manifest
    print: one `SUCCESS: {ext_id} {name} {uuid} -> {cdn_url} [license=..., source=...]` or
    `FAILED: {ext_id} {name} -- {error}` line per official. No fenced code in this action — the cornelius
    script is the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator (Task 3) runs `py C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-headshots.py` and reads the manifest</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-headshots.py
    - CDN_BASE is derived from SUPABASE_URL (grep finds a split on the https host; no hardcoded project-ref literal)
    - OFFICIALS has 14 dicts with ext_ids -4101921..-4101927 and -4100024..-4100030, each carrying a url + license
    - Contains resolve_politician_id (runtime UUID lookup by external_id) — no hardcoded politician_id in the roster
    - Contains crop_to_4_5, resize_600x750, process_headshot_bytes (with the RGBA→white-composite branch and MIN_DIM reject), upload_to_storage (x-upsert)
    - WR-01 (sys.exit(1) on any failure), WR-C (len(OFFICIALS)>0 guard), WR-02 (test guard returns reused bytes) all present
    - download_image uses verify=True (no verify=False SSL bypass copied from the secondary analog)
    - Guard-check rejects non-unique ext_ids / count != 14 / missing license before any network call
    - Manifest prints SUCCESS/FAILED per official
  </acceptance_criteria>
  <done>Headshot pipeline script authored for all 14 directors with WR-01/02/C fixes, runtime UUID resolution, and SUPABASE_URL-derived CDN base.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration 1204</name>
  <files>C:/EV-Accounts/backend/migrations/1204_or_westmetro_school_boards_wave1_headshots.sql</files>
  <read_first>
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-02-SUMMARY.md (the 14 external_id → politician UUID map — the UUIDs go in the url strings)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md §"1204_or_westmetro_school_boards_wave1_headshots.sql" (header + per-official INSERT patterns; audit-only, no ledger)
    - C:/EV-Accounts/backend/migrations/1197_cornelius_headshots.sql (freshest headshot-migration analog — AUDIT-ONLY header, one INSERT per sourced official, WHERE NOT EXISTS on politician_id, the exact Storage CDN base string https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg, gap-deletion note; read in full)
    - C:/EV-Accounts/backend/migrations/1108_ccsd_trustees_headshots.sql (school-board headshot analog — documented-gap section shape)
  </read_first>
  <action>
    Write an AUDIT-ONLY migration with a header block that: states it records politician_images rows for
    the 600×750 headshots uploaded by _tmp-westmetro-school-wave1-headshots.py; marks the file explicitly
    AUDIT-ONLY (does NOT write a ledger row — matches 1197); lists the exact column set
    (id, politician_id, url, type, photo_license) and notes there is no image-origin column; carries an
    ORCHESTRATOR NOTE: DELETE the entire INSERT block for any director the pipeline manifest does NOT
    report SUCCESS for (honest gap, no fabrication — not expected given confirmed sourcing), and keep a
    documented-gap comment for each deletion. Wrap the INSERTs in BEGIN;...COMMIT; (no ledger INSERT after).
    Write 14 INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) blocks
    — one per director — each: id=gen_random_uuid(); politician_id=(SELECT id FROM essentials.politicians
    WHERE external_id = -N); url = the fixed project Storage CDN base
    (https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/) + the
    director's minted UUID (from 183-02-SUMMARY.md) + '-headshot.jpg'; type='default'; photo_license per
    the manifest (uniform 'press_use' expected — match the string 1197 used); guarded by WHERE NOT EXISTS
    (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id ... WHERE external_id =
    -N)). Order the blocks Beaverton Zone 1-7 then Hillsboro Position 1-7 with a name/zone comment on each.
    No fenced code in this action — 1197 is the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator (Task 3) applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1204_or_westmetro_school_boards_wave1_headshots.sql` after the script run and confirms the headshot-count gate</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/1204_or_westmetro_school_boards_wave1_headshots.sql, wrapped in BEGIN;...COMMIT;
    - Header explicitly marks the file AUDIT-ONLY and states no ledger row is written; grep finds no `schema_migrations` INSERT
    - 14 `INSERT INTO essentials.politician_images` blocks with the exact column list (id, politician_id, url, type, photo_license); grep finds no `photo_origin_url` and no `slug`
    - Each url uses the kxsdzaojfaibhuzmclfq storage base + a UUID + '-headshot.jpg'; each politician_id resolves via WHERE external_id = -N for the 14 ext_ids
    - Each block type='default'; photo_license set (expected 'press_use'); each guarded by WHERE NOT EXISTS on politician_id
    - An ORCHESTRATOR NOTE documents the delete-block-on-gap procedure
    - File saved UTF-8
  </acceptance_criteria>
  <done>Audit-only migration 1204 authored with 14 idempotent politician_images INSERT blocks and a gap-deletion note; no ledger registration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs the pipeline, resolves any gaps, applies 1204, confirms the headshot count</name>
  <what-built>The headshot pipeline script + the audit-only migration 1204. The orchestrator runs the pipeline to upload the images, reconciles the manifest against 1204, and applies 1204.</what-built>
  <how-to-verify>
    A. Run: `py C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-headshots.py` (MUST use `py`,
       not bare python). Confirm a full 14/14 SUCCESS manifest (each SUCCESS line gives ext_id → UUID →
       CDN url). The script exits 0 only if all 14 succeeded (WR-01). If any FAILED: for a genuine
       low-resolution Hillsboro source (A3), first re-try the largest `f_auto,q_auto` rendition; if it is
       still unacceptable, document the gap (do NOT over-upscale a blurry image) — this is an honest
       partial, per D-R5. Record the per-official manifest in the SUMMARY.
    B. Reconcile 1204 against the manifest: confirm each retained INSERT block's UUID matches the manifest
       UUID for that ext_id (and the 183-02-SUMMARY.md UUID map). DELETE the entire INSERT block for any
       director that FAILED, adding a documented-gap comment. Confirm the remaining block count equals the
       number of SUCCESS uploads.
    C. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1204_or_westmetro_school_boards_wave1_headshots.sql`.
       Confirm no error and no ledger row was written (`SELECT COUNT(*) FROM
       supabase_migrations.schema_migrations WHERE version='1204'` → 0).
    D. Headshot-count gate: `SELECT COUNT(*) FROM essentials.politician_images pi JOIN
       essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4101927 AND -4101921
       OR p.external_id BETWEEN -4100030 AND -4100024` → equals the number of SUCCESS uploads (target 14
       minus any documented gap). Spot-check 2-3 CDN urls return HTTP 200.
    E. Re-confirm 0-stance-by-design: `SELECT COUNT(*) FROM inform.politician_answers pa JOIN
       essentials.politicians p ON pa.politician_id=p.id WHERE p.external_id BETWEEN -4101927 AND -4101921
       OR p.external_id BETWEEN -4100030 AND -4100024` → 0 (any row here is a defect). Confirm no
       student-rep/secretary image row exists.
  </how-to-verify>
  <acceptance_criteria>
    - Pipeline run via `py` produced a manifest; SUCCESS count recorded (target 14/14; any gap documented, not fabricated)
    - 1204 reconciled to the manifest (failed-director blocks deleted with a comment); UUIDs match the 183-02-SUMMARY.md map
    - 1204 applied cleanly; no ledger row for '1204'
    - Headshot-count gate equals the SUCCESS count; 2-3 CDN urls confirmed HTTP 200
    - 0 compass stance rows for all 14 directors (success state); no student-rep/secretary image inserted
  </acceptance_criteria>
  <resume-signal>Type "approved" with the manifest + headshot-count + 0-stance results, or describe which upload/gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external CDN | Downloading 14 images from resources.finalsite.net (the districts' own official asset host) |
| operator → Supabase Storage | Uploading processed 600×750 JPEGs via the service-role key |
| operator → live DB | Applying the audit-only politician_images migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-10 | Tampering | malicious/corrupt image payload (image bomb or disguised non-image) | mitigate | Pillow decode in process_headshot_bytes fails closed on non-image content; MIN_DIM reject; both sources are the districts' own finalsite CDN |
| T-183-11 | Tampering (data integrity) | applying 1204 on a silently-partial upload → DB rows pointing at missing Storage objects | mitigate | WR-01 non-zero exit halts the chain on any failure; the orchestrator reconciles 1204 to the manifest and deletes failed-director blocks before apply |
| T-183-12 | Tampering | migration re-run double-inserts images | mitigate | Every INSERT guarded by WHERE NOT EXISTS on politician_id |
| T-183-13 | Spoofing | fabricating/over-upscaling a blurry Hillsboro image to force 14/14 | mitigate | D-R5 document-gap-not-fabricate rule; try largest CDN rendition first, then honest partial |
| T-183-14 | Information Disclosure | leaking the service-role key from the gitignored _tmp-* script | mitigate | Key loaded at runtime from C:/EV-Accounts/backend/.env; the `_tmp-*` script is gitignored and never committed |
| T-183-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — Pillow/psycopg2/requests already present (verified in RESEARCH); no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Pipeline uploads all 14 (or documents genuine gaps); WR-01 blocks a partial-run apply.
- 1204 is audit-only (no ledger row) and reconciled to the manifest before apply.
- Headshot-count gate matches the success count; 0 compass stance rows remain (success state).
</verification>

<success_criteria>
- All 14 director headshots uploaded to Storage at 600×750 (or genuine gaps documented); politician_images
  rows applied via audit-only 1204; headshot-count gate green; 0 stance rows for all 14; no student-rep or
  secretary image inserted.
</success_criteria>

<output>
Create `.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-03-SUMMARY.md` when
done, recording: the per-official upload manifest (ext_id → UUID → CDN url + license), the SUCCESS count
and any documented gap, the applied audit-only migration (1204, no ledger row), the headshot-count gate
result, and the re-confirmed 0-stance-by-design baseline — for downstream plan 04.
</output>
