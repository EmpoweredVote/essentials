---
phase: 79-or-landing-elections-discovery
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Landing.jsx
  - C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql
autonomous: true
requirements: []
tags: [oregon, elections, landing, migration]

must_haves:
  truths:
    - "Portland, Oregon appears as a clickable coverage area on the Landing page"
    - "Clicking Portland OR routes to /results?browse_government_list=4159000&browse_label=Portland&browse_state=OR"
    - "essentials.elections has exactly 2 rows where state='OR' — 'OR 2026 Primary' (2026-05-19) and 'OR 2026 General' (2026-11-03)"
  artifacts:
    - path: "src/pages/Landing.jsx"
      provides: "Portland, Oregon entry in COVERAGE_AREAS"
      contains: "county: 'Portland', state: 'Oregon'"
    - path: "C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql"
      provides: "OR 2026 Primary + OR 2026 General election rows"
      contains: "OR 2026 General"
  key_links:
    - from: "src/pages/Landing.jsx COVERAGE_AREAS"
      to: "/results route via handleCountyClick"
      via: "browseGovernmentList=['4159000']"
      pattern: "browseGovernmentList.*4159000"
    - from: "migration 237"
      to: "essentials.elections"
      via: "INSERT ... ON CONFLICT (name, election_date, state) DO NOTHING"
      pattern: "OR 2026 (Primary|General)"
---

<objective>
Wire Portland, Oregon into the Landing page coverage map and seed the two OR 2026 election rows that all downstream race rows will link to.

Purpose: Establishes the user-visible entry point for OR coverage (D-01) and creates the elections table foundation that all subsequent OR race rows (Plans 79-02, 79-03, 79-04) reference via subquery on (name, election_date, state).

Output:
- One new entry in src/pages/Landing.jsx COVERAGE_AREAS (Portland Oregon).
- One new migration 237_or_2026_elections.sql in C:/EV-Accounts/backend/migrations/, applied to live Supabase production DB.
- 2 rows in essentials.elections with state='OR'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-or-landing-elections-discovery/79-CONTEXT.md
@.planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md
@.planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md
@src/pages/Landing.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Portland, Oregon entry to Landing.jsx COVERAGE_AREAS (D-01)</name>
  <files>src/pages/Landing.jsx</files>
  <read_first>
    - src/pages/Landing.jsx (read lines 1-25 to see current COVERAGE_AREAS shape and the Portland Maine entry that is the model)
    - .planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md (section "src/pages/Landing.jsx" — confirms exact entry shape and that line 19 currently ends with a trailing comma)
  </read_first>
  <action>
    Per D-01, append a single new object literal to the COVERAGE_AREAS array in src/pages/Landing.jsx immediately after the Portland, Maine entry (currently line 19) and before the closing `];` (currently line 20). The new entry must contain exactly these four keys in this order: `county` set to the string `'Portland'`, `state` set to the string `'Oregon'`, `browseGovernmentList` set to a single-element array containing the string `'4159000'` (Portland OR geo_id confirmed in Phase 72), and `browseStateAbbrev` set to the string `'OR'`. Use the same 2-space indentation as the surrounding entries. End the new entry with a trailing comma to match the Portland Maine entry style. Do not modify any other line of the file. Do not add `browseCountyGeoId` (per D-01: "No county geo_id addition needed"). Do not modify imports, the STEPS const, or the Landing component below.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/pages/Landing.jsx','utf8'); const m=c.match(/county:\s*'Portland',\s*state:\s*'Oregon',\s*browseGovernmentList:\s*\[\s*'4159000'\s*\],\s*browseStateAbbrev:\s*'OR'/); if(!m){console.error('Portland Oregon entry not found in expected shape'); process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Landing.jsx contains a literal substring matching the regex `county:\s*'Portland',\s*state:\s*'Oregon',\s*browseGovernmentList:\s*\[\s*'4159000'\s*\],\s*browseStateAbbrev:\s*'OR'`
    - The Portland Oregon entry appears AFTER the Portland Maine entry (`state: 'Maine'`) in COVERAGE_AREAS
    - The Portland Maine entry (`browseGovernmentList: ['2360545']`) is unchanged — still present and unmodified
    - All other COVERAGE_AREAS entries (Monroe County, LA County, San Diego, Fremont, San Francisco, San Jose, Sacramento, Berkeley, Collin County, Cambridge) are unchanged
    - File still parses as valid JS/JSX (no syntax errors); `node --check src/pages/Landing.jsx` exits 0 OR Vite dev server build succeeds
  </acceptance_criteria>
  <done>Landing.jsx contains the new Portland Oregon entry exactly as specified; existing entries unchanged; file remains valid JSX.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 237_or_2026_elections.sql (D-02)</name>
  <files>C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql</files>
  <read_first>
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (sections "elections.name Column", "Elections Table Unique Constraint", "OR Primary Date", "OR Governor 2026")
    - .planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md (section "migrations/237_or_elections.sql")
    - C:/EV-Accounts/backend/migrations/183_me_2026_elections_foundation.sql (direct ME analog — read top-level INSERT pattern only, not the candidate seed portion)
  </read_first>
  <action>
    Create a new file at exactly the path `C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql`. The file must be written in UTF-8 WITHOUT a BOM (use the Write tool — it does not emit a BOM; do NOT use PowerShell `Out-File -Encoding UTF8`). Migration number 237 is confirmed next-available per RESEARCH.md (235=fix_portland_officials_is_appointed and 236=fix_or_legislator_names_unicode were applied 2026-05-30). The file must contain two INSERT statements into `essentials.elections`. Per D-02, insert these two rows exactly: Row 1 — `name='OR 2026 Primary'`, `election_date='2026-05-19'`, `election_type='primary'`, `jurisdiction_level='state'`, `state='OR'` (uppercase, per RESEARCH.md "OR elections.state column"). Row 2 — `name='OR 2026 General'`, `election_date='2026-11-03'`, `election_type='general'`, `jurisdiction_level='state'`, `state='OR'`. Use the column name `name` (NOT `election_name` — per RESEARCH.md Pitfall 4). Both INSERTs must end with `ON CONFLICT (name, election_date, state) DO NOTHING` per RESEARCH.md "Elections Table Unique Constraint" (research finding #9). Add a comment header at the top of the file: `-- Migration 237: OR 2026 Elections — Phase 79 Plan 01` and a brief comment explaining D-03 (primary row is bare, all races link to general only). Do not include any race INSERTs in this file (D-03; those go in Plans 79-02, 79-03, 79-04). Do not insert any candidate rows. Do not include a `cron_active` column reference anywhere (per RESEARCH.md schema finding — column does not exist).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql';const b=fs.readFileSync(p);if(b[0]===0xEF&&b[1]===0xBB&&b[2]===0xBF){console.error('FAIL: UTF-8 BOM detected');process.exit(1);}const s=b.toString('utf8');if(!s.includes(\"'OR 2026 Primary'\"))throw new Error('missing primary row');if(!s.includes(\"'OR 2026 General'\"))throw new Error('missing general row');if(!s.includes(\"'2026-05-19'\"))throw new Error('missing primary date');if(!s.includes(\"'2026-11-03'\"))throw new Error('missing general date');if(s.includes('election_name'))throw new Error('uses wrong column election_name');if(s.includes('cron_active'))throw new Error('cron_active column does not exist');if(!s.includes('ON CONFLICT (name, election_date, state)'))throw new Error('missing ON CONFLICT clause');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql`
    - File is UTF-8 encoded without a BOM (first 3 bytes are NOT 0xEF 0xBB 0xBF)
    - File contains both literal strings `'OR 2026 Primary'` and `'OR 2026 General'`
    - File contains both literal strings `'2026-05-19'` and `'2026-11-03'`
    - File contains `state='OR'` or `state = 'OR'` (uppercase) for both rows
    - File uses column name `name` (NOT `election_name`)
    - File does NOT contain the string `cron_active`
    - Both INSERTs contain `ON CONFLICT (name, election_date, state) DO NOTHING`
    - File contains no race INSERTs (does not contain the substring `essentials.races`)
    - File contains no candidate INSERTs (does not contain `essentials.race_candidates`)
  </acceptance_criteria>
  <done>Migration 237 file exists, UTF-8 NoBOM, contains exactly two OR election INSERTs with correct ON CONFLICT and column names.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 237 to Supabase and verify (D-02, D-03)</name>
  <files>C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql (apply only — no file modification)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/237_or_2026_elections.sql (the file written in Task 2)
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (section "Validation Architecture — After Plan 01")
  </read_first>
  <action>
    Apply migration 237 to the live Supabase production database using the mcp__supabase-local__execute_sql tool (per memory: mcp__supabase-local IS the remote/production Supabase DB). Read the SQL file written in Task 2 and execute its contents in a single `execute_sql` call. After applying, run the verification query `SELECT name, election_date, election_type, jurisdiction_level, state FROM essentials.elections WHERE state='OR' ORDER BY election_date;` and confirm exactly 2 rows are returned with the expected values. Then run the migration idempotency check: re-execute the migration 237 SQL a second time and confirm no error is raised (ON CONFLICT DO NOTHING must work). Then run the section-split check: `SELECT COUNT(*) AS orphans FROM essentials.geofence_boundaries gb WHERE gb.state='41' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts WHERE state IN ('or','OR','41'));` and confirm result is 0 rows. Record the resulting election UUIDs (the `id` column for both OR 2026 Primary and OR 2026 General) in the plan SUMMARY for downstream plans to reference. Also add migration 237 to the migration ledger if your project tracks one (check whether `essentials.migrations` or similar ledger table exists and follow the established INSERT pattern from prior Phase 77.1 plan).
  </action>
  <verify>
    <automated>echo "Manual MCP verification — see acceptance_criteria for explicit SELECT queries that must return expected values via mcp__supabase-local__execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.elections WHERE state='OR'` returns exactly 2
    - `SELECT name FROM essentials.elections WHERE state='OR' AND election_date='2026-05-19'` returns exactly one row with name = `OR 2026 Primary`
    - `SELECT name FROM essentials.elections WHERE state='OR' AND election_date='2026-11-03'` returns exactly one row with name = `OR 2026 General`
    - `SELECT election_type FROM essentials.elections WHERE name='OR 2026 Primary' AND state='OR'` returns `primary`
    - `SELECT election_type FROM essentials.elections WHERE name='OR 2026 General' AND state='OR'` returns `general`
    - Re-running migration 237 SQL a second time returns no error and `SELECT COUNT(*) FROM essentials.elections WHERE state='OR'` still returns 2 (idempotency)
    - Section-split check `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.state='41' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts WHERE state IN ('or','OR','41'))` returns 0
    - SUMMARY file records both OR 2026 election UUIDs for downstream plan reference
    - `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR'` returns 0 (D-03: primary is bare, races are deferred to Plans 02/03/04)
  </acceptance_criteria>
  <done>Migration 237 applied; 2 OR election rows exist; idempotency verified; section-split check passes; election UUIDs recorded for Plans 02/03/04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → live Supabase production DB | Migration 237 SQL crosses this boundary via mcp__supabase-local__execute_sql; per memory, mcp__supabase-local IS the production DB |
| Developer → src/pages/Landing.jsx | Code edit committed to git, deployed via Render push to master |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-01 | Tampering | migration 237 SQL | accept | Plain INSERT with ON CONFLICT; no user input; static date/name literals; reviewable diff in git history |
| T-79-01-02 | Information Disclosure | OR election rows | accept | No PII; election dates and names are public record (sos.oregon.gov) |
| T-79-01-03 | Denial of Service | Supabase write | accept | Two INSERTs; idempotent via ON CONFLICT; no transactional load risk |
| T-79-01-04 | Elevation of Privilege | Landing.jsx edit | accept | Static JSX const; no auth path, no API call, no user input handling |

Note: This is a pure data migration phase with no auth paths, no new API endpoints, no user input handling. ASVS categories are not applicable. All threats rated low and accepted.
</threat_model>

<verification>
After all 3 tasks complete:

1. `SELECT COUNT(*) FROM essentials.elections WHERE state='OR'` returns 2
2. `grep -c "state: 'Oregon'" src/pages/Landing.jsx` returns 1 (filter comments out first if file later gains them — no comments today)
3. Migration 237 file exists, UTF-8 NoBOM, idempotent on re-apply
4. Section-split check returns 0 orphans
5. No race rows created yet (Plans 02/03/04 create races)
</verification>

<success_criteria>
- src/pages/Landing.jsx renders Portland Oregon as a clickable coverage area
- essentials.elections contains exactly 2 OR rows: 2026 Primary (May 19) and 2026 General (November 3)
- Migration 237 is the next ledger entry (after 236)
- Plans 79-02, 79-03, 79-04 can subquery `SELECT id FROM essentials.elections WHERE name='OR 2026 General' AND state='OR'` and receive the General election UUID
</success_criteria>

<output>
Create `.planning/phases/79-or-landing-elections-discovery/79-01-SUMMARY.md` documenting:
- Final OR 2026 Primary UUID (record explicitly)
- Final OR 2026 General UUID (record explicitly — referenced by Plans 02/03/04)
- Confirmation of section-split check (0 orphans)
- Confirmation of idempotency re-run
- Git commit hash of Landing.jsx change
- Migration 237 ledger entry confirmation (if applicable)
</output>
