---
phase: 120-new-bedford-deep-seed
plan: 02
type: execute
wave: 2
depends_on:
  - 120-01
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-new-bedford-headshots.py
  - C:/EV-Accounts/backend/migrations/588_new_bedford_headshots.sql
autonomous: true
requirements:
  - NEWBED-02

must_haves:
  truths:
    - "Python script runs to completion without exceptions; each official either uploaded or gap-documented"
    - "No politician_images rows with type != 'default' for any New Bedford official (external_ids -2545000001..-2545000012)"
    - "All gaps documented in migration 588 comments with the HTTP error code or block reason (Cloudflare, 404, etc.)"
    - "Mayor Mitchell headshot sourced from Wikipedia Commons or jonmitchell.com (newbedford-ma.gov is Cloudflare-blocked)"
    - "Migration 588 post-verification PASSED: wrong-type count = 0"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-new-bedford-headshots.py"
      provides: "Python script: attempts per-official headshot download from alternative sources, crops 4:5, resizes 600x750 Lanczos q90, uploads to politician_photos bucket"
      contains: "_tmp-new-bedford-headshots"
    - path: "C:/EV-Accounts/backend/migrations/588_new_bedford_headshots.sql"
      provides: "politician_images INSERT rows for successfully uploaded officials; gap comments with HTTP codes for failed officials"
      contains: "588_new_bedford_headshots"
  key_links:
    - from: "_tmp-new-bedford-headshots.py ROSTER"
      to: "Supabase Storage politician_photos bucket"
      via: "upload_to_storage() function; path pattern {politician_uuid}-headshot.jpg"
      pattern: "BUCKET = 'politician_photos'"
    - from: "migration 588 politician_images INSERT"
      to: "essentials.politicians (external_id subquery)"
      via: "SELECT id FROM essentials.politicians WHERE external_id = N"
      pattern: "type = 'default'"
---

<objective>
Upload headshots for New Bedford city officials (best-effort) and insert politician_images rows via migration 588. All gaps documented with HTTP error codes or block reason.

Purpose: Satisfies NEWBED-02 — New Bedford elected officials have headshots at 600x750 in Supabase Storage; all gaps documented honestly.
Output: Python script attempts 12 uploads to politician_photos bucket; migration 588 inserts successful rows + documents all gaps in comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-new-bedford-deep-seed/120-RESEARCH.md
@.planning/phases/120-new-bedford-deep-seed/120-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run _tmp-new-bedford-headshots.py Python upload script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-new-bedford-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-lynn-headshots.py (primary analog — copy all image processing functions verbatim; update only docstring, ROSTER, banner text, and Referer header)
    - .planning/phases/120-new-bedford-deep-seed/120-RESEARCH.md (headshot source analysis: Cloudflare block on newbedford-ma.gov, Wikipedia Commons for Mayor, per-councilor alternative source investigation strategy, Pitfall 1 warning about Cloudflare response format)
    - .planning/phases/120-new-bedford-deep-seed/120-01-SUMMARY.md (confirms politician UUIDs and external_ids loaded in migration 587)
  </read_first>
  <action>
Create C:/EV-Accounts/backend/scripts/_tmp-new-bedford-headshots.py by adapting _tmp-lynn-headshots.py with these changes:

COPY VERBATIM (do not modify):
- All imports (os, io, sys, time, requests, PIL)
- .env config parsing block (SUPABASE_URL, SERVICE_KEY, DATABASE_URL, BUCKET, CDN_BASE, TARGET_SIZE, JPEG_QUALITY, RESAMPLE)
- crop_to_4_5() function
- resize_600x750() function
- download_image() function
- upload_to_storage() function
- resolve_politician_id() function
- process_member() function

MODIFY:
- Docstring: update to Phase 120 New Bedford, migration 588, best-effort alternative sources (newbedford-ma.gov Cloudflare-blocked)
- BROWSER_HEADERS Referer: remove lynnma.gov; use generic Referer or Wikipedia URL as appropriate per source

ROSTER — 12 entries for city officials only (School Committee is out of scope):

Source strategy (apply in this priority order for each official):
1. Wikipedia Commons — check for individual Wikipedia article with Wikimedia Commons image
2. jonmitchell.com campaign site (Mayor only) — if Wikipedia Commons unavailable
3. Per-official investigation: search for official government page alternative (not newbedford-ma.gov — Cloudflare-blocked per Pitfall 1 in RESEARCH.md)
4. If no accessible source found: source_url=None (gap entry)

IMPORTANT: Do NOT attempt any URLs on newbedford-ma.gov (including bio page pattern /city-council/biographies/{slug}/). Any response from that domain will be a Cloudflare JS challenge page (starts with 'Just a moment...'), not an image. Treat all newbedford-ma.gov URLs as gaps.

ROSTER entries (external_id, name, initial_source_url):

Mayor:
- external_id=-2545000001, name='Jon Mitchell'
  Initial source: Wikipedia Commons — check https://en.wikipedia.org/wiki/Jon_Mitchell_(politician) for Wikimedia Commons image URL
  Fallback: https://jonmitchell.com (campaign site — look for headshot image in page source)
  If neither accessible: source_url=None, gap_reason='No accessible headshot (Wikipedia Commons no image; jonmitchell.com blocked); newbedford-ma.gov Cloudflare-blocked'

At-large councilors:
- external_id=-2545000002, name='Ian Abreu'
  Investigate at execution: Wikipedia, LinkedIn, news photo check on newbedfordguide.com or newbedfordlight.org (editorial — only use if license acceptable)
  If no accessible source: source_url=None, gap_reason='No accessible headshot; newbedford-ma.gov Cloudflare-blocked (HTTP challenge page)'
- external_id=-2545000003, name='Shane Burgo'
  Same investigation approach; gap_reason as above if no source found
- external_id=-2545000004, name='Naomi Carney'
  Same investigation approach
- external_id=-2545000005, name='Brian Gomes'
  Same investigation approach
- external_id=-2545000006, name='James Roy'
  New councilor (Jan 2026) — bio page may not exist; same investigation approach

Ward councilors:
- external_id=-2545000007, name='Leo Choquette', Ward 1
  Same investigation approach
- external_id=-2545000008, name='Scott Pemberton', Ward 2
  New councilor (Jan 2026); same investigation approach
- external_id=-2545000009, name='Shawn Oliver', Ward 3
  Same investigation approach (Oliver has entered MA lieutenant governor race per RESEARCH.md — may have campaign site photos)
- external_id=-2545000010, name='Derek Baptiste', Ward 4
  Same investigation approach
- external_id=-2545000011, name='Joseph Lopes', Ward 5
  Same investigation approach
- external_id=-2545000012, name='Ryan Pereira', Ward 6
  Same investigation approach

For each ROSTER entry where source_url is None (gap): set gap_reason to the specific HTTP error code or block reason encountered during the investigation (e.g., 'Cloudflare JS challenge (403/503)', '404 Not Found', 'No Wikimedia Commons image', etc.). Do not use a generic reason — be specific per official.

Image processing (copy from _tmp-lynn-headshots.py — do not modify):
- Download raw image from source_url
- crop_to_4_5() first
- resize_600x750() (600x750, Lanczos, q90)
- upload_to_storage() to politician_photos bucket
- Path pattern: {politician_uuid}-headshot.jpg

main() updates:
- Banner: 'Phase 120 — New Bedford Headshots (migration 588)'
- ROSTER count: 12 (city officials only; SC out of scope)
- Summary note: 'newbedford-ma.gov Cloudflare-blocked; alternative sources attempted per official; gaps documented with specific error codes'

Run the script after writing it. Record the actual upload count and per-official outcome (uploaded / gap + reason) for use in Task 2.
  </action>
  <verify>
    <automated>python C:/EV-Accounts/backend/scripts/_tmp-new-bedford-headshots.py — script must run to completion without Python exceptions; output must show per-official result (uploaded or gap with reason) and a final summary line</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/_tmp-new-bedford-headshots.py exists
    - Script runs to completion without Python exceptions or unhandled errors
    - Script output shows per-official status for all 12 officials (uploaded OR gap with specific reason)
    - Script output shows final summary: X uploaded, Y gaps, 0 Python errors
    - For each successfully uploaded official: Supabase Storage politician_photos bucket contains {politician_uuid}-headshot.jpg
    - No image uploaded with wrong dimensions — all uploads are 600x750 (confirmed by Pillow resize step)
    - No newbedford-ma.gov URLs attempted in script (Cloudflare-blocked per RESEARCH.md Pitfall 1)
    - Gap entries have specific reasons (not generic 'unknown')
  </acceptance_criteria>
  <done>Python script ran to completion; upload count and per-official gap documentation recorded for use in migration 588.</done>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration 588_new_bedford_headshots.sql</name>
  <files>C:/EV-Accounts/backend/migrations/588_new_bedford_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/586_lynn_headshots.sql (primary analog — copy INSERT block structure, WHERE NOT EXISTS guard pattern, and post-verification DO block from this file)
    - .planning/phases/120-new-bedford-deep-seed/120-RESEARCH.md (CDN base URL pattern, type='default' requirement, photo_license='public_domain')
    - Script output from Task 1 (the per-official results: which officials were uploaded and which are gaps with specific reasons)
  </read_first>
  <action>
Create C:/EV-Accounts/backend/migrations/588_new_bedford_headshots.sql by adapting 586_lynn_headshots.sql with these substitutions:

External ID range: -2537490001..-2537490012 (Lynn) → -2545000001..-2545000012 (New Bedford)
Migration ledger version: '586' → '588'

Migration header comment block:
-- Migration 588: New Bedford city officials headshots (NEWBED-02)
-- Storage bucket: politician_photos
-- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/
-- Total officials attempted: 12 (city only; SC out of scope for Phase 120)
-- Uploaded: [actual count from Task 1 script run]
-- Gap count: [actual gap count from Task 1 script run]
-- newbedford-ma.gov: Cloudflare JS challenge — all official city bio pages inaccessible programmatically
-- Photo processing: crop 4:5 first, then resize 600x750 Lanczos q90
-- type = 'default' (NOT 'headshot') — UI filter uses .find(img => img.type === 'default')
-- Upload script: C:/EV-Accounts/backend/scripts/_tmp-new-bedford-headshots.py
--   Run date: [today's date]
--   Result: [X uploaded, Y gaps]

For each successfully uploaded official (from Task 1 results): one INSERT block using this pattern:
INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license)
SELECT gen_random_uuid(),
       (SELECT id FROM essentials.politicians WHERE external_id = {ext_id}),
       'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/' ||
         (SELECT id FROM essentials.politicians WHERE external_id = {ext_id}) || '-headshot.jpg',
       'default', 'public_domain'
WHERE NOT EXISTS (
  SELECT 1 FROM essentials.politician_images
  WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = {ext_id})
);

For each gap official (from Task 1 results): a SQL comment documenting the gap reason:
-- GAP: {external_id} {full_name} — {specific reason from Task 1 output, e.g. 'No Wikimedia Commons image; newbedford-ma.gov Cloudflare-blocked (JS challenge)'}

Post-verification DO block (copy structure from 586, substituting New Bedford range):
DECLARE
  v_img_count INTEGER;
  v_wrong_type INTEGER;
BEGIN
  SELECT COUNT(*) INTO v_img_count
  FROM essentials.politician_images pi
  JOIN essentials.politicians p ON p.id = pi.politician_id
  WHERE p.external_id BETWEEN -2545000012 AND -2545000001
    AND pi.type = 'default';

  SELECT COUNT(*) INTO v_wrong_type
  FROM essentials.politician_images pi
  JOIN essentials.politicians p ON p.id = pi.politician_id
  WHERE p.external_id BETWEEN -2545000012 AND -2545000001
    AND pi.type != 'default';

  IF v_wrong_type > 0 THEN
    RAISE EXCEPTION 'Migration 588 post-verification FAILED: New Bedford headshots have wrong type: % rows', v_wrong_type;
  END IF;

  RAISE NOTICE 'Migration 588 post-verification PASSED: % headshots inserted (type=default), % gap officials documented', v_img_count, (12 - v_img_count);
END $$;

Migration ledger:
INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('588') ON CONFLICT (version) DO NOTHING;
COMMIT;

Apply the migration via mcp__supabase-local__execute_sql after writing the file.
  </action>
  <verify>
    <automated>Run migration 588 via mcp__supabase-local__execute_sql. Post-verification RAISE NOTICE must report: 'Migration 588 post-verification PASSED'</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/588_new_bedford_headshots.sql exists
    - Migration applies without errors
    - Post-verification PASSED notice appears (v_wrong_type = 0)
    - Query: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2545000012 AND -2545000001 AND pi.type = 'default' → matches Task 1 upload count
    - Query: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2545000012 AND -2545000001 AND pi.type != 'default' → 0 (no wrong-type rows)
    - Every gap official has a SQL comment in the migration with a specific reason (not generic 'blocked')
    - Migration header comment accurately records final upload count and gap count from Task 1
    - NEWBED-02 satisfied: headshots at 600x750 for all successfully uploaded officials; gaps documented honestly
  </acceptance_criteria>
  <done>Migration 588 applied; politician_images rows with type='default' inserted for all uploaded New Bedford officials; all gaps documented with specific HTTP error codes or block reasons in migration comments; post-verification PASSED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python script → external image sources (Wikipedia Commons, campaign sites, news sites) | Downloading from third-party URLs; image content is passively consumed |
| Python script → Supabase Storage | Service role key in .env file; uploads to politician_photos bucket |
| SQL migration → Supabase production DB | Storage CDN URL embedded in politician_images.url column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-02-01 | Information Disclosure | .env SERVICE_KEY | accept | Already in use in prior phases; key stays local; solo dev workflow |
| T-120-02-02 | Tampering | politician_images type field | mitigate | All INSERTs use type='default'; post-verification gate checks type != 'default' count = 0; RAISE EXCEPTION on any wrong-type row |
| T-120-02-03 | Tampering | Schema migrations ledger | mitigate | ON CONFLICT (version) DO NOTHING |
| T-120-02-04 | Spoofing | Image source authenticity | accept | Sources are Wikipedia Commons and official/campaign sites; editorial news photos avoided; solo review at execution |
| T-120-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; uses existing Pillow/requests from prior phases (_tmp-lynn-headshots.py already tested) |
</threat_model>

<verification>
After running Python script and applying migration 588:

1. SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2545000012 AND -2545000001 AND pi.type = 'default'; -- Expected: matches Task 1 upload count
2. SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -2545000012 AND -2545000001 AND pi.type != 'default'; -- Expected: 0
3. Section-split final check: SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id = '2545000' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id); -- Expected: 0
4. Confirm gap comments exist in 588 SQL file for each official that could not be uploaded — open the file and verify each gap has a specific reason string (not 'unknown' or blank)
</verification>

<success_criteria>
- Python script: X uploaded + Y gaps = 12 total, 0 Python errors
- Migration 588: politician_images rows type='default' for all uploaded officials; post-verification PASSED
- All gaps documented with specific HTTP error codes or Cloudflare block reason
- No wrong-type rows (type != 'default') for any New Bedford official
- NEWBED-02 satisfied: best-effort headshots at 600x750; honest gap documentation per project conventions
- Phase 120 complete: NEWBED-01 + NEWBED-02 both satisfied
</success_criteria>

<output>
Create `.planning/phases/120-new-bedford-deep-seed/120-02-SUMMARY.md` when done
</output>
