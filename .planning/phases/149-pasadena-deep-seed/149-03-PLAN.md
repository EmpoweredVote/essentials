---
phase: 149-pasadena-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["149-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql
autonomous: false
requirements: [PASA-01]

must_haves:
  truths:
    - "the exact image gap/quality set is determined by a pre-flight image-coverage + dimension query over the 8 current members (7 council + Mayor Gordo), NOT hardcoded; the 1 missing (Rivas -700150, 0 images), the already-deduped Lyon (657582, 1 row after Plan 01), and the existing dimension-suspect rows (Gordo/Hampton/Cole/Jones/Masuda/Madison) are all surfaced from live data"
    - "cityofpasadena.net is NO-WAF (RESEARCH §6 — all 8 official district/mayor portrait URLs returned HTTP 200 on direct curl 2026-06-20); portraits are sourced by DIRECT curl from the RESEARCH §6 WordPress wp-content/uploads/sites/ URLs (no operator-download fallback expected)"
    - "every uploaded portrait is the correct verified person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 FIRST (never stretched) then resized 600×750 Lanczos q90 JPEG, uploaded to Supabase Storage politician_photos at the canonical path {uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), x-upsert:true; politician_images type='default', photo_license='press_use', photo_origin_url set to the source page"
    - "no fabricated photos — where no acceptable verified portrait is obtainable for a member, an honest gap is documented rather than uploading a bad/wrong-person/overlaid image as type='default'"
    - "full-roster image audit confirms each of the 8 members has exactly one type='default' row (or a documented honest gap), with no member having >1 (Lyon deduped in Plan 01 stays at 1)"
    - "migration 948 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 947; on-disk counter authoritative; ledger MAX stays 947)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql"
      provides: "Audit-only headshot INSERT/replace (Rivas new + reprocess existing 7 to canonical 600×750) + photo_origin_url backfill + full-roster image-coverage audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos ({uuid}-headshot.jpg objects)"
      to: "essentials.politician_images.url (type='default', press_use)"
      via: "INSERT politician_images scoped by politician_id, guarded NOT EXISTS"
      pattern: "'default'"
---

<objective>
Give the current 8-member Pasadena roster (7 council + Mayor Gordo) clean, correct official headshots and
confirm single canonical-image coverage. After Plan 02 the survivor chamber 2e7f01d0 holds Gordo (Mayor) +
Hampton D1 + Cole D2 + Jones D3 + Masuda D4 + Rivas D5 + Madison D6 + Lyon D7, all bidirectionally linked.
This is the EASIEST headshot environment of the LA-County Wave-2 cities so far — cityofpasadena.net is
NO-WAF and all 8 portraits are directly curl-accessible. The EXACT gap/quality set is resolved by a pre-flight
image-coverage + dimension query, not assumed.

⚠️ cityofpasadena.net is NO-WAF (RESEARCH §6 — confirmed 2026-06-20). All 8 official portrait URLs returned
HTTP 200 on direct curl. Unlike Pomona (WAF-403) and Torrance (WAF surprise), Pasadena portraits are sourced
by DIRECT curl from the WordPress wp-content/uploads/sites/ URLs in RESEARCH §6. No operator-download/alt-host
fallback is expected, though the /find-headshots skill (~/.claude/commands/find-headshots.md) remains
available if any URL has gone stale post-June-2026-swearing-in (Open Q3).

Per RESEARCH §6 the state and source plan is (all URLs HTTP 200, 2026-06-20):
- Rivas (D5, -700150): MISSING (0 images) — curl
  https://www.cityofpasadena.net/district5/wp-content/uploads/sites/8/Jess-Rivas-Councilmember-District-5.jpg
  → crop 4:5 → 600×750 → upload canonical {uuid}-headshot.jpg.
- Gordo (Mayor, -200901): existing photo — reprocess from
  https://www.cityofpasadena.net/mayor/wp-content/uploads/sites/18/Victor-Gordo-Mayor-682x1024.jpg (682×1024,
  well above min) → crop 4:5 → 600×750 → upload canonical.
- Hampton (D1, -201094): .../district1/.../sites/5/Tyron-Hampton-District-1.jpg → reprocess.
- Cole (D2, 657577): .../district2/.../sites/6/rick-cole-2024.jpg → download → crop 4:5 → 600×750 → upload
  canonical (existing may be pre-2024 / old; verify it is Rick Cole, not Felicia Williams).
- Jones (D3, 657578): .../district3/.../sites/4/Councilmember-Justin-Jones.png → reprocess (PNG → JPEG fine).
- Masuda (D4, 657579): .../district4/.../sites/7/gene-masuda.jpg → reprocess.
- Madison (D6, 657581): .../district6/.../sites/9/SteveMadison.jpg → reprocess.
- Lyon (D7, 657582): deduped to 1 row in Plan 01 — reprocess from
  .../district7/.../sites/10/Councilmember-Jason-Lyon.png → crop 4:5 → 600×750 → upload canonical.

Processing (NON-NEGOTIABLE per project memory): for each member — visually confirm the CORRECT person →
crop to 4:5 FIRST (never stretch — crop only, eyes ~1/3 from top, full head+shoulders) → resize 600×750
Lanczos q90 JPEG (Python Pillow) → upload to the Supabase Storage politician_photos bucket at the canonical
path {uuid}-headshot.jpg using SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env) with header
x-upsert:true → INSERT politician_images (type='default', photo_license='press_use'); set photo_origin_url to
the source district/mayor page. Reject any source with superimposed text/graphics over the face — document a
gap honestly rather than upload a bad photo. Resolve each member's UUID by external_id at apply time.

Purpose: A voter sees the right face next to each official. Output: Migration 948 (audit-only — NOT registered
in schema_migrations) + verified portraits for all 8 members (Rivas newly uploaded; the other 7 reprocessed to
canonical 600×750) + a human checkpoint confirming the correct person + correct crop for each; any genuine gap
documented honestly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/149-pasadena-deep-seed/149-RESEARCH.md
@.planning/phases/149-pasadena-deep-seed/149-CONTEXT.md
@.planning/phases/149-pasadena-deep-seed/149-02-SUMMARY.md
@.planning/phases/147-pomona-deep-seed/147-03-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §6). cityofpasadena.net is NO-WAF — direct curl.
     Pre-flight resolves the exact gap/quality set; /find-headshots skill available if a URL is stale. -->

Current 8 members (resolve UUID by external_id; pre-flight a coverage + dimension query):
  -200901 Gordo (Mayor) · -201094 Hampton (D1) · 657577 Cole (D2) · 657578 Jones (D3) · 657579 Masuda (D4) ·
  -700150 Rivas (D5, MISSING — 0 images) · 657581 Madison (D6) · 657582 Lyon (D7, deduped to 1 in Plan 01)

Direct-curl source URLs (RESEARCH §6 — all HTTP 200 2026-06-20; NO WAF):
  Gordo   https://www.cityofpasadena.net/mayor/wp-content/uploads/sites/18/Victor-Gordo-Mayor-682x1024.jpg
  Hampton https://www.cityofpasadena.net/district1/wp-content/uploads/sites/5/Tyron-Hampton-District-1.jpg
  Cole    https://www.cityofpasadena.net/district2/wp-content/uploads/sites/6/rick-cole-2024.jpg
  Jones   https://www.cityofpasadena.net/district3/wp-content/uploads/sites/4/Councilmember-Justin-Jones.png
  Masuda  https://www.cityofpasadena.net/district4/wp-content/uploads/sites/7/gene-masuda.jpg
  Rivas   https://www.cityofpasadena.net/district5/wp-content/uploads/sites/8/Jess-Rivas-Councilmember-District-5.jpg
  Madison https://www.cityofpasadena.net/district6/wp-content/uploads/sites/9/SteveMadison.jpg
  Lyon    https://www.cityofpasadena.net/district7/wp-content/uploads/sites/10/Councilmember-Jason-Lyon.png
  (curl -L "{url}" -o {file}; if any 404/stale post-swearing-in, use /find-headshots to find the current portrait)

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face

Storage + DB:
  - Bucket: politician_photos · Path: canonical {uuid}-headshot.jpg (canonical convention)
  - Upload via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), header x-upsert:true
  - politician_images: type='default', photo_license='press_use'
  - UPDATE politicians.photo_origin_url to the source district/mayor page where NULL
  - Each INSERT guarded WHERE NOT EXISTS on politician_id → exactly ONE type='default' row per member
    (for the 7 with an existing row, replace by exact image id/url or x-upsert the canonical path)

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql or psql -f). It does NOT
register in schema_migrations (same convention as Pomona 928 / Torrance 938). On-disk counter authoritative;
ledger stays at 947 (Plan 02).

Pattern migration: C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql (audit-only INSERT +
photo_origin_url backfill + Storage URL shape + full-roster audit). For NO-WAF direct-curl sourcing precedent
see the Palmdale/Lancaster pipeline. /find-headshots skill: ~/.claude/commands/find-headshots.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight image-coverage/dimension check; source all 8 portraits by direct curl from cityofpasadena.net (NO-WAF); crop 4:5 → 600×750 → upload canonical (Rivas new + 7 reprocessed)</name>
  <files>C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql (audit-only INSERT + download/crop/upload pipeline + Storage URL shape + full-roster audit + ledger-unchanged check)
    - .planning/phases/147-pomona-deep-seed/147-03-PLAN.md (the exact pre-flight-coverage + crop/resize + checkpoint shape this plan mirrors — but Pasadena is NO-WAF, direct curl)
    - .planning/phases/149-pasadena-deep-seed/149-RESEARCH.md (§6 headshot sources/status incl. all 8 direct-curl URLs, §Common Pitfalls, §Open Questions 3, §Assumptions A6)
    - .planning/phases/149-pasadena-deep-seed/149-02-SUMMARY.md (final 8-member link state + the Cole name confirmation)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots — 4:5 crop FIRST → 600×750 Lanczos q90)
    - ~/.claude/commands/find-headshots.md (skill for finding a current portrait if any URL is stale)
  </read_first>
  <action>
    First run a pre-flight image-coverage + dimension query: for the 8 current members (-200901/-201094/657577/
    657578/657579/-700150/657581/657582), LEFT JOIN essentials.politician_images (type='default') and list who
    has a type='default' image, its url + dimensions where known — confirm Rivas (-700150) is missing (0
    images), Lyon (657582) has exactly 1 row (deduped in Plan 01), and which of the other 6 existing rows are
    below 600×750 / non-canonical and need reprocessing. Then source all 8 portraits by DIRECT curl from the
    RESEARCH §6 cityofpasadena.net URLs (curl -L "{url}" -o {file}). For EACH, visually confirm it is the
    correct named person and has NO superimposed text/graphics (Cole especially — confirm Rick Cole, not
    Felicia Williams; cross-check the Plan 02 name finding). If any URL 404s / is stale post-June-2026-swearing-in
    (Open Q3), use the /find-headshots skill to locate the current portrait. Then crop to 4:5 (crop only —
    never stretch; eyes ~1/3 from top, full head+shoulders), resize to 600×750 Lanczos q90 JPEG (Pillow),
    resolve each UUID by external_id, and upload to politician_photos at {uuid}-headshot.jpg via
    SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env) with x-upsert:true. Record each public Storage URL
    + source page for Task 2. If no acceptable verified portrait is obtainable for a member, document the
    genuine gap (do NOT upload a wrong-person/overlaid image as type='default').
  </action>
  <verify>
    <automated>For each sourced member the processed file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('{member}.jpg').size)"` → (600, 750)) and the uploaded Storage object at {uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight image-coverage + dimension query run; the missing/suspect set determined from live data (Rivas missing; Lyon 1 row; the other 6 existing rows assessed for reprocessing)
    - All 8 portraits sourced by DIRECT curl from cityofpasadena.net (NO-WAF); /find-headshots used only if a URL is stale
    - Each of the 8 portraits is the correct verified person (Cole confirmed Rick Cole not Williams), no superimposed text/graphics, processed to exactly 600×750, 4:5, not stretched, eyes ~1/3 from top, uploaded to {uuid}-headshot.jpg (HTTP 200)
    - Rivas (-700150) newly uploaded (was 0 images); the other 7 reprocessed/confirmed to canonical 600×750
    - Public Storage URL + source page recorded for Task 2 for each uploaded member
    - If any member has no acceptable portrait, an honest gap documented (NOT a wrong-person/overlaid image faked as type='default')
  </acceptance_criteria>
  <done>All 8 members (Rivas newly + Gordo/Hampton/Cole/Jones/Masuda/Madison/Lyon reprocessed) have verified portraits sourced by direct curl from cityofpasadena.net, each confirmed correct + clean, processed to 600×750 4:5, uploaded to Storage at {uuid}-headshot.jpg; Storage URLs + sources recorded for Task 2; any genuine gap documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 948 (audit-only) — INSERT/replace politician_images + photo_origin_url backfill + full-roster image audit; confirm ledger unchanged</name>
  <files>C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql (INSERT politician_images + photo_origin_url backfill + audit-only apply, ledger-unchanged check, full-roster coverage audit, documented-gap handling)
    - .planning/phases/149-pasadena-deep-seed/149-RESEARCH.md (§6 existing-headshot audit, §Validation Architecture Wave 3, §Assumptions A6)
    - .planning/phases/149-pasadena-deep-seed/149-02-SUMMARY.md (final link state)
    - ~/.claude/commands/find-headshots.md (skill reference)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql: for every member uploaded in Task 1,
    INSERT a politician_images row (resolved by external_id) url = the Storage public URL, type='default',
    photo_license='press_use', guarded WHERE NOT EXISTS on politician_id (for the 7 members with a pre-existing
    row, replace the existing too-small/non-canonical row by its exact image id/url so each ends with exactly
    one canonical type='default' row pointing at {uuid}-headshot.jpg); UPDATE essentials.politicians SET
    photo_origin_url = the source district/mayor page WHERE external_id=<id> AND photo_origin_url IS NULL.
    Apply via raw SQL (mcp__supabase-local execute_sql or psql -f) — do NOT run through the schema_migrations
    ledger (audit-only). Then run the full-roster coverage audit: LEFT JOIN the 8 current Pasadena officials
    (-200901/-201094/657577/657578/657579/-700150/657581/657582) to politician_images (type='default') and
    report who has exactly one type='default' row vs a documented gap; confirm Rivas now has 1 and Lyon still
    has exactly 1 (no member >1). Verify MAX(version) in schema_migrations is UNCHANGED by this file (stays 947).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT p.external_id, COUNT(pi.*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (-200901,-201094,657577,657578,657579,-700150,657581,657582) GROUP BY p.external_id;" shows each member with COUNT 0 (documented gap) or 1 (no member has >1) AND psql "$DATABASE_URL" -c "SELECT MAX(version) FROM supabase_migrations.schema_migrations;" is unchanged (stays 947)</automated>
  </verify>
  <acceptance_criteria>
    - Rivas (-700150): newly has exactly one type='default' press_use row at the canonical path (was 0)
    - Lyon (657582): still exactly one type='default' row (deduped in Plan 01; not doubled here)
    - Gordo/Hampton/Cole/Jones/Masuda/Madison: each reprocessed to one canonical 600×750 type='default' row; Cole's photo confirmed Rick Cole (not Williams)
    - Each member with a verified portrait has exactly one type='default' press_use row pointing at {uuid}-headshot.jpg; photo_origin_url populated; no member has >1 type='default' row
    - Members with no obtainable verified portrait are documented as honest gaps (not faked)
    - schema_migrations MAX(version) unchanged by migration 948 (audit-only confirmed; stays 947)
    - Migration is idempotent (re-apply = 0 rows changed)
  </acceptance_criteria>
  <done>948 applied (audit-only); Rivas newly imaged; the other 7 reprocessed to canonical 600×750; full-roster image audit shows each current member with exactly one default image or a documented honest gap; no member >1; ledger untouched (stays 947).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify every Pasadena headshot (correct person, Cole not Williams, clean crop)</name>
  <action>Pause for the user to visually verify each Pasadena portrait per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>Verified official headshots for the 8 current Pasadena members (7 council + Mayor Gordo), sourced by direct curl from cityofpasadena.net (NO-WAF): Rivas newly uploaded (was 0 images), the other 7 reprocessed to canonical 600×750; all processed to 600×750 4:5 and live in Supabase Storage + politician_images (type='default', press_use). Lyon deduped in Plan 01 stays at 1 row. Any honest gaps documented.</what-built>
  <how-to-verify>
    1. Confirm each member's photo is the CORRECT person (cross-check Gordo/Hampton/Cole/Jones/Masuda/Rivas/Madison/Lyon against the cityofpasadena.net district/mayor pages).
    2. CRITICAL: confirm the D2 photo is Rick Cole, NOT Felicia Williams (the prior D2 member — the known name/photo trap, Pitfall 2).
    3. Confirm no superimposed text/graphics over any face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    4. Confirm each member shows exactly one canonical image (no duplicate type='default' rows) on the Pasadena browse view; Rivas now has a photo; Lyon shows exactly one (not the old duplicate). Any documented gap is genuinely imageless (not faked).
    5. Confirm the on-disk counter is authoritative: schema_migrations MAX(version) is unchanged (still 947; the headshot file did not register).
  </how-to-verify>
  <resume-signal>Type "approved" or name which photo(s) need re-sourcing / re-cropping / identity-fix.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with a NO-WAF official image source. No untrusted user input reaches the DB — uploads +
image-row writes use service-role/operator against production. Threat surface = wrong-person photo (the
Cole-vs-Williams D2 trap, or a stale post-swearing-in URL serving the wrong person), a distorted/overlaid
photo, a fabricated/campaign photo filling a real gap, re-doubling Lyon's image, or accidental ledger
registration — mitigated by the pre-flight coverage/dimension check, the identity-verify-before-upload rule
(esp. Cole), the crop-then-resize/no-stretch rule, exact-id replacement for existing rows, a blocking
human-verify checkpoint that specifically checks Cole≠Williams, the honest-gap rule, and the audit-only
raw-SQL apply.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cityofpasadena.net (NO-WAF) | Official source AVAILABLE — direct curl; URLs may go stale post-June-2026-swearing-in (Open Q3) — human-verified before upload. |
| /find-headshots / alternate hosts → local processing | Fallback image sources if a URL is stale — human-verified before upload. |
| operator → Supabase Storage + production DB | Uploads + image-row INSERT against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-18 | Spoofing | Wrong-person headshot in the D2 seat (Cole vs Felicia Williams — name/photo trap, Pitfall 2) | mitigate | Identity-verify each portrait before upload; cross-check the Plan 02 Cole name finding; blocking human-verify specifically checks D2 photo is Rick Cole not Felicia Williams |
| T-149-19 | Spoofing | Stale post-swearing-in URL serves the wrong/old person (Open Q3) | mitigate | Visual identity confirmation per member before upload; /find-headshots fallback to the current portrait; human checkpoint |
| T-149-20 | Tampering | Image stretched / aspect-distorted / text-overlaid | mitigate | Crop-to-4:5-FIRST-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; human checkpoint |
| T-149-21 | Spoofing | Fabricated / campaign / wrong photo uploaded to fill a gap | mitigate | Honest-gap rule — if no acceptable verified portrait, leave imageless and document; never seed a wrong-person/overlaid photo as type='default' |
| T-149-22 | Tampering | Lyon re-doubled (a 2nd type='default' row added after the Plan 01 dedupe), or duplicate image row for any member | mitigate | Pre-flight coverage query; each INSERT guarded WHERE NOT EXISTS on politician_id; replacements target exact id/url; post-audit confirms ≤1 type='default' per official |
| T-149-23 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged (stays 947) before/after |
| T-149-SC | Tampering | npm/pip/cargo installs | accept | No package installs (existing Pillow + Supabase client; RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After 948 + checkpoint:
1. each current member with a verified portrait has exactly one type='default' press_use image at {uuid}-headshot.jpg; photo_origin_url set
2. full-roster coverage: every member is either imaged (exactly one default) or a documented honest gap; no member has >1; Rivas now imaged; Lyon still exactly 1
3. Cole is verified Rick Cole (not Felicia Williams)
4. schema_migrations MAX(version) unchanged by 948 (stays 947)
5. human checkpoint approved (correct person each, no overlays, correct crop)
</verification>

<success_criteria>
Every current Pasadena member with an obtainable correct photo has a single 600×750 verified image at the
canonical path; portraits sourced by direct curl from cityofpasadena.net (NO-WAF); the Cole-vs-Williams D2
trap is rejected; no wrong-person/overlaid/stretched photos; Rivas newly imaged; Lyon stays single; genuine
gaps documented honestly; the ledger counter is preserved (stays 947).
</success_criteria>

<output>
Create `.planning/phases/149-pasadena-deep-seed/149-03-SUMMARY.md` when done (record any honest image gaps).
</output>
</content>
