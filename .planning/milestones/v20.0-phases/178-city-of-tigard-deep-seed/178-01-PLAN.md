---
phase: 178-city-of-tigard-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-tigard-wave0-probe.sql
autonomous: false
requirements: [WASH-04]

must_haves:
  truths:
    - "Orchestrator has confirmed the city geo_id 4173650 exists as a G4110 geofence (research found it CORRECT — no correction needed, unlike Hillsboro)"
    - "Orchestrator has confirmed no City of Tigard government row already exists (greenfield) by geo_id AND by name search"
    - "Orchestrator has confirmed the ext_id block -4173651..-4173657 is free of collisions"
    - "Orchestrator has confirmed the on-disk migration MAX so the next structural migration number is safe (research snapshot: MAX 1158, next 1159)"
    - "The current seated 7-member roster + exact term types are re-confirmed live (Mayor Hu appointed, Anderson appointed interim, Ghoddusi/Robbins/Schlack/Shaw/Wolf elected; Wolf holds Council President) — WAF status noted: tigard-or.gov is Akamai 403 and NO CivicWeb/Legistar/Granicus mirror exists; use tigardlife.com/valleytimes.news + WebSearch aggregation"
    - "A fresh WebSearch pass confirms no council change occurred since the last confirmed Council Recap (2026-02-10) through plan/execution time"
    - "At least one headshot source URL is confirmed to download valid non-zero JPEG bytes (Shaw's tigardlife.com image spot-verified in research)"
    - "The live compass topic_key list (is_live=true) is captured for stance ingestion (research: 44 live, 8 judicial-*)"
    - "Wave-0 checkpoint confirms the pure at-large model (NO wards, NO numbered positions, NO ward geofences) and Youth Councilor exclusion before any write"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-tigard-wave0-probe.sql"
      provides: "The Wave-0 probe queries the orchestrator runs before any write"
      min_lines: 20
  key_links:
    - from: "probe.sql"
      to: "essentials.governments / essentials.politicians / essentials.geofence_boundaries / supabase_migrations.schema_migrations / inform.compass_topics"
      via: "SELECT probes"
      pattern: "essentials\\.(governments|politicians|geofence_boundaries)"
---

<objective>
Author the Wave-0 verification probe file for the City of Tigard deep-seed and drive the
orchestrator-run DB + roster checks that MUST pass before any structural write. This plan gates the
entire phase: no government, roster, headshot, or stance migration may be authored or applied until
every probe below returns its expected result.

Purpose: Prevent the highest-risk failure modes for this phase — a duplicate seed, an ext_id
collision, a wrong/stale migration number, and (uniquely for Tigard) a stale roster given the
unusually high recent-change rate (a Sept 2025 mayoral resignation + two council appointments in a
4-month window). Also confirm the pure at-large model, Youth Councilor exclusion, and
headshot-source retrievability before committing to the seed shape (RESEARCH Pitfalls 1, 2, 3, 6;
Assumptions A1-A8; Open Questions 2-3).
Output: `_tmp-tigard-wave0-probe.sql` (gitignored `_tmp-*` helper) + a recorded pass/fail for each
Wave-0 gate, recorded in the SUMMARY for downstream plans 02-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-city-of-tigard-deep-seed/178-RESEARCH.md
@.planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO
database access, NO WebSearch, and NO Supabase MCP. The INLINE ORCHESTRATOR (main context) runs the
probes via `psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`,
runs the roster re-fetch / WebSearch pass, and test-downloads one headshot. This probe file is a
gitignored `_tmp-*` helper — do NOT commit it.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except in probe E where a query genuinely reads that table), and
`photo_origin_url` out of comments in any file authored this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-tigard-wave0-probe.sql</files>
  <read_first>
    - .planning/phases/178-city-of-tigard-deep-seed/178-RESEARCH.md §"geo_id Verification", §"Ext_id Block", §"Next Migration Number", §"Live Compass Topics", §"Wave 0 Gaps"
    - .planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md §"Key Casing / Structural Traps"
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-01-PLAN.md (the direct prior-phase probe-file analog — same probe shape A–G)
  </read_first>
  <action>
    Write labeled SELECT probes, each preceded by a `\echo` label line so the orchestrator reads
    results unambiguously. Probe A (GEO_ID CONFIRM): count rows in essentials.geofence_boundaries
    WHERE geo_id='4173650' AND mtfcc='G4110' (expect 1 — research confirmed this geo_id CORRECT, no
    correction needed). Probe B: count essentials.districts WHERE geo_id='4173650' (expect 0 — no city
    districts yet). Probe C: count essentials.governments WHERE geo_id='4173650' OR name ILIKE
    '%tigard%' (expect 0 — greenfield). Probe D: select external_id from essentials.politicians WHERE
    external_id BETWEEN -4173660 AND -4173650 (expect 0 rows — ext_id block -4173651..-4173657 free).
    Probe E: select MAX(version::int) from supabase_migrations.schema_migrations (record it; the next
    STRUCTURAL migration number = on-disk file MAX + 1; research snapshot expects on-disk MAX 1158 →
    next 1159, ledger MAX 1150 — the orchestrator cross-checks with an `ls` of the migrations dir).
    Probe F: select distinct state from essentials.districts WHERE geo_id='4159000' (Portland) to
    confirm lowercase 'or' is the correct casing for LOCAL/LOCAL_EXEC rows. Probe G: select topic_key
    from inform.compass_topics WHERE is_live=true ORDER BY topic_key — the authoritative live-topic
    list for the stance plan (04); also count how many begin with 'judicial-' (expect 8, all skipped).
    Include a header comment noting the file is a gitignored orchestrator-run helper. Do NOT wrap in a
    transaction (read-only). No fenced code, no DDL — SELECT/\echo only.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-tigard-wave0-probe.sql` and records each result</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-tigard-wave0-probe.sql with labeled probes A–G
    - Probe A queries geofence_boundaries for geo_id='4173650' AND mtfcc='G4110'
    - Probe C queries governments by geo_id='4173650' OR name ILIKE '%tigard%'
    - Probe D queries the exact ext_id range -4173660..-4173650
    - Probe E reads MAX(version::int) from the migration ledger
    - Probe G selects topic_key WHERE is_live=true and counts judicial-* topics
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE)
  </acceptance_criteria>
  <done>Probe file authored with all queries and correct literal values (geo_id 4173650, ext_id range -4173660..-4173650).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes, re-fetches roster, tests one headshot, confirms all gates</name>
  <what-built>The Wave-0 probe file. The orchestrator now executes it against the live DB, re-fetches the live roster via local news + WebSearch, and test-downloads one headshot.</what-built>
  <how-to-verify>
    A. Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-tigard-wave0-probe.sql`
       (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
       1. Probe A returns geofence(4173650)=1. If ≠1 → STOP, city geofence missing.
       2. Probe B returns districts=0. If >0 → STOP, districts already exist (not greenfield).
       3. Probe C returns 0 (no existing Tigard gov). If >0 → STOP, phase already partly seeded — switch to reconcile.
       4. Probe D returns 0 rows (ext_id block free). If any row → shift the block and record the new range for plans 02/03/04.
       5. Probe E: cross-check against `ls C:/EV-Accounts/backend/migrations | sort -t_ -k1,1n | tail`. Next structural migration = on-disk file MAX + 1 (research snapshot: 1158 → 1159; ledger MAX 1150). Record the confirmed number for plan 02.
       6. Probe F returns 'or' (lowercase) — the casing plans 02 must use for LOCAL/LOCAL_EXEC districts.
       7. Probe G returns the live topic_key list (research: 44 live, 8 judicial-*) — save it verbatim for plan 04.
    B. Re-fetch the current roster from tigardlife.com / valleytimes.news + WebSearch aggregation of tigard-or.gov "Meet the Council" content (tigard-or.gov is Akamai WAF-403; NO CivicWeb/Legistar/Granicus mirror exists — use curl with a Chrome UA for ecode360.com, NOT WebFetch). Confirm the 7-member roster is unchanged since 2026-07-02: Mayor Yi-Kang Hu (appointed), Councilors Tom Anderson (appointed interim), Faraz Ghoddusi, Heather Robbins, Jake Schlack, Jeanette Shaw, Maureen Wolf (Council President). Run a FRESH WebSearch for "Tigard council recap"/"Tigard council resignation 2026" for any change after 2026-02-10. If any member changed → re-plan the roster in plan 02.
    C. Test-download ONE headshot (Shaw's spot-verified image: https://tigardlife.com/wp-content/uploads/2024/09/Jeanette-Shaw-696x462.jpg) with descriptive headers + `-L`; confirm it saves non-zero bytes and opens as a valid JPEG. Record that plan 03 must search per-official (tigardlife.com/valleytimes.news/Ballotpedia/campaign) with genuine gaps expected (no bulk portal, unlike Hillsboro).
    D. Confirm the pure at-large decision holds: charter §3.1 = "Mayor and six Councilors nominated and elected from the City at-large" → NO ward geofences, NO numbered-position titles, plain 'Mayor'/'Councilor' (RESEARCH Form-of-Government, Pitfall 1). Confirm the Youth Councilor is EXCLUDED (non-voting, mayor-appointed — Pitfall 6). Confirm Mayor Hu + Councilor Anderson are appointed seats (is_appointed handling in plan 02). If any of these is overturned by the re-fetch → STOP and re-plan.
  </how-to-verify>
  <acceptance_criteria>
    - All probe gates recorded with pass/fail
    - geo_id 4173650 geofence confirmed = 1
    - Confirmed ext_id block (default -4173651..-4173657 unless probe D forced a shift)
    - Confirmed next structural migration number (default 1159) via ledger + on-disk `ls` cross-check
    - Live topic_key list captured for plan 04; 8 judicial-* noted for exclusion
    - Live roster re-confirmed (7 members; Hu + Anderson appointed; Wolf Council President) via fresh WebSearch pass
    - One headshot confirmed downloadable (Shaw) OR fallback plan recorded; genuine-gap risk noted for plan 03
    - Pure at-large / no-ward / no-position decision confirmed; Youth Councilor exclusion confirmed
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |
| operator → external web | Roster re-fetch + one headshot test-download from tigardlife.com / valleytimes.news / WebSearch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-01 | Tampering | probe.sql executed on prod DB | accept | Probes are read-only SELECTs; no write path; no user input |
| T-178-02 | Spoofing | roster source (no official portal reachable — WAF-403) | mitigate | Triangulate roster from ≥2 independent local-news sources (tigardlife.com + valleytimes.news) cross-checked against WebSearch-indexed tigard-or.gov text; fresh WebSearch pass confirms currency before seed given the high recent-change rate |
| T-178-03 | Information Disclosure | topic_key list from inform.compass_topics | accept | Public compass topic keys; no PII |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- Any failed gate blocks plans 02–05 until resolved.
</verification>

<success_criteria>
- geo_id 4173650 geofence confirmed present; no duplicate Tigard gov; districts absent; ext_id block
  free; next migration number confirmed; live topic_key list captured; roster re-confirmed via fresh
  WebSearch; headshot retrievability tested; pure at-large + Youth-Councilor-exclusion + appointed-seat
  decisions confirmed.
</success_criteria>

<output>
Create `.planning/phases/178-city-of-tigard-deep-seed/178-01-SUMMARY.md` when done, recording the
confirmed geo_id (4173650), ext_id block, next structural migration number, live topic_key list,
re-confirmed 7-member roster (with appointed/elected term types), and headshot-source retrievability
for downstream plans.
</output>
