---
phase: 110-ma-2026-elections
plan: 02
type: execute
wave: 2
depends_on:
  - 110-01
files_modified:
  - C:/EV-Accounts/backend/migrations/358_ma_2026_legislative_races.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-358.ts
autonomous: true
requirements:
  - MA-ELECTIONS-03

must_haves:
  truths:
    - "Exactly 40 MA State Senate race rows exist for the 2026 Massachusetts General Election"
    - "Exactly 160 MA House race rows exist for the 2026 Massachusetts General Election"
    - "All 200 legislative race rows have non-null office_ids"
    - "Zero MA race rows (across both elections) have NULL office_id"
    - "Position names are derived from office titles (e.g., 'MA State Senate Berkshire-Hampden-Franklin-Hampshire District'), not numeric codes"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/358_ma_2026_legislative_races.sql"
      provides: "CTE-JOIN INSERT SQL for 200 MA legislative races"
      contains: "ch.name IN ('Massachusetts Senate', 'Massachusetts House of Representatives')"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-358.ts"
      provides: "Apply script with 5 smoke tests"
      exports: []
  key_links:
    - from: "migration 358"
      to: "essentials.offices (via chambers JOIN)"
      via: "ch.name IN ('Massachusetts Senate', 'Massachusetts House of Representatives')"
      pattern: "JOIN essentials.chambers ch ON o.chamber_id = ch.id"
    - from: "leg_offices CTE"
      to: "essentials.races"
      via: "INSERT SELECT from CTE cross-join gen_elec"
      pattern: "INSERT INTO essentials.races.*SELECT gen_elec.id, leg_offices.office_id"
---

<objective>
Apply migration 358: seed all 200 MA 2026 general election legislative race rows (40 STATE_UPPER + 160 STATE_LOWER) via a single-SQL CTE-JOIN INSERT. The ON CONFLICT guard idempotently skips the 5 pre-existing legislative race rows from v5.0. Satisfies MA-ELECTIONS-03.

Purpose: Without this migration, MA legislative candidates discovered by the pipeline have no race rows to attach to. This is the final DB-level step for MA 2026 elections.

Output: migration 358 SQL file + apply script; exactly 200 MA legislative race rows in production with non-null office_ids.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Transparent Motivations/essentials/.planning/phases/110-ma-2026-elections/110-RESEARCH.md
@C:/Transparent Motivations/essentials/.planning/phases/110-ma-2026-elections/110-PATTERNS.md
@C:/Transparent Motivations/essentials/.planning/phases/110-ma-2026-elections/110-01-SUMMARY.md

<interfaces>
<!-- Key patterns for the CTE-JOIN INSERT approach. -->

Chamber name guard (use this, NOT g.geo_id='25' — two governments share that geo_id):
  ch.name IN ('Massachusetts Senate', 'Massachusetts House of Representatives')

Position name derivation:
  STATE_UPPER: 'MA State Senate ' || regexp_replace(o.title, '^Senator, ', '')
  STATE_LOWER: 'MA House ' || regexp_replace(o.title, '^Representative, ', '')

ON CONFLICT guard for races:
  ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING

Ledger INSERT required at end of migration SQL:
  INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('358') ON CONFLICT (version) DO NOTHING;

Apply script run directory: C:/EV-Accounts/backend/
Apply command: npx tsx scripts/_apply-migration-358.ts

Pre-existing legislative race rows to expect idempotent conflict on (5 rows):
  - MA State Senate 2nd Middlesex District (primary)
  - MA House 25th Middlesex District (general)
  - MA House 26th Middlesex District (general)
  - MA State Senate 2nd Middlesex District (general)
  - MA State Senate Middlesex and Suffolk District (general)
  - MA State Senate Suffolk and Middlesex District (general)
  (3 senate + 2 house in general = 5 conflicts in general, ON CONFLICT DO NOTHING skips them)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 358 SQL (200 legislative races via CTE-JOIN)</name>
  <files>C:/EV-Accounts/backend/migrations/358_ma_2026_legislative_races.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/280_md_2026_legislative_races.sql (structural analog — but NOTE: MA uses single-CTE-JOIN, NOT the per-DO-$$ pattern used in MD; read for ledger + post-verify pattern only)
    - C:/Transparent Motivations/essentials/.planning/phases/110-ma-2026-elections/110-RESEARCH.md (Pattern 4 CTE-JOIN SQL, Pitfall 2 wrong government JOIN, Pitfall 5 pre-existing rows, Validation Architecture — Plan 02 post-verify block)
    - C:/Transparent Motivations/essentials/.planning/phases/110-ma-2026-elections/110-PATTERNS.md (migration 358 section — the exact CTE-JOIN SQL with ch.name filter)
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/migrations/358_ma_2026_legislative_races.sql with these sections:

    SECTION 1 — Single-statement CTE-JOIN INSERT:
    Use two CTEs: gen_elec (resolves the general election id by name='2026 Massachusetts General Election' AND state='MA') and leg_offices (derives all 200 office_ids + position_names via JOIN on offices, chambers, governments, districts).

    The leg_offices CTE must:
    - JOIN essentials.offices o ON o.chamber_id = ch.id
    - JOIN essentials.chambers ch ON ch.id = o.chamber_id  (correct ordering: offices JOIN chambers)
    - JOIN essentials.governments g ON ch.government_id = g.id
    - JOIN essentials.districts d ON o.district_id = d.id
    - Filter: ch.name IN ('Massachusetts Senate', 'Massachusetts House of Representatives')
    - Filter: d.district_type IN ('STATE_UPPER', 'STATE_LOWER')
    - Compute position_name with CASE: STATE_UPPER -> 'MA State Senate ' || regexp_replace(o.title, '^Senator, ', ''); else 'MA House ' || regexp_replace(o.title, '^Representative, ', '')

    INSERT INTO essentials.races (election_id, office_id, position_name, primary_party, seats)
    SELECT gen_elec.id, leg_offices.office_id, leg_offices.position_name, NULL, 1
    FROM gen_elec, leg_offices
    ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING;

    Do NOT include g.geo_id = '25' as a filter — use ch.name IN (...) instead (see Pitfall 2).
    Do NOT use a per-DO-$$ pattern (that is the MD approach with hardcoded districts; MA uses named districts derived from office titles).

    SECTION 2 — Ledger INSERT:
    INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('358') ON CONFLICT (version) DO NOTHING;

    SECTION 3 — Post-verify DO $$ block:
    Declare v_leg_count INT and v_null_count INT.
    Check 1: COUNT of races in MA general election WHERE position_name LIKE 'MA State Senate%' OR position_name LIKE 'MA House%' — RAISE EXCEPTION if != 200.
    Check 2: COUNT of MA races with NULL office_id (across all MA elections) — RAISE EXCEPTION if > 0.
  </action>
  <verify>
    <automated>
      After file is written, confirm it contains the CTE approach (not per-DO-$$ blocks):
      grep -c "ch.name IN" "C:/EV-Accounts/backend/migrations/358_ma_2026_legislative_races.sql" (expect >= 1)
      grep -c "regexp_replace" "C:/EV-Accounts/backend/migrations/358_ma_2026_legislative_races.sql" (expect >= 2 — one per district type)
    </automated>
  </verify>
  <acceptance_criteria>
    - File 358_ma_2026_legislative_races.sql exists at C:/EV-Accounts/backend/migrations/
    - File uses single CTE-JOIN INSERT (NOT 200 separate DO $$ blocks)
    - CTE filters on ch.name IN ('Massachusetts Senate', 'Massachusetts House of Representatives') — NOT on g.geo_id='25'
    - CASE expression derives position_name using regexp_replace to strip 'Senator, ' or 'Representative, ' prefix
    - ON CONFLICT guard present on the INSERT
    - Ledger INSERT for version '358' present
    - Post-verify block checks count=200 for legislative races and count=0 for NULL office_id races
  </acceptance_criteria>
  <done>migration 358 SQL written with CTE-JOIN pattern ready to create 200 legislative race rows idempotently</done>
</task>

<task type="auto">
  <name>Task 2: Write apply script 358 and execute migration</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-358.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-280.ts (structural analog for apply script — try/catch/finally style with pool.end() in finally; swap queries for MA)
    - C:/EV-Accounts/backend/migrations/358_ma_2026_legislative_races.sql (just written in Task 1)
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/scripts/_apply-migration-358.ts using the try/catch/finally style from _apply-migration-280.ts.

    Pool setup: import 'dotenv/config', import Pool from 'pg', import readFileSync + path. Pool with { connectionString: process.env['DATABASE_URL'], ssl: { rejectUnauthorized: false } }.

    Read SQL: readFileSync(path.join(process.cwd(), 'migrations', '358_ma_2026_legislative_races.sql'), 'utf8').

    Inside try block:
    - await pool.query(sql) — applies the full migration
    - console.log('Migration 358 applied successfully')

    Smoke test 1: SELECT COUNT(*) as cnt FROM essentials.races r JOIN essentials.elections e ON e.id = r.election_id WHERE e.state='MA' AND r.position_name LIKE 'MA State Senate%' AND e.name='2026 Massachusetts General Election' — log result, label "expected 40"

    Smoke test 2: SELECT COUNT(*) as cnt FROM essentials.races r JOIN essentials.elections e ON e.id = r.election_id WHERE e.state='MA' AND r.position_name LIKE 'MA House%' AND e.name='2026 Massachusetts General Election' — log result, label "expected 160"

    Smoke test 3: SELECT COUNT(*) as cnt FROM essentials.races r JOIN essentials.elections e ON e.id = r.election_id WHERE e.state='MA' AND e.name='2026 Massachusetts General Election' AND (r.position_name LIKE 'MA State Senate%' OR r.position_name LIKE 'MA House%') — log result, label "expected 200"

    Smoke test 4: SELECT COUNT(*) as cnt FROM essentials.races r JOIN essentials.elections e ON e.id = r.election_id WHERE e.state='MA' AND r.office_id IS NULL — log result, label "expected 0"

    Smoke test 5: SELECT version FROM supabase_migrations.schema_migrations WHERE version='358' — log PRESENT or MISSING

    Catch block: console.error('Error applying migration 358:', e.message); process.exit(1).
    Finally block: await pool.end().

    Then execute from C:/EV-Accounts/backend/ directory:
    cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-358.ts

    Verify all 5 smoke test outputs match expected values (40, 160, 200, 0, PRESENT).
  </action>
  <verify>
    <automated>
      cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-358.ts
      (All console.log outputs must show: State Senate=40, House=160, total=200, NULL office_id=0, ledger=PRESENT)
    </automated>
  </verify>
  <acceptance_criteria>
    - _apply-migration-358.ts exists at C:/EV-Accounts/backend/scripts/
    - Script uses try/catch/finally with pool.end() in finally block (not main().catch() style — either is valid but follow the 280 analog)
    - npx tsx run completes without error (exit code 0)
    - Smoke test 1 output: "MA State Senate race rows: 40 (expected 40)"
    - Smoke test 2 output: "MA House race rows: 160 (expected 160)"
    - Smoke test 3 output: "Total MA legislative race rows: 200 (expected 200)"
    - Smoke test 4 output: "MA races with NULL office_id: 0 (expected 0)"
    - Smoke test 5 output: "Ledger entry 358: PRESENT"
  </acceptance_criteria>
  <done>Migration 358 applied; 200 MA legislative races exist with non-null office_ids; MA-ELECTIONS-03 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apply script → DB | Node.js script connecting via DATABASE_URL; SQL is a local file, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-03 | Tampering | CTE-JOIN INSERT into essentials.races | accept | ON CONFLICT guard is idempotent; chamber name filter scopes to MA legislature only |
| T-110-04 | Information Disclosure | DATABASE_URL in .env | accept | .env is local to C:/EV-Accounts/backend; not committed to git |
| T-110-SC | Tampering | npm/npx installs | accept | No new packages; using existing pg + tsx from node_modules |
</threat_model>

<verification>
After Task 2 completes, run via mcp__supabase-local__execute_sql:
1. `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON r.election_id=e.id WHERE e.state='MA' AND e.name='2026 Massachusetts General Election' AND r.position_name LIKE 'MA State Senate%'` → 40
2. `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON r.election_id=e.id WHERE e.state='MA' AND e.name='2026 Massachusetts General Election' AND r.position_name LIKE 'MA House%'` → 160
3. `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON r.election_id=e.id WHERE e.state='MA' AND r.office_id IS NULL` → 0
4. `SELECT version FROM supabase_migrations.schema_migrations WHERE version='358'` → row present
</verification>

<success_criteria>
- Migration 358 SQL file created and applied without error
- Exactly 40 MA State Senate race rows exist for the 2026 general election
- Exactly 160 MA House race rows exist for the 2026 general election
- Total MA legislative races = 200 (verified by both SQL and smoke test)
- Zero MA race rows have NULL office_id
- Ledger entry 358 present in supabase_migrations.schema_migrations
- MA-ELECTIONS-03 satisfied
</success_criteria>

<output>
Create .planning/phases/110-ma-2026-elections/110-02-SUMMARY.md when done
</output>
