---
phase: 077-portland-officials
plan: "02"
type: execute
wave: 2
depends_on: ["077-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/231_portland_officials.sql
autonomous: true
requirements: []
tags: [portland, oregon, politicians, offices, incumbents, council, mayor, auditor, appointed]
must_haves:
  truths:
    - "16 Portland politicians exist with external_ids -690001..-690004 and -690010..-690021"
    - "Each of the 16 politicians has a non-null office_id back-filled"
    - "12 council offices link to the correct portland-or-council-district-{1-4} (3 per district)"
    - "Mayor, City Auditor, City Administrator, City Attorney offices all link to LOCAL_EXEC district geo_id='4159000'"
    - "City Administrator (Lee III) and City Attorney (Taylor) have is_appointed_position=true; the other 14 have is_appointed_position=false"
    - "Portland City Hall (lat=45.5231, lon=-122.6794) routes to 3 District 4 council members (Zimmerman, Green, Clark) + Mayor Keith Wilson"
    - "Section-split detector returns clean counts (City Council=12, Mayor=1, City Auditor=1, City Administrator=1, City Attorney=1)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/231_portland_officials.sql"
      provides: "16 politicians + 16 offices + office_id back-fill"
      contains: "external_id BETWEEN -690021 AND -690001"
  key_links:
    - from: "essentials.offices.district_id (council)"
      to: "essentials.districts (portland-or-council-district-{1-4}, district_type='LOCAL', state='or')"
      via: "geo_id + district_type + state match"
      pattern: "d.geo_id = 'portland-or-council-district-N' AND d.district_type='LOCAL' AND d.state='or'"
    - from: "essentials.offices.district_id (citywide)"
      to: "essentials.districts (geo_id='4159000', district_type='LOCAL_EXEC', state='or')"
      via: "geo_id + district_type IN ('LOCAL','LOCAL_EXEC') + state match"
      pattern: "d.geo_id = '4159000' AND d.district_type IN ('LOCAL','LOCAL_EXEC') AND d.state='or'"
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id"
      via: "back-fill UPDATE at end of migration"
      pattern: "UPDATE essentials.politicians SET office_id = o.id ... WHERE p.external_id BETWEEN -690021 AND -690001"
---

<objective>
Seed 16 Portland officials with their office rows, link each office to the correct district, and back-fill politicians.office_id so Plan 77-03's headshot work-list query returns 14 elected officials.

Purpose: This plan delivers the human-readable seed of Portland's government — the 14 elected officials (Mayor + 12 council + City Auditor) plus 2 appointed officials (City Administrator + City Attorney). All identifier corrections from RESEARCH.md CF-1/CF-2/CF-3 are applied: City Attorney is appointed (not elected per D-05); the 12 council names are the verified roster (not CONTEXT.md D-06); City Administrator is Raymond C. Lee III (not Michael Jordan).
Output: One applied SQL migration (231) producing 16 new politicians rows, 16 new offices rows, all 16 politicians with non-null office_id, and the section-split detector showing clean per-chamber counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/077-portland-officials/077-CONTEXT.md
@.planning/phases/077-portland-officials/077-RESEARCH.md
@.planning/phases/077-portland-officials/077-PATTERNS.md
@.planning/phases/077-portland-officials/077-01-SUMMARY.md
@.planning/phases/68-berkeley-deep-seed/68-01-SUMMARY.md
@.planning/phases/63-sf-deep-seed/63-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write and apply migration 231 — 16 politicians + 16 offices + back-fill</name>
  <files>C:/EV-Accounts/backend/migrations/231_portland_officials.sql</files>
  <read_first>
    - .planning/phases/077-portland-officials/077-RESEARCH.md (Critical Findings CF-1/CF-2/CF-3, Verified External ID Layout, Pattern 2 WITH ins_p CTE, Pitfalls 1–5)
    - .planning/phases/077-portland-officials/077-PATTERNS.md (Pattern Assignments → 231_portland_officials.sql; full 16-row roster table; Back-fill UPDATE pattern; verification queries)
    - .planning/phases/077-portland-officials/077-01-SUMMARY.md (confirms migration 230 applied; 5 chambers created; LOCAL_EXEC district exists)
    - C:/EV-Accounts/backend/migrations/214_berkeley_officials.sql (closest analog — Mayor + City Auditor + Council Members with WITH ins_p CTE + back-fill UPDATE)
    - C:/EV-Accounts/backend/migrations/206_sf_officials.sql (analog for appointed officials with is_appointed_position=true)
    - C:/EV-Accounts/backend/migrations/229_portland_council_districts.sql (confirms portland-or-council-district-{1-4} state='or' district_type='LOCAL')
  </read_first>
  <action>
    Run all 6 pre-flight verification queries from 077-RESEARCH.md "Pre-flight Verification Queries" against the live DB. Confirm:
      - Query 1: 0 rows in `essentials.politicians WHERE external_id BETWEEN -699999 AND -690000` (range clear)
      - Query 6 (name collision): 0 rows for the 16 full_name values (Keith Wilson, Simone Rede, Candace Avalos, Jamie Dunphy, Loretta Smith, Dan Ryan, Elana Pirtle-Guiney, Sameer Kanal, Angelita Morillo, Steve Novick, Tiffany Koyama Lane, Eric Zimmerman, Mitch Green, Olivia Clark, Raymond C. Lee III, Robert L. Taylor)
      - 077-01-SUMMARY confirms: 1 government row, 5 chambers, 1 LOCAL_EXEC district, ledger 230 present

    If any pre-flight check fails or a name collision is detected, STOP and surface — do NOT proceed.

    Create `C:/EV-Accounts/backend/migrations/231_portland_officials.sql` wrapping all statements in `BEGIN; ... COMMIT;`. The file MUST contain 16 separate `WITH ins_p AS ( INSERT ... RETURNING id ) INSERT INTO essentials.offices ...` blocks, one per official, in this EXACT order using these EXACT values:

    Per the 077-PATTERNS.md "16-official roster" table and the planning_context corrections (D-05 City Attorney appointed, D-06 corrected roster, D-04 Lee III):

    Block 1 — Mayor Keith Wilson (-690001):
      - politicians: full_name='Keith Wilson', first_name='Keith', last_name='Wilson', party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id=-690001
      - office: title='Mayor', representing_state='OR', is_appointed_position=false, is_vacant=false, role_canonical=NULL
      - chamber subquery: name='Mayor' under City of Portland, Oregon, US (state='OR')
      - district WHERE: d.geo_id='4159000' AND d.district_type IN ('LOCAL','LOCAL_EXEC') AND d.state='or'

    Block 2 — City Auditor Simone Rede (-690002, ELECTED):
      - politicians: full_name='Simone Rede', first_name='Simone', last_name='Rede', external_id=-690002, is_appointed=false, all other flags as Wilson
      - office: title='City Auditor', is_appointed_position=false
      - chamber: name='City Auditor'
      - district: same LOCAL_EXEC WHERE as Wilson

    Block 3 — City Administrator Raymond C. Lee III (-690003, APPOINTED — per CF-3, NOT Michael Jordan):
      - politicians: full_name='Raymond C. Lee III', first_name='Raymond', last_name='Lee', external_id=-690003, is_appointed=false (column on politicians table — leave false; the office row carries the appointed flag), other flags as Wilson
      - office: title='City Administrator', is_appointed_position=TRUE (per D-04)
      - chamber: name='City Administrator'
      - district: same LOCAL_EXEC WHERE as Wilson

    Block 4 — City Attorney Robert L. Taylor (-690004, APPOINTED — per CF-1, overrides CONTEXT D-05):
      - politicians: full_name='Robert L. Taylor', first_name='Robert', last_name='Taylor', external_id=-690004, is_appointed=false on politicians row, flags as Wilson
      - office: title='City Attorney', is_appointed_position=TRUE (per CF-1)
      - chamber: name='City Attorney'
      - district: same LOCAL_EXEC WHERE as Wilson

    Blocks 5–7 — District 1 council members (per CF-2 corrected roster, NOT CONTEXT D-06):
      - 5: Candace Avalos (first='Candace', last='Avalos'), external_id=-690010
      - 6: Jamie Dunphy (first='Jamie', last='Dunphy'), external_id=-690011
      - 7: Loretta Smith (first='Loretta', last='Smith'), external_id=-690012
      - office title for all 3: 'City Councilor (District 1)' per critical_corrections override of D-07
      - chamber: name='City Council'
      - district WHERE: d.geo_id='portland-or-council-district-1' AND d.district_type='LOCAL' AND d.state='or'

    Blocks 8–10 — District 2 council members (per CF-2):
      - 8: Dan Ryan, external_id=-690013
      - 9: Elana Pirtle-Guiney (first='Elana', last='Pirtle-Guiney' — preserve hyphen), external_id=-690014
      - 10: Sameer Kanal (first='Sameer', last='Kanal'), external_id=-690015
      - office title: 'City Councilor (District 2)'
      - district WHERE: d.geo_id='portland-or-council-district-2' (state='or')

    Blocks 11–13 — District 3 council members (per CF-2):
      - 11: Angelita Morillo, external_id=-690016
      - 12: Steve Novick, external_id=-690017
      - 13: Tiffany Koyama Lane (first='Tiffany', last='Koyama Lane' — preserve space in last name), external_id=-690018
      - office title: 'City Councilor (District 3)'
      - district WHERE: d.geo_id='portland-or-council-district-3' (state='or')

    Blocks 14–16 — District 4 council members (per CF-2):
      - 14: Eric Zimmerman, external_id=-690019
      - 15: Mitch Green, external_id=-690020
      - 16: Olivia Clark, external_id=-690021
      - office title: 'City Councilor (District 4)'
      - district WHERE: d.geo_id='portland-or-council-district-4' (state='or')

    Per-block CTE structure (every one of the 16 blocks follows the 077-PATTERNS.md "WITH ins_p CTE pattern"):
      WITH ins_p AS (
        INSERT INTO essentials.politicians
          (id, full_name, first_name, last_name, party, is_active, is_appointed,
           is_vacant, is_incumbent, external_id)
        VALUES (gen_random_uuid(), '<full_name>', '<first>', '<last>', NULL, true, false, false, true, <external_id>)
        ON CONFLICT (external_id) DO NOTHING
        RETURNING id
      )
      INSERT INTO essentials.offices
        (id, district_id, chamber_id, politician_id, title, representing_state,
         is_appointed_position, is_vacant, role_canonical)
      SELECT gen_random_uuid(),
             d.id,
             (SELECT id FROM essentials.chambers
              WHERE name='<chamber name>'
                AND government_id=(SELECT id FROM essentials.governments
                                   WHERE name='City of Portland, Oregon, US' AND state='OR')),
             p.id,
             '<office title>', 'OR', <is_appointed_position>, false, NULL
      FROM essentials.districts d
      CROSS JOIN ins_p p
      WHERE <district WHERE clause for this block>
        AND p.id IS NOT NULL
        AND NOT EXISTS (
          SELECT 1 FROM essentials.offices o
          WHERE o.district_id = d.id AND o.politician_id = p.id
        );

    Final statement BEFORE COMMIT — back-fill UPDATE (per 077-PATTERNS.md "Back-fill UPDATE"):
      UPDATE essentials.politicians p
      SET office_id = o.id
      FROM essentials.offices o
      WHERE o.politician_id = p.id
        AND p.external_id BETWEEN -690021 AND -690001
        AND p.office_id IS NULL;

    Ledger entry (after the back-fill UPDATE, before COMMIT):
      INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('231') ON CONFLICT (version) DO NOTHING;

    Constraints (077-PATTERNS.md "Shared Patterns"):
      - party MUST be NULL on every politicians row (antipartisan rule)
      - offices column list MUST be exactly: id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical — no seat_label, no email, no is_active
      - is_appointed_position MUST be true ONLY on -690003 (Lee III, City Administrator) and -690004 (Taylor, City Attorney); false on the other 14
      - title format: 'City Councilor (District N)' for all 12 council offices (per critical_corrections override of D-07; matches portland.gov display text)
      - Do NOT use 'City Council Member (District N)' (the CONTEXT D-07 wording — superseded)
      - Do NOT seed Michael Jordan as City Administrator (CF-3 — Lee III is current)
      - Do NOT seed any of these names from CONTEXT D-06: Timur Ataseven, Tiffany Kachima, Maxine Dexter, Chris Carey, Jonathan Tasini (CF-2 corrections)
      - Hyphens and spaces in names preserved exactly: 'Elana Pirtle-Guiney', 'Tiffany Koyama Lane', 'Raymond C. Lee III', 'Robert L. Taylor'

    Apply the migration to the live DB via Supabase Management API. Then run the 5 post-migration verification queries from 077-PATTERNS.md (politicians count, office count by chamber, is_appointed_position breakdown, District 4 routing, section-split detector) plus the LOCAL_EXEC Mayor routing query from 077-RESEARCH.md.
  </action>
  <verify>
    <automated>
SQL via Supabase Management API after migration 231 is applied:

-- Gate 1: 16 politicians seeded in range
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -690021 AND -690001;
-- Expected: 16

-- Gate 2: Every one of those 16 politicians has a non-null office_id (back-fill worked)
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -690021 AND -690001 AND office_id IS NULL;
-- Expected: 0

-- Gate 3: Office count by chamber (section-split detector — should be clean)
SELECT ch.name, COUNT(*) AS office_count
FROM essentials.offices o
JOIN essentials.politicians p ON p.id = o.politician_id
JOIN essentials.chambers ch ON ch.id = o.chamber_id
WHERE p.external_id BETWEEN -690021 AND -690001
GROUP BY ch.name ORDER BY ch.name;
-- Expected exactly: City Administrator=1, City Attorney=1, City Auditor=1, City Council=12, Mayor=1

-- Gate 4: is_appointed_position split — exactly 2 true (Lee III, Taylor) and 14 false
SELECT p.full_name, o.is_appointed_position
FROM essentials.offices o
JOIN essentials.politicians p ON p.id = o.politician_id
WHERE p.external_id BETWEEN -690021 AND -690001
  AND o.is_appointed_position = true
ORDER BY p.full_name;
-- Expected: exactly 2 rows — 'Raymond C. Lee III', 'Robert L. Taylor'

SELECT COUNT(*) FROM essentials.offices o
JOIN essentials.politicians p ON p.id = o.politician_id
WHERE p.external_id BETWEEN -690021 AND -690001
  AND o.is_appointed_position = false;
-- Expected: 14

-- Gate 5: District 4 routing — Portland City Hall returns 3 council members
SELECT p.full_name, o.title
FROM essentials.geofence_boundaries gb
JOIN essentials.districts d ON d.geo_id = gb.geo_id
JOIN essentials.offices o ON o.district_id = d.id
JOIN essentials.politicians p ON p.id = o.politician_id
WHERE gb.mtfcc = 'X0012'
  AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint(-122.6794, 45.5231), 4326))
  AND o.is_vacant = false
ORDER BY p.full_name;
-- Expected: exactly 3 rows — Eric Zimmerman, Mitch Green, Olivia Clark — all title='City Councilor (District 4)'

-- Gate 6: Mayor routing via LOCAL_EXEC
SELECT p.full_name, o.title
FROM essentials.districts d
JOIN essentials.offices o ON o.district_id = d.id
JOIN essentials.politicians p ON p.id = o.politician_id
WHERE d.geo_id = '4159000' AND d.district_type = 'LOCAL_EXEC' AND d.state = 'or'
  AND p.external_id = -690001;
-- Expected: 1 row | full_name='Keith Wilson' | title='Mayor'

-- Gate 7: Council title format check — must be 'City Councilor (District N)' for all 12
SELECT DISTINCT o.title
FROM essentials.offices o
JOIN essentials.politicians p ON p.id = o.politician_id
JOIN essentials.chambers ch ON ch.id = o.chamber_id
WHERE p.external_id BETWEEN -690010 AND -690021
  AND ch.name='City Council';
-- Expected exactly: 'City Councilor (District 1)','City Councilor (District 2)','City Councilor (District 3)','City Councilor (District 4)'

-- Gate 8: corrected roster spot-check — none of the wrong-names from CONTEXT D-06 present
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -690010 AND -690021
  AND full_name IN ('Timur Ataseven','Tiffany Kachima','Maxine Dexter','Chris Carey','Jonathan Tasini');
-- Expected: 0

-- Gate 9: Raymond C. Lee III (not Michael Jordan) seeded as City Administrator
SELECT p.full_name FROM essentials.politicians p
JOIN essentials.offices o ON o.id = p.office_id
JOIN essentials.chambers ch ON ch.id = o.chamber_id
WHERE ch.name = 'City Administrator'
  AND p.external_id = -690003;
-- Expected: 1 row | full_name='Raymond C. Lee III' (NOT 'Michael Jordan')

-- Gate 10: ledger entry recorded + idempotency (re-run migration; rowCount=0 on every INSERT)
SELECT version FROM supabase_migrations.schema_migrations WHERE version='231';
-- Expected: 1 row

-- After re-applying migration 231, every INSERT must return rowCount=0
-- and the back-fill UPDATE must return rowCount=0 (all office_ids already populated)
    </automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/231_portland_officials.sql` exists and is wrapped in `BEGIN; ... COMMIT;`
    - File contains exactly 16 `WITH ins_p AS` CTE blocks (one per politician)
    - File contains all 16 external_id integer literals: -690001, -690002, -690003, -690004, -690010, -690011, -690012, -690013, -690014, -690015, -690016, -690017, -690018, -690019, -690020, -690021
    - File contains all 16 verified full_name strings: 'Keith Wilson', 'Simone Rede', 'Raymond C. Lee III', 'Robert L. Taylor', 'Candace Avalos', 'Jamie Dunphy', 'Loretta Smith', 'Dan Ryan', 'Elana Pirtle-Guiney', 'Sameer Kanal', 'Angelita Morillo', 'Steve Novick', 'Tiffany Koyama Lane', 'Eric Zimmerman', 'Mitch Green', 'Olivia Clark'
    - File does NOT contain the substring 'Michael Jordan' (CF-3 — wrong administrator)
    - File does NOT contain the substring 'Timur Ataseven', 'Tiffany Kachima', 'Maxine Dexter', 'Chris Carey', or 'Jonathan Tasini' (CF-2 — wrong council roster)
    - File contains the literal `'City Councilor (District 1)'`, `'City Councilor (District 2)'`, `'City Councilor (District 3)'`, `'City Councilor (District 4)'` (one for each district, 3 occurrences each = 12 total)
    - File does NOT contain the substring `'City Council Member (District ` (the superseded D-07 wording)
    - File contains exactly 2 instances of `is_appointed_position` with value `true` in the SELECT column lists, and 14 with `false` (or expressible structurally — confirm via Gate 4 SQL)
    - File contains the back-fill UPDATE statement with `WHERE p.external_id BETWEEN -690021 AND -690001 AND p.office_id IS NULL`
    - File contains the offices column list exactly `(id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)` — no `seat_label`, `email`, or `is_active`
    - File does NOT contain the substring `seat_label` (case-insensitive)
    - File does NOT contain `slug` inside any INSERT column list (case-insensitive)
    - Every politicians INSERT VALUES has `party=NULL` (or `NULL` in the party position)
    - Gate 1 returns 16
    - Gate 2 returns 0 (every politician back-filled)
    - Gate 3 returns the exact rows: City Administrator=1, City Attorney=1, City Auditor=1, City Council=12, Mayor=1
    - Gate 4: 2 appointed rows ('Raymond C. Lee III', 'Robert L. Taylor'); 14 elected rows
    - Gate 5 returns exactly 3 rows: Eric Zimmerman, Mitch Green, Olivia Clark — each with title='City Councilor (District 4)'
    - Gate 6 returns 1 row: Keith Wilson / Mayor
    - Gate 7 returns exactly 4 distinct council title strings (one per district, with 'City Councilor' not 'City Council Member')
    - Gate 8 returns 0 (no wrong-roster names)
    - Gate 9 returns 1 row 'Raymond C. Lee III' (not 'Michael Jordan')
    - Gate 10: ledger has '231'; re-running migration 231 produces 0 new rows on every INSERT and 0 row updates on the back-fill UPDATE
  </acceptance_criteria>
  <done>
    Migration 231 file exists; migration is applied to the live Supabase DB; all 10 verification gates pass; Portland City Hall routing returns the 3 D4 council members + Mayor Wilson via LOCAL_EXEC; ledger '231' recorded; idempotent on re-run.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes, the following must all be true (this is roadmap SC-1, SC-2, SC-3 for Phase 77):
- 16 politicians (external_ids -690001..-690004 + -690010..-690021), all with non-null office_id
- 16 offices: 1 Mayor + 1 City Auditor + 1 City Administrator + 1 City Attorney + 12 council
- 2 offices is_appointed_position=true (Lee III + Taylor), 14 false
- Portland City Hall (-122.6794, 45.5231) routes to D4 → Zimmerman + Green + Clark (3 rows)
- Mayor Wilson routes via LOCAL_EXEC district (geo_id='4159000')
- `supabase_migrations.schema_migrations` contains '231'
- Re-applying migration 231 inserts 0 new rows (idempotent)
</verification>

<success_criteria>
Plan 77-03's headshot work-list query returns exactly 14 elected officials (Mayor + 12 council + City Auditor; City Administrator and City Attorney excluded because `o.is_appointed_position = false` filters them out).
</success_criteria>

<output>
Create `.planning/phases/077-portland-officials/077-02-SUMMARY.md` documenting:
- Pre-flight verification results (range clear, name collisions absent)
- Migration 231 applied (Supabase API response excerpt)
- All 10 verification gate outputs
- Idempotency re-run output (every INSERT and UPDATE rowCount=0)
- Routing verification: District 4 → 3 council members + Mayor Wilson via LOCAL_EXEC
</output>
