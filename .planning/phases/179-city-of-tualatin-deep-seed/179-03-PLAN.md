---
phase: 179-city-of-tualatin-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["179-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-tualatin-headshots.py
  - C:/EV-Accounts/backend/migrations/1170_tualatin_headshots.sql
autonomous: false
requirements: [WASH-05]

must_haves:
  truths:
    - "All 7 Tualatin officials have a 600×750 headshot uploaded to Supabase Storage politician_photos/{uuid}-headshot.jpg (7/7 expected — RESEARCH confirmed zero genuine gaps, unlike Tigard)"
    - "Every headshot was cropped to 4:5 FIRST, then resized to 600×750 Lanczos q90 (no distortion, no overlays)"
    - "All 7 sources are official tualatinoregon.gov/app/uploads/ portraits (NO WAF — best sourcing in the milestone; D-16 fallback chain NOT needed, documented explicitly)"
    - "One politician_images row per official (type='default', photo_license='press_use'), columns exactly (id, politician_id, url, type, photo_license) — NO photo_origin_url; guarded WHERE NOT EXISTS on politician_id"
    - "No image has superimposed text/graphics over the face; each is a valid non-zero JPEG (HTTP 200 from CDN)"
    - "The headshot script HARD-asserts len(OFFICIALS)==7 (Hillsboro-style) — a missing entry indicates a real regression (redesign path move), not an expected gap"
    - "Decision coverage — D-09: headshots sourced tualatinoregon.gov-first, crop-to-4:5 then resize 600×750 Lanczos q90, {uuid}-headshot.jpg naming, photo_license set by actual source, no fabrication; D-16: pre-authorized local-news fallback chain documented as NOT needed (7/7 from the official site)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-tualatin-headshots.py"
      provides: "Download → composite → crop 4:5 → resize 600×750 → upload pipeline for all 7 officials"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/1170_tualatin_headshots.sql"
      provides: "politician_images INSERTs for all 7 officials (audit-only, NOT registered in the ledger) with the url-embeds-uuid WR-02 identity gate"
      min_lines: 20
      contains: "politician_images"
  key_links:
    - from: "_tmp-tualatin-headshots.py"
      to: "Supabase Storage politician_photos bucket"
      via: "PUT {uuid}-headshot.jpg x-upsert:true"
      pattern: "politician_photos"
    - from: "1170_tualatin_headshots.sql"
      to: "essentials.politicians (by external_id)"
      via: "subselect politician_id per row"
      pattern: "external_id = -41749"
---

<objective>
Author the headshot pipeline script and the audit-only migration that records the 7 Tualatin
officials' portraits. Unlike Beaverton/Hillsboro/Tigard (all Akamai WAF-403 on their primary domain),
tualatinoregon.gov has NO WAF — all 7 portraits are hosted directly at /app/uploads/2025/09/ and were
confirmed downloadable in RESEARCH. Portraits are cropped 4:5 FIRST then resized to 600×750 Lanczos
q90 and mirrored to Supabase Storage. This is the cleanest headshot situation in the milestone: expect
7/7, HARD-assert on it (Hillsboro-style), and NO D-16 fallback chain is needed (document that
explicitly).

Purpose: Satisfy the headshot portion of WASH-05 — all 7 officials render with 600×750 photos.
Output: `_tmp-tualatin-headshots.py` (gitignored helper, run by the orchestrator) + audit-only
migration `1170_tualatin_headshots.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-RESEARCH.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES both files. It has NO DB/Storage access and NO
WebSearch. The INLINE ORCHESTRATOR runs `_tmp-tualatin-headshots.py` (which resolves each politician
UUID by external_id at runtime via psycopg2, downloads all 7 from tualatinoregon.gov, processes, and
uploads), then applies the audit migration via `psql -f`. The `.py` file is a gitignored `_tmp-*`
helper — do NOT commit it. The `.sql` migration IS committed to the EV-Accounts repo.

**AUDIT-ONLY MIGRATION:** `1170_tualatin_headshots.sql` does NOT write a schema_migrations ledger row
(headshot/stance migrations bypass the ledger; on-disk counter is authoritative). Keep the literal
strings `slug`, `schema_migrations`, and `photo_origin_url` OUT of comments.
</context>

<interfaces>
essentials.politician_images  (id, politician_id, url, type, photo_license)  -- NO photo_origin_url column; type='default'
-- politician UUIDs: resolve by external_id from 179-02-SUMMARY.md:
--   -4174951 Frank Bubenik (Mayor) | -4174952 María Reyes | -4174953 Christen Sacco | -4174954 Bridget Brooks
--   -4174955 Cyndy Hillier | -4174956 Octavio Gonzalez | -4174957 Valerie Pratt
-- Confirmed source URLs (RESEARCH §"Headshot Sources", all HTTP 200, no WAF):
--   Bubenik  -> https://tualatinoregon.gov/app/uploads/2025/09/Home_Mayor.jpg          (1250×1330, 185KB)
--   Reyes    -> https://tualatinoregon.gov/app/uploads/2025/09/Council_Maria-Reyes.jpg (484×484)
--   Sacco    -> https://tualatinoregon.gov/app/uploads/2025/09/Council_Christen-Sacco.jpg
--   Brooks   -> https://tualatinoregon.gov/app/uploads/2025/09/Council_Bridget-Brooks.jpg
--   Hillier  -> https://tualatinoregon.gov/app/uploads/2025/09/Council_Cyndy-Hillier.jpg
--   Gonzalez -> https://tualatinoregon.gov/app/uploads/2025/09/Council_Octavio-Gonzalez.jpg
--   Pratt    -> https://tualatinoregon.gov/app/uploads/2025/09/Council_Valerie-Pratt.jpg
-- All fallback_url values are None (no fallback chain needed — D-16 unused).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot pipeline script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-tualatin-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-tigard-headshots.py (the ~474-line pipeline template — copy structure/functions verbatim, rename city strings; gitignored but present on disk)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md §"_tmp-tualatin-headshots.py (utility, file-I/O)"
    - .planning/phases/179-city-of-tualatin-deep-seed/179-RESEARCH.md §"Headshot Sources — ALL 7 CONFIRMED, NO FALLBACK NEEDED"
    - .planning/phases/179-city-of-tualatin-deep-seed/179-02-SUMMARY.md (minted UUIDs by external_id)
  </read_first>
  <action>
    Copy the Tigard script structure (config block, crop_to_4_5, resize_600x750, process_headshot_bytes,
    transparent-composite guard, resolve_politician_id, download_image, upload_to_storage,
    process_member, manifest output, CDN_BASE-from-SUPABASE_URL derivation, descriptive User-Agent).
    Set an OFFICIALS list of 7 dicts, each with ext_id, name, url (the confirmed tualatinoregon.gov URL
    from the <interfaces> block above), license='press_use', and fallback_url=None. Resolve each
    politician UUID at RUNTIME by external_id via psycopg2 (do NOT hardcode UUIDs). Keep the config
    block: BUCKET='politician_photos', TARGET_SIZE=(600,750), JPEG_QUALITY=90,
    RESAMPLE=Image.Resampling.LANCZOS. Pipeline order per official: download (follow redirects),
    composite onto white if RGBA/PNG, CROP to 4:5 FIRST, RESIZE to 600×750 Lanczos, reject any image
    with superimposed text/graphics, upload to politician_photos/{uuid}-headshot.jpg via PUT with
    x-upsert:true, emit a manifest line (ext_id → uuid → CDN url → source). CRITICAL — unlike Tigard's
    soft partial tolerance, add a HARD assert `len(OFFICIALS) == 7` (Hillsboro-style) plus asserts that
    ext_ids are unique and every entry carries a license — RESEARCH found zero genuine gaps, so a
    missing entry indicates a real regression. Include a docstring SOURCE NOTE stating tualatinoregon.gov
    has NO WAF, all 7 sourced from the primary domain, D-16 fallback chain NOT needed, and marking the
    file a gitignored _tmp helper the orchestrator runs. No fenced code in this action — the Tigard
    analog + 179-PATTERNS.md are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `python C:/EV-Accounts/backend/scripts/_tmp-tualatin-headshots.py`; the script validates non-zero JPEG per image and prints the CDN manifest (expect 7/7)</automated>
  </verify>
  <acceptance_criteria>
    - OFFICIALS list has exactly 7 entries with ext_ids -4174951..-4174957 and the confirmed tualatinoregon.gov URLs
    - Every fallback_url is None (no fallback chain)
    - HARD assert len(OFFICIALS)==7 present (Hillsboro-style, NOT Tigard's soft check); asserts unique ext_ids + license present
    - UUIDs resolved at runtime by external_id (no hardcoded UUIDs)
    - TARGET_SIZE=(600,750), JPEG_QUALITY=90, Lanczos resample
    - Crop-to-4:5 happens BEFORE resize
    - Rejects overlay/text images
    - Docstring SOURCE NOTE records NO-WAF + D-16-unused; marks it a gitignored _tmp helper run by the orchestrator
  </acceptance_criteria>
  <done>Pipeline script authored; runs and uploads all 7 600×750 portraits from tualatinoregon.gov.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1170_tualatin_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1160_tigard_headshots.sql (128-line template — freshest WR-02 url-embeds-uuid identity gate)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md §"1170_tualatin_headshots.sql (audit-only headshot migration, CRUD)"
  </read_first>
  <action>
    Single BEGIN…COMMIT. Header comment: "AUDIT-ONLY (not registered in the ledger)". Seven
    politician_images INSERT … SELECT statements, one per official, columns EXACTLY
    (id, politician_id, url, type, photo_license) — NO photo_origin_url. politician_id resolved via
    subselect on external_id (within -4174951..-4174957). url is the final Supabase CDN path
    (politician_photos/{uuid}-headshot.jpg — the orchestrator fills each {uuid} from the script manifest
    before applying, or the executor templates them from 179-02-SUMMARY.md UUIDs). type='default'.
    photo_license='press_use' (official government-hosted portraits). Each INSERT guarded WHERE NOT
    EXISTS on politician_id (idempotent). Append the WR-02 post-verify DO block from mig 1160: count
    politician_images rows JOINed to politicians WHERE external_id BETWEEN -4174957 AND -4174951 AND
    url LIKE '%' || politician_id::text || '%' — RAISE EXCEPTION if the count <> 7 (url-embeds-uuid
    identity check; expected count is 7 with NO "lower the count" caveat since zero gaps are expected).
    NO ledger INSERT (audit-only). Keep `photo_origin_url`/`slug`/`schema_migrations` out of comments.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1170_tualatin_headshots.sql` (the WR-02 DO block RAISEs unless exactly 7 rows embed their politician uuid) then runs `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -4174957 AND -4174951)` expecting 7</automated>
  </verify>
  <acceptance_criteria>
    - Seven politician_images INSERTs; columns exactly (id, politician_id, url, type, photo_license)
    - No photo_origin_url column anywhere
    - politician_id resolved via external_id subselect (within -4174951..-4174957)
    - type='default'; photo_license='press_use'; each guarded WHERE NOT EXISTS on politician_id
    - WR-02 url-embeds-uuid DO block asserts exactly 7 rows (no gap caveat)
    - No ledger INSERT (audit-only); `photo_origin_url` does NOT appear in comments
  </acceptance_criteria>
  <done>Audit migration authored recording all 7 image rows with the url-embeds-uuid identity gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs pipeline, applies migration, verifies identity + quality</name>
  <what-built>The headshot pipeline script + audit migration. Orchestrator runs the pipeline (all 7 from tualatinoregon.gov) and applies the migration.</what-built>
  <how-to-verify>
    1. Run `python C:/EV-Accounts/backend/scripts/_tmp-tualatin-headshots.py`; confirm all 7 images
       download from tualatinoregon.gov and yield valid non-zero uploads (HTTP 200 on each CDN URL).
       Expect 7/7 — if any 404s, the mid-redesign moved the path; re-search that one URL before
       proceeding (do NOT fabricate).
    2. Apply `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1170_tualatin_headshots.sql`;
       confirm the WR-02 DO block passes and politician_images row count = 7.
    3. Visual identity + quality check on each portrait: correct person (Bubenik/Reyes/Sacco/Brooks/
       Hillier/Gonzalez/Pratt), 600×750, 4:5 framing, NO superimposed text/graphics over the face. Note
       the councilor sources are 484×484 upscaled ~1.55× (modest, acceptable); the Mayor's is
       1250×1330 (no upscale).
    4. Commit the migration: `git -C "C:/EV-Accounts" add backend/migrations/1170_tualatin_headshots.sql && git -C "C:/EV-Accounts" commit`. Do NOT commit the _tmp-*.py helper.
  </how-to-verify>
  <acceptance_criteria>
    - All 7 headshots uploaded (7/7 expected; any 404 re-searched, never fabricated)
    - politician_images row count = 7; WR-02 DO block passed
    - Each portrait is the correct person, 600×750, no overlays
    - Audit migration committed; _tmp .py NOT committed
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official source confirmation, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Portraits fetched from tualatinoregon.gov (no WAF) |
| operator → Supabase Storage | Processed images uploaded to the public politician_photos bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-10 | Spoofing | Wrong-person or mislabeled headshot | mitigate | Official-site named-file sources (Council_<Name>.jpg / Home_Mayor.jpg); Task 3 visual identity check per official before commit; WR-02 url-embeds-uuid gate ties each image row to the right politician |
| T-179-11 | Tampering | Corrupt/0-byte image or redesign-404 HTML saved as .jpg | mitigate | Script validates non-zero bytes + valid PIL open; HARD assert len==7 surfaces any missing entry; requests follows redirects; re-search any 404 rather than fabricate |
| T-179-12 | Information Disclosure | Superimposed text/graphics on portrait | mitigate | Pipeline rejects images with overlays; visual check confirms clean face |
| T-179-SC | Tampering | npm/pip/cargo installs (Pillow/psycopg2/requests) | mitigate | Pre-existing project deps used by all prior headshot phases — no NEW package install; if any pip install is needed, treat as [ASSUMED] and gate on a blocking human-verify before install |
</threat_model>

<verification>
- All 7 600×750 portraits uploaded from tualatinoregon.gov; politician_images rows = 7; WR-02 gate
  passes; identity/quality confirmed; audit migration committed.
</verification>

<success_criteria>
- All 7 officials have a 600×750 official-site-sourced headshot with no overlays; audit migration
  recorded with the url-embeds-uuid identity gate; D-16 fallback chain documented as unused.
</success_criteria>

<output>
Create `.planning/phases/179-city-of-tualatin-deep-seed/179-03-SUMMARY.md` when done, recording the
per-official image source (all tualatinoregon.gov) and confirmation of 7/7 coverage.
</output>
