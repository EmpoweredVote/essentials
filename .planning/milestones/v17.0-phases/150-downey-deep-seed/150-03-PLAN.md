---
phase: 150-downey-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["150-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/987_downey_headshots.sql
autonomous: false
requirements: [DWNY-01]

must_haves:
  truths:
    - "the exact image gap set is determined by a pre-flight image-coverage query over the 5 current members (Ortiz D1, Sosa D2/Mayor, Pemberton D3, Frometa D4, Trujillo D5), NOT hardcoded; CONTEXT confirms all seeded officials currently have 0 images (full greenfield) — the pre-flight re-confirms which of the 5 still lack a type='default' row at apply time"
    - "downeyca.org is WAF-403 to ALL curl regardless of user-agent (RESEARCH §D-04 — direct curl tests confirmed HTTP 403) — portraits are sourced by OPERATOR in-browser download + alternate hosts, NOT direct curl; the ONE confirmed-accessible direct source is Frometa's SCAG portrait (https://scag.ca.gov/sites/default/files/styles/memeber_image/public/2024-08/claudia-frometa.jpg?itok=sHDJLwxt, HTTP 200) — download it immediately at apply time (the itok token may rotate, Pitfall 7)"
    - "every uploaded portrait is the correct verified person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 FIRST (never stretched) then resized 600×750 Lanczos q90 JPEG, uploaded to Supabase Storage politician_photos at the canonical path {uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), x-upsert:true; politician_images type='default', photo_license matching the REAL source, photo_origin_url set to the source page"
    - "no fabricated photos — where no acceptable verified portrait is obtainable for a member, an honest gap is documented rather than uploading a bad/wrong-person/overlaid image as type='default' (CONTEXT D-04 honest-gap rule)"
    - "full-roster image audit confirms each of the 5 current members has exactly one type='default' row (or a documented honest gap), with no member having >1"
    - "migration 987 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 986; on-disk counter authoritative; ledger MAX stays 986)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/987_downey_headshots.sql"
      provides: "Audit-only headshot INSERT (5 current members) + photo_origin_url backfill + full-roster image-coverage audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos ({uuid}-headshot.jpg objects)"
      to: "essentials.politician_images.url (type='default')"
      via: "INSERT politician_images scoped by politician_id, guarded NOT EXISTS"
      pattern: "'default'"
---

<objective>
Give the current 5-member Downey roster (D1 Ortiz, D2 Sosa/Mayor, D3 Pemberton, D4 Frometa, D5 Trujillo) clean,
correct official headshots and confirm single canonical-image coverage. After Plan 02 the survivor chamber
7cb8a90c holds all 5 with consistent bidirectional links. CONTEXT confirms all officials currently have 0
images -> this is a FULL GREENFIELD headshot wave. The EXACT gap set is re-confirmed by a pre-flight
image-coverage query at apply time.

⚠️ downeyca.org is WAF-403 (RESEARCH §D-04 — confirmed 2026-06-20). Direct curl returns HTTP 403 with default
AND full Chrome UA (CivicPlus/CivicEngage CMS behind Cloudflare WAF). Unlike Pasadena (NO-WAF, direct curl),
Downey portraits require OPERATOR in-browser download for the 4 official-site portraits. The ONLY
confirmed-accessible DIRECT source is Frometa's SCAG portrait (download it FIRST and immediately — the SCAG
Drupal itok token may rotate, Pitfall 7). The /find-headshots skill (~/.claude/commands/find-headshots.md)
remains available for sourcing.

Per RESEARCH §D-04 the source plan per member (all currently 0 images):
- Frometa (D4, 675361): DIRECT — curl
  https://scag.ca.gov/sites/default/files/styles/memeber_image/public/2024-08/claudia-frometa.jpg?itok=sHDJLwxt
  (HTTP 200 confirmed — download immediately; if the itok URL fails, fall back to the base URL without itok or
  the operator's in-browser downeyca.org portrait). Fallback: facebook.com/claudiafrometa4downeycitycouncil.
- Sosa (D2/Mayor, 675353): OPERATOR in-browser from downeyca.org /our-city/mayor-city-council/hector-sosa-district-2.
  Fallback: campaign Facebook/Instagram, Downey Patriot news photos.
- Pemberton (D3, 675360): OPERATOR in-browser from downeyca.org (URL slug uses 'vacant-district-3' but the page
  title is Pemberton — verify identity). Fallback: facebook.com/dorothypembertonfordowney, Downey Latino News.
- Trujillo (D5, -201200): OPERATOR in-browser from downeyca.org /our-city/mayor-city-council/mario-trujillo-district-5.
  Fallback: Ballotpedia candidate page, Downey Latino News.
- Ortiz (D1, NEW from Plan 02): OPERATOR in-browser from downeyca.org (URL slug reflects prior occupant
  'timonthy-horn-district-1' — verify it is Horacio Ortiz, not Timothy Horn). Fallback:
  facebook.com/horacioortizfordowney, Downey Patriot news photos.

Processing (NON-NEGOTIABLE per project memory): for each member — visually confirm the CORRECT person (esp.
Ortiz vs the prior occupant Timothy Horn, and Pemberton vs the 'vacant' slug) and NO superimposed text/graphics
-> crop to 4:5 FIRST (never stretch — crop only, eyes ~1/3 from top, full head+shoulders) -> resize 600×750
Lanczos q90 JPEG (Python Pillow) -> upload to the Supabase Storage politician_photos bucket at the canonical
path {uuid}-headshot.jpg using SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env) with header
x-upsert:true -> INSERT politician_images (type='default', photo_license matching the REAL source — e.g.
'press_use' for news, the appropriate license for SCAG/campaign); set photo_origin_url to the source page.
Reject any source with superimposed text/graphics over the face — document a gap honestly rather than upload a
bad photo (CONTEXT D-04). Resolve each member's UUID by external_id at apply time (Ortiz's resolved ext_id
comes from the Plan 02 SUMMARY).

Purpose: A voter sees the right face next to each official. Output: Migration 987 (audit-only — NOT registered
in schema_migrations) + verified portraits for the 5 current members (Frometa via SCAG direct, the other 4 via
operator in-browser) + a human checkpoint confirming the correct person + correct crop for each; any genuine
gap documented honestly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/150-downey-deep-seed/150-RESEARCH.md
@.planning/phases/150-downey-deep-seed/150-CONTEXT.md
@.planning/phases/150-downey-deep-seed/150-02-SUMMARY.md
@.planning/phases/149-pasadena-deep-seed/149-03-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §D-04). downeyca.org is WAF-403 — operator in-browser for
     4 of 5; Frometa via SCAG direct. Pre-flight re-confirms the gap set; /find-headshots skill available. -->

Current 5 members (resolve UUID by external_id; pre-flight a coverage query — all currently 0 images):
  Horacio Ortiz (D1, NEW — resolved ext_id from Plan 02 SUMMARY) · Hector Sosa 675353 (D2, Mayor) ·
  Dorothy Pemberton 675360 (D3) · Claudia Frometa 675361 (D4) · Mario Trujillo -201200 (D5)

Source plan (RESEARCH §D-04; downeyca.org WAF-403 -> operator in-browser for 4):
  Frometa  DIRECT curl: https://scag.ca.gov/sites/default/files/styles/memeber_image/public/2024-08/claudia-frometa.jpg?itok=sHDJLwxt  (HTTP 200; download immediately — itok may rotate, Pitfall 7)
  Sosa     OPERATOR in-browser: downeyca.org /our-city/mayor-city-council/hector-sosa-district-2
  Pemberton OPERATOR in-browser: downeyca.org (slug 'vacant-district-3' — page title is Pemberton; verify identity)
  Trujillo OPERATOR in-browser: downeyca.org /our-city/mayor-city-council/mario-trujillo-district-5
  Ortiz    OPERATOR in-browser: downeyca.org (slug 'timonthy-horn-district-1' — verify it is Ortiz, NOT Timothy Horn)
  Fallbacks (RESEARCH §D-04): Frometa FB; Sosa FB/IG + Downey Patriot; Pemberton FB + Downey Latino News;
    Trujillo Ballotpedia + Downey Latino News; Ortiz FB + Downey Patriot. (use /find-headshots if needed.)

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face

Storage + DB:
  - Bucket: politician_photos · Path: canonical {uuid}-headshot.jpg
  - Upload via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), header x-upsert:true
  - politician_images: type='default', photo_license matching the REAL source (press_use for news; the
    appropriate license for SCAG/campaign-supplied — do NOT mislabel)
  - UPDATE politicians.photo_origin_url to the source page where NULL
  - Each INSERT guarded WHERE NOT EXISTS on politician_id -> exactly ONE type='default' row per member

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql). It does NOT register in
schema_migrations (same convention as Pasadena 948). On-disk counter authoritative; ledger stays at 986 (Plan 02).

Pattern migration: C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql (audit-only INSERT +
photo_origin_url backfill + Storage URL shape + full-roster audit + ledger-unchanged check). For WAF-403
operator-download sourcing precedent see the Pomona 928 / Palmdale headshot pipelines. /find-headshots skill:
~/.claude/commands/find-headshots.md. Project memory: feedback_headshot_no_graphics, feedback_headshot_image_sizing,
feedback_headshot_cropping, feedback_headshot_resize_no_distort.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight image-coverage check; source all 5 portraits (Frometa via SCAG direct curl; the other 4 via OPERATOR in-browser from downeyca.org WAF-403); crop 4:5 -> 600×750 -> upload canonical</name>
  <files>C:/EV-Accounts/backend/migrations/987_downey_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql (audit-only INSERT + download/crop/upload pipeline + Storage URL shape + full-roster audit + ledger-unchanged check)
    - C:/EV-Accounts/backend/migrations/928_pomona_headshots.sql (WAF-403 operator-download sourcing precedent)
    - .planning/phases/149-pasadena-deep-seed/149-03-PLAN.md (the exact pre-flight-coverage + crop/resize + checkpoint shape this plan mirrors — but Downey is WAF-403, operator in-browser for 4)
    - .planning/phases/150-downey-deep-seed/150-RESEARCH.md (§D-04 headshot sources/WAF status incl. the SCAG Frometa URL + per-member operator sources + fallbacks, §Common Pitfalls 7, §Assumptions A2/A4)
    - .planning/phases/150-downey-deep-seed/150-02-SUMMARY.md (final 5-member link state + Ortiz's resolved ext_id/UUID)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots — 4:5 crop FIRST -> 600×750 Lanczos q90)
    - ~/.claude/commands/find-headshots.md (skill for finding a current portrait if a source is unusable)
  </read_first>
  <action>
    First run a pre-flight image-coverage query: for the 5 current members (Ortiz resolved ext_id from Plan 02,
    675353 Sosa, 675360 Pemberton, 675361 Frometa, -201200 Trujillo), LEFT JOIN essentials.politician_images
    (type='default') and confirm which still have 0 images (CONTEXT says all 5 -> re-confirm at apply time).
    Then source the 5 portraits: Frometa by DIRECT curl from the SCAG URL (curl -L
    "https://scag.ca.gov/sites/default/files/styles/memeber_image/public/2024-08/claudia-frometa.jpg?itok=sHDJLwxt"
    -o frometa.jpg) IMMEDIATELY (the itok token may rotate — Pitfall 7; if it 403s/expires, retry the base URL
    without itok or use the operator downeyca.org portrait); the OTHER 4 (Sosa, Pemberton, Trujillo, Ortiz) via
    OPERATOR in-browser download from the downeyca.org pages in RESEARCH §D-04 (downeyca.org is WAF-403 to curl
    — do NOT attempt scripted curl on it). For EACH, visually confirm it is the correct named person and has NO
    superimposed text/graphics — CRITICAL: Ortiz's downeyca.org slug is 'timonthy-horn-district-1' (verify the
    photo is Horacio Ortiz, NOT the prior occupant Timothy Horn), and Pemberton's slug is 'vacant-district-3'
    (verify the photo is Dorothy Pemberton). If any source is unusable (overlay/wrong aspect/wrong person), use
    the RESEARCH §D-04 fallback (campaign FB / Ballotpedia / Downey Latino News / Downey Patriot) or the
    /find-headshots skill. Then crop to 4:5 (crop only — never stretch; eyes ~1/3 from top, full head+shoulders),
    resize to 600×750 Lanczos q90 JPEG (Pillow), resolve each UUID by external_id, and upload to
    politician_photos at {uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env) with
    x-upsert:true. Record each public Storage URL + source page + the real photo_license per member for Task 2.
    If no acceptable verified portrait is obtainable for a member, document the genuine gap (do NOT upload a
    wrong-person/overlaid image as type='default' — CONTEXT D-04).
  </action>
  <verify>
    <automated>For each sourced member the processed file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('{member}.jpg').size)"` -> (600, 750)) and the uploaded Storage object at {uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight image-coverage query run; the missing set re-confirmed from live data (CONTEXT says all 5 at 0 images)
    - Frometa sourced by DIRECT curl from SCAG (downloaded immediately; itok-rotation handled); the other 4 sourced via OPERATOR in-browser from downeyca.org (NO scripted curl on the WAF-403 site)
    - Each portrait is the correct verified person — Ortiz confirmed Horacio Ortiz (NOT Timothy Horn), Pemberton confirmed Dorothy Pemberton (despite the 'vacant' slug); no superimposed text/graphics; processed to exactly 600×750, 4:5, not stretched, eyes ~1/3 from top; uploaded to {uuid}-headshot.jpg (HTTP 200)
    - Public Storage URL + source page + REAL photo_license recorded for Task 2 per uploaded member
    - If any member has no acceptable portrait, an honest gap documented (NOT a wrong-person/overlaid image faked as type='default')
  </acceptance_criteria>
  <done>The 5 current members have verified portraits (Frometa via SCAG direct; Sosa/Pemberton/Trujillo/Ortiz via operator in-browser), each confirmed correct + clean, processed to 600×750 4:5, uploaded to Storage at {uuid}-headshot.jpg; Storage URLs + sources + licenses recorded for Task 2; any genuine gap documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 987 (audit-only) — INSERT politician_images + photo_origin_url backfill + full-roster image audit; confirm ledger unchanged</name>
  <files>C:/EV-Accounts/backend/migrations/987_downey_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql (INSERT politician_images + photo_origin_url backfill + audit-only apply, ledger-unchanged check, full-roster coverage audit, documented-gap handling)
    - .planning/phases/150-downey-deep-seed/150-RESEARCH.md (§D-04 headshot audit, §Validation Architecture DWNY-01e, §Assumptions A2/A4)
    - .planning/phases/150-downey-deep-seed/150-02-SUMMARY.md (final link state + Ortiz's resolved ext_id)
    - ~/.claude/commands/find-headshots.md (skill reference)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/987_downey_headshots.sql: for every member uploaded in Task 1,
    INSERT a politician_images row (resolved by external_id; Ortiz via his Plan 02 ext_id) url = the Storage
    public URL, type='default', photo_license = the REAL source license recorded in Task 1 (do NOT blanket all
    as press_use if a source differs), guarded WHERE NOT EXISTS on politician_id (each member ends with exactly
    one canonical type='default' row pointing at {uuid}-headshot.jpg); UPDATE essentials.politicians SET
    photo_origin_url = the source page WHERE external_id=<id> AND photo_origin_url IS NULL. Apply via raw SQL
    (mcp__supabase-local execute_sql) — do NOT run through the schema_migrations ledger (audit-only). Then run
    the full-roster coverage audit: LEFT JOIN the 5 current Downey officials (Ortiz ext_id, 675353, 675360,
    675361, -201200) to politician_images (type='default') and report who has exactly one type='default' row vs
    a documented gap; confirm no member has >1. Verify MAX(version) in schema_migrations is UNCHANGED by this
    file (stays 986).
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql "SELECT p.external_id, COUNT(pi.*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (<ORTIZ_EXT_ID>,675353,675360,675361,-201200) GROUP BY p.external_id;" shows each member with COUNT 0 (documented gap) or 1 (no member has >1) AND "SELECT MAX(version) FROM supabase_migrations.schema_migrations;" is unchanged (stays 986)</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 current members with a verified portrait has exactly one type='default' row at the canonical {uuid}-headshot.jpg path (all were 0 before — full greenfield); photo_license matches the REAL source per member; photo_origin_url populated
    - No member has >1 type='default' row; members with no obtainable verified portrait documented as honest gaps (not faked)
    - schema_migrations MAX(version) unchanged by migration 987 (audit-only confirmed; stays 986)
    - Migration is idempotent (re-apply = 0 rows changed)
  </acceptance_criteria>
  <done>987 applied (audit-only); the 5 current members each have exactly one default image (or a documented honest gap); photo_license per real source; no member >1; ledger untouched (stays 986).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify every Downey headshot (correct person, Ortiz not Timothy Horn, Pemberton not 'vacant', clean crop)</name>
  <action>Pause for the user to visually verify each Downey portrait per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>Verified official headshots for the 5 current Downey members (D1 Ortiz, D2 Sosa/Mayor, D3 Pemberton, D4 Frometa, D5 Trujillo), full greenfield (all were 0 images): Frometa sourced by direct curl from SCAG, the other 4 by operator in-browser download from downeyca.org (WAF-403 to curl); all processed to 600×750 4:5 and live in Supabase Storage + politician_images (type='default', license per real source). Any honest gaps documented.</what-built>
  <how-to-verify>
    1. Confirm each member's photo is the CORRECT person (cross-check Ortiz/Sosa/Pemberton/Frometa/Trujillo against downeyca.org / downeychamber.org).
    2. CRITICAL: confirm the D1 photo is Horacio Ortiz, NOT the prior occupant Timothy Horn (the downeyca.org slug is 'timonthy-horn-district-1' — a known identity trap).
    3. CRITICAL: confirm the D3 photo is Dorothy Pemberton, NOT a placeholder (the downeyca.org slug is 'vacant-district-3').
    4. Confirm no superimposed text/graphics over any face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    5. Confirm each member shows exactly one canonical image (no duplicate type='default' rows) on the Downey browse view. Any documented gap is genuinely imageless (not faked).
    6. Confirm the on-disk counter is authoritative: schema_migrations MAX(version) is unchanged (still 986; the headshot file did not register).
  </how-to-verify>
  <resume-signal>Type "approved" or name which photo(s) need re-sourcing / re-cropping / identity-fix.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with a WAF-403 official image source. No untrusted user input reaches the DB — uploads +
image-row writes use service-role/operator against production. Threat surface = wrong-person photo (the
Ortiz-vs-Timothy-Horn D1 trap from the stale slug, or the Pemberton 'vacant' slug serving a placeholder), a
distorted/overlaid photo, a fabricated/campaign photo filling a real gap, a mislabeled photo_license, a stale
SCAG itok token serving nothing, or accidental ledger registration — mitigated by the pre-flight coverage
check, the identity-verify-before-upload rule (esp. Ortiz + Pemberton), the WAF-403 operator-download path
(no scripted curl on downeyca.org), the immediate SCAG download (itok-rotation), the crop-then-resize/no-stretch
rule, the per-source real-license rule, a blocking human-verify checkpoint that specifically checks Ortiz≠Horn
and Pemberton≠placeholder, the honest-gap rule, and the audit-only raw-SQL apply.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downeyca.org (WAF-403) | Official source BLOCKED to curl — operator in-browser download required; stale URL slugs (Ortiz/Pemberton) — human-verified before upload. |
| SCAG / /find-headshots / alternate hosts -> local processing | Direct/fallback image sources; SCAG itok may rotate — download immediately; all human-verified before upload. |
| operator -> Supabase Storage + production DB | Uploads + image-row INSERT against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-19 | Spoofing | Wrong-person headshot in D1 (Ortiz vs Timothy Horn — stale 'timonthy-horn-district-1' slug) | mitigate | Identity-verify before upload; blocking human-verify specifically checks D1 is Horacio Ortiz not Timothy Horn |
| T-150-20 | Spoofing | D3 placeholder/wrong person (downeyca.org 'vacant-district-3' slug) | mitigate | Identity-verify Pemberton before upload; human checkpoint checks D3 is Dorothy Pemberton not a placeholder |
| T-150-21 | Availability | downeyca.org WAF-403 blocks scripted fetch; SCAG itok token rotates (Pitfall 7) | mitigate | Operator in-browser download for the 4 downeyca.org portraits (no scripted curl); Frometa's SCAG download done immediately at apply time; base-URL / fallback hosts if itok expires |
| T-150-22 | Tampering | Image stretched / aspect-distorted / text-overlaid | mitigate | Crop-to-4:5-FIRST-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; human checkpoint |
| T-150-23 | Spoofing | Fabricated / wrong campaign photo uploaded to fill a gap | mitigate | Honest-gap rule (CONTEXT D-04) — if no acceptable verified portrait, leave imageless and document; never seed a wrong-person/overlaid photo as type='default' |
| T-150-24 | Tampering | Mislabeled photo_license (e.g. blanket press_use on a SCAG/campaign source) | mitigate | photo_license set per the REAL source recorded in Task 1; not blanket-applied |
| T-150-25 | Tampering | Duplicate image row for any member | mitigate | Pre-flight coverage query; each INSERT guarded WHERE NOT EXISTS on politician_id; post-audit confirms ≤1 type='default' per official |
| T-150-26 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged (stays 986) before/after |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | No package installs (existing Pillow + Supabase client; RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After 987 + checkpoint:
1. each current member with a verified portrait has exactly one type='default' image at {uuid}-headshot.jpg; photo_origin_url + real photo_license set
2. full-roster coverage: every member is either imaged (exactly one default) or a documented honest gap; no member has >1
3. D1 is verified Horacio Ortiz (not Timothy Horn); D3 is verified Dorothy Pemberton (not a placeholder)
4. schema_migrations MAX(version) unchanged by 987 (stays 986)
5. human checkpoint approved (correct person each, no overlays, correct crop)
</verification>

<success_criteria>
Every current Downey member with an obtainable correct photo has a single 600×750 verified image at the
canonical path; Frometa sourced by direct curl from SCAG, the other 4 by operator in-browser download
(downeyca.org WAF-403); the Ortiz-vs-Timothy-Horn and Pemberton-'vacant' identity traps are rejected; no
wrong-person/overlaid/stretched photos; photo_license matches each real source; genuine gaps documented
honestly; the ledger counter is preserved (stays 986).
</success_criteria>

<output>
Create `.planning/phases/150-downey-deep-seed/150-03-SUMMARY.md` when done (record any honest image gaps + the per-member source/license).
</output>
</content>
