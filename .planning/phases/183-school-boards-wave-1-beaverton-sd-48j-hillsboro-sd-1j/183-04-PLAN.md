---
phase: 183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j
plan: 04
type: execute
wave: 4
depends_on: [183-03]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WSCH-01, WSCH-02]
user_setup: []

must_haves:
  truths:
    - "src/lib/coverage.js COVERAGE_SCHOOL_DISTRICTS carries two new PLAIN entries appended after the CCSD entry: { label: 'Beaverton School District 48J', browseGeoId: '4101920', browseMtfcc: 'G5420', browseStateAbbrev: 'OR' } and { label: 'Hillsboro School District 1J', browseGeoId: '4100023', browseMtfcc: 'G5420', browseStateAbbrev: 'OR' } — NO hasContext key on either (0-stances-by-design is the honest state, Phase 173 CCSD chip-reconciliation lesson)"
    - "The existing CCSD school-district entry and every other array in coverage.js is preserved unchanged; the file still parses (node -e import / node -c) and the two new entries flow into ALL_COVERAGE_AREAS as kind:'school district' automatically (no other file edited)"
    - "The frontend change is committed to THIS repo and pushed; the Render deploy is verified by grepping the DEPLOYED bundle CONTENT for the new labels (never by comparing a build hash — Render hash != local hash is a known false-negative)"
    - "Full E2E SQL/HTTP gate suite passes: Beaverton 7 offices on geo_id 4101920 SCHOOL/or; Hillsboro 7 offices on geo_id 4100023 SCHOOL/or; casing 'or' only; 0 section-split for both G5420 geo_ids; headshot count = success count from plan 03; 0 compass stance rows for all 14 (success state); smoke-or-westmetro-school.ts passes"
    - "Live browse verified via Playwright on essentials.empowered.vote: /results?browse_geo_id=4101920&browse_mtfcc=G5420 shows all 7 Beaverton directors with the 'Beaverton School District 48J' card heading and their headshots + Chair/Vice-Chair labels; /results?browse_geo_id=4100023&browse_mtfcc=G5420 shows all 7 Hillsboro directors with the 'Hillsboro School District 1J' heading — no 8th/9th/10th row, no student reps"
    - "The card subtitle renders the correct district name via the getAccordionKey SCHOOL government_name fallback (no frontend code change needed — RESEARCH confirmed this works live for PPS); each district chip renders PLAIN (no purple stance-count chip)"
    - "A Beaverton address returns the correct Beaverton SD board member and a Hillsboro address returns the correct Hillsboro SD board member (SC #3 — confirmed via the smoke test's city-hall coordinates and/or a live address lookup)"
    - "Human-verify checkpoint confirms correct-person headshots, exactly 7 directors per board (no student reps/secretary), plain chips, and the district-name headings on the live deployed app"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "COVERAGE_SCHOOL_DISTRICTS with the two OR school-district entries appended (plain, no hasContext); CCSD entry + all other arrays preserved"
      contains: "Beaverton School District 48J"
  key_links:
    - from: "src/lib/coverage.js COVERAGE_SCHOOL_DISTRICTS"
      to: "ALL_COVERAGE_AREAS (search typeahead)"
      via: "COVERAGE_SCHOOL_DISTRICTS.map((d) => ({ ...d, kind: 'school district' }))"
      pattern: "COVERAGE_SCHOOL_DISTRICTS"
    - from: "browse /results?browse_geo_id=4101920&browse_mtfcc=G5420"
      to: "the 7 Beaverton directors"
      via: "backend ST_Covers on the G5420 geofence → SCHOOL district → offices"
      pattern: "4101920|4100023"
---

<objective>
Surface both school districts as plain search-only coverage entries, ship the frontend change, and run
the full same-session E2E live verification: SQL/HTTP gate suite, Render deploy verification by bundle
content, Playwright browse of both G5420 links, and a human-verify checkpoint.

Purpose: Satisfy SC #3 (address routing) + SC #4 (both districts listed in coverage.js as plain chips)
and close the phase. Per D-B1 there is NO banner work and NO buildingImages.js edit; per
[[feedback_school_districts_search_only]] + the Phase 173 CCSD lesson the entries carry NO hasContext
(0-stances-by-design is honest). The frontend delta this phase is coverage.js only. RESEARCH confirmed
the card subtitle already renders the correct district name via the getAccordionKey SCHOOL fallback for
existing PPS data — no frontend code change is needed beyond the coverage.js append.
Output: the coverage.js edit + a recorded full-E2E pass + live-browse confirmation, closing WSCH-01 +
WSCH-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-RESEARCH.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-03-SUMMARY.md
@src/lib/coverage.js

**EXECUTION ARCHITECTURE:** The gsd-executor EDITS src/lib/coverage.js only (this frontend repo — normal
git). It has NO database access, NO Supabase MCP, and does NOT push/deploy. The INLINE ORCHESTRATOR runs
the E2E SQL/HTTP gates via `psql`/`curl`, commits + pushes THIS repo, verifies the Render deploy by
grepping the deployed bundle CONTENT (never a hash), drives the Playwright browse, and runs the
human-verify checkpoint. C:/EV-Accounts is a SEPARATE repo and is NOT touched in this plan.

**VERIFY-GATE HYGIENE / TAILWIND:** no raw Windows backslash paths in any committed file (Tailwind v4
scans .planning/*.md and a raw `\` path crashes the prod build — [[feedback_tailwind_scans_planning_md]]).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the two OR school-district entries to coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the COVERAGE_SCHOOL_DISTRICTS array ~line 253 with the existing CCSD entry — the exact template; and ALL_COVERAGE_AREAS ~line 273 that consumes it)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md §"src/lib/coverage.js" (the exact append shape, no hasContext)
  </read_first>
  <action>
    Append two entries to the COVERAGE_SCHOOL_DISTRICTS array, immediately after the existing Clark
    County School District entry, matching that entry's shape EXACTLY (object key order label,
    browseGeoId, browseMtfcc, browseStateAbbrev) and adding NO hasContext key on either:
    { label: 'Beaverton School District 48J', browseGeoId: '4101920', browseMtfcc: 'G5420',
    browseStateAbbrev: 'OR' } then { label: 'Hillsboro School District 1J', browseGeoId: '4100023',
    browseMtfcc: 'G5420', browseStateAbbrev: 'OR' }. Do NOT touch the CCSD entry, the COVERAGE_STATES /
    COVERAGE_COUNTIES / COVERAGE_BROWSE_STATES arrays, or ALL_COVERAGE_AREAS. Keep the file's existing
    quoting/indentation style. No fenced code in this action — the existing CCSD entry in the file is the
    source of truth for the shape.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && node -e "import('./src/lib/coverage.js').then(m => { const d = m.COVERAGE_SCHOOL_DISTRICTS; const b = d.find(x => x.browseGeoId === '4101920'); const h = d.find(x => x.browseGeoId === '4100023'); if (!b || !h) { console.error('missing entries'); process.exit(1); } if ('hasContext' in b || 'hasContext' in h) { console.error('hasContext must be absent'); process.exit(1); } if (b.browseMtfcc !== 'G5420' || h.browseMtfcc !== 'G5420') { console.error('bad mtfcc'); process.exit(1); } console.log('coverage.js OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE_SCHOOL_DISTRICTS contains the Beaverton (browseGeoId '4101920') and Hillsboro (browseGeoId '4100023') entries, both browseMtfcc 'G5420', browseStateAbbrev 'OR'
    - Neither new entry has a hasContext key (the node verify asserts this)
    - The existing Clark County School District entry is unchanged; node import parses the file and prints "coverage.js OK"
    - No other array in coverage.js is modified; no raw Windows backslash path introduced
  </acceptance_criteria>
  <done>coverage.js has the two plain OR school-district entries appended; node import verify passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs full E2E, ships the frontend, verifies live browse, and confirms on the deployed app</name>
  <what-built>The coverage.js edit + the fully-seeded pair of school-district boards. The orchestrator now runs the full E2E gate suite, pushes the frontend, verifies the Render deploy by bundle content, drives Playwright, and confirms the live render.</what-built>
  <how-to-verify>
    A. Full E2E SQL/HTTP gate suite (via psql on DATABASE_URL from C:/EV-Accounts/backend/.env + curl):
       1. Beaverton offices = 7: `... WHERE d.geo_id='4101920' AND d.district_type='SCHOOL' AND d.state='or'`.
       2. Hillsboro offices = 7: same with geo_id='4100023'.
       3. Casing: `SELECT DISTINCT state FROM essentials.districts WHERE geo_id IN ('4101920','4100023')
          AND district_type='SCHOOL'` → only 'or'.
       4. Section-split = 0 for both G5420 geo_ids.
       5. Headshot count = the plan-03 SUCCESS count: `SELECT COUNT(*) FROM essentials.politician_images
          pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4101927
          AND -4101921 OR p.external_id BETWEEN -4100030 AND -4100024`.
       6. 0-stance success state: `SELECT COUNT(*) FROM inform.politician_answers pa JOIN
          essentials.politicians p ON pa.politician_id=p.id WHERE p.external_id BETWEEN -4101927 AND
          -4101921 OR p.external_id BETWEEN -4100030 AND -4100024` → 0.
       7. Routing: `cd C:/EV-Accounts/backend && npx tsx scripts/smoke-or-westmetro-school.ts` passes.
    B. Ship the frontend (THIS repo only): commit the coverage.js change and push. After Render builds,
       verify the deploy by grepping the DEPLOYED bundle CONTENT for the new labels (fetch the live JS
       bundle and grep for "Beaverton School District 48J" / "Hillsboro School District 1J") — do NOT
       compare a build hash (Render hash != local hash is a known false-negative).
    C. Playwright live browse on essentials.empowered.vote:
       - /results?browse_geo_id=4101920&browse_mtfcc=G5420 → 7 Beaverton directors under a "Beaverton
         School District 48J" heading, each with a headshot; confirm Rajee shows "(Chair)" and Potter
         "(Vice Chair)"; NO 8th row.
       - /results?browse_geo_id=4100023&browse_mtfcc=G5420 → 7 Hillsboro directors under a "Hillsboro
         School District 1J" heading; confirm Pantoja "(Chair)" and Kim "(Vice Chair)"; NO student-rep
         rows (Hernandez Jimenez / Sayre / Woods absent), no secretary.
       - Confirm each district's coverage chip renders PLAIN (no purple stance-count chip).
    D. Address routing (SC #3): confirm a Beaverton address returns the Beaverton SD board and a Hillsboro
       address returns the Hillsboro SD board (the smoke test's city-hall coordinates satisfy this; a live
       address lookup is a stronger confirmation if quick).
  </how-to-verify>
  <acceptance_criteria>
    - All 7 E2E gates pass: 7/7 offices each, casing 'or' only, 0 section-split both geo_ids, headshot count = plan-03 success count, 0 stance rows, smoke test passes
    - coverage.js committed + pushed; deploy verified by grepping the live bundle CONTENT for both new labels (not by hash)
    - Playwright confirms 7 Beaverton directors under the correct heading with Chair/Vice-Chair labels and headshots; 7 Hillsboro directors likewise; no student-rep/secretary rows; both chips plain (no purple)
    - A Beaverton address routes to Beaverton SD; a Hillsboro address routes to Hillsboro SD
    - Human confirms correct-person headshots and the exactly-7-per-board render on the live app
  </acceptance_criteria>
  <resume-signal>Type "approved" with the E2E + deploy + live-browse results, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only E2E gate SELECTs against production Supabase |
| operator → git/Render | Committing + pushing the frontend repo and verifying the public deploy |
| public → deployed app | Anonymous browse of the two G5420 district links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-15 | Tampering | a raw Windows backslash path committed in a .planning/*.md or coverage.js crashes the Tailwind v4 prod build | mitigate | Verify-gate hygiene: no raw `\` paths in committed files; the node import verify + deploy-content grep catch a broken build |
| T-183-16 | Spoofing | trusting a build-hash match that is a Render false-negative (hash != local) | mitigate | Deploy verification greps the DEPLOYED bundle CONTENT for the new labels, never a hash |
| T-183-17 | Tampering (data integrity) | a stray hasContext key would render a false purple stance chip for a 0-stance board | mitigate | The node verify asserts hasContext is absent on both new entries; Playwright confirms the plain chip |
| T-183-18 | Information Disclosure | E2E gate SELECTs read production data | accept | Read-only public-official data; no PII beyond published board rosters |
| T-183-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — Playwright/tsx already present; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Full E2E gate suite green (offices 7/7, casing 'or', 0 section-split, headshot count, 0 stances, routing).
- Deploy verified by bundle content; both chips render plain (no hasContext).
- Playwright confirms both boards render correctly (7 each, correct headings + Chair/Vice-Chair, no student reps).
</verification>

<success_criteria>
- Both districts listed as plain search-only coverage entries (no hasContext); frontend shipped + deploy
  verified by content; a Beaverton/Hillsboro address routes to the correct board; both boards render 7
  directors with headshots + district-name headings on the live app; 0 stance rows confirmed. WSCH-01 +
  WSCH-02 satisfied end-to-end.
</success_criteria>

<output>
Create `.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-04-SUMMARY.md` when
done, recording: the coverage.js entries added, the full E2E gate results, the deploy-content
verification, the Playwright live-browse confirmation for both G5420 links (7 directors each, correct
headings, Chair/Vice-Chair, no student reps, plain chips), and the browse-link URLs for the operator:
essentials.empowered.vote/results?browse_geo_id=4101920&browse_mtfcc=G5420 and
...browse_geo_id=4100023&browse_mtfcc=G5420.
</output>
