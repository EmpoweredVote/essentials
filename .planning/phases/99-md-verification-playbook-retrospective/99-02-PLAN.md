---
phase: 99-md-verification-playbook-retrospective
plan: 2
type: execute
wave: 2
depends_on:
  - 99-01
files_modified:
  - LOCATION-ONBOARDING.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: false
requirements: []
tags:
  - playbook
  - milestone-close
  - maryland
  - documentation

must_haves:
  truths:
    - "LOCATION-ONBOARDING.md has 2 new rows in the 'Cities Onboarded' table — one for 'Maryland (state)' and one for 'Leonardtown'"
    - "LOCATION-ONBOARDING.md has a new '## Maryland Quick Reference' section after the Oregon Quick Reference section and before Step 1"
    - "LOCATION-ONBOARDING.md has 5 new [GOTCHA] inline blocks tagged '[STATE-SPECIFIC: MD]' — one each in Steps 1, 3, 4, 5, 6"
    - "LOCATION-ONBOARDING.md Step 7 Common Pitfalls table has 6 new MD-specific rows appended"
    - "ROADMAP.md milestone line for v11.0 shows ✅ status with shipped date 2026-06-08"
    - "ROADMAP.md Phase 99 entry lists both 99-01-PLAN.md and 99-02-PLAN.md as [x] complete"
    - "ROADMAP.md Phase 99 progress row reads '2/2 | Complete | 2026-06-08'"
    - "STATE.md front-matter shows status: milestone_complete, progress.percent: 100, milestone updated to next milestone or holding"
    - "STATE.md front-matter total_plans / completed_plans values are derived by counting ROADMAP.md Phase 90-99 plan entries (NOT hardcoded); completed_plans counts only plans that have a SUMMARY.md file on disk"
    - "PROJECT.md 'What This Is' paragraph mentions Maryland coverage; 'Validated' section has 3+ MD v11.0 bullets"
    - "Human-verify checkpoint confirms playbook reads cleanly (no broken markdown, GOTCHAs render as blockquotes, table rows align)"
  artifacts:
    - path: "LOCATION-ONBOARDING.md"
      provides: "MD-specific playbook entries — Cities Onboarded rows + Maryland Quick Reference + 5 inline GOTCHAs + Step 7 pitfall rows"
      contains: "## Maryland Quick Reference"
    - path: ".planning/ROADMAP.md"
      provides: "v11.0 milestone closed (✅ + shipped date) + Phase 99 plans marked [x] + phase table row Complete"
      contains: "✅ **v11.0 Maryland Essentials**"
    - path: ".planning/STATE.md"
      provides: "Milestone-close front-matter + Current Position block + Next migration counter (preserved from Plan 01)"
      contains: "status: milestone_complete"
    - path: ".planning/PROJECT.md"
      provides: "Maryland coverage in What This Is + MD v11.0 ✓ bullets in Validated section"
      contains: "all of Maryland"
  key_links:
    - from: "LOCATION-ONBOARDING.md ## Maryland Quick Reference"
      to: "LOCATION-ONBOARDING.md Step 1, Step 3, Step 4, Step 5, Step 6"
      via: "Each Quick Reference trap row's 'See Step' column points to a real Step heading"
      pattern: "Step [1-7]"
    - from: ".planning/ROADMAP.md milestone list"
      to: ".planning/ROADMAP.md phase 99 entry + phase table row"
      via: "milestone close requires all three locations to agree (✅, shipped date, 2/2 Complete)"
      pattern: "v11.0.*shipped"
    - from: ".planning/PROJECT.md 'Validated' bullets"
      to: ".planning/STATE.md milestone progress"
      via: "PROJECT.md Validated section additions cite '— v11.0' suffix matching STATE.md milestone"
      pattern: "— v11.0"
---

<objective>
Add Maryland-specific content to LOCATION-ONBOARDING.md (2 Cities Onboarded rows + Maryland Quick
Reference section + 5 inline GOTCHAs + 6 Step 7 pitfall rows), then close the v11.0 milestone
across ROADMAP.md, STATE.md, and PROJECT.md following the exact sequence used in v7.0 (Phase 78)
and v8.0 (Phase 81) closes.

Purpose: The location-onboarding playbook is the canonical resource for future state onboardings.
Without MD-specific GOTCHAs (multi-member delegate districts, A/B subdistrict polygons,
legislature-elected Treasurer, mgaleg headshot scrape pattern, Baltimore dual-tier), the next
state agent will re-discover these patterns by hitting the same traps Phase 91-98 hit. Milestone
close ensures STATE.md, ROADMAP.md, and PROJECT.md tell a consistent story about what v11.0
delivered.

Output: One updated playbook, three updated project-tracking docs, and a single human-verify
checkpoint confirming the playbook renders cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/99-md-verification-playbook-retrospective/99-RESEARCH.md
@.planning/phases/99-md-verification-playbook-retrospective/99-PATTERNS.md
@.planning/phases/99-md-verification-playbook-retrospective/99-01-SUMMARY.md
@LOCATION-ONBOARDING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Maryland content to LOCATION-ONBOARDING.md (Cities Onboarded rows + Maryland Quick Reference + 5 inline GOTCHAs + 6 Step 7 pitfall rows)</name>
  <files>LOCATION-ONBOARDING.md</files>
  <read_first>
    - LOCATION-ONBOARDING.md (read lines 1-90 to see Cities Onboarded table, CA Quick Reference at lines 51-67, OR Quick Reference at lines 71-84, and Step 1 header at line 88)
    - LOCATION-ONBOARDING.md (read lines 88-410 to see Step 1 through Step 7 + Step 7 pitfall table structure)
    - .planning/phases/99-md-verification-playbook-retrospective/99-RESEARCH.md (section "MD-Specific GOTCHAs Not Yet in Playbook" — exact GOTCHA bodies; section "Maryland Quick Reference Block (for Playbook)" — exact Quick Reference content)
    - .planning/phases/99-md-verification-playbook-retrospective/99-PATTERNS.md (LOCATION-ONBOARDING.md section, lines 23-69 — exact format strings + insertion locations)
  </read_first>
  <action>
    Edit `LOCATION-ONBOARDING.md` with these five concrete additions, in order:

    **(A) Cities Onboarded table — append 2 new rows after the existing "Portland | OR | 2026-05-30" row (currently last row, around line 47):**

    ```
    | Maryland (state) | MD | 2026-06-08 | plurality | State legislature: 47 Senate + 141 Delegates; 71 SLDL polygons (not 47 or 141 — sub-districts); legislature-elected Treasurer (is_appointed=true); mgaleg.maryland.gov headshot discovery (scrape HTML, not HEAD probe); Baltimore City dual-tier (G4110 + G4020); external_ids exec -240001..-240005, senators -2410001..-2410047, delegates -2420001..-2420141 |
    | Leonardtown | MD | 2026-06-08 | plurality | Tier 1 deep seed (migration 277); Mayor=LOCAL_EXEC + 5 council=LOCAL; mtfcc=NULL on district rows (migration 246 pattern); ext_ids under St. Mary's County government |
    ```

    Use 2026-06-08 as the Onboarded date (or current actual date per system context). Preserve
    existing column ordering: `City | State | Onboarded | Election method | Notable patterns`.

    **(B) Maryland Quick Reference section — insert AFTER the Oregon Quick Reference section
    (which currently ends around line 85) and BEFORE `## Step 1: Government Structure Research`
    (line 88). Insert a `---` horizontal rule before the new section header to match the
    California/Oregon pattern (lines 49, 69).**

    Header and intro:
    ```
    ## Maryland Quick Reference

    **Read this before starting any MD city or state work.** These traps are MD-specific — general playbook guidance above does not warn for them.

    | Trap | See Step | One-Line Summary |
    |------|----------|-----------------|
    | Multi-member delegate districts | Step 5, 6 | 47 TIGER SLDL polygons cover 141 delegates; 3 per whole-district polygon; NOT EXISTS guard uses (district_id, politician_id) |
    | A/B subdistrict polygons | Step 5 | Districts with A/B/C suffix have separate polygons; 71 total TIGER polygons (not 47 or 141) |
    | State Treasurer appointed by GA | Step 5 | Treasurer is legislature-elected: is_appointed_position=true, zero race rows; AG/Gov/LG/Comptroller ARE voter-elected |
    | mgaleg headshot URL discovery | Step 4 | Scrape roster page HTML for img src — HEAD probing misses higher suffix numbers (jackson04, young04, harris03) |
    | Compound last-name mgaleg keys | Step 4 | Lewis Young→young04, White Holland→white01, Fraser-Hidalgo→fraser01 — pattern varies; always scrape to confirm |
    | Baltimore City dual-tier | Step 3 | Like SF: returns G4110 (2404000) AND G4020 (24510) — assert BOTH in smoke tests |
    | politician_photos bucket | Step 7 | Upload to 'politician_photos' bucket (NOT 'politician-headshots' — that bucket does not exist); path: {politician_id}-headshot.jpg |
    | Peña-Melnyk headshot filename | Step 4 | mgaleg uses pena.jpg (strips Melnyk suffix and tilde); Jacobs J. filename has space → URL-encode |
    | MD-GOV-04 NOT EXISTS guard | Step 6 | Multi-member district INSERT must guard on (district_id, politician_id) NOT (district_id, chamber_id) |
    | discovery_jurisdictions cron_active | Step 6 | MD discovery_jurisdictions has no cron_active column; date-based eligibility is the correct mechanism |
    ```

    Then a "Maryland Key Facts" bullet list using the content from 99-RESEARCH.md section
    "Maryland Key Facts:" verbatim (FIPS 24, TIGER counts, legislature URLs, headshot sources,
    external_id scheme, US senator pre-existence note).

    End the section with `---` (horizontal rule) before `## Step 1`.

    **(C) Five inline GOTCHA blocks — insert at the locations specified in 99-PATTERNS.md "Where
    to insert inline GOTCHAs":**

    - Step 1 (after the "## Step 1: Government Structure Research" intro paragraphs and the
      existing CA + OR GOTCHA blocks at lines 103-106): Add MD GOTCHA covering State Treasurer
      appointed by GA + multi-member delegate districts (3 per polygon).
    - Step 3 (Geofence Sources, ## heading at line 184): Add MD GOTCHA covering Baltimore City
      dual-tier + the 71-polygon vs 141-delegate math.
    - Step 4 (Data Sources, ## heading at line 240): Add MD GOTCHA covering mgaleg HTML scrape +
      compound last-name pattern + Peña-Melnyk/Jacobs J. edge cases + politician_photos bucket.
    - Step 5 (Schema Decisions Before Migration, ## heading at line 273): Add MD GOTCHA covering
      NOT EXISTS guard using (district_id, politician_id) not (district_id, chamber_id).
    - Step 6 (Migration Order, ## heading at line 308): Add MD GOTCHA covering
      discovery_jurisdictions has no cron_active column.

    Each block uses the EXACT format from 99-PATTERNS.md GOTCHA Block Insertion Pattern:
    ```
    > [GOTCHA] **[STATE-SPECIFIC: MD] [Title]:** [Body — concrete problem + correct behavior + MD example.]
    ```
    Body must include all three: what goes wrong without the warning, the correct behavior, a
    concrete MD example (e.g., District 47A polygon split, delegate jackson04 example, Peña-Melnyk
    pena.jpg). Use 99-RESEARCH.md "MD-Specific GOTCHAs Not Yet in Playbook" subsections as the
    authoritative body content for each GOTCHA.

    **(D) Step 7 Common Pitfalls table — append 6 new rows after the last existing OR-related row.
    Use the exact rows specified in 99-PATTERNS.md (lines 62-69):**

    ```
    | MD multi-member delegate INSERT blocks on 2nd/3rd delegate | Use NOT EXISTS on (district_id, politician_id) NOT (district_id, chamber_id); chamber_id as discriminator blocks all but the first delegate per district |
    | mgaleg headshot suffix not guessable | Scrape roster HTML for actual img src; HEAD probing misses delegates with suffix >01 (e.g., jackson04, young04, harris03) |
    | Baltimore City dual-tier missed in smoke test | Assert BOTH geo_id='2404000' (G4110) AND geo_id='24510' (G4020) for any Baltimore City address; "exactly one local row" assertion fails incorrectly |
    | MD State Treasurer modeled as voter-elected | Treasurer is elected by General Assembly: is_appointed_position=true, zero race rows, no discovery_jurisdictions entry; AG/Gov/LG/Comptroller ARE voter-elected |
    | Upload to wrong MD headshot bucket | Use 'politician_photos' bucket (NOT 'politician-headshots' — that bucket does not exist); path pattern: {politician_id}-headshot.jpg |
    | discovery_jurisdictions cron_active column assumed | MD discovery_jurisdictions has no cron_active column; date-based eligibility is the correct mechanism; REQUIREMENTS.md MD-ELECTIONS-02 text is stale on this point |
    ```

    Preserve existing two-column structure: `| Pitfall | Fix |` (verify column header at line ~371 before appending).

    DO NOT: alter existing CA or OR Quick Reference sections; alter existing GOTCHA blocks; reorder
    Cities Onboarded table; renumber Steps; remove the "Core Principle" or any other existing
    section.
  </action>
  <verify>
    <automated>grep -c "^## Maryland Quick Reference$" LOCATION-ONBOARDING.md | grep -E "^1$" &amp;&amp; grep -c "STATE-SPECIFIC: MD" LOCATION-ONBOARDING.md | grep -E "^[5-9]$|^[1-9][0-9]+$" &amp;&amp; grep -c "^| Maryland (state) | MD |" LOCATION-ONBOARDING.md | grep -E "^1$" &amp;&amp; grep -c "^| Leonardtown | MD |" LOCATION-ONBOARDING.md | grep -E "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - LOCATION-ONBOARDING.md contains exactly one `## Maryland Quick Reference` section header
    - LOCATION-ONBOARDING.md contains the literal string `[STATE-SPECIFIC: MD]` at least 5 times (5 inline GOTCHAs + 10 Quick Reference trap rows; total occurrences ≥ 5 in inline blocks, plus 10+ table rows referencing MD)
    - Cities Onboarded table contains row matching `^| Maryland \(state\) | MD | 2026-` (regex; exactly one match)
    - Cities Onboarded table contains row matching `^| Leonardtown | MD | 2026-` (regex; exactly one match)
    - Maryland Quick Reference trap table has exactly 10 rows (the 10 traps listed)
    - Step 1 section contains a new GOTCHA block whose body mentions "State Treasurer" AND "General Assembly"
    - Step 3 section contains a new GOTCHA block whose body mentions "Baltimore" AND "2404000" AND "24510"
    - Step 4 section contains a new GOTCHA block whose body mentions "mgaleg" AND "scrape" AND ("Peña-Melnyk" OR "pena.jpg")
    - Step 5 section contains a new GOTCHA block whose body mentions `(district_id, politician_id)` (literal substring)
    - Step 6 section contains a new GOTCHA block whose body mentions "discovery_jurisdictions" AND "cron_active"
    - Step 7 pitfall table has 6 new MD rows (grep `^| MD ` OR `^| mgaleg headshot` OR `^| Baltimore City dual-tier` OR `^| Upload to wrong MD` OR `^| discovery_jurisdictions cron_active` ≥ 6 matches)
    - No existing section was removed (grep still finds: `^## Core Principle`, `^## Cities Onboarded`, `^## California Quick Reference`, `^## Oregon Quick Reference`, `^## Step 1` through `^## Step 8`)
  </acceptance_criteria>
  <done>
    LOCATION-ONBOARDING.md is fully updated with MD content: 2 onboarded rows + Maryland Quick
    Reference + 5 inline GOTCHAs + 6 Step 7 pitfall rows. Future MD city or state work can be
    onboarded by reading the playbook without re-discovering v11.0 patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close v11.0 milestone across ROADMAP.md + STATE.md + PROJECT.md (sequence per v7.0/v8.0 close pattern)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 1-18 for milestone list; lines 1076-1090 for Phase 99 entry; lines 1230 for phase progress table row; ALSO: read every Phase 90-99 plan list block to count plans for STATE.md derivation in Step 4)
    - .planning/STATE.md (lines 1-30 for front-matter + Current Position; line 58 for migration counter preserved from Plan 99-01)
    - .planning/PROJECT.md (lines 1-10 for "What This Is" paragraph; lines 110-120 for last Validated bullets — append-point; lines 127-145 for "Current Milestone" block)
    - .planning/phases/99-md-verification-playbook-retrospective/99-01-SUMMARY.md (Plan 99-01 outcome — confirms 22 PASS + 4 Phase 90 status; required precondition for milestone close)
    - .planning/phases/99-md-verification-playbook-retrospective/99-PATTERNS.md (Milestone Close Sequence list — 8 steps in required order)
  </read_first>
  <action>
    Execute the 8-step Milestone Close Sequence from 99-PATTERNS.md in EXACT order:

    **Step 1 — ROADMAP.md milestone line (line 18):**
    Change:
    `- 🚧 **v11.0 Maryland Essentials** — Phases 90-99 (in progress)`
    To:
    `- ✅ **v11.0 Maryland Essentials** — Phases 90-99 (shipped 2026-06-08) — [archive](milestones/v11.0-ROADMAP.md)`
    (Use today's actual date per system context. Archive file does NOT need to be created — leave
    the link for future archival work; the v8.0/v9.0 entries follow this same pattern.)

    **Step 2 — ROADMAP.md Phase 99 plan list (lines 1086-1088 area):**
    Under the Phase 99 entry, replace `**Plans**: TBD` with:
    ```
    **Plans**: 2 plans

    Plans:

    - [x] 99-01-PLAN.md — Verification sweep + REQUIREMENTS.md cleanup + STATE.md migration counter fix
    - [x] 99-02-PLAN.md — LOCATION-ONBOARDING.md MD content + v11.0 milestone close across ROADMAP/STATE/PROJECT
    ```

    **Step 3 — ROADMAP.md phase table row (line 1230 area):**
    Change:
    `| 99. MD Verification + Playbook Retrospective | v11.0 | 0/TBD | Not started | - |`
    To:
    `| 99. MD Verification + Playbook Retrospective | v11.0 | 2/2 | Complete | 2026-06-08 |`

    **Step 4 — STATE.md YAML front-matter (lines 1-15):**

    BEFORE writing the front-matter, COUNT plans from ROADMAP.md. Do NOT hardcode plan totals.

    Sub-step 4a — Count total_plans:
      For each Phase 90 through Phase 99 entry in ROADMAP.md, count the number of plan list lines
      (lines matching `^- \[[ x]\] .*-PLAN\.md` under each phase block). Sum them. The expected
      total for v11.0 is `35` (Phase 90 has 3 plans, Phase 91 has 4, Phase 92 has 2, Phase 93 has
      6, Phase 94 has 2, Phase 95 has 2, Phase 96 has 3, Phase 97 has 4, Phase 98 has 7, Phase 99
      has 2 — sum is 3+4+2+6+2+2+3+4+7+2 = 35). If the ROADMAP count differs from 35, USE THE
      ACTUAL COUNT — do not force-fit to 35.

      Concrete derivation command (run before writing):
        grep -cE '^- \[[ x]\] .*-PLAN\.md' .planning/ROADMAP.md  # gross total across whole file
        # For per-phase precision, awk the ROADMAP file between '### Phase 90' and end-of-file
        # markers and count PLAN.md lines per phase; sum 90..99.

    Sub-step 4b — Count completed_plans:
      `completed_plans` counts ONLY plans that have a SUMMARY.md file on disk. Do NOT assume
      completed_plans == total_plans. Concrete check:
        ls .planning/phases/9*/9*-SUMMARY.md 2>/dev/null | wc -l
      Cross-check: for each phase 90-99, count `9N-NN-SUMMARY.md` files (where NN is the plan
      number, excluding `9N-SUMMARY.md` rollup files if present). If Phase 90 Plan 03 has NOT been
      executed (no `90-03-SUMMARY.md`), completed_plans < total_plans, and the milestone close
      should NOT proceed without flagging this in 99-01-SUMMARY.md.

      The expected completed_plans value depends on whether 99-01-SUMMARY.md (this plan's
      precondition) recorded Phase 90 Plan 03 as PASS or DEFER:
        - If 90-03-SUMMARY.md exists AND Plan 95-02 SUMMARY exists (etc., all 35 SUMMARYs present):
          completed_plans = 35 (only 99-02 is currently running, so it will become 35 once this
          plan writes its own 99-02-SUMMARY.md at the end; for the STATE.md edit DURING this task,
          use total - 1 = 34, then have the SUMMARY-writing step bump to 35).
        - If 90-03-SUMMARY.md is absent (DEFER case):
          completed_plans = 35 minus the count of missing SUMMARY files (at minimum minus 1 for
          90-03; minus more if other plans also lack SUMMARYs). Record the exact missing count in
          the new "Key Decisions" bullet appended in this task.

    Sub-step 4c — Write the front-matter:
    Replace the front-matter block with the following YAML; substitute the placeholders
    `{TOTAL_PLANS}` and `{COMPLETED_PLANS}` with the values derived in 4a and 4b:
    ```yaml
    ---
    gsd_state_version: 1.0
    milestone: v11.0
    milestone_name: Maryland Essentials
    status: milestone_complete
    last_updated: 2026-06-09T{HH:MM:SS}.000Z
    last_activity: 2026-06-09 -- Phase 99 complete; v11.0 Maryland Essentials shipped
    progress:
      total_phases: 10
      completed_phases: 10
      total_plans: {TOTAL_PLANS}
      completed_plans: {COMPLETED_PLANS}
      percent: 100
    stopped_at: v11.0 complete — all 26 requirements verified; milestone closed; next milestone TBD
    ---
    ```
    Use actual current UTC time for `last_updated`. If completed_plans < total_plans (the DEFER
    case), set `percent` to the actual floor((completed_plans/total_plans)*100) — but ALSO note in
    the "Key Decisions" bullet that the milestone is closed-with-deferral, not fully complete. If
    completed_plans == total_plans, percent = 100.

    **Step 5 — STATE.md Current Position block (lines 19-26):**
    Replace with:
    ```markdown
    ## Current Position

    Phase: 99
    Plan: Complete
    Status: v11.0 milestone closed
    Last activity: 2026-06-09

    Progress: [█████████] 100%
    ```
    (Adjust the progress bar to reflect actual percent if not 100.)

    **Step 6 — STATE.md "Next migration" counter (line 58 area):**
    PRESERVE the value set by Plan 99-01 (expected 293). Do NOT re-edit this line unless its value
    is missing or clearly wrong.

    **Step 7 — PROJECT.md "What This Is" paragraph (lines 1-10):**
    Append `, and all of Maryland (St. Mary's County + Leonardtown deep seed)` after the existing
    Multnomah County clause. The result should read approximately:
    `...all of Oregon (Portland deep seed), Multnomah County, OR including 22 school districts across 4 states, and all of Maryland (St. Mary's County + Leonardtown deep seed)...`

    **Step 8 — PROJECT.md "Validated" section (after last v11.0 entry around line 113):**
    Append three new bullets after the last existing v11.0 line (the MD state government bullet
    around line 113). Use the exact text from 99-PATTERNS.md PROJECT.md analog block (lines
    198-202):
    ```
    - ✓ Maryland state coverage: 307 geofence boundaries; 47 senators + 141 delegates + 10 federal officials; 202 officials with headshots; 1516+ compass stances; 130 race rows + discovery pipeline — v11.0
    - ✓ St. Mary's County + Leonardtown deep seed: county commission + town council seeded with headshots — v11.0
    - ✓ MiniCompass compact overlay: dotRadius=2.5 + showLabels=false — v11.0
    ```
    (The exact stance count should match the Q-PHASE-1 from 98-07-SUMMARY.md; use 1516+ if exact
    value not retained, or correct it to the value cited in Plan 99-01 verification matrix for
    MD-STANCES-03.)

    Also update PROJECT.md "Current Milestone" block: change `### Current Milestone: v11.0 Maryland
    Essentials` to indicate completion. Add a "**v11.0 Shipped 2026-06-08:**" sub-section header
    above the existing Current Milestone heading following the v10.0 precedent at lines 117+, then
    leave Current Milestone block intact (next planner will rewrite it for v12.0).

    Update PROJECT.md footer (line 244 area): `*Last updated: 2026-06-09 after Phase 99 complete —
    v11.0 Maryland Essentials shipped*`.

    DO NOT touch the "Out of Scope", "Context", "Constraints", or "Key Decisions" sections of
    PROJECT.md. DO NOT remove any existing Validated bullets. DO NOT change STATE.md "Key MD Facts"
    or "Pending Todos" sections (the ME June 9 / lavote.gov pending todos remain unless Plan 99-01
    confirmed Phase 90 Plan 03 ran — in which case Plan 99-01 already updated them).
  </action>
  <verify>
    <automated>grep -c "^- ✅ \*\*v11.0 Maryland Essentials\*\*" .planning/ROADMAP.md | grep -E "^1$" &amp;&amp; grep -q "^status: milestone_complete$" .planning/STATE.md &amp;&amp; grep -qE "^  total_plans: [0-9]+$" .planning/STATE.md &amp;&amp; grep -qE "^  completed_plans: [0-9]+$" .planning/STATE.md &amp;&amp; grep -q "all of Maryland" .planning/PROJECT.md &amp;&amp; grep -c "— v11.0$" .planning/PROJECT.md | grep -E "^[5-9]$|^[1-9][0-9]+$"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md line 18 area contains literal string `✅ **v11.0 Maryland Essentials**` AND `shipped 2026-06-08`
    - ROADMAP.md Phase 99 plan list shows `- [x] 99-01-PLAN.md` AND `- [x] 99-02-PLAN.md`
    - ROADMAP.md phase progress table row for Phase 99 reads `| 2/2 | Complete | 2026-06-08 |`
    - STATE.md front-matter `status:` field equals `milestone_complete`
    - STATE.md front-matter `total_plans:` is a numeric value derived by counting ROADMAP.md Phase 90-99 PLAN.md entries (expected 35; NOT hardcoded 33)
    - STATE.md front-matter `completed_plans:` is a numeric value equal to the count of `9*-*-SUMMARY.md` files on disk for phases 90-99 (NOT hardcoded; may be less than total_plans if any plan lacks a SUMMARY)
    - STATE.md front-matter `progress.percent:` equals `100` (only if completed_plans == total_plans; otherwise floor((completed/total)*100))
    - STATE.md "Current Position" block contains `Status: v11.0 milestone closed`
    - STATE.md "Next migration:" line value is UNCHANGED from Plan 99-01 (preserved, not regressed)
    - PROJECT.md "What This Is" paragraph contains literal substring `all of Maryland`
    - PROJECT.md "Validated" section contains exactly 3 new `— v11.0` bullets added by this task (Maryland state coverage, St. Mary's/Leonardtown, MiniCompass) in addition to any pre-existing v11.0 bullets
    - PROJECT.md footer reads `*Last updated: 2026-06-09` (or current actual date) AND mentions `v11.0 Maryland Essentials shipped`
    - All three files parse as valid markdown (no broken YAML front-matter in STATE.md; no broken tables in ROADMAP.md or PROJECT.md)
    - No existing milestone (v2.0..v10.0) entry was altered in ROADMAP.md milestone list (lines 5-17)
  </acceptance_criteria>
  <done>
    v11.0 milestone is officially closed. ROADMAP.md, STATE.md, and PROJECT.md all tell a
    consistent story: 26 requirements satisfied (per Plan 99-01), 10 phases complete, milestone
    shipped today, MD coverage advertised in the project description. STATE.md total_plans and
    completed_plans reflect actual ROADMAP/disk state (no hardcoded values).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the playbook renders cleanly and milestone close is consistent</name>
  <what-built>
    LOCATION-ONBOARDING.md has been updated with 2 Cities Onboarded rows, a Maryland Quick
    Reference section (10-row trap table + Maryland Key Facts list), 5 inline [GOTCHA] blocks
    distributed across Steps 1/3/4/5/6, and 6 new pitfall rows in the Step 7 table. ROADMAP.md
    shows v11.0 as ✅ shipped. STATE.md shows milestone_complete + 100% progress. PROJECT.md
    advertises Maryland coverage and lists 3+ MD ✓ bullets in Validated.
  </what-built>
  <how-to-verify>
    1. Open `LOCATION-ONBOARDING.md` in a markdown previewer (or skim raw text):
       - Confirm the new "## Maryland Quick Reference" section appears between Oregon Quick Reference and Step 1.
       - Confirm the 10-row Maryland trap table renders with aligned columns and clickable Step references (Step 1, Step 3, etc.).
       - Skim Steps 1, 3, 4, 5, 6 — each must have a blockquoted `> [GOTCHA] **[STATE-SPECIFIC: MD] ...` block that renders correctly as an indented callout.
       - Step 7 Common Pitfalls table — confirm 6 new MD-related rows appear at the end without breaking the table alignment.
    2. Open `.planning/ROADMAP.md`:
       - Confirm v11.0 line shows ✅ + 2026-06-08 shipped date.
       - Confirm Phase 99 entry lists both plans as [x] complete.
       - Confirm phase table row 99 reads `2/2 | Complete | 2026-06-08`.
    3. Open `.planning/STATE.md`:
       - Confirm front-matter `status: milestone_complete` and `progress.percent: 100`.
       - Confirm `total_plans` matches the actual ROADMAP Phase 90-99 plan count (expected 35).
       - Confirm `completed_plans` matches the actual on-disk SUMMARY.md count (must NOT be hardcoded; expected ≤ total_plans).
       - Confirm Current Position block reads "Phase: 99 / Plan: Complete / Status: v11.0 milestone closed".
       - Confirm "Next migration:" line value matches Plan 99-01 output (typically 293).
    4. Open `.planning/PROJECT.md`:
       - Confirm "What This Is" paragraph mentions "all of Maryland".
       - Confirm Validated section has 3+ new MD `— v11.0` bullets.
    5. Spot-check rendering: open ev-ui or any markdown viewer if available; visually confirm Maryland Quick Reference table headers align and GOTCHA blocks render as blockquotes.
    6. Cross-check consistency: REQUIREMENTS.md (already updated by Plan 99-01), ROADMAP.md, STATE.md, and PROJECT.md should agree on "v11.0 complete + all 26 requirements verified".
  </how-to-verify>
  <resume-signal>
    Type "approved" to confirm the playbook updates and milestone close pass human review.
    Type a list of issues to send the executor back for fixes (e.g., "Maryland Quick Reference table column 3 is too long, wrap to 2 lines" or "missing v11.0 archive link"). Do NOT approve if any of: a GOTCHA block fails to render as a blockquote, a Cities Onboarded row breaks table alignment, ROADMAP.md still shows 🚧 for v11.0, STATE.md still says ready_to_plan, STATE.md total_plans/completed_plans appear to be hardcoded (verify by re-counting ROADMAP plan entries), or PROJECT.md fails to mention Maryland.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → repo markdown | Direct writes to `LOCATION-ONBOARDING.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/PROJECT.md` — no DB access in this plan |
| human reviewer → published playbook | Future agents read LOCATION-ONBOARDING.md as the canonical state-onboarding guide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02-01 | Tampering | LOCATION-ONBOARDING.md GOTCHA accuracy | mitigate | All GOTCHA bodies sourced verbatim from 99-RESEARCH.md (which is VERIFIED against STATE.md accumulated context); acceptance criteria require specific literal substrings ("State Treasurer", "2404000", "(district_id, politician_id)") to prevent paraphrasing drift |
| T-99-02-02 | Information Disclosure | Milestone close content | accept | No PII or secrets in any artifact; all values are aggregate stats from prior SUMMARY files |
| T-99-02-03 | Repudiation | Inconsistent milestone close across files | mitigate | Task 2 enforces strict 8-step sequence with grep-verifiable cross-file assertions (ROADMAP+STATE+PROJECT all reference v11.0 completion); STATE.md total_plans/completed_plans derived (not hardcoded) so the audit trail is auto-validated |
| T-99-02-04 | Denial of Service | Markdown table corruption breaks playbook rendering | mitigate | Human-verify checkpoint (Task 3) requires visual confirmation of table alignment and GOTCHA blockquote rendering |
| T-99-02-05 | Spoofing | Future agent trusts wrong GOTCHA pattern | mitigate | Every GOTCHA block carries `[STATE-SPECIFIC: MD]` tag; future agents can grep by state; never omit the tag |
| T-99-02-06 | Elevation of Privilege | Silent regression of v10.0+ Validated bullets | mitigate | Acceptance criteria require existing Validated bullets remain intact; only APPEND new MD bullets |
| T-99-02-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this plan; documentation-only |
</threat_model>

<verification>
- LOCATION-ONBOARDING.md has exactly 1 `## Maryland Quick Reference` section header
- LOCATION-ONBOARDING.md has 5+ occurrences of `[STATE-SPECIFIC: MD]` (inline GOTCHA blocks)
- LOCATION-ONBOARDING.md Cities Onboarded table has 1 `| Maryland (state) | MD |` row and 1 `| Leonardtown | MD |` row
- ROADMAP.md milestone list shows `✅ **v11.0 Maryland Essentials**` with shipped date
- ROADMAP.md Phase 99 phase-table row reads `| 2/2 | Complete |`
- STATE.md front-matter `status: milestone_complete` + `percent: 100`
- STATE.md front-matter `total_plans:` and `completed_plans:` are numeric and derived from ROADMAP/disk (not hardcoded — verify by independent recount)
- PROJECT.md "What This Is" mentions `all of Maryland`
- PROJECT.md Validated section has 3+ new `— v11.0` bullets added by this plan
- Human-verify checkpoint passes (no rendering issues, no broken tables, no missing sections)
</verification>

<success_criteria>
1. LOCATION-ONBOARDING.md is a future-agent-ready resource for MD city or state work — Maryland Quick Reference summarizes traps; inline GOTCHAs catch agents at the point of encounter; Step 7 pitfalls table is the post-execution debugging checklist
2. v11.0 is officially closed across all three project-tracking documents (ROADMAP.md, STATE.md, PROJECT.md) with consistent dates, status flags, and progress values
3. STATE.md plan counts (total_plans / completed_plans) accurately reflect ROADMAP.md and on-disk SUMMARY.md state — derived, not hardcoded
4. Human reviewer confirms playbook renders cleanly and milestone close is consistent
5. Next planner session opens to a STATE.md showing milestone_complete and can immediately begin v12.0 discussion without cleaning up v11.0 residue
</success_criteria>

<output>
Create `.planning/phases/99-md-verification-playbook-retrospective/99-02-SUMMARY.md` when done,
summarizing: LOCATION-ONBOARDING.md additions (Cities Onboarded rows, Quick Reference section, 5
inline GOTCHAs, 6 Step 7 rows), milestone-close artifacts updated (ROADMAP/STATE/PROJECT line
counts), STATE.md total_plans/completed_plans values DERIVED (with the actual counts recorded),
human-verify outcome, and final v11.0 status.
</output>
