---
phase: 214-unified-location-combobox-google-places-removal
plan: 02
type: execute
wave: 2
depends_on: [214-01]
files_modified:
  - src/components/LocationCombobox.jsx
  - src/lib/localitySearch.js
autonomous: true
requirements: [SRCH-02, SRCH-03, SRCH-04]
user_setup: []

must_haves:
  truths:
    - "The field is an accessible combobox (role=combobox, aria-expanded/controls/activedescendant, aria-autocomplete=list) with arrow-key list navigation"
    - "Typing a name-like value (>=3 chars) fires a single debounced /location-search call and renders ranked candidate rows"
    - "Typing an address-like or coordinate-like value fires NO resolver call and shows the matching inline hint row"
    - "Ambiguous candidates render inline as listbox rows showing City/County/State qualifier + kind tag + Stances badge when has_local_data"
    - "Picking a candidate, submitting an address, and submitting a coordinate each invoke a distinct callback the host page supplies"
  artifacts:
    - path: "src/components/LocationCombobox.jsx"
      provides: "Shared accessible combobox for Results + Landing"
      min_lines: 120
      exports: ["default"]
    - path: "src/lib/localitySearch.js"
      provides: "Google-free routing helper for candidate → browse URL"
      contains: "browseAreaRoute"
  key_links:
    - from: "src/components/LocationCombobox.jsx"
      to: "classifyInput"
      via: "import from src/lib/inputClassifier"
      pattern: "classifyInput"
    - from: "src/components/LocationCombobox.jsx"
      to: "searchLocationsByName"
      via: "debounced import from src/lib/api"
      pattern: "searchLocationsByName"
    - from: "src/components/LocationCombobox.jsx"
      to: "@floating-ui/react"
      via: "useListNavigation({virtual:true})"
      pattern: "useListNavigation"
---

<objective>
Build the shared `<LocationCombobox>` component (SRCH-02/03/04) — one accessible, always-editable field that classifies keystrokes via the Plan 01 classifier, debounce-queries `/location-search` for name-like input only, renders ranked candidate rows (lifting `LocalityMatches` markup), shows inline hint rows for address/coordinate input (D-01), and emits distinct submit callbacks the host pages wire. Also refactor `localitySearch.js` to drop the Google `classifyQuery` path and expose the Google-free `browseAreaRoute` candidate→URL helper the combobox dispatch needs (D-09 partial).

Purpose: This is the single reusable seam that unifies Phase 212 place-name search and Phase 213 coordinate lookup behind one field, replacing Google Places' classification/suggestion role.
Output: `src/components/LocationCombobox.jsx` (new) + refactored `src/lib/localitySearch.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-CONTEXT.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-RESEARCH.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-PATTERNS.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-UI-SPEC.md

<interfaces>
<!-- Props contract the combobox EXPORTS. Host pages (Plans 03/04) implement these callbacks. -->
LocationCombobox props:
  value: string                       // controlled input value (host owns state)
  onChange: (next: string) => void    // fully controlled, no imperative DOM writes
  onSubmitAddress: (raw: string) => void       // fired on Enter/Search for kind==='address'
  onSubmitCoordinate: (lat, lng, raw) => void  // fired on Enter/Search for kind==='coordinate'; raw is the literal typed text (D-05)
  onSelectCandidate: (candidate) => void       // fired when a listbox row is chosen; candidate = {geo_id, mtfcc, label, state, has_local_data}
  placeholder?: string
  ariaLabel?: string

candidate shape consumed (from Plan 01 searchLocationsByName):
  { geo_id, mtfcc, label, state, has_local_data }

browseAreaRoute(candidate) -> "/results?browse_geo_id=...&browse_mtfcc=...&browse_label=...&from_locality=1"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LocationCombobox accessible component (SRCH-02/03/04)</name>
  <read_first>
    - src/components/CampaignFinance/InfoTooltip.jsx (project's floating-ui idiom — useFloating/useInteractions/FloatingPortal skeleton; PATTERNS.md lines 30-61)
    - src/components/LocalityMatches.jsx (listbox row markup to lift; the document-level keydown-capture + .pac-container hack to DROP; PATTERNS.md lines 63-102)
    - src/pages/Landing.jsx nameQuery debounce effect (lines ~170-185) — the setTimeout/clearTimeout ref pattern to reuse (PATTERNS.md lines 290-310)
    - 214-RESEARCH.md "Pattern 1: Accessible Combobox" blueprint (lines 214-329) and "Anti-Patterns to Avoid" (lines 337-354)
    - 214-UI-SPEC.md "Combobox Interaction States" table + "Copywriting Contract" (exact copy for hint/error/no-match rows) + Color/Typography/Spacing tokens
    - 214-CONTEXT.md D-01 (Enter-hint), D-02/D-06 (classification split, ~250ms/3-char), D-04 (nothing on empty focus), D-08 (inline dropdown + inline error/no-match rows)
  </read_first>
  <action>
    Create `src/components/LocationCombobox.jsx` as a fully-controlled component (props per the interfaces block; `value`/`onChange` only — no imperative DOM writes, per RESEARCH Pitfall 2). Positioning: `useFloating` with `placement:'bottom-start'` and middleware `offset(4)`, `flip({padding:8})`, and `size({apply})` to match the listbox width to the input; `whileElementsMounted: autoUpdate`. Interactions: `useRole(context,{role:'listbox'})`, `useDismiss(context)`, and `useListNavigation(context,{ listRef, activeIndex, virtual:true, onNavigate, loop:true })` — this REPLACES LocalityMatches' document-level capture-phase keydown listener (do not reintroduce it). Input attributes exactly per UI-SPEC: `type="text"`, `role="combobox"`, `aria-expanded`, `aria-controls={listboxId}`, `aria-autocomplete="list"`, `aria-activedescendant` set only while open, `autoComplete="off"`, `spellCheck={false}`, `aria-label` and `placeholder` from UI-SPEC copy. On focus: select-all (D-03); do NOT open the listbox on empty focus (D-04). Debounced name-search: a `useEffect` on `value` that clears/sets a `setTimeout` ref, GATED on `classifyInput(value).kind === 'name' && value.trim().length >= 3` (gate the NETWORK call, not just rendering — RESEARCH Pitfall 4); ~250ms; calls `searchLocationsByName`, sets candidates, opens listbox; a teal 16px inline spinner (hand-authored SVG, Feather style per UI-SPEC) shows during the in-flight window. Candidate rows: lift LocalityMatches markup (role=listbox/option, bold teal label + `, ${state}`, kind pill derived from mtfcc, "Stances" badge when `has_local_data`), each `<li id="location-option-{i}">` wired via `getItemProps` with `onClick` → `onSelectCandidate(candidate)`. Inline rows below the field: for `kind==='address'` the Enter-hint "Press Enter to look up this address"; for `kind==='coordinate'` "Press Enter to look up this location"; for a name query returning 0 candidates the coral no-match row with the exact UI-SPEC copy including the `{query}` interpolation. On Enter/Search-click: dispatch by `classifyInput(value).kind` → call `onSubmitAddress`, `onSubmitCoordinate(lat,lng,value)`, or (name with an active candidate) `onSelectCandidate`. Coordinate 422 error rows are rendered by the HOST (Plan 03) since the combobox does not itself call lookupCoordinate — expose an optional `errorRow` prop (message string) the host passes to render the coral error in the hint-row slot. Theme both light and dark via existing Tailwind `dark:` variants and `--ev-*` tokens (no `!important`, no new tokens). 44x44px minimum touch targets on the submit button and option rows.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (component compiles, imports resolve).
    - `grep -n "role=\"combobox\"\|aria-expanded\|aria-controls\|aria-autocomplete\|aria-activedescendant" src/components/LocationCombobox.jsx` shows all five ARIA attributes present.
    - `grep -n "useListNavigation" src/components/LocationCombobox.jsx` present; `grep -n "addEventListener" src/components/LocationCombobox.jsx` returns ZERO (no reintroduced document-level keydown capture).
    - `grep -n "dangerouslySetInnerHTML\|pac-container\|window.google\|@googlemaps" src/components/LocationCombobox.jsx` returns ZERO.
    - Debounce effect body contains a `classifyInput(...)` gate and a `>= 3` length check (scoped read confirms the network call only fires for kind==='name').
    - Component is fully controlled: `grep -n "\.current\.value" src/components/LocationCombobox.jsx` returns ZERO (no ref-reading workaround carried forward — RESEARCH Pitfall 2).
  </acceptance_criteria>
  <done>Accessible combobox compiles; ARIA + virtual list-nav present; name-only debounced resolver call gated by the classifier; candidate rows, hint rows, and no-match row render per UI-SPEC; no Google/pac-container/imperative-DOM-write residue.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor localitySearch.js — drop Google classifyQuery, expose Google-free browseAreaRoute (D-09 partial)</name>
  <read_first>
    - src/lib/localitySearch.js (current file — the `@googlemaps/js-api-loader` import, `classifyQuery`, `resolveLocalityRoute`, `browseAreaRoute`; PATTERNS.md lines 314-342)
    - 214-CONTEXT.md D-09 (refactor-in-place OR delete-and-replace is discretion — as long as no Google dependency survives and covered routing still works) and Claude's Discretion notes
    - 214-RESEARCH.md "Google Places Removal Surface" localitySearch.js row + Dispatch/Navigation Wiring "Place-name path"
  </read_first>
  <action>
    Remove the `import { setOptions, importLibrary } from '@googlemaps/js-api-loader'` line and delete the `classifyQuery()` function (its Geocoder classification role is superseded by inputClassifier.js). Adapt `browseAreaRoute` to consume the new `/location-search` candidate shape: build the URL from `{geo_id, mtfcc, label}` using `browse_geo_id`, `browse_mtfcc`, `browse_label`, `from_locality=1` — and drop the `cleanAreaName()` TIGER-suffix stripping (the resolver's `label` is already the clean display string per 212's D-05/D-07 contract). Keep `browseAreaRoute` exported for the host pages. Preserve `resolveLocalityRoute`'s outer `{kind:'address'|'browse'|'coverage', to}` return contract for Landing's coverage fallback IF you refactor in place; you MAY delete `resolveLocalityRoute` entirely if Plans 03/04 no longer need it (coordinate/document that choice in the SUMMARY). Whatever remains, the file must import nothing from `@googlemaps/*` and reference no `window.google`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "@googlemaps|window\.google|classifyQuery|importLibrary" src/lib/localitySearch.js` returns ZERO.
    - `grep -n "browseAreaRoute" src/lib/localitySearch.js` shows it exported and building `browse_geo_id`/`browse_mtfcc`/`browse_label`/`from_locality` params.
    - `npm run build` succeeds and `npm test` stays green.
  </acceptance_criteria>
  <done>localitySearch.js has no Google dependency; browseAreaRoute consumes the resolver candidate shape and builds the browse URL; build + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver response → JSX | Candidate `label`/`state` strings from /location-search rendered into the listbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-03 | Tampering / Information Disclosure (reflected XSS) | LocationCombobox candidate rows | mitigate | Candidate labels rendered only as JSX text nodes (React auto-escapes); no `dangerouslySetInnerHTML` anywhere in the component (grep-gated in acceptance) |
| T-214-04 | Denial of Service (resolver call storm) | debounce effect | mitigate | Debounced (~250ms) AND gated on `kind==='name'` + ≥3 chars so address/coordinate typing never streams partial-fragment resolver calls (RESEARCH Pitfall 4) |
</threat_model>

<verification>
- `npm run build` succeeds; `npm test` green.
- Scoped greps in acceptance criteria confirm no Google residue and no reintroduced keydown-capture hack.
</verification>

<success_criteria>
A shared, accessible, fully-controlled `<LocationCombobox>` exists with WAI-ARIA combobox semantics, virtual list navigation, classifier-gated debounced name search, inline hint/no-match rows, and distinct submit callbacks; localitySearch.js is Google-free and exposes browseAreaRoute for candidate dispatch.
</success_criteria>

<output>
Create `.planning/phases/214-unified-location-combobox-google-places-removal/214-02-SUMMARY.md` when done, documenting whether resolveLocalityRoute was kept or deleted.
</output>
