---
phase: 122-ma-tier-3-stances-wave-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/598_laredo_stances.sql
  - C:/EV-Accounts/backend/migrations/599_albright_stances.sql
  - C:/EV-Accounts/backend/migrations/600_hume_charm_stances.sql
  - C:/EV-Accounts/backend/migrations/601_dahmubed_stances.sql
  - C:/EV-Accounts/backend/migrations/602_getz_stances.sql
  - C:/EV-Accounts/backend/migrations/603_golden_stances.sql
  - C:/EV-Accounts/backend/migrations/604_gordon_stances.sql
autonomous: true
requirements: [NEWTON-03]
must_haves:
  truths:
    - "Wave 0 pre-flight confirms next migration number, active topic count, and Somerville UUID mapping before any stance is written"
    - "Newton Mayor Laredo has evidence-only stance rows (or documented blank if no public record)"
    - "Newton councillors Albright, Hume Charm, Dahmubed, Getz, Golden, Gordon each have evidence-only stance rows or documented blanks"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/598_laredo_stances.sql"
      provides: "Mayor Laredo stance migration (or zero-stance ledger file)"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/603_golden_stances.sql"
      provides: "Councillor Golden stance migration"
      contains: "inform.politician_answers"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Run Wave 0 pre-flight verification, then research and apply evidence-only compass stances for the first 7 Newton officials (Mayor Laredo + councillors Albright, Hume Charm, Dahmubed, Getz, Golden, Gordon), one politician at a time.

Purpose: Establish the verified migration starting point and topic reference for the entire phase, and begin closing NEWTON-03.
Output: 7 per-individual stance migration files (598–604), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-RESEARCH.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 574_boston_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
</interfaces>

Newton roster (external_id → migration), from 122-PATTERNS.md (VERIFIED migration 578):
  -2545560001 Marc C. Laredo (Mayor)        → 598
  -2545560002 Susan Albright                 → 599
  -2545560003 Brittany Hume Charm            → 600
  -2545560004 Cyrus Dahmubed                 → 601
  -2545560005 Rena Getz                      → 602
  -2545560006 Brian Golden                   → 603
  -2545560007 Lisa Gordon                    → 604
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 pre-flight verification</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md (Wave 0 Pre-Flight Queries section, lines 383-409)
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-RESEARCH.md (Open Questions section)
  </read_first>
  <action>
    Run four verification queries via mcp__supabase-local__execute_sql before writing any migration:
    (1) `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('596','597') ORDER BY version;` — confirm whether 597 (Quincy) is applied. If 597 is present, the first Newton migration number is 598. If 597 is absent on disk but a later number exists, run `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations;` and start at MAX+1; record the confirmed starting number in the SUMMARY.
    (2) `SELECT COUNT(*) FROM inform.compass_topics WHERE is_active = true;` — expected 44; if different, run `SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;` and use that live list as the topic reference block (do NOT trust the cached block if count differs).
    (3) `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2545560025 AND -2545560001);` — informational; upsert handles pre-existing rows.
    (4) `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id BETWEEN -2545560007 AND -2545560001 ORDER BY external_id DESC;` — resolve UUIDs for the 7 officials in this plan and confirm name-to-external_id mapping matches the roster table above.
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql with the active-topic count query returns a single integer; the migration-version query returns the highest applied version. Record both in SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - Starting migration number confirmed and recorded (598 unless DB shows a higher applied number)
    - Active compass topic count confirmed (44 expected; if not, live slug→id list captured for use in headers)
    - UUIDs resolved for external_ids -2545560001 through -2545560007 with names matching the roster
  </acceptance_criteria>
  <done>Pre-flight queries run; starting migration number, topic count, and 7 Newton UUIDs confirmed and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Mayor Laredo + councillors Albright, Hume Charm (598–600)</name>
  <files>C:/EV-Accounts/backend/migrations/598_laredo_stances.sql, C:/EV-Accounts/backend/migrations/599_albright_stances.sql, C:/EV-Accounts/backend/migrations/600_hume_charm_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format: BEGIN/COMMIT, float literals, dollar-quoting)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (authoritative on ::text[]::text[] double-cast)
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md (Full Minimal Valid Migration Template, lines 291-351)
  </read_first>
  <action>
    Process ONE politician at a time, strictly sequentially — never run two research sessions in parallel (rate-limit rule, feedback_stance_research_one_at_a_time.md). For each of Laredo (-2545560001, Mayor), Albright (-2545560002, Ward 2 AL), Hume Charm (-2545560003, Ward 5 AL):
    (a) Research the official's public record across all active compass topics — campaign statements, votes, malegislature.gov sponsorships where applicable, newton.gov, local media. Place each topic with direct evidence on the 1.0–5.0 scale (1.0 most progressive/left, 5.0 most conservative/right per the topic axis definition).
    (b) Write the migration file using the canonical template. value is a float literal (e.g. 2.0); sources is ARRAY[...]::text[]::text[]; wrap in BEGIN;/COMMIT;; reasoning in $$...$$ citing the specific bill/vote/statement; include the topic UUID reference block (44 topics) in the header.
    (c) For any topic with NO direct evidence, write NO INSERT for that topic — never default to 3.0. If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; and zero INSERT blocks (Phase 115 ledger-consistency precedent), and note the blank result in the SUMMARY.
    (d) Apply via mcp__supabase-local__execute_sql, then immediately run the three per-person verification queries before moving to the next official.
    Mayor Laredo was inaugurated 2026-01-01 (thin record, expect 3–8 stances); senior councillor Albright has a multi-year record (expect more). Do NOT touch any external_id in the -2508610xxx range (Newton School Committee is out of scope per NEWTON-03).
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{uuid}' AND pc.politician_id IS NULL;` returns 0, AND `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - 3 migration files exist (598, 599, 600), each wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`; grep finds no bare-integer value lines (no `, [1-5])` without `.0`)
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT references an external_id in the -2508610xxx range
  </acceptance_criteria>
  <done>Migrations 598–600 written, applied, and verified (0 unpaired, 0 uncited) for Laredo, Albright, Hume Charm.</done>
</task>

<task type="auto">
  <name>Task 3: Councillors Dahmubed, Getz, Golden, Gordon (601–604)</name>
  <files>C:/EV-Accounts/backend/migrations/601_dahmubed_stances.sql, C:/EV-Accounts/backend/migrations/602_getz_stances.sql, C:/EV-Accounts/backend/migrations/603_golden_stances.sql, C:/EV-Accounts/backend/migrations/604_gordon_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
    - C:/EV-Accounts/backend/migrations/598_laredo_stances.sql (just-written sibling file as in-phase reference)
  </read_first>
  <action>
    Process ONE politician at a time, sequentially, for Dahmubed (-2545560004, Ward 4 AL), Getz (-2545560005, Ward 5 AL), Golden (-2545560006, Ward 7 AL), Gordon (-2545560007, Ward 6 AL). Same procedure as Task 2: research public record across all active topics, write canonical migration (float values, double-cast sources, BEGIN/COMMIT, dollar-quoted reasoning, topic UUID header), omit topics with no evidence (no 3.0 defaults), apply, then run per-person verification before the next official. Brian Golden (Ward 7 AL) is expected to have a richer record (public-policy voice); Dahmubed and Getz may be thin (0–3 stances, blank spokes common and acceptable). Key Newton topics: housing, residential-zoning, local-environment, growth-and-development, public-safety-approach, transportation-priorities. Do NOT touch -2508610xxx (Newton SC out of scope).
  </action>
  <verify>
    <automated>For each of the 4 UUIDs: unpaired-answer query returns 0 AND uncited-context query returns 0 (same two queries as Task 2).</automated>
  </verify>
  <acceptance_criteria>
    - 4 migration files exist (601–604), each wrapped in BEGIN;/COMMIT;
    - Every value is a float literal; every sources array double-cast
    - Unpaired-answer query returns 0 for all 4 UUIDs
    - Uncited-context query returns 0 for all 4 UUIDs
    - No INSERT references -2508610xxx
  </acceptance_criteria>
  <done>Migrations 601–604 written, applied, and verified for Dahmubed, Getz, Golden, Gordon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (mcp__supabase-local) | Writes go directly to live production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | stance value integrity | mitigate | Evidence-only rule: no INSERT without a direct cited source; per-person uncited-context query must return 0 |
| T-122-02 | Information Disclosure | reasoning text / sources | accept | All data is public-record political positions with public source URLs; no PII |
| T-122-03 | Repudiation | stance provenance | mitigate | Every value paired with a context row carrying path-bearing source URLs (100% citation rate) |
| T-122-04 | Tampering | accidental SC scope creep | mitigate | Acceptance criteria asserts no -2508610xxx external_id appears in any migration |
| T-122-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After all three tasks, run the partial phase-wide citation check for this plan's officials:
`SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2545560007 AND -2545560001) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` must return 0.
</verification>

<success_criteria>
- Wave 0 pre-flight complete; starting migration number and topic count confirmed
- Migrations 598–604 (7 officials) written, applied to production, and verified
- 0 unpaired answers, 0 uncited contexts across all 7 officials
- No School Committee external_ids touched
- Blank-spoke officials documented in SUMMARY (not defaulted)
</success_criteria>

<output>
Create `.planning/phases/122-ma-tier-3-stances-wave-1/122-01-SUMMARY.md` when done. Record: confirmed starting migration number, active topic count, per-official stance counts, and any blank-spoke officials.
</output>
