---
phase: 214-unified-location-combobox-google-places-removal
plan: 04
type: execute
wave: 3
depends_on: [214-02]
files_modified:
  - src/pages/Landing.jsx
autonomous: true
requirements: [SRCH-05, SRCH-06]
user_setup: []

must_haves:
  truths:
    - "The Landing search bar is the SAME <LocationCombobox> component used on Results (one shared implementation, not a parallel one)"
    - "A coordinate submit on Landing navigates to Results via the Plan 02 coordinateRoute(lat,lng,raw) contract (/results?lat=..&lng=..&coord_raw=..)"
    - "Landing's coverage list still renders and its click-to-browse routing is unchanged (D-04)"
    - "Landing's separate 'search candidates by name' feature is untouched (not conflated with location search)"
    - "Landing no longer imports useGooglePlacesAutocomplete or LocalityMatches"
  artifacts:
    - path: "src/pages/Landing.jsx"
      provides: "Combobox-driven Landing search bar sharing the Results component + coordinate hand-off"
      contains: "LocationCombobox"
  key_links:
    - from: "src/pages/Landing.jsx"
      to: "LocationCombobox"
      via: "search-bar render"
      pattern: "<LocationCombobox"
    - from: "src/pages/Landing.jsx"
      to: "coordinateRoute"
      via: "onSubmitCoordinate navigates to Results via the Plan 02 hand-off contract"
      pattern: "coordinateRoute"
    - from: "src/pages/Landing.jsx"
      to: "browseAreaRoute"
      via: "onSelectCandidate dispatch"
      pattern: "browseAreaRoute"
---

<objective>
Adopt the shared `<LocationCombobox>` as Landing.jsx's search bar (SRCH-06) — the same component instance powering Results, not a fork — while preserving Landing's coverage list as the browse entry point (D-04) and leaving its separate candidate-by-name search feature untouched. Wires the coordinate submit to the concrete Plan 02 `coordinateRoute(lat,lng,raw)` hand-off contract (SRCH-05) that Results reads on mount — a real dependency on Plan 02, not a same-wave read of Plan 03's summary. Removes Landing's Google Places wiring and `LocalityMatches` usage (import/usage only; the standalone files are deleted in Plan 05).

Purpose: Satisfies the milestone convention that Landing rides along inside this phase (it shares the exact Google-bound modules being retired), SRCH-06's one-shared-component requirement, and the SRCH-05 Landing→Results coordinate path via the published Plan 02 contract.
Output: Edited `src/pages/Landing.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-CONTEXT.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-RESEARCH.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-PATTERNS.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-UI-SPEC.md

<interfaces>
LocationCombobox props (from Plan 02): value, onChange, onSubmitAddress(raw), onSubmitCoordinate(lat,lng,raw), onSelectCandidate(candidate), placeholder, ariaLabel, errorRow.
browseAreaRoute(candidate) -> "/results?browse_geo_id=...&browse_mtfcc=...&browse_label=...&from_locality=1" (Plan 02).

COORDINATE HAND-OFF CONTRACT (Plan 02 — the checker-required, explicit Landing->Results seam):
  coordinateRoute(lat,lng,raw) -> "/results?lat=<lat>&lng=<lng>&coord_raw=<encodeURIComponent(raw)>".
  Landing is the WRITER: onSubmitCoordinate(lat,lng,raw) -> navigate(coordinateRoute(lat,lng,raw)).
  Results (Plan 03) reads `lat`/`lng`/`coord_raw` on mount and resolves via its shared resolveCoordinate.
  Both Landing and Results depend on 214-02 (where coordinateRoute lives) — NOT on each other. Do NOT
  read 214-03-SUMMARY.md to discover this contract; it is fully specified in 214-02.

On Landing, an address-classified submit navigates directly to /results?q=<raw> (skip the old
resolveLocalityRoute Google detour — RESEARCH Dispatch Wiring).
</interfaces>

<note>
Line numbers in PATTERNS.md/RESEARCH.md are pre-edit references and may have drifted. Re-grep each target symbol in Landing.jsx before editing.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Landing search bar with shared LocationCombobox + coordinate hand-off (SRCH-06/05)</name>
  <read_first>
    - src/pages/Landing.jsx — re-grep for: `useGooglePlacesAutocomplete`, `LocalityMatches`, `handleSearch`, `resolveLocalityRoute`, `handleAreaClick`, `nameQuery`, and the router `navigate`/`useNavigate` usage (PATTERNS.md lines 275-310 enumerate keep vs. remove)
    - src/lib/localitySearch.js — confirm `coordinateRoute` and `browseAreaRoute` are exported (from Plan 02) and import both
    - 214-02 interfaces block — the coordinateRoute contract (`lat`/`lng`/`coord_raw`) this task navigates with
    - 214-RESEARCH.md "Landing integration" (lines 556-564) — coverage list + name-search stay; only the address input + LocalityMatches + Google hook block is replaced
    - 214-CONTEXT.md D-04 (Landing keeps coverage list as browse entry point), D-05 (raw typed coords carried client-side), SRCH-05, SRCH-06
    - 214-UI-SPEC.md (same combobox visual contract applies on Landing)
  </read_first>
  <action>
    Remove Landing's `useGooglePlacesAutocomplete` import + call and its `LocalityMatches` import + usage. Import `browseAreaRoute` and `coordinateRoute` from `src/lib/localitySearch.js`. Replace the address `<input>` + `<LocalityMatches>` JSX block with a `<LocationCombobox>` using the same props contract as Results: controlled `value`/`onChange` local state; `onSubmitAddress(raw)` → `navigate('/results?q=' + encodeURIComponent(raw))` (skip the old `resolveLocalityRoute(q)` Google detour entirely per RESEARCH Dispatch Wiring — an address goes straight to the Results address path); `onSelectCandidate(candidate)` → `navigate(browseAreaRoute(candidate))`; `onSubmitCoordinate(lat, lng, raw)` → `navigate(coordinateRoute(lat, lng, raw))` (the explicit Plan 02 hand-off contract — Results reads `lat`/`lng`/`coord_raw` on mount and resolves via its shared resolveCoordinate; do NOT call lookupCoordinate from Landing, and do NOT inspect Plan 03's summary to find the mechanism — it is the coordinateRoute contract from Plan 02). Update `handleSearch` accordingly or replace its call sites. DO NOT touch: `handleAreaClick` + the coverage list rendering (D-04 browse entry point), and the separate `nameQuery`/candidate-by-name search feature (RESEARCH explicitly warns not to conflate it with location search).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useGooglePlacesAutocomplete\|<LocalityMatches\|import LocalityMatches" src/pages/Landing.jsx` returns ZERO.
    - `grep -n "<LocationCombobox" src/pages/Landing.jsx` returns exactly one usage (same component import path as Results — confirm both import `../components/LocationCombobox`).
    - `grep -n "coordinateRoute" src/pages/Landing.jsx` shows the onSubmitCoordinate handler navigating via the Plan 02 coordinateRoute contract (no `lookupCoordinate` call in Landing.jsx: `grep -n "lookupCoordinate" src/pages/Landing.jsx` returns ZERO).
    - `grep -n "handleAreaClick\|COVERAGE_STATES\|nameQuery" src/pages/Landing.jsx` still present (coverage list + name-search feature preserved — D-04, non-conflation).
    - `npm run build` succeeds; `npm test` stays green.
  </acceptance_criteria>
  <done>Landing's search bar is the shared LocationCombobox; Google hook + LocalityMatches usage removed; coordinate submit hands off to Results via the explicit coordinateRoute contract; coverage list and candidate-by-name search untouched; address/place/coordinate submits route correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate/query/coordinate → navigate() | Landing builds internal Results routes from user input (place, address, coordinate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-05 | Spoofing (open redirect) | Landing onSelectCandidate / onSubmitAddress / onSubmitCoordinate | mitigate | All navigation targets are internal `/results?...` built via browseAreaRoute / coordinateRoute + URLSearchParams/encodeURIComponent from geo_id/mtfcc/label/lat/lng/raw text; never navigate to a candidate-supplied full URL |
| T-214-02 | Information Disclosure | Landing onSubmitCoordinate | mitigate | Landing does not log the raw {lat,lng}; it only builds the internal coordinateRoute URL from the user's own typed input (client-sourced, upholding the D-05 / 213 no-echo spirit) |
| T-214-07 | Tampering (scope creep into unrelated feature) | Landing nameQuery search | accept | The candidate-by-name search is explicitly out of scope and left untouched; conflating it would risk regressing a live feature — guarded by the "still present" acceptance grep |
</threat_model>

<verification>
- `npm run build` succeeds; `npm test` green.
- Manual smoke (Plan 06 checkpoint): Landing search bar resolves all three input types; a coordinate submit navigates to Results and resolves there via the lat/lng/coord_raw params; coverage list still browses; name-search still works.
</verification>

<success_criteria>
Landing uses the identical shared LocationCombobox component, its coordinate submit hands off to Results through the explicit Plan 02 coordinateRoute contract, its coverage list and candidate-by-name search remain intact, and no Google Places or LocalityMatches wiring remains in Landing.jsx.
</success_criteria>

<output>
Create `.planning/phases/214-unified-location-combobox-google-places-removal/214-04-SUMMARY.md` when done, confirming the coordinate hand-off uses the Plan 02 coordinateRoute contract (not a Landing-local lookupCoordinate call).
</output>
</output>
