---
phase: 214-unified-location-combobox-google-places-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/inputClassifier.js
  - src/lib/inputClassifier.test.js
  - src/lib/api.jsx
  - src/lib/api.test.js
autonomous: true
requirements: [SRCH-03, SRCH-04, SRCH-05]
user_setup: []

must_haves:
  truths:
    - "classifyInput() buckets any string into exactly one of coordinate / address / name / empty"
    - "searchLocationsByName() returns a candidate array regardless of bare-array vs {candidates:[]} response shape"
    - "lookupCoordinate() distinguishes success, the 3 distinct 422 codes, and network/5xx errors"
    - "Neither new api function triggers an auth-redirect on 401 (uses publicFetch, not apiFetch)"
  artifacts:
    - path: "src/lib/inputClassifier.js"
      provides: "Pure classifyInput(raw) -> {kind, lat?, lng?}"
      exports: ["classifyInput"]
    - path: "src/lib/inputClassifier.test.js"
      provides: "SRCH-03 classification unit coverage incl. documented edge cases"
    - path: "src/lib/api.jsx"
      provides: "searchLocationsByName + lookupCoordinate client functions"
      contains: "searchLocationsByName"
    - path: "src/lib/api.test.js"
      provides: "SRCH-04/SRCH-05 response-shape + 422-code unit coverage with mocked fetch"
  key_links:
    - from: "src/lib/api.jsx"
      to: "publicFetch"
      via: "import from src/lib/auth.js"
      pattern: "publicFetch"
---

<objective>
Build the two pure, DOM-free foundations the combobox depends on: the client-side input classifier (D-02/D-06, SRCH-03) and the two new anonymous api.jsx client functions that call the already-live Phase 212 place-name resolver (SRCH-04) and Phase 213 coordinate-lookup endpoint (SRCH-05). Both ship with colocated Vitest unit tests — these are the Wave 0 test gaps named in 214-RESEARCH.md and 214-VALIDATION.md.

Purpose: Every downstream plan (combobox, Results, Landing) imports these; defining and testing them first gives executors the contracts up front instead of a scavenger hunt.
Output: `inputClassifier.js`, `api.jsx` additions, and two new test files, all green under `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-CONTEXT.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-RESEARCH.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-PATTERNS.md

<interfaces>
<!-- Contracts this plan defines. Downstream plans consume these exact shapes. -->

Phase 212 /essentials/location-search candidate shape (per 212-CONTEXT.md):
  { geo_id: string, mtfcc: string, label: string, state: string, has_local_data: boolean }
  Response envelope is EITHER a bare array OR { candidates: [...] } — handle both defensively (RESEARCH A2).

Phase 213 POST /essentials/coordinate-lookup contract (per 213-CONTEXT.md, live-smoke-verified):
  Request body: { lat: number, lng: number }
  Success: AddressSearchResult-shaped — { politicians: [...], ... } OR a flat array; matchedAddress is ALWAYS "" (no coordinate echo — 213 privacy contract).
  422 body: { code: "OUTSIDE_US_BOUNDS" | "SWAPPED_COORDINATES" | "INVALID_COORDINATES", ... }

classifyInput(raw) return contract this plan defines:
  { kind: 'empty' } | { kind: 'coordinate', lat: number, lng: number } | { kind: 'address' } | { kind: 'name' }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure input classifier + unit tests (SRCH-03)</name>
  <read_first>
    - src/lib/classify.js and src/lib/classify.test.js (the project's pure-classifier + colocated-vitest precedent to mirror exactly — PATTERNS.md lines 106-128)
    - 214-RESEARCH.md "Input Classification Heuristic" (drafted implementation, lines 369-426) and "Open Questions" 1 & 2 (documented misclassification edge cases)
    - 214-CONTEXT.md D-02, D-06 (locked split: coords + addresses classified locally, names → resolver; regexes/thresholds are discretion)
  </read_first>
  <behavior>
    - "39.17, -86.52" -> { kind: 'coordinate', lat: 39.17, lng: -86.52 }
    - "-33.9, 151.2" (negative lat) -> coordinate
    - "123 Main St" -> { kind: 'address' } (leading street number)
    - "90210" and "90210-1234" -> { kind: 'address' } (ZIP flows through existing Census path, never the resolver)
    - "Bloomington" -> { kind: 'name' }
    - "5th Ward" -> { kind: 'name' } (digit glued to letters, no space — NOT an address)
    - "" and "   " -> { kind: 'empty' }
    - DOCUMENTED-GAP tests (assert current behavior so the tradeoff is pinned, not silently drifting): "5 Points" -> address (Open Question 1); "39.17 -86.52" without comma -> address (Open Question 2). Add an inline comment on each of these two tests naming it as an accepted v1 tradeoff.
  </behavior>
  <action>
    Create `src/lib/inputClassifier.js` exporting a single pure function `classifyInput(raw)` returning the `{ kind, lat?, lng? }` contract in the interfaces block. Define three module-level regex constants matching RESEARCH's draft: a comma-separated decimal-pair `COORDINATE_RE`, a leading-street-number `ADDRESS_LEADING_DIGIT_RE`, and a `ZIP_RE`. Order of checks: trim → empty → coordinate → address (leading-digit OR zip) → fall through to name. On coordinate match, parse `lat`/`lng` via `Number()` on the comma-split halves. Header comment must tag the file with SRCH-03 / D-02 (mirror classify.js's requirement-ID header style). No DOM, no imports, no side effects. Create `src/lib/inputClassifier.test.js` mirroring classify.test.js structure: one `describe` per kind, `it`/`it.each` for the table cases in the behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/lib/inputClassifier.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/inputClassifier.test.js` exits 0 with every behavior-block case as a passing assertion.
    - `src/lib/inputClassifier.js` exports `classifyInput` and imports nothing (grep: no `import ` line in the file).
    - The two documented-gap cases ("5 Points", space-separated coords) each have a test asserting the current (accepted-tradeoff) result with an inline comment referencing Open Question 1/2.
  </acceptance_criteria>
  <done>classifyInput correctly buckets all behavior-block inputs; test file green; file is pure (zero imports, zero side effects).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: searchLocationsByName + lookupCoordinate api clients + tests (SRCH-04, SRCH-05)</name>
  <read_first>
    - src/lib/api.jsx browseByArea / fetchBrowseAreas (lines ~318-345) and searchPoliticians 422/error-body parsing (lines ~78-123) — the exact structural analogs (PATTERNS.md lines 132-204)
    - src/lib/auth.js publicFetch vs apiFetch (publicFetch never redirects on 401 — PATTERNS.md lines 184-204)
    - 214-RESEARCH.md "Two New api.jsx Client Functions" (drafted implementations, lines 428-501) and Assumptions A1/A2/A3
    - 214-CONTEXT.md D-05 (coordinate privacy: matchedAddress is empty by contract)
  </read_first>
  <behavior>
    - searchLocationsByName("Springfield"): mocked fetch 200 with a bare array -> { data: [...], error: null }; mocked 200 with { candidates: [...] } -> same data array; mocked non-ok -> { data: [], error: "<status>" }; thrown fetch -> { data: [], error: <message> }.
    - lookupCoordinate(39.17,-86.52): mocked 200 { politicians:[...], matchedAddress:"" } -> { data: politicians, error: null, code: null, formattedAddress: "" }; mocked 200 flat array -> data = that array.
    - lookupCoordinate 422 with body {code:"SWAPPED_COORDINATES"} -> { data:[], error:"validation", code:"SWAPPED_COORDINATES" }; likewise OUTSIDE_US_BOUNDS and INVALID_COORDINATES each mapped through; 422 with unparseable/missing code -> code defaults to "INVALID_COORDINATES".
    - lookupCoordinate non-422 non-ok (500) -> { data:[], error:"500", code:null }.
    - Both functions call publicFetch (assert the mocked publicFetch/global.fetch was invoked and no login redirect path fires).
  </behavior>
  <action>
    Append two exported async functions to `src/lib/api.jsx`, matching the file's established try/catch + `{ data, error }` idiom. `searchLocationsByName(query)`: GET `/essentials/location-search?q=${encodeURIComponent(query)}` via `publicFetch`; unwrap defensively `Array.isArray(data) ? data : (data.candidates || [])`. `lookupCoordinate(lat, lng)`: POST `/essentials/coordinate-lookup` via `publicFetch` with JSON body `{ lat, lng }`; on `res.status === 422` parse the body and thread `code` (default `INVALID_COORDINATES`) with `error: 'validation'`; on other non-ok return `{ data: [], error: '<status>', code: null }`; on success unwrap `politicians` defensively and set `formattedAddress` from `data.matchedAddress ?? ''`. Use `publicFetch` NOT `apiFetch` (both endpoints anonymous — apiFetch's 401→login redirect is wrong for a typeahead). Create `src/lib/api.test.js` (new file — no existing api test to extend) mocking `global.fetch` (or the `auth.js` module) per Vitest convention; cover every behavior-block case.
    Live-shape spot-check (RESEARCH A1/A2/A3): before finalizing, run one live curl against accounts-api for `/essentials/location-search?q=Springfield` and `/essentials/coordinate-lookup` to confirm the real envelope matches the defensive unwrap; if the wrapper key differs, adjust the unwrap and note it in the SUMMARY. Do not block on this if the network is unavailable — the defensive unwrap already tolerates both known shapes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/api.test.js` exits 0 with all behavior-block cases passing.
    - `grep -n "apiFetch" src/lib/api.jsx` shows the two NEW functions do not reference apiFetch (they use publicFetch); a scoped read confirms both new bodies call `publicFetch`.
    - Each of the 3 coordinate 422 codes maps to a `code` field in the returned object; a 422 with no parseable code defaults to `INVALID_COORDINATES`.
    - `npm test` (full suite) stays green after the additions.
  </acceptance_criteria>
  <done>Both functions exist, use publicFetch, thread the 422 taxonomy, tolerate both response envelopes; api.test.js green; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → accounts-api | Anonymous public GET/POST to /essentials/location-search and /coordinate-lookup; untrusted user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-01 | Spoofing (client-as-validator) | inputClassifier.js / lookupCoordinate | mitigate | Client classification is UX only, never a security boundary — lookupCoordinate always calls the real Phase 213 endpoint and honors its server-side 422 (bounds + swap guard); frontend never short-circuits on "looks valid" (ASVS V5) |
| T-214-02 | Information Disclosure | lookupCoordinate | accept | Function returns coordinates only to its own caller; matchedAddress is "" by 213 contract. No logging of lat/lng added here (analytics restraint enforced in Plan 03 where the call is wired) |
</threat_model>

<verification>
- `npm test` green (both new test files included).
- `npm run build` succeeds (no import/syntax regressions from the api.jsx additions).
</verification>

<success_criteria>
classifyInput and the two api client functions exist with the exact contracts in the interfaces block, all covered by green unit tests, and the full suite plus production build pass.
</success_criteria>

<output>
Create `.planning/phases/214-unified-location-combobox-google-places-removal/214-01-SUMMARY.md` when done, noting any live-curl envelope findings (RESEARCH A1/A2/A3).
</output>
