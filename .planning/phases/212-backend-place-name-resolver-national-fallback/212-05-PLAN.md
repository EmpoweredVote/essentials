---
phase: 212-backend-place-name-resolver-national-fallback
plan: 05
type: execute
wave: 4
depends_on: ["212-04"]
files_modified:
  - C:/EV-Accounts/backend/src/routes/essentialsLocationSearch.ts
  - C:/EV-Accounts/backend/src/index.ts
autonomous: false
requirements: [RSLV-01, RSLV-04, RSLV-05, RSLV-06, RSLV-07]
user_setup: []
must_haves:
  truths:
    - "GET /api/essentials/location-search?q= returns the resolver's ranked candidate list, 422 for q shorter than 2 chars, optionalAuth, with Cache-Control"
    - "A resolve path returns the national-fallback floor (US Senators + Governor/state execs from getStatewideOfficials/getFederalOfficials) for ANY resolved US location, including Gazetteer-only places, plus county officials where a county government exists, plus the CD-overlap note"
    - "The live deployed endpoint disambiguates Springfield/Baltimore/Franklin and never returns a different state's officials"
    - "EXPLAIN ANALYZE on the shipped resolver query shows an index scan (not a sequential scan)"
  artifacts:
    - path: "C:/EV-Accounts/backend/src/routes/essentialsLocationSearch.ts"
      provides: "GET /location-search (candidates) + GET /location-search/resolve (national fallback)"
      min_lines: 50
    - path: "C:/EV-Accounts/backend/src/index.ts"
      provides: "router mount at /api/essentials/location-search"
      contains: "essentialsLocationSearch"
  key_links:
    - from: "essentialsLocationSearch route"
      to: "searchPlaceNames + getStatewideOfficials + getFederalOfficials + getCongressionalOverlapNote"
      via: "route handlers call the service functions"
      pattern: "searchPlaceNames|getStatewideOfficials"
    - from: "src/index.ts"
      to: "essentialsLocationSearchRouter"
      via: "app.use('/api/essentials/location-search', ...)"
      pattern: "location-search"
---

<objective>
Expose the resolver over HTTP and wire the national-fallback floor, then smoke-test the whole phase LIVE. A new route file adds `GET /api/essentials/location-search?q=` (ranked candidates, RSLV-01/07) and `GET /api/essentials/location-search/resolve` (the guaranteed floor: US Senators + Governor/state execs for ANY resolved location — reusing the existing unconditional functions — plus county officials where seeded and the CD-overlap note, RSLV-05/06). After deploy to Render (`master`), a [BLOCKING] curl/psql smoke test proves the phase against production. This must ship and be smoke-tested live before Phase 214 consumes it.

Purpose: RSLV-05's floor is only real if a Gazetteer-only place (no curated geo_id) STILL returns state+federal — the resolve path guarantees that by feeding the candidate's own `state` into getStatewideOfficials/getFederalOfficials (never inferred). Independently consumable + smoke-tested (backend-before-frontend hard dependency).
Output: New route file + index.ts mount; live smoke-test evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-04-SUMMARY.md

<interfaces>
<!-- Service functions to call (all confirmed via direct reads). -->
From C:/EV-Accounts/backend/src/lib/locationSearchService.ts (Plan 04):
  export async function searchPlaceNames(query: string, limit: number): Promise<Candidate[]>
    // Candidate = { geo_id, mtfcc, label, state, area_type, has_local_data }

From C:/EV-Accounts/backend/src/lib/essentialsBrowseService.ts (reuse UNCHANGED except the Plan 04 helper):
  export async function getStatewideOfficials(stateAbbrev: string): Promise<PoliticianFlatRecord[]>   // runs on state alone
  export async function getFederalOfficials(): Promise<PoliticianFlatRecord[]>                         // runs unconditionally
  export async function getPoliticiansByGovernmentList(governmentGeoIds: string[], stateAbbrev?: string, options?: {countyGeoId?: string; skipOverlap?: boolean}): Promise<PoliticianFlatRecord[]>
  export async function getOverlappingGeoIdsForArea(geoId: string, mtfcc: string): Promise<{ geoIds: string[]; geoPairs: GeoPair[]; stateAbbrev: string | null }>
  export async function getCongressionalOverlapNote(geoId: string, mtfcc: string): Promise<{ cdGeoIds: string[]; needsExactAddress: boolean }>  // Plan 04

Route conventions to mirror:
  C:/EV-Accounts/backend/src/routes/essentialsCandidates.ts — search-by-name: q.length < 2 → res.status(422).json({code:'VALIDATION_ERROR',...}); optionalAuth; res.setHeader('Cache-Control','public, max-age=30').
  C:/EV-Accounts/backend/src/routes/essentialsBrowse.ts — Router() + optionalAuth + try/catch → 500 {code:'INTERNAL_ERROR'} pattern; STATE_FIPS map for FIPS→abbrev.
  C:/EV-Accounts/backend/src/index.ts (~line 145-152) — app.use('/api/essentials/...', router) mount block; mount the NEW router at '/api/essentials/location-search'. Note: /api/essentials (catch-all essentialsRouter) is mounted LAST (line 152) — register the new specific mount BEFORE it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New route file (search + resolve) + mount, with national-fallback wiring (RSLV-01/04/05/06/07)</name>
  <files>C:/EV-Accounts/backend/src/routes/essentialsLocationSearch.ts, C:/EV-Accounts/backend/src/index.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/src/routes/essentialsCandidates.ts (422-on-short-query, optionalAuth, Cache-Control convention)
    - C:/EV-Accounts/backend/src/routes/essentialsBrowse.ts (Router/try-catch/500 convention + STATE_FIPS)
    - C:/EV-Accounts/backend/src/index.ts (router import + app.use mount block, lines ~30-40 and ~145-152)
    - C:/EV-Accounts/backend/src/lib/locationSearchService.ts (searchPlaceNames — Plan 04)
    - C:/EV-Accounts/backend/src/lib/essentialsBrowseService.ts (getStatewideOfficials/getFederalOfficials/getPoliticiansByGovernmentList/getCongressionalOverlapNote)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md §"Pattern 3" (reuse, don't reimplement), §"Security Domain" (V5 input validation)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md §D-01, §D-03
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/src/routes/essentialsLocationSearch.ts as an Express Router with optionalAuth. Route A `GET /` → validate q is a string with length >= 2 (else 422 {code:'VALIDATION_ERROR'}), clamp limit to 1..50, call searchPlaceNames(q, limit), set Cache-Control public max-age=30, return the candidate list; on error 500 {code:'INTERNAL_ERROR'}. Route B `GET /resolve` → read geo_id, mtfcc, state (2-letter) query params; validate state matches /^[A-Z]{2}$/ (422 otherwise); assemble the national-fallback floor by REUSING (never reimplementing) getStatewideOfficials(state) + getFederalOfficials() [always run — D-01 floor], getCongressionalOverlapNote(geo_id, mtfcc) [RSLV-06 CD note], and, best-effort, county/local officials via getPoliticiansByGovernmentList([geo_id], state) where a government exists (D-03 — honest omission where none, never fabricate); return { statewide, federal, county, congressional: {cdGeoIds, needsExactAddress} }. The state fed to getStatewideOfficials MUST be the caller-supplied resolved candidate state (which the resolver sourced from the matched row) — never inferred from a name string (RSLV-07). Do NOT import or call geocodeAddress anywhere in this file (RSLV-04). Then mount in src/index.ts: import essentialsLocationSearchRouter and add app.use('/api/essentials/location-search', essentialsLocationSearchRouter) BEFORE the catch-all app.use('/api/essentials', essentialsRouter) line.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npm run build && node -e "const s=require('fs').readFileSync('src/index.ts','utf8');if(!/location-search/.test(s))throw new Error('mount missing');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc) passes with the new route file + mount
    - essentialsLocationSearch.ts: GET / returns 422 for q.length<2; GET /resolve validates state /^[A-Z]{2}$/ and calls getStatewideOfficials+getFederalOfficials+getCongressionalOverlapNote (grep confirms all three)
    - src/index.ts mounts '/api/essentials/location-search' BEFORE the '/api/essentials' catch-all
    - grep: route file does NOT import geocodeAddress (RSLV-04); reuses existing fallback functions (no new statewide/federal SQL)
  </acceptance_criteria>
  <done>Both routes live in code, mounted, type-checking; national-fallback floor wired by reuse; RSLV-04 boundary preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Live curl/psql smoke test of the deployed resolver + national-fallback endpoints</name>
  <files>(human-verify checkpoint — no executor file action; exercises the deployed accounts-api endpoints + live DB EXPLAIN)</files>
  <read_first>
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-05-SUMMARY.md (routes shipped in Task 1)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md §"Pitfall 3" (Springfield/Baltimore/Franklin regression cases), §"Pitfall 4" (EXPLAIN index scan)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md §"backend-before-frontend" (must be smoke-tested live before Phase 214)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor file action beyond running the listed curl/psql commands. The operator confirms the deployed endpoints pass all seven live checks below (multi-state Springfield, Baltimore city+county, ranked has_local_data, Gazetteer-only /resolve floor, no geocoder on the name path, EXPLAIN index scan, suite green) BEFORE Phase 212 is considered shippable and Phase 214 consumes it. See &lt;how-to-verify&gt;.</action>
  <what-built>Deployed `GET /api/essentials/location-search` (ranked candidates) + `GET /api/essentials/location-search/resolve` (national-fallback floor + CD note) to Render (accounts-api, push to `master`), backed by the live trgm indexes + Gazetteer data from Plan 03.</what-built>
  <how-to-verify>
    Against the live accounts-api host (accounts-api.empowered.vote):
    1. RSLV-07 collision — curl "https://accounts-api.empowered.vote/api/essentials/location-search?q=Springfield" → JSON has MULTIPLE candidates with distinct `state` values (IL, MO, MA, ...); no single silent pick.
    2. RSLV-07 city/county — curl ".../location-search?q=Baltimore" → both a `City` (area_type) and a `County` candidate appear, each label formatted "Name, MD · City|County".
    3. RSLV-01/D-06/D-07 — curl ".../location-search?q=Bloomington" → ranked candidates; a deep-seeded one (has_local_data:true) ranks above bare Gazetteer rows.
    4. RSLV-05 floor (Gazetteer-only) — pick a Gazetteer-only place candidate (has_local_data:false) and curl ".../location-search/resolve?geo_id=<geo_id>&mtfcc=<mtfcc>&state=<ST>" → response includes non-empty `statewide` (Governor/state execs) + `federal` (US Senators) for that state; `congressional.needsExactAddress` present.
    5. RSLV-04 — confirm no place-name query path invokes the Census one-line geocoder (grep the deployed logs / code: geocodeAddress not called by these routes).
    6. Pitfall 4 index — psql "$DATABASE_URL" -c "EXPLAIN ANALYZE SELECT ... (the shipped resolver query with a sample q)" → plan shows a Bitmap Index Scan on an idx_*_name_trgm index, NOT a Seq Scan.
    7. Full backend suite green: cd C:/EV-Accounts/backend && npm run test.
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 checks pass live (multi-state Springfield, Baltimore city+county, ranked has_local_data, Gazetteer-only floor returns state+federal, no geocoder on name path, EXPLAIN index scan, suite green) — or describe the failing check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public client → GET /location-search + /resolve | Unauthenticated (optionalAuth) public read endpoints; q/geo_id/mtfcc/state are untrusted input |
| resolve → state/federal officials | The resolved `state` determines which officials return — wrong-state binding here IS the security failure RSLV-07 guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-13 | Tampering (SQLi) | q / geo_id / mtfcc / state params | mitigate | All values flow only into parameterized service queries; state validated /^[A-Z]{2}$/ at the route; q length-validated (V5) |
| T-212-14 | Spoofing (wrong-state) | /resolve state param | mitigate | Officials resolved strictly from the supplied resolved-candidate state (sourced from the matched row in Plan 04), never re-derived from a name; regression vs the 2026-06-28 `?q=` leak + 2026-07-12 representing_city hijack |
| T-212-15 | Denial of Service | typeahead hammering resolver | mitigate | GIN index (Plan 03) + min length 2 + LIMIT; EXPLAIN index-scan verified; frontend debounce flagged to Phase 214 (out of scope here) |
| T-212-16 | Information Disclosure | error/response bodies | mitigate | Generic {code:'INTERNAL_ERROR'} on 500; candidate whitelist from Plan 04; no internal UUIDs leaked |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `npm run build` + `npm run test` green
- Live curl: Springfield multi-state, Baltimore city+county, Bloomington ranked has_local_data, Gazetteer-only /resolve returns state+federal, EXPLAIN shows trgm index scan
- No geocodeAddress on the place-name path
</verification>

<success_criteria>
- Phase 212 endpoints live on accounts-api and smoke-tested against production
- National-fallback floor guaranteed for ANY resolved US location (incl. Gazetteer-only); wrong-state guard holds under the three collision cases
- Ready for Phase 214 to consume
</success_criteria>

<output>
Create `.planning/phases/212-backend-place-name-resolver-national-fallback/212-05-SUMMARY.md` recording the 7 live smoke-test results (incl. the EXPLAIN plan line).
</output>
