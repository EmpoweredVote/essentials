---
phase: 218-vacancies-missing-people
plan: 02
type: execute
wave: 2
depends_on: ["218-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1389_collin_seat_cited_incumbents.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts
autonomous: true
requirements: [COLLIN-PEOPLE-01]
must_haves:
  truths:
    - "The 18 directly-cited unseated/new-row offices are each linked to a real, cited politician row — per D-01"
    - "Every seated politician has party NULL (nonpartisan) — per D-05"
    - "No compass-stance rows are created as a side effect — per D-06"
    - "Re-run of the migration seats zero additional people (idempotent via politician_id IS NULL guard)"
  artifacts:
    - "C:/EV-Accounts/backend/migrations/1389_collin_seat_cited_incumbents.sql"
    - "C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts"
  key_links:
    - "Each DO block finds its office by governments.geo_id + offices.title + politician_id IS NULL, then UPDATE offices SET politician_id"
    - "New Blue Ridge/Lowry Crossing/Weston rows created in Plan 01 are the seating targets here"
---

<objective>
Seat the 20 offices for which RESEARCH found a directly-cited current officeholder: Anna (Place 3 Walden, Place 5 Baker), Blue Ridge (Mayor Williams, Place 1 Apple, Place 5 Chitwood), Fairview (Seat 2 Boggs, Seat 6 Works), Josephine (Place 5 Chappell), Parker (Mayor Pettle, Place 3 Pilgrim, Place 5 Barron), Plano (Place 7 Thomas), Princeton (Place 4 Rutledge), Van Alstyne (Mayor Atchison), Weston (Place 5 Johnston), and Lowry Crossing (the 5 confirmed ward members — Place 4 first-Ward-4 winner + Place 5-8 Madrid/Rios/Cash/second-Ward-4-winner). This is the bulk of D-01. The 7 flagged-uncertain seats (Fairview Seat 4, Van Alstyne Place 6, Nevada trio, Lucas Place 1/2) are deliberately NOT here — they get the deeper re-verify pass in Plan 03.

Purpose: Convert the researched, cited incumbents into linked politician rows so they render on browse.
Output: One idempotent data migration (DO $$ blocks, one per office, guarded on politician_id IS NULL) + apply script with post-seed SQL gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-vacancies-missing-people/218-CONTEXT.md
@.planning/phases/218-vacancies-missing-people/218-RESEARCH.md
@.planning/phases/218-vacancies-missing-people/218-VALIDATION.md
@.planning/phases/218-vacancies-missing-people/218-01-SUMMARY.md

# Established idempotent seating pattern (read before writing):
@C:/EV-Accounts/backend/migrations/097_tier_3_politicians.sql
@C:/EV-Accounts/backend/scripts/_apply-migration-1213.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the idempotent seating migration for the 18 cited offices</name>
  <files>C:/EV-Accounts/backend/migrations/1389_collin_seat_cited_incumbents.sql</files>
  <action>
    Re-confirm the next on-disk migration number first (drifts; 1389 expected after Plan 01's 1388). For EACH of the 18 offices listed in the objective, write one DO $$ block using the upgraded idempotent pattern in RESEARCH "Pattern: Idempotent office-seating DO block": SELECT the office by governments.geo_id + exact offices.title + `AND o.politician_id IS NULL` (the idempotency guard — skip if already seated); if found, INSERT the politician then UPDATE offices SET politician_id. Pull the exact name, geo_id, office title, source URL, valid_from/valid_to and term_date_precision for every seat from the RESEARCH "Per-City Research Findings" section and the Plan 01 SUMMARY (which locked the Lowry Crossing Place->member mapping and confirmed any roster changes). Non-negotiable rules: party = NULL and party_short_name = NULL for every row (D-05 nonpartisan — never seed party); is_vacant false, is_appointed false, is_active true, is_incumbent true; data_source = the citing city site/TML; do NOT create any inform.politician_answers / compass-stance rows (D-06). Honor the special valid_from dates RESEARCH calls out (Plano Place 7 = the 2026-01-31 special-election/swearing-in date; Princeton Place 4 = 2026-06-23 certification date; Van Alstyne term_date_precision='day'). Do NOT rename any office title (Lucas landmine is not in this plan, but keep the same discipline — join on the existing `Place N` title). If Plan 01's SUMMARY reported a roster change for Weston or Lowry Crossing, seat the corrected current person, not the RESEARCH default.
  </action>
  <verify>
    <automated>MISSING — SQL proof is asserted in Task 2's apply-script gates. This task is the authoring step; correctness is checked when applied.</automated>
  </verify>
  <done>18 DO blocks written, each guarded on politician_id IS NULL, each with party NULL, each citing a source in data_source, matching the RESEARCH per-city findings + Plan 01 SUMMARY corrections; no stance rows authored.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the migration + run post-seed SQL gates</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts</files>
  <action>
    Write the apply script mirroring _apply-migration-1213.ts (read SQL, run via pg Pool on DATABASE_URL, run inline gate queries, print PASS/FAIL, exit non-zero on any failure). Run it, then commit+push to EV-Accounts via `git -C "C:/EV-Accounts"` to deploy through Render. Report the per-city seated counts in the SUMMARY against the RESEARCH target table.
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsx scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts</automated>
    Embedded gates (all must pass): (a) all 18 target offices now have politician_id NOT NULL. (b) Antipartisan: 0 of the newly-seated politicians have a non-NULL party. (c) No-duplicate gate — `SELECT office_id, COUNT(*) FROM essentials.politicians WHERE is_active GROUP BY office_id HAVING COUNT(*) > 1` returns 0 rows. (d) Split-section gate ([[section_split_check]]) returns 0 rows. (e) Re-run seats 0 additional people (idempotency). (f) No inform.politician_answers rows were created for these politicians (D-06).
  </verify>
  <done>Migration applied + pushed; 18 offices seated with cited nonpartisan incumbents; no duplicates, no split-section defect, no stance side effects; re-run is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research citation -> politician row | Cited civic facts become the authoritative officeholder record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-03 | Tampering | fabricated/incorrect incumbent | high | mitigate | Evidence-only — every seat carries a cited data_source from RESEARCH per-city findings; only directly-cited seats are in this plan (flagged-uncertain deferred to Plan 03) |
| T-218-04 | Tampering | duplicate seat on re-run | medium | mitigate | politician_id IS NULL guard in every DO block + no-duplicate gate asserts 0 |
| T-218-05 | Information Disclosure | party leakage | low | mitigate | party/party_short_name forced NULL (D-05); antipartisan gate asserts 0 non-NULL |
</threat_model>

<verification>
- 18 offices seated; antipartisan clean; no duplicates; split-section clean; idempotent; no stance rows (VALIDATION.md COLLIN-PEOPLE-01 gate + Split-section gate).
</verification>

<success_criteria>
Every one of the 18 directly-cited offices shows a real, cited, nonpartisan incumbent; migration idempotent; no duplicate/split-section/stance defects introduced.
</success_criteria>

<output>
Create `.planning/phases/218-vacancies-missing-people/218-02-SUMMARY.md` when done — list each seated person, office, geo_id, and source; note any RESEARCH deviations for Plan 04 (headshots) and Plan 05 (verification).
</output>
