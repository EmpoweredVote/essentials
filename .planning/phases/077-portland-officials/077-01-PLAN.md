---
phase: 077-portland-officials
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql
autonomous: true
requirements: []
tags: [portland, oregon, government, chambers, districts, scaffold]
must_haves:
  truths:
    - "Portland OR government row exists with geo_id='4159000', state='OR'"
    - "5 chambers exist under Portland government (Mayor, City Council, City Auditor, City Administrator, City Attorney)"
    - "LOCAL_EXEC district exists for geo_id='4159000', state='or' so citywide offices can attach"
    - "Section-split detector returns 0 orphans for Portland government bodies"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql"
      provides: "Portland government + 5 chambers + LOCAL_EXEC district migration"
      contains: "City of Portland, Oregon, US"
  key_links:
    - from: "essentials.chambers.government_id"
      to: "essentials.governments.id (City of Portland, Oregon, US)"
      via: "subquery by name+state"
      pattern: "WHERE name='City of Portland, Oregon, US' AND state='OR'"
    - from: "essentials.districts (geo_id='4159000', district_type='LOCAL_EXEC')"
      to: "geofence_boundaries (Phase 72 G4110 Portland row)"
      via: "geo_id match"
      pattern: "geo_id='4159000'"
---

<objective>
Create the Portland OR city government scaffold so Plan 77-02 has chambers and districts to attach officials to.

Purpose: Plan 77-02 cannot insert politicians + offices until the parent government row, 5 chambers, and the citywide LOCAL_EXEC district exist (per D-01, D-09, D-10). All scaffold INSERTs use WHERE NOT EXISTS guards (no unique constraints on geo_id / government_id+name / geo_id+district_type), so the migration is safe to re-run.
Output: One applied SQL migration (230) creating 1 government row, 5 chambers, and 1 LOCAL_EXEC district; section-split check returns 0 orphans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/077-portland-officials/077-CONTEXT.md
@.planning/phases/077-portland-officials/077-RESEARCH.md
@.planning/phases/077-portland-officials/077-PATTERNS.md
@.planning/phases/76-portland-council-geofences/76-01-SUMMARY.md
@.planning/phases/68-berkeley-deep-seed/68-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write and apply migration 230 — Portland government + 5 chambers + LOCAL_EXEC district</name>
  <files>C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql</files>
  <read_first>
    - .planning/phases/077-portland-officials/077-RESEARCH.md (Pattern 1 SQL, Verified Government Name Format, Verified Chambers to Create, pre-flight queries 1–5)
    - .planning/phases/077-portland-officials/077-PATTERNS.md (Pattern Assignments → 230_portland_government_structure.sql; State Casing Rule; Idempotency Guards)
    - .planning/phases/76-portland-council-geofences/76-01-SUMMARY.md (confirms migration 229 applied, next=230; geo_id='4159000'; districts.state='or')
    - .planning/phases/68-berkeley-deep-seed/68-01-SUMMARY.md (Berkeley analog — 3-chamber government structure, both Mayor + Auditor share single LOCAL_EXEC district)
    - C:/EV-Accounts/backend/migrations/213_berkeley_government_structure.sql (closest existing migration analog — Mayor + City Auditor pattern with LOCAL_EXEC for both)
    - C:/EV-Accounts/backend/migrations/207_sd_government_structure.sql (5-chamber-ish variant analog — 3 chambers with LOCAL_EXEC)
    - C:/EV-Accounts/backend/migrations/229_portland_council_districts.sql (Phase 76 — confirms state='or' lowercase for Portland districts)
  </read_first>
  <action>
    Run all 5 pre-flight verification queries from 077-RESEARCH.md "Pre-flight Verification Queries" against the live DB via Supabase Management API (project ref kxsdzaojfaibhuzmclfq). Confirm:
      - Query 2: 0 rows for `essentials.governments WHERE name='City of Portland, Oregon, US' AND state='OR'`
      - Query 3: 4 rows for `essentials.districts WHERE geo_id LIKE 'portland-or-council-district-%'` (district_type='LOCAL', state='or')
      - Query 4: 0 rows for `essentials.districts WHERE geo_id='4159000'`
      - Query 5: '229' present, '230' absent in `supabase_migrations.schema_migrations`

    If any precondition fails STOP and surface — do not proceed.

    Create `C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql` wrapping all statements in `BEGIN; ... COMMIT;`. The migration MUST contain exactly:

    STEP 1 — Government row (1 INSERT, WHERE NOT EXISTS on name+state):
      - columns: `id, name, type, state, city, geo_id`
      - values: `gen_random_uuid(), 'City of Portland, Oregon, US', 'LOCAL', 'OR', 'Portland', '4159000'`
      - state IS uppercase 'OR' on governments rows (per 077-PATTERNS.md State Casing Rule)
      - Do NOT include `slug` (no such column; if any are auto-generated leave them out)

    STEP 2 — Five chamber INSERTs (one per chamber, WHERE NOT EXISTS keyed on name + government_id subquery). Use this exact 5-row list (per 077-RESEARCH.md "Verified Chambers to Create" and the planning_context critical_corrections):
      | name | name_formal |
      | `City Council` | `Portland City Council` |
      | `Mayor` | `Mayor of Portland` |
      | `City Auditor` | `Portland City Auditor` |
      | `City Administrator` | `Portland City Administrator` |
      | `City Attorney` | `Portland City Attorney` |
      - columns: `id, name, name_formal, government_id` ONLY
      - NEVER include `slug` (GENERATED ALWAYS — runtime error)
      - government_id is resolved via subquery: `(SELECT id FROM essentials.governments WHERE name='City of Portland, Oregon, US' AND state='OR')`

    STEP 3 — LOCAL_EXEC district for citywide offices (1 INSERT, WHERE NOT EXISTS on geo_id+state+district_type IN ('LOCAL','LOCAL_EXEC')):
      - columns: `geo_id, district_type, label, state`
      - values: `'4159000', 'LOCAL_EXEC', 'Portland (Citywide)', 'or'`
      - state IS lowercase 'or' on districts rows (per Phase 76 SUMMARY SC-3 verification + 077-PATTERNS.md State Casing Rule)
      - Column is `label` NOT `name` (essentials.districts has no `name` column)
      - NEVER use `ON CONFLICT (geo_id, district_type)` — that unique constraint does NOT exist

    STEP 4 — Ledger entry (last statement before COMMIT):
      - `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('230') ON CONFLICT (version) DO NOTHING;`

    Apply the migration to the live DB via Supabase Management API. Then run the four post-migration verification gates from 077-PATTERNS.md "Post-scaffold verification query" (Gates A, B, C, D).
  </action>
  <verify>
    <automated>
SQL via Supabase Management API after migration 230 is applied:

-- Gate A: government row exists with correct values
SELECT name, type, state, geo_id FROM essentials.governments
WHERE name='City of Portland, Oregon, US' AND state='OR';
-- Expected: 1 row | type='LOCAL' | state='OR' | geo_id='4159000'

-- Gate B: 5 chambers
SELECT COUNT(*) FROM essentials.chambers
WHERE government_id=(SELECT id FROM essentials.governments
                     WHERE name='City of Portland, Oregon, US' AND state='OR');
-- Expected: 5

SELECT name FROM essentials.chambers
WHERE government_id=(SELECT id FROM essentials.governments
                     WHERE name='City of Portland, Oregon, US' AND state='OR')
ORDER BY name;
-- Expected (5 rows): City Administrator, City Attorney, City Auditor, City Council, Mayor

-- Gate C: LOCAL_EXEC district exists
SELECT geo_id, district_type, label, state FROM essentials.districts
WHERE geo_id='4159000' AND state='or';
-- Expected: 1 row | district_type='LOCAL_EXEC' | label='Portland (Citywide)' | state='or'

-- Gate D: section-split detector (0 offices yet = clean)
SELECT ch.name_formal, COUNT(DISTINCT o.id) AS office_count
FROM essentials.offices o
JOIN essentials.chambers ch ON ch.id = o.chamber_id
WHERE ch.government_id = (SELECT id FROM essentials.governments
                          WHERE name='City of Portland, Oregon, US' AND state='OR')
GROUP BY ch.name_formal;
-- Expected: 0 rows (no offices created in this plan)

-- Gate E: ledger entry recorded
SELECT version FROM supabase_migrations.schema_migrations WHERE version='230';
-- Expected: 1 row

-- Gate F: idempotency — re-apply migration; INSERT rowCount must be 0 on every INSERT
-- Re-run STEP 1, 2, 3 INSERTs and confirm zero new rows inserted (WHERE NOT EXISTS guards)
    </automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/230_portland_government_structure.sql` exists and is wrapped in `BEGIN; ... COMMIT;`
    - File contains the literal string `City of Portland, Oregon, US` at least 7 times (1 INSERT row + 5 chamber subqueries + 1 LOCAL_EXEC reference is not needed but governments row insert + 5 chamber WHERE NOT EXISTS subqueries → at least 6 occurrences across INSERT VALUES and subqueries)
    - File contains the literal string `'4159000'` (the Portland geo_id) at least 2 times (government row + LOCAL_EXEC district)
    - File contains the literal `state='OR'` (uppercase) in the governments INSERT and all chamber subqueries
    - File contains the literal `'or'` (lowercase) for the LOCAL_EXEC districts INSERT state value
    - File does NOT contain the substring `slug` anywhere inside an INSERT column list (case-insensitive)
    - File does NOT contain the substring `ON CONFLICT (geo_id, district_type)` (no such unique constraint)
    - File does NOT contain the substring `Michael Jordan` (CF-3 correction — Lee III, but no Admin officials in this plan anyway)
    - Gate A: `SELECT COUNT(*) FROM essentials.governments WHERE name='City of Portland, Oregon, US' AND state='OR' AND geo_id='4159000'` returns 1
    - Gate B: `SELECT COUNT(*) FROM essentials.chambers WHERE government_id=(SELECT id FROM essentials.governments WHERE name='City of Portland, Oregon, US' AND state='OR')` returns 5
    - Gate B (names): chambers query returns exactly the 5 names (City Administrator, City Attorney, City Auditor, City Council, Mayor) in alphabetical order
    - Gate C: `SELECT COUNT(*) FROM essentials.districts WHERE geo_id='4159000' AND district_type='LOCAL_EXEC' AND state='or'` returns 1
    - Gate D: section-split detector returns 0 rows (no offices yet)
    - Gate E: `SELECT COUNT(*) FROM supabase_migrations.schema_migrations WHERE version='230'` returns 1
    - Gate F: Re-running every INSERT in migration 230 produces rowCount=0 (idempotent)
  </acceptance_criteria>
  <done>
    Migration 230 file exists in `C:/EV-Accounts/backend/migrations/`; the migration is applied to the live Supabase DB; all 6 verification gates (A–F) pass; ledger version '230' is recorded; re-applying the migration produces 0 new rows.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes, the following must all be true:
- 1 row in `essentials.governments` for City of Portland, Oregon, US (state='OR', geo_id='4159000')
- 5 rows in `essentials.chambers` under that government (Mayor, City Council, City Auditor, City Administrator, City Attorney)
- 1 row in `essentials.districts` with geo_id='4159000', district_type='LOCAL_EXEC', state='or'
- '230' present in `supabase_migrations.schema_migrations`
- Section-split detector returns 0 orphans
</verification>

<success_criteria>
Plan 77-02 can resolve the chamber_id and government_id subqueries used in its WITH ins_p CTE blocks; the LOCAL_EXEC district FK is available for citywide office INSERTs.
</success_criteria>

<output>
Create `.planning/phases/077-portland-officials/077-01-SUMMARY.md` documenting:
- Pre-flight verification results (all 5 queries)
- Migration 230 applied (Supabase API response excerpt)
- All 6 verification gate outputs (A–F)
- Idempotency re-run output
</output>
