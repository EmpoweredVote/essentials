---
phase: 194-city-of-tucson-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-tucson-ward-boundaries.ts
autonomous: false
requirements: [TUC-01]
must_haves:
  truths:
    - "6 City of Tucson ward LOCAL geofences (mtfcc='X0020', state='az') are loaded into essentials.geofence_boundaries, one per WARD 1-6"
    - "Wards 4 and 5 are stored as genuine MultiPolygons (Ward 4 = 2 constituent polygons, Ward 5 = 7) — NO ring silently dropped; Wards 1/2/3/6 are single-exterior"
    - "Each geofence geometry is valid WGS84 (SRID 4326) in the ~-111°/32° Tucson-area range, NOT the native AZ State Plane (2868) projection"
    - "The 6 geofences gate (pre-flight >=6) the structural migration in Plan 02 — without them Plan 02 must refuse to apply"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-tucson-ward-boundaries.ts"
      provides: "MapServer f=json rings→GeoJSON loader for the 6 wards using the FULL winding-classification helper (both branches), parameterized INSERT ON CONFLICT (geo_id, mtfcc) DO NOTHING"
      contains: "X0020"
  key_links:
    - from: "load-tucson-ward-boundaries.ts"
      to: "gisdata.pima.gov GISOpenData/Boundaries2/MapServer/3"
      via: "f=json + outSR=4326 query, arcgisRingsToGeoJson multi-ring-aware helper"
      pattern: "outSR=4326"
    - from: "load-tucson-ward-boundaries.ts"
      to: "essentials.geofence_boundaries"
      via: "parameterized INSERT with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))"
      pattern: "ON CONFLICT \\(geo_id, mtfcc\\) DO NOTHING"
---

<objective>
Author and run the one-time ETL loader that sources the 6 official City of Tucson ward boundaries
from the Pima-County-hosted authoritative ArcGIS `MapServer` (layer 3 of `GISOpenData/Boundaries2`)
and inserts them as custom LOCAL geofences (`mtfcc='X0020'`, `state='az'`, geo_id
`tucson-az-ward-1..6`). These 6 geofences are the gating dependency for true per-ward address routing
(D-01) — the structural migration in Plan 02 refuses to apply until they exist.

Purpose: Satisfies the geofence half of ROADMAP success criterion #1 (per-ward routing). RESEARCH
live-verified (2026-07-10) the endpoint is reachable, returns exactly 6 features with a `WARD` (1-6) +
`NAME` field, reprojects cleanly with `outSR=4326`, and — CRITICAL DELTA from Pima — 2 of the 6 wards
are genuinely multi-ring (Ward 4 = 2 rings, Ward 5 = 7 rings), all confirmed exterior (clockwise, no
holes) via live signed-area classification. So the loader MUST use the FULL multi-ring-aware
winding-classification helper, NOT the single-ring fast path and NOT the naive LV pass-through
anti-pattern (WR-01). D-01's PAUSE+flag fallback is a low-risk safety net for the fetch, but the
multi-ring encoding is the real, load-bearing engineering surface this plan gates.
Output: `C:/EV-Accounts/backend/scripts/load-tucson-ward-boundaries.ts`, committed to `C:/EV-Accounts`
and run by the orchestrator so 6 X0020 rows exist in production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-city-of-tucson-deep-seed/194-CONTEXT.md
@.planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md
@.planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md
@.planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / GIS-network access. Task 1 (author the loader) is the only
executor file action. Task 2 is a BLOCKING loader ring-structure verify checkpoint (Wave-0 gate #1).
Task 3 is ORCHESTRATOR-RUN: the inline execute-phase orchestrator runs `npx tsx` against
`C:/EV-Accounts/backend/.env` `DATABASE_URL` and asserts the result. All backend files live in the
SEPARATE `C:/EV-Accounts` repo (`master`→Render) — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (live-verified in 194-RESEARCH.md / 194-PATTERNS.md — no exploration needed):
- Endpoint: `https://gisdata.pima.gov/arcgis1/rest/services/GISOpenData/Boundaries2/MapServer/3/query`
  — a `MapServer` (NOT a `FeatureServer`), so `f=json` returns `geometry.rings` (ArcGIS JSON);
  `f=geojson` is NOT available. Native SRID = 2868 (AZ State Plane); `outSR=4326` is MANDATORY.
  Fields: `WARD` (integer 1-6), `NAME` (current councilmember). Ring counts (live-verified):
  Ward1=1, Ward2=1, Ward3=1, Ward4=2, Ward5=7, Ward6=1 — Ward4/5 are MultiPolygons.
- Analog loader (copy shape verbatim, FULL multi-ring branch):
  `C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts` — copy `ringSignedArea`
  (shoelace) + `arcgisRingsToGeoJson` INCLUDING the branch below `if (rings.length === 1)` (the
  CW=exterior/CCW=hole classification, polygon grouping, and `polygons.length === 1 ? Polygon :
  MultiPolygon` emission), the `AREA_EPS` degenerate-ring guard, the parameterized INSERT with
  `ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))` and `RETURNING ST_IsValid`, and the
  conditional `ST_MakeValid` re-run guard.
- ANTI-PATTERN — do NOT copy `load-lv-ward-boundaries.ts`'s `arcgisRingsToGeoJson`: it blindly assigns
  the raw ArcGIS `rings` array as GeoJSON `Polygon.coordinates` (WR-01), which silently mis-encodes
  every ring past the first as a "hole" regardless of winding. LV is a precedent for READING multi-ring
  data but the WRONG reference for CONVERTING it.
- Config constants (194-PATTERNS.md): MTFCC `'X0020'` (DB-verified unused, above Pima's X0019);
  STATE_CODE `'az'` (lowercase, required for LOCAL routing); SOURCE
  `'gisdata.pima.gov-boundaries2-mapserver3-2026'`; GEO_ID_PREFIX `'tucson-az-ward-'`;
  EXPECTED_COUNT 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Tucson ward-boundary geofence loader (FULL multi-ring path)</name>
  <files>C:/EV-Accounts/backend/scripts/load-tucson-ward-boundaries.ts</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md (lines 22-90 — the full loader pattern: config block, the VERBATIM ringSignedArea + arcgisRingsToGeoJson helper with BOTH branches, the parameterized INSERT + ST_MakeValid guard, the D-01 PAUSE+flag EXPECTED_COUNT guard, and the NEW geometry-TYPE assertion this phase needs; plus the explicit LV anti-pattern warning at lines 32-43)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Architecture Pattern 3 + § Common Pitfall 1 — Ward4=2/Ward5=7 rings, all exterior; outSR=4326 mandatory; lowercase state='az'; X0020)
    - C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts (PRIMARY analog — read in full: copy the ringSignedArea + arcgisRingsToGeoJson helper INCLUDING the multi-ring branch, the INSERT, and the ST_MakeValid guard VERBATIM)
    - C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts (read lines 90-100 ONLY — confirm its arcgisRingsToGeoJson is the naive pass-through anti-pattern you must NOT copy; do NOT reuse its conversion)
  </read_first>
  <action>
    Copy `load-pima-supervisor-boundaries.ts` (NOT the LV loader) and adapt. Set the query URL to the
    Tucson wards endpoint
    `https://gisdata.pima.gov/arcgis1/rest/services/GISOpenData/Boundaries2/MapServer/3/query` with
    `where=1=1`, `outFields=WARD,NAME`, `returnGeometry=true`, `f=json`, and `outSR=4326` (outSR is
    MANDATORY — without it coordinates come back in native SRID 2868 and store as garbage that never
    matches an address).

    Set constants: MTFCC='X0020', STATE_CODE='az' (lowercase), SOURCE=
    'gisdata.pima.gov-boundaries2-mapserver3-2026', GEO_ID_PREFIX='tucson-az-ward-', EXPECTED_COUNT=6.
    Build geo_id per feature as `${GEO_ID_PREFIX}${feature.attributes.WARD}` and name from
    `feature.attributes.NAME`.

    Reuse VERBATIM the `ringSignedArea` shoelace helper AND the FULL `arcgisRingsToGeoJson(rings)`
    helper — INCLUDING the branch below `if (rings.length === 1)` that classifies each ring by signed
    area (CW=exterior, CCW=hole), groups rings into polygons, applies the AREA_EPS degenerate-ring
    guard, and emits a `MultiPolygon` when more than one exterior ring is present. Do NOT trim the
    function to the fast path; do NOT substitute the LV naive `{type:'Polygon', coordinates: rings}`
    pass-through. Ward 4 (2 rings) and Ward 5 (7 rings) WILL execute the multi-ring branch — that is
    the point of this phase.

    Reuse VERBATIM the parameterized INSERT into essentials.geofence_boundaries (columns id, geo_id,
    mtfcc, state, name, geometry, source) with `public.ST_Multi(public.ST_SetSRID(
    public.ST_GeomFromGeoJSON($3), 4326))` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING RETURNING
    public.ST_GeometryType(geometry), public.ST_IsValid(geometry)`. ALL GIS attribute/geometry values
    MUST be passed as bind params ($1 geo_id / $2 name / $3 geomJson / $4 source) — never
    string-concatenated into SQL (V5 input validation / T-194-SQLI). Keep the conditional
    `ST_MakeValid` re-run guard — it is MORE likely to trigger here than in Pima given the multi-ring
    geometry, and must hard-fail (exit non-zero) if a geometry is still invalid after repair.

    Assert EXPECTED_COUNT: if the fetch returns fewer than 6 features, print the shortfall and exit
    non-zero (D-01 PAUSE+flag — never silently load a partial ward set). After insert, add the NEW
    geometry-TYPE self-report (Pima's loader lacks this): print, per ward, WARD / NAME / geo_id /
    ST_IsValid / ST_NumGeometries / first-coordinate longitude, so the ring-verify checkpoint (Task 2)
    and the orchestrator run (Task 3) can confirm Ward 4 has 2 constituent polygons and Ward 5 has 7
    while Wards 1/2/3/6 have 1.

    Do NOT touch essentials.districts or any government/office table — this loader ONLY populates
    geofence_boundaries. The pre-existing whole-city G4110 row (geo_id='0477000') is untouched.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/load-tucson-ward-boundaries.ts"; test -f "$F" && grep -qF "outSR=4326" "$F" && grep -qF "X0020" "$F" && grep -qF "ON CONFLICT (geo_id, mtfcc) DO NOTHING" "$F" && grep -qF "ringSignedArea" "$F" && grep -qF "MultiPolygon" "$F" && grep -qF "ST_MakeValid" "$F" && ! grep -qE "f=geojson|X0019|Boundaries/MapServer/5" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/load-tucson-ward-boundaries.ts` exists.
    - Grep confirms `outSR=4326` and `f=json` in the query URL; NO `f=geojson`.
    - Grep confirms `X0020` present; NEITHER `X0019` NOR the Pima `Boundaries/MapServer/5` path appears (this is Boundaries2/MapServer/3).
    - Grep confirms `ringSignedArea` helper AND a `MultiPolygon` emission branch are present (the full multi-ring path, not the fast path only, not the LV pass-through).
    - Grep confirms lowercase `'az'` STATE_CODE.
    - Grep confirms the parameterized INSERT with `ST_GeomFromGeoJSON($3)` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING`; conditional `ST_MakeValid` guard present.
    - EXPECTED_COUNT=6 with a non-zero exit on shortfall (D-01 PAUSE+flag).
    - Per-ward summary print includes ST_NumGeometries so multi-ring wards are observable.
  </acceptance_criteria>
  <done>Loader authored: fetches 6 wards via f=json+outSR=4326, converts rings→GeoJSON with the FULL winding-classification helper (Ward4/5 → MultiPolygon), inserts 6 X0020/lowercase-az geofences idempotently with parameterized binds, pauses on shortfall.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING loader ring-structure verify (Wave-0 gate #1)</name>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-CONTEXT.md (Claude's Discretion — BLOCKING loader-verify checkpoint before loading Tucson wards; WR-01 multi-ring caveat)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfall 1 — Ward4=2/Ward5=7 rings, all exterior; the single-ring fast path is INCORRECT here)
    - .planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md (lines 26-43 — copy Pima's full helper, do NOT copy the LV naive pass-through)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor file action. The operator confirms the authored loader (Task 1) uses the FULL multi-ring-aware winding-classification helper (NOT the single-ring fast path only, NOT the LV naive rings pass-through) BEFORE the orchestrator runs it in Task 3. This is the first of the two locked pre-seed gates. See &lt;how-to-verify&gt;.</action>
  <what-built>
    The loader (Task 1) is authored but NOT run. Because Ward 4 (2 rings) and Ward 5 (7 rings) are
    genuinely multi-ring, a loader that copied Pima's single-ring fast path only — or the LV naive
    `{type:'Polygon', coordinates: rings}` pass-through — would silently drop 1 of Ward 4's rings and 6
    of Ward 5's, under-sizing those wards and mis-routing addresses in the dropped parcels. This gate
    confirms the correct multi-ring encoding is in place BEFORE writing to production (mcp__supabase-local
    IS live prod).
  </what-built>
  <how-to-verify>
    1. Read `C:/EV-Accounts/backend/scripts/load-tucson-ward-boundaries.ts` and confirm
       `arcgisRingsToGeoJson` contains a branch (below `if (rings.length === 1)`) that classifies rings
       by signed area, groups them, and emits `MultiPolygon` when more than one exterior ring exists —
       matching `load-pima-supervisor-boundaries.ts`, NOT the LV one-line pass-through.
    2. Confirm the AREA_EPS degenerate-ring guard and the `ST_MakeValid` re-run guard are present.
    3. Confirm the constants target Boundaries2/MapServer/3, X0020, lowercase 'az', EXPECTED_COUNT=6.
    4. On approval, the orchestrator proceeds to Task 3 (run the loader). If the multi-ring branch is
       missing or the LV pattern was copied, STOP and return to Task 1 — do NOT run the loader.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the full winding-classification helper (both branches + AREA_EPS guard) is present, matching the Pima analog.
    - Operator confirms the LV naive pass-through was NOT used.
    - Operator confirms Boundaries2/MapServer/3 + X0020 + lowercase 'az' + EXPECTED_COUNT=6.
    - Gate resolved before any write to production.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the loader is confirmed to use the full multi-ring winding-classification path (not the fast path, not the LV pass-through), or describe the fix needed in Task 1.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the loader + assert 6 X0020 geofences incl. multi-ring shape (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator runs the loader + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md (Per-Task Verification Map — ward geofences row: ST_IsValid + ST_NumGeometries per ward)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfall 1 — Ward4=2/Ward5=7 constituent polygons; the ~-111°/32° WGS84 sanity range vs native 2868)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/network access). First `git -C "C:/EV-Accounts" add` + commit
    the loader. Then run `npx tsx backend/scripts/load-tucson-ward-boundaries.ts` from the
    `C:/EV-Accounts/backend` checkout (uses `.env` `DATABASE_URL`). Confirm the script exits 0 with 6
    rows inserted (or already-present via ON CONFLICT). Then assert via `psql "$DATABASE_URL" -tAc`:
    (a) exactly 6 rows in essentials.geofence_boundaries WHERE mtfcc='X0020' AND state='az'; (b) all 6
    geo_ids match `tucson-az-ward-1..6`; (c) all 6 geometries are valid (ST_IsValid=true); (d) every
    centroid longitude between -112 and -110 and latitude between 31 and 33 (WGS84 sanity, NOT native
    2868); (e) MULTI-RING CHECK — `tucson-az-ward-4` has ST_NumGeometries=2, `tucson-az-ward-5` has
    ST_NumGeometries=7, and `tucson-az-ward-1/2/3/6` each have ST_NumGeometries=1 (no dropped rings).
    Also run the section-split scan to confirm the 6 wards nest cleanly inside the existing 0477000
    city boundary without creating a split section. If any assertion fails, PAUSE and flag — do NOT
    proceed to Plan 02. Record the 6 geo_id → ST_IsValid → ST_NumGeometries → centroid results in
    194-01-SUMMARY.md.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0020' AND state='az')=6 AND NOT EXISTS (SELECT 1 FROM essentials.geofence_boundaries WHERE mtfcc='X0020' AND NOT public.ST_IsValid(geometry)) AND (SELECT bool_and(public.ST_X(public.ST_Centroid(geometry)) BETWEEN -112 AND -110 AND public.ST_Y(public.ST_Centroid(geometry)) BETWEEN 31 AND 33) FROM essentials.geofence_boundaries WHERE mtfcc='X0020') AND (SELECT public.ST_NumGeometries(geometry) FROM essentials.geofence_boundaries WHERE geo_id='tucson-az-ward-4')=2 AND (SELECT public.ST_NumGeometries(geometry) FROM essentials.geofence_boundaries WHERE geo_id='tucson-az-ward-5')=7 AND (SELECT bool_and(public.ST_NumGeometries(geometry)=1) FROM essentials.geofence_boundaries WHERE geo_id IN ('tucson-az-ward-1','tucson-az-ward-2','tucson-az-ward-3','tucson-az-ward-6'))"</automated>
  </verify>
  <acceptance_criteria>
    - Loader exits 0; 6 X0020 rows present after the run.
    - `psql -tAc` count of X0020/state='az' geofences = 6; geo_ids are exactly `tucson-az-ward-1` through `-6`.
    - All 6 geometries valid; every centroid lon∈(-112,-110) and lat∈(31,33) (WGS84, confirming outSR=4326 worked and no 2868 garbage).
    - Multi-ring: Ward 4 ST_NumGeometries=2, Ward 5 ST_NumGeometries=7, Wards 1/2/3/6 = 1 each — NO ring dropped (Pitfall 1).
    - Section-split scan clean (6 wards nest inside 0477000, no split section).
    - The combined boolean SELECT returns `t`.
    - Loader committed to `C:/EV-Accounts` via `git -C`.
  </acceptance_criteria>
  <done>6 valid WGS84 ward geofences (X0020, lowercase az) are live in production — Ward 4/5 faithful MultiPolygons — gating Plan 02's structural migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tucson wards MapServer (remote) → loader | Untrusted ArcGIS JSON geometry + attribute strings enter the loader over HTTP |
| loader → production PostGIS | Converted geometry crosses into live `essentials.geofence_boundaries` via `npx tsx` (service-role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-SQLI | Tampering | GIS attribute/geometry → INSERT | mitigate | Parameterized binds ($1 geo_id / $2 name / $3 GeoJSON / $4 source) — never string-concatenate GIS response data into SQL (V5; matches Pima loader) |
| T-194-RING | Tampering | multi-ring geometry silently mis-encoded (dropped rings / wrong exterior-hole classification) | mitigate | FULL winding-order classification (signed-area shoelace, CW=exterior/CCW=hole) + AREA_EPS degenerate-ring guard; BLOCKING ring-verify checkpoint (Task 2); Task 3 asserts Ward4=2 / Ward5=7 constituent polygons — this phase actually exercises the defense |
| T-194-PROJ | Tampering | native SRID 2868 vs WGS84 | mitigate | `outSR=4326` mandatory in the query; Task 3 asserts centroid ∈ Tucson-area WGS84 range or PAUSES |
| T-194-GEOM | Tampering | invalid/self-intersecting polygon | mitigate | `RETURNING ST_IsValid` + conditional `ST_MakeValid` re-run guard, hard-fail if still invalid |
| T-194-MTFCC | Tampering | wrong X-code silently un-routable | mitigate | Use X0020 (DB-verified unused, above X0019, outside the excluded X0001-X0004 range); grep gate forbids X0019 in the loader |
| T-194-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `C:/EV-Accounts/backend/.env`; never hardcoded in the loader |
| T-194-SC | Tampering | package installs | accept | No new npm/pip installs — reuses installed `pg`/`tsx`/`dotenv`; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 6 X0020/state='az' geofences loaded, geo_id `tucson-az-ward-1..6`, all ST_IsValid.
- Ward 4 = 2-polygon MultiPolygon, Ward 5 = 7-polygon MultiPolygon, others single — no ring dropped.
- All valid WGS84 geometry (centroid in the -112/-110 lon, 31/33 lat Tucson-area box); section-split clean.
- Loader committed to `C:/EV-Accounts`; results recorded in 194-01-SUMMARY.md.
</verification>

<success_criteria>
The geofence half of ROADMAP #1 is TRUE in production: 6 clean per-ward LOCAL geofences exist (Wards
4/5 faithful MultiPolygons), enabling true one-ward-member-per-address routing and gating Plan 02.
</success_criteria>

<output>
Create `.planning/phases/194-city-of-tucson-deep-seed/194-01-SUMMARY.md` when done — include the 6
geo_id → ST_IsValid → ST_NumGeometries → centroid manifest.
</output>
</content>
