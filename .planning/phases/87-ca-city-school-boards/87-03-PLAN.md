---
phase: 87-ca-city-school-boards
plan: 03
type: execute
wave: 3
depends_on: ["87-02"]
files_modified:
  - src/pages/Results.jsx
autonomous: true
requirements: [CA-SCHOOL-03, CA-SCHOOL-04]
gap_closure: true
gaps_addressed:
  - "School board officials show their office title (e.g. 'Commissioner', 'Board Member (District A)')"
  - "School board official tiles display at normal size matching other rep sections"

must_haves:
  truths:
    - "SFUSD cards show 'Commissioner' as the title, not 'San Francisco Unified School District Board of Education'"
    - "SDUSD cards show 'Board Member (District A)' (etc.) as the title"
    - "SCUSD, FUSD, BUSD cards show the correct office title from office_title field"
    - "School board tiles have a subtitle line (e.g. 'Area 1', 'District A') making them the same visual size as council tiles"
    - "SJUSD titles also correct; SFUSD Commissioner cards show no subtitle (no parenthetical in title)"
  artifacts:
    - path: "src/pages/Results.jsx"
      provides: "Fixed SCHOOL cardTitle branch + extended deriveSeatSubtitle"
      contains: "district_type === 'SCHOOL'"
  key_links:
    - from: "renderPoliticianCard (Results.jsx ~line 1165)"
      to: "cleanTitle (pol.office_title)"
      via: "SCHOOL branch returning qualify(cleanTitle, pol)"
    - from: "deriveSeatSubtitle (Results.jsx ~line 252)"
      to: "parenthetical in cleanTitle"
      via: "SCHOOL type regex match on \\((.+?)\\)"
---

<objective>
Fix two visual bugs in Results.jsx that affect all six CA school board sections.

Gap 1 — Card title wrong: The SCHOOL branch of cardTitle (lines ~1164-1169) constructs the title from `government_name + chamber_name` instead of using `cleanTitle` (which holds `pol.office_title`). Every school board card shows the full district name instead of the elected title.

Gap 2 — Tiles visually smaller: `deriveSeatSubtitle()` (lines ~250-260) returns null immediately for `district_type !== 'LOCAL' && !== 'COUNTY'`, so school board cards get no subtitle. Council cards get "District N" / "Ward N" / "Area N" subtitles, making them taller. Both hit the same minHeight but council cards have more content.

Purpose: School board officials are shown on Reps tab but with meaningless titles and undersized tiles, making them appear broken. This fix makes school board cards consistent with council cards in both label and visual size.

Output: Modified src/pages/Results.jsx with two targeted surgical changes.
</objective>

<execution_context>
@C:\Users\Chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-ca-city-school-boards/87-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SCHOOL cardTitle to use office_title (cleanTitle)</name>
  <files>src/pages/Results.jsx</files>
  <action>
In `renderPoliticianCard`, locate the SCHOOL branch of the `cardTitle` IIFE (around line 1164). The current code reads:

    if (pol.district_type === 'SCHOOL' && pol.government_name) {
      const schoolName = pol.government_name.split(',')[0];
      const raw = cleanChamber ? `${schoolName} ${cleanChamber}` : schoolName;
      return pol.government_body_name ? simplifyForBody(raw, pol) : raw;
    }

Replace this entire branch with:

    if (pol.district_type === 'SCHOOL')
      return qualify(cleanTitle, pol);

Rationale: `cleanTitle` already holds `pol.office_title` (e.g. "Commissioner", "Board Member (Area 1)"). The old branch ignored it entirely and concatenated government_name + chamber_name, producing "San Francisco Unified School District Board of Education". The `qualify()` helper adds jurisdiction context when appropriate and is already used for LOCAL/COUNTY/EXEC types. Do NOT retain the `pol.government_name` guard — SCHOOL cards should use cleanTitle even when government_name is missing.
  </action>
  <verify>
    <automated>grep -n "district_type === 'SCHOOL'" src/pages/Results.jsx</automated>
  </verify>
  <done>The SCHOOL branch in cardTitle returns `qualify(cleanTitle, pol)`. The old `schoolName + cleanChamber` concatenation is gone. grep shows only the new one-liner form.</done>
</task>

<task type="auto">
  <name>Task 2: Extend deriveSeatSubtitle to handle SCHOOL district_type</name>
  <files>src/pages/Results.jsx</files>
  <action>
Locate `deriveSeatSubtitle()` (around line 250). The current guard is:

    if (!dt.startsWith('LOCAL') && dt !== 'COUNTY') return null;

Change it to also pass SCHOOL through:

    if (!dt.startsWith('LOCAL') && dt !== 'COUNTY' && dt !== 'SCHOOL') return null;

Then, after the existing `Ward` and `District` matchers but before the final `return null`, add a SCHOOL-specific parenthetical extractor:

    if (dt === 'SCHOOL') {
      const pm = t.match(/\((.+?)\)/);
      return pm ? pm[1].trim() : null;
    }

Behavior by example:
- "Board Member (Area 1)" → "Area 1"
- "Board Member (District A)" → "District A"
- "Board Member (Trustee Area 3)" → "Trustee Area 3"
- "Director" → null (no parenthetical, no subtitle; Berkeley BUSD)
- "Commissioner" → null (no parenthetical; SFUSD)

Place the SCHOOL block just before the closing `return null` of `deriveSeatSubtitle`, after the existing Ward/District matchers. The existing LOCAL/COUNTY Ward and District matchers remain untouched — they are only reached when dt starts with 'LOCAL' or equals 'COUNTY'.

IMPORTANT: the function comment at the top of `deriveSeatSubtitle` mentions it mirrors `deriveSeatSubtitle()` in ev-ui/src/PoliticianProfile.jsx. Note in a code comment that the ev-ui mirror does NOT need updating (ev-ui Profile page has a separate rendering path that is unaffected by this SCHOOL type addition).
  </action>
  <verify>
    <automated>grep -n "SCHOOL" src/pages/Results.jsx</automated>
  </verify>
  <done>deriveSeatSubtitle passes when dt === 'SCHOOL', and the parenthetical extractor returns pm[1].trim() for titles with parentheticals. Titles without parentheticals return null (Commissioner, Director). grep confirms SCHOOL appears in both the cardTitle branch and deriveSeatSubtitle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → Results.jsx render | pol.district_type and pol.office_title come from backend; tampered values would affect card display only (no auth/data mutation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03-01 | Tampering | cardTitle SCHOOL branch | accept | Pure display field; no auth surface; sanitized by React's JSX escaping |
| T-87-03-02 | Information Disclosure | deriveSeatSubtitle regex on office_title | accept | office_title is public data; regex match on display string only |
</threat_model>

<verification>
Manual verification after deploying or running Vite dev server:

1. Open an SF address (e.g. "1 Dr Carlton B Goodlett Pl, San Francisco, CA") → Reps tab → SCHOOL section must show "Commissioner" on each of the 7 SFUSD cards, not the full district name.
2. Open a San Diego address → SCHOOL section must show "Board Member (District A)" through "Board Member (District E)" as card titles.
3. Open a Sacramento address → SCHOOL section must show "Board Member (Area 1)" through "Board Member (Area 7)".
4. Check tile size: SDUSD / SCUSD / FUSD cards must show the parenthetical as a subtitle line (e.g. "District A", "Area 1") making them visually the same height as nearby council cards.
5. SFUSD cards (title="Commissioner") must have NO subtitle — no parenthetical in the title, so deriveSeatSubtitle returns null, which is correct.
6. BUSD cards (title="Director") must also have NO subtitle.

Automated smoke check:
`grep -n "qualify(cleanTitle, pol)" src/pages/Results.jsx` must return the SCHOOL branch line.
`grep -n "pm\[1\].trim()" src/pages/Results.jsx` must return the parenthetical extractor line.
</verification>

<success_criteria>
- School board card titles match pol.office_title exactly (per the district's convention)
- School board tiles with parenthetical seat designations show a subtitle, matching council tile visual height
- School board tiles without parentheticals (Commissioner, Director) show no subtitle — this is correct and expected
- No regression to LOCAL / COUNTY / JUDICIAL cardTitle behavior (those branches are untouched)
</success_criteria>

<output>
Create `.planning/phases/87-ca-city-school-boards/87-03-SUMMARY.md` when done.
</output>
