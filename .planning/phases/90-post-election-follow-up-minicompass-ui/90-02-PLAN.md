---
phase: 90-post-election-follow-up-minicompass-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 90-01
files_modified:
  - C:/Transparent Motivations/essentials/src/components/MiniCompass.jsx
  - C:/Transparent Motivations/essentials/package.json
  - C:/Transparent Motivations/essentials/package-lock.json
autonomous: false
requirements:
  - UI-01
  - UI-02
user_setup: []

must_haves:
  truths:
    - "essentials/package.json depends on a published @empoweredvote/ev-ui version that includes the dotRadius prop (output of Plan 90-01)"
    - "MiniCompass.jsx passes showLabels={false} to RadarChartCore (UI-02 — labels and their reserved whitespace are both gone)"
    - "MiniCompass.jsx passes dotRadius={2.5} (or 3.0 within planner discretion) to RadarChartCore (UI-01 — dots visually ~50% smaller)"
    - "MiniCompass.jsx no longer passes labelFontSize={0}, maxLabelLines, padding, or labelOffset (replaced by showLabels prop)"
    - "Existing portal tooltip on dot hover continues to function with smaller dots"
    - "Both candidate-tile (ElectionsView) and representative-tile (Results) MiniCompass embeds render without overflow, clipping, or visible labels"
  artifacts:
    - path: "src/components/MiniCompass.jsx"
      provides: "MiniCompass component using showLabels + dotRadius props on RadarChartCore"
      contains: "showLabels={false}"
    - path: "package.json"
      provides: "ev-ui dependency at the version published by Plan 90-01"
      contains: "@empoweredvote/ev-ui"
  key_links:
    - from: "src/components/MiniCompass.jsx"
      to: "RadarChartCore from @empoweredvote/ev-ui"
      via: "showLabels + dotRadius props"
      pattern: "showLabels=\\{false\\}|dotRadius=\\{"
    - from: "src/components/ElectionsView.jsx (call site at size={190})"
      to: "MiniCompass component"
      via: "candidate tile tooltip overlay"
      pattern: "<MiniCompass"
    - from: "src/pages/Results.jsx (call site at size={190})"
      to: "MiniCompass component"
      via: "representative tile tooltip overlay"
      pattern: "<MiniCompass"
---

<objective>
Consume the patched `@empoweredvote/ev-ui` published by Plan 90-01 from `MiniCompass.jsx`, replacing the `labelFontSize={0}` + `padding={10}` workaround with the cleaner `showLabels={false}` prop, and adding `dotRadius={2.5}` (Claude discretion allows 3.0) to shrink dots ~50%. Then human-verify the visual result in both candidate tiles (ElectionsView) and representative tiles (Results).

Purpose: This is the final UI integration step. Plan 90-01 published the library prop; this plan wires it in and confirms UI-01 and UI-02 are visually satisfied across BOTH consumer call sites.

Output:
- `package.json` and `package-lock.json` reflect the new published ev-ui version
- `MiniCompass.jsx` uses `showLabels={false}` and `dotRadius={2.5}` (or 3.0) and drops the obsolete label/padding props
- Human verification confirms dots are noticeably smaller, no labels or reserved whitespace, tooltips still work on hover, and the chart fits comfortably inside the existing 190px tile overlays
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-CONTEXT.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-RESEARCH.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-PATTERNS.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-01-SUMMARY.md
@src/components/MiniCompass.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install new ev-ui version + wire showLabels + dotRadius props</name>
  <files>C:/Transparent Motivations/essentials/package.json, C:/Transparent Motivations/essentials/package-lock.json, C:/Transparent Motivations/essentials/src/components/MiniCompass.jsx</files>
  <read_first>
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-01-SUMMARY.md (the version Plan 90-01 published — Task 1 must install this exact version)
    - C:/Transparent Motivations/essentials/src/components/MiniCompass.jsx (current RadarChartCore call site — full prop list to be modified, including labelFontSize={0}, maxLabelLines={3}, padding={10}, labelOffset={8})
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-PATTERNS.md (MiniCompass section — current vs target RadarChartCore call shown side-by-side; KEEP rules for tightFit/ringColor/darkMode)
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-RESEARCH.md (Pitfall 3 — 0.9.2 dots are LARGER than 0.8.x; dotRadius=2.5 is target relative to old r=5 baseline; 3.0 fallback per D-09 discretion)
    - C:/Transparent Motivations/essentials/package.json (current dep range "@empoweredvote/ev-ui": "^0.9.1" — verify range still satisfies the newly published version)
  </read_first>
  <action>
    Install the published ev-ui patch from Plan 90-01 and update `MiniCompass.jsx` to use the new props.

    1. Read the actual published version from `90-01-SUMMARY.md` (e.g., `0.9.3`). Confirm `package.json` dependency range `"@empoweredvote/ev-ui": "^0.9.1"` still satisfies it (it will — 0.9.3 is in ^0.9.1 range). No range bump needed.
    2. Run `npm install` from `C:/Transparent Motivations/essentials` so that package-lock.json resolves to the new version. Verify with `node -p "require('@empoweredvote/ev-ui/package.json').version"` from inside the essentials directory.
    3. Edit `src/components/MiniCompass.jsx` — locate the `<RadarChartCore …>` block (PATTERNS.md shows it spans current lines 152-169). Modify it as follows:
       - REMOVE: `labelFontSize={0}` — obsolete; superseded by showLabels.
       - REMOVE: `maxLabelLines={3}` — no longer relevant when labels are hidden.
       - REMOVE: `padding={10}` — showLabels={false} sets minPadding=10 internally.
       - REMOVE: `labelOffset={8}` — no longer relevant when labels are hidden.
       - ADD: `showLabels={false}` — UI-02 fix; zeros label padding and skips label render.
       - ADD: `dotRadius={2.5}` — UI-01 fix; shrinks user and compare dots ~50% relative to the old 0.8.x r=5 baseline. If the visual result at 2.5 looks too faint at size=190 (Task 2 human-verify will check this), bump to 3.0 per CONTEXT.md D-09 Claude discretion — but start at 2.5.
       - KEEP UNCHANGED: `topics`, `data`, `compareData`, `invertedSpokes`, `replacedSpokes`, `boldOriginalSpokes`, `onToggleInversion`, `onReplaceTopic`, `size={INNER_SVG_SIZE}`, `tightFit={true}`, `ringColor="transparent"`, `darkMode={!!isDark}`.
       - DO NOT touch the `RADAR_RADIUS` constant (line 9), the `dotPositions` math (lines 70-97), the `handleMouseMove` callback (lines 109-130), or the portal tooltip JSX (lines 176-199). The dot-position math mirrors RadarChartCore's `radius = size/2 - 40` formula and should continue to land correctly because the chart's `radius` calculation is unchanged in 0.9.x.
    4. Do NOT modify `paddingRight: 14` nudges in ElectionsView.jsx or Results.jsx in this task. The nudge removal is deferred to Task 2's visual check — if the chart looks visually centered without the nudge change, leave it; if it now looks off-center because labels no longer exist, the human verifier will surface that in Task 2.
    5. Save the file. Run `npm run build` (or `npm run lint` if no build is in scope) to confirm no syntax errors. If a Vite dev server is already running, hot reload should pick up the change.
  </action>
  <verify>
    <automated>node -p "require('C:/Transparent Motivations/essentials/node_modules/@empoweredvote/ev-ui/package.json').version" — must equal the version recorded in 90-01-SUMMARY.md; grep -c "showLabels={false}" src/components/MiniCompass.jsx — must return 1; grep -c "dotRadius=" src/components/MiniCompass.jsx — must return 1; grep -c "labelFontSize" src/components/MiniCompass.jsx — must return 0; grep -c "padding={10}" src/components/MiniCompass.jsx — must return 0; (cd "C:/Transparent Motivations/essentials" && npm run build) — exit 0 OR `npm run lint` exit 0 if no build step</automated>
  </verify>
  <acceptance_criteria>
    - node_modules/@empoweredvote/ev-ui/package.json version matches the version published in Plan 90-01 (per 90-01-SUMMARY.md)
    - src/components/MiniCompass.jsx contains the literal string `showLabels={false}` exactly once
    - src/components/MiniCompass.jsx contains exactly one `dotRadius={` occurrence with value `2.5` (or `3.0`)
    - src/components/MiniCompass.jsx contains zero occurrences of `labelFontSize`, `maxLabelLines`, `padding={10}`, or `labelOffset={8}`
    - src/components/MiniCompass.jsx still contains `tightFit={true}`, `ringColor="transparent"`, `darkMode={!!isDark}`, `size={INNER_SVG_SIZE}` (existing KEEP props)
    - RADAR_RADIUS constant on line 9 unchanged; dotPositions / handleMouseMove / portal tooltip JSX unchanged
    - Build (`npm run build`) or lint (`npm run lint`) exits 0
  </acceptance_criteria>
  <done>ev-ui at new version is installed; MiniCompass.jsx uses showLabels={false} and dotRadius={2.5} (or 3.0); obsolete label props removed; build is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify MiniCompass UI-01 + UI-02 in both consumer call sites</name>
  <files>C:/Transparent Motivations/essentials/src/components/MiniCompass.jsx, C:/Transparent Motivations/essentials/src/components/ElectionsView.jsx, C:/Transparent Motivations/essentials/src/pages/Results.jsx</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/components/MiniCompass.jsx (current state after Task 1 — must show showLabels={false} + dotRadius={2.5})
    - C:/Transparent Motivations/essentials/src/components/ElectionsView.jsx around line 735 (the paddingRight: 14 nudge call site #1)
    - C:/Transparent Motivations/essentials/src/pages/Results.jsx around line 1327 (the paddingRight: 14 nudge call site #2)
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-RESEARCH.md (Standard Stack: "Deprecated/outdated" notes that paddingRight:14 may be unnecessary after showLabels={false}; Open Question 3)
  </read_first>
  <action>
    This is a checkpoint:human-verify task. Claude does NOT make autonomous changes here — Claude orchestrates the verification flow, applies the optional polish (dotRadius bump or paddingRight nudge removal) based on the human verifier's signals, and re-verifies until the human signals approval.

    Procedure for the executor agent:

    1. Start the Vite dev server: `npm run dev` from `C:/Transparent Motivations/essentials`. Note the dev URL — typically `http://localhost:5173`.

    2. Present the verification checklist (the `<how-to-verify>` block below) to the user in the chat.

    3. Wait for the user's resume signal (see `<resume-signal>` block below).

    4. Apply targeted fixes if the user reports issues:
       - "dots still too big" → bump MiniCompass.jsx to `dotRadius={3.0}` (per CONTEXT.md D-09 discretion).
       - "off-center / nudge needs removal" → remove the `paddingRight: 14, // nudge compass left so the widest axis label clears the card edge` line from the MiniCompass wrapper style in ElectionsView.jsx (~line 735) AND/OR Results.jsx (~line 1327), based on which call site the user reports as off-center.
       - "tooltip broken" → investigate dotPositions math vs new dot rendering; reconcile (likely the 0.9.x source has a different `radius = size/2 - N` constant — update `RADAR_RADIUS` in MiniCompass.jsx line 9 to match the published 0.9.x value, verified by reading node_modules/@empoweredvote/ev-ui/dist/index.mjs).
       - "dark mode tooltips invisible" → inspect tooltip style in MiniCompass.jsx (lines 176-199) for hardcoded colors that conflict with the new dark mode rendering; fix and re-verify.

    5. Re-present the checklist after each fix. Repeat until user signals approval.

    6. Document the final dotRadius value, whether either paddingRight nudge was removed, and any other adjustments in 90-02-SUMMARY.md.
  </action>
  <how-to-verify>
    1. From `C:/Transparent Motivations/essentials`: `npm run dev` (Vite). Note the dev URL — typically `http://localhost:5173`.

    2. **ElectionsView call site (size={190}):**
       - Navigate to `/elections` while signed in as a connected user (or use the address form to load a ballot).
       - Hover over a candidate tile that shows a compass icon.
       - Confirm:
         - The MiniCompass radar chart appears as a tooltip overlay over the tile.
         - NO spoke labels are visible (no text around the perimeter).
         - NO chart title text appears above the chart.
         - NO obvious reserved whitespace gutter on the left/right/top/bottom of the radar — the radar fills the tile.
         - Dots at each stance position are visibly SMALLER than before (the dots should look subtle, not chunky).
         - Hover over individual dots — the portal tooltip showing stance text still appears.
         - The chart does NOT clip or overflow the tile.

    3. **Results call site (size={190}):**
       - Navigate to `/results` (or whatever route renders Results.jsx with representative tiles + compass).
       - Repeat the same checks: hover representative tile → MiniCompass overlay → confirm no labels, smaller dots, working tooltips, no clipping.

    4. **Dot size judgment:**
       - If dots look too faint at `2.5`, request `dotRadius={3.0}` (per CONTEXT.md D-09 discretion).
       - If dots look good at `2.5`, no further change needed.

    5. **paddingRight: 14 nudge (optional polish):**
       - Inspect ElectionsView.jsx (~line 735) and Results.jsx (~line 1327) — both have `paddingRight: 14, // nudge compass left so the widest axis label clears the card edge`.
       - With `showLabels={false}` no labels exist, so the nudge may no longer be needed.
       - Visually compare: does the chart look centered in the tile WITH the nudge, or visually off-center?
       - If off-center: signal to remove the `paddingRight: 14` line from BOTH ElectionsView.jsx AND Results.jsx (matching the comment that explicitly cites label clearance) and re-verify.
       - If still centered with the nudge: leave the nudge. Document the choice in the SUMMARY.

    6. **Dark mode spot-check:**
       - Toggle dark mode (if available in the UI). Confirm the dots still render correctly (color, contrast) and labels remain hidden.

    7. **Mobile / narrow viewport spot-check (optional):**
       - Resize the browser to ~375px wide. Confirm the MiniCompass tile still renders without clipping the chart.
  </how-to-verify>
  <verify>
    <human-check>User has signaled `approved` after running the steps in the &lt;how-to-verify&gt; block above on both /elections and /results call sites in light and dark modes. The MiniCompass shows no labels, no reserved whitespace, dots visibly smaller, and tooltips functional on dot hover.</human-check>
  </verify>
  <resume-signal>
    Reply `approved` if all checks pass.
    Reply with specific issues if not — e.g., `dots still too big — try 3.0`, `tooltip broken on small dots`, `nudge needs to come off in ElectionsView only`, `dark mode tooltips invisible`, etc.
    On issues, the executor will make the targeted fix and re-run this checkpoint.
  </resume-signal>
  <acceptance_criteria>
    - User has explicitly replied `approved` (or equivalent affirmative) after walking through the &lt;how-to-verify&gt; checklist
    - 90-02-SUMMARY.md records: final dotRadius value (2.5 or 3.0), whether ElectionsView paddingRight:14 was removed, whether Results paddingRight:14 was removed, dark-mode outcome, any other fixes applied during verification cycles
    - No outstanding human-reported regressions
  </acceptance_criteria>
  <done>Human verifier has approved both UI-01 (smaller dots) and UI-02 (no labels, no reserved whitespace) across both call sites; final dotRadius value and paddingRight nudge decisions are recorded in 90-02-SUMMARY.md.</done>
</task>

</tasks>

<verification>
- @empoweredvote/ev-ui in node_modules matches version from 90-01-SUMMARY.md
- MiniCompass.jsx contains showLabels={false} and dotRadius={N}; contains zero references to labelFontSize, maxLabelLines, padding={10}, labelOffset={8}
- Build/lint exits 0
- Human verifier confirms UI-01 (dots ~50% smaller) and UI-02 (no labels, no reserved whitespace) on BOTH /elections and /results
- Portal tooltips on dot hover still function with the smaller dot size
- No overflow or clipping in tile overlays
- paddingRight: 14 nudge removed from both ElectionsView.jsx and Results.jsx IF visual check shows it is no longer needed (documented either way in SUMMARY)
</verification>

<success_criteria>
- UI-01 satisfied: dots visually ~50% smaller than before, verified by human eye in two call sites
- UI-02 satisfied: no labels, no chart title, no reserved whitespace, verified by human eye in two call sites
- Dot-hover tooltips remain functional after dot shrinkage
- No regressions in dark mode or in either consumer call site
</success_criteria>

<output>
Create `.planning/phases/90-post-election-follow-up-minicompass-ui/90-02-SUMMARY.md` when done. Include: installed ev-ui version, final dotRadius value chosen (2.5 vs 3.0), whether the paddingRight:14 nudges were removed, any visual surprises in dark mode or mobile, and human-verifier approval signal.
</output>
