---
phase: 124-ma-tier3-stances-wave3
plan: 03
type: execute
wave: 3
depends_on: [124-02]
files_modified:
  - C:/EV-Accounts/backend/migrations/677_donahue_stances.sql
  - C:/EV-Accounts/backend/migrations/678_bradley_macarthur_stances.sql
  - C:/EV-Accounts/backend/migrations/679_brasco_stances.sql
  - C:/EV-Accounts/backend/migrations/680_king_stances.sql
  - C:/EV-Accounts/backend/migrations/681_leblanc_stances.sql
  - C:/EV-Accounts/backend/migrations/682_tzioumis_stances.sql
  - C:/EV-Accounts/backend/migrations/683_vidal_stances.sql
autonomous: true
requirements: [WALTHAM-03]
must_haves:
  truths:
    - "Waltham Mayor Arthur Donahue has evidence-only stance rows (or documented blank if no public record)"
    - "All 6 Waltham at-large City Councillors have evidence-only stance rows or documented blanks"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
    - "No INSERT references an external_id outside the -2572600001 to -2572600016 range"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/677_donahue_stances.sql"
      provides: "Mayor Donahue stance migration"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/683_vidal_stances.sql"
      provides: "At-Large Vidal stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Research and apply evidence-only compass stances for 7 Waltham officials: Mayor Donahue + 6 at-large City Councillors.

Purpose: Partial satisfaction of WALTHAM-03 (at-large batch). Ward councillors follow in Plan 04.
Output: 7 per-individual stance migration files (677–683), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-ma-tier3-stances-wave3/124-02-SUMMARY.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs
</interfaces>

Waltham at-large roster (external_id → migration), from Phase 121 deep seed (migration 592, geo_id=2572600):
  -2572600001 Arthur Donahue (Mayor)                           → 677
  -2572600002 Colleen Bradley-MacArthur (At-Large)             → 678
  -2572600003 Paul Brasco (At-Large)                           → 679
  -2572600004 Tim King (At-Large)                              → 680
  -2572600005 Randall LeBlanc (At-Large, Council VP)           → 681
  -2572600006 Emma Tzioumis (At-Large)                         → 682
  -2572600007 Carlos Vidal (At-Large)                          → 683

Ward roster handled in Plan 04 (migrations 684-692):
  -2572600008 to -2572600016 (Ward 1–9 councillors)

Out-of-scope guard: Do NOT write stances for any external_id outside -2572600001 to -2572600016.

IMPORTANT — Waltham spelling convention: Waltham uses double-L 'City Councillor' (British spelling) per official city website. This is confirmed from Phase 121 migration 592. Do NOT use single-L 'Councilor' in reasoning text.

Key Waltham context:
- Arthur Donahue: first elected Mayor Jan 2024 (succeeded Jeannette McCarthy after 12 years); relatively new; limited public record compared to longer-serving officials; expect thin stances
- Waltham is a Route 128 tech corridor city with diverse population; generally moderate politics
- LeBlanc (Council VP) and King have multi-year records; Brasco has strong involvement in community issues
- city.waltham.ma.us is Cloudflare-blocked for bots — use local news sources (Waltham News Tribune, Waltham Patch, Boston Globe Waltham coverage) and social media/press releases instead
- Bradley-MacArthur: hyphenated last name stored as 'Bradley-MacArthur' in DB (stored from migration 592)
- Key local topics: growth-and-development, economic-development, transportation-priorities (Route 128 corridor), housing, residential-zoning
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight UUID confirmation + Mayor Donahue + Bradley-MacArthur + Brasco (677–679)</name>
  <files>C:/EV-Accounts/backend/migrations/677_donahue_stances.sql, C:/EV-Accounts/backend/migrations/678_bradley_macarthur_stances.sql, C:/EV-Accounts/backend/migrations/679_brasco_stances.sql</files>
  <read_first>
    - .planning/phases/124-ma-tier3-stances-wave3/124-02-SUMMARY.md (confirmed topic count and pattern from Plan 02)
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md (Full Minimal Valid Migration Template)
  </read_first>
  <action>
    First, confirm UUIDs for all 16 Waltham officials via mcp__supabase-local__execute_sql:
    `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id BETWEEN -2572600016 AND -2572600001 ORDER BY external_id DESC;`
    Capture all 16 UUIDs (at-large -2572600001..-2572600007 for this plan; ward -2572600008..-2572600016 for Plan 04). Record all 16 in SUMMARY.

    Then process ONE politician at a time, strictly sequentially: Donahue (-2572600001, Mayor), Bradley-MacArthur (-2572600002, At-Large), Brasco (-2572600003, At-Large).
    (a) Research across ALL active compass topics; place each topic with direct evidence on the 1.0–5.0 scale.
    (b) Write the canonical migration file. Note: Donahue is relatively new (Jan 2024) — expect thin record; blank spokes are honest. Bradley-MacArthur: hyphenated name — search under both "Colleen Bradley-MacArthur" and "Colleen Bradley MacArthur" (without hyphen).
    (c) Apply via mcp__supabase-local__execute_sql, then immediately verify before next official.
    Do NOT touch external_ids outside -2572600001 to -2572600016.
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{uuid}' AND pc.politician_id IS NULL;` returns 0, AND `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - All 16 Waltham UUIDs captured and recorded in SUMMARY
    - 3 migration files exist (677, 678, 679), each wrapped in BEGIN;/COMMIT;
    - Every value is a float literal; every sources array double-cast ::text[]::text[]
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT outside -2572600001 to -2572600016
  </acceptance_criteria>
  <done>Waltham UUIDs captured; migrations 677–679 written, applied, and verified for Donahue, Bradley-MacArthur, Brasco.</done>
</task>

<task type="auto">
  <name>Task 2: King + LeBlanc + Tzioumis + Vidal (680–683)</name>
  <files>C:/EV-Accounts/backend/migrations/680_king_stances.sql, C:/EV-Accounts/backend/migrations/681_leblanc_stances.sql, C:/EV-Accounts/backend/migrations/682_tzioumis_stances.sql, C:/EV-Accounts/backend/migrations/683_vidal_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format)
    - C:/EV-Accounts/backend/migrations/677_donahue_stances.sql (in-phase sibling reference)
  </read_first>
  <action>
    Process ONE politician at a time, sequentially: King (-2572600004, At-Large), LeBlanc (-2572600005, At-Large Council VP), Tzioumis (-2572600006, At-Large), Vidal (-2572600007, At-Large). Same procedure as Task 1: research → write → apply → verify. Tim King and Randall LeBlanc may have longer records as at-large councillors; Emma Tzioumis and Carlos Vidal may be newer members with thinner records. LeBlanc: middle initial J. dropped per DB convention (stored as 'Randall LeBlanc'). Do NOT touch any external_id outside -2572600001 to -2572600016.
  </action>
  <verify>
    <automated>For each of the 4 UUIDs: unpaired-answer query returns 0 AND uncited-context query returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - 4 migration files exist (680, 681, 682, 683), each wrapped in BEGIN;/COMMIT;
    - Every value is a float literal; every sources array double-cast ::text[]::text[]
    - Unpaired-answer query returns 0 for all 4 UUIDs
    - Uncited-context query returns 0 for all 4 UUIDs
    - No INSERT outside -2572600001 to -2572600016
  </acceptance_criteria>
  <done>Migrations 680–683 written, applied, and verified for King, LeBlanc, Tzioumis, Vidal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (mcp__supabase-local) | Writes go directly to live production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | stance value integrity | mitigate | Evidence-only rule; per-person uncited-context query must return 0 |
| T-124-02 | Information Disclosure | reasoning / sources | accept | Public-record political positions; no PII |
| T-124-03 | Repudiation | stance provenance | mitigate | Every value paired with a path-bearing source URL |
| T-124-04 | Tampering | out-of-scope external_id | mitigate | Acceptance criteria: no INSERT outside -2572600001 to -2572600016 |
| T-124-SC | Tampering | npm/pip/cargo installs | n/a | No package installs |
</threat_model>

<verification>
After both tasks, run the Waltham at-large citation check:
`SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2572600007 AND -2572600001) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` must return 0.
</verification>

<success_criteria>
- Migrations 677–683 (7 Waltham at-large officials) written, applied to production, and verified
- 0 unpaired answers, 0 uncited contexts across all 7 Waltham at-large officials
- All 16 Waltham UUIDs recorded in SUMMARY for use in Plan 04
- Blank-spoke officials documented in SUMMARY (not defaulted to 3.0)
</success_criteria>

<output>
Create `.planning/phases/124-ma-tier3-stances-wave3/124-03-SUMMARY.md` when done. Record: all 16 Waltham UUIDs (at-large AND ward for Plan 04), per-official stance counts, blank-spoke officials.
</output>
