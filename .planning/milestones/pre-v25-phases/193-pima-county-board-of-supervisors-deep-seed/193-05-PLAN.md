---
phase: 193-pima-county-board-of-supervisors-deep-seed
plan: 05
type: execute
wave: 5
depends_on: ["193-04"]
files_modified:
  - src/lib/buildingImages.js
  - src/lib/coverage.js
autonomous: false
requirements: [BANR-01, PIMA-01]
must_haves:
  truths:
    - "A licensed Santa Catalina Mountains / Sonoran-desert banner (real photo, no AI, no aerial) is processed to 1700×540 and uploaded to Storage at cities/pima-county.jpg"
    - "buildingImages.js CURATED_LOCAL has a state-scoped 'pima county' entry pointing at the uploaded banner (first COUNTY-tier entry)"
    - "coverage.js COVERAGE_COUNTIES has a DB-honest 'Pima County' chip (hasContext:true, since stances landed in Plan 04) that browses geo_id 04019"
    - "The banner is visually distinct from the future Tucson-city banner and the AZ-state Phoenix skyline"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "CURATED_LOCAL 'pima county' → uploaded banner URL"
      contains: "pima county"
    - path: "src/lib/coverage.js"
      provides: "COVERAGE_COUNTIES 'Pima County' chip, browseGovernmentList ['04019'], hasContext:true"
      contains: "Pima County"
  key_links:
    - from: "coverage.js COVERAGE_COUNTIES 'Pima County'"
      to: "buildingImages.js CURATED_LOCAL 'pima county'"
      via: "browse_label 'Pima County' → representingCity → getBuildingImages substring match"
      pattern: "pima county"
    - from: "buildingImages.js 'pima county'"
      to: "Storage cities/pima-county.jpg"
      via: "src URL (state-scoped 'AZ')"
      pattern: "cities/pima-county.jpg"
---

<objective>
Give Pima County its licensed community banner (BANR-01) and its DB-honest coverage chip. Source a
real Santa Catalina Mountains / Sonoran-desert (saguaro) landscape photo ONE AT A TIME (no AI, no
aerial — D-04), process it to the 1700×540 banner spec via `scripts/banners/process_banner.py`,
upload to Storage at `cities/pima-county.jpg` via `upload_banner.py`, wire it into
`src/lib/buildingImages.js` CURATED_LOCAL as the FIRST county-tier entry (`'pima county'`), and
surface Pima County in `src/lib/coverage.js` COVERAGE_COUNTIES with a `hasContext:true` chip
(stances landed in Plan 04).

Purpose: Satisfies ROADMAP success criteria #4 (licensed banner sourced/processed/uploaded/wired)
and #5 (Pima County surfaced with a DB-honest chip). The banner must read "Pima County the place"
and stay visually distinct from the future Tucson-city banner (Phase 194, downtown streetscape
reserved) and the live AZ-state Phoenix skyline.
Output: uploaded `cities/pima-county.jpg` + edited `buildingImages.js` + `coverage.js` in the
`essentials` repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md

<execution_note>
Frontend files (`src/lib/buildingImages.js`, `src/lib/coverage.js`) live in the CURRENT `essentials`
repo (commit here, not `C:/EV-Accounts`). The banner SOURCING + processing + Storage upload are
ORCHESTRATOR-RUN (executor has no network/Storage access): the inline orchestrator sources the photo
one-at-a-time, runs the banner toolchain, and uploads. Per project convention
(`feedback_banner_source_one_at_a_time`, `feedback_banner_prefer_photo_over_gradient`) banner
sourcing is sequential and prefers a real photo — keep re-sourcing rather than falling back to a
gradient unless truly unusable. The CDN URL is deterministic (`cities/pima-county.jpg`) so the
frontend wiring can be authored regardless of upload order.
</execution_note>

<interfaces>
- Banner spec: 1700×540 @ 3.15:1, LANCZOS, q90 via `scripts/banners/process_banner.py`
  (`--url <src> --output <path> --vertical-anchor 0.3-0.5`); upload via
  `scripts/banners/upload_banner.py --file <path> --dest cities/pima-county.jpg` (needs
  SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`). Destination tier is `cities/`
  (keyed by banner TYPE, not government type — no `counties/` tier exists).
- CDN URL: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/pima-county.jpg
- buildingImages.js CURATED_LOCAL: append `'pima county': { state: 'AZ', src: '<CDN URL>' }` with an
  attribution comment (title | author | license) above it, matching the file's existing convention
  (e.g. the henderson/berkeley single-variant entries). getBuildingImages() needs NO code change —
  its substring match (`city.includes(key)`) + state-scoping already handle a county label.
- coverage.js COVERAGE_COUNTIES: append
  `{ label: 'Pima County', browseGovernmentList: ['04019'], browseStateAbbrev: 'AZ', hasContext: true }`
  (mirrors the existing Clark County / Washington County rows). hasContext:true is honest here
  because Plan 04 seeded ≥1 stance row.
- Pitfall 7 (NOT a defect to fix): the 'pima county' banner reliably renders via the COVERAGE_COUNTIES
  browse chip (browse_label 'Pima County' → representingCity); an arbitrary Tucson-area address parses
  to "Tucson" via parseCityFromAddress and will NOT show this banner until Phase 194 — expected
  graceful degradation, identical to Clark/WashCo. Do NOT broaden parseCityFromAddress in this phase.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Source + process + upload the Pima County banner (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Sources Tertiary — the one-at-a-time Wikimedia sourcing pass is required; no specific image was shortlisted; § user_constraints D-04)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md (lines 363-381 — the process_banner.py / upload_banner.py commands + cities/<slug>.jpg destination convention)
    - C:/Transparent Motivations/essentials/scripts/banners/process_banner.py (the banner spec: 1700×540, LANCZOS, q90, --vertical-anchor)
    - C:/Transparent Motivations/essentials/scripts/banners/upload_banner.py (the Storage upload + --dest convention)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. The orchestrator sources a licensed Catalinas/Sonoran photo ONE AT A TIME, processes it to 1700×540, uploads to cities/pima-county.jpg, and the operator visually approves the banner before wiring. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A licensed Santa Catalina Mountains / Sonoran-desert (saguaro) banner for Pima County — the FIRST
    county-tier banner in the app. RESEARCH did NOT shortlist a specific image, so the orchestrator
    performs the one-at-a-time sourcing pass against commons.wikimedia.org (Category:Santa_Catalina_
    Mountains and Sonoran_Desert), processes via process_banner.py (1700×540, --vertical-anchor tuned
    per shot), and uploads via upload_banner.py to cities/pima-county.jpg. Human visual QA is required
    because "real photo, no AI, no aerial, distinct from Tucson/Phoenix" is a judgment call a row-count
    cannot make.
  </what-built>
  <how-to-verify>
    1. The orchestrator sources ONE candidate photo (licensed CC/public-domain, real photo, NOT
       AI-generated, NOT aerial), records the title | author | license, runs
       `python scripts/banners/process_banner.py --url <src> --output /tmp/pima-county.jpg
       --vertical-anchor 0.3` (tune anchor per shot), then
       `python scripts/banners/upload_banner.py --file /tmp/pima-county.jpg --dest cities/pima-county.jpg`
       (SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
    2. Operator visually confirms: reads as Pima County the PLACE (Catalinas/Sonoran desert), a real
       photo (no AI artifacts, no aerial/drone shot), and visually DISTINCT from both the future
       Tucson-city downtown streetscape (Phase 194) and the AZ-state Phoenix skyline (already live —
       show the state banner alongside if there's any risk of conflict).
    3. If unusable, the orchestrator re-sources another candidate ONE AT A TIME (prefer a real photo;
       gradient only if truly nothing usable exists).
  </how-to-verify>
  <acceptance_criteria>
    - `curl -sI https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/pima-county.jpg` returns HTTP 200.
    - The uploaded image is 1700×540 (banner spec) — PIL/curl-download dimension check.
    - Operator approves: real photo, Catalinas/Sonoran subject, no AI, no aerial, distinct from Tucson-city + AZ-state banners.
    - Title | author | license recorded in 193-05-SUMMARY.md for the attribution comment.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the operator confirms the uploaded banner reads as Pima County, is a real non-aerial photo, and is distinct from the Tucson/Phoenix banners — or describe what to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner into buildingImages.js + surface Pima County in coverage.js</name>
  <files>src/lib/buildingImages.js, src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md (lines 317-360 — the exact CURATED_LOCAL 'pima county' entry + attribution comment, and the COVERAGE_COUNTIES append; note getBuildingImages needs NO code change)
    - C:/Transparent Motivations/essentials/src/lib/buildingImages.js (the CURATED_LOCAL object + an existing single-variant state-scoped entry like henderson/berkeley to mirror; getBuildingImages substring/state-scoping logic)
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the COVERAGE_COUNTIES array + the Clark County / Washington County rows to mirror; the file's hasContext doc comment)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-05-SUMMARY.md (the banner title | author | license from Task 1 for the attribution comment)
  </read_first>
  <action>
    In `src/lib/buildingImages.js`, append ONE entry to the CURATED_LOCAL object:
    `'pima county': { state: 'AZ', src: '<CDN URL for cities/pima-county.jpg>' }`, with an attribution
    comment above it (title | author | license from Task 1) matching the file's existing comment
    convention. This is the first COUNTY-tier CURATED_LOCAL key — note in the comment it stays
    distinct from the future Tucson CITY banner and the AZ STATE Phoenix skyline. Make NO change to
    getBuildingImages() — its substring match + state-scoping already handle a county label like a city.

    In `src/lib/coverage.js`, append ONE entry to COVERAGE_COUNTIES:
    `{ label: 'Pima County', browseGovernmentList: ['04019'], browseStateAbbrev: 'AZ', hasContext: true }`
    near the other county entries (order is cosmetic). hasContext:true is honest because Plan 04
    seeded ≥1 stance row (this plan depends on Plan 04). Do NOT broaden parseCityFromAddress or add
    address-parsing logic (Pitfall 7 — out of scope).

    Commit both files to the `essentials` repo via git.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qiF "pima county" src/lib/buildingImages.js && grep -qF "cities/pima-county.jpg" src/lib/buildingImages.js && grep -qF "Pima County" src/lib/coverage.js && grep -qF "'04019'" src/lib/coverage.js && node --input-type=module -e "import('./src/lib/coverage.js').then(()=>console.log('coverage-ok')).catch(e=>{console.error(e);process.exit(1)})" && node --input-type=module -e "import('./src/lib/buildingImages.js').then(()=>console.log('bi-ok')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has a `'pima county'` key with `state: 'AZ'` and src = the cities/pima-county.jpg CDN URL, plus an attribution comment (title | author | license).
    - coverage.js COVERAGE_COUNTIES has a `{ label: 'Pima County', browseGovernmentList: ['04019'], browseStateAbbrev: 'AZ', hasContext: true }` entry.
    - Both files parse cleanly via `node --input-type=module -e "import(...)"` (exit 0) — matches the Phase 162 coverage.js parse-check convention (no dedicated test file exists).
    - getBuildingImages() is unchanged; parseCityFromAddress is unchanged.
    - Both files committed to the `essentials` repo.
  </acceptance_criteria>
  <done>Pima County banner wired into CURATED_LOCAL and surfaced in COVERAGE_COUNTIES with a DB-honest hasContext:true chip; both frontend files parse and are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wikimedia (remote) → banner pipeline | Untrusted remote image bytes enter process_banner.py over HTTP |
| banner pipeline → Storage | Processed banner crosses into the production politician_photos bucket |
| frontend edits → live app | coverage.js / buildingImages.js changes ship to Render on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-BAN | Tampering | remote banner image fetch | mitigate | process_banner.py re-encodes to JPEG (strips embedded payloads); operator visual QA confirms a real non-AI non-aerial photo before wiring |
| T-193-LIC | Repudiation | banner license provenance | mitigate | Title \| author \| license recorded in the SUMMARY + the attribution comment; only CC/public-domain sources used |
| T-193-COVLIE | Tampering | dishonest coverage chip | mitigate | hasContext:true only set because Plan 04 landed ≥1 stance row (depends_on 193-04); node parse-check confirms valid module |
| T-193-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; never in a committed frontend file |
| T-193-SC | Tampering | package installs | accept | No new installs — reuses installed PIL/requests banner toolchain; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- cities/pima-county.jpg returns HTTP 200 at 1700×540; operator-approved as a real, distinct Catalinas/Sonoran photo.
- CURATED_LOCAL 'pima county' → banner URL; COVERAGE_COUNTIES 'Pima County' chip hasContext:true.
- Both frontend files parse and are committed.
</verification>

<success_criteria>
ROADMAP #4 (licensed banner sourced/processed/uploaded/wired) and #5 (Pima County surfaced with a
DB-honest chip) are TRUE.
</success_criteria>

<output>
Create `.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-05-SUMMARY.md` when done —
include the banner title | author | license and the CDN URL.
</output>
