---
phase: 173-nevada-playbook-retrospective-close
plan: 02
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - src/lib/coverage.js
  - LOCATION-ONBOARDING.md
autonomous: true
requirements: [NV-RETRO-01]

must_haves:
  truths:
    - "All 6 NV jurisdictions (5 cities/CCSD in COVERAGE_STATES NV block + Clark County in COVERAGE_COUNTIES) are present and browsable with the correct geo_id / browse params and browseStateAbbrev:'NV'"
    - "hasContext:true (purple chip) is set ONLY for NV jurisdictions the audit verified with >=1 seeded compass stance; 0-stance jurisdictions (CCSD by design) are listed but plain"
    - "The NV state-legislature ride-along is confirmed surfaced via the auto-built browse_state_officials=NV path (no separate city-grid row needed)"
    - "LOCATION-ONBOARDING.md has one Cities Onboarded row per NV jurisdiction"
    - "LOCATION-ONBOARDING.md has a Nevada Quick Reference block capturing the full D-07 playbook scope (ext_id schemes, ward MTFCCs, WAF map, lowercase nv casing, browse params, geo_ids, migration-counter convention)"
  artifacts:
    - path: "src/lib/coverage.js"
      provides: "COVERAGE_STATES NV block + COVERAGE_COUNTIES Clark County with audit-correct hasContext flags"
      contains: "3240000"
    - path: "LOCATION-ONBOARDING.md"
      provides: "NV Cities Onboarded rows + NV GOTCHAs + Nevada Quick Reference block"
      contains: "Nevada Quick Reference"
  key_links:
    - from: "src/pages/Landing.jsx"
      to: "src/lib/coverage.js"
      via: "import { COVERAGE_STATES } from '../lib/coverage'"
      pattern: "COVERAGE_STATES"
---

<objective>
Surface all covered NV jurisdictions honestly on the landing page (verify presence + reconcile the
purple chip against real DB stance counts) and capture the Nevada wave's learnings in the onboarding
playbook.

Purpose: Make every seeded NV jurisdiction browsable with an honest purple chip (chip only where the
Plan 01 audit verified >=1 stance, D-01; CCSD drops to plain per D-01a), and record the reusable Nevada
scaffolding in the playbook (D-06 GOTCHAs + Cities Onboarded rows, D-07 Nevada Quick Reference block).

Output: reconciled `src/lib/coverage.js` (NV block of COVERAGE_STATES + Clark County in
COVERAGE_COUNTIES) + edited `LOCATION-ONBOARDING.md` (NV Cities Onboarded rows + net-new NV GOTCHAs +
the Nevada Quick Reference block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-nevada-playbook-retrospective-close/173-CONTEXT.md
@.planning/phases/173-nevada-playbook-retrospective-close/173-01-SUMMARY.md
@.planning/v18.0-MILESTONE-AUDIT.md
</context>

<interfaces>
The landing-page surfacing target is `src/lib/coverage.js`, NOT Landing.jsx directly. Landing.jsx
imports `COVERAGE_STATES` from `../lib/coverage`; the locality typeahead (`searchCoverageAreas`) and
browse routing (`coverageAreaToPath`) also derive from it.

NV block (currently in `COVERAGE_STATES`, all 5 entries present, ALL `hasContext: true`):
  { label: 'Las Vegas', browseGovernmentList: ['3240000'], browseStateAbbrev: 'NV', hasContext: true }
  { label: 'Henderson', browseGovernmentList: ['3231900'], browseStateAbbrev: 'NV', hasContext: true }
  { label: 'North Las Vegas', browseGovernmentList: ['3251800'], browseStateAbbrev: 'NV', hasContext: true }
  { label: 'Boulder City', browseGovernmentList: ['3206500'], browseStateAbbrev: 'NV', hasContext: true }
  { label: 'Clark County School District', browseGeoId: '3200060', browseMtfcc: 'G5420', browseStateAbbrev: 'NV', hasContext: true }
Clark County (currently in `COVERAGE_COUNTIES`, present, `hasContext: true`):
  { label: 'Clark County', browseGovernmentList: ['32003'], browseStateAbbrev: 'NV', hasContext: true }

- `hasContext: true` renders the purple chip = "jurisdiction has >=1 compass stance seeded" (coverage.js
  header comment, line ~4). REMOVE the `hasContext` key entirely for a 0-stance jurisdiction so it renders
  plain (see the ME/OR/UT plain entries for the no-chip shape) — keep the entry listed and browsable.
- `STATE_NAME_TO_ABBREV` already contains `nevada: 'NV'` (line ~202), so `COVERAGE_BROWSE_STATES` already
  auto-builds the statewide `browse_state_officials=NV` ride-along. The legislature surfaces through that
  path — NO separate city-grid row is needed. This task VERIFIES that, it does not add a row.
- This task is VERIFY-AND-RECONCILE: all 6 entries already exist with correct geo_ids/browse params
  (verified 2026-06-30). Do NOT insert or move entries; confirm presence + correct params, then bring
  each entry's `hasContext` into agreement with the Plan 01 audit's actual stance count.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile NV hasContext chips in coverage.js (NV block + Clark County) against the audit</name>
  <read_first>
    - src/lib/coverage.js (the file being modified — the NV block of COVERAGE_STATES ~line 184, the Clark County entry in COVERAGE_COUNTIES ~line 240, the header comment defining hasContext semantics line ~4, and the STATE_NAME_TO_ABBREV nevada entry ~line 202)
    - .planning/v18.0-MILESTONE-AUDIT.md (Plan 01's recorded set of >=1-stance jurisdictions — the authoritative hasContext input, D-01)
    - .planning/phases/173-nevada-playbook-retrospective-close/173-CONTEXT.md (D-01 chip rule + D-01a CCSD-plain + D-01b LV-verify + D-01c geo_ids-already-correct)
  </read_first>
  <action>
    In `src/lib/coverage.js`, verify all 6 NV jurisdictions are present with correct identifiers and
    reconcile only their `hasContext` flags against the Plan 01 audit:
    - NV block of `COVERAGE_STATES`: confirm Las Vegas `3240000`, Henderson `3231900`, North Las Vegas
      `3251800`, Boulder City `3206500` each as
      `{ label, browseGovernmentList: ['<geo_id>'], browseStateAbbrev: 'NV' }`, and CCSD as
      `{ label: 'Clark County School District', browseGeoId: '3200060', browseMtfcc: 'G5420', browseStateAbbrev: 'NV' }`.
    - COVERAGE_COUNTIES: confirm Clark County `{ browseGovernmentList: ['32003'], browseStateAbbrev: 'NV' }`.
    For EACH of the 6 jurisdictions set `hasContext: true` ONLY if the audit verified >=1 seeded compass
    stance; for any the audit recorded as 0-stance/roster-only, REMOVE the `hasContext` key so it renders
    plain (D-01). CCSD specifically drops to plain (0 stances by design, D-01a) — keep it listed and
    browsable. Las Vegas: set the chip strictly per its audit-verified count (D-01b — do not assume).
    Then verify (no edit needed) that `STATE_NAME_TO_ABBREV` contains `nevada: 'NV'` so the statewide
    `browse_state_officials=NV` legislature ride-along is auto-built into `COVERAGE_BROWSE_STATES`.
    Do NOT add, move, or remove any entry; do NOT touch other state blocks, the CA/MA/UT/VA entries,
    the helper functions, or the geo_ids/browse params. No fenced code: edit the boolean flags in place.
  </action>
  <verify>
    <automated>node --input-type=module -e "const m=await import('./src/lib/coverage.js'); const nv=m.COVERAGE_STATES.find(s=>s.abbrev==='NV'); if(!nv){console.error('no NV block');process.exit(1)} const have=new Set(nv.areas.flatMap(a=>a.browseGovernmentList||[]).concat(nv.areas.flatMap(a=>a.browseGeoId?[a.browseGeoId]:[]))); const miss=['3240000','3231900','3251800','3206500','3200060'].filter(g=>!have.has(g)); if(miss.length){console.error('MISSING',miss);process.exit(1)} const ccsd=nv.areas.find(a=>a.browseGeoId==='3200060'); if(ccsd.hasContext){console.error('CCSD must be plain (no hasContext)');process.exit(1)} const cc=m.COVERAGE_COUNTIES.find(c=>(c.browseGovernmentList||[]).includes('32003')); if(!cc){console.error('no Clark County');process.exit(1)} if(m.STATE_NAME_TO_ABBREV.nevada!=='NV'){console.error('nevada->NV missing');process.exit(1)} console.log('NV coverage reconciled OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 NV city geo_ids (3240000, 3231900, 3251800, 3206500) + CCSD (browseGeoId 3200060, browseMtfcc G5420) are present in the NV block with browseStateAbbrev:'NV'.
    - Clark County (32003) is present in COVERAGE_COUNTIES with browseStateAbbrev:'NV'.
    - Each jurisdiction's `hasContext` flag matches the Plan 01 audit: present+true only for >=1-stance jurisdictions, key removed (plain) for 0-stance.
    - CCSD has NO `hasContext` key (plain — 0 stances by design, D-01a) while remaining listed/browsable.
    - `STATE_NAME_TO_ABBREV` contains `nevada: 'NV'` (legislature ride-along auto-builds; no separate grid row added).
    - No entry was added/moved/removed; no other state block, COVERAGE_COUNTIES non-NV entry, helper, geo_id, or browse param was modified.
    - coverage.js still parses (the node verify command exits 0).
  </acceptance_criteria>
  <done>
    All 6 NV jurisdictions browsable in coverage.js with audit-honest purple-chip flags (CCSD plain),
    the legislature ride-along confirmed surfaced via browse_state_officials=NV; landing grid and
    locality typeahead pick them up via the existing COVERAGE_STATES/COVERAGE_COUNTIES imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add NV Cities Onboarded rows + NV GOTCHAs + the Nevada Quick Reference block to LOCATION-ONBOARDING.md</name>
  <read_first>
    - LOCATION-ONBOARDING.md (the file being modified — the "Cities Onboarded" 5-column table ~line 29 and the rich "Notable patterns" cells of recent rows to mirror the row shape; locate where to append the Nevada Quick Reference block)
    - .planning/v18.0-MILESTONE-AUDIT.md (per-jurisdiction verified counts + verdicts to ground each row's Notable patterns + the stance counts)
    - .planning/phases/173-nevada-playbook-retrospective-close/173-CONTEXT.md (D-06 — the 6 NV GOTCHA themes; D-07 — the full Nevada Quick Reference scope, verbatim facts)
    - .planning/STATE.md (per-phase 158-168 outcome notes — ext_id ranges, WAF notes, mayor types, headshot sources — feeding the Notable-patterns cells + Quick Reference)
  </read_first>
  <action>
    In `LOCATION-ONBOARDING.md`:
    (A) Add one "Cities Onboarded" table row per NV jurisdiction using the existing 5-column format
    (City | State | Onboarded | Election method | Notable patterns): City of Las Vegas (3240000, X0015
    wards, ext -3206xxx area / per audit), Henderson (3231900, X0016 wards, ext -3206001..-3206005,
    cityofhenderson.com Akamai-403 fallback chain), North Las Vegas (3251800, X0017 wards from Clark
    County GISMO PLACE=80, ext -3207xxx, Arabic-numeral wards, cityofnorthlasvegas.com WAF-403 →
    Wikimedia fallback), Boulder City (3206500, AT-LARGE no wards, ext -3208xxx, flybouldercity.com
    clean/no-WAF), CCSD (3200060/G5420, Board of School Trustees, 7 elected A-G + 4 appointed, 0 stances
    by design), Clark County (32003, standalone county NOT under State of NV, Board of County
    Commissioners, ext -3200301..-3200307, single COUNTY district, Strip = unincorporated). Fill each
    Notable-patterns cell from the audit + STATE.md per-phase notes; mark NV (or "NV (county)" /
    "NV (school)") in the State column.
    (B) Add the net-new Nevada GOTCHAs covering the D-06 themes that recurred across 158-168: per-city
    WAF map; custom ward-MTFCC geofences (X0015/X0016/X0017); standalone-county-not-under-state pattern
    for Clark County; lowercase `'nv'` district casing; Wikimedia descriptive-UA requirement (429 on
    browser UA); statewide-officials browse via browse_state_officials=NV (not geo_id+G5200). Do not
    duplicate GOTCHAs already present from earlier states.
    (C) Add a consolidated `## Nevada Quick Reference` block (new D-07 deliverable) capturing the full
    playbook scope: ext_id schemes (Clark County -3200301..-3200307; Controller -3200006; State Senate
    -3203001..-3203021; Assembly -3204001..-3204042; Henderson -3206001..-3206005; Boulder City -3208xxx;
    North Las Vegas -3207xxx); geofence MTFCCs (Henderson wards X0016, North Las Vegas wards X0017,
    Las Vegas wards X0015, CCSD G5420; Strip = unincorporated Clark County); the per-city WAF map;
    casing rule (state code lowercase `'nv'` everywhere for districts/SLDU/SLDL); browse params (statewide
    `?browse_state_officials=NV`; city/county `browse_government_list=<geo_id>`; CCSD
    `browse_geo_id=3200060&browse_mtfcc=G5420`); geo_ids (LV 3240000, Henderson 3231900, N.Las Vegas
    3251800, Boulder City 3206500, Clark County 32003, CCSD 3200060); migration-counter convention
    (on-disk counter authoritative; stance migrations apply audit-only / unregistered, do not bump
    schema_migrations; next migration 1115 — confirm at execution). Use the Edit/Write tools (no heredoc).
  </action>
  <verify>
    <automated>grep -qi "Nevada Quick Reference" LOCATION-ONBOARDING.md && for c in "Las Vegas" "Henderson" "North Las Vegas" "Boulder City" "Clark County" "Clark County School District"; do grep -q "$c" LOCATION-ONBOARDING.md || { echo "MISSING ROW/REF: $c"; exit 1; }; done && grep -q "X0016" LOCATION-ONBOARDING.md && grep -q "X0017" LOCATION-ONBOARDING.md && grep -q "browse_state_officials=NV" LOCATION-ONBOARDING.md && grep -q "3200301" LOCATION-ONBOARDING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 6 new Cities Onboarded rows exist (City of Las Vegas, Henderson, North Las Vegas, Boulder City, CCSD, Clark County), each in the 5-column format with a populated Notable-patterns cell grounded in the audit.
    - Net-new NV GOTCHAs cover the D-06 themes (WAF map, ward MTFCCs, standalone-county pattern, lowercase nv casing, Wikimedia UA, browse_state_officials=NV) without duplicating earlier-state content.
    - A `## Nevada Quick Reference` block exists capturing the full D-07 scope: ext_id schemes, geofence MTFCCs (incl. X0016/X0017), WAF map, lowercase nv casing rule, browse params, geo_ids, migration-counter convention.
    - All 6 jurisdiction names + X0016 + X0017 + browse_state_officials=NV + ext-range -3200301 appear in the file (verify command lists none missing).
    - No earlier-state GOTCHA content was deleted or duplicated.
  </acceptance_criteria>
  <done>
    Onboarding playbook updated — 6 NV jurisdiction rows + net-new NV GOTCHAs + the consolidated Nevada
    Quick Reference block, ready to accelerate the next NV wave or any new state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → committed frontend data | A static JS data-array (coverage.js) is edited and committed |
| executor → committed playbook doc | LOCATION-ONBOARDING.md markdown is edited and committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-03 | Tampering | coverage.js static data array | mitigate | Edit is flag-only (remove/keep hasContext booleans); node parse-check in verify confirms the module still loads + NV entries intact. No new inputs/endpoints/auth. |
| T-173-04 | Information Disclosure | LOCATION-ONBOARDING.md | accept | Playbook records public officeholder/source patterns + ext_id schemes only; no secrets or credentials written. |
| T-173-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |

Realistic surface is minimal: static data-array flag edits + markdown edits, no runtime input handling.
</threat_model>

<verification>
- coverage.js parses; all 6 NV jurisdictions present with audit-correct hasContext flags (CCSD plain); nevada->NV ride-along confirmed.
- LOCATION-ONBOARDING.md contains 6 new NV rows + net-new NV GOTCHAs + the Nevada Quick Reference block.
</verification>

<success_criteria>
- Criterion 1: all NV jurisdictions surfaced with correct browse wiring + honest purple-chip state; legislature ride-along confirmed via browse_state_officials=NV.
- Criterion 2: onboarding playbook updated with NV GOTCHAs + Nevada Quick Reference block + NV Cities Onboarded rows.
</success_criteria>

<output>
Create `.planning/phases/173-nevada-playbook-retrospective-close/173-02-SUMMARY.md` when done — note which
NV jurisdictions kept the purple chip vs which dropped to plain (CCSD), and confirm the Nevada Quick
Reference block was added.
</output>
