---
phase: 191-arizona-state-federal-government
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1284_az_house_headshots.sql
  - C:/EV-Accounts/backend/scripts/_tmp-az-house-headshots.py
autonomous: true
requirements: [AZ-STATE-02]
user_setup: []

must_haves:
  truths:
    - "Each of the 9 AZ US House reps has a 600x750 headshot (Grijalva CD-7 already had one; this plan adds the other 8)"
    - "All 9 US House reps continue to route to residents by their CD geofence (already correct from prior seed + Phase 190 — not modified here)"
    - "The 2 US Senators (Kelly, Gallego) remain seeded with headshots and are not touched"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-az-house-headshots.py"
      provides: "unitedstates.github.io resize-only (already 4:5) download->upload for 8 House reps"
      contains: "unitedstates.github.io"
    - path: "C:/EV-Accounts/backend/migrations/1284_az_house_headshots.sql"
      provides: "Audit-only politician_images INSERT for 8 House reps (unregistered)"
      contains: "politician_images"
  key_links:
    - from: "1284 politician_images rows"
      to: "politicians -4001..-4009 (skip -4007)"
      via: "SELECT id FROM politicians WHERE external_id = ..."
      pattern: "external_id = -400"
---

<objective>
Add 600x750 headshots for the 8 Arizona US House reps that lack them, completing the federal-delegation half of AZ-STATE-02. All 9 House reps and both Senators are ALREADY seeded and routing correctly per RESEARCH's live DB audit — this plan does headshots only, zero structural work.

Purpose: RESEARCH confirmed 8 of 9 House reps have NO headshot (Adelita Grijalva CD-7 already has one). All 8 bioguide IDs are HTTP-200-verified against `unitedstates.github.io`, which serves public-domain, already-4:5 images (resize-only, no crop).

Output:
- `_tmp-az-house-headshots.py` (resize-only pipeline, 8 reps)
- `1284_az_house_headshots.sql` (audit-only, UNREGISTERED)
- Both committed to `C:/EV-Accounts` (branch `master`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Cross-repo + environment (READ — do not re-decide)
- Files live in `C:/EV-Accounts` — commit ONLY via `git -C "C:/EV-Accounts"`. Never stage in essentials.
- `mcp__supabase-local` IS production and is NOT exposed to you. Apply the migration yourself via `psql "$DATABASE_URL" -f ...` (`DATABASE_URL` in `C:/EV-Accounts/backend/.env`; psql at `C:/Program Files/PostgreSQL/18/bin/psql`).
- 1284 is audit-only: NO `schema_migrations` footer, applied via psql, ledger MAX stays unchanged.
- Upload uses `SUPABASE_SERVICE_ROLE_KEY` from `.env` (script reads it). CDN base: `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/`.
- This plan is INDEPENDENT of Plan 01 (disjoint external_ids -4001..-4009, disjoint migration file 1284, disjoint DB rows) — safe to run in the same wave.
</execution_context>

<context>
@.planning/phases/191-arizona-state-federal-government/191-RESEARCH.md
@.planning/phases/191-arizona-state-federal-government/191-PATTERNS.md

## AZ US House roster (from RESEARCH — bioguide IDs HTTP-200-verified 2026-07-08)
| CD | Name | external_id | bioguide | headshot needed? |
|----|------|-------------|----------|------------------|
| 1 | David Schweikert | -4001 | S001183 | YES |
| 2 | Elijah Crane | -4002 | C001132 | YES |
| 3 | Yassamin Ansari | -4003 | A000381 | YES |
| 4 | Greg Stanton | -4004 | S001211 | YES |
| 5 | Andy Biggs | -4005 | B001302 | YES |
| 6 | Juan Ciscomani | -4006 | C001133 | YES |
| 7 | Adelita S. Grijalva | -4007 | G000606 | NO — already has one (skip) |
| 8 | Abraham J. Hamadeh | -4008 | H001098 | YES |
| 9 | Paul A. Gosar | -4009 | G000565 | YES |

- Source URL: `https://unitedstates.github.io/images/congress/450x550/{bioguide}.jpg` — 450x550, already 4:5, resize-only to 600x750.
- photo_license = `public_domain` for all 8.
- 404-fallback (Pitfall 4, not expected for these 8): `clerk.house.gov/images/members/{bioguide}.jpg`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source + upload 8 US House headshots via _tmp-az-house-headshots.py</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py — PRIMARY analog: ROSTER loop, resize_600x750 (resize-only), download_image with 404 clerk.house.gov fallback, per-rep try/except, time.sleep(0.5), manifest print + sys.exit(1) required-failure gate, runtime UUID lookup
    - .planning/phases/191-arizona-state-federal-government/191-PATTERNS.md — "_tmp-az-house-headshots.py" section
    - .planning/phases/191-arizona-state-federal-government/191-RESEARCH.md — Headshot Pipeline table + Pitfall 4
    - C:/EV-Accounts/backend/.env — read DATABASE_URL + SUPABASE_SERVICE_ROLE_KEY lines
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/scripts/_tmp-az-house-headshots.py` by copy-adapting `_tmp-nv-house-headshots.py` nearly verbatim. Build an 8-entry ROSTER (external_id, bioguide) for CD 1,2,3,4,5,6,8,9 — external_ids -4001,-4002,-4003,-4004,-4005,-4006,-4008,-4009 with bioguides S001183, C001132, A000381, S001211, B001302, C001133, H001098, G000565. SKIP CD-7 / -4007 / Grijalva (already has a headshot). Source each from `https://unitedstates.github.io/images/congress/450x550/{bioguide}.jpg`; resize-only to 600x750 Lanczos q90 (NO crop — already 4:5); resolve each politician UUID at runtime via `SELECT id FROM politicians WHERE external_id=%s`; upload to `politician_photos/{uuid}-headshot.jpg`. Keep the per-rep failure isolation + manifest + `sys.exit(1)`-on-required-failure gate. Run the script; capture the printed UUID manifest for use in Task 2. Commit the script via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>python "C:/EV-Accounts/backend/scripts/_tmp-az-house-headshots.py" && echo SCRIPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0 (all 8 required downloads succeeded); manifest lists 8 SUCCESS lines with UUID + storage URL.
    - Every processed image is exactly 600x750 (script asserts dimensions before upload).
    - Grijalva (-4007) is NOT in the ROSTER and is not re-uploaded.
    - Script committed to C:/EV-Accounts.
  </acceptance_criteria>
  <done>8 House-rep headshots processed at 600x750 and uploaded to the politician_photos bucket; UUID manifest captured.</done>
</task>

<task type="auto">
  <name>Task 2: Write + apply audit-only migration 1284_az_house_headshots.sql</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql — PRIMARY analog: per-rep politician_images INSERT block + header bioguide table + NO footer + NO photo_origin_url
    - .planning/phases/191-arizona-state-federal-government/191-PATTERNS.md — "1284_az_house_headshots.sql" section
    - 191-01-SUMMARY.md is NOT required (this plan is independent); use the Task 1 manifest for UUIDs
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/1284_az_house_headshots.sql` = 8 `politician_images` INSERT blocks (mirror 1051), one per House rep -4001,-4002,-4003,-4004,-4005,-4006,-4008,-4009 (skip -4007). Columns EXACTLY `(id, politician_id, url, type, photo_license)` — NO photo_origin_url (Pitfall 5); type='default'; photo_license='public_domain'; URL uses the captured UUID from Task 1. Guard each block with `WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = ...))`. Header comment: audit-only, apply via psql, NO schema_migrations footer, list bioguide IDs per rep (like 1051's header table). Apply with `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1284_az_house_headshots.sql`. Commit via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (-4001,-4002,-4003,-4004,-4005,-4006,-4007,-4008,-4009) AND pi.url IS NOT NULL)=9 AND (SELECT max(version::int) FROM supabase_migrations.schema_migrations)<>1284"</automated>
  </verify>
  <acceptance_criteria>
    - Verify query returns `t`: all 9 CDs now have a non-null-url politician_images row (8 new + Grijalva's pre-existing), AND the ledger MAX is NOT 1284 (1284 stayed audit-only/unregistered).
    - Every new row has photo_license='public_domain' and type='default'.
    - Migration 1284 committed to C:/EV-Accounts.
  </acceptance_criteria>
  <done>All 9 AZ US House reps have headshots; migration 1284 applied audit-only (ledger unchanged); committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| unitedstates.github.io -> Storage bucket | Third-party public-domain image bytes cross into the public CDN |
| planner-authored SQL -> production DB | Hand-written politician_images INSERTs cross into live data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-06 | Tampering | 1284 INSERT correctness | mitigate | `WHERE NOT EXISTS` per-block guard; post-insert assert = 9 CDs with images; ledger-unchanged assert |
| T-191-07 | Tampering | wrong-photo binding (mismatched bioguide->UUID) | mitigate | Runtime UUID resolved by external_id (not hardcoded); manifest cross-checks CD/name; verify asserts exactly 9 |
| T-191-08 | Repudiation | image licensing | mitigate | photo_license='public_domain' recorded; source is the public-domain unitedstates.github.io congress set |
| T-191-09 | Tampering | accidental Grijalva overwrite | mitigate | -4007 excluded from ROSTER and from the migration; WHERE NOT EXISTS would no-op even if included |
| T-191-SC | Tampering | package installs | accept | No npm/pip installs; PIL/Pillow assumed present (ImageMagick fallback) |
</threat_model>

<verification>
- `psql` audit: all 9 AZ House reps (external_id -4001..-4009) have a non-null-url politician_images row.
- 8 new rows all photo_license='public_domain', type='default'.
- Ledger MAX unchanged by this plan (1284 not registered).
- Senators (Kelly, Gallego) untouched — still have their images.
</verification>

<success_criteria>
- 8 US House headshots sourced (resize-only) from unitedstates.github.io and uploaded at 600x750.
- All 9 CDs headshot-complete (Grijalva pre-existing).
- Migration 1284 applied audit-only and committed to C:/EV-Accounts.
</success_criteria>

<output>
Create `.planning/phases/191-arizona-state-federal-government/191-02-SUMMARY.md` when done. Record the 8 captured UUIDs + storage URLs, any 404-fallbacks used, and confirmation the ledger MAX was unchanged.
</output>
