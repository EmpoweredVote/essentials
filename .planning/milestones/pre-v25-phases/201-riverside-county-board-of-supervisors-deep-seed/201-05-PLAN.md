---
phase: 201-riverside-county-board-of-supervisors-deep-seed
plan: 05
type: execute
wave: 5
depends_on: ["201-04"]
files_modified:
  - src/lib/buildingImages.js
  - src/lib/coverage.js
autonomous: false
requirements: [BANR-01, CV-01]
must_haves:
  truths:
    - "A licensed downtown Riverside / Mission Inn civic street-scene banner (real photo, no AI, no aerial) is processed to 1700×540 and uploaded to Storage at cities/riverside-county.jpg"
    - "buildingImages.js CURATED_LOCAL has a state-scoped 'riverside county' entry pointing at the uploaded banner (second COUNTY-tier entry, after Pima County)"
    - "coverage.js COVERAGE_COUNTIES has a DB-honest 'Riverside County' chip (hasContext:true, since stances landed in Plan 04) that browses geo_id 06065"
    - "The banner is visually distinct from the future Palm Springs (202) and Indio (203) city banners in the same Coachella Valley track"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "CURATED_LOCAL 'riverside county' → uploaded banner URL"
      contains: "riverside county"
    - path: "src/lib/coverage.js"
      provides: "COVERAGE_COUNTIES 'Riverside County' chip, browseGovernmentList ['06065'], hasContext:true"
      contains: "Riverside County"
  key_links:
    - from: "coverage.js COVERAGE_COUNTIES 'Riverside County'"
      to: "buildingImages.js CURATED_LOCAL 'riverside county'"
      via: "browse_label 'Riverside County' → representingCity → getBuildingImages substring match"
      pattern: "riverside county"
    - from: "buildingImages.js 'riverside county'"
      to: "Storage cities/riverside-county.jpg"
      via: "src URL (state-scoped 'CA')"
      pattern: "cities/riverside-county.jpg"
---

<objective>
Give Riverside County its licensed community banner (BANR-01) and its DB-honest coverage chip. Source
a real downtown Riverside / Mission Inn civic street-scene photo ONE AT A TIME (no AI, no aerial —
D-03), process it to the 1700×540 banner spec via `scripts/banners/process_banner.py`, upload to
Storage at `cities/riverside-county.jpg` via `upload_banner.py`, wire it into
`src/lib/buildingImages.js` CURATED_LOCAL as the SECOND county-tier entry (`'riverside county'`, after
Pima County), and surface Riverside County in `src/lib/coverage.js` COVERAGE_COUNTIES with a
`hasContext:true` chip (stances landed in Plan 04).

Purpose: Satisfies ROADMAP success criterion #5 (licensed banner sourced; Riverside County surfaced in
coverage.js with a DB-honest chip) and BANR-01. The banner must read "Riverside County the place" (the
county seat civic scene) and stay visually distinct from the future Palm Springs (202) and Indio (203)
city banners in the same Coachella Valley track.
Output: uploaded `cities/riverside-county.jpg` + edited `buildingImages.js` + `coverage.js` in the
`essentials` repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md

<execution_note>
Frontend files (`src/lib/buildingImages.js`, `src/lib/coverage.js`) live in the CURRENT `essentials`
repo (commit here, not `C:/EV-Accounts`). The banner SOURCING + processing + Storage upload are
ORCHESTRATOR-RUN (executor has no network/Storage access): the inline orchestrator sources the photo
ONE AT A TIME, runs the banner toolchain, and uploads. Per project convention
(`feedback_banner_source_one_at_a_time`, `feedback_banner_prefer_photo_over_gradient`) banner sourcing
is sequential and prefers a real photo — keep re-sourcing rather than falling back to a gradient unless
truly unusable. If there is any risk of visual conflict with the CA state banner, show the state shot
in the review too (`feedback_banner_show_state_shot_on_conflict`). The CDN URL is deterministic
(`cities/riverside-county.jpg`) so the frontend wiring can be authored regardless of upload order.
</execution_note>

<interfaces>
- Banner spec: 1700×540 @ ~3.15:1, LANCZOS, q90 via `scripts/banners/process_banner.py`
  (`--url <src> --output <path> --vertical-anchor 0.3-0.5`; street-scene shots typically need less
  vertical crop than skyline/mountain shots); upload via `scripts/banners/upload_banner.py
  --file <path> --dest cities/riverside-county.jpg` (needs SUPABASE_SERVICE_ROLE_KEY from
  `C:/EV-Accounts/backend/.env`). Destination tier is `cities/` (keyed by banner TYPE, not government
  type — every county banner so far, incl. Pima's `cities/pima-county.jpg`, lives under `cities/`).
- CDN URL: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/riverside-county.jpg
- buildingImages.js CURATED_LOCAL (the existing `'pima county'` entry at ~line 419 is the analog —
  SECOND county-tier key): append `'riverside county': { state: 'CA', src: '<CDN URL>' }` with an
  attribution comment (title | author | license) above it, matching the file's convention.
  getBuildingImages() needs NO code change — its substring match + state-scoping already handle a
  county label. Key is space-form (`'riverside county'`, quoted) to match coverage.js browse_label.
- coverage.js COVERAGE_COUNTIES (append near the existing Pima County / Clark County rows, ~line 261):
  `{ label: 'Riverside County', browseGovernmentList: ['06065'], browseStateAbbrev: 'CA', hasContext: true }`.
  hasContext:true is honest because Plan 04 seeded ≥1 stance row.
- The 'riverside county' banner reliably renders via the COVERAGE_COUNTIES browse chip (browse_label
  'Riverside County' → representingCity); an arbitrary Riverside-County address parses to whatever city
  it is in (e.g. "Riverside"/"Indio"), NOT "Riverside County" — expected pre-existing behavior
  identical to Pima/Clark/Washington County, NOT a defect. Do NOT broaden parseCityFromAddress. No
  existing 'riverside' string in either file — no collision with a hypothetical future City of
  Riverside (out of scope; the two CV-track cities are Palm Springs 202 + Indio 203).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Source + process + upload the Riverside County banner (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (D-03 — downtown Riverside / Mission Inn civic street scene, real licensed photo, no AI/aerial, source one at a time, show candidates before committing; § specifics)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (lines 356-374 — the process_banner.py / upload_banner.py commands + cities/<slug>.jpg destination convention + --vertical-anchor guidance)
    - C:/Transparent Motivations/essentials/scripts/banners/process_banner.py (the banner spec: 1700×540, LANCZOS, q90, --vertical-anchor)
    - C:/Transparent Motivations/essentials/scripts/banners/upload_banner.py (the Storage upload + --dest convention)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. The orchestrator sources a licensed downtown Riverside / Mission Inn street-scene photo ONE AT A TIME, processes it to 1700×540, uploads to cities/riverside-county.jpg, and the operator visually approves the banner before wiring. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A licensed downtown Riverside / Mission Inn civic street-scene banner for Riverside County — the
    SECOND county-tier banner in the app (after Pima County). D-03 fixes the subject as the county-seat
    civic scene (the conventional way to represent the county), kept visually distinct from the future
    Palm Springs (202) and Indio (203) city banners. The orchestrator performs the one-at-a-time
    sourcing pass (commons.wikimedia.org / other licensed sources), processes via process_banner.py
    (1700×540, --vertical-anchor tuned per shot — street scenes usually 0.3-0.5), and uploads via
    upload_banner.py to cities/riverside-county.jpg. Human visual QA is required because "real photo, no
    AI, no aerial, distinct from Palm Springs/Indio" is a judgment call a row-count cannot make.
  </what-built>
  <how-to-verify>
    1. The orchestrator sources ONE candidate photo (licensed CC/public-domain, real photo, NOT
       AI-generated, NOT aerial), records the title | author | license, runs
       `python scripts/banners/process_banner.py --url <src> --output /tmp/riverside-county.jpg
       --vertical-anchor 0.3` (tune anchor per shot), then
       `python scripts/banners/upload_banner.py --file /tmp/riverside-county.jpg --dest cities/riverside-county.jpg`
       (SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
    2. Operator visually confirms: reads as Riverside County the PLACE (downtown Riverside / Mission
       Inn civic street scene), a real photo (no AI artifacts, no aerial/drone shot), and visually
       DISTINCT from the future Palm Springs + Indio city banners (and from the CA state banner — show
       the state shot alongside if there is any risk of conflict).
    3. If unusable, the orchestrator re-sources another candidate ONE AT A TIME (prefer a real photo;
       gradient only if truly nothing usable exists).
  </how-to-verify>
  <acceptance_criteria>
    - `curl -sI https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/riverside-county.jpg` returns HTTP 200.
    - The uploaded image is 1700×540 (banner spec) — PIL/curl-download dimension check.
    - Operator approves: real photo, downtown Riverside / Mission Inn subject, no AI, no aerial, distinct from Palm Springs/Indio + CA-state banners.
    - Title | author | license recorded in 201-05-SUMMARY.md for the attribution comment.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the operator confirms the uploaded banner reads as Riverside County, is a real non-aerial photo, and is distinct from the Palm Springs/Indio/CA-state banners — or describe what to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner into buildingImages.js + surface Riverside County in coverage.js</name>
  <files>src/lib/buildingImages.js, src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (lines 296-350 — the exact CURATED_LOCAL 'riverside county' entry + attribution comment, and the COVERAGE_COUNTIES append; note getBuildingImages needs NO code change; the space-form quoted key rationale)
    - C:/Transparent Motivations/essentials/src/lib/buildingImages.js (the CURATED_LOCAL object + the existing 'pima county' entry at ~line 419 to mirror; getBuildingImages substring/state-scoping logic)
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the COVERAGE_COUNTIES array at ~line 244 + the Pima County / Clark County rows at ~line 260-261 to mirror; the hasContext doc comment)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-05-SUMMARY.md (the banner title | author | license from Task 1 for the attribution comment)
  </read_first>
  <action>
    In `src/lib/buildingImages.js`, append ONE entry to the CURATED_LOCAL object:
    `'riverside county': { state: 'CA', src: '<CDN URL for cities/riverside-county.jpg>' }`, with an
    attribution comment above it (title | author | license from Task 1) matching the file's existing
    convention (mirror the 'pima county' entry). This is the SECOND county-tier CURATED_LOCAL key —
    note in the comment it stays distinct from the future Palm Springs (202) + Indio (203) CITY banners
    in the Coachella Valley track. Make NO change to getBuildingImages() — its substring match +
    state-scoping already handle a county label. Key is quoted space-form (`'riverside county'`) to
    match the coverage.js browse_label.

    In `src/lib/coverage.js`, append ONE entry to COVERAGE_COUNTIES near the other county rows (order
    is cosmetic): `{ label: 'Riverside County', browseGovernmentList: ['06065'], browseStateAbbrev: 'CA', hasContext: true }`.
    hasContext:true is honest because Plan 04 seeded ≥1 stance row (this plan depends on Plan 04). Do
    NOT broaden parseCityFromAddress or add address-parsing logic (out of scope).

    Commit both files to the `essentials` repo via git.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qiF "riverside county" src/lib/buildingImages.js && grep -qF "cities/riverside-county.jpg" src/lib/buildingImages.js && grep -qF "Riverside County" src/lib/coverage.js && grep -qF "'06065'" src/lib/coverage.js && node --input-type=module -e "import('./src/lib/coverage.js').then(()=>console.log('coverage-ok')).catch(e=>{console.error(e);process.exit(1)})" && node --input-type=module -e "import('./src/lib/buildingImages.js').then(()=>console.log('bi-ok')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has a `'riverside county'` key with `state: 'CA'` and src = the cities/riverside-county.jpg CDN URL, plus an attribution comment (title | author | license).
    - coverage.js COVERAGE_COUNTIES has a `{ label: 'Riverside County', browseGovernmentList: ['06065'], browseStateAbbrev: 'CA', hasContext: true }` entry.
    - Both files parse cleanly via `node --input-type=module -e "import(...)"` (exit 0) — the established coverage.js parse-check convention (no dedicated test file exists).
    - getBuildingImages() is unchanged; parseCityFromAddress is unchanged.
    - Both files committed to the `essentials` repo.
  </acceptance_criteria>
  <done>Riverside County banner wired into CURATED_LOCAL and surfaced in COVERAGE_COUNTIES with a DB-honest hasContext:true chip; both frontend files parse and are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wikimedia/licensed source (remote) → banner pipeline | Untrusted remote image bytes enter process_banner.py over HTTP |
| banner pipeline → Storage | Processed banner crosses into the production politician_photos bucket |
| frontend edits → live app | coverage.js / buildingImages.js changes ship to Render on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-BAN | Tampering | remote banner image fetch | mitigate | process_banner.py re-encodes to JPEG (strips embedded payloads); operator visual QA confirms a real non-AI non-aerial photo before wiring |
| T-201-LIC | Repudiation | banner license provenance | mitigate | Title \| author \| license recorded in the SUMMARY + the attribution comment; only CC/public-domain sources used |
| T-201-COVLIE | Tampering | dishonest coverage chip | mitigate | hasContext:true only set because Plan 04 landed ≥1 stance row (depends_on 201-04); node parse-check confirms valid module |
| T-201-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; never in a committed frontend file |
| T-201-SC | Tampering | package installs | accept | No new installs — reuses installed PIL/requests banner toolchain; Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- cities/riverside-county.jpg returns HTTP 200 at 1700×540; operator-approved as a real, distinct downtown Riverside / Mission Inn photo.
- CURATED_LOCAL 'riverside county' → banner URL; COVERAGE_COUNTIES 'Riverside County' chip hasContext:true.
- Both frontend files parse and are committed.
</verification>

<success_criteria>
ROADMAP #5 (licensed banner sourced; Riverside County surfaced in coverage.js with a DB-honest chip)
and BANR-01 are TRUE.
</success_criteria>

<output>
Create `.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-05-SUMMARY.md` when
done — include the banner title | author | license and the CDN URL.
</output>
</content>
