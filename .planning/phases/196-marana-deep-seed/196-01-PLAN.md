---
phase: 196-marana-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1345_town_of_marana.sql
autonomous: false
requirements: [SUB-02]
must_haves:
  truths:
    - "A greenfield 'Town of Marana, Arizona, US' government (type='Town', state='AZ', geo_id='0444270') exists — none exists today (RESEARCH-verified live 2026-07-15: 0 rows for '%marana%')"
    - "One 'Town Council' chamber (official_count=7) holds exactly 7 offices: an at-large Mayor + 6 undifferentiated at-large council members"
    - "TWO NEW essentials.districts rows exist for geo_id='0444270' (mtfcc='G4110', state='az'): ONE LOCAL_EXEC (Mayor) + ONE LOCAL (shared by all 6 council offices) — neither existed before (Phase 190 wrote only the G4110 geofence; districts had 0 rows for 0444270, DB-confirmed)"
    - "The Mayor office links to the LOCAL_EXEC/G4110/0444270 district; ALL 6 council offices link to the SAME single LOCAL/G4110/0444270 district (Torrance/Oro-Valley precedent) — never a bare geo_id join (0444270 collides with the FIPS-keyed geofence_boundaries row)"
    - "Roster is Post(Mayor)/Ziegler(VM)/Cavanaugh/Comerford/Kai/Murphy/Officer; party=NULL on all 7 (nonpartisan); is_appointed=false on all 7 politician rows (A4 convention); the FORMER member Jackie Craig is NOT seeded (she is a 2026 write-in challenger, not a sitting official)"
    - "The Vice Mayor is a TITLE ANNOTATION on Ziegler's council seat (title='Council Member (Vice Mayor)'); the other five council members carry plain 'Council Member'. Exactly 7 offices, no separate Vice Mayor seat"
    - "Post's Mayor seat and Murphy's council seat are held by APPOINTED (not elected) officials — flagged explicitly in the migration header — and the current roster is re-verified against a LIVE source at execute time given the July 21 2026 primary (which may already have occurred by execute time) BEFORE the migration is applied; the structural migration is applied to production and registered in the ledger; 7 politician UUIDs captured for Plans 02/03"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1345_town_of_marana.sql"
      provides: "government + Town Council chamber + 1 NEW LOCAL_EXEC Mayor district + 1 NEW shared LOCAL council district + 7 politicians (party NULL, is_appointed=false) + 7 offices (Ziegler Vice-Mayor-annotated) + office_id backfill + pre-flight geofence assertion + post-verify DO gates + ledger registration"
      contains: "Town Council"
  key_links:
    - from: "essentials.offices (Mayor)"
      to: "essentials.districts (LOCAL_EXEC / G4110 / 0444270)"
      via: "district_id join scoped by district_type='LOCAL_EXEC' AND mtfcc='G4110' AND state='az'"
      pattern: "LOCAL_EXEC"
    - from: "essentials.offices (6 council members)"
      to: "essentials.districts (ONE shared LOCAL / G4110 / 0444270)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='G4110' AND state='az'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'Town Council'"
      to: "essentials.governments 'Town of Marana, Arizona, US'"
      via: "government_id subquery scoped by name"
      pattern: "Town of Marana, Arizona, US"
---

<objective>
Author and apply the structural migration that seeds the Town of Marana as a greenfield town government
(its own `essentials.governments` row at geo_id `0444270`), a single `Town Council` chamber
(`official_count=7`), the TWO district rows the 7 offices attach to, and 7 politicians/offices (ext_id
`-4013001..-4013007`): an at-large **Mayor** (LOCAL_EXEC) plus 6 undifferentiated at-large council
members (all sharing ONE LOCAL district). Greenfield — no Town of Marana government row exists today
(RESEARCH-verified live 2026-07-15: `SELECT ... WHERE name ILIKE '%marana%'` => 0 rows).

CRITICAL DELTA (same as Oro Valley, Pitfall 4): Marana has NO wards and NO council districts — there is
NO geofence loader, NO ArcGIS query, and NO new X00xx mtfcc for this phase. The Town's whole-town
boundary geofence `0444270` (G4110, FIPS 04-44270) is already live from Phase 190, but
`essentials.districts` has ZERO rows for it (DB-confirmed this session). This migration creates exactly
TWO NEW `essentials.districts` rows for `0444270` (`mtfcc='G4110'`, `state='az'` lowercase): ONE
`LOCAL_EXEC` for the Mayor and ONE `LOCAL` shared by ALL 6 council offices (Torrance/Oro-Valley
precedent — not 6 separate rows). No geometry work.

CRITICAL (nonpartisan / Vice Mayor / titles): (1) party=NULL on all 7 — Marana runs NONPARTISAN
elections (confirmed via Ballotpedia's "Marana Town Council At-large" candidate-page naming convention);
do NOT record a party. (2) Council-seat titles are plain `'Council Member'` (Torrance block-voting
shape), NOT Beaverton's `'(Position N)'` numbering — unless the Task 2 checkpoint independently confirms
formal seat numbering. (3) The Vice Mayor (Roxanne Ziegler) is a TITLE ANNOTATION on her council seat
(`title='Council Member (Vice Mayor)'`), NOT a separate office; `role_canonical` stays NULL on all 7.

CRITICAL (the #1 risk of this phase — the tightest research-to-election window in the milestone): Marana
is mid-way through an actively contested 2026 election with a **July 21, 2026 municipal primary** (6
days after the 2026-07-15 research date — the execute date may land ON or AFTER the primary). Compound
succession situation: Mayor Ed Honea died Nov 22, 2024; the council APPOINTED sitting Vice Mayor Jon Post
to the Mayor's office Jan 7, 2025 (Post has never been ELECTED Mayor — the July 21 vote is a SPECIAL
election for the remaining 2 years of Honea's term); Teri Murphy was APPOINTED to fill Post's vacated
council seat; and Patti Comerford's seat is guaranteed to change hands (she is confirmed NOT seeking
re-election — an open seat). RESEARCH also caught a stale WebSearch composite mislabeling FORMER member
Jackie Craig (served 2020–2024, now a 2026 write-in CHALLENGER) as a current "incumbent" — Craig must
NOT be seeded (directly analogous to Oro Valley's Bohen/Solomon trap). Task 2 is a SUBSTANTIVE,
non-perfunctory BLOCKING roster-currency re-verification against a live source BEFORE apply — a genuine
decision point that must assume the primary may have ALREADY happened, not merely a copy-paste formality.

Purpose: Satisfies the government/roster half of SUB-02 and ROADMAP success criterion #1 (govt + council
roster seeded, election method verified). The existing G4110→(LOCAL,LOCAL_EXEC) routing map in
`essentialsService.ts` covers both the Mayor and the 6 at-large council members with NO code change, so
applying this migration makes every Marana address resolve to the at-large Mayor + all 6 council members.
Plans 02 (headshots) and 03 (stances) consume the 7 politician UUIDs captured here.
Output: `C:/EV-Accounts/backend/migrations/1345_town_of_marana.sql` (structural, registered), applied to
production, with the 7 UUIDs recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/196-marana-deep-seed/196-RESEARCH.md
@.planning/phases/196-marana-deep-seed/196-PATTERNS.md
@.planning/phases/196-marana-deep-seed/196-CONTEXT.md
@.planning/phases/196-marana-deep-seed/196-VALIDATION.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB access. Task 1 (author the `.sql` file) is the only executor
file action. Task 2 (SUBSTANTIVE BLOCKING roster-currency re-verify + apply + UUID capture) and Task 3
(post-apply assertions) are ORCHESTRATOR-RUN via `psql -f`/`psql -tAc` against
`C:/EV-Accounts/backend/.env` `DATABASE_URL`. A migration authored but not applied is a false positive —
the apply MUST happen (and its post-verify DO gate MUST pass) before Plans 02/03. All backend files live
in the SEPARATE `C:/EV-Accounts` repo (`master`→Render) — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (live-verified in 196-RESEARCH.md / 196-PATTERNS.md; the Oro Valley 1305 file is the
copy-verbatim-with-substitutions analog — no codebase exploration needed):
- essentials.governments (id, name, type, state, city, geo_id): insert 'Town of Marana, Arizona, US',
  type='Town' (per RESEARCH Architecture diagram; confirm this matches the governments.type convention
  used by the live Oro Valley 1305 row at author time and note any deviation), state='AZ' (UPPERCASE
  free-text), city='Marana', geo_id='0444270'. Greenfield. Idempotent WHERE NOT EXISTS on name.
- essentials.chambers (id, name, name_formal, government_id, official_count): name='Town Council',
  name_formal='Marana Town Council', official_count=7 (Mayor + 6 council), government_id via name
  subquery. The auto-generated `slug` column must NEVER appear in the INSERT column list. Idempotent
  WHERE NOT EXISTS on (name, government_id).
- essentials.districts (id, district_type, state, geo_id, label, mtfcc) — TWO NEW rows, BOTH reusing
  the ALREADY-LIVE Phase-190 geofence (NO geometry work):
  (1) 1 row district_type='LOCAL_EXEC', state='az' (lowercase), geo_id='0444270',
      label='Town of Marana (Mayor)', mtfcc='G4110'. Idempotent WHERE NOT EXISTS on
      (geo_id, district_type, state).
  (2) 1 row district_type='LOCAL', state='az' (lowercase), geo_id='0444270',
      label='Town of Marana (At-Large)', mtfcc='G4110'. Idempotent WHERE NOT EXISTS on
      (geo_id, district_type, state). ALL 6 council offices share this SINGLE row (Torrance precedent).
  Use `label`, NOT name_formal (no such column). mtfcc='G4110' on BOTH rows.
- essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed,
  is_vacant, is_incumbent, external_id, office_id): ext_id -4013001..-4013007 (DB-confirmed unused
  2026-07-15); external_id UNIQUE (ON CONFLICT (external_id) DO UPDATE SET is_active=EXCLUDED.is_active).
  party=NULL on ALL 7 (nonpartisan — do NOT record a party value). is_active=true, is_incumbent=true,
  is_vacant=false, is_appointed=false on all 7 (A4: `is_appointed` is reserved for STATE_EXEC-tier
  constitutional officers per prior-phase convention — it is NOT used to flag Post's/Murphy's
  council-appointed status; the appointed-not-elected nuance is documented in the migration HEADER
  comment instead, per RESEARCH's explicit recommendation).
- essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state,
  is_appointed_position, is_vacant, role_canonical): representing_state='AZ' (UPPERCASE),
  is_appointed_position=false, role_canonical=NULL on all 7. Guard on (district_id, politician_id).
- ext_id → seat → title (RESEARCH roster, 2026-07-15 — RE-VERIFY at Task 2, primary is imminent/past):
    -4013001 Jon Post           Mayor           title='Mayor'                        (LOCAL_EXEC / G4110 / 0444270)  [APPOINTED Jan 2025, not elected]
    -4013002 Roxanne Ziegler    Council (VM)    title='Council Member (Vice Mayor)'  (LOCAL / G4110 / 0444270)
    -4013003 Patrick Cavanaugh  Council         title='Council Member'               (LOCAL / G4110 / 0444270)
    -4013004 Patti Comerford    Council         title='Council Member'               (LOCAL / G4110 / 0444270)  [open seat — NOT seeking re-election; confirm still sitting at Task 2]
    -4013005 Herb Kai           Council         title='Council Member'               (LOCAL / G4110 / 0444270)
    -4013006 Teri Murphy        Council         title='Council Member'               (LOCAL / G4110 / 0444270)  [APPOINTED Jan 2025, not yet elected]
    -4013007 John Officer       Council         title='Council Member'               (LOCAL / G4110 / 0444270)
- CRITICAL geo_id collision guard: 0444270 also appears in geofence_boundaries (state stored as FIPS
  '04', an unrelated convention). Every office↔district join MUST scope by district_type + mtfcc +
  state='az', never a bare geo_id. Mayor join scopes LOCAL_EXEC/G4110/az; each council join scopes
  LOCAL/G4110/az.
- One-chamber-two-district-types precedent (live-applied at 1305 Oro Valley + Torrance): an at-large
  LOCAL_EXEC Mayor + 6 undifferentiated LOCAL 'Council Member' offices ALL sharing ONE LOCAL row in a
  single chamber. Copy that shape, not Beaverton's numbered titles.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the structural Town of Marana migration</name>
  <files>C:/EV-Accounts/backend/migrations/1345_town_of_marana.sql</files>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-PATTERNS.md (lines 21-150 — the full structural pattern: the Oro-Valley→Marana substitution table, header/warning-comment discipline incl. the Marana-specific "Post/Murphy appointed-not-elected" CRITICAL note, pre-flight hard-abort guards, government/chamber INSERT, the TWO NEW district rows with mtfcc='G4110', the WITH ins_p...INSERT INTO offices per-official block, the plain 'Council Member' titles, the Vice Mayor annotation on Ziegler, office_id backfill, the 7 post-verify DO gates, and the ledger footer)
    - .planning/phases/196-marana-deep-seed/196-RESEARCH.md (§ Summary roster table; § Architecture Patterns 1 & 2; § Anti-Patterns to Avoid — Post/Murphy appointed, Jackie Craig NOT current, nonpartisan; § Common Pitfalls 2, 4, 6, 7; § Code Examples — the greenfield/geofence/ext_id/ledger probes to re-run)
    - C:/EV-Accounts/backend/migrations/1305_town_of_oro_valley.sql (PRIMARY analog — read in full: government/chamber/district/office INSERT shapes, pre-flight + post-verify DO gates, the office_id backfill, ledger footer; copy the file STRUCTURE verbatim, substituting every Oro Valley fact for its Marana equivalent per the PATTERNS.md substitution table)
    - C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql (read only the '(Chair)' title-annotation post-verify gate — replicate that gate shape for '(Vice Mayor)' on Ziegler's -4013002 seat, asserting exactly 1 matching office belonging to that external_id)
  </read_first>
  <action>
    First run `ls C:/EV-Accounts/backend/migrations | sort -n | tail -5` to confirm on-disk MAX (RESEARCH
    computed disk MAX=1344, ledger MAX=1338, next structural=1345 on 2026-07-15 — but Pitfall 6 documents
    a real same-day collision risk, so RE-CHECK now). Name this file actual-disk-MAX+1 (provisionally
    `1345_town_of_marana.sql`). If disk MAX drifted, use actual disk-MAX+1 and note the deviation in a
    header comment and the SUMMARY.

    Author a STRUCTURAL migration wrapped in BEGIN/COMMIT. Header comment discipline (copy 1305's shape,
    substitute Marana facts): the geo_id-collision CRITICAL note with `'0444270'`; the districts.state
    must be 'az' lowercase note; governments.state='AZ' / offices.representing_state='AZ' uppercase note;
    district_type='LOCAL_EXEC' for Mayor / 'LOCAL' for council note; at-large / one shared LOCAL row note;
    party=NULL nonpartisan note; PLUS a Marana-specific CRITICAL/DEVIATION note that Jon Post's Mayor seat
    (-4013001) and Teri Murphy's council seat (-4013006) are currently held by APPOINTED, not elected,
    officials (Post appointed Jan 7 2025 after Mayor Honea's death; Murphy appointed Jan 2025 to Post's
    vacated seat), and that `is_appointed` stays false on both politician rows per the A4 convention
    (is_appointed reserved for STATE_EXEC constitutional officers) — the appointed status is documented in
    this header, not via the flag.

    Pre-flight DO block: assert the whole-town geofence exists — COUNT(*) >= 1 in
    essentials.geofence_boundaries WHERE geo_id='0444270' AND mtfcc='G4110' — RAISE EXCEPTION 'Marana
    G4110 geofence missing — run Phase 190 first' otherwise (codifies Pitfall 4). Add the standard
    idempotency hard-abort guard: RAISE EXCEPTION 'Migration 1345 already applied — aborting re-run' if
    the 'Town of Marana, Arizona, US' government already exists.

    Insert the greenfield government 'Town of Marana, Arizona, US' (type='Town', state='AZ' uppercase,
    city='Marana', geo_id='0444270') idempotently (WHERE NOT EXISTS on name). Confirm the governments.type
    value against the live Oro Valley 1305 row convention at author time (RESEARCH diagram calls for
    'Town'; note any deviation in a comment). Insert the 'Town Council' chamber (name_formal='Marana Town
    Council', official_count=7) scoped to that government's id via name subquery, idempotent on
    (name, government_id). Do NOT list the auto-generated `slug` column.

    Insert TWO NEW district rows, BOTH reusing the existing Phase-190 geofence (NO geometry work):
      (1) ONE LOCAL_EXEC Mayor row (district_type='LOCAL_EXEC', state='az' lowercase, geo_id='0444270',
          label='Town of Marana (Mayor)', mtfcc='G4110'), idempotent WHERE NOT EXISTS on
          (geo_id, district_type, state).
      (2) ONE LOCAL at-large row (district_type='LOCAL', state='az' lowercase, geo_id='0444270',
          label='Town of Marana (At-Large)', mtfcc='G4110'), idempotent WHERE NOT EXISTS on
          (geo_id, district_type, state). ALL 6 council offices share THIS SINGLE row.
    CRITICAL: mtfcc='G4110' on BOTH rows. Pitfall 4: without these rows the offices attach to nothing.

    Seed 7 officials using the WITH ins_p AS (INSERT INTO politicians ... RETURNING id) INSERT INTO
    offices ... FROM districts d CROSS JOIN ins_p p WHERE ... block, one per official. Scoping:
      - Mayor Post (-4013001) → title='Mayor'; office joins the LOCAL_EXEC row:
        d.geo_id='0444270' AND d.district_type='LOCAL_EXEC' AND d.state='az' AND d.mtfcc='G4110'.
      - Ziegler (-4013002) → title='Council Member (Vice Mayor)'  ← Vice Mayor title annotation; office
        joins d.geo_id='0444270' AND d.district_type='LOCAL' AND d.state='az' AND d.mtfcc='G4110'.
      - Cavanaugh (-4013003) → 'Council Member' (same LOCAL join)
      - Comerford (-4013004) → 'Council Member' (same LOCAL join)
      - Kai (-4013005) → 'Council Member' (same LOCAL join)
      - Murphy (-4013006) → 'Council Member' (same LOCAL join)
      - Officer (-4013007) → 'Council Member' (same LOCAL join)
    All 6 council offices join the SAME single LOCAL/G4110/0444270 row (Torrance precedent). Each join
    scopes district_type + mtfcc + state='az' plus geo_id — NEVER a bare geo_id. All 7 offices:
    representing_state='AZ' uppercase, is_appointed_position=false, role_canonical=NULL. Only Ziegler's
    office carries the `(Vice Mayor)` marker — the other five council members and the Mayor carry NO
    `(Vice Mayor)` suffix, and the five plain council seats use the exact title 'Council Member' (NOT
    '(Position N)' — Torrance shape). Guard each office INSERT on NOT EXISTS (... o.district_id=d.id AND
    o.politician_id=p.id). Politicians: party=NULL on ALL 7 (nonpartisan — do NOT record a party value),
    is_active=true, is_incumbent=true, is_vacant=false, is_appointed=false on all 7. Do NOT seed Jackie
    Craig (former member 2020–2024, a 2026 write-in challenger — RESEARCH Pitfall 3).

    office_id backfill: UPDATE politicians SET office_id=o.id FROM offices o WHERE o.politician_id=p.id
    AND p.external_id IN (-4013001,-4013002,-4013003,-4013004,-4013005,-4013006,-4013007) AND
    p.office_id IS NULL.

    Post-verify DO gate (RAISE EXCEPTION → rollback unless ALL hold): (a) exactly 1 government named
    'Town of Marana, Arizona, US'; (b) exactly 7 offices under the 'Town Council' chamber — 1 on the
    LOCAL_EXEC/G4110/0444270 district and 6 on the ONE LOCAL/G4110/0444270 district; (c) the LOCAL_EXEC
    district holds exactly 1 office AND the LOCAL district holds exactly 6 offices; (d) all 7 politicians
    have party IS NULL (nonpartisan gate); (e) EXACTLY 1 office carries a Vice-Mayor annotation
    (o.title LIKE '%(Vice Mayor)%') AND that office's politician has external_id=-4013002 (Ziegler) — no
    other office may carry it; (f) section-split: 0 offices attached under any government OTHER than Town
    of Marana via these 2 districts; (g) office_id backfill: 0 NULLs remaining across the 7 external_ids.

    Register OUTSIDE BEGIN/COMMIT: INSERT INTO supabase_migrations.schema_migrations (version) VALUES
    ('1345') ON CONFLICT (version) DO NOTHING (substitute actual disk-MAX+1). Do NOT invent politician
    UUIDs — offices resolve politician_id via the ins_p CTE at apply time.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1345_town_of_marana.sql"; test -f "$F" && grep -qF "Town Council" "$F" && grep -qF "Town of Marana, Arizona, US" "$F" && grep -qF "'LOCAL_EXEC'" "$F" && grep -qF "G4110" "$F" && grep -qF "'0444270'" "$F" && grep -qF "Council Member (Vice Mayor)" "$F" && [ "$(grep -c "'Council Member'" "$F")" -ge 5 ] && ! grep -qF "Council Member (Position" "$F" && ! grep -qiE "party[^a-z]*=[^a-z]*'(Democratic|Republican)" "$F" && ! grep -qi "Jackie Craig" "$F" && [ "$(grep -c 'o.district_id = d.id AND o.politician_id = p.id' "$F")" = "7" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1345; deviation noted if disk MAX drifted).
    - Grep confirms `Town Council` and `Town of Marana, Arizona, US` present; government type='Town' (or noted deviation), city='Marana', geo_id='0444270'.
    - Grep confirms a NEW `LOCAL_EXEC` districts INSERT with geo_id='0444270', mtfcc='G4110', state='az' AND a NEW `LOCAL` districts INSERT with geo_id='0444270', mtfcc='G4110', state='az' (Pitfall 4 — the two rows Phase 190 never wrote; mtfcc='G4110' NOT NULL).
    - Grep count of the office↔district guard `o.district_id = d.id AND o.politician_id = p.id` is exactly 7 (no 8th office).
    - Mayor office joins LOCAL_EXEC/G4110/0444270; all 6 council offices join the SAME LOCAL/G4110/0444270 row — never a bare geo_id.
    - Nonpartisan: NO office title contains `(Position` (plain 'Council Member', Torrance shape); NO politician row records a 'Democratic'/'Republican' party; `role_canonical` NULL for all 7; is_appointed=false on all 7 politician rows.
    - Vice Mayor modeling: Ziegler's office title='Council Member (Vice Mayor)'; the other five council seats + Mayor carry NO `(Vice Mayor)` marker.
    - Header carries the Marana-specific CRITICAL note that Post (-4013001) + Murphy (-4013006) are appointed-not-elected; Jackie Craig is NOT present anywhere in the file (grep -i finds nothing).
    - Grep confirms 7 external_id literals -4013001..-4013007.
    - Pre-flight DO block asserts the G4110/0444270 geofence exists; post-verify DO gate present with assertions (a-g) — including LOCAL district holds exactly 6, party-NULL gate, and exactly-one-Vice-Mayor on -4013002.
    - Ledger-registration line `('1345'` (or actual number) present OUTSIDE COMMIT.
  </acceptance_criteria>
  <done>Structural migration authored: greenfield Town of Marana government + Town Council chamber + 1 NEW LOCAL_EXEC/G4110 Mayor district + 1 NEW shared LOCAL/G4110 council district + 7 offices/politicians (party NULL, is_appointed=false; Ziegler carries the Vice Mayor title annotation; five plain 'Council Member'; Jackie Craig not seeded; Post/Murphy appointed-status flagged in header), backfill, pre-flight geofence + post-verify gates, ledger registration — ready to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] SUBSTANTIVE roster-currency re-verification (July 21 2026 primary — may already be past) + apply structural migration (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-RESEARCH.md (§ Summary — the full succession narrative + the confirmed roster table; § Common Pitfall 1 (6-day window), Pitfall 2 (Post/Murphy appointed), Pitfall 3 (Jackie Craig mislabeled incumbent); § Assumptions Log A1/A4; § Open Question 3 — how to handle a primary that has already landed)
    - .planning/phases/196-marana-deep-seed/196-VALIDATION.md (Manual-Only Verifications — the roster-currency test instructions: BLOCKING re-verify vs marana.gov/live source before apply)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. This is NOT a copy-paste formality — it is the genuine #1 risk of this phase and the shortest research-to-election window in the whole v22.0 milestone. The operator re-confirms the roster against a LIVE source, determines whether the July 21 2026 primary has occurred AND been certified, and — if it has changed any officeholder — explicitly decides with the operator whether to seed the outgoing or incoming officeholder for each affected seat; only then does the ORCHESTRATOR apply migration 1345 (its in-transaction post-verify DO gate MUST pass — a RAISE EXCEPTION rolls the whole thing back) and capture the 7 UUIDs. See &lt;how-to-verify&gt;.</action>
  <what-built>
    Migration 1345 (Task 1) is authored but NOT applied. RESEARCH (2026-07-15) documented the current
    7-person roster (Post Mayor [APPOINTED Jan 2025]; Ziegler VM; Cavanaugh; Comerford [open seat, not
    seeking re-election]; Kai; Murphy [APPOINTED Jan 2025]; Officer) as valid for effectively 0–6 days
    because Marana's July 21, 2026 municipal primary is 6 days from the research date. Two of the 7 seats
    are held by APPOINTED officials, and Comerford's seat is guaranteed to change hands. RESEARCH also
    caught a stale WebSearch composite mislabeling FORMER member Jackie Craig (2020–2024, now a 2026
    write-in challenger) as a current "incumbent" — Craig must NOT be seeded. Applying to production
    (writes are LIVE prod) without a fresh re-check risks seeding a roster that a certified primary has
    already superseded (T-196-04 data-honesty threat).
  </what-built>
  <how-to-verify>
    1. Establish the authoritative election facts from a LIVE source — the official marana.gov / Town
       Clerk elections page is Akamai-WAF-blocked (Pitfall 5), so use a fresh Playwright fetch
       (mcp__playwright__browser_navigate) OR a fresh news/civic search of the confirmed-reachable
       Tucson-metro outlets (tucsonlocalmedia.com/marana/, tucson.com, AZPM, KGUN9, Tucson Sentinel, AZ
       Luminaria) plus Ballotpedia candidate pages. Confirm the July 21, 2026 primary date and Nov 4, 2026
       general.
    2. Determine whether the primary has OCCURRED and whether results are CERTIFIED as of the execute date.
       An AZ nonpartisan primary can produce an outright >50% winner (no Nov general needed). Given the
       6-day window, treat "the primary has already happened" as the LIKELY branch, not a remote one.
    3. Decide the roster to seed WITH THE OPERATOR:
       - If the primary has NOT yet occurred / is not certified → seed the current 7-person roster
         (Post/Ziegler/Cavanaugh/Comerford/Kai/Murphy/Officer) documented in RESEARCH — these are who
         represents residents today.
       - If a primary result IS certified and has changed any officeholder (Comerford's open seat WILL
         resolve to someone new regardless) → explicitly decide with the operator whether to seed the
         outgoing (still-sitting until the term rolls over) or the newly-certified incoming officeholder
         for each affected seat, and patch the affected politician rows in 1345 BEFORE apply. Never
         silently assume "no change since research." NEVER seed Jackie Craig as a current officeholder.
    4. Confirm the seat-title convention: default is plain 'Council Member' (Torrance block-voting, Open
       Question 1). If a live source shows Marana formally numbers its seats, switch the five plain titles
       to the Beaverton '(Position N)' convention before apply (all still share the ONE LOCAL district
       row). Confirm Ziegler is still the sitting Vice Mayor (annotation on her -4013002 seat); if the VM
       changed, move the '(Vice Mayor)' suffix to the correct sitting member and update the post-verify
       gate's expected external_id.
    5. On approval, the ORCHESTRATOR applies via
       `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1345_town_of_marana.sql`, confirms the
       in-transaction post-verify DO gate passed (COMMIT, no RAISE EXCEPTION), captures the 7 UUIDs
       (`SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id IN
       (-4013001,-4013002,-4013003,-4013004,-4013005,-4013006,-4013007) ORDER BY external_id`) into
       196-01-SUMMARY.md for Plans 02/03, records the execute-date roster decision (before/after primary,
       what was seeded and why, per-seat), and commits via `git -C "C:/EV-Accounts"`.
  </action>
  <acceptance_criteria>
    - The authoritative primary date + general date + certification status are established from a LIVE source at execute time (not assumed from RESEARCH), and recorded in 196-01-SUMMARY.md.
    - The seeded roster is explicitly decided with the operator: current 7 if pre/uncertified primary, or the operator-chosen outgoing/incoming set per seat if a certified result changed anything (Comerford's open seat resolved deliberately). Jackie Craig NOT seeded.
    - Post's and Murphy's appointed-not-elected status is acknowledged in the SUMMARY; seat-title convention confirmed (plain 'Council Member' default, or '(Position N)' if a live source shows formal numbering); Ziegler confirmed as sitting Vice Mayor (or the annotation moved to the correct member + the gate updated).
    - Migration applies with the in-transaction post-verify DO gate passing (no RAISE EXCEPTION → COMMIT), including party-NULL, LOCAL-holds-6, and exactly-one-Vice-Mayor on the confirmed holder.
    - `psql -tAc "SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1345'"` returns 1 (structural registered; substitute actual number if disk MAX drifted).
    - The 7 politician UUIDs captured into 196-01-SUMMARY.md (external_id → UUID → full_name) for Plans 02/03; migration committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live roster + election dates + certification status are confirmed, the seed roster is explicitly decided per seat (outgoing vs incoming if the primary landed), and the migration is applied + gate-passed + UUIDs captured — or describe the roster/title/Vice-Mayor delta to patch first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply structural assertions (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB assertions, no files modified</files>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-VALIDATION.md (Per-Task Verification Map + Wave 0 requirements — geo_id 0444270 resolves; greenfield; 1 govt + 1 chamber + 7 offices, party NULL, Vice Mayor annotation, scoped joins, 0 section-split)
    - .planning/phases/196-marana-deep-seed/196-RESEARCH.md (§ Common Pitfall 4 — the two NEW districts rows; § Security Domain — section-split + wrong-district-attach mitigations)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). After apply, run seed assertions via
    `psql "$DATABASE_URL" -tAc` and record in 196-01-SUMMARY.md. Assert: (a) exactly 1 government
    'Town of Marana, Arizona, US' at geo_id='0444270'; (b) exactly 7 offices under the 'Town Council'
    chamber of that government — 1 on the LOCAL_EXEC/G4110/0444270 district and 6 on the ONE
    LOCAL/G4110/0444270 district; (c) the LOCAL_EXEC district holds exactly 1 office AND the LOCAL
    district holds exactly 6 offices; (d) both new districts rows exist (LOCAL_EXEC + LOCAL,
    geo_id='0444270', mtfcc='G4110', state='az'); (e) all 7 politicians have party IS NULL AND
    is_appointed=false; (f) DISTINCT district state on the 2 linked districts = only 'az' (lowercase);
    (g) section-split: 0 offices reachable from these 2 districts under any government other than Town of
    Marana; (h) Vice Mayor annotation: exactly 1 of the 7 offices has title LIKE '%(Vice Mayor)%' AND it
    is the office whose politician external_id=-4013002 (Ziegler, or the confirmed sitting VM from Task
    2), and no other office carries it. Any mismatch blocks the phase — do NOT proceed to Plan 02.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='Town of Marana, Arizona, US' AND geo_id='0444270')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0444270' AND d.state='az' AND d.mtfcc='G4110' AND d.district_type IN ('LOCAL','LOCAL_EXEC'))=7 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0444270' AND d.district_type='LOCAL_EXEC' AND d.state='az' AND d.mtfcc='G4110')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0444270' AND d.district_type='LOCAL' AND d.state='az' AND d.mtfcc='G4110')=6 AND NOT EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id IN (-4013001,-4013002,-4013003,-4013004,-4013005,-4013006,-4013007) AND (party IS NOT NULL OR is_appointed IS TRUE)) AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='0444270' AND d.mtfcc='G4110' AND d.state='az' AND o.title LIKE '%(Vice Mayor)%')=1"</automated>
  </verify>
  <acceptance_criteria>
    - Greenfield Town of Marana government count = 1 (geo_id 0444270).
    - 7 offices under 'Town Council' — 1 on LOCAL_EXEC/G4110/0444270, 6 on the ONE LOCAL/G4110/0444270 row.
    - LOCAL_EXEC district holds exactly 1 office; LOCAL district holds exactly 6 offices.
    - Both new districts rows (LOCAL_EXEC + LOCAL, 0444270/G4110/az) exist (Pitfall 4).
    - party IS NULL AND is_appointed=false for all 7 politicians (nonpartisan; A4 convention).
    - DISTINCT linked-district state = only 'az'.
    - Section-split query returns 0 rows (no office leaked under a non-Marana government).
    - Vice Mayor annotation: exactly 1 office carries `(Vice Mayor)` and it is Ziegler's (-4013002) seat (or the Task-2-confirmed sitting VM).
    - The combined boolean SELECT returns `t`.
  </acceptance_criteria>
  <done>Live production confirms the greenfield Town of Marana government, 7 offices (at-large Mayor on the new LOCAL_EXEC district + 6 council members on the ONE shared LOCAL district), party NULL / is_appointed false, the Vice Mayor title annotation on Ziegler's seat only, lowercase 'az' casing, clean section-split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | Hand-authored static seed SQL crosses into live `essentials` via `psql -f` (service-role) |
| Phase-190 G4110/0444270 geofence → new offices | The 2 new office↔district joins consume the existing whole-town geofence; wrong casing/type or a bare-geo_id collision (0444270 also exists FIPS-keyed) silently mislinks |
| active 2026 election → seeded roster | The July 21 2026 primary (6 days from research; likely already past at execute) can supersede the roster; 2 seats appointed, 1 open |

## STRIDE Threat Register

Honest scope: this is a static data-seed migration authored by the operator — no new untrusted input,
auth, or network endpoint. The genuine threats are data-integrity (correct linkage / current roster),
not injection or access-control. No HIGH-severity threat exists; none is fabricated.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-01 | Tampering | office↔chamber↔government section-split | mitigate | Chamber scoped to the Marana government id subquery; post-verify DO gate (f) + Task 3 section-split assertion → 0 rows or the transaction rolls back |
| T-196-02 | Tampering | office↔district WHERE vs the 0444270 multi-use geo_id | mitigate | Every join scopes district_type + mtfcc='G4110' + state='az' (Mayor: LOCAL_EXEC; council: the ONE LOCAL row); migration never joins a bare geo_id; post-verify asserts 1 LOCAL_EXEC + 6 LOCAL offices on the correct rows |
| T-196-03 | Tampering | missing LOCAL/LOCAL_EXEC districts rows (Pitfall 4) | mitigate | Migration creates BOTH new rows (0444270/G4110/az); pre-flight asserts the geofence exists; post-verify gate (b/c) + Task 3 assert both rows exist and hold exactly the Mayor / 6 council offices |
| T-196-04 | Tampering | seeding a roster superseded by the July 21 2026 primary (data-honesty) — the #1 risk | mitigate | Task 2 is a SUBSTANTIVE BLOCKING roster-currency re-verify against a LIVE source + certified-primary check (assumes primary may already be past) + explicit per-seat outgoing/incoming decision with the operator; FORMER member Jackie Craig never seeded; execute-date roster decision recorded in the SUMMARY |
| T-196-VM | Tampering | Vice Mayor modeling (double-count / mislabel / stale annotation) | mitigate | Title-annotation model: exactly 7 offices (no separate 8th seat), the `(Vice Mayor)` suffix on Ziegler's -4013002 seat ONLY (post-verify gate (e) + Task 3 assert exactly one); Task 2 re-confirms the sitting VM before apply |
| T-196-PARTY | Information disclosure | recording a party on a nonpartisan office | mitigate | party=NULL on all 7 (antipartisan; Ballotpedia At-large naming confirms nonpartisan); post-verify gate (d) + Task 3 assert 0 non-NULL party |
| T-196-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `C:/EV-Accounts/backend/.env`; applied by the inline orchestrator; never hardcoded in the migration |
| T-196-SC | Tampering | package installs | accept | No new npm/pip/cargo installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 1 greenfield Town of Marana government (geo_id 0444270).
- 1 'Town Council' chamber; 7 offices — at-large Mayor on a NEW LOCAL_EXEC/G4110 district + 6 council members on ONE shared NEW LOCAL/G4110 district.
- party NULL / is_appointed false on all 7; Vice Mayor = title annotation on Ziegler's seat; exactly one `(Vice Mayor)`; no separate 8th office; Jackie Craig not seeded.
- Section-split 0 rows; casing lowercase 'az' on districts; structural migration registered.
- Roster re-verified against a live source at execute time (July 21 primary decision recorded); 7 politician UUIDs captured for Plans 02/03.
</verification>

<success_criteria>
The government/roster half of SUB-02 and ROADMAP #1 is TRUE in production (Mayor + 6 at-large council
members, election method verified, at-large routing enabled via the two new districts rows); the
migration is applied + registered; the UUID manifest and execute-date roster decision (July 21 primary
branch) are recorded.
</success_criteria>

<output>
Create `.planning/phases/196-marana-deep-seed/196-01-SUMMARY.md` when done — include the 7 external_id →
UUID → full_name manifest (required by Plans 02 and 03), the authoritative primary/general dates +
certification status established at execute time, the explicit per-seat roster decision (current vs
outgoing/incoming, esp. Comerford's open seat), Post's/Murphy's appointed status, and record Ziegler as
Vice Mayor surfaced via the `title` annotation.
</output>
