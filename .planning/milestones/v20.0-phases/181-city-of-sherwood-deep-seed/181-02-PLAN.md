---
phase: 181-city-of-sherwood-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["181-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1187_sherwood_city_council.sql
autonomous: false
requirements: [WASH-07]

must_haves:
  truths:
    - "One 'City of Sherwood, Oregon, US' government row exists (geo_id 4167100 — CORRECTED, not the stated 4167450)"
    - "One 'City Council' chamber exists under it with official_count=7 (name_formal='Sherwood City Council')"
    - "Exactly one LOCAL_EXEC district and one LOCAL district exist on geo_id 4167100, state='or' lowercase"
    - "7 offices exist (Mayor + 6 Councilors), each with representing_city='Sherwood' set INLINE"
    - "Office titles are PLAIN 'Mayor' (LOCAL_EXEC) and 'Councilor' (each of the 6 LOCAL seats) — Tigard/Forest Grove convention, NO numbered positions, NO wards"
    - "ALL 7 politicians carry is_appointed=false + all 7 offices carry is_appointed_position=false (uniform — zero appointed seats, Forest Grove/Tualatin shape, NOT Tigard's appointed-seat block)"
    - "The Mayor's district label/comment records the 2-YEAR term (new this milestone; structural shape unchanged from the LOCAL_EXEC+LOCAL split)"
    - "All 7 politicians have office_id back-filled (no NULL office_id)"
    - "Section-split scan returns 0 rows for geo_id 4167100 (via the independent canonical GROUP BY/HAVING query, not the dead same-transaction gate)"
    - "The post-verify block includes an INDEPENDENT geofence-presence assertion for geo_id 4167100 AND mtfcc G4110"
    - "The post-verify block includes the D-15 WR-B PAIRWISE identity gate: it asserts exactly 7 (external_id, full_name) pairs match the researched roster — upgraded from Forest Grove/Tigard's set-membership two-IN-list gate, so a name/id transposition cannot pass"
    - "NO ward/X00xx geofences or districts were created (pure at-large model)"
    - "Kim Young is one Councilor office (Council President title-on-seat), not a separate office row; Keith Mays is a PLAIN Councilor (former Mayor/Council-President history is NOT attributed to his current seat)"
    - "Decision coverage — D-03: ward branch N/A (at-large fired; seat identity on office titles, no duplicate district rows); D-04: no section-split and no empty LOCAL section; D-06: Young's Council President designation is title-on-seat, no separate office row; D-11: representing_city='Sherwood' set INLINE (no backfill); D-15 WR-B: pairwise identity gate in the structural post-verify"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1187_sherwood_city_council.sql"
      provides: "Structural seed: government + chamber + 2 districts + 7 offices + office_id backfill + post-verify DO block (independent geofence assertion + section-split + WR-B pairwise identity gate) + ledger row"
      min_lines: 250
      contains: "City of Sherwood, Oregon, US"
  key_links:
    - from: "offices"
      to: "districts (geo_id 4167100)"
      via: "district_id FK on LOCAL_EXEC + LOCAL rows"
      pattern: "geo_id = '4167100'"
    - from: "politicians.office_id"
      to: "offices.id"
      via: "back-fill UPDATE keyed on external_id IN (-4167101..-4167107)"
      pattern: "office_id = o.id"
---

<objective>
Author the single structural migration that seeds the City of Sherwood government, City Council
chamber, two citywide districts (LOCAL_EXEC for the Mayor + LOCAL at-large for all 6 Councilors), and
the 7 officials. Sherwood is the exact Forest Grove (180) shape class: directly-elected Mayor + shared
at-large council, ZERO appointed seats, PLAIN 'Mayor'/'Councilor' titles (NO numbered positions, NO
wards). The one new wrinkle is the Mayor's 2-YEAR term (every other WashCo Mayor is 4-year) — record
it in the district label/comment; it does NOT change the structural shape. Set
`representing_city='Sherwood'` INLINE on every office INSERT so the community banner derives correctly
at first browse (no backfill migration). Upgrade the post-verify identity gate to the D-15 WR-B
PAIRWISE `(external_id, full_name)` form.

Purpose: Satisfy the roster/structure portion of WASH-07 with an idempotent, self-verifying migration
that uses the CORRECTED geo_id 4167100, produces no section-split and no empty LOCAL section, asserts
the geofence actually exists, and asserts each seated name maps to its exact external_id.
Output: `1187_sherwood_city_council.sql` (registered in schema_migrations), applied by the orchestrator
via psql, with its in-migration DO-block gates passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES the `.sql` file to C:/EV-Accounts/backend/migrations/.
It has NO DB access. The INLINE ORCHESTRATOR applies it via `psql "$DATABASE_URL" -f <file>`
(DATABASE_URL from C:/EV-Accounts/backend/.env) and commits it in the C:/EV-Accounts repo
(`git -C "C:/EV-Accounts"` — never cd into it). Use the exact migration number confirmed in
181-01-SUMMARY.md (default 1187; ledger MAX 1178 is a TRAP — on-disk MAX 1186 is authoritative) — if
Wave-0 recorded a different next number, rename the file and update the ledger INSERT.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug` and
`photo_origin_url` OUT of comments in this file. The ledger INSERT into
supabase_migrations.schema_migrations (version) is a genuine table write (expected), not a comment.
</context>

<interfaces>
<!-- Confirmed schema shapes — executor uses these directly, no exploration needed. -->

essentials.governments  (id, name, type, state, city, geo_id)   -- slug GENERATED; NO geo_id unique constraint; INSERT via WHERE NOT EXISTS on name
essentials.chambers     (id, name, name_formal, government_id, official_count)  -- slug GENERATED ALWAYS — NEVER include in INSERT column list
essentials.districts    (id, district_type, state, geo_id, label, mtfcc)  -- NO name_formal; government_id NULL; state='or' LOWERCASE for LOCAL/LOCAL_EXEC
essentials.politicians  (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id, office_id)  -- party ALWAYS NULL (antipartisan)
essentials.offices      (id, district_id, chamber_id, politician_id, title, representing_state, representing_city, is_appointed_position, is_vacant, role_canonical)
</interfaces>

<roster>
<!-- Seed exactly these 7 (re-confirmed at Wave-0). ext_id / title / district_type / appointed: -->
| ext_id    | full_name      | first_name | last_name | title     | district_type | is_appointed |
|-----------|----------------|------------|-----------|-----------|---------------|--------------|
| -4167101  | Tim Rosener    | Tim        | Rosener   | Mayor     | LOCAL_EXEC    | FALSE (directly elected citywide; 2-YEAR term, expires Jan 2027) |
| -4167102  | Kim Young      | Kim        | Young     | Councilor | LOCAL         | FALSE (elected Nov 2024, term through Jan 2029; holds Council President title-on-seat) |
| -4167103  | Renee Brouse   | Renee      | Brouse    | Councilor | LOCAL         | FALSE (elected Nov 2024, term through Jan 2029) |
| -4167104  | Taylor Giles   | Taylor     | Giles     | Councilor | LOCAL         | FALSE (term through Jan 2027) |
| -4167105  | Keith Mays     | Keith      | Mays      | Councilor | LOCAL         | FALSE (term through Jan 2027; FORMER Mayor 2018-2022/2005-2012 + former Council President 2001-2004 — now a PLAIN Councilor) |
| -4167106  | Doug Scott     | Doug       | Scott     | Councilor | LOCAL         | FALSE (term through Jan 2027) |
| -4167107  | Dan Standke    | Dan        | Standke   | Councilor | LOCAL         | FALSE (elected/re-elected Nov 2024, term through Jan 2029) |
<!-- Titles use the PLAIN convention ('Mayor' / 'Councilor') — NO numbered positions, NO ward suffixes.
     Mayor Rosener on LOCAL_EXEC; all 6 councilors on the shared LOCAL district. ALL 7 use
     is_appointed=false + is_appointed_position=false (uniform Forest Grove/Tualatin shape — do NOT
     import Tigard's appointed-seat block for any seat). party=NULL, representing_state='OR',
     representing_city='Sherwood' on all 7. Kim Young's "Council President" is a title-on-seat — note
     ONLY in a migration comment; do NOT create a separate office row and do NOT alter her title. Keith
     Mays is a PLAIN Councilor today despite his former-Mayor/Council-President history — add a pitfall
     guard comment; do NOT title him 'Mayor' or give him the Council President designation. There is NO
     Youth Advisory Board seat to seed (it is a standalone advisory board). Do NOT seed any 2026-election
     candidate as an incumbent. Mayor's term is 2 years — note it in the LOCAL_EXEC label/comment. -->
</roster>

<tasks>

<task type="auto">
  <name>Task 1: Author the Sherwood structural migration</name>
  <files>C:/EV-Accounts/backend/migrations/1187_sherwood_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1178_forest_grove_city_council.sql (the directly-elected-Mayor + shared-at-large + zero-appointed-seats + plain-title template — entire file; the exact shape class Sherwood needs)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md §"1187_sherwood_city_council.sql (structural migration, CRUD)" (every code block, adapted values — incl. the fully-specified WR-B pairwise identity gate, the 2-year-term label, the Young title-on-seat comment, and the Mays pitfall guard)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md (confirmed geo_id 4167100, ext_id block, migration number, roster)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md §"Schema Shapes", §"OR Casing Rules", §"Office Title Convention", §"Migration Template Fixes to Carry Forward (D-15)"
  </read_first>
  <action>
    Wrap everything in a single BEGIN...COMMIT transaction. Structure per Forest Grove mig 1178 (the
    directly-elected-Mayor + shared-at-large + zero-appointed-seats + plain-title shape), adapting
    values:
    (1) Pre-flight hard-abort DO block: RAISE EXCEPTION if a 'City of Sherwood, Oregon, US' government
    already exists (message references migration 1187).
    (2) Government INSERT ... SELECT (name='City of Sherwood, Oregon, US', type='LOCAL', state='OR'
    UPPERCASE, city='Sherwood', geo_id='4167100') guarded WHERE NOT EXISTS on name — NEVER insert slug.
    (3) Chamber INSERT (name='City Council', name_formal='Sherwood City Council', government_id
    subselect, official_count=7) guarded WHERE NOT EXISTS on (name, government_id) — NEVER insert slug.
    (4) TWO district INSERTs, both state='or' LOWERCASE, geo_id='4167100', mtfcc=NULL: one
    district_type='LOCAL_EXEC' label='Sherwood (Mayor, Citywide, 2-Year Term)', one
    district_type='LOCAL' label='Sherwood (At-Large)', each guarded WHERE NOT EXISTS on (geo_id,
    district_type, state). Add a comment above the LOCAL_EXEC row noting the Mayor is directly elected
    citywide on a 2-YEAR term (new pattern this milestone; structural shape unchanged). Create NO other
    districts — no ward/X00xx rows, and NO per-seat/per-position districts (all 6 councilors share the
    ONE LOCAL district).
    (5) SEVEN office blocks (the CTE pattern: INSERT politician ON CONFLICT (external_id) DO UPDATE SET
    is_active, then INSERT office CROSS JOIN the correct district guarded by NOT EXISTS on
    (district_id, politician_id)). Use the exact 7 rows in the <roster> table above: Mayor Rosener on
    the LOCAL_EXEC district (title 'Mayor'); the 6 councilors on the shared LOCAL district (title PLAIN
    'Councilor' for ALL six — do NOT append a position number or ward). party=NULL, role_canonical=NULL,
    representing_state='OR' UPPERCASE, representing_city='Sherwood' INLINE on every office. UNIFORM
    appointed flags: ALL 7 politicians is_appointed=false AND all 7 offices is_appointed_position=false
    (the Forest Grove/Tualatin uniform shape — do NOT import Tigard's is_appointed=true block for any
    seat). Add a comment above Young's block (-4167102) noting she holds the Council President
    title-on-seat (one office row only, title stays plain 'Councilor'). Add a PITFALL GUARD comment
    above Mays's block (-4167105) warning he is a FORMER Mayor (2018-2022, 2005-2012) and FORMER
    Council President (2001-2004) who is now a PLAIN Councilor — do NOT title him 'Mayor' or give him
    the Council President designation.
    (6) office_id back-fill UPDATE with an explicit external_id IN (-4167101,-4167102,-4167103,
    -4167104,-4167105,-4167106,-4167107) list, guarded AND p.office_id IS NULL.
    (7) Post-verification DO block with the independent assertions plus the WR-B PAIRWISE identity gate:
    gate gov_count=1; office_count=7 (JOIN districts on geo_id 4167100 + district_type IN
    ('LOCAL','LOCAL_EXEC') + state 'or'); an INDEPENDENT geofence-presence assertion (COUNT(*) FROM
    essentials.geofence_boundaries WHERE geo_id='4167100' AND mtfcc='G4110' must be >=1); the canonical
    section-split query (GROUP BY o.district_id HAVING COUNT(DISTINCT o.chamber_id) > 1 over offices
    JOIN districts on geo_id 4167100) = 0; office_id NULLs=0; representing_city='Sherwood' count=7; AND
    THE D-15 WR-B PAIRWISE IDENTITY GATE — SELECT COUNT(*) INTO v_pair_count FROM essentials.politicians
    WHERE (external_id, full_name) IN ((-4167101,'Tim Rosener'),(-4167102,'Kim Young'),
    (-4167103,'Renee Brouse'),(-4167104,'Taylor Giles'),(-4167105,'Keith Mays'),(-4167106,'Doug Scott'),
    (-4167107,'Dan Standke')), RAISE EXCEPTION unless it equals 7 (this is the WR-B upgrade — a pairwise
    pair assertion, NOT two independent IN lists, so a name/id transposition cannot pass). RAISE
    EXCEPTION on any mismatch; RAISE NOTICE on success listing all gates incl. pairwise_identity=7.
    (8) Ledger INSERT into supabase_migrations.schema_migrations (version) VALUES ('1187') ON CONFLICT
    DO NOTHING (STRUCTURAL migrations register — contrast with headshot/stance migrations).
    Do NOT place fenced code blocks in this action — the SQL itself lives in the file; the analog blocks
    in 181-PATTERNS.md are the source of truth for exact text.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1187_sherwood_city_council.sql`; the in-migration post-verify DO block (incl. the WR-B pairwise identity gate) RAISEs on any gate failure (all gates must emit the success NOTICE)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the confirmed migration number path (default 1187_sherwood_city_council.sql)
    - Single BEGIN...COMMIT; pre-flight abort guard present (references migration 1187)
    - governments INSERT uses geo_id='4167100', state='OR', WHERE NOT EXISTS on name; no slug column
    - chambers INSERT has official_count=7 and name_formal='Sherwood City Council'; no slug column
    - Exactly 2 district INSERTs (LOCAL_EXEC + LOCAL), both state='or' lowercase, geo_id='4167100'; LOCAL_EXEC label notes the 2-Year Term; NO ward/X00xx and NO per-seat districts
    - 7 office blocks with the exact ext_ids -4167101..-4167107; titles are PLAIN 'Mayor' + 'Councilor' x6 (NO position numbers, NO wards)
    - ALL 7 politicians is_appointed=false AND all 7 offices is_appointed_position=false (no appointed seats)
    - representing_city='Sherwood' appears on all 7 office INSERTs (inline, no backfill migration)
    - Young (-4167102) has exactly one office row, plain 'Councilor' title, with a Council-President title-on-seat comment; Mays (-4167105) has a plain 'Councilor' title with a former-Mayor/CP pitfall-guard comment
    - office_id back-fill UPDATE lists all 7 external_ids
    - Post-verify DO block includes the independent geofence assertion (geo_id='4167100' AND mtfcc='G4110' >= 1) + canonical section-split query + the WR-B PAIRWISE (external_id, full_name) identity gate (count must = 7); asserts gov=1, offices=7, section-split=0, office_id nulls=0, representing_city=7, pairwise_identity=7
    - Ledger INSERT VALUES ('1187') present (structural migration registers)
    - Literal strings `slug` and `photo_origin_url` do NOT appear in comments
  </acceptance_criteria>
  <done>Structural migration authored against geo_id 4167100 with plain titles, zero appointed seats, the 2-year-term Mayor note, and the WR-B pairwise identity gate; applies cleanly; post-verify DO block emits its success NOTICE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies the migration, runs the E2E structural gate, commits</name>
  <what-built>The Sherwood structural migration. The orchestrator applies it and runs the standalone verification gate.</what-built>
  <how-to-verify>
    1. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1187_sherwood_city_council.sql`. The in-migration DO block must emit "Post-verification PASSED". If it RAISEs, the transaction rolls back — fix and re-apply.
    2. Independent E2E gate (run as separate SELECTs):
       a. governments count for name='City of Sherwood, Oregon, US' = 1.
       b. chambers row exists, official_count=7, name_formal='Sherwood City Council'.
       c. districts: exactly 1 LOCAL_EXEC + 1 LOCAL for geo_id='4167100' state='or'; ZERO ward/X00xx and ZERO per-seat rows for Sherwood.
       d. offices count = 7 for the Sherwood chamber; representing_city='Sherwood' on all 7; titles are PLAIN 'Mayor' + 'Councilor' x6 (no position numbers, no wards).
       e. Section-split scan returns 0 rows for geo_id 4167100.
       f. All 7 politicians have non-NULL office_id.
       g. ALL 7 have is_appointed=false + is_appointed_position=false; Young (-4167102) has exactly ONE office row; Mays (-4167105) titled 'Councilor' (not Mayor/CP).
       h. Independent geofence check: essentials.geofence_boundaries WHERE geo_id='4167100' AND mtfcc='G4110' returns >=1.
       i. Pairwise identity gate: the 7 politicians carry exactly the researched (external_id, full_name) pairs (Rosener/Young/Brouse/Giles/Mays/Scott/Standke).
    3. Commit the migration in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/1187_sherwood_city_council.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied; "Post-verification PASSED" NOTICE emitted (incl. pairwise_identity=7)
    - Independent E2E gate a-i all pass
    - Zero ward/X00xx and zero per-seat districts for Sherwood; exactly 7 offices
    - All 7 uniform is_appointed=false; Young single office row; Mays plain 'Councilor'; plain titles throughout
    - Geofence assertion confirms 4167100 (G4110) present
    - Migration committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with the gate results, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Structural INSERTs applied to production Supabase via psql |
| migration file → schema | Idempotency + post-verify gates (incl. WR-B pairwise identity gate) gate the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-06 | Tampering | Silent politician-identity collision: an ON CONFLICT (external_id) DO UPDATE path re-attaches a wrong full_name to a colliding ext_id, or a name/id transposition | mitigate | D-15 WR-B PAIRWISE identity gate: post-verify asserts exactly 7 (external_id, full_name) pairs, RAISEs otherwise — stronger than Forest Grove's two-independent-IN-lists version (which would pass a transposition) |
| T-181-07 | Tampering | Duplicate/partial re-seed on prod | mitigate | Pre-flight hard-abort DO block + WHERE NOT EXISTS on gov/chamber/districts + office guard on (district_id, politician_id); single transaction rolls back on any post-verify failure |
| T-181-08 | Tampering | Dead post-verify gate lets a real section-split ship silently | mitigate | Independent geofence-presence assertion + canonical GROUP BY/HAVING section-split query, not the same-transaction "create-then-check-absence" gate |
| T-181-09 | Spoofing | Wrong seat modeling: titling Keith Mays as Mayor/Council President based on his former-office history | mitigate | Roster comment flags the pitfall; pairwise identity gate asserts the exact 7 names on their exact ext_ids; Mays block titled plain 'Councilor' |
| T-181-10 | Elevation of Privilege | Ward/position districts or an appointed-seat mislabel misrepresent the roster | mitigate | Pure at-large enforced (2 citywide districts, plain titles); uniform is_appointed=false; E2E gate 2c/2d/2g assert zero ward rows + exactly 7 offices + no appointed seats |
| T-181-11 | Repudiation | Untracked structural change | mitigate | Migration registered in schema_migrations ledger; committed to EV-Accounts git |
| T-181-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- In-migration post-verify DO block passes (incl. WR-B pairwise identity gate); independent E2E gate
  a-i passes; migration committed.
</verification>

<success_criteria>
- 1 government + 1 chamber (official_count=7) + 2 citywide districts + 7 offices (plain
  'Mayor'/'Councilor' titles, no appointed seats), all with representing_city='Sherwood', office_id
  back-filled, section-split=0, geofence 4167100 confirmed present, pairwise identity gate confirms the
  7 researched names, no ward geofences, Mays a plain Councilor.
</success_criteria>

<output>
Create `.planning/phases/181-city-of-sherwood-deep-seed/181-02-SUMMARY.md` when done, recording the
minted politician UUIDs (by external_id) for plans 03 (headshots) and 04 (stances).
</output>
