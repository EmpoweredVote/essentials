---
phase: 159-nevada-state-federal-government
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py
  - C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql
autonomous: false
requirements: [NV-STATE-02]
user_setup: []

must_haves:
  truths:
    - "Each of the 4 NV US House reps (Titus CD-1, Amodei CD-2, Lee CD-3, Horsford CD-4) has a 600x750 headshot"
    - "House rep headshots are stored at politician_photos/{uuid}-headshot.jpg and rendered in the app"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py"
      provides: "Download (unitedstates/images, clerk.house.gov fallback) + resize-to-600x750 Lanczos q90 + Storage upload for all 4 House reps"
      contains: "T000468"
    - path: "C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql"
      provides: "Audit-only politician_images INSERT for 4 House reps (external_ids -32001..-32004)"
      contains: "essentials.politician_images"
  key_links:
    - from: "essentials.politician_images (4 House rows)"
      to: "essentials.politicians (external_ids -32001..-32004)"
      via: "politician_id subquery on external_id"
      pattern: "external_id = -3200[1-4]"
    - from: "headshot URL"
      to: "Supabase Storage politician_photos bucket"
      via: "{uuid}-headshot.jpg path"
      pattern: "politician_photos/.*-headshot.jpg"
---

<objective>
Give all 4 Nevada US House representatives their missing 600x750 headshots. All 4 reps already exist in the DB with correct district routing (Phase 158 tiger_geoids 3201-3204 confirmed linked) — this plan ONLY adds headshots. No politician, chamber, district, or office rows are created. Source is unitedstates.github.io/images/congress/450x550/{bioguide}.jpg (already 4:5, public domain, resize-only).

Purpose: Satisfies the House-headshot half of NV-STATE-02 (Success Criterion 3 — each of the 4 US House reps renders for an address in their geofence-linked district with a 600x750 headshot).
Output: _tmp-nv-house-headshots.py (download+resize+upload pipeline), 1051_nv_house_headshots.sql (audit-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP):**
- The executor WRITES the Python script and the audit-only migration .sql to disk. The executor does NOT upload to Storage or apply the migration.
- Running _tmp-nv-house-headshots.py (Storage upload) and applying 1051 via mcp__supabase-local__execute_sql are INLINE ORCHESTRATOR steps performed by execute-phase.
</execution_context>

<context>
@.planning/phases/159-nevada-state-federal-government/159-RESEARCH.md
@.planning/phases/159-nevada-state-federal-government/159-PATTERNS.md

<interfaces>
<!-- DB-confirmed roster (verified 2026-06-23). Use these UUIDs directly — do NOT re-query. -->
CD-1 Dina Titus      external_id=-32001  uuid=786af5d2-9502-401c-a3ed-61de88e589e9  bioguide=T000468
CD-2 Mark E. Amodei  external_id=-32002  uuid=030b5074-8335-48b3-8d6b-0ea7c09814a5  bioguide=A000369
CD-3 Susie Lee       external_id=-32003  uuid=325c7cae-aae6-4d7b-9c03-e707c7423d3c  bioguide=L000602 [LOW confidence — verify via the 450x550 URL before relying on it]
CD-4 Steven Horsford external_id=-32004  uuid=7644cd40-b5c1-494a-8e65-f3126fc7f9ee  bioguide=H001066
Primary source:  https://unitedstates.github.io/images/congress/450x550/{bioguide}.jpg (4:5, public_domain)
Fallback source: https://clerk.house.gov/images/members/{bioguide}.jpg
CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
politician_images columns: (id, politician_id, url, type, photo_license) — NO photo_origin_url
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 4-rep House headshot pipeline script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-va-execs-headshots.py (PRIMARY ANALOG — pipeline + constants + upload headers)
    - .planning/phases/159-nevada-state-federal-government/159-PATTERNS.md (section "_tmp-nv-house-headshots.py" — full ROSTER + download/resize/upload functions + manifest format)
    - .planning/phases/159-nevada-state-federal-government/159-RESEARCH.md (Headshot Upload Pattern + Pitfall 3 — bioguide fallback; Assumption A1 — Lee bioguide LOW confidence)
  </read_first>
  <action>
Write _tmp-nv-house-headshots.py using the exact template in 159-PATTERNS.md. Constants: BUCKET='politician_photos', CDN_BASE (the politician_photos public URL), TARGET_SIZE=(600,750), JPEG_QUALITY=90. Define a ROSTER list of 4 dicts (external_id, full_name, politician_id, bioguide, title, is_required=True) using the exact UUIDs and bioguides from the interfaces block above. download_image(bioguide): GET https://unitedstates.github.io/images/congress/450x550/{bioguide}.jpg with 30s timeout; on 404 fall back to https://clerk.house.gov/images/members/{bioguide}.jpg; raise_for_status. resize_600x750(img): source is already 4:5 (450x550) so resize-only via Image.Resampling.LANCZOS — NO crop step. upload_to_storage(img, politician_id): save JPEG q90 to BytesIO, PUT to {SUPABASE_URL}/storage/v1/object/{BUCKET}/{politician_id}-headshot.jpg with Authorization Bearer SUPABASE_SERVICE_ROLE_KEY, Content-Type image/jpeg, x-upsert true; return the CDN URL. Loop the roster, print a manifest in the exact format from PATTERNS ("SUCCESS: {external_id} {full_name} {uuid} -> {cdn_url}") and a final "TOTALS: NV House N/4 succeeded" line. Add a comment flagging that Susie Lee's bioguide L000602 is LOW confidence (Assumption A1) — if the 450x550 URL 404s for her, the clerk.house.gov fallback handles it but the operator should eyeball the result. Do NOT run the script — that is the inline orchestrator step.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py" &amp;&amp; grep -q "T000468" "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py" &amp;&amp; grep -q "A000369" "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py" &amp;&amp; grep -q "L000602" "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py" &amp;&amp; grep -q "H001066" "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py" &amp;&amp; grep -q "clerk.house.gov" "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py" &amp;&amp; grep -q "LANCZOS" "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py" &amp;&amp; grep -q "x-upsert" "C:/EV-Accounts/backend/scripts/_tmp-nv-house-headshots.py" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-nv-house-headshots.py exists with a 4-entry ROSTER containing all 4 exact UUIDs and bioguides (T000468, A000369, L000602, H001066).
    - download_image tries unitedstates.github.io first, falls back to clerk.house.gov on 404.
    - resize is LANCZOS to (600,750) with NO crop step (source is already 4:5).
    - upload uses x-upsert PUT to politician_photos/{uuid}-headshot.jpg.
    - prints a per-rep SUCCESS manifest + a TOTALS line.
    - Susie Lee LOW-confidence bioguide flagged in a comment.
  </acceptance_criteria>
  <done>_tmp-nv-house-headshots.py is written to disk and passes all acceptance criteria. Not yet run.</done>
</task>

<task type="auto">
  <name>Task 2: Write audit-only migration 1051 + run script + apply inline</name>
  <files>C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/315_va_headshots.sql (EXACT ANALOG — 4-row politician_images INSERT pattern)
    - .planning/phases/159-nevada-state-federal-government/159-PATTERNS.md (section "1051_nv_house_headshots.sql" — the 4 INSERT blocks verbatim with the real UUIDs)
    - .planning/phases/159-nevada-state-federal-government/159-RESEARCH.md (Pitfall 5 — no photo_origin_url)
  </read_first>
  <action>
EXECUTOR STEP: Write 1051_nv_house_headshots.sql exactly as in 159-PATTERNS.md — 4 idempotent INSERT INTO essentials.politician_images blocks (one per rep). Header comment: file is 1051_nv_house_headshots.sql, Phase 159 NV-STATE-02, AUDIT-ONLY (NOT registered in supabase_migrations.schema_migrations; ledger stays at 1050), applied via execute_sql, source unitedstates.github.io public_domain, and the 4 bioguide IDs. Each INSERT: column list exactly (id, politician_id, url, type, photo_license); id=gen_random_uuid(); politician_id from (SELECT id FROM essentials.politicians WHERE external_id=<ext_id>); url = CDN politician_photos/{uuid}-headshot.jpg with the literal UUID baked in (Titus 786af5d2.../-32001, Amodei 030b5074.../-32002, Lee 325c7cae.../-32003, Horsford 7644cd40.../-32004); type='default'; photo_license='public_domain'; guarded by WHERE NOT EXISTS on politician_id. No photo_origin_url (Pitfall 5). No schema_migrations registration.

INLINE ORCHESTRATOR STEPS (documented in the plan, not executor-run): (1) run _tmp-nv-house-headshots.py and confirm "TOTALS: NV House 4/4 succeeded"; (2) apply 1051_nv_house_headshots.sql via mcp__supabase-local__execute_sql; (3) SELECT politician_images for external_ids in (-32001,-32002,-32003,-32004) → expect 4 rows with non-null url.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql" &amp;&amp; test $(grep -c "INSERT INTO essentials.politician_images" "C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql") -eq 4 &amp;&amp; grep -q "786af5d2-9502-401c-a3ed-61de88e589e9" "C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql" &amp;&amp; grep -q "7644cd40-b5c1-494a-8e65-f3126fc7f9ee" "C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql" &amp;&amp; ! grep -q "photo_origin_url" "C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql" &amp;&amp; ! grep -q "schema_migrations" "C:/EV-Accounts/backend/migrations/1051_nv_house_headshots.sql" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1051_nv_house_headshots.sql contains exactly 4 politician_images INSERT blocks, one per House rep external_id (-32001..-32004).
    - Each url uses the matching DB-confirmed UUID + -headshot.jpg; type='default'; photo_license='public_domain'.
    - Column list is exactly (id, politician_id, url, type, photo_license) — photo_origin_url absent; no schema_migrations line.
    - Each INSERT guarded by WHERE NOT EXISTS on politician_id.
    - INLINE: script reports 4/4 succeeded; 1051 applied; politician_images audit returns 4 rows with non-null url for the 4 House external_ids.
  </acceptance_criteria>
  <done>1051_nv_house_headshots.sql written; (inline) all 4 headshots uploaded and 4 politician_images rows applied with non-null urls.</done>
</task>

</tasks>

<verification>
After all tasks (inline orchestrator):
1. House headshot audit: SELECT pi.url FROM politician_images pi JOIN politicians p ON p.id=pi.politician_id WHERE p.external_id IN (-32001,-32002,-32003,-32004) → 4 rows, all non-null url.
2. supabase_migrations.schema_migrations does NOT contain a '1051' row (audit-only — ledger unchanged).
3. Each uploaded image is a valid 600x750 JPEG (spot-check at least one CDN URL returns HTTP 200).
</verification>

<success_criteria>
- All 4 NV US House reps have a 600x750 headshot at politician_photos/{uuid}-headshot.jpg.
- politician_images has exactly one row per House rep (no duplicates).
- No politician/chamber/district/office rows created — headshots only.
- 1051 is audit-only (schema_migrations ledger unchanged at 1050).
</success_criteria>

<output>
Create `.planning/phases/159-nevada-state-federal-government/159-02-SUMMARY.md` when done.
</output>
