---
phase: 158-nevada-tiger-geofences
plan: 02
type: execute
wave: 2
depends_on:
  - 158-01
files_modified: []
autonomous: true
requirements:
  - NV-GEO-01

must_haves:
  truths:
    - "geofence_boundaries rows exist for state='32' with all 5 MTFCC types (G4020, G4110, G5200, G5210, G5220)"
    - "geofence_boundaries has exactly 17 rows with state='32' AND mtfcc='G4020' (16 counties + Carson City independent city)"
    - "geofence_boundaries has exactly 4 rows with state='32' AND mtfcc='G5200' (pre-existing; idempotent re-load logs already_exists:4)"
    - "geofence_boundaries has exactly 21 rows with state='32' AND mtfcc='G5210' (21 NV Senate districts)"
    - "geofence_boundaries has N rows with state='32' AND mtfcc='G5220' where N matches EXPECTED_NV_MTFCC.sldl from Plan 01 (~42 Assembly districts)"
    - "geofence_boundaries has N rows with state='32' AND mtfcc='G4110' where N matches EXPECTED_NV_MTFCC.place from Plan 01 (~19 cities)"
    - "Las Vegas city, Henderson, North Las Vegas, and Boulder City all present as G4110 rows"
    - "Clark County (geo_id='32003') present as a G4020 row"
    - "districts rows exist for Nevada with state IN ('NV','nv') at 4 district_types: nv|COUNTY|17, nv|STATE_UPPER|21, nv|STATE_LOWER|N, NV|NATIONAL_LOWER|4"
    - "The Strip probe returns Clark County G4020 + legislative tiers but NO G4110 — the Strip is unincorporated (success criterion #1)"
    - "Section-split gate returns 0 rows — every G5200/G5210/G5220/G4020 geofence has a matching districts row"
    - "smoke-nv-geofences.ts exits 0 — Strip returns no G4110; all 4 incorporated cities return G4110 + county/CD/SLDU/SLDL"
  artifacts:
    - path: "essentials.geofence_boundaries (Supabase production)"
      provides: "5 layers of NV TIGER boundaries"
      contains: "state='32' rows for G4020, G4110, G5200, G5210, G5220"
    - path: "essentials.districts (Supabase production)"
      provides: "NV district rows with correct state casing"
      contains: "state='nv' for COUNTY/STATE tiers; state='NV' for NATIONAL_LOWER"
  key_links:
    - from: "TIGER PLACE layer (tl_2024_32_place.zip)"
      to: "geofence_boundaries G4110 rows"
      via: "upsertGeofence() with ST_MakeValid + state='32'; G4110-only MTFCC filter"
      pattern: "mtfcc='G4110' AND state='32'"
    - from: "TIGER COUNTY layer (tl_2024_us_county.zip filtered STATEFP='32')"
      to: "geofence_boundaries G4020 rows (including Clark County geo_id='32003')"
      via: "filterByStatefp=true in processLayer()"
      pattern: "mtfcc='G4020' AND state='32'"
    - from: "TIGER SLDU/SLDL layers"
      to: "districts table"
      via: "insertDistrictIfMissing() with abbrev='nv' (lowercase)"
      pattern: "state='nv' AND district_type IN ('STATE_UPPER','STATE_LOWER')"
    - from: "TIGER CD119 layer"
      to: "districts table NATIONAL_LOWER rows"
      via: "insertDistrictIfMissing() with abbrevUpper='NV' (uppercase)"
      pattern: "state='NV' AND district_type='NATIONAL_LOWER'"
---

<objective>
Live-load all 5 NV TIGER layers into geofence_boundaries and districts using the confirmed EXPECTED_NV_MTFCC counts from Plan 01, then verify via the SQL gate script and the 5-address smoke test. This is the data-write that makes Nevada geographically routable.

Purpose: This is the primary data write for Phase 158. After this plan completes, all NV address routing is possible end-to-end. Phases 159-168 (state government, federal officials, legislators, Clark County metro deep-seeds, CCSD, elections, retro) all depend on these geofence and district rows existing for state='32'/'nv'/'NV'. The headline correctness check is the Strip resolving to Clark County with NO city (success criterion #1).

Output: geofence_boundaries and districts rows for state='32' (all 5 layers, idempotent for cd119 which already has 4 rows); verify SQL all gates green (especially the Strip-unincorporated Gate 4 and the section-split Gate 7); smoke test exits 0 for 5 NV addresses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-nevada-tiger-geofences/158-CONTEXT.md
@.planning/phases/158-nevada-tiger-geofences/158-01-PLAN.md
@.planning/phases/158-nevada-tiger-geofences/158-01-SUMMARY.md
@.planning/phases/100-va-tiger-geofences/100-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load all 5 NV TIGER layers into geofence_boundaries and districts</name>
  <read_first>
    .planning/phases/158-nevada-tiger-geofences/158-01-SUMMARY.md — Confirm the actual sldl count (N) and place count (N) recorded from Plan 01 Task 3 dry-run, and the verified cd119 URL filename. The loader EXPECTED_NV_MTFCC must contain these confirmed values before running live.
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Verify EXPECTED_NV_MTFCC has non-zero sldl and place values (Plan 01 edits still in place); confirm STATE_LAYER_ALLOWLIST/STATE_CITY_ASSERTIONS/STATE_RUN_MAKEVALID all contain NV entries.
    .planning/phases/158-nevada-tiger-geofences/158-CONTEXT.md — Pre-existing state caveat: 4 G5200 congressional-district rows already exist for state='32'; cd119 live load logs already_exists:4 (correct, NOT an error). Do NOT touch any pre-existing NV federal/exec officials (that is Phase 159).
  </read_first>
  <action>
    Confirm Plan 01 is complete before proceeding: EXPECTED_NV_MTFCC.sldl and EXPECTED_NV_MTFCC.place must both be non-zero in load-state-tiger-boundaries.ts. If either is still 0, STOP — Plan 01 Task 3 must be re-run first; this task cannot proceed with sentinel values (the pre-flight assertion would fire got-N against expected-0 and abort).

    From C:/EV-Accounts/backend, run the live load command for all 5 NV layers (NO --dry-run flag):

      npx tsx scripts/load-state-tiger-boundaries.ts --state NV --fips 32 --layers cd119,sldu,sldl,place,county

    The loader will:
      1. Download all 5 TIGER zip files from census.gov (cached from Plan 01 dry-run; county is the ~80MB national file)
      2. Run the EXPECTED_NV_MTFCC pre-flight assertion for each layer (must pass before any DB write)
      3. Run the STATE_CITY_ASSERTIONS check for the place layer (confirm all 4 NV cities present in NAMELSAD)
      4. Stream each shapefile and upsert rows into essentials.geofence_boundaries with ON CONFLICT (geo_id, mtfcc) DO NOTHING; ST_MakeValid applied per STATE_RUN_MAKEVALID
      5. Insert corresponding district rows into essentials.districts via insertDistrictIfMissing() (WHERE NOT EXISTS guard) — abbrev='nv' (lowercase) for county/sldu/sldl, abbrevUpper='NV' (uppercase) for cd119 NATIONAL_LOWER

    Monitor console output for these expected results per layer:
      - cd119: pre-flight PASSED 4 records; upsert logs inserted_boundary:0 + already_exists:4 (correct — 4 G5200 rows existed before this phase); district insertion guarded by WHERE NOT EXISTS
      - sldu: pre-flight PASSED 21 records; upsert inserts 21 G5210 boundary rows + 21 nv|STATE_UPPER district rows
      - sldl: pre-flight PASSED N records (N from Plan 01, ~42); upsert inserts N G5220 boundary rows + N nv|STATE_LOWER district rows (single-member Assembly districts)
      - place: pre-flight PASSED N records (N from Plan 01, ~19); upsert inserts N G4110 boundary rows; the place layer does NOT write district rows
      - county: pre-flight PASSED 17 records; the county shapefile is national (tl_2024_us_county.zip), filtered by STATEFP='32'; upsert inserts 17 G4020 boundary rows + 17 nv|COUNTY district rows (including Clark County geo_id='32003' and Carson City independent city-county)

    Watch for these failure modes and STOP if any occur:
      - Any MtfccAssertionError — EXPECTED_NV_MTFCC value drifted from actual; re-run Plan 01 Task 3 to refresh
      - STATE_CITY_ASSERTIONS gate FAILED — an NV city not found in NAMELSAD; investigate casing and stop
      - cd119 logs inserted_boundary > 0 with a different geo_id scheme than the pre-existing 4 — would mean pre-existing rows used a different key; record and stop (do not create duplicate CD geofences)
      - Any non-zero ERROR line that is NOT already_exists — investigate before retrying
      - Loader exits non-zero — read the error; ON CONFLICT DO NOTHING makes re-running safe so a retry after a fix is acceptable

    Do not abort on already_exists:4 for cd119 — documented expected behavior (pre-existing 4 G5200 rows). The EXPECTED_NV_MTFCC.cd119=4 pre-flight assertion already validated the shapefile records; the upsert correctly skips pre-existing rows.

    Do NOT write a migration file for this load; the loader script IS the data-writing mechanism for TIGER geofences (established pattern from VA/ME/OR/MD/CA/TX/UT/IN/MA). The on-disk migration counter (next=1048) is NOT advanced by this load.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx -e "require('dotenv').config(); const {Client}=require('pg'); const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(\"SELECT mtfcc, COUNT(*) AS n FROM essentials.geofence_boundaries WHERE state='32' GROUP BY mtfcc ORDER BY mtfcc\")).then(r=>{r.rows.forEach(x=>console.log(x.mtfcc,x.n)); return c.end()})"</automated>
  </verify>
  <acceptance_criteria>
    - Loader exits 0 (no non-zero error exit)
    - cd119 layer logs already_exists:4 (not an error — confirms the 4 pre-existing rows are intact and no duplicates created)
    - sldu layer logs 21 G5210 boundary rows upserted
    - sldl layer logs N G5220 rows upserted (N = EXPECTED_NV_MTFCC.sldl confirmed in Plan 01)
    - place layer logs N G4110 rows upserted (N = EXPECTED_NV_MTFCC.place confirmed in Plan 01)
    - county layer logs 17 G4020 rows upserted
    - The mtfcc count query returns G4020=17, G4110=N, G5200=4, G5210=21, G5220=N — G4110/G5220 matching Plan 01 confirmed counts
    - No MtfccAssertionError in loader output
    - STATE_CITY_ASSERTIONS gate passes for all 4 NV cities
  </acceptance_criteria>
  <done>All 5 NV TIGER layers loaded into geofence_boundaries and districts. geofence_boundaries has rows for all 5 MTFCC types at state='32'. districts has rows for nv|COUNTY|17, nv|STATE_UPPER|21, nv|STATE_LOWER|N, NV|NATIONAL_LOWER|4.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the live load — geometry gates, casing, Strip-unincorporated probe, section-split gate</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/verify-nv-tiger-import.sql — The gate verification script created in Plan 01. Read it to understand expected values for each gate before running.
    .planning/phases/158-nevada-tiger-geofences/158-01-SUMMARY.md — Actual sldl and place counts from dry-run; match against Gate 3 and Gate 5 expected counts.
    .planning/phases/158-nevada-tiger-geofences/158-CONTEXT.md — D-03 (Strip is unincorporated — Gate 4 must show NO G4110 at the Strip probe) and the districts casing rule (uppercase 'NV' for NATIONAL tiers, lowercase 'nv' for state-subdivision tiers).
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the verification SQL:

      psql $DATABASE_URL -f scripts/verify-nv-tiger-import.sql

    If psql is unavailable (gsd-executor has no Supabase MCP per milestone conventions), run each gate inline via the same npx tsx -e inline-pg pattern used in Task 1's verify command, substituting each gate query. Check each gate result against the expected values in the SQL file comments.

    Expected gate results:
      - Gate 1 (invalid geometry): 0 rows — no invalid geometries for state='32'
      - Gate 2 (GeometryCollection): 0 rows — no GeometryCollection types for state='32'
      - Gate 3 (per-layer counts): G4020=17, G4110=N (Plan 01 place count), G5200=4, G5210=21, G5220=N (Plan 01 sldl count)
      - Gate 4 (Strip-unincorporated invariant — SUCCESS CRITERION #1): the spatial probe at the Las Vegas Strip coordinate returns a G4020 row named 'Clark County' (geo_id='32003') plus the G5200/G5210/G5220 legislative tiers, and NO G4110 row. If a G4110 row IS returned, the Strip is being falsely matched to a city — STOP and investigate (pick a Strip coordinate clearly inside unincorporated territory along the resort corridor between Tropicana and Flamingo)
      - Gate 5 (districts casing): nv|COUNTY|17, nv|STATE_UPPER|21, nv|STATE_LOWER|N, NV|NATIONAL_LOWER|4 — confirm lowercase 'nv' for county/sldu/sldl and uppercase 'NV' for cd119
      - Gate 6 (Clark County sentinel): exactly 1 row — geo_id='32003' 'Clark County' G4020
      - Gate 7 (section-split OR-direction): 0 rows — every G5200/G5210/G5220/G4020 geofence for state='32' has a matching districts row

    Stop and investigate if any gate returns unexpected results:
      - Gate 1/2 non-zero: invalid/mixed geometry in DB — the loader's ST_MakeValid may have failed on some records; check which MTFCC types are affected
      - Gate 3 count mismatch: a layer loaded fewer rows than expected; check for ON CONFLICT skips vs actual missing records
      - Gate 4 returns a G4110 row: the Strip-unincorporated invariant is violated — the headline correctness check fails; investigate the probe coordinate and the place polygons covering it
      - Gate 5 casing wrong (e.g. 'NV' for county, or 'nv' for cd119): the ME/OR casing rule is violated; backend queries matching exact casing would miss those tiers
      - Gate 7 returns any rows: section-split bug — a legislative or county geofence is missing its districts row; insertDistrictIfMissing() skipped it
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx -e "require('dotenv').config(); const {Client}=require('pg'); const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(\"SELECT mtfcc FROM essentials.geofence_boundaries WHERE state='32' AND ST_Covers(geometry, ST_SetSRID(ST_MakePoint(-115.1728,36.1147),4326)) ORDER BY mtfcc\")).then(r=>{const m=r.rows.map(x=>x.mtfcc); console.log('Strip tiers:',m.join(',')); console.log('G4110_present:',m.includes('G4110')); return c.end()})"</automated>
  </verify>
  <acceptance_criteria>
    - verify-nv-tiger-import.sql runs to completion with no error exit (or all gates run inline with no error)
    - Gate 1 returns 0 rows (no invalid geometries for state='32')
    - Gate 2 returns 0 rows (no GeometryCollection types)
    - Gate 3 shows G4020=17, G5200=4, G5210=21; G4110 and G5220 match confirmed counts from Plan 01
    - Gate 4 (Strip probe): the verify command above prints `G4110_present: false` and the tier list includes G4020 (Clark County) plus G5200/G5210/G5220 — success criterion #1 satisfied
    - Gate 5 includes rows: nv|COUNTY|17, nv|STATE_UPPER|21, NV|NATIONAL_LOWER|4 with correct casing (lowercase county/state, uppercase national)
    - Gate 6 returns exactly 1 row: Clark County (geo_id='32003')
    - Gate 7 returns 0 rows (no section-split — OR-direction check passes; success criterion #4)
  </acceptance_criteria>
  <done>All SQL gates pass. The Strip resolves to Clark County with NO city (success criterion #1). Districts casing is correct (nv/NV per tier). Section-split gate clean (success criterion #4). Database state is consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Run smoke-nv-geofences.ts — all 5 NV addresses must route correctly</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts — The smoke test created in Plan 01. Confirm expectedCounts.G4110 and expectedCounts.G5220 match the confirmed Plan 01 dry-run counts (updated in Plan 01 Task 3 Edit B).
    .planning/phases/158-nevada-tiger-geofences/158-01-SUMMARY.md — Confirmed sldl count (G5220) and place count (G4110) for cross-checking smoke test expectedCounts.
    .planning/phases/158-nevada-tiger-geofences/158-CONTEXT.md — The 5 verification addresses (Strip + 4 cities) and the Strip-forbids-G4110 invariant.
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the smoke test:

      npx tsx scripts/smoke-nv-geofences.ts

    The test queries production via DATABASE_URL (dotenv loaded from .env). It runs 5 address assertions:

      Address 1 (Las Vegas Strip — unincorporated Clark County):
        - expectedMtfcc: G4020, G5200, G5210, G5220 (county + legislative tiers)
        - forbiddenMtfcc: G4110, G4040 (the Strip is unincorporated — must NOT return a city or township)
        - expectedGeoIds: G4020='32003' (Clark County)
        - This is success criterion #1, the headline correctness check

      Addresses 2-5 (City of Las Vegas, Henderson, North Las Vegas, Boulder City — City Hall coordinates):
        - expectedMtfcc: G4020, G4110, G5200, G5210, G5220 (all 5 tiers — incorporated city)
        - expectedGeoIds: G4020='32003' (all four cities are in Clark County)
        - These are success criterion #2

    Watch for:
      - Any EXPECTED MTFCC MISSING error — a tier is absent for a test address; check which layer failed to load (most likely sldl or place if their sentinels were not updated)
      - Any FORBIDDEN MTFCC PRESENT error for Address 1 (Strip) — the Strip coordinate is inside an incorporated place or township; this is a success-criterion-#1 failure — investigate the place polygons or shift the coordinate clearly into unincorporated corridor and document
      - Any EXPECTED GEO_ID MISMATCH for G4020='32003' — the Clark County geo_id is wrong; investigate the county layer
      - SC3 expectedCounts mismatch — if G4110 or G5220 count in DB differs from smoke test expectedCounts; re-check Plan 01 Task 3 Edit B

    If smoke-nv-geofences.ts exits 1, read the console output for which assertion failed and investigate the specific geofence_boundaries query for that address. Coordinates may be refined to land cleanly inside the intended boundary; the criterion is correct tier resolution, not the exact coordinate.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-nv-geofences.ts; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsx scripts/smoke-nv-geofences.ts exits 0
    - Console output: Strip (Address 1) returns G4020 (Clark County, geo_id='32003') + G5200/G5210/G5220 and NO G4110/G4040 (forbiddenMtfcc satisfied — success criterion #1)
    - Console output: City of Las Vegas, Henderson, North Las Vegas, and Boulder City each return all 5 MTFCC tiers including G4110 + county/CD/SLDU/SLDL (success criterion #2)
    - Console output shows SC3 pass: total counts for state='32' match expected (G4020=17, G5200=4, G5210=21, G4110/G5220=confirmed counts from Plan 01)
    - Console output shows SC4 pass: no NULL/empty name rows for any state='32' geofence tier returned
    - No EXPECTED MTFCC MISSING, FORBIDDEN MTFCC PRESENT, or EXPECTED GEO_ID MISMATCH lines in output
    - smoke-nv-geofences.ts exit code is 0 (not 1)
  </acceptance_criteria>
  <done>All 5 NV address assertions pass. NV-GEO-01 satisfied end-to-end: the Strip routes to Clark County with no city (SC1); the 4 incorporated cities route to their G4110 tier plus county/CD/SLDU/SLDL (SC2); all tiers loaded (SC3); section-split clean (SC4 via Plan 02 Task 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader -> Supabase production | Live writes to essentials.geofence_boundaries and essentials.districts — this is the only plan that writes data (mcp__supabase-local IS the production DB) |
| loader -> census.gov | HTTPS download of TIGER zip files for the live load (same files cached from Plan 01) |
| smoke test / verify -> Supabase production | Read-only spatial queries via DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-07 | Integrity | Accidental wrong-state clobber (e.g. --fips 32 with a typo'd --state) writing NV rows under another state's FIPS | mitigate | main() round-trip check rejects mismatched --state/--fips; the FIPS arg is the literal state column value; loader prints State+FIPS banner before any write; executor confirms the banner reads NV (FIPS 32) |
| T-158-08 | Integrity | Live load with stale sentinel 0 in EXPECTED_NV_MTFCC | mitigate | Task 1 requires executor to confirm Plan 01 completion; EXPECTED_NV_MTFCC.sldl/place must be non-zero before running live; the loader pre-flight assertion blocks DB write on count mismatch (got-N vs expected-0) |
| T-158-09 | Integrity | Wrong loader key silently loads zero CD rows | mitigate | cd119 verified in Plan 01 Task 3 (URL filename) and re-validated here by the cd119 pre-flight assertion expecting 4 records (got 0 halts) |
| T-158-10 | Integrity | ON CONFLICT DO NOTHING silently drops rows | accept | Established loader pattern; the EXPECTED_NV_MTFCC pre-flight assertion validates shapefile count before upsert; post-load Gate 3 SQL verifies DB row counts match expected |
| T-158-11 | Integrity | Wrong district state casing (NV vs nv) breaks backend tier matching | mitigate | Gate 5 SQL verifies both state='nv' (COUNTY/STATE) and state='NV' (NATIONAL_LOWER); mismatch caught before the phase is marked complete |
| T-158-12 | Integrity | Strip falsely matched to a city geofence (success-criterion-#1 failure) | mitigate | Gate 4 SQL probe + smoke test Address 1 forbiddenMtfcc both assert NO G4110 at the Strip; two independent checks |
| T-158-13 | Availability | 80MB national county zip causes timeout/OOM | accept | tl_2024_us_county.zip already used by VA/OR/MD phases without issue; filtered by STATEFP='32' before upsert; cached from Plan 01 |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; all deps pre-existing in EV-Accounts/backend (pg, dotenv, shapefile, adm-zip) |
</threat_model>

<verification>
After all 3 tasks complete:
1. mtfcc count query for state='32' returns 5 rows: G4020/17, G4110/N, G5200/4, G5210/21, G5220/N (N from Plan 01 confirmed counts) — success criterion #3
2. Strip probe (-115.1728, 36.1147): G4110_present=false, tier list includes G4020 (Clark County) + G5200/G5210/G5220 — success criterion #1
3. districts casing query: state IN ('NV','nv') returns nv|COUNTY|17, nv|STATE_LOWER|N, nv|STATE_UPPER|21, NV|NATIONAL_LOWER|4
4. Section-split gate (G5200/G5210/G5220/G4020 geofences with no matching districts row): 0 rows — success criterion #4
5. npx tsx scripts/smoke-nv-geofences.ts exits 0 — success criteria #1 and #2 end-to-end
</verification>

<success_criteria>
- NV-GEO-01: geofence_boundaries rows exist for state='32' with all 5 MTFCC types (G4020/17, G4110/N, G5200/4, G5210/21, G5220/N) — loader keys verified from actual TIGER zip filenames (SC3)
- Success criterion #1: a Las Vegas Strip address returns Clark County tiers with NO city (no false G4110 match) — verified by Gate 4 probe and smoke test Address 1 forbiddenMtfcc
- Success criterion #2: City of Las Vegas, Henderson, North Las Vegas, and Boulder City addresses each return the correct G4110 city tier plus county/CD/SLDU/SLDL — verified by smoke test Addresses 2-5
- Success criterion #4: section-split gate returns 0 rows
- districts table has correct state casing: lowercase 'nv' for COUNTY/STATE tiers, uppercase 'NV' for NATIONAL_LOWER
- No sentinel 0 values remain (EXPECTED_NV_MTFCC.sldl/place and smoke expectedCounts.G4110/G5220 all confirmed non-zero integers)
- Pre-existing 4 G5200 CD rows intact; no NV federal/exec officials touched (Phase 159 scope)
</success_criteria>

<output>
Create `.planning/phases/158-nevada-tiger-geofences/158-02-SUMMARY.md` when done. Record final row counts for all 5 MTFCC types, confirmation of the Strip-unincorporated result (no G4110), districts casing, Gate 7 result, and smoke test exit code. Note next phase: Phase 159 (Nevada State & Federal Government).
</output>
