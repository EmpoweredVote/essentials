---
phase: 162-city-of-las-vegas-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["162-01", "162-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1066_las_vegas_berkley_stances.sql
  - C:/EV-Accounts/backend/migrations/1067_las_vegas_knudsen_stances.sql
  - C:/EV-Accounts/backend/migrations/1068_las_vegas_kelley_stances.sql
  - C:/EV-Accounts/backend/migrations/1069_las_vegas_diaz_stances.sql
  - C:/EV-Accounts/backend/migrations/1070_las_vegas_allenpalenske_stances.sql
  - C:/EV-Accounts/backend/migrations/1071_las_vegas_summersarmstrong_stances.sql
  - C:/EV-Accounts/backend/migrations/1072_las_vegas_brune_stances.sql
  - .planning/phases/162-city-of-las-vegas-deep-seed/162-03-SUMMARY.md
autonomous: false
requirements: [CLARK-02]
user_setup: []

must_haves:
  truths:
    - "Each council member with a public record has evidence-only compass stances across all live compass topics, researched one agent at a time, 100% cited, zero default values [D-04]"
    - "Topics with no city-level evidence are honest blank spokes (no value), never defaulted to Neutral/Likely"
    - "A City of Las Vegas address returns the Mayor (sorted first) + the ONE correct ward council member via X0015 ward-precise routing [CLARK-02 SC#1]"
    - "Stances are chairs (discrete positions the evidence matches), not a directional polarity scale"
    - "Casing 'nv' lowercase; zero section-split; structural ledger stays at the confirmed number (all stance migrations audit-only, unregistered)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1066_las_vegas_berkley_stances.sql"
      provides: "Audit-only evidence-only stance migration for Mayor Berkley (inform.politician_answers + inform.politician_context), topic_id resolved live by topic_key, NOT registered"
      contains: "inform.politician_answers"
    - path: ".planning/phases/162-city-of-las-vegas-deep-seed/162-03-SUMMARY.md"
      provides: "Recorded results for all 9 SQL/HTTP verification checks + the ward-routing/correct-person/stance human-verify outcome"
      contains: "CLARK-02"
  key_links:
    - from: "Mayor office (LOCAL_EXEC) + 6 ward offices (LOCAL X0015)"
      to: "districts (geo_id='3240000' + 'las-vegas-nv-council-ward-N', state='nv')"
      via: "address → /representatives/me ST_Covers PIP routing"
      pattern: "las-vegas-nv-council-ward-"
    - from: "7 council members (external_id -3205001..-3205007)"
      to: "inform.politician_answers + inform.politician_context"
      via: "topic_id resolved by topic_key WHERE is_live=true; ON CONFLICT (politician_id, topic_id)"
      pattern: "compass_topics"
---

<objective>
Research and install evidence-only compass stances for all 7 City of Las Vegas council members — ONE research agent at a time (parallel agents burn rate-limit quota with no usable output — hard standing rule), all live compass topics, 100% cited, honest blank spokes where no public record exists, ZERO default values (chairs model, not polarity). Write one audit-only stance migration per member (1066-1072), apply each inline, then prove the full phase goal end-to-end via the 9 SQL/HTTP verification checks and a human checkpoint on ward-precise address routing + correct-person headshots + stance honesty.

Purpose: Closes CLARK-02 by satisfying Success Criterion 3 (evidence-only stances, 100% cited, honest blanks, zero defaults) and confirming all 4 phase success criteria are TRUE.
Output: 7 per-member stance migrations (audit-only, unregistered) + 162-03-SUMMARY.md with recorded query results + the human-verify outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP):**
- Stance RESEARCH is run by research agents, ONE AT A TIME (memory feedback_stance_research_one_at_a_time — parallel agents burn quota with no output). The executor WRITES the 7 stance migration .sql files to disk from the researched evidence; the executor does NOT apply them or run DB SELECTs.
- Applying each stance migration via `mcp__supabase-local__execute_sql` (or psql -f), and every verification SQL/HTTP/browse check, are INLINE ORCHESTRATOR steps performed by execute-phase.
- Stance migrations live in the SEPARATE repo C:/EV-Accounts/backend/migrations/ (commit via `git -C "C:/EV-Accounts"`). The SUMMARY.md goes in the essentials .planning phase dir.
- Each task marks executor-write / research-agent / inline-orchestrator-apply.
</execution_context>

<context>
@.planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md
@.planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md
@.planning/phases/162-city-of-las-vegas-deep-seed/162-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor + research agents need. Do NOT re-derive. -->
Government 'City of Las Vegas, Nevada, US' geo_id='3240000'; chamber 'Las Vegas City Council' (official_count=7)
Mayor district: geo_id='3240000', district_type='LOCAL_EXEC', state='nv' lowercase (Berkley sorts first via groupHierarchy.js LOCAL_EXEC-before-LOCAL ordering)
Ward districts: geo_id='las-vegas-nv-council-ward-N', district_type='LOCAL', mtfcc='X0015', state='nv' lowercase
7 members external_id -3205001 (Berkley/Mayor) .. -3205007 (Brune/Ward 6); the external_id→UUID map is in 162-01-SUMMARY.md
Stance tables (inform schema): inform.politician_answers (politician_id, topic_id, value); inform.politician_context (politician_id, topic_id, reasoning, sources); inform.compass_topics (topic_key, is_live)
topic_id MUST be resolved LIVE via JOIN inform.compass_topics ct ON ct.topic_key=s.topic_key AND ct.is_live=true — NEVER hardcode topic UUIDs; live/retired topic IDs in memory project_compass_live_topic_ids (retired UUIDs a9f53bc4, 45ca4740, f2a62698, 83eeb217, be60844f, c6957429 — do NOT use)
ON CONFLICT (politician_id, topic_id) DO UPDATE for both answers and context (idempotent)
Stances are CHAIRS (value = the discrete position the evidence matches, 1-5), NOT a directional polarity scale (memory feedback_compass_chairs_not_polarity)
No value for a topic = honest blank (no row); NEVER default to Neutral/Likely (memory feedback_stance_no_default_value)
No judicial topics (council members are not judges)
City-scope topics likely to have evidence: homelessness, homelessness-response (LV camping ban, shelter capacity), housing (affordable housing, community housing fund), public-safety-approach (LVMPD oversight, crime), transportation-priorities (transit/BRT/cycling), data-centers (Switch expansion), local-immigration (sanctuary/enforcement), local-environment (water conservation/SNWA), cannabis-regulation (NV legal cannabis + city zoning), economic-development (resort/casino economy, small business)
Mayor Berkley pre-tenure rule: former US House Rep 1999-2013 — use ONLY her LV mayoral record (Jan 2025-present) unless a federal-era topic (abortion/healthcare/immigration/taxes/etc.) has a RECENT mayoral statement that updates her position. Do NOT import the 20-year congressional record as current city stances.
Stance source tiers: lasvegasnevada.gov / city council agendas+minutes → thenevadaindependent.com → Las Vegas Review-Journal / KLAS 8NewsNow / Fox5Vegas → campaign sites/first-party statements
Realistic depth: target ~18-21 where documented; Berkley (Mayor, high profile) + Knudsen/Diaz (multi-term) likely richer; Kelley (appointed Sept 2025, thin record) + others may yield fewer. Honest blanks expected.
Migration mapping (all AUDIT-ONLY, NOT registered): 1066 Berkley / 1067 Knudsen / 1068 Kelley / 1069 Diaz / 1070 Allen-Palenske / 1071 Summers-Armstrong / 1072 Brune. Structural ledger stays at the confirmed number (1064). (If Wave-0 renumbered, shift these to match.)
Ward-routing address spot-check: pick a residential address in a known LV ward (e.g. a downtown/medical-district address for Ward 1) → returns Mayor + that one ward member, NOT all 6.
Browse link: essentials.empowered.vote/results?browse_geo_id=3240000&browse_mtfcc=G4110
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research stances (one agent at a time) + write 7 audit-only stance migrations 1066-1072</name>
  <files>C:/EV-Accounts/backend/migrations/1066_las_vegas_berkley_stances.sql, C:/EV-Accounts/backend/migrations/1067_las_vegas_knudsen_stances.sql, C:/EV-Accounts/backend/migrations/1068_las_vegas_kelley_stances.sql, C:/EV-Accounts/backend/migrations/1069_las_vegas_diaz_stances.sql, C:/EV-Accounts/backend/migrations/1070_las_vegas_allenpalenske_stances.sql, C:/EV-Accounts/backend/migrations/1071_las_vegas_summersarmstrong_stances.sql, C:/EV-Accounts/backend/migrations/1072_las_vegas_brune_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1057_clark_county_commission_naft_stances.sql (EXACT ANALOG — the CTE shape: VALUES(topic_key,val,reasoning,sources) → JOIN compass_topics is_live → INSERT politician_answers ON CONFLICT → INSERT politician_context ON CONFLICT; audit-only, unregistered)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md (section "migrations/1066-1072_..._stances.sql" — full CTE pattern, key constraints, topic keys likely to have LV evidence, Berkley pre-tenure rule)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md ("Pattern 4" stance CTE; source tiers; city-scope topics; realistic depth; Berkley federal-era pre-tenure rule)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-01-SUMMARY.md (the 7 external_id→UUID map minted by mig 1064; confirmed ledger numbers)
  </read_first>
  <action>
RESEARCH (one agent at a time — HARD RULE, never parallel): for each member in turn (Berkley → Knudsen → Kelley → Diaz → Allen-Palenske → Summers-Armstrong → Brune), dispatch a single stance-research agent that researches ALL live compass topics from public evidence (lasvegasnevada.gov council agendas/minutes → Nevada Independent → LV Review-Journal/KLAS/Fox5 → first-party/campaign statements). The agent returns, per topic with evidence: topic_key, the chair value (1-5 — the discrete position the evidence matches, NOT a polarity), an evidence-based reasoning string, and the source URL(s). Topics with no city-level evidence get NO entry (honest blank). ZERO defaults — never Neutral/Likely as a placeholder. No judicial topics. For Mayor Berkley apply the pre-tenure rule: use ONLY her LV mayoral record (Jan 2025-present) unless a federal-era topic has a recent mayoral statement updating it — do NOT mass-import her 1999-2013 congressional record. Wait for each agent to finish and record its output before dispatching the next (avoids the rate-limit burn documented in memory). Aim for ~18-21 depth across the roster where documented; honest blanks for thin records (esp. Kelley, appointed Sept 2025).

EXECUTOR STEP (per member): write the stance migration modeled verbatim on 1057_clark_county_commission_naft_stances.sql. Header comment: file name, Phase 162 (CLARK-02), the member's name + external_id, AUDIT-ONLY (NOT registered, structural ledger stays 1064), idempotent, CHAIRS model, 100% citation, no defaults, honest blanks, topic_id resolved live by topic_key, stance sources (lasvegasnevada.gov + Nevada Independent). BEGIN; then a CTE: `WITH s(topic_key, val, reasoning, sources) AS (VALUES (<topic_key>, <1-5>, '<reasoning citing the specific vote/statement>', ARRAY['<url>',...]), ...)` covering ONLY the researched topics-with-evidence; `t AS (SELECT s.*, ct.id AS topic_id FROM s JOIN inform.compass_topics ct ON ct.topic_key=s.topic_key AND ct.is_live=true)`; `ins_ans AS (INSERT INTO inform.politician_answers (politician_id, topic_id, value) SELECT '<uuid>', topic_id, val FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value RETURNING 1)`; then `INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) SELECT '<uuid>', topic_id, reasoning, sources FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning=EXCLUDED.reasoning, sources=EXCLUDED.sources;` COMMIT;. Use the real per-member UUID from 162-01-SUMMARY.md. NEVER hardcode topic UUIDs (always JOIN on topic_key + is_live). Do NOT register any of these in the ledger. If a member genuinely has no sourceable city-level stance, write a migration with zero VALUES rows (or document the full blank) — no fabrication. GREP-GATE HYGIENE: keep the ledger table token out of comments (paraphrase as "ledger registration").

Do NOT apply these migrations — that is the inline orchestrator step (Task 2).
  </action>
  <verify>
    <automated>P="C:/EV-Accounts/backend/migrations"; ok=1; for f in 1066_las_vegas_berkley_stances 1067_las_vegas_knudsen_stances 1068_las_vegas_kelley_stances 1069_las_vegas_diaz_stances 1070_las_vegas_allenpalenske_stances 1071_las_vegas_summersarmstrong_stances 1072_las_vegas_brune_stances; do test -f "$P/$f.sql" || ok=0; done; F="$P/1066_las_vegas_berkley_stances.sql"; grep -q "inform.politician_answers" "$F" &amp;&amp; grep -q "inform.politician_context" "$F" &amp;&amp; grep -q "compass_topics" "$F" &amp;&amp; grep -q "is_live = true" "$F" &amp;&amp; grep -q "ON CONFLICT (politician_id, topic_id)" "$F" &amp;&amp; [ "$ok" -eq 1 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 7 stance migrations (1066-1072) exist on disk, one per member in the correct name→file mapping.
    - Stances researched ONE agent at a time (no parallel dispatch); each agent covered all live compass topics.
    - Each migration uses the 1057 CTE shape: VALUES(topic_key, val, reasoning, sources) → JOIN inform.compass_topics ON topic_key AND is_live=true → INSERT politician_answers ON CONFLICT (politician_id, topic_id) DO UPDATE → INSERT politician_context ON CONFLICT (politician_id, topic_id) DO UPDATE.
    - topic_id resolved LIVE by topic_key + is_live=true — no hardcoded topic UUIDs; none of the 6 retired UUIDs appear.
    - Every placed stance has a non-empty reasoning string and ≥1 source URL (100% cited); values are discrete chairs (1-5), not a polarity scale.
    - No defaulted values (no Neutral/Likely placeholder); topics without evidence are simply absent (honest blank).
    - No judicial topics; Mayor Berkley's stances respect the pre-tenure rule (no mass federal-era import).
    - None of 1066-1072 contain a ledger-registration line (all audit-only).
    - Real per-member UUIDs (from 162-01-SUMMARY.md) used as politician_id.
  </acceptance_criteria>
  <done>7 evidence-only stance migrations written to disk, researched one agent at a time, chairs model, 100% cited, honest blanks, zero defaults, topic_id resolved live, all audit-only. Not yet applied.</done>
</task>

<task type="auto">
  <name>Task 2: Apply stance migrations inline + run the 9 SQL/HTTP verification checks</name>
  <files>.planning/phases/162-city-of-las-vegas-deep-seed/162-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-VALIDATION.md (Per-Task Verification Map — all checks + Section-Split SQL + ward-routing spot check)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md (Validation Architecture → Test Map; the exact SQL per check)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-02-SUMMARY.md (documented headshot gaps, if any, to subtract from the 7 count)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): apply each stance migration 1066-1072 via `mcp__supabase-local__execute_sql` (or psql -f). After each apply, spot-count the answers for that member. Then run the full 9-check verification suite via mcp__supabase-local__execute_sql (or curl for HTTP) and record every result in 162-03-SUMMARY.md:
(1) Council office count — offices in chamber 'Las Vegas City Council' under government 'City of Las Vegas, Nevada, US' → expect 7 (1 Mayor + 6 wards). Record.
(2) District-type split — exactly 1 LOCAL_EXEC office (Mayor, geo_id='3240000') + exactly 6 LOCAL ward offices (mtfcc='X0015'), all state='nv'. Record.
(3) Headshots present — politician_images (type='default') for external_id BETWEEN -3205007 AND -3205001 → expect 7 minus documented gaps (from 162-02-SUMMARY.md). Record + list gaps.
(4) Headshots serve — curl -sI each CDN url (all 7) → 200. Record.
(5) Evidence-only stances — `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -3205007 AND -3205001)` → expect ≥1 per sourced member; verify 100% have a paired politician_context row with non-null reasoning + sources (zero uncited), and zero defaulted/placeholder values. Record per-member counts + honest blanks.
(6) Section-split — run the LOCAL-tier section-split SQL → expect 0 rows. Record.
(7) Casing — DISTINCT state on districts linked to the Las Vegas City Council chamber → expect only 'nv' lowercase. Record.
(8) Ledger — `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('1064','1065','1066','1067','1068','1069','1070','1071','1072')` → expect only 1064 registered (1065-1072 audit-only). Record.
(9) Ward-precise routing — pick a residential address inside a known LV ward (e.g. a Ward 1 downtown/medical-district address) and run a /representatives/me-style probe or extend smoke-nv-geofences.ts → returns the Mayor + that ONE ward member, NOT all 6, and NOT a Strip/county-only result. Record the ward → member resolution.
Plus the coverage.js check: confirm src/lib/coverage.js COVERAGE_STATES contains the Nevada/Las Vegas entry (browseGovernmentList ['3240000'], hasContext:true). Record.
Flag ANY deviation (office count off, a non-'nv' state, any uncited/defaulted stance, any extra ledger entry, a ward resolving to the wrong member or all 6) as a gap for a follow-up gap-closure plan.
  </action>
  <verify>
    <automated>F=".planning/phases/162-city-of-las-vegas-deep-seed/162-03-SUMMARY.md"; test -f "$F" &amp;&amp; grep -qi "office" "$F" &amp;&amp; grep -qi "headshot" "$F" &amp;&amp; grep -qi "stance" "$F" &amp;&amp; grep -qi "section-split" "$F" &amp;&amp; grep -qi "ward" "$F" &amp;&amp; grep -qi "ledger" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: all 7 stance migrations applied; per-member answer counts recorded.
    - Check (1) recorded: council office count = exactly 7 (1 Mayor + 6 wards).
    - Check (2) recorded: exactly 1 LOCAL_EXEC + exactly 6 LOCAL ward offices, all at state='nv'.
    - Check (3) recorded: politician_images count = 7 minus documented gaps; gaps listed.
    - Check (4) recorded: CDN headshots return HTTP 200 (all 7 spot-checked).
    - Check (5) recorded: ≥1 stance per sourced member; 100% cited (paired context with reasoning + sources); zero defaulted values; honest blanks noted.
    - Check (6) recorded: section-split query returns 0 rows.
    - Check (7) recorded: linked districts show only state='nv' lowercase.
    - Check (8) recorded: only ('1064') registered; 1065-1072 audit-only, unregistered.
    - Check (9) recorded: a known-ward address resolves to Mayor + that one ward member (ward-precise), not all 6.
    - coverage.js COVERAGE_STATES Nevada/Las Vegas entry confirmed (hasContext:true, ['3240000']).
    - Any deviation flagged for gap closure.
  </acceptance_criteria>
  <done>All 7 stance migrations applied; all 9 checks + coverage check recorded in 162-03-SUMMARY.md with expected results; any deviation flagged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify ward-precise routing + correct-person headshots + stance honesty</name>
  <action>Pause for human verification. Present the what-built summary and the how-to-verify steps below; wait for the resume signal before marking the phase complete. No code or DB changes in this task.</action>
  <what-built>Seeded the City of Las Vegas (Mayor + 6 ward council members) under a standalone city government, with the project's FIRST custom non-TIGER ward geofences (6 X0015 ward polygons) for ward-precise routing, the directly-elected Mayor on a city-wide LOCAL_EXEC district (sorted first), 600x750 headshots (minus any documented genuine gap), and evidence-only compass stances (chairs model, 100% cited, honest blanks, zero defaults). Las Vegas surfaces with the purple hasContext chip in coverage.js COVERAGE_STATES. All 9 SQL/HTTP audits passed inline (7 offices = 1 LOCAL_EXEC + 6 LOCAL, ward routing, 7 headshots CDN-200, stances cited, casing 'nv', 0 section-split, structural ledger 1064).</what-built>
  <how-to-verify>
1. Ward-precise routing — open the app and enter a residential address in a KNOWN City of Las Vegas ward (try one per ward if possible). Confirm the LOCAL section returns the Mayor + the ONE correct ward council member (not all 6), with NO empty LOCAL section and NO section-split. (Use essentials.empowered.vote/results?browse_geo_id=3240000&browse_mtfcc=G4110 to see the full roster.)
2. Mayor-first ordering + directly-elected model — confirm Mayor Shelley Berkley sorts FIRST (above the ward member), labeled 'Mayor', and that NO ward member is shown as Mayor/Mayor Pro Tem as the city's mayor (Berkley is the directly-elected Mayor; Knudsen's Mayor-Pro-Tem role does not make him the mayor). If Berkley does not sort first, check groupHierarchy.js covers the LOCAL_EXEC-before-LOCAL ordering for the city tier.
3. Correct-person headshots — spot-check each profile. Confirm each photo is the right person, 4:5, no text/graphic overlay, not a placeholder. Confirm the Olivia Diaz photo (sourced from a 600x400 landscape, center-cropped + upscaled) looks acceptable; confirm any documented gap renders cleanly without a photo.
4. Stance honesty — open 2-3 profiles (include the Mayor + a multi-term ward member). Confirm stances render with citations, blank spokes are genuinely blank (no defaulted Neutral), values look like documented positions (chairs, not a forced polarity), and Berkley's stances reflect her city/recent record (not a mass-import of her 1999-2013 congressional votes). Party must NOT display anywhere (antipartisan — council races are nonpartisan).
5. Browse review — optionally review essentials.empowered.vote/results?browse_geo_id=3240000&browse_mtfcc=G4110.
  </how-to-verify>
  <verify><human-check>Operator confirms LV ward addresses route to the Mayor + the one correct ward member (no all-6, no empty LOCAL, no section-split), Berkley sorts first as the directly-elected Mayor, correct-person headshots (no overlay/placeholder), and evidence-only stances with honest blanks and no party display.</human-check></verify>
  <done>Human approves; or issues are logged and routed to a gap-closure plan.</done>
  <resume-signal>Type "approved" or describe any wrong-person photo, mis-routed ward (wrong member or all-6 shown), Mayor-ordering/model issue, defaulted stance, federal-era over-import, or party-display leak.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| research evidence → stance migration | researched chair values/citations cross into seed SQL; only evidence-backed values are written |
| migration file → live DB | service-role apply of seed SQL; only the inline orchestrator applies |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-10 | Tampering | fabricated/defaulted stance value (no evidence) | mitigate | Evidence-only chairs model; every stance has paired context with reasoning + source(s); check (5) asserts 0 uncited + 0 defaults; honest blanks where no record |
| T-162-11 | Tampering | Mayor federal-era record imported as current city stances | mitigate | Pre-tenure rule documented + enforced; human checkpoint step 4 confirms Berkley's stances reflect her city/recent record |
| T-162-12 | Spoofing | wrong topic via hardcoded topic UUID | mitigate | topic_id resolved live by topic_key + is_live=true (never hardcoded); 6 retired topic UUIDs excluded |
| T-162-13 | Repudiation | unverified claim of completion | mitigate | All 9 checks recorded with actual query results in SUMMARY; human checkpoint confirms ward routing + correct-person photos + stance honesty |
| T-162-14 | Tampering | data-integrity drift (section-split / casing / wrong-ward routing) | mitigate | Section-split SQL gate (0 rows) + casing check ('nv') + ward-precise routing probe (check 9) run inline; structural ledger asserted |
| T-162-SC | Tampering | npm/pip/cargo installs | accept | No installs (stance SQL + verification only); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
All four phase success criteria proven:
1. A City of Las Vegas address returns the Mayor + the one correct ward council member via X0015 routing, no empty LOCAL section (checks 1+2+9 + browse step 1).
2. All 7 members render with 600x750 headshots, gaps documented (checks 3-4 + browse step 3).
3. Evidence-only stances render, 100% cited, honest blanks, zero defaults (check 5 — SC#3 + browse step 4).
4. Las Vegas surfaces with the purple hasContext chip (coverage.js check + browse step 5).
Cross-cutting: casing 'nv' (check 7), 0 section-split (check 6), structural ledger 1064 (check 8), Mayor sorts first as directly-elected (browse step 2).
</verification>

<success_criteria>
- 7 per-member stance migrations (1066-1072) written, researched one agent at a time, applied; evidence-only, 100% cited, honest blanks, zero defaults, chairs model; Berkley pre-tenure rule respected.
- 162-03-SUMMARY.md records all 9 SQL/HTTP checks + the coverage check with expected results.
- Human checkpoint approves ward-precise routing, Mayor-first ordering (directly elected), correct-person headshots (no overlay), and stance honesty (no party display).
- All stance migrations audit-only (structural ledger stays 1064); any deviation routed to a gap-closure plan.
</success_criteria>

<output>
Create `.planning/phases/162-city-of-las-vegas-deep-seed/162-03-SUMMARY.md` when done (record per-member stance counts + honest blanks, the 9 verification results incl. ward-precise routing, and the human-verify outcome).
</output>
