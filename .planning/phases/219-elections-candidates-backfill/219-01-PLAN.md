---
phase: 219-elections-candidates-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md
autonomous: false
requirements: [COLLIN-ELECT-01]
user_setup: []

must_haves:
  truths:
    - "The next-free migration number in C:/EV-Accounts/backend/migrations is confirmed (research says 1393) AND a full locked migration-number map (one number per Wave-2 plan 02..08, contiguous from MAX+1) is recorded in PREFLIGHT.md so each parallel Wave-2 plan consumes a fixed assignment and none re-decides its own number."
    - "The shared '2026 Texas Municipal General' election row (2026-05-02, TX) is confirmed to resolve to exactly one elections.id."
    - "For each of the 12 zero-race governments a reference-cycle decision (shared-2026-05-02 / fallback-year / own-runoff-row) is recorded with a citation (D-01)."
    - "Lavon, Saint Paul, Melissa and Josephine reference-cycle + roster leads are resolved from a cited source, or explicitly documented as legitimately-empty for a seat with no election found (D-06)."
    - "McKinney Mayor + At-Large 1 runoff finals, Richardson May-2025 date + Place 6 runoff final, and Longview D3 winner are re-confirmed against a cited source."
  artifacts:
    - ".planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md"
  key_links:
    - "PREFLIGHT.md reference-cycle decision per city → the seeding plan (02..08) that consumes it."
  prohibitions:
    - statement: "No fabricated candidate/roster — an unresolved city stays documented-open, never guessed."
      status: enforced
      verification: "Every roster lead in PREFLIGHT.md carries a citation URL or an explicit '[OPEN — no source]' marker."
    - statement: "No DB writes in this plan — it is read/probe + research only."
      status: enforced
      verification: "This plan authors no SQL migration; only PREFLIGHT.md is written."
  backstop:
    - statement: "candidate_staging may hold cron-proposed rows the operator can reuse instead of fresh research."
      verification: backstop
---

<objective>
Resolve the three RESEARCH.md open-question pre-checks and lock a cited reference-cycle decision for every zero-race government BEFORE any seeding migration is authored.

Purpose: The hard part of Phase 219 is research (which cycle, who ran, who won — per city, per staggered seat), not SQL mechanics. This plan removes the ambiguity (Melissa props-vs-candidate conflict, Lavon/Saint Paul fully-open, McKinney/Richardson runoff finals) up front so the Wave-2 seeding plans work from settled facts.
Output: `219-PREFLIGHT.md` — a cited, per-city reference-cycle + roster-lead ledger plus confirmed migration counter and shared election_id.
</objective>

<assumption_delta_decision>
The `assumption-delta scan "219"` returned `detected:true`, but the sole signal is the pluralization word "also" in the ROADMAP prose ("the 3 reconciled cities also have 0 races") — a regex false-positive, not a competing implementation of a responsibility. There is exactly ONE seeding mechanism (the `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING` idiom) and ONE election-linkage model. The "shared-2026-05-02 row vs. fallback-year row" branch is NOT two designs for one responsibility — it is the single correct election row selected per each city's actual, cited election date. Decision: no promote-vs-add-alongside action required; no identity to reconcile.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-elections-candidates-backfill/219-CONTEXT.md
@.planning/phases/219-elections-candidates-backfill/219-RESEARCH.md
@.planning/phases/219-elections-candidates-backfill/219-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator runs the three pre-check probes (executor has no DB access)</name>
  <read_first>
    - .planning/phases/219-elections-candidates-backfill/219-RESEARCH.md §"Open Questions" #4 (candidate_staging probe SQL), §"Standard Stack" (migration numbering), §Pattern 1 (shared election resolution).
    - C:/EV-Accounts/backend/migrations/099_collin_county_discovery_jurisdictions.sql (shared election row definition + per-city source_url map).
  </read_first>
  <action>Operator-run checkpoint (executor has no DB access): run the three probes in how-to-verify against the production DATABASE_URL and paste their output so Task 2 can record the migration counter, shared election_id, and any reusable candidate_staging rows.</action>
  <what-built>The executor cannot query Supabase (no MCP). This checkpoint hands the operator three concrete probes and captures their output into PREFLIGHT.md.</what-built>
  <how-to-verify>
    1. Confirm next-free migration number: `ls C:/EV-Accounts/backend/migrations | grep -oE '^[0-9]+' | sort -n | tail -1` — record MAX+1 (research expects 1393).
    2. Confirm the shared election resolves to exactly one id (run against production DATABASE_URL):
       `SELECT id FROM essentials.elections WHERE name='2026 Texas Municipal General' AND election_date='2026-05-02' AND state='TX';` — must return exactly one row.
    3. Probe candidate_staging for the 12 zero-race cities (verbatim SQL from RESEARCH §Open Question #4) — join defensively by BOTH jurisdiction_name and geo_id (Plano/Van Alstyne stale-geoid caveat). Paste any pending/flagged rows.
    Paste all three outputs back so Task 2 records them.
  </how-to-verify>
  <acceptance_criteria>
    - Migration MAX+1 recorded (a specific integer).
    - Shared election query returns exactly 1 id (recorded), OR the operator reports 0 rows (then Task 2 must add a mint-by-name step to the shared-tier plans).
    - candidate_staging probe output pasted (rows or explicit "0 rows").
  </acceptance_criteria>
  <resume-signal>Paste the three probe outputs, or type "no staging rows / counter=NNNN / election_id=..." to resume.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Resolve unresolved cities + runoff finals, write 219-PREFLIGHT.md</name>
  <read_first>
    - .planning/phases/219-elections-candidates-backfill/219-RESEARCH.md §"Per-City Research Findings" (the [OPEN]/[RE-VERIFY] tags), §"Assumptions Log" A2/A4/A5, §"Sources".
    - The per-city source_url/allowed_domains from migration 099 for Lavon (lavontx.gov), Saint Paul (stpaultexas.us), Melissa (cityofmelissa.com/287/Elections), Josephine (cityofjosephinetx.com).
  </read_first>
  <action>
    Using WebSearch/WebFetch (executor CAN do this) resolve, per D-01/D-06, and write `.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md`:
    (a) Melissa true May-2026 ballot content — fetch the Collin County canvass PDF for the May-2-2026 joint election and cityofmelissa.com/287/Elections; record whether Melissa had council candidate races (shared-tier) or was ISD/props-only (fallback/own-cycle). Resolve the migration-100-comment-vs-WebSearch conflict (RESEARCH A2).
    (b) Lavon — fresh research from lavontx.gov/election-information; record most-recent held election + reference cycle.
    (c) Saint Paul — disambiguated query "St. Paul Collin County Texas" (NOT Minnesota) against stpaultexas.us; record most-recent held election + reference cycle.
    (d) Josephine — per-seat cycle check (May-2026 was props-only per migration 100); record each of 6 offices' most-recent election year or mark legitimately-open.
    (e) Re-confirm runoff/close finals: McKinney Mayor (Cox v Sanford) + At-Large 1 (Lynch v Garrison) June-2025 finals; Richardson exact May-2025 date (May 3 vs the "May 6" headline) + Place 6 runoff final (Voelker won Mayor); Longview D3 runoff winner (Brandon Smith 223-204).
    For EACH of the 12 zero-race cities record a one-line reference-cycle decision: `shared-2026-05-02` | `fallback-YYYY-MM-DD (own election row)` | `own-runoff-row`, each with a citation URL or an explicit `[OPEN — seed at execute from canvass]` marker. Also record the confirmed migration counter and shared election_id from Task 1, and whether candidate_staging offered any reusable cited rows (per city). **Lock the full migration-number map** — assign each Wave-2 plan (02, 03, 04, 05, 06, 07, 08) one contiguous migration number starting at the confirmed MAX+1 (e.g. 02→1393 … 08→1399), and write this map into PREFLIGHT.md as the single arbitration point. Wave-2 plans MUST consume their assigned number from this map rather than each independently re-deciding a shifted block (prevents filename collisions / out-of-order gaps across the 7 parallel plans). Do NOT write any SQL migration in this plan.
  </action>
  <verify>
    <automated>test -f ".planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md" && grep -c "reference-cycle\|shared-2026-05-02\|fallback-\|own-runoff-row" ".planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md"</automated>
  </verify>
  <acceptance_criteria>
    - PREFLIGHT.md exists and contains a reference-cycle decision line for all 12 zero-race cities (Blue Ridge, Farmersville, Josephine, Lavon, McKinney, Melissa, Nevada, Saint Paul, Weston, Plano, Richardson, Van Alstyne).
    - Melissa's ballot-content conflict is resolved to a single documented read with a citation.
    - Lavon and Saint Paul each have a most-recent-election finding OR an explicit `[OPEN — seed at execute from canvass]` marker (never a guess).
    - McKinney/Richardson runoff finals and Longview D3 winner each carry a citation URL.
    - Migration counter (MAX+1) and shared election_id recorded verbatim from Task 1.
    - A locked migration-number map assigns exactly one contiguous number to each of the 7 Wave-2 plans (02..08) starting at MAX+1, recorded in PREFLIGHT.md as the single source of truth for numbering.
  </acceptance_criteria>
  <done>Every zero-race city has a cited, settled reference-cycle decision the Wave-2 plans can seed from; open seats are documented-open, not fabricated.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md` (new — cited reference-cycle + roster-lead ledger; consumed by plans 219-02..219-08)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external web → PREFLIGHT.md | Untrusted civic-data claims cross here; every claim must carry a citation or an [OPEN] marker |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Tampering/Info disclosure | roster leads in PREFLIGHT.md | high | mitigate | Evidence-only (D-06); every lead cited or marked [OPEN — no source]; never guess a roster |
| T-219-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (pure research + markdown); legitimacy gate N/A per its own scope condition |
</threat_model>

<verification>
- PREFLIGHT.md present with 12 cited reference-cycle decisions.
- Migration counter + shared election_id recorded.
- No SQL migration authored in this plan (data-only research + operator probes).
</verification>

<success_criteria>
Every Wave-2 seeding plan can begin from a settled, cited reference-cycle decision for its cities; the three RESEARCH open questions are closed or explicitly documented-open.
</success_criteria>

<output>
Create `.planning/phases/219-elections-candidates-backfill/219-01-SUMMARY.md` when done.
</output>
