---
phase: 90-post-election-follow-up-minicompass-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Transparent Motivations/essentials/supabase/migrations/272_me_winners_lavote_update.sql
autonomous: true
requirements:
  - POST-ELECTION-01
  - POST-ELECTION-02
user_setup: []

must_haves:
  truths:
    - "ME June 9 primary D-winner is present as a race_candidates row in the US Senate 2026 general race (state='23', election_date='2026-11-03', position_name LIKE 'U.S. Senator')"
    - "ME June 9 primary D-winner is present as a race_candidates row in the ME-01 2026 general race (state='23', NATIONAL_LOWER, district CD-01) — or, if Pingree ran uncontested, her existing row is confirmed and no duplicate is inserted"
    - "ME June 9 primary D-winner is present as a race_candidates row in the ME-02 2026 general race (state='23', NATIONAL_LOWER, district CD-02) — open seat (Golden not running per RESEARCH.md)"
    - "If lavote.gov has published the November 2026 CA general election ID by execution time, essentials.discovery_jurisdictions row for jurisdiction_geoid='06037' has source_url updated to the November 2026 lavote URL with the correct ?id=XXXX value and election_date updated to '2026-11-03'"
    - "If lavote.gov has NOT yet published the November 2026 ID at execution time, the deferred UPDATE is documented in the SUMMARY with a probe timestamp and the expected follow-up path"
    - "Migration 272 is idempotent — re-running inserts 0 additional politician/race_candidate rows"
  artifacts:
    - path: "supabase/migrations/272_me_winners_lavote_update.sql"
      provides: "ME June 9 primary winners INSERTed into 3 general races; lavote.gov discovery_jurisdictions UPDATE (conditional on Nov 2026 ID availability)"
      contains: "INSERT INTO essentials.race_candidates"
  key_links:
    - from: "supabase/migrations/272_me_winners_lavote_update.sql Section A"
      to: "essentials.race_candidates (US Senate general, ME-01 general, ME-02 general)"
      via: "INSERT … SELECT … WHERE NOT EXISTS (no unique constraint on race_id+full_name)"
      pattern: "INSERT INTO essentials\\.race_candidates"
    - from: "supabase/migrations/272_me_winners_lavote_update.sql Section B"
      to: "essentials.discovery_jurisdictions (jurisdiction_geoid='06037', LA County)"
      via: "UPDATE source_url + election_date"
      pattern: "UPDATE essentials\\.discovery_jurisdictions"
---

<objective>
Add the Maine June 9, 2026 primary winners as race_candidates in the three relevant ME general races (US Senate, ME-01, ME-02), and update the LA County (lavote.gov) discovery_jurisdictions row's source_url to point at the November 2026 election ID — if and only if lavote.gov has published that ID by execution time.

Purpose: Both updates are time-sensitive operational data — POST-ELECTION-01 cannot be authored before the June 9 primary is decided, and POST-ELECTION-02 cannot be authored before lavote.gov assigns the November 2026 election ID. Per CONTEXT.md D-01 + D-02, this plan executes after June 9, in a single migration.

Output:
- A single new migration file `supabase/migrations/272_me_winners_lavote_update.sql` applied to Supabase (which IS production per project memory)
- Section A: 3 race_candidates INSERTs (one per ME general race the D-primary affects) plus politician INSERTs for any winners not already in `essentials.politicians`
- Section B (conditional): UPDATE of the lavote.gov discovery_jurisdictions row with the November 2026 election ID, OR a clearly-commented "deferred — November ID not yet published as of [timestamp]" block
- Updated `STATE.md` "Next migration:" line from 272 to 273
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-CONTEXT.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-RESEARCH.md
@.planning/phases/90-post-election-follow-up-minicompass-ui/90-PATTERNS.md
@supabase/migrations/197_ca_governor_challengers.sql
@supabase/migrations/241_or_discovery_jurisdictions.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-probe primary winners + lavote November ID, then write migration 272</name>
  <files>C:/Transparent Motivations/essentials/supabase/migrations/272_me_winners_lavote_update.sql</files>
  <read_first>
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-RESEARCH.md (ME Primary Context section — known seeded politician IDs Collins=-230101, King=-230102, Pingree linked Phase 51, Golden NOT running; Pitfall 2 — primary winners may need new politician rows; Pitfall 4 — race_candidates direct insert, NOT staging; Common Pitfall 1 — lavote ID may not exist yet)
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-PATTERNS.md (Section migration patterns; race_candidates uses WHERE NOT EXISTS — NOT ON CONFLICT — because no unique constraint on race_id+full_name; race ID subquery uses state + election_date + position_name + primary_party IS NULL)
    - supabase/migrations/197_ca_governor_challengers.sql (lavote.gov INSERT pattern — Section D — shows the source_url format `https://www.lavote.gov/Apps/CandidateList/Index?id=4338`; jurisdiction_geoid='06037' for LA County)
    - supabase/migrations/241_or_discovery_jurisdictions.sql (most recent discovery_jurisdictions pattern — confirms columns: id, jurisdiction_geoid, jurisdiction_name, state, election_date, source_url, allowed_domains; NO election_id column — the ID lives inside source_url)
    - .planning/STATE.md (verify "Next migration: 272" — should match 272 in the filename)
    - C:/Users/Chris/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_supabase_local_is_remote.md (mcp__supabase-local IS production — treat writes as live)
  </read_first>
  <action>
    Per CONTEXT.md D-01: this task assumes June 9, 2026 has already passed at execution time. Per D-02 and D-03: both POST-ELECTION items are bundled into one migration.

    **Phase A — Live data probes (BEFORE writing SQL):**

    1. **ME primary winners.** For each of the three races (US Senate, ME-01, ME-02), identify the Democratic-primary winner from a citable public source — Maine Secretary of State results, AP, Ballotpedia, etc. Record candidate full name and any external identifiers.
       - Note from RESEARCH.md: ME-01 Pingree is the incumbent; if she ran uncontested in the D primary, she may already be in race_candidates from migration 183. Verify before inserting.
       - Note from RESEARCH.md: ME-02 is an open seat (Golden not running); the D winner here is genuinely new.
       - Note from RESEARCH.md: ME US Senate D primary had multiple candidates (Platner, Costello, possibly others) — identify which D candidate won.

    2. **Politician pre-check.** Use mcp__supabase-local to query existing ME politicians:
       ```sql
       SELECT id, full_name, external_id
       FROM essentials.politicians
       WHERE external_id BETWEEN -2399999 AND -2300100
       ORDER BY external_id;
       ```
       For each D-primary winner, determine whether they are already in the table. If yes, capture the existing `id` and `external_id`. If no, plan to INSERT a new row using the next available external_id in the `-23xxxx` range (find the lowest unused value by reading the result set).

    3. **Race pre-check.** Query the three target race rows so the migration uses verified subqueries (NOT hardcoded UUIDs per RESEARCH.md Pattern 1):
       ```sql
       SELECT r.id, r.position_name, r.primary_party, e.election_date, e.state
       FROM essentials.races r
       JOIN essentials.elections e ON e.id = r.election_id
       WHERE e.state = '23' AND e.election_date = '2026-11-03'
       ORDER BY r.position_name;
       ```
       Confirm all three races (US Senator, U.S. Representative District 1, U.S. Representative District 2) exist with `primary_party IS NULL` (general election rows, not party-primary scaffolds).

    4. **Existing candidate pre-check.** For each of those three race IDs, list current race_candidates rows:
       ```sql
       SELECT rc.full_name, rc.politician_id, rc.is_incumbent, rc.candidate_status
       FROM essentials.race_candidates rc
       WHERE rc.race_id IN (<race_id_1>, <race_id_2>, <race_id_3>);
       ```
       If a D-winner is already present (e.g., Pingree as incumbent), do not INSERT a duplicate. The WHERE NOT EXISTS guard will handle this defensively too, but knowing in advance avoids no-op INSERTs and clarifies the SUMMARY.

    5. **lavote November 2026 probe.** WebFetch `https://www.lavote.gov/Apps/CandidateList/Index`. Inspect the election dropdown for any November 3, 2026 General Election entry. If present, extract the `?id=XXXX` parameter value. If absent, defer the UPDATE per RESEARCH.md Pitfall 1.

    **Phase B — Write migration `supabase/migrations/272_me_winners_lavote_update.sql`:**

    Use the section-header pattern from migration 197 (per PATTERNS.md). Header:
    ```
    -- Migration 272: ME June 9 primary winners + lavote November 2026 election ID update
    -- Phase 90 Plan 03: POST-ELECTION-01 (race_candidates) + POST-ELECTION-02 (discovery_jurisdictions)
    -- Applied: <date>
    ```

    **Section A — ME primary winners:**
    - For each D-winner not already in `essentials.politicians`: an `INSERT INTO essentials.politicians (id, full_name, external_id, ...) SELECT gen_random_uuid(), '<name>', <next-23xxxx>, ... WHERE NOT EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id = <next-23xxxx>);` block. Use ON CONFLICT DO NOTHING on primary key as belt-and-suspenders.
    - For each D-winner / target race pair: an INSERT into `essentials.race_candidates` using the race_id subquery (NOT a hardcoded UUID) and WHERE NOT EXISTS guarded on `(race_id, full_name)` per PATTERNS.md Pattern A — because `race_candidates` has no unique constraint, ON CONFLICT is a no-op.
    - Required fields per PATTERNS.md elections-seed template: `id` (gen_random_uuid), `race_id` (subquery), `full_name`, `politician_id` (existing or newly-inserted), `is_incumbent` (true only for Pingree in ME-01).
    - For ME-01 Pingree: include a SELECT against existing race_candidates first to detect duplicate; if present, write the INSERT with a WHERE NOT EXISTS guard but flag in the SUMMARY whether it was an actual insert or a no-op.

    **Section B — lavote.gov UPDATE (CONDITIONAL):**

    Case 1: lavote published November 2026 ID. Write the UPDATE per PATTERNS.md Analog B:
    ```sql
    UPDATE essentials.discovery_jurisdictions
    SET source_url = 'https://www.lavote.gov/Apps/CandidateList/Index?id=<NEW_ID>',
        election_date = '2026-11-03'
    WHERE jurisdiction_geoid = '06037'
      AND source_url LIKE '%lavote.gov%';
    ```

    Case 2: lavote has NOT published. Insert a clearly-commented placeholder block instead of the UPDATE:
    ```sql
    -- ============================================================
    -- Section B: lavote.gov UPDATE — DEFERRED
    -- November 3, 2026 General Election ID not yet published on lavote.gov/Apps/CandidateList/Index
    -- Probe timestamp: <ISO-8601>
    -- Follow-up: Re-probe weekly until ID appears; then write migration 269 with the UPDATE.
    -- ============================================================
    ```
    Do NOT hardcode an assumed ID like 4339 — RESEARCH.md A1 marks this as LOW confidence.

    **Phase C — Apply and verify:**

    1. Apply via `mcp__supabase-local__apply_migration` with name `272_me_winners_lavote_update`.
    2. Re-run the race_candidates pre-check query — confirm the new D-winner rows now appear.
    3. Re-run the lavote source_url query — confirm UPDATE applied OR confirm the row is unchanged (deferred case).
    4. Run the migration a second time to confirm idempotency — count gates must report 0 new inserts.
    5. Update `.planning/STATE.md` to change `Next migration: 272` to `Next migration: 273` in the Accumulated Context section.

    **Phase D — Reachability check:**

    Hit a Maine address against the live API (e.g., a Portland ME address) and inspect the returned ballot for the US Senate, ME-01, and ME-02 races — confirm the new D-winner appears in `race_candidates` payload alongside the incumbents/R challengers already seeded. Document the address and response in the SUMMARY.
  </action>
  <verify>
    <automated>
      Test 1 (POST-ELECTION-01 count gate): `SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.races r ON rc.race_id = r.id JOIN essentials.elections e ON r.election_id = e.id WHERE e.state='23' AND e.election_date='2026-11-03' AND r.position_name IN ('U.S. Senator','U.S. Representative District 1','U.S. Representative District 2') AND r.primary_party IS NULL` — must be ≥ the pre-migration count + (number of D-winners inserted), and at minimum ≥ 3 across the three races combined after migration.

      Test 2 (POST-ELECTION-01 row-presence): For each D-winner identified in the probes, `SELECT 1 FROM essentials.race_candidates WHERE race_id = (<lookup subquery>) AND full_name = '<winner full name>'` returns 1 row.

      Test 3 (POST-ELECTION-02 conditional): If Section B applied, `SELECT source_url, election_date FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid = '06037' AND source_url LIKE '%lavote%'` returns source_url containing the new id and election_date = '2026-11-03'. If Section B deferred, the same query returns the prior id=4338 source_url and the migration file contains the literal token `DEFERRED`.

      Test 4 (idempotency): Apply migration 272 twice (locally simulated by running the SQL a second time via mcp__supabase-local). Row counts on `essentials.race_candidates` for the affected races and on `essentials.discovery_jurisdictions` for geoid '06037' do not change between the first and second runs.

      Test 5 (STATE.md update): grep "Next migration: 273" .planning/STATE.md — must return ≥ 1 match; grep "Next migration: 272" .planning/STATE.md — must return 0 matches.
    </automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/272_me_winners_lavote_update.sql` exists with the header line `-- Migration 272: ME June 9 primary winners + lavote November 2026 election ID update`
    - Section A contains exactly one INSERT INTO essentials.race_candidates block per D-winner / general race pair (3 races total; rows count = winners not already seeded)
    - Section A uses race ID subqueries `(SELECT r.id FROM essentials.races r JOIN essentials.elections e ON e.id = r.election_id WHERE e.state = '23' AND e.election_date = '2026-11-03' AND r.position_name = '<exact position name>' AND r.primary_party IS NULL LIMIT 1)` — NOT hardcoded UUIDs
    - Section A uses WHERE NOT EXISTS guards on race_candidates inserts (NOT ON CONFLICT — race_candidates has no unique constraint per PATTERNS.md GOTCHA)
    - Any politician INSERTs (for previously-unseeded winners) use external_id in the -23xxxx range, with the next-available value verified by the pre-check query
    - Section B is EITHER (a) a proper UPDATE on `essentials.discovery_jurisdictions` matching jurisdiction_geoid='06037' with source_url containing a real `?id=` value and election_date='2026-11-03', OR (b) a `-- DEFERRED` comment block stating the probe timestamp — never a hardcoded assumed ID
    - Migration applied successfully via mcp__supabase-local__apply_migration with no errors
    - Re-applying the same SQL produces zero new rows in race_candidates and zero changes to discovery_jurisdictions (idempotent)
    - The Maine address reachability check (Phase D) returns at least one of the D-winners in the live `/elections` API response for the appropriate race row
    - `.planning/STATE.md` "Next migration:" line is bumped from 272 to 273
  </acceptance_criteria>
  <done>Migration 272 applied; all three ME D-winners appear in race_candidates for their respective general races; lavote.gov source_url is either updated (if Nov 2026 ID was published) or explicitly deferred with a timestamped comment; STATE.md migration counter bumped; idempotency verified; live ME ballot lookup shows the new D-winner in the API response.</done>
</task>

</tasks>

<verification>
- File `supabase/migrations/272_me_winners_lavote_update.sql` exists and is applied
- 3 race_candidates rows added (or confirmed pre-existing for Pingree case)
- lavote.gov UPDATE applied OR deferred with timestamped comment
- Idempotency verified (second application produces 0 changes)
- STATE.md "Next migration:" reads 273
- A real ME address lookup against the live API returns the new D-winner in the appropriate race
</verification>

<success_criteria>
- POST-ELECTION-01 satisfied: ME June 9 primary winners appear in race_candidates for US Senate general, ME-01 general, ME-02 general
- POST-ELECTION-02 satisfied: lavote.gov source_url updated to November 2026 ID, OR deferred with documented reason and follow-up path
- Migration is idempotent and follows the section-header convention from migration 197
- STATE.md migration counter accurate
- No hardcoded race UUIDs; no hardcoded assumed lavote ID
</success_criteria>

<output>
Create `.planning/phases/90-post-election-follow-up-minicompass-ui/90-03-SUMMARY.md` when done. Include: D-winner names and politician_ids (existing vs newly created) for each of the three races, whether the lavote UPDATE applied or was deferred (with probe timestamp + URL inspected if deferred), Maine address used for the reachability check + the candidate names returned, and the new value of STATE.md "Next migration:".
</output>
