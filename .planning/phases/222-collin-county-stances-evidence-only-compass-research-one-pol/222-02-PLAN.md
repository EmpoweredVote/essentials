---
phase: 222-collin-county-stances-evidence-only-compass-research-one-pol
plan: 02
type: execute
wave: 2
depends_on: ["222-01"]
files_modified:
  - "[EV-Accounts] C:/EV-Accounts/backend/migrations/<live-next-number>_222_plano_stances.sql (NEW — number assigned live)"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md (APPEND)"
autonomous: false
requirements: [COLLIN-STANCE-01, COLLIN-STANCE-02]
must_haves:
  truths:
    - "Every un-stanced Plano officeholder on the 222-01 live worklist was individually attempted, ONE AT A TIME, never two research passes concurrently (D-01, D-03)"
    - "Every stance written carries a matching inform.politician_context row whose reasoning names a specific dated action, quote, vote, or questionnaire answer and whose sources array holds at least one real URL that was actually fetched (D-04, D-05)"
    - "Every Plano worklist name appears in exactly one of {this plan's applied migration rows, 222-CONFIRMED-BLANK.md} — never neither, never both (COLLIN-STANCE-01)"
    - "A Plano officeholder with no explicit on-topic evidence produces zero inform.politician_answers rows and exactly one blank-register entry naming the sources actually checked; an all-blank government is a SUCCESS outcome (COLLIN-STANCE-01/empty, COLLIN-STANCE-02/empty)"
    - "This plan writes only (politician_id, topic_id) pairs that had no prior row per the 222-01 do-not-overwrite set — no pre-existing stance is modified (COLLIN-STANCE-01/adjacency, D-07)"
    - "Every value is a whole integer 1 through 5 — never fractional, never a defaulted middle value (COLLIN-STANCE-02/adjacency)"
    - "100% of Plano stances, and 100% of any stance valued 1 or 5, were re-verified against their cited source before the migration was committed (222-VALIDATION.md resample contract)"
    - "The migration is a single BEGIN/COMMIT transaction using ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables, so re-applying it changes no row count and no answer row can exist without its context row (COLLIN-STANCE-02/idempotency, COLLIN-STANCE-02/concurrency)"
    - "The migration file is committed to the EV-Accounts repo locally and that repo is NOT pushed by this plan"
    - "The migration has been APPLIED to production and the evidence-integrity gate returns zero rows for this plan's politician_ids — authored-but-unapplied SQL does not count as done"
  artifacts:
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
      provides: "The Plano section of the per-person blank register (D-08)"
      contains: "## Plano"
  key_links:
    - "Stances attach by politician_id taken verbatim from 222-01-WORKLIST.md — never by name match, and never by geo_id (there is no unique geo_id)"
    - "topic_id values come from the 11 canonical UUIDs verified live in 222-01; question and answer wording comes from 222-RESEARCH.md section B, which overrides the researcher agent file for housing, taxes, and healthcare"
  prohibitions:
    - "MUST NOT infer or assign any stance from party affiliation, endorsement, donor history, or pattern-matching to another official in the same city — only an explicit, on-topic, cited statement, recorded vote, or questionnaire answer may set a chair"
    - "MUST NOT assign a chair when the evidence is suggestive-but-not-explicit, and MUST NOT default to a middle value to avoid leaving a blank — a blank spoke is the correct, successful outcome"
    - "MUST NOT record a source URL that was not actually fetched and read, or that does not contain the cited quote, vote, or questionnaire answer"
    - "MUST NOT attribute a stance to an officeholder on the basis of a same-named person in a different city or state; the source must confirm the Texas city before the evidence is used"
    - "MUST NOT name, imply, or rely on a politician's party in reasoning text — party is never used for inference and never displayed"
---

<objective>
Evidence-only compass stance research for every un-stanced **Plano** (geo_id `4858016`) officeholder on the 222-01 live worklist, one politician at a time, across all 11 canonical topics — then author, commit, and apply the Plano stance migration and record every honest blank.

Purpose: Plano is the largest Collin city and the highest-evidence jurisdiction in the phase (RESEARCH.md section C, High tier). D-02 puts it first because hit-rate is best here, and its profiles are the highest-traffic — a wrong chair here is seen by real users first.
Output: one AUDIT-ONLY migration in the EV-Accounts repo, a Plano section in `222-CONFIRMED-BLANK.md`, and applied production rows.
</objective>

## Artifacts this phase produces

Phase 222 (all plans) produces data + planning artifacts only. No application code, no schema change, no packages.

- **[EV-Accounts repo]** `C:/EV-Accounts/backend/migrations/<N>_222_<slug>_stances.sql` — one numbered, git-committed, **AUDIT-ONLY** (NOT registered in `schema_migrations`) migration per research plan. Numbers assigned LIVE. This plan owns `<N>_222_plano_stances.sql`.
- **[essentials repo]** `.planning/phases/222-.../222-01-WORKLIST.md` (222-01), `222-CONFIRMED-BLANK.md` (created 222-01, appended by every research plan), `222-NN-SUMMARY.md` per plan.
- **[essentials repo]** `.planning/REQUIREMENTS.md` — COLLIN-STANCE-01/02 + traceability (222-01).
- **[essentials repo]** `src/lib/coverage.js` — `hasContext: true` reconcile (222-18 only).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research every un-stanced Plano officeholder, one politician at a time</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the Plano rows are this task's exact scope; take politician_id and full_name verbatim from here.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B is the SOURCE OF TRUTH for the verbatim question and five answer options of all 11 topics. It OVERRIDES the embedded scale text in the researcher agent file for `housing`, `taxes`, and `healthcare` (Pitfall 1). Also read section C (Plano is High tier) and Pitfall 2 (homonym trap).
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — reuse its evidence hierarchy, its three quote-selection gates, its inversion-trap table, and its de-identification protocol. Ignore its housing/taxes/healthcare scale text.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md` — D-03, D-04, D-05, D-06, D-07.
  </read_first>
  <files>(no file writes this task — research output is held for Task 2)</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Never dispatch two research passes concurrently — parallel fan-outs burn API quota and this is a standing user rule, not a preference. Process the Plano worklist rows in the order they appear in 222-01-WORKLIST.md, mayor first.

    For each person, run one research pass. Prefer dispatching the `politician-stance-researcher` subagent; if that project-scoped agent does not resolve from this session, dispatch a single general-purpose research subagent seeded with the methodology read from `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md`. Either way, pass the full verbatim question plus five answer options for all 11 topics from RESEARCH.md section B — do not let the agent fall back to its own embedded wording. Use WebFetch as the primary tool; a light WebSearch budget of a few calls per person is allowed only to discover a URL when direct fetch patterns fail (the agent file's blanket no-WebSearch rule exists for parallel sibling dispatch, which D-03 has already eliminated).

    Plano source order (D-05 acceptable types): the official City of Plano council pages and searchable council agendas/minutes for recorded votes and motions; Ballotpedia individual candidate pages; VOTE411 and League of Women Voters of Collin County questionnaires; Community Impact Newspaper and Star Local Media candidate Q&As and coverage; the officeholder's campaign or official site. Confirm the page names Plano, Texas before accepting it — a same-named person in another city or state is not evidence (Pitfall 2).

    Assign a chair 1 through 5 ONLY where a cited source shows an explicit, on-topic position: a stated stance, a recorded council vote or motion, or a candidate-questionnaire answer (D-04). Suggestive-but-not-explicit is a blank. No party inference, no pattern inference from a colleague. Attempt all 8 canonical Local Lens topics (D-06) and additionally attempt the 3 legacy-tail topics — Taxation & Public Spending, Growth & Development Pace, Healthcare Access — only where explicit evidence exists (D-07); never force them.

    Skip any (politician_id, topic_id) pair listed in the 222-01 do-not-overwrite set — those belong to the already-stanced records this phase leaves alone (D-07).

    Record per person, in a structured working note you carry into Task 2: politician_id, full_name, office title, and for each of the 11 topics either {value 1-5, one-to-three-sentence reasoning citing a specific dated action/quote/vote, and the exact source URLs actually fetched} or `BLANK`. Also record, for every person, the list of source URLs actually checked — the blank register needs it whether or not anything was sourced.

    A person who yields zero sourced topics is a normal, correct outcome, not a failure. Do not lower the evidence bar to raise a count.
  </action>
  <verify>
    <automated>The working note lists one entry per Plano worklist row, each carrying either at least one {topic, value 1-5, reasoning, source URL} triple or an explicit BLANK marker, plus the sources-checked list. Entry count equals the Plano row count in 222-01-WORKLIST.md.</automated>
  </verify>
  <acceptance_criteria>
    - The number of researched entries equals the number of Plano rows in 222-01-WORKLIST.md — no person skipped.
    - Each researched entry names the politician_id verbatim from the worklist.
    - Every assigned value is a whole integer between 1 and 5 inclusive.
    - Every assigned value carries reasoning naming a specific dated action, quote, vote, or questionnaire answer, plus at least one source URL that was actually fetched.
    - Every person, sourced or not, carries a sources-checked list.
    - Research passes were sequential — the working note records them in worklist order, one at a time.
  </acceptance_criteria>
  <done>Every un-stanced Plano officeholder has been individually attempted against all 11 topics under the strict evidence bar, with per-person results and sources-checked lists captured.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Plano migration, self-audit every stance, append the blank register, and commit</name>
  <read_first>
    - The Task 1 working note.
    - `C:/EV-Accounts/backend/migrations/1307_oro_valley_mayor_stances.sql` — the exact answers-plus-context upsert shape to copy, including the AUDIT-ONLY header comment and dollar-quoting for reasoning text.
    - `C:/EV-Accounts/backend/migrations/998_horacio_ortiz_stances.sql` — the alternate topic_key-joined style, useful for a multi-topic single-politician block.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format to mirror.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — for the live next-available migration number and the Plano row set.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the cited-source resample contract.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_plano_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    First run the self-audit, before writing any SQL. Plano is a Tier-1 city, so re-verify 100% of its stances against their cited source: re-fetch each source URL and confirm the quote, vote, or questionnaire answer is genuinely present, is on-topic for that specific compass topic, and points in the direction of the assigned chair. Check the assigned direction against the inversion-trap table in `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` for that topic_key — the 1-to-5 scales are discrete chairs, not a shared left-right polarity, and several topics do not run progressive-at-1. Any stance whose source does not survive re-fetch is demoted to a blank and moved to the register; do not soften the reasoning to keep it. Any reasoning that reads as generically evaluative rather than citing a specific dated action is demoted the same way.

    Then re-read the live next-available migration number from 222-01-WORKLIST.md and confirm it is still free by listing `C:/EV-Accounts/backend/migrations`; if concurrent work has taken it, take the next free number. Create `C:/EV-Accounts/backend/migrations/<N>_222_plano_stances.sql` in the EV-Accounts repo — this file does NOT live in the essentials repo.

    The file opens with a header comment marking it AUDIT-ONLY and stating it is deliberately not registered in `schema_migrations`, names Phase 222 and Plano, and records the date. The body is a single BEGIN/COMMIT transaction. For each surviving (politician, topic) pair, write the pair of upserts: an insert into `inform.politician_answers (politician_id, topic_id, value)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value`, immediately followed by an insert into `inform.politician_context (politician_id, topic_id, reasoning, sources)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources`. Never write an answers row without its matching context row. Dollar-quote all reasoning text so scraped apostrophes and quotation marks cannot break the statement, and use a distinct tag if any quoted text would collide with the default delimiter. Values are whole numbers 1 through 5. topic_id values are the canonical UUIDs verified in 222-01. If Plano yields zero sourced stances across every person, do not create the migration file at all — record that outcome and proceed.

    Then append a `## Plano` section to `222-CONFIRMED-BLANK.md` in the essentials repo, mirroring the 221 format: a one-line `Evidence:` note naming the source families actually checked for Plano, then one bullet per attempted person with no sourced position, each carrying full_name, office title, and politician_id. If Plano produced no blanks, still write the `## Plano` heading with an explicit line stating all N attempted officeholders were sourced and zero blanks resulted. Update the running `Count:` line at the top of the register.

    Commit both repos separately and locally. In EV-Accounts use `git -C "C:/EV-Accounts" add backend/migrations/<N>_222_plano_stances.sql` then `git -C "C:/EV-Accounts" commit` with a message naming phase 222 and Plano — never `cd` into that repo. Do NOT run `git push` against EV-Accounts: a push deploys accounts-api to Render and carries every commit already ahead. In essentials, commit the register change normally.
  </action>
  <verify>
    <automated>grep -q "^## Plano" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - 100% of Plano stances were re-fetched and confirmed against their cited source before commit; demotions to blank are recorded in the register with a reason.
    - The migration file (if any stance survived) exists under `C:/EV-Accounts/backend/migrations/` with a numeric prefix taken live this session, an AUDIT-ONLY header, and a single BEGIN/COMMIT transaction.
    - Every `inform.politician_answers` insert in the file is immediately followed by a matching `inform.politician_context` insert for the same politician_id and topic_id.
    - Every value literal in the file is a whole number 1 through 5; no fractional value appears.
    - Every `sources` array in the file has at least one URL.
    - `222-CONFIRMED-BLANK.md` has a `## Plano` section, and the union of the migration's politician_ids and the Plano register bullets equals the Plano row set in 222-01-WORKLIST.md with no name in both and no name in neither.
    - `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending — the file is committed, not dangling.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>The Plano migration is authored, self-audited at the Tier-1 100% resample rate, committed locally in EV-Accounts, and the Plano blank register section is written and committed in essentials. Nothing has been applied to production yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator applies the Plano migration and runs the evidence-integrity gate</name>
  <read_first>
    - `C:/EV-Accounts/backend/migrations/<N>_222_plano_stances.sql` — the SQL to apply.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the evidence-integrity gate query.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the Plano section, for the exactly-one-bucket check.
  </read_first>
  <what-built>
    A committed, AUDIT-ONLY Plano stance migration plus the Plano blank-register section. The executor has no Supabase MCP access, so the SQL is authored and committed but NOT yet applied. Until it is applied, the repository looks complete while production is unchanged — this checkpoint exists specifically to close that gap.
  </what-built>
  <action>
    Halt here and hand control to the operator. Do NOT attempt the apply step from this session — gsd-executor has no Supabase MCP binding, so the SQL is committed but production is still unchanged until the operator runs it. Present the how-to-verify steps below verbatim, wait for the resume signal, then record the operator's reported apply confirmation, evidence-integrity gate result, and reconcile result in this plan's SUMMARY before the phase advances. If any gate returns rows, do not mark this plan done.
  </action>
  <how-to-verify>
    1. Apply the migration via `mcp__supabase-local__execute_sql` (this is production — the write is live). Do NOT register it in `schema_migrations`.
    2. Run the evidence-integrity gate from `222-VALIDATION.md` scoped to this plan's Plano politician_ids: it must return zero rows. It fails on a NULL value, a value outside 1 to 5, a fractional value, a missing `inform.politician_context` row, an empty or NULL `sources` array, or a topic_id outside the 11 canonical UUIDs.
    3. Run the exactly-one-bucket reconcile for Plano: every Plano row in `222-01-WORKLIST.md` must appear either as a politician_id with new rows in `inform.politician_answers` or as a bullet in the `## Plano` section of `222-CONFIRMED-BLANK.md` — never both, never neither.
    4. Spot-check one applied stance on the live profile compass to confirm the spoke renders.
  </how-to-verify>
  <acceptance_criteria>
    - The migration has been applied to production via `mcp__supabase-local__execute_sql` and the apply is confirmed, not assumed.
    - The evidence-integrity gate returns zero rows for this plan's politician_ids.
    - The Plano exactly-one-bucket reconcile passes: worklist count equals applied-people count plus register-bullet count, with no overlap.
    - No row was written to `schema_migrations`.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live and the gate returns zero rows, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped web text → SQL migration | Quotes and URLs harvested from the open web are embedded into a statement executed against production |
| research judgment → public civic profile | An assigned chair becomes a public claim about a real, named person |
| authored SQL → production DB | The executor cannot apply; a file on disk is not a live row |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-04 | Tampering | migration SQL text | high | mitigate | Dollar-quote all scraped reasoning text; never concatenate scraped text into a bare SQL string; use a distinct dollar tag on delimiter collision |
| T-222-05 | Repudiation | fabricated or mismatched source URL | critical | mitigate | 100% Tier-1 re-fetch before commit; a source that does not contain the cited claim demotes the stance to a blank; prohibitions P2/P3 |
| T-222-06 | Spoofing | wrong-person attribution (homonym) | high | mitigate | Confirm the Texas city named in the source before accepting evidence (RESEARCH.md Pitfall 2); prohibition P4 |
| T-222-07 | Denial of Service | false-positive completion (authored but unapplied SQL) | high | mitigate | Blocking apply-and-verify checkpoint; the gate runs against production, not the file |
| T-222-SC | Tampering | package installs | low | accept | This plan installs no packages; nothing to audit |
</threat_model>

<verification>
- Every Plano worklist name individually attempted, sequentially.
- Every applied stance has a whole 1-5 value, a specific dated reasoning, and at least one re-fetched source URL.
- Evidence-integrity gate returns zero rows for this plan's politician_ids after apply.
- Plano exactly-one-bucket reconcile passes.
- EV-Accounts migration committed locally and not pushed.
</verification>

<success_criteria>
Plano's un-stanced officeholders each carry evidence-cited compass chairs where a source explicitly documents a position, and an honest register entry where none exists — applied live, gated at zero defects, with nothing silently skipped and nothing defaulted.
</success_criteria>

<output>
Create `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-02-SUMMARY.md` when done — record people attempted, people sourced, total stances applied, blanks recorded, the migration number used, and any stance demoted during the self-audit.
</output>
