---
phase: 182-city-of-cornelius-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["182-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1196_cornelius_city_council.sql
autonomous: false
requirements: [WASH-08]

must_haves:
  truths:
    - "One 'City of Cornelius, Oregon, US' government row exists (geo_id 4115550 — CORRECTED, never the stated 4115350 which is Coquille)"
    - "One 'City Council' chamber exists under it with official_count=5 (name_formal='Cornelius City Council'); 5 = the Charter §7 designed seat count (Mayor + 4 councilors, one of which is currently vacant, seeded per the TX-23 precedent)"
    - "Exactly one LOCAL_EXEC district and one LOCAL district exist on geo_id 4115550, state='or' lowercase"
    - "5 offices exist (Mayor + 4 councilor seats), each with representing_city='Cornelius' set INLINE — 4 filled (Dalin/Godinez Valencia/Baker/López) + 1 vacant office row (politician_id=NULL, is_vacant=true)"
    - "Office titles are PLAIN 'Mayor' (LOCAL_EXEC) and 'Councilor' (each of the 4 LOCAL seats) — §24 'by position' is staggering only, NO numbered positions, NO wards"
    - "Mayor Dalin + Council President Godinez Valencia are is_appointed=false / is_appointed_position=false (elected); Baker + López are is_appointed=true / is_appointed_position=true (Tigard 1159 appointed-seat shape) — the highest appointed-seat density of the milestone"
    - "The 5th councilor seat is a genuine VACANT office row (TX-23 precedent): politician_id=NULL, is_vacant=true, is_appointed_position=false, NO politician row created, external_id -4115555 intentionally UNUSED — NEVER a placeholder person (D-13)"
    - "Citlalli Nuñez-Barragán is NOT seeded anywhere (stale alt-text only; her former seat is the vacant one)"
    - "Edén López is the FIRST accented UTF-8 name in the WashCo chain — the migration .sql file is saved UTF-8 without a BOM so 'Edén'/'López' insert cleanly"
    - "The Mayor's district label/comment records the 2-YEAR term (Charter §25; structural shape unchanged from the LOCAL_EXEC+LOCAL split)"
    - "The repeated chamber-lookup subquery is hoisted into a single CTE (WITH chamber AS ...) referenced by every office INSERT (D-16 / IN-01 CTE hoist), replacing the 7 inline copies in the Sherwood template"
    - "All filled politicians have office_id back-filled (no NULL office_id on the 4 seated members)"
    - "Section-split scan returns 0 rows for geo_id 4115550 (via the independent canonical GROUP BY/HAVING query, not the dead same-transaction gate)"
    - "The post-verify block includes an INDEPENDENT geofence assertion for geo_id 4115550 AND mtfcc='G4110' AND name='Cornelius city' (STRENGTHENED past Sherwood's existence-only check — this phase's Pitfall 1)"
    - "The post-verify block includes the pairwise identity gate asserting exactly 4 (external_id, full_name) pairs match the researched filled roster — a name/id transposition cannot pass"
    - "NO ward/X00xx geofences or districts were created (pure at-large model)"
    - "Decision coverage — D-03: ward branch N/A (at-large fired; seat identity on office titles, no duplicate district rows); D-04: no section-split and no empty LOCAL section; D-06: Godinez Valencia's Council President designation is title-on-seat, plain 'Councilor' in DB, no separate office row; D-11: representing_city='Cornelius' set INLINE (no backfill); D-13: vacant seat modeled without a placeholder person; D-16/IN-01: chamber-lookup CTE hoist + strengthened name-match geofence gate"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1196_cornelius_city_council.sql"
      provides: "Structural seed: government + chamber + 2 districts + 5 offices (4 filled + 1 vacant) + office_id backfill + post-verify DO block (name-matched geofence assertion + section-split + pairwise identity gate) + ledger row"
      min_lines: 250
      contains: "City of Cornelius, Oregon, US"
  key_links:
    - from: "offices"
      to: "districts (geo_id 4115550)"
      via: "district_id FK on LOCAL_EXEC + LOCAL rows"
      pattern: "geo_id = '4115550'"
    - from: "politicians.office_id"
      to: "offices.id"
      via: "back-fill UPDATE keyed on external_id IN (-4115551..-4115554)"
      pattern: "office_id = o.id"
---

<objective>
Author the single structural migration that seeds the City of Cornelius government, City Council
chamber, two citywide districts (LOCAL_EXEC for the Mayor + LOCAL at-large for the councilors), and the
5 designed offices. Cornelius combines Sherwood's directly-elected-Mayor-on-a-2-year-term district split
with Tigard's appointed-seat pattern (Baker + López) plus a genuinely new wrinkle for this milestone: a
real VACANT seat modeled with the TX-23 precedent (politician_id=NULL, is_vacant=true, no politician
row). PLAIN 'Mayor'/'Councilor' titles (NO numbered positions, NO wards — Charter §7 is pure at-large,
§24 "by position" is staggering only). Set `representing_city='Cornelius'` INLINE on every office INSERT
so the community banner derives correctly at first browse (no backfill migration). Apply the D-16/IN-01
CTE hoist for the repeated chamber lookup, the pairwise `(external_id, full_name)` identity gate, and a
STRENGTHENED geofence assertion that checks `name='Cornelius city'` (not just existence — Pitfall 1).
Edén López is the first accented name in the chain — save the file UTF-8 without a BOM.

Purpose: Satisfy the roster/structure portion of WASH-08 with an idempotent, self-verifying migration
that uses the CORRECTED geo_id 4115550, produces no section-split and no empty LOCAL section, asserts the
geofence is actually 'Cornelius city', models the two appointed seats + the vacant seat correctly, and
asserts each seated name maps to its exact external_id.
Output: `1196_cornelius_city_council.sql` (registered in schema_migrations), applied by the orchestrator
via psql, with its in-migration DO-block gates passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES the `.sql` file to C:/EV-Accounts/backend/migrations/.
It has NO DB access. The INLINE ORCHESTRATOR applies it via `psql "$DATABASE_URL" -f <file>`
(DATABASE_URL from C:/EV-Accounts/backend/.env) and commits it in the C:/EV-Accounts repo
(`git -C "C:/EV-Accounts"` — never cd into it). Before committing, CHECK `git -C "C:/EV-Accounts" status`
for another workstream's pre-staged files (D-16 — a stray file rode into a 181 batch commit); stage ONLY
this migration file. Use the exact migration number confirmed in 182-01-SUMMARY.md (default 1196; ledger
MAX 1187 is a TRAP — on-disk MAX 1195 is authoritative) — if Wave-0 recorded a different next number,
rename the file and update the ledger INSERT.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug` and
`photo_origin_url` OUT of comments in this file. The ledger INSERT into
supabase_migrations.schema_migrations (version) is a genuine table write (expected), not a comment.

**UTF-8 (López):** save the .sql file as UTF-8 WITHOUT a BOM so 'Edén' / 'López' insert cleanly
(Pasadena/Covina accented-name precedent).
</context>

<interfaces>
<!-- Confirmed schema shapes — executor uses these directly, no exploration needed. -->

essentials.governments  (id, name, type, state, city, geo_id)   -- slug GENERATED; NO geo_id unique constraint; INSERT via WHERE NOT EXISTS on name
essentials.chambers     (id, name, name_formal, government_id, official_count)  -- slug GENERATED ALWAYS — NEVER include in INSERT column list
essentials.districts    (id, district_type, state, geo_id, label, mtfcc)  -- NO name_formal; government_id NULL; state='or' LOWERCASE for LOCAL/LOCAL_EXEC
essentials.politicians  (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id, office_id)  -- party ALWAYS NULL (antipartisan)
essentials.offices      (id, district_id, chamber_id, politician_id, title, representing_state, representing_city, is_appointed_position, is_vacant, role_canonical)  -- politician_id is NULLABLE (vacant-seat support); vacant_since nullable timestamptz also exists
</interfaces>

<roster>
<!-- Seed exactly these 4 FILLED + 1 VACANT (re-confirmed at Wave-0). ext_id / title / district_type / appointed: -->
| ext_id    | full_name               | first_name | last_name        | title     | district_type | appointed / status |
|-----------|-------------------------|------------|------------------|-----------|---------------|--------------------|
| -4115551  | Jeffrey C. Dalin        | Jeffrey    | Dalin            | Mayor     | LOCAL_EXEC    | is_appointed=FALSE (directly elected citywide; 2-YEAR term, expires Dec 2026) |
| -4115552  | Angeles Godinez Valencia| Angeles    | Godinez Valencia | Councilor | LOCAL         | is_appointed=FALSE (elected 2021, re-elected 2025, term Dec 2028; holds Council President title-on-seat) |
| -4115553  | Edgar Baker             | Edgar      | Baker            | Councilor | LOCAL         | is_appointed=TRUE (appointed June 2026–Dec 2026) |
| -4115554  | Edén López              | Edén       | López            | Councilor | LOCAL         | is_appointed=TRUE (appointed April 2023–Dec 2026; ACCENTED UTF-8 name) |
| -4115555  | (VACANT — no politician)| —          | —                | Councilor | LOCAL         | office row only: politician_id=NULL, is_vacant=TRUE, is_appointed_position=FALSE, NO politician row; ext_id intentionally UNUSED (TX-23 precedent) |
<!-- Titles use the PLAIN convention ('Mayor' / 'Councilor') — NO numbered positions, NO ward suffixes.
     Mayor Dalin on LOCAL_EXEC; all 4 councilor seats (3 filled + 1 vacant) on the shared LOCAL district.
     party=NULL, representing_state='OR', representing_city='Cornelius' on all 5 offices. Godinez
     Valencia's "Council President" is a title-on-seat — note ONLY in a migration comment; do NOT create
     a separate office row and do NOT alter her plain 'Councilor' title. Baker + López use the Tigard
     1159 appointed-seat block (is_appointed=true on the politician, is_appointed_position=true on the
     office). The 5th seat is a genuine vacancy — TX-23 precedent office row with politician_id=NULL,
     is_vacant=true, NO politician row; DO NOT invent a name and DO NOT seat Citlalli Nuñez-Barragán.
     Do NOT seed any 2026-election candidate as an incumbent. Mayor's term is 2 years — note it in the
     LOCAL_EXEC label/comment. -->
</roster>

<tasks>

<task type="auto">
  <name>Task 1: Author the Cornelius structural migration</name>
  <files>C:/EV-Accounts/backend/migrations/1196_cornelius_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1187_sherwood_city_council.sql (the directly-elected-Mayor-2-year + shared-at-large + plain-title template — entire file; the base shape class Cornelius needs)
    - C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql lines 101-168 (the appointed-seat block for Baker + López: is_appointed=true politician + is_appointed_position=true office)
    - C:/EV-Accounts/backend/migrations/105_tx_congressional_house_officials.sql TX-23 block, lines ~458-469 (the vacant-seat office row: politician_id=NULL, is_vacant=true, NO politician row)
    - C:/EV-Accounts/backend/migrations/076_pasadena_races_and_candidates.sql line ~53 (accented-name UTF-8 precedent for Edén López)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md §"1196_cornelius_city_council.sql (structural migration, CRUD)" (every code block, adapted values — incl. the D-16 CTE hoist, the appointed-seat block, the TX-23 vacant-seat block, the accented-name precedent, the pairwise identity gate, and the strengthened name-match geofence gate)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md (confirmed geo_id 4115550, ext_id block, migration number, roster, vacant-seat decision)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md §"Form of Government — RESOLVED", §"Migration / Schema Technical Reference", §"Architecture Patterns" (the closest-analog diagram)
  </read_first>
  <action>
    Wrap everything in a single BEGIN...COMMIT transaction. Structure per Sherwood mig 1187 (the
    directly-elected-Mayor-2-year + shared-at-large + plain-title shape), adapting values and applying
    the deltas below:
    (1) Pre-flight hard-abort DO block: RAISE EXCEPTION if a 'City of Cornelius, Oregon, US' government
    already exists (message references migration 1196).
    (2) Government INSERT ... SELECT (name='City of Cornelius, Oregon, US', type='LOCAL', state='OR'
    UPPERCASE, city='Cornelius', geo_id='4115550') guarded WHERE NOT EXISTS on name — NEVER insert slug.
    (3) Chamber INSERT (name='City Council', name_formal='Cornelius City Council', government_id
    subselect, official_count=5) guarded WHERE NOT EXISTS on (name, government_id) — NEVER insert slug.
    official_count=5 per the Wave-0-recorded vacant-seat decision option (a) — the Charter's designed
    seat count (Mayor + 4 councilors, one vacant). If Wave-0 recorded option (b) instead, use 4 and omit
    the vacant office block in step (5e).
    (4) TWO district INSERTs, both state='or' LOWERCASE, geo_id='4115550', mtfcc=NULL: one
    district_type='LOCAL_EXEC' label='Cornelius (Mayor, Citywide, 2-Year Term)', one
    district_type='LOCAL' label='Cornelius (At-Large)', each guarded WHERE NOT EXISTS on (geo_id,
    district_type, state). Add a comment above the LOCAL_EXEC row noting the Mayor is directly elected
    citywide on a 2-YEAR term (Charter §25). Create NO other districts — no ward/X00xx rows, and NO
    per-seat/per-position districts (all councilor seats share the ONE LOCAL district).
    (5) D-16/IN-01 CTE HOIST + office blocks. FIRST define a single `WITH chamber AS (SELECT id FROM
    essentials.chambers WHERE name='City Council' AND government_id=(SELECT id FROM
    essentials.governments WHERE name='City of Cornelius, Oregon, US'))` and reference
    `(SELECT id FROM chamber)` in each office INSERT's chamber_id column — do NOT repeat the nested
    chamber subquery inline in every block the way the Sherwood template does. Then author:
    (5a) Mayor Dalin (-4115551) on the LOCAL_EXEC district, title 'Mayor', is_appointed=false /
    is_appointed_position=false (the elected block shape).
    (5b) Councilor Godinez Valencia (-4115552) on the shared LOCAL district, title PLAIN 'Councilor',
    is_appointed=false / is_appointed_position=false. Add a comment noting she holds the Council
    President title-on-seat (one office row only; title stays plain 'Councilor').
    (5c) Councilor Baker (-4115553) on the shared LOCAL district, title 'Councilor', APPOINTED block
    (Tigard 1159 shape): is_appointed=true on the politician, is_appointed_position=true on the office.
    (5d) Councilor Edén López (-4115554) on the shared LOCAL district, title 'Councilor', APPOINTED block
    (is_appointed=true / is_appointed_position=true). Use the exact accented literals 'Edén López' /
    'Edén' / 'López' — the file must be saved UTF-8 without a BOM.
    (5e) VACANT 5th councilor seat (TX-23 precedent): an office-ONLY INSERT on the shared LOCAL district,
    title 'Councilor', politician_id=NULL, is_vacant=true, is_appointed_position=false,
    representing_city='Cornelius', guarded WHERE NOT EXISTS on (district_id, title, is_vacant) for that
    chamber so a re-run does not duplicate it. Create NO politician row; external_id -4115555 stays
    intentionally UNUSED (mirror TX-23's unused-ext_id comment convention). Optionally set vacant_since
    if Wave-0 located the exact departure date; leave NULL otherwise.
    On ALL 5 offices: party=NULL (on politicians), role_canonical=NULL, representing_state='OR'
    UPPERCASE, representing_city='Cornelius' INLINE. Add a PITFALL GUARD comment near the vacant block
    warning that Citlalli Nuñez-Barragán (stale alt-text on the leftover image) must NOT be seated.
    (6) office_id back-fill UPDATE with an explicit external_id IN (-4115551,-4115552,-4115553,-4115554)
    list (the 4 FILLED seats only — the vacant seat has no politician to back-fill), guarded AND
    p.office_id IS NULL.
    (7) Post-verification DO block with the independent assertions plus the pairwise identity gate:
    gate gov_count=1; office_count=5 (JOIN districts on geo_id 4115550 + district_type IN
    ('LOCAL','LOCAL_EXEC') + state 'or'); a STRENGTHENED INDEPENDENT geofence assertion — COUNT(*) FROM
    essentials.geofence_boundaries WHERE geo_id='4115550' AND mtfcc='G4110' AND name='Cornelius city'
    must be >=1 (RAISE EXCEPTION with a message about the 4115350/Coquille trap if 0 — this is the added
    name-match check, this phase's Pitfall 1, going one step past Sherwood's existence-only gate); the
    canonical section-split query (GROUP BY o.district_id HAVING COUNT(DISTINCT o.chamber_id) > 1 over
    offices JOIN districts on geo_id 4115550) = 0; office_id NULLs=0 among the 4 FILLED politicians;
    representing_city='Cornelius' count=5; a vacant-office assertion (exactly 1 office row with
    is_vacant=true AND politician_id IS NULL for the Cornelius chamber); an appointed-seat assertion
    (exactly 2 offices with is_appointed_position=true — Baker + López); AND the pairwise identity gate —
    SELECT COUNT(*) INTO v_pair_count FROM essentials.politicians WHERE (external_id, full_name) IN
    ((-4115551,'Jeffrey C. Dalin'),(-4115552,'Angeles Godinez Valencia'),(-4115553,'Edgar Baker'),
    (-4115554,'Edén López')), RAISE EXCEPTION unless it equals 4 (a pairwise pair assertion, NOT two
    independent IN lists, so a name/id transposition cannot pass). RAISE EXCEPTION on any mismatch; RAISE
    NOTICE on success listing all gates incl. pairwise_identity=4, appointed_positions=2, vacant_office=1.
    (8) Ledger INSERT into supabase_migrations.schema_migrations (version) VALUES ('1196') ON CONFLICT DO
    NOTHING (STRUCTURAL migrations register — contrast with headshot/stance migrations).
    Do NOT place fenced code blocks in this action — the SQL itself lives in the file; the analog blocks
    in 182-PATTERNS.md are the source of truth for exact text.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1196_cornelius_city_council.sql`; the in-migration post-verify DO block (incl. the strengthened name-match geofence gate + pairwise identity gate + appointed/vacant assertions) RAISEs on any gate failure (all gates must emit the success NOTICE)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the confirmed migration number path (default 1196_cornelius_city_council.sql), saved UTF-8 without a BOM
    - Single BEGIN...COMMIT; pre-flight abort guard present (references migration 1196)
    - governments INSERT uses geo_id='4115550', state='OR', WHERE NOT EXISTS on name; no slug column
    - chambers INSERT has official_count=5 (per the Wave-0 vacant-seat option (a); 4 if option (b) recorded) and name_formal='Cornelius City Council'; no slug column
    - Exactly 2 district INSERTs (LOCAL_EXEC + LOCAL), both state='or' lowercase, geo_id='4115550'; LOCAL_EXEC label notes the 2-Year Term; NO ward/X00xx and NO per-seat districts
    - A single `WITH chamber AS (...)` CTE is defined and referenced by each office INSERT's chamber_id (D-16/IN-01 hoist — no repeated inline chamber subqueries)
    - 5 office blocks: Mayor Dalin (-4115551) plain 'Mayor' on LOCAL_EXEC; Godinez Valencia (-4115552), Baker (-4115553), López (-4115554), + 1 vacant on the shared LOCAL district; all titles PLAIN 'Councilor' (NO position numbers, NO wards)
    - Dalin + Godinez Valencia is_appointed=false/is_appointed_position=false; Baker + López is_appointed=true/is_appointed_position=true (Tigard block)
    - The 5th councilor seat is an office-only row: politician_id=NULL, is_vacant=true, is_appointed_position=false, NO politician row, ext_id -4115555 unused; a pitfall comment forbids seating Nuñez-Barragán
    - 'Edén López' / 'Edén' / 'López' appear with correct accents (file UTF-8 no BOM)
    - representing_city='Cornelius' appears on all 5 office INSERTs (inline, no backfill migration)
    - Godinez Valencia (-4115552) has exactly one office row, plain 'Councilor' title, with a Council-President title-on-seat comment
    - office_id back-fill UPDATE lists the 4 FILLED external_ids (-4115551..-4115554), not -4115555
    - Post-verify DO block includes the STRENGTHENED geofence assertion (geo_id='4115550' AND mtfcc='G4110' AND name='Cornelius city' >= 1) + canonical section-split query + pairwise (external_id, full_name) gate (count=4) + appointed_positions=2 + vacant_office=1; asserts gov=1, offices=5, section-split=0, office_id nulls=0 among filled, representing_city=5
    - Ledger INSERT VALUES ('1196') present (structural migration registers)
    - Literal strings `slug` and `photo_origin_url` do NOT appear in comments
  </acceptance_criteria>
  <done>Structural migration authored against geo_id 4115550 with plain titles, 2 appointed seats (Baker/López), a TX-23 vacant seat, the 2-year-term Mayor note, the D-16 CTE hoist, the accented López name (UTF-8 no BOM), and the strengthened name-match geofence + pairwise identity gates; applies cleanly; post-verify DO block emits its success NOTICE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies the migration, runs the E2E structural gate, commits</name>
  <what-built>The Cornelius structural migration. The orchestrator applies it and runs the standalone verification gate.</what-built>
  <how-to-verify>
    1. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1196_cornelius_city_council.sql`. The in-migration DO block must emit "Post-verification PASSED". If it RAISEs, the transaction rolls back — fix and re-apply.
    2. Independent E2E gate (run as separate SELECTs):
       a. governments count for name='City of Cornelius, Oregon, US' = 1.
       b. chambers row exists, official_count=5 (or 4 per the recorded option), name_formal='Cornelius City Council'.
       c. districts: exactly 1 LOCAL_EXEC + 1 LOCAL for geo_id='4115550' state='or'; ZERO ward/X00xx and ZERO per-seat rows for Cornelius.
       d. offices count = 5 for the Cornelius chamber; representing_city='Cornelius' on all 5; titles are PLAIN 'Mayor' + 'Councilor' x4 (no position numbers, no wards).
       e. Section-split scan returns 0 rows for geo_id 4115550.
       f. The 4 FILLED politicians (-4115551..-4115554) have non-NULL office_id; the vacant office row has politician_id IS NULL AND is_vacant=true.
       g. Dalin + Godinez Valencia is_appointed=false; Baker + López is_appointed=true / is_appointed_position=true (exactly 2 appointed offices); Godinez Valencia has exactly ONE office row titled 'Councilor' (not Mayor/CP).
       h. STRENGTHENED geofence check: essentials.geofence_boundaries WHERE geo_id='4115550' AND mtfcc='G4110' AND name='Cornelius city' returns >=1 (and confirm no Cornelius office attached to 4115350/Coquille).
       i. Pairwise identity gate: the 4 filled politicians carry exactly the researched (external_id, full_name) pairs (Dalin/Godinez Valencia/Baker/López); NO 'Citlalli Nuñez-Barragán' row exists.
    3. Check `git -C "C:/EV-Accounts" status` for pre-staged files from another workstream (D-16); stage ONLY this migration. Commit: `git -C "C:/EV-Accounts" add backend/migrations/1196_cornelius_city_council.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied; "Post-verification PASSED" NOTICE emitted (incl. pairwise_identity=4, appointed_positions=2, vacant_office=1)
    - Independent E2E gate a-i all pass
    - Zero ward/X00xx and zero per-seat districts for Cornelius; exactly 5 offices (4 filled + 1 vacant)
    - Baker + López is_appointed=true (exactly 2 appointed offices); Dalin + Godinez Valencia elected; Godinez Valencia single office row plain 'Councilor'; NO Nuñez-Barragán row
    - Vacant office row: politician_id NULL + is_vacant=true; no placeholder person
    - Strengthened geofence assertion confirms 4115550 (G4110) is 'Cornelius city' (not Coquille)
    - `git -C "C:/EV-Accounts" status` checked for pre-staged files before commit; only 1196 staged; migration committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with the gate results, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Structural INSERTs applied to production Supabase via psql |
| migration file → schema | Idempotency + post-verify gates (incl. strengthened name-match geofence + pairwise identity + appointed/vacant assertions) gate the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-06 | Spoofing | Seeding the roster onto geo_id 4115350 (which resolves to 'Coquille city', a different real city) instead of 4115550 | mitigate | Structural migration hardcodes geo_id='4115550'; post-verify STRENGTHENED geofence gate asserts the G4110 row is NAMED 'Cornelius city' (not just present) and RAISEs on the Coquille trap; Wave-0 already confirmed the value |
| T-182-07 | Tampering | Silent politician-identity collision or a name/id transposition on an ON CONFLICT (external_id) path | mitigate | Pairwise (external_id, full_name) identity gate: post-verify asserts exactly 4 exact pairs, RAISEs otherwise |
| T-182-08 | Tampering | Duplicate/partial re-seed on prod | mitigate | Pre-flight hard-abort DO block + WHERE NOT EXISTS on gov/chamber/districts + office guards; single transaction rolls back on any post-verify failure |
| T-182-09 | Tampering | Dead post-verify gate lets a real section-split ship silently | mitigate | Independent name-matched geofence assertion + canonical GROUP BY/HAVING section-split query, not the same-transaction "create-then-check-absence" gate |
| T-182-10 | Spoofing | Seating a placeholder person (or Citlalli Nuñez-Barragán's stale alt-text) on the vacant 5th seat, violating D-13 | mitigate | TX-23 precedent office-only row (politician_id=NULL, is_vacant=true, NO politician row); post-verify vacant_office=1 assertion; pitfall comment + pairwise gate ensure no fabricated name is inserted |
| T-182-11 | Spoofing | Modeling Baker/López as elected (missing is_appointed) | mitigate | Tigard appointed-seat block for both; post-verify appointed_positions=2 assertion; roster comment cites their bio-page "Appointed"-only entries |
| T-182-12 | Elevation of Privilege | Ward/position districts or numbered titles misrepresent the pure-at-large body | mitigate | Pure at-large enforced (2 citywide districts, plain titles); E2E gate 2c/2d assert zero ward rows + exactly 5 offices + plain titles |
| T-182-13 | Tampering (data integrity) | Mojibake corruption of 'Edén López' | mitigate | File saved UTF-8 without a BOM (Pasadena/Covina precedent); pairwise gate asserts the exact 'Edén López' literal |
| T-182-14 | Repudiation | Untracked structural change, or another workstream's file riding into the commit | mitigate | Migration registered in schema_migrations ledger; `git -C "C:/EV-Accounts" status` checked pre-commit; only 1196 staged (D-16) |
| T-182-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- In-migration post-verify DO block passes (incl. strengthened name-match geofence gate, pairwise
  identity gate, appointed/vacant assertions); independent E2E gate a-i passes; migration committed
  after a clean `git -C "C:/EV-Accounts" status` check.
</verification>

<success_criteria>
- 1 government + 1 chamber (official_count=5) + 2 citywide districts + 5 offices (plain
  'Mayor'/'Councilor' titles; Baker + López appointed; 1 TX-23 vacant seat), all with
  representing_city='Cornelius', filled offices' office_id back-filled, section-split=0, geofence 4115550
  confirmed named 'Cornelius city', pairwise identity gate confirms the 4 researched names, no ward
  geofences, no placeholder person, López accents intact.
</success_criteria>

<output>
Create `.planning/phases/182-city-of-cornelius-deep-seed/182-02-SUMMARY.md` when done, recording the
minted politician UUIDs (by external_id) for the 4 filled seats for plans 03 (headshots) and 04
(stances), and noting the vacant 5th office row (no UUID).
</output>
</content>
