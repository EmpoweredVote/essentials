---
phase: 105-va-2026-elections-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 105-01
files_modified:
  - C:/EV-Accounts/backend/migrations/324_va_2026_races.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-324.ts
autonomous: true
requirements:
  - VA-ELECTIONS-02
tags:
  - races
  - sql
  - virginia
  - va-2026
must_haves:
  truths:
    - "essentials.races has 12 VA 2026 rows linked to 2026 Virginia General Election"
    - "1 row is U.S. Senate Virginia (Warner re-election)"
    - "11 rows are U.S. House VA-01 through VA-11"
    - "Zero NULL office_ids across the 12 inserted rows"
    - "Re-running migration keeps the count at 12 (idempotent)"
    - "Migration 324 ledger entry is present"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/324_va_2026_races.sql
      provides: "12 VA 2026 race rows (1 Senate + 11 House) + post-verify + ledger"
      contains: "WITH gen_elec AS"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-324.ts
      provides: "Apply script with smoke tests for count=12, NULL office_id count=0, ledger"
      contains: "essentials.races"
  key_links:
    - from: essentials.races.election_id
      to: essentials.elections (Plan 01)
      via: "Subquery `WHERE name='2026 Virginia General Election' AND state='VA'`"
      pattern: "WITH gen_elec AS \\(SELECT id FROM essentials.elections WHERE name = '2026 Virginia General Election'"
    - from: essentials.races.office_id
      to: essentials.offices
      via: "Office UUID literals resolved at planning time from production DB (Phase 102 federal officials)"
      pattern: "VALUES \\("
---

<objective>
Seed 12 Virginia 2026 race rows into essentials.races via migration 324: 1 U.S. Senate Virginia (Mark Warner re-election) + 11 U.S. House VA-01 through VA-11. All rows link to the 2026 Virginia General Election seeded in Plan 01 (resolved via subquery on name), so no hardcoded election UUID is needed in the SQL.

Purpose: VA-ELECTIONS-02 requires race rows for Warner's Senate seat + all 11 House districts. No VA state legislature races exist in 2026 (HoD was Nov 2025; VA Senate is 2027) per ROADMAP/REQUIREMENTS.md, so the universe is exactly 12 rows.

Output: Migration 324 SQL + apply script committed and applied to production; 12 VA race rows verified with zero NULL office_ids.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-02-SUMMARY.md
@.planning/phases/102-va-federal-officials/102-01-SUMMARY.md
@C:/EV-Accounts/backend/migrations/279_md_2026_statewide_races.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve all 12 VA office UUIDs from production DB and stage the VALUES list</name>
  <files>
    (no file writes in this task — query-only; record results in plan execution scratch)
  </files>
  <read_first>
    C:/EV-Accounts/backend/migrations/279_md_2026_statewide_races.sql (note the WITH gen_elec + VALUES pattern; office UUIDs are hardcoded after live DB resolution)
    .planning/phases/102-va-federal-officials/102-01-SUMMARY.md (confirms VA House external_ids -5102001..-5102011 and Warner external_id -400080)
  </read_first>
  <action>
    Run two production DB queries via mcp__supabase-local (which IS the production DB per project memory) to resolve all 12 office UUIDs:

    Query A — Warner's Senate office (use external_id from Phase 102 Summary, V5: Warner=-400080):
    `SELECT o.id AS office_id, p.full_name, p.external_id FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id = -400080 AND d.district_type = 'NATIONAL_UPPER' AND d.state = 'VA';`

    Query B — 11 VA House offices (Phase 102 Summary V4 confirms external_ids -5102001 through -5102011 ordered by district 1..11):
    `SELECT p.external_id, o.id AS office_id, p.full_name, d.geo_id FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id BETWEEN -5102011 AND -5102001 AND d.district_type = 'NATIONAL_LOWER' AND d.state = 'VA' ORDER BY p.external_id DESC;` (DESC because external_id -5102001 is VA-1)

    Verify the results table contains 11 distinct office_ids and Warner returns exactly 1 office_id. If Warner's external_id is NOT -400080 (e.g. if planning_context noted -400079 as a typo), re-resolve by `WHERE p.full_name='Mark Warner' AND d.district_type='NATIONAL_UPPER' AND d.state='VA'` and record the discrepancy in the SUMMARY's Deviations section. If any office_id query returns 0 rows or NULL, STOP and surface the gap — do not proceed to Task 2.

    Stage the 12 (office_id, position_name) tuples for the migration in this exact order:
      1. (warner_office_uuid, 'U.S. Senate Virginia')
      2..12. (va_house_office_uuids[1..11], 'U.S. House VA-01' .. 'U.S. House VA-11')

    Use zero-padded district numbers: VA-01, VA-02, ..., VA-11 (matching MD's MD-01..MD-08 convention from migration 279).
  </action>
  <verify>
    <automated>Run the DB queries in the action above and confirm all 12 rows return non-null office UUIDs before proceeding. Record results in a scratch comment at the top of the migration file header.</automated>
  </verify>
  <acceptance_criteria>
    - Query A returns exactly 1 row with non-null office_id for Mark Warner
    - Query B returns exactly 11 rows, one per external_id -5102001..-5102011, all with non-null office_ids
    - All 12 office_ids are distinct
    - The 11 VA House office_ids correspond to geo_ids 5101..5111 (sanity: VA-1 through VA-11)
    - If Warner's external_id deviates from -400080, the deviation is recorded for the SUMMARY
  </acceptance_criteria>
  <done>
    12 office UUIDs are confirmed against production DB; Task 2 can hardcode them as VALUES literals without further DB lookups.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 324_va_2026_races.sql + apply script + apply to production</name>
  <files>
    C:/EV-Accounts/backend/migrations/324_va_2026_races.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-324.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/migrations/279_md_2026_statewide_races.sql (verbatim pattern for WITH gen_elec + VALUES + ON CONFLICT + post-verify)
    .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-02-SUMMARY.md (the "Wrong chamber name" lesson does NOT apply here — federal House/Senate office_ids are already resolved as literals, not by chamber name join)
  </read_first>
  <action>
    Create the migration SQL file at `C:/EV-Accounts/backend/migrations/324_va_2026_races.sql` following the migration 279 pattern verbatim:

    - Header comment block: name "Migration 324: VA 2026 Federal Races — Phase 105 Plan 02". Note that LG/AG/Governor are NOT seeded (no 2026 state-exec races in VA — those were Nov 2025). Note that 11 VA House CDs + Warner Class 2 Senate seat are the entire 2026 VA federal universe. Reference Phase 102 Summary for the office_id source.
    - `WITH gen_elec AS (SELECT id FROM essentials.elections WHERE name = '2026 Virginia General Election' AND state = 'VA')` CTE.
    - `INSERT INTO essentials.races (election_id, office_id, position_name, primary_party, seats) SELECT gen_elec.id, t.office_id_val::uuid, t.position_name_val, NULL, 1 FROM gen_elec, (VALUES ...) AS t(office_id_val, position_name_val)` with the 12 (office_uuid, position_name) tuples from Task 1.
    - Position names in this exact form: `'U.S. Senate Virginia'`, `'U.S. House VA-01'`, `'U.S. House VA-02'`, ..., `'U.S. House VA-11'`.
    - `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING` for idempotency.
    - Ledger INSERT: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('324') ON CONFLICT (version) DO NOTHING;` (mandatory).
    - Post-verify `DO $$` block scoped to the 12 position_names: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='VA' AND e.name='2026 Virginia General Election' AND r.position_name IN ('U.S. Senate Virginia','U.S. House VA-01',...,'U.S. House VA-11');` RAISE EXCEPTION if not 12. This scoping (per the migration 279 lesson) keeps the verify idempotent if future plans add more VA races.

    Create the apply script at `C:/EV-Accounts/backend/scripts/_apply-migration-324.ts` by copying `_apply-migration-279.ts` if it exists, otherwise base it on `_apply-migration-278.ts` structure. Substitute '324' for the migration number and '324_va_2026_races.sql' for the filename. Include four smoke tests:
      1. Total VA 2026 race count for the 12 specific position_names — expect 12.
      2. NULL office_id count for VA 2026 races — expect 0.
      3. Sanity row check: SELECT position_name, office_id IS NOT NULL FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='VA' ORDER BY position_name — print all 12.
      4. Ledger entry 324: PRESENT/MISSING check.

    Apply the migration via `cd C:/EV-Accounts && npx tsx backend/scripts/_apply-migration-324.ts`. Plan 01 must have applied migration 322 first (the apply script will fail with "FK violation" or post-verify RAISE if the 2026 Virginia General Election row does not exist). Plan 02 is Wave 2 (depends_on 105-01) — Plan 01 must complete before this plan runs.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-324.ts 2>&amp;1 | tee /tmp/_324.log; grep -q "VA 2026 race rows: 12" /tmp/_324.log &amp;&amp; grep -q "NULL office_id count: 0" /tmp/_324.log &amp;&amp; grep -q "Ledger entry 324: PRESENT" /tmp/_324.log</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/324_va_2026_races.sql exists
    - SQL file contains `WITH gen_elec AS (SELECT id FROM essentials.elections WHERE name = '2026 Virginia General Election' AND state = 'VA')`
    - SQL file contains the literal `'U.S. Senate Virginia'`
    - SQL file contains all 11 literals: `'U.S. House VA-01'` through `'U.S. House VA-11'` (zero-padded)
    - SQL file contains `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`
    - SQL file contains `RAISE EXCEPTION 'Expected 12 VA 2026 federal race rows`
    - SQL file contains `VALUES ('324')` (ledger entry)
    - File C:/EV-Accounts/backend/scripts/_apply-migration-324.ts exists and references `324_va_2026_races.sql`
    - Apply script prints `VA 2026 race rows: 12 (expected 12)`
    - Apply script prints `NULL office_id count: 0 (expected 0)`
    - Apply script prints `Ledger entry 324: PRESENT`
    - Re-running apply script keeps row count at 12 (idempotency)
    - Production DB query `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='VA' AND e.name='2026 Virginia General Election'` returns 12
    - Production DB query `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='VA' AND e.name='2026 Virginia General Election' AND r.office_id IS NULL` returns 0
  </acceptance_criteria>
  <done>
    12 VA 2026 race rows exist in production with non-null office_ids; migration 324 is recorded in the supabase_migrations ledger; Plan 03 (discovery + Landing.jsx) can begin Wave 3.
  </done>
</task>

</tasks>

<verification>
- 12 VA 2026 race rows exist (1 Senate + 11 House)
- Zero NULL office_ids
- Idempotent re-apply
- Ledger entry 324 PRESENT
- Position naming follows MD migration 279 convention (zero-padded district numbers)
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='VA' AND e.name='2026 Virginia General Election'` returns 12
- All 12 rows have non-null office_ids
- VA-ELECTIONS-02 satisfied
</success_criteria>

<output>
Create `.planning/phases/105-va-2026-elections-discovery/105-02-SUMMARY.md` when done with:
- Migration 324 file path + apply script path
- The 12 resolved office UUIDs (table of position_name + office_id + politician name)
- Acceptance criteria checklist
- Any deviations from plan (especially Warner's external_id if not -400080)
</output>
