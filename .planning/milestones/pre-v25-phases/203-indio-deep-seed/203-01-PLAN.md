---
phase: 203-indio-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-indio-council-boundaries.ts
autonomous: true
requirements: [CV-03]
must_haves:
  truths:
    - "5 Indio City Council district LOCAL geofences (mtfcc='X0023', state='ca') are loaded into essentials.geofence_boundaries, one per district 1-5"
    - "Each geofence geometry is valid WGS84 (SRID 4326) in the Indio range (lon ~-116.2, lat ~33.7), NOT a native state-plane projection"
    - "The 5 boundaries come from the city self-hosted ArcGIS REST service (gis.indio.org — CONTEXT.md D-07), cross-checked to feature count = 5 with a district-number attribute (Ordinance No. 1775, D-08)"
    - "The 5 geofences gate (pre-flight >=5) the structural migration in Plan 02 — without them Plan 02 must refuse to apply"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-indio-council-boundaries.ts"
      provides: "ArcGIS f=geojson loader for the 5 council districts, parameterized INSERT ON CONFLICT (geo_id, mtfcc) DO NOTHING, EXPECTED_COUNT=5 hard-abort"
      contains: "X0023"
  key_links:
    - from: "load-indio-council-boundaries.ts"
      to: "gis.indio.org/arcgis/rest/services district/boundaries FeatureServer"
      via: "f=geojson + outSR=4326 query; probe response shape before parsing (GeoJSON pass-through OR Esri-rings conversion fallback)"
      pattern: "outSR=4326"
    - from: "load-indio-council-boundaries.ts"
      to: "essentials.geofence_boundaries"
      via: "parameterized INSERT with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))"
      pattern: "ON CONFLICT \\(geo_id, mtfcc\\) DO NOTHING"
---

<objective>
Author and run the one-time ETL loader that sources the 5 official City of Indio City Council district
boundaries from the city self-hosted ArcGIS REST service (`gis.indio.org/arcgis/rest/services/`,
queried with `f=geojson`, CONTEXT.md D-07) and inserts them as custom LOCAL geofences (`mtfcc='X0023'`,
`state='ca'`, geo_id `indio-ca-council-district-1..5`). These 5 geofences are the gating dependency for
true per-councilmember address routing — the structural migration in Plan 02 refuses to apply until
they exist.

Purpose: Satisfies the geofence half of ROADMAP success criterion #2 for CV-03 (5 council-district
X-geofences; a probe address in each routes to exactly one member). This is the 3rd identical
Coachella Valley city deep-seed; the loader shape is Palm Springs' (Phase 202) verbatim, adapted for
the Indio endpoint and the fact that the gis.indio.org response shape (GeoJSON FeatureCollection vs.
Esri rings) and the district-number attribute NAME are unconfirmed until execute time.
Output: `C:/EV-Accounts/backend/scripts/load-indio-council-boundaries.ts`, committed to the
`C:/EV-Accounts` repo and run by the orchestrator so 5 X0023 rows exist in production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-indio-deep-seed/203-CONTEXT.md
@.planning/phases/203-indio-deep-seed/203-PATTERNS.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / GIS-network access. Task 1 (author the loader) is the only
executor file action. Task 2 is ORCHESTRATOR-RUN: the inline orchestrator probes the gis.indio.org
service, finalizes the loader URL, runs `npx tsx` against `C:/EV-Accounts/backend/.env`
`DATABASE_URL`, and asserts the result. All backend files live in the SEPARATE `C:/EV-Accounts` repo
(`master`→Render) — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (from 203-PATTERNS.md loader block + CONTEXT.md D-07/D-08/D-09):
- PRIMARY analog (copy shape verbatim): `C:/EV-Accounts/backend/scripts/load-palmsprings-council-boundaries.ts`
  (260 lines) — same city-council-district ArcGIS `f=geojson` fetch/insert shape:
  `JSON.stringify(feature.geometry)` pass-through, `ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))`
  INSERT, `ST_MakeValid` repair fallback, `districtMap`, EXPECTED_COUNT shortfall `process.exit(1)`.
  Only the URL / MTFCC / geo_id prefix / centroid-range / district-attribute-name differ.
- Endpoint (UNCONFIRMED path — RESEARCH TODO at execute time): under
  `https://gis.indio.org/arcgis/rest/services/` find the Boundaries/Districts FeatureServer (or
  MapServer) layer; query `.../FeatureServer/0/query?where=1%3D1&outFields=*&returnGeometry=true&f=geojson&outSR=4326`.
  `outSR=4326` MANDATORY (never trust the native SRID — Palm Springs' was CA state-plane). Palm Springs'
  service name had URL-encoded parentheses (`%28View%29`); Indio's may or may not — confirm before hardcoding.
- Response shape UNCONFIRMED: inspect ONE feature's response before writing the parse loop — if
  gis.indio.org returns a GeoJSON FeatureCollection directly, use Palm Springs'
  `JSON.stringify(feature.geometry)` pass-through; if it returns Esri rings, fall back to the Pima
  County rings-conversion helper (CONTEXT.md D-09). If NO usable ArcGIS layer exists, D-09 fallback =
  digitize from the Ordinance No. 1775 PDF (flag for a decision at execute time — do NOT silently skip).
- District-number attribute NAME UNCONFIRMED (do NOT assume `DISTRICT` the way Palm Springs' was):
  probe `props['DISTRICT'] ?? props['District'] ?? props['DIST_NUM'] ?? props['DISTRICTNO']`,
  `parseInt(String(rawDist ?? ''), 10)`, range-reject outside 1-5. Log any council-name attribute the
  layer carries as a free roster cross-check ONLY (never use it to form geo_id).
- Config constants: MTFCC `'X0023'` (next unused after X0022/Palm Springs — DB-verify unused at
  execute time); STATE_CODE `'ca'` (lowercase — required for LOCAL routing); SOURCE
  `'gis.indio.org-council-districts-2026'` (adjust once the real layer name is confirmed);
  GEO_ID_PREFIX `'indio-ca-council-district-'`; EXPECTED_COUNT 5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Indio council-district geofence loader</name>
  <files>C:/EV-Accounts/backend/scripts/load-indio-council-boundaries.ts</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 47-127 — the full loader pattern: header/config block with the 4 CRITICALs, the DISTRICT-attribute-unconfirmed probe, the CouncilName cross-check log, the verbatim INSERT + ST_MakeValid guard + centroid substitution note, the hard-abort-on-partial-set rule, and the D-09 Esri-rings fallback)
    - .planning/phases/203-indio-deep-seed/203-CONTEXT.md (D-07 primary ArcGIS source; D-08 cross-check feature count=5 + district-number attribute / Ordinance No. 1775; D-09 rings/PDF fallback)
    - C:/EV-Accounts/backend/scripts/load-palmsprings-council-boundaries.ts (PRIMARY analog — read in FULL: f=geojson fetch, JSON.stringify(feature.geometry) pass-through, districtMap, parameterized INSERT with ST_Multi(ST_SetSRID(...)), ST_MakeValid fallback, EXPECTED_COUNT shortfall exit — copy this shape verbatim)
  </read_first>
  <action>
    Copy `load-palmsprings-council-boundaries.ts` and adapt for Indio. Set the query URL to the
    gis.indio.org FeatureServer layer under `https://gis.indio.org/arcgis/rest/services/` with
    `where=1%3D1`, `outFields=*`, `returnGeometry=true`, `f=geojson`, and `outSR=4326`. Leave a clearly
    marked RESEARCH-TODO comment noting the exact FeatureServer/MapServer path is confirmed at execute
    time (Task 2 orchestrator probe). `outSR=4326` is MANDATORY (without it coordinates return in the
    native state-plane SRID and store as garbage that never matches an address). Keep `f=geojson`.

    Set constants: MTFCC='X0023', STATE_CODE='ca' (lowercase), SOURCE=
    'gis.indio.org-council-districts-2026', GEO_ID_PREFIX='indio-ca-council-district-', EXPECTED_COUNT=5.

    District-number extraction: the attribute NAME is UNCONFIRMED for Indio (Ordinance No. 1775 defines
    the 5 districts, D-08) — probe `props['DISTRICT'] ?? props['District'] ?? props['DIST_NUM'] ??
    props['DISTRICTNO']`, parse with `parseInt(String(rawDist ?? ''), 10)`, and reject any value that
    isNaN or is out of range 1-5 BEFORE forming geo_id (skip + warn, never accept silently). Also read
    and `console.log` any council-name/officeholder attribute the layer carries per district as a free
    cross-check against the roster (D1 Miller / D2 Fermon / D3 Holmes / D4 Ortiz / D5 Guitron IV) — do
    NOT use it to build geo_id. Build geo_id per feature as `${GEO_ID_PREFIX}${districtNumber}` and name
    as `Indio City Council District ${districtNumber}`.

    Response-shape handling: the loader must inspect one feature's geometry shape. Keep Palm Springs'
    `JSON.stringify(feature.geometry)` pass-through as the primary path (used when gis.indio.org returns
    a GeoJSON FeatureCollection), AND include the Pima County Esri-rings-to-GeoJSON conversion helper as
    a fallback branch for the case where the layer returns Esri rings despite `f=geojson` (CONTEXT.md
    D-09). Select the branch by detecting whether `feature.geometry.type` is a GeoJSON type vs. an Esri
    `rings` array.

    Reuse the parameterized INSERT into essentials.geofence_boundaries (columns id, geo_id, mtfcc,
    state, name, geometry, source) with `public.ST_Multi(public.ST_SetSRID(public.ST_GeomFromGeoJSON($3),
    4326))` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING RETURNING public.ST_GeometryType(geometry),
    public.ST_IsValid(geometry)`. All GIS attribute/geometry values MUST be passed as bind params
    ($1 geo_id / $2 name / $3 geomJson / $4 source) — never string-concatenated into SQL (T-203-SQLI).
    Keep a conditional `ST_MakeValid` re-run guard for any invalid/self-intersecting multipolygon; if
    `ST_IsValid` still fails post-repair, reject that feature rather than loading it.

    Assert EXPECTED_COUNT: if the fetch returns fewer than 5 valid districts (`distMap.size !== 5`),
    print the shortfall and `process.exit(1)` (PAUSE+flag — never silently load a partial set). Print a
    summary line per district (district number, geo_id, geometry type, ST_IsValid, first-coordinate
    sanity value) so the orchestrator can confirm the Indio WGS84 range in Task 2.

    Do NOT touch essentials.districts or any government/office table — this loader ONLY populates
    geofence_boundaries. The pre-existing whole-city TIGER boundary (geo_id='0636448', mtfcc 'G4110',
    state '06') is untouched. Ensure NO `X0022`/`X0021`/`X0020` token survives anywhere (including
    comments) after the copy-and-adapt — verify-greps check comments too (Ph202 GOTCHA).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/load-indio-council-boundaries.ts"; test -f "$F" && grep -qF "outSR=4326" "$F" && grep -qF "f=geojson" "$F" && grep -qF "X0023" "$F" && grep -qF "gis.indio.org" "$F" && grep -qF "ON CONFLICT (geo_id, mtfcc) DO NOTHING" "$F" && grep -qF "indio-ca-council-district-" "$F" && grep -qF "ST_MakeValid" "$F" && ! grep -qE "X0022|X0021|X0020|palm-springs|riverside-ca" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/load-indio-council-boundaries.ts` exists.
    - Grep confirms `outSR=4326`, `f=geojson`, `gis.indio.org`, `X0023`, geo_id prefix `indio-ca-council-district-`, and `ST_MakeValid` present.
    - NEITHER `X0022` NOR `X0021`/`X0020` appears anywhere (including comments); no leftover `palm-springs`/`riverside-ca` geo_id prefixes.
    - STATE_CODE is lowercase `'ca'` (no uppercase `'CA'` used as the geofence state value).
    - Parameterized INSERT with `ST_GeomFromGeoJSON($3)` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING` present — no string concatenation of GIS data into SQL.
    - District attribute probed defensively across multiple candidate names with parseInt + range-reject 1-5; council-name attribute logged as cross-check only.
    - GeoJSON-pass-through primary path AND an Esri-rings fallback branch both present (D-09); EXPECTED_COUNT=5 with a non-zero exit on shortfall.
  </acceptance_criteria>
  <done>Loader authored: fetches the 5 Indio council districts via f=geojson+outSR=4326 from gis.indio.org (GeoJSON pass-through with Esri-rings fallback, district-attribute probed defensively), inserts 5 X0023/lowercase-ca geofences idempotently with parameterized binds, hard-aborts on shortfall.</done>
</task>

<task type="auto">
  <name>Task 2: Probe the service + run the loader + assert 5 X0023 geofences (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator probes gis.indio.org, finalizes the loader URL, runs the loader + read-only DB assertions; no executor files modified</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 100-105 — X0023 DB-verify-unused note; line 104 EXPECTED_COUNT=5; lines 453-461 — orchestrator-run vs executor-authored split)
    - .planning/phases/203-indio-deep-seed/203-CONTEXT.md (D-07 gis.indio.org; D-08 feature count=5 + district-number attribute; D-09 fallback if the layer is missing/unusable)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/network access). (1) Probe `https://gis.indio.org/arcgis/rest/services/`
    to find the actual Boundaries/Districts FeatureServer (or MapServer) layer; fetch one feature with
    `f=geojson&outSR=4326` and inspect the response shape (GeoJSON FeatureCollection vs. Esri rings) +
    the real district-number attribute name. Finalize the loader's URL constant + confirm which parse
    branch fires; if NO usable ArcGIS layer exists, invoke D-09 (digitize from the Ordinance No. 1775
    PDF) and PAUSE for a decision — do not proceed on an empty set. (2) DB-verify X0023 is genuinely
    unused (`SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0023'` = 0 before load);
    if claimed by an interim phase, assign the next free X-code, update the loader constant, and note the
    deviation. (3) `git -C "C:/EV-Accounts" add` + commit the loader. (4) Run
    `npx tsx backend/scripts/load-indio-council-boundaries.ts` from `C:/EV-Accounts/backend` (uses
    `.env` `DATABASE_URL`); confirm exit 0 with 5 rows inserted (or already-present via ON CONFLICT) and
    the logged per-district council-name cross-check against D1 Miller / D2 Fermon / D3 Holmes / D4 Ortiz
    / D5 Guitron IV. (5) Assert via `psql "$DATABASE_URL" -tAc`: (a) exactly 5 rows WHERE mtfcc='X0023'
    AND state='ca'; (b) all 5 geo_ids match `indio-ca-council-district-1..5`; (c) all 5 geometries valid
    (ST_IsValid=true) and their centroid longitude is between -116.35 and -116.05 and latitude between
    33.6 and 33.85 (Indio WGS84 sanity, confirming outSR=4326 worked). If any assertion fails, PAUSE and
    flag — do NOT proceed to Plan 02. Record the 5 geo_id → ST_IsValid → centroid → council-name
    cross-check results + the confirmed gis.indio.org layer URL + the actual X-code used in
    203-01-SUMMARY.md.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0023' AND state='ca')=5 AND NOT EXISTS (SELECT 1 FROM essentials.geofence_boundaries WHERE mtfcc='X0023' AND NOT public.ST_IsValid(geometry)) AND (SELECT bool_and(public.ST_X(public.ST_Centroid(geometry)) BETWEEN -116.35 AND -116.05 AND public.ST_Y(public.ST_Centroid(geometry)) BETWEEN 33.6 AND 33.85) FROM essentials.geofence_boundaries WHERE mtfcc='X0023')"</automated>
  </verify>
  <acceptance_criteria>
    - gis.indio.org layer located + response shape confirmed; loader URL + parse branch finalized (or D-09 PDF fallback invoked with a paused decision).
    - X0023 confirmed unused before load (or the actual reassigned code recorded); loader exits 0; 5 X0023 rows present after the run.
    - `psql -tAc` count of X0023/state='ca' geofences = 5; all 5 geo_ids exactly `indio-ca-council-district-1` through `-5`.
    - All 5 geometries valid; every centroid lon∈(-116.35,-116.05) and lat∈(33.6,33.85) (WGS84, no state-plane garbage).
    - Logged per-district council-name matches the roster (Miller/Fermon/Holmes/Ortiz/Guitron IV).
    - The combined boolean SELECT returns `t`. Loader committed to `C:/EV-Accounts` via `git -C`; confirmed layer URL + X-code recorded in 203-01-SUMMARY.md.
  </acceptance_criteria>
  <done>5 valid WGS84 council-district geofences (X0023, lowercase ca) are live in production and roster-cross-checked, gating Plan 02's structural migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gis.indio.org ArcGIS FeatureServer (remote) → loader | Untrusted GeoJSON/Esri geometry + attribute strings enter the loader over HTTP |
| loader → production PostGIS | Converted geometry crosses into live `essentials.geofence_boundaries` via `npx tsx` (service-role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-01 | Tampering | malformed/self-intersecting ArcGIS geometry → INSERT | mitigate | `ST_MakeValid` repair fallback; reject the feature if `ST_IsValid` still fails post-repair (never load an invalid polygon) |
| T-203-SQLI | Tampering | GIS attribute/geometry → INSERT | mitigate | Parameterized binds ($1 geo_id / $2 name / $3 GeoJSON / $4 source) — never string-concatenate GIS response data into SQL |
| T-203-PROJ | Tampering | native state-plane SRID vs WGS84 | mitigate | `outSR=4326` mandatory in the query; Task 2 asserts centroid ∈ Indio WGS84 range or PAUSES |
| T-203-SHAPE | Tampering | unconfirmed response shape / wrong district attribute → wrong or empty boundaries | mitigate | Task 2 probes one feature before load; GeoJSON-pass-through + Esri-rings fallback branches; district attribute probed across candidate names + range-rejected; EXPECTED_COUNT=5 hard-abort; D-09 PDF fallback if no usable layer |
| T-203-SC | Tampering | npm/tsx installs | accept | Zero new package installs — loader reuses already-installed `pg`/`tsx` |
</threat_model>

<verification>
- 5 X0023 / state='ca' geofences present, all valid WGS84 in the Indio centroid range.
- geo_ids exactly `indio-ca-council-district-1..5`.
- Per-district council-name cross-checks the current roster.
- Loader committed to `C:/EV-Accounts`; pre-existing city boundary 0636448 untouched.
</verification>

<success_criteria>
5 valid council-district geofences (X0023, lowercase ca) live in production, gating Plan 02.
</success_criteria>

<output>
Create `.planning/phases/203-indio-deep-seed/203-01-SUMMARY.md` when done (record the 5 geo_id →
ST_IsValid → centroid → council-name results, the confirmed gis.indio.org layer URL, and the actual
X-code used).
</output>
</content>
</invoke>
