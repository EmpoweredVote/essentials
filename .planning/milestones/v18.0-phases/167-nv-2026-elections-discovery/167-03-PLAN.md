---
phase: 167-nv-2026-elections-discovery
plan: 03
type: execute
wave: 3
depends_on: ["167-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1113_nv_2026_discovery.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1113.ts
autonomous: false
requirements: [NV-ELEC-01]
user_setup:
  - service: render-accounts-api
    why: "Trigger one real candidate-discovery run against the seeded NV jurisdiction (D-03 acceptance bar)"
    env_vars:
      - name: ADMIN_INGEST_TOKEN
        source: "C:/EV-Accounts/backend/.env (already provisioned) — used as X-Admin-Token header; read from env only, never hardcoded"
      - name: DATABASE_URL
        source: "C:/EV-Accounts/backend/.env (already provisioned)"

must_haves:
  truths:
    - "Exactly one essentials.discovery_jurisdictions row exists for NV (state='NV', election_date='2026-11-03')"
    - "That row has jurisdiction_geoid='32', source_url=Ballotpedia 2026 NV elections page, and allowed_domains scoped to official/citation domains (ballotpedia.org + nvsos.gov + nevada.gov + leg.state.nv.us — no wildcard)"
    - "Re-running the migration keeps the NV discovery_jurisdictions count at exactly 1 (ON CONFLICT DO NOTHING)"
    - "One real discovery run was triggered for the NV jurisdiction and its discovery_runs row reached status='completed' with no error_message (zero candidates is acceptable)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1113_nv_2026_discovery.sql"
      provides: "Idempotent INSERT of the NV discovery_jurisdictions row"
      contains: "ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-1113.ts"
      provides: "Apply + smoke harness (row count, date, geoid, allowed_domains, source_url)"
      contains: "1113_nv_2026_discovery.sql"
  key_links:
    - from: "discovery trigger (POST /api/admin/discover/jurisdiction/:id)"
      to: "essentials.discovery_runs"
      via: "runDiscoveryForJurisdiction writes a discovery_runs row reaching status='completed'"
      pattern: "discovery_runs"
    - from: "discovery trigger auth"
      to: "ADMIN_INGEST_TOKEN env var"
      via: "X-Admin-Token header read from env, never hardcoded"
      pattern: "X-Admin-Token"
---

<objective>
Arm the NV candidate-discovery pipeline: seed one `essentials.discovery_jurisdictions` row for the Nov 3, 2026 general election, then execute one real discovery run against the official NV source and confirm it completes. This satisfies the third ROADMAP success criterion ("discovery_jurisdictions rows present; a test discovery run completes against an official NV source") and the binding D-03 acceptance bar.

Purpose: Closes NV-ELEC-01 by turning on automatic NV candidate discovery (date-based 180-day horizon — Nov 3 2026 is ~127 days out, inside the window per D-05) and proving the runner works end-to-end.
Output: `migrations/1113_nv_2026_discovery.sql` + paired `scripts/_apply-migration-1113.ts`, applied to the live DB (1 NV row), plus a `discovery_runs` row showing `status='completed'`.

This plan depends on Plan 02 (and transitively Plan 01) only for phase sequencing/sanity; the discovery row itself FK-references nothing from races, but the discovery test run is the final phase gate so it runs last.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-nv-2026-elections-discovery/167-CONTEXT.md
@.planning/phases/167-nv-2026-elections-discovery/167-RESEARCH.md
@.planning/phases/167-nv-2026-elections-discovery/167-PATTERNS.md

<interfaces>
<!-- essentials.discovery_jurisdictions column contract (verified live 2026-06-29, D-09). No cron_active column. -->
essentials.discovery_jurisdictions:
  jurisdiction_geoid text   -- '32' (Nevada state FIPS, 2-digit bare, matches MD='24' / VA='51')
  jurisdiction_name  text   -- 'State of Nevada'
  state              char   -- 'NV'
  election_date      date   -- '2026-11-03'
  source_url         text   -- 'https://ballotpedia.org/Nevada_elections,_2026' (nvsos.gov returns 403 to fetches; Ballotpedia is the VA precedent)
  allowed_domains    text[] -- ARRAY['ballotpedia.org', 'nvsos.gov', 'nevada.gov', 'leg.state.nv.us'] (scoped, NO wildcard)
  -- UNIQUE constraint EXISTS on (jurisdiction_geoid, election_date) → use ON CONFLICT DO NOTHING
  -- NO cron_active column (D-05); eligibility is the date-based 180-day horizon

<!-- Discovery trigger (VERIFIED: src/routes/essentialsDiscovery.ts line 46):
  POST https://accounts-api.onrender.com/api/admin/discover/jurisdiction/:id
  Header: X-Admin-Token: $ADMIN_INGEST_TOKEN   (requireAdminToken middleware)
  Returns HTTP 202 {"status":"accepted","jurisdictionId":"<uuid>"} immediately (async fire-and-forget).
  Run takes ~30-90s (Claude web_search). Completion proof:
    SELECT status, candidates_found, error_message FROM essentials.discovery_runs
    WHERE discovery_jurisdiction_id = '<disc_uuid>' ORDER BY started_at DESC LIMIT 1;
  Acceptance: status='completed', error_message NULL. candidates_found may be 0 (D-03). -->

Migration mechanics (D-08): migration 1113 follows the 325 pattern — NO BEGIN/COMMIT wrapper (325 has none),
ON CONFLICT DO NOTHING, post-verify DO $$ block, NO schema_migrations ledger INSERT.
Apply FROM C:/EV-Accounts/backend via: node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1113.ts.
NO Supabase MCP — verification via apply script (pg Pool) and/or psql.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 1113 (NV discovery_jurisdictions row) + paired apply/smoke script</name>
  <files>C:/EV-Accounts/backend/migrations/1113_nv_2026_discovery.sql, C:/EV-Accounts/backend/scripts/_apply-migration-1113.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/325_va_2026_discovery.sql (canonical analog — discovery_jurisdictions INSERT shape, ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING, post-verify DO block; NV seeds 1 row not 2, and writes NO ledger row unlike 325)
    - C:/EV-Accounts/backend/scripts/_apply-migration-325.ts (apply-script analog — row count / date / allowed_domains / source_url smoke tests; drop its ledger smoke test, add a jurisdiction_geoid check)
    - .planning/phases/167-nv-2026-elections-discovery/167-PATTERNS.md (section "migrations/1113_nv_2026_discovery.sql" — full adapted excerpt + "Key differences from VA 325")
  </read_first>
  <behavior>
    - After apply: SELECT count(*) FROM essentials.discovery_jurisdictions WHERE state='NV' returns exactly 1
    - That row: jurisdiction_geoid='32', jurisdiction_name='State of Nevada', election_date='2026-11-03', source_url='https://ballotpedia.org/Nevada_elections,_2026', allowed_domains length 4
    - Second apply (idempotency): count stays 1, no error (ON CONFLICT DO NOTHING)
  </behavior>
  <action>
    Write `migrations/1113_nv_2026_discovery.sql` following the 325 pattern (NO BEGIN/COMMIT — 325 has none). Single INSERT into essentials.discovery_jurisdictions (jurisdiction_geoid, jurisdiction_name, state, election_date, source_url, allowed_domains) VALUES ('32', 'State of Nevada', 'NV', '2026-11-03', 'https://ballotpedia.org/Nevada_elections,_2026', ARRAY['ballotpedia.org', 'nvsos.gov', 'nevada.gov', 'leg.state.nv.us']) — exactly ONE row (general only, primary is past per D-02). Use jurisdiction_geoid '32' (Nevada FIPS, bare 2-digit). source_url is the Ballotpedia NV 2026 page (nvsos.gov returns 403 to automated fetches; D-04). allowed_domains is the explicit 4-domain scoped list (NO wildcard) so citations from nvsos.gov score as confidence='official'. End with `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING` (this unique constraint EXISTS). Add a `DO $$ ... RAISE EXCEPTION` post-verify asserting count=1 for state='NV'. NO schema_migrations ledger INSERT (D-08). NO cron_active column (D-05 — it does not exist).

    Write `scripts/_apply-migration-1113.ts` mirroring `_apply-migration-325.ts`: dotenv, pg Pool, readFileSync of `migrations/1113_nv_2026_discovery.sql`, run, then smoke tests logging actual vs expected: NV row count = 1; election_date = 2026-11-03; allowed_domains length = 4; source_url = the Ballotpedia URL; jurisdiction_geoid = 32. OMIT the ledger smoke test. try/catch with process.exit(1), pool.end() in finally.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1113.ts</automated>
  </verify>
  <acceptance_criteria>
    - Apply-script output: "NV discovery_jurisdictions rows: 1 (expected 1)"
    - Output confirms election_date 2026-11-03, allowed_domains length 4, source_url the Ballotpedia URL, jurisdiction_geoid 32
    - Second run keeps count at 1 (idempotent via ON CONFLICT DO NOTHING, no error)
    - migrations/1113_nv_2026_discovery.sql contains 'ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING', contains NO 'cron_active', and contains NO 'schema_migrations' INSERT
  </acceptance_criteria>
  <done>Exactly one NV essentials.discovery_jurisdictions row exists with the correct geoid/date/source_url/scoped allowed_domains; migration idempotent; no cron_active reference; no ledger row.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Trigger one real NV discovery run and confirm it completes</name>
  <files>(no files written — live HTTP call to the Render backend + DB poll)</files>
  <read_first>
    - .planning/phases/167-nv-2026-elections-discovery/167-RESEARCH.md (section "C. Discovery runner mechanism — RESOLVED" — exact curl, 202 async behavior, ~90s wait, discovery_runs poll; Pitfall 4 async timing)
    - .planning/phases/167-nv-2026-elections-discovery/167-PATTERNS.md (section "Discovery Test Run (not a file — Plan 03 operator action)")
  </read_first>
  <what-built>
    Migration 1113 seeded the NV discovery_jurisdictions row (Task 1). This task executes the one real discovery run that the D-03 acceptance bar requires, then confirms completion. The trigger endpoint is admin-token-gated; the token is read from the ADMIN_INGEST_TOKEN env var and MUST NOT be hardcoded or echoed into any committed file or log.
  </what-built>
  <action>
    Resolve the NV discovery_jurisdictions UUID, trigger one discovery run against the live Render backend, wait for the async run, and poll for completion:
    1. `DISC_ID=$(psql "$DATABASE_URL" -At -c "SELECT id FROM essentials.discovery_jurisdictions WHERE state='NV' AND election_date='2026-11-03'")`
    2. `curl -s -X POST "https://accounts-api.onrender.com/api/admin/discover/jurisdiction/$DISC_ID" -H "X-Admin-Token: $ADMIN_INGEST_TOKEN" -H "Content-Type: application/json"` — expect HTTP 202 `{"status":"accepted","jurisdictionId":"<uuid>"}`. Read the token from the env var only; never paste it literally.
    3. Wait ~90 seconds (the run is async fire-and-forget; Claude web_search takes 30-90s — Pitfall 4).
    4. Poll: `psql "$DATABASE_URL" -c "SELECT id, status, candidates_found, error_message, completed_at FROM essentials.discovery_runs WHERE discovery_jurisdiction_id = '$DISC_ID' ORDER BY started_at DESC LIMIT 1"`. If status is still 'running', wait and re-poll. Acceptance = status='completed' with error_message NULL; candidates_found may be 0 (D-03).
    If ADMIN_INGEST_TOKEN is not available in the environment, or the Render call cannot run unattended, pause here and surface to the operator with the exact curl + poll commands so they can run it; this is why the task is a blocking human-verify checkpoint.
  </action>
  <how-to-verify>
    1. The curl returned HTTP 202 with `{"status":"accepted",...}`.
    2. After ~90s, the latest discovery_runs row for the NV jurisdiction shows `status = 'completed'` and `error_message` is NULL.
    3. candidates_found may be 0 — that still passes (D-03).
    If status shows 'failed' with an error_message, report it; the gate is NOT met until a run reaches 'completed'.
  </how-to-verify>
  <acceptance_criteria>
    - A discovery_runs row exists for the NV discovery_jurisdiction with status='completed' and error_message NULL
    - The admin token was supplied via $ADMIN_INGEST_TOKEN env only — it appears in NO committed file, plan, or summary
  </acceptance_criteria>
  <resume-signal>Type "approved" once a NV discovery_runs row shows status='completed' (zero candidates is fine), or describe the failure/error_message.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Operator-run SQL migration; hardcoded literals only |
| executor/operator → Render backend | POST /api/admin/discover/jurisdiction/:id over HTTPS, authenticated by X-Admin-Token (== ADMIN_INGEST_TOKEN). This is the only externally-reachable surface in the phase. |
| discovery agent → external web | Agent fetches/searches only domains within the seeded allowed_domains list when scoring citation confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-03-TOKEN | Information Disclosure | ADMIN_INGEST_TOKEN | mitigate | Token is read from the env var only and passed as the X-Admin-Token header; it MUST NOT be hardcoded in any committed migration/script/plan/summary or echoed to logs. Acceptance criterion enforces token absence from all committed artifacts. |
| T-167-03-SCOPE | Tampering | allowed_domains | mitigate | allowed_domains is an explicit 4-domain list (ballotpedia.org, nvsos.gov, nevada.gov, leg.state.nv.us) — official/citation domains only, NO wildcard — constraining which domains discovery treats as authoritative when scoring candidate confidence. |
| T-167-03-AUTHZ | Elevation of Privilege | POST /discover/jurisdiction/:id | accept | Endpoint is already gated by the existing requireAdminToken middleware (no change introduced this phase); the admin-token check is the established access control. |
| T-167-03-DATA | Tampering/Injection | migration 1113 INSERT | accept | Hardcoded-literal INSERT, no user input; SQL injection N/A. |
</threat_model>

<verification>
- `node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1113.ts` (from C:/EV-Accounts/backend) exits 0 with NV row count 1 and correct geoid/date/domains/source_url.
- Re-run: count remains 1 (idempotent via ON CONFLICT DO NOTHING).
- After the discovery trigger + ~90s wait: `psql $DATABASE_URL -c "SELECT status, candidates_found, error_message FROM essentials.discovery_runs WHERE discovery_jurisdiction_id=(SELECT id FROM essentials.discovery_jurisdictions WHERE state='NV' AND election_date='2026-11-03') ORDER BY started_at DESC LIMIT 1"` shows status='completed', error_message NULL.
- Confirm no committed file contains the literal admin token.
</verification>

<success_criteria>
One `essentials.discovery_jurisdictions` row for NV (geoid '32', Nov 3 2026, Ballotpedia source_url, scoped 4-domain allowed_domains, no cron_active) exists and is idempotent; one real discovery run reached `discovery_runs.status='completed'` with no error (zero candidates acceptable); the admin token never appears in any committed artifact.
</success_criteria>

<output>
Create `.planning/phases/167-nv-2026-elections-discovery/167-03-SUMMARY.md` when done. Record: migration 1113 applied, the NV discovery_jurisdiction UUID, the discovery_runs row id + final status + candidates_found, confirmation the token was env-only, and that next migration counter is 1114.
</output>
