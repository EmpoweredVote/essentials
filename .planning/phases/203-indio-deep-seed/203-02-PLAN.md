---
phase: 203-indio-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["203-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1338_indio_city_council.sql  # 1338 = DB-verified disk MAX+1 per 203-PATTERNS.md; re-verify actual next number at execute time
autonomous: false
requirements: [CV-03]
must_haves:
  truths:
    - "A 'City of Indio, California, US' government (type='City', state='CA', city='Indio', geo_id='0636448') exists"
    - "One 'City Council' chamber (official_count=5) holds exactly 5 by-district council offices"
    - "Each of the 5 council offices links to its OWN LOCAL X0023 district (never the pre-existing G4110 0636448 city row)"
    - "Roster is Miller(D1)/Fermon(D2)/Holmes(D3)/Ortiz(D4)/Guitron IV(D5); all 5 net-new greenfield politicians; no directly-elected LOCAL_EXEC mayor row"
    - "The rotational Mayor (Holmes, D3) and Mayor Pro Tem (Fermon, D2) are TITLES set at INSERT on their district seats (title='Mayor' / 'Mayor Pro Tem'); the other three carry title='Councilmember'. Exactly 5 offices, no separate Mayor office/chamber/district. Re-verify current Mayor/MPT + D5 full name at execute time."
    - "The structural migration is applied to production and registered in the ledger; 5 politician UUIDs + ext_ids captured for Plans 03/04"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1338_indio_city_council.sql"
      provides: "government + chamber + 5 LOCAL X0023 districts + 5 politicians (-4012001..-4012005) + 5 offices (Holmes Mayor D3, Fermon Mayor Pro Tem D2) + office_id backfill + pre-flight + post-verify DO gates (a)-(f) + ledger registration"
      contains: "City Council"
  key_links:
    - from: "essentials.offices (5 councilmembers)"
      to: "essentials.districts (X0023 LOCAL)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='X0023' AND state='ca'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'City Council'"
      to: "essentials.governments 'City of Indio, California, US'"
      via: "government_id subquery scoped by name"
      pattern: "City of Indio, California, US"
---

<objective>
Author the ONE structural migration that seeds the City of Indio government + City Council chamber +
5 LOCAL council-district rows (bound to the X0023 geofences from Plan 01) + 5 net-new by-district
councilmember politicians + their 5 offices, with the rotational Mayor (Holmes, D3) and Mayor Pro Tem
(Fermon, D2) set as TITLES on their seats at INSERT time. The orchestrator applies it via `psql -f`,
registers it in the ledger, and captures the 5 politician UUIDs + ext_ids for Plans 03/04.

Purpose: Satisfies ROADMAP success criteria #1 and #3 for CV-03 — the government/chamber/roster with
the by-district relabel (title-on-seat) pattern. Indio is greenfield (no existing government row per
CONTEXT.md DB pre-check), so all 5 members are created fresh — the same create-as-Mayor-at-INSERT shape
Palm Springs (Phase 202) used, only with the Mayor on D3 (Holmes) and the Mayor Pro Tem on D2 (Fermon).
Output: `C:/EV-Accounts/backend/migrations/1338_indio_city_council.sql` (1338 = DB-verified disk MAX+1
per 203-PATTERNS.md; re-verify the actual next number at execute time).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-indio-deep-seed/203-CONTEXT.md
@.planning/phases/203-indio-deep-seed/203-PATTERNS.md
@.planning/phases/203-indio-deep-seed/203-01-SUMMARY.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB access. Task 1 (author the migration SQL) is the only executor
file action. Task 2 is ORCHESTRATOR-RUN + roster reconfirm: the orchestrator re-verifies the live
roster/Mayor/MPT/D5-full-name, DB-verifies the migration number + ext_id block, applies the migration
via `psql -f` against `C:/EV-Accounts/backend/.env` `DATABASE_URL`, confirms the in-transaction
post-verify gates passed, and captures the 5 politician UUIDs into 203-02-SUMMARY.md. Backend files
live in the SEPARATE `C:/EV-Accounts` repo — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (from 203-PATTERNS.md structural-migration block + CONTEXT.md D-04/D-05/D-06):
- PRIMARY analog (copy overall shape verbatim, section by section): `C:/EV-Accounts/backend/migrations/1329_palm_springs_city_council.sql`
  (492 lines) — government + chamber + pre-flight geofence assertion + 5 LOCAL district rows +
  5×(politician+office) blocks with title-at-INSERT + office_id back-fill + post-verify DO block Gates
  (a)-(f) + ledger registration.
- Government: name `'City of Indio, California, US'`, type `'City'`, state `'CA'` (uppercase label),
  city `'Indio'`, geo_id `'0636448'`, WHERE NOT EXISTS guard on the name.
- Chamber: name `'City Council'`, name_formal `'Indio City Council'`, official_count `5` (the rotational
  Mayor is one of the 5 by-district seats, NOT a 6th excluded seat — CONTEXT.md D-05).
- Districts: 5× district_type `'LOCAL'`, state `'ca'` (LOWERCASE — routing key), geo_id
  `'indio-ca-council-district-1..5'`, mtfcc `'X0023'`.
- Roster + titles (title set DIRECTLY in the office INSERT's SELECT list, Bellflower/Palm-Springs model —
  all 5 greenfield): D1 Glenn Miller 'Councilmember' -4012001 / D2 Waymond Fermon 'Mayor Pro Tem'
  -4012002 / D3 Elaine Holmes 'Mayor' -4012003 / D4 Oscar Ortiz 'Councilmember' -4012004 / D5 Benjamin
  Guitron IV 'Councilmember' -4012005 (reconfirm D5 full name live per D-06). party=NULL on all
  (nonpartisan/antipartisan), representing_state 'CA' (uppercase), role_canonical NULL.
- ext_id block: `-4012001..-4012005` (203-PATTERNS.md confirmed unclaimed; DB-verify still free at
  execute time — do NOT hardcode blindly if an interim phase consumed it).
- Casing (critical): districts.state='ca' lowercase; governments.state='CA' and
  offices.representing_state='CA' uppercase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Indio City Council structural migration</name>
  <files>C:/EV-Accounts/backend/migrations/1338_indio_city_council.sql</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 131-270 — the full structural-migration pattern: government+chamber INSERT, pre-flight geofence assertion, 5 LOCAL district INSERTs, 5 politician+office blocks with title-at-INSERT, office_id back-fill, post-verify DO block incl. Gate (f) exactly-one-Mayor + exactly-one-Mayor-Pro-Tem asserts with the -4012003 Mayor / -4012002 MPT ext_id identity checks, ledger registration; casing lines 422-429; idempotency lines 431-441; section-split guard lines 463-483)
    - .planning/phases/203-indio-deep-seed/203-CONTEXT.md (D-05 rotational-Mayor title-on-seat; D-04 reconfirm whole roster live; D-06 reconfirm Benjamin Guitron IV full name)
    - C:/EV-Accounts/backend/migrations/1329_palm_springs_city_council.sql (PRIMARY analog — read in FULL: the government+chamber+preflight+5-district+5-office+backfill+post-verify Gates (a)-(f)+ledger shape)
  </read_first>
  <action>
    Author `1338_indio_city_council.sql` (VERIFY the actual next migration number at execute time — one
    past the true DB-verified disk MAX; 203-PATTERNS.md confirmed disk MAX = 1337 → next = 1338, but
    interim phases may consume more). Copy the Palm Springs 1329 shape section-by-section and substitute:

    (1) Government INSERT: `'City of Indio, California, US'`, type `'City'`, state `'CA'`, city
    `'Indio'`, geo_id `'0636448'`, guarded by WHERE NOT EXISTS on the name.

    (2) Chamber INSERT: name `'City Council'`, name_formal `'Indio City Council'`, official_count `5`,
    government_id subquery scoped by the government name, guarded by WHERE NOT EXISTS on
    (name, government_id).

    (3) Pre-flight geofence assertion (DO block): RAISE EXCEPTION unless >= 5 rows exist in
    essentials.geofence_boundaries WHERE state='ca' AND mtfcc='X0023' — this refuses to apply until
    Plan 01's loader has run.

    (4) 5 LOCAL district INSERTs: district_type 'LOCAL', state 'ca' (LOWERCASE), geo_id
    'indio-ca-council-district-1..5', label 'Indio City Council District N', mtfcc 'X0023', each guarded
    by WHERE NOT EXISTS on (geo_id, district_type, state).

    (5) 5 politician+office blocks (WITH ins_p AS (INSERT ... politicians ... ON CONFLICT (external_id)
    DO NOTHING RETURNING id) INSERT INTO offices ...). All 5 politicians are net-new greenfield — set
    the office `title` DIRECTLY in the INSERT's SELECT list (Bellflower/Palm-Springs model, NOT a later
    UPDATE): D1 Glenn Miller → 'Councilmember' (-4012001); D2 Waymond Fermon → 'Mayor Pro Tem'
    (-4012002); D3 Elaine Holmes → 'Mayor' (-4012003); D4 Oscar Ortiz → 'Councilmember' (-4012004); D5
    Benjamin Guitron IV → 'Councilmember' (-4012005). Each office links its politician to its OWN
    district via a join scoped by `d.geo_id='indio-ca-council-district-N' AND d.district_type='LOCAL' AND
    d.state='ca' AND d.mtfcc='X0023'`, chamber_id via the City Council subquery, representing_state 'CA'
    (uppercase), party NULL on the politician, is_appointed_position false, is_vacant false,
    role_canonical NULL, guarded by NOT EXISTS on (district_id, politician_id). Do NOT create any separate
    LOCAL_EXEC Mayor office/chamber/district — Indio is rotational (D-05). D5 full_name/last_name MUST
    reflect the D-06 live reconfirmation ("Benjamin Guitron IV" per CivicWeb; some snippets show only
    "Benjamin") — leave a marker comment that Task 2 confirms the final form before apply.

    (6) office_id back-fill: copy Palm Springs' shape, `external_id BETWEEN -4012005 AND -4012001`,
    `office_id IS NULL`.

    (7) Post-verify DO block (all inside the transaction, RAISE EXCEPTION on any failure): (a) exactly 1
    government named 'City of Indio, California, US'; (b) exactly 5 offices on LOCAL X0023 districts;
    (c) each of the 5 districts holds exactly 1 office; (d) appointed count = 0 (confirm at execute
    time); (e) section-split — 0 offices reachable via the 5 indio-ca-council-district-% districts under
    any non-Indio government; (f) exactly 1 office title='Mayor' AND exactly 1 office title='Mayor Pro
    Tem' across the X0023 council districts, with the identity asserts: Mayor is on external_id -4012003
    (Holmes, D3) and Mayor Pro Tem is on external_id -4012002 (Fermon, D2) — RAISE EXCEPTION otherwise.

    (8) Ledger registration: INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES
    ('<actual-migration-number>', 'indio_city_council') ON CONFLICT (version) DO NOTHING. CRITICAL:
    re-derive `<actual-migration-number>` from the REAL DB-verified disk/ledger MAX at apply time
    (MAX(version)+1) — if the placeholder 1338 is already registered by an interim migration, an
    `ON CONFLICT DO NOTHING` would silently no-op the ledger row while the structural rows still land
    under their WHERE NOT EXISTS guards, leaving the migration unregistered. Use the real next number.

    Never join/filter on a bare `geo_id='0636448'`; always scope the 5 new council districts by
    `district_type='LOCAL' AND mtfcc='X0023' AND state='ca'`, and never let this migration touch the
    pre-existing G4110 city geofence row. Ensure no `X0022`/`palm-springs`/`0655254` token survives the
    copy-and-adapt anywhere (including comments) — verify-greps check comments too (Ph202 GOTCHA).
  </action>
  <verify>
    <automated>F=$(ls C:/EV-Accounts/backend/migrations/*_indio_city_council.sql 2>/dev/null | head -1); test -n "$F" && test -f "$F" && grep -qF "City of Indio, California, US" "$F" && grep -qF "City Council" "$F" && grep -qF "official_count" "$F" && grep -qF "X0023" "$F" && grep -qF "'Mayor'" "$F" && grep -qF "'Mayor Pro Tem'" "$F" && grep -qF "indio-ca-council-district-" "$F" && grep -qF "-4012003" "$F" && grep -qF "-4012002" "$F" && grep -qF "schema_migrations" "$F" && ! grep -qE "LOCAL_EXEC|X0022|palm-springs|0655254" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (migration number = one past disk MAX at execute time).
    - Government name/type/geo_id exactly 'City of Indio, California, US'/'City'/'0636448'; WHERE NOT EXISTS guard present.
    - Chamber 'City Council' (name_formal 'Indio City Council') with official_count=5; WHERE NOT EXISTS guard present.
    - Pre-flight DO block asserts >=5 X0023 geofences (state='ca') before seeding.
    - Exactly 5 LOCAL district rows, state='ca' lowercase, mtfcc='X0023', geo_id prefix indio-ca-council-district-.
    - 5 politician+office blocks; title set at INSERT: Holmes(-4012003)='Mayor', Fermon(-4012002)='Mayor Pro Tem', Miller/Ortiz/Guitron IV='Councilmember'; party NULL. NO 'LOCAL_EXEC'; no leftover X0022/palm-springs/0655254.
    - ext_ids -4012001..-4012005 (DB-verified free); office_id back-fill present.
    - Post-verify DO block includes Gate (f) with the -4012003 Mayor / -4012002 MPT identity asserts, plus gates (a)-(e) incl. section-split=0. Ledger registration present (real next number).
    - Idempotency: WHERE NOT EXISTS on gov/chamber/districts/offices; ON CONFLICT (external_id) DO NOTHING on politicians.
  </acceptance_criteria>
  <done>Structural migration authored: greenfield Indio City gov + City Council (official_count=5) + 5 LOCAL X0023 districts + 5 by-district members with Holmes(D3)=Mayor / Fermon(D2)=Mayor Pro Tem titles-on-seat, idempotent, with pre-flight + post-verify Gates (a)-(f) + ledger registration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-verify roster + D5 name + apply migration + capture UUIDs (ORCHESTRATOR-RUN + roster confirm)</name>
  <files>N/A — orchestrator applies the migration + read-only assertions; captures UUIDs into SUMMARY</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-CONTEXT.md (D-04 reconfirm each member live against indio.civicweb.net/indio.org; D-05 rotational-mayor title-on-seat; D-06 reconfirm Benjamin Guitron IV full name)
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 443-451 — reconfirm the sitting Mayor/MPT live at execute time; do NOT model a LOCAL_EXEC Mayor)
    - .planning/phases/203-indio-deep-seed/203-01-SUMMARY.md (the loader's per-district council-name cross-check as a roster prior)
  </read_first>
  <what-built>
    The structural migration `1338_indio_city_council.sql` authored in Task 1 — greenfield City of Indio
    government + City Council (official_count=5) + 5 LOCAL X0023 districts + 5 by-district councilmembers
    with Holmes (D3)=Mayor and Fermon (D2)=Mayor Pro Tem as titles-on-seat.
  </what-built>
  <how-to-verify>
    ORCHESTRATOR steps before/at apply:
    1. Re-verify the CURRENT roster + Mayor/Mayor-Pro-Tem live against indio.civicweb.net/portal/members.aspx?id=10
       + indio.org (city-council page) + Ballotpedia/theindiopost.com (Holmes Dec-2025 mayoral
       installation). Confirm: Miller(D1)/Fermon(D2)/Holmes(D3)/Ortiz(D4)/Guitron IV(D5) all current;
       Holmes is Mayor and Fermon is Mayor Pro Tem (post-Dec-2025 rotation); and reconfirm D5's full name
       ("Benjamin Guitron IV") per D-06. If any member/title/name changed, PAUSE and revise the migration
       before applying.
    2. DB-verify the next migration number (one past current disk/ledger MAX) and that -4012001..-4012005
       is still free; adjust the file if they shifted.
    3. Apply the migration file (using the disk-verified `*_indio_city_council.sql` filename, NOT the
       illustrative 1338 if it shifted) via `psql "$DATABASE_URL" -f backend/migrations/<verified>_indio_city_council.sql`;
       confirm it commits with NO RAISE EXCEPTION (all pre/post-verify gates passed).
    4. Assert via `psql -tAc`: 1 government row; 5 offices on LOCAL X0023 districts (1 per district);
       exactly 1 title='Mayor' (on -4012003) + exactly 1 title='Mayor Pro Tem' (on -4012002); appointed=0;
       section-split=0; ledger has the new version with count=1.
    5. Capture the 5 politician UUIDs + ext_ids + geo_id→member mapping (the "Politician UUID Manifest"
       Plans 03/04 consume) + the confirmed D5 full name into 203-02-SUMMARY.md.
    Confirm: roster current, D5 name confirmed, all gates green, manifest recorded.
  </how-to-verify>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='City of Indio, California, US')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0023' AND d.district_type='LOCAL')=5 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id=o.politician_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0023' AND o.title='Mayor' AND p.external_id=-4012003)=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id=o.politician_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0023' AND o.title='Mayor Pro Tem' AND p.external_id=-4012002)=1"</automated>
  </verify>
  <acceptance_criteria>
    - Roster + Mayor/MPT re-verified current against live sources; Holmes=Mayor (D3), Fermon=Mayor Pro Tem (D2); D5 full name confirmed ("Benjamin Guitron IV" or the live-corrected form).
    - Migration applied and committed with no RAISE EXCEPTION; all gates passed.
    - psql assertions: 1 government, 5 X0023 LOCAL offices (1/district), exactly 1 Mayor (on -4012003) + exactly 1 Mayor Pro Tem (on -4012002), appointed=0, section-split=0.
    - Ledger registers the new version (count=1).
    - 5 politician UUIDs + ext_ids + geo_id→member manifest + confirmed D5 name recorded in 203-02-SUMMARY.md.
    - Migration committed to `C:/EV-Accounts` via `git -C`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the roster is confirmed current, D5's full name is confirmed, all gates are green, and the UUID manifest is recorded — or describe the roster/name/gate discrepancy to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research/recon roster → migration SQL | Officeholder names + Mayor/MPT titles + D5 full name asserted from external sources cross into canonical DB rows |
| migration SQL → production DB | Service-role `psql -f` writes government/chamber/district/office rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-02 | Tampering | wrong-district / stale roster / wrong Mayor / wrong D5 name | mitigate | Cross-verify the X0023 loader council-name log (Plan 01) + indio.civicweb.net/indio.org/theindiopost.com at Task 2 before apply (D-04/D-06); live-browse routing checkpoint in Plan 06 |
| T-203-03 | Tampering | non-idempotent re-application | mitigate | WHERE NOT EXISTS on gov/chamber/districts/offices; ON CONFLICT (external_id) DO NOTHING on politicians |
| T-203-MAYOR | Tampering | modeling a directly-elected LOCAL_EXEC Mayor | mitigate | Title-on-seat only (Holmes D3 / Fermon D2); post-verify Gate (f) asserts exactly 1 Mayor on -4012003 + 1 Mayor Pro Tem on -4012002; no LOCAL_EXEC row (D-05) |
| T-203-SPLIT | Tampering | section-split / cross-gov office leak | mitigate | Post-verify Gate (e) = 0 offices reachable under any non-Indio government; scope every district filter by district_type='LOCAL' AND mtfcc='X0023' AND state='ca' |
</threat_model>

<verification>
- 1 City of Indio government + 1 City Council chamber (official_count=5).
- 5 offices, 1 per X0023 LOCAL district; exactly 1 Mayor (-4012003) + 1 Mayor Pro Tem (-4012002); appointed=0; section-split=0.
- Ledger registers the migration; UUID manifest + confirmed D5 name captured for Plans 03/04.
</verification>

<success_criteria>
Greenfield Indio City Council seeded with the by-district relabel (title-on-seat) pattern, all
post-verify gates green, roster + D5 name re-verified current.
</success_criteria>

<output>
Create `.planning/phases/203-indio-deep-seed/203-02-SUMMARY.md` when done (MUST include the 5 politician
UUID + ext_id + geo_id→member manifest for Plans 03/04, the confirmed D5 full name, the actual migration
number, and the ext_id block used).
</output>
</content>
