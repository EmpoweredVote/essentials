---
phase: 201-riverside-county-board-of-supervisors-deep-seed
plan: 04
type: execute
# WAVE NOTE (deliberate serialization): wave 4 sits behind wave 3 (headshots) even though both depend
# ONLY on Plan 02 — this is an INTENTIONAL serialization, not a computed dependency inconsistency.
# Stance research must run ONE SUPERVISOR AT A TIME (feedback_stance_research_one_at_a_time: parallel
# stance fan-out burns the session quota), so this plan is kept in its own later wave to avoid a
# parallel stance pass and keep each execution context small.
wave: 4
depends_on: ["201-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1316_riverside_supervisor_1_stances.sql
  - C:/EV-Accounts/backend/migrations/1317_riverside_supervisor_2_stances.sql
  - C:/EV-Accounts/backend/migrations/1318_riverside_supervisor_3_stances.sql
  - C:/EV-Accounts/backend/migrations/1319_riverside_supervisor_4_stances.sql
  - C:/EV-Accounts/backend/migrations/1320_riverside_supervisor_5_stances.sql
autonomous: true
requirements: [CV-01]
must_haves:
  truths:
    - "Evidence-only compass stances are seeded for the 5 supervisors against the non-judicial live topics (never the 8 judicial-* topics — a Board of Supervisors is a non-judicial office)"
    - "Every stance row is 100% cited (has a non-empty sources array); there are ZERO neutral-default rows — a topic with no evidence gets NO row at all (honest blank)"
    - "Stances are researched one supervisor at a time (quota); each supervisor's rows carry evidence-based reasoning; discrete 1-5 chairs, not a polarity scale"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1316_riverside_supervisor_1_stances.sql"
      provides: "Jose Medina (D1, -4010001) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1320_riverside_supervisor_5_stances.sql"
      provides: "Dr. Yxstian Gutierrez (D5, -4010005) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "same (politician_id, topic_id) — every answer has a cited context row"
      pattern: "politician_context"
    - from: "stance rows"
      to: "inform.compass_topics (non-judicial live topics)"
      via: "topic_id references a non-judicial live topic UUID"
      pattern: "topic_id"
---

<objective>
Seed evidence-only compass stances for the 5 Riverside County supervisors. One audit-only migration
per supervisor (1316-1320), researched ONE SUPERVISOR AT A TIME (quota), against the NON-JUDICIAL live
compass topics (the 8 `judicial-*` keys have no bearing on a county supervisor — non-judicial office).
Every stance is 100% cited with evidence-based reasoning; a topic with no documented record gets NO
row at all (honest blank), never a neutral default. Discrete 1-5 "chairs", never a polarity scale.

Wave placement (deliberate): this plan is wave 4, behind wave 3 (headshots), even though BOTH depend
only on Plan 02. That is an INTENTIONAL serialization to honor the one-supervisor-at-a-time
stance-research quota (`feedback_stance_research_one_at_a_time` — parallel stance fan-out burns the
session quota) and to keep each execution context small — NOT a computed dependency inconsistency.
Within this plan, work each supervisor's migration one at a time (author → apply → next), 1316 through
1320, so a context that degrades partway is recoverable at a clean per-supervisor boundary.

Purpose: Satisfies ROADMAP success criterion #4 (evidence-only stances, one agent at a time, 100%
cited, honest blank spokes, no defaults).
Output: 5 audit-only migrations `1316..1320_riverside_supervisor_N_stances.sql` (unregistered), applied
so the supervisors' compasses populate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-02-SUMMARY.md

<execution_note>
Depends on Plan 02: the 5 politician UUIDs from 201-02-SUMMARY.md are REQUIRED (stance rows key on the
supervisor UUID). Executor authors the 5 `.sql` files, researching one supervisor at a time per the
`feedback_stance_research_one_at_a_time` quota rule — do NOT fan out parallel stance research. The
ORCHESTRATOR applies each via `psql -f` and asserts. Commit via `git -C "C:/EV-Accounts"`. These are
AUDIT-ONLY migrations — NOT registered in the ledger. Independent of Plan 03 (headshots); kept in a
separate, later wave DELIBERATELY (intentional serialization, not a wave-computer inference).
</execution_note>

<interfaces>
- inform.politician_answers (politician_id, topic_id, value): value = discrete 1.0-5.0 chair;
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value.
- inform.politician_context (politician_id, topic_id, reasoning, sources): reasoning = cited evidence
  prose ($$-quoted); sources = ARRAY[...] of source URLs (NEVER empty); ON CONFLICT (politician_id,
  topic_id) DO UPDATE.
- inform.compass_topics: the live topics; office_scope is empty/NULL (DB does NOT enforce judicial
  exclusion — it is a manual convention). The 8 EXCLUDED judicial keys: judicial-access-to-justice,
  judicial-bail-pretrial, judicial-criminal-justice, judicial-government-deference,
  judicial-interpretation, judicial-police-accountability, judicial-prosecution-priorities,
  judicial-transparency. Research against the NON-judicial topics only. (Confirm the exact live topic
  UUID set at execute time against inform.compass_topics; the count is whatever the DB currently has
  minus the 8 judicial keys — do NOT assert a hardcoded 36/44 count.)
- Evidence sources for Riverside County supervisors (candidate list — verify per member): Riverside
  County Board of Supervisors agendas / clerk-of-the-board records, The Press-Enterprise, The Desert
  Sun / desertsun.com, KESQ, Voice of OC / regional outlets, the 5 district sites.
- ext_ids: Medina -4010001, Spiegel -4010002, Washington -4010003, Perez -4010004,
  Gutierrez -4010005.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 per-supervisor evidence-only stance migrations (one supervisor at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1316_riverside_supervisor_1_stances.sql, C:/EV-Accounts/backend/migrations/1317_riverside_supervisor_2_stances.sql, C:/EV-Accounts/backend/migrations/1318_riverside_supervisor_3_stances.sql, C:/EV-Accounts/backend/migrations/1319_riverside_supervisor_4_stances.sql, C:/EV-Accounts/backend/migrations/1320_riverside_supervisor_5_stances.sql</files>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (lines 265-293 — the per-supervisor stance migration pattern: topic-UUID reference comment block, BEGIN/COMMIT, the two-table INSERT (politician_answers + politician_context) per topic, the judicial exclusion, honest-blank omission, one file per supervisor, audit-only/unregistered)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (§ decisions D-04 — stances evidence-only, one agent at a time, all live topics, 100% cited, no defaults, honest blanks)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-02-SUMMARY.md (the 5 external_id → UUID → full_name manifest — stance rows key on these UUIDs)
    - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql (analog — read header + first stance block: topic-UUID reference table, BEGIN/COMMIT wrapper, two-table INSERT/ON CONFLICT shape, sources-array pattern)
    - C:/EV-Accounts/backend/migrations/1290_pima_supervisor_1_stances.sql (secondary analog — the county-supervisor stance file shape: judicial exclusion, honest blanks, one file per supervisor)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1315 after Plan 03, or 1314 if Plan 03 not yet applied — either way
    name these disk-MAX+1 sequentially). Author 5 AUDIT-ONLY migrations, one per supervisor, numbered
    sequentially (provisionally 1316 Medina / 1317 Spiegel / 1318 Washington / 1319 Perez / 1320
    Gutierrez). NONE registered in the ledger.

    PER-SUPERVISOR CADENCE (save-point / recoverability): work strictly ONE SUPERVISOR AT A TIME and
    treat each supervisor as an independent save-point. For supervisor N: (1) research supervisor N's
    documented positions, (2) author `13{NN}_riverside_supervisor_N_stances.sql`, (3) hand off for the
    orchestrator to apply + assert that ONE file (Task 2 asserts per file), then (4) move to supervisor
    N+1 — 1316 → 1317 → 1318 → 1319 → 1320, in order. Do NOT research or author the whole batch in one
    pass. This honors the quota (one at a time) AND makes a mid-plan context degradation recoverable at
    a clean per-supervisor boundary. Record in the SUMMARY which supervisors are complete so a resumed
    pass knows where to restart.

    Research ONE SUPERVISOR AT A TIME (quota — never parallel). For each supervisor, mine the evidence
    sources (Riverside County BoS agendas / clerk records, The Press-Enterprise, The Desert Sun, KESQ,
    regional outlets, the district site) for documented positions across the non-judicial live topics.
    For each topic WHERE clear evidence exists, emit BOTH: an INSERT into inform.politician_answers
    (politician_id = the supervisor UUID from the Plan 02 manifest, topic_id = the non-judicial topic
    UUID, value = a discrete 1-5 chair) AND an INSERT into inform.politician_context (same keys,
    reasoning = $$-quoted evidence prose naming the vote/statement/date, sources = ARRAY of the actual
    source URLs — NEVER an empty array). Both guarded ON CONFLICT (politician_id, topic_id) DO UPDATE.

    HARD RULES: (1) NEVER emit a row for a topic with no evidence — omit it entirely (honest blank),
    never a neutral/default value (D-04 / feedback_stance_no_default_value). (2) NEVER touch any of the
    8 judicial-* topics — keep them in the reference comment block but write NO INSERT for them. (3)
    Every politician_answers row MUST have a matching politician_context row with a non-empty sources
    array (100% cited). (4) Values are discrete chairs (1-5), not a polarity scale
    (feedback_compass_chairs_not_polarity). (5) Attribute a vote only to supervisors seated at the time
    (no pre-tenure attribution).

    Copy the topic-UUID reference comment block from the analog into each file's header (confirm the
    live topic UUID set against inform.compass_topics at execute time); the INSERTs skip the 8 judicial
    rows. Wrap each file in BEGIN/COMMIT. Do NOT register in the ledger.
  </action>
  <verify>
    <automated>for n in 1 2 3 4 5; do f="C:/EV-Accounts/backend/migrations/$((1315+n))_riverside_supervisor_${n}_stances.sql"; test -f "$f" && grep -qF "politician_answers" "$f" && grep -qF "politician_context" "$f" && ! grep -qiE "INSERT INTO inform.politician_answers[^;]*judicial-" "$f" && ! grep -qF "schema_migrations" "$f" || { echo "FAIL $f"; exit 1; }; done; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist (disk-MAX+1 sequential names, provisionally 1316-1320).
    - Each file contains both `inform.politician_answers` and `inform.politician_context` INSERTs.
    - NO INSERT references any `judicial-*` topic (the 8 judicial keys appear ONLY in the reference comment, never in an INSERT).
    - Every politician_answers INSERT has a matching politician_context INSERT with a non-empty `ARRAY[...]` sources value (no empty arrays).
    - NO neutral-default rows — every emitted row is evidence-backed; topics without evidence are absent (no row).
    - NONE of the 5 files registers in `schema_migrations` (audit-only).
    - Each file wrapped in BEGIN/COMMIT; each keyed to the correct supervisor UUID from the Plan 02 manifest.
    - Files were authored one supervisor at a time (1316→1320), each a recoverable save-point; SUMMARY records per-supervisor completion order.
  </acceptance_criteria>
  <done>5 audit-only stance migrations authored, one per supervisor (1316→1320 in sequence, each a recoverable save-point), evidence-only against the non-judicial topics, 100% cited, honest blanks, discrete chairs, no defaults — unregistered.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the 5 stance migrations + assert evidence-only integrity (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator applies existing migrations + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (§ decisions D-04 — 100% cited, no defaults, honest blanks)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (lines 265-293 — judicial exclusion; per-supervisor counts vary, do NOT assert a fixed count)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). Apply the 5 migrations IN ORDER as each supervisor's
    file lands (per the per-supervisor cadence in Task 1) via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1316_riverside_supervisor_1_stances.sql`
    (etc. through 1320) — applying + asserting each file before the next supervisor keeps the
    per-supervisor save-point clean. Then assert the aggregate via `psql -tAc`, recording in
    201-04-SUMMARY.md: (a) every politician_answers row for the 5 Riverside UUIDs has a matching
    politician_context row (0 orphans); (b) 0 politician_context rows with a NULL or empty sources
    array (100% cited); (c) 0 politician_answers rows on any judicial-* topic for the 5 supervisors;
    (d) all values in [1.0, 5.0]; (e) per-supervisor stance counts (informational — expected to vary;
    honest blanks are fine, do NOT assert a fixed count). Any integrity failure (a-d) blocks the phase.
    Commit the 5 migrations via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id BETWEEN -4010005 AND -4010001 AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id)) AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc JOIN essentials.politicians p ON p.id=pc.politician_id WHERE p.external_id BETWEEN -4010005 AND -4010001 AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL)) AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN inform.compass_topics t ON t.id=pa.topic_id WHERE p.external_id BETWEEN -4010005 AND -4010001 AND t.topic_key LIKE 'judicial-%')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 migrations apply cleanly.
    - 0 politician_answers rows without a matching politician_context row (every answer cited).
    - 0 politician_context rows with a NULL/empty sources array (100% cited).
    - 0 stance rows on any `judicial-*` topic for the 5 supervisors.
    - All stance values in [1.0, 5.0].
    - The combined boolean SELECT returns `t`.
    - Per-supervisor counts recorded in the SUMMARY (informational; honest blanks expected, no fixed count asserted).
    - 5 migrations committed to `C:/EV-Accounts`; none registered in the ledger.
  </acceptance_criteria>
  <done>Evidence-only stances live for the 5 supervisors: 100% cited, no defaults, no judicial rows, honest blanks — audit migrations applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stance migration files → production DB | Hand-authored evidence-only SQL crosses into live `inform` schema via `psql -f` |
| research sources → stance values | External news/agenda evidence maps to a 1-5 chair; unsourced/mis-scoped values corrupt the compass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-CITE | Tampering | uncited / default stance rows | mitigate | Every answer requires a matching context row with a non-empty sources array; Task 2 asserts 0 orphans + 0 empty-sources; no-evidence topics omitted (honest blank), never defaulted |
| T-201-JUD | Tampering | judicial topics on a non-judicial office | mitigate | 8 judicial-* keys excluded from all INSERTs; Task 2 asserts 0 judicial rows for the 5 supervisors |
| T-201-TENURE | Spoofing | pre-tenure vote attribution | mitigate | Attribute a vote only to supervisors seated at the time; no member credited with pre-tenure votes |
| T-201-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `.env`; applied by the inline orchestrator |
| T-201-SC | Tampering | package installs | accept | No new installs — reuses installed psql; Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Every stance row 100% cited (matching context row + non-empty sources); 0 orphans.
- 0 judicial-* rows; 0 neutral-default rows; values in [1.0, 5.0].
- Researched one supervisor at a time (1316→1320, per-supervisor save-points); 5 audit migrations applied and unregistered.
</verification>

<success_criteria>
ROADMAP #4 (evidence-only compass stances, one agent at a time, 100% cited, honest blank spokes, no
defaults) is TRUE in production for all 5 supervisors against the non-judicial topics.
</success_criteria>

<output>
Create `.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-04-SUMMARY.md` when
done — include per-supervisor stance counts, notable honest blanks, the per-supervisor completion order
(save-point trail), and the evidence sources used.
</output>
</content>
