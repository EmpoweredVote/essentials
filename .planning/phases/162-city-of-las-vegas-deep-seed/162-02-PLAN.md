---
phase: 162-city-of-las-vegas-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-lv-city-council-headshots.py
  - C:/EV-Accounts/backend/migrations/1065_las_vegas_city_council_headshots.sql
autonomous: true
requirements: [CLARK-02]
user_setup: []

must_haves:
  truths:
    - "All 7 council members (minus any documented genuine gap) have a 600x750 headshot in the politician_photos bucket [CLARK-02 SC#2]"
    - "Each headshot is crop-to-4:5-then-resized (not stretched) and re-encoded clean JPEG; the Diaz 600x400 landscape source is center-cropped to 320x400 then resized to 600x750"
    - "Each headshot has a politician_images row (type='default', photo_license='us_government_work') pointing at its CDN URL"
    - "No fabricated photos, no text/graphic overlays; any unsourceable portrait is a documented genuine gap [D-05]"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-lv-city-council-headshots.py"
      provides: "Download (Azure Blob, no WAF, plain requests.get) → crop-4:5 → resize 600x750 Lanczos q90 → Storage upsert pipeline; runtime UUID resolve by external_id; 7-row manifest"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1065_las_vegas_city_council_headshots.sql"
      provides: "Audit-only politician_images INSERTs (one per member with an uploaded headshot), columns (id, politician_id, url, type, photo_license), NOT registered"
      contains: "essentials.politician_images"
  key_links:
    - from: "_tmp-lv-city-council-headshots.py"
      to: "essentials.politicians (external_id -3205001..-3205007)"
      via: "runtime psycopg2 lookup SELECT id WHERE external_id=%s"
      pattern: "external_id = %s"
    - from: "1065_las_vegas_city_council_headshots.sql"
      to: "politician_photos CDN ({uuid}-headshot.jpg)"
      via: "politician_images.url literal"
      pattern: "politician_photos"
---

<objective>
Acquire and install all 7 City of Las Vegas council headshots. Write the gitignored `_tmp-lv-city-council-headshots.py` pipeline (download the verified Azure Blob portraits — no WAF, plain requests.get — crop-to-4:5 then resize to 600x750, upload to Storage), then write the audit-only migration 1065 that records the politician_images rows. The script resolves each member's UUID at runtime by external_id (UUIDs minted by Plan 01's migration 1064). Olivia Diaz's source is a 600x400 landscape — the standard crop_to_4_5 handles it (center-crop width to 320x400, then resize to 600x750, ~1.875x upscale, within the Pasadena precedent). Any portrait that cannot be sourced is left empty and documented as a genuine gap — no fabrication, no overlays (D-05 / SC#2).

Purpose: Satisfies the headshot half of CLARK-02 (Success Criterion 2 — all 7 council members render with 600x750 headshots, gaps documented).
Output: _tmp-lv-city-council-headshots.py (pipeline), 1065_las_vegas_city_council_headshots.sql (audit-only), and the uploaded 600x750 JPEGs in politician_photos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and cannot upload to Storage):**
- The executor WRITES `_tmp-lv-city-council-headshots.py` and `1065_las_vegas_city_council_headshots.sql` to disk and documents the exact run/apply steps. The executor does NOT run the script, upload to Storage, or apply the migration.
- Running the headshot script (download + crop/resize + Storage PUT), capturing the manifest, and applying 1065 via `mcp__supabase-local__execute_sql` (or psql -f) are INLINE ORCHESTRATOR steps performed by execute-phase.
- Each task marks executor-write vs inline-orchestrator-apply.
- The migration + script live in the SEPARATE repo C:/EV-Accounts/backend/ (script is gitignored `_tmp-*`; commit the migration via `git -C "C:/EV-Accounts"`). SUMMARY.md goes in the essentials .planning phase dir.
- NOTE: coverage.js was edited in Plan 01 (COVERAGE_STATES NV block) — no coverage change in this plan.
</execution_context>

<context>
@.planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md
@.planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md

<interfaces>
<!-- Research-verified facts the executor needs. Do NOT re-derive. -->
Storage bucket: politician_photos
CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
Storage path: {politician_uuid}-headshot.jpg
TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS
politician_images columns: (id, politician_id, url, type, photo_license) — NO photo_origin_url (removed column); type='default'
photo_license: 'us_government_work' (lasvegasnevada.gov DNN CMS official portraits, served from Azure Blob)
Env (C:/EV-Accounts/backend/.env): SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL
The 7 external_id→UUID map is captured in 162-01-SUMMARY.md (minted by migration 1064)

7 council headshot source URLs (all verified HTTP 200, no WAF, no cookies, no special UA — Azure Blob sawebfilesprod001.blob.core.windows.net, plain requests.get):
  -3205001 Shelley Berkley           https://sawebfilesprod001.blob.core.windows.net/council/Mayor/Las_Vegas_Mayor_Shelley_Berkley_app_June-23-2025-600x800.jpg   (600x800 → top-crop to 600x750)
  -3205002 Brian Knudsen             https://sawebfilesprod001.blob.core.windows.net/images/Knudsen-Headshot.jpg                                                   (unknown res → crop at runtime)
  -3205003 Kara Kelley               https://sawebfilesprod001.blob.core.windows.net/images/Kara_Kelley-Sept-2025-600x800.jpg                                       (600x800 → top-crop)
  -3205004 Olivia Diaz               https://sawebfilesprod001.blob.core.windows.net/images/Olivia_Diaz_Portrait-600x400.jpg                                        (600x400 LANDSCAPE → crop_to_4_5 center-crops width to 320x400 → resize 600x750)
  -3205005 Francis Allen-Palenske    https://sawebfilesprod001.blob.core.windows.net/council/Ward_4_2019/Francis_Allen-Palenske_Dec-2022-600x800.jpg               (600x800 → top-crop)
  -3205006 Shondra Summers-Armstrong https://sawebfilesprod001.blob.core.windows.net/images/Shondra_Summers-Armstrong-2024-600x800.jpg                             (600x800 → top-crop)
  -3205007 Nancy Brune               https://sawebfilesprod001.blob.core.windows.net/images/Nancy_Brune%2025%20RGB.jpg                                             (%20 encoded spaces — requests.get handles transparently)
NO fallback registry needed (all 7 verified HTTP 200). On a runtime failure: SKIP + emit FAILED manifest line (no fabrication, no overlay). No Wikimedia branch needed for LV.
crop_to_4_5 handles BOTH portrait sources (600x800 → crop bottom → 600x750-ratio → resize) AND the Diaz landscape (600x400, ratio 1.5 > 0.8 → center-crop width to int(400*0.8)=320 → 320x400 → resize 600x750).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the headshot pipeline script _tmp-lv-city-council-headshots.py</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-lv-city-council-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-clark-county-commission-headshots.py (PRIMARY ANALOG — verbatim pipeline: env-load, runtime UUID resolve, requests download, PIL crop_to_4_5/resize_600x750/process_headshot_bytes, upload_to_storage x-upsert, manifest)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md (section "scripts/_tmp-lv-city-council-headshots.py" — config/OFFICIALS substitution, env-load + DB connect block, the 6 functions to copy verbatim, manifest, orchestration note)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md ("Pattern 3" headshot pipeline; "Headshot Source" 7 Azure URLs + dimensions; Pitfall 6 Nancy Brune %20 URL; Diaz 600x400 landscape note)
  </read_first>
  <action>
Write `_tmp-lv-city-council-headshots.py` modeled verbatim on `_tmp-clark-county-commission-headshots.py`. Copy the env-load block (reads C:/EV-Accounts/backend/.env), `crop_to_4_5(img)`, `resize_600x750(img)`, `process_headshot_bytes(raw_bytes)`, `upload_to_storage(uuid, jpeg_bytes)` (x-upsert PUT), and `resolve_politician_id(cursor, external_id)` (runtime psycopg2 lookup `SELECT id FROM essentials.politicians WHERE external_id=%s`) unchanged. Constants: BUCKET='politician_photos', the politician_photos CDN base, TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS. In the download function, remove the Clark County Wikimedia branch — LV needs only plain requests.get (no WAF, no special UA).

Build an OFFICIALS list of 7 dicts hardcoding the verified Azure Blob URLs from the interfaces block: each entry `{'ext_id': <int>, 'name': '<full_name>', 'url': '<full Azure Blob URL>'}`, ext_ids -3205001..-3205007. Add the three assert lines (len==7; distinct ext_ids==7; all in range -3205007..-3205001). Set DEFAULT_LICENSE='us_government_work'. Add a header comment that the source images are mostly 600x800 portraits (crop bottom to 600x750), Knudsen/Brune are unknown-resolution (crop at runtime), and Diaz is a 600x400 LANDSCAPE that crop_to_4_5 center-crops to 320x400 then upscales ~1.875x to 600x750 (within the Pasadena precedent). Pass Nancy Brune's %20-encoded URL exactly as shown (requests.get handles it — Pitfall 6).

Per member: resolve UUID at runtime by ext_id (never hardcode a UUID), download the JPEG with normal headers, `convert('RGB')`, `crop_to_4_5` → `resize_600x750` → save JPEG quality=90 optimize=True (re-encode strips EXIF/stego), upload to `politician_photos/{uuid}-headshot.jpg` with x-upsert. On a download/processing failure: SKIP (no fabrication, no overlay) and emit a FAILED manifest line. psycopg2 connect from DATABASE_URL (append sslmode=require if absent), autocommit=True.

Emit a manifest line per member: `SUCCESS: <ext_id> <name> <uuid> -> <cdn_url> [license=us_government_work]` or `FAILED: <ext_id> <name> <reason>`. Print a final tally `N/7 uploaded, M gaps`. Document (header comment) that running the script + Storage upload is the inline-orchestrator step, NOT the executor's. Do NOT run the script.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-lv-city-council-headshots.py"; test -f "$F" &amp;&amp; grep -q "crop_to_4_5" "$F" &amp;&amp; grep -q "resolve_politician_id" "$F" &amp;&amp; grep -q "external_id = %s" "$F" &amp;&amp; grep -q "600" "$F" &amp;&amp; grep -q "750" "$F" &amp;&amp; grep -q "LANCZOS" "$F" &amp;&amp; grep -q "x-upsert" "$F" &amp;&amp; grep -q "sawebfilesprod001.blob.core.windows.net" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; grep -q "us_government_work" "$F" &amp;&amp; [ "$(grep -c "'ext_id'" "$F")" -ge 7 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-lv-city-council-headshots.py exists with crop_to_4_5, resize to (600,750), LANCZOS, JPEG quality=90 optimize=True.
    - resolve_politician_id does a runtime psycopg2 lookup by external_id — no hardcoded UUIDs anywhere.
    - OFFICIALS hardcodes 7 entries (ext_id -3205001..-3205007 + the verified Azure Blob URL) from the RESEARCH roster; the 3 assert lines present.
    - Upload target is politician_photos/{uuid}-headshot.jpg via x-upsert PUT; DEFAULT_LICENSE='us_government_work'.
    - No Wikimedia/WAF branch (plain requests.get); Nancy Brune's %20 URL passed verbatim; Diaz landscape handled by crop_to_4_5 (no special-case code needed).
    - On failure: SKIP + FAILED manifest line (no fabrication, no overlay); manifest emits per-member SUCCESS/FAILED with ext_id, uuid, cdn_url, license; final N/7 tally.
  </acceptance_criteria>
  <done>_tmp-lv-city-council-headshots.py written to disk; passes acceptance criteria. Not yet run.</done>
</task>

<task type="auto">
  <name>Task 2: Run the script inline + write/apply audit-only migration 1065_las_vegas_city_council_headshots.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1065_las_vegas_city_council_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1056_clark_county_commission_headshots.sql (EXACT ANALOG — audit-only politician_images INSERT shape, external_id-subquery form, NOT registered)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md (section "migrations/1065_las_vegas_city_council_headshots.sql" — 5-col external_id-subquery form + column constraints + Headshot Pipeline Invariants + Migration Ledger Rule)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md (Pitfall — no photo_origin_url; do not register 1065; photo_license us_government_work; Pitfall 7 grep-gate)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-01-SUMMARY.md (the 7 external_id→UUID map minted by mig 1064; the confirmed ledger number if renumbered)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): run `python C:/EV-Accounts/backend/scripts/_tmp-lv-city-council-headshots.py` from the EV-Accounts repo root so it reads C:/EV-Accounts/backend/.env. Capture the manifest (per-member ext_id → uuid → cdn_url + license, and any FAILED gaps). Spot-check 2-3 uploaded CDN URLs return HTTP 200.

EXECUTOR STEP: Write `1065_las_vegas_city_council_headshots.sql` as an AUDIT-ONLY migration modeled on 1056_clark_county_commission_headshots.sql. Header comment states: audit-only, applied via execute_sql after the headshot upload, NOT registered in the migration ledger, the structural ledger stays at the confirmed structural number (1064), lists the 7 members by external_id. One idempotent INSERT per member that has an uploaded headshot, column list EXACTLY (id, politician_id, url, type, photo_license): id=gen_random_uuid(), politician_id from subquery (SELECT id FROM essentials.politicians WHERE external_id=<ext_id>), url = the politician_photos/{uuid}-headshot.jpg CDN literal from the manifest, type='default', photo_license='us_government_work'. Guard each with WHERE NOT EXISTS on (politician_id). Do NOT include the removed image-origin column (Pitfall — does not exist). Do NOT add a ledger-registration line. Emit one INSERT per SUCCESS manifest line; OMIT any documented-gap member (and note the gap in a header comment). GREP-GATE HYGIENE: keep the literal token for the removed image-origin column and the ledger table OUT of comments (paraphrase).

INLINE ORCHESTRATOR STEP (documented in the header, not executed by the executor): after the script uploads and the file is written with the real UUIDs substituted, apply `1065_las_vegas_city_council_headshots.sql` via `mcp__supabase-local__execute_sql` (or psql -f). Then count politician_images for the 7 ext_ids → expect 7 minus documented gaps.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1065_las_vegas_city_council_headshots.sql"; test -f "$F" &amp;&amp; grep -q "essentials.politician_images" "$F" &amp;&amp; grep -q "'default'" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; grep -q "us_government_work" "$F" &amp;&amp; grep -q "external_id" "$F" &amp;&amp; ! grep -q "photo_origin_url" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: script run; manifest captured; N/7 uploads recorded (genuine gaps, if any, listed); 2-3 CDN URLs spot-checked HTTP 200.
    - 1065_las_vegas_city_council_headshots.sql exists; one politician_images INSERT per uploaded member, column list exactly (id, politician_id, url, type, photo_license) — the removed image-origin column absent.
    - type='default', photo_license='us_government_work'; each INSERT guarded by WHERE NOT EXISTS on (politician_id); politician_id resolved via external_id subquery.
    - NO ledger registration line (audit-only; structural ledger stays 1064).
    - url literals point at politician_photos/{uuid}-headshot.jpg with the real per-member UUIDs from the manifest.
    - INLINE: 1065 applied; politician_images count for the 7 ext_ids = 7 minus documented gaps.
  </acceptance_criteria>
  <done>Script run inline (7/7 minus documented gaps uploaded), 1065_las_vegas_city_council_headshots.sql written and applied so each sourced member has a non-null 600x750 headshot URL; gaps (if any) documented; structural ledger still 1064.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| remote image host → pipeline | untrusted image bytes from sawebfilesprod001.blob.core.windows.net cross the boundary |
| script → Storage / DB | service-role key authorizes Storage PUT + DB lookup |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-07 | Tampering | malicious image payload (EXIF/stego) from remote Azure Blob | mitigate | PIL `convert('RGB')` + re-encode JPEG optimize=True drops EXIF/embedded data |
| T-162-08 | Information disclosure | service-role key leakage | mitigate | Key read from gitignored .env; script is gitignored `_tmp-*` (never committed); migration file has no key |
| T-162-09 | Spoofing | wrong-person / fabricated photo | mitigate | Hardcoded verified per-member Azure Blob URLs; no fabrication on failure (skip + document gap); operator visual spot-check in Plan 03 |
| T-162-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (Pillow/requests/psycopg2 already in repo scripts); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
After all tasks (inline orchestrator):
1. Script manifest: 7/7 uploads (minus documented gaps), each SUCCESS line has uuid + cdn_url + license.
2. politician_images count for the 7 ext_ids = 7 minus documented gaps, all type='default', photo_license='us_government_work'.
3. 2-3 CDN URLs return HTTP 200.
4. Structural ledger still 1064 (1065 NOT registered).
</verification>

<success_criteria>
- All 7 council members (minus any documented genuine gap) have a 600x750 crop-to-4:5 headshot in politician_photos.
- Each has a politician_images row (type='default', photo_license='us_government_work') with a non-null CDN url.
- Diaz 600x400 landscape correctly center-cropped then resized (no stretch); no fabricated photos, no overlays; gaps documented in the SUMMARY.
- Migration 1065 is audit-only and NOT registered (structural ledger stays 1064).
</success_criteria>

<output>
Create `.planning/phases/162-city-of-las-vegas-deep-seed/162-02-SUMMARY.md` when done (record the upload manifest: 7 ext_id→uuid→cdn_url→license lines, any documented gaps, and the politician_images count).
</output>
