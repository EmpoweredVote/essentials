---
phase: 162-city-of-las-vegas-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts
  - C:/EV-Accounts/backend/migrations/1064_las_vegas_city_council.sql
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [CLARK-02]
user_setup: []

must_haves:
  truths:
    - "6 Las Vegas ward boundary polygons are loaded into essentials.geofence_boundaries as X0015 rows (state='nv' lowercase, geo_id='las-vegas-nv-council-ward-1'..'-6'), each ST_IsValid=true [D-01]"
    - "A standalone government 'City of Las Vegas, Nevada, US' exists (type='City', state='NV' uppercase, geo_id='3240000'), NOT nested under State of Nevada [D-03]"
    - "A chamber 'Las Vegas City Council' (official_count=7) exists under that government [D-02]"
    - "Mayor Shelley Berkley (external_id -3205001) has a LOCAL_EXEC office on the city-wide G4110 district (geo_id='3240000'), title='Mayor', directly elected NOT rotational [D-02]"
    - "Each of the 6 ward council members has a LOCAL office on its per-ward X0015 district (geo_id='las-vegas-nv-council-ward-N'), title='Council Member, Ward N', external_id block -3205002..-3205007 [D-01]"
    - "Post-verify: exactly 1 LOCAL_EXEC office + exactly 6 LOCAL ward offices; office_id back-filled for all 7; zero section-split"
    - "City of Las Vegas surfaces with the purple hasContext chip in src/lib/coverage.js COVERAGE_STATES (new NV block) [CLARK-02 SC#4]"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts"
      provides: "ArcGIS-JSON ward-polygon loader: fetch 6 wards from LV GIS MapServer (f=json, outSR=4326), convert rings→GeoJSON Polygon, INSERT geofence_boundaries as X0015 with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON(...),4326)), assert ST_IsValid"
      contains: "X0015"
    - path: "C:/EV-Accounts/backend/migrations/1064_las_vegas_city_council.sql"
      provides: "Structural migration: pre-flight (asserts 6 X0015 geofences) + 1 government + 1 chamber + 1 LOCAL_EXEC district (Mayor) + 6 LOCAL ward districts + 7 politician+office CTEs + office_id back-fill + post-verify DO block; registers as 1064"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 200
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "COVERAGE_STATES Nevada block with Las Vegas (browseGovernmentList:['3240000'], browseStateAbbrev:'NV', hasContext:true)"
      contains: "Nevada"
  key_links:
    - from: "essentials.geofence_boundaries (6 X0015 ward polygons)"
      to: "essentials.districts (6 LOCAL ward rows, geo_id='las-vegas-nv-council-ward-N')"
      via: "shared geo_id; backend ST_Covers PIP + essentialsService.ts line 646 X% catchall routes X0015 to LOCAL"
      pattern: "las-vegas-nv-council-ward-"
    - from: "essentials.chambers (Las Vegas City Council)"
      to: "essentials.governments (City of Las Vegas, Nevada, US, geo_id='3240000')"
      via: "government_id subquery WHERE name='City of Las Vegas, Nevada, US'"
      pattern: "City of Las Vegas, Nevada, US"
    - from: "essentials.offices (Mayor + 6 ward members)"
      to: "essentials.districts (1 LOCAL_EXEC at geo_id='3240000' + 6 LOCAL at X0015 wards, all state='nv')"
      via: "district join on geo_id + district_type + state='nv' lowercase"
      pattern: "state = 'nv'"
    - from: "src/lib/coverage.js COVERAGE_STATES Nevada block"
      to: "Las Vegas browse (geo_id 3240000)"
      via: "browseGovernmentList array entry"
      pattern: "3240000"
---

<objective>
Seed the City of Las Vegas structurally and make its 6 wards routable. First load the project's FIRST custom non-TIGER ward geofences — 6 Las Vegas ward boundary polygons from the City of Las Vegas GIS MapServer — into essentials.geofence_boundaries as X0015 rows (D-01). Then create a standalone government "City of Las Vegas, Nevada, US" (NOT nested under State of Nevada geo_id='32' — D-03), one chamber "Las Vegas City Council" (official_count=7 — D-02), one LOCAL_EXEC district for the directly-elected Mayor (Shelley Berkley) on the existing city-wide G4110 geofence (geo_id='3240000'), six LOCAL ward districts on the X0015 ward polygons, all 7 politician+office rows, and back-fill office_id. Finally surface Las Vegas in src/lib/coverage.js COVERAGE_STATES (new Nevada block). A Wave-0 DB probe confirms the ledger MAX, the unused external_id block, X0015 availability, and the G4110 city geo_id + casing BEFORE any write, and a roster operator-verification checkpoint precedes the migration apply.

Purpose: Satisfies the structural half of CLARK-02 (Mayor + ward-routed council via the 6 ward polygons) plus the surfacing half (SC#4 — purple hasContext chip). Headshots are Plan 02; stances + final E2E verification are Plan 03.
Output: load-lv-ward-boundaries.ts, 1064_las_vegas_city_council.sql (structural, registered), the coverage.js NV block, plus the captured Wave-0 probe facts + the 7 external_id→UUID map recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and NO DB access):**
- The executor WRITES load-lv-ward-boundaries.ts, the migration .sql, and the coverage.js edit to disk and documents the exact run/apply steps. The executor does NOT run the loader, run DB SELECTs, or apply migrations.
- The Wave-0 DB probes, running the ward loader (`npx tsx scripts/load-lv-ward-boundaries.ts` from the EV-Accounts repo root so it reads C:/EV-Accounts/backend/.env DATABASE_URL), applying mig 1064 (via `psql -f` or `mcp__supabase-local__apply_migration`), and every post-apply audit SELECT are INLINE ORCHESTRATOR steps performed by execute-phase.
- Each task below marks which steps are executor-write vs inline-orchestrator-apply.
- The loader + migration live in the SEPARATE repo C:/EV-Accounts/backend/ (commit via `git -C "C:/EV-Accounts"`). The coverage.js edit lives in the essentials repo (committable normally). SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md
@.planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md
@.planning/phases/162-city-of-las-vegas-deep-seed/162-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
LV city geo_id (TIGER G4110 place): '3240000' (confirmed Phase 158 smoke test SC2)
Government: name='City of Las Vegas, Nevada, US', type='City', state='NV' UPPERCASE, city=NULL, geo_id='3240000' (standalone, NOT under State of Nevada geo_id='32')
Chamber: name='Las Vegas City Council', name_formal='Las Vegas City Council', official_count=7
Ward geofence source: https://mapdata.lasvegasnevada.gov/clvgis/rest/services/AdministrativeBoundaries/CityCouncilWards/MapServer/0/query?where=1%3D1&outFields=CLV_WARDS.WARD&returnGeometry=true&f=json&outSR=4326 (ArcGIS JSON rings, NOT GeoJSON; outSR=4326 mandatory — MapServer default WKID 3421 state plane)
Ward geofence rows: geofence_boundaries (id, geo_id, mtfcc, state, name, geometry, source); mtfcc='X0015', state='nv' LOWERCASE, geo_id='las-vegas-nv-council-ward-N' (N=1..6), source='lasvegasnevada.gov-gis-citcouncilwards-mapserver-2026'; ON CONFLICT (geo_id, mtfcc) DO NOTHING
Ward ring counts (confirmed live): Ward 1=4 rings, 2=1, 3=2, 4=30, 5=21, 6=22 — wrap with ST_Multi; assert ST_IsValid=true in RETURNING (apply ST_MakeValid if false)
Mayor district: 1 LOCAL_EXEC row, state='nv' lowercase, geo_id='3240000', label='City of Las Vegas', mtfcc='G4110' (uses EXISTING Phase 158 G4110 geofence; idempotent guard only)
Ward districts: 6 LOCAL rows, state='nv' lowercase, geo_id='las-vegas-nv-council-ward-N', label='Ward N', mtfcc='X0015'
Office district join keys: Mayor → d.geo_id='3240000' AND d.district_type='LOCAL_EXEC' AND d.state='nv'; ward N → d.geo_id='las-vegas-nv-council-ward-N' AND d.district_type='LOCAL' AND d.state='nv' (uppercase 'NV' matches ZERO rows — silent no-op, the #1 failure mode)
offices.representing_state: 'NV' UPPERCASE (free-text label column, NOT the district join key)
Office NOT EXISTS guard: (district_id, politician_id) — one person per office
chambers.slug is GENERATED ALWAYS — never include the auto-generated path column in INSERT column list; name_formal must NOT be ''
governments has NO unique constraint on geo_id — WHERE NOT EXISTS guard on name is mandatory
external_id block: -3205001 (Berkley/Mayor) .. -3205007 (Brune/Ward 6); BETWEEN ordering -3205007 AND -3205001 (more-negative bound first)
Backend routing — NO CODE CHANGE NEEDED: essentialsService.ts line 646 catches `gb.mtfcc LIKE 'X%' AND gb.mtfcc NOT IN ('X0001','X0002','X0003','X0004') AND d.district_type IN ('LOCAL','COUNTY')` → X0015 routes to LOCAL automatically. Document this; do NOT edit the backend.
Next structural migration: 1064 (Wave-0 probe asserts ledger MAX; memory says ~1064 parked, 161-03-SUMMARY says MAX=1055 with 1056-1063 audit-only). Register with (version,name)=('1064','las_vegas_city_council') OUTSIDE the transaction.
The 7-row roster (ward → external_id → full_name → first/last → party → title):
  Mayor  | -3205001 | Shelley Berkley           | Shelley | Berkley           | Democratic   | Mayor                      [directly elected, NOT rotational]
  Ward 1 | -3205002 | Brian Knudsen             | Brian   | Knudsen           | Non-Partisan | Council Member, Ward 1     [Mayor Pro Tem]
  Ward 2 | -3205003 | Kara Kelley               | Kara    | Kelley            | Non-Partisan | Council Member, Ward 2     [is_appointed=true — appointed Sept 2025]
  Ward 3 | -3205004 | Olivia Diaz               | Olivia  | Diaz              | Non-Partisan | Council Member, Ward 3
  Ward 4 | -3205005 | Francis Allen-Palenske    | Francis | Allen-Palenske    | Non-Partisan | Council Member, Ward 4
  Ward 5 | -3205006 | Shondra Summers-Armstrong | Shondra | Summers-Armstrong | Non-Partisan | Council Member, Ward 5
  Ward 6 | -3205007 | Nancy E. Brune            | Nancy   | Brune             | Non-Partisan | Council Member, Ward 6
LV City Council races are officially nonpartisan → party='Non-Partisan' for the 6 council members (party stored, NEVER displayed — antipartisan). Berkley 'Democratic' acceptable (no display impact).
coverage.js: COVERAGE_STATES array (line 7); insert a NEW Nevada block AFTER the Virginia block (ends line 182), before the closing `];` (line 183). Clark County is already in COVERAGE_COUNTIES — do NOT duplicate it. New NV block: { name: 'Nevada', abbrev: 'NV', areas: [ { label: 'Las Vegas', browseGovernmentList: ['3240000'], browseStateAbbrev: 'NV', hasContext: true } ] }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the ward-geofence loader load-lv-ward-boundaries.ts</name>
  <files>C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-dc-ward-boundaries.ts (PRIMARY ANALOG — pool + fetchJson helper + ward-loop + geofence_boundaries INSERT + error handling/summary; copy the scaffold verbatim)
    - C:/EV-Accounts/backend/scripts/import-greene-county-geofence.ts (geofence_boundaries INSERT with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON(...),4326)) + RETURNING ST_IsValid)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md (section "scripts/load-lv-ward-boundaries.ts" — imports/pool, config constants, fetchJson verbatim, arcgisRingsToGeoJson NEW function, ArcGIS attribute extraction, DB INSERT, error/summary)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md ("Ward Geofence Source" — endpoint, 6 features, ring counts; "Pattern 1"; Pitfall 1 multi-ring; Code Examples §Load LV Ward Boundaries)
  </read_first>
  <action>
Write `load-lv-ward-boundaries.ts` modeled verbatim on `load-dc-ward-boundaries.ts`, with the LV-specific differences documented in 162-PATTERNS.md. Copy the `import 'dotenv/config'` + `Pool` + DATABASE_URL guard + `fetchJson(url)` helper unchanged. The script does NOT insert into geo_districts (DC does; LV only writes geofence_boundaries).

Config constants: LV_WARD_URL = the MapServer query URL from the interfaces block (f=json NOT f=geojson; outSR=4326 mandatory — comment WHY: MapServer default is WKID 3421 state plane); MTFCC='X0015'; STATE_CODE='nv' (lowercase — comment that lowercase is mandatory for LOCAL-tier routing); SOURCE='lasvegasnevada.gov-gis-citcouncilwards-mapserver-2026'; GEO_ID_PREFIX='las-vegas-nv-council-ward-'; EXPECTED_COUNT=6; DRY_RUN from process.argv.includes('--dry-run').

NEW function arcgisRingsToGeoJson(rings: number[][][]): convert the ArcGIS JSON `feature.geometry.rings` to a GeoJSON Polygon string `JSON.stringify({ type: 'Polygon', coordinates: rings })`. This is the one genuinely-new sub-pattern (no codebase analog — the DC loader fetches true GeoJSON via f=geojson; the LV MapServer returns rings via f=json).

Fetch the MapServer URL via fetchJson; the response shape is `{ features: [{ attributes: { WARD: number }, geometry: { rings: number[][][] } }] }`. Build a map keyed by ward number (feature.attributes.WARD, integer 1-6). Assert the feature count === EXPECTED_COUNT (6); if not, console.error and process.exit(1).

Per ward: geo_id = `${GEO_ID_PREFIX}${ward}`; name = `Ward ${ward}`; geomJson = arcgisRingsToGeoJson(rings). INSERT into essentials.geofence_boundaries (id, geo_id, mtfcc, state, name, geometry, source) VALUES (gen_random_uuid(), $1, 'X0015', 'nv', $2, public.ST_Multi(public.ST_SetSRID(public.ST_GeomFromGeoJSON($3), 4326)), $4) ON CONFLICT (geo_id, mtfcc) DO NOTHING RETURNING public.ST_GeometryType(geometry) AS gtype, public.ST_IsValid(geometry) AS valid. Wards 4/5/6 have 21-30 rings (enclaves+holes) — ST_Multi wraps multi-body cases (Pitfall 1). Assert valid===true in the RETURNING; if false, log a clear error and re-run that ward through ST_MakeValid (wrap the geometry expression in public.ST_MakeValid(...)). Under DRY_RUN, skip the INSERT and just log the ward number + ring count + first coordinate (~-115° lon, 36° lat sanity check).

Print a verify hint at the end: `SELECT COUNT(*), bool_and(public.ST_IsValid(geometry)) FROM essentials.geofence_boundaries WHERE state='nv' AND mtfcc='X0015'; -- expect (6, true)`. Wrap with `main().catch((err) => { console.error('[load-lv-ward-boundaries] Fatal error:', err); process.exit(1); })`. Document (header comment) that running this script is the inline-orchestrator step, NOT the executor's. Do NOT run the script.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts"; test -f "$F" &amp;&amp; grep -q "X0015" "$F" &amp;&amp; grep -q "outSR=4326" "$F" &amp;&amp; grep -q "f=json" "$F" &amp;&amp; grep -q "las-vegas-nv-council-ward-" "$F" &amp;&amp; grep -q "ST_GeomFromGeoJSON" "$F" &amp;&amp; grep -q "ST_Multi" "$F" &amp;&amp; grep -q "ST_IsValid" "$F" &amp;&amp; grep -q "'nv'" "$F" &amp;&amp; grep -q "geofence_boundaries" "$F" &amp;&amp; grep -q "EXPECTED_COUNT" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - load-lv-ward-boundaries.ts exists; reads DATABASE_URL from env (guarded), uses pg Pool with ssl rejectUnauthorized:false.
    - LV_WARD_URL uses f=json (NOT f=geojson) and outSR=4326; an arcgisRingsToGeoJson function converts feature.geometry.rings → GeoJSON Polygon string.
    - Inserts into essentials.geofence_boundaries ONLY (no geo_districts) with mtfcc='X0015', state='nv' lowercase, geo_id pattern 'las-vegas-nv-council-ward-N', source label set; ON CONFLICT (geo_id, mtfcc) DO NOTHING.
    - Geometry wrapped in ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON(...),4326)); RETURNING asserts ST_IsValid; ST_MakeValid fallback on invalid.
    - Asserts feature count === 6 (EXPECTED_COUNT) and aborts otherwise; --dry-run path logs without writing.
    - Header comment marks running the script as an inline-orchestrator step. Not yet run.
  </acceptance_criteria>
  <done>load-lv-ward-boundaries.ts written to disk per the DC-substituted scaffold + the new ArcGIS rings→GeoJSON conversion; passes acceptance criteria. Not yet run.</done>
</task>

<task type="auto">
  <name>Task 2: Write structural migration 1064_las_vegas_city_council.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1064_las_vegas_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql (PRIMARY ANALOG — standalone-government structural template: pre-flight + government + chamber + district guard + politician/office CTEs + office_id back-fill + post-verify DO block + ledger registration)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md (section "migrations/1064_las_vegas_city_council.sql" — the step-by-step county→city diff table + pre-flight ward-geofence assertion + 2-district-type handling + the Mayor LOCAL_EXEC CTE + ward CTE + post-verify DO block + ledger line)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-RESEARCH.md ("Pattern 2" structural migration; "Common Pitfalls" 2 two-district-types, 3 lowercase nv, 4 Mayor-not-rotational, 5 Kelley appointed, 7 grep-gate)
  </read_first>
  <action>
Write the structural migration as an NV-city substitution of mig 1055 (Clark County), using the step diff in 162-PATTERNS.md and the 7-row roster from the interfaces block. Wrap STEPS pre-flight through post-verify in a single BEGIN/COMMIT transaction; the ledger registration goes OUTSIDE.

Open with a header comment block: file is 1064_las_vegas_city_council.sql, Phase 162 (CLARK-02), STRUCTURAL (registers in the migration ledger), idempotent, standalone government 'City of Las Vegas, Nevada, US' geo_id='3240000', chamber 'Las Vegas City Council' (official_count=7), 7 seats external_ids -3205001..-3205007, two district types (1 LOCAL_EXEC Mayor on G4110 city geofence + 6 LOCAL ward members on X0015 ward geofences), and the casing reminders (district join keys use LOWERCASE state='nv'; governments.state and offices.representing_state are UPPERCASE 'NV'; the auto-generated chamber path column must never appear in the column list; governments has no unique constraint on geo_id so WHERE NOT EXISTS is required). GREP-GATE HYGIENE: do NOT write the literal tokens for the auto-generated path column, the removed image-origin column, or the ledger table name in explanatory comments — paraphrase (e.g. "the auto-generated path column", "ledger registration"). The ledger registration INSERT keyword in the final step is unavoidable but must be the only occurrence.

PRE-FLIGHT DO block — assert the 6 X0015 ward geofences exist (loaded by load-lv-ward-boundaries.ts in Task 4): if `(SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='nv' AND mtfcc='X0015') < 6` then RAISE EXCEPTION 'Pre-flight FAILED: fewer than 6 X0015 ward geofences — run load-lv-ward-boundaries.ts first.'. Also RAISE NOTICE if the government already exists (idempotent re-run).

STEP 1 — government INSERT (columns id, name, type, state, city, geo_id): gen_random_uuid(), 'City of Las Vegas, Nevada, US', 'City', 'NV' (UPPERCASE), NULL, '3240000', guarded by WHERE NOT EXISTS on name='City of Las Vegas, Nevada, US'. (D-03 — standalone, NOT nested under State of Nevada.)

STEP 2 — chamber INSERT (columns id, name, name_formal, government_id, official_count): gen_random_uuid(), 'Las Vegas City Council', 'Las Vegas City Council', government_id subquery WHERE name='City of Las Vegas, Nevada, US', 7, guarded by WHERE NOT EXISTS on (name, government_id). name_formal must NOT be empty. Never list the auto-generated path column. (D-02.)

STEP 3a — LOCAL_EXEC district for the Mayor (columns id, district_type, state, geo_id, label, mtfcc): gen_random_uuid(), 'LOCAL_EXEC', 'nv' (LOWERCASE), '3240000', 'City of Las Vegas', 'G4110', guarded by WHERE NOT EXISTS on (geo_id='3240000', district_type='LOCAL_EXEC', state='nv'). Uses the EXISTING Phase 158 G4110 city geofence (no new geofence row for the Mayor).

STEP 3b — 6 LOCAL ward districts (one INSERT per ward, same shape): gen_random_uuid(), 'LOCAL', 'nv' (LOWERCASE), 'las-vegas-nv-council-ward-N', 'Ward N', 'X0015', guarded by WHERE NOT EXISTS on (geo_id='las-vegas-nv-council-ward-N', district_type='LOCAL', state='nv'). Repeat for N=1..6.

STEP 4 — 7 politician+office CTE blocks. BLOCK 1 = Mayor Shelley Berkley (-3205001): `WITH ins_p AS (INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id) VALUES (gen_random_uuid(),'Shelley Berkley','Shelley','Berkley','Democratic',true,false,false,true,-3205001) ON CONFLICT (external_id) DO NOTHING RETURNING id)` then INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical) SELECT gen_random_uuid(), d.id, (chamber subquery scoped to government 'City of Las Vegas, Nevada, US'), p.id, 'Mayor', 'NV', false, false, NULL FROM essentials.districts d CROSS JOIN ins_p p WHERE d.geo_id='3240000' AND d.district_type='LOCAL_EXEC' AND d.state='nv' AND p.id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id=d.id AND o.politician_id=p.id). CRITICAL: district_type='LOCAL_EXEC' for the Mayor (Pitfall 4 — directly elected, NOT rotational; NO title-on-seat, NO Mayor Pro Tem on a ward member).

BLOCKS 2-7 = the 6 ward members, same shape but district_type='LOCAL' and geo_id='las-vegas-nv-council-ward-N'. Use the exact roster: Ward 1 Brian Knudsen -3205002 title='Council Member, Ward 1'; Ward 2 Kara Kelley -3205003 title='Council Member, Ward 2' with is_appointed=true (Pitfall 5 — appointed Sept 2025); Ward 3 Olivia Diaz -3205004; Ward 4 Francis Allen-Palenske -3205005; Ward 5 Shondra Summers-Armstrong -3205006; Ward 6 Nancy E. Brune -3205007 (first_name 'Nancy', last_name 'Brune'). party='Non-Partisan' for all 6 council members. role_canonical NULL on all 7. representing_state='NV' UPPERCASE. Office NOT EXISTS guard on (district_id, politician_id).

STEP 5 — office_id back-fill: UPDATE essentials.politicians p SET office_id=o.id FROM essentials.offices o WHERE o.politician_id=p.id AND p.external_id BETWEEN -3205007 AND -3205001 AND p.office_id IS NULL.

STEP 6 — post-verification DO block (adapted from 1055 for 2 district types — Pitfall 2): Gate (a) exactly 1 government row 'City of Las Vegas, Nevada, US'; Gate (b) exactly 1 LOCAL_EXEC office joined to the city district (d.geo_id='3240000', district_type='LOCAL_EXEC', state='nv'); Gate (c) exactly 6 LOCAL ward offices (d.mtfcc='X0015', district_type='LOCAL', state='nv'); Gate (d) section-split detector returns 0 orphan rows. RAISE EXCEPTION on any mismatch; RAISE NOTICE the passing counts.

COMMIT. Then OUTSIDE the transaction, register the structural migration: INSERT the ('1064','las_vegas_city_council') row into the migration ledger using the 2-column (version, name) form ON CONFLICT (version) DO NOTHING. (If the Wave-0 probe finds the live ledger MAX differs from 1063, renumber this file and all of Plan 02/03's files accordingly before applying — Task 3 records the confirmed number.)

Do NOT apply this migration — that is an inline orchestrator step (Task 5). No headshot rows, no stance rows, no compass INSERTs in this file.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1064_las_vegas_city_council.sql"; test -f "$F" &amp;&amp; grep -q "INSERT INTO essentials.governments" "$F" &amp;&amp; grep -q "City of Las Vegas, Nevada, US" "$F" &amp;&amp; grep -q "INSERT INTO essentials.chambers" "$F" &amp;&amp; grep -q "Las Vegas City Council" "$F" &amp;&amp; grep -q "geo_id = '3240000'" "$F" &amp;&amp; grep -q "LOCAL_EXEC" "$F" &amp;&amp; grep -q "X0015" "$F" &amp;&amp; grep -q "las-vegas-nv-council-ward-1" "$F" &amp;&amp; grep -q "las-vegas-nv-council-ward-6" "$F" &amp;&amp; grep -q "state = 'nv'" "$F" &amp;&amp; grep -q -- "-3205001" "$F" &amp;&amp; grep -q -- "-3205007" "$F" &amp;&amp; [ "$(grep -c "ON CONFLICT (external_id) DO NOTHING" "$F")" -eq 7 ] &amp;&amp; grep -q "BETWEEN -3205007 AND -3205001" "$F" &amp;&amp; ! grep -qv '^[[:space:]]*--' "$F" 2>/dev/null; grep -vE '^[[:space:]]*--' "$F" | grep -q "X0015 ward geofences" ; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File 1064_las_vegas_city_council.sql exists.
    - Pre-flight DO block asserts ≥6 X0015 ward geofences exist (RAISE EXCEPTION otherwise).
    - Exactly one government INSERT ('City of Las Vegas, Nevada, US', type='City', state='NV' UPPERCASE, geo_id='3240000'), WHERE NOT EXISTS guarded — standalone, no parent linkage to State of Nevada.
    - Exactly one chamber INSERT ('Las Vegas City Council', name_formal non-empty, official_count=7), scoped to the LV government, WHERE NOT EXISTS guarded; the auto-generated chamber path column is NOT in the column list.
    - One idempotent LOCAL_EXEC district INSERT (geo_id='3240000', mtfcc='G4110', state='nv' lowercase) + six idempotent LOCAL ward district INSERTs (geo_id='las-vegas-nv-council-ward-1'..'-6', mtfcc='X0015', state='nv' lowercase).
    - Exactly 7 politician CTEs (`ON CONFLICT (external_id) DO NOTHING` appears 7 times) covering -3205001..-3205007 with the exact roster names/titles; the 6 council members are party='Non-Partisan'; Kelley (Ward 2) is is_appointed=true.
    - BLOCK 1 (Mayor) uses district_type='LOCAL_EXEC' on geo_id='3240000'; BLOCKS 2-7 use district_type='LOCAL' on the matching ward geo_id. Every office WHERE clause uses d.state='nv' lowercase; every office sets representing_state='NV' uppercase and role_canonical NULL; NOT EXISTS guard on (district_id, politician_id) and `AND p.id IS NOT NULL`.
    - Mayor is directly elected (LOCAL_EXEC), NOT rotational/title-on-seat; no Mayor Pro Tem set as a title on a ward member's office.
    - One office_id back-fill (BETWEEN -3205007 AND -3205001, guarded by office_id IS NULL).
    - Post-verification DO block asserts 1 government + 1 LOCAL_EXEC office + 6 LOCAL ward offices + 0 section-split.
    - The ledger registration line for ('1064','las_vegas_city_council') appears OUTSIDE the BEGIN/COMMIT block (2-column form); no audit/headshot/stance rows in this file.
    - No `photo_origin_url`; the auto-generated path column name does not appear in any INSERT column list or comment.
  </acceptance_criteria>
  <done>1064_las_vegas_city_council.sql is written to disk per the 1055-substituted template with two district types and the pre-flight ward assertion; passes all acceptance criteria. Not yet applied.</done>
</task>

<task type="auto">
  <name>Task 3: Add Nevada/Las Vegas to src/lib/coverage.js COVERAGE_STATES</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the COVERAGE_STATES array — the Virginia block at lines 176-182 for the exact shape; the closing `];` at line 183 is the insertion anchor)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md (section "src/lib/coverage.js" — exact NV block shape + insertion point + note that Clark County is already in COVERAGE_COUNTIES)
  </read_first>
  <action>
Add a NEW Nevada block to the COVERAGE_STATES array in src/lib/coverage.js, mirroring the existing Virginia block (lines 176-182). Insert it AFTER the Virginia block's closing `},` (line 182) and BEFORE the closing `];` of COVERAGE_STATES (line 183). The new block: `{ name: 'Nevada', abbrev: 'NV', areas: [ { label: 'Las Vegas', browseGovernmentList: ['3240000'], browseStateAbbrev: 'NV', hasContext: true } ] },`. Set hasContext:true because Phase 162 seeds evidence-only compass stances (purple chip). Do NOT add Clark County here — it already lives in COVERAGE_COUNTIES (line ~216 area). Do NOT touch any other state block, the COVERAGE_COUNTIES array, STATE_NAME_TO_ABBREV (nevada:'NV' already present at line 192), or coverageAreaToPath() logic. Additional NV cities (Henderson, North Las Vegas, Boulder City) will be added to this same NV block in Phases 163-165 — leave room. This is the only essentials-repo change in the phase (committable normally).
  </action>
  <verify>
    <automated>F="C:/Transparent Motivations/essentials/src/lib/coverage.js"; grep -q "name: 'Nevada'" "$F" &amp;&amp; grep -q "'3240000'" "$F" &amp;&amp; grep -q "label: 'Las Vegas'" "$F" &amp;&amp; node --input-type=module -e "import('file://$F').then(()=>console.log('PARSE_OK'))" 2>/dev/null; echo done</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE_STATES contains a new Nevada block: name 'Nevada', abbrev 'NV', areas with a Las Vegas entry (browseGovernmentList ['3240000'], browseStateAbbrev 'NV', hasContext true).
    - The block is inside COVERAGE_STATES (after Virginia, before the closing `];`), NOT in COVERAGE_COUNTIES.
    - Clark County is NOT duplicated into COVERAGE_STATES; COVERAGE_COUNTIES is untouched.
    - No edit to coverageAreaToPath() or STATE_NAME_TO_ABBREV; file still parses (no syntax error).
  </acceptance_criteria>
  <done>Nevada/Las Vegas added to COVERAGE_STATES with hasContext:true and browseGovernmentList ['3240000']; file parses cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Wave-0 DB probes + ward loader run (inline) + roster operator-verification checkpoint</name>
  <action>
INLINE ORCHESTRATOR STEPS (run BEFORE applying the structural migration — gsd-executor has NO supabase MCP):
(P1) Ledger MAX — run `SELECT MAX(CAST(version AS INTEGER)) FROM supabase_migrations.schema_migrations;`. Confirm the next structural number. If it is NOT 1064 (e.g. live MAX shows the v18.0-park drift), RENUMBER 1064 + Plan 02's 1065 + Plan 03's 1066-1072 to start at (MAX+1) before applying, and record the confirmed numbers in the SUMMARY.
(P2) external_id collision check — run `SELECT external_id FROM essentials.politicians WHERE external_id BETWEEN -3205007 AND -3205001 ORDER BY 1;`. Assert ZERO rows (the -3205001..-3205007 block is unused). If any row exists, pick the next unused 7-block and re-thread it through all three plans before proceeding.
(P3) X0015 availability — run `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0015';`. Assert 0 (X0015 unclaimed) BEFORE running the loader. (After the loader runs it should be 6.)
(P4) LV G4110 city geo_id + casing — run `SELECT geo_id, state, mtfcc FROM essentials.geofence_boundaries WHERE name ILIKE '%Las Vegas%' AND mtfcc='G4110';`. Assert exactly the LV place row with geo_id='3240000' and confirm the city/place geofence casing the LOCAL_EXEC district WHERE clause must match.

THEN run the ward loader inline: `npx tsx C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts` from the EV-Accounts repo root (reads C:/EV-Accounts/backend/.env DATABASE_URL). Optionally dry-run first (`--dry-run`) to confirm 6 features + sane coordinates. Then run for real and verify `SELECT COUNT(*), bool_and(public.ST_IsValid(geometry)) FROM essentials.geofence_boundaries WHERE state='nv' AND mtfcc='X0015';` returns (6, true). If ST_IsValid is false on any ward, fix via ST_MakeValid (re-run that ward) before proceeding.

Then PAUSE for the roster operator-verification checkpoint below. Wait for the resume signal BEFORE the structural migration is applied (Task 5). No structural DB writes in this task beyond the ward geofences.
  </action>
  <what-built>Wave-0 probes ran inline: ledger MAX confirmed (next structural migration number locked — renumbered if drift found); external_id block -3205001..-3205007 confirmed unused; X0015 confirmed unclaimed then loaded with 6 LV ward polygons; the LV G4110 city geofence confirmed at geo_id='3240000'. The 6 ward boundary polygons are loaded into geofence_boundaries (X0015, state='nv', all ST_IsValid=true). The structural migration 1064 is written (Task 2) but NOT yet applied. The 7-seat roster (Mayor + 6 ward members) is staged for your sign-off.</what-built>
  <how-to-verify>
1. Probe results — confirm P1 (next structural number), P2 returned 0 rows, P3 returned 0 BEFORE the loader, P4 returned the LV G4110 row at geo_id='3240000'. If any fails, STOP and report.
2. Ward geofences — confirm the loader inserted 6 X0015 rows, all ST_IsValid=true. If <6 or any invalid, STOP (the migration's pre-flight will block anyway).
3. Roster currency — spot-check the 7 seats against lasvegasnevada.gov/Government/Mayor-City-Council: Mayor Shelley Berkley (directly elected, in office since Jan 2025); Ward 1 Brian Knudsen (Mayor Pro Tem); Ward 2 Kara Kelley (appointed Sept 30 2025 to fill the Seaman vacancy); Ward 3 Olivia Diaz; Ward 4 Francis Allen-Palenske; Ward 5 Shondra Summers-Armstrong; Ward 6 Nancy E. Brune. Confirm no vacancy and no mid-2026 appointment/resignation since research.
4. MAYOR MODEL — confirm Berkley is DIRECTLY elected at-large (NOT a rotational/title-on-seat mayor): the migration gives her a LOCAL_EXEC office on the city-wide geofence with title='Mayor', and NO ward member is flagged as Mayor Pro Tem via title-on-seat (Knudsen's Mayor-Pro-Tem role is noted in research but his office title is 'Council Member, Ward 1').
5. Kelley appointment — confirm Kara Kelley (Ward 2) is is_appointed=true (appointed, still a legitimate incumbent).
6. Name forms — confirm 'Francis Allen-Palenske', 'Shondra Summers-Armstrong', 'Nancy E. Brune' spellings.
7. If any seat changed: tell the executor the corrected ward→name→party→title before approving.
  </how-to-verify>
  <verify><human-check>Operator confirms the Wave-0 probe results (ledger number locked, 0 external_id collisions, X0015 unclaimed then 6 valid ward polygons loaded, G4110 city geofence at 3240000), and the 7-seat roster currency + directly-elected-Mayor model against lasvegasnevada.gov before migration 1064 is applied.</human-check></verify>
  <done>Probes pass, 6 X0015 ward polygons loaded (all ST_IsValid), and the operator approves the 7-seat roster with Berkley modeled as a directly-elected Mayor (or supplies corrections).</done>
  <resume-signal>Type "approved" or describe any roster change (new appointment, vacancy, mayor model, spelling), probe mismatch, ledger renumber, or ward-geofence issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Apply migration 1064 inline + roster/district/section-split/ledger audit</name>
  <files>C:/EV-Accounts/backend/migrations/1064_las_vegas_city_council.sql</files>
  <read_first>
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-VALIDATION.md (Per-Task Verification Map — ward count, office counts, section-split)
    - .planning/phases/162-city-of-las-vegas-deep-seed/162-PATTERNS.md (Migration Ledger Rule + Casing Convention + post-verify DO block)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): apply C:/EV-Accounts/backend/migrations/1064_las_vegas_city_council.sql via `psql -f` (DATABASE_URL from C:/EV-Accounts/backend/.env) or `mcp__supabase-local__apply_migration`. The migration's pre-flight asserts 6 X0015 geofences; its DO block gates 1 government + 1 LOCAL_EXEC + 6 LOCAL + 0 section-split. Then run the audits and record results for the SUMMARY:
(1) Ward geofences — `SELECT COUNT(*), bool_and(public.ST_IsValid(geometry)) FROM essentials.geofence_boundaries WHERE state='nv' AND mtfcc='X0015'` → expect (6, true).
(2) Government + chamber — exactly 1 government 'City of Las Vegas, Nevada, US' (geo_id='3240000') + 1 chamber 'Las Vegas City Council' (official_count=7) under it.
(3) Mayor office — exactly 1 LOCAL_EXEC office joined to district geo_id='3240000', district_type='LOCAL_EXEC', state='nv', title='Mayor'.
(4) Ward offices — exactly 6 LOCAL offices joined to X0015 ward districts (district_type='LOCAL', state='nv'), one per ward 1-6.
(5) Total council offices = 7 (1 Mayor + 6 wards); no phantom 8th office; Mayor not duplicated across two districts.
(6) office_id back-fill — `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -3205007 AND -3205001 AND office_id IS NOT NULL` → expect 7.
(7) Casing — DISTINCT state on districts linked to the Las Vegas City Council chamber → expect only 'nv' lowercase.
(8) Section-split — run the LOCAL-tier section-split SQL → expect 0 rows.
(9) Ledger — confirm ('1064','las_vegas_city_council') registered and ledger MAX = the confirmed structural number.
Capture the 7 external_id → politician UUID map (`SELECT external_id, id FROM essentials.politicians WHERE external_id BETWEEN -3205007 AND -3205001 ORDER BY external_id`) — Plan 02's headshot script + migration 1065 and Plan 03's stance migrations consume this. Record any deviation (an office count below 7 implies a casing or guard miss — fix and re-apply idempotently).
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1064_las_vegas_city_council.sql" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: migration 1064 applied successfully (pre-flight + DO block passed; no path-column errors).
    - 6 X0015 ward geofences present, all ST_IsValid=true.
    - 1 LV government + 1 chamber (official_count=7).
    - Exactly 1 LOCAL_EXEC Mayor office + exactly 6 LOCAL ward offices = 7 total; no phantom 8th; Mayor not duplicated.
    - office_id back-filled for all 7 (count = 7).
    - DISTINCT linked-district state = only 'nv' lowercase.
    - Section-split query returns 0 rows.
    - Ledger registers ('1064','las_vegas_city_council') (or the renumbered structural version); MAX = that number.
    - 7 external_id → UUID map captured for all 7 seats (handed to Plans 02 + 03).
  </acceptance_criteria>
  <done>Migration 1064 applied; Mayor on LOCAL_EXEC + 6 ward members on LOCAL X0015 districts at state='nv', office_id back-filled, 6 valid ward geofences, 0 section-split, ledger registered, and the 7 UUIDs captured for Plans 02 + 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LV GIS MapServer → loader | untrusted geometry bytes from mapdata.lasvegasnevada.gov cross into geofence_boundaries |
| migration/loader → live DB | service-role apply of seed SQL + parameterized geofence INSERTs; only the inline orchestrator runs them |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01 | Tampering | SQL injection via dynamic ward geometry INSERT | mitigate | Loader uses parameterized $1/$2/$3/$4 binds (geo_id, name, GeoJSON, source) — no string concatenation; ST_GeomFromGeoJSON parses bound geometry text |
| T-162-02 | Tampering | invalid/malformed ward geometry (GEOMETRYCOLLECTION, self-intersection) | mitigate | RETURNING ST_IsValid asserted true; ST_MakeValid fallback; Task 5 check (1) bool_and(ST_IsValid)=true |
| T-162-03 | Tampering | silent zero-office no-op from uppercase state='NV' in district WHERE | mitigate | Every district/office WHERE uses lowercase d.state='nv'; Wave-0 P4 confirms G4110 casing; post-verify asserts 1 LOCAL_EXEC + 6 LOCAL |
| T-162-04 | Tampering | wrong-government section split (offices attach under State of Nevada geo_id='32') | mitigate | Chamber scoped to government_id WHERE name='City of Las Vegas, Nevada, US'; section-split SQL gate must return 0 rows + DO block Gate (d) |
| T-162-05 | Spoofing | Mayor mis-modeled as rotational / title-on-seat (Pitfall 4) | mitigate | Roster checkpoint (Task 4 step 4) confirms directly-elected LOCAL_EXEC model; post-verify asserts exactly 1 LOCAL_EXEC office |
| T-162-06 | Information disclosure | service-role key / DATABASE_URL leakage | accept | DATABASE_URL read from gitignored C:/EV-Accounts/backend/.env; never hardcoded in loader or migration; applied by inline orchestrator only |
| T-162-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (pg/tsx/dotenv already in repo scripts); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
After all tasks (inline orchestrator):
1. 6 X0015 ward geofences loaded, all ST_IsValid=true.
2. 1 LV government + 1 chamber (official_count=7); exactly 1 LOCAL_EXEC Mayor office + 6 LOCAL ward offices = 7; office_id back-filled for all 7.
3. DISTINCT linked-district state = only 'nv' lowercase; section-split SQL → 0 rows.
4. Ledger MAX = the confirmed structural number; ('1064','las_vegas_city_council') registered.
5. coverage.js COVERAGE_STATES contains the Nevada/Las Vegas entry (hasContext:true, ['3240000']).
</verification>

<success_criteria>
- 6 Las Vegas ward boundary polygons loaded as X0015 geofences (state='nv', all ST_IsValid).
- Standalone government 'City of Las Vegas, Nevada, US' (geo_id='3240000') + chamber 'Las Vegas City Council' (official_count=7) created, NOT under State of Nevada.
- Mayor Berkley on a LOCAL_EXEC city-wide district (directly elected, NOT rotational) + 6 ward members each on their LOCAL X0015 ward district; office_id back-filled; no phantom 8th seat.
- Zero section-split; casing 'nv' lowercase; migration ledger registered at the confirmed structural number.
- Las Vegas surfaced with the purple hasContext chip in coverage.js COVERAGE_STATES.
- No headshots, no stances, no fabricated members.
</success_criteria>

<output>
Create `.planning/phases/162-city-of-las-vegas-deep-seed/162-01-SUMMARY.md` when done (record the Wave-0 probe results incl. the confirmed/renumbered ledger number, the 6-ward load confirmation, the 7 external_id→UUID map, the coverage.js edit, and all Task-5 audit counts).
</output>
