---
phase: 181-city-of-sherwood-deep-seed
plan: 05
type: execute
wave: 4
depends_on: ["181-03", "181-04"]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/buildingImages.js
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WASH-07]

must_haves:
  truths:
    - "A legally-licensed 1700×540 community banner for Sherwood is uploaded to Supabase Storage cities/sherwood.jpg (single-word slug per the pipeline naming convention)"
    - "buildingImages.js CURATED_LOCAL has a single-word 'sherwood' key pointing at the banner CDN URL, with an attribution comment (verified license)"
    - "The 'sherwood' key correctly matches because getBuildingImages() does city.includes(key) against representing_city.toLowerCase() ('sherwood') — no space-vs-hyphen trap since Sherwood is a single word (unlike Forest Grove's two-word key)"
    - "coverage.js Oregon block has a 'Sherwood' entry with browseGovernmentList ['4167100'], browseStateAbbrev 'OR', hasContext:true, alphabetically between Portland and Tigard"
    - "npm run build passes with both edits"
    - "The live browse link (browse_geo_id=4167100) surfaces Mayor Rosener first + all 6 councilors with headshots + stances, the community banner renders (not the tier gradient), and no party label appears"
    - "The banner subject honors D-14: a recognizable everyday street-level Old Town scene (Railroad St primary) or a true multi-roofline skyline — NEVER a single-building roof crop, NEVER an aerial"
    - "Decision coverage — D-10: legally-licensed Wikimedia-first banner, no AI images, banner-asset-pipeline followed to cities/sherwood.jpg; D-11: banner asset work lives in this final surfacing plan; D-12: coverage.js Oregon block Sherwood entry with hasContext:true (geo_id 4167100); D-14: Old Town street-level / true-skyline subject honored (Railroad St primary, Downtown alternate), failure modes rejected; live browse link provided at completion (browse_geo_id=4167100&browse_mtfcc=G4110)"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "Sherwood purple hasContext chip in the Oregon COVERAGE_STATES block"
      contains: "Sherwood"
    - path: "C:/Transparent Motivations/essentials/src/lib/buildingImages.js"
      provides: "'sherwood' CURATED_LOCAL banner entry"
      contains: "sherwood"
  key_links:
    - from: "coverage.js Oregon areas"
      to: "browse route (browse_geo_id=4167100)"
      via: "browseGovernmentList entry"
      pattern: "label: 'Sherwood'"
    - from: "buildingImages.js CURATED_LOCAL 'sherwood'"
      to: "offices.representing_city='Sherwood' (set inline in mig 1187)"
      via: "getBuildingImages() lowercased substring match"
      pattern: "sherwood:"
---

<objective>
Close the phase: (1) source, process, and upload a legally-licensed 1700×540 community banner for
Sherwood; (2) wire it via the single-word `'sherwood'` CURATED_LOCAL key in buildingImages.js;
(3) surface Sherwood in the Oregon block of coverage.js with the purple hasContext chip, using geo_id
4167100 (CORRECTED). The banner renders because mig 1187 set `offices.representing_city='Sherwood'`
inline (no backfill needed).

Purpose: Satisfy WASH-07's purple hasContext chip criterion + the community banner standing
constraint, and verify the full roster end-to-end on the live app.
Output: banner in Storage (cities/sherwood.jpg) + two essentials-repo edits + passing build +
live-browse confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-03-SUMMARY.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-04-SUMMARY.md
@src/lib/coverage.js
@src/lib/buildingImages.js
@docs/banner-asset-pipeline.md

**This is the essentials repo** — coverage.js and buildingImages.js are committed HERE (`git`), NOT in
C:/EV-Accounts. The purple hasContext chip and the banner-render path are already built; only the data
row + banner asset + CURATED_LOCAL key are new. The banner upload uses the existing committed scripts
scripts/banners/process_banner.py + upload_banner.py — no new script file.

**TAILWIND HYGIENE:** do NOT introduce raw Windows `\` paths into any committed file (Tailwind v4
scans .md/.js and a `\f...` sequence crashes the prod build). Use forward-slash CDN URLs only.

**SINGLE-WORD KEY (no trap here):** Sherwood is a single word, so the CURATED_LOCAL key is simply
`sherwood` (lowercase, unquoted is fine) — getBuildingImages() does `city.includes(key)` against
`offices.representing_city.toLowerCase()` = `'sherwood'`, which matches cleanly. Unlike Forest Grove's
two-word `'forest grove'` (space) key, there is no space-vs-hyphen pitfall. The Supabase Storage PATH
is `cities/sherwood.jpg` (also single-word) — no asymmetry to manage.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Orchestrator sources + processes + uploads the community banner</name>
  <decision>Which legally-licensed wide photo becomes the Sherwood community banner</decision>
  <context>
    Priority hint (D-14): a recognizable everyday street-level Old Town Sherwood scene (the historic
    Smockville-era main street / Cannery Square area) OR a true wide multi-roofline skyline. RESEARCH
    identified two strong, visually-inspected Wikimedia Commons candidates, both CC BY 3.0 by
    photographer "dreid1987", both native 4:3 (~1.33:1) needing a substantial (~58%) vertical crop to
    the pipeline's 3.15:1 target (the compositional call was previewed in RESEARCH and re-viewed at
    Wave-0 — see 181-01-SUMMARY.md for the recorded crop assessment):
    - PRIMARY "File:Railroad St - panoramio.jpg" (3264×2448) — a vibrant, recognizable Old Town
      commercial street (multiple two/three-story storefronts incl. a turreted corner building, marked
      crosswalk, hanging flower baskets) — the exact match to D-14's top hint. Try --vertical-anchor in
      the 0.4-0.55 range to preserve the storefronts/crosswalk while trimming sky.
    - ALTERNATE "File:Downtown - panoramio - dreid1987.jpg" (3264×2448) — a quieter historic-cottage
      street scene; buildings sit lower in frame and may crop more forgivingly.
    D-14 REJECTED failure modes (do NOT reconsider): Sherwood DJIMini 1/2 (aerials), Library close-up
    (single-building), Looking down Sunset Blvd (generic suburban overlook), Old mill (no mill in
    frame), DSCN6746 coffee company (565×373, too low-res). A clean license + crops-well-to-3.15:1
    beats subject preference. Wikimedia Commons FIRST, Unsplash fallback. NO AI-generated images; no
    baked-in text/graphics. Follow docs/banner-asset-pipeline.md Stages 1-8. Run:
    `python scripts/banners/process_banner.py --input <raw> --output <processed_1700x540.jpg> --vertical-anchor <0.4-0.55>`
    then `python scripts/banners/upload_banner.py --file <processed_1700x540.jpg> --path cities/sherwood.jpg`.
    Record the final Title | Author | License (verified) for the attribution comment used in Task 2.
  </context>
  <options>
    <option id="railroad-st">
      <name>Wikimedia "Railroad St - panoramio.jpg" (D-14 top hint, preferred)</name>
      <pros>License confirmed CC BY 3.0 via imageinfo API; exact match to D-14's Old Town street-level hint; 3264×2448 high-res; recognizable downtown storefronts, no identifiable faces</pros>
      <cons>Native 4:3 needs a ~58% vertical crop to 3.15:1 — risks trimming storefront/crosswalk context; verify the actual crop at 1700×540 before finalizing (assessment recorded at Wave-0)</cons>
    </option>
    <option id="downtown">
      <name>Wikimedia "Downtown - panoramio - dreid1987.jpg" (D-14 alternate)</name>
      <pros>Same CC BY 3.0 license/photographer; buildings sit lower in frame so may crop more forgivingly; quieter historic-cottage street scene, still street-level per D-14</pros>
      <cons>Native ~1.33:1 still needs a substantial height crop; less busy/less obviously "downtown" than Railroad St</cons>
    </option>
    <option id="unsplash">
      <name>Unsplash fallback photo</name>
      <pros>Higher chance of a wide, crop-friendly street/skyline shot</pros>
      <cons>Attribution/license differs from the Wikimedia convention; confirm Unsplash license terms; must still satisfy D-14 (street-level or true skyline, no single-building crop, no aerial)</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen source + final Title | Author | License string (verified license), confirming cities/sherwood.jpg uploaded (CDN URL returns 200).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner in buildingImages.js and surface Sherwood in coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/buildingImages.js, C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - src/lib/buildingImages.js lines 102-118 (CURATED_LOCAL block + attribution comments, incl. beaverton/hillsboro/tigard/tualatin/'forest grove' entries) + the getBuildingImages() city.includes(key) match loop
    - src/lib/coverage.js lines 96-110 (the Oregon block — Beaverton…Wood Village, incl. Portland + Tigard)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md §"src/lib/coverage.js (config edit …)" and §"src/lib/buildingImages.js (config edit …)" (exact inserts + the single-word-key note)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md (confirmed 'sherwood' key format + geo_id 4167100) + the Task 1 attribution string
  </read_first>
  <action>
    Edit 1 — buildingImages.js: add a `sherwood` key (single word, lowercase) to CURATED_LOCAL pointing
    at
    `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/sherwood.jpg`,
    placed alongside the existing beaverton/hillsboro/tigard/tualatin/'forest grove' entries. Add an
    attribution comment line above CURATED_LOCAL in the existing format `//   sherwood - <Title> |
    <Author> | <License>` using the string recorded in Task 1 (default per RESEARCH: `Railroad St,
    Sherwood, Oregon | dreid1987 | CC BY 3.0`, with the verified license). The single-word key resolves
    because getBuildingImages() lowercases offices.representing_city and does a city.includes(key)
    substring match; mig 1187 set representing_city='Sherwood' inline so 'sherwood' resolves at first
    browse. There is NO space/hyphen trap (single word) — both the key and the Storage path are
    'sherwood'.
    Edit 2 — coverage.js: insert `{ label: 'Sherwood', browseGovernmentList: ['4167100'],
    browseStateAbbrev: 'OR', hasContext: true }` into the Oregon `areas` array, alphabetically BETWEEN
    'Portland' and 'Tigard' ("Po" < "Sh" < "Ti"). Match the existing column alignment of surrounding
    rows. Use geo_id '4167100' (CORRECTED — NOT the stated 4167450). Change nothing else in either file
    — no other state blocks, no other CURATED_LOCAL keys. No raw Windows `\` paths anywhere
    (forward-slash CDN URLs only).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has a `sherwood` key (single word, lowercase) → the cities/sherwood.jpg CDN URL
    - An attribution comment `//   sherwood - ... | ... | ...` is present above CURATED_LOCAL with the verified license
    - coverage.js Oregon areas has 'Sherwood' between 'Portland' and 'Tigard'
    - Entry is exactly { label: 'Sherwood', browseGovernmentList: ['4167100'], browseStateAbbrev: 'OR', hasContext: true }
    - geo_id is 4167100 (CORRECTED), NOT 4167450
    - No other lines changed in either file; no raw `\` path introduced
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Banner wired via the single-word 'sherwood' key + Sherwood surfaced with geo_id 4167100; production build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-browse end-to-end verification + commit</name>
  <what-built>The Sherwood banner (Storage) + buildingImages.js 'sherwood' key + coverage.js purple chip.</what-built>
  <how-to-verify>
    1. Confirm `npm run build` passed (Task 2 gate).
    2. Browse essentials.empowered.vote/results?browse_geo_id=4167100&browse_mtfcc=G4110 and confirm:
       - Mayor Tim Rosener sorts FIRST, all 6 councilors follow (Young/Brouse/Giles/Mays/Scott/Standke);
         no empty LOCAL section; no section-split.
       - Headshots render 600×750 with no overlays (7/7 per plan 03); compass stances visible; NO party
         label (antipartisan).
       - The Local section shows the Sherwood community banner photo (NOT the tier gradient fallback) —
         this confirms representing_city='Sherwood' (mig 1187) + the single-word 'sherwood' CURATED_LOCAL
         key resolve. If it shows the gradient, the key or representing_city is wrong.
       - The purple hasContext chip appears for Sherwood.
    3. Optionally enter a real Sherwood address and confirm the same roster resolves via /representatives/me.
    4. Commit both files in the essentials repo: `git add src/lib/coverage.js src/lib/buildingImages.js && git commit` (on a branch if the harness is on the default branch; end the message with the Co-Authored-By trailer).
  </how-to-verify>
  <acceptance_criteria>
    - Live browse shows Mayor Rosener first + 6 councilors, no empty LOCAL section, no section-split
    - Headshots 600×750 no overlays (7/7); stances visible; no party label; no wrong-person image
    - Community banner renders (not gradient fallback) — confirms the 'sherwood' key resolved
    - Purple hasContext chip shows for Sherwood
    - coverage.js + buildingImages.js committed in the essentials repo
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the live browse + banner, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Banner sourced from Wikimedia Commons / Unsplash |
| operator → Supabase Storage | Processed banner uploaded to public bucket cities/sherwood.jpg |
| build pipeline → prod | coverage.js + buildingImages.js edits shipped via the essentials build/deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-22 | Spoofing/IP | Unlicensed or AI-generated banner image | mitigate | Wikimedia-first with recorded Title/Author/License (Railroad St / Downtown both CC BY 3.0 confirmed via imageinfo API); NO AI-generated images (pipeline forbids); decision checkpoint records the license string |
| T-181-23 | Tampering | Wrong CURATED_LOCAL key never matches → banner silently falls back to the tier gradient | mitigate | Single-word 'sherwood' key matches representing_city.toLowerCase() via city.includes(key); acceptance criteria assert the key; live-browse gate confirms the banner renders (not gradient) |
| T-181-24 | Tampering | Malformed coverage.js/buildingImages.js breaks the prod build | mitigate | `npm run build` gate before commit; additive edits only |
| T-181-25 | Denial of Service | Windows `\` path in committed file crashes Tailwind scan | mitigate | Edits add only forward-slash CDN URLs + data rows; no raw backslash introduced |
| T-181-26 | Spoofing | Banner that violates D-14 (single-building roof crop or aerial) misrepresents the city | mitigate | D-14 winning classes enforced (street-level Old Town or true skyline); documented failure-mode candidates rejected; decision checkpoint + Wave-0 crop assessment |
| T-181-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; banner scripts (process_banner.py/upload_banner.py) are pre-existing committed tooling |
</threat_model>

<verification>
- `npm run build` exits 0; banner uploaded + rendered (not gradient); live browse surfaces
  Mayor-first roster with chip, stances, headshots, banner, no section-split, no party label; both
  files committed.
</verification>

<success_criteria>
- Sherwood visible with the purple hasContext chip + community banner in the Oregon block (geo_id
  4167100, single-word 'sherwood' key); build green; live browse confirms the full roster + banner
  end-to-end.
</success_criteria>

<output>
Create `.planning/phases/181-city-of-sherwood-deep-seed/181-05-SUMMARY.md` when done, recording the
banner source/license and the live-browse confirmation. This is the final plan — note WASH-07 is
satisfied end-to-end.
</output>
