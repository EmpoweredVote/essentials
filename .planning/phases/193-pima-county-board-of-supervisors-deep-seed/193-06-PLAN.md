---
phase: 193-pima-county-board-of-supervisors-deep-seed
plan: 06
type: execute
wave: 6
depends_on: ["193-01", "193-02", "193-03", "193-04", "193-05"]
files_modified: []
autonomous: false
requirements: [PIMA-01, BANR-01]
must_haves:
  truths:
    - "The full production audit block is all-green: 5 X0019 geofences, standalone Pima County government, 5 supervisor offices (1 per district), Cano appointed, 5/5 600×750 headshots, evidence-only cited stances, clean section-split, DB-honest coverage chip, banner CDN 200"
    - "A live address in each of the 5 districts routes to exactly 1 correct supervisor with the right headshot"
    - "The Pima County banner renders via the coverage.js browse chip"
  artifacts:
    - path: ".planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-06-SUMMARY.md"
      provides: "Full audit results + live-browse sign-off; the phase verification record"
      contains: "audit"
  key_links:
    - from: "live address per district"
      to: "one Pima County supervisor profile"
      via: "X0019 geofence PIP routing (X% MTFCC catchall)"
      pattern: "supervisor"
    - from: "coverage.js 'Pima County' chip"
      to: "Catalinas/Sonoran banner render"
      via: "browse_label → representingCity → getBuildingImages"
      pattern: "pima county"
---

<objective>
Final phase verification. Run the full production audit block covering every prior wave (geofences,
government/roster, headshots, stances, banner, coverage) and a blocking live-browse human-verify that
confirms per-district address routing, correct-person headshots, and the banner rendering via the
coverage chip. This is the goal-backward gate for ROADMAP success criteria #1-5 before
`/gsd-verify-work`.

Purpose: Proves PIMA-01 + BANR-01 are TRUE end-to-end in production — not just that migrations were
authored, but that a real Pima County resident sees their one supervisor with a full compass, a
correct headshot, and the county banner.
Output: `.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-06-SUMMARY.md` — the
phase verification record with the audit results + operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-01-SUMMARY.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-02-SUMMARY.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-03-SUMMARY.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-04-SUMMARY.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-05-SUMMARY.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / live-browser access. Both tasks are ORCHESTRATOR-RUN /
human-verify: the inline orchestrator runs the `psql`/`curl` audit block, and the operator performs
the live browse on essentials.empowered.vote. No files are modified. Per
`feedback_no_playwright_on_user_live_browser`, the live browse is read-only + operator-driven — never
wipe localStorage on the live page.
</execution_note>

<interfaces>
- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- Live app: https://essentials.empowered.vote ; per-county browse via the coverage chip
  (browse_label 'Pima County', browseGovernmentList ['04019']).
- ext_ids -4007001..-4007005 (Scott/Heinz/Allen/Christy/Cano).
- Section-split guard: no office reachable from the 5 X0019 districts under any government other than
  Pima County; the 04019 whole-county COUNTY row is a 3-way collision and must NOT carry a supervisor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full production audit block (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB/CDN assertions; no files modified</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md (the full Per-Task Verification Map — all 10 checks)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Common Pitfalls 2 & 6 — the 04019 collision + 36-not-44 topic scope so the audit does not misflag)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). Run the full audit via `psql "$DATABASE_URL" -tAc`
    + `curl -sI`, recording each result in 193-06-SUMMARY.md:
    (a) 5 geofences mtfcc='X0019' state='az', geo_id pima-az-supervisor-district-1..5, all ST_IsValid;
    (b) exactly 1 standalone government 'Pima County, Arizona, US' (geo_id 04019, type County), NOT
    parented to State of Arizona;
    (c) 5 offices under 'Board of Supervisors', each LOCAL X0019 district holding exactly 1 office
    (GROUP BY HAVING count<>1 → 0);
    (d) is_appointed=true count among the 5 = 1 (Cano);
    (e) 5/5 politician_images rows; `curl -sI` each of the 5 CDN URLs → HTTP 200; PIL-verify a sample
    is 600×750;
    (f) stances: 0 uncited (every answer has a context row with non-empty sources), 0 judicial-*
    rows, all values in [1.0,5.0] (do NOT assert a fixed per-supervisor count — honest blanks expected);
    (g) section-split: 0 offices reachable from the 5 X0019 districts under any non-Pima government;
    (h) coverage.js 'Pima County' chip present with hasContext:true; buildingImages.js 'pima county'
    entry present; banner `curl -sI cities/pima-county.jpg` → HTTP 200.
    ANY red assertion blocks phase verification — surface it for gap closure, do NOT hand-wave.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0019' AND state='az')=5 AND (SELECT count(*) FROM essentials.governments WHERE name='Pima County, Arizona, US' AND geo_id='04019')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0019' AND d.district_type='LOCAL' AND d.state='az')=5 AND (SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4007005 AND -4007001)=5 AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN inform.compass_topics t ON t.id=pa.topic_id WHERE p.external_id BETWEEN -4007005 AND -4007001 AND t.topic_key LIKE 'judicial-%') AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id BETWEEN -4007005 AND -4007001 AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id AND array_length(pc.sources,1) IS NOT NULL))"</automated>
  </verify>
  <acceptance_criteria>
    - Combined boolean SELECT returns `t` (5 geofences + 1 standalone gov + 5 offices + 5 headshots + 0 judicial stance rows + 0 uncited stance rows).
    - Each X0019 district holds exactly 1 office (no HAVING count<>1 violation).
    - Cano appointed count = 1; section-split query returns 0 rows.
    - `curl -sI` on the 5 headshot CDN URLs and the cities/pima-county.jpg banner all return HTTP 200; a sampled headshot is 600×750.
    - coverage.js 'Pima County' hasContext:true chip + buildingImages.js 'pima county' entry present.
    - All 10 VALIDATION.md Per-Task Map checks recorded green in 193-06-SUMMARY.md.
  </acceptance_criteria>
  <done>Full production audit is all-green across geofences, government/roster, headshots, stances, banner, and coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-browse verification — routing + correct-person photos + banner render</name>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md (Manual-Only Verifications table — routing, correct-person headshot, banner render)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Common Pitfall 7 — banner renders via the coverage chip, NOT an arbitrary Tucson address; do not treat that as a defect)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor/DB action beyond the operator's live browse. The operator confirms per-district routing, correct-person headshots, full compass, and banner render on essentials.empowered.vote (read-only browse; never wipe localStorage per feedback_no_playwright_on_user_live_browser). See &lt;how-to-verify&gt;.</action>
  <what-built>
    All 5 supervisors are seeded with offices on their own X0019 district geofences, 600×750 headshots,
    evidence-only stances, a Pima County banner, and a coverage chip. This checkpoint confirms a real
    resident's experience: address → their ONE supervisor, right person, right photo, compass populated,
    and the county banner rendering via the browse chip.
  </what-built>
  <how-to-verify>
    1. On https://essentials.empowered.vote, enter a known full street address in each of the 5
       supervisor districts (one per district) and confirm exactly ONE Pima County supervisor appears,
       labeled the correct district, with the correct current person (Scott D1 / Heinz D2 / Allen D3 /
       Christy D4 / Cano D5).
    2. Confirm each supervisor's headshot depicts the RIGHT current person (spot-check especially Cano
       D5, the Apr-2025 appointee) and their compass shows evidence-only stances (cited, honest blanks
       where no evidence).
    3. Browse Pima County via the coverage chip (browse_label 'Pima County') and confirm the
       Catalinas/Sonoran banner renders (Pitfall 7: it renders via the browse chip, not necessarily via
       a raw Tucson-area address — that is expected, not a defect).
    4. Report any wrong-district routing, wrong-person photo, missing compass, or banner failure as a
       gap for closure.
  </how-to-verify>
  <acceptance_criteria>
    - Each of the 5 sampled district addresses returns exactly 1 correctly-labeled supervisor (the right current person).
    - All 5 headshots depict the correct current person (Cano D5 spot-checked).
    - Each supervisor's compass shows evidence-only cited stances with honest blanks.
    - The Pima County banner renders via the coverage-chip browse.
    - Operator sign-off recorded in 193-06-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once per-district routing, correct-person headshots, populated compasses, and the banner render are all confirmed live — or list the gaps to close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production DB/CDN → audit | Read-only assertions observe the live end-state |
| live app → operator | The operator observes real routing/rendering behavior on essentials.empowered.vote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-FALSE | Repudiation | audit false-positive (authored-not-applied) | mitigate | Audit reads live production tables + CDN 200s, not files; any red assertion blocks the phase |
| T-193-WRONG | Spoofing | wrong-but-present photo / wrong-district routing | mitigate | Task 2 human live-browse spot-checks person + district (row-count cannot detect this) |
| T-193-MISFLAG | Tampering | audit misflag (44-topic / 04019 collision) | mitigate | Audit scopes to 36 non-judicial topics and X0019 LOCAL districts only (Pitfalls 2 & 6) so honest completeness is not misread as a gap |
| T-193-LSWIPE | Tampering | live-browser localStorage wipe | accept | Read-only operator browse only; no Playwright localStorage manipulation on the live page (feedback_no_playwright_on_user_live_browser) |
| T-193-SC | Tampering | package installs | accept | No installs — read-only psql/curl; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Full audit all-green (5 geofences / standalone gov / 5 offices 1-per-district / Cano appointed / 5-5 headshots / cited non-judicial stances / clean section-split / DB-honest chip / banner 200).
- Live per-district routing → 1 correct supervisor each; correct-person headshots; populated compasses; banner renders via the browse chip.
- Operator sign-off recorded.
</verification>

<success_criteria>
All 5 ROADMAP success criteria are TRUE end-to-end in production and operator-confirmed live —
PIMA-01 + BANR-01 satisfied, phase ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-06-SUMMARY.md` when done —
the phase verification record: the full audit results (all 10 checks) + the live-browse operator
sign-off.
</output>
