---
phase: 79-or-landing-elections-discovery
plan: 2
type: execute
wave: 2
depends_on: [79-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/238_or_statewide_races.sql
autonomous: true
requirements: []
tags: [oregon, elections, races, statewide, migration]

must_haves:
  truths:
    - "essentials.races contains exactly 8 OR statewide race rows for the November 3, 2026 general election"
    - "Each of the 8 statewide rows has a non-null office_id linked to the correct OR office (Governor, US Senate Merkley, US House CD-01 through CD-06)"
    - "All race_candidates for these 8 rows are empty (discovery agent will populate via cron)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/238_or_statewide_races.sql"
      provides: "8 OR statewide race rows: Governor + US Senate + 6 US House CDs, all general election only"
      contains: "Governor of Oregon"
  key_links:
    - from: "migration 238 race rows"
      to: "essentials.elections OR 2026 General UUID"
      via: "WITH gen_elec AS (SELECT id FROM essentials.elections WHERE name='OR 2026 General' AND state='OR')"
      pattern: "OR 2026 General"
    - from: "migration 238 race.office_id"
      to: "essentials.offices (Governor, Merkley, 6 CD offices)"
      via: "VALUES list of verified office_ids from Phase 74"
      pattern: "780f76cd-2ec0-42fc-bb67-74a8911ca1c8|3db3e08a-ed6c-4365-9e5a-9af1f94c4372"
---

<objective>
Create the 8 OR statewide race rows for the November 3, 2026 general election: Governor (Kotek, incumbent), US Senate (Merkley, incumbent), and US House CD-01 through CD-06.

Purpose: Implements D-04 (statewide races for Governor + US Senate + 6 US House CDs, general only) and D-05 (Merkley Senate race included; Wyden NOT included per RESEARCH.md "Ron Wyden is NOT up for reelection in 2026"). Race rows have empty race_candidates per D-11; the discovery cron (Plan 79-05) will populate them.

Output: One new migration 238_or_statewide_races.sql in C:/EV-Accounts/backend/migrations/, applied to live Supabase production DB. 8 rows in essentials.races linked to the OR 2026 General election.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-or-landing-elections-discovery/79-CONTEXT.md
@.planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md
@.planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md
@.planning/phases/79-or-landing-elections-discovery/79-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 238_or_statewide_races.sql (D-04, D-05)</name>
  <files>C:/EV-Accounts/backend/migrations/238_or_statewide_races.sql</files>
  <read_first>
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (sections "Race Row Patterns", "Position Name Convention", "ON CONFLICT Constraint Name", "OR Race Row Office Lookup Patterns", "US House CD-NN office_ids table")
    - .planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md (section "migrations/238_or_statewide_races.sql")
    - .planning/phases/79-or-landing-elections-discovery/79-01-SUMMARY.md (to get the OR 2026 General election UUID confirmed by Plan 01)
  </read_first>
  <action>
    Create a new file at exactly the path `C:/EV-Accounts/backend/migrations/238_or_statewide_races.sql`. The file must be written in UTF-8 WITHOUT a BOM (use the Write tool). Migration number 238 is next-available after 237. The file must contain a single WITH-CTE INSERT that produces exactly 8 race rows in essentials.races. Use the WITH-CTE pattern from RESEARCH.md "OR Race Row Office Lookup Patterns" and 79-PATTERNS.md (Phase 69-03-PLAN.md analog): top-level `WITH gen_elec AS (SELECT id FROM essentials.elections WHERE name = 'OR 2026 General' AND state = 'OR')`, then `INSERT INTO essentials.races (id, election_id, office_id, position_name, primary_party, seats) SELECT gen_random_uuid(), gen_elec.id, t.office_id_val::uuid, t.position_name_val, NULL, 1 FROM gen_elec, (VALUES ...) AS t(office_id_val, position_name_val) ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING;`. The VALUES list must contain exactly these 8 tuples in this order (office_id, position_name) per RESEARCH.md verified office_ids: ('780f76cd-2ec0-42fc-bb67-74a8911ca1c8', 'Governor of Oregon'); ('3db3e08a-ed6c-4365-9e5a-9af1f94c4372', 'U.S. Senate Oregon'); ('617febb8-3b45-4787-87af-8b8ecc008b05', 'U.S. House OR-01'); ('41b9876c-304d-4268-a751-25ea7e2009cc', 'U.S. House OR-02'); ('62cb1965-8401-430c-8681-03a3e22e7c77', 'U.S. House OR-03'); ('94d89181-58c5-42b3-886f-4538131fd461', 'U.S. House OR-04'); ('1207f28b-6eea-4113-889c-3127292e29b9', 'U.S. House OR-05'); ('1e17d814-d999-4399-974c-3b36ec825ba7', 'U.S. House OR-06'). All 8 rows have `primary_party = NULL` (per D-04 — antipartisan design + combined model) and `seats = 1`. Add a comment header: `-- Migration 238: OR 2026 Statewide Races — Phase 79 Plan 02` and one comment explaining D-05 (Merkley included; Wyden NOT included — term ends 2027). Do not include AG/SoS/Treasurer/Labor Commissioner rows (RESEARCH.md confirms these are not on the 2026 OR ballot). Do not insert any race_candidates rows (D-11). Do not include a `cron_active` reference.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='C:/EV-Accounts/backend/migrations/238_or_statewide_races.sql';const b=fs.readFileSync(p);if(b[0]===0xEF&&b[1]===0xBB&&b[2]===0xBF){console.error('FAIL: BOM');process.exit(1);}const s=b.toString('utf8');const must=['Governor of Oregon','U.S. Senate Oregon','U.S. House OR-01','U.S. House OR-02','U.S. House OR-03','U.S. House OR-04','U.S. House OR-05','U.S. House OR-06','780f76cd-2ec0-42fc-bb67-74a8911ca1c8','3db3e08a-ed6c-4365-9e5a-9af1f94c4372','617febb8-3b45-4787-87af-8b8ecc008b05','41b9876c-304d-4268-a751-25ea7e2009cc','62cb1965-8401-430c-8681-03a3e22e7c77','94d89181-58c5-42b3-886f-4538131fd461','1207f28b-6eea-4113-889c-3127292e29b9','1e17d814-d999-4399-974c-3b36ec825ba7','OR 2026 General','ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING'];for(const t of must){if(!s.includes(t))throw new Error('missing: '+t);}const forbid=['cron_active','election_name','race_candidates','U.S. House OR-07','Attorney General of Oregon'];for(const t of forbid){if(s.includes(t))throw new Error('forbidden token present: '+t);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/238_or_statewide_races.sql`
    - File is UTF-8 encoded without a BOM (first 3 bytes are NOT 0xEF 0xBB 0xBF)
    - File contains all 8 position_name string literals: `'Governor of Oregon'`, `'U.S. Senate Oregon'`, `'U.S. House OR-01'`, `'U.S. House OR-02'`, `'U.S. House OR-03'`, `'U.S. House OR-04'`, `'U.S. House OR-05'`, `'U.S. House OR-06'`
    - File contains all 8 verified office_id UUIDs exactly as listed in RESEARCH.md
    - File contains the literal string `'OR 2026 General'` and column name `name` (NOT `election_name`)
    - File contains `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`
    - File does NOT contain `cron_active`
    - File does NOT contain `essentials.race_candidates` (D-11: no candidate rows)
    - File does NOT contain `'U.S. House OR-07'` (Oregon has only 6 CDs)
    - File does NOT contain `'Attorney General of Oregon'` (RESEARCH.md: AG not on 2026 ballot)
    - File does NOT contain `Wyden` (D-05: Wyden NOT up in 2026)
  </acceptance_criteria>
  <done>Migration 238 file exists, UTF-8 NoBOM, contains exactly 8 statewide race INSERTs with correct office_ids, position_names, ON CONFLICT, and no out-of-scope rows.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 238 to Supabase and verify (D-04, D-05)</name>
  <files>C:/EV-Accounts/backend/migrations/238_or_statewide_races.sql (apply only)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/238_or_statewide_races.sql (the file written in Task 1)
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (section "Validation Architecture — After Plan 02")
  </read_first>
  <action>
    Apply migration 238 to the live Supabase production DB via mcp__supabase-local__execute_sql in a single execute_sql call. After applying, run these verifications:
    1. Count check: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR';` — expected 8.
    2. Per-row spot check by position_name: `SELECT position_name, office_id, primary_party, seats FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' ORDER BY position_name;` — expected 8 rows with position_names matching the 8 specified in Task 1, all with `primary_party IS NULL`, all with `seats=1`, all with non-null office_id.
    3. office_id-to-office join check: `SELECT r.position_name, c.name AS chamber FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' ORDER BY r.position_name;` — Governor row should join to chamber 'Governor', Senate row to 'United States Senate' (or whatever NATIONAL_UPPER chamber name is established), 6 House rows to 'United States House of Representatives' or equivalent.
    4. Idempotency: re-execute the migration 238 SQL — count must still be 8.
    5. Section-split check: `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.state='41' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts WHERE state IN ('or','OR','41'));` — expected 0.
    6. race_candidates emptiness check: `SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR';` — expected 0 (D-11).
  </action>
  <verify>
    <automated>echo "Manual MCP verification — see acceptance_criteria for explicit SELECT queries executed via mcp__supabase-local__execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR'` returns exactly 8
    - The 8 position_name values returned by `SELECT position_name FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' ORDER BY position_name` match this exact set: Governor of Oregon, U.S. House OR-01, U.S. House OR-02, U.S. House OR-03, U.S. House OR-04, U.S. House OR-05, U.S. House OR-06, U.S. Senate Oregon
    - All 8 rows have `office_id IS NOT NULL` (verified by `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.office_id IS NULL` returning 0)
    - All 8 rows have `primary_party IS NULL` and `seats=1`
    - The Governor race office_id resolves through `essentials.offices.chamber_id` to a chamber named 'Governor' under the State of Oregon government
    - The US Senate race office_id is exactly `3db3e08a-ed6c-4365-9e5a-9af1f94c4372` (Merkley's office)
    - Re-running migration 238 a second time returns no error and the count remains 8 (idempotent)
    - Section-split check returns 0 orphan boundaries
    - `SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR'` returns 0 (D-11)
  </acceptance_criteria>
  <done>Migration 238 applied; exactly 8 OR statewide race rows present with correct office_id links; idempotency confirmed; section-split passes; no race_candidates yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → live Supabase production DB | Migration 238 SQL crosses this boundary via mcp__supabase-local__execute_sql |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | migration 238 SQL | accept | Static INSERT with hardcoded office_id UUIDs verified against live DB; ON CONFLICT prevents duplicates |
| T-79-02-02 | Information Disclosure | race rows | accept | No PII; race data is public record |
| T-79-02-03 | Integrity | office_id linkage | mitigate | All 8 office_ids verified in RESEARCH.md against live DB on 2026-05-30; per-row spot-check verification in Task 2 confirms office_id-to-chamber linkage |

Note: Pure data migration phase — no auth paths, no API endpoints, no user input. ASVS categories not applicable. All threats low and accepted/mitigated.
</threat_model>

<verification>
1. `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR'` returns 8
2. The 8 position_names match the expected set (alphabetical sort listed in acceptance_criteria)
3. All 8 office_ids non-null
4. Idempotency: re-apply leaves count at 8
5. Section-split check returns 0
6. race_candidates count for OR remains 0 (D-11)
</verification>

<success_criteria>
- essentials.races contains 8 OR statewide race rows for OR 2026 General
- All 8 rows have non-null office_id linked to the correct Phase 74 office UUIDs
- Plans 79-03 and 79-04 can build on the foundation (race count grows to 98 after Plan 03, then 105 after Plan 04)
</success_criteria>

<output>
Create `.planning/phases/79-or-landing-elections-discovery/79-02-SUMMARY.md` documenting:
- Total OR race count after migration 238 (expected 8)
- 8 position_names with their office_ids (table format)
- Confirmation of idempotency re-run
- Confirmation of section-split check (0 orphans)
- Migration 238 ledger entry confirmation
</output>
