---
phase: 201-riverside-county-board-of-supervisors-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-riverside-supervisor-boundaries.ts
autonomous: true
requirements: [CV-01]
must_haves:
  truths:
    - "5 Riverside County supervisor-district LOCAL geofences (mtfcc='X0021', state='ca') are loaded into essentials.geofence_boundaries, one per district 1-5"
    - "Each geofence geometry is valid WGS84 (SRID 4326) in the Riverside-County range (lon ~-118/-114, lat ~33/35), NOT a native state-plane projection"
    - "The 5 geofences gate (pre-flight >=5) the structural migration in Plan 02 — without them Plan 02 must refuse to apply"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-riverside-supervisor-boundaries.ts"
      provides: "ArcGIS f=geojson loader for the 5 supervisor districts, parameterized INSERT ON CONFLICT (geo_id, mtfcc) DO NOTHING"
      contains: "X0021"
  key_links:
    - from: "load-riverside-supervisor-boundaries.ts"
      to: "gis.countyofriverside.us OpenData/SupervisorialDistricts/MapServer/0"
      via: "f=geojson + outSR=4326 query, JSON.stringify(feature.geometry) pass-through"
      pattern: "outSR=4326"
    - from: "load-riverside-supervisor-boundaries.ts"
      to: "essentials.geofence_boundaries"
      via: "parameterized INSERT with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))"
      pattern: "ON CONFLICT \\(geo_id, mtfcc\\) DO NOTHING"
---

<objective>
Author and run the one-time ETL loader that sources the 5 official Riverside County Board of
Supervisors district boundaries from the county's ArcGIS OpenData service
(`OpenData/SupervisorialDistricts/MapServer/0`, queried with `f=geojson`) and inserts them as custom
LOCAL geofences (`mtfcc='X0021'`, `state='ca'`, geo_id `riverside-ca-supervisor-district-1..5`).
These 5 geofences are the gating dependency for true per-supervisor address routing — the structural
migration in Plan 02 refuses to apply until they exist.

Purpose: Satisfies the geofence half of ROADMAP success criterion #2 (5 supervisorial-district
X-geofences; a probe address in each routes to exactly one supervisor). CONTEXT.md recon confirms the
endpoint returns GeoJSON directly with `?f=geojson`, so this loader mirrors the LA County / WashCo
`f=geojson` fetch shape, NOT Pima's `f=json` ArcGIS-rings conversion.
Output: `C:/EV-Accounts/backend/scripts/load-riverside-supervisor-boundaries.ts`, committed to the
`C:/EV-Accounts` repo and run by the orchestrator so 5 X0021 rows exist in production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / GIS-network access. Task 1 (author the loader) is the only
executor file action. Task 2 is ORCHESTRATOR-RUN: the inline orchestrator runs `npx tsx` against
`C:/EV-Accounts/backend/.env` `DATABASE_URL` and asserts the result. All backend files live in the
SEPARATE `C:/EV-Accounts` repo (`master`→Render) — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (from CONTEXT.md recon + 201-PATTERNS.md — RESEARCH was intentionally skipped, so the
exact district-attribute field name is UNCONFIRMED and must be probed live):
- Endpoint (authoritative): `https://gis.countyofriverside.us/arcgis_mapping/rest/services/OpenData/SupervisorialDistricts/MapServer/0/query`
  with `where=1=1`, `outFields=*`, `returnGeometry=true`, `f=geojson`, `outSR=4326`. Returns a
  GeoJSON FeatureCollection DIRECTLY (no ArcGIS-rings conversion) — `outSR=4326` MANDATORY (native
  SRID is a state-plane projection). Hub mirror: gisopendata-countyofriverside.opendata.arcgis.com
  "Supervisor Districts". Expected feature count = 5, 2021 boundaries.
- Primary analog (copy shape verbatim): `C:/EV-Accounts/backend/scripts/load-la-county-supervisor-boundaries.ts`
  — same role (county Board of Supervisors district boundaries) AND same `f=geojson` fetch shape;
  uses `JSON.stringify(feature.geometry)` pass-through (NOT Pima's `arcgisRingsToGeoJson`). Prefer the
  `ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))` INSERT form over LA County's `ST_ForcePolygonCCW`
  variant (ForcePolygonCCW is LA-specific, not the convention elsewhere).
- Secondary analog: `C:/EV-Accounts/backend/scripts/load-washco-commissioner-boundaries.ts` — the
  fixed-feature-count pre-flight + fetch/log/dry-run scaffold (also `f=geojson`).
- Config constants: MTFCC `'X0021'` (next unused — max seen X0020/Tucson wards; DB-verify unused at
  execute time); STATE_CODE `'ca'` (lowercase — required for LOCAL routing); SOURCE
  `'gis.countyofriverside.us-supervisorialdistricts-mapserver0-2026'`; GEO_ID_PREFIX
  `'riverside-ca-supervisor-district-'`; EXPECTED_COUNT 5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Riverside supervisor-district geofence loader</name>
  <files>C:/EV-Accounts/backend/scripts/load-riverside-supervisor-boundaries.ts</files>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (lines 30-85 — the full loader pattern: config block, f=geojson fetch shape, district-number fallback chain, the verbatim parameterized INSERT + ST_MakeValid guard, the EXPECTED_COUNT PAUSE-on-shortfall rule)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (§ code_context — District geofence source endpoint, feature count = 5, X-geofence loader convention; § decisions D-04 X-geofence)
    - C:/EV-Accounts/backend/scripts/load-la-county-supervisor-boundaries.ts (PRIMARY analog — read in full: the f=geojson fetch, JSON.stringify(feature.geometry) pass-through, the districtMap keyed by an integer district field with a fallback chain, the parameterized INSERT — copy this shape, but use ST_Multi(ST_SetSRID(...)) not ST_ForcePolygonCCW)
    - C:/EV-Accounts/backend/scripts/load-washco-commissioner-boundaries.ts (SECONDARY — read for the small-fixed-feature-count pre-flight + shortfall PAUSE style)
  </read_first>
  <action>
    Copy `load-la-county-supervisor-boundaries.ts` and adapt. Set the query URL to the Riverside
    endpoint `https://gis.countyofriverside.us/arcgis_mapping/rest/services/OpenData/SupervisorialDistricts/MapServer/0/query`
    with `where=1=1`, `outFields=*`, `returnGeometry=true`, `f=geojson`, and `outSR=4326` (outSR is
    MANDATORY — without it coordinates return in the native state-plane SRID and store as garbage that
    never matches an address). Keep `f=geojson` — do NOT use `f=json` and do NOT reuse Pima's
    `arcgisRingsToGeoJson` ring-winding helper (Riverside returns GeoJSON directly). If a live probe at
    execute time shows the endpoint actually needs `f=json`, that is the ONLY case to fall back to
    Pima's rings helper — note the deviation in the SUMMARY.

    Set constants: MTFCC='X0021', STATE_CODE='ca' (lowercase), SOURCE=
    'gis.countyofriverside.us-supervisorialdistricts-mapserver0-2026', GEO_ID_PREFIX=
    'riverside-ca-supervisor-district-', EXPECTED_COUNT=5.

    District-number extraction: the exact attribute name is UNCONFIRMED (RESEARCH skipped). Keep a
    defensive fallback chain mirroring LA County (try `DISTRICT`, then `SUPERVISORIAL_DISTRICT` /
    `SUP_DIST` / `DISTRICT_NUM` / `DIST` — whatever integer 1-5 field the live response carries).
    Reject any out-of-range (not 1-5) district number BEFORE forming geo_id. Build geo_id per feature
    as `${GEO_ID_PREFIX}${districtNumber}` and name as a human-readable
    `Riverside County Supervisor District ${districtNumber}` (or the feature's own name field if
    present).

    Reuse the parameterized INSERT into essentials.geofence_boundaries (columns id, geo_id, mtfcc,
    state, name, geometry, source) with `public.ST_Multi(public.ST_SetSRID(public.ST_GeomFromGeoJSON($3),
    4326))` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING RETURNING public.ST_GeometryType(geometry),
    public.ST_IsValid(geometry)`. All GIS attribute/geometry values MUST be passed as bind params
    ($1 geo_id / $2 name / $3 geomJson via JSON.stringify(feature.geometry) / $4 source) — never
    string-concatenated into SQL (T-201-SQLI). Keep a conditional `ST_MakeValid` re-run guard as
    defensive insurance for any invalid/self-intersecting multipolygon.

    Assert EXPECTED_COUNT: if the fetch returns fewer than 5 valid districts, print the shortfall and
    exit non-zero (PAUSE+flag — never silently load a partial set). Print a summary line per district
    (district number, geo_id, geometry type, ST_IsValid, first-coordinate sanity value) so the
    orchestrator can confirm the Riverside-County WGS84 range in Task 2.

    Do NOT touch essentials.districts or any government/office table — this loader ONLY populates
    geofence_boundaries. The pre-existing whole-county COUNTY boundary (geo_id='06065', mtfcc 'G4020',
    state '06') is untouched.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/load-riverside-supervisor-boundaries.ts"; test -f "$F" && grep -qF "outSR=4326" "$F" && grep -qF "f=geojson" "$F" && grep -qF "X0021" "$F" && grep -qF "ON CONFLICT (geo_id, mtfcc) DO NOTHING" "$F" && grep -qF "riverside-ca-supervisor-district-" "$F" && ! grep -qE "arcgisRingsToGeoJson|X0020|X0019" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/load-riverside-supervisor-boundaries.ts` exists.
    - Grep confirms `outSR=4326` and `f=geojson` present in the query URL; NO `arcgisRingsToGeoJson`, NO `f=json`.
    - Grep confirms `X0021` present; NEITHER `X0020` NOR `X0019` appears anywhere.
    - Grep confirms lowercase `'ca'` STATE_CODE (no uppercase `'CA'` used as the geofence state value).
    - Grep confirms the parameterized INSERT with `ST_GeomFromGeoJSON($3)` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING` — no string concatenation of GIS data into SQL.
    - geo_id prefix `riverside-ca-supervisor-district-` present; district-number fallback chain present (DISTRICT + alternates); out-of-range (not 1-5) rejected before geo_id.
    - Conditional `ST_MakeValid` guard present; EXPECTED_COUNT=5 with a non-zero exit on shortfall.
  </acceptance_criteria>
  <done>Loader authored: fetches 5 districts via f=geojson+outSR=4326 (GeoJSON pass-through, no rings conversion), inserts 5 X0021/lowercase-ca geofences idempotently with parameterized binds, pauses on shortfall.</done>
</task>

<task type="auto">
  <name>Task 2: Run the loader + assert 5 X0021 geofences (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator runs the loader + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (§ code_context — Integration Points: each district PIP-routes to exactly one supervisor; DB verification steps run inline)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (line 51 — X0021 DB-verify-unused note; line 55 — EXPECTED_COUNT=5)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/network access). First DB-verify X0021 is genuinely unused
    (`SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0021'` = 0 before load); if it
    is claimed, assign the next free X-code and note the deviation. Then `git -C "C:/EV-Accounts" add`
    + commit the loader. Run `npx tsx backend/scripts/load-riverside-supervisor-boundaries.ts` from the
    `C:/EV-Accounts/backend` checkout (uses `.env` `DATABASE_URL`). Confirm the script exits 0 with 5
    rows inserted (or already-present via ON CONFLICT). Then assert via `psql "$DATABASE_URL" -tAc`:
    (a) exactly 5 rows in essentials.geofence_boundaries WHERE mtfcc='X0021' AND state='ca';
    (b) all 5 geo_ids match `riverside-ca-supervisor-district-1..5`; (c) all 5 geometries valid
    (ST_IsValid=true) and their centroid longitude is between -118 and -114 and latitude between 33 and
    35 (Riverside-County WGS84 sanity, confirming outSR=4326 worked). If any assertion fails, PAUSE and
    flag — do NOT proceed to Plan 02. Record the 5 geo_id → ST_IsValid → centroid results in
    201-01-SUMMARY.md.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0021' AND state='ca')=5 AND NOT EXISTS (SELECT 1 FROM essentials.geofence_boundaries WHERE mtfcc='X0021' AND NOT public.ST_IsValid(geometry)) AND (SELECT bool_and(public.ST_X(public.ST_Centroid(geometry)) BETWEEN -118 AND -114 AND public.ST_Y(public.ST_Centroid(geometry)) BETWEEN 33 AND 35) FROM essentials.geofence_boundaries WHERE mtfcc='X0021')"</automated>
  </verify>
  <acceptance_criteria>
    - X0021 confirmed unused before load; loader exits 0; 5 X0021 rows present after the run.
    - `psql -tAc` count of X0021/state='ca' geofences = 5.
    - All 5 geo_ids are exactly `riverside-ca-supervisor-district-1` through `-5`.
    - All 5 geometries valid; every centroid lon∈(-118,-114) and lat∈(33,35) (WGS84, no state-plane garbage).
    - The combined boolean SELECT returns `t`.
    - Loader committed to `C:/EV-Accounts` via `git -C`.
  </acceptance_criteria>
  <done>5 valid WGS84 supervisor-district geofences (X0021, lowercase ca) are live in production, gating Plan 02's structural migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Riverside ArcGIS OpenData (remote) → loader | Untrusted GeoJSON geometry + attribute strings enter the loader over HTTP |
| loader → production PostGIS | Converted geometry crosses into live `essentials.geofence_boundaries` via `npx tsx` (service-role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-SQLI | Tampering | GIS attribute/geometry → INSERT | mitigate | Parameterized binds ($1 geo_id / $2 name / $3 GeoJSON / $4 source) — never string-concatenate GIS response data into SQL (matches LA County/WashCo loaders) |
| T-201-PROJ | Tampering | native state-plane SRID vs WGS84 | mitigate | `outSR=4326` mandatory in the query; Task 2 asserts centroid ∈ Riverside-County WGS84 range or PAUSES |
| T-201-COUNT | Tampering | untrusted feature count / partial load | mitigate | EXPECTED_COUNT=5 gate; non-zero exit + PAUSE on shortfall — never silently load a partial set |
| T-201-GEOM | Tampering | invalid/self-intersecting polygon | mitigate | `RETURNING ST_IsValid` + conditional `ST_MakeValid` re-run guard |
| T-201-MTFCC | Tampering | wrong X-code silently un-routable | mitigate | Use X0021 (next free); grep gate forbids X0020/X0019; Task 2 DB-verifies X0021 unused before load |
| T-201-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `C:/EV-Accounts/backend/.env`; never hardcoded in the loader |
| T-201-SC | Tampering | package installs | accept | No new npm installs — reuses installed `pg`/`tsx`/`dotenv`; Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 5 X0021/state='ca' geofences loaded, geo_id `riverside-ca-supervisor-district-1..5`.
- All valid WGS84 geometry (centroid in the -118/-114 lon, 33/35 lat Riverside-County box).
- Loader committed to `C:/EV-Accounts`; results recorded in 201-01-SUMMARY.md.
</verification>

<success_criteria>
The geofence half of ROADMAP #2 is TRUE in production: 5 clean per-district LOCAL geofences exist,
enabling true one-supervisor-per-address routing and gating Plan 02.
</success_criteria>

<output>
Create `.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-01-SUMMARY.md` when
done — include the 5 geo_id → ST_IsValid → centroid manifest and note the confirmed district-attribute
field name for downstream reference.
</output>
</content>
</invoke>
