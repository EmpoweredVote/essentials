---
phase: 189-smart-banner-integration-graceful-degradation
plan: 02
type: execute
wave: 2
depends_on: ["189-01"]
files_modified:
  - src/pages/Results.jsx
  - src/components/ElectionsView.jsx
autonomous: true
requirements: [SBAN-01, SBAN-02]
tags: [banner, refactor, frontend]

must_haves:
  truths:
    - "Both Results.jsx and ElectionsView.jsx render all three banner tiers via <SectionBanner {...buildBannerProps(tier, bannerCtx)} /> one-liners — no hand-assembled imageUrl/featureIcons/stats/locationName at any call site"
    - "All three tiers (city, state, federal) still show the enhanced treatment (stat + icon row where data exists) on both pages"
    - "The last page-specific divergence (STATE_NAMES vs stateNames locationName wording) is gone — both pages feed the same ctx into the same function"
  artifacts:
    - path: "src/pages/Results.jsx"
      provides: "bannerCtx useMemo + 3 buildBannerProps call sites (SBAN-01)"
      contains: "buildBannerProps"
    - path: "src/components/ElectionsView.jsx"
      provides: "bannerCtx useMemo + 3 buildBannerProps call sites (SBAN-02)"
      contains: "buildBannerProps"
  key_links:
    - from: "src/pages/Results.jsx"
      to: "src/lib/bannerProps.js"
      via: "import { buildBannerProps }"
      pattern: "buildBannerProps\\('(city|state|federal)', bannerCtx\\)"
    - from: "src/components/ElectionsView.jsx"
      to: "src/lib/bannerProps.js"
      via: "import { buildBannerProps }"
      pattern: "buildBannerProps\\('(city|state|federal)', bannerCtx\\)"
---

<objective>
Replace the six hand-assembled `<SectionBanner .../>` call sites (3 in Results.jsx, 3 in ElectionsView.jsx)
with uniform `<SectionBanner {...buildBannerProps(tier, bannerCtx)} />` one-liners, each page building one
referentially-stable `bannerCtx` via `useMemo`.

Purpose: SBAN-01 (Results banners keep icon row + stats across all 3 tiers) and SBAN-02 (Elections banners
identical, zero page-specific divergence). After this plan, prop-assembly logic exists in exactly one place
(`buildBannerProps`, Wave 1) and both pages are literal drop-ins.
Output: edited Results.jsx + ElectionsView.jsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-smart-banner-integration-graceful-degradation/189-RESEARCH.md
@.planning/phases/189-smart-banner-integration-graceful-degradation/189-01-SUMMARY.md
@src/lib/bannerProps.js

<interfaces>
<!-- Wave 1 produced this pure helper — call it, do not re-derive props: -->
import { buildBannerProps } from '../lib/bannerProps';   // Results.jsx path
import { buildBannerProps } from '../lib/bannerProps';   // ElectionsView.jsx path (both under src/, adjust relative depth)
buildBannerProps(tier, ctx) -> { tier, locationName, imageUrl, featureIcons, stats }

<!-- ctx fields already exist verbatim at each call site: -->
Results.jsx        : representingCity, userState, STATE_NAMES (const, line 93), buildingImageMap, featureIconMap, populationMap  (+ useMemo already imported, line 1)
ElectionsView.jsx  : representingCity, userState, stateNames, buildingImageMap, featureIconMap, populationMap  (all destructured props, lines 285-290; useMemo already imported, line 1)

<!-- EXACT current call sites to replace (verified 2026-07-08): -->
Results.jsx        : lines 1989-1995 (city), 1997-2003 (state), 2005-2011 (federal)
ElectionsView.jsx  : lines 579-585 (city), 587-593 (state), 595-601 (federal)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Results.jsx through buildBannerProps</name>
  <files>src/pages/Results.jsx</files>
  <read_first>
    - src/pages/Results.jsx (import block line 1; STATE_NAMES const ~line 93; populationMap/featureIconMap/buildingImageMap useMemos ~1155-1215; the 3 call sites 1989-2011)
    - src/lib/bannerProps.js (the helper contract from Wave 1)
    - .planning/phases/189-smart-banner-integration-graceful-degradation/189-RESEARCH.md (§"Call sites" Results.jsx block; Pitfall 1 capitalization note)
  </read_first>
  <action>
    Import `buildBannerProps` from `../lib/bannerProps`. Add a `bannerCtx` `useMemo` that returns
    `{ representingCity, userState, stateNames: STATE_NAMES, buildingImageMap, featureIconMap, populationMap }`
    with dependency array `[representingCity, userState, buildingImageMap, featureIconMap, populationMap]`
    (STATE_NAMES is a module constant — not a dep). Place it after the three existing map useMemos so those
    maps are in scope. Replace the three `? <SectionBanner .../> : tier === ... ` branches (lines 1989-2011)
    with `<SectionBanner {...buildBannerProps('city', bannerCtx)} />`, `...('state', bannerCtx)`,
    `...('federal', bannerCtx)` respectively — keep the surrounding `filteredHierarchy.map` /
    `tier === 'Local' ? ... : tier === 'State' ? ... : tier === 'Federal' ? ... : null` ternary shape,
    only the SectionBanner JSX inside each branch changes. Do NOT hand-assemble imageUrl/featureIcons/stats/locationName
    anymore (Anti-Pattern: no per-call-site locationName). Do not alter the maps' own resolution logic (D-03 defer).
  </action>
  <verify>
    <automated>npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Results.jsx imports `buildBannerProps` from `../lib/bannerProps`
    - Results.jsx contains a `bannerCtx` useMemo whose object literal includes all 6 keys (representingCity, userState, stateNames, buildingImageMap, featureIconMap, populationMap)
    - The three tier branches render `<SectionBanner {...buildBannerProps('city'|'state'|'federal', bannerCtx)} />`
    - grep shows no remaining `locationName={` or hand-passed `stats={populationMap.` inside Results.jsx's SectionBanner call sites
    - `npm run lint` exits 0 (no undefined/unused-var errors) and `npm test` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Results.jsx renders all three tiers via buildBannerProps one-liners; no inline prop assembly remains; lint + full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ElectionsView.jsx through buildBannerProps</name>
  <files>src/components/ElectionsView.jsx</files>
  <read_first>
    - src/components/ElectionsView.jsx (import block line 1; prop destructure lines 285-290; the 3 call sites 579-601)
    - src/lib/bannerProps.js (the helper contract from Wave 1)
    - .planning/phases/189-smart-banner-integration-graceful-degradation/189-RESEARCH.md (§"Call sites" ElectionsView.jsx block; SBAN-02 divergence note — stateNames vs STATE_NAMES)
  </read_first>
  <action>
    Import `buildBannerProps` from `./SectionBanner`'s sibling path (`../lib/bannerProps` — ElectionsView is under
    src/components, so `../lib/bannerProps`). Add a `bannerCtx` `useMemo` returning
    `{ representingCity, userState, stateNames, buildingImageMap, featureIconMap, populationMap }` (these are already
    ElectionsView's own destructured prop names — a literal drop-in, no renaming) with dependency array
    `[representingCity, userState, stateNames, buildingImageMap, featureIconMap, populationMap]`, placed inside the
    component body before the `processedElections.map` render. Replace the three `? <SectionBanner .../>` branches
    (lines 579-601) with `<SectionBanner {...buildBannerProps('city', bannerCtx)} />` / `...('state', bannerCtx)` /
    `...('federal', bannerCtx)`, keeping the `tier === 'Local' ? ... : ... : null` ternary shape. Remove the inline
    `locationName={... stateNames?.[userState] ...}` assembly — locationName now comes from the helper, closing the
    last page-specific divergence (SBAN-02). Keep the optional-chaining defaults behavior intact via the helper's own
    `?? null`/`?? []` (Pitfall 2). ElectionsView remains a pure pass-through — no router/browse_geo_id access added.
  </action>
  <verify>
    <automated>npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - ElectionsView.jsx imports `buildBannerProps` from `../lib/bannerProps`
    - ElectionsView.jsx contains a `bannerCtx` useMemo whose object literal includes all 6 keys (using `stateNames`, not `STATE_NAMES`)
    - The three tier branches render `<SectionBanner {...buildBannerProps('city'|'state'|'federal', bannerCtx)} />`
    - grep shows no remaining `locationName={` inside ElectionsView.jsx's SectionBanner call sites
    - `npm run lint` exits 0 and `npm test` exits 0
  </acceptance_criteria>
  <done>ElectionsView.jsx renders all three tiers via the identical buildBannerProps call pattern as Results.jsx; the STATE_NAMES/stateNames divergence is eliminated; lint + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page state -> SectionBanner | Both pages pass already-resolved, first-party maps into a pure helper; no new untrusted input, no network call added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-03 | Tampering | Results/ElectionsView call sites | accept | Refactor swaps inline prop assembly for a pure function call; no behavior/data-flow change, no user-controlled input. Lint + full suite guard against accidental regressions. |
| T-189-SC | Tampering | package installs | accept | Zero package installs this phase (RESEARCH §"Package Legitimacy Audit" — N/A). |
</threat_model>

<verification>
- `npm run lint` — 0 errors (catches undefined `buildBannerProps`/`bannerCtx` import/scope mistakes).
- `npm test` — full suite green (bannerProps + SectionBanner + all existing, no regression).
- Code inspection: exactly one prop-assembly path (`buildBannerProps`) feeds all 6 call sites; no inline `locationName=`/`stats=` on any SectionBanner.
</verification>

<success_criteria>
- Both pages render all three tiers via `buildBannerProps(tier, bannerCtx)` (SBAN-01, SBAN-02).
- No page-specific prop-assembly divergence remains (single source of truth confirmed by inspection).
- Lint clean, full Vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/189-smart-banner-integration-graceful-degradation/189-02-SUMMARY.md` when done.
</output>
