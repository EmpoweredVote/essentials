---
phase: 203-indio-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["203-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-indio-headshots.py
  - C:/EV-Accounts/backend/migrations/1339_indio_headshots.sql  # 1339 illustrative — verify actual next migration number (DB-verified disk MAX+1) at execute time
autonomous: false
requirements: [CV-03]
must_haves:
  truths:
    - "All 5 councilmembers have a 600×750 (4:5 crop-FIRST, Lanczos q90) headshot uploaded to the politician_photos Storage bucket"
    - "Each of the 5 politician_images rows binds to the CURRENT roster member via runtime external_id → UUID resolution (never an invented UUID)"
    - "Primary source is indio.civicweb.net/portal/members.aspx?id=10 (D-10); gaps filled from indio.org CivicPlus StaffDirectory component id 38 via Browser-UA/Playwright (D-11, indio.org is WAF-403 to bots); photo_license matches each image's actual source (never defaulted to us_government_work for a non-.gov source)"
    - "type='default' on every row (the UI filters on it)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-indio-headshots.py"
      provides: "per-member fetch → to_rgb_white_background → crop_to_4_5 → resize 600×750 Lanczos q90 → Storage x-upsert pipeline for 5 councilmembers (gitignored, orchestrator-run)"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1339_indio_headshots.sql"
      provides: "5 audit-only (unregistered) politician_images rows, UUIDs from Plan 02 manifest"
      contains: "politician_images"
  key_links:
    - from: "_tmp-indio-headshots.py"
      to: "indio.civicweb.net/portal/members.aspx?id=10 (+ indio.org CivicPlus StaffDirectory fallback)"
      via: "requests.get / Browser-UA-Playwright per-member hardcoded source URL, after per-URL HEAD/reachability probe"
      pattern: "resolve_politician_id"
    - from: "1339_indio_headshots.sql"
      to: "essentials.politicians"
      via: "SELECT id WHERE external_id = <member ext_id> subquery"
      pattern: "politician_images"
---

<objective>
Author the headshot ETL pipeline + the audit-only migration that gives all 5 Indio councilmembers a
600×750 (4:5 crop-first) headshot in Storage, each `politician_images` row bound to the current roster
member by runtime external_id→UUID resolution. Primary source is the city's CivicWeb member portal
(`indio.civicweb.net/portal/members.aspx?id=10`, D-10); gaps are filled from the indio.org CivicPlus
StaffDirectory (component id 38) via a Browser-UA/Playwright fetch, since indio.org returns WAF-403 to
bots (D-11).

Purpose: Satisfies the headshot half of ROADMAP success criterion #3 for CV-03.
Output: `_tmp-indio-headshots.py` (gitignored, orchestrator-run) + `1339_indio_headshots.sql`
(audit-only, unregistered — verify actual number at execute time).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-indio-deep-seed/203-CONTEXT.md
@.planning/phases/203-indio-deep-seed/203-PATTERNS.md
@.planning/phases/203-indio-deep-seed/203-02-SUMMARY.md

<execution_note>
`gsd-executor` has NO Storage/DB access and CANNOT run the Python pipeline. Task 1 (author the Python
script) and Task 3 (author the audit migration) are executor file actions. Task 2 is ORCHESTRATOR-RUN:
the orchestrator runs the pipeline (PIL/requests/psycopg2 + Storage + Playwright live only in its
shell), captures the emitted UUID→URL manifest, then the executor's audit migration is filled with
those UUIDs and the orchestrator applies it via `psql -f`. `_tmp-*` scripts are gitignored — never
committed.
</execution_note>

<interfaces>
Key contracts (from 203-PATTERNS.md headshot block + 203-02-SUMMARY.md manifest + CONTEXT.md D-10/11/12):
- PRIMARY analog (reuse functions VERBATIM): `C:/EV-Accounts/backend/scripts/_tmp-palmsprings-headshots.py`
  — reuse `to_rgb_white_background()` (RGBA→white composite, never naive convert('RGB')),
  `crop_to_4_5()` (crop FIRST, never stretch), `resize_600x750()` (Lanczos, optimize=True strips EXIF),
  `upload_to_storage()` (x-upsert PUT to politician_photos/{uuid}-headshot.jpg), `resolve_politician_id()`
  (parameterized external_id=%s), the dry-run HEAD-check-every-URL step, the env-load block, and the
  descriptive HEADERS User-Agent, and each member's explicit `crop_box`.
- What changes: the ROSTER list (5 entries — external_id from the Plan 02 manifest, per-member hardcoded
  photo_url after individual reachability probes), the guard assertions (assert len(ROSTER)==5, ext_id
  uniqueness, all ext_ids in -4012005..-4012001).
- Sources (D-10/D-11): PRIMARY = indio.civicweb.net/portal/members.aspx?id=10 (responded to bots in
  recon; carries member photos). GAPS = indio.org CivicPlus StaffDirectory component id 38 (e.g. Holmes =
  /Home/Components/StaffDirectory/StaffDirectory/38/181) fetched via Browser-UA/Playwright (indio.org is
  WAF-403 to plain bots). Probe every candidate URL for HTTP 200 individually before hardcoding.
- Audit migration analog: `C:/EV-Accounts/backend/migrations/1330_palm_springs_headshots.sql` — columns
  exactly (id, politician_id, url, type, photo_license); WHERE NOT EXISTS idempotency; type MUST be
  'default'; AUDIT-ONLY (NOT registered in schema_migrations).
- 5 members + ext_ids + UUIDs from 203-02-SUMMARY.md (D1 Miller -4012001 / D2 Fermon -4012002 / D3 Holmes
  -4012003 / D4 Ortiz -4012004 / D5 Guitron IV -4012005). photo_license = 'us_government_work' ONLY if the
  final source IS a .gov / city-hosted-CivicWeb page; otherwise 'press_use' — do NOT default it.
- Storage CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Indio headshot pipeline (Python, gitignored)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-indio-headshots.py</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 274-311 — the headshot-pipeline reuse spec: which functions to copy verbatim, what changes, the D-10/D-11 source order + license rule, the orchestration/gitignore note)
    - .planning/phases/203-indio-deep-seed/203-02-SUMMARY.md (the 5 politician ext_ids + geo_id→member mapping + confirmed D5 name for the ROSTER)
    - C:/EV-Accounts/backend/scripts/_tmp-palmsprings-headshots.py (PRIMARY analog — read in FULL: to_rgb_white_background / crop_to_4_5 / resize_600x750 / upload_to_storage / resolve_politician_id / HEAD dry-run / env-load / HEADERS / per-member crop_box — copy verbatim, only ROSTER + per-member source change)
  </read_first>
  <action>
    Copy `_tmp-palmsprings-headshots.py` verbatim and change only: (1) the ROSTER list — 5 entries, each
    with the member name, external_id (from 203-02-SUMMARY.md manifest: Miller -4012001, Fermon -4012002,
    Holmes -4012003, Ortiz -4012004, Guitron IV -4012005), district, a per-member hardcoded photo_url,
    photo_license, and a per-member `crop_box` set to the real image dimensions; (2) source_url_for(member)
    returns member['photo_url']; (3) guard assertions: assert len(ROSTER)==5, ext_id uniqueness, and all
    ext_ids within -4012005..-4012001. Keep `to_rgb_white_background()`, `crop_to_4_5()` (crop 4:5 FIRST,
    then resize — never stretch), `resize_600x750()` (Lanczos q90), `upload_to_storage()` (x-upsert PUT to
    politician_photos/{uuid}-headshot.jpg), `resolve_politician_id()` (parameterized external_id=%s), the
    HEAD dry-run-every-URL step, the env-load block, and the descriptive User-Agent HEADERS all verbatim.

    Per-member source: leave photo_url as a clearly-marked TODO placeholder per member with the D-10/D-11
    probe order to follow at execute time: (1) indio.civicweb.net/portal/members.aspx?id=10 member photo
    (PRIMARY); (2) for gaps, indio.org CivicPlus StaffDirectory component id 38 (e.g. Holmes =
    /Home/Components/StaffDirectory/StaffDirectory/38/181) fetched via Browser-UA/Playwright (indio.org is
    WAF-403 to plain bots — do NOT rely on a plain requests.get to indio.org). Set each member's
    photo_license to match the ACTUAL final source — 'us_government_work' ONLY if the final image is a
    .gov/city-hosted-CivicWeb page, otherwise 'press_use'; never blanket-default. The script must be
    gitignored (`backend/scripts/_*`) and never committed.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-indio-headshots.py"; test -f "$F" && grep -qF "crop_to_4_5" "$F" && grep -qF "resize_600x750" "$F" && grep -qF "to_rgb_white_background" "$F" && grep -qF "resolve_politician_id" "$F" && grep -qE "len\(ROSTER\)\s*==\s*5" "$F" && grep -qF "-4012003" "$F" && ! grep -qiE "palm-?springs|-4011" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File `_tmp-indio-headshots.py` exists and reuses crop_to_4_5 / resize_600x750 / to_rgb_white_background / upload_to_storage / resolve_politician_id / HEAD dry-run verbatim.
    - ROSTER has 5 entries with ext_ids matching the Plan 02 manifest (-4012001..-4012005); assert len(ROSTER)==5 present; no leftover palm-springs / -4011 tokens.
    - source_url_for returns member['photo_url']; per-member probe order documented (indio.civicweb.net primary → indio.org CivicPlus StaffDirectory id 38 via Browser-UA/Playwright).
    - photo_license per member set to the actual source (no blanket us_government_work default for non-.gov).
    - Script path matches the gitignored `_*` convention.
  </acceptance_criteria>
  <done>Headshot pipeline authored: reuses the Palm Springs crop-first-then-resize-600×750 functions verbatim; only ROSTER (5 members, manifest ext_ids) + per-member CivicWeb/StaffDirectory source change; gitignored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Probe sources + run pipeline + visual-QA (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator probes sources, runs the Python pipeline, uploads to Storage, emits UUID→URL manifest</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-CONTEXT.md (D-10 indio.civicweb.net primary; D-11 indio.org CivicPlus StaffDirectory id 38 via Browser-UA/Playwright; D-12 pipeline spec)
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 301-311 — the source probe order + license rule + orchestration note: executor writes, orchestrator runs)
  </read_first>
  <what-built>
    The `_tmp-indio-headshots.py` pipeline from Task 1 — reuses the verbatim crop-first→600×750 Lanczos
    functions; ROSTER = the 5 councilmembers with manifest ext_ids.
  </what-built>
  <how-to-verify>
    ORCHESTRATOR steps: (1) for EACH of the 5 members, probe sources in order (indio.civicweb.net/portal/members.aspx?id=10
    PRIMARY → indio.org CivicPlus StaffDirectory component id 38 via Browser-UA/Playwright for gaps),
    confirm a reachable ≥600px-ish image, and fill that member's photo_url + photo_license + crop_box in
    the script; (2) run `python backend/scripts/_tmp-indio-headshots.py` — HEAD dry-run first, then
    process (4:5 crop FIRST → 600×750 Lanczos q90 → x-upsert to politician_photos/{uuid}-headshot.jpg);
    (3) confirm all 5 upload with HTTP 200 and open each rendered image to visually confirm: correct
    person (identity match), 4:5 framing, eyes ~1/3 from top, head+shoulders, no distortion, no
    text/graphics on the face. Record the 5 UUID→URL→license→source manifest into 203-03-SUMMARY.md for
    Task 3. If any source is unreachable or any image is low-quality/wrong-person, PAUSE and re-source
    that one.
  </how-to-verify>
  <verify>
    <automated>MISSING — orchestrator-run visual-QA; no automated command (executor has no Storage/browser access). Verified via the 5 HTTP-200 upload confirmations + visual-QA artifact recorded in 203-03-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - All 5 members have a reachable per-member source (indio.civicweb.net primary; indio.org StaffDirectory via Browser-UA/Playwright for gaps — plain-bot indio.org WAF-403 avoided).
    - Pipeline runs; 5 images uploaded to politician_photos/{uuid}-headshot.jpg, each HTTP 200.
    - Visual QA per image: correct identity, 4:5, eyes ~1/3 from top, head+shoulders, no distortion, no graphics on face.
    - 5 UUID→URL→photo_license→source manifest recorded in 203-03-SUMMARY.md (license matches actual source per member).
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 5 headshots are uploaded (HTTP 200) and visually confirmed correct — or name the member(s) to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the audit-only headshots migration</name>
  <files>C:/EV-Accounts/backend/migrations/1339_indio_headshots.sql</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 315-336 — the audit-only headshots migration shape: exact columns, WHERE NOT EXISTS, type='default', NOT registered, UUIDs from the phase SUMMARY manifest never invented)
    - .planning/phases/203-indio-deep-seed/203-03-SUMMARY.md (the 5 UUID→URL→license manifest from Task 2)
    - C:/EV-Accounts/backend/migrations/1330_palm_springs_headshots.sql (PRIMARY analog — read in FULL: per-row shape, header comment, WHERE NOT EXISTS idempotency)
  </read_first>
  <action>
    Author `1339_indio_headshots.sql` (verify actual next number at execute time). Copy the Palm Springs
    1330 per-row shape, 5 rows — one per councilmember. Each row: INSERT INTO essentials.politician_images
    (id, politician_id, url, type, photo_license) SELECT gen_random_uuid(), (SELECT id FROM
    essentials.politicians WHERE external_id = <member ext_id>), '<Storage URL from Task 2 manifest>',
    'default', '<license from Task 2 manifest>' WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images
    WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = <member ext_id>)).
    Use the ACTUAL UUIDs/URLs/licenses from the 203-03-SUMMARY.md manifest — do NOT invent them. `type`
    MUST be 'default' on every row (the UI filters on it). Header comment must state this is AUDIT-ONLY,
    NOT registered in the migration ledger, applied via `psql -f` AFTER the Python pipeline uploads to
    Storage. Ensure no leftover palm-springs / -4011 token in comments or SQL (verify-greps check
    comments too). The orchestrator applies it and confirms 5/5 politician_images rows resolve to a live
    Storage URL.
  </action>
  <verify>
    <automated>F=$(ls C:/EV-Accounts/backend/migrations/*_indio_headshots.sql 2>/dev/null | head -1); test -n "$F" && test -f "$F" && grep -qF "politician_images" "$F" && grep -qF "'default'" "$F" && grep -qF "WHERE NOT EXISTS" "$F" && [ "$(grep -c 'INSERT INTO essentials.politician_images' "$F")" -ge 5 ] && ! grep -qiE "schema_migrations|palm-?springs|-4011" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; 5 INSERT INTO essentials.politician_images rows.
    - Columns exactly (id, politician_id, url, type, photo_license); type='default' on all 5.
    - politician_id resolved via SELECT ... WHERE external_id = <manifest ext_id, -4012001..-4012005>; WHERE NOT EXISTS idempotency guard on each.
    - URLs + licenses are the actual Task 2 manifest values (no invented UUIDs); no leftover palm-springs / -4011 tokens.
    - Header states AUDIT-ONLY / NOT registered; no schema_migrations INSERT.
    - Orchestrator confirms 5/5 rows resolve to a live Storage URL after `psql -f` apply.
  </acceptance_criteria>
  <done>Audit-only headshots migration authored (5 rows, type='default', WHERE NOT EXISTS, unregistered) using the real Task 2 manifest; applied by the orchestrator; 5/5 images live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image hosts (indio.civicweb.net / indio.org CivicPlus, remote) → pipeline | Untrusted image bytes enter the pipeline over HTTP/Playwright |
| pipeline → Supabase Storage + politician_images | Processed images + DB pointer rows written with service-role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-IMG | Spoofing | wrong-person / mislabeled photo | mitigate | Per-member identity confirmed at the Task 2 visual-QA blocking checkpoint before the audit migration binds it |
| T-203-LIC | Repudiation | wrong image license | mitigate | photo_license set per-member to the ACTUAL source; never defaulted to us_government_work for a non-.gov source |
| T-203-03 | Tampering | non-idempotent re-application | mitigate | WHERE NOT EXISTS (politician_id) guard on every politician_images row; audit-only (unregistered) so ledger untouched |
| T-203-SC | Tampering | Python package installs | accept | Zero new installs — reuses already-installed PIL/requests/psycopg2/Playwright |
</threat_model>

<verification>
- 5/5 councilmembers have a 600×750 4:5 crop-first headshot in Storage (HTTP 200), correct identity.
- 5 politician_images rows, type='default', bound by external_id→UUID, license matches source.
- Migration is audit-only (not in the ledger).
</verification>

<success_criteria>
All 5 Indio councilmembers carry a correct, correctly-licensed 600×750 headshot.
</success_criteria>

<output>
Create `.planning/phases/203-indio-deep-seed/203-03-SUMMARY.md` when done (record the 5
UUID→URL→photo_license→source manifest + per-member source used).
</output>
</content>
