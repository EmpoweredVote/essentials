---
phase: 64-san-jose-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/218_sj_officials.sql
autonomous: true

must_haves:
  truths:
    - "11 San Jose politicians exist with external_ids -640001 (Mayor) and -640010 through -640019 (council D1-D10)"
    - "11 office rows exist, each linked to the correct district and chamber"
    - "Mayor's office links to district geo_id='0668000' LOCAL_EXEC and Mayor chamber"
    - "Each council office links to sj-council-district-N LOCAL district and City Council chamber"
    - "All 11 politicians have office_id back-filled (non-NULL)"
    - "Section-split detector returns 0 rows for City of San Jose"
    - "SJ City Hall address lookup returns Anthony Tordillos (District 3) plus Matt Mahan (Mayor)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/218_sj_officials.sql"
      provides: "11 politician + office INSERT blocks + office_id back-fill"
      contains: "City of San Jose"
  key_links:
    - from: "218_sj_officials.sql politicians INSERT"
      to: "essentials.offices INSERT"
      via: "WITH ins_p AS (... RETURNING id) CROSS JOIN districts"
      pattern: "CROSS JOIN ins_p"
    - from: "218_sj_officials.sql offices INSERT"
      to: "essentials.districts"
      via: "WHERE d.geo_id = 'sj-council-district-N' AND d.district_type = 'LOCAL'"
      pattern: "sj-council-district"
    - from: "UPDATE politicians SET office_id"
      to: "essentials.offices"
      via: "WHERE o.politician_id = p.id AND p.external_id BETWEEN -640019 AND -640001"
      pattern: "BETWEEN -640019 AND -640001"
---

<objective>
Seed all 11 San Jose elected officials (Mayor + 10 council members) as politicians with linked office rows, then back-fill office_id on each politician so the headshot work-list query in Plan 64-03 returns all 11 rows.

Purpose: Without office rows, the address lookup returns geofence hits but no politician names. Without office_id back-fill, the headshot plan's politician JOIN offices query returns 0 rows.
Output: Migration 218 applied — 11 politicians, 11 offices, office_id populated on all 11.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-san-jose-deep-seed/64-CONTEXT.md
@.planning/phases/64-san-jose-deep-seed/64-RESEARCH.md
@.planning/phases/64-san-jose-deep-seed/64-01-SUMMARY.md

# Reference officials seed migration (adapt for SJ)
@C:/EV-Accounts/backend/migrations/214_berkeley_officials.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 218_sj_officials.sql</name>
  <files>C:/EV-Accounts/backend/migrations/218_sj_officials.sql</files>
  <action>
Create 218_sj_officials.sql following the exact pattern of 214_berkeley_officials.sql with San Jose specifics.

Pre-flight block (comment at top of file confirming the range is clear — research verified this, but document it):
  -- Pre-flight confirmed 2026-05-22: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001 returned 0

Pattern for each politician:
  WITH ins_p AS (
    INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id)
    VALUES (gen_random_uuid(), '{full_name}', '{first}', '{last}', NULL, true, false, false, true, {external_id})
    ON CONFLICT (external_id) DO NOTHING
    RETURNING id
  )
  INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)
  SELECT gen_random_uuid(), d.id, (SELECT id FROM essentials.chambers WHERE name='{chamber}' AND government_id=(SELECT id FROM essentials.governments WHERE name='City of San Jose' AND state='CA')), p.id, '{title}', 'CA', false, false, NULL
  FROM essentials.districts d CROSS JOIN ins_p p
  WHERE d.geo_id = '{geo_id}' AND d.district_type = '{type}' AND d.state = 'CA'
    AND p.id IS NOT NULL
    AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id);

Section 1 — 10 council members:
  All use chamber name='City Council', district_type='LOCAL', title='Council Member (District N)'

  | external_id | full_name         | first_name | last_name  | geo_id                   |
  |-------------|-------------------|------------|------------|--------------------------|
  | -640010     | Rosemary Kamei    | Rosemary   | Kamei      | sj-council-district-1    |
  | -640011     | Pamela Campos     | Pamela     | Campos     | sj-council-district-2    |
  | -640012     | Anthony Tordillos | Anthony    | Tordillos  | sj-council-district-3    |
  | -640013     | David Cohen       | David      | Cohen      | sj-council-district-4    |
  | -640014     | Peter Ortiz       | Peter      | Ortiz      | sj-council-district-5    |
  | -640015     | Michael Mulcahy   | Michael    | Mulcahy    | sj-council-district-6    |
  | -640016     | Bien Doan         | Bien       | Doan       | sj-council-district-7    |
  | -640017     | Domingo Candelas  | Domingo    | Candelas   | sj-council-district-8    |
  | -640018     | Pam Foley         | Pam        | Foley      | sj-council-district-9    |
  | -640019     | George Casey      | George     | Casey      | sj-council-district-10   |

Section 2 — Mayor:
  external_id=-640001, full_name='Matt Mahan', first='Matt', last='Mahan'
  chamber name='Mayor', district_type='LOCAL_EXEC', geo_id='0668000'
  title='Mayor'
  Note: district WHERE clause must match LOCAL_EXEC: WHERE d.geo_id='0668000' AND d.district_type IN ('LOCAL','LOCAL_EXEC') AND d.state='CA'
  (same pattern as Berkeley Mayor in migration 214 line ~330)

Section 3 — office_id back-fill:
  UPDATE essentials.politicians p
  SET office_id = o.id
  FROM essentials.offices o
  WHERE o.politician_id = p.id
    AND p.external_id BETWEEN -640019 AND -640001
    AND p.office_id IS NULL;

Wrap in BEGIN; ... COMMIT;

Notes from Berkeley pattern:
  - party = NULL (antipartisan design — never set a party value)
  - is_appointed_position = false for ALL 11 (all are elected officials)
  - is_appointed = false on politicians for ALL 11
  - slug is GENERATED on chambers — never include in WHERE using slug for this migration
  - No City Attorney, no City Auditor
  </action>
  <verify>
After writing the file, verify structure by reading the file and confirming:
- 10 council member blocks (external_ids -640010 through -640019)
- 1 Mayor block (external_id -640001)
- 1 UPDATE block for office_id back-fill (BETWEEN -640019 AND -640001)
- Wrapped in BEGIN/COMMIT
- No party value set (all NULL)
- No City Attorney or City Auditor entries
  </verify>
  <done>
218_sj_officials.sql written with correct structure: 11 WITH ins_p blocks, 1 back-fill UPDATE, no appointed positions seeded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 218 and verify end-to-end routing</name>
  <files>C:/EV-Accounts/backend/migrations/218_sj_officials.sql</files>
  <action>
Apply migration 218 via Supabase MCP (mcp__supabase-local__execute_sql).

Post-apply verification queries (run via Supabase MCP):

1. Count politicians:
   SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001;
   Expected: 11

2. Count offices:
   SELECT COUNT(*) FROM essentials.offices o
   JOIN essentials.politicians p ON p.id = o.politician_id
   WHERE p.external_id BETWEEN -640019 AND -640001;
   Expected: 11

3. Office_id back-fill:
   SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001 AND office_id IS NULL;
   Expected: 0

4. End-to-end routing (SJ City Hall → District 3):
   SELECT p.full_name, o.title
   FROM essentials.geofence_boundaries gb
   JOIN essentials.districts d ON d.geo_id = gb.geo_id AND d.state = gb.state
   JOIN essentials.offices o ON o.district_id = d.id
   JOIN essentials.politicians p ON p.id = o.politician_id
   WHERE gb.mtfcc = 'X0010'
     AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint(-121.88, 37.335), 4326));
   Expected: 1 row — Anthony Tordillos, 'Council Member (District 3)'

5. Mayor routing (citywide via LOCAL_EXEC):
   SELECT p.full_name, o.title
   FROM essentials.geofence_boundaries gb
   JOIN essentials.districts d ON d.geo_id = gb.geo_id AND d.state = gb.state
   JOIN essentials.offices o ON o.district_id = d.id
   JOIN essentials.politicians p ON p.id = o.politician_id
   WHERE gb.geo_id = '0668000' AND gb.state = '06';
   Expected: 1 row — Matt Mahan, 'Mayor'

6. Section-split detector (must return 0 rows):
   SELECT ch.name_formal, COUNT(DISTINCT COALESCE(gb.display_name,'')) AS distinct_section_names,
     array_agg(DISTINCT COALESCE(gb.display_name, '(none)') ORDER BY COALESCE(gb.display_name, '(none)')) AS section_names,
     COUNT(DISTINCT o.id) AS office_count
   FROM essentials.offices o
   JOIN essentials.chambers ch ON ch.id = o.chamber_id
   LEFT JOIN essentials.districts d ON d.id = o.district_id
   LEFT JOIN essentials.government_bodies gb
     ON gb.geo_id = d.geo_id AND gb.state = d.state
     AND gb.body_key = COALESCE(NULLIF(ch.name_formal,''), ch.name)
   WHERE ch.name_formal != ''
     AND ch.government_id = (SELECT id FROM essentials.governments WHERE name='City of San Jose' AND state='CA')
   GROUP BY ch.name_formal
   HAVING COUNT(DISTINCT COALESCE(gb.display_name,'')) > 1
   ORDER BY office_count DESC;
   Expected: 0 rows
  </action>
  <verify>
All 6 queries above return expected results:
- 11 politicians, 11 offices, 0 with NULL office_id
- SJ City Hall → Anthony Tordillos (District 3)
- Mayor → Matt Mahan
- Section-split detector: 0 rows
  </verify>
  <done>
Migration 218 applied. All 11 San Jose officials are seeded with correct office links, back-filled office_ids, and end-to-end routing confirmed for both district (D3) and citywide (Mayor) lookups.
  </done>
</task>

</tasks>

<verification>
Final check — query all 11 SJ politicians and their titles to confirm the full roster:
```sql
SELECT p.full_name, p.external_id, o.title
FROM essentials.politicians p
JOIN essentials.offices o ON o.politician_id = p.id
WHERE p.external_id BETWEEN -640019 AND -640001
ORDER BY p.external_id;
```
Expected: 11 rows with exact names from RESEARCH.md roster table.
</verification>

<success_criteria>
1. Migration 218 applied idempotently
2. 11 politicians and 11 offices in DB — all linked correctly
3. SJ City Hall (-121.88, 37.335) routes to Anthony Tordillos (Council Member, District 3)
4. Mayor Matt Mahan linked to LOCAL_EXEC district (0668000)
5. All 11 politicians have non-NULL office_id
6. Section-split detector returns 0 rows
</success_criteria>

<output>
After completion, create `.planning/phases/64-san-jose-deep-seed/64-02-SUMMARY.md`
</output>
