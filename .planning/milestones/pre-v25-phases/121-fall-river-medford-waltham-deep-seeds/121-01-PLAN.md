---
phase: 121-fall-river-medford-waltham-deep-seeds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/590_fall_river_city_government.sql
autonomous: true
requirements:
  - FALLRIV-01

must_haves:
  truths:
    - "essentials.governments row exists for 'City of Fall River, Massachusetts, US'"
    - "Fall River City Council chamber exists under the Fall River government"
    - "LOCAL_EXEC district (label='Fall River (Citywide)', geo_id=<verified Fall River geo_id>, state='ma') exists with Mayor linked"
    - "LOCAL district (label='Fall River', geo_id=<verified Fall River geo_id>, state='ma') exists with all City Councilors linked"
    - "All politicians have non-null office_id values after back-fill"
    - "Section-split check returns 0 orphan rows for Fall River geo_id"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/590_fall_river_city_government.sql"
      provides: "Fall River city government seed — Mayor + all City Council members"
      contains: "590_fall_river_city_government"
  key_links:
    - from: "essentials.politicians (Fall River external_ids)"
      to: "essentials.offices (district_type=LOCAL_EXEC and LOCAL, Fall River geo_id)"
      via: "office_id back-fill UPDATE"
      pattern: "external_id BETWEEN -<geo_id>NNN AND -<geo_id>001"
---

<objective>
Seed Fall River city government: Mayor + all City Council members linked to Fall River's geo_id.

Purpose: Satisfies FALLRIV-01 — a Fall River, MA address returns a LOCAL section listing the Mayor + all elected City Council members.
Output: Migration 590 applied to production; Mayor + City Council politicians + offices + 2 districts inserted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Fall River geo_id and research current council roster</name>
  <files></files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/587_new_bedford_city_government.sql (analog — pre-flight block structure, geo_id assertion pattern; New Bedford is another MA coastal city with similar Mayor-Council form)
    - .planning/STATE.md (MA FIPS facts, geo_id verification patterns, key schema invariants)
  </read_first>
  <action>
Run these two queries via mcp__supabase-local__execute_sql to gather the facts needed for Task 2:

QUERY 1 — Confirm Fall River geo_id:
SELECT geo_id, mtfcc, state
FROM essentials.geofence_boundaries
WHERE state = '25' AND mtfcc = 'G4110'
  AND geo_id LIKE '%2318%'
ORDER BY geo_id;

If that returns no rows, run a broader search:
SELECT geo_id, mtfcc, state
FROM essentials.geofence_boundaries
WHERE state = '25' AND mtfcc = 'G4110'
  AND geo_id IN ('2322640', '2322600', '2322580', '2322960');

The expected Fall River FIPS place code is 22640 → geo_id='2522640'. Verify this is present with mtfcc='G4110' and state='25'.

QUERY 2 — Confirm no pre-existing Fall River government:
SELECT id, name FROM essentials.governments WHERE name ILIKE '%Fall River%';

QUERY 3 — Confirm external_id range is clear (using geo_id digits from Query 1):
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -2522640030 AND -2522640001;

After running queries, record:
- Confirmed geo_id (expected '2522640')
- Pre-existing gov rows (expect 0)
- Clear external_id range (expect 0)

Then research the current Fall River city government structure:
1. Fall River uses a Mayor-Council form of government
2. Visit https://www.fallriverma.org/city-government/city-council/ to find current council roster
3. Current Mayor (as of 2026): Paul Coogan (inaugurated Jan 2022, re-elected 2023)
4. Fall River City Council: 9 members — 3 at-large + 6 ward (Wards 1–6); verify current seat count and members from official site

Record verified roster for Task 2.
  </action>
  <verify>
    <automated>SELECT geo_id, mtfcc, state FROM essentials.geofence_boundaries WHERE state = '25' AND mtfcc = 'G4110' AND geo_id = '2522640'; -- Expected: 1 row</automated>
  </verify>
  <acceptance_criteria>
    - Query returns geo_id='2522640', mtfcc='G4110', state='25' for Fall River
    - No pre-existing government row for Fall River (gov query returns 0 rows)
    - External ID range -2522640030..-2522640001 is clear (0 rows)
    - Current council roster confirmed (seat count + member names verified from fallriverma.org)
  </acceptance_criteria>
  <done>Fall River geo_id confirmed; roster verified; ready for migration authoring in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration 590_fall_river_city_government.sql</name>
  <files>C:/EV-Accounts/backend/migrations/590_fall_river_city_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/587_new_bedford_city_government.sql (primary analog — identical 11-member council structure; copy all structural patterns: pre-flight DO blocks, WITH ins_p CTE pattern, office_id back-fill, post-verification DO block)
    - C:/EV-Accounts/backend/migrations/584_lynn_city_government.sql (secondary analog — Mayor-Council with ward + at-large mix)
    - Task 1 output (confirmed geo_id, verified roster with seat count and member names)
  </read_first>
  <action>
Create C:/EV-Accounts/backend/migrations/590_fall_river_city_government.sql modeled on 587_new_bedford_city_government.sql with Fall River-specific values.

FALL RIVER FACTS (use Task 1 verified values; defaults shown below):
- geo_id: '2522640' (G4110, confirmed in Task 1)
- Government name: 'City of Fall River, Massachusetts, US'
- Chamber name: 'City Council', name_formal: 'Fall River City Council'
- LOCAL_EXEC district label: 'Fall River (Citywide)', mtfcc=NULL, state='ma'
- LOCAL district label: 'Fall River', mtfcc=NULL, state='ma'
- Mayor: Paul Coogan — external_id=-2522640001, first_name='Paul', last_name='Coogan', full_name='Paul Coogan', title='Mayor'
- Council structure: 9 members — 3 at-large (title='City Councilor') + 6 ward (title='City Councilor (Ward N)', Wards 1–6)
  Use verified names from Task 1. Assign external_ids -2522640002 through -2522640010 (at-large first, then ward order).
  Spelling: 'City Councilor' (single-L American spelling, same as New Bedford convention)
- Migration ledger version: '590'

Key schema invariants (carry from STATE.md):
- governments.state = 'MA' (uppercase)
- districts.state = 'ma' (lowercase)
- offices.representing_state = 'MA' (uppercase)
- district mtfcc = NULL (not 'G4110')
- geofence_boundaries.state = '25' (only in pre-flight assertion, read-only)
- NEVER include slug in chambers INSERT (GENERATED ALWAYS column)
- slug is GENERATED ALWAYS — never include in INSERT column list
- essentials.governments has NO unique constraint on geo_id — use WHERE NOT EXISTS guard on name

Migration structure (copy from 587):

Migration header comment block:
-- Migration 590: City of Fall River, Massachusetts government (FALLRIV-01)
-- geo_id: '2522640' (G4110, confirmed in DB, state='25')
-- 1 chamber: 'City Council' (name_formal='Fall River City Council')
-- 1 LOCAL_EXEC district: label='Fall River (Citywide)', mtfcc=NULL, state='ma'
-- 1 LOCAL district: label='Fall River', mtfcc=NULL, state='ma'
-- N politicians: Mayor Coogan (-2522640001) + 3 at-large (-2522640002..-2522640004) + 6 ward (-2522640005..-2522640010)
-- N offices: Mayor → LOCAL_EXEC; all councilors → LOCAL
-- CRITICAL: 'City Councilor' NOT 'City Councillor' — American spelling

Pre-flight blocks (3 — copy DO $$ structure from 587):
- Block 1: RAISE EXCEPTION if government 'City of Fall River, Massachusetts, US' already exists
- Block 2: Assert geo_id='2522640' AND mtfcc='G4110' row exists in geofence_boundaries (RAISE EXCEPTION if missing)
- Block 3: Assert external_id BETWEEN -2522640010 AND -2522640001 returns 0 rows (RAISE EXCEPTION if any found)

BEGIN/COMMIT wrapper: YES (copy from 587).

Politician+office blocks (use WITH ins_p AS (INSERT ... RETURNING id) INSERT INTO offices ... FROM districts d CROSS JOIN ins_p p CTE pattern):
- One block per politician; Mayor uses district_type=LOCAL_EXEC; all councilors use district_type=LOCAL
- All politicians: party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true
- ON CONFLICT (external_id) DO NOTHING

Office_id back-fill UPDATE (after all politician+office blocks):
UPDATE essentials.politicians p SET office_id = o.id
FROM essentials.offices o WHERE o.politician_id = p.id
  AND p.external_id BETWEEN -2522640010 AND -2522640001
  AND p.office_id IS NULL;

Post-verification DO block (7 gates — copy from 587, substituting Fall River values):
- Gate (a): gov count = 1 (WHERE name = 'City of Fall River, Massachusetts, US')
- Gate (b): chamber count = 1 (WHERE name = 'City Council' AND government_id = Fall River gov)
- Gate (c): district count = 2 (WHERE geo_id = '2522640' AND state = 'ma' AND district_type IN ('LOCAL_EXEC', 'LOCAL'))
- Gate (d): politician count = N (BETWEEN -2522640010 AND -2522640001; N = 1 Mayor + 9 councilors = 10)
- Gate (e): office count = N (JOIN districts WHERE geo_id='2522640' AND state='ma' AND district_type IN ('LOCAL_EXEC', 'LOCAL'))
- Gate (f): section-split orphans = 0 (geofence_boundaries WHERE geo_id='2522640' AND mtfcc='G4110' AND NOT EXISTS in districts)
- Gate (g): null office_ids = 0 (WHERE external_id BETWEEN -2522640010 AND -2522640001 AND office_id IS NULL)
RAISE NOTICE on PASS: 'Migration 590 post-verification PASSED: gov=%, chambers=%, districts=%, politicians=%, offices=%, split_orphans=%, null_office_ids=%'

Migration ledger:
INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('590') ON CONFLICT (version) DO NOTHING;
COMMIT;

NOTE: Adjust external_id upper bound (-2522640010 vs -2522640NNN) to exactly match the total politician count confirmed in Task 1. If Fall River has a different council seat count than 9, adjust accordingly — use the verified roster from Task 1 as ground truth, not the default shown here.

After writing the file, apply it via mcp__supabase-local__execute_sql.
  </action>
  <verify>
    <automated>Run the SQL via mcp__supabase-local__execute_sql. Post-verification DO block must emit: 'Migration 590 post-verification PASSED'</automated>
  </verify>
  <acceptance_criteria>
    - SQL file C:/EV-Accounts/backend/migrations/590_fall_river_city_government.sql exists
    - Migration applies without errors (no EXCEPTION raised)
    - Post-verification PASSED notice appears with values: gov=1, chambers=1, districts=2, politicians=N, offices=N, split_orphans=0, null_office_ids=0
    - Query: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2522640030 AND -2522640001 → N (matches Task 1 roster count)
    - Query: SELECT title FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id = -2522640001 → 'Mayor'
    - Query: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2522640030 AND -2522640001 AND office_id IS NULL → 0
    - Query: SELECT COUNT(*) FROM essentials.districts WHERE geo_id = '2522640' AND state = 'ma' AND district_type IN ('LOCAL_EXEC', 'LOCAL') → 2
    - Section-split: SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id = '2522640' AND gb.mtfcc = 'G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.state = 'ma') → 0
    - No 'City Councillor' (double-L) spelling anywhere in the migration file — only 'City Councilor' (single-L)
    - Slug column absent from the chambers INSERT statement
    - FALLRIV-01 satisfied: Fall River address will return LOCAL section with Mayor + all City Councilors
  </acceptance_criteria>
  <done>Migration 590 applied; Fall River Mayor + all City Councilors in DB with correct titles, districts, and non-null office_ids; post-verification PASSED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → Supabase production DB | Idempotent INSERT statements; no user input crosses this boundary |
| Web research → roster data | Executor fetches current council roster from fallriverma.org; data consumed as reference only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01-01 | Tampering | Schema migrations ledger | mitigate | ON CONFLICT (version) DO NOTHING prevents duplicate application |
| T-121-01-02 | Tampering | governments table | mitigate | WHERE NOT EXISTS guard on name prevents duplicate gov rows; pre-flight EXCEPTION aborts if already applied |
| T-121-01-03 | Tampering | chambers INSERT | mitigate | Executor must not include slug column (GENERATED ALWAYS) — accepted via explicit instruction; acceptance criteria verifies absence |
| T-121-01-04 | Denial of Service | Pre-flight assertions | accept | Pre-flight EXCEPTION blocks abort migration cleanly if applied twice; expected behavior in solo dev workflow |
| T-121-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; SQL-only migration |
</threat_model>

<verification>
After applying migration 590 via mcp__supabase-local__execute_sql:

1. SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2522640030 AND -2522640001; -- Expected: N (roster count)
2. SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = '2522640' AND d.state = 'ma'; -- Expected: N
3. SELECT COUNT(*) FROM essentials.districts WHERE geo_id = '2522640' AND state = 'ma'; -- Expected: 2 (LOCAL_EXEC + LOCAL)
4. SELECT title FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id = -2522640001; -- Expected: 'Mayor'
5. SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2522640030 AND -2522640001 AND office_id IS NULL; -- Expected: 0
6. Section-split: SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id = '2522640' AND gb.mtfcc = 'G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.state = 'ma'); -- Expected: 0
</verification>

<success_criteria>
- Migration 590 applied cleanly; post-verification PASSED
- Fall River Mayor Coogan + all City Councilors seeded with correct titles
- All officials have non-null office_ids
- Section-split check = 0 rows
- FALLRIV-01 satisfied: Fall River address returns LOCAL section with Mayor + all City Councilors
</success_criteria>

<output>
Create `.planning/phases/121-fall-river-medford-waltham-deep-seeds/121-01-SUMMARY.md` when done
</output>
