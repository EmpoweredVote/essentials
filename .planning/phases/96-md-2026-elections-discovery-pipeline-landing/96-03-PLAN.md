---
phase: 96-md-2026-elections-discovery-pipeline-landing
plan: 03
type: execute
wave: 2
depends_on:
  - 96-01
files_modified:
  - C:/EV-Accounts/backend/migrations/281_md_2026_discovery.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-281.ts
  - C:/Transparent Motivations/essentials/src/pages/Landing.jsx
autonomous: true
requirements:
  - MD-ELECTIONS-02
  - MD-ELECTIONS-03
tags:
  - discovery
  - landing
  - jsx
  - sql
  - maryland

must_haves:
  truths:
    - "essentials.discovery_jurisdictions contains 2 MD rows (state='MD', jurisdiction_geoid='24') — one per election_date (2026-06-23 + 2026-11-03)"
    - "Both MD discovery rows have source_url pointing at elections.maryland.gov 2026 candidate filing page"
    - "Both MD discovery rows have allowed_domains array of exactly 4 entries (elections.maryland.gov, mgaleg.maryland.gov, ballotpedia.org, maryland.gov)"
    - "src/pages/Landing.jsx COVERAGE_CITIES array contains a Maryland entry with browseGovernmentList=['2446475','24037'] (Leonardtown + St. Mary's County)"
    - "Landing.jsx Maryland entry is inserted AFTER the Portland Oregon entry — no other COVERAGE_CITIES rows reordered"
    - "Both MD discovery rows have election_date values within the active 180-day cron eligibility window as of phase execution (2026-06-23 primary is ~17 days out; 2026-11-03 general is ~150 days out — cron agent will pick both up automatically)"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/281_md_2026_discovery.sql
      provides: "Two INSERT statements for essentials.discovery_jurisdictions covering 2026-06-23 primary + 2026-11-03 general"
      contains: "elections.maryland.gov/elections/2026/primary_candidates"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-281.ts
      provides: "Apply script for migration 281 with row-count + allowed_domains verification"
      contains: "281_md_2026_discovery.sql"
    - path: C:/Transparent Motivations/essentials/src/pages/Landing.jsx
      provides: "COVERAGE_CITIES array with Maryland entry"
      contains: "browseGovernmentList: ['2446475', '24037']"
  key_links:
    - from: Landing.jsx COVERAGE_CITIES
      to: essentials.governments
      via: "browseGovernmentList array passed to representatives API"
      pattern: "'2446475', '24037'"
    - from: 281_md_2026_discovery.sql
      to: essentials.discovery_jurisdictions
      via: "INSERT ... ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING"
      pattern: "INSERT INTO essentials\\.discovery_jurisdictions"
---

<objective>
Arm the discovery pipeline for Maryland's 2026 election cycle and add Maryland to the public coverage map.

Two deliverables in one plan:

1. **Migration 281**: Insert 2 rows into `essentials.discovery_jurisdictions` — one for the 2026-06-23 primary, one for the 2026-11-03 general. Both target `jurisdiction_geoid='24'` (Maryland FIPS) with the same source_url and allowed_domains. This satisfies MD-ELECTIONS-02.

2. **Landing.jsx edit**: Insert one new entry into the `COVERAGE_CITIES` array (in `src/pages/Landing.jsx`) immediately after the Portland Oregon entry. The entry bundles BOTH Leonardtown (geo_id `'2446475'`) AND St. Mary's County (geo_id `'24037'`) into one `browseGovernmentList` array so clicking Maryland on the landing page shows the town + county officials together. This satisfies MD-ELECTIONS-03.

**Critical research findings to honor (from CONTEXT.md D-03 + D-04 and RESEARCH.md):**
- `essentials.discovery_jurisdictions` has NO `cron_active` column. The ROADMAP wording "cron_active=true" is STALE — discovery eligibility is date-based (180-day cron window). Do NOT add or reference a cron_active column.
- Schema has UNIQUE (jurisdiction_geoid, election_date) — idempotency clause: `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING`.
- Use the verified primary date `2026-06-23` (NOT July 14). Use general date `2026-11-03`.
- source_url for BOTH rows: `https://elections.maryland.gov/elections/2026/primary_candidates/index.html` (verified by researcher).
- allowed_domains array: `['elections.maryland.gov', 'mgaleg.maryland.gov', 'ballotpedia.org', 'maryland.gov']` — 4 entries.
- Landing.jsx exact object (D-04, copy verbatim): `{ label: 'Leonardtown', state: 'Maryland', browseGovernmentList: ['2446475', '24037'], browseStateAbbrev: 'MD' }`. Insert AFTER the Portland Oregon entry (currently line 23 of Landing.jsx as of 2026-06-06).

Purpose: Make the cron discovery agent aware of Maryland's election cycle and expose Maryland coverage on the public landing page.
Output: 1 SQL migration applied to production + 1 apply script + 1 JSX file edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-CONTEXT.md
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-RESEARCH.md
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-PATTERNS.md
@C:/EV-Accounts/backend/migrations/183_me_2026_elections_foundation.sql
@C:/EV-Accounts/backend/scripts/_apply-migration-277.ts
@C:/Transparent Motivations/essentials/src/pages/Landing.jsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write migration 281 (discovery_jurisdictions) + apply script + apply to production</name>
  <files>C:/EV-Accounts/backend/migrations/281_md_2026_discovery.sql, C:/EV-Accounts/backend/scripts/_apply-migration-281.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/183_me_2026_elections_foundation.sql (canonical analog — Section 2 discovery_jurisdictions pattern; note "no cron_active column" header comment)
    - C:/EV-Accounts/backend/scripts/_apply-migration-277.ts (apply-script shape)
    - .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-PATTERNS.md (Pattern Assignment for 281 — exact MD substitutions)
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/281_md_2026_discovery.sql`. Mirror migration 183 Section 2 structure (two INSERT statements, one per election_date).

    Header comment block MUST include:
    - "Migration 281: MD 2026 Discovery Jurisdictions — Phase 96 Plan 03"
    - Citation of D-03: "essentials.discovery_jurisdictions has NO cron_active column — eligibility is date-based (180-day cron window before election_date). ROADMAP wording 'cron_active=true' for MD-ELECTIONS-02 is stale and ignored."
    - Citation of D-04 in note: "Landing.jsx edit is co-deployed (NOT in this SQL file)"

    Two INSERT statements, each touching ONLY these columns: `jurisdiction_geoid`, `jurisdiction_name`, `state`, `election_date`, `source_url`, `allowed_domains`.

    Row 1 (primary):
    - jurisdiction_geoid: `'24'`
    - jurisdiction_name: `'State of Maryland'`
    - state: `'MD'`
    - election_date: `'2026-06-23'`
    - source_url: `'https://elections.maryland.gov/elections/2026/primary_candidates/index.html'`
    - allowed_domains: `ARRAY['elections.maryland.gov', 'mgaleg.maryland.gov', 'ballotpedia.org', 'maryland.gov']`

    Row 2 (general): identical EXCEPT `election_date='2026-11-03'`.

    Both INSERTs MUST use idempotency clause: `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING`.

    Append a post-verification DO $$ block:
    - Count rows: `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='MD'` — expect 2
    - `IF v_count <> 2 THEN RAISE EXCEPTION 'Expected 2 MD discovery_jurisdictions rows, found %', v_count`

    Create `C:/EV-Accounts/backend/scripts/_apply-migration-281.ts` mirroring _apply-migration-277.ts. Verification queries to log:
    - Row count: `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='MD'` — expect 2
    - Both dates present: `SELECT election_date FROM essentials.discovery_jurisdictions WHERE state='MD' ORDER BY election_date` — expect 2026-06-23 then 2026-11-03
    - allowed_domains length spot-check: `SELECT array_length(allowed_domains, 1) FROM essentials.discovery_jurisdictions WHERE state='MD' AND election_date='2026-06-23'` — expect 4
    - source_url spot-check: `SELECT source_url FROM essentials.discovery_jurisdictions WHERE state='MD' AND election_date='2026-06-23'` — expect the elections.maryland.gov URL
    - Ledger: `SELECT version FROM supabase_migrations.schema_migrations WHERE version='281'`

    Apply: `cd C:/EV-Accounts/backend; npx tsx scripts/_apply-migration-281.ts`. Re-run for idempotency (count stays at 2).
  </action>
  <acceptance_criteria>
    - File exists: `C:/EV-Accounts/backend/migrations/281_md_2026_discovery.sql`
    - File contains literal: `'2026-06-23'`
    - File contains literal: `'2026-11-03'`
    - File contains literal: `'State of Maryland'`
    - File contains literal: `https://elections.maryland.gov/elections/2026/primary_candidates/index.html`
    - File contains literal: `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING`
    - File contains literal: `ARRAY['elections.maryland.gov', 'mgaleg.maryland.gov', 'ballotpedia.org', 'maryland.gov']`
    - File does NOT contain the string `cron_active` (D-03 — column does not exist)
    - File does NOT contain `2026-07-14`
    - File contains: `RAISE EXCEPTION 'Expected 2 MD discovery_jurisdictions rows`
    - File exists: `C:/EV-Accounts/backend/scripts/_apply-migration-281.ts`
    - SQL after apply: `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='MD'` returns 2
    - SQL: `SELECT array_length(allowed_domains, 1) FROM essentials.discovery_jurisdictions WHERE state='MD' AND election_date='2026-06-23'` returns 4
    - SQL: `SELECT source_url FROM essentials.discovery_jurisdictions WHERE state='MD' AND election_date='2026-06-23'` returns `https://elections.maryland.gov/elections/2026/primary_candidates/index.html`
    - Re-running apply script keeps count at 2 (idempotency)
    - Ledger row 281 exists
  </acceptance_criteria>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-281.ts</automated>
  </verify>
  <done>
    Migration 281 applied, 2 MD discovery_jurisdictions rows present with correct dates/URL/allowed_domains, idempotent on re-run, ledger entry present.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Edit Landing.jsx — add Maryland entry to COVERAGE_CITIES</name>
  <files>C:/Transparent Motivations/essentials/src/pages/Landing.jsx</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/pages/Landing.jsx (current COVERAGE_CITIES array, lines 14-24 — verify Portland Oregon is still line 23 before inserting)
    - .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-CONTEXT.md (D-04 — exact JS object verbatim)
    - .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-PATTERNS.md (Pattern Assignment for Landing.jsx — current array structure + insertion point)
  </read_first>
  <action>
    Open `src/pages/Landing.jsx` and insert exactly one new element into the `COVERAGE_CITIES` array, immediately after the `Portland Oregon` entry and before the closing `];`. The new element MUST be the EXACT object from D-04, copy verbatim with single quotes (matching surrounding style):

    `  { label: 'Leonardtown', state: 'Maryland', browseGovernmentList: ['2446475', '24037'], browseStateAbbrev: 'MD' },`

    Do NOT modify:
    - The Berkeley/Fremont/Sacramento/San Diego/San Francisco/San Jose/Portland ME/Cambridge/Portland OR entries (no reordering)
    - The `COVERAGE_COUNTIES` array
    - The `handleAreaClick` function or any other code in the file
    - Any imports

    The new entry adds Maryland to the public coverage map. Two geo_ids are bundled because clicking the entry routes to a combined browse that shows BOTH the Town of Leonardtown government (geo_id `'2446475'`) AND St. Mary's County government (geo_id `'24037'`) — both were seeded in Phase 95 and confirmed present in production.

    After the edit, run a syntax sanity check:
    - From the project root: `npm run build` (or `npx vite build` if no script) to confirm the JSX still parses. If the build fails, inspect the error, fix the syntax (likely a stray comma), and re-run. Do NOT proceed until the build succeeds.

    Optional manual check (do NOT block on it): start the dev server with `npm run dev`, navigate to `/`, and confirm "Leonardtown" appears in the Alpha Areas / coverage tiles. This is informational; the build check is the gate.
  </action>
  <acceptance_criteria>
    - File `src/pages/Landing.jsx` contains literal string `browseGovernmentList: ['2446475', '24037']`
    - File contains literal: `label: 'Leonardtown'`
    - File contains literal: `browseStateAbbrev: 'MD'`
    - File contains literal: `state: 'Maryland'`
    - The Maryland entry appears AFTER the Portland Oregon entry (line-order check: grep the file for the Maryland entry; its line number must be > the Portland Oregon entry's line number)
    - The 9 existing COVERAGE_CITIES entries (Berkeley, Fremont, Sacramento, San Diego, San Francisco, San Jose, Portland Maine, Cambridge, Portland Oregon) all still exist with their browseGovernmentList values unchanged
    - `npm run build` (or `npx vite build`) completes without errors after the edit
    - File does NOT contain a duplicate Maryland entry (grep count for `state: 'Maryland'` in COVERAGE_CITIES area == 1)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    Landing.jsx COVERAGE_CITIES has exactly one Maryland entry (Leonardtown) inserted after Portland Oregon, all 9 prior entries unchanged, build passes.
  </done>
</task>

</tasks>

<verification>
1. Migration 281 apply output shows count=2, allowed_domains length=4, source_url matches verified value.
2. Landing.jsx build passes after the edit.
3. Both Leonardtown and St. Mary's County are bundled in one COVERAGE_CITIES entry (per D-04).
4. No `cron_active` references in migration 281 (per D-03).
5. Idempotency: re-running migration 281 leaves count at 2.
</verification>

<success_criteria>
- MD discovery armed: 2 rows in essentials.discovery_jurisdictions with correct dates, URL, and allowed_domains
- Cron agent will pick up MD jurisdictions within the 180-day window (no cron_active toggle needed — date-based)
- Landing.jsx publicly exposes Maryland coverage
- Phase 96 deliverables fully met: 130 race rows (96-02), 2 election rows (96-01), 2 discovery rows + landing entry (96-03)
- All requirements satisfied: MD-ELECTIONS-02 (discovery) + MD-ELECTIONS-03 (landing) by this plan; MD-ELECTIONS-01 (elections + races) by 96-01 + 96-02
</success_criteria>

<output>
Create `.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-03-SUMMARY.md` when done. Capture:
- Discovery row count + dates + URL confirmation
- Landing.jsx diff (just the one added line and its line number)
- Build pass confirmation
- Final phase row counts: 2 elections + 130 races + 2 discovery jurisdictions
- Phase 96 marked complete; next phase is 97 (MD Compass Stances — Executives + Senators)
</output>
