---
phase: 88-tx-collin-county-school-boards
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/267_allen_mayor_office_fix.sql
autonomous: true
requirements: [TX-SCHOOL-04]
gap_closure: true
gaps_addressed:
  - "GAP 3: Allen TX Mayor completely missing — offices.politician_id stale (points to inactive Baine Brooks)"

must_haves:
  truths:
    - "An Allen TX address returns Mayor Chris Schulmeister in the Representatives tab"
    - "Allen city section shows 7 members total (Mayor + 6 council)"
    - "Chris Schulmeister politician row has valid_from and data_source populated"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/267_allen_mayor_office_fix.sql"
      provides: "Migration wiring Schulmeister to Allen Mayor office + metadata fill"
      contains: "698da6ca-eadd-46a0-8e27-94ae48d23279"
  key_links:
    - from: "essentials.offices (id=684ffdb3)"
      to: "essentials.politicians (id=698da6ca)"
      via: "offices.politician_id UPDATE"
      pattern: "SET politician_id='698da6ca"
---

<objective>
Write and apply migration 267 to fix the Allen TX Mayor gap. The Allen Mayor office row currently points to former (inactive) Mayor Baine Brooks. New Mayor Chris Schulmeister was seeded into politicians but the office.politician_id was never updated. This migration links the office to Schulmeister and populates his missing metadata.

Purpose: Allen TX address returns a Mayor, fixing the "no mayor at all for Allen" UAT failure.
Output: Migration 267 applied to production DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/88-tx-collin-county-school-boards/88-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 267 — Allen Mayor office re-point to Schulmeister</name>
  <files>C:/EV-Accounts/backend/migrations/267_allen_mayor_office_fix.sql</files>
  <action>
Create C:/EV-Accounts/backend/migrations/267_allen_mayor_office_fix.sql with this exact content:

Pre-flight DO block: assert that Chris Schulmeister exists in essentials.politicians with id='698da6ca-eadd-46a0-8e27-94ae48d23279' and is_active=true. RAISE EXCEPTION if not found.

Step 1: UPDATE essentials.offices SET politician_id='698da6ca-eadd-46a0-8e27-94ae48d23279' WHERE id='684ffdb3-4073-4164-86f6-c151334fccb1';

Step 2: UPDATE essentials.politicians SET valid_from='2026-05-03', data_source='collin_county_official' WHERE id='698da6ca-eadd-46a0-8e27-94ae48d23279';

Post-verification DO block — RAISE EXCEPTION if any assertion fails:
  (a) Assert offices row (id=684ffdb3) now has politician_id = '698da6ca-eadd-46a0-8e27-94ae48d23279'
  (b) Assert politicians row (id=698da6ca) has valid_from IS NOT NULL and data_source IS NOT NULL
  (c) Assert politicians row (id=698da6ca) has is_active=true

Migration ledger: INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('267') ON CONFLICT DO NOTHING;

Do NOT include a DROP or DELETE of the former Mayor (Baine Brooks). His politician row stays; only the office.politician_id pointer changes.
  </action>
  <verify>
    <automated>Test-Path "C:/EV-Accounts/backend/migrations/267_allen_mayor_office_fix.sql"</automated>
  </verify>
  <done>File exists at C:/EV-Accounts/backend/migrations/267_allen_mayor_office_fix.sql with all three steps and post-verification block.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 267 to production DB</name>
  <files>C:/EV-Accounts/backend/migrations/267_allen_mayor_office_fix.sql</files>
  <action>
Apply the migration using the Supabase MCP tool (mcp__supabase-local execute_sql). Run the entire content of 267_allen_mayor_office_fix.sql against the production database.

After applying, run these two spot-check queries to confirm:

Query A — offices pointer:
  SELECT o.id, o.politician_id, p.full_name, p.is_active
  FROM essentials.offices o
  JOIN essentials.politicians p ON p.id = o.politician_id
  WHERE o.id = '684ffdb3-4073-4164-86f6-c151334fccb1';

Expected: politician_id='698da6ca-eadd-46a0-8e27-94ae48d23279', full_name='Chris Schulmeister', is_active=true

Query B — politician metadata:
  SELECT id, full_name, valid_from, data_source, is_active
  FROM essentials.politicians
  WHERE id = '698da6ca-eadd-46a0-8e27-94ae48d23279';

Expected: valid_from='2026-05-03', data_source='collin_county_official', is_active=true

Query C — migration ledger:
  SELECT version FROM supabase_migrations.schema_migrations WHERE version = '267';

Expected: one row returned.

If any query fails, do NOT proceed — diagnose and fix the migration SQL before re-applying.
  </action>
  <verify>
    <automated>SELECT version FROM supabase_migrations.schema_migrations WHERE version = '267';</automated>
  </verify>
  <done>All three spot-check queries return expected values. Migration ledger has version='267'. Allen Mayor office points to Chris Schulmeister (is_active=true).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → production DB | Admin-authored SQL; Supabase MCP executes with service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-88-04-01 | Tampering | offices.politician_id UPDATE | mitigate | Pre-flight asserts Schulmeister UUID exists before UPDATE runs |
| T-88-04-02 | Integrity | Baine Brooks stale row | accept | Brooks row stays in politicians (is_active=false); no routing query returns him (WHERE p.is_active=true filter in essentialsService.ts) |
</threat_model>

<verification>
After migration 267 is applied:
- Query A confirms offices pointer updated
- Query B confirms Schulmeister metadata populated
- Query C confirms ledger entry present
- Visual check: Allen TX address in ev-ui shows Mayor Chris Schulmeister first in city section (after Plan 03 code fix lands)
</verification>

<success_criteria>
- Migration 267 applied; ledger confirms version='267'
- essentials.offices row 684ffdb3 has politician_id=698da6ca-eadd-46a0-8e27-94ae48d23279
- essentials.politicians row 698da6ca has valid_from='2026-05-03' and data_source='collin_county_official'
- GAP 3 closed: Allen TX address returns Mayor Chris Schulmeister
</success_criteria>

<output>
Create `.planning/phases/88-tx-collin-county-school-boards/88-04-SUMMARY.md` when done
</output>
