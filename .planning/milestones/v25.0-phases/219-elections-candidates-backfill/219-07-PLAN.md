---
phase: 219-elections-candidates-backfill
plan: 07
type: execute
wave: 2
depends_on: [219-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1398_collin_thin_backfill_a.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1398_collin_thin_backfill_a.ts
autonomous: false
requirements: [COLLIN-ELECT-02, COLLIN-ELECT-03]
user_setup: []

must_haves:
  truths:
    - "Allen (4801924), Anna (4803300), Lucas (4845012), Murphy (4850100) and Prosper (4863276) each have their remaining offices' races backfilled where that seat had a known election, ADDING to the existing migration-100 races (not re-seeding them) (COLLIN-ELECT-02)."
    - "Uncontested/declared-elected remaining seats are seeded as a race + the single candidate (D-03); seats whose most-recent election predates a reasonable research horizon stay documented-open (D-06)."
    - "Existing migration-100 races for these cities are untouched (idempotent guards make re-seeding net-zero)."
    - "Every seeded race links to the correct existing office_id and renders on /results (COLLIN-ELECT-03)."
  artifacts:
    - "C:/EV-Accounts/backend/migrations/1398_collin_thin_backfill_a.sql"
    - "C:/EV-Accounts/backend/scripts/_apply-migration-1398_collin_thin_backfill_a.ts"
  key_links:
    - "backfilled races.office_id → the previously race-less existing offices.id for each city."
    - "races.election_id → shared 2026-05-02 row (or a minted earlier-cycle row for continuing-incumbent seats whose last election predates 2026)."
  prohibitions:
    - statement: "candidate_status only ever uses a legal CHECK value (active/withdrawn/filed)."
      status: enforced
      verification: "apply-script gate: 0 candidate rows with candidate_status NOT IN ('active','withdrawn','filed')."
    - statement: "position_name always city-prefixed; backfilled names never collide with the existing migration-100 races for the same city."
      status: enforced
      verification: "apply-script gate: total race count per city = existing + newly-seeded, no ON CONFLICT swallow."
    - statement: "primary_party stays NULL (D-06); no inform.* writes (D-07)."
      status: enforced
      verification: "0 non-NULL primary_party on seeded races; inform.* before/after unchanged."
    - statement: "No fabricated candidate; a remaining office with no resolvable election stays race-less with a documented reason (Pitfall 4: '0 unseated offices' ≠ 'races complete')."
      status: enforced
      verification: "Every seeded candidate cited; each still-race-less office has a written reason."
    - statement: "Existing migration-100 races are not duplicated or modified."
      status: enforced
      verification: "apply-script re-run net-zero; existing race ids unchanged."
  backstop:
    - statement: "Per Lucas, Places 3-6 are continuing incumbents (Bierman/Lawrence/Fisher/Peterson) likely from an EARLIER cycle — their races may need a minted earlier-cycle election row, not 2026-05-02; honor the Place↔Seat naming drift (DB title stays 'Place')."
      verification: backstop
---

<objective>
Backfill the remaining race-less offices for five thin cities — Allen, Anna, Lucas, Murphy, Prosper — adding to (never re-seeding) the contested races migration 100 already captured. Many remaining seats are uncontested/declared-elected or continuing incumbents from an earlier cycle.

Purpose: Closes COLLIN-ELECT-02 for these five cities. COLLIN-ELECT-03 for their render path.
Output: migration 1398 + gated apply-script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-elections-candidates-backfill/219-RESEARCH.md
@.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md
@.planning/phases/219-elections-candidates-backfill/219-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify each city's remaining offices + source the missing-office rosters</name>
  <read_first>
    - 219-RESEARCH.md §"Thin governments" rows for Allen/Anna/Lucas/Murphy/Prosper (existing races to REUSE + offices still needing a race), §Pitfall 1, §Pitfall 4.
    - C:/EV-Accounts/backend/migrations/100_collin_county_may2026_races.sql (the existing races per city — do NOT re-seed these).
    - Sources: cityofallen.org, annatexas.gov/1015/Elections, lucastexas.us/164/City-Council, murphytx.org, prospertx.gov/479/May-2026-General-Election.
  </read_first>
  <action>
    For each city, list the offices that currently have NO race (offices minus the migration-100-covered seats). For each such office, source its most-recent held election + full filed field (winners + losers cited — D-04) or single declared-elected candidate (D-03). Determine per seat whether it voted 2026-05-02 (shared row) or an earlier cycle (needs a minted earlier-cycle election row — esp. Lucas Places 3-6 continuing incumbents). Map winners to offices.politician_id where Phase 218 seated them (D-05); losers name-only; is_incumbent explicit. Honor the Place/Seat naming drift (DB title stays 'Place'/'Seat' as stored — join by the stored title). Any office with no resolvable election stays race-less with a written reason. Escape apostrophes; preserve diacritics.
  </action>
  <verify>
    <automated>MISSING — roster verified by Task 2 apply-script gates.</automated>
  </verify>
  <acceptance_criteria>
    - Each city's race-less offices enumerated against migration 100.
    - Each backfilled seat has a cited roster + election-date decision; unresolvable seats documented-open.
  </acceptance_criteria>
  <done>Seed-ready cited backfill rosters exist for all five cities with earlier-cycle vs shared-row decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 1398 + gated apply-script</name>
  <read_first>
    - 219-RESEARCH.md §Pattern 1/2/3/4 (+ Pattern 5 preamble for any minted earlier-cycle row).
    - C:/EV-Accounts/backend/migrations/1375 (ON CONFLICT idiom); 100 (existing races to preserve).
    - C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts (gate shape).
    - Migration number from PREFLIGHT (research: 1398; re-confirm + shift block if drifted).
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1398_collin_thin_backfill_a.sql in BEGIN/COMMIT. Where a backfilled seat voted 2026-05-02, resolve the shared election id by name/date/state; where it voted an earlier cycle, INSERT (ON CONFLICT (name, election_date, state) DO NOTHING) an earlier-cycle election row and resolve it. INSERT each backfill race with a city-prefixed position_name distinct from the existing migration-100 races, correct office_id, primary_party NULL, ON CONFLICT position-name idiom (this guarantees the existing races are untouched). INSERT race_candidates WHERE NOT EXISTS; winners→politician_id, candidate_status='active', is_incumbent explicit, cited source. Write C:/EV-Accounts/backend/scripts/_apply-migration-1398_collin_thin_backfill_a.ts mirroring 1389 with gates: per-geo_id total race count = pre-existing + newly-seeded (assert it increased by the expected count per city); every seeded race has ≥1 candidate; 0 illegal candidate_status; 0 non-NULL primary_party; inform.* unchanged; split-section 0; idempotent re-run net-zero (proving migration-100 races unaffected).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/_apply-migration-1398_collin_thin_backfill_a.ts 2>&1 | head; test -f migrations/1398_collin_thin_backfill_a.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; earlier-cycle rows minted by name/date/state where needed.
    - Every backfill race uses the ON CONFLICT position-name idiom with a city-prefixed name distinct from existing races.
    - Apply-script asserts the per-city race count increased by the expected number and the idempotent re-run is net-zero.
  </acceptance_criteria>
  <done>Migration 1398 + gated apply-script authored and type-check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator applies 1398, confirms gates green, pushes, browse spot-check</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-1398_collin_thin_backfill_a.ts.
    - [[no_git_in_ev_accounts]] / [[backend_architecture]].
  </read_first>
  <action>Operator-run checkpoint: apply migration 1398 via `npx tsx`, confirm every apply-script gate exits 0 (incl. the per-city race-count-increase and net-zero re-run), commit + push to C:/EV-Accounts (Render auto-deploys), then browse-spot-check Anna on /results — per the numbered how-to-verify steps.</action>
  <what-built>Executor has no DB access / prod push; operator applies + verifies.</what-built>
  <how-to-verify>
    1. `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-1398_collin_thin_backfill_a.ts` — all gates green, exit 0.
    2. `git -C "C:/EV-Accounts" add -A && git -C "C:/EV-Accounts" commit -m "feat(219): backfill thin-city races A (Allen/Anna/Lucas/Murphy/Prosper)" && git -C "C:/EV-Accounts" push origin master`.
    3. Browse spot-check `/results?browse_geo_id=4803300` (Anna) — confirm the new backfilled races render alongside the existing ones.
  </how-to-verify>
  <acceptance_criteria>
    - Apply-script exits 0, all gates green; per-city race count increased by the expected number.
    - Anna browse shows more races than before (backstop: human visual).
  </acceptance_criteria>
  <resume-signal>Type "approved" once gates green + browse passes, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `C:/EV-Accounts/backend/migrations/1398_collin_thin_backfill_a.sql` (new)
- `C:/EV-Accounts/backend/scripts/_apply-migration-1398_collin_thin_backfill_a.ts` (new)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external canvass/news → race_candidates | Untrusted civic claims; every row cited |
| migration SQL → production essentials schema | Idempotent, guarded ADD-only writes; existing races preserved |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Tampering/Info disclosure | race_candidates rows | high | mitigate | Evidence-only; still-race-less offices documented, never guessed |
| T-219-02 | Tampering (schema integrity) | candidate_status | high | mitigate | Only legal CHECK values; gate asserts 0 illegal |
| T-219-05 | Data integrity (silent loss) | backfill vs existing migration-100 races | high | mitigate | City-prefixed distinct position_name; per-city count-increase gate; idempotent re-run |
| T-219-04 | Integrity (scope violation) | inform.* tables | high | mitigate | before/after row-count assertion (D-07) |
| T-219-03 | Tampering (SQL literal) | apostrophe/diacritic names | medium | mitigate | Escape '→''; preserve UTF-8; type-check |
| T-219-SC | Tampering | npm/pip installs | low | accept | No package installs; legitimacy gate N/A |
</threat_model>

<verification>
- All apply-script gates green.
- Per-city race count increased by the expected number; existing races untouched; split-section 0; primary_party 0; inform.* net-zero.
</verification>

<success_criteria>
Allen/Anna/Lucas/Murphy/Prosper render their backfilled races on /results alongside the existing ones; still-race-less offices are documented; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/219-elections-candidates-backfill/219-07-SUMMARY.md` when done.
</output>
