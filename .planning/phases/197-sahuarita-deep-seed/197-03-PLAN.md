---
phase: 197-sahuarita-deep-seed
plan: 03
type: execute
# WAVE NOTE (deliberate serialization): wave 3 sits behind wave 2 (headshots) even though both depend
# ONLY on Plan 01. This is an INTENTIONAL serialization, not a computed dependency inconsistency.
# Stance research must run ONE OFFICIAL AT A TIME (feedback_stance_research_one_at_a_time: parallel
# stance fan-out burns the session quota), so this plan is kept in its own later wave to avoid a
# parallel stance pass and keep each execution context small.
wave: 3
depends_on: ["197-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1356_sahuarita_mayor_stances.sql
  - C:/EV-Accounts/backend/migrations/1357_sahuarita_vice_mayor_stances.sql
  - C:/EV-Accounts/backend/migrations/1358_sahuarita_council_1_stances.sql
  - C:/EV-Accounts/backend/migrations/1359_sahuarita_council_2_stances.sql
  - C:/EV-Accounts/backend/migrations/1360_sahuarita_council_3_stances.sql
  - C:/EV-Accounts/backend/migrations/1361_sahuarita_council_4_stances.sql
  - C:/EV-Accounts/backend/migrations/1362_sahuarita_council_5_stances.sql
autonomous: true
requirements: [SUB-03]
must_haves:
  truths:
    - "Evidence-only compass stances are seeded for the 7 officials against the 36 non-judicial live topics (never the 8 judicial-* topics)"
    - "Every stance row is 100% cited (has a non-empty sources array); there are ZERO neutral-default rows — a topic with no evidence gets NO row at all (honest blank)"
    - "Stances are researched one official at a time (quota); citations are from reachable non-WAF sources (tucson.com / gvnews.com / tucsonsentinel.com / citizenportal.ai / kgun9.com); salient Sahuarita issues probed: growth/development, Santa Cruz Valley / Colorado River-CAP water, mining (Rosemont / Copper World / Hudbay), data centers"
    - "D-04: full stances per convention — evidence-only across all live compass topics, one research agent at a time (rate-limit), 100% citation, NO default values, honest blank spokes; applies to Sahuarita's 7 elected council members"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1356_sahuarita_mayor_stances.sql"
      provides: "Mayor Murphy (-4014001) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1362_sahuarita_council_5_stances.sql"
      provides: "Council member Lytle (-4014007) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "same (politician_id, topic_id) — every answer has a cited context row"
      pattern: "politician_context"
    - from: "stance rows"
      to: "inform.compass_topics (36 non-judicial)"
      via: "topic_id references a non-judicial live topic UUID"
      pattern: "topic_id"
---

<objective>
Seed evidence-only compass stances for the 7 Town of Sahuarita officials. One audit-only migration per
official (1356 Mayor Murphy, 1357 Vice Mayor Egbert, 1358-1362 the 5 plain council members), researched
ONE OFFICIAL AT A TIME (quota), against the 36 NON-JUDICIAL live compass topics (44 live topics minus the
8 `judicial-*` keys, which have no bearing on an at-large council member — Pitfall 6). Every stance is
100% cited with evidence-based reasoning; a topic with no documented record gets NO row at all (honest
blank), never a neutral default (global `feedback_stance_no_default_value`). Discrete 1-5 "chairs", never
a polarity scale.

CRITICAL (salient Sahuarita local issues to probe — RESEARCH): the Copper World / Hudbay (formerly
Rosemont) copper-mine proposal (water, traffic, environment — very active as of 2026), Colorado River /
CAP water allocation and Santa Cruz Valley groundwater, data-center growth, and general
residential/commercial growth pressure. These are the highest-yield evidence topics for this council.

CRITICAL (sources): cite the reachable non-WAF Tucson-metro / southern-AZ outlets confirmed reachable
2026-07-16: tucson.com / Arizona Daily Star, gvnews.com (Green Valley News), tucsonsentinel.com,
citizenportal.ai, kgun9.com, and Sahuarita council meeting minutes at pub-sahuarita.escribemeetings.com.
sahuaritaaz.gov HTML itself is reachable (NOT WAF-blocked) so a fetched sahuaritaaz.gov page IS a citable
source when it actually renders — but never cite an unfetched URL.

Wave placement (deliberate): this plan is wave 3, behind wave 2 (headshots), even though BOTH depend only
on Plan 01. That is an INTENTIONAL serialization to honor the one-official-at-a-time stance-research quota
(`feedback_stance_research_one_at_a_time`) and keep each execution context small — NOT a computed
dependency inconsistency. Within this plan, work each official's migration one at a time (author → apply →
next), 1356 through 1362, so a context that degrades partway is recoverable at a clean per-official
boundary.

Purpose: Satisfies SUB-03 / ROADMAP success criterion #3 (evidence-only stances for all seated officials,
100% cited, no defaults, honest blanks).
Output: 7 audit-only migrations `1356_sahuarita_mayor_stances.sql` +
`1357_sahuarita_vice_mayor_stances.sql` + `1358..1362_sahuarita_council_N_stances.sql` (unregistered),
applied so the officials' compasses populate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md
@.planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md
@.planning/phases/197-sahuarita-deep-seed/197-01-SUMMARY.md

<execution_note>
Depends on Plan 01: the 7 politician UUIDs from 197-01-SUMMARY.md are REQUIRED (stance rows key on the
official UUID). Executor authors the 7 `.sql` files (researching one official at a time per the
`feedback_stance_research_one_at_a_time` quota rule — do NOT fan out parallel stance research). The
ORCHESTRATOR applies each via `psql -f` and asserts. Commit via `git -C "C:/EV-Accounts"`. These are
AUDIT-ONLY migrations — NOT registered in the ledger. Independent of Plan 02 (headshots) — both depend
only on Plan 01, kept in separate, later waves DELIBERATELY to honor the one-official-at-a-time quota.
Research the Task-2-confirmed sitting roster (if any seat changed at the July 21 primary, stance the
seated official, not a research-date name).
</execution_note>

<interfaces>
- inform.politician_answers (politician_id, topic_id, value): value = discrete 1.0-5.0 chair;
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value.
- inform.politician_context (politician_id, topic_id, reasoning, sources): reasoning = cited evidence prose
  ($$-quoted, avoids apostrophe-escaping); sources = ARRAY[...]::text[] of source URLs (NEVER empty);
  ON CONFLICT (politician_id, topic_id) DO UPDATE.
- inform.compass_topics: 44 live topics (DB-confirmed 2026-07-16); office_scope is empty/NULL (DB does NOT
  enforce judicial exclusion — manual convention). The 8 EXCLUDED judicial keys: judicial-access-to-justice,
  judicial-bail-pretrial, judicial-criminal-justice, judicial-government-deference, judicial-interpretation,
  judicial-police-accountability, judicial-prosecution-priorities, judicial-transparency. Research against
  the remaining 36 only.
- Evidence sources (RESEARCH — non-WAF, reachable): tucson.com / Arizona Daily Star, gvnews.com,
  tucsonsentinel.com, citizenportal.ai, kgun9.com, pub-sahuarita.escribemeetings.com, and rendered
  sahuaritaaz.gov pages. Salient issues: Copper World/Hudbay mine, Colorado River/CAP + Santa Cruz Valley
  water, data centers, growth pressure.
- ext_ids → file: -4014001 Murphy (Mayor) → 1356; -4014002 Egbert (Vice Mayor) → 1357; -4014003 Morales →
  1358 (Council 1); -4014004 Gillespie → 1359 (Council 2); -4014005 Priolo → 1360 (Council 3); -4014006
  Lisk → 1361 (Council 4); -4014007 Lytle → 1362 (Council 5).
- Tenure caveat: attribute a documented position only to an official actually in office at the time of the
  vote/statement (Lisk and Lytle were sworn Nov 2024 — shorter tenure; do NOT attribute pre-Nov-2024
  votes to them). Egbert (-4014002) is not seeking re-election — stance her documented record while seated;
  if Task 2 replaced her with a certified successor, stance the successor's record instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 7 per-official evidence-only stance migrations (one official at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1356_sahuarita_mayor_stances.sql, C:/EV-Accounts/backend/migrations/1357_sahuarita_vice_mayor_stances.sql, C:/EV-Accounts/backend/migrations/1358_sahuarita_council_1_stances.sql, C:/EV-Accounts/backend/migrations/1359_sahuarita_council_2_stances.sql, C:/EV-Accounts/backend/migrations/1360_sahuarita_council_3_stances.sql, C:/EV-Accounts/backend/migrations/1361_sahuarita_council_4_stances.sql, C:/EV-Accounts/backend/migrations/1362_sahuarita_council_5_stances.sql</files>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md (§ `1356..1362_sahuarita_<official>_stances.sql` mapping — the per-official stance migration pattern from the Marana 1347 analog: topic-UUID reference comment block, the two-table INSERT (politician_answers + politician_context) per topic with ON CONFLICT DO UPDATE, the 36/36 judicial exclusion, the DELIBERATELY BLANK honest-blank documentation section, one file per official, $$-quoting + ARRAY[...]::text[] sources, tenure caveat; cite non-WAF sources)
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Common Pitfall 6 — the 8 judicial keys to exclude, 36 non-judicial; § Sources — the reachable southern-AZ news list; § Architecture Patterns diagram — the salient Sahuarita issues: Copper World/Hudbay mine, Colorado River/CAP water, data centers, growth)
    - C:/EV-Accounts/backend/migrations/1347_marana_mayor_stances.sql (analog — read header + first stance block: topic-UUID table, BEGIN/COMMIT wrapper, two-table INSERT/ON CONFLICT shape, $$-quoted reasoning, ARRAY[...]::text[] sources pattern, DELIBERATELY BLANK section)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1355 after Plan 02, or 1354 if Plan 02 not yet applied — either way name
    these sequentially disk-MAX+1). Author 7 AUDIT-ONLY migrations, one per official, numbered 1356 Mayor
    (Murphy) / 1357 Vice Mayor (Egbert) / 1358 Council 1 (Morales) / 1359 Council 2 (Gillespie) / 1360
    Council 3 (Priolo) / 1361 Council 4 (Lisk) / 1362 Council 5 (Lytle). NONE registered in the ledger.

    PER-OFFICIAL CADENCE (save-point / recoverability): work strictly ONE OFFICIAL AT A TIME and treat each
    as an independent save-point. For official N: (1) research N's documented positions, (2) author the
    file, (3) hand off for the orchestrator to apply + assert that ONE file (Task 2 asserts per file), then
    (4) move to the next — 1356 → 1357 → ... → 1362, in order. Do NOT research or author the whole batch in
    one pass. This keeps the quota honored (one at a time) AND makes a mid-plan context degradation
    recoverable at a clean per-official boundary. Record in the SUMMARY which officials are complete so a
    resumed pass knows where to restart.

    Research ONE OFFICIAL AT A TIME (quota — never parallel). For each official, mine the reachable non-WAF
    evidence sources (tucson.com, gvnews.com, tucsonsentinel.com, citizenportal.ai, kgun9.com,
    pub-sahuarita.escribemeetings.com, rendered sahuaritaaz.gov pages) for documented positions across the
    36 non-judicial live topics — with particular attention to the salient Sahuarita issues (Copper
    World/Hudbay mine, Colorado River/CAP + Santa Cruz Valley water, data centers, growth). For each topic
    WHERE clear evidence exists, emit BOTH: an INSERT into inform.politician_answers (politician_id = the
    official UUID from the Plan 01 manifest, topic_id = the non-judicial topic UUID, value = a discrete 1-5
    chair) AND an INSERT into inform.politician_context (same keys, reasoning = $$-quoted evidence prose
    naming the vote/statement/date, sources = ARRAY[...]::text[] of the actual non-WAF source URLs — NEVER
    an empty array, NEVER an unfetched URL). Both guarded ON CONFLICT (politician_id, topic_id) DO UPDATE.

    HARD RULES: (1) NEVER emit a row for a topic with no evidence — omit it entirely (honest blank),
    documented in the DELIBERATELY BLANK header section, never a neutral/default value
    (feedback_stance_no_default_value). (2) NEVER touch any of the 8 judicial-* topics — keep them in the
    reference comment block but write NO INSERT for them (Pitfall 6). (3) Every politician_answers row MUST
    have a matching politician_context row with a non-empty sources array (100% cited). (4) Values are
    discrete chairs (1-5), not a polarity scale (feedback_compass_chairs_not_polarity). (5) Attribute a
    vote/position only to officials in office at the time (no pre-tenure attribution — esp. Lisk/Lytle
    sworn Nov 2024).

    Copy the 44-topic UUID reference comment block from the analog into each file's header; the INSERTs skip
    the 8 judicial rows. Wrap each file in BEGIN/COMMIT. Do NOT add a ledger-registration INSERT.
  </action>
  <verify>
    <automated>for f in 1356_sahuarita_mayor 1357_sahuarita_vice_mayor 1358_sahuarita_council_1 1359_sahuarita_council_2 1360_sahuarita_council_3 1361_sahuarita_council_4 1362_sahuarita_council_5; do F="C:/EV-Accounts/backend/migrations/${f}_stances.sql"; test -f "$F" && grep -qF "politician_answers" "$F" && grep -qF "politician_context" "$F" && ! grep -qiE "INSERT INTO inform.politician_answers[^;]*judicial-" "$F" && ! grep -qE "INSERT INTO supabase_migrations" "$F" || { echo "FAIL $F"; exit 1; }; done; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist (1356 Mayor + 1357 Vice Mayor + 1358-1362 Council 1-5, disk-MAX+1 sequential names).
    - Each file contains both `inform.politician_answers` and `inform.politician_context` INSERTs.
    - NO INSERT references any `judicial-*` topic (the 8 judicial keys appear ONLY in the reference comment, never in an INSERT).
    - Every politician_answers INSERT has a matching politician_context INSERT with a non-empty `ARRAY[...]::text[]` sources value (no empty arrays); no unfetched URL cited as evidence.
    - NO neutral-default rows — every emitted row is evidence-backed; topics without evidence are absent (documented in the DELIBERATELY BLANK section).
    - NONE of the 7 files adds an `INSERT INTO supabase_migrations` line (audit-only).
    - Each file wrapped in BEGIN/COMMIT; each keyed to the correct official UUID from the Plan 01 manifest.
    - Files authored one official at a time (1356→1362), each a recoverable save-point; SUMMARY records per-official completion order.
  </acceptance_criteria>
  <done>7 audit-only stance migrations authored, one per official (1356→1362 in sequence, each a recoverable save-point), evidence-only against 36 non-judicial topics, 100% cited from non-WAF sources, honest blanks, discrete chairs, no defaults, no pre-tenure attribution to Lisk/Lytle — unregistered.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the 7 stance migrations + assert evidence-only integrity (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator applies existing migrations + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-VALIDATION.md (Per-Task Verification Map — stances row: 100% cited, no default rows, honest blanks)
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Common Pitfall 6 — 36 not 44; a "44 per official" check would wrongly flag complete coverage)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). BLOCKING apply-and-assert: Plan 04's coverage chip sets
    hasContext:true ONLY because these stance rows exist — so this apply must land (and assert green)
    before Plan 04's coverage.js edit. Apply the 7 migrations IN ORDER as each official's file lands (per
    the per-official cadence in Task 1) via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1356_sahuarita_mayor_stances.sql` (etc.
    through 1362) — applying + asserting each file before the next official keeps the per-official
    save-point clean. Then assert the aggregate via `psql -tAc`, recording in 197-03-SUMMARY.md: (a) every
    politician_answers row for the 7 Sahuarita UUIDs has a matching politician_context row (0 orphans); (b)
    0 politician_context rows with a NULL or empty sources array (100% cited); (c) 0 politician_answers rows
    on any judicial-* topic for the 7 officials; (d) all values in [1.0, 5.0]; (e) per-official stance
    counts (informational — expected to vary; honest blanks are fine, do NOT assert a fixed count). Any
    integrity failure (a-d) blocks the phase. Commit the 7 migrations via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id IN (-4014001,-4014002,-4014003,-4014004,-4014005,-4014006,-4014007) AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id)) AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc JOIN essentials.politicians p ON p.id=pc.politician_id WHERE p.external_id IN (-4014001,-4014002,-4014003,-4014004,-4014005,-4014006,-4014007) AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL)) AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN inform.compass_topics t ON t.id=pa.topic_id WHERE p.external_id IN (-4014001,-4014002,-4014003,-4014004,-4014005,-4014006,-4014007) AND t.topic_key LIKE 'judicial-%') AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id IN (-4014001,-4014002,-4014003,-4014004,-4014005,-4014006,-4014007) AND (pa.value < 1.0 OR pa.value > 5.0))"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 migrations apply cleanly.
    - 0 politician_answers rows without a matching politician_context row (every answer cited).
    - 0 politician_context rows with a NULL/empty sources array (100% cited).
    - 0 stance rows on any `judicial-*` topic for the 7 officials.
    - All stance values in [1.0, 5.0].
    - The combined boolean SELECT returns `t`.
    - Per-official counts recorded in the SUMMARY (informational; honest blanks expected, no fixed count asserted).
    - 7 migrations committed to `C:/EV-Accounts`; none registered in the ledger.
  </acceptance_criteria>
  <done>Evidence-only stances live for the 7 officials: 100% cited from non-WAF sources, no defaults, no judicial rows, honest blanks, no pre-tenure attribution — audit migrations applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stance migration files → production DB | Hand-authored evidence-only SQL crosses into live `inform` schema via `psql -f` |
| research sources → stance values | External news evidence maps to a 1-5 chair; unsourced/mis-scoped values corrupt the compass |

## STRIDE Threat Register

Honest scope: hand-authored evidence-only INSERTs into `inform`. Genuine threats are citation integrity
and mis-scoped/pre-tenure attribution — not injection or access control. No HIGH-severity threat; none
fabricated. Mirrors the Marana 196 threat model.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-CITE | Tampering | uncited / default stance rows | mitigate | Every answer requires a matching context row with a non-empty sources array; Task 2 asserts 0 orphans + 0 empty-sources; no-evidence topics omitted (honest blank), never defaulted |
| T-197-JUD | Tampering | judicial topics on a non-judicial office | mitigate | 8 judicial-* keys excluded from all INSERTs; Task 2 asserts 0 judicial rows for the 7 officials |
| T-197-SRC | Repudiation | citing an unfetched URL | mitigate | Cite only actually-fetched reachable non-WAF sources (tucson.com / gvnews.com / tucsonsentinel.com / citizenportal.ai / kgun9.com / escribemeetings / rendered sahuaritaaz.gov), never an unverified URL |
| T-197-TENURE | Spoofing | pre-tenure vote attribution | mitigate | Attribute a position only to officials in office at the time; Lisk/Lytle (sworn Nov 2024) not credited with pre-tenure votes |
| T-197-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `.env`; applied by the inline orchestrator |
| T-197-SC | Tampering | package installs | accept | No new installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Every stance row 100% cited (matching context row + non-empty sources); 0 orphans.
- 0 judicial-* rows; 0 neutral-default rows; values in [1.0, 5.0]; no pre-tenure attribution to Lisk/Lytle.
- Researched one official at a time (1356→1362, per-official save-points); 7 audit migrations applied and unregistered.
</verification>

<success_criteria>
SUB-03 / ROADMAP #3 (evidence-only compass stances, 100% cited, no defaults, honest blanks) is TRUE in
production for all 7 officials against the 36 non-judicial topics.
</success_criteria>

<output>
Create `.planning/phases/197-sahuarita-deep-seed/197-03-SUMMARY.md` when done — include per-official stance
counts, notable honest blanks (esp. on the salient Copper World/water/data-center/growth topics), the
per-official completion order (save-point trail), and the evidence sources used.
</output>
</content>
