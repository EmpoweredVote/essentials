---
phase: 196-marana-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["196-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-marana-headshots.py
  - C:/EV-Accounts/backend/migrations/1346_town_of_marana_headshots.sql
autonomous: true
requirements: [SUB-02]
must_haves:
  truths:
    - "All 7 officials (Mayor + 6 council members) have a 600×750 (4:5, Lanczos q90) headshot uploaded to the politician_photos Storage bucket"
    - "Each of the 7 politician_images rows binds to the CURRENT roster member seeded in Plan 01 (the execute-time-confirmed roster, never the FORMER member Jackie Craig)"
    - "Because maranaaz.gov is Akamai-WAF-blocked (same signature as Tucson/Oro Valley), source portraits come from non-WAF hosts (Ballotpedia candidate pages for Post/Kai/Officer/Murphy first, then Wikipedia/press) resolved via Playwright; photo_license is documented PER image (not a uniform value)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-marana-headshots.py"
      provides: "crop_to_4_5 → resize 600×750 Lanczos q90 → Storage x-upsert pipeline for 7 officials; source URLs resolved outside the WAF"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1346_town_of_marana_headshots.sql"
      provides: "7 audit-only politician_images rows (unregistered), UUIDs from Plan 01 manifest, per-image photo_license"
      contains: "politician_images"
  key_links:
    - from: "_tmp-marana-headshots.py"
      to: "non-WAF image hosts (Ballotpedia / Wikipedia / press)"
      via: "requests.get per-official resolved image URL with descriptive User-Agent"
      pattern: "crop_to_4_5"
    - from: "1346_town_of_marana_headshots.sql"
      to: "essentials.politicians"
      via: "url binds to politician UUID resolved by external_id"
      pattern: "politician_images"
---

<objective>
Acquire and bind 7/7 headshots (Mayor + 6 council members). Author a gitignored pipeline script that
fetches each official's portrait, crops to 4:5 (crop-first, never distort), resizes to 600×750 Lanczos
q90, and uploads to the `politician_photos` Storage bucket; plus an AUDIT-ONLY migration that inserts
the 7 `politician_images` rows.

CRITICAL (Pitfall 5): the official host `maranaaz.gov` is Akamai-WAF-blocked (403 to both `curl` and
`WebFetch`, identical `X-Reference-Error` signature to Tucson/Oro Valley — confirmed live 2026-07-15), so
source URLs must be resolved from NON-WAF hosts. RESEARCH's recommended order: check Ballotpedia
candidate pages FIRST for the four sitting officials who are also 2026 candidates (Post, Kai, Officer,
Murphy) — 2026 candidate pages often already carry a usable photo — then Wikipedia / press coverage for
the rest, all via the `/find-headshots` skill's Playwright browser-navigation flow
(`mcp__playwright__browser_navigate` + snapshot + manual image-URL extraction), which is an ORCHESTRATOR
step. Do NOT retry raw HTTP against maranaaz.gov (a 403 is a WAF, not an outage) and do NOT craft evasive
request headers to bypass the WAF (T-196-WAF). `photo_license` is documented PER image (varies by
source), NOT a uniform value.

Purpose: Satisfies SUB-02 / ROADMAP success criterion #2 (all seated officials have 600×750 headshots).
Output: `_tmp-marana-headshots.py` (gitignored `backend/scripts/_*`) +
`1346_town_of_marana_headshots.sql` (audit-only, unregistered), both applied so all 7 images serve from
the CDN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/196-marana-deep-seed/196-RESEARCH.md
@.planning/phases/196-marana-deep-seed/196-PATTERNS.md
@.planning/phases/196-marana-deep-seed/196-01-SUMMARY.md

<execution_note>
Depends on Plan 01: the 7 politician UUIDs (external_id → UUID → full_name) captured in
196-01-SUMMARY.md are REQUIRED — the audit migration's CDN url values derive from the politician UUID
({uuid}-headshot.jpg), and the script resolves UUIDs at runtime via `SELECT id FROM
essentials.politicians WHERE external_id=%s`. Executor authors both files; the ORCHESTRATOR resolves the
7 source image URLs via Playwright (WAF fallback), runs the script, and applies the audit migration
(executor has no DB/Storage/web access). Commit the migration via `git -C "C:/EV-Accounts"`; the
`_tmp-*` script is gitignored and never committed. If the Task 2 roster changed any seat (Comerford's
open seat, or a certified July 21 primary result), source the CONFIRMED sitting member — not the
research-date name.
</execution_note>

<interfaces>
- Source hosts: NOT maranaaz.gov (Akamai WAF, 403 confirmed 2026-07-15). Use Ballotpedia candidate pages
  FIRST for Post/Kai/Officer/Murphy (2026 candidates — often carry a photo), then Wikipedia / press
  coverage for the rest, resolved via Playwright. The 7 resolved image URLs are hardcoded per member into
  ROSTER at run time (orchestrator supplies them). News-outlet listings carry the roster but rarely an
  embedded portrait — not a primary photo source.
- essentials.politician_images columns: exactly (id, politician_id, url, type, photo_license) — NO
  photo_origin_url. type='default'; photo_license PER image (Wikimedia/CC terms, campaign/Ballotpedia
  press_use, or operator_supplied) — NOT a uniform value.
- Storage: bucket politician_photos; canonical path {politician_uuid}-headshot.jpg; x-upsert PUT;
  CDN base https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- ext_id block: -4013001 Post (Mayor), -4013002 Ziegler (VM), -4013003 Cavanaugh, -4013004 Comerford,
  -4013005 Kai, -4013006 Murphy, -4013007 Officer (subject to the Task 2 confirmed roster).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Marana headshot pipeline script (WAF-fallback sourcing)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-marana-headshots.py</files>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-PATTERNS.md (§ headshot analog `1346_town_of_marana_headshots.sql` mapping + the shared-patterns pipeline note: reuse crop_to_4_5/resize_600x750/process_headshot_bytes/upload_to_storage/resolve_politician_id/dry_run_head_check/HEADERS verbatim from the Oro Valley analog; ONLY ROSTER + source-acquisition change; Ballotpedia-first fallback; mixed per-image license)
    - .planning/phases/196-marana-deep-seed/196-RESEARCH.md (§ Common Pitfall 5 — maranaaz.gov WAF; Playwright fallback; Ballotpedia-first for Post/Kai/Officer/Murphy; per-image license discipline)
    - C:/EV-Accounts/backend/scripts/_tmp-oro-valley-headshots.py (PRIMARY analog — read in full: reuse crop_to_4_5, resize_600x750, process_headshot_bytes, upload_to_storage, resolve_politician_id, env-load, dry_run_head_check, descriptive-UA HEADERS verbatim; only ROSTER + source-URL builder + count assertions change)
  </read_first>
  <action>
    Copy `_tmp-oro-valley-headshots.py` and adapt. Reuse crop_to_4_5 / resize_600x750 /
    process_headshot_bytes (crop-FIRST then resize) / upload_to_storage (x-upsert PUT to
    `politician_photos/{uuid}-headshot.jpg`) / resolve_politician_id (parameterized `external_id=%s`,
    never hardcode a UUID) / the env-load block / dry_run_head_check / the descriptive User-Agent HEADERS
    — VERBATIM. Keep TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=LANCZOS, MIN_DIM=100.

    Build a ROSTER list of 7 dicts (assert len(ROSTER)==7), each with: external_id (-4013001..-4013007),
    full_name, seat label, photo_url (initially the `_MISSING`/`TODO_` sentinel — the ORCHESTRATOR fills
    the resolved non-WAF image URL per member in Task 3 via Playwright), photo_license (PER member — set
    from the actual source at fill time, NOT a uniform value), is_required=True (all 7). Add
    `source_url_for(member)` returning `member['photo_url']`.

    Keep the `_MISSING = 'TODO_...'` sentinel + `main()` fail-fast guard so a placeholder URL can NEVER
    reach a Storage write — if any ROSTER photo_url is still the sentinel, print the member and
    `sys.exit(1)`. Do a dry-run HEAD per member first (verify 200) before the real batch. On any
    required-member fetch/upload failure, print the member + `sys.exit(1)`. PIL `convert('RGB')` + JPEG
    re-encode optimize=True (strips EXIF/embedded payloads, T-196-IMG). Print a {external_id, uuid,
    cdn_url, photo_license} manifest for the audit migration.

    Add a comment block documenting that the official host maranaaz.gov is WAF-blocked and source URLs are
    resolved from non-WAF hosts (Ballotpedia candidate pages first for Post/Kai/Officer/Murphy, then
    Wikipedia / press) via Playwright (do NOT put a maranaaz.gov URL in ROSTER, and do NOT add
    header-spoofing logic to defeat a WAF).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-marana-headshots.py"; test -f "$F" && grep -qF "crop_to_4_5" "$F" && grep -qF "external_id=%s" "$F" && grep -qE "len\(ROSTER\)\s*==\s*7" "$F" && grep -qF "sys.exit(1)" "$F" && ! grep -qF "maranaaz.gov" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/_tmp-marana-headshots.py` (gitignored `backend/scripts/_*`).
    - Contains crop_to_4_5, resize_600x750, upload_to_storage; TARGET_SIZE (600, 750); LANCZOS; JPEG quality 90; MIN_DIM 100.
    - ROSTER has exactly 7 members (`assert len(ROSTER)==7`), ext_ids -4013001..-4013007.
    - politician_id resolved via parameterized `external_id=%s` (no f-string/`%`-format into SQL); PIL `convert('RGB')` present.
    - `_MISSING`/`TODO_` sentinel + fail-fast `sys.exit(1)` guard present so no placeholder URL reaches Storage; dry-run HEAD check present; is_required=True on all 7.
    - photo_license is a PER-member field (not a single hardcoded uniform value).
    - NO `maranaaz.gov` URL anywhere in the script; no WAF header-spoofing logic.
  </acceptance_criteria>
  <done>Pipeline script authored: 7-member ROSTER, crop-first 600×750 pipeline, runtime UUID resolution, sentinel + dry-run + required-failure PAUSE guards, per-image license, no WAF host.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1346_town_of_marana_headshots.sql</files>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-PATTERNS.md (§ `1346_town_of_marana_headshots.sql` mapping — the audit-only politician_images pattern from the Oro Valley 1306 analog: header, per-row INSERT ... WHERE NOT EXISTS, exact 5 columns, NO ledger registration, per-image mixed license, final `COMMIT;` only; the header notes maranaaz.gov WAF + Ballotpedia-first)
    - .planning/phases/196-marana-deep-seed/196-01-SUMMARY.md (the 7 external_id → UUID → full_name manifest)
    - C:/EV-Accounts/backend/migrations/1306_town_of_oro_valley_headshots.sql (analog — read the header + first few rows: audit-only, unregistered, WHERE NOT EXISTS idempotency, 5-column shape, mixed per-image license)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1345 after Plan 01); name this file disk-MAX+1 (provisionally
    `1346_town_of_marana_headshots.sql`). Author an AUDIT-ONLY migration (NO ledger-registration INSERT
    into supabase_migrations, applied via `psql -f`). The header may note it is audit-only / unregistered,
    that the source is NON-WAF hosts (Ballotpedia candidate pages first for Post/Kai/Officer/Murphy, then
    Wikipedia / press) resolved via Playwright because the official host maranaaz.gov is WAF-blocked
    (Pitfall 5), and that photo_license VARIES per image.

    Emit 7 per-row INSERTs into essentials.politician_images (id, politician_id, url, type, photo_license)
    — columns EXACTLY these five, no photo_origin_url. Each row: id=gen_random_uuid(); politician_id =
    `(SELECT id FROM essentials.politicians WHERE external_id = -4013001)` (etc. through -4013007); url =
    the CDN path `{CDN_BASE}/{politician_uuid}-headshot.jpg` using the UUID from the Plan 01 manifest;
    type='default'; photo_license = the PER-IMAGE license value (from the Task 1 sourcing manifest — the
    orchestrator finalizes any that differ after Task 3's Playwright pass). Guard each with WHERE NOT
    EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (...same subselect...)) —
    idempotent. Cover all 7 ext_ids -4013001..-4013007. Do NOT add a ledger-registration INSERT.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1346_town_of_marana_headshots.sql"; test -f "$F" && [ "$(grep -c 'INSERT INTO essentials.politician_images' "$F")" = "7" ] && ! grep -qF "photo_origin_url" "$F" && ! grep -qE "INSERT INTO supabase_migrations" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1346).
    - Grep count of `INSERT INTO essentials.politician_images` = 7.
    - Grep confirms all 7 ext_ids -4013001..-4013007 referenced.
    - Columns are exactly (id, politician_id, url, type, photo_license) — NO `photo_origin_url`.
    - NO `INSERT INTO supabase_migrations` line (audit-only, unregistered).
    - Every row guarded by `WHERE NOT EXISTS` on politician_id (idempotent).
    - photo_license set per row (may differ across the 7).
  </acceptance_criteria>
  <done>Audit-only migration authored with 7 idempotent politician_images rows bound to Plan 01 UUIDs, per-image license, unregistered.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Resolve source URLs via Playwright + run pipeline + apply audit migration + assert 7/7 (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator resolves URLs, runs an existing script, applies an existing migration; no files modified</files>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-VALIDATION.md (Per-Task Verification Map — headshots row: count=7 + PIL 600×750 + curl CDN 200)
    - .planning/phases/196-marana-deep-seed/196-RESEARCH.md (§ Common Pitfall 5 — WAF + Playwright fallback + Ballotpedia-first for Post/Kai/Officer/Murphy + MIN_DIM floor + per-image license)
    - C:/Users/Chris/.claude/commands/find-headshots.md (the Playwright browser-navigation sourcing flow for a WAF-blocked host)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/Storage/web access). This is a BLOCKING apply-and-assert step:
    Plan 04's coverage chip depends on these rows only indirectly, but Plan 02 is not complete (and Plan
    03 should not proceed) until 7/7 images serve from the CDN. First, for each of the 7 CONFIRMED sitting
    officials (Task 2 roster), resolve a usable direct image URL from a NON-WAF host using the
    `/find-headshots` Playwright flow (`mcp__playwright__browser_navigate` + snapshot + image-URL
    extraction) — check Ballotpedia candidate pages FIRST for Post/Kai/Officer/Murphy, then Wikipedia /
    press coverage for the others. Follow the project headshot rules (600×750 4:5, Lanczos, crop-only,
    eyes ~1/3 from top, no graphics on faces, no distortion). Do NOT fetch maranaaz.gov (WAF 403) and do
    NOT spoof headers to bypass a WAF. Record the resolved URL + license per official, fill them into the
    ROSTER `photo_url` / `photo_license` fields, and finalize the matching photo_license values in
    migration 1346.

    Then run `py -3 C:/EV-Accounts/backend/scripts/_tmp-marana-headshots.py` — it crops/resizes/uploads
    all 7 processed images to Storage and prints the {external_id, uuid, cdn_url, photo_license} manifest.
    If any REQUIRED member cannot be sourced (script exits 1), PAUSE for an operator-supplied file
    (photo_license='operator_supplied'). Then apply the audit migration via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1346_town_of_marana_headshots.sql`. Assert:
    politician_images count for the 7 ext_ids = 7; `curl -sI` each CDN url → HTTP 200; PIL verify a sample
    (include Mayor Post and one council member) is exactly 600×750. Record results + final manifest (incl.
    per-image license) in 196-02-SUMMARY.md. Commit the audit migration via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (-4013001,-4013002,-4013003,-4013004,-4013005,-4013006,-4013007)"</automated>
  </verify>
  <acceptance_criteria>
    - Each official's source URL resolved from a NON-WAF host via Playwright (Ballotpedia-first for Post/Kai/Officer/Murphy; no maranaaz.gov fetch, no header-spoofing); per-image license recorded.
    - Script run exits 0 (all 7 required images fetched, processed, uploaded) — or an operator file was supplied for any exception.
    - `politician_images` count for the 7 Marana ext_ids (-4013001..-4013007) = 7.
    - `curl -sI` on each of the 7 CDN URLs returns HTTP 200.
    - PIL dimension check on a sampled downloaded CDN image (incl. Post + one council member) = exactly 600×750.
    - Migration 1346's photo_license values match the actual sourced licenses; audit migration NOT registered in the ledger; committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <done>All 7 officials serve a 600×750 CDN headshot bound to the correct current politician row, sourced from non-WAF hosts with per-image licenses; audit migration applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-WAF image hosts (remote) → pipeline | Untrusted remote image bytes enter the pipeline over HTTP |
| pipeline → Storage/DB | Processed image + politician_images row cross into production |

## STRIDE Threat Register

Honest scope: an image-processing pipeline + audit-only INSERTs authored by the operator. Genuine
threats are payload-in-image and wrong-person binding — not injection or access control. No HIGH-severity
threat; none fabricated.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-IMG | Tampering | remote image fetch | mitigate | PIL `convert('RGB')` + JPEG re-encode `optimize=True` strips EXIF/embedded/stego payloads; only `image/jpeg` written to Storage |
| T-196-WAF | Tampering | WAF-evasion for a `.gov` host | mitigate | Use the sanctioned Playwright browser-navigation flow (real browser context) rather than crafting evasive request headers against maranaaz.gov; grep gate forbids a maranaaz.gov URL in the script |
| T-196-BIND | Spoofing | politician_images ↔ politician binding | mitigate | Fetch ONLY the Task-2-confirmed current-roster URLs (never the FORMER member Jackie Craig); runtime UUID resolution by external_id prevents wrong-person binding on retry |
| T-196-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; script itself gitignored (`backend/scripts/_*`); never in a committed migration; parameterized `external_id=%s` query |
| T-196-LIC | Repudiation | image license provenance | mitigate | photo_license recorded PER image (Wikimedia/CC terms, Ballotpedia/campaign press_use, operator_supplied); documented in the migration header + SUMMARY manifest |
| T-196-WRONG | Spoofing | wrong-but-present image | transfer | Row-count cannot detect a wrong-but-present photo; deferred to a human identity spot-check at phase verification |
| T-196-SC | Tampering | package installs | accept | No new pip installs — reuses installed Pillow/requests/psycopg2 + already-available Playwright MCP; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 7/7 politician_images rows for the Marana ext_id block, each 600×750 at HTTP 200.
- Every image binds to the correct current official (Task 2 roster; never Jackie Craig).
- Source URLs from non-WAF hosts via Playwright (Ballotpedia-first); per-image license recorded; audit migration NOT registered.
</verification>

<success_criteria>
SUB-02 / ROADMAP #2 (all seated officials have 600×750 headshots) is TRUE in production; every image
binds to the correct current member; the audit migration is applied and left unregistered.
</success_criteria>

<output>
Create `.planning/phases/196-marana-deep-seed/196-02-SUMMARY.md` when done — include the final
{external_id → UUID → cdn_url → photo_license} manifest and any operator-supplied exceptions.
</output>
