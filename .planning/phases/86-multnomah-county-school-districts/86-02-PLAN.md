---
phase: 86-multnomah-county-school-districts
plan: 02
type: execute
wave: 2
depends_on: [86-01]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py
  - C:/EV-Accounts/backend/migrations/255_or_school_headshots.sql
autonomous: true
requirements: [OR-SCHOOL-04]

must_haves:
  truths:
    - "Each of the 38 board members (external_ids -860001..-860055) has either (a) a politician_images row with type='default' pointing at a 600×750 JPEG hosted at the Supabase Storage public URL `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{politician_id}-headshot.jpg`, OR (b) is explicitly documented in migration 255 as 'No photo found on official district website' with no politician_images row inserted (D-15, D-17)"
    - "Every uploaded headshot is a 4:5-cropped image resized to exactly 600×750 pixels via Lanczos resampling at q90 — verified by either PIL `Image.open(...).size == (600, 750)` after download from Storage, OR by trust of the upload script's resize step which uses these exact parameters (D-16)"
    - "Every politician_images row inserted by Plan 02 has `type='default'` (NOT 'headshot') so the UI's `.find(img => img.type === 'default')` filter resolves it (Pitfall 'politician_images.type must be default' from STATE.md)"
    - "Migration 255 is AUDIT-ONLY — opens with a DO block that RAISE EXCEPTION on bare apply; no Supabase MCP ledger entry attempted; matches the pattern of migrations 245 and 247 (D-17)"
    - "Migration 255 is saved as UTF-8 and includes Unicode names in comments where applicable (Aaron Muñoz, Ana Gonzalez Muñoz, José Gamero-Georgeson)"
    - "Headshot photos must NOT have superimposed text or graphics over the face (e.g. 'Re-Elect' banners) — verified by human visual review during processing (memory feedback_headshot_no_graphics.md)"
    - "Eyes are positioned at roughly 1/3 from the top of the frame; full head + shoulders visible; aspect ratio is 4:5 from the source crop (NEVER stretched to fit 600×750 directly) (memory feedback_headshot_cropping.md, feedback_headshot_resize_no_distort.md)"
    - "Each headshot is sourced from the district's OFFICIAL website only — no LinkedIn, Twitter/X, Facebook, local news, or other third-party sources (D-15)"
    - "Photos that are square or wrong aspect ratio MUST be center-cropped (square sources) or top-cropped (tall sources) to 4:5 first, then resized to 600×750 (memory feedback_headshot_resize_no_distort.md)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py"
      provides: "Python PIL + Supabase Storage upload script — downloads source headshots, crops to 4:5, resizes to 600×750 Lanczos q90, uploads to politician_photos bucket"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/255_or_school_headshots.sql"
      provides: "AUDIT-ONLY documentation of 38 politician_images live inserts (or 'No photo' notes) — one block per board member, grouped by district"
      contains: "AUDIT-ONLY"
      min_lines: 250
  key_links:
    - from: "essentials.politician_images.url"
      to: "Supabase Storage politician_photos/{politician_id}-headshot.jpg"
      via: "live INSERTs performed by _tmp-or-school-headshots.py against production DB; documented retrospectively in migration 255"
      pattern: "kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos"
    - from: "essentials.politician_images.politician_id"
      to: "essentials.politicians.id (external_id BETWEEN -860055 AND -860001)"
      via: "FK; resolved via SELECT id FROM essentials.politicians WHERE external_id = <N>"
      pattern: "politician_id = \\(SELECT id FROM essentials.politicians WHERE external_id = -8600[0-9][0-9]\\)"
    - from: "essentials.politician_images.type='default'"
      to: "Frontend Profile.jsx / Results.jsx `.find(img => img.type === 'default')`"
      via: "UI image selection filter — wrong type = silent invisibility"
      pattern: "type = 'default'"
---

<objective>
Source, process, and upload headshots for all 38 Multnomah County school board members (38 politicians, external_ids -860001..-860055 across 6 districts), then write migration 255 as an audit-only SQL file documenting every INSERT that was performed live against essentials.politician_images.

Per D-15, sources are restricted to each district's OFFICIAL website. Per D-16, all images are processed to 600×750 JPEG via 4:5 crop first then Lanczos resize at q90. Per D-17 (and STATE.md precedent for migrations 245 + 247), the SQL file is AUDIT-ONLY: it documents what was done live but does not get applied via the Supabase ledger.

Purpose: OR-SCHOOL-04 ("Board member headshots at 600×750 where available online; documented gaps noted"). RESEARCH.md confirms all 6 districts have photos for all 38 members on their official websites, though Drupal itok tokens (Reynolds) and Finalsite UUIDs (PPS, Riverdale) must be fetched per-member at execution time.

Output: 38 politician_images rows inserted live to production (or per-member 'No photo' documentation if a source becomes unavailable at execution time); migration 255 committed to the repo as a retrospective audit log.

NOTE ON MIGRATION NUMBERING: RESEARCH.md and CONTEXT.md reference "migration 254" for headshots. Because Plan 01 uses migration 254 (research 253 was already taken by `253_fix_ca_legislature_orphan_context_rows.sql`), this plan's audit migration is **255**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-multnomah-county-school-districts/86-CONTEXT.md
@.planning/phases/86-multnomah-county-school-districts/86-RESEARCH.md
@.planning/phases/86-multnomah-county-school-districts/86-PATTERNS.md
@.planning/phases/86-multnomah-county-school-districts/86-01-SUMMARY.md

# Reference migrations (audit-only headshot patterns — source-of-truth)
@C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql
@C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write _tmp-or-school-headshots.py upload script + capture per-member source URLs</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py</files>
  <read_first>
    - .planning/phases/86-multnomah-county-school-districts/86-RESEARCH.md (D-15 headshot availability summary table per district; per-district URL patterns: PPS Finalsite UUID pattern, Parkrose direct `/images/about/school_board/[filename].jpg`, Reynolds Drupal `?itok=` tokenized paths, Centennial Finalsite-like CMS, David Douglas WordPress `/wp-content/uploads/`, Riverdale Finalsite; Pitfall 5 Unicode names; "Open Question 2" Finalsite UUID capture)
    - .planning/phases/86-multnomah-county-school-districts/86-01-SUMMARY.md (the 38-politician roster table — read to capture politician_id UUIDs after Plan 01 applied)
    - C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql (canonical audit-only migration pattern showing the politician_images.url shape — Supabase Storage public URL with {politician_id}-headshot.jpg suffix)
    - Project memory references already in scope: feedback_headshot_image_sizing.md (600×750 LANCZOS q90); feedback_headshot_cropping.md (eyes ~1/3 from top, full head + shoulders, never change aspect ratio); feedback_headshot_resize_no_distort.md (crop 4:5 first then resize, never stretch); feedback_headshot_no_graphics.md (no superimposed text/banners)
  </read_first>
  <action>
Create a Python upload helper at C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py that processes and uploads all 38 board member headshots to Supabase Storage. The leading underscore in the filename signals "temporary helper" per established convention (Phase 66/77/84 used the same pattern, e.g. sac_headshots.sql + helper py).

REQUIRED IMPORTS: os, sys, io, requests, PIL.Image (from Pillow), psycopg2 (or supabase Python client — choose whichever is already in C:/EV-Accounts/backend's Python env).

REQUIRED CONFIG:
- SUPABASE_URL = read from environment (already set in repo .env): https://kxsdzaojfaibhuzmclfq.supabase.co
- SUPABASE_SERVICE_KEY = read from env (do NOT log)
- DATABASE_URL = read from env
- BUCKET = 'politician_photos'
- TARGET_SIZE = (600, 750)
- JPEG_QUALITY = 90
- RESAMPLE = PIL.Image.Resampling.LANCZOS

REQUIRED ROSTER STRUCTURE (in-script Python dict, one entry per member, exactly these 38 external_ids):

PORTLAND PUBLIC SCHOOLS (Finalsite — UUIDs must be fetched per-member from https://www.pps.net/board/board-of-education/board-members at execution time; record each fetched URL in the script and in the SUMMARY):
- -860001 Edward Wang (Chair, Zone 7)
- -860002 Michelle DePass (Vice-Chair, Zone 2)
- -860003 Christy Splitt (Zone 1)
- -860004 Patte Sullivan (Zone 3)
- -860005 Rashelle Chase-Miller (Zone 4)
- -860006 Virginia La Forte (Zone 5)
- -860007 Stephanie Engelsman (Zone 6)

PARKROSE (https://www.parkrose.com/school-board — direct paths under /images/about/school_board/):
- -860011 Paul Tabron Jr.
- -860012 Brenda Rivas
- -860013 Joash Bullock
- -860014 Adolfo Jimenez
- -860015 Mariah Galaviz

REYNOLDS (https://www.reynolds.k12.or.us/schoolboard — Drupal itok tokens, must visit each member's profile page e.g. /schoolboard/michael-reyes and capture the live URL with its current ?itok= parameter):
- -860021 Aaron Muñoz
- -860022 Joyce Rosenau
- -860023 Michael Reyes
- -860024 Cayle Tern
- -860025 Patty Carrera
- -860026 Ana Gonzalez Muñoz
- -860027 Francisco Ibarra

CENTENNIAL (https://csd28j.org/boardmembers — Finalsite-like CMS, visit per-member to capture URLs):
- -860031 David Linn
- -860032 Ronald Hardin
- -860033 Will Mohring
- -860034 Melissa Standley
- -860035 Rose Solowski
- -860036 Michael Newman
- -860037 Pam Shields

DAVID DOUGLAS (https://www.ddouglas.k12.or.us/school-board/board-members/ — WordPress /wp-content/uploads/<YEAR>/<MONTH>/<FILE>.jpg):
- -860041 Althea Ender
- -860042 Stephanie Stephens
- -860043 Sara Epstein
- -860044 Muriel Jordan
- -860045 Thomas Stephenson
- -860046 Heather Franklin
- -860047 José Gamero-Georgeson

RIVERDALE (https://www.riverdaleschool.com/about-us/school-board-policy — Finalsite, UUIDs per member):
- -860051 Shaina Weinstein
- -860052 Mina Stricklin
- -860053 Michele Rosenbaum
- -860054 Ali Lanenga
- -860055 Milessa Lowrie

REQUIRED FUNCTIONS:
1. `fetch_politician_id(cursor, external_id) -> str (UUID)` — SELECT id FROM essentials.politicians WHERE external_id = %s; returns the UUID string.
2. `download_image(url) -> PIL.Image` — uses requests.get with a 30s timeout and a browser-like User-Agent header (some district sites 403 default requests UA, see Fremont gov 403 workaround memory); fails loudly with the source URL if HTTP error.
3. `crop_to_4_5(img) -> PIL.Image` — Determines source aspect ratio (w/h). If exactly 4:5 already, return as-is. If wider than 4:5 (landscape or square), center-crop to 4:5 (preserve full height, crop sides). If taller than 4:5 (portrait taller), top-crop to 4:5 (preserve top to position eyes at ~1/3 — DO NOT center-crop tall sources because that crops out the head; per memory feedback_headshot_cropping.md). Return the cropped image.
4. `resize_600x750(img) -> PIL.Image` — img.resize(TARGET_SIZE, RESAMPLE). Returns the resized image.
5. `upload_to_storage(politician_uuid, jpeg_bytes) -> str (public URL)` — PUT/upsert to bucket politician_photos at path `{politician_uuid}-headshot.jpg` with content-type image/jpeg; returns the public URL `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{politician_uuid}-headshot.jpg`.
6. `insert_politician_images_row(cursor, politician_uuid, url)` — INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) VALUES (gen_random_uuid(), %s, %s, 'default', 'public_domain') ... use a NOT EXISTS guard. type MUST be the literal string 'default' (Pitfall: type='default').
7. `process_member(cursor, external_id, full_name, source_url, district_label)` — wraps fetch + download + crop + resize + upload + insert; logs at each step; on any exception, log the exception and continue to next member (do NOT abort the whole run).
8. `main()` — opens psycopg2 connection (or equivalent), iterates the ROSTER list, calls process_member for each. At end, prints a summary table: 38 attempted, N succeeded, N skipped with reason.

EXECUTION SEQUENCE:
a) Set up env: confirm SUPABASE_URL, SUPABASE_SERVICE_KEY, DATABASE_URL are set.
b) Build the ROSTER as a Python list-of-dicts. Each entry has external_id (int), full_name (str), source_url (str — left as empty string for the 4 districts where per-member URL discovery happens at runtime), district_label (str), title (str — for migration 255 comment).
c) For PPS, Reynolds, Centennial, Riverdale: visit each member's per-member profile page to discover the live source_url (Finalsite UUID or Drupal itok token). For Parkrose + David Douglas: source URLs are predictable from the school-board page and can be hardcoded after a single fetch.
d) Run the script. Streaming output goes to stdout; the SUMMARY at the end lists every external_id with source_url + final Storage URL OR skip reason.
e) Capture stdout to a log file for inclusion in 86-02-SUMMARY.md.

CRITICAL CONSTRAINTS:
- type column = 'default' (Pitfall from STATE.md).
- photo_license = 'public_domain' (matches Sacramento Phase 66 / Multnomah Phase 84 pattern).
- NEVER stretch a source image to 600×750 — crop to 4:5 FIRST then resize (feedback_headshot_resize_no_distort.md).
- Eyes at ~1/3 from top — top-crop tall portraits, do NOT center-crop them.
- Reject any source with banner/text overlay over the face (per memory feedback_headshot_no_graphics.md — if a source has a "Re-Elect" banner or similar over the face, document as 'No photo (banner-only source)' and do NOT upload).
- Use a browser User-Agent header on all district GETs — district sites (e.g. Fremont) have 403'd default requests UA in prior phases.

After the script runs successfully, DO NOT commit the source images locally — they live only in Supabase Storage. The script itself goes in the repo (with the underscore prefix marking it as a one-time helper).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; python -c "import ast; tree = ast.parse(open('scripts/_tmp-or-school-headshots.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; required = ['crop_to_4_5','resize_600x750','upload_to_storage','insert_politician_images_row','process_member','main']; missing = [r for r in required if r not in funcs]; print('MISSING:' if missing else 'OK:', missing or funcs)" | grep -q "^OK:"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: _tmp-or-school-headshots.py exists in C:/EV-Accounts/backend/scripts/
    - SOURCE: file contains all 6 required Python function definitions: `def crop_to_4_5(`, `def resize_600x750(`, `def upload_to_storage(`, `def insert_politician_images_row(`, `def process_member(`, `def main(`
    - SOURCE: file contains `TARGET_SIZE = (600, 750)` and `JPEG_QUALITY = 90`
    - SOURCE: file references `Image.Resampling.LANCZOS` or `Image.LANCZOS`
    - SOURCE: file contains the literal string `'default'` used for the type column (NOT `'headshot'`)
    - SOURCE: file contains all 38 external_ids in the ROSTER (verify: -860001, -860002, ..., -860007, -860011..-860015, -860021..-860027, -860031..-860037, -860041..-860047, -860051..-860055 each appear at least once)
    - SOURCE: file contains Unicode UTF-8 characters `ñ` and `é` in the roster strings (Aaron Muñoz, Ana Gonzalez Muñoz, José Gamero-Georgeson)
    - SOURCE: file contains a browser-like User-Agent string in requests.get headers (e.g. `'User-Agent': 'Mozilla/5.0'` or similar)
    - SOURCE: file contains the bucket name string `'politician_photos'`
    - BEHAVIOR (after live run): production DB query `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -860055 AND -860001 AND pi.type = 'default'` returns ≥ 33 (allow up to 5 documented gaps; D-15 says check official site only — if some sites are unreachable at runtime, documented gaps are acceptable)
    - BEHAVIOR (after live run): every uploaded Storage object at `politician_photos/{uuid}-headshot.jpg` is a 600×750 JPEG — verified by downloading any one of the 38 (e.g. Edward Wang) and confirming `PIL.Image.open(bytes).size == (600, 750)` and format == 'JPEG'
  </acceptance_criteria>
  <done>
_tmp-or-school-headshots.py committed; live run completed; stdout log captured for the SUMMARY. Storage bucket contains 33–38 new {uuid}-headshot.jpg objects, each 600×750 JPEG. politician_images table has matching `type='default'` rows for all uploaded members. Any documented 'No photo' cases are noted with reason.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 255 (AUDIT-ONLY) documenting all 38 politician_images inserts</name>
  <files>C:/EV-Accounts/backend/migrations/255_or_school_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql (canonical multi-district audit-only headshot migration — copy file header structure, safety guard, per-member INSERT block pattern with NOT EXISTS, 'No photo found' comment pattern, summary block; verify file STARTS with the RAISE EXCEPTION guard)
    - C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql (single-government audit-only headshot migration pattern — second analog)
    - .planning/phases/86-multnomah-county-school-districts/86-PATTERNS.md (`migrations/254_or_school_headshots.sql` section — note PATTERNS.md says 254 but actual is 255; copy the file header safety guard and INSERT-per-official block structure; the 38-official assignment table)
    - C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py log output from Task 1 (captures actual source URLs used and which members succeeded vs were documented as gaps)
  </read_first>
  <action>
Write migration 255 at C:/EV-Accounts/backend/migrations/255_or_school_headshots.sql as an AUDIT-ONLY record of the live politician_images writes performed by _tmp-or-school-headshots.py in Task 1. Per D-17 the file MUST NOT be applied — opening DO block raises an exception.

FILE STRUCTURE (mirror 247_multnomah_cities_headshots.sql exactly):

1. HEADER COMMENT BLOCK (lines 1-25):
```
-- Migration 255: OR School District Board Member Headshots
-- AUDIT-ONLY: captures the live politician_images INSERTs performed during Phase 86-02
-- execution on <YYYY-MM-DD>.
-- DO NOT apply via Supabase ledger -- actual DB writes happened live via
-- scripts/_tmp-or-school-headshots.py (Python PIL + Supabase Storage API).
-- Pattern matches 247_multnomah_cities_headshots.sql / 245_multnomah_county_headshots.sql.
--
-- 38 officials documented across 6 districts:
--   PORTLAND PUBLIC SCHOOLS (7 officials, external_ids -860001..-860007)
--   PARKROSE (5 officials, external_ids -860011..-860015)
--   REYNOLDS (7 officials, external_ids -860021..-860027)
--   CENTENNIAL (7 officials, external_ids -860031..-860037)
--   DAVID DOUGLAS (7 officials, external_ids -860041..-860047)
--   RIVERDALE (5 officials, external_ids -860051..-860055)
--
-- Photo processing: crop to 4:5 first, then resize 600x750 Lanczos q90.
-- Storage bucket: politician_photos; path: {politician_id}-headshot.jpg.
-- politician_images.type = 'default' (UI filter: .find(img => img.type === 'default')).
```

2. SAFETY GUARD (first DO block — file MUST fail loudly on bare apply):
```sql
DO $$
BEGIN
  RAISE EXCEPTION 'Migration 255 is AUDIT-ONLY and must not be applied. Actual DB writes happened live via scripts/_tmp-or-school-headshots.py.';
END $$;
```

3. PER-DISTRICT SECTIONS — one block per official, in external_id order. For each official with a photo (per Task 1's log):
```
-- ====================== PORTLAND PUBLIC SCHOOLS (PPS) ======================
-- Edward Wang (-860001) — Board Member (Zone 7), Chair
-- source: <actual source URL captured during Task 1 — e.g. https://ppsnet.finalsite.com/fs/resource-manager/view/<UUID>>
INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license)
SELECT gen_random_uuid(),
       (SELECT id FROM essentials.politicians WHERE external_id = -860001),
       'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/' ||
         (SELECT id FROM essentials.politicians WHERE external_id = -860001)::text || '-headshot.jpg',
       'default', 'public_domain'
WHERE NOT EXISTS (
  SELECT 1 FROM essentials.politician_images
  WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -860001)
);
```
   For each official WITHOUT a photo (Task 1's log marked them as gap):
```
-- Edward Wang (-860001) — Board Member (Zone 7), Chair
-- No photo found on official district website (www.pps.net).
-- No politician_images row inserted.
```

   Use the actual Storage URL captured by the script (which contains the resolved politician_id UUID). DO NOT use a literal `{uuid}` placeholder — substitute the real UUID, or use the subquery form `... || (SELECT id FROM ...)::text || '-headshot.jpg'` as shown above so the audit migration is self-documenting and self-resolving.

4. ORDER (38 blocks total, organized by district section with banner comments between sections):
   Section 1: PPS — -860001, -860002, -860003, -860004, -860005, -860006, -860007 (7 blocks)
   Section 2: PARKROSE — -860011, -860012, -860013, -860014, -860015 (5 blocks)
   Section 3: REYNOLDS — -860021..-860027 (7 blocks; preserve Unicode ñ in Aaron Muñoz, Ana Gonzalez Muñoz comments)
   Section 4: CENTENNIAL — -860031..-860037 (7 blocks)
   Section 5: DAVID DOUGLAS — -860041..-860047 (7 blocks; preserve Unicode é in José Gamero-Georgeson comment)
   Section 6: RIVERDALE — -860051..-860055 (5 blocks)

5. SUMMARY BLOCK at end of file (mirror 247_multnomah_cities_headshots.sql lines ~323-327):
```
-- =============== SUMMARY ===============
-- Total officials: 38
-- Headshots uploaded: <N> (PPS <a>, Parkrose <b>, Reynolds <c>, Centennial <d>, David Douglas <e>, Riverdale <f>)
-- No photo documented: <38-N> (with per-district breakdown)
-- =====================================
```
   The N values come from Task 1's execution log — replace <N>, <a>..<f> with actual counts.

6. NO LEDGER ENTRY. NO COMMIT. The file is audit-only and ends with the SUMMARY comment block.

CRITICAL CONSTRAINTS:
- File MUST be saved as UTF-8 (preserves ñ and é).
- The opening `RAISE EXCEPTION` guard is mandatory — proves the file is not accidentally applied.
- type column MUST be the literal string `'default'` (single quotes inside the SELECT).
- photo_license MUST be `'public_domain'` (matches Sacramento + Multnomah county/cities convention).
- DO NOT include a `INSERT INTO supabase_migrations.schema_migrations` ledger row — that would mark a non-existent migration as applied.
- DO NOT include `BEGIN;` / `COMMIT;` wrappers — audit-only migrations are not transactional from the ledger's perspective (matches 245 + 247 pattern; both end without COMMIT).
- For any documented gap, the comment block MUST identify the official site domain that was checked (e.g. `(checked www.parkrose.com)`) to satisfy D-15's "official website only" requirement traceability.

After writing the file, DO NOT attempt to apply it. Verify by running `psql ... -f migrations/255_or_school_headshots.sql` against a NON-PRODUCTION DB only — it MUST raise the exception immediately. (If a non-prod DB is unavailable, skip this and rely on visual inspection that the safety guard is the first executable statement.)
  </action>
  <verify>
    <automated>grep -c "Migration 255 is AUDIT-ONLY and must not be applied" "/c/EV-Accounts/backend/migrations/255_or_school_headshots.sql" | grep -q "^1$" &amp;&amp; grep -v '^--' "/c/EV-Accounts/backend/migrations/255_or_school_headshots.sql" | grep -E "WHERE external_id = -8600(0[1-7]|1[1-5]|2[1-7]|3[1-7]|4[1-7]|5[1-5])" | sort -u | wc -l | grep -qE "^(33|34|35|36|37|38)$"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: migrations/255_or_school_headshots.sql exists in C:/EV-Accounts/backend/migrations/
    - SOURCE: file's first non-comment statement is the RAISE EXCEPTION DO block with the literal text `Migration 255 is AUDIT-ONLY and must not be applied`
    - SOURCE: file does NOT contain `INSERT INTO supabase_migrations.schema_migrations` (audit-only — no ledger entry)
    - SOURCE: file does NOT contain a `BEGIN;` followed by `COMMIT;` wrapper around the inserts
    - SOURCE: file contains 6 district section banner comments (PPS / Parkrose / Reynolds / Centennial / David Douglas / Riverdale)
    - SOURCE: file contains references to all 38 external_ids in the -860001..-860055 range when filtering out comments (verify with grep that strips `^--` lines)
    - SOURCE: file contains `'default'` as the type column value and `'public_domain'` as photo_license
    - SOURCE: file contains UTF-8 Unicode `ñ` and `é` in the comment lines for Reynolds and David Douglas members
    - SOURCE: file contains the SUMMARY block at the end with `Total officials: 38` and actual breakdown numbers per district
    - SOURCE: file does NOT contain the literal placeholder string `{uuid}` (every Storage URL is either a real UUID literal OR a `(SELECT id FROM essentials.politicians WHERE external_id = -8600NN)::text` subquery)
    - SOURCE: file does NOT contain `type = 'headshot'` (Pitfall: must be 'default' not 'headshot')
    - BEHAVIOR: `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '255'` returns 0 rows (audit-only — never recorded as applied)
    - BEHAVIOR: `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -860055 AND -860001 AND pi.type = 'default'` matches the SUMMARY count claimed in the file
  </acceptance_criteria>
  <done>
Migration 255 committed as a UTF-8 SQL file with the AUDIT-ONLY safety guard, 6 district sections, 38 documented officials (mix of INSERT blocks and 'No photo' comments depending on Task 1's outcome), and an accurate SUMMARY block. The file is NOT applied via the ledger. Re-reading the file in a non-prod psql session raises the expected exception on the first DO block.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| District websites → Python script (HTTPS download) | External fetch of board member photos from 6 separate district CDNs (Finalsite, Drupal, WordPress, custom CMS) |
| Python script → Supabase Storage (HTTPS upload) | Authenticated upload using SUPABASE_SERVICE_KEY to the politician_photos bucket |
| Python script → essentials.politician_images (DB write) | Direct INSERT using DATABASE_URL credentials |
| Migration 255 file → repo (audit artifact) | Static SQL file checked into the repo; never applied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-08 | Tampering | Headshot image bytes during download | mitigate | All sources are HTTPS; per-member URL must come from each district's OFFICIAL website (D-15 — no LinkedIn / news / social); banner-overlay / "Re-Elect" graphic sources are rejected during human visual review (feedback_headshot_no_graphics.md); EXIF stripped via PIL re-save as JPEG |
| T-86-09 | Information Disclosure | SUPABASE_SERVICE_KEY in logs | mitigate | Script reads service key from `os.environ` but never prints it; verbose mode only logs URLs and external_ids, not env values |
| T-86-10 | Tampering | politician_images.url pointing at attacker-controlled CDN | mitigate | url column is constructed from Supabase Storage public URL only (kxsdzaojfaibhuzmclfq.storage.supabase.co); never stores the upstream district CDN URL; upload step is atomic — UUID-named object in our own bucket |
| T-86-11 | Repudiation | Live DB writes without ledger entry | mitigate | Migration 255 is the audit log; checked into the repo with district section banners and per-member source URL comments providing a permanent record of which official site was the source for each upload |
| T-86-12 | Denial of Service | District site rate-limits Python script | accept | Phase 86 is a one-time bulk run; ~38 sequential downloads with a normal User-Agent; risk is bounded; if 429s occur the script logs and skips that member to a documented gap rather than retrying aggressively |
| T-86-13 | Elevation of Privilege | None | accept | No new endpoints, no role changes; politician_images write requires DATABASE_URL which already grants schema-level access for migrations |
| T-86-14 | Spoofing | District CDN serves photo of wrong person | mitigate | Per-member URL must be discovered by visiting the official board-members listing page (not constructed); human verification step during Task 1 confirms photo matches member name before upload (matches Phase 84 + Phase 77 workflows) |
| T-86-SC | Tampering | npm/pip installs | accept | No new pip packages added — `Pillow`, `requests`, `psycopg2` already present in the C:/EV-Accounts/backend Python env (used by Phase 66 / 84 / 77 headshot scripts); no new npm installs (this plan touches no Node code); no Package Legitimacy Audit required |
</threat_model>

<verification>
The verification gates for this plan run in two layers:

1. **Static file checks on migration 255** — safety guard present, no ledger row, no BEGIN/COMMIT, 38 external_ids covered, UTF-8 preserved, correct type/photo_license literals.
2. **Live DB + Storage state checks** — after Task 1's script ran, the politician_images table has matching `type='default'` rows for every uploaded member, the Storage bucket has matching `{uuid}-headshot.jpg` objects at 600×750 JPEG, and the SUMMARY count in migration 255 matches the live DB count.

End-state SQL gates:
- `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -860055 AND -860001 AND pi.type = 'default'` ∈ [33, 38] (allowing up to 5 documented gaps per D-15)
- `SELECT COUNT(DISTINCT pi.url) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -860055 AND -860001 AND pi.url LIKE '%politician_photos/%-headshot.jpg'` equals the above COUNT (every uploaded headshot has a unique URL)
- `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '255'` returns 0 rows (audit migration not applied)

Manual visual gates (per phase memory):
- Photo aspect ratio is 4:5 (eyes ~1/3 from top, full head + shoulders, never stretched).
- Photo has no superimposed text/banner over the face.
- Photo is sourced from each district's official website only.
</verification>

<success_criteria>
Plan 02 is COMPLETE when ALL of the following are true:

1. `C:/EV-Accounts/backend/scripts/_tmp-or-school-headshots.py` exists and was successfully run live (stdout log captured)
2. Production DB query `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id = pi.politician_id WHERE p.external_id BETWEEN -860055 AND -860001 AND pi.type = 'default'` returns between 33 and 38 (38 ideal; up to 5 documented gaps acceptable per D-15)
3. Supabase Storage `politician_photos/` bucket contains a matching `{uuid}-headshot.jpg` object for every politician_images row inserted; spot-check at least one (e.g. -860001 Edward Wang) confirms 600×750 JPEG via PIL inspection
4. `C:/EV-Accounts/backend/migrations/255_or_school_headshots.sql` exists, opens with the AUDIT-ONLY safety guard, includes all 38 officials in 6 district sections, accurate SUMMARY counts, and was NEVER written into the ledger
5. Visual review confirms each uploaded photo: official-website-sourced, 4:5 cropped, no banner/text overlay, eyes ~1/3 from top
6. OR-SCHOOL-04 marked verifiable in 86-VERIFICATION.md (created by execute-phase)
7. Phase 86 overall closed: all 4 requirements OR-SCHOOL-01..04 PASS; STATE.md and ROADMAP.md updated by execute-phase post-flow
</success_criteria>

<output>
Create `.planning/phases/86-multnomah-county-school-districts/86-02-SUMMARY.md` when done.

Required sections:
- Files modified (with absolute paths + commit SHA)
- Python script run output (per-member success/skip log)
- Per-district headshot count table (PPS X/7, Parkrose X/5, Reynolds X/7, Centennial X/7, David Douglas X/7, Riverdale X/5; total X/38)
- Storage bucket sample verification (one downloaded object confirmed 600×750 JPEG)
- Migration 255 file size + first-line safety-guard confirmation
- Any documented gaps with reason (e.g. "Patte Sullivan: Finalsite UUID page 404'd 2026-06-XX — no photo uploaded; documented in migration 255")
- Photo source URL audit (one column per member with the live district-CDN URL captured during Task 1)
- Confirmation that the live DB count matches the SUMMARY block at the end of migration 255
- Migration number discrepancy note: research said "254" for headshots, actual is "255" — record reasoning chain (253 taken → seed bumped to 254 → headshots bumped to 255)
</output>
