---
phase: 203-indio-deep-seed
plan: 05
type: execute
wave: 5
depends_on: ["203-04"]
files_modified:
  - src/lib/buildingImages.js
  - src/lib/coverage.js
autonomous: false
requirements: [BANR-01, CV-03]
must_haves:
  truths:
    - "A licensed, real (non-AI, non-aerial) Old Town / downtown Indio streetscape banner is processed to the banner spec and uploaded to Storage as politician_photos/cities/indio.jpg (D-01/D-02/D-03)"
    - "buildingImages.js CURATED_LOCAL has a NEW 'indio' key ({ state: 'CA', src: '.../cities/indio.jpg' }) inserted among the Coachella Valley CITY banners near 'palm springs' — Indio has NO pre-existing key (unlike Ph202's no-op)"
    - "coverage.js COVERAGE_STATES → California → areas[] has a DB-honest 'Indio' chip (hasContext:true, since stances landed in Plan 04) that browses geo_id 0636448, inserted alphabetically between 'Hawthorne' and 'Inglewood'"
    - "npm run build succeeds and the coverage generator sanity-checks the array shape"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "NEW CURATED_LOCAL 'indio' banner key → cities/indio.jpg"
      contains: "'indio'"
    - path: "src/lib/coverage.js"
      provides: "COVERAGE_STATES California 'Indio' chip, browseGovernmentList ['0636448'], browseStateAbbrev 'CA', hasContext:true"
      contains: "Indio"
  key_links:
    - from: "coverage.js 'Indio' chip"
      to: "buildingImages.js CURATED_LOCAL 'indio'"
      via: "browse_label 'Indio' → representingCity → getBuildingImages substring match"
      pattern: "'indio'"
    - from: "coverage.js 'Indio' chip"
      to: "essentials government geo_id 0636448"
      via: "browseGovernmentList ['0636448']"
      pattern: "0636448"
---

<objective>
Source, process, and upload a licensed Indio-specific community banner, then wire it into the app: add
a NEW `'indio'` CURATED_LOCAL key to `src/lib/buildingImages.js` and append a DB-honest `'Indio'` chip
to `src/lib/coverage.js`. This is the FULL BANR-01 deliverable for this phase — unlike Palm Springs
(Ph202), whose banner already shipped in Ph201 (making Ph202's coverage step a no-op), Indio has NO
existing banner asset and NO existing `'indio'` key (grep-confirmed in 203-PATTERNS.md). D-01 explicitly
requires an Indio-specific banner — do NOT reuse the Coachella Valley / Mission Inn banner.

Purpose: Satisfies ROADMAP success criterion #5 for CV-03 + BANR-01 (licensed community banner sourced;
city surfaced with a DB-honest chip). Banner subject preference (D-02): Old Town / downtown Indio
streetscape (reads as a city), date-palm / festival-heritage as an acceptable second choice; real photo
only, no AI, no aerial/satellite (D-03); sourced ONE AT A TIME (banner-sourcing quota rule).
Output: `politician_photos/cities/indio.jpg` in Storage + edits to `src/lib/buildingImages.js` and
`src/lib/coverage.js` in THIS repo (`main`→Render).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-indio-deep-seed/203-CONTEXT.md
@.planning/phases/203-indio-deep-seed/203-PATTERNS.md
@src/lib/buildingImages.js
@src/lib/coverage.js
@docs/banner-asset-pipeline.md

<execution_note>
Two distinct actors: (1) banner SOURCING + processing + Storage upload is ORCHESTRATOR/HUMAN work
(one candidate at a time, licensing + no-AI/no-aerial judgment, `scripts/banners/process_banner.py` +
`upload_banner.py`, Storage write) — the executor cannot source images or write Storage. (2) The
`buildingImages.js` + `coverage.js` edits + `npm run build` are FRONTEND changes in the CURRENT repo
(`essentials`, `main`→Render) that the executor CAN make. `hasContext:true` is correct ONLY because
Plan 04 already landed ≥1 stance row (depends_on 203-04). Storage overwrite is no-cache (instant
refresh — no cache-busting query string needed, per project_banner_redo_technique).
</execution_note>

<interfaces>
Key contracts (from 203-PATTERNS.md coverage + buildingImages blocks + the live files + CONTEXT.md D-01/02/03):
- buildingImages.js insertion (NEW key): inside CURATED_LOCAL, among the Coachella Valley CITY banners,
  immediately after the existing `'palm springs'` key (currently line 436). Exact entry:
  `'indio': { state: 'CA', src: 'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/indio.jpg' },`
  preceded by a one-line credit comment `//   indio         - <subject, credit> | <license>`. Key is
  single-word `'indio'` (no space). Longest-key-first matching already keeps 'riverside county' distinct
  from 'riverside'; 'indio' has no collision.
- coverage.js insertion: in COVERAGE_STATES → California → areas[], ALPHABETICALLY between
  `{ label: 'Hawthorne', browseGovernmentList: ['0632548'], ... }` (line 25) and
  `{ label: 'Inglewood', browseGovernmentList: ['0636546'], ... }` (line 26) — "Indio" sorts before
  "Inglewood" (Ind < Ing). Exact chip:
  `{ label: 'Indio', browseGovernmentList: ['0636448'], browseStateAbbrev: 'CA', hasContext: true },`
- Banner pipeline: `scripts/banners/process_banner.py` (crop/resize to banner spec) + `upload_banner.py`
  (Storage upload) per `docs/banner-asset-pipeline.md`; target key `cities/indio.jpg`.
- Storage CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- Sanity: `node scripts/gen-coverage.mjs` (prebuild generator) + `npm run build` (Vite).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Source + process + upload the Indio community banner (ORCHESTRATOR/HUMAN, one at a time)</name>
  <files>N/A — banner sourcing + scripts/banners processing + Storage upload; no repo file committed (asset lives in Storage as cities/indio.jpg)</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-CONTEXT.md (D-01 Indio-specific, do NOT reuse the CV/Mission Inn banner; D-02 Old Town/downtown streetscape preferred, date-palm/festival heritage second choice; D-03 real photo only, no AI, no aerial, source one at a time, CV reuse only as last resort)
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 398-418 — the NEW buildingImages key + banner-asset sourcing/processing spec; storage no-cache overwrite note)
    - docs/banner-asset-pipeline.md (process_banner.py + upload_banner.py mechanics, banner spec dimensions)
  </read_first>
  <what-built>
    A NEW licensed Indio community banner, sourced fresh for this phase (Indio has no existing banner
    asset). The frontend wiring (buildingImages.js key + coverage chip) lands in Task 2.
  </what-built>
  <how-to-verify>
    ORCHESTRATOR/OPERATOR steps (source ONE candidate at a time — big parallel fan-outs blow the session
    quota, per feedback_banner_source_one_at_a_time):
    1. Source a REAL, licensed Old Town / downtown Indio streetscape photo (reads clearly as a city).
       Acceptable second choice: date-palm / festival-heritage imagery. HARD constraints (D-03): no
       AI-generated imagery, no aerial/satellite. Prefer re-sourcing a real photo over falling back to a
       gradient (feedback_banner_prefer_photo_over_gradient); reuse the CV/Mission Inn banner only as a
       genuine last resort if nothing usable/licensable is found. Record subject, credit, and license.
    2. If the chosen Indio shot could visually conflict with the California STATE banner or the reused-CV
       banner, show the state/CV shot alongside the Indio candidate in the review so the operator can
       confirm they read as distinct (feedback_banner_show_state_shot_on_conflict).
    3. Process via `scripts/banners/process_banner.py` (crop/resize to the banner spec in
       docs/banner-asset-pipeline.md — real photo, level, ground-level street scene) then
       `scripts/banners/upload_banner.py` to `politician_photos/cities/indio.jpg` (storage no-cache
       overwrite; instant refresh).
    4. Confirm `curl -sI` on the CDN URL `.../politician_photos/cities/indio.jpg` returns HTTP 200 and the
       rendered image is the correct, non-distorted Indio street scene.
    Record the banner subject + credit + license + CDN 200 in 203-05-SUMMARY.md. If no licensable
    non-AI/non-aerial photo can be found after sourcing candidates one at a time, PAUSE and surface the
    CV-reuse last-resort decision.
  </how-to-verify>
  <verify>
    <automated>MISSING — orchestrator/human banner sourcing + Storage upload; no executor automated command (no image-sourcing / Storage access). Verified via `curl -sI cities/indio.jpg` HTTP 200 + subject/credit/license recorded in 203-05-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - A real, licensed Old Town/downtown Indio streetscape (or date-palm/festival-heritage second choice) sourced one at a time; NO AI, NO aerial; NOT the reused CV/Mission Inn banner (unless the documented last-resort applies).
    - Processed via scripts/banners/process_banner.py + upload_banner.py to politician_photos/cities/indio.jpg (no-cache overwrite).
    - `curl -sI` on the CDN URL returns HTTP 200; image renders correctly (not distorted, not a gradient).
    - Banner subject + photographer credit + license recorded in 203-05-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the Indio banner is uploaded (cities/indio.jpg, HTTP 200) and confirmed a correct, licensed, non-AI, non-aerial Indio street scene — or describe the sourcing issue / last-resort decision.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add the 'indio' buildingImages key + the Indio coverage chip + build sanity</name>
  <files>src/lib/buildingImages.js, src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/203-indio-deep-seed/203-PATTERNS.md (lines 375-418 — the coverage.js Hawthorne/Inglewood insertion point + exact chip; the NEW buildingImages 'indio' key shape + placement near 'palm springs'; hasContext-only-once-stances-exist rule)
    - src/lib/buildingImages.js (the CURATED_LOCAL Coachella Valley CITY banner block, ~lines 428-436 ending at the 'palm springs' key — insert 'indio' immediately after it)
    - src/lib/coverage.js (the live California areas[] array — confirm the exact Hawthorne/Inglewood gap and the surrounding chip shape before editing)
    - .planning/phases/203-indio-deep-seed/203-05-SUMMARY.md (the banner subject/credit/license from Task 1 for the buildingImages comment)
  </read_first>
  <action>
    (1) In `src/lib/buildingImages.js`, inside CURATED_LOCAL, insert a NEW key immediately AFTER the
    existing `'palm springs'` entry (among the Coachella Valley CITY banners): a one-line credit comment
    `//   indio         - <subject, credit from Task 1> | <license>` followed by
    `'indio': { state: 'CA', src: 'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/indio.jpg' },`.
    Match the surrounding entries' indentation and quoting style. Do NOT modify any other CURATED_LOCAL key.

    (2) In `src/lib/coverage.js`, inside COVERAGE_STATES California `areas[]`, insert exactly one new
    object ALPHABETICALLY between the existing `Hawthorne` entry and the `Inglewood` entry:
    `{ label: 'Indio', browseGovernmentList: ['0636448'], browseStateAbbrev: 'CA', hasContext: true },`.
    Match indentation/trailing-comma style. Set `hasContext: true` (Plan 04 already landed stance rows —
    a DB-honest purple chip). Do NOT edit any other coverage entry, do NOT add Indio to COVERAGE_COUNTIES.

    (3) Run `node scripts/gen-coverage.mjs` (prebuild array-shape sanity) and `npm run build` (Vite) to
    confirm no syntax error.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qF "'indio': { state: 'CA', src: 'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/indio.jpg' }" src/lib/buildingImages.js && grep -qF "{ label: 'Indio', browseGovernmentList: ['0636448'], browseStateAbbrev: 'CA', hasContext: true }" src/lib/coverage.js && node scripts/gen-coverage.mjs && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/buildingImages.js` contains a NEW `'indio'` CURATED_LOCAL key → cities/indio.jpg, placed after 'palm springs'; a credit comment precedes it; no other key modified.
    - `src/lib/coverage.js` contains exactly one `Indio` chip: browseGovernmentList ['0636448'], browseStateAbbrev 'CA', hasContext true, alphabetically between Hawthorne and Inglewood.
    - Indio NOT added to COVERAGE_COUNTIES; no other coverage entry modified.
    - `node scripts/gen-coverage.mjs` exits 0; `npm run build` succeeds.
  </acceptance_criteria>
  <done>NEW 'indio' banner key + DB-honest Indio coverage chip added (geo_id 0636448, hasContext:true); build + coverage generator green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image host (banner source, remote) → banner pipeline | Untrusted image bytes enter process_banner.py over HTTP |
| banner pipeline → Supabase Storage | Processed banner written to cities/indio.jpg with service-role |
| build-time static config → app render | The buildingImages key + coverage chip are consumed at build/render; no runtime user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-BANNER | Information Disclosure | wrong/AI/aerial/unlicensed banner | mitigate | Task 1 human-verify blocking checkpoint confirms a real, licensed, non-AI, non-aerial Indio street scene sourced one at a time; credit+license recorded; curl 200 |
| T-203-CHIP | Tampering | wrong geo_id / premature hasContext | mitigate | geo_id '0636448' matches the Plan 02 government row; hasContext:true only because Plan 04 (depends_on) landed stance rows; `npm run build` catches shape errors |
| T-203-KEY | Tampering | banner key collision / wrong substring match | accept | 'indio' has no key collision in CURATED_LOCAL; longest-key-first matching already isolates 'riverside county' from 'riverside'; Plan 06 confirms the correct banner renders |
| T-203-SC | Tampering | npm/python build-tool installs | accept | Zero new package installs — reuses existing Vite build + scripts/banners tooling |
</threat_model>

<verification>
- cities/indio.jpg live in Storage (curl 200), a correct licensed non-AI/non-aerial Indio street scene.
- buildingImages.js has the NEW 'indio' key; coverage.js has the Indio chip (alphabetical, hasContext:true, 0636448).
- Build + coverage generator pass; Indio not added to COVERAGE_COUNTIES.
</verification>

<success_criteria>
Indio carries a licensed community banner and surfaces in browse with a DB-honest chip that resolves the
new banner.
</success_criteria>

<output>
Create `.planning/phases/203-indio-deep-seed/203-05-SUMMARY.md` when done (banner subject + credit +
license + CDN 200; the 'indio' key added; the Indio chip added; build result).
</output>
</content>
