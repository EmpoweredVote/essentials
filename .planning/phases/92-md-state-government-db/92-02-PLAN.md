---
phase: 92-md-state-government-db
plan: 02
type: execute
wave: 2
depends_on:
  - 92-01
files_modified:
  - C:/EV-Accounts/backend/migrations/270_md_state_executives.sql
  - C:/EV-Accounts/backend/migrations/271_md_executive_headshots.sql
  - scripts/md_executives_headshots.py
autonomous: true
requirements:
  - MD-GOV-02
  - MD-GOV-06
tags:
  - maryland
  - state-government
  - executives
  - headshots
  - migration

user_setup: []

must_haves:
  truths:
    - "5 STATE_EXEC districts exist with state='MD' (uppercase) and geo_id='24' (one per executive office), per D-01 and OR 223a casing lesson"
    - "5 politicians seeded with external_ids -240001..-240005 (Moore, Miller, Brown, Lierman, Davis); Davis has is_appointed=true per D-03"
    - "5 office rows exist linking each politician to their chamber+district; Davis office has is_appointed_position=true per D-03; the other 4 have is_appointed_position=false"
    - "5 politician_images rows exist with type='default' and url pointing to Supabase Storage 600x750 JPEG files in the politician_photos bucket"
    - "Every politicians.office_id is back-filled (non-null) for external_ids in range -240010..-240001"
    - "All migrations are idempotent — re-running adds 0 rows and back-fill returns 0 affected"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/270_md_state_executives.sql"
      provides: "Migration B: 5 STATE_EXEC districts + 5 politicians + 5 offices + office_id back-fill"
      contains: "external_id BETWEEN -240010 AND -240001"
    - path: "C:/EV-Accounts/backend/migrations/271_md_executive_headshots.sql"
      provides: "AUDIT-ONLY migration recording the politician_images INSERTs performed live"
      contains: "INSERT INTO essentials.politician_images"
    - path: "scripts/md_executives_headshots.py"
      provides: "Python tool that downloads + crops 4:5 + resizes 600x750 Lanczos q90 + uploads to politician_photos bucket"
      contains: "Pillow"
  key_links:
    - from: "essentials.offices"
      to: "essentials.chambers"
      via: "chamber_id FK"
      pattern: "chamber_id = (SELECT id FROM essentials.chambers WHERE name = '<chamber_name>' AND government_id = ...)"
    - from: "essentials.offices"
      to: "essentials.districts"
      via: "district_id FK"
      pattern: "district_id linked to STATE_EXEC district with matching label"
    - from: "essentials.politicians"
      to: "essentials.offices"
      via: "office_id back-fill"
      pattern: "UPDATE ... SET office_id = o.id WHERE p.external_id BETWEEN -240010 AND -240001"
    - from: "essentials.politician_images.url"
      to: "Supabase Storage politician_photos bucket"
      via: "URL path containing politician UUID"
      pattern: "https://kxsdzaojfaibhuzmclfq.*storage.*supabase.co/storage/v1/object/public/politician_photos/"
---

<objective>
Apply Migration 270 (`270_md_state_executives.sql`) to seed 5 STATE_EXEC districts, 5 politicians (Wes Moore, Aruna Miller, Anthony G. Brown, Brooke Lierman, Dereck E. Davis), 5 offices linking them to the Phase 92 Plan 01 chambers, and back-fill office_id on each politician. Then process and upload 5 headshots (600x750 JPEG, Lanczos q90, cropped 4:5) to Supabase Storage and insert 5 politician_images rows. Record the headshot DB writes in audit-only migration 271. Implements requirements MD-GOV-02, MD-GOV-06 (executives portion); decisions D-02 (Davis seeded in this phase) and D-03 (Davis is_appointed=true and is_appointed_position=true).

Purpose: Complete the MD executive branch in one phase pass so Phase 93 (legislature) can add MD Senate / MD House of Delegates chambers without revisiting MD executives.

Output:
- One applied migration (270_md_state_executives.sql) that creates 5 districts + 5 politicians + 5 offices + back-fill
- One Python script (scripts/md_executives_headshots.py) that downloads + processes + uploads 5 headshots
- 5 JPEG files in Supabase Storage `politician_photos` bucket at 600x750
- 5 politician_images rows (inserted live during headshot upload, NOT via migration 271)
- One audit-only migration (271_md_executive_headshots.sql) that documents the politician_images INSERTs
- Verification SQL queries confirming all 5 acceptance criteria gates pass
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-md-state-government-db/92-CONTEXT.md
@.planning/phases/92-md-state-government-db/92-RESEARCH.md
@.planning/phases/92-md-state-government-db/92-PATTERNS.md
@.planning/phases/92-md-state-government-db/92-01-SUMMARY.md
@C:/EV-Accounts/backend/migrations/169_me_state_executives.sql
@C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql
@C:/EV-Accounts/backend/migrations/223a_or_executive_district_fix.sql
@C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and apply migration 270 (districts + politicians + offices + back-fill)</name>
  <files>C:/EV-Accounts/backend/migrations/270_md_state_executives.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/169_me_state_executives.sql (primary template — copy the 3-step structure: STATE_EXEC districts, CTE politician+office blocks, office_id back-fill)
    - C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql (secondary reference for OR-pattern chamber name lookup using short names)
    - C:/EV-Accounts/backend/migrations/223a_or_executive_district_fix.sql (cautionary fix — confirms state='MD' uppercase requirement; lowercase silently breaks routing)
    - .planning/phases/92-md-state-government-db/92-RESEARCH.md (officials data table with exact full_name/first_name/last_name/external_id/party values, and `Pitfall 1` STATE_EXEC casing note)
    - .planning/phases/92-md-state-government-db/92-PATTERNS.md (MD officials data table with exact title, district label, chamber name mappings)
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/migrations/270_md_state_executives.sql following the ME 169 template structure exactly. Wrap in BEGIN; ... COMMIT;.

    Header comment block: state purpose ("5 MD executives + STATE_EXEC districts + offices + back-fill"), document D-01 (LG standalone chamber from Plan 01), document D-02 (Davis seeded in this phase, not deferred), document D-03 (Davis is_appointed=true and is_appointed_position=true; the only one), document state='MD' uppercase requirement (cite OR 223a lesson), document geo_id='24' (MD FIPS), document external_id range -240005..-240001. Note that role_canonical is set NULL for all 5.

    Pre-flight assert block (same as migration 269): IF (SELECT COUNT(*) FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD') <> 1 THEN RAISE EXCEPTION ...

    STEP 1 — Five STATE_EXEC district INSERTs, each guarded by WHERE NOT EXISTS on (district_type='STATE_EXEC' AND state='MD' AND label=<label>). Each insert uses: INSERT INTO essentials.districts (id, district_type, state, geo_id, label, district_id, mtfcc) SELECT gen_random_uuid(), 'STATE_EXEC', 'MD', '24', '<label>', '', ''. Use state='MD' uppercase on every row. The 5 labels are: 'Maryland Governor', 'Maryland Lieutenant Governor', 'Maryland Attorney General', 'Maryland Comptroller', 'Maryland State Treasurer'.

    STEP 2 — Five CTE blocks (one per executive), each using WITH ins_p AS (INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id) VALUES (gen_random_uuid(), '<full_name>', '<first>', '<last>', 'Democrat', true, <is_appointed>, false, true, <external_id>) ON CONFLICT (external_id) DO NOTHING RETURNING id) INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical) SELECT gen_random_uuid(), d.id, (SELECT id FROM essentials.chambers WHERE name = '<chamber_name>' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD')), p.id, '<title>', 'MD', <is_appointed_position>, false, NULL FROM essentials.districts d CROSS JOIN ins_p p WHERE d.district_type = 'STATE_EXEC' AND d.state = 'MD' AND d.label = '<district_label>' AND p.id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.chamber_id = (SELECT id FROM essentials.chambers WHERE name = '<chamber_name>' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Maryland' AND state = 'MD'))).

    Use these exact values for the 5 CTE blocks (verified in RESEARCH.md and PATTERNS.md):

    Block 1 — Wes Moore (-240001, Governor):
      politician: full_name='Wes Moore', first_name='Wes', last_name='Moore', party='Democrat', is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id=-240001
      office: chamber name='Governor', district label='Maryland Governor', title='Governor', representing_state='MD', is_appointed_position=false, is_vacant=false, role_canonical=NULL

    Block 2 — Aruna Miller (-240002, Lieutenant Governor; D-01 standalone chamber):
      politician: full_name='Aruna Miller', first_name='Aruna', last_name='Miller', party='Democrat', is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id=-240002
      office: chamber name='Lieutenant Governor', district label='Maryland Lieutenant Governor', title='Lieutenant Governor', representing_state='MD', is_appointed_position=false, is_vacant=false, role_canonical=NULL

    Block 3 — Anthony G. Brown (-240003, Attorney General):
      politician: full_name='Anthony G. Brown', first_name='Anthony', last_name='Brown', party='Democrat', is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id=-240003
      office: chamber name='Attorney General', district label='Maryland Attorney General', title='Attorney General', representing_state='MD', is_appointed_position=false, is_vacant=false, role_canonical=NULL

    Block 4 — Brooke Lierman (-240004, Comptroller):
      politician: full_name='Brooke Lierman', first_name='Brooke', last_name='Lierman', party='Democrat', is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id=-240004
      office: chamber name='Comptroller', district label='Maryland Comptroller', title='Comptroller', representing_state='MD', is_appointed_position=false, is_vacant=false, role_canonical=NULL

    Block 5 — Dereck E. Davis (-240005, State Treasurer; D-02 + D-03):
      politician: full_name='Dereck E. Davis', first_name='Dereck', last_name='Davis', party='Democrat', is_active=true, is_appointed=true (D-03), is_vacant=false, is_incumbent=true, external_id=-240005
      office: chamber name='State Treasurer', district label='Maryland State Treasurer', title='State Treasurer', representing_state='MD', is_appointed_position=true (D-03), is_vacant=false, role_canonical=NULL

    STEP 3 — office_id back-fill:
      UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -240010 AND -240001 AND p.office_id IS NULL;

    Apply the migration via `mcp__supabase-local__apply_migration` with name `270_md_state_executives`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('C:/EV-Accounts/backend/migrations/270_md_state_executives.sql','utf8');const c=t.replace(/--.*$/gm,'').replace(/\/\*[\s\S]*?\*\//g,'');const checks=[[/BEGIN;/,'BEGIN'],[/COMMIT;/,'COMMIT'],[/RAISE EXCEPTION/,'pre-flight assert'],[/'Wes Moore'/,'Moore name'],[/'Aruna Miller'/,'Miller name'],[/'Anthony G\. Brown'/,'Brown name'],[/'Brooke Lierman'/,'Lierman name'],[/'Dereck E\. Davis'/,'Davis name'],[/-240001/,'Moore external_id'],[/-240005/,'Davis external_id'],[/'STATE_EXEC'/,'STATE_EXEC type'],[/'Maryland Governor'/,'Gov district label'],[/'Maryland Lieutenant Governor'/,'LG district label'],[/'Maryland Attorney General'/,'AG district label'],[/'Maryland Comptroller'/,'Comptroller district label'],[/'Maryland State Treasurer'/,'Treasurer district label'],[/BETWEEN\s+-240010\s+AND\s+-240001/,'back-fill range'],[/p\.office_id IS NULL/,'idempotent back-fill guard']];const missing=checks.filter(([r])=>!r.test(c));if(missing.length){console.error('MISSING:',missing.map(([,n])=>n).join(', '));process.exit(1);}const lc=c.match(/state\s*=\s*'md'/gi)||[];const lowerOnly=lc.filter(s=>/state\s*=\s*'md'$/.test(s)||/state\s*=\s*'md'[^A-Z]/.test(s));if(lowerOnly.length){console.error('FORBIDDEN: lowercase state=\\'md\\' found — must be uppercase MD');process.exit(1);}const mdUpper=(c.match(/state\s*=\s*'MD'/g)||[]).length;if(mdUpper<10){console.error('SUSPECT: expected many state=\\'MD\\' references, found',mdUpper);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/270_md_state_executives.sql exists
    - File contains the literal strings: `'Wes Moore'`, `'Aruna Miller'`, `'Anthony G. Brown'`, `'Brooke Lierman'`, `'Dereck E. Davis'`, `-240001`, `-240002`, `-240003`, `-240004`, `-240005`, `'STATE_EXEC'`, `'Maryland Governor'`, `'Maryland Lieutenant Governor'`, `'Maryland Attorney General'`, `'Maryland Comptroller'`, `'Maryland State Treasurer'`
    - File contains zero occurrences of `state = 'md'` (lowercase) outside SQL comments — only `state = 'MD'` uppercase per OR 223a lesson
    - File contains the literal `BETWEEN -240010 AND -240001` for the back-fill UPDATE
    - File begins with `BEGIN;` and ends with `COMMIT;`
    - `mcp__supabase-local__apply_migration` with name=`270_md_state_executives` returns success
    - SQL: `SELECT COUNT(*) FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='MD'` returns exactly `5`
    - SQL: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -240010 AND -240001` returns exactly `5`
    - SQL: `SELECT external_id, full_name FROM essentials.politicians WHERE external_id BETWEEN -240010 AND -240001 ORDER BY external_id DESC` returns rows in order: `-240001 / Wes Moore`, `-240002 / Aruna Miller`, `-240003 / Anthony G. Brown`, `-240004 / Brooke Lierman`, `-240005 / Dereck E. Davis`
    - SQL: `SELECT COUNT(*) FROM essentials.offices WHERE representing_state='MD'` returns exactly `5`
    - SQL: `SELECT p.full_name, o.is_appointed_position FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id WHERE p.external_id=-240005` returns exactly one row with `is_appointed_position=true` (D-03)
    - SQL: `SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id WHERE p.external_id BETWEEN -240010 AND -240001 AND o.is_appointed_position=false` returns exactly `4` (Moore, Miller, Brown, Lierman)
    - SQL: `SELECT is_appointed FROM essentials.politicians WHERE external_id=-240005` returns `true` (D-03)
    - SQL: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -240010 AND -240001 AND office_id IS NULL` returns exactly `0` (back-fill confirmed)
    - Idempotency: re-applying migration 270 a second time produces no additional rows in districts/politicians/offices and back-fill UPDATE affects 0 rows
  </acceptance_criteria>
  <done>Migration 270 applied successfully; 5 districts + 5 politicians + 5 offices exist with correct values per D-01/D-02/D-03; back-fill confirmed; idempotency verified.</done>
</task>

<task type="auto">
  <name>Task 2: Process headshots, upload to Supabase Storage, insert politician_images rows, record audit migration 271</name>
  <files>scripts/md_executives_headshots.py, C:/EV-Accounts/backend/migrations/271_md_executive_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql (template for the audit-only migration — note the comment header style, the politician_images INSERT pattern with `url` column, type='default', photo_license='public_domain', and the WHERE NOT EXISTS guard)
    - .planning/phases/92-md-state-government-db/92-RESEARCH.md (§Headshot Sources — 5 verified URLs, formats, processing notes; §Pitfall 4 confirming column is `url` not `storage_url`)
    - .planning/phases/92-md-state-government-db/92-PATTERNS.md (§Supabase Storage uploads + politician_images rows — MD headshot sources table with all 5 URLs and processing notes)
  </read_first>
  <action>
    Step A — Create Python script `scripts/md_executives_headshots.py` that:
    1. Connects to Supabase using SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY (or use existing helper if present in repo — search `scripts/` for any prior headshot uploader to reuse credentials pattern; otherwise read env vars).
    2. For each of the 5 officials, downloads the source image from the URL below (use requests with a real User-Agent header — fremont.gov 403 lesson applies generally to .gov hosts):
       - external_id=-240001 Wes Moore: `https://cdn.maryland.gov/maryland-cms/prod/governor/s3fs-public/styles/3_4_504x672_focal_point_webp/public/images/2026-04/gov%201st%20size.png.webp`
       - external_id=-240002 Aruna Miller: `https://cdn.maryland.gov/maryland-cms/prod/governor/s3fs-public/styles/3_4_504x672_focal_point_webp/public/images/2026-04/lg%201st%20size.png.webp`
       - external_id=-240003 Anthony G. Brown: `https://oag.maryland.gov/our-office/PublishingImages/AttorneyGeneral.jpg`
       - external_id=-240004 Brooke Lierman: `https://www.marylandcomptroller.gov/about/brooke-lierman/_jcr_content/root/container/heroContainer/hero.coreimg.png/1740686184941/comptroller-portrait-cropped.png`
       - external_id=-240005 Dereck E. Davis: `https://upload.wikimedia.org/wikipedia/commons/c/cb/Dereck_E._Davis_4_23_2025_%2854473095147%29_%28cropped%29.jpg`
    3. Uses Pillow to: open the image, convert to RGB JPEG-compatible mode (for WebP/PNG sources), inspect dimensions, center-crop to 4:5 aspect ratio FIRST (compute crop box keeping eyes at ~1/3 from top — for 4:5 portrait that's centered crop on a square or wider source), THEN resize to 600x750 using Image.LANCZOS resampling, save as JPEG quality=90 to a temp file. Never stretch/distort.
    4. For each official, queries the live DB for that politician's UUID via `SELECT id FROM essentials.politicians WHERE external_id = <ext_id>` (or accepts UUID mapping from the executor — print to stdout to capture in SUMMARY).
    5. Uploads the JPEG to Supabase Storage bucket `politician_photos` at path `{politician_id}-headshot.jpg` using the supabase-py storage client (or REST PUT). Use the Multnomah path style `{politician_id}-headshot.jpg` (NOT `{politician_id}/default.jpg`) — see migration 245 for the live precedent.
    6. Inserts a politician_images row via the supabase-py table client OR returns the SQL snippet for the executor to apply. Each row uses: id=gen_random_uuid (handled by DB default if column has default; otherwise generate UUID4), politician_id=<uuid>, url='https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{politician_id}-headshot.jpg' (or whichever storage URL prefix the bucket exposes — verify via a HEAD request after upload), type='default', photo_license='public_domain' per RESEARCH.md §Open Question 3.

    Step B — After all 5 uploads succeed and all 5 politician_images rows exist, write the audit-only migration `C:/EV-Accounts/backend/migrations/271_md_executive_headshots.sql` following the 245_multnomah_county_headshots.sql template structure. Header comment must explicitly state "AUDIT-ONLY: captures the live politician_images INSERTs performed during Phase 92-02 execution on <date>. DO NOT apply via Supabase ledger — actual DB writes happened live via scripts/md_executives_headshots.py." (Multnomah 245 wording).

    Each of the 5 INSERT INTO essentials.politician_images statements uses the verified live storage URL (substitute the actual uploaded file URL with the politician's UUID), with WHERE NOT EXISTS guard on politician_id, type='default', photo_license='public_domain'. Use `url` column name (NOT `storage_url` — RESEARCH.md Pitfall 4).

    Do NOT call `mcp__supabase-local__apply_migration` for migration 271 — it is audit-only. The DB rows are inserted live by the Python script. (This mirrors how Multnomah 245, OR 225, SF 200 were handled per project memory.)

    Run idempotency check on the Python script: invoke a second time — must skip already-uploaded files and not duplicate politician_images rows.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const py='scripts/md_executives_headshots.py';const aud='C:/EV-Accounts/backend/migrations/271_md_executive_headshots.sql';if(!fs.existsSync(py)){console.error('MISSING:',py);process.exit(1);}if(!fs.existsSync(aud)){console.error('MISSING:',aud);process.exit(1);}const t=fs.readFileSync(py,'utf8');const pyChecks=[/Pillow|PIL/,/LANCZOS|Lanczos/,/600/,/750/,/politician_photos/,/cdn\.maryland\.gov/,/oag\.maryland\.gov/,/marylandcomptroller\.gov/,/upload\.wikimedia\.org/,/-240001/,/-240005/];const pm=pyChecks.filter(r=>!r.test(t));if(pm.length){console.error('PY MISSING:',pm.map(r=>r.toString()).join(', '));process.exit(1);}const a=fs.readFileSync(aud,'utf8');const audChecks=[/AUDIT-ONLY/i,/-240001/,/-240005/,/'default'/,/'public_domain'/,/url/,/INSERT INTO essentials\.politician_images/,/WHERE NOT EXISTS/];const am=audChecks.filter(r=>!r.test(a));if(am.length){console.error('AUDIT MISSING:',am.map(r=>r.toString()).join(', '));process.exit(1);}if(/storage_url/.test(a.replace(/--.*$/gm,''))){console.error('FORBIDDEN: storage_url in audit migration — column is url');process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File scripts/md_executives_headshots.py exists
    - Python script contains references to: `Pillow` or `PIL`, `LANCZOS` (case-insensitive), `600`, `750`, `politician_photos`, all 5 source URL hosts (`cdn.maryland.gov`, `oag.maryland.gov`, `marylandcomptroller.gov`, `upload.wikimedia.org`), and external_ids `-240001` through `-240005`
    - File C:/EV-Accounts/backend/migrations/271_md_executive_headshots.sql exists
    - Audit migration contains the literal `AUDIT-ONLY` in its header comment
    - Audit migration contains exactly 5 `INSERT INTO essentials.politician_images` statements (one per official)
    - Audit migration contains zero occurrences of the token `storage_url` (RESEARCH.md Pitfall 4 — column is `url`)
    - Audit migration contains the literals `'default'` (type column) and `'public_domain'` (photo_license column)
    - SQL: `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -240010 AND -240001 AND pi.type='default'` returns exactly `5`
    - SQL: `SELECT pi.url FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -240010 AND -240001` returns 5 rows whose URLs all match the pattern `https://kxsdzaojfaibhuzmclfq.*storage.*supabase.co/storage/v1/object/public/politician_photos/`
    - HTTP HEAD against each of the 5 stored URLs returns 200 (storage objects exist and are publicly readable)
    - SQL: `SELECT COUNT(DISTINCT pi.politician_id) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -240010 AND -240001 AND pi.type='default'` returns exactly `5` (one image per politician, no duplicates)
    - Re-running scripts/md_executives_headshots.py a second time produces zero new politician_images rows (idempotency guard via WHERE NOT EXISTS or check-then-insert in the script)
  </acceptance_criteria>
  <done>5 headshots processed at 600x750 JPEG q90, uploaded to politician_photos bucket, 5 politician_images rows inserted with `url` column + type='default' + photo_license='public_domain', audit-only migration 271 recorded. All SQL gates pass. MD-GOV-06 (executives portion) satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Run full phase-completion verification gates and capture summary</name>
  <files>(no file modifications — verification queries via MCP)</files>
  <read_first>
    - .planning/phases/92-md-state-government-db/92-VALIDATION.md (per-task verification map — defines the 6 SQL gates for Phase 92)
    - .planning/phases/92-md-state-government-db/92-RESEARCH.md (§Validation Architecture for the 5 expected SQL gates)
  </read_first>
  <action>
    Run all 6 phase-completion SQL gates via `mcp__supabase-local__execute_sql` and capture the result of each into the phase SUMMARY:

    Gate 1 — chambers: `SELECT COUNT(*) FROM essentials.chambers c JOIN essentials.governments g ON g.id=c.government_id WHERE g.name='State of Maryland' AND g.state='MD'` — expect 5.

    Gate 2 — STATE_EXEC districts: `SELECT COUNT(*) FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='MD'` — expect 5.

    Gate 3 — politicians: `SELECT external_id, full_name FROM essentials.politicians WHERE external_id BETWEEN -240010 AND -240001 ORDER BY external_id DESC` — expect 5 rows: (-240001, Wes Moore), (-240002, Aruna Miller), (-240003, Anthony G. Brown), (-240004, Brooke Lierman), (-240005, Dereck E. Davis).

    Gate 4 — offices + appointed flags: `SELECT p.external_id, p.full_name, o.is_appointed_position, o.title FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id WHERE p.external_id BETWEEN -240010 AND -240001 ORDER BY p.external_id DESC` — expect 5 rows; only Davis (-240005) has is_appointed_position=true; the other 4 have false. Titles: Governor, Lieutenant Governor, Attorney General, Comptroller, State Treasurer.

    Gate 5 — politician_images: `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -240010 AND -240001 AND pi.type='default'` — expect 5.

    Gate 6 — back-fill: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -240010 AND -240001 AND office_id IS NULL` — expect 0.

    Also run a spot-check that the joined chamber/office linkage is correct: `SELECT p.full_name, c.name AS chamber_name, o.title, d.label AS district_label FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.chambers c ON c.id=o.chamber_id JOIN essentials.districts d ON d.id=o.district_id WHERE p.external_id BETWEEN -240010 AND -240001 ORDER BY p.external_id DESC` — expect every chamber name to match its district label (e.g., chamber 'Governor' ↔ district 'Maryland Governor'; chamber 'Lieutenant Governor' ↔ district 'Maryland Lieutenant Governor').

    Spot-check headshot HTTP accessibility: HEAD request against each of the 5 storage URLs from Gate 5; all 5 must return HTTP 200.

    Capture all results in the SUMMARY file.
  </action>
  <verify>
    <automated>echo "Verification is performed via MCP execute_sql + HTTP HEAD during execution. Acceptance criteria below define the SQL gates that must return the expected counts."</automated>
  </verify>
  <acceptance_criteria>
    - All 6 SQL gates listed in `<action>` return the expected counts/rows
    - All 5 storage URLs return HTTP 200 (publicly accessible)
    - Spot-check chamber↔district linkage shows correct pairing (e.g., chamber 'State Treasurer' linked to district 'Maryland State Treasurer' for Davis)
    - Davis (`external_id=-240005`) is the only politician with `is_appointed=true` and the only office with `is_appointed_position=true` in the MD executive set
    - Phase summary `92-02-SUMMARY.md` exists and documents: all 6 gate results, idempotency confirmation for migrations 270 and the Python script, and the 5 politician UUID-to-storage-URL mapping for future cross-reference
  </acceptance_criteria>
  <done>All 6 phase-completion gates green; storage URLs accessible; spot-checks confirm D-01/D-02/D-03; Phase 92 success criteria (5 chambers + 5 politicians + 5 headshots) verified. Ready for ROADMAP.md / STATE.md / REQUIREMENTS.md updates and phase close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → production DB | SQL applied via mcp__supabase-local__apply_migration; pre-flight DO block + WHERE NOT EXISTS guards are the validation surface. mcp__supabase-local IS production per project memory. |
| Python script → external HTTP sources | Downloads images from cdn.maryland.gov, oag.maryland.gov, marylandcomptroller.gov, upload.wikimedia.org — all public government / public domain. No auth flows, no user input. |
| Python script → Supabase Storage | Uses SUPABASE_SERVICE_ROLE_KEY (privileged) to upload to politician_photos bucket; key handled via env var, never logged or committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-01 | Tampering | essentials.districts STATE_EXEC casing | mitigate | All 5 district INSERTs use `state='MD'` uppercase per OR 223a lesson; the Task 1 grep gate fails the build if lowercase `state='md'` appears outside SQL comments. Lowercase silently breaks routing — gate is non-optional. |
| T-92-02-02 | Tampering | essentials.politicians is_appointed flag for Davis | mitigate | Davis (-240005) is the only is_appointed=true politician per D-03; Gate 4 in Task 3 fails if any of the other 4 has is_appointed_position=true or if Davis has false. |
| T-92-02-03 | Tampering | politician_images column name | mitigate | RESEARCH.md Pitfall 4 confirms column is `url` not `storage_url`; Task 2 grep gate fails if `storage_url` appears in the audit migration. |
| T-92-02-04 | Tampering | Migration 271 applied via ledger | mitigate | Migration 271 is audit-only per Multnomah 245 pattern; header comment explicitly says "DO NOT apply via Supabase ledger". Task 3 SUMMARY documents that DB writes happened live via Python, not via apply_migration. |
| T-92-02-05 | Spoofing | Headshot source authenticity | accept | All 5 source URLs are official government domains (cdn.maryland.gov, oag.maryland.gov, marylandcomptroller.gov) or Wikimedia Commons (public domain). No third-party hosting. Source URLs are documented in audit migration 271 comments for traceability. |
| T-92-02-06 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY in Python script | mitigate | Script reads key from env var only; key MUST NOT be hardcoded or printed. Acceptance check: grep the Python file for the literal `eyJ` (JWT prefix) — must be zero matches. |
| T-92-02-07 | Tampering | Headshot crop/resize integrity | mitigate | Pillow Lanczos resize at q90; crop to 4:5 BEFORE resize (memory: feedback_headshot_resize_no_distort.md and feedback_headshot_cropping.md). No stretching. Visual verification deferred to Phase 94 sweep. |
| T-92-02-08 | Denial of Service | Migration apply path | accept | Migration 270 inserts ~15 rows; execution time < 200ms. No DoS risk. |
| T-92-02-SC | Tampering | npm/pip/cargo installs | mitigate | Pillow is the only Python dependency. If not already in repo's existing scripts, executor verifies via `pypi.org/project/pillow` (well-known, billions of downloads — legitimate) before install. RESEARCH.md §Environment Availability flagged Pillow as [ASSUMED]; install step is bounded and well-known. |
</threat_model>

<verification>
- All 13 acceptance criteria SQL gates across the 3 tasks return expected counts
- All 5 storage URLs HEAD HTTP 200
- Davis (-240005) is the unique politician with is_appointed=true AND the unique office with is_appointed_position=true (D-03)
- LG Miller (-240002) is linked to her own standalone chamber 'Lieutenant Governor' AND her own STATE_EXEC district 'Maryland Lieutenant Governor' (D-01)
- All STATE_EXEC district rows use state='MD' uppercase (OR 223a lesson)
- politician_images column used is `url` (not `storage_url`) — RESEARCH.md Pitfall 4
- Idempotency: re-applying migration 270 and re-running headshot script produces zero new rows
- Audit migration 271 exists but was NOT applied via Supabase ledger (Multnomah 245 pattern)
</verification>

<success_criteria>
1. Migration 270 applied successfully with 5 districts + 5 politicians + 5 offices + back-fill
2. 5 headshots at 600x750 JPEG q90 in Supabase Storage politician_photos bucket
3. 5 politician_images rows with type='default', photo_license='public_domain', `url` column populated
4. Audit migration 271 recorded but not applied via ledger
5. All Phase 92 ROADMAP success criteria met:
   - essentials.governments has a State of Maryland row (asserted in Plan 01)
   - 5 constitutional officer chambers exist (Plan 01)
   - Governor Wes Moore + LG Aruna Miller + AG Anthony Brown + Comptroller Brooke Lierman + State Treasurer Dereck E. Davis seeded
   - All 5 executives have headshots at 600x750 in Supabase Storage
   - State Treasurer chamber/office has is_appointed_position=true (D-03)
   - LG has standalone chamber + standalone STATE_EXEC district (D-01)
</success_criteria>

<output>
Create `.planning/phases/92-md-state-government-db/92-02-SUMMARY.md` when done. Include:
- Migration 270 apply status + file path
- Migration 271 file path (recorded; NOT applied)
- Python script path + idempotency confirmation
- All 6 phase-completion gate results from Task 3
- 5 politician UUID → storage URL mapping (for Phase 94 reference)
- Davis appointed-flag confirmation (D-03)
- LG standalone chamber/district confirmation (D-01)
- Any deviations from this plan
</output>
