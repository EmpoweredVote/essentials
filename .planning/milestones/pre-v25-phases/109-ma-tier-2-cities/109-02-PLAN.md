---
phase: 109-ma-tier-2-cities
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/352_springfield_government.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-352.ts
autonomous: true
requirements: [MA-TIER2-02]
must_haves:
  truths:
    - "A Springfield address returns a LOCAL section listing Mayor Sarno + 13 city councillors"
    - "Mayor Domenic J. Sarno is in the LOCAL_EXEC district; the 13 councillors are in the LOCAL district"
    - "All 14 Springfield officials have a non-NULL office_id"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/352_springfield_government.sql"
      provides: "Springfield government + chamber + 2 districts + 14 politicians + 14 offices (per MA-TIER2-02)"
      contains: "City of Springfield, Massachusetts, US"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-352.ts"
      provides: "Apply + smoke-test harness for migration 352"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts (geo_id=2567000)"
      via: "district_id FK"
      pattern: "geo_id = '2567000'"
    - from: "essentials.politicians (office_id)"
      to: "essentials.offices"
      via: "office_id back-fill"
      pattern: "office_id IS NULL"
---

<objective>
Seed the City of Springfield government so any Springfield address returns a populated LOCAL section with Mayor Domenic J. Sarno plus the 13 sitting city councillors (5 at-large + 8 ward). Implements the Springfield portion of MA-TIER2-02.

Purpose: Springfield is Massachusetts' third-largest city. Its G4110 geofence (geo_id='2567000') is already loaded from v5.0; this plan adds the government structure.
Output: Migration `352_springfield_government.sql` + apply harness `_apply-migration-352.ts`, applied to production via Supabase MCP, with all post-verification gates passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-ma-tier-2-cities/109-RESEARCH.md
@.planning/phases/109-ma-tier-2-cities/109-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 352 — Springfield government</name>
  <files>C:/EV-Accounts/backend/migrations/352_springfield_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/347_boston_government.sql (the exact-match analog: 7-step pattern)
    - .planning/phases/109-ma-tier-2-cities/109-PATTERNS.md (lines 25-264: full migration pattern; lines 268-300: Springfield-specific differences + external_id table)
    - .planning/phases/109-ma-tier-2-cities/109-RESEARCH.md (lines 89-114: Springfield roster + notes; lines 536-539: Pitfall 7 title precision)
  </read_first>
  <action>
    Create migration 352 following the same 7-step structure as migration 351 (Worcester), adapted for Springfield.

    Concrete values:
      - Government: name='City of Springfield, Massachusetts, US', type='LOCAL', state='MA', city='Springfield', geo_id='2567000'
      - Chamber: name='City Council', name_formal='Springfield City Council', NO slug
      - LOCAL_EXEC district: 'ma', '2567000', label='Springfield (Citywide)', mtfcc=NULL
      - LOCAL district: 'ma', '2567000', label='Springfield', mtfcc=NULL

    14 politician+office WITH ins_p blocks (party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true; ON CONFLICT (external_id) DO NOTHING; office NOT EXISTS guard on (district_id, politician_id)):
      - Domenic J. Sarno, -256700001, title='Mayor', LOCAL_EXEC district
      - Michael A. Fenton, -256700002, title='City Councilor (Ward 2)', LOCAL (per Pitfall 7: store ward title, NOT 'Council President')
      - Melvin A. Edwards, -256700003, title='City Councilor (Ward 3)', LOCAL
      - Maria Perez, -256700004, title='City Councilor (Ward 1)', LOCAL
      - Malo L. Brown, -256700005, title='City Councilor (Ward 4)', LOCAL
      - Lavar Click-Bruce, -256700006, title='City Councilor (Ward 5)', LOCAL
      - Victor G. Davila, -256700007, title='City Councilor (Ward 6)', LOCAL
      - Gerry Martin, -256700008, title='City Councilor (Ward 7)', LOCAL
      - Zaida Govan, -256700009, title='City Councilor (Ward 8)', LOCAL
      - Justin Hurst, -256700010, title='City Councilor', LOCAL
      - Jose Delgado, -256700011, title='City Councilor', LOCAL
      - Kateri Walsh, -256700012, title='City Councilor', LOCAL
      - Tracye Whitfield, -256700013, title='City Councilor', LOCAL
      - Brian Santaniello, -256700014, title='City Councilor', LOCAL
      For compound last names like 'Click-Bruce', set last_name='Click-Bruce', first_name='Lavar'.

    Header CRITICAL rules identical to migration 351 (slug GENERATED, no geo_id unique constraint, state casing, mtfcc=NULL, party=NULL, is_appointed=false).
    Step 6 office_id back-fill: external_id BETWEEN -256700014 AND -256700001.
    Step 7 post-verification gates: 1 gov, 1 chamber, 2 districts, 14 politicians, 14 offices, 0 section-split orphans for geo_id='2567000', 0 NULL office_ids.
    Step 8 ledger: version '352'.
    Use exact string '2567000' for every geo_id reference. No per-district geofences.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/migrations/352_springfield_government.sql','utf8'); let miss=[]; for(let i=1;i<=14;i++){const id='-2567000'+String(i).padStart(2,'0'); if(!s.includes(id))miss.push(id)} if(miss.length){console.error('MISSING:',miss);process.exit(1)} if(!s.includes(\"VALUES ('352')\")){console.error('missing ledger');process.exit(1)} if(!s.includes('City Councilor (Ward 8)')){console.error('missing Ward 8');process.exit(1)} console.log('OK 14 ids + ledger + wards')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/352_springfield_government.sql
    - Contains exact string 'City of Springfield, Massachusetts, US'
    - All 14 external_ids -256700001 through -256700014 appear
    - Fenton (-256700002) office title is 'City Councilor (Ward 2)' — NOT 'Council President'
    - 8 ward titles present: 'City Councilor (Ward 1)' through 'City Councilor (Ward 8)'
    - District INSERTs use mtfcc=NULL; chamber INSERT has no 'slug' column
    - Ledger INSERT version '352' present
  </acceptance_criteria>
  <done>Migration 352 SQL written with all 14 officials (Mayor + 8 ward + 5 at-large), 2 districts, gates, and ledger entry.</done>
</task>

<task type="auto">
  <name>Task 2: Write apply harness _apply-migration-352.ts</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-352.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-347.ts (apply-harness analog)
    - .planning/phases/109-ma-tier-2-cities/109-PATTERNS.md (lines 611-690: apply-script pattern + adaptation table)
  </read_first>
  <action>
    Create _apply-migration-352.ts by adapting _apply-migration-347.ts. Read+execute migrations/352_springfield_government.sql via pg Pool (DATABASE_URL, ssl rejectUnauthorized:false). Smoke tests:
      1. government rows name='City of Springfield, Massachusetts, US' → expect 1
      2. politicians external_id BETWEEN -256700014 AND -256700001 → expect 14
      3. districts state='ma' AND geo_id='2567000' AND district_type IN ('LOCAL_EXEC','LOCAL') → expect 2
      4. politicians in range with office_id IS NULL → expect 0
      5. ledger version='352' → PRESENT/MISSING
    Error → exit(1); finally pool.end(). This is the fallback harness; authoritative apply is Supabase MCP (Task 3).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/scripts/_apply-migration-352.ts','utf8'); if(!s.includes('352_springfield_government.sql')||!s.includes('-256700014')||!s.includes('2567000')){process.exit(1)} console.log('OK apply-352')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_apply-migration-352.ts
    - References migrations/352_springfield_government.sql
    - Contains 5 smoke tests with expected counts (1, 14, 2, 0, ledger)
    - Reads DATABASE_URL from process.env (no hardcoded credentials)
  </acceptance_criteria>
  <done>Apply harness written for migration 352 with smoke tests matching Springfield's expected counts.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply migration 352 to production via Supabase MCP</name>
  <files>C:/EV-Accounts/backend/migrations/352_springfield_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/352_springfield_government.sql (read full file before executing)
  </read_first>
  <action>
    Apply migration 352 to production using `mcp__supabase-local__execute_sql` (NOT the .ts harness, NOT supabase db push). Pass the full SQL file contents. The Step 7 post-verification DO block raises an exception on any gate failure.

    After applying, run the scoped MA section-split query via execute_sql:
      SELECT COUNT(*) AS orphan_count FROM essentials.geofence_boundaries g WHERE g.state='25' AND g.mtfcc='G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=g.geo_id);
    Expect 54 if applied after Worcester (351). If 352 is applied before 351 in execution order, the absolute number differs — the invariant is that Springfield's geo_id 2567000 is no longer an orphan (decremented by 1 from whatever the pre-352 count was).

    feedback_supabase_local_is_remote: this is the live production DB.
  </action>
  <verify>
    <automated>MISSING — verified via Supabase MCP execute_sql at runtime: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -256700014 AND -256700001 returns 14; geo_id 2567000 no longer in section-split orphan set</automated>
  </verify>
  <acceptance_criteria>
    - mcp__supabase-local__execute_sql applied migration 352 without exception (Step 7 gates passed)
    - SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -256700014 AND -256700001 returns 14
    - 0 of those politicians have NULL office_id
    - geo_id 2567000 is not present in the MA G4110 section-split orphan set
    - Ledger row version='352' present
  </acceptance_criteria>
  <done>Migration 352 applied to production; all gates passed; Springfield geo_id no longer a section-split orphan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | SQL executed via Supabase MCP against live production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04 | Tampering | migration 352 re-run | mitigate | WHERE NOT EXISTS on government; ON CONFLICT (external_id) DO NOTHING; NOT EXISTS guard on (district_id, politician_id) — idempotent |
| T-109-05 | Information Disclosure | seeded politician records | accept | Public elected officials from official city websites; no non-public PII |
| T-109-06 | Tampering | wrong office title (Fenton 'Council President') | mitigate | Action specifies title='City Councilor (Ward 2)'; acceptance criterion asserts it; procedural title not stored as office |
</threat_model>

<verification>
- Migration 352 applied without exception
- 14 Springfield politicians present (external_id -256700014..-256700001), 0 NULL office_id
- Springfield geo_id 2567000 no longer a section-split orphan
- Government, chamber, LOCAL_EXEC + LOCAL districts present
</verification>

<success_criteria>
A Springfield address routes (via geo_id='2567000') to a LOCAL section listing Mayor Sarno + 13 councillors, all with non-NULL office_ids. Springfield portion of MA-TIER2-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/109-ma-tier-2-cities/109-02-SUMMARY.md` when done
</output>
