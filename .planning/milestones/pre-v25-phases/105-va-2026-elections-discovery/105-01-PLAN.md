---
phase: 105-va-2026-elections-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/322_va_2026_elections.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-322.ts
autonomous: true
requirements:
  - VA-ELECTIONS-01
tags:
  - elections
  - sql
  - virginia
  - va-2026
must_haves:
  truths:
    - "essentials.elections has exactly 2 VA rows: 2026-08-04 primary + 2026-11-03 general"
    - "Both rows persist idempotently across re-apply (count stays at 2)"
    - "Migration 322 ledger entry is present in supabase_migrations.schema_migrations"
  artifacts:
    - path: C:/EV-Accounts/backend/migrations/322_va_2026_elections.sql
      provides: "Idempotent INSERTs for both VA 2026 election rows + ledger entry"
      contains: "ON CONFLICT (name, election_date, state) DO NOTHING"
    - path: C:/EV-Accounts/backend/scripts/_apply-migration-322.ts
      provides: "Apply script with smoke tests confirming both rows + ledger"
      contains: "essentials.elections"
  key_links:
    - from: essentials.elections
      to: essentials.races
      via: "FK target election_id (resolved by Plan 02 via subquery against name='2026 Virginia General Election')"
      pattern: "WHERE name = '2026 Virginia General Election' AND state = 'VA'"
---

<objective>
Seed the two Virginia 2026 election rows (primary 2026-08-04, general 2026-11-03) into essentials.elections via migration 322. These rows are the FK target for Plan 02's race INSERTs (which resolve election_id via subquery against the general election name).

Purpose: Establish the election rows that VA-ELECTIONS-01 requires and that downstream plans (races, discovery) depend on by name. This mirrors the MD Phase 96 Plan 01 pattern exactly (migration 278).

Output: Migration 322 SQL + apply script committed and applied to production; 2 VA elections rows verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-01-SUMMARY.md
@C:/EV-Accounts/backend/migrations/278_md_2026_elections.sql
@C:/EV-Accounts/backend/scripts/_apply-migration-278.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 322_va_2026_elections.sql + apply script + apply to production</name>
  <files>
    C:/EV-Accounts/backend/migrations/322_va_2026_elections.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-322.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/migrations/278_md_2026_elections.sql
    C:/EV-Accounts/backend/scripts/_apply-migration-278.ts
    .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-01-SUMMARY.md
    .planning/STATE.md (confirm next migration counter = 322; per planning_context VA elections claims 322)
  </read_first>
  <action>
    Create the migration SQL file at `C:/EV-Accounts/backend/migrations/322_va_2026_elections.sql` following the MD migration 278 pattern verbatim, substituting VA-specific values:

    - Header comment block (mirrors migration 278): name as "Migration 322: VA 2026 Elections — Phase 105 Plan 01"; note that the primary row is bare (no races link to it; all races link to the general election only); note both dates verified from ROADMAP and STATE.md.
    - Two INSERT statements against `essentials.elections (name, election_date, election_type, jurisdiction_level, state)`:
      - Row 1: name='2026 Virginia State Primary', election_date='2026-08-04', election_type='primary', jurisdiction_level='state', state='VA'
      - Row 2: name='2026 Virginia General Election', election_date='2026-11-03', election_type='general', jurisdiction_level='state', state='VA'
    - Both INSERTs guarded by `ON CONFLICT (name, election_date, state) DO NOTHING` for idempotency.
    - Final ledger INSERT: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('322') ON CONFLICT (version) DO NOTHING;` (this is mandatory per the MD 278/281 lesson — apply script will RAISE if missing).

    Create the apply script at `C:/EV-Accounts/backend/scripts/_apply-migration-322.ts` by copying `_apply-migration-278.ts` verbatim and substituting '278' -> '322', '278_md_2026_elections.sql' -> '322_va_2026_elections.sql', state 'MD' -> 'VA', election dates '2026-06-23' -> '2026-08-04' and '2026-11-03' (general date unchanged), election names "2026 Maryland State Primary" -> "2026 Virginia State Primary" and "2026 Maryland General Election" -> "2026 Virginia General Election". Keep the same 4 smoke tests (count=2, primary date, general date, ledger present).

    Apply the migration via `cd C:/EV-Accounts && npx tsx backend/scripts/_apply-migration-322.ts`. (Note: do NOT run git in C:/EV-Accounts; per project memory, EV-Accounts is deployed by Render push to master, not via this essentials repo.) The migration is applied directly to the production Supabase DB via DATABASE_URL.

    Do NOT commit a ledger INSERT separately or via Supabase MCP — the ledger row is inside the SQL file and applied with it.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && npx tsx backend/scripts/_apply-migration-322.ts 2>&amp;1 | tee /tmp/_322.log; grep -q "VA 2026 election rows: 2" /tmp/_322.log &amp;&amp; grep -q "Primary election_date: 2026-08-04" /tmp/_322.log &amp;&amp; grep -q "General election_date: 2026-11-03" /tmp/_322.log &amp;&amp; grep -q "Ledger entry 322: PRESENT" /tmp/_322.log</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/322_va_2026_elections.sql exists
    - SQL file contains the literal string `'2026 Virginia State Primary'`
    - SQL file contains the literal string `'2026 Virginia General Election'`
    - SQL file contains the literal string `'2026-08-04'`
    - SQL file contains the literal string `'2026-11-03'`
    - SQL file contains the literal string `ON CONFLICT (name, election_date, state) DO NOTHING`
    - SQL file contains the literal string `INSERT INTO supabase_migrations.schema_migrations (version)` paired with `VALUES ('322')`
    - File C:/EV-Accounts/backend/scripts/_apply-migration-322.ts exists and references `322_va_2026_elections.sql`
    - Apply script run prints `VA 2026 election rows: 2 (expected 2)`
    - Apply script run prints `Primary election_date: 2026-08-04 (expected 2026-08-04)`
    - Apply script run prints `General election_date: 2026-11-03 (expected 2026-11-03)`
    - Apply script run prints `Ledger entry 322: PRESENT`
    - Re-running apply script keeps row count at 2 (idempotency)
    - Production DB query `SELECT COUNT(*) FROM essentials.elections WHERE state='VA' AND election_date IN ('2026-08-04','2026-11-03')` returns 2
  </acceptance_criteria>
  <done>
    Both VA 2026 election rows exist in production essentials.elections; migration 322 is recorded in the supabase_migrations ledger; downstream Plan 02 race INSERTs can resolve `(SELECT id FROM essentials.elections WHERE name='2026 Virginia General Election' AND state='VA')` to a non-null UUID.
  </done>
</task>

</tasks>

<verification>
- Both VA 2026 election rows exist with correct dates and types
- Idempotent: re-apply leaves count at 2
- Ledger entry 322 PRESENT
- Plan 02 (race rows) can resolve general election by name
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM essentials.elections WHERE state='VA'` returns 2 (or more if prior phases seeded other VA elections — current expectation is exactly 2)
- `SELECT election_date FROM essentials.elections WHERE name='2026 Virginia State Primary' AND state='VA'` returns 2026-08-04
- `SELECT election_date FROM essentials.elections WHERE name='2026 Virginia General Election' AND state='VA'` returns 2026-11-03
- VA-ELECTIONS-01 satisfied
</success_criteria>

<output>
Create `.planning/phases/105-va-2026-elections-discovery/105-01-SUMMARY.md` when done with:
- Migration 322 file path
- Apply script file path
- Verified election UUIDs (UUID + name + date table)
- Acceptance criteria checklist
- Any deviations (none expected — pattern is well-trodden)
</output>
