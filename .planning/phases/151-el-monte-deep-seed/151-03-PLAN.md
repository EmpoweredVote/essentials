---
phase: 151-el-monte-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["151-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql
autonomous: false
requirements: [ELMN-01]

must_haves:
  truths:
    - "the exact image gap set is determined by a pre-flight image-coverage query over the 7 current members (Crippen-Thomas D1, Herrera D2, Ruedas D3, Longoria D4, Galvan D5, Cortez D6, Ancona Mayor), NOT hardcoded; CONTEXT confirms the 6 PRE-EXISTING officials each already have 1 politician_images row (dimensions UNVERIFIED) and Cortez (NEW from Plan 02) has 0 — Wave 3 is VERIFY-AND-FIX for the 6 + greenfield for Cortez; the pre-flight re-confirms which images exist + their dimensions at apply time"
    - "ci.el-monte.ca.us has NO WAF (RESEARCH §D-04 — all 7 documentIds returned HTTP 200 via direct curl) — portraits are sourced by DIRECT curl from https://www.ci.el-monte.ca.us/ImageRepository/Document?documentId=NNNN (Pitfall 7: use ci.el-monte.ca.us, NOT elmonteca.gov which is an alias/redirect). Confirmed documentIds: 7434=Ancona, 7431=Crippen-Thomas(D1), 7432=Herrera(D2), 7433=Ruedas(D3), 7430=Longoria(D4), 7429=Galvan(D5), 7435=Cortez(D6)"
    - "for each of the 6 PRE-EXISTING images: verify it is the correct named person + no superimposed text/graphics + exactly 600×750 (4:5); KEEP good ones as-is; re-source/re-crop ONLY those that fail (wrong person, wrong dimensions, low quality) — Cortez (D6) is always sourced fresh from documentId=7435 (greenfield)"
    - "every uploaded/re-cropped portrait is the correct verified person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 FIRST (never stretched) then resized 600×750 Lanczos q90 JPEG, uploaded to Supabase Storage politician_photos at the canonical path {uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), x-upsert:true; politician_images type='default', photo_license matching the REAL source (press_use for official city portrait), photo_origin_url set to the documentId URL"
    - "no fabricated photos — where no acceptable verified portrait is obtainable for a member, an honest gap is documented rather than uploading a bad/wrong-person/overlaid image as type='default' (CONTEXT D-04 honest-gap rule); fallbacks per RESEARCH §D-04 (Ballotpedia / campaign FB / Instagram) only if the official portrait is unusable"
    - "full-roster image audit confirms each of the 7 current members has exactly one type='default' row (or a documented honest gap), with no member having >1"
    - "migration 1002 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 1001; on-disk counter authoritative); the migration file is COMMITTED to the EV-Accounts repo (feedback_no_git_in_ev_accounts)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql"
      provides: "Audit-only headshot INSERT/UPDATE (7 current members) + photo_origin_url backfill + full-roster image-coverage audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos ({uuid}-headshot.jpg objects)"
      to: "essentials.politician_images.url (type='default')"
      via: "INSERT politician_images scoped by politician_id, guarded NOT EXISTS (or UPDATE the existing row for re-cropped pre-existing images)"
      pattern: "'default'"
---

<objective>
Give the current 7-member El Monte roster (D1 Crippen-Thomas, D2 Herrera, D3 Ruedas, D4 Longoria, D5 Galvan, D6
Cortez, Mayor Ancona) clean, correct official headshots and confirm single canonical-image coverage. After Plan
02 the survivor chamber 5ca38f3a holds all 7 with consistent bidirectional links. CONTEXT confirms the 6
PRE-EXISTING officials each already have 1 politician_images row (dimensions UNVERIFIED) and Cortez (NEW from
Plan 02) has 0 -> this is a VERIFY-AND-FIX wave for the 6 + greenfield for Cortez. The EXACT gap set is
re-confirmed by a pre-flight image-coverage query at apply time.

⚠️ ci.el-monte.ca.us has NO WAF (RESEARCH §D-04 — confirmed 2026-06-21: all 7 documentIds returned HTTP 200 via
direct curl). Unlike Downey (WAF-403, operator in-browser), El Monte portraits are sourced by DIRECT curl from
the ImageRepository, the same CivicEngage pattern as Pasadena/Palmdale. CRITICAL (Pitfall 7): use
ci.el-monte.ca.us as the host — NOT elmonteca.gov (an alias/redirect that may behave differently with curl).
The /find-headshots skill (~/.claude/commands/find-headshots.md) remains available if any official portrait is
unusable.

Confirmed documentIds (RESEARCH §D-04 — all HTTP 200):
  Ancona (Mayor): https://www.ci.el-monte.ca.us/ImageRepository/Document?documentId=7434
  Crippen-Thomas (D1): documentId=7431
  Herrera (D2): documentId=7432
  Ruedas (D3): documentId=7433
  Longoria (D4): documentId=7430
  Galvan (D5): documentId=7429
  Cortez (D6, NEW from Plan 02 — greenfield): documentId=7435
  (Note Pitfall 6: an older Ancona image documentId=4243 also exists; prefer the City Council page image 7434
  unless testing shows 4243 is higher quality.)

Per member (the 6 pre-existing are VERIFY-FIRST; Cortez is fresh):
- For each of the 6 pre-existing officials (Ancona, Crippen-Thomas, Herrera, Ruedas, Longoria, Galvan): verify
  the existing politician_images row is the correct named person + has no superimposed text/graphics + is exactly
  600×750 (4:5). KEEP good ones as-is. Re-source from the documentId above (direct curl) + re-crop ONLY those
  that fail (wrong person / wrong dimensions / low quality / overlay).
- Cortez (D6): source fresh from documentId=7435 (greenfield — she has 0 images).

Processing (NON-NEGOTIABLE per project memory) for any portrait being uploaded/re-cropped: visually confirm the
CORRECT named person and NO superimposed text/graphics -> crop to 4:5 FIRST (never stretch — crop only, eyes
~1/3 from top, full head+shoulders) -> resize 600×750 Lanczos q90 JPEG (Python Pillow) -> upload to the Supabase
Storage politician_photos bucket at the canonical path {uuid}-headshot.jpg using SUPABASE_SERVICE_ROLE_KEY
(C:/EV-Accounts/backend/.env) with header x-upsert:true -> INSERT (or UPDATE for a re-cropped pre-existing row)
politician_images (type='default', photo_license='press_use' for the official city portrait); set
photo_origin_url to the documentId URL. Reject any source with superimposed text/graphics over the face —
document a gap honestly rather than upload a bad photo (CONTEXT D-04). Resolve each member's UUID by external_id
at apply time (Cortez's resolved ext_id comes from the Plan 02 SUMMARY).

Purpose: A voter sees the right face next to each official. Output: Migration 1002 (audit-only — NOT registered
in schema_migrations) committed to EV-Accounts + verified portraits for the 7 current members (Cortez fresh from
documentId=7435; the 6 pre-existing kept or re-cropped) + a human checkpoint confirming the correct person +
correct crop for each; any genuine gap documented honestly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/151-el-monte-deep-seed/151-RESEARCH.md
@.planning/phases/151-el-monte-deep-seed/151-CONTEXT.md
@.planning/phases/151-el-monte-deep-seed/151-02-SUMMARY.md
@.planning/phases/149-pasadena-deep-seed/149-03-PLAN.md
@.planning/phases/150-downey-deep-seed/150-03-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §D-04). ci.el-monte.ca.us is NO-WAF — direct curl for all 7.
     Pre-flight re-confirms the gap set + dimensions; /find-headshots skill available for fallback. -->

Current 7 members (resolve UUID by external_id; pre-flight a coverage + dimension query):
  Sheila Crippen-Thomas -201202 (D1) · Martin Herrera -201204 (D2) · Julia Ruedas 657390 (D3) ·
  Viviana Longoria 657386 (D4) · Cindy Galvan -201203 (D5) · Marisol Cortez <resolved ext_id from Plan 02> (D6, NEW) ·
  Jessica Ancona -200669 (Mayor)
  (6 pre-existing have 1 image each, dimensions UNVERIFIED; Cortez has 0 — greenfield.)

Source plan (RESEARCH §D-04; ci.el-monte.ca.us NO-WAF -> DIRECT curl for all 7; Pitfall 7: ci.el-monte.ca.us NOT elmonteca.gov):
  Ancona         DIRECT curl: https://www.ci.el-monte.ca.us/ImageRepository/Document?documentId=7434  (HTTP 200; prefer over older 4243, Pitfall 6)
  Crippen-Thomas DIRECT curl: documentId=7431
  Herrera        DIRECT curl: documentId=7432
  Ruedas         DIRECT curl: documentId=7433
  Longoria       DIRECT curl: documentId=7430
  Galvan         DIRECT curl: documentId=7429
  Cortez         DIRECT curl: documentId=7435  (NEW — greenfield)
  Fallbacks (RESEARCH §D-04, only if the official portrait is unusable): Ancona jessicaancona.com / IG;
    Crippen-Thomas Ballotpedia / FB; Herrera herreraforelmonte.com / Ballotpedia; Ruedas Ballotpedia / SCAG;
    Longoria Ballotpedia / FB; Galvan Ballotpedia / FB; Cortez Ballotpedia / FB "Marisol for El Monte".
    (use /find-headshots if needed.)

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face

Storage + DB:
  - Bucket: politician_photos · Path: canonical {uuid}-headshot.jpg
  - Upload via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), header x-upsert:true
  - politician_images: type='default', photo_license='press_use' (official city portrait)
  - UPDATE politicians.photo_origin_url to the documentId URL where NULL
  - For a pre-existing-but-good image: keep the existing row as-is (no-op). For a re-cropped pre-existing image:
    UPDATE the existing row's url + photo_license. For Cortez (new): INSERT guarded WHERE NOT EXISTS on
    politician_id -> exactly ONE type='default' row per member.

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql). It does NOT register in
schema_migrations (same convention as Pasadena 948 / Downey 993). On-disk counter authoritative; ledger stays at
1001 (Plan 02). The FILE still must be committed to EV-Accounts (feedback_no_git_in_ev_accounts).

Pattern migration: C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql (NO-WAF direct-curl audit-only
INSERT + photo_origin_url backfill + Storage URL shape + full-roster audit + ledger-unchanged check — the exact
NO-WAF El Monte precedent) and 993_downey_headshots.sql (most recent headshot audit-only file shape). /find-headshots
skill: ~/.claude/commands/find-headshots.md. Project memory: feedback_headshot_no_graphics,
feedback_headshot_image_sizing, feedback_headshot_cropping, feedback_headshot_resize_no_distort.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight image-coverage + dimension check; verify the 6 pre-existing portraits + source Cortez fresh (all via DIRECT curl from ci.el-monte.ca.us NO-WAF documentIds); re-crop only failures; crop 4:5 -> 600×750 -> upload canonical</name>
  <files>C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql (NO-WAF direct-curl audit-only INSERT + download/crop/upload pipeline + Storage URL shape + full-roster audit + ledger-unchanged check — the exact El Monte precedent)
    - C:/EV-Accounts/backend/migrations/993_downey_headshots.sql (most recent headshot audit-only file shape)
    - .planning/phases/149-pasadena-deep-seed/149-03-PLAN.md (the NO-WAF direct-curl + pre-flight-coverage + crop/resize + checkpoint shape this plan mirrors)
    - .planning/phases/150-downey-deep-seed/150-03-PLAN.md (the verify-identity-before-upload + honest-gap + crop discipline shape)
    - .planning/phases/151-el-monte-deep-seed/151-RESEARCH.md (§D-04 headshot sources/WAF status incl. all 7 documentIds + per-member fallbacks, §Common Pitfalls 6/7, §Assumptions A3)
    - .planning/phases/151-el-monte-deep-seed/151-02-SUMMARY.md (final 7-member link state + Cortez's resolved ext_id/UUID)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots — 4:5 crop FIRST -> 600×750 Lanczos q90)
    - ~/.claude/commands/find-headshots.md (skill for finding a current portrait if a source is unusable)
  </read_first>
  <action>
    First run a pre-flight image-coverage + dimension query: for the 7 current members (-201202 Crippen-Thomas,
    -201204 Herrera, 657390 Ruedas, 657386 Longoria, -201203 Galvan, Cortez resolved ext_id from Plan 02, -200669
    Ancona), LEFT JOIN essentials.politician_images (type='default') and record which have an image + the stored
    dimensions/url (the 6 pre-existing have 1 each, Cortez 0 — re-confirm at apply time). For each of the 6
    pre-existing images, fetch/inspect the current image and verify: correct named person + no superimposed
    text/graphics + exactly 600×750 (4:5). KEEP any that pass (no-op). For any that FAIL + for Cortez (always):
    source by DIRECT curl from ci.el-monte.ca.us (e.g. curl -L
    "https://www.ci.el-monte.ca.us/ImageRepository/Document?documentId=7435" -o cortez.jpg — Pitfall 7: use
    ci.el-monte.ca.us, NOT elmonteca.gov). documentIds: 7434 Ancona / 7431 Crippen-Thomas / 7432 Herrera / 7433
    Ruedas / 7430 Longoria / 7429 Galvan / 7435 Cortez (prefer 7434 over the older 4243 for Ancona, Pitfall 6).
    For EACH sourced portrait, visually confirm it is the correct named person and has NO superimposed
    text/graphics. If any official portrait is unusable (overlay/wrong aspect/wrong person), use the RESEARCH
    §D-04 fallback (Ballotpedia / campaign FB / IG) or the /find-headshots skill. Then crop to 4:5 (crop only —
    never stretch; eyes ~1/3 from top, full head+shoulders), resize to 600×750 Lanczos q90 JPEG (Pillow), resolve
    each UUID by external_id, and upload to politician_photos at {uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY
    (C:/EV-Accounts/backend/.env) with x-upsert:true. Record each public Storage URL + documentId source + the
    photo_license ('press_use') per member for Task 2, AND record which of the 6 pre-existing were KEPT vs
    re-cropped. If no acceptable verified portrait is obtainable for a member, document the genuine gap (do NOT
    upload a wrong-person/overlaid image as type='default' — CONTEXT D-04).
  </action>
  <verify>
    <automated>For each sourced/re-cropped member the processed file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('{member}.jpg').size)"` -> (600, 750)) and the uploaded Storage object at {uuid}-headshot.jpg returns HTTP 200; Cortez's portrait confirmed uploaded (she was greenfield)</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight image-coverage + dimension query run; the 6 pre-existing images + their dimensions re-confirmed from live data; Cortez confirmed at 0 images (greenfield)
    - All sourcing via DIRECT curl from ci.el-monte.ca.us (NO-WAF; NOT elmonteca.gov — Pitfall 7); documentIds 7429-7435 used per member
    - Each of the 6 pre-existing images verified for correct person + no overlay + 600×750; passing ones KEPT, failing ones re-sourced/re-cropped; Cortez (D6) sourced fresh from documentId=7435
    - Each uploaded/re-cropped portrait is the correct verified person; no superimposed text/graphics; processed to exactly 600×750, 4:5, not stretched, eyes ~1/3 from top; uploaded to {uuid}-headshot.jpg (HTTP 200)
    - Public Storage URL + documentId source + photo_license ('press_use') recorded for Task 2 per uploaded member; KEEP-vs-re-crop status recorded for the 6 pre-existing
    - If any member has no acceptable portrait, an honest gap documented (NOT a wrong-person/overlaid image faked as type='default')
  </acceptance_criteria>
  <done>The 7 current members have verified portraits (Cortez fresh from documentId=7435; the 6 pre-existing kept or re-cropped from their documentIds via direct curl), each confirmed correct + clean, processed to 600×750 4:5, uploaded to Storage at {uuid}-headshot.jpg; Storage URLs + sources + licenses + KEEP/re-crop status recorded for Task 2; any genuine gap documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 1002 (audit-only) — INSERT/UPDATE politician_images + photo_origin_url backfill + full-roster image audit; confirm ledger unchanged; COMMIT the file to EV-Accounts</name>
  <files>C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql (INSERT/UPDATE politician_images + photo_origin_url backfill + audit-only apply, ledger-unchanged check, full-roster coverage audit, documented-gap handling)
    - .planning/phases/151-el-monte-deep-seed/151-RESEARCH.md (§D-04 headshot audit, §Validation Architecture ELMN-01h, §Assumptions A3)
    - .planning/phases/151-el-monte-deep-seed/151-02-SUMMARY.md (final link state + Cortez's resolved ext_id)
    - ~/.claude/commands/find-headshots.md (skill reference)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql: for Cortez (new) and any of the 6
    pre-existing that were re-cropped in Task 1, write a politician_images row (resolved by external_id; Cortez
    via her Plan 02 ext_id) url = the Storage public URL, type='default', photo_license='press_use', guarded
    WHERE NOT EXISTS on politician_id for Cortez (INSERT) and an UPDATE of the existing row's url/photo_license
    for any re-cropped pre-existing member (each member ends with exactly one canonical type='default' row
    pointing at {uuid}-headshot.jpg); leave KEPT pre-existing rows untouched (no-op). UPDATE
    essentials.politicians SET photo_origin_url = the documentId URL WHERE external_id=<id> AND photo_origin_url
    IS NULL. Apply via raw SQL (mcp__supabase-local execute_sql) — do NOT run through the schema_migrations ledger
    (audit-only). Then run the full-roster coverage audit: LEFT JOIN the 7 current El Monte officials (-201202,
    -201204, 657390, 657386, -201203, Cortez ext_id, -200669) to politician_images (type='default') and report
    who has exactly one type='default' row vs a documented gap; confirm no member has >1. Verify MAX(version) in
    schema_migrations is UNCHANGED by this file (stays 1001). Then COMMIT the file to EV-Accounts: git -C
    "C:/EV-Accounts" add backend/migrations/1002_elmonte_headshots.sql && git -C "C:/EV-Accounts" commit -m
    "feat(151): El Monte headshots — Cortez D6 + re-crops, audit-only" (feedback_no_git_in_ev_accounts).
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql "SELECT p.external_id, COUNT(pi.*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (-201202,-201204,657390,657386,-201203,<CORTEZ_EXT_ID>,-200669) GROUP BY p.external_id;" shows each member with COUNT 0 (documented gap) or 1 (no member has >1) AND "SELECT MAX(version::int) FROM supabase_migrations.schema_migrations;" is unchanged (stays 1001). Also: git -C "C:/EV-Accounts" log --oneline -1 -- backend/migrations/1002_elmonte_headshots.sql shows the commit</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 7 current members with a verified portrait has exactly one type='default' row at the canonical {uuid}-headshot.jpg path (Cortez new; the 6 pre-existing kept or re-cropped); photo_license='press_use'; photo_origin_url populated with the documentId URL
    - No member has >1 type='default' row; members with no obtainable verified portrait documented as honest gaps (not faked)
    - schema_migrations MAX(version) unchanged by migration 1002 (audit-only confirmed; stays 1001)
    - Migration is idempotent (re-apply = 0 rows changed)
    - Migration file 1002_elmonte_headshots.sql committed to the EV-Accounts repo
  </acceptance_criteria>
  <done>1002 applied (audit-only) + committed to EV-Accounts; the 7 current members each have exactly one default image (or a documented honest gap); Cortez new + the 6 pre-existing reconciled; photo_license='press_use'; no member >1; ledger untouched (stays 1001).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify every El Monte headshot (correct person for all 7, Cortez present, clean 4:5 crop, no overlays)</name>
  <action>Pause for the user to visually verify each El Monte portrait per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>Verified official headshots for the 7 current El Monte members (D1 Crippen-Thomas, D2 Herrera, D3 Ruedas, D4 Longoria, D5 Galvan, D6 Cortez, Mayor Ancona): Cortez sourced fresh from ci.el-monte.ca.us documentId=7435 (greenfield); the 6 pre-existing images verified and kept-or-re-cropped from their documentIds (all direct curl, NO-WAF); all uploaded ones processed to 600×750 4:5 and live in Supabase Storage + politician_images (type='default', photo_license='press_use'). Any honest gaps documented.</what-built>
  <how-to-verify>
    1. Confirm each member's photo is the CORRECT person (cross-check all 7 against ci.el-monte.ca.us council pages).
    2. CRITICAL: confirm the D6 photo is Marisol Cortez (newly seated in Plan 02) and that she is shown as a Councilmember, NOT in a Mayor role (she lost the 2024 mayor race).
    3. Confirm Mayor Ancona's photo is the current portrait (documentId=7434, not the older 4243).
    4. Confirm no superimposed text/graphics over any face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    5. Confirm each member shows exactly one canonical image (no duplicate type='default' rows) on the El Monte browse view. Any documented gap is genuinely imageless (not faked).
    6. Confirm the on-disk counter is authoritative: schema_migrations MAX(version) is unchanged (still 1001; the headshot file did not register) and 1002 is committed to EV-Accounts.
  </how-to-verify>
  <resume-signal>Type "approved" or name which photo(s) need re-sourcing / re-cropping / identity-fix.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with a NO-WAF official image source. No untrusted user input reaches the DB — uploads +
image-row writes use service-role/operator against production. Threat surface = wrong-person photo (a stale
pre-existing image being the wrong person, or Cortez confused with a Mayor role), a distorted/overlaid photo, a
fabricated/campaign photo filling a real gap, a mislabeled photo_license, fetching from elmonteca.gov instead of
ci.el-monte.ca.us (Pitfall 7), using the older Ancona 4243 instead of 7434 (Pitfall 6), or accidental ledger
registration — mitigated by the pre-flight coverage + dimension check, the identity-verify-before-upload rule
(esp. the 6 pre-existing + Cortez), the NO-WAF direct-curl from ci.el-monte.ca.us, the crop-then-resize/no-stretch
rule, the press_use license, a blocking human-verify checkpoint that checks each of the 7 + Cortez≠Mayor, the
honest-gap rule, and the audit-only raw-SQL apply.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ci.el-monte.ca.us (NO-WAF) | Official source — direct curl HTTP 200; use ci.el-monte.ca.us NOT elmonteca.gov (Pitfall 7); each image human-verified before upload. |
| Ballotpedia / campaign / /find-headshots -> local processing | Fallback sources only if the official portrait is unusable; all human-verified before upload. |
| operator -> Supabase Storage + production DB | Uploads + image-row INSERT/UPDATE against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Tampering | Idempotent guarded writes — duplicate image row for any member; audit-only migration accidentally registered in schema_migrations | mitigate | Cortez INSERT guarded WHERE NOT EXISTS on politician_id; re-cropped pre-existing handled via UPDATE; KEPT rows untouched; post-audit confirms ≤1 type='default' per official; apply via raw SQL only, verify MAX(version) unchanged (stays 1001) |
| T-151-03 | Spoofing | Identity-verified images — wrong-person photo (a stale pre-existing image is the wrong person, or Cortez confused with the mayoral role she lost; Pitfall 1); fetching from elmonteca.gov not ci.el-monte.ca.us (Pitfall 7); older Ancona 4243 vs 7434 (Pitfall 6) | mitigate | Identity-verify each of the 6 pre-existing + Cortez before keep/upload; direct curl from ci.el-monte.ca.us documentIds (7429-7435); prefer Ancona 7434; blocking human-verify checks each of the 7 + confirms D6 is Cortez as Councilmember |
| T-151-03b | Tampering | Image stretched / aspect-distorted / text-overlaid; fabricated/wrong campaign photo filling a gap; mislabeled photo_license | mitigate | Crop-to-4:5-FIRST-then-resize rule + 600×750 dimension check; reject superimposed-graphics sources; honest-gap rule (CONTEXT D-04) — never seed a wrong-person/overlaid photo as type='default'; photo_license='press_use' for the official city portrait (per real source) |
| T-151-SC | Tampering | npm/pip/cargo installs | accept | No package installs (existing Pillow + Supabase client; RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After 1002 + checkpoint:
1. each current member with a verified portrait has exactly one type='default' image at {uuid}-headshot.jpg; photo_origin_url (documentId URL) + photo_license='press_use' set
2. full-roster coverage: every member is either imaged (exactly one default) or a documented honest gap; no member has >1
3. D6 is verified Marisol Cortez (Councilmember, not a Mayor role); Mayor Ancona is the current 7434 portrait
4. schema_migrations MAX(version) unchanged by 1002 (stays 1001); 1002 committed to EV-Accounts
5. human checkpoint approved (correct person each, no overlays, correct crop)
</verification>

<success_criteria>
Every current El Monte member with an obtainable correct photo has a single 600×750 verified image at the
canonical path; Cortez sourced fresh from ci.el-monte.ca.us documentId=7435, the 6 pre-existing verified and
kept-or-re-cropped (all NO-WAF direct curl); no wrong-person/overlaid/stretched photos; Cortez shown as
Councilmember (not the mayoral role she lost); photo_license='press_use'; genuine gaps documented honestly;
the ledger counter is preserved (stays 1001) and the file committed to EV-Accounts.
</success_criteria>

<output>
Create `.planning/phases/151-el-monte-deep-seed/151-03-SUMMARY.md` when done (record any honest image gaps, which of the 6 pre-existing were KEPT vs re-cropped, and the per-member source/license).
</output>
</content>
