---
phase: 201-riverside-county-board-of-supervisors-deep-seed
plan: 06
type: execute
wave: 6
depends_on: ["201-01", "201-02", "201-03", "201-04", "201-05"]
files_modified: []
autonomous: false
requirements: [CV-01, BANR-01]
must_haves:
  truths:
    - "The full production audit block is all-green: 5 X0021 geofences, standalone Riverside County government, 5 supervisor offices (1 per district), board-only (no constitutional officer), 5/5 600×750 headshots, evidence-only cited stances, clean section-split, DB-honest coverage chip, banner CDN 200"
    - "A live address in each of the 5 districts routes to exactly 1 correct supervisor with the right headshot"
    - "The Riverside County banner renders via the coverage.js browse chip"
  artifacts:
    - path: ".planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-06-SUMMARY.md"
      provides: "Full audit results + live-browse sign-off; the phase verification record"
      contains: "audit"
  key_links:
    - from: "live address per district"
      to: "one Riverside County supervisor profile"
      via: "X0021 geofence PIP routing (X% MTFCC catchall)"
      pattern: "supervisor"
    - from: "coverage.js 'Riverside County' chip"
      to: "downtown Riverside / Mission Inn banner render"
      via: "browse_label → representingCity → getBuildingImages"
      pattern: "riverside county"
---

<objective>
Final phase verification. Run the full production audit block covering every prior wave (geofences,
government/roster, headshots, stances, banner, coverage) and a blocking live-browse human-verify that
confirms per-district address routing, correct-person headshots, and the banner rendering via the
coverage chip. This is the goal-backward gate for ROADMAP success criteria #1-5 before
`/gsd-verify-work`.

Purpose: Proves CV-01 + BANR-01 are TRUE end-to-end in production — not just that migrations were
authored, but that a real Riverside County resident sees their one supervisor with a full compass, a
correct headshot, and the county banner. Board-only per D-01 (no constitutional officer expected).
Output: `.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-06-SUMMARY.md` — the
phase verification record with the audit results + operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-01-SUMMARY.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-02-SUMMARY.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-03-SUMMARY.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-04-SUMMARY.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-05-SUMMARY.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / live-browser access. Both tasks are ORCHESTRATOR-RUN /
human-verify: the inline orchestrator runs the `psql`/`curl` audit block, and the operator performs
the live browse on essentials.empowered.vote. No files are modified. Per
`feedback_no_playwright_on_user_live_browser`, the live browse is read-only + operator-driven — never
wipe localStorage on the live page.
</execution_note>

<interfaces>
- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- Live app: https://essentials.empowered.vote ; per-county browse via the coverage chip (browse_label
  'Riverside County', browseGovernmentList ['06065']).
- ext_ids -4010001..-4010005 (Medina/Spiegel/Washington/Perez/Gutierrez).
- Section-split guard: no office reachable from the 5 X0021 districts under any government other than
  Riverside County; the pre-existing COUNTY G4020 06065 boundary row must NOT carry a supervisor.
- Chair annotation: exactly 1 office carries `(Chair)`, on the confirmed 2026 chair's D-seat
  (recon: Spiegel, -4010002, D2 — re-verified at Plan 02 Task 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full production audit block (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB/CDN assertions; no files modified</files>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (§ code_context Integration Points — PIP routing, coverage chip reconciled against real DB stance counts, section-split)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (§ Section-split / district-collision guard — X0021 LOCAL scoping; never bare geo_id='06065')
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-02-SUMMARY.md (the confirmed chair seat + roster manifest for the Chair-annotation assertion)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). Run the full audit via `psql "$DATABASE_URL" -tAc`
    + `curl -sI`, recording each result in 201-06-SUMMARY.md:
    (a) 5 geofences mtfcc='X0021' state='ca', geo_id riverside-ca-supervisor-district-1..5, all
    ST_IsValid;
    (b) exactly 1 standalone government 'Riverside County, California, US' (geo_id 06065, type County),
    NOT parented to State of California;
    (c) 5 offices under 'Board of Supervisors', each LOCAL X0021 district holding exactly 1 office
    (GROUP BY HAVING count<>1 → 0); no constitutional-officer office (board-only, D-01);
    (d) appointed count among the 5 = the confirmed count (0 per recon);
    (e) 5/5 politician_images rows; `curl -sI` each of the 5 CDN URLs → HTTP 200; PIL-verify a sample
    is 600×750;
    (f) stances: 0 uncited (every answer has a context row with non-empty sources), 0 judicial-* rows,
    all values in [1.0,5.0] (do NOT assert a fixed per-supervisor count — honest blanks expected);
    (g) section-split: 0 offices reachable from the 5 X0021 districts under any non-Riverside government;
    (h) Chair annotation: exactly 1 office carries `(Chair)`, on the confirmed chair's D-seat
    (recon -4010002/D2);
    (i) coverage.js 'Riverside County' chip present with hasContext:true; buildingImages.js
    'riverside county' entry present; banner `curl -sI cities/riverside-county.jpg` → HTTP 200.
    ANY red assertion blocks phase verification — surface it for gap closure, do NOT hand-wave.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0021' AND state='ca')=5 AND (SELECT count(*) FROM essentials.governments WHERE name='Riverside County, California, US' AND geo_id='06065')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0021' AND d.district_type='LOCAL' AND d.state='ca')=5 AND (SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4010005 AND -4010001)=5 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0021' AND o.title LIKE '%(Chair)%')=1 AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN inform.compass_topics t ON t.id=pa.topic_id WHERE p.external_id BETWEEN -4010005 AND -4010001 AND t.topic_key LIKE 'judicial-%') AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id BETWEEN -4010005 AND -4010001 AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id AND array_length(pc.sources,1) IS NOT NULL))"</automated>
  </verify>
  <acceptance_criteria>
    - Combined boolean SELECT returns `t` (5 geofences + 1 standalone gov + 5 offices + 5 headshots + exactly 1 Chair annotation + 0 judicial stance rows + 0 uncited stance rows).
    - Each X0021 district holds exactly 1 office (no HAVING count<>1 violation); no constitutional-officer office (board-only).
    - Appointed count = confirmed (0 per recon); section-split query returns 0 rows.
    - `curl -sI` on the 5 headshot CDN URLs and the cities/riverside-county.jpg banner all return HTTP 200; a sampled headshot is 600×750.
    - coverage.js 'Riverside County' hasContext:true chip + buildingImages.js 'riverside county' entry present.
    - All audit checks recorded green in 201-06-SUMMARY.md.
  </acceptance_criteria>
  <done>Full production audit is all-green across geofences, government/roster (board-only), headshots, stances, banner, and coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-browse verification — routing + correct-person photos + banner render</name>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (§ code_context Integration Points — per-district routing; § Recon facts — the 5-member roster + chair to spot-check)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (lines 344-350 — the banner renders via the coverage chip, NOT an arbitrary address; not a defect)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor/DB action beyond the operator's live browse. The operator confirms per-district routing, correct-person headshots, full compass, and banner render on essentials.empowered.vote (read-only browse; never wipe localStorage per feedback_no_playwright_on_user_live_browser). See &lt;how-to-verify&gt;.</action>
  <what-built>
    All 5 supervisors are seeded with offices on their own X0021 district geofences, 600×750 headshots,
    evidence-only stances, a Riverside County banner, and a coverage chip. This checkpoint confirms a
    real resident's experience: address → their ONE supervisor, right person, right photo, compass
    populated, and the county banner rendering via the browse chip.
  </what-built>
  <how-to-verify>
    1. On https://essentials.empowered.vote, enter a known full street address in each of the 5
       supervisor districts (one per district) and confirm exactly ONE Riverside County supervisor
       appears, labeled the correct district, with the correct current person (Medina D1 / Spiegel D2 /
       Washington D3 / Perez D4 / Gutierrez D5 — or the confirmed current roster).
    2. Confirm each supervisor's headshot depicts the RIGHT current person and their compass shows
       evidence-only stances (cited, honest blanks where no evidence). Confirm the D2 (Spiegel, or
       confirmed chair) seat shows the "(Chair)" title annotation and NO constitutional-officer profile
       appears (board-only, D-01).
    3. Browse Riverside County via the coverage chip (browse_label 'Riverside County') and confirm the
       downtown Riverside / Mission Inn banner renders (it renders via the browse chip, not necessarily
       via a raw Riverside-County address — expected, not a defect).
    4. Report any wrong-district routing, wrong-person photo, missing compass, or banner failure as a
       gap for closure.
  </how-to-verify>
  <acceptance_criteria>
    - Each of the 5 sampled district addresses returns exactly 1 correctly-labeled supervisor (the right current person).
    - All 5 headshots depict the correct current person; the confirmed chair's seat shows the "(Chair)" annotation; no constitutional-officer profile appears.
    - Each supervisor's compass shows evidence-only cited stances with honest blanks.
    - The Riverside County banner renders via the coverage-chip browse.
    - Operator sign-off recorded in 201-06-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once per-district routing, correct-person headshots, populated compasses, and the banner render are all confirmed live — or list the gaps to close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production DB/CDN → audit | Read-only assertions observe the live end-state |
| live app → operator | The operator observes real routing/rendering behavior on essentials.empowered.vote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-FALSE | Repudiation | audit false-positive (authored-not-applied) | mitigate | Audit reads live production tables + CDN 200s, not files; any red assertion blocks the phase |
| T-201-WRONG | Spoofing | wrong-but-present photo / wrong-district routing | mitigate | Task 2 human live-browse spot-checks person + district (row-count cannot detect this) |
| T-201-MISFLAG | Tampering | audit misflag (topic-count / bare-06065) | mitigate | Audit scopes to non-judicial topics and X0021 LOCAL districts only so honest completeness is not misread as a gap; never joins bare geo_id='06065' |
| T-201-LSWIPE | Tampering | live-browser localStorage wipe | accept | Read-only operator browse only; no Playwright localStorage manipulation on the live page (feedback_no_playwright_on_user_live_browser) |
| T-201-SC | Tampering | package installs | accept | No installs — read-only psql/curl; Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Full audit all-green (5 geofences / standalone gov / 5 offices 1-per-district board-only / appointed=0 / 5-5 headshots / cited non-judicial stances / clean section-split / exactly-1 Chair annotation / DB-honest chip / banner 200).
- Live per-district routing → 1 correct supervisor each; correct-person headshots; populated compasses; banner renders via the browse chip.
- Operator sign-off recorded.
</verification>

<success_criteria>
All 5 ROADMAP success criteria are TRUE end-to-end in production and operator-confirmed live — CV-01 +
BANR-01 satisfied, phase ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-06-SUMMARY.md` when
done — the phase verification record: the full audit results (all checks) + the live-browse operator
sign-off.
</output>
</content>
