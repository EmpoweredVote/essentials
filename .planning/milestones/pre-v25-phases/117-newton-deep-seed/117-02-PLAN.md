---
phase: 117-newton-deep-seed
plan: 02
type: execute
wave: 2
depends_on:
  - 117-01
files_modified:
  - C:/EV-Accounts/backend/migrations/579_newton_school_committee.sql
autonomous: true
requirements:
  - NEWTON-01

must_haves:
  truths:
    - "A Newton, MA address returns a SCHOOL section with 8 elected School Committee members + Mayor Laredo (ex officio)"
    - "Newton Public Schools government row exists in essentials.governments"
    - "G5420 geofence for geo_id='2508610' exists in geofence_boundaries"
    - "Newton school section-split returns 0 (geo_id=2508610, mtfcc=G5420)"
    - "Mayor Laredo has TWO offices in DB: LOCAL_EXEC from migration 578 AND SCHOOL ex-officio from migration 579"
    - "Mayor Laredo's office_id still points to his LOCAL_EXEC office (not overwritten by migration 579 back-fill)"
    - "8 new SC politicians seeded (external_ids -2508610001 through -2508610008); Mayor NOT re-seeded"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/579_newton_school_committee.sql"
      provides: "Newton School Committee seed — 8 elected + Mayor ex-officio linkage"
      contains: "Newton Public Schools, Massachusetts, US"
    - path: "essentials.geofence_boundaries (DB)"
      provides: "G5420 geofence for Newton school district"
      contains: "geo_id='2508610', mtfcc='G5420', state='25'"
    - path: "essentials.politicians (DB)"
      provides: "8 new SC members"
      contains: "external_id BETWEEN -2508610008 AND -2508610001"
    - path: "essentials.offices (DB)"
      provides: "9 SCHOOL offices (8 elected + 1 Mayor ex-officio)"
      contains: "district linked to geo_id='2508610'"
  key_links:
    - from: "essentials.offices (Mayor ex-officio)"
      to: "essentials.politicians (external_id=-2545560001)"
      via: "subquery SELECT id FROM essentials.politicians WHERE external_id = -2545560001"
      pattern: "Cross-join existing politician; no new INSERT"
    - from: "essentials.offices"
      to: "essentials.districts"
      via: "district_id JOIN on geo_id='2508610', district_type='SCHOOL'"
      pattern: "district_type = 'SCHOOL'"
---

<objective>
Seed Newton Public Schools school committee: G5420 geofence + government + chamber + SCHOOL district + 8 elected members (new politicians) + Mayor Laredo ex-officio linkage (no new politician row — reuses external_id=-2545560001 from migration 578). Migration 579 is the complete deliverable.

Purpose: Establishes the Newton SCHOOL section so any Newton address returns the full 9-member School Committee. Migration MUST NOT re-seed Mayor Laredo — it links his existing politician_id to a second SCHOOL district office.

Output: migration 579_newton_school_committee.sql applied to production; G5420 geofence + 1 government + 1 chamber + 1 SCHOOL district + 8 new politicians + 9 offices (8 elected + 1 Mayor ex-officio); Mayor's office_id unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-newton-deep-seed/117-RESEARCH.md
@.planning/phases/117-newton-deep-seed/117-01-SUMMARY.md

<interfaces>
<!-- Reference migration the executor MUST read before writing migration 579. -->
<!-- Pattern source: Boston School Committee (migration 348) — G5420 geofence, SCHOOL district, ex-officio pattern -->
C:/EV-Accounts/backend/migrations/348_boston_school_committee.sql

<!-- Key structural conventions extracted from 348: -->
<!-- Step 0: INSERT geofence_boundaries (geo_id, mtfcc='G5420', state='25') WHERE NOT EXISTS -->
<!-- Step 1: governments WHERE NOT EXISTS on name -->
<!-- Step 2: chambers — NO slug column (GENERATED ALWAYS) -->
<!-- Step 3: districts (district_type='SCHOOL', state='ma', geo_id, label, mtfcc='G5420') -->
<!-- Step 4: politician+office WITH ins_p pattern; is_appointed=false for elected members -->
<!-- Step 5: office_id back-fill UPDATE — WHERE external_id IN SC range AND office_id IS NULL -->
<!-- Step 6: Post-verification DO block -->
<!-- Step 7: schema_migrations ledger entry -->

<!-- CRITICAL DIFFERENCE from 348: Mayor Laredo block (9th office) must NOT insert a new politician -->
<!-- Instead, use a SELECT subquery on existing external_id=-2545560001 (seeded in migration 578) -->
<!-- The office_id back-fill in Step 5 must EXCLUDE external_id=-2545560001 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 579 — Newton School Committee</name>
  <files>C:/EV-Accounts/backend/migrations/579_newton_school_committee.sql</files>

  <read_first>
    C:/EV-Accounts/backend/migrations/348_boston_school_committee.sql — primary pattern for G5420 geofence, SCHOOL district, chamber, and politician+office blocks.
    .planning/phases/117-newton-deep-seed/117-RESEARCH.md — school committee roster, SC external_id assignments, Mayor ex-officio pattern, Pitfalls 2/3/4.
    .planning/phases/117-newton-deep-seed/117-01-SUMMARY.md — confirms Mayor Laredo's external_id=-2545560001 is seeded; needed for the ex-officio subquery.
  </read_first>

  <action>
Create C:/EV-Accounts/backend/migrations/579_newton_school_committee.sql modeled on 348_boston_school_committee.sql with the following Newton-specific values:

HEADER COMMENT — include:
- Migration 579: Newton Public Schools school committee (NEWTON-01)
- geo_id='2508610' (NCES LEAID for Newton Public Schools, MA FIPS=25)
- G5420 geofence inserted directly (no MA G5420 TIGER loader)
- 8 new politicians (-2508610001..-2508610008, is_appointed=false, elected)
- Mayor Laredo (external_id=-2545560001) linked via subquery — NO new politician INSERT
- 9 offices total in SCHOOL district (8 elected + 1 Mayor ex-officio)
- office_id back-fill excludes external_id=-2545560001 (Mayor's office_id must remain LOCAL_EXEC from migration 578)
- CRITICAL: district_type='SCHOOL' (NOT 'SCHOOL_DISTRICT')
- CRITICAL: districts.state='ma' lowercase; governments.state='MA' uppercase; offices.representing_state='MA' uppercase
- CRITICAL: slug is GENERATED ALWAYS on chambers — never include in INSERT column list
- CRITICAL: essentials.governments has NO unique constraint — WHERE NOT EXISTS guard on name

PRE-FLIGHT BLOCK 1 — RAISE EXCEPTION if government already exists:
  SELECT COUNT(*) FROM essentials.governments WHERE name = 'Newton Public Schools, Massachusetts, US'
  If > 0: RAISE EXCEPTION 'Migration 579 already applied — aborting re-run'

PRE-FLIGHT BLOCK 2 — RAISE EXCEPTION if SC external_id block not clear:
  SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2508610008 AND -2508610001
  If > 0: RAISE EXCEPTION with count

PRE-FLIGHT BLOCK 3 — RAISE EXCEPTION if Mayor Laredo (external_id=-2545560001) is NOT found:
  SELECT COUNT(*) FROM essentials.politicians WHERE external_id = -2545560001
  If = 0: RAISE EXCEPTION 'Pre-flight FAILED: Mayor Laredo (external_id=-2545560001) not found — run migration 578 first'

Do NOT wrap in BEGIN/COMMIT (matching 348 pattern — autocommit).

STEP 0 — Insert G5420 geofence:
  INSERT INTO essentials.geofence_boundaries (geo_id, mtfcc, state)
  SELECT '2508610', 'G5420', '25'
  WHERE NOT EXISTS (SELECT 1 FROM essentials.geofence_boundaries WHERE geo_id = '2508610' AND mtfcc = 'G5420')

STEP 1 — Government row:
  INSERT (id, name, type, state, city, geo_id)
  VALUES (gen_random_uuid(), 'Newton Public Schools, Massachusetts, US', 'LOCAL', 'MA', NULL, '2508610')
  WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name = 'Newton Public Schools, Massachusetts, US')

STEP 2 — School Committee chamber:
  INSERT (id, name, name_formal, government_id)
  name='School Committee', name_formal='Newton School Committee'
  government_id = (SELECT id FROM essentials.governments WHERE name = 'Newton Public Schools, Massachusetts, US')
  WHERE NOT EXISTS on (name, government_id)
  NEVER include slug.

STEP 3 — SCHOOL district row:
  INSERT (id, district_type, state, geo_id, label, mtfcc)
  district_type='SCHOOL', state='ma', geo_id='2508610', label='Newton Public Schools', mtfcc='G5420'
  WHERE NOT EXISTS on (geo_id, district_type, state)

STEP 4 — 8 elected politician+office blocks (Ward 1-8, is_appointed=false):

BLOCK 1: Arrianna Proia (Ward 1) — external_id=-2508610001
  full_name='Arrianna Proia', first_name='Arrianna', last_name='Proia'
  is_appointed=false; title='School Committee Member'

BLOCK 2: Linda Swain (Ward 2) — external_id=-2508610002
  full_name='Linda Swain', first_name='Linda', last_name='Swain'
  is_appointed=false; title='School Committee Member'

BLOCK 3: Jason Bhardwaj (Ward 3, Vice Chair) — external_id=-2508610003
  full_name='Jason Bhardwaj', first_name='Jason', last_name='Bhardwaj'
  is_appointed=false; title='School Committee Vice Chair'

BLOCK 4: Tamika Olszewski (Ward 4) — external_id=-2508610004
  full_name='Tamika Olszewski', first_name='Tamika', last_name='Olszewski'
  is_appointed=false; title='School Committee Member'

BLOCK 5: Ben Schlesinger (Ward 5) — external_id=-2508610005
  full_name='Ben Schlesinger', first_name='Ben', last_name='Schlesinger'
  is_appointed=false; title='School Committee Member'

BLOCK 6: Jonathan Greene (Ward 6) — external_id=-2508610006
  full_name='Jonathan Greene', first_name='Jonathan', last_name='Greene'
  is_appointed=false; title='School Committee Member'

BLOCK 7: Alicia Piedalue (Ward 7, Chair) — external_id=-2508610007
  full_name='Alicia Piedalue', first_name='Alicia', last_name='Piedalue'
  is_appointed=false; title='School Committee Chair'

BLOCK 8: Victor Lee (Ward 8) — external_id=-2508610008
  full_name='Victor Lee', first_name='Victor', last_name='Lee'
  is_appointed=false; title='School Committee Member'

Each of Blocks 1-8 uses the standard 348 pattern:
  WITH ins_p AS (INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id)
    VALUES (gen_random_uuid(), ..., NULL, true, false, false, true, -XXXX)
    ON CONFLICT (external_id) DO NOTHING RETURNING id)
  INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)
  SELECT gen_random_uuid(), d.id, (chamber_subquery), p.id, 'title', 'MA', false, false, NULL
  FROM essentials.districts d CROSS JOIN ins_p p
  WHERE d.geo_id = '2508610' AND d.district_type = 'SCHOOL' AND d.state = 'ma'
    AND p.id IS NOT NULL
    AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id)

chamber_subquery for all blocks:
  (SELECT id FROM essentials.chambers WHERE name = 'School Committee'
   AND government_id = (SELECT id FROM essentials.governments WHERE name = 'Newton Public Schools, Massachusetts, US'))

BLOCK 9 (Mayor ex-officio) — DO NOT INSERT a new politician row. Use subquery on existing politician:
  INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical)
  SELECT gen_random_uuid(), d.id, (chamber_subquery), p.id, 'Mayor (ex officio)', 'MA', false, false, NULL
  FROM essentials.districts d
  CROSS JOIN (SELECT id FROM essentials.politicians WHERE external_id = -2545560001) p
  WHERE d.geo_id = '2508610' AND d.district_type = 'SCHOOL' AND d.state = 'ma'
    AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.politician_id = p.id)

STEP 5 — office_id back-fill (8 SC members ONLY — NOT Mayor):
  UPDATE essentials.politicians p SET office_id = o.id
  FROM essentials.offices o
  WHERE o.politician_id = p.id
    AND p.external_id BETWEEN -2508610008 AND -2508610001
    AND p.office_id IS NULL
  NOTE: external_id=-2545560001 (Mayor) is intentionally excluded from this range.
  Mayor's office_id must continue pointing to his LOCAL_EXEC office from migration 578.

STEP 6 — Post-verification DO block with 9 gates:
  Gate (a): governments WHERE name='Newton Public Schools, Massachusetts, US' = 1
  Gate (b): chambers WHERE name='School Committee' AND government_id=(NPS gov) = 1
  Gate (c): districts WHERE district_type='SCHOOL' AND state='ma' AND geo_id='2508610' = 1
  Gate (d): politicians WHERE external_id BETWEEN -2508610008 AND -2508610001 = 8
  Gate (e): offices JOIN districts WHERE district_type='SCHOOL' AND geo_id='2508610' AND state='ma' = 9
  Gate (f): section-split: geofence_boundaries WHERE geo_id='2508610' AND mtfcc='G5420' AND NOT EXISTS (districts WHERE geo_id='2508610' AND district_type='SCHOOL' AND state='ma') = 0
  Gate (g): politicians WHERE external_id BETWEEN -2508610008 AND -2508610001 AND office_id IS NULL = 0
  Gate (h): geofence_boundaries WHERE geo_id='2508610' AND mtfcc='G5420' AND state='25' = 1
  Gate (i): Mayor office_id still points to LOCAL_EXEC district:
    SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.id = p.office_id
    JOIN essentials.districts d ON d.id = o.district_id
    WHERE p.external_id = -2545560001 AND d.district_type = 'LOCAL_EXEC' AND d.geo_id = '2545560'
    Expected: 1 (if <> 1, RAISE EXCEPTION 'Mayor Laredo office_id back-fill overwrote LOCAL_EXEC — CRITICAL BUG')

STEP 7 — Ledger entry:
  INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('579') ON CONFLICT (version) DO NOTHING
  </action>

  <verify>
    <automated>
      Apply migration via mcp__supabase-local execute_sql. Post-verification DO block raises EXCEPTION on any of 9 gate failures. Migration succeeds = all 9 gates passed.

      Critical spot-checks after apply:
        SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = '2508610' AND d.district_type = 'SCHOOL' AND d.state = 'ma';
        -- Expected: 9 (8 elected + 1 Mayor ex-officio)

        SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2508610008 AND -2508610001;
        -- Expected: 8 (Mayor NOT in this range)

        SELECT p.full_name FROM essentials.politicians p JOIN essentials.offices o ON o.id = p.office_id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id = -2545560001 AND d.district_type = 'LOCAL_EXEC';
        -- Expected: 'Marc C. Laredo' (confirms office_id was NOT overwritten)

        SELECT COUNT(*) FROM essentials.politicians WHERE external_id = -2545560001;
        -- Expected: 1 (Mayor seeded exactly once, not twice)

        SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id = '2508610' AND gb.mtfcc = 'G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.district_type = 'SCHOOL' AND d.state = 'ma');
        -- Expected: 0 (section-split clean)
    </automated>
  </verify>

  <done>
    Migration 579 applied without errors; post-verification emits PASSED notice with all 9 gates; 8 new SC politicians in DB; 9 SCHOOL offices present; Mayor seeded exactly once total; Mayor's office_id confirmed pointing to LOCAL_EXEC; G5420 geofence present with state='25'; section-split returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → Supabase production | Data inserted directly via mcp__supabase-local; no user input path |
| Migration 579 → migration 578 state | Depends on Mayor external_id=-2545560001 existing; pre-flight gate enforces this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-03 | Tampering | Mayor Laredo dual-insert risk | mitigate | Pre-flight gate 3 asserts external_id=-2545560001 exists; Block 9 uses subquery (not INSERT); no ON CONFLICT needed |
| T-117-04 | Tampering | office_id overwrite (back-fill must skip Mayor) | mitigate | UPDATE range explicitly excludes -2545560001; gate (i) in post-verification catches any overwrite immediately |
| T-117-05 | Tampering | Wrong geo_id on SCHOOL district | mitigate | Pre-flight + post-verification gates explicitly check geo_id='2508610' not '2545560' |
| T-117-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After migration 579 applies successfully:

1. Newton Public Schools government row exists (name = 'Newton Public Schools, Massachusetts, US')
2. School Committee chamber exists (name_formal = 'Newton School Committee')
3. G5420 geofence present (geo_id='2508610', mtfcc='G5420', state='25')
4. 1 SCHOOL district row (geo_id='2508610', state='ma', district_type='SCHOOL')
5. 8 new politicians in external_id range -2508610008 to -2508610001
6. 9 offices linked to SCHOOL district (8 elected + 1 Mayor ex-officio)
7. Section-split check: 0 rows for geo_id='2508610', mtfcc='G5420'
8. Mayor Laredo (external_id=-2545560001) still has office_id pointing to LOCAL_EXEC district
9. Mayor appears in politicians table exactly once (not re-seeded)
</verification>

<success_criteria>
Migration 579 applied without error; post-verification DO block emits NOTICE with gov=1, chambers=1, districts=1, sc_politicians=8, total_school_offices=9, split_orphans=0, null_sc_office_ids=0, geo_count=1, mayor_local_exec_intact=1. Newton SCHOOL section is populated; Mayor Laredo's LOCAL_EXEC office_id is undisturbed.
</success_criteria>

<output>
Create .planning/phases/117-newton-deep-seed/117-02-SUMMARY.md when done.
</output>
