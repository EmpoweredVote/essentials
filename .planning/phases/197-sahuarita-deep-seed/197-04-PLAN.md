---
phase: 197-sahuarita-deep-seed
plan: 04
type: execute
wave: 4
depends_on: ["197-03"]
files_modified:
  - src/lib/buildingImages.js
  - src/lib/coverage.js
autonomous: false
requirements: [BANR-01, SUB-03]
must_haves:
  truths:
    - "A licensed Sahuarita community banner (real photo, no AI, no aerial) is processed to the banner spec (1700×540 @ 3.15:1) and uploaded to Storage at cities/sahuarita.jpg"
    - "buildingImages.js CURATED_LOCAL has a state-scoped single-word `sahuarita` entry pointing at the uploaded banner"
    - "coverage.js gets a 'Sahuarita' area APPENDED to the EXISTING Arizona COVERAGE_STATES block (which today has 3 entries — Tucson, Oro Valley, Marana) — NOT a new Arizona block, and NOT an entry in COVERAGE_COUNTIES (where Pima County lives) — with a DB-honest chip (hasContext:true, since stances landed in Plan 03) browsing geo_id 0462140"
    - "The Sahuarita banner is a DISTINCT southern/Santa-Cruz-Valley identity — MUST avoid Catalina-range shots (collide with Pima County 193 + Oro Valley 195) and Tortolita/Dove Mountain (Marana 196); distinct also from the Tucson downtown streetscape and the AZ-state Phoenix skyline"
    - "A real Sahuarita street address, queried against the live candidates-search API, resolves to the seeded Town of Sahuarita council of 7 at-large members (the phase-goal's resident-facing outcome, end-to-end)"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "CURATED_LOCAL `sahuarita` → uploaded banner URL"
      contains: "sahuarita"
    - path: "src/lib/coverage.js"
      provides: "'Sahuarita' area appended to the EXISTING Arizona COVERAGE_STATES block, browseGovernmentList ['0462140'], hasContext:true"
      contains: "Sahuarita"
  key_links:
    - from: "coverage.js COVERAGE_STATES Arizona → 'Sahuarita'"
      to: "buildingImages.js CURATED_LOCAL sahuarita"
      via: "browse label 'Sahuarita' → representingCity → getBuildingImages substring match"
      pattern: "sahuarita"
    - from: "buildingImages.js sahuarita"
      to: "Storage cities/sahuarita.jpg"
      via: "src URL (state-scoped 'AZ')"
      pattern: "cities/sahuarita.jpg"
---

<objective>
Give the Town of Sahuarita its licensed community banner (BANR-01) and its DB-honest coverage chip. Source
a real Sahuarita photo ONE AT A TIME (no AI, no aerial), process it to the banner spec via
`scripts/banners/process_banner.py` (1700×540 @ 3.15:1), upload to Storage at `cities/sahuarita.jpg` via
`upload_banner.py`, wire it into `src/lib/buildingImages.js` CURATED_LOCAL as a single state-scoped
`sahuarita` entry (single-word, unquoted key), and surface the Town of Sahuarita in `src/lib/coverage.js`.

CRITICAL DELTA (Pitfall 7): the Arizona `COVERAGE_STATES` block ALREADY EXISTS in `coverage.js` today with
THREE entries — `'Tucson'` (Phase 194), `'Oro Valley'` (Phase 195), and `'Marana'` (Phase 196). This phase
APPENDS a fourth `'Sahuarita'` area object to that EXISTING block's `areas` array. Do NOT create a second
`{ name: 'Arizona', ... }` block (that would be a duplicate-block bug), and do NOT add Sahuarita to
`COVERAGE_COUNTIES` (where Pima County lives — untouched). Likewise `buildingImages.js` is a plain APPEND
of a new `sahuarita` key (single-word → no quotes, mirror the `marana:` / `tucson:` style, NOT the quoted
multi-word `'oro valley'` style).

CRITICAL (D-03 / BANR-01 — visual distinctiveness): Sahuarita needs its own DISTINCT southern/Santa-Cruz-
Valley identity. The banner MUST avoid Catalina-range imagery (which belongs to the Pima County 193 and
Oro Valley 195 banners) AND Tortolita/Dove Mountain imagery (Marana 196) — either would visually collide
with a sibling banner. Candidate priority (RESEARCH Open Question 2): (1) pecan orchards (FICO / Green
Valley Pecan) — the most distinctive Sahuarita identity and D-03's front-runner, BUT the only Commons
photo found is low-res 532×800 portrait (>3× upscale) — the one-at-a-time sourcing pass should FIRST
attempt a fresh higher-resolution re-source (Flickr CC / direct Green Valley Pecan photography) before
accepting visible softness; (2) Sahuarita Lake / Rancho Sahuarita — the STRONGEST licensing-clean ready
candidate (Wikimedia Commons, Brian Basgen, CC BY-SA 3.0, 3008×1645, ground-level, landscape); (3) Santa
Rita Mountains; (4) Titan Missile Museum exterior. Present options for review; show the Pima Catalinas +
Oro Valley bridge + Marana Dove-Mountain + Tucson streetscape + AZ-state Phoenix banners alongside during
review per `feedback_banner_show_state_shot_on_conflict`. Source ONE candidate at a time
(`feedback_banner_source_one_at_a_time`).

Purpose: Satisfies BANR-01 + SUB-03 / ROADMAP success criteria #4 (licensed banner
sourced/processed/uploaded/wired) and #5 (Sahuarita surfaced with a DB-honest chip). This is a
Tucson-metro deep-seed banner that must stay visually distinct from Pima County (Catalina landscape), Oro
Valley (CDO Trail Bridge), Marana (Tortolita/Dove Mountain), Tucson (downtown streetscape), and the
AZ-state Phoenix skyline.
Output: uploaded `cities/sahuarita.jpg` + edited `buildingImages.js` + `coverage.js` in the `essentials`
repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md
@.planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md
@.planning/phases/197-sahuarita-deep-seed/197-CONTEXT.md

<execution_note>
Frontend files (`src/lib/buildingImages.js`, `src/lib/coverage.js`) live in the CURRENT `essentials` repo
(commit here, not `C:/EV-Accounts`). The banner SOURCING + processing + Storage upload are ORCHESTRATOR-RUN
(executor has no network/Storage access): the inline orchestrator sources the photo one-at-a-time, runs
the banner toolchain, and uploads. Per project convention (`feedback_banner_source_one_at_a_time`,
`feedback_banner_prefer_photo_over_gradient`, `feedback_banner_show_state_shot_on_conflict`,
`feedback_deepseed_community_banner`) banner sourcing is sequential, prefers a real photo, and shows the
sibling AZ banners alongside during review because there is a genuine visual-conflict risk. The CDN URL is
deterministic (`cities/sahuarita.jpg`) so the frontend wiring can be authored regardless of upload order.
Depends on Plan 03 — hasContext:true is only honest because Plan 03 seeded ≥1 stance row.
</execution_note>

<interfaces>
- Banner spec: process via `scripts/banners/process_banner.py` (`--url <src> --output <path>
  --vertical-anchor` tuned per shot) → 1700×540 @ 3.15:1, upload via `scripts/banners/upload_banner.py
  --file <path> --dest cities/sahuarita.jpg` (needs SUPABASE_SERVICE_ROLE_KEY from
  `C:/EV-Accounts/backend/.env`). Destination tier is `cities/` (banner TYPE, not government type). Storage
  is no-cache → overwrite = instant refresh.
- CDN URL: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/sahuarita.jpg
- buildingImages.js CURATED_LOCAL: add `sahuarita: { state: 'AZ', src: '<CDN URL>' }` (single-variant —
  `sahuarita` has no same-named-city collision, so NOT the array-of-{state,src} form; single-word key → NO
  quotes, mirror `marana:` / `tucson:`, NOT the quoted `'oro valley'` style) with an attribution comment
  (subject | author | license) above it that explicitly notes it is deliberately distinct from the Pima
  County Catalinas, the Oro Valley CDO Trail Bridge, the Marana Tortolita/Dove-Mountain, the Tucson
  downtown streetscape, and the AZ-state Phoenix skyline. getBuildingImages() needs NO code change. Storage
  file is `cities/sahuarita.jpg`; coverage.js label is 'Sahuarita'.
- coverage.js: APPEND one area object to the EXISTING Arizona COVERAGE_STATES block's `areas` array (which
  already holds 'Tucson' + 'Oro Valley' + 'Marana'):
  `{ label: 'Sahuarita', browseGovernmentList: ['0462140'], browseStateAbbrev: 'AZ', hasContext: true }`.
  hasContext:true is honest because Plan 03 seeded ≥1 stance row. Do NOT create a second Arizona block
  (Pitfall 7); do NOT touch COVERAGE_COUNTIES (Pima County lives there — unrelated). Placement within the
  areas array is cosmetic. browseGovernmentList value is the G4110 geo_id ('0462140').
- Banner candidates (RESEARCH Open Question 2 — visual selection done at execute time): front-runner =
  pecan orchards (FICO / Green Valley Pecan) — try a fresh higher-res re-source FIRST; confirmed-usable
  ready fallback = Sahuarita Lake (`Bbasgen-sahuarita-lake.JPG`, Wikimedia Commons, CC BY-SA 3.0, Brian
  Basgen, 3008×1645, ground-level, landscape — no Catalina/Tortolita overlap). Secondary: Santa Rita
  Mountains, Titan Missile Museum exterior.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Source + process + upload the Sahuarita banner, one at a time (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Standard Stack banner mechanism; § Open Question 2 — pecan-orchard low-res re-source risk + the Sahuarita Lake confirmed fallback; § Assumptions Log A5 — licensing/resolution)
    - .planning/phases/197-sahuarita-deep-seed/197-CONTEXT.md (D-03 — no AI, no aerial, NO Catalina-range, NO Tortolita/Dove-Mountain; pecan-orchard / Santa Rita / Sahuarita Lake / Titan Missile candidate subjects; show state shot if conflict)
    - .planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md (§ buildingImages.js mapping — the CURATED_LOCAL `sahuarita` single-word-key entry + distinctiveness attribution comment convention; the process_banner.py / upload_banner.py commands + cities/sahuarita.jpg destination)
    - C:/Transparent Motivations/essentials/scripts/banners/process_banner.py (the banner spec 1700×540 @ 3.15:1 + --vertical-anchor)
    - C:/Transparent Motivations/essentials/scripts/banners/upload_banner.py (the Storage upload + --dest convention)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. The orchestrator sources a licensed Sahuarita photo ONE AT A TIME (pecan-orchard fresh higher-res attempt first, Sahuarita Lake ready fallback), processes it to the banner spec (1700×540 @ 3.15:1), uploads to cities/sahuarita.jpg, and the operator visually approves — with explicit attention to NO Catalina-range and NO Tortolita/Dove-Mountain imagery (D-03) and distinctiveness from the Pima / Oro Valley / Marana / Tucson / AZ-state banners — before wiring. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A licensed community banner for the Town of Sahuarita — its own distinct southern/Santa-Cruz-Valley
    identity. RESEARCH's D-03 front-runner (pecan orchards, FICO/Green Valley Pecan) is the most distinctive
    subject but the only Commons photo found is low-res (532×800 portrait, >3× upscale) — a fresh
    higher-resolution re-source (Flickr CC / direct pecan-company photography) should be attempted first,
    with the Sahuarita Lake photo (Brian Basgen, CC BY-SA 3.0, 3008×1645) as the confirmed-usable ready
    fallback. Human visual QA is required because "real photo, no AI, no aerial, NO Catalina-range, NO
    Tortolita/Dove-Mountain, distinct from the sibling AZ banners" is a judgment a row-count cannot make.
  </what-built>
  <how-to-verify>
    1. The orchestrator FIRST attempts a fresh higher-resolution re-source of a Sahuarita pecan-orchard
       subject (Flickr CC / direct Green Valley Pecan photography) — the D-03 front-runner. If none is
       clearly licensed at adequate resolution for a 1700-wide crop, fall back to the confirmed Sahuarita
       Lake photo (Brian Basgen, CC BY-SA 3.0, 3008×1645). Source ONE candidate at a time (licensed
       CC/public-domain, real photo, NOT AI-generated, NOT aerial/drone, NOT Catalina-range, NOT
       Tortolita/Dove-Mountain), record subject | author | license, run `python
       scripts/banners/process_banner.py --url <src> --output /tmp/sahuarita.jpg --vertical-anchor 0.4`
       (tune anchor per shot), then `python scripts/banners/upload_banner.py --file /tmp/sahuarita.jpg
       --dest cities/sahuarita.jpg` (SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
    2. Operator visually confirms: reads as Sahuarita / Santa Cruz Valley, a real photo (no AI artifacts, no
       aerial/drone shot), contains NO Catalina-range imagery AND NO Tortolita/Dove-Mountain imagery (D-03),
       and is visually DISTINCT from the Pima County Catalina/Pusch-Ridge banner, the Oro Valley CDO Trail
       Bridge, the Marana Dove-Mountain shot, the Tucson downtown streetscape, AND the AZ-state Phoenix
       skyline — show all five alongside during review per feedback_banner_show_state_shot_on_conflict.
    3. If unusable, too similar to a sibling banner, or shows a forbidden range, the orchestrator re-sources
       another candidate ONE AT A TIME (prefer a real photo; gradient only if truly nothing usable exists,
       per feedback_banner_prefer_photo_over_gradient).
  </how-to-verify>
  <acceptance_criteria>
    - `curl -sI https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/sahuarita.jpg` returns HTTP 200.
    - The uploaded image matches the banner spec (process_banner.py output = 1700×540 @ 3.15:1) — PIL/curl-download dimension check.
    - Operator approves: real photo, Sahuarita/Santa-Cruz-Valley subject, no AI, no aerial, NO Catalina-range AND NO Tortolita/Dove-Mountain (D-03), distinct from the Pima Catalina banner AND the Oro Valley CDO bridge AND the Marana Dove-Mountain shot AND the Tucson streetscape AND the AZ-state Phoenix skyline.
    - Subject | author | license recorded in 197-04-SUMMARY.md for the attribution comment.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the operator confirms the uploaded banner reads as Sahuarita, is a real non-aerial photo with NO Catalina-range and NO Tortolita/Dove-Mountain imagery, and is distinct from the Pima County + Oro Valley + Marana + Tucson + AZ-state banners — or describe what to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner into buildingImages.js + append Sahuarita to the EXISTING Arizona coverage.js block</name>
  <files>src/lib/buildingImages.js, src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-PATTERNS.md (§ buildingImages.js mapping + § coverage.js mapping — the exact CURATED_LOCAL `sahuarita` single-word-unquoted entry + distinctiveness attribution comment, and the APPEND of the 4th 'Sahuarita' area to the EXISTING Arizona COVERAGE_STATES areas array; the Pitfall 7 warning that the Arizona block already exists with 3 entries and Pima is in COVERAGE_COUNTIES; getBuildingImages needs NO change)
    - C:/Transparent Motivations/essentials/src/lib/buildingImages.js (the CURATED_LOCAL object + the existing single-word `marana:` / `tucson:` entries to mirror the unquoted-key style; getBuildingImages substring/state-scoping logic)
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the EXISTING Arizona COVERAGE_STATES block with its 'Tucson' + 'Oro Valley' + 'Marana' areas — append to its areas array; confirm NO second Arizona block is created; the separate COVERAGE_COUNTIES array holding Pima County — do NOT touch it)
    - .planning/phases/197-sahuarita-deep-seed/197-04-SUMMARY.md (the banner subject | author | license from Task 1 for the attribution comment)
  </read_first>
  <action>
    In `src/lib/buildingImages.js`, add ONE entry to the CURATED_LOCAL object:
    `sahuarita: { state: 'AZ', src: '<CDN URL for cities/sahuarita.jpg>' }` (single-variant, NOT the
    array-of-{state,src} form — `sahuarita` has no same-named-city collision; single-word key → NO quotes,
    mirror the `marana:` / `tucson:` style, NOT the quoted `'oro valley'` multi-word style), with an
    attribution comment above it (subject | author | license from Task 1). Note in the comment it is
    deliberately distinct from the Pima County Catalina landscape banner, the Oro Valley CDO Trail Bridge,
    the Marana Tortolita/Dove-Mountain shot, the Tucson downtown streetscape, and the AZ-state Phoenix
    skyline, and that Sahuarita carries a southern/Santa-Cruz-Valley identity (D-03 — no Catalinas, no
    Tortolita). Make NO change to getBuildingImages(). Storage file is `cities/sahuarita.jpg`.

    In `src/lib/coverage.js`, APPEND one area object to the EXISTING Arizona COVERAGE_STATES block's `areas`
    array (the block that already contains 'Tucson' + 'Oro Valley' + 'Marana'):
    `{ label: 'Sahuarita', browseGovernmentList: ['0462140'], browseStateAbbrev: 'AZ', hasContext: true }`.
    Do NOT create a second `{ name: 'Arizona', ... }` block (Pitfall 7), do NOT add Sahuarita to
    COVERAGE_COUNTIES (where Pima County lives), and do NOT broaden parseCityFromAddress. hasContext:true is
    honest because Plan 03 seeded ≥1 stance row (this plan depends on Plan 03). Placement within the areas
    array is cosmetic. browseGovernmentList value is the G4110 geo_id ('0462140').

    Commit both files to the `essentials` repo via git.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qE "^\s*sahuarita:\s*\{" src/lib/buildingImages.js && grep -qF "cities/sahuarita.jpg" src/lib/buildingImages.js && grep -qF "'Sahuarita'" src/lib/coverage.js && grep -qF "'0462140'" src/lib/coverage.js && [ "$(grep -c "name: 'Arizona'" src/lib/coverage.js)" = "1" ] && node --input-type=module -e "import('./src/lib/coverage.js').then(()=>console.log('coverage-ok')).catch(e=>{console.error(e);process.exit(1)})" && node --input-type=module -e "import('./src/lib/buildingImages.js').then(()=>console.log('bi-ok')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has a single-word unquoted `sahuarita:` key with `state: 'AZ'` and src = the cities/sahuarita.jpg CDN URL, plus an attribution comment (subject | author | license) noting distinctiveness from the 5 sibling AZ banners.
    - coverage.js Arizona COVERAGE_STATES block's areas array has a NEW `{ label: 'Sahuarita', browseGovernmentList: ['0462140'], browseStateAbbrev: 'AZ', hasContext: true }` entry.
    - There is EXACTLY ONE `name: 'Arizona'` block in coverage.js (no duplicate/second block created — Pitfall 7).
    - COVERAGE_COUNTIES is NOT modified (Pima County entry untouched); no Sahuarita entry added there.
    - Both files parse cleanly via `node --input-type=module -e "import(...)"` (exit 0).
    - getBuildingImages() and parseCityFromAddress unchanged.
    - `npx vitest run src/lib/buildingImages.test.js` stays green (established non-gap; run as a spot-check).
    - Both files committed to the `essentials` repo.
  </acceptance_criteria>
  <done>Sahuarita banner wired into CURATED_LOCAL (single-word key) and surfaced via a new area appended to the EXISTING Arizona COVERAGE_STATES block with a DB-honest hasContext:true chip; exactly one Arizona block; COVERAGE_COUNTIES untouched; both frontend files parse, buildingImages.test.js green, both committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Verify the resident-facing outcome end-to-end — real Sahuarita address → council of 7 (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/197-sahuarita-deep-seed/197-01-SUMMARY.md (the 7 seeded officials + their titles to expect back — Mayor Murphy, Vice Mayor Egbert, 5 Council Members)
    - .planning/phases/197-sahuarita-deep-seed/197-RESEARCH.md (§ Architecture Patterns — "Address routing (NO CODE CHANGE)"; the existing G4110→LOCAL mapping is expected to cover all 7 at-large seats with zero code change, no LOCAL_EXEC join)
  </read_first>
  <action>ORCHESTRATOR-RUN closing gate — no executor file action. The whole phase goal is "Sahuarita residents can SEE their council with a compass." Every other plan verifies DB structure (row counts, joins) but not the live resident-facing result. The orchestrator POSTs a real Sahuarita street address to the live candidates-search API (POST /candidates/search {query}, full street address required per the search contract) and confirms the response includes all 7 seeded at-large council members (2 of whom carry the Mayor / Vice Mayor title), all on geo_id 0462140. This exercises the geofence→district→office join that Plan 01 seeded, end-to-end, against the shared-LOCAL-row shape already live in prod for Marana/Torrance. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A live end-to-end confirmation that the phase goal is actually TRUE for a resident — not just that the
    rows exist. Sahuarita's at-large shape means every Sahuarita address should return the SAME 7 council
    members (2 title-annotated as Mayor / Vice Mayor), which IS the correct at-large behavior (no per-ward
    differentiation, no separate LOCAL_EXEC Mayor to check).
  </what-built>
  <how-to-verify>
    1. The orchestrator POSTs a real Sahuarita street address (e.g. Sahuarita Town Hall, 375 W Sahuarita
       Center Way, Sahuarita, AZ 85629, or another verified in-town street address) to the live
       candidates-search API (accounts-api.empowered.vote, POST /candidates/search {query} — full street
       address required).
    2. Confirm the response includes the Town of Sahuarita government with all 7 at-large Council Members
       (2 of whom carry the Mayor / Vice Mayor title), matching the roster seeded in Plan 01 (party never
       displayed — antipartisan).
    3. If any council member is missing, an extra/wrong office appears, or a phantom LOCAL_EXEC Mayor
       surfaces, STOP — this indicates a district-attach or geofence-mapping defect from Plan 01 that must
       be fixed before the phase closes.
  </how-to-verify>
  <acceptance_criteria>
    - A real Sahuarita street address POSTed to the live candidates-search API returns the Town of Sahuarita with all 7 at-large Council Members (7 offices total; 2 title-annotated Mayor / Vice Mayor), matching Plan 01's seeded roster.
    - No wrong-jurisdiction, duplicate, or phantom LOCAL_EXEC Sahuarita office appears in the response (section-split / wrong-district-attach / phantom-Mayor would surface here).
    - Result recorded in 197-04-SUMMARY.md (the address used + the offices returned).
  </acceptance_criteria>
  <resume-signal>Type "approved" once a real Sahuarita address returns all 7 at-large Council Members (2 title-annotated Mayor / Vice Mayor) from the live search API — or describe the routing discrepancy found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wikimedia/Flickr (remote) → banner pipeline | Untrusted remote image bytes enter process_banner.py over HTTP |
| banner pipeline → Storage | Processed banner crosses into the production politician_photos bucket |
| frontend edits → live app | coverage.js / buildingImages.js changes ship to Render on push |

## STRIDE Threat Register

Honest scope: a licensed static image + two static frontend appends. Genuine threats are payload-in-image,
license provenance, and a dishonest/mis-placed coverage chip — not injection or access control. No
HIGH-severity threat; none fabricated. Mirrors the Marana 196 threat model.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-BAN | Tampering | remote banner image fetch | mitigate | process_banner.py re-encodes to JPEG (strips embedded payloads); operator visual QA confirms a real non-AI non-aerial photo before wiring |
| T-197-LIC | Repudiation | banner license provenance | mitigate | Subject \| author \| license recorded in the SUMMARY + the attribution comment; only CC/public-domain sources used |
| T-197-COVLIE | Tampering | dishonest coverage chip | mitigate | hasContext:true only set because Plan 03 landed ≥1 stance row (depends_on 197-03); node parse-check confirms valid module |
| T-197-COVBLK | Tampering | wrong coverage array (duplicate AZ block / append to COUNTIES) | mitigate | APPEND to the EXISTING Arizona COVERAGE_STATES block (Pitfall 7); grep asserts exactly ONE `name: 'Arizona'` block; COVERAGE_COUNTIES Pima entry untouched |
| T-197-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; never in a committed frontend file |
| T-197-SC | Tampering | package installs | accept | No new installs — reuses installed PIL/requests banner toolchain; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- cities/sahuarita.jpg returns HTTP 200 at the banner spec (1700×540 @ 3.15:1); operator-approved as a real, distinct, non-Catalina, non-Tortolita Sahuarita photo.
- CURATED_LOCAL `sahuarita` → banner URL; 'Sahuarita' area APPENDED to the EXISTING Arizona COVERAGE_STATES block (exactly one Arizona block); COVERAGE_COUNTIES untouched.
- Both frontend files parse, buildingImages.test.js green, both committed.
- A real Sahuarita address returns all 7 at-large Council Members from the live search API.
</verification>

<success_criteria>
BANR-01 + SUB-03 / ROADMAP #4 (licensed banner sourced/processed/uploaded/wired) and #5 (Sahuarita
surfaced with a DB-honest chip appended to the existing Arizona block) are TRUE; a real Sahuarita address
resolves to the seeded 7 at-large council members end-to-end.
</success_criteria>

<output>
Create `.planning/phases/197-sahuarita-deep-seed/197-04-SUMMARY.md` when done — include the banner subject
| author | license, the CDN URL, and the end-to-end address-routing result (address used + offices
returned).
</output>
</content>
