---
phase: 174-west-metro-school-district-geofences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-or-westmetro-school-boundaries.ts
  - C:/EV-Accounts/backend/scripts/smoke-or-westmetro-school.ts
autonomous: true
requirements: [WM-GEO-01]

must_haves:
  truths:
    - "A Beaverton in-district address routes to G5420 geo_id 4101920 (Beaverton SD 48J)"
    - "A Hillsboro in-district address routes to G5420 geo_id 4100023 (Hillsboro SD 1J)"
    - "A Tigard in-district address routes to G5420 geo_id 4112240 (Tigard-Tualatin SD 23J)"
    - "A Forest Grove in-district address routes to G5420 geo_id 4105160 (Forest Grove SD 15)"
    - "A Sherwood in-district address routes to G5420 geo_id 4111290 (Sherwood SD 88J)"
    - "Exactly 5 west-metro G5420 rows exist tagged source='tiger_unsd_or_2024_westmetro'"
    - "The 6 existing Multnomah G5420 rows (source='tiger_unsd_or_2024') are unchanged"
    - "No other geofence tier (city/county/CD/SLDU/SLDL) is modified"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-or-westmetro-school-boundaries.ts"
      provides: "West-metro TIGER UNSD G5420 loader (5-GEOID filter, EXPECTED_COUNT=5, distinct source tag)"
      contains: "tiger_unsd_or_2024_westmetro"
    - path: "C:/EV-Accounts/backend/scripts/smoke-or-westmetro-school.ts"
      provides: "5-address routing spot-check + west-metro count assertion"
      contains: "ST_Covers"
  key_links:
    - from: "load-or-westmetro-school-boundaries.ts"
      to: "essentials.geofence_boundaries"
      via: "parameterized INSERT with ST_ForcePolygonCCW(ST_SetSRID(ST_Force2D(ST_GeomFromGeoJSON))) + ON CONFLICT (geo_id, mtfcc) DO NOTHING"
      pattern: "INSERT INTO essentials.geofence_boundaries"
    - from: "smoke-or-westmetro-school.ts"
      to: "essentials.geofence_boundaries"
      via: "ST_Covers PIP query WHERE state='41' AND mtfcc='G5420'"
      pattern: "ST_Covers.*ST_MakePoint"
---

<objective>
Load `G5420` geofence boundaries for the 5 west-metro Oregon school districts (Beaverton SD 48J,
Hillsboro SD 1J, Tigard-Tualatin SD 23J, Forest Grove SD 15, Sherwood SD 88J) via the proven v10.0
TIGER UNSD loader pattern, then prove every district routes correctly via real-address spot-checks
and a clean section-split scan.

Purpose: School-board phases 183-184 link board officials to these G5420 boundaries via geo_id.
These 5 geo_ids are the contract those phases depend on. (WM-GEO-01)

Output: Two new scripts in `C:/EV-Accounts/backend/scripts/`; 5 new rows in
`essentials.geofence_boundaries` tagged `source='tiger_unsd_or_2024_westmetro'`. NO migration is
registered and the on-disk migration counter is NOT bumped (v10.0 loader precedent — the loader
writes via its own pg.Pool).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-west-metro-school-district-geofences/174-CONTEXT.md
@.planning/phases/174-west-metro-school-district-geofences/174-RESEARCH.md
@.planning/phases/174-west-metro-school-district-geofences/174-PATTERNS.md

<interfaces>
<!-- The exact constants the loader hardcodes. Verified via TIGERweb ACS2024 layer 10. -->
TARGET_GEOIDS (D-01):
  4101920 → Beaverton School District 48J
  4100023 → Hillsboro School District 1J
  4112240 → Tigard-Tualatin School District 23J
  4105160 → Forest Grove School District 15
  4111290 → Sherwood School District 88J
EXPECTED_COUNT = 5
SOURCE         = 'tiger_unsd_or_2024_westmetro'   (D-02 — distinct from Multnomah 'tiger_unsd_or_2024')
MTFCC          = 'G5420'   STATE = '41'   (FIPS numeric string, NOT 'or')
TIGER_URL      = https://www2.census.gov/geo/tiger/TIGER2024/UNSD/tl_2024_41_unsd.zip
tmpRoot        = .tmp-or-westmetro-school-unsd   (fresh dir, avoids Multnomah cache mixing)

geofence_boundaries columns: geo_id, ocd_id, name, state, mtfcc, geometry, source, imported_at
  (ocd_id := geoId; geo_id := geoId — same value, per TIGER convention)

Existing baseline (verified clean 2026-06-30): exactly 6 G5420/state='41' rows, all
source='tiger_unsd_or_2024' (Centennial, David Douglas, Parkrose, Portland Public, Reynolds,
Riverdale). None of the 5 west-metro districts present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clone loader + smoke script and confirm 5 GEOIDs resolve via --dry-run</name>
  <files>C:/EV-Accounts/backend/scripts/load-or-westmetro-school-boundaries.ts, C:/EV-Accounts/backend/scripts/smoke-or-westmetro-school.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-or-school-boundaries.ts (THE analog — clone verbatim, 269 lines)
    - C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts (smoke-test analog — ST_Covers query pattern, AddressTest interface, assertion loop)
    - .planning/phases/174-west-metro-school-district-geofences/174-PATTERNS.md (exact copy-verbatim line ranges + the 3 changed constants + TEST_ADDRESSES block)
    - .planning/phases/174-west-metro-school-district-geofences/174-RESEARCH.md (verified GEOIDs, test addresses, pitfalls)
  </read_first>
  <behavior>
    - `--dry-run` reads the statewide UNSD shapefile, finds all 5 target GEOIDs, prints one "Found GEOID=<id>: <name>" line per district and one "[dry-run] Would insert: geo_id=<id>" line per district, and writes nothing to the DB.
    - If any GEOID is missing the loader exits non-zero with "Expected 5 GEOIDs, found N" (the all-5-found assert).
    - The smoke script compiles/imports cleanly under `npx tsx` (no DB writes in this task).
  </behavior>
  <action>
    Create `load-or-westmetro-school-boundaries.ts` as a verbatim clone of `load-or-school-boundaries.ts`,
    changing ONLY: SOURCE → 'tiger_unsd_or_2024_westmetro' (per D-02); EXPECTED_COUNT → 5 (per D-01);
    TARGET_GEOIDS → the 5-entry Map (4101920 Beaverton SD 48J, 4100023 Hillsboro SD 1J, 4112240
    Tigard-Tualatin SD 23J, 4105160 Forest Grove SD 15, 4111290 Sherwood SD 88J); and the temp-dir
    constant → '.tmp-or-westmetro-school-unsd'. Leave TIGER_URL, MTFCC='G5420', STATE='41', the
    download-with-redirects helper, zip extract, shapefile GEOID filter, the ST_ForcePolygonCCW/
    ST_SetSRID/ST_Force2D/ST_GeomFromGeoJSON INSERT, the ON CONFLICT (geo_id, mtfcc) DO NOTHING guard,
    the all-5-found assert, the post-insert COUNT verification, and the pool teardown UNCHANGED.
    Do NOT mutate the Multnomah loader (D-01, Pitfall 3). Do NOT add any INSERT INTO schema_migrations.
    Create `smoke-or-westmetro-school.ts` cloned from `smoke-or-geofences.ts`: keep the AddressTest
    interface and the assertion loop; replace TEST_ADDRESSES with the 5 west-metro city-hall entries
    from RESEARCH.md (each expectedMtfcc ['G5420'] + expectedGeoIds { G5420: <district GEOID> }); set
    the routing query to `WHERE state = '41' AND mtfcc = 'G5420' AND ST_Covers(geometry,
    ST_SetSRID(ST_MakePoint($1,$2),4326))`; add a pre-loop assertion that COUNT WHERE state='41' AND
    mtfcc='G5420' AND source='tiger_unsd_or_2024_westmetro' = 5. Then run the loader dry-run from
    `C:/EV-Accounts/backend`: `npx tsx scripts/load-or-westmetro-school-boundaries.ts --dry-run`.
    Note: city-hall coordinates are LOW-confidence (Assumption A1) — if any of the 5 fail to route in
    Task 2, geocode the address and adjust lon/lat; the load-correctness assertion is the geo_id match.
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsx scripts/load-or-westmetro-school-boundaries.ts --dry-run 2>&1 | grep -c "\[dry-run\] Would insert: geo_id=41"</automated>
  </verify>
  <acceptance_criteria>
    - `--dry-run` output contains exactly 5 "[dry-run] Would insert: geo_id=..." lines, one each for
      4101920, 4100023, 4112240, 4105160, 4111290 (the grep -c above returns 5).
    - `--dry-run` prints "All 5 target GEOIDs found (districtMap.size === 5)" and exits 0.
    - `git -C "C:/EV-Accounts" diff --name-only` shows scripts/load-or-school-boundaries.ts is NOT in
      the changeset (Multnomah loader untouched, D-01).
    - `grep -c "schema_migrations" scripts/load-or-westmetro-school-boundaries.ts` returns 0 (no ledger registration).
    - The new loader contains the literal string `tiger_unsd_or_2024_westmetro` and `EXPECTED_COUNT = 5`.
  </acceptance_criteria>
  <done>
    Both scripts exist; dry-run resolves exactly the 5 west-metro GEOIDs and writes nothing; the
    Multnomah loader is byte-for-byte unchanged; no migration is registered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run loader live, then verify 5-district routing + 5 SQL section-split gates</name>
  <files>C:/EV-Accounts/backend/scripts/load-or-westmetro-school-boundaries.ts, C:/EV-Accounts/backend/scripts/smoke-or-westmetro-school.ts</files>
  <read_first>
    - .planning/phases/174-west-metro-school-district-geofences/174-RESEARCH.md ("Section-Split Scan for G5420 Geofence Tier" — the 5 gates + expected counts)
    - .planning/phases/174-west-metro-school-district-geofences/174-PATTERNS.md ("Section-Split Scan SQL" — copy-ready gate SQL)
    - C:/EV-Accounts/backend/scripts/load-or-westmetro-school-boundaries.ts (the script being run)
    - C:/EV-Accounts/backend/scripts/smoke-or-westmetro-school.ts (the smoke test being run)
  </read_first>
  <action>
    From `C:/EV-Accounts/backend`, run the loader live: `npx tsx scripts/load-or-westmetro-school-boundaries.ts`.
    Expect 5 "Inserted:" lines (or "Skipped (already exists)" on a safe re-run — both pass) and the
    post-insert "All 5 ... loaded successfully." Then run the smoke test:
    `npx tsx scripts/smoke-or-westmetro-school.ts` — it must print "ALL ASSERTIONS PASSED" and exit 0,
    covering all 5 in-district address spot-checks (D-03) plus the count=5 assertion. Then run the 5
    section-split / integrity gates via psql against DATABASE_URL (the exact SQL is in PATTERNS.md
    "Section-Split Scan SQL"): Gate 1 no duplicate west-metro geo_ids (0 rows); Gate 2 west-metro count
    = 5; Gate 3 Multnomah count (source='tiger_unsd_or_2024') = 6 unchanged; Gate 4 invalid/NULL
    geometry count = 0; Gate 5 sentinel geo_id 4101920 returns 1 row named "Beaverton School District
    48J" with mtfcc='G5420'. If any address fails to route, geocode it, adjust the lon/lat in the smoke
    script, and re-run the smoke test (load rows are correct; only the test point needs nudging). Do NOT
    re-run with any altered SOURCE/GEOID — those are locked. Confirm SC#4 by leaving every non-G5420
    tier untouched (the loader only ever touches G5420/state='41' rows).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsx scripts/load-or-westmetro-school-boundaries.ts && npx tsx scripts/smoke-or-westmetro-school.ts</automated>
  </verify>
  <acceptance_criteria>
    - Smoke test prints "ALL ASSERTIONS PASSED" and exits 0; all 5 addresses (Beaverton→4101920,
      Hillsboro→4100023, Tigard→4112240, Forest Grove→4105160, Sherwood→4111290) return their expected
      G5420 geo_id.
    - Gate 1: `SELECT geo_id, COUNT(*) FROM essentials.geofence_boundaries WHERE state='41' AND mtfcc='G5420' AND source='tiger_unsd_or_2024_westmetro' GROUP BY geo_id HAVING COUNT(*)>1` returns 0 rows.
    - Gate 2: `SELECT COUNT(*) ... WHERE state='41' AND mtfcc='G5420' AND source='tiger_unsd_or_2024_westmetro'` = 5.
    - Gate 3: `SELECT COUNT(*) ... WHERE state='41' AND mtfcc='G5420' AND source='tiger_unsd_or_2024'` = 6 (Multnomah untouched).
    - Gate 4: `SELECT COUNT(*) ... AND (geometry IS NULL OR NOT ST_IsValid(geometry))` for the west-metro batch = 0.
    - Gate 5: `SELECT geo_id, name, mtfcc FROM ... WHERE state='41' AND geo_id='4101920'` returns exactly 1 row, name='Beaverton School District 48J', mtfcc='G5420'.
  </acceptance_criteria>
  <done>
    5 west-metro G5420 rows live and tagged; all 5 districts route correctly from a real address;
    all 5 SQL gates pass; the 6 Multnomah rows and all non-G5420 tiers are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov → loader | Public TIGER UNSD zip fetched over HTTPS; untrusted-network bytes cross here |
| loader → essentials.geofence_boundaries | Loader writes 5 polygon rows to the production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Tampering | TIGER zip download (census.gov) | accept | HTTPS-only URL to authoritative census.gov; download-with-redirects checks HTTP 200; ~3 MB shapefile is public, non-sensitive geometry. Low severity — no integrity check beyond TLS, acceptable for a public boundary file. |
| T-174-02 | Tampering | INSERT into geofence_boundaries | mitigate | All values are hardcoded constants (GEOIDs, source, state) — no user input. Parameterized queries ($1..$7) used regardless. ON CONFLICT (geo_id, mtfcc) DO NOTHING prevents duplicate/clobber of the 6 existing Multnomah rows. |
| T-174-03 | Denial of Service | re-run / partial load | mitigate | Idempotent ON CONFLICT DO NOTHING + all-5-found assert means a re-run is safe and a partial download fails closed (exit 1) rather than inserting a subset. |
| T-174-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — all deps (pg, adm-zip, shapefile, dotenv) pre-exist in C:/EV-Accounts/backend. No legitimacy gate required (RESEARCH.md "No Package Legitimacy Audit Required"). |

No HIGH-severity threats: read-public-data + idempotent parameterized insert, no API endpoints, no auth flows, no user input.
</threat_model>

<verification>
Phase-level gate (run after Task 2):
1. `npx tsx scripts/smoke-or-westmetro-school.ts` exits 0 — all 5 addresses route to the correct G5420 geo_id (SC#1 + D-03).
2. Loader dry-run filter confirmed against the 5 west-metro GEOIDs, not the 6 Multnomah (SC#2; Task 1 grep returns 5; Multnomah loader untouched).
3. Section-split scan: Gates 1-5 all clean (SC#3) — 0 duplicates, count=5, Multnomah=6, 0 invalid geometry, sentinel present.
4. No other geofence tier modified (SC#4) — loader and gates only ever touch G5420/state='41' rows; no migration registered; on-disk counter unchanged.
</verification>

<success_criteria>
- 5 west-metro G5420 boundaries exist (source='tiger_unsd_or_2024_westmetro'); Beaverton→4101920 and Hillsboro→4100023 confirmed by address routing (SC#1).
- Loader ran against the correct OR UNSD zip; GEOID filter confirmed = the 5 west-metro districts (SC#2).
- Section-split / integrity scan across the 5 new geo_ids returns 0 defects (SC#3).
- No other geofence tier modified; the 6 Multnomah rows unchanged; no schema_migration registered (SC#4).
- D-03 satisfied: all 5 districts (not just 2) demonstrated routing a real in-district address to the correct geo_id.
</success_criteria>

<output>
Create `.planning/phases/174-west-metro-school-district-geofences/174-01-SUMMARY.md` when done.
</output>
