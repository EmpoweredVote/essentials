---
phase: 107-ma-town-geofences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [MA-GEO-01, MA-GEO-02]

must_haves:
  truths:
    - "A resident of any MA town can enter their address and route to correct state + federal representatives (MA-GEO-01)"
    - "293 active G4040 COUSUB town boundaries are present in production for state='25'"
    - "Concord and Brookline town addresses route to G4040 + G5200 + G5210 + G5220 tiers (MA-GEO-01)"
    - "Boston still routes via G4110 (not G4040) — FUNCSTAT exclusion intact (MA-GEO-01)"
    - "Section-split check returns 0 rows — every routing geofence has a matching district (MA-GEO-02 prerequisite)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/smoke-ma-towns.ts"
      provides: "PIP routing smoke test for Lexington, Concord, Cambridge exclusion"
      contains: "expectG4040"
    - path: "C:/EV-Accounts/backend/scripts/verify-ma-tiger-import.sql"
      provides: "MACOUSUB-01..06 SQL gates for G4040 layer"
      contains: "MACOUSUB-01"
    - path: ".planning/phases/107-ma-town-geofences/107-01-VERIFICATION.md"
      provides: "Recorded pass evidence for all 4 success criteria"
      min_lines: 20
  key_links:
    - from: "essentials.geofence_boundaries (state='25', mtfcc='G4040')"
      to: "PostGIS ST_Covers address routing"
      via: "smoke-ma-towns.ts ST_Covers query"
      pattern: "ST_Covers"
    - from: "essentials.geofence_boundaries G5200/G5210/G5220/G4020"
      to: "essentials.districts"
      via: "section-split join (geofence NOT IN districts)"
      pattern: "NOT IN \\(SELECT geo_id FROM essentials.districts\\)"
---

<objective>
Verify and close Phase 107 (MA Town Geofences). Research (107-RESEARCH.md, HIGH confidence) confirms all 293 active G4040 COUSUB town boundaries were already loaded into `essentials.geofence_boundaries` during v5.0 (2026-05-19), point-in-polygon routing works for towns, Boston still routes via G4110, and the section-split check returns 0 rows. The loader (`load-state-tiger-boundaries.ts`) already has full MA COUSUB support (allowlist, MakeValid, EXPECTED_MA_MTFCC cousub:293, FUNCSTAT='A' filter).

This phase is therefore VERIFICATION-ONLY. Do NOT re-run the loader — `ON CONFLICT DO NOTHING` would silently skip all rows and give a false sense of "loading" (Pitfall 1 in RESEARCH). Instead, assert each of the 4 ROADMAP success criteria against production, run the existing smoke + SQL gates, record evidence, and close MA-GEO-01 + MA-GEO-02.

Purpose: Confirm any MA address (city OR town) routes correctly, so downstream phases (108 Boston, 109 Tier 2) can attach officials to a geofence layer that is proven present and clean.
Output: A `107-01-VERIFICATION.md` evidence file with recorded results for all 4 success criteria, plus a SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-ma-town-geofences/107-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert G4040 town rows present, geometry valid, and FUNCSTAT exclusions intact</name>
  <files>.planning/phases/107-ma-town-geofences/107-01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/107-ma-town-geofences/107-RESEARCH.md (Current Production State section + Pitfall 1)
    - C:/EV-Accounts/backend/scripts/verify-ma-tiger-import.sql (MACOUSUB-01 through MACOUSUB-06 gates, lines 80-114)
  </read_first>
  <action>
    Run the MACOUSUB gate queries against production via the Supabase MCP (`mcp__supabase-local`). Do NOT run the TIGER loader. Execute each assertion and record exact returned values into a new file `.planning/phases/107-ma-town-geofences/107-01-VERIFICATION.md`:

    - MACOUSUB-01: `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='25' AND mtfcc='G4040'` MUST equal 293.
    - MACOUSUB-02: `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='25' AND mtfcc='G4040' AND geo_id='2501711000'` (Cambridge) MUST equal 0 (FUNCSTAT='F' excluded).
    - Boston exclusion: `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='25' AND mtfcc='G4040' AND geo_id='2507000'` MUST equal 0 (Boston is G4110, not G4040).
    - MACOUSUB-03: Lexington geo_id='2501735215' present (1 row).
    - MACOUSUB-04: Concord geo_id='2501715060' present (1 row).
    - MACOUSUB-05: `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='25' AND mtfcc='G4040' AND NOT ST_IsValid(geometry)` MUST equal 0.
    - MACOUSUB-06: full MA picture by mtfcc — expect G4020=14, G4040=293, G4110=58, G5200=9, G5210=40, G5220=160.

    Record this evidence under a `## Criterion 1: 293 G4040 rows present + clean` heading. This satisfies ROADMAP success criterion #1 and the routing-prerequisite half of MA-GEO-01 and MA-GEO-02.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx -e "import {Client} from 'pg'; import * as d from 'dotenv'; d.config(); (async()=>{const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});await c.connect();const r=await c.query(\"SELECT COUNT(*)::int n FROM essentials.geofence_boundaries WHERE state='25' AND mtfcc='G4040'\");await c.end();process.exit(r.rows[0].n===293?0:1)})()"</automated>
  </verify>
  <acceptance_criteria>
    - MACOUSUB-01 query returns exactly 293.
    - Cambridge (geo_id='2501711000') count in G4040 is 0.
    - Boston (geo_id='2507000') count in G4040 is 0.
    - Lexington (2501735215) and Concord (2501715060) each return exactly 1 G4040 row.
    - Invalid-geometry count for G4040 is 0.
    - MACOUSUB-06 returns G4020=14, G4040=293, G4110=58, G5200=9, G5210=40, G5220=160.
    - `107-01-VERIFICATION.md` exists with a `## Criterion 1` section containing the recorded numeric results above.
  </acceptance_criteria>
  <done>All MACOUSUB gates pass against production with the exact expected counts, recorded in 107-01-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Run town routing smoke test and assert town/city PIP tiers</name>
  <files>.planning/phases/107-ma-town-geofences/107-01-VERIFICATION.md</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-ma-towns.ts (PIP test for Lexington 2501735215, Concord 2501715060, Cambridge exclusion)
    - .planning/phases/107-ma-town-geofences/107-RESEARCH.md (Point-in-polygon routing section: Concord/Brookline/Boston expected tiers)
  </read_first>
  <action>
    Run the existing smoke test from the backend directory: `npx tsx scripts/smoke-ma-towns.ts`. It exits 0 when all assertions pass (Lexington G4040=2501735215, Concord G4040=2501715060, Cambridge returns G4110 but NO G4040), exits 1 on any failure.

    Then, via Supabase MCP, run the explicit ST_Covers tier check for Concord (-71.3490, 42.4604) and Brookline (-71.1219, 42.3318) to confirm each town address routes through all required tiers (ROADMAP criterion #2 + MA-GEO-01): for each point, the ST_Covers query against `essentials.geofence_boundaries WHERE state='25'` MUST return rows for mtfcc G4040 (town), G5200 (NATIONAL_LOWER / congressional), G5210 (STATE_UPPER / senate), and G5220 (STATE_LOWER / house). Expected values from RESEARCH: Concord → G4040 2501715060, G5200 2503, G5210 25D15, G5220 25071; Brookline → G4040 2502109175, G5200 2504, G5210 25D17, G5220 25110.

    Also run the Boston point (-71.0589, 42.3601) and confirm it returns G4110 2507000 and NO G4040 row (ROADMAP criterion #3, Boston routes unchanged).

    Append all results under a `## Criterion 2: Town + city PIP routing` heading and a `## Criterion 3: Boston routes via G4110 unchanged` heading in `107-01-VERIFICATION.md`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/smoke-ma-towns.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/smoke-ma-towns.ts` exits 0 and prints "MA towns smoke test PASSED".
    - Concord PIP returns G4040=2501715060, G5200=2503, G5210=25D15, G5220=25071.
    - Brookline PIP returns G4040=2502109175, G5200=2504, G5210=25D17, G5220=25110.
    - Boston PIP returns G4110=2507000 and zero G4040 rows.
    - `107-01-VERIFICATION.md` contains `## Criterion 2` and `## Criterion 3` sections with the recorded tier rows above.
  </acceptance_criteria>
  <done>Smoke test passes; Concord/Brookline route through G4040+G5200+G5210+G5220; Boston routes via G4110 with no G4040 row; evidence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Run section-split check and close MA-GEO-01 + MA-GEO-02</name>
  <files>.planning/phases/107-ma-town-geofences/107-01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/107-ma-town-geofences/107-RESEARCH.md (Section-split check section + Pitfall 2: directionality of the check)
    - .planning/REQUIREMENTS.md (MA-GEO-01, MA-GEO-02 definitions)
  </read_first>
  <action>
    Run the OR-pattern section-split check against production via Supabase MCP (direction matters — see Pitfall 2): `SELECT gb.geo_id, gb.name, gb.mtfcc FROM essentials.geofence_boundaries gb WHERE gb.mtfcc IN ('G5200','G5210','G5220','G4020') AND gb.state='25' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts) LIMIT 10`. The PASS signal is 0 rows (ROADMAP criterion #4). Do NOT run the reverse direction (districts NOT IN geofence) as a failure gate — statewide STATE_EXEC/NATIONAL_UPPER districts intentionally have no polygon and would produce 7 expected rows.

    Note explicitly in the evidence file: G4040 COUSUB rows are excluded from this split check by design because the cousub layer dispatch has `writeDistrictRow: false` — towns inherit STATE/NATIONAL tiers from the G5200/G5210/G5220 geofences and never write their own `districts` row (RESEARCH: "Why COUSUB Does NOT Write districts Rows"). Confirm `SELECT COUNT(*) FROM essentials.districts d JOIN essentials.geofence_boundaries gb ON d.geo_id=gb.geo_id WHERE gb.mtfcc='G4040'` returns 0.

    Append results under `## Criterion 4: Section-split clean` and add a `## Requirement Closure` section recording: MA-GEO-01 = PASS (293 town geofences present; town addresses route to correct state+federal tiers), MA-GEO-02 = PASS for the geofence prerequisite (non-empty LOCAL section is gated on Phase 108/109 official seeding, which is out of scope here per REQUIREMENTS.md traceability). Finalize `107-01-VERIFICATION.md` with an overall PASS verdict on all 4 ROADMAP success criteria.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx -e "import {Client} from 'pg'; import * as d from 'dotenv'; d.config(); (async()=>{const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});await c.connect();const r=await c.query(\"SELECT COUNT(*)::int n FROM essentials.geofence_boundaries gb WHERE gb.mtfcc IN ('G5200','G5210','G5220','G4020') AND gb.state='25' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts)\");await c.end();process.exit(r.rows[0].n===0?0:1)})()"</automated>
  </verify>
  <acceptance_criteria>
    - Section-split query (geofence NOT IN districts, mtfcc IN G5200/G5210/G5220/G4020, state='25') returns 0 rows.
    - G4040-to-districts join count returns 0 (confirming COUSUB writes no district rows by design).
    - `107-01-VERIFICATION.md` contains `## Criterion 4` and `## Requirement Closure` sections.
    - Requirement Closure records MA-GEO-01 = PASS and MA-GEO-02 = PASS (geofence prerequisite) with the Phase 108/109 dependency note.
    - Overall verdict in the file states all 4 ROADMAP success criteria PASS.
  </acceptance_criteria>
  <done>Section-split returns 0 rows; MA-GEO-01 and MA-GEO-02 recorded as closed with evidence; verification file finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous user → PostGIS routing | Untrusted lat/lon from address geocode crosses into ST_Covers query; read-only, parameterized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering | Re-running TIGER loader corrupting/duplicating G4040 rows | mitigate | Verification-only phase; loader is NOT invoked; tasks assert via read-only SELECT/ST_Covers. ON CONFLICT DO NOTHING means even an accidental run is non-destructive (Pitfall 1). |
| T-107-02 | Information Disclosure | PIP query returns geofence rows for an address | accept | Geofence boundaries are public TIGER data; no PII; ST_Covers params are bound, not interpolated. |
| T-107-03 | Denial of Service | Malicious lat/lon causing expensive PostGIS scan | accept | ST_Covers backed by spatial index; RESEARCH confirms sub-100ms; no new query surface added by this phase. |

No package installs in this phase — `T-{phase}-SC` not applicable.
</threat_model>

<verification>
- MACOUSUB-01 = 293; Cambridge & Boston absent from G4040; geometries valid; full MA picture matches expected counts.
- `npx tsx scripts/smoke-ma-towns.ts` exits 0.
- Concord + Brookline route through G4040 + G5200 + G5210 + G5220; Boston via G4110 only.
- Section-split (geofence NOT IN districts) = 0 rows; G4040→districts join = 0.
- `107-01-VERIFICATION.md` records all 4 criteria as PASS and closes MA-GEO-01 + MA-GEO-02.
</verification>

<success_criteria>
1. 293 G4040 COUSUB rows confirmed in `geofence_boundaries` (state='25', mtfcc='G4040').
2. A town address (Concord, Brookline) returns correct STATE_UPPER + STATE_LOWER + NATIONAL tiers.
3. Boston address still routes via G4110 (unchanged, no G4040 row).
4. Section-split check returns 0 rows.
</success_criteria>

<output>
Create `.planning/phases/107-ma-town-geofences/107-01-SUMMARY.md` when done.
</output>
