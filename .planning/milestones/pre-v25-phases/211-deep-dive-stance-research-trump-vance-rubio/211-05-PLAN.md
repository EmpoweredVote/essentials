---
phase: 211-deep-dive-stance-research-trump-vance-rubio
plan: 05
type: execute
wave: 5
depends_on: [211-02, 211-03, 211-04]
files_modified:
  - .planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-PROVENANCE-AUDIT.md
autonomous: false
requirements: [RES-01]

must_haves:
  truths:
    - "For all three officials, every live inform.politician_answers row maps to a bundle.json citation (quote_text + source_url) — 100% provenance, 0 uncited (SC-2)"
    - "No answer row has a null/defaulted value and no fabricated spoke exists; topics with no evidence are absent, not chair-3 defaults (SC-3)"
    - "Each official's compass renders on their live profile/cards reflecting the researched stances (SC-4)"
  artifacts:
    - path: ".planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-PROVENANCE-AUDIT.md"
      provides: "Cross-check table: per-official DB answer count vs CSV non-blank count vs bundle cited count; 0 uncited proof; legacy-delete tally"
  key_links:
    - from: "inform.politician_answers (3 officials)"
      to: "the three bundle.json artifacts"
      via: "topic_id ↔ cited stance cross-check"
      pattern: "source_url"
---

<objective>
Prove the pass met SC-1..SC-4 and get live sign-off. Cross-check the live DB rows for all three
officials against their bundle.json citations (100% cited, no uncited, no defaults), then a human-verify
checkpoint confirms each compass actually renders on the live app (SC-4).

Purpose: This is the phase's audit gate. Since `inform.politician_answers` has no citation column,
"100% cited" is proven by cross-referencing every live row to a bundle entry that carries a source_url
+ quote_text. The render check confirms the data actually surfaces to users.
Output: `211-PROVENANCE-AUDIT.md` + operator render sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-CONTEXT.md
@.planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-02-SUMMARY.md
@.planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-03-SUMMARY.md
@.planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-04-SUMMARY.md

<interfaces>
Officials: Trump 104102e6-08c1-494f-a9d4-6ef129595bf2 / Vance a809747d-3e53-4e9e-b3a1-6641dac2455c /
  Rubio 7c8e4442-e13e-485a-8993-b05ca110410d.
Bundles: 2026-07-19-{donald-trump,jd-vance,marco-rubio}.bundle.json under C:\EV-Accounts\backend\data\stance-research\.
App URL: essentials.empowered.vote (compass renders on profile + result cards).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provenance cross-check — every live row is cited, none defaulted</name>
  <files>.planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-PROVENANCE-AUDIT.md</files>
  <read_first>
    - .planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-CONTEXT.md (SC-1..SC-3; D-05 nothing uncited)
    - C:\EV-Accounts\backend\data\stance-research\2026-07-19-donald-trump.bundle.json
    - C:\EV-Accounts\backend\data\stance-research\2026-07-19-jd-vance.bundle.json
    - C:\EV-Accounts\backend\data\stance-research\2026-07-19-marco-rubio.bundle.json
    - C:\EV-Accounts\backend\data\stance-research\2026-07-19-trump-vance-pre-pass-snapshot.json (to report legacy overwritten/deleted deltas)
  </read_first>
  <action>
    From `C:\EV-Accounts\backend`, for each of the three politician_ids: SELECT topic_id, value from
    inform.politician_answers, then for EACH live row confirm the corresponding topic exists in that
    official's bundle.json with stance.value == the live value AND a non-empty source_url + quote_text.
    Any live row without a matching cited bundle entry is an uncited-row DEFECT (must be 0, per D-05).
    Also assert: 0 rows with NULL/empty value (no defaults, SC-3); the live value equals the bundle's
    assigned chair for every row (parseInt integrity, no polarity drift). Compute the legacy delta vs the
    pre-pass snapshot (Trump was 21, Vance 24, Rubio 0): how many overwritten, how many legacy topics
    deleted as unsourced. Write `211-PROVENANCE-AUDIT.md` with a per-official table (DB count / CSV
    non-blank count / bundle cited count / uncited defects=0 / legacy overwritten / legacy deleted /
    honest-blank topic list). This is a read-only audit — no DB writes.
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && node -e "const {Pool}=require('pg'); const ids={trump:'104102e6-08c1-494f-a9d4-6ef129595bf2',vance:'a809747d-3e53-4e9e-b3a1-6641dac2455c',rubio:'7c8e4442-e13e-485a-8993-b05ca110410d'}; const files={trump:'donald-trump',vance:'jd-vance',rubio:'marco-rubio'}; const p=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); (async()=>{for(const k of Object.keys(ids)){const b=require('C:/EV-Accounts/backend/data/stance-research/2026-07-19-'+files[k]+'.bundle.json'); const topics=b.topics||b; const cited={}; for(const t of Object.values(topics)){const qs=t.quotes||[t]; for(const q of qs){const st=q.stance||q; if(st.value!=null&&q.source_url&&q.quote_text) cited[String(q.topic_id||t.topic_id)]=st.value;}} const r=await p.query('SELECT topic_id, value FROM inform.politician_answers WHERE politician_id=$1',[ids[k]]); for(const row of r.rows){if(row.value==null) throw new Error(k+' has null value row'); if(!(String(row.topic_id) in cited)) throw new Error(k+' UNCITED row topic '+row.topic_id);} console.log(k,'rows',r.rows.length,'all cited');} await p.end();})().catch(e=>{console.error(e);process.exit(1);})"</automated>
  </verify>
  <acceptance_criteria>
    - For all three officials: every live inform.politician_answers row has a matching bundle entry with a
      source_url + quote_text — uncited-row defect count = 0 (SC-2, D-05).
    - Zero rows with NULL/empty value across the three officials (SC-3 no defaults).
    - `211-PROVENANCE-AUDIT.md` exists with the per-official table (counts, uncited=0, legacy overwritten/deleted, honest-blank list).
    - Legacy delta reported: Trump/Vance legacy rows either overwritten with a cited value or deleted as unsourced — none left uncited.
  </acceptance_criteria>
  <done>Provenance proven: 100% cited, 0 uncited, 0 defaults, legacy fully reconciled — documented in the audit file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-verify all three compasses render (SC-4)</name>
  <read_first>
    - .planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-CONTEXT.md (SC-4; specifics: expect national topics populated, hyper-local/judicial blank)
    - .planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-PROVENANCE-AUDIT.md (the counts + honest-blank lists to spot-check against live render)
  </read_first>
  <files>none — live human verification only (no repository files modified)</files>
  <action>
    Operator (human) opens each of the three officials' profiles on essentials.empowered.vote and confirms
    the compass renders with the researched stances. Claude does not edit data in this plan; it records the
    approval or a defect list into the SUMMARY. If any spoke looks wrong/uncited/defaulted, capture the
    official + topic + observed value for a follow-up gap-closure plan.
  </action>
  <what-built>
    Full-compass, 100%-cited stances were applied for Trump, Vance, and Rubio (Waves 2-4) and the Task-1
    provenance audit confirmed 0 uncited / 0 defaulted rows across all three, with legacy uncited rows
    overwritten or deleted (D-05).
  </what-built>
  <how-to-verify>
    On essentials.empowered.vote, open each official's profile/compass:
    1. Donald Trump (President) — compass renders with the researched spokes; national topics (abortion,
       tariffs, immigration, deportation, taxes, etc.) show a stance; hyper-local/judicial spokes are
       honestly blank (not forced). Confirm it no longer looks like the old 21-answer uncited set.
    2. J.D. Vance (VP) — same: compass renders; national topics populated; blanks where honest.
    3. Marco Rubio (Sec. of State) — compass now renders (was empty/0 before); national topics populated.
    Spot-check 2-3 spokes per official against the bundle.json citation (the assigned chair matches the
    cited evidence). Confirm no fabricated/placeholder spokes and no party label is shown (antipartisan).
  </how-to-verify>
  <acceptance_criteria>
    - All three compasses render on live (Trump, Vance, Rubio) — SC-4.
    - National topics show stances; hyper-local/judicial spokes are honestly blank, not forced.
    - Rubio's compass is now populated (was empty before the pass).
    - Spot-checked spokes match their bundle.json citations; no party label displayed.
  </acceptance_criteria>
  <verify>
    <human-check>All three officials' compasses render correctly on essentials.empowered.vote with the researched, cited stances; national topics populated, honest blanks elsewhere, no fabricated spokes, no party shown.</human-check>
  </verify>
  <done>Operator has signed off that Trump, Vance, and Rubio compasses render live per SC-4 (or logged defects for gap closure).</done>
  <resume-signal>Type "approved" once all three compasses render correctly on live, or describe any spoke that looks wrong/uncited/defaulted.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live DB rows → user-facing compass | Data integrity/provenance must hold all the way to render; a cited row that renders wrong misinforms voters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-211-05-01 | Tampering/Integrity | uncited or defaulted row surviving in production | mitigate | Task 1 automated cross-check fails the plan if any live row lacks a cited bundle entry or has a null value |
| T-211-05-02 | Repudiation | claiming SC-4 render without proof | mitigate | Blocking human-verify checkpoint requires operator sign-off that all three compasses render live |
| T-211-05-03 | Integrity | polarity drift (value ≠ assigned chair) | mitigate | Task 1 asserts live value == bundle assigned chair per row (parseInt integrity, no 3−value) |
</threat_model>

<verification>
- Automated: 0 uncited rows, 0 null-value rows across all three officials; every live value matches its bundle chair.
- Human: all three compasses render correctly on essentials.empowered.vote (SC-4).
</verification>

<success_criteria>
SC-1..SC-4 proven: applicable topics answered for all three, 100% cited, honest blanks / no defaults,
legacy reconciled, and each compass renders live with operator sign-off.
</success_criteria>

<output>
Create `.planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-05-SUMMARY.md` when done.
</output>
