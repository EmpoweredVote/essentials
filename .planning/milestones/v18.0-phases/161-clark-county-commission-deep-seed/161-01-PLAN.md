---
phase: 161-clark-county-commission-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql
autonomous: false
requirements: [CLARK-01]
user_setup: []

must_haves:
  truths:
    - "A standalone government 'Clark County, Nevada, US' exists (type='County', state='NV', geo_id='32003'), NOT nested under State of Nevada"
    - "A chamber 'Board of County Commissioners' exists under that government"
    - "All 7 commissioners have offices linked to the single pre-existing COUNTY district (geo_id='32003', district_type='COUNTY', state='nv' lowercase) [D-01]; offices link via geo_id='32003' and use external_id block -3200301..-3200307 [D-05]"
    - "Chair Naft (District A) and Vice-Chair McCurdy (District D) are modeled as titles on their commissioner seats — no phantom 8th office row [D-03]"
    - "Each of the 7 commissioners has office_id back-filled and exactly one office; no section-split defect"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql"
      provides: "Structural migration: 1 government + 1 chamber + idempotent COUNTY district guard + 7 politician+office CTEs on the single COUNTY district + office_id back-fill + post-verify DO block; registers as 1055"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 150
  key_links:
    - from: "essentials.chambers (Board of County Commissioners)"
      to: "essentials.governments (Clark County, Nevada, US, geo_id='32003')"
      via: "government_id subquery WHERE name='Clark County, Nevada, US'"
      pattern: "Clark County, Nevada, US"
    - from: "essentials.offices (7 commissioners)"
      to: "essentials.districts (COUNTY at geo_id='32003', state='nv')"
      via: "district join geo_id='32003' AND district_type='COUNTY' AND state='nv'"
      pattern: "state = 'nv'"
    - from: "essentials.politicians (external_id -3200301..-3200307)"
      to: "essentials.offices"
      via: "office_id back-fill BETWEEN range"
      pattern: "BETWEEN -3200307 AND -3200301"
---

<objective>
Seed Clark County structurally: create a standalone government "Clark County, Nevada, US" (NOT nested under State of Nevada geo_id='32'), one chamber "Board of County Commissioners", all 7 sitting commissioners, link each to the SINGLE pre-existing COUNTY district (geo_id='32003', loaded by Phase 158 — NO new districts, NO per-district A–G geofences), and back-fill office_id. Chair Naft (District A) and Vice-Chair McCurdy (District D) are modeled as titles on their seats — no separate Chair office. A Wave-0 DB probe confirms the ledger MAX, the unused external_id block, and the COUNTY district casing BEFORE any write, and a roster operator-verification checkpoint (Chair = Naft, NOT Kirkpatrick) precedes the migration apply.

Purpose: Satisfies the structural half of CLARK-01 (7-member board routed via the single COUNTY polygon). Headshots are Plan 02; stances + final verification are Plan 03.
Output: 1055_clark_county_commission.sql (structural, registered), plus the captured Wave-0 probe facts + the 7 external_id→UUID map recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP):**
- The executor WRITES the migration .sql file to disk and documents the exact SQL. The executor does NOT apply migrations or run DB SELECTs.
- The Wave-0 DB probes, applying mig 1055 (via `psql -f` against the EV-Accounts `.env` DATABASE_URL, or `mcp__supabase-local__apply_migration`), and every post-apply audit SELECT are INLINE ORCHESTRATOR steps performed by execute-phase, not the executor subagent.
- Each task below marks which steps are executor-write vs inline-orchestrator-apply.
- All migration files live in the SEPARATE repo C:/EV-Accounts/backend/. The SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/161-clark-county-commission-deep-seed/161-RESEARCH.md
@.planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md
@.planning/phases/161-clark-county-commission-deep-seed/161-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
Government: name='Clark County, Nevada, US', type='County', state='NV' UPPERCASE, city=NULL, geo_id='32003' (standalone, NOT under State of Nevada geo_id='32')
Chamber: name='Board of County Commissioners', name_formal='Clark County Board of County Commissioners'
Pre-existing COUNTY district (Phase 158): id=f3708f34-6e23-4771-a8f7-44e400a23337, geo_id='32003', district_type='COUNTY', state='nv' LOWERCASE, label='Clark County', mtfcc='G4020'
District INSERT is idempotent guard only (Phase 158 already loaded it) — guard on (geo_id='32003', district_type='COUNTY', state='nv')
Office district join key: d.geo_id='32003' AND d.district_type='COUNTY' AND d.state='nv' LOWERCASE (uppercase 'NV' matches ZERO rows — silent no-op)
offices.representing_state: 'NV' UPPERCASE (free-text label column, NOT the district join key)
Office NOT EXISTS guard: (district_id, politician_id) — NOT (district_id, chamber_id); county pattern is one person per office, all 7 share the one COUNTY district
external_id block: -3200301 (Naft, Dist A) .. -3200307 (Gibson, Dist G); BETWEEN ordering -3200307 AND -3200301 (more-negative bound first)
chambers.slug is GENERATED ALWAYS — never include the auto-generated path column in INSERT column list; name_formal must NOT be ''
Next structural migration: 1055 (Wave-0 probe asserts ledger MAX=1053; 1054 was audit-only and unregistered). Register with (version,name)=('1055','clark_county_commission') OUTSIDE the transaction.
The 7-row roster (district → external_id → full_name → first/last → party → title):
  A | -3200301 | Michael Naft        | Michael    | Naft     | Democratic | Commissioner (District A)  [Chair — title-on-seat]
  B | -3200302 | Marilyn Kirkpatrick | Marilyn    | Kirkpatrick | Democratic | Commissioner (District B)
  C | -3200303 | April Becker        | April      | Becker   | Republican | Commissioner (District C)
  D | -3200304 | William McCurdy II  | William    | McCurdy II | Democratic | Commissioner (District D)  [Vice-Chair — title-on-seat]
  E | -3200305 | Tick Segerblom      | Tick       | Segerblom | Democratic | Commissioner (District E)
  F | -3200306 | Justin Jones        | Justin     | Jones    | Democratic | Commissioner (District F)
  G | -3200307 | James B. Gibson     | James B.   | Gibson   | Democratic | Commissioner (District G)
CHAIR CORRECTION: Chair is Michael Naft (District A), NOT Marilyn Kirkpatrick (elected 2026-01-07). Vice-Chair = McCurdy. Kirkpatrick is a plain Commissioner (District B).
Chair display ordering is handled by groupHierarchy.js (no role_canonical change, no LOCAL_EXEC) — same as Multnomah; verify Naft sorts first in Plan 03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write structural migration 1055_clark_county_commission.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql (PRIMARY ANALOG — verbatim county-government structural template: government + chamber + idempotent COUNTY district guard + politician/office CTEs on a single COUNTY district + office_id back-fill + post-verify DO block + ledger registration)
    - C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql (SECOND county analog — confirms the geo_id-derived external_id scheme)
    - .planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md (section "migrations/1055_clark_county_commission.sql" — the exact 7-step template + NV substitutions + grep-gate-aware header)
    - .planning/phases/161-clark-county-commission-deep-seed/161-RESEARCH.md ("The Roster" — all 7 district/external_id/name/party/title rows; "Migration Shape" Patterns 1-7; "Common Pitfalls" 1-10)
  </read_first>
  <action>
Write the structural migration as a verbatim NV-substitution of mig 244 (Multnomah County), using the 7-step template in 161-PATTERNS.md and the 7-row roster from 161-RESEARCH.md. Wrap STEPS 1-6 in a single BEGIN/COMMIT transaction; STEP 7 (ledger registration) goes OUTSIDE.

Open with a header comment block stating: file is 1055_clark_county_commission.sql, Phase 161 (CLARK-01), STRUCTURAL (registers in the migration ledger), idempotent, standalone government 'Clark County, Nevada, US' geo_id='32003', chamber 'Board of County Commissioners', 7 commissioners external_ids -3200301..-3200307, and the casing reminders (COUNTY district join key uses LOWERCASE state='nv'; governments.state and offices.representing_state are UPPERCASE 'NV'; the auto-generated chamber path column must never appear in the column list; governments has no unique constraint on geo_id so a WHERE NOT EXISTS guard is required). GREP-GATE HYGIENE: do NOT write the literal tokens for the auto-generated path column, the removed image-origin column, or the ledger table name in explanatory comments — paraphrase (e.g. "the auto-generated path column", "ledger registration"). The ledger registration INSERT keyword in STEP 7 is unavoidable but must be the only occurrence.

STEP 1 — government INSERT (columns id, name, type, state, city, geo_id): gen_random_uuid(), 'Clark County, Nevada, US', 'County', 'NV' (UPPERCASE), NULL, '32003', guarded by WHERE NOT EXISTS on name='Clark County, Nevada, US'. (Per D-04 — standalone, NOT nested under State of Nevada.)

STEP 2 — chamber INSERT (columns id, name, name_formal, government_id): gen_random_uuid(), 'Board of County Commissioners', 'Clark County Board of County Commissioners', government_id from a subquery WHERE name='Clark County, Nevada, US', guarded by WHERE NOT EXISTS on (name, government_id). name_formal must NOT be empty. Never list the auto-generated path column. (Per D-02.)

STEP 3 — idempotent COUNTY district INSERT (columns id, district_type, state, geo_id, label, mtfcc): gen_random_uuid(), 'COUNTY', 'nv' (LOWERCASE), '32003', 'Clark County', 'G4020', guarded by WHERE NOT EXISTS on (geo_id='32003', district_type='COUNTY', state='nv'). This NO-OPs because Phase 158 already loaded the row — but never skip the guard (Pitfall 3).

STEP 4 — 7 politician+office CTE blocks (one per commissioner, in district order A→G). Each block: `WITH ins_p AS (INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id) VALUES (...) ON CONFLICT (external_id) DO NOTHING RETURNING id)` followed by `INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical) SELECT gen_random_uuid(), d.id, (chamber subquery scoped to government 'Clark County, Nevada, US'), p.id, '<title>', 'NV', false, false, NULL FROM essentials.districts d CROSS JOIN ins_p p WHERE d.geo_id='32003' AND d.district_type='COUNTY' AND d.state='nv' AND p.id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id=d.id AND o.politician_id=p.id)`. Use the exact 7-row roster from the interfaces block: title='Commissioner (District A)'..'(District G)', party 6 Democratic + Becker 'Republican', preserve 'William McCurdy II' / 'Tick Segerblom' / 'James B. Gibson' name forms (first_name 'James B.', last_name 'Gibson'). role_canonical NULL on all 7 (Chair/Vice-Chair are display-ordering only, no LOCAL_EXEC row). NOTE: the office NOT EXISTS guard is on (district_id, politician_id) — NOT (district_id, chamber_id) — because all 7 share the one COUNTY district (Pitfall 2 / county pattern).

STEP 5 — office_id back-fill: UPDATE essentials.politicians p SET office_id=o.id FROM essentials.offices o WHERE o.politician_id=p.id AND p.external_id BETWEEN -3200307 AND -3200301 AND p.office_id IS NULL. (More-negative bound first.)

STEP 6 — post-verification DO block (NV-substituted from mig 244 Step 6): Gate (a) exactly 1 government row named 'Clark County, Nevada, US'; Gate (b) exactly 7 offices joined to the COUNTY district (geo_id='32003', district_type='COUNTY', state='nv'); Gate (c) section-split detector returns 0 orphan rows. RAISE EXCEPTION on any mismatch; RAISE NOTICE the passing counts.

COMMIT. Then OUTSIDE the transaction, register the structural migration: INSERT the ('1055','clark_county_commission') row into the migration ledger using the 2-column (version, name) form ON CONFLICT (version) DO NOTHING.

Do NOT apply this migration — that is an inline orchestrator step (Task 3). No headshot rows, no stance rows, no compass INSERTs in this file.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql"; test -f "$F" &amp;&amp; grep -q "INSERT INTO essentials.governments" "$F" &amp;&amp; grep -q "Clark County, Nevada, US" "$F" &amp;&amp; grep -q "INSERT INTO essentials.chambers" "$F" &amp;&amp; grep -q "Board of County Commissioners" "$F" &amp;&amp; grep -q "geo_id = '32003'" "$F" &amp;&amp; grep -q "COUNTY" "$F" &amp;&amp; grep -q "state = 'nv'" "$F" &amp;&amp; grep -q -- "-3200301" "$F" &amp;&amp; grep -q -- "-3200307" "$F" &amp;&amp; [ "$(grep -c "ON CONFLICT (external_id) DO NOTHING" "$F")" -eq 7 ] &amp;&amp; grep -q "BETWEEN -3200307 AND -3200301" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql exists.
    - Contains exactly one government INSERT ('Clark County, Nevada, US', type='County', state='NV' UPPERCASE, geo_id='32003'), guarded by WHERE NOT EXISTS — standalone, with no `government_id`/parent linkage to State of Nevada.
    - Contains exactly one chamber INSERT ('Board of County Commissioners', name_formal non-empty), scoped to the Clark County government, guarded by WHERE NOT EXISTS; the auto-generated chamber path column is NOT in the column list.
    - Contains an idempotent COUNTY district INSERT guarded by WHERE NOT EXISTS on (geo_id='32003', district_type='COUNTY', state='nv' LOWERCASE).
    - Contains exactly 7 politician CTEs (`ON CONFLICT (external_id) DO NOTHING` appears 7 times) covering -3200301..-3200307 with the exact roster names/parties/titles (Naft A / Kirkpatrick B / Becker C Republican / McCurdy II D / Segerblom E / Jones F / Gibson G); the 6 Democrats + Becker Republican split is correct.
    - Every office WHERE clause uses d.geo_id='32003' AND d.district_type='COUNTY' AND d.state='nv' LOWERCASE; every office sets representing_state='NV' UPPERCASE and role_canonical NULL; every office INSERT guarded by NOT EXISTS on (district_id, politician_id) and `AND p.id IS NOT NULL`.
    - No phantom 8th seat: exactly 7 office INSERTs; Chair/Vice-Chair are titles-on-seats (Naft/McCurdy), no separate Chair office row, no LOCAL_EXEC.
    - One office_id back-fill present (BETWEEN -3200307 AND -3200301, guarded by office_id IS NULL).
    - A post-verification DO block asserts 1 government + 7 offices + 0 section-split.
    - The ledger registration line for ('1055','clark_county_commission') appears OUTSIDE the BEGIN/COMMIT block (2-column form); no audit/headshot/stance rows in this file.
    - No `photo_origin_url`; the auto-generated path column name does not appear in any INSERT column list or comment.
  </acceptance_criteria>
  <done>1055_clark_county_commission.sql is written to disk per the 7-step Multnomah-substituted template and passes all acceptance criteria. Not yet applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Wave-0 DB probes (inline) + roster operator-verification checkpoint</name>
  <action>
INLINE ORCHESTRATOR STEPS (run BEFORE applying anything — gsd-executor has NO supabase MCP):
(P1) Ledger MAX — run `SELECT MAX(CAST(version AS INTEGER)) FROM supabase_migrations.schema_migrations;`. Assert MAX=1053; next structural=1055. Resolve any counter drift before proceeding.
(P2) external_id collision check — run `SELECT external_id FROM essentials.politicians WHERE external_id BETWEEN -3200307 AND -3200301 ORDER BY 1;`. Assert ZERO rows (the -3200301..-3200307 block is unused).
(P3) COUNTY district existence + casing — run `SELECT id, geo_id, district_type, state, label FROM essentials.districts WHERE geo_id='32003' AND district_type='COUNTY';`. Assert exactly 1 row with state='nv' LOWERCASE (id should be f3708f34-6e23-4771-a8f7-44e400a23337). If state is uppercase or the row is missing, STOP — the office WHERE clauses would silently match 0 rows.

Then PAUSE for the roster operator-verification checkpoint below. Present the what-built summary and how-to-verify; wait for the resume signal BEFORE the migration is applied (Task 3). No DB writes in this task.
  </action>
  <what-built>Wave-0 probes ran inline: ledger MAX confirmed 1053 (next structural=1055); external_id block -3200301..-3200307 confirmed unused; the Clark County COUNTY district confirmed present with state='nv' lowercase. The structural migration 1055 is written (Task 1) but NOT yet applied. The 7-member Board of County Commissioners roster (from clarkcountynv.gov + Wikipedia cross-check) is staged for your sign-off.</what-built>
  <how-to-verify>
1. Probe results — confirm P1 returned MAX=1053, P2 returned 0 rows, P3 returned exactly 1 COUNTY row with state='nv' lowercase. If any fails, STOP and report.
2. Roster currency — spot-check the 7 seats against clarkcountynv.gov "Board of County Commissioners": District A Michael Naft, B Marilyn Kirkpatrick, C April Becker, D William McCurdy II, E Tick Segerblom, F Justin Jones, G James B. Gibson. Confirm there are NO vacancies and no mid-2026 appointment/resignation since research.
3. CHAIR CORRECTION — confirm the CHAIR is Michael Naft (District A), NOT Marilyn Kirkpatrick (Naft elected Chair 2026-01-07). Vice-Chair = McCurdy (District D). The migration sets Naft's title as 'Commissioner (District A)' with Chair handled by display ordering (no separate office row).
4. Party — confirm April Becker (District C) is the sole Republican; the other 6 are Democratic. (Party is stored, never displayed — antipartisan.)
5. Name forms — confirm 'William McCurdy II' (with the II suffix), 'Tick Segerblom', 'James B. Gibson' are spelled as written in the migration.
6. If any seat changed: tell the executor the corrected district→name→party→title before approving.
  </how-to-verify>
  <verify><human-check>Operator confirms the Wave-0 probe results (MAX=1053, 0 collisions, COUNTY district state='nv'), and the 7-member roster currency + Chair=Naft against clarkcountynv.gov before migration 1055 is applied.</human-check></verify>
  <done>Probes pass (MAX=1053, 0 external_id collisions, COUNTY district state='nv'), and the operator approves the 7-member roster with Chair=Naft (or supplies corrections).</done>
  <resume-signal>Type "approved" or describe any roster change (new appointment, vacancy, Chair change, spelling) or probe mismatch.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply migration 1055 inline + roster/section-split/ledger audit</name>
  <files>C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql</files>
  <read_first>
    - .planning/phases/161-clark-county-commission-deep-seed/161-VALIDATION.md (Per-Task Verification Map — checks 1,2,6,7,8,9 + Section-Split SQL)
    - .planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md (Migration Counter Reference + Section-Split Verification SQL)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): apply C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql via `psql -f` (DATABASE_URL from C:/EV-Accounts/backend/.env) or `mcp__supabase-local__apply_migration`. The migration's own DO block gates 1 government + 7 offices + 0 section-split. Then run the audits and record results for the SUMMARY:
(1) BCC office count — offices joined to chamber 'Board of County Commissioners' under government 'Clark County, Nevada, US' → expect 7.
(2) County-district linkage — offices→districts WHERE d.geo_id='32003' AND d.district_type='COUNTY' AND d.state='nv' AND chamber name='Board of County Commissioners' → expect 7.
(3) No phantom 8th seat — same count = exactly 7 (Chair is a title-on-seat, not a separate office).
(4) office_id back-fill — `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -3200307 AND -3200301 AND office_id IS NOT NULL` → expect 7.
(5) Casing — `SELECT DISTINCT state FROM essentials.districts d JOIN essentials.offices o ON o.district_id=d.id JOIN essentials.chambers c ON c.id=o.chamber_id WHERE c.name='Board of County Commissioners'` → expect only 'nv' lowercase.
(6) Section-split — run the exact COUNTY-tier section-split SQL from 161-VALIDATION.md → expect 0 rows.
(7) Ledger — confirm ('1055','clark_county_commission') is registered and ledger MAX=1055.
Capture the 7 external_id → politician UUID map (`SELECT external_id, id FROM essentials.politicians WHERE external_id BETWEEN -3200307 AND -3200301 ORDER BY external_id`) — Plan 02's headshot script + migration 1056 and Plan 03's stance migrations consume this. Record any deviation (e.g. an office count below 7 implies a casing or guard miss — fix and re-apply idempotently).
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: migration 1055 applied successfully (DO block passed; no path-column/slug errors).
    - BCC office count = 7; county-district linkage = 7 (all at state='nv').
    - Exactly 7 offices — no phantom 8th seat.
    - office_id back-filled for all 7 commissioners (count = 7).
    - DISTINCT linked-district state = only 'nv' lowercase.
    - Section-split query returns 0 rows.
    - Ledger registers ('1055','clark_county_commission'); MAX version = 1055.
    - 7 external_id → UUID map captured for all 7 commissioners (handed to Plans 02 + 03).
  </acceptance_criteria>
  <done>Migration 1055 applied; 7 commissioner offices linked to the single COUNTY district at state='nv', office_id back-filled, 0 section-split, ledger MAX=1055, and the 7 UUIDs captured for Plans 02 + 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration file → live DB | service-role apply of seed SQL; only the inline orchestrator applies |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-01 | Tampering | wrong-government section split (offices attach under State of Nevada geo_id='32' instead of Clark County geo_id='32003') | mitigate | Chamber scoped to government_id WHERE name='Clark County, Nevada, US'; section-split SQL gate must return 0 rows (Task 3 check 6) + inline DO block Gate (c) |
| T-161-02 | Tampering | silent zero-office no-op from uppercase state='NV' in the COUNTY district WHERE | mitigate | Every office WHERE uses d.state='nv' lowercase + district_type='COUNTY'; Wave-0 P3 confirms casing; BCC count audit asserts 7 (Task 3 checks 1-3) |
| T-161-03 | Spoofing | wrong Chair (Kirkpatrick instead of Naft) | mitigate | Roster operator-verification checkpoint (Task 2) confirms Chair=Naft against clarkcountynv.gov before apply; RESEARCH chair correction documented |
| T-161-04 | Information disclosure | service-role key leakage | accept | Migration applied via psql/MCP by inline orchestrator only; no key in the .sql file; file committed to EV-Accounts repo |
| T-161-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (data-seed only); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
After all tasks (inline orchestrator):
1. BCC office count = 7; county-district linkage = 7 at state='nv'.
2. Exactly 7 offices (no phantom 8th seat); office_id back-filled for all 7.
3. DISTINCT linked-district state = only 'nv' lowercase.
4. Section-split SQL → 0 rows; inline DO block passed.
5. Ledger MAX = 1055; ('1055','clark_county_commission') registered.
</verification>

<success_criteria>
- Standalone government 'Clark County, Nevada, US' (geo_id='32003') + chamber 'Board of County Commissioners' created, NOT under State of Nevada.
- 7 commissioners seeded, each with a single office linked to the single pre-existing COUNTY district at state='nv', office_id back-filled.
- Chair Naft + Vice-Chair McCurdy modeled as titles-on-seats; no phantom 8th office row; no LOCAL_EXEC.
- Zero section-split defects; migration ledger MAX = 1055.
- No headshots, no stances, no new districts, no fabricated members.
</success_criteria>

<output>
Create `.planning/phases/161-clark-county-commission-deep-seed/161-01-SUMMARY.md` when done (record the Wave-0 probe results, the 7 external_id→UUID map, and all Task-3 audit counts).
</output>
