---
phase: 170-section-banners-continuous-scroll-results
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FilterBar.jsx
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "The Local/State/National tier sort control is gone from FilterBar — no Tier dropdown renders."
    - "The Elected/Appointed Type dropdown, the name search input, and the Compass toggle still render and still work."
  artifacts:
    - path: "src/components/FilterBar.jsx"
      provides: "FilterBar with tier dropdown removed; type filter + name search + compass toggle preserved"
      contains: "TYPE_OPTIONS"
  key_links:
    - from: "src/components/FilterBar.jsx"
      to: "src/pages/Results.jsx"
      via: "FilterBar props (selectedFilter/onFilterChange removed; appointedFilter/searchQuery preserved)"
      pattern: "appointedFilter"
---

<objective>
Remove the Local/State/Federal tier sort control from `FilterBar.jsx` (NAV-01, component side).
Delete the `TIER_OPTIONS` constant and the Tier `<Dropdown>`, and drop `selectedFilter` /
`onFilterChange` from the component's props. The Elected/Appointed `Type` dropdown, the name-search
input, and the Compass toggle are preserved unchanged.

Purpose: With banners dividing the tiers (Plan 170-03), the tier dropdown is obsolete — all tiers
always render in one continuous scroll. This plan makes the FilterBar contract change in isolation;
Plan 170-03 reconciles the Results.jsx call site and the now-constant `selectedFilter` logic.

Output: modified `src/components/FilterBar.jsx`.

NOTE FOR DOWNSTREAM (Plan 170-03): after this plan, `FilterBar` no longer accepts `selectedFilter`
or `onFilterChange`. Plan 170-03 MUST remove those two props from the `<FilterBar … />` call site
in `src/pages/Results.jsx` (lines ~1827-1828) or the page will pass dead props. Both plans run in
sequence (this is Wave 1, 170-03 is Wave 2) so the call-site fix lands in 170-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/170-section-banners-continuous-scroll-results/170-CONTEXT.md
@.planning/phases/170-section-banners-continuous-scroll-results/170-UI-SPEC.md
@.planning/phases/170-section-banners-continuous-scroll-results/170-PATTERNS.md

<interfaces>
<!-- Current FilterBar contract (src/components/FilterBar.jsx). Extracted — use directly. -->

Current props (line 75-81):
  selectedFilter, onFilterChange,           // ← REMOVE these two
  appointedFilter, onAppointedFilterChange, // keep
  searchQuery, onSearchChange,              // keep
  compassMode, onCompassModeChange,         // keep
  isDark,                                   // keep

To remove:
  - TIER_OPTIONS constant (lines 11-16)
  - the Tier <Dropdown ... options={TIER_OPTIONS} ... /> render (lines 97-104)

To preserve unchanged:
  - TYPE_OPTIONS constant + its <Dropdown ... options={TYPE_OPTIONS} /> (the Elected/Appointed filter)
  - the name-search <input> block (lines 113-141)
  - the Compass toggle <label> block (lines 142-163)
  - the inner Dropdown() helper component (still used by the Type dropdown)
  - the exported StickyCompassKey() at the bottom of the file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Tier dropdown and its props from FilterBar</name>
  <files>src/components/FilterBar.jsx</files>
  <read_first>
    - src/components/FilterBar.jsx (full file — current props, TIER_OPTIONS, the two Dropdown renders, the Dropdown helper, StickyCompassKey)
    - .planning/phases/170-section-banners-continuous-scroll-results/170-PATTERNS.md §FilterBar.jsx (exact lines to remove)
    - .planning/phases/170-section-banners-continuous-scroll-results/170-CONTEXT.md D-11 (remove tier control; preserve Type + name search)
  </read_first>
  <action>
    Delete the `TIER_OPTIONS` array (currently lines 11-16). Remove `selectedFilter` and
    `onFilterChange` from the `FilterBar` props destructuring (currently line 76) — the remaining props
    `appointedFilter, onAppointedFilterChange, searchQuery, onSearchChange, compassMode,
    onCompassModeChange, isDark` stay exactly as-is. Delete the Tier `<Dropdown label="Tier"
    ariaLabel="Filter by tier" value={selectedFilter} onChange={onFilterChange} options={TIER_OPTIONS}
    isDark={isDark} />` render block (currently lines 97-104).

    Do NOT touch: the inner `Dropdown` helper function (still used by the Type dropdown), `TYPE_OPTIONS`,
    the Type `<Dropdown>` render, the name-search `<input>` block, the Compass toggle `<label>` block,
    or the exported `StickyCompassKey`. The outer flex container layout stays as-is — after removing the
    Tier dropdown the Type dropdown simply becomes the first child (minimal reflow per D-11; no new
    wrapper needed). Update the top-of-file JSDoc comment that says "consolidates tier/type/name filters"
    to drop the "tier/" reference so the comment matches the new behavior.
  </action>
  <verify>
    <automated>grep -c "TIER_OPTIONS\|selectedFilter\|onFilterChange" src/components/FilterBar.jsx | grep -qx 0 && echo TIER_GONE ; npx eslint src/components/FilterBar.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TIER_OPTIONS" src/components/FilterBar.jsx` returns 0.
    - `grep -c "selectedFilter" src/components/FilterBar.jsx` returns 0 and `grep -c "onFilterChange" src/components/FilterBar.jsx` returns 0.
    - `grep -c "TYPE_OPTIONS" src/components/FilterBar.jsx` returns >= 2 (constant + render still present).
    - The name-search input (`Search by name`) and the Compass toggle label are still present (grep both match).
    - `StickyCompassKey` export is still present.
    - `npx eslint src/components/FilterBar.jsx` exits 0 (no unused-var / undefined errors).
  </acceptance_criteria>
  <done>FilterBar no longer renders or accepts a tier filter; Type dropdown, name search, and Compass toggle are preserved and lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → filter state | name-search text + type-filter selection drive client-side filtering only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-03 | Tampering | name-search input value | accept | Client-side filter only; React escapes rendered text; no query reaches the backend from FilterBar. |
| T-170-SC | Tampering | npm installs | mitigate | No new packages this plan (pure removal). No install task → no slopcheck gate needed. |
</threat_model>

<verification>
- `grep` confirms TIER_OPTIONS, selectedFilter, onFilterChange are fully removed.
- `grep` confirms TYPE_OPTIONS, name search, Compass toggle, StickyCompassKey survive.
- `npx eslint src/components/FilterBar.jsx` exits 0.
</verification>

<success_criteria>
- The tier sort control is removed from FilterBar (NAV-01, component side).
- The Elected/Appointed Type filter, name search, and Compass toggle are preserved and functional.
- FilterBar lints clean with no unused variables or dangling references.
</success_criteria>

<output>
Create `.planning/phases/170-section-banners-continuous-scroll-results/170-02-SUMMARY.md` when done
</output>
