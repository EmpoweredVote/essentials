---
phase: 115-boston-stances
plan: 02
type: execute
wave: 2
depends_on:
  - 115-01
files_modified:
  - C:/EV-Accounts/backend/migrations/578_ruthzee_louijeune_stances.sql
  - C:/EV-Accounts/backend/migrations/579_julia_mejia_stances.sql
  - C:/EV-Accounts/backend/migrations/580_erin_murphy_stances.sql
  - C:/EV-Accounts/backend/migrations/581_henry_santana_stances.sql
autonomous: true
requirements:
  - MA-STANCES-05
must_haves:
  truths:
    - "All 4 at-large councillors (external_ids -2507000002 through -2507000005) have rows attempted in inform.politician_answers (blank spokes acceptable per D-01 if no evidence)"
    - "100% citation rate for this batch: every inform.politician_context row has at least 1 URL in sources array (uncited = 0)"
    - "Zero unpaired rows for this batch: every politician_answers row has a matching politician_context row (unpaired = 0)"
    - "No defaulted stances: topics with zero evidence are omitted entirely — no neutral 3.0 inserted"
    - "Migrations 578–581 created and applied one at a time (never in parallel)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/578_ruthzee_louijeune_stances.sql"
      provides: "Stance migration for Ruthzee Louijeune (At-Large, external_id -2507000002)"
    - path: "C:/EV-Accounts/backend/migrations/579_julia_mejia_stances.sql"
      provides: "Stance migration for Julia M. Mejia (At-Large, external_id -2507000003)"
    - path: "C:/EV-Accounts/backend/migrations/580_erin_murphy_stances.sql"
      provides: "Stance migration for Erin J. Murphy (At-Large, external_id -2507000004)"
    - path: "C:/EV-Accounts/backend/migrations/581_henry_santana_stances.sql"
      provides: "Stance migration for Henry Santana (At-Large, external_id -2507000005)"
  key_links:
    - from: "migrations 578–581"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE upsert"
      pattern: "INSERT INTO inform.politician_answers / INSERT INTO inform.politician_context"
---

<objective>
Evidence-only compass stances for the 4 Boston City Council At-Large members — Ruthzee Louijeune, Julia M. Mejia, Erin J. Murphy, and Henry Santana (external_ids -2507000002 through -2507000005). At-large councillors have citywide records; Louijeune and Mejia are well-documented progressives; Murphy is more moderate; Santana is a newer member with a thinner record.

Migrations 578–581, one per councillor, applied immediately after each research session. Each politician researched and applied fully before the next begins (sequential, never parallel per D-08).

Output: 4 migration files applied; per-politician unpaired=0 and uncited=0 for each; stance counts recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/115-boston-stances/115-RESEARCH.md
@.planning/phases/115-boston-stances/115-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ruthzee Louijeune (At-Large, external_id -2507000002) — migration 578</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: authoritative 44-topic UUID reference block + exact SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (At-Large Councillors section: Louijeune — immigration, civil-rights, housing focus; Research Source Guidance for City Councillors; Anti-Patterns section)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01: no defaults; D-08: one at a time; D-10: 100% citation)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 2. Do not start Task 2 while Task 1 is in progress.

    Step A — Research (web search, ONE session per D-08):
      Politician: Ruthzee Louijeune, Boston City Councillor At-Large (external_id -2507000002).
      Sources: https://boston.gov/departments/city-council/ruthzee-louijeune, https://boston.legistar.com/ (voting records), https://www.wbur.org (WBUR coverage), ballotpedia.org/Ruthzee_Louijeune, Boston Globe.
      Focus topics for Louijeune: immigration, local-immigration, civil-rights, housing, rent-regulation, public-safety-approach, homelessness, transportation-priorities, city-sanitation.
      Per D-01: only insert topics with documented evidence. Do NOT insert neutral 3.0 for unknown topics.
      Resolve UUID via: SELECT id FROM essentials.politicians WHERE external_id = -2507000002;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/578_ruthzee_louijeune_stances.sql
      Header: "Migration 578: Ruthzee Louijeune Stances" (At-Large Councillor, external_id -2507000002).
      Copy topic UUID block from 416_christopher_flanagan_stances.sql lines 14–58.
      Follow exact SQL pattern: politician_answers INSERT first, then politician_context INSERT, both ON CONFLICT DO UPDATE.
      Reasoning in $$...$$ dollar-quoting; sources as ARRAY[...]::text[]::text[] with full URLs.

    Step C — Apply:
      Pass full content of 578_ruthzee_louijeune_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify:
      SELECT COUNT(*) AS n FROM inform.politician_answers
        WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -2507000002);
      SELECT COUNT(*) AS unpaired FROM inform.politician_answers pa
        LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
        WHERE pa.politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000002)
          AND pc.politician_id IS NULL;
      SELECT COUNT(*) AS uncited FROM inform.politician_context
        WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000002)
          AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/578_ruthzee_louijeune_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000002.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 578_ruthzee_louijeune_stances.sql exists and contains BEGIN; ... COMMIT;
    - SELECT unpaired for external_id=-2507000002 returns 0
    - SELECT uncited for external_id=-2507000002 returns 0
    - Stance count (n) recorded in SUMMARY (0 acceptable if no public evidence found)
  </acceptance_criteria>
  <done>Migration 578 applied; unpaired=0, uncited=0 for Louijeune.</done>
</task>

<task type="auto">
  <name>Task 2: Julia M. Mejia (At-Large, external_id -2507000003) — migration 579</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID reference block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (At-Large Councillors: Mejia — education, civil-rights, immigration; Research Source Guidance)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 3.

    Step A — Research (web search, ONE session):
      Politician: Julia M. Mejia, Boston City Councillor At-Large (external_id -2507000003).
      Sources: https://boston.gov/departments/city-council/julia-mejia, https://boston.legistar.com/, https://www.wbur.org, ballotpedia.org/Julia_Mejia, Boston Globe.
      Focus topics: immigration, local-immigration, civil-rights, education (school-vouchers), housing, public-safety-approach, homelessness, city-sanitation, healthcare.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000003;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/579_julia_mejia_stances.sql
      Header: "Migration 579: Julia M. Mejia Stances" (At-Large Councillor, external_id -2507000003).
      Same format as 578 (416 reference file exact pattern).

    Step C — Apply:
      Pass full content of 579_julia_mejia_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify:
      SELECT COUNT(*) AS n / unpaired / uncited for external_id=-2507000003 (same three queries as Task 1 Step D).
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/579_julia_mejia_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000003.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 579_julia_mejia_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000003
    - Stance count recorded (0 acceptable if no evidence)
  </acceptance_criteria>
  <done>Migration 579 applied; unpaired=0, uncited=0 for Mejia.</done>
</task>

<task type="auto">
  <name>Task 3: Erin J. Murphy (At-Large, external_id -2507000004) — migration 580</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID reference block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (At-Large Councillors: Murphy — more moderate, community-safety, education focus)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D before moving to Task 4.

    Step A — Research (web search, ONE session):
      Politician: Erin J. Murphy, Boston City Councillor At-Large (external_id -2507000004).
      Sources: https://boston.gov/departments/city-council/erin-murphy, https://boston.legistar.com/, https://www.wbur.org, ballotpedia.org, Boston Globe, Boston Herald.
      Focus topics: public-safety-approach, education (school-vouchers), housing, city-sanitation, homelessness.
      Murphy is more moderate than other at-large councillors; expect fewer progressive stances and potentially conservative values on public safety.
      Per D-01: no default neutral values. Blank spoke is correct if no documented position.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000004;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/580_erin_murphy_stances.sql
      Header: "Migration 580: Erin J. Murphy Stances" (At-Large Councillor, external_id -2507000004).

    Step C — Apply:
      Pass full content of 580_erin_murphy_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (same three queries; external_id=-2507000004):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/580_erin_murphy_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000004.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 580_erin_murphy_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000004
    - Stance count recorded
  </acceptance_criteria>
  <done>Migration 580 applied; unpaired=0, uncited=0 for Murphy.</done>
</task>

<task type="auto">
  <name>Task 4: Henry Santana (At-Large, external_id -2507000005) — migration 581</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: topic UUID reference block + SQL format)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (At-Large Councillors: Santana — newer member first term 2023, thinner record; Research Source Guidance)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-08, D-10)
  </read_first>
  <action>
    IMPORTANT: Complete ALL of Steps A–D. This is the last task in Plan 02.

    Step A — Research (web search, ONE session):
      Politician: Henry Santana, Boston City Councillor At-Large (external_id -2507000005).
      Sources: https://boston.gov/departments/city-council/henry-santana, https://boston.legistar.com/, https://www.wbur.org, Boston Globe.
      Santana is a first-term councillor elected 2023 — expect a thinner public record. Blank spokes are the correct outcome if no evidence is found per D-01. Do NOT fabricate stances.
      Focus if any evidence exists: housing, immigration, local-immigration, economic-development, public-safety-approach.
      Resolve UUID: SELECT id FROM essentials.politicians WHERE external_id = -2507000005;

    Step B — Write migration:
      File: C:/EV-Accounts/backend/migrations/581_henry_santana_stances.sql
      Header: "Migration 581: Henry Santana Stances" (At-Large Councillor, external_id -2507000005).
      If no evidence found: write header + BEGIN; -- No evidence found for any active compass topic per D-01. Blank spokes intentional.; COMMIT;

    Step C — Apply:
      Pass full content of 581_henry_santana_stances.sql to mcp__supabase-local__execute_sql.

    Step D — Verify (same three queries; external_id=-2507000005):
      Required: unpaired=0, uncited=0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/581_henry_santana_stances.sql" returns True.
      DB: unpaired=0, uncited=0 for external_id=-2507000005.
    </automated>
  </verify>
  <acceptance_criteria>
    - File 581_henry_santana_stances.sql exists with BEGIN; ... COMMIT;
    - unpaired=0, uncited=0 for external_id=-2507000005
    - Stance count recorded (0 acceptable and expected for thin-record member)
  </acceptance_criteria>
  <done>Migration 581 applied; unpaired=0, uncited=0 for Santana.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web research → SQL migration | Scraped reasoning text enters SQL dollar-quoted strings |
| mcp__supabase-local | All SQL executes against remote production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-04 | Tampering | reasoning text in politician_context | mitigate | Dollar-quoting ($$...$$) for all reasoning — never single-quote concatenation |
| T-115-05 | Spoofing | sources array URLs | mitigate | Only real URLs from pages actually consulted; never fabricated |
| T-115-06 | Tampering | politician_id UUID cross-contamination | mitigate | Resolve each UUID fresh via WHERE external_id=N; never copy UUID from prior politician's migration |
</threat_model>

<verification>
After all 4 tasks complete, run batch check via mcp__supabase-local__execute_sql:

  SELECT p.external_id, p.full_name, COUNT(pa.*) AS stance_count
  FROM essentials.politicians p
  LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
  WHERE p.external_id BETWEEN -2507000005 AND -2507000002
  GROUP BY p.external_id, p.full_name
  ORDER BY p.external_id;
Expected: 4 rows; stance_count ≥0 for all.

  SELECT COUNT(*) AS uncited_batch FROM inform.politician_context pc
  JOIN essentials.politicians p ON p.id=pc.politician_id
  WHERE p.external_id BETWEEN -2507000005 AND -2507000002
    AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL OR array_length(pc.sources,1)=0);
Expected: 0.

  SELECT COUNT(*) AS unpaired_batch FROM inform.politician_answers pa
  JOIN essentials.politicians p ON p.id=pa.politician_id
  LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
  WHERE p.external_id BETWEEN -2507000005 AND -2507000002 AND pc.politician_id IS NULL;
Expected: 0.
</verification>

<success_criteria>
- Migrations 578–581 created and applied (one at a time, sequential per D-08)
- All 4 at-large councillors have rows attempted (blank documented as intentional per D-01)
- Batch uncited=0, unpaired=0 confirmed
- D-01 through D-10 honored throughout
</success_criteria>

<output>
Create `.planning/phases/115-boston-stances/115-02-SUMMARY.md` documenting:
- Stance count per councillor (table: external_id | name | migration | stance_count | status)
- Councillors with blank spokes and reason
- Batch quality gate results (uncited=0, unpaired=0)
- Next migration: 582
</output>
