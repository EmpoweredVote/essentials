---
phase: 69-landing-elections-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Landing.jsx
autonomous: true
requirements:
  - CITIES-07

must_haves:
  truths:
    - "Landing.jsx COVERAGE_AREAS contains exactly 7 entries for CA cities: Los Angeles County, San Diego, Fremont, San Francisco, San Jose, Sacramento, Berkeley"
    - "Each new entry uses the shape { county, state, browseGovernmentList, browseStateAbbrev } with no extra fields"
    - "New entries use city name in the county field (e.g., 'San Francisco'), not county descriptors (per D-03)"
  artifacts:
    - path: "src/pages/Landing.jsx"
      provides: "COVERAGE_AREAS array with 4 new CA city entries"
      contains: "San Francisco"
  key_links:
    - from: "COVERAGE_AREAS entry browseGovernmentList"
      to: "essentials.geofence_boundaries geo_id"
      via: "browseGovernmentList parameter passed to /results route"
      pattern: "browseGovernmentList.*0667000"
---

<objective>
Add 4 missing CA cities to the Landing.jsx COVERAGE_AREAS array so users can browse San Francisco, San Jose, Sacramento, and Berkeley government officials from the landing page.

Purpose: Complete the v7.0 CA coverage map — LA, SD, and Fremont are already present; this task adds the 4 cities seeded in Phases 63-68.
Output: Modified src/pages/Landing.jsx with 4 new COVERAGE_AREAS entries inserted after the Fremont entry.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-landing-elections-discovery/69-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 CA city entries to Landing.jsx COVERAGE_AREAS</name>
  <files>src/pages/Landing.jsx</files>

  <read_first>
    src/pages/Landing.jsx — read the full COVERAGE_AREAS array (lines 8-16) to see its exact current shape before editing. Confirm: Los Angeles County, San Diego, and Fremont entries are already present. Confirm the exact indentation and comma style used.
  </read_first>

  <action>
    Insert 4 new entries into the COVERAGE_AREAS array in src/pages/Landing.jsx, immediately after the Fremont entry (currently line 12) and before the Collin County entry.

    The 4 new entries to insert (per D-01, D-02, D-03):

    { county: 'San Francisco', state: 'California', browseGovernmentList: ['0667000'], browseStateAbbrev: 'CA' },
    { county: 'San Jose', state: 'California', browseGovernmentList: ['0668000'], browseStateAbbrev: 'CA' },
    { county: 'Sacramento', state: 'California', browseGovernmentList: ['0664000'], browseStateAbbrev: 'CA' },
    { county: 'Berkeley', state: 'California', browseGovernmentList: ['0606000'], browseStateAbbrev: 'CA' },

    Geo_id reference (all verified in DB against essentials.geofence_boundaries G4110 rows):
    - San Francisco: geo_id='0667000'
    - San Jose: geo_id='0668000'
    - Sacramento: geo_id='0664000'
    - Berkeley: geo_id='0606000'

    DO NOT modify the existing Los Angeles County entry (browseGovernmentList: ['0644000','06037','0622710'] with browseCountyGeoId). DO NOT add a second LA or Los Angeles entry. LA, SD, and Fremont are already present — only add the 4 entries above (per D-02, D-10).

    Match the existing indentation and trailing comma style exactly.
  </action>

  <verify>
    <automated>
      node -e "const src = require('fs').readFileSync('src/pages/Landing.jsx','utf8'); const counts = ['0667000','0668000','0664000','0606000'].filter(id => src.includes(id)); console.log('Found geo_ids:', counts.length, counts); if(counts.length !== 4) process.exit(1);"
    </automated>
  </verify>

  <acceptance_criteria>
    - src/pages/Landing.jsx COVERAGE_AREAS contains exactly these 4 new browseGovernmentList values: '0667000' (SF), '0668000' (SJ), '0664000' (SAC), '0606000' (Berkeley)
    - Each new entry uses the exact shape: { county: 'CityName', state: 'California', browseGovernmentList: ['geo_id'], browseStateAbbrev: 'CA' } — no extra fields like browseCountyGeoId
    - The county field for each new entry is the city name ('San Francisco', 'San Jose', 'Sacramento', 'Berkeley') — not county descriptors
    - The existing Los Angeles County entry retains its original browseGovernmentList: ['0644000', '06037', '0622710'] and browseCountyGeoId: '06037' — unchanged
    - The file parses without JavaScript syntax errors: `node -e "require('./src/pages/Landing.jsx')"` or visual inspection of array commas
    - Total CA city entries in COVERAGE_AREAS: 7 (LA County, San Diego, Fremont, San Francisco, San Jose, Sacramento, Berkeley)
  </acceptance_criteria>

  <done>COVERAGE_AREAS contains all 7 CA cities; 4 new entries match the D-01/D-03 shape exactly; no existing entries modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| COVERAGE_AREAS → /results route | browseGovernmentList geo_ids passed as URL params; values are hardcoded constants, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — JSX edit only |
| T-69-01-01 | Tampering | Landing.jsx COVERAGE_AREAS | accept | Geo_ids are hardcoded constants sourced from verified DB; no user-controlled input path |
</threat_model>

<verification>
After applying the edit, run:
```
node -e "const src = require('fs').readFileSync('src/pages/Landing.jsx','utf8'); ['0667000','0668000','0664000','0606000'].forEach(id => { if(!src.includes(id)) { console.error('MISSING: ' + id); process.exit(1); } }); console.log('All 4 geo_ids present');"
```
Expected output: "All 4 geo_ids present"
</verification>

<success_criteria>
Landing.jsx COVERAGE_AREAS contains all 7 CA cities with correct geo_ids. New entries: SF='0667000', SJ='0668000', SAC='0664000', Berkeley='0606000'. No existing entries modified.
</success_criteria>

<output>
Create `.planning/phases/69-landing-elections-discovery/69-01-SUMMARY.md` when done.
</output>
