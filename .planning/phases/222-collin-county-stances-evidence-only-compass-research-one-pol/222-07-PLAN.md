---
phase: 222-collin-county-stances-evidence-only-compass-research-one-pol
plan: 07
type: execute
wave: 7
depends_on: ["222-06"]
files_modified:
  - "[EV-Accounts] C:/EV-Accounts/backend/migrations/<live-next-number>_222_prosper_celina_stances.sql (NEW — number assigned live)"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md (APPEND)"
autonomous: false
requirements: [COLLIN-STANCE-01, COLLIN-STANCE-02]
must_haves:
  truths:
    - "Every un-stanced Prosper and Celina officeholder on the 222-01 live worklist was individually attempted, ONE AT A TIME, never two research passes concurrently (D-01, D-02, D-03)"
    - "Longview's disposition is explicit: either its un-stanced officeholders were researched here, or the register records that the 222-01 BEFORE snapshot showed zero un-stanced Longview officeholders — it is never silently omitted (RESEARCH.md Open Question 2)"
    - "Every stance written carries a matching inform.politician_context row whose reasoning names a specific dated action, quote, vote, or questionnaire answer and whose sources array holds at least one real URL that was actually fetched (D-04, D-05)"
    - "Every Prosper, Celina, and (if applicable) Longview worklist name appears in exactly one of {this plan's applied migration rows, 222-CONFIRMED-BLANK.md} — never neither, never both (COLLIN-STANCE-01)"
    - "A government where every attempted person yields no evidence produces zero inform.politician_answers rows and a complete register section — an all-blank government is a SUCCESS outcome, not a failure (COLLIN-STANCE-01/empty, COLLIN-STANCE-02/empty)"
    - "This plan writes only (politician_id, topic_id) pairs that had no prior row per the 222-01 do-not-overwrite set (COLLIN-STANCE-01/adjacency, D-07)"
    - "Every value is a whole integer 1 through 5 — never fractional, never a defaulted middle value (COLLIN-STANCE-02/adjacency)"
    - "100% of stances in this plan, and 100% of any stance valued 1 or 5, were re-verified against their cited source before commit — Prosper and Celina are D-02 first-pass cities and inherit the Tier-1 resample rate (222-VALIDATION.md)"
    - "The migration is a single BEGIN/COMMIT transaction using ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables (COLLIN-STANCE-02/idempotency, COLLIN-STANCE-02/concurrency)"
    - "Prosper is committed before Celina research begins, so an interruption loses at most one government's un-committed work (COLLIN-STANCE-01/concurrency)"
    - "The migration has been APPLIED to production and the evidence-integrity gate returns zero rows for this plan's politician_ids — authored-but-unapplied SQL does not count as done"
  artifacts:
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
      provides: "The Prosper, Celina, and Longview sections of the per-person blank register (D-08)"
      contains: "## Prosper"
  key_links:
    - "Stances attach by politician_id taken verbatim from 222-01-WORKLIST.md — never by name match"
    - "topic_id values come from the 11 canonical UUIDs verified live in 222-01; wording comes from 222-RESEARCH.md section B, which overrides the researcher agent file for housing, taxes, and healthcare"
  prohibitions:
    - "MUST NOT infer or assign any stance from party affiliation, endorsement, donor history, or pattern-matching to another official in the same city — only an explicit, on-topic, cited statement, recorded vote, or questionnaire answer may set a chair"
    - "MUST NOT assign a chair when the evidence is suggestive-but-not-explicit, and MUST NOT default to a middle value to avoid leaving a blank — a blank spoke is the correct, successful outcome"
    - "MUST NOT record a source URL that was not actually fetched and read, or that does not contain the cited quote, vote, or questionnaire answer"
    - "MUST NOT attribute a stance to an officeholder on the basis of a same-named person in a different city or state; the source must confirm the Texas city before the evidence is used"
    - "MUST NOT name, imply, or rely on a politician's party in reasoning text — party is never used for inference and never displayed"
---

<objective>
Evidence-only compass stance research for every un-stanced **Prosper** (geo_id `4859696`) and **Celina** (geo_id `4813684`) officeholder on the 222-01 live worklist — plus the explicit **Longview** (geo_id `4843888`) disposition — one politician at a time, across all 11 canonical topics. Author, commit, and apply the migration and record every honest blank.

Purpose: Prosper and Celina complete D-02's seven named first-pass cities. Longview is the Gregg County city bundled into the Texas browse list; it already carries a coverage chip, so it is expected to have zero un-stanced people — but "expected" is not "verified", and it must not fall out of the phase silently.
Output: one AUDIT-ONLY migration in the EV-Accounts repo, Prosper / Celina / Longview sections in `222-CONFIRMED-BLANK.md`, and applied production rows.
</objective>

## Artifacts this phase produces

Phase 222 (all plans) produces data + planning artifacts only. No application code, no schema change, no packages.

- **[EV-Accounts repo]** `C:/EV-Accounts/backend/migrations/<N>_222_<slug>_stances.sql` — one numbered, git-committed, **AUDIT-ONLY** (NOT registered in `schema_migrations`) migration per research plan. Numbers assigned LIVE. This plan owns `<N>_222_prosper_celina_stances.sql`.
- **[essentials repo]** `.planning/phases/222-.../222-01-WORKLIST.md` (222-01), `222-CONFIRMED-BLANK.md` (created 222-01, appended by every research plan), `222-NN-SUMMARY.md` per plan.
- **[essentials repo]** `.planning/REQUIREMENTS.md` — COLLIN-STANCE-01/02 + traceability (222-01).
- **[essentials repo]** `src/lib/coverage.js` — `hasContext: true` reconcile (222-18 only).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prosper — research one politician at a time, write the migration block, append the register, commit</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the Prosper rows are this task's exact scope, plus the live next-available migration number; take politician_id and full_name verbatim.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B is the SOURCE OF TRUTH for the verbatim question and five answer options of all 11 topics, OVERRIDING the researcher agent file for `housing`, `taxes`, and `healthcare` (Pitfall 1). Also read Pitfall 2 (homonym trap) and section C (Prosper is Medium tier).
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — evidence hierarchy, quote-selection gates, inversion-trap table, de-identification protocol. Ignore its housing/taxes/healthcare scale text.
    - `C:/EV-Accounts/backend/migrations/1307_oro_valley_mayor_stances.sql` — the exact answers-plus-context upsert shape and AUDIT-ONLY header to copy.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format to mirror.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_prosper_celina_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Never dispatch two research passes concurrently. Process the Prosper worklist rows in 222-01-WORKLIST.md order, mayor first.

    For each person, run one research pass, preferring the `politician-stance-researcher` subagent and falling back to a single general-purpose research subagent seeded with that file's methodology if the project-scoped agent does not resolve. Pass the full verbatim question plus five answer options for all 11 topics from RESEARCH.md section B. WebFetch is the primary tool; a light WebSearch budget of a few calls per person is allowed only to discover a URL when direct fetch patterns fail.

    Prosper source order (D-05 acceptable types): prospertx.gov council pages and the searchable agenda and minutes archive for recorded votes and motions; Ballotpedia individual candidate pages; Community Impact Newspaper Collin County candidate Q&As; VOTE411 and League of Women Voters of Collin County; local news coverage; the officeholder's campaign site. Confirm the source names Prosper, Texas (Pitfall 2).

    Assign a chair 1 through 5 ONLY where a cited source shows an explicit, on-topic position — a stated stance, a recorded council vote or motion, or a candidate-questionnaire answer (D-04). Suggestive-but-not-explicit is a blank. No party inference, no pattern inference. Attempt all 8 canonical Local Lens topics (D-06) plus the 3 legacy-tail topics where explicit evidence exists (D-07). Skip any (politician_id, topic_id) pair in the 222-01 do-not-overwrite set.

    Before writing SQL, self-audit: Prosper is a D-02 first-pass city and inherits the Tier-1 100% resample rate. Re-fetch every cited source and confirm the quote, vote, or questionnaire answer is genuinely present, on-topic for that specific compass topic, and pointing in the direction of the assigned chair, checking direction against the inversion-trap table for that topic_key. Demote any stance that fails to a blank rather than softening its reasoning; demote generically-evaluative reasoning the same way.

    Re-read the live next-available migration number from 222-01-WORKLIST.md, confirm it is still free by listing `C:/EV-Accounts/backend/migrations`, and create `C:/EV-Accounts/backend/migrations/<N>_222_prosper_celina_stances.sql` in the EV-Accounts repo with an AUDIT-ONLY header naming Phase 222, Prosper and Celina, and the date, stating it is deliberately not registered in `schema_migrations`. Open a single BEGIN/COMMIT transaction and write the Prosper block: for each surviving (politician, topic) pair, an insert into `inform.politician_answers (politician_id, topic_id, value)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value`, immediately followed by an insert into `inform.politician_context (politician_id, topic_id, reasoning, sources)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources`. Dollar-quote all reasoning text, using a distinct tag on delimiter collision. Values are whole numbers 1 through 5. If Prosper sources nothing, leave the file uncreated for now — Celina may still need it.

    Append a `## Prosper` section to `222-CONFIRMED-BLANK.md` with an `Evidence:` line naming the source families actually checked and one bullet per attempted person with no sourced position (full_name, office title, politician_id); write the heading even if there are zero blanks, stating that all N attempted officeholders were sourced. Update the running `Count:` line.

    Commit both repos locally before starting Celina — this is the per-government commit boundary that bounds interruption loss. In EV-Accounts use `git -C "C:/EV-Accounts" add` then `git -C "C:/EV-Accounts" commit`; never `cd` into that repo and never `git push` it (a push deploys accounts-api to Render and carries every commit already ahead).
  </action>
  <verify>
    <automated>grep -q "^## Prosper" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - The number of Prosper entries researched equals the number of Prosper rows in 222-01-WORKLIST.md — no person skipped.
    - 100% of Prosper stances were re-fetched and confirmed before commit; demotions are recorded with a reason.
    - Every value literal in the Prosper block is a whole number 1 through 5, each answers insert is immediately followed by its matching context insert, and every `sources` array has at least one URL.
    - `222-CONFIRMED-BLANK.md` has a `## Prosper` section, and the union of Prosper migration politician_ids and Prosper register bullets equals the Prosper worklist rows with no overlap and no omission.
    - Both repos are committed before Celina research begins; `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>Prosper is fully attempted, self-audited, written into the migration, registered for blanks, and committed in both repos.</done>
</task>

<task type="auto">
  <name>Task 2: Celina, plus the explicit Longview disposition — research one politician at a time, extend the migration, append the register, commit</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the Celina rows and the recorded Longview un-stanced count are this task's exact scope.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B (verbatim topic text, overriding the agent file for housing, taxes, healthcare), Pitfall 2, section C (Celina is Medium tier), and the Longview caveat.
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — methodology and inversion-trap table.
    - `C:/EV-Accounts/backend/migrations/<N>_222_prosper_celina_stances.sql` — the file being extended (created in Task 1, or created here if Prosper sourced nothing).
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format; note that Celina's Shea Scott and Shane Lambert were headshot blanks, which says nothing about stance-evidence availability.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_prosper_celina_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Process the Celina worklist rows in 222-01-WORKLIST.md order, mayor first, using the same research method, the same RESEARCH.md section B topic text, and the same strict D-04 evidence bar as Task 1.

    Celina source order (D-05 acceptable types): the official City of Celina council pages and the searchable agenda and minutes archive; Ballotpedia individual candidate pages; Community Impact Newspaper Collin County coverage and candidate Q&As; VOTE411 and League of Women Voters of Collin County; local news; the officeholder's campaign site. Confirm the source names Celina, Texas (Pitfall 2).

    Longview disposition — do this explicitly, never by omission. Read the Longview un-stanced count recorded in 222-01-WORKLIST.md. If it is zero, append a `## Longview` section to the register stating that the BEFORE snapshot showed zero un-stanced Longview officeholders, so none were attempted, and that Longview is a Gregg County city bundled into the Texas browse list rather than a Collin government. If it is one to three people, research them here under the identical rules and include them in this plan's migration and register. If it is more than three, STOP and flag it to the operator: a full Longview pass needs its own plan, and thinning it to fit this one is not an acceptable outcome.

    Self-audit before extending the SQL: Celina is a D-02 first-pass city and inherits the Tier-1 100% resample rate. Re-fetch every cited source and confirm presence, on-topic fit, and direction against the inversion-trap table for that topic_key. Demote failures to blanks.

    Extend `C:/EV-Accounts/backend/migrations/<N>_222_prosper_celina_stances.sql` with the Celina (and any Longview) blocks inside the same single BEGIN/COMMIT transaction, using the identical answers-plus-context upsert pair, dollar-quoted reasoning, and whole 1-through-5 values. If the file does not exist because Prosper sourced nothing, create it now with the same AUDIT-ONLY header. If neither Prosper nor Celina nor Longview sources anything, do not create the file at all — the register alone carries the outcome.

    Append `## Celina` (and `## Longview`) sections to `222-CONFIRMED-BLANK.md` in the 221 format, with the `Evidence:` line and one bullet per attempted person with no sourced position. Write each heading even when there are zero blanks. Update the running `Count:` line.

    Commit both repos locally. In EV-Accounts use `git -C "C:/EV-Accounts" add` then `git -C "C:/EV-Accounts" commit`; never `cd` into that repo and never `git push` it.
  </action>
  <verify>
    <automated>grep -q "^## Celina" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; grep -q "^## Longview" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - The number of Celina entries researched equals the number of Celina rows in 222-01-WORKLIST.md — no person skipped.
    - The register contains a `## Longview` section stating its disposition explicitly (zero un-stanced, or the people researched here, or a flagged escalation).
    - 100% of Celina stances were re-fetched and confirmed before commit; demotions are recorded with a reason.
    - Every value literal is a whole number 1 through 5, each answers insert is immediately followed by its matching context insert, and every `sources` array has at least one URL.
    - The union of this plan's migration politician_ids and its register bullets equals the Prosper + Celina + Longview worklist rows with no overlap and no omission.
    - `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>Celina is fully attempted and Longview's disposition is explicit; the migration is complete and committed in both repos, nothing applied yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator applies the Prosper/Celina migration and runs the evidence-integrity gate</name>
  <read_first>
    - `C:/EV-Accounts/backend/migrations/<N>_222_prosper_celina_stances.sql` — the SQL to apply.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the evidence-integrity gate query.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the Prosper, Celina, and Longview sections.
  </read_first>
  <what-built>
    A committed, AUDIT-ONLY migration covering Prosper, Celina, and any Longview people, plus their blank-register sections. The executor has no Supabase MCP access, so the SQL is authored and committed but NOT yet applied — the repository looks complete while production is unchanged until this checkpoint runs. This closes D-02's seven named first-pass cities.
  </what-built>
  <action>
    Halt here and hand control to the operator. Do NOT attempt the apply step from this session — gsd-executor has no Supabase MCP binding, so the SQL is committed but production is still unchanged until the operator runs it. Present the how-to-verify steps below verbatim, wait for the resume signal, then record the operator's reported apply confirmation, evidence-integrity gate result, and reconcile result in this plan's SUMMARY before the phase advances. If any gate returns rows, do not mark this plan done.
  </action>
  <how-to-verify>
    1. Apply the migration via `mcp__supabase-local__execute_sql` (this is production — the write is live). Do NOT register it in `schema_migrations`.
    2. Run the evidence-integrity gate from `222-VALIDATION.md` scoped to this plan's politician_ids: it must return zero rows.
    3. Run the exactly-one-bucket reconcile for Prosper, Celina, and Longview against `222-01-WORKLIST.md` and the register sections — never both, never neither.
    4. Confirm the Longview disposition reads as an explicit decision, not an omission.
  </how-to-verify>
  <acceptance_criteria>
    - The migration has been applied to production via `mcp__supabase-local__execute_sql` and the apply is confirmed, not assumed.
    - The evidence-integrity gate returns zero rows for this plan's politician_ids.
    - The Prosper, Celina, and Longview exactly-one-bucket reconciles pass.
    - No row was written to `schema_migrations`.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live and the gate returns zero rows, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped web text → SQL migration | Quotes and URLs harvested from the open web are embedded into a statement executed against production |
| research judgment → public civic profile | An assigned chair becomes a public claim about a real, named person |
| out-of-county city in a county browse list | Longview is a Gregg County city bundled into the Texas list and can fall out of scope silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-04 | Tampering | migration SQL text | high | mitigate | Dollar-quote all scraped reasoning text; never concatenate scraped text into a bare SQL string |
| T-222-05 | Repudiation | fabricated or mismatched source URL | critical | mitigate | 100% re-fetch before commit for both first-pass cities; failing sources demote the stance to a blank |
| T-222-06 | Spoofing | wrong-person attribution (homonym) | high | mitigate | Confirm the Texas city named in the source before accepting evidence |
| T-222-07 | Denial of Service | false-positive completion (authored but unapplied SQL) | high | mitigate | Blocking apply-and-verify checkpoint; the gate runs against production, not the file |
| T-222-08 | Information Disclosure | silent scope loss (Longview) | medium | mitigate | Explicit register section recording Longview's disposition; escalate rather than thin if it exceeds three people |
| T-222-SC | Tampering | package installs | low | accept | This plan installs no packages |
</threat_model>

<verification>
- Every Prosper and Celina worklist name individually attempted, sequentially; Prosper committed before Celina began.
- Longview's disposition recorded explicitly in the register.
- Every applied stance has a whole 1-5 value, a specific dated reasoning, and at least one re-fetched source URL.
- Evidence-integrity gate returns zero rows for this plan's politician_ids after apply.
- Exactly-one-bucket reconcile passes for all three governments.
- EV-Accounts migration committed locally and not pushed.
</verification>

<success_criteria>
D-02's seven named first-pass cities are complete: Prosper and Celina officeholders carry evidence-cited chairs where documented and honest register entries where not, and Longview's status is an explicit recorded decision rather than a silent gap.
</success_criteria>

<output>
Create `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-07-SUMMARY.md` when done — record per-government attempted, sourced, stances applied, blanks, the migration number used, the Longview disposition, and any stance demoted during the self-audit.
</output>
