---
phase: 202-palm-springs-deep-seed
plan: 04
type: execute
# WAVE NOTE (deliberate serialization): wave 4 sits behind wave 3 (headshots) even though both depend
# ONLY on Plan 02 — this is an INTENTIONAL serialization, not a computed dependency inconsistency.
# Stance research must run ONE COUNCILMEMBER AT A TIME (feedback_stance_research_one_at_a_time:
# parallel stance fan-out burns the session quota), so this plan is kept in its own later wave.
wave: 4
depends_on: ["202-02"]
files_modified:
  # 1330-1334 are ILLUSTRATIVE placeholder numbers — verify actual next migration numbers (DB-verified disk MAX+1..) at execute time; the councilmember_N stem is the stable descriptive key
  - C:/EV-Accounts/backend/migrations/1330_palm_springs_councilmember_1_stances.sql
  - C:/EV-Accounts/backend/migrations/1331_palm_springs_councilmember_2_stances.sql
  - C:/EV-Accounts/backend/migrations/1332_palm_springs_councilmember_3_stances.sql
  - C:/EV-Accounts/backend/migrations/1333_palm_springs_councilmember_4_stances.sql
  - C:/EV-Accounts/backend/migrations/1334_palm_springs_councilmember_5_stances.sql
autonomous: true
requirements: [CV-02]
must_haves:
  truths:
    - "Evidence-only compass stances are seeded for the 5 councilmembers against the non-judicial live topics (never the 8 judicial-* topics — a City Council is a non-judicial office)"
    - "Every stance row is 100% cited (non-empty sources array); ZERO neutral-default rows — a topic with no evidence gets NO row at all (honest blank)"
    - "Stances are researched one councilmember at a time (quota); each member's rows carry evidence-based reasoning; discrete 1-5 chairs, not a polarity scale"
    - "The active directly-elected-mayor petition is background context only — NOT forced into an unrelated compass topic"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1330_palm_springs_councilmember_1_stances.sql"
      provides: "Grace Elena Garner (D1) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1334_palm_springs_councilmember_5_stances.sql"
      provides: "David H. Ready (D5, Mayor Pro Tem) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "shared (politician_id, topic_id) — every answer has a matching cited context row"
      pattern: "politician_context"
    - from: "each stance file"
      to: "essentials.politicians (the councilmember)"
      via: "politician_id UUID from the Plan 02 manifest"
      pattern: "politician_id"
---

<objective>
Author 5 audit-only stance migrations (one per councilmember) seeding evidence-only compass stances
for the Palm Springs City Council, researched ONE MEMBER AT A TIME. Every stance is 100% cited, uses
the discrete 1-5 chairs model, and any topic with no evidence gets NO row (honest blank) — never a
neutral default.

Purpose: Satisfies ROADMAP success criterion #4 for CV-02 (evidence-only compass).
Output: 5 files `1330..1334_palm_springs_councilmember_N_stances.sql` (audit-only, unregistered —
verify actual numbers at execute time).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md
@.planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md
@.planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md

<execution_note>
`gsd-executor` has NO DB access. The executor RESEARCHES each councilmember (one at a time — parallel
burns quota) and AUTHORS the 5 stance SQL files. The ORCHESTRATOR applies them via `psql -f` (audit-only,
NOT registered) and asserts 0 uncited answers + 0 judicial topics. Backend repo `C:/EV-Accounts` —
commit via `git -C`.
</execution_note>

<interfaces>
Key contracts (from 202-PATTERNS.md stance block + memory rules):
- Primary analog: `C:/EV-Accounts/backend/migrations/1316_riverside_supervisor_1_stances.sql` — the
  file-header identity/context comment block, evidence-only/honest-blank discipline statement,
  BEGIN;/COMMIT; wrapper, and the two-table INSERT per topic.
- Two-table pattern per topic: INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES (...) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value; then INSERT
  INTO inform.politician_context (politician_id, topic_id, reasoning, sources) VALUES (..., reasoning,
  sources ARRAY::text[]) ON CONFLICT (politician_id, topic_id) DO UPDATE.
- Live compass topics: use the project's live non-judicial topic set (~36 topics; City Council is
  non-judicial — NEVER the 8 judicial-* topics). Reference the live topic IDs already used across
  prior CA city phases (project_compass_live_topic_ids memory note).
- politician_id UUIDs: from 202-02-SUMMARY.md manifest (D1 Garner / D2 Bernstein / D3 deHarte /
  D4 Soto / D5 Ready).
- Value = discrete 1-5 chair, NOT a polarity scale.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research + author stances for councilmembers D1, D2, D3 (one at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1330_palm_springs_councilmember_1_stances.sql, C:/EV-Accounts/backend/migrations/1331_palm_springs_councilmember_2_stances.sql, C:/EV-Accounts/backend/migrations/1332_palm_springs_councilmember_3_stances.sql</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 313-342 — the stance-migration shape + mandatory non-negotiable conventions: one agent at a time, all live topics, honest blanks, one file per member, audit-only)
    - .planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md (Open Question 3 — the directly-elected-mayor debate is background context only, do not force it into a topic)
    - .planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md (the D1/D2/D3 politician UUIDs)
    - C:/EV-Accounts/backend/migrations/1316_riverside_supervisor_1_stances.sql (PRIMARY analog — header comment block + the two-table INSERT-per-topic shape)
  </read_first>
  <action>
    Research ONE councilmember at a time (D1 Grace Elena Garner, then D2 Jeffrey Bernstein, then D3
    Ron deHarte — never parallel; parallel stance fan-out burns the session quota). For each: gather
    documented evidence (council votes, public statements, campaign platforms, local press) across ALL
    live non-judicial compass topics. Author one file per member (1330 Garner / 1331 Bernstein / 1332
    deHarte — verify actual numbers at execute time). Each file: the identity/context header comment
    (name, ext_id, politician_id, party-not-displayed note, district), the evidence-only/honest-blank
    discipline statement, a BEGIN;/COMMIT; wrapper, and for each topic WITH evidence the two-table
    INSERT pair (politician_answers value 1-5 discrete chair + politician_context reasoning +
    non-empty sources ARRAY). A topic with NO documented evidence gets NO row at all — never a
    neutral/default value. Use the politician UUIDs from the Plan 02 manifest. Do NOT touch any
    judicial-* topic (City Council is non-judicial). Do NOT force the active directly-elected-mayor
    petition into an unrelated topic (background context only). These migrations are AUDIT-ONLY — NO
    schema_migrations registration.
  </action>
  <verify>
    <automated>for m in 1 2 3; do F=$(ls C:/EV-Accounts/backend/migrations/*_palm_springs_councilmember_${m}_stances.sql 2>/dev/null | head -1); { test -n "$F" && test -f "$F" && grep -qF "politician_answers" "$F" && grep -qF "politician_context" "$F" && grep -qF "ON CONFLICT (politician_id, topic_id)" "$F" && ! grep -qiE "judicial|schema_migrations" "$F"; } || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist (1330 Garner / 1331 Bernstein / 1332 deHarte, or the actual numbers at execute time).
    - Each has the two-table INSERT pattern (politician_answers + politician_context) with ON CONFLICT (politician_id, topic_id) DO UPDATE.
    - Every politician_answers row has a matching politician_context row with a NON-EMPTY sources array (0 uncited).
    - No judicial-* topic referenced; no schema_migrations registration (audit-only).
    - Zero neutral-default rows — topics with no evidence are omitted entirely.
    - politician_id matches the Plan 02 manifest UUID for each member.
    - Research was done one member at a time (documented in 202-04-SUMMARY.md).
  </acceptance_criteria>
  <done>D1/D2/D3 stance migrations authored: evidence-only, 100% cited, honest blanks, discrete chairs, non-judicial, audit-only; researched one member at a time.</done>
</task>

<task type="auto">
  <name>Task 2: Research + author stances for councilmembers D4 (Mayor) and D5 (Mayor Pro Tem)</name>
  <files>C:/EV-Accounts/backend/migrations/1333_palm_springs_councilmember_4_stances.sql, C:/EV-Accounts/backend/migrations/1334_palm_springs_councilmember_5_stances.sql</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 313-342 — stance shape + mandatory conventions)
    - .planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md (Open Question 3 — mayor-petition context is background only)
    - .planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md (the D4 Soto / D5 Ready politician UUIDs)
    - C:/EV-Accounts/backend/migrations/1316_riverside_supervisor_1_stances.sql (PRIMARY analog — two-table INSERT-per-topic shape)
  </read_first>
  <action>
    Research ONE councilmember at a time (D4 Naomi Soto — the rotational Mayor, then D5 David H. Ready —
    Mayor Pro Tem; never parallel). Same discipline as Task 1: all live non-judicial topics,
    evidence-only, 100% cited, honest blanks, discrete 1-5 chairs. Author one file per member (1333
    Soto / 1334 Ready — verify actual numbers at execute time) using the same header + two-table
    INSERT-per-topic shape and the Plan 02 manifest UUIDs. Attribute stances to each member's OWN
    documented record — do NOT attribute a ceremonial mayoral role's institutional position as a
    personal stance unless there is documented individual evidence. Any topic with no evidence gets no
    row. No judicial-* topics. No schema_migrations registration (audit-only). Do NOT force the
    directly-elected-mayor petition into an unrelated topic even for Soto/Ready (background only).
  </action>
  <verify>
    <automated>for m in 4 5; do F=$(ls C:/EV-Accounts/backend/migrations/*_palm_springs_councilmember_${m}_stances.sql 2>/dev/null | head -1); { test -n "$F" && test -f "$F" && grep -qF "politician_answers" "$F" && grep -qF "politician_context" "$F" && grep -qF "ON CONFLICT (politician_id, topic_id)" "$F" && ! grep -qiE "judicial|schema_migrations" "$F"; } || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 2 files exist (1333 Soto / 1334 Ready, or actual numbers at execute time).
    - Two-table INSERT pattern with ON CONFLICT (politician_id, topic_id) DO UPDATE; 0 uncited answers.
    - No judicial-* topic; no schema_migrations registration.
    - Zero neutral-default rows; topics with no evidence omitted.
    - politician_id matches the Plan 02 manifest for Soto and Ready.
    - Ceremonial mayoral role not attributed as a personal stance without individual evidence.
  </acceptance_criteria>
  <done>D4 Soto (Mayor) + D5 Ready (Mayor Pro Tem) stance migrations authored: evidence-only, 100% cited, honest blanks, non-judicial, audit-only; one member at a time.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the 5 stance migrations + assert 0 uncited / 0 judicial (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator applies the 5 audit-only migrations + read-only assertions</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 391-401 — idempotency convention; lines 331-342 — audit-only / not-registered rule)
    - .planning/phases/202-palm-springs-deep-seed/202-04-SUMMARY.md (the per-member stance counts + citation notes as they are authored)
  </read_first>
  <action>
    ORCHESTRATOR-RUN. Apply the 5 stance migrations via `psql "$DATABASE_URL" -f` in order (audit-only —
    do NOT register in schema_migrations). Then assert via `psql -tAc`: (a) 0 politician_answers rows
    (for the 5 Palm Springs politician_ids) that lack a matching politician_context row OR have an
    empty/NULL sources array (0 uncited); (b) 0 stance rows against any judicial-* topic; (c) each of
    the 5 members has >= 0 rows with all rows cited (record per-member counts); (d) schema_migrations
    MAX unchanged by these 5 files (they are unregistered). Commit the 5 files to `C:/EV-Accounts` via
    `git -C`. Record per-member stance counts + any honest-blank notes into 202-04-SUMMARY.md. If any
    uncited/judicial row is found, PAUSE and fix the offending file before continuing.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM inform.politician_answers a JOIN essentials.politicians p ON p.id=a.politician_id JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022' AND NOT EXISTS (SELECT 1 FROM inform.politician_context c WHERE c.politician_id=a.politician_id AND c.topic_id=a.topic_id AND c.sources IS NOT NULL AND array_length(c.sources,1)>=1))=0"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 stance migrations applied (audit-only); schema_migrations MAX unchanged.
    - psql assertion: 0 uncited answers among the 5 Palm Springs members (every answer has a cited context row).
    - 0 stance rows against any judicial-* topic.
    - Per-member stance counts + honest-blank notes recorded in 202-04-SUMMARY.md.
    - 5 files committed to `C:/EV-Accounts` via `git -C`.
  </acceptance_criteria>
  <done>5 evidence-only stance migrations applied and audited: 0 uncited, 0 judicial, per-member counts recorded, ledger untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external evidence sources → stance SQL | Cited reasoning + values asserted from press/votes cross into DB stance rows |
| stance SQL → production DB | Service-role `psql -f` writes politician_answers/context (audit-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-UNCITED | Repudiation | uncited / defaulted stance | mitigate | Every politician_answers row must have a matching politician_context with a non-empty sources array; Task 3 asserts 0 uncited; honest blanks (no row) never a neutral default |
| T-202-JUDICIAL | Tampering | judicial topic on a non-judicial office | mitigate | Never reference any judicial-* topic (City Council is non-judicial); Task 3 asserts 0 judicial rows |
| T-202-03 | Tampering | non-idempotent re-application | mitigate | ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables; audit-only (unregistered) so ledger untouched |
</threat_model>

<verification>
- 5 stance migrations applied (audit-only); ledger MAX unchanged.
- 0 uncited answers; 0 judicial-topic rows; per-member counts recorded.
- Evidence-only, discrete chairs, honest blanks; researched one member at a time.
</verification>

<success_criteria>
All 5 councilmembers carry an evidence-only, 100%-cited compass with honest blanks.
</success_criteria>

<output>
Create `.planning/phases/202-palm-springs-deep-seed/202-04-SUMMARY.md` when done (per-member stance
counts, honest-blank notes, actual migration numbers).
</output>
