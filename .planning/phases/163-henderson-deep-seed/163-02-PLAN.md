---
phase: 163-henderson-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["163-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-henderson-council-headshots.py
  - C:/EV-Accounts/backend/migrations/1085_henderson_city_council_headshots.sql
  - .planning/phases/163-henderson-deep-seed/163-02-SUMMARY.md
autonomous: true
requirements: [CLARK-03]
user_setup: []

must_haves:
  truths:
    - "Each of the 5 Henderson council members (Mayor Romero + Ward I-IV) has a 600×750 (4:5) headshot mirrored to Supabase Storage politician_photos/{uuid}-headshot.jpg, OR a genuine gap is documented (no fabrication) [D-06]"
    - "Headshots are crop-to-4:5 FIRST then resize to 600×750 Lanczos q90 — never stretch; no text/graphic overlays over the face [D-06]"
    - "cityofhenderson.com is Akamai WAF-403 (all UA blocked) — sourcing goes directly to the per-member fallback chain (campaign sites, Nevada Business Magazine / LV Sun, Ballotpedia, Wikimedia descriptive-UA); cityofhenderson.com is NOT attempted [D-06]"
    - "Migration 1085 inserts politician_images rows (id, politician_id, url, type='default', photo_license) resolved by external_id — AUDIT-ONLY, NOT registered, structural ledger stays at 1084 [D-06]"
    - "politician_images has NO photo_origin_url column; columns are exactly (id, politician_id, url, type, photo_license); photo_license set per member from the actual source [D-06]"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-henderson-council-headshots.py"
      provides: "Headshot pipeline: per-member fallback URL → download → crop 4:5 FIRST → resize 600×750 Lanczos q90 → upload to Storage politician_photos/{uuid}-headshot.jpg; UUID resolved at runtime from external_id via psycopg2; emits a manifest (SUCCESS ext_id→uuid→cdn_url→license, FAILED for gaps)"
      contains: "600"
    - path: "C:/EV-Accounts/backend/migrations/1085_henderson_city_council_headshots.sql"
      provides: "Audit-only headshot migration: one INSERT block per member (external_id -3206001..-3206005), type='default', photo_license from manifest, idempotent NOT EXISTS, NOT registered"
      contains: "essentials.politician_images"
  key_links:
    - from: "5 council members (external_id -3206001..-3206005)"
      to: "essentials.politician_images (url → Storage CDN politician_photos/{uuid}-headshot.jpg)"
      via: "politician_id resolved by external_id; type='default'"
      pattern: "politician_images"
    - from: "_tmp-henderson-council-headshots.py manifest (cdn_url + license per member)"
      to: "migration 1085 INSERT blocks"
      via: "manifest SUCCESS line → migration url + photo_license values"
      pattern: "headshot.jpg"
---

<objective>
Source, process, and install 600×750 headshots for all 5 City of Henderson council members. cityofhenderson.com is Akamai WAF-403 (blocks all User-Agents including Chrome 126 — more restrictive than LV's Azure Blob source), so sourcing goes DIRECTLY to the per-member fallback chain: Romero (Nevada Business Magazine portrait or LV Sun, both confirmed 200 OK), Seebock (votejimseebock.com campaign portrait, confirmed 200 OK), and Larson / Cox / Stewart (campaign sites / Ballotpedia / news media / Wikimedia — sourced at execution time). Crop to 4:5 FIRST then resize to 600×750 Lanczos q90, reject any image with text/graphic overlays over the face, mirror to Storage politician_photos/{uuid}-headshot.jpg, and write the audit-only migration 1085 to insert politician_images rows. Document any genuine gap honestly — no fabrication.

Purpose: Satisfies CLARK-03 SC#2 (all seated officials render with 600×750 headshots; genuine gaps documented).
Output: _tmp-henderson-council-headshots.py + the headshot manifest, 1085_henderson_city_council_headshots.sql (audit-only), recorded results + documented gaps in 163-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and NO DB access):**
- The executor WRITES _tmp-henderson-council-headshots.py and 1085_henderson_city_council_headshots.sql to disk. The executor does NOT run the script, run DB SELECTs, or apply migrations.
- Running the headshot script (`python scripts/_tmp-henderson-council-headshots.py` from C:/EV-Accounts so it reads backend/.env for DATABASE_URL + SUPABASE_SERVICE_ROLE_KEY), applying mig 1085, and every verification SELECT/curl are INLINE ORCHESTRATOR steps.
- Per-member fallback URL sourcing (Larson/Cox/Stewart) is an inline-orchestrator research step at execution time (browse the campaign/news/Ballotpedia pages, confirm a portrait-style image with no overlay, capture the URL into the OFFICIALS roster).
- The script + migration live in the SEPARATE repo C:/EV-Accounts/backend/ (commit via `git -C "C:/EV-Accounts"`; the _tmp-* script is gitignored). SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/163-henderson-deep-seed/163-RESEARCH.md
@.planning/phases/163-henderson-deep-seed/163-PATTERNS.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
The 5 external_id→UUID map is in 163-01-SUMMARY.md (minted by mig 1084). UUID resolved at runtime from external_id via psycopg2 — never hardcode a UUID in the script.
Bucket: politician_photos (NOT politician-headshots). Storage path: {politician_uuid}-headshot.jpg. CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
politician_images columns: (id, politician_id, url, type, photo_license) — NO photo_origin_url (removed column). type='default' on all headshot rows.
Pipeline invariants: crop to 4:5 ratio FIRST, THEN resize 600×750 Lanczos q90, optimize=True strips EXIF. Never stretch/resize before cropping (memory feedback_headshot_resize_no_distort).
WAF: cityofhenderson.com = Akamai WAF-403, blocks ALL UA including Chrome 126. Do NOT attempt curl from cityofhenderson.com — go directly to fallbacks. (Pitfall 3.)
Roster + confirmed/pending sources:
  -3206001 Michelle Romero (Mayor): Nevada Business Magazine portrait https://nevadabusiness.com/wp-content/uploads/2025/04/MR-2023-Headshot-SR-scaled.jpeg (200 OK, try first — portrait); fallback LV Sun media library 1200x600 landscape (200 OK; needs crop). license='press_use' (campaign/editorial; NOT us_government_work).
  -3206002 Jim Seebock (Ward I): https://votejimseebock.com/wp-content/themes/jim-seebock/images/jim_headshot_header.png (200 OK). license='press_use'.
  -3206003 Monica Larson (Ward II): votedrmonicalarson.com/about/ or news source — INLINE-VERIFY at execution. license='press_use'.
  -3206004 Carrie Cox (Ward III): checktheboxforcarriecox.com / Ballotpedia / Nevada Independent — INLINE-VERIFY at execution. license='press_use'.
  -3206005 Dan H. Stewart (Ward IV): danstewartnv.com/about/ / Ballotpedia — INLINE-VERIFY at execution. license='press_use'.
photo_license guidance: campaign-site = press_use (unless verified otherwise); LV Sun = editorial (NOT us_government_work); official city portrait (if ever accessible) = us_government_work. Reject any image with campaign banners / "Re-Elect" text / graphic overlays over the face (memory feedback_headshot_no_graphics).
Gap handling: if no clean source for a member, emit a FAILED manifest line and document the gap in SUMMARY — do NOT fabricate or insert a placeholder image.
Migration 1085 is AUDIT-ONLY: NOT registered; structural ledger stays at 1084 (or Wave-0 number). If Wave-0 renumbered the structural migration, shift 1085 by the same offset.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the headshot pipeline script + source per-member URLs + run it</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-henderson-council-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-lv-city-council-headshots.py (EXACT ANALOG — all functions copy verbatim; only the docstring, OFFICIALS roster, guard assertions, and manifest label change)
    - .planning/phases/163-henderson-deep-seed/163-PATTERNS.md (section "scripts/_tmp-henderson-council-headshots.py" — the OFFICIALS config block, env-load block, the list of functions to copy verbatim, the manifest label)
    - .planning/phases/163-henderson-deep-seed/163-RESEARCH.md (§Headshot Sources — per-member fallback table + photo_license guidance; Pitfall 3 Akamai WAF; Open Questions 1-2 Larson/Cox/Stewart + Romero source preference)
    - .planning/phases/163-henderson-deep-seed/163-01-SUMMARY.md (the 5 external_id→UUID map minted by mig 1084 + the confirmed structural migration number)
  </read_first>
  <action>
INLINE ORCHESTRATOR SOURCING STEP (at execution): confirm/finalize per-member fallback URLs. Romero — try the Nevada Business Magazine portrait URL first (portrait, cleaner 4:5 crop); fall back to the LV Sun media-library landscape if the crop is poor. Seebock — votejimseebock.com campaign portrait (confirmed 200 OK). Larson / Cox / Stewart — browse the campaign/news/Ballotpedia pages, confirm a portrait-style image (head + shoulders) with NO text/graphic overlay over the face, and capture each URL. Do NOT attempt cityofhenderson.com (Akamai WAF-403). For any member with no clean source, leave the URL as a sentinel so the script emits a FAILED line.

EXECUTOR STEP: write `_tmp-henderson-council-headshots.py` by copying `_tmp-lv-city-council-headshots.py` verbatim for all functions — resolve_politician_id, download_image, crop_to_4_5, resize_600x750, upload_to_storage, process_headshot_bytes, process_member — plus the env-load + DB-connect block (reads C:/EV-Accounts/backend/.env: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL; BUCKET='politician_photos'; CDN_BASE as in interfaces; TARGET_SIZE=(600,750); JPEG_QUALITY=90; RESAMPLE=LANCZOS). Replace the config block with the 5-member OFFICIALS roster (ext_id, name, url, per-member license) from the interfaces section, with the per-member sourced URLs filled in. Add the guard assertions: len(OFFICIALS)==5, unique ext_ids, all ext_ids in -3206005..-3206001. Adapt process_member to use member['license'] (per-member, not a single DEFAULT_LICENSE — sources differ). Add a docstring noting the Akamai WAF on cityofhenderson.com and that sourcing uses fallbacks. Adapt the manifest label to "=== HENDERSON CITY COUNCIL HEADSHOT MANIFEST ===" and "{successes}/5 uploaded, {gaps} gaps", with each SUCCESS line carrying ext_id → uuid → cdn_url → license (consumed by migration 1085).

INLINE ORCHESTRATOR STEP: run `python scripts/_tmp-henderson-council-headshots.py` from C:/EV-Accounts. Confirm it processes 5 members, crops 4:5 → 600×750, uploads to Storage, and prints the manifest. Record the manifest (SUCCESS + any FAILED lines) in the SUMMARY.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-henderson-council-headshots.py"; test -f "$F" &amp;&amp; grep -q "600" "$F" &amp;&amp; grep -q "750" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; grep -q -- "-3206001" "$F" &amp;&amp; grep -q -- "-3206005" "$F" &amp;&amp; grep -q "external_id" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-henderson-council-headshots.py exists; all pipeline functions copied verbatim from the LV script; crop-4:5-FIRST then resize-600×750 preserved.
    - OFFICIALS roster has 5 members (-3206001..-3206005) with per-member url + license; guard assertions present (count 5, unique, in range).
    - cityofhenderson.com is NOT used as a source; sourcing uses the fallback chain.
    - UUID resolved at runtime from external_id (psycopg2) — no hardcoded UUID.
    - INLINE: script run; manifest recorded in SUMMARY (SUCCESS ext_id→uuid→cdn_url→license; FAILED lines for any gap).
    - Any image with a text/graphic overlay over the face is rejected; gaps documented, never fabricated.
  </acceptance_criteria>
  <done>Headshot script written + run; per-member URLs sourced (no cityofhenderson.com); manifest recorded with successes + any documented gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Write audit-only migration 1085 + apply inline + verify</name>
  <files>C:/EV-Accounts/backend/migrations/1085_henderson_city_council_headshots.sql, .planning/phases/163-henderson-deep-seed/163-02-SUMMARY.md</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1076_las_vegas_city_council_headshots.sql (EXACT ANALOG — one INSERT block per member, type='default', NOT EXISTS idempotency, audit-only / unregistered)
    - .planning/phases/163-henderson-deep-seed/163-PATTERNS.md (section "migrations/1085_henderson_city_council_headshots.sql" — the per-member INSERT block + CDN URL format + the forbidden-token paraphrase rules)
  </read_first>
  <action>
EXECUTOR STEP: write `1085_henderson_city_council_headshots.sql` modeled on 1076. Header comment: file name, Phase 163 (CLARK-03), AUDIT-ONLY (NOT registered, structural ledger stays at 1084 / Wave-0 number), columns exactly (id, politician_id, url, type, photo_license), idempotent via NOT EXISTS, type='default'. Paraphrase the removed image-origin column note ("the removed image-origin column is intentionally absent") — do NOT write the literal removed-column token. BEGIN; then one INSERT block per member (external_id -3206001..-3206005, only the members with a SUCCESS manifest line): `INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) SELECT gen_random_uuid(), (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>), '<cdn_url_from_manifest>', 'default', '<license_from_manifest>' WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>))`. CDN URL format: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg. photo_license per member from the manifest (likely 'press_use'). COMMIT. NO ledger-registration line (audit-only). For any FAILED member, OMIT the block and document the gap in SUMMARY (no placeholder row).

INLINE ORCHESTRATOR STEP: apply 1085 via `psql -f` (DATABASE_URL from C:/EV-Accounts/backend/.env) or mcp__supabase-local__execute_sql. Then verify + record in SUMMARY:
- `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -3206005 AND -3206001` → target 5 minus documented gaps; list gaps.
- `curl -sI` each CDN url (the SUCCESS members) → 200.
- `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '1085'` → expect 0 rows (audit-only, ledger stays at 1084).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1085_henderson_city_council_headshots.sql"; S=".planning/phases/163-henderson-deep-seed/163-02-SUMMARY.md"; test -f "$F" &amp;&amp; grep -q "essentials.politician_images" "$F" &amp;&amp; grep -q "'default'" "$F" &amp;&amp; grep -q "NOT EXISTS" "$F" &amp;&amp; grep -vq "^[[:space:]]*--.*photo_origin_url" "$F" &amp;&amp; test -f "$S" &amp;&amp; grep -qi "headshot" "$S" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1085_henderson_city_council_headshots.sql exists; one INSERT block per SUCCESS member; type='default'; NOT EXISTS idempotency; photo_license per member from manifest.
    - CDN URL format matches politician_photos/{uuid}-headshot.jpg.
    - No ledger-registration line (audit-only); no forbidden tokens in comments (image-origin column paraphrased).
    - INLINE: applied; politician_images count recorded (target 5 minus gaps); CDN urls return 200; gaps listed in SUMMARY.
    - Ledger check recorded: 1085 NOT registered (structural ledger stays at 1084).
  </acceptance_criteria>
  <done>Migration 1085 written + applied; headshot rows present for sourced members (CDN-200); gaps documented; ledger stays at 1084; results recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external image source → headshot pipeline | fetched image (campaign/news/Wikimedia) crosses into Storage + DB; validate it is a real portrait, no overlay, correct person |
| manifest → migration 1085 | manifest cdn_url/license cross into seed SQL; only SUCCESS lines become INSERT blocks |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-06 | Tampering | wrong-person or overlay-laden headshot inserted | mitigate | Per-member source confirmed at execution (portrait, head+shoulders); reject text/graphic overlays; Plan 03 human-verify confirms correct-person |
| T-163-07 | Tampering | fabricated/placeholder image for a member with no source | mitigate | No fabrication rule; FAILED manifest line + documented gap instead of a placeholder row |
| T-163-08 | Spoofing | Akamai WAF returns a 403 error body saved as an image | mitigate | cityofhenderson.com skipped entirely; download_image validates content-type/decodes via PIL before processing |
| T-163-09 | Information disclosure | mis-set photo_license (editorial source labeled us_government_work) | mitigate | photo_license set per member from the actual source (campaign/news = press_use, not us_government_work) |
| T-163-SC | Tampering | npm/pip/cargo installs | accept | No new packages (Pillow/psycopg2/requests pre-installed; RESEARCH audit = N/A) |
</threat_model>

<verification>
CLARK-03 SC#2: all 5 seated officials render with 600×750 headshots, genuine gaps documented.
- Pipeline crops 4:5 first then 600×750; mirrored to politician_photos/{uuid}-headshot.jpg (Task 1).
- politician_images rows present (target 5 minus documented gaps); CDN urls 200 (Task 2).
- Migration 1085 audit-only — structural ledger stays at 1084.
</verification>

<success_criteria>
- _tmp-henderson-council-headshots.py written + run; per-member fallback URLs sourced (cityofhenderson.com skipped); 600×750 4:5 crops mirrored to Storage; manifest recorded.
- 1085_henderson_city_council_headshots.sql written + applied (audit-only); politician_images rows for all sourced members; CDN-200; any gap documented (no fabrication).
- Ledger stays at 1084; results + gaps recorded in 163-02-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/163-henderson-deep-seed/163-02-SUMMARY.md` when done (record the per-member manifest, the politician_images count + CDN-200 results, any documented gaps, photo_license per member, and confirmation the ledger stayed at 1084).
</output>
