---
phase: 187-tethered-feature-icon-row
plan: 02
type: execute
wave: 2
depends_on: ["187-01"]
files_modified:
  - src/components/SectionBanner.jsx
  - src/pages/Results.jsx
  - src/components/ElectionsView.jsx
autonomous: false
requirements: [ICON-01, ICON-02, ICON-03, TETH-01, TETH-02]

must_haves:
  truths:
    - "A banner whose location has Treasury data shows a Treasury chip bottom-right"
    - "Hovering OR keyboard-focusing the chip shows a tooltip naming 'Treasury Tracker'; the link has an aria-label"
    - "Clicking/activating the chip opens financials.empowered.vote/?entity=<banner-location> for the banner's OWN location, not the user's"
    - "A banner with no resolved Treasury entity shows no chip at all (no greyed/disabled icon)"
    - "The chip row never overlaps the bottom-left location title; population top-right slot stays free for Phase 188"
  artifacts:
    - path: "src/components/SectionBanner.jsx"
      provides: "Bottom-right circular-chip icon row with accessible hover+focus tooltip (fills the inert featureIcons slot)"
      contains: "featureIcons"
    - path: "src/pages/Results.jsx"
      provides: "featureIconMap useMemo (resolveFeatureIcons) + featureIcons prop on 3 banners + featureIconMap prop to ElectionsView + TREASURY_URL imported from treasury.js"
      contains: "resolveFeatureIcons"
    - path: "src/components/ElectionsView.jsx"
      provides: "featureIconMap prop drilled to its own 3 SectionBanner calls"
      contains: "featureIconMap"
  key_links:
    - from: "src/pages/Results.jsx"
      to: "src/lib/featureIcons.js"
      via: "resolveFeatureIcons in a useMemo keyed on representingCity/userState/treasuryCities"
      pattern: "resolveFeatureIcons"
    - from: "src/pages/Results.jsx"
      to: "src/components/SectionBanner.jsx"
      via: "featureIcons={featureIconMap.Local|State|Federal}"
      pattern: "featureIcons=\\{featureIconMap"
    - from: "src/components/ElectionsView.jsx"
      to: "src/components/SectionBanner.jsx"
      via: "featureIcons={featureIconMap?.Local|State|Federal} drilled to ElectionsView's 3 banner calls (cross-page parity)"
      pattern: "featureIcons=\\{featureIconMap"
    - from: "src/components/SectionBanner.jsx"
      to: "financials.empowered.vote"
      via: "chip <a href={icon.href} target=_blank rel=noopener noreferrer>"
      pattern: "rel=\"noopener noreferrer\""
---

<objective>
Fill `SectionBanner.jsx`'s inert `featureIcons` slot with a real bottom-right row of circular
semi-transparent chips, each an accessible external deep-link with a hover+keyboard-focus tooltip,
and wire the resolved `featureIconMap` from `Results.jsx` down through `ElectionsView.jsx` to all
six `<SectionBanner>` call sites — mirroring the existing `buildingImageMap` flow exactly.

Purpose: Deliver the visible, tethered Treasury icon end-to-end (D-04): a working deep-link that
carries the banner's OWN location (TETH-01), never the user's; legible on any banner art via the
chip treatment (ICON-03); with an accessible tooltip (ICON-02).

Output: extended `SectionBanner.jsx`, `Results.jsx`, `ElectionsView.jsx`.

## Locked decisions honored
- D-05: bottom-right circular semi-transparent chips (one per icon); chip wrapper guarantees
  legibility because `treasury-symbol.svg` ships no dark variant (RESEARCH Pitfall 3).
- D-06/ICON-03: title stays bottom-left; chip row bottom-right, no overlap.
- D-07: leave the top-right slot free — do NOT build the population stat (Phase 188), just do not
  place chips or tooltips in a way that blocks the top-right corner.
- D-08/ICON-02: minimal accessible custom tooltip on hover AND keyboard focus + `aria-label` on the
  link; native `title=` is rejected. Reimplement the `IconWithTooltip` @floating-ui pattern locally
  in SectionBanner (extraction to a shared component deferred — research Open Question 3).
- Architecture (Claude's Discretion): parent-resolved + prop-drilled `featureIconMap` (buildingImageMap
  precedent) — resolution already done in plan 187-01; this plan only threads + renders it. Chosen for
  Phase 189 (one shared component, no page-specific divergence) and to avoid N redundant fetches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/187-tethered-feature-icon-row/187-CONTEXT.md
@.planning/phases/187-tethered-feature-icon-row/187-RESEARCH.md
@.planning/phases/187-tethered-feature-icon-row/187-PATTERNS.md
@.planning/phases/187-tethered-feature-icon-row/187-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by plan 187-01 (already implemented) — consume directly. -->

From src/lib/featureIcons.js:
```javascript
export function resolveFeatureIcons({ representingCity, userState, treasuryCities })
// -> { Local: [{key,href,label,iconSrc}], State: [...], Federal: [...] }  (arrays; [] when nothing resolves)
export const PRODUCT_REGISTRY  // reserved order [treasury, ...]; treasury.iconSrc === '/treasury-symbol.svg'
```
From src/lib/treasury.js:
```javascript
export const TREASURY_URL  // 'https://financials.empowered.vote' (import this; remove the local const at Results.jsx:34)
```

Existing SectionBanner signature + inert slot (src/components/SectionBanner.jsx):
```javascript
export default function SectionBanner({ tier, locationName, imageUrl, stats, featureIcons }) { ... }
// title block: position:absolute; bottom:0; left:0; right:0; paddingBottom:16px  (lines 94-98)
// inert slot to replace (line 115): {featureIcons && <div className="sr-only" data-slot="feature-icons" />}
```

Tooltip pattern to reimplement locally (src/components/IconOverlay.jsx:22-81, @floating-ui/react):
```javascript
import { useFloating, useHover, useFocus, useDismiss, useRole, useInteractions,
         FloatingPortal, offset, flip, shift, autoUpdate } from '@floating-ui/react';
// useHover + useFocus + useDismiss + useRole('tooltip') + FloatingPortal; aria-label on the reference el
```

buildingImageMap precedent (the exact flow to mirror):
```javascript
// Results.jsx:1132  const buildingImageMap = useMemo(() => getBuildingImages(representingCity, userState), [...]);
// Results.jsx:1941/1947/1953  imageUrl={buildingImageMap.Local|State|Federal}
// Results.jsx:2066  <ElectionsView buildingImageMap={buildingImageMap} ... />
// ElectionsView.jsx:285  buildingImageMap = {},   (destructured prop, default {})
// ElectionsView.jsx:580/586/592  imageUrl={buildingImageMap?.Local|State|Federal}
// Results.jsx:531-532  treasuryCities already fetched via fetchTreasuryCities() — REUSE, do not add a 2nd fetch
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the featureIcons chip row + accessible tooltip in SectionBanner.jsx</name>
  <files>src/components/SectionBanner.jsx</files>
  <read_first>
    - src/components/SectionBanner.jsx (FULL — the file being modified; note the title block position:absolute bottom:0 left:0 at lines 94-98, the inert featureIcons slot at line 115, the imageFailed/showImage graceful-degradation idiom at lines 46-53, and the @theme-token color convention header lines 12-14)
    - src/components/IconOverlay.jsx (§ IconWithTooltip lines 22-81 — the exact @floating-ui hover+focus+dismiss+role tooltip pattern to reimplement locally)
    - .planning/phases/187-tethered-feature-icon-row/187-PATTERNS.md § "src/components/SectionBanner.jsx" (chip placement, tooltip adaptation to wrap an <a> not a <span>, token vs raw-hex guidance)
    - .planning/phases/187-tethered-feature-icon-row/187-RESEARCH.md § Pitfall 3 (chip wrapper rationale — no dark variant) + Pitfall 4 (root-relative asset path, no JS import)
  </read_first>
  <action>
    Replace the inert `{featureIcons && <div className="sr-only" data-slot="feature-icons" />}` line
    with a real render. Add a local private component (e.g. `FeatureIconChip`) reimplementing the
    `IconWithTooltip` @floating-ui pattern from IconOverlay.jsx, but wrapping an
    `<a href={icon.href} target="_blank" rel="noopener noreferrer" aria-label={icon.label}>` (a real
    link, not a bare span — D-08 requires the aria-label on the link) as the reference element; use
    `useHover` + `useFocus` + `useDismiss` + `useRole(context,{role:'tooltip'})` so the tooltip shows
    on both hover AND keyboard focus, rendered through `FloatingPortal` (so it is never clipped by the
    banner's overflow). Tooltip content is `icon.label` ("Treasury Tracker"). Inside the link, render
    the SVG via `<img src={icon.iconSrc} alt="" />` inside a circular semi-transparent chip `<div>`
    (D-05) — the chip is the background layer that guarantees legibility since treasury-symbol.svg has
    no dark variant. Render the chip row as an absolutely-positioned container at the banner's
    BOTTOM-RIGHT (mirror the title block's `position:absolute` idiom: `bottom:16px; right:16px`),
    laying chips out left-to-right in array order. Guard with `{featureIcons?.length > 0 && (...)}` so
    an empty/absent array renders nothing (TETH-03 / graceful degradation, matching the file's existing
    ternary-fallback style). Do NOT place anything in the TOP-RIGHT corner (reserved for Phase 188
    population stat — D-07). Trace chip/tooltip colors to `src/index.css` @theme tokens where one
    exists (DARK-01), no `!important`; fall back to raw hex only if no token covers it (as IconOverlay
    does). Keep the existing `stats` inert slot untouched.
  </action>
  <acceptance_criteria>
    - SectionBanner renders a bottom-right chip row for each entry in `featureIcons`; `featureIcons=[]`/undefined renders zero chips (grep: guarded by `?.length > 0`).
    - Each chip is an `<a target="_blank" rel="noopener noreferrer" aria-label=...>` wrapping `<img src={icon.iconSrc}>` inside a circular semi-transparent chip div.
    - Tooltip uses @floating-ui useHover+useFocus (shows on hover AND focus), role="tooltip", FloatingPortal; no native `title=` attribute on the link.
    - No JS import of any cross-repo SVG path; icon referenced by root-relative `icon.iconSrc`.
    - `npm run build` succeeds (compiles; asset path resolves).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <automated>MISSING — no jsdom/React-render harness in this repo (SectionBanner.test.js documents "no jsdom, no React render"); chip/tooltip behavior verified in the Task 3 human checkpoint</automated>
    <human-check>Hover and Tab-focus a chip both surface the "Treasury Tracker" tooltip; chip sits bottom-right and does not overlap the bottom-left title.</human-check>
  </verify>
  <done>Chip row + accessible hover/focus tooltip render from the featureIcons prop; empty array renders nothing; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve featureIconMap in Results.jsx and drill it through ElectionsView to all six banners</name>
  <files>src/pages/Results.jsx, src/components/ElectionsView.jsx</files>
  <read_first>
    - src/pages/Results.jsx (lines 30-40 for the imports + local TREASURY_URL const at line 34; lines 525-535 for the existing treasuryCities fetch; lines 1125-1140 for the buildingImageMap useMemo precedent; lines 1937-1955 for the 3 direct <SectionBanner> calls; lines 1965-1996 for the existing per-body Treasury text link; line ~2066 for the <ElectionsView> call)
    - src/components/ElectionsView.jsx (lines 277-289 for the destructured prop list incl. buildingImageMap={} default; lines 576-594 for its own 3 <SectionBanner> calls)
    - .planning/phases/187-tethered-feature-icon-row/187-PATTERNS.md § "src/pages/Results.jsx" and "src/components/ElectionsView.jsx" (exact precedent lines + optional-chaining convention difference between the two files)
  </read_first>
  <action>
    In `Results.jsx`: import `resolveFeatureIcons` from `../lib/featureIcons` and import `TREASURY_URL`
    from `../lib/treasury` (add to the existing treasury import at line 32); DELETE the local
    `const TREASURY_URL = ...` at line 34 so the existing per-body text link (line 1984) now uses the
    centralized `financials.empowered.vote` constant (this is what makes TETH-02 consistent across both
    links). Add a `featureIconMap` `useMemo(() => resolveFeatureIcons({ representingCity, userState,
    treasuryCities }), [representingCity, userState, treasuryCities])` next to the existing
    `buildingImageMap` useMemo — REUSE the already-fetched `treasuryCities` state (lines 531-532), do
    NOT add a second fetch. Add `featureIcons={featureIconMap.Local}` / `.State` / `.Federal` to the
    three direct `<SectionBanner>` calls (lines 1941/1947/1953), exactly mirroring how
    `imageUrl={buildingImageMap.X}` is already threaded. Add `featureIconMap={featureIconMap}` to the
    `<ElectionsView ...>` call (next to the existing `buildingImageMap={buildingImageMap}` at ~line 2066).
    Do NOT modify the existing per-body `findMatchingMunicipality(body.title, treasuryCities, bodyState)`
    text-link logic other than the domain change inherited from the shared constant.
    In `ElectionsView.jsx`: add `featureIconMap = {}` to the destructured prop list (same default-empty-
    object idiom as `buildingImageMap = {}` at line 285). Add `featureIcons={featureIconMap?.Local}` /
    `?.State` / `?.Federal` to its three `<SectionBanner>` calls (lines 580/586/592), following this
    file's optional-chaining convention.
  </action>
  <acceptance_criteria>
    - Results.jsx: single `featureIconMap` useMemo keyed on `[representingCity, userState, treasuryCities]`; no second fetchTreasuryCities call added.
    - Results.jsx local `TREASURY_URL` const removed; `TREASURY_URL` now imported from `../lib/treasury`; the existing text link at ~1984 resolves to `financials.empowered.vote`.
    - All three Results.jsx direct banners AND all three ElectionsView banners receive a `featureIcons`/`featureIcons=featureIconMap?.X` prop; `featureIconMap` passed to `<ElectionsView>`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <automated>npx vitest run src/lib/treasury.test.js src/lib/featureIcons.test.js</automated>
    <human-check>On /results and the Elections tab for the same location, city/state/federal banners show the identical Treasury chip where data exists.</human-check>
  </verify>
  <done>featureIconMap resolved once and drilled to all six banners; single centralized financials.empowered.vote domain; build + logic suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual + tether verification (no render harness — required manual gate)</name>
  <files>src/components/SectionBanner.jsx, src/pages/Results.jsx, src/components/ElectionsView.jsx</files>
  <action>
    Manual verification gate — no code changes. A human executes the how-to-verify steps below against
    a running dev server and confirms every behavior. This gate is required because the repo has no
    jsdom/React-render harness, so ICON-01/02/03 + TETH-01 cannot be asserted by an automated test.
    Blocking and non-auto-approvable — a human must confirm before the phase advances.
  </action>
  <what-built>
    A bottom-right circular-chip Treasury icon row on city/state/federal section banners with an
    accessible hover+keyboard-focus tooltip, deep-linking each banner's OWN location into
    financials.empowered.vote. Tasks 1-2 automated everything possible; this repo has no
    jsdom/component-render harness, so the interaction, placement, cross-location tether, and
    absence-when-no-data behaviors must be human-verified (roadmap: "UI hint: yes").
  </what-built>
  <how-to-verify>
    1. Run the app (`npm run dev`). Load `/results` for a location known to have Treasury data (e.g. a
       Texas city like Plano) — confirm a single Treasury chip appears BOTTOM-RIGHT on the city banner,
       legible against the banner art, and does NOT overlap the bottom-left title. (ICON-01, ICON-03, D-05/D-06)
    2. Hover the chip -> "Treasury Tracker" tooltip appears. Press Tab to move keyboard focus onto the
       chip -> the SAME tooltip appears on focus (not just hover). Inspect the DOM: the `<a>` has an
       `aria-label`, and there is no native `title=` attribute. (ICON-02 / D-08)
    3. TETHER TEST (TETH-01): set your saved/current user location to City A, then view a banner for a
       DIFFERENT City B; click the Treasury chip and confirm the opened URL is
       `financials.empowered.vote/?entity=<City-B-slug>` — carrying the BANNER's location, not City A.
       Repeat for a state-tier banner (e.g. Texas -> `?entity=texas-tx`) and, if present, the federal
       banner (`?entity=united-states-us`). (TETH-01, TETH-02, TETH-04)
    4. Confirm the existing "Explore {city} revenue and expenses" text link opens the SAME
       financials.empowered.vote domain as the chip (domain consistency).
    5. ABSENCE TEST (TETH-03): view a banner for a location with no Treasury dataset — confirm there is
       ZERO Treasury chip (no greyed/disabled/placeholder icon, no dead link).
    6. Confirm the top-right corner of every banner remains empty (reserved for Phase 188 — D-07) and
       no console errors appear on any banner.
    7. Cross-page parity spot check: the Elections tab shows the identical chip behavior as /results for
       the same location.
  </how-to-verify>
  <verify>
    <human-check>All 7 how-to-verify steps confirmed by a human against a running dev server; no console errors.</human-check>
  </verify>
  <done>Human confirms placement, hover+focus tooltip, cross-location tether, absence-when-no-data, empty top-right, and cross-page parity.</done>
  <resume-signal>Type "approved" or describe issues (wrong corner, tooltip missing on focus, wrong location in URL, chip shown when no data, console errors).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Essentials app -> external Treasury SPA | new `<a target="_blank">` opens a separately-deployed origin |
| banner location vs user location | banner must tether its OWN location, never the ev-context/saved user location |
| location strings -> outbound URL | resolved slug is interpolated into the `?entity=` query value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-02 | Spoofing | SectionBanner chip `<a>` / Results featureIconMap wiring | mitigate | Icon href is sourced exclusively from `featureIconMap` which resolveFeatureIcons built from `representingCity`/`userState` (the banner's own location) — no ev-context/broker/saved-location value ever reaches the link (TETH-01 core contract) |
| T-187-03 | Tampering (reverse tabnabbing) | external `<a target="_blank">` in SectionBanner | mitigate | Every chip link uses `rel="noopener noreferrer"`, mirroring the existing per-body Treasury link (Results.jsx:1985-1986) |
| T-187-01 | Tampering | inherited href/slug from plan 187-01 | mitigate | Slug already `encodeURIComponent`-wrapped + `toTreasurySlug`-sanitized in featureIcons.js (187-01); this plan passes it through unmodified |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH.md Package Legitimacy Audit: N/A — reuses installed `@floating-ui/react`); no supply-chain surface |
</threat_model>

<verification>
- `npm run build` succeeds after both code tasks.
- `npx vitest run src/lib/treasury.test.js src/lib/featureIcons.test.js` still green (no regression).
- Manual checkpoint (Task 3) approved: placement, hover+focus tooltip, cross-location tether,
  absence-when-no-data, empty top-right, cross-page parity, no console errors.
</verification>

<success_criteria>
- Treasury chip renders bottom-right with an accessible hover+focus tooltip (ICON-01/02/03).
- Chip deep-links the banner's OWN location to financials.empowered.vote (TETH-01/02).
- No chip when no Treasury entity exists (TETH-03); top-right reserved for Phase 188 (D-07).
- Identical behavior across Results and Elections banners (sets up Phase 189 consolidation).
</success_criteria>

<output>
Create `.planning/phases/187-tethered-feature-icon-row/187-02-SUMMARY.md` when done.
</output>
