---
phase: 205-u-s-senate-2026-candidate-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/878_link_us_senate_2026_races.sql
autonomous: false
requirements: [REQ-1, REQ-4]
must_haves:
  truths:
    - "The idempotent migration file 878 exists containing the explicit 35-row state->office_id VALUES map"
    - "Every state's Senate race rows map to a real NATIONAL_UPPER seat office (title Senator or U.S. Senate - {State}), never a Candidate for U.S. Senate stray office"
    - "The two 2026 special-election seats (OH->Husted, FL->Moody) are present and flagged SPECIAL in the review table"
    - "The full derived seat map is presented at a blocking human checkpoint and explicitly approved before any UPDATE is authorized to run"
    - "A skip report is produced listing any state that could not be confidently mapped (research found 0)"
  artifacts:
    - path: "supabase/migrations/878_link_us_senate_2026_races.sql"
      provides: "Idempotent UPDATE ... FROM (VALUES ...) linking U.S. Senate 2026 races to correct NATIONAL_UPPER seat offices"
      contains: "UPDATE essentials.races"
      min_lines: 45
  key_links:
    - from: "supabase/migrations/878_link_us_senate_2026_races.sql"
      to: "essentials.races.office_id"
      via: "UPDATE ... SET office_id = v.office_id WHERE r.position_name = v.position_name AND r.office_id IS NULL"
      pattern: "office_id IS NULL"
---

<objective>
Author the checked-in, idempotent, re-runnable production data-repair migration
`supabase/migrations/878_link_us_senate_2026_races.sql` from the VERIFIED 35-row
state->office_id seat map in RESEARCH.md, then present that full map at a BLOCKING
human checkpoint and obtain explicit approval before any write is applied.

Purpose: The migration file IS the auditable, reviewed artifact (per D-03). No `UPDATE`
runs against production until a human eyeballs the 35-row state->seat(->incumbent) table
— with the two SPECIAL seats (OH->Husted, FL->Moody) flagged distinctly — and approves it
(D-01, D-04, D-05). This plan produces and gates the artifact; Plan 205-02 applies it.
Output: One new migration file + a written skip report (expected empty) + recorded human approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-u-s-senate-2026-candidate-wiring/205-SPEC.md
@.planning/phases/205-u-s-senate-2026-candidate-wiring/205-CONTEXT.md
@.planning/phases/205-u-s-senate-2026-candidate-wiring/205-RESEARCH.md

<interfaces>
<!-- The complete, VERIFIED 35-row state->office_id map lives in RESEARCH.md
     `## The Derived 2026 Seat Map`. The executor MUST copy the office_id UUIDs
     from that table verbatim into the VALUES list — do NOT re-derive them.

     Join key (RESEARCH `## DB Facts Verified`): position_name uses the FULL
     state name, no abbreviation, no dash: 'U.S. Senate Minnesota' (not 'U.S.
     Senate - MN'). The dashed-title states in this set are Maine, Massachusetts,
     Oregon, Texas (title 'U.S. Senate - {State}'); the other 31 use title 'Senator'.
     Both are valid seat titles — never 'Candidate for U.S. Senate — {State}'.

     Idempotency shape (RESEARCH `## Migration Mechanics`, analog 203/204 style):
       UPDATE essentials.races AS r
       SET office_id = v.office_id::uuid
       FROM (VALUES (position_name, office_id), ...) AS v(position_name, office_id)
       WHERE r.position_name = v.position_name
         AND r.office_id IS NULL;
     The `AND r.office_id IS NULL` guard makes a re-run a safe no-op. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 878 from the verified seat map + skip report</name>
  <files>supabase/migrations/878_link_us_senate_2026_races.sql</files>
  <read_first>
    - .planning/phases/205-u-s-senate-2026-candidate-wiring/205-RESEARCH.md — copy the 35-row `## The Derived 2026 Seat Map` office_id UUIDs VERBATIM; read `## Migration Mechanics` (idempotency pattern) and `## Common Pitfalls` (never link a `Candidate for U.S. Senate` office; two numbering schemes — use disk convention 878, NOT a 900s/1300s ledger number).
    - supabase/migrations/203_ca_federal_grouping_fixes.sql — hand-authored SQL style: hardcoded UUIDs, plain `--` comments explaining the why, no ORM.
    - supabase/migrations/204_la_council_orphan_cleanup.sql — single surgical statement precedent.
  </read_first>
  <action>
    Create `supabase/migrations/878_link_us_senate_2026_races.sql` (878 = disk max 877 + 1, disk-authoritative per D-03 — do NOT use a ledger version number). Write ONE idempotent `UPDATE essentials.races AS r SET office_id = v.office_id::uuid FROM (VALUES ...) AS v(position_name, office_id) WHERE r.position_name = v.position_name AND r.office_id IS NULL;` statement. The VALUES list contains exactly the 35 rows from RESEARCH.md `## The Derived 2026 Seat Map` — each `('U.S. Senate {FullStateName}', '{office_id UUID}')` pair copied verbatim (full state name join key, no abbreviation/dash). Add a header comment block explaining: purpose (link orphaned 2026 U.S. Senate races to their correct NATIONAL_UPPER seat office), the `office_id IS NULL` re-run-safety guard, that the two SPECIAL seats (Ohio->Husted, Florida->Moody) fill vacated seats, and that link targets are real seat offices only (title `Senator`/`U.S. Senate - {State}`), NEVER stray `Candidate for U.S. Senate — {State}` offices (D-07 scope guard). Below the UPDATE, add a `-- SKIP REPORT:` comment section listing any state omitted from the VALUES map with its reason (per D-02/REQ-4 — research found 0 states to skip, so state explicitly "0 states skipped: all 35 DB Senate-race states map 1:1 onto the public Class-2 + 2-special list"). Do NOT paste incumbent names as the link key — link by stable office_id only (Pitfall 2). Do NOT add a DELETE or any statement touching offices/districts/politicians/race_candidates/House races (REQ-5). Do NOT decide the ledger-row question here (RESEARCH Open Question #1 — default is audit-only, i.e. do not register a schema_migrations row; leave that to apply-time in 205-02).
  </action>
  <verify>
    <automated>test -f supabase/migrations/878_link_us_senate_2026_races.sql && grep -c "U.S. Senate " supabase/migrations/878_link_us_senate_2026_races.sql | { read n; [ "$n" -ge 35 ]; } && grep -q "office_id IS NULL" supabase/migrations/878_link_us_senate_2026_races.sql && ! grep -qi "Candidate for U.S. Senate" <(grep -v '^--' supabase/migrations/878_link_us_senate_2026_races.sql) && ! grep -qiE "DELETE|INSERT|DROP|ALTER" <(grep -v '^--' supabase/migrations/878_link_us_senate_2026_races.sql)</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/878_link_us_senate_2026_races.sql` exists (disk-convention number 878, no >900 number).
    - Contains exactly one `UPDATE essentials.races` statement, guarded by `AND r.office_id IS NULL` (idempotent re-run = no-op).
    - The VALUES list has 35 `('U.S. Senate {FullStateName}', '{UUID}')` rows whose UUIDs match RESEARCH.md `## The Derived 2026 Seat Map` verbatim; Ohio and Florida rows are present and annotated SPECIAL in the header/comment.
    - No `Candidate for U.S. Senate` string appears as a link target (only allowed inside a `-- ` comment explaining it is forbidden).
    - No DELETE/INSERT/DROP/ALTER and no statement referencing offices/districts/politicians/race_candidates/House rows (executable SQL only touches `essentials.races.office_id`).
    - A `-- SKIP REPORT:` section is present; it states 0 states skipped (or lists each skipped state + reason if any).
  </acceptance_criteria>
  <done>Idempotent migration 878 authored from the verified map with a skip report; only `essentials.races.office_id` is mutated by its SQL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING seat-map approval checkpoint (D-04 / REQ-1)</name>
  <files>supabase/migrations/878_link_us_senate_2026_races.sql</files>
  <read_first>
    - supabase/migrations/878_link_us_senate_2026_races.sql — the just-authored file (present its VALUES map).
    - .planning/phases/205-u-s-senate-2026-candidate-wiring/205-RESEARCH.md `## The Derived 2026 Seat Map` — the source table with incumbent names + SPECIAL flags for human-legible presentation.
  </read_first>
  <action>Present the full 35-row state -> seat(-> incumbent) table (rendered from RESEARCH.md `## The Derived 2026 Seat Map`) to the user, with the two SPECIAL seats (Ohio->Husted, Florida->Moody) flagged distinctly and the CONTEXT-confirmed correct-seat examples (MN->Smith, TX->Cornyn, TN->Hagerty) called out. State the skip report result (0 states). Then BLOCK and wait for explicit user approval — no production write is authorized until approval is given (D-04). This is a review gate, not an implementation step: no SQL executes here.</action>
  <what-built>
    An idempotent migration (878) that will set `essentials.races.office_id` for 35 states' 2026
    U.S. Senate races to their correct NATIONAL_UPPER seat office. NOTHING has been written to
    production yet — this checkpoint gates the write (which happens in Plan 205-02).
  </what-built>
  <how-to-verify>
    1. Review the full 35-row state -> seat(-> incumbent) table below (rendered from RESEARCH.md
       `## The Derived 2026 Seat Map`), presented for spot-check.
    2. Confirm the two SPECIAL seats are flagged distinctly and correct:
         - Ohio  -> Jon Husted   (SPECIAL — filling Vance's vacated seat)
         - Florida -> Ashley Moody (SPECIAL — filling Rubio's vacated seat)
    3. Spot-check the CONTEXT-confirmed "correct-seat-not-the-famous-one" examples:
         - Minnesota -> Tina Smith (NOT Klobuchar)
         - Texas     -> John Cornyn (NOT Cruz)
         - Tennessee -> Bill Hagerty (NOT Blackburn)
    4. Confirm the skip report: 0 states skipped (all 35 DB Senate-race states mapped).
    5. Sanity-check any state you personally know the 2026 Class-2 seat for.
    Note: incumbent NAMES may drift on live prod (e.g. SC/Lindsey Graham died 2026-07-11) but the
    office_id link target is stable regardless — a name mismatch is informational, not a blocker,
    unless the office's state/district_type is wrong.
  </how-to-verify>
  <verify>
    <automated>MANUAL — human approval gate; no automated command. Approval signal recorded in 205-01-SUMMARY.md before the plan completes.</automated>
  </verify>
  <resume-signal>Type "approved" to authorize applying migration 878 in Plan 205-02, or list any state whose seat is wrong (that state will be removed from the VALUES map and moved to the skip report before apply).</resume-signal>
  <acceptance_criteria>
    - The full 35-row map (incumbent + SPECIAL flags) was presented to the user; the two SPECIAL seats were called out distinctly.
    - The user gave an explicit "approved" signal (or named corrections, which were applied to the VALUES map + skip report before proceeding).
    - The approval signal (and any corrections) is recorded in 205-01-SUMMARY.md.
    - No SQL was executed against production in this task.
  </acceptance_criteria>
  <done>User has explicitly approved the seat map (or corrections applied); approval recorded; apply is authorized for Plan 205-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research map -> authored SQL | A wrong office_id copied into the VALUES list would link a race to the wrong 2026 seat |
| authored SQL -> human approval | Last human gate before any production write is authorized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-01 | Tampering | VALUES map in migration 878 (wrong-seat linkage) | mitigate | UUIDs copied verbatim from RESEARCH `## The Derived 2026 Seat Map`; seat-title guard forbids `Candidate for U.S. Senate%` targets; BLOCKING D-04 human checkpoint reviews all 35 rows incl. the 2 SPECIAL seats before any apply |
| T-205-02 | Tampering | Stray `Candidate for U.S. Senate — {State}` offices used as a link target | mitigate | VALUES map hardcodes only real seat-office UUIDs; automated grep gate rejects any `Candidate for U.S. Senate` link target in executable SQL (D-07) |
| T-205-03 | Elevation/Denial | Non-idempotent re-apply corrupting state | mitigate | `AND r.office_id IS NULL` guard makes re-run a no-op; no INSERT/DELETE/DDL in the file |
| T-205-04 | Information Disclosure | Stale incumbent name at review (e.g. SC Graham) misleads approver | accept | office_id is stable across incumbent churn (Pitfall 2); name shown as informational only; state/district_type is the load-bearing field, verified by 205-02 parity query |

Block on: T-205-01, T-205-02 (high severity — wrong production data). Both are gated by the D-04 checkpoint and the seat-title grep.
</threat_model>

<verification>
- File 878 exists, single idempotent UPDATE, 35 VALUES rows, `office_id IS NULL` guard, no forbidden statements/targets (automated grep in Task 1).
- Human approval recorded at the D-04 checkpoint before this plan is considered complete (REQ-1).
- Skip report present (REQ-4 authoring half).
</verification>

<success_criteria>
- `supabase/migrations/878_link_us_senate_2026_races.sql` authored, idempotent, seat-title-safe, 35 rows.
- Full 35-row seat map (SPECIALs flagged) presented and explicitly approved by the user.
- Skip report written (0 states expected).
- Nothing written to production in this plan.
</success_criteria>

<output>
Create `.planning/phases/205-u-s-senate-2026-candidate-wiring/205-01-SUMMARY.md` when done, recording the final approved VALUES row count, any states moved to the skip report, and the human approval signal.
</output>
