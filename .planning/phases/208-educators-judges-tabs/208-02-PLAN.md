---
phase: 208-educators-judges-tabs
plan: 02
type: execute
wave: 2
depends_on: [208-01]
files_modified: []
autonomous: false
requirements: [TAB-01, TAB-02, TAB-03]
user_setup: []

must_haves:
  truths:
    - "On a live location, the tab row reads Representatives · Educators · Judges · Elections when all buckets are populated"
    - "Switching to Educators shows only school-board office-holders; switching to Judges shows only judicial office-holders"
    - "Representatives no longer contains school-board/judicial officials"
    - "A location with 0 educators does NOT render an Educators tab; a location with 0 judges does NOT render a Judges tab"
    - "A stale ?view=judges on a no-judges location renders Representatives, not a blank tab"
    - "The election summary + yellow 'N days away' pill appear in the location-header row (matching mockup) and persist across all four tabs; the Elections tab label is plain 'Elections'"
    - "At ~280px width every tab stays reachable (short labels and/or horizontal scroll)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the four-tab officials view built in Plan 208-01 against real locations and the user
mockup, confirming tab routing, decluttering, hide-when-empty, active-tab fallback, the relocated
election summary, and mobile fit.

Purpose: TAB-01/02/03 are UI behaviors (UI hint: yes) that grep/build cannot fully prove — a human
confirms the rendered result on live/dev data.
Output: Operator approval (or a punch-list of issues) recorded in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/208-educators-judges-tabs/208-CONTEXT.md
@.planning/phases/208-educators-judges-tabs/208-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and launch the dev server for verification</name>
  <files>none (build + dev server only; no source files modified)</files>
  <read_first>
    - .planning/phases/208-educators-judges-tabs/208-01-SUMMARY.md (what shipped + any deviations)
  </read_first>
  <action>
    Run `npm run build` to confirm a clean production build, then start `npm run dev` (Vite) so the
    operator can exercise the four-tab view. Report the local dev URL (typically http://localhost:5173).
    Identify concrete test locations for the operator: (a) a location with school-board AND judicial
    officials for the populated-tab case (e.g. an LA address / browse Los Angeles — LA school-board
    districts are the canonical clutter case from TAB-02); (b) a plain representatives-only location
    (e.g. a small town) to confirm Educators/Judges tabs are absent; (c) a stale-URL test by appending
    `?view=judges` to a no-judges location.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Dev server is running and the URL + the three suggested test locations are reported to the operator
  </acceptance_criteria>
  <done>Dev server is up and the operator has URLs + test addresses to verify against.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the four-tab officials view (routing, hide-when-empty, fallback, relocated summary, 280px)</name>
  <what-built>
    Educators and Judges tabs added beside Representatives and Elections in the officials view,
    classifyBucket-routed, with hide-when-empty tabs, active-tab fallback to Representatives, a plain
    "Elections" tab label, and the election summary + yellow day-pill relocated to the location-header row.
  </what-built>
  <how-to-verify>
    1. Open the dev URL and load a location with school-board + judicial officials (e.g. an LA address or
       browse Los Angeles). Confirm the tab row reads, left to right: Representatives · Educators · Judges
       · Elections.
    2. Click Educators — confirm it lists ONLY school-board office-holders (e.g. the LA school-board
       districts), using the same card/tier layout as Representatives.
    3. Click Judges — confirm it lists ONLY judicial office-holders, same layout.
    4. Click Representatives — confirm school-board and judicial office-holders are GONE from this list
       (decluttered); mayors/council/legislators/federal remain.
    5. Load a representatives-only location (small town). Confirm NO Educators tab and NO Judges tab
       appear (hidden entirely, not greyed) — only Representatives and Elections.
    6. On that no-judges location, append `?view=judges` to the URL and reload. Confirm it renders the
       Representatives tab (clean fallback), not a blank/broken tab.
    7. Confirm the Elections tab label is plain "Elections" (no "- California General…" suffix, no day
       badge on the tab), and that the election summary text + yellow "N days away" pill now appear in
       the top location-header row (pin + address line) and stay visible on all four tabs. Compare to the
       user mockup format "Elections - {suffix} · {date}" + pill.
    8. Confirm the compass-mode control still works on the Educators and Judges tabs (compass renders/toggles).
    9. Narrow the browser to ~280-375px (device toolbar). Confirm all four tabs stay reachable (short
       labels and/or horizontal scroll) with no overflow breaking the layout.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms items 1-9, or provides a specific punch-list
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Operator sign-off on the 9-point checklist above covers TAB-01 (switchable tabs), TAB-02 (declutter),
and TAB-03 as revised by D-06 (hide-when-empty + fallback), plus D-01/D-02/D-03/D-04/D-10 behaviors.
</verification>

<success_criteria>
- Operator confirms the four-tab layout, classifyBucket routing, decluttered Representatives,
  hidden-when-empty Educators/Judges, stale-URL fallback, relocated election summary, working compass
  on all people-tabs, and 280px reachability — or a punch-list is recorded for a gap-closure pass.
</success_criteria>

<output>
Create `.planning/phases/208-educators-judges-tabs/208-02-SUMMARY.md` when done, recording the operator
verdict (approved / punch-list).
</output>
