---
phase: 84-multnomah-smaller-cities
plan: 02
type: execute
wave: 2
depends_on: [84-01]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-cities-headshots.py
  - C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql
autonomous: true
requirements: [CITIES-06]
tags: [phase-84, multnomah, oregon, cities, headshots, image-processing]

must_haves:
  truths:
    - "Every Gresham official (-4131251..-4131257) has exactly one essentials.politician_images row with type='default' and photo_license='public_domain' (7/7 photos from greshamoregon.gov per D-08, D-09)"
    - "Every Troutdale official (-4174851..-4174857) has exactly one essentials.politician_images row with type='default' (7/7 photos from troutdaleoregon.gov WebP source)"
    - "Every Wood Village official (-4183951..-4183955) has exactly one essentials.politician_images row with type='default' (5/5 photos from woodvillageor.gov)"
    - "Maywood Park officials (-4146731..-4146735): photo upload attempted; politician_images rows inserted where photos exist on cityofmaywoodpark.com, or documented as 'No photo found on official city website.' per D-09"
    - "Fairview officials (-4124251..-4124257) have NO politician_images rows; migration 247 documents each as 'No photo found on official city website.' (fairvieworegon.gov verified to have no photos)"
    - "Every uploaded image is exactly 600 wide by 750 tall pixels, JPEG quality 90, processed via crop-to-4:5-first then Lanczos resize (per D-10)"
    - "Migration 247 is AUDIT-ONLY: NOT applied via Supabase ledger (matches 245_multnomah_county_headshots.sql pattern)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-cities-headshots.py"
      provides: "Python PIL fetch + crop-to-4:5 + Lanczos resize + Supabase Storage upload + politician_images INSERT for all officials with discoverable photos (per D-08)"
      exports: ["main"]
    - path: "C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql"
      provides: "AUDIT-ONLY record per D-09: one INSERT block (or no-photo comment) per official across all 31 officials in all 5 cities, organized by city section"
      contains: "AUDIT-ONLY"
  key_links:
    - from: "Supabase Storage bucket politician_photos"
      to: "essentials.politician_images.url"
      via: "public storage URL written to politician_images row at upload time"
      pattern: "https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{politician_id}-headshot.jpg"
    - from: "essentials.politician_images.politician_id"
      to: "essentials.politicians.id (external_ids -4131251..-4146735 from Phase 84-01)"
      via: "FK; matched via external_id subquery in INSERT"
      pattern: "(SELECT id FROM essentials.politicians WHERE external_id = <id>)"
    - from: "Results.jsx / Profile.jsx photo filter"
      to: "essentials.politician_images.type"
      via: "frontend .find(img => img.type === 'default')"
      pattern: "type = 'default' (NOT 'headshot' — per D-10 and PATTERNS.md photo type='default' shared pattern)"
---

<objective>
Source, crop, resize, and upload headshots for all 31 Multnomah smaller-cities officials where a photo exists on the official city website (per D-08 — no LinkedIn, news, or social media); insert essentials.politician_images rows with type='default'; and capture all 31 outcomes (upload URL or 'No photo found' comment) in audit-only migration 247.

Purpose: CITIES-06 — headshots for smaller city officials where available online. Expected coverage based on RESEARCH.md verification of each city's official site:
- Gresham (7/7): all from greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/{filename}.jpg
- Troutdale (7/7): all from troutdaleoregon.gov WebP (`/sites/g/files/vyhlif13696/files/styles/convert_image_to_webp/.../{filename}.jpg.webp`)
- Wood Village (5/5): all from woodvillageor.gov/wp-content/uploads/{filename}.jpg (WordPress CDN)
- Fairview (0/7): no photos on fairvieworegon.gov (verified absent 2026-05-31 — document with 'No photo found' comments)
- Maywood Park: verify during execution (RESEARCH.md A1 marked LOW confidence — page truncation during research); document outcome (likely 0/5 → 'No photo found' comments)

Output: ~19+ JPEGs (600×750) uploaded to Supabase Storage at `politician_photos/{politician_id}-headshot.jpg`; politician_images rows in essentials schema (type='default', photo_license='public_domain'); audit-only SQL migration 247 documenting all 31 outcomes (one block per official — either INSERT or no-photo comment).

Process model: Live upload happens via a one-shot Python helper (_tmp-cities-headshots.py) that mirrors C:/EV-Accounts/backend/scripts/portland-headshots-process.py (multi-official, multi-source, includes WebP support natively via Pillow). The audit-only migration 247 is generated AFTER live writes and serves as a forensic record per D-09 — it is NOT applied via the Supabase ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/84-multnomah-smaller-cities/84-CONTEXT.md
@.planning/phases/84-multnomah-smaller-cities/84-RESEARCH.md
@.planning/phases/84-multnomah-smaller-cities/84-PATTERNS.md
@.planning/phases/84-multnomah-smaller-cities/84-01-SUMMARY.md

# Reference upload script (canonical Python PIL pattern; multi-official; WebP support via Pillow Image.open)
@C:/EV-Accounts/backend/scripts/portland-headshots-process.py

# Reference audit-only migration (matches exactly the file 247 produces)
@C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql

# Additional headshot audit-only reference (OR-style)
@C:/EV-Accounts/backend/migrations/225_or_headshots.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch + crop + resize + upload headshots and INSERT politician_images rows</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-cities-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/portland-headshots-process.py (canonical Python PIL + requests + Supabase Storage upload pattern — read in full; copy env loading, BROWSER_HEADERS, OFFICIALS dict shape, process_image function lines 183-241, upload_to_supabase function lines 244-258, main loop lines 261-342, exit-non-zero on partial failure)
    - C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql (politician_images INSERT shape used by audit migration; informs what columns must be written)
    - .planning/phases/84-multnomah-smaller-cities/84-RESEARCH.md (Headshot Availability section per city for all URLs and filenames; Verified Incumbent Roster for the 31 external_id → name → city mapping; Pitfall 5 for type='default')
    - .planning/phases/84-multnomah-smaller-cities/84-PATTERNS.md (lines 619-799 — full inline Python template including OFFICIALS dict, process_image, upload_to_supabase, main loop)
    - .planning/phases/84-multnomah-smaller-cities/84-01-SUMMARY.md (for the 31 politician UUIDs generated by migration 246; if SUMMARY not yet written, look up UUIDs via SQL query at script start)
  </read_first>
  <action>
    Create file C:/EV-Accounts/backend/scripts/_tmp-cities-headshots.py. One-shot helper that runs once to perform live writes; deleted after Task 2 generates the audit migration. Mirrors portland-headshots-process.py structure.

    Script responsibilities (in order):

    1. Docstring (5-10 lines) describing the pipeline and Phase 84 Plan 02 context.

    2. Imports: os, io, sys, json, time, requests, PIL.Image.

    3. Env loading: read C:/EV-Accounts/backend/.env line-by-line (per portland-headshots-process.py lines 19-30 pattern); extract SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY. Fail fast (sys.exit(1)) if either is missing.

    4. Constants:
       - SUPABASE_URL = env['SUPABASE_URL']
       - SERVICE_KEY = env['SUPABASE_SERVICE_ROLE_KEY']
       - CDN_BASE = 'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos'
       - TMP_DIR from environ TEMP fallback to 'C:/Windows/Temp'
       - TARGET_W, TARGET_H = 600, 750
       - TARGET_RATIO = 1.25 (4:5)
       - BROWSER_HEADERS dict with User-Agent 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36' and Accept 'image/webp,image/jpeg,image/*,*/*;q=0.8'. The Referer header MUST be set per-city before each fetch (see step 7); default to 'https://www.greshamoregon.gov/' for the first batch.

    5. fetch_politician_uuid_map() function: at script start, query Supabase REST or use psycopg2 (whichever pattern portland-headshots-process.py uses — if neither, add a small psycopg2 connection block using DATABASE_URL) to SELECT id, external_id FROM essentials.politicians WHERE external_id IN (<list of 31 ids from Phase 84-01>). Build dict keyed by external_id, value = politician UUID. Fail fast if any of the 31 external_ids is missing (Phase 84-01 must have been applied first).

       NOTE: portland-headshots-process.py hard-codes UUIDs in OFFICIALS. Use the same pattern — populate UUIDs via DB lookup AT SCRIPT START (lookup function described above), then assign into OFFICIALS entries before the main loop. Do NOT require the executor to hand-edit UUIDs.

    6. OFFICIALS list (Python list of dicts). One entry per official WITH a photo source URL. Entries for Fairview (7) and Maywood Park (5, unless executor confirms photos exist) are OMITTED from OFFICIALS — they get no-photo audit comments only.

       OFFICIALS entries — each dict has: external_id (int), name (str), id (str, filled by fetch_politician_uuid_map), source_url (str), license (str = 'public_domain'), referer (str, the city domain). Sections separated by `# === CITY ===` comments:

       GRESHAM (7 entries, all source URLs from greshamoregon.gov per RESEARCH.md Headshot Availability table):
       - -4131251 Travis Stovall https://www.greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/mayor-stovall-thumbnail.jpg
       - -4131252 Kayla Brown https://www.greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/kaylabrown-portrait-thumb.jpg
       - -4131253 Eddy Morales https://www.greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/councilor-morales-thumbnail.jpg
       - -4131254 Cathy Keathley https://www.greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/cathy-keathley-thumbnail.jpg
       - -4131255 Jerry Hinton https://www.greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/councilor-hinton-thumbnail.jpg
       - -4131256 Sue Piazza https://www.greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/councilor-piazza-thumbnail.jpg
       - -4131257 Janine Gladfelter https://www.greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/councilor-gladfelter-thumbnail.jpg
       All referers: 'https://www.greshamoregon.gov/'.

       TROUTDALE (7 entries — WebP source per RESEARCH.md). RESEARCH.md only confirmed 2 of 7 URLs (Mayor Ripma 821, Carol Allen 30261). Script MUST scrape troutdaleoregon.gov/node/721 at runtime to extract the 5 remaining /sites/g/files/vyhlif13696/files/styles/convert_image_to_webp/.../{filename}.jpg.webp URLs. Implement a pre-scrape step: `requests.get('https://www.troutdaleoregon.gov/node/721', headers=BROWSER_HEADERS, timeout=30)` → regex-extract `/sites/g/files/vyhlif\d+/files/styles/convert_image_to_webp/.+?\.jpg\.webp` URLs from the response.text → match each councilor name in the surrounding HTML context to populate OFFICIALS source_url values. If any of the 7 URLs cannot be auto-resolved, log the missing names and continue with the resolvable ones (RESEARCH A4 documents this as known gap; do not block the run).
       Referers: 'https://www.troutdaleoregon.gov/'. External_ids: -4174851..-4174857. Names per Phase 84-01 roster.

       WOOD VILLAGE (5 entries — woodvillageor.gov/wp-content/uploads/). Like Troutdale, RESEARCH.md only confirmed 1 URL pattern. Script MUST scrape woodvillageor.gov/government/city-council/ at runtime to extract /wp-content/uploads/.../{filename}.jpg URLs and match by name. If any URL cannot be auto-resolved, log and continue.
       Referers: 'https://www.woodvillageor.gov/'. External_ids: -4183951..-4183955. Names per Phase 84-01 roster.

       FAIRVIEW: NOT in OFFICIALS — 7 officials documented as no-photo in Task 2 audit migration. Skipped here entirely.

       MAYWOOD PARK: Add a runtime probe — `requests.get('https://www.cityofmaywoodpark.com/government/city-council', headers=BROWSER_HEADERS, timeout=30)` (URL pattern from RESEARCH.md; if page truncation prevented research, executor verifies live) and regex-search for any `<img>` tags with src pointing to council member photos. If photos are discovered, populate OFFICIALS entries dynamically. If none found (likely outcome per RESEARCH.md A1), leave OFFICIALS Maywood Park section empty; Task 2 will document all 5 as no-photo.
       Referer when present: 'https://www.cityofmaywoodpark.com/'. External_ids: -4146731..-4146735. Names per Phase 84-01 roster.

    7. process_image(raw_data: bytes, name: str) -> bytes function: VERBATIM from portland-headshots-process.py lines 183-241. Behavior summary:
       - Image.open(io.BytesIO(raw_data)); if mode != 'RGB': convert('RGB') — Pillow handles WebP natively, no special decoding needed.
       - Compute current_ratio = h / w.
       - If portrait (ratio >= TARGET_RATIO 1.25): top-crop. If very tall (>2.0), top=5%; elif >1.5, top=2%; else top=0.
       - If square or landscape (ratio < TARGET_RATIO): center-crop width to achieve 4:5.
       - Edge case: if new_w > w (extreme landscape), crop height instead.
       - img.resize((600, 750), Image.LANCZOS).
       - Save as JPEG quality=90 optimize=True.
       - Return bytes.
       Crop-to-4:5-FIRST is mandatory per D-10. Never stretch. Output buffer must be exactly 600×750 — verify after resize using a metadata assertion before upload.

    8. upload_to_supabase(politician_id: str, jpeg_data: bytes) -> str function: VERBATIM from portland-headshots-process.py lines 244-258.
       - URL: f"{SUPABASE_URL}/storage/v1/object/politician_photos/{politician_id}-headshot.jpg"
       - Headers: Authorization Bearer SERVICE_KEY, Content-Type image/jpeg, x-upsert true
       - requests.put with timeout=30
       - Raise on non-2xx with body text in exception
       - Return CDN URL: f"{CDN_BASE}/{politician_id}-headshot.jpg"

    9. insert_politician_image(politician_id: str, cdn_url: str, license: str) function: write to essentials.politician_images via the same psycopg2 connection. Columns: id (gen by uuid.uuid4()), politician_id, url=cdn_url, type='default' (literal — NOT 'headshot' per Pitfall 5), photo_license=license. Use INSERT with WHERE NOT EXISTS guard on politician_id (idempotent re-run). Connection commits on success; rolls back on exception (per-row, not per-batch).

    10. main() function:
       - Print start banner.
       - Call fetch_politician_uuid_map() to populate id field in OFFICIALS.
       - Run pre-scrapes for Troutdale, Wood Village, Maywood Park as described in step 6 (auto-populate source_url entries; log missing URLs).
       - results = []; for each official in OFFICIALS:
         * Print `\n{'='*60}\nProcessing: {name} (external_id={external_id})`.
         * Set BROWSER_HEADERS['Referer'] = official['referer'].
         * try: raw = requests.get(official['source_url'], headers=BROWSER_HEADERS, timeout=30).content; processed = process_image(raw, name); cdn_url = upload_to_supabase(id, processed); insert_politician_image(id, cdn_url, license); results.append({...success: True...}); time.sleep(0.5).
         * except Exception as e: print error; results.append({...success: False, error: str(e)}).
       - Print summary: f"\n{success_count}/{len(results)} headshots uploaded successfully".
       - Write results JSON to TMP_DIR/cities-headshots-results.json (per portland-headshots-process.py pattern); print path.
       - sys.exit(1) if any official with a source_url failed (partial success is failure for those entries).
       - Note: officials not in OFFICIALS list (Fairview 7 + any Maywood Park with no photo) are NOT counted as failures — they are handled by Task 2 audit comments.

    11. `if __name__ == '__main__': main()` guard.

    Idempotency: Storage uploads use x-upsert=true (overwrite OK on re-run). politician_images INSERTs use WHERE NOT EXISTS guard keyed on politician_id (skip if row exists). Re-running the script is safe.

    Connection security: SUPABASE_SERVICE_ROLE_KEY is loaded from .env at startup; never logged; psycopg2 uses DATABASE_URL via same dotenv pattern. Temp script must be deleted by Task 2 (per established pattern — script is one-shot, durable record is the audit migration).
  </action>
  <verify>
    <automated>
      Run script: `cd C:/EV-Accounts/backend ; python scripts/_tmp-cities-headshots.py`
      Expected exit 0 if all officials in OFFICIALS list (post-scrape) succeed; expected stdout contains the line `N/N headshots uploaded successfully` where N matches the number of officials with discoverable source URLs.
      Post-run SQL gates (run via mcp__supabase-local__execute_sql):
      (a) Gresham photo gate: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON pi.politician_id=p.id WHERE p.external_id BETWEEN -4131257 AND -4131251 AND pi.type='default' — returns 7.
      (b) Troutdale photo gate: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON pi.politician_id=p.id WHERE p.external_id BETWEEN -4174857 AND -4174851 AND pi.type='default' — returns 7 (or matches actual count discovered by scrape; document any gap in SUMMARY).
      (c) Wood Village photo gate: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON pi.politician_id=p.id WHERE p.external_id BETWEEN -4183955 AND -4183951 AND pi.type='default' — returns 5 (or matches actual count discovered by scrape).
      (d) Fairview no-photo gate: SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON pi.politician_id=p.id WHERE p.external_id BETWEEN -4124257 AND -4124251 — returns 0.
      (e) Storage spot-check: for one uploaded politician_id, download the image via curl from the CDN URL and verify dimensions exactly 600x750 (via PIL or `file` command).
    </automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/_tmp-cities-headshots.py exists
    - File imports PIL.Image (via `from PIL import Image`)
    - File contains the literal constant CDN_BASE pointing to `kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos`
    - File contains TARGET_W = 600 and TARGET_H = 750 (or equivalent constants)
    - File contains the literal string `type` paired with `'default'` (the politician_images.type value — never 'headshot')
    - File contains a desktop User-Agent header string in BROWSER_HEADERS
    - File contains the 7 Gresham source URLs verbatim from RESEARCH.md (greshamoregon.gov/globalassets/government/mayor-and-council/meet-the-council/...)
    - File contains a runtime-scrape block (regex or BeautifulSoup) for troutdaleoregon.gov/node/721 to discover the 7 WebP URLs
    - File contains a runtime-scrape block for woodvillageor.gov/government/city-council/ to discover the 5 WordPress URLs
    - File contains a runtime-probe block for cityofmaywoodpark.com council page to discover any photos
    - File does NOT contain any Fairview source URLs (Fairview officials skipped in OFFICIALS list)
    - File contains a fetch_politician_uuid_map() function (or equivalent) that loads UUIDs via SQL at script start
    - File contains a process_image function that crops to 4:5 BEFORE resizing
    - File contains an upload_to_supabase function that uses x-upsert: true header
    - File contains an insert_politician_image function (or inline insert) that writes type='default' and photo_license='public_domain'
    - After script run: SQL gate (a) returns 7 (Gresham 7/7)
    - After script run: SQL gate (d) returns 0 (Fairview no photos)
    - After script run: stdout includes the literal `headshots uploaded` summary line
    - After script run: at least one storage object exists at `politician_photos/{uuid}-headshot.jpg` and is exactly 600x750 pixels (spot-check via curl -sI or PIL re-open)
    - Re-running the script is a no-op for politician_images (no duplicate rows; existing-row guard works); Storage upserts (overwrite acceptable)
    - Script exits non-zero ONLY when at least one official with a discovered source_url fails to upload — Fairview no-photos and Maywood Park-no-photos do NOT trigger non-zero exit
  </acceptance_criteria>
  <done>
    Script exists, ran successfully against production Supabase; all Gresham officials (7/7) have 600×750 JPEG headshots in Supabase Storage and politician_images rows with type='default'; Troutdale and Wood Village photos uploaded for every official whose URL was discoverable via the runtime scrape; Fairview officials remain with 0 politician_images rows (documented in Task 2); Maywood Park photo outcome is documented in stdout. SQL gates (a) through (d) return the expected counts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write AUDIT-ONLY migration 247 documenting all 31 official outcomes (per D-09)</name>
  <files>C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql (canonical AUDIT-ONLY migration; copy file header convention, per-official INSERT shape, no-photo comment pattern; closest analog — read in full)
    - C:/EV-Accounts/backend/migrations/225_or_headshots.sql (additional OR audit-only reference for comparison)
    - .planning/phases/84-multnomah-smaller-cities/84-PATTERNS.md (lines 340-396 — full inline AUDIT-ONLY header + per-official INSERT pattern + no-photo comment pattern)
    - .planning/phases/84-multnomah-smaller-cities/84-RESEARCH.md (Verified Incumbent Roster for all 31 names + external_ids + which city; Headshot Availability section to confirm which officials get INSERT vs. no-photo comment)
    - The post-run state of essentials.politician_images: query SELECT p.external_id, pi.url FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id = p.id AND pi.type='default' WHERE p.external_id IN (<31 external_ids>) ORDER BY p.external_id — use the actual URLs as the canonical source for INSERTs in migration 247
  </read_first>
  <action>
    Create file C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql as an AUDIT-ONLY migration. Implements D-09: every official documented — either an INSERT block (photo uploaded) or a no-photo comment. Per D-10 the type column value is always 'default'. This migration is NOT applied via Supabase ledger — it documents the writes performed by Task 1 (matches the 245_multnomah_county_headshots.sql pattern from Phase 83).

    Structure (mirror 245_multnomah_county_headshots.sql):

    SECTION A — File header comment block (12-18 lines):
    - "Migration 247: Multnomah Smaller Cities Official Headshots"
    - "AUDIT-ONLY: captures the live politician_images INSERTs performed during Phase 84-02 execution on YYYY-MM-DD" (use actual run date)
    - "DO NOT apply via Supabase ledger — actual DB writes happened live via scripts/_tmp-cities-headshots.py."
    - "Pattern matches 245_multnomah_county_headshots.sql / 225_or_headshots.sql."
    - "31 officials documented across 5 cities:"
    - "  GRESHAM (7 officials, external_ids -4131251..-4131257): all have photos on greshamoregon.gov"
    - "  TROUTDALE (7 officials, external_ids -4174851..-4174857): photos on troutdaleoregon.gov (WebP)"
    - "  FAIRVIEW (7 officials, external_ids -4124251..-4124257): no photos on official site"
    - "  WOOD VILLAGE (5 officials, external_ids -4183951..-4183955): photos on woodvillageor.gov"
    - "  MAYWOOD PARK (5 officials, external_ids -4146731..-4146735): [actual outcome from Task 1 run]"
    - "Photo processing: crop 4:5 first, resize 600x750 Lanczos q90."
    - "Storage bucket: politician_photos; path: {politician_id}-headshot.jpg."

    SECTION B — No BEGIN/COMMIT wrapper. No ledger entry. AUDIT-ONLY migrations are reference documents, not transactions.

    SECTION C — Five city sections, each delimited by:
       -- ============================================================
       -- {CITY NAME} (N officials)
       -- ============================================================

    For each official, write ONE of these two block patterns:

    PATTERN 1 — Photo uploaded (Gresham 7, Troutdale 7 minus any auto-resolve gaps, Wood Village 5 minus gaps, any Maywood Park photos discovered):
    ```
    -- {Full Name} ({external_id}) — {Title}
    -- source: {original source URL from official city website}
    INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license)
    SELECT gen_random_uuid(),
           (SELECT id FROM essentials.politicians WHERE external_id = {external_id}),
           '{actual Supabase Storage CDN URL written by Task 1}',
           'default', 'public_domain'
    WHERE NOT EXISTS (
      SELECT 1 FROM essentials.politician_images
      WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = {external_id})
    );
    ```
    Notes:
    - type literal MUST be 'default' (per D-10; per Pitfall 5)
    - photo_license MUST be 'public_domain'
    - {actual Supabase Storage CDN URL} — pull from the actual essentials.politician_images.url value created by Task 1 (query the DB AFTER Task 1 to get verbatim URLs)

    PATTERN 2 — No photo found (Fairview 7, any Troutdale/Wood Village officials whose URL did not auto-resolve, any Maywood Park officials with no photo):
    ```
    -- {Full Name} ({external_id}) — {Title}
    -- No photo found on official city website ({city domain}).
    -- No politician_images row inserted.
    ```
    Notes:
    - The 3-line comment block is mandatory per D-09 — every official documented
    - No INSERT statement written

    SECTION D — Final comment block summarizing counts:
    ```
    -- =============== SUMMARY ===============
    -- Total officials: 31
    -- Headshots uploaded: {N} (Gresham {x}, Troutdale {x}, Wood Village {x}, Maywood Park {x})
    -- No photo documented: {31 - N} (Fairview 7, plus any others)
    -- =====================================
    ```

    Critical constraints (acceptance-criteria verified):
    - File contains NO `INSERT INTO supabase_migrations.schema_migrations` (audit-only — no ledger entry)
    - File contains NO `BEGIN;` or `COMMIT;` (audit-only — not a transaction)
    - Every one of the 31 external_ids (-4131251..-4131257, -4174851..-4174857, -4124251..-4124257, -4183951..-4183955, -4146731..-4146735) appears at least once in either an INSERT block or a no-photo comment block
    - All INSERT blocks use type='default' and photo_license='public_domain'
    - All Fairview external_ids (-4124251..-4124257) appear ONLY in no-photo comment blocks (no INSERT)
    - All Gresham external_ids (-4131251..-4131257) appear in INSERT blocks
    - The URLs in INSERT blocks exactly match the urls present in essentials.politician_images at audit time (cross-check via DB query)
    - "E'an Todd" appears in its Fairview comment block with standard ASCII apostrophe (matching the migration 246 escaping rule)

    After Task 2 completes and migration 247 is committed to git, DELETE C:/EV-Accounts/backend/scripts/_tmp-cities-headshots.py — it is one-shot and its purpose is preserved in the audit migration (per Phase 83 precedent for _tmp-multnomah-headshots.ts).
  </action>
  <verify>
    <automated>
      Run these checks against the file:
      (1) `test -f C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql` exits 0.
      (2) `grep -c "AUDIT-ONLY" C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql` returns >= 1.
      (3) `grep -c "DO NOT apply via Supabase ledger" C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql` returns >= 1.
      (4) `grep -c "INSERT INTO supabase_migrations.schema_migrations" C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql` returns 0 (no ledger entry; audit-only).
      (5) `grep -cE "^BEGIN;|^COMMIT;" C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql` returns 0 (no transaction wrapper).
      (6) For each of the 31 external_ids, `grep -c "external_id = -<id>" C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql` returns >= 1 (every official documented).
      (7) `grep -cE "external_id = -412425[1-7]" file | grep -v INSERT` confirms Fairview's 7 ids appear in COMMENT context only (no INSERT for any Fairview official).
      (8) Cross-reference DB: `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON pi.politician_id=p.id WHERE p.external_id IN (<31 ids>) AND pi.type='default'` matches the number of INSERT blocks in 247.
      (9) `test ! -f C:/EV-Accounts/backend/scripts/_tmp-cities-headshots.py` exits 0 after audit migration is final (temp script deleted per established pattern).
    </automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/247_multnomah_cities_headshots.sql exists
    - File header contains the literal string `AUDIT-ONLY`
    - File header contains the literal string `DO NOT apply via Supabase ledger`
    - File does NOT contain the string `INSERT INTO supabase_migrations.schema_migrations` (no ledger entry)
    - File does NOT contain any lines starting with `BEGIN;` or `COMMIT;`
    - File contains a city section header for each of: GRESHAM, TROUTDALE, FAIRVIEW, WOOD VILLAGE, MAYWOOD PARK
    - File references every one of these 31 external_id values (each appears at least once): -4131251, -4131252, -4131253, -4131254, -4131255, -4131256, -4131257, -4174851, -4174852, -4174853, -4174854, -4174855, -4174856, -4174857, -4124251, -4124252, -4124253, -4124254, -4124255, -4124256, -4124257, -4183951, -4183952, -4183953, -4183954, -4183955, -4146731, -4146732, -4146733, -4146734, -4146735
    - File contains 7 INSERT INTO essentials.politician_images statements for Gresham external_ids -4131251 through -4131257
    - File contains zero INSERT statements for Fairview external_ids -4124251 through -4124257 (all 7 appear in `-- No photo found on official city website` comment blocks instead)
    - Every INSERT statement contains the literal `'default'` for the type column and `'public_domain'` for the photo_license column
    - Every INSERT statement contains a URL matching the regex `https://.+supabase\.co/storage/v1/object/public/politician_photos/.+-headshot\.jpg`
    - The set of URLs in INSERT blocks of 247 matches exactly the set of URLs in essentials.politician_images WHERE p.external_id IN (<31 ids>) AND pi.type='default' (cross-check via DB query — counts must match the SUMMARY block at end of file)
    - File ends with a SUMMARY comment block containing per-city photo counts
    - File C:/EV-Accounts/backend/scripts/_tmp-cities-headshots.py has been deleted after migration 247 is final (one-shot temp script convention per Phase 83)
  </acceptance_criteria>
  <done>
    Audit migration 247 file exists with INSERT blocks for every official whose photo was uploaded by Task 1 and no-photo comment blocks for every official with no available photo. Storage URLs in the INSERT blocks exactly match the live politician_images rows. Temporary upload script has been deleted. No ledger entry written. SUMMARY block at end of file accurately reports per-city counts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web → greshamoregon.gov, troutdaleoregon.gov, woodvillageor.gov, cityofmaywoodpark.com | Read-only fetch of public government photo data; no auth; no secrets |
| developer → Supabase Storage | Service-role PUT to politician_photos bucket via SUPABASE_SERVICE_ROLE_KEY |
| developer → Supabase Postgres | Service-role INSERT into essentials.politician_images via DATABASE_URL psycopg2 connection |
| Supabase Storage → end users | Photos served as public objects (politician_photos bucket is public read) — by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-09 | Spoofing | source URL substitution at scraped city sites | accept | URLs are from public official city pages; Task 1 runs a scrape verifying URL pattern matches expected `/globalassets/...` (Gresham), `/sites/g/files/vyhlif.../...webp` (Troutdale), or `/wp-content/uploads/...` (Wood Village). Manual visual UI verification post-upload would catch any face mismatch (covered by VALIDATION.md manual gate for CITIES-06). |
| T-84-10 | Tampering | image content modification by upstream host | accept | Public-domain government portraits; even if a city site were compromised the downstream impact is a display-only image — no PII processed. Storage upserts so re-running the script fully overwrites. |
| T-84-11 | Information disclosure | SUPABASE_SERVICE_ROLE_KEY leak via accidental log | mitigate | Key loaded only from .env (dotenv); never printed; temp script deleted by Task 2 after audit migration written; never committed to git (file is in `_tmp-*` naming convention that is git-ignored by Phase 83 precedent — confirm before committing). |
| T-84-12 | Tampering | wrong politician_images.type ('headshot' instead of 'default') causes silent UI invisibility | mitigate | Acceptance criteria require literal `'default'` in every INSERT (Task 1 live + Task 2 audit). Frontend filter is hard-coded to `type === 'default'` (Results.jsx, Profile.jsx). |
| T-84-13 | Tampering | image dimensions != 600x750 | mitigate | Task 1 verifies output buffer dimensions after sharp/PIL resize; fails loudly on mismatch. Acceptance criteria include a spot-check that downloads one uploaded image and verifies exact 600x750. |
| T-84-14 | Denial of service | source-site rate limiting or 403 | mitigate | Desktop User-Agent + Referer per source domain; 0.5s sleep between requests; per-official try/except so one failure does not halt the batch. Failed officials are documented in stdout + audit migration. |
| T-84-15 | Repudiation | live writes without audit trail | mitigate | Audit-only migration 247 (Task 2) captures all 31 outcomes (INSERTs + no-photo comments) as a written record per D-09, mirroring 245_multnomah_county_headshots.sql / 225_or_headshots.sql convention. |
| T-84-16 | Information disclosure | source URL points to image with embedded EXIF GPS / device metadata | accept | Source images are government portraits; PIL re-encoding to JPEG q90 strips EXIF by default. Verified by Phase 83 (Multnomah County) headshot run — no EXIF data carried through. |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — `Pillow`, `requests`, `psycopg2` already present in the Python environment used by C:/EV-Accounts/backend/scripts/portland-headshots-process.py and Phase 83 headshot runs. |
</threat_model>

<verification>
After Task 1 + Task 2 complete:

1. Photo upload coverage gate: SELECT external_id, COUNT(pi.id) AS n FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (<31 ids>) GROUP BY external_id ORDER BY external_id — confirm Gresham 7×1, Troutdale ~7×1 (modulo scrape gaps), Wood Village ~5×1 (modulo scrape gaps), Fairview 7×0, Maywood Park 5×0 (or per actual scrape).
2. Storage object count gate: list politician_photos bucket — count matches sum of INSERT blocks in migration 247.
3. Image size spot-check: download one storage object via curl and verify exactly 600×750 JPEG (via PIL Image.open or `file` command).
4. Audit migration 247 exists with AUDIT-ONLY header, no ledger entry, all 31 external_ids referenced.
5. Temporary upload script (_tmp-cities-headshots.py) has been deleted.
6. Manual UI verification (VALIDATION.md manual gate): load a Gresham, Troutdale, or Wood Village address in the app and confirm city councilor cards display headshots correctly cropped at 600×750 (4:5).
7. Phase 84-01 routing remains intact: re-run `npx tsx scripts/smoke-multnomah-cities.ts` — must still exit 0 (this plan does not modify routing data).
</verification>

<success_criteria>
- CITIES-06 satisfied: every official whose photo exists on their official city website (per D-08) has a politician_images row with type='default' and a 600×750 JPEG in Supabase Storage; every official with no available photo is explicitly documented in audit migration 247 with a `-- No photo found on official city website.` comment.
- Gresham coverage: 7/7 (full coverage from greshamoregon.gov).
- Fairview coverage: 0/7 (explicit no-photo comments per D-09 — verified absent on fairvieworegon.gov).
- Troutdale, Wood Village, Maywood Park coverage: per actual outcome of Task 1 runtime scrape; documented in audit migration regardless.
- Audit migration 247 follows AUDIT-ONLY convention (no ledger entry, no BEGIN/COMMIT wrapper, mirrors 245_multnomah_county_headshots.sql).
- Temporary upload script (_tmp-cities-headshots.py) has been deleted after audit migration is finalized.
- Phase 84-01 routing/seeding is not regressed by this plan.
</success_criteria>

<output>
Create `.planning/phases/84-multnomah-smaller-cities/84-02-SUMMARY.md` when done. Include: the per-city photo upload outcome (N/N counts), the full set of CDN URLs written to politician_images (mapped by external_id), the actual Troutdale + Wood Village + Maywood Park URLs discovered by runtime scrape, the source image dimensions and crop method applied per official, source-site fetch failures (if any) with explanation, post-run SQL gate output for each of the 5 cities, confirmation that _tmp-cities-headshots.py was deleted, and the actual SUMMARY block from the bottom of migration 247.
</output>
