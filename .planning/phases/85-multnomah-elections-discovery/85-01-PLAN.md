---
phase: 85-multnomah-elections-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/251_multnomah_elections.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-251.ts
autonomous: true
requirements:
  - ELECTIONS-01
  - ELECTIONS-02
tags:
  - sql-migration
  - elections
  - multnomah

must_haves:
  truths:
    - "essentials.races contains 2 race rows for OR 2026 General linked to Multnomah County offices (Chair + Commissioner District 2) per D-04"
    - "essentials.races contains 16 race rows for OR 2026 General linked to the 5 smaller Multnomah cities (Gresham 4 + Troutdale 3 + Fairview 4 + Wood Village 2 + Maywood Park 3) per D-05"
    - "Migration 251 is idempotent — re-running inserts zero additional race rows"
    - "Migration 251 ledger entry exists in supabase_migrations.schema_migrations (version='251')"
    - "All inserted race rows have primary_party=NULL and seats=1 per RESEARCH.md rules"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/251_multnomah_elections.sql"
      provides: "18 race row INSERTs + post-verify DO block + ledger entry"
      contains: "INSERT INTO essentials.races"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-251.ts"
      provides: "Apply script using pg.Pool that runs 251_multnomah_elections.sql and runs post-apply verification queries"
      exports: ["(default tsx script — no exports)"]
  key_links:
    - from: "migrations/251_multnomah_elections.sql"
      to: "essentials.elections row 'OR 2026 General' (state='OR')"
      via: "SELECT id INTO v_general_id"
      pattern: "name = 'OR 2026 General'"
    - from: "migrations/251_multnomah_elections.sql"
      to: "essentials.races (election_id, position_name) partial unique index"
      via: "ON CONFLICT clause"
      pattern: "ON CONFLICT \\(election_id, position_name\\) WHERE primary_party IS NULL DO NOTHING"
    - from: "scripts/_apply-migration-251.ts"
      to: "migrations/251_multnomah_elections.sql"
      via: "readFileSync"
      pattern: "251_multnomah_elections.sql"
---

<objective>
Seed 18 race rows into essentials.races for the OR 2026 General election (id=de10e3a7-f5c2-47e6-acd7-ee87be9413db; November General only per D-01, no primary race rows per D-03, 2-plan phase structure per D-08) covering Multnomah County Chair + Commissioner District 2 (per D-04) and council/mayor seats for Gresham, Troutdale, Fairview, Wood Village, and Maywood Park (per D-05). Implements requirements ELECTIONS-01 and ELECTIONS-02.

Purpose: Surfaces 2026 November General ballot races on the Elections page for Multnomah County residents and the 5 smaller incorporated cities.

Output: migrations/251_multnomah_elections.sql + scripts/_apply-migration-251.ts (both under C:/EV-Accounts/backend/). Migration is applied to production Supabase via the apply script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-multnomah-elections-discovery/85-CONTEXT.md
@.planning/phases/85-multnomah-elections-discovery/85-RESEARCH.md
@.planning/phases/85-multnomah-elections-discovery/85-PATTERNS.md
@.planning/phases/85-multnomah-elections-discovery/85-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 251 with 18 race rows + post-verify DO block + ledger entry</name>
  <files>C:/EV-Accounts/backend/migrations/251_multnomah_elections.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/240_portland_city_races.sql (canonical race row migration — copy DO $$ DECLARE structure, SELECT election_id pattern, INSERT column order, ON CONFLICT partial-index guard verbatim)
    - C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql (source of Multnomah County office_ids — verify against RESEARCH.md office_id table)
    - C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql (source of all 5 smaller-city office_ids — verify against RESEARCH.md office_id tables)
    - .planning/phases/85-multnomah-elections-discovery/85-RESEARCH.md (Verified 2026 Ballot Information section + Production DB Office IDs section + Migration 251 Structure Template + Post-Verification Pattern + Pitfalls 1-6)
    - .planning/phases/85-multnomah-elections-discovery/85-PATTERNS.md (Pattern Assignments for migrations/251_multnomah_elections.sql — full 18-row office_id → position_name list)
  </read_first>
  <action>
    Create new file C:/EV-Accounts/backend/migrations/251_multnomah_elections.sql following migration 240 structure exactly. File contents:

    1. Header comment: `-- Migration 251: Multnomah County + smaller city 2026 race rows — Phase 85 Plan 01` and total count comment (18 race rows: 2 county + 4 Gresham + 3 Troutdale + 4 Fairview + 2 Wood Village + 3 Maywood Park).

    2. Single DO $$ DECLARE block:
       - DECLARE `v_general_id UUID;`
       - SELECT id INTO v_general_id FROM essentials.elections WHERE name = 'OR 2026 General' AND state = 'OR'.
       - Optional guard (per D-02): if v_general_id IS NULL then RAISE EXCEPTION 'OR 2026 General election row missing'. The election exists in production per RESEARCH.md so this is a fail-fast guard, not an INSERT path.
       - 18 INSERT statements into essentials.races (id, election_id, office_id, position_name, primary_party, seats), each using gen_random_uuid() for id, v_general_id for election_id, the exact office_id UUIDs cast as ::uuid, primary_party=NULL, seats=1, and the position_name string from 85-PATTERNS.md.
       - Every INSERT must end with `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING` per D-07 (the WHERE clause is mandatory — partial index match per Pitfall 2).

    3. The 18 row tuples are (office_id, position_name) per D-06 / 85-PATTERNS.md:
       - 4b4821cf-9a97-4044-8132-706290d22e27 → 'Multnomah County Chair'
       - 3f01e9e8-bac6-4f0c-9793-ed14fbe2b22b → 'Multnomah County Commissioner District 2'
       - 4658f141-8cfd-4739-a959-23322a3182e7 → 'Gresham Mayor'
       - be91f6d5-b46f-4ca2-9605-a1eb62b47c01 → 'Gresham City Council Position 2'
       - 3c3cc31d-384e-4837-bd72-5c43faca3bc8 → 'Gresham City Council Position 4'
       - 4cf1b2b1-e005-48d7-b8a4-67040d8199cd → 'Gresham City Council Position 6'
       - 0b80a890-44f1-47be-bf6b-b17fc3eef9cb → 'Troutdale City Council Seat 1'
       - f291ef52-c368-472c-bce1-948e803eaf23 → 'Troutdale City Council Seat 2'
       - 10292aee-a4c1-4a74-88b2-b85e3ff40722 → 'Troutdale City Council Seat 3'
       - 0ff020a1-224f-4363-9c2f-8944b12ffcf2 → 'Fairview Mayor'
       - 15f9aaf4-3d4b-4f34-9213-cb3a8ca19e94 → 'Fairview City Council Position 4'
       - 15da3e65-a69f-429b-b0db-c4d450fb1c71 → 'Fairview City Council Position 5'
       - db927fb8-7627-4a22-b486-9888c22559b4 → 'Fairview City Council Position 6'
       - 8e42ac99-e2bb-4ea5-b8f6-02372ca0b4a6 → 'Wood Village City Council Seat 1'
       - c6c3259e-8883-4893-9fe4-50384d131f72 → 'Wood Village City Council Seat 2'
       - 23370dd5-9602-40b7-a820-74fd4b5055a1 → 'Maywood Park City Council Seat 1'
       - bec2352c-e2ff-46c9-bdda-bd5bf13ae254 → 'Maywood Park City Council Seat 2'
       - bbd553e7-1e67-4504-96dc-5f5c017eabd5 → 'Maywood Park City Council Seat 3'

    4. After the main DO block, append a second post-verify DO $$ block per 85-RESEARCH.md "Post-Verification Pattern for Migration 251": SELECT COUNT(*) into v_county_count for races joined to elections where name='OR 2026 General' and position_name ILIKE '%Multnomah County%'; RAISE EXCEPTION if v_county_count < 2. Same pattern with v_city_count using `position_name ~ '(Gresham|Troutdale|Fairview|Wood Village|Maywood Park)'`; RAISE EXCEPTION if v_city_count < 16. End with RAISE NOTICE summary.

    5. After the post-verify block, append the ledger entry: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('251') ON CONFLICT (version) DO NOTHING;`.

    Anti-patterns to avoid (per RESEARCH.md):
    - Do NOT add Auditor or Sheriff race rows (Pitfall 1 — no office rows exist for them).
    - Do NOT omit the `WHERE primary_party IS NULL` clause from ON CONFLICT (Pitfall 2).
    - Do NOT use the same position_name twice within Troutdale, Wood Village, or Maywood Park (Pitfall 3).
    - Do NOT INSERT the 'OR 2026 General' election row — it already exists (Pitfall 4); SELECT-only.
    - Do NOT seed Maywood Park Mayor/Council President (Pitfall 5) or Wood Village Mayor (Pitfall 6) — they are council-selected.
    - Do NOT do dynamic office_id lookups in the INSERT (RESEARCH.md anti-pattern) — use hardcoded UUIDs only.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend &amp;&amp; npx tsx scripts/_apply-migration-251.ts</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/251_multnomah_elections.sql exists.
    - File begins with `-- Migration 251` comment header.
    - File contains exactly 18 `INSERT INTO essentials.races` statements (one per ballot seat).
    - Every INSERT statement ends with the literal string `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`.
    - File contains `SELECT id INTO v_general_id FROM essentials.elections` with `WHERE name = 'OR 2026 General' AND state = 'OR'`.
    - File contains a post-verify DO block that RAISES EXCEPTION when county count &lt; 2 or city count &lt; 16.
    - File ends with `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('251')` followed by `ON CONFLICT (version) DO NOTHING`.
    - The 18 office_id UUIDs in the file match the 18 listed in this task's &lt;action&gt; verbatim.
    - The 18 position_name strings in the file match the 18 listed in this task's &lt;action&gt; verbatim.
    - File does NOT contain any reference to County Auditor, County Sheriff, Mayor Akers, Mayor Rios-Campos, Council President Bussema, or any other office NOT in the 18-row list.
    - File does NOT contain `INSERT INTO essentials.elections` (election row exists per D-02; SELECT-only).
  </acceptance_criteria>
  <done>
    251_multnomah_elections.sql exists with all 18 race row INSERTs, the post-verify DO block, and the ledger entry. The file conforms to the migration 240 canonical pattern. (Apply will be exercised in Task 2.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write apply script _apply-migration-251.ts + apply migration to production + verify counts</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-251.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-244.ts (canonical apply script analog — copy imports, pg.Pool construction, readFileSync pattern, try/catch/finally, post-apply verification queries)
    - C:/EV-Accounts/backend/migrations/251_multnomah_elections.sql (the migration file produced in Task 1 — confirm filename + ledger version match)
    - .planning/phases/85-multnomah-elections-discovery/85-PATTERNS.md (scripts/_apply-migration-251.ts pattern block — full Apply + verify pattern)
    - .planning/phases/85-multnomah-elections-discovery/85-RESEARCH.md (Environment Availability section + Migration Ledger State — confirms DATABASE_URL is in .env)
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/scripts/_apply-migration-251.ts. The script follows _apply-migration-244.ts structure exactly. Required content:

    1. Imports: `import 'dotenv/config'`; `import { Pool } from 'pg'`; `import { readFileSync } from 'fs'`; `import path from 'path'`.

    2. Top-level async IIFE or `main()` function. Construct `const pool = new Pool({ connectionString: process.env['DATABASE_URL'], ssl: { rejectUnauthorized: false } })`.

    3. Load SQL: `const sql = readFileSync(path.join(process.cwd(), 'migrations', '251_multnomah_elections.sql'), 'utf8')`.

    4. Apply: inside try block, `await pool.query(sql)` then `console.log('Migration 251 applied successfully')`.

    5. Post-apply verification queries (three queries, each logging count vs expected):
       - Query 1: count rows in essentials.races JOIN essentials.elections where name='OR 2026 General' and position_name ILIKE '%Multnomah County%' — log `County race rows: N (expected 2)`.
       - Query 2: count rows where position_name ~ '(Gresham|Troutdale|Fairview|Wood Village|Maywood Park)' — log `City race rows: N (expected 16)`.
       - Query 3: count supabase_migrations.schema_migrations WHERE version = '251' — log `Ledger entry: N (expected 1)`.

    6. catch (e: any): `console.error('Error applying migration 251:', e.message)` and `process.exit(1)`.

    7. finally: `await pool.end()`.

    After writing the file, RUN the script via the verify command. This applies migration 251 to production. The post-verify DO block inside the SQL will RAISE EXCEPTION if counts are wrong; the apply script's verification queries log the same counts after.

    Confirm via the script output that:
    - County race rows: 2 (expected 2)
    - City race rows: 16 (expected 16)
    - Ledger entry: 1 (expected 1)

    If re-applied (idempotency check), the ON CONFLICT DO NOTHING clauses make additional INSERTs no-ops; row counts remain 2 / 16 / 1.

    The script is invoked from the C:/EV-Accounts/backend/ working directory (path.join uses process.cwd()), matching the convention of all prior _apply-migration-*.ts scripts.

    Important: Per memory feedback "no git in EV-Accounts" — do NOT run git commands in C:/EV-Accounts. The migration SQL and apply script are part of the EV-Accounts repo (separately managed).
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend &amp;&amp; npx tsx scripts/_apply-migration-251.ts</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/_apply-migration-251.ts exists.
    - File imports `Pool` from `pg` (NOT `Client`).
    - File imports `'dotenv/config'`.
    - File contains `readFileSync(path.join(process.cwd(), 'migrations', '251_multnomah_elections.sql'`.
    - File contains `await pool.query(sql)`.
    - File runs three verification queries with logged outputs labelled `County race rows`, `City race rows`, and `Ledger entry`.
    - File contains `catch (e: any)` block that calls `process.exit(1)` on error.
    - File ends with `await pool.end()` in a `finally` block.
    - Running `npx tsx scripts/_apply-migration-251.ts` in C:/EV-Accounts/backend exits 0.
    - Script output contains the literal string `County race rows: 2`.
    - Script output contains the literal string `City race rows: 16`.
    - Script output contains the literal string `Ledger entry: 1`.
    - Re-running the script (idempotency check) exits 0 and counts remain 2 / 16 / 1 (no duplicates inserted).
  </acceptance_criteria>
  <done>
    Migration 251 is applied to production Supabase. essentials.races contains 2 Multnomah County race rows + 16 smaller-city race rows for OR 2026 General. Ledger entry version='251' exists. Re-run is a no-op.
  </done>
</task>

</tasks>

<verification>
After both tasks pass:

- SQL gate (run via Supabase MCP or pg client):
  ```sql
  SELECT COUNT(*) FROM essentials.races r
  JOIN essentials.elections e ON e.id = r.election_id
  WHERE e.name = 'OR 2026 General'
    AND (r.position_name ILIKE '%Multnomah County%'
         OR r.position_name ~ '(Gresham|Troutdale|Fairview|Wood Village|Maywood Park)');
  -- expected: 18
  ```
- Ledger gate: `SELECT 1 FROM supabase_migrations.schema_migrations WHERE version='251'` returns 1 row.
- Idempotency gate: re-running the apply script results in the same row counts (no duplicate INSERTs).
</verification>

<success_criteria>
- migrations/251_multnomah_elections.sql exists and matches the 240 canonical pattern.
- scripts/_apply-migration-251.ts exists and matches the 244 canonical pattern.
- essentials.races has 2 county + 16 city rows linked to OR 2026 General.
- All 18 inserted rows satisfy: primary_party IS NULL, seats=1, office_id references the live DB UUIDs from RESEARCH.md.
- Migration ledger contains version '251'.
- Plan 02 can now proceed to add the discovery_jurisdictions row.
</success_criteria>

<output>
Create `.planning/phases/85-multnomah-elections-discovery/85-01-SUMMARY.md` when done. Include: actual file paths created, apply script output (county / city / ledger counts), confirmation that idempotency check returned the same counts.
</output>
