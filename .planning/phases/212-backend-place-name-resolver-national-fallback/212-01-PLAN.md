---
phase: 212-backend-place-name-resolver-national-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1377_location_search_trgm_indexes.sql
  - C:/EV-Accounts/backend/migrations/1378_gazetteer_places_counties.sql
autonomous: true
requirements: [RSLV-01, RSLV-02, RSLV-05, RSLV-06]
must_haves:
  truths:
    - "Nationwide US House CD boundaries (mtfcc=G5200) are confirmed present (>=435) in the live DB before any dependent work, converting D-02's assumption into a verified fact"
    - "Migration 1377 authored: GIN trigram indexes on governments.name + geofence_boundaries.name mirroring migration 040"
    - "Migration 1378 authored: gazetteer_places + gazetteer_counties reference tables with GIN trigram indexes on their name columns"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1377_location_search_trgm_indexes.sql"
      provides: "GIN trgm indexes for the place-name resolver query"
      contains: "gin_trgm_ops"
    - path: "C:/EV-Accounts/backend/migrations/1378_gazetteer_places_counties.sql"
      provides: "Nationwide Gazetteer reference tables + their trgm indexes"
      contains: "gazetteer_places"
  key_links:
    - from: "1377 index expression"
      to: "migration 040 f_unaccent(lower(name)) idiom"
      via: "identical GIN expression so the resolver query hits the index"
      pattern: "f_unaccent\\(lower\\(.*name.*\\)\\) extensions.gin_trgm_ops"
---

<objective>
Establish the DB foundation for the place-name resolver: (1) verify — do NOT re-ingest — the nationwide US House congressional-district data that D-02 assumes is new work but that RESEARCH.md shows already shipped in Phase 116/125; and (2) author the two migrations the resolver needs (trigram indexes on existing name columns + new nationwide Gazetteer reference tables). This plan authors SQL only — no live-DB apply happens here (that is Plan 03, gated as [BLOCKING]).

Purpose: Convert D-02's stale "ingest from scratch" premise into a verified fact via a cheap pre-flight audit, and ship the resolver's index+table DDL in the same phase as the query (Pitfall 4).
Output: Pre-flight audit result recorded; two authored migration files (1377, 1378).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md

<interfaces>
<!-- Verbatim idiom to mirror. Extracted from the live backend repo. Executor uses these directly. -->

Migration 040 GIN index idiom (C:/EV-Accounts/backend/migrations/040_pg_trgm_search.sql):
- Extensions pg_trgm + unaccent already live in schema `extensions`; do NOT re-create them.
- IMMUTABLE wrapper `public.f_unaccent(text)` already exists; reuse verbatim.
- Index shape: CREATE INDEX IF NOT EXISTS <name> ON <schema.table> USING GIN (public.f_unaccent(lower(<col>)) extensions.gin_trgm_ops);

Confirmed schema facts (from direct repo reads this session):
- essentials.governments columns in use: geo_id, id (uuid PK), name, type, state. governments.state = UPPERCASE 2-letter USPS abbrev (e.g. 'AZ'). NO unique constraint on geo_id (join on id, never geo_id).
- essentials.geofence_boundaries columns in use: geo_id, name, state (FIPS, e.g. '04'), mtfcc, geometry.
- Migration directory in use through 1376 = C:/EV-Accounts/backend/migrations/ . Next numbers: 1377, 1378.
- Gazetteer record layout (pipe-delimited): Places = USPS|GEOID|ANSICODE|NAME|LSAD|FUNCSTAT|ALAND|AWATER|ALAND_SQMI|AWATER_SQMI|INTPTLAT|INTPTLONG ; Counties = same minus LSAD|FUNCSTAT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 DB pre-flight audit — verify (not re-ingest) nationwide CD/House data</name>
  <files>(read-only inline psql audit against the live DB — no repo files created; counts recorded in 212-01-SUMMARY.md)</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-national-house-districts.ts (the already-shipped per-state cd119 loader — confirms Census publishes NO single nationwide cd119 file)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md §Summary, §"Pitfall 1", §"Anti-Patterns to Avoid" (the D-02 verify-not-reingest correction)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md §D-02
  </read_first>
  <action>
    Run an inline read-only psql audit against the live DB (use the backend's existing DATABASE_URL / connection env — gsd-executor has NO Supabase MCP, so this is a scripted psql/curl check, not an MCP call). Count three facts that D-02's dependent plans rely on: (a) `SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc = 'G5200'` — expect >=435; (b) `SELECT count(*) FROM essentials.districts WHERE district_type = 'NATIONAL_LOWER' AND tiger_geoid IS NOT NULL` — expect >=435; (c) confirm the general-purpose overlap path uses G5200 (current officeholder vintage) NOT G5200V26 (2026 redistricting vintage, excluded per geoIdGuard.ts). Record the three counts verbatim in the plan SUMMARY. If (a) or (b) come up short (<435), STOP and surface a blocking finding that a genuine nationwide cd119 re-load task is required (via the existing per-state load-national-house-districts.ts, NOT a fabricated single-file loader) — do NOT author a re-ingest task blindly. If counts confirm completeness (expected), record "D-02 verified: CD data complete, no re-ingest" and proceed.
  </action>
  <verify>
    <automated>MISSING — run inline: psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5200'" (expect >=435) and psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.districts WHERE district_type='NATIONAL_LOWER' AND tiger_geoid IS NOT NULL" (expect >=435)</automated>
  </verify>
  <acceptance_criteria>
    - psql count of geofence_boundaries WHERE mtfcc='G5200' returns >= 435
    - psql count of districts WHERE district_type='NATIONAL_LOWER' AND tiger_geoid IS NOT NULL returns >= 435
    - Both counts recorded verbatim in the SUMMARY with a one-line "D-02 verified — no re-ingest" or "SHORTFALL — reload required" determination
  </acceptance_criteria>
  <done>Both CD/House row counts recorded and >=435 (or an explicit shortfall finding surfaced); D-02's mechanism decided as verify-only, not re-ingest.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 1377 (resolver trgm indexes) + migration 1378 (Gazetteer tables)</name>
  <files>C:/EV-Accounts/backend/migrations/1377_location_search_trgm_indexes.sql, C:/EV-Accounts/backend/migrations/1378_gazetteer_places_counties.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/040_pg_trgm_search.sql (the GIN + f_unaccent index idiom to mirror EXACTLY)
    - C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql (recent migration file header/comment conventions + the "no unique constraint on governments.geo_id" note)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md §"Code Examples" (index idiom), §"Recommended Project Structure", Assumptions Log A2 (Gazetteer column layout)
  </read_first>
  <action>
    Author two SQL files in C:/EV-Accounts/backend/migrations/ (author only — do NOT apply to any DB in this plan). Migration 1377_location_search_trgm_indexes.sql: create `idx_governments_name_trgm` on essentials.governments and `idx_geofence_boundaries_name_trgm` on essentials.geofence_boundaries, both USING GIN (public.f_unaccent(lower(name)) extensions.gin_trgm_ops), each guarded with IF NOT EXISTS; do NOT re-create pg_trgm/unaccent/f_unaccent (already live since 040). Migration 1378_gazetteer_places_counties.sql: CREATE TABLE IF NOT EXISTS essentials.gazetteer_places (geo_id text PRIMARY KEY, name text NOT NULL, state text NOT NULL, lsad text, aland_sqmi numeric, intptlat double precision, intptlong double precision) and essentials.gazetteer_counties (geo_id text PRIMARY KEY, name text NOT NULL, state text NOT NULL, aland_sqmi numeric, intptlat double precision, intptlong double precision); then GIN trgm indexes `idx_gazetteer_places_name_trgm` and `idx_gazetteer_counties_name_trgm` on each table's public.f_unaccent(lower(name)). The `state` columns hold the Gazetteer USPS 2-letter abbrev (uppercase). Add a top-of-file comment noting these are STRUCTURAL migrations applied live in Plan 03, and that the column set is the canonical contract the Plan 02 ingest script writes to.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && node -e "const fs=require('fs');for(const f of ['migrations/1377_location_search_trgm_indexes.sql','migrations/1378_gazetteer_places_counties.sql']){const s=fs.readFileSync(f,'utf8');if(!/gin_trgm_ops/.test(s))throw new Error('missing gin_trgm_ops in '+f)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 1377 contains two CREATE INDEX ... USING GIN (public.f_unaccent(lower(name)) extensions.gin_trgm_ops) statements (governments + geofence_boundaries), IF NOT EXISTS guarded
    - 1377 does NOT contain CREATE EXTENSION or CREATE OR REPLACE FUNCTION public.f_unaccent (grep -v '^--' file | grep -ci "create extension\|create or replace function public.f_unaccent" == 0)
    - 1378 creates gazetteer_places (geo_id PK, name, state, lsad, aland_sqmi, intptlat, intptlong) + gazetteer_counties (same minus lsad) + two GIN trgm name indexes
    - Both files live in C:/EV-Accounts/backend/migrations/ named exactly 1377_* and 1378_*
  </acceptance_criteria>
  <done>Two authored, IF-NOT-EXISTS-guarded migration files exist; 1377 reuses (never re-creates) the 040 extension/wrapper; 1378 defines the canonical Gazetteer schema + trgm indexes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → live Postgres | DDL migrations authored here will be applied to the production DB in Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-01 | Tampering | migration 1377/1378 DDL | mitigate | IF NOT EXISTS on every CREATE; no destructive DROP; indexes only add, tables only create — idempotent re-apply safe |
| T-212-02 | Denial of Service | resolver query without an index | mitigate | GIN trgm indexes authored in the SAME phase as the query (Pitfall 4); verified index-scan in Plan 05 EXPLAIN |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO new packages (RESEARCH.md Package Legitimacy Audit: N/A); no install task, no slopcheck needed |
</threat_model>

<verification>
- psql pre-flight counts both >=435 (G5200 boundaries + NATIONAL_LOWER tiger_geoid)
- 1377 + 1378 exist, contain gin_trgm_ops, and 1377 adds no extension/wrapper re-creation
</verification>

<success_criteria>
- D-02 converted from assumption to verified fact (no blind re-ingest authored)
- Resolver index DDL (1377) + Gazetteer table DDL (1378) authored and ready for the Plan 03 live apply
</success_criteria>

<output>
Create `.planning/phases/212-backend-place-name-resolver-national-fallback/212-01-SUMMARY.md` when done, recording the three pre-flight counts verbatim.
</output>
