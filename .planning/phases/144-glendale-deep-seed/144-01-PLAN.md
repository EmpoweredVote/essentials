---
phase: 144-glendale-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/902_glendale_reconcile.sql
autonomous: true
requirements: [GLEN-01]

must_haves:
  truths:
    - "D-01/D-02: this phase reconciles the EXISTING partial Glendale seed (UPDATE-not-INSERT on gov a7433437) — no greenfield rebuild, no duplicate offices/external_ids"
    - "Glendale government row carries geo_id '0630000' (no longer NULL), guarded WHERE geo_id IS NULL"
    - "The empty duplicate chamber c019a553 (external_id -200687, 0 offices) is DELETED after an inline assert that it has 0 offices; exactly ONE 'Glendale City Council' chamber (survivor 771727ec, external_id 10450) remains under the gov"
    - "Ardy Kassakhian's existing council seat (office b1c10c09, external_id 686339) is flagged Mayor (title 'Councilmember' → 'Mayor', D-08/D-09); the other 4 seats remain title 'Councilmember'"
    - "feedback_section_split_check SQL returns 0 rows for Glendale after consolidation (D-05)"
    - "This reconcile is a STRUCTURAL migration (902) that registers in supabase_migrations.schema_migrations normally (D-04)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/902_glendale_reconcile.sql"
      provides: "Idempotent geo_id backfill + empty-duplicate-chamber delete + Mayor flag for Glendale"
      contains: "c019a553"
  key_links:
    - from: "essentials.governments (a7433437-341a-48e7-907e-a61318954f0a)"
      to: "geo_id '0630000'"
      via: "UPDATE ... WHERE id = 'a7433437-341a-48e7-907e-a61318954f0a' AND geo_id IS NULL"
      pattern: "geo_id\\s*=\\s*'0630000'"
    - from: "essentials.chambers (c019a553, external_id -200687, empty duplicate)"
      to: "deleted (asserted 0 offices first)"
      via: "DO $$ assert 0 offices $$ then DELETE FROM essentials.chambers WHERE id = 'c019a553...'"
      pattern: "DELETE FROM essentials\\.chambers"
    - from: "essentials.offices (Kassakhian seat b1c10c09, politician external_id 686339)"
      to: "title flagged 'Mayor' (rotational role on a council seat)"
      via: "UPDATE offices SET title='Mayor' WHERE id='b1c10c09...'"
      pattern: "title\\s*=\\s*'Mayor"
---

<objective>
Reconcile the existing partial, duplicate-chamber Glendale seed in a single idempotent structural
migration (902). This is the foundation wave — every later wave (roster, headshots, stances) assumes a
clean, unambiguous, single-chamber structure with the rotational Mayor correctly flagged.

Glendale's Wave 1 is SIMPLER than Santa Clarita's 894: there is NO member retirement here (Najarian's
retirement belongs to Wave 2, not Wave 1 — orchestrator decision #1), and the duplicate chamber
c019a553 is EMPTY (0 offices, 0 members) — no member migration is required before deletion.

Three fixes, all DB-verified against the live state on 2026-06-19:
1. Backfill `essentials.governments.geo_id = '0630000'` on gov a7433437 (currently NULL), guarded
   `WHERE geo_id IS NULL` (D-01 / D-02 / GLEN-01).
2. DELETE the empty duplicate chamber c019a553 (external_id -200687, "Glendale City Council", 0 offices).
   Target by UUID only — both chambers share the name (D-03). An inline assert MUST confirm 0 offices
   under c019a553 before the DELETE (Pitfall 5); survivor is 771727ec (external_id 10450, official_count 5).
3. UPDATE Ardy Kassakhian's existing council seat (office b1c10c09, external_id 686339) title
   'Councilmember' → 'Mayor' (D-08/D-09). NO separate LOCAL_EXEC district / Mayor chamber / Mayor office.
   The other 4 seats stay 'Councilmember'.

Purpose: A clean structural base. Duplicate 'Glendale City Council' chamber names cause split-section
render bugs; NULL geo_id breaks routing; the rotational Mayor must be flagged on a real council seat.
Output: Migration 902 applied to production; all post-verification queries green; split-section check
returns 0 rows for Glendale.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally (it is
NOT a stance/headshot audit-only migration). Ledger MAX is 895; next structural is 902 (896–901 were
SC audit-only headshot/stance files, not registered).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-glendale-deep-seed/144-CONTEXT.md
@.planning/phases/144-glendale-deep-seed/144-RESEARCH.md
@.planning/phases/144-glendale-deep-seed/144-VALIDATION.md

<interfaces>
<!-- DB-verified live values (2026-06-19) the executor uses directly. Task 1 confirms them with a
     read-only pre-flight query before any write. -->

Government row:
  id     = 'a7433437-341a-48e7-907e-a61318954f0a'
  name   = 'City of Glendale, California, US'
  geo_id = NULL  → set to '0630000'   (state already 'CA')

Two chambers, BOTH named 'Glendale City Council':
  KEEP   771727ec  external_id 10450    (SURVIVOR — 5 'Councilmember' offices, official_count 5)
  DELETE c019a553  external_id -200687  (empty duplicate — 0 offices, official_count NULL)
  (chambers.slug is a GENERATED column — NEVER include slug in any write)

Survivor chamber 771727ec offices (all currently titled 'Councilmember'):
  615de18c  Elen Asatryan    686337
  0b17284a  Daniel Brotman   686340
  c728231c  Vartan Gharpetian 686336
  b1c10c09  Ardy Kassakhian  686339   → UPDATE title to 'Mayor' (current rotational Mayor, D-09)
  c6f4e77d  Ara Najarian     -700100  (retired in Wave 2, NOT here)

Rotational Mayor (D-08/D-09): Kassakhian (686339) selected Mayor April 2026. Flag by title on his
  EXISTING seat (office b1c10c09). NO separate LOCAL_EXEC row. Other 4 seats stay 'Councilmember'.

Empty-duplicate-chamber pre-delete assert (Pitfall 5):
  DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='c019a553-...') > 0
  THEN RAISE EXCEPTION 'Chamber c019a553 has offices'; END IF; END $$;  before the DELETE.

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after
  consolidation; expect 0 rows for Glendale. (5 OTHER LA-area cities have pre-existing split-section
  defects — out of scope here.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state (gov, two chambers, Kassakhian seat)</name>
  <files>C:/EV-Accounts/backend/migrations/902_glendale_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql (the direct STRUCTURAL template — geo_id backfill + FK-safe chamber teardown + idempotent guard patterns)
    - .planning/phases/143-santa-clarita-deep-seed/143-01-SUMMARY.md (exact reconcile migration shape + post-verification query set)
    - .planning/phases/144-glendale-deep-seed/144-RESEARCH.md (§Live-Data Findings 2 DB State, §Common Pitfalls 5, §Reusable Patterns Wave 1)
    - .planning/phases/144-glendale-deep-seed/144-CONTEXT.md (§decisions D-01..D-05, D-08, D-09)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA' uppercase)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE, run read-only confirmation queries via mcp__supabase-local
    execute_sql (= production) or psql -f using C:/EV-Accounts/backend/.env DATABASE_URL:
    (a) confirm governments id a7433437-341a-48e7-907e-a61318954f0a has geo_id NULL and state 'CA';
    (b) confirm exactly two chambers under that government both named 'Glendale City Council' — id
    771727ec (external_id 10450, 5 offices, official_count 5, SURVIVOR) and id c019a553 (external_id
    -200687, 0 offices, EMPTY DUPLICATE);
    (c) confirm c019a553 has exactly 0 rows in essentials.offices (SELECT COUNT(*) WHERE chamber_id=
    'c019a553...') — this is the precondition for a clean DELETE (Pitfall 5);
    (d) confirm office b1c10c09 belongs to Kassakhian (external_id 686339) in chamber 771727ec and its
    current title is 'Councilmember'; capture the current titles of the other 4 survivor offices
    (expect all 'Councilmember');
    (e) confirm Najarian (external_id -700100, office c6f4e77d) is still seated — do NOT touch him in
    this wave (he is retired in Wave 2).
    If ANY precondition does not match RESEARCH.md (especially: c019a553 has >0 offices, or a third
    chamber exists, or b1c10c09 is not Kassakhian's seat), STOP and report the discrepancy — do not
    write a migration against a moved target.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id, state FROM essentials.governments WHERE id='a7433437-341a-48e7-907e-a61318954f0a';" — returns one row, geo_id NULL, state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - All 5 pre-flight checks executed; results captured in the executor's notes
    - Two chambers confirmed sharing the name 'Glendale City Council' with ids 771727ec (KEEP, external_id 10450) and c019a553 (DELETE, external_id -200687)
    - c019a553 confirmed to have exactly 0 offices (`SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='c019a553...'` → 0)
    - Office b1c10c09 confirmed as Kassakhian's (686339) seat in 771727ec with current title 'Councilmember'
    - Najarian (-700100, office c6f4e77d) confirmed seated and explicitly NOT in this wave's scope
    - Any mismatch vs RESEARCH.md is surfaced and execution paused (no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches RESEARCH.md; c019a553 confirmed empty; Kassakhian's seat id confirmed; the geo_id backfill, chamber delete, and Mayor flag are safe to author.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 902 (geo_id backfill + empty-chamber delete + Mayor flag)</name>
  <files>C:/EV-Accounts/backend/migrations/902_glendale_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql (copy the idempotent geo_id backfill + chamber DELETE + title UPDATE patterns; Glendale is simpler — no member retirement, empty duplicate)
    - .planning/phases/143-santa-clarita-deep-seed/143-01-SUMMARY.md (exact post-verification query set + idempotency guard shapes)
    - .planning/phases/144-glendale-deep-seed/144-RESEARCH.md (§Reusable Patterns Wave 1, §Common Pitfalls 5, §Validation Architecture Wave 1 checks)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/902_glendale_reconcile.sql, all statements idempotent, using
    the captured ids from Task 1, in this order:
    (1) UPDATE essentials.governments SET geo_id='0630000' WHERE id='a7433437-341a-48e7-907e-a61318954f0a'
        AND geo_id IS NULL (D-01 / D-02 / GLEN-01).
    (2) Inline assert that the duplicate chamber is empty: a DO $$ block that RAISEs EXCEPTION if
        (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='c019a553-...') > 0 (Pitfall 5).
    (3) DELETE essentials.chambers WHERE id='c019a553-...' (external_id -200687). Target the duplicate
        by its UUID ONLY, never by name (both chambers share 'Glendale City Council').
    (4) UPDATE essentials.offices SET title='Mayor' WHERE id='b1c10c09-...' (Kassakhian's existing seat,
        external_id 686339) AND title <> 'Mayor' (D-08/D-09). Do NOT create a separate LOCAL_EXEC
        district/chamber/Mayor office; do NOT touch the other 4 offices' titles ('Councilmember').
    Do NOT touch the slug column anywhere (GENERATED). End the file with the post-verification queries:
    geo_id check + a chamber-name listing under gov a7433437 (expect exactly one 'Glendale City Council',
    the survivor 771727ec) + a Kassakhian-office-title check + the feedback_section_split_check SQL
    (project memory; expect 0 rows for Glendale) + a survivor official_count=5 check. Apply via
    mcp__supabase-local execute_sql or psql -f. This registers in schema_migrations normally (structural
    migration 902).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id FROM essentials.governments WHERE id='a7433437-341a-48e7-907e-a61318954f0a';" returns '0630000' AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.chambers WHERE government_id='a7433437-341a-48e7-907e-a61318954f0a' AND name='Glendale City Council';" returns 1</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='a7433437...'` → '0630000'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='a7433437...' AND name='Glendale City Council'` → 1 (not 0, not 2)
    - `SELECT COUNT(*) FROM essentials.chambers WHERE external_id='-200687'` → 0 (duplicate c019a553 deleted)
    - `SELECT title FROM essentials.offices WHERE id='b1c10c09...'` → 'Mayor'; the other 4 survivor offices remain 'Councilmember'
    - Survivor chamber 771727ec (external_id 10450) has official_count=5 and its 5 offices intact
    - feedback_section_split_check SQL returns 0 rows for Glendale (D-05)
    - Migration 902 registered in supabase_migrations.schema_migrations (structural)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
  </acceptance_criteria>
  <done>Migration 902 applied; one 'Glendale City Council' chamber remains; geo_id 0630000 set; Kassakhian flagged Mayor; split-section check 0 rows; re-running the file is a no-op.</done>
</task>

</tasks>

<threat_model>
This is a DB-seeding phase with NO new external attack surface. No untrusted user input crosses any
boundary — migration SQL is applied via service-role/operator (psql/MCP) directly against the internal
production Supabase DB. The real threat surface is accidental data corruption, mitigated by idempotent
guards + an inline empty-chamber assert + post-migration SQL verification.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator (psql/MCP) directly against production Supabase. No untrusted user input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01 | Tampering | Chamber DELETE targets the wrong chamber (both share name 'Glendale City Council') | mitigate | DELETE keyed on UUID 'c019a553…' ONLY, never on name; post-verify exactly one 'Glendale City Council' chamber remains and survivor 771727ec persists |
| T-144-02 | Tampering | Duplicate chamber DELETE silently orphans offices (if it were non-empty) | mitigate | Inline DO $$ assert RAISEs EXCEPTION if c019a553 has >0 offices before the DELETE (Pitfall 5); Task 1 pre-flight confirms 0 offices |
| T-144-03 | Tampering | geo_id overwritten on a re-run or wrong gov | mitigate | UPDATE guarded by id-scope + WHERE geo_id IS NULL; second apply = 0 rows |
| T-144-04 | Tampering | Mayor flag applied to the wrong seat or a LOCAL_EXEC row created | mitigate | title UPDATE keyed on office UUID b1c10c09 (Kassakhian) only, guarded title <> 'Mayor'; no LOCAL_EXEC/Mayor chamber created (D-08) |
| T-144-05 | Tampering | Non-idempotent migration applied twice corrupts state | mitigate | All writes guarded (geo_id IS NULL, id-scoped delete, title <> target); second apply = 0 rows changed |
| T-144-06 | Tampering | Accidental write to production without verifying pre-state | mitigate | Task 1 read-only pre-flight confirms live state matches RESEARCH before any write; STOP-on-mismatch |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 902:
1. gov geo_id = '0630000'
2. exactly 1 chamber 'Glendale City Council' under gov a7433437; duplicate c019a553 (-200687) gone
3. Kassakhian office b1c10c09 title = 'Mayor'; other 4 seats = 'Councilmember'
4. survivor chamber 771727ec official_count = 5
5. feedback_section_split_check SQL → 0 rows for Glendale
6. migration 902 registered in schema_migrations
</verification>

<success_criteria>
All three structural defects fixed and independently verified; the empty duplicate chamber is gone with
an inline assert proving it was empty; the rotational Mayor is flagged on Kassakhian's seat; migration
902 is idempotent and registered; the Glendale structure is a single unambiguous 'Glendale City Council'
chamber ready for roster completion in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/144-glendale-deep-seed/144-01-SUMMARY.md` when done.
</output>
