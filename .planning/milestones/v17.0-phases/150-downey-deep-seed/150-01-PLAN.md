---
phase: 150-downey-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/985_downey_reconcile.sql
autonomous: true
requirements: [DWNY-01]

must_haves:
  truths:
    - "This phase RECONCILES the EXISTING partial, dual-chamber, structurally-defective Downey seed (geo_id backfill + merge the two duplicate 'City Council' chambers + relabel the wrongly-'At-Large' council rows to 'District 1'..'District 5' + collapse the stray LOCAL_EXEC row into a normal District 2 seat with title='Mayor' + fix Trujillo's corrupted last_name) — NOT a greenfield rebuild. Downey is a CVRA general-law city: 5 single-member districts with a ROTATIONAL (council-selected) mayor (Palmdale 146 model — NOT a directly-elected mayor). A Wave-1 STOP-on-drift pre-flight re-confirms gov 1a31cf01 geo_id NULL/empty, BOTH chamber UUIDs (survivor candidate 7cb8a90c official_count 5 + orphan a30fd533 official_count NULL holding Trujillo), the 6 offices + members/ext_ids, BOTH link directions, the At-Large row count, Sosa's stray LOCAL_EXEC office cc3bacd0, Trujillo's corrupted last_name='-', the live schema_migrations MAX + on-disk MAX, and STOPs on drift"
    - "The Downey government row 'City of Downey, California, US' (id 1a31cf01-5e05-46d9-88f3-6b94aaa0c607) carries geo_id '0619766' (no longer NULL/empty), guarded WHERE (geo_id IS NULL OR geo_id = ''); state already 'CA'"
    - "move-then-delete (UUID-targeted): the orphan chamber a30fd533's ONE office — Trujillo 2afa4fd2 — is MOVED into the survivor chamber 7cb8a90c (UPDATE chamber_id WHERE chamber_id='a30fd533…') FIRST, an inline assert proves a30fd533 then has 0 offices, THEN a30fd533 is DELETEd (target by UUID — both chambers share the name 'City Council' / slug downey-city-council) — end state is ONE 'City Council' chamber (7cb8a90c) with exactly 6 offices (the structural 6→5 roster reconcile is Plan 02)"
    - "by-district relabel (Palmdale 146 pattern): the wrongly-'At-Large' council LOCAL district rows are relabeled to their occupant's real district per RESEARCH §D-03 — D1 (Ortiz seat, created Plan 02), D2 (Sosa, office cc3bacd0, ALSO rotational Mayor), D3 (Pemberton, office 3718d3c0), D4 (Frometa, office 6fa79f0e), D5 (Trujillo, office 2afa4fd2) — keeping geo_id '0619766', state 'CA'; the pre-flight office→district map resolves each At-Large UUID before relabel"
    - "ROTATIONAL MAYOR collapsed into District 2 (Palmdale 146 / 146-CONTEXT D-08, NOT Pasadena LOCAL_EXEC): Sosa's stray LOCAL_EXEC district row (the district referenced by office cc3bacd0) is converted to district_type='LOCAL' and relabeled 'District 2'; office cc3bacd0 gets title='Mayor'; NO separate Mayor office/district/LOCAL_EXEC row survives; the other 4 council seats keep title='Councilmember'"
    - "any missing district row (e.g. 'District 1' for Ortiz) is CREATED guarded WHERE NOT EXISTS (LOCAL, geo_id '0619766', state 'CA'); after this wave exactly 5 LOCAL district rows exist for geo_id '0619766' ('District 1'..'District 5') and ZERO LOCAL_EXEC rows under gov 1a31cf01"
    - "Trujillo's corrupted name is fixed: the politician row for ext_id -201200 (currently last_name='-' with the full name in first_name) is corrected to first_name='Mario', last_name='Trujillo' (matching the -201xxx convention confirmed in pre-flight), guarded WHERE last_name='-' OR last_name IS NULL"
    - "feedback_section_split_check SQL returns 0 rows for Downey (geo_id '0619766') after consolidation"
    - "this reconcile (geo_id backfill + chamber merge + district relabel/create + rotational-mayor collapse + Trujillo name fix) is a STRUCTURAL migration (985) that registers in supabase_migrations.schema_migrations normally"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/985_downey_reconcile.sql"
      provides: "Idempotent geo_id backfill + move-Trujillo-then-delete-orphan-chamber-a30fd533 + relabel At-Large council rows to District 1-5 + collapse Sosa LOCAL_EXEC into District 2 (title='Mayor') + create any missing District row + Trujillo name fix"
      contains: "a30fd533"
  key_links:
    - from: "essentials.governments (id 1a31cf01-5e05-46d9-88f3-6b94aaa0c607)"
      to: "geo_id '0619766'"
      via: "UPDATE ... WHERE id = '1a31cf01-5e05-46d9-88f3-6b94aaa0c607' AND (geo_id IS NULL OR geo_id = '')"
      pattern: "geo_id\\s*=\\s*'0619766'"
    - from: "essentials.offices (2afa4fd2 Trujillo, the orphan-chamber office)"
      to: "survivor chamber 7cb8a90c"
      via: "UPDATE offices SET chamber_id='7cb8a90c...' WHERE chamber_id='a30fd533...'"
      pattern: "chamber_id\\s*=\\s*'7cb8a90c"
    - from: "essentials.chambers (a30fd533, emptied orphan duplicate)"
      to: "deleted (asserted 0 offices first)"
      via: "DO $$ assert 0 offices $$ then DELETE FROM essentials.chambers WHERE id = 'a30fd533...'"
      pattern: "DELETE FROM essentials\\.chambers"
    - from: "essentials.districts (Sosa's LOCAL_EXEC row, office cc3bacd0)"
      to: "district_type='LOCAL', label 'District 2'; office cc3bacd0 title='Mayor'"
      via: "UPDATE districts SET district_type='LOCAL', label='District 2' WHERE id='<Sosa district UUID>'; UPDATE offices SET title='Mayor' WHERE id='cc3bacd0...'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.politicians (external_id -201200 Trujillo, last_name='-')"
      to: "name corrected first_name='Mario', last_name='Trujillo'"
      via: "UPDATE politicians SET first_name='Mario', last_name='Trujillo' WHERE external_id=-201200 AND (last_name='-' OR last_name IS NULL)"
      pattern: "last_name\\s*=\\s*'Trujillo'"
---

<objective>
Reconcile the existing partial, duplicate-chamber, structurally-defective Downey seed in a single idempotent
structural migration (985). This is the foundation wave — every later wave (roster reconcile, headshots,
stances) assumes a clean, single-chamber, correctly-districted structure with the rotational mayor modeled as
a title on a district seat.

⚠️ RECONCILE, NOT GREENFIELD (DB-verified 2026-06-20 in CONTEXT.md §DB Pre-Check). The Downey gov already
exists with geo_id NULL, TWO duplicate 'City Council' chambers (both slug downey-city-council), 6 offices for
5 real seats, ALL mislabeled 'At-Large', a stray LOCAL_EXEC row (Sosa, office cc3bacd0), and Trujillo's
politician row corrupted (last_name='-'). This wave does NOT create Ortiz or unlink the stale members — it
(1) backfills geo_id, (2) merges the duplicate chamber, (3) fixes the form of government from (wrong) At-Large
to Downey's real 5-single-member-district structure, (4) collapses the rotational mayor into a District 2
title, and (5) corrects Trujillo's name. The Ortiz creation, the Saab/Pelc unlinks, the bidirectional
back-pointer repairs, and official_count are Plan 02 — this wave is STRUCTURE ONLY.

⚠️ DOWNEY IS A ROTATIONAL-MAYOR CITY (Palmdale 146 model — NOT Pasadena's directly-elected LOCAL_EXEC model).
RESEARCH §D-02a CONFIRMED ROTATIONAL: downeyca.org states "The Mayor, as well as the Mayor Pro Tem, are
selected annually by and among the members of the City Council." Downey explicitly REJECTED a 4+1 directly-
elected-mayor option. Therefore the stray LOCAL_EXEC row (Sosa, office cc3bacd0) is a MISLABEL: collapse it
into a normal LOCAL District 2 seat with title='Mayor' on the office, per CONTEXT D-02a (default applied —
research confirmed) and the Palmdale 146-CONTEXT D-08 precedent. Do NOT create or keep any separate Mayor
office / LOCAL_EXEC district (Pasadena's pattern does NOT apply here).

⚠️ DOWNEY IS BY-DISTRICT. Use the proven Palmdale (146) / Pomona (147) by-district relabel pattern: relabel
the existing wrongly-'At-Large' council rows to 'District 1'..'District 5' per the RESEARCH §D-03 occupant
map, and CREATE any missing district row (District 1 for Ortiz likely needed — Ortiz himself is created/seated
in Plan 02; this wave only ensures his District 1 row exists). Do NOT tear down and rebuild.

Five structural fixes, all DB-verified against the live state on 2026-06-20:
1. Backfill essentials.governments.geo_id = '0619766' on gov 1a31cf01-5e05-46d9-88f3-6b94aaa0c607 (currently
   NULL), guarded WHERE (geo_id IS NULL OR geo_id = ''). state already 'CA'.
2. MERGE the duplicate chamber: MOVE the orphan-chamber office (Trujillo 2afa4fd2) into the survivor chamber
   7cb8a90c (UPDATE chamber_id WHERE chamber_id='a30fd533…'), then an inline assert confirms a30fd533 now has
   0 offices, then DELETE the orphan chamber a30fd533. Target both chambers/offices by UUID ONLY — both share
   the name 'City Council'. End state: ONE chamber (7cb8a90c) with 6 offices (6→5 roster reconcile is Plan 02).
3. RELABEL the wrongly-'At-Large' council district rows to their occupant's REAL district per the RESEARCH
   §D-03 map (D2 Sosa, D3 Pemberton, D4 Frometa, D5 Trujillo; D1 Ortiz row created here, seated Plan 02),
   resolving each At-Large UUID from the pre-flight office→district map. Keep geo_id '0619766', state 'CA'.
4. COLLAPSE the rotational mayor (Palmdale D-08): convert Sosa's LOCAL_EXEC district row (referenced by office
   cc3bacd0) to district_type='LOCAL', relabel it 'District 2', and set office cc3bacd0 title='Mayor'. No
   separate Mayor office/district survives; other seats title='Councilmember'.
5. FIX Trujillo's name: correct the -201200 politician row (last_name='-') to first_name='Mario',
   last_name='Trujillo' (match the -201xxx convention confirmed in pre-flight), guarded WHERE last_name='-'.

Purpose: A clean structural base. Duplicate 'City Council' chamber names cause split-section render bugs; NULL
geo_id breaks routing; the wrong At-Large labels + the stray LOCAL_EXEC row misrepresent Downey's real
by-district, rotational-mayor form of government; Trujillo's corrupted name breaks display. Output: Migration
985 applied to production; all post-verification queries green; ZERO LOCAL_EXEC rows; split-section check
returns 0 rows for Downey.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally (NOT a
stance/headshot audit-only migration). On-disk file counter is authoritative; next structural is 985 (research
§Migration Ledger: on-disk MAX 984 = state_exec batches 980-984; schema_migrations MAX 947; 957-979 unused gap
— pre-flight MUST re-confirm BOTH the on-disk MAX and the live schema_migrations MAX before committing to 985).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-downey-deep-seed/150-RESEARCH.md
@.planning/phases/150-downey-deep-seed/150-CONTEXT.md
@.planning/phases/146-palmdale-deep-seed/146-01-PLAN.md
@.planning/phases/149-pasadena-deep-seed/149-01-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- DB-verified live values (2026-06-20, CONTEXT.md §DB Pre-Check + RESEARCH §D-02a/§D-03). Task 1 re-confirms
     them read-only before any write and STOPs on drift. -->

Government row:
  id     = 1a31cf01-5e05-46d9-88f3-6b94aaa0c607   name = 'City of Downey, California, US'
  geo_id = NULL  -> set to '0619766'   (state already 'CA')
  (NOT in scope: Downey Unified school district gov 32e2fad0-dfde-406a-a0ce-93e0a58f40ac)

Two chambers, BOTH named 'City Council' / slug 'downey-city-council' under gov 1a31cf01 (MERGE into one):
  KEEP   7cb8a90c-1214-4840-bd75-5f6b9504532d  (SURVIVOR — official_count 5, 5 offices)
  DELETE a30fd533-2188-4f36-bf23-a76d75296f2e  (ORPHAN — official_count NULL, 1 office: Trujillo)
  (chambers.slug is a GENERATED column — NEVER include slug in any chamber write)

Survivor 7cb8a90c offices (5, via offices.politician_id; relabel their districts; 6->5 reconcile is Plan 02):
  6fa79f0e  Claudia Frometa  (ext_id 675361,  At-Large LOCAL     -> relabel district 'District 4')
  3718d3c0  Dorothy Pemberton(ext_id 675360,  At-Large LOCAL     -> relabel district 'District 3')
  cc3bacd0  Hector Sosa      (ext_id 675353,  At-Large LOCAL_EXEC -> relabel 'District 2' + district_type LOCAL + office title 'Mayor')
  44ca5c68  Alex Saab        (ext_id -700160, At-Large LOCAL     -> STALE; unlink + repurpose for Ortiz D1 in Plan 02)
  2ecc0a3e  Don Pelc         (ext_id -700161, At-Large LOCAL     -> STALE; unlink in Plan 02)

Orphan a30fd533 office (MOVE into survivor, then delete the empty chamber):
  2afa4fd2  Mario Trujillo   (ext_id -201200, At-Large LOCAL     -> relabel district 'District 5'; politician last_name='-' to FIX)

Total = 6 offices for 5 real seats. All council rows currently district_type='LOCAL' label 'At-Large' EXCEPT
  Sosa's (cc3bacd0) which is LOCAL_EXEC -> all must become 'District 1'..'District 5', all LOCAL, ZERO LOCAL_EXEC.

Move-then-delete (Palmdale 918 / Pasadena 946 pattern — UUID-targeted, 1 office; RESEARCH §Pitfall 5 ordering):
  Step order to avoid confusion: (1) move Trujillo from a30fd533 to 7cb8a90c FIRST, (2) THEN do the
  district relabel + Sosa collapse, (3) THEN create any missing District row. (Saab/Pelc unlink is Plan 02.)
  1. UPDATE essentials.offices SET chamber_id='7cb8a90c-1214-4840-bd75-5f6b9504532d'
     WHERE chamber_id='a30fd533-2188-4f36-bf23-a76d75296f2e'   (moves Trujillo 2afa4fd2 into the survivor)
  2. DO $$ BEGIN IF (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='a30fd533-...') > 0
     THEN RAISE EXCEPTION 'Chamber a30fd533 still has offices'; END IF; END $$;
  3. DELETE FROM essentials.chambers WHERE id='a30fd533-2188-4f36-bf23-a76d75296f2e'

District relabel (relabel label ONLY for the non-mayor seats; geo_id/state stay as-is). The MANDATORY
  pre-flight office→district map (RESEARCH §D-03 Open Q1) maps each office to its current district_id + label
  + district_type — use THOSE UUIDs:
  SELECT o.id AS office_id, p.external_id, p.last_name, o.title, d.id AS district_id, d.label, d.district_type
  FROM essentials.offices o
  LEFT JOIN essentials.politicians p ON p.id = o.politician_id
  LEFT JOIN essentials.districts d ON d.id = o.district_id
  WHERE o.chamber_id IN ('7cb8a90c-...','a30fd533-...')
  ORDER BY d.district_type, d.label;
  Occupant -> district relabel map (RESEARCH §D-03 — authoritative):
    Sosa    (cc3bacd0) -> 'District 2'  (ALSO: district_type LOCAL + office title 'Mayor' — rotational, see below)
    Pemberton(3718d3c0)-> 'District 3'
    Frometa (6fa79f0e) -> 'District 4'
    Trujillo(2afa4fd2) -> 'District 5'
    (D1 Ortiz: create the 'District 1' row here; Ortiz politician + office seat is Plan 02)

ROTATIONAL MAYOR collapse (Palmdale 146-CONTEXT D-08 — NOT Pasadena LOCAL_EXEC; RESEARCH §D-02a CONFIRMED):
  - Sosa's district row (the district_id referenced by office cc3bacd0, currently district_type='LOCAL_EXEC'):
    UPDATE essentials.districts SET district_type='LOCAL', label='District 2'
    WHERE id='<Sosa district UUID from pre-flight>' AND district_type IS DISTINCT FROM 'LOCAL';
  - Sosa's office title:
    UPDATE essentials.offices SET title='Mayor' WHERE id='cc3bacd0-...' AND title IS DISTINCT FROM 'Mayor';
  - NO separate Mayor office/district created; NO LOCAL_EXEC row survives under gov 1a31cf01.
  - Re-confirm at apply time that Sosa is the CURRENT rotational mayor (rotates each December — RESEARCH
    §metadata "re-confirm rotational mayor at apply time"); if a December rotation has changed the mayor, set
    title='Mayor' on the CURRENT mayor's district-2..5 office instead and report the change.

District CREATE (guarded NOT EXISTS — 'District 1' for Ortiz; Palmdale 918 'District 3' precedent):
  INSERT INTO essentials.districts (label, district_type, geo_id, state)
  SELECT 'District 1','LOCAL','0619766','CA'
  WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE label='District 1' AND geo_id='0619766');
  (Ortiz's office seat + politician creation is Plan 02; this wave only ensures the District 1 row exists.)
  NOTE (RESEARCH §Pitfall 6): D5 is currently elected citywide-at-large; model it with the city-level geo_id
  '0619766' (no sub-district boundary). The Nov 2026 redistricting is OUT OF SCOPE — seed current reality.

Trujillo name fix (RESEARCH §Pitfall 3 / §Open Q3): confirm the -201xxx name convention in pre-flight (e.g.
  SELECT first_name, last_name FROM essentials.politicians WHERE external_id BETWEEN -201300 AND -201100
  LIMIT 5), then:
  UPDATE essentials.politicians SET first_name='Mario', last_name='Trujillo'
  WHERE external_id=-201200 AND (last_name='-' OR last_name IS NULL);
  (If a full_name column exists, also set full_name='Mario Trujillo' guarded.)

Schema notes (CRITICAL — CA convention, LOCATION-ONBOARDING §California Quick Reference):
  - chambers.slug is GENERATED — NEVER include slug in any chamber write
  - district_type + label live on essentials.districts, NOT on offices (no offices.district_type column)
  - districts.state is uppercase 'CA'
  - Downey's mayor is ROTATIONAL (Glendale/Palmdale title-on-seat model) — title='Mayor' on the District 2
    office; do NOT create a LOCAL_EXEC Mayor row (Pasadena's directly-elected model does NOT apply, RESEARCH
    §Anti-Patterns)
  - geo_id guard MUST include the empty-string case: WHERE (geo_id IS NULL OR geo_id = '')
  - This wave does NOT create Ortiz, unlink Saab/Pelc, repair back-pointers, or set official_count (all Plan 02)

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after
  consolidation; expect 0 rows for Downey geo_id '0619766'. (5 OTHER LA-area cities have pre-existing
  split-section defects — OUT OF SCOPE, project_split_section_defects_5_cities.)

Pattern migrations to copy:
  C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql (most recent: geo_id backfill + move-then-delete
    + At-Large->District relabel + create-missing-district + idempotent guards).
  C:/EV-Accounts/backend/migrations/947_pasadena_complete.sql (roster reconcile shape — referenced for context).
  Palmdale 918 reconcile (the rotational-mayor collapse precedent — title='Mayor' on a LOCAL district seat,
    NO LOCAL_EXEC; if the file is on disk in C:/EV-Accounts/backend/migrations/, read it directly).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state (gov, two chambers, both link directions, the office→district map, the At-Large/LOCAL_EXEC set, Sosa's stray LOCAL_EXEC, Trujillo last_name='-', the -201xxx name convention, migration counters) — STOP on drift</name>
  <files>C:/EV-Accounts/backend/migrations/985_downey_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql (the DIRECT by-district STRUCTURAL template — geo_id backfill + move-then-delete + At-Large->District relabel + create-missing-district + idempotent guards)
    - C:/EV-Accounts/backend/migrations/947_pasadena_complete.sql (roster reconcile shape for context)
    - .planning/phases/146-palmdale-deep-seed/146-01-PLAN.md (the rotational-mayor collapse precedent: title='Mayor' on a LOCAL district seat, NO LOCAL_EXEC — the governing pattern for Sosa)
    - .planning/phases/150-downey-deep-seed/150-RESEARCH.md (§D-02a rotational-mayor verdict, §D-03 roster + office-to-district map, §Common Pitfalls 1-7, §Open Questions, §Migration Ledger, §Validation Architecture)
    - .planning/phases/150-downey-deep-seed/150-CONTEXT.md (LOCKED decisions D-01/D-02/D-02a + §DB Pre-Check live values)
    - .planning/phases/150-downey-deep-seed/150-VALIDATION.md (the per-wave verification SELECT contract)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA', district_type on districts)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE/INSERT, run read-only confirmation queries via mcp__supabase-local
    execute_sql (= production). Confirm EACH against the LOCKED CONTEXT §DB Pre-Check + RESEARCH §D-02a/§D-03:
    (a) gov: SELECT id, geo_id, state, name FROM essentials.governments WHERE
    id='1a31cf01-5e05-46d9-88f3-6b94aaa0c607' — confirm geo_id NULL/empty, state 'CA', name 'City of Downey,
    California, US';
    (b) two chambers under that gov both named 'City Council' / slug 'downey-city-council' — 7cb8a90c
    (SURVIVOR, official_count 5, 5 offices) and a30fd533 (ORPHAN, official_count NULL, 1 office); confirm NO
    third chamber;
    (c) survivor 7cb8a90c has exactly 5 offices occupied via offices.politician_id: 6fa79f0e (Frometa 675361),
    3718d3c0 (Pemberton 675360), cc3bacd0 (Sosa 675353), 44ca5c68 (Saab -700160), 2ecc0a3e (Pelc -700161);
    (d) orphan a30fd533 has exactly 1 office: 2afa4fd2 (Trujillo -201200) — the MOVE target;
    (e) run the MANDATORY office→district map (RESEARCH §D-03 query over BOTH chambers) — record each office's
    current district_id + label + district_type + title; determine the count of 'At-Large' LOCAL rows, identify
    Sosa's LOCAL_EXEC district UUID (office cc3bacd0), and whether any 'District 1'..'District 5' row already
    exists for geo_id 0619766;
    (f) confirm Sosa's office cc3bacd0 references a district_type='LOCAL_EXEC' row (the stray exec row to
    collapse) and that it is the ONLY LOCAL_EXEC row under gov 1a31cf01;
    (g) confirm NO shared-district defect (no two offices pointing at the same district_id) — if found, note it
    for a create+repoint resolution;
    (h) Trujillo name defect (Pitfall 3): SELECT external_id, first_name, last_name FROM essentials.politicians
    WHERE external_id=-201200 — confirm last_name='-' (or NULL) with the full name in first_name; AND sample the
    -201xxx convention: SELECT first_name, last_name FROM essentials.politicians WHERE external_id BETWEEN
    -201300 AND -201100 LIMIT 5 — to confirm the canonical first/last split;
    (i) Ortiz absence (Pitfall 2 / Plan 02 prep): SELECT id, external_id FROM essentials.politicians WHERE
    first_name ILIKE '%horacio%' AND last_name ILIKE '%ortiz%' — record whether an Ortiz row already exists
    (Plan 02 needs this);
    (j) re-confirm the CURRENT ROSTER + rotational mayor live (RESEARCH §D-03): Mayor Sosa (D2) + D1 Ortiz /
    D3 Pemberton / D4 Frometa / D5 Trujillo against downeychamber.org / downeyca.org; confirm Sosa is the
    current rotational mayor (rotates each December).
    Also confirm the migration ledger: SELECT MAX(version) FROM supabase_migrations.schema_migrations (expect
    947) AND the on-disk MAX file in C:/EV-Accounts/backend/migrations/ (expect 984 = state_exec_seed_batch_e)
    — next structural is 985. If ANY precondition does not match (a third chamber, a30fd533 has !=1 office,
    7cb8a90c has !=5 offices, an office occupied by a different ext_id than locked, Sosa NOT LOCAL_EXEC, MORE
    than one LOCAL_EXEC row, a shared-district defect, Trujillo's last_name NOT '-'/NULL, the on-disk MAX not
    984, the schema_migrations MAX not 947, or the current roster/mayor differs), STOP and report the
    discrepancy — do NOT write a migration against a moved target. Record the resolved district UUIDs (incl.
    Sosa's LOCAL_EXEC UUID), the Ortiz finding, and the confirmed migration numbers in the SUMMARY for Plan 02.
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql "SELECT geo_id, state FROM essentials.governments WHERE id='1a31cf01-5e05-46d9-88f3-6b94aaa0c607';" returns one row, geo_id NULL or '', state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - Gov 1a31cf01 has geo_id NULL/empty and state 'CA' (pre-state); confirmed name 'City of Downey, California, US'
    - Two chambers confirmed sharing name 'City Council': 7cb8a90c (KEEP, 5 offices, official_count 5) and a30fd533 (DELETE, 1 office, official_count NULL); no third chamber
    - Survivor 7cb8a90c offices confirmed OCCUPIED via offices.politician_id (6fa79f0e=Frometa 675361, 3718d3c0=Pemberton 675360, cc3bacd0=Sosa 675353, 44ca5c68=Saab -700160, 2ecc0a3e=Pelc -700161)
    - Orphan a30fd533 confirmed to have exactly 1 office: 2afa4fd2 (Trujillo -201200) — the move target
    - Office->district map run; the At-Large LOCAL row count, Sosa's LOCAL_EXEC district UUID (office cc3bacd0), and the existence of any 'District 1'..'District 5' row all resolved; any shared-district defect surfaced
    - Sosa's office cc3bacd0 confirmed pointing at the ONLY LOCAL_EXEC district row under gov 1a31cf01 (the row to collapse)
    - Trujillo (-201200) confirmed last_name='-'/NULL with full name in first_name; the -201xxx first/last convention sampled and recorded
    - Ortiz absence/presence recorded for Plan 02 (SELECT by name)
    - Ledger confirmed: on-disk MAX 984, schema_migrations MAX 947, next structural 985
    - Current roster + rotational mayor re-confirmed live = Sosa Mayor/D2 + Ortiz D1 / Pemberton D3 / Frometa D4 / Trujillo D5; STOP-and-report if changed
    - Any mismatch vs CONTEXT §DB Pre-Check / RESEARCH §D-02a/§D-03 is surfaced and execution paused (STOP-on-drift; no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches CONTEXT §DB Pre-Check + RESEARCH §D-02a/§D-03; gov UUID, the move-target office, the office->district map, Sosa's LOCAL_EXEC UUID, Trujillo's name defect + the -201xxx convention, the Ortiz finding, and the migration numbers (985 next) are all confirmed; the geo_id backfill, chamber merge, district relabel/create, rotational-mayor collapse, and Trujillo name fix are safe to author.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 985 (geo_id backfill + merge duplicate chamber + relabel At-Large->District 1-5 + collapse Sosa LOCAL_EXEC into District 2 with title='Mayor' + create any missing District + fix Trujillo name)</name>
  <files>C:/EV-Accounts/backend/migrations/985_downey_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/946_pasadena_reconcile.sql (copy the idempotent geo_id backfill + office move + FK-safe chamber DELETE + inline-assert + At-Large->District relabel + create-missing-district patterns)
    - .planning/phases/146-palmdale-deep-seed/146-01-PLAN.md (the rotational-mayor collapse authoring shape: district_type LOCAL + title='Mayor', NO LOCAL_EXEC)
    - .planning/phases/149-pasadena-deep-seed/149-01-PLAN.md (the exact authoring + post-verification query set this plan mirrors)
    - .planning/phases/150-downey-deep-seed/150-RESEARCH.md (§Architecture Patterns 1+2 SQL templates, §D-02a, §D-03 relabel map, §Common Pitfalls 1/3/5/6, §Validation Architecture DWNY-01a..01g)
    - .planning/phases/150-downey-deep-seed/150-CONTEXT.md (LOCKED D-01/D-02/D-02a)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA')
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/985_downey_reconcile.sql wrapped in BEGIN/COMMIT, all statements
    idempotent, in this order:
    (1) UPDATE essentials.governments SET geo_id='0619766' WHERE
        id='1a31cf01-5e05-46d9-88f3-6b94aaa0c607' AND (geo_id IS NULL OR geo_id = '') (DWNY-01a, empty-string guard).
    (2) MERGE — MOVE the orphan-chamber office into the survivor (Pitfall 5 ordering — move FIRST): UPDATE
        essentials.offices SET chamber_id='7cb8a90c-1214-4840-bd75-5f6b9504532d' WHERE
        chamber_id='a30fd533-2188-4f36-bf23-a76d75296f2e' (carries Trujillo 2afa4fd2; target by the orphan UUID).
    (3) Inline assert the orphan is now empty: a DO $$ block that RAISEs EXCEPTION if
        (SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='a30fd533-2188-4f36-bf23-a76d75296f2e') > 0.
    (4) DELETE essentials.chambers WHERE id='a30fd533-2188-4f36-bf23-a76d75296f2e'. Target by UUID ONLY, never
        by name/slug (both chambers share 'City Council' / downey-city-council).
    (5) RELABEL the wrongly-'At-Large' council district rows to their occupant's real district, each guarded
        label IS DISTINCT FROM target, using the At-Large UUIDs resolved by the Task 1 office->district map:
        'District 3' (Pemberton 3718d3c0), 'District 4' (Frometa 6fa79f0e), 'District 5' (Trujillo 2afa4fd2).
        (Sosa's 'District 2' relabel happens in step 6 together with the LOCAL_EXEC->LOCAL conversion.) Do NOT
        alter geo_id or state. (Saab's 44ca5c68 + Pelc's 2ecc0a3e rows are touched in Plan 02 — leave as-is here.)
    (6) COLLAPSE the rotational mayor (Palmdale D-08, Pitfall 1 — change BOTH district_type AND office title):
        UPDATE essentials.districts SET district_type='LOCAL', label='District 2' WHERE id='<Sosa district UUID
        from Task 1>' AND (district_type IS DISTINCT FROM 'LOCAL' OR label IS DISTINCT FROM 'District 2');
        UPDATE essentials.offices SET title='Mayor' WHERE id='cc3bacd0-...' AND title IS DISTINCT FROM 'Mayor'.
        Do NOT create any separate Mayor office/district; do NOT leave any LOCAL_EXEC row under gov 1a31cf01.
        (If Task 1 found a December rotation changed the mayor, set title='Mayor' on the CURRENT mayor's office
        and Councilmember on Sosa instead — per the apply-time confirmation.)
    (7) CREATE the missing 'District 1' row guarded WHERE NOT EXISTS (for Ortiz; seated in Plan 02): INSERT INTO
        essentials.districts (label, district_type, geo_id, state) SELECT 'District 1','LOCAL','0619766','CA'
        WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE label='District 1' AND geo_id='0619766').
        (If the pre-flight found a shared-district defect, resolve it with the same create-new-row-and-repoint
        pattern.)
    (8) FIX Trujillo's name (Pitfall 3): UPDATE essentials.politicians SET first_name='Mario',
        last_name='Trujillo' (+ full_name='Mario Trujillo' if that column exists) WHERE external_id=-201200 AND
        (last_name='-' OR last_name IS NULL). Match the -201xxx convention confirmed in Task 1.
    Do NOT touch the slug column anywhere (GENERATED). Do NOT write to offices.district_type (no such column).
    Do NOT create Ortiz, seat any office, unlink Saab/Pelc, repair back-pointers, or set official_count (all
    Plan 02). Do NOT create a LOCAL_EXEC Mayor row (Downey is rotational — Pasadena's model does NOT apply).
    End the file with post-verification queries: geo_id check + a chamber listing under gov 1a31cf01 (expect
    exactly one 'City Council', the survivor 7cb8a90c) + a count of offices under 7cb8a90c (expect 6) + a
    confirm a30fd533 is gone + the district labels for geo_id 0619766 ('District 1'..'District 5', all LOCAL,
    ZERO LOCAL_EXEC) + a confirm Sosa's office cc3bacd0 title='Mayor' on the now-LOCAL 'District 2' + a confirm
    Trujillo (-201200) reads first_name='Mario' last_name='Trujillo' + the feedback_section_split_check SQL
    (expect 0 rows for Downey). Apply via mcp__supabase-local execute_sql, and register migration 985 in
    supabase_migrations.schema_migrations (structural). Record the newly-created 'District 1' UUID + the
    relabeled district UUIDs in the SUMMARY for Plan 02.
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql checks ALL of: SELECT geo_id FROM essentials.governments WHERE id='1a31cf01-5e05-46d9-88f3-6b94aaa0c607' returns '0619766'; SELECT COUNT(*) FROM essentials.chambers WHERE government_id='1a31cf01-5e05-46d9-88f3-6b94aaa0c607' AND name='City Council' returns 1; SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='7cb8a90c-1214-4840-bd75-5f6b9504532d' returns 6; SELECT COUNT(*) FROM essentials.districts d JOIN essentials.offices o ON o.district_id=d.id WHERE o.chamber_id='7cb8a90c-1214-4840-bd75-5f6b9504532d' AND d.district_type='LOCAL_EXEC' returns 0; SELECT o.title FROM essentials.offices o WHERE o.id='cc3bacd0-...' returns 'Mayor'; SELECT last_name FROM essentials.politicians WHERE external_id=-201200 returns 'Trujillo'</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='1a31cf01-...'` -> '0619766'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='1a31cf01-...' AND name='City Council'` -> 1 (not 0, not 2); a30fd533 deleted
    - `SELECT COUNT(*) FROM essentials.chambers WHERE id='a30fd533-2188-4f36-bf23-a76d75296f2e'` -> 0 (orphan deleted)
    - `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='7cb8a90c-1214-4840-bd75-5f6b9504532d'` -> 6 (Trujillo moved in; 6->5 roster reconcile is Plan 02)
    - District relabel: rows for geo_id '0619766' = 'District 1'..'District 5' (all LOCAL); Pemberton->D3, Frometa->D4, Trujillo->D5; Sosa->D2 (converted from LOCAL_EXEC); each still geo_id '0619766', state 'CA'
    - ZERO LOCAL_EXEC district rows remain under gov 1a31cf01 (Sosa's collapsed); Sosa's office cc3bacd0 title='Mayor'; no new Mayor office/district created
    - 'District 1' row created (guarded NOT EXISTS) for Ortiz; no shared-district defect remains
    - Trujillo (-201200): first_name='Mario', last_name='Trujillo' (corrected from last_name='-')
    - No politician created/unlinked; no back-pointers repaired; official_count NOT set (all Plan 02)
    - feedback_section_split_check SQL returns 0 rows for Downey (geo_id '0619766')
    - Migration 985 registered in supabase_migrations.schema_migrations (structural)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
  </acceptance_criteria>
  <done>Migration 985 applied + registered; one 'City Council' chamber (7cb8a90c) remains with 6 offices; geo_id 0619766 set; the council districts relabeled D1-D5 (D1 created), the rotational mayor collapsed into District 2 (title='Mayor', district_type LOCAL, ZERO LOCAL_EXEC), Trujillo's name fixed; split-section check 0 rows; re-running the file is a no-op; new/relabeled district UUIDs recorded for Plan 02.</done>
</task>

</tasks>

<threat_model>
DB-seeding phase with NO new external attack surface. No untrusted user input crosses any boundary — migration
SQL is applied via service-role/operator (MCP) directly against the internal production Supabase DB. The real
threat surface is accidental data corruption — deleting the orphan chamber before moving Trujillo's office
(orphan), relabeling the wrong district row (multiple similar At-Large UUIDs), leaving Sosa as LOCAL_EXEC or
creating a separate Mayor row (wrong Pasadena model on a rotational city), creating a duplicate 'District 1',
overwriting Trujillo's name on a re-run, missing the empty-string geo_id case, writing to a non-existent
offices.district_type column, or touching the wrong gov — mitigated by move-FIRST ordering, an inline
empty-chamber assert, UUID-scoped targeting of every write, relabel-only guards, the BOTH-district_type-AND-
title rotational collapse, NOT EXISTS guards on the new district, the empty-string geo_id guard, the
last_name='-' guard on the name fix, and a STOP-on-drift pre-flight that checks BOTH link directions, the
office->district map, Sosa's LOCAL_EXEC UUID, Trujillo's name defect, and the current roster/mayor.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> production DB | Migration SQL applied by a trusted operator (MCP) directly against production Supabase. No untrusted user input crosses here. |
| official sources (downeyca.org / downeychamber.org) -> roster confirmation | Live re-confirmation of the current rotational mayor + 5 district occupants before any write; read-only here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Tampering | Chamber DELETE targets the wrong chamber (both share name 'City Council' / slug downey-city-council) | mitigate | DELETE keyed on UUID 'a30fd533…' ONLY, never on name/slug; post-verify exactly one 'City Council' chamber (7cb8a90c) remains |
| T-150-02 | Tampering | Orphan chamber deleted BEFORE Trujillo's office is moved -> orphaned office (Pitfall 5) | mitigate | Move-FIRST (UPDATE WHERE chamber_id='a30fd533…') + inline DO $$ assert RAISEs if a30fd533 still has >0 offices before the DELETE; post-verify 6 offices in survivor |
| T-150-03 | Tampering | geo_id overwritten on a re-run or wrong gov, or empty-string case missed | mitigate | UPDATE keyed on id 1a31cf01 + WHERE (geo_id IS NULL OR geo_id = ''); pre-flight confirms it is the City of Downey gov (not the Downey Unified school district 32e2fad0); second apply = 0 rows |
| T-150-04 | Tampering | Wrong district relabeled (multiple similar At-Large UUIDs; non-obvious occupant mapping) | mitigate | UPDATE keyed on each exact At-Large UUID from the Task 1 office->district map per the locked RESEARCH §D-03 mapping; geo_id/state untouched; post-verify labels D1-D5 |
| T-150-05 | Tampering | Wrong mayor model applied — Sosa left as LOCAL_EXEC, or a separate Mayor office/LOCAL_EXEC district fabricated (Pasadena model on a rotational city; Pitfall 1) | mitigate | Palmdale D-08 collapse: district_type LOCAL + label 'District 2' + office title='Mayor'; NO INSERT of any Mayor office/district; post-verify ZERO LOCAL_EXEC rows under gov 1a31cf01 |
| T-150-06 | Tampering | Duplicate 'District 1' row created on re-run, or a real shared-district defect missed | mitigate | INSERT guarded WHERE NOT EXISTS on (label, geo_id='0619766'); pre-flight maps the office->district matrix + any shared district; second apply = 0 rows |
| T-150-07 | Tampering | Trujillo's name overwritten on re-run, or the -201xxx convention guessed wrong | mitigate | Name fix guarded WHERE external_id=-201200 AND (last_name='-' OR last_name IS NULL); -201xxx convention sampled in pre-flight; second apply = 0 rows |
| T-150-08 | Tampering | Write to non-existent offices.district_type column, or altering district_type/geo_id on a non-mayor relabel | mitigate | Non-mayor district relabel touches label ONLY; only Sosa's row changes district_type (LOCAL_EXEC->LOCAL); no offices.district_type write anywhere; post-verify district_type/geo_id |
| T-150-09 | Tampering | Non-idempotent migration applied twice corrupts state | mitigate | All writes guarded (geo_id IS NULL/empty, id-scoped move/delete, label IS DISTINCT FROM, district_type IS DISTINCT FROM, NOT EXISTS, last_name='-' guard); second apply = 0 rows |
| T-150-10 | Tampering | Accidental write without verifying pre-state (drift since 2026-06-20; mayor rotates each December) | mitigate | Task 1 read-only pre-flight confirms BOTH link directions + the office->district map + Sosa's LOCAL_EXEC UUID + the current roster/rotational mayor before any write; STOP-on-mismatch |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable; existing Pillow + Supabase client only) |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 985:
1. gov 1a31cf01 geo_id = '0619766' (state 'CA')
2. exactly 1 chamber 'City Council' under gov 1a31cf01; orphan a30fd533 gone
3. survivor chamber 7cb8a90c has exactly 6 offices (6->5 roster reconcile is Plan 02)
4. district rows for geo_id 0619766: 'District 1'..'District 5' (all LOCAL); Pemberton D3 / Frometa D4 / Trujillo D5 / Sosa D2; ZERO LOCAL_EXEC rows under gov 1a31cf01
5. Sosa's office cc3bacd0 title='Mayor' on the now-LOCAL 'District 2'; no separate Mayor office/district; 'District 1' row created for Ortiz; no shared-district defect; new/relabeled UUIDs recorded for Plan 02
6. Trujillo (-201200) reads first_name='Mario' last_name='Trujillo'
7. feedback_section_split_check SQL -> 0 rows for Downey
8. no Ortiz created; Saab/Pelc not unlinked; back-pointers not repaired; official_count not set (all Plan 02)
9. migration 985 registered in schema_migrations
</verification>

<success_criteria>
All structural defects fixed and independently verified: geo_id backfilled (empty-string-safe); the duplicate
chamber merged via move-then-delete of Trujillo's office (with an inline assert proving it was empty before
deletion); the form of government corrected to Downey's real by-district structure (council rows relabeled
D1-D5 + District 1 created); the rotational mayor correctly collapsed into a District 2 title (district_type
LOCAL, ZERO LOCAL_EXEC — Palmdale model, not Pasadena); Trujillo's corrupted name fixed; migration 985
idempotent and registered; Downey is a single unambiguous 'City Council' chamber with correctly-districted
offices, ready for the roster reconcile in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/150-downey-deep-seed/150-01-SUMMARY.md` when done (record the newly-created 'District 1' UUID, the relabeled district UUIDs, Sosa's collapsed district UUID, the Ortiz absence/presence finding, the -201xxx name convention, and the confirmed migration numbers for Plan 02).
</output>
</content>
