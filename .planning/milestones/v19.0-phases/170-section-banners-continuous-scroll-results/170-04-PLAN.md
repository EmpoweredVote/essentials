---
phase: 170-section-banners-continuous-scroll-results
plan: 04
type: execute
wave: 3
depends_on: ["170-03"]
files_modified: []
autonomous: false
requirements: [BANR-01, BANR-02, BANR-03, BANR-04, NAV-01]
must_haves:
  truths:
    - "A human confirms on the live dev server that the three banners render in Aditi's Bloomington treatment, divide the tiers in one continuous scroll, fall back gracefully when no art exists, and the Type filter + name search still work with the tier control gone."
  artifacts: []
  key_links: []
---

<objective>
Human visual + functional sign-off for the section-banner Results redesign. All implementation is done
(Plans 170-01..03); the dev server is started and the user verifies the five phase success criteria on
real addresses. This is the verification that build/lint cannot prove: the Bloomington-treatment look,
the continuous-scroll divider behavior, the graceful fallback, and filter reconciliation.

Purpose: BANR-01..04 and NAV-01 are visual/interaction requirements — a human must confirm the banners
look right and the filters still behave before the phase is marked complete.

Output: human approval (or a list of issues to feed gap-closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-section-banners-continuous-scroll-results/170-CONTEXT.md
@.planning/phases/170-section-banners-continuous-scroll-results/170-UI-SPEC.md
@.planning/phases/170-section-banners-continuous-scroll-results/170-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the dev server</name>
  <files></files>
  <read_first>
    - package.json (the `dev` script → `vite`)
  </read_first>
  <action>
    Start the Vite dev server (`npm run dev`) in the background so the user can open the Results page.
    Report the local URL (default http://localhost:5173). Confirm the server compiles with no errors
    before handing off to the checkpoint.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (the app compiles).
    - The dev server is running and a local URL is reported to the user.
  </acceptance_criteria>
  <done>Dev server is running and the Results page is reachable in a browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual + functional sign-off (checkpoint)</name>
  <files></files>
  <action>
    Pause for human verification. Present the eight verification steps below; do not auto-approve.
    On "approved" the phase is complete; on issues, capture them for gap closure.
  </action>
  <what-built>
    A reusable `SectionBanner` divides Results into City → State → Federal in one continuous scroll,
    each banner showing a teal eyebrow (`YOUR CITY` / `YOUR STATE` / `FEDERAL`), a coral location pin,
    and a bold title (`📍 Bloomington, IN` / `📍 Indiana` / `📍 United States`). Image banners use a dark
    gradient overlay; tiers with no art fall back to a tier-tinted dark gradient. The tier sort dropdown
    is removed; the Elected/Appointed Type filter and name search remain.
  </what-built>
  <how-to-verify>
    1. Open the dev URL and enter a Bloomington, IN street address (a curated-art city, e.g. a Bloomington address).
       - Confirm dark mode is on (toggle if needed).
       - Scroll down: confirm a CITY banner appears before the City officials (skyline image + dark gradient,
         `YOUR CITY` eyebrow, coral pin + `Bloomington, IN` title), then a STATE banner before State officials,
         then a FEDERAL banner before Federal officials.
       - Confirm school-board sections (if any) appear UNDER the City banner — there is NO separate school banner.
       - Confirm all three tiers render in one continuous vertical scroll (no tier buttons/tabs to switch).
    2. Enter an LA, CA street address.
       - Confirm the banners are location-aware: `Los Angeles, CA` / `California` / `United States`.
    3. Enter an address in a city/state with NO curated art (e.g. a small town in a state without a capitol image).
       - Confirm the banner still renders as a dark tier-tinted gradient band with the same eyebrow + pin + title
         — NOT a broken image and NOT the old light generic SVG.
    4. Confirm the tier sort dropdown is GONE from the filter bar.
    5. Use the Elected/Appointed Type filter — confirm it still filters cards within the banner-divided layout.
    6. Type a name in the search box — confirm name search still narrows the cards.
    7. Resize to mobile width — confirm the banners are shorter (~120px) and still legible (text clears the gradient).
    8. Confirm no horizontal scrollbar is introduced by the full-bleed banners; PoliticianCard tiles look unchanged.
  </how-to-verify>
  <verify>
    <human-check>All eight verification steps pass on the live dev server.</human-check>
  </verify>
  <done>Human types "approved" after all eight checks pass.</done>
  <resume-signal>Type "approved" if all eight checks pass, or describe any issues (which banner/tier/filter and what went wrong).</resume-signal>
</task>

</tasks>

<verification>
- Dev server compiles and serves the Results page.
- Human confirms all eight verification steps (three banners in Bloomington treatment, continuous scroll, School folded under City, location-aware labels, graceful fallback, tier control gone, Type filter + name search working, mobile legibility, no horizontal overflow).
</verification>

<success_criteria>
- Human approves: banners render in Aditi's Bloomington treatment, divide the three tiers in one continuous scroll, fall back gracefully without art, and the tier control is gone while Type filter + name search still work (BANR-01, BANR-02, BANR-03, BANR-04, NAV-01).
</success_criteria>

<output>
Create `.planning/phases/170-section-banners-continuous-scroll-results/170-04-SUMMARY.md` when done
</output>
