---
phase: 36-global-controls-compass-default
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/pages/Results.jsx
autonomous: false

must_haves:
  truths:
    - "Results.jsx renders <CompassControlsBar /> instead of the duplicated inline sticky controls markup (lines 1526-1575 in current Results.jsx)"
    - "The compass controls bar appears above the rep list when activeView === 'representatives' AND compassMode is true"
    - "The compass controls bar also appears above the elections list when activeView === 'elections' AND compassMode is true (DEFAULT-05 parity with /elections page)"
    - "Stance buttons (Local Lens, Judicial Lens, Min, Max) appear only when rawUserAnswers.length >= 3 — preserving the existing Results.jsx defensive guard"
    - "Calibrated users (rawUserAnswers.length >= 3) arriving on Results page with no prior 'ev:compassMode' localStorage key auto-enable compass mode (DEFAULT-02, DEFAULT-03) — this already exists in Results.jsx today, just verify it"
    - "Existing Results.jsx FilterBar compass checkbox continues to toggle compassMode via handleCompassModeChange"
  artifacts:
    - path: "src/pages/Results.jsx"
      provides: "Consumes CompassControlsBar in place of inline duplicated controls markup"
      contains: "CompassControlsBar"
  key_links:
    - from: "src/pages/Results.jsx"
      to: "src/components/CompassControlsBar.jsx"
      via: "import + render when compassMode && (activeQuery || browseResults)"
      pattern: "import CompassControlsBar from"
    - from: "src/pages/Results.jsx"
      to: "FilterBar compass checkbox"
      via: "compassMode + handleCompassModeChange props"
      pattern: "onCompassModeChange=\\{handleCompassModeChange\\}"
---

<objective>
Replace the inline sticky controls markup in Results.jsx (lines 1526-1575) with the shared `<CompassControlsBar />` component from Plan 36-01. Then verify (via human checkpoint) that the controls bar correctly renders for BOTH the Representatives tab and the Elections tab — establishing the cross-page feature parity required by DEFAULT-05.

Purpose: Results.jsx already has the localStorage auto-enable pattern (lines 472-481), the FilterBar compass checkbox wiring (line 1518), and the inline controls bar markup (lines 1526-1575). The remaining Phase 36 work for this page is (a) deduplicate by consuming the shared component from Plan 36-01, and (b) verify that the controls bar already shows for both Results tabs as the research expects (RESEARCH.md Pitfall 4 notes that the inline block is outside the `activeView === 'representatives'` conditional, so it should already cover both tabs — but this needs human confirmation).

Output: Results.jsx with the inline controls block deleted and replaced by `<CompassControlsBar />`, and a human-verified confirmation that both tabs show the controls bar when compass mode is active.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-global-controls-compass-default/36-RESEARCH.md
@.planning/phases/36-global-controls-compass-default/36-01-SUMMARY.md

# Files to reference:
@src/pages/Results.jsx
@src/components/CompassControlsBar.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline sticky controls bar in Results.jsx with CompassControlsBar</name>
  <files>src/pages/Results.jsx</files>
  <action>
Locate the inline sticky controls block in Results.jsx. It currently spans lines 1525-1575:

```jsx
{/* Sticky CompassKey — only shown when compass mode is active */}
{compassMode && (activeQuery || browseResults) && (
  <div style={{ position: 'sticky', top: 8, /* ...~50 lines of duplicated markup... */ }}>
    {/* outer pointerEvents:none wrapper */}
    <div style={{ pointerEvents: 'auto', ... }}>
      {(rawUserAnswers?.length ?? 0) >= 3 && (
        <div style={{ display: 'flex', gap: 4 }}>
          {/* Local Lens button */}
          {/* Judicial Lens button */}
          {/* Stance Min button */}
          {/* Stance Max button */}
        </div>
      )}
      <CompassKey compact={!isDesktop} />
    </div>
  </div>
)}
```

Replace with:

```jsx
{compassMode && (activeQuery || browseResults) && (
  <CompassControlsBar
    userAnswers={rawUserAnswers}
    localLensActive={localLensActive}
    toggleLocalLens={toggleLocalLens}
    judicialLensActive={judicialLensActive}
    toggleJudicialLens={toggleJudicialLens}
    onStanceMin={handleStanceMin}
    onStanceMax={handleStanceMax}
    isDesktop={isDesktop}
  />
)}
```

CRITICAL: Preserve the existing render condition `compassMode && (activeQuery || browseResults)`. The `(activeQuery || browseResults)` guard ensures the controls bar only appears once a search has run — without it, the bar would render on the empty landing state. This is different from Elections.jsx which uses just `compassMode` because Elections.jsx already gates its entire results area on `electionsData` being loaded.

CRITICAL: Keep this controls block in its current position in the JSX tree — OUTSIDE the `activeView === 'representatives' ? (...) : (...)` ternary at line 1577. That position is what allows the bar to show for BOTH tabs (per RESEARCH.md Pitfall 4). Do not move it inside either branch.

IMPORT: Add at the top of Results.jsx alongside the existing component imports:
```javascript
import CompassControlsBar from '../components/CompassControlsBar';
```

CLEANUP: After deleting the inline block, check whether `CompassKey` is still imported but unused in Results.jsx. The original `CompassKey` import comes from `@empoweredvote/ev-ui`. If the inline controls bar was its only usage in Results.jsx, remove `CompassKey` from that import line. Verify by grepping the rest of Results.jsx for `<CompassKey` or `CompassKey` after deletion — if no other references remain, remove from the import. If `CompassKey` appears elsewhere (e.g. a `StickyCompassKey` reference inside FilterBar — but that's exported from FilterBar.jsx, not consumed here), leave the import as-is.

Do NOT touch the existing compassMode state, handleCompassModeChange, auto-enable useEffect, handleStanceMin, handleStanceMax, or FilterBar wiring. They remain correct.

Do NOT change the `(rawUserAnswers?.length ?? 0) >= 3` guard that was inside the inline block — `CompassControlsBar` already enforces this internally (per Plan 36-01) using the `userAnswers` prop. Passing `rawUserAnswers` to the component preserves identical behavior.
  </action>
  <verify>
1. `npm run build` succeeds.
2. Grep Results.jsx for `import CompassControlsBar` — must exist exactly once.
3. Grep Results.jsx for `<CompassControlsBar` — must exist exactly once.
4. Grep Results.jsx for the markers that identified the old block: search for `Stance Min — pull strong spokes inward` — must NOT appear anymore (that comment lived inside the deleted block).
5. Grep Results.jsx for `marginBottom: -70` — must NOT appear anymore (was only inside the deleted block).
6. Grep Results.jsx for `compassMode && (activeQuery || browseResults)` — must still appear exactly once, now wrapping the `<CompassControlsBar />` call.
  </verify>
  <done>
- Inline sticky controls markup deleted from Results.jsx
- `<CompassControlsBar />` rendered in its place with the same render condition
- Imports updated; no unused imports left over
- Build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Results.jsx now consumes the shared `<CompassControlsBar />` component (from Plan 36-01) instead of the previously duplicated inline markup. Behavior expectations:

1. REPRESENTATIVES TAB: Controls bar appears above the rep list when compassMode is on and there's an active search.
2. ELECTIONS TAB: Controls bar also appears above the elections list — establishing the cross-page parity with /elections (DEFAULT-05).
3. Visual layout identical to before — `CompassControlsBar` reproduces the exact markup (validated in Plan 36-01).
4. Auto-enable already exists in Results.jsx (lines 472-481) — calibrated users land on /results/* and see compass tiles by default. This checkpoint also verifies that pre-existing behavior survives the refactor.

Cross-page parity check: Plan 36-02 made /elections behave the same way. After this plan, /elections and /results/?view=elections should be functionally equivalent for compass interactions.
  </what-built>
  <how-to-verify>
Start dev server if not already running: `npm run dev`.

PREP — calibrated logged-in user:
- Ensure your account has >= 3 compass answers.
- DevTools → Application → Local Storage → http://localhost:5173 → DELETE `ev:compassMode` so auto-enable can trigger.

TEST 1 — Representatives tab (DEFAULT-03):
1. Navigate to http://localhost:5173/results?q=YOUR_ADDRESS (e.g. `?q=100 W Kirkwood Ave, Bloomington, IN 47404`). Or use the address search from the landing page.
2. EXPECTED: Active view defaults to `representatives` (no `&view=elections` in URL).
3. EXPECTED: Mini compasses appear on the right side of representative tiles. (Verifies auto-enable triggered.)
4. EXPECTED: Sticky controls bar pinned at top of results: Local Lens (green magnifier), Judicial Lens (orange gavel), ⊟ (Min), ⊞ (Max), CompassKey legend.
5. EXPECTED: localStorage now has `ev:compassMode = 'true'`.

TEST 2 — Elections tab parity (DEFAULT-02, DEFAULT-05):
1. From the same Results page, click the "Elections" tab.
2. EXPECTED: URL becomes `&view=elections`.
3. EXPECTED: Elections tiles render with mini compasses (compassMode is still true).
4. EXPECTED: Same sticky controls bar (Local Lens, Judicial Lens, Min, Max, CompassKey) is pinned above the elections list — IDENTICAL to what appeared on the Representatives tab.
5. EXPECTED: Toggling the Local Lens button updates ALL mini compasses on the Elections tab simultaneously.
6. EXPECTED: Toggling ⊞ (Stance Max) repositions spokes on the Elections tab tiles.

TEST 3 — /elections vs /results?view=elections parity (DEFAULT-05):
1. In one tab, open http://localhost:5173/elections.
2. In another tab, open http://localhost:5173/results?q=YOUR_ADDRESS&view=elections.
3. EXPECTED: Both pages show the same candidate tiles for the same address (data parity).
4. EXPECTED: Both pages show the sticky controls bar at the top with the same buttons in the same order.
5. EXPECTED: Toggling Local Lens on one page does NOT auto-sync to the other (they're separate page loads), but the visual behavior when toggled on each page is identical.

TEST 4 — Off-preference persistence on Results page:
1. On /results page, uncheck the Compass checkbox in the FilterBar.
2. EXPECTED: Mini compasses disappear; sticky controls bar disappears; rep tiles return to horizontal PoliticianCard view.
3. EXPECTED: localStorage `ev:compassMode = 'false'`.
4. Reload — EXPECTED: compass stays OFF (auto-enable does not re-enable because localStorage key is no longer null).

TEST 5 — Uncalibrated user regression (DEFAULT-04):
1. Open incognito (or log out). Clear localStorage.
2. Visit http://localhost:5173/results?q=YOUR_ADDRESS.
3. EXPECTED: Standard horizontal PoliticianCard tiles. No mini compasses. No sticky controls bar.
4. EXPECTED: The FilterBar Compass checkbox is visible but unchecked.

TEST 6 — Visual regression: marginBottom: -70 preservation (RESEARCH.md Pitfall 3):
1. With compass on and results loaded, scroll the list slowly.
2. EXPECTED: The sticky bar stays pinned at the top with the first tile flush against / slightly overlapped by the bar. NO awkward vertical gap. (This is the load-bearing `marginBottom: -70` baked into CompassControlsBar from Plan 36-01.)

If any test fails, describe which one and what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 tests pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` succeeds.
2. Results.jsx no longer contains `marginBottom: -70` or the inline sticky controls markup.
3. Results.jsx imports and renders `<CompassControlsBar />` with the `compassMode && (activeQuery || browseResults)` guard preserved.
4. The 6-test human verification above passes — both tabs show the controls bar, parity between /elections and /results?view=elections holds, uncalibrated users see no regression.
</verification>

<success_criteria>
- Results.jsx consumes `<CompassControlsBar />` from Plan 36-01 — zero duplicated inline markup remains
- DEFAULT-02 satisfied: Results Elections tab default-enables compass for calibrated users (was already true, refactor preserved it)
- DEFAULT-03 satisfied: Results Representatives tab default-enables compass for calibrated users (was already true, refactor preserved it)
- DEFAULT-05 satisfied: /elections and Results Elections tab show feature parity in controls bar rendering and behavior
- CTRL-01 + CTRL-02 satisfied: Single source of truth (`CompassControlsBar`) now hosts all compass controls across both pages
- No visual regression — `marginBottom: -70` preserved via the extracted component
</success_criteria>

<output>
After completion, create `.planning/phases/36-global-controls-compass-default/36-03-SUMMARY.md`
</output>
