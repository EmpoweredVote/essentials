---
phase: 219-elections-candidates-backfill
plan: 02
type: execute
wave: 2
depends_on: [219-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1393_collin_zero_race_shared_may2026.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1393_collin_zero_race_shared_may2026.ts
autonomous: false
requirements: [COLLIN-ELECT-01, COLLIN-ELECT-03]
user_setup: []

must_haves:
  truths:
    - "Blue Ridge (4808872), Farmersville (4825488), Nevada (4850760) and Van Alstyne (4874924) each have ≥1 seeded 2026-05-02 race with ≥1 candidate (COLLIN-ELECT-01)."
    - "Every seeded race links to the correct existing office_id and renders on /results (COLLIN-ELECT-03)."
    - "Uncontested/declared-elected seats (e.g. Farmersville's 2 unopposed incumbents) are seeded as a race + that single candidate (D-03)."
    - "Winners already seated in Phase 218 are linked via race_candidates.politician_id (carrying their existing headshot through the FK), candidate_status='active' (D-05)."
    - "Re-applying migration 1393 is net-zero (idempotent)."
  artifacts:
    - "C:/EV-Accounts/backend/migrations/1393_collin_zero_race_shared_may2026.sql"
    - "C:/EV-Accounts/backend/scripts/_apply-migration-1393_collin_zero_race_shared_may2026.ts"
  key_links:
    - "races.office_id → existing essentials.offices.id for each seat (join by geo_id + title)."
    - "race_candidates.politician_id → offices.politician_id for the 218-seated winner (photo reuse)."
  prohibitions:
    - statement: "candidate_status only ever uses a legal CHECK value (active/withdrawn/filed) — winner expressed via politician_id, never a status value."
      status: enforced
      verification: "apply-script gate: SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id WHERE r.election_id=<shared> AND rc.candidate_status NOT IN ('active','withdrawn','filed') MUST = 0 (schema also hard-fails illegal INSERT)."
    - statement: "position_name is always city-prefixed (e.g. 'Blue Ridge Mayor') so ON CONFLICT cannot swallow another city's race."
      status: enforced
      verification: "apply-script gate: every seeded position_name for this migration starts with its city name; per-geo_id race count matches the seeded roster."
    - statement: "primary_party stays NULL on every seeded municipal race (D-06 antipartisan)."
      status: enforced
      verification: "SELECT COUNT(*) FROM essentials.races WHERE election_id=<shared> AND office_id IN (<these 4 cities' offices>) AND primary_party IS NOT NULL MUST = 0."
    - statement: "No writes to inform.* (no stance side effect, D-07)."
      status: enforced
      verification: "apply-script before/after row-count assertion on inform.politician_answers unchanged."
    - statement: "No fabricated candidate — honest-blank where no source exists (5 zero-source cities incl. Blue Ridge, Farmersville, Nevada, Saint Paul)."
      status: enforced
      verification: "Every seeded candidate row carries a non-empty cited source; unresolved seats left race-less with a documented reason."
    - statement: "No frontend/backend code changes in the essentials repo (data-only)."
      status: enforced
      verification: "This plan writes only files under C:/EV-Accounts."
---

<objective>
Seed the 2026-05-02 races + candidates for the four zero-race cities whose reference cycle is the shared '2026 Texas Municipal General' election: Blue Ridge, Farmersville, Nevada, Van Alstyne. Winners for many seats are already seated by Phase 218 (reuse their politician_id + photo); this plan sources the opponents and remaining offices and links everything under the existing shared election row.

Purpose: Closes COLLIN-ELECT-01 for the four confirmed shared-cycle zero-race cities and COLLIN-ELECT-03 for their render path.
Output: migration 1393 + its apply-script with embedded SQL gates, committed to C:/EV-Accounts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-elections-candidates-backfill/219-RESEARCH.md
@.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md
@.planning/phases/219-elections-candidates-backfill/219-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock cited rosters for Blue Ridge, Farmersville, Nevada, Van Alstyne (2026-05-02)</name>
  <read_first>
    - .planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md (reference-cycle decision + candidate_staging reuse for these 4 cities).
    - 219-RESEARCH.md §"Zero-race governments" table rows for Blue Ridge / Farmersville / Nevada / Van Alstyne (known winners + [RE-VERIFY] flags) and §Pattern 3 / Pattern 4.
    - C:/EV-Accounts/backend/migrations/1389_collin_seat_cited_incumbents.sql (the Phase-218 politician_id for Blue Ridge Mayor/Place1/Place5, Van Alstyne Mayor — reuse those seats' office→politician_id).
    - Collin County canvass archive (collincountytx.gov/Elections/election-results-archive) + each city site (blueridgecity.com/council, farmersvilletx.com/city-secretary, cityofnevadatx.org, van-alstyne pages).
  </read_first>
  <action>
    For each of the four cities, using WebSearch/WebFetch + the Collin canvass PDF, assemble the FULL filed field per contested 2026-05-02 seat (winners AND losers, each cited — D-04) and the single declared-elected candidate for each uncontested seat (D-03). For every seat determine the correct existing office_id by joining essentials.offices→chambers→governments on geo_id + office title. Reuse Phase-218 winners via offices.politician_id (D-05); losers get denormalized name fields only, no politician_id. Set is_incumbent explicitly per seat (true only if the person held THAT seat before this election). Leave a seat race-less ONLY where no election was found for it in the reference cycle, and record that documented reason. Escape apostrophes ('→'') and preserve UTF-8 diacritics. Capture the cited roster as header comments for the migration (reference the source by concept/URL, not by pasting a raw status literal).
  </action>
  <verify>
    <automated>MISSING — roster capture is verified by Task 2's apply-script gates (per-geo_id race+candidate counts); no standalone test.</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 cities has a cited roster covering every seat with a confirmed 2026-05-02 election; each candidate row has a source URL.
    - Each winner reusable from Phase 218 is mapped to its offices.politician_id (Blue Ridge Mayor/Place1/Place5, Van Alstyne Mayor at minimum).
    - Any race-less seat has a written "no election found in cycle" reason.
  </acceptance_criteria>
  <done>Cited, seed-ready rosters exist for all four cities with winner→politician_id links identified.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 1393 + apply-script with embedded gates</name>
  <read_first>
    - 219-RESEARCH.md §Pattern 1 (resolve shared election by name/date/state, never hardcode UUID), §Pattern 2 (ON CONFLICT idiom), §Pattern 3 (winner/loser candidate attach), §Pattern 4 (uncontested seat).
    - C:/EV-Accounts/backend/migrations/1375_az_2026_local_races.sql (live ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING idiom).
    - C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts (the gate shape: split-section, antipartisan, no-stances before/after — mirror it).
    - The migration number confirmed in PREFLIGHT.md (research: 1393; re-confirm next-free at execute and shift the whole 1393-1399 block preserving order if drifted).
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1393_collin_zero_race_shared_may2026.sql wrapped in BEGIN/COMMIT. Resolve the shared election_id by (name='2026 Texas Municipal General', election_date='2026-05-02', state='TX') into a local variable. INSERT each race with a city-prefixed position_name (e.g. 'Blue Ridge Mayor', 'Van Alstyne Council Member Place 6') linked to its office_id via the geo_id+title subquery, seats set per seat, using ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING (leave primary_party NULL — D-06). INSERT each race_candidate with a WHERE NOT EXISTS (race_id, full_name) guard; winners carry politician_id from offices.politician_id (D-05), all candidates candidate_status='active' (the only legal way to seed — winner distinguished by politician_id, NOT a status value, per RESEARCH Pitfall 2), is_incumbent explicit, source = the cited URL. Then write C:/EV-Accounts/backend/scripts/_apply-migration-1393_collin_zero_race_shared_may2026.ts mirroring 1389's structure: capture inform.politician_answers count before apply; apply the SQL; then run gates that increment a failure counter and exit non-zero on any failure: (g-a) per-geo_id race count > 0 for all 4 geo_ids; (g-b) every seeded race has ≥1 candidate; (g-c) 0 candidate rows with candidate_status NOT IN ('active','withdrawn','filed'); (g-d) 0 seeded races with primary_party NOT NULL; (g-e) inform.politician_answers count unchanged; (g-f) the project-standard split-section query returns 0 rows; (g-g) re-run the SQL a second time in the same script and assert net-zero new rows (idempotency).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/_apply-migration-1393_collin_zero_race_shared_may2026.ts 2>&1 | head; test -f migrations/1393_collin_zero_race_shared_may2026.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; the .ts references the correct .sql filename and mirrors 1389's gate pattern.
    - Migration resolves the election by name/date/state (no hardcoded UUID literal in an INSERT target).
    - Every race INSERT uses the ON CONFLICT ... WHERE primary_party IS NULL DO NOTHING clause; every candidate INSERT uses WHERE NOT EXISTS.
    - The apply-script contains gates g-a..g-g as described.
  </acceptance_criteria>
  <done>Migration 1393 and its gated apply-script are authored and type-check clean, ready for the operator to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator applies 1393, confirms gates green, pushes, browse spot-check</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-1393_collin_zero_race_shared_may2026.ts (the gates being run).
    - [[no_git_in_ev_accounts]] / [[backend_architecture]] (commit + Render deploy path).
  </read_first>
  <action>Operator-run checkpoint: apply migration 1393 via `npx tsx`, confirm every apply-script gate exits 0, commit + push to C:/EV-Accounts (Render auto-deploys), then browse-spot-check Blue Ridge on /results — per the numbered how-to-verify steps.</action>
  <what-built>The executor cannot query the DB or push to the EV-Accounts remote on the operator's behalf with production credentials; the operator applies + verifies.</what-built>
  <how-to-verify>
    1. From C:/EV-Accounts/backend run `npx tsx scripts/_apply-migration-1393_collin_zero_race_shared_may2026.ts` — confirm it prints all gates green and exits 0 (any FAIL exits non-zero).
    2. Commit + push: `git -C "C:/EV-Accounts" add -A && git -C "C:/EV-Accounts" commit -m "feat(219): seed shared-cycle zero-race city races (Blue Ridge/Farmersville/Nevada/Van Alstyne)" && git -C "C:/EV-Accounts" push origin master` (Render auto-deploys).
    3. Browse spot-check: open `/results?browse_geo_id=4808872` (Blue Ridge) and confirm the Elections section shows the seeded races + candidates (not empty).
  </how-to-verify>
  <acceptance_criteria>
    - Apply-script exits 0 with every gate green (race count >0 per geo_id, 0 illegal candidate_status, 0 non-NULL primary_party, inform.* unchanged, split-section 0 rows, idempotent re-run net-zero).
    - Blue Ridge browse shows real races (backstop: human visual confirmation).
  </acceptance_criteria>
  <resume-signal>Type "approved" once gates are green and the browse check passes, or describe the failing gate.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `C:/EV-Accounts/backend/migrations/1393_collin_zero_race_shared_may2026.sql` (new)
- `C:/EV-Accounts/backend/scripts/_apply-migration-1393_collin_zero_race_shared_may2026.ts` (new)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external canvass/news → race_candidates | Untrusted civic claims; every row cited |
| migration SQL → production essentials schema | Idempotent, guarded writes only |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Tampering/Info disclosure | race_candidates rows | high | mitigate | Evidence-only (D-04/D-06); every candidate traces to a cited source; honest-blank gaps |
| T-219-02 | Tampering (schema integrity) | candidate_status | high | mitigate | Only legal CHECK values; winner via politician_id linkage; gate g-c asserts 0 illegal values |
| T-219-03 | Tampering (SQL literal injection) | apostrophe/diacritic names | medium | mitigate | Escape '→''; preserve UTF-8; type-check before apply |
| T-219-04 | Integrity (scope violation) | inform.* stance tables | high | mitigate | Before/after row-count assertion (gate g-e), D-07 |
| T-219-05 | Data integrity (silent loss) | races via ON CONFLICT DO NOTHING | high | mitigate | City-prefixed position_name; per-geo_id race-count gate g-a |
| T-219-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; legitimacy gate N/A |
</threat_model>

<verification>
- All apply-script gates green (see Task 3).
- Per-geo_id race count > 0 for 4808872, 4825488, 4850760, 4874924.
- Split-section query returns 0 rows; 0 non-NULL primary_party; inform.* net-zero.
</verification>

<success_criteria>
Blue Ridge, Farmersville, Nevada, Van Alstyne each render ≥1 real 2026-05-02 race with cited candidates on /results; migration is idempotent.
</success_criteria>

<output>
Create `.planning/phases/219-elections-candidates-backfill/219-02-SUMMARY.md` when done.
</output>
