---
phase: 193-pima-county-board-of-supervisors-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts
autonomous: true
requirements: [PIMA-01]
must_haves:
  truths:
    - "5 Pima County supervisor-district LOCAL geofences (mtfcc='X0019', state='az') are loaded into essentials.geofence_boundaries, one per DISTRICT 1-5"
    - "Each geofence geometry is valid WGS84 (SRID 4326) in the ~-110°/32° Tucson-area range, NOT the native AZ State Plane (2868) projection"
    - "The 5 geofences gate (pre-flight >=5) the structural migration in Plan 02 — without them Plan 02 must refuse to apply"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts"
      provides: "MapServer f=json rings→GeoJSON loader for the 5 supervisor districts, parameterized INSERT ON CONFLICT (geo_id, mtfcc) DO NOTHING"
      contains: "X0019"
  key_links:
    - from: "load-pima-supervisor-boundaries.ts"
      to: "gisdata.pima.gov GISOpenData/Boundaries/MapServer/5"
      via: "f=json + outSR=4326 query, arcgisRingsToGeoJson helper"
      pattern: "outSR=4326"
    - from: "load-pima-supervisor-boundaries.ts"
      to: "essentials.geofence_boundaries"
      via: "parameterized INSERT with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))"
      pattern: "ON CONFLICT \\(geo_id, mtfcc\\) DO NOTHING"
---

<objective>
Author and run the one-time ETL loader that sources the 5 official Pima County Board of Supervisors
district boundaries from the county's ArcGIS `MapServer` (layer 5 of `GISOpenData/Boundaries`) and
inserts them as custom LOCAL geofences (`mtfcc='X0019'`, `state='az'`, geo_id
`pima-az-supervisor-district-1..5`). These 5 geofences are the gating dependency for true
per-supervisor address routing (D-01) — the structural migration in Plan 02 refuses to apply until
they exist.

Purpose: Satisfies the geofence half of ROADMAP success criterion #1 (5 supervisor districts on
LOCAL geofences). RESEARCH live-verified the endpoint is reachable, returns exactly 5 single-ring
features with a `DISTRICT` (1-5) + `NAME` field, and reprojects cleanly with `outSR=4326` — so
D-01's PAUSE+flag fallback is a low-risk safety net, not the expected path.
Output: `C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts`, committed to the
`C:/EV-Accounts` repo and run by the orchestrator so 5 X0019 rows exist in production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-CONTEXT.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / GIS-network access. Task 1 (author the loader) is the only
executor file action. Task 2 is ORCHESTRATOR-RUN: the inline execute-phase orchestrator runs
`npx tsx` against `C:/EV-Accounts/backend/.env` `DATABASE_URL` and asserts the result. All backend
files live in the SEPARATE `C:/EV-Accounts` repo (`master`→Render) — commit via
`git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (live-verified in 193-RESEARCH.md / 193-PATTERNS.md — no exploration needed):
- Endpoint: `https://gisdata.pima.gov/arcgis1/rest/services/GISOpenData/Boundaries/MapServer/5/query`
  — a `MapServer` (NOT a `FeatureServer`), so `f=json` returns `geometry.rings` (ArcGIS JSON), and
  `f=geojson` is NOT available. Native SRID = 2868 (AZ State Plane); `outSR=4326` is MANDATORY.
  Fields: `DISTRICT` (integer 1-5), `NAME` (e.g. "REX SCOTT"). All 5 features are single-ring.
- Analog loader (copy shape verbatim): `C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts`
  — `arcgisRingsToGeoJson(rings) => JSON.stringify({type:'Polygon', coordinates: rings})`;
  the feature-map/validation loop; the parameterized INSERT with
  `ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3), 4326))` and `RETURNING ST_IsValid`; the conditional
  `ST_MakeValid` re-run guard.
- Config constants: MTFCC `'X0019'` (next unused; NOT `X0002` — excluded from routing — nor
  `X0018` — WashCo); STATE_CODE `'az'` (lowercase, required for LOCAL routing); SOURCE
  `'gisdata.pima.gov-boundaries-mapserver5-2026'`; GEO_ID_PREFIX `'pima-az-supervisor-district-'`;
  EXPECTED_COUNT 5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Pima supervisor-district geofence loader</name>
  <files>C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md (lines 22-75 — the full loader pattern: config block, arcgisRingsToGeoJson helper, DISTRICT→NAME feature-map keying, the verbatim parameterized INSERT + ST_MakeValid guard)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Architecture Pattern 3 + § Common Pitfalls 1, 3, 4 — outSR=4326 mandatory, lowercase state='az', X0019 not X0002/X0018)
    - C:/EV-Accounts/backend/scripts/load-lv-ward-boundaries.ts (primary analog — read in full: rings→GeoJSON MapServer shape, copy the helper + INSERT + ST_MakeValid guard verbatim)
    - C:/EV-Accounts/backend/scripts/load-washco-commissioner-boundaries.ts (secondary — read for the small-fixed-feature-count pre-flight style only; its f=geojson FeatureServer FETCH shape is the WRONG one for Pima, do NOT copy the fetch)
  </read_first>
  <action>
    Copy `load-lv-ward-boundaries.ts` and adapt. Set the query URL to the Pima layer-5 endpoint
    `https://gisdata.pima.gov/arcgis1/rest/services/GISOpenData/Boundaries/MapServer/5/query` with
    `where=1=1`, `outFields=DISTRICT,NAME`, `returnGeometry=true`, `f=json`, and `outSR=4326`
    (outSR is MANDATORY — Pitfall 1: without it, coordinates come back in native SRID 2868 and store
    as garbage that never matches an address).

    Set constants: MTFCC='X0019', STATE_CODE='az' (lowercase — Pitfall 3), SOURCE=
    'gisdata.pima.gov-boundaries-mapserver5-2026', GEO_ID_PREFIX='pima-az-supervisor-district-',
    EXPECTED_COUNT=5. Build geo_id per feature as `${GEO_ID_PREFIX}${feature.attributes.DISTRICT}`
    and name from `feature.attributes.NAME`.

    Reuse verbatim the `arcgisRingsToGeoJson(rings)` helper and the parameterized INSERT into
    essentials.geofence_boundaries (columns id, geo_id, mtfcc, state, name, geometry, source) with
    `public.ST_Multi(public.ST_SetSRID(public.ST_GeomFromGeoJSON($3), 4326))` and
    `ON CONFLICT (geo_id, mtfcc) DO NOTHING RETURNING public.ST_GeometryType(geometry),
    public.ST_IsValid(geometry)`. All GIS attribute/geometry values MUST be passed as bind params
    ($1 geo_id / $2 name / $3 geomJson / $4 source) — never string-concatenated into SQL (V5 input
    validation / T-193-SQLI). Keep the conditional `ST_MakeValid` re-run guard verbatim as defensive
    insurance (Pima's single-ring geometry is unlikely to trigger it, but keep it).

    Assert EXPECTED_COUNT: if the fetch returns fewer than 5 features, print the shortfall and
    exit non-zero (D-01 PAUSE+flag — never silently load a partial set). Print a summary line per
    district (DISTRICT, NAME, geo_id, ST_IsValid, first-coordinate sanity value) so the orchestrator
    can confirm the ~-110°/32° range in Task 2.

    Do NOT touch essentials.districts or any government/office table — this loader ONLY populates
    geofence_boundaries. The pre-existing whole-county COUNTY row (geo_id='04019') is untouched.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts" && grep -qF "outSR=4326" "C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts" && grep -qF "X0019" "C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts" && grep -qF "ON CONFLICT (geo_id, mtfcc) DO NOTHING" "C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts" && ! grep -qE "f=geojson|X0002|X0018" "C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/load-pima-supervisor-boundaries.ts` exists.
    - Grep confirms `outSR=4326` present in the query URL; `f=json` present; NO `f=geojson`.
    - Grep confirms `X0019` present; NEITHER `X0002` NOR `X0018` appears anywhere.
    - Grep confirms lowercase `'az'` STATE_CODE (no uppercase `'AZ'` used as the geofence state value).
    - Grep confirms the parameterized INSERT with `ST_GeomFromGeoJSON($3)` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING` — no string concatenation of GIS data into SQL.
    - `arcgisRingsToGeoJson` helper present; conditional `ST_MakeValid` guard present.
    - EXPECTED_COUNT=5 with a non-zero exit on shortfall (D-01 PAUSE+flag).
  </acceptance_criteria>
  <done>Loader authored: fetches 5 single-ring districts via f=json+outSR=4326, converts rings→GeoJSON, inserts 5 X0019/lowercase-az geofences idempotently with parameterized binds, pauses on shortfall.</done>
</task>

<task type="auto">
  <name>Task 2: Run the loader + assert 5 X0019 geofences (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator runs the loader + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md (Per-Task Verification Map — geofence row: 5 X0019 single-ring, DISTRICT 1-5)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Common Pitfall 1 — the ~-110°/32° WGS84 sanity range vs native 2868)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/network access). First `git -C "C:/EV-Accounts" add` +
    commit the loader. Then run `npx tsx backend/scripts/load-pima-supervisor-boundaries.ts` from the
    `C:/EV-Accounts/backend` checkout (uses `.env` `DATABASE_URL`). Confirm the script exits 0 with 5
    rows inserted (or already-present via ON CONFLICT). Then assert via
    `psql "$DATABASE_URL" -tAc`: (a) exactly 5 rows in essentials.geofence_boundaries WHERE
    mtfcc='X0019' AND state='az'; (b) all 5 geo_ids match `pima-az-supervisor-district-1..5`;
    (c) all 5 geometries are valid (ST_IsValid=true) and their envelope centroid longitude is between
    -112 and -110 and latitude between 31 and 33 (WGS84 sanity, NOT native 2868). If any assertion
    fails, PAUSE and flag — do NOT proceed to Plan 02. Record the 5 geo_id → ST_IsValid → centroid
    results in 193-01-SUMMARY.md.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0019' AND state='az')=5 AND NOT EXISTS (SELECT 1 FROM essentials.geofence_boundaries WHERE mtfcc='X0019' AND NOT public.ST_IsValid(geometry)) AND (SELECT bool_and(public.ST_X(public.ST_Centroid(geometry)) BETWEEN -112 AND -110 AND public.ST_Y(public.ST_Centroid(geometry)) BETWEEN 31 AND 33) FROM essentials.geofence_boundaries WHERE mtfcc='X0019')"</automated>
  </verify>
  <acceptance_criteria>
    - Loader exits 0; 5 X0019 rows present after the run.
    - `psql -tAc` count of X0019/state='az' geofences = 5.
    - All 5 geo_ids are exactly `pima-az-supervisor-district-1` through `-5`.
    - All 5 geometries valid; every centroid lon∈(-112,-110) and lat∈(31,33) (WGS84, confirming outSR=4326 worked and no 2868 garbage).
    - The combined boolean SELECT returns `t`.
    - Loader committed to `C:/EV-Accounts` via `git -C`.
  </acceptance_criteria>
  <done>5 valid WGS84 supervisor-district geofences (X0019, lowercase az) are live in production, gating Plan 02's structural migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pima GIS MapServer (remote) → loader | Untrusted ArcGIS JSON geometry + attribute strings enter the loader over HTTP |
| loader → production PostGIS | Converted geometry crosses into live `essentials.geofence_boundaries` via `npx tsx` (service-role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-SQLI | Tampering | GIS attribute/geometry → INSERT | mitigate | Parameterized binds ($1 geo_id / $2 name / $3 GeoJSON / $4 source) — never string-concatenate GIS response data into SQL (V5; matches LV/WashCo loaders) |
| T-193-PROJ | Tampering | native SRID 2868 vs WGS84 | mitigate | `outSR=4326` mandatory in the query; Task 2 asserts centroid ∈ Tucson-area WGS84 range or PAUSES |
| T-193-GEOM | Tampering | invalid/self-intersecting polygon | mitigate | `RETURNING ST_IsValid` + conditional `ST_MakeValid` re-run guard (kept verbatim; single-ring makes trigger unlikely) |
| T-193-MTFCC | Tampering | wrong X-code silently un-routable | mitigate | Use X0019 (outside the excluded X0001-X0004 routing range, above WashCo's X0018); grep gate forbids X0002/X0018 in the loader |
| T-193-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `C:/EV-Accounts/backend/.env`; never hardcoded in the loader |
| T-193-SC | Tampering | package installs | accept | No new npm/pip installs — reuses installed `pg`/`tsx`/`dotenv`; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 5 X0019/state='az' geofences loaded, geo_id `pima-az-supervisor-district-1..5`.
- All valid WGS84 geometry (centroid in the -112/-110 lon, 31/33 lat Tucson-area box).
- Loader committed to `C:/EV-Accounts`; results recorded in 193-01-SUMMARY.md.
</verification>

<success_criteria>
The geofence half of ROADMAP #1 is TRUE in production: 5 clean per-district LOCAL geofences exist,
enabling true one-supervisor-per-address routing and gating Plan 02.
</success_criteria>

<output>
Create `.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-01-SUMMARY.md` when done —
include the 5 geo_id → ST_IsValid → centroid manifest.
</output>
