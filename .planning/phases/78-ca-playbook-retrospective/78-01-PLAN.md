---
phase: 78-ca-playbook-retrospective
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LOCATION-ONBOARDING.md
autonomous: true
requirements:
  - PLAYBOOK-01

must_haves:
  truths:
    - "LOCATION-ONBOARDING.md Cities Onboarded table has 7 new CA rows (California state + 6 CA cities) appended after the Maine rows (per D-05)"
    - "A 'California Quick Reference' section sits between the Cities Onboarded table and Step 1 listing all 11 CA traps with step pointers (per D-04)"
    - "All 11 CA-specific GOTCHAs (CA-1 through CA-11 from 78-RESEARCH.md) are placed inline in the correct steps using the [STATE-SPECIFIC: CA] tag pattern (per D-01, D-02, D-03, D-04)"
    - "Step 7 pitfall table has at least 5 new CA-specific rows (jungle primary, pre-existing seed, ArcGIS outSR, AEM/CQ5 headshots, external_id collision)"
    - "All existing Cambridge and Maine content is preserved (no regression)"
  artifacts:
    - path: "LOCATION-ONBOARDING.md"
      provides: "Updated playbook with California Quick Reference block, 7 new Cities Onboarded rows, 11 CA GOTCHAs inline across Steps 1-7, and 5+ new pitfall table rows"
      contains: "California Quick Reference"
  key_links:
    - from: "LOCATION-ONBOARDING.md Cities Onboarded table"
      to: "California Quick Reference block"
      via: "section placement: Quick Reference immediately follows the table"
      pattern: "California Quick Reference"
    - from: "California Quick Reference table"
      to: "inline [STATE-SPECIFIC: CA] GOTCHA callouts"
      via: "step number pointers in 'See Step' column"
      pattern: "STATE-SPECIFIC: CA"
    - from: "Step 7 Common Pitfalls table"
      to: "CA-specific failure modes"
      via: "new pitfall rows for jungle primary, pre-existing seed, ArcGIS outSR, AEM/CQ5 headshots, external_id collision"
      pattern: "jungle primary|outSR|AEM"
---

<objective>
Implement requirement PLAYBOOK-01: update LOCATION-ONBOARDING.md with all 11 CA-specific GOTCHAs discovered during v7.0 (Phases 57–70), plus a top-level "California Quick Reference" summary block, plus 7 new rows in the Cities Onboarded table, plus 5+ new rows in the Step 7 pitfall table. Implements decisions D-01, D-02, D-03, D-04, and D-05 from 78-CONTEXT.md.

Note: PLAYBOOK-01 is the canonical requirement ID in REQUIREMENTS.md line 51 (Phase 78 inherits this requirement from the original Phase 71 mapping per ROADMAP line 786 — Phase 71 was folded into Phase 78).

Purpose: Phase 78 is the v7.0 playbook retrospective. The playbook is the cold-start contract for the next state onboarding (likely Oregon completion or a v7.1 CA city like Oakland). 11 CA-specific traps were discovered during v7.0 — they currently live only in phase SUMMARY.md files and STATE.md context. Without this update, the next agent will re-discover the same traps the hard way. The "California Quick Reference" block exists so a future agent can pre-flight scan all CA traps before starting any CA work.

Output: Updated LOCATION-ONBOARDING.md (currently 373 lines) with Cities Onboarded rows for California (state) + SF + San Jose + San Diego + Sacramento + Fremont + Berkeley; a new "California Quick Reference" H2 section between the Cities Onboarded table (ends line 38) and Step 1 (starts line 42); 11 inline GOTCHAs added across Steps 1-7 (some as new `> [GOTCHA]` callouts, some as annotations to existing GOTCHAs per 78-RESEARCH.md placement map); and 5 new rows appended to the Step 7 Common Pitfalls table.
</objective>

<execution_context>
@C:\Users\Chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-ca-playbook-retrospective/78-CONTEXT.md
@.planning/phases/78-ca-playbook-retrospective/78-RESEARCH.md
@.planning/phases/78-ca-playbook-retrospective/78-PATTERNS.md

# Playbook file being updated — primary deliverable
@LOCATION-ONBOARDING.md

# Phase 56 analog: prior playbook retrospective with the exact same edit approach
@.planning/phases/56-me-playbook-retrospective/56-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CA rows to Cities Onboarded table and insert California Quick Reference summary block</name>
  <files>LOCATION-ONBOARDING.md</files>
  <read_first>
    - LOCATION-ONBOARDING.md (the file being edited — read in full before any Edit; lines 25-40 are the Cities Onboarded table; lines 40-42 are the boundary between the table and Step 1 where the Quick Reference block must be inserted)
    - .planning/phases/78-ca-playbook-retrospective/78-RESEARCH.md (lines 390-432 contain the exact 7 table rows verbatim and the Quick Reference block content; lines 414-432 contain the exact Quick Reference markdown)
    - .planning/phases/78-ca-playbook-retrospective/78-PATTERNS.md (lines 44-60 describe the Cities Onboarded column format; lines 78-99 describe the Quick Reference table format)
    - .planning/phases/78-ca-playbook-retrospective/78-CONTEXT.md (D-04 mandates both inline AND summary, D-05 mandates 7 new rows)
  </read_first>
  <action>
Use the Edit tool to make two targeted insertions into LOCATION-ONBOARDING.md. Do NOT use Write — the file is 373 lines of working content that must be preserved.

**A. Append 7 new rows to the Cities Onboarded table.** The table ends at line 38 (Maine state row). Insert all 7 CA rows immediately after the Maine state row, BEFORE the `---` horizontal rule at line 40. Row ordering per D-05 is chronological by onboarding (CA state → SF → San Jose → San Diego → Sacramento → Fremont → Berkeley). Use the exact 7-row markdown table block from 78-RESEARCH.md lines 396-402 verbatim — those rows have already been authored with the correct column count (5 columns: City, State, Onboarded, Election method, Notable patterns) matching the existing table at lines 29-30. Do not paraphrase or shorten the Notable Patterns column content from RESEARCH.md — copy verbatim.

**B. Insert "California Quick Reference" H2 section between the Cities Onboarded table (ends line 38) and Step 1 (begins line 42).** Per D-04, this block sits between the table and Step 1 so a future agent reads it before starting any step. The section must contain:

- An H2 heading: `## California Quick Reference`
- A one-sentence introduction: "**Read this before starting any CA city.** These traps are CA-specific — general playbook guidance above does not warn for them."
- A markdown table with 3 columns: `Trap | See Step | One-Line Summary`
- 11 table rows, one per CA-1 through CA-11 trap, using the exact wording from 78-RESEARCH.md lines 419-432 (verbatim — already authored)
- A trailing `---` horizontal rule before Step 1 begins

The 11 trap rows (ordered as in RESEARCH.md lines 419-432, do not reorder):
1. Pre-existing CA seed → Step 1, Step 5
2. districts.state casing → Step 3, Step 6
3. mtfcc swap → Step 3
4. External ID range collision → Step 5
5. DataSF vs ArcGIS (outSR) → Step 3
6. SF consolidated city-county → Step 3
7. CA COUSUB = CCDs → Step 3
8. CA jungle primary → Step 2
9. RCV at seed time → Step 2, Step 6
10. AEM/CQ5 headshots (Sacramento) → Step 4
11. lavote.gov election ID → Step 2

Use copy-from-source for content, then verify by reading the inserted section back. Do NOT generate new wording for trap descriptions — RESEARCH.md is canonical.

**Constraints:**
- Do NOT modify any rows above line 38 (existing Cambridge + Maine rows must remain unchanged)
- Do NOT change the table header row at line 29
- Insert the Quick Reference block AFTER the table's trailing blank line and BEFORE the horizontal rule that precedes Step 1 — verify the boundary with Read after editing
- The H2 must be exactly `## California Quick Reference` (no emoji, no period)
- After both edits, the Cities Onboarded section count remains 1 (no duplicates) and the Quick Reference section count is exactly 1
  </action>
  <acceptance_criteria>
    - LOCATION-ONBOARDING.md contains the exact substring `| California (state) | CA | 2026-05-21 |` (Cities Onboarded row 1 of 7)
    - LOCATION-ONBOARDING.md contains the exact substring `| San Francisco | CA | 2026-05-22 |` (Cities Onboarded row 2 of 7)
    - LOCATION-ONBOARDING.md contains the exact substring `| San Jose | CA | 2026-05-23 |` (row 3 of 7)
    - LOCATION-ONBOARDING.md contains the exact substring `| San Diego | CA | 2026-05-22 |` (row 4 of 7)
    - LOCATION-ONBOARDING.md contains the exact substring `| Sacramento | CA | 2026-05-28 |` (row 5 of 7)
    - LOCATION-ONBOARDING.md contains the exact substring `| Fremont | CA | 2026-05-22 |` (row 6 of 7)
    - LOCATION-ONBOARDING.md contains the exact substring `| Berkeley | CA | 2026-05-22 |` (row 7 of 7)
    - LOCATION-ONBOARDING.md contains exactly one occurrence of `## California Quick Reference` (count via `grep -c "^## California Quick Reference$"` returns 1)
    - LOCATION-ONBOARDING.md contains the Quick Reference introduction `Read this before starting any CA city.`
    - The Quick Reference table contains all 11 trap labels: `Pre-existing CA seed`, `districts.state casing`, `mtfcc swap`, `External ID range collision`, `DataSF vs ArcGIS`, `SF consolidated city-county`, `CA COUSUB = CCDs`, `CA jungle primary`, `RCV at seed time`, `AEM/CQ5 headshots`, `lavote.gov election ID` (each verifiable via `grep -c "<label>" LOCATION-ONBOARDING.md` returning at least 1)
    - The existing Cambridge row (`| Cambridge | MA | 2026-05-17 |`) is still present (no regression)
    - The existing Portland ME row (`| Portland | ME | 2026-05-19 | rcv |`) is still present (no regression)
    - The existing Maine state row (`| Maine (state) | ME | 2026-05-20 |`) is still present (no regression)
    - `grep -c "^| " LOCATION-ONBOARDING.md` shows the Cities Onboarded section has 7 more rows than before (8 existing + 7 new = 15 rows under the Cities Onboarded H2; account for other tables in the file when verifying section-specific count)
    - The California Quick Reference H2 sits between line containing `Maine (state)` row and the line containing `## Step 1: Government Structure Research` (verify with line-number reads after edit)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -c "^## California Quick Reference$" LOCATION-ONBOARDING.md | grep -q "^1$" && grep -q "| California (state) | CA | 2026-05-21 |" LOCATION-ONBOARDING.md && grep -q "| San Francisco | CA | 2026-05-22 |" LOCATION-ONBOARDING.md && grep -q "| San Jose | CA | 2026-05-23 |" LOCATION-ONBOARDING.md && grep -q "| San Diego | CA | 2026-05-22 |" LOCATION-ONBOARDING.md && grep -q "| Sacramento | CA | 2026-05-28 |" LOCATION-ONBOARDING.md && grep -q "| Fremont | CA | 2026-05-22 |" LOCATION-ONBOARDING.md && grep -q "| Berkeley | CA | 2026-05-22 |" LOCATION-ONBOARDING.md && grep -q "Read this before starting any CA city" LOCATION-ONBOARDING.md && grep -q "| Cambridge | MA | 2026-05-17 |" LOCATION-ONBOARDING.md && grep -q "| Portland | ME | 2026-05-19 | rcv |" LOCATION-ONBOARDING.md && echo PASS</automated>
  </verify>
  <done>
LOCATION-ONBOARDING.md Cities Onboarded table has the original 8 rows (Cambridge + 7 Maine) plus 7 new CA rows (15 total) appended in the order CA state → SF → San Jose → San Diego → Sacramento → Fremont → Berkeley. A new H2 section "California Quick Reference" sits between the table and Step 1, containing the introduction sentence plus an 11-row trap → step → summary table. All 11 trap labels match RESEARCH.md verbatim. No regressions to existing Cambridge or Maine rows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert 11 inline CA-specific GOTCHAs across Steps 1-7 using [STATE-SPECIFIC: CA] tag pattern</name>
  <files>LOCATION-ONBOARDING.md</files>
  <read_first>
    - LOCATION-ONBOARDING.md (file being edited — Task 1 already added the Cities Onboarded rows and Quick Reference; this task adds inline GOTCHAs to Steps 1-7)
    - .planning/phases/78-ca-playbook-retrospective/78-RESEARCH.md (lines 181-388 contain the full text of all 11 CA GOTCHAs with problem, solution, and CA example — this is the canonical source; lines 437-454 contain the exact step-by-step placement map)
    - .planning/phases/78-ca-playbook-retrospective/78-PATTERNS.md (lines 26-42 confirm the GOTCHA format: `> [GOTCHA] **[STATE-SPECIFIC: CA] Short label:** problem... solution... CA example...`; lines 62-77 are the placement map)
    - .planning/phases/78-ca-playbook-retrospective/78-CONTEXT.md (D-01 mandates ALL 11; D-04 mandates inline placement at relevant existing steps)
  </read_first>
  <action>
Use the Edit tool to make 11 targeted insertions into LOCATION-ONBOARDING.md. Some insertions are new `> [GOTCHA]` blockquote lines; others are CA annotations appended to existing GOTCHAs (per the RESEARCH.md placement map at lines 437-454).

**Format conventions (must match existing playbook style):**
- New CA-specific GOTCHA line begins: `> [GOTCHA] **[STATE-SPECIFIC: CA] <short label>:** <problem>. <solution>. <CA example referencing phase>.`
- Annotation to existing GOTCHA: append a new sentence inside the existing blockquote line beginning with `**[STATE-SPECIFIC: CA]** <annotation>. <CA example>.`
- Reference the specific source phase in every example (Phase 57, Phase 59, Phase 60, Phase 61, Phase 62, Phase 63, Phase 66, Phase 68) so future agents can trace back if needed
- Use the exact body content from 78-RESEARCH.md lines 181-388 — paraphrase only for length, never invent new facts

**Placement map (from 78-RESEARCH.md lines 437-454 and 78-PATTERNS.md lines 64-76):**

| GOTCHA | Step | Insertion approach | Format |
|--------|------|--------------------|--------|
| CA-1: Pre-existing CA seed | Step 1 (Required Questions area, around line 50-55) AND Step 5 (Schema Decisions, around line 215-220 alongside existing GOTCHAs) | New `> [GOTCHA]` in Step 1 + new `> [GOTCHA]` in Step 5 | New |
| CA-2: districts.state='CA' uppercase | Step 3 — annotate the existing districts.state casing GOTCHA at line 165 | Append CA annotation inside the existing GOTCHA blockquote | Annotation |
| CA-3: mtfcc swap (G5220/G5210 reversed) | Step 3 — new inline GOTCHA after the existing mtfcc-related lines around line 167-170 | New `> [GOTCHA]` | New |
| CA-4: External ID range -1000xx collision | Step 5 — new GOTCHA in the Schema Decisions area (after the legislature-elected and senator uniqueness GOTCHAs around line 217-218) | New `> [GOTCHA]` | New |
| CA-5: DataSF Socrata vs ArcGIS outSR=4326 | Step 3 — new inline GOTCHA, place after the cousub GOTCHA around line 167 | New `> [GOTCHA]` | New |
| CA-6: SF consolidated city-county returns G4110 + G4020 | Step 3 — new inline GOTCHA | New `> [GOTCHA]` | New |
| CA-7: CA COUSUB = CCDs (FUNCSTAT='S') | Step 3 — annotate the existing G4040 COUSUB GOTCHA at line 167 | Append CA annotation inside the existing GOTCHA blockquote | Annotation |
| CA-8: CA jungle primary (one unified race row) | Step 2 — new inline GOTCHA, place near the RCV GOTCHA at line 95 | New `> [GOTCHA]` | New |
| CA-9: Berkeley/SF RCV at seed time | Step 2 — annotate the existing RCV GOTCHA at line 95 | Append CA annotation inside the existing GOTCHA blockquote | Annotation |
| CA-10: AEM/CQ5 CMS headshots (Sacramento) | Step 4 — new inline GOTCHA, place after the Cambridge example block (after line 198) | New `> [GOTCHA]` | New |
| CA-11: lavote.gov election ID maintenance | Step 2 — new inline GOTCHA | New `> [GOTCHA]` | New |

**Content sources (do NOT rewrite — copy from RESEARCH.md verbatim or near-verbatim, condensed to single-line blockquote where natural):**
- CA-1 from 78-RESEARCH.md lines 185-200
- CA-2 from 78-RESEARCH.md lines 204-216
- CA-3 from 78-RESEARCH.md lines 220-230
- CA-4 from 78-RESEARCH.md lines 234-253
- CA-5 from 78-RESEARCH.md lines 257-273
- CA-6 from 78-RESEARCH.md lines 277-287
- CA-7 from 78-RESEARCH.md lines 291-303
- CA-8 from 78-RESEARCH.md lines 307-325
- CA-9 from 78-RESEARCH.md lines 329-344
- CA-10 from 78-RESEARCH.md lines 348-364 (use the exact curl+grep command from 78-RESEARCH.md line 356 and the Specifics block in 78-CONTEXT.md line 107: `curl -s <url> | grep -o 'background-image:url([^)]+)'`)
- CA-11 from 78-RESEARCH.md lines 368-386 (per 78-CONTEXT.md line 108, MUST explicitly note ID must be updated per cycle — June primary AND November general — two updates per year)

**Concrete behavior requirements (from 78-CONTEXT.md specifics):**
- CA-1 GOTCHA body must include the literal SQL pre-check string `SELECT id, geo_id FROM essentials.governments WHERE name = 'State of California'` (per 78-CONTEXT.md line 109)
- CA-8 GOTCHA body must call out `sos.ca.gov` as the authoritative source (NOT Ballotpedia) per 78-CONTEXT.md line 110
- CA-10 GOTCHA body must include the literal command `curl -s <url> | grep -o 'background-image:url([^)]+)'` per 78-CONTEXT.md line 107
- CA-11 GOTCHA body must explicitly state "two updates per year" (June primary AND November general) per 78-CONTEXT.md line 108
- Every CA GOTCHA must include `[STATE-SPECIFIC: CA]` in the bold label

**Constraints:**
- Use Edit (not Write) for every change — preserve all existing content
- Do NOT remove or alter any existing Cambridge or Maine GOTCHA, example, or pitfall content
- Do NOT add CA examples to Maine examples (do not mix state contexts within a single blockquote — CA gets its own callouts)
- After all 11 edits, `grep -c "STATE-SPECIFIC: CA" LOCATION-ONBOARDING.md` returns at least 11 (the count may be higher if annotations + new GOTCHAs both fire — that is acceptable)
- After edits, the existing Maine `[STATE-SPECIFIC]` and `[STATE-SPECIFIC: Maine]` markers must still be present
  </action>
  <acceptance_criteria>
    - `grep -c "STATE-SPECIFIC: CA" LOCATION-ONBOARDING.md` returns >= 11 (one per CA GOTCHA)
    - `grep -c "cd119" LOCATION-ONBOARDING.md` still returns >= 1 (Maine TIGER cd119 GOTCHA not regressed)
    - `grep -c "STATE-SPECIFIC.*Maine\|STATE-SPECIFIC] TIGER\|STATE-SPECIFIC: Maine" LOCATION-ONBOARDING.md` returns >= 2 (existing Maine STATE-SPECIFIC tags preserved)
    - CA-1 phrase present: `grep -q "State of California" LOCATION-ONBOARDING.md` returns 0 AND `grep -q "geo_id=NULL\|geo_id = NULL\|geo_id IS NULL" LOCATION-ONBOARDING.md` returns 0 (the CA-1 GOTCHA mentions both)
    - CA-1 contains the literal SQL pre-check: `grep -q "SELECT id, geo_id FROM essentials.governments" LOCATION-ONBOARDING.md` returns 0
    - CA-2 phrase present: `grep -q "state='CA'\|state = 'CA'" LOCATION-ONBOARDING.md` returns 0 (uppercase 'CA' casing called out)
    - CA-3 phrase present: `grep -q "G5220\|G5210" LOCATION-ONBOARDING.md` returns 0 (both swap mtfcc codes mentioned)
    - CA-4 phrase present: `grep -q "60003\|-60003xx\|-6000301" LOCATION-ONBOARDING.md` returns 0 (CA House rep external_id scheme called out)
    - CA-5 phrase present: `grep -q "outSR=4326\|outSR" LOCATION-ONBOARDING.md` returns 0 AND `grep -q "DataSF\|Socrata" LOCATION-ONBOARDING.md` returns 0
    - CA-6 phrase present: `grep -q "0667000\|06075\|consolidated city-county" LOCATION-ONBOARDING.md` returns 0
    - CA-7 phrase present: `grep -q "COUSUB_FUNCSTAT_STATES\|CCDs\|FUNCSTAT" LOCATION-ONBOARDING.md` returns 0
    - CA-8 phrase present: `grep -q "jungle primary\|top-two" LOCATION-ONBOARDING.md` returns 0 AND `grep -q "sos.ca.gov" LOCATION-ONBOARDING.md` returns 0 (authoritative source called out)
    - CA-9 phrase present: `grep -q "Berkeley\|seed time" LOCATION-ONBOARDING.md` returns 0 (Berkeley RCV punt referenced)
    - CA-10 phrase present: `grep -q "AEM\|CQ5\|cityofsacramento.gov\|background-image" LOCATION-ONBOARDING.md` returns 0
    - CA-10 contains the curl+grep command: `grep -q "grep -o 'background-image:url" LOCATION-ONBOARDING.md` returns 0
    - CA-11 phrase present: `grep -q "lavote.gov\|discovery_jurisdictions" LOCATION-ONBOARDING.md` returns 0 AND `grep -q "June.*November\|two updates per year\|per cycle" LOCATION-ONBOARDING.md` returns 0
    - No existing Cambridge content removed: `grep -c "Cambridge" LOCATION-ONBOARDING.md` returns the same count as before edits (or higher)
    - No existing Maine content removed: `grep -c "Maine\|Portland\|cd119" LOCATION-ONBOARDING.md` returns the same count as before edits (or higher)
    - File line count is GREATER than 373 (the starting count) — confirms additions without unintended deletions

    Note: the `returns 0` phrasing in the criteria above refers to grep exit status 0 (match found), which is the correct success condition for `grep -q`. Each of the listed `grep -q` invocations must exit 0 (a match was found).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && [ "$(grep -c 'STATE-SPECIFIC: CA' LOCATION-ONBOARDING.md)" -ge 11 ] && grep -q "SELECT id, geo_id FROM essentials.governments" LOCATION-ONBOARDING.md && grep -q "state='CA'\|state = 'CA'" LOCATION-ONBOARDING.md && grep -qE "G5220|G5210" LOCATION-ONBOARDING.md && grep -qE "60003|-60003xx" LOCATION-ONBOARDING.md && grep -q "outSR=4326" LOCATION-ONBOARDING.md && grep -qE "DataSF|Socrata" LOCATION-ONBOARDING.md && grep -qE "0667000|06075|consolidated city-county" LOCATION-ONBOARDING.md && grep -qE "COUSUB_FUNCSTAT_STATES|FUNCSTAT" LOCATION-ONBOARDING.md && grep -qE "jungle primary|top-two" LOCATION-ONBOARDING.md && grep -q "sos.ca.gov" LOCATION-ONBOARDING.md && grep -qE "AEM|CQ5|background-image" LOCATION-ONBOARDING.md && grep -q "grep -o 'background-image:url" LOCATION-ONBOARDING.md && grep -q "lavote.gov" LOCATION-ONBOARDING.md && [ "$(grep -c 'Cambridge' LOCATION-ONBOARDING.md)" -ge 30 ] && [ "$(grep -c 'cd119' LOCATION-ONBOARDING.md)" -ge 1 ] && [ "$(wc -l < LOCATION-ONBOARDING.md)" -gt 373 ] && echo PASS</automated>
  </verify>
  <done>
LOCATION-ONBOARDING.md contains 11 distinct `[STATE-SPECIFIC: CA]` markers — one for each CA-1 through CA-11 trap. New `> [GOTCHA]` blockquotes added for CA-1, CA-3, CA-4, CA-5, CA-6, CA-8, CA-10, CA-11 (8 new). CA-2, CA-7, CA-9 are annotations appended to existing Maine/general GOTCHAs. CA-1 SQL pre-check, CA-8 sos.ca.gov authoritative source, CA-10 curl+grep command, and CA-11 two-updates-per-year are all present verbatim. All existing Cambridge and Maine GOTCHAs preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 5+ new CA-specific rows to Step 7 Common Pitfalls table</name>
  <files>LOCATION-ONBOARDING.md</files>
  <read_first>
    - LOCATION-ONBOARDING.md (the file being edited — Step 7 pitfall table begins at line 293; existing rows at lines 297-315)
    - .planning/phases/78-ca-playbook-retrospective/78-RESEARCH.md (lines 539-543 contain 5 new CA pitfall table rows in exact format)
    - .planning/phases/78-ca-playbook-retrospective/78-PATTERNS.md (lines 102-115 confirm the pitfall table format: `| <pitfall> | <how to catch it> |`)
  </read_first>
  <action>
Use the Edit tool to append 5 new rows to the Step 7 Common Pitfalls table in LOCATION-ONBOARDING.md.

The existing pitfall table starts at line 297 with the header `| Pitfall | How to Catch It |` and runs through line 315 (or thereabouts — confirm exact end-of-table line by reading lines 290-320 first). Append the 5 new rows BEFORE the blank line that ends the table, so they extend the existing table rather than create a new one.

**Source rows (copy verbatim from 78-RESEARCH.md lines 539-543):**

```
| CA jungle primary modeled as separate D/R primaries | CA uses top-two jungle primary — ONE unified race row for ALL candidates regardless of party; sos.ca.gov is authoritative |
| CA pre-existing seed silently duplicated | Before any CA state-level INSERT, run `SELECT id, geo_id FROM essentials.governments WHERE name = 'State of California'`; if geo_id IS NULL, UPDATE — do not INSERT |
| ArcGIS outSR=4326 omitted for CA city boundaries | CA State Plane feet (SRID 2229) looks valid to PostGIS but ST_Covers returns 0 rows for all addresses — always add outSR=4326 to ArcGIS MapServer queries for CA cities |
| AEM/CQ5 CMS headshots not extractable by WebFetch | Sacramento cityofsacramento.gov uses CSS background-image — use curl+grep pattern |
| CA external_id range -1000xx occupied | CA Assembly pre-existing seed occupied -1000xx; CA House reps use -60003xx scheme |
```

Each row must match the existing 2-column pipe-delimited format. Do NOT add a third column. Do NOT add CA examples to the existing Maine/Cambridge pitfall rows (those rows describe general pitfalls; CA-specific rows go alongside as their own rows).

**Constraints:**
- Use Edit (not Write)
- Do NOT modify the table header row at line 297
- Do NOT remove or alter any of the existing 9 pitfall rows
- Append all 5 rows together as one Edit operation (find existing last row, replace with last-row + 5-new-rows)
- After edits, `grep -c "^| " LOCATION-ONBOARDING.md` (when scoped to the Step 7 area) should show 5 more rows than before
  </action>
  <acceptance_criteria>
    - LOCATION-ONBOARDING.md contains the exact substring `| CA jungle primary modeled as separate D/R primaries |`
    - LOCATION-ONBOARDING.md contains the exact substring `| CA pre-existing seed silently duplicated |`
    - LOCATION-ONBOARDING.md contains the exact substring `| ArcGIS outSR=4326 omitted for CA city boundaries |`
    - LOCATION-ONBOARDING.md contains the exact substring `| AEM/CQ5 CMS headshots not extractable by WebFetch |`
    - LOCATION-ONBOARDING.md contains the exact substring `| CA external_id range -1000xx occupied |`
    - All 5 new rows sit between the existing pitfall table header and the start of `## Step 8` (verify by reading lines 290-325 after edit; all 5 new rows appear within that range)
    - Existing pitfall rows preserved: `grep -q "Mayor modeled as LOCAL_EXEC when actually council-selected" LOCATION-ONBOARDING.md` matches
    - Existing pitfall rows preserved: `grep -q "Wrong geo_id (county FIPS instead of city place code)" LOCATION-ONBOARDING.md` matches
    - Total pitfall table row count increases by exactly 5
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -q "| CA jungle primary modeled as separate D/R primaries |" LOCATION-ONBOARDING.md && grep -q "| CA pre-existing seed silently duplicated |" LOCATION-ONBOARDING.md && grep -q "| ArcGIS outSR=4326 omitted for CA city boundaries |" LOCATION-ONBOARDING.md && grep -q "| AEM/CQ5 CMS headshots not extractable by WebFetch |" LOCATION-ONBOARDING.md && grep -q "| CA external_id range -1000xx occupied |" LOCATION-ONBOARDING.md && grep -q "Mayor modeled as LOCAL_EXEC when actually council-selected" LOCATION-ONBOARDING.md && grep -q "Wrong geo_id (county FIPS instead of city place code)" LOCATION-ONBOARDING.md && echo PASS</automated>
  </verify>
  <done>
Step 7 Common Pitfalls table has all 9 original rows plus 5 new CA-specific rows (14 total). The 5 new rows are: jungle primary, pre-existing seed, ArcGIS outSR=4326, AEM/CQ5 headshots, external_id collision. Format matches existing 2-column pipe-delimited rows.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run holistic playbook integrity check:

```bash
cd "C:/Transparent Motivations/essentials"

# 1. All 11 CA GOTCHAs landed (one [STATE-SPECIFIC: CA] marker per trap)
test "$(grep -c 'STATE-SPECIFIC: CA' LOCATION-ONBOARDING.md)" -ge 11 || { echo "FAIL: <11 CA GOTCHAs"; exit 1; }

# 2. California Quick Reference block exists exactly once
test "$(grep -c '^## California Quick Reference$' LOCATION-ONBOARDING.md)" -eq 1 || { echo "FAIL: CA Quick Ref count != 1"; exit 1; }

# 3. All 7 new Cities Onboarded rows present
for city in "California (state)" "San Francisco" "San Jose" "San Diego" "Sacramento" "Fremont" "Berkeley"; do
  grep -q "| $city" LOCATION-ONBOARDING.md || { echo "FAIL: missing city row $city"; exit 1; }
done

# 4. All 5 new Step 7 pitfall rows present
grep -q "jungle primary modeled as separate" LOCATION-ONBOARDING.md || { echo "FAIL: jungle primary pitfall"; exit 1; }
grep -q "outSR=4326 omitted" LOCATION-ONBOARDING.md || { echo "FAIL: outSR pitfall"; exit 1; }
grep -q "AEM/CQ5 CMS headshots not extractable" LOCATION-ONBOARDING.md || { echo "FAIL: AEM pitfall"; exit 1; }

# 5. No regression — existing content preserved
grep -q "Cambridge" LOCATION-ONBOARDING.md && grep -q "cd119" LOCATION-ONBOARDING.md && grep -q "Frey\|Bellows" LOCATION-ONBOARDING.md || { echo "FAIL: regression on Cambridge/Maine"; exit 1; }

# 6. File grew (no accidental truncation)
LINES=$(wc -l < LOCATION-ONBOARDING.md)
test "$LINES" -gt 373 || { echo "FAIL: file did not grow (was 373, now $LINES)"; exit 1; }

echo ALL_PASS
```
</verification>

<success_criteria>
- LOCATION-ONBOARDING.md Cities Onboarded table has 7 new CA rows in chronological order (per D-05)
- "California Quick Reference" H2 section exists between Cities Onboarded table and Step 1 with 11 trap rows (per D-04)
- All 11 CA GOTCHAs (CA-1 through CA-11 from 78-RESEARCH.md) are present inline at the correct steps with `[STATE-SPECIFIC: CA]` markers (per D-01, D-02, D-03)
- Step 7 Common Pitfalls table has 5 new CA-specific rows
- All existing Cambridge and Maine content preserved (no regressions)
- File line count increased from 373 (baseline) to a higher number reflecting the additions
- Quality bar: a future agent onboarding a new CA city (e.g., Oakland) can scan the California Quick Reference block, click through to the inline GOTCHAs, and pre-flight all 11 traps before writing any migration (per requirement PLAYBOOK-01)
</success_criteria>

<output>
After completion, create `.planning/phases/78-ca-playbook-retrospective/78-01-SUMMARY.md` documenting:
- Final line count of LOCATION-ONBOARDING.md (delta from 373)
- Confirmation that 7 Cities Onboarded rows landed in the table
- Confirmation that California Quick Reference block landed between table and Step 1
- For each of CA-1 through CA-11: which step the inline GOTCHA landed in, and whether it was a new `> [GOTCHA]` or an annotation to an existing one
- Count of new Step 7 pitfall rows (should be 5)
- Any deviations from the placement map (with rationale)
- Confirmation that no Cambridge or Maine content was removed (grep counts before vs. after)
</output>
</output>
