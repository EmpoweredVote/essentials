---
phase: 210-per-tab-compass-integration
plan: 02
type: execute
wave: 2
depends_on: ["210-01"]
files_modified: []
autonomous: false
requirements: [CMP-01, CMP-02]

must_haves:
  truths:
    - "Switching Reps→Judges lights the Judicial chip and renders judicial spokes; switching back restores Representatives' Best Match/prior lens"
    - "The Educators tab shows honest blanks / Best Match (no fabricated spokes) and a highlighted chip, never a broken or empty compass"
    - "An explicit lens pick on a tab is remembered for that tab across leaving and returning, and does not affect other tabs"
    - "Per-tab picks survive an in-session location/browse change but reset to per-tab defaults on a full page reload"
    - "Arriving at the Elections tab from a people-tab with an explicit lens pick does not reset or hijack the lens switcher (Elections is exempt from per-tab lens memory)"
  artifacts:
    - path: ".planning/phases/210-per-tab-compass-integration/210-02-SUMMARY.md"
      provides: "Live human-verify sign-off record for the runtime tab/lens behaviors"
  key_links:
    - from: "Judges tab entry"
      to: "activeLensKey === 'judicial'"
      via: "resolveTabLens → setActiveLens, observed as a lit Judicial chip + judicial spokes"
      pattern: "judicial"
---

<objective>
Human-verify the React-runtime behaviors that cannot be asserted headlessly:
per-tab default-lens shift, explicit-pick memory per tab, cross-location persistence,
reset-on-reload, and honest-blanks / chip-highlight integrity on the Educators tab.

Purpose: Confirms CMP-01 and CMP-02 on the live app exactly as Phases 204 and 208 were
verified (no component-test harness exists for Results.jsx; this is the phase gate).
Output: A recorded sign-off (or a defect list to feed a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-per-tab-compass-integration/210-CONTEXT.md
@.planning/phases/210-per-tab-compass-integration/210-VALIDATION.md
@.planning/phases/210-per-tab-compass-integration/210-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-verify per-tab lens behavior on a location with judges</name>
  <read_first>
    - .planning/phases/210-per-tab-compass-integration/210-CONTEXT.md:154-161 — the exact endorsed scenario (Reps[Best Match] → Judges[Judicial] → back; pick Custom on Judges, leave/return → Custom; reload → Judicial)
    - .planning/phases/210-per-tab-compass-integration/210-VALIDATION.md:59-69 — the Manual-Only verification table (five behaviors + why each is manual)
    - .planning/phases/210-per-tab-compass-integration/210-RESEARCH.md:294-318 — Pitfall 3 (chip-highlight integrity: confirm a chip is always lit, never "nothing selected")
    - .planning/phases/210-per-tab-compass-integration/210-RESEARCH.md:200-205 — Assumption A1 (verify tabLensMemory survives an in-session location change — Results.jsx is not remounted)
  </read_first>
  <files>none — live human verification only (no repository files modified)</files>
  <action>Operator (human) performs the live eight-step walkthrough described in <how-to-verify> on essentials.empowered.vote at a location with judges (and educators), confirming per-tab default-lens shift, explicit-pick memory, cross-location persistence, reset-on-reload, and chip-highlight/honest-blanks integrity. Claude does not edit code in this plan; it records the outcome (approval or a defect list) into the SUMMARY. If any step fails, capture the exact step and observed behavior for a follow-up gap-closure plan.</action>
  <what-built>
    Plan 01 added per-tab lens memory: each people-tab (Representatives / Educators / Judges) applies
    its remembered-or-default lens on entry via resolveTabLens + a tab-entry effect, and explicit lens
    picks are stored per tab in memory (no localStorage). Judges defaults to the live Judicial lens;
    Educators defaults to 'education' which resolves to Best Match/Custom while the lens is unlit;
    Representatives is unchanged.
  </what-built>
  <how-to-verify>
    Open essentials.empowered.vote at a location that HAS judges (e.g. an LA-County address with
    judicial office-holders) with compass mode ON and the lens switcher visible. Then:
    1. On Representatives, note the active chip (Best Match/Custom or your prior default).
    2. Switch to Judges → the **Judicial** chip highlights and cards render judicial spokes (not a blank/no-compass plate).
    3. Switch back to Representatives → the Representatives Best Match/prior chip is restored.
    4. On Judges, explicitly pick **Custom** (or another lens) → leave to Representatives, return to Judges → Judges still shows your explicit pick; confirm Representatives was NOT changed by that pick.
    5. Switch to the Educators tab (on a location that has educators) → a chip is **highlighted** (Best Match — never "no chip selected") and the compass shows honest blanks / no-stances rather than a broken or empty overlay.
    6. With your explicit Custom pick still on Judges, browse/search to a DIFFERENT location within the same session (no page reload) → return to Judges → the Custom pick still persists (in-session cross-location memory).
    7. Do a FULL page reload → return to Judges → it is back to the **Judicial** default (in-memory reset, no localStorage).
    8. On Judges, make an explicit lens pick (e.g. **Custom**), then switch to the **Elections** tab → confirm the lens switcher is **NOT** unexpectedly reset to Best Match by arriving at Elections (Elections is exempt from per-tab lens memory); returning to Judges still shows your explicit pick.
    Also sanity-check: PostHog `essentials_compass_lens_selected` fires exactly once per explicit chip click (no effect-loop double-fire).
  </how-to-verify>
  <acceptance_criteria>
    - Judges tab entry lights the Judicial chip and renders judicial spokes (step 2); returning to Reps restores its lens (step 3).
    - Explicit pick on Judges is remembered on return and isolated from other tabs (step 4).
    - Educators tab shows a highlighted chip + honest-blanks compass, never an unhighlighted chip row or empty/broken compass (step 5).
    - Per-tab pick survives an in-session location change (step 6) and resets to the per-tab default after a full reload (step 7).
    - Arriving at the Elections tab from a people-tab with an explicit lens pick does NOT reset/hijack the lens switcher, and returning to the people-tab still shows the explicit pick (step 8).
    - No duplicate lens-selected analytics events per click.
  </acceptance_criteria>
  <verify>
    <human-check>All eight live steps behave as described (Judicial chip lights on Judges with judicial spokes; Reps restored on return; explicit pick remembered per tab and isolated; Educators shows a highlighted chip + honest blanks; pick survives in-session location change; full reload resets Judges to Judicial; arriving at the Elections tab does NOT reset the switcher; one analytics event per click).</human-check>
  </verify>
  <done>Operator has approved all eight steps on the live app, OR a specific defect list has been recorded for a gap-closure plan.</done>
  <resume-signal>Type "approved" if all eight steps behave as described, or list the specific step(s) and observed behavior for a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → client state | A stale/empty `?view=` on a location lacking that tab must fall back cleanly and still light a chip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-01 | Tampering / DoS (broken UI) | ?view= → effectiveActiveView → resolveTabLens → setActiveLens | mitigate | Live-verified here: on a no-judges location with a stale `?view=judges`, the resolved (Representatives) tab still shows a highlighted chip and a working compass — confirming resolveTabLens never leaves an unresolved key on activeLensKey (step 5 chip-highlight check + the fallback path). Unit-covered in Plan 01. |
| T-210-03 | Information Disclosure (fabricated data) | Educators tab, zero educator stance data | accept | Verified in step 5: Educators shows honest blanks, never fabricated spokes. Accepted as designed. |

No package installs in this plan.
</threat_model>

<verification>
- All seven live steps pass on essentials.empowered.vote with an operator watching.
- Chip is always highlighted (no silent "nothing selected") across Reps/Judges/Educators.
- Per-tab memory persists across in-session location changes and resets on reload.
</verification>

<success_criteria>
- CMP-01 and CMP-02 confirmed live: compass + switcher work in Educators/Judges, the default lens shifts per tab (Judges→Judicial, Educators→best-available, Reps→Best Match), explicit picks override and are remembered per tab for the session, and reset on reload — with honest blanks and no broken/empty compass anywhere.
</success_criteria>

<output>
Create `.planning/phases/210-per-tab-compass-integration/210-02-SUMMARY.md` when done (record the sign-off or the defect list).
</output>
