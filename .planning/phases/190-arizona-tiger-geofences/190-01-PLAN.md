---
phase: 190-arizona-tiger-geofences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts
  - C:/EV-Accounts/backend/scripts/verify-az-tiger-import.sql
  - C:/EV-Accounts/backend/scripts/smoke-az-geofences.ts
autonomous: true
requirements:
  - AZ-GEO-01

must_haves:
  truths:
    - "Running the loader with --state AZ --fips 04 --dry-run does not throw 'layer not in allowlist' for any of cd119,sldu,sldl,place,county"
    - "The verify SQL script exists at C:/EV-Accounts/backend/scripts/verify-az-tiger-import.sql with state='04' filters and an OR-direction section-split gate"
    - "The smoke test script exists at C:/EV-Accounts/backend/scripts/smoke-az-geofences.ts with 6 AZ addresses (Catalina Foothills unincorporated Pima County, City of Tucson, Oro Valley, Marana, Sahuarita, South Tucson)"
    - "The Catalina Foothills test address forbids G4110 and G4040 — the unincorporated-Pima-County invariant (success criterion analog to NV's Strip check)"
    - "EXPECTED_AZ_MTFCC.sldl resolves to exactly 30 polygons (D-04: one polygon per legislative district shared by 2 house seats; NOT 60) and EXPECTED_AZ_MTFCC.place is a non-zero confirmed count (~91) after this plan completes"
    - "smoke-az-geofences.ts expectedCounts.G5220 matches EXPECTED_AZ_MTFCC.sldl and expectedCounts.G4110 matches EXPECTED_AZ_MTFCC.place"
    - "Full 5-layer dry-run prints exactly 5 'AZ MTFCC pre-flight assertion PASSED' lines with no MtfccAssertionError"
    - "A read-only pre-existing-row check for state='04' geofence_boundaries has been run and its result (greenfield 0 rows, or any stray rows) recorded in the SUMMARY"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts"
      provides: "AZ entries in STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, STATE_RUN_MAKEVALID, plus EXPECTED_AZ_MTFCC block in processLayer() with confirmed counts for all 5 layers (sldl=30, place=~91)"
      contains: "AZ: new Set(['cd119', 'sldu', 'sldl', 'place', 'county'])"
    - path: "C:/EV-Accounts/backend/scripts/verify-az-tiger-import.sql"
      provides: "SQL verification gates for state='04'; per-layer counts; districts casing; unincorporated-Pima sentinel; OR-direction section-split gate"
      contains: "state = '04'"
    - path: "C:/EV-Accounts/backend/scripts/smoke-az-geofences.ts"
      provides: "6-address TypeScript smoke test (Catalina Foothills unincorporated + 5 cities) exiting 0/1"
      contains: "smoke-az-geofences"
  key_links:
    - from: "load-state-tiger-boundaries.ts STATE_LAYER_ALLOWLIST"
      to: "processLayer() EXPECTED_AZ_MTFCC block"
      via: "fipsArg === '04' guard"
      pattern: "fipsArg === '04'"
    - from: "smoke-az-geofences.ts Catalina Foothills TEST_ADDRESS"
      to: "forbiddenMtfcc assertion"
      via: "forbiddenMtfcc: ['G4110', 'G4040']"
      pattern: "forbiddenMtfcc"
    - from: "Plan 01 Task 3 dry-run MtfccAssertionError 'got N' output"
      to: "EXPECTED_AZ_MTFCC.sldl and EXPECTED_AZ_MTFCC.place final values"
      via: "manual edit replacing sentinel 0 with confirmed N (sldl MUST be 30 per D-04)"
      pattern: "got \\d+"
---

<objective>
Add Arizona (FIPS 04) to the generalized TIGER loader, create the verification and smoke test scripts, run a read-only pre-existing-row check, and discover the actual SLDL and G4110 polygon counts via dry-run — leaving the scaffold fully armed for the live load in Plan 02. No database rows are written in this plan.

Purpose: Plan 02 (the live data load) requires (a) the loader to recognize AZ across the three inline allowlists + the EXPECTED_AZ_MTFCC pre-flight block, (b) the verify+smoke scripts to exist, and (c) the EXPECTED_AZ_MTFCC pre-flight counts to match the actual TIGER 2024 file contents. The sentinel-0 dry-run trick surfaces the actual sldl and place counts without writing any data — and crucially confirms the D-04 headline: AZ SLDL is 30 polygons (one per legislative district, shared by 2 house seats), NOT 60. This phase mirrors the proven NV/VA/MD/OR/ME 5-layer pattern (cd119 key, no cousub, no aiannh).

Output: 3 additive config edits + 1 EXPECTED_AZ_MTFCC block in load-state-tiger-boundaries.ts (with confirmed counts for all 5 layers); 2 new scripts (verify-az-tiger-import.sql, smoke-az-geofences.ts) with matching confirmed counts; the pre-existing-row check result recorded; no database rows written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/190-arizona-tiger-geofences/190-CONTEXT.md
@.planning/milestones/v18.0-phases/158-nevada-tiger-geofences/158-CONTEXT.md
@.planning/milestones/v18.0-phases/158-nevada-tiger-geofences/158-01-PLAN.md
@.planning/milestones/v18.0-phases/158-nevada-tiger-geofences/158-01-SUMMARY.md

<interfaces>
<!-- Key contracts in load-state-tiger-boundaries.ts the executor edits. Extracted from the live file. -->
<!-- These are ADDITIVE insertion points — no existing state block (CA/TX/UT/IN/MA/ME/OR/MD/VA/NV/DC) is removed. -->

FIPS_TO_STATE ALREADY contains `'04': 'az'` (verified at ~line 126) — NO edit needed there, and adding a
second '04' key would be a TypeScript duplicate-key error. The main() round-trip check
(`expectedAbbrev !== args.state`) therefore already accepts `--state AZ --fips 04`. This differs from the
literal wording in CONTEXT D-00 ("add ... the FIPS map (AZ→04)") — the intent (AZ→04 wired) is already
satisfied by the pre-existing 50-state map; do NOT add a duplicate entry.

The three AZ config insertion points (mirror the NV entries, which sit directly above DC):
  1. STATE_LAYER_ALLOWLIST: `AZ: new Set([...])` after the NV entry, before the DC entry.
  2. STATE_CITY_ASSERTIONS: `AZ: [...]` after the NV entry, before the closing brace.
  3. STATE_RUN_MAKEVALID: `AZ: new Set([...])` after the NV entry, before the DC entry.
And the pre-flight block:
  4. EXPECTED_AZ_MTFCC block in processLayer(): a `if (fipsArg === '04') { ... }` block modeled VERBATIM
     on the existing NV block (`if (fipsArg === '32')`), inserted after the NV block and before the DC
     block (`if (fipsArg === '11')`).

cd119 dispatch entry URL template = `tl_${v}_us_cd119.zip` (vintage 2024, congress 119). That `cd119`
key is the verified one — NOT `cd`. Using bare `cd` for AZ would still resolve a file but is the wrong
established key for TIGER 2024 — use `cd119` per D-00 (the ME/OR/NV silent-no-op trap).

place layer: G4110-only MTFCC hard filter; geo_id = GEOID (7-char STATEFP+PLACEFP).
county layer: national file `tl_2024_us_county.zip`, filtered STATEFP='04'; geo_id = GEOID (5-char; Pima County = 04019).
sldu/sldl: state-scoped files `tl_2024_04_sldu.zip` / `tl_2024_04_sldl.zip`; filterByStatefp=false.
districts casing (D-00, established loader behavior): insertDistrictIfMissing receives `abbrev` (lowercase
  'az') for county/sldu/sldl and `abbrevUpper` ('AZ') for cd119 NATIONAL_LOWER. This matches D-00 exactly
  ('az' for SLDU/SLDL/COUNTY/LOCAL, 'AZ' for STATE_EXEC/NATIONAL). The place layer writes NO district row
  (writeDistrictRow=false).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AZ to TIGER loader (3 additive config edits + EXPECTED_AZ_MTFCC block)</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Read before editing. The insertion points mirror the NV entries (the most recent 5-layer state, exact analog):
      (1) STATE_LAYER_ALLOWLIST (~line 44) — insert after the `NV: new Set([...])` entry, before the `DC:` entry;
      (2) STATE_CITY_ASSERTIONS (~line 97) — insert after the `NV: [...]` entry, before the closing brace;
      (3) STATE_RUN_MAKEVALID (~line 112) — insert after the `NV:` entry, before the `DC:` entry;
      (4) EXPECTED_AZ_MTFCC block in processLayer() — insert after the NV block (the `if (fipsArg === '32') { ... }` block ending ~line 1018), before the DC block (`if (fipsArg === '11')` ~line 1021).
    Confirm FIPS_TO_STATE already has `'04': 'az'` (~line 126) — do NOT add it again (duplicate-key TS error).
    .planning/milestones/v18.0-phases/158-nevada-tiger-geofences/158-01-PLAN.md — Task 1 is the structural template; AZ substitutes 32->04, NV->AZ, the city-assertion strings, and the per-key counts.
    .planning/phases/190-arizona-tiger-geofences/190-CONTEXT.md — D-00 (layer set + casing), D-01 (skip aiannh), D-02 (5 city assertions), D-03 (skip cousub), D-04 (SLDL 30 not 60).
  </read_first>
  <action>
    Make exactly 3 additive config edits plus 1 pre-flight block in load-state-tiger-boundaries.ts. No existing lines are removed; all existing state blocks (CA/TX/UT/IN/MA/ME/OR/MD/VA/NV/DC) are preserved verbatim. Each edit is placed immediately adjacent to the corresponding NV entry to keep the diff reviewable.

    Edit 1 — STATE_LAYER_ALLOWLIST (insert after the NV entry, before the DC entry): the AZ key mapped to a new Set of exactly the five layer strings cd119, sldu, sldl, place, county. Note: cd119 key (NOT cd) per D-00. No cousub per D-03 (Pima County's large unincorporated communities — Catalina Foothills, Casas Adobes, Tanque Verde, Drexel Heights — have no municipal government; the county governs them). No aiannh per D-01 (tribal lands skipped this milestone; San Xavier / Pascua Yaqui / Navajo Nation addresses resolve to county/city only).

    Edit 2 — STATE_CITY_ASSERTIONS (insert after the NV entry, before the closing brace of the const): the AZ key mapped to an array of exactly these five case-insensitive substrings — 'Tucson city', 'Oro Valley town', 'Marana town', 'Sahuarita town', 'South Tucson city'. These are matched against TIGER NAMELSAD via .toLowerCase().includes(...). NOTE the LSAD token: Oro Valley, Marana, and Sahuarita are TOWNS in Arizona (TIGER NAMELSAD appends 'town'), while Tucson and South Tucson are CITIES ('city'). Task 3 dry-run confirms the exact NAMELSAD strings. If dry-run shows different strings, fail the task and report the actual NAMELSAD values seen; do NOT silently mutate the strings (this is the STATE_CITY_ASSERTIONS vintage gate, D-02). If a town/city LSAD guess is wrong, the gate fails loudly — that is the intended signal.

    Edit 3 — STATE_RUN_MAKEVALID (insert after the NV entry, before the DC entry): the AZ key mapped to a new Set of the same five layers cd119, sldu, sldl, place, county — all 5 AZ layers get ST_MakeValid, identical to ME/OR/MD/VA/NV.

    Edit 4 — EXPECTED_AZ_MTFCC block (insert after the NV block closing brace, before the DC block opening `if (fipsArg === '11')`). Copy the NV block skeleton verbatim; only change the guard value, Record name, error-message label, and per-key counts. The block must have:
      - Guard: if (fipsArg === '04') { ... }
      - Record literal EXPECTED_AZ_MTFCC with all 5 keys:
          cd119: 9    (9 AZ congressional districts)
          sldu: 30    (30 AZ legislative districts — single senator each)
          sldl: 0     (SENTINEL — Task 3 dry-run reveals actual count; per D-04 expect EXACTLY 30, one polygon per legislative district shared by the 2 house seats — NOT 60)
          place: 0    (SENTINEL — Task 3 dry-run reveals actual count; expect ~91 AZ G4110 incorporated municipalities per D-02)
          county: 15  (15 AZ counties; NO independent cities, unlike VA/NV — a plain 15)
      - if (layer in EXPECTED_AZ_MTFCC) dispatch that re-streams the shapefile with IDENTICAL filter logic to the upsert pass (filterByStatefp, place MTFCC===G4110 guard, districtNumField skipDistrictCodes). Copy this filter body verbatim from the NV block; AZ has no cousub layer — do NOT add a FUNCSTAT guard.
      - Named MtfccAssertionError thrown when actualCount !== expected, with a message of the form: [AZ MTFCC assertion] layer=${layer}: expected ${expected} records, got ${actualCount}. TIGER file: ${url}. Aborting before any DB write — verify TIGER 2024 FIPS 04 file is correct.
      - console.log on PASS of the form: [${layer}] AZ MTFCC pre-flight assertion PASSED: ${actualCount} records (expected ${expected}).

    The sldl: 0 and place: 0 values are intentional integer sentinels (not a TBD keyword, which would break TypeScript). Task 3 dry-run will fire MtfccAssertionError revealing the actual N. Do NOT skip Task 3 — Plan 02's live load depends on confirmed values being in place (and sldl being verified as 30, not 60).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit --skipLibCheck scripts/load-state-tiger-boundaries.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - load-state-tiger-boundaries.ts STATE_LAYER_ALLOWLIST contains an `AZ:` key whose Set literal lists exactly cd119, sldu, sldl, place, county (grep `AZ: new Set` shows cd119, never bare `cd`)
    - load-state-tiger-boundaries.ts STATE_CITY_ASSERTIONS contains an `AZ:` key with the five strings 'Tucson city', 'Oro Valley town', 'Marana town', 'Sahuarita town', 'South Tucson city'
    - load-state-tiger-boundaries.ts STATE_RUN_MAKEVALID contains an `AZ:` key with the same five layers
    - The file contains exactly one occurrence of `if (fipsArg === '04')` introducing the EXPECTED_AZ_MTFCC block
    - The EXPECTED_AZ_MTFCC Record contains all 5 keys: cd119, sldu, sldl, place, county with integer values 9, 30, 0, 0, 15 respectively
    - The error thrown inside the block has name 'MtfccAssertionError' and a message containing '[AZ MTFCC assertion]'
    - FIPS_TO_STATE still contains exactly one `'04'` key (no duplicate added)
    - `npx tsc --noEmit --skipLibCheck scripts/load-state-tiger-boundaries.ts` exits 0 (no TypeScript errors)
    - Existing VA/NV/MD/OR/ME blocks are still present (grep for EXPECTED_NV_MTFCC, EXPECTED_VA_MTFCC, EXPECTED_MD_MTFCC still returns matches — no regressions)
  </acceptance_criteria>
  <done>AZ is registered in the 3 config blocks and the EXPECTED_AZ_MTFCC pre-flight assertion exists with sentinel 0s for sldl/place (cd119=9, sldu=30, county=15 hard-coded). TypeScript compiles cleanly. Task 3 will replace the sentinel 0s with confirmed counts (sldl must be 30).</done>
</task>

<task type="auto">
  <name>Task 2: Create verify-az-tiger-import.sql and smoke-az-geofences.ts (adapt from NV analogs)</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/smoke-nv-geofences.ts — Full template for smoke-az-geofences.ts. Copy structure verbatim (imports from 'pg' and 'dotenv', AddressTest interface, queryBoundaries function, main() flow, expectedMtfcc/forbiddenMtfcc/expectedGeoIds/NULL-name checks, error accumulation, exit 0/1).
    C:/EV-Accounts/backend/scripts/verify-nv-tiger-import.sql — Reference for gate structure and the OR-direction section-split gate (geofence -> districts). AZ must use the same OR/NV direction so any stray pre-existing rows do not produce a false positive.
    .planning/phases/190-arizona-tiger-geofences/190-CONTEXT.md — D-00..D-04, the unincorporated-Pima-County invariant, and Claude's-discretion note on verification addresses.
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/scripts/verify-az-tiger-import.sql by adapting verify-nv-tiger-import.sql with these substitutions (every gate present; no gate omitted):
      - Header: a `-- Phase 190 AZ TIGER import verification` comment and a run line `Run via: psql $DATABASE_URL -f backend/scripts/verify-az-tiger-import.sql`
      - All `state = '32'` filters changed to `state = '04'`; remove any NV-specific Clark County / Strip text
      - Gate 1 (invalid geometry): count of rows for state='04' WHERE NOT ST_IsValid(geometry); expected 0
      - Gate 2 (GeometryCollection): count for state='04' WHERE ST_GeometryType = 'ST_GeometryCollection'; expected 0
      - Gate 3 (per-layer row counts): SELECT mtfcc, COUNT(*) FROM essentials.geofence_boundaries WHERE state='04' GROUP BY mtfcc ORDER BY mtfcc; with an expected comment `-- Expected: G4020|15, G4110|[DRY-RUN-COUNT ~91], G5200|9, G5210|30, G5220|30` — leave [DRY-RUN-COUNT] as a literal comment placeholder (verify SQL runs after the live load in Plan 02; the executor compares actual DB rows against EXPECTED_AZ_MTFCC). Note G5220 expected 30 per D-04
      - Gate 4 (unincorporated-Pima invariant — headline correctness check): a comment-documented spatial probe at a known Catalina Foothills coordinate (lon=-110.9210, lat=32.3130 — unincorporated Pima County north of Tucson city limits) returning the covering boundaries: SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='04' AND ST_Covers(geometry, ST_SetSRID(ST_MakePoint(-110.9210, 32.3130), 4326)) ORDER BY mtfcc; — expected: a G4020 row named 'Pima County' (geo_id='04019') and the G5200/G5210/G5220 tiers, but NO G4110 row (Catalina Foothills is unincorporated) and NO G4040 row
      - Gate 5 (districts table casing): SELECT state, district_type, COUNT(*) FROM essentials.districts WHERE state IN ('AZ','az') GROUP BY state, district_type ORDER BY state, district_type; — expected: az|COUNTY|15, az|STATE_UPPER|30, az|STATE_LOWER|30, AZ|NATIONAL_LOWER|9 (cd119 uses uppercase 'AZ' per abbrevUpper; county/sldu/sldl use lowercase 'az' per D-00)
      - Gate 6 (Pima County sentinel — confirms county layer): SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='04' AND mtfcc='G4020' AND name LIKE '%Pima County%'; — expected 1 row: geo_id='04019' 'Pima County'
      - Gate 7 (section-split — MUST use OR/NV-direction): SELECT gb.geo_id, gb.name, gb.mtfcc FROM essentials.geofence_boundaries gb WHERE gb.mtfcc IN ('G5200','G5210','G5220','G4020') AND gb.state='04' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts) LIMIT 10; — expected 0 rows. Include an SQL comment explaining the OR-direction choice: a pre-existing NATIONAL_UPPER row (no polygon) would produce a false positive under the inverse (districts -> geofence) query

    Create C:/EV-Accounts/backend/scripts/smoke-az-geofences.ts by adapting smoke-nv-geofences.ts in full. Copy verbatim: the imports; the AddressTest interface; the main() structure with client.connect/try/finally/client.end; the per-address loop (expectedMtfcc check, forbiddenMtfcc check, expectedGeoIds check, NULL-name check); the SC3 layer-count block; the error accumulation; the final exit 0/1 branching. Apply these AZ-specific substitutions:
      - File header comment block: smoke-az-geofences.ts / Phase 190: Arizona geofences smoke test / Usage `npx tsx scripts/smoke-az-geofences.ts` from C:/EV-Accounts/backend; SC1-SC4 describe Catalina-Foothills-unincorporated (no G4110), the 5 incorporated municipalities each returning G4110 + county/CD/SLDU/SLDL, layer counts, and non-NULL names
      - queryBoundaries function: state = '04' (not '32')
      - TEST_ADDRESSES array — replace NV addresses entirely with these 6 AZ addresses (the executor MAY refine coordinates to land cleanly inside each boundary; the criterion is correct tier resolution). All are in Pima County (G4020='04019'):
          Address 1: label='Catalina Foothills (unincorporated Pima County)', lon=-110.9210, lat=32.3130, expectedMtfcc=['G4020','G5200','G5210','G5220'], forbiddenMtfcc=['G4110','G4040'], expectedGeoIds={G4020:'04019'}  // Pima County; NO city, NO township — headline check
          Address 2: label='City of Tucson (City Hall)', lon=-110.9760, lat=32.2226, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'04019'}
          Address 3: label='Oro Valley (Town Hall)', lon=-110.9770, lat=32.4160, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'04019'}
          Address 4: label='Marana (Town Hall)', lon=-111.2210, lat=32.4300, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'04019'}  // NOTE: Marana straddles Pima/Pinal; Town Hall is in Pima (04019). If the chosen coordinate lands in Pinal (04021), pick a Pima-side coordinate or adjust the expected geo_id and document
          Address 5: label='Sahuarita (Town Hall)', lon=-110.9720, lat=31.9260, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'04019'}
          Address 6: label='South Tucson (City Hall)', lon=-110.9700, lat=32.1980, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4020:'04019'}  // South Tucson is a separate 1-sq-mi incorporated city fully enclaved within Tucson; the G4110 returned must be 'South Tucson city', NOT 'Tucson city'
      - SC3 expectedCounts block (state='04' query): G4020:15, G4110:0, G5200:9, G5210:30, G5220:0. The 0 values for G4110 and G5220 are intentional placeholders — Task 3 dry-run reveals actual counts and updates these here (G5220 must become 30 per D-04). With 0 as expected, SC3 fails loudly until updated (prevents silent skip)
      - Target city lookup block: query for AZ municipalities 'Tucson city', 'Oro Valley town', 'Marana town', 'Sahuarita town', 'South Tucson city' in state='04' G4110 rows
      - Final result console.log block: reference Phase 190 success criteria SC1-SC4 using Catalina Foothills / Tucson / Oro Valley / Marana / Sahuarita / South Tucson language

    Do NOT inline the loader file contents into the smoke test. The smoke test only queries Postgres and asserts results.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit --skipLibCheck scripts/smoke-az-geofences.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/verify-az-tiger-import.sql exists
    - verify-az-tiger-import.sql contains `state = '04'` (multiple gates) and contains NO `state = '32'` or `state = '51'` (no leftover NV/VA text)
    - verify-az-tiger-import.sql Gate 4 probes coordinate (-110.9210, 32.3130) and the comment states the expected result has NO G4110 and NO G4040 row (Catalina Foothills is unincorporated)
    - verify-az-tiger-import.sql Gate 6 contains `name LIKE '%Pima County%'` and expects 1 row (geo_id='04019')
    - verify-az-tiger-import.sql Gate 7 uses OR-direction: FROM essentials.geofence_boundaries gb ... WHERE ... AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts) (NOT the inverse direction)
    - File C:/EV-Accounts/backend/scripts/smoke-az-geofences.ts exists
    - smoke-az-geofences.ts contains `state = '04'` in queryBoundaries and the SC3 block
    - smoke-az-geofences.ts TEST_ADDRESSES[0] (Catalina Foothills) has forbiddenMtfcc: ['G4110', 'G4040'] and expectedGeoIds G4020: '04019'
    - smoke-az-geofences.ts contains 6 TEST_ADDRESSES entries (Catalina Foothills + 5 municipalities)
    - smoke-az-geofences.ts imports from 'pg' and 'dotenv' (same as NV analog)
    - `npx tsc --noEmit --skipLibCheck scripts/smoke-az-geofences.ts` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>verify-az-tiger-import.sql and smoke-az-geofences.ts both exist; TypeScript compiles cleanly; SQL contains state='04' with the unincorporated-Pima Gate 4 and OR-direction Gate 7; smoke test asserts Catalina Foothills forbids G4110/G4040 and each of the 5 municipalities returns G4110 + all tiers.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-existing-row check + dry-run all 5 AZ TIGER layers; confirm sldl=30 (D-04) and place count; update sentinels</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Confirm EXPECTED_AZ_MTFCC.sldl=0 and EXPECTED_AZ_MTFCC.place=0 sentinels from Task 1 are present; confirm the `fipsArg === '04'` guard is in place.
    C:/EV-Accounts/backend/scripts/smoke-az-geofences.ts — Confirm expectedCounts.G4110=0 and expectedCounts.G5220=0 sentinels from Task 2 are present.
    .planning/phases/190-arizona-tiger-geofences/190-CONTEXT.md — D-00 (cd119 not cd — verify the actual TIGER zip filename in loader output), D-02 (~91 G4110 municipalities), D-04 (SLDL 30 NOT 60).
    .planning/milestones/v18.0-phases/158-nevada-tiger-geofences/158-CONTEXT.md — the note that NV 158 found 4 stray pre-existing CD rows for state='32'; AZ is asserted greenfield but must be verified (executor DB-checks rather than assumes).
  </read_first>
  <action>
    STEP A — Pre-existing-row check (read-only, no writes). From C:/EV-Accounts/backend, run an inline pg query against production (DATABASE_URL) to detect any pre-existing AZ geofence rows:

      npx tsx -e "require('dotenv').config(); const {Client}=require('pg'); const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(\"SELECT mtfcc, COUNT(*) AS n FROM essentials.geofence_boundaries WHERE state='04' GROUP BY mtfcc ORDER BY mtfcc\")).then(r=>{if(r.rows.length===0){console.log('GREENFIELD: 0 state=04 rows')}else{r.rows.forEach(x=>console.log('PRE-EXISTING',x.mtfcc,x.n))}; return c.end()})"

    Record the result in the SUMMARY. Two acceptable outcomes: (1) greenfield (0 rows) — the roadmap assertion holds; (2) some stray rows found (as NV had 4 CD rows) — acceptable because the loader upsert is ON CONFLICT DO NOTHING and idempotent, but note the exact mtfcc/counts so Plan 02 knows to expect already_exists:N for those layers. If stray rows exist for a NON-CD layer with unexpected volume, stop and investigate before Plan 02.

    STEP B — Dry-run all 5 AZ layers. Run:

      npx tsx scripts/load-state-tiger-boundaries.ts --state AZ --fips 04 --layers cd119,sldu,sldl,place,county --dry-run

    The loader will:
      1. Print the resolved URL for each layer — CONFIRM the cd119 URL is .../CD/tl_2024_us_cd119.zip (the D-00 loader-key verification: the filename must contain cd119, not cd). If the URL contains a different key, STOP — the wrong key silently loads zero rows.
      2. Download all 5 TIGER zip files from census.gov to the local cache (cd119, sldu, sldl, place state-scoped; county is the ~80MB national file)
      3. Run the STATE_CITY_ASSERTIONS gate for the place layer — confirm all 5 AZ municipalities are found in the NAMELSAD values seen
      4. Run the EXPECTED_AZ_MTFCC pre-flight assertion for each layer
      5. NOT write to the database (--dry-run flag)

    Expected outcomes per layer:
      - cd119: PASSES with [cd119] AZ MTFCC pre-flight assertion PASSED: 9 records (expected 9)
      - sldu:  PASSES with [sldu] AZ MTFCC pre-flight assertion PASSED: 30 records (expected 30)
      - sldl:  THROWS MtfccAssertionError [AZ MTFCC assertion] layer=sldl: expected 0 records, got N
      - place: THROWS MtfccAssertionError [AZ MTFCC assertion] layer=place: expected 0 records, got N
      - county: PASSES with [county] AZ MTFCC pre-flight assertion PASSED: 15 records (expected 15)

    Record the actual N values from the sldl and place MtfccAssertionError messages. Required outcomes:
      - sldl: MUST be exactly 30 (D-04: Arizona's 30 legislative districts each elect 2 house members but share ONE polygon; TIGER SLDL has 30 polygons, NOT 60). If N is 60, STOP and escalate — this contradicts D-04 and means the phase 192 60-rep seeding assumption must be revisited. If N is any value other than 30, stop and investigate.
      - place: expect ~91 (Arizona has ~91 incorporated municipalities per D-02). Stop and investigate if N is below 80 or above 100.

    Stop and investigate if any of these red flags appear:
      - cd119 throws MtfccAssertionError with got 0 — the loader key is wrong (the silent-no-op trap); re-verify the URL contains cd119
      - sldu actual count is NOT 30 (Arizona has exactly 30 legislative districts, single senator each)
      - county throws MtfccAssertionError (would mean 15 is wrong — Arizona has exactly 15 counties, no independent cities)
      - STATE_CITY_ASSERTIONS gate fails for any of the 5 AZ municipalities — record the actual NAMELSAD values seen and stop; do NOT mutate the assertion strings silently. The most likely failure is a city/town LSAD mismatch (e.g. TIGER labels one 'Marana CDP' or a different suffix) — fix the assertion string to the actual NAMELSAD only after confirming the correct incorporated-place record exists

    STEP C — Update sentinels. After confirming the actual counts, apply two follow-up edits:

      Edit A (load-state-tiger-boundaries.ts): In the EXPECTED_AZ_MTFCC Record, replace `sldl: 0,` with `sldl: 30,` (the confirmed D-04 count) and `place: 0,` with `place: N,` (N = actual dry-run place count). Add a line comment next to each: `// confirmed via dry-run YYYY-MM-DD — 30 AZ G5220 legislative-district polygons (2 house seats per district, one polygon; D-04)` and `// confirmed via dry-run YYYY-MM-DD — N AZ G4110 incorporated municipalities`.

      Edit B (smoke-az-geofences.ts): In the expectedCounts Record (SC3 block), replace `G4110: 0,` with `G4110: N,` (matching EXPECTED_AZ_MTFCC.place) and `G5220: 0,` with `G5220: 30,` (matching EXPECTED_AZ_MTFCC.sldl). The two files must stay in sync — they reference the same underlying TIGER counts.

    STEP D — Re-run the full 5-layer dry-run to confirm all 5 assertions now PASS:

      npx tsx scripts/load-state-tiger-boundaries.ts --state AZ --fips 04 --layers cd119,sldu,sldl,place,county --dry-run

    Expect 5 "AZ MTFCC pre-flight assertion PASSED" lines with no MtfccAssertionError. If any layer fails, the recorded counts are wrong — re-read the error message and re-apply Edit A.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/load-state-tiger-boundaries.ts --state AZ --fips 04 --layers cd119,sldu,sldl,place,county --dry-run 2>&1 | grep -c "AZ MTFCC pre-flight assertion PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - The pre-existing-row check ran and its result (GREENFIELD 0 rows, or the exact stray mtfcc/counts) is recorded for the SUMMARY
    - The dry-run output shows the cd119 resolved URL ending in tl_2024_us_cd119.zip (loader-key D-00 verification — NOT a bare cd filename)
    - First dry-run (with sentinel 0s) prints MtfccAssertionError for sldl with `got 30` (EXACTLY 30 per D-04; escalate if 60 or any other value)
    - First dry-run prints MtfccAssertionError for place with `got N` where N is between 80 and 100
    - First dry-run prints PASSED for cd119 (9 records), sldu (30 records), county (15 records)
    - First dry-run shows the STATE_CITY_ASSERTIONS gate PASSED for all 5 AZ municipalities (or, if it fails, the executor stops and records the actual NAMELSAD values)
    - load-state-tiger-boundaries.ts EXPECTED_AZ_MTFCC.sldl is now the integer 30 with a `// confirmed via dry-run` comment; place is now the recorded non-zero integer with a dry-run comment
    - smoke-az-geofences.ts expectedCounts.G4110 equals EXPECTED_AZ_MTFCC.place; expectedCounts.G5220 equals 30
    - Re-run full 5-layer dry-run output contains exactly 5 occurrences of "AZ MTFCC pre-flight assertion PASSED" and 0 occurrences of "MtfccAssertionError"
    - The verify command above prints exactly the integer 5
  </acceptance_criteria>
  <done>Pre-existing-row check recorded; EXPECTED_AZ_MTFCC has confirmed counts for all 5 layers (sldl=30 verified per D-04, no sentinel 0 remaining); smoke-az-geofences.ts expectedCounts matches; full 5-layer dry-run passes all pre-flight assertions with zero MtfccAssertionErrors; the cd119 loader key is verified from the actual zip filename. Plan 02 live load is now unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TIGER scripts -> filesystem | New script files written to EV-Accounts/backend/scripts/ |
| loader -> census.gov | HTTPS download of TIGER zip files during dry-run (read-only; no DB writes) |
| pre-existing-row check -> Supabase production | Read-only SELECT via DATABASE_URL (mcp__supabase-local IS production; this query is read-only) |
| dry-run MtfccAssertionError output -> code edits | Actual sldl/place counts surfaced in error messages flow into code as literal integers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-01 | Tampering | load-state-tiger-boundaries.ts edits | mitigate | 3 config inserts + 1 pre-flight block only; no deletions; CA/TX/UT/IN/MA/ME/OR/MD/VA/NV/DC blocks preserved verbatim; TypeScript compile check gates Task 1; regression grep for EXPECTED_NV/VA/MD_MTFCC |
| T-190-02 | Integrity | Wrong loader key (`cd` instead of `cd119`) silently loads zero CD rows | mitigate | D-00: Task 3 asserts the resolved cd119 URL ends in tl_2024_us_cd119.zip; cd119 pre-flight expects 9 records — got 0 halts before any DB write |
| T-190-03 | Integrity | SLDL loaded as 60 polygons instead of 30 (D-04 violation) | mitigate | Task 3 requires sldl dry-run to be EXACTLY 30; 60 (or any non-30) halts and escalates — this is the AZ-specific headline gotcha that phase 192 60-rep seeding depends on |
| T-190-04 | Integrity | Live load with stale sentinel 0 in EXPECTED_AZ_MTFCC | mitigate | Task 3 gate: 5-layer dry-run must print 5 PASSED lines before Plan 01 is done; sentinel 0 left in place fails Plan 02 immediately on the first affected layer |
| T-190-05 | Integrity | Wrong place count written / wrong city LSAD assertion (town vs city) | mitigate | Task 3 acceptance: place N in [80,100]; STATE_CITY_ASSERTIONS gate fails loudly on a town/city LSAD mismatch; executor records actual NAMELSAD before any edit |
| T-190-06 | Tampering | TIGER zip files from census.gov | accept | census.gov is the authoritative US government source; files are read-only during dry-run; no DB writes; HTTPS download with redirect following is the established pattern |
| T-190-07 | Information Disclosure | DATABASE_URL in smoke test / pre-existing check | accept | DATABASE_URL already used in existing NV/VA/MD smoke tests; dotenv.config() pattern; pre-existing check is read-only; Plan 01 does not write to production |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; all deps pre-existing in EV-Accounts/backend (pg, dotenv, shapefile, adm-zip). RESEARCH.md Package Legitimacy audit not required — no installs in this phase |
</threat_model>

<verification>
After all 3 tasks complete:
1. `grep -c "AZ: new Set" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns 2 (STATE_LAYER_ALLOWLIST + STATE_RUN_MAKEVALID)
2. `grep -c "South Tucson city" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns >= 1 (STATE_CITY_ASSERTIONS AZ entry)
3. `grep -c "fipsArg === '04'" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns 1
4. `grep -n "sldl:" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts | grep "confirmed via dry-run"` — the EXPECTED_AZ_MTFCC.sldl line carries the integer 30 with a dry-run comment
5. `ls C:/EV-Accounts/backend/scripts/verify-az-tiger-import.sql` — file exists
6. `ls C:/EV-Accounts/backend/scripts/smoke-az-geofences.ts` — file exists
7. `npx tsc --noEmit --skipLibCheck scripts/smoke-az-geofences.ts` from C:/EV-Accounts/backend — exits 0
8. Full 5-layer dry-run prints exactly 5 "PASSED" lines and zero "MtfccAssertionError" lines
</verification>

<success_criteria>
- AZ is registered in STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, STATE_RUN_MAKEVALID with the correct 5-layer set (cd119 key per D-00; no cousub per D-03; no aiannh per D-01)
- EXPECTED_AZ_MTFCC block inserted after the NV block; all 5 keys present (cd119=9, sldu=30, sldl=30 confirmed, place=~91 confirmed, county=15)
- SLDL confirmed as 30 polygons (D-04) via dry-run — NOT 60
- verify-az-tiger-import.sql created with all gates: geometry checks for state='04'; per-layer counts; unincorporated-Pima probe (Gate 4); districts casing (Gate 5); Pima County sentinel (Gate 6); OR-direction section-split (Gate 7)
- smoke-az-geofences.ts created and TypeScript-clean; Catalina Foothills address forbids G4110/G4040; each of the 5 municipalities asserts G4110 + county/CD/SLDU/SLDL
- Pre-existing-row check run and result recorded (greenfield or stray-row inventory)
- Full 5-layer dry-run passes all pre-flight assertions (no MtfccAssertionError); the cd119 loader key is verified from the actual zip filename; loader and smoke test expectedCounts are in sync for G4110/G5220
- No database rows written in Plan 01 (dry-run only)
</success_criteria>

<output>
Create `.planning/phases/190-arizona-tiger-geofences/190-01-SUMMARY.md` when done. Record the confirmed sldl count (must be 30) and place count (place=N), the verified cd119 URL filename, the 5 confirmed AZ municipality NAMELSAD strings (with correct city/town LSAD), and the pre-existing-row check result — Plan 02 references these when checking the live load row counts.
</output>
