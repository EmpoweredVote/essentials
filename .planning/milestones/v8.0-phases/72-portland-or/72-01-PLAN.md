---
phase: 72-portland-or
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts
  - C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql
  - C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts
autonomous: true
requirements:
  - GEO-OR-01
  - GEO-OR-02
  - GEO-OR-03
  - GEO-OR-04
  - GEO-OR-05
  - GEO-OR-06

must_haves:
  truths:
    - "OR is registered in STATE_LAYER_ALLOWLIST with layers cd119/sldu/sldl/place/county (per D-01)"
    - "OR is registered in STATE_CITY_ASSERTIONS with sentinel 'Portland city' (per D-01)"
    - "OR is registered in STATE_RUN_MAKEVALID with all 5 layers (per D-01)"
    - "fipsArg === '41' pre-flight assertion block exists in processLayer with counts cd119:6, sldu:30, sldl:60, place:<dry-run-confirmed>, county:36 (per D-01, D-02 — OR FIPS=41)"
    - "verify-or-tiger-import.sql exists with 7 gates scoped to state='41' (per D-01)"
    - "smoke-or-geofences.ts exists with 3 test addresses and CA-style structured assertions (per D-01)"
    - "All 5 TIGER layers loaded for OR FIPS 41: G4110, G4020, G5200, G5210, G5220"
    - "npx tsc --noEmit exits 0 after all loader modifications"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts"
      provides: "TIGER loader with OR registered in all 4 required config structures"
      contains: "OR.*cd119"
    - path: "C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql"
      provides: "7-gate SQL verification for Oregon TIGER import"
      contains: "state = '41'"
    - path: "C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts"
      provides: "Address smoke test for Portland + Bend + Salem"
      contains: "4159000"
  key_links:
    - from: "STATE_LAYER_ALLOWLIST['OR']"
      to: "processLayer dispatch"
      via: "allowlist check at top of loader"
      pattern: "OR.*cd119.*sldu.*sldl.*place.*county"
    - from: "fipsArg === '41'"
      to: "pre-flight assertion block"
      via: "processLayer conditional after CA block"
      pattern: "fipsArg.*41"
    - from: "geofence_boundaries state='41'"
      to: "point-in-polygon routing"
      via: "ST_Covers query in essentialsService"
      pattern: "state = '41'"
---

<objective>
Register Oregon (FIPS 41) in the TIGER loader's four configuration structures, create the verification SQL and smoke test scripts, dry-run the place layer to confirm the G4110 count, then run all 5 layers to populate essentials.geofence_boundaries and essentials.districts.

Purpose: Without Oregon boundaries in geofence_boundaries, no address in Oregon can route to a federal, state, or local representative. This is the foundation for all subsequent v8.0 Oregon phases.

Output: 374 geofence_boundaries rows (6 + 30 + 60 + 242 + 36) and 132 districts rows (6 + 30 + 60 + 36 — place has writeDistrictRow=false) for state='41' (geofences) and state='or' (districts). All counts subject to dry-run confirmation for the place layer.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Transparent Motivations/essentials/.planning/PROJECT.md
@C:/Transparent Motivations/essentials/.planning/ROADMAP.md
@C:/Transparent Motivations/essentials/.planning/STATE.md
@C:/Transparent Motivations/essentials/.planning/phases/72-portland-or/72-RESEARCH.md
@C:/Transparent Motivations/essentials/.planning/phases/72-portland-or/72-PATTERNS.md

# Primary file to modify — read before editing
@C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts

# Analog files — read for exact copy patterns
@C:/EV-Accounts/backend/scripts/smoke-ca-geofences.ts
@C:/EV-Accounts/backend/scripts/verify-me-tiger-import.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register OR in the 4 loader config structures + TypeScript compile check</name>
  <files>C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — read lines 34-93 (STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, STATE_RUN_MAKEVALID) and lines 684-803 (ME block and CA block) to find exact insertion points before editing
  </read_first>
  <action>
Make exactly 4 additions to load-state-tiger-boundaries.ts. Do not change any existing entries; insert only after the ME entry in each config structure.

Addition 1 — STATE_LAYER_ALLOWLIST (after the ME entry at line ~40):
  OR: new Set(['cd119', 'sldu', 'sldl', 'place', 'county']),

Use cd119 NOT cd (per D-05 — TIGER 2024 OR file is tl_2024_41_cd119.zip, verified 2026-05-28). No cousub entry (per D-01 — OR COUSUBs are statistical CCDs FUNCSTAT='S', not in Phase 72 scope).

Addition 2 — STATE_CITY_ASSERTIONS (after the ME entry at line ~81):
  OR: ['Portland city'],

Same sentinel city name as ME (both states have a Portland city). NAMELSAD match is case-insensitive substring.

Addition 3 — STATE_RUN_MAKEVALID (after the ME entry at line ~92):
  OR: new Set(['cd119', 'sldu', 'sldl', 'place', 'county']),

All 5 OR layers receive ST_MakeValid. Mirrors ME entry exactly.

Addition 4 — fipsArg === '41' pre-flight assertion block inside processLayer, inserted immediately after the closing brace of the CA fipsArg === '06' block (at line ~803), before the "Stream records" comment at line ~805:

Copy the ME block structure (lines 684-725) substituting:
  - '23' → '41'
  - EXPECTED_ME_MTFCC → EXPECTED_OR_MTFCC
  - cd119: 2 → cd119: 6 (Oregon has 6 congressional districts post-2022 redistricting)
  - sldu: 35 → sldu: 30
  - sldl: 151 → sldl: 60
  - place: 23 → place: 242 (MEDIUM confidence — see Task 2 dry-run; update if actual count differs)
  - county: 16 → county: 36
  - "[ME MTFCC assertion]" error prefix → "[OR MTFCC assertion]"
  - "FIPS 23" in error message → "FIPS 41"
  - "ME MTFCC" log prefix → "OR MTFCC"

After all 4 insertions, run the TypeScript compile check from C:/EV-Accounts (the root, not backend subdirectory):
  cd C:/EV-Accounts && npx tsc --noEmit

Fix any syntax errors before proceeding.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts && npx tsc --noEmit && grep -c "OR:" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 with no TypeScript errors
    - grep -c "OR:" returns at least 3 (one hit each in STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, STATE_RUN_MAKEVALID)
    - grep "fipsArg.*41" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts returns at least 1 line
    - The OR allowlist entry contains exactly these layers: cd119, sldu, sldl, place, county — no cousub entry
    - No existing state entries (CA, TX, UT, IN, MA, ME) were modified
  </acceptance_criteria>
  <done>tsc --noEmit exits 0. OR is present in all 3 config Records and a fipsArg === '41' block exists in processLayer. No pre-existing state entries changed.</done>
</task>

<task type="auto">
  <name>Task 2: Create verify-or-tiger-import.sql and smoke-or-geofences.ts</name>
  <files>
    C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql
    C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts
  </files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/verify-me-tiger-import.sql — read full file; this is the direct template; substitute '23' → '41' and update expected counts
    - C:/EV-Accounts/backend/scripts/smoke-ca-geofences.ts — read full file (295 lines); this is the structural template; substitute '06' → '41', remove G4040 from counts, update test addresses and expectedCounts
  </read_first>
  <action>
Create two new script files.

FILE 1 — verify-or-tiger-import.sql:
Model on verify-me-tiger-import.sql substituting state='23' → state='41' throughout, and updating expected values:

Gate 1: invalid geometry count WHERE state = '41' — expected 0
Gate 2: non-Polygon/MultiPolygon geometry count WHERE state = '41' — expected 0
Gate 3: mtfcc row counts WHERE state = '41' — expected G4020|36, G4110|242, G5200|6, G5210|30, G5220|60 (update comment if dry-run revealed a different place count)
Gate 4: Portland OR sentinel — WHERE state = '41' AND geo_id = '4159000' — expected 1 row, name='Portland city', mtfcc='G4110'. Note: '4159000' is ASSUMED (STATEFP='41' + PLACEFP='59000'); update if actual geo_id differs after place layer loads
Gate 5: districts table counts — WHERE state IN ('OR', 'or') expected COUNTY|36, NATIONAL_LOWER|6, STATE_LOWER|60, STATE_UPPER|30. Note: OR has no pre-existing uppercase rows (unlike CA), so all rows should be lowercase 'or'; the IN clause covers both cases for robustness
Gate 6: Multnomah County sentinel — WHERE state = '41' AND mtfcc = 'G4020' AND geo_id = '41051' — expected 1 row, name='Multnomah County'
Gate 7: section-split check — geofence_boundaries rows with mtfcc IN ('G5200','G5210','G5220','G4020') AND state = '41' AND geo_id NOT IN (SELECT geo_id FROM essentials.districts) — expected 0 rows

File header comment: "Phase 72 OR TIGER import verification — SELECT only, no writes"
Run command comment: "psql $DATABASE_URL -f backend/scripts/verify-or-tiger-import.sql"

FILE 2 — smoke-or-geofences.ts:
Model on smoke-ca-geofences.ts using the CA-style structured assertions with allPassed flag and process.exit(1) on failure (not the simpler ME printout pattern — OR needs the forbiddenMtfcc check for Bend).

Imports: copy the pg + dotenv imports from smoke-ca-geofences.ts verbatim.

AddressTest interface: copy verbatim from smoke-ca-geofences.ts (fields: label, lon, lat, expectedMtfcc, forbiddenMtfcc?, expectedGeoIds?).

TEST_ADDRESSES — 3 entries:
  1. Portland OR City Hall: lon: -122.6794, lat: 45.5231
     expectedMtfcc: ['G4020', 'G4110', 'G5200', 'G5210', 'G5220']
     expectedGeoIds: { G4110: '4159000', G4020: '41051' }
     (Both geo_ids are ASSUMED — add a comment noting they will be confirmed after load)
  2. Bend OR (unincorporated rural, Deschutes County): lon: -121.3153, lat: 44.0582
     expectedMtfcc: ['G4020', 'G5200', 'G5210', 'G5220']
     forbiddenMtfcc: ['G4110']
     (Bend is incorporated but this coordinate is outside city limits — choose a clearly unincorporated Deschutes County point. If this coordinate happens to fall inside Bend city limits when tested, update to lon: -121.4, lat: 44.12 which is firmly rural)
  3. Salem OR (state capital, Marion County): lon: -123.0351, lat: 44.9429
     expectedMtfcc: ['G4020', 'G4110', 'G5200', 'G5210', 'G5220']

DB connection and queryBoundaries function: copy from smoke-ca-geofences.ts verbatim, substituting state = '06' → state = '41'.

Layer count check block: copy the CA pattern substituting '06' → '41', remove G4040 from mtfcc IN list (no COUSUB loaded for OR), update expectedCounts to: { G4020: 36, G4110: 242, G5200: 6, G5210: 30, G5220: 60 }.

Address assertion loop (expectedMtfcc, forbiddenMtfcc, expectedGeoIds checks): copy from smoke-ca-geofences.ts verbatim — generic logic, no state-specific changes needed.

Target city geo_id lookup block: replace CA's SF/LA/SJ/SD/SAC/Fremont/Berkeley lookup with an OR block querying WHERE state = '41' AND mtfcc = 'G4110' AND name IN ('Portland city', 'Salem city', 'Eugene city') — prints geo_ids for confirmation.

Final result block and error handler: copy from smoke-ca-geofences.ts verbatim.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts && npx tsc --noEmit && ls C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql && ls C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts</automated>
  </verify>
  <acceptance_criteria>
    - verify-or-tiger-import.sql exists at C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql
    - verify-or-tiger-import.sql contains state = '41' (not '23') in all 7 gates
    - verify-or-tiger-import.sql Gate 3 comment shows expected G4020|36, G4110|242, G5200|6, G5210|30, G5220|60
    - verify-or-tiger-import.sql Gate 7 section-split check uses mtfcc IN ('G5200','G5210','G5220','G4020')
    - smoke-or-geofences.ts exists at C:/EV-Accounts/backend/scripts/smoke-or-geofences.ts
    - smoke-or-geofences.ts contains forbiddenMtfcc: ['G4110'] for the Bend address test
    - smoke-or-geofences.ts queryBoundaries uses state = '41'
    - smoke-or-geofences.ts expectedCounts does NOT include G4040 key
    - npx tsc --noEmit exits 0 (smoke-or-geofences.ts compiles cleanly)
  </acceptance_criteria>
  <done>Both files exist, compile cleanly, and contain the correct OR-specific constants. verify-or-tiger-import.sql has 7 gates with state='41'. smoke-or-geofences.ts has 3 test addresses including the Bend forbiddenMtfcc check.</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run place layer, lock count, then run all 5 TIGER layers</name>
  <files>C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — re-read only the fipsArg === '41' block you just added to confirm place: 242 before dry-run
  </read_first>
  <action>
Run from C:/EV-Accounts/backend (not C:/EV-Accounts root — dotenv looks for .env in cwd).

Step 1 — Dry-run the place layer to confirm G4110 count:
  npx tsx scripts/load-state-tiger-boundaries.ts --state OR --fips 41 --layers place --dry-run

Read the output carefully. The dry-run prints the actual G4110 record count without writing to DB.

If the actual count differs from 242: update EXPECTED_OR_MTFCC.place in load-state-tiger-boundaries.ts to the actual count, also update the Gate 3 comment in verify-or-tiger-import.sql, and update the expectedCounts.G4110 in smoke-or-geofences.ts. Run tsc --noEmit from C:/EV-Accounts after any edit.

Step 2 — Run all 5 layers live (sequentially, wait for each to complete):
  npx tsx scripts/load-state-tiger-boundaries.ts --state OR --fips 41 --layers cd119
  npx tsx scripts/load-state-tiger-boundaries.ts --state OR --fips 41 --layers sldu
  npx tsx scripts/load-state-tiger-boundaries.ts --state OR --fips 41 --layers sldl
  npx tsx scripts/load-state-tiger-boundaries.ts --state OR --fips 41 --layers place
  npx tsx scripts/load-state-tiger-boundaries.ts --state OR --fips 41 --layers county

Expected terminal output per layer:
  - cd119: OR MTFCC pre-flight assertion PASSED: 6 records
  - sldu: OR MTFCC pre-flight assertion PASSED: 30 records
  - sldl: OR MTFCC pre-flight assertion PASSED: 60 records
  - place: OR MTFCC pre-flight assertion PASSED: N records (N = dry-run confirmed count)
  - county: OR MTFCC pre-flight assertion PASSED: 36 records

If any layer reports 0 records inserted or the pre-flight assertion throws MtfccAssertionError, stop and diagnose before proceeding:
  - cd119 silent no-op → verify STATE_LAYER_ALLOWLIST OR entry uses 'cd119' not 'cd'
  - MtfccAssertionError on place → actual G4110 count changed from dry-run; update EXPECTED_OR_MTFCC.place to match
  - county reports 0 → confirm filterByStatefp is true for county layer and fipsArg '41' matches STATEFP field

Upserts are idempotent (ON CONFLICT DO NOTHING) — re-runs are safe if needed.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsx -e "require('dotenv').config(); const {Client}=require('pg'); const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(\"SELECT mtfcc, COUNT(*) AS cnt FROM essentials.geofence_boundaries WHERE state='41' GROUP BY mtfcc ORDER BY mtfcc\")).then(r=>{console.log(JSON.stringify(r.rows));c.end()})"</automated>
  </verify>
  <acceptance_criteria>
    - cd119 layer: pre-flight PASSED with exactly 6 records; loader exits without error
    - sldu layer: pre-flight PASSED with exactly 30 records; loader exits without error
    - sldl layer: pre-flight PASSED with exactly 60 records; loader exits without error
    - place layer: pre-flight PASSED; the actual count matches the value set in EXPECTED_OR_MTFCC.place (locked after dry-run)
    - county layer: pre-flight PASSED with exactly 36 records; loader exits without error
    - DB query SELECT mtfcc, COUNT(*) FROM essentials.geofence_boundaries WHERE state='41' GROUP BY mtfcc ORDER BY mtfcc returns 5 rows: G4020|36, G4110|N (dry-run confirmed), G5200|6, G5210|30, G5220|60
    - No MtfccAssertionError thrown on any layer
  </acceptance_criteria>
  <done>All 5 layers ran to completion. DB query returns 5 rows with correct MTFCC counts. Total geofence_boundaries rows for state='41' = 36 + N + 6 + 30 + 60 where N is the dry-run confirmed G4110 count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → loader | TIGER ZIP files downloaded over HTTPS from www2.census.gov |
| Loader → PostGIS | Parsed shapefile records upserted into essentials.geofence_boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | TIGER download URL construction | mitigate | STATE_LAYER_ALLOWLIST hardcodes exact layer keys; URL is constructed from Census HTTPS base + state FIPS + layer key — no user input in URL path; OR key verified 2026-05-28 at TIGER server |
| T-72-02 | Tampering | Pre-flight assertion bypass | mitigate | fipsArg === '41' block streams the shapefile in a counting pass before any DB write; MtfccAssertionError is fatal and named — loader cannot silently proceed with wrong counts |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; all tooling (shapefile, adm-zip, pg) pre-existing in node_modules; no slopcheck needed |
</threat_model>

<verification>
1. npx tsc --noEmit passes in C:/EV-Accounts after all 3 file changes
2. grep "OR:" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts returns entries in STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, and STATE_RUN_MAKEVALID
3. grep "fipsArg.*41" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts returns the pre-flight block
4. Both verify-or-tiger-import.sql and smoke-or-geofences.ts exist in C:/EV-Accounts/backend/scripts/
5. DB query SELECT mtfcc, COUNT(*) FROM essentials.geofence_boundaries WHERE state='41' GROUP BY mtfcc ORDER BY mtfcc returns 5 rows with correct counts
6. No MtfccAssertionError thrown on any of the 5 layers
</verification>

<success_criteria>
- OR is registered in STATE_LAYER_ALLOWLIST with layers cd119/sldu/sldl/place/county (D-01)
- OR is registered in STATE_CITY_ASSERTIONS with sentinel 'Portland city' (D-01)
- OR is registered in STATE_RUN_MAKEVALID with all 5 layers (D-01)
- fipsArg === '41' pre-flight block exists with counts matching dry-run actuals
- verify-or-tiger-import.sql exists with 7 gates covering GEO-OR-01/05/06
- smoke-or-geofences.ts exists with 3 test addresses (Portland + Bend + Salem) following CA structured-assertion pattern
- 5 layer rows in geofence_boundaries for state='41': G4020|36, G4110|N, G5200|6, G5210|30, G5220|60
- 132 rows in essentials.districts for state='or' across COUNTY|36, NATIONAL_LOWER|6, STATE_LOWER|60, STATE_UPPER|30
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create .planning/phases/72-portland-or/72-01-SUMMARY.md
</output>
