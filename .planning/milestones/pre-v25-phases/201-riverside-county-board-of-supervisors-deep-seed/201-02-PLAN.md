---
phase: 201-riverside-county-board-of-supervisors-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["201-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1314_riverside_county_board_of_supervisors.sql
autonomous: false
requirements: [CV-01]
must_haves:
  truths:
    - "A standalone 'Riverside County, California, US' government (type='County', state='CA', geo_id='06065') exists, NOT nested under State of California"
    - "One 'Board of Supervisors' chamber (official_count=5) holds exactly 5 by-district supervisor offices"
    - "Each of the 5 supervisor offices links to its OWN LOCAL X0021 district (never the pre-existing COUNTY G4020 06065 row)"
    - "Roster is Medina(D1)/Spiegel(D2)/Washington(D3)/Perez(D4)/Gutierrez(D5); board ONLY — no constitutional-officer office (D-01); no departed member seeded"
    - "The 2026 Chair (Spiegel, D2, ext_id -4010002) is surfaced as a TITLE ANNOTATION on her existing D2 supervisor seat (title='Supervisor, District 2 (Chair)'); the other four carry plain 'Supervisor, District N'. Still exactly 5 offices, no separate Chair office (D-02). Re-verify the current chair at execute time."
    - "The structural migration is applied to production and registered in the ledger; 5 politician UUIDs captured for Plans 03/04"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1314_riverside_county_board_of_supervisors.sql"
      provides: "government + chamber + 5 LOCAL districts + 5 politicians + 5 offices (D2 Chair-annotated) + office_id backfill + pre-flight + post-verify DO gates + ledger registration"
      contains: "Board of Supervisors"
  key_links:
    - from: "essentials.offices (5 supervisors)"
      to: "essentials.districts (X0021 LOCAL)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='X0021' AND state='ca'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'Board of Supervisors'"
      to: "essentials.governments 'Riverside County, California, US'"
      via: "government_id subquery scoped by name"
      pattern: "Riverside County, California, US"
---

<objective>
Author and apply the structural migration that seeds Riverside County as a STANDALONE county
government (its own `essentials.governments` row at geo_id `06065`, NOT parented to State of
California), a single `Board of Supervisors` chamber, 5 LOCAL supervisor-district rows on the Plan 01
X0021 geofences, and 5 by-district supervisor offices + politicians (ext_id `-4010001..-4010005`).
Greenfield — no Riverside County government row exists today. BOARD ONLY (D-01): the 5 elected
supervisors — Sheriff/DA/Assessor and other constitutional officers are DEFERRED (matches
Pima/Clark/WashCo board-only precedent).

Purpose: Satisfies ROADMAP success criterion #1 (standalone `County of Riverside` government + Board
of Supervisors chamber) and the government/roster half of #3 (5 supervisors on 5 LOCAL geofences). The
X% MTFCC catch-all already routes X0021 with NO code change, so applying this migration makes every
Riverside County address resolve to its ONE supervisor. Plans 03 (headshots) and 04 (stances) consume
the 5 politician UUIDs captured here.
Output: `C:/EV-Accounts/backend/migrations/1314_riverside_county_board_of_supervisors.sql` (structural,
registered), applied to production, with the 5 UUIDs recorded in the SUMMARY.

CRITICAL (D-01 + D-02): exactly 5 offices — no 6th "Chair" office, no constitutional-officer office,
one 'Board of Supervisors' chamber, all 5 elected BY DISTRICT. The 2026 Chair (Karen Spiegel, D2,
ext_id -4010002) is surfaced as a TITLE ANNOTATION on her existing D2 supervisor seat —
`title='Supervisor, District 2 (Chair)'` — per the by-district relabel / rotational-mayor pattern (the
rotational role lives in the `title` field ON the occupant's seat, NOT in `role_canonical` and NOT as
a separate office). The other four carry plain `'Supervisor, District N'`. `role_canonical` stays NULL
for all 5. The Chair rotates annually and June-2026 certification is pending — RE-VERIFY the current
chair at the Task 2 roster checkpoint before applying (D-02); if it rotated, move the `(Chair)` suffix
to the confirmed chair's seat (only one office may carry it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md
@.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-01-SUMMARY.md

<execution_note>
Depends on Plan 01: the 5 X0021 geofences MUST be live (the migration's pre-flight DO block RAISE
EXCEPTIONs if <5 exist). `gsd-executor` authors the `.sql` file only. Task 2 (roster checkpoint +
apply + UUID capture) and Task 3 (post-apply assertions) are ORCHESTRATOR-RUN via `psql -f` against
`C:/EV-Accounts/backend/.env` `DATABASE_URL`. A migration authored but not applied is a false positive
— the apply MUST happen before Plans 03/04. Commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (from 201-PATTERNS.md lines 88-200, mirroring Pima's 1288 near-exact template):
- essentials.governments (id, name, type, state, city, geo_id): insert 'Riverside County, California,
  US', type='County', state='CA' (UPPERCASE — free-text label), city=NULL, geo_id='06065'. Standalone:
  NO parent link to State of California. Idempotent WHERE NOT EXISTS on name.
- essentials.chambers (id, name, name_formal, government_id, official_count): name='Board of
  Supervisors', name_formal='Riverside County Board of Supervisors', official_count=5, government_id
  via name subquery. `slug` is a GENERATED column — NEVER include it in the INSERT. Idempotent WHERE
  NOT EXISTS on (name, government_id).
- essentials.districts (id, district_type, state, geo_id, label, mtfcc): 5 rows, district_type='LOCAL',
  state='ca' (LOWERCASE — the LOCAL-tier routing join key), geo_id='riverside-ca-supervisor-district-N',
  label='Riverside County Supervisor District N', mtfcc='X0021'. Idempotent WHERE NOT EXISTS on
  (geo_id, district_type, state).
- essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed,
  is_vacant, is_incumbent, external_id): ext_id -4010001..-4010005 (the -4010 block is DB-verified free
  — -4007 Pima / -4008 Tucson / -4009 Oro Valley are taken; re-verify unclaimed at execute time before
  hardcoding). external_id UNIQUE (ON CONFLICT (external_id) DO NOTHING). party stored, NEVER displayed
  (antipartisan).
- essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state,
  is_appointed_position, is_vacant, role_canonical): title per-seat — D1 Medina 'Supervisor, District
  1', D2 Spiegel 'Supervisor, District 2 (Chair)' (the rotational-Chair title annotation), D3
  Washington 'Supervisor, District 3', D4 Perez 'Supervisor, District 4', D5 Gutierrez 'Supervisor,
  District 5'; representing_state='CA' (UPPERCASE), role_canonical=NULL (annotation lives in `title`),
  guard on (district_id, politician_id).
- Roster (CONTEXT.md recon — RE-VERIFY current officeholders at execute time; June-2026 certification
  pending): D1 Jose Medina (-4010001), D2 Karen Spiegel (-4010002, 2026 Chair — title-annotated), D3
  Chuck Washington (-4010003), D4 V. Manuel "Manny" Perez (-4010004), D5 Dr. Yxstian Gutierrez
  (-4010005). NO appointee flagged in recon — confirm at execute time; if none, gate (d) asserts 0
  appointed (contrast Pima, which asserted 1).
- Collision note: unlike Pima's 04019 3-way collision, NO known 06065 collision (201-PATTERNS.md
  confirmed no 06065 references in migrations; CA state legislative districts are not keyed by county
  FIPS). Re-verify at execute time; keep the defensive discipline regardless — NEVER join/filter on a
  bare `geo_id='06065'` for an office; always scope `district_type='LOCAL' AND mtfcc='X0021' AND
  state='ca'`, and NEVER touch the pre-existing COUNTY G4020 06065 boundary row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the structural Riverside County Board of Supervisors migration</name>
  <files>C:/EV-Accounts/backend/migrations/1314_riverside_county_board_of_supervisors.sql</files>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (lines 88-200 — the full structural pattern: government/chamber INSERT, pre-flight geofence assertion, 5× LOCAL district INSERT, the WITH ins_p...INSERT INTO offices per-supervisor block, Chair title-annotation on D2, office_id backfill, post-verify DO gate (a-f), ledger registration; note gate (d) appointed-count = 0 for Riverside unless an appointee is confirmed at execute time)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (D-01 board-only — NO constitutional officers; D-02 LOCKED — Chair is a title annotation on the sitting supervisor's D2 seat; § code_context established patterns — slug GENERATED, lowercase districts.state)
    - C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql (PRIMARY analog — read in full: this is a near-exact template. Substitute names/geo_id/mtfcc/ext_id range only. Riverside diverges on (1) the Chair sits on D2 not D3, (2) NO appointee in recon so gate (d) asserts 0 appointed, (3) no 04019-style collision guard needed but keep the LOCAL/X0021 scoping discipline)
  </read_first>
  <action>
    First run `ls C:/EV-Accounts/backend/migrations | grep -oE '^[0-9]+' | sort -n | tail -1` to confirm
    on-disk MAX (mapping saw 1313 — `1313_oro_valley_council_6_stances.sql`). Name this file
    disk-MAX+1 (provisionally `1314_riverside_county_board_of_supervisors.sql`). If disk MAX drifted,
    use actual disk-MAX+1 and note the deviation (disk-MAX authoritative, not ledger-MAX).

    Author a STRUCTURAL migration wrapped in BEGIN/COMMIT. Pre-flight DO block: assert
    `COUNT(*) >= 5` in essentials.geofence_boundaries WHERE state='ca' AND mtfcc='X0021' — RAISE
    EXCEPTION 'run load-riverside-supervisor-boundaries.ts first' otherwise (gates on Plan 01).

    Insert the standalone government 'Riverside County, California, US' (type='County', state='CA'
    uppercase, city=NULL, geo_id='06065') idempotently (WHERE NOT EXISTS on name) — NO parent link to
    State of California. Insert the 'Board of Supervisors' chamber (name_formal='Riverside County Board
    of Supervisors', official_count=5) scoped to that government's id via name subquery, idempotent on
    (name, government_id). Do NOT include the GENERATED `slug` column.

    Insert 5 LOCAL district rows (district_type='LOCAL', state='ca' LOWERCASE,
    geo_id='riverside-ca-supervisor-district-1..5', label='Riverside County Supervisor District N',
    mtfcc='X0021'), each idempotent WHERE NOT EXISTS on (geo_id, district_type, state). Do NOT insert
    or reference the pre-existing COUNTY G4020 06065 boundary row.

    Seed 5 supervisors using the Pima `WITH ins_p AS (INSERT INTO politicians ... RETURNING id)
    INSERT INTO offices ... FROM districts d CROSS JOIN ins_p p WHERE d.geo_id=
    'riverside-ca-supervisor-district-N' AND d.district_type='LOCAL' AND d.state='ca' AND
    d.mtfcc='X0021'` block, one per supervisor. Office titles (each keyed to the matching
    district/politician):
      - D1 Jose Medina (ext_id -4010001) → title='Supervisor, District 1'
      - D2 Karen Spiegel (ext_id -4010002) → title='Supervisor, District 2 (Chair)'  ← Chair title annotation (D-02)
      - D3 Chuck Washington (ext_id -4010003) → title='Supervisor, District 3'
      - D4 V. Manuel "Manny" Perez (ext_id -4010004) → title='Supervisor, District 4'
      - D5 Dr. Yxstian Gutierrez (ext_id -4010005) → title='Supervisor, District 5'
    All 5 offices: representing_state='CA' uppercase, is_appointed_position=false, role_canonical=NULL
    (the Chair distinction lives ONLY in the D2 `title` string). Guard each office INSERT on
    `NOT EXISTS (... o.district_id=d.id AND o.politician_id=p.id)`. Politicians: is_active=true,
    is_incumbent=true, is_vacant=false, is_appointed=false on all 5 (recon flags NO appointee — if the
    execute-time roster check reveals one, set is_appointed=true on that member and adjust gate (d)).

    Karen Spiegel (D2) is the 2026 Chair, surfaced ONLY via her office `title` annotation
    (`'Supervisor, District 2 (Chair)'`) — NOT a separate office, and elected by District 2 like the
    others. The other four titles carry NO `(Chair)` marker.

    office_id backfill: UPDATE politicians SET office_id=o.id FROM offices o WHERE o.politician_id=
    p.id AND p.external_id BETWEEN -4010005 AND -4010001 AND p.office_id IS NULL.

    Post-verify DO gate (RAISE EXCEPTION → rollback unless ALL hold): (a) exactly 1 government named
    'Riverside County, California, US'; (b) exactly 5 offices joined to LOCAL X0021 districts
    (d.geo_id LIKE 'riverside-ca-supervisor-district-%' AND d.district_type='LOCAL' AND d.state='ca'
    AND d.mtfcc='X0021'); (c) each of those 5 districts holds exactly 1 office (GROUP BY district_id
    HAVING count(*)<>1 → 0 rows); (d) is_appointed=true count among the 5 politicians = exactly 0
    (recon flags no appointee — adjust only if execute-time roster check finds one); (e) section-split:
    0 offices attached under any government OTHER than Riverside County via these 5 districts; (f)
    EXACTLY 1 of the 5 offices carries a Chair annotation (`o.title LIKE '%(Chair)%'`) AND that
    office's politician has external_id=-4010002 (Karen Spiegel, D2) — no other office may carry
    `(Chair)`. There is NO separate 6th Chair office and NO constitutional-officer office (still
    exactly 5 offices total).

    Register OUTSIDE BEGIN/COMMIT: INSERT INTO supabase_migrations.schema_migrations (version, name)
    VALUES ('1314','riverside_county_board_of_supervisors') ON CONFLICT (version) DO NOTHING
    (substitute actual disk-MAX+1). Do NOT invent politician UUIDs — offices resolve politician_id via
    the ins_p CTE at apply time.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1314_riverside_county_board_of_supervisors.sql"; test -f "$F" && grep -qF "Board of Supervisors" "$F" && grep -qF "Riverside County, California, US" "$F" && grep -qF "Supervisor, District 1" "$F" && grep -qF "Supervisor, District 2 (Chair)" "$F" && grep -qF "Supervisor, District 3" "$F" && grep -qF "Supervisor, District 4" "$F" && grep -qF "Supervisor, District 5" "$F" && ! grep -qF "Supervisor, District 1 (Chair)" "$F" && ! grep -qF "Supervisor, District 3 (Chair)" "$F" && ! grep -qF "Supervisor, District 4 (Chair)" "$F" && ! grep -qF "Supervisor, District 5 (Chair)" "$F" && ! grep -qiE "role_canonical\s*=\s*'" "$F" && ! grep -qiE "\bslug\b\s*[,)]" "$F" && [ "$(grep -c "o.district_id = d.id AND o.politician_id = p.id" "$F")" = "5" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1314).
    - Grep confirms `Board of Supervisors` and `Riverside County, California, US` present.
    - Grep count of the office↔district guard `o.district_id = d.id AND o.politician_id = p.id` is exactly 5 (no 6th office; no constitutional-officer office — D-01).
    - Every office↔district WHERE scopes `district_type = 'LOCAL'` AND `mtfcc = 'X0021'`; lowercase `state = 'ca'` on district rows; uppercase `'CA'` only on governments.state / offices.representing_state.
    - Grep confirms 5 external_id literals -4010001..-4010005.
    - Chair modeling (D-02): D2 office carries `title='Supervisor, District 2 (Chair)'`; the other four are plain `'Supervisor, District N'` with NO `(Chair)` marker; `role_canonical` is NULL for all 5.
    - GENERATED `slug` column NOT present in the chambers INSERT.
    - Grep confirms `is_appointed` is false on all 5 (unless execute-time roster check confirmed an appointee).
    - Pre-flight DO block asserts >=5 X0021 geofences; post-verify DO gate present with the 6 assertions (a-f) — including gate (d) appointed-count=0 and gate (f): exactly 1 office carries `(Chair)` on the -4010002 (D2) seat.
    - Ledger-registration line `('1314'` (or actual number) present OUTSIDE COMMIT.
    - Migration never inserts or joins bare `geo_id = '06065'` for an office; never touches the COUNTY G4020 boundary.
  </acceptance_criteria>
  <done>Structural migration authored: standalone Riverside County government + Board chamber + 5 LOCAL X0021 districts + 5 supervisor offices/politicians (Spiegel D2 carries the Chair title annotation, no appointee, board-only), backfill, pre-flight + post-verify gates (incl. exactly-one-Chair-annotation on D2), ledger registration — ready to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Roster-currency re-verification + apply structural migration (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (§ Recon facts — the 5-member roster + 2026 chair Spiegel D2, "verify current officeholders at execute time; 2026 certification pending"; D-02 re-verify chair; Claude's Discretion — roster-currency re-check)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-01-SUMMARY.md (the 5 X0021 geofences the migration gates on)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor file action. The operator re-confirms the current 5 sitting supervisors + who chairs the board (the chair drives the D2-vs-other title annotation) + the June-2026 election certification status; on approval the ORCHESTRATOR applies migration 1314 via `psql -f` and captures the 5 politician UUIDs. See &lt;how-to-verify&gt; for the exact sequence.</action>
  <what-built>
    Migration 1314 (Task 1) is authored but NOT applied. This checkpoint confirms the 5-member roster +
    2026 chair are still current before applying to production (mcp__supabase-local IS live prod). Recon
    (2026-07-12) lists Medina/Spiegel/Washington/Perez/Gutierrez with Spiegel as 2026 Chair, but the
    annual chair vote and June-2026 election results are pending final certification. Because the Chair
    is a TITLE ANNOTATION on the sitting supervisor (D-02), a chair rotation since recon would move the
    `(Chair)` suffix to a different district seat — confirm the current chair here so the annotation
    lands on the right seat. Constitutional officers stay OUT (D-01, board-only).
  </what-built>
  <how-to-verify>
    1. Open the county Board-of-Supervisors page (and each district site: rivcodistrict1.org,
       rivcodistrict2.org, supervisorchuckwashington.com, rivco4.org, rivcodistrict5.org) and confirm
       the 5 sitting supervisors still match recon: D1 Jose Medina, D2 Karen Spiegel (Chair), D3 Chuck
       Washington, D4 Manny Perez, D5 Dr. Yxstian Gutierrez.
    2. Confirm no new resignation/appointment/chair rotation has landed since 2026-07-12, and check
       whether any June-2026 election result has certified and seated a new member. If the chair has
       rotated, move the `(Chair)` title annotation to THAT supervisor's seat before apply (only one
       office may carry it). If a member was appointed, set is_appointed=true on that member and adjust
       gate (d) to expect that count. If a seat is genuinely vacant, seed a vacant office
       (is_vacant=true), never a departed member.
    3. On approval, the ORCHESTRATOR applies via
       `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1314_riverside_county_board_of_supervisors.sql`
       (executor has no DB access), then captures the 5 UUIDs
       (`SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id BETWEEN -4010005 AND -4010001 ORDER BY external_id`)
       into 201-02-SUMMARY.md for Plans 03/04, and commits the migration via `git -C "C:/EV-Accounts"`.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the live roster + chair match the seeded roster (or supplies the exact delta to patch before apply — including moving the Chair annotation if the chair rotated, or flagging an appointee).
    - Migration applies with the in-transaction post-verify DO gate passing (no RAISE EXCEPTION → COMMIT), including gate (f) — exactly one office carries the Chair annotation, on the confirmed chair's D-seat — and gate (d) matching the confirmed appointed count.
    - `psql -tAc "SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1314'"` returns 1 (structural registered).
    - The 5 politician UUIDs captured into 201-02-SUMMARY.md (external_id → UUID → full_name) for Plans 03/04.
    - Migration committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live roster + chair are confirmed and the migration is applied + UUIDs captured, or describe the roster/chair delta to patch first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply structural assertions (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB assertions, no files modified</files>
  <read_first>
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-CONTEXT.md (§ code_context Integration Points — 5 offices 1-per-district, section-split; Established Patterns — casing)
    - .planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-PATTERNS.md (§ Section-split / district-collision guard — never bare geo_id='06065'; always scope LOCAL/X0021)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). After apply, run seed assertions via
    `psql "$DATABASE_URL" -tAc` and record in 201-02-SUMMARY.md. Assert: (a) exactly 1 government
    'Riverside County, California, US' at geo_id='06065', type='County', NOT parented to State of
    California; (b) exactly 5 offices under the 'Board of Supervisors' chamber of that government;
    (c) each of the 5 LOCAL X0021 districts holds exactly 1 office (GROUP BY district_id HAVING
    count(*)<>1 → 0 rows); (d) is_appointed=true among the 5 = the confirmed count (0 per recon);
    (e) DISTINCT district state on the 5 linked districts = only 'ca' (lowercase) and DISTINCT mtfcc =
    only 'X0021'; (f) section-split: 0 offices reachable from these 5 districts under any government
    other than Riverside County; (g) Chair annotation: exactly 1 of the 5 offices has
    `title LIKE '%(Chair)%'` AND it is the office whose politician external_id=-4010002 (Karen Spiegel,
    D2), and no other office carries `(Chair)`. Any mismatch blocks the phase — do NOT proceed to
    Plan 03.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='Riverside County, California, US' AND geo_id='06065' AND type='County')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id LIKE 'riverside-ca-supervisor-district-%' AND d.district_type='LOCAL' AND d.state='ca' AND d.mtfcc='X0021')=5 AND NOT EXISTS (SELECT 1 FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0021' GROUP BY o.district_id HAVING count(*)<>1) AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0021' AND o.title LIKE '%(Chair)%')=1 AND (SELECT p.external_id FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.politicians p ON p.id=o.politician_id WHERE d.mtfcc='X0021' AND o.title LIKE '%(Chair)%')=-4010002"</automated>
  </verify>
  <acceptance_criteria>
    - Standalone Riverside County government count = 1 (geo_id 06065, type County, not under State of CA).
    - 5 offices under 'Board of Supervisors' on LOCAL X0021 districts.
    - Each X0021 district holds exactly 1 office (no HAVING count<>1 violation).
    - Appointed count among the 5 = the confirmed count (0 per recon).
    - DISTINCT linked-district state = only 'ca'; DISTINCT mtfcc = only 'X0021'.
    - Section-split query returns 0 rows (no office leaked under a non-Riverside government).
    - Chair annotation: exactly 1 office carries `(Chair)`, and it is the D2/-4010002 (Karen Spiegel) seat; the other four carry no marker.
    - The combined boolean SELECT returns `t`.
  </acceptance_criteria>
  <done>Live production confirms the standalone Riverside County government, 5 supervisor offices (1 per LOCAL X0021 district), board-only (no appointee/no constitutional officer), the Chair title annotation on the D2/Spiegel seat only, correct casing, clean section-split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | Hand-authored static SQL crosses into live `essentials` via `psql -f` (service-role) |
| Plan 01 X0021 geofences → new offices | Office↔district joins consume the X0021 rows; wrong casing/type silently mislinks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-COLL | Tampering | office↔district WHERE / bare geo_id join | mitigate | Every join scopes `district_type='LOCAL' AND mtfcc='X0021' AND state='ca'`; migration never touches bare geo_id='06065' or the COUNTY G4020 row; post-verify asserts 5 offices on X0021 only |
| T-201-SPLIT | Tampering | office↔chamber↔government section-split | mitigate | Chamber scoped to the Riverside government id subquery; post-verify DO gate section-split assertion → 0 rows or the transaction rolls back |
| T-201-SCOPE | Tampering | scope creep beyond board-only (D-01) | mitigate | Exactly 5 offices, one Board of Supervisors chamber, NO constitutional-officer office; grep gate + post-verify assert the office guard count = 5 |
| T-201-ROST | Spoofing | roster / appointed-member rows | mitigate | Seed only the 5 current supervisors; departed members never inserted; Task 2 blocking human roster-currency check re-confirms officeholders + June-2026 certification |
| T-201-CHAIR | Tampering | Chair modeling (double-count / mislabel / stale-annotation) | mitigate | Title-annotation model per D-02: exactly 5 offices (no separate Chair office), the `(Chair)` suffix on the D2/-4010002 seat ONLY (post-verify gate (f) + Task 3 assert); annotation rotates annually so Task 2 re-confirms the chair before apply |
| T-201-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `.env`; applied by the inline orchestrator; never hardcoded |
| T-201-SC | Tampering | package installs | accept | No new installs — reuses installed psql; Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 1 standalone Riverside County government (geo_id 06065, type County), NOT under State of CA.
- 5 supervisor offices under 'Board of Supervisors', 1 per LOCAL X0021 district; board-only (no constitutional officer).
- Spiegel (D2) chair = title annotation on her D2 seat; exactly one office carries `(Chair)`; no separate 6th office.
- Section-split 0 rows; casing lowercase 'ca' on districts; structural migration registered.
- 5 politician UUIDs captured for Plans 03/04.
</verification>

<success_criteria>
ROADMAP #1 (standalone `County of Riverside` government + Board of Supervisors chamber, NOT nested
under State of CA) and the roster half of #3 are TRUE in production; the migration is applied +
registered; the UUID manifest is recorded.
</success_criteria>

<output>
Create `.planning/phases/201-riverside-county-board-of-supervisors-deep-seed/201-02-SUMMARY.md` when
done — include the 5 external_id → UUID → full_name manifest (required by Plans 03 and 04) and record
Spiegel as 2026 Chair surfaced via the D2 `title` annotation (`'Supervisor, District 2 (Chair)'`),
noting the annotation rotates annually and any June-2026 certification delta.
</output>
</content>
