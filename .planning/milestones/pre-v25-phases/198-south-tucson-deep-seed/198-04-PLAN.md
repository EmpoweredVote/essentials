---
phase: 198-south-tucson-deep-seed
plan: 04
type: execute
wave: 4
depends_on: ["198-03"]
files_modified:
  - src/lib/buildingImages.js
  - src/lib/coverage.js
autonomous: false
requirements: [BANR-01, SUB-04]
must_haves:
  truths:
    - "A licensed South Tucson community banner (real photo, no AI, no aerial) is processed to the banner spec (1700×540 @ 3.15:1) and uploaded to Storage at cities/south-tucson.jpg"
    - "buildingImages.js CURATED_LOCAL has a state-scoped `'south tucson'` entry (QUOTED key — contains a space, mirroring the 'oro valley' style) pointing at the uploaded banner"
    - "coverage.js gets a 'South Tucson' area APPENDED to the EXISTING Arizona COVERAGE_STATES block (which today holds Tucson, Oro Valley, Marana, Sahuarita) — NOT a new Arizona block, and NOT an entry in COVERAGE_COUNTIES (where Pima County lives) — with a DB-honest chip (hasContext:true, since stances landed in Plan 03) browsing geo_id 0468850"
    - "The South Tucson banner is a DISTINCT cultural/urban identity — MUST avoid Catalina/mountain shots (Pima County 193 + Oro Valley 195), Tortolita/Dove Mountain (Marana 196), downtown Tucson/Hotel Congress (Tucson 194), Sahuarita Lake/Santa Ritas (Sahuarita 197), and the AZ-state Phoenix skyline; South Tucson is the ONE covered jurisdiction whose banner reads as people/street/art (mural / South-4th-Ave streetscape / welcome sign / City Hall) rather than landscape"
    - "A real South Tucson street address, queried against the live candidates-search API, resolves to the seeded City of South Tucson council of 7 at-large members (3 title-annotated Mayor/Vice-Mayor/Acting-Mayor), never double-matched with Tucson's council (the enclave donut-hole verified in Plan 01) — the phase-goal's resident-facing outcome, end-to-end"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "CURATED_LOCAL `'south tucson'` → uploaded banner URL"
      contains: "south tucson"
    - path: "src/lib/coverage.js"
      provides: "'South Tucson' area appended to the EXISTING Arizona COVERAGE_STATES block, browseGovernmentList ['0468850'], hasContext:true"
      contains: "South Tucson"
  key_links:
    - from: "coverage.js COVERAGE_STATES Arizona → 'South Tucson'"
      to: "buildingImages.js CURATED_LOCAL 'south tucson'"
      via: "browse label 'South Tucson' → representingCity → getBuildingImages substring match"
      pattern: "south tucson"
    - from: "buildingImages.js 'south tucson'"
      to: "Storage cities/south-tucson.jpg"
      via: "src URL (state-scoped 'AZ')"
      pattern: "cities/south-tucson.jpg"
---

<objective>
Give the City of South Tucson its licensed community banner (BANR-01) and its DB-honest coverage chip.
Source a real South Tucson photo ONE AT A TIME (no AI, no aerial), process it to the banner spec via
`scripts/banners/process_banner.py` (1700×540 @ 3.15:1), upload to Storage at `cities/south-tucson.jpg` via
`upload_banner.py`, wire it into `src/lib/buildingImages.js` CURATED_LOCAL as a state-scoped `'south tucson'`
entry (QUOTED key — it contains a space), and surface the City of South Tucson in `src/lib/coverage.js`.

CRITICAL DELTA (Pitfall 7): the Arizona `COVERAGE_STATES` block ALREADY EXISTS in `coverage.js` today,
holding `'Tucson'` (Phase 194), `'Oro Valley'` (Phase 195), `'Marana'` (Phase 196), and `'Sahuarita'`
(Phase 197). This phase APPENDS a new `'South Tucson'` area object to that EXISTING block's `areas` array
(after the Sahuarita entry). Do NOT create a second `{ name: 'Arizona', ... }` block (that would be a
duplicate-block bug), and do NOT add South Tucson to `COVERAGE_COUNTIES` (where Pima County lives —
untouched). In `buildingImages.js` the key MUST be quoted `'south tucson':` (it contains a space — mirror
the `'oro valley':` style, NOT the unquoted single-word `marana:`/`sahuarita:` style); the Storage file is
`cities/south-tucson.jpg` (hyphenated).

CRITICAL (D-04 / BANR-01 — visual distinctiveness, cultural/urban not landscape): South Tucson needs its own
DISTINCT cultural/urban identity — it is the ONE covered jurisdiction whose banner should read as
people/street/art rather than landscape. The banner MUST avoid: Catalina/mountain imagery (Pima County 193
+ Oro Valley 195), Tortolita/Dove Mountain (Marana 196), downtown Tucson / Hotel Congress (Tucson 194),
Sahuarita Lake / Santa Rita Mountains (Sahuarita 197), and the AZ-state Phoenix skyline. Candidate priority
(D-04 / RESEARCH Open Question 1): (1) a Chicano/community MURAL — the front-runner subject, but RESEARCH
found NO confirmed Commons-licensed photo of a specific South Tucson mural this session; the one-at-a-time
sourcing pass should FIRST attempt a dedicated Flickr-CC / Borderlore search for one of three real, named,
address-confirmed South Tucson murals (La Tusa/Tattoo Mural at 29th & 4th Ave; Amor Querido at 1802 S. 4th
Ave; El Pueblo Viejo Salsa at 2514 S. 4th Ave) — CONFIRM each is inside South Tucson city limits, NOT
Historic 4th Ave in Tucson proper (Pitfall 6); (2) a South-4th-Avenue restaurant-district streetscape /
signage (e.g. the "Star motel south tucson" vintage sign, CC BY 2.0, Dan DeLuca/Flickr 2017, 3,840×2,866);
(3) the two verified safe Commons fallbacks — "South Tucson city limits from 6th ave." or "South Tucson City
Hall" (both CC BY-SA 4.0, Rgper22008 2009, 2,816×2,112). Present options for review; show the Pima Catalinas
+ Oro Valley bridge + Marana Dove-Mountain + Tucson streetscape + Sahuarita Lake + AZ-state Phoenix banners
alongside during review per `feedback_banner_show_state_shot_on_conflict`. Source ONE candidate at a time
(`feedback_banner_source_one_at_a_time`); prefer a real photo (`feedback_banner_prefer_photo_over_gradient`).

Purpose: Satisfies BANR-01 + SUB-04 / ROADMAP success criteria #4 (licensed banner
sourced/processed/uploaded/wired) and #5 (South Tucson surfaced with a DB-honest chip). This CLOSES the
Tucson-metro local-deep-seed track (Phases 193-198) before AZ elections (Phase 199).
Output: uploaded `cities/south-tucson.jpg` + edited `buildingImages.js` + `coverage.js` in the `essentials`
repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md
@.planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md
@.planning/phases/198-south-tucson-deep-seed/198-CONTEXT.md

<execution_note>
Frontend files (`src/lib/buildingImages.js`, `src/lib/coverage.js`) live in the CURRENT `essentials` repo
(commit here, not `C:/EV-Accounts`). The banner SOURCING + processing + Storage upload are ORCHESTRATOR-RUN
(executor has no network/Storage access): the inline orchestrator sources the photo one-at-a-time, runs the
banner toolchain, and uploads. Per project convention (`feedback_banner_source_one_at_a_time`,
`feedback_banner_prefer_photo_over_gradient`, `feedback_banner_show_state_shot_on_conflict`,
`feedback_deepseed_community_banner`) banner sourcing is sequential, prefers a real photo, and shows the
sibling AZ banners alongside during review because there is a genuine visual-conflict risk. The CDN URL is
deterministic (`cities/south-tucson.jpg`) so the frontend wiring can be authored regardless of upload order.
Depends on Plan 03 — hasContext:true is only honest because Plan 03 seeded ≥1 stance row.
</execution_note>

<interfaces>
- Banner spec: process via `scripts/banners/process_banner.py` (`--url <src> --output <path>
  --vertical-anchor` tuned per shot) → 1700×540 @ 3.15:1, upload via `scripts/banners/upload_banner.py
  --file <path> --dest cities/south-tucson.jpg` (needs SUPABASE_SERVICE_ROLE_KEY from
  `C:/EV-Accounts/backend/.env`). Destination tier is `cities/` (banner TYPE, not government type). Storage
  is no-cache → overwrite = instant refresh.
- CDN URL: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/south-tucson.jpg
- buildingImages.js CURATED_LOCAL: add `'south tucson': { state: 'AZ', src: '<CDN URL>' }` (single-variant —
  `'south tucson'` has no same-named-city collision, so NOT the array-of-{state,src} form; the key MUST be
  QUOTED because it contains a space — mirror the `'oro valley':` style, NOT the unquoted `marana:` /
  `sahuarita:` style) with an attribution comment (subject | author | license) above it that explicitly
  notes it is deliberately distinct from the Pima County Catalinas, the Oro Valley CDO Trail Bridge, the
  Marana Tortolita/Dove-Mountain, the Tucson downtown/Hotel-Congress streetscape, the Sahuarita Lake/Santa-
  Ritas, and the AZ-state Phoenix skyline — and that South Tucson carries a cultural/urban (people/street/art)
  identity, the milestone's one non-landscape banner. getBuildingImages() needs NO code change. Storage file
  is `cities/south-tucson.jpg`; coverage.js label is 'South Tucson'.
- coverage.js: APPEND one area object to the EXISTING Arizona COVERAGE_STATES block's `areas` array (which
  already holds 'Tucson' + 'Oro Valley' + 'Marana' + 'Sahuarita', in that order — append after 'Sahuarita'):
  `{ label: 'South Tucson', browseGovernmentList: ['0468850'], browseStateAbbrev: 'AZ', hasContext: true }`.
  hasContext:true is honest because Plan 03 seeded ≥1 stance row. Do NOT create a second Arizona block
  (Pitfall 7); do NOT touch COVERAGE_COUNTIES (Pima County lives there — unrelated). Placement within the
  areas array is cosmetic. browseGovernmentList value is the G4110 geo_id ('0468850').
- Banner candidates (RESEARCH Open Question 1 / A5 — visual selection done at execute time): front-runner =
  a Chicano/community mural (attempt a fresh Flickr-CC/Borderlore re-source of La Tusa / Amor Querido / El
  Pueblo Viejo Salsa FIRST — confirm South-Tucson-limits per Pitfall 6); streetscape alt = "Star motel south
  tucson" (CC BY 2.0, 3,840×2,866); confirmed-usable safe fallbacks = "South Tucson city limits from 6th
  ave." or "South Tucson City Hall" (both CC BY-SA 4.0, 2,816×2,112).
- End-to-end routing: South Tucson has no distinct USPS post office (mailing city reads "Tucson, AZ 85713")
  — the geofence/geo_id is authoritative. Use a real in-South-Tucson street address (e.g. South Tucson City
  Hall, 1601 S 6th Ave, Tucson AZ 85713 — inside the 0468850 geofence despite the Tucson postal city) for
  the routing probe.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Source + process + upload the South Tucson banner, one at a time (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Standard Stack banner mechanism; § Open Question 1 — the mural-photo sourcing gap + the 3 named murals + the verified Commons/streetscape fallbacks; § Assumptions Log A5 — licensing/resolution; § Common Pitfall 6 — South-4th-Ave vs Historic-4th-Ave)
    - .planning/phases/198-south-tucson-deep-seed/198-CONTEXT.md (D-04 — no AI, no aerial; cultural/urban NOT Catalina/mountain, NOT Tortolita/Dove-Mountain, NOT downtown Tucson; mural / South-4th-Ave streetscape / welcome sign / Mercado subjects; show state shot if conflict)
    - .planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md (§ buildingImages.js mapping — the CURATED_LOCAL `'south tucson'` QUOTED-key entry + distinctiveness attribution comment convention; the process_banner.py / upload_banner.py commands + cities/south-tucson.jpg destination)
    - C:/Transparent Motivations/essentials/scripts/banners/process_banner.py (the banner spec 1700×540 @ 3.15:1 + --vertical-anchor)
    - C:/Transparent Motivations/essentials/scripts/banners/upload_banner.py (the Storage upload + --dest convention)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. The orchestrator sources a licensed South Tucson photo ONE AT A TIME (attempt a fresh Flickr-CC/Borderlore mural re-source FIRST — La Tusa / Amor Querido / El Pueblo Viejo Salsa, confirmed inside South Tucson limits per Pitfall 6; then a South-4th-Ave streetscape/Star-motel-sign; then the verified City-Hall / welcome-sign Commons fallbacks), processes it to the banner spec (1700×540 @ 3.15:1), uploads to cities/south-tucson.jpg, and the operator visually approves — with explicit attention to a cultural/urban subject and NO Catalina/mountain, NO Tortolita/Dove-Mountain, NO downtown-Tucson/Hotel-Congress imagery (D-04), and distinctiveness from the Pima / Oro Valley / Marana / Tucson / Sahuarita / AZ-state banners — before wiring. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A licensed community banner for the City of South Tucson — its own distinct cultural/urban (people/
    street/art) identity, the milestone's one non-landscape banner. RESEARCH's D-04 front-runner (a
    Chicano/community mural) has NO confirmed Commons photo this session, so a fresh higher-resolution
    re-source of one of three real, named, address-confirmed South Tucson murals (La Tusa, Amor Querido, El
    Pueblo Viejo Salsa) should be attempted first, with a South-4th-Ave streetscape (Star motel sign, CC BY
    2.0) or the two verified City-Hall / welcome-sign Commons photos (CC BY-SA 4.0) as the confirmed-usable
    fallbacks. Human visual QA is required because "real photo, no AI, no aerial, cultural/urban not
    landscape, NOT Catalina/Tortolita/Dove-Mountain, NOT downtown Tucson, distinct from the sibling AZ
    banners" is a judgment a row-count cannot make.
  </what-built>
  <how-to-verify>
    1. The orchestrator FIRST attempts a fresh Flickr-CC / Borderlore re-source of a specific South Tucson
       mural (La Tusa/Tattoo Mural 29th & 4th Ave; Amor Querido 1802 S. 4th Ave; El Pueblo Viejo Salsa 2514
       S. 4th Ave) — the D-04 front-runner — CONFIRMING each address is inside South Tucson city limits (NOT
       Historic 4th Ave in Tucson proper, Pitfall 6). If none is clearly licensed at adequate resolution for
       a 1700-wide crop, fall back to a South-4th-Ave streetscape (the "Star motel south tucson" CC BY 2.0
       sign, 3,840×2,866) or the confirmed City-Hall / welcome-sign Commons photos (CC BY-SA 4.0, 2,816×
       2,112). Source ONE candidate at a time (licensed CC/public-domain, real photo, NOT AI-generated, NOT
       aerial/drone, cultural/urban not landscape), record subject | author | license, run `python
       scripts/banners/process_banner.py --url <src> --output /tmp/south-tucson.jpg --vertical-anchor 0.5`
       (tune anchor per shot), then `python scripts/banners/upload_banner.py --file /tmp/south-tucson.jpg
       --dest cities/south-tucson.jpg` (SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
    2. Operator visually confirms: reads as South Tucson / cultural-urban (mural, street, restaurant-district
       sign, or welcome sign — NOT landscape), a real photo (no AI artifacts, no aerial/drone shot), contains
       NO Catalina/mountain imagery, NO Tortolita/Dove-Mountain imagery, and NO downtown-Tucson/Hotel-Congress
       imagery (D-04), and is visually DISTINCT from the Pima County Catalina banner, the Oro Valley CDO Trail
       Bridge, the Marana Dove-Mountain shot, the Tucson downtown streetscape, the Sahuarita Lake/Santa-Ritas,
       AND the AZ-state Phoenix skyline — show all six alongside during review per
       feedback_banner_show_state_shot_on_conflict.
    3. If unusable, too similar to a sibling banner, shows a forbidden subject, or a mural cannot be confirmed
       inside South Tucson limits, the orchestrator re-sources another candidate ONE AT A TIME (prefer a real
       photo; gradient only if truly nothing usable exists, per feedback_banner_prefer_photo_over_gradient).
  </how-to-verify>
  <acceptance_criteria>
    - `curl -sI https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/south-tucson.jpg` returns HTTP 200.
    - The uploaded image matches the banner spec (process_banner.py output = 1700×540 @ 3.15:1) — PIL/curl-download dimension check.
    - Operator approves: real photo, South Tucson cultural/urban (not landscape) subject, no AI, no aerial, NO Catalina/mountain AND NO Tortolita/Dove-Mountain AND NO downtown-Tucson/Hotel-Congress (D-04), distinct from the Pima Catalina banner AND the Oro Valley CDO bridge AND the Marana Dove-Mountain shot AND the Tucson streetscape AND the Sahuarita Lake AND the AZ-state Phoenix skyline; any mural is confirmed inside South Tucson city limits (Pitfall 6).
    - Subject | author | license recorded in 198-04-SUMMARY.md for the attribution comment.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the operator confirms the uploaded banner reads as South Tucson cultural/urban, is a real non-aerial photo with NO Catalina/mountain, NO Tortolita/Dove-Mountain, and NO downtown-Tucson imagery, any mural is confirmed inside South Tucson limits, and it is distinct from the Pima + Oro Valley + Marana + Tucson + Sahuarita + AZ-state banners — or describe what to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner into buildingImages.js + append South Tucson to the EXISTING Arizona coverage.js block</name>
  <files>src/lib/buildingImages.js, src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md (§ buildingImages.js mapping + § coverage.js mapping — the exact CURATED_LOCAL `'south tucson'` QUOTED-key entry + distinctiveness attribution comment, and the APPEND of the 'South Tucson' area to the EXISTING Arizona COVERAGE_STATES areas array after 'Sahuarita'; the Pitfall 7 warning that the Arizona block already exists with 4 entries and Pima is in COVERAGE_COUNTIES; getBuildingImages needs NO change)
    - C:/Transparent Motivations/essentials/src/lib/buildingImages.js (the CURATED_LOCAL object + the existing quoted `'oro valley':` entry to mirror the quoted-key style, and the unquoted `marana:` / `sahuarita:` entries for contrast; getBuildingImages substring/state-scoping logic)
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the EXISTING Arizona COVERAGE_STATES block with its 'Tucson' + 'Oro Valley' + 'Marana' + 'Sahuarita' areas — append after 'Sahuarita'; confirm NO second Arizona block is created; the separate COVERAGE_COUNTIES array holding Pima County — do NOT touch it)
    - .planning/phases/198-south-tucson-deep-seed/198-04-SUMMARY.md (the banner subject | author | license from Task 1 for the attribution comment)
  </read_first>
  <action>
    In `src/lib/buildingImages.js`, add ONE entry to the CURATED_LOCAL object:
    `'south tucson': { state: 'AZ', src: '<CDN URL for cities/south-tucson.jpg>' }` (single-variant, NOT the
    array-of-{state,src} form — `'south tucson'` has no same-named-city collision; the key MUST be QUOTED
    because it contains a space — mirror the `'oro valley':` style, NOT the unquoted `marana:`/`sahuarita:`
    style), with an attribution comment above it (subject | author | license from Task 1). Note in the
    comment it is deliberately distinct from the Pima County Catalina landscape banner, the Oro Valley CDO
    Trail Bridge, the Marana Tortolita/Dove-Mountain shot, the Tucson downtown/Hotel-Congress streetscape,
    the Sahuarita Lake/Santa-Ritas, and the AZ-state Phoenix skyline, and that South Tucson carries a
    cultural/urban (people/street/art) identity — the milestone's one non-landscape banner (D-04). Make NO
    change to getBuildingImages(). Storage file is `cities/south-tucson.jpg`.

    In `src/lib/coverage.js`, APPEND one area object to the EXISTING Arizona COVERAGE_STATES block's `areas`
    array (the block that already contains 'Tucson' + 'Oro Valley' + 'Marana' + 'Sahuarita' — append after
    'Sahuarita'):
    `{ label: 'South Tucson', browseGovernmentList: ['0468850'], browseStateAbbrev: 'AZ', hasContext: true }`.
    Do NOT create a second `{ name: 'Arizona', ... }` block (Pitfall 7), do NOT add South Tucson to
    COVERAGE_COUNTIES (where Pima County lives), and do NOT broaden parseCityFromAddress. hasContext:true is
    honest because Plan 03 seeded ≥1 stance row (this plan depends on Plan 03). Placement within the areas
    array is cosmetic. browseGovernmentList value is the G4110 geo_id ('0468850').

    Commit both files to the `essentials` repo via git.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qE "^\s*'south tucson':\s*\{" src/lib/buildingImages.js && grep -qF "cities/south-tucson.jpg" src/lib/buildingImages.js && grep -qF "'South Tucson'" src/lib/coverage.js && grep -qF "'0468850'" src/lib/coverage.js && [ "$(grep -c "name: 'Arizona'" src/lib/coverage.js)" = "1" ] && node --input-type=module -e "import('./src/lib/coverage.js').then(()=>console.log('coverage-ok')).catch(e=>{console.error(e);process.exit(1)})" && node --input-type=module -e "import('./src/lib/buildingImages.js').then(()=>console.log('bi-ok')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has a QUOTED `'south tucson':` key with `state: 'AZ'` and src = the cities/south-tucson.jpg CDN URL, plus an attribution comment (subject | author | license) noting distinctiveness from the 6 sibling AZ banners and the cultural/urban identity.
    - coverage.js Arizona COVERAGE_STATES block's areas array has a NEW `{ label: 'South Tucson', browseGovernmentList: ['0468850'], browseStateAbbrev: 'AZ', hasContext: true }` entry appended after 'Sahuarita'.
    - There is EXACTLY ONE `name: 'Arizona'` block in coverage.js (no duplicate/second block created — Pitfall 7).
    - COVERAGE_COUNTIES is NOT modified (Pima County entry untouched); no South Tucson entry added there.
    - Both files parse cleanly via `node --input-type=module -e "import(...)"` (exit 0).
    - getBuildingImages() and parseCityFromAddress unchanged.
    - `npx vitest run src/lib/buildingImages.test.js` stays green (established non-gap; run as a spot-check).
    - Both files committed to the `essentials` repo.
  </acceptance_criteria>
  <done>South Tucson banner wired into CURATED_LOCAL (quoted `'south tucson'` key) and surfaced via a new area appended to the EXISTING Arizona COVERAGE_STATES block with a DB-honest hasContext:true chip; exactly one Arizona block; COVERAGE_COUNTIES untouched; both frontend files parse, buildingImages.test.js green, both committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Verify the resident-facing outcome end-to-end — real South Tucson address → council of 7, not swallowed by Tucson (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-01-SUMMARY.md (the 7 seeded officials + their titles to expect back — Mayor Valenzuela, Vice Mayor Brown-Dominguez, Acting Mayor Robles, 4 Council Members; the enclave re-check result)
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Architecture Patterns — "Address routing (NO CODE CHANGE)"; the existing G4110→LOCAL mapping is expected to cover all 7 at-large seats with zero code change, no LOCAL_EXEC join; the enclave donut-hole precludes a Tucson double-match)
  </read_first>
  <action>ORCHESTRATOR-RUN closing gate — no executor file action. The whole phase goal is "South Tucson residents can SEE their council with a compass." Every other plan verifies DB structure (row counts, joins, enclave geometry) but not the live resident-facing result. The orchestrator POSTs a real South Tucson street address to the live candidates-search API (POST /candidates/search {query}, full street address required per the search contract) and confirms the response includes all 7 seeded at-large council members (3 of whom carry the Mayor / Vice Mayor / Acting Mayor title), all on geo_id 0468850, AND that the response is South Tucson's council — NOT Tucson's (the enclave donut-hole must route the address to South Tucson exclusively). This exercises the geofence→district→office join that Plan 01 seeded, end-to-end, against the shared-LOCAL-row shape already live in prod for Sahuarita/Marana/Torrance. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A live end-to-end confirmation that the phase goal is actually TRUE for a resident — not just that the
    rows exist. South Tucson's at-large shape means every South Tucson address should return the SAME 7
    council members (3 title-annotated Mayor/Vice-Mayor/Acting-Mayor), which IS the correct at-large behavior
    (no per-ward differentiation, no separate LOCAL_EXEC Mayor to check) — and, critically for this first
    enclave phase, the address must resolve to South Tucson's council and NOT be swallowed by Tucson's larger
    enclosing geofence.
  </what-built>
  <how-to-verify>
    1. The orchestrator POSTs a real South Tucson street address (e.g. South Tucson City Hall, 1601 S 6th
       Ave, Tucson AZ 85713 — inside the 0468850 geofence despite the "Tucson" postal city, since South
       Tucson has no distinct USPS post office; or another verified in-limits street address) to the live
       candidates-search API (accounts-api.empowered.vote, POST /candidates/search {query} — full street
       address required).
    2. Confirm the response includes the City of South Tucson government with all 7 at-large Council Members
       (3 of whom carry the Mayor / Vice Mayor / Acting Mayor title), matching the roster seeded in Plan 01
       (party never displayed — antipartisan), and that it is South Tucson's council — NOT Tucson's.
    3. If any council member is missing, an extra/wrong office appears, a phantom LOCAL_EXEC Mayor surfaces,
       OR the address resolves to Tucson's council instead of (or in addition to) South Tucson's, STOP — this
       indicates a district-attach, geofence-mapping, or enclave-routing defect from Plan 01 that must be
       fixed before the phase closes.
  </how-to-verify>
  <acceptance_criteria>
    - A real South Tucson street address POSTed to the live candidates-search API returns the City of South Tucson with all 7 at-large Council Members (7 offices total; 3 title-annotated Mayor / Vice Mayor / Acting Mayor), matching Plan 01's seeded roster.
    - The address resolves to South Tucson's council EXCLUSIVELY — it is NOT swallowed by or double-matched with Tucson's council (enclave donut-hole correct end-to-end).
    - No wrong-jurisdiction, duplicate, or phantom LOCAL_EXEC South Tucson office appears in the response.
    - Result recorded in 198-04-SUMMARY.md (the address used + the offices returned + confirmation it did not double-match Tucson).
  </acceptance_criteria>
  <resume-signal>Type "approved" once a real South Tucson address returns all 7 at-large Council Members (3 title-annotated Mayor / Vice Mayor / Acting Mayor) from the live search API, resolving to South Tucson exclusively and not Tucson — or describe the routing discrepancy found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wikimedia/Flickr (remote) → banner pipeline | Untrusted remote image bytes enter process_banner.py over HTTP |
| banner pipeline → Storage | Processed banner crosses into the production politician_photos bucket |
| frontend edits → live app | coverage.js / buildingImages.js changes ship to Render on push |

## STRIDE Threat Register

Honest scope: a licensed static image + two static frontend appends (data-seeding/asset-wiring phase, no new
runtime/request surface). Genuine threats are payload-in-image, license provenance, a dishonest/mis-placed
coverage chip, and (this first enclave phase) an enclave routing defect — not injection or access control.
No HIGH-severity threat; none fabricated. Mirrors the Sahuarita 197 threat model.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-BAN | Tampering | remote banner image fetch | mitigate | process_banner.py re-encodes to JPEG (strips embedded payloads); operator visual QA confirms a real non-AI non-aerial photo before wiring |
| T-198-LIC | Repudiation | banner license provenance | mitigate | Subject \| author \| license recorded in the SUMMARY + the attribution comment; only CC/public-domain sources used; mural confirmed inside South Tucson limits (Pitfall 6) |
| T-198-COVLIE | Tampering | dishonest coverage chip | mitigate | hasContext:true only set because Plan 03 landed ≥1 stance row (depends_on 198-03); node parse-check confirms valid module |
| T-198-COVBLK | Tampering | wrong coverage array (duplicate AZ block / append to COUNTIES) | mitigate | APPEND to the EXISTING Arizona COVERAGE_STATES block (Pitfall 7); grep asserts exactly ONE `name: 'Arizona'` block; COVERAGE_COUNTIES Pima entry untouched |
| T-198-ENCLAVE | Tampering (data integrity) | enclave routing defect — a South Tucson address swallowed by Tucson's geofence | mitigate | Task 3 end-to-end address probe confirms the address resolves to South Tucson's council EXCLUSIVELY, not Tucson's; backed by Plan 01's ST_Area(ST_Intersection)=0 invariant |
| T-198-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; never in a committed frontend file |
| T-198-SC | Tampering | package installs | accept | No new installs — reuses installed PIL/requests banner toolchain; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- cities/south-tucson.jpg returns HTTP 200 at the banner spec (1700×540 @ 3.15:1); operator-approved as a real, distinct, cultural/urban (non-landscape) South Tucson photo.
- CURATED_LOCAL `'south tucson'` (quoted key) → banner URL; 'South Tucson' area APPENDED to the EXISTING Arizona COVERAGE_STATES block (exactly one Arizona block); COVERAGE_COUNTIES untouched.
- Both frontend files parse, buildingImages.test.js green, both committed.
- A real South Tucson address returns all 7 at-large Council Members from the live search API, resolving to South Tucson exclusively (not swallowed by Tucson).
</verification>

<success_criteria>
BANR-01 + SUB-04 / ROADMAP #4 (licensed banner sourced/processed/uploaded/wired) and #5 (South Tucson
surfaced with a DB-honest chip appended to the existing Arizona block) are TRUE; a real South Tucson address
resolves to the seeded 7 at-large council members end-to-end, exclusively (not Tucson). This CLOSES the
Tucson-metro local-deep-seed track (193-198).
</success_criteria>

<output>
Create `.planning/phases/198-south-tucson-deep-seed/198-04-SUMMARY.md` when done — include the banner subject
| author | license, the CDN URL, and the end-to-end address-routing result (address used + offices returned
+ confirmation it did not double-match Tucson).
</output>
