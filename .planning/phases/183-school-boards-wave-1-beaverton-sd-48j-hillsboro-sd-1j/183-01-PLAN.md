---
phase: 183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-probe.sql
autonomous: false
requirements: [WSCH-01, WSCH-02]
user_setup: []

must_haves:
  truths:
    - "Orchestrator has re-confirmed both G5420 geofences exist with valid geometry and correct names: geo_id 4101920 = 'Beaverton School District 48J' AND geo_id 4100023 = 'Hillsboro School District 1J' (source='tiger_unsd_or_2024_westmetro', mtfcc='G5420', ST_IsValid=true) — loaded by Phase 174, NOT re-loaded this phase"
    - "Orchestrator has confirmed greenfield: 0 essentials.governments rows named 'Beaverton School District 48J, Oregon, US' or 'Hillsboro School District 1J, Oregon, US' AND 0 SCHOOL districts on either geo_id"
    - "Orchestrator has confirmed the ext_id blocks -4101921..-4101927 (Beaverton) and -4100024..-4100030 (Hillsboro) are collision-free (probed with margin -4101930..-4101920 and -4100030..-4100020)"
    - "Orchestrator has confirmed the on-disk migration MAX so the next STRUCTURAL migration number is safe (research snapshot: on-disk MAX 1202 → next structural 1203, headshots 1204; the DB ledger MAX is a known TRAP — on-disk counter authoritative)"
    - "Orchestrator has confirmed districts.state casing for an EXISTING OR SCHOOL district (Portland Public Schools, geo_id 4110040) returns lowercase 'or' — the casing the structural migration WHERE/JOIN clauses must use"
    - "The Beaverton SD 48J 7-director roster is re-confirmed LIVE from beaverton.k12.or.us/school-board/board-members (no WAF): Zone 1 Van Truong / Zone 2 Karen Pérez / Zone 3 Melissa Potter (Vice Chair) / Zone 4 Sunita Garg / Zone 5 Syed Qasim / Zone 6 Justice Rajee (Chair) / Zone 7 Tammy Carpenter — no vacancies; the district's own language 'voters in the District elect them at-large' re-confirmed (WHOLE-DISTRICT at-large — D-Z1 zone-voted branch does NOT fire, D-Z2 fallback moot, NO sub-zone geofences)"
    - "The Hillsboro SD 1J 7-director roster is re-confirmed LIVE from hsd.k12.or.us/about-us/board-of-directors/board-members (no WAF): Position 1 Yessica Hardin Mercado / Position 2 Mark Watson / Position 3 Nancy Thomas / Position 4 See Eun Kim (Vice Chair) / Position 5 Ivette Pantoja (Chair) / Position 6 Katie Rhyne / Position 7 Patrick Maguire — no vacancies; the 3 student representatives (Ethan Hernandez Jimenez, Keeton Sayre, Ma'Kaia Woods) and Board Secretary Rose Roman are confirmed NON-DIRECTORS and EXCLUDED (D-R4)"
    - "All 14 headshot source URLs (resources.finalsite.net, listed verbatim in 183-RESEARCH.md §'Code Examples') are re-confirmed HTTP 200 and directly downloadable at execution time; any Hillsboro low-resolution source (Kim/Pantoja/Watson/Thomas per A3) is noted for plan 03 (document a genuine gap rather than over-upscale, D-R5)"
    - "The verbatim chamber names are locked before plan 02: Beaverton = 'School Board', Hillsboro = 'Board of Directors' (per-district verbatim, NOT the 254_or blanket 'Board of Education' — D-R1/D-R2); the verbatim office-title convention is locked: Beaverton 'Director, Zone N' (HIGH confidence), Hillsboro 'Director, Position N' (MEDIUM — optionally reconfirm via a Washington County candidate-filing PDF, non-blocking per Open Question 1)"
    - "Decision coverage — D-Z1: WHO-VOTES routing re-verified (whole-district at-large for BOTH → single G5420 district each); D-Z2: confirmed moot (never fires); D-R1/R2: verbatim body + office-title names locked; D-R4: student reps + secretary excluded; D-R5: headshot sources + any low-res gap noted; D-B1: no banner work confirmed"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-probe.sql"
      provides: "The read-only Wave-0 probe queries the orchestrator runs before any structural write"
      min_lines: 25
  key_links:
    - from: "probe.sql"
      to: "essentials.geofence_boundaries / essentials.governments / essentials.districts / essentials.politicians / supabase_migrations.schema_migrations"
      via: "read-only SELECT probes"
      pattern: "essentials\\.(geofence_boundaries|governments|districts|politicians)"
---

<objective>
Author the Wave-0 verification probe file for the two-district school-board deep-seed and drive the
orchestrator-run DB + roster + headshot checks that MUST pass before any structural write. This plan
gates the entire phase: no government, chamber, district, office, or headshot migration may be authored
or applied until every probe below returns its expected result.

Purpose: Lock the load-bearing facts before writing SQL. RESEARCH (2026-07-04) already live-verified
everything at HIGH confidence, but roster/site/CDN state can drift between research and execution, so
Wave-0 re-confirms: (1) both Phase-174 G5420 geofences still exist, valid, correctly named (do NOT
re-load); (2) greenfield status + collision-free ext_id blocks; (3) the next structural migration number
via on-disk `ls` (ledger MAX is a trap — on-disk 1202 → next 1203); (4) lowercase 'or' districts casing;
(5) both 7/7 rosters are unchanged and BOTH boards are whole-district at-large (the single most important
finding — it OVERTURNS the CONTEXT.md's "Beaverton is zone-voted" assumption, so NO sub-zone geofence
work of any kind); (6) all 14 finalsite headshot URLs still return HTTP 200; (7) the verbatim chamber
and office-title strings are locked. The 3 Hillsboro student representatives and the board secretary are
re-confirmed as excluded non-directors (D-R4).
Output: `_tmp-westmetro-school-wave1-probe.sql` (a gitignored `_tmp-*` orchestrator-run helper) + a
recorded pass/fail for each Wave-0 gate, saved in the SUMMARY for downstream plans 02-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-RESEARCH.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md
@.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-CONTEXT.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO database
access, NO WebSearch, and NO Supabase MCP. The INLINE ORCHESTRATOR (main context) runs the probes via
`psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`, re-fetches both
rosters from beaverton.k12.or.us / hsd.k12.or.us (NO WAF — plain curl returns 200), and re-downloads the
14 finalsite headshot URLs. This probe file is a gitignored `_tmp-*` helper — do NOT commit it.
C:/EV-Accounts is a SEPARATE git repo — never cd into it; use `git -C "C:/EV-Accounts"` if a commit is
ever needed.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except where a probe genuinely reads that table), and `photo_origin_url` out of
comments in any file authored this phase. Save the file as UTF-8 (the roster carries "Vân"/"Pérez").
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-probe.sql</files>
  <read_first>
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-RESEARCH.md §"Pattern 1: No geofence loader", §"Migration ledger check", §"External_id collision probe", §"Wave 0 Requirements", §"Pattern 3: Verified Rosters"
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-PATTERNS.md §"Casing convention"
    - C:/EV-Accounts/backend/scripts/_tmp-cornelius-wave0-probe.sql (the freshest prior-phase probe-file analog — copy the labeled `\echo`-per-probe shape verbatim, adapting values; gitignored but present on disk)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-01-PLAN.md (the prior-phase probe-plan analog for probe structure)
  </read_first>
  <action>
    Write labeled read-only SELECT probes, each preceded by a `\echo` label line so the orchestrator
    reads results unambiguously. Do NOT wrap in a transaction (read-only); no DDL — SELECT/\echo only.
    Probe A (GEOFENCE EXISTENCE-AND-NAME, both districts): select geo_id, mtfcc, name, source,
    ST_IsValid(geometry) from essentials.geofence_boundaries WHERE geo_id IN ('4101920','4100023') AND
    mtfcc='G5420' (EXPECT exactly 2 rows: 4101920='Beaverton School District 48J', 4100023='Hillsboro
    School District 1J', both source='tiger_unsd_or_2024_westmetro', both ST_IsValid=true). Probe B
    (GREENFIELD governments): count essentials.governments WHERE name IN ('Beaverton School District 48J,
    Oregon, US','Hillsboro School District 1J, Oregon, US') OR name ILIKE '%beaverton school%' OR name
    ILIKE '%hillsboro school%' (EXPECT 0). Probe C (GREENFIELD districts): count essentials.districts
    WHERE geo_id IN ('4101920','4100023') AND district_type='SCHOOL' (EXPECT 0). Probe D (EXT_ID
    collision, both blocks with margin): select external_id from essentials.politicians WHERE external_id
    BETWEEN -4101930 AND -4101920 OR external_id BETWEEN -4100030 AND -4100020 ORDER BY external_id
    (EXPECT 0 rows). Probe E (MIGRATION LEDGER): select MAX(version::bigint) from
    supabase_migrations.schema_migrations WHERE version ~ '^[0-9]{1,6}$' (record it; note in a header
    comment that the on-disk file MAX is authoritative — research snapshot on-disk 1202 → next structural
    1203 — and the orchestrator cross-checks with `ls`). Probe F (CASING): select distinct state from
    essentials.districts WHERE geo_id='4110040' AND district_type='SCHOOL' (Portland Public Schools — the
    existing OR school-district precedent; EXPECT 'or' lowercase). Probe G (0-STANCE BASELINE): count
    inform.politician_answers pa JOIN essentials.politicians p ON pa.politician_id=p.id WHERE
    p.external_id BETWEEN -4101927 AND -4101921 OR p.external_id BETWEEN -4100030 AND -4100024 (EXPECT 0 —
    trivially, since no politicians exist yet; documents the 0-by-design baseline for the phase gate).
    Include a header comment noting the file is a gitignored orchestrator-run read-only helper, that both
    G5420 geofences are pre-loaded by Phase 174 and must NOT be re-loaded, and that BOTH boards are
    whole-district at-large (no sub-zone geofences). No fenced code in this action — the
    _tmp-cornelius-wave0-probe.sql analog is the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator (Task 2) runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-probe.sql` and records each result</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-probe.sql with labeled probes A-G, each prefixed by a `\echo` line
    - Probe A selects geo_id, mtfcc, name, source, ST_IsValid for geo_id IN ('4101920','4100023') AND mtfcc='G5420' (expects 2 rows with the two verbatim district names)
    - Probe B queries governments by both verbatim names AND ILIKE '%beaverton school%'/'%hillsboro school%'
    - Probe C queries districts WHERE geo_id IN ('4101920','4100023') AND district_type='SCHOOL'
    - Probe D queries external_id BETWEEN -4101930 AND -4101920 OR BETWEEN -4100030 AND -4100020
    - Probe E reads MAX(version::bigint) from the migration ledger with a numeric-version filter
    - Probe F reads distinct districts.state for geo_id='4110040' AND district_type='SCHOOL' (expect 'or' lowercase)
    - Probe G counts inform.politician_answers for both ext_id ranges (expect 0 baseline)
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE); not wrapped in a transaction; saved UTF-8
  </acceptance_criteria>
  <done>Probe file authored with all 7 labeled probes and correct literal values (geo_ids 4101920/4100023, ext_id ranges -4101930..-4101920 and -4100030..-4100020, PPS casing geo_id 4110040).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes, re-fetches both rosters, re-downloads 14 headshots, confirms all gates</name>
  <what-built>The Wave-0 probe file. The orchestrator now executes it against the live DB, re-fetches both live rosters directly from the district sites (no WAF), and re-downloads all 14 finalsite headshot URLs.</what-built>
  <how-to-verify>
    A. Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-probe.sql`
       (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
       1. Probe A returns 2 rows: 4101920='Beaverton School District 48J', 4100023='Hillsboro School
          District 1J', both source='tiger_unsd_or_2024_westmetro', both ST_IsValid=true. If either row
          is missing, mis-named, or invalid geometry → STOP (Phase 174 dependency broken; do NOT re-load
          — investigate).
       2. Probe B returns 0. If >0 → STOP, a district is already seeded — switch to reconcile.
       3. Probe C returns 0. If >0 → STOP, SCHOOL districts already exist.
       4. Probe D returns 0 rows. If any row → shift the affected ext_id block and record the new range
          for plans 02/03.
       5. Probe E: cross-check against `ls C:/EV-Accounts/backend/migrations | grep -oE '^[0-9]+' | sort -n | tail`.
          Next STRUCTURAL migration = on-disk file MAX + 1 (research snapshot: on-disk 1202 → 1203;
          headshots = 1204). The DB ledger MAX is a TRAP — do NOT use it. Record 1203/1204 for plans 02/03.
       6. Probe F returns 'or' (lowercase) — the casing plans 02/03 must use for SCHOOL districts.
       7. Probe G returns 0 — the 0-stance-by-design baseline for the phase gate.
    B. Re-fetch both rosters directly (NO WAF — plain curl returns 200):
       - beaverton.k12.or.us/school-board/board-members — confirm the 7 directors unchanged: Zone 1 Van
         Truong (site spelling "Vân"), Zone 2 Karen Pérez, Zone 3 Melissa Potter (Vice Chair), Zone 4
         Sunita Garg, Zone 5 Syed Qasim, Zone 6 Justice Rajee (Chair), Zone 7 Tammy Carpenter; no
         vacancies. Re-confirm the district's OWN election language on the page: "voters in the District
         elect them at-large" (WHOLE-DISTRICT at-large — the D-Z1 zone-voted branch does NOT fire; do NOT
         plan any sub-zone geofences; D-Z2 is moot). Honorifics ("Dr.") dropped per the credential-drop
         precedent.
       - hsd.k12.or.us/about-us/board-of-directors/board-members — confirm the 7 directors unchanged:
         Position 1 Yessica Hardin Mercado, Position 2 Mark Watson, Position 3 Nancy Thomas, Position 4
         See Eun Kim (Vice Chair), Position 5 Ivette Pantoja (Chair), Position 6 Katie Rhyne, Position 7
         Patrick Maguire; no vacancies. SPECIAL EXCLUSION CHECK (D-R4): the 3 student representatives
         (Ethan Hernandez Jimenez, Keeton Sayre, Ma'Kaia Woods) each labeled "Student Representative to
         the Board" and Board Secretary Rose Roman are NON-DIRECTORS and MUST be excluded — confirm the
         board carries exactly 7 elected directors, not 8/9/10.
       - If EITHER roster has changed (new director, vacancy, name/position change) → STOP and re-plan
         that district's roster in plan 02.
    C. Headshot retrievability: re-download all 14 finalsite URLs listed verbatim in 183-RESEARCH.md
       §"Code Examples" (7 Beaverton + 7 Hillsboro, resources.finalsite.net) and confirm each returns
       HTTP 200 with a non-empty image body. Note native pixel dimensions per image; flag any Hillsboro
       source under 600×750 (Kim/Pantoja/Watson/Thomas per A3) so plan 03 tries the largest
       `f_auto,q_auto` (no size-suffix) rendition first and documents a genuine gap rather than
       over-upscaling (D-R5). A full 14/14 is expected (cleanest sourcing in the milestone). Do NOT sweep
       up the 3 Hillsboro student-rep images.
    D. Lock the verbatim naming for plans 02/03: chamber names Beaverton='School Board' /
       Hillsboro='Board of Directors' (per-district verbatim, NOT 254_or's 'Board of Education' — D-R1/R2);
       office titles Beaverton 'Director, Zone N' (HIGH confidence, confirmed by election-filing document
       title) / Hillsboro 'Director, Position N' (MEDIUM — optionally reconfirm via a Washington County
       candidate-filing PDF for a Hillsboro seat, non-blocking per Open Question 1; proceed with 'Director,
       Position N' if none found quickly). Chair/Vice-Chair are title-on-seat suffixes, NOT separate rows
       (7 offices per district, not 8/9).
  </how-to-verify>
  <acceptance_criteria>
    - All 7 probe gates recorded with pass/fail; A=2 correctly-named valid geofences, B/C/D=0, E cross-checked (next structural 1203 / headshots 1204, ledger recognized as trap), F='or', G=0
    - Both 7-director rosters re-confirmed unchanged via direct district-site fetch; both boards confirmed WHOLE-DISTRICT at-large (no sub-zone geofences); D-Z1 zone-voted branch confirmed NOT firing for either
    - Hillsboro 3 student reps (Hernandez Jimenez/Sayre/Woods) + Board Secretary Rose Roman confirmed excluded (exactly 7 directors)
    - All 14 finalsite headshot URLs re-confirmed HTTP 200; any sub-600×750 Hillsboro source flagged for plan 03 (document-gap-not-fabricate)
    - Verbatim chamber names ('School Board' / 'Board of Directors') and office-title conventions ('Director, Zone N' / 'Director, Position N') locked; Chair (Rajee Z6 / Pantoja P5) + Vice-Chair (Potter Z3 / Kim P4) confirmed as title-on-seat suffixes
    - Confirmed ext_id blocks (default Beaverton -4101921..-4101927, Hillsboro -4100024..-4100030 unless probe D forced a shift) and next migration numbers (1203/1204) recorded for plans 02/03
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |
| operator → external web | Roster re-fetch + headshot re-download from beaverton.k12.or.us / hsd.k12.or.us / resources.finalsite.net (no WAF) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01 | Tampering | probe.sql executed on prod DB | accept | Probes are read-only SELECTs; no write path; no user input |
| T-183-02 | Spoofing | acting on the stale CONTEXT.md assumption that Beaverton is zone-voted would trigger unnecessary sub-zone geofence work that stalls the phase | mitigate | Probe B/C + a direct beaverton.k12.or.us re-fetch of the "voters in the District elect them at-large" language confirms whole-district at-large before any write; D-Z1 branch confirmed not firing |
| T-183-03 | Spoofing | seeding the 3 Hillsboro student representatives or the board secretary as directors (their images sit in the same page markup) | mitigate | Wave-0 re-confirms exactly 7 elected directors per board and names the excluded non-directors (Hernandez Jimenez / Sayre / Woods / Roman — D-R4) |
| T-183-04 | Spoofing | stale roster (a director changed since the 2026-07-04 research pull) | mitigate | Direct district-site re-fetch of both 7-director rosters; STOP-and-re-plan on any change |
| T-183-05 | Tampering (data integrity) | wrong migration number from trusting the DB ledger MAX (a known trap) | mitigate | Probe E cross-checked against on-disk `ls` — on-disk MAX authoritative (1202 → 1203/1204) |
| T-183-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- Both G5420 geofences confirmed present, valid, and correctly named before any structural write.
- Both boards confirmed whole-district at-large (no sub-zone geofences); D-Z2 confirmed moot.
- Any failed gate blocks plans 02-04 until resolved.
</verification>

<success_criteria>
- Both geofences confirmed present + name-matched + valid; greenfield (0 govs, 0 SCHOOL districts);
  ext_id blocks free; next migration numbers confirmed (1203 structural / 1204 headshots, ledger
  recognized as trap); lowercase 'or' casing confirmed; both 7/7 rosters re-confirmed via direct fetch;
  both boards whole-district at-large; student reps + secretary excluded; all 14 headshot URLs HTTP 200;
  verbatim chamber + office-title strings locked; 0-stance baseline recorded.
</success_criteria>

<output>
Create `.planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-01-SUMMARY.md` when
done, recording: both confirmed geo_ids + names, greenfield status, confirmed ext_id blocks, next
migration numbers (1203/1204), lowercase 'or' casing, both re-confirmed 7-director rosters (with
Chair/Vice-Chair and Zone/Position numbers), the whole-district-at-large confirmation, the excluded
Hillsboro non-directors, per-image headshot HTTP-200 confirmations + any low-res flags, and the locked
verbatim chamber + office-title strings — for downstream plans 02-04.
</output>
