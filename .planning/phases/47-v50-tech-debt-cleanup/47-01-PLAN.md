---
phase: 47-v50-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Elections.jsx
  - src/pages/Results.jsx
  - .planning/phases/39-ma-government-db/39-VERIFICATION.md
  - .planning/phases/42-cambridge-headshots/42-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "Navigating to /elections still works (redirects via App.jsx Navigate)"
    - "Anonymous users on the Results page see a Cambridge shortcut button below the address input"
    - "Clicking the Cambridge shortcut loads Cambridge officials via browse_government_list=2511000 (no address input required)"
    - "Phase 39 VERIFICATION.md exists and records 40 senators, 160 house offices, and the government row UUID"
    - "Phase 42 VERIFICATION.md reflects Yi-An Huang gap closed in Phase 46; Luisa de Paula Santos still documented as open"
  artifacts:
    - path: "src/pages/Elections.jsx"
      provides: "File deleted — must not exist"
    - path: "src/pages/Results.jsx"
      provides: "SHORTCUTS constant + shortcut button rendered in address-mode search UI"
      contains: "SHORTCUTS"
    - path: ".planning/phases/39-ma-government-db/39-VERIFICATION.md"
      provides: "Phase 39 verification report"
      contains: "85783e20-3031-4d71-89a5-5dd61f4a593f"
    - path: ".planning/phases/42-cambridge-headshots/42-VERIFICATION.md"
      provides: "Updated Phase 42 verification report"
      contains: "gap closed Phase 46"
  key_links:
    - from: "src/App.jsx"
      to: "/results?prefilled=true&view=elections"
      via: "Navigate redirect at line 60"
      pattern: "Navigate to.*results.*elections"
    - from: "Results.jsx SHORTCUTS Cambridge entry"
      to: "fetchElectionsByGovernmentList"
      via: "navigate('/results?browse_government_list=2511000&browse_label=Cambridge&browse_state=MA')"
      pattern: "browse_government_list.*2511000"
---

<objective>
Close four discrete tech debt items identified in the v5.0 milestone audit: delete dead Elections.jsx component, add a Cambridge shortcut to Results.jsx for anonymous users, and write two missing/outdated planning verification documents.

Purpose: Clean up artifacts and documentation gaps left by the v5.0 sprint so the codebase and planning history are consistent with the shipped state.
Output: Elections.jsx deleted; Results.jsx has Cambridge shortcut button; 39-VERIFICATION.md created; 42-VERIFICATION.md updated.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-v50-tech-debt-cleanup/47-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Elections.jsx and add Cambridge shortcut to Results.jsx</name>
  <files>src/pages/Elections.jsx, src/pages/Results.jsx</files>
  <action>
    **Step A — Delete Elections.jsx:**
    Delete `src/pages/Elections.jsx`. This file is dead code: App.jsx line 60 has a Navigate redirect from /elections to /results and Elections.jsx is not imported anywhere in App.jsx. Verify by grepping all .jsx/.js files for `import Elections` before deleting — there must be zero hits.

    Elections.jsx contained a SHORTCUTS constant with entries for Monroe County and LA County. These shortcuts are intentionally retired and must NOT be ported to Results.jsx. Reason: Elections.jsx was dead code — the /elections route already redirected to Results.jsx via App.jsx Navigate, so these shortcuts were never visible to users. The Phase 47 success criteria call for Cambridge only as the new shortcut in Results.jsx.

    **Step B — Add Cambridge shortcut to Results.jsx:**
    Add a `SHORTCUTS` constant near the top of the file (before the component function, after the imports/utility functions block, around line 328 where the component starts). Place it just before `export default function Results()`:

    ```jsx
    const SHORTCUTS = [
      {
        label: 'Cambridge, MA',
        browseGovernmentList: '2511000',
        browseLabel: 'Cambridge',
        browseState: 'MA',
      },
    ];
    ```

    Add a shortcut button section inside the address-mode search div (line ~1430, inside the `className={searchMode !== 'address' ? 'hidden' : 'flex gap-3'}` div). Place it BELOW the input row (after the closing `</div>` of the input+button+cancel row, still inside the outer address-mode div):

    ```jsx
    {/* Quick-access shortcuts for anonymous users */}
    {SHORTCUTS.length > 0 && (
      <div className="mt-2 flex flex-wrap gap-2">
        {SHORTCUTS.map((sc) => (
          <button
            key={sc.label}
            type="button"
            onClick={() => {
              const params = new URLSearchParams({
                browse_government_list: sc.browseGovernmentList,
                browse_label: sc.browseLabel,
              });
              if (sc.browseState) params.set('browse_state', sc.browseState);
              navigate(`/results?${params}`);
            }}
            className="border border-[var(--ev-teal)] dark:border-ev-teal-light text-[var(--ev-teal)] dark:text-ev-teal-light px-3 py-1.5 rounded text-sm font-medium hover:bg-[var(--ev-bg-light)] dark:hover:bg-gray-800 disabled:opacity-60"
          >
            {sc.label}
          </button>
        ))}
      </div>
    )}
    ```

    IMPORTANT: Place the shortcut section INSIDE the address-mode div (the one with `className={searchMode !== 'address' ? 'hidden' : ...}`), not outside it. The shortcuts should only be visible in address mode. Do NOT use `--ev-bg-light` if it does not exist in the design system — use `hover:bg-gray-100` as fallback.

    Do NOT modify App.jsx.
  </action>
  <verify>
    1. `ls src/pages/Elections.jsx` must return "file not found" (file deleted)
    2. `grep -r "import Elections" src/` must return zero matches
    3. `grep -n "Navigate.*results.*view=elections" src/App.jsx` must match (confirms redirect is intact after Elections.jsx deletion)
    4. `grep -n "SHORTCUTS" src/pages/Results.jsx` must show the constant definition
    5. `grep -n "browse_government_list.*2511000" src/pages/Results.jsx` must match
    6. `npm run build` must complete without errors (no broken imports)
  </verify>
  <done>
    Elections.jsx does not exist on disk; zero import references to Elections exist in the codebase; App.jsx Navigate redirect confirmed intact by grep; Results.jsx has SHORTCUTS constant with Cambridge entry; shortcut button renders below the address input in address mode; build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 39 VERIFICATION.md</name>
  <files>.planning/phases/39-ma-government-db/39-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/39-ma-government-db/39-VERIFICATION.md`.

    Use the same YAML frontmatter + section structure as the Phase 42 VERIFICATION.md. All data comes from the three plan SUMMARYs (already in planning context). Status is PASSED — everything was verified at execution time.

    Write the file with this content:

    ---
    Frontmatter:
      phase: 39-ma-government-db
      verified: 2026-05-16T00:00:00Z
      checked: 2026-05-16
      status: passed
      score: 3/3 must-haves verified
      re_verification: false
    ---

    # Phase 39: MA Government DB Verification Report

    **Phase Goal:** Seed the Commonwealth of Massachusetts government row, Massachusetts Senate chamber, Massachusetts House of Representatives chamber, all 40 state senators with district-linked offices, and all 160 state house offices (158 named representatives + 2 vacant) into the Essentials database.
    **Verified:** 2026-05-16
    **Status:** passed
    **Re-verification:** No — initial verification

    ## Goal Achievement

    ### Observable Truths

    | # | Truth | Status | Evidence |
    |---|-------|--------|----------|
    | 1 | Commonwealth of Massachusetts government row exists with correct UUID, type=STATE, state=MA | VERIFIED | id=85783e20-3031-4d71-89a5-5dd61f4a593f; type='STATE'; state='MA'; geo_id='25'; confirmed by migration 150 INSERT 1 and idempotent re-run INSERT 0 (39-01-SUMMARY.md) |
    | 2 | 40 Massachusetts state senator rows + 40 senate office rows exist, district-linked; Cambridge senators (25D26/25D27/25D28) return correct names | VERIFIED | 40 politician rows (external_ids -210001 to -210040) + 40 office rows inserted by migration 151; Cambridge routing confirmed: DiDomenico (25D26), Jehlen (25D27), Brownsberger (25D28); idempotent re-run INSERT 0 all 40 blocks (39-02-SUMMARY.md) |
    | 3 | 160 Massachusetts house office rows exist (158 named reps + 2 vacant); Cambridge reps (25082/25083/25084) return correct names | VERIFIED | 160 office rows + 158 politician rows confirmed by DB query; vacant offices: 25042 and 25075 (is_vacant=true, politician_id=NULL); Cambridge routing confirmed: Rogers (25082), Decker (25083), Connolly (25084) with email_addresses seeded; idempotent re-run: 160 offices unchanged (39-03-SUMMARY.md) |

    **Score:** 3/3 truths verified

    ### Required Artifacts

    | Artifact | Expected | Status | Details |
    |----------|----------|--------|---------|
    | `essentials.governments` | 1 row for Commonwealth of Massachusetts | VERIFIED | id=85783e20-3031-4d71-89a5-5dd61f4a593f; name='Commonwealth of Massachusetts'; type='STATE'; state='MA'; geo_id='25' |
    | `essentials.chambers` | 2 rows for MA Senate + MA House | VERIFIED | MA Senate id=ddc43e0f-3157-4201-b882-ae2f75d06d5a; MA House id=5f3d03da-68fe-4413-9fdc-96cde252f899; both linked to government row via government_id |
    | `C:/EV-Accounts/backend/migrations/150_ma_government_chambers.sql` | Migration file | VERIFIED | Committed at f58e953; 3 idempotent inserts with NOT EXISTS guards |
    | `C:/EV-Accounts/backend/migrations/151_ma_state_senate_officials.sql` | Migration file (40 senator CTEs) | VERIFIED | Committed at 844798a + 29e14e9; 40 CTE blocks with ON CONFLICT external_id |
    | `C:/EV-Accounts/backend/migrations/152_ma_state_house_officials.sql` | Migration file (160 house offices) | VERIFIED | Committed at 325bd98; generated via generate_ma_house.ps1 (UTF-8 BOM required for diacritics) |

    ### Key Link Verification

    | From | To | Via | Status | Details |
    |------|----|-----|--------|---------|
    | `essentials.governments.id` | `essentials.chambers.government_id` | Subquery in migration 150 | VERIFIED | Both chamber rows reference government row via `(SELECT id FROM essentials.governments WHERE name = 'Commonwealth of Massachusetts')` |
    | `essentials.chambers.id` | `essentials.offices.chamber_id` | Subquery pattern in migrations 151/152 | VERIFIED | Senate offices link to ddc43e0f; House offices link to 5f3d03da |
    | Cambridge geo_id 25D26 | DiDomenico senator row | District lookup via STATE_UPPER + state='ma' | VERIFIED | lowercase 'ma' required in district WHERE clause (uppercase returns 0 rows — key decision from Phase 38 research) |
    | Cambridge geo_ids 25082/25083/25084 | Rogers/Decker/Connolly rep rows | District lookup via STATE_LOWER + state='ma' | VERIFIED | Email addresses seeded; routing confirmed in 39-03-SUMMARY.md |

    ### Anti-Patterns Found

    None. All migrations are idempotent (NOT EXISTS guards on government/chamber rows; ON CONFLICT external_id on politician rows). The PowerShell generator script (generate_ma_house.ps1) required a UTF-8 BOM for diacritic characters — documented in 39-03-SUMMARY.md as a reusable pattern for future state legislative bodies.

    ### Key Decisions Preserved

    - `state='MA'` uppercase in `essentials.governments` (districts table uses lowercase `'ma'` — different convention; critical for correct routing)
    - `state='ma'` lowercase in district WHERE clauses for STATE_UPPER and STATE_LOWER office lookups
    - Cambridge-area senators (25D26/27/28) and reps (25082/83/84) seeded with email_addresses; all others default NULL
    - Migration 150 UUID unknown at write time — chamber inserts use subquery, not hardcoded UUID (idempotent pattern)

    ### Verification Data

    **Migration 150 (government + chambers):** INSERT 1 1 1 on first run; INSERT 0 0 0 on re-run
    **Migration 151 (40 senators):** 40 × INSERT 0 1 on first run; 40 × INSERT 0 0 on re-run
    **Migration 152 (160 house offices):** 160 office rows confirmed; 158 named rep rows; 2 vacant (25042, 25075)

    **Cambridge Senate routing confirmed:**
    - 25D26 → DiDomenico (Middlesex and Suffolk)
    - 25D27 → Jehlen (Second Middlesex)
    - 25D28 → Brownsberger (Suffolk and Middlesex)

    **Cambridge House routing confirmed:**
    - 25082 → Rogers, Dave (Dave.Rogers@mahouse.gov)
    - 25083 → Decker, Marjorie (Marjorie.Decker@mahouse.gov)
    - 25084 → Connolly, Mike (Mike.Connolly@mahouse.gov)

    ---
    *Verified: 2026-05-16*
    *Verifier: Claude (gsd-verifier — retrospective)*
    *Cross-reference: Phase 40 VERIFICATION.md corroborates Phase 39 chamber/government row data*
  </action>
  <verify>
    `ls .planning/phases/39-ma-government-db/39-VERIFICATION.md` must exist.
    File must contain the government UUID `85783e20-3031-4d71-89a5-5dd61f4a593f`.
    File must contain "40" (senators) and "160" (house offices).
    File must contain "status: passed" in frontmatter.
  </verify>
  <done>
    .planning/phases/39-ma-government-db/39-VERIFICATION.md exists with YAML frontmatter status=passed, all three observable truths documented, government UUID recorded, Cambridge routing ground truth documented.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Phase 42 VERIFICATION.md for Yi-An Huang gap closure</name>
  <files>.planning/phases/42-cambridge-headshots/42-VERIFICATION.md</files>
  <action>
    Read `.planning/phases/42-cambridge-headshots/42-VERIFICATION.md` first (mandatory — file exists).

    Make these targeted edits:

    1. **Truth #3 row** — Change:
       - Status from `VERIFIED (gap documented)` to `VERIFIED (gap closed Phase 46)`
       - Evidence: replace "DB: Yi-An Huang has no politician_images row; SUMMARY documents 10+ sources searched..." with "Gap closed in Phase 46: portrait sourced from cambridgema.gov and uploaded to Supabase Storage at project standard (600x750 JPEG)"

    2. **DB Coverage Query Results table** — Change the City Council row:
       - `with_headshot=9` → `with_headshot=10`
       - `missing_headshot=1 (Yi-An Huang — documented gap)` → `missing_headshot=0`

    3. **Required Artifacts table** — Update `essentials.politician_images`:
       - Change "14 rows confirmed" to "15 rows confirmed" (Huang row added in Phase 46)
       - Change "14 imaged politicians have url_set" to "15 imaged politicians have url_set"

    4. **Gap Documentation Assessment section** — Remove Yi-An Huang from the gap list. The section currently documents two gaps. Keep only Luisa de Paula Santos. Replace the Yi-An Huang paragraph with: "**Yi-An Huang (City Manager):** Gap closed in Phase 46. Portrait sourced from cambridgema.gov and uploaded to Supabase Storage."

    5. **Score line** — Change "4/4 truths verified (2 with documented gaps satisfying plan's missing-photo policy)" to "4/4 truths verified (1 with documented gap satisfying plan's missing-photo policy; 1 gap subsequently closed in Phase 46)"

    6. **Add re-verification note** — At the very bottom of the file (after the existing `*Verified: 2026-05-17*` line), add:
       ```
       *Re-verified: 2026-05-18 — Yi-An Huang gap closed in Phase 46 (portrait sourced from cambridgema.gov)*
       ```

    CRITICAL: Do NOT change `verified: 2026-05-17T00:00:00Z` in the YAML frontmatter. Do NOT change `checked: 2026-05-17`. The original verification date must be preserved.
  </action>
  <verify>
    1. `grep "gap closed Phase 46" .planning/phases/42-cambridge-headshots/42-VERIFICATION.md` must match
    2. `grep "verified: 2026-05-17" .planning/phases/42-cambridge-headshots/42-VERIFICATION.md` must still match (date preserved)
    3. `grep "with_headshot=10" .planning/phases/42-cambridge-headshots/42-VERIFICATION.md` must match (City Council row updated)
    4. `grep "Luisa de Paula Santos" .planning/phases/42-cambridge-headshots/42-VERIFICATION.md` must match (still documented as open gap)
    5. `grep "Re-verified: 2026-05-18" .planning/phases/42-cambridge-headshots/42-VERIFICATION.md` must match
  </verify>
  <done>
    42-VERIFICATION.md reflects Huang gap closed (Phase 46), coverage table updated to 10 with_headshot for City Council, Luisa de Paula Santos preserved as open gap, original verified date untouched, re-verification note added at bottom.
  </done>
</task>

</tasks>

<verification>
1. `ls src/pages/Elections.jsx` returns file-not-found
2. `grep -r "import Elections" src/` returns zero matches
3. `grep -n "Navigate.*results.*view=elections" src/App.jsx` matches (redirect intact)
4. `grep -n "SHORTCUTS" src/pages/Results.jsx` returns the constant definition
5. `grep -n "2511000" src/pages/Results.jsx` returns the Cambridge shortcut entry
6. `npm run build` completes without errors
7. `ls .planning/phases/39-ma-government-db/39-VERIFICATION.md` exists
8. `grep "status: passed" .planning/phases/39-ma-government-db/39-VERIFICATION.md` matches
9. `grep "gap closed Phase 46" .planning/phases/42-cambridge-headshots/42-VERIFICATION.md` matches
10. `grep "verified: 2026-05-17" .planning/phases/42-cambridge-headshots/42-VERIFICATION.md` still matches
</verification>

<success_criteria>
1. `src/pages/Elections.jsx` does not exist; no import of Elections exists anywhere in the project
2. `src/App.jsx` is unchanged — Navigate redirect at line 60 is intact; confirmed by grep
3. `src/pages/Results.jsx` contains a SHORTCUTS constant with a Cambridge entry (browse_government_list=2511000, browse_label=Cambridge, browse_state=MA)
4. The Cambridge shortcut button renders below the address input in address mode in Results.jsx
5. `.planning/phases/39-ma-government-db/39-VERIFICATION.md` exists, status=passed, records government UUID 85783e20-3031-4d71-89a5-5dd61f4a593f, 40 senators, 160 house offices, Cambridge routing
6. `.planning/phases/42-cambridge-headshots/42-VERIFICATION.md` reflects Yi-An Huang gap closed in Phase 46; Luisa de Paula Santos still documented as open; original verified date 2026-05-17 preserved
7. `npm run build` passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/47-v50-tech-debt-cleanup/47-01-SUMMARY.md`
</output>
