---
phase: 179-city-of-tualatin-deep-seed
plan: 04
type: execute
wave: 3
depends_on: ["179-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1171_bubenik_stances.sql
  - C:/EV-Accounts/backend/migrations/1172_reyes_stances.sql
  - C:/EV-Accounts/backend/migrations/1173_sacco_stances.sql
  - C:/EV-Accounts/backend/migrations/1174_brooks_stances.sql
  - C:/EV-Accounts/backend/migrations/1175_hillier_stances.sql
  - C:/EV-Accounts/backend/migrations/1176_gonzalez_stances.sql
  - C:/EV-Accounts/backend/migrations/1177_pratt_stances.sql
autonomous: false
requirements: [WASH-05]

must_haves:
  truths:
    - "Each of the 7 officials has evidence-only compass stances where a public record exists"
    - "Every stance is 100% cited (reasoning + sources in politician_context); ZERO default/neutral values"
    - "Topics with no evidence are omitted entirely (honest blank spokes), never defaulted"
    - "topic_id is resolved LIVE via JOIN on compass_topics.topic_key AND is_live=true (no hardcoded topic UUIDs)"
    - "Each stance migration ends with the TRIPLE-GATE DO block (WR-01 identity + WR-02 answers count + WR-03 context-parity count) from mig 1161"
    - "No judicial-* topics are used (City Attorney + Municipal Court Judge are appointed, not elected)"
    - "Stance research was done one agent at a time (never parallel)"
    - "local-immigration expected thinner (Oregon statewide sanctuary law applies uniformly, no Tualatin-specific council action) — honest blanks acceptable"
    - "Decision coverage — D-08: all live compass topics per official, one research agent at a time, evidence-only, 100% cited, honest blank spokes, zero default values, 18-21+ depth where the record supports it, judicial-* topics skipped"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1171_bubenik_stances.sql"
      provides: "Mayor Frank Bubenik stances (audit-only)"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1177_pratt_stances.sql"
      provides: "Council President Valerie Pratt stances (audit-only)"
      contains: "politician_answers"
  key_links:
    - from: "each stance migration"
      to: "inform.compass_topics (is_live=true)"
      via: "JOIN on topic_key"
      pattern: "topic_key = s.topic_key AND ct.is_live = true"
    - from: "each stance migration"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\)"
---

<objective>
Research and author 7 audit-only stance migrations — one per Tualatin official — populating
evidence-only compass stances. Research runs ONE AGENT AT A TIME (rate-limit rule), evidence-only,
100% cited, honest blank spokes where no public record exists, ZERO default values, and skips all
judicial-* topics (Tualatin's City Attorney + Municipal Court Judge are appointed). Every file carries
the TRIPLE-GATE DO block (WR-01 identity + WR-02 answers-count + WR-03 context-parity) from the
freshest analog, mig 1161 — this is one generation more evolved than the identity+count pair
documented in 178-PATTERNS.md.

Purpose: Satisfy the stance portion of WASH-05 — evidence-only stances render on profiles.
Output: 7 audit-only migrations (`1171_bubenik_stances.sql` … `1177_pratt_stances.sql`), NOT registered
in the ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-RESEARCH.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-01-SUMMARY.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** stance research runs via ORCHESTRATOR-spawned general-purpose agents
(one at a time — gsd-executor has NO WebSearch and NO Supabase MCP). The executor WRITES the 7 `.sql`
files from the researched evidence; the INLINE ORCHESTRATOR applies each via `psql -f` and commits
them to the EV-Accounts repo. These are AUDIT-ONLY migrations — none writes a schema_migrations
ledger row; the on-disk file counter is authoritative. Migration numbers 1171-1177 assume plan 02
used 1169 + plan 03 used 1170; if Wave-0 shifted the base, renumber consistently.

**STANCE RULES (non-negotiable — from operator conventions + memory):**
- ONE research agent at a time — never parallel (parallel burns rate-limit quota with no output);
  time-box each agent (~20–25 web ops).
- Evidence-only chairs model (integer 1–5); a "chair" is the position statement the evidence matches,
  NOT a polarity scale — pick the chair the evidence supports.
- 100% cited: every answer has reasoning + sources in politician_context.
- NO default values — a topic with no evidence is OMITTED entirely (blank spoke is honest).
- Skip all 8 judicial-* topics.
- Use the LIVE topic_key list captured in 179-01-SUMMARY.md; resolve topic_id via JOIN on
  compass_topics.topic_key AND is_live=true — never hardcode a topic UUID.
- Aim for as many cited stances as the record supports (18-21+ where the record is deep); thin records
  get honest blanks.
- Tenure/attribution: Bubenik has the deepest record (councilor since 2010, Mayor since 2018) — his
  citywide record supports the deepest coverage. Reyes/Brooks (since 2018/2019) and Gonzalez (since
  2023) have solid records; Sacco/Hillier (since 2021) and Pratt (councilor since 2019/2020, though
  seat origin 2019 was appointment) also have real records. Attribute only actions during each
  member's actual service.
- local-immigration is expected THINNER (Oregon's statewide sanctuary law applies uniformly — no found
  Tualatin-specific sanctuary resolution); an honest blank is acceptable, do NOT force it.
- Do NOT seed any 2026-election candidate position as a current stance — this is the currently-seated
  roster only (Phase 185 owns 2026 candidate data).
</context>

<interfaces>
inform.compass_topics    (id, topic_key, is_live)   -- 44 live topics; 8 are judicial-* (SKIP)
inform.politician_answers (politician_id, topic_id, value)                    -- ON CONFLICT (politician_id, topic_id) DO UPDATE SET value
inform.politician_context (politician_id, topic_id, reasoning, sources)       -- sources is text[]; ON CONFLICT DO UPDATE
-- politician UUIDs: resolve by external_id from 179-02-SUMMARY.md:
--   -4174951 Frank Bubenik (Mayor) | -4174952 María Reyes | -4174953 Christen Sacco | -4174954 Bridget Brooks
--   -4174955 Cyndy Hillier | -4174956 Octavio Gonzalez | -4174957 Valerie Pratt (Council President)
-- File→official mapping: 1171=Bubenik, 1172=Reyes, 1173=Sacco, 1174=Brooks, 1175=Hillier, 1176=Gonzalez, 1177=Pratt
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Research + author stance migrations for Bubenik, Reyes, Brooks, and Gonzalez (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1171_bubenik_stances.sql, C:/EV-Accounts/backend/migrations/1172_reyes_stances.sql, C:/EV-Accounts/backend/migrations/1174_brooks_stances.sql, C:/EV-Accounts/backend/migrations/1176_gonzalez_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1161_hu_stances.sql (62-line stance template — the freshest: two-statement politician_answers + politician_context structure + the TRIPLE-GATE DO block, identity + count + context-parity)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md §"1171_bubenik_stances.sql through 1177_pratt_stances.sql (7 stance migrations, CRUD)"
    - .planning/phases/179-city-of-tualatin-deep-seed/179-01-SUMMARY.md (live topic_key list)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Bubenik (-4174951), Reyes (-4174952), Brooks (-4174954), Gonzalez (-4174956), IN
    SEQUENCE (one research agent at a time — do not spawn parallel research): research the official's
    documented positions across the live compass topics (skip judicial-*), gathering evidence with
    URLs. Then write the migration file using the two-statement structure from the 1161 analog: a
    WITH s(topic_key, val, reasoning, sources) VALUES block (one row per topic WITH evidence — omit
    topics with no evidence), an INSERT into inform.politician_answers … JOIN compass_topics ON
    topic_key AND is_live=true ON CONFLICT (politician_id, topic_id) DO UPDATE SET value, then the
    IDENTICAL VALUES block feeding INSERT into inform.politician_context … ON CONFLICT DO UPDATE SET
    reasoning, sources. THEN append the TRIPLE-GATE DO block from 1161: (WR-01) resolve external_id
    from essentials.politicians WHERE id='<uuid>' and RAISE EXCEPTION if it is DISTINCT FROM the
    expected ext_id (identity gate — a wrong-but-existing UUID would satisfy the FK while
    misattributing stances); (WR-02) SELECT COUNT(*) from inform.politician_answers for the uuid and
    RAISE if <> expected authored count; (WR-03) SELECT COUNT(*) from inform.politician_context for the
    uuid and RAISE if <> expected count (context-parity — a single-sided VALUES edit cannot silently
    skew reasoning/sources). Hardcode the politician UUID (from 179-02-SUMMARY.md) and its expected
    ext_id in each file. val is an integer 1–5 chair; never default; blank spoke = omit the row. Wrap
    each file in BEGIN…COMMIT with the audit-only header comment; NO ledger INSERT. Bubenik's record
    (councilor since 2010, Mayor since 2018) supports the deepest coverage. No fenced code in this
    action — 1161 + 179-PATTERNS.md are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (the triple-gate DO block RAISEs on an identity/count/parity mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 4 files authored: 1171_bubenik_stances.sql, 1172_reyes_stances.sql, 1174_brooks_stances.sql, 1176_gonzalez_stances.sql
    - Each uses the two-statement politician_answers + politician_context structure
    - Each ends with the TRIPLE-GATE DO block (WR-01 identity + WR-02 answers count + WR-03 context-parity), expected counts matching authored rows
    - topic_id resolved via JOIN on topic_key AND is_live=true (no hardcoded topic UUIDs)
    - No judicial-* topic_key appears in any VALUES block
    - Every VALUES row has non-empty reasoning + at least one source URL
    - No default/neutral row for a topic lacking evidence (omitted instead)
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>4 stance migrations authored from one-at-a-time research; triple-gate assertions present; audit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Research + author stance migrations for Sacco, Hillier, and Pratt (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1173_sacco_stances.sql, C:/EV-Accounts/backend/migrations/1175_hillier_stances.sql, C:/EV-Accounts/backend/migrations/1177_pratt_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1161_hu_stances.sql
    - .planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md §"1171_bubenik_stances.sql through 1177_pratt_stances.sql (7 stance migrations, CRUD)"
    - .planning/phases/179-city-of-tualatin-deep-seed/179-01-SUMMARY.md (live topic_key list)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Sacco (-4174953), Hillier (-4174955), Pratt (-4174957), IN SEQUENCE (one agent at a
    time): research documented positions across live compass topics (skip judicial-*), then author the
    migration with the same two-statement structure + TRIPLE-GATE DO block as Task 1. Sacco and Hillier
    (seated Jan 2021, reelected 2024) have real council records — use votes + first-party statements as
    cited evidence. Pratt (Council President; councilor since her 2020 election, seat origin 2019
    appointment) has the deepest council-leadership record of the three — attribute her documented
    positions and leadership actions; do NOT let her Council President title inflate stances beyond the
    evidence. Never default. Expect local-immigration to be an honest blank for most (Oregon statewide
    sanctuary law, no Tualatin-specific action). Audit-only header; NO ledger INSERT. UUIDs + expected
    ext_ids hardcoded from 179-02-SUMMARY.md.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (triple-gate DO block RAISEs on mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 3 files authored: 1173_sacco_stances.sql, 1175_hillier_stances.sql, 1177_pratt_stances.sql
    - Same two-statement structure + TRIPLE-GATE DO block (identity + count + context-parity); topic_id via is_live JOIN
    - No judicial-* topics; every row cited; no defaults (thin topics → honest blanks)
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>3 stance migrations authored from one-at-a-time research; triple-gate assertions present; audit passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator applies all 7, runs stance audit, commits</name>
  <what-built>7 audit-only stance migrations. Orchestrator applies each and runs the citation/no-default audit.</what-built>
  <how-to-verify>
    1. Apply all 7 in order: `psql "$DATABASE_URL" -f <each stance file>`. Each triple-gate DO block must pass (no identity/count/parity RAISE).
    2. Stance audit (run as SELECTs against the 7 Tualatin politician UUIDs):
       a. Every politician_answers row has a matching politician_context row with non-null reasoning + non-empty sources (100% cited).
       b. ZERO rows where value is a defaulted/neutral placeholder for a topic lacking evidence (spot-check reasoning quality).
       c. ZERO judicial-* topics linked to any Tualatin official.
       d. Each official has ≥1 stance (all 7 have real records — a 0-count for any official = re-check, not expected here).
    3. Commit all 7 in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/117{1,2,3,4,5,6,7}_*.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - All 7 stance migrations applied; all triple-gate assertions passed
    - Audit a–d all pass (100% cited, no defaults, no judicial, honest blanks documented)
    - All 7 committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official stance counts + any honest blanks, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Stance INSERTs applied to production inform schema via psql |
| research agent → public web | Evidence gathered from official/news/campaign sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-13 | Tampering | Uncited or fabricated stance value | mitigate | 100%-cited rule enforced by audit (every answer needs reasoning + sources); no-default rule; one-agent-at-a-time keeps evidence traceable |
| T-179-14 | Tampering | topic_key typo silently drops authored rows | mitigate | WR-02 answers-count + WR-03 context-parity assertions in every stance file RAISE on a count mismatch |
| T-179-15 | Repudiation | Wrong-UUID misattribution (a valid-but-wrong politician UUID) | mitigate | WR-01 identity gate resolves external_id from the hardcoded UUID and RAISEs unless it matches the intended official's ext_id |
| T-179-16 | Elevation of Privilege | Judicial-* topic seeded for a non-judicial official | mitigate | Skip-judicial rule; audit gate 2c asserts zero judicial-* topics |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- 7 stance migrations applied; triple-gate assertions pass; audit confirms 100% cited, zero defaults,
  zero judicial topics, honest blanks documented; all committed.
</verification>

<success_criteria>
- Evidence-only stances for all 7 officials, 100% cited, no defaults, no judicial topics, triple-gate
  assertions present, researched one agent at a time.
</success_criteria>

<output>
Create `.planning/phases/179-city-of-tualatin-deep-seed/179-04-SUMMARY.md` when done, recording
per-official stance counts and any honest blanks.
</output>
