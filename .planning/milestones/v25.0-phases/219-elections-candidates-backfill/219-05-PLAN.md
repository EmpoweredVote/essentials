---
phase: 219-elections-candidates-backfill
plan: 05
type: execute
wave: 2
depends_on: [219-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/1396_plano_weston_staggered_races.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1396_plano_weston_staggered_races.ts
autonomous: false
requirements: [COLLIN-ELECT-01, COLLIN-ELECT-03]
user_setup: []

must_haves:
  truths:
    - "Plano (4858016) has ≥1 seeded race with candidates — at minimum the Jan-31-2026 Place 7 special (Shun Thomas won ~60.4%) under its own special-election row — plus each other Plano seat's most-recent staggered election where resolvable (D-01)."
    - "Plano Council Member Place 6 is left race-less with the documented-vacancy reason (migration 1392) — a legitimately-empty seat, NOT a defect (D-03)."
    - "Weston (4877740) has ≥1 seeded race under its real November-cycle election row (218 confirmed Weston had NO May-2026 election; Marla Johnston's Place 5 term is 2024-11-01→2026-11-01) — NOT the shared 2026-05-02 row (D-01)."
    - "Every seeded race links to the correct existing office_id and renders on /results (COLLIN-ELECT-03)."
    - "Re-applying migration 1396 is net-zero (idempotent)."
  artifacts:
    - "C:/EV-Accounts/backend/migrations/1396_plano_weston_staggered_races.sql"
    - "C:/EV-Accounts/backend/scripts/_apply-migration-1396_plano_weston_staggered_races.ts"
  key_links:
    - "races.election_id → Plano Jan-2026 special row + per-seat staggered rows + Weston November-cycle row (NOT the shared 2026-05-02 row for Weston)."
    - "races.office_id → existing Plano/Weston offices.id per seat."
  prohibitions:
    - statement: "candidate_status only ever uses a legal CHECK value (active/withdrawn/filed)."
      status: enforced
      verification: "apply-script gate: 0 candidate rows with candidate_status NOT IN ('active','withdrawn','filed')."
    - statement: "position_name always city-prefixed AND cycle-disambiguated across the multiple Plano election dates."
      status: enforced
      verification: "apply-script gate: no ON CONFLICT collision across Plano's staggered rows."
    - statement: "No race is seeded for Plano Place 6 (documented vacancy, migration 1392)."
      status: enforced
      verification: "apply-script gate: 0 races for the Plano Place 6 office_id."
    - statement: "Weston is NOT linked to the shared 2026-05-02 election row."
      status: enforced
      verification: "apply-script gate: 0 Weston races whose election_id = the shared 2026-05-02 id."
    - statement: "primary_party stays NULL (D-06); no inform.* writes (D-07)."
      status: enforced
      verification: "0 non-NULL primary_party on seeded races; inform.* before/after unchanged."
    - statement: "No fabricated candidate; Plano's un-resolvable staggered seats stay documented-open, not guessed."
      status: enforced
      verification: "Every seeded candidate cited; open seats have a written reason."
  backstop:
    - statement: "Plano's per-seat staggered term-history depth (2023-2026) may exceed a cheap research horizon; document any seat whose last election predates a reasonable horizon as a legitimately-out-of-cheap-reach gap rather than open-ended digging."
      verification: backstop
---

<objective>
Seed Plano and Weston — the two most staggered zero-race cities. Plano runs 3-year staggered terms across 2023-2026 plus a Jan-31-2026 Place 7 special; Weston votes on a November cycle (no May-2026 election at all). Both need their own election rows; Plano Place 6 stays a documented vacancy.

Purpose: Closes COLLIN-ELECT-01 for Plano + Weston with per-seat honesty (Pitfall 1). COLLIN-ELECT-03 for their render path.
Output: migration 1396 + gated apply-script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-elections-candidates-backfill/219-RESEARCH.md
@.planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md
@.planning/phases/219-elections-candidates-backfill/219-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock Plano (Jan-2026 special + staggered seats) and Weston (November cycle) cited rosters</name>
  <read_first>
    - .planning/phases/219-elections-candidates-backfill/219-PREFLIGHT.md.
    - 219-RESEARCH.md §"Zero-race governments" Plano/Weston rows, §Pitfall 1, §Pitfall 4.
    - C:/EV-Accounts/backend/migrations/1392_plano_place6_documented_vacancy.sql (Plano Place 6 vacancy fact) and 1389 (Plano Place 7 = Shun Thomas already seated).
    - Sources: plano.gov/1402/Elections, Ballotpedia "City elections in Plano, Texas (2026)", westontexas.com/page/Mayor_Aldermen.
  </read_first>
  <action>
    Plano: confirm the Jan-31-2026 Place 7 special (Shun Thomas ~60.4%, full filed field, cited — D-04); then for each remaining Plano seat pull its most-recent staggered election year + roster from plano.gov/Ballotpedia. Skip Place 6 entirely (documented vacancy — D-03). Any seat whose last election predates a reasonable research horizon: document-open, don't guess. Weston: pull its November-cycle roster (Place 5 = Marla Johnston 2024-11 term at minimum); assemble the full filed field per resolvable seat. Map winners to offices.politician_id where Phase 218 seated them (D-05); losers name-only; is_incumbent explicit. Escape apostrophes; preserve diacritics.
  </action>
  <verify>
    <automated>MISSING — roster verified by Task 2 apply-script gates.</automated>
  </verify>
  <acceptance_criteria>
    - Plano: Place 7 special roster cited; each other resolvable seat has a cited roster + election date; Place 6 explicitly excluded; unresolvable seats documented-open.
    - Weston: ≥1 cited November-cycle race roster with its election date.
  </acceptance_criteria>
  <done>Seed-ready cited rosters + election dates exist for Plano (minus Place 6) and Weston, with open-seat documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 1396 (mint Plano/Weston election rows) + gated apply-script</name>
  <read_first>
    - 219-RESEARCH.md §Pattern 1/2/3/4 + §Pattern 5.
    - C:/EV-Accounts/backend/migrations/1375 (ON CONFLICT idiom), 187 (own election-row shape), 1392 (Plano Place 6 vacancy).
    - C:/EV-Accounts/backend/scripts/_apply-migration-1389_collin_seat_cited_incumbents.ts (gate shape).
    - Migration number from PREFLIGHT (research: 1396; re-confirm + shift block if drifted).
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1396_plano_weston_staggered_races.sql in BEGIN/COMMIT. INSERT (ON CONFLICT (name, election_date, state) DO NOTHING) the Plano Jan-31-2026 special election row (reuse if it already exists — resolve by name/date/state), any per-seat staggered Plano election rows needed, and a Weston November-cycle election row. INSERT races with city-prefixed + cycle-disambiguated position_name, correct office_id, primary_party NULL, ON CONFLICT position-name idiom; do NOT insert any race for the Plano Place 6 office. INSERT race_candidates WHERE NOT EXISTS; winners→politician_id, candidate_status='active', is_incumbent explicit, cited source. Write C:/EV-Accounts/backend/scripts/_apply-migration-1396_plano_weston_staggered_races.ts mirroring 1389 with gates: per-geo_id race count >0 (4858016, 4877740); every seeded race has ≥1 candidate; 0 illegal candidate_status; 0 non-NULL primary_party; 0 races for the Plano Place 6 office_id; 0 Weston races on the shared 2026-05-02 id; inform.* unchanged; split-section 0; idempotent re-run net-zero.
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/_apply-migration-1396_plano_weston_staggered_races.ts 2>&1 | head; test -f migrations/1396_plano_weston_staggered_races.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; election rows minted/resolved by name/date/state.
    - Apply-script includes the Plano-Place-6 zero-race guard and the Weston wrong-row guard plus the standard gate set.
  </acceptance_criteria>
  <done>Migration 1396 + gated apply-script authored and type-check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator applies 1396, confirms gates green, pushes, browse spot-check</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-1396_plano_weston_staggered_races.ts.
    - [[no_git_in_ev_accounts]] / [[backend_architecture]].
  </read_first>
  <action>Operator-run checkpoint: apply migration 1396 via `npx tsx`, confirm every apply-script gate exits 0 (incl. Place-6 and Weston wrong-row guards), commit + push to C:/EV-Accounts (Render auto-deploys), then browse-spot-check Plano on /results — per the numbered how-to-verify steps.</action>
  <what-built>Executor has no DB access / prod push; operator applies + verifies.</what-built>
  <how-to-verify>
    1. `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-1396_plano_weston_staggered_races.ts` — all gates green, exit 0.
    2. `git -C "C:/EV-Accounts" add -A && git -C "C:/EV-Accounts" commit -m "feat(219): seed Plano staggered + Jan-2026 special + Weston November races" && git -C "C:/EV-Accounts" push origin master`.
    3. Browse spot-check `/results?browse_geo_id=4858016` (Plano) — confirm the seeded races render and Place 6 remains correctly race-less.
  </how-to-verify>
  <acceptance_criteria>
    - Apply-script exits 0, all gates green incl. Place-6 and Weston wrong-row guards.
    - Plano browse shows real races; Place 6 shows no phantom race (backstop: human visual).
  </acceptance_criteria>
  <resume-signal>Type "approved" once gates green + browse passes, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `C:/EV-Accounts/backend/migrations/1396_plano_weston_staggered_races.sql` (new)
- `C:/EV-Accounts/backend/scripts/_apply-migration-1396_plano_weston_staggered_races.ts` (new)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external canvass/news → race_candidates | Untrusted civic claims; every row cited |
| migration SQL → production essentials schema | Idempotent, guarded writes; vacancy + wrong-row guards |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Tampering/Info disclosure | race_candidates rows | high | mitigate | Evidence-only; staggered open seats documented, never guessed |
| T-219-02 | Tampering (schema integrity) | candidate_status | high | mitigate | Only legal CHECK values; gate asserts 0 illegal |
| T-219-05 | Data integrity (silent loss / mislink) | election_id linkage + Place 6 vacancy | high | mitigate | Own election rows; Place-6 zero-race guard; Weston wrong-row guard; cycle-disambiguated position_name |
| T-219-04 | Integrity (scope violation) | inform.* tables | high | mitigate | before/after row-count assertion (D-07) |
| T-219-03 | Tampering (SQL literal) | apostrophe/diacritic names | medium | mitigate | Escape '→''; preserve UTF-8; type-check |
| T-219-SC | Tampering | npm/pip installs | low | accept | No package installs; legitimacy gate N/A |
</threat_model>

<verification>
- All apply-script gates green incl. Place-6 and Weston wrong-row guards.
- Plano + Weston race counts > 0; 0 races for Plano Place 6; 0 Weston races on shared id; split-section 0; primary_party 0; inform.* net-zero.
</verification>

<success_criteria>
Plano (minus documented-vacant Place 6) and Weston render their real staggered/November races on /results, linked to their own election rows; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/219-elections-candidates-backfill/219-05-SUMMARY.md` when done.
</output>
