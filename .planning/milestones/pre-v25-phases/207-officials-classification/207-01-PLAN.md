---
phase: 207-officials-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/classify.js
  - src/lib/classify.test.js
autonomous: true
requirements: [CLASS-01]

must_haves:
  truths:
    - "A single exported source-of-truth classifyBucket(pol) in src/lib/classify.js returns exactly one of 'representative' | 'educator' | 'judge' for any input, and never throws on null/missing fields — the same function today's Results grouping and Phase 208's tabs both call, so classification can never drift (D-06)"
    - "Every SCHOOL / STATE_BOARD / SCHOOL_BOARD row classifies as 'educator' (SC-02)"
    - "Every JUDICIAL / NATIONAL_JUDICIAL row classifies as 'judge' (SC-03)"
    - "DA / prosecutor / public-defender titles (under COUNTY or LOCAL_EXEC) classify as 'judge'; Attorney General and City Attorney stay 'representative' (SC-03 / SC-04)"
    - "School superintendent (of public instruction / of schools) classifies as 'educator'; a non-education superintendent (police / public works / streets) stays 'representative' (SC-02 / SC-04)"
    - "A LOCAL-mistyped school board (title/chamber contains 'school board' / 'board of education') classifies as 'educator' (SC-02, live Portland-ME case)"
    - "Ordinary reps (mayor, council, state legislators, federal delegation) classify as 'representative' (SC-04)"
    - "A cleanly-typed JUDICIAL/SCHOOL/STATE_BOARD/SCHOOL_BOARD row is never pulled OUT of its bucket by a stray keyword — overrides are additive-only (D-08)"
    - "Classification verified across 3 contrasting real locations: LA, Bloomington/Monroe County IN, an AZ city (SC-05 / D-11)"
  artifacts:
    - path: "src/lib/classify.js"
      provides: "Exported classifyBucket(pol) single-source-of-truth 3-bucket classifier"
      contains: "export function classifyBucket"
    - path: "src/lib/classify.test.js"
      provides: "classifyBucket unit + live-location-fixture coverage"
      contains: "describe('classifyBucket'"
  key_links:
    - from: "src/lib/classify.js classifyBucket"
      to: "pol.district_type / pol.office_title / pol.chamber_name(_formal)"
      via: "direct field read with optional-chaining + || '' idiom (no groupHierarchy/branchType imports)"
      pattern: "pol\\?\\.(district_type|office_title|chamber_name)"
    - from: "src/lib/classify.js EDUCATOR district_type set"
      to: "DC State Board of Education rows"
      via: "SCHOOL_BOARD literal included alongside SCHOOL/STATE_BOARD (live-DB correction to D-04)"
      pattern: "SCHOOL_BOARD"
---

<objective>
Add one pure, side-effect-free classifier — `classifyBucket(pol) -> 'representative' | 'educator' | 'judge'` —
to the existing `src/lib/classify.js`, plus unit + live-location-fixture tests in `src/lib/classify.test.js`.
This is the single source of truth Phase 208's Educators & Judges tabs will call; classification can never
drift between today's Results list and the future tabs because both call the same function.

Purpose: Reliably bucket every office-holder returned for a location so school-board and judicial officials
leave the Representatives clutter and ordinary reps are never misfiled (CLASS-01 / SC-01..SC-05).

Output: A new exported `classifyBucket` in `src/lib/classify.js`; a `describe('classifyBucket', ...)`
edge-case block and a `describe('classifyBucket — live location fixtures (SC-05)', ...)` real-data block
appended to `src/lib/classify.test.js`.

Scope discipline (from planning context): pure frontend logic only. NO DB writes, NO backend/API change,
NO new seeding, NO tabs UI (Phase 208), NO per-tab lens shift (Phase 210), NO Education-lens data
(Phase 209). Do NOT fork existing classifiers; do NOT touch `classifyCategory` or `computeVariant`.
Classification is per office-holder row (D-10) — no person-level cross-government merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/207-officials-classification/207-CONTEXT.md
@.planning/phases/207-officials-classification/207-RESEARCH.md
@.planning/phases/207-officials-classification/207-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the file being modified. No codebase exploration required. -->

From src/lib/classify.js (existing siblings — REFERENCE the field-read idiom, do NOT modify these):
- `export function classifyCategory(pol)` reads exactly the three fields classifyBucket needs:
  `const dt = pol?.district_type || ""; const chamber = pol?.chamber_name_formal || pol?.chamber_name || ""; const title = pol?.office_title || "";`
  It ends every unmatched row in a safe default (`return { tier: "Unknown", group: "Uncategorized" };`) — the
  precedent for classifyBucket's `return 'representative'` catch-all (D-09).
- `export function computeVariant(pol, userAnswers, hasStances = true)` demonstrates the file's regex-title
  style to mirror: module-level intent, inline `/judge|justice|court/.test(title)` — copy the
  `const ..._RE = /.../i;` + `.test(title)` shape for classifyBucket's overrides.
- Module-private helpers `word` / `hasAny` exist (lines 1-6). Do NOT route the guarded overrides through
  `hasAny` — its plain substring match causes Pitfall 3 (a bare "attorney" substring matches "Attorney
  General"). Use `RegExp.test()` directly.

New export this plan adds — the contract Phase 208 will import:
- `export function classifyBucket(pol): 'representative' | 'educator' | 'judge'`

From src/lib/classify.test.js (existing test conventions to reuse):
- `import { computeVariant, classifyCategory } from './classify.js';` (line 7) — extend to add `classifyBucket`.
- `function makePol(overrides)` (lines 9-15) defaults `{ district_type: 'LOCAL', office_title: 'Council Member' }` —
  reuse as-is; do NOT invent a second fixture shape.
- `it.each([...])('...', (kw) => {...})` table-test pattern (lines 36-44) — use for the multi-title override tests.

Do NOT import from src/lib/groupHierarchy.js (`getTier`/`isJudicialOfficial`/`isAdminOfficer` are NOT exported —
importing would require an unrelated edit) or src/utils/branchType.js (`getBranch`'s COUNTY regex misses
"County Attorney"/"Prosecuting Attorney" — wrong for D-02). Read district_type/title/chamber directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing classifyBucket unit tests (RED)</name>
  <files>src/lib/classify.test.js</files>
  <read_first>
    - src/lib/classify.test.js — makePol helper (lines 9-15), it.each pattern (lines 36-44), existing STATE_BOARD block (lines 95-100), import line (7)
    - .planning/phases/207-officials-classification/207-RESEARCH.md — "District Type Enumeration" table (14 live literals + buckets), "Common Pitfalls" 1-5, "Validation Architecture" test map
    - .planning/phases/207-officials-classification/207-PATTERNS.md — the classify.test.js analog section (it.each / makePol reuse guidance)
    - .planning/phases/207-officials-classification/207-CONTEXT.md — D-01..D-11
  </read_first>
  <behavior>
    - Base cases (one assertion per live district_type literal): JUDICIAL and NATIONAL_JUDICIAL -> 'judge'; SCHOOL, STATE_BOARD, SCHOOL_BOARD -> 'educator'; LOCAL, STATE_LOWER, NATIONAL_LOWER, STATE_UPPER, COUNTY (plain title), STATE_EXEC (plain title), LOCAL_EXEC (plain title), NATIONAL_UPPER, NATIONAL_EXEC, CITY_COUNCIL -> 'representative'.
    - DA/PD override (Pitfall 1 — exercise BOTH bases): each of 'District Attorney', 'County Attorney', 'Prosecuting Attorney', "State's Attorney", 'City Prosecutor', 'Public Defender' under district_type 'COUNTY' AND under 'LOCAL_EXEC' -> 'judge'.
    - Negative attorney guards (Pitfall 3): 'Attorney General' (STATE_EXEC) -> 'representative'; 'City Attorney' (LOCAL_EXEC) -> 'representative'.
    - Superintendent override (Pitfall 5): 'Superintendent of Public Instruction' (STATE_EXEC) and 'State Superintendent of Schools' -> 'educator'; 'Superintendent of Police' and 'Superintendent of Public Works' (LOCAL_EXEC) -> 'representative'.
    - LOCAL-mistyped school board (D-04, Portland-ME): district_type 'LOCAL' with office_title containing 'School Board Member' -> 'educator'; district_type 'LOCAL' with chamber_name 'Board of Education' -> 'educator'.
    - Title-detected judge/justice fallback (D-03 — must PROVE the fallback fires, not just the D-08 non-subtraction): district_type '' (empty) with office_title 'Circuit Judge' -> 'judge'; a mistyped/unmapped district_type (e.g. 'FOO') with office_title containing 'Justice' -> 'judge'.
    - Additive-only invariant (D-08): district_type 'SCHOOL' with office_title containing 'Judge' -> 'educator' (keyword must NOT pull a clean row out); district_type 'JUDICIAL' with office_title containing 'School' -> 'judge'.
    - Null-safety (D-09 / T-207-01): classifyBucket(null), classifyBucket({}), classifyBucket({ district_type: null }) -> 'representative' without throwing.
  </behavior>
  <action>
    Extend the named import on line 7 to add classifyBucket from './classify.js'. Append a new self-contained
    top-level describe('classifyBucket', ...) block AFTER the final existing block (after line 112) — do not
    interleave with the computeVariant/classifyCategory blocks. Build every fixture with the existing
    makePol({ ... }) helper; add chamber_name / chamber_name_formal overrides only for the school-board
    chamber-text case. Use one test(...) per district_type base literal (or a single it.each table over
    [literal, expected] pairs) covering all 14 literals from the RESEARCH District Type Enumeration. Use
    it.each for the DA/PD title list under BOTH 'COUNTY' and 'LOCAL_EXEC' bases, and a second it.each for the
    'Attorney General' / 'City Attorney' negative guards. Add explicit superintendent positive + negative
    cases, the Portland-ME LOCAL-mistyped school-board title AND chamber cases, the two additive-only
    invariant cases, and the three null-safety cases. Do NOT implement classifyBucket in this task — the
    tests must reference the not-yet-exported function so the suite is RED.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npx vitest run src/lib/classify.test.js -t classifyBucket</automated>
    Expected: the new classifyBucket describe block executes and its assertions FAIL (function not yet
    implemented) — RED confirmed. This is a failing-assertion run, not a syntax/parse error.
  </verify>
  <acceptance_criteria>
    - src/lib/classify.test.js contains `describe('classifyBucket'`
    - The import on line 7 includes `classifyBucket`
    - Running `npx vitest run src/lib/classify.test.js -t classifyBucket` executes the block and reports failing assertions (RED), not a collection/parse error
    - Existing computeVariant / classifyCategory tests are untouched and still pass
  </acceptance_criteria>
  <done>RED state confirmed: comprehensive classifyBucket tests exist and fail because the function is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement classifyBucket in classify.js (GREEN)</name>
  <files>src/lib/classify.js</files>
  <read_first>
    - src/lib/classify.js — classifyCategory field-read idiom (lines 105-108), JUDICIAL branch precedent (228-240), catch-all (247), computeVariant regex style (316-332), hasAny (1-6)
    - .planning/phases/207-officials-classification/207-RESEARCH.md — "Recommended classifyBucket implementation shape" (draft with the exact override regexes), "District Type Enumeration" (SCHOOL_BOARD correction), Common Pitfalls 1/3/5
    - .planning/phases/207-officials-classification/207-PATTERNS.md — the classify.js analog section (Set-vs-if-chain, regex-not-hasAny guidance, where to insert)
    - .planning/phases/207-officials-classification/207-CONTEXT.md — D-01..D-10
  </read_first>
  <action>
    Add a single new exported function classifyBucket(pol) returning the string enum
    'representative' | 'educator' | 'judge', inserted directly below classifyCategory (after line 248) or below
    computeVariant (after line 332) — do NOT intersperse inside classifyCategory. Read pol?.district_type,
    pol?.office_title, and pol?.chamber_name_formal || pol?.chamber_name with the file's `|| ''` defaulting
    idiom (this is what makes a null/missing row fall safely to the catch-all — D-09 / T-207-01). Define the
    override regex consts at module scope above the function, mirroring computeVariant's style.
    BASE (D-07, decided first so a clean row is never pulled out — D-08): a JUDGE_DISTRICT_TYPES Set containing
    'JUDICIAL' and 'NATIONAL_JUDICIAL' returns 'judge'; an EDUCATOR_DISTRICT_TYPES Set containing 'SCHOOL',
    'STATE_BOARD', and 'SCHOOL_BOARD' returns 'educator'. SCHOOL_BOARD is the live-DB correction to D-04's
    literal list (DC's 9 State Board of Education rows) — omitting it violates SC-02; it is mandatory, not
    optional. ADDITIVE OVERRIDES (only reachable when the base is still representative): a whitelist regex
    matching district attorney | county attorney | prosecuting attorney | state's-attorney (apostrophe
    optional) | city prosecutor | public defender (word-boundaried, case-insensitive) -> 'judge' (D-02) — it
    must NOT match 'Attorney General' or 'City Attorney' (Pitfall 3); a judge|justice word-boundaried regex ->
    'judge' (D-03); a superintendent-of-(public instruction|schools) regex -> 'educator' (D-05, guarded so it
    does NOT catch superintendent of police / public works / streets — Pitfall 5); a school board | board of
    education regex tested against BOTH title and chamber -> 'educator' (D-04, rescues the Portland-ME
    LOCAL-mistyped board). Final line returns 'representative' (D-09 catch-all). Use the exact regex forms
    given in RESEARCH.md's recommended-shape draft. Do NOT import from groupHierarchy.js or branchType.js. Do
    NOT modify classifyCategory or computeVariant.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npx vitest run src/lib/classify.test.js -t classifyBucket</automated>
    Expected: exit 0 — all classifyBucket tests from Task 1 pass (GREEN).
  </verify>
  <acceptance_criteria>
    - src/lib/classify.js contains `export function classifyBucket(`
    - The EDUCATOR district_type set contains the literal 'SCHOOL_BOARD'
    - classifyBucket({ district_type: 'SCHOOL_BOARD' }) === 'educator'
    - classifyBucket({ district_type: 'COUNTY', office_title: 'District Attorney' }) === 'judge'
    - classifyBucket({ district_type: 'LOCAL_EXEC', office_title: 'Public Defender' }) === 'judge'
    - classifyBucket({ district_type: 'STATE_EXEC', office_title: 'Attorney General' }) === 'representative'
    - classifyBucket({ district_type: 'LOCAL_EXEC', office_title: 'City Attorney' }) === 'representative'
    - classifyBucket({ district_type: 'STATE_EXEC', office_title: 'Superintendent of Public Instruction' }) === 'educator'
    - classifyBucket({ district_type: 'LOCAL_EXEC', office_title: 'Superintendent of Police' }) === 'representative'
    - classifyBucket({ district_type: '', office_title: 'Circuit Judge' }) === 'judge' (D-03 title-fallback fires on missing/mistyped district_type)
    - classifyBucket({ district_type: 'SCHOOL', office_title: 'Board Judge' }) === 'educator' (additive-only, D-08)
    - classifyBucket(null) === 'representative' (no throw)
    - `npx vitest run src/lib/classify.test.js -t classifyBucket` exits 0; `npm test` full suite stays green (computeVariant/classifyCategory unaffected)
  </acceptance_criteria>
  <done>GREEN: classifyBucket implemented as the single source of truth; all its unit tests pass; the pre-existing suite is unbroken.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-location fixtures verification (SC-05 / D-11)</name>
  <files>src/lib/classify.test.js</files>
  <read_first>
    - src/lib/classify.test.js — makePol helper, existing describe/it.each conventions
    - .planning/phases/207-officials-classification/207-RESEARCH.md — "Verification Approach" (the 3-location plan + Pitfall 4 on why LA alone is insufficient), "District Type Enumeration" (real verified politician names / district_types)
    - .planning/phases/207-officials-classification/207-CONTEXT.md — D-11
  </read_first>
  <action>
    Append a describe('classifyBucket — live location fixtures (SC-05)', ...) block using the real,
    live-DB-verified office-holder field values documented in 207-RESEARCH.md for three contrasting locations,
    built with makePol. LA (CA): a LAUSD board-member row (district_type 'SCHOOL') -> 'educator'; LA County
    District Attorney 'Nathan Hochman' (district_type 'COUNTY') -> 'judge'; representative rows (Mayor, City
    Council member, a CA state legislator, a US House delegate) -> 'representative'. Bloomington / Monroe
    County (IN) — the true JUDICIAL base case LA cannot exercise (Pitfall 4): a Monroe Circuit/Superior Court
    judge row (district_type 'JUDICIAL') -> 'judge'; a Monroe County school-corporation row (district_type
    'SCHOOL') -> 'educator'. AZ city (Marana / Oro Valley / Sahuarita — reps-only statewide): each state/local
    row -> 'representative', AND a nationwide SCOTUS row (district_type 'NATIONAL_JUDICIAL') -> 'judge'
    (true-positive check — every US address receives the 9 SCOTUS rows). Plus the two live-data-correction
    guards: DC State Board of Education member (district_type 'SCHOOL_BOARD', title 'SBOE Member (Ward N)') ->
    'educator'; SF District Attorney 'Brooke Jenkins' (district_type 'LOCAL_EXEC') -> 'judge'. Keep every
    assertion inside the automated test. As a NON-blocking confidence step, the executor MAY run a read-only
    live spot check against the production search / browse_geo_id flow for one location and record observed
    bucket counts in the SUMMARY — no DB writes, no reliance on it for the gate.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm test</automated>
    Expected: exit 0 — full suite green including the new live-location fixtures block.
  </verify>
  <acceptance_criteria>
    - src/lib/classify.test.js contains `describe('classifyBucket — live location fixtures`
    - LA assertions present: SCHOOL -> 'educator', COUNTY DA -> 'judge', ordinary reps -> 'representative'
    - Bloomington/Monroe IN assertions present: JUDICIAL -> 'judge', SCHOOL -> 'educator'
    - AZ-city assertions present: state/local rows -> 'representative', NATIONAL_JUDICIAL SCOTUS -> 'judge'
    - DC SCHOOL_BOARD -> 'educator' and SF LOCAL_EXEC DA -> 'judge' guards present
    - `npm test` exits 0
  </acceptance_criteria>
  <done>SC-05 satisfied: classification verified via automated real-data fixtures across 3 contrasting locations (LA / Bloomington IN / AZ city) plus the DC and SF live-data-correction guards; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| already-fetched API response row -> classifyBucket(pol) | The `pol` object is upstream-trusted data (from the app's own authenticated officials fetch), but individual rows may have missing / null / empty `district_type`, `office_title`, or `chamber_name` fields. This is the only boundary classifyBucket sits on: no network I/O, no DB access, no auth, no user-supplied-string parsing, no rendering of untrusted text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-207-01 | Denial of Service | classifyBucket(pol) robustness | mitigate | Null-safe field access (`pol?.district_type || ''`, `pol?.office_title || ''`, `pol?.chamber_name_formal || pol?.chamber_name || ''`) so a malformed / partial / null row returns 'representative' instead of throwing and breaking the officials-list render (D-09 guarantee). Verified by the null-safety unit tests in Task 1 (classifyBucket(null) / ({}) / ({district_type:null}) -> 'representative', no throw). |
| T-207-02 | Tampering (misclassification) | override regexes (D-02 / D-05) | mitigate | Whitelist / phrase-anchored regexes (not a broad /attorney/ or bare /superintendent/) plus additive-only precedence (D-07/D-08) prevent a mistyped or adversarially-worded title from misfiling a cleanly-typed row or sweeping a civil office (Attorney General, City Attorney, Superintendent of Police) into the wrong bucket. Verified by the negative-guard unit tests (Pitfall 3 / Pitfall 5) in Task 1. |
| T-207-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs no packages (Vitest is already configured). No supply-chain vector introduced; no legitimacy checkpoint required. |

**Spoofing / Repudiation / Information Disclosure / Elevation of Privilege:** Not applicable. classifyBucket is a
pure, read-only enum-returning function with no identity, logging, secret, data-egress, or privilege surface.
No high-severity threats exist for this phase (consistent with ASVS L1 for a side-effect-free client-side
transform).
</threat_model>

<verification>
- `npx vitest run src/lib/classify.test.js -t classifyBucket` — targeted, exits 0 after Task 2.
- `npm test` — full suite green after Task 3 (no regression to computeVariant / classifyCategory).
- SC-01: every base district_type literal + catch-all resolves to exactly one of the 3 buckets (Task 1/2 base-case tests).
- SC-02: SCHOOL / STATE_BOARD / SCHOOL_BOARD + LOCAL-mistyped school board + superintendent -> educator (never left in reps).
- SC-03: JUDICIAL / NATIONAL_JUDICIAL + DA/PD override + title-judge -> judge (never left in reps).
- SC-04: Attorney General / City Attorney / non-education superintendent / ordinary reps -> representative (never misfiled).
- SC-05: LA / Bloomington-IN / AZ-city live fixtures (Task 3).
</verification>

<success_criteria>
- `src/lib/classify.js` exports `classifyBucket(pol)` returning 'representative' | 'educator' | 'judge', reading district_type/office_title/chamber directly, additive-only, null-safe.
- EDUCATOR base set includes SCHOOL_BOARD; DA/PD whitelist excludes Attorney General and City Attorney; superintendent guard excludes non-education titles.
- `classifyCategory` and `computeVariant` are unchanged.
- `npm test` exits 0 with the new classifyBucket edge-case block and live-location-fixtures block both green.
</success_criteria>

<output>
Create `.planning/phases/207-officials-classification/207-01-SUMMARY.md` when done.
</output>
