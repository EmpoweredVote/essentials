---
phase: 213-anonymous-coordinate-lookup-endpoint
plan: 02
type: execute
wave: 2
depends_on: ["213-01"]
files_modified:
  - C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.ts
  - C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.test.ts
  - C:/EV-Accounts/backend/src/index.ts
autonomous: true
requirements: [RSLV-03]

must_haves:
  truths:
    - "POST /api/essentials/coordinate-lookup with a valid US { lat, lng } body returns 200 with an AddressSearchResult (officials), no auth required"
    - "Out-of-box coordinates return 422 with code OUTSIDE_US_BOUNDS"
    - "Swapped lat/lng return 422 with code SWAPPED_COORDINATES"
    - "Non-numeric / missing coordinates return 422 with code INVALID_COORDINATES"
    - "The 200 response carries no raw lat/lng and matchedAddress is empty; no handler logs the coordinate"
    - "The route is mounted before the /api/essentials catch-all router"
  artifacts:
    - path: "C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.ts"
      provides: "POST coordinate-lookup handler wiring classifyCoordinate → 422 taxonomy → getRepresentativesByCoordinate"
      contains: "router.post('/'"
    - path: "C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.test.ts"
      provides: "Supertest coverage of the 200 path + all three 422 codes + no-echo assertion"
    - path: "C:/EV-Accounts/backend/src/index.ts"
      provides: "Route mount at /api/essentials/coordinate-lookup before the essentials catch-all"
      contains: "coordinate-lookup"
  key_links:
    - from: "essentialsCoordinateLookup.ts"
      to: "classifyCoordinate"
      via: "import from lib/coordinateValidation.js → 422 code passthrough"
      pattern: "classifyCoordinate"
    - from: "essentialsCoordinateLookup.ts"
      to: "getRepresentativesByCoordinate"
      via: "import from lib/essentialsService.js"
      pattern: "getRepresentativesByCoordinate"
    - from: "index.ts"
      to: "essentialsCoordinateLookupRouter"
      via: "app.use before essentialsRouter catch-all"
      pattern: "coordinate-lookup"
---

<objective>
Expose the Plan 01 building blocks as the anonymous HTTP endpoint (D-01): `POST /api/essentials/coordinate-lookup` accepting a JSON body `{ lat, lng }` (POST body, NOT query string, so coordinates stay out of Render/proxy access logs per Criterion 3). The handler classifies the coordinate, maps rejections to the three distinct `422` codes (D-07), and on a valid US point returns the officials from `getRepresentativesByCoordinate` — with zero auth, zero DB writes, and no coordinate echo/logging.

Purpose: Turn the tested logic into the live contract Phase 214's combobox will consume, mirroring the Phase 212 dedicated-router pattern (`essentialsLocationSearch.ts`).
Output: New route file + supertest suite + index.ts mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-CONTEXT.md
@.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-01-PLAN.md

Cross-repo reality: code lives in `C:/EV-Accounts`; commit via `git -C "C:/EV-Accounts" ...`. No Supabase MCP — DB checks run inline via psql/curl (live smoke test is Plan 03, not here).

<interfaces>
From Plan 01 (C:/EV-Accounts/backend/src/lib/coordinateValidation.ts):
  classifyCoordinate(lat, lng) → { ok: true } | { ok: false; code: 'OUTSIDE_US_BOUNDS' | 'SWAPPED_COORDINATES' | 'INVALID_COORDINATES' }

From Plan 01 (C:/EV-Accounts/backend/src/lib/essentialsService.ts):
  getRepresentativesByCoordinate(lat, lng): Promise<AddressSearchResult>

Precedents to mirror:
  C:/EV-Accounts/backend/src/routes/essentialsLocationSearch.ts — dedicated public router, optionalAuth, 422 { code, message } envelope, default export mounted in index.ts.
  C:/EV-Accounts/backend/src/routes/essentials.ts:139-155 — GET /elections 422 VALIDATION_ERROR pattern.
  C:/EV-Accounts/backend/src/routes/events.ts:16-26 — express-rate-limit config (windowMs/max/keyGenerator on req.ip) applied to an anonymous POST.
  C:/EV-Accounts/backend/src/index.ts:146-158 — the /api/essentials/* mount block; location-search (line 153) is mounted BEFORE the essentials catch-all (line 158).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /api/essentials/coordinate-lookup route + index.ts mount</name>
  <files>C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.ts, C:/EV-Accounts/backend/src/index.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/src/routes/essentialsLocationSearch.ts (full — dedicated-router shape, optionalAuth, 422 envelope, default export)
    - C:/EV-Accounts/backend/src/routes/essentials.ts (lines 139-155: 422 code/message envelope to match)
    - C:/EV-Accounts/backend/src/routes/events.ts (lines 1-30: express-rate-limit import + config pattern for an anonymous POST)
    - C:/EV-Accounts/backend/src/index.ts (lines 30-40 imports block, lines 146-158 the /api/essentials mount ordering)
    - C:/EV-Accounts/backend/src/lib/coordinateValidation.ts and C:/EV-Accounts/backend/src/lib/essentialsService.ts (the Plan 01 exports being wired)
  </read_first>
  <action>
    Create `essentialsCoordinateLookup.ts` exporting a default Router with a single `router.post('/', optionalAuth, <limiter>, handler)`. The handler:
      - Reads `lat` and `lng` from `req.body` (JSON body — D-01; the express json body parser is already applied app-wide). Coerce each with `Number(...)`; do NOT read from `req.query`.
      - Calls `classifyCoordinate(lat, lng)`. On `{ ok: false }` respond `res.status(422).json({ code: result.code, message: <specific message> })` — the `code` MUST be the exact `OUTSIDE_US_BOUNDS` / `SWAPPED_COORDINATES` / `INVALID_COORDINATES` string (D-07), with a message the Phase 214 frontend can surface.
      - On `{ ok: true }` call `getRepresentativesByCoordinate(lat, lng)` and `res.status(200).json(result)`.
      - Wrap in try/catch → `500 { code: 'INTERNAL_ERROR', message: '...' }`.
      - PRIVACY (Criterion 3 / D-06 / D-08): NEVER log the coordinate. Any `console.error` in the catch logs only `(err as Error).message` — never `req.body`, `lat`, or `lng`. Emit NO analytics/telemetry event carrying the coordinate. The 422 messages must NOT echo the submitted numbers back.
      - Apply a modest `express-rate-limit` limiter (planner discretion, D-07 note — cheap abuse protection on an anonymous route): windowMs 60_000, max ~30, keyGenerator on `req.ip`, standardHeaders true. Mirror events.ts.
    Choose the exact route base path `coordinate-lookup`. Mount in `index.ts`: import the router (e.g. `essentialsCoordinateLookupRouter`) and `app.use('/api/essentials/coordinate-lookup', essentialsCoordinateLookupRouter)` placed with the other specific `/api/essentials/*` mounts, BEFORE the `/api/essentials` catch-all (`essentialsRouter`).
    No fenced code in comments. No new dependency (`express-rate-limit` already installed).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit && grep -n "coordinate-lookup" src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean.
    - `grep -c "router.post('/'" src/routes/essentialsCoordinateLookup.ts` == 1.
    - `grep -c "classifyCoordinate\|getRepresentativesByCoordinate" src/routes/essentialsCoordinateLookup.ts` ≥ 2 (both Plan 01 exports wired).
    - `grep -n "coordinate-lookup" src/index.ts` shows the mount, and it appears at a lower line number than the `app.use('/api/essentials', essentialsRouter)` catch-all.
    - `grep -v '^\s*//' src/routes/essentialsCoordinateLookup.ts | grep -c "req.body\b.*console\|console.*lat\|console.*lng\|console.*req.body"` == 0 (no coordinate/body logging).
    - Handler reads from `req.body`, not `req.query`: `grep -c "req.query" src/routes/essentialsCoordinateLookup.ts` == 0.
  </acceptance_criteria>
  <done>POST /api/essentials/coordinate-lookup is defined, wired to classifyCoordinate + getRepresentativesByCoordinate, rate-limited, mounted before the essentials catch-all, and logs no coordinate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supertest route suite — 200 path, three 422 codes, no-echo assertion</name>
  <files>C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.test.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/src/routes/essentialsLocationSearch.test.ts (the existing route-test harness: how the app/router is mounted for supertest, how the DB/service is stubbed or hit)
    - C:/EV-Accounts/backend/src/routes/essentialsCoordinateLookup.ts (the handler under test, from Task 1)
  </read_first>
  <behavior>
    - POST { lat: 39.17, lng: -86.52 } (Bloomington, IN — real geo-linked point) → 200; body.politicians is a non-empty array; body.matchedAddress === ''; JSON.stringify(body) contains neither "39.17" nor "-86.52" (no coordinate echo).
    - POST { lat: -86.52, lng: 39.17 } (Bloomington swapped) → 422; body.code === 'SWAPPED_COORDINATES'.
    - POST { lat: 51.5, lng: -0.12 } (London) → 422; body.code === 'OUTSIDE_US_BOUNDS'.
    - POST { lat: "abc", lng: null } (malformed) → 422; body.code === 'INVALID_COORDINATES'.
    - POST {} (missing both) → 422; body.code === 'INVALID_COORDINATES'.
  </behavior>
  <action>
    Write a supertest suite following the harness in `essentialsLocationSearch.test.ts`. Mount the coordinate-lookup router on a minimal express app with `express.json()`. If that harness stubs the service layer, stub `getRepresentativesByCoordinate` to return a fixture AddressSearchResult with a non-empty politicians array and `matchedAddress: ''` for the 200 case (the real DB round-trip is exercised live in Plan 03). Assert exact status codes and `code` strings per <behavior>, and assert the serialized 200 body contains neither submitted coordinate value (privacy no-echo). Write tests RED first where practical, then confirm GREEN.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx vitest run src/routes/essentialsCoordinateLookup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/routes/essentialsCoordinateLookup.test.ts` passes with all five <behavior> cases.
    - The swap case asserts `code === 'SWAPPED_COORDINATES'` (distinct from OUTSIDE_US_BOUNDS) — proving D-03 surfaces through the route.
    - The 200 case asserts the response body contains neither the submitted lat nor lng string (no-echo, Criterion 3) and `matchedAddress === ''`.
  </acceptance_criteria>
  <done>Route suite green: valid point → 200 non-empty officials with no coordinate echo; each of the three 422 codes proven through the HTTP layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /coordinate-lookup | Anonymous, unauthenticated public HTTP; untrusted JSON body |
| handler → access logs / proxy | Render + proxy may log request lines; POST body is not logged by default |
| handler → error response | 422/500 responses must not reflect input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-05 | Information Disclosure | Render/proxy access logs | mitigate | POST body transport (D-01) keeps coordinates out of URL/query access logs; handler logs only error messages, never req.body/lat/lng (D-06/D-08); 422 messages do not echo input |
| T-213-06 | Denial of Service | anonymous POST route | mitigate | express-rate-limit (windowMs 60s, max ~30, keyed on req.ip); classifyCoordinate short-circuits invalid input before any DB query |
| T-213-07 | Tampering | JSON body coercion | mitigate | Number() coercion + classifyCoordinate reject non-finite/out-of-range as 422 INVALID_COORDINATES before reaching PostGIS parameters |
| T-213-08 | Repudiation | no-op — read-only endpoint | accept | endpoint performs no writes; nothing to repudiate; zero-write proven in Plan 03 |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; both vitest suites green.
- Route mounted before the essentials catch-all; reads req.body not req.query; logs no coordinate.
</verification>

<success_criteria>
- POST valid US { lat, lng } → 200 officials, no auth, no coordinate echo.
- Out-of-box / swapped / malformed → 422 OUTSIDE_US_BOUNDS / SWAPPED_COORDINATES / INVALID_COORDINATES respectively.
</success_criteria>

<output>
Create `.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-02-SUMMARY.md` when done.
</output>
