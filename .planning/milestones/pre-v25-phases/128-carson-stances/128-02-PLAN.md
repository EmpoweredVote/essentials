---
phase: 128-carson-stances
plan: 02
type: execute
wave: 2
depends_on: [128-01]
files_modified:
  - C:/EV-Accounts/backend/migrations/721_dear_stances.sql
  - C:/EV-Accounts/backend/migrations/722_hicks_stances.sql
  - C:/EV-Accounts/backend/migrations/723_rojas_stances.sql
autonomous: true
requirements: [CARSON-01]
must_haves:
  truths:
    - "Jim Dear (external_id -700302) has evidence-only stance rows written, applied, and verified — long-tenured member, richest expected record among D1-D4"
    - "Cedric L. Hicks Sr. (external_id -700303) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Arleen B. Rojas (external_id -700304) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
    - "Khaleah K. Bradshaw (-700305) and Monica Cooper (-700306) receive ZERO stance rows in any of these migrations"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/721_dear_stances.sql"
      provides: "Jim Dear stance migration"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/722_hicks_stances.sql"
      provides: "Cedric L. Hicks Sr. stance migration"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/723_rojas_stances.sql"
      provides: "Arleen B. Rojas stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Research and apply evidence-only compass stances for the final 3 Carson officials (Jim Dear D2, Cedric L. Hicks Sr. D3, Arleen B. Rojas D4), one politician at a time. Blocked on Plan 01 (requires confirmed migration numbers and all 5 UUIDs from 128-01-SUMMARY.md).

Purpose: Complete all 5 Carson officials' stance research, closing the data work for CARSON-01. Unblocks Plan 03 phase-wide closure.
Output: 3 per-individual stance migration files (721 through 723), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-carson-stances/128-RESEARCH.md
@.planning/phases/128-carson-stances/128-01-SUMMARY.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 718_wells_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs (Phase 115 ledger-consistency precedent)
- CRITICAL: Khaleah K. Bradshaw (-700305) and Monica Cooper (-700306) are EXCLUDED. NEVER write any INSERT with their UUIDs.
</interfaces>

Apply path: psql CLI with DATABASE_URL from C:/EV-Accounts/backend/.env (NOT C:/EV-Accounts/.env). Load DATABASE_URL first. Do NOT run git commands in C:/EV-Accounts.

Migration numbers for this plan (from Plan 01 confirmed NNN=719):
  -700302 Jim Dear (D2)          → 721 (NNN+2)
  -700303 Cedric L. Hicks Sr. (D3) → 722 (NNN+3)
  -700304 Arleen B. Rojas (D4)   → 723 (NNN+4)

All 5 target UUIDs and exclusion UUIDs are in 128-01-SUMMARY.md — read that file first before writing any SQL.

Out-of-scope guard: Do NOT write stances for any external_id outside the target set {-700300, -700301, -700302, -700303, -700304}. NEVER write for -700305 (Bradshaw) or -700306 (Cooper). Do NOT create any new offices, districts, or chambers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jim Dear (District 2) stances — migration 721</name>
  <files>C:/EV-Accounts/backend/migrations/721_dear_stances.sql</files>
  <read_first>
    - .planning/phases/128-carson-stances/128-01-SUMMARY.md (confirmed NNN, all 5 UUIDs including Dear's UUID, exclusion UUIDs)
    - .planning/phases/128-carson-stances/128-RESEARCH.md (Jim Dear section + Source Map + Anti-Patterns + Pitfalls 1-11)
    - C:/EV-Accounts/backend/migrations/720_hilton_stances.sql (most recent sibling file as in-phase format reference)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process Jim Dear (external_id -700302, UUID from 128-01-SUMMARY.md, Council Member District 2) as first task in this plan. Research, write, apply, and verify before moving to Task 2.

    (a) Research: Search "Jim Dear Carson council" and "Jim Dear Carson District 2" on carsonca.gov/government/city-council/city-council-agendas-and-minutes, dailybreeze.com, latimes.com, carson.patch.com, ballotpedia.org/Jim_Dear. Jim Dear is a long-tenured council member who has also served as Carson Mayor in earlier rotation cycles — expected yield 6–10 stances, likely the richest record among D1-D4. High-probability topics: housing (housing element compliance votes), homelessness-response (well-documented positions on encampment enforcement), public-safety-approach (vocal on police issues), economic-development (Amazon facility debates, Carson Marketplace), local-environment (refinery/air quality issues), growth-and-development (development project votes over long tenure), taxes (city budget and tax measure positions). Place each topic with direct evidence on the 1.0-5.0 scale. No INSERT for any topic with no direct evidence. If zero evidence found, write empty migration (ledger-consistency).

    (b) Write migration 721_dear_stances.sql:
    - Header: "Migration 721: Jim Dear Stances"
    - Full 44-topic UUID reference block from 128-RESEARCH.md (or live list from Task 1 if count differed)
    - BEGIN;
    - paired politician_answers + politician_context INSERT per evidenced topic
    - value = float literal; sources = ARRAY[...]::text[]::text[] double-cast, path-bearing URLs; reasoning = $$...$$ dollar-quoting
    - Reasoning text uses "Council Member Dear"
    - COMMIT;

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/721_dear_stances.sql`.

    (d) Verify immediately:
    Row count: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{dear_uuid}';"`
    Unpaired check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{dear_uuid}' AND pc.politician_id IS NULL;"` — must return 0
    Citation check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{dear_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Dear's UUID. Citation check returns 0 for Dear's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file 721_dear_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Dear's UUID
    - Uncited-context query returns 0 for Dear's UUID
    - Reasoning text uses "Council Member Dear"
    - No INSERT references excluded officials or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration 721_dear_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Jim Dear.</done>
</task>

<task type="auto">
  <name>Task 2: Cedric L. Hicks Sr. (District 3) stances — migration 722</name>
  <files>C:/EV-Accounts/backend/migrations/722_hicks_stances.sql</files>
  <read_first>
    - .planning/phases/128-carson-stances/128-01-SUMMARY.md (Hicks's UUID + exact stored full_name including "Sr." suffix)
    - .planning/phases/128-carson-stances/128-RESEARCH.md (Cedric L. Hicks Sr. section + Pitfalls)
    - C:/EV-Accounts/backend/migrations/721_dear_stances.sql (just-written sibling file as format reference)
  </read_first>
  <action>
    Process Cedric L. Hicks Sr. (external_id -700303, UUID from 128-01-SUMMARY.md, Council Member District 3) strictly after Task 1 completes — never in parallel (rate-limit rule). Research, write, apply, and verify.

    (a) Research: Search "Cedric Hicks Carson council" and "Cedric Hicks Carson District 3" on carsonca.gov/government/city-council/city-council-agendas-and-minutes, dailybreeze.com, precinctreporter.com, carson.patch.com. NOTE: his stored full_name includes "Sr." suffix — use exactly as stored (from 128-01-SUMMARY.md) in the politician_id UUID lookup. Expected yield 4–7 stances. High-probability topics: housing (D3 rezoning), homelessness-response (Carson-wide policy votes), public-safety-approach (Carson PD funding/oversight), economic-development (D3 commercial development), local-environment (industrial boundary issues). Place each evidenced topic on the 1.0-5.0 scale. No INSERT for any topic with no direct evidence. If zero evidence, write empty migration (ledger-consistency) and note in SUMMARY.

    (b) Write migration 722_hicks_stances.sql:
    - Header: "Migration 722: Cedric L. Hicks Sr. Stances"
    - Full 44-topic UUID reference block
    - BEGIN;
    - paired politician_answers + politician_context INSERT per evidenced topic
    - value = float literal; sources double-cast; reasoning = $$...$$ dollar-quoting
    - Reasoning text uses "Council Member Hicks"
    - COMMIT;

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/722_hicks_stances.sql`.

    (d) Verify immediately, substituting Hicks's UUID:
    Row count, Unpaired check, Citation check — same pattern as Tasks 1/2 in Plan 01. All must return 0 for paired/citation checks.
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Hicks's UUID. Citation check returns 0 for Hicks's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file 722_hicks_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Hicks's UUID
    - Uncited-context query returns 0 for Hicks's UUID
    - Reasoning text uses "Council Member Hicks"
    - No INSERT references excluded officials or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration 722_hicks_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Cedric L. Hicks Sr.</done>
</task>

<task type="auto">
  <name>Task 3: Arleen B. Rojas (District 4) stances — migration 723</name>
  <files>C:/EV-Accounts/backend/migrations/723_rojas_stances.sql</files>
  <read_first>
    - .planning/phases/128-carson-stances/128-01-SUMMARY.md (Rojas's UUID + exact stored full_name including middle initial "B.")
    - .planning/phases/128-carson-stances/128-RESEARCH.md (Arleen B. Rojas section + Pitfalls)
    - C:/EV-Accounts/backend/migrations/722_hicks_stances.sql (just-written sibling file as format reference)
  </read_first>
  <action>
    Process Arleen B. Rojas (external_id -700304, UUID from 128-01-SUMMARY.md, Council Member District 4) strictly after Task 2 completes — never in parallel (rate-limit rule). Research, write, apply, and verify.

    (a) Research: Search "Arleen Rojas Carson council" and "Arleen Rojas Carson District 4" on carsonca.gov/government/city-council/city-council-agendas-and-minutes, dailybreeze.com, carson.patch.com. NOTE: her stored full_name is "Arleen B. Rojas" — use UUID from 128-01-SUMMARY.md. Expected yield 4–7 stances. High-probability topics: housing/residential-zoning (southern Carson residential density), homelessness-response (D4 encampment proximity), local-environment (D4 industrial areas), public-safety-approach (police votes), economic-development (southern industrial/commercial corridor). Place each evidenced topic on the 1.0-5.0 scale. No INSERT for any topic with no direct evidence. If zero evidence, write empty migration (ledger-consistency) and note in SUMMARY.

    (b) Write migration 723_rojas_stances.sql:
    - Header: "Migration 723: Arleen B. Rojas Stances"
    - Full 44-topic UUID reference block
    - BEGIN;
    - paired politician_answers + politician_context INSERT per evidenced topic
    - value = float literal; sources double-cast; reasoning = $$...$$ dollar-quoting
    - Reasoning text uses "Council Member Rojas"
    - COMMIT;

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/723_rojas_stances.sql`.

    (d) Verify immediately, substituting Rojas's UUID:
    Row count, Unpaired check, Citation check — same pattern as prior tasks. All must return 0 for paired/citation checks.
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Rojas's UUID. Citation check returns 0 for Rojas's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file 723_rojas_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Rojas's UUID
    - Uncited-context query returns 0 for Rojas's UUID
    - Reasoning text uses "Council Member Rojas"
    - No INSERT references excluded officials or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration 723_rojas_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Arleen B. Rojas.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (Plan 02)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-P2-01 | Tampering | stance value integrity | mitigate | Evidence-only rule; per-person uncited-context query must return 0 |
| T-128-P2-02 | Repudiation | stance provenance | mitigate | 100% citation rate: every value paired with context row + path-bearing source URL |
| T-128-P2-03 | Tampering | excluded officials in Plan 02 migrations | mitigate | Acceptance criteria assert no INSERT references Bradshaw (-700305) or Cooper (-700306) UUIDs |
| T-128-P2-04 | Tampering | "Sr." suffix name mismatch | mitigate | Hicks UUID from SUMMARY (not derived from name); SUMMARY records exact stored full_name |
</threat_model>

<verification>
After all three tasks, run partial phase-wide citation check for Plan 02 officials (Dear + Hicks + Rojas):
`psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-700302, -700303, -700304)) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` must return 0.
</verification>

<success_criteria>
- Migrations 721, 722, 723 (3 officials: Dear, Hicks, Rojas) written, applied, and verified
- 0 unpaired answers, 0 uncited contexts across all 3 officials
- No INSERT references Bradshaw (-700305) or Cooper (-700306)
- No external_ids outside the 5-target set touched
- Blank-spoke topics documented in SUMMARY (never defaulted to 3.0)
- All 5 Carson officials' stances now applied (combined with Plan 01)
</success_criteria>

<output>
Create `.planning/phases/128-carson-stances/128-02-SUMMARY.md` when done. Record: per-official stance counts for Dear/Hicks/Rojas, any blank-spoke topics, and confirmation that no excluded official received stances.
</output>
