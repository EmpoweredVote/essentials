---
phase: 124-ma-tier3-stances-wave3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/659_coogan_stances.sql
  - C:/EV-Accounts/backend/migrations/660_ponte_stances.sql
  - C:/EV-Accounts/backend/migrations/661_dionne_stances.sql
  - C:/EV-Accounts/backend/migrations/662_hart_stances.sql
  - C:/EV-Accounts/backend/migrations/663_camara_stances.sql
  - C:/EV-Accounts/backend/migrations/664_pereira_stances.sql
  - C:/EV-Accounts/backend/migrations/665_raposo_stances.sql
  - C:/EV-Accounts/backend/migrations/666_cadime_stances.sql
  - C:/EV-Accounts/backend/migrations/667_canuel_stances.sql
  - C:/EV-Accounts/backend/migrations/668_peckham_stances.sql
autonomous: true
requirements: [FALLRIV-03]
must_haves:
  truths:
    - "Wave 0 pre-flight confirms next migration is 659, active topic count is 44, and Fall River UUID mapping resolves for all 10 officials before any stance is written"
    - "Fall River Mayor Paul Coogan has evidence-only stance rows (or documented blank if no public record)"
    - "All 9 Fall River at-large councillors have evidence-only stance rows or documented blanks"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/659_coogan_stances.sql"
      provides: "Mayor Coogan stance migration (or zero-stance ledger file)"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/668_peckham_stances.sql"
      provides: "At-Large Peckham stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Run Wave 0 pre-flight verification, then research and apply evidence-only compass stances for all 10 Fall River officials (Mayor Coogan + 9 at-large City Councilors), one politician at a time.

Purpose: Establish the verified migration starting point and topic reference for the entire phase, and fully close FALLRIV-03.
Output: 10 per-individual stance migration files (659–668), each applied to production and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-RESEARCH.md
@.planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 574_boston_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs (Phase 115 ledger-consistency precedent)
</interfaces>

Fall River roster (external_id → migration), from Phase 121 deep seed (migration 590, geo_id=2523000):
  -2523000001 Paul Coogan (Mayor)                      → 659
  -2523000002 Cliff Ponte (At-Large, Council President) → 660
  -2523000003 Michelle Dionne (At-Large, Council VP)    → 661
  -2523000004 Paul Hart (At-Large)                     → 662
  -2523000005 Joseph Camara (At-Large)                 → 663
  -2523000006 Linda Pereira (At-Large)                 → 664
  -2523000007 Andrew Raposo (At-Large)                 → 665
  -2523000008 Shawn Cadime (At-Large)                  → 666
  -2523000009 Michael Canuel (At-Large)                → 667
  -2523000010 Christopher Peckham (At-Large)           → 668

Out-of-scope guard: Do NOT write stances for any external_id outside the -2523000001 to -2523000010 range. FALLRIV-03 covers Mayor + City Council only.

Key Fall River context:
- Mayor Coogan is a Republican (first elected 2019, re-elected 2023); former TV weatherman; relatively strong public safety and economic development record
- Fall River is a post-industrial coastal city (formerly "Spindle City"); large Portuguese-American community
- Council is nominally nonpartisan but several members align with Democrats; Ponte is Council President 2026-2027
- Strong local topics: economic-development, growth-and-development, public-safety-approach, housing, residential-zoning, homelessness
- Fall River is NOT a sanctuary city; no known ICE/local-immigration resolution passed by council
- Coogan has a more conservative tilt on public safety; council members vary
- Peckham stored without "Sr." suffix per DB convention
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 pre-flight verification</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md (Wave 0 Pre-Flight Queries section and Topic UUID Reference Block)
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-RESEARCH.md (established research approach)
  </read_first>
  <action>
    Run four verification queries via mcp__supabase-local__execute_sql before writing any migration:
    (1) `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations;` — confirm the highest applied migration. Expected: 658 (Phase 123 last migration was 658_pereira_stances.sql). If MAX is 658, the next migration is 659. If MAX is higher, use MAX+1 and record the deviation in SUMMARY.
    (2) `SELECT COUNT(*) FROM inform.compass_topics WHERE is_active = true;` — expected 44. If different, run `SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;` and use that live list as the topic reference (do NOT trust the cached block in PATTERNS.md if count differs).
    (3) `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id BETWEEN -2523000010 AND -2523000001 ORDER BY external_id DESC;` — resolve UUIDs for ALL 10 Fall River officials and confirm name-to-external_id mapping matches the roster table above. Capture all 10 UUIDs for use in this plan.
    (4) `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2523000010 AND -2523000001);` — informational; upsert handles pre-existing rows.
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql with the active-topic count query returns a single integer; the MAX migration query returns 658 (or documents the actual value). UUIDs for all 10 Fall River officials are captured.</automated>
  </verify>
  <acceptance_criteria>
    - Starting migration number confirmed as 659 (or higher if DB shows a later applied migration)
    - Active compass topic count confirmed (44 expected; if not, live slug→id list captured)
    - UUIDs resolved for ALL 10 external_ids (-2523000001 through -2523000010) with names matching the roster
    - All 10 UUIDs recorded in SUMMARY for use in subsequent tasks
  </acceptance_criteria>
  <done>Pre-flight queries run; starting migration number, topic count, and all 10 Fall River UUIDs confirmed and recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Mayor Coogan + Ponte + Dionne (659–661)</name>
  <files>C:/EV-Accounts/backend/migrations/659_coogan_stances.sql, C:/EV-Accounts/backend/migrations/660_ponte_stances.sql, C:/EV-Accounts/backend/migrations/661_dionne_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format: BEGIN/COMMIT, float literals, dollar-quoting, per-person section header)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (authoritative on ::text[]::text[] double-cast)
    - .planning/phases/122-ma-tier-3-stances-wave-1/122-PATTERNS.md (Full Minimal Valid Migration Template, lines 291-351)
  </read_first>
  <action>
    Process ONE politician at a time, strictly sequentially — never run two research sessions in parallel (rate-limit rule). For each of Coogan (-2523000001, Mayor), Ponte (-2523000002, At-Large), Dionne (-2523000003, At-Large):
    (a) Research the official's public record across ALL active compass topics — campaign statements, votes, local news (Herald News, Fall River Reporter, WJAR, WPRI), fallriverma.org agenda/minutes, state records where applicable. Place each topic with direct evidence on the 1.0–5.0 scale (1.0 most progressive/left, 5.0 most conservative/right).
    (b) Write the migration file using the canonical template: value is a float literal (e.g. 4.0); sources is ARRAY[...]::text[]::text[]; wrap in BEGIN;/COMMIT;; reasoning in $$...$$ citing the specific vote/statement/source; include the 44-topic UUID reference block in the file header.
    (c) For any topic with NO direct evidence, write NO INSERT — never default to 3.0. If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; and zero INSERT blocks, and note in SUMMARY.
    (d) Apply via mcp__supabase-local__execute_sql, then immediately run both per-person verification queries before moving to the next official.
    Coogan context: Republican mayor; strong on economic development (waterfront redevelopment, Amazon fulfillment center), public safety (anti-crime initiatives), skeptical of sanctuary policies. Expect 8–15 stances. Ponte and Dionne: at-large councillors with multi-year records on zoning, budget, and city services.
    Do NOT touch any external_id outside the -2523000001 to -2523000010 range.
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: `SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{uuid}' AND pc.politician_id IS NULL;` returns 0, AND `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - 3 migration files exist (659, 660, 661), each wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`; no bare-integer value lines
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT references an external_id outside the -2523000001 to -2523000010 range
  </acceptance_criteria>
  <done>Migrations 659–661 written, applied, and verified (0 unpaired, 0 uncited) for Coogan, Ponte, Dionne.</done>
</task>

<task type="auto">
  <name>Task 3: Hart + Camara + Pereira (662–664)</name>
  <files>C:/EV-Accounts/backend/migrations/662_hart_stances.sql, C:/EV-Accounts/backend/migrations/663_camara_stances.sql, C:/EV-Accounts/backend/migrations/664_pereira_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format)
    - C:/EV-Accounts/backend/migrations/659_coogan_stances.sql (just-written sibling file as in-phase reference)
  </read_first>
  <action>
    Process ONE politician at a time, sequentially: Hart (-2523000004), Camara (-2523000005), Pereira (-2523000006). Same procedure as Task 2: research public record across all active topics, write canonical migration, omit topics with no evidence, apply, then verify before next official. These are at-large councillors with varying public profiles — Hart and Camara may have moderate records; Pereira (Linda, not to be confused with the New Bedford Pereira from Phase 123) is an at-large councillor. Blank spokes are acceptable and honest for thin-record members. Do NOT touch any external_id outside the -2523000001 to -2523000010 range.
  </action>
  <verify>
    <automated>For each of the 3 UUIDs: unpaired-answer query returns 0 AND uncited-context query returns 0 (same two queries as Task 2).</automated>
  </verify>
  <acceptance_criteria>
    - 3 migration files exist (662, 663, 664), each wrapped in BEGIN;/COMMIT;
    - Every value is a float literal; every sources array double-cast ::text[]::text[]
    - Unpaired-answer query returns 0 for all 3 UUIDs
    - Uncited-context query returns 0 for all 3 UUIDs
    - No INSERT references an external_id outside the -2523000001 to -2523000010 range
  </acceptance_criteria>
  <done>Migrations 662–664 written, applied, and verified for Hart, Camara, Pereira.</done>
</task>

<task type="auto">
  <name>Task 4: Raposo + Cadime + Canuel + Peckham (665–668)</name>
  <files>C:/EV-Accounts/backend/migrations/665_raposo_stances.sql, C:/EV-Accounts/backend/migrations/666_cadime_stances.sql, C:/EV-Accounts/backend/migrations/667_canuel_stances.sql, C:/EV-Accounts/backend/migrations/668_peckham_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/574_boston_stances.sql (canonical format)
    - C:/EV-Accounts/backend/migrations/659_coogan_stances.sql (in-phase sibling reference)
  </read_first>
  <action>
    Process ONE politician at a time, sequentially: Raposo (-2523000007), Cadime (-2523000008), Canuel (-2523000009), Peckham (-2523000010). Same procedure as Tasks 2-3. These are the remaining at-large councillors; some may have thin public records (blank spokes are acceptable). Note: Christopher Peckham is stored without the "Sr." suffix per DB convention — search under both "Christopher Peckham Sr." and "Chris Peckham" to find public records. Do NOT touch any external_id outside the -2523000001 to -2523000010 range.
  </action>
  <verify>
    <automated>For each of the 4 UUIDs: unpaired-answer query returns 0 AND uncited-context query returns 0 (same two queries as Task 2).</automated>
  </verify>
  <acceptance_criteria>
    - 4 migration files exist (665, 666, 667, 668), each wrapped in BEGIN;/COMMIT;
    - Every value is a float literal; every sources array double-cast ::text[]::text[]
    - Unpaired-answer query returns 0 for all 4 UUIDs
    - Uncited-context query returns 0 for all 4 UUIDs
    - No INSERT references an external_id outside the -2523000001 to -2523000010 range
  </acceptance_criteria>
  <done>Migrations 665–668 written, applied, and verified for Raposo, Cadime, Canuel, Peckham.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (mcp__supabase-local) | Writes go directly to live production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | stance value integrity | mitigate | Evidence-only rule: no INSERT without a direct cited source; per-person uncited-context query must return 0 |
| T-124-02 | Information Disclosure | reasoning text / sources | accept | All data is public-record political positions with public source URLs; no PII |
| T-124-03 | Repudiation | stance provenance | mitigate | Every value paired with a context row carrying path-bearing source URLs (100% citation rate) |
| T-124-04 | Tampering | out-of-scope external_id | mitigate | Acceptance criteria asserts no external_id outside -2523000001 to -2523000010 appears in any migration |
| T-124-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
After all four tasks, run the partial phase-wide citation check for Fall River officials:
`SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -2523000010 AND -2523000001) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);` must return 0.
</verification>

<success_criteria>
- Wave 0 pre-flight complete; starting migration number (659) and topic count (44) confirmed
- Migrations 659–668 (10 officials) written, applied to production, and verified
- 0 unpaired answers, 0 uncited contexts across all 10 Fall River officials
- No out-of-scope external_ids touched
- Blank-spoke officials documented in SUMMARY (not defaulted to 3.0)
</success_criteria>

<output>
Create `.planning/phases/124-ma-tier3-stances-wave3/124-01-SUMMARY.md` when done. Record: confirmed starting migration number, active topic count, all 10 Fall River UUIDs, per-official stance counts, and any blank-spoke officials.
</output>
