---
phase: 153-inglewood-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["153-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1019_inglewood_complete.sql
autonomous: true
requirements: [INGL-01]

must_haves:
  truths:
    - "George Dotson (defeated March 2023) is unlinked from his office, rows kept"
    - "Alex Padilla holds District 2 — relinked if present in the DB, created fresh if absent"
    - "The final roster is Mayor Butts + District 1 Gray + District 2 Padilla + District 3 Eloy Morales + District 4 Faulk"
    - "The survivor chamber official_count is 4 (council seats only; the directly-elected Mayor is excluded)"
    - "Every active office has a bidirectional politician link"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1019_inglewood_complete.sql"
      provides: "Dotson unlink + Padilla locate-or-create + back-pointer repair + official_count=4"
      contains: "inglewood_complete"
  key_links:
    - from: "essentials.chambers a25a6dea"
      to: "official_count=4"
      via: "El Monte convention (Mayor excluded)"
      pattern: "official_count = 4"
    - from: "Alex Padilla (D2)"
      to: "essentials.offices District 2"
      via: "relink or create-fresh with -7010xx ext_id"
      pattern: "District 2"
---

<objective>
Wave 2 of the Inglewood deep-seed: finalize the current 5-office roster in ONE structural migration
(`1019_inglewood_complete.sql`, registers in `supabase_migrations.schema_migrations`).

Runs after Wave 1 left a single 'City Council' chamber (`a25a6dea`) with the Mayor + council offices
moved in and District 1/3/4 relabeled. This wave: unlink departed George Dotson, locate-or-create
Alex Padilla at District 2, repair any remaining NULL back-pointers, and set `official_count=4`
(council seats only — the directly-elected Mayor is a separate LOCAL_EXEC office, El Monte 151 convention).

Purpose: the verified current roster — Mayor Butts + Gray D1 + Padilla D2 + Eloy Morales D3 + Faulk D4.
Output: `1019_inglewood_complete.sql` applied live via Supabase MCP, committed to EV-Accounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-inglewood-deep-seed/153-CONTEXT.md
@.planning/phases/153-inglewood-deep-seed/153-RESEARCH.md
@.planning/phases/153-inglewood-deep-seed/153-PATTERNS.md
@.planning/phases/153-inglewood-deep-seed/153-VALIDATION.md
@.planning/phases/153-inglewood-deep-seed/153-01-SUMMARY.md

<reference_data>
Survivor chamber: a25a6dea-7f26-4f5e-bc6a-2a5d321063d5 | gov af811c4b-e4da-4f30-ac33-9a7fe7d434ba | geo_id 0636546
Verified current roster (RESEARCH CQ-2):
  Mayor (LOCAL_EXEC)  James T. Butts Jr.  -200740  pol f5775ca1  office 90121859  (KEEP — not counted)
  District 1          Gloria Gray         666261   pol 7a04bf87  office 8e9b0c61   (seated, health-excused — KEEP)
  District 2          Alex Padilla        ext_id=TBD (Task 1 verdict from Wave 1 pre-flight)  -> locate-or-create
  District 3          Eloy Morales Jr.    666263   pol 6ed19c10  office ddcd280b   (dedup survivor)
  District 4          Dionne Faulk        666264   pol 729bc539  office 35b92278
DEPARTED (UNLINK, keep rows):  George Dotson  -201082  pol 3e73448b  office 6b20a733  (lost March 2023 D1 runoff to Gray)
official_count target: 4 (council only; Mayor excluded — El Monte 151 / Pasadena / Pomona convention).
Padilla create-fresh: new ext_id from `SELECT MIN(external_id)-1 FROM essentials.politicians WHERE external_id<0` (expect ~-7010xx); source 'cityofinglewood.org'.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roster pre-flight — confirm Wave-1 end state, Dotson currency, Padilla verdict, and new ext_id</name>
  <files>(read-only; no file written this task)</files>
  <read_first>
    - .planning/phases/153-inglewood-deep-seed/153-01-SUMMARY.md (Wave-1 Padilla verdict + post-apply office count + resolved district UUIDs)
    - .planning/phases/153-inglewood-deep-seed/153-RESEARCH.md §CQ-2 (verified roster; Dotson CONFIRMED departed; Gray seated/health-excused — do NOT unlink) + §Common Pitfalls Pitfall 1 (Padilla) + §Assumptions Log A3 (re-verify Gray's seat at apply time)
    - C:/EV-Accounts/backend/migrations/1001_elmonte_complete.sql (lines 1-24 — the pre-flight findings header convention)
  </read_first>
  <action>
    Via `mcp__supabase-local__execute_sql`:
    (a) Confirm Wave-1 end state: single 'City Council' chamber a25a6dea; the moved offices present; geo_id 0636546. If Wave 1 did not complete, STOP.
    (b) Re-confirm the Padilla verdict from Wave 1 (PRESENT with ext_id+office, or ABSENT). If PRESENT but on a wrong district/label or one-directional, record the repair needed. If ABSENT, this task computes his new ext_id.
    (c) Compute the new ext_id only if Padilla ABSENT: `SELECT MIN(external_id) FROM essentials.politicians WHERE external_id<0` then choose MIN-1 (or the next free value in the -7010xx band; West Covina/El Monte used -701001..). Record the exact value to use.
    (d) Resolve the District 2 districts row UUID (if Padilla is to be seated there): query for an existing `essentials.districts` row label 'District 2' geo_id '0636546'; if none, Task 2 INSERTs it.
    (e) Confirm Dotson -201082: still has a non-null office_id and/or his office 6b20a733 still references him -> UNLINK in Task 2. Resolve the full office UUID 6b20a733.
    (f) Re-verify Gray (666261) is still the seated D1 councilmember per RESEARCH A3 (health-excused != vacated). If the official site at apply time shows a replacement, STOP and surface.
    (g) List back-pointers that are still NULL for any current member -> Task 2 repairs them.
    Produce a written pre-flight block: Padilla branch (RELINK with ext_id+office, or CREATE with computed ext_id + D2 district UUID), Dotson office UUID, NULL back-pointers list, Gray re-confirmation.
  </action>
  <verify>
    <automated>MISSING — read-only MCP assertion queries; capture results in SUMMARY pre-flight block</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba' AND name='City Council'` -> 1 (Wave 1 confirmed complete)
    - Padilla verdict recorded as RELINK(ext_id, office_uuid) OR CREATE(new_ext_id, d2_district_uuid); the chosen new ext_id (if CREATE) is confirmed free: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id=<chosen>` -> 0
    - Dotson still linked pre-fix: `SELECT office_id FROM essentials.politicians WHERE external_id=-201082` returns non-NULL OR `SELECT politician_id FROM essentials.offices WHERE id='6b20a733...'` non-NULL
    - Gray (666261) confirmed still the current D1 holder (RESEARCH A3); any contradiction surfaced as STOP
  </acceptance_criteria>
  <done>Wave-1 end state confirmed; Padilla branch decided with concrete identifiers; Dotson office UUID resolved; NULL back-pointers enumerated; Gray re-confirmed seated.</done>
</task>

<task type="auto">
  <name>Task 2: Locate-or-create Alex Padilla (District 2) — branching create vs relink</name>
  <files>C:/EV-Accounts/backend/migrations/1019_inglewood_complete.sql (Part A)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1001_elmonte_complete.sql (lines 28-71 — Part A: INSERT politician guarded by external_id, INSERT new office guarded NOT EXISTS on chamber_id+district_id, A3 back-pointer; this is the create-fresh template)
    - .planning/phases/153-inglewood-deep-seed/153-PATTERNS.md §Pattern 11 (Padilla create-fresh, Inglewood substitutions) + §Shared Patterns (districts.label not name; chambers.slug GENERATED; offices insert column list)
    - Task 1 pre-flight block (Padilla branch + computed ext_id + D2 district UUID)
  </read_first>
  <action>
    Begin authoring `C:/EV-Accounts/backend/migrations/1019_inglewood_complete.sql` (Part A — Padilla). Branch per Task 1:
    BRANCH CREATE (Padilla ABSENT from DB): inside the migration's `BEGIN;` block —
      (A0) IF no 'District 2' districts row for geo_id 0636546: `INSERT INTO essentials.districts (label, district_type, geo_id, state) SELECT 'District 2','LOCAL','0636546','CA' WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE label='District 2' AND geo_id='0636546')`.
      (A1) INSERT Padilla politician guarded by external_id (mirror 1001 lines 33-41): full_name 'Alex Padilla', first/last 'Alex'/'Padilla', is_active/is_incumbent true, source 'cityofinglewood.org', alternate_names '{}', external_id = the Task-1 computed value, `ON CONFLICT (external_id) DO NOTHING`.
      (A2) INSERT the District 2 office in survivor chamber a25a6dea (mirror 1001 lines 46-61): politician_id = Padilla's id (subselect by external_id), chamber_id 'a25a6dea-7f26-4f5e-bc6a-2a5d321063d5', district_id = the D2 districts row, title 'Councilmember', representing_city 'Inglewood', normalized_position_name 'Council Member', `WHERE NOT EXISTS (... chamber_id + district_id ...)`.
      (A3) Back-pointer: `UPDATE essentials.politicians SET office_id=(subselect the new D2 office) WHERE external_id=<padilla> AND office_id IS DISTINCT FROM (subselect)`.
    BRANCH RELINK (Padilla PRESENT but on the wrong chamber/label/one-directional): move his office to chamber a25a6dea by UUID, relabel his district to 'District 2' (guarded `IS DISTINCT FROM`), and repair the bidirectional link (set both `offices.politician_id` and `politicians.office_id`). Use his existing ext_id — do NOT create a new record.
    Document the chosen branch in a header comment. (Dotson unlink + back-pointer repair + official_count come in Task 3, same file.)
  </action>
  <verify>
    <automated>MISSING — assertions run after Task 3 applies the full file; this task authors Part A only</automated>
  </verify>
  <acceptance_criteria>
    - After full-file apply (Task 3): exactly one office in chamber a25a6dea has a district labeled 'District 2' and a non-null politician_id resolving to Alex Padilla.
    - CREATE branch: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id=<chosen_ext_id> AND full_name ILIKE '%padilla%'` -> 1; his `office_id` is non-NULL and points at the D2 office.
    - RELINK branch: no new politician row created (`SELECT COUNT(*) FROM essentials.politicians WHERE full_name ILIKE '%padilla%'` unchanged from pre-flight); his office now in a25a6dea, district label 'District 2', bidirectional.
    - exactly one 'District 2' districts row for geo_id 0636546 (`SELECT COUNT(*) FROM essentials.districts WHERE label='District 2' AND geo_id='0636546'` -> 1)
  </acceptance_criteria>
  <done>Part A of 1019 authored with the correct Padilla branch (create-fresh with new -7010xx ext_id, or relink existing); District 2 row + office + bidirectional link defined.</done>
</task>

<task type="auto">
  <name>Task 3: Unlink Dotson + repair back-pointers + official_count=4; apply + commit 1019</name>
  <files>C:/EV-Accounts/backend/migrations/1019_inglewood_complete.sql (Part B + apply)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1001_elmonte_complete.sql (lines 73-113 — Part B back-pointer repair, official_count guarded UPDATE, ledger registration, post-verification block)
    - C:/EV-Accounts/backend/migrations/1011_west_covina_complete.sql (lines 28-44 — official_count guarded + post-verification for a no-new-member roster)
    - .planning/phases/153-inglewood-deep-seed/153-PATTERNS.md §Pattern 10 (Dotson unlink-not-delete) + §Pattern 12 (back-pointer repair) + §Pattern 13 (official_count=4) + §Pattern 14 (ledger + post-verification)
    - Task 1 pre-flight block (Dotson office UUID, NULL back-pointers list)
  </read_first>
  <action>
    Complete `1019_inglewood_complete.sql` (Part B) after Part A, still inside `BEGIN; ... COMMIT;`:
    (B1) Unlink Dotson (unlink-not-delete, keep politician/stance/image rows): `UPDATE essentials.offices SET politician_id=NULL WHERE id='6b20a733...' AND politician_id IS NOT NULL` and `UPDATE essentials.politicians SET office_id=NULL WHERE external_id=-201082 AND office_id IS NOT NULL`. (Then the now-empty Dotson office can be left in the chamber as an unoccupied seat, OR if Task-1 found a separate D1 row, ensure Gray's office holds District 1 — Dotson's seat must NOT compete with Gray's D1; if Dotson's office shares Gray's district_id, leave it office-less/unoccupied. Do NOT delete the office row unless Task 1 confirmed it is a stray with no district — follow the unlink-not-delete default.)
    (B2) Repair any remaining NULL back-pointers for current members enumerated in Task 1 (Butts -200740, Gray 666261, Eloy 666263, Faulk 666264, Padilla) using `UPDATE essentials.politicians SET office_id='<office>' WHERE external_id=<ext> AND office_id IS DISTINCT FROM '<office>'`.
    (B3) official_count: `UPDATE essentials.chambers SET official_count=4 WHERE id='a25a6dea-7f26-4f5e-bc6a-2a5d321063d5' AND official_count IS DISTINCT FROM 4`.
    AFTER `COMMIT;`, register OUTSIDE the transaction: `INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('1019','inglewood_complete') ON CONFLICT (version) DO NOTHING`. Append a post-verification comment block (adapt 1001 lines 104-113: final 5-member roster Butts/Gray D1/Padilla D2/Eloy D3/Faulk D4, official_count=4, exactly one LOCAL_EXEC office, split-section 0 rows).
    APPLY the full file live via `mcp__supabase-local__apply_migration`. Commit: `git -C "C:/EV-Accounts" add backend/migrations/1019_inglewood_complete.sql && git -C "C:/EV-Accounts" commit -m "feat(153): Inglewood roster complete — unlink Dotson + Padilla D2 + official_count=4"`.
  </action>
  <verify>
    <automated>MISSING — apply via MCP then run the acceptance assertion queries via mcp__supabase-local__execute_sql</automated>
  </verify>
  <acceptance_criteria>
    - Dotson unlinked: `SELECT office_id FROM essentials.politicians WHERE external_id=-201082` -> NULL; Dotson politician row still exists (`SELECT COUNT(*) FROM essentials.politicians WHERE external_id=-201082` -> 1)
    - official_count: `SELECT official_count FROM essentials.chambers WHERE id='a25a6dea-7f26-4f5e-bc6a-2a5d321063d5'` -> 4
    - district labels: the 4 council offices read District 1/2/3/4 with NO 'At-Large' (`SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE o.chamber_id='a25a6dea-7f26-4f5e-bc6a-2a5d321063d5' AND d.label='At-Large'` -> 0)
    - Mayor LOCAL_EXEC intact: `SELECT COUNT(*) FROM essentials.offices WHERE government_id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba' AND district_type='LOCAL_EXEC'` -> 1 (Butts)
    - five active bidirectional members: `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id=o.politician_id WHERE o.government_id='af811c4b-e4da-4f30-ac33-9a7fe7d434ba' AND p.office_id=o.id` -> 5 (Butts + Gray + Padilla + Eloy + Faulk)
    - split-section check returns 0 rows for gov af811c4b
    - ledger: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` -> 1019; file committed to EV-Accounts
  </acceptance_criteria>
  <done>1019_inglewood_complete.sql authored, applied live, committed; Dotson unlinked (rows kept); final roster = Mayor Butts + Gray D1 + Padilla D2 + Eloy D3 + Faulk D4; official_count=4; one LOCAL_EXEC office; split-section 0 rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor SQL -> production Supabase | Static migration mutating live roster rows; surface is data integrity, not injection |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-05 | Tampering | ext_id collision when creating Padilla | mitigate | Compute new ext_id from live `MIN(external_id)-1` at pre-flight; INSERT guarded `ON CONFLICT (external_id) DO NOTHING`; Task-1 confirms the value is free |
| T-153-06 | Tampering | Hard-deleting departed Dotson (loses history) | mitigate | Unlink-not-delete: null both pointers, KEEP politician/stance/image rows; guarded `IS NOT NULL` |
| T-153-07 | Tampering | Wrongly unlinking Gray (health-excused, still seated) | mitigate | RESEARCH A3 re-verify at pre-flight; only Dotson (-201082) is unlinked; Gray (666261) explicitly KEPT |
| T-153-08 | Tampering | Re-run duplicating Padilla office / double-counting | mitigate | `WHERE NOT EXISTS (chamber_id+district_id)` on office INSERT; `IS DISTINCT FROM` on count/back-pointer UPDATEs |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Final roster = 5 bidirectional members (Mayor Butts + D1-D4); exactly one LOCAL_EXEC office.
- official_count=4; all four council labels are District 1-4 (no At-Large).
- Dotson unlinked, rows preserved; Padilla seated at D2 (created or relinked).
- Split-section check 0 rows; migration 1019 registered and committed.
</verification>

<success_criteria>
- ROADMAP success criterion 1: current mayor + full council seated as offices linked to geo_id 0636546.
- ROADMAP success criterion 2: structure matches Inglewood's real form of government (by-district D1-4 + directly-elected Mayor), official site-verified.
- INGL-01 roster + structure hard requirement satisfied.
</success_criteria>

<output>
Create `.planning/phases/153-inglewood-deep-seed/153-02-SUMMARY.md` when done (include the Padilla branch taken + his final ext_id, Dotson unlink confirmation, and the final 5-member roster table).
</output>
