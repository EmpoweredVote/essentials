---
phase: 85-multnomah-elections-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 85-01
files_modified:
  - C:/EV-Accounts/backend/migrations/252_multnomah_discovery.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-252.ts
  - C:/EV-Accounts/backend/scripts/smoke-multnomah-elections.ts
autonomous: true
requirements:
  - ELECTIONS-03
tags:
  - sql-migration
  - discovery-jurisdiction
  - multnomah
  - smoke-test

must_haves:
  truths:
    - "essentials.discovery_jurisdictions has exactly one row with jurisdiction_geoid='41051' and election_date='2026-11-03' per D-09"
    - "The new discovery_jurisdictions row has jurisdiction_name='Multnomah County, Oregon', state='OR', source_url='https://www.multco.us/elections' per D-09/D-10, and allowed_domains=ARRAY['multco.us','ballotpedia.org','sos.oregon.gov'] per D-11"
    - "Migration 252 is idempotent — re-running inserts zero additional discovery_jurisdictions rows (WHERE NOT EXISTS guard per D-13)"
    - "Migration 252 ledger entry exists in supabase_migrations.schema_migrations (version='252')"
    - "A smoke test using the Corbett OR unincorporated address (lon=-122.2 lat=45.5) confirms the 18 race rows seeded in Plan 01 are reachable for a Multnomah County resident (D-14)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/252_multnomah_discovery.sql"
      provides: "Idempotent INSERT into essentials.discovery_jurisdictions for Multnomah County + ledger entry"
      contains: "INSERT INTO essentials.discovery_jurisdictions"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-252.ts"
      provides: "Apply script using pg.Pool that runs 252_multnomah_discovery.sql and runs post-apply verification queries"
      exports: ["(default tsx script — no exports)"]
    - path: "C:/EV-Accounts/backend/scripts/smoke-multnomah-elections.ts"
      provides: "Smoke test verifying ELECTIONS-01/02/03 + D-14 (Corbett unincorporated address returns Multnomah races)"
      exports: ["(default tsx script — no exports)"]
  key_links:
    - from: "migrations/252_multnomah_discovery.sql"
      to: "essentials.discovery_jurisdictions row (jurisdiction_geoid='41051', election_date='2026-11-03')"
      via: "WHERE NOT EXISTS guard"
      pattern: "jurisdiction_geoid = '41051' AND election_date = '2026-11-03'"
    - from: "scripts/smoke-multnomah-elections.ts"
      to: "essentials.races (18 seeded rows from Plan 01)"
      via: "JOIN essentials.elections via election_id"
      pattern: "name = 'OR 2026 General'"
    - from: "scripts/smoke-multnomah-elections.ts"
      to: "essentials.discovery_jurisdictions row inserted by migration 252"
      via: "pg.Client.query"
      pattern: "jurisdiction_geoid = '41051'"
---

<objective>
Add a single discovery_jurisdictions row for the Multnomah County area (geo_id='41051', election_date='2026-11-03') so the weekly Sunday cron pipeline finds candidates for the 18 race rows seeded in Plan 01. Then write and run a smoke test that verifies ELECTIONS-01/02/03 + D-14 (the Corbett OR unincorporated address — which is in Multnomah County but in no incorporated city — surfaces the seeded races). Implements requirement ELECTIONS-03.

Purpose: Arms the candidate discovery cron for Multnomah County (covers county commissioner + all 5 smaller city races since Multnomah County Elections administers them all per D-09) and confirms the full phase result with an automated smoke check.

Output: migrations/252_multnomah_discovery.sql + scripts/_apply-migration-252.ts + scripts/smoke-multnomah-elections.ts (all under C:/EV-Accounts/backend/). Migration applied to production Supabase; smoke test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-multnomah-elections-discovery/85-CONTEXT.md
@.planning/phases/85-multnomah-elections-discovery/85-RESEARCH.md
@.planning/phases/85-multnomah-elections-discovery/85-PATTERNS.md
@.planning/phases/85-multnomah-elections-discovery/85-VALIDATION.md
@.planning/phases/85-multnomah-elections-discovery/85-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 252 + apply script + apply to production</name>
  <files>
    C:/EV-Accounts/backend/migrations/252_multnomah_discovery.sql,
    C:/EV-Accounts/backend/scripts/_apply-migration-252.ts
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql (canonical discovery_jurisdictions migration — copy column order, WHERE NOT EXISTS guard, ARRAY literal syntax verbatim)
    - C:/EV-Accounts/backend/scripts/_apply-migration-244.ts (apply script analog — imports, Pool construction, readFileSync, try/catch/finally pattern)
    - .planning/phases/85-multnomah-elections-discovery/85-PATTERNS.md (migrations/252_multnomah_discovery.sql section + scripts/_apply-migration-252.ts section — both contain full templates)
    - .planning/phases/85-multnomah-elections-discovery/85-RESEARCH.md (Pattern 2: Discovery Jurisdiction, Migration 252 Structure Template, Existing Discovery Jurisdictions for OR — confirms geo_id='41051' is missing and must be added)
  </read_first>
  <action>
    Create two files in C:/EV-Accounts/backend/:

    **File 1: migrations/252_multnomah_discovery.sql** — follows migration 241 structure exactly.

    Contents:
    1. Header: `-- Migration 252: Multnomah County discovery_jurisdictions row — Phase 85 Plan 02`.
    2. Single `INSERT INTO essentials.discovery_jurisdictions ... SELECT ... WHERE NOT EXISTS (...)` statement with column order `(id, jurisdiction_geoid, jurisdiction_name, state, election_date, source_url, allowed_domains)`.
    3. SELECT values (per D-09 through D-12):
       - `gen_random_uuid()`
       - `'41051'`
       - `'Multnomah County, Oregon'`
       - `'OR'`
       - `'2026-11-03'`
       - `'https://www.multco.us/elections'`
       - `ARRAY['multco.us', 'ballotpedia.org', 'sos.oregon.gov']`
    4. WHERE NOT EXISTS guard (per D-13): SELECT 1 FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid = '41051' AND election_date = '2026-11-03'.
    5. Append ledger entry: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('252') ON CONFLICT (version) DO NOTHING;`.

    Anti-pattern guards (per RESEARCH.md):
    - Do NOT add cron_active column — it does not exist; cron arms via election_date 180-day window.
    - Do NOT use DELETE+INSERT for idempotency — use WHERE NOT EXISTS.
    - Do NOT duplicate the OR statewide row (geo_id='41') or the Portland row (geo_id='4159000') — both already exist; only the geo_id='41051' row is being added.

    **File 2: scripts/_apply-migration-252.ts** — follows _apply-migration-244.ts structure exactly.

    Contents:
    1. Same imports as _apply-migration-251.ts: `import 'dotenv/config'`; `import { Pool } from 'pg'`; `import { readFileSync } from 'fs'`; `import path from 'path'`.
    2. Pool: `const pool = new Pool({ connectionString: process.env['DATABASE_URL'], ssl: { rejectUnauthorized: false } })`.
    3. Load: `const sql = readFileSync(path.join(process.cwd(), 'migrations', '252_multnomah_discovery.sql'), 'utf8')`.
    4. try: `await pool.query(sql)` + `console.log('Migration 252 applied successfully')`.
    5. Post-apply verification queries (per 85-PATTERNS.md):
       - Query 1: `SELECT COUNT(*) as cnt FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid = '41051' AND election_date = '2026-11-03'` — log `Discovery jurisdiction rows: N (expected 1)`.
       - Query 2: `SELECT jurisdiction_geoid, jurisdiction_name, source_url, allowed_domains, election_date FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid = '41051'` — log row as JSON for visual confirmation of all column values.
       - Query 3: `SELECT COUNT(*) as cnt FROM supabase_migrations.schema_migrations WHERE version = '252'` — log `Ledger entry: N (expected 1)`.
    6. catch (e: any): `console.error('Error applying migration 252:', e.message)` + `process.exit(1)`.
    7. finally: `await pool.end()`.

    After both files are written, RUN the apply script via the verify command. This applies migration 252 to production Supabase. Confirm script output shows:
    - `Discovery jurisdiction rows: 1 (expected 1)`
    - JSON row shows jurisdiction_name='Multnomah County, Oregon', source_url='https://www.multco.us/elections', allowed_domains contains exactly ['multco.us','ballotpedia.org','sos.oregon.gov'].
    - `Ledger entry: 1 (expected 1)`.

    Idempotency: re-running the script must produce the same counts (WHERE NOT EXISTS guard prevents duplicate INSERT).

    Note: Do NOT run any git commands in C:/EV-Accounts — per memory feedback, EV-Accounts is not a git repo.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend &amp;&amp; npx tsx scripts/_apply-migration-252.ts</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/252_multnomah_discovery.sql exists.
    - File contains literal string `INSERT INTO essentials.discovery_jurisdictions`.
    - File contains literal string `'41051'` (jurisdiction_geoid value).
    - File contains literal string `'Multnomah County, Oregon'` (jurisdiction_name).
    - File contains literal string `'https://www.multco.us/elections'` (source_url).
    - File contains the literal substring `ARRAY['multco.us', 'ballotpedia.org', 'sos.oregon.gov']` (allowed_domains).
    - File contains literal string `WHERE NOT EXISTS` followed by a SELECT against essentials.discovery_jurisdictions filtered by `jurisdiction_geoid = '41051' AND election_date = '2026-11-03'`.
    - File ends with `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('252')` + `ON CONFLICT (version) DO NOTHING`.
    - File does NOT contain the string `cron_active` (column does not exist).
    - File does NOT contain `DELETE FROM essentials.discovery_jurisdictions` (anti-pattern).
    - File C:/EV-Accounts/backend/scripts/_apply-migration-252.ts exists.
    - File imports `Pool` from `pg` and references `'252_multnomah_discovery.sql'`.
    - File runs three labelled verification queries: `Discovery jurisdiction rows`, the row JSON dump, and `Ledger entry`.
    - Running `npx tsx scripts/_apply-migration-252.ts` in C:/EV-Accounts/backend exits 0.
    - Script output contains literal string `Discovery jurisdiction rows: 1`.
    - Script output contains literal string `Ledger entry: 1`.
    - Re-running the apply script (idempotency check) exits 0 and Discovery jurisdiction rows count remains 1.
  </acceptance_criteria>
  <done>
    Migration 252 is applied to production. essentials.discovery_jurisdictions contains the new geo_id='41051' row with election_date='2026-11-03'. Ledger entry version='252' exists. Re-run is a no-op.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write smoke-multnomah-elections.ts smoke test + run it to verify ELECTIONS-01/02/03 + D-14</name>
  <files>C:/EV-Accounts/backend/scripts/smoke-multnomah-elections.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/smoke-multnomah-cities.ts (canonical smoke test analog — copy Client/dotenv pattern, DATABASE_URL pre-flight guard, connect/disconnect pattern, allPassed/errors aggregation, exit-code convention)
    - C:/EV-Accounts/backend/scripts/smoke-multnomah-county.ts (Phase 83 smoke test — already documents the Corbett (-122.2, 45.5) coordinate as verified unincorporated Multnomah County; copy any ST_Covers query pattern if needed)
    - .planning/phases/85-multnomah-elections-discovery/85-PATTERNS.md (scripts/smoke-multnomah-elections.ts section — full Race count verification query pattern + Discovery jurisdiction verification query pattern + All-passed exit pattern)
    - .planning/phases/85-multnomah-elections-discovery/85-RESEARCH.md (Smoke Test Address section — Corbett OR coordinate verified; Pattern shown: electionService.getElectionsByGovernmentGeoIds(['41051', '4131250', ...]))
    - .planning/phases/85-multnomah-elections-discovery/85-VALIDATION.md (Per-Task Verification Map row 85-02-02 — confirms this script covers ELECTIONS-01/02/03 + D-14)
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/scripts/smoke-multnomah-elections.ts following the smoke-multnomah-cities.ts structure.

    Top-level structure:
    1. Imports: `import { Client } from 'pg'`; `import * as dotenv from 'dotenv'`; `dotenv.config()`.
    2. `main()` async function. Pre-flight: if DATABASE_URL is unset, write error to stderr and process.exit(1).
    3. Connect: `const client = new Client({ connectionString: process.env['DATABASE_URL'], ssl: { rejectUnauthorized: false } })`; `await client.connect()`.
    4. Inside try/finally, run the assertions below and aggregate failures into `const errors: string[] = []`, `let allPassed = true`.
    5. Use `console.log` for human-readable progress and `errors.push(...)` for assertion failures.
    6. finally: `await client.end()`.
    7. Final block: if allPassed log `ALL ASSERTIONS PASSED` and exit 0; else log `FAILED (N assertion(s)):` followed by each error and exit 1.
    8. Call `main().catch((err) => { console.error('Smoke test error:', err); process.exit(1); })`.

    Assertions (each must contribute at minimum one labelled console.log AND push to errors on failure):

    **Assertion A — Multnomah County race rows exist (ELECTIONS-01):**
    Query: `SELECT COUNT(*) as cnt FROM essentials.races r JOIN essentials.elections e ON e.id = r.election_id WHERE e.name = 'OR 2026 General' AND r.position_name ILIKE '%Multnomah County%'`. Expected: 2. On mismatch push `FAIL: expected 2 county race rows, got N`.

    **Assertion B — Smaller-city race rows exist (ELECTIONS-02):**
    Query: `SELECT COUNT(*) as cnt FROM essentials.races r JOIN essentials.elections e ON e.id = r.election_id WHERE e.name = 'OR 2026 General' AND r.position_name ~ '(Gresham|Troutdale|Fairview|Wood Village|Maywood Park)'`. Expected: 16. On mismatch push `FAIL: expected 16 city race rows, got N`.

    **Assertion C — Discovery jurisdiction row exists (ELECTIONS-03):**
    Query: `SELECT COUNT(*) as cnt FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid = '41051' AND election_date = '2026-11-03'`. Expected: exactly 1. On mismatch push `FAIL: expected 1 discovery_jurisdictions row for geo_id=41051, got N`.

    **Assertion D — Discovery row values are correct (ELECTIONS-03 detail):**
    Query: `SELECT jurisdiction_name, source_url, allowed_domains, state FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid = '41051'`. Verify:
    - jurisdiction_name === 'Multnomah County, Oregon'
    - state === 'OR'
    - source_url === 'https://www.multco.us/elections'
    - allowed_domains array equals ['multco.us','ballotpedia.org','sos.oregon.gov'] (exact set, any order). On mismatch push specific failure messages per field.

    **Assertion E — Corbett unincorporated address surfaces races (D-14):**
    Use the Corbett OR coordinate (-122.2, 45.5). The address is documented in 85-RESEARCH.md as verified-unincorporated (G4020 geo_id=41051 present, G4110 absent, geo_id=4159000 absent).
    Run a query that mirrors how the backend's electionService surfaces races:
    `SELECT DISTINCT g.geo_id, r.position_name FROM essentials.races r
       JOIN essentials.offices o ON o.id = r.office_id
       JOIN essentials.chambers c ON c.id = o.chamber_id
       JOIN essentials.governments g ON g.id = c.government_id
       JOIN essentials.elections e ON e.id = r.election_id
       WHERE e.name = 'OR 2026 General'
         AND g.geo_id IN ('41051', '4131250', '4174950', '4123700', '4183650', '4146100')`.
    NOTE: confirm the 5 smaller-city geo_ids by reading 85-RESEARCH.md and the Phase 84 SUMMARY/migration 246 if any differ; if a geo_id is wrong this assertion will under-count.
    Expected: at least 18 distinct position_names returned (2 county + 16 city). On mismatch push `FAIL: Corbett address pattern yielded N race rows, expected >= 18`.

    Log a summary line: `Corbett (-122.2, 45.5) Multnomah County race rows reachable: N`.

    After writing the file, RUN it via the verify command. The script must exit 0 with `ALL ASSERTIONS PASSED`.

    Pool vs Client convention reminder (per 85-PATTERNS.md): smoke tests use `Client`, apply scripts use `Pool`. Do not mix them.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend &amp;&amp; npx tsx scripts/smoke-multnomah-elections.ts</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/scripts/smoke-multnomah-elections.ts exists.
    - File imports `Client` from `pg` (NOT `Pool`).
    - File imports `* as dotenv` and calls `dotenv.config()`.
    - File contains a DATABASE_URL pre-flight guard that exits 1 when the env var is unset.
    - File contains all 5 assertions A–E as described in &lt;action&gt;, each producing a console.log line and pushing to `errors` on failure.
    - File contains the literal string `ALL ASSERTIONS PASSED` reachable on the happy path.
    - File contains the literal coordinate substring `-122.2` AND `45.5` (Corbett OR address per D-14).
    - File contains the literal substring `geo_id IN ('41051'` (county geo_id from D-09).
    - File ends with `main().catch(...)` invocation.
    - Running `npx tsx scripts/smoke-multnomah-elections.ts` in C:/EV-Accounts/backend exits 0.
    - Script output contains the literal string `ALL ASSERTIONS PASSED`.
    - Script output does NOT contain the literal string `FAIL:` anywhere.
  </acceptance_criteria>
  <done>
    Smoke test exists at scripts/smoke-multnomah-elections.ts and exits 0 with ALL ASSERTIONS PASSED. ELECTIONS-01, ELECTIONS-02, ELECTIONS-03, and D-14 are all empirically verified against production Supabase.
  </done>
</task>

</tasks>

<verification>
After both tasks pass:

- Wave-level smoke run: `cd C:/EV-Accounts/backend && npx tsx scripts/smoke-multnomah-elections.ts` exits 0 with `ALL ASSERTIONS PASSED`.
- Production state check (run via Supabase MCP or pg client):
  ```sql
  -- Race rows
  SELECT COUNT(*) FROM essentials.races r
  JOIN essentials.elections e ON e.id = r.election_id
  WHERE e.name = 'OR 2026 General'
    AND (r.position_name ILIKE '%Multnomah County%'
         OR r.position_name ~ '(Gresham|Troutdale|Fairview|Wood Village|Maywood Park)');
  -- expected: 18

  -- Discovery jurisdiction
  SELECT jurisdiction_geoid, jurisdiction_name, election_date, source_url
  FROM essentials.discovery_jurisdictions WHERE jurisdiction_geoid='41051';
  -- expected: 1 row, Multnomah County, Oregon, 2026-11-03, multco.us/elections

  -- Ledger
  SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('251','252');
  -- expected: 2 rows
  ```
</verification>

<success_criteria>
- migrations/252_multnomah_discovery.sql exists with WHERE NOT EXISTS guard + ledger entry.
- scripts/_apply-migration-252.ts exists and applies the migration cleanly.
- scripts/smoke-multnomah-elections.ts exists and exits 0 with ALL ASSERTIONS PASSED.
- essentials.discovery_jurisdictions has the new geo_id='41051' row with election_date='2026-11-03'.
- Migration ledger contains version '252'.
- The discovery cron will now find candidates for the 18 race rows from Plan 01 starting at the next Sunday 02:00 UTC run (election_date is well within the 180-day window).
- Phase 85 closes: ELECTIONS-01, ELECTIONS-02, ELECTIONS-03 all PASS.
</success_criteria>

<output>
Create `.planning/phases/85-multnomah-elections-discovery/85-02-SUMMARY.md` when done. Include: actual file paths created, apply script output (discovery row + ledger counts), smoke test output (ALL ASSERTIONS PASSED), confirmation that Phase 85 closes.
</output>
