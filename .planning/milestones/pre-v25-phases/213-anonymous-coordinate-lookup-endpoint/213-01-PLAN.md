---
phase: 213-anonymous-coordinate-lookup-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/src/lib/coordinateValidation.ts
  - C:/EV-Accounts/backend/src/lib/coordinateValidation.test.ts
  - C:/EV-Accounts/backend/src/lib/essentialsService.ts
autonomous: true
requirements: [RSLV-03]

must_haves:
  truths:
    - "classifyCoordinate returns a distinct discriminated result for valid / OUTSIDE_US_BOUNDS / SWAPPED_COORDINATES / INVALID_COORDINATES"
    - "The malformed-input short-circuit rejects only genuinely un-interpretable input and never pre-empts the swap-guard branch"
    - "A generous US bbox accepts continental, Alaska (incl. Aleutian antimeridian), Hawaii, DC and Puerto Rico points"
    - "getRepresentativesByCoordinate(lat, lng) returns officials for a point via ST_Covers without ever calling geocodeAddress"
    - "The precise point resolves EXACTLY ONE US House (NATIONAL_LOWER) rep — the nationwide federal roster is never merged into politicians"
    - "A valid US point in an unseeded area still returns the state-scoped floor (Senators + Governor/state execs) — never an empty politicians array"
    - "The coordinate path leaves matchedAddress empty and never carries the raw lat/lng in the returned object"
  artifacts:
    - path: "C:/EV-Accounts/backend/src/lib/coordinateValidation.ts"
      provides: "US_BBOX constant + classifyCoordinate pure function returning the 422 code taxonomy"
      exports: ["US_BBOX", "classifyCoordinate", "CoordinateRejectionCode"]
    - path: "C:/EV-Accounts/backend/src/lib/coordinateValidation.test.ts"
      provides: "Unit coverage for bbox membership, Aleutian antimeridian, swap guard, malformed input"
    - path: "C:/EV-Accounts/backend/src/lib/essentialsService.ts"
      provides: "getRepresentativesByCoordinate coordinate-only entry point + extracted shared point-resolution core"
      contains: "export async function getRepresentativesByCoordinate"
  key_links:
    - from: "getRepresentativesByCoordinate"
      to: "ST_Covers over geofence_boundaries"
      via: "shared point-resolution core (extracted from getRepresentativesByAddress)"
      pattern: "ST_MakePoint\\(\\$1::float8, \\$2::float8\\)"
    - from: "getRepresentativesByCoordinate"
      to: "getStatewideOfficials (state-scoped floor only) / getPoliticiansByArea (single-House-rep fallback)"
      via: "state-scoped fallback floor (D-05) — getFederalOfficials is NOT merged into politicians"
      pattern: "getStatewideOfficials|getPoliticiansByArea"
---

<objective>
Build the two backend building blocks the anonymous coordinate endpoint needs, with zero HTTP surface yet:

1. A coordinate-validation module (`coordinateValidation.ts`) that decides — as a pure function — whether a submitted `(lat, lng)` is a valid US point, an out-of-box point, a lat/lng-swapped point, or malformed input. This is the D-02/D-03/D-07 logic in isolation so it is exhaustively unit-testable without a DB or HTTP.
2. A coordinate-only service entry point `getRepresentativesByCoordinate(lat, lng)` factored out of the existing `getRepresentativesByAddress`, so the anonymous endpoint resolves officials via PostGIS `ST_Covers` **without re-geocoding via Census** (D-04), returns the **exact single US House rep** the precise point falls in (RSLV-05), and applies the Phase 212 **state-scoped** floor so a valid US point never returns empty (D-05).

Purpose: Isolate the privacy-critical validation and the no-geocode coordinate resolution before wiring any route, so Plan 02 (route) and Plan 03 (live smoke test) build on tested, reviewed logic.
Output: `coordinateValidation.ts` + its test, and a new exported `getRepresentativesByCoordinate` in `essentialsService.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-CONTEXT.md

Cross-repo reality: ALL code lives in `C:/EV-Accounts` (accounts-api Express backend). Deploy = push to `master` -> Render auto-deploys to `accounts-api.empowered.vote`. Commit backend changes via `git -C "C:/EV-Accounts" ...`. gsd-executor has NO Supabase MCP — any DB check runs inline via psql/curl.

<interfaces>
<!-- Contracts the executor must build against. Read the files in each task's <read_first> for full detail. -->

AddressSearchResult (C:/EV-Accounts/backend/src/lib/essentialsService.ts:153) — the response shape to reuse verbatim. It carries NO lat/lng field, so leaving `matchedAddress: ''` keeps the raw coordinate out of the response (D-06).

geocodeAddress return (C:/EV-Accounts/backend/src/lib/geocodingService.ts) -> `{ lat, lng, matchedAddress, state, city }` where `state` is a 2-letter UPPERCASE abbreviation e.g. "IN". The coordinate path has NO geocode, so it must DERIVE the equivalent 2-letter abbrev from covering district rows.

FIPS_TO_ABBREV (C:/EV-Accounts/backend/src/lib/essentialsBrowseService.ts:39) — maps a 2-digit state FIPS ("18") to a 2-letter abbrev ("IN"). Use it to derive the statewide-query `state` value from a covering row's geo_id.

getStatewideOfficials(stateAbbrev) (essentialsBrowseService.ts:978) — STATE-SCOPED floor: Senators + Governor/state execs for one state. Returns PoliticianFlatRecord[]. Use this for the D-05 floor.

getFederalOfficials() (essentialsBrowseService.ts:1040) — WARNING: its own doc comment says it returns "every representative, not just one state's" (all ~435 US House members + all Senators + federal exec/judicial). It has ZERO geographic scoping — it is for the national browse page. DO NOT merge it into a single-point result; doing so returns the entire national House roster and breaks D-04/RSLV-05.

getPoliticiansByArea(geoId, mtfcc) + pickHouseRep(records, cdGeoId) — the Phase 212 single-House-rep derivation (see essentialsLocationSearch.ts:54-61 & 170-201). pickHouseRep selects the one record whose `district_type === 'NATIONAL_LOWER'` AND `geo_id === cdGeoId`. Mirror this selection logic (inline it in the lib — do NOT import a routes/ helper into lib/, which would invert the dependency direction).

The statewide sub-query inside getRepresentativesByAddress filters `d.state = $1` with the 2-letter uppercase abbrev and already returns the state-scoped Senators/Governor/state-exec floor plus the President/VP/federal-judicial national rows — so the extracted core already carries the state floor.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Coordinate-validation module (US bbox + swap guard + 422 taxonomy)</name>
  <files>C:/EV-Accounts/backend/src/lib/coordinateValidation.ts, C:/EV-Accounts/backend/src/lib/coordinateValidation.test.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/src/routes/essentials.ts (lines 139-155: the GET /elections coordinate-validation + 422 precedent to mirror the code-string style of)
    - C:/EV-Accounts/backend/src/lib/coordinateValidation.test.ts (does not exist yet — create it)
    - .planning/phases/213-anonymous-coordinate-lookup-endpoint/213-CONTEXT.md (D-02, D-03, D-07)
  </read_first>
  <behavior>
    - classifyCoordinate(45.0, -93.0) (Minneapolis) → { ok: true } (continental valid)
    - classifyCoordinate(21.3, -157.8) (Honolulu, HI) → { ok: true }
    - classifyCoordinate(64.8, -147.7) (Fairbanks, AK) → { ok: true }
    - classifyCoordinate(52.9, 173.1) (Attu Island, Aleutians, POSITIVE longitude across the antimeridian) → { ok: true }
    - classifyCoordinate(18.2, -66.5) (San Juan, PR) → { ok: true }
    - classifyCoordinate(38.9, -77.0) (Washington DC) → { ok: true }
    - classifyCoordinate(51.5, -0.12) (London — valid coordinate, outside US box, swap also outside) → { ok: false, code: 'OUTSIDE_US_BOUNDS' }
    - classifyCoordinate(-93.0, 45.0) (Minneapolis entered swapped: (lat,lng) out of box but (lng,lat) in box; note lat magnitude 93 exceeds 90 yet must NOT be rejected as INVALID before the swap branch) → { ok: false, code: 'SWAPPED_COORDINATES' }
    - classifyCoordinate(NaN, -93.0) → { ok: false, code: 'INVALID_COORDINATES' }
    - classifyCoordinate(200, 5000) (absurd magnitude) → { ok: false, code: 'INVALID_COORDINATES' }
  </behavior>
  <action>
    Create `coordinateValidation.ts` exporting: (a) `US_BBOX` constant capturing a generous US envelope — latitude [17.5, 72.0] (PR/USVI south up through northern Alaska) and longitude accepted as the union of [-180.0, -64.0] (continental + AK + HI + PR/USVI) OR [172.0, 180.0] (the far-western Aleutian sliver that crosses the antimeridian into positive longitude, per the D-02 footgun); (b) a `CoordinateRejectionCode` union type of the three exact strings `'OUTSIDE_US_BOUNDS' | 'SWAPPED_COORDINATES' | 'INVALID_COORDINATES'`; (c) `classifyCoordinate(lat: number, lng: number)` returning `{ ok: true } | { ok: false; code: CoordinateRejectionCode }`.

    Implement an internal `inUsBox(lat, lng)` helper used twice. Evaluation ORDER is load-bearing (this is the blocker fix — the malformed guard must NOT pre-empt the swap branch):
      1. INVALID_COORDINATES ONLY for genuinely un-interpretable input: `!Number.isFinite(lat) || !Number.isFinite(lng) || Math.abs(lat) > 180 || Math.abs(lng) > 180`. Do NOT apply the strict per-axis lat∈[-90,90] check here — a swapped US coordinate legitimately has a "latitude" slot beyond ±90 (e.g. -93.0) and MUST fall through to the swap guard.
      2. If `inUsBox(lat, lng)` → `{ ok: true }`.
      3. Swap guard (D-03): else if `inUsBox(lng, lat)` → `SWAPPED_COORDINATES` (never silently query the swapped point).
      4. Else → `OUTSIDE_US_BOUNDS`.

    Territory note (WARNING 2 — make the choice explicit, not silent): document in a source comment that Guam (~13.4N,144.8E), American Samoa (~-14.3S,-170.7W) and CNMI are INTENTIONALLY EXCLUDED from US_BBOX because no politician/geofence data is seeded there yet (D-02 grants bbox-constant discretion); the box covers all 50 states + DC + PR/USVI.

    Write `coordinateValidation.test.ts` covering every case in <behavior> plus the Aleutian antimeridian branch AND an explicit assertion that the swapped-Minneapolis case (lat=-93.0) returns SWAPPED_COORDINATES and NOT INVALID_COORDINATES. This is TDD: write the failing tests first (RED), commit, then implement (GREEN). No DB, no HTTP, no imports from essentialsService.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx vitest run src/lib/coordinateValidation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/coordinateValidation.test.ts` passes with all <behavior> cases green, including `classifyCoordinate(-93.0, 45.0) === SWAPPED_COORDINATES` (proves the ordering fix).
    - Source grep confirms all three code strings exist exactly: `grep -c "OUTSIDE_US_BOUNDS\|SWAPPED_COORDINATES\|INVALID_COORDINATES" src/lib/coordinateValidation.ts` ≥ 3.
    - The Attu/Aleutian positive-longitude test (`classifyCoordinate(52.9, 173.1)` → ok) is present and passing.
    - The INVALID guard does NOT contain a bare `lat < -90 || lat > 90` per-axis reject before the swap branch (grep the ordering; the first guard keys off `Math.abs(...) > 180` / `Number.isFinite`).
    - A source comment explicitly names Guam/American Samoa/CNMI as intentionally excluded.
    - `grep -v '^\s*//' src/lib/coordinateValidation.ts | grep -c "geocode\|essentialsService"` returns 0 (module is standalone).
  </acceptance_criteria>
  <done>classifyCoordinate + US_BBOX exported and unit-tested; the swap guard is reached for swapped US coordinates (not shadowed by the malformed guard); malformed input maps to INVALID_COORDINATES; territory exclusion documented.</done>
</task>

<task type="auto">
  <name>Task 2: Coordinate-only service core — getRepresentativesByCoordinate (no geocode, exact House rep, state-scoped floor; D-04/D-05/D-06)</name>
  <files>C:/EV-Accounts/backend/src/lib/essentialsService.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/src/lib/essentialsService.ts (lines 565-870: getRepresentativesByAddress — the districtQueryText, statewideQueryText, tribalQueryText blocks, the ST_MakePoint($1=lng,$2=lat) footgun comments at 573-576 & 647-648 & 776, pickCountyFromDistrictRows, pickJurisdictionFromDistrictRows, and the final AddressSearchResult assembly at 804-869)
    - C:/EV-Accounts/backend/src/routes/essentialsLocationSearch.ts (THE precedent — the /resolve handler lines 54-61 pickHouseRep + 170-201: federal kept as a SEPARATE key, single House rep derived via getPoliticiansByArea(cdGeoId,'G5200') + pickHouseRep; copy this shape)
    - C:/EV-Accounts/backend/src/lib/essentialsBrowseService.ts (lines 39-56 FIPS_TO_ABBREV; 978-1026 getStatewideOfficials (state-scoped — USE); 1040+ getFederalOfficials (nationwide — DO NOT merge); getPoliticiansByArea)
    - .planning/phases/213-anonymous-coordinate-lookup-endpoint/213-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <action>
    Refactor so the coordinate→officials core of `getRepresentativesByAddress` (the districtQueryText + statewideQueryText + tribalQueryText execution + row-mapping + AddressSearchResult assembly, currently lines ~649-869) is reachable WITHOUT a Census geocode. Extract a shared private helper (e.g. `resolveOfficialsAtPoint(lngLat: { lng: number; lat: number }, stateAbbrev: string, matchedAddress: string)`) that both `getRepresentativesByAddress` and the new function call. Keep `getRepresentativesByAddress` behavior byte-for-byte identical after the extraction (it still geocodes, still passes its geocoded lng/lat/state/matchedAddress). Note the shared core's `statewideQueryText` already returns the STATE-SCOPED Senators/Governor/state-exec floor (plus President/VP/federal-judicial national rows) — this is the state floor; there is no need to also merge the nationwide roster.

    Add exported `async function getRepresentativesByCoordinate(lat: number, lng: number): Promise<AddressSearchResult>`:
      - MUST NOT call `geocodeAddress` (RSLV-04 / D-04). Do not import it into this path.
      - Derives the state: run the district+tribal `ST_Covers` queries on the point FIRST (remember: `ST_MakePoint($1::float8, $2::float8)` = (longitude, latitude), so `$1` = lng, `$2` = lat — NON-NEGOTIABLE). From a covering row prefer a `NATIONAL_LOWER` (congressional) or `G4020`/COUNTY row's geo_id and take its leading 2 chars as the state FIPS, then `FIPS_TO_ABBREV[fips]` → 2-letter uppercase abbrev. If NO covering district rows exist (valid-in-box but unseeded point), run a narrow `G4000` state-boundary `ST_Covers` lookup to still obtain the state FIPS.
      - Runs the shared core with the derived abbrev, which yields the point's covering officials INCLUDING the exact single US House member — the point falls inside exactly one congressional geofence, so its `ST_Covers` district query returns that one `NATIONAL_LOWER` row. This is the RSLV-05 "exact single US House rep" guarantee and it comes from the point's own coverage, NOT from any nationwide roster.
      - DO NOT call or merge `getFederalOfficials()` into `politicians` (BLOCKER 1). The nationwide federal roster must never appear in a single-point response.
      - State-scoped floor (D-05), applied ONLY as a genuine fallback (WARNING 1 — avoid the redundant extra round-trip): rely on the shared core's `statewideQueryText` rows for the normal seeded case; ONLY when the core returns zero state-scoped rows (unseeded state) call `getStatewideOfficials(abbrev)` and union those records (de-duplicated by politician `id`) so the result still carries Senators + Governor/state execs.
      - Single-House-rep fallback for the genuinely-unseeded case: if the point's `ST_Covers` district rows contain zero `NATIONAL_LOWER` rows but a congressional geo_id (cdGeoId) is known, derive the one rep via `getPoliticiansByArea(cdGeoId, 'G5200')` then select the record where `district_type === 'NATIONAL_LOWER' && geo_id === cdGeoId` (inline the pickHouseRep logic — do NOT import the routes/ helper into lib/). Add at most that one rep — never the whole area roster.
      - Sets `matchedAddress: ''` (D-06 — there is no reverse geocode; never echo or derive a location label from the raw point). The raw lat/lng MUST NOT appear anywhere in the returned object.
      - Returns the same `AddressSearchResult` shape (politicians, jurisdiction, matchedAddress, tribal_land, county, jurisdictionGeoIds) so Phase 214 consumes it identically to the address path.
    Keep the ST_Covers queries GIST-index-driven (no wrapping functions on `gb.geometry`; see project_geofence_overlap_perf). No fenced code blocks in comments; keep the coordinate-order footgun comment intact on the new path.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit && grep -c "export async function getRepresentativesByCoordinate" src/lib/essentialsService.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (clean typecheck after the refactor).
    - `grep -c "export async function getRepresentativesByCoordinate" src/lib/essentialsService.ts` == 1.
    - Within the getRepresentativesByCoordinate path, `grep` confirms NO call to `geocodeAddress` (no Census on this path).
    - `grep` confirms getFederalOfficials() is NOT called on the getRepresentativesByCoordinate path (the nationwide roster is never merged into politicians).
    - `grep` confirms the new path references `getStatewideOfficials` (used only as the zero-rows state floor fallback) and the single-House-rep selection filters on `NATIONAL_LOWER` + matching geo_id.
    - The `ST_MakePoint($1::float8, $2::float8)` order is preserved with `$1`=lng, `$2`=lat (order-footgun comment present on the new path).
    - Existing getRepresentativesByAddress tests (if any) still pass: `npx vitest run src/lib/essentialsService.test.ts` (skip cleanly if that test file does not exist).
  </acceptance_criteria>
  <done>getRepresentativesByCoordinate exported, compiles, resolves officials via ST_Covers with no geocode, returns the point's exact single US House rep from its own coverage, applies the state-scoped floor only as a fallback, never merges the nationwide getFederalOfficials roster, and returns an empty matchedAddress carrying no raw coordinate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → coordinateValidation | Untrusted numeric `(lat, lng)` classified before any DB access |
| service → PostGIS | Coordinate crosses into a parameterized ST_Covers query |
| service → response object | Raw coordinate must NOT leak into the returned AddressSearchResult |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | Tampering | getRepresentativesByCoordinate DB query | mitigate | lat/lng bound as `$1::float8/$2::float8` parameters to `ST_MakePoint` — never string-concatenated into SQL; classifyCoordinate rejects non-finite/absurd input upstream |
| T-213-02 | Information Disclosure | AddressSearchResult returned to caller | mitigate | `matchedAddress: ''`; AddressSearchResult carries no lat/lng field (verified interface); raw coordinate never placed in the returned object (D-06) |
| T-213-03 | Denial of Service | unbounded ST_Covers on a public path | accept (mitigated in Plan 02) | validation short-circuits out-of-US points before any query; per-request rate-limit applied at the route layer in Plan 02 |
| T-213-04 | Spoofing | swapped lat/lng silently resolving a wrong point | mitigate | distinct SWAPPED_COORDINATES branch (D-03), reachable for swapped US coordinates (ordering fix), never queries the swapped point as if valid |
</threat_model>

<verification>
- `npx vitest run src/lib/coordinateValidation.test.ts` green (incl. swapped-Minneapolis → SWAPPED_COORDINATES).
- `npx tsc --noEmit` clean across the backend.
- getRepresentativesByCoordinate exists, exported, geocode-free, does NOT merge getFederalOfficials, applies state floor only as fallback, empty matchedAddress.
</verification>

<success_criteria>
- classifyCoordinate distinguishes valid / OUTSIDE_US_BOUNDS / SWAPPED_COORDINATES / INVALID_COORDINATES with Aleutian-antimeridian coverage and a swap guard the malformed check does not shadow.
- getRepresentativesByCoordinate resolves officials via ST_Covers with no Census geocode, yields the exact single US House rep for the point, guarantees a state-scoped floor, echoes no raw coordinate, and never returns the nationwide federal roster.
</success_criteria>

<output>
Create `.planning/phases/213-anonymous-coordinate-lookup-endpoint/213-01-SUMMARY.md` when done.
</output>
