---
phase: 100-va-tiger-geofences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts
  - C:/EV-Accounts/backend/scripts/verify-va-tiger-import.sql
  - C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts
autonomous: true
requirements:
  - VA-GEO-01
  - VA-GEO-02
  - VA-GEO-03

must_haves:
  truths:
    - "Running the loader with --state VA --fips 51 --dry-run does not throw 'layer not in allowlist'"
    - "The verify SQL script exists at C:/EV-Accounts/backend/scripts/verify-va-tiger-import.sql with state='51' filters and OR-direction Gate 7"
    - "The smoke test script exists at C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts with 3 VA addresses (Alexandria, Richmond, rural Shenandoah)"
    - "The smoke test references Alexandria coordinates with expectedGeoIds for both G4110='5101000' AND G4020='51510' (VA-GEO-02 dual-tier invariant)"
    - "EXPECTED_VA_MTFCC.sldl and EXPECTED_VA_MTFCC.place are non-zero confirmed counts (from dry-run) after this plan completes"
    - "smoke-va-geofences.ts expectedCounts.G5220 matches EXPECTED_VA_MTFCC.sldl and expectedCounts.G4110 matches EXPECTED_VA_MTFCC.place"
    - "Full 5-layer dry-run prints exactly 5 'VA MTFCC pre-flight assertion PASSED' lines with no MtfccAssertionError"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts"
      provides: "VA entries in STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, STATE_RUN_MAKEVALID, plus EXPECTED_VA_MTFCC block in processLayer() with confirmed non-zero counts for all 5 layers"
      contains: "VA: new Set(['cd119', 'sldu', 'sldl', 'place', 'county'])"
    - path: "C:/EV-Accounts/backend/scripts/verify-va-tiger-import.sql"
      provides: "7-gate SQL verification script for state='51'; Gate 4 Alexandria dual-tier; Gate 6 Fairfax County+city; Gate 7 OR-direction (geofence -> districts)"
      contains: "state = '51'"
    - path: "C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts"
      provides: "3-address TypeScript smoke test (Alexandria/Richmond/rural Shenandoah) exiting 0/1"
      contains: "smoke-va-geofences"
  key_links:
    - from: "load-state-tiger-boundaries.ts STATE_LAYER_ALLOWLIST"
      to: "processLayer() EXPECTED_VA_MTFCC block"
      via: "fipsArg === '51' guard"
      pattern: "fipsArg === '51'"
    - from: "smoke-va-geofences.ts TEST_ADDRESSES[0]"
      to: "expectedGeoIds assertion"
      via: "G4110: '5101000', G4020: '51510'"
      pattern: "expectedGeoIds"
    - from: "Plan 01 Task 3 dry-run MtfccAssertionError 'got N' output"
      to: "EXPECTED_VA_MTFCC.sldl and EXPECTED_VA_MTFCC.place final values"
      via: "manual edit replacing sentinel 0 with confirmed N"
      pattern: "got \\d+"
---

<objective>
Add Virginia (FIPS 51) to the generalized TIGER loader, create the verification and smoke test scripts, and discover the actual SLDL and G4110 polygon counts via dry-run — leaving the scaffold fully armed for the live load in Plan 02.

Purpose: Plan 02 (the live data load) requires (a) the loader to recognize VA, (b) the verify+smoke scripts to exist, and (c) the EXPECTED_VA_MTFCC pre-flight counts to match the actual TIGER 2024 file contents. The sentinel-0 dry-run trick surfaces the actual sldl and place counts without writing any data to the database.

Output: 4 additive edits to load-state-tiger-boundaries.ts (with confirmed non-zero counts for all 5 layers); 2 new scripts (verify-va-tiger-import.sql, smoke-va-geofences.ts) with matching confirmed counts; no database rows written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-va-tiger-geofences/100-RESEARCH.md
@.planning/phases/100-va-tiger-geofences/100-PATTERNS.md
@.planning/phases/100-va-tiger-geofences/100-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VA to TIGER loader (4 additive edits — allowlist, city-assert, makevalid, EXPECTED_VA_MTFCC block)</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Read before editing. Per PATTERNS.md, the 4 insertion points are:
      (1) STATE_LAYER_ALLOWLIST at line ~43, after the MD entry, before the DC entry;
      (2) STATE_CITY_ASSERTIONS at line ~94, after the MD entry, before the closing brace;
      (3) STATE_RUN_MAKEVALID at line ~107, after the MD entry, before the DC entry;
      (4) EXPECTED_VA_MTFCC block in processLayer() — after the MD block ending at line ~905, before the DC block starting at line ~908.
    FIPS_TO_STATE already has '51': 'va' — no change needed there.
    .planning/phases/100-va-tiger-geofences/100-PATTERNS.md — Contains the exact 4 edit blocks to insert verbatim (Edit 1, Edit 2, Edit 3, Edit 4).
    .planning/phases/100-va-tiger-geofences/100-RESEARCH.md — EXPECTED_VA_MTFCC values: cd119=11, sldu=40, sldl=0 (sentinel), place=0 (sentinel), county=133 (95 counties + 38 independent cities). The sentinel 0s are intentional — Task 3 dry-run reveals actual counts.
  </read_first>
  <action>
    Make exactly 4 additive edits to load-state-tiger-boundaries.ts. No existing lines are removed; all existing state blocks (UT/TX/MA/ME/OR/MD/DC) are preserved verbatim.

    Edit 1 — STATE_LAYER_ALLOWLIST (insert after the MD entry, before the DC entry):
      VA: new Set(['cd119', 'sldu', 'sldl', 'place', 'county']),
    Note: cd119 key (not cd) — matches the established TIGER 2024 dispatch key. No cousub — Virginia uses independent cities, not townships.

    Edit 2 — STATE_CITY_ASSERTIONS (insert after the MD entry, before the closing brace of the const):
      VA: ['Alexandria city'],
    The sentinel string must match TIGER NAMELSAD exactly (lowercase 'city'). If dry-run output shows different casing, fail the task and report the actual NAMELSAD values seen — do not silently mutate to other casings.

    Edit 3 — STATE_RUN_MAKEVALID (insert after the MD entry, before the DC entry):
      VA: new Set(['cd119', 'sldu', 'sldl', 'place', 'county']),
    All 5 VA layers get ST_MakeValid — identical to ME, OR, MD.

    Edit 4 — EXPECTED_VA_MTFCC block (insert after the MD block closing brace, before the DC block opening `if (fipsArg === '11')`). The block must have:
      - Guard: if (fipsArg === '51') { ... }
      - Record literal EXPECTED_VA_MTFCC with all 5 keys:
          cd119: 11 (11 VA congressional districts — already exist as G5200 rows; assertion validates shapefile count)
          sldu: 40 (40 VA Senate districts, 1 senator each)
          sldl: 0 (SENTINEL — Task 3 dry-run reveals actual count; expected ~100 single-member districts)
          place: 0 (SENTINEL — Task 3 dry-run reveals actual count; TIGERweb shows 433 G4110 but G4110-only filter may differ)
          county: 133 (95 counties + 38 independent cities)
      - if (layer in EXPECTED_VA_MTFCC) dispatch that re-streams the shapefile with IDENTICAL filter logic to the upsert pass (filterByStatefp, place MTFCC===G4110 guard, districtNumField skipDistrictCodes)
      - Named MtfccAssertionError thrown when actualCount !== expected, with message: `[VA MTFCC assertion] layer=${layer}: expected ${expected} records, got ${actualCount}. TIGER file: ${url}. Aborting before any DB write — verify TIGER 2024 FIPS 51 file is correct.`
      - console.log on PASS: `  [${layer}] VA MTFCC pre-flight assertion PASSED: ${actualCount} records (expected ${expected}).`

    Copy the structural skeleton from the MD block at lines ~863-905 verbatim. Only change: fipsArg guard value (51), Record name (EXPECTED_VA_MTFCC), error message label ([VA MTFCC assertion]), and the per-key counts above. VA has no cousub layer — do not add a FUNCSTAT guard for VA.

    The sldl: 0 and place: 0 values are intentional integer sentinels (not TBD keyword which would break TypeScript). Task 3 dry-run will fire MtfccAssertionError revealing actual N. Do NOT skip Task 3 — Plan 02's live load depends on confirmed non-zero values being in place.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit --skipLibCheck scripts/load-state-tiger-boundaries.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - load-state-tiger-boundaries.ts STATE_LAYER_ALLOWLIST contains literal `VA: new Set(['cd119', 'sldu', 'sldl', 'place', 'county'])`
    - load-state-tiger-boundaries.ts STATE_CITY_ASSERTIONS contains literal `VA: ['Alexandria city']`
    - load-state-tiger-boundaries.ts STATE_RUN_MAKEVALID contains literal `VA: new Set(['cd119', 'sldu', 'sldl', 'place', 'county'])`
    - The file contains exactly one occurrence of `if (fipsArg === '51')` introducing the EXPECTED_VA_MTFCC block
    - The EXPECTED_VA_MTFCC Record contains all 5 keys: cd119, sldu, sldl, place, county with integer values 11, 40, 0, 0, 133 respectively
    - The error thrown inside the block has name 'MtfccAssertionError' and message containing '[VA MTFCC assertion]'
    - `npx tsc --noEmit --skipLibCheck scripts/load-state-tiger-boundaries.ts` exits 0 (no TypeScript errors)
    - Existing MD/OR/ME blocks are still present (grep for `EXPECTED_MD_MTFCC`, `EXPECTED_OR_MTFCC` still returns matches — no regressions)
  </acceptance_criteria>
  <done>VA is registered in the 3 config blocks and the EXPECTED_VA_MTFCC pre-flight assertion exists with sentinel 0s for sldl/place. TypeScript compiles cleanly. Task 3 will replace the sentinel 0s with confirmed counts.</done>
</task>

<task type="auto">
  <name>Task 2: Create verify-va-tiger-import.sql and smoke-va-geofences.ts (adapt from MD/OR analogs)</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/verify-or-tiger-import.sql — Reference for Gate 7 direction (geofence -> districts). VA must use OR-direction, NOT MD-direction.
    C:/EV-Accounts/backend/scripts/verify-md-tiger-import.sql — Reference for Gate structure (1-7) and Baltimore City dual-tier pattern in Gate 4 (mapped to Alexandria for VA).
    C:/EV-Accounts/backend/scripts/smoke-md-geofences.ts — Full template for smoke-va-geofences.ts. Copy structure (imports, AddressTest interface, queryBoundaries function, main() flow, error accumulation).
    .planning/phases/100-va-tiger-geofences/100-PATTERNS.md — Contains the complete SQL file body (Gates 1-7) and the TypeScript blocks (TEST_ADDRESSES, queryBoundaries, SC3 expectedCounts, target city lookup, file header).
    .planning/phases/100-va-tiger-geofences/100-RESEARCH.md — Confirms Alexandria geo_ids (G4110='5101000', G4020='51510'), VA-8 Beyer routing (G5200='5108'), and smoke test address coordinates.
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/scripts/verify-va-tiger-import.sql by adapting verify-md-tiger-import.sql with these specific substitutions (every gate must be present; no gate omitted):
      - Header: `-- Phase 100 VA TIGER import verification` and `Run via: psql $DATABASE_URL -f backend/scripts/verify-va-tiger-import.sql`
      - All `state = '24'` filters changed to `state = '51'`
      - Gate 1: invalid geometry count for state='51'; expected 0
      - Gate 2: GeometryCollection check for state='51'; expected 0
      - Gate 3: Per-layer row counts; expected comment `-- Expected: G4020|133, G4110|[DRY-RUN-COUNT], G5200|11, G5210|40, G5220|[DRY-RUN-COUNT]` — leave [DRY-RUN-COUNT] as a literal comment placeholder (the verify SQL is run after the live load in Plan 02; the executor checks the actual rows in DB against EXPECTED_VA_MTFCC values from the loader)
      - Gate 4 (Alexandria dual-tier — VA-GEO-02 invariant): `SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='51' AND geo_id IN ('5101000', '51510') ORDER BY mtfcc;` — expected 2 rows: geo_id='51510' G4020 + geo_id='5101000' G4110, both named 'Alexandria city'
      - Gate 5: districts table counts WHERE state IN ('VA', 'va'); expected: va|COUNTY|133, va|STATE_LOWER|[DRY-RUN], va|STATE_UPPER|40, VA|NATIONAL_LOWER|11 (cd119 uses uppercase 'VA' per abbrevUpper)
      - Gate 6 (Fairfax County sentinel — confirms county layer; NOTE Fairfax County AND Fairfax city are SEPARATE entities in VA — independent city pattern): `SELECT geo_id, name, mtfcc FROM essentials.geofence_boundaries WHERE state='51' AND mtfcc='G4020' AND name LIKE '%Fairfax%' ORDER BY name;` — expected 2 rows: geo_id='51059' 'Fairfax County' + geo_id='51600' 'Fairfax city'
      - Gate 7 (section-split — MUST use OR-direction, NOT MD-direction): `SELECT gb.geo_id, gb.name, gb.mtfcc FROM essentials.geofence_boundaries gb WHERE gb.mtfcc IN ('G5200','G5210','G5220','G4020') AND gb.state='51' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts) LIMIT 10;` — expected 0 rows. Include an SQL comment explaining why OR-direction is required: the pre-existing NATIONAL_UPPER row (geo_id='51', no polygon) would produce a false positive under the MD-direction (districts -> geofence) query.

    Create C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts by adapting smoke-md-geofences.ts in full. Copy verbatim (no VA-specific change): the `import { Client } from 'pg'` and `dotenv` imports; the AddressTest interface; the main() function structure with client.connect/try/finally/client.end; the per-address loop logic (expectedMtfcc check, forbiddenMtfcc check, expectedGeoIds check, NULL name check); the error accumulation pattern; the final exit 0/1 branching. Apply these VA-specific substitutions:
      - File header comment block: `smoke-va-geofences.ts` / `Phase 100: Virginia geofences smoke test` / Usage `npx tsx scripts/smoke-va-geofences.ts` from C:/EV-Accounts/backend; SC1-SC4 describe Alexandria dual-tier, rural Shenandoah no-G4110, layer counts, and non-NULL names across all tiers
      - queryBoundaries function: `state = '51'` (not '24')
      - TEST_ADDRESSES array — replace MD addresses entirely with the 3 VA addresses from PATTERNS.md:
          Address 1: label='Alexandria VA City Hall', lon=-77.0469, lat=38.8048, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220'], expectedGeoIds={G4110:'5101000', G4020:'51510', G5200:'5108'}
          Address 2: label='Rural Shenandoah County VA (unincorporated)', lon=-78.6, lat=38.9, expectedMtfcc=['G4020','G5200','G5210','G5220'], forbiddenMtfcc=['G4110']
          Address 3: label='Richmond VA City Hall', lon=-77.4360, lat=37.5407, expectedMtfcc=['G4020','G4110','G5200','G5210','G5220']
      - SC3 expectedCounts block (state='51' query): G4020:133, G4110:0, G5200:11, G5210:40, G5220:0. The 0 values for G4110 and G5220 are intentional placeholders — Task 3 dry-run reveals actual counts and updates these here in this file. With 0 as expected, SC3 fails loudly until updated (prevents silent skip)
      - Target city lookup block: query for VA cities `'Alexandria city', 'Richmond city', 'Norfolk city'` in state='51' G4110 rows
      - Final result console.log block: reference Phase 100 success criteria SC1-SC4 using Alexandria/Shenandoah/Richmond language

    Do NOT inline the loader file contents into the smoke test. The smoke test only queries Postgres and asserts results.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit --skipLibCheck scripts/smoke-va-geofences.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/verify-va-tiger-import.sql` exists
    - verify-va-tiger-import.sql contains `state = '51'` (multiple gates) and contains NO `state = '24'` or `state = '41'` (no leftover MD/OR text)
    - verify-va-tiger-import.sql Gate 4 contains literal `geo_id IN ('5101000', '51510')` (VA-GEO-02 dual-tier sentinel)
    - verify-va-tiger-import.sql Gate 6 contains `name LIKE '%Fairfax%'` and expects 2 rows (independent city pattern)
    - verify-va-tiger-import.sql Gate 7 uses OR-direction: `FROM essentials.geofence_boundaries gb ... WHERE ... AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts)` (NOT the inverse direction)
    - File `C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts` exists
    - smoke-va-geofences.ts contains `state = '51'` in queryBoundaries and SC3
    - smoke-va-geofences.ts TEST_ADDRESSES[0] has expectedGeoIds with both `G4110: '5101000'` AND `G4020: '51510'` (VA-GEO-02 dual-tier invariant)
    - smoke-va-geofences.ts TEST_ADDRESSES[1] (rural Shenandoah) has `forbiddenMtfcc: ['G4110']`
    - smoke-va-geofences.ts imports from 'pg' and 'dotenv' (same as MD analog)
    - `npx tsc --noEmit --skipLibCheck scripts/smoke-va-geofences.ts` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>verify-va-tiger-import.sql and smoke-va-geofences.ts both exist; TypeScript compiles cleanly; SQL contains state='51' with Gate 4 Alexandria dual-tier and Gate 7 OR-direction; smoke test asserts Alexandria dual-tier (VA-GEO-02 invariant).</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run all 5 VA TIGER layers; record actual sldl/place counts; update sentinels in loader and smoke test</name>
  <read_first>
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Confirm EXPECTED_VA_MTFCC.sldl=0 and EXPECTED_VA_MTFCC.place=0 sentinels from Task 1 are present. Confirm fipsArg === '51' guard is in place.
    C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts — Confirm expectedCounts.G4110=0 and expectedCounts.G5220=0 sentinels from Task 2 are present.
    .planning/phases/100-va-tiger-geofences/100-RESEARCH.md — Pitfall 2 ("SLDL Count = Delegate Count is NOT True for VA, Unlike MD") and Pitfall 6 ("TIGERweb G4110 Count vs Actual TIGER File Count") — sldl is expected ~100 (single-member districts); place may differ from TIGERweb's 433.
  </read_first>
  <action>
    From C:/EV-Accounts/backend, run the dry-run command for all 5 VA layers (Plan 01 Task 5 in VALIDATION.md):

      npx tsx scripts/load-state-tiger-boundaries.ts --state VA --fips 51 --layers cd119,sldu,sldl,place,county --dry-run

    The loader will:
      1. Download all 5 TIGER zip files from census.gov to the local cache (cd119 1.7MB, sldu 3.1MB, sldl 4.8MB, place 3.6MB, county 80MB national)
      2. Run the STATE_CITY_ASSERTIONS gate for the place layer — confirm 'Alexandria city' is found in the NAMELSAD values seen
      3. Run the EXPECTED_VA_MTFCC pre-flight assertion for each layer
      4. NOT write to the database (--dry-run flag)

    Expected outcomes per layer:
      - cd119: PASSES with `[cd119] VA MTFCC pre-flight assertion PASSED: 11 records (expected 11)`
      - sldu:  PASSES with `[sldu] VA MTFCC pre-flight assertion PASSED: 40 records (expected 40)`
      - sldl:  THROWS `MtfccAssertionError [VA MTFCC assertion] layer=sldl: expected 0 records, got N`
      - place: THROWS `MtfccAssertionError [VA MTFCC assertion] layer=place: expected 0 records, got N`
      - county: PASSES with `[county] VA MTFCC pre-flight assertion PASSED: 133 records (expected 133)`

    Record the actual N values from the sldl and place MtfccAssertionError messages. Expected ranges per RESEARCH.md and PATTERNS.md:
      - sldl: ~100 (Virginia has 100 single-member House of Delegates districts — DIFFERENT from MD which had 71 polygons for 141 delegates)
      - place: unknown actual G4110-only count; TIGERweb shows 433 (all incorporated places including non-G4110); the actual G4110-only count after the loader filter is expected somewhere between 100 and 250

    Stop and investigate if any of these red flags appear:
      - sldl actual count is NOT in the range [95, 105] (Virginia has exactly 100 districts; small deviation possible if TIGER includes a few special-purpose records, but a wildly different count means investigation is required)
      - place actual count is below 50 or above 450 (TIGERweb upper bound is 433)
      - county throws MtfccAssertionError (would mean 133 is wrong — should be 95 + 38 = 133 exactly)
      - STATE_CITY_ASSERTIONS gate fails for 'Alexandria city' (casing mismatch — record the actual NAMELSAD values seen and stop)

    After confirming the actual counts, apply two follow-up edits:

      Edit A (load-state-tiger-boundaries.ts): In the EXPECTED_VA_MTFCC Record, replace `sldl: 0,` with `sldl: N,` (where N is the actual sldl count from the dry-run) and replace `place: 0,` with `place: N,` (where N is the actual place count from the dry-run). Add a line comment next to each updated entry: `// confirmed via dry-run YYYY-MM-DD — N VA G5220 single-member House of Delegates polygons` and `// confirmed via dry-run YYYY-MM-DD — N VA G4110 incorporated places`.

      Edit B (smoke-va-geofences.ts): In the expectedCounts Record (SC3 block), replace `G4110: 0,` with `G4110: N,` (matching EXPECTED_VA_MTFCC.place) and replace `G5220: 0,` with `G5220: N,` (matching EXPECTED_VA_MTFCC.sldl). The two files must stay in sync — they reference the same underlying TIGER counts.

    After both edits, re-run the full 5-layer dry-run to confirm all 5 assertions now PASS:

      npx tsx scripts/load-state-tiger-boundaries.ts --state VA --fips 51 --layers cd119,sldu,sldl,place,county --dry-run

    Expect 5 "VA MTFCC pre-flight assertion PASSED" lines with no MtfccAssertionError. If any layer fails, the recorded counts are wrong — re-read the error message and re-apply Edit A.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/load-state-tiger-boundaries.ts --state VA --fips 51 --layers cd119,sldu,sldl,place,county --dry-run 2>&1 | grep -c "VA MTFCC pre-flight assertion PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - First dry-run (with sentinel 0s) prints MtfccAssertionError for sldl with `got N` where N is in range [95, 105]
    - First dry-run prints MtfccAssertionError for place with `got N` where N is between 50 and 450
    - First dry-run prints PASSED for cd119 (11 records), sldu (40 records), county (133 records)
    - First dry-run prints `STATE_CITY_ASSERTIONS gate passed` for 'Alexandria city' (or equivalent confirmation; if it fails, the executor stops)
    - load-state-tiger-boundaries.ts EXPECTED_VA_MTFCC.sldl is now a non-zero integer (the recorded N); place is now a non-zero integer (the recorded N); each entry has a `// confirmed via dry-run` line comment
    - smoke-va-geofences.ts expectedCounts.G4110 equals EXPECTED_VA_MTFCC.place; expectedCounts.G5220 equals EXPECTED_VA_MTFCC.sldl
    - Re-run full 5-layer dry-run command output contains exactly 5 occurrences of "VA MTFCC pre-flight assertion PASSED" (one per layer) and 0 occurrences of "MtfccAssertionError"
    - The verify command above prints exactly the integer 5
  </acceptance_criteria>
  <done>EXPECTED_VA_MTFCC has confirmed non-zero counts for all 5 layers (no sentinel 0 remaining); smoke-va-geofences.ts expectedCounts matches; full 5-layer dry-run passes all pre-flight assertions with zero MtfccAssertionErrors. Plan 02 live load is now unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TIGER scripts -> filesystem | New script files written to EV-Accounts/backend/scripts/ |
| loader -> census.gov | HTTPS download of TIGER zip files during dry-run (read-only; no DB writes) |
| dry-run MtfccAssertionError output -> code edits | Actual sldl/place counts surfaced in error messages flow into code as literal integers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | load-state-tiger-boundaries.ts edits | mitigate | 4 additive inserts only; no deletions; OR/ME/MD blocks preserved verbatim; TypeScript compile check (npx tsc --noEmit) gates Task 1 acceptance |
| T-100-02 | Information Disclosure | DATABASE_URL in smoke test | accept | DATABASE_URL already used in existing OR/MD smoke tests; dotenv.config() pattern; no new exposure surface; Plan 01 does not run the smoke test against production |
| T-100-03 | Denial of Service | sldl/place set to 0 (sentinel) without follow-up | mitigate | Task 3 explicitly gates: 5-layer dry-run must print 5 PASSED lines before Plan 01 is done; sentinel 0 left in place would fail Plan 02 immediately on first layer |
| T-100-04 | Integrity | Wrong sldl count written if not in expected range | mitigate | Task 3 acceptance criteria gate: N must be in range [95, 105] for sldl and [50, 450] for place; executor halts and documents discrepancy before edit |
| T-100-05 | Tampering | TIGER zip files from census.gov | accept | census.gov is the authoritative US government source; files are read-only during dry-run; no DB writes occur; HTTPS download with redirect following is established pattern |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies pre-existing in EV-Accounts/backend (pg, dotenv, shapefile, adm-zip) |
</threat_model>

<verification>
After all 3 tasks complete:
1. `grep -c "VA: new Set" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns 2 (one for STATE_LAYER_ALLOWLIST, one for STATE_RUN_MAKEVALID)
2. `grep -c "VA: \['Alexandria city'\]" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns 1
3. `grep -c "fipsArg === '51'" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts` — returns 1
4. `grep -n "sldl:" C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts | grep -v "// "` — the EXPECTED_VA_MTFCC.sldl line contains a non-zero integer (not `sldl:   0,`)
5. `ls C:/EV-Accounts/backend/scripts/verify-va-tiger-import.sql` — file exists
6. `ls C:/EV-Accounts/backend/scripts/smoke-va-geofences.ts` — file exists
7. `npx tsc --noEmit --skipLibCheck scripts/smoke-va-geofences.ts` from C:/EV-Accounts/backend — exits 0
8. Full 5-layer dry-run command: `npx tsx scripts/load-state-tiger-boundaries.ts --state VA --fips 51 --layers cd119,sldu,sldl,place,county --dry-run` prints exactly 5 "PASSED" lines and zero "MtfccAssertionError" lines
</verification>

<success_criteria>
- VA is registered in STATE_LAYER_ALLOWLIST, STATE_CITY_ASSERTIONS, STATE_RUN_MAKEVALID with the correct 5-layer set (cd119 key, no cousub)
- EXPECTED_VA_MTFCC block inserted after MD block; all 5 keys present (cd119, sldu, sldl, place, county); ALL values are confirmed non-zero integers after Task 3
- verify-va-tiger-import.sql created with all 7 gates: Gate 1/2 geometry checks for state='51'; Gate 3 per-layer counts; Gate 4 Alexandria dual-tier sentinel; Gate 5 districts table casing; Gate 6 Fairfax County+city; Gate 7 OR-direction section-split
- smoke-va-geofences.ts created and TypeScript-clean; TEST_ADDRESSES[0] asserts both G4110='5101000' AND G4020='51510' for Alexandria (VA-GEO-02 dual-tier invariant); TEST_ADDRESSES[1] forbids G4110 for rural Shenandoah; TEST_ADDRESSES[2] asserts Richmond returns G4110+G4020
- Full 5-layer dry-run passes all pre-flight assertions (no MtfccAssertionError); loader and smoke test expectedCounts are in sync for G4110/G5220
- No database rows written in Plan 01 (dry-run only)
</success_criteria>

<output>
Create `.planning/phases/100-va-tiger-geofences/100-01-SUMMARY.md` when done. Record the confirmed sldl and place counts (sldl=N, place=N) in the summary — Plan 02 references these values when checking the live load row counts.
</output>
