---
phase: 161-clark-county-commission-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-clark-county-commission-headshots.py
  - C:/EV-Accounts/backend/migrations/1056_clark_county_commission_headshots.sql
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: true
requirements: [CLARK-01]
user_setup: []

must_haves:
  truths:
    - "All 7 commissioners (minus any documented genuine gap) have a 600x750 headshot in the politician_photos bucket"
    - "Each headshot is crop-to-4:5-then-resized (not stretched) and re-encoded clean JPEG"
    - "Each headshot has a politician_images row (type='default') pointing at its CDN URL"
    - "Clark County is surfaced with the purple hasContext chip in src/lib/coverage.js COVERAGE_COUNTIES"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-clark-county-commission-headshots.py"
      provides: "Download (clarkcountynv.gov AEM) → crop-4:5 → resize 600x750 Lanczos q90 → Storage upsert pipeline; runtime UUID resolve by external_id; 7-row manifest"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1056_clark_county_commission_headshots.sql"
      provides: "Audit-only politician_images INSERTs (one per commissioner with an uploaded headshot), columns (id, politician_id, url, type, photo_license), NOT registered"
      contains: "essentials.politician_images"
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "COVERAGE_COUNTIES entry for Clark County (browseGovernmentList:['32003'], browseStateAbbrev:'NV', hasContext:true)"
      contains: "Clark County"
  key_links:
    - from: "_tmp-clark-county-commission-headshots.py"
      to: "essentials.politicians (external_id -3200301..-3200307)"
      via: "runtime psycopg2 lookup SELECT id WHERE external_id=%s"
      pattern: "external_id = %s"
    - from: "1056_clark_county_commission_headshots.sql"
      to: "politician_photos CDN ({uuid}-headshot.jpg)"
      via: "politician_images.url literal"
      pattern: "politician_photos"
    - from: "src/lib/coverage.js COVERAGE_COUNTIES"
      to: "Clark County browse (geo_id 32003)"
      via: "browseGovernmentList array entry"
      pattern: "32003"
---

<objective>
Acquire and install all 7 commissioner headshots and surface Clark County in coverage. Write the gitignored `_tmp-clark-county-commission-headshots.py` pipeline (download the clarkcountynv.gov AEM `/original/as/` portrait, crop-to-4:5 then resize to 600x750, upload to Storage), write the audit-only migration 1056 that records the politician_images rows, and add the Clark County entry to `src/lib/coverage.js` COVERAGE_COUNTIES (the one committable-in-essentials change). The script resolves each commissioner's UUID at runtime by external_id (UUIDs minted by Plan 01's migration 1055). Any portrait that cannot be sourced is left empty and documented as a genuine gap — no fabrication (D-07 / SC#2).

Purpose: Satisfies the headshot half of CLARK-01 (Success Criterion 2 — all 7 commissioners render with 600x750 headshots, gaps documented) + the surfacing half (Success Criterion 4 — purple hasContext chip).
Output: _tmp-clark-county-commission-headshots.py (pipeline), 1056_clark_county_commission_headshots.sql (audit-only), the uploaded 600x750 JPEGs in politician_photos, and the coverage.js COVERAGE_COUNTIES entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and cannot upload to Storage):**
- The executor WRITES `_tmp-clark-county-commission-headshots.py`, `1056_clark_county_commission_headshots.sql`, and the `coverage.js` edit to disk and documents the exact run/apply steps. The executor does NOT run the script, upload to Storage, or apply the migration.
- Running the headshot script (download + crop/resize + Storage PUT), capturing the manifest, and applying 1056 via `mcp__supabase-local__execute_sql` (or psql -f) are INLINE ORCHESTRATOR steps performed by execute-phase.
- Each task marks executor-write vs inline-orchestrator-apply.
- The migration + script live in the SEPARATE repo C:/EV-Accounts/backend/ (script is gitignored `_tmp-*`). The coverage.js edit lives in the essentials repo (committable normally). SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/161-clark-county-commission-deep-seed/161-RESEARCH.md
@.planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md

<interfaces>
<!-- Research-verified facts the executor needs. Do NOT re-derive. -->
Storage bucket: politician_photos
CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
Storage path: {politician_uuid}-headshot.jpg
TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS
politician_images columns: (id, politician_id, url, type, photo_license) — NO photo_origin_url; type='default'
photo_license default (A2): 'us_government_work' (clarkcountynv.gov county government portraits; 'public_domain' also defensible)
Env (C:/EV-Accounts/backend/.env): SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL
The 7 external_id→UUID map is captured in 161-01-SUMMARY.md (minted by migration 1055)

7 commissioner AEM source URLs (all verified HTTP 200, no WAF, no special UA — clarkcountynv.gov, 175x175 source):
  -3200301 Michael Naft        https://www.clarkcountynv.gov/adobe/assets/urn:aaid:aem:216e1b45-bc22-4f67-9ddb-6af03b44cf8c/original/as/commissioner-naft-dist-a.jpg
  -3200302 Marilyn Kirkpatrick https://www.clarkcountynv.gov/adobe/assets/urn:aaid:aem:6e201ef6-6aaa-47bc-9ea3-1c54202ebd69/original/as/commissioner-kirkpatrick-dist-b.jpg
  -3200303 April Becker        https://www.clarkcountynv.gov/adobe/assets/urn:aaid:aem:d2f2d7c7-5d31-4d0b-9a02-423ccce4d989/original/as/commissioner-becker-dist-c.jpg
  -3200304 William McCurdy II  https://www.clarkcountynv.gov/adobe/assets/urn:aaid:aem:a7f197d7-f947-4987-9235-161775bc136a/original/as/commissioner-mccurdy-ii-dist-d.jpg
  -3200305 Tick Segerblom      https://www.clarkcountynv.gov/adobe/assets/urn:aaid:aem:b2de002e-8832-4492-b3a6-491cf8dacd23/original/as/commissioner-segerblom-dist-e.jpg
  -3200306 Justin Jones        https://www.clarkcountynv.gov/adobe/assets/urn:aaid:aem:83fba492-9def-41bd-ae69-40c72bb61337/original/as/commissioner-jones-dist-f.jpg
  -3200307 James B. Gibson     https://www.clarkcountynv.gov/adobe/assets/urn:aaid:aem:6c1b8a56-975f-4ade-ba2a-8c897996561f/original/as/commissioner-gibson-dist-g.jpg
Source images are 175x175 square AEM thumbnails → crop_to_4_5 gives 140x175 → resize 600x750 (~4.3x upscale, within Pasadena precedent). Use the /original/as/ path (NOT /as/ thumbnail which is ~7KB). ?width=N does NOT enlarge.
Wikimedia fallback (ONLY if a primary fails): Kirkpatrick CC BY 2.0 (3136x2823, photo_license='cc_by_2.0', attribute Horasis); Jones CC BY-SA 4.0 TIFF (photo_license='cc_by_sa_4.0'). No known Wikimedia fallback for Naft/Becker/McCurdy/Segerblom/Gibson — document genuine gap if primary fails.
Wikimedia descriptive UA (ONLY for *.wikimedia.org fetches — generic UA → HTTP 429): 'EmpoweredVote-HeadshotBot/1.0 (https://empowered.vote; contact chris@empowered.vote)'
DISCRETIONARY (A4): the planner permits using the high-res Wikimedia CC BY 2.0 image as the PRIMARY source for Kirkpatrick (3136x2823 → far better 600x750 than the 175x175 county source); set photo_license='cc_by_2.0' if so. Operator may pick at run time.
coverage.js: COVERAGE_COUNTIES array; insert AFTER the Weber County entry (browseGovernmentList:['49057'], browseStateAbbrev:'UT'), before the closing `];`. New entry: { label: 'Clark County', browseGovernmentList: ['32003'], browseStateAbbrev: 'NV', hasContext: true }. No COVERAGE_STATES change (NV cities come in later phases).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the headshot pipeline script _tmp-clark-county-commission-headshots.py</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-clark-county-commission-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py (PRIMARY ANALOG — verbatim pipeline: env-load, runtime UUID resolve, requests download, PIL crop_to_4_5/resize, Storage upsert, manifest)
    - .planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md (section "scripts/_tmp-clark-county-commission-headshots.py" — config block, COMMISSIONERS roster substitution, crop-first pipeline, psycopg2 connection, manifest)
    - .planning/phases/161-clark-county-commission-deep-seed/161-RESEARCH.md ("Headshot Sources" — 7 AEM URLs, 175x175 low-res note, Wikimedia fallback, Wikimedia UA)
  </read_first>
  <action>
Write `_tmp-clark-county-commission-headshots.py` modeled verbatim on `_tmp-va-delegates-headshots.py`. Copy the env-load block, `crop_to_4_5(img)`, `resize_600x750(img)`, `upload_to_storage(uuid, jpeg_bytes)` (x-upsert PUT), and `resolve_politician_id(cursor, external_id)` (runtime psycopg2 lookup `SELECT id FROM essentials.politicians WHERE external_id=%s`) unchanged. Constants: BUCKET='politician_photos', the politician_photos CDN base, TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS.

Build a COMMISSIONERS list of 7 dicts hardcoding the verified AEM URLs from the interfaces block: each entry `{'ext_id': <int>, 'name': '<full_name>', 'url': '<full /original/as/ AEM URL>'}`. Use the exact /original/as/ URLs (already HTTP-200 verified, no WAF, no special UA). Include a header-comment note that the source images are 175x175 squares (crop_to_4_5 → 140x175 → resize 600x750, ~4.3x upscale, within Pasadena precedent).

Per member: resolve UUID at runtime by ext_id (never hardcode a UUID), download the JPEG with normal headers, `convert('RGB')`, `crop_to_4_5` → `resize_600x750` → save JPEG quality=90 optimize=True (re-encode strips EXIF/stego), upload to `politician_photos/{uuid}-headshot.jpg` with x-upsert. On a primary-URL failure, attempt the D-07 fallback: Kirkpatrick → Wikimedia CC BY 2.0 (photo_license='cc_by_2.0'); Jones → Wikimedia CC BY-SA 4.0 TIFF (photo_license='cc_by_sa_4.0'); use the WIKIMEDIA_UA descriptive User-Agent ONLY for *.wikimedia.org fetches. Naft/Becker/McCurdy/Segerblom/Gibson have NO Wikimedia fallback — if the primary fails, SKIP (no fabrication) and emit a FAILED manifest line. Each SUCCESS line carries the photo_license used (default 'us_government_work', or the Wikimedia license if fallback). psycopg2 connect from DATABASE_URL with sslmode=require appended if absent, autocommit=True.

(Optional per A4: the script may treat Kirkpatrick's high-res Wikimedia CC BY 2.0 image as the PRIMARY source for better quality — make that a clearly-commented switch the operator can flip; default to the county AEM source.)

Emit a manifest line per member: `SUCCESS: <ext_id> <name> <uuid> -> <cdn_url> [license=<license>]` or `FAILED: <ext_id> <name> <reason>`. Print a final tally `N/7 uploaded, M gaps`. Document (as a header comment) that running the script + Storage upload is the inline-orchestrator step, NOT the executor's. Do NOT run the script.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-clark-county-commission-headshots.py"; test -f "$F" &amp;&amp; grep -q "crop_to_4_5" "$F" &amp;&amp; grep -q "resolve_politician_id" "$F" &amp;&amp; grep -q "external_id = %s" "$F" &amp;&amp; grep -q "600" "$F" &amp;&amp; grep -q "750" "$F" &amp;&amp; grep -q "LANCZOS" "$F" &amp;&amp; grep -q "x-upsert" "$F" &amp;&amp; grep -q "clarkcountynv.gov" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; [ "$(grep -c "'ext_id'" "$F")" -ge 7 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-clark-county-commission-headshots.py exists with crop_to_4_5, resize to (600,750), LANCZOS, JPEG quality=90 optimize=True.
    - resolve_politician_id does a runtime psycopg2 lookup by external_id — no hardcoded UUIDs anywhere.
    - COMMISSIONERS hardcodes 7 entries (ext_id -3200301..-3200307 + the verified /original/as/ AEM URL) from the RESEARCH roster.
    - Upload target is politician_photos/{uuid}-headshot.jpg via x-upsert PUT.
    - D-07 fallback present (Kirkpatrick CC BY 2.0; Jones CC BY-SA 4.0 TIFF); WIKIMEDIA_UA used ONLY for *.wikimedia.org; no fabrication on failure (SKIP + FAILED manifest line) for the 5 commissioners with no fallback.
    - Manifest emits per-member SUCCESS/FAILED with ext_id, uuid, cdn_url, license; final N/7 tally.
  </acceptance_criteria>
  <done>_tmp-clark-county-commission-headshots.py written to disk; passes acceptance criteria. Not yet run.</done>
</task>

<task type="auto">
  <name>Task 2: Run the script inline + write audit-only migration 1056_clark_county_commission_headshots.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1056_clark_county_commission_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql (EXACT ANALOG — audit-only county politician_images INSERT shape, external_id-subquery form, NOT registered)
    - .planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md (section "migrations/1056_clark_county_commission_headshots.sql" — 5-col external_id-subquery form + column constraints; Migration Counter Reference)
    - .planning/phases/161-clark-county-commission-deep-seed/161-RESEARCH.md (Pitfall 5 — no photo_origin_url; Pitfall 7 — do not register 1056; A2 photo_license)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): run `python C:/EV-Accounts/backend/scripts/_tmp-clark-county-commission-headshots.py` from the EV-Accounts repo root so it reads C:/EV-Accounts/backend/.env. Capture the manifest (per-member ext_id → uuid → cdn_url + license, and any FAILED gaps). Spot-check 2-3 uploaded CDN URLs return HTTP 200.

EXECUTOR STEP: Write `1056_clark_county_commission_headshots.sql` as an AUDIT-ONLY migration modeled on 245_multnomah_county_headshots.sql. Header comment states: audit-only, applied via execute_sql after the headshot upload, NOT registered in the migration ledger, ledger stays at 1055, lists the 7 commissioners by external_id. One idempotent INSERT per commissioner that has an uploaded headshot, column list EXACTLY (id, politician_id, url, type, photo_license): id=gen_random_uuid(), politician_id from subquery (SELECT id FROM essentials.politicians WHERE external_id=<ext_id>), url = the politician_photos/{uuid}-headshot.jpg CDN literal from the manifest, type='default', photo_license=the license from the manifest (default 'us_government_work', or the Wikimedia license for any fallback). Guard each with WHERE NOT EXISTS on (politician_id). Do NOT include photo_origin_url (Pitfall 5 — column does not exist). Do NOT add a ledger-registration line (Pitfall 7). Emit one INSERT per SUCCESS manifest line; OMIT any documented-gap commissioner (and note the gap in a header comment). GREP-GATE HYGIENE: keep the literal token for the removed image-origin column and the ledger table OUT of comments (paraphrase).

INLINE ORCHESTRATOR STEP (documented in the header, not executed by the executor): after the script uploads and the file is written with the real UUIDs substituted, apply `1056_clark_county_commission_headshots.sql` via `mcp__supabase-local__execute_sql` (or psql -f). Then count politician_images for the 7 ext_ids → expect 7 minus documented gaps.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1056_clark_county_commission_headshots.sql"; test -f "$F" &amp;&amp; grep -q "essentials.politician_images" "$F" &amp;&amp; grep -q "'default'" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; grep -q "external_id" "$F" &amp;&amp; ! grep -q "photo_origin_url" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: script run; manifest captured; N/7 uploads recorded (genuine gaps, if any, listed); 2-3 CDN URLs spot-checked HTTP 200.
    - 1056_clark_county_commission_headshots.sql exists; one politician_images INSERT per uploaded commissioner, column list exactly (id, politician_id, url, type, photo_license) — photo_origin_url absent.
    - type='default'; each INSERT guarded by WHERE NOT EXISTS on (politician_id); politician_id resolved via external_id subquery.
    - NO ledger registration line (audit-only; ledger stays 1055).
    - url literals point at politician_photos/{uuid}-headshot.jpg with the real per-member UUIDs from the manifest.
    - INLINE: 1056 applied; politician_images count for the 7 ext_ids = 7 minus documented gaps.
  </acceptance_criteria>
  <done>Script run inline (7/7 minus documented gaps uploaded), 1056_clark_county_commission_headshots.sql written and applied so each sourced commissioner has a non-null 600x750 headshot URL; gaps (if any) documented; ledger still MAX=1055.</done>
</task>

<task type="auto">
  <name>Task 3: Add Clark County to src/lib/coverage.js COVERAGE_COUNTIES</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the COVERAGE_COUNTIES array — Multnomah entry for the pattern, Weber County as the insertion anchor before the closing bracket)
    - .planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md (section "src/lib/coverage.js" — exact entry shape + insertion point + the county-branch note)
  </read_first>
  <action>
Add a single Clark County entry to the COVERAGE_COUNTIES array in src/lib/coverage.js, mirroring the existing Multnomah County entry. Insert it AFTER the last existing entry (the Weber County entry: browseGovernmentList ['49057'], browseStateAbbrev 'UT'), before the closing `];` of the array. The new entry: `{ label: 'Clark County', browseGovernmentList: ['32003'], browseStateAbbrev: 'NV', hasContext: true },`. Set hasContext:true because Phase 161 seeds evidence-only compass stances (purple chip). Do NOT add any COVERAGE_STATES entry (NV state-level cities come in Phases 162-165/168). Do NOT touch the county-branch logic in coverageAreaToPath() — the kind==='county' branch already applies browse_skip_overlap automatically. This is the only essentials-repo change in the phase (committable normally).
  </action>
  <verify>
    <automated>F="C:/Transparent Motivations/essentials/src/lib/coverage.js"; grep -q "Clark County" "$F" &amp;&amp; grep -q "'32003'" "$F" &amp;&amp; grep -q "hasContext: true" "$F" &amp;&amp; node -e "require('$F')" 2>/dev/null; echo done</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE_COUNTIES contains a Clark County entry: label 'Clark County', browseGovernmentList ['32003'], browseStateAbbrev 'NV', hasContext true.
    - The entry is inside the COVERAGE_COUNTIES array (after Weber County, before the closing bracket), not in COVERAGE_STATES.
    - No COVERAGE_STATES change; no edit to coverageAreaToPath() county-branch logic.
    - File still parses (no syntax error introduced).
  </acceptance_criteria>
  <done>Clark County added to COVERAGE_COUNTIES with hasContext:true and browseGovernmentList ['32003']; file parses cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| remote image host → pipeline | untrusted image bytes from clarkcountynv.gov / Wikimedia cross the boundary |
| script → Storage / DB | service-role key authorizes Storage PUT + DB lookup |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-05 | Tampering | malicious image payload (EXIF/stego) from remote source | mitigate | PIL `convert('RGB')` + re-encode JPEG optimize=True drops EXIF/embedded data (pipeline already does this) |
| T-161-06 | Information disclosure | service-role key leakage | mitigate | Key read from gitignored .env; script is gitignored `_tmp-*` (never committed); migration file has no key |
| T-161-07 | Spoofing | wrong-person / fabricated photo | mitigate | Hardcoded verified per-district AEM URLs (commissioner-naft-dist-a.jpg etc.); no fabrication on failure (skip + document gap); operator visual spot-check in Plan 03 |
| T-161-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (Pillow/requests/psycopg2 already in repo scripts); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
After all tasks (inline orchestrator):
1. Script manifest: 7/7 uploads (minus documented gaps), each SUCCESS line has uuid + cdn_url + license.
2. politician_images count for the 7 ext_ids = 7 minus documented gaps, all type='default'.
3. 2-3 CDN URLs return HTTP 200.
4. Ledger MAX still 1055 (1056 NOT registered).
5. coverage.js COVERAGE_COUNTIES contains Clark County (hasContext:true, browseGovernmentList ['32003']).
</verification>

<success_criteria>
- All 7 commissioners (minus any documented genuine gap) have a 600x750 crop-to-4:5 headshot in politician_photos.
- Each has a politician_images row (type='default') with a non-null CDN url and recorded license.
- No fabricated photos; gaps documented in the SUMMARY.
- Migration 1056 is audit-only and NOT registered (ledger MAX stays 1055).
- Clark County surfaced with the purple hasContext chip in coverage.js COVERAGE_COUNTIES.
</success_criteria>

<output>
Create `.planning/phases/161-clark-county-commission-deep-seed/161-02-SUMMARY.md` when done (record the upload manifest: 7 ext_id→uuid→cdn_url→license lines, any documented gaps, the politician_images count, and the coverage.js edit).
</output>
