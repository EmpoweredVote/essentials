---
phase: 142-long-beach-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["142-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/880_long_beach_headshots.sql
autonomous: false
requirements: [LBCH-01]

must_haves:
  truths:
    - "Tunua Thrash-Ntuk (D8) has a 600×750 headshot in Supabase Storage and a politician_images row"
    - "Dawn McIntosh, Doug Haubert, Laura L. Doud each have a 600×750 headshot in Storage + a politician_images row"
    - "Every Long Beach official (all 13) has exactly one type='default' politician_images row, or a documented gap"
    - "Each new headshot shows the correct person, no superimposed text/graphics, eyes ~1/3 from top, 4:5 not stretched"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/880_long_beach_headshots.sql"
      provides: "Audit-only headshot INSERTs for the 4 new officials + photo_origin_url backfill"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      to: "essentials.politician_images.url (type='default', press_use)"
      via: "INSERT politician_images guarded by WHERE NOT EXISTS"
      pattern: "type\\s*=?\\s*'default'|'default'"
---

<objective>
Give the 4 new officials official headshots and confirm the whole 13-person roster has clean image
coverage. The existing 9 officials already have (now deduped, post-Plan-01) headshots — this wave does
NOT re-upload them; it only verifies coverage and fills the 4 new gaps.

New headshots needed (longbeach.gov globalassets CDN — direct JPEG, standard curl/WebFetch; NOT the
Sacramento AEM/CSS-background pattern):
- Tunua Thrash-Ntuk (D8, -700050): https://www.longbeach.gov/globalassets/officials/media-library/images/tunua185x200.jpg
- Dawn McIntosh (City Attorney, -700051): https://www.longbeach.gov/globalassets/city-attorney/media-library/images/dawnmcintosh_06-20-23_003.jpg
- Doug Haubert (City Prosecutor, -700052): https://www.longbeach.gov/globalassets/officials/media-library/images/haubert185x200.jpg
- Laura L. Doud (City Auditor, -700053): https://www.longbeach.gov/globalassets/officials/media-library/images/doud185x200.jpg

Processing: download 185×200 source → crop to 4:5 (never stretch — crop only, eyes ~1/3 from top, full
head+shoulders) → resize 600×750 Lanczos q90 JPEG → upload to Supabase Storage politician_photos bucket
as {politician_uuid}-headshot.jpg → INSERT politician_images (type='default', photo_license='press_use').

Purpose: A voter sees the right face next to each official. Wrong-person or text-overlaid photos
destroy trust (per memory: no superimposed graphics, no aspect-ratio distortion).
Output: Migration 880 (audit-only — NOT registered in schema_migrations) + 4 portraits live; a
human checkpoint confirms the 4 new portraits are the correct person and correctly cropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/142-long-beach-deep-seed/142-RESEARCH.md
@.planning/phases/142-long-beach-deep-seed/142-02-SUMMARY.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §KEY RESEARCH QUESTION 3 / §Pattern 5) -->

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face (reject "Re-Elect"-style banners)

Storage + DB:
  - Bucket: politician_photos
  - Path: {politician_uuid}-headshot.jpg
  - politician_images: type='default', photo_license='press_use'
  - Also UPDATE politicians.photo_origin_url to the source page where it is NULL
  - INSERT guarded by WHERE NOT EXISTS on politician_id (Fremont pattern, migration 212)

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql or psql -f).
It does NOT register in schema_migrations (same convention as migrations 200/209/212).

New official UUIDs are resolved by external_id (-700050…-700053) at apply time — do not hardcode.

Pattern migration: 212_fremont_headshots.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download, crop, resize, upload the 4 new official headshots</name>
  <files>C:/EV-Accounts/backend/migrations/880_long_beach_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/212_fremont_headshots.sql (audit-only headshot INSERT pattern)
    - .planning/phases/142-long-beach-deep-seed/142-RESEARCH.md (§KEY RESEARCH QUESTION 3 — exact URLs + globalassets CDN method, §Pattern 5)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots)
  </read_first>
  <action>
    For each of the 4 new officials, download the source JPEG from the exact globalassets URL in the
    objective (standard curl/WebFetch — no special headers; the globalassets CDN is directly accessible,
    no WAF). Note Thrash-Ntuk's file uses her FIRST name (tunua185x200.jpg) and McIntosh's lives under
    /globalassets/city-attorney/. Crop each to 4:5 (crop only — never stretch; eyes ~1/3 from top, full
    head+shoulders) then resize to 600×750 Lanczos q90 JPEG (Python Pillow or equivalent). Reject any
    image with superimposed text/graphics over the face — if a source is unusable, document the gap
    honestly and skip that person rather than upload a bad photo. Resolve each politician UUID by
    external_id, then upload to Supabase Storage bucket politician_photos as {uuid}-headshot.jpg. Record
    the public Storage URL for each for use in Task 2.
  </action>
  <verify>
    <automated>For each of the 4 new officials: the processed local file is exactly 600×750 px (e.g. `python -c "from PIL import Image; print(Image.open('tunua.jpg').size)"` → (600, 750)) and the Storage object {uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - 4 source images downloaded from the documented globalassets URLs (or a documented gap if a source is unusable)
    - Each processed image is exactly 600×750, 4:5, not stretched
    - No image has superimposed text/graphics over the face
    - Each uploaded to politician_photos/{uuid}-headshot.jpg and reachable (HTTP 200)
    - Public Storage URLs recorded for Task 2
  </acceptance_criteria>
  <done>4 portraits processed (or gaps documented) and live in Storage at the canonical path.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 880 (headshot rows) and audit full-roster image coverage</name>
  <files>C:/EV-Accounts/backend/migrations/880_long_beach_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/212_fremont_headshots.sql (INSERT politician_images + photo_origin_url UPDATE)
    - .planning/phases/142-long-beach-deep-seed/142-RESEARCH.md (§Pattern 5, §Validation Architecture headshot smoke query)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/880_long_beach_headshots.sql. For each of the 4 new officials
    (resolved by external_id -700050…-700053), INSERT a politician_images row (url = the Storage public URL
    from Task 1, type='default', photo_license='press_use') guarded by WHERE NOT EXISTS on politician_id,
    and UPDATE politicians.photo_origin_url to the longbeach.gov source page where currently NULL. Apply via
    raw SQL (mcp__supabase-local execute_sql or psql -f) — do NOT run through the schema_migrations ledger
    (audit-only). Then run the full-roster coverage audit: LEFT JOIN all 13 LB officials to
    politician_images and confirm 0 officials missing an image (except any documented gap), and confirm no
    official has >1 type='default' row. Verify MAX(version) in schema_migrations is unchanged by this file.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -700053 AND -700050 AND pi.type='default';" returns 4 (or 4 minus documented gaps)</automated>
  </verify>
  <acceptance_criteria>
    - All 4 new officials have a type='default' politician_images row pointing at their Storage URL (or a documented gap)
    - All 13 LB officials have ≥1 image OR an explicitly documented gap (LEFT JOIN missing-image query returns only documented gaps)
    - No LB official has >1 type='default' image row (dedupe from Plan 01 still holds)
    - photo_origin_url populated for the 4 new officials
    - schema_migrations MAX(version) unchanged by migration 880 (audit-only confirmed)
    - Migration is idempotent (re-apply = 0 rows changed)
  </acceptance_criteria>
  <done>880 applied; new headshot rows present; full-roster image audit clean; ledger untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the 4 new headshots</name>
  <action>Pause for the user to visually verify the 4 new portraits per the how-to-verify steps below; do not proceed to phase completion until approved.</action>
  <what-built>4 new official headshots (Thrash-Ntuk, McIntosh, Haubert, Doud) processed to 600×750 and live in Supabase Storage + politician_images.</what-built>
  <how-to-verify>
    1. Open each of the 4 Storage URLs (or the Long Beach browse view once it renders).
    2. Confirm each photo is the CORRECT person (cross-check against longbeach.gov/officials).
    3. Confirm no superimposed text/graphics over the face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5).
    4. Confirm image quality is acceptable (not heavily pixelated beyond the 185×200 source limit).
  </how-to-verify>
  <resume-signal>Type "approved" or describe which photo(s) need re-cropping / re-sourcing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| longbeach.gov CDN → local processing | External image source; could be wrong-person or text-overlaid. |
| operator → Supabase Storage + production DB | Uploads + image-row INSERTs against production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-12 | Spoofing | Wrong-person headshot uploaded for an official | mitigate | URLs verified against longbeach.gov/officials in RESEARCH; blocking human-verify checkpoint cross-checks each face before sign-off |
| T-142-13 | Tampering | Image stretched / aspect-distorted / text-overlaid | mitigate | Crop-to-4:5-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; human checkpoint |
| T-142-14 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged before/after |
| T-142-15 | Tampering | Duplicate image rows re-introduced for an official | mitigate | INSERT guarded by WHERE NOT EXISTS on politician_id; post-audit confirms ≤1 type='default' per official |
</threat_model>

<verification>
After 880 + checkpoint:
1. 4 new officials each have exactly one type='default' image (or documented gap)
2. full-roster LEFT JOIN missing-image query returns only documented gaps
3. no official has >1 type='default' image
4. schema_migrations MAX(version) unchanged by 880
5. human checkpoint approved (correct person, correct crop, no overlays)
</verification>

<success_criteria>
All 4 new officials have correct, correctly-cropped 600×750 portraits live; the full 13-person roster
has clean single-image coverage with any genuine gap documented; the ledger counter is preserved.
</success_criteria>

<output>
Create `.planning/phases/142-long-beach-deep-seed/142-03-SUMMARY.md` when done.
</output>
