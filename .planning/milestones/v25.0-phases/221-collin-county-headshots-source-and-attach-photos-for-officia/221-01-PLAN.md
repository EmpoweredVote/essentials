---
phase: 221-collin-county-headshots-source-and-attach-photos-for-officia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-WORKLIST.md
autonomous: true
requirements: [COLLIN-HEADSHOT-01]
must_haves:
  truths:
    - "The authoritative in-scope missing-photo set is derived LIVE from essentials at execution — the 82-day-old audit memo is used only as a cross-check, never as the source of truth (COLLIN-HEADSHOT-01)"
    - "BEFORE coverage counts are recorded: total in-scope seated Collin County officeholders, count WITH a politician_images row, count WITHOUT — so 221-04 can measure a coverage delta"
    - "Brandon Smith (Longview D3, seated mig 1400) and Gopal Ponangi (Frisco Place 4, seated mig 1404) are both present in the derived worklist"
    - "The worklist is grouped into reviewable per-government batches (never one monolithic fan-out) so sourcing stays quota-safe and interactive"
  artifacts:
    - ".planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-WORKLIST.md"
  key_links:
    - "Each worklist row carries the exact politician_id + full_name + city/office so 221-02/03 can attach politician_images to the correct officeholder (join on politician_id)"
  prohibitions:
    - "Never treat the ~82-day-old prior-audit memo as the authoritative missing-photo list — it predates Ph218/219/220 roster changes; the live DB is the source of truth"
---

<objective>
Derive — LIVE from the database at execution time — the authoritative list of in-scope Collin County officeholders who currently have no headshot, and group it into reviewable per-government batches for the sourcing plans (COLLIN-HEADSHOT-01). This is the pre-flight audit: it produces the worklist and the BEFORE coverage counts that 221-04 will diff against. It writes NO photo assets and NO politician_images rows.

Purpose: The prior audit memo (~82 days old) predates the Ph218/219/220 roster changes, so it is stale — the missing-photo set MUST be re-derived from the live DB, not taken from memory.
Output: `221-01-WORKLIST.md` (batched worklist + BEFORE counts).
</objective>

## Artifacts this phase produces

This phase (221, all plans) creates only data + assets — no application code, no schema, no migrations:

- New rows in `essentials.politician_images` (`type='default'`, `photo_license` set) — one per sourced headshot, inserted by the `/find-headshots` skill.
- New processed 600×750 JPEG objects in Supabase Storage bucket `politician_photos` (underscore — the bucket the `/find-headshots` skill uploads to; NOT `politician-photos`), named `{politician_id}-headshot.jpg`.
- `221-01-WORKLIST.md` — the live audit worklist + BEFORE coverage counts (this plan).
- `221-CONFIRMED-BLANK.md` — the register of officeholders intentionally left blank because no public photo exists, with per-person search evidence (221-04).

No new code symbols. No files in the essentials or EV-Accounts repos are modified except the two `.planning/` documents above.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the live in-scope missing-photo set + record BEFORE counts</name>
  <read_first>
    - `~/.claude/commands/find-headshots.md` (the `find-headshots` skill) — reuse its "Query A — politicians without images" as the base query; this plan scopes that query to Collin County.
    - `src/lib/coverage.js` (Texas block) — the authoritative Collin County browse government set (23-24 hardcoded geo_ids); use it to cross-check the city-name scope.
  </read_first>
  <files>(read-only DB query via mcp__supabase-local__execute_sql; no writes this task)</files>
  <action>
    Using mcp__supabase-local__execute_sql, run the `/find-headshots` skill's Query A shape scoped to Collin County: select from essentials.politicians p LEFT JOIN essentials.offices o ON o.id = p.office_id LEFT JOIN essentials.politician_images pi ON pi.politician_id = p.id, WHERE pi.id IS NULL AND p.is_active = true AND p.is_vacant = false AND o.representing_state ILIKE 'tx' AND o.representing_city ILIKE ANY of the Collin County city set: Allen, Anna, Blue Ridge, Celina, Fairview, Farmersville, Frisco, Josephine, Lavon, Lowry Crossing, Lucas, McKinney, Melissa, Murphy, Nevada, Parker, Plano, Princeton, Prosper, Richardson, Saint Paul, Van Alstyne, Weston. Cross-check the resolved government set against the Collin geo_id list in src/lib/coverage.js (Texas block) — flag any government present in coverage.js but absent from the query (and vice-versa) in the SUMMARY. Separately, run a targeted lookup for Brandon Smith (Longview D3, seated migration 1400) and Gopal Ponangi (Frisco Place 4, seated migration 1404) by full_name and UNION them into the set even if the city/state filter would otherwise drop Longview — these two MUST appear. Then record BEFORE counts with a second query over the same scope: total in-scope seated officeholders, count WITH a politician_images row, count WITHOUT (the worklist size). Do NOT drop the pi.id IS NULL test — it is the authoritative "no image" condition (a person may have photo_origin_url set yet still have no image row; include them).
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql returns the scoped no-image set and the BEFORE counts (with-image / without-image / total) in one session; row output includes Brandon Smith and Gopal Ponangi.</automated>
  </verify>
  <acceptance_criteria>
    - The query returns the in-scope set of Collin County seated officeholders with pi.id IS NULL.
    - BEFORE counts recorded: total in-scope, WITH image, WITHOUT image (numbers, not prose).
    - Both "Brandon Smith" and "Gopal Ponangi" appear as rows in the derived set (each with a real politician_id).
    - Any coverage.js-vs-query government mismatch is noted (or explicitly "none").
  </acceptance_criteria>
  <done>The live missing-photo set is derived and the BEFORE coverage counts are captured; Smith and Ponangi confirmed present; stale memo NOT used as the source.</done>
</task>

<task type="auto">
  <name>Task 2: Write the batched worklist (221-01-WORKLIST.md)</name>
  <read_first>
    - The Task 1 query output (in-scope no-image set + BEFORE counts).
    - `src/lib/coverage.js` (Texas block) — for the per-government grouping.
  </read_first>
  <files>.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-WORKLIST.md</files>
  <action>
    Write 221-01-WORKLIST.md listing every in-scope imageless officeholder as a row: politician_id, full_name, city (representing_city), office/title. Group the rows into two reviewable sourcing batches matching the 221-02 and 221-03 government assignments — Batch A (higher-source group): Plano, Richardson, Frisco, Allen, McKinney, Prosper, Celina, Murphy, Anna, Fairview, Lucas, Parker, plus Brandon Smith (Longview D3); Batch B (small-town / low-source group): Princeton, Van Alstyne, Melissa, Lavon, Josephine, Weston, Blue Ridge, Farmersville, Lowry Crossing, Nevada, Saint Paul. Put each derived person in the batch matching their city; if a person's city is not in either list, place them in Batch B and flag it. Record the BEFORE counts at the top. Add a one-line note that Blue Ridge, Farmersville, Lowry Crossing, Nevada, and Saint Paul were flagged as zero-known-online-source in the v25.0 roadmap and are EXPECTED to end mostly blank — but must still each be attempted, not pre-skipped (COLLIN-HEADSHOT-01: attempt every person; honest blank only after a genuine search).
  </action>
  <verify>
    <automated>test -f .planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-WORKLIST.md && grep -q "Batch A" .planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-WORKLIST.md && grep -q "Batch B" .planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-WORKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - 221-01-WORKLIST.md exists with BEFORE counts at the top.
    - Every derived imageless officeholder appears as a row with politician_id + full_name + city + office, assigned to Batch A or Batch B.
    - Brandon Smith is in Batch A; the five zero-source cities carry the "attempt anyway, expect blank" note.
  </acceptance_criteria>
  <done>A batched, per-government worklist exists that 221-02/03 can consume directly; no photo assets or DB image rows were written this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale memo → planned scope | An 82-day-old audit could misdirect the sourcing set if trusted over the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-221-01 | Information Disclosure | scope derivation | medium | mitigate | Derive the worklist live via pi.id IS NULL over the Collin scope; cross-check coverage.js; explicitly union Smith + Ponangi so no seated person is silently dropped |
| T-221-02 | Repudiation | read-only audit | low | accept | Task 1 is SELECT-only via MCP; no writes to reconcile |
</threat_model>

<verification>
- The derived set is live (pi.id IS NULL over the Collin scope), Smith + Ponangi present, BEFORE counts recorded, worklist batched into A/B and written to disk.
</verification>

<success_criteria>
A live, batched worklist of every in-scope imageless Collin County officeholder exists with BEFORE coverage counts; Brandon Smith and Gopal Ponangi are both included; no photo assets or politician_images rows were written.
</success_criteria>

<output>
Create `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-SUMMARY.md` when done — record the BEFORE counts and the Batch A / Batch B membership for 221-02/03.
</output>
