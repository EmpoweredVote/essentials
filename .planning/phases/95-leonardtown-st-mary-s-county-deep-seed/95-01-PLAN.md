---
phase: 95-leonardtown-st-mary-s-county-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql
  - C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql
autonomous: true
requirements:
  - MD-DEEP-01
  - MD-DEEP-03
tags:
  - md
  - local-government
  - county-seed
  - migration
must_haves:
  truths:
    - "Exactly one essentials.governments row with name='St. Mary''s County, Maryland, US', type='County', state='MD', geo_id='24037'"
    - "Exactly one essentials.chambers row named 'Board of County Commissioners' linked to the St. Mary's County government"
    - "Exactly one essentials.districts row with geo_id='24037', district_type='COUNTY', state='md', mtfcc='G4020' (lowercase state per routing convention)"
    - "Exactly 5 essentials.politicians rows with external_id BETWEEN -24037005 AND -24037001 (President Guy + 4 district commissioners)"
    - "Exactly 5 essentials.offices rows linked to the COUNTY district via geo_id='24037'; titles: 'President, Board of County Commissioners' + 'Commissioner, District 1..4'"
    - "All 5 St. Mary's commissioner politicians have office_id back-filled (politicians.office_id IS NOT NULL)"
    - "Exactly one essentials.governments row with name='Town of Leonardtown, Maryland, US', type='LOCAL', state='MD', city='Leonardtown', geo_id='2446475'"
    - "Exactly one essentials.chambers row named 'Town Council' linked to the Leonardtown government"
    - "Exactly one essentials.districts row with geo_id='2446475', district_type='LOCAL_EXEC', state='md', mtfcc=NULL (Mayor's townwide district)"
    - "Exactly one essentials.districts row with geo_id='2446475', district_type='LOCAL', state='md', mtfcc=NULL (at-large council district)"
    - "Exactly 6 essentials.politicians rows with external_id BETWEEN -2446475006 AND -2446475001 (Mayor Burris + 5 council members)"
    - "Exactly 6 essentials.offices rows for Leonardtown: 1 Mayor (LOCAL_EXEC), 5 Council Member (LOCAL); all 6 council/mayor titles correct"
    - "Section-split detector returns 0 rows for both geo_id='24037' and geo_id='2446475' (every geofence has a matching districts row)"
    - "Both migrations are idempotent: re-running inserts zero additional rows in governments, chambers, districts, politicians, or offices"
    - "Both migrations appended to supabase_migrations.schema_migrations as versions '276' and '277'"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql"
      provides: "St. Mary's County government + chamber + COUNTY district + 5 commissioner politicians + 5 offices + office_id back-fill + post-verification DO block + ledger entry"
      contains: "St. Mary''s County, Maryland, US"
      min_lines: 200
    - path: "C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql"
      provides: "Leonardtown government + Town Council chamber + LOCAL_EXEC + LOCAL districts + 6 politicians + 6 offices + office_id back-fill + post-verification DO block + ledger entry"
      contains: "Town of Leonardtown, Maryland, US"
      min_lines: 230
  key_links:
    - from: "C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql"
      to: "essentials.geofence_boundaries (geo_id='24037', mtfcc='G4020')"
      via: "essentials.districts INSERT with matching geo_id + district_type='COUNTY' + state='md'"
      pattern: "geo_id\\s*=\\s*'24037'"
    - from: "C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql"
      to: "essentials.geofence_boundaries (geo_id='2446475', mtfcc='G4110')"
      via: "essentials.districts INSERT (LOCAL_EXEC + LOCAL) with matching geo_id + state='md'"
      pattern: "geo_id\\s*=\\s*'2446475'"
    - from: "Post-verification DO block in 276"
      to: "essentials.offices joined to essentials.districts"
      via: "v_office_count SELECT joining offices+districts on geo_id='24037'"
      pattern: "v_office_count\\s+<>\\s+5"
    - from: "Post-verification DO block in 277"
      to: "essentials.offices joined to essentials.districts"
      via: "v_office_count SELECT joining offices+districts on geo_id='2446475'"
      pattern: "v_office_count\\s+<>\\s+6"
---

<objective>
Seed St. Mary's County and Town of Leonardtown government rows, chambers, districts, politicians, and offices via two idempotent SQL migrations (276 and 277), so an address lookup against either jurisdiction returns the correct elected officials (MD-DEEP-01, MD-DEEP-03).

Purpose: Phase 91 loaded the geofence boundaries for St. Mary's County (geo_id='24037', G4020) and Leonardtown (geo_id='2446475', G4110). This plan creates the linking districts rows + the governments, chambers, politicians, and offices that the PostGIS routing query joins on, closing the data side of MD-DEEP-01/03. Phase 95 D-05 is resolved (Option A — one COUNTY district, all 5 commissioners share it). Verified rosters and authoritative photo URLs come from RESEARCH.md and PATTERNS.md.

Output:
- `C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql` — full St. Mary's County seed (analog: migration 244)
- `C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql` — full Leonardtown seed (analog: migration 246)
- Both migrations applied to the live Supabase database with post-verification DO blocks PASS, ledger entries '276' and '277' written
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-CONTEXT.md
@.planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-RESEARCH.md
@.planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-PATTERNS.md

# Migration analogs — copy structure verbatim then substitute MD values
@C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql
@C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write migration 276 — St. Mary's County government + chamber + COUNTY district + 5 commissioners + offices</name>
  <files>C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql — EXACT structural template (transaction wrapper, pre-flight DO block, government INSERT, chamber INSERT without slug, COUNTY district INSERT, 5 politician+office WITH CTE blocks, office_id back-fill, post-verification DO block with 3 gates, ledger entry); copy lines 1-321 structure verbatim then substitute St. Mary's values
    - .planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-PATTERNS.md — Section "C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql" lines 21-194; contains ALL SQL patterns adapted from 244 with St. Mary's substitutions already worked out (file header, transaction wrapper, government INSERT, chamber INSERT, COUNTY district INSERT, Block 1 President WITH CTE, title format for D1-D4, office_id back-fill range, post-verification DO block, ledger entry)
    - .planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-RESEARCH.md — Section "Verified Roster Data" with 5 commissioners' full_name, first_name, last_name, title, external_id; Section "Common Pitfalls" Pitfalls 3 (slug GENERATED), 4 (state='md' lowercase); Section "Architecture Patterns" "Pattern: Post-Verification DO Block"
    - .planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-CONTEXT.md — D-01 (elected only), D-05 (Option A: single COUNTY district, all 5 share)
  </read_first>
  <behavior>
    - File begins with `-- Migration 276: St. Mary's County government + chamber + COUNTY district + 5 officials + offices` header followed by CRITICAL comment block (slug GENERATED ALWAYS; governments has no unique geo_id constraint; districts.state='md' lowercase for COUNTY; governments.state='MD' uppercase)
    - Wraps all DDL/DML in a single `BEGIN; ... COMMIT;` transaction so failures roll back atomically
    - Pre-flight DO block emits a RAISE NOTICE (not exception) when 'St. Mary''s County, Maryland, US' already exists (idempotent re-run signal)
    - Step 1: One `INSERT INTO essentials.governments (id, name, type, state, city, geo_id) SELECT gen_random_uuid(), 'St. Mary''s County, Maryland, US', 'County', 'MD', NULL, '24037' WHERE NOT EXISTS (...)` (uppercase state on governments)
    - Step 2: One `INSERT INTO essentials.chambers (id, name, name_formal, government_id)` — name='Board of County Commissioners', name_formal='St. Mary''s County Board of County Commissioners'; NEVER lists slug column (GENERATED ALWAYS)
    - Step 3: One `INSERT INTO essentials.districts (id, district_type, state, geo_id, label, mtfcc) SELECT gen_random_uuid(), 'COUNTY', 'md', '24037', 'St. Mary''s County', 'G4020'` (lowercase state on COUNTY district; mtfcc='G4020' because G4020 county boundary exists in geofence_boundaries)
    - Step 4: Five WITH CTE blocks, one per politician — each ON CONFLICT (external_id) DO NOTHING on politicians, NOT EXISTS guard on offices via (district_id, politician_id); all 5 offices link to the same COUNTY district where geo_id='24037' AND district_type='COUNTY' AND state='md'
    - Step 5: Single UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id=p.id AND p.external_id BETWEEN -24037005 AND -24037001 AND p.office_id IS NULL (idempotent)
    - Step 6: Post-verification DO block with 3 gates — (a) v_gov_count=1, (b) v_office_count=5 (joined to districts on geo_id='24037'+'COUNTY'+'md'), (c) v_split_count=0 (section-split detector: geofence_boundaries where geo_id='24037' AND mtfcc='G4020' AND NOT EXISTS districts); each gate RAISE EXCEPTION on failure (rolls back transaction); on success RAISE NOTICE with all 3 counts
    - Step 7: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('276') ON CONFLICT (version) DO NOTHING;`
    - File ends with `COMMIT;`
    - 5 commissioners use exactly these tuples (full_name, first_name, last_name, external_id, title): per Roster Reference in PATTERNS.md
  </behavior>
  <action>
    Create `C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql` by adapting `C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql` per the verbatim SQL snippets in PATTERNS.md section "C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql" (lines 21-194 of PATTERNS.md).

    Government identity values (substitute throughout):
      name: 'St. Mary''s County, Maryland, US'
      type: 'County'   (case matches Multnomah precedent)
      state: 'MD'      (uppercase, governments table convention)
      city: NULL
      geo_id: '24037'

    Chamber identity values:
      name: 'Board of County Commissioners'
      name_formal: 'St. Mary''s County Board of County Commissioners'
      DO NOT include slug column (GENERATED ALWAYS — Pitfall 3)

    COUNTY district identity values:
      district_type: 'COUNTY'
      state: 'md'      (lowercase, routing query convention — Pitfall 4)
      geo_id: '24037'
      label: 'St. Mary''s County'
      mtfcc: 'G4020'

    Five politician+office WITH CTE blocks (one per row below). Each block uses pattern from PATTERNS.md lines 96-127 (President block) and the title format note at lines 130-137 for D1-D4. All 5 offices link to the SAME COUNTY district (D-05 Option A). For every block: `representing_state='MD'` uppercase; `is_appointed_position=false`; `is_vacant=false`; `role_canonical=NULL`. Politician row: `is_active=true`, `is_appointed=false`, `is_vacant=false`, `is_incumbent=true`, `party=NULL`.

      | external_id  | full_name              | first_name | last_name | title                                       |
      |-------------:|------------------------|------------|-----------|---------------------------------------------|
      | -24037001    | James R. Guy           | James      | Guy       | President, Board of County Commissioners    |
      | -24037002    | Eric Colvin            | Eric       | Colvin    | Commissioner, District 1                    |
      | -24037003    | Michael L. Hewitt      | Michael    | Hewitt    | Commissioner, District 2                    |
      | -24037004    | Mike Alderson, Jr.     | Mike       | Alderson  | Commissioner, District 3                    |
      | -24037005    | Scott R. Ostrow        | Scott      | Ostrow    | Commissioner, District 4                    |

    Office_id back-fill: `UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -24037005 AND -24037001 AND p.office_id IS NULL;`

    Post-verification DO block: copy verbatim from PATTERNS.md lines 151-184 (already adapted for St. Mary's). Gates: v_gov_count=1, v_office_count=5, v_split_count=0.

    Ledger entry: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('276') ON CONFLICT (version) DO NOTHING;` before `COMMIT;`

    Do NOT apply this migration in this task — Task 3 applies both migrations together.
  </action>
  <verify>
    <automated>powershell -Command "$f='C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql'; if(-not (Test-Path $f)){throw 'FAIL: 276 missing'}; $c=Get-Content $f -Raw; $b = $c -replace '(?m)^\s*--.*$',''; $checks=@(@('St. Mary''''s County, Maryland, US',2),@('Board of County Commissioners',2),@('''24037''',5),@('-24037001',1),@('-24037002',1),@('-24037003',1),@('-24037004',1),@('-24037005',1),@('''COUNTY''',2),@('''md''',3),@('''MD''',6),@('GENERATED',0),@('slug',0),@('president, board of county commissioners',1),@('commissioner, district 1',1),@('commissioner, district 4',1),@('v_office_count <> 5',1),@('schema_migrations',1),@('VALUES (''276'')',1),@('BEGIN;',1),@('COMMIT;',1)); $bad=0; foreach($p in $checks){$n=([regex]::Matches($b,[regex]::Escape($p[0]),'IgnoreCase')).Count; if($n -lt $p[1]){Write-Host \"FAIL: '$($p[0])' want>=$($p[1]) got $n\"; $bad++}}; if($bad -gt 0){throw \"$bad assertions failed\"}; Write-Host 'PASS: migration 276 structural assertions OK'\"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql` exists and is non-empty
    - File contains `BEGIN;` exactly once and `COMMIT;` exactly once
    - File contains the literal string `'St. Mary''s County, Maryland, US'` at least twice (government INSERT + chamber INSERT)
    - File contains the literal string `'Board of County Commissioners'` at least twice (chamber INSERT + offices SELECT chamber lookup)
    - File contains `'24037'` at least 5 times (government, district INSERT, post-verify gates)
    - File contains all 5 external_ids as literals: -24037001, -24037002, -24037003, -24037004, -24037005
    - File contains both `'COUNTY'` (district_type) at least twice and `'md'` (lowercase district state) at least 3 times
    - File contains `'MD'` uppercase at least 6 times (governments.state + 5x offices.representing_state)
    - File does NOT contain a column-list reference to slug (case-insensitive — `slug` is GENERATED ALWAYS)
    - File contains the literal `'President, Board of County Commissioners'` exactly once (President title)
    - File contains the literal `'Commissioner, District 1'` exactly once (and similarly for District 4)
    - File contains the literal `v_office_count <> 5` (post-verification expected office count gate)
    - File contains `INSERT INTO supabase_migrations.schema_migrations` and `VALUES ('276')`
  </acceptance_criteria>
  <done>Migration 276 SQL file exists, parses structurally for all required pattern strings, and is ready to apply via Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write migration 277 — Leonardtown government + Town Council chamber + LOCAL_EXEC + LOCAL districts + 6 officials + offices</name>
  <files>C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql — EXACT structural template for incorporated-town pattern (type='LOCAL' government, LOCAL_EXEC district for Mayor, LOCAL district for council; mtfcc=NULL on LOCAL/LOCAL_EXEC rows); copy structure verbatim
    - C:/EV-Accounts/backend/migrations/244_multnomah_county_government.sql — Reference for office_id back-fill UPDATE and post-verification DO block structure (3 gates pattern)
    - .planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-PATTERNS.md — Section "C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql" lines 198-393: ALL Leonardtown SQL pre-adapted (government type='LOCAL', Town Council chamber, LOCAL_EXEC + LOCAL districts both with mtfcc=NULL, Mayor block links to LOCAL_EXEC, council blocks link to LOCAL, office_id back-fill range, post-verification DO block, ledger entry); also "Critical Distinctions: Migration 276 vs 277" table at lines 741-753
    - .planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-RESEARCH.md — Section "Town of Leonardtown" with 6 officials' verified names + titles + external_ids; Pitfall 5 (Mattingly NOT 'Vice President' — use 'Council Member'); Pitfall 6 (Hollander name); Pitfall 4 (state='md' lowercase for LOCAL+LOCAL_EXEC); A3 verification query (already resolved: LOCAL is correct district_type)
  </read_first>
  <behavior>
    - File begins with `-- Migration 277: Town of Leonardtown government + Town Council chamber + LOCAL_EXEC + LOCAL districts + 6 officials + offices` header with CRITICAL comment block matching 276's style
    - Wraps DDL/DML in `BEGIN; ... COMMIT;`
    - Pre-flight DO block emits RAISE NOTICE when 'Town of Leonardtown, Maryland, US' already exists (idempotent re-run signal)
    - Step 1: One `INSERT INTO essentials.governments (id, name, type, state, city, geo_id) SELECT gen_random_uuid(), 'Town of Leonardtown, Maryland, US', 'LOCAL', 'MD', 'Leonardtown', '2446475' WHERE NOT EXISTS (...)`
    - Step 2: One `INSERT INTO essentials.chambers (id, name, name_formal, government_id)` — name='Town Council', name_formal='Leonardtown Town Council'; slug omitted (GENERATED)
    - Step 3a: One `INSERT INTO essentials.districts` for LOCAL_EXEC — district_type='LOCAL_EXEC', state='md', geo_id='2446475', label='Leonardtown (Townwide)', mtfcc=NULL
    - Step 3b: One `INSERT INTO essentials.districts` for LOCAL — district_type='LOCAL', state='md', geo_id='2446475', label='Leonardtown (At-Large)', mtfcc=NULL
    - Step 4: Six WITH CTE blocks. Block 1 (Mayor Burris) links to LOCAL_EXEC district; Blocks 2-6 (5 Council Members) link to LOCAL district. Every council title='Council Member' (NOT 'Vice President' per Pitfall 5). All offices: representing_state='MD' uppercase, is_appointed_position=false, is_vacant=false, role_canonical=NULL
    - Step 5: Single UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -2446475006 AND -2446475001 AND p.office_id IS NULL
    - Step 6: Post-verification DO block, 3 gates — (a) v_gov_count=1 for 'Town of Leonardtown, Maryland, US', (b) v_office_count=6 (count ALL offices joined to districts where geo_id='2446475' AND state='md', covering both LOCAL_EXEC + LOCAL), (c) v_split_count=0 (section-split detector on geo_id='2446475' AND mtfcc='G4110')
    - Step 7: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('277') ON CONFLICT (version) DO NOTHING;`
    - File ends with `COMMIT;`
  </behavior>
  <action>
    Create `C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql` by adapting `C:/EV-Accounts/backend/migrations/246_multnomah_cities_government.sql` per the verbatim SQL snippets in PATTERNS.md section "C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql" (lines 198-393 of PATTERNS.md).

    Government identity values:
      name: 'Town of Leonardtown, Maryland, US'
      type: 'LOCAL'    (NOT 'County' — this is an incorporated town)
      state: 'MD'      (uppercase, governments table)
      city: 'Leonardtown'
      geo_id: '2446475'   (verified geo_id from DB — Pitfall 1 warns against estimate '2443700')

    Chamber identity values:
      name: 'Town Council'
      name_formal: 'Leonardtown Town Council'
      slug omitted (GENERATED — Pitfall 3)

    Two district rows (both mtfcc=NULL — per migration 246 pattern):
      Row A: district_type='LOCAL_EXEC', state='md', geo_id='2446475', label='Leonardtown (Townwide)', mtfcc=NULL
      Row B: district_type='LOCAL',     state='md', geo_id='2446475', label='Leonardtown (At-Large)', mtfcc=NULL

    Six politician+office blocks. Mayor block links to LOCAL_EXEC; 5 council blocks link to LOCAL. Every politician row: is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, party=NULL. Every office row: representing_state='MD', is_appointed_position=false, is_vacant=false, role_canonical=NULL.

      | external_id   | full_name                  | first_name | last_name  | title          | district_type to link |
      |--------------:|----------------------------|------------|------------|----------------|-----------------------|
      | -2446475001   | Daniel W. Burris           | Daniel     | Burris     | Mayor          | LOCAL_EXEC            |
      | -2446475002   | J. Maguire Mattingly IV    | Jay        | Mattingly  | Council Member | LOCAL                 |
      | -2446475003   | Nick B. Colvin             | Nick       | Colvin     | Council Member | LOCAL                 |
      | -2446475004   | Heather M. Earhart         | Heather    | Earhart    | Council Member | LOCAL                 |
      | -2446475005   | Christy Hollander          | Christy    | Hollander  | Council Member | LOCAL                 |
      | -2446475006   | Mary Maday Slade           | Mary       | Slade      | Council Member | LOCAL                 |

    Per Pitfall 5: Mattingly's title is 'Council Member', NOT 'Vice President'. Per Pitfall 6: Hollander uses short form 'Christy Hollander'. Per the WITH CTE block in PATTERNS.md lines 264-295 for Mayor and lines 299-335 for Council Members.

    Office_id back-fill: `UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -2446475006 AND -2446475001 AND p.office_id IS NULL;`

    Post-verification DO block: copy verbatim from PATTERNS.md lines 348-383 (already adapted for Leonardtown). Gates: v_gov_count=1, v_office_count=6 (note: v_office_count select joins offices+districts on geo_id='2446475' AND state='md' WITHOUT filtering district_type — must capture both LOCAL_EXEC + LOCAL offices), v_split_count=0.

    Ledger entry: `INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('277') ON CONFLICT (version) DO NOTHING;` before `COMMIT;`

    Do NOT apply this migration in this task — Task 3 applies both migrations.
  </action>
  <verify>
    <automated>powershell -Command "$f='C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql'; if(-not (Test-Path $f)){throw 'FAIL: 277 missing'}; $c=Get-Content $f -Raw; $b = $c -replace '(?m)^\s*--.*$',''; $checks=@(@('Town of Leonardtown, Maryland, US',2),@('Town Council',2),@('''2446475''',5),@('-2446475001',1),@('-2446475002',1),@('-2446475003',1),@('-2446475004',1),@('-2446475005',1),@('-2446475006',1),@('''LOCAL_EXEC''',2),@('''LOCAL''',3),@('''md''',4),@('''MD''',7),@('''Mayor''',1),@('''Council Member''',5),@('Daniel W. Burris',1),@('J. Maguire Mattingly IV',1),@('Christy Hollander',1),@('Mary Maday Slade',1),@('v_office_count <> 6',1),@('schema_migrations',1),@('VALUES (''277'')',1),@('BEGIN;',1),@('COMMIT;',1),@('Vice President',0)); $bad=0; foreach($p in $checks){$n=([regex]::Matches($b,[regex]::Escape($p[0]),'IgnoreCase')).Count; if($n -lt $p[1]){Write-Host \"FAIL: '$($p[0])' want>=$($p[1]) got $n\"; $bad++}}; if($bad -gt 0){throw \"$bad assertions failed\"}; Write-Host 'PASS: migration 277 structural assertions OK'\"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql` exists and is non-empty
    - File contains `BEGIN;` exactly once and `COMMIT;` exactly once
    - File contains `'Town of Leonardtown, Maryland, US'` at least twice
    - File contains `'Town Council'` at least twice (chamber INSERT + offices SELECT chamber lookup)
    - File contains `'2446475'` at least 5 times (government + 2 district rows + post-verify gates)
    - File contains all 6 external_ids as literals: -2446475001, -2446475002, -2446475003, -2446475004, -2446475005, -2446475006
    - File contains `'LOCAL_EXEC'` at least twice (district INSERT + Mayor office WHERE clause)
    - File contains `'LOCAL'` at least 3 times (governments.type + district INSERT + council office WHERE clauses)
    - File contains the literal `'Mayor'` exactly once
    - File contains the literal `'Council Member'` exactly 5 times
    - File does NOT contain the literal `'Vice President'` (Pitfall 5)
    - File contains `Daniel W. Burris`, `J. Maguire Mattingly IV`, `Christy Hollander`, `Mary Maday Slade` (verified roster)
    - File contains the literal `v_office_count <> 6`
    - File contains `INSERT INTO supabase_migrations.schema_migrations` and `VALUES ('277')`
    - File does NOT contain a column-list reference to slug
  </acceptance_criteria>
  <done>Migration 277 SQL file exists, parses structurally, and is ready to apply via Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply migrations 276 and 277 to Supabase + verify section-split detector returns 0</name>
  <files>C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql, C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql (from Task 1)
    - C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql (from Task 2)
    - .planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-RESEARCH.md — "Migration Counter Verification" + Pitfall 7 (apply via Supabase MCP; ledger entry is the project standard)
    - feedback memory: feedback_supabase_local_is_remote.md (mcp__supabase-local IS production — treat all writes as live)
  </read_first>
  <behavior>
    - Migration 276 is applied to production Supabase via mcp__supabase-local__execute_sql with the entire file contents as a single SQL string; on success, the post-verification DO block emits RAISE NOTICE 'Post-verification PASSED: gov_count=1, office_count=5, split_orphans=0'
    - Migration 277 is applied next, same mechanism; DO block emits 'Post-verification PASSED: gov_count=1, office_count=6, split_orphans=0'
    - Section-split detector cross-check returns 0 rows for both geo_ids together
    - Re-applying both files (idempotency test) inserts 0 additional rows in any of: governments, chambers, districts, politicians, offices, politician_images
    - supabase_migrations.schema_migrations table contains versions '276' and '277'
  </behavior>
  <action>
    Apply migration 276 first, then 277. Use mcp__supabase-local__execute_sql (Supabase MCP — per feedback_supabase_local_is_remote.md this IS the live production DB).

    Step A — Apply 276:
      1. Read the full file contents of C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql
      2. Invoke mcp__supabase-local__execute_sql with that exact text as the `query` parameter
      3. Confirm the response contains 'Post-verification PASSED: gov_count=1, office_count=5, split_orphans=0' (the DO block RAISE NOTICE). If RAISE EXCEPTION fires, the entire transaction rolls back — fix the migration SQL and retry; do not move on.

    Step B — Apply 277:
      1. Read the full file contents of C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql
      2. Invoke mcp__supabase-local__execute_sql with that exact text as the `query` parameter
      3. Confirm 'Post-verification PASSED: gov_count=1, office_count=6, split_orphans=0' in the response.

    Step C — Cross-check section-split detector via a single SELECT on Supabase MCP:
      `SELECT gb.geo_id, gb.mtfcc FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('24037','2446475') AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.state = 'md')`
      Expected: 0 rows.

    Step D — Cross-check ledger versions present:
      `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('276','277') ORDER BY version`
      Expected: 2 rows ('276','277').

    Step E — Idempotency: re-apply both migration files once more via Supabase MCP. Each must complete without raising the post-verification EXCEPTION; ROW COUNT side effects on governments/chambers/districts/politicians/offices must be 0 for the second application. Capture row counts for the SUMMARY.

    Step F — Confirm office counts and key joins:
      `SELECT COUNT(*) AS stm_offices FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='24037' AND d.district_type='COUNTY' AND d.state='md'` → expect 5
      `SELECT COUNT(*) AS ltown_offices FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='2446475' AND d.state='md'` → expect 6
      `SELECT COUNT(*) AS stm_pols FROM essentials.politicians WHERE external_id BETWEEN -24037005 AND -24037001` → expect 5
      `SELECT COUNT(*) AS ltown_pols FROM essentials.politicians WHERE external_id BETWEEN -2446475006 AND -2446475001` → expect 6

    Do NOT use psql or any other DB tool — only the Supabase MCP per project convention. Do NOT run any git commands in C:/EV-Accounts (per feedback_no_git_in_ev_accounts.md).
  </action>
  <verify>
    <automated>powershell -Command "if(-not (Test-Path 'C:/EV-Accounts/backend/migrations/276_stmarys_county_government.sql')){throw 'FAIL: 276 missing'}; if(-not (Test-Path 'C:/EV-Accounts/backend/migrations/277_leonardtown_government.sql')){throw 'FAIL: 277 missing'}; Write-Host 'PASS: both migration files present (apply-and-verify steps are executed via Supabase MCP at runtime; DB assertions checked in acceptance_criteria)'"</automated>
  </verify>
  <acceptance_criteria>
    - Supabase MCP execution of migration 276 returns no error and surfaces the RAISE NOTICE text `Post-verification PASSED: gov_count=1, office_count=5, split_orphans=0` (post-verification DO block PASS in 276)
    - Supabase MCP execution of migration 277 returns no error and surfaces the RAISE NOTICE text `Post-verification PASSED: gov_count=1, office_count=6, split_orphans=0` (post-verification DO block PASS in 277)
    - SQL `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id IN ('24037','2446475') AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.state = 'md')` returns 0
    - SQL `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='24037' AND d.district_type='COUNTY' AND d.state='md'` returns 5
    - SQL `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='2446475' AND d.state='md'` returns 6
    - SQL `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -24037005 AND -24037001 AND office_id IS NOT NULL` returns 5 (office_id back-fill confirmed)
    - SQL `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2446475006 AND -2446475001 AND office_id IS NOT NULL` returns 6
    - SQL `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('276','277') ORDER BY version` returns exactly 2 rows ['276','277']
    - Idempotent re-application of both migrations completes without raising EXCEPTION and does not change any of the COUNT(*) results above
  </acceptance_criteria>
  <done>Migrations 276 and 277 are applied to production Supabase, post-verification DO blocks PASSED, section-split detector reports 0 orphans for both geo_ids, and re-running both migrations changes nothing (idempotent). MD-DEEP-01 and MD-DEEP-03 data side is complete; Plan 95-02 can now resolve politician UUIDs and upload headshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local SQL files → production Supabase | Migration SQL crosses from filesystem into the production DB via Supabase MCP. A malformed migration without WHERE NOT EXISTS guards would corrupt government/chamber/district rows. |
| Verified roster (RESEARCH.md) → SQL literals | Hand-authored politician names + external_ids must match the verified roster exactly; transcription errors create orphan politicians with no matching offices. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-01 | Tampering | Migration SQL strings (governments INSERT) | mitigate | WHERE NOT EXISTS guard on every governments INSERT (essentials.governments has no unique constraint on geo_id per CONTEXT canonical_refs); post-verification gate fails if v_gov_count<>1 (rolls back transaction). |
| T-95-01-02 | Tampering | Migration SQL strings (chambers INSERT) | mitigate | WHERE NOT EXISTS guard on (name, government_id) for chamber; slug column omitted (GENERATED ALWAYS); idempotency proven by re-application step in Task 3. |
| T-95-01-03 | Tampering | Migration SQL strings (districts INSERT) | mitigate | WHERE NOT EXISTS on (geo_id, district_type, state); state='md' lowercase per routing convention; section-split detector gate (v_split_count=0) rolls back if any geofence lacks a matching district row. |
| T-95-01-04 | Repudiation | Production data writes | mitigate | All writes go through numbered migration files + supabase_migrations.schema_migrations ledger entries ('276','277'); the file is the audit trail. |
| T-95-01-05 | Denial of Service | Long-running migrations | accept | Both migrations contain only ~20 row inserts; runtime is sub-second. No risk of DoS. |
| T-95-01-06 | Information Disclosure | Politician PII in migrations | accept | All names + titles are public-record elected officials sourced from official government websites (stmaryscountymd.gov, leonardtown.somd.com). No private data. |
| T-95-01-07 | Elevation of Privilege | Supabase service-role usage | accept | Migration application uses standard Supabase MCP execute_sql — already governed by project conventions and feedback_supabase_local_is_remote.md. No new privilege surface. |
| T-95-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; SQL-only changes. RESEARCH.md "Package Legitimacy Audit" confirms no new packages introduced. |
</threat_model>

<verification>
Plan-level success state after all 3 tasks:
- Two new migration files exist (276 + 277) with all structural assertions in their verify blocks passing
- Both migrations applied to production Supabase with PASSED post-verification DO blocks
- 1 St. Mary's County government row + 1 chamber + 1 COUNTY district + 5 politicians + 5 offices (all office_id back-filled)
- 1 Leonardtown government row + 1 chamber + 1 LOCAL_EXEC district + 1 LOCAL district + 6 politicians + 6 offices (all office_id back-filled)
- supabase_migrations.schema_migrations contains versions '276' and '277'
- Section-split detector returns 0 rows across both geo_ids
- Idempotent re-application of both migrations produces no additional rows
- MD-DEEP-01 (St. Mary's County government + commissioners chamber + county boundary linked) — data side COMPLETE
- MD-DEEP-03 (Leonardtown government + town officials seeded) — data side COMPLETE (headshots in Plan 95-02)
- MD-DEEP-02 data side ready for Plan 95-02 (5 commissioner politician UUIDs now resolvable from -24037005..-24037001)
</verification>

<success_criteria>
- Both migration files committed to disk and applied to Supabase with no rollback
- Section-split detector returns 0 across geo_ids 24037 and 2446475
- 11 politicians total seeded (5 St. Mary's + 6 Leonardtown), each with office_id back-filled
- Both migration ledger versions present ('276','277')
- Idempotent re-run produces zero deltas (proves migration safety for future re-application)
</success_criteria>

<output>
Create `.planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-01-SUMMARY.md` on completion including:
- Counts confirming all DB assertions in Task 3 acceptance criteria
- Verbatim RAISE NOTICE text from each migration's post-verification DO block
- ID mapping table: external_id → politician.id (UUID) for all 11 officials (Plan 95-02 will need this; query: `SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id BETWEEN -24037005 AND -24037001 OR external_id BETWEEN -2446475006 AND -2446475001 ORDER BY external_id DESC`)
- Idempotency re-run row counts (must all be 0)
- Ledger entry confirmation (versions '276','277' present)
</output>
