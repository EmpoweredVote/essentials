---
phase: 111-ma-stances-execs-federal
plan: 03
type: execute
wave: 3
depends_on:
  - 111-02
files_modified:
  - C:/EV-Accounts/backend/migrations/365_warren_stances.sql
autonomous: true
requirements:
  - MA-STANCES-02
must_haves:
  truths:
    - "Elizabeth Warren has >=15 stance rows in inform.politician_answers after migration 365 applies"
    - "Every Warren stance row in inform.politician_context has at least 1 URL in sources (100% citation rate per D-10)"
    - "No defaulted/neutral fallback rows exist — only topics with documented evidence are inserted"
    - "D-05: per-individual SQL file 365_warren_stances.sql created"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/365_warren_stances.sql"
      provides: "Per-individual stance migration for US Senator Elizabeth Warren"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "365_warren_stances.sql"
      to: "essentials.politicians (external_id=-200101)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research Elizabeth Warren's (US Senator MA, external_id=-200101) public record across all currently-active compass topics, write per-individual stance migration 365, apply it to production Supabase, and verify stance row count.

Purpose: Partially satisfies MA-STANCES-02 (MA federal stances). Warren has 12-year Senate career (2013–present) + 2020 presidential run — target 18–21+ stances per D-09; ROADMAP minimum ≥15.

Output: `365_warren_stances.sql` applied; ≥15 stance rows with 100% citation rate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md
@.planning/phases/111-ma-stances-execs-federal/111-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Warren UUID + active topics + research all topics</name>
  <read_first>
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01 through D-10)
    - .planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120)
    - C:/EV-Accounts/backend/migrations/155_ma_us_senators.sql (lines 19-30 to confirm Warren external_id=-200101)
  </read_first>
  <action>
    Step A (UUID + topic resolution via mcp__supabase-local__execute_sql):
      SELECT id FROM essentials.politicians WHERE external_id = -200101;
      Record as ${WARREN_UUID}. If 0 rows, abort — migration 155 must be applied first.
      SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;
      Record full list as ${TOPIC_LIST}.

    Step B (research — ONE agent only, NEVER parallel per D-08):
      Research Warren's documented positions on EVERY topic in ${TOPIC_LIST}.
      Target: 18–21+ stances; ROADMAP minimum ≥15.
      Warren's documented record includes:
        - campaign-finance: DISCLOSE Act, Citizens United opposition, wealth tax proposals
        - healthcare: Medicare for All advocate, ACA defender
        - housing: Housing Act legislation, anti-speculation proposals
        - taxes: wealth tax (2-cent tax on ultra-wealthy), corporate minimum tax
        - social-security: expansion advocate
        - abortion: strong supporter, WHPA co-sponsor
        - redistricting: democracy reform packages
        - voting-rights: For the People Act, John Lewis VRA
        - civil-rights: broad civil rights record
        - immigration: path to citizenship, DACA defense; also critiqued certain enforcement
        - tariffs: mixed record — supported some tariffs on China
        - ukraine-support: consistent support
        - economic-development/fossil-fuels: Green New Deal support
        - climate-change: strong record
        - ai-regulation: algorithmic accountability legislation
        - misinformation: social media regulation proposals
        - trans-athletes: LGBTQ+ support
        - childcare/school-vouchers: universal childcare proposals
        - deportation: opposed Trump mass deportation
        - judicial-*: court reform proposals; judicial independence
      Omit topics with no evidence — no neutral defaults.
      For each topic found: record { topic_slug, topic_id, value (1-5), reasoning, sources }.

    Step C: Save to `.planning/phases/111-ma-stances-execs-federal/_tmp-warren-research.json`.
  </action>
  <verify>
    <automated>
      SELECT id FROM essentials.politicians WHERE external_id = -200101; -- must return 1 row
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/111-ma-stances-execs-federal/_tmp-warren-research.json"
      ((Get-Content ".../_tmp-warren-research.json" | ConvertFrom-Json).Count -ge 15)
    </automated>
  </verify>
  <done>Warren UUID resolved; research JSON ≥15 entries with non-empty sources.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 365_warren_stances.sql</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251)
    - .planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md
    - .planning/phases/111-ma-stances-execs-federal/_tmp-warren-research.json
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/365_warren_stances.sql` following 282 pattern exactly:
    Header comment (Migration 365: Elizabeth Warren Stances / US Senator MA) → Topic UUID reference block → BEGIN; → section header (-- Elizabeth Warren) → INSERT pairs per research JSON → COMMIT; → Verification comment block with ${WARREN_UUID}.
    Hard rules: exactly one BEGIN; and COMMIT;, all values as float (1.0..5.0), $$...$$ dollar-quoting, ARRAY[...]::text[]::text[], paired inserts, ≥15 paired entries.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/365_warren_stances.sql"
      (Select-String -Path "..." -Pattern '^BEGIN;$' -SimpleMatch).Count -eq 1
      (Select-String -Path "..." -Pattern '^COMMIT;$' -SimpleMatch).Count -eq 1
      $a=(Select-String -Path "..." -Pattern 'INSERT INTO inform.politician_answers').Count; $c=(Select-String -Path "..." -Pattern 'INSERT INTO inform.politician_context').Count; ($a -eq $c) -and ($a -ge 15)
    </automated>
  </verify>
  <done>Migration file exists; BEGIN/COMMIT paired; ≥15 answer+context INSERT pairs.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 365 + verify + cleanup</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/365_warren_stances.sql
  </read_first>
  <action>
    Step A: Pass full file content to mcp__supabase-local__execute_sql.
    Step B: Verify:
      SELECT (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-200101)) AS answer_count,
             (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-200101) AND pc.politician_id IS NULL) AS unpaired,
             (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-200101) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0)) AS uncited;
      Expected: answer_count>=15, unpaired=0, uncited=0.
    Step C: Delete `_tmp-warren-research.json`.
  </action>
  <verify>
    <automated>answer_count>=15, unpaired=0, uncited=0 from verification query.</automated>
  </verify>
  <done>Migration 365 applied; row count ≥15; 0 unpaired; 0 uncited; temp file deleted.</done>
</task>

</tasks>

<verification>
- Stance count for Warren >=15, unpaired=0, uncited=0
- Migration 365_warren_stances.sql committed to EV-Accounts repo
</verification>

<success_criteria>
- MA-STANCES-02 partially satisfied (Warren portion)
- ROADMAP Phase 111 success criterion #1 partially met (≥15 for Warren)
- D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-10 all honored
</success_criteria>

<output>
Create `.planning/phases/111-ma-stances-execs-federal/111-03-SUMMARY.md` documenting stance count for Warren, topics omitted, sources used.
</output>
