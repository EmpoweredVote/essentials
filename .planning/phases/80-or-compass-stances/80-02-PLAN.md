---
phase: 80-or-compass-stances
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-suzanne-bonamici.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-cliff-bentz.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-maxine-dexter.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-val-hoyle.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-janelle-bynum.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-andrea-salinas.csv
  - C:/EV-Accounts/backend/scripts/apply-suzanne-bonamici-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-cliff-bentz-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-maxine-dexter-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-val-hoyle-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-janelle-bynum-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-andrea-salinas-stances.ts
autonomous: true
requirements:
  - SC-1
  - SC-3

must_haves:
  truths:
    - "All 6 OR US House representatives have ≥1 row in inform.politician_answers"
    - "Bentz (R) stance values include at least one value ≥ 3 on a national topic (conservative lean is observable)"
    - "Every research + apply ran sequentially within Plan 02 (D-05)"
    - "Every stance value is an integer 1–5 (D-07)"
    - "Every stance row has a matching inform.politician_context citation (D-06)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/apply-suzanne-bonamici-stances.ts"
      provides: "CSV→DB apply script for Bonamici stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-cliff-bentz-stances.ts"
      provides: "CSV→DB apply script for Bentz stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-maxine-dexter-stances.ts"
      provides: "CSV→DB apply script for Dexter stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-val-hoyle-stances.ts"
      provides: "CSV→DB apply script for Hoyle stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-janelle-bynum-stances.ts"
      provides: "CSV→DB apply script for Bynum stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-andrea-salinas-stances.ts"
      provides: "CSV→DB apply script for Salinas stances"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-suzanne-bonamici.csv"
      provides: "Bonamici stance research CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-cliff-bentz.csv"
      provides: "Bentz stance research CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-maxine-dexter.csv"
      provides: "Dexter stance research CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-val-hoyle.csv"
      provides: "Hoyle stance research CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-janelle-bynum.csv"
      provides: "Bynum stance research CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-andrea-salinas.csv"
      provides: "Salinas stance research CSV"
  key_links:
    - from: "apply-*-stances.ts"
      to: "inform.politician_answers"
      via: "INSERT ... ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "INSERT INTO inform.politician_answers"
    - from: "inform.politician_answers row"
      to: "inform.politician_context row"
      via: "shared (politician_id, topic_id) citation"
      pattern: "INSERT INTO inform.politician_context"
---

<objective>
Research and ingest compass stances for the 6 Oregon US House Representatives (Bonamici CD-1, Bentz CD-2, Dexter CD-3, Hoyle CD-4, Bynum CD-5, Salinas CD-6) so each has ≥1 row in `inform.politician_answers`. Congressional vote records (congress.gov, ProPublica Congress API, GovTrack) are the primary evidence source, supplemented by Ballotpedia and OR legislature records where applicable.

Purpose: Closes SC-1 for federal officials (already-done senators Wyden + Merkley plus the 6 House reps), contributes to SC-3 (one-at-a-time rule), and demonstrates Val Hoyle compass render for SC-4.

Output:
- 6 CSV files in `C:/EV-Accounts/backend/data/stance-research/`
- 6 apply scripts in `C:/EV-Accounts/backend/scripts/`
- New rows in `inform.politician_answers` and `inform.politician_context` for all 6 House reps
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-or-compass-stances/80-CONTEXT.md
@.planning/phases/80-or-compass-stances/80-RESEARCH.md
@.planning/phases/80-or-compass-stances/80-VALIDATION.md
@C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
@.planning/phases/80-or-compass-stances/80-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research + ingest Suzanne Bonamici (CD-1, D) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-suzanne-bonamici.csv
    C:/EV-Accounts/backend/scripts/apply-suzanne-bonamici-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Single research agent only. Do NOT spawn parallel research tasks. Next task starts only after this task's apply script logs `Done — Upserted: N, Skipped: M`.

    Step 1 — Research Suzanne Bonamici (politician_id UUID `6ffb9093-7489-4197-aebc-67065c239fc3`, external_id -4102001, OR CD-1, Democrat, multiple terms). Run ONE research agent across ALL 36 compass topics per D-08. Primary sources: congress.gov vote records, ProPublica Congress API (`projects.propublica.org/api-docs/congress-api/`), GovTrack.us scoring, ballotpedia.org, votesmart.org candidate questionnaire. Aim for 18+ stances given her long tenure and consistent record.

    Step 2 — Write CSV `2026-05-30-suzanne-bonamici.csv` with politician_id = `6ffb9093-7489-4197-aebc-67065c239fc3`. Header + format identical to Plan 01.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-suzanne-bonamici-stances.ts`. Update csvPath. Keep `parseInt(r.value)` (D-07 — apply-solis-stances `3 - parseInt` inversion is forbidden, see Pitfall 2 of 80-RESEARCH.md).

    Step 4 — Run `npx tsx scripts/apply-suzanne-bonamici-stances.ts` from `C:/EV-Accounts/backend/`. Wait for `Done — Upserted: N, Skipped: M`.

    Step 5 — Write citation rows in `inform.politician_context` for every upserted answer (D-06).

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '6ffb9093-7489-4197-aebc-67065c239fc3'` returns ≥ 10. Value range check returns MIN ≥ 1 AND MAX ≤ 5.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-suzanne-bonamici.csv` exists with politician_id = `6ffb9093-7489-4197-aebc-67065c239fc3` on every row; ≥ 10 rows
    - File `apply-suzanne-bonamici-stances.ts` exists with `parseInt(r.value)` and no `3 - parseInt` inversion
    - `npx tsx scripts/apply-suzanne-bonamici-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 10)
    - SQL: stance count for Bonamici ≥ 10
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>Bonamici has ≥10 evidence-cited stances with citations; task fully completed before Task 2 (D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Research + ingest Cliff Bentz (CD-2, R) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-cliff-bentz.csv
    C:/EV-Accounts/backend/scripts/apply-cliff-bentz-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Starts ONLY after Task 1 completed. Single research agent — no parallel runs.

    Step 1 — Research Cliff Bentz (politician_id UUID `fb00c887-11f5-46f2-b822-f9848368bbd2`, external_id -4102002, OR CD-2, Republican, multiple terms). Run ONE research agent across ALL 36 compass topics. Primary sources: congress.gov, ProPublica, GovTrack, ballotpedia.org, bentz.house.gov press releases. Bentz has a documented conservative record on energy / public lands / immigration / abortion / taxes — expect values ≥ 3 on national topics. Aim for 12+ stances. CRITICAL: D-06 — record actual cited positions, do not extrapolate from party.

    Step 2 — Write CSV `2026-05-30-cliff-bentz.csv` with politician_id = `fb00c887-11f5-46f2-b822-f9848368bbd2`.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-cliff-bentz-stances.ts`. Update csvPath. Keep `parseInt(r.value)` (D-07).

    Step 4 — `npx tsx scripts/apply-cliff-bentz-stances.ts` from `C:/EV-Accounts/backend/`. Wait for completion.

    Step 5 — Write citation rows (D-06).

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = 'fb00c887-11f5-46f2-b822-f9848368bbd2'` returns ≥ 8. AND `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id='fb00c887-11f5-46f2-b822-f9848368bbd2' AND value >= 3` returns ≥ 1 (conservative lean observable).</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-cliff-bentz.csv` exists with politician_id = `fb00c887-11f5-46f2-b822-f9848368bbd2` on every row
    - File `apply-cliff-bentz-stances.ts` uses `parseInt(r.value)` (no inversion)
    - `npx tsx scripts/apply-cliff-bentz-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 8)
    - SQL: stance count for Bentz ≥ 8; at least one value ≥ 3 (sanity gate — confirms value scale not inverted)
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>Bentz has ≥8 evidence-cited stances with at least one value ≥ 3 (sanity check on scale orientation); Task 1 completed first.</done>
</task>

<task type="auto">
  <name>Task 3: Research + ingest Maxine Dexter (CD-3, D) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-maxine-dexter.csv
    C:/EV-Accounts/backend/scripts/apply-maxine-dexter-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Starts ONLY after Task 2 completed. Single research agent only.

    Step 1 — Research Maxine Dexter (politician_id UUID `13dcf1a8-c0bf-4e2f-92aa-46637182b42a`, external_id -4102003, OR CD-3, Democrat, first House term but multi-term OR state rep). Run ONE research agent across ALL 36 compass topics. Primary sources: congress.gov (limited first-term record), OR legislature vote history (richer for state-level topics), ballotpedia.org, dexter.house.gov, campaign archive. Aim for 8+ stances combining federal + state legislative votes.

    Step 2 — Write CSV `2026-05-30-maxine-dexter.csv` with politician_id = `13dcf1a8-c0bf-4e2f-92aa-46637182b42a`.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-maxine-dexter-stances.ts`. Update csvPath. Keep `parseInt(r.value)`.

    Step 4 — `npx tsx scripts/apply-maxine-dexter-stances.ts`. Wait for completion.

    Step 5 — Write citation rows (D-06).

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '13dcf1a8-c0bf-4e2f-92aa-46637182b42a'` returns ≥ 5. Value range MIN ≥ 1 AND MAX ≤ 5.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-maxine-dexter.csv` exists with politician_id = `13dcf1a8-c0bf-4e2f-92aa-46637182b42a` on every row
    - File `apply-maxine-dexter-stances.ts` uses `parseInt(r.value)`
    - `npx tsx scripts/apply-maxine-dexter-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 5)
    - SQL: stance count for Dexter ≥ 5
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>Dexter has ≥5 evidence-cited stances with citations; Task 2 completed first.</done>
</task>

<task type="auto">
  <name>Task 4: Research + ingest Val Hoyle (CD-4, D) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-val-hoyle.csv
    C:/EV-Accounts/backend/scripts/apply-val-hoyle-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Starts ONLY after Task 3 completed. Single research agent only.

    Step 1 — Research Val Hoyle (politician_id UUID `f6202cef-4e46-4db5-a9c0-c69ac9a8eccd`, external_id -4102004, OR CD-4, Democrat, 2 House terms + former OR Labor Commissioner + former state legislator). Run ONE research agent across ALL 36 compass topics. Primary sources: congress.gov, ProPublica, GovTrack, ballotpedia.org, boli.oregon.gov archive (her Labor Commissioner press releases), hoyle.house.gov, campaign site. Hoyle is the Nyquist spot-check target in 80-VALIDATION.md — aim for ≥ 8 stances per that target. Labor / healthcare / climate topics should be richest.

    Step 2 — Write CSV `2026-05-30-val-hoyle.csv` with politician_id = `f6202cef-4e46-4db5-a9c0-c69ac9a8eccd`.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-val-hoyle-stances.ts`. Update csvPath. Keep `parseInt(r.value)` (D-07).

    Step 4 — `npx tsx scripts/apply-val-hoyle-stances.ts`. Wait for completion.

    Step 5 — Write citation rows (D-06).

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = 'f6202cef-4e46-4db5-a9c0-c69ac9a8eccd'` returns ≥ 8 (per 80-VALIDATION.md Nyquist target). All values integers 1–5.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-val-hoyle.csv` exists with politician_id = `f6202cef-4e46-4db5-a9c0-c69ac9a8eccd` on every row
    - File `apply-val-hoyle-stances.ts` uses `parseInt(r.value)`
    - `npx tsx scripts/apply-val-hoyle-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 8)
    - SQL: stance count for Hoyle ≥ 8 (Nyquist spot-check requirement)
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>Hoyle has ≥8 evidence-cited stances with citations (Nyquist spot-check satisfied); Task 3 completed first.</done>
</task>

<task type="auto">
  <name>Task 5: Research + ingest Janelle Bynum (CD-5, D) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-janelle-bynum.csv
    C:/EV-Accounts/backend/scripts/apply-janelle-bynum-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Starts ONLY after Task 4 completed. Single research agent only.

    Step 1 — Research Janelle Bynum (politician_id UUID `7aad2a83-2f05-4570-aa7a-eb7a8c602ebd`, external_id -4102005, OR CD-5, Democrat, first House term but multi-term OR state rep with documented record). Run ONE research agent across ALL 36 compass topics. Primary sources: congress.gov (limited first-term), OR legislature vote history (rich state-level record), ballotpedia.org, bynum.house.gov, campaign archive. Aim for 8+ stances.

    Step 2 — Write CSV `2026-05-30-janelle-bynum.csv` with politician_id = `7aad2a83-2f05-4570-aa7a-eb7a8c602ebd`.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-janelle-bynum-stances.ts`. Update csvPath. Keep `parseInt(r.value)`.

    Step 4 — `npx tsx scripts/apply-janelle-bynum-stances.ts`. Wait for completion.

    Step 5 — Write citation rows (D-06).

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '7aad2a83-2f05-4570-aa7a-eb7a8c602ebd'` returns ≥ 5. Value range check passes.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-janelle-bynum.csv` exists with politician_id = `7aad2a83-2f05-4570-aa7a-eb7a8c602ebd` on every row
    - File `apply-janelle-bynum-stances.ts` uses `parseInt(r.value)`
    - `npx tsx scripts/apply-janelle-bynum-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 5)
    - SQL: stance count for Bynum ≥ 5
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>Bynum has ≥5 evidence-cited stances with citations; Task 4 completed first.</done>
</task>

<task type="auto">
  <name>Task 6: Research + ingest Andrea Salinas (CD-6, D) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-andrea-salinas.csv
    C:/EV-Accounts/backend/scripts/apply-andrea-salinas-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Starts ONLY after Task 5 completed. Single research agent only.

    Step 1 — Research Andrea Salinas (politician_id UUID `5f6c498b-87dd-48fe-b744-62c8dced2ac3`, external_id -4102006, OR CD-6, Democrat, first House term + former OR state rep). Per 80-RESEARCH.md Open Question #3 — Andrea Salinas is the OR CD-6 representative; do NOT confuse with any other person of the same name. UUID is authoritative. Run ONE research agent across ALL 36 compass topics. Primary sources: congress.gov, ProPublica, GovTrack, OR legislature vote history, ballotpedia.org, salinas.house.gov, campaign archive. Aim for 8+ stances.

    Step 2 — Write CSV `2026-05-30-andrea-salinas.csv` with politician_id = `5f6c498b-87dd-48fe-b744-62c8dced2ac3`.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-andrea-salinas-stances.ts`. Update csvPath. Keep `parseInt(r.value)`.

    Step 4 — `npx tsx scripts/apply-andrea-salinas-stances.ts`. Wait for completion.

    Step 5 — Write citation rows (D-06).

    Step 6 — Run the per-wave SC-1 sampling SQL from 80-VALIDATION.md to confirm all 6 House reps have stances before declaring Plan 02 complete:
    ```
    SELECT p.full_name, COUNT(pa.id) AS stance_count
    FROM essentials.politicians p
    LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
    WHERE p.external_id BETWEEN -4102006 AND -4102001
    GROUP BY p.full_name
    ORDER BY p.full_name;
    ```
    Expected: 6 rows; Bonamici ≥ 10; Hoyle ≥ 8; Bentz ≥ 8; others ≥ 5.

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(DISTINCT p.id) FROM essentials.politicians p JOIN inform.politician_answers pa ON pa.politician_id=p.id WHERE p.external_id BETWEEN -4102006 AND -4102001` returns 6. And `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '5f6c498b-87dd-48fe-b744-62c8dced2ac3'` returns ≥ 5.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-andrea-salinas.csv` exists with politician_id = `5f6c498b-87dd-48fe-b744-62c8dced2ac3` on every row
    - File `apply-andrea-salinas-stances.ts` uses `parseInt(r.value)`
    - `npx tsx scripts/apply-andrea-salinas-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 5)
    - SQL: stance count for Salinas ≥ 5
    - SQL per-wave check: all 6 OR US House reps (external_ids -4102001..-4102006) have stance_count > 0
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>All 6 OR US House reps have ≥5 evidence-cited stances (Bonamici ≥10, Hoyle ≥8, Bentz ≥8); SC-1 satisfied for federal officials; Task 5 completed before Task 6 (D-05).</done>
</task>

</tasks>

<verification>
After all 6 tasks complete, run the Plan-02 per-wave SQL from `80-VALIDATION.md`:

```sql
SELECT p.full_name, COUNT(pa.id) AS stance_count
FROM essentials.politicians p
LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
WHERE p.external_id BETWEEN -4102006 AND -4102001
GROUP BY p.full_name
ORDER BY p.full_name;
```

Expected: 6 rows; Bonamici ≥ 10; Hoyle ≥ 8; Bentz ≥ 8 (with ≥1 value ≥ 3 confirming scale orientation); Dexter/Bynum/Salinas ≥ 5.

Then process-audit: confirm 6 sequential research runs (no overlapping) — contributes to SC-3.
</verification>

<success_criteria>
- [ ] All 6 CSVs exist in `C:/EV-Accounts/backend/data/stance-research/`
- [ ] All 6 apply scripts exist and use `parseInt(r.value)` (no inversion)
- [ ] All 6 House reps have ≥1 row in `inform.politician_answers`
- [ ] Bentz has at least one value ≥ 3 on a national topic (sanity check on scale)
- [ ] Every `inform.politician_answers` row has a matching `inform.politician_context` row
- [ ] Process audit confirms one-at-a-time execution (SC-3)
</success_criteria>

<output>
Create `.planning/phases/80-or-compass-stances/80-02-SUMMARY.md` listing:
- The 6 CSV files and their row counts
- The 6 apply scripts written
- For each rep: external_id, UUID, party, final stance count, top 3 evidence sources
- Confirmation each research agent ran one-at-a-time (D-05)
- Output of the per-wave SQL gate
- Sanity result for Bentz (`SELECT MIN(value), MAX(value), AVG(value) FROM inform.politician_answers WHERE politician_id = 'fb00c887-11f5-46f2-b822-f9848368bbd2'`)
</output>
