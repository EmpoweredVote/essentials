---
phase: 106-va-compass-stances
plan: 02
type: execute
wave: 2
depends_on:
  - 106-01
files_modified:
  - C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql
autonomous: true
requirements:
  - VA-STANCES-01
must_haves:
  truths:
    - "Ghazala Hashmi has >=10 stance rows in inform.politician_answers after migration 327 applies"
    - "Every Hashmi context row has at least 1 URL in sources (100% citation rate per D-10)"
    - "No defaulted/neutral fallback rows exist — only topics with documented evidence are inserted"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql"
      provides: "Per-individual stance migration for Lt. Governor Ghazala Hashmi"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "327_hashmi_stances.sql"
      to: "essentials.politicians (external_id=-510002)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research Ghazala Hashmi's (Lt. Governor of Virginia, external_id=-510002) public record across all currently-active compass topics, write per-individual stance migration 327, apply to production Supabase, and verify counts.

Purpose: Continues VA-STANCES-01 satisfaction. Hashmi served as VA state senator (SD-15, then SD-10) 2019-2026 before becoming LG; her senate voting record is a primary source. ROADMAP minimum: >=10 stances. Apply per D-06 immediately upon completion before Plan 03 starts.

Output: `327_hashmi_stances.sql` applied; >=10 stance rows in `inform.politician_answers` for Hashmi; all rows paired in `inform.politician_context` with >=1 source URL each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/106-va-compass-stances/106-CONTEXT.md
@.planning/phases/106-va-compass-stances/106-PATTERNS.md
@.planning/phases/106-va-compass-stances/106-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Hashmi UUID, then research all active topics</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-01..D-10)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120)
    - C:/EV-Accounts/backend/migrations/317_va_state_executives.sql (lines 128-140 for Hashmi external_id=-510002)
  </read_first>
  <action>
    Step A: Run via mcp__supabase-local__execute_sql:
      SELECT id FROM essentials.politicians WHERE external_id = -510002;
    Record returned UUID as ${HASHMI_UUID}. If 0 rows, abort.

    Step B: Run ONE web-research subagent (per D-08) to gather Hashmi's documented positions on every active compass topic. Hashmi has documented record on (likely-evidence topics):
      - VA Senate Education Committee chair: school-vouchers, childcare, education-adjacent topics
      - Authored the VA Reproductive Rights Constitutional Amendment: abortion (strong evidence)
      - Climate / renewable energy votes in VA Senate: climate-change, fossil-fuels, local-environment
      - Immigration / refugee policy as Indian-American Muslim legislator: immigration, deportation, religious-freedom, civil-rights
      - Healthcare votes (Medicaid expansion era): healthcare, medicare/aid
      - Voting-rights and redistricting record in VA Senate: voting-rights, redistricting, campaign-finance
      - Public safety votes: public-safety-approach, judicial-criminal-justice, judicial-police-accountability
      - Trans rights / same-sex marriage votes: same-sex-marriage, trans-athletes
      - Tax / budget votes: taxes
      - Tariffs / Ukraine / social security / ai-regulation / misinformation: likely no direct VA Senate record — OMIT if no evidence (no defaults per D-01)

    Target: >=10 stances per ROADMAP; aim higher if evidence supports.

    Save research output as `.planning/phases/106-va-compass-stances/_tmp-hashmi-research.json` with same shape as Spanberger.
  </action>
  <verify>
    <automated>
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-hashmi-research.json"
      ((Get-Content "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-hashmi-research.json" | ConvertFrom-Json).Count -ge 10)
    </automated>
  </verify>
  <done>
    - Hashmi UUID resolved
    - Research JSON file exists with >=10 entries
    - Every entry has non-empty sources array
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 327_hashmi_stances.sql matching 282 pattern exactly</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md ("Full Example")
    - .planning/phases/106-va-compass-stances/_tmp-hashmi-research.json
    - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql (just-written reference for VA exec stance pattern)
  </read_first>
  <action>
    Create file `C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql` following the EXACT same structure as 326_spanberger_stances.sql, with these substitutions:
      - Header: "Migration 327: Ghazala Hashmi Stances" / "Lt. Governor of Virginia"
      - Section header: "Ghazala Hashmi"
      - politician_id literal: ${HASHMI_UUID} from Task 1
      - INSERTs sourced from _tmp-hashmi-research.json (one pair per topic with evidence)
      - Verification comment at end with ${HASHMI_UUID} and expected count

    Same hard rules as Plan 01 Task 2:
      - Exactly one BEGIN; and one COMMIT;
      - Equal counts of answers/context INSERTs (>=10 each)
      - Float-form value literals (N.0)
      - Dollar-quoted reasoning ($$..$$)
      - ARRAY['url']::text[]::text[] sources cast
      - Topic UUID reference block in header comments
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql"
      (Select-String -Path "C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql" -Pattern '^BEGIN;$' -SimpleMatch).Count -eq 1
      (Select-String -Path "C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql" -Pattern '^COMMIT;$' -SimpleMatch).Count -eq 1
      $a = (Select-String -Path "C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql" -Pattern 'INSERT INTO inform.politician_answers').Count
      $c = (Select-String -Path "C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql" -Pattern 'INSERT INTO inform.politician_context').Count
      ($a -eq $c) -and ($a -ge 10)
    </automated>
  </verify>
  <done>
    - Migration file exists with valid BEGIN/COMMIT wrapper
    - Equal counts of answers/context INSERTs (>=10 each)
    - Topic UUID reference block present
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 327 + verify counts</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-06)
  </read_first>
  <action>
    Step A: Read the full content of `C:/EV-Accounts/backend/migrations/327_hashmi_stances.sql` and pass to mcp__supabase-local__execute_sql (NOT apply_migration).

    Step B (verification via mcp__supabase-local__execute_sql):
      SELECT
        (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510002)) AS answer_count,
        (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510002) AND pc.politician_id IS NULL) AS unpaired,
        (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510002) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)) AS uncited;
    Required: answer_count >= 10, unpaired = 0, uncited = 0.

    Step C: Delete `.planning/phases/106-va-compass-stances/_tmp-hashmi-research.json`.
  </action>
  <verify>
    <automated>
      Via mcp__supabase-local__execute_sql, the SELECT above must return answer_count >= 10, unpaired = 0, uncited = 0.
    </automated>
  </verify>
  <done>
    - Migration 327 applied without error
    - Hashmi stance count >= 10
    - unpaired = 0, uncited = 0
  </done>
</task>

</tasks>

<verification>
- mcp__supabase-local__execute_sql confirms Hashmi stance count >= 10
- Citation gate (uncited = 0) passes
- Pairing gate (unpaired = 0) passes
- Migration 327 file committed to EV-Accounts repo
</verification>

<success_criteria>
- VA-STANCES-01 advanced (Hashmi portion)
- ROADMAP Phase 106 success criterion #1 partially met (>=10 for Hashmi)
- D-01, D-06, D-07, D-08, D-10 honored
</success_criteria>

<output>
Create `.planning/phases/106-va-compass-stances/106-02-SUMMARY.md` documenting stance count delivered, omitted topics, source domain summary.
</output>
