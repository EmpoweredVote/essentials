---
phase: 212-backend-place-name-resolver-national-fallback
plan: 03
type: execute
wave: 2
depends_on: ["212-01", "212-02"]
files_modified: []
autonomous: false
requirements: [RSLV-01, RSLV-02]
must_haves:
  truths:
    - "Migrations 1377 + 1378 are applied to the LIVE production DB and their indexes/tables exist (not just authored on disk)"
    - "The Gazetteer Places+Counties data is loaded into the live tables (counties ~3143; places tens of thousands)"
    - "A second ingest run produces zero net-new rows (live idempotency proof, D-11)"
  artifacts: []
  key_links:
    - from: "live DB catalog"
      to: "idx_governments_name_trgm / idx_geofence_boundaries_name_trgm / gazetteer_places / gazetteer_counties"
      via: "pg_indexes + information_schema.tables confirm existence post-apply"
      pattern: "gazetteer_places|idx_.*_name_trgm"
---

<objective>
Apply the Plan 01 migrations (1377 trgm indexes + 1378 Gazetteer tables) to the LIVE production Supabase DB, then run the Plan 02 ingest to populate nationwide place coverage — and prove idempotency with a second net-zero run. This is the [BLOCKING] schema-push gate: build/type checks pass WITHOUT the migration applied (types are not DB-derived here), so a live apply + row-count verify is the only thing that prevents a false-positive verification state downstream. Nothing later (the resolver query, its EXPLAIN index-scan check, the smoke test) is real until this runs.

Purpose: Ship the index + query enablement live in the same phase/PR (Pitfall 4); make RSLV-01/RSLV-02 verifiable against real data.
Output: Applied migrations + populated Gazetteer tables in production; recorded row counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-01-SUMMARY.md
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-02-SUMMARY.md
@.planning/phases/212-backend-place-name-resolver-national-fallback/212-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply migrations 1377 + 1378 to the live DB and verify catalog objects</name>
  <files>(live-DB apply of C:/EV-Accounts/backend/migrations/1377_location_search_trgm_indexes.sql + 1378_gazetteer_places_counties.sql via psql — no new repo files)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1377_location_search_trgm_indexes.sql
    - C:/EV-Accounts/backend/migrations/1378_gazetteer_places_counties.sql
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md §"gsd-executor has no Supabase MCP — DB-verify/smoke steps run inline"
  </read_first>
  <action>
    Apply both migration files INLINE via psql against the live DB using the backend's DATABASE_URL (gsd-executor has NO Supabase MCP — this is a scripted psql apply, NOT an MCP apply; mirrors the project's inline-migration convention). Apply 1377 then 1378 in order. Then verify catalog state: (a) `SELECT indexname FROM pg_indexes WHERE indexname IN ('idx_governments_name_trgm','idx_geofence_boundaries_name_trgm','idx_gazetteer_places_name_trgm','idx_gazetteer_counties_name_trgm')` returns all 4; (b) `SELECT to_regclass('essentials.gazetteer_places'), to_regclass('essentials.gazetteer_counties')` are both non-null. Record confirmations in the SUMMARY. If applying requires interactive auth that cannot be scripted, halt at the checkpoint below for the operator to run the apply.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT count(*) FROM pg_indexes WHERE indexname IN ('idx_governments_name_trgm','idx_geofence_boundaries_name_trgm','idx_gazetteer_places_name_trgm','idx_gazetteer_counties_name_trgm')"</automated>
  </verify>
  <acceptance_criteria>
    - psql confirms all 4 trgm indexes present (count == 4)
    - to_regclass for both gazetteer tables is non-null
    - No errors during apply; 1377 applied before 1378
  </acceptance_criteria>
  <done>Both migrations live in production; 4 indexes + 2 tables confirmed via catalog query.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Run the Gazetteer ingest live + prove idempotency</name>
  <files>(live run of C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.ts against the production DB — no new repo files)</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/ingest-gazetteer-places-counties.ts
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md §D-11 (idempotent)
  </read_first>
  <action>
    Run the Plan 02 ingest script against the live DB (Places + Counties). Verify row counts: `SELECT count(*) FROM essentials.gazetteer_counties` ~3143 (all US counties/equivalents) and `SELECT count(*) FROM essentials.gazetteer_places` in the tens of thousands (incorporated places + CDPs). Then run the ingest a SECOND time and confirm the counts are UNCHANGED (net-zero-new — live proof of D-11 idempotency). Record before/after counts in the SUMMARY.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.gazetteer_counties) AS counties, (SELECT count(*) FROM essentials.gazetteer_places) AS places"</automated>
  </verify>
  <acceptance_criteria>
    - gazetteer_counties count is ~3143 (>= 3100)
    - gazetteer_places count is in the tens of thousands (>= 20000)
    - Second ingest run leaves both counts identical (delta == 0)
  </acceptance_criteria>
  <done>Nationwide Gazetteer data loaded; re-run is net-zero-new (idempotency proven live).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator confirmation of live migration apply + Gazetteer ingest</name>
  <files>(human-verify checkpoint — no executor file action; confirms live DB state from Tasks 1-2)</files>
  <read_first>
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-03-SUMMARY.md (the applied-migrations + before/after ingest counts recorded by Tasks 1-2)
    - .planning/phases/212-backend-place-name-resolver-national-fallback/212-CONTEXT.md §schema_push_requirement (build/type checks pass WITHOUT the migration applied — false-positive guard)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor file action. The operator confirms, against the LIVE production DB, that migrations 1377+1378 applied and the Gazetteer data ingested (4 trgm indexes present, both tables populated with sane counts, spot-check resolves) BEFORE any resolver/route work proceeds. This prevents the documented false-positive verification state where tsc passes without the live schema. See &lt;how-to-verify&gt;.</action>
  <what-built>Migrations 1377 (trgm indexes) + 1378 (Gazetteer tables) applied to the LIVE production DB, and the Gazetteer Places+Counties data ingested (with an idempotent re-run).</what-built>
  <how-to-verify>
    1. Confirm the four trgm indexes exist: psql "$DATABASE_URL" -c "SELECT indexname FROM pg_indexes WHERE indexname LIKE 'idx_%name_trgm'"
    2. Confirm row counts are sane: psql "$DATABASE_URL" -c "SELECT count(*) FROM essentials.gazetteer_counties; SELECT count(*) FROM essentials.gazetteer_places;" (~3143 counties; tens of thousands of places)
    3. Spot-check a known non-curated place resolves: psql "$DATABASE_URL" -c "SELECT geo_id,name,state FROM essentials.gazetteer_places WHERE name ILIKE 'Paradise' LIMIT 5" (expect multiple states)
  </how-to-verify>
  <resume-signal>Type "approved" once the live DB shows the 4 indexes, both tables populated with sane counts, and the spot-check returns rows.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy pipeline → production Postgres | Live DDL + bulk DML against the production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-06 | Tampering | live migration apply | mitigate | IF NOT EXISTS DDL (Plan 01) makes re-apply safe; apply order 1377→1378; catalog verify before proceeding |
| T-212-07 | Denial of Service | index build lock on large tables | accept | governments/geofence_boundaries are modest; GIN build is fast; off-peak apply if operator prefers |
| T-212-08 | Repudiation | unverified "applied" state | mitigate | [BLOCKING] human-verify checkpoint requires live catalog + count confirmation before any downstream plan runs |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- 4 trgm indexes present in pg_indexes; both gazetteer tables non-null via to_regclass
- gazetteer_counties ~3143, gazetteer_places >= 20000; second run net-zero-new
- Operator checkpoint approved
</verification>

<success_criteria>
- Resolver DB foundation is LIVE (not just authored) — false-positive verification state prevented
- Nationwide place coverage populated and idempotent
</success_criteria>

<output>
Create `.planning/phases/212-backend-place-name-resolver-national-fallback/212-03-SUMMARY.md` recording applied migrations + before/after ingest counts.
</output>
