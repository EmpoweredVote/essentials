---
phase: 177-city-of-hillsboro-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["177-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-hillsboro-headshots.py
  - C:/EV-Accounts/backend/migrations/1151_hillsboro_headshots.sql
autonomous: false
requirements: [WASH-03]

must_haves:
  truths:
    - "All 7 Hillsboro officials have a 600×750 headshot uploaded to Supabase Storage politician_photos/{uuid}-headshot.jpg"
    - "Every headshot was cropped to 4:5 FIRST, then resized to 600×750 Lanczos q90 (no distortion, no overlays)"
    - "Sources are the CivicWeb portal (hillsboro-oregon.civicweb.net) with Ballotpedia/campaign fallback for genuine gaps (hillsboro-oregon.gov is WAF-403)"
    - "7 politician_images rows exist (type='default', photo_license per source), one per official"
    - "No image has superimposed text/graphics over the face; each is a valid non-zero JPEG (HTTP 200 from CDN)"
    - "Any genuine no-photo gap is documented, not fabricated"
    - "Implements CONTEXT decision D-12 (headshot sourcing + pipeline)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-hillsboro-headshots.py"
      provides: "Download → composite → crop 4:5 → resize 600×750 → upload pipeline for 7 officials"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/1151_hillsboro_headshots.sql"
      provides: "7 politician_images INSERTs (audit-only, NOT registered in the ledger)"
      min_lines: 30
      contains: "politician_images"
  key_links:
    - from: "_tmp-hillsboro-headshots.py"
      to: "Supabase Storage politician_photos bucket"
      via: "PUT {uuid}-headshot.jpg x-upsert:true"
      pattern: "politician_photos"
    - from: "1151_hillsboro_headshots.sql"
      to: "essentials.politicians (by external_id)"
      via: "subselect politician_id per row"
      pattern: "external_id = -41341"
---

<objective>
Author the headshot pipeline script and the audit-only migration that records the 7 Hillsboro
officials' portraits. Portraits come from the CivicWeb portal (hillsboro-oregon.gov is Akamai
WAF-403), are cropped 4:5 then resized to 600×750 Lanczos q90, and are mirrored to Supabase Storage.

Purpose: Satisfy the headshot portion of WASH-03 — all 7 officials render with 600×750 photos,
genuine gaps documented.
Output: `_tmp-hillsboro-headshots.py` (gitignored helper, run by the orchestrator) + audit-only
migration `1151_hillsboro_headshots.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-RESEARCH.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES both files. It has NO DB/Storage access. The
INLINE ORCHESTRATOR runs `_tmp-hillsboro-headshots.py` (which resolves each politician UUID by
external_id at runtime via psycopg2, downloads, processes, and uploads), then applies the audit
migration via `psql -f`. The `.py` file is a gitignored `_tmp-*` helper — do NOT commit it. The
`.sql` migration IS committed to the EV-Accounts repo.

**AUDIT-ONLY MIGRATION:** `1151_hillsboro_headshots.sql` does NOT write a schema_migrations ledger
row (headshot/stance migrations bypass the ledger; on-disk counter is authoritative). Keep the
literal strings `slug`, `schema_migrations`, and `photo_origin_url` OUT of comments.
</context>

<interfaces>
essentials.politician_images  (id, politician_id, url, type, photo_license)  -- NO photo_origin_url column; type='default'
-- CivicWeb portal headshot paths (verify each downloads valid non-zero JPEG bytes at Wave-0/runtime):
--   Beach Pace (-4134101)        user-29.jpg
--   Cristian Salgado (-4134102)  user-1249.jpg
--   Saba Anvery (-4134103)       user-1233.jpg
--   Kipperlyn Sinclair (-4134104) user-37.jpg
--   Elizabeth Case (-4134105)    user-90.jpg
--   Olivia Alcaire (-4134106)    user-28.jpg
--   Rob Harris (-4134107)        user-1234.jpg
-- Base: https://hillsboro-oregon.civicweb.net/FileStorage/content/UserImages/user-{N}.jpg
-- Fallbacks (per official, genuine gaps only): Ballotpedia (Rob Harris confirmed), campaign sites
--   (elizabethcase.com, electrobharris.com confirmed to exist). No Wikimedia hit for Beach Pace.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot pipeline script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-hillsboro-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-beaverton-headshots.py (the direct 400-line template — copy structure verbatim, rename city strings)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md §"_tmp-hillsboro-headshots.py"
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-RESEARCH.md §"Headshot Sources"
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-02-SUMMARY.md (minted UUIDs by external_id)
  </read_first>
  <action>
    Copy the Beaverton script structure. Set an OFFICIALS list of 7 dicts, each with ext_id, name,
    url (the CivicWeb user-{N}.jpg path from the <interfaces> block), and license ('press_use'
    default for CivicWeb official photos). Assert len(OFFICIALS)==7 and unique ext_ids. Resolve each
    politician UUID at RUNTIME by external_id via psycopg2 (do NOT hardcode UUIDs — read them live).
    Keep the config block identical to Beaverton: BUCKET='politician_photos', TARGET_SIZE=(600,750),
    JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS, and DESCRIPTIVE_HEADERS (requests-based, not
    a browser UA — the CivicWeb CDN returned a 0-byte response to one plain curl in research). Pipeline
    order per official: download (follow redirects), composite onto white if RGBA/PNG, CROP to 4:5
    FIRST, RESIZE to 600×750 Lanczos, reject any image with superimposed text/graphics, upload to
    politician_photos/{uuid}-headshot.jpg via PUT with x-upsert:true, emit a manifest line
    (ext_id → uuid → CDN url → source). Add a TEST-DOWNLOAD guard: before the full loop, fetch one
    image and assert non-zero bytes + valid PIL open; if it fails, fall back to the per-official
    Ballotpedia/campaign URL. Include the "gitignored _tmp helper, orchestrator runs it" docstring
    note. No fenced code in this action — the analog + 177-PATTERNS.md are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `python C:/EV-Accounts/backend/scripts/_tmp-hillsboro-headshots.py`; the script asserts 7 officials, valid non-zero JPEG per image, and prints the CDN manifest</automated>
  </verify>
  <acceptance_criteria>
    - OFFICIALS list has 7 entries with the exact ext_ids -4134101..-4134107 and CivicWeb user-{N}.jpg URLs
    - Asserts len==7 and unique ext_ids
    - UUIDs resolved at runtime by external_id (no hardcoded UUIDs)
    - TARGET_SIZE=(600,750), JPEG_QUALITY=90, Lanczos resample
    - Crop-to-4:5 happens BEFORE resize
    - Test-download guard present with per-official fallback
    - Docstring marks it a gitignored _tmp helper run by the orchestrator
  </acceptance_criteria>
  <done>Pipeline script authored; runs and uploads 7 valid 600×750 portraits (or documents genuine gaps).</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1151_hillsboro_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1132_beaverton_headshots.sql (91-line template)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md §"<N+1>_hillsboro_headshots.sql"
  </read_first>
  <action>
    Single BEGIN…COMMIT. Header comment: "AUDIT-ONLY (not registered in the ledger)". Seven
    politician_images INSERT … SELECT statements, one per official, columns EXACTLY
    (id, politician_id, url, type, photo_license) — NO photo_origin_url. politician_id resolved via
    subselect on external_id (-4134101..-4134107). url is the final Supabase CDN path
    (politician_photos/{uuid}-headshot.jpg — use the actual {uuid} minted per official; the
    orchestrator fills these from the script manifest before applying, or the executor templates
    them from 177-02-SUMMARY.md UUIDs). type='default'. photo_license per actual source ('press_use'
    for CivicWeb/Ballotpedia). Each INSERT guarded WHERE NOT EXISTS on politician_id (idempotent).
    NO ledger INSERT (audit-only). Keep `photo_origin_url`/`slug`/`schema_migrations` out of comments.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1151_hillsboro_headshots.sql` then runs `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -4134107 AND -4134101)` expecting 7</automated>
  </verify>
  <acceptance_criteria>
    - 7 politician_images INSERTs, columns exactly (id, politician_id, url, type, photo_license)
    - No photo_origin_url column anywhere
    - politician_id resolved via external_id subselect for each of -4134101..-4134107
    - type='default'; each guarded WHERE NOT EXISTS on politician_id
    - No ledger INSERT (audit-only)
    - `photo_origin_url` does NOT appear in comments
  </acceptance_criteria>
  <done>Audit migration authored; applies to record 7 image rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs pipeline, applies migration, verifies identity + quality</name>
  <what-built>The headshot pipeline script + audit migration. Orchestrator runs the pipeline and applies the migration.</what-built>
  <how-to-verify>
    1. Run `python C:/EV-Accounts/backend/scripts/_tmp-hillsboro-headshots.py`; confirm 7 valid uploads (non-zero bytes, HTTP 200 on each CDN URL). Note any official that fell back to Ballotpedia/campaign, and any genuine gap.
    2. Apply `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1151_hillsboro_headshots.sql`; confirm 7 politician_images rows (or documented gap count).
    3. Visual identity + quality check on each portrait: correct person (esp. verify no wrong-person swap — Pace/Salgado/Anvery/Sinclair/Case/Alcaire/Harris), 600×750, 4:5 framing, NO superimposed text/graphics over the face.
    4. Commit the migration: `git -C "C:/EV-Accounts" add backend/migrations/1151_hillsboro_headshots.sql && git -C "C:/EV-Accounts" commit`. Do NOT commit the _tmp-*.py helper.
  </how-to-verify>
  <acceptance_criteria>
    - 7 headshots uploaded (or genuine gaps documented, not fabricated)
    - 7 politician_images rows present (or count minus documented gaps)
    - Each portrait is the correct person, 600×750, no overlays
    - Audit migration committed; _tmp .py NOT committed
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official source + any gap, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Portraits fetched from hillsboro-oregon.civicweb.net / Ballotpedia / campaign sites |
| operator → Supabase Storage | Processed images uploaded to the public politician_photos bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-07 | Spoofing | Wrong-person or mislabeled headshot | mitigate | Source from official CivicWeb portal keyed to the roster; Task 3 visual identity check per official before commit |
| T-177-08 | Tampering | Corrupt/0-byte image (research saw a 0-byte CivicWeb response) or WAF "Access Denied" HTML saved as .jpg | mitigate | Test-download guard + valid-PIL-open assertion; requests DESCRIPTIVE_HEADERS + follow-redirects; never fetch WAF-403 hillsboro-oregon.gov |
| T-177-09 | Information Disclosure | Superimposed campaign text/graphics on portrait | mitigate | Pipeline rejects images with overlays; visual check confirms clean face |
| T-177-SC | Tampering | npm/pip/cargo installs (Pillow/psycopg2/requests) | mitigate | These are pre-existing project deps used by all prior headshot phases (Beaverton/Henderson) — no NEW package install introduced; if any pip install is needed, treat as [ASSUMED] and gate on a blocking human-verify before install |
</threat_model>

<verification>
- 7 valid 600×750 portraits uploaded; 7 politician_images rows; identity/quality confirmed; audit
  migration committed.
</verification>

<success_criteria>
- All 7 officials have a 600×750 CivicWeb/Ballotpedia-sourced headshot with no overlays; genuine
  gaps documented; audit migration recorded.
</success_criteria>

<output>
Create `.planning/phases/177-city-of-hillsboro-deep-seed/177-03-SUMMARY.md` when done, recording the
per-official image source and any documented gap.
</output>
