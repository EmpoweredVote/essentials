---
phase: 211-deep-dive-stance-research-trump-vance-rubio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\EV-Accounts\backend\scripts\apply-federal-deepdive-stances.ts
  - C:\EV-Accounts\backend\data\stance-research\2026-07-19-federal-active-topics.json
  - C:\EV-Accounts\backend\data\stance-research\2026-07-19-trump-vance-pre-pass-snapshot.json
autonomous: true
requirements: [RES-01]

must_haves:
  truths:
    - "The live set of active compass topics (topic_id, topic_key, question, 5 chair texts) is captured from inform.compass_topics/inform.compass_stances, not from the stale 2026-06-07 reference md"
    - "A pre-pass snapshot of Trump's and Vance's existing inform.politician_answers rows exists so the overwrite is reversible"
    - "A single reconcile apply script exists that upserts researched chairs AND deletes legacy rows for any topic not in the researched set, scoped to exactly one politician_id"
  artifacts:
    - path: "C:\\EV-Accounts\\backend\\data\\stance-research\\2026-07-19-federal-active-topics.json"
      provides: "Live topic_id + topic_key + question_text + 5 chairs per active topic (research-prompt source of truth)"
    - path: "C:\\EV-Accounts\\backend\\data\\stance-research\\2026-07-19-trump-vance-pre-pass-snapshot.json"
      provides: "Pre-overwrite snapshot of Trump (21) + Vance (24) politician_answers rows for reversibility (D-04)"
    - path: "C:\\EV-Accounts\\backend\\scripts\\apply-federal-deepdive-stances.ts"
      provides: "Reconcile apply: upsert non-blank chairs (parseInt) + context, delete legacy rows not in researched set, scoped to --politician-id"
      contains: "DELETE FROM inform.politician_answers"
  key_links:
    - from: "apply-federal-deepdive-stances.ts"
      to: "inform.politician_answers"
      via: "INSERT ... ON CONFLICT (politician_id, topic_id) DO UPDATE + scoped DELETE"
      pattern: "politician_answers"
    - from: "apply-federal-deepdive-stances.ts"
      to: "inform.politician_context"
      via: "reasoning + sources upsert / delete in lockstep with answers"
      pattern: "politician_context"
---

<objective>
Build the shared foundation for the Trump/Vance/Rubio deep-dive stance pass: capture the LIVE
compass-topic + chair definitions (the stale on-disk reference md lists 41 topics with 3 excluded;
CONTEXT says 44 are active — trust the DB, not the md), snapshot the existing Trump/Vance answers so
the full-overwrite pass (D-03) is reversible (D-04), and author ONE reconcile apply script that both
upserts researched chairs and deletes legacy uncited rows (D-05) scoped to a single politician_id.

Purpose: The three per-official plans (Waves 2-4) all consume these artifacts. Getting the live
topic/chair list and the delete-capable apply script right once, here, guarantees every downstream
apply is chair-accurate, reversible, and leaves nothing uncited.
Output: `2026-07-19-federal-active-topics.json`, `2026-07-19-trump-vance-pre-pass-snapshot.json`,
`scripts/apply-federal-deepdive-stances.ts` (all under `C:\EV-Accounts\backend`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-CONTEXT.md

<interfaces>
<!-- Established pipeline the executor must match. All paths are in the SEPARATE backend repo. -->
Target table: inform.politician_answers (politician_id uuid, topic_id uuid, value int, write_in_text)
  — UNIQUE (politician_id, topic_id). value = chair number 1-5 (NOT polarity; D-12).
Citation store: inform.politician_context (politician_id, topic_id, reasoning text, sources text[])
  — UNIQUE (politician_id, topic_id). Prior apply scripts write reasoning=notes, sources=[url].
Live topic tables: inform.compass_topics (is_active bool, topic_key), inform.compass_stances (5 chair
  rows per topic, each with a value 1-5 and chair text).
Closest analog to clone: C:\EV-Accounts\backend\scripts\apply-khanh-pham-stances.ts (upsert-only;
  reads CSV columns politician_id, topic_id, value, notes; skips value null/''; extracts URL from
  notes via regex; upserts answers + context). This plan ADDS delete-reconcile + a --politician-id arg.
CSV contract for this phase (D-11): politician_id,topic_id,topic_key,value,notes
  (value 1-5 integer; notes < 120 chars; use semicolons NOT commas inside notes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture live active topics + chairs and snapshot Trump/Vance answers</name>
  <files>C:\EV-Accounts\backend\data\stance-research\2026-07-19-federal-active-topics.json, C:\EV-Accounts\backend\data\stance-research\2026-07-19-trump-vance-pre-pass-snapshot.json</files>
  <read_first>
    - .planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-CONTEXT.md (D-01, D-04; live-state table)
    - C:\EV-Accounts\backend\data\stance-research\compass-topics-reference.md (STALE 2026-06-07 / 41 topics — read only to understand shape; DO NOT use as the topic list)
    - C:\EV-Accounts\backend\scripts\apply-khanh-pham-stances.ts (pg.Pool + DATABASE_URL connection pattern to reuse for the read queries)
  </read_first>
  <action>
    Run from `C:\EV-Accounts\backend` (has DATABASE_URL / pg). Query `inform.compass_topics WHERE
    is_active = true` joined to `inform.compass_stances` and write `2026-07-19-federal-active-topics.json`:
    one entry per active topic with topic_id, topic_key, question_text, and the 5 chairs (each {v, text})
    ordered 1..5 exactly as stored. This file — NOT the stale reference md — is the authoritative topic
    list every Wave 2-4 research agent is handed (D-01: all active topics, no pre-filtering; expect ~44
    incl. 8 judicial-* + ~11 hyper-local). Then snapshot the existing answers for Trump
    (`104102e6-08c1-494f-a9d4-6ef129595bf2`) and Vance (`a809747d-3e53-4e9e-b3a1-6641dac2455c`): SELECT
    politician_id, topic_id, topic_key, value from inform.politician_answers (join compass_topics for the
    key) and write `2026-07-19-trump-vance-pre-pass-snapshot.json`. Rubio
    (`7c8e4442-e13e-485a-8993-b05ca110410d`) has 0 rows — include him with an empty array for
    completeness. This snapshot is the D-04 reversibility record; do NOT write/delete any answer rows in
    this task.
  </action>
  <verify>
    <automated>node -e "const t=require('C:/EV-Accounts/backend/data/stance-research/2026-07-19-federal-active-topics.json'); const s=require('C:/EV-Accounts/backend/data/stance-research/2026-07-19-trump-vance-pre-pass-snapshot.json'); const arr=Array.isArray(t)?t:Object.values(t); if(arr.length<40) throw new Error('too few topics: '+arr.length); if(!arr.every(x=>x.topic_id&&x.topic_key&&Array.isArray(x.chairs)&&x.chairs.length===5)) throw new Error('topic entry missing id/key/5 chairs'); const tr=s['104102e6-08c1-494f-a9d4-6ef129595bf2']||s.trump; const va=s['a809747d-3e53-4e9e-b3a1-6641dac2455c']||s.vance; if(!tr||!va) throw new Error('snapshot missing trump/vance'); console.log('topics',arr.length,'trump',tr.length,'vance',va.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `2026-07-19-federal-active-topics.json` contains one entry per active topic; count matches
      `SELECT count(*) FROM inform.compass_topics WHERE is_active = true` (CONTEXT expects 44); every
      entry has topic_id, topic_key, question_text, and exactly 5 ordered chairs.
    - Snapshot file contains Trump's pre-pass rows (CONTEXT expects 21) and Vance's (expects 24), each
      row carrying topic_id + value; Rubio present with an empty array.
    - No write to inform.politician_answers occurred in this task (verify Trump count still equals the
      snapshot count after running).
  </acceptance_criteria>
  <done>Live topic/chair list captured from the DB and Trump/Vance answers snapshotted; nothing overwritten.</done>
</task>

<task type="auto">
  <name>Task 2: Author the reconcile apply script (upsert + delete-unsourced, scoped to one politician)</name>
  <files>C:\EV-Accounts\backend\scripts\apply-federal-deepdive-stances.ts</files>
  <read_first>
    - C:\EV-Accounts\backend\scripts\apply-khanh-pham-stances.ts (clone base: CSV parse, parseInt, ON CONFLICT upsert of answers + context, extractSource regex)
    - .planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-CONTEXT.md (D-03 overwrite, D-05 delete-unsourced, D-11 CSV columns, D-12 parseInt, D-13 run-from-backend)
  </read_first>
  <action>
    Create `scripts/apply-federal-deepdive-stances.ts` by cloning `apply-khanh-pham-stances.ts`, changing:
    (1) accept a `--csv <path>` and `--politician-id <uuid>` CLI arg instead of a hardcoded path;
    (2) parse the D-11 CSV columns (politician_id, topic_id, topic_key, value, notes); assert every row's
    politician_id equals the `--politician-id` arg (abort on mismatch — prevents cross-official writes);
    (3) for rows with a non-blank integer value: upsert inform.politician_answers via
    `INSERT ... ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value` using
    `parseInt(r.value)` directly — NO `3 - value` conversion (D-12) — and upsert inform.politician_context
    (reasoning = notes, sources = [extracted URL]);
    (4) RECONCILE-DELETE (D-05): after upserts, `DELETE FROM inform.politician_answers WHERE
    politician_id = $1 AND topic_id <> ALL($2)` where $2 is the array of researched non-blank topic_ids,
    and the identical scoped DELETE from inform.politician_context — so every legacy row for a topic that
    came back blank/unsourced is removed and nothing uncited survives (D-03). The DELETE must be scoped by
    politician_id so no other official's rows are ever touched.
    (5) print a summary: upserted, deleted, final answer count. Do NOT run it in this task — Waves 2-4 run it.
    Reuse the existing csv-parse/pg/dotenv deps already in the backend package (no new npm installs).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsc --noEmit scripts/apply-federal-deepdive-stances.ts 2>&1 | grep -v '^#' | grep -iE "error TS" && exit 1 || echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Script accepts `--csv` and `--politician-id`; grep confirms it contains both a
      `politician_answers ... ON CONFLICT` upsert and a `DELETE FROM inform.politician_answers` scoped by
      `politician_id` (bare unscoped DELETE is forbidden).
    - Script uses `parseInt(r.value)` and contains NO `3 -` polarity conversion (grep `3 -` returns none in the value path).
    - Script asserts each CSV row's politician_id matches the `--politician-id` arg before any write.
    - Script deletes inform.politician_context rows in lockstep with answer deletes.
    - Typecheck passes (no `error TS`).
  </acceptance_criteria>
  <done>Reconcile apply script exists, typechecks, and enforces single-politician scope + delete-unsourced; not yet executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research artifact (CSV) → production DB (`inform.politician_answers`) | Untrusted, human/agent-authored data crosses into live production rows on read by the apply script |
| apply script CLI arg → SQL WHERE/DELETE scope | A wrong/missing politician_id could widen writes/deletes beyond the intended official |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-211-01 | Tampering | apply-federal-deepdive-stances.ts DELETE scope | mitigate | DELETE is always `WHERE politician_id = $1 AND topic_id <> ALL(...)`; script aborts if any CSV row's politician_id ≠ the `--politician-id` arg (no bare/unscoped DELETE) |
| T-211-02 | Repudiation | Trump/Vance overwrite (D-03) | mitigate | Pre-pass snapshot (`2026-07-19-trump-vance-pre-pass-snapshot.json`) written before any write, making the overwrite reversible (D-04) |
| T-211-03 | Information disclosure | using stale reference md instead of live chairs | mitigate | Topic/chair list is pulled live from inform.compass_topics/compass_stances into `2026-07-19-federal-active-topics.json`; stale md is read for shape only |
| T-211-SC | Tampering (supply chain) | npm/npx deps | accept | No new packages installed — reuses backend's existing csv-parse/pg/dotenv; `npx tsx` runs local script only. No `[ASSUMED]`/`[SUS]` package, so no install checkpoint applies |
</threat_model>

<verification>
- `2026-07-19-federal-active-topics.json` topic count equals live `inform.compass_topics WHERE is_active=true`.
- Snapshot file present with Trump (21) + Vance (24) rows; Trump/Vance answer counts in the DB unchanged after this plan.
- `apply-federal-deepdive-stances.ts` typechecks, is single-politician scoped, and reconcile-deletes.
</verification>

<success_criteria>
Live topic/chair authority captured, overwrite made reversible, and a delete-capable single-scope apply
script ready for Waves 2-4 — with zero production answer rows modified in this wave.
</success_criteria>

<output>
Create `.planning/phases/211-deep-dive-stance-research-trump-vance-rubio/211-01-SUMMARY.md` when done.
</output>
