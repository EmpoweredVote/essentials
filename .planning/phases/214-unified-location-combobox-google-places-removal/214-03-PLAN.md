---
phase: 214-unified-location-combobox-google-places-removal
plan: 03
type: execute
wave: 3
depends_on: [214-02]
files_modified:
  - src/pages/Results.jsx
autonomous: true
requirements: [SRCH-01, SRCH-05]
user_setup: []

must_haves:
  truths:
    - "The Results header shows one pre-filled, click-to-edit LocationCombobox — the Address/Browse mode toggle and the LocationBrowser tree are gone"
    - "Submitting an address routes through the unchanged handleAddressSearch / Census path"
    - "Picking a place candidate navigates to /results?browse_geo_id=...&browse_mtfcc=...&browse_label=...&from_locality=1"
    - "Submitting decimal coordinates calls lookupCoordinate and renders returned officials via the direct-injection path"
    - "After a coordinate lookup the resting label is the client-typed lat, lng — never the server response (D-05)"
    - "A coordinate lookup near a jurisdiction boundary shows the correct banner (representingCity guard extended, no hijack)"
  artifacts:
    - path: "src/pages/Results.jsx"
      provides: "Combobox-driven Results header + coordinate render path"
      contains: "LocationCombobox"
  key_links:
    - from: "src/pages/Results.jsx"
      to: "LocationCombobox"
      via: "header render"
      pattern: "<LocationCombobox"
    - from: "src/pages/Results.jsx"
      to: "lookupCoordinate"
      via: "onSubmitCoordinate handler"
      pattern: "lookupCoordinate"
    - from: "src/pages/Results.jsx"
      to: "browseAreaRoute"
      via: "onSelectCandidate dispatch"
      pattern: "browseAreaRoute"
---

<objective>
Replace the Results header's Address/Browse mode toggle, address `<input>`, `<LocalityMatches>`, and `<LocationBrowser>` tree with a single `<LocationCombobox>` (SRCH-01), and build the coordinate render path (SRCH-05) — the one genuinely-undecided design point in this phase. The coordinate path reuses Results' existing `browseResults` direct-injection state (the closest precedent for "inject a list, bypass the address-geocode hook"), synthesizes the D-05 client-side resting label, and extends the `representingCity` banner-hijack guard for the new dispatch.

Purpose: Turns the combobox into a working Results experience across all three input types with no manual mode switch.
Output: Edited `src/pages/Results.jsx` (import/usage of the Google files removed here; the standalone files are deleted in Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-CONTEXT.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-RESEARCH.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-PATTERNS.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-UI-SPEC.md

<interfaces>
LocationCombobox props (from Plan 02): value, onChange, onSubmitAddress(raw), onSubmitCoordinate(lat,lng,raw), onSelectCandidate(candidate), placeholder, ariaLabel, errorRow.
lookupCoordinate(lat,lng) -> { data, error, code, formattedAddress } (Plan 01).
browseAreaRoute(candidate) -> "/results?browse_geo_id=...&browse_mtfcc=...&browse_label=...&from_locality=1" (Plan 02).
D-08 coordinate 422 → coral message map (exact copy in UI-SPEC Copywriting Contract):
  INVALID_COORDINATES / OUTSIDE_US_BOUNDS / SWAPPED_COORDINATES.
</interfaces>

<note>
Line numbers in PATTERNS.md/RESEARCH.md are pre-edit references and WILL have drifted. Re-grep each target symbol in Results.jsx before editing; do not trust absolute line numbers.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap header toggle/LocationBrowser/Google wiring for LocationCombobox (SRCH-01)</name>
  <read_first>
    - src/pages/Results.jsx — re-grep for: `useGooglePlacesAutocomplete`, `LocationBrowser`, `LocalityMatches`, `searchMode`, `handleAddressSearch`, `addressInput`, `addressInputRef`, `toAddressTitleCase` (PATTERNS.md lines 208-271 lists the removal targets, but re-locate by symbol)
    - 214-CONTEXT.md D-01, D-03 (always-editable, focus-select-all), D-04 (LocationBrowser retired, not reincarnated), D-07 (resting-label derivation)
    - 214-RESEARCH.md Dispatch/Navigation Wiring "Address path" + "Place-name path"; Pitfall 2 (do NOT carry forward the addressInputRef.current?.value workaround)
    - 214-UI-SPEC.md Combobox Interaction States (Resting / Focused / Candidate selected / Address submitted rows)
  </read_first>
  <action>
    Remove the `useGooglePlacesAutocomplete` import + its call (incl. `addressInputRef` wiring) and the `LocationBrowser` import from Results.jsx. Remove the `LocalityMatches` import/usage from the header. Replace the entire header JSX block (mode-toggle buttons, address `<input>`, `<LocalityMatches>`, `<LocationBrowser>`) with one `<LocationCombobox>` invocation: `value` bound to a controlled state pre-filled with the current resting label (D-07: address → `toAddressTitleCase(formattedAddress)`, place → `City, ST`, coordinate → typed coords per Task 2), `onChange` updating that state, `onSubmitAddress` calling the existing `handleAddressSearch` body directly (KEEP handleAddressSearch's URL/param wiring — but drop the `addressInputRef.current?.value ?? addressInput` fallback; read the controlled value), `onSelectCandidate` navigating via `browseAreaRoute(candidate)`, and `onSubmitCoordinate` from Task 2. KEEP the internal `searchMode` state for data-fetch/label derivation (do not delete it) — just stop rendering the user-facing toggle; you MAY add a `'coordinate'` value (Task 2). `toAddressTitleCase` stays unchanged and is reused for the address resting label.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LocationBrowser\|useGooglePlacesAutocomplete\|<LocalityMatches" src/pages/Results.jsx` returns ZERO.
    - `grep -n "<LocationCombobox" src/pages/Results.jsx` returns exactly one usage.
    - `grep -n "addressInputRef" src/pages/Results.jsx` returns ZERO (ref-reading workaround removed — RESEARCH Pitfall 2).
    - `npm run build` succeeds; `npm test` stays green.
  </acceptance_criteria>
  <done>Header renders a single LocationCombobox; toggle/LocationBrowser/LocalityMatches/Google-hook wiring removed from Results.jsx; address submit still routes through handleAddressSearch.</done>
</task>

<task type="auto">
  <name>Task 2: Coordinate render path + D-05 label + banner-hijack guard (SRCH-05)</name>
  <read_first>
    - src/pages/Results.jsx — re-grep for: `browseResults`, `browseLoading`, the list/phase derivation branch keyed on `searchMode === 'browse'`, and the `representingCity` useMemo (PATTERNS.md lines 242-271)
    - 214-RESEARCH.md Dispatch/Navigation Wiring "Coordinate path" (reuse browseResults direct-injection; skip browse_geo_id/mtfcc URL params; synthesize label from typed coords) + Open Question 3 (the design decision to make explicit) + Pitfall 3 (representing_city hijack)
    - 214-CONTEXT.md D-05 (client-echoed coordinate label, never server), D-08 (inline 422 error rows, no navigation)
    - project_representing_city_banner_hijack memory (Results already skips NATIONAL_*/STATE_* and prefers browse_label)
    - 214-UI-SPEC.md "Coordinate submitted" interaction row + the 3 coral error messages
  </read_first>
  <action>
    Implement `onSubmitCoordinate(lat, lng, raw)`: capture `raw` (the literal typed text) into component state BEFORE the fetch (D-05 — the resting label and the banner label-of-record derive from this, never from the response). Call `lookupCoordinate(lat, lng)`. On success: inject `data` into the existing `browseResults`/`browseLoading` direct-injection state (the same mechanism `searchMode === 'browse'` uses to render a list without the address-geocode hook), set the internal mode to a new `'coordinate'` value, set the resting label to `raw`, and do NOT set `browse_geo_id`/`browse_mtfcc` in the URL (there is no geo_id for a raw point). On a 422: pass the matching coral message (map `code` → the 3 UI-SPEC strings) to the combobox `errorRow` prop, do NOT navigate, do NOT clear the input (UI-SPEC "Coordinate submitted" row). Extend the `representingCity` useMemo with an explicit `mode === 'coordinate'` branch that returns the typed-coords-derived label-of-record (or null) BEFORE the "derive from politician records" fallback — mirroring the existing `searchMode === 'browse'` + `browse_label` branch — so a boundary-straddling coordinate never surfaces a neighboring city's stray `representing_city` (Pitfall 3). Note the banner label-of-record and the D-05 input resting label are two different surfaces both sourced client-side. Do NOT send raw lat/lng to any PostHog/analytics event — capture method/outcome only, matching the existing `essentials_*` events (threat T-214-02).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lookupCoordinate" src/pages/Results.jsx` shows it called from the coordinate submit handler.
    - The `representingCity` useMemo contains an explicit coordinate-mode branch (scoped read confirms a guard analogous to the `searchMode === 'browse'` branch) so it does not fall through to record-derivation for coordinate results.
    - The coordinate resting label is sourced from the typed `raw`/`lat,lng` state, not from `formattedAddress`/response (scoped read confirms — D-05).
    - `grep -niE "capture\(|posthog" src/pages/Results.jsx` — no new event passes a raw lat/lng pair (scoped read of any coordinate-path capture confirms method/outcome only).
    - `npm run build` succeeds; `npm test` green.
  </acceptance_criteria>
  <done>Coordinate input resolves to officials via direct-injection; resting label + banner label are client-sourced (D-05); 422s render inline coral messages without navigation; representingCity guard extended; no raw coordinates logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user coordinate keystrokes → analytics/URL/banner | Raw lat/lng handled client-side; must not leak to logs or hijack the banner |
| candidate.geo_id/mtfcc → navigate() | Identifiers drive internal route building |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-02 | Information Disclosure | coordinate submit handler | mitigate | No PostHog/analytics event carries the raw {lat,lng}; capture method/outcome only, mirroring existing essentials_* events (upholds the 213 client-side privacy spirit — D-05) |
| T-214-05 | Spoofing (open redirect) | onSelectCandidate dispatch | mitigate | Navigation builds INTERNAL `/results?...` only via browseAreaRoute + URLSearchParams from geo_id/mtfcc/label; never `navigate(candidate.<anyUrlField>)` |
| T-214-06 | Tampering (banner hijack regression) | representingCity useMemo | mitigate | Explicit coordinate-mode branch prevents falling through to record-derivation that can surface a neighbor's representing_city (project_representing_city_banner_hijack) |
</threat_model>

<verification>
- `npm run build` succeeds; `npm test` green.
- Manual smoke (covered fully in the Plan 06 checkpoint): address / place / coordinate each resolve; coordinate resting label = typed coords; boundary coordinate shows correct banner.
</verification>

<success_criteria>
Results header is a single always-editable combobox with no mode toggle or LocationBrowser; address, place-name, and coordinate input all resolve correctly; coordinate resting label and banner label are client-sourced per D-05; banner-hijack guard extended; no raw-coordinate telemetry.
</success_criteria>

<output>
Create `.planning/phases/214-unified-location-combobox-google-places-removal/214-03-SUMMARY.md` when done, documenting the exact coordinate-mode integration chosen (browseResults reuse vs. new state) for the Plan 06 verifier.
</output>
