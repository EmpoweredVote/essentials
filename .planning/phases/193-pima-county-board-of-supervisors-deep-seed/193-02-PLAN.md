---
phase: 193-pima-county-board-of-supervisors-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["193-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql
autonomous: false
requirements: [PIMA-01]
must_haves:
  truths:
    - "A standalone 'Pima County, Arizona, US' government (type='County', state='AZ', geo_id='04019') exists, NOT nested under State of Arizona"
    - "One 'Board of Supervisors' chamber (official_count=5) holds exactly 5 by-district supervisor offices"
    - "Each of the 5 supervisor offices links to its OWN LOCAL X0019 district (never the 3-way-collision COUNTY row 04019)"
    - "Roster is Scott(D1)/Heinz(D2)/Allen(D3)/Christy(D4)/Cano(D5); Cano carries is_appointed=true; no departed member is seeded"
    - "The 2026 Chair (Allen, D3) carries ZERO DB footprint — no title suffix, role_canonical=NULL (Clark precedent)"
    - "The structural migration is applied to production and registered in the ledger; 5 politician UUIDs captured for Plans 03/04"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql"
      provides: "government + chamber + 5 LOCAL districts + 5 politicians + 5 offices + office_id backfill + pre-flight + post-verify DO gates + ledger registration"
      contains: "Board of Supervisors"
  key_links:
    - from: "essentials.offices (5 supervisors)"
      to: "essentials.districts (X0019 LOCAL)"
      via: "district_id join scoped by district_type='LOCAL' AND mtfcc='X0019' AND state='az'"
      pattern: "district_type\\s*=\\s*'LOCAL'"
    - from: "essentials.chambers 'Board of Supervisors'"
      to: "essentials.governments 'Pima County, Arizona, US'"
      via: "government_id subquery scoped by name"
      pattern: "Pima County, Arizona, US"
---

<objective>
Author and apply the structural migration that seeds Pima County as a STANDALONE county government
(its own `essentials.governments` row at geo_id `04019`, NOT parented to State of Arizona), a single
`Board of Supervisors` chamber, 5 LOCAL supervisor-district rows on the Plan 01 X0019 geofences, and
5 by-district supervisor offices + politicians (ext_id `-4007001..-4007005`). Greenfield — no Pima
County government row exists today.

Purpose: Satisfies the government/roster half of ROADMAP success criterion #1 (standalone county,
5 supervisors on 5 LOCAL geofences). The X% MTFCC catch-all in `essentialsService.ts` already routes
X0019 with NO code change, so applying this migration makes every Pima address resolve to its ONE
supervisor. Plans 03 (headshots) and 04 (stances) consume the 5 politician UUIDs captured here.
Output: `C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql` (structural,
registered), applied to production, with the 5 UUIDs recorded in the SUMMARY.

CRITICAL (D-02): exactly 5 offices — no 6th "Chair" office. The 2026 Chair (Jennifer Allen, D3) is
modeled with ZERO DB footprint (Clark County precedent): no `(Chair)` title suffix, `role_canonical`
NULL. This reconciles CONTEXT D-02's loose "title annotation" language toward the cleaner Clark
zero-footprint default (a literal suffix would go stale the moment the board rotates chairs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-CONTEXT.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md
@.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-01-SUMMARY.md

<execution_note>
Depends on Plan 01: the 5 X0019 geofences MUST be live (the migration's pre-flight DO block RAISE
EXCEPTIONs if <5 exist). `gsd-executor` authors the `.sql` file only. Task 2 (roster checkpoint +
apply + UUID capture) and Task 3 (post-apply assertions) are ORCHESTRATOR-RUN via `psql -f` against
`C:/EV-Accounts/backend/.env` `DATABASE_URL`. A migration authored but not applied is a false
positive — the apply MUST happen before Plans 03/04. Commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (live-verified in 193-RESEARCH.md / 193-PATTERNS.md):
- essentials.governments (id, name, type, state, city, geo_id): insert 'Pima County, Arizona, US',
  type='County', state='AZ' (UPPERCASE — free-text label), city=NULL, geo_id='04019'. Standalone:
  NO parent link to State of Arizona. Idempotent WHERE NOT EXISTS on name.
- essentials.chambers (id, name, name_formal, government_id, official_count): name='Board of
  Supervisors', name_formal='Pima County Board of Supervisors', official_count=5, government_id via
  name subquery. Idempotent WHERE NOT EXISTS on (name, government_id).
- essentials.districts (id, district_type, state, geo_id, label, mtfcc): 5 rows, district_type='LOCAL',
  state='az' (lowercase), geo_id='pima-az-supervisor-district-N', label='Pima County Supervisor
  District N', mtfcc='X0019'. NO name_formal column — use `label`. Idempotent WHERE NOT EXISTS on
  (geo_id, district_type, state).
- essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed,
  is_vacant, is_incumbent, external_id, office_id): ext_id -4007001..-4007005; external_id UNIQUE
  (ON CONFLICT (external_id) DO NOTHING). party stored, NEVER displayed (antipartisan).
- essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state,
  is_appointed_position, is_vacant, role_canonical): title='Supervisor, District N',
  representing_state='AZ' (UPPERCASE), role_canonical=NULL, guard on (district_id, politician_id).
- CRITICAL Pitfall 2: geo_id='04019' is a 3-WAY collision (COUNTY + STATE_UPPER SD-19 + STATE_LOWER
  HD-19). Every office↔district join MUST scope `district_type='LOCAL' AND mtfcc='X0019' AND
  state='az'`. This migration NEVER touches the pre-existing COUNTY 04019 row.
- Roster (193-RESEARCH.md § Summary): Rex Scott (D1, Dem), Dr. Matt Heinz (D2, Dem), Jennifer Allen
  (D3, Dem, 2026 Chair), Steve Christy (D4, Rep), Andrés Cano (D5, Dem, is_appointed=true — appointed
  Apr 2025 succeeding Adelita Grijalva → US House CD-7, per Phase 191).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the structural Pima County Board of Supervisors migration</name>
  <files>C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-PATTERNS.md (lines 79-217 — the full structural pattern: government/chamber INSERT, pre-flight geofence assertion, 5× LOCAL district INSERT, the Clark WITH ins_p...INSERT INTO offices per-supervisor block, office_id backfill, post-verify DO gate, ledger registration; note the CRITICAL WashCo divergence — DROP the Chair-on-COUNTY block, all 5 get the LOCAL shape)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Summary roster; § Architecture Patterns 1 & 2; § Common Pitfalls 2, 3, 5; § Open Questions 1 & 2 — title convention + zero-footprint Chair)
    - C:/EV-Accounts/backend/migrations/1120_washco_commission.sql (primary analog — read in full: split COUNTY/LOCAL shape + pre-flight assertion; replicate the LOCAL-district block 5× not 4×, DROP its separate Chair-on-COUNTY office)
    - C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql (secondary — the WITH ins_p CTE office block + zero-footprint Chair; diverge by pointing each office at its OWN LOCAL X0019 district instead of one shared COUNTY district)
  </read_first>
  <action>
    First run `ls C:/EV-Accounts/backend/migrations | sort` to confirm on-disk MAX (research saw
    1287, the audit-only az-legislature-headshots file). Name this file disk-MAX+1 (provisionally
    `1288_pima_county_board_of_supervisors.sql`). If disk MAX drifted, use actual disk-MAX+1 and note
    the deviation (Pitfall 5: disk-MAX authoritative, not ledger-MAX which is 1286).

    Author a STRUCTURAL migration wrapped in BEGIN/COMMIT. Pre-flight DO block: assert
    `COUNT(*) >= 5` in essentials.geofence_boundaries WHERE state='az' AND mtfcc='X0019' — RAISE
    EXCEPTION 'run load-pima-supervisor-boundaries.ts first' otherwise (gates on Plan 01).

    Insert the standalone government 'Pima County, Arizona, US' (type='County', state='AZ' uppercase,
    city=NULL, geo_id='04019') idempotently (WHERE NOT EXISTS on name) — NO parent link to State of
    Arizona. Insert the 'Board of Supervisors' chamber (name_formal='Pima County Board of
    Supervisors', official_count=5) scoped to that government's id via name subquery, idempotent on
    (name, government_id).

    Insert 5 LOCAL district rows (district_type='LOCAL', state='az' lowercase — Pitfall 3,
    geo_id='pima-az-supervisor-district-1..5', label='Pima County Supervisor District N',
    mtfcc='X0019'), each idempotent WHERE NOT EXISTS on (geo_id, district_type, state). Do NOT insert
    or reference the pre-existing COUNTY 04019 row (Pitfall 2 — 3-way collision).

    Seed 5 supervisors using the Clark `WITH ins_p AS (INSERT INTO politicians ... RETURNING id)
    INSERT INTO offices ... FROM districts d CROSS JOIN ins_p p WHERE d.geo_id=
    'pima-az-supervisor-district-N' AND d.district_type='LOCAL' AND d.state='az'` block, one per
    supervisor. Each office: title='Supervisor, District N' (Open Question 2 normalized form — NOT
    pima.gov's inconsistent phrasing), representing_state='AZ' uppercase, is_appointed_position=false,
    role_canonical=NULL. Guard each office INSERT on `NOT EXISTS (... o.district_id=d.id AND
    o.politician_id=p.id)`. Politician ext_ids: Scott -4007001, Heinz -4007002, Allen -4007003,
    Christy -4007004, Cano -4007005; is_active=true, is_incumbent=true, is_vacant=false on all 5.

    Cano (D5) ONLY: politician is_appointed=true (Apr 2025 appointment); office
    is_appointed_position stays false. The other 4: is_appointed=false.

    Jennifer Allen (D3) is the 2026 Chair but carries ZERO DB footprint (D-02 / Clark precedent): NO
    `(Chair)` suffix in title, role_canonical=NULL — identical shape to the other 4. Chair status is
    informational-only (record it in the SUMMARY, never in the DB).

    office_id backfill: UPDATE politicians SET office_id=o.id FROM offices o WHERE o.politician_id=
    p.id AND p.external_id BETWEEN -4007005 AND -4007001 AND p.office_id IS NULL.

    Post-verify DO gate (RAISE EXCEPTION → rollback unless ALL hold): (a) exactly 1 government named
    'Pima County, Arizona, US'; (b) exactly 5 offices joined to LOCAL X0019 districts
    (d.geo_id LIKE 'pima-az-supervisor-district-%' AND d.district_type='LOCAL' AND d.state='az');
    (c) each of those 5 districts holds exactly 1 office (GROUP BY district_id HAVING count(*)<>1 → 0
    rows); (d) is_appointed=true count among the 5 politicians = exactly 1 (Cano); (e) section-split:
    0 offices attached under any government OTHER than Pima County via these 5 districts. NO Gate for a
    Chair office (there is none).

    Register OUTSIDE BEGIN/COMMIT: INSERT INTO supabase_migrations.schema_migrations (version, name)
    VALUES ('1288','pima_county_board_of_supervisors') ON CONFLICT (version) DO NOTHING (substitute
    actual disk-MAX+1). Do NOT invent politician UUIDs — offices resolve politician_id via the ins_p
    CTE at apply time.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql" && grep -qF "Board of Supervisors" "C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql" && grep -c "district_type = 'LOCAL'" "C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql" && grep -c "o.district_id = d.id AND o.politician_id = p.id" "C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql" && ! grep -qiE "\(Chair\)|County Chair|role_canonical\s*=\s*'" "C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1288).
    - Grep confirms `Board of Supervisors` and `Pima County, Arizona, US` present.
    - Grep count of the office↔district guard `o.district_id = d.id AND o.politician_id = p.id` is exactly 5.
    - Every office↔district WHERE scopes `district_type = 'LOCAL'` AND `mtfcc = 'X0019'`; lowercase `state = 'az'` on district rows; uppercase `'AZ'` only on governments.state / offices.representing_state.
    - Grep confirms 5 external_id literals -4007001..-4007005; NO 6th office; NO `(Chair)`/`County Chair` string; NO non-NULL `role_canonical`.
    - Grep confirms `is_appointed` set true exactly once (Cano); the other 4 false.
    - Pre-flight DO block asserts >=5 X0019 geofences; post-verify DO gate present with the 5 assertions (a-e); NO Gate references a Chair office.
    - Ledger-registration line `('1288'` (or actual number) present OUTSIDE COMMIT.
    - Migration never inserts or joins bare `geo_id = '04019'` for an office (Pitfall 2).
  </acceptance_criteria>
  <done>Structural migration authored: standalone Pima County government + Board chamber + 5 LOCAL X0019 districts + 5 supervisor offices/politicians (Cano appointed, Allen zero-footprint chair), backfill, pre-flight + post-verify gates, ledger registration — ready to apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Roster-currency re-verification + apply structural migration (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Summary roster + § Assumptions Log A1/A2 — party + chair-rotation residual risk; "valid until 7 days for roster-currency")
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-CONTEXT.md (Claude's Discretion — roster-currency re-check before seeding; vacant seat → vacant office, never a departed member)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor file action. The operator re-confirms the current 5 sitting supervisors + who chairs the board; on approval the ORCHESTRATOR applies migration 1288 via `psql -f` and captures the 5 politician UUIDs. See &lt;how-to-verify&gt; for the exact sequence.</action>
  <what-built>
    Migration 1288 (Task 1) is authored but NOT applied. This checkpoint confirms the 5-member roster
    + 2026 chair are still current before applying to production (mcp__supabase-local IS live prod).
    RESEARCH (2026-07-09) confirmed Scott/Heinz/Allen/Christy/Cano with Allen as 2026 Chair and Cano
    appointed Apr 2025; board composition/chair can change (research "valid until ~7 days for roster").
  </what-built>
  <how-to-verify>
    1. Open https://www.pima.gov/2317/Board-of-Supervisors and confirm the 5 sitting supervisors still
       match 193-RESEARCH.md § Summary: D1 Rex Scott, D2 Dr. Matt Heinz, D3 Jennifer Allen (Chair),
       D4 Steve Christy, D5 Andrés Cano.
    2. Confirm no new resignation/appointment/chair rotation has landed since 2026-07-09. If a seat is
       genuinely vacant, seed a vacant office (is_vacant=true), never a departed member.
    3. On approval, the ORCHESTRATOR applies via
       `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1288_pima_county_board_of_supervisors.sql`
       (executor has no DB access), then captures the 5 UUIDs
       (`SELECT external_id, id, full_name FROM essentials.politicians WHERE external_id BETWEEN -4007005 AND -4007001 ORDER BY external_id`)
       into 193-02-SUMMARY.md for Plans 03/04, and commits the migration via `git -C "C:/EV-Accounts"`.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the live pima.gov roster + chair match the seeded roster (or supplies the exact delta to patch before apply).
    - Migration applies with the in-transaction post-verify DO gate passing (no RAISE EXCEPTION → COMMIT).
    - `psql -tAc "SELECT count(*) FROM supabase_migrations.schema_migrations WHERE version='1288'"` returns 1 (structural registered).
    - The 5 politician UUIDs captured into 193-02-SUMMARY.md (external_id → UUID → full_name) for Plans 03/04.
    - Migration committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live roster + chair are confirmed and the migration is applied + UUIDs captured, or describe the roster delta to patch first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply structural assertions (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB assertions, no files modified</files>
  <read_first>
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-VALIDATION.md (Per-Task Verification Map — seed/roster/route rows: standalone gov, 5 offices 1/district, Cano appointed, section-split)
    - .planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-RESEARCH.md (§ Common Pitfall 2 — the 04019 3-way collision the section-split guard protects against)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). After apply, run seed assertions via
    `psql "$DATABASE_URL" -tAc` and record in 193-02-SUMMARY.md. Assert: (a) exactly 1 government
    'Pima County, Arizona, US' at geo_id='04019', type='County', NOT parented to State of Arizona;
    (b) exactly 5 offices under the 'Board of Supervisors' chamber of that government; (c) each of the
    5 LOCAL X0019 districts holds exactly 1 office (GROUP BY district_id HAVING count(*)<>1 → 0 rows);
    (d) is_appointed=true among the 5 = exactly 1 (Cano D5); (e) DISTINCT district state on the 5
    linked districts = only 'az' (lowercase) and DISTINCT mtfcc = only 'X0019'; (f) section-split:
    0 offices reachable from these 5 districts under any government other than Pima County. Any
    mismatch blocks the phase — do NOT proceed to Plan 03.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.governments WHERE name='Pima County, Arizona, US' AND geo_id='04019' AND type='County')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id LIKE 'pima-az-supervisor-district-%' AND d.district_type='LOCAL' AND d.state='az' AND d.mtfcc='X0019')=5 AND NOT EXISTS (SELECT 1 FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0019' GROUP BY o.district_id HAVING count(*)<>1) AND (SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -4007005 AND -4007001 AND is_appointed)=1"</automated>
  </verify>
  <acceptance_criteria>
    - Standalone Pima County government count = 1 (geo_id 04019, type County, not under State of AZ).
    - 5 offices under 'Board of Supervisors' on LOCAL X0019 districts.
    - Each X0019 district holds exactly 1 office (no HAVING count<>1 violation).
    - Exactly 1 appointed politician (Cano) among the 5.
    - DISTINCT linked-district state = only 'az'; DISTINCT mtfcc = only 'X0019'.
    - Section-split query returns 0 rows (no office leaked under a non-Pima government).
    - The combined boolean SELECT returns `t`.
  </acceptance_criteria>
  <done>Live production confirms the standalone Pima County government, 5 supervisor offices (1 per LOCAL X0019 district), Cano appointed, correct casing, clean section-split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | Hand-authored static SQL crosses into live `essentials` via `psql -f` (service-role) |
| Plan 01 X0019 geofences → new offices | Office↔district joins consume the X0019 rows; wrong casing/type or the 04019 collision silently mislinks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-COLL | Tampering | office↔district WHERE vs the 04019 3-way collision | mitigate | Every join scopes `district_type='LOCAL' AND mtfcc='X0019' AND state='az'`; migration never touches bare geo_id='04019'; post-verify asserts 5 offices on X0019 only |
| T-193-SPLIT | Tampering | office↔chamber↔government section-split | mitigate | Chamber scoped to the Pima government id subquery; post-verify DO gate section-split assertion → 0 rows or the transaction rolls back |
| T-193-ROST | Spoofing | roster / appointed-member rows | mitigate | Seed only the 5 current supervisors; Cano is_appointed=true; departed members never inserted; Task 2 blocking human roster-currency check |
| T-193-CHAIR | Tampering | Chair modeling (double-count risk) | mitigate | Zero-footprint Chair (no 6th office, no title suffix, role_canonical NULL) per D-02/Clark; grep gate forbids `(Chair)`/`County Chair` and non-NULL role_canonical |
| T-193-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `.env`; applied by the inline orchestrator; never hardcoded |
| T-193-SC | Tampering | package installs | accept | No new installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 1 standalone Pima County government (geo_id 04019, type County), NOT under State of AZ.
- 5 supervisor offices under 'Board of Supervisors', 1 per LOCAL X0019 district, Cano appointed.
- Allen (D3) chair = zero DB footprint; no 6th office.
- Section-split 0 rows; casing lowercase 'az' on districts; structural migration registered.
- 5 politician UUIDs captured for Plans 03/04.
</verification>

<success_criteria>
ROADMAP #1 (standalone county, 5 supervisors on 5 LOCAL geofences, NOT nested under State of AZ) is
TRUE in production; the migration is applied + registered; the UUID manifest is recorded.
</success_criteria>

<output>
Create `.planning/phases/193-pima-county-board-of-supervisors-deep-seed/193-02-SUMMARY.md` when done —
include the 5 external_id → UUID → full_name manifest (required by Plans 03 and 04) and record Allen
as 2026 Chair (informational only).
</output>
