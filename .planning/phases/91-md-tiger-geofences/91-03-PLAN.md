---
phase: 91-md-tiger-geofences
plan: 03
type: execute
wave: 3
depends_on:
  - 91-02
files_modified: []
autonomous: true
requirements:
  - MD-GEO-01
  - MD-GEO-02
  - MD-GEO-03
  - MD-GEO-04
  - MD-GEO-05

must_haves:
  truths:
    - "geofence_boundaries rows exist for state='24' with all 5 MTFCC types (G4020, G4110, G5200, G5210, G5220)"
    - "geofence_boundaries has exactly 24 rows with state='24' AND mtfcc='G4020'"
    - "geofence_boundaries has exactly 8 rows with state='24' AND mtfcc='G5200'"
    - "geofence_boundaries has exactly 47 rows with state='24' AND mtfcc='G5210'"
    - "Both Baltimore City rows exist: geo_id='2404000' (G4110) and geo_id='24510' (G4020)"
    - "districts rows exist for Maryland with state IN ('MD','md') at all 4 district_types"
  artifacts:
    - path: "essentials.geofence_boundaries (Supabase production)"
      provides: "5 layers of MD TIGER boundaries"
      contains: "state='24' rows for G4020, G4110, G5200, G5210, G5220"
    - path: "essentials.districts (Supabase production)"
      provides: "MD district rows with correct state casing per D-07"
      contains: "state='md' for COUNTY/STATE tiers, state='MD' for NATIONAL_LOWER"
  key_links:
    - from: "TIGER PLACE layer (tl_2024_24_place.zip)"
      to: "geofence_boundaries G4110 rows"
      via: "upsertGeofence() with ST_MakeValid + state='24'"
      pattern: "mtfcc='G4110' AND state='24'"
    - from: "TIGER COUNTY layer (tl_2024_us_county.zip filtered STATEFP='24')"
      to: "geofence_boundaries G4020 rows (including Baltimore City geo_id='24510')"
      via: "filterByStatefp=true in processLayer()"
      pattern: "mtfcc='G4020' AND state='24'"
---

<objective>
Live-load all 5 MD TIGER layers into geofence_boundaries and districts using the confirmed counts from Plan 02.

Purpose: This is the primary data write for Phase 91. After this plan completes, all MD address routing is possible. Plans 92-96 depend on these boundaries existing.

Output: geofence_boundaries and districts rows for state='24' (all 5 layers); all geometries ST_MakeValid-processed; Baltimore City dual-tier rows (D-01 invariant) present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-md-tiger-geofences/91-CONTEXT.md
@.planning/phases/91-md-tiger-geofences/91-RESEARCH.md
@.planning/phases/91-md-tiger-geofences/91-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load all 5 MD TIGER layers</name>
  <read_first>
    .planning/phases/91-md-tiger-geofences/91-02-SUMMARY.md — Confirm the actual sldl count (N) and place count (N) recorded from Plan 02 dry-run. The loader must have these confirmed values before running live.
    C:/EV-Accounts/backend/scripts/load-state-tiger-boundaries.ts — Verify EXPECTED_MD_MTFCC has non-zero sldl and place values (confirm Plan 02 edits are in place).
  </read_first>
  <action>
    Confirm Plan 02 is complete: EXPECTED_MD_MTFCC.sldl and EXPECTED_MD_MTFCC.place must both be non-zero before running live. If either is still 0, stop — Plan 02 must be re-run first.

    From C:/EV-Accounts/backend, run the live load for all 5 layers:

      npx tsx scripts/load-state-tiger-boundaries.ts --state MD --fips 24 --layers cd119,sldu,sldl,place,county

    No --dry-run flag. The loader will:
      1. Download all 5 TIGER zip files from census.gov to a temp directory
      2. Run the EXPECTED_MD_MTFCC pre-flight assertion for each layer (must pass before writes)
      3. Run the STATE_CITY_ASSERTIONS check for the place layer (confirm 'Baltimore city' is present before writes)
      4. Stream each shapefile and upsert rows into geofence_boundaries (ST_MakeValid applied per STATE_RUN_MAKEVALID)
      5. Insert corresponding district rows into districts with correct state casing (abbrev='md' for COUNTY/STATE tiers, abbrevUpper='MD' for NATIONAL_LOWER cd119 layer)

    The county layer downloads tl_2024_us_county.zip (US-wide file, ~50MB). It uses filterByStatefp=true to retain only STATEFP='24' records — expect 24 county rows including Baltimore City (FIPS 24510) as an independent city-county.

    Monitor console output for:
      - Pre-flight assertions: 5 "PASSED" lines before any DB writes
      - City assertion: "Baltimore city" sentinel found
      - Per-layer upsert counts matching the expected EXPECTED_MD_MTFCC values
      - Any ON CONFLICT DO NOTHING messages (fine if rows already exist from a prior partial run)
      - No "ERROR:" lines in the output

    If the loader exits non-zero, diagnose from the error message before retrying. The ON CONFLICT DO NOTHING clause makes re-running safe.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx -e "require('dotenv').config(); const {Client} = require('pg'); const c = new Client({connectionString: process.env.DATABASE_URL, ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(\"SELECT mtfcc, COUNT(*) AS n FROM essentials.geofence_boundaries WHERE state='24' GROUP BY mtfcc ORDER BY mtfcc\")).then(r=>{r.rows.forEach(row=>console.log(row.mtfcc,row.n)); c.end()})"</automated>
  </verify>
  <acceptance_criteria>
    - geofence_boundaries contains rows for all 5 MTFCC codes with state='24': G4020, G4110, G5200, G5210, G5220
    - G4020 count = 24 (exact)
    - G5200 count = 8 (exact)
    - G5210 count = 47 (exact)
    - G5220 count = N (matches the confirmed sldl count from Plan 02)
    - G4110 count = N (matches the confirmed place count from Plan 02)
    - Console output includes 5 "MD MTFCC pre-flight assertion PASSED" messages and no "ERROR:" lines
    - Loader exits 0
  </acceptance_criteria>
  <done>All 5 MD TIGER layers are loaded into geofence_boundaries with state='24'; pre-flight assertions passed; 24 counties, 8 CDs, 47 senate, N house sub-districts, N cities present.</done>
</task>

<task type="auto">
  <name>Task 2: Spot-check districts table and Baltimore City dual-tier sentinel</name>
  <read_first>
    .planning/phases/91-md-tiger-geofences/91-RESEARCH.md — Baltimore City Sentinel Values section: G4110 geo_id='2404000', G4020 geo_id='24510'; districts.state casing rule (D-07).
  </read_first>
  <action>
    Run 3 verification queries via mcp__supabase-local__execute_sql to confirm the critical post-load invariants:

    Query 1 — Baltimore City dual-tier (D-01 invariant):
      SELECT geo_id, name, mtfcc
      FROM essentials.geofence_boundaries
      WHERE state = '24' AND geo_id IN ('2404000', '24510')
      ORDER BY mtfcc;
    Expected: 2 rows — geo_id='24510'/mtfcc='G4020' and geo_id='2404000'/mtfcc='G4110', both named 'Baltimore city'.

    Query 2 — districts table state casing (D-07):
      SELECT state, district_type, COUNT(*) AS cnt
      FROM essentials.districts
      WHERE state IN ('MD', 'md')
      GROUP BY state, district_type
      ORDER BY state, district_type;
    Expected: md|COUNTY|24, md|STATE_LOWER|N, md|STATE_UPPER|47, MD|NATIONAL_LOWER|8.
    No rows with any other state value. NATIONAL_LOWER must use uppercase 'MD'.

    Query 3 — No pre-existing MD geofence rows from other layers:
      SELECT COUNT(*) AS total_md_rows
      FROM essentials.geofence_boundaries
      WHERE state = '24';
    Expected: total = 24 (G4020) + 8 (G5200) + 47 (G5210) + sldl_count (G5220) + place_count (G4110).

    If Query 1 returns fewer than 2 rows, the Baltimore City dual-tier invariant has failed — investigate before proceeding to Plan 04 verification.
    If Query 2 returns NATIONAL_LOWER with state='md' (lowercase), the loader wrote the wrong casing for congressional districts — this is a casing bug to fix before Plan 04.
  </action>
  <verify>
    <automated>Queries run via mcp__supabase-local__execute_sql tool — see action above</automated>
  </verify>
  <acceptance_criteria>
    - Query 1 returns exactly 2 rows: geo_id='24510' (G4020) and geo_id='2404000' (G4110), both name='Baltimore city'
    - Query 2 returns exactly 4 groups with correct counts and correct casing: md (lowercase) for COUNTY/STATE_LOWER/STATE_UPPER; MD (uppercase) for NATIONAL_LOWER
    - Query 3 total_md_rows = 24 + 8 + 47 + actual_sldl + actual_place (sum of all 5 layer counts — no extra rows, no missing rows)
    - No "Baltimore County" confusion: geo_id='24005' has mtfcc='G4020' only (not G4110)
  </acceptance_criteria>
  <done>Baltimore City dual-tier confirmed present; districts.state casing correct per D-07; total geofence_boundaries row count equals sum of 5 confirmed layer counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov → EV-Accounts backend | TIGER zip files downloaded over HTTPS; 5 files totaling ~60MB |
| EV-Accounts backend → Supabase production | geofence_boundaries and districts rows written; upserts use ON CONFLICT DO NOTHING |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-07 | Tampering | Live writes to geofence_boundaries (production) | mitigate | Pre-flight assertion block (EXPECTED_MD_MTFCC) validates exact row counts before any DB write; assertion failure is fatal and named; ON CONFLICT DO NOTHING makes re-runs safe |
| T-91-08 | Tampering | Baltimore City dual-tier data integrity | mitigate | Task 2 Query 1 asserts both geo_ids present immediately after load; Plan 04 smoke test re-asserts via PostGIS spatial query |
| T-91-09 | Injection | TIGER shapefile data into PostGIS geometries | accept | Data source is census.gov (US government authoritative); geometries processed through ST_MakeValid + ST_Force2D; parameterized queries used by upsertGeofence() |
| T-91-10 | Denial of Service | US-wide county file (~50MB) download | accept | tl_2024_us_county.zip download is within normal operational range; filterByStatefp='24' reduces storage to 24 rows |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No new packages; all dependencies pre-existing |
</threat_model>

<verification>
After both tasks complete:
1. Verify 5 MTFCC types exist for state='24' in geofence_boundaries
2. Verify G4020=24, G5200=8, G5210=47 exact counts
3. Verify both Baltimore City rows exist (geo_id='2404000' G4110 and '24510' G4020)
4. Verify districts.state casing: md (lowercase) for COUNTY/STATE; MD (uppercase) for NATIONAL_LOWER
</verification>

<success_criteria>
- All 5 MD TIGER layers loaded into geofence_boundaries with state='24'; pre-flight assertions passed with confirmed counts
- geofence_boundaries has G4020=24, G5200=8, G5210=47 exact rows; G4110 and G5220 match dry-run confirmed counts
- Baltimore City appears as both G4110 geo_id='2404000' and G4020 geo_id='24510' (D-01 invariant satisfied)
- districts.state casing correct per D-07: lowercase 'md' for COUNTY/STATE tiers, uppercase 'MD' for NATIONAL_LOWER
</success_criteria>

<output>
Create `.planning/phases/91-md-tiger-geofences/91-03-SUMMARY.md` when done. Record total geofence_boundaries row counts by MTFCC and districts row counts by district_type — Plan 04 uses these as expected values.
</output>
