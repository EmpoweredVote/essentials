---
phase: 106-va-compass-stances
plan: 03
type: execute
wave: 3
depends_on:
  - 106-02
files_modified:
  - C:/EV-Accounts/backend/migrations/328_jones_stances.sql
autonomous: true
requirements:
  - VA-STANCES-01
must_haves:
  truths:
    - "Jay Jones has >=10 stance rows in inform.politician_answers after migration 328 applies"
    - "Every Jones context row has at least 1 URL in sources (100% citation rate per D-10)"
    - "No defaulted/neutral fallback rows exist"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/328_jones_stances.sql"
      provides: "Per-individual stance migration for Attorney General Jay Jones"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "328_jones_stances.sql"
      to: "essentials.politicians (external_id=-510003)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research Jay Jones's (Attorney General of Virginia, external_id=-510003) public record across all currently-active compass topics, write per-individual stance migration 328, apply to production Supabase, and verify counts.

Purpose: Completes VA-STANCES-01 (3 of 3 VA executives). Jay Jones served as VA Delegate (HD-89, Norfolk) 2018-2022 before becoming AG; his House voting record is the primary source. ROADMAP minimum: >=10 stances.

Output: `328_jones_stances.sql` applied; >=10 stance rows in `inform.politician_answers` for Jones; all rows paired in `inform.politician_context` with >=1 source URL each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/106-va-compass-stances/106-CONTEXT.md
@.planning/phases/106-va-compass-stances/106-PATTERNS.md
@.planning/phases/106-va-compass-stances/106-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Jones UUID, then research all active topics</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-01..D-10)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120)
    - C:/EV-Accounts/backend/migrations/317_va_state_executives.sql (lines 162-174 for Jones external_id=-510003)
  </read_first>
  <action>
    Step A: Run via mcp__supabase-local__execute_sql:
      SELECT id FROM essentials.politicians WHERE external_id = -510003;
    Record as ${JONES_UUID}. If 0 rows, abort.

    Step B: Run ONE web-research subagent (per D-08). Jones's documented record:
      - VA Delegate (HD-89, Norfolk) 2018-2022 — voting record on healthcare, voting-rights, criminal-justice reform, marijuana legalization
      - 2021 AG primary candidate (lost to Mark Herring) — published platform positions on judicial reform, criminal-justice, civil-rights
      - 2025 AG general election winner — campaign platform positions on consumer-protection (taxes-adjacent), voting-rights, abortion (post-Dobbs era), gun-safety
      - Likely-evidence topics: abortion (strong D record), voting-rights, civil-rights, judicial-criminal-justice, judicial-police-accountability, judicial-prosecution-priorities, public-safety-approach, gun-safety-adjacent topics, healthcare, immigration, deportation (as AG-elect, declined to coordinate with ICE per public statements), trans-athletes, same-sex-marriage, religious-freedom
      - Federal-only topics (tariffs, ukraine-support, social-security, ai-regulation): likely no direct record — OMIT if no evidence
      - Local-only topics (city-sanitation, rent-regulation, residential-zoning, jail-capacity, local-environment, local-immigration): may have HD-89 (Norfolk) voting record on some — research, omit if none

    Target: >=10 stances per ROADMAP.

    Save research output as `.planning/phases/106-va-compass-stances/_tmp-jones-research.json`.
  </action>
  <verify>
    <automated>
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-jones-research.json"
      ((Get-Content "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-jones-research.json" | ConvertFrom-Json).Count -ge 10)
    </automated>
  </verify>
  <done>
    - Jones UUID resolved
    - Research JSON file exists with >=10 entries
    - Every entry has non-empty sources array
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 328_jones_stances.sql matching 282 pattern exactly</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251)
    - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql (recent VA exec analog)
    - .planning/phases/106-va-compass-stances/_tmp-jones-research.json
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/328_jones_stances.sql` following the exact 326 structure with substitutions:
      - Header: "Migration 328: Jay Jones Stances" / "Attorney General of Virginia"
      - Section header: "Jay Jones"
      - politician_id literal: ${JONES_UUID} from Task 1
      - One INSERT pair per topic in _tmp-jones-research.json

    Same hard rules as Plan 01 Task 2 (BEGIN/COMMIT wrapper, paired INSERTs, float values, dollar quoting, ARRAY cast, topic reference block).
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/328_jones_stances.sql"
      (Select-String -Path "C:/EV-Accounts/backend/migrations/328_jones_stances.sql" -Pattern '^BEGIN;$' -SimpleMatch).Count -eq 1
      (Select-String -Path "C:/EV-Accounts/backend/migrations/328_jones_stances.sql" -Pattern '^COMMIT;$' -SimpleMatch).Count -eq 1
      $a = (Select-String -Path "C:/EV-Accounts/backend/migrations/328_jones_stances.sql" -Pattern 'INSERT INTO inform.politician_answers').Count
      $c = (Select-String -Path "C:/EV-Accounts/backend/migrations/328_jones_stances.sql" -Pattern 'INSERT INTO inform.politician_context').Count
      ($a -eq $c) -and ($a -ge 10)
    </automated>
  </verify>
  <done>
    - Migration file exists with valid structure
    - Equal answers/context INSERT counts (>=10)
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 328 + verify + close VA-STANCES-01</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/328_jones_stances.sql
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-06)
  </read_first>
  <action>
    Step A: Read migration 328 SQL and pass to mcp__supabase-local__execute_sql.

    Step B (per-politician verification via mcp__supabase-local__execute_sql):
      SELECT
        (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510003)) AS answer_count,
        (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510003) AND pc.politician_id IS NULL) AS unpaired,
        (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -510003) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)) AS uncited;
    Required: answer_count >= 10, unpaired = 0, uncited = 0.

    Step C (VA-STANCES-01 closure check — all 3 execs combined):
      SELECT external_id, COUNT(*) AS stance_count
      FROM inform.politician_answers pa
      JOIN essentials.politicians p ON p.id = pa.politician_id
      WHERE p.external_id IN (-510001, -510002, -510003)
      GROUP BY external_id
      ORDER BY external_id;
    Expected: 3 rows, with counts: -510003 >=10, -510002 >=10, -510001 >=15.

    Step D: Delete `.planning/phases/106-va-compass-stances/_tmp-jones-research.json`.
  </action>
  <verify>
    <automated>
      Via mcp__supabase-local__execute_sql, the closure SELECT in Step C must return exactly 3 rows with Spanberger (-510001) >=15, Hashmi (-510002) >=10, Jones (-510003) >=10.
    </automated>
  </verify>
  <done>
    - Migration 328 applied
    - Jones stance count >= 10
    - VA-STANCES-01 fully satisfied (all 3 VA execs covered)
  </done>
</task>

</tasks>

<verification>
- Closure query confirms 3 rows for execs with required minimum counts
- VA-STANCES-01 satisfied
- All 3 migration files (326, 327, 328) committed to EV-Accounts repo
</verification>

<success_criteria>
- VA-STANCES-01 fully satisfied (Spanberger >=15, Hashmi >=10, Jones >=10)
- ROADMAP Phase 106 success criterion #1 fully met
- Phase ready to advance to senators (Plan 04)
</success_criteria>

<output>
Create `.planning/phases/106-va-compass-stances/106-03-SUMMARY.md` documenting Jones stance count, omitted topics, and VA-STANCES-01 closure status.
</output>
