---
phase: 155-norwalk-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["155-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1036_norwalk_headshots.sql
autonomous: false
requirements: [NRWK-01]

must_haves:
  truths:
    - "Each of the 5 current officials has exactly one type='default' headshot at the canonical Storage path"
    - "Rick Ramirez's image is replaced — the DB-seeded image used the broken 638169002126970000.jpg URL (404); the corrected RR - Digital Images - Copy.jpg portrait is used"
    - "The other 4 existing images (Perez, Rios, Ayala, Valencia) are verified correct-person and re-processed to 600x750 4:5"
    - "Every headshot is the actual Norwalk official (no name-collision, no superimposed text), 600x750 4:5"
    - "photo_origin_url on each politician row records the real norwalkca.gov Revize source (Rios uses the double-space %20%20 path)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1036_norwalk_headshots.sql"
      provides: "verify-and-fix all 5 existing images (Ramirez URL replaced) + photo_origin_url backfill (AUDIT-ONLY, not registered)"
      contains: "norwalk_headshots"
  key_links:
    - from: "essentials.politician_images (type='default')"
      to: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      via: "canonical x-upsert upload"
      pattern: "politician_photos/.*-headshot.jpg"
    - from: "essentials.politicians.photo_origin_url"
      to: "norwalkca.gov Revize CMS portrait URL"
      via: "source backfill"
      pattern: "norwalkca.gov/Image/Government"
---

<objective>
Wave 3 of the Norwalk deep-seed: give all 5 current officials a clean, correct, 600x750 headshot in ONE
AUDIT-ONLY migration (`1036_norwalk_headshots.sql` — raw SQL, NOT registered in `schema_migrations`; ledger
stays 1035).

Image state entering this wave: all 5 (Perez, Rios, Ayala, Ramirez, Valencia) have exactly 1 existing image
— so all 5 are UPDATEs, ZERO greenfield INSERTs (contrast Burbank which had 2 greenfield inserts). Source is
norwalkca.gov Revize CMS — RESEARCH confirmed NO WAF: standard `curl -L` returns HTTP 200, NO special UA
needed (simpler than Burbank). TWO documented gotchas: (1) Ramirez's DB image uses the OLD filename
`638169002126970000.jpg` which now 404s — MUST replace with the corrected `RR - Digital Images - Copy.jpg`
portrait; (2) Rios's folder name has a DOUBLE SPACE ('Margarita  Rios') — the URL needs `%20%20` (two %20).
Crop 4:5 FIRST -> resize 600x750 Lanczos q90 -> upload to `politician_photos/{uuid}-headshot.jpg` (x-upsert).
Wrong-person guard carried from West Covina (verify each portrait is the actual Norwalk official, not a
name-collision — "Margarita Rios" and "Tony Ayala" are common names).

Purpose: a correct portrait for every current official; honest documented gaps if any portrait fails.
Output: processed images in Supabase Storage + `1036_norwalk_headshots.sql` committed to EV-Accounts.
This wave ENDS WITH A BLOCKING HUMAN-VERIFY CHECKPOINT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/155-norwalk-deep-seed/155-CONTEXT.md
@.planning/phases/155-norwalk-deep-seed/155-RESEARCH.md
@.planning/phases/155-norwalk-deep-seed/155-PATTERNS.md
@.planning/phases/155-norwalk-deep-seed/155-VALIDATION.md
@.planning/phases/155-norwalk-deep-seed/155-02-SUMMARY.md
@LOCATION-ONBOARDING.md

<reference_data>
norwalkca.gov Revize CMS — NO WAF, standard `curl -s -L` returns HTTP 200 (NO special UA; WebFetch may 403 — use curl).
URL pattern: https://www.norwalkca.gov/Image/Government/Mayor%20And%20City%20Council/{Name}/{Initials}%20-%20Digital%20Images%20-%20Copy.jpg?t={ts}
All 5 verified HTTP 200 via Python urllib (RESEARCH §Headshots):
  Perez    pol 3ed36508  ext_id 666845   (existing 1 image — verify-and-fix; Dec 2025 portrait, 33919 bytes)
    https://www.norwalkca.gov/Image/Government/Mayor%20And%20City%20Council/Jennifer%20Perez/JP%20-%20Digital%20Images%20-%20Copy.jpg?t=202512101807300
  Rios     pol bd64253b  ext_id -201328  (existing 1 image — verify-and-fix; 28505 bytes) ⚠ DOUBLE SPACE 'Margarita%20%20Rios'
    https://www.norwalkca.gov/Image/Government/Mayor%20And%20City%20Council/Margarita%20%20Rios/MR%20-%20Digital%20Images%20-%20Copy.jpg?t=202512101808020
  Ayala    pol 5e8bcf17  ext_id -200876  (existing 1 image — verify-and-fix; Aug 2025, 33710 bytes)
    https://www.norwalkca.gov/Image/Government/Mayor%20And%20City%20Council/Tony%20Ayala/TA%20-%20Digital%20Images%20-%20Copy.jpg?t=202508201329270
  Ramirez  pol e3b9af1b  ext_id -201327  (existing 1 image — REPLACE; DB image uses 638169002126970000.jpg which 404s; 27965 bytes)
    https://www.norwalkca.gov/Image/Government/Mayor%20And%20City%20Council/Rick%20Ramirez/RR%20-%20Digital%20Images%20-%20Copy.jpg?t=202508201332230
  Valencia pol ba647863  ext_id -201329  (existing 1 image — verify-and-fix; Aug 2025, 22441 bytes)
    https://www.norwalkca.gov/Image/Government/Mayor%20And%20City%20Council/Ana%20Valencia/AV%20-%20Digital%20Images%20-%20Copy.jpg?t=202508201340330
Canonical Storage URL (use the 1028 form): https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg
  (verify the exact host against an existing Norwalk politician_images row — PATTERNS notes the kxsdzaojfaibhuzmclfq host)
photo_license: 'press_use' for norwalkca.gov portraits. photo_origin_url lives on essentials.politicians (NOT on politician_images).
Prefer norwalkca.gov (canonical) over norwalk.org for photo_origin_url (Pitfall 6).
Skill: .claude/skills/find-headshots (find-headshots) available for sourcing/fallback.
Fallback sources (only if a city portrait fails inspection): Ballotpedia 2024 pages (Ramirez/Rios/Valencia), Facebook campaign (Ramirez4Norwalk, AyalaforNorwalkCityCouncil, jenniferperezfornorwalkcitycouncil) — no-graphics check required.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Headshot pre-flight — capture per-official image state + confirm canonical Storage host + Ramirez broken-URL note</name>
  <files>(read-only; no file written this task)</files>
  <read_first>
    - .planning/phases/155-norwalk-deep-seed/155-RESEARCH.md §Headshots (all 5 URLs HTTP 200, NO WAF; Ramirez old URL 404s; Rios double-space) + §Existing DB Images Known Issue (Ramirez) + §Common Pitfalls Pitfall 3 (broken Ramirez URL) + Pitfall 4 (Rios double-space) + Pitfall 6 (norwalkca.gov canonical) + §Assumptions Log A2/A3
    - .planning/phases/155-norwalk-deep-seed/155-PATTERNS.md §`1036_norwalk_headshots.sql` (all 5 UPDATE existing — no greenfield INSERT; photo_origin_url backfill on politicians; canonical host kxsdzaojfaibhuzmclfq; Ramirez REPLACE + Rios double-space) + §Shared Patterns
    - .planning/phases/155-norwalk-deep-seed/155-02-SUMMARY.md (final roster + office titles)
  </read_first>
  <action>
    Via `mcp__supabase-local__execute_sql`, capture exact image state per current official:
    (a) `SELECT pi.id, pi.politician_id, pi.url, pi.type, pi.photo_license, p.external_id, p.full_name, p.photo_origin_url FROM essentials.politician_images pi RIGHT JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (666845,-201328,-200876,-201327,-201329) ORDER BY p.external_id`. Confirm all 5 have exactly 1 existing image -> all 5 are UPDATE (no greenfield INSERT). Record each member's current url + photo_origin_url. SPECIAL: confirm whether Ramirez's existing image url/photo_origin_url references the broken `638169002126970000.jpg` filename.
    (b) Confirm the canonical Storage host: read one existing Norwalk politician_images.url and confirm whether it uses `kxsdzaojfaibhuzmclfq.storage.supabase.co` (1028 form) or another host. Use the form that matches existing rows. Record the chosen host.
    (c) Record Ramirez's existing image row UUID + URL for the REPLACE in Task 2 (old URL 404s on the Revize CDN).
    Produce a written pre-flight image-state block: all-5-UPDATE plan, chosen Storage host, Ramirez broken-URL note.
  </action>
  <verify>
    <automated>MISSING — read-only MCP queries; capture in SUMMARY pre-flight block</automated>
  </verify>
  <acceptance_criteria>
    - per-member image count recorded: all 5 (Perez/Rios/Ayala/Ramirez/Valencia) = 1 existing image (all UPDATE plan; zero greenfield INSERT)
    - canonical Storage host confirmed against an existing row; chosen host string recorded
    - Ramirez existing image row UUID + URL recorded; whether it references the broken 638169002126970000.jpg filename noted
  </acceptance_criteria>
  <done>Per-official image-state block written: all 5 are UPDATE (no greenfield INSERT), canonical Storage host confirmed, Ramirez broken-URL note captured.</done>
</task>

<task type="auto">
  <name>Task 2: Source + process + upload 5 headshots; author + apply + commit 1036_norwalk_headshots.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1036_norwalk_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1028_burbank_headshots.sql (AUDIT-ONLY header, UPDATE existing default row, photo_origin_url backfill on politicians, audit post-verification — primary template; OMIT its greenfield INSERT blocks since Norwalk has 0 gaps)
    - .planning/phases/155-norwalk-deep-seed/155-PATTERNS.md §`1036_norwalk_headshots.sql` (UPDATE pattern WHERE politician_id+type for all 5; Ramirez REPLACE; photo_origin_url backfill with Rios %20%20 + Ramirez corrected URL; canonical Storage host; NO greenfield INSERTs needed) + §Shared Patterns (600x750 4:5 Lanczos q90; wrong-person guard)
    - .planning/phases/155-norwalk-deep-seed/155-RESEARCH.md §Code Examples (curl commands incl. Rios %20%20 + Ramirez corrected URL) + §Headshots URL table
    - LOCATION-ONBOARDING.md §Step 4 Headshots (4:5 crop FIRST then resize)
    - Task 1 pre-flight image-state block
  </read_first>
  <action>
    For each of the 5 current officials:
    (1) `curl -s -L -o <name>.jpg "<norwalkca.gov URL from reference_data>"`. NO special User-Agent needed (Revize has no WAF; -L follows the 302->200 redirect). CRITICAL gotchas: Rios's URL has DOUBLE SPACE in the folder — use `Margarita%20%20Rios` (two %20); Ramirez's correct URL is `RR%20-%20Digital%20Images%20-%20Copy.jpg?t=202508201332230` (the old DB `638169002126970000.jpg` is HTTP 404 — do NOT use it).
    (2) WRONG-PERSON GUARD (West Covina lesson): visually confirm each image is the actual Norwalk official matching the name on norwalkca.gov/government/mayor_and_city_council/{member}.php; reject any name-collision (a different Margarita Rios / Tony Ayala) or image with superimposed text/graphics over the face.
    (3) Crop 4:5 FIRST, THEN resize to 600x750 Lanczos q90 (never stretch; eyes ~1/3 from top; full head+shoulders). Use the find-headshots skill / established pipeline.
    (4) Upload to Supabase Storage `politician_photos/{pol_uuid}-headshot.jpg` with x-upsert (public). All 5 are replacements (Ramirez especially — its old source 404s). If a city portrait fails inspection, use a fallback source (Ballotpedia / campaign site) with the right license, or document an honest gap (no fabricated photo).
    Then author `C:/EV-Accounts/backend/migrations/1036_norwalk_headshots.sql` (AUDIT-ONLY — `BEGIN; ... COMMIT;`, NO schema_migrations registration; header notes ledger stays 1035, NO WAF, Rios double-space, Ramirez REPLACE). In SQL:
    - All 5 (existing 1 image each): `UPDATE essentials.politician_images SET url='<canonical {uuid}-headshot.jpg>', type='default', photo_license='press_use' WHERE politician_id='<uuid>' AND type='default'`. (No greenfield INSERT — all 5 already have a default image. If a re-crop forces a delete+reinsert, use the guarded INSERT...WHERE NOT EXISTS pattern from 1028.)
    - photo_origin_url backfill on `essentials.politicians` (NOT on the image row): one guarded `UPDATE essentials.politicians SET photo_origin_url='<norwalkca.gov URL>' WHERE external_id=<ext> AND photo_origin_url IS DISTINCT FROM '<url>'` per official, using each member's URL from reference_data. RAMIREZ: use the CORRECTED URL (`RR%20-%20Digital%20Images%20-%20Copy.jpg?t=202508201332230`), NOT the old 638169 one. RIOS: use the `%20%20` double-space URL. For any operator-supplied/fallback portrait with no canonical URL: `SET photo_origin_url=NULL` + image photo_license='fair_use'.
    Append the audit post-verification comment block (each n_default=1; all 5 urls = canonical path; photo_origin_url set on all 5 incl. corrected Ramirez; schema_migrations MAX unchanged at 1035). APPLY live via `mcp__supabase-local__execute_sql`. Commit: `git -C "C:/EV-Accounts" add backend/migrations/1036_norwalk_headshots.sql && git -C "C:/EV-Accounts" commit -m "feat(155): Norwalk headshots — verify-and-fix all 5 (Ramirez URL replaced), 600x750"`.
  </action>
  <verify>
    <automated>MISSING — apply via MCP then run acceptance assertion queries via mcp__supabase-local__execute_sql; image dimensions verified during processing</automated>
  </verify>
  <acceptance_criteria>
    - each current official has exactly one type='default' image (VALIDATION Wave-3): `SELECT p.external_id, COUNT(pi.*) FILTER (WHERE pi.type='default') n FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id WHERE p.external_id IN (666845,-201328,-200876,-201327,-201329) GROUP BY p.external_id` -> every n=1
    - Ramirez image replaced (VALIDATION Wave-3): Ramirez (-201327) photo_origin_url references `RR%20-%20Digital%20Images%20-%20Copy.jpg` (NOT 638169002126970000.jpg); his Storage object returns HTTP 200
    - all 5 carry canonical Storage URL: each url matches `politician_photos/{their_uuid}-headshot.jpg`; each uploaded object returns HTTP 200
    - photo_origin_url set (VALIDATION Wave-3): `SELECT external_id, photo_origin_url FROM essentials.politicians WHERE external_id IN (666845,-201328,-200876,-201327,-201329)` -> each non-NULL (unless documented operator-supplied fallback); Rios's contains `%20%20` (double-space path)
    - all 5 processed images are 600x750 (4:5), correct person, no superimposed text (confirmed at checkpoint)
    - ledger UNCHANGED: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` -> 1035; file committed to EV-Accounts
  </acceptance_criteria>
  <done>5 portraits sourced (NO-WAF curl -L)/verified/processed to 600x750 and uploaded; 1036_norwalk_headshots.sql applied (audit-only, ledger 1035) and committed; Ramirez's broken URL replaced; all 5 verified-and-fixed; photo_origin_url backfilled (Rios %20%20, Ramirez corrected); any gap documented honestly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>5 Norwalk official headshots (Mayor Perez, Vice Mayor Rios, Councilmembers Ayala/Ramirez/Valencia) sourced from norwalkca.gov (NO-WAF curl -L), cropped 4:5 to 600x750, uploaded to Supabase Storage, and wired into politician_images (all 5 verified-and-fixed; Ramirez's broken 638169002126970000.jpg URL replaced with the corrected RR portrait). photo_origin_url backfilled on all 5 (Rios uses the %20%20 double-space path).</what-built>
  <how-to-verify>
    1. Visit https://essentials.empowered.vote/results?browse_geo_id=0652526&browse_mtfcc=G4110 and confirm each of the 5 officials renders with a photo.
    2. For EACH official, compare the rendered headshot against norwalkca.gov/government/mayor_and_city_council/{member}.php — confirm it is the ACTUAL person (wrong-person guard, the West Covina lesson), not a name-collision (esp. "Margarita Rios" / "Tony Ayala" common names).
    3. RAMIREZ SPECIAL: confirm his image is the corrected current portrait (the old DB URL 638169002126970000.jpg 404s) and is the correct person.
    4. Confirm no superimposed text/graphics over any face; framing is head+shoulders, eyes ~1/3 from top; no distortion.
    5. Confirm no duplicate/stale image rows (each official has exactly one default image).
    6. If any portrait is an honest documented gap, confirm it is genuinely unavailable (no fabricated photo).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 4 (stances), or describe which headshot(s) need re-sourcing/re-cropping.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| norwalkca.gov (untrusted external) -> processed image -> Supabase Storage + DB | External image content crosses into our store; surface is wrong-person/integrity, not injection |
| executor SQL -> production Supabase | Audit-only static SQL mutating image rows |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-12 | Spoofing/Integrity | Wrong-person headshot (name-collision: a different Margarita Rios / Tony Ayala) | mitigate | Visual match against norwalkca.gov member bio page before upload; filenames include member names; BLOCKING human-verify checkpoint |
| T-155-13 | Integrity | Ramirez image left as the broken 638169002126970000.jpg (404 on CDN) | mitigate | Pitfall 3: use the corrected `RR - Digital Images - Copy.jpg` URL; acceptance asserts photo_origin_url references the corrected path; Storage object HTTP 200; checkpoint confirms |
| T-155-14 | Integrity | Rios fetch fails / wrong image due to single-space URL (folder has double space) | mitigate | Pitfall 4: encode `Margarita%20%20Rios` (two %20); RESEARCH confirms HTTP 200 with %20%20; acceptance asserts photo_origin_url contains %20%20 |
| T-155-15 | Tampering | INSERT creates a second image row for an already-imaged member | mitigate | All 5 use UPDATE (existing image), not INSERT; assert exactly 1 default image per member |
| T-155-16 | Repudiation | Missing photo_origin_url (can't trace source) | mitigate | Guarded photo_origin_url backfill per official (norwalkca.gov URL); NULL only for documented operator-supplied/fair_use |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- 5 officials each have exactly one type='default' headshot at the canonical path; all HTTP 200.
- Ramirez's broken URL replaced; Rios's photo_origin_url uses %20%20; photo_origin_url set or documented NULL for all 5.
- All images 600x750, correct person, no superimposed text — confirmed at the blocking human-verify checkpoint.
- schema_migrations MAX unchanged (1035); file committed.
</verification>

<success_criteria>
- ROADMAP success criterion 3: 600x750 headshots uploaded for all officials with an available official portrait; genuine gaps documented (no fabricated photos).
- ROADMAP success criterion 5 (partial): browse view renders the roster with photos; no duplicate/stale image rows.
</success_criteria>

<output>
Create `.planning/phases/155-norwalk-deep-seed/155-03-SUMMARY.md` when done (per-official source URL, license, any fallback/operator-supplied note, Ramirez replacement verdict, and checkpoint approval).
</output>
