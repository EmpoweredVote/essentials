---
phase: 182-city-of-cornelius-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["182-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-cornelius-headshots.py
  - C:/EV-Accounts/backend/migrations/1197_cornelius_headshots.sql
autonomous: false
requirements: [WASH-08]

must_haves:
  truths:
    - "Each of the 4 FILLED Cornelius officials (Dalin/Godinez Valencia/Baker/López) has a 600×750 headshot uploaded to Supabase Storage politician_photos/{uuid}-headshot.jpg — RESEARCH confirmed 4/4 direct-download sourcing from corneliusor.gov (the BEST in the milestone), so a full 4/4 is expected and any shortfall is a regression to investigate, not a planned gap"
    - "The 4 source PNGs are 1600×2000 EXACT 4:5 transparent circle-cutouts → each is composited onto a WHITE background FIRST (Henderson/Seebock RGBA precedent), then resized straight to 600×750 Lanczos q90 — NO crop judgment is needed (ratio already matches; the crop_to_4_5 early-return fires)"
    - "Sourcing is direct from corneliusor.gov/ImageRepository/Document?documentID=N (2325 Dalin, 1977 Godinez Valencia, 2324 Baker, 1979 López) — NO fallback chain expected; ci.cornelius.or.us is DEAD and must not be used"
    - "The vacant 5th seat has NO headshot (documentID=1975 is a blank placeholder) — documented as a genuine, correct gap; NEVER force a former officeholder's image"
    - "One politician_images row per FILLED official (type='default', photo_license per actual source — official city studio portraits are 'press_use'), columns exactly (id, politician_id, url, type, photo_license) — NO photo_origin_url; guarded WHERE NOT EXISTS on politician_id"
    - "No image has superimposed text/graphics over the face; each is a valid non-zero JPEG (HTTP 200 from CDN)"
    - "WR-01 (carried in the Sherwood template): the pipeline script main() EXITS NON-ZERO if ANY configured official failed to upload — a chained automation cannot apply the audit migration on a silently-partial run"
    - "WR-C: the headshot script's main() guards `if len(OFFICIALS) > 0:` before calling test_download_guard(OFFICIALS[0])"
    - "WR-02: the prefetched_bytes reuse guard is retained (source downloaded once, reused through composite/resize/upload)"
    - "The headshot script hard-asserts len(OFFICIALS)==4 AND asserts ext_ids are unique"
    - "The audit-only headshot migration post-verify asserts 4 politician_images rows whose url embeds the politician uuid; the ORCHESTRATOR NOTE's stated count matches the gate (WR-A no drift)"
    - "Decision coverage — D-09: headshots resize to 600×750 Lanczos q90, {uuid}-headshot.jpg naming, photo_license by actual source, no fabrication, vacant seat = documented genuine gap; RGBA-composite-onto-white step is the PRIMARY step this phase (transparent-PNG sources); WR-01 non-zero exit + WR-C empty-roster guard + WR-02 prefetch guard retained (clone from _tmp-sherwood-headshots.py ONLY)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-cornelius-headshots.py"
      provides: "Download → composite-onto-white → (no-op crop) → resize 600×750 → upload pipeline with the WR-01 non-zero-exit + WR-C empty-roster guard + WR-02 prefetch guard"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/1197_cornelius_headshots.sql"
      provides: "politician_images INSERTs for all 4 filled officials (audit-only, NOT registered in the ledger) with the WR-A-synced note + url-embeds-uuid count=4 gate"
      min_lines: 20
      contains: "politician_images"
  key_links:
    - from: "_tmp-cornelius-headshots.py"
      to: "Supabase Storage politician_photos bucket"
      via: "PUT {uuid}-headshot.jpg x-upsert:true"
      pattern: "politician_photos"
    - from: "1197_cornelius_headshots.sql"
      to: "essentials.politicians (by external_id)"
      via: "subselect politician_id per row"
      pattern: "external_id = -41155"
---

<objective>
Author the headshot pipeline script and the audit-only migration that records the 4 filled Cornelius
officials' portraits. Cornelius has the BEST headshot sourcing in the milestone: all 4 filled-seat
portraits are static ImageRepository downloads from corneliusor.gov (no WAF), each already an exact
1600×2000 (4:5) studio portrait — expect a full 4/4, NO fallback chain needed. The one genuinely
exercised pipeline step is the RGBA-to-white COMPOSITE: all 4 sources are a circular photo cutout on a
fully transparent PNG canvas, so a naive RGB conversion would produce black corners — composite onto
white FIRST, then (because the canvas is already exactly 4:5) skip crop judgment and resize straight to
600×750. The vacant 5th seat has no photo (its leftover image is blank) — documented as a genuine gap.
Clone from `_tmp-sherwood-headshots.py` ONLY (it alone carries the WR-01/WR-02/WR-C fixes).

Purpose: Satisfy the headshot portion of WASH-08 — all 4 filled officials render with 600×750 photos.
Output: `_tmp-cornelius-headshots.py` (gitignored helper, run by the orchestrator, WR-01 + WR-C + WR-02
intact) + audit-only migration `1197_cornelius_headshots.sql` (WR-A-synced note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES both files. It has NO DB/Storage access and NO
WebSearch. The INLINE ORCHESTRATOR runs `_tmp-cornelius-headshots.py` (which resolves each politician
UUID by external_id at runtime via psycopg2, downloads each portrait, composites, resizes, and uploads),
then applies the audit migration via `psql -f`. The `.py` file is a gitignored `_tmp-*` helper — do NOT
commit it. The `.sql` migration IS committed to the EV-Accounts repo (`git -C "C:/EV-Accounts"` — never
cd into it; check `git -C "C:/EV-Accounts" status` for pre-staged files before committing, per D-16).

**AUDIT-ONLY MIGRATION:** `1197_cornelius_headshots.sql` does NOT write a schema_migrations ledger row
(headshot/stance migrations bypass the ledger; on-disk counter is authoritative). Keep the literal
strings `slug`, `schema_migrations`, and `photo_origin_url` OUT of comments.
</context>

<interfaces>
essentials.politician_images  (id, politician_id, url, type, photo_license)  -- NO photo_origin_url column; type='default'
-- politician UUIDs: resolve by external_id from 182-02-SUMMARY.md (4 FILLED seats only):
--   -4115551 Jeffrey C. Dalin (Mayor) | -4115552 Angeles Godinez Valencia (Council President)
--   -4115553 Edgar Baker | -4115554 Edén López
-- Headshot sources (all confirmed HTTP 200, 1600×2000 transparent RGBA PNG, at RESEARCH time; re-confirmed at Wave-0):
--   documentID 2325 (Dalin) | 1977 (Godinez Valencia) | 2324 (Baker) | 1979 (López)
--   base: https://www.corneliusor.gov/ImageRepository/Document?documentID=
-- The vacant 5th seat (-4115555) has NO politician row and NO image; documentID=1975 is a blank placeholder — do NOT use.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot pipeline script (WR-01 exit + WR-C empty-roster guard + WR-02 prefetch, transparent-PNG composite)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-cornelius-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-sherwood-headshots.py (the ~490-line pipeline template — the ONLY copy carrying the WR-01 non-zero-exit + WR-02 prefetched_bytes + WR-C empty-roster fixes; copy structure/functions verbatim, rename city strings; gitignored but present on disk. Do NOT clone from Forest Grove's script.)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md §"_tmp-cornelius-headshots.py (utility, file-I/O + batch)" (incl. the RGBA-to-white composite step at lines 288-299, the crop_to_4_5 early-return note, the OFFICIALS roster with the 4 documentID URLs, the WR-C-guarded main(), and the WR-01 exit gate)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md §"Headshot Sources — 4/4 filled seats CONFIRMED"
    - .planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md (Wave-0 re-download confirmations + transparent-PNG note) + 182-02-SUMMARY.md (minted UUIDs by external_id for the 4 filled seats)
  </read_first>
  <action>
    Copy the Sherwood script structure (config block, crop_to_4_5, resize_600x750, process_headshot_bytes
    incl. the RGBA-onto-white composite branch, resolve_politician_id, download_image, upload_to_storage,
    process_member, manifest output, CDN_BASE-from-SUPABASE_URL derivation, descriptive User-Agent,
    test_download_guard). Set an OFFICIALS list of exactly 4 dicts (the FILLED seats ONLY), each with
    ext_id (-4115551 Dalin, -4115552 Godinez Valencia, -4115553 Baker, -4115554 López), name, url (the
    confirmed corneliusor.gov ImageRepository URL:
    https://www.corneliusor.gov/ImageRepository/Document?documentID=N for N=2325/1977/2324/1979
    respectively), and license='press_use' for all 4 (official city studio portraits — the orchestrator
    may adjust per actual source at run time). Do NOT include a 5th entry for the vacant seat — its
    leftover image (documentID=1975) is confirmed blank; document that as a genuine gap in the migration
    comment, NOT in this roster list. Resolve each politician UUID at RUNTIME by external_id via psycopg2
    (do NOT hardcode UUIDs). Keep the config block: BUCKET='politician_photos', TARGET_SIZE=(600,750),
    JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS. Pipeline order per official: download once (WR-02
    prefetched_bytes — reuse the same bytes through the rest of the pipeline, do NOT re-fetch),
    COMPOSITE ONTO WHITE (the RGBA/PNG branch of process_headshot_bytes — this is the PRIMARY exercised
    step for Cornelius since all 4 sources are transparent-background PNGs; keep it unmodified),
    crop_to_4_5 (a NO-OP here — the 1600×2000 source is already exactly 4:5, so the
    abs(current_ratio - target_ratio) < 0.001 early-return fires; keep the guard, do not remove it),
    resize to 600×750 Lanczos, reject any image with superimposed text/graphics, upload to
    politician_photos/{uuid}-headshot.jpg via PUT with x-upsert:true, emit a manifest line (ext_id →
    uuid → CDN url → source). HARD-ASSERT len(OFFICIALS)==4 (RESEARCH confirmed 4/4 direct sourcing —
    the vacant seat is a genuine gap, not a source failure) AND assert ext_ids are unique. Keep the
    WR-01 exit gate: collect per-official results, print the manifest, and if ANY official failed to
    upload call sys.exit(1). Keep the WR-C guard in main(): `if len(OFFICIALS) > 0:` before calling
    test_download_guard(OFFICIALS[0]). Include a docstring SOURCE NOTE describing the best-in-milestone
    direct ImageRepository sourcing (no fallback chain, ci.cornelius.or.us DEAD), the transparent-PNG
    composite-then-no-crop nuance, the vacant-seat gap, the WR-01/WR-02/WR-C fixes, and marking the file
    a gitignored _tmp helper the orchestrator runs. No fenced code in this action — the Sherwood analog +
    182-PATTERNS.md (which fully specifies the composite step, OFFICIALS list, and the WR-C guard) are the
    source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `python C:/EV-Accounts/backend/scripts/_tmp-cornelius-headshots.py`; the script validates non-zero JPEG per image, prints the CDN manifest, and EXITS NON-ZERO if any configured upload failed (WR-01)</automated>
  </verify>
  <acceptance_criteria>
    - OFFICIALS list has exactly 4 entries (ext_ids -4115551..-4115554, the FILLED seats) with the confirmed corneliusor.gov ImageRepository documentID URLs (2325/1977/2324/1979); NO vacant-seat entry
    - license='press_use' for all 4 (official city studio portraits; orchestrator may adjust per actual source)
    - hard-asserts len(OFFICIALS)==4 AND asserts unique ext_ids
    - UUIDs resolved at runtime by external_id (no hardcoded UUIDs)
    - TARGET_SIZE=(600,750), JPEG_QUALITY=90, Lanczos resample; the RGBA-onto-white composite branch runs BEFORE resize; crop_to_4_5 early-return fires on the 1600×2000 (4:5) source (no crop); rejects overlay/text images
    - WR-02 prefetched_bytes: each source downloaded once and reused (no re-fetch through composite/resize/upload)
    - main() calls sys.exit(1) when ANY configured official failed to upload (WR-01)
    - main() guards `if len(OFFICIALS) > 0:` before test_download_guard(OFFICIALS[0]) (WR-C)
    - Docstring SOURCE NOTE records the best-in-milestone direct ImageRepository sourcing (no fallback chain, ci.cornelius.or.us dead), the transparent-PNG composite-then-no-crop nuance, the vacant-seat gap, and the WR-01/WR-02/WR-C fixes; marks it a gitignored _tmp helper run by the orchestrator
  </acceptance_criteria>
  <done>Pipeline script authored with the WR-01 exit + WR-C guard + WR-02 prefetch + transparent-PNG composite (no-crop) handling; runs and uploads 600×750 portraits for all 4 filled officials, exiting non-zero on any upload failure.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration (WR-A note-text sync)</name>
  <files>C:/EV-Accounts/backend/migrations/1197_cornelius_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1188_sherwood_headshots.sql (the ~125-line template — url-embeds-uuid post-verify gate; the WR-A-synced note is already correct in this file)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md §"1197_cornelius_headshots.sql (migration, audit-only, CRUD)" (incl. the WR-A note-sync rule and the count=4 post-verify gate)
    - .planning/phases/182-city-of-cornelius-deep-seed/182-03 headshot manifest (from Task 1 run — the per-official UUIDs and the actual sourced count, expected 4)
  </read_first>
  <action>
    Single BEGIN...COMMIT. Header comment: "AUDIT-ONLY (not registered in the ledger)" plus a source
    note stating the best-in-milestone direct ImageRepository sourcing from corneliusor.gov (no WAF, no
    fallback needed; expect 4/4) and that the 5th councilor seat is genuinely VACANT with NO photo (its
    leftover documentID=1975 image is blank — do not force a former officeholder's image). FOUR
    politician_images INSERT ... SELECT blocks (one per FILLED official), columns EXACTLY
    (id, politician_id, url, type, photo_license) — NO photo_origin_url. politician_id resolved via
    subselect on external_id (within -4115551..-4115554). url is the final Supabase CDN path
    (politician_photos/{uuid}-headshot.jpg — the orchestrator fills each {uuid} from the script manifest
    before applying). type='default'. photo_license set per the actual source recorded in the manifest
    (default 'press_use' for the official city studio portraits). Each INSERT guarded WHERE NOT EXISTS on
    politician_id (idempotent). Append the post-verify DO block from mig 1188: count politician_images
    rows JOINed to politicians WHERE external_id BETWEEN -4115554 AND -4115551 AND url LIKE '%' ||
    politician_id::text || '%' — RAISE EXCEPTION if the count <> 4. APPLY THE WR-A FIX: write the
    ORCHESTRATOR NOTE's stated gate default to match whatever the post-verify gate literally says at the
    moment of authoring — if the gate reads `IF n <> 4`, the note must say the expected count is 4 (no
    stale drift). NO ledger INSERT (audit-only). Keep `photo_origin_url`/`slug`/`schema_migrations` out
    of comments. If the file carries the accented 'López' anywhere in a comment, save it UTF-8 without a
    BOM.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1197_cornelius_headshots.sql` (the url-embeds-uuid DO block RAISEs unless 4 rows embed their politician uuid) then runs `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -4115554 AND -4115551)`</automated>
  </verify>
  <acceptance_criteria>
    - Four politician_images INSERT blocks; columns exactly (id, politician_id, url, type, photo_license)
    - No photo_origin_url column anywhere
    - politician_id resolved via external_id subselect (within -4115551..-4115554); NO row for the vacant -4115555 seat
    - type='default'; photo_license per source (default 'press_use'); each guarded WHERE NOT EXISTS on politician_id
    - url-embeds-uuid DO block asserts count = 4
    - ORCHESTRATOR NOTE's stated expected-count matches the gate's actual code (WR-A — no drift)
    - No ledger INSERT (audit-only); `photo_origin_url` does NOT appear in comments
    - Header source note documents the best-in-milestone direct sourcing (no fallback) AND the vacant-seat genuine gap
  </acceptance_criteria>
  <done>Audit migration authored recording all 4 filled-seat image rows with the WR-A-synced note, the count=4 url-embeds-uuid gate, and a documented vacant-seat gap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs pipeline, applies migration, verifies identity + quality</name>
  <what-built>The headshot pipeline script (WR-01 + WR-C + WR-02) + audit migration (WR-A). Orchestrator runs the pipeline (direct corneliusor.gov ImageRepository sourcing) and applies the migration.</what-built>
  <how-to-verify>
    1. Run `python C:/EV-Accounts/backend/scripts/_tmp-cornelius-headshots.py`; confirm each of the 4
       images downloads from corneliusor.gov, composites onto white (transparent circle-cutout → clean
       white corners, NOT black), skips the crop (already 4:5), resizes to 600×750, and uploads (HTTP 200
       on each CDN URL). The script EXITS NON-ZERO if any upload failed (WR-01). A 4/4 outcome is
       expected — investigate any shortfall as a regression (the vacant seat is the only genuine gap).
    2. Apply `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1197_cornelius_headshots.sql`;
       confirm the url-embeds-uuid DO block passes (count = 4) and the politician_images row count matches.
    3. Visual identity + quality check on each portrait: correct person (Dalin/Godinez Valencia/Baker/
       López), 600×750, clean white background (no black corners from the transparent source), NO
       superimposed text/graphics over the face. Confirm no wrong-person image and that López's portrait
       is correctly attributed.
    4. Check `git -C "C:/EV-Accounts" status` for pre-staged files (D-16); stage ONLY the migration.
       Commit: `git -C "C:/EV-Accounts" add backend/migrations/1197_cornelius_headshots.sql && git -C "C:/EV-Accounts" commit`. Do NOT commit the _tmp-*.py helper.
  </how-to-verify>
  <acceptance_criteria>
    - Pipeline ran; WR-01 non-zero exit behaves correctly on any failure; all 4 filled-seat headshots uploaded (4/4 expected)
    - Composite-onto-white produced clean white corners (no black) from the transparent-PNG sources
    - politician_images row count = 4; url-embeds-uuid DO block passed
    - Each portrait is the correct person, 600×750, no overlays, correct white background
    - Vacant seat correctly has NO image (documented gap, no forced photo)
    - `git -C "C:/EV-Accounts" status` checked; audit migration committed; _tmp .py NOT committed
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official confirmation (4/4), or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Portraits sourced directly from corneliusor.gov ImageRepository (no WAF, no fallback chain; ci.cornelius.or.us dead) |
| operator → Supabase Storage | Processed images uploaded to the public politician_photos bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-15 | Tampering | Partial-upload false success: a chained automation applies the audit migration after only some uploads succeeded | mitigate | WR-01 — main() calls sys.exit(1) when ANY configured official failed to upload; the audit migration's count=4 gate re-catches a mismatch |
| T-182-16 | Spoofing | Wrong-person or mislabeled headshot (esp. reusing the vacant seat's leftover blank image) | mitigate | URLs are per-official confirmed at Wave-0; the vacant seat is excluded from OFFICIALS; Task 3 visual identity check per official before commit; url-embeds-uuid gate ties each row to the right politician uuid |
| T-182-17 | Tampering | Black corners from a naive RGB conversion of the transparent-background PNG source | mitigate | The RGBA-onto-white composite branch runs before resize (Henderson/Seebock precedent); Task 3 confirms clean white corners |
| T-182-18 | Tampering | Corrupt/0-byte image | mitigate | Script validates non-zero bytes + valid PIL open; WR-02 prefetched_bytes reused; visual check confirms clean face |
| T-182-19 | Tampering | IndexError on an empty OFFICIALS list at test time | mitigate | WR-C guard: `if len(OFFICIALS) > 0:` before test_download_guard(OFFICIALS[0]) |
| T-182-20 | Information Disclosure | Superimposed text/graphics on portrait | mitigate | Pipeline rejects images with overlays; visual check confirms clean face |
| T-182-SC | Tampering | npm/pip/cargo installs (Pillow/psycopg2/requests) | mitigate | Pre-existing project deps used by all prior headshot phases — no NEW package install; if any pip install is needed, treat as [ASSUMED] and gate on a blocking human-verify before install |
</threat_model>

<verification>
- Pipeline runs with WR-01 non-zero exit + WR-C empty-roster guard + WR-02 prefetch; each of 4 filled
  600×750 portraits composited-onto-white then uploaded; politician_images rows = 4; url-embeds-uuid
  gate passes (WR-A-synced note); identity/quality confirmed (clean white corners); vacant seat has no
  image; audit migration committed after a clean git status check.
</verification>

<success_criteria>
- All 4 filled officials have a 600×750 headshot with no overlays and a clean white background from the
  transparent-PNG source; the pipeline exits non-zero on any upload failure (WR-01), guards the empty
  roster (WR-C), and reuses prefetched bytes (WR-02); audit migration recorded with the count=4
  url-embeds-uuid gate and a WR-A-synced note; the vacant 5th seat is a documented genuine gap.
</success_criteria>

<output>
Create `.planning/phases/182-city-of-cornelius-deep-seed/182-03-SUMMARY.md` when done, recording the
per-official image source + the 4/4 coverage (or any documented regression) and the vacant-seat gap.
</output>
</content>
