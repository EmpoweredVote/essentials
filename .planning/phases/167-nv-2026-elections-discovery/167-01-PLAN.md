---
phase: 167-nv-2026-elections-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1111_nv_2026_general_election.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1111.ts
autonomous: true
requirements: [NV-ELEC-01]

must_haves:
  truths:
    - "Exactly one essentials.elections row named 'NV 2026 Statewide General' exists with state='NV'"
    - "That election row carries election_type='general', jurisdiction_level='state', election_date='2026-11-03'"
    - "Re-running the migration leaves the row count at exactly 1 (idempotent NOT EXISTS guard)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1111_nv_2026_general_election.sql"
      provides: "Idempotent INSERT of the single NV 2026 general election row"
      contains: "NV 2026 Statewide General"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-1111.ts"
      provides: "Apply + smoke-test harness for migration 1111"
      contains: "1111_nv_2026_general_election.sql"
  key_links:
    - from: "scripts/_apply-migration-1111.ts"
      to: "migrations/1111_nv_2026_general_election.sql"
      via: "readFileSync(path.join(process.cwd(), 'migrations', '1111_nv_2026_general_election.sql'))"
      pattern: "1111_nv_2026_general_election\\.sql"
---

<objective>
Seed the single foundational `essentials.elections` row for Nevada's November 3, 2026 statewide general election. Every race row in Plan 02 resolves its `election_id` FK by name-matching this row, so it must exist first.

Purpose: Establishes the parent election record (NV-ELEC-01) that anchors all 63 NV 2026 race rows and gives the `/elections` page a 2026 NV ballot to read against.
Output: `migrations/1111_nv_2026_general_election.sql` + paired `scripts/_apply-migration-1111.ts` smoke harness, applied to the live DB with exactly one new row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-nv-2026-elections-discovery/167-CONTEXT.md
@.planning/phases/167-nv-2026-elections-discovery/167-RESEARCH.md
@.planning/phases/167-nv-2026-elections-discovery/167-PATTERNS.md

<interfaces>
<!-- essentials.elections column contract (verified live 2026-06-29, D-09). Use these directly. -->
essentials.elections:
  id                 uuid (gen_random_uuid())
  name               text NOT NULL  -- 'NV 2026 Statewide General'
  election_date      date / timestamptz cast accepted ('2026-11-03T08:00:00.000Z'::timestamptz)
  election_type      text           -- 'general'
  jurisdiction_level text NOT NULL  -- 'state'
  state              char nullable  -- 'NV'
  description        text nullable

Migration mechanics (D-08): files live in C:/EV-Accounts/backend/migrations/.
Apply scripts run FROM C:/EV-Accounts/backend via:
  node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1111.ts
The executor has NO Supabase MCP — DB verification runs through the apply script (pg Pool) and/or psql.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 1111 (NV 2026 general election row) + paired apply/smoke script</name>
  <files>C:/EV-Accounts/backend/migrations/1111_nv_2026_general_election.sql, C:/EV-Accounts/backend/scripts/_apply-migration-1111.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1109_seed_tx_ny_2026_house_elections_races.sql (canonical analog — copy its elections-INSERT structure, NOT EXISTS guard, DO $$ post-verify, BEGIN/COMMIT; note it writes NO schema_migrations ledger row)
    - C:/EV-Accounts/backend/scripts/_apply-migration-322.ts (apply-script analog — imports, pg Pool, readFileSync, smoke-test try/catch shape)
    - .planning/phases/167-nv-2026-elections-discovery/167-PATTERNS.md (sections "migrations/1111_nv_2026_general_election.sql" and "scripts/_apply-migration-1111.ts" — exact adapted excerpts + the "Critical naming rule" callout)
  </read_first>
  <behavior>
    - After apply: SELECT count(*) FROM essentials.elections WHERE name='NV 2026 Statewide General' returns exactly 1
    - That row has state='NV', election_type='general', jurisdiction_level='state', election_date='2026-11-03'
    - Second apply (idempotency): count stays 1, no error
  </behavior>
  <action>
    Write `migrations/1111_nv_2026_general_election.sql` wrapped in BEGIN;/COMMIT;. Single INSERT into essentials.elections (id, name, election_date, election_type, jurisdiction_level, state) selecting gen_random_uuid(), the literal name string `NV 2026 Statewide General` (this EXACT casing — NOT `2026 Nevada General Election`; the `{ST} 2026 Statewide General` convention is set by migration 1109), election_date `'2026-11-03T08:00:00.000Z'::timestamptz`, election_type `general`, jurisdiction_level `state`, state `NV`. Guard with `WHERE NOT EXISTS (SELECT 1 FROM essentials.elections e WHERE e.name = 'NV 2026 Statewide General')` — NO ON CONFLICT (follow 1109, not VA/MD). Add a `DO $$ ... RAISE EXCEPTION` post-verify block asserting the count is exactly 1, with a RAISE NOTICE on success. Do NOT write a `supabase_migrations.schema_migrations` ledger INSERT — migration 1109 omits it and the on-disk counter is authoritative (D-08).

    Write `scripts/_apply-migration-1111.ts` mirroring `_apply-migration-322.ts`: `import 'dotenv/config'`, pg Pool from `process.env['DATABASE_URL']` with `ssl: { rejectUnauthorized: false }`, readFileSync of `path.join(process.cwd(), 'migrations', '1111_nv_2026_general_election.sql')`, run it, then smoke-test queries logging actuals vs expected — (1) count of rows named `NV 2026 Statewide General` = 1, (2) election_date = 2026-11-03, (3) jurisdiction_level = state, (4) state = NV. OMIT any schema_migrations ledger smoke test (there is no ledger INSERT). Wrap in try/catch with `process.exit(1)` on error and `pool.end()` in finally.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1111.ts</automated>
  </verify>
  <acceptance_criteria>
    - Apply-script output line: "NV 2026 Statewide General election rows: 1 (expected 1)" (or equivalent count log) with actual=1
    - Output confirms election_date 2026-11-03, jurisdiction_level state, state NV
    - Running the apply script a SECOND time still reports count 1 (no duplicate, no error) — idempotency proven
    - migrations/1111_nv_2026_general_election.sql contains the literal string 'NV 2026 Statewide General' and NO 'schema_migrations' INSERT
  </acceptance_criteria>
  <done>One essentials.elections row named 'NV 2026 Statewide General' exists with the correct columns; apply script passes all smoke assertions and is idempotent on re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Operator-run SQL migration against live Supabase; no untrusted/user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-01-NA | Tampering/Injection | migration 1111 INSERT | accept | All values are hardcoded literals — no user input, no externally-reachable surface. SQL injection N/A. No new endpoint, no auth surface. Threats N/A for this pure-data INSERT plan. |

No externally-reachable surface is introduced by this plan; STRIDE categories S/T/R/I/D/E do not apply to a hardcoded-literal operator migration.
</threat_model>

<verification>
- `node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1111.ts` (from C:/EV-Accounts/backend) exits 0 and prints count=1.
- Re-run the same command: count remains 1 (idempotent).
- `psql $DATABASE_URL -c "SELECT name, state, election_type, jurisdiction_level, election_date FROM essentials.elections WHERE name='NV 2026 Statewide General'"` shows exactly one row with the expected values.
</verification>

<success_criteria>
Exactly one `essentials.elections` row named `NV 2026 Statewide General` (state='NV', election_type='general', jurisdiction_level='state', election_date='2026-11-03') exists in the live DB; the migration is idempotent; no schema_migrations ledger row was written.
</success_criteria>

<output>
Create `.planning/phases/167-nv-2026-elections-discovery/167-01-SUMMARY.md` when done. Record: migration 1111 applied, row UUID, confirmation that no ledger row was written, and that next migration counter is 1112.
</output>
