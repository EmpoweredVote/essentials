---
phase: 077-portland-officials
plan: "03"
type: execute
wave: 3
depends_on: ["077-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/232_portland_headshots.sql
autonomous: true
requirements: []
tags: [portland, oregon, headshots, supabase-storage, politician-images, audit-migration]
must_haves:
  truths:
    - "14 elected Portland officials have a headshot file at 600x750 in the politician_photos Supabase Storage bucket"
    - "14 essentials.politician_images rows exist with type='default' and photo_license='public_domain'"
    - "Each headshot URL is the canonical Supabase public URL for {politician_id}-headshot.jpg"
    - "City Administrator Lee III and City Attorney Taylor do NOT have headshots (appointed officials excluded per D-14 + CF-1)"
    - "232_portland_headshots.sql exists as an audit-only file (not added to schema_migrations ledger)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/232_portland_headshots.sql"
      provides: "Audit log of 14 politician_images INSERTs + 14 photo_origin_url UPDATEs"
      contains: "type='default'"
  key_links:
    - from: "essentials.politician_images.politician_id"
      to: "essentials.politicians.id (external_id IN (-690001, -690002, -690010..-690021))"
      via: "subquery WHERE external_id = X"
      pattern: "SELECT id FROM essentials.politicians WHERE external_id = -690001"
    - from: "Supabase Storage politician_photos/{politician_id}-headshot.jpg"
      to: "essentials.politician_images.url"
      via: "public URL https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{politician_id}-headshot.jpg"
      pattern: "{politician_id}-headshot.jpg"
---

<objective>
Upload 14 headshots (600x750 JPEG, Lanczos q90) to Supabase Storage for the 14 elected Portland officials, and insert matching essentials.politician_images rows with type='default' so the UI renders them.

Purpose: This closes roadmap SC-4 for Phase 77 — all elected Portland officials have headshots. City Administrator (Lee III) and City Attorney (Taylor) are excluded per D-14 + CF-1 (appointed officials). Audit migration 232 captures the 14 INSERTs and the photo_origin_url UPDATEs but is NOT added to the schema_migrations ledger (mirrors 215_berkeley_headshots.sql, 212_fremont_headshots.sql, 228_or_legislature_headshots.sql patterns).
Output: 14 image files in Supabase Storage at `politician_photos/{politician_id}-headshot.jpg`; 14 politician_images rows; 14 politicians.photo_origin_url values populated; one audit-only SQL file at `C:/EV-Accounts/backend/migrations/232_portland_headshots.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/077-portland-officials/077-CONTEXT.md
@.planning/phases/077-portland-officials/077-RESEARCH.md
@.planning/phases/077-portland-officials/077-PATTERNS.md
@.planning/phases/077-portland-officials/077-02-SUMMARY.md
@.planning/phases/68-berkeley-deep-seed/68-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Source, process, upload 14 headshots + write audit migration 232</name>
  <files>C:/EV-Accounts/backend/migrations/232_portland_headshots.sql</files>
  <read_first>
    - .planning/phases/077-portland-officials/077-RESEARCH.md (Headshot Sources table with 9 known URLs; Pitfalls 6–7; politician_images INSERT pattern)
    - .planning/phases/077-portland-officials/077-PATTERNS.md (Pattern Assignments → 232_portland_headshots.sql; work-list query; processing pipeline; CRITICAL type='default' rule)
    - .planning/phases/077-portland-officials/077-02-SUMMARY.md (confirms 14 elected officials have non-null office_id; 2 appointed excluded by is_appointed_position=true filter)
    - C:/EV-Accounts/backend/migrations/215_berkeley_headshots.sql (closest analog — audit-only headshot migration; Mayor + 8 council + Auditor)
    - C:/EV-Accounts/backend/migrations/228_or_legislature_headshots.sql (audit-only pattern; Lanczos q90 600x750 processing)
    - C:/Users/Chris/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_headshot_resize_no_distort.md (crop to 4:5 FIRST, then resize — never stretch)
    - C:/Users/Chris/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_headshot_image_sizing.md (600x750 Lanczos q90 mandatory)
    - C:/Users/Chris/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_headshot_cropping.md (eyes at ~1/3 from top, full head + shoulders visible)
  </read_first>
  <action>
    Step 1 — Run the work-list query from 077-PATTERNS.md "Work-list query" against the live DB. Expected: 14 rows with `o.is_appointed_position = false`, sorted by chamber and external_id. The 14 rows MUST be:
      external_id  | full_name              | chamber
      -690001      | Keith Wilson           | Mayor
      -690002      | Simone Rede            | City Auditor
      -690010      | Candace Avalos         | City Council (D1)
      -690011      | Jamie Dunphy           | City Council (D1)
      -690012      | Loretta Smith          | City Council (D1)
      -690013      | Dan Ryan               | City Council (D2)
      -690014      | Elana Pirtle-Guiney    | City Council (D2)
      -690015      | Sameer Kanal           | City Council (D2)
      -690016      | Angelita Morillo       | City Council (D3)
      -690017      | Steve Novick           | City Council (D3)
      -690018      | Tiffany Koyama Lane    | City Council (D3)
      -690019      | Eric Zimmerman         | City Council (D4)
      -690020      | Mitch Green            | City Council (D4)
      -690021      | Olivia Clark           | City Council (D4)

    If -690003 (Lee III) or -690004 (Taylor) appear, STOP — they are appointed and must be excluded. If a 14-row mismatch occurs, STOP.

    Step 2 — Capture each politician's UUID (essentials.politicians.id) for use in Storage paths and politician_images INSERTs.

    Step 3 — Source the original full-size portland.gov image for each of the 14 officials.

    Use the 9 known URLs from 077-RESEARCH.md "Headshot Sources" table verbatim:
      - Keith Wilson:        https://www.portland.gov/sites/default/files/public/2024/Wilson-Blue-Background_0.png
      - Simone Rede:         https://www.portland.gov/sites/default/files/public/2022/auditor-simone-rede_1.jpg
      - Candace Avalos:      https://www.portland.gov/sites/default/files/public/2025/Pink-Official-Background_0.png
      - Jamie Dunphy:        https://www.portland.gov/sites/default/files/public/2025/Dunphy---IMG_8672---square---web.jpg
      - Loretta Smith:       https://www.portland.gov/sites/default/files/public/2025/CouncilorSmithheadshot.jpg
      - Dan Ryan:            https://www.portland.gov/sites/default/files/public/2025/Ryan---IMG_8965---square---web.jpg
      - Angelita Morillo:    https://www.portland.gov/sites/default/files/public/2025/Morillo---IMG_9092---square---web.jpg
      - Tiffany Koyama Lane: https://www.portland.gov/sites/default/files/public/2025/Koyama-Lane---IMG_9037---square---web.jpg
      - Mitch Green:         https://www.portland.gov/sites/default/files/public/2025/Green---IMG_8827---square---web.jpg

    For the remaining 5 (Elana Pirtle-Guiney, Sameer Kanal, Steve Novick, Eric Zimmerman, Olivia Clark): visit the per-council-member page at `https://www.portland.gov/council/districts/[N]/[first-name]-[last-name]` and identify the full-size image. The naming convention is `[LastName]---IMG_XXXX---square---web.jpg` (per 077-RESEARCH.md). Drop the `styles/1_1_160w/` thumbnail prefix to get the full-size URL. If portland.gov returns 404 or no image, fall back to Wikimedia Commons (only public-domain or CC-BY-SA-4.0 official portraits are acceptable; record the license accordingly).

    Step 4 — For each of the 14 officials, run the headshot pipeline using `sharp` or PIL (project convention — Phase 75/67 used both successfully):
      - Download the original to a working directory
      - CROP to 4:5 ratio FIRST — for square (1:1) sources, crop top so eyes land at ~1/3 from the top (per `feedback_headshot_cropping.md`); for tall sources, top-crop to retain face; DO NOT stretch
      - Resize to exactly 600x750 with Lanczos resampling and JPEG quality=90 (per `feedback_headshot_image_sizing.md`)
      - Write the JPEG to disk as `{politician_id}-headshot.jpg`

    Step 5 — Upload each file to Supabase Storage at `politician_photos/{politician_id}-headshot.jpg` using the Supabase Storage API (project ref kxsdzaojfaibhuzmclfq, bucket `politician_photos`). Confirm 14 uploads succeed.

    Step 6 — Write `C:/EV-Accounts/backend/migrations/232_portland_headshots.sql` as an audit-only migration (wrap in `BEGIN; ... COMMIT;`). The file MUST contain:
      - 14 `INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license, focal_point) VALUES (...)` statements — one per official — using:
          - id: gen_random_uuid()
          - politician_id: `(SELECT id FROM essentials.politicians WHERE external_id = <X>)`
          - url: `'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/' || (SELECT id::text FROM essentials.politicians WHERE external_id = <X>) || '-headshot.jpg'` OR the resolved literal URL with the UUID baked in (either works — match the Berkeley/SD audit migrations' style)
          - type: 'default'  (CRITICAL — must be 'default' not 'headshot'; UI filters `.find(img => img.type === 'default')`)
          - photo_license: 'public_domain' for portland.gov sources; 'cc-by-sa-4.0' only if a Wikimedia Commons fallback is used
          - focal_point: NULL
      - Each INSERT guarded by `WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = <X>) AND type='default')` for idempotency (Berkeley pattern)
      - 14 `UPDATE essentials.politicians SET photo_origin_url = '<source URL>' WHERE external_id = <X>` statements — one per official, using the original portland.gov (or Wikimedia) URL the file was downloaded from
      - Do NOT add this migration to `supabase_migrations.schema_migrations` — audit-only, per the established pattern in 215_berkeley_headshots.sql, 212_fremont_headshots.sql, 228_or_legislature_headshots.sql

    Step 7 — Apply the 28 SQL statements (14 INSERTs + 14 UPDATEs) to the live DB. Run the 4 verification gates below.
  </action>
  <verify>
    <automated>
SQL via Supabase Management API after headshot uploads + audit migration 232 are applied:

-- Gate 1: Exactly 14 politician_images rows exist for the elected Portland officials
SELECT COUNT(*) FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id BETWEEN -690021 AND -690001
  AND p.external_id NOT IN (-690003, -690004);
-- Expected: 14

-- Gate 2: Every one of the 14 rows has type='default'
SELECT COUNT(*) FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id BETWEEN -690021 AND -690001
  AND p.external_id NOT IN (-690003, -690004)
  AND pi.type = 'default';
-- Expected: 14

-- Gate 3: Appointed officials (Lee III, Taylor) have NO politician_images row
SELECT COUNT(*) FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id IN (-690003, -690004);
-- Expected: 0

-- Gate 4: All 14 URLs point at the Supabase public bucket and use the canonical {politician_id}-headshot.jpg pattern
SELECT COUNT(*) FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id BETWEEN -690021 AND -690001
  AND p.external_id NOT IN (-690003, -690004)
  AND pi.url LIKE 'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/%-headshot.jpg';
-- Expected: 14

-- Gate 5: photo_license must be 'public_domain' or 'cc-by-sa-4.0' (no other values acceptable)
SELECT DISTINCT pi.photo_license
FROM essentials.politician_images pi
JOIN essentials.politicians p ON p.id = pi.politician_id
WHERE p.external_id BETWEEN -690021 AND -690001
  AND p.external_id NOT IN (-690003, -690004);
-- Expected: only 'public_domain' (or 'public_domain' + 'cc-by-sa-4.0' if Wikimedia fallback used)

-- Gate 6: photo_origin_url populated for each of the 14
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -690021 AND -690001
  AND external_id NOT IN (-690003, -690004)
  AND photo_origin_url IS NOT NULL;
-- Expected: 14

-- Gate 7: '232' must NOT be in the ledger (audit-only)
SELECT COUNT(*) FROM supabase_migrations.schema_migrations WHERE version='232';
-- Expected: 0

-- Gate 8: Live HTTP HEAD probe — every politician_images.url returns 200 OK
-- Iterate over each url in Gate 1's result set and assert HTTP 200 (script-based)
    </automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/232_portland_headshots.sql` exists and is wrapped in `BEGIN; ... COMMIT;`
    - File contains exactly 14 `INSERT INTO essentials.politician_images` statements
    - File contains exactly 14 `UPDATE essentials.politicians SET photo_origin_url` statements
    - File contains all 14 elected external_id literals: -690001, -690002, -690010, -690011, -690012, -690013, -690014, -690015, -690016, -690017, -690018, -690019, -690020, -690021
    - File does NOT contain external_id -690003 or -690004 (City Administrator + City Attorney are appointed; excluded per D-14 + CF-1)
    - File contains the literal `type` value `'default'` at least 14 times (once per INSERT); does NOT contain the literal `'headshot'` as a type value (CRITICAL per Pitfall 6)
    - Every INSERT uses `photo_license='public_domain'` or `'cc-by-sa-4.0'`; no other values
    - File contains the substring `kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/` in every INSERT
    - File contains the substring `-headshot.jpg` in every INSERT (canonical Storage path)
    - 14 image files exist in the `politician_photos` Supabase Storage bucket, each named `{politician_id}-headshot.jpg`, each exactly 600x750 pixels JPEG (verify via Storage API HEAD or download a sample)
    - Gate 1 returns 14
    - Gate 2 returns 14 (every row type='default')
    - Gate 3 returns 0 (appointed officials excluded)
    - Gate 4 returns 14 (every URL conforms to Supabase Storage pattern)
    - Gate 5 returns only `public_domain` (or `public_domain` + `cc-by-sa-4.0` if any Wikimedia fallbacks used; the latter must be documented)
    - Gate 6 returns 14 (photo_origin_url populated)
    - Gate 7 returns 0 ('232' NOT in schema_migrations ledger — audit-only pattern)
    - Gate 8: every URL returns HTTP 200 OK
  </acceptance_criteria>
  <done>
    14 elected Portland officials have a 600x750 JPEG headshot stored at `politician_photos/{politician_id}-headshot.jpg`; 14 politician_images rows exist with type='default' and photo_license='public_domain' (or 'cc-by-sa-4.0' for Wikimedia fallbacks, documented); 14 photo_origin_url values populated; audit migration 232 file exists but is NOT in the ledger; all 8 verification gates pass.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes, the following must all be true (this closes roadmap SC-4 for Phase 77):
- 14 image files at `politician_photos/{politician_id}-headshot.jpg` in Supabase Storage, each 600x750 JPEG q90
- 14 essentials.politician_images rows with type='default'
- 14 essentials.politicians rows with non-null photo_origin_url
- 0 politician_images for external_ids -690003 and -690004 (appointed officials excluded)
- 232_portland_headshots.sql exists as audit-only file (NOT in supabase_migrations.schema_migrations)
- Every politician_images.url returns HTTP 200
</verification>

<success_criteria>
A user visiting any of the 14 elected Portland official profile pages sees their headshot rendered. Profile.jsx's `.find(img => img.type === 'default')` filter returns the image for each. Phase 77 SC-4 satisfied.
</success_criteria>

<output>
Create `.planning/phases/077-portland-officials/077-03-SUMMARY.md` documenting:
- Work-list query output (14 rows confirmed)
- Source URL list for each of the 14 officials (portland.gov or Wikimedia fallback)
- Storage upload confirmations (14 files)
- All 8 verification gate outputs
- Note: 232_portland_headshots.sql is AUDIT-ONLY (not in ledger)
- Any photo_license deviations (which Wikimedia fallbacks, if any)
- Phase 77 SUMMARY trigger: all 3 plans complete → phase done
</output>
