---
phase: 185-washco-2026-elections-discovery
plan: 02
type: execute
wave: 2
depends_on: ["185-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/{BASE+1}_seed_washco_2026_race_candidates.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-{BASE+1}.ts
autonomous: true
requirements: [WM-ELEC-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Every confirmed Nov 2026 candidate appears on their race with a real source-URL citation"
    - "No fabricated candidate name exists — unconfirmed races ship with 0 race_candidates"
    - "Reused incumbents point to their existing politician row (no duplicates); new challengers get a new politician row"
    - "New challengers with an available official/campaign photo have a 600×750 (4:5) press_use headshot"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/{BASE+1}_seed_washco_2026_race_candidates.sql"
      provides: "politicians (new challengers) + race_candidates rows with per-row source citations + DO $$ gate"
      contains: "INSERT INTO essentials.race_candidates"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-{BASE+1}.ts"
      provides: "Idempotent smoke-test harness"
      contains: "essentials.race_candidates"
  key_links:
    - from: "essentials.race_candidates.race_id"
      to: "essentials.races.id"
      via: "race resolved by office → OR 2026 General"
      pattern: "rc\\.race_id = r\\.id"
    - from: "essentials.race_candidates.politician_id"
      to: "essentials.politicians.id"
      via: "reuse existing row or external_id lookup for new challengers"
      pattern: "politician_id"
---

<objective>
Ingest the confirmed Nov 2026 candidate slate for the west-metro races seeded in Plan 01 (D-01/D-02):
seed new `essentials.politicians` rows for genuinely-new challengers, wire `essentials.race_candidates`
linkage (party-blind — the table has NO party column) with a mandatory per-row source citation, and add
600×750 (4:5 Lanczos q90, `press_use`, `type='default'`) headshots for new challengers where an
official/campaign photo exists. NO compass stances (deferred).

Purpose: WM-ELEC-01 — a west-metro voter on `/elections` sees WHO is running (0-candidate races are
hidden by `100eda9`, so only candidate-bearing races render). This is the "populate the elections tab"
mandate from `project_or_2026_candidate_gap`.

Output: migration `BASE+1` (BASE from Plan 01's SUMMARY) + smoke script, applied to the LIVE DB,
idempotent, with an embedded gate asserting no NULL politician_id on active rows, no duplicate full_name
within a race, and every ingested candidate carries a citation. Unconfirmed races correctly stay at 0
candidates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-washco-2026-elections-discovery/185-CONTEXT.md
@.planning/phases/185-washco-2026-elections-discovery/185-RESEARCH.md
@.planning/phases/185-washco-2026-elections-discovery/185-PATTERNS.md
@.planning/phases/185-washco-2026-elections-discovery/185-01-SUMMARY.md
@$HOME/.claude/commands/find-headshots.md

<candidate_slate>
Confirmed today (RESEARCH Candidate Slate Table) — 8 candidate-bearing races:
- WashCo Chair (runoff): **Nafisa Fai** + **Pam Treece** — both REUSE existing Phase 175 politician rows + existing headshots. Do NOT create new rows.
- WashCo D4 (runoff): **Steve Callaway** (NEW challenger — needs politician row + headshot) + **Kipperlyn Sinclair** (REUSE her existing Phase 177 Hillsboro Ward-2 row — do NOT duplicate).
- Beaverton Position 1 (runoff): **Rachel Philip** + **Evelyn Kocher** — both NEW (Ballotpedia candidate pages exist; source headshots there).
- Tualatin Mayor: **Octavio Gonzalez** + **Valerie Pratt** — both REUSE existing Phase 179 rows + headshots.
- Tualatin Position 5: **Beth Dittman** — NEW challenger (headshot to source).
- Cornelius Mayor: **Jeffrey C. Dalin** (unopposed) — REUSE Phase 182 row + headshot.
- Cornelius councilor seats: **Edgar Baker**, **Edén López** — REUSE Phase 182 rows + headshots (confirm at Task 1 whether each is unopposed or has a challenger).

Unconfirmed at research time (16 of 25 races) — Hillsboro (3 seats), Tigard (4 seats), Forest Grove
(4 seats), Sherwood (4 seats), Tualatin Pos 1/Pos 3: NO independently-confirmed candidate. Task 1
re-fetches; whatever is NOT confirmed with a citation ships as 0 race_candidates (accepted per D-07).
NEVER fabricate a name (Pitfall 3).
</candidate_slate>

<interfaces>
- `essentials.race_candidates`: `id, race_id (NOT NULL FK), politician_id (nullable), full_name text NOT NULL, first_name, last_name, is_incumbent bool DEFAULT false, candidate_status text DEFAULT 'active', source text, external_id (unique when non-NULL)`. **NO party column** (schema-enforced antipartisan). Idempotency = `NOT EXISTS (race_id, full_name)`, NEVER `ON CONFLICT` (Pitfall 4).
- `essentials.politicians`: new challengers inserted with a negative-sentinel `external_id` in an empty band (verify the band is unused via SELECT first — mirror migration 1110). `politicians.external_id` HAS a unique index → `ON CONFLICT (external_id) DO NOTHING` is correct there ONLY.
- race resolution: resolve `race_id` via the office anchoring from Plan 01 (`JOIN races r ... JOIN offices o ... WHERE the office's current incumbent = the seat`), so each candidate attaches to the correct one of the 25 races.
- Reuse resolution: `politician_id = (SELECT id FROM essentials.politicians WHERE full_name = '<name>')` for the 6 already-seeded reuse candidates — verify each returns exactly one row before insert.
- Headshots: `essentials.politician_images` rows, `press_use`, `type='default'`, canonical `politician_photos/{uuid}-headshot.jpg`, 600×750 via the existing PIL pipeline (4:5 crop FIRST then Lanczos q90). Reuse the most recent per-city template (Cornelius) which documents the RGBA→white-composite step for circular-cutout campaign PNGs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 re-fetch — direct-fetch each unresolved city's elections page for the actual filed candidate slate</name>
  <files>(no files written — records confirmed names + citations for Task 2; sources headshot images to a scratch dir)</files>
  <read_first>
    - 185-RESEARCH.md Candidate Slate Table + Open Question 3 + Discovery Config reachability table (confirmed-live per-city URLs)
    - 185-CONTEXT.md D-01/D-02
  </read_first>
  <action>
    Directly fetch (curl / WebFetch, Chrome-UA if a plain fetch 403s) each unresolved city's own
    confirmed-reachable elections page for the ACTUAL filed Nov 2026 candidate list: Hillsboro
    (filing opened July 6 2026 — check for the now-filed slate for Ward1-A/Ward2-A/Ward3-A), Tigard
    (`tigard-or.gov/your-government/council/election` — the 2 councilors vying for Mayor + the councilor
    seats), Forest Grove (`forestgrove-or.gov/362/Elections`), Sherwood (`sherwoodoregon.gov/elections`),
    Tualatin Positions 1 & 3 (`tualatinoregon.gov/city-council/elections/`). For each candidate found,
    record full_name + the exact citation URL. For Cornelius, confirm whether Baker/López are unopposed
    or have a filed challenger. For any candidate WITH an official/campaign photo, download the source
    image to a scratch dir for Task 2's headshot processing. If a city's filing still has not closed and
    no slate is published, record it as "0 confirmed" — that race ships with 0 candidates. NEVER infer or
    fabricate a name (e.g. do not assume an appointed incumbent will run).
  </action>
  <verify>
    <automated>echo "MANUAL — record confirmed candidate names + citation URLs; verified programmatically by Task 2's DB citation-coverage assertion"</automated>
  </verify>
  <acceptance_criteria>
    - A written list of every confirmed Nov 2026 candidate (full_name + citation URL) per unresolved race, OR an explicit "0 confirmed — filing not closed" note for that race.
    - No candidate recorded without a real citation URL.
    - Source headshot images (where available) saved to a scratch dir with their origin URL noted.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration BASE+1 — politicians (new challengers) + race_candidates + smoke script</name>
  <files>C:/EV-Accounts/backend/migrations/{BASE+1}_seed_washco_2026_race_candidates.sql, C:/EV-Accounts/backend/scripts/_apply-migration-{BASE+1}.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1110_seed_tx_2026_house_candidates.sql (analog — politicians-then-race_candidates order, reuse-vs-new resolution, per-row citation, DO $$ gate)
    - C:/EV-Accounts/backend/scripts/_apply-migration-1111.ts (harness shape)
    - 185-PATTERNS.md race_candidates section + Shared Patterns (per-table idempotency, antipartisan)
    - 185-01-SUMMARY.md (BASE number; confirms races exist to attach to)
  </read_first>
  <action>
    FIRST — re-verify the live migration counter has not drifted since Plan 01 recorded BASE (RESEARCH
    documents the shared counter advancing multiple times per day from parallel workstreams, and this plan
    may run in the same Wave-2 window as Plan 03): run `ls C:/EV-Accounts/backend/migrations | grep -oE
    '^[0-9]+' | sort -n | tail -1` and confirm the live max is still exactly `BASE` (Plan 01's races
    migration is the highest number on disk). If the max is still `BASE`, this migration is `BASE+1`. If
    drift is detected (max > BASE — another workstream landed a number between Plan 01 and now), do NOT
    reuse the recorded `BASE+1`: re-derive this migration's number as live_max + 1 and note the drift in the
    SUMMARY before writing any file. Then:
    Create `{BASE+1}_seed_washco_2026_race_candidates.sql` wrapped in `BEGIN; ... COMMIT;`. Step 1: INSERT
    genuinely-new challengers (Steve Callaway, Rachel Philip, Evelyn Kocher, Beth Dittman, plus any
    Task-1-confirmed names) into essentials.politicians with negative-sentinel external_ids in an empty
    band (SELECT-verify the band is unused first), guarded `ON CONFLICT (external_id) DO NOTHING`. Step 2:
    INSERT INTO essentials.race_candidates (id, race_id, politician_id, full_name, first_name, last_name,
    is_incumbent, candidate_status, source) — resolving `race_id` by the office→incumbent anchoring used
    in Plan 01, and `politician_id` by REUSE lookup (`SELECT id FROM essentials.politicians WHERE
    full_name=...`) for the 6 already-seeded candidates (Fai, Treece, Sinclair, Gonzalez, Pratt, Dalin,
    Baker, López) or by external_id lookup for the new rows. Set `candidate_status='active'`, `is_incumbent`
    true only for a sitting officeholder defending their own seat, and `source` = the real citation URL
    from RESEARCH's slate table or Task 1 (MANDATORY per Pitfall 3 / T-185-02 — every row cites a URL).
    Guard every row with `WHERE NOT EXISTS (SELECT 1 FROM essentials.race_candidates rc WHERE
    rc.race_id=v.race_id AND rc.full_name=v.full_name)` — NEVER `ON CONFLICT` on race_candidates. NO party
    value anywhere (no column exists). NO ledger INSERT (data-seed, 1110 family). Add a `DO $$ ... RAISE
    EXCEPTION` gate asserting: 0 `candidate_status='active'` rows with NULL politician_id, 0 duplicate
    lower(full_name) within any single race, and every ingested row has a non-NULL/non-empty `source`.
    Create the paired `_apply-migration-{BASE+1}.ts` (1111 harness shape). Do NOT git-commit inside
    C:/EV-Accounts.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-{BASE+1}.ts</automated>
  </verify>
  <acceptance_criteria>
    - Before writing any file, the live migration counter was re-checked (`ls ... | grep -oE '^[0-9]+' | sort -n | tail -1`) and confirmed still `BASE`; if drift was found, the migration number was re-derived as live_max+1 and the drift noted in the SUMMARY.
    - Every confirmed candidate from RESEARCH's slate table + Task 1 appears as exactly one race_candidates row on the correct race; the 8 known reuse candidates point to their PRE-EXISTING politician row (no new duplicate politician created for them).
    - 0 active race_candidates rows have NULL politician_id; 0 duplicate full_name within a race; every row's `source` is a non-empty citation URL.
    - Unconfirmed races (Task 1 found no slate) have 0 race_candidates rows — no fabricated names.
    - Smoke script exits 0 on first apply AND on immediate re-run (idempotent). No ledger row written.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Headshots for new challengers — 600×750 (4:5) press_use images where a source photo exists</name>
  <files>C:/EV-Accounts/backend/migrations/{BASE+1}_seed_washco_2026_race_candidates.sql (append politician_images INSERT), scratch headshot-processing script</files>
  <read_first>
    - @$HOME/.claude/commands/find-headshots.md (600×750 4:5 Lanczos q90 pipeline)
    - C:/EV-Accounts/backend/migrations/1196_cornelius_city_council.sql (most recent headshot INSERT + RGBA→white-composite note for circular-cutout campaign PNGs)
    - project memory project_headshot_skill / feedback_headshot_* rules (crop only, eyes ~1/3 from top, no distortion, no graphics)
  </read_first>
  <action>
    For each NEW challenger created in Task 2 that has an available official/campaign photo (Callaway,
    Philip, Kocher, Dittman, plus any Task-1-confirmed with a source image): process the source image
    through the existing pipeline — crop to 4:5 FIRST (never stretch; eyes ~1/3 from top; head+shoulders;
    for circular-cutout PNG sources composite onto white per the Cornelius RGBA step), then resize to
    600×750 Lanczos q90, upload to `politician_photos/{uuid}-headshot.jpg`, and INSERT the
    `essentials.politician_images` row (`press_use`, `type='default'`, `photo_origin_url` = the source URL).
    Reused candidates (Fai/Treece/Sinclair/Gonzalez/Pratt/Dalin/Baker/López) already have headshots — do
    NOT re-process or duplicate them. Where no source photo exists for a new challenger, leave them
    headshot-less (honest gap — do not fabricate or upscale a tiny thumbnail beyond acceptability). Guard
    the INSERTs idempotently (NOT EXISTS on the politician_id + type, or ON CONFLICT if a unique index
    exists — verify at plan time). Do NOT git-commit inside C:/EV-Accounts.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-{BASE+1}.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each new challenger WITH an available source photo has exactly one `politician_images` row (`press_use`, `type='default'`, `photo_origin_url` set) and a reachable `politician_photos/{uuid}-headshot.jpg` at 600×750.
    - No headshot is duplicated for the 8 reused candidates.
    - `SELECT COUNT(*) FROM essentials.politician_images WHERE ...` for the new challengers matches the count of new challengers that had a source photo (no phantom rows, no missing rows for available-photo candidates).
    - Headshot visual identity/quality spot-check is deferred to the phase-level manual UAT (VALIDATION manual-only table).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external city/Ballotpedia pages → race_candidates | Public-record candidate data crosses into the DB; risk is fabricated/uncited names |
| migration → LIVE production DB | Every write is live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-02 | Tampering (data integrity) | Fabricated or guessed candidate injected into race_candidates without a citation | mitigate | Every row carries a mandatory `source` URL; post-verify gate asserts non-empty source on every ingested row; Task 1 re-fetch requires a citation before a name is recorded; unconfirmed races ship 0 candidates rather than a guess |
| T-185-01-DB | Tampering | Partial/duplicate writes to politicians / race_candidates on live DB | mitigate | `BEGIN;/COMMIT;` + `NOT EXISTS` (race_candidates) / `ON CONFLICT (external_id)` (politicians) idempotency + `DO $$ RAISE EXCEPTION` gate (0 NULL politician_id, 0 dup full_name) |
| T-185-03-DUP | Tampering | Duplicate politician row created for an already-seeded incumbent (Sinclair, Fai, etc.) | mitigate | Reuse candidates resolved by `full_name` lookup against existing rows; no new politicians row for the 8 reuse candidates; smoke-test verifies |
| T-185-04-COLL | Tampering | Migration-number collision with a parallel workstream on the shared LIVE DB | mitigate | Task 2 re-verifies the live counter (`ls ... | sort -n | tail -1`) still equals `BASE` immediately before writing `{BASE+1}`; re-derives as live_max+1 on drift |
| T-185-SC | Tampering | npm/pip/cargo installs | accept | No package installs — existing PIL pipeline + tsx harness; N/A |
</threat_model>

<verification>
- Smoke script `_apply-migration-{BASE+1}.ts` exits 0 twice.
- Live DB: every confirmed candidate on the correct race with a citation; 0 NULL politician_id on active rows; 0 dup full_name per race; 8 reuse candidates share existing rows.
- New-challenger headshots present at 600×750 where a source existed.
- Manual UAT (phase gate): candidate names + headshots render on `/elections` for a west-metro address.
</verification>

<success_criteria>
The confirmed Nov 2026 west-metro candidate slate is ingested with per-row citations and no fabricated
names; reused incumbents share their existing politician rows; new challengers with a source photo have a
600×750 press_use headshot; unconfirmed races correctly carry 0 candidates.
</success_criteria>

<output>
Create `.planning/phases/185-washco-2026-elections-discovery/185-02-SUMMARY.md` when done — record which
races got candidates, which shipped 0 (and why), and which challengers got/lacked a headshot.
</output>
</content>
