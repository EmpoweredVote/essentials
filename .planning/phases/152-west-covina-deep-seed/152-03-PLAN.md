---
phase: 152-west-covina-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["152-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1012_west_covina_headshots.sql
autonomous: false
requirements: [WCOV-01]

must_haves:
  truths:
    - "the exact image gap set is determined by a pre-flight image-coverage + dimension query over the 5 current members (Gutierrez D1, Lopez-Viado D2 Mayor, Diaz D3, Cantos D4 Mayor Pro Tem, Wu D5), NOT hardcoded; CONTEXT confirms all 5 ALREADY have 1 politician_images row each (dimensions UNVERIFIED) — Wave 3 is VERIFY-AND-FIX (no greenfield member); the pre-flight re-confirms which images exist + their dimensions at apply time"
    - "westcovina.gov has NO WAF (RESEARCH §D-04 — all 5 documentIDs returned HTTP 200 via direct curl) — portraits are sourced by DIRECT curl from https://www.westcovina.gov/ImageRepository/Document?documentID=NNNN (capital-D documentID; westcovina.org 301-redirects to westcovina.gov — fetch the .gov directly). Confirmed documentIDs: 1053=Gutierrez(D1), 1054=Lopez-Viado(D2), 1056=Diaz(D3), 1052=Cantos(D4), 1055=Wu(D5)"
    - "⚠ ALL city portraits are LOW-RES (RESEARCH §D-04: JPEGs 4.6-7.3 KB; Wu 1055 is a 147×190 PNG) — none is near 600×750. For each member: verify the existing image is the correct person + no superimposed text/graphics + exactly 600×750 (4:5). KEEP good ones as-is; re-source ONLY those that fail. Where the city portrait is too low-res, prefer a HIGHER-RES fallback (Cantos has a high-res RespectAbility/ABA portrait; otherwise Ballotpedia/SCAG/Wikimedia/campaign FB) rather than a soft upscale"
    - "every uploaded/re-cropped portrait is the correct verified person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 FIRST (never stretched) then resized 600×750 Lanczos q90 JPEG, uploaded to Supabase Storage politician_photos at the canonical path {uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), x-upsert:true; politician_images type='default', photo_license matching the REAL source (press_use for the official city portrait), photo_origin_url set to the documentID URL (or the real fallback source URL)"
    - "no fabricated photos — where no acceptable verified portrait is obtainable for a member, an honest gap is documented rather than uploading a bad/wrong-person/overlaid image as type='default' (CONTEXT D-04 honest-gap rule); fallbacks per RESEARCH §D-04 only if the official portrait is unusable"
    - "full-roster image audit confirms each of the 5 current members has exactly one type='default' row (or a documented honest gap), with no member having >1"
    - "migration 1012 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 1011; on-disk counter authoritative); the migration file is COMMITTED to the EV-Accounts repo (feedback_no_git_in_ev_accounts)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1012_west_covina_headshots.sql"
      provides: "Audit-only headshot INSERT/UPDATE (5 current members) + photo_origin_url backfill + full-roster image-coverage audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos ({uuid}-headshot.jpg objects)"
      to: "essentials.politician_images.url (type='default')"
      via: "UPDATE the existing politician_images row for a re-cropped image (or INSERT guarded NOT EXISTS if a member somehow has 0)"
      pattern: "'default'"
---

<objective>
Give the current 5-member West Covina roster (D1 Gutierrez, D2 Lopez-Viado Mayor, D3 Diaz, D4 Cantos Mayor Pro
Tem, D5 Wu) clean, correct official headshots and confirm single canonical-image coverage. After Plan 02 the
survivor chamber 12c9360a holds all 5 with consistent bidirectional links + the Mayor/Mayor-Pro-Tem titles.
CONTEXT confirms all 5 ALREADY have 1 politician_images row each (dimensions UNVERIFIED) -> this is a
VERIFY-AND-FIX wave (no greenfield member, unlike El Monte's Cortez). The EXACT gap set is re-confirmed by a
pre-flight image-coverage + dimension query at apply time.

⚠️ westcovina.gov has NO WAF (RESEARCH §D-04 — confirmed 2026-06-21: all 5 documentIDs returned HTTP 200 via
direct curl). Unlike Downey/Glendale/Pomona (WAF-403, operator in-browser), West Covina portraits are sourced by
DIRECT curl from the ImageRepository, the same CivicEngage pattern as El Monte/Pasadena/Palmdale. CRITICAL: use
the canonical host westcovina.gov (the URL param is capital-D documentID; westcovina.org 301-redirects to
westcovina.gov — fetch the .gov directly to avoid redirect surprises). The /find-headshots skill
(~/.claude/commands/find-headshots.md) remains available if any official portrait is unusable.

Confirmed documentIDs (RESEARCH §D-04 — all HTTP 200):
  Gutierrez (D1):    https://www.westcovina.gov/ImageRepository/Document?documentID=1053  (JPEG 7,276 B)
  Lopez-Viado (D2):  documentID=1054  (JPEG 4,635 B; Mayor)
  Diaz (D3):         documentID=1056  (JPEG 5,880 B)
  Cantos (D4):       documentID=1052  (JPEG 6,418 B; Mayor Pro Tem)
  Wu (D5):           documentID=1055  (PNG 147×190, 63,821 B)

⚠️ ALL city portraits are LOW-RES (RESEARCH §D-04 quality warning) — the JPEGs are 4.6-7.3 KB thumbnails and Wu's
is a 147×190 PNG; none is near 600×750. Upscaling from these will be soft. For each member:
- Verify the existing politician_images row is the correct named person + has no superimposed text/graphics + is
  exactly 600×750 (4:5). KEEP good ones as-is.
- For any that FAIL (wrong person / wrong dimensions / low quality / overlay): re-source. Where the city portrait
  is too low-res, PREFER a higher-res fallback over a soft upscale — Cantos is a nationally-prominent disability
  advocate (RespectAbility board chair / ABA) with likely high-res portraits; otherwise Ballotpedia / SCAG /
  Wikimedia / campaign FB (RESEARCH §D-04 fallback table). Only fall back to the soft city-thumbnail upscale if no
  higher-res source exists.

Processing (NON-NEGOTIABLE per project memory) for any portrait being uploaded/re-cropped: visually confirm the
CORRECT named person and NO superimposed text/graphics -> crop to 4:5 FIRST (never stretch — crop only, eyes
~1/3 from top, full head+shoulders) -> resize 600×750 Lanczos q90 JPEG (Python Pillow) -> upload to the Supabase
Storage politician_photos bucket at the canonical path {uuid}-headshot.jpg using SUPABASE_SERVICE_ROLE_KEY
(C:/EV-Accounts/backend/.env) with header x-upsert:true -> UPDATE (for a re-cropped pre-existing row) or INSERT
(guarded NOT EXISTS, only if a member somehow has 0) politician_images (type='default', photo_license matching
the REAL source — press_use for the official city portrait); set photo_origin_url to the documentID URL (or the
real fallback source URL). Reject any source with superimposed text/graphics over the face — document a gap
honestly rather than upload a bad photo (CONTEXT D-04). Resolve each member's UUID by external_id at apply time.

Purpose: A voter sees the right face next to each official. Output: Migration 1012 (audit-only — NOT registered
in schema_migrations) committed to EV-Accounts + verified portraits for the 5 current members (kept or
re-sourced/re-cropped) + a human checkpoint confirming the correct person + correct crop for each; any genuine
gap documented honestly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/152-west-covina-deep-seed/152-RESEARCH.md
@.planning/phases/152-west-covina-deep-seed/152-CONTEXT.md
@.planning/phases/152-west-covina-deep-seed/152-VALIDATION.md
@.planning/phases/152-west-covina-deep-seed/152-02-SUMMARY.md
@.planning/phases/151-el-monte-deep-seed/151-03-PLAN.md
@.planning/phases/149-pasadena-deep-seed/149-03-PLAN.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §D-04). westcovina.gov is NO-WAF — direct curl for all 5.
     Pre-flight re-confirms the gap set + dimensions; /find-headshots skill available for fallback. -->

Current 5 members (resolve UUID by external_id; pre-flight a coverage + dimension query):
  Brian Gutierrez -201108 (D1) · Letty Lopez-Viado 687361 (D2, Mayor) · Rosario Diaz -201107 (D3) ·
  Ollie Cantos 687365 (D4, Mayor Pro Tem) · Tony Wu 687367 (D5)
  (all 5 pre-existing have 1 image each, dimensions UNVERIFIED — VERIFY-AND-FIX, no greenfield member.)

Source plan (RESEARCH §D-04; westcovina.gov NO-WAF -> DIRECT curl for all 5; capital-D documentID; use .gov not .org):
  Gutierrez   DIRECT curl: https://www.westcovina.gov/ImageRepository/Document?documentID=1053  (JPEG 7,276 B — LOW-RES)
  Lopez-Viado DIRECT curl: documentID=1054  (JPEG 4,635 B — LOW-RES; Mayor)
  Diaz        DIRECT curl: documentID=1056  (JPEG 5,880 B — LOW-RES)
  Cantos      DIRECT curl: documentID=1052  (JPEG 6,418 B — LOW-RES; Mayor Pro Tem; PREFER high-res RespectAbility/ABA fallback)
  Wu          DIRECT curl: documentID=1055  (PNG 147×190 — LOW-RES)
  Fallbacks (RESEARCH §D-04, only if the official portrait is unusable / too low-res for an acceptable 600×750):
    Lopez-Viado facebook.com/LettyLopezWestCovina (campaign) / Ballotpedia; Cantos RespectAbility.org leadership /
    ABA Commission on Disability Rights / news (HIGH-RES — prefer over the 6.4 KB city thumbnail); Diaz Ballotpedia
    / FB; Wu Ballotpedia / FB; Gutierrez Ballotpedia / FB. (use /find-headshots if needed.)

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face

Storage + DB:
  - Bucket: politician_photos · Path: canonical {uuid}-headshot.jpg
  - Upload via SUPABASE_SERVICE_ROLE_KEY (C:/EV-Accounts/backend/.env), header x-upsert:true
  - politician_images: type='default', photo_license='press_use' (official city portrait) or the real fallback license
  - UPDATE politicians.photo_origin_url to the documentID URL (or fallback source URL) where NULL
  - For a pre-existing-but-good image: keep the existing row as-is (no-op). For a re-cropped pre-existing image:
    UPDATE the existing row's url + photo_license. (INSERT guarded WHERE NOT EXISTS on politician_id only if a
    member somehow has 0.) Each member ends with exactly ONE type='default' row -> {uuid}-headshot.jpg.

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql). It does NOT register in
schema_migrations (same convention as El Monte 1002 / Pasadena 948 / Downey 993). On-disk counter authoritative;
ledger stays at 1011 (Plan 02). The FILE still must be committed to EV-Accounts (feedback_no_git_in_ev_accounts).

Pattern migration: C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql (NO-WAF direct-curl audit-only
INSERT/UPDATE + photo_origin_url backfill + Storage URL shape + full-roster audit + ledger-unchanged check — the
exact NO-WAF West Covina precedent) and 948_pasadena_headshots.sql (the other NO-WAF direct-curl audit-only
shape). /find-headshots skill: ~/.claude/commands/find-headshots.md. Project memory: feedback_headshot_no_graphics,
feedback_headshot_image_sizing, feedback_headshot_cropping, feedback_headshot_resize_no_distort.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight image-coverage + dimension check; verify the 5 pre-existing portraits (all via DIRECT curl from westcovina.gov NO-WAF documentIDs); re-source low-res with high-res fallback (esp. Cantos); re-crop only failures; crop 4:5 -> 600×750 -> upload canonical</name>
  <files>C:/EV-Accounts/backend/migrations/1012_west_covina_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql (NO-WAF direct-curl audit-only INSERT/UPDATE + download/crop/upload pipeline + Storage URL shape + full-roster audit + ledger-unchanged check — the exact West Covina precedent)
    - C:/EV-Accounts/backend/migrations/948_pasadena_headshots.sql (the other NO-WAF direct-curl audit-only shape)
    - .planning/phases/151-el-monte-deep-seed/151-03-PLAN.md (the NO-WAF direct-curl + pre-flight-coverage + crop/resize + checkpoint shape this plan mirrors)
    - .planning/phases/149-pasadena-deep-seed/149-03-PLAN.md (the verify-identity-before-upload + low-res-source + honest-gap + crop discipline shape)
    - .planning/phases/152-west-covina-deep-seed/152-RESEARCH.md (§D-04 headshot sources/WAF status incl. all 5 documentIDs + LOW-RES warning + per-member fallbacks [Cantos high-res RespectAbility/ABA], §Open Questions 2/3, §Assumptions A2)
    - .planning/phases/152-west-covina-deep-seed/152-02-SUMMARY.md (final 5-member link state + Mayor/Mayor-Pro-Tem seats)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots — 4:5 crop FIRST -> 600×750 Lanczos q90)
    - ~/.claude/commands/find-headshots.md (skill for finding a current portrait if a source is unusable)
  </read_first>
  <action>
    First run a pre-flight image-coverage + dimension query: for the 5 current members (-201108 Gutierrez, 687361
    Lopez-Viado, -201107 Diaz, 687365 Cantos, 687367 Wu), LEFT JOIN essentials.politician_images (type='default')
    and record which have an image + the stored dimensions/url (all 5 have 1 each — re-confirm at apply time). For
    each of the 5 pre-existing images, fetch/inspect the current image and verify: correct named person + no
    superimposed text/graphics + exactly 600×750 (4:5). KEEP any that pass (no-op). For any that FAIL: source by
    DIRECT curl from westcovina.gov (e.g. curl -L
    "https://www.westcovina.gov/ImageRepository/Document?documentID=1053" -o gutierrez.jpg — capital-D documentID;
    use .gov not .org). documentIDs: 1053 Gutierrez / 1054 Lopez-Viado / 1056 Diaz / 1052 Cantos / 1055 Wu.
    ⚠ ALL city portraits are LOW-RES (4.6-7.3 KB; Wu is 147×190) — where the city portrait cannot produce an
    acceptable 600×750, PREFER a higher-res fallback (RESEARCH §D-04): Cantos via RespectAbility.org / ABA / news
    (high-res likely — prefer over the 6.4 KB thumbnail); Lopez-Viado/Diaz/Wu/Gutierrez via Ballotpedia / SCAG /
    Wikimedia / campaign FB; or the /find-headshots skill. For EACH sourced portrait, visually confirm it is the
    correct named person and has NO superimposed text/graphics. Then crop to 4:5 (crop only — never stretch; eyes
    ~1/3 from top, full head+shoulders), resize to 600×750 Lanczos q90 JPEG (Pillow), resolve each UUID by
    external_id, and upload to politician_photos at {uuid}-headshot.jpg via SUPABASE_SERVICE_ROLE_KEY
    (C:/EV-Accounts/backend/.env) with x-upsert:true. Record each public Storage URL + source (documentID URL or
    fallback URL) + the photo_license per member for Task 2, AND record which of the 5 pre-existing were KEPT vs
    re-cropped/re-sourced. If no acceptable verified portrait is obtainable for a member, document the genuine gap
    (do NOT upload a wrong-person/overlaid image as type='default' — CONTEXT D-04).
  </action>
  <verify>
    <automated>For each re-sourced/re-cropped member the processed file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('{member}.jpg').size)"` -> (600, 750)) and the uploaded Storage object at {uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight image-coverage + dimension query run; all 5 pre-existing images + their dimensions re-confirmed from live data (verify-and-fix, no greenfield member)
    - All sourcing via DIRECT curl from westcovina.gov (NO-WAF; capital-D documentID; .gov not .org); documentIDs 1052-1056 used per member
    - Each of the 5 pre-existing images verified for correct person + no overlay + 600×750; passing ones KEPT, failing/low-res ones re-sourced (high-res fallback preferred over soft upscale, esp. Cantos via RespectAbility/ABA)
    - Each uploaded/re-cropped portrait is the correct verified person; no superimposed text/graphics; processed to exactly 600×750, 4:5, not stretched, eyes ~1/3 from top; uploaded to {uuid}-headshot.jpg (HTTP 200)
    - Public Storage URL + source (documentID or fallback URL) + photo_license recorded for Task 2 per uploaded member; KEEP-vs-re-source status recorded for the 5 pre-existing
    - If any member has no acceptable portrait, an honest gap documented (NOT a wrong-person/overlaid image faked as type='default')
  </acceptance_criteria>
  <done>The 5 current members have verified portraits (the 5 pre-existing kept or re-sourced/re-cropped from their documentIDs via direct curl, or a high-res fallback where the city thumbnail was too low-res), each confirmed correct + clean, processed to 600×750 4:5, uploaded to Storage at {uuid}-headshot.jpg; Storage URLs + sources + licenses + KEEP/re-source status recorded for Task 2; any genuine gap documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 1012 (audit-only) — INSERT/UPDATE politician_images + photo_origin_url backfill + full-roster image audit; confirm ledger unchanged; COMMIT the file to EV-Accounts</name>
  <files>C:/EV-Accounts/backend/migrations/1012_west_covina_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql (INSERT/UPDATE politician_images + photo_origin_url backfill + audit-only apply, ledger-unchanged check, full-roster coverage audit, documented-gap handling)
    - .planning/phases/152-west-covina-deep-seed/152-RESEARCH.md (§D-04 headshot audit, §Validation Architecture WCOV-01i, §Assumptions A2)
    - .planning/phases/152-west-covina-deep-seed/152-02-SUMMARY.md (final link state + Mayor/Mayor-Pro-Tem seats)
    - ~/.claude/commands/find-headshots.md (skill reference)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/1012_west_covina_headshots.sql: for any of the 5 members that were
    re-sourced/re-cropped in Task 1, write the politician_images change (resolved by external_id) url = the
    Storage public URL, type='default', photo_license matching the real source (press_use for the official city
    portrait), via an UPDATE of the existing row's url/photo_license (each of the 5 already has a row) — or an
    INSERT guarded WHERE NOT EXISTS on politician_id only if a member somehow has 0 (each member ends with exactly
    one canonical type='default' row pointing at {uuid}-headshot.jpg); leave KEPT pre-existing rows untouched
    (no-op). UPDATE essentials.politicians SET photo_origin_url = the documentID URL (or fallback source URL)
    WHERE external_id=<id> AND photo_origin_url IS NULL. Apply via raw SQL (mcp__supabase-local execute_sql) — do
    NOT run through the schema_migrations ledger (audit-only). Then run the full-roster coverage audit: LEFT JOIN
    the 5 current West Covina officials (-201108, 687361, -201107, 687365, 687367) to politician_images
    (type='default') and report who has exactly one type='default' row vs a documented gap; confirm no member has
    >1. Verify MAX(version) in schema_migrations is UNCHANGED by this file (stays 1011). Then COMMIT the file to
    EV-Accounts: git -C "C:/EV-Accounts" add backend/migrations/1012_west_covina_headshots.sql && git -C
    "C:/EV-Accounts" commit -m "feat(152): West Covina headshots — verify-and-fix 5 members, audit-only"
    (feedback_no_git_in_ev_accounts).
  </action>
  <verify>
    <automated>mcp__supabase-local execute_sql "SELECT p.external_id, COUNT(pi.*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (-201108,687361,-201107,687365,687367) GROUP BY p.external_id;" shows each member with COUNT 0 (documented gap) or 1 (no member has >1) AND "SELECT MAX(version::int) FROM supabase_migrations.schema_migrations;" is unchanged (stays 1011). Also: git -C "C:/EV-Accounts" log --oneline -1 -- backend/migrations/1012_west_covina_headshots.sql shows the commit</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 current members with a verified portrait has exactly one type='default' row at the canonical {uuid}-headshot.jpg path (kept or re-sourced); photo_license matches the real source (press_use for official city portraits); photo_origin_url populated with the documentID URL or fallback source URL
    - No member has >1 type='default' row; members with no obtainable verified portrait documented as honest gaps (not faked)
    - schema_migrations MAX(version) unchanged by migration 1012 (audit-only confirmed; stays 1011)
    - Migration is idempotent (re-apply = 0 rows changed)
    - Migration file 1012_west_covina_headshots.sql committed to the EV-Accounts repo
  </acceptance_criteria>
  <done>1012 applied (audit-only) + committed to EV-Accounts; the 5 current members each have exactly one default image (or a documented honest gap); kept-or-re-sourced reconciled; photo_license matches the real source; no member >1; ledger untouched (stays 1011).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify every West Covina headshot (correct person for all 5, clean 4:5 crop, no overlays, acceptable resolution given the low-res city sources)</name>
  <action>Pause for the user to visually verify each West Covina portrait per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>Verified official headshots for the 5 current West Covina members (D1 Gutierrez, D2 Lopez-Viado Mayor, D3 Diaz, D4 Cantos Mayor Pro Tem, D5 Wu): the 5 pre-existing images verified and kept-or-re-sourced from their documentIDs (1052-1056, all direct curl, NO-WAF) — with a high-res fallback preferred where the city thumbnail was too low-res (esp. Cantos via RespectAbility/ABA); all uploaded ones processed to 600×750 4:5 and live in Supabase Storage + politician_images (type='default', photo_license matching the real source). Any honest gaps documented.</what-built>
  <how-to-verify>
    1. Confirm each member's photo is the CORRECT person (cross-check all 5 against westcovina.gov/172 + /177 council pages).
    2. Confirm Mayor Lopez-Viado (D2) and Mayor Pro Tem Cantos (D4) photos are the current portraits; Cantos should be a clean (ideally high-res RespectAbility/ABA) portrait, not a soft 6 KB upscale.
    3. Confirm no superimposed text/graphics over any face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    4. Given the city sources are LOW-RES (4-7 KB; Wu a 147×190 PNG), confirm each final image is acceptably sharp — if any is unacceptably soft, name it for a higher-res re-source.
    5. Confirm each member shows exactly one canonical image (no duplicate type='default' rows) on the West Covina browse view (https://essentials.empowered.vote/results?browse_geo_id=0684200&browse_mtfcc=G4110). Any documented gap is genuinely imageless (not faked).
    6. Confirm the on-disk counter is authoritative: schema_migrations MAX(version) is unchanged (still 1011; the headshot file did not register) and 1012 is committed to EV-Accounts.
  </how-to-verify>
  <resume-signal>Type "approved" or name which photo(s) need re-sourcing / re-cropping / identity-fix.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with a NO-WAF official image source. No untrusted user input reaches the DB — uploads +
image-row writes use service-role/operator against production. Threat surface = wrong-person photo (a stale
pre-existing image being the wrong person), a distorted/overlaid photo, an unacceptably-soft upscale of a low-res
city thumbnail (all 5 sources are 4-7 KB / 147×190), a fabricated/campaign photo filling a real gap, a mislabeled
photo_license, fetching from westcovina.org instead of .gov, or accidental ledger registration — mitigated by the
pre-flight coverage + dimension check, the identity-verify-before-upload rule, the NO-WAF direct-curl from
westcovina.gov, the prefer-high-res-fallback-over-soft-upscale rule (esp. Cantos), the crop-then-resize/no-stretch
rule, the license matched to the real source, a blocking human-verify checkpoint that checks each of the 5 +
resolution acceptability, the honest-gap rule, and the audit-only raw-SQL apply.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| westcovina.gov (NO-WAF) | Official source — direct curl HTTP 200; use westcovina.gov (capital-D documentID) NOT westcovina.org; each image human-verified before upload. |
| Ballotpedia / RespectAbility / ABA / campaign / /find-headshots -> local processing | Higher-res fallback sources (preferred where the city thumbnail is too low-res); all human-verified before upload. |
| operator -> Supabase Storage + production DB | Uploads + image-row INSERT/UPDATE against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01 | Tampering | Idempotent guarded writes — duplicate image row for any member; audit-only migration accidentally registered in schema_migrations | mitigate | Re-cropped pre-existing handled via UPDATE; INSERT guarded WHERE NOT EXISTS only if a member has 0; KEPT rows untouched; post-audit confirms ≤1 type='default' per official; apply via raw SQL only, verify MAX(version) unchanged (stays 1011) |
| T-152-03 | Spoofing | Identity-verified images — wrong-person photo (a stale pre-existing image is the wrong person); fetching from westcovina.org not .gov; an unacceptably-soft upscale of a 4-7 KB city thumbnail | mitigate | Identity-verify each of the 5 before keep/upload; direct curl from westcovina.gov documentIDs (1052-1056, capital-D); prefer high-res fallback over soft upscale (esp. Cantos RespectAbility/ABA); blocking human-verify checks each of the 5 + resolution acceptability |
| T-152-03b | Tampering | Image stretched / aspect-distorted / text-overlaid; fabricated/wrong campaign photo filling a gap; mislabeled photo_license | mitigate | Crop-to-4:5-FIRST-then-resize rule + 600×750 dimension check; reject superimposed-graphics sources; honest-gap rule (CONTEXT D-04) — never seed a wrong-person/overlaid photo as type='default'; photo_license matched to the real source (press_use for official city portrait) |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | No package installs (existing Pillow + Supabase client; RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After 1012 + checkpoint:
1. each current member with a verified portrait has exactly one type='default' image at {uuid}-headshot.jpg; photo_origin_url (documentID or fallback URL) + photo_license (matching the real source) set
2. full-roster coverage: every member is either imaged (exactly one default) or a documented honest gap; no member has >1
3. each photo is the correct person (all 5 cross-checked against westcovina.gov); Cantos is a clean (ideally high-res) portrait, not a soft upscale
4. schema_migrations MAX(version) unchanged by 1012 (stays 1011); 1012 committed to EV-Accounts
5. human checkpoint approved (correct person each, no overlays, correct crop, acceptable resolution)
</verification>

<success_criteria>
Every current West Covina member with an obtainable correct photo has a single 600×750 verified image at the
canonical path; the 5 pre-existing verified and kept-or-re-sourced (all NO-WAF direct curl, high-res fallback
preferred over soft upscale where the city thumbnail was too low-res, esp. Cantos); no wrong-person/overlaid/
stretched photos; photo_license matched to the real source; genuine gaps documented honestly; the ledger counter
is preserved (stays 1011) and the file committed to EV-Accounts.
</success_criteria>

<output>
Create `.planning/phases/152-west-covina-deep-seed/152-03-SUMMARY.md` when done (record any honest image gaps, which of the 5 pre-existing were KEPT vs re-sourced/re-cropped, and the per-member source/license).
</output>
</content>
