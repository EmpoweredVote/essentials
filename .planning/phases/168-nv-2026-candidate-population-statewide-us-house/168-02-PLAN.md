---
phase: 168-nv-2026-candidate-population-statewide-us-house
plan: 02
type: execute
wave: 2
depends_on: ["168-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_find-headshots-nv-candidates.ts
autonomous: false
requirements: [NV-CAND-01]

must_haves:
  truths:
    - "Every NV 2026 challenger left politician_id=NULL by Plan 01 has either a fetched headshot OR an honest-skip recorded (D-03)"
    - "Fetched headshots follow project rules: 600×750 (4:5, Lanczos, q90), crop-not-stretch, eyes ~⅓ from top, no superimposed text/graphics over the face"
    - "A fetched challenger's photo surfaces on their /elections card (via race_candidates.photo_url OR a created politician record's image with race_candidates.politician_id linked)"
    - "Candidates already linked to an existing NV politician record (incumbents + resolved cross-office) need NO headshot work — they already render a photo"
    - "Each headshot subject is researched one at a time (per project memory feedback_stance_research_one_at_a_time — preserves source availability)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_find-headshots-nv-candidates.ts"
      provides: "per-candidate photo_url / politician-record-link updates produced by the find-headshots flow (gitignored helper)"
  key_links:
    - from: "essentials.race_candidates.photo_url (or linked politician's image)"
      to: "ElectionsView.jsx candidate card"
      via: "withCandidatePhotoFallback COALESCE(rc.photo_url, pi.url)"
      pattern: "photo_url"
---

<objective>
Fetch headshots for the NV 2026 general-election challengers that Plan 01 left `politician_id=NULL`,
using the interactive `find-headshots` flow (D-03 — user chose fetch-now). Where no usable
official/Ballotpedia photo exists, record an honest-skip rather than forcing a low-quality image
(CA/TX/NY precedent). Candidates already linked to an existing NV politician record (incumbents +
resolved cross-office Ford/Cannizzaro/Buck) are SKIPPED — they already render a photo.

Purpose: A candidate card on `/elections` reads `COALESCE(rc.photo_url, pi.url)` — without this
plan, every new challenger renders with no photo. This plan gives the real ballot faces.
Output: Per-challenger `race_candidates.photo_url` set (and/or a minimal politician record created +
linked via `race_candidates.politician_id`), with mirrored 600×750 images in Supabase Storage; plus
an honest-skip ledger in the SUMMARY for any un-findable photo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-CONTEXT.md
@.planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-RESEARCH.md
@.planning/phases/168-01-SUMMARY.md
@~/.claude/commands/find-headshots.md

<interfaces>
<!-- find-headshots handles: web search → user approval → PIL 4:5 crop → 600×750 Lanczos q90 →
     Supabase Storage upload → politician_images row + politicians.photo_origin_url. For a
     candidate with no politician record it creates a minimal record (gen_random_uuid, no
     external_id) then links via race_candidates.politician_id UPDATE. (168-RESEARCH §"Headshot Mechanics") -->

Challengers needing headshots (those with politician_id = NULL after Plan 01 — CONFIRM the exact
list against the Plan 01 SUMMARY, since Cannizzaro/Buck/Ford may have resolved to existing records):
  Statewide: Sandra Jauregui, Adriana Guzmán Fralick, Jim Marchant, Tya Mathis-Coleman,
             Drew Johnson, Michael MacDougall
  US House:  David Flippo, Teresa Benitez-Thompson, Lynn Chapman, Marty O'Donnell, Cody Whipple
             (+ Carrie Buck ONLY IF Plan 01 left her NULL)

Per-candidate source leads in 168-RESEARCH.md §"Headshot Sources by Candidate".
Project headshot rules: feedback_headshot_image_sizing (600×750), feedback_headshot_cropping
(eyes ~⅓, crop not change-ratio), feedback_headshot_resize_no_distort (crop to 4:5 FIRST then
resize), feedback_headshot_no_graphics (no superimposed text/banners over face).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch challenger headshots one at a time via find-headshots (honest-skip where none)</name>
  <files>C:/EV-Accounts/backend/scripts/_find-headshots-nv-candidates.ts</files>
  <read_first>
    - .planning/phases/168-01-SUMMARY.md (the authoritative list of which candidates ended politician_id=NULL — only those need work)
    - ~/.claude/commands/find-headshots.md (the skill: search → approval → PIL crop/resize → Storage upload → record create/link)
    - .planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-RESEARCH.md (§"Headshot Mechanics", §"Headshots Sources by Candidate", §"Challengers Needing Headshots")
  </read_first>
  <action>
    First read the Plan 01 SUMMARY and SELECT the actual NULL-politician_id NV 2026 challengers from
    the live DB — only those need photos (incumbents + any resolved cross-office links already render
    a photo and are SKIPPED). For each NULL challenger, run the find-headshots flow ONE AT A TIME
    (per project memory feedback_stance_research_one_at_a_time — parallel burns rate limit / loses
    source context): search the per-candidate source from RESEARCH (legislative .gov for Jauregui;
    Ballotpedia / campaign for the rest), navigate, extract the best official/Ballotpedia/campaign
    photo. Reject any image with superimposed text/graphics over the face (feedback_headshot_no_graphics).
    Process each accepted photo per project rules: crop to 4:5 FIRST, THEN resize to 600×750 Lanczos
    q90 (never stretch/change ratio); eyes ~⅓ from top; full head + shoulders. Upload to Supabase
    Storage (politician_photos bucket, service-role key in C:/EV-Accounts/backend/.env) and either set
    race_candidates.photo_url to the Storage URL, OR (for a notable figure warranting a profile) create
    a minimal politician record (gen_random_uuid, no external_id, per the skill's create_politician_record
    guard) + politician_images row, then UPDATE race_candidates.politician_id to link it.

    Where NO usable photo exists from an official/Ballotpedia/campaign source (expected for thin
    challengers like Cody Whipple), DO NOT force a low-quality image — record an honest-skip
    (leave photo_url NULL; the card shows initials/placeholder). Track imaged-vs-skipped counts for
    the SUMMARY in the CA/TX/NY "{N} imaged, {M} honest-skips" format.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT rc.full_name, (rc.photo_url IS NOT NULL OR pi.url IS NOT NULL) AS has_photo FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.elections e ON e.id=r.election_id LEFT JOIN essentials.politicians p ON p.id=rc.politician_id LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id WHERE e.name='NV 2026 Statewide General' ORDER BY has_photo, rc.full_name;"</automated>
  </verify>
  <acceptance_criteria>
    - Every NULL-politician_id challenger from the Plan 01 SUMMARY appears in the query output with has_photo = true (fetched) OR is explicitly listed as an honest-skip in the SUMMARY.
    - Every fetched image is 600×750 (4:5), produced by crop-then-resize (no distortion), eyes ~⅓ from top, no superimposed graphics — spot-confirmed at the find-headshots approval gate.
    - Already-linked candidates (incumbents + resolved cross-office) are NOT re-processed.
    - SUMMARY records the imaged-vs-honest-skip tally and the per-candidate source URL used.
  </acceptance_criteria>
  <done>Every NV 2026 challenger has a project-compliant headshot or a documented honest-skip; fetched photos surface on the candidate card via photo_url or a linked politician record.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify NV 2026 candidate cards + headshot crops on live /elections</name>
  <what-built>
    Headshots fetched (or honest-skips recorded) for the NV 2026 general-election challengers,
    seeded in Plan 01. Candidate cards on /elections now render the real ballot faces.
  </what-built>
  <how-to-verify>
    1. Open `essentials.empowered.vote/elections` with a Las Vegas, NV street address (or use the
       statewide browse link `?browse_state_officials=NV` if applicable).
    2. Confirm the Governor of Nevada race shows candidates (Lombardo + Ford) — no longer
       "No candidates have filed".
    3. Spot-check each of the 10 races (6 statewide + NV-01..04): candidates appear; incumbents and
       linked cross-office candidates show photos; fetched challengers show their new headshot;
       honest-skip challengers show initials/placeholder (acceptable).
    4. On 2-3 fetched challenger cards confirm the crop quality: 4:5 ratio, eyes ~⅓ from top, full
       head + shoulders, NO superimposed text/banner over the face, not stretched.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (wrong person, bad crop, missing photo to retry, or a candidate that should be honest-skipped instead)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| find-headshots → external image source | fetches images from official .gov / Ballotpedia / campaign sites |
| operator → production DB + Storage | photo_url / politician record updates + Storage upload (service-role key) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168H-IMG | Tampering | fetched headshot image | mitigate | Mandatory user-approval gate in find-headshots (no silent import); identity-confirm at approval; reject superimposed-graphics images per project rule. |
| T-168H-WRONG | Spoofing | wrong-person photo | mitigate | Human-verify checkpoint confirms identity per card before phase close (CA/TX/NY wrong-person precedent, e.g. stale PCE photos). |
| T-168H-SECRET | Information Disclosure | service-role key | accept | Key already lives in C:/EV-Accounts/backend/.env (gitignored); no new exposure; upload helper is gitignored (_*). |
| T-168H-AUTH | Elevation | (none) | n/a | No new endpoint/auth surface; Storage write uses existing service-role flow. |
</threat_model>

<verification>
- Live-DB query shows every Plan-01 NULL challenger either has a photo or is an honest-skip in the SUMMARY.
- Human-verify checkpoint approved on the live `/elections` page (Governor not blank; crops compliant).
</verification>

<success_criteria>
- All NV 2026 challengers have a 600×750 project-compliant headshot OR a documented honest-skip (D-03).
- Fetched photos render on the candidate cards; incumbents/cross-office already render (no rework).
- Honest-skips recorded in the "{N} imaged, {M} honest-skips" format; no forced low-quality images.
</success_criteria>

<output>
Create `.planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-02-SUMMARY.md` when done
(commit to the essentials repo). Any DB-write helper under C:/EV-Accounts/backend/scripts/_* is
gitignored; the durable artifacts are the DB rows + Storage images (production). Provide the operator a
live browse link to spot-check.
</output>
