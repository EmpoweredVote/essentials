---
phase: 79-or-landing-elections-discovery
plan: 5
type: execute
wave: 4
depends_on: [79-01, 79-02, 79-03, 79-04]
files_modified:
  - C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql
autonomous: true
requirements: []
tags: [oregon, portland, discovery, jurisdictions, migration, final-verification]

must_haves:
  truths:
    - "essentials.discovery_jurisdictions contains exactly 2 OR rows: one for OR statewide (geo_id='41', election_date='2026-11-03') and one for Portland (geo_id='4159000', election_date='2026-11-03')"
    - "Both OR discovery rows have election_date within 180 days of the discovery cron run date, so the next Sunday cron sweep will include them"
    - "Total OR race count is exactly 105 (8 statewide + 90 legislative + 7 Portland) confirmed in final verification"
    - "Section-split check returns 0 orphaned geofence_boundaries after the entire phase"
    - "All Phase 79 ROADMAP.md success criteria (6) are satisfied"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql"
      provides: "2 discovery_jurisdictions rows armed for Portland + OR statewide"
      contains: "State of Oregon"
  key_links:
    - from: "discovery_jurisdictions OR statewide row"
      to: "discoveryCron.ts election_date window sweep"
      via: "election_date='2026-11-03' (157 days from 2026-05-30 — within 180-day horizon)"
      pattern: "election_date <= horizon"
    - from: "discovery_jurisdictions Portland row"
      to: "portland.gov/auditor/elections source URL"
      via: "source_url with allowed_domains array"
      pattern: "portland.gov"
---

<objective>
Arm the discovery pipeline for OR by inserting 2 discovery_jurisdictions rows (one for OR statewide, one for Portland city), and execute the final phase-level verification (race counts, section-split check, Landing.jsx, ROADMAP success criteria).

Purpose: Implements D-08 (Portland discovery row), D-09 (sequential processing rule — set by row insertion only, no flag), D-10 (Portland + OR statewide rows; ON CONFLICT NOT NULL fix via WHERE NOT EXISTS per Pitfall 7), and D-11 (race_candidates remain empty — discovery fills them).

CRITICAL: The `cron_active` column does NOT exist (per RESEARCH.md schema finding and critical_research_findings #2). The cron sweeps rows by `election_date` within a 180-day horizon. Simply inserting a row with `election_date='2026-11-03'` (157 days from 2026-05-30) is sufficient to arm discovery.

Output:
- One migration 241_or_discovery_jurisdictions.sql applied to live Supabase production DB.
- 2 new rows in essentials.discovery_jurisdictions.
- Final phase-level verification report documenting all 6 ROADMAP success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-or-landing-elections-discovery/79-CONTEXT.md
@.planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md
@.planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md
@.planning/phases/79-or-landing-elections-discovery/79-01-SUMMARY.md
@.planning/phases/79-or-landing-elections-discovery/79-02-SUMMARY.md
@.planning/phases/79-or-landing-elections-discovery/79-03-SUMMARY.md
@.planning/phases/79-or-landing-elections-discovery/79-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 241_or_discovery_jurisdictions.sql (D-08, D-10)</name>
  <files>C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql</files>
  <read_first>
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (sections "Discovery Jurisdictions", "Pitfall 2: cron_active Column Does Not Exist", "Pitfall 7: No CONCAT or ON CONFLICT for discovery_jurisdictions", "OR Statewide Discovery Source URL", "Portland Discovery Source URL")
    - .planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md (section "migrations/241_or_discovery_jurisdictions.sql")
  </read_first>
  <action>
    Create a new file at exactly `C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql` (UTF-8 NoBOM via Write tool). Migration 241 is next-available after 240. Add header comments: `-- Migration 241: OR Discovery Jurisdictions — Phase 79 Plan 05`, `-- D-10: Two rows — OR statewide (FIPS 41) + Portland city (geo_id 4159000)`, `-- Cron arms rows automatically via election_date window (no cron_active column exists)`.

    Write 2 INSERT statements, each using the `WHERE NOT EXISTS` idempotency guard pattern from RESEARCH.md "Discovery Jurisdictions" section (do NOT use ON CONFLICT — Pitfall 7: table has no unique constraint beyond primary key, so ON CONFLICT DO NOTHING is silently accepted but does not prevent duplicates).

    Row 1 (OR Statewide):
    ```sql
    INSERT INTO essentials.discovery_jurisdictions
      (id, jurisdiction_geoid, jurisdiction_name, state, election_date, source_url, allowed_domains)
    SELECT
      gen_random_uuid(), '41', 'State of Oregon', 'OR', '2026-11-03',
      'https://sos.oregon.gov/elections/Pages/Candidate-Filings-Local-Measures.aspx',
      ARRAY['sos.oregon.gov', 'oregonlegislature.gov', 'ballotpedia.org']
    WHERE NOT EXISTS (
      SELECT 1 FROM essentials.discovery_jurisdictions
      WHERE jurisdiction_geoid = '41' AND election_date = '2026-11-03'
    );
    ```

    Row 2 (Portland City):
    ```sql
    INSERT INTO essentials.discovery_jurisdictions
      (id, jurisdiction_geoid, jurisdiction_name, state, election_date, source_url, allowed_domains)
    SELECT
      gen_random_uuid(), '4159000', 'City of Portland, Oregon', 'OR', '2026-11-03',
      'https://www.portland.gov/auditor/elections',
      ARRAY['portland.gov', 'multco.us', 'ballotpedia.org']
    WHERE NOT EXISTS (
      SELECT 1 FROM essentials.discovery_jurisdictions
      WHERE jurisdiction_geoid = '4159000' AND election_date = '2026-11-03'
    );
    ```

    Column order: `id, jurisdiction_geoid, jurisdiction_name, state, election_date, source_url, allowed_domains` per RESEARCH.md confirmed schema. Do NOT include `cron_active` (does not exist — Pitfall 2). Do NOT use `ON CONFLICT ... DO NOTHING` (Pitfall 7 — use WHERE NOT EXISTS instead).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql';const b=fs.readFileSync(p);if(b[0]===0xEF&&b[1]===0xBB&&b[2]===0xBF){console.error('FAIL: BOM');process.exit(1);}const s=b.toString('utf8');const must=[\"'41'\",\"'4159000'\",\"'State of Oregon'\",\"'City of Portland, Oregon'\",\"'2026-11-03'\",'sos.oregon.gov/elections/Pages/Candidate-Filings-Local-Measures.aspx','portland.gov/auditor/elections','ARRAY[','WHERE NOT EXISTS','jurisdiction_geoid = ''41''','jurisdiction_geoid = ''4159000'''];for(const t of must){if(!s.includes(t))throw new Error('missing: '+t);}const forbid=['cron_active','ON CONFLICT'];for(const t of forbid){if(s.includes(t))throw new Error('forbidden: '+t);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql`
    - File is UTF-8 without BOM
    - File contains both jurisdiction_geoid literals: `'41'` and `'4159000'`
    - File contains both jurisdiction_name literals: `'State of Oregon'` and `'City of Portland, Oregon'`
    - File contains the election_date literal `'2026-11-03'` (used twice)
    - File contains both source_url strings: `sos.oregon.gov/elections/Pages/Candidate-Filings-Local-Measures.aspx` and `portland.gov/auditor/elections`
    - File contains `ARRAY[` (allowed_domains is a text array)
    - File contains `WHERE NOT EXISTS` (correct idempotency guard per Pitfall 7)
    - File does NOT contain `cron_active` (column does not exist — Pitfall 2)
    - File does NOT contain `ON CONFLICT` (would be silently no-op per Pitfall 7)
  </acceptance_criteria>
  <done>Migration 241 file exists, NoBOM, contains 2 discovery_jurisdictions INSERTs with WHERE NOT EXISTS guards and correct schema (no cron_active, no ON CONFLICT).</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 241 and verify discovery_jurisdictions rows (D-08, D-10)</name>
  <files>C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql (apply only)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/241_or_discovery_jurisdictions.sql (the file written in Task 1)
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (section "Validation Architecture — After Plan 05", "Cron sweep mechanics")
  </read_first>
  <action>
    Apply migration 241 to live Supabase production DB via mcp__supabase-local__execute_sql. After applying, run these verifications:
    1. Row count: `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='OR';` — expected 2.
    2. Per-row spot check: `SELECT jurisdiction_geoid, jurisdiction_name, election_date, source_url FROM essentials.discovery_jurisdictions WHERE state='OR' ORDER BY jurisdiction_geoid;` — expected 2 rows with the exact values from Task 1 (geo_ids '41' and '4159000', election_date 2026-11-03, source URLs as specified).
    3. allowed_domains array check: `SELECT jurisdiction_geoid, allowed_domains FROM essentials.discovery_jurisdictions WHERE state='OR';` — confirm `'sos.oregon.gov'` is in the '41' row's array and `'portland.gov'` is in the '4159000' row's array.
    4. Cron sweep eligibility: `SELECT jurisdiction_geoid, election_date, (election_date - CURRENT_DATE) AS days_until FROM essentials.discovery_jurisdictions WHERE state='OR';` — confirm both rows show days_until between 1 and 180 (within cron horizon, per RESEARCH.md "Cron sweep mechanics").
    5. Idempotency: re-execute migration 241 — row count still 2, no error.
    6. Schema sanity: confirm `cron_active` column was NOT created by anything — `SELECT column_name FROM information_schema.columns WHERE table_schema='essentials' AND table_name='discovery_jurisdictions';` — column list must not include `cron_active`.
  </action>
  <verify>
    <automated>echo "Manual MCP verification — see acceptance_criteria for explicit queries"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='OR'` returns exactly 2
    - `SELECT jurisdiction_geoid FROM essentials.discovery_jurisdictions WHERE state='OR' ORDER BY jurisdiction_geoid` returns the two values: `41`, `4159000`
    - The '41' row has jurisdiction_name='State of Oregon', election_date='2026-11-03', source_url contains 'sos.oregon.gov'
    - The '4159000' row has jurisdiction_name='City of Portland, Oregon', election_date='2026-11-03', source_url contains 'portland.gov/auditor'
    - Both rows have non-empty `allowed_domains` arrays; '41' row's array contains 'sos.oregon.gov'; '4159000' row's array contains 'portland.gov'
    - Both rows have `election_date - CURRENT_DATE` between 1 and 180 days (cron-eligible)
    - Re-applying migration 241 returns no error and row count remains 2 (WHERE NOT EXISTS guard worked)
    - `information_schema.columns` for `essentials.discovery_jurisdictions` does NOT include a `cron_active` column
  </acceptance_criteria>
  <done>Migration 241 applied; 2 OR discovery rows present; both cron-eligible by date window; idempotency confirmed; cron_active confirmed absent.</done>
</task>

<task type="auto">
  <name>Task 3: Final phase verification — all 6 ROADMAP success criteria</name>
  <files>(verification only — no file modification; produces SUMMARY content)</files>
  <read_first>
    - .planning/ROADMAP.md (read Phase 79 success criteria — 6 items)
    - .planning/phases/79-or-landing-elections-discovery/79-01-SUMMARY.md
    - .planning/phases/79-or-landing-elections-discovery/79-02-SUMMARY.md
    - .planning/phases/79-or-landing-elections-discovery/79-03-SUMMARY.md
    - .planning/phases/79-or-landing-elections-discovery/79-04-SUMMARY.md
    - src/pages/Landing.jsx (re-check Portland Oregon entry survived all 5 plans)
  </read_first>
  <action>
    Run a final phase-level verification covering every Phase 79 ROADMAP.md success criterion (6 items). For each criterion, run the corresponding SQL or code check via mcp__supabase-local__execute_sql or file inspection, and record the result:

    SC-1 — `Landing.jsx COVERAGE_AREAS includes Portland, OR with correct browseGovernmentList ID (geo_id='4159000')`:
    Verify by reading src/pages/Landing.jsx and confirming the Portland Oregon entry from Plan 01 is still present with the regex match used in Plan 01 Task 1 verify.

    SC-2 — `OR 2026 primary (May 19) and general (November 3) election rows exist in essentials.elections`:
    `SELECT name, election_date FROM essentials.elections WHERE state='OR' ORDER BY election_date;` — expected 2 rows.

    SC-3 — `OR Governor 2026 race exists with SOS-verified candidates and discovery armed`:
    `SELECT position_name, office_id FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name='Governor of Oregon';` — expected 1 row.
    Note for SUMMARY: race_candidates are empty by design (D-11) — discovery cron fills them after next Sunday run.

    SC-4 — `OR US House 2026 race rows exist for all 6 CDs with discovery armed`:
    `SELECT position_name FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'U.S. House OR-%' ORDER BY position_name;` — expected 6 rows: OR-01 through OR-06.

    SC-5 — `Portland city council 2026 race rows exist (if any seats are up) with discovery armed`:
    `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'Portland %';` — expected 7 (D3×3 + D4×3 + Auditor — per RESEARCH.md correction of D-07).

    SC-6 — `discovery_jurisdictions rows exist for Portland with cron_active=true`:
    Refined per RESEARCH.md schema finding: cron_active column does not exist; "armed" means `election_date` within 180 days. Run `SELECT jurisdiction_geoid, election_date, (election_date - CURRENT_DATE) AS days_until FROM essentials.discovery_jurisdictions WHERE state='OR';` — expected 2 rows with days_until <= 180.

    Then run two final cross-cutting checks:
    - Total OR race count: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR';` — expected 105.
    - Final section-split: `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.state='41' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts WHERE state IN ('or','OR','41'));` — expected 0.

    Document each SC pass/fail in the phase SUMMARY with the SQL result.
  </action>
  <verify>
    <automated>echo "Manual MCP verification — see acceptance_criteria for the 6 ROADMAP SC + 2 cross-cutting checks"</automated>
  </verify>
  <acceptance_criteria>
    - SC-1 PASS: src/pages/Landing.jsx still contains Portland Oregon entry matching `county:\s*'Portland',\s*state:\s*'Oregon',\s*browseGovernmentList:\s*\[\s*'4159000'\s*\],\s*browseStateAbbrev:\s*'OR'`
    - SC-2 PASS: essentials.elections has exactly 2 OR rows (Primary 2026-05-19 + General 2026-11-03)
    - SC-3 PASS: Governor of Oregon race row exists in essentials.races linked to OR 2026 General with non-null office_id; race_candidates noted as empty by design (D-11)
    - SC-4 PASS: 6 US House OR race rows exist for OR-01 through OR-06
    - SC-5 PASS: Exactly 7 Portland race rows exist (D3 Seats A/B/C + D4 Seats A/B/C + Auditor) — corrects D-07 expectation per RESEARCH.md
    - SC-6 PASS (refined): 2 OR discovery_jurisdictions rows exist with `election_date - CURRENT_DATE` between 1 and 180 days (cron-eligible); cron_active column confirmed not to exist
    - Cross-cutting: total OR race count = exactly 105 (8 statewide + 90 legislative + 7 Portland)
    - Cross-cutting: final section-split check returns 0 orphaned geofence_boundaries
    - SUMMARY document records pass/fail for each SC with the exact SQL output
    - SUMMARY explicitly notes that D-07 was corrected per research (Portland D3/D4/Auditor ARE on 2026 ballot) and the discovery_jurisdictions schema does not include cron_active
  </acceptance_criteria>
  <done>All 6 Phase 79 ROADMAP success criteria verified and documented; total OR race count is 105; section-split is 0; phase ready to mark complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → live Supabase production DB | Migration 241 applied via mcp__supabase-local__execute_sql |
| discovery_jurisdictions row → discoveryCron.ts | Inserted rows are swept on Sunday cron run if election_date within horizon; rows trigger Claude discovery agent runs that fetch external URLs in allowed_domains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-05-01 | Tampering | discovery_jurisdictions inserts | mitigate | WHERE NOT EXISTS idempotency guard per Pitfall 7 (NOT ON CONFLICT, which is silently no-op on this table); explicit duplicate-key check on (jurisdiction_geoid, election_date) |
| T-79-05-02 | Information Disclosure | source URLs | accept | All URLs are public records sites (sos.oregon.gov, portland.gov, ballotpedia.org) |
| T-79-05-03 | Tampering | scope creep (cron_active column inclusion) | mitigate | Task 1 verify command explicitly forbids `cron_active` literal; SQL would error if column referenced |
| T-79-05-04 | Spoofing | allowed_domains array | mitigate | Domain allowlist prevents discovery agent from fetching arbitrary URLs; downstream discoveryService.ts honors this list |
| T-79-05-05 | Repudiation | final SC verification | accept | All 6 ROADMAP success criteria verified with explicit SQL queries documented in SUMMARY |

Pure data migration. No new auth, no new API endpoints, no user input handling. Discovery cron is pre-existing infrastructure (Phase 7).
</threat_model>

<verification>
1. essentials.discovery_jurisdictions has exactly 2 OR rows (geo_ids '41' + '4159000')
2. Both rows have election_date='2026-11-03' (within 180-day cron horizon)
3. Idempotency confirmed on re-run
4. cron_active column does not exist in schema
5. All 6 ROADMAP SC verified and documented
6. Total OR race count = 105
7. Section-split check = 0
</verification>

<success_criteria>
- Phase 79 complete: Landing, elections, races, discovery all in place
- Discovery cron will sweep Portland + OR statewide on next Sunday run
- All 6 Phase 79 ROADMAP success criteria pass
- Phase ready to close; ROADMAP.md status updated to Complete
</success_criteria>

<output>
Create `.planning/phases/79-or-landing-elections-discovery/79-05-SUMMARY.md` documenting:
- 2 OR discovery_jurisdictions rows confirmed (geo_id, jurisdiction_name, election_date, days_until_election)
- Each of the 6 ROADMAP SC pass/fail status with SQL output
- Final total OR race count (expected 105)
- Final section-split check result (expected 0)
- Note that D-07 was corrected per research — Portland D3/D4/Auditor ARE on 2026 ballot
- Note that cron_active column does not exist; armed via election_date window
- Migration 241 ledger entry confirmation
- Phase 79 closeout readiness statement
</output>
