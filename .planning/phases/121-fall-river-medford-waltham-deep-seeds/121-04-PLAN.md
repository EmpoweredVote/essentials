---
phase: 121-fall-river-medford-waltham-deep-seeds
plan: 04
type: execute
wave: 2
depends_on:
  - 121-02
files_modified:
  - C:/EV-Accounts/backend/migrations/593_medford_school_committee.sql
autonomous: true
requirements:
  - MEDFORD-01

must_haves:
  truths:
    - "G5420 geofence row exists for Medford school district geo_id (LEAID prefix), mtfcc='G5420', state='25'"
    - "essentials.governments row exists for 'Medford Public Schools, Massachusetts, US'"
    - "Medford School Committee chamber exists under the Medford Public Schools government"
    - "SCHOOL district exists for Medford LEAID geo_id, district_type='SCHOOL', state='ma'"
    - "All elected SC members seeded (verify seat count from medfordschools.org)"
    - "Mayor Lungo-Koehn ex-officio office row exists in SCHOOL district (title='Mayor (ex officio)')"
    - "Mayor's office_id still points to LOCAL_EXEC district from migration 591 (not overwritten)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/593_medford_school_committee.sql"
      provides: "Medford School Committee seed — elected members + Mayor ex-officio + G5420 geofence"
      contains: "593_medford_school_committee"
  key_links:
    - from: "essentials.politicians (external_id=-2540115001, Mayor Lungo-Koehn)"
      to: "essentials.offices (district_type=SCHOOL, Medford LEAID geo_id)"
      via: "CROSS JOIN subquery on existing politician row (no new INSERT)"
      pattern: "SELECT id FROM essentials.politicians WHERE external_id = -2540115001"
    - from: "SC back-fill UPDATE"
      to: "essentials.politicians (SC external_id range)"
      via: "BETWEEN clause MUST exclude -2540115001 (Mayor)"
      pattern: "external_id BETWEEN -<LEAID>NNN AND -<LEAID>001"
---

<objective>
Seed Medford School Committee: G5420 geofence + government + all elected SC members + Mayor Lungo-Koehn as ex-officio Chair.

Purpose: Completes MEDFORD-01 — a Medford address also returns a SCHOOL section with elected School Committee members. Follows the Newton 579 single-ex-officio pattern exactly.
Output: Migration 593 applied; G5420 geofence + elected SC politicians + school offices inserted; Mayor ex-officio linked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-fall-river-medford-waltham-deep-seeds/121-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research Medford School Committee structure and LEAID</name>
  <files></files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/585_lynn_school_committee.sql (primary analog — single ex-officio Mayor pattern; G5420 geofence INSERT; back-fill exclusion of Mayor external_id; all post-verification gates)
    - C:/EV-Accounts/backend/migrations/579_newton_school_committee.sql (secondary analog — elected member structure)
    - .planning/STATE.md (MA LEAID pattern, G5420 geofence pattern, SCHOOL district_type rules)
    - .planning/phases/121-fall-river-medford-waltham-deep-seeds/121-02-SUMMARY.md (confirms Mayor external_id=-2540115001 exists from migration 591)
  </read_first>
  <action>
Research and verify before writing the migration:

STEP 1 — Find Medford LEAID (NCES Local Education Agency ID):
Medford Public Schools LEAID is expected to be 2506600 (MA FIPS=25, NCES district code 06600).
Verify using this query:
SELECT geo_id, mtfcc, state FROM essentials.geofence_boundaries WHERE geo_id LIKE '250660%' AND mtfcc = 'G5420';
(This checks if a prior phase accidentally inserted it already — expect 0 rows.)

If the LEAID is unknown, check: https://nces.ed.gov/ccd/districtsearch/ → search Massachusetts → Medford → find the 7-digit NCES LEAID.
The geo_id will be the NCES LEAID as a string: e.g., '2506600'.

STEP 2 — Confirm external_id range is clear:
SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2506600010 AND -2506600001;
(Expected 0 rows; adjust bounds to match expected SC seat count)

STEP 3 — Verify Mayor Lungo-Koehn exists (from migration 591):
SELECT id, external_id, full_name FROM essentials.politicians WHERE external_id = -2540115001;
(Expected: 1 row, full_name='Breanna Lungo-Koehn')

STEP 4 — Research SC roster from https://www.medfordschools.org/o/mps/page/school-committee:
Medford School Committee: verify current seat count and member names.
Expected structure: 5 or 7 elected at-large members + Mayor as ex-officio.
Document confirmed names, seat count, and whether Lungo-Koehn is confirmed as ex-officio Chair.

Record all findings for Task 2.
  </action>
  <verify>
    <automated>SELECT id, external_id, full_name FROM essentials.politicians WHERE external_id = -2540115001; -- Expected: 1 row (Mayor Lungo-Koehn from migration 591)</automated>
  </verify>
  <acceptance_criteria>
    - Medford LEAID confirmed (e.g., '2506600' or verified actual value)
    - External_id range for SC members confirmed clear (0 rows)
    - Mayor Lungo-Koehn exists in DB (external_id=-2540115001, from migration 591)
    - SC seat count and current member names confirmed from medfordschools.org
    - Mayor confirmed as ex-officio School Committee Chair (standard Medford charter pattern)
  </acceptance_criteria>
  <done>Medford SC structure verified; LEAID confirmed; Mayor exists in DB; roster ready for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration 593_medford_school_committee.sql</name>
  <files>C:/EV-Accounts/backend/migrations/593_medford_school_committee.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/585_lynn_school_committee.sql (primary analog — copy all structural patterns exactly: pre-flight DO blocks, G5420 geofence INSERT, government INSERT, chamber INSERT, SCHOOL district INSERT, elected member CTE blocks, Mayor ex-officio CROSS JOIN block, back-fill UPDATE with Mayor exclusion, post-verification DO block with Gate i)
    - Task 1 output (confirmed LEAID, SC roster, SC seat count)
  </read_first>
  <action>
Create C:/EV-Accounts/backend/migrations/593_medford_school_committee.sql by adapting 585_lynn_school_committee.sql with Medford-specific values.

MEDFORD SC FACTS (use Task 1 verified values; defaults below):
- LEAID geo_id: '2506600' (verify in Task 1; this is the NCES LEAID for Medford Public Schools)
- Government name: 'Medford Public Schools, Massachusetts, US'
- Chamber: name='School Committee', name_formal='Medford School Committee' — no slug column (GENERATED ALWAYS)
- SCHOOL district: district_type='SCHOOL', state='ma', geo_id='<LEAID>', label='Medford Public Schools', mtfcc='G5420'
- Mayor external_id: -2540115001 (Breanna Lungo-Koehn, from migration 591 — confirmed in Task 1)
- Elected SC members: use Task 1 verified roster; assign external_ids -<LEAID>001 through -<LEAID>N (alphabetical by last name)
  All elected members: title='School Committee Member', is_appointed_position=false
- Mayor ex-officio: title='Mayor (ex officio)', NOT a new politician INSERT — CROSS JOIN on existing external_id=-2540115001
- Migration ledger version: '593'
- Autocommit pattern: NO BEGIN/COMMIT wrapper (same as 585 which follows 579 Newton pattern)

Migration header:
-- Migration 593: Medford School Committee (MEDFORD-01 — SC portion)
-- geo_id: '<LEAID>' (G5420, NCES LEAID for Medford Public Schools)
-- N elected at-large members + Mayor Lungo-Koehn as ex-officio Chair
-- Mayor external_id=-2540115001 (from migration 591) — NOT re-inserted
-- CRITICAL: back-fill UPDATE must exclude -2540115001 to preserve LOCAL_EXEC office_id

Pre-flight blocks (3 — copy from 585):
- Block 1: RAISE EXCEPTION if government 'Medford Public Schools, Massachusetts, US' already exists
- Block 2: RAISE EXCEPTION if external_id range -<LEAID>N..-<LEAID>001 is not empty
- Block 3: RAISE EXCEPTION if Mayor Lungo-Koehn (external_id=-2540115001) does NOT exist (missing = migration 591 not applied)

G5420 geofence INSERT (copy exact pattern from 585 — state='25', not 'ma' or 'MA'):
INSERT INTO essentials.geofence_boundaries (geo_id, mtfcc, state)
SELECT '<LEAID>', 'G5420', '25'
WHERE NOT EXISTS (SELECT 1 FROM essentials.geofence_boundaries WHERE geo_id = '<LEAID>' AND mtfcc = 'G5420');

Medford Public Schools government INSERT:
- state='MA', type='LOCAL', city=NULL (school district — no city value), geo_id='<LEAID>'

School Committee chamber INSERT: no slug column.

SCHOOL district INSERT:
- district_type='SCHOOL', state='ma', geo_id='<LEAID>', label='Medford Public Schools', mtfcc='G5420'

Elected SC member blocks (use WITH ins_p CTE pattern from 585):
- N blocks, one per confirmed SC member; title='School Committee Member' for all
- All: party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true
- ON CONFLICT (external_id) DO NOTHING

Mayor ex-officio office block (CRITICAL — no new politician INSERT):
INSERT INTO essentials.offices (id, politician_id, district_id, title, is_appointed_position, is_vacant)
SELECT gen_random_uuid(), p.id, d.id, 'Mayor (ex officio)', false, false
FROM (SELECT id FROM essentials.politicians WHERE external_id = -2540115001) p
CROSS JOIN (SELECT id FROM essentials.districts WHERE geo_id = '<LEAID>' AND district_type = 'SCHOOL' AND state = 'ma') d
WHERE NOT EXISTS (
  SELECT 1 FROM essentials.offices o2
  WHERE o2.politician_id = p.id AND o2.district_id = d.id
);

Office_id back-fill UPDATE (CRITICAL — exclude Mayor external_id):
UPDATE essentials.politicians p SET office_id = o.id
FROM essentials.offices o WHERE o.politician_id = p.id
  AND p.external_id BETWEEN -(<LEAID>::bigint * 1000 + N) AND -(<LEAID>::bigint * 1000 + 1)
  AND p.office_id IS NULL;
(Range covers ONLY SC members, NOT -2540115001)

Post-verification DO block (9 gates — copy from 585, substituting Medford values):
- Gate (a): gov count = 1 (Medford Public Schools)
- Gate (b): chambers count = 1 (School Committee under Medford PS gov)
- Gate (c): districts count = 1 (SCHOOL, LEAID geo_id, state='ma')
- Gate (d): sc_politicians count = N (BETWEEN -<LEAID>N AND -<LEAID>001)
- Gate (e): total_school_offices count = N+1 (N elected + 1 ex-officio Mayor)
- Gate (f): split_orphans = 0 (G5420 geofence has matching SCHOOL district)
- Gate (g): null_sc_office_ids = 0 (SC members only — excludes Mayor)
- Gate (h): G5420 geofence present = 1 (geo_id='<LEAID>', mtfcc='G5420')
- Gate (i): Mayor's office_id still LOCAL_EXEC = 1 (external_id=-2540115001, d.district_type='LOCAL_EXEC', d.geo_id='2540115') — RAISE EXCEPTION if 0

Migration ledger: INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('593') ON CONFLICT (version) DO NOTHING;
(No COMMIT — autocommit pattern)

After writing, apply via mcp__supabase-local__execute_sql.
  </action>
  <verify>
    <automated>Run migration 593 via mcp__supabase-local__execute_sql. Post-verification DO block must emit: 'Migration 593 post-verification PASSED' with all 9 gates.</automated>
  </verify>
  <acceptance_criteria>
    - SQL file C:/EV-Accounts/backend/migrations/593_medford_school_committee.sql exists
    - Migration applies without errors
    - Post-verification PASSED notice with all 9 gates
    - Query: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -<LEAID>N AND -<LEAID>001 → N (elected SC count)
    - Query: SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = '<LEAID>' AND d.district_type = 'SCHOOL' AND d.state = 'ma' → N+1 (includes ex-officio)
    - Query: SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id = '<LEAID>' AND mtfcc = 'G5420' → 1
    - Query (Gate i — CRITICAL): SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.id = p.office_id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id = -2540115001 AND d.district_type = 'LOCAL_EXEC' AND d.geo_id = '2540115' → 1 (Mayor's city office_id NOT overwritten)
    - Query: SELECT o.title FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id JOIN essentials.politicians p ON p.id = o.politician_id WHERE d.geo_id = '<LEAID>' AND d.district_type = 'SCHOOL' AND p.external_id = -2540115001 → 'Mayor (ex officio)'
    - Section-split (school): SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id = '<LEAID>' AND gb.mtfcc = 'G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.district_type = 'SCHOOL' AND d.state = 'ma') → 0
    - MEDFORD-01 fully satisfied: Medford address returns LOCAL section (Mayor + City Council) + SCHOOL section (elected SC + Mayor ex-officio)
  </acceptance_criteria>
  <done>Migration 593 applied; Medford SC elected members + Mayor ex-officio seeded; G5420 geofence present; Mayor's LOCAL_EXEC office_id intact; all 9 gates PASSED; MEDFORD-01 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → Supabase production DB | Idempotent INSERT statements; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-04-01 | Tampering | Mayor office_id (back-fill overwrite) | mitigate | Back-fill UPDATE range restricted to SC member external_ids only; Gate (i) RAISES EXCEPTION if overwrite occurred |
| T-121-04-02 | Tampering | politicians table (Mayor re-insert) | mitigate | No WITH ins_p CTE for Mayor block; CROSS JOIN on existing external_id=-2540115001 only |
| T-121-04-03 | Tampering | Schema migrations ledger | mitigate | ON CONFLICT (version) DO NOTHING |
| T-121-04-04 | Tampering | G5420 geofence INSERT | mitigate | WHERE NOT EXISTS guard on (geo_id, mtfcc) prevents duplicate geofence row |
| T-121-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs; SQL-only migration |
</threat_model>

<verification>
After applying migration 593:

1. SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -<LEAID>N AND -<LEAID>001; -- Expected: N (elected SC)
2. SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE d.geo_id = '<LEAID>' AND d.district_type = 'SCHOOL' AND d.state = 'ma'; -- Expected: N+1
3. SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id = '<LEAID>' AND mtfcc = 'G5420'; -- Expected: 1
4. Gate (i): SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.id = p.office_id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id = -2540115001 AND d.district_type = 'LOCAL_EXEC' AND d.geo_id = '2540115'; -- Expected: 1
5. Section-split (school): SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.geo_id = '<LEAID>' AND gb.mtfcc = 'G5420' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id = gb.geo_id AND d.district_type = 'SCHOOL' AND d.state = 'ma'); -- Expected: 0
</verification>

<success_criteria>
- Migration 593 applied cleanly; 9 post-verification gates PASSED
- N elected Medford SC members seeded
- Mayor Lungo-Koehn has ex-officio SCHOOL office with title='Mayor (ex officio)'
- Mayor's office_id still points to LOCAL_EXEC district (Gate i = 1)
- G5420 geofence present for Medford LEAID geo_id
- MEDFORD-01 fully closed: city gov (591) + SC (593)
</success_criteria>

<output>
Create `.planning/phases/121-fall-river-medford-waltham-deep-seeds/121-04-SUMMARY.md` when done
</output>
