---
phase: 177-city-of-hillsboro-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["177-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1150_hillsboro_city_council.sql
autonomous: false
requirements: [WASH-03]

must_haves:
  truths:
    - "One 'City of Hillsboro, Oregon, US' government row exists (geo_id 4134100)"
    - "One 'City Council' chamber exists under it with official_count=7"
    - "Exactly one LOCAL_EXEC district and one LOCAL district exist on geo_id 4134100, state='or' lowercase"
    - "7 offices exist (Mayor + 6 councilors), each with representing_city='Hillsboro' set INLINE"
    - "All 7 politicians have office_id back-filled (no NULL office_id)"
    - "Section-split scan returns 0 rows for geo_id 4134100"
    - "NO ward/X00xx geofences were created (at-large model)"
    - "Rob Harris is one councilor office (title-on-seat Council President), not a separate office row"
    - "Implements CONTEXT decisions D-01/D-02 (at-large routing), D-03/D-05 (no split), D-06 (directly-elected Mayor LOCAL_EXEC), D-07 (Council President title-on-seat), D-09 (representing_city inline)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1150_hillsboro_city_council.sql"
      provides: "Structural seed: government + chamber + 2 districts + 7 offices + office_id backfill + post-verify DO block + ledger row"
      min_lines: 250
      contains: "City of Hillsboro, Oregon, US"
  key_links:
    - from: "offices"
      to: "districts (geo_id 4134100)"
      via: "district_id FK on LOCAL_EXEC + LOCAL rows"
      pattern: "geo_id = '4134100'"
    - from: "politicians.office_id"
      to: "offices.id"
      via: "back-fill UPDATE keyed on external_id IN (-4134101..-4134107)"
      pattern: "office_id = o.id"
---

<objective>
Author the single structural migration that seeds the City of Hillsboro government, City Council
chamber, two citywide districts (LOCAL_EXEC for the Mayor + LOCAL at-large for all 6 councilors),
and the 7 officials — mirroring the Beaverton migration 1131 template almost 1:1. Unlike Beaverton
(which needed a follow-up backfill migration 1141), Hillsboro sets `representing_city='Hillsboro'`
INLINE on every office INSERT (D-09), so the community banner derives correctly at first browse.

Purpose: Satisfy the roster/structure portion of WASH-03 with an idempotent, self-verifying
migration that produces no section-split and no empty LOCAL section.
Output: `1150_hillsboro_city_council.sql` (registered in schema_migrations), applied by the
orchestrator via psql, with its in-migration DO-block gates passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-RESEARCH.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-01-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES the `.sql` file to C:/EV-Accounts/backend/migrations/.
It has NO DB access. The INLINE ORCHESTRATOR applies it via `psql "$DATABASE_URL" -f <file>`
(DATABASE_URL from C:/EV-Accounts/backend/.env) and commits it in the C:/EV-Accounts repo
(`git -C "C:/EV-Accounts"`). Use the exact migration number confirmed in 177-01-SUMMARY.md (default
1150) — if Wave-0 recorded a different next number, rename the file and update the ledger INSERT.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug` and
`photo_origin_url` OUT of comments in this file. The ledger INSERT into
supabase_migrations.schema_migrations is a genuine table write (expected), not a comment.
</context>

<interfaces>
<!-- Confirmed schema shapes — executor uses these directly, no exploration needed. -->

essentials.governments  (id, name, type, state, city, geo_id)   -- slug GENERATED; NO geo_id unique constraint; INSERT via WHERE NOT EXISTS on name
essentials.chambers     (id, name, name_formal, government_id, official_count)  -- slug GENERATED ALWAYS — NEVER include in INSERT column list
essentials.districts    (id, district_type, state, geo_id, label, mtfcc)  -- NO name_formal; government_id NULL; state='or' LOWERCASE for LOCAL/LOCAL_EXEC
essentials.politicians  (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id, office_id)  -- party ALWAYS NULL (antipartisan)
essentials.offices      (id, district_id, chamber_id, politician_id, title, representing_state, representing_city, is_appointed_position, is_vacant, role_canonical)
</interfaces>

<roster>
<!-- Seed exactly these 7 (re-confirmed at Wave-0). ext_id / title / district_type: -->
| ext_id    | full_name         | title                            | district_type |
|-----------|-------------------|----------------------------------|---------------|
| -4134101  | Beach Pace        | Mayor                            | LOCAL_EXEC    |
| -4134102  | Cristian Salgado  | Councilor, Ward 1, Position A    | LOCAL         |
| -4134103  | Saba Anvery       | Councilor, Ward 1, Position B    | LOCAL         |
| -4134104  | Kipperlyn Sinclair| Councilor, Ward 2, Position A    | LOCAL         |
| -4134105  | Elizabeth Case    | Councilor, Ward 2, Position B    | LOCAL         |
| -4134106  | Olivia Alcaire    | Councilor, Ward 3, Position A    | LOCAL         |
| -4134107  | Rob Harris        | Councilor, Ward 3, Position B    | LOCAL         |
<!-- Rob Harris holds "Council President" — note ONLY in a migration comment; do NOT create a
     separate office row and do NOT alter his title. representing_state='OR', representing_city='Hillsboro' on all 7. -->
</roster>

<tasks>

<task type="auto">
  <name>Task 1: Author the Hillsboro structural migration</name>
  <files>C:/EV-Accounts/backend/migrations/1150_hillsboro_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1131_beaverton_city_council.sql (the 1:1 template — entire file)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md §"<N>_hillsboro_city_council.sql" (every code block, adapted values)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-01-SUMMARY.md (confirmed geo_id, ext_id block, migration number)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-RESEARCH.md §"Schema Shapes", §"OR Casing Rules"
  </read_first>
  <action>
    Wrap everything in a single BEGIN…COMMIT transaction. Structure exactly as Beaverton mig 1131,
    adapting values:
    (1) Pre-flight hard-abort DO block: RAISE EXCEPTION if a 'City of Hillsboro, Oregon, US'
    government already exists.
    (2) Government INSERT … SELECT (name='City of Hillsboro, Oregon, US', type='LOCAL', state='OR'
    UPPERCASE, city='Hillsboro', geo_id='4134100') guarded WHERE NOT EXISTS on name — NEVER insert
    slug.
    (3) Chamber INSERT (name='City Council', name_formal='Hillsboro City Council', government_id
    subselect, official_count=7) guarded WHERE NOT EXISTS on (name, government_id) — NEVER insert slug.
    (4) TWO district INSERTs, both state='or' LOWERCASE, geo_id='4134100', mtfcc=NULL: one
    district_type='LOCAL_EXEC' label='Hillsboro (Mayor, Citywide)', one district_type='LOCAL'
    label='Hillsboro (At-Large)', each guarded WHERE NOT EXISTS on (geo_id, district_type, state).
    Create NO other districts — no ward/X00xx rows (at-large model, D-01/D-02).
    (5) SEVEN office blocks (the CTE pattern: INSERT politician ON CONFLICT (external_id) DO UPDATE
    SET is_active, then INSERT office CROSS JOIN the correct district guarded by NOT EXISTS on
    (district_id, politician_id)). Use the exact 7 rows in the <roster> table above: Mayor Pace on
    the LOCAL_EXEC district; the 6 councilors on the shared LOCAL district. party=NULL on all;
    is_appointed_position=false; role_canonical=NULL; representing_state='OR' UPPERCASE;
    representing_city='Hillsboro' INLINE on every office (D-09). Add a comment above the Harris block
    noting he holds the Council President title (title-on-seat only, no separate row — D-07).
    (6) office_id back-fill UPDATE with an explicit external_id IN (-4134101,…,-4134107) list, guarded
    AND p.office_id IS NULL.
    (7) Post-verification DO block with gates: gov_count=1, office_count=7 (JOIN districts on geo_id
    4134100 + district_type IN ('LOCAL','LOCAL_EXEC') + state 'or'), section-split=0 (geofence row
    for 4134100/G4110 with no matching LOCAL/LOCAL_EXEC district → 0 orphans), office_id NULLs=0, and
    representing_city='Hillsboro' count=7. RAISE EXCEPTION on any mismatch; RAISE NOTICE on success.
    (8) Ledger INSERT into supabase_migrations.schema_migrations (version) VALUES ('1150') ON CONFLICT
    DO NOTHING (STRUCTURAL migrations register — contrast with headshot/stance migrations).
    Do NOT place fenced code blocks in this action — the SQL itself lives in the file; the analog
    blocks in 177-PATTERNS.md are the source of truth for exact text.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1150_hillsboro_city_council.sql`; the in-migration post-verify DO block RAISEs on any gate failure (all gates must emit the success NOTICE)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the confirmed migration number path (default 1150_hillsboro_city_council.sql)
    - Single BEGIN…COMMIT; pre-flight abort guard present
    - governments INSERT uses geo_id='4134100', state='OR', WHERE NOT EXISTS on name; no slug column
    - chambers INSERT has official_count=7; no slug column
    - Exactly 2 district INSERTs (LOCAL_EXEC + LOCAL), both state='or' lowercase, geo_id='4134100'; NO ward/X00xx districts
    - 7 office blocks with the exact ext_ids -4134101..-4134107 and titles from the <roster> table
    - representing_city='Hillsboro' appears on all 7 office INSERTs (inline, no backfill migration)
    - office_id back-fill UPDATE lists all 7 external_ids
    - Post-verify DO block asserts gov=1, offices=7, section-split=0, office_id nulls=0, representing_city=7
    - Ledger INSERT VALUES ('1150') present (structural migration registers)
    - Literal strings `slug` and `photo_origin_url` do NOT appear in comments
  </acceptance_criteria>
  <done>Structural migration authored; applies cleanly; post-verify DO block emits its success NOTICE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies the migration, runs the E2E structural gate, commits</name>
  <what-built>The Hillsboro structural migration. The orchestrator applies it and runs the standalone verification gate.</what-built>
  <how-to-verify>
    1. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1150_hillsboro_city_council.sql`. The in-migration DO block must emit "Post-verification PASSED". If it RAISEs, the transaction rolls back — fix and re-apply.
    2. Independent E2E gate (run as separate SELECTs):
       a. governments count for name='City of Hillsboro, Oregon, US' = 1.
       b. chambers row exists, official_count=7.
       c. districts: exactly 1 LOCAL_EXEC + 1 LOCAL for geo_id='4134100' state='or'; ZERO ward/X00xx rows for Hillsboro.
       d. offices count = 7 for the Hillsboro chamber; representing_city='Hillsboro' on all 7.
       e. Section-split scan returns 0 rows for geo_id 4134100.
       f. All 7 politicians have non-NULL office_id.
       g. Rob Harris (-4134107) has exactly ONE office row.
    3. Commit the migration in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/1150_hillsboro_city_council.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied; "Post-verification PASSED" NOTICE emitted
    - Independent E2E gate a–g all pass
    - Zero ward/X00xx districts for Hillsboro
    - Migration committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with the gate results, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Structural INSERTs applied to production Supabase via psql |
| migration file → schema | Idempotency + post-verify gates gate the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-01 | Tampering | Duplicate/partial re-seed on prod | mitigate | Pre-flight hard-abort DO block + WHERE NOT EXISTS on gov/chamber/districts + office guard on (district_id, politician_id); single transaction rolls back on any post-verify failure |
| T-177-04 | Tampering | Wrong geo_id (4133850) silently seeds unroutable rows | mitigate | geo_id='4134100' confirmed at Wave-0 probe A; post-verify section-split gate = 0 catches a mismatch |
| T-177-05 | Repudiation | Untracked structural change | mitigate | Migration registered in schema_migrations ledger; committed to EV-Accounts git |
| T-177-06 | Elevation of Privilege | Ward geofences hide 4 of 6 councilors | mitigate | At-large model enforced (2 citywide districts only); E2E gate 2c asserts zero ward rows |
| T-177-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- In-migration post-verify DO block passes; independent E2E gate a–g passes; migration committed.
</verification>

<success_criteria>
- 1 government + 1 chamber (official_count=7) + 2 citywide districts + 7 offices, all with
  representing_city='Hillsboro', office_id back-filled, section-split=0, no ward geofences.
</success_criteria>

<output>
Create `.planning/phases/177-city-of-hillsboro-deep-seed/177-02-SUMMARY.md` when done, recording the
minted politician UUIDs (by external_id) for plans 03 (headshots) and 04 (stances).
</output>
