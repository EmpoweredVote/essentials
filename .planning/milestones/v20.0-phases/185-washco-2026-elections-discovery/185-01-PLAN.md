---
phase: 185-washco-2026-elections-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1212_seed_washco_2026_local_races.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1212.ts
autonomous: true
requirements: [WM-ELEC-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A west-metro user on /elections can see an OR 2026 General race row for each county-commission and city-council seat actually on the Nov 3 2026 ballot (only seats up in Nov 2026 are seeded, per D-03)"
    - "Every seeded race links to a real pre-existing office_id (0 rows with NULL office_id), resolved via the government geo_id → chamber → office linkage (not representing_city, which is NULL — D-11), respecting the OR districts.state 'or' lowercase casing trap"
    - "No May-decided seat, no school-board seat, and no seat not up in 2026 receives a General race row (D-03)"
    - "Every race has primary_party NULL (antipartisan invariant holds)"
    - "The races migration is idempotent via NOT EXISTS guards (never ON CONFLICT — no unique constraint), writes NO schema_migrations ledger row, has a paired _apply-migration smoke test, and its number is live-verified before write (D-12 migration mechanics)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1212_seed_washco_2026_local_races.sql"
      provides: "25 office-anchored OR 2026 General race rows + embedded DO $$ post-verify gate"
      contains: "INSERT INTO essentials.races"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-1212.ts"
      provides: "Idempotent smoke-test harness (exit 0 twice)"
      contains: "essentials.races"
  key_links:
    - from: "essentials.races.office_id"
      to: "essentials.offices.id"
      via: "resolution through the current incumbent politician's office linkage"
      pattern: "o\\.politician_id = pol\\.id"
    - from: "essentials.races.election_id"
      to: "essentials.elections (OR 2026 General)"
      via: "FK subquery on name + state ILIKE 'or'"
      pattern: "el\\.name = 'OR 2026 General'"
---

<objective>
Seed the 25 `essentials.races` rows for the west-metro county + city offices that are actually on the
**Nov 3, 2026 General** ballot (Washington County Commission Chair + D4, and the on-ballot mayor/council
seats across all 7 cities), each anchored to a pre-existing `office_id` from Phases 175–182.

Purpose: WM-ELEC-01 criterion #1/#2 — a west-metro address on `/elections` must return its correct 2026
races. The frontend hides 0-candidate races (`100eda9`); this plan lays the office-anchored race shells
that Plan 02 attaches candidates to.

Output: migration `1212` (final number confirmed live in Task 1) + paired smoke-test script, applied to
the LIVE production DB, idempotent, with an embedded post-verify gate asserting exactly 25 races, 0 NULL
office_id, 0 non-NULL primary_party, and 0 school-board race rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-washco-2026-elections-discovery/185-CONTEXT.md
@.planning/phases/185-washco-2026-elections-discovery/185-RESEARCH.md
@.planning/phases/185-washco-2026-elections-discovery/185-PATTERNS.md

<seed_decisions>
Settled planner decisions for this plan (do not re-derive):
- **25 races total** = only seats on the Nov 3 2026 General ballot (RESEARCH seats-up table). Breakdown:
  WashCo 2 (Chair, D4) · Beaverton 1 (Pos 1) · Hillsboro 3 (Ward1-A, Ward2-A, Ward3-A) ·
  Tigard 4 (Mayor + Anderson/Ghoddusi/Robbins councilor seats) · Tualatin 4 (Mayor, Pos 1, Pos 3, Pos 5) ·
  Forest Grove 4 (Mayor + Marshall/Martinez/Valenzuela) · Sherwood 4 (Mayor + Giles/Mays/Scott) ·
  Cornelius 3 (Mayor + Baker + López).
- **OMIT the 3 May-decided seats** (WashCo D2 / Beaverton Pos 2 / Pos 5). These seats were already decided
  outright in the **May 19 2026 primary** (Monteblanco / Teater / Dugger won majorities) and therefore have
  NO item on the Nov 3 2026 General ballot. Omission is governed by **D-03's literal reading — "only seats
  actually up in **Nov** 2026"** — reinforced by **RESEARCH Pitfall 1** ("up in the 2026 cycle" ≠ "has a
  Nov 2026 race"; a May-decided seat has no November ballot item). RESEARCH Open Question 1 floated seeding
  "ceremonial" uncontested General rows for these 3, but the Nov-ballot-only reading of D-03 governs → OMIT,
  yielding exactly 25 races. (Note: D-08 concerns not creating primary-TYPE rows, not this omission — it is
  not the controlling source here.)
- **Seats NOT up in 2026** (20 offices) are correctly excluded per D-03.
</seed_decisions>

<office_resolution>
CRITICAL correction to the PATTERNS "resolve by `o.title`" shape: plain-title at-large councils
(Tigard, Forest Grove, Sherwood, Cornelius) have MULTIPLE `essentials.offices` rows all titled
`'Councilor'` sharing one LOCAL district (verified in migrations 1159/1178). Resolving by `o.title`
alone would collapse 3 councilor races into 1. **Resolve each seat's `office_id` via the CURRENT
incumbent politician's office linkage** — this uniquely pins each office_id even for identical titles.
Open/retiring incumbents still hold their office row through Dec 2026, so this works for open seats too.
`position_name` = `o.title` verbatim (Claude's Discretion in CONTEXT: mirror the seeded official title).
</office_resolution>

<interfaces>
Resolution shape (per RESEARCH lines 311–338 + this plan's incumbent-name correction):
- `essentials.races`: `id, election_id, office_id, position_name text NOT NULL, primary_party (NULL), seats int DEFAULT 1`. NO unique constraint → idempotency is `NOT EXISTS (election_id, office_id)`, NEVER `ON CONFLICT`.
- Election FK: `JOIN essentials.elections el ON el.name = 'OR 2026 General' AND el.state ILIKE 'or'` (Task 1 re-verifies the exact stored name/state).
- Office FK: `JOIN essentials.politicians pol ON pol.full_name = v.incumbent JOIN essentials.offices o ON o.politician_id = pol.id JOIN essentials.districts d ON d.id = o.district_id AND d.geo_id = v.geo_id AND d.state = 'or'` (lowercase for districts.state — OR casing trap).
- WashCo Chair scoped by geo_id `41067` (COUNTY); WashCo D4 by geo_id `washco-or-commissioner-district-4` (custom LOCAL) per RESEARCH lines 325–334.
- The 25 incumbents (from the RESEARCH seats-up table) key the resolution: Harrington (Chair), Willey (D4), Hartmeier-Prigg (Bvtn Pos1), Salgado/Sinclair/Alcaire (Hillsboro W1-A/W2-A/W3-A), Hu/Anderson/Ghoddusi/Robbins (Tigard), Bubenik/Reyes/Brooks/Gonzalez (Tualatin Mayor/Pos1/Pos3/Pos5), Wenzl/Marshall/Martinez/Valenzuela (Forest Grove), Rosener/Giles/Mays/Scott (Sherwood), Dalin/Baker/López (Cornelius). Confirm each against the live DB before insert — do NOT hardcode a name the DB doesn't have.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 verification — live migration counter, OR 2026 General election row casing, seats-up spot-check</name>
  <files>(no files written — verification only; records findings for Task 2)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/ (directory listing for the counter)
    - .planning/phases/185-washco-2026-elections-discovery/185-RESEARCH.md (Migration Mechanics §, seats-up table, Assumption A1)
  </read_first>
  <action>
    Re-verify the on-disk migration counter live: list `C:/EV-Accounts/backend/migrations`, strip to
    leading integers, numeric-sort, take the max. Base number for this plan = max + 1 (RESEARCH found
    1211 → next 1212, but a parallel workstream may have advanced it — the LIVE value wins). Record the
    3-number block for the phase: races = BASE, candidates = BASE+1, discovery = BASE+2. Record BASE in
    this plan's SUMMARY so Plans 02/03 read it (avoids parallel-Wave-2 number collision).
    Then query the LIVE production DB (mcp__supabase-local IS production, read-only SELECT) for the
    pre-existing OR 2026 General election row: SELECT id, name, state, election_type, jurisdiction_level
    FROM essentials.elections WHERE election_date='2026-11-03'. Record the EXACT stored `name` and `state`
    casing — the FK subquery in Task 2 must match it (ILIKE 'or' is the defensive guard, but confirm the
    literal name string is 'OR 2026 General' and not a variant). Spot-check (Assumption A1) that the
    WashCo Chair / D4 and Beaverton Pos 1 runoffs have not been reversed by late certification via a quick
    check of the seats-up table's cited sources; if any changed, flag before proceeding.
  </action>
  <verify>
    <automated>ls C:/EV-Accounts/backend/migrations | grep -E '^[0-9]+_' | sed -E 's/^([0-9]+)_.*/\1/' | sort -n | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - The live max migration number is recorded and BASE (= max+1) is written to this plan's SUMMARY.
    - The exact stored `name` and `state` of the OR 2026-11-03 General election row are recorded (a single
      row is returned; if 0 or >1 rows, STOP and surface — D-08 assumed exactly one General row exists).
    - Seats-up certification spot-check done; any reversal is flagged before Task 2.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration BASE — 25 office-anchored OR 2026 General race rows</name>
  <files>C:/EV-Accounts/backend/migrations/{BASE}_seed_washco_2026_local_races.sql, C:/EV-Accounts/backend/scripts/_apply-migration-{BASE}.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1112_nv_2026_races.sql (analog — header convention, VALUES-list insert, DO $$ post-verify gate)
    - C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql AND 1178_forest_grove_city_council.sql (confirm plain 'Councilor' title collision + how offices link to politicians)
    - C:/EV-Accounts/backend/scripts/_apply-migration-1112.ts (smoke-test harness shape)
    - 185-PATTERNS.md (races migration section + Shared Patterns: idempotency, antipartisan, no-ledger, BEGIN/COMMIT gate, OR casing)
    - 185-RESEARCH.md seats-up table (the 25 seats + their incumbents) and office-resolution join (lines 311–338)
  </read_first>
  <action>
    Create `{BASE}_seed_washco_2026_local_races.sql` wrapped in `BEGIN; ... COMMIT;`. Build a VALUES list
    of the 25 (geo_id, incumbent_full_name) tuples from the `<interfaces>` block, and INSERT INTO
    essentials.races (id, election_id, office_id, position_name, primary_party, seats) SELECT
    gen_random_uuid(), el.id, o.id, o.title, NULL, 1 — resolving `el.id` via the Task-1-confirmed election
    row (`el.name = 'OR 2026 General' AND el.state ILIKE 'or'`) and `o.id` via the incumbent→office linkage
    (`pol.full_name = v.incumbent` → `o.politician_id = pol.id` → `d.geo_id = v.geo_id AND d.state = 'or'`).
    WashCo Chair uses geo_id 41067 (COUNTY district); WashCo D4 uses geo_id
    `washco-or-commissioner-district-4` (custom LOCAL) per RESEARCH lines 325–334. Guard EVERY race with
    `WHERE NOT EXISTS (SELECT 1 FROM essentials.races r WHERE r.election_id = el.id AND r.office_id = o.id)`
    — NEVER `ON CONFLICT` (essentials.races has no unique constraint, Pitfall 4). `primary_party` stays
    NULL (D-10 antipartisan). NO `schema_migrations` ledger INSERT (pure data-seed, 1109/1112 family — D-08).
    Add an embedded `DO $$ ... RAISE EXCEPTION` post-verify block asserting: (a) exactly 25 OR-2026-General
    races for these offices, (b) 0 with NULL office_id, (c) 0 with non-NULL primary_party, (d) the NEGATIVE
    school-board assertion — 0 races whose office resolves to any of the 5 west-metro G5420 districts (per
    D-04; PATTERNS lines 109–119). Create the paired `_apply-migration-{BASE}.ts` (copy the 1112 harness:
    reads DATABASE_URL from env, applies the SQL, then re-runs the count assertions and exits non-zero on
    mismatch). Do NOT `cd` into or git-commit inside C:/EV-Accounts — the migration file lands there but
    commits are the executor's separate `git -C` concern.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-{BASE}.ts</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) FROM essentials.races r JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='OR 2026 General' AND el.state ILIKE 'or' AND r.office_id IN (<the 25 resolved offices>)` = **25**.
    - 0 of those 25 races have NULL `office_id`; 0 have non-NULL `primary_party`.
    - Negative school-board assertion passes: 0 OR-2026-General races resolve to any of the 5 G5420 west-metro school-district geo_ids.
    - The smoke script exits 0 on first apply AND exits 0 (0 new rows) on an immediate second run (idempotency).
    - The migration writes NO `supabase_migrations.schema_migrations` row.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → LIVE production DB | mcp__supabase-local IS production; every write is live and irreversible without a follow-up migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-01-DB | Tampering | Partial/duplicate/mis-linked writes to essentials.races on the live DB | mitigate | `BEGIN;/COMMIT;` transaction wrap + `NOT EXISTS` idempotency guards + embedded `DO $$ RAISE EXCEPTION` post-verify gate that aborts the whole transaction on any count mismatch (25 races / 0 NULL office_id / 0 party / 0 school-board) |
| T-185-02-SCOPE | Tampering | Seeding a race for a May-decided or not-up-in-2026 seat | mitigate | Fixed 25-seat VALUES list keyed to confirmed incumbents; post-verify asserts exactly 25 and the negative school-board check; May-decided seats explicitly omitted |
| T-185-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure SQL + existing tsx harness; N/A |
</threat_model>

<verification>
- Smoke script `_apply-migration-{BASE}.ts` exits 0 twice (apply + idempotent re-run).
- Live DB: exactly 25 OR-2026-General races on the resolved west-metro offices; 0 NULL office_id; 0 non-NULL primary_party; 0 school-board races.
- Manual UAT (phase gate, deferred to phase verification): load `/elections` for a Beaverton and a Hillsboro address — county-commission + city-council race shells appear (candidates arrive in Plan 02).
</verification>

<success_criteria>
25 office-anchored OR 2026 General race rows exist on the live DB, correctly linked to pre-existing
office_ids (0 NULL), antipartisan (0 non-NULL primary_party), with 0 school-board races, applied via an
idempotent migration that writes no ledger row.
</success_criteria>

<output>
Create `.planning/phases/185-washco-2026-elections-discovery/185-01-SUMMARY.md` when done — MUST record the
confirmed BASE migration number and the exact OR 2026 General election `name`/`state` casing (Plans 02/03
consume both).
</output>
</content>
</invoke>
