---
phase: 111-ma-stances-execs-federal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/359_healey_stances.sql
autonomous: true
requirements:
  - MA-STANCES-01
must_haves:
  truths:
    - "Maura Healey has >=15 stance rows in inform.politician_answers after migration 359 applies"
    - "Every Healey stance row in inform.politician_context has at least 1 URL in sources (100% citation rate per D-10)"
    - "No defaulted/neutral fallback rows exist — only topics with documented evidence are inserted"
    - "D-05: per-individual SQL file 359_healey_stances.sql created following naming convention {N}_{firstname}_{lastname}_stances.sql"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/359_healey_stances.sql"
      provides: "Per-individual stance migration for Governor Maura Healey"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "359_healey_stances.sql"
      to: "essentials.politicians (external_id=-200001)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
    - from: "inform.politician_answers"
      to: "computeDisplaySpokes() in src/lib/compass.js"
      via: "compass spoke selection reads politician_answers directly"
      pattern: "politician_answers.value"
---

<objective>
Research Maura Healey's (Governor of Massachusetts, external_id=-200001) public record across all currently-active compass topics, write per-individual stance migration 359, apply it to production Supabase, and verify the stance row count.

Purpose: Partially satisfies MA-STANCES-01 (MA executive stances) for the highest-priority official. Healey has a rich record as MA AG (2013–2023) and now Governor (2023–present), so target 18–21+ stances per D-09.

Output: `359_healey_stances.sql` applied; ≥15 stance rows in `inform.politician_answers` for Healey; ≥15 paired rows in `inform.politician_context`, every row with at least 1 source URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Healey UUID + active topic list, then research all topics</name>
  <read_first>
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (all decisions D-01 through D-10)
    - .planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md (UUID table, per-stance INSERT pattern)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120 — header, topic UUID reference block, BEGIN, first section)
    - C:/EV-Accounts/backend/migrations/154_ma_state_executives.sql (lines 129-140 to confirm Healey external_id=-200001)
  </read_first>
  <action>
    Step A (UUID + topic resolution, run via mcp__supabase-local__execute_sql):
      1. Resolve Healey UUID:
         SELECT id FROM essentials.politicians WHERE external_id = -200001;
         Record returned UUID as ${HEALEY_UUID}. If 0 rows, abort — migration 154 must be applied first.
      2. Resolve active compass topics:
         SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;
         Record full list as ${TOPIC_LIST}. Use the live count from this query as ground truth (not the hardcoded list in PATTERNS.md).

    Step B (research, ONE agent only per D-08, NEVER in parallel):
      Perform web research to gather Healey's documented positions on EVERY topic in ${TOPIC_LIST}.
      Per D-01: attempt all topics. Per chair philosophy: omit topics with no evidence — do NOT default to neutral.
      Per D-09: target 18–21+ stances; ROADMAP minimum is ≥15.
      For each topic where evidence is found, record: { topic_slug, value (1-5 integer per D-02), reasoning (2-5 sentences citing specific cases/statements/actions), sources (1+ URLs with paths, no bare domains) }.

      Healey's documented record includes:
        - abortion: MA AG defended Roe; filed SCOTUS briefs; signed abortion access legislation as Governor
        - campaign-finance: strong public integrity focus from AG years
        - civil-rights: numerous civil rights cases as AG (LGBTQ+ discrimination, voting rights enforcement)
        - climate-change: MA climate commitments as Governor; Clean Energy and Climate Plan
        - deportation/immigration: AG challenged Trump immigration enforcement; limited sanctuary city overlap
        - economic-development: economic development initiatives as Governor
        - healthcare: MA universal healthcare defender; ACA compliance enforcement as AG
        - housing: MA housing crisis response; MBTA Communities Act enforcement as Governor
        - judicial-*: AG enforcement record — prosecution priorities, police accountability
        - redistricting: MA constitutional officer during redistricting cycles
        - same-sex-marriage: LGBTQ+ champion; landmark civil rights cases as AG
        - taxes: MA tax policy positions as Governor
        - trans-athletes: LGBTQ+ advocate
        - voting-rights: AG led national VRA coalition
        - Local-scope topics (city-sanitation, rent-regulation, residential-zoning, jail-capacity, local-environment, local-immigration): check for MA Governor-level positions; include if found.

    Step C (do NOT write the migration file yet — that is Task 2):
      Save intermediate research output as `.planning/phases/111-ma-stances-execs-federal/_tmp-healey-research.json` with shape:
        [{ "topic_slug": "...", "topic_id": "uuid", "value": 1-5, "reasoning": "...", "sources": ["url1","url2"] }, ...]
      This file is the input to Task 2.
  </action>
  <verify>
    <automated>
      Run mcp__supabase-local__execute_sql with:
        SELECT id FROM essentials.politicians WHERE external_id = -200001;
      Must return exactly 1 row. Then verify temp research file exists and has >=15 entries:
        Test-Path "C:/Transparent Motivations/essentials/.planning/phases/111-ma-stances-execs-federal/_tmp-healey-research.json"
        ((Get-Content "C:/Transparent Motivations/essentials/.planning/phases/111-ma-stances-execs-federal/_tmp-healey-research.json" | ConvertFrom-Json).Count -ge 15)
    </automated>
  </verify>
  <done>
    - Healey UUID resolved and recorded
    - Live active topic list resolved from inform.compass_topics
    - Research JSON file exists with ≥15 entries, every entry has non-empty sources array
    - No defaulted neutral entries (per D-01: blank > neutral fallback)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 359_healey_stances.sql matching the 282 pattern exactly</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120 for header + first politician section structure)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1233-1251 for COMMIT + verification comment block)
    - .planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md ("Per-Stance INSERT Pattern" and "Verification Comment Block")
    - .planning/phases/111-ma-stances-execs-federal/_tmp-healey-research.json (created in Task 1)
  </read_first>
  <action>
    Create file `C:/EV-Accounts/backend/migrations/359_healey_stances.sql` with this exact structure:

      1. Header comment block:
         -- Migration 359: Maura Healey Stances
         -- Purpose: Insert/upsert stance data for Maura Healey (Governor of Massachusetts).
         -- Topic scope: All active compass topics attempted; evidence-only — topics with no
         --   evidence are omitted entirely (no neutral defaults per D-01).
         -- Idempotency: ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables.
         -- Apply to remote Supabase via Supabase MCP (mcp__supabase-local is remote production per CLAUDE.md).

      2. Topic UUID reference block (copy verbatim from 282 lines 14-57, then append any extra rows present in the live active set from Task 1 Step A).

      3. `BEGIN;` then politician section header:
         -- ============================================================
         -- Maura Healey
         -- ============================================================

      4. For each entry in _tmp-healey-research.json, emit a pair of INSERTs per 111-PATTERNS.md "Per-Stance INSERT Pattern":
         - politician_answers INSERT (value as float: 1.0, 2.0, etc.)
         - politician_context INSERT (reasoning in $$...$$ dollar-quotes, sources as ARRAY[...]::text[]::text[])
         - Both with ON CONFLICT DO UPDATE

      5. `COMMIT;`

      6. Verification comment block at end per 111-PATTERNS.md, substituting ${HEALEY_UUID} and expected row count.

    Hard rules (verifiable):
      - File must start with `-- Migration 359:` on line 1
      - File must contain exactly one `BEGIN;` and exactly one `COMMIT;`
      - Every `INSERT INTO inform.politician_answers` must be paired with a following `INSERT INTO inform.politician_context` for the same topic_id
      - Every `politician_context` INSERT must contain `ARRAY[` followed by at least one quoted URL
      - All `value` literals are float form: `1.0`, `2.0`, `3.0`, `4.0`, or `5.0`
      - All `reasoning` blocks use `$$...$$` dollar quoting
      - Do NOT apply the migration in this task — Task 3 applies it
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/359_healey_stances.sql"
      (Select-String -Path "C:/EV-Accounts/backend/migrations/359_healey_stances.sql" -Pattern '^BEGIN;$' -SimpleMatch).Count -eq 1
      (Select-String -Path "C:/EV-Accounts/backend/migrations/359_healey_stances.sql" -Pattern '^COMMIT;$' -SimpleMatch).Count -eq 1
      $a = (Select-String -Path "C:/EV-Accounts/backend/migrations/359_healey_stances.sql" -Pattern 'INSERT INTO inform.politician_answers').Count
      $c = (Select-String -Path "C:/EV-Accounts/backend/migrations/359_healey_stances.sql" -Pattern 'INSERT INTO inform.politician_context').Count
      ($a -eq $c) -and ($a -ge 15)
    </automated>
  </verify>
  <done>
    - Migration file exists at C:/EV-Accounts/backend/migrations/359_healey_stances.sql
    - File begins with header comment then `BEGIN;` and ends with `COMMIT;` then verification block
    - Equal counts of INSERT INTO politician_answers and INSERT INTO politician_context (both ≥15)
    - Topic UUID reference block present in header
    - Verification comment block at end with Healey UUID embedded
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 359 to production Supabase + verify counts</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/359_healey_stances.sql (the file written in Task 2)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-06: apply immediately; D-07: format)
  </read_first>
  <action>
    Step A: Read the full content of `C:/EV-Accounts/backend/migrations/359_healey_stances.sql` and pass it as the `query` argument to mcp__supabase-local__execute_sql. Per CLAUDE.md, mcp__supabase-local IS production.

    Step B (verification via mcp__supabase-local__execute_sql):
      Run this query — must return count >= 15:
        SELECT COUNT(*) AS n FROM inform.politician_answers
        WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -200001);

      Run this query — must return 0 (every answer must have a context row):
        SELECT COUNT(*) AS unpaired
        FROM inform.politician_answers pa
        LEFT JOIN inform.politician_context pc
          ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
        WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -200001)
          AND pc.politician_id IS NULL;

      Run this query — must return 0 (100% citation rate per D-10):
        SELECT COUNT(*) AS uncited
        FROM inform.politician_context
        WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -200001)
          AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0);

    Step C: Delete the temp research file `.planning/phases/111-ma-stances-execs-federal/_tmp-healey-research.json`.
  </action>
  <verify>
    <automated>
      Run via mcp__supabase-local__execute_sql:
        SELECT
          (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -200001)) AS answer_count,
          (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -200001) AND pc.politician_id IS NULL) AS unpaired,
          (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -200001) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)) AS uncited;
      Expected: answer_count >= 15, unpaired = 0, uncited = 0.
    </automated>
  </verify>
  <done>
    - Migration 359 applied successfully (no SQL error)
    - inform.politician_answers row count for Healey >= 15
    - All Healey answer rows have paired context rows (unpaired = 0)
    - All Healey context rows have >=1 source URL (uncited = 0)
    - Temp research JSON deleted
  </done>
</task>

</tasks>

<verification>
- mcp__supabase-local__execute_sql query: stance count for Healey >= 15
- All paired (answer ↔ context) rows present, unpaired = 0
- 100% citation rate (uncited = 0)
- Migration file 359_healey_stances.sql committed to EV-Accounts repo
</verification>

<success_criteria>
- MA-STANCES-01 partially satisfied (Healey portion)
- ROADMAP Phase 111 success criterion #1 partially met (>=15 for Healey)
- D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-10 all honored
</success_criteria>

<output>
Create `.planning/phases/111-ma-stances-execs-federal/111-01-SUMMARY.md` documenting:
- Stance count delivered for Healey
- Topics with no evidence found (omitted) — list slugs
- Sources used (domain summary)
- Any deviations from D-01..D-10 (should be none)
</output>
