---
phase: 143-santa-clarita-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql
autonomous: true
requirements: [SCLR-01]

must_haves:
  truths:
    - "D-01: this phase reconciles the EXISTING partial Santa Clarita seed (UPDATE-not-INSERT on gov 42164a8f) — no greenfield rebuild, no duplicate offices/external_ids"
    - "Santa Clarita government row carries geo_id 0669088 (no longer NULL)"
    - "Exactly ONE chamber named 'City Council' remains under the SC government; duplicate Chamber A (external_id -200978, id 315e67c5) and its 3 offices are deleted (split-section risk eliminated)"
    - "Cameron Smyth (external_id -700180, id dcf156cb) is RETIRED — office_id NULL, is_incumbent=false, is_active=false — NOT reseated into Chamber B"
    - "Jason Gibbs (external_id 665692, politician_images politician_id 434cd9b0) has exactly one politician_images row (deduped 2→1)"
    - "Surviving Chamber B council titles are normalized to 'Councilmember' (the 'Council Member' space-variant is gone); any NULL politicians.office_id back-link for Gibbs/Weste/Ayala is filled"
    - "feedback_section_split_check SQL returns 0 rows after consolidation (D-04)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql"
      provides: "Idempotent data-hygiene + chamber-consolidation migration for Santa Clarita"
      contains: "315e67c5"
  key_links:
    - from: "essentials.governments (42164a8f-2e0a-4786-9099-ce36f3f97101)"
      to: "geo_id '0669088'"
      via: "UPDATE ... WHERE id = '42164a8f-2e0a-4786-9099-ce36f3f97101' AND geo_id IS NULL"
      pattern: "geo_id\\s*=\\s*'0669088'"
    - from: "essentials.politicians (external_id -700180, Cameron Smyth)"
      to: "retired state (office_id NULL, is_incumbent=false, is_active=false)"
      via: "UPDATE politicians SET ... WHERE external_id = -700180"
      pattern: "is_incumbent\\s*=\\s*false"
    - from: "essentials.chambers (315e67c5, external_id -200978, Chamber A)"
      to: "deleted (offices-before-chamber FK order)"
      via: "DELETE offices WHERE chamber_id = '315e67c5...' then DELETE chamber"
      pattern: "DELETE FROM essentials\\.chambers"
---

<objective>
Reconcile the existing partial, duplicate-chamber Santa Clarita seed and fix all data-hygiene defects
in a single idempotent migration (894). This is the foundation wave — every later wave (roster,
headshots, stances) assumes a clean, unambiguous, single-chamber structure.

⚠️ CRITICAL ROSTER CORRECTION (RESEARCH overrides CONTEXT D-02): Cameron Smyth DEPARTED the council
December 10, 2024 (CVRA district transition made his seat non-continuable). He is NOT reseated into
Chamber B — he is RETIRED. The true current 5-member council is Weste / Ayala / Gibbs (in DB, Chamber B)
+ McLean + Miranda (NOT in DB — inserted in Plan 02). Any step that "migrates Smyth into Chamber B" is wrong.

Six fixes, all DB-verified against the live state on 2026-06-19:
1. Backfill `essentials.governments.geo_id = '0669088'` on the SC gov row (currently NULL) — D-01 / SCLR-01
2. RETIRE Cameron Smyth (external_id -700180, id dcf156cb): set office_id NULL, is_incumbent=false,
   is_active=false (Pitfall 1 — do NOT reseat) — RESEARCH correction to D-02
3. Delete Chamber A (external_id -200978, id 315e67c5) and its 3 offices (empty Mayor + 2 Council Member),
   FK-safe order: detach Smyth → DELETE offices by chamber_id → DELETE chamber — D-02
4. Normalize surviving Chamber B titles to 'Councilmember' (kill the 'Council Member' space-variant) — D-03
5. Dedupe Jason Gibbs' 2 `politician_images` rows down to 1 (keep press_use over scraped; Pitfall 6) — image hygiene
6. Back-fill `politicians.office_id` for any of Gibbs/Weste/Ayala where NULL; confirm/set
   `chambers.official_count` on the surviving Chamber B (id eeabd028, external_id 11243)

Purpose: A clean structural base. Duplicate 'City Council' chamber names cause split-section render
bugs; NULL geo_id breaks routing; a "still-active" departed member corrupts the roster; duplicate
images render twice. Output: Migration 894 applied to production; all post-verification queries green;
split-section check returns 0 rows.

This IS a structural migration — it registers in supabase_migrations.schema_migrations normally
(it is NOT a stance/headshot audit-only migration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-santa-clarita-deep-seed/143-CONTEXT.md
@.planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md
@.planning/phases/143-santa-clarita-deep-seed/143-VALIDATION.md

<interfaces>
<!-- DB-verified live values (2026-06-19) the executor uses directly. Task 1 confirms them with a
     read-only pre-flight query before any write. -->

Government row:
  id     = '42164a8f-2e0a-4786-9099-ce36f3f97101'
  name   = 'City of Santa Clarita, California, US'
  geo_id = NULL  → set to '0669088'   (state already 'CA')

Two chambers, BOTH named 'City Council' (slug santa-clarita-city-council collides):
  KEEP   eeabd028-35b3-4aae-97ec-0fba4c829c00  external_id 11243   (Chamber B — 3 'Councilmember' offices, official_count 5) → SURVIVOR
  RETIRE 315e67c5-9fb3-480b-8647-a05a86a0cefd  external_id -200978 (Chamber A — 3 offices: 1 empty Mayor + 2 Council Member, 1 seated = Smyth)
  (chambers.slug is a GENERATED column — NEVER include slug in any write)

Cameron Smyth — DEPARTED Dec 10 2024 → RETIRE (do NOT reseat):
  politician id   = 'dcf156cb-919b-4ca6-93ef-70fd6767f77c'
  external_id     = -700180
  target state    = office_id NULL, is_incumbent=false, is_active=false   (NEVER DELETE a politician row)

Surviving Chamber B politicians (keep external_ids — real source IDs, do NOT renumber):
  665693 Laurene Weste · 665689 Patsy Ayala · 665692 Jason Gibbs

Jason Gibbs image dedupe (2 rows → keep 1):
  politician_images.politician_id = '434cd9b0-ce80-42fd-b71d-f221349e33f5'
  Identify both rows by their specific id + photo_license first; DELETE the lower-priority one
  (keep press_use over scraped_no_license; if same license, keep either). NEVER bulk-delete by politician_id.

Title normalization: Chamber B uses 'Councilmember'; any 'Council Member' (with space) variant → 'Councilmember' (D-03).

feedback_section_split_check SQL: see project memory (feedback_section_split_check.md) — run after consolidation; 0 rows = clean.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of live DB state + FK behavior</name>
  <files>C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql</files>
  <read_first>
    - .planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md (§KEY RESEARCH QUESTION 5 reconciliation order, §Common Pitfalls 1/2/6, §Open Questions 1/3)
    - .planning/phases/143-santa-clarita-deep-seed/143-CONTEXT.md (§decisions D-01..D-05, §Domain verified DB state)
    - .planning/phases/142-long-beach-deep-seed/142-01-PLAN.md (the direct precedent — reconcile plan shape)
    - C:/EV-Accounts/backend/migrations/878_long_beach_reconcile.sql (FK-safe chamber DELETE precedent — read first)
  </read_first>
  <action>
    Before writing any UPDATE/DELETE, run read-only confirmation queries via mcp__supabase-local
    execute_sql (production) or psql -f using C:/EV-Accounts/backend/.env DATABASE_URL:
    (a) confirm governments id 42164a8f-2e0a-4786-9099-ce36f3f97101 has geo_id NULL and state 'CA';
    (b) confirm exactly two chambers under that government both named 'City Council' — id eeabd028…
    (external_id 11243, ~3 offices, official_count 5) and id 315e67c5… (external_id -200978, 3 offices);
    (c) confirm Cameron Smyth external_id -700180 (id dcf156cb…) is currently linked into a Chamber A
    office and capture his current office_id / is_active / is_incumbent;
    (d) confirm external_id 665692 (Gibbs, politician_images politician_id 434cd9b0…) has exactly 2
    politician_images rows and capture the specific id + photo_license of each;
    (e) capture the current title strings on all Chamber B offices (look for the 'Council Member'
    space-variant) and which of 665693/665689/665692 (if any) have a NULL politicians.office_id;
    (f) inspect the FK on essentials.offices.chamber_id (schema: `\d essentials.offices` or query
    information_schema) to confirm RESTRICT vs CASCADE — write offices-before-chamber regardless (safe
    either way). If ANY precondition does not match RESEARCH.md (especially: Smyth NOT in Chamber A, or
    a third chamber, or Gibbs has ≠2 images), STOP and report the discrepancy — do not write a migration
    against a moved target.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id, state FROM essentials.governments WHERE id='42164a8f-2e0a-4786-9099-ce36f3f97101';" — returns one row, geo_id NULL, state 'CA' (pre-state confirmed)</automated>
  </verify>
  <acceptance_criteria>
    - All 6 pre-flight checks executed; results captured in the executor's notes
    - Two chambers confirmed sharing the name 'City Council' with ids eeabd028… (KEEP) and 315e67c5… (RETIRE)
    - Smyth's current office_id captured; confirmed he is in Chamber A, not Chamber B
    - The specific politician_images id to DELETE for Gibbs (434cd9b0…) is identified (lower-priority row)
    - The set of Chamber B offices needing a title normalize ('Council Member'→'Councilmember') and any NULL office_id back-link is recorded
    - FK behavior of offices.chamber_id is known; deletion order decided (offices first, then chamber)
    - Any mismatch vs RESEARCH.md is surfaced and execution paused (no blind writes)
  </acceptance_criteria>
  <done>Live DB pre-state matches RESEARCH.md; exact ids for the Gibbs dedupe DELETE, title normalizations, and the Chamber A teardown are known.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 894 (reconcile / data hygiene / chamber consolidation)</name>
  <files>C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/878_long_beach_reconcile.sql (FK-safe DELETE + idempotent geo_id backfill pattern)
    - .planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md (§Pattern 1 chamber DELETE, §Common Pitfalls 1/2/6, §Security Domain — target Chamber A by UUID not name)
    - LOCATION-ONBOARDING.md (§California Quick Reference — chambers.slug GENERATED, districts.state='CA' uppercase)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/894_santa_clarita_reconcile.sql, all statements idempotent,
    using the captured ids from Task 1, in this FK-safe order:
    (1) UPDATE essentials.governments SET geo_id='0669088' WHERE id='42164a8f-2e0a-4786-9099-ce36f3f97101'
        AND geo_id IS NULL (D-01 / SCLR-01).
    (2) RETIRE Smyth: UPDATE essentials.politicians SET office_id=NULL, is_incumbent=false, is_active=false
        WHERE external_id=-700180 (detach BEFORE deleting his Chamber A office; Pitfall 1 — do NOT reseat).
    (3) DELETE essentials.offices WHERE chamber_id='315e67c5-9fb3-480b-8647-a05a86a0cefd' (all 3 Chamber A
        offices: empty Mayor + 2 Council Member). Target Chamber A by its UUID ONLY, never by name (both
        chambers share 'City Council' — §Security Domain T-143-03).
    (4) DELETE essentials.chambers WHERE id='315e67c5-9fb3-480b-8647-a05a86a0cefd' (Chamber A gone).
    (5) Dedupe Gibbs images: DELETE the specific lower-priority politician_images row id captured in Task 1
        for politician_id 434cd9b0-ce80-42fd-b71d-f221349e33f5 (keep press_use; scope by id, never bulk-delete
        by politician_id; Pitfall 6).
    (6) Normalize titles: UPDATE essentials.offices SET title='Councilmember' WHERE chamber_id=
        'eeabd028-35b3-4aae-97ec-0fba4c829c00' AND title <> 'Councilmember' (kill the 'Council Member'
        space-variant; D-03).
    (7) Back-fill politicians.office_id for any of 665693/665689/665692 where NULL (link to their Chamber B
        office), and UPDATE essentials.chambers SET official_count=5 WHERE id=
        'eeabd028-35b3-4aae-97ec-0fba4c829c00' if not already 5.
    Do NOT touch the slug column anywhere (GENERATED). End the file with the post-verification queries:
    the feedback_section_split_check SQL (project memory) + a chamber-name listing under gov 42164a8f
    (expect exactly one 'City Council') + a Gibbs image count. Apply via mcp__supabase-local execute_sql
    or psql -f. This registers in schema_migrations normally (structural migration).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT geo_id FROM essentials.governments WHERE id='42164a8f-2e0a-4786-9099-ce36f3f97101';" returns '0669088' AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.chambers WHERE government_id='42164a8f-2e0a-4786-9099-ce36f3f97101' AND name='City Council';" returns 1</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT geo_id FROM essentials.governments WHERE id='42164a8f...'` → '0669088'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='42164a8f...' AND name='City Council'` → 1 (not 0, not 2)
    - `SELECT COUNT(*) FROM essentials.chambers WHERE external_id='-200978'` → 0 (Chamber A deleted)
    - `SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='315e67c5...'` → 0 (Chamber A offices deleted)
    - `SELECT is_incumbent, is_active, office_id FROM essentials.politicians WHERE external_id=-700180` → false, false, NULL (Smyth retired, NOT reseated)
    - `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id='434cd9b0...'` → 1 (Gibbs deduped, not 0, not 2)
    - No Chamber B office title equals 'Council Member' (space-variant) — all 'Councilmember'
    - Surviving Chamber B (eeabd028…) has official_count=5; Gibbs/Weste/Ayala office_id back-links non-NULL
    - feedback_section_split_check SQL returns 0 rows (D-04)
    - Migration is idempotent: a second apply changes 0 rows and raises no error
  </acceptance_criteria>
  <done>Migration 894 applied; one 'City Council' chamber remains; Smyth retired (not reseated); Gibbs deduped 2→1; titles normalized; split-section check 0 rows; re-running the file is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator (psql/MCP) directly against production Supabase. No untrusted user input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Tampering | Chamber DELETE targets the wrong chamber (both share name 'City Council') | mitigate | DELETE keyed on UUID '315e67c5…' ONLY, never on name; post-verify exactly one 'City Council' chamber remains and Chamber B (eeabd028…) survives |
| T-143-02 | Tampering | Chamber DELETE fails on FK / leaves orphan offices | mitigate | FK-safe order: detach Smyth office_id → DELETE offices by chamber_id → DELETE chamber; pre-flight inspects FK behavior; offices-before-chamber regardless |
| T-143-03 | Spoofing | Departed Smyth left active / reseated as a 5th member (roster corruption) | mitigate | RESEARCH correction enforced: Smyth RETIRED (office_id NULL, is_incumbent/is_active false), never reseated; post-verify his flags are false; true 5th members are McLean/Miranda (Plan 02) |
| T-143-04 | Tampering | Image dedupe deletes both Gibbs rows | mitigate | DELETE scoped to the specific captured row id; post-verify exactly 1 image row for 434cd9b0… |
| T-143-05 | Tampering | Non-idempotent migration applied twice corrupts state | mitigate | All writes guarded (geo_id IS NULL, id-scoped deletes, title <> target); second apply = 0 rows changed |
| T-143-06 | Tampering | Accidental write to production without verifying pre-state | mitigate | Task 1 read-only pre-flight confirms live state matches RESEARCH before any write; STOP-on-mismatch |
</threat_model>

<verification>
Run the full structure-wave smoke check after applying 894:
1. gov geo_id = '0669088'
2. exactly 1 chamber 'City Council' under gov 42164a8f; Chamber A (-200978 / 315e67c5) and its offices gone
3. Smyth (-700180): office_id NULL, is_incumbent=false, is_active=false (retired, NOT reseated)
4. Gibbs (434cd9b0…) has exactly 1 politician_images row
5. no Chamber B office titled 'Council Member' (space-variant); official_count=5
6. feedback_section_split_check SQL → 0 rows
</verification>

<success_criteria>
All six data-hygiene/consolidation defects fixed and independently verified; Smyth retired (not
reseated); migration is idempotent; the Santa Clarita structure is a single unambiguous 'City Council'
chamber ready for roster completion in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/143-santa-clarita-deep-seed/143-01-SUMMARY.md` when done.
</output>
