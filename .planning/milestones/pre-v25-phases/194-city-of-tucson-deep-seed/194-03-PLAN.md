---
phase: 194-city-of-tucson-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["194-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-tucson-headshots.py
  - C:/EV-Accounts/backend/migrations/1297_city_of_tucson_headshots.sql
autonomous: true
requirements: [TUC-01]
must_haves:
  truths:
    - "All 7 officials (Mayor + 6 ward members) have a 600×750 (4:5, Lanczos q90) headshot uploaded to the politician_photos Storage bucket"
    - "Each of the 7 politician_images rows binds to the CURRENT roster member (never a departed member: no Fimbres/Kozachik photos)"
    - "Because tucsonaz.gov is Akamai-WAF-blocked, source portraits come from non-WAF hosts (Wikipedia / campaign sites) resolved via Playwright; photo_license is documented PER image (not a uniform value)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-tucson-headshots.py"
      provides: "crop_to_4_5 → resize 600×750 Lanczos q90 → Storage x-upsert pipeline for 7 officials; source URLs resolved outside the WAF"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1297_city_of_tucson_headshots.sql"
      provides: "7 audit-only politician_images rows (unregistered), UUIDs from Plan 02 manifest, per-image photo_license"
      contains: "politician_images"
  key_links:
    - from: "_tmp-tucson-headshots.py"
      to: "non-WAF image hosts (Wikipedia / campaign sites)"
      via: "requests.get per-official resolved image URL with descriptive User-Agent"
      pattern: "crop_to_4_5"
    - from: "1297_city_of_tucson_headshots.sql"
      to: "essentials.politicians"
      via: "url binds to politician UUID resolved by external_id"
      pattern: "politician_images"
---

<objective>
Acquire and bind 7/7 headshots (Mayor + 6 ward members). Author a gitignored pipeline script that
fetches each official's portrait, crops to 4:5 (crop-first, never distort), resizes to 600×750 Lanczos
q90, and uploads to the `politician_photos` Storage bucket; plus an AUDIT-ONLY migration that inserts
the 7 `politician_images` rows.

CRITICAL DELTA from Pima (Pitfall 3): the official host `tucsonaz.gov` is Akamai-WAF-blocked (403 to
both `curl` and `requests`, any User-Agent) — unlike Pima's un-gated CivicPlus CMS. Source URLs must
therefore be resolved from NON-WAF hosts (Wikipedia, confirmed HTTP 200 for at least Mayor Romero;
ward members' personal campaign sites) via the `/find-headshots` skill's Playwright browser-navigation
flow (`mcp__playwright__browser_navigate` + snapshot + manual image-URL extraction) — this is an
ORCHESTRATOR step. Do NOT retry raw HTTP against tucsonaz.gov (a 403 is a WAF, not an outage) and do
NOT craft evasive request headers to bypass the WAF (T-194-WAF). `photo_license` is documented PER
image (varies by source), NOT a uniform value.

Purpose: Satisfies ROADMAP success criterion #2 (7/7 officials have 600×750 headshots).
Output: `_tmp-tucson-headshots.py` (gitignored `backend/scripts/_*`) + `1297_city_of_tucson_headshots.sql`
(audit-only, unregistered), both applied so all 7 images serve from the CDN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md
@.planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md
@.planning/phases/194-city-of-tucson-deep-seed/194-02-SUMMARY.md

<execution_note>
Depends on Plan 02: the 7 politician UUIDs (external_id → UUID → full_name) captured in
194-02-SUMMARY.md are REQUIRED — the audit migration's CDN url values derive from the politician UUID
({uuid}-headshot.jpg), and the script resolves UUIDs at runtime via `SELECT id FROM
essentials.politicians WHERE external_id=%s`. Executor authors both files; the ORCHESTRATOR resolves
the 7 source image URLs via Playwright (WAF fallback), runs the script, and applies the audit
migration (executor has no DB/Storage/web access). Commit the migration via `git -C "C:/EV-Accounts"`;
the `_tmp-*` script is gitignored and never committed.
</execution_note>

<interfaces>
- Source hosts: NOT tucsonaz.gov (Akamai WAF, 403). Use Wikipedia (200 OK — has Romero) + campaign
  sites (nikkilee.vote, mirandaforward6.com, tucward1.com, etc.) resolved via Playwright. The 7
  resolved image URLs are hardcoded per member into ROSTER at run time (orchestrator supplies them).
- essentials.politician_images columns: exactly (id, politician_id, url, type, photo_license) — NO
  photo_origin_url. type='default'; photo_license PER image (e.g. Wikimedia/CC terms, campaign
  press_use, or operator_supplied) — NOT a uniform 'us_government_work'.
- Storage: bucket politician_photos; canonical path {politician_uuid}-headshot.jpg; x-upsert PUT;
  CDN base https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- ext_id block: -4008001 Romero (Mayor), -4008002 Santa Cruz (W1), -4008003 Cunningham (W2),
  -4008004 Dahl (W3), -4008005 Lee (W4), -4008006 Barajas (W5), -4008007 Schubert (W6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Tucson headshot pipeline script (WAF-fallback sourcing)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-tucson-headshots.py</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md (lines 304-352 — the pipeline pattern: reuse crop_to_4_5/resize_600x750/process_headshot_bytes/upload_to_storage/resolve_politician_id/dry_run_head_check/HEADERS verbatim; ONLY ROSTER + source-acquisition change; the _MISSING sentinel + fail-fast guard)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfall 3 — tucsonaz.gov WAF; Playwright fallback; Wikipedia 200; per-image license discipline)
    - C:/EV-Accounts/backend/scripts/_tmp-pima-supervisors-headshots.py (PRIMARY analog — read in full: reuse crop_to_4_5, resize_600x750, process_headshot_bytes, upload_to_storage, resolve_politician_id, env-load, dry_run_head_check, descriptive-UA HEADERS verbatim; only ROSTER + source-URL builder + count assertions change)
  </read_first>
  <action>
    Copy `_tmp-pima-supervisors-headshots.py` and adapt. Reuse crop_to_4_5 / resize_600x750 /
    process_headshot_bytes (crop-FIRST then resize) / upload_to_storage (x-upsert PUT to
    `politician_photos/{uuid}-headshot.jpg`) / resolve_politician_id (parameterized `external_id=%s`,
    never hardcode a UUID) / the env-load block / dry_run_head_check / the descriptive User-Agent
    HEADERS — VERBATIM. Keep TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=LANCZOS, MIN_DIM=100.

    Build a ROSTER list of 7 dicts (assert len(ROSTER)==7), each with: external_id
    (-4008001..-4008007), full_name, chamber label, photo_url (initially the `_MISSING`/`TODO_`
    sentinel — the ORCHESTRATOR fills the resolved non-WAF image URL per member in Task 3 via
    Playwright), photo_license (PER member — set from the actual source at fill time, NOT a uniform
    value), is_required=True (all 7). Add `source_url_for(member)` returning `member['photo_url']`.

    Keep the `_MISSING = 'TODO_...'` sentinel + `main()` fail-fast guard so a placeholder URL can
    NEVER reach a Storage write — if any ROSTER photo_url is still the sentinel, print the member and
    `sys.exit(1)`. Do a dry-run HEAD per member first (verify 200) before the real batch. On any
    required-member fetch/upload failure, print the member + `sys.exit(1)`. PIL `convert('RGB')` +
    JPEG re-encode optimize=True (strips EXIF/embedded payloads, T-194-IMG). Print a
    {external_id, uuid, cdn_url, photo_license} manifest for the audit migration.

    Add a comment block documenting that the official host is WAF-blocked and source URLs are resolved
    from non-WAF hosts via Playwright (do NOT put a tucsonaz.gov URL in ROSTER, and do NOT add
    header-spoofing logic to defeat a WAF).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-tucson-headshots.py"; test -f "$F" && grep -qF "crop_to_4_5" "$F" && grep -qF "external_id=%s" "$F" && grep -qE "len\(ROSTER\)\s*==\s*7" "$F" && grep -qF "sys.exit(1)" "$F" && ! grep -qF "tucsonaz.gov" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/_tmp-tucson-headshots.py` (gitignored `backend/scripts/_*`).
    - Contains crop_to_4_5, resize_600x750, upload_to_storage; TARGET_SIZE (600, 750); LANCZOS; JPEG quality 90; MIN_DIM 100.
    - ROSTER has exactly 7 members (`assert len(ROSTER)==7`), ext_ids -4008001..-4008007.
    - politician_id resolved via parameterized `external_id=%s` (no f-string/`%`-format into SQL); PIL `convert('RGB')` present.
    - `_MISSING`/`TODO_` sentinel + fail-fast `sys.exit(1)` guard present so no placeholder URL reaches Storage; dry-run HEAD check present; is_required=True on all 7.
    - photo_license is a PER-member field (not a single hardcoded uniform value).
    - NO `tucsonaz.gov` URL anywhere in the script; no WAF header-spoofing logic.
  </acceptance_criteria>
  <done>Pipeline script authored: 7-member ROSTER, crop-first 600×750 pipeline, runtime UUID resolution, sentinel + dry-run + required-failure PAUSE guards, per-image license, no WAF host.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1297_city_of_tucson_headshots.sql</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-PATTERNS.md (lines 240-264 — the audit-only politician_images pattern: header, per-row INSERT ... WHERE NOT EXISTS, exact 5 columns, NO ledger registration; per-image license note)
    - .planning/phases/194-city-of-tucson-deep-seed/194-02-SUMMARY.md (the 7 external_id → UUID → full_name manifest)
    - C:/EV-Accounts/backend/migrations/1289_pima_county_headshots.sql (analog — read the header + first few rows: audit-only, unregistered, WHERE NOT EXISTS idempotency, 5-column shape)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1296 after Plan 02); name this file disk-MAX+1 (provisionally
    `1297_city_of_tucson_headshots.sql`). Author an AUDIT-ONLY migration (NO ledger-registration
    INSERT into supabase_migrations, applied via `psql -f`). The header may note it is audit-only /
    unregistered, that the source is NON-WAF hosts (Wikipedia / campaign sites) resolved via Playwright
    because the official host is WAF-blocked, and that photo_license VARIES per image.

    Emit 7 per-row INSERTs into essentials.politician_images (id, politician_id, url, type,
    photo_license) — columns EXACTLY these five, no photo_origin_url. Each row: id=gen_random_uuid();
    politician_id = `(SELECT id FROM essentials.politicians WHERE external_id = -4008001)` (etc. through
    -4008007); url = the CDN path `{CDN_BASE}/{politician_uuid}-headshot.jpg` using the UUID from the
    Plan 02 manifest; type='default'; photo_license = the PER-IMAGE license value (from the Task 1
    sourcing manifest — the orchestrator finalizes any that differ after Task 3's Playwright pass).
    Guard each with WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id =
    (...same subselect...)) — idempotent. Cover all 7 ext_ids -4008001..-4008007. Do NOT add a
    ledger-registration INSERT.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1297_city_of_tucson_headshots.sql"; test -f "$F" && [ "$(grep -c 'INSERT INTO essentials.politician_images' "$F")" = "7" ] && ! grep -qF "photo_origin_url" "$F" && ! grep -qE "INSERT INTO supabase_migrations" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1297).
    - Grep count of `INSERT INTO essentials.politician_images` = 7.
    - Grep confirms all 7 ext_ids -4008001..-4008007 referenced.
    - Columns are exactly (id, politician_id, url, type, photo_license) — NO `photo_origin_url`.
    - NO `INSERT INTO supabase_migrations` line (audit-only, unregistered).
    - Every row guarded by `WHERE NOT EXISTS` on politician_id (idempotent).
    - photo_license set per row (may differ across the 7).
  </acceptance_criteria>
  <done>Audit-only migration authored with 7 idempotent politician_images rows bound to Plan 02 UUIDs, per-image license, unregistered.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve source URLs via Playwright + run pipeline + apply audit migration + assert 7/7 (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator resolves URLs, runs an existing script, applies an existing migration; no files modified</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md (Per-Task Verification Map — headshots row: count=7 + PIL 600×750 + curl CDN 200)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfall 3 — WAF + Playwright fallback + Wikipedia/campaign hosts + MIN_DIM=100 floor + per-image license)
    - C:/Users/Chris/.claude/commands/find-headshots.md (the Playwright browser-navigation sourcing flow for a WAF-blocked host)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/Storage/web access). First, for each of the 7 officials,
    resolve a usable direct image URL from a NON-WAF host using the `/find-headshots` Playwright flow
    (`mcp__playwright__browser_navigate` + snapshot + image-URL extraction) — Wikipedia for Romero,
    campaign sites (nikkilee.vote, mirandaforward6.com, tucward1.com, etc.) or Wikipedia for the ward
    members. Do NOT fetch tucsonaz.gov (WAF 403) and do NOT spoof headers to bypass a WAF. Record the
    resolved URL + license per official, fill them into the ROSTER `photo_url` / `photo_license`
    fields, and finalize the matching photo_license values in migration 1297.

    Then run `py -3 C:/EV-Accounts/backend/scripts/_tmp-tucson-headshots.py` — it crops/resizes/uploads
    all 7 processed images to Storage and prints the {external_id, uuid, cdn_url, photo_license}
    manifest. If any REQUIRED member cannot be sourced (script exits 1), PAUSE for an operator-supplied
    file (photo_license='operator_supplied'). Then apply the audit migration via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1297_city_of_tucson_headshots.sql`.
    Assert: politician_images count for the 7 ext_ids = 7; `curl -sI` each CDN url → HTTP 200; PIL
    verify a sample (include Mayor Romero and a newly-seated ward member — Barajas W5 or Schubert W6)
    is exactly 600×750. Record results + final manifest (incl. per-image license) in 194-03-SUMMARY.md.
    Commit the audit migration via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4008007 AND -4008001"</automated>
  </verify>
  <acceptance_criteria>
    - Each official's source URL resolved from a NON-WAF host via Playwright (no tucsonaz.gov fetch, no header-spoofing); per-image license recorded.
    - Script run exits 0 (all 7 required images fetched, processed, uploaded) — or an operator file was supplied for any exception.
    - `politician_images` count for the 7 Tucson ext_ids (-4008007..-4008001) = 7.
    - `curl -sI` on each of the 7 CDN URLs returns HTTP 200.
    - PIL dimension check on a sampled downloaded CDN image (incl. Romero + a Dec-2025 new member) = exactly 600×750.
    - Migration 1297's photo_license values match the actual sourced licenses; audit migration NOT registered in the ledger; committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <done>All 7 officials serve a 600×750 CDN headshot bound to the correct current politician row, sourced from non-WAF hosts with per-image licenses; audit migration applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-WAF image hosts (remote) → pipeline | Untrusted remote image bytes enter the pipeline over HTTP |
| pipeline → Storage/DB | Processed image + politician_images row cross into production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-IMG | Tampering | remote image fetch | mitigate | PIL `convert('RGB')` + JPEG re-encode `optimize=True` strips EXIF/embedded/stego payloads; only `image/jpeg` written to Storage |
| T-194-WAF | Tampering | WAF-evasion for a `.gov` host | mitigate | Use the sanctioned Playwright browser-navigation flow (real browser context) rather than crafting evasive request headers against tucsonaz.gov — avoids any appearance of adversarial scraping; grep gate forbids a tucsonaz.gov URL in the script |
| T-194-BIND | Spoofing | politician_images ↔ politician binding | mitigate | Fetch ONLY current-roster URLs; runtime UUID resolution by external_id prevents wrong-person binding on retry; Plan 06 human identity spot-check |
| T-194-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; script itself gitignored (`backend/scripts/_*`); never in a committed migration; parameterized `external_id=%s` query |
| T-194-LIC | Repudiation | image license provenance | mitigate | photo_license recorded PER image (Wikimedia/CC terms, campaign press_use, operator_supplied); documented in the migration header + SUMMARY manifest |
| T-194-WRONG | Spoofing | wrong-but-present image | transfer | Row-count cannot detect a wrong-but-present photo; deferred to Plan 06 human-verify spot-check (esp. the Dec-2025 new members) |
| T-194-SC | Tampering | package installs | accept | No new pip installs — reuses installed Pillow/requests/psycopg2 + already-available Playwright MCP; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- 7/7 politician_images rows for the Tucson ext_id block, each 600×750 at HTTP 200.
- Every image binds to the correct current official; no departed member (Fimbres/Kozachik).
- Source URLs from non-WAF hosts via Playwright; per-image license recorded; audit migration NOT registered.
</verification>

<success_criteria>
ROADMAP #2 (7/7 officials have 600×750 headshots) is TRUE in production; every image binds to the
correct current member; the audit migration is applied and left unregistered.
</success_criteria>

<output>
Create `.planning/phases/194-city-of-tucson-deep-seed/194-03-SUMMARY.md` when done — include the final
{external_id → UUID → cdn_url → photo_license} manifest and any operator-supplied exceptions.
</output>
</content>
