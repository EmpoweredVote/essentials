---
phase: 101-va-state-government-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql
autonomous: true
requirements:
  - VA-GOV-01
tags:
  - virginia
  - state-government
  - chambers
  - sql-migration

must_haves:
  truths:
    - "State of Virginia government row already exists (UUID bf1095e6-8f88-41cd-b758-23c1ba1297b5, geo_id='51', state='VA') and is asserted by the migration pre-flight"
    - "5 chambers are seeded under that government row: Governor, Lieutenant Governor, Attorney General, Virginia Senate, House of Delegates"
    - "Chamber INSERTs are idempotent — re-running the migration produces 0 new rows"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql"
      provides: "VA chambers migration — 5 idempotent INSERTs + government pre-flight"
      contains: "INSERT INTO essentials.chambers"
  key_links:
    - from: "essentials.chambers"
      to: "essentials.governments"
      via: "government_id FK = bf1095e6-8f88-41cd-b758-23c1ba1297b5"
      pattern: "government_id = .SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA'"
---

<objective>
Create and apply migration 300 to seed the 5 State of Virginia chambers (Governor, Lieutenant Governor, Attorney General, Virginia Senate, House of Delegates) under the pre-existing State of Virginia government row. Implements VA-GOV-01.

Purpose: All downstream Wave 2 migrations (executives 301, senators 302, delegates 303) link offices to chamber_ids. The chambers must exist before any office row can be inserted.

Output:
- SQL file: C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql
- DB state: 5 new rows in essentials.chambers, all linked to government_id='bf1095e6-8f88-41cd-b758-23c1ba1297b5'
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-va-state-government-db/101-RESEARCH.md
@.planning/phases/101-va-state-government-db/101-PATTERNS.md
@C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write migration 300_va_government_chambers.sql</name>
  <files>C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/269_md_government_chambers.sql (exact analog; copy structure, substitute MD->VA values)
    - .planning/phases/101-va-state-government-db/101-PATTERNS.md (Pattern Assignments for `migrations/300_va_government_chambers.sql`; VA substitutions list lines 63-71)
    - .planning/phases/101-va-state-government-db/101-RESEARCH.md (Pattern 1, lines 133-176; chamber name/name_formal table lines 167-176)
  </read_first>
  <behavior>
    - Pre-flight DO block raises EXCEPTION if `essentials.governments` count for (name='State of Virginia' AND state='VA') is not exactly 1
    - INSERTing migration on a clean DB adds exactly 5 rows to essentials.chambers
    - Re-running the migration after a successful first apply adds 0 new rows (WHERE NOT EXISTS guards)
    - Every inserted chamber has `government_id` equal to the State of Virginia row id (`bf1095e6-8f88-41cd-b758-23c1ba1297b5`)
    - `slug` column is NEVER referenced in the INSERT column list (GENERATED ALWAYS column)
  </behavior>
  <action>
    Create file `C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql` by adapting migration 269 verbatim with the following exact substitutions:

    1. `BEGIN;` opens the migration, `COMMIT;` closes it.
    2. Pre-flight DO $$ block: assert COUNT(*) FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA' equals exactly 1. RAISE EXCEPTION otherwise with the same message format as migration 269.
    3. Five `INSERT INTO essentials.chambers (id, name, name_formal, government_id) SELECT ... WHERE NOT EXISTS` blocks, one per chamber, using `gen_random_uuid()` for id and the subquery `(SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA')` for government_id. NEVER include `slug` in the column list — it is GENERATED ALWAYS and including it will error.
    4. The five chamber tuples (name, name_formal) are exactly:
       - ('Governor', 'Governor of Virginia')
       - ('Lieutenant Governor', 'Lieutenant Governor of Virginia')
       - ('Attorney General', 'Attorney General of Virginia')
       - ('Virginia Senate', 'Virginia Senate')   — same string for both columns; self-qualifying name precedent (OR/MD pattern)
       - ('House of Delegates', 'Virginia House of Delegates')
    5. Every WHERE NOT EXISTS clause filters on `name = '<chamber name>'` AND `government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA')`.
    6. Do NOT seed Comptroller or State Treasurer — VA has only 3 executives, not 5 like MD.
    7. Do NOT include any `INSERT INTO essentials.governments` statement — the State of Virginia row already exists (verified production state).
    8. Header comment block at top of file must state: source analog = 269_md_government_chambers.sql; phase = 101; requirement = VA-GOV-01; warning that slug is GENERATED ALWAYS and must not be in column list.

    Write the file as UTF-8 without BOM. Save in `C:/EV-Accounts/backend/migrations/`.
  </action>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql` exists
    - File contains exactly 5 `INSERT INTO essentials.chambers` statements
    - File contains exactly 0 occurrences of the literal substring `slug` (case-sensitive)
    - File contains the literal substring `'State of Virginia'` AND `'VA'` (assertion of correct gov name + state)
    - File contains the literal substring `Pre-flight failed: expected exactly 1 State of Virginia` (matches MD 269 error format)
    - File contains exactly 5 occurrences of `WHERE NOT EXISTS` (idempotency guards)
    - Grep for chamber names: `grep -v '^--' 300_va_government_chambers.sql | grep -c "'Governor'"` returns >= 1; same for 'Lieutenant Governor', 'Attorney General', 'Virginia Senate', 'House of Delegates'
    - File contains zero occurrences of `Comptroller` or `Treasurer` (VA has 3 executives, not 5)
    - File does NOT contain `INSERT INTO essentials.governments`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$f='C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql'; if (-not (Test-Path $f)) { exit 1 }; $c=Get-Content $f -Raw; $errors=@(); if (($c -split 'INSERT INTO essentials.chambers').Length - 1 -ne 5) { $errors += 'chamber inserts != 5' }; if ($c -match '\\bslug\\b') { $errors += 'slug found' }; if (-not ($c -match 'State of Virginia')) { $errors += 'missing State of Virginia' }; if (-not ($c -match \"= 'VA'\")) { $errors += 'missing state VA' }; foreach ($n in @('Governor','Lieutenant Governor','Attorney General','Virginia Senate','House of Delegates')) { if (-not ($c -match \"'$n'\")) { $errors += \"missing chamber $n\" } }; if ($c -match 'Comptroller|Treasurer') { $errors += 'forbidden chamber (Comptroller/Treasurer)' }; if ($c -match 'INSERT INTO essentials.governments') { $errors += 'forbidden gov insert' }; if ($errors.Count -gt 0) { Write-Host ('FAIL: ' + ($errors -join '; ')); exit 1 } else { Write-Host 'PASS: 300_va_government_chambers.sql'; exit 0 }"</automated>
  </verify>
  <done>
    SQL file is syntactically clean, contains exactly 5 chamber INSERTs (no Comptroller/Treasurer), pre-flight asserts State of Virginia row exists, and has zero `slug` references in the column list.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply migration 300 to production Supabase and verify</name>
  <files>none (DB-only writes to essentials.chambers)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql (the file produced by Task 1)
    - .planning/phases/101-va-state-government-db/101-RESEARCH.md (Production DB State table, lines 284-300 — confirms gov row exists; Common Pitfalls section)
  </read_first>
  <behavior>
    - After apply, exactly 5 rows in essentials.chambers have government_id='bf1095e6-8f88-41cd-b758-23c1ba1297b5'
    - The chamber name set under that government_id equals exactly {Governor, Lieutenant Governor, Attorney General, Virginia Senate, House of Delegates}
    - Re-applying the migration in the same session adds 0 new rows (idempotency)
    - Pre-flight DO block does NOT raise (because State of Virginia row exists with state='VA')
  </behavior>
  <action>
    Apply the migration via the mcp__supabase-local__execute_sql tool (which IS the production Supabase per memory rule `feedback_supabase_local_is_remote.md`). Note: essentials schema is not exposed in REST API, but mcp__supabase-local__execute_sql runs raw SQL with the postgres role and works for essentials writes.

    1. Read the full content of `C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql` and submit as a single SQL execution call.
    2. Confirm no error returned (pre-flight passed, all 5 INSERTs succeeded).
    3. Run verification query 1 — chamber count:
       `SELECT COUNT(*)::int AS cnt FROM essentials.chambers WHERE government_id = 'bf1095e6-8f88-41cd-b758-23c1ba1297b5'` → must return cnt=5.
    4. Run verification query 2 — chamber name set:
       `SELECT name FROM essentials.chambers WHERE government_id = 'bf1095e6-8f88-41cd-b758-23c1ba1297b5' ORDER BY name` → must return exactly the 5 names: Attorney General, Governor, House of Delegates, Lieutenant Governor, Virginia Senate.
    5. Run verification query 3 — idempotency: re-submit the entire migration SQL a second time. Then re-run query 1 — cnt must still equal 5 (no duplicate rows).
    6. Update `.planning/STATE.md` next-migration counter from 300 to 301 in the Accumulated Context "Next migration" line (file already lists 293; update to 301 to reflect current state after applying 300).

    Do NOT use any psql command via Bash — use the Supabase MCP tool only.
  </action>
  <acceptance_criteria>
    - mcp__supabase-local__execute_sql submitting the migration file returns no error and no exception
    - Query `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='bf1095e6-8f88-41cd-b758-23c1ba1297b5'` returns 5
    - Query `SELECT array_agg(name ORDER BY name) FROM essentials.chambers WHERE government_id='bf1095e6-8f88-41cd-b758-23c1ba1297b5'` returns {Attorney General, Governor, House of Delegates, Lieutenant Governor, Virginia Senate}
    - Re-applying the same migration in the same session leaves the count at 5 (no rows added on second apply)
    - STATE.md Accumulated Context section reflects the migration counter advancement (current value updated to indicate migrations 293-299 + 300 consumed)
  </acceptance_criteria>
  <verify>
    <automated>echo "Manual verification via Supabase MCP query results captured in task action steps 3-5; agent must paste the COUNT and array_agg query outputs into the task completion notes before marking done."</automated>
    <human-check>Confirm Supabase MCP query results match: COUNT=5; chamber names sorted = [Attorney General, Governor, House of Delegates, Lieutenant Governor, Virginia Senate].</human-check>
  </verify>
  <done>
    Migration 300 applied to production Supabase, 5 VA chambers exist under government_id bf1095e6, idempotency confirmed by re-apply, STATE.md updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → production Supabase | SQL migration text is authored locally and sent to production DB via mcp__supabase-local__execute_sql with postgres role privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Tampering | essentials.chambers production rows | mitigate | Pre-flight DO block aborts if State of Virginia government row missing or duplicated; WHERE NOT EXISTS guards prevent duplicate chamber rows on re-run |
| T-101-02 | Spoofing | Government row identity collision | mitigate | Filter `AND state = 'VA'` on government subquery prevents matching 'State of West Virginia' (UUID 74564736) which has overlapping name prefix (per Research Pitfall 5) |
| T-101-03 | Repudiation | Audit trail for chamber creation | accept | essentials.chambers has no created_by/created_at; project does not track per-row provenance for reference data |
| T-101-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; gate non-applicable |
| T-101-04 | DoS | Long migration blocks production | accept | Migration is 5 INSERTs wrapped in BEGIN/COMMIT; sub-second execution time |
</threat_model>

<verification>
After Task 2 completes:

1. `SELECT COUNT(*) FROM essentials.chambers WHERE government_id='bf1095e6-8f88-41cd-b758-23c1ba1297b5'` = 5
2. Chamber name set under that gov id is exactly: Attorney General, Governor, House of Delegates, Lieutenant Governor, Virginia Senate
3. Re-apply of migration 300 produces 0 new rows
4. `SELECT name, name_formal FROM essentials.chambers WHERE government_id='bf1095e6-8f88-41cd-b758-23c1ba1297b5' AND name='Virginia Senate'` returns name=name_formal='Virginia Senate'
5. `SELECT name, name_formal FROM essentials.chambers WHERE government_id='bf1095e6-8f88-41cd-b758-23c1ba1297b5' AND name='House of Delegates'` returns name='House of Delegates', name_formal='Virginia House of Delegates'
</verification>

<success_criteria>
- VA-GOV-01 partially satisfied (chamber piece; gov row was pre-existing)
- 5 chambers seeded with correct names and formal names
- Idempotency verified
- Downstream Wave 2 plans (executives, senators, delegates) unblocked — they can now resolve chamber_id subqueries
</success_criteria>

<output>
Create `.planning/phases/101-va-state-government-db/101-01-SUMMARY.md` documenting:
- Migration 300 SQL file path
- Number of rows inserted (expected 5)
- Verification query results (counts + name set)
- Idempotency re-apply result
- STATE.md update note
</output>
