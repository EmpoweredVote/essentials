---
phase: 194-city-of-tucson-deep-seed
plan: 06
type: execute
wave: 6
depends_on: ["194-01", "194-02", "194-03", "194-04", "194-05"]
files_modified: []
autonomous: false
requirements: [TUC-01, BANR-01]
must_haves:
  truths:
    - "The full production audit block is all-green: 6 X0020 ward geofences (Ward 4/5 faithful MultiPolygons), greenfield City of Tucson government, 7 offices (at-large Mayor + 6 ward members, 1 per district), Vice Mayor annotation on Ward 1 only, 7/7 600×750 headshots, evidence-only cited stances, clean section-split, DB-honest Arizona coverage block, banner CDN 200"
    - "A live address in each of the 6 wards routes to exactly 1 correct ward member PLUS the at-large Mayor, each with the right headshot"
    - "The Tucson banner renders via the new coverage.js Arizona chip"
  artifacts:
    - path: ".planning/phases/194-city-of-tucson-deep-seed/194-06-SUMMARY.md"
      provides: "Full audit results + live-browse sign-off; the phase verification record"
      contains: "audit"
  key_links:
    - from: "live address per ward"
      to: "one ward member profile + the at-large Mayor"
      via: "X0020 geofence PIP routing (X% MTFCC catchall) + G4110 LOCAL_EXEC routing"
      pattern: "ward"
    - from: "coverage.js Arizona 'Tucson' chip"
      to: "downtown-Tucson banner render"
      via: "browse_label → representingCity → getBuildingImages"
      pattern: "tucson"
---

<objective>
Final phase verification. Run the full production audit block covering every prior wave (ward
geofences, government/roster, headshots, stances, banner, coverage) and a blocking live-browse
human-verify that confirms per-ward + Mayor address routing, correct-person headshots, and the banner
rendering via the coverage chip. This is the goal-backward gate for ROADMAP success criteria #1-5
before `/gsd-verify-work`.

Purpose: Proves TUC-01 + BANR-01 are TRUE end-to-end in production — not just that migrations were
authored, but that a real Tucson resident sees their ONE ward member plus the city-wide Mayor, each
with a full compass, a correct headshot, and the city banner.
Output: `.planning/phases/194-city-of-tucson-deep-seed/194-06-SUMMARY.md` — the phase verification
record with the audit results + operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md
@.planning/phases/194-city-of-tucson-deep-seed/194-01-SUMMARY.md
@.planning/phases/194-city-of-tucson-deep-seed/194-02-SUMMARY.md
@.planning/phases/194-city-of-tucson-deep-seed/194-03-SUMMARY.md
@.planning/phases/194-city-of-tucson-deep-seed/194-04-SUMMARY.md
@.planning/phases/194-city-of-tucson-deep-seed/194-05-SUMMARY.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / live-browser access. Both tasks are ORCHESTRATOR-RUN /
human-verify: the inline orchestrator runs the `psql`/`curl` audit block, and the operator performs the
live browse on essentials.empowered.vote. No files are modified. Per
`feedback_no_playwright_on_user_live_browser`, the live browse is read-only + operator-driven — never
wipe localStorage on the live page.
</execution_note>

<interfaces>
- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- Live app: https://essentials.empowered.vote ; Tucson browse via the new Arizona coverage chip
  (browse_label 'Tucson', browseGovernmentList ['0477000']).
- ext_ids -4008001..-4008007 (Romero Mayor + Santa Cruz/Cunningham/Dahl/Lee/Barajas/Schubert Wards 1-6).
- Routing: 6 ward members via X0020 LOCAL geofences (X% MTFCC catchall); at-large Mayor via the new
  LOCAL_EXEC/G4110/0477000 district on the existing Phase-190 city geofence.
- Multi-ring: Ward 4 = 2-polygon MultiPolygon, Ward 5 = 7-polygon MultiPolygon — a resident in a
  detached Ward 4/5 parcel must still route to the right ward.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full production audit block (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB/CDN assertions; no files modified</files>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md (the full Per-Task Verification Map — all deliverable checks)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfalls 1, 5, 8 — multi-ring geometry, the new LOCAL_EXEC row, 36-not-44 topic scope so the audit does not misflag)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). Run the full audit via `psql "$DATABASE_URL" -tAc` +
    `curl -sI`, recording each result in 194-06-SUMMARY.md:
    (a) 6 geofences mtfcc='X0020' state='az', geo_id tucson-az-ward-1..6, all ST_IsValid; Ward 4
    ST_NumGeometries=2, Ward 5 ST_NumGeometries=7, Wards 1/2/3/6 =1 (no dropped rings);
    (b) exactly 1 greenfield government 'City of Tucson, Arizona, US' (geo_id 0477000, type City);
    (c) the NEW LOCAL_EXEC/G4110/0477000/az district row exists; 7 offices under 'City Council' — 1 on
    the LOCAL_EXEC/G4110 district + 6 on LOCAL/X0020 wards, each district holding exactly 1 office
    (GROUP BY HAVING count<>1 → 0);
    (d) Vice Mayor: exactly 1 office carries `(Vice Mayor)` and it is the Ward-1/-4008002 (Santa Cruz) seat;
    (e) 7/7 politician_images rows; `curl -sI` each of the 7 CDN URLs → HTTP 200; PIL-verify a sample
    is 600×750;
    (f) stances: 0 uncited (every answer has a context row with non-empty sources), 0 judicial-* rows,
    all values in [1.0,5.0] (do NOT assert a fixed per-official count — honest blanks expected);
    (g) section-split: 0 offices reachable from the 7 Tucson districts under any non-Tucson government;
    (h) coverage.js NEW Arizona block present with a 'Tucson' hasContext:true chip; buildingImages.js
    'tucson' entry present; banner `curl -sI cities/tucson.jpg` → HTTP 200.
    ANY red assertion blocks phase verification — surface it for gap closure, do NOT hand-wave.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0020' AND state='az')=6 AND (SELECT public.ST_NumGeometries(geometry) FROM essentials.geofence_boundaries WHERE geo_id='tucson-az-ward-4')=2 AND (SELECT public.ST_NumGeometries(geometry) FROM essentials.geofence_boundaries WHERE geo_id='tucson-az-ward-5')=7 AND (SELECT count(*) FROM essentials.governments WHERE name='City of Tucson, Arizona, US' AND geo_id='0477000')=1 AND (SELECT count(*) FROM essentials.districts WHERE geo_id='0477000' AND district_type='LOCAL_EXEC' AND state='az' AND mtfcc='G4110')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE (d.mtfcc='X0020' AND d.district_type='LOCAL' AND d.state='az') OR (d.geo_id='0477000' AND d.mtfcc='G4110' AND d.district_type='LOCAL_EXEC'))=7 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc IN ('X0020','G4110') AND o.title LIKE '%(Vice Mayor)%')=1 AND (SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4008007 AND -4008001)=7 AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN inform.compass_topics t ON t.id=pa.topic_id WHERE p.external_id BETWEEN -4008007 AND -4008001 AND t.topic_key LIKE 'judicial-%') AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id BETWEEN -4008007 AND -4008001 AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id AND array_length(pc.sources,1) IS NOT NULL))"</automated>
  </verify>
  <acceptance_criteria>
    - Combined boolean SELECT returns `t` (6 geofences incl. Ward4=2/Ward5=7 + 1 greenfield gov + new LOCAL_EXEC row + 7 offices + 1 Vice-Mayor annotation + 7 headshots + 0 judicial stance rows + 0 uncited stance rows).
    - Each of the 7 districts holds exactly 1 office (no HAVING count<>1 violation); Vice Mayor annotation on the Ward-1/-4008002 seat only.
    - Section-split query returns 0 rows.
    - `curl -sI` on the 7 headshot CDN URLs and cities/tucson.jpg all return HTTP 200; a sampled headshot is 600×750.
    - coverage.js NEW Arizona 'Tucson' hasContext:true chip + buildingImages.js 'tucson' entry present.
    - All VALIDATION.md Per-Task Map checks recorded green in 194-06-SUMMARY.md.
  </acceptance_criteria>
  <done>Full production audit is all-green across ward geofences (incl. multi-ring), government/roster, headshots, stances, banner, and coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-browse verification — per-ward + Mayor routing + correct-person photos + banner render</name>
  <read_first>
    - .planning/phases/194-city-of-tucson-deep-seed/194-VALIDATION.md (Manual-Only Verifications table — routing, correct-person headshot, roster-currency, banner render)
    - .planning/phases/194-city-of-tucson-deep-seed/194-RESEARCH.md (§ Common Pitfall 1 — a detached Ward 4/5 parcel must still route correctly; banner renders via the coverage chip)
  </read_first>
  <action>Blocking human-verify checkpoint — no executor/DB action beyond the operator's live browse. The operator confirms per-ward + Mayor routing, correct-person headshots, full compass, and banner render on essentials.empowered.vote (read-only browse; never wipe localStorage per feedback_no_playwright_on_user_live_browser). See &lt;how-to-verify&gt;.</action>
  <what-built>
    All 7 officials are seeded — an at-large Mayor on the new LOCAL_EXEC/G4110 district + 6 ward members
    on their own X0020 ward geofences (Ward 4/5 faithful MultiPolygons), 600×750 headshots,
    evidence-only stances, a downtown-Tucson banner, and a coverage chip in a new Arizona block. This
    checkpoint confirms a real resident's experience: address → their ONE ward member PLUS the city-wide
    Mayor, right people, right photos, compasses populated, and the city banner rendering via the browse
    chip.
  </what-built>
  <how-to-verify>
    1. On https://essentials.empowered.vote, enter a known full street address in each of the 6 wards
       (one per ward — include a coordinate inside a detached Ward 4 or Ward 5 parcel if known, to
       confirm the multi-ring load routes it correctly) and confirm the resident sees exactly ONE ward
       council member (the correct current person: Santa Cruz W1 / Cunningham W2 / Dahl W3 / Lee W4 /
       Barajas W5 / Schubert W6) PLUS the at-large Mayor (Regina Romero) — not all 6 ward members.
    2. Confirm each official's headshot depicts the RIGHT current person (spot-check especially the
       Dec-2025 new members Barajas W5 + Schubert W6, and the Vice Mayor label on Santa Cruz W1) and
       their compass shows evidence-only stances (cited, honest blanks where no evidence).
    3. Confirm the Ward 1 profile surfaces the Vice Mayor title annotation; confirm party is NOT
       displayed anywhere (antipartisan, D-04).
    4. Browse the City of Tucson via the coverage chip (browse_label 'Tucson') and confirm the
       downtown-Tucson banner renders (distinct from the Pima Catalinas + Phoenix skyline banners).
    5. Report any wrong-ward routing, missing Mayor, wrong-person photo, missing compass, party leak, or
       banner failure as a gap for closure.
  </how-to-verify>
  <acceptance_criteria>
    - Each of the 6 sampled ward addresses returns exactly 1 correctly-labeled ward member PLUS the at-large Mayor (the right current people).
    - A detached Ward 4/5 parcel address (if available) routes to the correct ward (multi-ring load validated live).
    - All 7 headshots depict the correct current person (Barajas W5 + Schubert W6 spot-checked); Ward 1 shows the Vice Mayor annotation; party is not displayed.
    - Each official's compass shows evidence-only cited stances with honest blanks.
    - The Tucson banner renders via the coverage-chip browse.
    - Operator sign-off recorded in 194-06-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once per-ward + Mayor routing, correct-person headshots (incl. Vice Mayor label), populated compasses, no party leak, and the banner render are all confirmed live — or list the gaps to close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production DB/CDN → audit | Read-only assertions observe the live end-state |
| live app → operator | The operator observes real routing/rendering behavior on essentials.empowered.vote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-FALSE | Repudiation | audit false-positive (authored-not-applied) | mitigate | Audit reads live production tables + CDN 200s, not files; any red assertion blocks the phase |
| T-194-WRONG | Spoofing | wrong-but-present photo / wrong-ward routing | mitigate | Task 2 human live-browse spot-checks person + ward + Mayor (row-count cannot detect this); explicit detached-parcel routing check for the multi-ring wards |
| T-194-MISFLAG | Tampering | audit misflag (44-topic / 0477000 collision) | mitigate | Audit scopes to 36 non-judicial topics and X0020/G4110 districts with district_type + state, so honest completeness is not misread as a gap |
| T-194-PARTY | Information disclosure | party leaking onto a profile | mitigate | Task 2 confirms party is not displayed anywhere (antipartisan, D-04) |
| T-194-LSWIPE | Tampering | live-browser localStorage wipe | accept | Read-only operator browse only; no Playwright localStorage manipulation on the live page (feedback_no_playwright_on_user_live_browser) |
| T-194-SC | Tampering | package installs | accept | No installs — read-only psql/curl; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Full audit all-green (6 ward geofences incl. Ward4=2/Ward5=7 / greenfield gov / new LOCAL_EXEC row / 7 offices 1-per-district / Vice Mayor on Ward 1 / 7-7 headshots / cited non-judicial stances / clean section-split / DB-honest Arizona chip / banner 200).
- Live per-ward routing → 1 correct ward member + the at-large Mayor each; detached-parcel routing validated; correct-person headshots; populated compasses; no party leak; banner renders via the browse chip.
- Operator sign-off recorded.
</verification>

<success_criteria>
All 5 ROADMAP success criteria are TRUE end-to-end in production and operator-confirmed live —
TUC-01 + BANR-01 satisfied, phase ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/194-city-of-tucson-deep-seed/194-06-SUMMARY.md` when done — the phase
verification record: the full audit results (all checks) + the live-browse operator sign-off.
</output>
</content>
