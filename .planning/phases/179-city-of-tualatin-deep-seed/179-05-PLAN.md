---
phase: 179-city-of-tualatin-deep-seed
plan: 05
type: execute
wave: 4
depends_on: ["179-03", "179-04"]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/buildingImages.js
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WASH-05]

must_haves:
  truths:
    - "A legally-licensed 1700×540 community banner for Tualatin is uploaded to Supabase Storage cities/tualatin.jpg"
    - "buildingImages.js CURATED_LOCAL has a lowercase 'tualatin' key pointing at the banner CDN URL, with an attribution comment (verified CC BY-SA version)"
    - "coverage.js Oregon block has a 'Tualatin' entry with browseGovernmentList ['4174950'] (CORRECTED geo_id, never 4175200), browseStateAbbrev 'OR', hasContext:true, alphabetically between Troutdale and Wood Village (NOT next to Tigard)"
    - "npm run build passes with both edits"
    - "The live browse link (browse_geo_id=4174950) surfaces Mayor Bubenik first + all 6 numbered-Position councilors with headshots + stances, the community banner renders (not the tier gradient), and no party label appears"
    - "Decision coverage — D-10: legally-licensed Wikimedia-first banner, no AI images, banner-asset-pipeline followed to cities/tualatin.jpg; D-11: banner asset work lives in this final surfacing plan; D-12: coverage.js Oregon block Tualatin entry with hasContext:true (corrected geo_id 4174950, not CONTEXT's 4175200); D-13: live browse link provided at completion (browse_geo_id=4174950&browse_mtfcc=G4110); D-15: Tualatin Commons subject priority hint honored"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "Tualatin purple hasContext chip in the Oregon COVERAGE_STATES block"
      contains: "Tualatin"
    - path: "C:/Transparent Motivations/essentials/src/lib/buildingImages.js"
      provides: "tualatin CURATED_LOCAL banner entry"
      contains: "tualatin"
  key_links:
    - from: "coverage.js Oregon areas"
      to: "browse route (browse_geo_id=4174950)"
      via: "browseGovernmentList entry"
      pattern: "label: 'Tualatin'"
    - from: "buildingImages.js CURATED_LOCAL 'tualatin'"
      to: "offices.representing_city='Tualatin' (set inline in mig 1169)"
      via: "getBuildingImages() lowercased substring match"
      pattern: "tualatin:"
---

<objective>
Close the phase: (1) source, process, and upload a legally-licensed 1700×540 community banner for
Tualatin; (2) wire it via the `tualatin` CURATED_LOCAL key in buildingImages.js; (3) surface Tualatin
in the Oregon block of coverage.js with the purple hasContext chip, using the CORRECTED geo_id
4174950. The banner renders because mig 1169 set `offices.representing_city='Tualatin'` inline (no
backfill needed).

Purpose: Satisfy WASH-05's purple hasContext chip criterion + the community banner standing
constraint, and verify the full roster end-to-end on the live app.
Output: banner in Storage (cities/tualatin.jpg) + two essentials-repo edits + passing build +
live-browse confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-RESEARCH.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-03-SUMMARY.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-04-SUMMARY.md
@src/lib/coverage.js
@src/lib/buildingImages.js
@docs/banner-asset-pipeline.md

**This is the essentials repo** — coverage.js and buildingImages.js are committed HERE (`git`), NOT
in C:/EV-Accounts. The purple hasContext chip and the banner-render path are already built; only the
data row + banner asset + CURATED_LOCAL key are new. The banner upload uses the existing committed
scripts scripts/banners/process_banner.py + upload_banner.py — no new script file.

**TAILWIND HYGIENE:** do NOT introduce raw Windows `\` paths into any committed file (Tailwind v4
scans .md/.js and a `\f...` sequence crashes the prod build). Use forward-slash CDN URLs only. Never
write the wrong geo_id `4175200` — the coverage.js entry and browse link use 4174950.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Orchestrator sources + processes + uploads the community banner</name>
  <decision>Which legally-licensed wide photo becomes the Tualatin community banner</decision>
  <context>
    Priority hint (D-15): Tualatin Commons lake plaza / Tualatin River civic horizon. RESEARCH
    identified a strong primary candidate with a directly-confirmed license — commons.wikimedia.org
    "File:Tualatin Commons daytime.JPG" (M.O. Stevens / Aboutmovies — the same photographer credited on
    Tigard's banner, 3655×2345, CC BY-SA 3.0 confirmed via the Wikimedia imageinfo API; matches D-15's
    first-listed hint exactly, crops cleanly to 3.15:1 by trimming height to ≈1160px, no upscale). VIEW
    the actual image before finalizing (RESEARCH used metadata-only judgment). Two alternates exist if
    the Commons image proves compositionally unsuitable, but their licenses are NOT yet verified —
    confirm the license directly before use: "Panorama of Tualatin River and Railway Bridge …" (4912×1080,
    Adam Jones) and "Tualatin River at Browns Ferry Park Tualatin.jpg" (1600×1200, Aboutmovies). A clean
    license + crops-well-to-3.15:1 beats subject preference. Wikimedia Commons FIRST, Unsplash fallback.
    NO AI-generated images; no baked-in text/graphics. This is a decision checkpoint because the
    subject/source/license is a human-judgment call that drives the attribution comment. Follow
    docs/banner-asset-pipeline.md Stages 1–8. Run:
    `python scripts/banners/process_banner.py --input <raw> --output <processed_1700x540.jpg>` then
    `python scripts/banners/upload_banner.py --file <processed_1700x540.jpg> --path cities/tualatin.jpg`.
    Record the final Title | Author | License (with the verified CC BY-SA version) for the attribution
    comment used in Task 2.
  </context>
  <options>
    <option id="wikimedia-commons">
      <name>Wikimedia "Tualatin Commons daytime.JPG" (research-identified, preferred)</name>
      <pros>License directly confirmed (CC BY-SA 3.0 via imageinfo API); 3655×2345 high-res; matches D-15's first hint (Commons lake plaza); crops cleanly to 3.15:1 with no upscale; same photographer as the Tigard banner</pros>
      <cons>2009 upload — view the actual composition at 1700×540 before finalizing (metadata-only judgment so far)</cons>
    </option>
    <option id="wikimedia-river">
      <name>Alternate Wikimedia Commons Tualatin River civic-horizon photo (D-15 second hint)</name>
      <pros>Matches D-15's river-horizon alternative; one candidate is 4912×1080 (very wide)</pros>
      <cons>Licenses NOT yet verified — must confirm CC license directly before use; the wide one needs width-cropping</cons>
    </option>
    <option id="unsplash">
      <name>Unsplash fallback photo</name>
      <pros>Higher chance of a wide, crop-friendly horizon shot</pros>
      <cons>Attribution/license differs from Wikimedia convention; confirm Unsplash license terms</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen source + final Title | Author | License string (verified CC BY-SA version), confirming cities/tualatin.jpg uploaded (CDN URL returns 200).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner in buildingImages.js and surface Tualatin in coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/buildingImages.js, C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - src/lib/buildingImages.js lines 107–114 (CURATED_LOCAL block + attribution comments, incl. beaverton/hillsboro/tigard entries)
    - src/lib/coverage.js lines 96–108 (the Oregon block — Beaverton…Wood Village, incl. Troutdale + Wood Village)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md §"src/lib/coverage.js (config edit …)" and §"src/lib/buildingImages.js (config edit …)" (exact inserts)
  </read_first>
  <action>
    Edit 1 — buildingImages.js: add a lowercase `tualatin` key to CURATED_LOCAL pointing at
    `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/tualatin.jpg`,
    placed alongside the existing `beaverton` + `hillsboro` + `tigard` entries. Add an attribution
    comment line above CURATED_LOCAL in the existing format `//   tualatin - <Title> | <Author> | <License>`
    using the string recorded in Task 1 (default per RESEARCH: `Tualatin Commons daytime | M.O. Stevens
    (Aboutmovies) | CC BY-SA 3.0`, with the verified version). The lowercase key is required because
    getBuildingImages() lowercases offices.representing_city and does a substring match; mig 1169 set
    representing_city='Tualatin' inline so this resolves at first browse.
    Edit 2 — coverage.js: insert `{ label: 'Tualatin', browseGovernmentList: ['4174950'],
    browseStateAbbrev: 'OR', hasContext: true }` into the Oregon `areas` array, alphabetically BETWEEN
    'Troutdale' and 'Wood Village' ("Tr" < "Tu" < "Wo" — a DIFFERENT slot than Tigard's, which sits
    between Portland and Troutdale; do NOT place Tualatin next to Tigard). Match the existing column
    alignment of surrounding rows. Use geo_id '4174950' (NEVER 4175200). Change nothing else in either
    file — no other state blocks, no other CURATED_LOCAL keys. No raw Windows `\` paths anywhere
    (forward-slash CDN URLs only).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has `tualatin:` → the cities/tualatin.jpg CDN URL
    - An attribution comment `//   tualatin - ... | ... | ...` is present above CURATED_LOCAL with the verified CC BY-SA version
    - coverage.js Oregon areas has 'Tualatin' between 'Troutdale' and 'Wood Village' (NOT next to Tigard)
    - Entry is exactly { label: 'Tualatin', browseGovernmentList: ['4174950'], browseStateAbbrev: 'OR', hasContext: true }
    - The literal '4175200' appears nowhere; no other lines changed in either file; no raw `\` path introduced
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Banner wired + Tualatin surfaced with corrected geo_id 4174950; production build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-browse end-to-end verification + commit</name>
  <what-built>The Tualatin banner (Storage) + buildingImages.js key + coverage.js purple chip.</what-built>
  <how-to-verify>
    1. Confirm `npm run build` passed (Task 2 gate).
    2. Browse essentials.empowered.vote/results?browse_geo_id=4174950&browse_mtfcc=G4110 and confirm:
       - Mayor Frank Bubenik sorts FIRST, all 6 numbered-Position councilors follow (Positions 1–6:
         Reyes/Sacco/Brooks/Hillier/Gonzalez/Pratt); no empty LOCAL section; no section-split.
       - Headshots render 600×750 with no overlays (7/7 expected); compass stances visible; NO party
         label (antipartisan).
       - The Local section shows the Tualatin community banner photo (NOT the tier gradient fallback) —
         this confirms representing_city='Tualatin' (mig 1169) + the lowercase CURATED_LOCAL key resolve.
       - The purple hasContext chip appears for Tualatin.
    3. Optionally enter a real Tualatin address and confirm the same roster resolves via /representatives/me.
    4. Commit both files in the essentials repo: `git add src/lib/coverage.js src/lib/buildingImages.js && git commit` (on a branch if the harness is on the default branch; end the message with the Co-Authored-By trailer).
  </how-to-verify>
  <acceptance_criteria>
    - Live browse shows Mayor Bubenik first + 6 numbered-Position councilors, no empty LOCAL section, no section-split
    - Headshots 600×750 no overlays (7/7); stances visible; no party label
    - Community banner renders (not gradient fallback)
    - Purple hasContext chip shows for Tualatin
    - coverage.js + buildingImages.js committed in the essentials repo
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the live browse + banner, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Banner sourced from Wikimedia Commons / Unsplash |
| operator → Supabase Storage | Processed banner uploaded to public bucket cities/tualatin.jpg |
| build pipeline → prod | coverage.js + buildingImages.js edits shipped via the essentials build/deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-17 | Spoofing/IP | Unlicensed or AI-generated banner image | mitigate | Wikimedia-first with recorded Title/Author/License (primary candidate CC BY-SA 3.0 confirmed via imageinfo API; alternates' licenses verified before use); NO AI-generated images (pipeline forbids); decision checkpoint records the license string |
| T-179-18 | Tampering | Wrong geo_id 4175200 in the coverage.js browse entry → chip routes nowhere | mitigate | Entry hardcodes 4174950; acceptance criteria assert 4175200 appears nowhere; live-browse gate confirms the roster resolves |
| T-179-19 | Tampering | Malformed coverage.js/buildingImages.js breaks the prod build | mitigate | `npm run build` gate before commit; additive edits only |
| T-179-20 | Denial of Service | Windows `\` path in committed file crashes Tailwind scan | mitigate | Edits add only forward-slash CDN URLs + data rows; no raw backslash introduced |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; banner scripts (process_banner.py/upload_banner.py) are pre-existing committed tooling |
</threat_model>

<verification>
- `npm run build` exits 0; banner uploaded + rendered; live browse surfaces Mayor-first roster with
  chip, stances, headshots, banner, no section-split, no party label; both files committed.
</verification>

<success_criteria>
- Tualatin visible with the purple hasContext chip + community banner in the Oregon block (corrected
  geo_id 4174950); build green; live browse confirms the full roster + banner end-to-end.
</success_criteria>

<output>
Create `.planning/phases/179-city-of-tualatin-deep-seed/179-05-SUMMARY.md` when done, recording the
banner source/license and the live-browse confirmation. This is the final plan — note WASH-05 is
satisfied end-to-end.
</output>
