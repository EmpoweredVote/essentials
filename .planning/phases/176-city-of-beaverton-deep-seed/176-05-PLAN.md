---
phase: 176-city-of-beaverton-deep-seed
plan: 05
type: execute
wave: 4
depends_on: ["176-03", "176-04"]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WASH-02]

must_haves:
  truths:
    - "Beaverton appears in the Oregon block of coverage.js with browseGovernmentList ['4105350'] and hasContext:true"
    - "Beaverton is the first (alphabetical) entry in the Oregon areas array, before Fairview"
    - "npm run build passes with the new entry"
    - "The live browse link surfaces Mayor Beaty first + all 6 councilors with stances"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "Beaverton purple hasContext chip in the Oregon COVERAGE_STATES block"
      contains: "Beaverton"
  key_links:
    - from: "coverage.js Oregon areas"
      to: "browse route (browse_geo_id=4105350)"
      via: "browseGovernmentList entry"
      pattern: "label: 'Beaverton'"
---

<objective>
Surface Beaverton in the app by adding one data row to the Oregon block of `src/lib/coverage.js`,
reusing the already-built generic purple `hasContext` chip. This is the ONLY frontend change in the
phase — no UI-SPEC, no component work (matches Phase 175 and all prior deep-seeds).

Purpose: Satisfy WASH-02 Success Criterion 4 (Beaverton surfaces with the purple hasContext chip
in the Oregon block).
Output: One-line coverage.js edit + a passing `npm run build` + live-browse confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-03-SUMMARY.md
@.planning/phases/176-city-of-beaverton-deep-seed/176-04-SUMMARY.md
@src/lib/coverage.js

**This is the essentials repo** — coverage.js is committed here (`git`), NOT in C:/EV-Accounts.
The purple hasContext chip is generic and already built; only the data row is new.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Beaverton to the Oregon block of coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - src/lib/coverage.js lines 95–105 (the existing Oregon block — Fairview…Wood Village)
    - .planning/phases/176-city-of-beaverton-deep-seed/176-PATTERNS.md §"src/lib/coverage.js" (exact insert)
  </read_first>
  <action>
    Insert a single new entry as the FIRST item in the Oregon `areas` array (alphabetically before
    'Fairview'): `{ label: 'Beaverton', browseGovernmentList: ['4105350'], browseStateAbbrev: 'OR',
    hasContext: true }`. Match the existing column alignment/formatting of the surrounding rows. Change
    nothing else — do not touch Fairview/Gresham/Portland or any other state block. geo_id is 4105350
    (NOT 4105000).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - coverage.js Oregon areas array has 'Beaverton' as the first entry
    - Entry is exactly { label: 'Beaverton', browseGovernmentList: ['4105350'], browseStateAbbrev: 'OR', hasContext: true }
    - No other lines changed
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Beaverton row added; production build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-browse verification + commit</name>
  <what-built>The Beaverton purple hasContext chip in coverage.js (Oregon block).</what-built>
  <how-to-verify>
    1. Confirm `npm run build` passed (Task 1 automated gate).
    2. Browse essentials.empowered.vote/results?browse_geo_id=4105350&browse_mtfcc=G4110 — confirm Mayor Beaty sorts FIRST, all 6 councilors follow, no empty LOCAL section, no section-split, headshots render at 600x750 with no overlays, compass stances visible, and NO party label appears (antipartisan).
    3. Optionally enter a real Beaverton address and confirm the same roster resolves via /representatives/me.
    4. Commit coverage.js in the essentials repo.
  </how-to-verify>
  <acceptance_criteria>
    - Live browse shows Mayor first + 6 councilors, no empty LOCAL section, no section-split
    - Headshots render 600x750, no overlays; compass stances visible; no party label
    - coverage.js committed in the essentials repo
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the live browse, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → prod | coverage.js edit shipped via the essentials build/deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-11 | Tampering | Malformed coverage.js breaks the prod build | mitigate | `npm run build` gate before commit; single-line additive edit |
| T-176-12 | Denial of Service | Windows `\` path in committed file crashes Tailwind scan | accept | Edit adds no file paths — pure data row (geo_id string); no raw backslash introduced |
| T-176-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- `npm run build` exits 0; live browse surfaces Mayor-first roster with chip, stances, headshots,
  no section-split, no party label.
</verification>

<success_criteria>
- Beaverton visible with the purple hasContext chip in the Oregon block; build green; live browse
  confirms the full roster end-to-end.
</success_criteria>

<output>
Create `.planning/phases/176-city-of-beaverton-deep-seed/176-05-SUMMARY.md` when done.
</output>
