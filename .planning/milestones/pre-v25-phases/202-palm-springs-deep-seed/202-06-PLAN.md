---
phase: 202-palm-springs-deep-seed
plan: 06
type: execute
wave: 6
depends_on: ["202-01", "202-02", "202-03", "202-04", "202-05"]
files_modified: []
autonomous: false
requirements: [CV-02, BANR-01]
must_haves:
  truths:
    - "The full production audit block is all-green: 5 X0022 geofences, City of Palm Springs government, 5 council offices (1 per district), exactly 1 Mayor + 1 Mayor Pro Tem, 5/5 600×750 headshots, evidence-only cited stances, clean section-split, DB-honest coverage chip, banner CDN 200"
    - "A live address in each of the 5 districts routes to exactly 1 correct councilmember with the right headshot"
    - "The already-shipped Palm Springs city banner renders via the coverage.js browse chip"
  artifacts:
    - path: ".planning/phases/202-palm-springs-deep-seed/202-06-SUMMARY.md"
      provides: "Full audit results + live-browse sign-off; the phase verification record"
      contains: "audit"
  key_links:
    - from: "live address per district"
      to: "one Palm Springs councilmember profile"
      via: "X0022 geofence PIP routing"
      pattern: "councilmember"
    - from: "coverage.js 'Palm Springs' chip"
      to: "the already-live cities/palm-springs.jpg banner render"
      via: "browse_label → representingCity → getBuildingImages"
      pattern: "palm springs"
---

<objective>
Final phase verification. Run the full production audit block covering every prior wave (geofences,
government/roster with Mayor/Mayor-Pro-Tem titles, headshots, stances, coverage chip, already-shipped
banner) and a blocking live-browse human-verify that confirms per-district address routing, correct
headshots, and the banner rendering via the coverage chip. Goal-backward gate for ROADMAP success
criteria #1-5 before `/gsd-verify-work`.

Purpose: Proves CV-02 + BANR-01 are TRUE end-to-end in production — a real Palm Springs resident sees
their one councilmember with a full evidence-only compass, a correct headshot, and the city banner.
Output: `.planning/phases/202-palm-springs-deep-seed/202-06-SUMMARY.md` — the phase verification record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-palm-springs-deep-seed/202-CONTEXT.md
@.planning/phases/202-palm-springs-deep-seed/202-01-SUMMARY.md
@.planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md
@.planning/phases/202-palm-springs-deep-seed/202-03-SUMMARY.md
@.planning/phases/202-palm-springs-deep-seed/202-04-SUMMARY.md
@.planning/phases/202-palm-springs-deep-seed/202-05-SUMMARY.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / live-browser access. Both tasks are ORCHESTRATOR-RUN /
human-verify: the inline orchestrator runs the `psql`/`curl` audit block, and the operator performs the
live browse on essentials.empowered.vote. No files are modified. Per
`feedback_no_playwright_on_user_live_browser`, the live browse is read-only + operator-driven — never
wipe localStorage on the live page.
</execution_note>

<interfaces>
- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- Banner (already live, Ph201): .../politician_photos/cities/palm-springs.jpg
- Live app: https://essentials.empowered.vote ; browse via the coverage chip (browse_label
  'Palm Springs', browseGovernmentList ['0655254']) or directly /results?browse_geo_id=0655254.
- ext_ids: the -401Nxxx block assigned in Plan 02 (from 202-02-SUMMARY.md manifest).
- Section-split guard: no office reachable from the 5 X0022 LOCAL districts under any government other
  than City of Palm Springs; the pre-existing G4110 0655254 city boundary row must NOT carry a council office.
- Title-on-seat: exactly 1 office title='Mayor' (Soto D4) + exactly 1 title='Mayor Pro Tem' (Ready D5);
  no LOCAL_EXEC row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full production audit block (ORCHESTRATOR-RUN)</name>
  <files>N/A — read-only DB/CDN assertions; no files modified</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (§ Section-split / district-collision guard lines 422-429 — X0022 LOCAL scoping; never bare geo_id='0655254')
    - .planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md (the roster + ext_id block + Mayor/MPT seats for the title assertion)
    - .planning/phases/202-palm-springs-deep-seed/202-05-SUMMARY.md (coverage chip confirmation + buildingImages.js unchanged)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). Run the full audit via `psql "$DATABASE_URL" -tAc`
    + `curl -sI`, recording each result in 202-06-SUMMARY.md:
    (a) 5 geofences mtfcc='X0022' state='ca', geo_id palm-springs-ca-council-district-1..5, all ST_IsValid;
    (b) exactly 1 government 'City of Palm Springs, California, US' (geo_id 0655254, type City);
    (c) 5 offices under 'City Council', each LOCAL X0022 district holding exactly 1 office (GROUP BY
    HAVING count<>1 → 0); no LOCAL_EXEC Mayor office;
    (d) exactly 1 office title='Mayor' + exactly 1 office title='Mayor Pro Tem' + 3 title='Councilmember';
    appointed count among the 5 = the confirmed count (0 per recon);
    (e) 5/5 politician_images rows (type='default'); `curl -sI` each of the 5 CDN URLs → HTTP 200;
    PIL-verify a sample is 600×750;
    (f) stances: 0 uncited (every answer has a context row with non-empty sources), 0 judicial-* rows,
    all values in [1.0,5.0] (do NOT assert a fixed per-member count — honest blanks expected);
    (g) section-split: 0 offices reachable from the 5 X0022 districts under any non-Palm-Springs government;
    the pre-existing G4110 0655254 city row carries no council office;
    (h) coverage.js 'Palm Springs' chip present with hasContext:true; buildingImages.js 'palm springs'
    entry present and UNCHANGED; banner `curl -sI cities/palm-springs.jpg` → HTTP 200.
    ANY red assertion blocks phase verification — surface it for gap closure, do NOT hand-wave.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0022' AND state='ca')=5 AND (SELECT count(*) FROM essentials.governments WHERE name='City of Palm Springs, California, US' AND geo_id='0655254')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022' AND d.district_type='LOCAL' AND d.state='ca')=5 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022' AND o.title='Mayor')=1 AND (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022' AND o.title='Mayor Pro Tem')=1 AND (SELECT count(*) FROM essentials.politician_images pi JOIN essentials.offices o ON o.politician_id=pi.politician_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022')=5 AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.offices o ON o.politician_id=pa.politician_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.mtfcc='X0022' AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id AND array_length(pc.sources,1) IS NOT NULL))"</automated>
  </verify>
  <acceptance_criteria>
    - Combined boolean SELECT returns `t` (5 X0022 geofences + 1 City of Palm Springs gov + 5 council offices + exactly 1 Mayor + exactly 1 Mayor Pro Tem + 5 headshots + 0 uncited stance rows).
    - Each X0022 district holds exactly 1 office (no HAVING count<>1 violation); no LOCAL_EXEC Mayor office.
    - 3 offices title='Councilmember'; appointed count = confirmed (0 per recon); section-split query returns 0 rows; G4110 0655254 row carries no council office.
    - 0 judicial-* stance rows; all stance values in [1.0,5.0].
    - `curl -sI` on the 5 headshot CDN URLs and cities/palm-springs.jpg all return HTTP 200; a sampled headshot is 600×750.
    - coverage.js 'Palm Springs' hasContext:true chip present; buildingImages.js 'palm springs' entry present and unchanged.
    - All audit checks recorded green in 202-06-SUMMARY.md.
  </acceptance_criteria>
  <done>Full production audit is all-green across geofences, government/roster (Mayor/MPT titles), headshots, stances, coverage, and the reused banner.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-browse per-district routing + banner render sign-off (OPERATOR)</name>
  <files>N/A — operator-driven read-only live browse; no files modified</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-01-SUMMARY.md (the 5 district centroids for choosing a probe address per district)
    - .planning/phases/202-palm-springs-deep-seed/202-02-SUMMARY.md (the roster → district mapping to confirm the right member appears)
  </read_first>
  <what-built>
    The complete Palm Springs deep-seed: 5 X0022 council-district geofences, City of Palm Springs
    government + City Council, 5 by-district members (Soto=Mayor D4, Ready=Mayor Pro Tem D5), 5
    headshots, evidence-only stances, a coverage.js chip, and the already-live city banner.
  </what-built>
  <how-to-verify>
    OPERATOR, read-only on https://essentials.empowered.vote (never wipe localStorage on the live page):
    1. Enter a full street address in EACH of the 5 council districts (one per district; use the Plan 01
       centroids as a guide). Confirm each routes to EXACTLY ONE Palm Springs councilmember — the right
       person for that district — with their headshot and evidence-only compass. Confirm D4 shows the
       'Mayor' title and D5 shows 'Mayor Pro Tem'.
    2. Browse the coverage chip (or /results?browse_geo_id=0655254). Confirm the 'Palm Springs' chip
       renders, lists the 5 members, and the Palm Springs city banner (Palm Canyon Dr street scene)
       displays — NOT a gradient, NOT a different city's banner, NOT the state shot.
    3. Confirm no console errors and no party affiliation displayed on any profile (antipartisan).
    Report any mis-routing, wrong-person, missing headshot, wrong/missing banner, or stray title.
  </how-to-verify>
  <verify>
    <automated>MISSING — operator-driven live browse; no automated command (executor has no live-browser access). Verified via the 5 per-district routing confirmations + banner render recorded in 202-06-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 district probe addresses routes to exactly 1 correct councilmember with the right headshot + compass.
    - D4 shows 'Mayor', D5 shows 'Mayor Pro Tem'; no duplicate/extra members.
    - The 'Palm Springs' coverage chip renders and the Palm Canyon Dr city banner displays (not a gradient/other city/state shot).
    - No console errors; no party affiliation displayed (antipartisan).
    - Operator sign-off recorded in 202-06-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 5 districts route correctly, titles are right, and the city banner renders — or describe the routing/banner discrepancy for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live resident address → PIP routing | Untrusted address input resolves to a district/office via the geofence GIST query |
| production DB/CDN → operator browse | Rendered roster/headshot/banner/compass observed on the live app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-02 | Tampering | wrong-district routing / wrong member | mitigate | Task 2 operator probes all 5 districts and confirms exactly-one-correct-member routing; Task 1 asserts 1 office per X0022 district |
| T-202-SPLIT | Tampering | section-split / cross-gov office leak | mitigate | Task 1 gate (g) = 0 offices under any non-Palm-Springs government; G4110 0655254 row carries no office |
| T-202-BANNER | Information Disclosure | broken/wrong banner | mitigate | Task 1 `curl -sI` cities/palm-springs.jpg → 200; Task 2 operator confirms the correct Palm Canyon Dr banner renders |
| T-202-PARTY | Information Disclosure | party affiliation displayed | mitigate | Task 2 confirms no party shown on any profile (antipartisan, project-wide) |
</threat_model>

<verification>
- Full production audit all-green (geofences, gov/roster/titles, headshots, stances, coverage, banner CDN 200).
- 5 per-district addresses each route to exactly 1 correct member; banner renders via the chip.
- No console errors, no party display.
</verification>

<success_criteria>
CV-02 + BANR-01 are TRUE end-to-end: a Palm Springs resident sees their one councilmember with a
correct headshot, evidence-only compass, and the city banner.
</success_criteria>

<output>
Create `.planning/phases/202-palm-springs-deep-seed/202-06-SUMMARY.md` when done (full audit results +
per-district routing confirmations + operator sign-off — the phase verification record).
</output>
