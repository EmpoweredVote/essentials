---
phase: 187-tethered-feature-icon-row
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/treasury.js
  - src/lib/treasury.test.js
  - src/lib/featureIcons.js
  - src/lib/featureIcons.test.js
  - public/treasury-symbol.svg
  - .env.example
autonomous: true
requirements: [ICON-01, TETH-02, TETH-03, TETH-04]

must_haves:
  truths:
    - "findStateTreasuryEntity('TX', cities) returns the Texas state entity; a state with no entity returns null"
    - "findFederalTreasuryEntity(cities) returns the 'United States' federal entity; an empty list returns null"
    - "resolveFeatureIcons returns an empty array for a tier whose location has no matching Treasury entity (icon omitted, TETH-03)"
    - "The Treasury deep-link slug for a real record is <name>-<state> lowercase-dash (e.g. texas-tx, united-states-us)"
    - "The product registry lists treasury first in the reserved order [treasury, compass, readrank] and renders only products with a live resolver (Treasury only today)"
  artifacts:
    - path: "src/lib/treasury.js"
      provides: "findStateTreasuryEntity, findFederalTreasuryEntity, centralized TREASURY_URL export"
      exports: ["findStateTreasuryEntity", "findFederalTreasuryEntity", "TREASURY_URL"]
    - path: "src/lib/featureIcons.js"
      provides: "PRODUCT_REGISTRY (reserved order) + resolveFeatureIcons -> {Local,State,Federal}"
      exports: ["PRODUCT_REGISTRY", "resolveFeatureIcons"]
    - path: "src/lib/treasury.test.js"
      provides: "findStateTreasuryEntity / findFederalTreasuryEntity cases (real TX / United States records)"
      contains: "findStateTreasuryEntity"
    - path: "src/lib/featureIcons.test.js"
      provides: "registry-ordering + TETH-03 omission tests"
      contains: "resolveFeatureIcons"
    - path: "public/treasury-symbol.svg"
      provides: "Treasury icon asset served at root-relative /treasury-symbol.svg"
  key_links:
    - from: "src/lib/featureIcons.js"
      to: "src/lib/treasury.js"
      via: "import { findMatchingMunicipality, findStateTreasuryEntity, findFederalTreasuryEntity, toTreasurySlug, TREASURY_URL }"
      pattern: "from './treasury'"
---

<objective>
Build the pure-logic core of the tethered feature-icon row: the state/federal Treasury
entity resolvers, a generic product registry with a reserved icon order, and the
`resolveFeatureIcons({representingCity, userState, treasuryCities}) -> {Local,State,Federal}`
function — plus the icon asset and full automated test coverage. This is everything the
UI plan (187-02) prop-drills; no React rendering happens here.

Purpose: Establish the resolve-or-null contract (TETH-02/03/04) that guarantees an icon is
produced ONLY when a real per-location Treasury deep-link exists, following the existing
`buildingImages.js`/`treasury.js` precedents exactly.

Output: `src/lib/treasury.js` (extended), `src/lib/featureIcons.js` (new), both test files,
`public/treasury-symbol.svg`, and a centralized `TREASURY_URL` set to the locked
`financials.empowered.vote` contract.

## Locked decisions honored
- D-01/D-03: generic product registry with reserved order `[treasury, compass, readrank]`.
- D-02/TETH-03: only products with a live per-location resolver render — Treasury only; no
  greyed/placeholder entries (compass/readrank reserved as commented/inert, not rendered).
- D-04: Treasury wired end-to-end (real resolvers, real slug, real href).
- D-09/TETH-04: separate `findStateTreasuryEntity` / `findFederalTreasuryEntity` — do NOT
  modify `findMatchingMunicipality` (fuzzy municipality-only, protected regression suite).

## Open question resolved (Treasury domain)
Centralize a single `export const TREASURY_URL` in `treasury.js` defaulting to
`import.meta.env.VITE_TREASURY_URL || 'https://financials.empowered.vote'` (the locked
CONTEXT/REQUIREMENTS contract), and update `.env.example`. Both the existing per-body text
link and the new icon consume this one constant (Results.jsx re-import happens in plan 187-02).
Rationale: research Pitfall 1 — both domains serve the identical app for explicit `?entity=`
links, so switching to the canonical `financials` domain fixes the inconsistency with zero
functional risk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/187-tethered-feature-icon-row/187-CONTEXT.md
@.planning/phases/187-tethered-feature-icon-row/187-RESEARCH.md
@.planning/phases/187-tethered-feature-icon-row/187-PATTERNS.md
@.planning/phases/187-tethered-feature-icon-row/187-VALIDATION.md

<interfaces>
<!-- Existing exports the new code builds on. Do NOT re-derive — use these directly. -->

From src/lib/treasury.js (existing, verified):
```javascript
export async function fetchTreasuryCities()            // -> Array of entity objects
export function toTreasurySlug(city)                    // { name, state } -> "name-state" lowercase, strips /?#
export function findMatchingMunicipality(bodyTitle, cities, state) // fuzzy municipality-only, DO NOT MODIFY
```

Real live /treasury/cities records (probed 2026-07-07, RESEARCH.md Code Examples):
- State (Texas):  { name:'Texas', state:'TX', entity_type:'state', available_datasets:[...20] }
- Federal:        { name:'United States', state:'US', entity_type:'federal', available_datasets:[...300+] }
- City (Plano):   { name:'Plano', state:'TX', entity_type:'municipality', available_datasets:[...19] }
  (note: ordinary cities are entity_type 'municipality'/'city'/'town' — that is why the city
   tier keeps using findMatchingMunicipality, which ignores entity_type.)

Precedent shape to mirror — src/lib/buildingImages.js:
```javascript
export function getBuildingImages(representingCity, stateAbbrev) // -> { Local, State, Federal }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend treasury.js with state/federal resolvers + centralize TREASURY_URL; copy icon asset; extend tests</name>
  <files>src/lib/treasury.js, src/lib/treasury.test.js, public/treasury-symbol.svg, .env.example</files>
  <read_first>
    - src/lib/treasury.js (FULL — the file being modified; note findMatchingMunicipality lines 67-100 which MUST stay untouched, toTreasurySlug lines 28-37, ENTITY_TYPE_WORDS const style line 65)
    - src/lib/treasury.test.js (FULL — existing Vitest pure-logic convention: `ds` fixture + CITIES array + describe/it, no jsdom)
    - .planning/phases/187-tethered-feature-icon-row/187-RESEARCH.md § "Code Examples" (real Texas / United States records + the exact new-resolver implementation shape)
    - .planning/phases/187-tethered-feature-icon-row/187-PATTERNS.md § "src/lib/treasury.js" and "src/lib/treasury.test.js" (exact function bodies + doc-comment convention)
  </read_first>
  <behavior>
    - findStateTreasuryEntity('TX', CITIES) -> the Texas state entity (entity_type==='state', state==='TX', has datasets)
    - findStateTreasuryEntity('ZZ', CITIES) -> null (no such state entity)
    - findStateTreasuryEntity('TX', []) / (null, ...) -> null (guards)
    - findStateTreasuryEntity ignores a state row with empty available_datasets -> null (TETH-03)
    - findFederalTreasuryEntity(CITIES) -> the 'United States' federal entity
    - findFederalTreasuryEntity([]) -> null
    - toTreasurySlug(texasRecord) === 'texas-tx'; toTreasurySlug(unitedStatesRecord) === 'united-states-us'
  </behavior>
  <action>
    Add two new exported pure functions to `treasury.js` exactly as specified in RESEARCH.md Code
    Examples / PATTERNS.md: `findStateTreasuryEntity(state, cities)` (filter `entity_type === 'state'`
    AND uppercased `state` match AND `available_datasets.length > 0`, else null) and
    `findFederalTreasuryEntity(cities)` (filter `entity_type === 'federal'` AND has datasets, else
    null). Both guard non-array `cities` and falsy inputs by returning null. Add JSDoc
    `@param`/`@returns` matching the file's existing convention plus a one-line "confirmed via live
    /treasury/cities probe 2026-07-07" cross-reference comment. Do NOT touch `findMatchingMunicipality`
    or its `ENTITY_TYPE_WORDS` list.
    Add a module-level `export const TREASURY_URL = import.meta.env.VITE_TREASURY_URL || 'https://financials.empowered.vote';`
    near the top alongside the other constants (this is the single source of truth per the resolved
    open question; Results.jsx re-imports it in plan 187-02).
    Update `.env.example` line for `VITE_TREASURY_URL` to default to `https://financials.empowered.vote`.
    Copy the icon asset from `C:/ev-landing/ev-landing-main/icons/treasury-symbol.svg` into
    `public/treasury-symbol.svg` (file copy — NOT a JS import; referenced later by root-relative path
    `/treasury-symbol.svg` per the Layout.jsx logoSrc convention). Do NOT copy compass/readrank symbols.
    Extend `treasury.test.js`: add `describe('findStateTreasuryEntity', ...)` and
    `describe('findFederalTreasuryEntity', ...)` blocks reusing the existing `ds` fixture style,
    adding `entity_type:'state'`/`'federal'` rows mirroring the real Texas / United States records,
    plus at least one no-match-returns-null case per function (TETH-03 guard) and a slug assertion
    (`texas-tx`, `united-states-us`). Match the existing "renders NO match" naming convention.
  </action>
  <acceptance_criteria>
    - `public/treasury-symbol.svg` exists and is byte-identical to the ev-landing source (4062 bytes).
    - `src/lib/treasury.js` exports `findStateTreasuryEntity`, `findFederalTreasuryEntity`, and `TREASURY_URL`; `findMatchingMunicipality` body is unchanged (diff shows no edits between lines 67-100).
    - `.env.example` `VITE_TREASURY_URL` default is `https://financials.empowered.vote`.
    - `src/lib/treasury.test.js` contains new describe blocks covering: TX state hit, wrong/absent state -> null, empty-datasets -> null, federal hit, empty list -> null, and slug format for both real records.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/treasury.test.js</automated>
    <check>ls public/treasury-symbol.svg && grep -c "findStateTreasuryEntity\|findFederalTreasuryEntity" src/lib/treasury.js (>=2)</check>
  </verify>
  <done>All treasury.test.js cases green; both resolvers + TREASURY_URL exported; findMatchingMunicipality untouched; asset copied; .env.example updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create featureIcons.js product registry + resolveFeatureIcons, with tests</name>
  <files>src/lib/featureIcons.js, src/lib/featureIcons.test.js</files>
  <read_first>
    - src/lib/buildingImages.js (§ getBuildingImages lines 484-531 — the `{Local,State,Federal}` tier-map shape + doc-comment style to mirror)
    - src/lib/treasury.js (AFTER Task 1 — the resolvers + toTreasurySlug + TREASURY_URL this module imports)
    - .planning/phases/187-tethered-feature-icon-row/187-RESEARCH.md § "Pattern 3: Product registry" (the PRODUCT_REGISTRY entry shape with resolve(ctx) returning {key,href,label}|null)
    - .planning/phases/187-tethered-feature-icon-row/187-PATTERNS.md § "src/lib/featureIcons.js" and "src/lib/featureIcons.test.js" (import convention, has-data predicate style, real-record fixtures)
  </read_first>
  <behavior>
    - PRODUCT_REGISTRY[0].key === 'treasury' and iconSrc === '/treasury-symbol.svg' (reserved order treasury first; compass/readrank reserved but NOT rendered)
    - resolveFeatureIcons({representingCity:'Plano', userState:'TX', treasuryCities}) -> { Local:[{key:'treasury',href,label:'Treasury Tracker'}], State:[{...texas-tx}], Federal:[{...united-states-us}] }
    - Each resolved href === `${TREASURY_URL}/?entity=<slug>` with slug from toTreasurySlug (e.g. financials.empowered.vote/?entity=texas-tx)
    - A tier with no matching entity -> that tier's array is [] (TETH-03: icon omitted, never a null/placeholder entry)
    - resolveFeatureIcons({treasuryCities: []}) -> { Local:[], State:[], Federal:[] } (no throw)
    - Only the treasury product ever appears in output today (compass/readrank produce nothing)
  </behavior>
  <action>
    Create `src/lib/featureIcons.js`. Export `PRODUCT_REGISTRY`: a fixed-order array whose first
    (and only live) entry is `{ key:'treasury', label:'Treasury Tracker', iconSrc:'/treasury-symbol.svg',
    resolve(ctx) }`. `resolve({tier, representingCity, userState, treasuryCities})` calls
    `findMatchingMunicipality(representingCity, treasuryCities, userState)` for tier 'city',
    `findStateTreasuryEntity(userState, treasuryCities)` for tier 'state',
    `findFederalTreasuryEntity(treasuryCities)` for tier 'federal'; returns null when no entity, else
    `{ key:'treasury', href: `${TREASURY_URL}/?entity=${encodeURIComponent(toTreasurySlug(entity))}`,
    label:'Treasury Tracker', iconSrc:'/treasury-symbol.svg' }`. Reserve the compass and readrank slots
    as commented-out registry entries (D-03: zero layout change when their contracts land) — do NOT
    add rendering entries for them (D-02). Export `resolveFeatureIcons({representingCity, userState,
    treasuryCities})` returning `{ Local, State, Federal }`, each an array built by iterating
    PRODUCT_REGISTRY in order, calling `resolve` with the matching tier ('city'/'state'/'federal'),
    and collecting non-null results (filter out nulls — never push a placeholder). Mirror the
    `getBuildingImages` doc-comment + return-shape style. Wrap the slug in `encodeURIComponent`
    (defense-in-depth over toTreasurySlug's existing `/?#` stripping — threat T-187-01).
    Create `src/lib/featureIcons.test.js` (Vitest, pure-logic, no jsdom) with fixtures built from the
    real Texas / United States / Plano records (RESEARCH.md). Cover: registry order (treasury index 0,
    iconSrc), Plano city resolves to a treasury icon with a `financials.empowered.vote/?entity=plano-tx`
    href, Texas state -> texas-tx, federal -> united-states-us, a tier with no matching entity -> []
    (TETH-03), empty treasuryCities -> all three arrays [], and that no non-treasury product ever appears.
  </action>
  <acceptance_criteria>
    - `src/lib/featureIcons.js` exports `PRODUCT_REGISTRY` (treasury first) and `resolveFeatureIcons`.
    - Every resolved icon href begins with `https://financials.empowered.vote/?entity=` (uses the imported TREASURY_URL, no second hardcoded domain).
    - Unresolved tiers yield `[]`, not `[null]` or a disabled entry (TETH-03).
    - `npx vitest run src/lib/featureIcons.test.js` all green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/treasury.test.js src/lib/featureIcons.test.js</automated>
    <check>grep -c "compass\|readrank" src/lib/featureIcons.js shows only commented/reserved (no rendered entry)</check>
  </verify>
  <done>featureIcons.js registry + resolveFeatureIcons implemented and green; only Treasury renders; unresolved tiers return empty arrays; full quick-suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| location data -> outbound URL | banner-location strings (name/state) are interpolated into the Treasury `?entity=` query value |
| npm/pip/cargo installs | none this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-01 | Tampering | href construction in featureIcons.js resolve() | mitigate | Reuse `toTreasurySlug()` (already strips `/ ? #`, documented T-122-02 mitigation) AND wrap the slug in `encodeURIComponent` before interpolation into `?entity=` — no raw location string reaches the query value |
| T-187-04 | Information Disclosure | resolver inputs | mitigate | Resolvers accept only `representingCity`/`userState` (the banner's own location) — never any ev-context/broker/saved-location value; the module has no import of any user-location source |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH.md Package Legitimacy Audit: N/A — reuses installed `@floating-ui/react`, copies a static SVG); no supply-chain surface introduced |
</threat_model>

<verification>
- `npx vitest run src/lib/treasury.test.js src/lib/featureIcons.test.js` fully green.
- `findMatchingMunicipality` unchanged (protected regression suite still passes).
- Icon asset present at `public/treasury-symbol.svg`; no cross-repo JS import anywhere.
- Single `TREASURY_URL` source of truth in `treasury.js`, pointing at `financials.empowered.vote`.
</verification>

<success_criteria>
- State-tier and federal-tier Treasury entities resolve (TETH-04) with real live-API record shapes.
- Slug/href contract produces `financials.empowered.vote/?entity=<name-state>` (TETH-02).
- Unresolvable tiers omit the icon entirely — empty array, no placeholder (TETH-03).
- Registry data feeds the icon row (ICON-01) in reserved order, Treasury only (D-01/D-02/D-03).
</success_criteria>

<output>
Create `.planning/phases/187-tethered-feature-icon-row/187-01-SUMMARY.md` when done.
</output>
