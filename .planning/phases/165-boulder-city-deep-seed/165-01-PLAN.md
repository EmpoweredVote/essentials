---
phase: 165-boulder-city-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1100_boulder_city_city_council.sql
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [CLARK-05]
user_setup: []

must_haves:
  truths:
    - "Wave-0 BLOCKING probes confirm (before any write): live on-disk migration MAX (highest file +1, trust on-disk over the registered ledger — expected 1099 → next structural 1100); external_id block -3208001..-3208005 has 0 existing rows; no pre-existing 'City of Boulder City, Nevada, US' government; no pre-existing LOCAL/LOCAL_EXEC districts on geo_id='3206500' state='nv'; Boulder City G4110 geo_id='3206500' + its geofence_boundaries casing (state='32' FIPS) confirmed loaded by Phase 158 [D-04, D-04b]"
    - "A Wave-0 operator checkpoint verifies the live roster + seat count (Mayor Joe Hardy + council Sherri Jorgensen / Cokie Booth / Steve Walton / Denise E. Ashurst = 5 seats, at-large, council-manager) against bcnv.org/159/City-Council before the structural migration applies; Hardy/Booth/Walton are on the June 9 / Nov 3 2026 ballot but all 5 remain seated [specifics, D-02]"
    - "NO ward-boundary loader and NO new geofence rows are created this phase — Boulder City is at-large; the load-*-ward-boundaries.ts scripts from 162-164 do NOT apply [D-01, D-01a]"
    - "A standalone government 'City of Boulder City, Nevada, US' exists (type='City', state='NV' uppercase, geo_id='3206500'), NOT nested under State of Nevada geo_id='32' [D-04]"
    - "A chamber 'Boulder City City Council' (official_count=5, name_formal non-empty, the auto-generated path column NEVER in the INSERT list) exists under that government [D-04a]"
    - "Mayor Joe Hardy (external_id -3208001) has a LOCAL_EXEC office on the city-wide G4110 district (geo_id='3206500', state='nv'), title='Mayor', directly elected NOT rotational, sorted first [D-02]"
    - "The 4 at-large council members (Jorgensen -3208002 / Booth -3208003 / Walton -3208004 / Ashurst -3208005) each have a LOCAL office on the ONE shared LOCAL district (geo_id='3206500', district_type='LOCAL', state='nv'), title='Council Member' (NO ward number); Jorgensen seeded title='Council Member' NOT 'Mayor Pro Tem' and is NOT a 6th seat [D-01b, D-04a]"
    - "Post-verify: exactly 1 government row; exactly 1 LOCAL_EXEC office (Mayor); exactly 4 LOCAL council offices (total 5); office_id back-filled for all 5; zero section-split (the G4110 geo_id='3206500' geofence has a matching LOCAL district); exactly 2 district rows on 3206500 (1 LOCAL_EXEC + 1 LOCAL, NOT 4 LOCAL); all 5 seeded is_active=true is_incumbent=true party='Non-Partisan'"
    - "City of Boulder City surfaces with the purple hasContext chip in src/lib/coverage.js COVERAGE_STATES (appended to the EXISTING Nevada block after North Las Vegas — LV + Henderson + NLV already present) [CLARK-05 SC#4]"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1100_boulder_city_city_council.sql"
      provides: "Structural migration: pre-flight (NOTICE if gov exists; NO ward assertion) + 1 government + 1 chamber + 1 LOCAL_EXEC district (Mayor) + 1 shared LOCAL district (4 council) both on geo_id='3206500' + 5 politician+office CTEs + office_id back-fill + post-verify DO block; registers as 1100 (or Wave-0-confirmed number)"
      contains: "INSERT INTO essentials.chambers"
      min_lines: 180
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "COVERAGE_STATES Nevada block with Boulder City appended (browseGovernmentList:['3206500'], browseStateAbbrev:'NV', hasContext:true), LV + Henderson + North Las Vegas entries preserved"
      contains: "Boulder City"
  key_links:
    - from: "essentials.chambers (Boulder City City Council)"
      to: "essentials.governments (City of Boulder City, Nevada, US, geo_id='3206500')"
      via: "government_id subquery WHERE name='City of Boulder City, Nevada, US'"
      pattern: "City of Boulder City, Nevada, US"
    - from: "essentials.offices (Mayor + 4 at-large council)"
      to: "essentials.districts (1 LOCAL_EXEC + 1 shared LOCAL, both geo_id='3206500', all state='nv')"
      via: "district join on geo_id + district_type + state='nv' lowercase"
      pattern: "state = 'nv'"
    - from: "essentials.geofence_boundaries (existing G4110 geo_id='3206500')"
      to: "essentials.districts (LOCAL + LOCAL_EXEC, geo_id='3206500')"
      via: "shared geo_id; backend ST_Covers PIP resolves G4110 → both districts (no ward polygons)"
      pattern: "3206500"
    - from: "src/lib/coverage.js COVERAGE_STATES Nevada block"
      to: "Boulder City browse (geo_id 3206500)"
      via: "browseGovernmentList array entry"
      pattern: "3206500"
---

<objective>
Seed the City of Boulder City structurally — a directly-elected at-large Mayor + 4 at-large council members, ALL attaching to the ONE existing Boulder City G4110 city geofence (geo_id='3206500', loaded by Phase 158). This is the simplest NV city deep-seed to date: Boulder City has NO wards (D-01) — no ward loader, no custom ward MTFCC, no new geofence rows this phase. First a Wave-0 BLOCKING probe confirms the live migration MAX (trust the highest on-disk file +1 — expected 1099 → next structural 1100), the unused external_id block -3208001..-3208005, no pre-existing Boulder City government, no pre-existing districts on geo_id='3206500', and the Boulder City G4110 geo_id='3206500' + its geofence casing (state='32'). A Wave-0 operator checkpoint then verifies the live roster + seat count (5: Mayor Joe Hardy + Sherri Jorgensen / Cokie Booth / Steve Walton / Denise E. Ashurst) against bcnv.org / flybouldercity.com BEFORE any migration applies. Then create a standalone government "City of Boulder City, Nevada, US" (NOT nested under State of Nevada geo_id='32' — D-04), one chamber "Boulder City City Council" (official_count=5 — D-04a), one LOCAL_EXEC district for the directly-elected Mayor (Hardy) on the city-wide G4110 geofence (geo_id='3206500'), ONE shared LOCAL district on the SAME geo_id='3206500' carrying all 4 at-large council offices (NOT one per member — Clark County 1055 single-shared-district shape, NOT the NLV ward shape), all 5 politician+office rows (at-large 'Council Member' titles — NO ward numbers), and back-fill office_id. Finally append Boulder City to the EXISTING Nevada block in src/lib/coverage.js COVERAGE_STATES (LV + Henderson + North Las Vegas already present).

Purpose: Satisfies the structural half of CLARK-05 (Mayor + at-large council via the single G4110 geofence) plus the surfacing half (SC#4 — purple hasContext chip). Headshots are Plan 02; stances + final E2E verification are Plan 03.
Output: 1100_boulder_city_city_council.sql (structural, registered), the coverage.js NV-block edit, plus the captured Wave-0 probe facts + the 5 external_id→UUID map recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and NO DB access):**
- The executor WRITES the migration .sql and the coverage.js edit to disk and documents the exact apply steps. The executor does NOT run DB SELECTs or apply migrations.
- The Wave-0 DB probes, the roster operator checkpoint, applying mig 1100 (via `psql -f` using the EV-Accounts/backend/.env DATABASE_URL), and every post-apply audit SELECT are INLINE ORCHESTRATOR steps performed by execute-phase.
- Each task below marks which steps are executor-write vs inline-orchestrator-apply.
- The migration lives in the SEPARATE repo C:/EV-Accounts/backend/ (commit via `git -C "C:/EV-Accounts"`, always from the repo root — never cd into backend). The coverage.js edit lives in the essentials repo. SUMMARY.md goes in the essentials .planning phase dir.
- There is NO ward loader to write or run this phase (Boulder City is at-large) — do not adapt the NLV/Henderson load-*-ward-boundaries.ts scripts.
</execution_context>

<context>
@.planning/phases/165-boulder-city-deep-seed/165-RESEARCH.md
@.planning/phases/165-boulder-city-deep-seed/165-PATTERNS.md
@.planning/phases/165-boulder-city-deep-seed/165-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
Boulder City city geo_id (TIGER G4110 place): '3206500' (loaded by Phase 158)
Government: name='City of Boulder City, Nevada, US', type='City', state='NV' UPPERCASE, city=NULL, geo_id='3206500' (standalone, NOT under State of Nevada geo_id='32'). governments has NO unique constraint on geo_id or name — WHERE NOT EXISTS guard mandatory.
Chamber: name='Boulder City City Council', name_formal='Boulder City City Council', official_count=5
NO ward geofence source — Boulder City is at-large, no wards. NO load-*-ward-boundaries.ts, NO custom X-MTFCC, NO new geofence_boundaries rows this phase.
Two districts on the SAME geo_id (the at-large pattern, NOT ward-per-member):
  - 1 LOCAL_EXEC row: state='nv' lowercase, geo_id='3206500', label='City of Boulder City', mtfcc='G4110' (Mayor; uses EXISTING Phase 158 G4110 geofence — no new geofence row)
  - 1 LOCAL row: state='nv' lowercase, geo_id='3206500', label='City of Boulder City', mtfcc='G4110' (4 at-large council members all attach to this ONE shared district — Clark County 1055 single-shared-district shape, NOT 4 separate LOCAL rows)
  Result: exactly 2 district rows on geo_id='3206500' (1 LOCAL_EXEC + 1 LOCAL). Backend ST_Covers PIP resolves G4110 → both districts → Mayor (LOCAL_EXEC) + all 4 council (LOCAL).
Office district join keys: Mayor → d.geo_id='3206500' AND d.district_type='LOCAL_EXEC' AND d.state='nv'; each council member → d.geo_id='3206500' AND d.district_type='LOCAL' AND d.state='nv' (uppercase 'NV' matches ZERO rows — silent no-op, the #1 failure mode across all NV phases)
offices.representing_state: 'NV' UPPERCASE (free-text label column, NOT the district join key)
Office NOT EXISTS guard: (district_id, politician_id) — one person per office
chambers auto-generated path column is GENERATED ALWAYS — never include it in the INSERT column list; name_formal must NOT be ''
Roster + external_ids (party='Non-Partisan' all — antipartisan, never displayed; ALL is_active=true, is_incumbent=true, is_appointed=false, is_vacant=false):
  -3208001 Joe Hardy (Mayor, directly elected at-large, LOCAL_EXEC, title='Mayor'; term expires 2026 but seated through inauguration — NOT rotational)
  -3208002 Sherri Jorgensen (Council Member; "Mayor Pro Tem" is an internal council designation, NOT a separate seat — seed title='Council Member', do NOT create a 6th seat or a 2nd LOCAL_EXEC row)
  -3208003 Cokie Booth (Council Member; term expires 2026, on ballot, seated)
  -3208004 Steve Walton (Council Member; term expires 2026, on ballot, seated)
  -3208005 Denise E. Ashurst (Council Member; elected Nov 2024, term 2028)
Migration: structural number 1100 (on-disk MAX confirmed 1099 — highest file is 1099_north_las_vegas_cherchio_stances.sql); registers in the ledger. Wave-0 re-verifies the live on-disk MAX and uses highest-file +1.
Backend routing: G4110 geofence already exists (Phase 158) — no backend code change; LOCAL_EXEC sorts before LOCAL via groupHierarchy.js (Mayor first).
Grep-gate forbidden tokens in .sql comments: paraphrase the auto-generated path column (→ "the auto-generated path column"); keep the removed image-origin column literal out (N/A here); the ledger table token appears ONLY in the actual Step 7 registration INSERT, never in a comment.
Casing convention: districts.state = 'nv' lowercase (routing join key — 'NV' = 0 rows); governments.state / offices.representing_state = 'NV' uppercase; the G4110 TIGER place geofence row is state='32' (FIPS) per Phase 158; the LOCAL/LOCAL_EXEC district rows are still state='nv'.
Browse link: essentials.empowered.vote/results?browse_geo_id=3206500&browse_mtfcc=G4110
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave-0 BLOCKING DB probes + roster/seat operator checkpoint (inline orchestrator)</name>
  <read_first>
    - .planning/phases/165-boulder-city-deep-seed/165-RESEARCH.md (§external_id Collision Analysis; §Boulder City geo_id; §Migration Counter; §Key Verified Facts roster; §Validation Architecture Wave 0 Requirements)
    - .planning/phases/165-boulder-city-deep-seed/165-VALIDATION.md (Wave 0 Requirements — the exact probe queries; the NO-ward-loader note)
    - .planning/phases/164-north-las-vegas-deep-seed/164-01-SUMMARY.md (the prior NV city Wave-0 probe→roster checkpoint flow + the on-disk-MAX-vs-ledger drift lesson)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor — these need DB/filesystem access). Run all BLOCKING probes and the roster checkpoint BEFORE the executor's migration task proceeds. Record every result in the SUMMARY.
(a) Live on-disk migration MAX: `ls C:/EV-Accounts/backend/migrations | grep -oE '^[0-9]+' | sort -n | tail -1` — trust the highest on-disk file +1 as the structural migration number (expected 1099 → 1100; the on-disk file number is the authority, NOT the registered ledger which is timestamp-versioned). If the on-disk MAX differs from 1099, renumber the structural migration (1100) and ALL downstream audit-only migrations (Plan 02 headshot 1101, Plan 03 stances 1102-1106) by the same offset and note it.
(b) external_id collision probe: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -3208005 AND -3208001` — expect 0. If non-zero, pick the next unused -3208xxx block and propagate to every downstream file.
(c) No pre-existing government: `SELECT COUNT(*) FROM essentials.governments WHERE name='City of Boulder City, Nevada, US'` — expect 0 (greenfield).
(d) No pre-existing districts: `SELECT COUNT(*) FROM essentials.districts WHERE geo_id='3206500' AND state='nv'` — expect 0 (the migration creates exactly 2: 1 LOCAL_EXEC + 1 LOCAL).
(e) Boulder City G4110 geo_id + casing: `SELECT geo_id, state FROM essentials.geofence_boundaries WHERE name ILIKE '%Boulder City%' AND mtfcc='G4110'` — expect geo_id='3206500', state='32' (TIGER FIPS string). Confirms the single at-large attach point exists. (NO ward/X-MTFCC probe — Boulder City has no wards.)
Then read the CURRENT coverage.js NV block to confirm Las Vegas (3240000), Henderson (3231900), AND North Las Vegas (3251800) entries are present (Plan 01 Task 3 appends Boulder City after them).
Then the ROSTER OPERATOR CHECKPOINT: verify the live Boulder City roster + seat count against bcnv.org/159/City-Council and flybouldercity.com/941/Meet-the-Council — Mayor Joe Hardy + council Sherri Jorgensen + Cokie Booth + Steve Walton + Denise E. Ashurst = 5 seats, council-manager, all at-large (NO wards). Confirm Jorgensen's "Mayor Pro Tem" is an internal council designation (NOT a separately-elected 6th seat). Confirm Hardy/Booth/Walton remain seated despite their 2026 terms being on the June 9 / Nov 3 2026 ballot (seated until inauguration → seed is_active=true, is_incumbent=true). Pause for operator confirmation before the migration applies. If the roster has shifted, adjust names/ext_ids before writing the migration.
This task GATES the structural migration (Task 2): do not apply mig 1100 until all probes pass and the roster is operator-confirmed.
  </action>
  <verify><human-check>Operator confirms: on-disk migration MAX recorded (structural number = MAX+1, expected 1100); external_id block -3208001..-3208005 returns 0 rows; no pre-existing Boulder City government (0 rows); no pre-existing districts on geo_id='3206500' (0 rows); Boulder City G4110 geo_id='3206500' state='32' confirmed; coverage.js NV block has LV + Henderson + North Las Vegas; and the 5-seat at-large roster (Hardy + Jorgensen/Booth/Walton/Ashurst) verified against the live source with Jorgensen confirmed NOT a 6th seat and Hardy/Booth/Walton still seated.</human-check></verify>
  <acceptance_criteria>
    - On-disk migration MAX recorded in SUMMARY; structural number set to MAX+1 (expected 1100); any drift offset applied to all downstream files.
    - external_id probe recorded = 0 for -3208005..-3208001 (or a new block chosen + propagated).
    - No-government probe recorded = 0 (greenfield).
    - No-districts probe recorded = 0 on geo_id='3206500' state='nv'.
    - G4110 probe recorded: geo_id='3206500', state='32'.
    - coverage.js NV block confirmed to currently hold Las Vegas + Henderson + North Las Vegas.
    - Roster + seat count operator-verified against the live source = 5 seats, all at-large; Jorgensen confirmed NOT a separate seat (Mayor Pro Tem = internal designation); Hardy/Booth/Walton confirmed seated.
    - This checkpoint is recorded as passed BEFORE mig 1100 applies.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded probe values + roster confirmation, or describe any collision (pick a new block), drift (renumber), or roster change (adjust names/ext_ids).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write structural migration 1100 + apply inline + post-verify</name>
  <files>C:/EV-Accounts/backend/migrations/1100_boulder_city_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1093_north_las_vegas_city_council.sql (the migration-shape template — the 7-step skeleton: pre-flight → government → chamber → LOCAL_EXEC district → LOCAL district(s) → politician+office CTEs → office_id back-fill → post-verify DO block → ledger registration outside COMMIT. ADAPT: drop ALL ward logic.)
    - C:/EV-Accounts/backend/migrations/1055_clark_county_commission.sql (the single-shared-district analog — all members on ONE district; copy this for Step 3b + the post-verify section-split shape)
    - .planning/phases/165-boulder-city-deep-seed/165-PATTERNS.md (section "1100_boulder_city_city_council.sql" — every step's adapted SQL, the THE-key-divergence note, the post-verify DO block, the ledger line)
    - .planning/phases/165-boulder-city-deep-seed/165-RESEARCH.md (Pattern 1 structural migration; Anti-Patterns; Pitfalls 1-6 — esp. Pitfall 2 Jorgensen-not-a-6th-seat, Pitfall 4 Hardy/Booth/Walton-seated, Pitfall 5 ONE-LOCAL-district-not-4)
  </read_first>
  <action>
EXECUTOR STEP: write `1100_boulder_city_city_council.sql` modeled on 1093_north_las_vegas_city_council.sql for the 7-step skeleton but with ALL ward logic DROPPED and Step 3b replaced by the 1055 single-shared-district shape. Steps:
- Pre-flight DO block: NOTICE if the Boulder City government already exists (idempotent re-run). NO ward assertion (Boulder City has no wards — do NOT carry the NLV X0017 pre-flight count check; it would fire on 0 rows).
- Step 1 Government: INSERT 'City of Boulder City, Nevada, US', type='City', state='NV', city=NULL, geo_id='3206500' WHERE NOT EXISTS (mandatory — no unique constraint).
- Step 2 Chamber: INSERT 'Boulder City City Council' / name_formal='Boulder City City Council' / government_id subquery / official_count=5 WHERE NOT EXISTS. NEVER include the auto-generated path column in the INSERT list; name_formal must be non-empty.
- Step 3a LOCAL_EXEC district (Mayor): INSERT district_type='LOCAL_EXEC', state='nv' lowercase, geo_id='3206500', label='City of Boulder City', mtfcc='G4110' WHERE NOT EXISTS (uses the existing Phase 158 G4110 geofence — no new geofence row).
- Step 3b ONE shared LOCAL district (4 at-large council members): a SINGLE INSERT, district_type='LOCAL', state='nv', geo_id='3206500' (SAME geo_id as 3a), label='City of Boulder City', mtfcc='G4110', WHERE NOT EXISTS. This is the Clark County 1055 single-shared-district pattern — do NOT create 4 LOCAL rows, and do NOT carry the NLV per-ward geo_ids.
- Step 4 politician+office CTEs (5 blocks): BLOCK 1 Mayor Hardy (-3208001) title='Mayor' on the LOCAL_EXEC district (geo_id='3206500', district_type='LOCAL_EXEC', state='nv'); BLOCKS 2-5 council members all on the SAME LOCAL district (geo_id='3206500', district_type='LOCAL', state='nv') title='Council Member' (NO ward number) — Jorgensen (-3208002; seed title='Council Member', NOT 'Mayor Pro Tem', NOT a 6th seat), Booth (-3208003), Walton (-3208004), Ashurst (-3208005). Each block: WITH ins_p INSERT politicians (party='Non-Partisan', is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id) ON CONFLICT (external_id) DO NOTHING RETURNING id → INSERT offices (chamber_id subquery, representing_state='NV' uppercase, role_canonical=NULL) joined on the district by geo_id+district_type+state='nv' with NOT EXISTS (district_id, politician_id) guard. Mayor uses district_type='LOCAL_EXEC' (NOT 'LOCAL'); all 4 council members use district_type='LOCAL'.
- Step 5 office_id back-fill: UPDATE politicians SET office_id=o.id WHERE external_id BETWEEN -3208005 AND -3208001 AND office_id IS NULL.
- Step 6 post-verify DO block (RAISE EXCEPTION on mismatch): exactly 1 Boulder City government row; exactly 1 LOCAL_EXEC office (Mayor, geo_id='3206500'); exactly 4 LOCAL council offices on the shared LOCAL district (geo_id='3206500'); section-split detector (0 orphan rows — the G4110 geo_id='3206500' geofence must have a matching LOCAL district). The exact DO-block shape is in 165-PATTERNS.md "Step 6". COMMIT.
- Step 7 ledger registration (OUTSIDE the transaction COMMIT): INSERT the version '1100' (or Wave-0 number) + name 'boulder_city_city_council' ON CONFLICT (version) DO NOTHING.
GREP-GATE HYGIENE: keep the auto-generated path column literal out of comments (say "the auto-generated path column"); keep the removed image-origin column literal out (N/A here); the ledger table token appears only in the Step 7 INSERT, never in a comment.

INLINE ORCHESTRATOR STEP: apply 1100 via `psql -f` (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm the post-verify DO block prints PASSED (gov=1, exec=1, local=4, split_orphans=0). Then run + record in SUMMARY:
- `SELECT district_type, COUNT(*) FROM essentials.districts WHERE geo_id='3206500' AND state='nv' GROUP BY district_type` → LOCAL_EXEC: 1, LOCAL: 1 (exactly 2 district rows).
- `SELECT d.district_type, COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='3206500' AND d.state='nv' GROUP BY d.district_type` → LOCAL_EXEC: 1, LOCAL: 4.
- `SELECT official_count FROM essentials.chambers WHERE name ILIKE '%Boulder City%Council%'` → 5.
- Capture the 5 external_id→UUID map (`SELECT external_id, id FROM essentials.politicians WHERE external_id BETWEEN -3208005 AND -3208001`) for Plans 02/03.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1100_boulder_city_city_council.sql"; test -f "$F" &amp;&amp; grep -q "City of Boulder City, Nevada, US" "$F" &amp;&amp; grep -q "INSERT INTO essentials.chambers" "$F" &amp;&amp; grep -q "Boulder City City Council" "$F" &amp;&amp; grep -q "LOCAL_EXEC" "$F" &amp;&amp; grep -q -- "-3208001" "$F" &amp;&amp; grep -q -- "-3208005" "$F" &amp;&amp; grep -q "official_count" "$F" &amp;&amp; grep -q "3206500" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1100_boulder_city_city_council.sql exists, ≥180 lines, follows the 1093 7-step skeleton with ward logic dropped.
    - Pre-flight has NO ward assertion (only the gov-exists NOTICE).
    - Government 'City of Boulder City, Nevada, US' (state='NV', geo_id='3206500') with WHERE NOT EXISTS; standalone (NOT under geo_id='32').
    - Chamber 'Boulder City City Council' official_count=5; the auto-generated path column NOT in the INSERT list; name_formal non-empty.
    - Exactly 2 districts on geo_id='3206500': 1 LOCAL_EXEC (Mayor) + 1 shared LOCAL (4 council), both state='nv' — NOT 4 LOCAL rows, NO ward geo_ids.
    - 5 politician+office blocks: Mayor Hardy (-3208001, LOCAL_EXEC, title='Mayor') + 4 council (-3208002..-3208005, LOCAL, title='Council Member' no ward number); Jorgensen seeded title='Council Member' NOT 'Mayor Pro Tem' and not a 6th seat; all party='Non-Partisan', is_active=true, is_incumbent=true; representing_state='NV'.
    - office_id back-filled BETWEEN -3208005 AND -3208001.
    - Post-verify DO block asserts gov=1, exec=1, local=4, section-split=0.
    - Ledger registration line present OUTSIDE the COMMIT (version 1100 / 'boulder_city_city_council'); no forbidden tokens in comments.
    - INLINE: applied; post-verify PASSED; district-type counts (2 districts: 1 LOCAL_EXEC + 1 LOCAL; offices 1 LOCAL_EXEC + 4 LOCAL); official_count=5; the 5 external_id→UUID map recorded in SUMMARY.
  </acceptance_criteria>
  <done>Structural migration written + applied; post-verify PASSED (gov=1, exec=1, local=4, split=0); exactly 2 districts (1 LOCAL_EXEC + 1 LOCAL); external_id→UUID map captured in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Append Boulder City to coverage.js Nevada block</name>
  <files>C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (lines ~183-190 — the existing Nevada block with the Las Vegas + Henderson + North Las Vegas entries; NOTE there is a DIFFERENT "Nevada" — a TX city — elsewhere; do NOT confuse it with the NV state block abbrev 'NV')
    - .planning/phases/165-boulder-city-deep-seed/165-PATTERNS.md (section "src/lib/coverage.js" — before/after of the NV block, append after North Las Vegas)
  </read_first>
  <action>
EXECUTOR STEP: edit src/lib/coverage.js — locate the existing Nevada block in COVERAGE_STATES (the one already containing the Las Vegas AND Henderson AND North Las Vegas entries from Phases 162/163/164 — abbrev 'NV', NOT the TX city "Nevada") and APPEND a Boulder City entry to its `areas` array, after North Las Vegas: `{ label: 'Boulder City', browseGovernmentList: ['3206500'], browseStateAbbrev: 'NV', hasContext: true }`. Keep the Las Vegas (3240000), Henderson (3231900), and North Las Vegas (3251800) entries intact. browseGovernmentList uses the CITY geo_id ('3206500'). Do NOT create a second Nevada block and do NOT touch Landing.jsx (surfacing target is coverage.js).
  </action>
  <verify>
    <automated>F="C:/Transparent Motivations/essentials/src/lib/coverage.js"; test -f "$F" &amp;&amp; grep -q "Boulder City" "$F" &amp;&amp; grep -q "3206500" "$F" &amp;&amp; grep -q "North Las Vegas" "$F" &amp;&amp; grep -q "3251800" "$F" &amp;&amp; grep -q "3231900" "$F" &amp;&amp; grep -q "3240000" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - coverage.js Nevada block contains Las Vegas (3240000), Henderson (3231900), North Las Vegas (3251800), AND Boulder City (3206500) entries.
    - Boulder City entry: label 'Boulder City', browseGovernmentList ['3206500'], browseStateAbbrev 'NV', hasContext: true.
    - Exactly one NV state block (no duplicate); the TX "Nevada" city entry untouched; Landing.jsx untouched.
  </acceptance_criteria>
  <done>Boulder City appended to the existing coverage.js Nevada block with hasContext:true; LV + Henderson + North Las Vegas entries preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| generated .sql migration → live DB | service-role apply of seed SQL; only the inline orchestrator applies, after review |
| Wave-0 roster source (bcnv.org / flybouldercity.com) → seeded roster | external roster claims cross into seed SQL; operator checkpoint verifies before write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-01 | Tampering | external_id collision (-3208001..-3208005) corrupting existing rows | mitigate | Wave-0 BLOCKING probe (Task 1b) confirms the block is unclaimed before any write; ON CONFLICT (external_id) DO NOTHING guards |
| T-165-02 | Tampering | wrong migration number overwriting/skipping a sibling migration | mitigate | Wave-0 probe (1a) trusts highest on-disk file +1 (1100), not the registered ledger; renumber + offset downstream if drift |
| T-165-03 | Tampering | wrong-person routing from a 6th-seat / 2nd-LOCAL_EXEC error (Jorgensen Mayor Pro Tem) or from 4 LOCAL districts instead of 1 | mitigate | Jorgensen seeded title='Council Member' on the shared LOCAL district; post-verify asserts exactly 2 districts (1 LOCAL_EXEC + 1 LOCAL) and 5 offices (1 exec + 4 local) — a 6th seat or extra district fails the DO block |
| T-165-04 | Tampering | silent no-op from uppercase state='NV' on the district routing join | mitigate | districts.state='nv' lowercase enforced; post-verify asserts 1 LOCAL_EXEC + 4 LOCAL offices exist (would be 0 on a casing bug) |
| T-165-05 | Tampering | seeding Hardy/Booth/Walton inactive due to 2026 ballot | mitigate | All 5 seeded is_active=true, is_incumbent=true (seated until inauguration); Plan 03 human-verify confirms no vacant seats |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit = N/A; all tooling pre-installed). No high-severity threats; data-seed phase. |
</threat_model>

<verification>
Structural + surfacing halves of CLARK-05:
- SC#1 (structural): 1 government + 1 chamber (official_count=5) + exactly 2 districts on geo_id='3206500' (1 LOCAL_EXEC + 1 shared LOCAL) + 1 LOCAL_EXEC Mayor office + 4 LOCAL at-large council offices, all state='nv', section-split=0 (Task 2 post-verify DO block + inline counts).
- SC#4 (surfacing): Boulder City in coverage.js with hasContext:true alongside Las Vegas + Henderson + North Las Vegas (Task 3).
- Cross-cutting: Wave-0 probes confirm no collision/drift; structural migration registered at 1100 (or Wave-0 number); external_id→UUID map captured for Plans 02/03.
</verification>

<success_criteria>
- Wave-0 probes + roster checkpoint recorded and passed before any write; on-disk MAX → structural number set (expected 1100).
- 1100_boulder_city_city_council.sql written + applied; post-verify PASSED (gov=1, exec=1 Mayor LOCAL_EXEC, local=4 council on ONE shared LOCAL district, section-split=0); exactly 2 districts on 3206500; registered in the ledger.
- 5 external_id→UUID map recorded in SUMMARY (consumed by Plans 02/03).
- Boulder City surfaces in coverage.js Nevada block (hasContext:true), LV + Henderson + North Las Vegas preserved.
</success_criteria>

<output>
Create `.planning/phases/165-boulder-city-deep-seed/165-01-SUMMARY.md` when done (record the Wave-0 probe values + roster confirmation, the post-verify results, the district/office counts, the 5 external_id→UUID map, and the coverage.js edit).
</output>
