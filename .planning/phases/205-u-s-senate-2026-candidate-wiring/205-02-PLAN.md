---
phase: 205-u-s-senate-2026-candidate-wiring
plan: 02
type: execute
wave: 2
depends_on: ["205-01"]
files_modified: []
autonomous: false
requirements: [REQ-2, REQ-3, REQ-4, REQ-5]
must_haves:
  truths:
    - "Every confidently-mapped U.S. Senate % race has a non-null office_id resolving through offices->districts to a NATIONAL_UPPER district for the race's state"
    - "No linked Senate race resolves to an office whose title starts with Candidate for U.S. Senate"
    - "For >=3 mapped states (MN, TX, TN + OH special), an in-state address surfaces the 2026 Senate race and its candidates, with House parity in the same response"
    - "Before/after diff shows only U.S. Senate % races' office_id changed; races total, race_candidates total, and NATIONAL_UPPER office/incumbent counts are unchanged"
    - "Any state left office_id NULL post-migration is listed with a reason in the skip report (research expects 0)"
  artifacts:
    - path: "supabase/migrations/878_link_us_senate_2026_races.sql"
      provides: "The approved migration applied to production"
      contains: "office_id IS NULL"
  key_links:
    - from: "essentials.races.office_id"
      to: "essentials.districts (NATIONAL_UPPER)"
      via: "races.office_id -> offices.id -> offices.district_id -> districts.id"
      pattern: "district_type.*NATIONAL_UPPER"
---

<objective>
Apply the human-approved migration `878_link_us_senate_2026_races.sql` to production, then
prove the fix with the RESEARCH.md 5-query verification block (DB parity + before/after diff)
and >=3 live in-state address checks (MN, TX, TN + the OH special).

Purpose: Realize the linkage (REQ-2), confirm candidates actually surface by address with House
parity (REQ-3), confirm the skip report is empty / documented (REQ-4), and prove no collateral
mutation touched House races, offices, districts, politicians, or race_candidates (REQ-5).
Output: A production-applied migration + a verification record + confirmed live surfacing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-u-s-senate-2026-candidate-wiring/205-SPEC.md
@.planning/phases/205-u-s-senate-2026-candidate-wiring/205-RESEARCH.md
@.planning/phases/205-u-s-senate-2026-candidate-wiring/205-VALIDATION.md
@.planning/phases/205-u-s-senate-2026-candidate-wiring/205-01-SUMMARY.md

<interfaces>
<!-- Baseline snapshot to diff against (RESEARCH `## Before/After Diff Footprint`),
     these counts MUST be byte-for-byte identical after apply:
       essentials.races total .............. 1,674
       U.S. Senate % races ................. 51 (all office_id NULL pre-apply)
       essentials.race_candidates total .... 2,483
       NATIONAL_UPPER office rows .......... 152
       NATIONAL_UPPER rows w/ incumbent .... 99

     Verification queries (RESEARCH `## Validation Architecture` / VALIDATION.md):
       REQ-2 parity: SELECT r.position_name, o.title, d.district_type, d.state
         FROM essentials.races r JOIN essentials.offices o ON r.office_id=o.id
         JOIN essentials.districts d ON o.district_id=d.id
         WHERE r.position_name ILIKE 'U.S. Senate %';
       REQ-4 skip:  SELECT position_name FROM essentials.races
         WHERE position_name ILIKE 'U.S. Senate %' AND office_id IS NULL;

     DB access: mcp__supabase-local IS production (writes are live). If that MCP tool
     is not exposed at execute time, fall back to psql via a Supabase-CLI short-lived
     credential (RESEARCH `## Environment Availability` / Open Question #2) — the SQL is
     identical either way; the committed file 878 is what gets applied. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot baseline, apply migration 878 to production, run DB parity + diff (REQ-2/4/5)</name>
  <files>supabase/migrations/878_link_us_senate_2026_races.sql</files>
  <read_first>
    - .planning/phases/205-u-s-senate-2026-candidate-wiring/205-01-SUMMARY.md — confirm the D-04 human approval was recorded and note any states moved to the skip report; do NOT apply if approval is absent.
    - supabase/migrations/878_link_us_senate_2026_races.sql — the exact SQL to apply.
    - .planning/phases/205-u-s-senate-2026-candidate-wiring/205-RESEARCH.md `## Before/After Diff Footprint` and `## Validation Architecture` — baseline counts + the named verification queries.
    - .planning/phases/205-u-s-senate-2026-candidate-wiring/205-VALIDATION.md — the per-req verification map.
  </read_first>
  <action>
    GATE: Verify 205-01-SUMMARY.md records explicit user approval of the seat map. If not approved, STOP — do not write to production.
    (1) BASELINE: capture the pre-apply snapshot via `mcp__supabase-local__execute_sql` (or psql fallback per RESEARCH `## Environment Availability`): `SELECT id, position_name, office_id FROM essentials.races WHERE position_name ILIKE 'U.S. Senate %' ORDER BY id` plus the 5 baseline counts (races total 1,674; race_candidates 2,483; NATIONAL_UPPER offices 152; NATIONAL_UPPER incumbents 99; U.S. Senate % races 51/all NULL). Record them in the SUMMARY.
    (2) APPLY: execute the exact contents of `supabase/migrations/878_link_us_senate_2026_races.sql` against production. This is the production write — it runs ONLY because it is gated behind the wave-1 D-04 approval (autonomous: false). Decide the ledger-row question at this point per RESEARCH Open Question #1 (default: audit-only, do NOT register a schema_migrations row unless there is a strong reason).
    (3) VERIFY IN THE SAME SESSION: run the REQ-2 parity query and assert every `U.S. Senate %` row now has `district_type='NATIONAL_UPPER'`, `o.title NOT ILIKE 'Candidate for U.S. Senate%'`, and `d.state` matching the race's state. Run the REQ-4 skip query (`... AND office_id IS NULL`) and assert it is empty (or every remaining row is documented in the skip report with a reason). Re-run the 5 baseline counts and assert races total = 1,674, race_candidates = 2,483, NATIONAL_UPPER offices = 152, NATIONAL_UPPER incumbents = 99 are UNCHANGED, and diff the before/after `id, position_name, office_id` snapshot to confirm the ONLY changed rows are previously-NULL `U.S. Senate %` office_ids (REQ-5). Do NOT run any second apply attempt if the first succeeded; re-running is a safe no-op but is unnecessary.
  </action>
  <verify>
    <automated>MANUAL — DB verification runs via mcp__supabase-local__execute_sql (or psql fallback) against production; assert: REQ-2 parity query returns 0 rows with district_type != 'NATIONAL_UPPER' or title ILIKE 'Candidate for U.S. Senate%'; REQ-4 NULL-count query returns 0 (or documented); post-apply counts (races=1674, race_candidates=2483, NATIONAL_UPPER offices=152, incumbents=99) equal baseline. Record query outputs in 205-02-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - 205-01-SUMMARY.md confirmed approval recorded before any write (hard gate).
    - Baseline snapshot + 5 counts captured pre-apply and recorded.
    - Migration 878 applied; every `U.S. Senate %` race that was mapped now has non-null `office_id`.
    - REQ-2 parity query: 100% of mapped rows resolve to `district_type='NATIONAL_UPPER'`, seat-title (NOT `Candidate for U.S. Senate%`), correct state — 0 violations.
    - REQ-4 query (`office_id IS NULL`) returns empty, or every remaining NULL row is listed with a reason in the skip report.
    - REQ-5 diff: races total = 1,674, race_candidates = 2,483, NATIONAL_UPPER offices = 152, NATIONAL_UPPER incumbents = 99 — all unchanged; only previously-NULL `U.S. Senate %` office_ids differ; no House race/office/district/politician row changed.
  </acceptance_criteria>
  <done>Migration 878 applied to production; DB parity + before/after diff prove correct linkage with zero collateral change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING live address surfacing check — MN, TX, TN, OH (REQ-3)</name>
  <files>N/A — read-only live-site verification; no files modified</files>
  <read_first>
    - .planning/phases/205-u-s-senate-2026-candidate-wiring/205-RESEARCH.md `## Validation Architecture` (REQ-3 row) — the >=3 sample states incl. the OH special.
    - .planning/phases/205-u-s-senate-2026-candidate-wiring/205-02-SUMMARY.md (in progress) — the recorded parity/diff results from Task 1.
  </read_first>
  <action>Direct the user to enter a real in-state address on essentials.empowered.vote for each of MN, TX, TN (regular) and OH (SPECIAL), and confirm each response surfaces the 2026 U.S. Senate race with its candidate list alongside the state's House race (House parity in the same response). BLOCK until the user reports the outcome. This is a live user-facing verification, not an implementation step — no data is written.</action>
  <what-built>
    Migration 878 is applied and DB parity confirms every mapped 2026 U.S. Senate race now resolves
    to its state's NATIONAL_UPPER seat office. This checkpoint confirms the real user-facing path
    (address -> district -> office -> race -> race_candidates) actually surfaces those candidates,
    at House parity.
  </what-built>
  <how-to-verify>
    On the deployed site (essentials.empowered.vote), enter a real in-state address for each of these
    >=3 mapped states plus the OH special (4 total), and confirm each response shows the 2026 U.S.
    Senate race with its candidate list ALONGSIDE the House race (House parity in the same response):
      1. Minnesota  (regular — Tina Smith seat)
      2. Texas      (regular — John Cornyn seat)
      3. Tennessee  (regular — Bill Hagerty seat)
      4. Ohio       (SPECIAL — Jon Husted seat)
    For each: the Senate race appears (not missing), lists its candidates from race_candidates, and
    appears next to the state's House race the same way it did before for the House alone.
  </how-to-verify>
  <verify>
    <automated>MANUAL — live browser check on essentials.empowered.vote for 4 states (MN, TX, TN, OH); no automated command. Outcome recorded in 205-02-SUMMARY.md.</automated>
  </verify>
  <resume-signal>Type "approved" if all 4 states surface the Senate race + candidates with House parity, or describe which state failed to surface (that state's linkage will be re-investigated as a gap).</resume-signal>
  <acceptance_criteria>
    - For all 4 states (MN, TX, TN, OH), an in-state address surfaces the 2026 U.S. Senate race with its candidate list.
    - The Senate race appears alongside the state's House race (House parity) in the same response.
    - The OH SPECIAL seat surfaces correctly (special-election case confirmed).
    - The user gave an explicit "approved" signal; the outcome is recorded in 205-02-SUMMARY.md (any non-surfacing state is logged as a gap).
  </acceptance_criteria>
  <done>User confirms Senate candidates surface by address with House parity for MN, TX, TN, and the OH special; outcome recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved SQL -> production DB | The actual mutation crosses here; only `essentials.races.office_id` should change |
| DB state -> live results page | Verifies the mutation surfaces correctly to voters without collateral regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-05 | Tampering | Collateral mutation of House races / offices / districts / politicians / race_candidates | mitigate | UPDATE scoped to `races` where `position_name ILIKE 'U.S. Senate %'` AND `office_id IS NULL`; before/after diff asserts races=1674, race_candidates=2483, NATIONAL_UPPER offices=152, incumbents=99 unchanged (REQ-5) |
| T-205-06 | Tampering | Wrong-seat / stray-office linkage surviving to prod | mitigate | Post-apply REQ-2 parity query asserts every mapped row is NATIONAL_UPPER + seat-title (not `Candidate for U.S. Senate%`) + correct state; live address check on 4 states confirms real candidates surface |
| T-205-07 | Denial | Non-idempotent re-apply corrupting state | mitigate | `AND office_id IS NULL` guard = re-run no-op; no second apply attempted after a successful first |
| T-205-08 | Repudiation | Production write with no recorded authorization | mitigate | Hard gate in Task 1: apply blocked unless 205-01-SUMMARY.md records the D-04 human approval; plan is autonomous:false |
| T-205-SC | Tampering | Package/dependency installs | accept | N/A — data-only phase installs no packages (RESEARCH `## Standard Stack`) |

Block on: T-205-05, T-205-06 (high severity — wrong or collateral production data). Both are proven by post-apply parity + diff before the plan completes.
</threat_model>

<verification>
- REQ-2: parity query returns 0 violations (all mapped rows NATIONAL_UPPER, seat-title, correct state).
- REQ-3: 4 live in-state addresses (MN, TX, TN, OH) surface the Senate race + candidates with House parity.
- REQ-4: post-migration NULL-count query empty (or documented skip report).
- REQ-5: before/after diff — only `U.S. Senate %` office_ids changed; all baseline counts unchanged.
</verification>

<success_criteria>
- Migration 878 applied to production (behind the wave-1 approval gate).
- DB parity + before/after diff pass (REQ-2, REQ-4, REQ-5) with zero collateral change.
- >=3 mapped states + the OH special surface Senate candidates by address with House parity (REQ-3).
</success_criteria>

<output>
Create `.planning/phases/205-u-s-senate-2026-candidate-wiring/205-02-SUMMARY.md` when done, recording: baseline vs post-apply counts, the REQ-2 parity result, the REQ-4 skip result, the before/after diff of changed rows, and the 4-state live-address check outcome.
</output>
