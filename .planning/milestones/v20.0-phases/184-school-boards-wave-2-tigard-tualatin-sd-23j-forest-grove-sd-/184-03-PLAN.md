---
phase: 184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd
plan: 03
type: execute
wave: 3
depends_on: [184-02]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-headshots.py
  - C:/EV-Accounts/backend/migrations/1207_or_westmetro_school_boards_wave2_headshots.sql
autonomous: false
requirements: [WSCH-03, WSCH-04, WSCH-05]
user_setup: []

must_haves:
  truths:
    - "A headshot pipeline script _tmp-westmetro-school-wave2-headshots.py exists that sources all director headshots across THREE distinct CDN paths — TTSD finalsite untransformed originals, FGSD Edlio direct JPEGs, SSD via the WordPress REST API `/wp-json/wp/v2/media/{id}` → media_details.sizes.large.source_url (NOT the on-page fly-images pre-cropped URL) — crops to 4:5 FIRST then resizes to 600×750 (Lanczos q90), composites any RGBA source onto white, uploads each to Supabase Storage politician_photos/{uuid}-headshot.jpg (x-upsert), and resolves the politician UUID at runtime by external_id (NOT hardcoded)"
    - "The script carries the IN-01 FIX (`.env` parser strips surrounding quotes and an optional leading `export `) and the standing WR-01/WR-02/WR-C fixes verbatim: main() exits non-zero if ANY sourced official fails; the test-download guard returns bytes reused by the first official; the len(OFFICIALS)>0 guard protects the index access"
    - "The script contains a `resolve_wp_media_large_url(media_api_url)` helper (net-new this wave) that GETs the Sherwood WP media endpoint and returns media_details.sizes.large.source_url (falling back to source_url), used for the 5 Sherwood sources whose roster entries carry a `wp_media_api_url` field (attachment ids 10012 Carson / 10127 Kaufman / 10011 Hawkins / 10010 Moller / 10013 Thornton); it NEVER uses the on-page fly-images URL directly (transform-output trap, same class as finalsite t_image_size_6)"
    - "The OFFICIALS roster's expected-count guard reflects the ACTUAL run count (14 if Linda Harrington ships as a documented gap, 15 if a usable local-news photo was found) — the Wave-1 `len(OFFICIALS) != 14: raise` literal is replaced with the actual expected count and a comment explaining why; Harrington is either omitted with a documented-gap comment or carries a real local-news-sourced url, never the 'Coming Soon' placeholder"
    - "The script hard-fails fast on a malformed roster (unique ext_ids, every official carries a license string and a source url, MIN_DIM reject); it derives the Storage CDN base from SUPABASE_URL (never a hardcoded project ref) and uses the `py` launcher (bare python/python3 are non-functional stubs on this machine)"
    - "The orchestrator ran the script via `py` and captured a per-official manifest (external_id → UUID → CDN url + license + source); TTSD small-source upscale-with-caveat (A5) and Matt Kaufman's near-square center-crop (A6) are exercised as expected; the Harrington gap decision is recorded"
    - "An AUDIT-ONLY migration 1207_or_westmetro_school_boards_wave2_headshots.sql exists with the WR-03 FIX applied to every INSERT block — each block is `INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) SELECT gen_random_uuid(), p.id, '<CDN_URL>', 'default', '<license>' FROM essentials.politicians p WHERE p.external_id = -N AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id = p.id)` so a missing politician skips the block instead of inserting a NULL politician_id"
    - "1207 is AUDIT-ONLY: it does NOT write a supabase_migrations.schema_migrations ledger row (matching the freshest headshot migration 1205); it carries a post-verify uuid-embed gate that derives its identity set from the SAME uuid literals the INSERTs use (so a clone that edits INSERT uuids fails loudly, not vacuously); any director whose upload failed / gap stands has its entire INSERT block removed with a documented-gap comment"
    - "The orchestrator applied 1207 and the headshot-count gate confirms the number of politician_images rows for the 15 ext_ids equals the number of successful uploads (target 15, or 14 if the Harrington gap stands)"
    - "0 compass stance rows remain for all 15 directors (0-by-design success state — verified unchanged); no FGSD student-rep image is inserted (D-R4)"
    - "Both files saved UTF-8; verify-gate hygiene: literal strings slug / schema_migrations / photo_origin_url do not appear in 1207 (politician_images has NO photo_origin_url column); the filename number is re-confirmed against on-disk `ls` MAX immediately before authoring (1207 expected, but re-probe — collisions are real)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-headshots.py"
      provides: "Headshot ETL: source 15 (or 14) images across 3 CDN paths (finalsite/Edlio/WP-REST) → crop-4:5-first → 600×750 Lanczos q90 → white-composite RGBA → upload to Storage {uuid}-headshot.jpg → print SUCCESS/FAILED manifest; IN-01 + WR-01/02/C fixes; resolve_wp_media_large_url helper; non-zero exit on any failure"
      contains: "resolve_wp_media_large_url"
      min_lines: 250
    - path: "C:/EV-Accounts/backend/migrations/1207_or_westmetro_school_boards_wave2_headshots.sql"
      provides: "Audit-only politician_images rows (one per success director, WR-03-fixed FROM-politicians-p shape, columns id/politician_id/url/type/photo_license, type='default', idempotent NOT EXISTS); uuid-embed post-verify gate; NO ledger registration"
      contains: "INSERT INTO essentials.politician_images"
      min_lines: 60
  key_links:
    - from: "_tmp-westmetro-school-wave2-headshots.py"
      to: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      via: "requests.put with x-upsert, CDN base derived from SUPABASE_URL"
      pattern: "x-upsert"
    - from: "_tmp-westmetro-school-wave2-headshots.py (Sherwood sources)"
      to: "sherwood.k12.or.us/wp-json/wp/v2/media/{id}"
      via: "resolve_wp_media_large_url GET → media_details.sizes.large.source_url"
      pattern: "wp-json/wp/v2/media"
    - from: "essentials.politician_images (15 rows)"
      to: "essentials.politicians (ext_id -4112241..-4112245 / -4105161..-4105165 / -4111291..-4111295)"
      via: "FROM essentials.politicians p WHERE p.external_id = -N (WR-03 shape)"
      pattern: "external_id = -41122|external_id = -41051|external_id = -41112"
---

<objective>
Source, process, and record 600×750 headshots for all 15 directors across three different CDN sourcing
paths: author the ETL pipeline script, run it to upload the images to Supabase Storage, then author and
apply the audit-only politician_images migration (WR-03 fix baked in) for every successfully-uploaded
director. Linda Harrington's placeholder gap is either resolved with a real local-news photo or documented
honestly, never fabricated (D-R5).

Purpose: Satisfy the headshot half of WSCH-03 + WSCH-04 + WSCH-05. Unlike Wave 1's uniform finalsite
sourcing, this wave needs THREE code paths in one script: (1) TTSD finalsite untransformed originals (small
circular photos, Lanczos-upscale-with-caveat per A5, same class as Hillsboro Wave 1); (2) FGSD Edlio direct
JPEGs (4/5 genuine, Position 4 Harrington a documented gap); (3) SSD via the WP REST API
`media_details.sizes.large.source_url` (4/5 native 2400×3000 exact-4:5 no-op crop, Matt Kaufman near-square
1831×1694 needing a real center-crop per A6). The script is a gitignored `_tmp-*` helper; 1207 is an
audit-only migration (no ledger row, like 1205), with the WR-03 fix applied so a missing politician skips
its block rather than inserting a NULL politician_id.
Output: `_tmp-westmetro-school-wave2-headshots.py` + `1207_or_westmetro_school_boards_wave2_headshots.sql`
+ recorded upload manifest and headshot-count gate for downstream plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-RESEARCH.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md
@.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.py` script and the `.sql` migration. It has NO
database access, NO Storage access, and NO Supabase MCP. The INLINE ORCHESTRATOR runs the script via
`py C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-headshots.py` (bare `python`/`python3` are
non-functional Windows Store stubs — MUST use `py`), reads the SUCCESS/FAILED manifest, then applies 1207
via `psql "$DATABASE_URL" -f <file>`. The `.py` script is a gitignored `_tmp-*` helper — do NOT commit it.
C:/EV-Accounts is a SEPARATE git repo — never cd into it for commits; use `git -C "C:/EV-Accounts"`.

**THREE CDN PATHS (Key Finding 4):** finalsite untransformed originals (TTSD — strip the transform
segment, never `t_image_size_N`); Edlio direct JPEGs (FGSD — but Harrington's on-page image is a 4.3KB
"Coming Soon" placeholder, NOT a photo); WordPress REST API `media_details.sizes.large.source_url` (SSD —
NEVER the on-page fly-images pre-cropped URL). See 184-RESEARCH.md §"Pattern 4" + §"Code Examples" for the
verbatim 15 source URLs and the 5 Sherwood attachment ids.

**WR-03 BAKED IN (D-F1):** do NOT clone 1205's `INSERT ... SELECT (SELECT id FROM politicians WHERE
external_id=...)` scalar-subquery shape — it degrades to a NULL politician_id if the politician is absent
(the guard is vacuously true). Author the FIXED `FROM essentials.politicians p WHERE p.external_id = -N
AND NOT EXISTS (...)` shape directly, per 184-PATTERNS.md §"Fixed shape for 1207".

**VERIFY-GATE HYGIENE:** keep the literal strings `slug`, `schema_migrations`, and `photo_origin_url`
out of 1207 (politician_images is exactly `(id, politician_id, url, type, photo_license)` — no
photo_origin_url column). Save both files UTF-8.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot ETL pipeline script (3 CDN sourcing paths)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-headshots.py</files>
  <read_first>
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-02-SUMMARY.md (the 15 external_id → politician UUID map; roster names + Position; TTSD Chair/VC re-verify result)
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-RESEARCH.md §"Code Examples" (the 15 verbatim source URLs + the 5 Sherwood attachment ids), §"Pattern 4" (WP REST original-recovery), §"Assumptions Log" (A5 TTSD small sources, A6 Kaufman center-crop), §"Environment Availability" (py launcher, Pillow/psycopg2/requests versions)
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md §"_tmp-westmetro-school-wave2-headshots.py" (IN-01 env-parser fix, roster shape + count-guard note, the resolve_wp_media_large_url helper, "everything else unchanged from Wave 1" list)
    - C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave1-headshots.py (PRIMARY analog — read in full: config/env-load, OFFICIALS shape, resolve_politician_id, download_image, crop_to_4_5 (incl. RGBA/circle-cutout branch), resize_600x750, process_headshot_bytes, upload_to_storage, test_download_guard, process_member, main() with the WR-01/WR-C/WR-02 fixes — copy these verbatim except the two deltas below)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-REVIEW.md §IN-01 (the exact `.env` parser fix)
  </read_first>
  <action>
    Adapt _tmp-westmetro-school-wave1-headshots.py into a 15-official (or 14 if Harrington's gap stands),
    3-CDN-path pipeline. TWO deltas from Wave 1, everything else copied verbatim:
    DELTA 1 — IN-01 env-parser fix: the `.env` loader strips an optional leading `export ` and surrounding
    single/double quotes from each value before assigning (per 184-PATTERNS §"Config/env-load pattern").
    Keep the SUPABASE_URL-derived CDN_BASE (never hardcode the project ref), BUCKET 'politician_photos',
    TARGET_SIZE (600,750), JPEG_QUALITY 90, RESAMPLE Lanczos, DESCRIPTIVE_HEADERS with the EmpoweredVote
    UA, DOWNLOAD_TIMEOUT 30, MIN_DIM reject.
    DELTA 2 — resolve_wp_media_large_url(media_api_url) helper (net-new, per 184-PATTERNS §"NEW sourcing
    code path"): GET the Sherwood WP media endpoint with DESCRIPTIVE_HEADERS, raise on non-200, read
    data['media_details']['sizes'] and return sizes['large']['source_url'] if present else
    data['source_url']. NEVER use the on-page fly-images URL directly.
    OFFICIALS list — 15 dicts (5 TTSD + 5 FGSD + 5 SSD), each {'ext_id','name','url'|'wp_media_api_url',
    'license'} + an inline sourcing comment:
      TTSD (ext_id -4112241..-4112245, license 'press_use'): the 5 finalsite untransformed-original URLs
      from RESEARCH §Code Examples (Jaimes/Miles/Irvin/Zurschmeide/Weston) — small circular, Lanczos-upscale
      with a documented caveat (A5), same class as Hillsboro Wave 1.
      FGSD (ext_id -4105161..-4105165, license 'press_use'): the 4 genuine Edlio JPEG URLs
      (Franco/Truax/Lozano/Kottkey). Position 4 Linda Harrington (-4105164): do NOT use the 4.3KB "Coming
      Soon" placeholder — at execution time attempt a targeted local-news photo fetch
      (forestgrovenewstimes.com / newsinthegrove.com June-2026 appointment coverage); if a usable photo is
      found, add her entry with that url; else OMIT her entry with a documented-gap comment (do not silently
      drop her from the roster comment).
      SSD (ext_id -4111291..-4111295, license 'press_use'): each carries a `wp_media_api_url`
      (https://sherwood.k12.or.us/wp-json/wp/v2/media/{id}) — 10012 Carson / 10127 Kaufman / 10011 Hawkins /
      10010 Moller / 10013 Thornton — resolved via resolve_wp_media_large_url() before download. Matt
      Kaufman (near-square 1831×1694) exercises the genuine (non-no-op) crop_to_4_5 center-crop branch.
    process_member(): if a roster entry has `wp_media_api_url`, resolve it to a real image url via
    resolve_wp_media_large_url() first, then hand that to download_image(); otherwise download the plain
    `url` directly. Guard-check before any network call: unique ext_ids, every official has a license
    string AND a source url, count == the actual expected run count (14 or 15 — NOT the Wave-1 literal 14
    unmodified; replace it with the real number and comment why). Copy verbatim from Wave 1:
    resolve_politician_id (runtime UUID lookup by external_id — no hardcoded id), download_image
    (verify=True, status + empty-body checks — NO SSL bypass), crop_to_4_5 (incl. the RGBA/circle-cutout
    inscribed-crop branch), resize_600x750, process_headshot_bytes (MIN_DIM reject; assert final ==
    (600,750)), upload_to_storage (x-upsert PUT), and main()'s WR-01 (sys.exit(1) on any failure) / WR-C
    (len(OFFICIALS)>0 guard) / WR-02 (test guard returns reused bytes). Final manifest print: one
    `SUCCESS: {ext_id} {name} {uuid} -> {cdn_url} [license=..., source=...]` or `FAILED: {ext_id} {name}
    -- {error}` line per official. No fenced code in this action — the wave1 script is the structural
    source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator (Task 3) runs `py C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-headshots.py` and reads the manifest</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-headshots.py
    - `.env` parser strips a leading `export ` and surrounding quotes (IN-01); CDN_BASE is derived from SUPABASE_URL (grep finds a split on the https host; no hardcoded project-ref literal)
    - Contains resolve_wp_media_large_url reading media_details.sizes.large.source_url; grep finds `wp-json/wp/v2/media` in the Sherwood roster entries and no `fly-images` URL used as a download source
    - OFFICIALS has 14 or 15 dicts with ext_ids -4112241..-4112245, -4105161..-4105165 (minus -4105164 if the gap stands), -4111291..-4111295; TTSD/FGSD carry `url`, SSD carry `wp_media_api_url`; each carries a license
    - The Wave-1 `len(OFFICIALS) != 14` literal is replaced with the actual expected run count and a comment; Harrington is omitted-with-comment or carries a real local-news url (never the placeholder)
    - Contains resolve_politician_id (runtime UUID lookup) — no hardcoded politician_id in the roster
    - Contains crop_to_4_5 (with RGBA→white branch and center-crop that genuinely fires for Kaufman), resize_600x750, process_headshot_bytes (MIN_DIM reject), upload_to_storage (x-upsert)
    - WR-01 (sys.exit(1) on any failure), WR-C (len(OFFICIALS)>0 guard), WR-02 (test guard returns reused bytes) all present; download_image uses verify=True (no SSL bypass)
    - Guard-check rejects non-unique ext_ids / wrong count / missing license or url before any network call
    - Manifest prints SUCCESS/FAILED per official
  </acceptance_criteria>
  <done>Headshot pipeline authored for all 15 directors across 3 CDN paths, with IN-01 + WR-01/02/C fixes, the WP-REST original-recovery helper, runtime UUID resolution, and SUPABASE_URL-derived CDN base.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration 1207 (WR-03 fix)</name>
  <files>C:/EV-Accounts/backend/migrations/1207_or_westmetro_school_boards_wave2_headshots.sql</files>
  <read_first>
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-02-SUMMARY.md (the 15 external_id → politician UUID map — the UUIDs go in the url strings AND the post-verify gate)
    - .planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-PATTERNS.md §"1207 ..._headshots.sql" (header w/ 3-CDN sourcing notes + documented-gap section; the WR-03-FIXED per-official INSERT shape; the uuid-embed post-verify gate)
    - .planning/phases/183-school-boards-wave-1-beaverton-sd-48j-hillsboro-sd-1j/183-REVIEW.md §WR-03 (the exact fixed INSERT SQL)
    - C:/EV-Accounts/backend/migrations/1205_or_westmetro_school_boards_wave1_headshots.sql (PRIMARY analog — read in full: AUDIT-ONLY header, no ledger row, the exact Storage CDN base string https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg, the uuid-embed post-verify gate shape; do NOT copy its scalar-subquery INSERT shape — apply the WR-03 fix)
  </read_first>
  <action>
    Write an AUDIT-ONLY migration with a header block that: states it records politician_images rows for
    the 600×750 headshots uploaded by _tmp-westmetro-school-wave2-headshots.py; marks the file explicitly
    AUDIT-ONLY (does NOT write a ledger row — matches 1205); lists the exact column set (id, politician_id,
    url, type, photo_license) and notes there is no image-origin column; carries THREE CDN sourcing notes
    (TTSD finalsite untransformed originals / FGSD Edlio direct JPEGs / SSD WP-REST media_details.sizes.large
    — per 184-PATTERNS §header pattern); carries a DOCUMENTED GAPS section naming -4105164 Linda Harrington
    (FGSD Position 4) as a "Coming Soon" placeholder gap unless a real photo was found; carries an
    ORCHESTRATOR NOTE: DELETE the entire INSERT block for any director the pipeline manifest does NOT report
    SUCCESS for (honest gap, no fabrication) and keep a documented-gap comment for each deletion. Wrap the
    INSERTs in BEGIN;...COMMIT; (no ledger INSERT after).
    Write 15 (or 14) INSERT INTO essentials.politician_images blocks — one per SUCCESS director — each using
    the WR-03-FIXED shape: `INSERT INTO essentials.politician_images (id, politician_id, url, type,
    photo_license) SELECT gen_random_uuid(), p.id, '<CDN_URL>', 'default', '<license>' FROM
    essentials.politicians p WHERE p.external_id = -N AND NOT EXISTS (SELECT 1 FROM
    essentials.politician_images pi WHERE pi.politician_id = p.id)`. `<CDN_URL>` = the fixed project Storage
    CDN base (https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/)
    + the director's minted UUID (from 184-02-SUMMARY.md) + '-headshot.jpg'; type='default'; photo_license
    per the manifest (uniform 'press_use' expected). Order the blocks TTSD Position 1-5, then FGSD Position
    1-5 (skip 4 if the Harrington gap stands), then SSD Position 1-5, with a name/position + source comment
    on each. Then a post-verify uuid-embed DO block (per 184-PATTERNS §"Post-verify uuid-embed gate"): a
    VALUES list of the SAME uuid literals used in the INSERTs (one row per shipped director), asserting each
    has a politician_images row whose url embeds that uuid — RAISE EXCEPTION if any is missing. No fenced
    code in this action — 1205 + 184-PATTERNS §"1207 ..." + 183-REVIEW WR-03 are the structural source of
    truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator (Task 3) applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1207_or_westmetro_school_boards_wave2_headshots.sql` after the script run and confirms the headshot-count gate</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/1207_or_westmetro_school_boards_wave2_headshots.sql, wrapped in BEGIN;...COMMIT;
    - Header explicitly marks the file AUDIT-ONLY and states no ledger row is written; grep finds no `schema_migrations` INSERT; carries the 3-CDN sourcing notes + the Harrington documented-gap section
    - 14-15 `INSERT INTO essentials.politician_images` blocks using the WR-03 shape (`FROM essentials.politicians p WHERE p.external_id = -N`); grep confirms NO scalar-subquery `(SELECT id FROM ... WHERE external_id`) inside the INSERT column-value or NOT EXISTS clauses; grep finds no `photo_origin_url` and no `slug`
    - Each url uses the kxsdzaojfaibhuzmclfq storage base + a UUID + '-headshot.jpg'; each block resolves via WHERE p.external_id = -N for the ext_ids -4112241..-4112245, -4105161..-4105165 (minus -4105164 if gap), -4111291..-4111295
    - Each block type='default'; photo_license set (expected 'press_use'); each guarded by NOT EXISTS on pi.politician_id
    - A post-verify uuid-embed DO block derives its VALUES set from the same uuid literals the INSERTs use and RAISEs if any lacks a uuid-embedding url
    - An ORCHESTRATOR NOTE documents the delete-block-on-gap procedure
    - File saved UTF-8; filename number re-confirmed against on-disk `ls` MAX at authoring time
  </acceptance_criteria>
  <done>Audit-only migration 1207 authored with 14-15 WR-03-fixed idempotent politician_images INSERT blocks, a uuid-embed post-verify gate, the Harrington gap section, and no ledger registration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs the pipeline, resolves the Harrington gap, applies 1207, confirms the headshot count</name>
  <what-built>The headshot pipeline script + the audit-only migration 1207. The orchestrator runs the pipeline to upload the images across 3 CDN paths, decides the Harrington gap, reconciles the manifest against 1207, and applies 1207.</what-built>
  <how-to-verify>
    A. Run: `py C:/EV-Accounts/backend/scripts/_tmp-westmetro-school-wave2-headshots.py` (MUST use `py`,
       not bare python). Confirm the manifest: TTSD 5/5 (small finalsite originals, upscaled with caveat
       A5), FGSD 4/5 genuine (Position 4 Harrington handled per the gap decision), SSD 5/5 (via WP-REST
       large rendition; Kaufman center-cropped, A6). The script exits 0 only if all SOURCED officials
       succeeded (WR-01). Before the run, attempt one targeted local-news photo fetch for Linda Harrington
       (forestgrovenewstimes.com / newsinthegrove.com June-2026 appointment coverage); if a usable
       600×750-capable photo is found, add her to OFFICIALS and 1207; else document the gap (do NOT ship
       the "Coming Soon" placeholder) — an honest partial per D-R5. Record the per-official manifest + the
       Harrington decision in the SUMMARY.
    B. Reconcile 1207 against the manifest: confirm each retained INSERT block's UUID matches the manifest
       UUID for that ext_id (and the 184-02-SUMMARY.md UUID map), and that the post-verify uuid-embed
       VALUES list matches the retained blocks. DELETE the entire INSERT block (and its VALUES-list row) for
       any director that FAILED or whose gap stands, adding a documented-gap comment. Confirm the remaining
       block count equals the number of SUCCESS uploads (15, or 14 if the Harrington gap stands).
    C. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1207_or_westmetro_school_boards_wave2_headshots.sql`.
       Confirm no error, the uuid-embed post-verify passes, and no ledger row was written (`SELECT COUNT(*)
       FROM supabase_migrations.schema_migrations WHERE version='1207'` → 0).
    D. Headshot-count gate: `SELECT COUNT(*) FROM essentials.politician_images pi JOIN
       essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -4112245 AND -4112241
       OR p.external_id BETWEEN -4105165 AND -4105161 OR p.external_id BETWEEN -4111295 AND -4111291` →
       equals the number of SUCCESS uploads (target 15, or 14 if the Harrington gap stands). Spot-check
       2-3 CDN urls return HTTP 200 (include one TTSD upscaled + one SSD).
    E. Re-confirm 0-stance-by-design: `SELECT COUNT(*) FROM inform.politician_answers pa JOIN
       essentials.politicians p ON pa.politician_id=p.id WHERE p.external_id BETWEEN -4112245 AND -4112241
       OR p.external_id BETWEEN -4105165 AND -4105161 OR p.external_id BETWEEN -4111295 AND -4111291` → 0
       (any row here is a defect). Confirm no FGSD student-rep image row exists.
  </how-to-verify>
  <acceptance_criteria>
    - Pipeline run via `py` produced a manifest across all 3 CDN paths; SUCCESS count recorded (target 15, or 14 if the Harrington gap is documented); the Harrington decision (real photo vs honest gap) recorded
    - 1207 reconciled to the manifest (failed/gap blocks + their uuid-embed VALUES rows deleted with a comment); UUIDs match the 184-02-SUMMARY.md map
    - 1207 applied cleanly; uuid-embed post-verify passed; no ledger row for '1207'
    - Headshot-count gate equals the SUCCESS count; 2-3 CDN urls confirmed HTTP 200 (incl. one TTSD + one SSD)
    - 0 compass stance rows for all 15 directors (success state); no FGSD student-rep image inserted
  </acceptance_criteria>
  <resume-signal>Type "approved" with the manifest + Harrington decision + headshot-count + 0-stance results, or describe which upload/gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external CDN | Downloading images from resources.finalsite.net (TTSD), 3.files.edl.io (FGSD), and sherwood.k12.or.us WP uploads (SSD) — all the districts' own official asset hosts |
| operator → external web API | GET sherwood.k12.or.us/wp-json/wp/v2/media/{id} (public, unauthenticated WP REST endpoint) for original-image recovery |
| operator → Supabase Storage | Uploading processed 600×750 JPEGs via the service-role key |
| operator → live DB | Applying the audit-only politician_images migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-11 | Tampering | malicious/corrupt image payload (image bomb or disguised non-image) | mitigate | Pillow decode in process_headshot_bytes fails closed on non-image content; MIN_DIM reject; all sources are the districts' own official hosts |
| T-184-12 | Tampering (data integrity) | applying 1207 on a silently-partial upload → DB rows pointing at missing Storage objects | mitigate | WR-01 non-zero exit halts on any failure; the orchestrator reconciles 1207 to the manifest and deletes failed/gap blocks before apply |
| T-184-13 | Spoofing | shipping Linda Harrington's "Coming Soon" placeholder as a real headshot | mitigate | D-R5 document-gap-not-fabricate: the placeholder (4.3KB) is never shipped; attempt a real local-news photo first, else honest gap |
| T-184-14 | Tampering | shipping a needlessly-cropped/low-quality Sherwood fly-images rendition instead of the 2400×3000 original | mitigate | resolve_wp_media_large_url queries the WP REST API and prefers media_details.sizes.large; the on-page fly-images URL is never used as a download source |
| T-184-15 | Tampering | headshot INSERT degrading to politician_id=NULL when a politician lookup misses | mitigate | WR-03 fix: `FROM politicians p WHERE p.external_id = -N` skips the block entirely on a miss (never inserts NULL); the uuid-embed post-verify RAISEs if any targeted uuid lacks its row |
| T-184-16 | Information Disclosure | leaking the service-role key from the gitignored _tmp-* script | mitigate | Key loaded at runtime from C:/EV-Accounts/backend/.env; the `_tmp-*` script is gitignored and never committed |
| T-184-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — Pillow/psycopg2/requests already present (RESEARCH §Environment); no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Pipeline uploads all 15 across 3 CDN paths (or documents the Harrington gap); WR-01 blocks a partial-run apply.
- 1207 is audit-only (no ledger row), WR-03-fixed, and reconciled to the manifest before apply.
- Headshot-count gate matches the success count; the uuid-embed post-verify passes; 0 compass stance rows remain (success state).
</verification>

<success_criteria>
- All 15 director headshots uploaded to Storage at 600×750 (or the Harrington gap documented) across the
  three CDN sourcing paths; politician_images rows applied via audit-only WR-03-fixed 1207; headshot-count
  gate green; 0 stance rows for all 15; no FGSD student-rep image inserted.
</success_criteria>

<output>
Create `.planning/phases/184-school-boards-wave-2-tigard-tualatin-sd-23j-forest-grove-sd-/184-03-SUMMARY.md`
when done, recording: the per-official upload manifest (ext_id → UUID → CDN url + license + which CDN path),
the SUCCESS count and the Linda Harrington gap decision, the applied audit-only migration (1207, no ledger
row), the headshot-count gate result, and the re-confirmed 0-stance-by-design baseline — for downstream
plan 04.
</output>
