---
phase: 143-santa-clarita-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["143-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/896_santa_clarita_headshots.sql
autonomous: false
requirements: [SCLR-01]

must_haves:
  truths:
    - "Marsha McLean (external_id -700181) has a 600×750 headshot in Supabase Storage politician_photos + a type='default' politician_images row"
    - "Bill Miranda (external_id -700182) has a 600×750 headshot in Storage + a type='default' politician_images row"
    - "Every current Santa Clarita official (all 5: Weste/Ayala/Gibbs/McLean/Miranda) has exactly ONE type='default' politician_images row, or a documented gap (Gibbs already deduped 2→1 in Plan 01)"
    - "Each new headshot shows the correct person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 (not stretched)"
    - "Migration 896 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/896_santa_clarita_headshots.sql"
      provides: "Audit-only headshot INSERTs for McLean + Miranda + photo_origin_url backfill + full-roster image audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      to: "essentials.politician_images.url (type='default', press_use)"
      via: "INSERT politician_images guarded by WHERE NOT EXISTS on politician_id"
      pattern: "'default'"
---

<objective>
Give the 2 newly-seated officials (McLean, Miranda) official headshots and confirm the whole current
5-person roster has clean single-image coverage. The 3 existing members (Weste/Ayala/Gibbs) already
have headshots — Gibbs was deduped 2→1 in Plan 01 — so this wave does NOT re-upload them; it fills the
2 new gaps and audits coverage. No headshot is needed for Cameron Smyth (departed/retired; 0 images is correct).

New headshots needed (santaclarita.gov WordPress media CDN — direct PNG download, standard curl/WebFetch;
HTTP 200 confirmed 2026-06-19; NOT the Sacramento AEM/CSS-background pattern):
- Marsha McLean (-700181): https://santaclarita.gov/city-council/wp-content/uploads/sites/39/2023/09/MarshaMclean.png
- Bill Miranda  (-700182): https://santaclarita.gov/city-council/wp-content/uploads/sites/39/2023/09/BillMiranda.png

Processing (NON-NEGOTIABLE per project memory): download PNG → crop to 4:5 FIRST (never stretch — crop
only, eyes ~1/3 from top, full head+shoulders) → resize 600×750 Lanczos q90 JPEG → upload to Supabase
Storage politician_photos bucket as {politician_uuid}-headshot.jpg → INSERT politician_images
(type='default', photo_license='press_use'). Reject any source with superimposed text/graphics over the face.

Optional quality pass (Claude's discretion): the Ayala portrait was uploaded Dec 2024 (fresher than the
2023 set) — if the existing Ayala/Weste image is visibly low quality, re-source from the confirmed
santaclarita.gov URLs (Weste 2023/09/LaureneWeste.png, Ayala 2024/12/PatsyAyala.png). Do this only if it
improves quality; do not churn good existing images.

Purpose: A voter sees the right face next to each official. Wrong-person or text-overlaid photos destroy
trust (memory: no superimposed graphics, no aspect-ratio distortion). Output: Migration 896 (audit-only
— NOT registered in schema_migrations) + 2 new portraits live; a human checkpoint confirms the new
portraits are the correct person and correctly cropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md
@.planning/phases/143-santa-clarita-deep-seed/143-02-SUMMARY.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §KEY RESEARCH QUESTION 3) -->

Confirmed source URLs (santaclarita.gov WordPress CDN, all HTTP 200 image/png 2026-06-19):
  McLean (-700181): https://santaclarita.gov/city-council/wp-content/uploads/sites/39/2023/09/MarshaMclean.png
  Miranda (-700182): https://santaclarita.gov/city-council/wp-content/uploads/sites/39/2023/09/BillMiranda.png
  (existing, only if a quality re-source is warranted:)
  Weste  (665693): https://santaclarita.gov/city-council/wp-content/uploads/sites/39/2023/09/LaureneWeste.png
  Ayala  (665689): https://santaclarita.gov/city-council/wp-content/uploads/sites/39/2024/12/PatsyAyala.png
  Gibbs  (665692): https://santaclarita.gov/city-council/wp-content/uploads/sites/39/2023/09/JasonGibbs.png

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face (reject "Re-Elect"-style banners)

Storage + DB:
  - Bucket: politician_photos
  - Path: {politician_uuid}-headshot.jpg
  - politician_images: type='default', photo_license='press_use'
  - Also UPDATE politicians.photo_origin_url to the santaclarita.gov source page where NULL
  - INSERT guarded by WHERE NOT EXISTS on politician_id (Fremont pattern, migration 212)

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql or psql -f). It does
NOT register in schema_migrations (same convention as migrations 880/212). On-disk counter authoritative.

New official UUIDs resolved by external_id (-700181, -700182) at apply time — do not hardcode.
Cameron Smyth (-700180) is retired — NO headshot action.

Pattern migration: 212_fremont_headshots.sql · precedent: 880_long_beach_headshots.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download, crop, resize, upload the 2 new official headshots (McLean, Miranda)</name>
  <files>C:/EV-Accounts/backend/migrations/896_santa_clarita_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/212_fremont_headshots.sql (audit-only headshot INSERT pattern)
    - C:/EV-Accounts/backend/migrations/880_long_beach_headshots.sql (direct precedent — download/crop/upload flow)
    - .planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md (§KEY RESEARCH QUESTION 3 — exact URLs + WordPress CDN method)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots)
  </read_first>
  <action>
    For McLean (-700181) and Miranda (-700182), download the source PNG from the exact santaclarita.gov
    WordPress URL in the interfaces (standard curl/WebFetch — no special headers; the WordPress CDN is
    directly accessible, HTTP 200, no WAF). Crop each to 4:5 (crop only — never stretch; eyes ~1/3 from top,
    full head+shoulders) then resize to 600×750 Lanczos q90 JPEG (Python Pillow or equivalent). Reject any
    image with superimposed text/graphics over the face — if a source is unusable, document the gap honestly
    and skip that person rather than upload a bad photo. Resolve each politician UUID by external_id, then
    upload to Supabase Storage bucket politician_photos as {uuid}-headshot.jpg. Record the public Storage
    URL for each for use in Task 2. (Optional, Claude's discretion: if the existing Weste/Ayala image is
    visibly low quality, re-source + re-process from the confirmed URLs — only if it improves quality.)
  </action>
  <verify>
    <automated>For McLean and Miranda: the processed local file is exactly 600×750 px (e.g. `python -c "from PIL import Image; print(Image.open('mclean.jpg').size)"` → (600, 750)) and the Storage object {uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - McLean + Miranda source images downloaded from the documented santaclarita.gov URLs (or a documented gap if a source is unusable)
    - Each processed image is exactly 600×750, 4:5, not stretched
    - No image has superimposed text/graphics over the face
    - Each uploaded to politician_photos/{uuid}-headshot.jpg and reachable (HTTP 200)
    - Public Storage URLs recorded for Task 2
  </acceptance_criteria>
  <done>McLean + Miranda portraits processed (or gaps documented) and live in Storage at the canonical path.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 896 (headshot rows) and audit full-roster image coverage</name>
  <files>C:/EV-Accounts/backend/migrations/896_santa_clarita_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/212_fremont_headshots.sql (INSERT politician_images + photo_origin_url UPDATE)
    - C:/EV-Accounts/backend/migrations/880_long_beach_headshots.sql (audit-only apply + roster image audit precedent)
    - .planning/phases/143-santa-clarita-deep-seed/143-RESEARCH.md (§Validation Architecture headshot smoke query, §Common Pitfalls 6)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/896_santa_clarita_headshots.sql. For McLean (-700181) and
    Miranda (-700182), resolved by external_id, INSERT a politician_images row (url = the Storage public
    URL from Task 1, type='default', photo_license='press_use') guarded by WHERE NOT EXISTS on
    politician_id, and UPDATE politicians.photo_origin_url to the santaclarita.gov source page where NULL.
    Apply via raw SQL (mcp__supabase-local execute_sql or psql -f) — do NOT run through the
    schema_migrations ledger (audit-only). Then run the full-roster coverage audit: LEFT JOIN the 5 current
    SC officials (665693/665689/665692/-700181/-700182) to politician_images and confirm 0 officials
    missing an image (except any documented gap), and confirm no official has >1 type='default' row
    (Gibbs 434cd9b0… must still be exactly 1 from Plan 01; Pitfall 6). Verify MAX(version) in
    schema_migrations is unchanged by this file.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (-700181,-700182) AND pi.type='default';" returns 2 (or 2 minus documented gaps)</automated>
  </verify>
  <acceptance_criteria>
    - McLean + Miranda each have a type='default' politician_images row pointing at their Storage URL (or a documented gap)
    - All 5 current SC officials have ≥1 image OR an explicitly documented gap (LEFT JOIN missing-image query returns only documented gaps)
    - No current SC official has >1 type='default' image row (Gibbs dedupe from Plan 01 still holds: 434cd9b0… = 1)
    - photo_origin_url populated for McLean + Miranda
    - schema_migrations MAX(version) unchanged by migration 896 (audit-only confirmed)
    - Migration is idempotent (re-apply = 0 rows changed)
  </acceptance_criteria>
  <done>896 applied; McLean/Miranda headshot rows present; full-roster image audit clean; ledger untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the new headshots</name>
  <action>Pause for the user to visually verify the new portraits per the how-to-verify steps below; do not proceed to phase completion until approved.</action>
  <what-built>New official headshots (Marsha McLean, Bill Miranda) processed to 600×750 and live in Supabase Storage + politician_images; full 5-person roster image coverage audited.</what-built>
  <how-to-verify>
    1. Open the McLean + Miranda Storage URLs (or the Santa Clarita browse view once it renders).
    2. Confirm each photo is the CORRECT person (cross-check against santaclarita.gov/city-council).
    3. Confirm no superimposed text/graphics over the face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5).
    4. Confirm the existing Weste/Ayala/Gibbs portraits look correct and Gibbs shows exactly one image (dedupe held).
  </how-to-verify>
  <resume-signal>Type "approved" or describe which photo(s) need re-cropping / re-sourcing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| santaclarita.gov CDN → local processing | External image source; could be wrong-person or text-overlaid. |
| operator → Supabase Storage + production DB | Uploads + image-row INSERTs against production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-13 | Spoofing | Wrong-person headshot uploaded for McLean/Miranda | mitigate | URLs verified against santaclarita.gov/city-council in RESEARCH; blocking human-verify checkpoint cross-checks each face before sign-off |
| T-143-14 | Tampering | Image stretched / aspect-distorted / text-overlaid | mitigate | Crop-to-4:5-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; human checkpoint |
| T-143-15 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged before/after |
| T-143-16 | Tampering | Duplicate image rows re-introduced (esp. Gibbs) | mitigate | INSERT guarded by WHERE NOT EXISTS on politician_id; post-audit confirms ≤1 type='default' per official; Gibbs 434cd9b0… stays at 1 |
| T-143-17 | Tampering | Headshot uploaded for retired Smyth | mitigate | Smyth (-700180) explicitly excluded — no headshot action for a departed official |
</threat_model>

<verification>
After 896 + checkpoint:
1. McLean + Miranda each have exactly one type='default' image (or documented gap)
2. full-roster LEFT JOIN missing-image query (5 current officials) returns only documented gaps
3. no current official has >1 type='default' image; Gibbs 434cd9b0… = 1
4. schema_migrations MAX(version) unchanged by 896
5. human checkpoint approved (correct person, correct crop, no overlays)
</verification>

<success_criteria>
The 2 new officials have correct, correctly-cropped 600×750 portraits live; the full 5-person current
roster has clean single-image coverage with any genuine gap documented; Smyth correctly has none; the
ledger counter is preserved.
</success_criteria>

<output>
Create `.planning/phases/143-santa-clarita-deep-seed/143-03-SUMMARY.md` when done.
</output>
