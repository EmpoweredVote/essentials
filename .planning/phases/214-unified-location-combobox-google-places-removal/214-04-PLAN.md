---
phase: 214-unified-location-combobox-google-places-removal
plan: 04
type: execute
wave: 3
depends_on: [214-02]
files_modified:
  - src/pages/Landing.jsx
autonomous: true
requirements: [SRCH-06]
user_setup: []

must_haves:
  truths:
    - "The Landing search bar is the SAME <LocationCombobox> component used on Results (one shared implementation, not a parallel one)"
    - "Landing's coverage list still renders and its click-to-browse routing is unchanged (D-04)"
    - "Landing's separate 'search candidates by name' feature is untouched (not conflated with location search)"
    - "Landing no longer imports useGooglePlacesAutocomplete or LocalityMatches"
  artifacts:
    - path: "src/pages/Landing.jsx"
      provides: "Combobox-driven Landing search bar sharing the Results component"
      contains: "LocationCombobox"
  key_links:
    - from: "src/pages/Landing.jsx"
      to: "LocationCombobox"
      via: "search-bar render"
      pattern: "<LocationCombobox"
    - from: "src/pages/Landing.jsx"
      to: "browseAreaRoute"
      via: "onSelectCandidate dispatch"
      pattern: "browseAreaRoute"
---

<objective>
Adopt the shared `<LocationCombobox>` as Landing.jsx's search bar (SRCH-06) — the same component instance powering Results, not a fork — while preserving Landing's coverage list as the browse entry point (D-04) and leaving its separate candidate-by-name search feature untouched. Removes Landing's Google Places wiring and `LocalityMatches` usage (import/usage only; the standalone files are deleted in Plan 05).

Purpose: Satisfies the milestone convention that Landing rides along inside this phase (it shares the exact Google-bound modules being retired) and SRCH-06's one-shared-component requirement.
Output: Edited `src/pages/Landing.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-CONTEXT.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-RESEARCH.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-PATTERNS.md
@.planning/phases/214-unified-location-combobox-google-places-removal/214-UI-SPEC.md

<interfaces>
LocationCombobox props (from Plan 02): value, onChange, onSubmitAddress(raw), onSubmitCoordinate(lat,lng,raw), onSelectCandidate(candidate), placeholder, ariaLabel, errorRow.
browseAreaRoute(candidate) -> "/results?browse_geo_id=...&browse_mtfcc=...&browse_label=...&from_locality=1" (Plan 02).
On Landing, an address-classified submit navigates directly to /results?q=<raw> (skip the old resolveLocalityRoute Google detour — RESEARCH Dispatch Wiring). A coordinate-classified submit navigates to Results carrying the coordinate (reuse the same coordinate entry Results Plan 03 exposes, e.g. /results with the coordinate handed off) OR calls lookupCoordinate then routes — match whatever hand-off Plan 03 established; document the choice.
</interfaces>

<note>
Line numbers in PATTERNS.md/RESEARCH.md are pre-edit references and may have drifted. Re-grep each target symbol in Landing.jsx before editing.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Landing search bar with shared LocationCombobox (SRCH-06)</name>
  <read_first>
    - src/pages/Landing.jsx — re-grep for: `useGooglePlacesAutocomplete`, `LocalityMatches`, `handleSearch`, `resolveLocalityRoute`, `handleAreaClick`, `nameQuery` (PATTERNS.md lines 275-310 enumerate keep vs. remove)
    - 214-RESEARCH.md "Landing integration" (lines 556-564) — coverage list + name-search stay; only the address input + LocalityMatches + Google hook block is replaced
    - 214-CONTEXT.md D-04 (Landing keeps coverage list as browse entry point), SRCH-06
    - 214-UI-SPEC.md (same combobox visual contract applies on Landing)
  </read_first>
  <action>
    Remove Landing's `useGooglePlacesAutocomplete` import + call and its `LocalityMatches` import + usage. Replace the address `<input>` + `<LocalityMatches>` JSX block with a `<LocationCombobox>` using the same props contract as Results: controlled `value`/`onChange` local state; `onSubmitAddress(raw)` → `navigate('/results?q=' + encodeURIComponent(raw))` (skip the old `resolveLocalityRoute(q)` Google detour entirely per RESEARCH Dispatch Wiring — an address goes straight to the Results address path); `onSelectCandidate(candidate)` → `navigate(browseAreaRoute(candidate))`; `onSubmitCoordinate(lat,lng,raw)` → hand off to Results using the same coordinate entry mechanism Plan 03 established (re-read 214-03-SUMMARY.md if present; otherwise route to `/results` carrying the coordinate and let Results' onSubmitCoordinate path run). Update `handleSearch` accordingly or replace its call sites; if `localitySearch.js` still exports `resolveLocalityRoute`, its Google-classification step is already gone (Plan 02) — but per D-02 an address should not detour through it at all. DO NOT touch: `handleAreaClick` + the coverage list rendering (D-04 browse entry point), and the separate `nameQuery`/candidate-by-name search feature (RESEARCH explicitly warns not to conflate it with location search).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useGooglePlacesAutocomplete\|<LocalityMatches\|import LocalityMatches" src/pages/Landing.jsx` returns ZERO.
    - `grep -n "<LocationCombobox" src/pages/Landing.jsx` returns exactly one usage (same component import path as Results — confirm both import `../components/LocationCombobox`).
    - `grep -n "handleAreaClick\|COVERAGE_STATES\|nameQuery" src/pages/Landing.jsx` still present (coverage list + name-search feature preserved — D-04, non-conflation).
    - `npm run build` succeeds; `npm test` stays green.
  </acceptance_criteria>
  <done>Landing's search bar is the shared LocationCombobox; Google hook + LocalityMatches usage removed; coverage list and candidate-by-name search untouched; address/place/coordinate submits route correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate/query → navigate() | Landing builds internal Results routes from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-05 | Spoofing (open redirect) | Landing onSelectCandidate / onSubmitAddress | mitigate | All navigation targets are internal `/results?...` built via browseAreaRoute + URLSearchParams/encodeURIComponent from geo_id/mtfcc/label/raw text; never navigate to a candidate-supplied full URL |
| T-214-07 | Tampering (scope creep into unrelated feature) | Landing nameQuery search | accept | The candidate-by-name search is explicitly out of scope and left untouched; conflating it would risk regressing a live feature — guarded by the "still present" acceptance grep |
</threat_model>

<verification>
- `npm run build` succeeds; `npm test` green.
- Manual smoke (Plan 06 checkpoint): Landing search bar resolves all three input types; coverage list still browses; name-search still works.
</verification>

<success_criteria>
Landing uses the identical shared LocationCombobox component, its coverage list and candidate-by-name search remain intact, and no Google Places or LocalityMatches wiring remains in Landing.jsx.
</success_criteria>

<output>
Create `.planning/phases/214-unified-location-combobox-google-places-removal/214-04-SUMMARY.md` when done, noting how the coordinate hand-off to Results was wired.
</output>
