---
phase: 146-palmdale-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["146-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/920_palmdale_headshots.sql
autonomous: false
requirements: [PLMD-01]

must_haves:
  truths:
    - "D-11: the one image GAP is Laura Bettencourt (-700657, created in Plan 02) — the exact gap set is determined by a pre-flight image-coverage query over the 5 current members, NOT hardcoded; the other 4 (Bishop/Loa/Ohlsen/Alarcón) already have a type='default' image and are left as-is unless a quick check shows one visibly broken/low-res"
    - "D-11: Bettencourt's portrait is sourced from cityofpalmdaleca.gov ImageRepository documentID=13184 (RESEARCH confirms HTTP 200, no WAF, clean professional portrait, no superimposed text); fallbacks Ballotpedia / laurabettencourt.com / LegiStorm only if the city URL breaks at apply time"
    - "D-11: the new headshot is the correct person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 FIRST (never stretched) then resized 600×750 Lanczos q90 JPEG, uploaded to Supabase Storage politician_photos (canonical path), politician_images type='default' photo_license='press_use', photo_origin_url set"
    - "D-11: no fabricated photos — if no acceptable Bettencourt portrait is obtainable, document a genuine gap honestly rather than upload a bad/overlaid/campaign image as type='default'"
    - "D-06: migration 920 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 919; on-disk counter authoritative)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/920_palmdale_headshots.sql"
      provides: "Audit-only headshot INSERT (Bettencourt; optional Bishop upgrade) + photo_origin_url backfill + full-roster image-coverage audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos (Bettencourt's canonical object)"
      to: "essentials.politician_images.url (type='default', press_use)"
      via: "INSERT politician_images scoped by politician_id, guarded NOT EXISTS"
      pattern: "'default'"
---

<objective>
Give the current 5-member Palmdale roster clean official headshots and confirm single canonical-image
coverage. After Plan 02 the survivor chamber 000d672d holds Bishop (D1) + Loa (D2) + Bettencourt (D3) +
Ohlsen (D4, Mayor) + Alarcón (D5), all bidirectionally linked. Per CONTEXT/RESEARCH the ONLY genuine image
gap is Laura Bettencourt (newly created in Plan 02) — the other 4 already have a type='default' image. The
EXACT gap set is resolved by a pre-flight image-coverage query, not assumed.

⚠️ cityofpalmdaleca.gov is FULLY ACCESSIBLE — HTTP 200, NO WAF (CivicEngage CMS; Pitfall 8). This is the
cleanest headshot environment of the LA-County Wave-2 cities. Do NOT copy the Lancaster/Glendale
WAF-403/browser assumption — Bettencourt's portrait can be downloaded directly with a standard curl +
Chrome UA. Source: cityofpalmdaleca.gov/ImageRepository/Document?documentID=13184 (RESEARCH confirmed HTTP
200, 216×288 PNG, clean professional portrait with flag background, no superimposed text). Fallbacks only if
that URL breaks at apply time: Ballotpedia (ballotpedia.org/Laura_Bettencourt), her campaign site
(laurabettencourt.com), or LegiStorm.

Processing (NON-NEGOTIABLE per project memory): download portrait → crop to 4:5 FIRST (never stretch —
crop only, eyes ~1/3 from top, full head+shoulders) → resize 600×750 Lanczos q90 JPEG (Python Pillow) →
upload to Supabase Storage politician_photos bucket at the canonical path (match the existing convention
the other 4 use — confirm at apply time; recent files use politician_photos/{uuid}/default.jpeg, the
project memory describes {uuid}-headshot.jpg; use whatever the live bucket/other-4 rows use) using the
service-role key in C:/EV-Accounts/backend/.env → INSERT politician_images (type='default',
photo_license='press_use'); set photo_origin_url to the source page. Reject any source with superimposed
text/graphics over the face — document a gap honestly rather than upload a bad photo. Resolve Bettencourt's
UUID by external_id -700657 at apply time.

Optional quality step (Claude's discretion, D-11): RESEARCH notes Bishop's existing image is a
scraped_no_license legacy/old-path entry (a newer official photo exists at documentID=15391) and Ohlsen has
a duplicate old-path row. You MAY upgrade Bishop's image to the official press_use photo + canonical path
and/or clean up Ohlsen's duplicate old-path row IF a quick check shows value — but the PRIMARY deliverable
is Bettencourt. Do NOT needlessly re-source the 4 existing good images.

Purpose: A voter sees the right face next to each official. Wrong-person, text-overlaid, or stretched
photos destroy trust. Output: Migration 920 (audit-only — NOT registered in schema_migrations) +
Bettencourt's portrait sourced + a human checkpoint confirming the correct person + correct crop; any
genuine gap documented honestly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/146-palmdale-deep-seed/146-CONTEXT.md
@.planning/phases/146-palmdale-deep-seed/146-RESEARCH.md
@.planning/phases/146-palmdale-deep-seed/146-VALIDATION.md
@.planning/phases/146-palmdale-deep-seed/146-02-SUMMARY.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §3/§4). cityofpalmdaleca.gov is NOT WAF-blocked.
     Only Bettencourt lacks an image — pre-flight resolves the exact gap. -->

Current 5 members (resolve UUID by external_id; pre-flight a coverage query to find who lacks an image):
  -201331 Bishop · 692504 Loa · -700657 Bettencourt (NEW, the gap) · 692516 Ohlsen · 692518 Alarcón

Bettencourt (-700657) — PRIMARY GAP, automated download (NO WAF):
  https://www.cityofpalmdaleca.gov/ImageRepository/Document?documentID=13184
  HTTP 200, 216×288 PNG, clean portrait (flag background), no superimposed text — crop 4:5 → 600×750.
  Fallbacks if it breaks: ballotpedia.org/Laura_Bettencourt · laurabettencourt.com · LegiStorm.

Existing 4 (RESEARCH §4 — leave as-is unless visibly broken; optional discretion upgrades):
  Bishop (-201331): scraped_no_license, OLD path (la_county/cities/palmdale/austin-bishop.png), 216×288.
    Optional: re-source documentID=15391 (HTTP 200), crop/resize, canonical path, upgrade license press_use.
  Loa (692504): scraped_no_license, canonical path, already 600×750. Optional: upgrade license to press_use.
  Ohlsen (692516): has 2 rows (old-path scraped + canonical press_use). Optional: delete the old-path row.
  Alarcón (692518): press_use, canonical, 216×288. Leave as-is (acceptable).

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face

Storage + DB:
  - Bucket: politician_photos · Path: the canonical convention the other 4 rows use (confirm live;
    recent files use {uuid}/default.jpeg) · service-role key in C:/EV-Accounts/backend/.env
  - politician_images: type='default', photo_license='press_use'
  - UPDATE politicians.photo_origin_url to the source page where NULL
  - Each INSERT guarded WHERE NOT EXISTS on politician_id → exactly ONE type='default' row per member

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql or psql -f). It does NOT
register in schema_migrations (same convention as Lancaster 912 / Glendale 904). On-disk counter
authoritative; ledger stays at 919 (Plan 02).

Pattern migration: C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql (audit-only INSERT +
photo_origin_url backfill + Storage URL shape). Also 904_glendale_headshots.sql.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight image-coverage check; download Bettencourt (documentID=13184, no WAF); crop 4:5 → 600×750 → upload</name>
  <files>C:/EV-Accounts/backend/migrations/920_palmdale_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql (audit-only INSERT + download/crop/upload pipeline + Storage URL shape)
    - .planning/phases/145-lancaster-deep-seed/145-03-PLAN.md (the exact pre-flight-coverage + crop/resize + checkpoint shape this plan mirrors — but note Palmdale is NOT WAF-blocked)
    - .planning/phases/146-palmdale-deep-seed/146-RESEARCH.md (§3 Bettencourt headshot source documentID=13184 HTTP 200, §4 existing-headshot audit, §Common Pitfalls 8 — NO WAF)
    - .planning/phases/146-palmdale-deep-seed/146-CONTEXT.md (§decisions D-11; only Bettencourt is the gap)
    - .planning/phases/146-palmdale-deep-seed/146-VALIDATION.md (Wave 3 SQL assertions)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots — 4:5 crop FIRST → 600×750 Lanczos q90)
  </read_first>
  <action>
    First run a pre-flight image-coverage query: for the 5 current members (-201331/692504/-700657/692516/
    692518), LEFT JOIN essentials.politician_images (type='default') and list who already has a type='default'
    image vs who is missing one — confirm Bettencourt (-700657) is the gap (CONTEXT/RESEARCH say she is the
    only one; do not re-source members who already have a good image). Download Bettencourt's official
    portrait from cityofpalmdaleca.gov/ImageRepository/Document?documentID=13184 with a standard curl +
    Chrome UA (the site is NOT WAF-blocked — Pitfall 8; do NOT default to a browser checkpoint for the
    download). If that URL 404s/changes at apply time, fall back to Ballotpedia / laurabettencourt.com /
    LegiStorm. Verify it is the correct Laura Bettencourt and has NO superimposed text/graphics; then crop
    to 4:5 (crop only — never stretch; eyes ~1/3 from top, full head+shoulders), then resize to 600×750
    Lanczos q90 JPEG (Pillow). Resolve Bettencourt's UUID by external_id -700657 and upload to the
    politician_photos bucket at the canonical path the other 4 rows use (confirm the live convention first —
    recent files use {uuid}/default.jpeg). Record the public Storage URL + the source page for Task 2.
    OPTIONAL (discretion, D-11): if a quick check shows Bishop's old-path scraped image is poor, re-source
    documentID=15391 and/or clean up Ohlsen's duplicate old-path row — but Bettencourt is the priority; do
    NOT needlessly touch the 4 existing good images. If no acceptable Bettencourt portrait is obtainable,
    document the genuine gap (do NOT upload a campaign/overlaid image as type='default').
  </action>
  <verify>
    <automated>The processed Bettencourt file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('bettencourt.jpg').size)"` → (600, 750)) and the uploaded Storage object returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight image-coverage query run; the missing-image set determined from live data confirms Bettencourt (-700657) is the gap; the 4 existing imaged members are not needlessly re-sourced
    - Bettencourt portrait downloaded from cityofpalmdaleca.gov documentID=13184 via curl (NO browser checkpoint needed for the download — not WAF-blocked); fallback used only if the URL breaks
    - Portrait is the correct Laura Bettencourt, no superimposed text/graphics, processed to exactly 600×750, 4:5, not stretched, eyes ~1/3 from top, uploaded to politician_photos at the canonical path (HTTP 200)
    - Public Storage URL + source page recorded for Task 2
    - Any optional Bishop upgrade / Ohlsen old-path cleanup done only if it clearly improves quality; the 4 existing good images otherwise untouched
    - If no acceptable portrait obtainable, an honest gap documented (NOT a campaign/overlaid image faked as type='default')
  </acceptance_criteria>
  <done>Bettencourt's portrait sourced from the (non-WAF) city ImageRepository, verified correct + clean, processed to 600×750 4:5, and uploaded to Storage at the canonical path; Storage URL + source recorded for Task 2; optional existing-image upgrades only where clearly worthwhile.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 920 (Bettencourt headshot row + optional upgrades) and audit full-roster image coverage</name>
  <files>C:/EV-Accounts/backend/migrations/920_palmdale_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/912_lancaster_headshots.sql (INSERT politician_images + photo_origin_url backfill + audit-only apply, ledger-unchanged check)
    - .planning/phases/145-lancaster-deep-seed/145-03-PLAN.md (full-roster coverage audit query + audit-only ledger check + documented-gap handling)
    - .planning/phases/146-palmdale-deep-seed/146-RESEARCH.md (§Validation Architecture Wave 3 checks, §4 existing rows)
    - .planning/phases/146-palmdale-deep-seed/146-VALIDATION.md (Wave 3 SQL assertions)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/920_palmdale_headshots.sql. INSERT a politician_images row for
    Bettencourt (resolved by external_id -700657): url = the Storage public URL from Task 1, type='default',
    photo_license='press_use', guarded WHERE NOT EXISTS on her politician_id; and UPDATE
    essentials.politicians SET photo_origin_url = the source page (documentID=13184 URL or the fallback used)
    WHERE external_id=-700657 AND photo_origin_url IS NULL. If you performed any optional upgrades in Task 1
    (Bishop documentID=15391 to canonical path + license press_use, or Ohlsen old-path duplicate cleanup),
    include the corresponding guarded INSERT/UPDATE/DELETE here (license upgrades guarded so a re-run is a
    no-op; delete the Ohlsen old-path row by its exact image id/url only). Do NOT touch the other members'
    good images. Apply via raw SQL (mcp__supabase-local execute_sql or psql -f) — do NOT run through the
    schema_migrations ledger (audit-only). Then run the full-roster coverage audit: LEFT JOIN the 5 current
    Palmdale officials (-201331/692504/-700657/692516/692518) to politician_images (type='default') and
    confirm all 5 now have exactly one type='default' row (no current official has 0 or >1). Verify
    MAX(version) in schema_migrations is UNCHANGED by this file (stays 919).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT p.external_id, COUNT(pi.*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id AND pi.type='default' WHERE p.external_id IN (-201331,692504,-700657,692516,692518) GROUP BY p.external_id;" returns 5 rows each with COUNT = 1 (all 5 have exactly one default image)</automated>
  </verify>
  <acceptance_criteria>
    - Bettencourt (-700657) has exactly one type='default' press_use politician_images row pointing at her Storage URL; photo_origin_url populated
    - All 5 current members have exactly one type='default' image (no current official has 0 or >1)
    - Any optional Ohlsen old-path duplicate removed by exact id/url only; any Bishop upgrade left exactly one canonical press_use row
    - The 4 pre-existing good images otherwise untouched
    - schema_migrations MAX(version) unchanged by migration 920 (audit-only confirmed; stays 919)
    - Migration is idempotent (re-apply = 0 rows changed)
    - If Bettencourt was documented as a genuine gap (no obtainable portrait), her row is honestly absent (not faked) and the gap is recorded
  </acceptance_criteria>
  <done>920 applied; Bettencourt has a single press_use headshot row (or a documented honest gap); full-roster image audit shows exactly one default image per current member; any optional cleanup done by exact id; ledger untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify Bettencourt's new headshot (correct person, clean crop)</name>
  <action>Pause for the user to visually verify Bettencourt's new portrait (and any optional Bishop/Ohlsen change) per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>Laura Bettencourt's official headshot, sourced from cityofpalmdaleca.gov documentID=13184 (non-WAF, HTTP 200), processed to 600×750 4:5 and live in Supabase Storage + politician_images (type='default', press_use); the other 4 current members already had images; any optional Bishop upgrade / Ohlsen old-path cleanup applied.</what-built>
  <how-to-verify>
    1. Confirm the new Bettencourt photo is the CORRECT person (cross-check against cityofpalmdaleca.gov/305/Councilmember-Laura-Bettencourt / Ballotpedia).
    2. Confirm no superimposed text/graphics over the face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    3. Confirm each of the 5 current members shows exactly one canonical image (no duplicate type='default' rows) on the Palmdale browse view.
    4. If any optional change was made (Bishop's official photo, Ohlsen old-path cleanup), confirm it is correct and did not break the existing good image.
    5. Confirm the on-disk counter is authoritative: schema_migrations MAX(version) is unchanged (still 919; the headshot file did not register).
  </how-to-verify>
  <resume-signal>Type "approved" or describe which photo(s) need re-sourcing / re-cropping.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with a limited external image source. No untrusted user input reaches the DB — uploads +
image-row writes use service-role/operator against production. Threat surface = wrong-person or
distorted/overlaid photo for Bettencourt, a fabricated/campaign photo filling a real gap, re-sourcing over
the 4 good existing images, or accidental ledger registration — mitigated by the pre-flight coverage check,
the crop-then-resize/no-stretch rule, the official city source, a blocking human-verify checkpoint, the
honest-gap rule, and the audit-only raw-SQL apply.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cityofpalmdaleca.gov (NO WAF) → curl → local processing | External official image source; HTTP 200, downloadable directly; could be wrong-person or text-overlaid — human-verified. |
| Ballotpedia / campaign site / LegiStorm → local processing | Fallback image sources only if the city URL breaks. |
| operator → Supabase Storage + production DB | Uploads + image-row INSERT against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-17 | Spoofing | Wrong-person headshot uploaded for Bettencourt | mitigate | Portrait sourced from the official cityofpalmdaleca.gov bio page (documentID=13184); blocking human-verify checkpoint cross-checks the face |
| T-146-18 | Tampering | Image stretched / aspect-distorted / text-overlaid | mitigate | Crop-to-4:5-FIRST-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; human checkpoint |
| T-146-19 | Spoofing | Fabricated / campaign photo uploaded to fill a gap if the city URL breaks | mitigate | Honest-gap rule — if no acceptable official portrait, leave imageless and document; never seed a campaign/overlaid photo as type='default' |
| T-146-20 | Tampering | Re-sourcing over one of the 4 existing good images, or duplicate image row | mitigate | Pre-flight coverage query works only the missing set (Bettencourt); each INSERT guarded WHERE NOT EXISTS on politician_id; post-audit confirms exactly 1 type='default' per official; optional cleanup targets exact id only |
| T-146-21 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged (stays 919) before/after |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No package installs (existing Pillow + Supabase client; RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
After 920 + checkpoint:
1. Bettencourt (-700657) has exactly one type='default' press_use image; photo_origin_url set
2. full-roster coverage: all 5 current officials have exactly one type='default' image
3. no current official has 0 or >1 type='default' image
4. any optional Bishop/Ohlsen change is correct and left exactly one canonical row
5. schema_migrations MAX(version) unchanged by 920 (stays 919)
6. human checkpoint approved (correct person, correct crop, no overlays)
</verification>

<success_criteria>
Every current Palmdale member has a single correct 600×750 image; Bettencourt's portrait is sourced from
the official (non-WAF) city ImageRepository, verified correct and clean, and stored as a press_use
type='default' row; the 4 pre-existing good images are preserved (or cleanly upgraded); no
wrong-person/overlaid/stretched photos; the ledger counter is preserved.
</success_criteria>

<output>
Create `.planning/phases/146-palmdale-deep-seed/146-03-SUMMARY.md` when done.
</output>
</content>
