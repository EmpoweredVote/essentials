---
phase: 180-city-of-forest-grove-deep-seed
plan: 05
type: execute
wave: 4
depends_on: ["180-03", "180-04"]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/buildingImages.js
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WASH-06]

must_haves:
  truths:
    - "A legally-licensed 1700×540 community banner for Forest Grove is uploaded to Supabase Storage cities/forest-grove.jpg (hyphenated Storage path per the pipeline naming convention)"
    - "buildingImages.js CURATED_LOCAL has a 'forest grove' key (WITH A SPACE, never 'forest-grove') pointing at the banner CDN URL, with an attribution comment (verified license)"
    - "The 'forest grove' key format is required because getBuildingImages() does city.includes(key) against representing_city.toLowerCase() ('forest grove' with a space) — a hyphenated key would never match and the banner would silently fall back to the tier gradient (the pre-existing 'los angeles'/'west covina' keys are the proof pattern)"
    - "coverage.js Oregon block has a 'Forest Grove' entry with browseGovernmentList ['4126200'], browseStateAbbrev 'OR', hasContext:true, alphabetically between Fairview and Gresham"
    - "npm run build passes with both edits"
    - "The live browse link (browse_geo_id=4126200) surfaces Mayor Wenzl first + all 6 councilors with headshots + stances, the community banner renders (not the tier gradient), and no party label appears"
    - "Decision coverage — D-10: legally-licensed Wikimedia-first banner, no AI images, banner-asset-pipeline followed to cities/forest-grove.jpg; D-11: banner asset work lives in this final surfacing plan; D-12: coverage.js Oregon block Forest Grove entry with hasContext:true (geo_id 4126200); D-13: live browse link provided at completion (browse_geo_id=4126200&browse_mtfcc=G4110); D-15: Old College Hall / Pacific University subject priority hint honored, Downtown FG alternate"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "Forest Grove purple hasContext chip in the Oregon COVERAGE_STATES block"
      contains: "Forest Grove"
    - path: "C:/Transparent Motivations/essentials/src/lib/buildingImages.js"
      provides: "'forest grove' CURATED_LOCAL banner entry"
      contains: "forest grove"
  key_links:
    - from: "coverage.js Oregon areas"
      to: "browse route (browse_geo_id=4126200)"
      via: "browseGovernmentList entry"
      pattern: "label: 'Forest Grove'"
    - from: "buildingImages.js CURATED_LOCAL 'forest grove'"
      to: "offices.representing_city='Forest Grove' (set inline in mig 1178)"
      via: "getBuildingImages() lowercased substring match"
      pattern: "'forest grove':"
---

<objective>
Close the phase: (1) source, process, and upload a legally-licensed 1700×540 community banner for
Forest Grove; (2) wire it via the `'forest grove'` CURATED_LOCAL key (WITH a space) in
buildingImages.js; (3) surface Forest Grove in the Oregon block of coverage.js with the purple
hasContext chip, using geo_id 4126200. The banner renders because mig 1178 set
`offices.representing_city='Forest Grove'` inline (no backfill needed).

Purpose: Satisfy WASH-06's purple hasContext chip criterion + the community banner standing
constraint, and verify the full roster end-to-end on the live app.
Output: banner in Storage (cities/forest-grove.jpg) + two essentials-repo edits + passing build +
live-browse confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-03-SUMMARY.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-04-SUMMARY.md
@src/lib/coverage.js
@src/lib/buildingImages.js
@docs/banner-asset-pipeline.md

**This is the essentials repo** — coverage.js and buildingImages.js are committed HERE (`git`), NOT
in C:/EV-Accounts. The purple hasContext chip and the banner-render path are already built; only the
data row + banner asset + CURATED_LOCAL key are new. The banner upload uses the existing committed
scripts scripts/banners/process_banner.py + upload_banner.py — no new script file.

**TAILWIND HYGIENE:** do NOT introduce raw Windows `\` paths into any committed file (Tailwind v4
scans .md/.js and a `\f...` sequence crashes the prod build). Use forward-slash CDN URLs only.

**TWO-WORD KEY TRAP (RESEARCH Pitfall 4, live-confirmed at Wave-0):** the CURATED_LOCAL key MUST be
the literal `'forest grove'` (with a space) — getBuildingImages() does `city.includes(key)` against
`offices.representing_city.toLowerCase()` = `'forest grove'`. A hyphenated `'forest-grove'` key would
NEVER match and the banner would silently fall back to the tier gradient. The Supabase Storage PATH,
however, stays hyphenated (`cities/forest-grove.jpg`) per the pipeline naming convention — key format
and path format are independent; do not conflate them.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Orchestrator sources + processes + uploads the community banner</name>
  <decision>Which legally-licensed wide photo becomes the Forest Grove community banner</decision>
  <context>
    Priority hint (D-15): Pacific University campus / Old College Hall (Forest Grove's signature
    landmark, oldest university building west of the Mississippi), with historic downtown/Main Street
    or wine-country horizon as alternates. RESEARCH identified strong candidates with directly-confirmed
    licenses — the D-15 top hint: commons.wikimedia.org "File:Old College Hall Pacific University
    front.JPG" (M.O. Stevens / Aboutmovies — the same photographer credited on Tigard's and Tualatin's
    banners; 2254×1445, CC BY 3.0 confirmed via the Wikimedia imageinfo API). Two other angles exist
    (back.JPG 2028×1465, side.JPG 2204×1448, both CC BY 3.0). The D-15 alternate: "File:Downtown Forest
    Grove, Oregon.JPG" (1748×1086, Aboutmovies, 2009, Public Domain / PD-self — a street-level wide shot
    looking west along Pacific Avenue at Main Street). BOTH candidate families have a ~1.5–1.6:1 native
    aspect ratio and need a substantial (~50%) height crop to hit the pipeline's 3.15:1 target — the
    compositional call was flagged UNTESTED in RESEARCH and VIEWED directly at Wave-0 (see
    180-01-SUMMARY.md for the recorded crop assessment). Prefer Old College Hall front if it crops
    without losing the building's identity; if it loses too much, fall back to the Downtown Forest Grove
    Public-Domain street shot (may crop more forgivingly). A clean license + crops-well-to-3.15:1 beats
    subject preference. Wikimedia Commons FIRST, Unsplash fallback. NO AI-generated images; no baked-in
    text/graphics. Follow docs/banner-asset-pipeline.md Stages 1–8. Run:
    `python scripts/banners/process_banner.py --input <raw> --output <processed_1700x540.jpg>` then
    `python scripts/banners/upload_banner.py --file <processed_1700x540.jpg> --path cities/forest-grove.jpg`.
    Record the final Title | Author | License (with the verified license) for the attribution comment
    used in Task 2.
  </context>
  <options>
    <option id="old-college-hall">
      <name>Wikimedia "Old College Hall Pacific University front.JPG" (D-15 top hint, preferred)</name>
      <pros>License directly confirmed (CC BY 3.0 via imageinfo API); matches D-15's top-priority hint exactly; 2254×1445, highest-res of the three angles; same photographer as the Tigard/Tualatin banners; NRHP-listed landmark</pros>
      <cons>~1.5:1 native aspect needs a ~50% height crop to 3.15:1 — risks cutting the cupola/base; view all 3 angles at 1700×540 before finalizing (crop assessment recorded at Wave-0)</cons>
    </option>
    <option id="downtown-forest-grove">
      <name>Wikimedia "Downtown Forest Grove, Oregon.JPG" (D-15 alternate hint)</name>
      <pros>Public Domain (no attribution legally required, though the convention credits it); street-level wide shot along Pacific Ave at Main St that may crop more forgivingly than a building portrait; same photographer</pros>
      <cons>1748×1086 (lower res); still ~1.6:1 native, needs a substantial height crop</cons>
    </option>
    <option id="unsplash">
      <name>Unsplash fallback photo</name>
      <pros>Higher chance of a wide, crop-friendly horizon shot</pros>
      <cons>Attribution/license differs from Wikimedia convention; confirm Unsplash license terms</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen source + final Title | Author | License string (verified license), confirming cities/forest-grove.jpg uploaded (CDN URL returns 200).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner in buildingImages.js and surface Forest Grove in coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/buildingImages.js, C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - src/lib/buildingImages.js lines 103–128 (CURATED_LOCAL block + attribution comments, incl. beaverton/hillsboro/tigard/tualatin entries AND the existing multi-word 'los angeles'/'long beach'/'west covina' keys that prove the space-key format) + lines 199–223 (the getBuildingImages() city.includes(key) match loop)
    - src/lib/coverage.js lines 96–109 (the Oregon block — Beaverton…Wood Village, incl. Fairview + Gresham)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md §"src/lib/coverage.js (config edit …)" and §"src/lib/buildingImages.js (config edit …)" (exact inserts + the deliberate key/path asymmetry)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md (confirmed 'forest grove' key format) + the Task 1 attribution string
  </read_first>
  <action>
    Edit 1 — buildingImages.js: add a `'forest grove'` key (WITH A SPACE, quoted — never 'forest-grove')
    to CURATED_LOCAL pointing at
    `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/forest-grove.jpg`
    (note the Storage PATH is hyphenated even though the object KEY has a space — they are independent),
    placed alongside the existing beaverton/hillsboro/tigard/tualatin entries. Add an attribution
    comment line above CURATED_LOCAL in the existing format `//   forest grove - <Title> | <Author> |
    <License>` using the string recorded in Task 1 (default per RESEARCH: `Old College Hall, Pacific
    University (front) | M.O. Stevens (Aboutmovies) | CC BY 3.0`, with the verified license). The
    space key is required because getBuildingImages() lowercases offices.representing_city and does a
    city.includes(key) substring match; mig 1178 set representing_city='Forest Grove' inline so
    'forest grove' resolves at first browse (a hyphenated key would silently fall to the tier gradient).
    Edit 2 — coverage.js: insert `{ label: 'Forest Grove', browseGovernmentList: ['4126200'],
    browseStateAbbrev: 'OR', hasContext: true }` into the Oregon `areas` array, alphabetically BETWEEN
    'Fairview' and 'Gresham' ("Fa" < "Fo" < "Gr"). Match the existing column alignment of surrounding
    rows. Use geo_id '4126200'. Change nothing else in either file — no other state blocks, no other
    CURATED_LOCAL keys. No raw Windows `\` paths anywhere (forward-slash CDN URLs only).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has `'forest grove':` (quoted, WITH a space) → the cities/forest-grove.jpg CDN URL
    - The key is NOT 'forest-grove' (hyphenated) anywhere; the Storage path IS cities/forest-grove.jpg (hyphenated)
    - An attribution comment `//   forest grove - ... | ... | ...` is present above CURATED_LOCAL with the verified license
    - coverage.js Oregon areas has 'Forest Grove' between 'Fairview' and 'Gresham'
    - Entry is exactly { label: 'Forest Grove', browseGovernmentList: ['4126200'], browseStateAbbrev: 'OR', hasContext: true }
    - No other lines changed in either file; no raw `\` path introduced
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Banner wired via the 'forest grove' space-key + Forest Grove surfaced with geo_id 4126200; production build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-browse end-to-end verification + commit</name>
  <what-built>The Forest Grove banner (Storage) + buildingImages.js 'forest grove' key + coverage.js purple chip.</what-built>
  <how-to-verify>
    1. Confirm `npm run build` passed (Task 2 gate).
    2. Browse essentials.empowered.vote/results?browse_geo_id=4126200&browse_mtfcc=G4110 and confirm:
       - Mayor Malynda Wenzl sorts FIRST, all 6 councilors follow (Marshall/Martinez/Valenzuela/
         Gustafson/Falconer/Schimmel); no empty LOCAL section; no section-split.
       - Headshots render 600×750 with no overlays (per the actual N/7 sourced in plan 03; documented
         gaps show the default avatar, not a wrong person); compass stances visible; NO party label
         (antipartisan).
       - The Local section shows the Forest Grove community banner photo (NOT the tier gradient
         fallback) — this confirms representing_city='Forest Grove' (mig 1178) + the 'forest grove'
         space CURATED_LOCAL key resolve. If it shows the gradient, the key is wrong (check for a
         hyphenated key).
       - The purple hasContext chip appears for Forest Grove.
    3. Optionally enter a real Forest Grove address and confirm the same roster resolves via /representatives/me.
    4. Commit both files in the essentials repo: `git add src/lib/coverage.js src/lib/buildingImages.js && git commit` (on a branch if the harness is on the default branch; end the message with the Co-Authored-By trailer).
  </how-to-verify>
  <acceptance_criteria>
    - Live browse shows Mayor Wenzl first + 6 councilors, no empty LOCAL section, no section-split
    - Headshots 600×750 no overlays (per actual N/7); stances visible; no party label; no wrong-person image
    - Community banner renders (not gradient fallback) — confirms the 'forest grove' space key resolved
    - Purple hasContext chip shows for Forest Grove
    - coverage.js + buildingImages.js committed in the essentials repo
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the live browse + banner, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Banner sourced from Wikimedia Commons / Unsplash |
| operator → Supabase Storage | Processed banner uploaded to public bucket cities/forest-grove.jpg |
| build pipeline → prod | coverage.js + buildingImages.js edits shipped via the essentials build/deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-22 | Spoofing/IP | Unlicensed or AI-generated banner image | mitigate | Wikimedia-first with recorded Title/Author/License (Old College Hall CC BY 3.0 / Downtown FG Public Domain confirmed via imageinfo API); NO AI-generated images (pipeline forbids); decision checkpoint records the license string |
| T-180-23 | Tampering | Hyphenated 'forest-grove' key never matches → banner silently falls back to the tier gradient | mitigate | Key MUST be 'forest grove' (space), live-confirmed at Wave-0 against getBuildingImages() city.includes(key); acceptance criteria assert the space key; live-browse gate confirms the banner renders (not gradient) |
| T-180-24 | Tampering | Malformed coverage.js/buildingImages.js breaks the prod build | mitigate | `npm run build` gate before commit; additive edits only |
| T-180-25 | Denial of Service | Windows `\` path in committed file crashes Tailwind scan | mitigate | Edits add only forward-slash CDN URLs + data rows; no raw backslash introduced |
| T-180-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; banner scripts (process_banner.py/upload_banner.py) are pre-existing committed tooling |
</threat_model>

<verification>
- `npm run build` exits 0; banner uploaded + rendered (not gradient); live browse surfaces
  Mayor-first roster with chip, stances, headshots, banner, no section-split, no party label; both
  files committed.
</verification>

<success_criteria>
- Forest Grove visible with the purple hasContext chip + community banner in the Oregon block (geo_id
  4126200, 'forest grove' space key); build green; live browse confirms the full roster + banner
  end-to-end.
</success_criteria>

<output>
Create `.planning/phases/180-city-of-forest-grove-deep-seed/180-05-SUMMARY.md` when done, recording
the banner source/license and the live-browse confirmation. This is the final plan — note WASH-06 is
satisfied end-to-end.
</output>
