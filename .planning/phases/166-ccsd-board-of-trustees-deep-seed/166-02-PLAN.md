---
phase: 166-ccsd-board-of-trustees-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-ccsd-trustees-headshots.py
  - C:/EV-Accounts/backend/migrations/1108_ccsd_trustees_headshots.sql
  - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-02-SUMMARY.md
autonomous: false
requirements: [CCSD-01]
user_setup: []

must_haves:
  truths:
    - "Each CCSD trustee with a clean, correct-person source has a 600×750 (4:5) headshot mirrored to Supabase Storage politician_photos/{uuid}-headshot.jpg; genuine gaps are documented honestly (no fabrication, no placeholder) — appointed trustees (newer AB175 seats) + Linda Cavazos are the highest gap risk [D-05]"
    - "ccsd.net/trustees and BoardDocs (go.boarddocs.com/nv/ccsdlv) BOTH return WAF-403 — this is the Henderson/NLV situation; the pipeline uses a per-trustee fallback chain (ccsd.net Chrome-UA+Referer test → background-image grep → Ballotpedia / Wikimedia descriptive-UA / official campaign / city portals) resolved in Wave-0, NOT a single clean source [D-05, RESEARCH Pitfall 3]"
    - "Headshots are crop-to-4:5 FIRST then resize to 600×750 Lanczos q90 — never stretch; RGBA→white-composite (no black halo); no text/graphic overlays over the face; each visually spot-checked for correct-person before insert [D-05]"
    - "The headshot script (_tmp-ccsd-trustees-headshots.py) is reconstructed from the NLV/Boulder City pipeline pattern (the _tmp-*.py scripts are gitignored/absent on disk) — env-load, resolve_politician_id by external_id (psycopg2, never hardcode UUID), crop_to_4_5, resize_600x750, upload_to_storage (x-upsert), Wikimedia descriptive-UA branch; guard len(OFFICIALS)==11, unique ext_ids in -3209011..-3209001 [PATTERNS Pattern 3]"
    - "Migration 1108 inserts politician_images rows (id, politician_id, url, type='default', photo_license) resolved by external_id for SUCCESS members only — AUDIT-ONLY, NOT registered, structural ledger stays at 1107 (or Wave-0 number); politician_images columns are exactly (id, politician_id, url, type, photo_license) with NO image-origin column; photo_license set per member from the actual source [D-05]"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-ccsd-trustees-headshots.py"
      provides: "Headshot pipeline: per-trustee resolved source URL → download (WAF fallback chain) → crop 4:5 FIRST → resize 600×750 Lanczos q90 → upload to Storage politician_photos/{uuid}-headshot.jpg; UUID resolved at runtime from external_id via psycopg2; emits a manifest (SUCCESS ext_id→uuid→cdn_url→license, FAILED for any gap)"
      contains: "600"
    - path: "C:/EV-Accounts/backend/migrations/1108_ccsd_trustees_headshots.sql"
      provides: "Audit-only headshot migration: one INSERT block per SUCCESS trustee (external_id -3209001..-3209011), type='default', photo_license from manifest, idempotent NOT EXISTS, NOT registered"
      contains: "essentials.politician_images"
  key_links:
    - from: "11 trustees (external_id -3209001..-3209011)"
      to: "essentials.politician_images (url → Storage CDN politician_photos/{uuid}-headshot.jpg)"
      via: "politician_id resolved by external_id; type='default'"
      pattern: "politician_images"
    - from: "_tmp-ccsd-trustees-headshots.py manifest (cdn_url + license per trustee)"
      to: "migration 1108 INSERT blocks"
      via: "manifest SUCCESS line → migration url + photo_license values; FAILED line → no row + documented gap"
      pattern: "headshot.jpg"
---

<objective>
Source, process, and install 600×750 headshots for the 11 CCSD trustees. Unlike Boulder City (clean flybouldercity.com), CCSD's primary sources are WAF-blocked: ccsd.net/trustees AND BoardDocs (go.boarddocs.com/nv/ccsdlv) both return HTTP 403 (Akamai-style WAF) — this is the Henderson/North Las Vegas situation. Wave-0 (Plan 01) probed the WAF and built a per-trustee fallback chain; this plan reconstructs the headshot pipeline from the NLV/Boulder City pattern (the `_tmp-*.py` scripts are gitignored and absent on disk — rebuild from the 1094 migration manifest + the 164/165 SUMMARYs + Pattern 3), runs it against the resolved per-trustee sources (ccsd.net Chrome-UA+Referer if it bypasses; else Ballotpedia / Wikimedia descriptive-UA / official campaign / city portals), crops to 4:5 FIRST then resizes to 600×750 Lanczos q90, rejects any image with text/graphic overlays over the face, visually spot-checks each for correct-person, mirrors to Storage politician_photos/{uuid}-headshot.jpg, and writes the audit-only migration 1108. Document genuine gaps honestly — appointed trustees (newer AB175 seats) and Linda Cavazos are the highest gap risk. No fabrication, no placeholder rows.

Purpose: Satisfies CCSD-01 SC#2 (trustees render with 600×750 headshots; genuine gaps documented).
Output: _tmp-ccsd-trustees-headshots.py + the headshot manifest, 1108_ccsd_trustees_headshots.sql (audit-only), recorded results + the correct-person spot-check + documented gaps in 166-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and NO DB access):**
- The executor WRITES _tmp-ccsd-trustees-headshots.py and 1108_ccsd_trustees_headshots.sql to disk. The executor does NOT run the script, run DB SELECTs, or apply migrations.
- Running the headshot script (`python scripts/_tmp-ccsd-trustees-headshots.py` from C:/EV-Accounts so it reads backend/.env for DATABASE_URL + SUPABASE_SERVICE_ROLE_KEY), the per-trustee source resolution / WAF-bypass testing, the visual correct-person spot-check, applying mig 1108, and every verification SELECT/curl are INLINE ORCHESTRATOR / operator steps.
- The per-trustee source URLs are NOT pre-confirmed (both primary sources WAF-403) — the inline orchestrator resolves each trustee's working source at execution from the Wave-0 fallback chain (Ballotpedia / Wikimedia descriptive-UA / campaign / city portals), and any trustee with no clean correct-person source is a documented gap (FAILED manifest line, no 1108 row).
- The script + migration live in the SEPARATE repo C:/EV-Accounts/backend/ (commit via `git -C "C:/EV-Accounts"`; the _tmp-* script is gitignored). SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/166-ccsd-board-of-trustees-deep-seed/166-RESEARCH.md
@.planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor needs. Do NOT re-derive. -->
The 11 external_id→UUID map is in 166-01-SUMMARY.md (minted by mig 1107). UUID resolved at runtime from external_id via psycopg2 — never hardcode a UUID in the script.
Bucket: politician_photos (NOT politician-headshots). Storage path: {politician_uuid}-headshot.jpg. CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
politician_images columns: (id, politician_id, url, type, photo_license) — NO image-origin column (removed). type='default' on all headshot rows.
Pipeline invariants: crop to 4:5 ratio FIRST, THEN resize 600×750 Lanczos q90, optimize=True strips EXIF; RGBA/transparent → white-composite (no black halo); MIN_DIM=100. Never stretch/resize before cropping (memory feedback_headshot_resize_no_distort).
Source situation: ccsd.net/trustees AND go.boarddocs.com/nv/ccsdlv BOTH return HTTP 403 (Akamai WAF — confirmed in research). This is the Henderson/NLV situation, NOT the clean Boulder City situation. Per-trustee fallback chain (resolved inline at execution):
  1. ccsd.net trustee portrait JPEGs IF a Chrome-UA + Referer:https://ccsd.net/trustees/ combination bypasses the WAF (test first; NLV/Henderson Akamai stayed blocked, so expect to fall through).
  2. Background-image grep on the curl-fetched trustee HTML if portraits are CSS background images.
  3. Ballotpedia candidate-photo / Wikimedia Commons (descriptive UA 'EmpoweredVote-HeadshotBot/1.0 (https://empowered.vote; contact chris@empowered.vote)') / official campaign sites for elected trustees.
  4. City portals for appointed trustees (Isaac Barron is also a North Las Vegas figure; Esparza-Stoffregen via Henderson; Johnson via Las Vegas; Satory via Clark County).
Roster + external_ids (sources resolved inline — NOT pre-confirmed):
  -3209001 Emily Stevens (District A, President) — elected
  -3209002 Lydia Dominguez (District B, Clerk) — elected
  -3209003 Tameka Henry (District C) — elected
  -3209004 Brenda Zamora (District D) — elected
  -3209005 Lorena Biassotti (District E) — elected
  -3209006 Irene Bustamante Adams (District F, Vice President) — elected (highest-profile / former Assembly — best photo odds)
  -3209007 Linda P. Cavazos (District G) — elected (gap risk)
  -3209008 Isaac Barron (Appointed – City of North Las Vegas) — appointed
  -3209009 Ramona Esparza-Stoffregen (Appointed – City of Henderson) — appointed (gap risk)
  -3209010 Adam Johnson (Appointed – City of Las Vegas) — appointed (gap risk)
  -3209011 Lisa Satory (Appointed – Clark County) — appointed (gap risk)
photo_license guidance: set per member from the ACTUAL source found inline — Wikimedia cc_by_sa_* / public_domain; campaign or city portal press_use; us_government_work for official-government portraits. Reject any image with campaign banners / "Re-Elect" text / graphic overlays over the face (memory feedback_headshot_no_graphics).
Gap handling: if no clean correct-person source exists for a trustee (expected for some appointed members), emit a FAILED manifest line + document the gap in SUMMARY — do NOT fabricate or insert a placeholder image. Some gaps are acceptable and honest for this phase (appointed trustees are new AB175 seats).
Migration 1108 is AUDIT-ONLY: NOT registered; structural ledger stays at 1107 (or Wave-0 number). If Wave-0 renumbered the structural migration, shift 1108 by the same offset.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the headshot pipeline script + resolve sources inline + run it + correct-person spot-check</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-ccsd-trustees-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1094_north_las_vegas_city_council_headshots.sql (the WAF-fallback headshot-migration manifest — shows the source/license mix the NLV pipeline produced; the closest on-disk evidence of the gitignored script's output shape)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md (§_tmp-ccsd-trustees-headshots.py — the pipeline body to reconstruct: env-load, OFFICIALS shape, guard range, crop/resize/upload functions, WAF fallback chain, manifest; and the No-Analog note that the .py is gitignored)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-RESEARCH.md (§Pattern 3 headshot script; §Common Pitfalls 3 WAF; §State of the Art appointed-trustee thin-photo note; Assumptions A3 photo_license)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-01-SUMMARY.md (the 11 external_id→UUID map minted by mig 1107 + the confirmed structural migration number)
    - .planning/phases/165-boulder-city-deep-seed/165-02-SUMMARY.md (a prior NV headshot pipeline run + manifest shape for reference)
  </read_first>
  <action>
EXECUTOR STEP: write `_tmp-ccsd-trustees-headshots.py` by reconstructing the NLV/Boulder City pipeline (the analog .py is gitignored/absent — rebuild from the 1094 manifest + 164/165 SUMMARYs + Pattern 3). Functions to reproduce: resolve_politician_id (psycopg2, by external_id — never hardcode UUID), download_image (with the WAF fallback chain + the Wikimedia descriptive-UA branch), crop_to_4_5, resize_600x750 (Lanczos q90), process_headshot_bytes (RGBA→white-composite, MIN_DIM=100, optimize=True), upload_to_storage (PUT with x-upsert, path politician_photos/{uuid}-headshot.jpg), process_member, main; plus the env-load + DB-connect block (reads C:/EV-Accounts/backend/.env: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL; BUCKET='politician_photos'; CDN_BASE as in interfaces; TARGET_SIZE=(600,750); JPEG_QUALITY=90; RESAMPLE=LANCZOS; BROWSER_HEADERS Chrome UA; WIKIMEDIA_HEADERS descriptive UA 'EmpoweredVote-HeadshotBot/1.0 (https://empowered.vote; contact chris@empowered.vote)'; a ccsd.net branch that adds Referer:https://ccsd.net/trustees/). Build the OFFICIALS list of 11 (ext_id, name, source_url, photo_license) — the source_url per trustee is filled in by the inline orchestrator at execution after WAF testing (leave clearly-marked placeholders the orchestrator replaces, or have main() accept a resolved source map). Guard assertions: len(OFFICIALS)==11, unique ext_ids, all ext_ids in -3209011..-3209001. Docstring: city=Clark County School District, Phase 166 (CCSD-01); note ccsd.net + BoardDocs are WAF-403 (per-trustee fallback chain required); the appointed four + Cavazos are gap risks. Manifest label "=== CCSD BOARD OF TRUSTEES HEADSHOT MANIFEST ===" and "{successes}/11 uploaded, {gaps} gaps"; each SUCCESS line carrying ext_id → uuid → cdn_url → license (consumed by migration 1108); each FAILED line = a documented gap. Keep crop-4:5-FIRST then resize-600×750.

INLINE ORCHESTRATOR STEP: FIRST resolve each trustee's working source — test ccsd.net with Chrome-UA + Referer; if blocked (expected), fall through to Ballotpedia / Wikimedia (descriptive UA) / official campaign / city portals; record the per-trustee resolved source + license. Fill the OFFICIALS source map. Then run `python scripts/_tmp-ccsd-trustees-headshots.py` from C:/EV-Accounts. Confirm it crops 4:5 → 600×750, uploads to Storage, and prints the manifest. Then VISUALLY SPOT-CHECK each uploaded headshot for correct-person (right individual — esp. disambiguate common names, head + shoulders, no text/graphic overlay over the face — reject + document if any image is wrong-person or overlaid). Record the manifest (SUCCESS + FAILED lines) + the per-trustee source/license + the spot-check outcome in the SUMMARY. Document every gap honestly (no fabrication) — appointed-trustee gaps are acceptable for this phase.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-ccsd-trustees-headshots.py"; test -f "$F" &amp;&amp; grep -q "600" "$F" &amp;&amp; grep -q "750" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; grep -q "external_id" "$F" &amp;&amp; grep -q -- "-3209001" "$F" &amp;&amp; grep -q -- "-3209011" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-ccsd-trustees-headshots.py exists; pipeline functions reconstructed from the NLV/Boulder City pattern; crop-4:5-FIRST then resize-600×750 preserved; RGBA→white-composite + MIN_DIM guard present.
    - OFFICIALS roster has 11 members (-3209001..-3209011) with per-trustee source_url (resolved inline) + photo_license; guard assertions present (count 11, unique, in range).
    - WAF fallback chain implemented: ccsd.net Chrome-UA+Referer branch + Wikimedia descriptive-UA branch + general Chrome-UA download.
    - UUID resolved at runtime from external_id (psycopg2) — no hardcoded UUID.
    - INLINE: per-trustee sources resolved + recorded; script run; manifest recorded in SUMMARY (SUCCESS ext_id→uuid→cdn_url→license; FAILED lines); correct-person spot-check recorded.
    - Any image with a text/graphic overlay over the face is rejected; genuine gaps documented (FAILED line, no row), never fabricated; appointed-trustee gaps acceptable.
  </acceptance_criteria>
  <done>Headshot script written + run with inline-resolved per-trustee sources; correct-person portraits cropped 4:5 → 600×750 → Storage; manifest + per-trustee source/license + correct-person spot-check + documented gaps recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Write audit-only migration 1108 + apply inline + verify</name>
  <files>C:/EV-Accounts/backend/migrations/1108_ccsd_trustees_headshots.sql, .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-02-SUMMARY.md</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1094_north_las_vegas_city_council_headshots.sql (EXACT ANALOG — one INSERT block per member, type='default', NOT EXISTS idempotency, audit-only/unregistered, the WAF-source manifest header + footer)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-PATTERNS.md (§1108_ccsd_trustees_headshots.sql — the per-trustee INSERT block + CDN URL format + the forbidden-token paraphrase rules + the FAILED-member-omitted note)
    - .planning/phases/166-ccsd-board-of-trustees-deep-seed/166-02-... (this plan's interfaces + the Task 1 manifest in SUMMARY)
  </read_first>
  <action>
EXECUTOR STEP: write `1108_ccsd_trustees_headshots.sql` modeled on 1094. Header comment: file name, Phase 166 (CCSD-01), AUDIT-ONLY (NOT registered, structural ledger stays at 1107 / Wave-0 number), columns exactly (id, politician_id, url, type, photo_license), idempotent via NOT EXISTS, type='default', per-trustee source + license manifest, note ccsd.net/BoardDocs skipped (WAF-403). Paraphrase the removed image-origin column note ("the removed image-origin column is intentionally absent") — do NOT write the literal removed-column token. BEGIN; then ONE INSERT block per SUCCESS trustee (external_id -3209001..-3209011, only the members with a SUCCESS manifest line): `INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) SELECT gen_random_uuid(), (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>), '<cdn_url_from_manifest>', 'default', '<license_from_manifest>' WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = <ext_id>))`. CDN URL format: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg. photo_license per member from the manifest (mixed — Wikimedia/public_domain/press_use/us_government_work). COMMIT. NO ledger-registration line (audit-only). For any FAILED trustee, OMIT the block and document the gap in SUMMARY (no placeholder row). GREP-GATE HYGIENE: keep slug / the removed image-origin column literal / the ledger table token out of comments.

INLINE ORCHESTRATOR STEP: apply 1108 via `psql -f` (DATABASE_URL from C:/EV-Accounts/backend/.env). Then verify + record in SUMMARY:
- `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -3209011 AND -3209001` → target = 11 minus documented gaps; list every gap with the reason.
- `curl -sI` each CDN url (the SUCCESS members) → 200.
- `SELECT version FROM supabase_migrations.schema_migrations WHERE version='1108'` → expect 0 rows (audit-only, ledger stays at 1107).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1108_ccsd_trustees_headshots.sql"; S=".planning/phases/166-ccsd-board-of-trustees-deep-seed/166-02-SUMMARY.md"; test -f "$F" &amp;&amp; grep -q "essentials.politician_images" "$F" &amp;&amp; grep -q "'default'" "$F" &amp;&amp; grep -q "NOT EXISTS" "$F" &amp;&amp; test -f "$S" &amp;&amp; grep -qi "headshot" "$S" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1108_ccsd_trustees_headshots.sql exists; one INSERT block per SUCCESS trustee; type='default'; NOT EXISTS idempotency; photo_license per member from manifest.
    - CDN URL format matches politician_photos/{uuid}-headshot.jpg.
    - No ledger-registration line (audit-only); no forbidden tokens in comments (image-origin column paraphrased, slug/ledger token absent).
    - FAILED trustees omitted (no placeholder row) and listed as documented gaps in SUMMARY.
    - INLINE: applied; politician_images count recorded (target 11 minus gaps); CDN urls return 200; every gap listed with reason.
    - Ledger check recorded: 1108 NOT registered (structural ledger stays at 1107).
  </acceptance_criteria>
  <done>Migration 1108 written + applied; headshot rows present for all SUCCESS trustees (CDN-200); genuine gaps documented (no fabrication); ledger stays at 1107; results + gaps recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
Server-side data-seed run by the operator; no new user-input paths/endpoints. The realistic threat surface is low. Real risks: wrong-person / overlaid headshots, fabricated photos for WAF-blocked trustees, and mis-set licenses.

## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external image source (Ballotpedia/Wikimedia/campaign/city portal) → headshot pipeline | fetched image crosses into Storage + DB; validate it is a real portrait, no overlay, correct person |
| manifest → migration 1108 | manifest cdn_url/license cross into seed SQL; only SUCCESS lines become INSERT blocks |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-07 | Tampering | wrong-person or overlay-laden headshot inserted (esp. common-name disambiguation) | mitigate | Per-trustee source resolved + recorded; reject text/graphic overlays; INLINE visual correct-person spot-check before insert; Plan 03 human-verify reconfirms |
| T-166-08 | Tampering | fabricated/placeholder image for a WAF-blocked trustee with no clean source | mitigate | No-fabrication rule; FAILED manifest line + documented gap instead of a placeholder row; appointed-trustee gaps acceptable + honest |
| T-166-09 | Spoofing | server returns an error/WAF body saved as an image | mitigate | download_image validates content-type / decodes via PIL before processing; sizes confirmed > MIN_DIM; 403 bodies rejected |
| T-166-10 | Information disclosure | mis-set photo_license (esp. Wikimedia CC attribution) | mitigate | photo_license set per member from the actual source found inline (Wikimedia cc_by_sa_*/public_domain; campaign/city press_use; gov us_government_work) |
| T-166-SC | Tampering | npm/pip/cargo installs | accept | No new packages (Pillow/psycopg2/requests pre-installed; RESEARCH audit = N/A). No high-severity threats; data-seed phase. |
</threat_model>

<verification>
CCSD-01 SC#2: trustees render with 600×750 headshots, genuine gaps documented.
- Pipeline crops 4:5 first then 600×750; mirrored to politician_photos/{uuid}-headshot.jpg; per-trustee source resolved (WAF fallback chain); correct-person spot-checked (Task 1).
- politician_images rows present for SUCCESS members (target 11 minus documented gaps); CDN urls 200 (Task 2).
- Migration 1108 audit-only — structural ledger stays at 1107.
</verification>

<success_criteria>
- _tmp-ccsd-trustees-headshots.py written + run with inline-resolved per-trustee sources (ccsd.net/BoardDocs WAF-403 → fallback chain); 600×750 4:5 crops mirrored to Storage; correct-person spot-check + manifest + per-trustee source/license recorded.
- 1108_ccsd_trustees_headshots.sql written + applied (audit-only); politician_images rows for all SUCCESS trustees; CDN-200; every gap documented (no fabrication).
- Ledger stays at 1107; results + gaps recorded in 166-02-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/166-ccsd-board-of-trustees-deep-seed/166-02-SUMMARY.md` when done (record the per-trustee resolved source + license + manifest, the politician_images count + CDN-200 results, the correct-person spot-check, every documented gap with reason, and confirmation the ledger stayed at 1107).
</output>
</content>
</invoke>
