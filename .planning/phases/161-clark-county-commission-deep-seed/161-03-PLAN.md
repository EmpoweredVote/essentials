---
phase: 161-clark-county-commission-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["161-01", "161-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1057_clark_county_commission_naft_stances.sql
  - C:/EV-Accounts/backend/migrations/1058_clark_county_commission_kirkpatrick_stances.sql
  - C:/EV-Accounts/backend/migrations/1059_clark_county_commission_becker_stances.sql
  - C:/EV-Accounts/backend/migrations/1060_clark_county_commission_mccurdy_stances.sql
  - C:/EV-Accounts/backend/migrations/1061_clark_county_commission_segerblom_stances.sql
  - C:/EV-Accounts/backend/migrations/1062_clark_county_commission_jones_stances.sql
  - C:/EV-Accounts/backend/migrations/1063_clark_county_commission_gibson_stances.sql
  - .planning/phases/161-clark-county-commission-deep-seed/161-03-SUMMARY.md
autonomous: false
requirements: [CLARK-01]
user_setup: []

must_haves:
  truths:
    - "Each commissioner with a public record has evidence-only compass stances across all live compass topics, researched one agent at a time, 100% cited, zero default values [D-06]"
    - "Topics with no county-level evidence are honest blank spokes (no value), never defaulted to Neutral"
    - "All 7 commissioners route from Strip/Paradise/unincorporated addresses to the Clark County board with headshots"
    - "Stances are chairs (discrete positions the evidence matches), not a directional polarity scale"
    - "Casing 'nv' lowercase; zero section-split; ledger MAX=1055 (all stance migrations audit-only, unregistered)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1057_clark_county_commission_naft_stances.sql"
      provides: "Audit-only evidence-only stance migration for Naft (inform.politician_answers + inform.politician_context), topic_id resolved live by topic_key, NOT registered"
      contains: "inform.politician_answers"
    - path: ".planning/phases/161-clark-county-commission-deep-seed/161-03-SUMMARY.md"
      provides: "Recorded results for all 9 SQL/HTTP verification checks + the address-routing/correct-person/stance human-verify outcome"
      contains: "CLARK-01"
  key_links:
    - from: "7 commissioner offices"
      to: "COUNTY district at geo_id='32003', state='nv'"
      via: "address → /representatives/me routing"
      pattern: "state = 'nv'"
    - from: "7 commissioners (external_id -3200301..-3200307)"
      to: "inform.politician_answers + inform.politician_context"
      via: "topic_id resolved by topic_key WHERE is_live=true; ON CONFLICT (politician_id, topic_id)"
      pattern: "compass_topics"
---

<objective>
Research and install evidence-only compass stances for all 7 Clark County commissioners — ONE research agent at a time (parallel agents burn rate-limit quota with no usable output — hard standing rule), all live compass topics, 100% cited, honest blank spokes where no public record exists, ZERO default values (chairs model, not polarity). Write one audit-only stance migration per commissioner (1057-1063), apply each inline, then prove the full phase goal end-to-end via the 9 SQL/HTTP verification checks and a human checkpoint on address routing + correct-person headshots + stance honesty.

Purpose: Closes CLARK-01 by satisfying Success Criterion 3 (evidence-only stances, 100% cited, honest blanks, zero defaults) and confirming all 4 phase success criteria are TRUE.
Output: 7 per-commissioner stance migrations (audit-only, unregistered) + 161-03-SUMMARY.md with recorded query results + the human-verify outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP):**
- Stance RESEARCH is run by research agents, ONE AT A TIME (memory feedback_stance_research_one_at_a_time — parallel agents burn quota with no output). The executor WRITES the 7 stance migration .sql files to disk from the researched evidence; the executor does NOT apply them or run DB SELECTs.
- Applying each stance migration via `mcp__supabase-local__execute_sql` (or psql -f), and every verification SQL/HTTP/browse check, are INLINE ORCHESTRATOR steps performed by execute-phase.
- Stance migrations live in the SEPARATE repo C:/EV-Accounts/backend/migrations/. The SUMMARY.md goes in the essentials .planning phase dir.
- Each task marks executor-write / research-agent / inline-orchestrator-apply.
</execution_context>

<context>
@.planning/phases/161-clark-county-commission-deep-seed/161-RESEARCH.md
@.planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md
@.planning/phases/161-clark-county-commission-deep-seed/161-VALIDATION.md

<interfaces>
<!-- DB-confirmed / research-verified facts the executor + research agents need. Do NOT re-derive. -->
Government 'Clark County, Nevada, US' geo_id='32003'; chamber 'Board of County Commissioners'
COUNTY district: geo_id='32003', district_type='COUNTY', state='nv' lowercase
7 commissioners external_id -3200301..-3200307 (full range -3200307..-3200301); the external_id→UUID map is in 161-01-SUMMARY.md
Stance tables (inform schema): inform.politician_answers (politician_id, topic_id, value); inform.politician_context (politician_id, topic_id, reasoning, sources); inform.compass_topics (topic_key, is_live)
topic_id MUST be resolved LIVE via JOIN inform.compass_topics ct ON ct.topic_key=s.topic_key AND ct.is_live=true — NEVER hardcode topic UUIDs; live/retired topic IDs in memory project_compass_live_topic_ids
ON CONFLICT (politician_id, topic_id) DO UPDATE for both answers and context (idempotent)
Stances are CHAIRS (value = the discrete position the evidence matches, 1-5), NOT a directional polarity scale (memory feedback_compass_chairs_not_polarity)
No value for a topic = honest blank (no row); NEVER default to Neutral/Likely (memory feedback_stance_no_default_value)
No judicial topics (county commissioners are not judges)
County-scope topics likely to have evidence: homelessness, homelessness-response, local-immigration, public-safety-approach, housing, rent-regulation, cannabis-regulation, economic-development, transportation-priorities, local-environment
Out-of-county-jurisdiction topics (likely honest blanks): social-security, trans-athletes, school-vouchers, abortion (no county jurisdiction) — only place if a commissioner has a specific documented county-relevant stance
Stance source tiers: clark.legistar.com (vote records/minutes) → thenevadaindependent.com → Las Vegas Review-Journal / KLAS 8NewsNow / Fox5Vegas → campaign sites/first-party statements
Realistic depth: 6-12 stances for well-documented seats (Naft, Jones attorney/ex-state-senator, Segerblom cannabis/progressive, Kirkpatrick ex-Assembly-Speaker); Becker (1st-term Republican, sworn 2025) + Gibson may yield fewer. Honest blanks expected.
Stance-anchor notes: Segerblom is THE canonical cannabis-regulation anchor (authored NV legalization); Clark County is a partial sanctuary county (Jones involved when policy set) → local-immigration evidence likely.
Migration mapping (all AUDIT-ONLY, NOT registered): 1057 Naft / 1058 Kirkpatrick / 1059 Becker / 1060 McCurdy / 1061 Segerblom / 1062 Jones / 1063 Gibson. Ledger stays MAX=1055.
Address routing spot-check: 3600 S Las Vegas Blvd, Las Vegas, NV 89109 (Bellagio, unincorporated Paradise) → Clark County board, NOT City of Las Vegas.
Browse link: essentials.empowered.vote/results?browse_government_list=32003&browse_label=Clark+County&browse_state=NV&browse_skip_overlap=1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research stances (one agent at a time) + write 7 audit-only stance migrations 1057-1063</name>
  <files>C:/EV-Accounts/backend/migrations/1057_clark_county_commission_naft_stances.sql, C:/EV-Accounts/backend/migrations/1058_clark_county_commission_kirkpatrick_stances.sql, C:/EV-Accounts/backend/migrations/1059_clark_county_commission_becker_stances.sql, C:/EV-Accounts/backend/migrations/1060_clark_county_commission_mccurdy_stances.sql, C:/EV-Accounts/backend/migrations/1061_clark_county_commission_segerblom_stances.sql, C:/EV-Accounts/backend/migrations/1062_clark_county_commission_jones_stances.sql, C:/EV-Accounts/backend/migrations/1063_clark_county_commission_gibson_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1037_norwalk_ramirez_stances.sql (EXACT ANALOG — the CTE shape: VALUES(topic_key,val,reasoning,sources) → JOIN compass_topics is_live → INSERT politician_answers ON CONFLICT → INSERT politician_context ON CONFLICT; audit-only, unregistered)
    - .planning/phases/161-clark-county-commission-deep-seed/161-PATTERNS.md (section "migrations/1057..._stances.sql" — header pattern, CTE shape, key constraints)
    - .planning/phases/161-clark-county-commission-deep-seed/161-RESEARCH.md ("Stances" — source tiers, county-scope topics, realistic depth, anchors)
    - .planning/phases/161-clark-county-commission-deep-seed/161-01-SUMMARY.md (the 7 external_id→UUID map minted by mig 1055)
  </read_first>
  <action>
RESEARCH (one agent at a time — HARD RULE, never parallel): for each commissioner in turn (Naft → Kirkpatrick → Becker → McCurdy → Segerblom → Jones → Gibson), dispatch a single stance-research agent that researches ALL live compass topics from public evidence (clark.legistar.com vote records → Nevada Independent → LV Review-Journal/KLAS/Fox5 → first-party/campaign statements). The agent returns, per topic with evidence: topic_key, the chair value (1-5 — the discrete position the evidence matches, NOT a polarity), an evidence-based reasoning string, and the source URL(s). Topics with no county-level evidence get NO entry (honest blank). ZERO defaults — never Neutral/Likely as a placeholder. No judicial topics. Wait for each agent to finish and record its output before dispatching the next (this avoids the rate-limit burn documented in memory).

EXECUTOR STEP (per commissioner): write the stance migration modeled verbatim on 1037_norwalk_ramirez_stances.sql. Header comment: file name, Phase 161 (CLARK-01), the commissioner's name + external_id, AUDIT-ONLY (NOT registered, ledger stays 1055), idempotent, CHAIRS model, 100% citation, no defaults, honest blanks, topic_id resolved live by topic_key, stance sources (clark.legistar.com + Nevada Independent). BEGIN; then a CTE: `WITH s(topic_key, val, reasoning, sources) AS (VALUES (<topic_key>, <1-5>, '<reasoning citing the specific vote/statement>', ARRAY['<url>',...]), ...)` covering ONLY the researched topics-with-evidence; `t AS (SELECT s.*, ct.id AS topic_id FROM s JOIN inform.compass_topics ct ON ct.topic_key=s.topic_key AND ct.is_live=true)`; `ins_ans AS (INSERT INTO inform.politician_answers (politician_id, topic_id, value) SELECT '<uuid>', topic_id, val FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value RETURNING 1)`; then `INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) SELECT '<uuid>', topic_id, reasoning, sources FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning=EXCLUDED.reasoning, sources=EXCLUDED.sources;` COMMIT;. Use the real per-commissioner UUID from 161-01-SUMMARY.md. NEVER hardcode topic UUIDs (always JOIN on topic_key + is_live). Do NOT register any of these in the ledger. If a commissioner genuinely has no sourceable county-level stance, write a migration with zero VALUES rows (or document the full blank) — no fabrication.

Do NOT apply these migrations — that is the inline orchestrator step (Task 2).
  </action>
  <verify>
    <automated>P="C:/EV-Accounts/backend/migrations"; ok=1; for f in 1057_clark_county_commission_naft_stances 1058_clark_county_commission_kirkpatrick_stances 1059_clark_county_commission_becker_stances 1060_clark_county_commission_mccurdy_stances 1061_clark_county_commission_segerblom_stances 1062_clark_county_commission_jones_stances 1063_clark_county_commission_gibson_stances; do test -f "$P/$f.sql" || ok=0; done; F="$P/1057_clark_county_commission_naft_stances.sql"; grep -q "inform.politician_answers" "$F" &amp;&amp; grep -q "inform.politician_context" "$F" &amp;&amp; grep -q "compass_topics" "$F" &amp;&amp; grep -q "is_live = true" "$F" &amp;&amp; grep -q "ON CONFLICT (politician_id, topic_id)" "$F" &amp;&amp; [ "$ok" -eq 1 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 7 stance migrations (1057-1063) exist on disk, one per commissioner in the correct name→file mapping.
    - Stances researched ONE agent at a time (no parallel dispatch); each agent covered all live compass topics.
    - Each migration uses the 1037 CTE shape: VALUES(topic_key, val, reasoning, sources) → JOIN inform.compass_topics ON topic_key AND is_live=true → INSERT politician_answers ON CONFLICT (politician_id, topic_id) DO UPDATE → INSERT politician_context ON CONFLICT (politician_id, topic_id) DO UPDATE.
    - topic_id is resolved LIVE by topic_key + is_live=true — no hardcoded topic UUIDs.
    - Every placed stance has a non-empty reasoning string and at least one source URL (100% cited); values are discrete chairs (1-5), not a polarity scale.
    - No defaulted values (no Neutral/Likely placeholder); topics without evidence are simply absent (honest blank).
    - No judicial topics; no out-of-county-jurisdiction topic placed without specific county-relevant evidence.
    - None of 1057-1063 contain a ledger-registration line (all audit-only).
    - Real per-commissioner UUIDs (from 161-01-SUMMARY.md) used as politician_id.
  </acceptance_criteria>
  <done>7 evidence-only stance migrations written to disk, researched one agent at a time, chairs model, 100% cited, honest blanks, zero defaults, topic_id resolved live, all audit-only. Not yet applied.</done>
</task>

<task type="auto">
  <name>Task 2: Apply stance migrations inline + run the 9 SQL/HTTP verification checks</name>
  <files>.planning/phases/161-clark-county-commission-deep-seed/161-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/161-clark-county-commission-deep-seed/161-VALIDATION.md (Per-Task Verification Map — all 9 checks + Section-Split SQL + address-routing spot check)
    - .planning/phases/161-clark-county-commission-deep-seed/161-RESEARCH.md (Validation Architecture → Test Map; the exact SQL per check)
    - .planning/phases/161-clark-county-commission-deep-seed/161-02-SUMMARY.md (documented headshot gaps, if any, to subtract from the 7 count)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): apply each stance migration 1057-1063 via `mcp__supabase-local__execute_sql` (or psql -f). After each apply, spot-count the answers for that commissioner. Then run the full 9-check verification suite from 161-VALIDATION.md via mcp__supabase-local__execute_sql (or curl for HTTP) and record every result in 161-03-SUMMARY.md:
(1) BCC office count — offices in chamber 'Board of County Commissioners' under government 'Clark County, Nevada, US' → expect 7. Record.
(2) County-district linkage — offices→districts WHERE d.geo_id='32003' AND d.district_type='COUNTY' AND d.state='nv' AND chamber name='Board of County Commissioners' → expect 7. Record.
(3) Headshots present — politician_images (type='default') for external_id BETWEEN -3200307 AND -3200301 → expect 7 minus documented gaps (from 161-02-SUMMARY.md). Record + list gaps.
(4) Headshots serve — curl -sI each CDN url (spot-check all 7) → 200. Record.
(5) Evidence-only stances — `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -3200307 AND -3200301)` → expect ≥1 per sourced commissioner; verify 100% have a paired politician_context row with non-null reasoning + sources (zero uncited), and zero defaulted/placeholder values. Record per-commissioner counts + any honest blanks.
(6) Section-split — run the exact COUNTY-tier section-split SQL from 161-VALIDATION.md → expect 0 rows. Record.
(7) Casing — DISTINCT state on districts linked to the BCC chamber → expect only 'nv' lowercase. Record.
(8) Ledger — `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('1055','1056','1057','1058','1059','1060','1061','1062','1063')` → expect only 1055 registered; MAX=1055 (all audit-only migrations unregistered). Record.
(9) No phantom 8th seat — same office count = exactly 7. Record.
Plus the coverage.js check: confirm src/lib/coverage.js COVERAGE_COUNTIES contains the Clark County entry (browseGovernmentList ['32003'], hasContext:true). Record.
Flag ANY deviation (office count below 7, a non-'nv' state, any uncited/defaulted stance, any extra ledger entry) as a gap for a follow-up gap-closure plan.
  </action>
  <verify>
    <automated>F=".planning/phases/161-clark-county-commission-deep-seed/161-03-SUMMARY.md"; test -f "$F" &amp;&amp; grep -qi "office" "$F" &amp;&amp; grep -qi "headshot" "$F" &amp;&amp; grep -qi "stance" "$F" &amp;&amp; grep -qi "section-split" "$F" &amp;&amp; grep -qi "ledger" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: all 7 stance migrations applied; per-commissioner answer counts recorded.
    - Check (1)+(9) recorded: BCC office count = exactly 7 (no phantom 8th seat).
    - Check (2) recorded: county-district linkage = 7 at state='nv'.
    - Check (3) recorded: politician_images count = 7 minus documented gaps; gaps listed.
    - Check (4) recorded: CDN headshots return HTTP 200 (all 7 spot-checked).
    - Check (5) recorded: ≥1 stance per sourced commissioner; 100% cited (paired context with reasoning + sources); zero defaulted values; honest blanks noted.
    - Check (6) recorded: section-split query returns 0 rows.
    - Check (7) recorded: linked districts show only state='nv' lowercase.
    - Check (8) recorded: only ('1055') registered; ledger MAX=1055 (1056-1063 audit-only, unregistered).
    - coverage.js COVERAGE_COUNTIES Clark County entry confirmed (hasContext:true, ['32003']).
    - Any deviation flagged for gap closure.
  </acceptance_criteria>
  <done>All 7 stance migrations applied; all 9 checks + coverage check recorded in 161-03-SUMMARY.md with expected results; any deviation flagged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify address routing + correct-person headshots + stance honesty</name>
  <action>Pause for human verification. Present the what-built summary and the how-to-verify steps below; wait for the resume signal before marking the phase complete. No code or DB changes in this task.</action>
  <what-built>Seeded the Clark County Board of County Commissioners (7 members) under a standalone Clark County government, with offices linked to the single COUNTY district (geo_id 32003), 600x750 headshots (minus any documented genuine gap), and evidence-only compass stances (chairs model, 100% cited, honest blanks, zero defaults). Clark County is surfaced with the purple hasContext chip in coverage.js. All 9 SQL/HTTP audits passed inline (7 offices, county-district linkage, 7 headshots CDN-200, stances cited, casing 'nv', 0 section-split, ledger MAX=1055).</what-built>
  <how-to-verify>
1. Address routing (Strip / Paradise) — open the app and enter an unincorporated Strip/Paradise address (e.g. 3600 S Las Vegas Blvd, Las Vegas, NV 89109 — Bellagio). Confirm the LOCAL section returns the Clark County board with all 7 commissioners and headshots, and NO empty LOCAL section and NO City-of-Las-Vegas officials (the Strip is unincorporated → county only).
2. Chair-first ordering — confirm Michael Naft (District A, Chair) sorts FIRST in the board list, and that Kirkpatrick is NOT shown as Chair. If Naft does not sort first, check groupHierarchy.js (the Multnomah Chair-first fix, commit a235f25) covers the COUNTY tier — extend if needed (RESEARCH Open Question 1 / A5).
3. Correct-person headshots — spot-check each commissioner profile. Confirm each photo is the right person, 4:5, no text/graphic overlay, not a placeholder. Note the 175x175 source upscale (acceptable per Pasadena precedent); confirm any documented gap renders cleanly without a photo.
4. Stance honesty — open 2-3 commissioner profiles (include Segerblom for cannabis-regulation, Jones for local-immigration). Confirm stances render with citations, that blank spokes are genuinely blank (no defaulted Neutral), and values look like the documented positions (chairs, not a forced polarity). Party must NOT display anywhere (antipartisan).
5. Browse review — optionally review essentials.empowered.vote/results?browse_government_list=32003&browse_label=Clark+County&browse_state=NV&browse_skip_overlap=1.
  </how-to-verify>
  <verify><human-check>Operator confirms Strip/Paradise addresses route to the Clark County board (no city, no empty LOCAL section), Naft sorts first as Chair, correct-person headshots (no overlay/placeholder), and evidence-only stances with honest blanks and no party display.</human-check></verify>
  <done>Human approves; or issues are logged and routed to a gap-closure plan.</done>
  <resume-signal>Type "approved" or describe any wrong-person photo, mis-routed/missing commissioner, Chair-ordering issue, defaulted stance, or party-display leak.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| research evidence → stance migration | researched chair values/citations cross into seed SQL; only evidence-backed values are written |
| migration file → live DB | service-role apply of seed SQL; only the inline orchestrator applies |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-08 | Tampering | fabricated/defaulted stance value (no evidence) | mitigate | Evidence-only chairs model; every stance has paired context with reasoning + source(s); check (5) asserts 0 uncited + 0 defaults; honest blanks where no record |
| T-161-09 | Spoofing | wrong topic via hardcoded topic UUID | mitigate | topic_id resolved live by topic_key + is_live=true (never hardcoded); retired topic IDs excluded |
| T-161-10 | Repudiation | unverified claim of completion | mitigate | All 9 checks recorded with actual query results in SUMMARY; human checkpoint confirms routing + correct-person photos + stance honesty |
| T-161-11 | Tampering | data-integrity drift (section-split / casing) | mitigate | Section-split SQL gate (0 rows) + casing check ('nv') run inline; ledger MAX=1055 asserted |
| T-161-SC | Tampering | npm/pip/cargo installs | accept | No installs (stance SQL + verification only); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
All four phase success criteria proven:
1. A Strip/Paradise address returns the correct Clark County commissioners, no empty LOCAL section (checks 1+2 + browse step 1).
2. All 7 commissioners render with 600x750 headshots, gaps documented (checks 3-4 + browse step 3).
3. Evidence-only stances render, 100% cited, honest blanks, zero defaults (check 5 — SC#3 + browse step 4).
4. Clark County surfaces with the purple hasContext chip (coverage.js check + browse step 5).
Cross-cutting: casing 'nv' (check 7), 0 section-split (check 6), ledger MAX=1055 (check 8), Chair=Naft sorts first (browse step 2).
</verification>

<success_criteria>
- 7 per-commissioner stance migrations (1057-1063) written, researched one agent at a time, applied; evidence-only, 100% cited, honest blanks, zero defaults, chairs model.
- 161-03-SUMMARY.md records all 9 SQL/HTTP checks + the coverage check with expected results.
- Human checkpoint approves Strip/Paradise routing, Chair-first ordering (Naft), correct-person headshots (no overlay), and stance honesty (no party display).
- All stance migrations audit-only (ledger MAX stays 1055); any deviation routed to a gap-closure plan.
</success_criteria>

<output>
Create `.planning/phases/161-clark-county-commission-deep-seed/161-03-SUMMARY.md` when done (record per-commissioner stance counts + honest blanks, the 9 verification results, and the human-verify outcome).
</output>
