---
phase: 106-va-compass-stances
plan: 05
type: execute
wave: 5
depends_on:
  - 106-04
files_modified:
  - C:/EV-Accounts/backend/migrations/330_kaine_stances.sql
autonomous: true
requirements:
  - VA-STANCES-02
must_haves:
  truths:
    - "Tim Kaine has >=15 stance rows in inform.politician_answers after migration 330 applies"
    - "Every Kaine context row has >=1 URL in sources (100% citation rate per D-10)"
    - "VA-STANCES-02 fully satisfied — both Warner and Kaine have >=15 stances each"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/330_kaine_stances.sql"
      provides: "Per-individual stance migration for US Senator Tim Kaine"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "330_kaine_stances.sql"
      to: "essentials.politicians (external_id=-400079)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research Tim Kaine's (US Senator from Virginia, external_id=-400079) public record across all currently-active compass topics, write migration 330, apply, and verify. This closes VA-STANCES-02.

Purpose: Completes federal senator coverage. Kaine has served in the US Senate since 2013, was VA Governor 2006-2010, and was 2016 VP nominee — extensive multi-tier record. Target >=15 stances per ROADMAP, aim 18-21+ per D-09.

Output: `330_kaine_stances.sql` applied; >=15 stance rows for Kaine; VA-STANCES-02 fully closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/106-va-compass-stances/106-CONTEXT.md
@.planning/phases/106-va-compass-stances/106-PATTERNS.md
@.planning/phases/106-va-compass-stances/106-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Kaine UUID, then research all active topics</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-01..D-10)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md
    - C:/EV-Accounts/backend/migrations/311_va_federal_officials.sql (lines 76-87 for Kaine external_id=-400079)
  </read_first>
  <action>
    Step A: Run via mcp__supabase-local__execute_sql:
      SELECT id FROM essentials.politicians WHERE external_id = -400079;
    Record as ${KAINE_UUID}.

    Step B: Run ONE web-research subagent (per D-08). Kaine's documented record covers:
      - Senate Foreign Relations Committee senior: ukraine-support (cosponsor of multiple Ukraine aid bills), tariffs, war-powers focus
      - HELP Committee: healthcare (ACA defender, public option support), childcare (cosponsor of Child Care for Working Families Act), education
      - Armed Services Committee: deportation-adjacent (immigration enforcement votes)
      - Strong gun-safety record (post-Sandy Hook), public-safety-approach
      - Abortion: pro-choice, cosponsor of Women's Health Protection Act despite personal Catholic views (well-documented)
      - Voting-rights, redistricting, campaign-finance (For The People Act cosponsor)
      - Climate-change (voted yes IRA), fossil-fuels (mixed VA coal-country moderation history)
      - Civil-rights, same-sex-marriage (voted for Respect for Marriage Act), trans-athletes (Senate vote available), religious-freedom
      - Social-security defender, medicare/aid (expansion supporter), taxes
      - Immigration (2013 Gang of Eight)
      - Misinformation, ai-regulation: less prominent than Warner but has cosponsored bills — research
      - Judicial-* topics: Senate confirmation vote record
      - Local-only topics (city-sanitation, rent-regulation, residential-zoning, jail-capacity, local-environment, local-immigration): likely no Senate record — but Kaine was Richmond Mayor 1998-2001, so SOME local-* topics may have evidence; research carefully

    Target: 18-21+ stances per D-09; ROADMAP minimum >=15.

    Use sources: ontheissues.org/Senate/Tim_Kaine.htm, ballotpedia.org, govtrack.us, kaine.senate.gov, congress.gov.

    Save research as `.planning/phases/106-va-compass-stances/_tmp-kaine-research.json`.
  </action>
  <verify>
    <automated>
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-kaine-research.json"
      ((Get-Content "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-kaine-research.json" | ConvertFrom-Json).Count -ge 15)
    </automated>
  </verify>
  <done>
    - Kaine UUID resolved
    - Research JSON file exists with >=15 entries, all cited
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 330_kaine_stances.sql matching 282 pattern exactly</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251)
    - C:/EV-Accounts/backend/migrations/329_warner_stances.sql (just-written federal analog)
    - .planning/phases/106-va-compass-stances/_tmp-kaine-research.json
  </read_first>
  <action>
    Create `C:/EV-Accounts/backend/migrations/330_kaine_stances.sql` following exact 329/326 structure with substitutions:
      - Header: "Migration 330: Tim Kaine Stances" / "US Senator from Virginia"
      - Section header: "Tim Kaine"
      - politician_id literal: ${KAINE_UUID}
      - One INSERT pair per topic in _tmp-kaine-research.json

    Same hard rules: single BEGIN/COMMIT, paired INSERTs (>=15 each), float values, dollar quoting, ARRAY cast, topic reference block.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/330_kaine_stances.sql"
      (Select-String -Path "C:/EV-Accounts/backend/migrations/330_kaine_stances.sql" -Pattern '^BEGIN;$' -SimpleMatch).Count -eq 1
      (Select-String -Path "C:/EV-Accounts/backend/migrations/330_kaine_stances.sql" -Pattern '^COMMIT;$' -SimpleMatch).Count -eq 1
      $a = (Select-String -Path "C:/EV-Accounts/backend/migrations/330_kaine_stances.sql" -Pattern 'INSERT INTO inform.politician_answers').Count
      $c = (Select-String -Path "C:/EV-Accounts/backend/migrations/330_kaine_stances.sql" -Pattern 'INSERT INTO inform.politician_context').Count
      ($a -eq $c) -and ($a -ge 15)
    </automated>
  </verify>
  <done>
    - Migration file exists with valid structure
    - Equal answers/context INSERTs (>=15 each)
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 330 + verify + close VA-STANCES-02</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/330_kaine_stances.sql
  </read_first>
  <action>
    Step A: Read migration 330 SQL, pass to mcp__supabase-local__execute_sql.

    Step B (per-politician verification):
      SELECT
        (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -400079)) AS answer_count,
        (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -400079) AND pc.politician_id IS NULL) AS unpaired,
        (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -400079) AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)) AS uncited;
    Required: answer_count >= 15, unpaired = 0, uncited = 0.

    Step C (VA-STANCES-02 closure check — both senators):
      SELECT external_id, COUNT(*) AS stance_count
      FROM inform.politician_answers pa
      JOIN essentials.politicians p ON p.id = pa.politician_id
      WHERE p.external_id IN (-400079, -400080)
      GROUP BY external_id
      ORDER BY external_id;
    Expected: 2 rows, both with stance_count >= 15.

    Step D: Delete `.planning/phases/106-va-compass-stances/_tmp-kaine-research.json`.
  </action>
  <verify>
    <automated>
      Closure SELECT in Step C must return exactly 2 rows, both with stance_count >= 15.
    </automated>
  </verify>
  <done>
    - Migration 330 applied
    - Kaine stance count >= 15
    - VA-STANCES-02 fully satisfied
  </done>
</task>

</tasks>

<verification>
- mcp__supabase-local__execute_sql confirms Kaine stance count >= 15
- Closure query confirms both senators meet minimum
- VA-STANCES-02 satisfied
</verification>

<success_criteria>
- VA-STANCES-02 fully satisfied
- ROADMAP Phase 106 success criterion #2 fully met
- Phase ready to advance to Alexandria council (Plan 06)
</success_criteria>

<output>
Create `.planning/phases/106-va-compass-stances/106-05-SUMMARY.md` documenting Kaine stance count, omitted topics, and VA-STANCES-02 closure status.
</output>
