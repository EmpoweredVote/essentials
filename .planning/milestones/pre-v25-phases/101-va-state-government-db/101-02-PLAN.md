---
phase: 101-va-state-government-db
plan: 02
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - C:/EV-Accounts/backend/migrations/301_va_state_executives.sql
autonomous: true
requirements:
  - VA-GOV-02
  - VA-GOV-05
tags:
  - virginia
  - state-government
  - executives
  - sql-migration

must_haves:
  truths:
    - "3 STATE_EXEC districts seeded for Virginia (state='VA' uppercase, geo_id='51', district_id='' empty string) — labels: Virginia Governor, Virginia Lieutenant Governor, Virginia Attorney General"
    - "3 politician rows seeded — Spanberger (-510001), Hashmi (-510002), Jones (-510003) — all party='Democrat', is_appointed=false, is_active=true, is_vacant=false, is_incumbent=true"
    - "3 office rows seeded, each linking the executive politician to the correct STATE_EXEC district and the matching chamber (Governor/Lt Gov/AG) under State of Virginia government"
    - "All 3 office rows have is_appointed_position=false (VA-GOV-05: all voter-elected, no legislature-elected officials)"
    - "All 3 politicians have office_id back-filled (NULL count = 0 after migration)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/301_va_state_executives.sql"
      provides: "VA executives migration — 3 STATE_EXEC districts + 3 politicians + 3 offices + office_id back-fill"
      contains: "INSERT INTO essentials.politicians"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts"
      via: "district_id (STATE_EXEC type, state='VA')"
      pattern: "d.district_type = 'STATE_EXEC' AND d.state = 'VA'"
    - from: "essentials.offices"
      to: "essentials.chambers"
      via: "chamber_id subquery (name + government_id under State of Virginia)"
      pattern: "WHERE name = 'Governor' AND government_id = .SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA'"
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id"
      via: "back-fill UPDATE scoped to external_id BETWEEN -510010 AND -510001"
      pattern: "p.external_id BETWEEN -510010 AND -510001 AND p.office_id IS NULL"
---

<objective>
Create and apply migration 301 to seed Virginia's 3 voter-elected state executives — Governor Abigail Spanberger, Lieutenant Governor Ghazala Hashmi, Attorney General Jay Jones — each with a STATE_EXEC district, a politician row, an office row linked to the appropriate chamber, and the office_id back-fill on politicians.

Purpose: Implements VA-GOV-02 (3 execs seeded with STATE_EXEC districts) and VA-GOV-05 (Virginia constitutional structure — all 3 are voter-elected, is_appointed_position=false; no legislature-elected officials like ME/MD Treasurer).

Output:
- SQL file: C:/EV-Accounts/backend/migrations/301_va_state_executives.sql
- DB state: 3 new STATE_EXEC district rows, 3 politician rows, 3 office rows, office_id back-filled on all 3 politicians
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-va-state-government-db/101-RESEARCH.md
@.planning/phases/101-va-state-government-db/101-PATTERNS.md
@C:/EV-Accounts/backend/migrations/270_md_state_executives.sql
@.planning/phases/101-va-state-government-db/101-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write migration 301_va_state_executives.sql</name>
  <files>C:/EV-Accounts/backend/migrations/301_va_state_executives.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/270_md_state_executives.sql (exact analog; copy structure, drop Comptroller + Treasurer blocks)
    - .planning/phases/101-va-state-government-db/101-PATTERNS.md (Pattern Assignments for `migrations/301_va_state_executives.sql`, lines 80-159; VA substitutions list)
    - .planning/phases/101-va-state-government-db/101-RESEARCH.md (Pattern 2 lines 178-218; Executives roster lines 308-316; Pitfall 2 on STATE_EXEC casing)
  </read_first>
  <behavior>
    - Migration inserts 3 STATE_EXEC district rows (state='VA' UPPERCASE, geo_id='51', district_id='' empty string, mtfcc='')
    - Migration inserts 3 politician rows (Spanberger -510001, Hashmi -510002, Jones -510003) with party='Democrat', is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true
    - Migration inserts 3 office rows, each with is_appointed_position=false, representing_state='VA', title in {Governor, Lieutenant Governor, Attorney General}, linked to the corresponding STATE_EXEC district + chamber
    - Office NOT EXISTS guard uses `(district_id, chamber_id)` — single-member pattern (matches OR/MD single-member precedent)
    - office_id back-fill UPDATE scoped to `external_id BETWEEN -510010 AND -510001 AND office_id IS NULL`
    - Re-running the migration on populated DB produces 0 new rows (ON CONFLICT external_id DO NOTHING + WHERE NOT EXISTS on offices + office_id IS NULL guard)
  </behavior>
  <action>
    Create file `C:/EV-Accounts/backend/migrations/301_va_state_executives.sql` by adapting migration 270 with these exact substitutions:

    1. `BEGIN;` / `COMMIT;` wrap. Header comment cites source analog (270_md_state_executives.sql), phase 101, requirements VA-GOV-02 + VA-GOV-05, plus four CRITICAL warnings: (a) state='VA' UPPERCASE for STATE_EXEC; (b) district_id='' empty string (not named); (c) geo_id='51'; (d) all 3 is_appointed=false / is_appointed_position=false.

    2. **STEP 1 — 3 STATE_EXEC district INSERTs**, each as:
       ```
       INSERT INTO essentials.districts (id, district_type, state, geo_id, label, district_id, mtfcc)
       SELECT gen_random_uuid(), 'STATE_EXEC', 'VA', '51', '<label>', '', ''
       WHERE NOT EXISTS (
         SELECT 1 FROM essentials.districts
         WHERE district_type = 'STATE_EXEC' AND state = 'VA' AND label = '<label>'
       );
       ```
       Labels (exact): 'Virginia Governor', 'Virginia Lieutenant Governor', 'Virginia Attorney General'. CRITICAL: state must be uppercase 'VA' (OR 223a lesson — lowercase silently breaks routing). district_id must be empty string '' (NOT 'Virginia (Statewide)' or any named string).

    3. **STEP 2 — 3 CTE blocks**, one per executive. Template per Pattern 2 in PATTERNS.md lines 95-128. The 3 executive tuples (full_name, first_name, last_name, party, external_id, district label, chamber name, title) are exactly:
       - ('Abigail Spanberger', 'Abigail', 'Spanberger', 'Democrat', -510001, 'Virginia Governor', 'Governor', 'Governor')
       - ('Ghazala Hashmi', 'Ghazala', 'Hashmi', 'Democrat', -510002, 'Virginia Lieutenant Governor', 'Lieutenant Governor', 'Lieutenant Governor')
       - ('Jay Jones', 'Jay', 'Jones', 'Democrat', -510003, 'Virginia Attorney General', 'Attorney General', 'Attorney General')

       Each CTE block must:
       - Use `WITH ins_p AS (INSERT INTO essentials.politicians (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id) VALUES (gen_random_uuid(), '<full>', '<first>', '<last>', 'Democrat', true, false, false, true, <ext_id>) ON CONFLICT (external_id) DO NOTHING RETURNING id)`
       - Follow with `INSERT INTO essentials.offices (id, district_id, chamber_id, politician_id, title, representing_state, is_appointed_position, is_vacant, role_canonical) SELECT gen_random_uuid(), d.id, (SELECT id FROM essentials.chambers WHERE name = '<chamber>' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA')), p.id, '<title>', 'VA', false, false, NULL FROM essentials.districts d CROSS JOIN ins_p p WHERE d.district_type = 'STATE_EXEC' AND d.state = 'VA' AND d.label = '<label>' AND p.id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM essentials.offices o WHERE o.district_id = d.id AND o.chamber_id = (SELECT id FROM essentials.chambers WHERE name = '<chamber>' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA')))`
       - The NOT EXISTS guard MUST use `(district_id, chamber_id)` — single-member single-position guard.

    4. **STEP 3 — office_id back-fill**: single UPDATE at end:
       ```
       UPDATE essentials.politicians p
       SET office_id = o.id
       FROM essentials.offices o
       WHERE o.politician_id = p.id
         AND p.external_id BETWEEN -510010 AND -510001
         AND p.office_id IS NULL;
       ```
       Note the range -510010..-510001 (not -510003..-510001) — matches MD 270 pattern of widening the range slightly for future-proofing.

    5. DO NOT include Comptroller, State Treasurer, or any 4th/5th executive. DO NOT include any office with is_appointed_position=true. DO NOT include `slug` in any INSERT column list. DO NOT insert into essentials.governments or essentials.chambers — those are upstream.

    6. Write file UTF-8 without BOM. Save in `C:/EV-Accounts/backend/migrations/`.
  </action>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/301_va_state_executives.sql` exists
    - File contains exactly 3 `INSERT INTO essentials.districts` statements (STATE_EXEC districts)
    - File contains exactly 3 `WITH ins_p AS` CTE blocks (politician + office per executive)
    - File contains exactly 1 `UPDATE essentials.politicians` statement (office_id back-fill)
    - File contains the literal substring `'STATE_EXEC', 'VA', '51'` (correct district type + uppercase state + FIPS geo_id)
    - File contains the literal substring `BETWEEN -510010 AND -510001` (back-fill scope)
    - File contains the literal substrings `'Abigail Spanberger'`, `'Ghazala Hashmi'`, `'Jay Jones'`
    - File contains the literal substrings `-510001`, `-510002`, `-510003` (executive external IDs)
    - File contains zero occurrences of `'va'` quoted-lowercase as a state value for STATE_EXEC — grep `"state = 'va'"` returns no STATE_EXEC context (only senate/delegate use lowercase, but this plan has no senate/delegate refs)
    - File contains zero occurrences of `is_appointed_position = true` or `is_appointed = true`
    - File contains zero occurrences of `'Comptroller'` or `'Treasurer'`
    - File contains zero occurrences of `'Virginia (Statewide)'` or any non-empty district_id literal for STATE_EXEC rows
    - File contains zero occurrences of `slug` (case-sensitive substring)
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$f='C:/EV-Accounts/backend/migrations/301_va_state_executives.sql'; if (-not (Test-Path $f)) { exit 1 }; $c=Get-Content $f -Raw; $errors=@(); if (($c -split 'INSERT INTO essentials.districts').Length - 1 -ne 3) { $errors += 'district inserts != 3' }; if (($c -split 'WITH ins_p AS').Length - 1 -ne 3) { $errors += 'CTE blocks != 3' }; if (($c -split 'UPDATE essentials.politicians').Length - 1 -ne 1) { $errors += 'backfill UPDATE != 1' }; foreach ($n in @('Abigail Spanberger','Ghazala Hashmi','Jay Jones','-510001','-510002','-510003','''STATE_EXEC''','BETWEEN -510010 AND -510001')) { if (-not ($c.Contains($n))) { $errors += \"missing: $n\" } }; if ($c -match 'is_appointed = true') { $errors += 'is_appointed=true forbidden' }; if ($c -match 'is_appointed_position = true|is_appointed_position=true') { $errors += 'is_appointed_position=true forbidden' }; if ($c -match \"'Comptroller'|'Treasurer'\") { $errors += 'Comptroller/Treasurer forbidden' }; if ($c -match \"'Virginia .Statewide.'\") { $errors += 'named district_id forbidden' }; if ($c -match '\\bslug\\b') { $errors += 'slug forbidden' }; if ($errors.Count -gt 0) { Write-Host ('FAIL: ' + ($errors -join '; ')); exit 1 } else { Write-Host 'PASS: 301_va_state_executives.sql'; exit 0 }"</automated>
  </verify>
  <done>
    SQL file is syntactically clean; 3 STATE_EXEC districts + 3 CTE blocks + 1 back-fill UPDATE; all VA-specific substitutions made; zero forbidden patterns (lowercase STATE_EXEC state, appointed positions, Comptroller/Treasurer, named district_id, slug).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply migration 301 to production Supabase and verify</name>
  <files>none (DB-only writes to essentials.districts, essentials.politicians, essentials.offices)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/301_va_state_executives.sql (file produced by Task 1)
    - .planning/phases/101-va-state-government-db/101-RESEARCH.md (Post-Phase Verification Queries section, queries 2-4, lines 534-555)
  </read_first>
  <behavior>
    - After apply: essentials.districts has exactly 3 new rows where district_type='STATE_EXEC' AND state='VA'
    - After apply: essentials.politicians has exactly 3 new rows with external_id IN (-510001, -510002, -510003), all party='Democrat', is_appointed=false, is_vacant=false
    - After apply: essentials.offices has exactly 3 new rows linked to those 3 politicians, all is_appointed_position=false, representing_state='VA'
    - Each office row has title matching the chamber name (Governor / Lieutenant Governor / Attorney General)
    - 0 of those 3 politicians have office_id IS NULL after back-fill
    - Re-applying migration produces 0 new rows
  </behavior>
  <action>
    1. Read full content of `C:/EV-Accounts/backend/migrations/301_va_state_executives.sql` and submit via mcp__supabase-local__execute_sql as one call.
    2. Confirm no error returned.
    3. Run verification queries via mcp__supabase-local__execute_sql:
       - Q1 district count: `SELECT COUNT(*)::int FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='VA'` → must return 3.
       - Q2 district detail: `SELECT label, district_id, geo_id, state, mtfcc FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='VA' ORDER BY label` → must return 3 rows; all district_id='' AND geo_id='51' AND state='VA' AND mtfcc=''.
       - Q3 politicians: `SELECT external_id, full_name, party, is_appointed, is_vacant, is_active, is_incumbent FROM essentials.politicians WHERE external_id BETWEEN -510010 AND -510001 ORDER BY external_id DESC` → must return 3 rows, all party='Democrat', is_appointed=false, is_vacant=false, is_active=true, is_incumbent=true.
       - Q4 offices: `SELECT p.full_name, o.title, o.representing_state, o.is_appointed_position, o.is_vacant FROM essentials.offices o JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id BETWEEN -510010 AND -510001 ORDER BY p.external_id DESC` → must return 3 rows, all is_appointed_position=false AND is_vacant=false AND representing_state='VA'; titles {Governor, Lieutenant Governor, Attorney General}.
       - Q5 office_id NULL check: `SELECT COUNT(*)::int FROM essentials.politicians WHERE external_id BETWEEN -510010 AND -510001 AND office_id IS NULL` → must return 0.
    4. Run idempotency test: submit migration SQL again. Then re-run Q1 — must still return 3.
  </action>
  <acceptance_criteria>
    - mcp__supabase-local__execute_sql with migration content returns no error
    - Q1 returns 3
    - Q2 returns 3 rows; for every row district_id='' (empty string) AND geo_id='51' AND state='VA' AND mtfcc=''
    - Q3 returns 3 rows; full_name set = {Abigail Spanberger, Ghazala Hashmi, Jay Jones}; every row: party='Democrat', is_appointed=false, is_vacant=false, is_active=true, is_incumbent=true
    - Q4 returns 3 rows; the (full_name, title) pairs are {(Abigail Spanberger, Governor), (Ghazala Hashmi, Lieutenant Governor), (Jay Jones, Attorney General)}; every row: is_appointed_position=false, is_vacant=false, representing_state='VA'
    - Q5 returns 0 (all 3 politicians have office_id back-filled)
    - Idempotency: second apply leaves Q1 count at 3
  </acceptance_criteria>
  <verify>
    <automated>echo "Verification via Supabase MCP queries Q1-Q5 documented in task action; agent pastes each query result before marking done."</automated>
    <human-check>Confirm Q5 returns 0 (no NULL office_ids); Q4 (full_name, title) pairs match exactly Spanberger=Governor, Hashmi=Lieutenant Governor, Jones=Attorney General; all rows is_appointed_position=false.</human-check>
  </verify>
  <done>
    Migration 301 applied; 3 STATE_EXEC districts + 3 politicians + 3 offices seeded; office_id back-fill complete (0 NULLs); idempotency verified; VA-GOV-02 and VA-GOV-05 satisfied at data layer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → production Supabase | SQL migration writes 9 rows across 3 tables (essentials.districts, .politicians, .offices) with postgres role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-05 | Tampering | STATE_EXEC routing correctness | mitigate | state='VA' uppercase enforced (OR 223a lesson); district_id='' empty string enforced; verified in static gates and Q2 post-apply assertion |
| T-101-06 | Tampering | is_appointed_position misclassification | mitigate | Hard-coded false on all 3 office rows; VA-GOV-05 dictates all voter-elected; static gate rejects `is_appointed_position = true` substring |
| T-101-07 | Spoofing | Wrong chamber linkage (e.g., LG under Governor) | mitigate | Each CTE block names chamber explicitly in subquery `WHERE name = '<chamber>'`; Q4 post-apply verifies (full_name, title) tuples |
| T-101-08 | Information disclosure | Party storage vs. display | accept | Party stored in essentials.politicians but UI is antipartisan (memory: feedback_antipartisan_display); not displayed on politician profiles |
| T-101-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; gate non-applicable |
| T-101-09 | Repudiation | No audit trail on inserts | accept | essentials schema does not track per-row provenance; consistent with project pattern |
</threat_model>

<verification>
After Task 2:

1. `SELECT COUNT(*) FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='VA'` = 3
2. All 3 STATE_EXEC rows have district_id='', geo_id='51', state='VA', mtfcc=''
3. 3 politician rows with external_ids -510001, -510002, -510003 all Democrat, is_appointed=false, is_vacant=false
4. 3 office rows linked to chambers Governor, Lieutenant Governor, Attorney General under State of Virginia; all is_appointed_position=false
5. 0 politicians in range -510010..-510001 have NULL office_id
6. Re-apply of migration 301 produces 0 new rows
</verification>

<success_criteria>
- VA-GOV-02 satisfied: 3 executives with STATE_EXEC districts seeded and linked
- VA-GOV-05 satisfied: all 3 voter-elected (is_appointed_position=false)
- No regression in OR/MD executive data (this migration touches only external_id range -510010..-510001 and STATE_EXEC districts with state='VA')
</success_criteria>

<output>
Create `.planning/phases/101-va-state-government-db/101-02-SUMMARY.md` documenting:
- Migration 301 SQL file path
- Q1-Q5 query results pasted verbatim
- Idempotency re-apply count
- Confirmation that VA-GOV-02 + VA-GOV-05 are satisfied at data layer
</output>
