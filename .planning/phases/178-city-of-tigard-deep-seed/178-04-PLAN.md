---
phase: 178-city-of-tigard-deep-seed
plan: 04
type: execute
wave: 3
depends_on: ["178-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1161_hu_stances.sql
  - C:/EV-Accounts/backend/migrations/1162_anderson_stances.sql
  - C:/EV-Accounts/backend/migrations/1163_ghoddusi_stances.sql
  - C:/EV-Accounts/backend/migrations/1164_robbins_stances.sql
  - C:/EV-Accounts/backend/migrations/1165_schlack_stances.sql
  - C:/EV-Accounts/backend/migrations/1166_shaw_stances.sql
  - C:/EV-Accounts/backend/migrations/1167_wolf_stances.sql
autonomous: false
requirements: [WASH-04]

must_haves:
  truths:
    - "Each of the 7 officials has evidence-only compass stances where a public record exists"
    - "Every stance is 100% cited (reasoning + sources in politician_context); ZERO default/neutral values"
    - "Topics with no evidence are omitted entirely (honest blank spokes), never defaulted"
    - "topic_id is resolved LIVE via JOIN on compass_topics.topic_key AND is_live=true (no hardcoded topic UUIDs)"
    - "Each stance migration ends with the WR-02 row-count assertion DO block (catches a topic_key typo that silently drops rows)"
    - "No judicial-* topics are used (City Attorney + Municipal Court Judge are appointed, not elected)"
    - "Stance research was done one agent at a time (never parallel)"
    - "Appointed/interim seats (Hu, Anderson) attributed only to post-seating actions; local-immigration expected thinner than Hillsboro (no Tigard-specific sanctuary resolution)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1161_hu_stances.sql"
      provides: "Mayor Yi-Kang Hu stances (audit-only)"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1167_wolf_stances.sql"
      provides: "Councilor Maureen Wolf stances (audit-only)"
      contains: "politician_answers"
  key_links:
    - from: "each stance migration"
      to: "inform.compass_topics (is_live=true)"
      via: "JOIN on topic_key"
      pattern: "topic_key = s.topic_key AND ct.is_live = true"
    - from: "each stance migration"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\)"
---

<objective>
Research and author 7 audit-only stance migrations — one per Tigard official — populating
evidence-only compass stances. Research runs ONE AGENT AT A TIME (rate-limit rule), evidence-only,
100% cited, honest blank spokes where no public record exists, ZERO default values, and skips all
judicial-* topics (Tigard's City Attorney + Municipal Court Judge are appointed). Every file carries
the WR-02 row-count assertion to catch a silently-dropped topic_key.

Purpose: Satisfy the stance portion of WASH-04 — evidence-only stances render on profiles.
Output: 7 audit-only migrations (`1161_hu_stances.sql` … `1167_wolf_stances.sql`), NOT registered in
the ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/178-city-of-tigard-deep-seed/178-RESEARCH.md
@.planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md
@.planning/phases/178-city-of-tigard-deep-seed/178-01-SUMMARY.md
@.planning/phases/178-city-of-tigard-deep-seed/178-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** stance research runs via ORCHESTRATOR-spawned general-purpose agents
(one at a time — gsd-executor has NO WebSearch and NO Supabase MCP). The executor WRITES the 7 `.sql`
files from the researched evidence; the INLINE ORCHESTRATOR applies each via `psql -f` and commits
them to the EV-Accounts repo. These are AUDIT-ONLY migrations — none writes a schema_migrations
ledger row; the on-disk file counter is authoritative. Migration numbers 1161–1167 assume plan 02
used 1159 + plan 03 used 1160; if Wave-0 shifted the base, renumber consistently.

**STANCE RULES (non-negotiable — from operator conventions + memory):**
- ONE research agent at a time — never parallel (parallel burns rate-limit quota with no output);
  time-box each agent (~20–25 web ops).
- Evidence-only chairs model (integer 1–5); a "chair" is the position statement the evidence matches,
  NOT a polarity scale — pick the chair the evidence supports.
- 100% cited: every answer has reasoning + sources in politician_context.
- NO default values — a topic with no evidence is OMITTED entirely (blank spoke is honest).
- Skip all 8 judicial-* topics.
- Use the LIVE topic_key list captured in 178-01-SUMMARY.md; resolve topic_id via JOIN on
  compass_topics.topic_key AND is_live=true — never hardcode a topic UUID.
- Aim for as many cited stances as the record supports; thin records get honest blanks.
- Pre-tenure attribution: Hu was appointed Mayor Oct 2025 (elected councilor Nov 2022 — his council
  record IS attributable); Anderson was appointed Dec 2025 (very thin — honest blanks expected, do NOT
  force). Ghoddusi/Robbins/Schlack/Shaw/Wolf seated Jan 2025 (recent — use campaign-platform
  first-party statements as cited evidence where votes don't yet exist).
- local-immigration is expected THINNER than Hillsboro (no found Tigard-specific sanctuary
  resolution — Oregon's statewide sanctuary law applies uniformly, not a Tigard council action).
</context>

<interfaces>
inform.compass_topics    (id, topic_key, is_live)   -- 44 live topics; 8 are judicial-* (SKIP)
inform.politician_answers (politician_id, topic_id, value)                    -- ON CONFLICT (politician_id, topic_id) DO UPDATE SET value
inform.politician_context (politician_id, topic_id, reasoning, sources)       -- sources is text[]; ON CONFLICT DO UPDATE
-- politician UUIDs: resolve by external_id from 178-02-SUMMARY.md:
--   -4173651 Yi-Kang Hu (Mayor) | -4173652 Tom Anderson | -4173653 Faraz Ghoddusi | -4173654 Heather Robbins
--   -4173655 Jake Schlack | -4173656 Jeanette Shaw | -4173657 Maureen Wolf (Council President)
-- File→official mapping: 1161=Hu, 1162=Anderson, 1163=Ghoddusi, 1164=Robbins, 1165=Schlack, 1166=Shaw, 1167=Wolf
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Research + author stance migrations for Mayor Hu and 3 elected councilors (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1161_hu_stances.sql, C:/EV-Accounts/backend/migrations/1167_wolf_stances.sql, C:/EV-Accounts/backend/migrations/1166_shaw_stances.sql, C:/EV-Accounts/backend/migrations/1165_schlack_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1152_pace_stances.sql (42-line stance template — two-statement politician_answers + politician_context structure, chairs model)
    - .planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md §"1161_hu_stances.sql through 1167_wolf_stances.sql" (incl. the WR-02 fix block)
    - .planning/phases/178-city-of-tigard-deep-seed/178-01-SUMMARY.md (live topic_key list)
    - .planning/phases/178-city-of-tigard-deep-seed/178-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Hu (-4173651), Wolf (-4173657), Shaw (-4173656), Schlack (-4173655), IN SEQUENCE (one
    research agent at a time — do not spawn parallel research): research the official's documented
    positions across the live compass topics (skip judicial-*), gathering evidence with URLs. Then
    write the migration file using the two-statement structure from the 1152 analog: a
    WITH s(topic_key, val, reasoning, sources) VALUES block (one row per topic WITH evidence — omit
    topics with no evidence), an INSERT into inform.politician_answers … JOIN compass_topics ON
    topic_key AND is_live=true ON CONFLICT (politician_id, topic_id) DO UPDATE SET value, then the
    IDENTICAL VALUES block feeding INSERT into inform.politician_context … ON CONFLICT DO UPDATE SET
    reasoning, sources. THEN append the WR-02 row-count assertion DO block: SELECT COUNT(*) INTO n FROM
    inform.politician_answers WHERE politician_id='<uuid>'; RAISE EXCEPTION if n <> the expected count
    of authored rows (catches a topic_key typo silently dropping rows). Hardcode the politician UUID
    (from 178-02-SUMMARY.md) in each file. val is an integer 1–5 chair; never default; blank spoke =
    omit the row. Wrap each file in BEGIN…COMMIT with the audit-only header comment; NO ledger INSERT.
    Hu's council record since Nov 2022 supports the deepest coverage; Wolf/Shaw/Schlack seated Jan 2025
    (recent — use cited campaign-platform statements where votes don't yet exist). No fenced code in
    this action — 1152 + 178-PATTERNS.md are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (the WR-02 DO block RAISEs on a row-count mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 4 files authored: 1161_hu_stances.sql, 1167_wolf_stances.sql, 1166_shaw_stances.sql, 1165_schlack_stances.sql
    - Each uses the two-statement politician_answers + politician_context structure
    - Each ends with the WR-02 row-count assertion DO block (expected count matches authored rows)
    - topic_id resolved via JOIN on topic_key AND is_live=true (no hardcoded topic UUIDs)
    - No judicial-* topic_key appears in any VALUES block
    - Every VALUES row has non-empty reasoning + at least one source URL
    - No default/neutral row for a topic lacking evidence (omitted instead)
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>4 stance migrations authored from one-at-a-time research; WR-02 assertions present; audit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Research + author stance migrations for Anderson, Ghoddusi, and Robbins (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1162_anderson_stances.sql, C:/EV-Accounts/backend/migrations/1163_ghoddusi_stances.sql, C:/EV-Accounts/backend/migrations/1164_robbins_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1152_pace_stances.sql
    - .planning/phases/178-city-of-tigard-deep-seed/178-PATTERNS.md §"1161_hu_stances.sql through 1167_wolf_stances.sql" (incl. the WR-02 fix block)
    - .planning/phases/178-city-of-tigard-deep-seed/178-01-SUMMARY.md (live topic_key list)
    - .planning/phases/178-city-of-tigard-deep-seed/178-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Anderson (-4173652), Ghoddusi (-4173653), Robbins (-4173654), IN SEQUENCE (one agent at
    a time): research documented positions across live compass topics (skip judicial-*), then author
    the migration with the same two-statement structure + WR-02 row-count assertion as Task 1. Anderson
    was APPOINTED interim Dec 2025 and will NOT run in Nov 2026 (a placeholder by design) — his record
    is very thin; apply the pre-tenure attribution rule strictly and expect many honest blanks (do NOT
    force stances). Ghoddusi and Robbins were both elected Nov 2024 to the 2 new charter-expansion seats
    (seated Jan 2025) — recent, likely thinner records; use campaign-platform first-party statements as
    cited evidence where votes don't yet exist. Never default. Expect local-immigration to be an honest
    blank for most (no Tigard-specific sanctuary action). Audit-only header; NO ledger INSERT. UUIDs
    hardcoded from 178-02-SUMMARY.md.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (WR-02 DO block RAISEs on mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 3 files authored: 1162_anderson_stances.sql, 1163_ghoddusi_stances.sql, 1164_robbins_stances.sql
    - Same two-statement structure + WR-02 row-count assertion; topic_id via is_live JOIN
    - No judicial-* topics; every row cited; no defaults (thin records → honest blanks, especially Anderson)
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>3 stance migrations authored from one-at-a-time research; WR-02 assertions present; audit passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator applies all 7, runs stance audit, commits</name>
  <what-built>7 audit-only stance migrations. Orchestrator applies each and runs the citation/no-default audit.</what-built>
  <how-to-verify>
    1. Apply all 7 in order: `psql "$DATABASE_URL" -f <each stance file>`. Each WR-02 DO block must pass (no row-count mismatch RAISE).
    2. Stance audit (run as SELECTs against the 7 Tigard politician UUIDs):
       a. Every politician_answers row has a matching politician_context row with non-null reasoning + non-empty sources (100% cited).
       b. ZERO rows where value is a defaulted/neutral placeholder for a topic lacking evidence (spot-check reasoning quality).
       c. ZERO judicial-* topics linked to any Tigard official.
       d. Each official has ≥1 stance if any public record exists; a genuinely empty official (e.g. Anderson) is documented, not padded.
    3. Commit all 7 in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/116{1,2,3,4,5,6,7}_*.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - All 7 stance migrations applied; all WR-02 assertions passed
    - Audit a–d all pass (100% cited, no defaults, no judicial, honest blanks documented)
    - All 7 committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official stance counts + any honest blanks, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Stance INSERTs applied to production inform schema via psql |
| research agent → public web | Evidence gathered from official/news/campaign sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-11 | Tampering | Uncited or fabricated stance value | mitigate | 100%-cited rule enforced by audit (every answer needs reasoning + sources); no-default rule; one-agent-at-a-time keeps evidence traceable |
| T-178-12 | Tampering | topic_key typo silently drops authored rows | mitigate | WR-02 row-count assertion DO block in every stance file RAISEs on a count mismatch |
| T-178-13 | Repudiation | Wrong attribution (pre-tenure vote) | mitigate | Pre-tenure attribution rule (Hu appointed Mayor 2025 but councilor since 2022; Anderson appointed Dec 2025; others seated Jan 2025); verify seating before attributing older actions |
| T-178-14 | Elevation of Privilege | Judicial-* topic seeded for a non-judicial official | mitigate | Skip-judicial rule; audit gate 2c asserts zero judicial-* topics |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- 7 stance migrations applied; WR-02 assertions pass; audit confirms 100% cited, zero defaults,
  zero judicial topics, honest blanks documented; all committed.
</verification>

<success_criteria>
- Evidence-only stances for all 7 officials (where a record exists), 100% cited, no defaults,
  no judicial topics, WR-02 assertions present, researched one agent at a time.
</success_criteria>

<output>
Create `.planning/phases/178-city-of-tigard-deep-seed/178-04-SUMMARY.md` when done, recording
per-official stance counts and any honest blanks.
</output>
