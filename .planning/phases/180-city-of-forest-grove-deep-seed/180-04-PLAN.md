---
phase: 180-city-of-forest-grove-deep-seed
plan: 04
type: execute
wave: 3
depends_on: ["180-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1180_wenzl_stances.sql
  - C:/EV-Accounts/backend/migrations/1181_marshall_stances.sql
  - C:/EV-Accounts/backend/migrations/1182_martinez_stances.sql
  - C:/EV-Accounts/backend/migrations/1183_valenzuela_stances.sql
  - C:/EV-Accounts/backend/migrations/1184_gustafson_stances.sql
  - C:/EV-Accounts/backend/migrations/1185_falconer_stances.sql
  - C:/EV-Accounts/backend/migrations/1186_schimmel_stances.sql
autonomous: false
requirements: [WASH-06]

must_haves:
  truths:
    - "Each of the 7 officials has evidence-only compass stances where a public record exists"
    - "Every stance is 100% cited (reasoning + sources in politician_context); ZERO default/neutral values"
    - "Topics with no evidence are omitted entirely (honest blank spokes), never defaulted"
    - "topic_id is resolved LIVE via JOIN on compass_topics.topic_key AND is_live=true (no hardcoded topic UUIDs)"
    - "Each stance migration ends with the TRIPLE-GATE DO block (identity + answers-count + context-parity) from mig 1171"
    - "No judicial-* topics are used (City Attorney + Municipal Court Judge confirmed appointed at Wave-0 — the A7 assumption resolved)"
    - "Stance research was done one agent at a time (never parallel)"
    - "Angel Falconer's pre-2022 Milwaukie, OR record (2016 election, council president, 2020 re-election) is NOT cited as Forest Grove stance evidence; Peter Truax is not researched or cited"
    - "Marshall and Schimmel expected thinner (bios contain almost no specific policy language) — honest blanks acceptable; primary sources (Agenda Center, forestgrovenewstimes.com, newsinthegrove.com) consulted directly rather than relying on general web search"
    - "Decision coverage — D-08: all live compass topics per official, one research agent at a time, evidence-only, 100% cited, honest blank spokes, zero default values, 18-21+ depth where the record supports it, judicial-* topics skipped"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1180_wenzl_stances.sql"
      provides: "Mayor Malynda Wenzl stances (audit-only)"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1183_valenzuela_stances.sql"
      provides: "Council President Mariana Valenzuela stances (audit-only)"
      contains: "politician_answers"
  key_links:
    - from: "each stance migration"
      to: "inform.compass_topics (is_live=true)"
      via: "JOIN on topic_key"
      pattern: "topic_key = s.topic_key AND ct.is_live = true"
    - from: "each stance migration"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\)"
---

<objective>
Research and author 7 audit-only stance migrations — one per Forest Grove official — populating
evidence-only compass stances. Research runs ONE AGENT AT A TIME (rate-limit rule), evidence-only,
100% cited, honest blank spokes where no public record exists, ZERO default values, and skips all
judicial-* topics (Forest Grove's City Attorney + Municipal Court Judge confirmed appointed at Wave-0,
resolving the A7 assumption). Every file carries the TRIPLE-GATE DO block (identity + answers-count +
context-parity) from the freshest analog, mig 1171.

Purpose: Satisfy the stance portion of WASH-06 — evidence-only stances render on profiles.
Output: 7 audit-only migrations (`1180_wenzl_stances.sql` … `1186_schimmel_stances.sql`), NOT
registered in the ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** stance research runs via ORCHESTRATOR-spawned general-purpose agents
(one at a time — gsd-executor has NO WebSearch and NO Supabase MCP). The executor WRITES the 7 `.sql`
files from the researched evidence; the INLINE ORCHESTRATOR applies each via `psql -f` and commits
them to the EV-Accounts repo (`git -C "C:/EV-Accounts"` — never cd into it). These are AUDIT-ONLY
migrations — none writes a schema_migrations ledger row; the on-disk file counter is authoritative.
Migration numbers 1180-1186 assume plan 02 used 1178 + plan 03 used 1179; if Wave-0 shifted the base,
renumber consistently.

**STANCE RULES (non-negotiable — from operator conventions + memory):**
- ONE research agent at a time — never parallel (parallel burns rate-limit quota with no output);
  time-box each agent (~20–25 web ops).
- Evidence-only chairs model (integer 1–5); a "chair" is the position statement the evidence matches,
  NOT a polarity scale — pick the chair the evidence supports.
- 100% cited: every answer has reasoning + sources in politician_context.
- NO default values — a topic with no evidence is OMITTED entirely (blank spoke is honest).
- Skip all 8 judicial-* topics (A7 confirmed at Wave-0: City Attorney/Municipal Court Judge appointed).
- Use the LIVE topic_key list captured in 180-01-SUMMARY.md; resolve topic_id via JOIN on
  compass_topics.topic_key AND is_live=true — never hardcode a topic UUID.
- Aim for as many cited stances as the record supports (18-21+ where the record is deep); thin records
  get honest blanks.
- CRITICAL — Angel Falconer: her official bio's "first elected... in 2016 / council president /
  re-elected in 2020" history is about MILWAUKIE, OR (a different city), NOT Forest Grove. Her Forest
  Grove seat begins with her Nov 2024 election. Do NOT cite any pre-2022 date/action as Forest Grove
  stance evidence. Re-read the full bio sentence ("Angel's commitment to public service began in
  Milwaukie...") before citing anything.
- Do NOT research or cite Peter Truax — he lost the close 2024 race to Brian Schimmel (who holds the
  seat). Any "Truax" hit is stale pre-certification data.
- Marshall and Schimmel have bios with almost no specific policy language — expect honest blanks; a
  thinner record than Tualatin's. No Forest Grove-specific council vote was found via general web
  search in RESEARCH — consult primary sources directly: the city's Agenda Center
  (forestgrove-or.gov/AgendaCenter) and the local outlets forestgrovenewstimes.com / newsinthegrove.com.
- Do NOT seed any 2026-election candidate position as a current stance — this is the currently-seated
  roster only (Phase 185 owns 2026 candidate data).
</context>

<interfaces>
inform.compass_topics    (id, topic_key, is_live)   -- 44 live topics; 8 are judicial-* (SKIP)
inform.politician_answers (politician_id, topic_id, value)                    -- ON CONFLICT (politician_id, topic_id) DO UPDATE SET value
inform.politician_context (politician_id, topic_id, reasoning, sources)       -- sources is text[]; ON CONFLICT DO UPDATE
-- politician UUIDs: resolve by external_id from 180-02-SUMMARY.md:
--   -4126201 Malynda Wenzl (Mayor) | -4126202 Michael Marshall | -4126203 Karen Martinez
--   -4126204 Mariana Valenzuela (Council President) | -4126205 Donna Gustafson
--   -4126206 Angel Falconer | -4126207 Brian Schimmel
-- File→official mapping: 1180=Wenzl, 1181=Marshall, 1182=Martinez, 1183=Valenzuela, 1184=Gustafson, 1185=Falconer, 1186=Schimmel
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Research + author stance migrations for Wenzl, Martinez, Valenzuela, and Gustafson (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1180_wenzl_stances.sql, C:/EV-Accounts/backend/migrations/1182_martinez_stances.sql, C:/EV-Accounts/backend/migrations/1183_valenzuela_stances.sql, C:/EV-Accounts/backend/migrations/1184_gustafson_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1171_bubenik_stances.sql (68-line stance template — the freshest: two-statement politician_answers + politician_context structure + the TRIPLE-GATE DO block, identity + count + context-parity)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md §"1180_wenzl_stances.sql through 1186_schimmel_stances.sql (7 stance migrations, CRUD)"
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md §"Stance Evidence Landscape"
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md (live topic_key list) + 180-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Wenzl (-4126201), Martinez (-4126203), Valenzuela (-4126204), Gustafson (-4126205), IN
    SEQUENCE (one research agent at a time — do not spawn parallel research): research the official's
    documented positions across the live compass topics (skip judicial-*), gathering evidence with
    URLs — consult the city Agenda Center and forestgrovenewstimes.com / newsinthegrove.com directly,
    not just a general web search. Then write the migration file using the two-statement structure from
    the 1171 analog: a WITH s(topic_key, val, reasoning, sources) VALUES block (one row per topic WITH
    evidence — omit topics with no evidence), an INSERT into inform.politician_answers … JOIN
    compass_topics ON topic_key AND is_live=true ON CONFLICT (politician_id, topic_id) DO UPDATE SET
    value, then the IDENTICAL VALUES block feeding INSERT into inform.politician_context … ON CONFLICT
    DO UPDATE SET reasoning, sources. THEN append the TRIPLE-GATE DO block from 1171: (1) resolve
    external_id from essentials.politicians WHERE id='<uuid>' and RAISE EXCEPTION if it is DISTINCT
    FROM the expected ext_id (identity gate — a wrong-but-existing UUID would satisfy the FK while
    misattributing stances); (2) SELECT COUNT(*) from inform.politician_answers for the uuid and RAISE
    if <> expected authored count; (3) SELECT COUNT(*) from inform.politician_context for the uuid and
    RAISE if <> expected count (context-parity). Hardcode the politician UUID (from 180-02-SUMMARY.md)
    and its expected ext_id in each file. val is an integer 1–5 chair; never default; blank spoke =
    omit the row. Wrap each file in BEGIN…COMMIT with the audit-only header comment; NO ledger INSERT.
    Wenzl (Mayor since Nov 2022, councilor since 2014) and Gustafson (councilor since Nov 2020) have
    the deeper records; Valenzuela (Council President) has real leadership actions — do NOT let her
    title inflate stances beyond the evidence. No fenced code in this action — 1171 + 180-PATTERNS.md
    are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (the triple-gate DO block RAISEs on an identity/count/parity mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 4 files authored: 1180_wenzl_stances.sql, 1182_martinez_stances.sql, 1183_valenzuela_stances.sql, 1184_gustafson_stances.sql
    - Each uses the two-statement politician_answers + politician_context structure
    - Each ends with the TRIPLE-GATE DO block (identity + answers-count + context-parity), expected counts matching authored rows
    - topic_id resolved via JOIN on topic_key AND is_live=true (no hardcoded topic UUIDs)
    - No judicial-* topic_key appears in any VALUES block
    - Every VALUES row has non-empty reasoning + at least one source URL
    - No default/neutral row for a topic lacking evidence (omitted instead)
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>4 stance migrations authored from one-at-a-time research; triple-gate assertions present; audit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Research + author stance migrations for Marshall, Falconer, and Schimmel (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1181_marshall_stances.sql, C:/EV-Accounts/backend/migrations/1185_falconer_stances.sql, C:/EV-Accounts/backend/migrations/1186_schimmel_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1171_bubenik_stances.sql
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md §"1180_wenzl_stances.sql through 1186_schimmel_stances.sql (7 stance migrations, CRUD)"
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md §"Stance Evidence Landscape" + §"Common Pitfalls" (Pitfall 1 Falconer/Milwaukie, Pitfall 2 Truax)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md (live topic_key list) + 180-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Marshall (-4126202), Falconer (-4126206), Schimmel (-4126207), IN SEQUENCE (one agent
    at a time): research documented positions across live compass topics (skip judicial-*), then author
    the migration with the same two-statement structure + TRIPLE-GATE DO block as Task 1. Marshall and
    Schimmel have bios with almost no specific policy language — expect a thin record and honest blanks;
    consult the Agenda Center and local outlets directly for any council-vote evidence rather than
    relying on general search. FOR FALCONER: her Forest Grove record begins with her Nov 2024 election
    only — her bio's "2016 / council president / re-elected 2020" history is about MILWAUKIE, OR (a
    different city) and must NOT be cited as Forest Grove stance evidence; re-read the full bio sentence
    before citing any pre-2022 date. Do NOT research or cite Peter Truax for the Schimmel seat. Never
    default; blank spoke = omit the row. Audit-only header; NO ledger INSERT. UUIDs + expected ext_ids
    hardcoded from 180-02-SUMMARY.md.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (triple-gate DO block RAISEs on mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 3 files authored: 1181_marshall_stances.sql, 1185_falconer_stances.sql, 1186_schimmel_stances.sql
    - Same two-statement structure + TRIPLE-GATE DO block (identity + count + context-parity); topic_id via is_live JOIN
    - No judicial-* topics; every row cited; no defaults (thin topics → honest blanks)
    - Falconer file cites ONLY Forest Grove (Nov 2024+) evidence — no Milwaukie, OR pre-2022 record; no Truax anywhere
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>3 stance migrations authored from one-at-a-time research; Falconer/Truax pitfalls avoided; triple-gate assertions present; audit passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator applies all 7, runs stance audit, commits</name>
  <what-built>7 audit-only stance migrations. Orchestrator applies each and runs the citation/no-default audit.</what-built>
  <how-to-verify>
    1. Apply all 7 in order: `psql "$DATABASE_URL" -f <each stance file>`. Each triple-gate DO block must pass (no identity/count/parity RAISE).
    2. Stance audit (run as SELECTs against the 7 Forest Grove politician UUIDs):
       a. Every politician_answers row has a matching politician_context row with non-null reasoning + non-empty sources (100% cited).
       b. ZERO rows where value is a defaulted/neutral placeholder for a topic lacking evidence (spot-check reasoning quality).
       c. ZERO judicial-* topics linked to any Forest Grove official.
       d. Each official has ≥1 stance where the record supports it; honest blanks (a 0-count) are acceptable for Marshall/Schimmel if the record is genuinely thin — document rather than force.
       e. Spot-check the Falconer file has no pre-2022 Milwaukie-attributed evidence; no Truax anywhere.
    3. Commit all 7 in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/118{0,1,2,3,4,5,6}_*.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - All 7 stance migrations applied; all triple-gate assertions passed
    - Audit a–e all pass (100% cited, no defaults, no judicial, honest blanks documented, no Milwaukie/Truax misattribution)
    - All 7 committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official stance counts + any honest blanks, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Stance INSERTs applied to production inform schema via psql |
| research agent → public web | Evidence gathered from official/Agenda-Center/news sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-17 | Tampering | Uncited or fabricated stance value | mitigate | 100%-cited rule enforced by audit (every answer needs reasoning + sources); no-default rule; one-agent-at-a-time keeps evidence traceable |
| T-180-18 | Tampering | topic_key typo silently drops authored rows | mitigate | answers-count + context-parity assertions in every stance file RAISE on a count mismatch |
| T-180-19 | Repudiation | Wrong-UUID misattribution (a valid-but-wrong politician UUID) | mitigate | Identity gate resolves external_id from the hardcoded UUID and RAISEs unless it matches the intended official's ext_id |
| T-180-20 | Spoofing | Falconer's Milwaukie, OR record misattributed to Forest Grove, or Truax stances cited for the Schimmel seat | mitigate | Explicit Falconer/Milwaukie + Truax pitfall rules; audit gate 2e spot-checks the Falconer file and the absence of Truax |
| T-180-21 | Elevation of Privilege | Judicial-* topic seeded for a non-judicial official | mitigate | Skip-judicial rule (A7 confirmed at Wave-0); audit gate 2c asserts zero judicial-* topics |
| T-180-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- 7 stance migrations applied; triple-gate assertions pass; audit confirms 100% cited, zero defaults,
  zero judicial topics, honest blanks documented, no Milwaukie/Truax misattribution; all committed.
</verification>

<success_criteria>
- Evidence-only stances for all 7 officials, 100% cited, no defaults, no judicial topics, triple-gate
  assertions present, researched one agent at a time, Falconer/Truax pitfalls avoided.
</success_criteria>

<output>
Create `.planning/phases/180-city-of-forest-grove-deep-seed/180-04-SUMMARY.md` when done, recording
per-official stance counts and any honest blanks.
</output>
