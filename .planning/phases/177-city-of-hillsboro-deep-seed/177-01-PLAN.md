---
phase: 177-city-of-hillsboro-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-hillsboro-wave0-probe.sql
autonomous: false
requirements: [WASH-03]

must_haves:
  truths:
    - "Orchestrator has confirmed the city geo_id is 4134100 (NOT 4133850) and its G4110 geofence exists"
    - "Orchestrator has confirmed no City of Hillsboro government row already exists (greenfield)"
    - "Orchestrator has confirmed the ext_id block -4134101..-4134107 is free of collisions"
    - "Orchestrator has confirmed the on-disk migration MAX so the next structural migration number is safe (research snapshot: MAX 1149, next 1150)"
    - "The current seated roster is re-confirmed live from hillsboro-oregon.civicweb.net (Mayor + 6 councilors, ward/position labels)"
    - "At least one CivicWeb headshot URL is confirmed to download valid non-zero JPEG bytes"
    - "The live compass topic_key list (is_live=true) is captured for stance ingestion"
    - "Wave-0 checkpoint confirms CONTEXT decisions D-01/D-02 (at-large routing, NO ward geofences) before any write"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-hillsboro-wave0-probe.sql"
      provides: "The Wave-0 probe queries the orchestrator runs before any write"
      min_lines: 20
  key_links:
    - from: "probe.sql"
      to: "essentials.governments / essentials.politicians / essentials.geofence_boundaries / supabase_migrations.schema_migrations / inform.compass_topics"
      via: "SELECT probes"
      pattern: "essentials\\.(governments|politicians|geofence_boundaries)"
---

<objective>
Author the Wave-0 verification probe file for the City of Hillsboro deep-seed and drive the
orchestrator-run DB + roster checks that MUST pass before any structural write. This plan gates the
entire phase: no government, roster, headshot, or stance migration may be authored or applied until
every probe below returns its expected result.

Purpose: Prevent the four highest-risk failure modes for this phase — the wrong geo_id
(4133850 does not exist; correct is 4134100), a duplicate seed, an ext_id collision, and a
wrong/stale migration number — plus verify the at-large routing decision and headshot-source
retrievability before committing to the seed shape (RESEARCH Pitfalls 1, 2, 6; Assumptions A1-A7).
Output: `_tmp-hillsboro-wave0-probe.sql` (gitignored `_tmp-*` helper) + a recorded pass/fail for
each Wave-0 gate, recorded in the SUMMARY for downstream plans 02-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-RESEARCH.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-VALIDATION.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO
database access. The INLINE ORCHESTRATOR (main context) runs the probes via
`psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`.
This file is a gitignored `_tmp-*` helper — do NOT commit it.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except where a query genuinely reads that table — see probe D), and
`photo_origin_url` out of comments in any file authored this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-hillsboro-wave0-probe.sql</files>
  <read_first>
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-RESEARCH.md §"CRITICAL: geo_id Correction", §"Geofence Pre-Check", §"Ext_id Block", §"Next Migration Number", §"Live Compass Topics", §"Wave 0 Gaps"
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-VALIDATION.md §"Wave 0 Requirements"
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md §"Key Casing / Structural Traps"
  </read_first>
  <action>
    Write labeled SELECT probes, each preceded by a `\echo` label line so the orchestrator reads
    results unambiguously. Probe A (GEO_ID CORRECTION — the single most important gate): count rows
    in essentials.geofence_boundaries WHERE geo_id='4134100' AND mtfcc='G4110' (expect 1), AND
    separately count rows WHERE geo_id='4133850' (expect 0 — proves the CONTEXT.md value is wrong).
    Probe B: count essentials.districts WHERE geo_id='4134100' (expect 0 — no city districts yet).
    Probe C: count essentials.governments WHERE geo_id='4134100' OR name ILIKE '%hillsboro%' (expect 0
    — greenfield). Probe D: select external_id from essentials.politicians WHERE external_id BETWEEN
    -4134110 AND -4134100 (expect 0 rows — ext_id block -4134101..-4134107 free). Probe E: select
    MAX(version::int) from supabase_migrations.schema_migrations (record it; the next STRUCTURAL
    migration number = ledger MAX + 1; research snapshot expects on-disk MAX 1149 → next 1150 — the
    orchestrator cross-checks with an `ls` of the migrations dir). Probe F: select distinct state from
    essentials.districts WHERE geo_id='4159000' (Portland) to confirm lowercase 'or' is the correct
    casing for LOCAL/LOCAL_EXEC rows. Probe G: select topic_key from inform.compass_topics WHERE
    is_live=true ORDER BY topic_key — the authoritative live-topic list for the stance plan (04).
    Include a header comment noting the file is a gitignored orchestrator-run helper. Do NOT wrap in a
    transaction (read-only). No fenced code, no DDL — SELECT/\echo only.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-hillsboro-wave0-probe.sql` and records each result</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-hillsboro-wave0-probe.sql with labeled probes A–G
    - Probe A queries geofence_boundaries for geo_id='4134100' AND mtfcc='G4110', AND separately for geo_id='4133850'
    - Probe C queries governments by geo_id='4134100' OR name ILIKE '%hillsboro%'
    - Probe D queries the exact ext_id range -4134110..-4134100
    - Probe E reads MAX(version::int) from the migration ledger
    - Probe G selects topic_key WHERE is_live=true
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE)
  </acceptance_criteria>
  <done>Probe file authored with all queries and correct literal values (geo_id 4134100).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes, re-fetches roster, tests one headshot, confirms all gates</name>
  <what-built>The Wave-0 probe file. The orchestrator now executes it against the live DB, re-fetches the live roster, and test-downloads one CivicWeb headshot.</what-built>
  <how-to-verify>
    A. Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-hillsboro-wave0-probe.sql`
       (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
       1. Probe A returns geofence(4134100)=1 AND geofence(4133850)=0. If 4134100≠1 → STOP, city geofence missing. If 4133850>0 → note it but still use 4134100 (research-confirmed correct place code).
       2. Probe B returns districts=0. If >0 → STOP, districts already exist (not greenfield).
       3. Probe C returns 0 (no existing Hillsboro gov). If >0 → STOP, phase already partly seeded — switch to reconcile.
       4. Probe D returns 0 rows (ext_id block free). If any row → shift the block (e.g. -4134111..-4134117) and record the new range for plans 02/03/04.
       5. Probe E: cross-check against `ls C:/EV-Accounts/backend/migrations | sort -t_ -k1,1n | tail`. Next structural migration = on-disk MAX + 1 (research snapshot: 1149 → 1150). Record the confirmed number for plan 02.
       6. Probe F returns 'or' (lowercase) — the casing plan 02 must use for LOCAL/LOCAL_EXEC districts.
       7. Probe G returns the live topic_key list — save it verbatim for plan 04.
    B. Re-fetch `hillsboro-oregon.civicweb.net/portal/members.aspx?id=10` (NO-WAF; hillsboro-oregon.gov is Akamai WAF-403) and confirm the 7-member roster is unchanged since 2026-07-01: Mayor Beach Pace + Salgado (W1 PosA), Anvery (W1 PosB), Sinclair (W2 PosA), Case (W2 PosB), Alcaire (W3 PosA), Harris (W3 PosB, Council President). If any member changed → re-plan the roster in plan 02.
    C. Test-download ONE CivicWeb headshot (e.g. Mayor Pace: hillsboro-oregon.civicweb.net/FileStorage/content/UserImages/user-29.jpg) with descriptive headers + `-L`; confirm it saves non-zero bytes and opens as a valid JPEG (research saw one 200/0-byte response). If unretrievable → record that plan 03 must use Ballotpedia/campaign fallbacks.
    D. Confirm the at-large decision holds: wards are candidacy-residency only, all voters vote citywide → NO ward geofences (RESEARCH Form-of-Government resolution, Pitfall 1). If the re-fetch surfaces contradicting ward-voting language → STOP and re-plan.
  </how-to-verify>
  <acceptance_criteria>
    - All probe gates recorded with pass/fail
    - geo_id 4134100 geofence confirmed = 1; 4133850 confirmed nonexistent
    - Confirmed ext_id block (default -4134101..-4134107 unless probe D forced a shift)
    - Confirmed next structural migration number (default 1150) via ledger + on-disk `ls` cross-check
    - Live topic_key list captured for plan 04
    - Live roster re-confirmed (7 members, exact ward/position labels)
    - One CivicWeb headshot confirmed downloadable OR fallback plan recorded
    - At-large / no-ward-geofence decision confirmed
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |
| operator → external web | Roster re-fetch + one headshot test-download from hillsboro-oregon.civicweb.net |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-01 | Tampering | probe.sql executed on prod DB | accept | Probes are read-only SELECTs; no write path; no user input |
| T-177-02 | Spoofing | roster/headshot source (CivicWeb portal) | mitigate | Use the official city CivicWeb subdomain (not a third-party mirror); cross-checked vs LegiStorm/local news in RESEARCH; re-fetch confirms currency before seed |
| T-177-03 | Information Disclosure | topic_key list from inform.compass_topics | accept | Public compass topic keys; no PII |
| T-177-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- Any failed gate blocks plans 02–05 until resolved.
</verification>

<success_criteria>
- geo_id 4134100 geofence confirmed present; 4133850 confirmed absent; no duplicate Hillsboro gov;
  districts absent; ext_id block free; next migration number confirmed; live topic_key list captured;
  roster re-confirmed; headshot retrievability tested; at-large decision confirmed.
</success_criteria>

<output>
Create `.planning/phases/177-city-of-hillsboro-deep-seed/177-01-SUMMARY.md` when done, recording
the confirmed geo_id (4134100), ext_id block, next structural migration number, live topic_key list,
re-confirmed roster, and headshot-source retrievability for downstream plans.
</output>
