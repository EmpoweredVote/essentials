---
phase: 69-landing-elections-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - ELECT-01
  - ELECT-02

must_haves:
  truths:
    - "CA 2026 Statewide Primary election row exists with election_date='2026-06-02', election_type='primary', jurisdiction_level='state', state='CA'"
    - "CA 2026 Statewide General election row exists with election_date='2026-11-03', election_type='general', jurisdiction_level='state', state='CA'"
    - "Governor race (id=bc936a36) has election_id pointing to the CA 2026 Statewide General row and office_id='08454462-a1f0-4d11-9f61-aba7a173a3de'"
  artifacts:
    - path: "essentials.elections"
      provides: "CA statewide primary + general election rows"
      contains: "CA 2026 Statewide General"
    - path: "essentials.races"
      provides: "Governor race correctly linked to general election + Governor office"
  key_links:
    - from: "essentials.races (id=bc936a36)"
      to: "essentials.elections (CA 2026 Statewide General)"
      via: "races.election_id FK"
      pattern: "SELECT election_id FROM essentials.races WHERE id='bc936a36-287c-4ffd-abd8-5e4fd798bae5'"
---

<objective>
Create the two CA 2026 statewide election rows (primary + general) and patch the existing Governor race to link to the general election with the correct office_id.

Purpose: ELECT-01 requires both statewide election rows. ELECT-02 requires the Governor race to be linked to the correct election with office_id set. The existing Governor race (bc936a36) is currently linked to the LA County Primary (1ebca37f) — a county-level election — and has office_id=NULL, both of which are wrong.
Output: Migration 221 applied — 2 new elections rows inserted, Governor race UPDATEd.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-landing-elections-discovery/69-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert CA statewide election rows (migration 221)</name>
  <files></files>

  <read_first>
    .planning/phases/69-landing-elections-discovery/69-RESEARCH.md — section "2. CA Elections — Current DB State" to confirm the exact name format for existing rows before inserting. Also confirms the ON CONFLICT guard is safe to use.

    Run this pre-flight query to confirm current state before inserting:
    SELECT id, name, election_date::text, election_type, jurisdiction_level
    FROM essentials.elections WHERE state='CA' ORDER BY election_date;
    Expected: 1 row — id='1ebca37f-cf96-47f4-bc2b-47ef266721fe', name='2026 LA County Primary', jurisdiction_level='county'
  </read_first>

  <action>
    Apply migration 221 via mcp__supabase-local__execute_sql. The SQL inserts two new rows into essentials.elections:

    Row 1 — CA 2026 Statewide Primary:
    - name: 'CA 2026 Statewide Primary'
    - election_date: '2026-06-02'
    - election_type: 'primary'
    - jurisdiction_level: 'state'
    - state: 'CA'

    Row 2 — CA 2026 Statewide General:
    - name: 'CA 2026 Statewide General'
    - election_date: '2026-11-03'
    - election_type: 'general'
    - jurisdiction_level: 'state'
    - state: 'CA'

    Use gen_random_uuid() for each row's id. Use ON CONFLICT DO NOTHING for idempotency.

    CRITICAL: The existing row '2026 LA County Primary' (id=1ebca37f) has jurisdiction_level='county' — do NOT modify or reference it. This migration only INSERTs new rows.

    CRITICAL: Do NOT use November 4 or June 3 — both dates are incorrect. Use '2026-06-02' and '2026-11-03' exactly.

    Schema reference (verified 2026-05-28):
    - essentials.elections columns: id uuid, name text, election_date date, election_type text, jurisdiction_level text, state char(2), description text, created_at timestamptz, updated_at timestamptz
    - There is NO cron_active column — do not include it
  </action>

  <verify>
    <automated>
      SELECT name, election_date::text, election_type, jurisdiction_level
      FROM essentials.elections WHERE state='CA' AND jurisdiction_level='state'
      ORDER BY election_date;
    </automated>
  </verify>

  <acceptance_criteria>
    - Query `SELECT COUNT(*) FROM essentials.elections WHERE state='CA' AND jurisdiction_level='state'` returns 2
    - Row 1: name='CA 2026 Statewide Primary', election_date='2026-06-02', election_type='primary', jurisdiction_level='state', state='CA'
    - Row 2: name='CA 2026 Statewide General', election_date='2026-11-03', election_type='general', jurisdiction_level='state', state='CA'
    - Existing row '2026 LA County Primary' (1ebca37f) is unchanged: `SELECT jurisdiction_level FROM essentials.elections WHERE id='1ebca37f-cf96-47f4-bc2b-47ef266721fe'` still returns 'county'
    - No SQL error on apply
  </acceptance_criteria>

  <done>Both CA statewide election rows exist in essentials.elections with correct dates, types, and jurisdiction_level='state'.</done>
</task>

<task type="auto">
  <name>Task 2: Patch Governor race to general election + office_id (migration 221, continued)</name>
  <files></files>

  <read_first>
    .planning/phases/69-landing-elections-discovery/69-RESEARCH.md — section "3. Governor Race — Current DB State" to confirm the race id, current wrong election_id, and the correct office_id to use.

    Pre-flight check before UPDATE:
    SELECT id, position_name, election_id::text, office_id::text
    FROM essentials.races WHERE id = 'bc936a36-287c-4ffd-abd8-5e4fd798bae5';
    Expected: election_id='1ebca37f-...' (LA County Primary — wrong), office_id=NULL (wrong)
  </read_first>

  <action>
    Apply as part of migration 221 (same SQL block as Task 1, appended after the election INSERTs) via mcp__supabase-local__execute_sql.

    UPDATE essentials.races:
    - WHERE id = 'bc936a36-287c-4ffd-abd8-5e4fd798bae5' (the existing CA Governor race — this exact UUID was verified DB 2026-05-28)
    - SET election_id = (SELECT id FROM essentials.elections WHERE name = 'CA 2026 Statewide General')
    - SET office_id = '08454462-a1f0-4d11-9f61-aba7a173a3de'

    The subquery resolves the general election UUID from the row inserted in Task 1 above. Both SET clauses must be in the same UPDATE statement.

    DO NOT insert a new Governor race row — the existing race bc936a36 already has race_candidates linked to it from Phase 62. An INSERT would create a duplicate with no candidates.

    DO NOT use the LA County Primary election_id (1ebca37f) — this is the current wrong value being replaced.

    office_id '08454462-a1f0-4d11-9f61-aba7a173a3de' is the Governor office row for title='Governor' in the CA government (verified DB 2026-05-28).

    Recommended full migration 221 SQL block (Tasks 1 + 2 combined):
    1. INSERT CA Statewide Primary (gen_random_uuid())
    2. INSERT CA Statewide General (gen_random_uuid())
    3. UPDATE races SET election_id + office_id WHERE id='bc936a36...'
  </action>

  <verify>
    <automated>
      SELECT r.position_name, e.name AS election_name, r.office_id::text
      FROM essentials.races r
      JOIN essentials.elections e ON e.id = r.election_id
      WHERE r.id = 'bc936a36-287c-4ffd-abd8-5e4fd798bae5';
    </automated>
  </verify>

  <acceptance_criteria>
    - `SELECT election_id FROM essentials.races WHERE id='bc936a36-287c-4ffd-abd8-5e4fd798bae5'` returns the UUID of the 'CA 2026 Statewide General' election row (NOT '1ebca37f-cf96-47f4-bc2b-47ef266721fe')
    - `SELECT office_id FROM essentials.races WHERE id='bc936a36-287c-4ffd-abd8-5e4fd798bae5'` returns '08454462-a1f0-4d11-9f61-aba7a173a3de'
    - JOIN query: `SELECT e.name FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE r.id='bc936a36-287c-4ffd-abd8-5e4fd798bae5'` returns 'CA 2026 Statewide General'
    - `SELECT COUNT(*) FROM essentials.races WHERE position_name='CA Governor'` returns 1 (no duplicate created)
    - `SELECT COUNT(*) FROM essentials.race_candidates WHERE race_id='bc936a36-287c-4ffd-abd8-5e4fd798bae5'` returns same count as before the migration (candidates still linked — migration only changed election_id and office_id, not race_candidates)
  </acceptance_criteria>

  <done>Governor race (bc936a36) election_id points to CA 2026 Statewide General and office_id is set to '08454462-a1f0-4d11-9f61-aba7a173a3de'. Existing race_candidates are unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → essentials DB | Direct DB writes via Supabase MCP; no user-supplied values — all values are hardcoded constants from verified research |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-02-01 | Tampering | essentials.races UPDATE | mitigate | WHERE clause uses specific UUID (bc936a36) — no risk of mass update; pre-flight SELECT confirms state before UPDATE |
| T-69-02-02 | Information Disclosure | election rows | accept | Election data is public record; no PII involved |
| T-69-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SQL only |
</threat_model>

<verification>
Run both smoke queries after migration 221 is applied:

1. Elections smoke:
```sql
SELECT name, election_date::text, election_type, jurisdiction_level
FROM essentials.elections WHERE state='CA' ORDER BY election_date;
```
Expected: at least 3 rows — LA County Primary (county), CA Statewide Primary (state, 2026-06-02), CA Statewide General (state, 2026-11-03)

2. Governor race smoke:
```sql
SELECT r.position_name, e.name AS election_name, r.office_id::text
FROM essentials.races r
JOIN essentials.elections e ON e.id = r.election_id
WHERE r.id = 'bc936a36-287c-4ffd-abd8-5e4fd798bae5';
```
Expected: election_name='CA 2026 Statewide General', office_id='08454462-a1f0-4d11-9f61-aba7a173a3de'
</verification>

<success_criteria>
- 2 new statewide election rows in essentials.elections (primary June 2 + general Nov 3) with jurisdiction_level='state'
- Governor race (bc936a36) linked to CA 2026 Statewide General election with office_id filled — ELECT-01 and ELECT-02 satisfied
- No duplicate election rows, no duplicate race rows
</success_criteria>

<output>
Create `.planning/phases/69-landing-elections-discovery/69-02-SUMMARY.md` when done.
</output>
