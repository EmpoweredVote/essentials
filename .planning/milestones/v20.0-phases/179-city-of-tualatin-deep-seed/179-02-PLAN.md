---
phase: 179-city-of-tualatin-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["179-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1169_tualatin_city_council.sql
autonomous: false
requirements: [WASH-05]

must_haves:
  truths:
    - "One 'City of Tualatin, Oregon, US' government row exists (geo_id 4174950 — the CORRECTED value, never 4175200)"
    - "One 'City Council' chamber exists under it with official_count=7"
    - "Exactly one LOCAL_EXEC district and one LOCAL district exist on geo_id 4174950, state='or' lowercase"
    - "7 offices exist (Mayor + 6 numbered Council Positions), each with representing_city='Tualatin' set INLINE"
    - "Office titles are 'Mayor' (LOCAL_EXEC) and 'Council Member (Position N)' for N=1..6 (Beaverton numbered convention — NOT Tigard's plain 'Councilor')"
    - "ALL 7 politicians carry is_appointed=false + all 7 offices carry is_appointed_position=false (uniform — zero appointed seats, unlike Tigard)"
    - "All 7 politicians have office_id back-filled (no NULL office_id)"
    - "Section-split scan returns 0 rows for geo_id 4174950 (via the WR-01 independent canonical query, not the dead same-transaction gate)"
    - "The post-verify block includes an INDEPENDENT geofence-presence assertion for geo_id 4174950 AND mtfcc G4110 (the exact check that would have caught the 4175200 error)"
    - "NO ward/X00xx geofences or districts were created (pure at-large model)"
    - "Valerie Pratt is one Position-6 office (Council President title-on-seat), not a separate office row; her is_appointed=false despite the 2019 appointment mentioned in her bio"
    - "Decision coverage — D-03: ward branch N/A (at-large fired; seat identity on office titles, no duplicate district rows); D-04: no section-split and no empty LOCAL section (independent canonical scan = 0 rows); D-06: Pratt's Council President designation is title-on-seat on Position 6, no separate office row; D-11: representing_city='Tualatin' set INLINE in this structural migration (no backfill)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1169_tualatin_city_council.sql"
      provides: "Structural seed: government + chamber + 2 districts + 7 offices + office_id backfill + WR-01-fixed post-verify DO block (incl. independent geofence assertion) + ledger row"
      min_lines: 250
      contains: "City of Tualatin, Oregon, US"
  key_links:
    - from: "offices"
      to: "districts (geo_id 4174950)"
      via: "district_id FK on LOCAL_EXEC + LOCAL rows"
      pattern: "geo_id = '4174950'"
    - from: "politicians.office_id"
      to: "offices.id"
      via: "back-fill UPDATE keyed on external_id IN (-4174951..-4174957)"
      pattern: "office_id = o.id"
---

<objective>
Author the single structural migration that seeds the City of Tualatin government, City Council
chamber, two citywide districts (LOCAL_EXEC for the Mayor + LOCAL at-large for all 6 numbered Council
Positions), and the 7 officials. Tualatin is the Beaverton 176 shape: directly-elected Mayor + 6
numbered at-large Positions, ZERO appointed seats. Use Beaverton mig 1131 for the numbered-Position
title convention and uniform is_appointed=false shape, but overlay the Tigard mig 1159 verification
fixes (WR-01 independent geofence assertion + inline representing_city — Beaverton's original 1131
predates both). Set `representing_city='Tualatin'` INLINE on every office INSERT so the community
banner derives correctly at first browse (no backfill migration).

Purpose: Satisfy the roster/structure portion of WASH-05 with an idempotent, self-verifying migration
that uses the CORRECTED geo_id 4174950, produces no section-split and no empty LOCAL section, and
asserts the geofence actually exists (catching the phantom-seed risk the wrong 4175200 would create).
Output: `1169_tualatin_city_council.sql` (registered in schema_migrations), applied by the
orchestrator via psql, with its in-migration DO-block gates passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-RESEARCH.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-01-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES the `.sql` file to C:/EV-Accounts/backend/migrations/.
It has NO DB access. The INLINE ORCHESTRATOR applies it via `psql "$DATABASE_URL" -f <file>`
(DATABASE_URL from C:/EV-Accounts/backend/.env) and commits it in the C:/EV-Accounts repo
(`git -C "C:/EV-Accounts"`). Use the exact migration number confirmed in 179-01-SUMMARY.md (default
1169; ledger MAX 1159 is a TRAP — on-disk MAX 1168 is authoritative) — if Wave-0 recorded a different
next number, rename the file and update the ledger INSERT.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug` and
`photo_origin_url` OUT of comments in this file. Never write the wrong value `4175200` anywhere in
this file — every geo_id reference (INSERTs, comments, post-verify) uses 4174950. The ledger INSERT
into supabase_migrations.schema_migrations is a genuine table write (expected), not a comment.
</context>

<interfaces>
<!-- Confirmed schema shapes — executor uses these directly, no exploration needed. -->

essentials.governments  (id, name, type, state, city, geo_id)   -- slug GENERATED; NO geo_id unique constraint; INSERT via WHERE NOT EXISTS on name
essentials.chambers     (id, name, name_formal, government_id, official_count)  -- slug GENERATED ALWAYS — NEVER include in INSERT column list
essentials.districts    (id, district_type, state, geo_id, label, mtfcc)  -- NO name_formal; government_id NULL; state='or' LOWERCASE for LOCAL/LOCAL_EXEC
essentials.politicians  (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id, office_id)  -- party ALWAYS NULL (antipartisan)
essentials.offices      (id, district_id, chamber_id, politician_id, title, representing_state, representing_city, is_appointed_position, is_vacant, role_canonical)
</interfaces>

<roster>
<!-- Seed exactly these 7 (re-confirmed at Wave-0). ext_id / title / district_type / appointed: -->
| ext_id    | full_name        | first_name | last_name | title                         | district_type | is_appointed |
|-----------|------------------|------------|-----------|-------------------------------|---------------|--------------|
| -4174951  | Frank Bubenik    | Frank      | Bubenik   | Mayor                         | LOCAL_EXEC    | FALSE (directly elected citywide, term through Dec 31 2026) |
| -4174952  | María Reyes      | María      | Reyes     | Council Member (Position 1)   | LOCAL         | FALSE (elected, term through Dec 31 2026) |
| -4174953  | Christen Sacco   | Christen   | Sacco     | Council Member (Position 2)   | LOCAL         | FALSE (elected, term through Dec 31 2028) |
| -4174954  | Bridget Brooks   | Bridget    | Brooks    | Council Member (Position 3)   | LOCAL         | FALSE (elected, term through Dec 31 2026) |
| -4174955  | Cyndy Hillier    | Cyndy      | Hillier   | Council Member (Position 4)   | LOCAL         | FALSE (elected, term through Dec 31 2028) |
| -4174956  | Octavio Gonzalez | Octavio    | Gonzalez  | Council Member (Position 5)   | LOCAL         | FALSE (elected, term through Dec 31 2026) |
| -4174957  | Valerie Pratt    | Valerie    | Pratt     | Council Member (Position 6)   | LOCAL         | FALSE (elected; holds Council President title-on-seat; 2019 appointment superseded by 2020+2024 elections) |
<!-- Titles use Beaverton's numbered 'Council Member (Position N)' convention — NOT Tigard's plain
     'Councilor'. Mayor Bubenik on LOCAL_EXEC; all 6 councilors on the shared LOCAL district.
     ALL 7 use is_appointed=false + is_appointed_position=false (uniform Beaverton/Hillsboro shape —
     do NOT import Tigard's appointed-seat block for any seat). party=NULL, representing_state='OR',
     representing_city='Tualatin' on all 7. Pratt's "Council President" is a title-on-seat — note ONLY
     in a migration comment; do NOT create a separate office row and do NOT alter her title. There is
     NO Youth Councilor seat to seed (Youth Advisory Council is a separate advisory board). Do NOT seed
     any 2026-election candidate as an incumbent. -->
</roster>

<tasks>

<task type="auto">
  <name>Task 1: Author the Tualatin structural migration</name>
  <files>C:/EV-Accounts/backend/migrations/1169_tualatin_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1131_beaverton_city_council.sql (the numbered-Position + zero-appointed-seats structural template — entire file)
    - C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql (the verification-gate fixes to overlay: WR-01 independent geofence assertion + inline representing_city + post-verify DO block)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md §"1169_tualatin_city_council.sql (structural migration, CRUD)" (every code block, adapted values)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-01-SUMMARY.md (confirmed corrected geo_id 4174950, ext_id block, migration number)
    - .planning/phases/179-city-of-tualatin-deep-seed/179-RESEARCH.md §"Schema Shapes", §"OR Casing Rules", §"Office Title Convention", §"Migration Template Fixes to Carry Forward"
  </read_first>
  <action>
    Wrap everything in a single BEGIN…COMMIT transaction. Structure per Beaverton mig 1131 for the
    numbered-Position shape, overlaying the Tigard 1159 gate fixes, adapting values:
    (1) Pre-flight hard-abort DO block: RAISE EXCEPTION if a 'City of Tualatin, Oregon, US' government
    already exists (message references migration 1169).
    (2) Government INSERT … SELECT (name='City of Tualatin, Oregon, US', type='LOCAL', state='OR'
    UPPERCASE, city='Tualatin', geo_id='4174950') guarded WHERE NOT EXISTS on name — NEVER insert slug.
    (3) Chamber INSERT (name='City Council', name_formal='Tualatin City Council', government_id
    subselect, official_count=7) guarded WHERE NOT EXISTS on (name, government_id) — NEVER insert slug.
    (4) TWO district INSERTs, both state='or' LOWERCASE, geo_id='4174950', mtfcc=NULL: one
    district_type='LOCAL_EXEC' label='Tualatin (Mayor, Citywide)', one district_type='LOCAL'
    label='Tualatin (At-Large)', each guarded WHERE NOT EXISTS on (geo_id, district_type, state).
    Create NO other districts — no ward/X00xx rows, and NO per-Position districts (the position numbers
    are labels on the office title, not districts — all 6 councilors share the ONE LOCAL district).
    (5) SEVEN office blocks (the CTE pattern: INSERT politician ON CONFLICT (external_id) DO UPDATE SET
    is_active, then INSERT office CROSS JOIN the correct district guarded by NOT EXISTS on
    (district_id, politician_id)). Use the exact 7 rows in the <roster> table above: Mayor Bubenik on
    the LOCAL_EXEC district (title 'Mayor'); the 6 councilors on the shared LOCAL district (titles
    'Council Member (Position 1)' … 'Council Member (Position 6)'). party=NULL, role_canonical=NULL,
    representing_state='OR' UPPERCASE, representing_city='Tualatin' INLINE on every office. UNIFORM
    appointed flags: ALL 7 politicians is_appointed=false AND all 7 offices is_appointed_position=false
    (the Beaverton/Hillsboro uniform shape — do NOT import Tigard's is_appointed=true block for any
    seat). Add a comment above Pratt's block (Position 6) noting she holds the Council President
    title-on-seat (one office row only, no separate row) and that her 2019 appointment was superseded
    by 2020+2024 elections so is_appointed=false is correct.
    (6) office_id back-fill UPDATE with an explicit external_id IN (-4174951,-4174952,-4174953,
    -4174954,-4174955,-4174956,-4174957) list, guarded AND p.office_id IS NULL.
    (7) Post-verification DO block WITH THE WR-01 FIX (use the Tigard 1159 version, NOT Beaverton
    1131's original dead same-transaction gate): gate gov_count=1; office_count=7 (JOIN districts on
    geo_id 4174950 + district_type IN ('LOCAL','LOCAL_EXEC') + state 'or'); an INDEPENDENT
    geofence-presence assertion (COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id='4174950'
    AND mtfcc='G4110' must be ≥1 — this is the exact check that catches the 4175200 phantom-seed error);
    the canonical section-split query (GROUP BY o.district_id HAVING COUNT(DISTINCT o.chamber_id) > 1
    over offices JOIN districts on geo_id 4174950) = 0; office_id NULLs=0; representing_city='Tualatin'
    count=7. RAISE EXCEPTION on any mismatch; RAISE NOTICE on success.
    (8) Ledger INSERT into supabase_migrations.schema_migrations (version) VALUES ('1169') ON CONFLICT
    DO NOTHING (STRUCTURAL migrations register — contrast with headshot/stance migrations).
    Do NOT place fenced code blocks in this action — the SQL itself lives in the file; the analog
    blocks in 179-PATTERNS.md are the source of truth for exact text.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1169_tualatin_city_council.sql`; the in-migration WR-01-fixed post-verify DO block RAISEs on any gate failure (all gates must emit the success NOTICE)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the confirmed migration number path (default 1169_tualatin_city_council.sql)
    - Single BEGIN…COMMIT; pre-flight abort guard present
    - governments INSERT uses geo_id='4174950', state='OR', WHERE NOT EXISTS on name; no slug column
    - chambers INSERT has official_count=7 and name_formal='Tualatin City Council'; no slug column
    - Exactly 2 district INSERTs (LOCAL_EXEC + LOCAL), both state='or' lowercase, geo_id='4174950'; NO ward/X00xx and NO per-Position districts
    - 7 office blocks with the exact ext_ids -4174951..-4174957; titles are 'Mayor' + 'Council Member (Position 1..6)' (numbered Beaverton convention, NOT plain 'Councilor')
    - ALL 7 politicians is_appointed=false AND all 7 offices is_appointed_position=false (no appointed seats)
    - representing_city='Tualatin' appears on all 7 office INSERTs (inline, no backfill migration)
    - office_id back-fill UPDATE lists all 7 external_ids
    - Post-verify DO block includes the WR-01 independent geofence assertion (geo_id='4174950' AND mtfcc='G4110' ≥ 1) + canonical section-split query (not the dead same-transaction gate); asserts gov=1, offices=7, section-split=0, office_id nulls=0, representing_city=7
    - Ledger INSERT VALUES ('1169') present (structural migration registers)
    - The literal '4175200' does NOT appear anywhere in the file; literal strings `slug` and `photo_origin_url` do NOT appear in comments
  </acceptance_criteria>
  <done>Structural migration authored against corrected geo_id 4174950; applies cleanly; WR-01-fixed post-verify DO block (with independent geofence assertion) emits its success NOTICE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies the migration, runs the E2E structural gate, commits</name>
  <what-built>The Tualatin structural migration. The orchestrator applies it and runs the standalone verification gate.</what-built>
  <how-to-verify>
    1. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1169_tualatin_city_council.sql`. The in-migration DO block must emit "Post-verification PASSED". If it RAISEs, the transaction rolls back — fix and re-apply.
    2. Independent E2E gate (run as separate SELECTs):
       a. governments count for name='City of Tualatin, Oregon, US' = 1.
       b. chambers row exists, official_count=7, name_formal='Tualatin City Council'.
       c. districts: exactly 1 LOCAL_EXEC + 1 LOCAL for geo_id='4174950' state='or'; ZERO ward/X00xx and ZERO per-Position rows for Tualatin.
       d. offices count = 7 for the Tualatin chamber; representing_city='Tualatin' on all 7; titles are 'Mayor' + 'Council Member (Position 1..6)'.
       e. Section-split scan returns 0 rows for geo_id 4174950.
       f. All 7 politicians have non-NULL office_id.
       g. ALL 7 have is_appointed=false + is_appointed_position=false; Pratt (-4174957) has exactly ONE office row.
       h. Independent geofence check: essentials.geofence_boundaries WHERE geo_id='4174950' AND mtfcc='G4110' returns ≥1 (and 4175200 returns 0 — the correction held).
    3. Commit the migration in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/1169_tualatin_city_council.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied; "Post-verification PASSED" NOTICE emitted
    - Independent E2E gate a–h all pass
    - Zero ward/X00xx and zero per-Position districts for Tualatin; exactly 7 offices
    - All 7 uniform is_appointed=false; Pratt single office row; numbered Position titles present
    - Geofence assertion confirms 4174950 (G4110) present and 4175200 absent
    - Migration committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with the gate results, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Structural INSERTs applied to production Supabase via psql |
| migration file → schema | Idempotency + WR-01-fixed post-verify gates gate the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-05 | Spoofing | Seeding against the wrong geo_id 4175200 → phantom government no address routes to | mitigate | Migration uses only 4174950; WR-01 independent geofence-presence assertion (geo_id='4174950' AND mtfcc='G4110' ≥1) fails the transaction if the geofence is absent; E2E gate 2h re-confirms |
| T-179-06 | Tampering | Duplicate/partial re-seed on prod | mitigate | Pre-flight hard-abort DO block + WHERE NOT EXISTS on gov/chamber/districts + office guard on (district_id, politician_id); single transaction rolls back on any post-verify failure |
| T-179-07 | Tampering | Dead post-verify gate lets a real section-split ship silently | mitigate | WR-01 fix: independent geofence-presence assertion + canonical GROUP BY/HAVING section-split query, not the same-transaction "create-then-check-absence" gate |
| T-179-08 | Repudiation | Untracked structural change | mitigate | Migration registered in schema_migrations ledger; committed to EV-Accounts git |
| T-179-09 | Elevation of Privilege | Ward/position districts or an appointed-seat mislabel misrepresent the roster | mitigate | Pure at-large enforced (2 citywide districts, numbered titles are labels not districts); uniform is_appointed=false; E2E gate 2c/2d/2g assert zero ward rows + exactly 7 offices + no appointed seats |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- In-migration WR-01-fixed post-verify DO block passes; independent E2E gate a–h passes; migration committed.
</verification>

<success_criteria>
- 1 government + 1 chamber (official_count=7) + 2 citywide districts + 7 offices (numbered Position
  titles, no appointed seats), all with representing_city='Tualatin', office_id back-filled,
  section-split=0, geofence 4174950 confirmed present, no ward geofences.
</success_criteria>

<output>
Create `.planning/phases/179-city-of-tualatin-deep-seed/179-02-SUMMARY.md` when done, recording the
minted politician UUIDs (by external_id) for plans 03 (headshots) and 04 (stances).
</output>
