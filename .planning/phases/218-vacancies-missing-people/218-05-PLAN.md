---
phase: 218-vacancies-missing-people
plan: 05
type: execute
wave: 5
depends_on: ["218-04"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_verify-collin-218-completeness.ts
autonomous: false
requirements: [COLLIN-PEOPLE-01, COLLIN-PEOPLE-02]
must_haves:
  truths:
    - "Across ALL 23 resolving Collin County governments, offices WHERE politician_id IS NULL AND is_vacant IS NOT TRUE returns 0 rows — per COLLIN-PEOPLE-02 + ROADMAP success criterion 3"
    - "Across the 11 target cities, offices WHERE politician_id IS NULL AND is_vacant IS NOT TRUE returns 0 rows — per COLLIN-PEOPLE-01"
    - "The other 12 non-target govs have office-row counts matching their real seat counts (D-02 spot-check) — no undiscovered missing-seat gap"
    - "Split-section check is clean across all 23 govs"
    - "Newly-seated names render on live browse for >=3 target cities"
  artifacts:
    - "C:/EV-Accounts/backend/scripts/_verify-collin-218-completeness.ts"
  key_links:
    - "Verification joins offices->chambers->governments over the 23 Collin geo_ids; browse spot-check hits /results?browse_government_list=<geo_id>&browse_state=TX"
---

<objective>
Prove the phase is complete: no office across all 23 resolving Collin County governments is left in an ambiguous empty state, the D-02 missing-seat check has been run against the OTHER 12 non-target govs (not just the 3 RESEARCH already fixed), the split-section check is clean, and newly-seated people actually render on live browse. This is the phase gate for COLLIN-PEOPLE-01, COLLIN-PEOPLE-02, and ROADMAP success criteria 3 and 4.

Purpose: Close the phase against the VALIDATION.md sign-off contract with an auditable, re-runnable verification.
Output: A verification script running the full SQL gate battery + a human-verified live browse spot-check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-vacancies-missing-people/218-CONTEXT.md
@.planning/phases/218-vacancies-missing-people/218-RESEARCH.md
@.planning/phases/218-vacancies-missing-people/218-VALIDATION.md
@.planning/phases/218-vacancies-missing-people/218-01-SUMMARY.md
@.planning/phases/218-vacancies-missing-people/218-02-SUMMARY.md
@.planning/phases/218-vacancies-missing-people/218-03-SUMMARY.md
@.planning/phases/218-vacancies-missing-people/218-04-SUMMARY.md
@C:/EV-Accounts/backend/scripts/_apply-migration-1213.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full SQL verification battery across all 23 govs</name>
  <files>C:/EV-Accounts/backend/scripts/_verify-collin-218-completeness.ts</files>
  <action>
    Write a re-runnable verification script (read-only, mirroring the pg-Pool/DATABASE_URL structure of _apply-migration-1213.ts — no schema writes) that runs and asserts, printing PASS/FAIL and exiting non-zero on any failure:
    (1) COLLIN-PEOPLE-02 gate: across all 23 Collin geo_ids, `SELECT COUNT(*) FROM essentials.offices o JOIN chambers ch ON ch.id=o.chamber_id JOIN governments g ON g.id=ch.government_id WHERE g.geo_id IN (<23 FIPS>) AND o.politician_id IS NULL AND o.is_vacant IS NOT TRUE` = 0.
    (2) COLLIN-PEOPLE-01 gate: same query scoped to the 11 target city geo_ids = 0.
    (3) D-02 missing-seat spot-check for the OTHER 12 non-target govs (Allen, Celina, Frisco, Farmersville, Lavon, McKinney, Melissa, Murphy, Prosper, Richardson, Saint Paul, and Weston if not counted): compare each chamber's `official_count` against `COUNT(*)` of its office rows and flag any gov whose office-row count is LOWER than official_count (RESEARCH Open Question 3). For any flagged gov, cross-check against its live roster page and record the finding — if a genuine missing-seat gap is found, STOP and report it (it needs its own follow-up before phase close).
    (4) Split-section gate ([[section_split_check]]) = 0 rows across all 23.
    (5) No-duplicate gate = 0 rows.
    (6) Full reconcile: re-run the offices/seated/unseated count per city and confirm it matches the RESEARCH target table (21 target seated-or-documented + 6 missing rows created).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && npx tsx scripts/_verify-collin-218-completeness.ts</automated>
  </verify>
  <done>All 6 gate groups PASS; the 12 other govs' seat-count spot-check is clean (or any gap is explicitly reported for follow-up); 0 ambiguous empty seats across all 23; split-section + no-duplicate clean; reconcile matches RESEARCH target table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live browse spot-check of newly-seated names + vacancy flags</name>
  <action>Human-verify checkpoint — the operator loads live browse for the spot-check cities below and confirms newly-seated names, vacancy flags, and headshots render correctly. No code is written in this task; it gates phase close on real rendered output.</action>
  <what-built>Newly-seated Collin County officials (Plans 02/03) linked to their offices, with headshots (Plan 04) where a source existed.</what-built>
  <how-to-verify>
    1. Open the live app at essentials.empowered.vote and hit `/results?browse_government_list=<geo_id>&browse_state=TX` for at least 3 target cities — recommended: Parker (4855152), Princeton (4859576), and Van Alstyne (4874924) — plus one missing-seat city, Lowry Crossing (4844308).
    2. Confirm the newly-seated names from the Plan 02/03 SUMMARYs appear on their offices (e.g. Parker Mayor Lee Pettle; Princeton Place 4 Jaisen Rutledge; Van Alstyne Mayor Jim Atchison).
    3. Confirm no office renders as an ambiguous blank seat, and any documented vacancy (Plan 03) shows as a flagged vacancy rather than an empty row.
    4. Spot-check that headshots display for at least a couple of the sourced people and that the honest-blank cities (Blue Ridge/Lowry Crossing/Nevada) show a blank rather than a wrong/placeholder face.
  </how-to-verify>
  <resume-signal>Type "approved" if the newly-seated names + vacancy flags render correctly on all spot-checked cities, or describe what's wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB state -> resident-facing browse | Seeded rows must render correctly and completely to residents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-11 | Repudiation | undiscovered ambiguous empty seat | high | mitigate | All-23 SQL gate asserts 0 ambiguous seats; D-02 spot-check extends to the 12 non-target govs |
| T-218-12 | Tampering | split-section defect from seeding | medium | mitigate | Split-section gate asserts 0 across all 23 govs before phase close |
</threat_model>

<verification>
- 0 ambiguous empty seats across 23 govs; 12-other-gov D-02 spot-check clean; split-section + no-duplicate clean; live browse renders newly-seated names for >=3 cities (VALIDATION.md full sign-off).
</verification>

<success_criteria>
`politician_id IS NULL AND is_vacant IS NOT TRUE` returns 0 rows across all 23 Collin govs; the 6 missing rows exist; the other-12 seat-count spot-check surfaces no undiscovered gap; split-section clean; >=3 target cities show newly-seated names on live browse. VALIDATION.md sign-off checklist fully satisfiable.
</success_criteria>

<output>
Create `.planning/phases/218-vacancies-missing-people/218-05-SUMMARY.md` when done — record the final gate outputs, the 12-other-gov spot-check result, and the browse spot-check confirmation. Flip VALIDATION.md nyquist_compliant to true once gates pass.
</output>
