---
phase: 106-va-compass-stances
plan: 06
type: execute
wave: 6
depends_on:
  - 106-05
files_modified:
  - C:/EV-Accounts/backend/migrations/331_gaskins_stances.sql
  - C:/EV-Accounts/backend/migrations/332_aguirre_stances.sql
  - C:/EV-Accounts/backend/migrations/333_bagley_stances.sql
  - C:/EV-Accounts/backend/migrations/334_chapman_stances.sql
  - C:/EV-Accounts/backend/migrations/335_elnoubi_stances.sql
  - C:/EV-Accounts/backend/migrations/336_greene_stances.sql
  - C:/EV-Accounts/backend/migrations/337_marks_stances.sql
autonomous: true
requirements:
  - VA-STANCES-03
must_haves:
  truths:
    - "Each Alexandria council member with public-record evidence has a stance migration applied"
    - "Members with no public record found within the 5-minute sliding cap (per D-03) have NO migration row and are documented as 'no public record' in the summary"
    - "Every applied migration has 100% citation rate (uncited = 0)"
    - "Every applied migration has paired answers/context rows (unpaired = 0)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/331_gaskins_stances.sql"
      provides: "Stance migration for Mayor Gaskins (if evidence found)"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "33{1..7}_*_stances.sql"
      to: "essentials.politicians (external_id BETWEEN -5101000007 AND -5101000001)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research each Alexandria city council member's public record on all currently-active compass topics, applying the 5-minute sliding cap per D-03/D-04. Write per-individual stance migrations 331-337 ONLY for members with evidence found within cap. Apply each migration immediately after research per D-06.

Purpose: VA-STANCES-03 partial closure (Alexandria council half). Best-effort scope — declaring "no public record" for low-profile members is acceptable per CONTEXT.md and ROADMAP success criterion #3.

Output: 0-7 migration files applied (depends on what public record exists for each member); summary documents which members had no record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/106-va-compass-stances/106-CONTEXT.md
@.planning/phases/106-va-compass-stances/106-PATTERNS.md
@.planning/phases/106-va-compass-stances/106-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve all 7 council UUIDs + roster confirmation</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-03, D-04, D-08, D-10)
    - C:/EV-Accounts/backend/migrations/312_alexandria_government.sql (lines 109-320 — confirms roster + external_ids)
  </read_first>
  <action>
    Run via mcp__supabase-local__execute_sql:
      SELECT external_id, full_name, id
      FROM essentials.politicians
      WHERE external_id BETWEEN -5101000007 AND -5101000001
      ORDER BY external_id DESC;

    Expected 7 rows in this exact mapping (confirmed from migration 312 ground truth, NOT from PATTERNS.md which has Bagley/Aguirre swapped):
      -5101000001  Alyia Gaskins (Mayor)         -> migration 331
      -5101000002  Canek Aguirre                  -> migration 332
      -5101000003  Sarah Bagley (Vice Mayor)     -> migration 333
      -5101000004  John Chapman                   -> migration 334
      -5101000005  Abdel-Rahman Elnoubi           -> migration 335
      -5101000006  Jacinta E. Greene              -> migration 336
      -5101000007  Sandy Marks                    -> migration 337

    If counts differ, abort. Save the (external_id, name, uuid) mapping as `.planning/phases/106-va-compass-stances/_tmp-alexandria-roster.json`.
  </action>
  <verify>
    <automated>
      Via mcp__supabase-local__execute_sql:
        SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5101000007 AND -5101000001;
      Must return 7.
      Then:
        Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-alexandria-roster.json"
    </automated>
  </verify>
  <done>
    - 7 council UUIDs resolved and saved to _tmp roster JSON
    - external_id -> name -> migration_number mapping confirmed against migration 312
  </done>
</task>

<task type="auto">
  <name>Task 2: For each of 7 council members SEQUENTIALLY (per D-08), research within 5-minute cap and apply migration if evidence found</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-03 sliding cap rule, D-04, D-08, D-10)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md ("Full Example" template)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251)
    - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql (recent VA per-individual analog)
    - .planning/phases/106-va-compass-stances/_tmp-alexandria-roster.json
  </read_first>
  <action>
    Process the 7 council members IN ORDER (Gaskins -> Aguirre -> Bagley -> Chapman -> Elnoubi -> Greene -> Marks). ONE at a time per D-08 — NEVER launch parallel research agents.

    For each member, follow this sub-protocol exactly:

    SUB-STEP A — Start a 5-minute timer (record start timestamp). Launch ONE web-research subagent (or perform research yourself) on this single member. Search:
      - alexandriava.gov city council member page + voting records
      - alxnow.com news coverage
      - 2024/2026 campaign sites if any
      - Vote tracking via city council meeting minutes (when available)
      Topics likely to have local evidence (per ACPS/council scope):
        - housing, residential-zoning, rent-regulation, growth-and-development (Alexandria zoning debates)
        - homelessness, homelessness-response
        - local-environment, climate-change, transportation-priorities (King Street, Metro WMATA)
        - public-safety-approach, judicial-police-accountability (Alexandria PD oversight)
        - economic-development, taxes (city budget votes)
        - local-immigration, immigration (sanctuary city resolutions)
        - school-vouchers, childcare (city/ACPS coordination votes)
        - jail-capacity (city/county jail policies)
        - city-sanitation, civil-rights
      Federal-only topics (tariffs, ukraine-support, social-security, ai-regulation, medicare/aid): likely no local record — OMIT.

    SUB-STEP B — Cap rule per D-03: if no evidence on ANY topic surfaces in 5 minutes, declare "no public record" for this member; SKIP migration creation; document in summary. If a finding comes in during the last 3 minutes of the cap, extend by 3 more minutes; re-apply cap rule.

    SUB-STEP C — If evidence found (>=1 topic with documented stance): write migration file `C:/EV-Accounts/backend/migrations/{N}_{lastname-lowercase}_stances.sql` per the mapping in roster JSON. Migration file MUST follow the 326 pattern exactly:
      - Header: "Migration {N}: {Full Name} Stances" / "{Title} of Alexandria"
      - Topic UUID reference block (copy from 282 lines 14-57)
      - BEGIN; section header; INSERT pairs; COMMIT;
      - Verification comment block at end with member's UUID
      - 100% citation per D-10 — every INSERT INTO politician_context has >=1 URL in ARRAY[]
      - No defaults — only topics with evidence

    SUB-STEP D — Immediately apply migration per D-06: read full SQL file and pass to mcp__supabase-local__execute_sql. Then verify via:
      SELECT
        (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{uuid}') AS answer_count,
        (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = '{uuid}' AND pc.politician_id IS NULL) AS unpaired,
        (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = '{uuid}' AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)) AS uncited;
      Required for applied migrations: answer_count >= 1, unpaired = 0, uncited = 0.

    SUB-STEP E — Move to next member. Do NOT batch — D-06 forbids it.

    After all 7 processed, save outcome log to `.planning/phases/106-va-compass-stances/_tmp-alexandria-outcomes.json`:
      [{"external_id": -5101000001, "name": "Alyia Gaskins", "outcome": "applied|no_record", "migration": 331, "stance_count": N, "topics_with_evidence": ["housing", ...]}, ...]
  </action>
  <verify>
    <automated>
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-alexandria-outcomes.json"
      # All applied migrations must satisfy: paired and cited
      # Via mcp__supabase-local__execute_sql:
      #   SELECT COUNT(*) FROM (
      #     SELECT p.external_id, COUNT(pa.*) AS n_ans,
      #            COUNT(pa.*) FILTER (WHERE pc.politician_id IS NULL) AS unpaired,
      #            COUNT(pc.*) FILTER (WHERE sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0) AS uncited
      #     FROM essentials.politicians p
      #     LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
      #     LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
      #     WHERE p.external_id BETWEEN -5101000007 AND -5101000001
      #     GROUP BY p.external_id
      #     HAVING COUNT(pa.*) > 0
      #   ) t WHERE unpaired > 0 OR uncited > 0;
      # Must return 0 — zero defective applied migrations.
    </automated>
  </verify>
  <done>
    - All 7 council members processed sequentially (timestamped log proves no parallelism)
    - For each member: either migration applied with paired+cited stances OR "no public record" documented
    - Outcome log saved to _tmp file for Task 3 cleanup
    - Zero defective migrations (no unpaired, no uncited)
  </done>
</task>

<task type="auto">
  <name>Task 3: Cleanup + summary preparation</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/_tmp-alexandria-outcomes.json
  </read_first>
  <action>
    Step A: Run final phase-progress query via mcp__supabase-local__execute_sql:
      SELECT p.external_id, p.full_name, COUNT(pa.*) AS stance_count
      FROM essentials.politicians p
      LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
      WHERE p.external_id BETWEEN -5101000007 AND -5101000001
      GROUP BY p.external_id, p.full_name
      ORDER BY p.external_id DESC;
    Record this 7-row result for the SUMMARY.

    Step B: Read `_tmp-alexandria-outcomes.json` and prepare summary content listing:
      - Members with migrations applied (name, migration number, stance count)
      - Members with "no public record" (name, reason: 5-min cap exhausted with no findings)

    Step C: Delete `_tmp-alexandria-roster.json` and `_tmp-alexandria-outcomes.json`.
  </action>
  <verify>
    <automated>
      Not Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-alexandria-roster.json"
      Not Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-alexandria-outcomes.json"
    </automated>
  </verify>
  <done>
    - Temp files deleted
    - Summary content ready including per-member outcomes
  </done>
</task>

</tasks>

<verification>
- Per-member stance counts queried and recorded
- All applied migrations are paired + cited (zero defective)
- D-03 sliding cap honored (timestamps in research log)
- D-08 one-at-a-time honored (sequential processing log)
- D-06 immediate-apply honored (no batched applies after research)
</verification>

<success_criteria>
- Alexandria council portion of VA-STANCES-03 advanced
- ROADMAP Phase 106 success criterion #3 honored — "best-effort, skip with no public record = acceptable"
- All applied migrations satisfy quality gates
</success_criteria>

<output>
Create `.planning/phases/106-va-compass-stances/106-06-SUMMARY.md` documenting:
- 7-row per-member result table (external_id, name, stance_count)
- Members with migrations applied and topic count
- Members with "no public record" outcome (and confirmation cap was honored)
- Any deviations from D-01..D-10
</output>
