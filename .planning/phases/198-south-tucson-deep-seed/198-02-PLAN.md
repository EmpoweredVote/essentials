---
phase: 198-south-tucson-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["198-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-south-tucson-headshots.py
  - C:/EV-Accounts/backend/migrations/1364_city_of_south_tucson_headshots.sql
autonomous: true
requirements: [SUB-04]
must_haves:
  truths:
    - "All 7 officials have a 600×750 (4:5, Lanczos q90) headshot uploaded to the politician_photos Storage bucket (type='default', press_use)"
    - "Each of the 7 politician_images rows binds to the CURRENT roster member seeded in Plan 01 (the execute-time-confirmed roster)"
    - "Because southtucsonaz.gov is Cloudflare-JS-challenge-blocked (HTTP 403 'Just a moment...' managed challenge to curl/WebFetch — a different vendor than Sahuarita's CivicPlus soft-block or prior AZ phases' Akamai), source portraits are retrieved via the /find-headshots Playwright flow (a real browser context that solves/waits out the Cloudflare challenge), also trying the /directory staff page alongside /citycouncil, with Ballotpedia/Wikimedia/news as fallback; photo_license is documented PER image (not a uniform value); honest blank (no placeholder) if genuinely none found"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-south-tucson-headshots.py"
      provides: "crop_to_4_5 → resize 600×750 Lanczos q90 → Storage x-upsert pipeline for 7 officials; source URLs resolved via Playwright (Cloudflare-challenge fallback)"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1364_city_of_south_tucson_headshots.sql"
      provides: "up to 7 audit-only politician_images rows (unregistered), UUIDs from Plan 01 manifest, per-image photo_license"
      contains: "politician_images"
  key_links:
    - from: "_tmp-south-tucson-headshots.py"
      to: "southtucsonaz.gov (Playwright) / Ballotpedia / Wikimedia"
      via: "browser-rendered image bytes (byte-count > 0 verified), then requests/PIL pipeline"
      pattern: "crop_to_4_5"
    - from: "1364_city_of_south_tucson_headshots.sql"
      to: "essentials.politicians"
      via: "url binds to politician UUID resolved by external_id"
      pattern: "politician_images"
---

<objective>
Acquire and bind headshots for the South Tucson council. Author a gitignored pipeline script that processes
each official's portrait, crops to 4:5 (crop-first, never distort), resizes to 600×750 Lanczos q90, and
uploads to the `politician_photos` Storage bucket; plus an AUDIT-ONLY migration that inserts the
`politician_images` rows for the officials whose portraits were found.

CRITICAL (Pitfall 4 — a DIFFERENT block signature than every prior AZ phase): the official host
southtucsonaz.gov is Cloudflare-JS-challenge-protected. A direct `curl`/WebFetch (even with a Chrome
User-Agent) of both `/citycouncil` and `/elections/page/city-council-election-information` returns HTTP 403
with a genuine Cloudflare "Just a moment..." managed-JS-challenge page (JS-required, cookie-required —
confirmed via the raw response body this session; `cZone: 'www.southtucsonaz.gov'`, "Enable JavaScript and
cookies to continue"). This is a DIFFERENT vendor than prior AZ phases' Akamai hard-403 and Sahuarita's
CivicPlus HTTP-200-empty-body soft-block. Per D-06: attempt the direct southtucsonaz.gov fetch FIRST, but a
"Just a moment..." body (not real HTML/image bytes) is the failure signature — verify byte-count > 0 AND
that PIL can open any downloaded image. The `/find-headshots` skill's Playwright browser-navigation flow (a
real browser context that solves/waits out a Cloudflare managed challenge) is the EXPECTED effective path;
also try the `/directory` staff-directory page alongside `/citycouncil`. Ballotpedia/Wikimedia/news are the
secondary fallback. NOTE (D-06): South Tucson is a very small city — the official site may lack usable
portraits, so the Ballotpedia/Wikimedia fallback is more likely exercised than in prior phases; an honest
blank (no placeholder) is acceptable for any official with genuinely no usable portrait. Do NOT craft
evasive headers to defeat the Cloudflare challenge. `photo_license` is documented PER image (varies by
source), NOT a uniform value.

Purpose: Satisfies SUB-04 / ROADMAP success criterion #2 (all seated officials have 600×750 headshots).
Output: `_tmp-south-tucson-headshots.py` (gitignored `backend/scripts/_*`) +
`1364_city_of_south_tucson_headshots.sql` (audit-only, unregistered), both applied so the sourced images
serve from the CDN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md
@.planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md
@.planning/phases/198-south-tucson-deep-seed/198-01-SUMMARY.md

<execution_note>
Depends on Plan 01: the 7 politician UUIDs (external_id → UUID → full_name) captured in 198-01-SUMMARY.md
are REQUIRED — the audit migration's CDN url values derive from the politician UUID
({uuid}-headshot.jpg), and the script resolves UUIDs at runtime via `SELECT id FROM essentials.politicians
WHERE external_id=%s`. Executor authors both files; the ORCHESTRATOR resolves the source image URLs
(direct southtucsonaz.gov first with a byte-count check, Playwright Cloudflare-challenge fallback trying
`/citycouncil` + `/directory`, then Ballotpedia/Wikimedia/news), runs the script, and applies the audit
migration (executor has no DB/Storage/web access). Commit the migration via `git -C "C:/EV-Accounts"`; the
`_tmp-*` script is gitignored and never committed. If the Task 2 roster changed any seat, source the
CONFIRMED sitting member — not the research-date name.
</execution_note>

<interfaces>
- Source hosts: southtucsonaz.gov FIRST (Cloudflare-JS-challenge-blocked — HTTP 403 "Just a moment..." to
  curl/WebFetch) → retrieve via the `/find-headshots` Playwright flow (real browser context solves the
  challenge); try `/citycouncil` AND `/directory` staff pages; VERIFY byte-count > 0 and PIL-openable.
  Ballotpedia candidate pages + Wikimedia are the secondary fallback (note: Ballotpedia returned
  thin/JS-rendered content to a direct curl this session — use Playwright for it too). Do NOT spoof headers.
- essentials.politician_images columns: exactly (id, politician_id, url, type, photo_license) — NO
  photo_origin_url. type='default'; photo_license PER image (municipal government portrait / press_use, or
  Wikimedia/CC terms, or Ballotpedia press_use, or operator_supplied) — NOT a uniform value.
- Storage: bucket politician_photos; canonical path {politician_uuid}-headshot.jpg; x-upsert PUT; CDN base
  https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
- ext_id block: -4015001 Valenzuela (Mayor), -4015002 Brown-Dominguez (Vice Mayor), -4015003 Robles (Acting
  Mayor), -4015004 Jimenez, -4015005 Diaz, -4015006 Flagg, -4015007 Aguirre (subject to the Task 2 confirmed
  roster). Honest blank allowed for any official with no usable portrait.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the South Tucson headshot pipeline script (Cloudflare-challenge Playwright fallback + byte-count guard)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-south-tucson-headshots.py</files>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md (§ headshot analog `1364_city_of_south_tucson_headshots.sql` mapping + the shared-pipeline note: reuse crop_to_4_5/resize_600x750/process_headshot_bytes/upload_to_storage/resolve_politician_id/dry_run_head_check/HEADERS verbatim from the Sahuarita/Marana analog; ONLY ROSTER + source-acquisition change; southtucsonaz.gov-first with a byte-count check then Playwright; mixed per-image license; honest blank allowed)
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Common Pitfall 4 — southtucsonaz.gov Cloudflare "Just a moment..." managed challenge; verify byte-count > 0; Playwright fallback (try /citycouncil + /directory); per-image license discipline; small-city → Ballotpedia/Wikimedia fallback more likely; honest blank)
    - C:/EV-Accounts/backend/scripts/_tmp-sahuarita-headshots.py (PRIMARY analog — read in full: reuse crop_to_4_5, resize_600x750, process_headshot_bytes, upload_to_storage, resolve_politician_id, env-load, dry_run_head_check, descriptive-UA HEADERS verbatim; only ROSTER + source-URL builder + count assertions change)
  </read_first>
  <action>
    Copy `_tmp-sahuarita-headshots.py` and adapt. Reuse crop_to_4_5 / resize_600x750 / process_headshot_bytes
    (crop-FIRST then resize) / upload_to_storage (x-upsert PUT to `politician_photos/{uuid}-headshot.jpg`) /
    resolve_politician_id (parameterized `external_id=%s`, never hardcode a UUID) / the env-load block /
    dry_run_head_check / the descriptive User-Agent HEADERS — VERBATIM. Keep TARGET_SIZE=(600,750),
    JPEG_QUALITY=90, RESAMPLE=LANCZOS, MIN_DIM=100.

    Build a ROSTER list of 7 dicts (assert len(ROSTER)==7), each with: external_id (-4015001..-4015007),
    full_name, seat/title label, photo_url (initially the `_MISSING`/`TODO_` sentinel — the ORCHESTRATOR
    fills the resolved image URL per member in Task 3), photo_license (PER member — set from the actual
    source at fill time, NOT a uniform value), is_required (True where a portrait is expected; may be set
    False for an official with a confirmed honest-blank — see D-06). Add `source_url_for(member)` returning
    `member['photo_url']`.

    KEEP the byte-count guard for the Cloudflare-challenge signature: after any direct fetch, assert the
    response body length > 0 AND that the bytes are a real image (PIL-openable, MIN_DIM) BEFORE treating it
    as a success — a "Just a moment..." HTML body or a 0-byte 200 is NOT a real image. Keep the
    `_MISSING = 'TODO_...'` sentinel + `main()` fail-fast guard so a placeholder URL can NEVER reach a
    Storage write — if any REQUIRED ROSTER photo_url is still the sentinel, print the member and
    `sys.exit(1)`; a member explicitly marked is_required=False (honest blank) is skipped, not failed. Do a
    dry-run HEAD/GET per required member first (verify 200 AND non-zero real-image bytes) before the real
    batch. On any required-member fetch/upload/empty-body failure, print the member + `sys.exit(1)`. PIL
    `convert('RGB')` + JPEG re-encode optimize=True (strips EXIF/embedded payloads). Print a {external_id,
    uuid, cdn_url, photo_license} manifest for the audit migration.

    Add a comment block documenting that southtucsonaz.gov is Cloudflare-JS-challenge-blocked (HTTP 403
    "Just a moment...") to non-browser clients, so source URLs are retrieved via the /find-headshots
    Playwright flow (trying /citycouncil + /directory) with a byte-count > 0 real-image check, and
    Ballotpedia/Wikimedia as fallback; honest blank permitted for a small-city official with no usable
    portrait (do NOT add header-spoofing logic).
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-south-tucson-headshots.py"; test -f "$F" && grep -qF "crop_to_4_5" "$F" && grep -qF "external_id=%s" "$F" && grep -qE "len\(ROSTER\)\s*==\s*7" "$F" && grep -qF "sys.exit(1)" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/scripts/_tmp-south-tucson-headshots.py` (gitignored `backend/scripts/_*`).
    - Contains crop_to_4_5, resize_600x750, upload_to_storage; TARGET_SIZE (600, 750); LANCZOS; JPEG quality 90; MIN_DIM 100.
    - ROSTER has exactly 7 members (`assert len(ROSTER)==7`), ext_ids -4015001..-4015007.
    - politician_id resolved via parameterized `external_id=%s` (no f-string/`%`-format into SQL); PIL `convert('RGB')` present.
    - `_MISSING`/`TODO_` sentinel + fail-fast `sys.exit(1)` guard present so no placeholder URL reaches Storage; dry-run 200-AND-non-zero-real-image check present; byte-count > 0 / PIL-openable guard for the Cloudflare challenge; is_required flag present (honest blank permitted).
    - photo_license is a PER-member field (not a single hardcoded uniform value).
    - No WAF/Cloudflare-challenge header-spoofing logic.
  </acceptance_criteria>
  <done>Pipeline script authored: 7-member ROSTER, crop-first 600×750 pipeline, runtime UUID resolution, sentinel + dry-run + byte-count/real-image + required-failure guards, per-image license, honest-blank support.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1364_city_of_south_tucson_headshots.sql</files>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md (§ `1364_city_of_south_tucson_headshots.sql` mapping — the audit-only politician_images pattern from the Sahuarita 1355 analog: header, per-row INSERT ... WHERE NOT EXISTS, exact 5 columns, NO ledger registration, per-image mixed license, final `COMMIT;` only)
    - .planning/phases/198-south-tucson-deep-seed/198-01-SUMMARY.md (the 7 external_id → UUID → full_name manifest)
    - C:/EV-Accounts/backend/migrations/1355_town_of_sahuarita_headshots.sql (analog — read the header + first few rows: audit-only, unregistered, WHERE NOT EXISTS idempotency, 5-column shape, mixed per-image license)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1363 after Plan 01); name this file disk-MAX+1 (provisionally
    `1364_city_of_south_tucson_headshots.sql`). Author an AUDIT-ONLY migration (NO ledger-registration INSERT
    into supabase_migrations, applied via `psql -f`). The header may note it is audit-only / unregistered,
    that the source is southtucsonaz.gov (Cloudflare-challenge-blocked) retrieved via Playwright with
    Ballotpedia/Wikimedia fallback (Pitfall 4), and that photo_license VARIES per image.

    Emit one per-row INSERT into essentials.politician_images (id, politician_id, url, type, photo_license)
    for each official who HAS a sourced portrait — columns EXACTLY these five, no photo_origin_url. Each row:
    id=gen_random_uuid(); politician_id = `(SELECT id FROM essentials.politicians WHERE external_id =
    -4015001)` (etc. through -4015007); url = the CDN path `{CDN_BASE}/{politician_uuid}-headshot.jpg` using
    the UUID from the Plan 01 manifest; type='default'; photo_license = the PER-IMAGE license value (from the
    Task 1 sourcing manifest). Guard each with WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images
    WHERE politician_id = (...same subselect...)) — idempotent. Include a row for each of the ext_ids
    -4015001..-4015007 that was successfully sourced; omit (honest blank) any official with genuinely no
    usable portrait, documenting the omission in a header comment. Do NOT add a ledger-registration INSERT.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1364_city_of_south_tucson_headshots.sql"; test -f "$F" && [ "$(grep -c 'INSERT INTO essentials.politician_images' "$F")" -ge 1 ] && ! grep -qF "photo_origin_url" "$F" && ! grep -qE "INSERT INTO supabase_migrations" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File exists (disk-MAX+1 name, provisionally 1364).
    - Grep count of `INSERT INTO essentials.politician_images` ≥ 1 (one per sourced official; up to 7); any honest-blank official is documented in a header comment.
    - Columns are exactly (id, politician_id, url, type, photo_license) — NO `photo_origin_url`.
    - NO `INSERT INTO supabase_migrations` line (audit-only, unregistered).
    - Every row guarded by `WHERE NOT EXISTS` on politician_id (idempotent).
    - photo_license set per row (may differ across officials).
  </acceptance_criteria>
  <done>Audit-only migration authored with one idempotent politician_images row per sourced official bound to Plan 01 UUIDs, per-image license, honest blanks documented, unregistered.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Resolve source URLs (southtucsonaz.gov-first + Playwright Cloudflare-challenge fallback) + run pipeline + apply audit migration + assert (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator resolves URLs, runs an existing script, applies an existing migration; no files modified</files>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-VALIDATION.md (Per-Task Verification Map — headshots row: count per sourced official + PIL 600×750 + curl CDN 200; the byte-count/real-image check specific to the Cloudflare challenge)
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Common Pitfall 4 — Cloudflare "Just a moment..."; Playwright fallback (try /citycouncil + /directory); verify byte-count/real-image; MIN_DIM floor; per-image license; small-city honest-blank)
    - C:/Users/Chris/.claude/commands/find-headshots.md (the Playwright browser-navigation sourcing flow)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/Storage/web access). BLOCKING apply-and-assert: Plan 02 is not
    complete (and Plan 03 should not proceed) until every sourceable official's image serves from the CDN.
    First, for each of the 7 CONFIRMED sitting officials (Task 2 roster), resolve a usable direct image URL:
    try the southtucsonaz.gov `/citycouncil` and `/directory` pages FIRST via the `/find-headshots`
    Playwright flow (`mcp__playwright__browser_navigate` + snapshot + image-URL extraction) — a real browser
    context solves/waits out the Cloudflare "Just a moment..." challenge that blocks curl/WebFetch — and
    verify the retrieved bytes are a non-zero real image (a "Just a moment..." HTML body or 0-byte 200 is the
    block, not an image); fall back to Ballotpedia/Wikimedia/news (also via Playwright). Follow the project
    headshot rules (600×750 4:5, Lanczos, crop-only, eyes ~1/3 from top, no graphics on faces, no
    distortion). For any official with genuinely no usable portrait, record an honest blank (no placeholder)
    and mark is_required=False rather than seeding a placeholder. Do NOT spoof headers. Record the resolved
    URL + license per official, fill them into the ROSTER `photo_url` / `photo_license` fields, and finalize
    the matching photo_license values in migration 1364.

    Then run `py -3 C:/EV-Accounts/backend/scripts/_tmp-south-tucson-headshots.py` — it
    crops/resizes/uploads the sourced images to Storage and prints the {external_id, uuid, cdn_url,
    photo_license} manifest. If any REQUIRED member cannot be sourced (script exits 1), either source an
    operator-supplied file (photo_license='operator_supplied') or, if truly none exists, mark it an honest
    blank. Then apply the audit migration via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1364_city_of_south_tucson_headshots.sql`.
    Assert: politician_images count for the sourced ext_ids matches the number of officials with a portrait
    (up to 7; honest blanks excluded); `curl -sI` each CDN url → HTTP 200 AND non-zero Content-Length; PIL
    verify a sample (include Mayor Valenzuela if sourced and one council member) is exactly 600×750. Record
    results + final manifest (incl. per-image license and any honest blanks) in 198-02-SUMMARY.md. Commit the
    audit migration via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (-4015001,-4015002,-4015003,-4015004,-4015005,-4015006,-4015007)"</automated>
  </verify>
  <acceptance_criteria>
    - Each official's source URL resolved via Playwright (southtucsonaz.gov /citycouncil + /directory first with a byte-count/real-image check; Ballotpedia/Wikimedia secondary; no header-spoofing) OR recorded as an honest blank; per-image license recorded.
    - Script run exits 0 (all required images fetched with non-zero real-image bytes, processed, uploaded; honest blanks skipped) — or an operator file was supplied for any exception.
    - `politician_images` count for the South Tucson ext_ids equals the number of officials with a sourced portrait (up to 7; honest blanks excluded, documented in the SUMMARY).
    - `curl -sI` on each seeded CDN URL returns HTTP 200 with a non-zero Content-Length.
    - PIL dimension check on a sampled downloaded CDN image (incl. Valenzuela if sourced + one council member) = exactly 600×750.
    - Migration 1364's photo_license values match the actual sourced licenses; audit migration NOT registered in the ledger; committed to `C:/EV-Accounts`.
  </acceptance_criteria>
  <done>Every sourceable official serves a 600×750 CDN headshot bound to the correct current politician row, sourced southtucsonaz.gov-first (Playwright Cloudflare-challenge fallback) with per-image licenses; honest blanks documented; audit migration applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| image hosts (remote) → pipeline | Untrusted remote image bytes enter the pipeline over HTTP / via a Playwright browser context |
| pipeline → Storage/DB | Processed image + politician_images row cross into production |

## STRIDE Threat Register

Honest scope: an image-processing pipeline + audit-only INSERTs authored by the operator (data-seeding
phase, no runtime/request surface). Genuine threats are payload-in-image, wrong-person binding, and a
Cloudflare challenge page masquerading as an image — not injection or access control. No HIGH-severity
threat; none fabricated. Mirrors the Sahuarita 197 threat model.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-IMG | Tampering | remote image fetch | mitigate | PIL `convert('RGB')` + JPEG re-encode `optimize=True` strips EXIF/embedded/stego payloads; only `image/jpeg` written to Storage |
| T-198-CF | Tampering | Cloudflare "Just a moment..." page / 0-byte 200 masquerading as a valid image | mitigate | Byte-count > 0 + PIL-openable + MIN_DIM guard before any Storage write; Playwright browser context retrieves real bytes when the direct endpoint is challenge-blocked; grep gate + dry-run check |
| T-198-BIND | Spoofing | politician_images ↔ politician binding | mitigate | Fetch ONLY the Task-2-confirmed current-roster URLs; runtime UUID resolution by external_id prevents wrong-person binding on retry |
| T-198-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; script itself gitignored (`backend/scripts/_*`); never in a committed migration; parameterized `external_id=%s` query |
| T-198-LIC | Repudiation | image license provenance | mitigate | photo_license recorded PER image (municipal portrait/press_use, Wikimedia/CC terms, Ballotpedia press_use, operator_supplied); documented in the migration header + SUMMARY manifest |
| T-198-WRONG | Spoofing | wrong-but-present image | transfer | Row-count cannot detect a wrong-but-present photo; deferred to a human identity spot-check at phase verification |
| T-198-SC | Tampering | package installs | accept | No new pip installs — reuses installed Pillow/requests/psycopg2 + already-available Playwright MCP; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- One politician_images row per sourced South Tucson official (up to 7), each 600×750 at HTTP 200 with non-zero bytes; honest blanks documented.
- Every image binds to the correct current official (Task 2 roster).
- Source URLs southtucsonaz.gov-first via Playwright (Cloudflare-challenge fallback, try /citycouncil + /directory) with a byte-count/real-image guard; per-image license recorded; audit migration NOT registered.
</verification>

<success_criteria>
SUB-04 / ROADMAP #2 (all seated officials have 600×750 headshots) is TRUE in production for every official
with a usable portrait; every image binds to the correct current member; honest blanks are documented; the
audit migration is applied and left unregistered.
</success_criteria>

<output>
Create `.planning/phases/198-south-tucson-deep-seed/198-02-SUMMARY.md` when done — include the final
{external_id → UUID → cdn_url → photo_license} manifest, which officials required the Playwright
Cloudflare-challenge fallback, any honest blanks, and any operator-supplied exceptions.
</output>
