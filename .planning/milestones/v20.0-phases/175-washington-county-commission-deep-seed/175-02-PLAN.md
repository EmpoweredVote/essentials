---
phase: 175-washington-county-commission-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["175-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1121_washco_commission_headshots.sql
  - C:/EV-Accounts/backend/scripts/_tmp-washco-headshots.py
autonomous: true
requirements: [WASH-01]

must_haves:
  truths:
    - "All seated Washington County commissioners render with 600×750 headshots (genuine gaps documented, no fabrication)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1121_washco_commission_headshots.sql"
      provides: "Audit-only migration inserting one politician_images row per seated commissioner (id, politician_id, url, type, photo_license) — NO photo_origin_url column"
      contains: "us_government_work"
  key_links:
    - from: "1121_washco_commission_headshots.sql"
      to: "essentials.politicians external_id -410100/-410110..-410113 (minted by 1120)"
      via: "INSERT ... SELECT (SELECT id FROM essentials.politicians WHERE external_id = -41011N)"
      pattern: "external_id = -4101"
    - from: "politician_images.url"
      to: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      via: "mirrored 600x750 JPEG"
      pattern: "politician_photos/"
---

<objective>
Source, process, and mirror headshots for the 5 seated Washington County commissioners (Chair Harrington + D1-D4), then author the audit-only migration that records each image in essentials.politician_images. Photos come from washingtoncountyor.gov / media-production.washcotech.net; cropped to 4:5 then resized to 600×750 (Lanczos), mirrored to Supabase Storage. Genuine gaps are documented, never fabricated (D-08).

Purpose: Satisfies the headshot half of ROADMAP success criterion #2 — every seated official renders with a 600×750 portrait.
Output: One headshot pipeline script (executor authors) and one audit-only migration 1121 (executor authors). The INLINE ORCHESTRATOR runs the pipeline (fetch + crop + resize + Storage upload — executor cannot reach the network/Storage credentials reliably here, and has NO DB) and applies 1121 via psql.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Executor / Orchestrator split
- Executor AUTHORS the Python pipeline script and the `.sql` migration (with `{uuid}` placeholders for the Storage paths).
- Inline orchestrator runs the pipeline (curl -L the source images, 4:5 crop FIRST then 600×750 Lanczos q90, upload to Storage `politician_photos/{uuid}-headshot.jpg`), fills the real per-politician Storage UUIDs into 1121, then applies it via `psql "$DATABASE_URL" -f .../1121_washco_commission_headshots.sql`. Audit-only — it does NOT register in the ledger.
- The 5 politician UUIDs come from Plan 01's SUMMARY (minted by migration 1120).
</execution_context>

<context>
@.planning/phases/175-washington-county-commission-deep-seed/175-PATTERNS.md
@.planning/phases/175-washington-county-commission-deep-seed/175-RESEARCH.md
@.planning/phases/175-washington-county-commission-deep-seed/175-01-SUMMARY.md

<critical_facts>
- `essentials.politician_images` columns are EXACTLY `(id, politician_id, url, type, photo_license)` — there is NO photo_origin_url column (including it errors). Do NOT put that token anywhere in the file, even in comments (grep gates).
- Headshot source: washingtoncountyor.gov/elections/county-officials → media-production.washcotech.net CDN. Try `curl -L` with a descriptive User-Agent first. Fallback chain: inline img src on the .gov page → Ballotpedia → official press/media kit. No WAF detected, but escalate per-person on 403.
- Processing order is non-negotiable: crop to 4:5 ratio FIRST, THEN resize to 600×750 Lanczos q90 (never stretch directly). RGBA → white-composite if a source PNG has alpha.
- `type='default'`, `photo_license='us_government_work'` (Oregon county government portraits).
- Storage URL form: `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg`.
- Migration file = `1121_washco_commission_headshots.sql` (next after structural 1120). Audit-only — NO ledger INSERT.
- Photos must have no superimposed text/graphics over the face (reject "Re-Elect" banners etc.).
</critical_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot processing pipeline script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-washco-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-henderson-council-headshots.py (PRIMARY ANALOG — fetch + 4:5-crop-first + 600×750 Lanczos resize + Storage upload pipeline from the v18.0 deep-seeds)
    - .planning/phases/175-washington-county-commission-deep-seed/175-RESEARCH.md (SECTION D — source domain, URL pattern, fallback chain, processing rules)
    - .planning/phases/175-washington-county-commission-deep-seed/175-01-SUMMARY.md (the 5 minted politician UUIDs)
  </read_first>
  <action>
    Clone the Henderson headshot pipeline. Configure 5 entries keyed by commissioner (Harrington/Fai/Treece/Snider/Willey) with their source URLs from washingtoncountyor.gov/elections/county-officials (media-production.washcotech.net CDN) and their politician UUIDs from Plan 01's SUMMARY. For each: curl -L with a descriptive User-Agent; if RGBA, composite onto white; crop to 4:5 aspect FIRST (eyes ~1/3 from top, full head + shoulders, no aspect-ratio change); resize to 600×750 Lanczos q90 JPEG; upload to Supabase Storage path politician_photos/{uuid}-headshot.jpg. On 403/missing source, log a documented gap (do NOT fabricate) and continue the others. Emit a final summary listing each commissioner's source URL, final dimensions, and Storage path (these feed the migration). Keep tokens slug / photo_origin_url out of the file entirely.
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts/backend" && python -c "import ast; ast.parse(open('scripts/_tmp-washco-headshots.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Python (ast.parse OK).
    - File references all 5 commissioners and the `politician_photos/` Storage prefix.
    - File performs 4:5 crop BEFORE resize to 600×750 (crop call precedes resize call).
    - File does NOT contain the token `photo_origin_url`.
  </acceptance_criteria>
  <done>Pipeline authored covering 5 commissioners; crop-first→600×750; Storage upload; documented-gap handling on source failure.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshots migration 1121</name>
  <files>C:/EV-Accounts/backend/migrations/1121_washco_commission_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/245_multnomah_county_headshots.sql (PRIMARY ANALOG — exact politician_images INSERT shape; no ledger registration; per-official WHERE NOT EXISTS guard)
    - .planning/phases/175-washington-county-commission-deep-seed/175-PATTERNS.md (the 1119/1121 headshots section)
    - .planning/phases/175-washington-county-commission-deep-seed/175-01-SUMMARY.md (politician UUIDs / external_ids)
  </read_first>
  <action>
    Author `1121_washco_commission_headshots.sql` as an AUDIT-ONLY migration (header notes it is not registered in the ledger). For each seated commissioner with a successfully mirrored photo, INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) SELECT gen_random_uuid(), (SELECT id FROM essentials.politicians WHERE external_id = -41011N), '<Storage URL with the real {uuid}-headshot.jpg from the pipeline summary>', 'default', 'us_government_work' WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -41011N)). Use external_id -410100 (Chair), -410110 (D1), -410111 (D2), -410112 (D3), -410113 (D4). The inline orchestrator fills the real Storage UUIDs after running the pipeline. ONLY the 5 listed columns — never add the non-existent origin-url column. If a commissioner is a documented gap, omit that INSERT and note the gap in a comment WITHOUT using the forbidden tokens. Do NOT add a ledger INSERT (audit-only).
  </action>
  <verify>
    <automated>grep -c "us_government_work" "C:/EV-Accounts/backend/migrations/1121_washco_commission_headshots.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File `1121_washco_commission_headshots.sql` exists.
    - INSERT column list is exactly `(id, politician_id, url, type, photo_license)` — file does NOT contain the token `photo_origin_url`.
    - File contains `us_government_work` and `politician_photos/` for each inserted commissioner.
    - File references external_ids in the -410100/-410110..-410113 block.
    - File contains NO `INSERT INTO supabase_migrations` (audit-only) — `grep -v '^--' file | grep -c "supabase_migrations"` returns 0.
  </acceptance_criteria>
  <done>Audit-only migration authored; one politician_images row per seated commissioner with a mirrored photo; correct 5-column shape; documented gaps for any missing source; no ledger registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| washingtoncountyor.gov / washcotech CDN → pipeline | External image bytes fetched into local processing then mirrored to Storage |
| pipeline → Supabase Storage | Authenticated write of processed JPEGs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-H1 | Spoofing | Wrong-person headshot mirrored | mitigate | Source from official county officials page (identity-bound); human-verify checkpoint in Plan 03 / phase gate confirms identity before WASH-01 close |
| T-175-H2 | Tampering | Image with superimposed text/graphics over face | mitigate | Pipeline rejects banner/overlay images; visual spot-check during phase verification |
| T-175-SC | Tampering | npm/pip/cargo installs | accept | No new packages — Pillow already present (RESEARCH Environment Availability) |

No HIGH-severity threats: read-only image fetch + authenticated Storage write; no user input, no SQL injection surface (politician_id resolved by trusted external_id constants).
</threat_model>

<verification>
Inline-orchestrator gates after the pipeline runs and 1121 is applied:
1. Headshot count: `SELECT COUNT(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -410113 AND -410100;` → equals (5 minus documented gaps).
2. Schema shape: every inserted row has type='default' and photo_license='us_government_work'.
3. Storage reachability: each url returns HTTP 200 (curl -I).
4. Documented gaps (if any) recorded in the SUMMARY with the reason no source was available.
</verification>

<success_criteria>
- Pipeline authored and run; seated commissioners' photos cropped 4:5 → 600×750 and mirrored to Storage.
- 1121 authored and applied (audit-only, not in ledger); politician_images rows present for every commissioner with a real source.
- Genuine gaps documented, no fabrication.
- Satisfies the headshot half of ROADMAP success criterion #2.
</success_criteria>

<output>
Create `.planning/phases/175-washington-county-commission-deep-seed/175-02-SUMMARY.md` when done. Record per-commissioner source URL, final dimensions, Storage path, any documented gaps, and the headshot-count gate output.
</output>
