---
phase: 222-collin-county-stances-evidence-only-compass-research-one-pol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
  - "[essentials] .planning/REQUIREMENTS.md"
autonomous: false
requirements: [COLLIN-STANCE-01, COLLIN-STANCE-02]
must_haves:
  truths:
    - "The in-scope worklist is derived LIVE from production at execution; CONTEXT.md's 157/55/102 discussion-time anchor is used only as a cross-check and is never hardcoded into any acceptance criterion (D-01)"
    - "The BEFORE snapshot (total in-scope / with >=1 stance / without) is recorded as real numbers before any write occurs anywhere in the phase"
    - "The worklist query uses (p.is_vacant = false OR p.is_vacant IS NULL) — never `p.is_vacant = false` alone, the exact filter bug that hid 7 officeholders in the Phase 221 audit"
    - "Every name on the live worklist is assigned to exactly one downstream plan (222-02 through 222-17); the assignment table is exhaustive and non-overlapping (COLLIN-STANCE-01/empty)"
    - "Wave order follows D-02 evidence-first: the 7 higher-evidence cities first (222-02..222-07), then every remaining city's mayor (222-08, 222-09), then the remaining council members by evidence tier (222-10..222-17) (COLLIN-STANCE-01/ordering)"
    - "No two stance-research plans share a wave — research is strictly sequential, one politician at a time (D-03) (COLLIN-STANCE-01/concurrency)"
    - "An interruption at any point loses at most one plan's un-applied work, because each plan owns one migration file, commits per government, and ends in its own blocking apply checkpoint (COLLIN-STANCE-01/concurrency)"
    - "The set of politician/topic pairs already answered by the pre-existing already-stanced officeholders is recorded in the worklist so no downstream plan overwrites them; this phase writes only pairs with no prior row (COLLIN-STANCE-01/adjacency)"
    - "Re-running the worklist derivation produces the same worklist and writes nothing — it is a read-only, repeatable derivation (COLLIN-STANCE-01/idempotency)"
    - "The 11 canonical topic UUIDs from RESEARCH.md section B are verified to resolve live in inform.compass_topics before any research pass is dispatched (COLLIN-STANCE-02/adjacency)"
    - "A person with no explicit on-topic evidence yields zero inform.politician_answers rows and exactly one blank-register entry — never a partial row, never a NULL-valued row (COLLIN-STANCE-02/empty)"
    - "Where two sources support different chairs for the same politician and topic, the migration cites the source actually used and its reasoning names that source's date; an unresolvable conflict yields a blank, not an arbitrary pick (COLLIN-STANCE-02/ordering)"
    - "Every write in this phase is idempotent via ON CONFLICT (politician_id, topic_id) DO UPDATE on both inform.politician_answers and inform.politician_context (COLLIN-STANCE-02/idempotency)"
    - "Every migration in this phase is a single BEGIN/COMMIT transaction, so an interrupted apply can never leave an answer row without its matching context row (COLLIN-STANCE-02/concurrency)"
    - "REQUIREMENTS.md carries real COLLIN-STANCE-01 and COLLIN-STANCE-02 entries and both appear in the Traceability table mapped to Phase 222; the stale `Compass stances for Collin County` deferral bullet is superseded in place with a dated note"
  artifacts:
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md"
      provides: "Live worklist, BEFORE snapshot, and the exhaustive per-plan city/person assignment"
      contains: "BEFORE snapshot"
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
      provides: "Blank-register skeleton (D-08) that every research plan appends to"
      contains: "Search method"
    - path: ".planning/REQUIREMENTS.md"
      provides: "COLLIN-STANCE-01/02 promoted from the stale deferral into tracked requirements + traceability"
      contains: "COLLIN-STANCE-01"
  key_links:
    - "222-01-WORKLIST.md rows carry politician_id + full_name + government + geo_id + title, so every downstream plan attaches stances to the correct officeholder (join on politician_id — there is no unique geo_id)"
    - "The per-plan assignment table in 222-01-WORKLIST.md is what plans 222-02..222-17 read to know their exact scope; 222-18 reconciles against it"
  prohibitions:
    - "MUST NOT infer or assign any stance from party affiliation, endorsement, donor history, or pattern-matching to another official in the same city — only an explicit, on-topic, cited statement, recorded vote, or questionnaire answer may set a chair"
    - "MUST NOT assign a chair when the evidence is suggestive-but-not-explicit, and MUST NOT default to a middle value to avoid leaving a blank — a blank spoke is the correct, successful outcome"
    - "MUST NOT record a source URL that was not actually fetched and read, or that does not contain the cited quote, vote, or questionnaire answer"
    - "MUST NOT attribute a stance to an officeholder on the basis of a same-named person in a different city or state; the source must confirm the Texas city before the evidence is used"
    - "MUST NOT name, imply, or rely on a politician's party in reasoning text — party is never used for inference and never displayed"
---

<objective>
Establish the phase's factual ground truth before a single stance is researched: derive the Collin County un-stanced worklist LIVE from production, record the BEFORE coverage snapshot, verify the 11 topic UUIDs still resolve, find the live next-available migration number, assign every worklist name to exactly one downstream plan, create the blank register (D-08), and promote the stale REQUIREMENTS.md deferral into real tracked requirements.

Purpose: CONTEXT.md's 157 / 55 / 102 numbers are discussion-time estimates and the researcher had no live DB access. Rosters shift between sessions (Phase 221 discovered its own scope memo was wrong by 7 people). Every downstream plan reads its scope from this plan's output, so this plan's accuracy is the phase's accuracy.
Output: `222-01-WORKLIST.md`, the `222-CONFIRMED-BLANK.md` skeleton, and updated `.planning/REQUIREMENTS.md`. Zero stance rows are written by this plan.
</objective>

## Artifacts this phase produces

Phase 222 (all plans) produces data + planning artifacts only. No application code, no schema change, no npm/pip/cargo packages.

- **[EV-Accounts repo]** `C:/EV-Accounts/backend/migrations/<N>_222_<slug>_stances.sql` — one numbered, git-committed, **AUDIT-ONLY** (NOT registered in `schema_migrations`) migration per research plan, up to 16 files. Numbers are assigned LIVE at execution (highest on-disk prefix was 1415 at research time and moves between sessions). Each writes only `inform.politician_answers` (value) + `inform.politician_context` (reasoning, sources).
- **[essentials repo]** `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — live worklist, BEFORE snapshot, per-plan assignment table (this plan).
- **[essentials repo]** `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the per-person blank register (D-08), created here and appended per government by every research plan.
- **[essentials repo]** `.planning/REQUIREMENTS.md` — new `### Compass stances (COLLIN-STANCE)` section carrying **COLLIN-STANCE-01** and **COLLIN-STANCE-02**, the stale deferral bullet superseded, and both IDs added to `## Traceability` mapped to Phase 222 (this plan).
- **[essentials repo]** `src/lib/coverage.js` — `hasContext: true` added to any Texas entry that moves from zero to at least one stance (close-out plan 222-18 only).
- **[essentials repo]** `.planning/phases/222-.../222-NN-SUMMARY.md` — one per plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md
</context>

## Multi-Source Coverage Audit

| SOURCE | ID | Item | Plan | Status |
|--------|----|------|------|--------|
| GOAL | — | Each researched Collin officeholder carries evidence-cited compass stances where documented, blank spokes where not | 02–17 (research), 18 (verify) | COVERED |
| REQ | COLLIN-STANCE-01 | Per-politician, all-topic, evidence-only research | 01 (worklist), 02–17 (research), 18 (reconcile) | COVERED |
| REQ | COLLIN-STANCE-02 | Apply 1–5 stances, blank where unsourced | 02–17 (author+apply), 18 (gates) | COVERED |
| RESEARCH | §A | Live worklist re-derivation + `is_vacant IS NULL` fix + BEFORE snapshot | 01 | COVERED |
| RESEARCH | §B | Verbatim 11-topic question/answer text; override stale agent text for housing/taxes/healthcare | 01 (verify UUIDs), 02–17 (`read_first`) | COVERED |
| RESEARCH | §C | Per-city evidence tier map drives wave order | 01 (assignment), 02–17 (wave order) | COVERED |
| RESEARCH | §D | One migration per plan, commit per government, resumable batching | 02–17 | COVERED |
| RESEARCH | §E | `politician_answers` + `politician_context` pair, ON CONFLICT upsert, AUDIT-ONLY numbering, operator applies | 02–17 | COVERED |
| RESEARCH | §F | BEFORE/AFTER delta, split-section check, browse spot-check | 01 (BEFORE), 18 (AFTER + checks) | COVERED |
| RESEARCH | Pitfall 1 | Stale agent scale text for housing/taxes/healthcare | 02–17 (`read_first` + action) | COVERED |
| RESEARCH | Pitfall 2 | Ballotpedia homonym trap for small Collin towns | 02–17 (prohibition P4 + action) | COVERED |
| RESEARCH | Pitfall 3 | Frisco Place 4 — attach to whoever the live worklist returns | 01 (explicit check), 03 | COVERED |
| RESEARCH | Pitfall 4 | `is_vacant IS NULL` filter | 01 | COVERED |
| RESEARCH | Pitfall 5 | `coverage.js` `hasContext` reconcile | 18 | COVERED |
| RESEARCH | Open Q1 | Which 55/102 definition was used | 01 (run both variants) | COVERED |
| RESEARCH | Open Q2 | Longview status | 01 (determine), 07 (conditional) | COVERED |
| CONTEXT | D-01 | Attempt all un-stanced officeholders, comprehensive | 01 + 02–17 | COVERED |
| CONTEXT | D-02 | Evidence-first ordering; 7 high-evidence cities + every city's mayor first | 01 (assignment), 02–09 | COVERED |
| CONTEXT | D-03 | One politician at a time, sequential, no worktree | Every research plan; one plan per wave | COVERED |
| CONTEXT | D-04 | Strict evidence bar; suggestive → blank | 02–17 (prohibitions P1/P2) | COVERED |
| CONTEXT | D-05 | Acceptable source types; each stance carries evidence | 02–17 | COVERED |
| CONTEXT | D-06 | The 8 canonical Local Lens topics | 01 (UUID verify + REQUIREMENTS supersede), 02–17 | COVERED |
| CONTEXT | D-07 | Attempt the 3 legacy-tail topics where evidence exists; leave existing records as-is | 02–17 | COVERED |
| CONTEXT | D-08 | Per-person blank register mirroring 221 | 01 (create), 02–17 (append), 18 (reconcile) | COVERED |

Excluded (not gaps): CONTEXT.md Deferred Ideas — standardizing the existing already-stanced 55, school-board stances (blocked until a board badge exists), non-Collin backfill.

<tasks>

<task type="auto">
  <name>Task 1: Derive the live worklist, BEFORE snapshot, topic-UUID verification, and next migration number</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section "Research Question A" for the exact runnable worklist SQL, section "Research Question B" for the 11 topic UUIDs, section "Research Question E" for the migration-numbering convention, and Pitfalls 3 and 4.
    - `src/lib/coverage.js` (essentials repo) — the Texas block, the authoritative 24-entry geo_id set for the Collin browse scope.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — read the post-close addendum: the `is_vacant = false` undercount that hid 7 officeholders.
  </read_first>
  <files>(read-only: production SQL via mcp__supabase-local__execute_sql, plus a directory listing of C:/EV-Accounts/backend/migrations — no writes this task)</files>
  <action>
    Run RESEARCH.md section A Step 1 verbatim via mcp__supabase-local__execute_sql: the `collin_scope` CTE over `essentials.politicians p` joined to `essentials.offices o`, `essentials.chambers ch`, `essentials.governments g`, filtered on `p.is_active = true AND (p.is_vacant = false OR p.is_vacant IS NULL)` and `g.geo_id IN` the 24 Texas geo_ids from coverage.js (4801924 Allen, 4803300 Anna, 4808872 Blue Ridge, 4813684 Celina, 4825224 Fairview, 4825488 Farmersville, 4827684 Frisco, 4838068 Josephine, 4841800 Lavon, 4843888 Longview, 4844308 Lowry Crossing, 4845012 Lucas, 4845744 McKinney, 4847496 Melissa, 4850100 Murphy, 4850760 Nevada, 4855152 Parker, 4858016 Plano, 4859576 Princeton, 4859696 Prosper, 4861796 Richardson, 4864220 Saint Paul, 4874924 Van Alstyne, 4877740 Weston), LEFT JOINed to `inform.politician_answers` for a per-person `stance_count`. Do NOT use `p.is_vacant = false` alone — NULL means seated and that exact filter hid 7 officeholders in the Phase 221 audit (RESEARCH.md Pitfall 4).

    Then run Step 2 twice to settle RESEARCH.md Open Question 1: once counting `stance_count` over ANY `inform.politician_answers` row, and once restricted to the 11 canonical topic UUIDs listed in RESEARCH.md section B. Record BOTH totals (total in-scope, with at least one stance, without any) and note which variant matches CONTEXT.md's 55/102 anchor. Treat 102 as an estimate to be superseded, never as a target.

    Also capture, for every in-scope person who already has at least one stance, the set of `(politician_id, topic_id)` pairs they already hold — this is the do-not-overwrite set that keeps this phase off the already-stanced records (D-07 says leave existing records as-is).

    Verify the 11 topic UUIDs resolve live: select `id, topic_key, short_title, is_live` from `inform.compass_topics` for the 8 Local Lens IDs (669cac97-66a6-4087-b036-936fbe62efb3, 0bc588c6-39e1-4084-b5de-cac909b8b762, 4938766b-b45a-46e3-93bd-b8b30651271a, eb3d1247-0de1-4b7f-baec-7259861efd53, b9ccee94-ad96-4f10-b655-889d8e5abe92, e9ebefcd-c496-45e8-b816-a79f8442ba85, d4f18138-a2e0-4110-b925-7387d9d0d16d, ba59337e-30e2-4aba-a39a-426b3366eb27) and the 3 legacy-tail IDs (f7e5678d-dadd-4556-a2fc-446e24642ceb taxes, fb25c1ac-91cc-49bf-8afc-c7fa22ef45e4 growth-and-development, e8dad4a8-eb93-4931-91f5-d8fb5d7dd529 healthcare). Confirm none resolves to `homelessness-response` (6fbf39ae-6b19-4182-b4c2-6a8d25c86c0f), which is a distinct non-Local-Lens topic. Spot-check the live question text of `housing`, `taxes`, and `healthcare` against RESEARCH.md section B and report any divergence as a blocking finding — those three are exactly the topics whose wording the researcher agent file has stale (Pitfall 1).

    Run the two named live cross-checks: (a) Frisco Place 4 — confirm exactly one active politician holds that seat and record which politician_id the worklist returns; if Gopal Ponangi (d6e0d762-f7a2-4566-8718-452e4c33781b) appears active, flag it as a live-data regression rather than proceeding. (b) Longview (geo_id 4843888, Gregg County) — record its un-stanced count; it carries `hasContext: true` and is expected to be zero.

    Finally, list `C:/EV-Accounts/backend/migrations` and record the highest numeric filename prefix present RIGHT NOW plus the next available number. Do not carry 1415 or 1416 forward from RESEARCH.md — that counter is shared with concurrent milestone work and moves between sessions.

    If `mcp__supabase-local__execute_sql` is not bound in this session, STOP and emit the exact SQL to the operator as a blocking request. Do not substitute estimates, and do not proceed to Task 2 on CONTEXT.md's discussion-time numbers.
  </action>
  <verify>
    <automated>The MCP session returns, in one pass: the per-person worklist rows (politician_id, full_name, title, representing_city, geo_id, government_name, stance_count); both BEFORE count variants; the 11-row topic-UUID resolution set; the Frisco Place 4 row; the Longview un-stanced count. A directory listing of C:/EV-Accounts/backend/migrations returns the current highest numeric prefix.</automated>
  </verify>
  <acceptance_criteria>
    - The worklist query text contains `is_vacant IS NULL` (the OR-NULL form), and the returned total in-scope count is reported as a live number.
    - BOTH BEFORE variants are recorded as numbers: any-topic and 11-topic-restricted (total / with-stance / without-stance for each).
    - All 11 topic UUIDs resolve in `inform.compass_topics` with `is_live = true`; any divergence between the live question text for housing, taxes, or healthcare and RESEARCH.md section B is reported explicitly (or explicitly "none").
    - Exactly one active politician is returned for Frisco Place 4, and that politician_id is recorded.
    - Longview's un-stanced count is recorded as a number.
    - The live next-available migration number is recorded, sourced from a directory listing performed this session — not from RESEARCH.md.
    - The already-answered `(politician_id, topic_id)` pair set for in-scope people is captured.
  </acceptance_criteria>
  <done>Live ground truth exists: worklist rows, both BEFORE variants, verified topic UUIDs, the Frisco Place 4 resolution, the Longview determination, the do-not-overwrite pair set, and the live next migration number. Nothing has been written.</done>
</task>

<task type="auto">
  <name>Task 2: Write 222-01-WORKLIST.md, create the blank register, and promote the REQUIREMENTS.md deferral</name>
  <read_first>
    - The Task 1 query output (worklist rows, both BEFORE variants, topic verification, migration number).
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the exact register format to mirror: a per-government heading, a one-line `Evidence:` note naming the sources checked, then one bullet per person with name, office, and politician_id.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-01-WORKLIST.md` — the worklist document shape to mirror.
    - `.planning/REQUIREMENTS.md` (essentials repo) — the file being modified: the `## Future Requirements (deferred)` block and the `## Traceability` table.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md` — D-06 (why the deferral note is stale) and D-08 (register contract).
  </read_first>
  <files>.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md, .planning/REQUIREMENTS.md</files>
  <action>
    Write `222-01-WORKLIST.md` in the essentials repo containing: (1) the BEFORE snapshot with both count variants and an explicit line stating that these supersede CONTEXT.md's 157/55/102 discussion-time anchor; (2) the live next-available migration number and the date it was checked; (3) the topic-UUID verification result including the housing/taxes/healthcare wording check; (4) the Frisco Place 4 and Longview determinations; (5) one row per un-stanced officeholder with politician_id, full_name, title, government_name, geo_id; (6) the do-not-overwrite table of already-answered politician/topic pairs; and (7) the per-plan assignment table below, populated with the actual names from the live worklist.

    Assignment table (D-02 evidence-first ordering — the 7 higher-evidence cities first, then every remaining city's mayor, then remaining council members by evidence tier): 222-02 Plano (4858016); 222-03 Frisco (4827684); 222-04 McKinney (4845744); 222-05 Allen (4801924); 222-06 Richardson (4861796); 222-07 Prosper (4859696) + Celina (4813684) + Longview (4843888) conditional; 222-08 mayors only of Anna, Murphy, Fairview, Princeton, Melissa, Farmersville, Parker, Lucas; 222-09 mayors only of Weston, Blue Ridge, Josephine, Lavon, Lowry Crossing, Nevada, Saint Paul, Van Alstyne; 222-10 Anna + Murphy councils; 222-11 Fairview + Princeton councils; 222-12 Melissa + Farmersville councils; 222-13 Parker + Lucas councils; 222-14 Weston + Blue Ridge councils; 222-15 Josephine + Lavon councils; 222-16 Lowry Crossing + Nevada councils; 222-17 Saint Paul + Van Alstyne councils. "Councils" means every un-stanced officeholder of that government EXCEPT the mayor already covered in 222-08/222-09.

    Then assert exhaustiveness in the document: state the worklist row count and confirm the assignment table's row count equals it, with zero names unassigned and zero names assigned twice. If any live worklist name does not fit an assignment row (for example a government not in the 24-entry set, or a Longview count above 3), list it under a clearly-labelled `UNASSIGNED — needs operator decision` heading rather than dropping it or quietly folding it into a neighbouring plan.

    Create `222-CONFIRMED-BLANK.md` as a skeleton mirroring the 221 register: a title, a paragraph stating that everyone listed was genuinely searched and that blank is the correct terminal state (D-08, and blank is a SUCCESS state, never a defaulted stance), a `Search method` paragraph naming the D-05 acceptable source types (candidate questionnaires from Ballotpedia and VOTE411, public statements and news coverage, council votes and meeting minutes, official government bios, campaign and official sites), and an explicit completeness contract line: every name on the 222-01 worklist must end the phase in exactly one of {an applied migration row, an entry in this register} — never neither, never both. Leave a running `Count:` line the research plans update. Do not pre-populate any person.

    Then edit `.planning/REQUIREMENTS.md` with scoped edits only (never a whole-file rewrite): add a new `### Compass stances (COLLIN-STANCE)` section after the `### Vacancies / missing people (COLLIN-PEOPLE)` section containing an unchecked `**COLLIN-STANCE-01**` entry (every in-scope un-stanced Collin officeholder is individually researched across all applicable compass topics, evidence-only — a chair is assigned only where a cited source shows an explicit on-topic position, and every attempted person with no sourced position is recorded in the phase blank register) and an unchecked `**COLLIN-STANCE-02**` entry (every applied stance is a whole integer 1-5 on one of the 11 canonical topic IDs and carries a matching `inform.politician_context` row with reasoning citing a specific dated action, quote, or vote plus at least one real source URL). Supersede the stale `**Compass stances for Collin County**` bullet under `## Future Requirements (deferred)` in place: keep the bullet text but prefix it with a dated SUPERSEDED note stating the Local Lens question set is locked in `inform.compass_lenses` and already in use by the existing already-stanced Collin officeholders, so the deferral no longer applies (D-06), and point to the new COLLIN-STANCE section. Finally add two rows to the `## Traceability` table — `COLLIN-STANCE-01 | Phase 222 | Pending` and `COLLIN-STANCE-02 | Phase 222 | Pending` — and update the "All 12 v25.0 requirements" sentence above the table to the new total.
  </action>
  <verify>
    <automated>test -f .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md && test -f .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md && grep -q "COLLIN-STANCE-01" .planning/REQUIREMENTS.md && grep -q "COLLIN-STANCE-02" .planning/REQUIREMENTS.md && grep -c "COLLIN-STANCE" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `222-01-WORKLIST.md` exists and contains a BEFORE snapshot with both count variants as numbers, the live migration number with its check date, and the topic-UUID verification result.
    - Every live worklist row appears with politician_id, full_name, title, government_name, and geo_id.
    - The per-plan assignment table lists plans 222-02 through 222-17, and the document states the worklist row count and asserts the assignment count matches it with no name unassigned and no name duplicated.
    - `222-CONFIRMED-BLANK.md` exists with the search-method paragraph and the exactly-one-bucket completeness-contract line, and no person pre-populated.
    - `.planning/REQUIREMENTS.md` contains a `### Compass stances (COLLIN-STANCE)` heading with both COLLIN-STANCE-01 and COLLIN-STANCE-02 entries.
    - The `## Traceability` table contains a `COLLIN-STANCE-01` row and a `COLLIN-STANCE-02` row, both mapped to Phase 222.
    - The previously-deferred Collin compass-stances bullet carries a dated SUPERSEDED note referencing D-06; it is not silently deleted.
    - `.planning/REQUIREMENTS.md` was changed with scoped edits — the COLLIN-BROWSE, COLLIN-ELECT, COLLIN-CONTACT, and COLLIN-PEOPLE sections and their traceability rows are byte-identical to before.
  </acceptance_criteria>
  <done>The worklist, the blank-register skeleton, and the promoted requirements exist and are internally consistent; downstream plans have an unambiguous scope to read.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator sign-off on the live scope before any research begins</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the document under review.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md` — D-01, D-02, D-03 to confirm the assignment honours them.
  </read_first>
  <what-built>
    A live-derived worklist of every un-stanced in-scope Collin County officeholder, a BEFORE coverage snapshot in two count variants, verified topic UUIDs, the live next-available migration number, an exhaustive per-plan assignment table across plans 222-02..222-17, an empty blank register, and promoted COLLIN-STANCE-01/02 requirements with traceability rows.
  </what-built>
  <action>
    Halt here and hand control to the operator. Present the how-to-verify steps below verbatim and wait for the resume signal. Do NOT begin any stance research until the operator has approved the live scope — every downstream plan reads its worklist from this document, so an unapproved or incomplete assignment table would propagate into all 16 research plans. Record the operator's answers, including any correction to the housing, taxes, or healthcare topic wording, in this plan's SUMMARY.
  </action>
  <how-to-verify>
    1. Open `222-01-WORKLIST.md`. Confirm the BEFORE snapshot is live numbers dated today, not CONTEXT.md's 157/55/102.
    2. Confirm the worklist total and the assignment-table total match, and that there is no `UNASSIGNED — needs operator decision` section (if there is one, resolve it before approving — do not approve a plan set with unassigned people).
    3. Confirm the Longview determination. If Longview shows more than 3 un-stanced officeholders, say so — Longview then needs its own plan rather than riding along in 222-07.
    4. Confirm the Frisco Place 4 resolution names a single active politician and that it is not Gopal Ponangi.
    5. Confirm the housing / taxes / healthcare live question text matches RESEARCH.md section B. If it does not, the research plans must be given the live text instead — flag it here.
    6. Open `.planning/REQUIREMENTS.md` and confirm both COLLIN-STANCE rows are in the Traceability table and the old deferral bullet is marked superseded rather than deleted.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the BEFORE snapshot is live data recorded today.
    - Operator confirms assignment exhaustiveness (worklist count equals assignment count; nothing unassigned; nothing duplicated).
    - Operator confirms the Longview and Frisco Place 4 determinations.
    - Operator confirms the housing/taxes/healthcare wording check result and, if divergent, supplies the live text for the research plans.
    - Operator confirms the REQUIREMENTS.md promotion.
  </acceptance_criteria>
  <resume-signal>Type "approved" to unlock wave 2 (222-02 Plano), or describe what must change first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale planning anchor → live scope | Discussion-time counts (157/55/102) could misdirect the entire phase if trusted over the live database |
| planner-supplied SQL → production DB | Read-only this plan, but the same MCP path later carries writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-01 | Information Disclosure | worklist derivation | high | mitigate | Derive live with `(is_vacant = false OR is_vacant IS NULL)`; record both count variants; blocking operator sign-off before any research starts |
| T-222-02 | Tampering | REQUIREMENTS.md edit | medium | mitigate | Scoped `Edit` only; unrelated requirement sections must be byte-identical after the change; the stale bullet is superseded in place, never deleted |
| T-222-03 | Repudiation | read-only audit | low | accept | Task 1 is SELECT-only via MCP; nothing to reconcile |
</threat_model>

<verification>
- Worklist derived live with the OR-NULL vacancy filter; both BEFORE variants recorded as numbers.
- All 11 topic UUIDs resolve live; housing/taxes/healthcare wording checked against RESEARCH.md section B.
- Assignment table is exhaustive and non-overlapping against the live worklist.
- `222-CONFIRMED-BLANK.md` exists with the exactly-one-bucket completeness contract.
- REQUIREMENTS.md carries COLLIN-STANCE-01/02 plus traceability rows; the stale deferral is superseded in place.
- Operator has approved before wave 2 begins.
</verification>

<success_criteria>
Phase 222 has a live, operator-approved scope: every un-stanced in-scope Collin officeholder is named, assigned to exactly one downstream plan, and measurable against a real BEFORE snapshot — with tracked requirements to trace against and an empty blank register ready to receive honest blanks. Zero stance rows written.
</success_criteria>

<output>
Create `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-SUMMARY.md` when done — record both BEFORE variants, the live next migration number, the topic verification result, the Longview and Frisco Place 4 determinations, and the per-plan assignment counts.
</output>
