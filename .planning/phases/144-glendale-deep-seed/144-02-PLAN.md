---
phase: 144-glendale-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["144-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/903_glendale_complete.sql
autonomous: false
requirements: [GLEN-01]

must_haves:
  truths:
    - "Ara Najarian (external_id -700100) is RETIRED — office_id NULL, is_incumbent=false, is_active=false — NOT deleted (he did not re-run in the June 2 2026 election; orchestrator decision #1)"
    - "Alek Bartrosouf is seated as a Glendale Councilmember in the survivor chamber 771727ec (external_id 10450) on the existing shared LOCAL district — he was NOT in the DB before this wave"
    - "Bartrosouf uses the reserved external_id -700101 (confirmed vacant by a pre-flight COUNT/NOT EXISTS guard); -700100 is Najarian's"
    - "The Bartrosouf INSERT is gated by a checkpoint:human-verify — the executor confirms the certified/final lavote.gov result (LA County certifies June 26 2026; today is June 19) BEFORE applying the INSERT (orchestrator decision #2)"
    - "After the wave, the survivor chamber 771727ec has exactly 5 ACTIVE office-linked councilmembers (Kassakhian/Asatryan/Gharpetian/Brotman/Bartrosouf); Najarian is excluded from the active roster"
    - "No new districts/geofences and no duplicate person rows are created; Bartrosouf reuses the existing shared geo_id 0630000 LOCAL district"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/903_glendale_complete.sql"
      provides: "Roster completion: retire Najarian, INSERT Bartrosouf (certification-gated), confirm official_count=5"
      contains: "-700101"
  key_links:
    - from: "essentials.politicians (external_id -700100, Ara Najarian)"
      to: "retired state (office_id NULL, is_incumbent=false, is_active=false)"
      via: "UPDATE politicians SET ... WHERE external_id = -700100"
      pattern: "is_incumbent\\s*=\\s*false"
    - from: "essentials.politicians (external_id -700101, Alek Bartrosouf)"
      to: "essentials.offices in chamber 771727ec on the LOCAL district geo_id 0630000"
      via: "CTE INSERT politician → INSERT office → back-fill office_id, guarded NOT EXISTS on external_id"
      pattern: "771727ec"
---

<objective>
Complete the Glendale council to its current, post-election 5-member roster. This wave runs only after
Plan 01 has consolidated the structure to one 'Glendale City Council' chamber and flagged the Mayor.

⚠️ ROSTER CHANGE (orchestrator decisions #1 + #2, confirmed by RESEARCH §Live-Data Findings 1): the
June 2 2026 city council election turned over one seat. Ara Najarian (-700100) did NOT re-run and his
term ends ~July 2026 — he is RETIRED here (not deleted), the same retire-not-delete pattern as Santa
Clarita's Smyth, but it belongs in THIS roster wave (Wave 2), not Wave 1. Alek Bartrosouf is the
unofficial winner of his seat (3rd place, 15,396 votes) and is INSERTED as a new politician.

⚠️ CERTIFICATION GATE (orchestrator decision #2): results are uncertified until LA County certifies on
June 26 2026 (today is June 19). The Bartrosouf INSERT MUST be gated behind a checkpoint:human-verify —
the executor confirms the certified/final lavote.gov result before applying the INSERT. Wave 1 already
ran with no gate. (Najarian's retirement is independent of certification — he is not re-running
regardless — but it is applied in the same gated migration step for atomicity.)

Part A — RETIRE Ara Najarian (-700100):
- UPDATE essentials.politicians SET office_id=NULL, is_incumbent=false, is_active=false WHERE
  external_id=-700100. NEVER DELETE the politician row. He carries 0 stances — acceptable for a
  departing member (orchestrator decision #3; no Najarian stance task in Wave 4).

Part B — INSERT Alek Bartrosouf (-700101), AFTER the certification checkpoint:
- external_id -700101 (confirmed vacant by pre-flight COUNT/NOT EXISTS guard; -700100 is Najarian's),
  full_name 'Alek Bartrosouf', first 'Alek', last 'Bartrosouf', is_active true, is_appointed false,
  is_incumbent true, is_vacant false, source 'glendaleca.gov'. Office in survivor chamber 771727ec,
  title 'Councilmember', on the EXISTING shared LOCAL district (geo_id 0630000) the other 5 offices
  already use — reuse it; NO new districts/geofences. Back-fill politicians.office_id.

Part C — Confirm survivor chamber 771727ec official_count=5 (active members) after the Najarian→
Bartrosouf swap; run feedback_section_split_check (expect 0 rows for Glendale).

Purpose: A complete, accurate, current post-election roster is the prerequisite for headshots (Plan 03)
and stances (Plan 04). Output: Migration 903 applied; 5 current councilmembers seated + office-linked;
Najarian retired.

This IS a structural migration (903) — it registers in supabase_migrations.schema_migrations normally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-glendale-deep-seed/144-CONTEXT.md
@.planning/phases/144-glendale-deep-seed/144-RESEARCH.md
@.planning/phases/144-glendale-deep-seed/144-01-SUMMARY.md

<interfaces>
<!-- DB-verified live values (2026-06-19). Pre-flight confirms -700101 is free before INSERT. -->

Government: a7433437-341a-48e7-907e-a61318954f0a  geo_id '0630000'  state 'CA'
Survivor council chamber: 771727ec  external_id 10450

Existing survivor members (do NOT renumber — real source IDs):
  686339 Ardy Kassakhian (Mayor — flagged in Plan 01)
  686337 Elen Asatryan
  686336 Vartan Gharpetian
  686340 Daniel Brotman
  -700100 Ara Najarian → RETIRE (office_id NULL, is_incumbent=false, is_active=false); office c6f4e77d

NEW member to INSERT (after certification checkpoint):
  Alek Bartrosouf  external_id -700101  first 'Alek'  last 'Bartrosouf'  title 'Councilmember'
  (urban planner, GEC co-founder; unofficial June 2 2026 winner of Najarian's seat)

Reserved-range / duplicate-person pre-flight (Pitfall 4) — run BEFORE assigning:
  SELECT COUNT(*) FROM essentials.politicians WHERE external_id = -700101;  → expect 0
  SELECT full_name FROM essentials.politicians WHERE full_name ILIKE '%Bartrosouf%';  → expect 0 rows
  If -700101 is taken or a Bartrosouf row already exists elsewhere, STOP-report rather than INSERT.
  Use NOT EXISTS on external_id=-700101 in the INSERT (belt-and-suspenders with ON CONFLICT).

LOCAL district for the council (flat single-geo_id, like SC — NO per-seat geofences):
  Pre-flight: SELECT id, label, district_type FROM essentials.districts WHERE geo_id='0630000'
  AND state='CA' — reuse the existing LOCAL district row the survivor offices already link to. Do NOT
  create new districts/geofences or any LOCAL_EXEC row.

Schema rules (CA convention):
  - chambers.slug is GENERATED — NEVER include slug in any write
  - governments/districts have NO unique constraint on geo_id — guard with WHERE NOT EXISTS, never ON CONFLICT(geo_id)
  - districts.state = 'CA' (UPPERCASE)
  - politicians.external_id is unique → ON CONFLICT (external_id) DO NOTHING is safe

Certification gate (orchestrator decision #2): the Bartrosouf INSERT step runs only AFTER the
  checkpoint:human-verify confirms the certified/final lavote.gov result (results.lavote.gov/text-results/4338).
  Wave 1 had no gate. Najarian's retirement is certification-independent but applied in the same step.

Pattern migrations to copy:
  C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql — retire-not-delete + reserved
    -700xxx INSERT + office_id back-fill + district reuse (direct precedent)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Certification gate — confirm the final lavote.gov result before seeding Bartrosouf</name>
  <action>Pause for the user to confirm the certified/final June 2 2026 Glendale City Council result before any Bartrosouf INSERT is applied; do not proceed to Task 3 until approved. (Najarian's retirement, Task 2, is certification-independent and may proceed regardless.)</action>
  <what-built>Wave 1 reconcile is complete (geo_id, single chamber, Mayor flag). This wave will retire Najarian and INSERT Alek Bartrosouf as the new councilmember. Bartrosouf's win was unofficial as of research (June 19); LA County certifies June 26 2026.</what-built>
  <how-to-verify>
    1. Open results.lavote.gov/text-results/4338 (LA County official Glendale City Council results).
    2. Confirm Alek Bartrosouf is a CERTIFIED/final winner of the seat (3rd place behind Brotman + Asatryan), or that the result is final enough to seed.
    3. Confirm Ara Najarian did NOT re-run / is departing (independent of certification).
    4. If Bartrosouf's win has NOT held / not certified, instruct the executor to apply ONLY Task 2 (Najarian retirement) and defer the Bartrosouf INSERT to a follow-up.
  </how-to-verify>
  <resume-signal>Type "approved — seed Bartrosouf" to proceed with the full migration, or "retire Najarian only" to defer the Bartrosouf INSERT.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pre-flight uniqueness + district check, then author migration 903 Part A (retire Najarian)</name>
  <files>C:/EV-Accounts/backend/migrations/903_glendale_complete.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql (retire-not-delete + reserved -700xxx INSERT + office_id back-fill + district reuse — direct precedent)
    - .planning/phases/143-santa-clarita-deep-seed/143-02-SUMMARY.md (exact roster-completion migration shape + roster post-verification query)
    - .planning/phases/144-glendale-deep-seed/144-RESEARCH.md (§Live-Data Findings 1 roster change, §Reusable Patterns Wave 2, §Common Pitfalls 4, §Assumptions A5)
    - .planning/phases/144-glendale-deep-seed/144-CONTEXT.md (§decisions D-01, D-06, D-07)
  </read_first>
  <action>
    First run the pre-flight: (a) SELECT COUNT(*) FROM essentials.politicians WHERE external_id=-700101 —
    expect 0; (b) SELECT full_name FROM essentials.politicians WHERE full_name ILIKE '%Bartrosouf%' —
    expect 0 rows (Pitfall 4 — if a real Bartrosouf person exists, STOP-report rather than INSERT);
    (c) SELECT id, label, district_type FROM essentials.districts WHERE geo_id='0630000' AND state='CA'
    to capture the existing LOCAL district_id the survivor offices already use — reuse it (NO new
    districts/geofences/LOCAL_EXEC). Then begin C:/EV-Accounts/backend/migrations/903_glendale_complete.sql
    with Part A — RETIRE Najarian: UPDATE essentials.politicians SET office_id=NULL, is_incumbent=false,
    is_active=false WHERE external_id=-700100 (detach his Chamber c6f4e77d office link; NEVER DELETE the
    row; same pattern as SC Smyth in 894 but here in Wave 2). Do NOT include slug anywhere. This is a
    structural migration (903, registers in schema_migrations normally). Najarian's retirement is
    certification-independent — it may be applied regardless of the Task 1 outcome.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT is_incumbent, is_active, office_id FROM essentials.politicians WHERE external_id=-700100;" returns false, false, NULL (Najarian retired, not deleted)</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: -700101 confirmed free (COUNT 0); no pre-existing Bartrosouf person row anywhere
    - Existing shared LOCAL district_id (geo_id 0630000, state 'CA') captured for reuse in Part B
    - Najarian (-700100): `office_id IS NULL AND is_incumbent=false AND is_active=false` (retired, NOT deleted — the politician row still exists)
    - No essentials.politicians row was DELETEd
    - Part A statements are idempotent (second apply changes 0 rows)
  </acceptance_criteria>
  <done>Najarian retired (detached, not deleted); -700101 confirmed vacant; shared LOCAL district captured; Part A of migration 903 authored + applied.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 903 Part B/C — INSERT Bartrosouf (post-checkpoint), confirm official_count=5</name>
  <files>C:/EV-Accounts/backend/migrations/903_glendale_complete.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql (CTE INSERT politician → INSERT office → back-fill office_id; NOT EXISTS office guard; reserved -700xxx pattern)
    - .planning/phases/143-santa-clarita-deep-seed/143-02-SUMMARY.md (roster post-verification + idempotency shape)
    - .planning/phases/144-glendale-deep-seed/144-RESEARCH.md (§Live-Data Findings 1 Bartrosouf, §Common Pitfalls 2/4, §Validation Architecture Wave 2 checks)
    - .planning/phases/144-glendale-deep-seed/144-CONTEXT.md (§decisions D-01, D-06, D-07)
  </read_first>
  <action>
    ONLY after Task 1's checkpoint approved seeding Bartrosouf, append Part B and Part C to migration 903.
    Part B — INSERT Bartrosouf: a CTE that INSERTs the politician (external_id -700101, full_name
    'Alek Bartrosouf', first 'Alek', last 'Bartrosouf', is_active true, is_appointed false, is_incumbent
    true, is_vacant false, source 'glendaleca.gov') with ON CONFLICT (external_id) DO NOTHING RETURNING id,
    then INSERTs an office into chamber 771727ec on the captured existing shared LOCAL district_id, title
    exactly 'Councilmember', representing_state 'CA', is_appointed_position false, guarded by NOT EXISTS on
    (chamber_id, politician_id). Immediately back-fill politicians.office_id to the new office id. Do NOT
    create new districts/geofences/LOCAL_EXEC; do NOT include slug. Part C — UPDATE essentials.chambers
    SET official_count=5 WHERE id='771727ec-...' if not already 5 (after the Najarian→Bartrosouf swap).
    End the file with the roster post-verification query: the 5 current active councilmembers
    (686339/686337/686336/686340/-700101) seated in 771727ec with office_id non-NULL; Najarian (-700100)
    NOT among the active roster; feedback_section_split_check → 0 rows for Glendale. Apply via
    mcp__supabase-local execute_sql or psql -f. (If Task 1 deferred Bartrosouf, document the deferral in
    the summary and leave Part B/C for the follow-up — Part A still applied.)
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id WHERE o.chamber_id='771727ec-...' AND p.is_active=true;" returns 5 AND psql "$DATABASE_URL" -c "SELECT p.full_name, o.title FROM essentials.politicians p JOIN essentials.offices o ON o.id=p.office_id WHERE p.external_id=-700101;" returns Alek Bartrosouf / Councilmember</automated>
  </verify>
  <acceptance_criteria>
    - Bartrosouf (-700101) politician row exists with correct names/flags, source 'glendaleca.gov', is_active=true, is_incumbent=true
    - Bartrosouf has an office in chamber 771727ec on the existing shared geo_id 0630000 LOCAL district, title exactly 'Councilmember', is_appointed_position=false
    - Bartrosouf's politicians.office_id back-link is non-NULL
    - No new essentials.districts / geofence_boundaries / LOCAL_EXEC rows were created
    - No duplicate Bartrosouf person row (exactly one with external_id -700101)
    - Active members in chamber 771727ec = 5 (Kassakhian/Asatryan/Gharpetian/Brotman/Bartrosouf); Najarian (-700100) NOT among them
    - Survivor chamber 771727ec official_count=5; feedback_section_split_check → 0 rows for Glendale
    - Migration 903 idempotent (second apply changes 0 rows, no error)
  </acceptance_criteria>
  <done>Bartrosouf seated in 771727ec on the shared LOCAL district (post-checkpoint); official_count=5; 5 current active members office-linked; Najarian excluded; migration idempotent.</done>
</task>

</tasks>

<threat_model>
DB-seeding phase with NO new external attack surface. No untrusted user input — migration SQL applied
via service-role/operator directly against the internal production DB. Threat surface = accidental data
corruption (duplicate person, premature seeding of an uncertified winner), mitigated by idempotent
guards + a NOT EXISTS/COUNT pre-flight + the certification human-verify gate + post-migration SQL
verification.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator directly against production Supabase. No untrusted input. |
| LA County election result → DB roster | External (uncertified-until-June-26) election outcome drives the Bartrosouf INSERT; risk = seeding a winner whose result is later reversed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-07 | Spoofing | Premature seeding of Bartrosouf before certification (result could shift) | mitigate | checkpoint:human-verify gate confirms certified/final lavote.gov result before the INSERT (orchestrator decision #2); Najarian retirement is certification-independent |
| T-144-08 | Tampering | Duplicate Bartrosouf person row from a partial/double apply | mitigate | Pre-flight COUNT on external_id=-700101 (expect 0) + name ILIKE check; INSERT guarded NOT EXISTS + ON CONFLICT (external_id) DO NOTHING |
| T-144-09 | Spoofing | Najarian left active / reseated (roster corruption) | mitigate | Najarian RETIRED (office_id NULL, is_incumbent/is_active false), never deleted, never reseated; post-verify his flags false and he is excluded from the 5 active members |
| T-144-10 | Tampering | New external_id (-700101) collides with an existing CA politician | mitigate | Pre-flight COUNT on -700101 before INSERT; ON CONFLICT (external_id) DO NOTHING belt-and-suspenders; STOP-report on collision |
| T-144-11 | Tampering | Per-seat geofence/district explosion or a LOCAL_EXEC row created | mitigate | Reuse the existing geo_id 0630000 LOCAL district; no geofence_boundaries / new district / LOCAL_EXEC INSERTs |
| T-144-12 | Tampering | chambers.slug included in INSERT → error; or non-idempotent re-apply duplicates rows | mitigate | slug omitted (GENERATED); all writes guarded by ON CONFLICT / NOT EXISTS / IS DISTINCT FROM; second apply = 0 rows |
</threat_model>

<verification>
After applying 903 (post-checkpoint):
1. Najarian (-700100): office_id NULL, is_incumbent=false, is_active=false (retired, not deleted)
2. Bartrosouf (-700101): politician row + linked office in chamber 771727ec, office_id non-NULL, title 'Councilmember'
3. active members in chamber 771727ec = 5 (Kassakhian/Asatryan/Gharpetian/Brotman/Bartrosouf)
4. no new geofence/district rows (flat single-geo_id); no duplicate Bartrosouf row
5. survivor chamber official_count=5; feedback_section_split_check → still 0 rows for Glendale
6. certification checkpoint approved before the Bartrosouf INSERT
</verification>

<success_criteria>
Glendale's elected council is complete and current post-June-2-election: 5 at-large councilmembers
(Kassakhian/Asatryan/Gharpetian/Brotman/Bartrosouf) all office-linked in one chamber; Najarian correctly
retired (not deleted); the Bartrosouf INSERT applied only after the certification gate; no duplicate
people, no new geofences — ready for headshots in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/144-glendale-deep-seed/144-02-SUMMARY.md` when done.
</output>
