---
phase: 118-somerville-deep-seed
plan: 03
type: execute
wave: 3
depends_on:
  - 118-01
  - 118-02
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-somerville-headshots.py
  - C:/EV-Accounts/backend/migrations/583_somerville_headshots.sql
autonomous: true
requirements:
  - SOMERVILLE-02

must_haves:
  truths:
    - "All successfully-uploaded Somerville officials have a type='default' row in essentials.politician_images"
    - "Every uploaded headshot is 600x750 pixels (4:5 ratio) processed Lanczos q90"
    - "Headshots are stored in the 'politician_photos' Supabase Storage bucket"
    - "Gap officials (no online source found) are documented in migration 583 comments — not blocking"
    - "No official has type != 'default' in politician_images"
    - "At minimum 9 city officials are uploaded (confirmed-200 sources from RESEARCH); School Committee is best-effort"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-somerville-headshots.py"
      provides: "Python script — download confirmed URLs, crop 4:5, resize 600x750 Lanczos q90, upload to politician_photos"
      contains: "politician_photos"
    - path: "C:/EV-Accounts/backend/migrations/583_somerville_headshots.sql"
      provides: "politician_images INSERT rows for all uploaded Somerville officials"
      contains: "type = 'default'"
    - path: "essentials.politician_images (DB)"
      provides: "Headshot rows for uploaded Somerville officials"
      contains: "type='default', politician_id IN (Somerville external_id ranges)"
  key_links:
    - from: "_tmp-somerville-headshots.py"
      to: "Supabase Storage bucket 'politician_photos'"
      via: "supabase-py client upload"
      pattern: "bucket='politician_photos'"
    - from: "migration 583"
      to: "essentials.politicians"
      via: "SELECT id FROM essentials.politicians WHERE external_id = -XXXX"
      pattern: "WHERE NOT EXISTS on politician_id"
---

<objective>
Upload headshots for all Somerville officials (city + school committee) and record results in essentials.politician_images via migration 583. Produces a Python script (_tmp-somerville-headshots.py) that downloads photos from confirmed somervillema.gov URLs (no 403 block unlike Newton), crops to 4:5, resizes to 600x750 Lanczos q90, uploads to the 'politician_photos' bucket, and logs CDN URLs. Migration 583 inserts politician_images rows for all successful uploads.

Purpose: Satisfies SOMERVILLE-02. somervillema.gov is accessible (verified 200 for 9 of 12 city officials). 3 city officials (Link, Wheeler, Hardt) and all 7 SC members are gaps — best-effort fallback via campaign sites/news sources. Gaps are acceptable and must be documented.

Output: _tmp-somerville-headshots.py (run once, then archive); migration 583_somerville_headshots.sql applied to production; best-effort headshot coverage documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-somerville-deep-seed/118-RESEARCH.md
@.planning/phases/118-somerville-deep-seed/118-01-SUMMARY.md
@.planning/phases/118-somerville-deep-seed/118-02-SUMMARY.md

<interfaces>
<!-- Reference migrations the executor MUST read before writing the headshot script and migration 583. -->
<!-- Pattern source 1: Newton headshots (migration 580) — same milestone, same script structure -->
C:/EV-Accounts/backend/migrations/580_newton_headshots.sql
<!-- Pattern source 2: MA Tier 2 headshots (migration 356) — politician_images INSERT conventions -->
C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql

<!-- Key conventions: -->
<!-- Storage bucket: 'politician_photos' (NOT 'politician-headshots') -->
<!-- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/ -->
<!-- politician_images columns: id, politician_id, url, type='default', photo_license='public_domain' -->
<!-- INSERT pattern: WHERE NOT EXISTS (SELECT 1 FROM essentials.politician_images WHERE politician_id = ...) -->
<!-- Upload path: {politician_uuid}-headshot.jpg -->
<!-- Photo processing: crop to 4:5 FIRST, then resize 600x750 Lanczos q90 — never stretch directly -->
<!-- GAP documentation: comment in SQL with external_id and reason -->
<!-- UNLIKE NEWTON: somervillema.gov is NOT blocked — no special browser UA required for main URLs -->
<!-- Emily Hardt Ward 7: do NOT attempt /councilor-emily-hardt-2022.jpg — known 403; use campaign site -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run Somerville headshot script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-somerville-headshots.py</files>

  <read_first>
    C:/EV-Accounts/backend/scripts/_tmp-newton-headshots.py — same-session script to model the structure (roster dict, image processing, upload pattern, UPLOADED/GAP logging).
    C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql — upload path pattern, bucket name, type='default' convention, gap documentation format.
    .planning/phases/118-somerville-deep-seed/118-RESEARCH.md — headshot sources section; 9 confirmed-200 URLs; 3 city gaps; SC gaps; CMS URL patterns; fallback sources; Pitfall 3 (Emily Hardt).
    .planning/phases/118-somerville-deep-seed/118-01-SUMMARY.md — confirms city external_id range.
    .planning/phases/118-somerville-deep-seed/118-02-SUMMARY.md — confirms SC external_id range.
  </read_first>

  <action>
Write C:/EV-Accounts/backend/scripts/_tmp-somerville-headshots.py as a standalone Python script modeled on _tmp-newton-headshots.py.

Script requirements:

1. IMPORTS: requests, PIL (Image, ImageOps), io, os, supabase (create_client), logging. Read Supabase URL + anon key from environment variables (same pattern as prior scripts).

2. ROSTER: Define a list of dicts with all 19 Somerville officials (city politicians only — do NOT include ex-officio SC members Mayor Wilson and Davis, as they are covered by city entries):

   City officials (12 entries, external_id -2562535001 through -2562535012):
   - {'ext_id': -2562535001, 'name': 'Jake Wilson', 'url': 'https://s3.amazonaws.com/somervillema-live/s3fs-public/JW_City_Hall_Steps.jpeg'}
   - {'ext_id': -2562535002, 'name': 'Jon Link', 'url': None, 'fallback': 'jonforsomerville.com'}
   - {'ext_id': -2562535003, 'name': 'Wilfred N. Mbah', 'url': 'https://s3.amazonaws.com/somervillema-live/s3fs-public/images/profile-councilor-mbah.jpg'}
   - {'ext_id': -2562535004, 'name': 'Kristen E. Strezo', 'url': 'https://s3.amazonaws.com/somervillema-live/s3fs-public/images/profile-councilor-strezo.jpg'}
   - {'ext_id': -2562535005, 'name': 'Ben Wheeler', 'url': None, 'fallback': 'benwheelerforsomerville.com'}
   - {'ext_id': -2562535006, 'name': 'Matthew McLaughlin', 'url': 'https://www.somervillema.gov/sites/default/files/councilor-matt-mclaughlin-2022.jpg'}
   - {'ext_id': -2562535007, 'name': 'Jefferson Thomas Scott', 'url': 'https://www.somervillema.gov/sites/default/files/councilor-jt-scott-2022.jpg'}
   - {'ext_id': -2562535008, 'name': 'Ben Ewen-Campen', 'url': 'https://www.somervillema.gov/sites/default/files/councilor-ben-ewen-campen-2022.jpg'}
   - {'ext_id': -2562535009, 'name': 'Jesse Clingan', 'url': 'https://www.somervillema.gov/sites/default/files/councilor-jesse-clingan-2022.jpg'}
   - {'ext_id': -2562535010, 'name': 'Naima Sait', 'url': 'https://s3.amazonaws.com/somervillema-live/s3fs-public/headshot-naima-sait.jpg'}
   - {'ext_id': -2562535011, 'name': 'Lance L. Davis', 'url': 'https://www.somervillema.gov/sites/default/files/councilor-lance-davis-2022.jpg'}
   - {'ext_id': -2562535012, 'name': 'Emily Hardt', 'url': None, 'fallback': 'emilyhardtforsomerville.com'}
     CRITICAL: Do NOT attempt /councilor-emily-hardt-2022.jpg — the Ward 7 page still shows stale Judy Pineda Neufeld entry. Emily Hardt's city page does not have a headshot URL yet.

   School Committee (7 entries, external_id -2510890001 through -2510890007):
   - {'ext_id': -2510890001, 'name': 'Emily Ackman', 'url': None, 'fallback': 'emilyackmanforward1 Facebook page; Ballotpedia'}
   - {'ext_id': -2510890002, 'name': 'Elizabeth Eldridge', 'url': None, 'fallback': 'Somerville SEPAC sources'}
   - {'ext_id': -2510890003, 'name': 'Michele Lippens', 'url': None, 'fallback': 'local news election coverage'}
   - {'ext_id': -2510890004, 'name': 'Andre L. Green', 'url': None, 'fallback': 'local news election coverage'}
   - {'ext_id': -2510890005, 'name': 'Laura Pitone', 'url': None, 'fallback': 'local news election coverage'}
   - {'ext_id': -2510890006, 'name': 'Emma Stellman', 'url': None, 'fallback': 'local news election coverage'}
   - {'ext_id': -2510890007, 'name': 'Leiran Biton', 'url': None, 'fallback': 'leiran4somerville Facebook; thesomervilletimes.com Spotlight'}

3. DOWNLOAD LOGIC for each official:
   a. If confirmed URL provided (not None): attempt direct GET request. Standard requests.get() — no special browser UA required for somervillema.gov main URLs. Add User-Agent header just in case: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120.0.0.0 Safari/537.36'.
   b. If URL is None: log as GAP with the fallback note. Do NOT raise exception — continue to next official.
   c. If GET returns non-200: log as GAP with status code and continue.
   Note: SC headshot sources are all unconfirmed — expect all 7 to be GAP unless executor finds working URLs from fallback sources noted above.

4. IMAGE PROCESSING for each successfully downloaded image:
   - Open with PIL.Image.open(io.BytesIO(response.content))
   - Crop to 4:5 aspect ratio FIRST (center crop):
     target_ratio = 4/5
     current_ratio = width / height
     if current_ratio > target_ratio: new_width = int(height * target_ratio); left = (width - new_width) // 2; img = img.crop((left, 0, left + new_width, height))
     else: new_height = int(width / target_ratio); top = (height - new_height) // 2; img = img.crop((0, top, width, top + new_height))
   - THEN resize to 600x750 using Image.LANCZOS resampling.
   - Convert to RGB if needed (handles RGBA PNG source files).
   - Save to BytesIO buffer as JPEG with quality=90.

5. UPLOAD to Supabase Storage:
   - Bucket: 'politician_photos' (NOT 'politician-headshots')
   - Query supabase.table('politicians').select('id').eq('external_id', ext_id).execute() to get politician UUID.
     If no row returned, log as SKIP and continue (safety guard).
   - Upload path: f"{politician_uuid}-headshot.jpg"
   - Upload: supabase.storage.from_('politician_photos').upload(path, image_bytes, file_options={"content-type": "image/jpeg", "upsert": "true"})
   - CDN URL: f"https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/{politician_uuid}-headshot.jpg"

6. OUTPUT: After each official, print one line:
   UPLOADED: {ext_id} {name} -> {cdn_url}
   or
   GAP: {ext_id} {name} -- {reason}

7. Final summary: print counts of UPLOADED and GAP.

After writing the script, run it and capture the output. The printed UPLOADED/GAP log is needed for Task 2.
  </action>

  <verify>
    <automated>
      Run the script and verify it completes without uncaught exceptions. Expected output format:
        UPLOADED: -2562535001 Jake Wilson -> https://kxsdzaojfaibhuzmclfq.storage.supabase.co/...
        UPLOADED: -2562535006 Matthew McLaughlin -> https://kxsdzaojfaibhuzmclfq.storage.supabase.co/...
        GAP: -2562535002 Jon Link -- url=None; fallback: jonforsomerville.com (manual retrieval needed)
        ...
        Summary: X uploaded, Y gaps

      Confirm uploaded files in Supabase Storage:
        SELECT COUNT(*) FROM storage.objects WHERE bucket_id = 'politician_photos' AND name LIKE '%-headshot.jpg' AND created_at > NOW() - INTERVAL '4 hours';
        -- Expected: at least 9 (all 9 confirmed-200 city officials; ideally more if fallbacks work)
    </automated>
  </verify>

  <acceptance_criteria>
    - Script ran to completion without Python exceptions
    - At least 9 headshots uploaded (confirmed-200 city officials: Mayor Wilson, Mbah, Strezo, McLaughlin, Scott, Ewen-Campen, Clingan, Sait, Davis)
    - All 3 gap city officials (Link, Wheeler, Hardt) logged as GAP with fallback notes
    - All SC members logged as GAP or UPLOADED based on actual source availability
    - No attempt made on Emily Hardt via /councilor-emily-hardt-2022.jpg path (Pitfall 3)
    - Full UPLOADED/GAP log captured for Task 2
  </acceptance_criteria>

  <done>
    Script ran to completion (no Python exceptions). At least 9 city official headshots uploaded to 'politician_photos' bucket. Full list of UPLOADED CDN URLs and GAP officials documented in script output — ready for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration 583 — Somerville headshots</name>
  <files>C:/EV-Accounts/backend/migrations/583_somerville_headshots.sql</files>

  <read_first>
    C:/EV-Accounts/backend/migrations/356_ma_tier2_headshots.sql — exact politician_images INSERT pattern (WHERE NOT EXISTS guard, type='default', photo_license='public_domain', url column).
    C:/EV-Accounts/backend/migrations/580_newton_headshots.sql — same-milestone headshot migration structure (header comment format, post-verification DO block, ledger entry).
    Script output from Task 1 — the printed UPLOADED/GAP log is the source of truth for which officials were uploaded and their CDN URLs.
  </read_first>

  <action>
Write C:/EV-Accounts/backend/migrations/583_somerville_headshots.sql using the politician_images INSERT pattern from migration 356. Then apply via mcp__supabase-local.

HEADER COMMENT — include:
- Migration 583: Somerville city government + school committee headshots (SOMERVILLE-02)
- Storage bucket: politician_photos
- CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/
- Total officials attempted: 19 (12 city + 7 SC; Mayor Wilson and Davis counted once each under city)
- Uploaded count: X (from Task 1 script output)
- Gap count: Y (from Task 1 script output)
- somervillema.gov accessible (no 403 block) — 9 city officials confirmed 200
- 3 city gap officials: Jon Link, Ben Wheeler, Emily Hardt (newly elected Nov 2025; no city site photo yet)
- SC headshots: all 7 members are gaps (no individual headshots on somervillema.gov or somerville.k12.ma.us)
- Photo processing: crop 4:5 first, then resize 600x750 Lanczos q90
- type = 'default' (NOT 'headshot') — UI filter uses .find(img => img.type === 'default')
- DOCUMENTED GAPS: list each gap official with external_id, full_name, and reason

For each successfully uploaded official (from Task 1 UPLOADED output), one INSERT block per official:
  INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license)
  SELECT gen_random_uuid(),
         (SELECT id FROM essentials.politicians WHERE external_id = {ext_id}),
         '{cdn_url}',
         'default', 'public_domain'
  WHERE NOT EXISTS (
    SELECT 1 FROM essentials.politician_images
    WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = {ext_id})
  );

Group inserts with section divider comments:
  -- === CITY COUNCIL (external_ids -2562535001 through -2562535012) ===
  -- === SCHOOL COMMITTEE (external_ids -2510890001 through -2510890007) ===

Comment out gap officials with:
  -- GAP: {ext_id} {name} — {reason}

POST-VERIFICATION DO block:
  v_img_count = SELECT COUNT(*) FROM essentials.politician_images pi
    JOIN essentials.politicians p ON p.id = pi.politician_id
    WHERE (p.external_id BETWEEN -2562535012 AND -2562535001
        OR p.external_id BETWEEN -2510890007 AND -2510890001)
    AND pi.type = 'default'
  v_wrong_type = SELECT COUNT(*) FROM essentials.politician_images pi
    JOIN essentials.politicians p ON p.id = pi.politician_id
    WHERE (p.external_id BETWEEN -2562535012 AND -2562535001
        OR p.external_id BETWEEN -2510890007 AND -2510890001)
    AND pi.type != 'default'
  If v_wrong_type > 0: RAISE EXCEPTION 'Somerville headshots have wrong type (not default): % rows', v_wrong_type
  RAISE NOTICE 'Migration 583 post-verification PASSED: % headshots inserted (type=default), % gap officials documented', v_img_count, {gap_count}

LEDGER:
  INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('583') ON CONFLICT (version) DO NOTHING
  </action>

  <verify>
    <automated>
      After applying migration 583, run:
        SELECT COUNT(*) FROM essentials.politician_images pi
        JOIN essentials.politicians p ON p.id = pi.politician_id
        WHERE (p.external_id BETWEEN -2562535012 AND -2562535001
            OR p.external_id BETWEEN -2510890007 AND -2510890001)
          AND pi.type = 'default';
        -- Expected: matches UPLOADED count from Task 1 script output (minimum 9)

        SELECT COUNT(*) FROM essentials.politician_images pi
        JOIN essentials.politicians p ON p.id = pi.politician_id
        WHERE (p.external_id BETWEEN -2562535012 AND -2562535001
            OR p.external_id BETWEEN -2510890007 AND -2510890001)
          AND pi.type != 'default';
        -- Expected: 0

        SELECT COUNT(*) FROM supabase_migrations.schema_migrations WHERE version = '583';
        -- Expected: 1
    </automated>
  </verify>

  <acceptance_criteria>
    - Migration 583 applied without errors; post-verification NOTICE emitted
    - politician_images row count matches UPLOADED count from Task 1 script output
    - Zero rows with type != 'default' for Somerville external_id ranges
    - Gap officials documented in migration 583 comments with reason
    - Ledger entry for '583' present in supabase_migrations.schema_migrations
    - At least 9 rows (minimum confirmed-200 city officials)
    - No duplicate politician_images rows for any official (WHERE NOT EXISTS guard confirmed functional)
  </acceptance_criteria>

  <done>
    Migration 583 applied without errors; all inserted politician_images rows have type='default'; gap officials documented in migration comments; ledger entry present; post-verification NOTICE emitted. Phase 118 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python script → external HTTP sources | Fetches images from somervillema.gov, S3 bucket (somervillema-live), and fallback sites (campaign pages, news) |
| Python script → Supabase Storage | Uploads binary image data to politician_photos bucket |
| migration 583 → essentials.politician_images | Inserts CDN URLs as immutable reference data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-08 | Tampering | Image source substitution (wrong person's photo) | mitigate | Script logs source URL per official in UPLOADED output; human spot-check of at least 3 officials during migration 583 review; Emily Hardt URL path guard in script (Pitfall 3) |
| T-118-09 | Information Disclosure | Sensitive image content from unofficial sources | mitigate | Only public-domain official/campaign photos used; photo_license='public_domain' set in migration |
| T-118-10 | Tampering | Wrong bucket name ('politician-headshots' vs 'politician_photos') | mitigate | Script hardcodes bucket='politician_photos'; migration header CRITICAL comment warns against wrong name |
| T-118-11 | Tampering | type='headshot' instead of type='default' | mitigate | Post-verification gate checks v_wrong_type = 0; any non-'default' rows cause RAISE EXCEPTION |
| T-118-12 | Tampering | Stale Hardt URL path (/councilor-emily-hardt-2022.jpg → returns 403 + shows wrong person) | mitigate | Script roster entry for ext_id=-2562535012 has url=None; action explicitly forbids the -2022.jpg path attempt |
| T-118-SC | Tampering | pip installs during script development | accept | No new pip installs — requests/PIL/supabase-py already present from prior phases |
</threat_model>

<verification>
After migration 583 applies successfully:

1. essentials.politician_images has rows for all uploaded Somerville officials with type='default'
2. Zero rows with type != 'default' for Somerville external_id ranges (-2562535012..-2562535001 and -2510890007..-2510890001)
3. All uploaded images accessible at CDN URLs (spot-check 2-3 in browser)
4. Image dimensions confirmed 600x750 for at least one uploaded photo
5. Gap officials documented in migration 583 comments with reason (Link, Wheeler, Hardt, and SC members)
6. Supabase Storage bucket 'politician_photos' contains new objects created this session
7. Ledger entry for '583' present in supabase_migrations.schema_migrations
8. Minimum 9 rows (all confirmed-200 city officials from RESEARCH)
</verification>

<success_criteria>
Migration 583 applied; at least 9 Somerville officials have a type='default' headshot in politician_images; zero type!='default' rows; gaps documented; SOMERVILLE-02 closed at best-effort (~47-60% city coverage + best-effort SC). Phase 118 complete — Somerville address returns populated LOCAL + SCHOOL sections with headshots.
</success_criteria>

<output>
Create .planning/phases/118-somerville-deep-seed/118-03-SUMMARY.md when done. Include in SUMMARY: final UPLOADED count, gap count, list of gap officials with external_ids, and which sources were successfully used.
</output>
