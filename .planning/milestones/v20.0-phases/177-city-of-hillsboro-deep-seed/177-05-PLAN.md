---
phase: 177-city-of-hillsboro-deep-seed
plan: 05
type: execute
wave: 4
depends_on: ["177-03", "177-04"]
files_modified:
  - C:/Transparent Motivations/essentials/src/lib/buildingImages.js
  - C:/Transparent Motivations/essentials/src/lib/coverage.js
autonomous: false
requirements: [WASH-03]

must_haves:
  truths:
    - "A legally-licensed 1700×540 community banner for Hillsboro is uploaded to Supabase Storage cities/hillsboro.jpg"
    - "buildingImages.js CURATED_LOCAL has a lowercase 'hillsboro' key pointing at the banner CDN URL, with an attribution comment"
    - "coverage.js Oregon block has a 'Hillsboro' entry with browseGovernmentList ['4134100'], browseStateAbbrev 'OR', hasContext:true, alphabetically between Gresham and Maywood Park"
    - "npm run build passes with both edits"
    - "The live browse link surfaces Mayor Pace first + all 6 councilors with headshots + stances, the community banner renders (not the tier gradient), and no party label appears"
    - "Implements CONTEXT decisions D-08 (banner subject/licensing), D-09 (banner in final plan), D-13 (coverage.js Oregon entry)"
  artifacts:
    - path: "C:/Transparent Motivations/essentials/src/lib/coverage.js"
      provides: "Hillsboro purple hasContext chip in the Oregon COVERAGE_STATES block"
      contains: "Hillsboro"
    - path: "C:/Transparent Motivations/essentials/src/lib/buildingImages.js"
      provides: "hillsboro CURATED_LOCAL banner entry"
      contains: "hillsboro"
  key_links:
    - from: "coverage.js Oregon areas"
      to: "browse route (browse_geo_id=4134100)"
      via: "browseGovernmentList entry"
      pattern: "label: 'Hillsboro'"
    - from: "buildingImages.js CURATED_LOCAL 'hillsboro'"
      to: "offices.representing_city='Hillsboro' (set inline in mig 1150)"
      via: "getBuildingImages() lowercased substring match"
      pattern: "hillsboro:"
---

<objective>
Close the phase: (1) source, process, and upload a legally-licensed 1700×540 community banner for
Hillsboro; (2) wire it via the `hillsboro` CURATED_LOCAL key in buildingImages.js; (3) surface
Hillsboro in the Oregon block of coverage.js with the purple hasContext chip. The banner renders
because mig 1150 set `offices.representing_city='Hillsboro'` inline (no backfill needed).

Purpose: Satisfy WASH-03 Success Criterion 4 (purple hasContext chip) + the community banner
standing constraint (D-08/D-09), and verify the full roster end-to-end on the live app.
Output: banner in Storage (cities/hillsboro.jpg) + two essentials-repo edits + passing build +
live-browse confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-RESEARCH.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-03-SUMMARY.md
@.planning/phases/177-city-of-hillsboro-deep-seed/177-04-SUMMARY.md
@src/lib/coverage.js
@src/lib/buildingImages.js
@docs/banner-asset-pipeline.md

**This is the essentials repo** — coverage.js and buildingImages.js are committed HERE (`git`), NOT
in C:/EV-Accounts. The purple hasContext chip and the banner-render path are already built; only the
data row + banner asset + CURATED_LOCAL key are new. The banner upload uses the existing committed
scripts scripts/banners/process_banner.py + upload_banner.py — no new script file.

**TAILWIND HYGIENE:** do NOT introduce raw Windows `\` paths into any committed file (Tailwind v4
scans .md/.js and a `\f...` sequence crashes the prod build). Use forward-slash CDN URLs only.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Orchestrator sources + processes + uploads the community banner</name>
  <decision>Which legally-licensed wide photo becomes the Hillsboro community banner</decision>
  <context>
    D-08: executor/orchestrator picks the best legally-licensed wide shot. Priority hint: downtown/
    civic horizon (Main Street, Hillsboro Civic Center, Orenco Station) — but a clean license +
    crops-well-to-3.15:1 (1700×540) beats subject preference. Wikimedia Commons FIRST, Unsplash
    fallback. NO AI-generated images; no baked-in text/graphics. This is a decision checkpoint because
    the subject/source/license is a human-judgment call that then drives the attribution comment.
    Follow docs/banner-asset-pipeline.md Stages 1–8. Run:
    `python scripts/banners/process_banner.py --input <raw> --output <processed_1700x540.jpg>` then
    `python scripts/banners/upload_banner.py --file <processed_1700x540.jpg> --path cities/hillsboro.jpg`.
    Record the final Title | Author | License for the attribution comment used in Task 2.
  </context>
  <options>
    <option id="wikimedia">
      <name>Wikimedia Commons photo (preferred)</name>
      <pros>Clear CC license + attribution string; matches Beaverton precedent (M.O. Stevens CC BY 3.0)</pros>
      <cons>May lack a clean 3.15:1-croppable downtown/civic shot for Hillsboro</cons>
    </option>
    <option id="unsplash">
      <name>Unsplash fallback photo</name>
      <pros>Higher chance of a wide, crop-friendly horizon shot</pros>
      <cons>Attribution/license differs from Wikimedia convention; confirm Unsplash license terms</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen source + final Title | Author | License string, confirming cities/hillsboro.jpg uploaded (CDN URL returns 200).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner in buildingImages.js and surface Hillsboro in coverage.js</name>
  <files>C:/Transparent Motivations/essentials/src/lib/buildingImages.js, C:/Transparent Motivations/essentials/src/lib/coverage.js</files>
  <read_first>
    - src/lib/buildingImages.js lines 104–113 (CURATED_LOCAL block + attribution comments, incl. the beaverton entry)
    - src/lib/coverage.js lines 95–106 (the Oregon block — Beaverton…Wood Village)
    - .planning/phases/177-city-of-hillsboro-deep-seed/177-PATTERNS.md §"src/lib/buildingImages.js" and §"src/lib/coverage.js" (exact inserts)
  </read_first>
  <action>
    Edit 1 — buildingImages.js: add a lowercase `hillsboro` key to CURATED_LOCAL pointing at
    `https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/hillsboro.jpg`,
    placed alongside the existing `beaverton` entry. Add an attribution comment line above
    CURATED_LOCAL in the existing format `//   hillsboro - <Title> | <Author> | <License>` using the
    string recorded in Task 1. The lowercase key is required because getBuildingImages() lowercases
    offices.representing_city and does a substring match; mig 1150 set representing_city='Hillsboro'
    inline so this resolves at first browse.
    Edit 2 — coverage.js: insert `{ label: 'Hillsboro', browseGovernmentList: ['4134100'],
    browseStateAbbrev: 'OR', hasContext: true }` into the Oregon `areas` array, alphabetically
    BETWEEN 'Gresham' and 'Maywood Park'. Match the existing column alignment of surrounding rows.
    Use geo_id '4134100' (NOT 4133850). Change nothing else in either file — no other state blocks,
    no other CURATED_LOCAL keys. No raw Windows `\` paths anywhere (forward-slash CDN URLs only).
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has `hillsboro:` → the cities/hillsboro.jpg CDN URL
    - An attribution comment `//   hillsboro - ... | ... | ...` is present above CURATED_LOCAL
    - coverage.js Oregon areas has 'Hillsboro' between 'Gresham' and 'Maywood Park'
    - Entry is exactly { label: 'Hillsboro', browseGovernmentList: ['4134100'], browseStateAbbrev: 'OR', hasContext: true }
    - No other lines changed in either file; no raw `\` path introduced
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Banner wired + Hillsboro surfaced; production build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-browse end-to-end verification + commit</name>
  <what-built>The Hillsboro banner (Storage) + buildingImages.js key + coverage.js purple chip.</what-built>
  <how-to-verify>
    1. Confirm `npm run build` passed (Task 2 gate).
    2. Browse essentials.empowered.vote/results?browse_geo_id=4134100&browse_mtfcc=G4110 and confirm:
       - Mayor Beach Pace sorts FIRST, all 6 councilors follow; no empty LOCAL section; no section-split.
       - Headshots render 600×750 with no overlays; compass stances visible; NO party label (antipartisan).
       - The Local section shows the Hillsboro community banner photo (NOT the tier gradient fallback) — this confirms representing_city='Hillsboro' (mig 1150) + the lowercase CURATED_LOCAL key resolve correctly.
       - The purple hasContext chip appears for Hillsboro.
    3. Optionally enter a real Hillsboro address and confirm the same roster resolves via /representatives/me.
    4. Commit both files in the essentials repo: `git add src/lib/coverage.js src/lib/buildingImages.js && git commit` (on a branch if the harness is on the default branch).
  </how-to-verify>
  <acceptance_criteria>
    - Live browse shows Mayor first + 6 councilors, no empty LOCAL section, no section-split
    - Headshots 600×750 no overlays; stances visible; no party label
    - Community banner renders (not gradient fallback)
    - Purple hasContext chip shows for Hillsboro
    - coverage.js + buildingImages.js committed in the essentials repo
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the live browse + banner, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Banner sourced from Wikimedia Commons / Unsplash |
| operator → Supabase Storage | Processed banner uploaded to public bucket cities/hillsboro.jpg |
| build pipeline → prod | coverage.js + buildingImages.js edits shipped via the essentials build/deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-13 | Spoofing/IP | Unlicensed or AI-generated banner image | mitigate | Wikimedia-first with recorded Title/Author/License; NO AI-generated images (pipeline forbids); decision checkpoint records the license string |
| T-177-14 | Tampering | Malformed coverage.js/buildingImages.js breaks the prod build | mitigate | `npm run build` gate before commit; additive edits only |
| T-177-15 | Denial of Service | Windows `\` path in committed file crashes Tailwind scan | mitigate | Edits add only forward-slash CDN URLs + data rows; no raw backslash introduced |
| T-177-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; banner scripts (process_banner.py/upload_banner.py) are pre-existing committed tooling |
</threat_model>

<verification>
- `npm run build` exits 0; banner uploaded + rendered; live browse surfaces Mayor-first roster with
  chip, stances, headshots, banner, no section-split, no party label; both files committed.
</verification>

<success_criteria>
- Hillsboro visible with the purple hasContext chip + community banner in the Oregon block; build
  green; live browse confirms the full roster + banner end-to-end.
</success_criteria>

<output>
Create `.planning/phases/177-city-of-hillsboro-deep-seed/177-05-SUMMARY.md` when done, recording the
banner source/license and the live-browse confirmation. This is the final plan — note WASH-03 is
satisfied end-to-end.
</output>
