---
phase: 198-south-tucson-deep-seed
plan: 03
type: execute
# WAVE NOTE (deliberate serialization): wave 3 sits behind wave 2 (headshots) even though both depend
# ONLY on Plan 01. This is an INTENTIONAL serialization, not a computed dependency inconsistency.
# Stance research must run ONE OFFICIAL AT A TIME (feedback_stance_research_one_at_a_time: parallel
# stance fan-out burns the session quota), so this plan is kept in its own later wave to avoid a
# parallel stance pass and keep each execution context small.
wave: 3
depends_on: ["198-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1365_south_tucson_mayor_stances.sql
  - C:/EV-Accounts/backend/migrations/1366_south_tucson_vice_mayor_stances.sql
  - C:/EV-Accounts/backend/migrations/1367_south_tucson_acting_mayor_stances.sql
  - C:/EV-Accounts/backend/migrations/1368_south_tucson_council_1_stances.sql
  - C:/EV-Accounts/backend/migrations/1369_south_tucson_council_2_stances.sql
  - C:/EV-Accounts/backend/migrations/1370_south_tucson_council_3_stances.sql
  - C:/EV-Accounts/backend/migrations/1371_south_tucson_council_4_stances.sql
autonomous: true
requirements: [SUB-04]
must_haves:
  truths:
    - "Evidence-only compass stances are seeded for the 7 officials against the 36 non-judicial live topics (never the 8 judicial-* topics)"
    - "Every stance row is 100% cited (has a non-empty sources array); there are ZERO neutral-default rows — a topic with no evidence gets NO row at all (honest blank)"
    - "Stances are researched one official at a time (quota); citations are from reachable non-WAF sources (azluminaria.org / tucsonspotlight.org / tucson.com / tucsonsentinel.com / kgun9.com); salient South Tucson issues probed: public safety/policing (Mayor Valenzuela's stated top priority), city budget/department funding, the Mercado/South-4th-Ave small-business corridor"
    - "D-05: full stances per convention — evidence-only across all live compass topics, one research agent at a time (rate-limit), 100% citation, NO default values, honest blank spokes; applies to South Tucson's 7 elected council members"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1365_south_tucson_mayor_stances.sql"
      provides: "Mayor Valenzuela (-4015001) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1371_south_tucson_council_4_stances.sql"
      provides: "Council member Aguirre (-4015007) evidence-only stances: politician_answers + politician_context rows"
      contains: "politician_answers"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "same (politician_id, topic_id) — every answer has a cited context row"
      pattern: "politician_context"
    - from: "stance rows"
      to: "inform.compass_topics (36 non-judicial)"
      via: "topic_id references a non-judicial live topic UUID"
      pattern: "topic_id"
---

<objective>
Seed evidence-only compass stances for the 7 City of South Tucson officials. One audit-only migration per
official (1365 Mayor Valenzuela, 1366 Vice Mayor Brown-Dominguez, 1367 Acting Mayor Robles, 1368-1371 the 4
plain council members Jimenez/Diaz/Flagg/Aguirre), researched ONE OFFICIAL AT A TIME (quota), against the 36
NON-JUDICIAL live compass topics (44 live topics minus the 8 `judicial-*` keys, which have no bearing on an
at-large council member/Mayor/Vice-Mayor/Acting-Mayor — Pitfall 8). Every stance is 100% cited with
evidence-based reasoning; a topic with no documented record gets NO row at all (honest blank), never a
neutral default (global `feedback_stance_no_default_value`). Discrete 1-5 "chairs", never a polarity scale.

CRITICAL (salient South Tucson local issues to probe — RESEARCH): public safety / policing (Mayor
Valenzuela's stated top priority), city budget / department funding, and the Mercado / South-4th-Avenue
Mexican-food/mural small-business corridor. These are the highest-yield evidence topics for this council.
Background context (NOT current-roster fact): South Tucson has a well-documented history of contentious
recalls — treat as background, never attribute a historical recall-era position to the current roster.

CRITICAL (sources): cite the reachable non-WAF outlets confirmed reachable 2026-07-17: azluminaria.org,
tucsonspotlight.org, tucson.com / Arizona Daily Star, tucsonsentinel.com, kgun9.com. NOTE (Pitfall 4):
southtucsonaz.gov itself is Cloudflare-JS-challenge-blocked to curl/WebFetch — cite a southtucsonaz.gov page
ONLY if it was actually rendered via Playwright and read; never cite an unfetched URL. (Pitfall 6) Confirm
any "4th Avenue" evidence is about SOUTH 4th Avenue inside South Tucson, NOT Historic 4th Avenue in Tucson
proper.

Wave placement (deliberate): this plan is wave 3, behind wave 2 (headshots), even though BOTH depend only on
Plan 01. That is an INTENTIONAL serialization to honor the one-official-at-a-time stance-research quota
(`feedback_stance_research_one_at_a_time`) and keep each execution context small — NOT a computed dependency
inconsistency. Within this plan, work each official's migration one at a time (author → apply → next), 1365
through 1371, so a context that degrades partway is recoverable at a clean per-official boundary.

Purpose: Satisfies SUB-04 / ROADMAP success criterion #3 (evidence-only stances for all seated officials,
100% cited, no defaults, honest blanks).
Output: 7 audit-only migrations `1365_south_tucson_mayor_stances.sql` +
`1366_south_tucson_vice_mayor_stances.sql` + `1367_south_tucson_acting_mayor_stances.sql` +
`1368..1371_south_tucson_council_N_stances.sql` (unregistered), applied so the officials' compasses populate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md
@.planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md
@.planning/phases/198-south-tucson-deep-seed/198-01-SUMMARY.md

<execution_note>
Depends on Plan 01: the 7 politician UUIDs from 198-01-SUMMARY.md are REQUIRED (stance rows key on the
official UUID). Executor authors the 7 `.sql` files (researching one official at a time per the
`feedback_stance_research_one_at_a_time` quota rule — do NOT fan out parallel stance research). The
ORCHESTRATOR applies each via `psql -f` and asserts. Commit via `git -C "C:/EV-Accounts"`. These are
AUDIT-ONLY migrations — NOT registered in the ledger. Independent of Plan 02 (headshots) — both depend only
on Plan 01, kept in separate, later waves DELIBERATELY to honor the one-official-at-a-time quota. Research
the Task-2-confirmed sitting roster (if any seat changed at the July 21 primary, stance the seated official,
not a research-date name).
</execution_note>

<interfaces>
- inform.politician_answers (politician_id, topic_id, value): value = discrete 1.0-5.0 chair;
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value.
- inform.politician_context (politician_id, topic_id, reasoning, sources): reasoning = cited evidence prose
  ($$-quoted, avoids apostrophe-escaping); sources = ARRAY[...]::text[] of source URLs (NEVER empty);
  ON CONFLICT (politician_id, topic_id) DO UPDATE.
- inform.compass_topics: 44 live topics (DB-confirmed 2026-07-17); office_scope is empty/NULL (DB does NOT
  enforce judicial exclusion — manual convention). The 8 EXCLUDED judicial keys: judicial-access-to-justice,
  judicial-bail-pretrial, judicial-criminal-justice, judicial-government-deference, judicial-interpretation,
  judicial-police-accountability, judicial-prosecution-priorities, judicial-transparency. Research against
  the remaining 36 only.
- Evidence sources (RESEARCH — non-WAF, reachable 2026-07-17): azluminaria.org, tucsonspotlight.org,
  tucson.com / Arizona Daily Star, tucsonsentinel.com, kgun9.com, and Playwright-rendered southtucsonaz.gov
  pages. Salient issues: public safety/policing (Valenzuela's top priority), city budget/department funding,
  Mercado/South-4th-Ave small-business corridor.
- ext_ids → file: -4015001 Valenzuela (Mayor) → 1365; -4015002 Brown-Dominguez (Vice Mayor) → 1366;
  -4015003 Robles (Acting Mayor) → 1367; -4015004 Jimenez → 1368 (Council 1); -4015005 Diaz → 1369
  (Council 2); -4015006 Flagg → 1370 (Council 3); -4015007 Aguirre → 1371 (Council 4).
- Tenure caveat: attribute a documented position only to an official actually in office at the time of the
  vote/statement (Diaz is a former Mayor pre-Nov-2024; do NOT mis-attribute post-Nov-2024 leadership actions
  to him if he was not in that role). If Task 2 replaced any seat with a certified successor, stance the
  successor's record instead of the research-date name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 7 per-official evidence-only stance migrations (one official at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1365_south_tucson_mayor_stances.sql, C:/EV-Accounts/backend/migrations/1366_south_tucson_vice_mayor_stances.sql, C:/EV-Accounts/backend/migrations/1367_south_tucson_acting_mayor_stances.sql, C:/EV-Accounts/backend/migrations/1368_south_tucson_council_1_stances.sql, C:/EV-Accounts/backend/migrations/1369_south_tucson_council_2_stances.sql, C:/EV-Accounts/backend/migrations/1370_south_tucson_council_3_stances.sql, C:/EV-Accounts/backend/migrations/1371_south_tucson_council_4_stances.sql</files>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-PATTERNS.md (§ `1365..1371_south_tucson_<official>_stances.sql` mapping — the per-official stance migration pattern from the Sahuarita 1356 analog: topic-UUID reference comment block, the two-table INSERT (politician_answers + politician_context) per topic with ON CONFLICT DO UPDATE, the 36/36 judicial exclusion, the DELIBERATELY BLANK honest-blank documentation section, one file per official, $$-quoting + ARRAY[...]::text[] sources, tenure caveat; cite non-WAF sources)
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Common Pitfall 8 — the 8 judicial keys to exclude, 36 non-judicial; Pitfall 6 — South 4th Ave vs Historic 4th Ave; § Sources — the reachable southern-AZ news list; § Architecture Patterns diagram — the salient South Tucson issues: public safety/policing, city budget/department funding, Mercado/South-4th-Ave small-business corridor; recall history = background only)
    - C:/EV-Accounts/backend/migrations/1356_sahuarita_mayor_stances.sql (analog — read header + first stance block: topic-UUID table, BEGIN/COMMIT wrapper, two-table INSERT/ON CONFLICT shape, $$-quoted reasoning, ARRAY[...]::text[] sources pattern, DELIBERATELY BLANK section)
  </read_first>
  <action>
    Confirm on-disk MAX (should be 1364 after Plan 02, or 1363 if Plan 02 not yet applied — either way name
    these sequentially disk-MAX+1). Author 7 AUDIT-ONLY migrations, one per official, numbered 1365 Mayor
    (Valenzuela) / 1366 Vice Mayor (Brown-Dominguez) / 1367 Acting Mayor (Robles) / 1368 Council 1 (Jimenez)
    / 1369 Council 2 (Diaz) / 1370 Council 3 (Flagg) / 1371 Council 4 (Aguirre). NONE registered in the
    ledger.

    PER-OFFICIAL CADENCE (save-point / recoverability): work strictly ONE OFFICIAL AT A TIME and treat each
    as an independent save-point. For official N: (1) research N's documented positions, (2) author the file,
    (3) hand off for the orchestrator to apply + assert that ONE file (Task 2 asserts per file), then (4)
    move to the next — 1365 → 1366 → ... → 1371, in order. Do NOT research or author the whole batch in one
    pass. This keeps the quota honored (one at a time) AND makes a mid-plan context degradation recoverable
    at a clean per-official boundary. Record in the SUMMARY which officials are complete so a resumed pass
    knows where to restart.

    Research ONE OFFICIAL AT A TIME (quota — never parallel). For each official, mine the reachable non-WAF
    evidence sources (azluminaria.org, tucsonspotlight.org, tucson.com, tucsonsentinel.com, kgun9.com, and
    Playwright-rendered southtucsonaz.gov pages) for documented positions across the 36 non-judicial live
    topics — with particular attention to the salient South Tucson issues (public safety/policing, city
    budget/department funding, Mercado/South-4th-Ave small-business corridor). For each topic WHERE clear
    evidence exists, emit BOTH: an INSERT into inform.politician_answers (politician_id = the official UUID
    from the Plan 01 manifest, topic_id = the non-judicial topic UUID, value = a discrete 1-5 chair) AND an
    INSERT into inform.politician_context (same keys, reasoning = $$-quoted evidence prose naming the
    vote/statement/date, sources = ARRAY[...]::text[] of the actual non-WAF source URLs — NEVER an empty
    array, NEVER an unfetched URL). Both guarded ON CONFLICT (politician_id, topic_id) DO UPDATE.

    HARD RULES: (1) NEVER emit a row for a topic with no evidence — omit it entirely (honest blank),
    documented in the DELIBERATELY BLANK header section, never a neutral/default value
    (feedback_stance_no_default_value). (2) NEVER touch any of the 8 judicial-* topics — keep them in the
    reference comment block but write NO INSERT for them (Pitfall 8). (3) Every politician_answers row MUST
    have a matching politician_context row with a non-empty sources array (100% cited). (4) Values are
    discrete chairs (1-5), not a polarity scale (feedback_compass_chairs_not_polarity). (5) Attribute a
    vote/position only to officials in office at the time (no pre-tenure/mis-role attribution — esp. Diaz's
    former-Mayor status pre-Nov-2024; recall-era history is background, not current-roster fact).

    Copy the 44-topic UUID reference comment block from the analog into each file's header; the INSERTs skip
    the 8 judicial rows. Wrap each file in BEGIN/COMMIT. Do NOT add a ledger-registration INSERT.
  </action>
  <verify>
    <automated>for f in 1365_south_tucson_mayor 1366_south_tucson_vice_mayor 1367_south_tucson_acting_mayor 1368_south_tucson_council_1 1369_south_tucson_council_2 1370_south_tucson_council_3 1371_south_tucson_council_4; do F="C:/EV-Accounts/backend/migrations/${f}_stances.sql"; test -f "$F" && grep -qF "politician_answers" "$F" && grep -qF "politician_context" "$F" && ! grep -qiE "INSERT INTO inform.politician_answers[^;]*judicial-" "$F" && ! grep -qE "INSERT INTO supabase_migrations" "$F" || { echo "FAIL $F"; exit 1; }; done; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist (1365 Mayor + 1366 Vice Mayor + 1367 Acting Mayor + 1368-1371 Council 1-4, disk-MAX+1 sequential names).
    - Each file contains both `inform.politician_answers` and `inform.politician_context` INSERTs.
    - NO INSERT references any `judicial-*` topic (the 8 judicial keys appear ONLY in the reference comment, never in an INSERT).
    - Every politician_answers INSERT has a matching politician_context INSERT with a non-empty `ARRAY[...]::text[]` sources value (no empty arrays); no unfetched URL cited as evidence.
    - NO neutral-default rows — every emitted row is evidence-backed; topics without evidence are absent (documented in the DELIBERATELY BLANK section).
    - NONE of the 7 files adds an `INSERT INTO supabase_migrations` line (audit-only).
    - Each file wrapped in BEGIN/COMMIT; each keyed to the correct official UUID from the Plan 01 manifest.
    - Files authored one official at a time (1365→1371), each a recoverable save-point; SUMMARY records per-official completion order.
  </acceptance_criteria>
  <done>7 audit-only stance migrations authored, one per official (1365→1371 in sequence, each a recoverable save-point), evidence-only against 36 non-judicial topics, 100% cited from non-WAF sources, honest blanks, discrete chairs, no defaults, no pre-tenure/mis-role attribution, recall history kept as background — unregistered.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the 7 stance migrations + assert evidence-only integrity (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator applies existing migrations + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/198-south-tucson-deep-seed/198-VALIDATION.md (Per-Task Verification Map — stances row: 100% cited, no default rows, honest blanks)
    - .planning/phases/198-south-tucson-deep-seed/198-RESEARCH.md (§ Common Pitfall 8 — 36 not 44; a "44 per official" check would wrongly flag complete coverage)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB access). BLOCKING apply-and-assert: Plan 04's coverage chip sets
    hasContext:true ONLY because these stance rows exist — so this apply must land (and assert green) before
    Plan 04's coverage.js edit. Apply the 7 migrations IN ORDER as each official's file lands (per the
    per-official cadence in Task 1) via
    `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1365_south_tucson_mayor_stances.sql` (etc.
    through 1371) — applying + asserting each file before the next official keeps the per-official save-point
    clean. Then assert the aggregate via `psql -tAc`, recording in 198-03-SUMMARY.md: (a) every
    politician_answers row for the 7 South Tucson UUIDs has a matching politician_context row (0 orphans); (b)
    0 politician_context rows with a NULL or empty sources array (100% cited); (c) 0 politician_answers rows
    on any judicial-* topic for the 7 officials; (d) all values in [1.0, 5.0]; (e) per-official stance counts
    (informational — expected to vary; honest blanks are fine, do NOT assert a fixed count). Any integrity
    failure (a-d) blocks the phase. Commit the 7 migrations via `git -C "C:/EV-Accounts"`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id IN (-4015001,-4015002,-4015003,-4015004,-4015005,-4015006,-4015007) AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id)) AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc JOIN essentials.politicians p ON p.id=pc.politician_id WHERE p.external_id IN (-4015001,-4015002,-4015003,-4015004,-4015005,-4015006,-4015007) AND (pc.sources IS NULL OR array_length(pc.sources,1) IS NULL)) AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN inform.compass_topics t ON t.id=pa.topic_id WHERE p.external_id IN (-4015001,-4015002,-4015003,-4015004,-4015005,-4015006,-4015007) AND t.topic_key LIKE 'judicial-%') AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id WHERE p.external_id IN (-4015001,-4015002,-4015003,-4015004,-4015005,-4015006,-4015007) AND (pa.value < 1.0 OR pa.value > 5.0))"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 migrations apply cleanly.
    - 0 politician_answers rows without a matching politician_context row (every answer cited).
    - 0 politician_context rows with a NULL/empty sources array (100% cited).
    - 0 stance rows on any `judicial-*` topic for the 7 officials.
    - All stance values in [1.0, 5.0].
    - The combined boolean SELECT returns `t`.
    - Per-official counts recorded in the SUMMARY (informational; honest blanks expected, no fixed count asserted).
    - 7 migrations committed to `C:/EV-Accounts`; none registered in the ledger.
  </acceptance_criteria>
  <done>Evidence-only stances live for the 7 officials: 100% cited from non-WAF sources, no defaults, no judicial rows, honest blanks, no pre-tenure/mis-role attribution — audit migrations applied and unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stance migration files → production DB | Hand-authored evidence-only SQL crosses into live `inform` schema via `psql -f` |
| research sources → stance values | External news evidence maps to a 1-5 chair; unsourced/mis-scoped values corrupt the compass |

## STRIDE Threat Register

Honest scope: hand-authored evidence-only INSERTs into `inform` (data-seeding phase, no runtime/request
surface). Genuine threats are citation integrity and mis-scoped/pre-tenure attribution — not injection or
access control. No HIGH-severity threat; none fabricated. Mirrors the Sahuarita 197 threat model.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-CITE | Tampering | uncited / default stance rows | mitigate | Every answer requires a matching context row with a non-empty sources array; Task 2 asserts 0 orphans + 0 empty-sources; no-evidence topics omitted (honest blank), never defaulted |
| T-198-JUD | Tampering | judicial topics on a non-judicial office | mitigate | 8 judicial-* keys excluded from all INSERTs; Task 2 asserts 0 judicial rows for the 7 officials |
| T-198-SRC | Repudiation | citing an unfetched URL / wrong district | mitigate | Cite only actually-fetched reachable non-WAF sources (azluminaria.org / tucsonspotlight.org / tucson.com / tucsonsentinel.com / kgun9.com / Playwright-rendered southtucsonaz.gov), never an unverified URL; confirm South-4th-Ave vs Historic-4th-Ave (Pitfall 6) |
| T-198-TENURE | Spoofing | pre-tenure / mis-role / recall-era attribution | mitigate | Attribute a position only to officials in office/role at the time; Diaz's former-Mayor status handled correctly; South Tucson's recall history kept as background, not current-roster fact |
| T-198-KEY | Information disclosure | DATABASE_URL leakage | mitigate | Read only from gitignored `.env`; applied by the inline orchestrator |
| T-198-SC | Tampering | package installs | accept | No new installs — reuses installed psql; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Every stance row 100% cited (matching context row + non-empty sources); 0 orphans.
- 0 judicial-* rows; 0 neutral-default rows; values in [1.0, 5.0]; no pre-tenure/mis-role attribution.
- Researched one official at a time (1365→1371, per-official save-points); 7 audit migrations applied and unregistered.
</verification>

<success_criteria>
SUB-04 / ROADMAP #3 (evidence-only compass stances, 100% cited, no defaults, honest blanks) is TRUE in
production for all 7 officials against the 36 non-judicial topics.
</success_criteria>

<output>
Create `.planning/phases/198-south-tucson-deep-seed/198-03-SUMMARY.md` when done — include per-official stance
counts, notable honest blanks (esp. on the salient public-safety/budget/Mercado-corridor topics), the
per-official completion order (save-point trail), and the evidence sources used.
</output>
