---
phase: 128-carson-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/719_davis_holmes_stances.sql
  - C:/EV-Accounts/backend/migrations/720_hilton_stances.sql
autonomous: true
requirements: [CARSON-01]
must_haves:
  truths:
    - "Wave 0 pre-flight [BLOCKING] confirms actual next migration number (expected 719 but MUST verify against DB-applied MAX), resolves all 5 Carson target UUIDs, confirms active topic count is 44, confirms excluded officials' UUIDs (Bradshaw -700305, Cooper -700306), and records any pre-existing Carson stance counts"
    - "Lula Davis-Holmes (Mayor, external_id -700300) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Jawane Hilton (D1, external_id -700301) has evidence-only stance rows written, applied, and verified — or a zero-INSERT ledger file if no public record found"
    - "Every stance value written has a paired context row with at least one path-bearing source URL"
    - "Khaleah K. Bradshaw (external_id -700305) and Monica Cooper (external_id -700306) receive ZERO stance rows — explicitly excluded per CARSON-01"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/719_davis_holmes_stances.sql"
      provides: "Lula Davis-Holmes stance migration (NNN confirmed by Wave 0 — expected 719)"
      contains: "BEGIN;"
    - path: "C:/EV-Accounts/backend/migrations/720_hilton_stances.sql"
      provides: "Jawane Hilton stance migration"
      contains: "BEGIN;"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "paired INSERT per topic (politician_id, topic_id)"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
---

<objective>
Run Wave 0 pre-flight verification [BLOCKING], then research and apply evidence-only compass stances for the first 2 Carson officials (Mayor Lula Davis-Holmes, Council Member District 1 Jawane Hilton), one politician at a time.

Purpose: Establish the verified migration starting point, active topic count, all 5 Carson target UUIDs, and both excluded officials' UUIDs for the entire phase; begin closing CARSON-01.
Output: 2 per-individual stance migration files (719 through 720), each applied to production and verified. All 5 target UUIDs + 2 exclusion UUIDs recorded in SUMMARY for Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-carson-stances/128-RESEARCH.md

<interfaces>
<!-- Canonical stance migration contract. Copy verbatim. Source: 282_md_exec_stances.sql + 718_wells_stances.sql -->

Per-topic paired INSERT (politician_answers FIRST, then politician_context):
  INSERT INTO inform.politician_answers (politician_id, topic_id, value)
  VALUES ('{uuid}', '{topic_uuid}', {N}.0)
  ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;

  INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
  VALUES ('{uuid}', '{topic_uuid}', $$reasoning$$,
          ARRAY['https://host/path']::text[]::text[])
  ON CONFLICT (politician_id, topic_id)
  DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

Hard rules:
- value is float literal (4.0), NEVER bare integer (4), NEVER string ('4')
- sources double-cast ::text[]::text[] (project standard from 282), never single cast
- wrap whole file in BEGIN; ... COMMIT;
- reasoning uses plain $$...$$ dollar-quoting (never named $body$)
- at least 1 source URL, must include a path segment (no bare domains)
- NO row written for a topic with no evidence — blank spoke is correct, never default 3.0
- If an official has zero findable stances, still create the migration file with header + BEGIN;/COMMIT; + zero INSERTs (Phase 115 ledger-consistency precedent)
- CRITICAL: Lula Davis-Holmes IS the directly-elected Mayor (LOCAL_EXEC office, -700300). "Mayor Davis-Holmes" is CORRECT in reasoning text — do NOT add any "rotational" qualifier.
- CRITICAL: Khaleah K. Bradshaw (-700305, City Clerk) and Monica Cooper (-700306, City Treasurer) are EXCLUDED. NEVER write any INSERT with Bradshaw's or Cooper's UUID.
</interfaces>

Apply path: psql CLI with DATABASE_URL from C:/EV-Accounts/backend/.env (NOT C:/EV-Accounts/.env — that path does not exist). Phase 126 confirmed mcp__supabase-local is NOT callable from the bash executor context — use psql CLI as the primary apply and query path. Command form: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/NNN_name_stances.sql` to apply, and `psql "$DATABASE_URL" -c "<query>"` for verification queries. Load DATABASE_URL from C:/EV-Accounts/backend/.env first (do NOT run git commands in C:/EV-Accounts — it is not a git repo).

Carson roster (external_id → expected migration), from DB query [VERIFIED 2026-06-16]:
  -700300 Lula Davis-Holmes (Mayor, LOCAL_EXEC — directly elected)  → 719  [this plan]
  -700301 Jawane Hilton (D1, LOCAL)                                 → 720  [this plan]
  -700302 Jim Dear (D2, LOCAL)                                      → 721  [Plan 02]
  -700303 Cedric L. Hicks Sr. (D3, LOCAL)                          → 722  [Plan 02]
  -700304 Arleen B. Rojas (D4, LOCAL)                              → 723  [Plan 02]
  -700305 Khaleah K. Bradshaw (City Clerk)                         → EXCLUDED — no stances ever
  -700306 Monica Cooper (City Treasurer)                           → EXCLUDED — no stances ever

NNN = actual next migration confirmed at Wave 0. Expected 719 (718_wells_stances.sql is highest applied per DB MAX; STATE.md says next migration 719), but Wave 0 MUST verify the DB-applied MAX.

Out-of-scope guard: Do NOT write stances for any external_id outside the target set {-700300, -700301, -700302, -700303, -700304}. NEVER write for -700305 (Bradshaw) or -700306 (Cooper). Do NOT create any new offices, districts, or chambers for Carson — all Carson offices already exist.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 pre-flight verification [BLOCKING]</name>
  <files>(no files written — verification queries only)</files>
  <read_first>
    - .planning/phases/128-carson-stances/128-RESEARCH.md (Migration Numbering section + Wave 0 Pre-Flight Queries section + Pitfall 1 integer-cast filter + Pitfall 7 directly-elected Mayor + Pitfall 8 excluded officials + Pitfall 11 .env path)
    - .planning/STATE.md (Current Position section — confirms "next migration: 719")
    - C:/EV-Accounts/backend/.env (read DATABASE_URL for psql CLI; do NOT run git in C:/EV-Accounts)
  </read_first>
  <action>
    Load DATABASE_URL from C:/EV-Accounts/backend/.env, then run all 8 Wave 0 pre-flight queries via `psql "$DATABASE_URL" -c "<query>"` before writing any migration. Do not skip any query — this task is BLOCKING for all subsequent tasks.

    Query 1 — Confirm highest applied integer migration (determines actual starting number). MUST include the integer-format filter (Pitfall 1 — bare version::int overflows on timestamp-format versions):
    `SELECT MAX(version::int) AS max_applied FROM supabase_migrations.schema_migrations WHERE version ~ '^[0-9]{3}$';`
    If MAX = 718, next migration = 719. If MAX is higher, use MAX+1. Record the confirmed starting number (NNN). NOTE: some migrations may appear twice on disk; rely on the DB-applied MAX, not the disk listing.

    Query 2 — Confirm 716-718 are specifically applied:
    `SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('716', '717', '718') AND version ~ '^[0-9]{3}$' ORDER BY version;`
    Expected: 3 rows. If fewer rows, note which migrations are missing — this changes the starting number.

    Query 3 — Confirm active topic count (must be 44):
    `SELECT COUNT(*) FROM inform.compass_topics WHERE is_active = true;`
    If count differs from 44, run `SELECT topic_key, id FROM inform.compass_topics WHERE is_active = true ORDER BY topic_key;` and capture the live list — do NOT use the cached UUID reference block from 128-RESEARCH.md if the count differs. NOTE: column is topic_key, NOT slug (Pitfall 6).

    Query 4 — Resolve all 5 Carson target UUIDs (non-contiguous external_ids — use an IN list, NOT a BETWEEN range):
    `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id IN (-700300, -700301, -700302, -700303, -700304) ORDER BY external_id;`
    Expected 5 rows: -700300 Lula Davis-Holmes, -700301 Jawane Hilton, -700302 Jim Dear, -700303 Cedric L. Hicks Sr., -700304 Arleen B. Rojas. Capture all 5 UUIDs and the exact stored full_name for each (especially middle initials: "Hicks Sr." suffix, "Rojas" middle initial check) for use in this plan and Plan 02.

    Query 5 — Confirm excluded officials' UUIDs (informational — to EXCLUDE from all stances):
    `SELECT external_id, full_name, id FROM essentials.politicians WHERE external_id IN (-700305, -700306);`
    Expected: 2 rows: Khaleah K. Bradshaw (City Clerk) and Monica Cooper (City Treasurer). Record both UUIDs in SUMMARY explicitly as the DO-NOT-WRITE UUIDs.

    Query 6 — Check for pre-existing Carson stance rows (informational; upsert handles them):
    `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-700300, -700301, -700302, -700303, -700304));`

    Query 7 — Confirm Davis-Holmes Mayor office exists as LOCAL_EXEC (directly elected — confirms she is NOT rotational):
    `SELECT d.district_type, o.title, p.full_name FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id JOIN essentials.politicians p ON p.id = o.politician_id WHERE p.external_id = -700300;`
    Expected: district_type=LOCAL_EXEC, title='Mayor', full_name=Lula Davis-Holmes. This confirms "Mayor Davis-Holmes" is correct (directly elected, not rotational). If district_type is not LOCAL_EXEC, document the deviation but proceed (the UUID is what matters for INSERTs).

    Query 8 — Confirm Carson City Council chamber exists:
    `SELECT c.name FROM essentials.chambers c JOIN essentials.governments g ON g.id = c.government_id WHERE g.geo_id = '0611530';`
    Expected: rows for Mayor, City Council, City Clerk, City Treasurer chambers.

    Record all query results in the plan SUMMARY. The confirmed migration starting number (NNN) is the authoritative value for all 5 migration filenames in this phase. Record both excluded UUIDs explicitly.
  </action>
  <verify>
    <automated>All 8 queries return results via psql CLI. Starting migration number recorded (expected 719). Topic count confirmed. All 5 Carson target UUIDs resolved. Both excluded officials' UUIDs recorded as DO-NOT-WRITE.</automated>
  </verify>
  <acceptance_criteria>
    - MAX(version::int) query (with `WHERE version ~ '^[0-9]{3}$'` filter) returns a value >= 718; starting migration number (NNN) confirmed and recorded
    - Queries 1+2 results are consistent (if 716-718 all applied, MAX=718 and 3 rows returned)
    - Active topic count is confirmed (44 expected; if different, live topic_key→id list captured)
    - All 5 target UUIDs resolved: -700300 Davis-Holmes, -700301 Hilton, -700302 Dear, -700303 Hicks, -700304 Rojas — names and external_ids match roster; exact stored full_names recorded
    - Both excluded officials' UUIDs (-700305 Bradshaw, -700306 Cooper) recorded in SUMMARY as explicit DO-NOT-WRITE exclusions
    - Pre-existing stance count recorded (informational)
    - Davis-Holmes Mayor district confirmed as LOCAL_EXEC with title 'Mayor' (or deviation documented)
    - Carson chambers query returns rows including City Council
    - All 5 target UUIDs + 2 exclusion UUIDs recorded in SUMMARY for Plan 02
  </acceptance_criteria>
  <done>Pre-flight queries run via psql CLI; actual starting migration number (NNN) confirmed, topic count confirmed, all 5 Carson target UUIDs + 2 exclusion UUIDs resolved and recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Lula Davis-Holmes (Mayor) stances — migration 719</name>
  <files>C:/EV-Accounts/backend/migrations/719_davis_holmes_stances.sql</files>
  <read_first>
    - .planning/phases/128-carson-stances/128-RESEARCH.md (Lula Davis-Holmes section + Source Map + Anti-Patterns + Pitfalls 1-11 + Pitfall 7 directly-elected Mayor + Pitfall 8 excluded officials + Topic UUID Reference Block)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (canonical format: BEGIN/COMMIT, float literals, dollar-quoting, double-cast sources)
    - C:/EV-Accounts/backend/migrations/718_wells_stances.sql (most recent canonical per-local-official format, same city tier)
  </read_first>
  <action>
    Process Lula Davis-Holmes (external_id -700300, UUID resolved in Task 1, directly-elected Mayor, LOCAL_EXEC office) strictly after Task 1 completes. Research her public record across all 44 active compass topics, then write, apply, and verify her migration before any other official is processed.

    (a) Research: Search "Lula Davis-Holmes Carson Mayor" across carsonca.gov/government/mayor, dailybreeze.com, precinctreporter.com, latimes.com, ballotpedia.org, carson.patch.com. Davis-Holmes is the directly elected Mayor — expected yield 5–9 stances. High-probability topics for Carson (RESEARCH.md per-official notes): homelessness-response (encampment enforcement), housing (RHNA compliance, ADU policies), public-safety-approach (Carson PD), economic-development (Amazon logistics debates, stadium area), local-environment (Tesoro/Alon refinery pollution), growth-and-development (new developments), taxes (utility tax, business license), transportation-priorities (I-405 corridor). Place each topic with direct evidence on the 1.0-5.0 scale per that topic's axis definition (1=most progressive, 5=most conservative). No INSERT for any topic with no direct evidence — low-probability topics (ukraine-support, judicial-*, tariffs, school-vouchers, redistricting) will most likely be blank.

    (b) Write migration 719_davis_holmes_stances.sql (NNN = confirmed number from Task 1, expected 719) using the canonical template:
    - Header comment block: "Migration 719: Lula Davis-Holmes Stances"
    - Full 44-topic UUID reference block from 128-RESEARCH.md Topic UUID Reference Block (or live list if Task 1 count differed)
    - BEGIN;
    - One politician_answers INSERT + one politician_context INSERT per evidenced topic, using exactly the paired-INSERT contract from <interfaces>
    - value = float literal (e.g. 4.0) — NEVER bare integer or string
    - sources = ARRAY['https://dailybreeze.com/path/specific-article']::text[]::text[] — double-cast, path-bearing URLs (no bare domains)
    - reasoning = $$....$$ dollar-quoting, 2-5 sentences citing specific vote/statement/action
    - Reasoning text uses "Mayor Davis-Holmes" — this is CORRECT (directly elected, not rotational; do NOT add any "rotational" qualifier — Pitfall 7)
    - COMMIT;
    - No INSERT INTO essentials.offices, essentials.districts, or essentials.chambers (Davis-Holmes's Mayor office already exists)

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/719_davis_holmes_stances.sql` (DATABASE_URL from C:/EV-Accounts/backend/.env).

    (d) Verify immediately (before Task 3), substituting Davis-Holmes's UUID from Task 1:
    Row count: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{davis_holmes_uuid}';"`
    Unpaired check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{davis_holmes_uuid}' AND pc.politician_id IS NULL;"` — must return 0
    Citation check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{davis_holmes_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` — must return 0
  </action>
  <verify>
    <automated>Unpaired check returns 0 for Davis-Holmes's UUID. Citation check returns 0 for Davis-Holmes's UUID.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file 719_davis_holmes_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0` (grep: no `, [1-5]\)` without `.0`)
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Davis-Holmes's UUID
    - Uncited-context query returns 0 for Davis-Holmes's UUID
    - Reasoning text contains "Mayor Davis-Holmes" with NO "rotational" qualifier (she is directly elected)
    - File contains no INSERT INTO essentials.offices, essentials.districts, or essentials.chambers
    - No INSERT references external_id -700305 or -700306 (excluded officials) or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration 719_davis_holmes_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Mayor Lula Davis-Holmes.</done>
</task>

<task type="auto">
  <name>Task 3: Jawane Hilton (District 1) stances — migration 720</name>
  <files>C:/EV-Accounts/backend/migrations/720_hilton_stances.sql</files>
  <read_first>
    - .planning/phases/128-carson-stances/128-RESEARCH.md (Jawane Hilton section + Anti-Patterns + Pitfalls 1-11 + Pitfall 8 excluded officials)
    - C:/EV-Accounts/backend/migrations/719_davis_holmes_stances.sql (just-written sibling file as in-phase format reference)
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (double-cast authority)
  </read_first>
  <action>
    Process Jawane Hilton (external_id -700301, UUID from Task 1, Council Member District 1) strictly after Task 2 completes — never in parallel (rate-limit rule). Research, write, apply, and verify.

    (a) Research: Search "Jawane Hilton Carson council" and "Jawane Hilton Carson District 1" on carsonca.gov/government/city-council/city-council-agendas-and-minutes, dailybreeze.com, carson.patch.com, ballotpedia.org. Hilton represents D1 — expected yield 4–7 stances. High-probability topics: housing/residential-zoning (D1 neighborhoods, ADU policies), homelessness-response (regional camp clearance), public-safety-approach (Carson PD votes), local-environment (industrial zones near D1), economic-development (D1 commercial corridors). Place each evidenced topic on the 1.0-5.0 scale per that topic's axis definition. No INSERT for any topic with no direct evidence. If zero evidence found, still write the migration file with header + BEGIN;/COMMIT; + zero INSERT blocks (ledger-consistency precedent) and note in SUMMARY.

    (b) Write migration 720_hilton_stances.sql (expected 720). Same canonical format as Task 2:
    - Header: "Migration 720: Jawane Hilton Stances"
    - Full 44-topic UUID reference block
    - BEGIN;
    - paired politician_answers + politician_context INSERT per evidenced topic
    - value = float literal; sources = ARRAY[...]::text[]::text[] double-cast, path-bearing URLs; reasoning = $$...$$ dollar-quoting citing specific vote/statement/action
    - Reasoning text uses "Council Member Hilton" (he is a council member, not the Mayor)
    - COMMIT;

    (c) Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/720_hilton_stances.sql`.

    (d) Verify immediately, substituting Hilton's UUID:
    Row count: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{hilton_uuid}';"`
    Unpaired check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id='{hilton_uuid}' AND pc.politician_id IS NULL;"` — must return 0
    Citation check: `psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context WHERE politician_id='{hilton_uuid}' AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` — must return 0
  </action>
  <verify>
    <automated>For Hilton's UUID: unpaired check returns 0 AND citation check returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - Migration file 720_hilton_stances.sql exists at C:/EV-Accounts/backend/migrations/
    - File is wrapped in BEGIN;/COMMIT;
    - Every politician_answers value is a float literal with `.0`
    - Every sources array uses `::text[]::text[]` double-cast
    - Unpaired-answer query returns 0 for Hilton's UUID
    - Uncited-context query returns 0 for Hilton's UUID
    - Reasoning text uses "Council Member Hilton"
    - No INSERT references excluded officials (-700305 Bradshaw, -700306 Cooper) or any UUID outside the 5-target set
    - Row count recorded in SUMMARY
  </acceptance_criteria>
  <done>Migration 720_hilton_stances.sql written, applied to production via psql CLI, and verified (0 unpaired, 0 uncited) for Council Member Jawane Hilton.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent → migration SQL | Researched evidence converted to SQL values/reasoning; risk of fabricated/uncited claims |
| migration SQL → production DB (psql CLI / live Supabase) | Writes go directly to live production Supabase via DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | stance value integrity | mitigate | Evidence-only rule: no INSERT without a direct cited source; per-person uncited-context query must return 0 |
| T-128-02 | Information Disclosure | reasoning text / sources | accept | All data is public-record political positions with public source URLs; no PII |
| T-128-03 | Repudiation | stance provenance | mitigate | Every value paired with a context row carrying path-bearing source URLs (100% citation rate) |
| T-128-04 | Tampering | writing stances for excluded Bradshaw (-700305) or Cooper (-700306) | mitigate | Acceptance criteria assert no INSERT references excluded UUIDs; Wave 0 records both exclusion UUIDs explicitly |
| T-128-05 | Tampering | stale migration number collision | mitigate | Wave 0 Query 1+2 confirms actual DB-applied MAX (with integer-format filter) before any file is written |
| T-128-06 | Tampering | unnecessary Mayor office creation | mitigate | Davis-Holmes's LOCAL_EXEC Mayor office already exists (Wave 0 Query 7); acceptance criteria assert no INSERT into essentials.offices/districts/chambers |
| T-128-07 | Tampering | wrong .env path | mitigate | RESEARCH.md Pitfall 11: correct path is C:/EV-Accounts/backend/.env, NOT C:/EV-Accounts/.env |
| T-128-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
After all three tasks, run the partial phase-wide citation check for this plan's officials (Davis-Holmes + Hilton):
`psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_context pc WHERE pc.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id IN (-700300, -700301)) AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);"` must return 0.
</verification>

<success_criteria>
- Wave 0 pre-flight complete; actual starting migration number (NNN) confirmed (BLOCKING gate passed)
- Active topic count confirmed as 44 (or live list captured)
- All 5 Carson target UUIDs + 2 exclusion UUIDs resolved and recorded in SUMMARY for Plan 02
- Davis-Holmes's LOCAL_EXEC Mayor district confirmed (directly elected — "Mayor Davis-Holmes" correct)
- Migrations 719, 720 (2 officials: Davis-Holmes, Hilton) written, applied, and verified
- 0 unpaired answers, 0 uncited contexts across both officials
- No INSERT references Bradshaw (-700305) or Cooper (-700306)
- No external_ids outside the 5-target set touched
- Blank-spoke topics documented in SUMMARY (never defaulted to 3.0)
</success_criteria>

<output>
Create `.planning/phases/128-carson-stances/128-01-SUMMARY.md` when done. Record: confirmed starting migration number (NNN), active topic count, all 5 Carson target UUIDs + exact stored full_names (for Plan 02), both excluded officials' UUIDs, per-official stance counts for Davis-Holmes/Hilton, and any blank-spoke topics.
</output>
