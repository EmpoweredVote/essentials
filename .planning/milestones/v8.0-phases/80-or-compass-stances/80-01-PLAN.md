---
phase: 80-or-compass-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-tina-kotek.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-dan-rayfield.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-tobias-read.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-elizabeth-steiner.csv
  - C:/EV-Accounts/backend/data/stance-research/2026-05-30-christina-stephenson.csv
  - C:/EV-Accounts/backend/scripts/apply-tina-kotek-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-dan-rayfield-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-tobias-read-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-elizabeth-steiner-stances.ts
  - C:/EV-Accounts/backend/scripts/apply-christina-stephenson-stances.ts
autonomous: true
requirements:
  - SC-1
  - SC-3

must_haves:
  truths:
    - "Tina Kotek profile renders the compass widget"
    - "All 5 OR constitutional officers with public record have ≥1 row in inform.politician_answers"
    - "Every research + apply ran sequentially — never two research agents at once (D-05)"
    - "Every stance value is an integer 1–5 (D-07)"
    - "Every stance row has a citation in inform.politician_context (D-06)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/apply-tina-kotek-stances.ts"
      provides: "CSV→DB apply script for Tina Kotek stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-dan-rayfield-stances.ts"
      provides: "CSV→DB apply script for Dan Rayfield stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-tobias-read-stances.ts"
      provides: "CSV→DB apply script for Tobias Read stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-elizabeth-steiner-stances.ts"
      provides: "CSV→DB apply script for Elizabeth Steiner stances"
    - path: "C:/EV-Accounts/backend/scripts/apply-christina-stephenson-stances.ts"
      provides: "CSV→DB apply script for Christina Stephenson stances"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-tina-kotek.csv"
      provides: "Kotek stance research CSV (politician_id, topic_id, topic_key, value, notes)"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-dan-rayfield.csv"
      provides: "Rayfield stance research CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-tobias-read.csv"
      provides: "Read stance research CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-elizabeth-steiner.csv"
      provides: "Steiner stance research CSV"
    - path: "C:/EV-Accounts/backend/data/stance-research/2026-05-30-christina-stephenson.csv"
      provides: "Stephenson stance research CSV"
  key_links:
    - from: "apply-*-stances.ts"
      to: "inform.politician_answers"
      via: "INSERT ... ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "INSERT INTO inform.politician_answers"
    - from: "inform.politician_answers row"
      to: "inform.politician_context row"
      via: "shared (politician_id, topic_id) citation"
      pattern: "INSERT INTO inform.politician_context"
---

<objective>
Research and ingest compass stances for the 5 Oregon constitutional officers (Tina Kotek, Dan Rayfield, Tobias Read, Elizabeth Steiner, Christina Stephenson) so each has ≥1 row in `inform.politician_answers` and the compass widget renders on their profiles.

Purpose: Closes SC-1 (constitutional officers with verifiable public record have ≥1 stance) and contributes to SC-3 (one-at-a-time ingestion rule) and SC-4 (compass renders on at least one OR official). Establishes the ingestion rhythm for Plans 02 and 03.

Output:
- 5 CSV files in `C:/EV-Accounts/backend/data/stance-research/`
- 5 apply scripts in `C:/EV-Accounts/backend/scripts/`
- New rows in `inform.politician_answers` and `inform.politician_context` for the 5 officials
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-or-compass-stances/80-CONTEXT.md
@.planning/phases/80-or-compass-stances/80-RESEARCH.md
@.planning/phases/80-or-compass-stances/80-VALIDATION.md
@C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
@.planning/phases/74-or-executives-federal/74-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research + ingest Tina Kotek (Governor) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-tina-kotek.csv
    C:/EV-Accounts/backend/scripts/apply-tina-kotek-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): This is a SINGLE research run. Do not spawn parallel research agents inside this task. The next politician must not start until Kotek's apply script has logged "Done — Upserted: N, Skipped: M".

    Step 1 — Research Tina Kotek (politician_id UUID `66c3bd97-94d1-4287-b1b8-86605a38cb97`, external_id -4100001, Governor of Oregon). Run ONE research agent (claude-haiku or claude-sonnet) using the evidence-only rule from D-06 across ALL 36 active compass topics from CONTEXT.md (D-08 — not just LOCAL-scope). Primary sources: oregonlive.com, ballotpedia.org, OR legislature vote history (Kotek was OR House Speaker), governor.oregon.gov press releases, campaign archive. Aim for 18–21+ stances. Values are integers 1=progressive .. 5=conservative per D-07. Skip topics with no citable public record (D-06).

    Step 2 — Write CSV to `C:/EV-Accounts/backend/data/stance-research/2026-05-30-tina-kotek.csv` with header `politician_id,topic_id,topic_key,value,notes`. Every row uses politician_id=`66c3bd97-94d1-4287-b1b8-86605a38cb97`. Notes column must contain a citation (URL or bill number + brief reasoning). Omit rows with no evidence — never fabricate a value (D-06).

    Step 3 — Copy `C:/EV-Accounts/backend/scripts/apply-allen-stances.ts` to `C:/EV-Accounts/backend/scripts/apply-tina-kotek-stances.ts`. Change the `csvPath` to point at `2026-05-30-tina-kotek.csv`. KEEP `parseInt(r.value)` unchanged (NOT `3 - parseInt(r.value)` — that is the apply-solis-stances outlier flagged in 80-RESEARCH.md Pitfall 2).

    Step 4 — Run the apply script from `C:/EV-Accounts/backend/`: `npx tsx scripts/apply-tina-kotek-stances.ts`. Capture stdout — must show `Done — Upserted: N, Skipped: M`.

    Step 5 — Write citation rows by calling `POST /api/compass/politicians/context` (or the equivalent inform.politician_context INSERT) for each upserted answer using the `notes` field as the citation source. Every politician_answers row needs a matching politician_context row (D-06).

    Implements D-01, D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '66c3bd97-94d1-4287-b1b8-86605a38cb97'` returns ≥ 10. Then `SELECT DISTINCT value FROM inform.politician_answers WHERE politician_id = '66c3bd97-94d1-4287-b1b8-86605a38cb97'` returns only integers in {1,2,3,4,5}. Then `SELECT COUNT(*) FROM inform.politician_answers pa WHERE pa.politician_id='66c3bd97-94d1-4287-b1b8-86605a38cb97' AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id)` returns 0.</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/data/stance-research/2026-05-30-tina-kotek.csv` exists with header `politician_id,topic_id,topic_key,value,notes` and ≥ 10 data rows
    - Every data row's `politician_id` column equals `66c3bd97-94d1-4287-b1b8-86605a38cb97`
    - Every data row's `value` is an integer 1–5; no blanks, no decimals, no out-of-range
    - File `C:/EV-Accounts/backend/scripts/apply-tina-kotek-stances.ts` exists; `grep "parseInt(r.value)"` matches and `grep "3 - parseInt"` does NOT match
    - `npx tsx scripts/apply-tina-kotek-stances.ts` stdout ends with `Done — Upserted: N, Skipped: M` where N ≥ 10
    - SQL: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '66c3bd97-94d1-4287-b1b8-86605a38cb97'` returns ≥ 10
    - SQL: `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = '66c3bd97-94d1-4287-b1b8-86605a38cb97'` matches the answer count (every answer has a citation)
  </acceptance_criteria>
  <done>Tina Kotek has ≥10 evidence-cited compass stances in `inform.politician_answers`, each with a `politician_context` citation; this task fully ran before Task 2 begins (D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Research + ingest Dan Rayfield (Attorney General) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-dan-rayfield.csv
    C:/EV-Accounts/backend/scripts/apply-dan-rayfield-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
    C:/EV-Accounts/backend/scripts/apply-tina-kotek-stances.ts
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): This task starts ONLY after Task 1's apply script printed `Done — Upserted: N, Skipped: M`. Do NOT launch in parallel with any other research task. One research agent only.

    Step 1 — Research Dan Rayfield (politician_id UUID `15dbbf1b-da3d-4fb9-8fc5-67b734e7979e`, external_id -4100002, Oregon Attorney General; former OR House Speaker). Run ONE research agent across ALL 36 compass topics per D-08. Primary sources: ballotpedia.org, doj.oregon.gov press releases, OR legislature vote records, campaign site archives. Per RESEARCH.md Open Question #2, attempt judicial topics (bail reform, prosecutorial discretion, criminal justice) but skip `judicial-transparency` (judges-only). Aim for 15+ stances given his extensive Speaker tenure.

    Step 2 — Write CSV to `C:/EV-Accounts/backend/data/stance-research/2026-05-30-dan-rayfield.csv`. politician_id column = `15dbbf1b-da3d-4fb9-8fc5-67b734e7979e` on every row. Header + format identical to Kotek CSV.

    Step 3 — Copy `apply-tina-kotek-stances.ts` to `apply-dan-rayfield-stances.ts`. Update `csvPath` to `2026-05-30-dan-rayfield.csv`. Preserve `parseInt(r.value)` (D-07 — no inversion).

    Step 4 — Run `npx tsx scripts/apply-dan-rayfield-stances.ts` from `C:/EV-Accounts/backend/`. Wait for `Done — Upserted: N, Skipped: M` before starting Task 3.

    Step 5 — Write `inform.politician_context` citation rows for every upserted answer (D-06).

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '15dbbf1b-da3d-4fb9-8fc5-67b734e7979e'` returns ≥ 5. Then `SELECT MIN(value), MAX(value) FROM inform.politician_answers WHERE politician_id = '15dbbf1b-da3d-4fb9-8fc5-67b734e7979e'` returns MIN ≥ 1 AND MAX ≤ 5.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-dan-rayfield.csv` exists; ≥ 5 data rows; every politician_id column = `15dbbf1b-da3d-4fb9-8fc5-67b734e7979e`
    - File `apply-dan-rayfield-stances.ts` exists with `parseInt(r.value)` and no `3 - parseInt` inversion
    - `npx tsx scripts/apply-dan-rayfield-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 5)
    - SQL: stance count for Rayfield ≥ 5; all values integers 1–5
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>Dan Rayfield has ≥5 evidence-cited compass stances ingested with citations; Task 1 completed first (D-05).</done>
</task>

<task type="auto">
  <name>Task 3: Research + ingest Tobias Read (Secretary of State) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-tobias-read.csv
    C:/EV-Accounts/backend/scripts/apply-tobias-read-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Start ONLY after Task 2's apply script completed. One research agent only — no parallel runs.

    Step 1 — Research Tobias Read (politician_id UUID `94105ea6-e6f7-4629-b30c-a8fe713e1cad`, external_id -4100003, Oregon Secretary of State; former State Treasurer; former OR state legislator). Run ONE research agent across ALL 36 compass topics. Primary sources: ballotpedia.org, sos.oregon.gov press releases, OR legislature vote records, prior treasurer policy positions. Read has voting-rights / redistricting / election admin focus — those topics should have richer evidence than others. Aim for 8+ stances.

    Step 2 — Write CSV `2026-05-30-tobias-read.csv` with politician_id = `94105ea6-e6f7-4629-b30c-a8fe713e1cad`.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-tobias-read-stances.ts`. Update csvPath. Keep `parseInt(r.value)` (D-07).

    Step 4 — `npx tsx scripts/apply-tobias-read-stances.ts` from `C:/EV-Accounts/backend/`. Wait for completion message before Task 4.

    Step 5 — Write citation rows in `inform.politician_context` (D-06).

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '94105ea6-e6f7-4629-b30c-a8fe713e1cad'` returns ≥ 3. Value range check: `SELECT MIN(value), MAX(value) FROM inform.politician_answers WHERE politician_id = '94105ea6-e6f7-4629-b30c-a8fe713e1cad'` returns MIN ≥ 1 AND MAX ≤ 5.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-tobias-read.csv` exists with politician_id = `94105ea6-e6f7-4629-b30c-a8fe713e1cad` on every row
    - File `apply-tobias-read-stances.ts` exists with `parseInt(r.value)` (no inversion)
    - `npx tsx scripts/apply-tobias-read-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 3)
    - SQL: stance count for Read ≥ 3
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>Tobias Read has ≥3 evidence-cited compass stances ingested with citations; Task 2 completed first.</done>
</task>

<task type="auto">
  <name>Task 4: Research + ingest Elizabeth Steiner (State Treasurer) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-elizabeth-steiner.csv
    C:/EV-Accounts/backend/scripts/apply-elizabeth-steiner-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Start ONLY after Task 3 completed. One research agent only — no parallel runs.

    Step 1 — Research Elizabeth Steiner (politician_id UUID `c712d9cb-6a42-4fc6-b025-67cd5064605f`, external_id -4100004, Oregon State Treasurer; former OR state senator). Run ONE research agent across ALL 36 compass topics. Primary sources: ballotpedia.org, oregon.gov/treasury, OR senate vote records (multi-term — substantial vote history exists), campaign archive. Steiner is a former MD — health-related topics may be richer. Aim for 8+ stances.

    Step 2 — Write CSV `2026-05-30-elizabeth-steiner.csv` with politician_id = `c712d9cb-6a42-4fc6-b025-67cd5064605f`.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-elizabeth-steiner-stances.ts`. Update csvPath. Keep `parseInt(r.value)` (D-07).

    Step 4 — `npx tsx scripts/apply-elizabeth-steiner-stances.ts` from `C:/EV-Accounts/backend/`. Wait for completion before Task 5.

    Step 5 — Write citation rows in `inform.politician_context` (D-06).

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = 'c712d9cb-6a42-4fc6-b025-67cd5064605f'` returns ≥ 3. Value range check returns MIN ≥ 1 AND MAX ≤ 5.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-elizabeth-steiner.csv` exists with politician_id = `c712d9cb-6a42-4fc6-b025-67cd5064605f` on every row
    - File `apply-elizabeth-steiner-stances.ts` exists with `parseInt(r.value)` (no inversion)
    - `npx tsx scripts/apply-elizabeth-steiner-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 3)
    - SQL: stance count for Steiner ≥ 3
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>Elizabeth Steiner has ≥3 evidence-cited compass stances ingested with citations; Task 3 completed first.</done>
</task>

<task type="auto">
  <name>Task 5: Research + ingest Christina Stephenson (Labor Commissioner) — RUN ALONE</name>
  <files>
    C:/EV-Accounts/backend/data/stance-research/2026-05-30-christina-stephenson.csv
    C:/EV-Accounts/backend/scripts/apply-christina-stephenson-stances.ts
  </files>
  <read_first>
    C:/EV-Accounts/backend/scripts/apply-allen-stances.ts
    .planning/phases/80-or-compass-stances/80-CONTEXT.md
    .planning/phases/80-or-compass-stances/80-RESEARCH.md
  </read_first>
  <action>
    HARD CONSTRAINT (D-05): Start ONLY after Task 4 completed. One research agent only — no parallel runs.

    Step 1 — Research Christina Stephenson (politician_id UUID `8548989d-ff40-4b25-bb42-e1a7cbb03c88`, external_id -4100005, Oregon Labor Commissioner; civil rights attorney; first elected to BOLI 2022). Run ONE research agent across ALL 36 compass topics. Primary sources: ballotpedia.org, boli.oregon.gov press releases + policy actions, campaign archive, news coverage. Labor / worker-protection / wage-theft / civil-rights topics will have the richest evidence. Aim for 5+ stances.

    Step 2 — Write CSV `2026-05-30-christina-stephenson.csv` with politician_id = `8548989d-ff40-4b25-bb42-e1a7cbb03c88`.

    Step 3 — Copy `apply-allen-stances.ts` → `apply-christina-stephenson-stances.ts`. Update csvPath. Keep `parseInt(r.value)` (D-07).

    Step 4 — `npx tsx scripts/apply-christina-stephenson-stances.ts` from `C:/EV-Accounts/backend/`.

    Step 5 — Write citation rows in `inform.politician_context` (D-06).

    Step 6 — Run the per-wave SC-1 sampling SQL from 80-VALIDATION.md to confirm all 5 executives have stances before declaring Plan 01 complete:
    ```
    SELECT p.full_name, COUNT(pa.id) AS stance_count
    FROM essentials.politicians p
    LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
    WHERE p.external_id BETWEEN -4100005 AND -4100001
    GROUP BY p.full_name
    ORDER BY p.full_name;
    ```
    Expected: 5 rows, each with stance_count > 0.

    Implements D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>SQL via mcp__supabase-local: `SELECT COUNT(DISTINCT p.id) FROM essentials.politicians p JOIN inform.politician_answers pa ON pa.politician_id=p.id WHERE p.external_id BETWEEN -4100005 AND -4100001` returns 5. And `SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '8548989d-ff40-4b25-bb42-e1a7cbb03c88'` returns ≥ 3.</automated>
  </verify>
  <acceptance_criteria>
    - File `2026-05-30-christina-stephenson.csv` exists with politician_id = `8548989d-ff40-4b25-bb42-e1a7cbb03c88` on every row
    - File `apply-christina-stephenson-stances.ts` exists with `parseInt(r.value)`
    - `npx tsx scripts/apply-christina-stephenson-stances.ts` stdout: `Done — Upserted: N, Skipped: M` (N ≥ 3)
    - SQL: stance count for Stephenson ≥ 3
    - SQL per-wave check: all 5 constitutional officers (external_ids -4100001..-4100005) have stance_count > 0
    - SQL: every answer row has a matching `inform.politician_context` row
  </acceptance_criteria>
  <done>All 5 OR constitutional officers have ≥3 evidence-cited stances (Kotek ≥10); SC-1 satisfied for executives; per-wave SQL gate passes; Task 4 completed before Task 5 (D-05).</done>
</task>

</tasks>

<verification>
After all 5 tasks complete, run the Plan-01 per-wave SQL from `80-VALIDATION.md`:

```sql
SELECT p.full_name, COUNT(pa.id) AS stance_count
FROM essentials.politicians p
LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
WHERE p.external_id BETWEEN -4100005 AND -4100001
GROUP BY p.full_name
ORDER BY p.full_name;
```

Expected: 5 rows; Kotek ≥ 10; Rayfield ≥ 5; Read/Steiner/Stephenson ≥ 3 each. All values integers 1–5.

Then process-audit: confirm the execution log shows 5 sequential research runs (no two overlapping) — this contributes to SC-3.
</verification>

<success_criteria>
- [ ] All 5 CSVs exist in `C:/EV-Accounts/backend/data/stance-research/`
- [ ] All 5 apply scripts exist in `C:/EV-Accounts/backend/scripts/` and use `parseInt(r.value)` (no inversion)
- [ ] All 5 politicians have ≥1 row in `inform.politician_answers`
- [ ] Every `inform.politician_answers` row has a matching `inform.politician_context` citation row
- [ ] All stance values are integers in {1,2,3,4,5}
- [ ] Process audit confirms one-at-a-time execution (SC-3)
</success_criteria>

<output>
Create `.planning/phases/80-or-compass-stances/80-01-SUMMARY.md` listing:
- The 5 CSV files written and their row counts
- The 5 apply scripts written
- For each official: external_id, UUID, final stance count, top 3 evidence sources used
- Confirmation that each research agent ran ONE AT A TIME (D-05 compliance)
- Output of the per-wave SQL gate
</output>
