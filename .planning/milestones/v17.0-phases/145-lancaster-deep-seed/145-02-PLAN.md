---
phase: 145-lancaster-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/911_lancaster_complete.sql
autonomous: true
requirements: [LANC-01]

must_haves:
  truths:
    - "D-07: Raj Malhi (external_id -201280) is RETIRED — he LOST the April 2026 election — set is_active=false, office_id=NULL (preserve the row, do NOT delete); this FREES office 03a0dae7"
    - "D-07: Marvin Crist (external_id 686320) is RETIRED — he did not file for re-election — set is_active=false, office_id=NULL (preserve the row, do NOT delete); this FREES office afd045ec (moved into the survivor chamber in Plan 01)"
    - "D-01: Cedric White (external_id -700655) and Rocio Castellanos (external_id -700656) are CREATED as new politician rows — confirmed absent by a pre-flight NOT EXISTS guard; campaign-finance committee rows of those names are NOT politicians and are ignored"
    - "D-01/D-03b: each new member is seated into ONE of the two FREED offices (03a0dae7 ex-Malhi, afd045ec ex-Crist) by setting BOTH offices.politician_id AND the new politicians.office_id (bidirectional link kept in sync); title 'Council Member', at-large district 0097ca4d — NO new offices created"
    - "After the wave the survivor chamber 9b9014b4 has exactly 5 ACTIVE members with consistent bidirectional links: Mayor Parris (-200795) + Council Hughes-Leslie (-201279)/Mann (-201281)/White (-700655)/Castellanos (-700656); Malhi + Crist excluded from the active roster; 0 active stale members; official_count=5"
    - "D-08: no new chambers/districts/geofences and NO duplicate person rows are created; the 4 Councilmember seats stay at-large on district 0097ca4d (no per-seat district_id changes)"
    - "D-04: this roster turnover (retire 2 + create+seat 2) is a STRUCTURAL migration (911) that registers in supabase_migrations.schema_migrations normally"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/911_lancaster_complete.sql"
      provides: "Roster turnover: retire Malhi (-201280) + Crist (686320), create White (-700655) + Castellanos (-700656), seat each into a freed office with synced bidirectional links, confirm official_count=5"
      contains: "-700655"
  key_links:
    - from: "essentials.politicians (external_id -201280 Malhi, 686320 Crist)"
      to: "retired state (is_active=false, office_id NULL); offices 03a0dae7 + afd045ec freed"
      via: "UPDATE politicians SET is_active=false, office_id=NULL WHERE external_id IN (-201280, 686320); UPDATE offices SET politician_id=NULL WHERE id IN ('03a0dae7...','afd045ec...')"
      pattern: "is_active\\s*=\\s*false"
    - from: "essentials.politicians (external_id -700655 White, -700656 Castellanos)"
      to: "freed Councilmember offices in chamber 9b9014b4 (at-large 0097ca4d) — BOTH pointers synced"
      via: "guarded INSERT politician → UPDATE offices SET politician_id=<new> → UPDATE politicians SET office_id=<office>"
      pattern: "-700656"
---

<objective>
Turn the Lancaster council over to its current, post-April-2026-election 5-member roster. This wave runs
only after Plan 01 has merged the structure to one 'City Council' chamber (9b9014b4) with 5 offices and
repaired the reverse pointer for the 3 continuing members (Parris/Hughes-Leslie/Mann).

⚠️ CORRECTED ROSTER REALITY (CONTEXT §db_precheck, DB-verified 2026-06-20). The April 2026 election turned
over 2 of 5 seats, and BOTH departing members ARE in the DB and seated (this is the correction — the old
plan missed Malhi entirely):
- Raj Malhi (-201280) was seated on office 03a0dae7 but LOST the April 2026 election → RETIRE.
- Marvin Crist (686320) was seated on office afd045ec (moved into the survivor in Plan 01) but did not
  file for re-election → RETIRE.
The 2 April-2026 winners — Cedric White + Rocio Castellanos — have NO real politician rows yet (only
campaign-finance committee rows of those names; ignore those). The going-forward 5: Mayor R. Rex Parris +
Vice Mayor Lauren Hughes-Leslie + Ken Mann + Cedric White + Rocio Castellanos.

⚠️ NO RESEAT OF CONTINUING MEMBERS HERE. Parris/Hughes-Leslie/Mann were already seated AND had their
reverse pointer repaired in Plan 01 — they are DONE. This wave does NOT touch them. (The old plan's
"reseat Parris/Hughes-Leslie/Mann into empty shells" step is obsolete — there were never empty shells.)

⚠️ OFFICE REUSE, NOT OFFICE CREATE. The 2 freed offices — 03a0dae7 (ex-Malhi) and afd045ec (ex-Crist) —
are REASSIGNED to White + Castellanos. Do NOT create new offices; the chamber must stay at exactly 5
offices. For each new member set BOTH offices.politician_id AND politicians.office_id (keep both pointers
in sync — this phase exists in part because that link was historically desynced; D-03b).

Part A — RETIRE the two departing members (preserve rows, do NOT delete):
- UPDATE essentials.politicians SET is_active=false, office_id=NULL WHERE external_id IN (-201280, 686320)
  (Malhi lost; Crist did not file). Then free their offices: UPDATE essentials.offices SET
  politician_id=NULL WHERE id IN ('03a0dae7-...','afd045ec-...'). They carry 0 stances going forward
  (no stance task for them in Wave 4).

Part B — CREATE + seat the two new members (guarded INSERT, then sync BOTH pointers):
- Cedric White: external_id -700655, first 'Cedric', last 'White', title 'Council Member'.
- Rocio Castellanos: external_id -700656, first 'Rocio', last 'Castellanos', title 'Council Member'.
- Both at-large in chamber 9b9014b4 on district 0097ca4d; is_active=true, is_incumbent=true,
  source='cityoflancasterca.org'. Seat each into ONE freed office (03a0dae7 / afd045ec): set
  offices.politician_id = the new politician's id AND politicians.office_id = that office's id.

Part C — Confirm the survivor chamber 9b9014b4 has exactly 5 ACTIVE office-linked members with consistent
bidirectional links and official_count=5; re-run feedback_section_split_check (expect 0 rows for Lancaster).

Purpose: A complete, accurate, current post-election roster with consistent links is the prerequisite for
headshots (Plan 03) and stances (Plan 04). Output: Migration 911 applied; 5 current members seated +
bidirectionally linked; Malhi + Crist retired.

This IS a structural migration (911) — it registers in supabase_migrations.schema_migrations normally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-lancaster-deep-seed/145-CONTEXT.md
@.planning/phases/145-lancaster-deep-seed/145-RESEARCH.md
@.planning/phases/145-lancaster-deep-seed/145-01-SUMMARY.md

<interfaces>
<!-- DB-verified live values (2026-06-20). Pre-flight re-confirms the 2 departing seated rows + 2 absences. -->

Government: id f6732517-76d8-4f5f-b528-e49d60f32a4c  geo_id '0640130' (set in Plan 01)  state 'CA'
Survivor council chamber: 9b9014b4-0106-417f-a104-ac2055fc8134  (5 offices after Plan 01)
  Mayor office ed37230d: Parris -200795 (seated + linked in Plan 01 — DO NOT TOUCH)
  Council 052a2e17: Hughes-Leslie -201279 (linked in Plan 01 — DO NOT TOUCH)
  Council 6e17ff80: Mann -201281 (linked in Plan 01 — DO NOT TOUCH)
  Council 03a0dae7: Malhi -201280 (RETIRE → frees this office)
  Council afd045ec: Crist 686320 (RETIRE → frees this office; was moved in from a9be708e in Plan 01)

RETIRE (preserve rows, do NOT delete; then free their offices):
  -201280 Raj Malhi    → is_active=false, office_id=NULL  (LOST April 2026); free office 03a0dae7
  686320  Marvin Crist → is_active=false, office_id=NULL  (did not file);   free office afd045ec

CREATE (genuinely new — guarded NOT EXISTS on external_id; ignore same-name campaign-committee rows):
  Cedric White       external_id -700655  first 'Cedric'  last 'White'        title 'Council Member'
  Rocio Castellanos  external_id -700656  first 'Rocio'   last 'Castellanos'  title 'Council Member'
  Seat each into a FREED office (03a0dae7 / afd045ec), at-large district 0097ca4d, is_active=true,
  is_incumbent=true, is_appointed false, source 'cityoflancasterca.org'. Set BOTH offices.politician_id
  AND politicians.office_id (sync both pointers).

Pre-flight (run BEFORE any write):
  SELECT external_id, is_active, office_id FROM essentials.politicians WHERE external_id IN (-201280, 686320);
    → expect both active + office-linked (Malhi 03a0dae7, Crist afd045ec) — the retire targets
  SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-700655, -700656);  → expect 0
  SELECT id, external_id, is_active FROM essentials.politicians
    WHERE (last_name ILIKE 'White' OR last_name ILIKE 'Castellanos');
    → a campaign-finance COMMITTEE row of either name is NOT a politician/official — ignore it; only STOP
      if a REAL active Lancaster-official person of that name already exists.
  If Malhi/Crist are NOT both active+office-linked as locked, or -700655/-700656 are not free, or a real
    White/Castellanos official row already exists, STOP-report (do not blind-write).

Schema rules (CA convention):
  - chambers.slug is GENERATED — NEVER include slug in any write
  - district_type lives on essentials.districts, NOT offices — there is NO offices.district_type column
  - the 4 Councilmember offices are at-large on district 0097ca4d (D-08); do NOT create new districts/geofences
  - politicians.external_id is unique → ON CONFLICT (external_id) DO NOTHING is safe for the 2 new rows
  - Vice Mayor (Hughes-Leslie) is a role flag, NOT a separate elected seat → no LOCAL_EXEC row for it
  - keep offices.politician_id AND politicians.office_id in sync on every roster write (D-03b)

Pattern migration to copy:
  C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql — retire-not-delete + reserved -700xxx
    INSERT + office seating with synced pointers (direct precedent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight roster confirmation, then author migration 911 Part A (retire Malhi -201280 + Crist 686320 and free their offices)</name>
  <files>C:/EV-Accounts/backend/migrations/911_lancaster_complete.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql (retire-not-delete + free-office precedent; SC retired Smyth the same way)
    - .planning/phases/143-santa-clarita-deep-seed/143-02-SUMMARY.md (roster-completion shape + roster post-verification query)
    - .planning/phases/145-lancaster-deep-seed/145-CONTEXT.md (§db_precheck resolved-reconcile table — LOCKED, §decisions D-01, D-07)
    - .planning/phases/145-lancaster-deep-seed/145-RESEARCH.md (§Live-Data Findings 1/2/3, §Common Pitfalls 1/7)
    - .planning/phases/145-lancaster-deep-seed/145-01-SUMMARY.md (confirm Plan 01 already linked Parris/Hughes-Leslie/Mann — they are NOT touched here)
  </read_first>
  <action>
    First run the pre-flight: (a) SELECT external_id, is_active, office_id FROM essentials.politicians
    WHERE external_id IN (-201280, 686320) — confirm Malhi -201280 active + office_id 03a0dae7 and Crist
    686320 active + office_id afd045ec (the retire targets); (b) SELECT COUNT(*) FROM essentials.politicians
    WHERE external_id IN (-700655, -700656) — expect 0; (c) confirm no REAL active White/Castellanos
    Lancaster-official row exists (campaign-committee rows of the same name are NOT politicians — ignore).
    Also confirm Parris/-200795, Hughes-Leslie/-201279, Mann/-201281 are already office-linked from Plan 01
    (sanity — do NOT re-link them here). If Malhi/Crist are not both active+linked as locked, or the new
    ext_ids are not free, or a real duplicate White/Castellanos exists, STOP-report (never blind-write).
    Then begin C:/EV-Accounts/backend/migrations/911_lancaster_complete.sql (BEGIN/COMMIT, idempotent) with:
    Part A — RETIRE the two departing members: UPDATE essentials.politicians SET is_active=false,
    office_id=NULL WHERE external_id IN (-201280, 686320) AND (is_active OR office_id IS NOT NULL); then
    free their offices: UPDATE essentials.offices SET politician_id=NULL WHERE
    id IN ('03a0dae7-...','afd045ec-...') AND politician_id IS NOT NULL. NEVER DELETE the politician rows
    (preserve them). Do NOT touch Parris/Hughes-Leslie/Mann or their offices. This is a structural
    migration (911, registers in schema_migrations normally). Do NOT include slug; do NOT write
    offices.district_type (no such column); do NOT add district_id to the at-large offices (D-08).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT external_id, is_active, office_id FROM essentials.politicians WHERE external_id IN (-201280, 686320);" returns both rows with is_active=false, office_id NULL (Malhi + Crist retired, not deleted) AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.offices WHERE id IN ('03a0dae7-...','afd045ec-...') AND politician_id IS NULL;" returns 2 (both offices freed)</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: Malhi -201280 (office 03a0dae7) + Crist 686320 (office afd045ec) confirmed active+linked; -700655/-700656 free; no real duplicate White/Castellanos official row
    - Malhi (-201280): `is_active=false AND office_id IS NULL` (retired, NOT deleted — row still exists)
    - Crist (686320): `is_active=false AND office_id IS NULL` (retired, NOT deleted — row still exists)
    - Offices 03a0dae7 and afd045ec freed (`politician_id IS NULL`) and ready for the new members
    - Parris/Hughes-Leslie/Mann and their offices were NOT modified in this task (still linked from Plan 01)
    - No essentials.politicians row was DELETEd
    - Part A statements are idempotent (second apply changes 0 rows)
  </acceptance_criteria>
  <done>Malhi + Crist retired (preserved, not deleted); their offices (03a0dae7, afd045ec) freed; the 3 continuing members left untouched; Part A of migration 911 authored + applied.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 911 Part B/C — create White (-700655) + Castellanos (-700656), seat into freed offices with synced pointers, confirm official_count=5</name>
  <files>C:/EV-Accounts/backend/migrations/911_lancaster_complete.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/895_santa_clarita_complete.sql (CTE INSERT politician → seat into office → sync office_id; reserved -700xxx pattern)
    - .planning/phases/143-santa-clarita-deep-seed/143-02-SUMMARY.md (roster post-verification + idempotency shape; official_count=5 confirm)
    - .planning/phases/145-lancaster-deep-seed/145-CONTEXT.md (§db_precheck LOCKED ids, §decisions D-01, D-03b, D-08)
    - .planning/phases/145-lancaster-deep-seed/145-RESEARCH.md (§Live-Data Findings 1/3, §Common Pitfalls 2/5, §Validation Architecture Wave 2 checks)
  </read_first>
  <action>
    Append Part B and Part C to migration 911. Part B — CREATE + seat the two new members, each guarded
    NOT EXISTS on external_id (belt-and-suspenders with ON CONFLICT (external_id) DO NOTHING). For Cedric
    White (external_id -700655, first 'Cedric', last 'White', is_active true, is_appointed false,
    is_incumbent true, is_vacant false, source 'cityoflancasterca.org') and Rocio Castellanos (external_id
    -700656, first 'Rocio', last 'Castellanos', same flags): INSERT the politician row, then SEAT each into
    one of the two FREED offices (03a0dae7 ex-Malhi and afd045ec ex-Crist) — keep the title 'Council Member'
    and the existing at-large district 0097ca4d on those offices (do NOT create new offices). Set BOTH
    pointers in sync: UPDATE essentials.offices SET politician_id=<new politician id> WHERE id=<freed office>
    AND politician_id IS NULL; UPDATE essentials.politicians SET office_id=<freed office> WHERE
    external_id=<new ext_id> AND office_id IS NULL. Do NOT create new districts/geofences/offices/LOCAL_EXEC;
    do NOT include slug; do NOT write offices.district_type. Part C — UPDATE essentials.chambers SET
    official_count=5 WHERE id='9b9014b4-...' AND official_count IS DISTINCT FROM 5. End the file with the
    roster post-verification query: the 5 current active members (-200795 Mayor; -201279/-201281/-700655/
    -700656 Council Member) seated in 9b9014b4 with BOTH pointers consistent (politicians.office_id matches
    the office whose offices.politician_id points back); Malhi -201280 + Crist 686320 NOT among the active
    roster; exactly 5 offices under 9b9014b4 (no 6th); feedback_section_split_check → 0 rows for Lancaster.
    Apply via mcp__supabase-local execute_sql or psql -f. This registers in schema_migrations (structural 911).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.id=p.office_id AND o.politician_id=p.id WHERE o.chamber_id='9b9014b4-0106-417f-a104-ac2055fc8134' AND p.is_active=true;" returns 5 (5 active members with CONSISTENT bidirectional links) AND psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.offices WHERE chamber_id='9b9014b4-0106-417f-a104-ac2055fc8134';" returns 5</automated>
  </verify>
  <acceptance_criteria>
    - White (-700655) + Castellanos (-700656) politician rows exist with correct names/flags, source 'cityoflancasterca.org', is_active=true, is_incumbent=true
    - Each new member seated into one of the FREED offices (03a0dae7 / afd045ec), title 'Council Member', at-large district 0097ca4d; NO new office created
    - Bidirectional link consistent for both new members: offices.politician_id = the new politician's id AND politicians.office_id = that office's id
    - Exactly 5 offices under 9b9014b4 (1 Mayor + 4 Council Member) — no 6th office
    - Active members in 9b9014b4 with consistent links = 5 (Parris Mayor + Hughes-Leslie/Mann/White/Castellanos); Malhi -201280 + Crist 686320 NOT among them
    - No duplicate person row for White/Castellanos (exactly one each by external_id)
    - No new essentials.districts / geofence_boundaries / offices / LOCAL_EXEC rows created
    - Survivor chamber 9b9014b4 official_count=5; feedback_section_split_check → 0 rows for Lancaster
    - Migration 911 idempotent (second apply changes 0 rows, no error); registered in schema_migrations
  </acceptance_criteria>
  <done>White + Castellanos created and seated into the freed offices with synced bidirectional links; official_count=5; the 5 current active members are consistently office-linked; Malhi + Crist excluded; exactly 5 offices; migration idempotent + registered.</done>
</task>

</tasks>

<threat_model>
DB-seeding phase with NO new external attack surface. No untrusted user input — migration SQL applied via
service-role/operator directly against the internal production DB. Threat surface = accidental data
corruption: duplicating a person, leaving a departed member active, seating into a non-freed office, an
asymmetric bidirectional link, or exceeding the 5-seat structure — mitigated by retire-then-free ordering,
the create-not-duplicate guards, both-pointers-in-sync seating, pre-flight existence checks, and
post-migration SQL verification of link consistency.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Migration SQL applied by a trusted operator directly against production Supabase. No untrusted input. |
| election result → DB roster | The certified April 2026 result (swearing-in April 28) drives the retire/create; re-confirmed at apply time (VALIDATION manual-only roster-currency check). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-09 | Spoofing | Departed member left active / reseated (roster corruption) | mitigate | Malhi -201280 + Crist 686320 RETIRED (is_active=false, office_id=NULL), never deleted, never reseated; post-verify they are excluded from the 5 active members |
| T-145-08 | Tampering | Duplicate White/Castellanos row from a partial/double apply | mitigate | Pre-flight COUNT on -700655/-700656 (expect 0) + real-person name check; INSERT guarded NOT EXISTS + ON CONFLICT (external_id) DO NOTHING |
| T-145-28 | Tampering | New member seated into a NOT-freed office, or one pointer set without the other (link desync) | mitigate | Seat only into 03a0dae7/afd045ec guarded politician_id IS NULL; set BOTH offices.politician_id AND politicians.office_id; post-verify the join requires both pointers to agree |
| T-145-11 | Tampering | A 6th office created or a per-seat district/LOCAL_EXEC explosion | mitigate | Reuse the 2 freed offices (no INSERT office); at-large district 0097ca4d unchanged; post-verify exactly 5 offices under 9b9014b4 |
| T-145-10 | Spoofing | A non-current person (e.g. campaign-committee row) seated as a member | mitigate | Pre-flight distinguishes committee rows from real officials; only -700655/-700656 created; roster post-verify count=5 by external_id |
| T-145-12 | Tampering | chambers.slug / offices.district_type included in a write → error; or non-idempotent re-apply duplicates rows | mitigate | slug + district_type omitted (GENERATED / non-existent); all writes guarded by ON CONFLICT / NOT EXISTS / IS NULL / IS DISTINCT FROM; second apply = 0 rows |
</threat_model>

<verification>
After applying 911:
1. Malhi (-201280) + Crist (686320): is_active=false, office_id NULL (retired, not deleted)
2. White (-700655) + Castellanos (-700656) created + seated into the freed offices (03a0dae7 / afd045ec) with synced bidirectional links
3. active members in 9b9014b4 with consistent links = 5 (Parris Mayor + Hughes-Leslie/Mann/White/Castellanos); exactly 5 offices total; no new geofence/district/office rows
4. survivor chamber official_count=5; feedback_section_split_check → still 0 rows for Lancaster
5. Parris/Hughes-Leslie/Mann links from Plan 01 unchanged
6. migration 911 registered in schema_migrations
</verification>

<success_criteria>
Lancaster's elected council is complete and current post-April-2026-election: Mayor Parris + 4 at-large
councilmembers (Hughes-Leslie/Mann/White/Castellanos) all bidirectionally office-linked in one chamber;
Malhi (lost) and Crist (retired) correctly retired (not deleted); White + Castellanos created and seated
into the freed offices; no duplicate people, no new offices/geofences, exactly 5 offices — ready for
headshots in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/145-lancaster-deep-seed/145-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
