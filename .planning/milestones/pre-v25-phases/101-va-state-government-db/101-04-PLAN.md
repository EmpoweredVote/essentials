---
phase: 101-va-state-government-db
plan: 04
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - C:/EV-Accounts/backend/migrations/generate_va_house.ps1
  - C:/EV-Accounts/backend/migrations/303_va_delegates.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-303.ts
autonomous: true
requirements:
  - VA-GOV-04
tags:
  - virginia
  - state-government
  - delegates
  - sql-migration
  - powershell-generator

must_haves:
  truths:
    - "100 Virginia House of Delegates members (HD-1 through HD-100) are seeded in essentials.politicians with external_ids -5120001 through -5120100, party per roster (64 Democrats, 35 Republicans, 1 Vacant), is_active=true for all non-vacant"
    - "HD-20 (Michelle Maldonado seat) is seeded as full_name='Vacant', is_active=false, is_vacant=true, is_incumbent=false, party='' (or NULL); placeholder pattern matches MD District 42A precedent"
    - "100 office rows are seeded, each linking the delegate politician to the matching STATE_LOWER district via geo_id (51001-51100) under the House of Delegates chamber of State of Virginia"
    - "Every non-vacant office row has title='Delegate', representing_state='VA', is_appointed_position=false; vacant office row uses is_vacant=true"
    - "All 100 delegate politicians have office_id back-filled (NULL count = 0 after migration)"
    - "Section-split check returns 0 rows after migration (VA-GOV-04 phase-gate)"
    - "NOT EXISTS guard uses (district_id, chamber_id) — single-member pattern (NOT MD multi-member)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/generate_va_house.ps1"
      provides: "PowerShell generator emitting 303_va_delegates.sql with 100 CTE blocks + back-fill"
      contains: "function RepBlock"
    - path: "C:/EV-Accounts/backend/migrations/303_va_delegates.sql"
      provides: "Generated 100-delegate migration (BEGIN/COMMIT wrapped, 100 CTE blocks, 1 back-fill UPDATE, HD-20 vacancy special case)"
      contains: "WITH ins_p AS"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-303.ts"
      provides: "Apply script for delegate migration + smoke tests including HD-20 vacancy assertion"
      contains: "303_va_delegates.sql"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts (STATE_LOWER, state='va' lowercase, geo_id='51001'..'51100')"
      via: "d.geo_id = '<gid>' AND d.district_type = 'STATE_LOWER' AND d.state = 'va'"
      pattern: "d.district_type = 'STATE_LOWER' AND d.state = 'va'"
    - from: "essentials.offices.chamber_id"
      to: "essentials.chambers (name='House of Delegates' under State of Virginia)"
      via: "SELECT id FROM essentials.chambers WHERE name = 'House of Delegates' AND government_id = (...)"
      pattern: "name = 'House of Delegates' AND government_id = .SELECT id FROM essentials.governments WHERE name = 'State of Virginia' AND state = 'VA'"
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id"
      via: "back-fill UPDATE scoped to external_id BETWEEN -5120100 AND -5120001"
      pattern: "p.external_id BETWEEN -5120100 AND -5120001 AND p.office_id IS NULL"
---

<objective>
Generate, write, and apply migration 303 to seed all 100 Virginia House of Delegates members (HD-1 through HD-100) — including HD-20 as a vacant placeholder — as politicians with offices linked to STATE_LOWER districts under the House of Delegates chamber. Implements VA-GOV-04. Also runs the phase-level section-split sanity check (final phase gate).

Purpose: VA House of Delegates is the second chamber seeded in Plan 01. Virginia uses single-member SLDL districts (1 delegate per district), differing from Maryland's multi-member pattern. The generator pattern must mirror Oregon's generate_or_house.ps1, NOT generate_md_house.ps1.

Output:
- PowerShell generator: `C:/EV-Accounts/backend/migrations/generate_va_house.ps1`
- Generated SQL: `C:/EV-Accounts/backend/migrations/303_va_delegates.sql` (100 CTE blocks + back-fill)
- Apply script: `C:/EV-Accounts/backend/scripts/_apply-migration-303.ts`
- DB state: 100 delegate politician rows + 100 office rows + office_id back-filled; HD-20 as vacant placeholder
- Phase gate: section-split check returns 0 rows
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-va-state-government-db/101-RESEARCH.md
@.planning/phases/101-va-state-government-db/101-PATTERNS.md
@C:/EV-Accounts/backend/migrations/generate_or_house.ps1
@C:/EV-Accounts/backend/migrations/227_or_state_house.sql
@C:/EV-Accounts/backend/scripts/_apply-migration-273.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write generate_va_house.ps1 and emit 303_va_delegates.sql</name>
  <files>
    C:/EV-Accounts/backend/migrations/generate_va_house.ps1,
    C:/EV-Accounts/backend/migrations/303_va_delegates.sql
  </files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/generate_or_house.ps1 (exact analog — single-member; NOT MD)
    - .planning/phases/101-va-state-government-db/101-PATTERNS.md (`generate_va_house.ps1` section lines 264-345; HD-20 vacancy special case lines 326-332; multi-member vs single-member NOT EXISTS guard warning)
    - .planning/phases/101-va-state-government-db/101-RESEARCH.md (Complete Rosters > VA House of Delegates table lines 369-475 — full 100-row roster; HD-20 vacancy details lines 476-478; A1 party assumption lines 480 + 594-598)
  </read_first>
  <behavior>
    - Generator accepts optional `-Out` parameter (default `C:/EV-Accounts/backend/migrations/303_va_delegates.sql`)
    - Generator hard-codes the 100-delegate roster (district, full, first, last, party, ext_id, plus is_vacant flag for HD-20)
    - For HD-20: full='Vacant', first='', last='', party='', is_active=false, is_vacant=true, is_incumbent=false (per MD 42A precedent)
    - For all other 99 delegates: is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true
    - Generator emits one CTE block per delegate using RepBlock function with VA substitutions
    - Output SQL: 1 BEGIN, 1 COMMIT, 100 `WITH ins_p AS` blocks, exactly 1 back-fill UPDATE
    - Every CTE specifies `d.district_type = 'STATE_LOWER' AND d.state = 'va'` (lowercase)
    - Every office row uses chamber `'House of Delegates'`, title `'Delegate'`, representing_state `'VA'`
    - NOT EXISTS guard uses `(district_id, chamber_id)` — single-member pattern (matches OR house, NOT MD house)
    - Politician INSERT includes `AND p.id IS NOT NULL` guard (MD senate lesson — OR house analog omits it; VA adds it per Pattern 4 line 334)
    - Generator prints `CTE blocks (delegates): 100  (expected 100)` after writing
    - Output file is UTF-8 NoBOM
  </behavior>
  <action>
    Create `C:/EV-Accounts/backend/migrations/generate_va_house.ps1` by adapting `generate_or_house.ps1` with these exact substitutions:

    1. `param([string]$Out = "C:/EV-Accounts/backend/migrations/303_va_delegates.sql")`.
    2. FIPS prefix in $gid: `$gid = '51' + ([int]$r.dist).ToString().PadLeft(3, '0')` (replaces '41').
    3. Chamber name: `'House of Delegates'` (replaces 'Oregon House of Representatives').
    4. Government subquery: `WHERE name = 'State of Virginia' AND state = 'VA'` (add `AND state = 'VA'` — OR omits it; VA must add per Pitfall 5).
    5. District JOIN: `d.state = 'va'` lowercase AND `AND d.district_type = 'STATE_LOWER'` (MUST keep district_type filter — geo_ids 51001-51040 overlap with STATE_UPPER per Pitfall 3).
    6. Title: `'Delegate'` (replaces 'Representative').
    7. `representing_state = 'VA'`.
    8. NOT EXISTS guard: keep `(district_id, chamber_id)` — single-member pattern from OR.
    9. ADD `AND p.id IS NOT NULL` guard in the CROSS JOIN (MD senate pattern; OR house omits this — add it for VA per PATTERNS.md line 334).
    10. Modify `RepBlock` to handle vacancy: if `$r.is_vacant -eq $true`, emit the CTE block with `is_active=false, is_appointed=false, is_vacant=true, is_incumbent=false` in the politicians VALUES tuple, AND set the office row's `is_vacant=true` (replaces the hard-coded `false`). For non-vacant rows, emit `is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true` (politician) and `is_vacant=false` (office).
    11. Roster array: 100 entries from RESEARCH.md lines 375-474. HD-20 entry uses `@{ dist=20; ext_id=-5120020; full='Vacant'; first=''; last=''; party=''; is_vacant=$true }`. All others use `is_vacant=$false`. Distinct first/last from each full_name following MD compound-name rule (final word of multi-word last names: e.g., 'Joseline Pena-Melnyk' → last='Pena-Melnyk'; 'Kathy KL Tran' → last='Tran'; 'Joseph P. McNamara' → last='McNamara' with first='Joseph P.'). For Sr./Jr./III suffixes drop from last (e.g., 'Richard C. Sullivan, Jr.' → last='Sullivan' first='Richard C.' suffix dropped per MD pattern).
    12. After foreach, append back-fill:
       ```
       -- ===== office_id back-fill =====
       UPDATE essentials.politicians p
       SET office_id = o.id
       FROM essentials.offices o
       WHERE o.politician_id = p.id
         AND p.external_id BETWEEN -5120100 AND -5120001
         AND p.office_id IS NULL;
       ```
    13. Write file as UTF-8 NoBOM via `[System.IO.File]::WriteAllText($Out, $sb.ToString(), [System.Text.UTF8Encoding]::new($false))`.
    14. After write, count `WITH ins_p AS` matches; print `CTE blocks (delegates): $cteCount  (expected 100)`.
    15. Header comment cites source analog (generate_or_house.ps1, 227_or_state_house.sql), phase 101, requirement VA-GOV-04; CRITICAL warnings: (a) single-member NOT EXISTS (district_id, chamber_id) — NOT (district_id, politician_id); (b) state='va' lowercase + district_type='STATE_LOWER' filter required; (c) HD-20 vacancy.

    Then EXECUTE: `powershell -NoProfile -ExecutionPolicy Bypass -File C:/EV-Accounts/backend/migrations/generate_va_house.ps1`

    Output must report `CTE blocks (delegates): 100  (expected 100)`.

    The 100-delegate roster from RESEARCH.md lines 375-474 is the source of truth. Use full names, parties, and the HD-20 vacancy flag exactly as listed. Spot-check 5 random delegates against elections.virginia.gov before EXECUTING migration to validate the A1 party assumption (per VALIDATION.md Manual-Only Verifications). If a spot-check disagrees with the roster, correct the roster before generating SQL.
  </action>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/migrations/generate_va_house.ps1` exists
    - File `C:/EV-Accounts/backend/migrations/303_va_delegates.sql` exists (produced by running generator)
    - Generator console output contains `CTE blocks (delegates): 100  (expected 100)`
    - Generated SQL contains exactly 100 occurrences of `WITH ins_p AS`
    - Generated SQL contains exactly 1 `BEGIN;`, 1 `COMMIT;`, 1 `UPDATE essentials.politicians` (back-fill)
    - Generated SQL contains: `'House of Delegates'`, `'Delegate'`, `BETWEEN -5120100 AND -5120001`, `d.district_type = 'STATE_LOWER'`, `d.state = 'va'`
    - Generated SQL contains 100 distinct external_ids -5120001 through -5120100
    - Generated SQL contains `'Vacant'` (for HD-20 placeholder)
    - Generated SQL contains zero occurrences of `(district_id, politician_id)` — multi-member guard forbidden; the only NOT EXISTS guard tuple should be (district_id, chamber_id)
    - Generated SQL contains zero occurrences of `'Oregon'`, `'OR'` as state value, `'Representative'`, `-4120` (OR external_id prefix)
    - Generated SQL contains zero occurrences of `is_appointed = true` or `is_appointed_position = true`
    - Generated SQL contains zero BOM (first 3 bytes != 0xEF 0xBB 0xBF)
    - The CTE block for ext_id `-5120020` (HD-20) contains the literal substring `'Vacant'` AND `is_vacant=true|is_vacant = true` AND `is_active=false|is_active = false`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$g='C:/EV-Accounts/backend/migrations/generate_va_house.ps1'; $s='C:/EV-Accounts/backend/migrations/303_va_delegates.sql'; if (-not (Test-Path $g) -or -not (Test-Path $s)) { Write-Host 'FAIL: file missing'; exit 1 }; $c=Get-Content $s -Raw; $bytes=[System.IO.File]::ReadAllBytes($s); $errors=@(); if ($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF) { $errors += 'BOM present' }; $cte=([regex]::Matches($c, 'WITH ins_p AS')).Count; if ($cte -ne 100) { $errors += \"CTE count $cte != 100\" }; if (($c -split 'UPDATE essentials.politicians').Length - 1 -ne 1) { $errors += 'UPDATE count != 1' }; foreach ($n in @(\"'House of Delegates'\",\"'Delegate'\",\"BETWEEN -5120100 AND -5120001\",\"d.district_type = 'STATE_LOWER'\",\"d.state = 'va'\",\"'Vacant'\")) { if (-not ($c.Contains($n))) { $errors += \"missing: $n\" } }; foreach ($f in @(\"o.district_id = d.id`n      AND o.politician_id\",\"'Representative'\",\"-4120\",\"is_appointed = true\",\"is_appointed_position = true\")) { if ($c.Contains($f)) { $errors += \"forbidden: $f\" } }; for ($i=1; $i -le 100; $i++) { $id = '-' + (5120000 + $i); if (-not ($c.Contains($id))) { $errors += \"missing ext_id $id\" } }; $hd20Block = ($c -split '-- =====' | Where-Object { $_ -match '-5120020' }); if (-not ($hd20Block -match \"'Vacant'\") -or -not ($hd20Block -match 'is_vacant')) { $errors += 'HD-20 missing Vacant or is_vacant=true' }; if ($errors.Count -gt 0) { Write-Host ('FAIL: ' + ($errors -join '; ')); exit 1 } else { Write-Host 'PASS: 303_va_delegates.sql'; exit 0 }"</automated>
  </verify>
  <done>
    Generator emits 303_va_delegates.sql with 100 CTE blocks, all VA substitutions, single-member NOT EXISTS guard, HD-20 vacancy correctly encoded, no BOM, no MD multi-member patterns.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write _apply-migration-303.ts and execute it</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-303.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-273.ts (analog senator template — single-member, no multi-member integrity checks)
    - .planning/phases/101-va-state-government-db/101-PATTERNS.md (`scripts/_apply-migration-303.ts` section lines 411-433; HD-20 vacancy extra smoke test)
  </read_first>
  <behavior>
    - Script reads `migrations/303_va_delegates.sql` and submits via pg.Pool
    - Prints `Migration 303 applied successfully` on success
    - Smoke test 1 (count): `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers c ON o.chamber_id = c.id WHERE c.name = 'House of Delegates' AND o.representing_state = 'VA'` returns 100
    - Spot-check 1: HD-1 Patrick A. Hope (-5120001) → geo_id='51001', district_type='STATE_LOWER', state='va'
    - Spot-check 2: HD-50 Thomas C. Wright, Jr. (-5120050) → geo_id='51050'
    - Spot-check 3: HD-100 Robert S. Bloxom, Jr. (-5120100) → geo_id='51100'
    - HD-20 vacancy smoke test: `SELECT full_name, is_vacant, is_active FROM essentials.politicians WHERE external_id = -5120020` returns {Vacant, true, false}
    - NULL office_id check: `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5120100 AND -5120001 AND office_id IS NULL` returns 0
  </behavior>
  <action>
    Create `C:/EV-Accounts/backend/scripts/_apply-migration-303.ts` from `_apply-migration-273.ts` with these substitutions:

    1. Migration filename: `'273_md_state_senators.sql'` → `'303_va_delegates.sql'`.
    2. Chamber name: `'Maryland Senate'` → `'House of Delegates'`.
    3. representing_state: `'MD'` → `'VA'`.
    4. external_id NULL range: `BETWEEN -2410047 AND -2410001` → `BETWEEN -5120100 AND -5120001`.
    5. Expected count: 47 → 100.
    6. Spot-check ext_ids: -5120001, -5120050, -5120100.
    7. Add extra smoke test for HD-20 vacancy:
       ```typescript
       const rVacant = await pool.query(`
         SELECT p.full_name, p.is_vacant, p.is_active FROM essentials.politicians p
         WHERE p.external_id = -5120020
       `);
       console.log('\nHD-20 vacancy check:', JSON.stringify(rVacant.rows[0]));
       // Expected: { full_name: 'Vacant', is_vacant: true, is_active: false }
       ```
    8. Error message: `'Error applying migration 273:'` → `'Error applying migration 303:'`.
    9. Success log: `'Migration 273 applied successfully'` → `'Migration 303 applied successfully'`.

    Execute: `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-303.ts`

    Console must show:
    - `Migration 303 applied successfully`
    - `Delegate offices (House of Delegates chamber): 100 (expected 100)` (or similar count line)
    - 3 spot-check rows for HD-1, HD-50, HD-100 with geo_ids 51001, 51050, 51100, district_type='STATE_LOWER', state='va'
    - HD-20 vacancy check: `{"full_name":"Vacant","is_vacant":true,"is_active":false}`
    - `NULL office_id count (expected 0): 0`

    After successful apply, run the phase-gate section-split SQL via mcp__supabase-local__execute_sql:
    ```sql
    SELECT gb.geo_id, gb.name, gb.mtfcc
    FROM essentials.geofence_boundaries gb
    WHERE gb.mtfcc IN ('G5200', 'G5210', 'G5220', 'G4020')
      AND gb.state = '51'
      AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts)
    LIMIT 10;
    ```
    Must return 0 rows. If rows returned, escalate as phase blocker.

    Also run delegate-count sanity:
    ```sql
    SELECT COUNT(*)::int FROM essentials.offices o
    JOIN essentials.chambers c ON c.id = o.chamber_id
    JOIN essentials.governments g ON g.id = c.government_id
    WHERE c.name = 'House of Delegates' AND g.name = 'State of Virginia' AND g.state = 'VA';
    ```
    Expected: 100.

    Also run party split:
    ```sql
    SELECT party, COUNT(*)::int FROM essentials.politicians
    WHERE external_id BETWEEN -5120100 AND -5120001 GROUP BY party ORDER BY party;
    ```
    Expected: Democrat=64, Republican=35, ''=1 (or NULL=1 depending on storage chosen for vacant placeholder).

    Finally, update `.planning/STATE.md` Accumulated Context to reflect migrations 300-303 applied and advance the next-migration counter.
  </action>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/_apply-migration-303.ts` exists
    - File contains the literal substrings: `'303_va_delegates.sql'`, `'House of Delegates'`, `BETWEEN -5120100 AND -5120001`, `-5120020`, `is_vacant`, `is_active`
    - File contains zero occurrences of `'Maryland Senate'`, `'273_md_state_senators.sql'`, `-2410`
    - Execution console output contains: `Migration 303 applied successfully`; count `100`; HD-20 vacancy JSON with full_name=Vacant, is_vacant=true, is_active=false; `NULL office_id count (expected 0): 0`
    - Post-apply Supabase query `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5120100 AND -5120001` returns 100
    - Post-apply Supabase query `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id WHERE c.name='House of Delegates'` returns 100
    - Post-apply Supabase query `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5120100 AND -5120001 AND office_id IS NULL` returns 0
    - HD-20 verification: `SELECT full_name, is_vacant, is_active FROM essentials.politicians WHERE external_id = -5120020` returns full_name='Vacant' AND is_vacant=true AND is_active=false
    - Party split: Democrat=64, Republican=35, ''=1 (vacant placeholder)
    - Section-split phase-gate query returns 0 rows
    - `.planning/STATE.md` Accumulated Context updated to note migrations 300-303 applied
  </acceptance_criteria>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsx scripts/_apply-migration-303.ts 2>&1 | tee /tmp/303_apply.log && grep -q "Migration 303 applied successfully" /tmp/303_apply.log && grep -q "expected 100" /tmp/303_apply.log && grep -q "\"full_name\":\"Vacant\"" /tmp/303_apply.log && grep -q "NULL office_id count (expected 0): 0" /tmp/303_apply.log</automated>
  </verify>
  <done>
    Apply script created, executed successfully; 100 delegate offices seeded; HD-20 correctly vacant; 0 NULL office_ids; section-split check returns 0 rows; party split D=64/R=35/Vacant=1; STATE.md updated; VA-GOV-04 satisfied at data layer; phase gate clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → production Supabase | Migration writes 200 rows (100 politicians + 100 offices) via pg.Pool with postgres role |
| PowerShell generator output → SQL parser | Generated SQL must round-trip cleanly; UTF-8 BOM would corrupt parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-16 | Tampering | Wrong NOT EXISTS guard would allow office duplication | mitigate | Generator hard-codes `(district_id, chamber_id)` single-member guard; static gate forbids `(district_id, politician_id)` literal — would produce >100 office rows for single chamber |
| T-101-17 | Tampering | District JOIN ambiguity (51001-51040 overlap with STATE_UPPER) | mitigate | Generator hard-codes `AND d.district_type = 'STATE_LOWER' AND d.state = 'va'`; post-apply count assertion = 100 catches a CROSS-JOIN doubling |
| T-101-18 | Tampering | HD-20 vacancy mis-encoded (is_active=true would falsely display vacant seat as elected) | mitigate | Generator hard-codes vacancy fields for HD-20; static gate asserts `'Vacant'` AND `is_vacant` AND `is_active=false` present in HD-20 CTE block; apply script smoke test verifies post-apply DB row |
| T-101-19 | Tampering | West Virginia government row collision | mitigate | Government subquery filters `AND state = 'VA'` per Pitfall 5 |
| T-101-20 | Tampering | Incorrect party data for HD-31..HD-100 (A1 assumption from research) | mitigate | Manual spot-check of 5 random delegates against elections.virginia.gov required before generator runs (per VALIDATION.md Manual-Only Verifications); planner records check outcome in task SUMMARY |
| T-101-21 | Tampering | Encoding corruption in generator output | mitigate | UTF-8 NoBOM enforced; static gate checks file bytes |
| T-101-SC | Tampering | npm/pip/cargo installs | mitigate | Reuses pre-installed pg and tsx; no new installs |
| T-101-22 | Information disclosure | Party stored vs displayed | accept | Antipartisan UI design — DB store is fine, no display |
</threat_model>

<verification>
After Task 2:

1. `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5120100 AND -5120001` = 100
2. `SELECT COUNT(*) FROM essentials.offices o JOIN essentials.chambers c ON c.id=o.chamber_id WHERE c.name='House of Delegates'` = 100
3. `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5120100 AND -5120001 AND office_id IS NULL` = 0
4. `SELECT full_name, is_vacant, is_active FROM essentials.politicians WHERE external_id=-5120020` returns (Vacant, true, false)
5. Party split: Democrat=64, Republican=35, ''/NULL=1
6. Section-split phase-gate query returns 0 rows
7. Re-apply of migration 303 produces 0 new rows
</verification>

<success_criteria>
- VA-GOV-04 satisfied: 100 delegates with offices linked to SLDL geofence districts; HD-20 correctly vacant
- Phase 101 phase gate clean: section-split check = 0 rows
- Idempotency proven
- No regression in OR/MD/ME delegate data (this migration touches only external_id range -5120100..-5120001 and House of Delegates chamber under State of Virginia)
</success_criteria>

<output>
Create `.planning/phases/101-va-state-government-db/101-04-SUMMARY.md` documenting:
- Generator + SQL + apply script paths
- CTE count line from generator console
- Apply script console output (count, 3 spot-checks, HD-20 vacancy JSON, NULL count)
- Post-apply Supabase verification results: count, party split, section-split check
- Manual spot-check result for HD-31+ party assumption (per VALIDATION.md Manual-Only Verifications) — 5 random delegates verified against elections.virginia.gov
- STATE.md update note
</output>
