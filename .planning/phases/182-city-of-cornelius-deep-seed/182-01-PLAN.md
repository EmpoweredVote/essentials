---
phase: 182-city-of-cornelius-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-cornelius-wave0-probe.sql
autonomous: false
requirements: [WASH-08]

must_haves:
  truths:
    - "Orchestrator has re-confirmed geo_id 4115550 has exactly 1 G4110 geofence row AND its name = 'Cornelius city' — RESEARCH CORRECTED the ROADMAP/CONTEXT-stated 4115350 (which EXISTS but resolves to 'Coquille city', a DIFFERENT real Oregon city) to 4115550; this probe runs FIRST, before any other, and must assert BOTH the 4115550 name-match AND that 4115350 is NOT Cornelius (existence alone is INSUFFICIENT — the highest-risk failure mode this milestone)"
    - "Orchestrator has confirmed no City of Cornelius government/chamber row already exists (greenfield) by geo_id 4115550 AND by name ILIKE '%cornelius%'"
    - "Orchestrator has confirmed the ext_id block -4115551..-4115555 is free of collisions (probed with margin -4115560..-4115540)"
    - "Orchestrator has confirmed the on-disk migration MAX so the next STRUCTURAL migration number is safe (research snapshot: on-disk MAX 1195 → next 1196; ledger MAX 1187 is a TRAP — on-disk counter authoritative)"
    - "The current seated roster is re-confirmed live from corneliusor.gov/267/City-Council: Mayor Jeffrey C. Dalin (ELECTED, 2-year term) + 3 filled councilor seats (Angeles Godinez Valencia ELECTED holds Council President title-on-seat; Edgar Baker APPOINTED; Edén López APPOINTED) + 1 genuinely VACANT seat (formerly Citlalli Nuñez-Barragán) — with special re-confirmation that the vacancy has NOT been filled (application window closes 2026-07-22; if a 5th member has been seated, STOP and re-research the roster)"
    - "corneliusor.gov WAF status confirmed NO-WAF for text AND headshots; ci.cornelius.or.us confirmed DEAD (must not be cited/fetched); all 4 filled-seat headshot ImageRepository URLs (documentIDs 2325, 1977, 2324, 1979) re-confirmed HTTP 200 as 1600×2000 transparent-background RGBA PNGs; documentID=1975 (vacant seat's leftover image) re-confirmed BLANK (no person)"
    - "The live compass topic_key list (is_live=true) is captured for stance ingestion; judicial-* topics counted and confirmed skipped (council-manager form — City Attorney/Municipal Judge appointed, not elected)"
    - "The banner candidate 'Cornelius Civic Center - Oregon.JPG' (native 1679×1412, ~1.19:1) is viewed directly at the 3.15:1 crop and a compositional call on the materially-heavy crop to 1700×540 is recorded for plan 05; optionally deepen the Wikimedia Category:Washington County, Oregon subcategories + Unsplash for a street-level alternative first (D-14)"
    - "Wave-0 checkpoint confirms the PURE AT-LARGE / directly-elected-Mayor (2-YEAR term, Charter §25) / PLAIN-TITLE (no positions, no wards, no X00xx geofences) model before any write — Charter §7 primary text is unambiguous"
    - "The vacant-seat modeling decision is recorded before plan 02 is authored: RECOMMENDED option (a) — seed the 5th office row with politician_id=NULL, is_vacant=true (TX-23 precedent, ext_id -4115555 intentionally unused, NO politician row), official_count=5; NEVER a placeholder person (D-13)"
    - "Decision coverage — D-01: council structure ground-truthed from corneliusor.gov + 2008 Charter, not memory; D-02: WHO-VOTES tie-breaker applied (whole city votes all seats per §7 → at-large branch, no custom geofences); D-05: Mayor's directly-elected citywide 2-year role re-confirmed; D-07: seated roster + exact body name verbatim + WAF/photo status captured; D-13: vacant seat modeled without a placeholder person; D-14: banner candidates viewed street-level-first; D-15: bilingual evidence landscape noted for plan 04"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-cornelius-wave0-probe.sql"
      provides: "The Wave-0 probe queries the orchestrator runs before any write"
      min_lines: 20
  key_links:
    - from: "probe.sql"
      to: "essentials.governments / essentials.politicians / essentials.geofence_boundaries / supabase_migrations.schema_migrations / inform.compass_topics"
      via: "SELECT probes"
      pattern: "essentials\\.(governments|politicians|geofence_boundaries)"
---

<objective>
Author the Wave-0 verification probe file for the City of Cornelius deep-seed and drive the
orchestrator-run DB + roster + web checks that MUST pass before any structural write. This plan gates
the entire phase: no government, roster, headshot, or stance migration may be authored or applied until
every probe below returns its expected result.

Purpose: Prevent the highest-risk failure modes for this phase. RESEARCH found the ROADMAP/CONTEXT
geo_id 4115350 is WRONG — and, uniquely in this milestone, it EXISTS but silently resolves to a
DIFFERENT real city ("Coquille city"), the most dangerous variant yet (a naive "does this geo_id have a
G4110 row" check passes incorrectly). The correct value is 4115550 — the geofence probe runs FIRST and
asserts BOTH that 4115550 has exactly one G4110 row named 'Cornelius city' AND that 4115350 is NOT
Cornelius. Other load-bearing checks: greenfield status + ext_id block + migration counter (ledger 1187
is a trap; on-disk MAX 1195 → next 1196 is authoritative); re-confirm the live roster (4 filled seats +
1 genuine vacancy — special attention to whether the vacancy has been filled since research, given the
open application window closing 2026-07-22, and to the PITFALL of never seating Citlalli
Nuñez-Barragán, whose name persists only as stale alt-text); re-download all 4 filled-seat headshot
ImageRepository URLs (RESEARCH found the BEST sourcing in the milestone — 4/4 direct, no WAF,
transparent 1600×2000 PNGs needing a white composite); capture the live topic_key list; view the banner
candidate; and record the vacant-seat modeling decision (recommended TX-23 option (a)) before plan 02.
Output: `_tmp-cornelius-wave0-probe.sql` (gitignored `_tmp-*` helper) + a recorded pass/fail for each
Wave-0 gate, saved in the SUMMARY for downstream plans 02-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md
@.planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO database
access, NO WebSearch, and NO Supabase MCP. The INLINE ORCHESTRATOR (main context) runs the probes via
`psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`, re-fetches the
roster from corneliusor.gov (NO WAF — direct curl works), re-downloads the 4 filled-seat headshot URLs,
and views the banner candidate image. This probe file is a gitignored `_tmp-*` helper — do NOT commit
it. C:/EV-Accounts is a SEPARATE git repo — never cd into it; use `git -C "C:/EV-Accounts"` if a commit
is ever needed.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except in Probe E where a query genuinely reads that table), and `photo_origin_url`
out of comments in any file authored this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-cornelius-wave0-probe.sql</files>
  <read_first>
    - .planning/phases/182-city-of-cornelius-deep-seed/182-RESEARCH.md §"geo_id Verification — CORRECTED (4115350 → 4115550)", §"Migration / Schema Technical Reference", §"Stance Evidence Landscape", §"Wave 0 Gaps"
    - .planning/phases/182-city-of-cornelius-deep-seed/182-PATTERNS.md §"D-16 geofence-existence-AND-name-match probe" and the geo_id note in "Shared Patterns"
    - C:/EV-Accounts/backend/scripts/_tmp-sherwood-wave0-probe.sql (the direct prior-phase probe-file analog — copy the labeled-probe A-G shape verbatim, adapting values; gitignored but present on disk)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-01-PLAN.md (the prior-phase probe-plan analog)
  </read_first>
  <action>
    Write labeled SELECT probes, each preceded by a `\echo` label line so the orchestrator reads results
    unambiguously. Do NOT wrap in a transaction (read-only); no DDL — SELECT/\echo only.
    Probe A (GEO_ID EXISTENCE-AND-NAME — RUN FIRST): select name, geo_id, mtfcc from
    essentials.geofence_boundaries WHERE geo_id='4115550' AND mtfcc='G4110' (EXPECT exactly 1 row, name
    = 'Cornelius city' — the RESEARCH-CORRECTED value). Add a part A2 that selects name from
    essentials.geofence_boundaries WHERE geo_id='4115350' AND mtfcc='G4110' — this WILL return a row, but
    its name is 'Coquille city' (NOT Cornelius); the probe's purpose is to prove the stated 4115350
    resolves to a DIFFERENT real city, so if A2's name ILIKE '%cornelius%' the whole assumption is
    inverted and the orchestrator must STOP. Add a part A3 that runs the authoritative name-search:
    select name, geo_id from essentials.geofence_boundaries WHERE mtfcc='G4110' AND name ILIKE
    '%cornelius%' (EXPECT exactly 1 row: 'Cornelius city' at geo_id 4115550). Probe B: count
    essentials.districts WHERE geo_id='4115550' (expect 0 — no city districts yet). Probe C: count
    essentials.governments WHERE geo_id='4115550' OR name ILIKE '%cornelius%' (expect 0 — greenfield).
    Probe D: select external_id from essentials.politicians WHERE external_id BETWEEN -4115560 AND
    -4115540 (expect 0 rows — ext_id block -4115551..-4115555 free, probed with margin); add a second
    name-based scan selecting full_name WHERE full_name ILIKE ANY of the roster names (Dalin / Godinez
    Valencia / Baker / López — expect 0, confirms no legacy partial-seed by name). Probe E: select
    MAX(version::bigint) from supabase_migrations.schema_migrations WHERE version ~ '^[0-9]{1,6}$'
    (record it; the research snapshot is ledger MAX 1187 which is a TRAP — the next STRUCTURAL migration
    number = on-disk file MAX + 1, research snapshot on-disk MAX 1195 → next 1196; the orchestrator
    cross-checks with an `ls` of the migrations dir). Probe F: select distinct state from
    essentials.districts WHERE geo_id='4159000' (Portland) to confirm lowercase 'or' is the correct
    casing for LOCAL/LOCAL_EXEC rows. Probe G: select topic_key from inform.compass_topics WHERE
    is_live=true ORDER BY topic_key — the authoritative live-topic list for the stance plan (04); also
    count how many begin with 'judicial-'. Include a header comment noting the file is a gitignored
    orchestrator-run helper. No fenced code in this action — the _tmp-sherwood-wave0-probe.sql analog is
    the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-cornelius-wave0-probe.sql` and records each result</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-cornelius-wave0-probe.sql with labeled probes A-G
    - Probe A selects name for geo_id='4115550' AND mtfcc='G4110' and expects name='Cornelius city' (name-match, not just existence); A2 selects name for geo_id='4115350' G4110 to prove it is 'Coquille city' (the wrong stated value, which EXISTS as a different city); A3 runs the name-search mtfcc='G4110' AND name ILIKE '%cornelius%' expecting the sole 4115550 row
    - Probe C queries governments by geo_id='4115550' OR name ILIKE '%cornelius%'
    - Probe D queries the exact ext_id range -4115560..-4115540 AND a name-based scan for the 4 roster names
    - Probe E reads MAX(version::bigint) from the migration ledger with a numeric-version filter
    - Probe F reads distinct districts.state for geo_id='4159000' (expect 'or' lowercase)
    - Probe G selects topic_key WHERE is_live=true and counts judicial-* topics
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE); not wrapped in a transaction
  </acceptance_criteria>
  <done>Probe file authored with all queries and correct literal values (CORRECTED geo_id 4115550 with name-match, the 4115350→Coquille trap check, ext_id range -4115560..-4115540).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes, re-fetches roster, re-downloads headshots, confirms all gates</name>
  <what-built>The Wave-0 probe file. The orchestrator now executes it against the live DB, re-fetches the live roster directly from corneliusor.gov (no WAF), re-downloads all 4 filled-seat headshot URLs, and views the banner candidate image.</what-built>
  <how-to-verify>
    A. Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-cornelius-wave0-probe.sql`
       (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
       1. Probe A returns geofence(4115550, G4110)=1 with name='Cornelius city'. If the name is NOT
          'Cornelius city' → STOP. A2 confirms geo_id=4115350 returns name='Coquille city' (a DIFFERENT
          real city — the trap); if A2's name ILIKE '%cornelius%' → STOP and reconcile which geo_id is
          correct. A3 confirms the name-search returns exactly the one 4115550 'Cornelius city' row.
       2. Probe B returns districts=0. If >0 → STOP, districts already exist (not greenfield).
       3. Probe C returns 0 (no existing Cornelius gov). If >0 → STOP, phase already partly seeded —
          switch to reconcile.
       4. Probe D returns 0 rows on BOTH the range scan and the name scan (ext_id block free, no legacy
          rows). If any row → shift the block and record the new range for plans 02/03/04.
       5. Probe E: cross-check against `ls C:/EV-Accounts/backend/migrations | grep -E '^[0-9]+_' | sort -t_ -k1 -n | tail`.
          Next STRUCTURAL migration = on-disk file MAX + 1 (research snapshot: on-disk 1195 → 1196;
          ledger MAX 1187 is a TRAP — do NOT use it). Record the confirmed number for plan 02.
       6. Probe F returns 'or' (lowercase) — the casing plan 02 must use for LOCAL/LOCAL_EXEC districts.
       7. Probe G returns the live topic_key list — save it verbatim for plan 04; note the judicial-*
          count for exclusion.
    B. Re-fetch the current roster directly from corneliusor.gov/267/City-Council (NO WAF — plain curl
       returns 200) and each individual bio page corneliusor.gov/Directory.aspx?eid={31,32,34,35}.
       Confirm the roster is unchanged since 2026-07-03: Mayor Jeffrey C. Dalin (directly elected
       citywide, 2-year term, term Jan 2025–Dec 2026, is_appointed=false); Councilor Angeles Godinez
       Valencia (ELECTED 2021 + re-elected 2025, term Dec 2028, holds Council President title-on-seat,
       is_appointed=false); Councilor Edgar Baker (APPOINTED June 2026–Dec 2026, is_appointed=true);
       Councilor Edén López (APPOINTED April 2023–Dec 2026, is_appointed=true); + 1 VACANT councilor
       seat. SPECIAL CHECK (RESEARCH Pitfall 2 + 3): re-confirm Baker AND López each show ONLY an
       "Appointed" entry (no "Elected"/"Re-Elected" line) on their bio pages → both is_appointed=true;
       and re-confirm the 5th seat is still "Vacant Position" and that Citlalli Nuñez-Barragán is NOT on
       the current roster (her name persists only as stale alt-text). CRITICAL TIMING CHECK: the vacancy
       application window closes 2026-07-22 — run a FRESH check for whether a new councilor has been
       appointed since research. If the vacancy has been FILLED (or any other member changed) → STOP and
       re-plan the roster in plan 02 (a filled 5th seat means a real person + a 5th headshot + a 5th
       stance migration, changing the ext_id assignment for -4115555).
    C. Headshot retrievability (RESEARCH §"Headshot Sources — 4/4 filled seats CONFIRMED"): re-download
       all 4 filled-seat ImageRepository URLs
       (corneliusor.gov/ImageRepository/Document?documentID=N for N=2325 Dalin, 1977 Godinez Valencia,
       2324 Baker, 1979 López) and confirm each returns HTTP 200 as a 1600×2000 (exact 4:5) RGBA PNG
       with a transparent background outside a circular photo mask. Note the pipeline nuance for plan 03:
       these need a WHITE-BACKGROUND COMPOSITE step (Henderson/Seebock RGBA precedent) but NO crop
       (already exactly 4:5) — composite then resize straight to 600×750. Separately re-download
       documentID=1975 (the vacant seat's leftover image, still alt-tagged "Citlalli Nuñez-Barragán")
       and confirm it is a BLANK placeholder (no person) — do NOT use it. Record per-official
       confirmations for plan 03. If any of the 4 filled-seat URLs now 404s, note it and fall to the D-9
       fallback chain for that official only — but a full 4/4 is expected.
    D. Confirm the structural decision holds (RESEARCH Form-of-Government, 2008 Charter §7/§24/§25):
       PURE AT-LARGE with a directly-elected Mayor + 4 designed councilor seats (3 filled + 1 vacant),
       every seat voted citywide → Beaverton/Tualatin/Forest Grove/Sherwood shape (1 LOCAL_EXEC Mayor
       district + 1 LOCAL at-large district on geo_id 4115550), NO ward/X00xx geofences, PLAIN
       'Mayor'/'Councilor' titles (§24 "by position" is a staggering mechanism, NOT numbered public
       seats — the live roster/directory use plain titles exclusively). NOTE THE 2-YEAR MAYOR TERM (§25
       — matches Sherwood, unlike the 4-year Beaverton/Hillsboro/Tigard/Tualatin/Forest Grove Mayors;
       affects the migration comment/label only, not the shape). Confirm judicial-* skip is safe:
       council-manager form (§33 City Manager) → City Attorney/Municipal Court Judge are appointed, not
       elected → skip all judicial-* topics for plan 04. If any of these is overturned → STOP and
       re-plan.
    E. RECORD THE VACANT-SEAT MODELING DECISION (RESEARCH Open Question 1) before plan 02 is authored:
       the RECOMMENDED option (a) — seed the 5th councilor office row with politician_id=NULL,
       is_vacant=true (TX-23 precedent, migrations/105_tx_congressional_house_officials.sql), external_id
       -4115555 intentionally UNUSED, NO politician row created, official_count=5 (matches the Charter's
       designed 5-seat body). Option (b) is to omit the vacant seat's office row entirely
       (official_count=4). Record the chosen option explicitly; NEVER seed a placeholder person (D-13).
    F. View the banner candidate image directly (RESEARCH §"Community Banner" / A6/A7 / Wave-0 Gap): the
       leading candidate "Cornelius Civic Center - Oregon.JPG" (the Public Library/City Hall — D-14's
       named alternate, CC BY-SA 3.0, M.O. Stevens), native 1679×1412 (~1.19:1) needing a materially
       heavier crop to the pipeline's 3.15:1 target than any predecessor banner. Record a compositional
       call (try `--vertical-anchor` in the 0.4-0.55 range) for plan 05. Optionally deepen the Wikimedia
       Commons search first — Category:Scenic images of Washington County, Oregon and Category:Structures
       in Washington County, Oregon, plus Unsplash — for a street-level Adair/Baseline main-street scene
       (D-14's top hint), since a clean license + good crop-fit beats subject preference; RESEARCH found
       no ideal street-level candidate as of 2026-07-03. Reject documented failure modes: single-building
       roof crops (the Town Center portrait / Grande Foods storefront) and aerials (D-14).
  </how-to-verify>
  <acceptance_criteria>
    - All probe gates recorded with pass/fail
    - geo_id confirmed: 4115550 (G4110) returns 1 with name='Cornelius city'; 4115350 (G4110) confirmed to be 'Coquille city' (the trap — a different real city, not absent); name-search returns the sole 4115550 Cornelius row
    - Confirmed ext_id block (default -4115551..-4115555 unless probe D forced a shift); no legacy rows by name
    - Confirmed next structural migration number (default 1196) via ledger + on-disk `ls` cross-check (ledger 1187 recognized as a trap)
    - Live topic_key list captured for plan 04; judicial-* count noted for exclusion
    - Live roster re-confirmed (Dalin ELECTED Mayor 2-year; Godinez Valencia ELECTED, Council President title-on-seat; Baker + López BOTH is_appointed=true; 5th seat VACANT) via direct corneliusor.gov fetch + fresh check; vacancy NOT filled since research (if filled → STOP and re-plan); Citlalli Nuñez-Barragán confirmed NOT on the roster
    - All 4 filled-seat headshot URLs re-downloaded HTTP 200 as 1600×2000 transparent RGBA PNG; white-composite-then-no-crop nuance noted for plan 03; documentID=1975 confirmed blank (unusable)
    - Pure at-large / directly-elected-Mayor (2-YEAR term §25) / PLAIN-title decision confirmed; council-manager form → judicial-* skip confirmed safe; no X00xx geofences
    - Vacant-seat modeling decision recorded (default option (a): TX-23 NULL-politician office row, official_count=5) before plan 02
    - Banner candidate image viewed; 3.15:1 crop call recorded for plan 05 (Civic Center/Library leading; deeper Commons/Unsplash street-level search optionally done first)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |
| operator → external web | Roster re-fetch, headshot re-download, and banner-image viewing from corneliusor.gov / Wikimedia (no WAF) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01 | Tampering | probe.sql executed on prod DB | accept | Probes are read-only SELECTs; no write path; no user input |
| T-182-02 | Spoofing | trusting the stated geo_id 4115350 (which EXISTS but is 'Coquille city') would seed the entire Cornelius roster onto a DIFFERENT real city's geofence — the worst geo_id failure mode this milestone | mitigate | Probe A runs FIRST and asserts 4115550 (G4110) returns exactly 1 row NAMED 'Cornelius city' AND that 4115350 resolves to 'Coquille city', before any write — existence alone is insufficient because the wrong value silently resolves |
| T-182-03 | Spoofing | stale roster (the open vacancy filled, or a member changed, since research — application window closes 2026-07-22) | mitigate | Direct corneliusor.gov re-fetch (no WAF) + fresh check specifically for whether the vacancy has been filled; STOP-and-re-plan if the 5th seat is now occupied |
| T-182-04 | Spoofing | seating Citlalli Nuñez-Barragán (stale alt-text) or modeling Baker/López as elected | mitigate | Wave-0 re-confirms the 5th seat is vacant (blank placeholder image) and that Baker + López each show only an "Appointed" bio entry → is_appointed=true; pitfall carried into plan 02 |
| T-182-05 | Information Disclosure | topic_key list from inform.compass_topics | accept | Public compass topic keys; no PII |
| T-182-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- geo_id 4115550 (G4110) confirmed present AND named 'Cornelius city', and 4115350 confirmed to be
  'Coquille city' (the trap), before any structural write is authored.
- The vacant-seat modeling decision (default option (a)) is recorded before the structural migration.
- Any failed gate blocks plans 02-05 until resolved.
</verification>

<success_criteria>
- geo_id 4115550 confirmed present + name-matched 'Cornelius city' (4115350 confirmed to be a different
  real city, Coquille); no duplicate Cornelius gov; districts absent; ext_id block free; next migration
  number confirmed (1196, ledger 1187 recognized as trap); live topic_key list captured; roster
  re-confirmed via direct fetch (Dalin Mayor 2-year, Godinez Valencia Council President, Baker + López
  appointed, 5th seat vacant, Nuñez-Barragán excluded); all 4 filled-seat headshot URLs re-downloaded
  HTTP 200 (transparent-PNG composite nuance noted); pure at-large + directly-elected-Mayor (2-year) +
  plain-title + judicial-skip decisions confirmed; vacant-seat modeling decision recorded; banner
  candidate viewed.
</success_criteria>

<output>
Create `.planning/phases/182-city-of-cornelius-deep-seed/182-01-SUMMARY.md` when done, recording the
confirmed geo_id (4115550), ext_id block, next structural migration number, live topic_key list,
re-confirmed roster (Dalin Mayor 2-year / Godinez Valencia Council President ELECTED / Baker + López
APPOINTED / 5th seat VACANT), per-official headshot-download confirmations, the vacant-seat modeling
decision (default option (a)), and the banner crop call for downstream plans 02-05.
</output>
</content>
</invoke>
