---
phase: 180-city-of-forest-grove-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["180-02"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-forest-grove-headshots.py
  - C:/EV-Accounts/backend/migrations/1179_forest_grove_headshots.sql
autonomous: false
requirements: [WASH-06]

must_haves:
  truths:
    - "Each Forest Grove official for whom a usable photo is found has a 600×750 headshot uploaded to Supabase Storage politician_photos/{uuid}-headshot.jpg (a partial N/7 outcome is an acceptable, honestly-documented result — RESEARCH confirmed a genuine JS-rendering sourcing gap, unlike Tualatin's 7/7)"
    - "Every headshot was cropped to 4:5 FIRST, then resized to 600×750 Lanczos q90 (no distortion, no overlays)"
    - "Sourcing followed the D-16 chain, JS-capable fetch of forestgrove-or.gov FIRST (photos are JS/AJAX-loaded, not curl-visible — not a WAF block), then Ballotpedia → Wikimedia → local-news (forestgrovenewstimes.com / newsinthegrove.com) as pre-authorized last resort"
    - "One politician_images row per SOURCED official (type='default', photo_license set by ACTUAL source — press_use only for government-hosted; verify per source), columns exactly (id, politician_id, url, type, photo_license) — NO photo_origin_url; guarded WHERE NOT EXISTS on politician_id"
    - "No image has superimposed text/graphics over the face; each is a valid non-zero JPEG (HTTP 200 from CDN)"
    - "D-14 WR-01: the pipeline script main() EXITS NON-ZERO if ANY configured official failed to upload — a chained automation cannot apply the audit migration on a silently-partial run (NEW template hardening; the inherited _tmp-tualatin-headshots.py only printed a WARNING and returned 0)"
    - "The headshot script does NOT hard-assert len(OFFICIALS)==7 (a partial outcome is expected given the sourcing gap); it DOES assert ext_ids are unique"
    - "The audit-only headshot migration post-verify asserts the ACTUAL sourced count (not a hard 7) of politician_images rows whose url embeds the politician uuid"
    - "Decision coverage — D-09: headshots crop-to-4:5 then resize 600×750 Lanczos q90, {uuid}-headshot.jpg naming, photo_license set by actual source, no fabrication; D-14 WR-01: non-zero exit on ANY upload failure; D-16: JS-capable fetch first, then external fallback chain, local news pre-authorized"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-forest-grove-headshots.py"
      provides: "Download → composite → crop 4:5 → resize 600×750 → upload pipeline with the WR-01 non-zero-exit fix"
      min_lines: 100
    - path: "C:/EV-Accounts/backend/migrations/1179_forest_grove_headshots.sql"
      provides: "politician_images INSERTs for each sourced official (audit-only, NOT registered in the ledger) with the actual-count url-embeds-uuid gate"
      min_lines: 20
      contains: "politician_images"
  key_links:
    - from: "_tmp-forest-grove-headshots.py"
      to: "Supabase Storage politician_photos bucket"
      via: "PUT {uuid}-headshot.jpg x-upsert:true"
      pattern: "politician_photos"
    - from: "1179_forest_grove_headshots.sql"
      to: "essentials.politicians (by external_id)"
      via: "subselect politician_id per row"
      pattern: "external_id = -41262"
---

<objective>
Author the headshot pipeline script and the audit-only migration that records the Forest Grove
officials' portraits. Unlike Tualatin (7/7 direct, no WAF, no fallback), forestgrove-or.gov's photos
are a GENUINE sourcing gap: the Meet the Council / Staff Directory pages are NO-WAF for text but the
photo widget is JS/AJAX-loaded and NOT curl-visible (a different failure mode than a WAF block). The
pipeline attempts a JS-capable fetch of forestgrove-or.gov FIRST, then falls to the D-16 chain
(Ballotpedia → Wikimedia → local news). Portraits are cropped 4:5 FIRST then resized to 600×750
Lanczos q90 and mirrored to Supabase Storage. Do NOT hard-assert 7/7 — a partial outcome is an
honest, acceptable result. Apply the D-14 WR-01 fix: main() MUST exit non-zero on ANY upload failure
(a NEW template hardening — every prior _tmp-*-headshots.py silently returned 0 on partial success).

Purpose: Satisfy the headshot portion of WASH-06 — officials render with 600×750 photos where sourced,
gaps documented honestly.
Output: `_tmp-forest-grove-headshots.py` (gitignored helper, run by the orchestrator, WR-01-fixed) +
audit-only migration `1179_forest_grove_headshots.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES both files. It has NO DB/Storage access and NO
WebSearch. The INLINE ORCHESTRATOR runs `_tmp-forest-grove-headshots.py` (which resolves each
politician UUID by external_id at runtime via psycopg2, sources each portrait per the D-16 chain,
processes, and uploads), then applies the audit migration via `psql -f`. The `.py` file is a
gitignored `_tmp-*` helper — do NOT commit it. The `.sql` migration IS committed to the EV-Accounts
repo (`git -C "C:/EV-Accounts"` — never cd into it).

**AUDIT-ONLY MIGRATION:** `1179_forest_grove_headshots.sql` does NOT write a schema_migrations ledger
row (headshot/stance migrations bypass the ledger; on-disk counter is authoritative). Keep the literal
strings `slug`, `schema_migrations`, and `photo_origin_url` OUT of comments.
</context>

<interfaces>
essentials.politician_images  (id, politician_id, url, type, photo_license)  -- NO photo_origin_url column; type='default'
-- politician UUIDs: resolve by external_id from 180-02-SUMMARY.md:
--   -4126201 Malynda Wenzl (Mayor) | -4126202 Michael Marshall | -4126203 Karen Martinez
--   -4126204 Mariana Valenzuela (Council President) | -4126205 Donna Gustafson
--   -4126206 Angel Falconer | -4126207 Brian Schimmel
-- Headshot sources: RESEARCH found 0 curl-visible URLs (JS-rendered widget). Wave-0 (180-01-SUMMARY.md)
-- recorded per-official findings from the JS-capable fetch attempt + any D-16 fallback results — use
-- those. Where Wave-0 found no usable photo for an official, OMIT that entry (honest gap, no fabrication).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the headshot pipeline script (with the WR-01 non-zero-exit fix)</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-forest-grove-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-tualatin-headshots.py (the ~470-line pipeline template — copy structure/functions verbatim, rename city strings; gitignored but present on disk. NOTE its main() at lines ~410-469 only prints a WARNING and returns 0 on partial success — that is the exact WR-01 defect to fix)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md §"_tmp-forest-grove-headshots.py (utility, file-I/O)" (incl. the fully-specified WR-01-fixed main()) and §"No Analog Found" (why WR-01 is new)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md §"Headshot Sources — GENUINE SOURCING GAP, D-16 FALLBACK CHAIN REQUIRED"
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md (per-official headshot-source findings from the Wave-0 JS fetch) + 180-02-SUMMARY.md (minted UUIDs by external_id)
  </read_first>
  <action>
    Copy the Tualatin script structure (config block, crop_to_4_5, resize_600x750, process_headshot_bytes,
    transparent-composite guard, resolve_politician_id, download_image, upload_to_storage,
    process_member, manifest output, CDN_BASE-from-SUPABASE_URL derivation, descriptive User-Agent,
    test_download_guard). Set an OFFICIALS list of dicts (up to 7), each with ext_id, name, url (the
    per-official source URL recorded in 180-01-SUMMARY.md from the Wave-0 JS-capable fetch, or from the
    D-16 fallback chain), and license set by ACTUAL source (press_use ONLY for government-hosted;
    verify per source for Ballotpedia/Wikimedia/local-news — do NOT assume press_use for a
    non-government host per D-09). Omit any official for whom Wave-0 found no usable photo (honest gap,
    no fabrication). Resolve each politician UUID at RUNTIME by external_id via psycopg2 (do NOT
    hardcode UUIDs). Keep the config block: BUCKET='politician_photos', TARGET_SIZE=(600,750),
    JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS. Pipeline order per official: download (follow
    redirects), composite onto white if RGBA/PNG, CROP to 4:5 FIRST, RESIZE to 600×750 Lanczos, reject
    any image with superimposed text/graphics, upload to politician_photos/{uuid}-headshot.jpg via PUT
    with x-upsert:true, emit a manifest line (ext_id → uuid → CDN url → source). Assert ext_ids are
    unique; DO NOT hard-assert len(OFFICIALS)==7 (a partial outcome is expected given the sourcing gap).
    APPLY THE D-14 WR-01 FIX to main(): collect per-official results (success/failure), print the
    manifest, then if ANY official failed to upload, print an EXIT 1 message and call sys.exit(1) — so
    a chained automation cannot proceed to apply migration 1179 on a silently-partial run. This is the
    exact defect being fixed: the inherited main() only printed a WARNING and returned 0. Include a
    docstring SOURCE NOTE describing the JS-rendering gap (not a WAF block), the D-16 chain order
    (JS-capable fetch first), the WR-01 fix, and marking the file a gitignored _tmp helper the
    orchestrator runs. No fenced code in this action — the Tualatin analog + 180-PATTERNS.md (which
    fully specifies the corrected main()) are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `python C:/EV-Accounts/backend/scripts/_tmp-forest-grove-headshots.py`; the script validates non-zero JPEG per image, prints the CDN manifest, and EXITS NON-ZERO if any configured upload failed (WR-01)</automated>
  </verify>
  <acceptance_criteria>
    - OFFICIALS list has one entry per SOURCED official (ext_ids within -4126201..-4126207) with the source URLs recorded at Wave-0
    - license is set per ACTUAL source (press_use only for government-hosted); non-government sources verified
    - asserts unique ext_ids; does NOT hard-assert len(OFFICIALS)==7 (partial outcome expected)
    - UUIDs resolved at runtime by external_id (no hardcoded UUIDs)
    - TARGET_SIZE=(600,750), JPEG_QUALITY=90, Lanczos resample; crop-to-4:5 happens BEFORE resize; rejects overlay/text images
    - main() calls sys.exit(1) when ANY configured official failed to upload (WR-01 fix — not just a printed WARNING/return 0)
    - Docstring SOURCE NOTE records the JS-rendering gap (not WAF), the D-16 chain order, and the WR-01 fix; marks it a gitignored _tmp helper run by the orchestrator
  </acceptance_criteria>
  <done>Pipeline script authored with the WR-01 non-zero-exit fix; runs and uploads 600×750 portraits for each sourced official, exiting non-zero on any upload failure.</done>
</task>

<task type="auto">
  <name>Task 2: Author the audit-only headshot migration</name>
  <files>C:/EV-Accounts/backend/migrations/1179_forest_grove_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1170_tualatin_headshots.sql (122-line template — url-embeds-uuid post-verify gate)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md §"1179_forest_grove_headshots.sql (audit-only headshot migration, CRUD)"
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-03 headshot manifest (from Task 1 run — the actual sourced count + per-official UUIDs)
  </read_first>
  <action>
    Single BEGIN…COMMIT. Header comment: "AUDIT-ONLY (not registered in the ledger)" plus a source note
    stating the JS-rendering gap and that a partial N/7 outcome is honest (do not fabricate). One
    politician_images INSERT … SELECT per SOURCED official (only those the pipeline actually uploaded),
    columns EXACTLY (id, politician_id, url, type, photo_license) — NO photo_origin_url. politician_id
    resolved via subselect on external_id (within -4126201..-4126207). url is the final Supabase CDN
    path (politician_photos/{uuid}-headshot.jpg — the orchestrator fills each {uuid} from the script
    manifest before applying). type='default'. photo_license set per ACTUAL source (press_use for
    government-hosted; the verified license for any Ballotpedia/Wikimedia/local-news source). Each
    INSERT guarded WHERE NOT EXISTS on politician_id (idempotent). Append the post-verify DO block from
    mig 1170: count politician_images rows JOINed to politicians WHERE external_id BETWEEN -4126207 AND
    -4126201 AND url LIKE '%' || politician_id::text || '%' — RAISE EXCEPTION if the count <> the ACTUAL
    number of officials sourced (NOT a hard 7 — this is the key deviation from Tualatin's no-caveat
    version, matching Tigard's honest-partial framing). NO ledger INSERT (audit-only). Keep
    `photo_origin_url`/`slug`/`schema_migrations` out of comments.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1179_forest_grove_headshots.sql` (the url-embeds-uuid DO block RAISEs unless the actual sourced count of rows embed their politician uuid) then runs `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -4126207 AND -4126201)`</automated>
  </verify>
  <acceptance_criteria>
    - One politician_images INSERT per SOURCED official; columns exactly (id, politician_id, url, type, photo_license)
    - No photo_origin_url column anywhere
    - politician_id resolved via external_id subselect (within -4126201..-4126207)
    - type='default'; photo_license set per actual source; each guarded WHERE NOT EXISTS on politician_id
    - url-embeds-uuid DO block asserts the ACTUAL sourced count (not a hard 7)
    - No ledger INSERT (audit-only); `photo_origin_url` does NOT appear in comments
    - Header source note documents the JS-rendering gap and honest-partial outcome
  </acceptance_criteria>
  <done>Audit migration authored recording each sourced image row with the actual-count url-embeds-uuid gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator runs pipeline, applies migration, verifies identity + quality</name>
  <what-built>The headshot pipeline script (WR-01-fixed) + audit migration. Orchestrator runs the pipeline (JS-capable fetch first, then D-16 chain) and applies the migration.</what-built>
  <how-to-verify>
    1. Run `python C:/EV-Accounts/backend/scripts/_tmp-forest-grove-headshots.py`; confirm each
       configured image sources (JS fetch of forestgrove-or.gov first, then D-16 fallback), processes,
       and yields a valid non-zero upload (HTTP 200 on each CDN URL). The script EXITS NON-ZERO if any
       configured official failed to upload (WR-01) — resolve or document each gap before applying the
       migration. A partial N/7 outcome is acceptable; document which officials had no usable photo.
    2. Apply `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1179_forest_grove_headshots.sql`;
       confirm the url-embeds-uuid DO block passes (count = actual sourced number) and the
       politician_images row count matches.
    3. Visual identity + quality check on each portrait: correct person (Wenzl/Marshall/Martinez/
       Valenzuela/Gustafson/Falconer/Schimmel), 600×750, 4:5 framing, NO superimposed text/graphics
       over the face. Confirm no wrong-person image (esp. no Truax photo for the Schimmel seat).
    4. Commit the migration: `git -C "C:/EV-Accounts" add backend/migrations/1179_forest_grove_headshots.sql && git -C "C:/EV-Accounts" commit`. Do NOT commit the _tmp-*.py helper.
  </how-to-verify>
  <acceptance_criteria>
    - Pipeline ran; WR-01 non-zero exit behaves correctly on any failure; each sourced headshot uploaded (partial N/7 documented honestly, never fabricated)
    - politician_images row count = actual sourced number; url-embeds-uuid DO block passed
    - Each portrait is the correct person (no Truax for Schimmel), 600×750, no overlays
    - Audit migration committed; _tmp .py NOT committed
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official source confirmation + any documented gaps, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → external image source | Portraits sourced via JS-capable fetch of forestgrove-or.gov, then the D-16 chain (Ballotpedia/Wikimedia/local news) |
| operator → Supabase Storage | Processed images uploaded to the public politician_photos bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-12 | Tampering | Partial-upload false success: a chained automation applies the audit migration after only some uploads succeeded | mitigate | D-14 WR-01 fix — main() calls sys.exit(1) when ANY configured official failed to upload (NEW; the inherited script returned 0); the audit migration's actual-count gate re-catches a mismatch |
| T-180-13 | Spoofing | Wrong-person or mislabeled headshot (esp. Truax vs Schimmel, or a local-news photo of a different person) | mitigate | Per-official named sources recorded at Wave-0; Task 3 visual identity check per official before commit; url-embeds-uuid gate ties each row to the right politician uuid |
| T-180-14 | Tampering | Corrupt/0-byte image or JS-widget HTML saved as .jpg | mitigate | Script validates non-zero bytes + valid PIL open; requests follows redirects; re-search any failure rather than fabricate |
| T-180-15 | Spoofing/IP | Unlicensed image from a local-news / community-paper source used without a verified license | mitigate | photo_license set per ACTUAL source (press_use only for government-hosted); non-government sources' licenses verified per D-09; genuine gaps documented, never fabricated |
| T-180-16 | Information Disclosure | Superimposed text/graphics on portrait | mitigate | Pipeline rejects images with overlays; visual check confirms clean face |
| T-180-SC | Tampering | npm/pip/cargo installs (Pillow/psycopg2/requests) | mitigate | Pre-existing project deps used by all prior headshot phases — no NEW package install; if any pip install is needed, treat as [ASSUMED] and gate on a blocking human-verify before install |
</threat_model>

<verification>
- Pipeline runs with WR-01 non-zero exit on any failure; each sourced 600×750 portrait uploaded;
  politician_images rows = actual sourced count; url-embeds-uuid gate passes; identity/quality
  confirmed; gaps documented; audit migration committed.
</verification>

<success_criteria>
- Each sourced official has a 600×750 headshot with no overlays; genuine gaps documented honestly (no
  fabrication); the pipeline exits non-zero on any upload failure (WR-01); audit migration recorded
  with the actual-count url-embeds-uuid gate; D-16 chain + JS-capable-fetch-first documented.
</success_criteria>

<output>
Create `.planning/phases/180-city-of-forest-grove-deep-seed/180-03-SUMMARY.md` when done, recording
the per-official image source (JS fetch vs which D-16 fallback) and the actual N/7 coverage with any
documented gaps.
</output>
