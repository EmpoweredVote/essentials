---
phase: 153-inglewood-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["153-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1020_inglewood_headshots.sql
autonomous: false
requirements: [INGL-01]

must_haves:
  truths:
    - "Each of the 5 current officials has exactly one type='default' headshot at the canonical Storage path"
    - "Dionne Faulk's two images are deduped to a single canonical row"
    - "The Eloy Morales survivor (666263) carries one valid headshot (migrated then upgraded to the official D3 portrait)"
    - "Every headshot is the actual Inglewood official (no name-collision, no superimposed text), 600x750 4:5"
    - "photo_origin_url on the politician row records the real source for each official"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1020_inglewood_headshots.sql"
      provides: "headshot dedup + verify-and-fix + greenfield insert + photo_origin_url backfill (AUDIT-ONLY, not registered)"
      contains: "inglewood_headshots"
  key_links:
    - from: "essentials.politician_images (type='default')"
      to: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      via: "canonical x-upsert upload"
      pattern: "politician_photos/.*-headshot.jpg"
    - from: "essentials.politicians.photo_origin_url"
      to: "cityofinglewood.org ImageRepository documentID"
      via: "source backfill"
      pattern: "ImageRepository/Document.documentID="
---

<objective>
Wave 3 of the Inglewood deep-seed: give all 5 current officials a clean, correct, 600x750 headshot in
ONE AUDIT-ONLY migration (`1020_inglewood_headshots.sql` — raw SQL, NOT registered in
`schema_migrations`; ledger stays 1019).

Image state entering this wave: Butts 1, Gray 1, Faulk 2 (DEDUP to 1), Eloy 666263 1 (migrated from
the dup in Wave 1 — upgrade to the official D3 portrait), Padilla 0 (greenfield INSERT, or pre-existing
if relinked). Source is cityofinglewood.org `/ImageRepository/Document?documentID=NNNN` — RESEARCH
confirmed NO-WAF (direct curl HTTP 200). Crop 4:5 FIRST -> resize 600x750 Lanczos q90 -> upload to
`politician_photos/{uuid}-headshot.jpg` (x-upsert). Wrong-person guard carried from West Covina.

Purpose: a correct portrait for every current official; honest documented gaps if any portrait fails.
Output: processed images in Supabase Storage + `1020_inglewood_headshots.sql` committed to EV-Accounts.
This wave ENDS WITH A BLOCKING HUMAN-VERIFY CHECKPOINT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-inglewood-deep-seed/153-CONTEXT.md
@.planning/phases/153-inglewood-deep-seed/153-RESEARCH.md
@.planning/phases/153-inglewood-deep-seed/153-PATTERNS.md
@.planning/phases/153-inglewood-deep-seed/153-VALIDATION.md
@.planning/phases/153-inglewood-deep-seed/153-02-SUMMARY.md
@LOCATION-ONBOARDING.md

<reference_data>
Headshot documentIDs (cityofinglewood.org/ImageRepository/Document?documentID=NNNN — NO-WAF, all HTTP 200):
  Gray D1     21642   pol 7a04bf87  ext_id 666261
  Padilla D2  21957   pol/ext_id from Wave 2 (created -7010xx or relinked)
  Eloy D3     21958   pol 6ed19c10  ext_id 666263   (overwrites the Wave-1 migrated photo — higher quality, correct per Pitfall 3)
  Faulk D4    21989   pol 729bc539  ext_id 666264   (DEDUP existing 2 rows -> 1, then point at this)
  Butts Mayor 20637   pol f5775ca1  ext_id -200740  (test 20637 vs 20639; pick higher quality)
Canonical Storage URL: https://kxsdzaojfaibhuzmclfq.supabase.co/storage/v1/object/public/politician_photos/{uuid}-headshot.jpg
photo_license: 'press_use' for cityofinglewood.org portraits; 'fair_use' + photo_origin_url=NULL for operator-supplied.
Skill: .claude/skills/find-headshots (find-headshots) is available for sourcing/fallback.
Fallback sources if a city portrait fails inspection: Ballotpedia (Faulk/Morales 2024), joinjamesbutts.com (Butts), LA Sentinel.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Headshot pre-flight — inspect Faulk's 2 rows + Eloy migrated photo + each current image state</name>
  <files>(read-only; no file written this task)</files>
  <read_first>
    - .planning/phases/153-inglewood-deep-seed/153-RESEARCH.md §CQ-4 (documentIDs + NO-WAF) + §Open Questions 2 (Faulk 2-image dedup strategy) + §Common Pitfalls Pitfall 3 (Eloy double-image after migrate+insert) + §Assumptions Log A2/A4
    - .planning/phases/153-inglewood-deep-seed/153-PATTERNS.md §Pattern 18 (Faulk 2->1 dedup, no file precedent — pre-flight identify the row to delete by UUID) + §Pattern 15-17/19-20
    - .planning/phases/153-inglewood-deep-seed/153-02-SUMMARY.md (Padilla's final ext_id + pol UUID)
  </read_first>
  <action>
    Via `mcp__supabase-local__execute_sql`, capture exact image state per current official:
    (a) Faulk dedup decision: `SELECT id, url, type, photo_license, photo_origin_url FROM essentials.politician_images WHERE politician_id='729bc539-3175-4e5d-96ba-c18768890e1e'`. Determine same-URL-dup vs two-sources; pick the row UUID to DELETE (keep one type='default'). Record both UUIDs.
    (b) Eloy migrated photo: `SELECT id, url FROM essentials.politician_images WHERE politician_id='6ed19c10-7b34-47f0-8705-0d154271e362'` — confirm exactly 1 row from the Wave-1 migration; it will be UPDATEd to the canonical path (official D3 portrait 21958), NOT inserted as a second row (avoid Pitfall 3).
    (c) Each of Butts/Gray/Padilla: confirm the type='default' row count (Butts 1, Gray 1, Padilla 0-or-1 depending on Wave-2 branch).
    (d) Resolve Padilla's pol UUID + ext_id from the Wave-2 summary.
    Produce a written pre-flight image-state block: Faulk delete-row UUID, Eloy upgrade-not-insert note, per-member insert-vs-update plan, Padilla identifiers.
  </action>
  <verify>
    <automated>MISSING — read-only MCP queries; capture in SUMMARY pre-flight block</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id='729bc539-3175-4e5d-96ba-c18768890e1e'` -> 2; both row UUIDs recorded; delete-target chosen
    - `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id='6ed19c10-7b34-47f0-8705-0d154271e362'` -> 1 (migrated); upgrade-not-insert plan recorded
    - Padilla pol UUID + ext_id resolved from Wave-2 summary; his image count recorded (0 = greenfield INSERT plan)
  </acceptance_criteria>
  <done>Per-official image-state block written: Faulk delete-row UUID chosen, Eloy upgrade plan set, insert-vs-update decided per member, Padilla identifiers resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Source + process + upload 5 headshots; author + apply + commit 1020_inglewood_headshots.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1020_inglewood_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1002_elmonte_headshots.sql (lines 1-68 — AUDIT-ONLY header, greenfield guarded INSERT 20-26, UPDATE existing default row 29-52, photo_origin_url backfill 55-61, audit post-verification 63-68)
    - C:/EV-Accounts/backend/migrations/1012_west_covina_headshots.sql (lines 1-63 — verify-and-fix existing rows; the WRONG-PERSON Gutierrez lesson 8-11; operator-supplied fair_use + photo_origin_url=NULL pattern 51-52)
    - .planning/phases/153-inglewood-deep-seed/153-PATTERNS.md §Pattern 15-20 + §Shared Patterns (Storage path; 600x750 4:5 Lanczos q90; photo_license; wrong-person guard)
    - LOCATION-ONBOARDING.md §Step 4 Headshots (4:5 crop FIRST then resize)
    - Task 1 pre-flight image-state block
  </read_first>
  <action>
    For each of the 5 current officials:
    (1) `curl -L -o <name>.jpg "https://www.cityofinglewood.org/ImageRepository/Document?documentID=<id>"` (Gray 21642, Padilla 21957, Eloy 21958, Faulk 21989, Butts 20637 — also pull 20639 for Butts and pick the higher-quality one).
    (2) WRONG-PERSON GUARD (West Covina lesson): visually confirm each image is the actual Inglewood official matching the name/district on the official city page; reject any name-collision (e.g. Eloy Morales the painter; any unrelated James Butts) or image with superimposed text/graphics over the face.
    (3) Crop 4:5 FIRST, THEN resize to 600x750 Lanczos q90 (never stretch; eyes ~1/3 from top; full head+shoulders). Use the find-headshots skill / established pipeline.
    (4) Upload to Supabase Storage `politician_photos/{pol_uuid}-headshot.jpg` with x-upsert (public). If a city portrait fails inspection, use a fallback source (Ballotpedia/campaign/LA Sentinel) with the right license, or document an honest gap (no fabricated photo).
    Then author `C:/EV-Accounts/backend/migrations/1020_inglewood_headshots.sql` (AUDIT-ONLY — `BEGIN; ... COMMIT;`, NO schema_migrations registration; header notes ledger stays 1019). In SQL:
    - Faulk DEDUP: `DELETE FROM essentials.politician_images WHERE id='<delete_target_uuid>' AND politician_id='729bc539-3175-4e5d-96ba-c18768890e1e' AND type='default'` (the row chosen in Task 1), then UPDATE the surviving row to the canonical path + press_use.
    - Eloy 666263: UPDATE the migrated row (do NOT insert a second) to canonical path + press_use (Pitfall 3).
    - Butts + Gray: UPDATE their existing type='default' row to canonical path + press_use.
    - Padilla: greenfield guarded INSERT (Pattern 16) if 0 images, else UPDATE.
    - photo_origin_url backfill on `essentials.politicians` (NOT on the image row — Pattern 19): one guarded `UPDATE essentials.politicians SET photo_origin_url='https://www.cityofinglewood.org/ImageRepository/Document?documentID=<id>' WHERE external_id=<ext> AND photo_origin_url IS DISTINCT FROM '<url>'` per official. For any operator-supplied/fallback portrait with no canonical URL: `SET photo_origin_url=NULL` + image photo_license='fair_use'.
    Append the audit post-verification comment block (each n_default=1; schema_migrations MAX unchanged at 1019). APPLY live via `mcp__supabase-local__execute_sql`. Commit: `git -C "C:/EV-Accounts" add backend/migrations/1020_inglewood_headshots.sql && git -C "C:/EV-Accounts" commit -m "feat(153): Inglewood headshots — Faulk dedup + 5 official portraits 600x750"`.
  </action>
  <verify>
    <automated>MISSING — apply via MCP then run acceptance assertion queries via mcp__supabase-local__execute_sql; image dimensions verified during processing</automated>
  </verify>
  <acceptance_criteria>
    - each current official has exactly one type='default' image: `SELECT p.external_id, COUNT(pi.*) FILTER (WHERE pi.type='default') n FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id=p.id WHERE p.external_id IN (-200740,666261,666263,666264,<padilla_ext>) GROUP BY p.external_id` -> every n=1
    - Faulk deduped: `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id='729bc539-3175-4e5d-96ba-c18768890e1e' AND type='default'` -> 1
    - Eloy single image (no Pitfall-3 dup): `SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id='6ed19c10-7b34-47f0-8705-0d154271e362'` -> 1
    - all 5 carry canonical Storage URL: each url matches `politician_photos/{their_uuid}-headshot.jpg`; each uploaded object returns HTTP 200
    - photo_origin_url set (or deliberately NULL for operator-supplied): `SELECT external_id, photo_origin_url FROM essentials.politicians WHERE external_id IN (-200740,666261,666263,666264,<padilla_ext>)` — each non-NULL unless documented operator-supplied
    - all processed images are 600x750 (4:5), correct person, no superimposed text
    - ledger UNCHANGED: `SELECT MAX(version::int) FROM supabase_migrations.schema_migrations` -> 1019; file committed to EV-Accounts
  </acceptance_criteria>
  <done>5 portraits sourced/verified/processed to 600x750 and uploaded; 1020_inglewood_headshots.sql applied (audit-only, ledger 1019) and committed; Faulk deduped to 1; Eloy single image; photo_origin_url backfilled; any gap documented honestly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>5 Inglewood official headshots (Mayor Butts, Gray D1, Padilla D2, Eloy Morales D3, Faulk D4) sourced from cityofinglewood.org, cropped 4:5 to 600x750, uploaded to Supabase Storage, and wired into politician_images (Faulk deduped 2->1; Eloy upgraded from the Wave-1 migrated photo). photo_origin_url backfilled.</what-built>
  <how-to-verify>
    1. Visit https://essentials.empowered.vote/results?browse_geo_id=0636546&browse_mtfcc=G4110 and confirm each of the 5 officials renders with a photo.
    2. For EACH official, compare the rendered headshot against the official city page (Gray /574, Padilla /587, Eloy /592, Faulk /596, Butts /820 + /491) — confirm it is the ACTUAL person (wrong-person guard, the West Covina Gutierrez lesson), not a name-collision.
    3. Confirm no superimposed text/graphics over any face; framing is head+shoulders, eyes ~1/3 from top; no distortion.
    4. Confirm no duplicate/stale image rows (Faulk = 1, Eloy = 1).
    5. If any portrait is an honest documented gap, confirm it is genuinely unavailable (no fabricated photo).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 4 (stances), or describe which headshot(s) need re-sourcing/re-cropping.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cityofinglewood.org (untrusted external) -> processed image -> Supabase Storage + DB | External image content crosses into our store; surface is wrong-person/integrity, not injection |
| executor SQL -> production Supabase | Audit-only static SQL mutating image rows |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-09 | Spoofing/Integrity | Wrong-person headshot (name-collision: Eloy Morales painter, unrelated James Butts) | mitigate | Visual match against official city district page before upload; BLOCKING human-verify checkpoint |
| T-153-10 | Tampering | Faulk dedup deletes the wrong row / leaves 2 rows | mitigate | Pre-flight identifies delete-target by UUID; DELETE guarded by id+politician_id+type; assert exactly 1 remains |
| T-153-11 | Tampering | Eloy double-image (migrate then insert — Pitfall 3) | mitigate | UPDATE the migrated row to the official portrait; never INSERT a second row for 666263 |
| T-153-12 | Repudiation | Missing photo_origin_url (can't trace source) | mitigate | Guarded photo_origin_url backfill per official; NULL only for documented operator-supplied/fair_use |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- 5 officials each have exactly one type='default' headshot at the canonical path; all HTTP 200.
- Faulk deduped to 1; Eloy single image; photo_origin_url set or documented NULL.
- All images 600x750, correct person, no superimposed text — confirmed at the blocking human-verify checkpoint.
- schema_migrations MAX unchanged (1019); file committed.
</verification>

<success_criteria>
- ROADMAP success criterion 3: 600x750 headshots uploaded for all officials with an available official portrait; genuine gaps documented (no fabricated photos).
- ROADMAP success criterion 5 (partial): browse view renders the roster with photos; no duplicate/stale image rows.
</success_criteria>

<output>
Create `.planning/phases/153-inglewood-deep-seed/153-03-SUMMARY.md` when done (per-official source documentID, license, any fallback/operator-supplied note, Faulk dedup outcome, and checkpoint approval).
</output>
