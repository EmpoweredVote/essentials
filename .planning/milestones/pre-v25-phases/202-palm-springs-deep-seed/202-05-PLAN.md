---
phase: 202-palm-springs-deep-seed
plan: 05
type: execute
wave: 5
depends_on: ["202-04"]
files_modified:
  - src/lib/coverage.js
autonomous: true
requirements: [BANR-01, CV-02]
must_haves:
  truths:
    - "coverage.js COVERAGE_STATES → California → areas[] has a DB-honest 'Palm Springs' chip (hasContext:true, since stances landed in Plan 04) that browses geo_id 0655254"
    - "The chip is inserted alphabetically between 'Norwalk' and 'Palmdale' in the CA areas array"
    - "buildingImages.js is UNCHANGED — the 'palm springs' CURATED_LOCAL banner key already shipped in Phase 201 and already resolves via the representingCity path"
    - "npm run build succeeds and the coverage generator sanity-checks the array shape"
  artifacts:
    - path: "src/lib/coverage.js"
      provides: "COVERAGE_STATES California 'Palm Springs' chip, browseGovernmentList ['0655254'], browseStateAbbrev 'CA', hasContext:true"
      contains: "Palm Springs"
  key_links:
    - from: "coverage.js 'Palm Springs' chip"
      to: "buildingImages.js CURATED_LOCAL 'palm springs' (already live)"
      via: "browse_label 'Palm Springs' → representingCity → getBuildingImages substring match"
      pattern: "Palm Springs"
    - from: "coverage.js 'Palm Springs' chip"
      to: "essentials government geo_id 0655254"
      via: "browseGovernmentList ['0655254']"
      pattern: "0655254"
---

<objective>
Surface Palm Springs in the app's browse UI by appending one DB-honest chip to
`src/lib/coverage.js` (COVERAGE_STATES → California → areas[]), inserted alphabetically between
Norwalk and Palmdale. This is the ENTIRETY of BANR-01 for this phase: the Palm Springs community
banner was already sourced and shipped in Phase 201 (`cities/palm-springs.jpg`, live in
`buildingImages.js` line 436), so NO new banner sourcing and NO change to `buildingImages.js` is
needed — only the coverage chip that makes the already-live banner + roster + compass reachable via
browse.

Purpose: Satisfies ROADMAP success criterion #5 for CV-02/BANR-01 (city surfaced with a DB-honest
chip; banner reuse verified).
Output: one-line append to `src/lib/coverage.js` in THIS repo (`main`→Render).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md
@.planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md
@src/lib/coverage.js

<execution_note>
This is a FRONTEND change in the CURRENT repo (`essentials`, `main`→Render) — the executor CAN edit
`coverage.js` and run `npm run build`. Do NOT touch `buildingImages.js` (banner already shipped Ph201).
`hasContext:true` is correct ONLY because Plan 04 already landed ≥1 stance row; if this plan somehow
runs before Plan 04 (it must not — depends_on 202-04), stage `hasContext:false` first.
</execution_note>

<interfaces>
Key contracts (from 202-PATTERNS.md coverage block + the live file):
- Insertion point: between `{ label: 'Norwalk', browseGovernmentList: ['0652526'], ... }` and
  `{ label: 'Palmdale', browseGovernmentList: ['0655156'], ... }` in the California areas[] array.
- Exact chip to insert: `{ label: 'Palm Springs', browseGovernmentList: ['0655254'], browseStateAbbrev: 'CA', hasContext: true },`
- buildingImages.js already has (line ~436, NO CHANGE): `'palm springs': { state: 'CA', src: '.../cities/palm-springs.jpg' }`.
- Sanity: `node scripts/gen-coverage.mjs` (prebuild generator) + `npm run build`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Palm Springs coverage chip + build sanity</name>
  <files>src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 346-377 — the coverage.js insertion point + exact chip shape + hasContext-only-once-stances-exist rule; the buildingImages.js NO-CHANGE note)
    - src/lib/coverage.js (the live California areas[] array — confirm the exact Norwalk/Palmdale gap and the surrounding chip shape before editing)
  </read_first>
  <action>
    In `src/lib/coverage.js`, inside the COVERAGE_STATES California `areas[]` array, insert exactly one
    new object ALPHABETICALLY between the existing `Norwalk` entry and the `Palmdale` entry:
    `{ label: 'Palm Springs', browseGovernmentList: ['0655254'], browseStateAbbrev: 'CA', hasContext: true }`.
    Match the surrounding entries' indentation and trailing-comma style exactly. Set `hasContext: true`
    (Plan 04 already landed stance rows — this is a DB-honest purple chip). Do NOT edit any other entry,
    do NOT touch `src/lib/buildingImages.js` (the 'palm springs' CURATED_LOCAL banner key is already
    live from Phase 201 and resolves via the representingCity path once the Plan 02 offices exist). Then
    run `node scripts/gen-coverage.mjs` (prebuild array-shape sanity) and `npm run build` (Vite build)
    to confirm no syntax error.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && git diff --quiet -- src/lib/buildingImages.js && grep -qF "{ label: 'Palm Springs', browseGovernmentList: ['0655254'], browseStateAbbrev: 'CA', hasContext: true }" src/lib/coverage.js && node scripts/gen-coverage.mjs && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/coverage.js` contains exactly one `Palm Springs` chip: browseGovernmentList ['0655254'], browseStateAbbrev 'CA', hasContext true.
    - The chip sits alphabetically between Norwalk and Palmdale in the CA areas[] array.
    - `src/lib/buildingImages.js` is unchanged (git diff shows no modification to it).
    - `node scripts/gen-coverage.mjs` exits 0; `npm run build` succeeds.
    - No other coverage entry modified.
  </acceptance_criteria>
  <done>Palm Springs DB-honest chip appended to coverage.js (alphabetical, hasContext:true, geo_id 0655254); build + coverage generator green; buildingImages.js untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time static config → app render | The coverage chip is consumed at build/render; no runtime user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-CHIP | Tampering | wrong geo_id / premature hasContext | mitigate | geo_id '0655254' matches the Plan 02 government row; hasContext:true only because Plan 04 (depends_on) landed stance rows; `npm run build` catches shape errors |
| T-202-BANNER | Information Disclosure | broken/missing banner image | accept | Banner already live from Ph201 (`cities/palm-springs.jpg`); Plan 06 verifies the CDN URL returns 200 and renders via the browse chip |
| T-202-SC | Tampering | npm build-tool installs | accept | Zero new package installs — coverage.js + existing Vite build only |
</threat_model>

<verification>
- coverage.js has the Palm Springs chip (alphabetical, hasContext:true, 0655254).
- buildingImages.js unchanged; banner reuse confirmed in Plan 06.
- Build + coverage generator pass.
</verification>

<success_criteria>
Palm Springs surfaces in browse with a DB-honest chip; the already-shipped banner resolves.
</success_criteria>

<output>
Create `.planning/phases/202-palm-springs-deep-seed/202-05-SUMMARY.md` when done (record the chip added +
confirmation that buildingImages.js was NOT changed + build result).
</output>
