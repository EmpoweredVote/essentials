---
phase: 160-nevada-legislature-seed-headshots
plan: 02
type: execute
wave: 2
depends_on: ["160-01"]
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-nv-legislature-headshots.py
  - C:/EV-Accounts/backend/migrations/1054_nv_legislature_headshots.sql
autonomous: true
requirements: [NV-LEG-01, NV-LEG-02]
user_setup: []

must_haves:
  truths:
    - "All 63 legislators (minus any documented genuine gaps) have a 600x750 headshot in the politician_photos bucket"
    - "Each headshot is crop-to-4:5-then-resized (not stretched) and re-encoded clean JPEG"
    - "Each headshot has a politician_images row (type='default') pointing at its CDN URL"
    - "Any legislator with no findable authoritative portrait is documented as a genuine gap (no fabricated photo)"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-nv-legislature-headshots.py"
      provides: "Download (archive.leg.state.nv.us) → crop-4:5 → resize 600x750 Lanczos q90 → Storage upsert pipeline; runtime UUID resolve by external_id; 63-row manifest"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1054_nv_legislature_headshots.sql"
      provides: "Audit-only politician_images INSERTs (one per legislator with a uploaded headshot), columns (id, politician_id, url, type, photo_license), NOT registered"
      contains: "essentials.politician_images"
  key_links:
    - from: "_tmp-nv-legislature-headshots.py"
      to: "essentials.politicians (external_id -3203001..-3204042)"
      via: "runtime psycopg2 lookup SELECT id WHERE external_id=%s"
      pattern: "external_id = %s"
    - from: "1054_nv_legislature_headshots.sql"
      to: "politician_photos CDN ({uuid}-headshot.jpg)"
      via: "politician_images.url literal"
      pattern: "politician_photos"
---

<objective>
Acquire and install all 63 legislator headshots: write the gitignored `_tmp-nv-legislature-headshots.py` pipeline (download the full-size official portrait from archive.leg.state.nv.us, crop-to-4:5 then resize to 600x750, upload to Storage), and write the audit-only migration 1054 that records the politician_images rows. The script resolves each legislator's UUID at runtime by external_id (UUIDs are minted by Plan 01's migration 1053). Any portrait that cannot be sourced is left empty and documented as a genuine gap — no fabrication (D-03 / SC#3).

Purpose: Satisfies the headshot half of NV-LEG-01 + NV-LEG-02 (Success Criterion 3 — all 63 legislators have 600x750 headshots, genuine gaps documented).
Output: _tmp-nv-legislature-headshots.py (pipeline), 1054_nv_legislature_headshots.sql (audit-only), and the uploaded 600x750 JPEGs in the politician_photos bucket.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP and cannot upload to Storage):**
- The executor WRITES `_tmp-nv-legislature-headshots.py` and `1054_nv_legislature_headshots.sql` to disk and documents the exact run/apply steps. The executor does NOT run the script, upload to Storage, or apply the migration.
- Running the headshot script (download + crop/resize + Storage PUT), capturing the manifest, and applying 1054 via `mcp__supabase-local__execute_sql` are INLINE ORCHESTRATOR steps performed by execute-phase.
- Each task marks executor-write vs inline-orchestrator-apply.
- Files live in the SEPARATE repo C:/EV-Accounts/backend/. The script is gitignored (`_tmp-*`); SUMMARY.md goes in the essentials .planning phase dir.
</execution_context>

<context>
@.planning/phases/160-nevada-legislature-seed-headshots/160-RESEARCH.md
@.planning/phases/160-nevada-legislature-seed-headshots/160-PATTERNS.md

<interfaces>
<!-- Research-verified facts the executor needs. Do NOT re-derive. -->
Storage bucket: politician_photos
CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
Storage path: {politician_uuid}-headshot.jpg
TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS
Senate source base:   https://archive.leg.state.nv.us/Session/84th2027/legislators/Senators/Images/
Assembly source base: https://archive.leg.state.nv.us/Session/84th2027/legislators/Assembly/Images/
Full-size file = {Mangled}.{id}.jpg  (NOT .Thumb.jpg — that is ~7-8KB, too small)
Per-member {Mangled}.{id} filenames + external_ids + chamber are in 160-RESEARCH.md "The Roster" (all 63 verified HTTP-200)
photo_license default (A2): 'us_government_work' (NV state-legislature work; 'public_domain' also defensible)
Fallback order (D-02, only if a primary URL fails): Wikimedia Commons → Open States (openstates.org)
Wikimedia descriptive UA (ONLY for *.wikimedia.org fetches — generic UA → HTTP 429):
  'EmpoweredVote-HeadshotBot/1.0 (https://empowered.vote; contact chris@empowered.vote)'
Env (C:/EV-Accounts/backend/.env): SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL
politician_images columns: (id, politician_id, url, type, photo_license) — NO photo_origin_url; type='default'
The 63 external_id→UUID map is captured in 160-01-SUMMARY.md (minted by migration 1053)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the headshot pipeline script _tmp-nv-legislature-headshots.py</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-nv-legislature-headshots.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-va-delegates-headshots.py (PRIMARY ANALOG — verbatim pipeline: env-load, runtime UUID resolve, requests download, PIL crop_to_4_5/resize, Storage upsert, manifest)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-PATTERNS.md (section "_tmp-nv-legislature-headshots.py" — config block, ROSTER substitution, crop-first pipeline, psycopg2 connection, manifest)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-RESEARCH.md ("The Roster" — 63 {Mangled}.{id} filenames + ext_ids + chamber; "Headshot Sources" — full-size vs .Thumb, fallback, Wikimedia UA)
  </read_first>
  <action>
Write `_tmp-nv-legislature-headshots.py` modeled verbatim on `_tmp-va-delegates-headshots.py`. Copy the env-load block, `crop_to_4_5(img)`, `resize_600x750(img)`, `upload_to_storage(uuid, jpeg_bytes)` (x-upsert PUT), and `resolve_politician_id(cursor, external_id)` (runtime psycopg2 lookup `SELECT id FROM essentials.politicians WHERE external_id=%s`) unchanged. Constants: BUCKET='politician_photos', the politician_photos CDN base, TARGET_SIZE=(600,750), JPEG_QUALITY=90, RESAMPLE=Image.Resampling.LANCZOS. Define SENATE_BASE and ASSEMBLY_BASE archive URLs.

Build a ROSTER list of 63 dicts hardcoding the verified filenames from 160-RESEARCH.md "The Roster": each entry `{'ext_id': <int>, 'name': '<full_name>', 'image': '<Mangled>.<id>', 'chamber': 'Senators'|'Assembly'}`. Hardcode the exact {Mangled}.{id} strings (already HTTP-200 verified) rather than re-deriving the name-mangling rule. Source URL = `{base}{image}.jpg` (full-size, NOT .Thumb.jpg).

Per member: resolve UUID at runtime by ext_id (never hardcode a UUID), download the full-size JPEG with normal browser headers, `convert('RGB')`, min-dimension sanity check, `crop_to_4_5` → `resize_600x750` → save JPEG quality=90 optimize=True (re-encode strips EXIF/stego), upload to `politician_photos/{uuid}-headshot.jpg` with x-upsert. On a primary-URL failure, attempt the D-02 fallback (Wikimedia Commons → Open States); use the WIKIMEDIA_UA descriptive User-Agent ONLY for *.wikimedia.org fetches (generic UA → HTTP 429, the 159 lesson). If no source resolves, SKIP the member (no fabrication) and emit a FAILED manifest line. psycopg2 connect from DATABASE_URL with sslmode=require appended if absent, autocommit=True.

Emit a manifest line per member: `SUCCESS: <ext_id> <name> <uuid> -> <cdn_url> [license=<license>]` or `FAILED: <ext_id> <name> <reason>`. Print a final tally `N/63 uploaded, M gaps`. Document (as a header comment) that running the script + Storage upload is the inline-orchestrator step, NOT the executor's. Do NOT run the script.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/_tmp-nv-legislature-headshots.py"; test -f "$F" &amp;&amp; grep -q "crop_to_4_5" "$F" &amp;&amp; grep -q "resolve_politician_id" "$F" &amp;&amp; grep -q "external_id = %s" "$F" &amp;&amp; grep -q "600" "$F" &amp;&amp; grep -q "750" "$F" &amp;&amp; grep -q "LANCZOS" "$F" &amp;&amp; grep -q "x-upsert" "$F" &amp;&amp; grep -q "archive.leg.state.nv.us" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _tmp-nv-legislature-headshots.py exists with crop_to_4_5, resize to (600,750), LANCZOS, JPEG quality=90 optimize=True.
    - resolve_politician_id does a runtime psycopg2 lookup by external_id — no hardcoded UUIDs anywhere.
    - ROSTER hardcodes 63 entries (ext_id + verified {Mangled}.{id} image + chamber) from the RESEARCH roster; source = full-size `.jpg` (NOT .Thumb.jpg).
    - Upload target is politician_photos/{uuid}-headshot.jpg via x-upsert PUT.
    - D-02 fallback present; WIKIMEDIA_UA used ONLY for *.wikimedia.org; no fabrication on failure (SKIP + FAILED manifest line).
    - Manifest emits per-member SUCCESS/FAILED with ext_id, uuid, cdn_url, license; final N/63 tally.
  </acceptance_criteria>
  <done>_tmp-nv-legislature-headshots.py written to disk; passes acceptance criteria. Not yet run.</done>
</task>

<task type="auto">
  <name>Task 2: Run the script inline + write audit-only migration 1054_nv_legislature_headshots.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1054_nv_legislature_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/228_or_legislature_headshots.sql (EXACT ANALOG — audit-only politician_images INSERT shape, type-scoped NOT EXISTS guard, NOT registered)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-PATTERNS.md (section "1054_nv_legislature_headshots.sql" — 5-col external_id-subquery form + column constraints; Migration Counter Reference)
    - .planning/phases/160-nevada-legislature-seed-headshots/160-RESEARCH.md (Pitfall 5 — no photo_origin_url; Pitfall 7 — do not register 1054; A2 photo_license)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): run `python C:/EV-Accounts/backend/scripts/_tmp-nv-legislature-headshots.py` from the EV-Accounts repo root so it reads C:/EV-Accounts/backend/.env. Capture the manifest (per-member ext_id → uuid → cdn_url + license, and any FAILED gaps). Spot-check 2-3 uploaded CDN URLs return HTTP 200.

EXECUTOR STEP: Write `1054_nv_legislature_headshots.sql` as an AUDIT-ONLY migration modeled on 228_or_legislature_headshots.sql. Header comment states: audit-only, applied via execute_sql after the headshot upload, NOT registered in the migration ledger, ledger stays at 1053. One idempotent INSERT per legislator that has an uploaded headshot, column list EXACTLY (id, politician_id, url, type, photo_license): id=gen_random_uuid(), politician_id from subquery (SELECT id FROM essentials.politicians WHERE external_id=<ext_id>), url = the politician_photos/{uuid}-headshot.jpg CDN literal from the manifest, type='default', photo_license=the license from the manifest (default 'us_government_work'). Guard each with WHERE NOT EXISTS on (politician_id, type='default'). Do NOT include photo_origin_url (Pitfall 5 — column does not exist). Do NOT add a ledger-registration line (Pitfall 7). Emit one INSERT per SUCCESS manifest line; OMIT any documented-gap member (and note the gap in a header comment). GREP-GATE HYGIENE: keep the literal token for the removed image-origin column and the ledger table OUT of comments (paraphrase).

INLINE ORCHESTRATOR STEP (documented in the header, not executed by the executor): after the script uploads and the file is written with real UUIDs substituted, apply `1054_nv_legislature_headshots.sql` via `mcp__supabase-local__execute_sql`. Then count politician_images for the 63 ext_ids → expect 63 minus documented gaps.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/migrations/1054_nv_legislature_headshots.sql"; test -f "$F" &amp;&amp; grep -q "essentials.politician_images" "$F" &amp;&amp; grep -q "'default'" "$F" &amp;&amp; grep -q "politician_photos" "$F" &amp;&amp; grep -q "external_id" "$F" &amp;&amp; ! grep -q "photo_origin_url" "$F" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: script run; manifest captured; N/63 uploads recorded (genuine gaps, if any, listed); 2-3 CDN URLs spot-checked HTTP 200.
    - 1054_nv_legislature_headshots.sql exists; one politician_images INSERT per uploaded legislator, column list exactly (id, politician_id, url, type, photo_license) — photo_origin_url absent.
    - type='default'; each INSERT guarded by WHERE NOT EXISTS on (politician_id, type='default'); politician_id resolved via external_id subquery.
    - NO ledger registration line (audit-only; ledger stays 1053).
    - url literals point at politician_photos/{uuid}-headshot.jpg with the real per-member UUIDs from the manifest.
    - INLINE: 1054 applied; politician_images count for the 63 ext_ids = 63 minus documented gaps.
  </acceptance_criteria>
  <done>Script run inline (63/63 minus documented gaps uploaded), 1054_nv_legislature_headshots.sql written and applied so each sourced legislator has a non-null 600x750 headshot URL; gaps (if any) documented; ledger still MAX=1053.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| remote image host → pipeline | untrusted image bytes from archive.leg.state.nv.us / Wikimedia cross the boundary |
| script → Storage / DB | service-role key authorizes Storage PUT + DB lookup |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-04 | Tampering | malicious image payload (EXIF/stego) from remote source | mitigate | PIL `convert('RGB')` + re-encode JPEG optimize=True drops EXIF/embedded data (pipeline already does this) |
| T-160-05 | Information disclosure | service-role key leakage | mitigate | Key read from gitignored .env; script is gitignored `_tmp-*` (never committed); migration file has no key |
| T-160-06 | Spoofing | wrong-person / fabricated photo | mitigate | Hardcoded verified {Mangled}.{id} filenames per member; no fabrication on failure (skip + document gap); operator visual spot-check in Plan 03 |
| T-160-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (Pillow/requests/psycopg2 already in repo scripts); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
After all tasks (inline orchestrator):
1. Script manifest: 63/63 uploads (minus documented gaps), each SUCCESS line has uuid + cdn_url + license.
2. politician_images count for the 63 ext_ids = 63 minus documented gaps, all type='default'.
3. 2-3 CDN URLs return HTTP 200.
4. Ledger MAX still 1053 (1054 NOT registered).
</verification>

<success_criteria>
- All 63 legislators (minus any documented genuine gap) have a 600x750 crop-to-4:5 headshot in politician_photos.
- Each has a politician_images row (type='default') with a non-null CDN url and recorded license.
- No fabricated photos; gaps documented in the SUMMARY.
- Migration 1054 is audit-only and NOT registered (ledger MAX stays 1053).
</success_criteria>

<output>
Create `.planning/phases/160-nevada-legislature-seed-headshots/160-02-SUMMARY.md` when done (record the upload manifest: 63 ext_id→uuid→cdn_url→license lines, any documented gaps, and the politician_images count).
</output>
