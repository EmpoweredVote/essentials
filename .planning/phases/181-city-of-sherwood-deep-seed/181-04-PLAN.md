---
phase: 181-city-of-sherwood-deep-seed
plan: 04
type: execute
wave: 3
depends_on: ["181-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1189_rosener_stances.sql
  - C:/EV-Accounts/backend/migrations/1190_young_stances.sql
  - C:/EV-Accounts/backend/migrations/1191_brouse_stances.sql
  - C:/EV-Accounts/backend/migrations/1192_giles_stances.sql
  - C:/EV-Accounts/backend/migrations/1193_mays_stances.sql
  - C:/EV-Accounts/backend/migrations/1194_scott_stances.sql
  - C:/EV-Accounts/backend/migrations/1195_standke_stances.sql
autonomous: false
requirements: [WASH-07]

must_haves:
  truths:
    - "Each of the 7 officials has evidence-only compass stances where a public record exists"
    - "Every stance is 100% cited (reasoning + sources in politician_context); ZERO default/neutral values"
    - "Topics with no evidence are omitted entirely (honest blank spokes), never defaulted"
    - "topic_id is resolved LIVE via JOIN on compass_topics.topic_key AND is_live=true (no hardcoded topic UUIDs)"
    - "Each stance migration ends with the TRIPLE-GATE DO block (WR-01 identity + answers-count + WR-03 context-parity) from mig 1180"
    - "No judicial-* topics are used (City Attorney + Municipal Court Judge confirmed council-appointed at Wave-0 via Sherwood's Dec-2024 org chart — the A4 assumption resolved)"
    - "Stance research was done one agent at a time (never parallel)"
    - "The Oct 28 2025 unanimous 3-resolution Charter-amendment housing vote is treated as growth-and-development evidence primarily (residential-zoning fit evaluated independently per official, NOT mechanically copied across all 7); it is attributed per-official ONLY after the Open Question 2 seated-roster-on-vote-date check confirms each official was seated on that date"
    - "Keith Mays's long institutional record (former Mayor/Council President, Oregon Mayors Association, League of Oregon Cities) is mined for his CURRENT-seat stance evidence, but his former-Mayor titles are not conflated with his present PLAIN-Councilor seat"
    - "Pamplin Media (Sherwood Gazette) content is recovered via D-16 search-index extraction (pamplinmedia.com TLS-fails all fetchers), cited to the original article URL; valleytimes.news/beavertonvalleytimes.com are preferred where a direct URL exists"
    - "Decision coverage — D-08: all live compass topics per official, one research agent at a time, evidence-only, 100% cited, honest blank spokes, zero default values, 18-21+ depth where the record supports it, judicial-* topics skipped; D-16: Pamplin search-index extraction cited to original URLs"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1189_rosener_stances.sql"
      provides: "Mayor Tim Rosener stances (audit-only)"
      contains: "politician_answers"
    - path: "C:/EV-Accounts/backend/migrations/1193_mays_stances.sql"
      provides: "Councilor Keith Mays stances (audit-only)"
      contains: "politician_answers"
  key_links:
    - from: "each stance migration"
      to: "inform.compass_topics (is_live=true)"
      via: "JOIN on topic_key"
      pattern: "topic_key = s.topic_key AND ct.is_live = true"
    - from: "each stance migration"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\)"
---

<objective>
Research and author 7 audit-only stance migrations — one per Sherwood official — populating
evidence-only compass stances. Research runs ONE AGENT AT A TIME (rate-limit rule), evidence-only,
100% cited, honest blank spokes where no public record exists, ZERO default values, and skips all
judicial-* topics (Sherwood's City Attorney + Municipal Court Judge confirmed council-appointed at
Wave-0 via the Dec-2024 org chart, resolving the A4 assumption). Every file carries the TRIPLE-GATE
DO block (WR-01 identity + answers-count + WR-03 context-parity) from the freshest analog, mig 1180.
The strong shared evidence anchor — the Oct 28 2025 unanimous 3-resolution Charter-amendment vote on
state-housing-law local control — is available for most/all 7 officials, but must be attributed
per-official only after the Open Question 2 seated-roster-on-vote-date check, treated primarily as
growth-and-development (residential-zoning fit judged independently, NOT copy-pasted across all 7).

Purpose: Satisfy the stance portion of WASH-07 — evidence-only stances render on profiles.
Output: 7 audit-only migrations (`1189_rosener_stances.sql` ... `1195_standke_stances.sql`), NOT
registered in the ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md
@.planning/phases/181-city-of-sherwood-deep-seed/181-02-SUMMARY.md

**EXECUTION ARCHITECTURE:** stance research runs via ORCHESTRATOR-spawned general-purpose agents (one
at a time — gsd-executor has NO WebSearch and NO Supabase MCP). The executor WRITES the 7 `.sql` files
from the researched evidence; the INLINE ORCHESTRATOR applies each via `psql -f` and commits them to
the EV-Accounts repo (`git -C "C:/EV-Accounts"` — never cd into it). These are AUDIT-ONLY migrations —
none writes a schema_migrations ledger row; the on-disk file counter is authoritative. Migration
numbers 1189-1195 assume plan 02 used 1187 + plan 03 used 1188; if Wave-0 shifted the base, renumber
consistently.

**STANCE RULES (non-negotiable — from operator conventions + memory):**
- ONE research agent at a time — never parallel (parallel burns rate-limit quota with no output);
  time-box each agent (~20-25 web ops).
- Evidence-only chairs model (integer 1-5); a "chair" is the position statement the evidence matches,
  NOT a polarity scale — pick the chair the evidence supports.
- 100% cited: every answer has reasoning + sources in politician_context.
- NO default values — a topic with no evidence is OMITTED entirely (blank spoke is honest).
- Skip all 8 judicial-* topics (A4 confirmed at Wave-0: City Attorney/Municipal Court Judge appointed).
- Use the LIVE topic_key list captured in 181-01-SUMMARY.md; resolve topic_id via JOIN on
  compass_topics.topic_key AND is_live=true — never hardcode a topic UUID.
- Aim for as many cited stances as the record supports (18-21+ where the record is deep); thin records
  get honest blanks. RESEARCH expects a comparable-or-deeper outcome than Forest Grove's 39/7 given
  the directly-fetchable Agenda Center meeting-packet PDFs.
- HOUSING-CHARTER ANCHOR (Oct 28 2025, unanimous): the three Charter-amendment resolutions
  (2025-073/074/075) on state-housing-law local control passed unanimously and were ratified in the
  Jan 2026 special election. This is a WHO-DECIDES / local-control / annexation-and-land-use-process
  vote — treat it PRIMARILY as growth-and-development; evaluate residential-zoning fit independently
  per official (do NOT mechanically copy one value or reasoning across all 7). Before attributing it
  to any specific official, confirm that official was SEATED on Oct 28 2025 (Open Question 2 — check
  the actual meeting minutes/packet PDF from the Agenda Center; Young/Brouse/Standke seated by Jan 2025,
  the rest since at least 2022, but verify per official rather than assume).
- KEITH MAYS: mine his long institutional record (former Mayor 2018-2022/2005-2012, former Council
  President 2001-2004, Oregon Mayors Association president, League of Oregon Cities) for CURRENT-seat
  stance evidence via meeting minutes/Agenda Center — but do NOT conflate his former-Mayor titles with
  his present PLAIN-Councilor seat.
- D-16 PAMPLIN: pamplinmedia.com (Sherwood Gazette) TLS-fails for ALL fetchers — recover Q&A/coverage
  via search-index extraction, cite the ORIGINAL article URL, stay strictly evidence-only. Prefer the
  directly-WebFetch-able valleytimes.news / beavertonvalleytimes.com where a direct URL exists. Never
  fabricate content the index does not actually show.
- Do NOT seed any 2026-election candidate position as a current stance — this is the currently-seated
  roster only (Phase 185 owns 2026 candidate data).
</context>

<interfaces>
inform.compass_topics    (id, topic_key, is_live)   -- 44 live topics; 8 are judicial-* (SKIP)
inform.politician_answers (politician_id, topic_id, value)                    -- ON CONFLICT (politician_id, topic_id) DO UPDATE SET value
inform.politician_context (politician_id, topic_id, reasoning, sources)       -- sources is text[]; ON CONFLICT DO UPDATE
-- politician UUIDs: resolve by external_id from 181-02-SUMMARY.md:
--   -4167101 Tim Rosener (Mayor) | -4167102 Kim Young (Council President) | -4167103 Renee Brouse
--   -4167104 Taylor Giles | -4167105 Keith Mays | -4167106 Doug Scott | -4167107 Dan Standke
-- File→official mapping: 1189=Rosener, 1190=Young, 1191=Brouse, 1192=Giles, 1193=Mays, 1194=Scott, 1195=Standke
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Research + author stance migrations for Rosener, Mays, Standke, and Giles (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1189_rosener_stances.sql, C:/EV-Accounts/backend/migrations/1193_mays_stances.sql, C:/EV-Accounts/backend/migrations/1195_standke_stances.sql, C:/EV-Accounts/backend/migrations/1192_giles_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1180_wenzl_stances.sql (the 62-line stance template — the freshest: two-statement politician_answers + politician_context structure + the TRIPLE-GATE DO block, WR-01 identity + count + WR-03 context-parity)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md §"1189_rosener_stances.sql through 1195_standke_stances.sql (7 stance migrations, CRUD)" (the two-statement CTE shape + triple-gate block + the housing-charter anchor + polarity nuance + Mays note)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md §"Stance Evidence Landscape" + §"Common Pitfalls" (Pitfall 3 Mays, Pitfall 4 housing-charter polarity)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md (live topic_key list + Open Question 2 seated-roster prerequisite) + 181-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Rosener (-4167101), Mays (-4167105), Standke (-4167107), Giles (-4167104), IN SEQUENCE
    (one research agent at a time — do not spawn parallel research): research the official's documented
    positions across the live compass topics (skip judicial-*), gathering evidence with URLs — consult
    the city Agenda Center meeting-packet PDFs (directly fetchable) and valleytimes.news /
    beavertonvalleytimes.com directly, using D-16 search-index extraction for any pamplinmedia.com
    (Sherwood Gazette) content and citing the original article URL. Then write the migration file using
    the two-statement structure from the 1180 analog: a WITH s(topic_key, val, reasoning, sources)
    VALUES block (one row per topic WITH evidence — omit topics with no evidence), an INSERT into
    inform.politician_answers ... JOIN compass_topics ON topic_key AND is_live=true ON CONFLICT
    (politician_id, topic_id) DO UPDATE SET value, then the IDENTICAL VALUES block feeding INSERT into
    inform.politician_context ... ON CONFLICT DO UPDATE SET reasoning, sources. THEN append the
    TRIPLE-GATE DO block from 1180: (1) WR-01 identity gate — resolve external_id from
    essentials.politicians WHERE id='<uuid>' and RAISE EXCEPTION if it IS DISTINCT FROM the expected
    ext_id; (2) SELECT COUNT(*) from inform.politician_answers for the uuid and RAISE if <> expected
    authored count; (3) WR-03 context-parity — SELECT COUNT(*) from inform.politician_context for the
    uuid and RAISE if <> expected count. Hardcode the politician UUID (from 181-02-SUMMARY.md) and its
    expected ext_id in each file. val is an integer 1-5 chair; never default; blank spoke = omit the
    row. Wrap each file in BEGIN...COMMIT with the audit-only header comment; NO ledger INSERT.
    ROSENER (Mayor, housing-charter leader — his public statements frame it explicitly around local
    control vs. state housing mandates) and MAYS (long institutional record via meeting minutes) have
    the deeper records — mine them accordingly. Attribute the Oct 28 2025 housing-charter vote per
    official only after confirming each was seated on that date (Open Question 2), treat it primarily
    as growth-and-development, and judge residential-zoning fit independently. For MAYS: do NOT let his
    former-Mayor/Council-President titles inflate stances beyond his current-seat evidence. No fenced
    code in this action — 1180 + 181-PATTERNS.md are the source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (the triple-gate DO block RAISEs on an identity/count/parity mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 4 files authored: 1189_rosener_stances.sql, 1193_mays_stances.sql, 1195_standke_stances.sql, 1192_giles_stances.sql
    - Each uses the two-statement politician_answers + politician_context structure
    - Each ends with the TRIPLE-GATE DO block (WR-01 identity + answers-count + WR-03 context-parity), expected counts matching authored rows
    - topic_id resolved via JOIN on topic_key AND is_live=true (no hardcoded topic UUIDs)
    - No judicial-* topic_key appears in any VALUES block
    - Every VALUES row has non-empty reasoning + at least one source URL
    - No default/neutral row for a topic lacking evidence (omitted instead)
    - Housing-charter vote attributed only to officials confirmed seated on Oct 28 2025, primarily as growth-and-development, residential-zoning judged independently (not copy-pasted)
    - Mays file mines current-seat evidence without conflating his former-Mayor titles
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>4 stance migrations authored from one-at-a-time research; triple-gate assertions present; housing-charter polarity + Mays pitfalls handled; audit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Research + author stance migrations for Young, Brouse, and Scott (one agent at a time)</name>
  <files>C:/EV-Accounts/backend/migrations/1190_young_stances.sql, C:/EV-Accounts/backend/migrations/1191_brouse_stances.sql, C:/EV-Accounts/backend/migrations/1194_scott_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1180_wenzl_stances.sql
    - .planning/phases/181-city-of-sherwood-deep-seed/181-PATTERNS.md §"1189_rosener_stances.sql through 1195_standke_stances.sql (7 stance migrations, CRUD)"
    - .planning/phases/181-city-of-sherwood-deep-seed/181-RESEARCH.md §"Stance Evidence Landscape" (Young/Brouse/Scott bios emphasize community-service background — expect Agenda-Center research beyond the housing-charter anchor)
    - .planning/phases/181-city-of-sherwood-deep-seed/181-01-SUMMARY.md (live topic_key list + Open Question 2 seated-roster prerequisite) + 181-02-SUMMARY.md (politician UUIDs by external_id)
  </read_first>
  <action>
    For EACH of Young (-4167102), Brouse (-4167103), Scott (-4167106), IN SEQUENCE (one agent at a
    time): research documented positions across live compass topics (skip judicial-*), then author the
    migration with the same two-statement structure + TRIPLE-GATE DO block as Task 1. RESEARCH notes
    these three (plus the Mayor's Council President Young) have bios emphasizing community-service
    background rather than specific policy positions — expect to lean on the Agenda Center /
    meeting-minutes PDFs and the housing-charter anchor for issue-specific stances; honest blanks are
    acceptable where the record is genuinely thin. Attribute the Oct 28 2025 housing-charter vote per
    official only after confirming seating on that date (Young/Brouse seated by Jan 2025 per term math —
    still verify), primarily as growth-and-development, residential-zoning judged independently. Use
    D-16 search-index extraction for any pamplinmedia.com content, citing original URLs; prefer
    valleytimes.news/beavertonvalleytimes.com direct fetches. Kim Young holds the Council President
    title but do NOT let it inflate stances beyond her evidence. Never default; blank spoke = omit the
    row. Audit-only header; NO ledger INSERT. UUIDs + expected ext_ids hardcoded from 181-02-SUMMARY.md.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies each `psql "$DATABASE_URL" -f <file>` (triple-gate DO block RAISEs on mismatch) then runs the stance audit: 0 NULL/defaulted values, 0 judicial-* topics</automated>
  </verify>
  <acceptance_criteria>
    - 3 files authored: 1190_young_stances.sql, 1191_brouse_stances.sql, 1194_scott_stances.sql
    - Same two-statement structure + TRIPLE-GATE DO block (WR-01 identity + count + WR-03 context-parity); topic_id via is_live JOIN
    - No judicial-* topics; every row cited; no defaults (thin topics → honest blanks)
    - Housing-charter vote attributed only where seating is confirmed, primarily growth-and-development, residential-zoning judged independently
    - Each file is audit-only (no schema_migrations INSERT)
  </acceptance_criteria>
  <done>3 stance migrations authored from one-at-a-time research; triple-gate assertions present; honest blanks documented; audit passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator applies all 7, runs stance audit, commits</name>
  <what-built>7 audit-only stance migrations. Orchestrator applies each and runs the citation/no-default audit.</what-built>
  <how-to-verify>
    1. Apply all 7 in order: `psql "$DATABASE_URL" -f <each stance file>`. Each triple-gate DO block must pass (no identity/count/parity RAISE).
    2. Stance audit (run as SELECTs against the 7 Sherwood politician UUIDs):
       a. Every politician_answers row has a matching politician_context row with non-null reasoning + non-empty sources (100% cited).
       b. ZERO rows where value is a defaulted/neutral placeholder for a topic lacking evidence (spot-check reasoning quality).
       c. ZERO judicial-* topics linked to any Sherwood official.
       d. Each official has >=1 stance where the record supports it; honest blanks (a 0-count) acceptable only where the record is genuinely thin — document rather than force.
       e. Spot-check the housing-charter vote: attributed only to officials confirmed seated on Oct 28 2025, primarily under growth-and-development, with distinct per-official reasoning (not identical text across all 7); Mays not credited with former-Mayor-title stances.
    3. Commit all 7 in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/118{9}_*.sql backend/migrations/119{0,1,2,3,4,5}_*.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - All 7 stance migrations applied; all triple-gate assertions passed
    - Audit a-e all pass (100% cited, no defaults, no judicial, honest blanks documented, housing-charter attributed correctly with distinct reasoning)
    - All 7 committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with per-official stance counts + any honest blanks, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Stance INSERTs applied to production inform schema via psql |
| research agent → public web | Evidence gathered from official/Agenda-Center/news sources (incl. D-16 search-index extraction for Pamplin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-17 | Tampering | Uncited or fabricated stance value (incl. fabricating Pamplin content the index does not show) | mitigate | 100%-cited rule enforced by audit (every answer needs reasoning + sources); no-default rule; D-16 search-index extraction cites the original article URL; one-agent-at-a-time keeps evidence traceable |
| T-181-18 | Tampering | topic_key typo silently drops authored rows | mitigate | answers-count + context-parity (WR-03) assertions in every stance file RAISE on a count mismatch |
| T-181-19 | Repudiation | Wrong-UUID misattribution (a valid-but-wrong politician UUID) | mitigate | WR-01 identity gate resolves external_id from the hardcoded UUID and RAISEs unless it matches the intended official's ext_id |
| T-181-20 | Spoofing | Housing-charter vote attributed to an official NOT seated on Oct 28 2025, or Mays's former-Mayor record credited to his current seat | mitigate | Open Question 2 seated-roster check before per-official attribution; audit gate 2e spot-checks distinct reasoning + Mays handling |
| T-181-21 | Elevation of Privilege | Judicial-* topic seeded for a non-judicial official | mitigate | Skip-judicial rule (A4 confirmed at Wave-0); audit gate 2c asserts zero judicial-* topics |
| T-181-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- 7 stance migrations applied; triple-gate assertions pass; audit confirms 100% cited, zero defaults,
  zero judicial topics, honest blanks documented, housing-charter attributed only where seating is
  confirmed (distinct per-official reasoning); all committed.
</verification>

<success_criteria>
- Evidence-only stances for all 7 officials, 100% cited, no defaults, no judicial topics, triple-gate
  assertions present, researched one agent at a time, housing-charter polarity + Mays pitfalls handled.
</success_criteria>

<output>
Create `.planning/phases/181-city-of-sherwood-deep-seed/181-04-SUMMARY.md` when done, recording
per-official stance counts and any honest blanks.
</output>
