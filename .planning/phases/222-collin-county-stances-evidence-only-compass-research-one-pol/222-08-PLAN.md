---
phase: 222-collin-county-stances-evidence-only-compass-research-one-pol
plan: 08
type: execute
wave: 8
depends_on: ["222-07"]
files_modified:
  - "[EV-Accounts] C:/EV-Accounts/backend/migrations/<live-next-number>_222_collin_mayors_a_stances.sql (NEW — number assigned live)"
  - "[essentials] .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md (APPEND)"
autonomous: false
requirements: [COLLIN-STANCE-01, COLLIN-STANCE-02]
must_haves:
  truths:
    - "The mayor of every one of Anna, Murphy, Fairview, Princeton, Melissa, Farmersville, Parker, and Lucas that the 222-01 live worklist shows as un-stanced was individually attempted, ONE AT A TIME (D-01, D-02, D-03)"
    - "Mayors are researched before their councils because D-02 puts every city's mayor in the first pass regardless of city size — a mayor is disproportionately likely to have a Ballotpedia page, a State-of-the-City address, or news coverage even in a small town"
    - "Every stance written carries a matching inform.politician_context row whose reasoning names a specific dated action, quote, vote, or questionnaire answer and whose sources array holds at least one real URL that was actually fetched (D-04, D-05)"
    - "Every mayor in this plan's scope appears in exactly one of {this plan's applied migration rows, 222-CONFIRMED-BLANK.md} — never neither, never both (COLLIN-STANCE-01)"
    - "A mayor with no explicit on-topic evidence produces zero inform.politician_answers rows and exactly one blank-register entry naming the sources actually checked — a fully blank sweep is a SUCCESS outcome (COLLIN-STANCE-01/empty, COLLIN-STANCE-02/empty)"
    - "This plan writes only (politician_id, topic_id) pairs that had no prior row per the 222-01 do-not-overwrite set (COLLIN-STANCE-01/adjacency, D-07)"
    - "Every value is a whole integer 1 through 5 — never fractional, never a defaulted middle value (COLLIN-STANCE-02/adjacency)"
    - "100% of any stance valued 1 or 5 was re-verified against its cited source before commit, and at least a 20% random sample of the remaining stances was re-verified (222-VALIDATION.md resample contract for Tiers 2 to 4)"
    - "The migration is a single BEGIN/COMMIT transaction using ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables (COLLIN-STANCE-02/idempotency, COLLIN-STANCE-02/concurrency)"
    - "The first four mayors are committed before the second four are researched, so an interruption loses at most half a sweep (COLLIN-STANCE-01/concurrency)"
    - "The migration has been APPLIED to production and the evidence-integrity gate returns zero rows for this plan's politician_ids — authored-but-unapplied SQL does not count as done"
  artifacts:
    - path: ".planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md"
      provides: "Per-city Mayor sections of the blank register for the eight Tier 2/3 cities (D-08)"
      contains: "— Mayor"
  key_links:
    - "Stances attach by politician_id taken verbatim from 222-01-WORKLIST.md — never by name match"
    - "Register headings are written as `## {City} — Mayor` so the later council plan can append `## {City} — Council` without colliding, and 222-18 can reconcile both against the worklist"
  prohibitions:
    - "MUST NOT infer or assign any stance from party affiliation, endorsement, donor history, or pattern-matching to another official in the same city — only an explicit, on-topic, cited statement, recorded vote, or questionnaire answer may set a chair"
    - "MUST NOT assign a chair when the evidence is suggestive-but-not-explicit, and MUST NOT default to a middle value to avoid leaving a blank — a blank spoke is the correct, successful outcome"
    - "MUST NOT record a source URL that was not actually fetched and read, or that does not contain the cited quote, vote, or questionnaire answer"
    - "MUST NOT attribute a stance to an officeholder on the basis of a same-named person in a different city or state; the source must confirm the Texas city before the evidence is used"
    - "MUST NOT name, imply, or rely on a politician's party in reasoning text — party is never used for inference and never displayed"
---

<objective>
D-02 mayors sweep, part A: evidence-only compass stance research for the mayor of **Anna** (`4803300`), **Murphy** (`4850100`), **Fairview** (`4825224`), **Princeton** (`4859576`), **Melissa** (`4847496`), **Farmersville** (`4825488`), **Parker** (`4855152`), and **Lucas** (`4845012`) — one politician at a time, all 11 canonical topics — then author, commit, and apply one migration and record every honest blank.

Purpose: D-02 puts every city's mayor in the first pass, not just the seven named cities. Mayors carry the highest per-person evidence odds in small and mid-size towns (State-of-the-City addresses, named news coverage, Ballotpedia pages), so sweeping them before their councils front-loads the phase's hit rate.
Output: one AUDIT-ONLY migration in the EV-Accounts repo, eight `## {City} — Mayor` register sections, and applied production rows.
</objective>

## Artifacts this phase produces

Phase 222 (all plans) produces data + planning artifacts only. No application code, no schema change, no packages.

- **[EV-Accounts repo]** `C:/EV-Accounts/backend/migrations/<N>_222_<slug>_stances.sql` — one numbered, git-committed, **AUDIT-ONLY** (NOT registered in `schema_migrations`) migration per research plan. Numbers assigned LIVE. This plan owns `<N>_222_collin_mayors_a_stances.sql`.
- **[essentials repo]** `.planning/phases/222-.../222-01-WORKLIST.md` (222-01), `222-CONFIRMED-BLANK.md` (created 222-01, appended by every research plan), `222-NN-SUMMARY.md` per plan.
- **[essentials repo]** `.planning/REQUIREMENTS.md` — COLLIN-STANCE-01/02 + traceability (222-01).
- **[essentials repo]** `src/lib/coverage.js` — `hasContext: true` reconcile (222-18 only).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONTEXT.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md
@.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mayors of Anna, Murphy, Fairview, and Princeton — one at a time, then migration block, register, commit</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the mayor rows for Anna, Murphy, Fairview, and Princeton are this task's exact scope, plus the live next-available migration number.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B is the SOURCE OF TRUTH for the verbatim question and five answer options of all 11 topics, OVERRIDING the researcher agent file for `housing`, `taxes`, and `healthcare` (Pitfall 1). Also read Pitfall 2 (Ballotpedia homonym and scope trap) and section C (these are Medium tier).
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — evidence hierarchy, quote-selection gates, inversion-trap table, de-identification protocol. Ignore its housing/taxes/healthcare scale text.
    - `C:/EV-Accounts/backend/migrations/1307_oro_valley_mayor_stances.sql` — the exact answers-plus-context upsert shape and AUDIT-ONLY header to copy; it is itself a single-mayor stance migration.
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format to mirror.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_collin_mayors_a_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Never dispatch two research passes concurrently. Process in this order: Anna's mayor, Murphy's mayor, Fairview's mayor, Princeton's mayor — taking each politician_id and full_name verbatim from 222-01-WORKLIST.md. Research only the mayor of each city here; that city's council members belong to a later plan.

    For each mayor, run one research pass, preferring the `politician-stance-researcher` subagent and falling back to a single general-purpose research subagent seeded with that file's methodology. Pass the full verbatim question plus five answer options for all 11 topics from RESEARCH.md section B. WebFetch is the primary tool; a light WebSearch budget of a few calls per person is allowed only to discover a URL when direct fetch patterns fail — these towns are small enough that URL-pattern guessing dead-ends quickly.

    Source order for each city (D-05 acceptable types): the mayor's page on the official city site (annatexas.gov, the Murphy city site, the Fairview town site, princetontx.gov) plus that site's searchable agenda and minutes archive for recorded votes and motions; any State-of-the-City address, which is the single strongest small-town mayoral source; Ballotpedia individual candidate pages; Community Impact Newspaper Collin County candidate Q&As; VOTE411 and League of Women Voters of Collin County; local news; the mayor's campaign site. Confirm the source names the correct Texas city before accepting it — a same-named person in another state is not evidence (Pitfall 2).

    Assign a chair 1 through 5 ONLY where a cited source shows an explicit, on-topic position — a stated stance, a recorded council vote or motion, or a candidate-questionnaire answer (D-04). Suggestive-but-not-explicit is a blank. No party inference, no pattern inference. Attempt all 8 canonical Local Lens topics (D-06) plus the 3 legacy-tail topics where explicit evidence exists (D-07). Skip any (politician_id, topic_id) pair in the 222-01 do-not-overwrite set.

    Self-audit before writing SQL: re-fetch and confirm 100% of any stance valued 1 or 5, plus at least a 20% random sample of the rest, checking that the cited source genuinely contains the claim, is on-topic for that specific compass topic, and points in the direction of the assigned chair per the inversion-trap table for that topic_key. Demote failures to blanks; demote generically-evaluative reasoning the same way.

    Re-read the live next-available migration number from 222-01-WORKLIST.md, confirm it is still free by listing `C:/EV-Accounts/backend/migrations`, and create `C:/EV-Accounts/backend/migrations/<N>_222_collin_mayors_a_stances.sql` in the EV-Accounts repo with an AUDIT-ONLY header naming Phase 222 and the mayors sweep, the eight cities in scope, and the date, stating it is deliberately not registered in `schema_migrations`. Open a single BEGIN/COMMIT transaction and write this task's blocks: for each surviving (politician, topic) pair, an insert into `inform.politician_answers (politician_id, topic_id, value)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value`, immediately followed by an insert into `inform.politician_context (politician_id, topic_id, reasoning, sources)` with `ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources`. Dollar-quote all reasoning text. Values are whole numbers 1 through 5. If none of these four mayors sources anything, leave the file uncreated for now.

    Append a `## {City} — Mayor` section to `222-CONFIRMED-BLANK.md` for each of Anna, Murphy, Fairview, and Princeton, with an `Evidence:` line naming the source families actually checked and a bullet for the mayor if no position was sourced (full_name, office title, politician_id). Write the heading even when the mayor was sourced, stating so explicitly. Update the running `Count:` line.

    Commit both repos locally before starting Task 2. In EV-Accounts use `git -C "C:/EV-Accounts" add` then `git -C "C:/EV-Accounts" commit`; never `cd` into that repo and never `git push` it (a push deploys accounts-api to Render and carries every commit already ahead).
  </action>
  <verify>
    <automated>grep -q "^## Anna — Mayor" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; grep -q "^## Princeton — Mayor" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - One research entry exists for each of the Anna, Murphy, Fairview, and Princeton mayor rows the worklist shows as un-stanced — none skipped.
    - Only mayors were researched; no council member of these cities appears in this task's output.
    - 100% of value-1 and value-5 stances plus at least a 20% sample of the rest were re-fetched and confirmed; demotions are recorded with a reason.
    - Every value literal is a whole number 1 through 5, each answers insert is immediately followed by its matching context insert, and every `sources` array has at least one URL.
    - Four `## {City} — Mayor` register sections exist, and each mayor appears in exactly one of {migration rows, register bullet}.
    - Both repos are committed before Task 2 begins; `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>The Anna, Murphy, Fairview, and Princeton mayors are fully attempted, self-audited, written into the migration, registered, and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Mayors of Melissa, Farmersville, Parker, and Lucas — one at a time, then extend the migration, register, commit</name>
  <read_first>
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-01-WORKLIST.md` — the mayor rows for Melissa, Farmersville, Parker, and Lucas are this task's exact scope.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-RESEARCH.md` — section B (verbatim topic text, overriding the agent file for housing, taxes, healthcare), Pitfall 2, section C (Melissa is Medium tier; Farmersville, Parker, and Lucas are Low tier).
    - `C:/EV-Accounts/.claude/agents/politician-stance-researcher.md` — methodology and inversion-trap table.
    - `C:/EV-Accounts/backend/migrations/<N>_222_collin_mayors_a_stances.sql` — the file being extended (created in Task 1, or created here if Task 1 sourced nothing).
    - `.planning/phases/221-collin-county-headshots-source-and-attach-photos-for-officia/221-CONFIRMED-BLANK.md` — the register format; note Farmersville's mayor Craig Overstreet was a headshot blank, which says nothing about stance-evidence availability.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the file being appended to.
  </read_first>
  <files>C:/EV-Accounts/backend/migrations/&lt;live-next-number&gt;_222_collin_mayors_a_stances.sql, .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md</files>
  <action>
    Work strictly ONE POLITICIAN AT A TIME (D-03). Process in this order: Melissa's mayor, Farmersville's mayor, Parker's mayor, Lucas's mayor — taking each politician_id and full_name verbatim from 222-01-WORKLIST.md. Research only mayors here.

    Use the same research method, the same RESEARCH.md section B topic text, and the same strict D-04 evidence bar as Task 1.

    Source order for each city (D-05 acceptable types): the mayor's page on the official city site (the Melissa city site, farmersvilletx.com, the Parker city site, the Lucas city site) plus that site's agenda and minutes archive; any State-of-the-City or State-of-the-Town address; Ballotpedia where a page genuinely exists for the Texas person; Community Impact Newspaper Collin County coverage; VOTE411 and League of Women Voters of Collin County; local news; the mayor's campaign site. Farmersville, Parker, and Lucas sit below Ballotpedia's stated coverage scope, so do not burn fetch cycles on Ballotpedia URL guesses there — go to official minutes and local news first, and treat a Ballotpedia hit as evidence only after the page body confirms the Texas city (Pitfall 2).

    A mayor who yields zero sourced topics is a normal, correct outcome for a Low-tier town. Do not lower the evidence bar to raise a count.

    Self-audit before extending the SQL: re-fetch and confirm 100% of any stance valued 1 or 5, plus at least a 20% random sample of the rest, checking presence, on-topic fit, and direction against the inversion-trap table for that topic_key. Demote failures to blanks.

    Extend `C:/EV-Accounts/backend/migrations/<N>_222_collin_mayors_a_stances.sql` with this task's blocks inside the same single BEGIN/COMMIT transaction, using the identical answers-plus-context upsert pair, dollar-quoted reasoning, and whole 1-through-5 values. If the file does not exist because Task 1 sourced nothing, create it now with the same AUDIT-ONLY header. If no mayor across all eight cities sources anything, do not create the file at all — the register alone carries the outcome.

    Append a `## {City} — Mayor` section to `222-CONFIRMED-BLANK.md` for each of Melissa, Farmersville, Parker, and Lucas, in the 221 format, writing the heading even when the mayor was sourced. Update the running `Count:` line.

    Commit both repos locally. In EV-Accounts use `git -C "C:/EV-Accounts" add` then `git -C "C:/EV-Accounts" commit`; never `cd` into that repo and never `git push` it.
  </action>
  <verify>
    <automated>grep -q "^## Melissa — Mayor" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; grep -q "^## Lucas — Mayor" .planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md &amp;&amp; [ -z "$(git -C "C:/EV-Accounts" status --porcelain backend/migrations)" ]</automated>
  </verify>
  <acceptance_criteria>
    - One research entry exists for each of the Melissa, Farmersville, Parker, and Lucas mayor rows the worklist shows as un-stanced — none skipped.
    - Only mayors were researched; no council member of these cities appears in this task's output.
    - 100% of value-1 and value-5 stances plus at least a 20% sample of the rest were re-fetched and confirmed; demotions are recorded with a reason.
    - Every value literal is a whole number 1 through 5, each answers insert is immediately followed by its matching context insert, and every `sources` array has at least one URL.
    - Eight `## {City} — Mayor` register sections now exist across Tasks 1 and 2, and each mayor appears in exactly one of {migration rows, register bullet}.
    - `git -C "C:/EV-Accounts" status --porcelain backend/migrations` reports nothing pending.
    - No push was executed against the EV-Accounts remote.
  </acceptance_criteria>
  <done>All eight Tier 2/3 mayors are fully attempted, self-audited, written into the migration where sourced, registered where blank, and committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator applies the mayors sweep A migration and runs the evidence-integrity gate</name>
  <read_first>
    - `C:/EV-Accounts/backend/migrations/<N>_222_collin_mayors_a_stances.sql` — the SQL to apply.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-VALIDATION.md` — the evidence-integrity gate query.
    - `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-CONFIRMED-BLANK.md` — the eight Mayor sections.
  </read_first>
  <what-built>
    A committed, AUDIT-ONLY migration covering the mayors of eight Tier 2/3 Collin cities, plus their per-city Mayor register sections. The executor has no Supabase MCP access, so the SQL is authored and committed but NOT yet applied — the repository looks complete while production is unchanged until this checkpoint runs.
  </what-built>
  <action>
    Halt here and hand control to the operator. Do NOT attempt the apply step from this session — gsd-executor has no Supabase MCP binding, so the SQL is committed but production is still unchanged until the operator runs it. Present the how-to-verify steps below verbatim, wait for the resume signal, then record the operator's reported apply confirmation, evidence-integrity gate result, and reconcile result in this plan's SUMMARY before the phase advances. If any gate returns rows, do not mark this plan done.
  </action>
  <how-to-verify>
    1. Apply the migration via `mcp__supabase-local__execute_sql` (this is production — the write is live). Do NOT register it in `schema_migrations`.
    2. Run the evidence-integrity gate from `222-VALIDATION.md` scoped to this plan's eight politician_ids: it must return zero rows.
    3. Run the exactly-one-bucket reconcile for the eight mayors against `222-01-WORKLIST.md` and the Mayor register sections — never both, never neither.
    4. If any of Fairview, Farmersville, Parker, or Lucas gained its first-ever stance, note it — those cities currently have no coverage chip and 222-18 will need to flip it.
  </how-to-verify>
  <acceptance_criteria>
    - The migration has been applied to production via `mcp__supabase-local__execute_sql` and the apply is confirmed, not assumed.
    - The evidence-integrity gate returns zero rows for this plan's politician_ids.
    - The eight-mayor exactly-one-bucket reconcile passes.
    - Any city moving from zero to at least one stance is recorded for the 222-18 coverage-chip reconcile.
    - No row was written to `schema_migrations`.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live and the gate returns zero rows, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped web text → SQL migration | Quotes and URLs harvested from the open web are embedded into a statement executed against production |
| research judgment → public civic profile | An assigned chair becomes a public claim about a real, named person |
| small-town name collisions | Below Ballotpedia's coverage scope, a same-named person in another state is the most likely search result |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-04 | Tampering | migration SQL text | high | mitigate | Dollar-quote all scraped reasoning text; never concatenate scraped text into a bare SQL string |
| T-222-05 | Repudiation | fabricated or mismatched source URL | critical | mitigate | 100% re-fetch of value-1/value-5 stances plus a 20% sample; failing sources demote the stance to a blank |
| T-222-06 | Spoofing | wrong-person attribution (homonym) | high | mitigate | Confirm the Texas city in the page body before accepting a Ballotpedia or news hit; skip Ballotpedia guesses for sub-scope towns |
| T-222-07 | Denial of Service | false-positive completion (authored but unapplied SQL) | high | mitigate | Blocking apply-and-verify checkpoint; the gate runs against production, not the file |
| T-222-SC | Tampering | package installs | low | accept | This plan installs no packages |
</threat_model>

<verification>
- All eight Tier 2/3 mayors individually attempted, sequentially, mayors only.
- Every applied stance has a whole 1-5 value, a specific dated reasoning, and at least one re-fetched source URL at the Tier 2-4 sampling rate.
- Evidence-integrity gate returns zero rows for this plan's politician_ids after apply.
- Eight-mayor exactly-one-bucket reconcile passes.
- EV-Accounts migration committed locally and not pushed.
</verification>

<success_criteria>
Every Tier 2/3 Collin city's mayor has been genuinely attempted across all 11 topics and carries either evidence-cited chairs or an honest register entry — D-02's "every city's mayor first" clause is half satisfied, with the low-evidence half following in wave 9.
</success_criteria>

<output>
Create `.planning/phases/222-collin-county-stances-evidence-only-compass-research-one-pol/222-08-SUMMARY.md` when done — record per-city mayor attempted/sourced, stances applied, blanks, the migration number used, any stance demoted, and any city that moved from zero to at least one stance.
</output>
