---
phase: 216-unincorporated-locality-label
plan: 02
type: execute
wave: 2
depends_on: ["216-01"]
files_modified: []
autonomous: false
requirements: [LOC-01, LOC-02, LOC-03]
must_haves:
  truths:
    - "accounts-api locality change is live on Render (master deployed)"
    - "An unincorporated Pima County, AZ point (address AND coordinate) returns locality.incorporated===false + county_name==='Pima County'"
    - "A Tucson, AZ city point returns locality.incorporated===true + place_name populated"
    - "A point in an un-loaded state returns locality.incorporated===null"
    - "/candidates/search response includes locality; /coordinate-lookup inherits it"
    - "Zero rows written to any table during the smoke test"
  artifacts:
    - "216-02-SUMMARY.md with live curl transcripts + operator sign-off"
  key_links:
    - "Backend must be live + smoke-verified before the frontend plan (216-03) starts — hard backend-before-frontend rule (v24.0 convention)"
    - "county_name must read exactly 'Pima County' (no embedded state) so the frontend banner yields 'Unincorporated Pima County, AZ' without double state"
---

<objective>
Deploy the 216-01 accounts-api locality change to production (Render, via `git -C "C:/EV-Accounts"`
push to `master`) and prove it live before any frontend work begins. This is the hard
backend-before-frontend gate (v24.0 milestone convention) — the frontend (216-03) consumes the live
`locality` field, so it must exist and be smoke-verified first.

Purpose: Confirm the three-state gate (true / false / null) behaves against REAL production geofence
data — the RESEARCH place-loaded-state list is static-source-derived (Assumption A1) and MUST be
confirmed against live DB before the frontend renders labels from it.
Output: locality live in production; verified fixtures; operator sign-off (LOC-01/02/03 end-to-end backend).

**Repo:** C:/EV-Accounts. Push via `git -C "C:/EV-Accounts" push origin master` (Render auto-deploys).
Backend host: accounts-api.empowered.vote. This plan writes NO source files (deploy + verify only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-CONTEXT.md
@.planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm PLACE_LOADED_STATES against live production DB, then commit + push to Render</name>
  <read_first>
    - .planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-01-SUMMARY.md
    - .planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-CONTEXT.md (⭐ AUTHORITATIVE DB GROUND TRUTH)
  </read_first>
  <action>
    First run against production essentials DB:
    `SELECT LEFT(geo_id,2) AS fips, COUNT(*) FROM essentials.geofence_boundaries WHERE mtfcc='G4110' GROUP BY 1 ORDER BY 1;`
    Confirm the loaded-place FIPS set matches the committed PLACE_LOADED_STATES 11-state list from 216-01
    (AZ, CA, IN, ME, MD, MA, NV, OR, TX, UT, VA) and that MO still shows negligible (1) coverage that
    remains correctly EXCLUDED. If live data diverges (a new state loaded, or a listed state dropped),
    STOP and flag — the hardcoded list must be corrected in 216-01 before deploy. If it matches, commit the
    216-01 changes with `git -C "C:/EV-Accounts" commit` and push to master to trigger the Render deploy.
    Wait for the Render deploy to go live (poll the health/version endpoint).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The live `SELECT ... mtfcc='G4110' GROUP BY fips` result set equals the committed 11-state list; MO (fips 29) is present with count 1 and is NOT in PLACE_LOADED_STATES
    - Any divergence between live DB and the hardcoded list is surfaced (STOP) rather than silently deployed
    - `git -C "C:/EV-Accounts" log origin/master..HEAD` is empty after push (all local commits pushed)
    - Render deploy for accounts-api reports the new commit SHA live
  </acceptance_criteria>
  <done>Live G4110 coverage confirms the 11-state gate; accounts-api change is deployed to production.</done>
</task>

<task type="auto">
  <name>Task 2: Live smoke — three fixtures × both entry paths + zero-write assertion</name>
  <read_first>
    - .planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-CONTEXT.md (Test fixtures + Success criteria)
    - .planning/STATE.md (Phase 213 live-smoke transcript style)
  </read_first>
  <action>
    Against production accounts-api.empowered.vote, capture curl transcripts for:
    (a) UNINCORPORATED Pima County, AZ — an address outside any AZ incorporated place via
        POST /essentials/candidates/search, AND the same point via POST /essentials/coordinate-lookup
        {lat,lng} — both must return locality.incorporated===false and county_name==="Pima County".
    (b) A TUCSON, AZ city point (address + coordinate) — must return locality.incorporated===true with
        place_name populated (place-name path unchanged, D-04).
    (c) A point in an UN-LOADED state (a state absent from PLACE_LOADED_STATES) — must return
        locality.incorporated===null (and county_name still populated per D-03).
    Also confirm /candidates/search response body contains the locality key and /coordinate-lookup inherits
    it verbatim. Take a production row-count snapshot of essentials.politicians/offices/districts before and
    after the smoke to assert zero writes (mirror Phase 213 SMOKE_OK). Confirm no raw lat/lng echoed in any
    response body (matchedAddress empty on coordinate path).
  </action>
  <verify>
    <human-check>Review the captured curl transcripts: fixture (a) both paths show incorporated:false + "Pima County"; (b) incorporated:true + place_name; (c) incorporated:null; zero-write delta = 0; no coordinate echo.</human-check>
  </verify>
  <acceptance_criteria>
    - Fixture (a) address path: `locality.incorporated === false` AND `locality.county_name === "Pima County"` (exact string, no embedded ", AZ")
    - Fixture (a) coordinate path: identical locality (incorporated:false + "Pima County")
    - Fixture (b): `locality.incorporated === true` AND `locality.place_name` non-null on both paths
    - Fixture (c): `locality.incorporated === null` AND `county_name` non-null
    - Before/after production row-count delta === 0 across politicians/offices/districts
    - No raw submitted coordinate appears in any coordinate-lookup response body
  </acceptance_criteria>
  <done>All three fixtures behave correctly on both entry paths; zero writes; no coordinate leak.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off on live backend smoke</name>
  <action>Present the Task 2 curl transcripts + zero-write delta for operator review; do not proceed to the frontend plan until approved.</action>
  <what-built>accounts-api locality field deployed to production; three fixtures smoke-verified on both address and coordinate paths with a zero-write assertion.</what-built>
  <how-to-verify>
    1. Review Task 2's curl transcripts for the unincorporated Pima County, AZ fixture (both paths) — confirm `locality.incorporated:false` and `county_name:"Pima County"`.
    2. Confirm the Tucson city fixture shows `incorporated:true` with a place_name.
    3. Confirm the un-loaded-state fixture shows `incorporated:null`.
    4. Confirm the before/after row-count delta is 0 (no writes).
  </how-to-verify>
  <resume-signal>Type "approved" to unblock the frontend plan (216-03), or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB / Render | Live deploy + live psql smoke against production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-04 | Tampering | live production deploy | high | mitigate | Read-only SELECT smoke + before/after row-count delta === 0 assertion; SELECT-only source path (no INSERT/UPDATE in the locality code) |
| T-216-05 | Information Disclosure | live coordinate-lookup smoke | low | mitigate | Assert no raw lat/lng in any response body; matchedAddress empty (Phase-213 contract preserved) |
| T-216-06 | Repudiation | wrong gate list shipped | medium | mitigate | Task 1 re-confirms PLACE_LOADED_STATES against live G4110 coverage before deploy; divergence STOPs the deploy |
</threat_model>

<verification>
- Live curl transcripts for all three fixtures × both paths archived in the SUMMARY
- Zero-write row-count delta recorded
- Operator sign-off captured
</verification>

<success_criteria>
locality is live in production and behaves correctly for unincorporated, incorporated, and un-loaded-state
points on BOTH the address and coordinate entry paths, with zero writes and no coordinate echo. The
backend-before-frontend gate is satisfied — 216-03 may begin.
</success_criteria>

<output>
Create `.planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-02-SUMMARY.md`.
Record: deployed commit SHA, live G4110 FIPS coverage result, all fixture transcripts, zero-write delta, operator approval.
</output>
