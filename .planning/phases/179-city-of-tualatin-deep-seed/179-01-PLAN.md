---
phase: 179-city-of-tualatin-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/_tmp-tualatin-wave0-probe.sql
autonomous: false
requirements: [WASH-05]

must_haves:
  truths:
    - "Orchestrator has CONFIRMED THE geo_id CORRECTION: the ROADMAP/CONTEXT value 4175200 returns 0 rows and the correct City of Tualatin geofence is 4174950 (exactly 1 G4110 row) — this probe runs FIRST, before any other"
    - "Orchestrator has confirmed no City of Tualatin government row already exists (greenfield) by corrected geo_id 4174950 AND by name search"
    - "Orchestrator has confirmed the ext_id block -4174951..-4174957 is free of collisions"
    - "Orchestrator has confirmed the on-disk migration MAX so the next STRUCTURAL migration number is safe (research snapshot: on-disk MAX 1168 → next 1169; ledger MAX 1159 is a TRAP — on-disk counter authoritative)"
    - "The current seated 7-member roster + exact term types are re-confirmed live from tualatinoregon.gov (Mayor Frank Bubenik + Positions 1-6 Reyes/Sacco/Brooks/Hillier/Gonzalez/Pratt; Pratt holds Council President title-on-seat; ALL 7 currently held by ELECTION — zero appointed seats)"
    - "tualatinoregon.gov WAF status confirmed as NO-WAF (direct curl 200) — best sourcing situation in the milestone; D-16 fallback chain NOT needed"
    - "At least 2-3 of the 7 headshot source URLs are confirmed to still download valid non-zero JPEG bytes (site is mid-redesign; /app/uploads/2025/09/ batch)"
    - "The live compass topic_key list (is_live=true) is captured for stance ingestion (research: 44 live, 8 judicial-*)"
    - "Wave-0 checkpoint confirms the PURE AT-LARGE / directly-elected-Mayor / numbered-Position model (NO wards, NO X00xx geofences) and the Youth Advisory Council non-council-seat finding (nothing to exclude) before any write"
    - "Decision coverage — D-01: council structure ground-truthed from tualatinoregon.gov, not memory; D-02: WHO-VOTES tie-breaker applied (whole city votes all seats → at-large branch, no custom geofences); D-05: Mayor's directly-elected citywide role re-confirmed at Wave-0; D-07: seated roster + exact body name verbatim + WAF status captured for the executor; D-14: no non-voting dais seat exists to exclude"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/_tmp-tualatin-wave0-probe.sql"
      provides: "The Wave-0 probe queries the orchestrator runs before any write"
      min_lines: 20
  key_links:
    - from: "probe.sql"
      to: "essentials.governments / essentials.politicians / essentials.geofence_boundaries / supabase_migrations.schema_migrations / inform.compass_topics"
      via: "SELECT probes"
      pattern: "essentials\\.(governments|politicians|geofence_boundaries)"
---

<objective>
Author the Wave-0 verification probe file for the City of Tualatin deep-seed and drive the
orchestrator-run DB + roster checks that MUST pass before any structural write. This plan gates the
entire phase: no government, roster, headshot, or stance migration may be authored or applied until
every probe below returns its expected result.

Purpose: Prevent the highest-risk failure modes for this phase. The single most load-bearing check is
the geo_id correction — RESEARCH found the ROADMAP/CONTEXT/phase-description value 4175200 returns
ZERO rows in essentials.geofence_boundaries; the correct value is 4174950. A structural migration
written against 4175200 would create a phantom government no address ever routes to (worse than the
Hillsboro 4133850→4134100 error, which at least pointed at a real place). Also confirm the pure
at-large / directly-elected-Mayor / numbered-Position model, the ext_id block, the migration counter,
the live roster (no appointment churn found, lower stakes than Tigard), and headshot-URL
retrievability before committing to the seed shape (RESEARCH Pitfalls 1, 2, 3, 5; Assumptions A1-A10;
Open Questions 1-3).
Output: `_tmp-tualatin-wave0-probe.sql` (gitignored `_tmp-*` helper) + a recorded pass/fail for each
Wave-0 gate, recorded in the SUMMARY for downstream plans 02-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-RESEARCH.md
@.planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md

**EXECUTION ARCHITECTURE:** The gsd-executor WRITES the `.sql` probe file only. It has NO
database access, NO WebSearch, and NO Supabase MCP. The INLINE ORCHESTRATOR (main context) runs the
probes via `psql "$DATABASE_URL" -f <file>` using the DATABASE_URL in `C:/EV-Accounts/backend/.env`,
re-fetches the roster from tualatinoregon.gov (NO WAF — direct curl works), and test-downloads 2-3
headshots. This probe file is a gitignored `_tmp-*` helper — do NOT commit it.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug`,
`schema_migrations` (except in Probe E where a query genuinely reads that table), and
`photo_origin_url` out of comments in any file authored this phase. Never write the wrong value
`4175200` anywhere except the one explicit "confirm-it-returns-0" probe that documents the correction.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave-0 probe file</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-tualatin-wave0-probe.sql</files>
  <read_first>
    - .planning/phases/179-city-of-tualatin-deep-seed/179-RESEARCH.md §"geo_id Correction", §"Ext_id Block", §"Next Migration Number", §"Live Compass Topics", §"Wave 0 Gaps"
    - .planning/phases/179-city-of-tualatin-deep-seed/179-PATTERNS.md §"Key Casing / Structural Traps" and §"geo_id Trust — Always Verify"
    - .planning/phases/178-city-of-tigard-deep-seed/178-01-PLAN.md (the direct prior-phase probe-file analog — same probe shape A-G)
  </read_first>
  <action>
    Write labeled SELECT probes, each preceded by a `\echo` label line so the orchestrator reads
    results unambiguously. Do NOT wrap in a transaction (read-only); no DDL — SELECT/\echo only.
    Probe A (GEO_ID CORRECTION — RUN FIRST, MOST CRITICAL): a two-part probe — (A1) count rows in
    essentials.geofence_boundaries WHERE geo_id='4175200' AND mtfcc='G4110' (EXPECT 0 — documents the
    ROADMAP/CONTEXT value is wrong; this is the ONLY place the value 4175200 may appear), and (A2)
    count rows WHERE geo_id='4174950' AND mtfcc='G4110' (EXPECT 1 — the corrected value). Add a header
    comment on Probe A stating "4175200 is the WRONG value from ROADMAP/CONTEXT; 4174950 is correct".
    Probe B: count essentials.districts WHERE geo_id='4174950' (expect 0 — no city districts yet).
    Probe C: count essentials.governments WHERE geo_id='4174950' OR name ILIKE '%tualatin%' (expect 0 —
    greenfield). Probe D: select external_id from essentials.politicians WHERE external_id BETWEEN
    -4174960 AND -4174940 (expect 0 rows — ext_id block -4174951..-4174957 free). Probe E: select
    MAX(version::bigint) from supabase_migrations.schema_migrations WHERE version ~ '^[0-9]{1,6}$'
    (record it; the research snapshot is ledger MAX 1159 which is a TRAP — the next STRUCTURAL
    migration number = on-disk file MAX + 1, research snapshot on-disk MAX 1168 → next 1169; the
    orchestrator cross-checks with an `ls` of the migrations dir). Probe F: select distinct state from
    essentials.districts WHERE geo_id='4159000' (Portland) to confirm lowercase 'or' is the correct
    casing for LOCAL/LOCAL_EXEC rows. Probe G: select topic_key from inform.compass_topics WHERE
    is_live=true ORDER BY topic_key — the authoritative live-topic list for the stance plan (04); also
    count how many begin with 'judicial-' (expect 8, all skipped). Include a header comment noting the
    file is a gitignored orchestrator-run helper. No fenced code in this action — the 178-01 analog is
    the structural source of truth.
  </action>
  <verify>
    <automated>MISSING — orchestrator runs `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-tualatin-wave0-probe.sql` and records each result</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_tmp-tualatin-wave0-probe.sql with labeled probes A-G
    - Probe A has TWO parts: A1 asserts geofence_boundaries geo_id='4175200' returns 0, A2 asserts geo_id='4174950' AND mtfcc='G4110' returns 1
    - The literal '4175200' appears ONLY inside Probe A1 (nowhere else in the file)
    - Probe C queries governments by geo_id='4174950' OR name ILIKE '%tualatin%'
    - Probe D queries the exact ext_id range -4174960..-4174940
    - Probe E reads MAX(version::bigint) from the migration ledger with a numeric-version filter
    - Probe G selects topic_key WHERE is_live=true and counts judicial-* topics
    - File contains only SELECT/\echo statements (no INSERT/UPDATE/CREATE); not wrapped in a transaction
  </acceptance_criteria>
  <done>Probe file authored with all queries and correct literal values (corrected geo_id 4174950, ext_id range -4174960..-4174940).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator runs probes, re-fetches roster, tests headshots, confirms all gates</name>
  <what-built>The Wave-0 probe file. The orchestrator now executes it against the live DB, re-fetches the live roster directly from tualatinoregon.gov (no WAF), and test-downloads 2-3 headshots.</what-built>
  <how-to-verify>
    A. Run: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/scripts/_tmp-tualatin-wave0-probe.sql`
       (DATABASE_URL from C:/EV-Accounts/backend/.env). Confirm each gate:
       1. Probe A1 returns geofence(4175200)=0 AND Probe A2 returns geofence(4174950)=1. If A2 ≠ 1 →
          STOP, city geofence missing/mislabeled — re-run a name search before proceeding. If A1 ≠ 0
          (i.e. 4175200 unexpectedly exists) → STOP and reconcile which row is the real city.
       2. Probe B returns districts=0. If >0 → STOP, districts already exist (not greenfield).
       3. Probe C returns 0 (no existing Tualatin gov). If >0 → STOP, phase already partly seeded —
          switch to reconcile.
       4. Probe D returns 0 rows (ext_id block free). If any row → shift the block and record the new
          range for plans 02/03/04.
       5. Probe E: cross-check against `ls C:/EV-Accounts/backend/migrations | grep -E '^[0-9]+_' | sort -t_ -k1 -n | tail`.
          Next STRUCTURAL migration = on-disk file MAX + 1 (research snapshot: on-disk 1168 → 1169;
          ledger MAX 1159 is a TRAP — do NOT use it). Record the confirmed number for plan 02.
       6. Probe F returns 'or' (lowercase) — the casing plans 02 must use for LOCAL/LOCAL_EXEC districts.
       7. Probe G returns the live topic_key list (research: 44 live, 8 judicial-*) — save it verbatim for plan 04.
    B. Re-fetch the current roster directly from tualatinoregon.gov/city-council/ (NO WAF — plain curl
       or curl with a Chrome UA both return 200; do NOT need any fallback). Confirm the 7-member roster
       is unchanged since 2026-07-02: Mayor Frank Bubenik (directly elected, term through Dec 2026);
       Position 1 María Reyes; Position 2 Christen Sacco; Position 3 Bridget Brooks; Position 4 Cyndy
       Hillier; Position 5 Octavio Gonzalez; Position 6 Valerie Pratt (Council President). Confirm ALL 7
       are currently held by ELECTION (zero appointed seats) — Pratt's original 2019 appointment was
       superseded by 2020+2024 elections, so is_appointed=false is correct for her present seating. Run
       a FRESH WebSearch for any Tualatin council resignation/appointment after 2026-07-02. If any
       member changed → re-plan the roster in plan 02.
    C. Test-download 2-3 of the 7 headshot URLs (RESEARCH §"Headshot Sources"): e.g. the Mayor
       (https://tualatinoregon.gov/app/uploads/2025/09/Home_Mayor.jpg) + two councilors
       (Council_Maria-Reyes.jpg, Council_Valerie-Pratt.jpg) with `-L`; confirm each saves non-zero
       bytes and opens as a valid JPEG. If any 404s, the mid-redesign moved the path — record so plan
       03 re-searches. Record that all 7 are expected from tualatinoregon.gov directly (no fallback
       chain; D-16 unused, documented).
    D. Confirm the structural decision holds (RESEARCH Form-of-Government): PURE AT-LARGE with a
       directly-elected Mayor + 6 numbered Council Positions, every seat voted citywide → Beaverton 176
       shape (1 LOCAL_EXEC Mayor district + 1 LOCAL at-large district on geo_id 4174950), NO ward/X00xx
       geofences, numbered 'Council Member (Position N)' titles (NOT Tigard's plain 'Councilor'). Confirm
       the Youth Advisory Council is a SEPARATE advisory board (not a council-dais seat) — nothing to
       exclude from the 7-member roster (D-14 satisfied cleanly). Confirm City Attorney / Municipal
       Court Judge are appointed → skip all 8 judicial-* topics. If any of these is overturned by the
       re-fetch → STOP and re-plan.
  </how-to-verify>
  <acceptance_criteria>
    - All probe gates recorded with pass/fail
    - geo_id CORRECTION confirmed: 4175200 returns 0, 4174950 (G4110) returns 1
    - Confirmed ext_id block (default -4174951..-4174957 unless probe D forced a shift)
    - Confirmed next structural migration number (default 1169) via ledger + on-disk `ls` cross-check (ledger 1159 recognized as a trap)
    - Live topic_key list captured for plan 04; 8 judicial-* noted for exclusion
    - Live roster re-confirmed (7 members; ALL is_appointed=false; Pratt Council President title-on-seat) via direct tualatinoregon.gov fetch + fresh WebSearch
    - 2-3 headshot URLs confirmed downloadable (Mayor + 2 councilors); no-fallback-needed recorded; D-16 documented as unused
    - Pure at-large / directly-elected-Mayor / numbered-Position decision confirmed; Youth Advisory Council non-seat finding confirmed; judicial-* skip confirmed
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Read-only probe SQL executed by the orchestrator against production Supabase |
| operator → external web | Roster re-fetch + 2-3 headshot test-downloads from tualatinoregon.gov (no WAF) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-01 | Tampering | probe.sql executed on prod DB | accept | Probes are read-only SELECTs; no write path; no user input |
| T-179-02 | Spoofing | wrong geo_id (4175200) from ROADMAP/CONTEXT would seed a phantom government | mitigate | Probe A runs FIRST: A1 asserts 4175200 returns 0, A2 asserts 4174950 (G4110) returns 1 — the exact check that catches the phantom-seed risk before any write |
| T-179-03 | Spoofing | stale roster (member change since research) | mitigate | Direct tualatinoregon.gov re-fetch (no WAF) + fresh WebSearch pass confirms currency before seed |
| T-179-04 | Information Disclosure | topic_key list from inform.compass_topics | accept | Public compass topic keys; no PII |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All Wave-0 gates pass with expected values (see Task 2 acceptance criteria).
- The geo_id correction (4175200→4174950) is confirmed before any structural write is authored.
- Any failed gate blocks plans 02-05 until resolved.
</verification>

<success_criteria>
- geo_id correction confirmed (4175200=0, 4174950=1); no duplicate Tualatin gov; districts absent;
  ext_id block free; next migration number confirmed (1169, ledger 1159 recognized as trap); live
  topic_key list captured; roster re-confirmed via direct fetch; 2-3 headshots test-downloaded; pure
  at-large + directly-elected-Mayor + numbered-Position + Youth-Advisory-non-seat + judicial-skip
  decisions confirmed.
</success_criteria>

<output>
Create `.planning/phases/179-city-of-tualatin-deep-seed/179-01-SUMMARY.md` when done, recording the
CONFIRMED corrected geo_id (4174950), ext_id block, next structural migration number, live topic_key
list, re-confirmed 7-member roster (all elected, Pratt Council President), and headshot-source
retrievability for downstream plans.
</output>
