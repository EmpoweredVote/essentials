---
phase: 168-nv-2026-candidate-population-statewide-us-house
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1114_nv_2026_candidates.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1114.ts
autonomous: true
requirements: [NV-CAND-01]

must_haves:
  truths:
    - "All 6 NV statewide-exec races (Governor, Lt. Governor, Attorney General, Secretary of State, Treasurer, Controller) have race_candidates rows (D-05)"
    - "All 4 US House races (NV-01..NV-04) have race_candidates rows (D-05)"
    - "The Governor of Nevada race shows candidates (Lombardo + Ford) — no longer blank"
    - "All certified Nov-3 nominees are seeded incl. confirmed minor-party (Lynn Chapman IAP, NV-02); uncertified independents held back with a migration comment (D-01, D-04)"
    - "Incumbents and cross-office candidates carry a valid politician_id; new challengers are politician_id=NULL (D-02)"
    - "candidate_status='active' for every seeded row; party is never stored (D-06)"
    - "Open-seat races (AG, Treasurer, NV-02) have zero is_incumbent=true rows"
    - "Re-running the migration inserts 0 additional rows (idempotent via NOT EXISTS race_id, full_name)"
    - "Zach Conine appears in NO race_candidates row (ran for AG, lost primary, not on Nov-3 ballot)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1114_nv_2026_candidates.sql"
      provides: "race_candidates INSERTs for 10 NV 2026 general races (per 1072 analog)"
      contains: "INSERT INTO essentials.race_candidates"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-1114.ts"
      provides: "apply + smoke harness (8 assertions incl. idempotency, open-seat, Conine-absent)"
      contains: "1114_nv_2026_candidates.sql"
  key_links:
    - from: "essentials.race_candidates.race_id"
      to: "essentials.races (Phase 167, election 'NV 2026 Statewide General')"
      via: "live-resolved race_id UUID per office/district"
      pattern: "race_id::uuid"
    - from: "essentials.race_candidates.politician_id"
      to: "essentials.politicians (Phases 159-160)"
      via: "live-resolved UUID for incumbents + cross-office (Ford/Cannizzaro/Buck); -3200006 subquery for Matthews"
      pattern: "politician_id"
---

<objective>
Seed `essentials.race_candidates` for the 10 in-scope NV 2026 general-election races (6 statewide
constitutional executives + 4 US House districts, per D-05) with the verified Nov-3 ballot field
(D-01, D-04), bound to the Phase 167 `race_id`s and linked to existing NV politician records where
they exist (D-02). Follows the migration `1072` analog (single-step `race_candidates` insert — NO
new politician records created here; those come in Plan 02 via find-headshots).

Purpose: NV `/elections` currently shows "No candidates have filed" for the Governor race and the
other 9 in-scope races. This plan makes the real nominee field surface.
Output: Migration `1114_nv_2026_candidates.sql` (applied to the live production DB) + paired
`_apply-migration-1114.ts` smoke harness, both committed to the C:/EV-Accounts repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-CONTEXT.md
@.planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-RESEARCH.md
@.planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-PATTERNS.md

<interfaces>
<!-- race_candidates schema (verified live 2026-06-29). Executor uses these columns directly. -->
essentials.race_candidates columns:
  id, race_id (NOT NULL → essentials.races), politician_id (nullable → essentials.politicians),
  full_name (NOT NULL), first_name, last_name, photo_url, is_incumbent (NOT NULL bool),
  candidate_status (NOT NULL; use 'active'), last_verified_at, source, external_id,
  occupational_designation, website_url, created_at, updated_at
  — NO party column (antipartisan, D-06). politician_id is nullable (challengers stay NULL).

Verified Nov-3 2026 general field (full table with sources in 168-RESEARCH.md "Verified Nov-3
2026 General Election Field"). Linking map in 168-RESEARCH.md "Politician Linking Map".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — live-resolve the 10 race_ids and the incumbent/cross-office politician_ids</name>
  <files>(no files written — captures UUIDs for Task 2)</files>
  <read_first>
    - .planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-PATTERNS.md (§"Wave 0 Pre-Check Queries" — the 4 exact queries to run)
    - .planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-RESEARCH.md (§"race_id Resolution Pattern", §"politician_id Resolution Pattern", §"Politician Linking Map", Pitfalls 1/2/3/6)
  </read_first>
  <action>
    The 10 race UUIDs were generated by gen_random_uuid() in migration 1112 and CANNOT be predicted —
    they MUST be resolved live before the migration VALUES can be authored. Run the 4 Wave-0 pre-check
    queries from 168-PATTERNS.md §"Wave 0 Pre-Check Queries" against the live DB via psql
    ($DATABASE_URL from C:/EV-Accounts/backend/.env; the executor has NO Supabase MCP — use psql or
    a throwaway tsx query script).

    Resolve and record:
    (a) The 10 race_id UUIDs — query races→elections→offices→districts WHERE e.name = 'NV 2026
        Statewide General' AND d.district_type IN ('STATE_EXEC','NATIONAL_LOWER') AND d.state ILIKE 'nv'.
        Map each UUID to its office: Governor, Lt. Governor, Attorney General, Secretary of State,
        Treasurer, Controller (6 STATE_EXEC) + NV-01, NV-02, NV-03, NV-04 (4 NATIONAL_LOWER, by geo_id).
        Expect exactly 10 rows. If ≠ 10, STOP — Phase 167 race set is incomplete; do not author the migration.
    (b) The incumbent politician_id UUIDs — Lombardo (Gov), Anthony (Lt Gov), Aguilar (SoS),
        Titus (NV-01), Lee (NV-03), Horsford (NV-04). Andy Matthews (Controller) is confirmed
        external_id=-3200006 → use the subquery `(SELECT id FROM essentials.politicians WHERE
        external_id = -3200006)` rather than a hardcoded UUID.
    (c) Cross-office links per D-02 — query by name: Aaron Ford (current AG, running for Governor),
        Nicole Cannizzaro (NV State Senator, running for AG), Carrie Buck (NV State Senator, running
        for NV-01), Teresa Benitez-Thompson (former Assembly, running for NV-02; likely NO record).
        Where a record exists, capture its UUID for politician_id; where none, the candidate stays NULL.

    Apply the linking rules from the research: AG and Treasurer are OPEN SEATS (is_incumbent=false
    for ALL their candidates — Ford ran for Gov, Conine term-limited/lost). Zach Conine gets NO row.
    NV-02 is an open seat (Amodei retired). Record the full resolved race_id→office and
    candidate→politician_id mapping in the plan SUMMARY so Task 2 (and verification) can audit it.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT r.id, r.position_name, d.district_type, d.geo_id FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id WHERE e.name='NV 2026 Statewide General' AND d.district_type IN ('STATE_EXEC','NATIONAL_LOWER') AND d.state ILIKE 'nv' ORDER BY d.district_type, d.geo_id;"</automated>
  </verify>
  <acceptance_criteria>
    - The race_id query returns exactly 10 rows (6 STATE_EXEC + 4 NATIONAL_LOWER); each maps to one named office.
    - Incumbent politician_id UUIDs captured for Lombardo, Anthony, Aguilar, Titus, Lee, Horsford; Matthews uses the -3200006 subquery.
    - Cross-office checks run for Ford / Cannizzaro / Buck / Benitez-Thompson; for each, either a UUID is captured or "no record → NULL" is recorded.
    - Resolved mapping documented in the SUMMARY (race_id→office, candidate→politician_id or NULL).
  </acceptance_criteria>
  <done>The 10 race_id UUIDs and all incumbent/cross-office politician_id UUIDs (or confirmed-NULL) are resolved and recorded — the migration VALUES can now be authored without guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 1114 (race_candidates seed) with the smoke harness</name>
  <files>C:/EV-Accounts/backend/migrations/1114_nv_2026_candidates.sql, C:/EV-Accounts/backend/scripts/_apply-migration-1114.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1072_seed_2026_statewide_general_candidates.sql (THE analog — single-step race_candidates insert, NOT EXISTS guard, no schema_migrations, held-back comment block, party-not-stored)
    - C:/EV-Accounts/backend/scripts/_apply-migration-1113.ts (harness boilerplate: Pool + dotenv + readFileSync + try/catch/finally)
    - .planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-PATTERNS.md (§ for 1114.sql header/INSERT/verify patterns and § for the 8 smoke assertions)
    - .planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-RESEARCH.md (§"Verified Nov-3 2026 General Election Field" for the exact names + per-candidate source URLs; §"Smoke Harness")
  </read_first>
  <action>
    Write `1114_nv_2026_candidates.sql` mirroring the 1072 analog and the shapes in 168-PATTERNS.md.
    Substitute the live race_id and politician_id UUIDs resolved in Task 1 into the VALUES — do NOT
    invent UUIDs. Two `INSERT … SELECT … FROM (VALUES …) WHERE NOT EXISTS (race_id, full_name)`
    blocks: Section 1 = 6 statewide-exec races, Section 2 = 4 US House races. Per row set
    candidate_status literal 'active', is_incumbent per the field table, source = the per-candidate
    citation URL from RESEARCH, party stored NOWHERE (no party column). Wrap in BEGIN/COMMIT with the
    verification SELECT before COMMIT. NO `INSERT INTO schema_migrations` (D-06; on-disk counter
    authoritative). Include the held-back comment block for the uncertified Governor independents and
    NV-01 independents (D-04). Mark AG/Treasurer/NV-02 OPEN SEAT in comments (is_incumbent=false for
    all their rows). Escape "Marty O''Donnell". Seed the full certified field (D-01): both major-party
    nominees per race PLUS confirmed Lynn Chapman (IAP) in NV-02. Do NOT seed Zach Conine anywhere.

    Write `_apply-migration-1114.ts` from the 1113 boilerplate (change only the migration filename
    and error-message number), then implement the 8 smoke assertions enumerated in 168-PATTERNS.md
    §"_apply-migration-1114.ts": (1) total NV-2026 race_candidates count, (2) all 6 STATE_EXEC races
    have ≥2 candidates, (3) all 4 NATIONAL_LOWER races have ≥2 candidates, (4) no race has >1
    is_incumbent=true, (5) AG+Treasurer open-seat guard (0 incumbents), (6) idempotency (count →
    re-apply sql → count must be equal), (7) candidate_status='active' for all NV-2026 rows,
    (8) Zach Conine absent (count = 0). Set the expected total to the actual seeded row count once
    the field is finalized.

    Apply from C:/EV-Accounts/backend: `node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1114.ts`.
    Writes hit the LIVE production DB; the migration is idempotent so a re-run is safe.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1114.ts</automated>
  </verify>
  <acceptance_criteria>
    - Harness prints "Migration 1114 applied successfully" and all 8 assertions print their expected values.
    - Assertion 2 + 3: 0 races with <2 candidates (all 10 races populated; Governor not blank).
    - Assertion 4: 0 races with >1 incumbent. Assertion 5: 0 incumbents in AG + Treasurer.
    - Assertion 6: idempotency before==after (re-run inserts 0 rows).
    - Assertion 7: 0 non-active rows. Assertion 8: Conine count = 0.
    - Incumbents (Lombardo/Anthony/Aguilar/Matthews/Titus/Lee/Horsford) + any resolved cross-office (Ford/Cannizzaro/Buck) carry a non-NULL politician_id; challengers without a record are NULL.
    - Migration file has NO `INSERT INTO schema_migrations`; held-back independents noted in a comment.
  </acceptance_criteria>
  <done>Migration 1114 is applied to the live DB, all 8 smoke assertions pass, the 10 races each show their nominee field, and both files are ready to commit to the C:/EV-Accounts repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | developer-authored migration with hardcoded literal INSERTs; no user input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-DATA | Tampering | 1114 race_candidates INSERT | accept | All values are developer-authored literals (no user input); idempotent NOT EXISTS guard prevents duplicate/garbage rows; verified against NV SoS official + AP-called sources (evidence-only, D-04). |
| T-168-SCOPE | Information Disclosure | candidate name/source data | accept | Reference data only (public candidate names + public citation URLs); no PII, no secrets, no auth surface. |
| T-168-SQLI | Tampering | psql migration apply | n/a | SQL injection N/A — no dynamic/user-supplied input; literal-only migration authored by operator. |
| T-168-AUTH | Spoofing/Elevation | (none) | n/a | No new endpoint, auth, or cryptography introduced; pure data migration. |

(Mirrors Phase 167's accept/N-A dispositions — data-only migration phase.)
</threat_model>

<verification>
- `node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1114.ts` from C:/EV-Accounts/backend passes all 8 assertions.
- On-disk migration counter advanced to 1114; `schema_migrations` ledger MAX unchanged (no ledger row written).
- The Wave-0 resolved race_id→office mapping and candidate→politician_id mapping are recorded in the SUMMARY.
</verification>

<success_criteria>
- All 10 NV 2026 in-scope races (6 statewide exec + 4 US House) have ≥2 active race_candidates rows.
- Governor of Nevada race shows Lombardo (incumbent, linked) + Ford (cross-office link if resolved).
- AG/Treasurer/NV-02 open seats carry zero is_incumbent=true rows; Conine absent everywhere.
- Migration idempotent; party never stored; candidate_status='active' throughout; sources cited per row.
- D-01..D-06 each traceable to the seeded data + migration comments.
</success_criteria>

<output>
Create `.planning/phases/168-nv-2026-candidate-population-statewide-us-house/168-01-SUMMARY.md` when done.
Commit `1114_nv_2026_candidates.sql` + `_apply-migration-1114.ts` to the C:/EV-Accounts repo via
`git -C "C:/EV-Accounts"` (scripts/_* are gitignored — verify it is or isn't tracked per repo rules;
the migration SQL is the tracked artifact). The SUMMARY.md commits to the essentials repo.
</output>
