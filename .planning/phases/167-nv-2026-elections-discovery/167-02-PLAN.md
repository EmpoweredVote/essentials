---
phase: 167-nv-2026-elections-discovery
plan: 02
type: execute
wave: 2
depends_on: ["167-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1112_nv_2026_races.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1112.ts
autonomous: true
requirements: [NV-ELEC-01]

must_haves:
  truths:
    - "Exactly 63 essentials.races rows are linked to the 'NV 2026 Statewide General' election"
    - "Those 63 rows break down as 6 STATE_EXEC + 11 STATE_UPPER (Senate) + 42 STATE_LOWER (Assembly) + 4 NATIONAL_LOWER (US House)"
    - "Zero of those 63 race rows have a NULL office_id (every race resolved to a real incumbent office)"
    - "Zero of those 63 race rows have a non-NULL primary_party (antipartisan invariant, D-05)"
    - "The 11 Senate races cover districts 2, 8, 9, 10, 12, 13, 14, 16, 17, 20, 21 (16 not 15)"
    - "Neither of NV's two US Senators has a 2026 race row (Cortez Masto 2028, Rosen 2030)"
    - "Re-running the migration keeps the race count at exactly 63 (idempotent NOT EXISTS guard)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1112_nv_2026_races.sql"
      provides: "Idempotent INSERT of 63 NV 2026 race rows across 4 tiers"
      contains: "NV 2026 Statewide General"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-1112.ts"
      provides: "Apply + tier-count + NULL-office + non-NULL-party smoke harness for migration 1112"
      contains: "1112_nv_2026_races.sql"
  key_links:
    - from: "migrations/1112_nv_2026_races.sql"
      to: "essentials.elections (NV 2026 Statewide General)"
      via: "JOIN essentials.elections el ON el.name = 'NV 2026 Statewide General'"
      pattern: "NV 2026 Statewide General"
    - from: "migrations/1112_nv_2026_races.sql"
      to: "essentials.offices via essentials.districts"
      via: "JOIN districts d ON d.geo_id/district_type AND d.state ILIKE 'nv' JOIN offices o ON o.district_id=d.id"
      pattern: "ILIKE 'nv'"
---

<objective>
Seed all 63 `essentials.races` rows for the NV 2026 general election, each linked to an existing incumbent `office_id` (Phases 159–160) and to the election row from Plan 01: 6 statewide constitutional executives + 11 State Senate districts up in 2026 + 42 Assembly districts + 4 US House districts. NV's two US Senators are correctly absent (not up in 2026).

Purpose: This is the core deliverable of NV-ELEC-01 — the race rows the `/elections` page reads to show a NV user their 2026 ballot.
Output: `migrations/1112_nv_2026_races.sql` + paired `scripts/_apply-migration-1112.ts`, applied to the live DB with exactly 63 race rows, 0 NULL office_id, 0 non-NULL primary_party.

This plan depends on Plan 01: the race INSERTs resolve their `election_id` by name-matching the `NV 2026 Statewide General` row, which Plan 01 must have created first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-nv-2026-elections-discovery/167-CONTEXT.md
@.planning/phases/167-nv-2026-elections-discovery/167-RESEARCH.md
@.planning/phases/167-nv-2026-elections-discovery/167-PATTERNS.md
@.planning/phases/167-nv-2026-elections-discovery/167-01-SUMMARY.md

<interfaces>
<!-- essentials.races column contract (verified live 2026-06-29, D-09). Use directly. -->
essentials.races:
  id            uuid (gen_random_uuid())
  election_id   uuid NOT NULL   -- FK to essentials.elections.id, resolved by name subquery
  office_id     uuid nullable   -- MUST resolve non-NULL for all 63 rows; via districts→offices
  position_name text NOT NULL   -- per Pattern 'position_name strings' below
  primary_party text nullable   -- ALWAYS NULL (antipartisan, D-05)
  seats         int NOT NULL    -- 1 per race
  description   text nullable
  -- NO geo_id column on races. Resolve office via districts.geo_id / district_type.

<!-- District/office resolution (D-09 verified live):
  - districts.state contains BOTH 'nv' AND 'NV' → MUST use `d.state ILIKE 'nv'` (mixed-casing trap)
  - office resolves: JOIN districts d (by geo_id/district_type + ILIKE 'nv') JOIN offices o ON o.district_id = d.id
  - Live counts confirmed: 6 STATE_EXEC, 4 NATIONAL_LOWER, 42 STATE_LOWER, 21 STATE_UPPER NV offices exist -->

<!-- The 11 Senate districts UP in 2026 (VERIFIED from leg.state.nv.us official roster + NVSoS
     primary cross-check): 2, 8, 9, 10, 12, 13, 14, 16, 17, 20, 21.
     CRITICAL: District 16 (Krasner, 2026), NOT District 15 (Taylor, 2028). -->

<!-- position_name strings (D-09 discretion; VERIFY against live offices.title in Task 0):
  STATE_EXEC (6): 'Governor of Nevada', 'Lieutenant Governor of Nevada', 'Attorney General of Nevada',
                  'Secretary of State of Nevada', 'State Treasurer of Nevada', 'State Controller of Nevada'
  STATE_UPPER:    'Nevada State Senate District NN'   (NN 2-digit zero-padded)
  STATE_LOWER:    'Nevada State Assembly District NN' (NN 2-digit zero-padded, 01..42)
  NATIONAL_LOWER: 'U.S. Representative District N'     (N single-digit, 1..4) -->

Migration mechanics (D-08): files in C:/EV-Accounts/backend/migrations/. Apply FROM
C:/EV-Accounts/backend via: node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1112.ts.
NO Supabase MCP — DB verification runs via the apply script (pg Pool) and/or psql.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0 pre-check): Resolve live NV district geo_id formats + confirm all 63 target offices exist</name>
  <files>(no files written — read-only DB inspection that determines the JOIN conditions for Task 1)</files>
  <read_first>
    - .planning/phases/167-nv-2026-elections-discovery/167-RESEARCH.md (section "CRITICAL FACTUAL DELIVERABLE: NV Senate Districts Up in 2026" + Assumption A4 flagging SLDU geo_id format as LOW confidence + Pitfall 1/3)
    - .planning/phases/167-nv-2026-elections-discovery/167-PATTERNS.md (the geo_id verification queries embedded in the 1112 section)
  </read_first>
  <action>
    Before writing ANY race INSERT, run these read-only queries against the live DB (via psql with $DATABASE_URL, or a throwaway tsx snippet using the pg Pool boilerplate) and RECORD the results — they decide the exact JOIN conditions in Task 1 (research flagged the SLDU geo_id format as LOW-confidence, A4):

    1. STATE_UPPER geo_id format + labels: `SELECT geo_id, label FROM essentials.districts WHERE district_type='STATE_UPPER' AND state ILIKE 'nv' ORDER BY geo_id` — confirm the exact geo_id pattern (research assumed '32002' = 5-char FIPS-prefix + 3-digit district, but this MUST be verified) and that all 11 target districts (2, 8, 9, 10, 12, 13, 14, 16, 17, 20, 21) are present.
    2. STATE_LOWER geo_id format + labels: `SELECT geo_id, label FROM essentials.districts WHERE district_type='STATE_LOWER' AND state ILIKE 'nv' ORDER BY geo_id` — confirm all 42 Assembly districts (01..42) resolve.
    3. NATIONAL_LOWER geo_ids: `SELECT geo_id, label FROM essentials.districts WHERE district_type='NATIONAL_LOWER' AND state ILIKE 'nv' ORDER BY geo_id` — confirm the 4 US House district geo_ids (research assumed '3201'..'3204').
    4. STATE_EXEC offices + titles: `SELECT o.title FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.state ILIKE 'nv' AND d.district_type='STATE_EXEC' ORDER BY o.title` — confirm EXACTLY 6 active offices and capture their exact `title` strings so position_name wording matches the DB convention.
    5. Office resolvability sanity: for each tier, confirm `JOIN offices o ON o.district_id = d.id` yields exactly one active office per target district (no duplicate/inactive office collisions — Pitfall 3).

    Decide and record: the precise geo_id values (or a derived expression / VALUES list) to use for STATE_UPPER, STATE_LOWER, NATIONAL_LOWER, and the office-resolution method for STATE_EXEC (by title or by the -3200001..-3200006 external_id range from Phase 159). If any target district/office does NOT resolve, STOP and surface the gap before writing Task 1 — do not write a migration that would produce NULL office_id rows.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && psql "$DATABASE_URL" -At -c "SELECT (SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.state ILIKE 'nv' AND d.district_type='STATE_EXEC'), (SELECT count(*) FROM essentials.districts WHERE district_type='STATE_UPPER' AND state ILIKE 'nv'), (SELECT count(*) FROM essentials.districts WHERE district_type='STATE_LOWER' AND state ILIKE 'nv'), (SELECT count(*) FROM essentials.districts WHERE district_type='NATIONAL_LOWER' AND state ILIKE 'nv')"</automated>
  </verify>
  <acceptance_criteria>
    - STATE_EXEC office count returns exactly 6; STATE_UPPER ≥ 21; STATE_LOWER ≥ 42; NATIONAL_LOWER ≥ 4
    - The exact geo_id format for STATE_UPPER / STATE_LOWER / NATIONAL_LOWER is recorded (actual DB values, not assumed)
    - The 6 STATE_EXEC office titles are captured verbatim and position_name strings are reconciled to them
    - All 11 target Senate districts (2,8,9,10,12,13,14,16,17,20,21) and all 42 Assembly districts confirmed present
    - No target office is missing or duplicated; if any gap exists it is surfaced, not silently worked around
  </acceptance_criteria>
  <done>The exact geo_id formats and office-resolution conditions for all four race tiers are known and verified against the live DB; all 63 target offices confirmed resolvable; position_name strings reconciled to actual office titles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 1112 (63 NV 2026 race rows) + paired apply/smoke script</name>
  <files>C:/EV-Accounts/backend/migrations/1112_nv_2026_races.sql, C:/EV-Accounts/backend/scripts/_apply-migration-1112.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1109_seed_tx_ny_2026_house_elections_races.sql (canonical analog — VALUES+JOIN office resolution, NOT EXISTS guard on (election_id, office_id), DO $$ tier-count post-verify, BEGIN/COMMIT, NULL primary_party, no ledger row)
    - C:/EV-Accounts/backend/migrations/280_md_2026_legislative_races.sql (state-legislative race-row analog for Assembly/Senate position_name shape — but use NOT EXISTS, NOT its ON CONFLICT)
    - C:/EV-Accounts/backend/scripts/_apply-migration-280.ts (apply-script analog — tier-by-tier count smoke tests + NULL office_id detector)
    - .planning/phases/167-nv-2026-elections-discovery/167-PATTERNS.md (section "migrations/1112_nv_2026_races.sql" — the four per-tier adapted INSERT excerpts + the ON CONFLICT anti-pattern callout) [if path 404s, read 167-PATTERNS.md in this phase dir]
    - The Task 0 recorded geo_id formats + office titles from this plan (authoritative over any assumed values)
  </read_first>
  <behavior>
    - After apply: 63 races linked to 'NV 2026 Statewide General' — exactly 6 STATE_EXEC + 11 STATE_UPPER + 42 STATE_LOWER + 4 NATIONAL_LOWER
    - 0 rows with NULL office_id; 0 rows with non-NULL primary_party
    - The 11 Senate races are districts 2,8,9,10,12,13,14,16,17,20,21 (NOT 15)
    - No race row resolves to either US Senate office
    - Second apply (idempotency): counts unchanged, no error
  </behavior>
  <action>
    Write `migrations/1112_nv_2026_races.sql` wrapped in BEGIN;/COMMIT;, using the geo_id formats and office-resolution method recorded in Task 0. Four INSERT blocks into essentials.races (id, election_id, office_id, position_name, primary_party, seats), each selecting gen_random_uuid(), the election id (via `JOIN essentials.elections el ON el.name = 'NV 2026 Statewide General'`), the resolved office id, the tier-appropriate position_name, `NULL` primary_party, and `1` seats:
    - STATE_EXEC (6): resolve each of the 6 offices via districts (district_type='STATE_EXEC', state ILIKE 'nv') JOIN offices; position_name per the captured titles ('Governor of Nevada', 'Lieutenant Governor of Nevada', 'Attorney General of Nevada', 'Secretary of State of Nevada', 'State Treasurer of Nevada', 'State Controller of Nevada' unless Task 0 found different title wording).
    - STATE_UPPER (11): VALUES list of districts (2),(8),(9),(10),(12),(13),(14),(16),(17),(20),(21) — district 16 NOT 15; JOIN districts on district_type='STATE_UPPER' AND state ILIKE 'nv' AND the geo_id condition from Task 0; position_name = 'Nevada State Senate District ' || lpad(dn::text, 2, '0').
    - STATE_LOWER (42): districts 1..42; JOIN districts on district_type='STATE_LOWER' AND state ILIKE 'nv' AND the geo_id condition from Task 0; position_name = 'Nevada State Assembly District ' || lpad(dn::text, 2, '0').
    - NATIONAL_LOWER (4): the 4 US House geo_ids from Task 0; district_type='NATIONAL_LOWER' AND state ILIKE 'nv'; position_name = 'U.S. Representative District ' || cd (single-digit 1..4).
    Every block ends with `WHERE NOT EXISTS (SELECT 1 FROM essentials.races r WHERE r.election_id = el.id AND r.office_id = o.id)`. Use `d.state ILIKE 'nv'` on EVERY districts JOIN (mixed-casing trap, D-09). Do NOT use ON CONFLICT anywhere (races has no unique constraint on (election_id, office_id) — it will error; D-07). primary_party stays NULL everywhere (antipartisan, D-05).

    Add a `DO $$ ... RAISE EXCEPTION` post-verify block (mirror 1109) asserting: n_exec=6, n_upper=11, n_lower=42, n_house=4, n_nulloff=0 (zero NULL office_id for this election), and a non-NULL primary_party count of 0. RAISE NOTICE on success. NO schema_migrations ledger INSERT (D-08).

    Write `scripts/_apply-migration-1112.ts` mirroring `_apply-migration-280.ts`: dotenv, pg Pool, readFileSync of `migrations/1112_nv_2026_races.sql`, run, then smoke tests logging actual vs expected: total NV-2026-General races = 63; STATE_EXEC = 6; STATE_UPPER = 11; STATE_LOWER = 42; NATIONAL_LOWER = 4; NULL office_id = 0; non-NULL primary_party = 0; and an explicit check that no race row's office is a US Senate office (district_type NATIONAL_UPPER or equivalent = 0). OMIT any ledger smoke test. try/catch with process.exit(1), pool.end() in finally.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1112.ts</automated>
  </verify>
  <acceptance_criteria>
    - Apply-script output: "Total NV 2026 races: 63 (expected 63)"
    - Tier lines: STATE_EXEC 6, STATE_UPPER (Senate) 11, STATE_LOWER (Assembly) 42, NATIONAL_LOWER (House) 4 — each actual matches expected
    - "NV races with NULL office_id: 0 (expected 0)"
    - "NV races with non-NULL primary_party: 0 (expected 0)"
    - US Senate race count line shows 0
    - Second run of the apply script keeps total at 63 (idempotent, no error)
    - migrations/1112_nv_2026_races.sql contains `ILIKE 'nv'` and contains NO `ON CONFLICT` and NO `schema_migrations` INSERT
  </acceptance_criteria>
  <done>Exactly 63 NV 2026 race rows exist across the 4 tiers (6+11+42+4) with 0 NULL office_id and 0 non-NULL primary_party; the 11 Senate races are the correct districts (16 not 15); US Senators absent; migration idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | Operator-run SQL migration against live Supabase; values resolved from existing DB rows and hardcoded district numbers; no untrusted/user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-02-NA | Tampering/Injection | migration 1112 INSERTs | accept | All values are hardcoded literals or resolved from trusted existing DB rows via parameterless SQL — no user input, no externally-reachable surface. SQL injection N/A. No new endpoint, no auth surface. Threats N/A for this pure-data INSERT plan. |
| T-167-02-INT | Tampering (data integrity) | office_id resolution | mitigate | Mixed-casing trap could silently produce NULL office_id rows; mitigated by mandatory `ILIKE 'nv'` on every JOIN + the post-verify DO $$ block asserting n_nulloff=0 (migration aborts the transaction on any NULL office_id). |

No externally-reachable surface is introduced by this plan; classic STRIDE categories beyond the data-integrity mitigation above do not apply to a hardcoded operator migration.
</threat_model>

<verification>
- `node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-1112.ts` (from C:/EV-Accounts/backend) exits 0 with all tier counts matching (6/11/42/4 = 63) and 0 NULL office_id / 0 non-NULL primary_party.
- Re-run: counts unchanged (idempotent).
- `psql $DATABASE_URL -c "SELECT d.district_type, count(*) FROM essentials.races r JOIN essentials.elections el ON el.id=r.election_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id WHERE el.name='NV 2026 Statewide General' GROUP BY 1 ORDER BY 1"` shows the 4 tiers with expected counts.
- Confirm Senate races: `psql $DATABASE_URL -c "SELECT position_name FROM essentials.races r JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='NV 2026 Statewide General' AND position_name LIKE 'Nevada State Senate%' ORDER BY position_name"` lists districts 02,08,09,10,12,13,14,16,17,20,21 (no 15).
</verification>

<success_criteria>
Exactly 63 `essentials.races` rows linked to `NV 2026 Statewide General` (6 STATE_EXEC + 11 STATE_UPPER + 42 STATE_LOWER + 4 NATIONAL_LOWER), all with non-NULL office_id and NULL primary_party; Senate districts 2,8,9,10,12,13,14,16,17,20,21; no US Senate race; migration idempotent and ledger-free.
</success_criteria>

<output>
Create `.planning/phases/167-nv-2026-elections-discovery/167-02-SUMMARY.md` when done. Record: migration 1112 applied, the verified geo_id formats per tier, the 6 captured STATE_EXEC office titles, tier counts, NULL/party assertions, and that next migration counter is 1113.
</output>
