---
phase: 185-washco-2026-elections-discovery
plan: 03
type: execute
wave: 2
depends_on: ["185-01", "185-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/{BASE+2}_washco_2026_discovery.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-{BASE+2}.ts
autonomous: true
requirements: [WM-ELEC-01]
nyquist_compliant: true

must_haves:
  truths:
    - "8 discovery_jurisdictions rows exist for Washington County + the 7 west-metro cities, all election_date 2026-11-03, state 'OR'; source_url uses the sos.oregon.gov Candidate-Filings-Local-Measures fallback with the county page + ballotpedia.org in allowed_domains (D-06 resilience pattern)"
    - "Discovery eligibility is date-based (180-day horizon before election_date) — there is NO cron_active column; idempotency is ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING (D-09)"
    - "The 5 school-board jurisdictions are NOT armed (D-05)"
    - "One real discovery run completes on the live pipeline (status='completed', error_message IS NULL)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/{BASE+2}_washco_2026_discovery.sql"
      provides: "8 discovery_jurisdictions rows + DO $$ count assertion, no ledger, no BEGIN/COMMIT wrap"
      contains: "INSERT INTO essentials.discovery_jurisdictions"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-{BASE+2}.ts"
      provides: "Idempotent smoke-test harness"
      contains: "discovery_jurisdictions"
  key_links:
    - from: "essentials.discovery_jurisdictions"
      to: "POST /api/admin/discover/jurisdiction/:id → essentials.discovery_runs"
      via: "admin-token-gated live trigger + poll"
      pattern: "discover/jurisdiction"
---

<objective>
Arm the candidate-discovery pipeline for the west-metro 2026 election by seeding 8
`essentials.discovery_jurisdictions` rows (Washington County + 7 cities; D-05), then trigger ONE real
discovery run against the live pipeline and confirm it completes without error (D-07 / ROADMAP criterion
#3). School boards are NOT armed (D-05).

Purpose: WM-ELEC-01 — the date-based cron (180-day horizon before `election_date`) will thereafter keep
finding candidates from official OR/WashCo sources; the test run proves the pipeline is wired end-to-end
for these jurisdictions.

Output: migration `BASE+2` (BASE from Plan 01's SUMMARY) + smoke script, applied to the LIVE DB,
idempotent via `ON CONFLICT`, plus one `essentials.discovery_runs` row with `status='completed'` and
`error_message IS NULL`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-washco-2026-elections-discovery/185-CONTEXT.md
@.planning/phases/185-washco-2026-elections-discovery/185-RESEARCH.md
@.planning/phases/185-washco-2026-elections-discovery/185-PATTERNS.md
@.planning/phases/185-washco-2026-elections-discovery/185-01-SUMMARY.md

<discovery_config>
The 8 rows (RESEARCH Discovery Config table). All: `state='OR'` (UPPERCASE — discovery_jurisdictions.state
matches the NV/VA/MD convention, NOT the districts.state lowercase 'or' trap), `election_date='2026-11-03'`.
| geo_id | jurisdiction_name | source_url | allowed_domains |
|---|---|---|---|
| 41067 | Washington County, Oregon | sos.oregon.gov Candidate-Filings-Local-Measures | sos.oregon.gov, washingtoncountyor.gov, ballotpedia.org |
| 4105350 | Beaverton, Oregon | beavertonoregon.gov/944/Elections | beavertonoregon.gov, washingtoncountyor.gov, sos.oregon.gov, ballotpedia.org |
| 4134100 | Hillsboro, Oregon | hillsboro-oregon.gov (Task 1 confirms a stable elections URL; else root domain) | hillsboro-oregon.gov, washingtoncountyor.gov, sos.oregon.gov, ballotpedia.org |
| 4173650 | Tigard, Oregon | tigard-or.gov/your-government/council/election | tigard-or.gov, washingtoncountyor.gov, sos.oregon.gov, ballotpedia.org |
| 4174950 | Tualatin, Oregon | tualatinoregon.gov/city-council/elections/ | tualatinoregon.gov, washingtoncountyor.gov, sos.oregon.gov, ballotpedia.org |
| 4126200 | Forest Grove, Oregon | forestgrove-or.gov/362/Elections | forestgrove-or.gov, washingtoncountyor.gov, sos.oregon.gov, ballotpedia.org |
| 4167100 | Sherwood, Oregon | sherwoodoregon.gov/elections | sherwoodoregon.gov, washingtoncountyor.gov, sos.oregon.gov, ballotpedia.org |
| 4115550 | Cornelius, Oregon | corneliusor.gov Elections page (verify redirect target) | corneliusor.gov, washingtoncountyor.gov, sos.oregon.gov, ballotpedia.org |
The county's own candidate-filing sub-page 403s → the SOS fallback in source_url + county page in allowed_domains is the D-06 resilience pattern (the discovery agent navigates hub pages the way a raw scraper cannot).
</discovery_config>

<interfaces>
- `essentials.discovery_jurisdictions`: `jurisdiction_geoid text, jurisdiction_name text, state char(2), election_date date, source_url text, allowed_domains text[]`. **Unique index on (jurisdiction_geoid, election_date)** → idempotency is `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING` (this is the ONE place `ON CONFLICT` is correct in this phase). No `cron_active` column (D-09 — eligibility is date-based).
- Discovery trigger (D-07): `POST https://accounts-api.empowered.vote/api/admin/discover/jurisdiction/:id` where `:id` = a `discovery_jurisdictions.id` UUID. Header `X-Admin-Token: $ADMIN_INGEST_TOKEN` (read from C:/EV-Accounts/backend/.env — NEVER echo/log/commit). Returns `202 {status:'accepted'}`; run continues async. `409 ALREADY_RUNNING` if a run is in progress.
- Host trap: use `accounts-api.empowered.vote` — `accounts-api.onrender.com` 404s (carried from Phase 167).
- Completion evidence: poll `essentials.discovery_runs` for `status='completed'` and `error_message IS NULL`. `candidates_found` may be 0 (accepted).
- Discovery migrations do NOT use `BEGIN;/COMMIT;` wrapping and do NOT write a ledger row (1113 family — PATTERNS Shared Patterns; do NOT copy 325/281's ledger tail).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and apply migration BASE+2 — 8 west-metro discovery_jurisdictions rows</name>
  <files>C:/EV-Accounts/backend/migrations/{BASE+2}_washco_2026_discovery.sql, C:/EV-Accounts/backend/scripts/_apply-migration-{BASE+2}.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1113_nv_2026_discovery.sql (analog — column set, ON CONFLICT, no-ledger tail, DO $$ count assertion, no BEGIN/COMMIT)
    - C:/EV-Accounts/backend/scripts/_apply-migration-1113.ts (harness shape)
    - 185-PATTERNS.md discovery section + Shared Patterns (casing, no-ledger warning re 325/281)
    - 185-01-SUMMARY.md (BASE number)
  </read_first>
  <action>
    FIRST — re-verify the live migration counter has not drifted (RESEARCH documents the shared counter
    advancing multiple times per day from parallel workstreams; this plan may run in the same Wave-2 window
    as Plan 02): run `ls C:/EV-Accounts/backend/migrations | grep -oE '^[0-9]+' | sort -n | tail -1`. The
    expected live max is `BASE+1` (Plan 01 wrote `BASE`, Plan 02 wrote `BASE+1`) → this migration is
    `BASE+2`. If Plan 02 has not yet landed its file, the max may be `BASE` — either way, take this
    migration's number as `live_max + 1` and confirm it does not collide with an existing file before
    writing. If any drift beyond the expected `BASE`/`BASE+1` is detected (another workstream landed a
    number in between), re-derive as `live_max + 1` and note the drift in the SUMMARY before writing any
    file. Then:
    Confirm a stable Hillsboro elections URL by briefly browsing hillsboro-oregon.gov navigation (RESEARCH
    Open Question 2); default to the root domain in source_url if none is found (allowed_domains still
    lists hillsboro-oregon.gov). Re-verify the Cornelius elections URL redirect target. Create the
    discovery migration file at the freshly-derived number — a single `INSERT INTO
    essentials.discovery_jurisdictions (jurisdiction_geoid, jurisdiction_name, state, election_date,
    source_url, allowed_domains) VALUES ...` with the 8 tuples from `<discovery_config>` (state='OR'
    UPPERCASE; election_date='2026-11-03'; allowed_domains as text[] arrays), terminated `ON CONFLICT
    (jurisdiction_geoid, election_date) DO NOTHING`. Do NOT arm the 5 school-board jurisdictions (D-05). Do
    NOT wrap in `BEGIN;/COMMIT;` and do NOT write a `schema_migrations` ledger row (match 1113 exactly; do
    NOT copy 325/281's ledger block). Add a `DO $$ ... RAISE EXCEPTION` block asserting exactly 8 rows for
    state='OR' with the 8 west-metro geo_ids and election_date='2026-11-03'. Create the paired
    `_apply-migration-{derived}.ts` (1113 harness shape). Do NOT git-commit inside C:/EV-Accounts.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && node node_modules/tsx/dist/cli.mjs scripts/_apply-migration-{BASE+2}.ts</automated>
  </verify>
  <acceptance_criteria>
    - Before writing any file, the live migration counter was re-checked (`ls ... | grep -oE '^[0-9]+' | sort -n | tail -1`); this migration's number = live_max+1 and does not collide with any existing file; any drift beyond expected `BASE`/`BASE+1` was noted in the SUMMARY.
    - `SELECT count(*) FROM essentials.discovery_jurisdictions WHERE state='OR' AND election_date='2026-11-03' AND jurisdiction_geoid IN ('41067','4105350','4134100','4173650','4174950','4126200','4167100','4115550')` = **8**.
    - 0 discovery_jurisdictions rows exist for any of the 5 west-metro G5420 school-board geo_ids.
    - Smoke script exits 0 on first apply AND on immediate re-run (ON CONFLICT no-op — 0 new rows). No ledger row written.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Trigger one live discovery run and confirm it completes without error</name>
  <files>(no files written — live HTTP trigger + DB poll; D-07 operational step)</files>
  <read_first>
    - 185-RESEARCH.md Discovery Runner section (endpoint, auth, host trap, discovery_runs columns, acceptance bar)
    - 185-PATTERNS.md discovery test-run trigger section
    - .planning/phases/167-nv-2026-elections-discovery/167-03-SUMMARY.md (Phase 167 precedent: 32 candidates found, 0 errors; admin-token handling)
  </read_first>
  <action>
    Select the `discovery_jurisdictions.id` UUID for one armed row (Washington County `41067` is the
    natural choice — most authoritative source). Read `ADMIN_INGEST_TOKEN` from
    `C:/EV-Accounts/backend/.env` at trigger time ONLY — never echo it to stdout, never write it to a
    file, never include it in the SUMMARY or any commit. POST to
    `https://accounts-api.empowered.vote/api/admin/discover/jurisdiction/{id}` with header
    `X-Admin-Token: <token>`. Expect `202 {status:'accepted'}` (a `409 ALREADY_RUNNING` means a run is in
    progress — wait and retry). Then poll `essentials.discovery_runs` (filter on the jurisdiction geoid /
    election_date, newest row) until `status` transitions to `completed` or `failed`. Acceptance bar =
    `status='completed'` with `error_message IS NULL`; `candidates_found` may be 0 (accepted per D-07). If
    `status='failed'`, capture `error_message` and surface it — do not silently pass.
  </action>
  <verify>
    <automated>echo "Poll essentials.discovery_runs for the triggered run: expect status='completed' AND error_message IS NULL (query via mcp__supabase-local)"</automated>
  </verify>
  <acceptance_criteria>
    - The POST returned `202 accepted` (host `accounts-api.empowered.vote`, NOT the .onrender.com dead host).
    - A new `essentials.discovery_runs` row reaches `status='completed'` with `error_message IS NULL`.
    - `ADMIN_INGEST_TOKEN` appears in NO committed file, log line, or SUMMARY.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env (.env) → admin discovery endpoint | `ADMIN_INGEST_TOKEN` pre-shared secret crosses into an HTTP header on a live production call |
| migration → LIVE production DB | Every write is live |
| discovery agent → external OR/WashCo/Ballotpedia sources | Agent-driven fetch of public election data (existing pipeline; citation_url NOT NULL enforced by candidate_staging schema) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-01 | Information Disclosure | `ADMIN_INGEST_TOKEN` leaking into a committed file, log, or SUMMARY | mitigate | Token read from `$ADMIN_INGEST_TOKEN` / .env at trigger time only; never echoed, logged, hardcoded, or committed (Phase 167 T-167-03 pattern); acceptance criterion explicitly asserts absence |
| T-185-01-DB | Tampering | Partial/duplicate discovery_jurisdictions writes on live DB | mitigate | `ON CONFLICT (jurisdiction_geoid, election_date) DO NOTHING` idempotency + `DO $$ RAISE EXCEPTION` 8-row count assertion |
| T-185-04-COLL | Tampering | Migration-number collision with a parallel workstream (or Plan 02 in the same Wave-2 window) on the shared LIVE DB | mitigate | Task 1 re-verifies the live counter (`ls ... | sort -n | tail -1`) immediately before writing; migration number = live_max+1, collision-checked; drift noted in SUMMARY |
| T-185-05 | Spoofing/Tampering | Discovery agent ingesting a fabricated candidate | accept | Existing pipeline enforces `candidate_staging.citation_url NOT NULL` + human staging approval (not modified this phase); run only writes to staging, not directly to race_candidates |
| T-185-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure SQL + existing route + tsx harness; N/A |
</threat_model>

<verification>
- Smoke script `_apply-migration-{BASE+2}.ts` exits 0 twice.
- Live DB: exactly 8 OR discovery_jurisdictions rows (election_date 2026-11-03), 0 for school boards.
- One `essentials.discovery_runs` row with `status='completed'`, `error_message IS NULL`.
- Admin token never persisted anywhere.
</verification>

<success_criteria>
8 west-metro discovery_jurisdictions rows are armed (date-based cron horizon), school boards excluded,
and one real discovery run completes on the live pipeline without error — the pipeline is proven wired.
</success_criteria>

<output>
Create `.planning/phases/185-washco-2026-elections-discovery/185-03-SUMMARY.md` when done — record the
triggered jurisdiction, the discovery_runs outcome (status + candidates_found), and confirm the token was
never persisted. Do NOT include the token value.
</output>
</content>
