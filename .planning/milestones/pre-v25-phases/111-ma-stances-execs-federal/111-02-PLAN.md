---
phase: 111-ma-stances-execs-federal
plan: 02
type: execute
wave: 2
depends_on:
  - 111-01
files_modified:
  - C:/EV-Accounts/backend/migrations/360_driscoll_stances.sql
  - C:/EV-Accounts/backend/migrations/361_campbell_stances.sql
  - C:/EV-Accounts/backend/migrations/362_goldberg_stances.sql
  - C:/EV-Accounts/backend/migrations/363_dizoglio_stances.sql
  - C:/EV-Accounts/backend/migrations/364_galvin_stances.sql
autonomous: true
requirements:
  - MA-STANCES-01
must_haves:
  truths:
    - "Each of the 5 remaining MA executives has a stance migration applied (migrations 360-364)"
    - "Every applied migration has 100% citation rate (uncited = 0)"
    - "Every applied migration has paired answers/context rows (unpaired = 0)"
    - "D-08: all 5 processed sequentially — never in parallel"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/360_driscoll_stances.sql"
      provides: "Stance migration for LG Kim Driscoll"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "36{0..4}_*_stances.sql"
      to: "essentials.politicians (external_id IN (-200003, -200004, -200005, -200006, -200007))"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research the remaining 5 MA state executives' public records across all currently-active compass topics, writing and applying per-individual stance migrations 360–364 sequentially. Officials: LG Kim Driscoll (360), AG Andrea Joy Campbell (361), Treasurer Deborah B. Goldberg (362), Auditor Diana DiZoglio (363), Secretary of State William Francis Galvin (364).

Purpose: Partially satisfies MA-STANCES-01. These officials have substantial public records — target ≥8 stances each per D-09; no artificial time cap per D-03.

Output: 5 migration files applied (360–364); ≥8 stance rows each; 100% citation rate; all paired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md
@.planning/phases/111-ma-stances-execs-federal/111-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve all 5 executive UUIDs</name>
  <read_first>
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-03, D-08, D-10)
    - C:/EV-Accounts/backend/migrations/154_ma_state_executives.sql (lines 159-320 — confirms roster + external_ids)
  </read_first>
  <action>
    Run via mcp__supabase-local__execute_sql:
      SELECT external_id, full_name, id
      FROM essentials.politicians
      WHERE external_id IN (-200003, -200004, -200005, -200006, -200007)
      ORDER BY external_id;

    Expected 5 rows mapping to:
      -200003  Kim Driscoll (LG)              -> migration 360
      -200004  Andrea Joy Campbell (AG)        -> migration 361
      -200005  Deborah B. Goldberg (Treasurer) -> migration 362
      -200006  Diana DiZoglio (Auditor)        -> migration 363
      -200007  William Francis Galvin (SoS)    -> migration 364

    If any row is missing, abort — migration 154 must be applied first.
    Save the (external_id, name, uuid) mapping as `.planning/phases/111-ma-stances-execs-federal/_tmp-execs-roster.json`.
  </action>
  <verify>
    <automated>
      Via mcp__supabase-local__execute_sql:
        SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-200003, -200004, -200005, -200006, -200007);
      Must return 5.
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/111-ma-stances-execs-federal/_tmp-execs-roster.json"
    </automated>
  </verify>
  <done>
    - 5 executive UUIDs resolved and saved to _tmp roster JSON
    - external_id -> name -> migration_number mapping confirmed against migration 154
  </done>
</task>

<task type="auto">
  <name>Task 2: For each of 5 executives SEQUENTIALLY (per D-08), research all topics and apply migration if evidence found</name>
  <read_first>
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01, D-03, D-08, D-09, D-10)
    - .planning/phases/111-ma-stances-execs-federal/111-PATTERNS.md ("Per-Stance INSERT Pattern", "Topic UUID Reference Block")
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251 — structure template)
    - .planning/phases/111-ma-stances-execs-federal/_tmp-execs-roster.json
  </read_first>
  <action>
    Process the 5 executives IN ORDER: Driscoll → Campbell → Goldberg → DiZoglio → Galvin. ONE at a time per D-08 — NEVER launch parallel research agents.

    For each executive, follow this sub-protocol:

    SUB-STEP A — Research (no artificial time cap per D-03): Perform web research on this official's public record across ALL active compass topics. Sources to check:
      - mass.gov official pages
      - ballotpedia.org
      - ontheissues.org (if available)
      - Boston Globe, WBUR, MassLive news archives
      - Campaign sites / prior race records
      - Driscoll: former Salem MA mayor — municipal record available; LG since 2023
      - Campbell: former Cambridge city councilor + Middlesex DA + AG since 2023 — prosecution priorities, judicial-* topics
      - Goldberg: MA Treasurer since 2015 — taxes, economic-development, social-security/pension focus
      - DiZoglio: former MA House + Senate member before Auditor (2023-present) — legislative voting record available
      - Galvin: longest-serving MA Secretary (since 1995) — voting-rights, redistricting, campaign-finance, elections record
      Omit topics with no evidence — do NOT insert neutral/center fallbacks.

    SUB-STEP B — Write migration file: If evidence found (≥1 topic), write `C:/EV-Accounts/backend/migrations/{N}_{lastname-lowercase}_stances.sql` per the mapping in Task 1:
      - Driscoll: 360_driscoll_stances.sql
      - Campbell: 361_campbell_stances.sql
      - Goldberg: 362_goldberg_stances.sql
      - DiZoglio: 363_dizoglio_stances.sql
      - Galvin: 364_galvin_stances.sql
      Migration file MUST follow the 282 pattern exactly per 111-PATTERNS.md:
        - Header: "Migration {N}: {Full Name} Stances" / "{Title} of Massachusetts"
        - Topic UUID reference block
        - BEGIN; section header; INSERT pairs; COMMIT;
        - Verification comment block at end with politician's UUID
        - 100% citation per D-10 — every INSERT INTO politician_context has ≥1 URL in ARRAY[]
        - No defaults — only topics with evidence

    SUB-STEP C — Immediately apply migration per D-06: read full SQL file and pass to mcp__supabase-local__execute_sql. Then verify via:
      SELECT
        (SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '{uuid}') AS answer_count,
        (SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = '{uuid}' AND pc.politician_id IS NULL) AS unpaired,
        (SELECT COUNT(*) FROM inform.politician_context WHERE politician_id = '{uuid}' AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)) AS uncited;
      Required: answer_count >= 8, unpaired = 0, uncited = 0.

    SUB-STEP D — Move to next executive. Do NOT batch per D-06.

    After all 5 processed, save outcome log to `.planning/phases/111-ma-stances-execs-federal/_tmp-execs-outcomes.json`:
      [{"external_id": -200003, "name": "Kim Driscoll", "outcome": "applied", "migration": 360, "stance_count": N}, ...]
  </action>
  <verify>
    <automated>
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/111-ma-stances-execs-federal/_tmp-execs-outcomes.json"
      Via mcp__supabase-local__execute_sql:
        SELECT COUNT(*) FROM (
          SELECT p.external_id, COUNT(pa.*) FILTER (WHERE pc.politician_id IS NULL) AS unpaired,
                 COUNT(pc.*) FILTER (WHERE sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0) AS uncited
          FROM essentials.politicians p
          LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
          LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
          WHERE p.external_id IN (-200003, -200004, -200005, -200006, -200007)
          GROUP BY p.external_id
          HAVING COUNT(pa.*) > 0
        ) t WHERE unpaired > 0 OR uncited > 0;
      Must return 0 — zero defective applied migrations.
    </automated>
  </verify>
  <done>
    - All 5 executives processed sequentially
    - Each migration applied with paired+cited stances
    - Outcome log saved to _tmp file for Task 3 cleanup
    - Zero defective migrations (no unpaired, no uncited)
  </done>
</task>

<task type="auto">
  <name>Task 3: Cleanup + summary preparation</name>
  <read_first>
    - .planning/phases/111-ma-stances-execs-federal/_tmp-execs-outcomes.json
  </read_first>
  <action>
    Step A: Run final per-exec query via mcp__supabase-local__execute_sql:
      SELECT p.external_id, p.full_name, COUNT(pa.*) AS stance_count
      FROM essentials.politicians p
      LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
      WHERE p.external_id IN (-200003, -200004, -200005, -200006, -200007)
      GROUP BY p.external_id, p.full_name
      ORDER BY p.external_id;
    Record this 5-row result for the SUMMARY.

    Step B: Delete `_tmp-execs-roster.json` and `_tmp-execs-outcomes.json`.
  </action>
  <verify>
    <automated>
      -not (Test-Path "C:/Transparent Motivations/essentials/.planning/phases/111-ma-stances-execs-federal/_tmp-execs-roster.json")
      -not (Test-Path "C:/Transparent Motivations/essentials/.planning/phases/111-ma-stances-execs-federal/_tmp-execs-outcomes.json")
    </automated>
  </verify>
  <done>
    - Temp files deleted
    - Summary content ready including per-executive stance counts
  </done>
</task>

</tasks>

<verification>
- Per-executive stance counts queried and recorded
- All applied migrations are paired + cited (zero defective)
- D-08 one-at-a-time honored (sequential processing)
- D-06 immediate-apply honored (no batched applies after research)
- D-10 100% citation rate
</verification>

<success_criteria>
- MA-STANCES-01 further advanced (5 remaining execs processed)
- ROADMAP Phase 111 success criterion #2 partially met (≥8 each for remaining execs)
- All applied migrations satisfy quality gates
</success_criteria>

<output>
Create `.planning/phases/111-ma-stances-execs-federal/111-02-SUMMARY.md` documenting:
- 5-row per-executive result table (external_id, name, migration_number, stance_count)
- Topics with most evidence per official (to flag gaps)
- Any deviations from D-01..D-10
</output>
