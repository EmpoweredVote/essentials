---
phase: 220-contact-data-backfill
plan: 06
type: execute
wave: 3
depends_on: [220-02, 220-03, 220-04, 220-05]
files_modified:
  - .planning/phases/220-contact-data-backfill/220-VERIFICATION.md
  - C:/EV-Accounts/backend/migrations/1409_collin_valid_to_correction.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-1409_collin_valid_to_correction.ts
autonomous: false
requirements: [COLLIN-CONTACT-01, COLLIN-CONTACT-02, COLLIN-CONTACT-03]
user_setup: []

must_haves:
  truths:
    - "Migrations 1405, 1406, 1407, 1408 are applied to production in order, every apply-script gate exits 0, and the changes are pushed to C:/EV-Accounts (Render auto-deploy) in a single push."
    - "The four RESEARCH verification-map queries pass: per-government web_form_url count matches the sourced-form seat counts (COLLIN-CONTACT-01); the generic-catch-all guard returns 0 rows (COLLIN-CONTACT-02); the working-contact-method guard's remaining blanks are ALL documented honest-blanks (Anna + the 3 GAP cities); the split-section check returns 0 rows (D-07)."
    - "The valid_to 5-city spot-check (Allen, Frisco [after the 220-01 resolution], Saint Paul [reshuffle], Princeton, Nevada) is reconciled against the term data in RESEARCH.md; outliers (if any) are corrected via a small migration 1409 and no mass re-write is performed (COLLIN-CONTACT-03, D-03)."
    - "Every city/official that stays blank by design is DOCUMENTED as an intentional honest-blank in 220-VERIFICATION.md — not treated as a failure (D-04); the 3 GAP cities (Josephine, Plano, Richardson) are documented as deferred Migration C with an exact recovery path."
  artifacts:
    - ".planning/phases/220-contact-data-backfill/220-VERIFICATION.md"
  key_links:
    - "220-VERIFICATION.md honest-blank register → Success Criterion #4 working-contact-method guard (every remaining blank is a documented entry)."
    - "220-VERIFICATION.md Migration C deferral → the Playwright/operator recovery path for Josephine/Plano/Richardson."
  prohibitions:
    - statement: "No web sourcing by the executor for the GAP cities — Josephine/Plano/Richardson are documented-deferred, never guessed (D-05; executor has no web tools)."
      status: enforced
      verification: "220-VERIFICATION.md records the GAP cities as deferred with a recovery path; no migration seeds their unconfirmed form URLs or emails."
    - statement: "No mass valid_to re-write — only surfaced outliers are corrected (D-03)."
      status: enforced
      verification: "Migration 1409 (if authored) touches only the specific outlier politician rows named in the spot-check; absent if no outlier found."
    - statement: "Richardson non-Barrios emails and Plano emails are NEVER seeded on the unconfirmed pattern (RESEARCH A3/A4, D-05)."
      status: enforced
      verification: "No migration in this phase writes a Richardson email other than a directly-confirmed one, nor any Plano email."
---

<objective>
Apply the four contact-backfill migrations to production, prove the phase's requirements with the RESEARCH verification-map queries + split-section check, reconcile the valid_to spot-check (COLLIN-CONTACT-03), and document every intentional honest-blank plus the deferred Migration C recovery path so no honest-blank reads as a failure and the 3 GAP cities are cleanly parked.

Purpose: Closes COLLIN-CONTACT-01/02/03 and Success Criterion #4 with evidence, and records the deferred GAP-city work.
Output: production data applied + `220-VERIFICATION.md` (verification results, honest-blank register, Migration C deferral) + an optional valid_to correction migration 1409.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-contact-data-backfill/220-RESEARCH.md
@.planning/phases/220-contact-data-backfill/220-CONTEXT.md
@.planning/phases/220-contact-data-backfill/220-PREFLIGHT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator applies 1405-1408, pushes, runs verification + valid_to spot-check</name>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-1405_collin_web_form_url_batch.ts, _1406_collin_seat_alias_emails.ts, _1407_collin_personal_emails_a.ts, _1408_collin_personal_emails_b.ts (the gated apply-scripts being run).
    - .planning/phases/220-contact-data-backfill/220-RESEARCH.md §"Phase Requirements → Verification Map" (the four exact queries with the 24 geo_ids) and §"Spot-Check: valid_to" (the 5-city sample + term data).
    - [[no_git_in_ev_accounts]] / [[backend_architecture]] (commit + Render deploy path).
  </read_first>
  <action>Operator-run checkpoint (the executor has no Supabase MCP and cannot push production credentials): apply the four migrations in order confirming each apply-script's gates exit 0, commit each + push once to C:/EV-Accounts (Render auto-deploys), then run the four verification-map queries + the split-section check + the 5-city valid_to spot-check, and browse-spot-check one city, per the numbered how-to-verify. Report all outputs (including any valid_to outlier) so Task 2 records them and authors a correction only if needed.</action>
  <what-built>The four gated migrations authored in Wave 2 are ready; only the operator can apply them against production, push, and run the DB verification queries.</what-built>
  <how-to-verify>
    1. From C:/EV-Accounts/backend, apply in order and confirm each exits 0 with all gates green: `npx tsx scripts/_apply-migration-1405_collin_web_form_url_batch.ts`, then `…1406…`, `…1407…`, `…1408…` (each FAIL exits non-zero — stop and report the failing gate).
    2. Commit each migration and push once: `git -C "C:/EV-Accounts" add -A && git -C "C:/EV-Accounts" commit -m "feat(220): backfill Collin County contact data (web_form_url + emails)" && git -C "C:/EV-Accounts" push origin master` (Render auto-deploys; a single push minimizes redeploys — see memory note on push carrying all ahead commits).
    3. Run the four verification-map queries from RESEARCH §"Phase Requirements → Verification Map" over the 24 Collin geo_ids: (a) per-government web_form_url non-null count vs the 11 sourced-form seat counts; (b) the generic-catch-all guard (ILIKE the info/cityhall/contact/townhall/publiccomments/bare-council styles) must return 0 rows; (c) the working-contact-method guard (active officials with web_form_url NULL AND email_addresses empty) — the only remaining rows should be Anna's officials + the 3 GAP cities (Josephine/Plano/Richardson); (d) the standing split-section SQL check ([[section_split_check]]) must return 0 rows.
    4. valid_to spot-check (COLLIN-CONTACT-03, D-03): for Allen, Frisco (using the officeholder confirmed in 220-01), Saint Paul (current Seat 3/4 occupants), Princeton, and Nevada, compare DB valid_to against the term-end data in RESEARCH §"Spot-Check". Report any outlier (obviously-wrong / stale) — do NOT re-verify all officials.
    5. Browse spot-check: open `/results?browse_geo_id=4859696` (Prosper) and confirm an official's profile now shows a contact method (form CTA and/or email).
    Report all outputs (query results, any valid_to outlier, browse result).
  </how-to-verify>
  <acceptance_criteria>
    - All four apply-scripts exit 0 with every gate green; changes pushed.
    - Query (a) matches the sourced-form seat counts; query (b) returns 0 rows; query (c)'s remaining blanks are only Anna + the 3 GAP cities; query (d) returns 0 rows.
    - valid_to spot-check outputs reported for all 5 sample cities, with any outlier explicitly named (or "no outliers").
    - Browse spot-check shows a working contact method on a Prosper profile.
  </acceptance_criteria>
  <resume-signal>Paste the query outputs + valid_to findings + browse result, or type "gates green / verification clean / no valid_to outliers / browse OK".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 220-VERIFICATION.md (results + honest-blank register + Migration C deferral); author valid_to correction only if an outlier was found</name>
  <files>.planning/phases/220-contact-data-backfill/220-VERIFICATION.md, C:/EV-Accounts/backend/migrations/1409_collin_valid_to_correction.sql, C:/EV-Accounts/backend/scripts/_apply-migration-1409_collin_valid_to_correction.ts</files>
  <read_first>
    - The operator's Task 1 report (query outputs, valid_to findings, browse result).
    - .planning/phases/220-contact-data-backfill/220-RESEARCH.md §"Per-City Sourcing Table" (the honest-blank cases + the 3 GAP-city rows with their form-URL candidates and roster leads) and §"Open Questions" (Frisco / GAP-city / Allen-311 recommendations).
  </read_first>
  <action>
    Write `.planning/phases/220-contact-data-backfill/220-VERIFICATION.md` recording: (1) the four verification-map query results + split-section result from Task 1, mapped to COLLIN-CONTACT-01/02 and Success Criterion #4; (2) the valid_to spot-check outcome for the 5 sample cities (COLLIN-CONTACT-03, D-03); (3) an HONEST-BLANK REGISTER (D-04) documenting each city/official that stays blank by design as intentional, NOT a failure — at minimum: Anna (only a mayor-named form exists; no council-wide form and no published emails → all seats blank; note the mayor-only form is an operator option for the Mayor seat only), and the email-blank-but-form-covered cities Farmersville / Lavon / Longview / Murphy / Van Alstyne (their officials meet Success Criterion #4 via the city form; individual emails are genuinely unpublished); (4) a MIGRATION C DEFERRAL section for Josephine (4838068), Plano (4858016), Richardson (4861796) — record each GAP city's form-URL candidate + WebSearch-only roster leads from RESEARCH, the exact recovery path (a Playwright/JS-capable session or a human-operator phone call to the city secretary, matching the /find-headshots WAF-fallback pattern), and the explicit prohibition on seeding unconfirmed data (Plano emails not to be guessed; only Dan.Barrios@cor.gov is directly confirmed for Richardson, the rest of the cor.gov pattern is NOT to be seeded — RESEARCH A3/A4). IF Task 1 reported a valid_to outlier, ALSO author C:/EV-Accounts/backend/migrations/1409_collin_valid_to_correction.sql (a targeted UPDATE of only the named outlier politician rows, idempotent, valid_to only — no mass re-write, D-03) + its apply-script _apply-migration-1409_collin_valid_to_correction.ts mirroring 1393's gate shape; if NO outlier was found, state "no valid_to correction required" and do NOT author 1409.
  </action>
  <verify>
    <automated>test -f ".planning/phases/220-contact-data-backfill/220-VERIFICATION.md" && grep -qiE 'honest.blank|honest blank' ".planning/phases/220-contact-data-backfill/220-VERIFICATION.md" && grep -qiE 'josephine|plano|richardson' ".planning/phases/220-contact-data-backfill/220-VERIFICATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - 220-VERIFICATION.md exists with the four query results, the valid_to spot-check outcome, an honest-blank register (incl. Anna + the form-covered email-blank cities), and a Migration C deferral section for all 3 GAP cities with a recovery path.
    - A valid_to correction migration 1409 exists IFF Task 1 named an outlier; otherwise the doc explicitly states none was required.
    - No GAP-city or unconfirmed email/form is seeded anywhere in this phase.
  </acceptance_criteria>
  <done>The phase's requirements are documented as met with evidence, every honest-blank is recorded as intentional, and the deferred GAP-city work has a clear recovery path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: (Conditional) Operator applies valid_to correction 1409 if Task 2 authored one</name>
  <read_first>
    - .planning/phases/220-contact-data-backfill/220-VERIFICATION.md (whether a 1409 correction was authored).
    - C:/EV-Accounts/backend/scripts/_apply-migration-1409_collin_valid_to_correction.ts (if present).
  </read_first>
  <action>Operator-run checkpoint: if Task 2 authored migration 1409 (a valid_to outlier was found), apply it via `npx tsx`, confirm its gates exit 0, and push to C:/EV-Accounts. If 220-VERIFICATION.md states no correction was required, skip this step and approve.</action>
  <what-built>The optional valid_to outlier correction from Task 2, if any, needs an operator apply + push against production.</what-built>
  <how-to-verify>
    1. If migration 1409 exists: `cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-1409_collin_valid_to_correction.ts` — confirm gates green, then `git -C "C:/EV-Accounts" add -A && git -C "C:/EV-Accounts" commit -m "fix(220): correct valid_to outlier(s) from spot-check" && git -C "C:/EV-Accounts" push origin master`.
    2. If no 1409 exists (no outlier), confirm 220-VERIFICATION.md says so and approve.
  </how-to-verify>
  <acceptance_criteria>
    - If 1409 was authored: it applies with gates green and is pushed.
    - If not: the operator confirms the doc records "no valid_to correction required" and approves.
  </acceptance_criteria>
  <resume-signal>Type "1409 applied + pushed" or "no correction needed — approved".</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `.planning/phases/220-contact-data-backfill/220-VERIFICATION.md` (new — verification results, honest-blank register, Migration C deferral)
- `C:/EV-Accounts/backend/migrations/1409_collin_valid_to_correction.sql` + apply-script (conditional — only if a valid_to outlier is found)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration apply → production essentials schema | Operator-applied, gated, idempotent |
| GAP-city web data → phase scope | Deferred (no web tools); documented recovery path, never guessed |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-220-08 | Repudiation / Integrity | honest-blank read as a failure | medium | mitigate | Honest-blank register (D-04) documents each intentional blank; Success Criterion #4 guard reconciled against it |
| T-220-09 | Tampering (fabrication) | GAP-city / unconfirmed emails (Plano, Richardson non-Barrios) | high | mitigate | Migration C deferred; explicit prohibition + recovery path; no unconfirmed seed (D-05, A3/A4) |
| T-220-10 | Integrity (over-correction) | valid_to mass re-write | medium | mitigate | Only named outliers corrected via 1409; no bulk UPDATE (D-03) |
| T-220-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs (apply-scripts reuse existing deps); legitimacy gate N/A |
</threat_model>

<verification>
- All four migrations applied with gates green and pushed.
- Verification-map queries (a)-(d) pass; valid_to spot-check reconciled; browse spot-check shows a working contact method.
- 220-VERIFICATION.md records results + honest-blank register + Migration C deferral; 1409 authored+applied only if an outlier was found.
</verification>

<success_criteria>
COLLIN-CONTACT-01/02/03 and Success Criterion #4 are proven met with evidence; every honest-blank is documented as intentional; the 3 GAP cities are cleanly deferred with a recovery path and no unconfirmed data seeded.
</success_criteria>

<output>
Create `.planning/phases/220-contact-data-backfill/220-06-SUMMARY.md` when done.
</output>
