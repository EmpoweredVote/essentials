---
phase: 189-smart-banner-integration-graceful-degradation
plan: 03
type: execute
wave: 3
depends_on: ["189-02"]
files_modified: []
autonomous: false
requirements: [SBAN-01, SBAN-04]
tags: [banner, verification, frontend]

must_haves:
  truths:
    - "On both Results and Elections, for the same location, the icon row and population stat render identically on each of the city/state/federal banners (no page-specific divergence)"
    - "The repositioned stat scrim floats mid-left (desktop centered / mobile upper-left), left-aligned to the title, clear of both the title and the feature-icon row at 390px and 1280px"
    - "A city-tier location that resolves NEITHER a population NOR any feature icon renders title + art only — no empty scrim, no empty icon container, no layout shift, zero console errors (v19.0 parity)"
  artifacts: []
  key_links:
    - from: "Results page banners"
      to: "Elections page banners"
      via: "same buildBannerProps output for the same location"
      pattern: "identical stat value + icon set per tier"
---

<objective>
Human-verify the two things automated tests cannot cover: (1) the D-05 mid-left visual placement across
both breakpoints and both pages, and (2) the SBAN-04 empty-state parity on a genuine no-data location.

Purpose: Close SBAN-01's cross-page/all-tiers visual claim and SBAN-04's v19.0 empty-state parity claim
per D-04 (lightweight: live spot-check, no new snapshot/visual-regression infra). All prior automated
gates (Wave 1 + Wave 2) are already green before this plan runs.
Output: operator sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/189-smart-banner-integration-graceful-degradation/189-RESEARCH.md
@.planning/phases/189-smart-banner-integration-graceful-degradation/189-VALIDATION.md
@.planning/phases/189-smart-banner-integration-graceful-degradation/189-CONTEXT.md
@.planning/phases/188-location-stats-strip/188-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live spot-check — D-05 placement, cross-page parity, and SBAN-04 empty state</name>
  <what-built>
    Wave 1 landed the pure `buildBannerProps` helper + the D-05 mid-left stat reposition + the exported
    `shouldRenderStat`/`shouldRenderIcons` guards (all unit-tested green). Wave 2 wired both Results.jsx and
    ElectionsView.jsx to render all three banner tiers through `buildBannerProps(tier, bannerCtx)` one-liners.
    Full Vitest suite + lint are green. This checkpoint confirms the on-screen result matches the contract.
  </what-built>
  <how-to-verify>
    Run the dev server (`npm run dev`) and open the app in a browser with the console visible.

    A) D-05 placement + cross-page parity (SBAN-01/02):
      1. Load a fully-covered city (e.g. a Texas city such as Plano, or any seeded city that resolves a
         population + Treasury icon) on the Representatives/Results page.
      2. Confirm on the CITY banner: the POPULATION scrim floats on the LEFT edge, left-aligned to the location
         title, with the feature-icon row still bottom-right. Repeat visual confirmation on the STATE and
         FEDERAL banners (Federal always shows POPULATION + Treasury icon).
      3. At desktop width (~1280px): the scrim is vertically CENTERED on the left edge. At mobile width
         (~390px, 120px-tall banner): the scrim sits UPPER-LEFT (top-4) with a clear gap above the title
         (no overlap).
      4. Navigate to the Elections page for the SAME location. Confirm each tier's banner shows the IDENTICAL
         population number and the same icon row as the Results page — no page-specific difference.

    B) SBAN-04 empty-state parity:
      5. Load a city-tier location that resolves NEITHER a population NOR any feature icon (per RESEARCH Open
         Question 1: pick a non-Census-place / non-Treasury-tracked location — e.g. a government-list county
         browse, or a deliberately unmatched `browse_geo_id` for the city tier while keeping `browse_state`
         valid so state/federal still render). Federal can NEVER be the empty case — it always resolves both.
      6. Confirm the empty city banner shows ONLY its title + art: no scrim box, no empty icon container, no
         layout shift versus v19.0, and ZERO console errors/warnings.
      7. Confirm this empty-state behavior is identical on both the Results and Elections pages.
  </how-to-verify>
  <acceptance_criteria>
    - Stat scrim is left-anchored (not top-right) on all three tiers, desktop-centered / mobile-upper-left, clear of title + icon row at both 390px and 1280px.
    - Results and Elections show the identical population number + icon set per tier for the same location.
    - A no-data city banner renders title + art only — no empty containers, no layout shift, no console errors — on both pages.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all three (D-05 placement, cross-page parity, empty-state) check out, or describe any visual/console issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered banner -> operator eyes | Pure visual/console verification of already-public, already-rendered data; no data flow, no input crosses any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-04 | Information Disclosure | live banner render | accept | Verification-only; displays public population/deep-link data already shipped in 187/188. No new surface. |
| T-189-SC | Tampering | package installs | accept | Zero package installs this phase (verification only). |
</threat_model>

<verification>
- Operator confirms D-05 placement at 390px + 1280px on both pages.
- Operator confirms cross-page parity (same numbers/icons per tier).
- Operator confirms clean empty state (no containers/shift/console errors) on a genuine no-data city location.
</verification>

<success_criteria>
- D-05 mid-left placement verified at both breakpoints, all three tiers, both pages (SBAN-01).
- v19.0 empty-state parity verified on a real no-icon/no-stat location, console clean (SBAN-04).
</success_criteria>

<output>
Create `.planning/phases/189-smart-banner-integration-graceful-degradation/189-03-SUMMARY.md` when done, recording the exact locations used for both the covered spot-check and the no-data spot-check.
</output>
