---
phase: 171-banner-asset-pipeline-exemplar-art
plan: 02
type: execute
wave: 2
depends_on: [171-01]
files_modified:
  - src/lib/buildingImages.js
  - public/images/bloomington-city-hall.jpg
  - public/images/state-capitols
  - public/images/city-hall-generic.svg
  - public/images/state-capitol-generic.svg
autonomous: false
requirements: [ASST-01]
user_setup:
  - service: supabase-storage
    why: "Bloomington banner is uploaded to the politician_photos bucket via the Plan 01 upload script"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "EV-Accounts backend env file (forward-slash path documented in docs/banner-asset-pipeline.md)"

must_haves:
  truths:
    - "A real licensed Bloomington banner (city-as-subject from an IU vantage, or downtown/courthouse-square fallback) exists in Supabase Storage at cities/bloomington.jpg in the unified 1700x540 dark-overlay family"
    - "buildingImages.js CURATED_LOCAL['bloomington'] resolves to the cities/bloomington.jpg Storage URL (no longer /public/images)"
    - "Browsing a Bloomington address on the live Results page shows the new banner, not the gradient fallback (criterion 2)"
    - "Jurisdictions without art still fall back to the graceful tier-gradient (criterion 4) — getBuildingImages returns null Local for unknown cities"
    - "The unreachable STATE_CAPITOLS image-fallback branch, FALLBACK_LOCAL/FALLBACK_STATE constants, and their dead local assets are removed (D-04)"
    - "STATE_CAPITOLS object itself is RETAINED — STATE_NAME_TO_ABBREV and VALID_STATE_ABBREVS still reference it and the address parsers still work"
    - "AI image generation is NOT used (D-09); Bloomington is a real licensed photo — intentional divergence from ASST-01 text, not a gap"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "Bloomington rewired to Storage URL; dead fallback branch + FALLBACK_* removed; attribution comment added; STATE_CAPITOLS object kept"
      contains: "cities/bloomington.jpg"
  key_links:
    - from: "src/lib/buildingImages.js CURATED_LOCAL['bloomington']"
      to: "Supabase Storage cities/bloomington.jpg"
      via: "full public Storage URL"
      pattern: "cities/bloomington\\.jpg"
    - from: "STATE_NAME_TO_ABBREV / VALID_STATE_ABBREVS"
      to: "STATE_CAPITOLS object"
      via: "Object.entries / Object.keys"
      pattern: "STATE_CAPITOLS"
---

<objective>
Produce the genuine remaining ASST-01 art gap — the Bloomington exemplar banner — by running the
Plan 01 toolchain end to end (source -> treat -> optimize -> upload to `cities/bloomington.jpg` ->
rewire `buildingImages.js` -> attribution), then perform the confirmed-safe dead-code/dead-asset
cleanup (D-04), and verify live on the Results page.

Purpose: ASST-01 — close the one real art gap and prove the ASST-02 pipeline; reconcile storage
consistency by removing superseded local assets and unreachable code.
Output: `cities/bloomington.jpg` in Storage; rewired + cleaned `src/lib/buildingImages.js`; removed
dead local assets.

NOTE: most of ASST-01 (50 state panoramas, federal Capitol, 11 LA-county skylines, onError gradient
fallback) was already shipped in Phase 170's UAT and is live — this plan does NOT redo it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-banner-asset-pipeline-exemplar-art/171-CONTEXT.md
@.planning/phases/171-banner-asset-pipeline-exemplar-art/171-PATTERNS.md
@docs/banner-asset-pipeline.md
@src/lib/buildingImages.js

<interfaces>
<!-- Bloomington rewire target (D-01 + D-05 cities/ prefix): -->
<!--   bloomington: 'https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/bloomington.jpg' -->
<!-- Sibling LA-county entries (lines 76-87) are the URL-shape precedent. -->

<!-- D-04 DELETE (PATTERNS reachability-verified) — delete ONLY these: -->
<!--   - the else-if STATE_CAPITOLS[abbrev] branch building /images/state-capitols/...jpg (lines 178-180) -->
<!--   - const FALLBACK_LOCAL (line 89) and const FALLBACK_STATE (line 90) -->
<!--   - public/images/state-capitols/*.jpg (50 files), public/images/bloomington-city-hall.jpg, -->
<!--     public/images/city-hall-generic.svg, public/images/state-capitol-generic.svg -->
<!-- D-04 KEEP (load-bearing — DO NOT DELETE): -->
<!--   - the STATE_CAPITOLS object (lines 9-60) — feeds STATE_NAME_TO_ABBREV (line 63) + VALID_STATE_ABBREVS (line 197) -->
<!--   - both consumed by parseCityFromAddress (lines 220, 223, 230) and parseStateFromAddress (lines 244, 249, 257) -->
<!-- Also update the stale top doc-comment (lines 1-6) which still claims "fallback to generic SVG". -->

Live verify browse link (a Bloomington, IN address):
  essentials.empowered.vote/results?...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source, treat, optimize, and upload the Bloomington banner</name>
  <files>scripts/banners (run only — produces cities/bloomington.jpg in Storage)</files>
  <read_first>
    - docs/banner-asset-pipeline.md (the runbook produced in Plan 01 — follow it)
    - scripts/banners/process_banner.py (Plan 01 processing script)
    - scripts/banners/upload_banner.py (Plan 01 upload script)
    - .planning/phases/171-banner-asset-pipeline-exemplar-art/171-CONTEXT.md (D-02 subject direction, D-05 path, D-08/D-09)
  </read_first>
  <action>
    Following `docs/banner-asset-pipeline.md`, source ONE real licensed wide photo for Bloomington with
    the D-02 subject direction: the CITY of Bloomington seen from an IU vantage (Sample Gates / a campus
    rise looking out over the town) where the city/downtown is the subject and campus is only framing —
    NOT a campus-architecture shot. Require a usable license (Wikimedia Commons CC / Unsplash). If no such
    city-from-the-gate shot exists with an acceptable license, use the D-02 fallback: a wide downtown /
    Monroe County Courthouse-square view. Do NOT use AI-generated imagery (D-09). Record the image
    title/author/license for attribution.
    Run `process_banner.py` to crop ~3:1 and produce a 1700x540 JPEG q90 (match the state-panorama family,
    per PATTERNS). Run `upload_banner.py` with dest path `cities/bloomington.jpg`, exporting
    `SUPABASE_SERVICE_ROLE_KEY` first (from the EV-Accounts env file per the runbook). Confirm the public
    URL ending in `public/politician_photos/cities/bloomington.jpg` returns the new JPEG.
    This run is the live test of the Plan 01 toolchain — report any script defect rather than working around it.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code} %{content_type}\n" "https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/bloomington.jpg"</automated>
  </verify>
  <acceptance_criteria>
    - The public Storage URL ending in `cities/bloomington.jpg` returns HTTP 200 with content-type image/jpeg
    - The uploaded image is 1700x540 (the measured panorama spec) JPEG q90, in the same dark-overlay-ready family as the state panoramas
    - The subject is the city of Bloomington (D-02 framing or the courthouse-square fallback), NOT a campus-architecture close-up
    - Image is a real licensed photo (Wikimedia/Unsplash), NOT AI-generated; title/author/license captured for attribution
  </acceptance_criteria>
  <done>cities/bloomington.jpg is live in Storage at the banner spec, ready to wire into buildingImages.js.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire buildingImages.js + attribution + D-04 dead-code/asset cleanup</name>
  <files>src/lib/buildingImages.js, public/images/state-capitols, public/images/bloomington-city-hall.jpg, public/images/city-hall-generic.svg, public/images/state-capitol-generic.svg</files>
  <read_first>
    - src/lib/buildingImages.js (the file being modified — lines 1-6 doc-comment, 9-60 STATE_CAPITOLS, 74-90 CURATED_LOCAL/FALLBACK_*, 95-145 attribution block, 161-197 getBuildingImages + VALID_STATE_ABBREVS)
    - .planning/phases/171-banner-asset-pipeline-exemplar-art/171-PATTERNS.md (D-04 reachability verification + KEEP/DELETE caveat)
  </read_first>
  <action>
    FIRST re-confirm reachability before deleting (PATTERNS already proved it): confirm `STATE_PANORAMAS`
    covers all 50 `STATE_CAPITOLS` keys (set-difference empty) so the `else if (STATE_CAPITOLS[abbrev])`
    branch is unreachable, and confirm `FALLBACK_LOCAL`/`FALLBACK_STATE` have zero reads across
    `src/ public/ index.html`. Only then proceed.
    (1) Rewire: change `CURATED_LOCAL.bloomington` from `/images/bloomington-city-hall.jpg` to the full
    Storage URL ending in `cities/bloomington.jpg` (mirror the sibling LA-county URL shape). No change to
    `getBuildingImages` lookup logic.
    (2) Attribution: add an inline comment in the `// <KEY> - Title | Author | License` convention for the
    Bloomington asset (use the real captured title/author/license), grouped as curated standalone-city art.
    (3) D-04 cleanup — delete ONLY: the `else if (STATE_CAPITOLS[abbrev]) { stateImage = ... }` branch
    (so unknown states return null State -> gradient fallback, criterion 4); `const FALLBACK_LOCAL` and
    `const FALLBACK_STATE`; and the dead local assets `public/images/state-capitols/` (50 jpgs),
    `public/images/bloomington-city-hall.jpg`, `public/images/city-hall-generic.svg`,
    `public/images/state-capitol-generic.svg`.
    (4) KEEP the `STATE_CAPITOLS` object — it still feeds `STATE_NAME_TO_ABBREV` and `VALID_STATE_ABBREVS`.
    Update the stale top doc-comment (lines 1-6) which claims "fallback to generic SVG" to reflect the
    Storage-panorama + gradient-fallback reality. Do NOT touch the adjacent out-of-scope orphans listed in PATTERNS.
  </action>
  <verify>
    <automated>grep -c "cities/bloomington.jpg" src/lib/buildingImages.js; grep -v '^//' src/lib/buildingImages.js | grep -c "FALLBACK_LOCAL\|FALLBACK_STATE\|state-capitols"; grep -c "STATE_CAPITOLS\b" src/lib/buildingImages.js; test ! -d public/images/state-capitols && echo "state-capitols-removed"; test ! -f public/images/bloomington-city-hall.jpg && echo "bloomington-local-removed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep cities/bloomington.jpg src/lib/buildingImages.js` matches (rewire done); old `/images/bloomington-city-hall.jpg` value gone
    - No code (non-comment) reference to `FALLBACK_LOCAL`, `FALLBACK_STATE`, or `state-capitols` remains in buildingImages.js
    - `STATE_CAPITOLS` object still present and still referenced by STATE_NAME_TO_ABBREV and VALID_STATE_ABBREVS
    - `public/images/state-capitols/`, `bloomington-city-hall.jpg`, `city-hall-generic.svg`, `state-capitol-generic.svg` deleted
    - Top doc-comment no longer claims generic-SVG fallback
    - A new `// bloomington - Title | Author | License` attribution comment exists
  </acceptance_criteria>
  <done>buildingImages.js points Bloomington at Storage with attribution; dead branch/constants/assets removed; STATE_CAPITOLS retained and parsers intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Address-parser regression guard (STATE_CAPITOLS retained correctly)</name>
  <files>src/lib/buildingImages.test.js</files>
  <read_first>
    - src/lib/buildingImages.js (parseCityFromAddress, parseStateFromAddress, STATE_NAME_TO_ABBREV, VALID_STATE_ABBREVS)
    - .planning/phases/171-banner-asset-pipeline-exemplar-art/171-PATTERNS.md (the KEEP-STATE_CAPITOLS caveat)
  </read_first>
  <behavior>
    - getBuildingImages('Bloomington','IN').Local === the cities/bloomington.jpg Storage URL
    - getBuildingImages('Nowhere','ZZ').Local === null AND .State === null (graceful gradient fallback, criterion 4)
    - getBuildingImages('Anytown','CA').State === the states/CA.jpg panorama URL (unchanged behavior)
    - parseStateFromAddress('100 W Kirkwood Ave, Bloomington, IN, 47404') === 'IN'
    - parseCityFromAddress('100 W Kirkwood Ave, Bloomington, IN, 47404') === 'Bloomington'
    - parseStateFromAddress('Pierre, South Dakota, USA') === 'SD' (full-name path via STATE_NAME_TO_ABBREV still works)
  </behavior>
  <action>
    Add a lightweight test file alongside buildingImages.js (e.g. `src/lib/buildingImages.test.js`,
    matching the repo's existing test runner — check for vitest/jest config; if no runner exists, set the
    automated verify to `MISSING — no test runner; assert via a node one-off import` and create a
    `node`-runnable assertion script instead). Cover the six behaviors above. This locks in that the D-04
    deletion did NOT break the address parsers that still depend on the retained STATE_CAPITOLS object,
    and that the gradient fallback (criterion 4) still resolves to null for unknown jurisdictions.
    Do not modify production logic in this task — it is a regression guard only.
  </action>
  <verify>
    <automated>npx vitest run src/lib/buildingImages.test.js 2>/dev/null || node --test src/lib/buildingImages.test.js 2>/dev/null || node -e "import('./src/lib/buildingImages.js').then(m=>{if(m.parseStateFromAddress('100 W Kirkwood Ave, Bloomington, IN, 47404')!=='IN')process.exit(1);if(m.getBuildingImages('Nowhere','ZZ').Local!==null)process.exit(1);console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - All six behaviors pass under the repo's test runner (or the node fallback assertion)
    - parseStateFromAddress / parseCityFromAddress still resolve IN/Bloomington and the SD full-name form
    - getBuildingImages returns null Local and null State for an unknown ('ZZ') jurisdiction
    - getBuildingImages('Bloomington','IN').Local resolves to the cities/bloomington.jpg URL
  </acceptance_criteria>
  <done>A committed test asserts Bloomington wiring, unknown-jurisdiction fallback, and intact address parsing post-cleanup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual sign-off of Bloomington banner + LA + fallback</name>
  <action>Human verifies the live Bloomington banner, LA regression, and gradient fallback per the steps below. No autonomous work — verification only.</action>
  <what-built>
    Bloomington exemplar banner uploaded to Supabase Storage (cities/bloomington.jpg), wired into
    buildingImages.js, with D-04 dead code/assets removed and a parser regression guard added. The LA
    and 50-state/federal sets were already live from Phase 170.
  </what-built>
  <how-to-verify>
    1. Start the dev server (or use the deployed preview).
    2. Browse a Bloomington, IN address: essentials.empowered.vote/results?... — confirm the CITY tier
       shows the new Bloomington banner (real photo + dark overlay), NOT the tier-gradient fallback (criterion 2).
    3. Browse an LA address — confirm the LA skyline exemplar still renders (already-live regression check).
    4. Browse an address in a covered state with NO curated city (e.g. a small town) — confirm the City
       tier shows the graceful gradient fallback and no broken image (criterion 4).
    5. Confirm the Bloomington image reads as the same panoramic dark-overlay family as the state banners
       (consistent dimensions/treatment).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (subject, framing, treatment, fallback).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> Supabase Storage | service-role key crosses here on the Bloomington upload (full-write credential) |
| committed buildingImages.js -> Tailwind v4 build | repo files scanned at build; only forward-slash Storage URLs added (no Windows paths) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-04 | Information Disclosure | service-role key during Bloomington upload | mitigate | Upload runs via Plan 01 `upload_banner.py`, key from `SUPABASE_SERVICE_ROLE_KEY` env only (D-08); no key committed in buildingImages.js or the test. |
| T-171-05 | Tampering / DoS | buildingImages.js path strings | mitigate | Only forward-slash https Storage URLs added; no raw `C:\` backslash path introduced (Tailwind v4 prod build crash hazard). |
| T-171-06 | Spoofing | Bloomington image provenance | mitigate | Real licensed Wikimedia/Unsplash photo only, attribution recorded inline; AI fabrication excluded (D-09) to avoid inventing landmarks for a real place. |
| T-171-07 | Denial of Service | broken-image on missing art | accept | `SectionBanner.jsx` onError -> gradient fallback (built in 170); Task 3 asserts null-Local/null-State for unknown jurisdictions (criterion 4). |
</threat_model>

<verification>
- `cities/bloomington.jpg` returns HTTP 200 image/jpeg from public Storage.
- buildingImages.js: Bloomington points at the Storage URL; no code reference to FALLBACK_*/state-capitols; STATE_CAPITOLS object retained.
- Deleted assets no longer exist on disk.
- Address-parser + fallback regression test passes.
- Human checkpoint confirms live Bloomington banner (criterion 2), LA still live, and gradient fallback for art-less jurisdictions (criterion 4).
</verification>

<success_criteria>
- ASST-01 art gap closed: Bloomington exemplar live in Storage and wired, in the unified panoramic dark-overlay family.
- Storage is the canonical home for all banner art (D-03); superseded local assets + unreachable code removed (D-04) with STATE_CAPITOLS object and address parsers intact.
- Criterion 2 (Bloomington + LA render live) and criterion 4 (graceful fallback) verified.
- AI generation intentionally absent (D-09) — documented divergence, not a gap.
</success_criteria>

<output>
Create `.planning/phases/171-banner-asset-pipeline-exemplar-art/171-02-SUMMARY.md` when done.
</output>
