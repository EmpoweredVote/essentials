---
phase: 144-glendale-deep-seed
plan: 03
type: execute
wave: 3
depends_on: ["144-02"]
files_modified:
  - C:/EV-Accounts/backend/migrations/904_glendale_headshots.sql
autonomous: false
requirements: [GLEN-01]

must_haves:
  truths:
    - "Daniel Brotman (external_id 686340, 0 images before this wave) has a 600×750 headshot in Supabase Storage politician_photos + a type='default' politician_images row (press_use), sourced via browser (glendaleca.gov is Akamai/WAF-blocked)"
    - "Vartan Gharpetian (external_id 686336) is RE-SOURCED off the scraped_no_license / old la_county/cities/glendale path to a canonical {uuid}-headshot.jpg, 600×750, photo_license='press_use'"
    - "Kassakhian (686339) and Asatryan (686337) images are license-audited and confirmed canonical/press-grade (already cc_by_sa_4.0 canonical) — re-sourced only if a quality pass improves them"
    - "Each new/re-sourced headshot shows the correct person, no superimposed text/graphics, eyes ~1/3 from top, cropped 4:5 (not stretched), 600×750 Lanczos q90 JPEG"
    - "Bartrosouf (-700101) has no official city portrait yet — documented as a known gap, deferred to post-swearing-in; Najarian (-700100) photo is OPTIONAL/discretionary (Metro S3 source)"
    - "Migration 904 is applied via raw SQL and does NOT register in schema_migrations (counter unchanged from Plan 02's 903)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/904_glendale_headshots.sql"
      provides: "Audit-only headshot INSERT (Brotman) + UPDATE (Gharpetian re-source) + photo_origin_url backfill + full-roster image audit"
      contains: "politician_images"
  key_links:
    - from: "Supabase Storage politician_photos/{uuid}-headshot.jpg"
      to: "essentials.politician_images.url (type='default', press_use)"
      via: "INSERT (Brotman) / UPDATE in place (Gharpetian) on politician_images, scoped by politician_id"
      pattern: "'default'"
---

<objective>
Give the missing/bad-license officials clean official headshots and confirm the current 5-person roster
has single canonical-image coverage. After Plan 02, the survivor chamber holds Kassakhian, Asatryan,
Gharpetian, Brotman, Bartrosouf. Image state (DB-verified 2026-06-19):
- Kassakhian (686339): 1 canonical cc_by_sa_4.0 {uuid}-headshot.jpg — LICENSE AUDIT only
- Asatryan (686337): 1 canonical cc_by_sa_4.0 {uuid}/default.jpeg — LICENSE AUDIT only
- Gharpetian (686336): 1 image, scraped_no_license, old la_county/cities/glendale/vartan-gharpetian.jpg
  path — RE-SOURCE required (bad license + non-canonical path; Pitfall 7)
- Brotman (686340): 0 images — SOURCE required
- Bartrosouf (-700101): 0 images, no official city portrait yet — DOCUMENTED GAP, defer to post-swearing-in
- Najarian (-700100): retired; 0 images — OPTIONAL/discretionary (good Metro S3 source available)

⚠️ glendaleca.gov is Akamai/WAF-blocked — curl AND WebFetch return HTTP 403 (Pitfall 3; orchestrator
decision #5). The official councilmember-page portraits for Brotman and Gharpetian CANNOT be fetched
programmatically. Both MUST be sourced via a checkpoint:human-verify — the executor opens the page in a
browser and downloads the official portrait. (Contrast SC, where santaclarita.gov's WordPress CDN was
directly curl-able.)

Processing (NON-NEGOTIABLE per project memory): download portrait → crop to 4:5 FIRST (never stretch —
crop only, eyes ~1/3 from top, full head+shoulders) → resize 600×750 Lanczos q90 JPEG → upload to
Supabase Storage politician_photos bucket as {politician_uuid}-headshot.jpg → INSERT/UPDATE
politician_images (type='default', photo_license='press_use'); set photo_origin_url. Reject any source
with superimposed text/graphics over the face.

Purpose: A voter sees the right face next to each official. Wrong-person or text-overlaid photos destroy
trust (memory: no superimposed graphics, no aspect-ratio distortion). Output: Migration 904 (audit-only —
NOT registered in schema_migrations) + Brotman sourced + Gharpetian re-sourced off the bad-license path;
a human checkpoint confirms the new portraits are the correct person and correctly cropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/144-glendale-deep-seed/144-RESEARCH.md
@.planning/phases/144-glendale-deep-seed/144-02-SUMMARY.md
@LOCATION-ONBOARDING.md

<interfaces>
<!-- Headshot conventions (project memory + RESEARCH §Headshot Sources). glendaleca.gov is WAF-blocked. -->

Source pages (glendaleca.gov — Akamai 403 to all automation; require BROWSER download):
  Brotman (686340):   https://www.glendaleca.gov/government/city-council/councilmember-dan-brotman
  Gharpetian (686336):https://www.glendaleca.gov/government/city-council/councilmember-vartan-gharpetian
Najarian (OPTIONAL — accessible, no WAF): Metro Board S3
  https://la-metro-headshots-production.s3.amazonaws.com/images/ara-j-najarian.width-640.jpg
Bartrosouf (-700101): NO official city portrait yet — document gap (campaign/Ballotpedia only); defer.

Image processing (NON-NEGOTIABLE per memory):
  - Crop to 4:5 aspect FIRST, then resize to 600×750 (Pillow Lanczos, q90) — never stretch directly
  - Eyes ~1/3 from top; full head + shoulders visible
  - No superimposed text/graphics over the face (reject "Re-Elect"-style banners)

Storage + DB:
  - Bucket: politician_photos  ·  Path: {politician_uuid}-headshot.jpg
  - politician_images: type='default', photo_license='press_use'
  - UPDATE politicians.photo_origin_url to the glendaleca.gov source page (or Metro S3 for Najarian) where NULL
  - Brotman: INSERT (0 images) guarded by WHERE NOT EXISTS on politician_id
  - Gharpetian: RE-SOURCE — UPDATE the existing image row in place (url → canonical {uuid}-headshot.jpg,
    photo_license scraped_no_license → 'press_use', photo_origin_url → glendaleca.gov), OR delete the bad
    row + INSERT canonical; net result = exactly ONE type='default' canonical press_use row, no duplicate
  - Kassakhian/Asatryan: license audit only — confirm canonical + press-grade; re-source only if it improves quality

Supabase Storage upload uses the service-role key in C:/EV-Accounts/backend/.env.

This migration is AUDIT-ONLY: apply via raw SQL (mcp__supabase-local execute_sql or psql -f). It does
NOT register in schema_migrations (same convention as SC 896). On-disk counter authoritative; ledger
stays at whatever Plan 02 left it (903).

New official UUIDs resolved by external_id at apply time — do not hardcode.

Pattern migration: C:/EV-Accounts/backend/migrations/896_santa_clarita_headshots.sql (audit-only INSERT +
re-source UPDATE pattern; see 143-03-SUMMARY.md §Deviations for the re-source-existing-image case).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source (Brotman) + re-source (Gharpetian) + license-audit (Kassakhian/Asatryan); crop/resize/upload</name>
  <files>C:/EV-Accounts/backend/migrations/904_glendale_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/896_santa_clarita_headshots.sql (audit-only INSERT + re-source UPDATE; download/crop/upload flow)
    - .planning/phases/143-santa-clarita-deep-seed/143-03-SUMMARY.md (§What was done + §Deviations — exact re-source-existing-image pattern, Weste re-source / Ayala upgrade)
    - .planning/phases/144-glendale-deep-seed/144-RESEARCH.md (§Headshot Sources — exact URLs, WAF note, Gharpetian re-source, Najarian optional, Bartrosouf gap; §Common Pitfalls 3/7)
    - LOCATION-ONBOARDING.md (§Step 4 Headshots)
  </read_first>
  <action>
    glendaleca.gov is WAF-blocked, so Brotman + Gharpetian official portraits are downloaded by the human
    in Task 3's checkpoint flow — but the executor still drives the crop/resize/upload pipeline on whatever
    portrait files the human provides (or that the executor obtains via browser). For each of Brotman
    (686340, 0 images → new) and Gharpetian (686336, re-source off scraped_no_license / old
    la_county/cities/glendale path): obtain the official glendaleca.gov portrait (browser; do NOT attempt
    bare curl/WebFetch — it 403s), crop to 4:5 (crop only — never stretch; eyes ~1/3 from top, full
    head+shoulders), then resize to 600×750 Lanczos q90 JPEG (Python Pillow). Reject any image with
    superimposed text/graphics over the face — document a gap honestly rather than upload a bad photo.
    Resolve each politician UUID by external_id, then upload to Supabase Storage bucket politician_photos
    as {uuid}-headshot.jpg (service-role key in C:/EV-Accounts/backend/.env). Record each public Storage
    URL for Task 2. License-audit Kassakhian (686339) + Asatryan (686337): confirm their existing rows are
    canonical {uuid}-headshot.jpg / canonical path + press-grade license (already cc_by_sa_4.0) — re-source
    only if a quality pass improves them. OPTIONAL (Claude's discretion, ~5 min): source Najarian
    (-700100) from the Metro S3 URL. Bartrosouf (-700101) has no official city portrait yet — document as a
    known gap, defer to post-swearing-in (do NOT upload a campaign photo as type='default').
  </action>
  <verify>
    <automated>For Brotman and Gharpetian: the processed local file is exactly 600×750 px (`python -c "from PIL import Image; print(Image.open('brotman.jpg').size)"` → (600, 750)) and the Storage object {uuid}-headshot.jpg returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - Brotman portrait obtained via browser (glendaleca.gov WAF — no bare curl), processed to exactly 600×750, 4:5, not stretched, no superimposed text/graphics, uploaded to politician_photos/{uuid}-headshot.jpg (HTTP 200)
    - Gharpetian portrait obtained via browser, processed 600×750 4:5, uploaded to the canonical {uuid}-headshot.jpg path (replacing the old la_county/cities/glendale path)
    - Kassakhian + Asatryan existing images confirmed canonical + press-grade (license-audit pass); re-sourced only if quality improved
    - Bartrosouf (-700101) documented as a known gap (no official city portrait yet); Najarian (-700100) optional photo sourced from Metro S3 or documented as skipped
    - Public Storage URLs recorded for Task 2
  </acceptance_criteria>
  <done>Brotman sourced + Gharpetian re-sourced off the bad-license path (or gaps documented); Kassakhian/Asatryan audited; Bartrosouf gap documented; processed images live in Storage at canonical paths.</done>
</task>

<task type="auto">
  <name>Task 2: Author + apply migration 904 (headshot rows) and audit full-roster image coverage</name>
  <files>C:/EV-Accounts/backend/migrations/904_glendale_headshots.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/896_santa_clarita_headshots.sql (INSERT politician_images + re-source UPDATE + photo_origin_url backfill + roster image audit; audit-only apply)
    - .planning/phases/143-santa-clarita-deep-seed/143-03-SUMMARY.md (full-roster coverage audit query + audit-only ledger check)
    - .planning/phases/144-glendale-deep-seed/144-RESEARCH.md (§Validation Architecture Wave 3 checks, §Common Pitfalls 7)
  </read_first>
  <action>
    Write C:/EV-Accounts/backend/migrations/904_glendale_headshots.sql. For Brotman (686340), resolved by
    external_id, INSERT a politician_images row (url = the Storage public URL from Task 1, type='default',
    photo_license='press_use') guarded by WHERE NOT EXISTS on politician_id, and UPDATE
    politicians.photo_origin_url to the glendaleca.gov source page where NULL. For Gharpetian (686336),
    RE-SOURCE: UPDATE the existing politician_images row in place — url → canonical {uuid}-headshot.jpg
    Storage URL, photo_license scraped_no_license → 'press_use', and UPDATE photo_origin_url — scoped by
    politician_id so the net result is exactly ONE type='default' canonical press_use row (no duplicate;
    if a delete+insert is cleaner, ensure the final count is exactly 1). For Najarian (-700100) if sourced,
    INSERT guarded by NOT EXISTS. Apply via raw SQL (mcp__supabase-local execute_sql or psql -f) — do NOT
    run through the schema_migrations ledger (audit-only). Then run the full-roster coverage audit: LEFT
    JOIN the 5 current Glendale officials (686339/686337/686336/686340/-700101) to politician_images and
    confirm only the documented gap (Bartrosouf) is missing an image, and confirm no current official has
    >1 type='default' row (especially Gharpetian must be exactly 1 after the re-source). Verify MAX(version)
    in schema_migrations is unchanged by this file.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT p.external_id, pi.photo_license FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (686336,686340) AND pi.type='default';" returns Gharpetian (686336) + Brotman (686340) each with photo_license='press_use', exactly one row each</automated>
  </verify>
  <acceptance_criteria>
    - Brotman (686340) has a type='default' politician_images row pointing at his Storage URL, photo_license='press_use'
    - Gharpetian (686336) has exactly ONE type='default' row, now canonical {uuid}-headshot.jpg path + photo_license='press_use' (no scraped_no_license remaining; no duplicate)
    - photo_origin_url populated for Brotman + Gharpetian (+ Najarian if sourced)
    - Full-roster LEFT JOIN missing-image query returns only the documented Bartrosouf gap (and Najarian if skipped)
    - No current Glendale official has >1 type='default' image row
    - schema_migrations MAX(version) unchanged by migration 904 (audit-only confirmed; stays at 903)
    - Migration is idempotent (re-apply = 0 rows changed)
  </acceptance_criteria>
  <done>904 applied; Brotman headshot row present; Gharpetian re-sourced to canonical press_use (exactly 1 row); full-roster image audit clean (only Bartrosouf gap); ledger untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-source the WAF-blocked portraits + visually verify the new headshots</name>
  <action>Pause for the user to (a) download the WAF-blocked glendaleca.gov portraits for Brotman + Gharpetian in a browser and hand them to the executor for processing, and (b) visually verify all new/re-sourced portraits per the how-to-verify steps; do not proceed to phase completion until approved.</action>
  <what-built>New/re-sourced official headshots (Daniel Brotman — new; Vartan Gharpetian — re-sourced off scraped_no_license/old path) processed to 600×750 and live in Supabase Storage + politician_images; Kassakhian/Asatryan license-audited; Bartrosouf gap documented. glendaleca.gov is Akamai/WAF-blocked so the portraits require manual browser download.</what-built>
  <how-to-verify>
    1. Open https://www.glendaleca.gov/government/city-council/councilmember-dan-brotman and
       https://www.glendaleca.gov/government/city-council/councilmember-vartan-gharpetian in a browser;
       download each official portrait and provide it to the executor (curl/WebFetch will 403).
    2. After processing, confirm each photo is the CORRECT person (cross-check against the glendaleca.gov page).
    3. Confirm no superimposed text/graphics over the face, eyes ~1/3 from top, head+shoulders visible, not stretched (4:5, 600×750).
    4. Confirm Gharpetian now shows exactly one canonical image (the old scraped_no_license la_county path is gone).
    5. Confirm the existing Kassakhian/Asatryan portraits look correct; confirm Bartrosouf's missing photo is documented (not faked).
  </how-to-verify>
  <resume-signal>Type "approved" or describe which photo(s) need re-sourcing / re-cropping.</resume-signal>
</task>

</tasks>

<threat_model>
DB-seeding phase with a limited external image source. No untrusted user input reaches the DB —
uploads + image-row writes use service-role/operator against production. Threat surface = wrong-person
or distorted/overlaid photos, and a bad-license row left in place, mitigated by the
crop-then-resize/no-stretch rule + browser-sourced official portraits + a blocking human-verify
checkpoint + the explicit Gharpetian re-source.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| glendaleca.gov (WAF-blocked) → browser → local processing | External image source; WAF blocks automation so a human downloads; could be wrong-person or text-overlaid. |
| operator → Supabase Storage + production DB | Uploads + image-row INSERT/UPDATE against production via service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-13 | Spoofing | Wrong-person headshot uploaded for Brotman/Gharpetian | mitigate | Portraits sourced from the official glendaleca.gov councilmember pages (browser); blocking human-verify checkpoint cross-checks each face |
| T-144-14 | Tampering | Image stretched / aspect-distorted / text-overlaid | mitigate | Crop-to-4:5-then-resize rule; reject superimposed-graphics sources; 600×750 dimension check; human checkpoint |
| T-144-15 | Tampering | Gharpetian's scraped_no_license / old-path row left in place | mitigate | Explicit re-source UPDATE to canonical {uuid}-headshot.jpg + press_use; post-audit confirms exactly 1 canonical row, no scraped_no_license remaining (Pitfall 7) |
| T-144-16 | Tampering | Audit-only migration accidentally registered in schema_migrations | mitigate | Apply via raw SQL only; verify MAX(version) unchanged (stays 903) before/after |
| T-144-17 | Tampering | Duplicate image rows re-introduced (esp. Gharpetian after re-source) | mitigate | Brotman INSERT guarded WHERE NOT EXISTS; Gharpetian re-source scoped by politician_id to a single row; post-audit confirms ≤1 type='default' per official |
| T-144-18 | Spoofing | Fake/campaign photo uploaded for Bartrosouf to fill the gap | mitigate | Bartrosouf has no official city portrait yet → documented gap, deferred; no campaign photo seeded as type='default' |
</threat_model>

<verification>
After 904 + checkpoint:
1. Brotman (686340) has exactly one type='default' press_use image; Gharpetian (686336) re-sourced to exactly one canonical press_use image (no scraped_no_license)
2. full-roster LEFT JOIN missing-image query (5 current officials) returns only the documented Bartrosouf gap
3. no current official has >1 type='default' image
4. Kassakhian/Asatryan confirmed canonical + press-grade
5. schema_migrations MAX(version) unchanged by 904 (stays 903)
6. human checkpoint approved (correct person, correct crop, no overlays; Bartrosouf gap documented)
</verification>

<success_criteria>
Brotman has a correct, correctly-cropped 600×750 portrait; Gharpetian is re-sourced off the
scraped_no_license/old path to a canonical press_use image; Kassakhian/Asatryan are audited clean; the
Bartrosouf gap is honestly documented and deferred; the full current roster has clean single-image
coverage; the ledger counter is preserved.
</success_criteria>

<output>
Create `.planning/phases/144-glendale-deep-seed/144-03-SUMMARY.md` when done.
</output>
