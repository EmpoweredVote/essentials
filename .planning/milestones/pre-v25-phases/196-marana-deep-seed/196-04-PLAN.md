---
phase: 196-marana-deep-seed
plan: 04
type: execute
wave: 4
depends_on: ["196-03"]
files_modified:
  - src/lib/buildingImages.js
  - src/lib/coverage.js
autonomous: false
requirements: [BANR-01, SUB-02]
must_haves:
  truths:
    - "A licensed Marana community banner (real photo, no AI, no aerial) is processed to the banner spec (1700×540 @ 3.15:1) and uploaded to Storage at cities/marana.jpg"
    - "buildingImages.js CURATED_LOCAL has a state-scoped single-word `marana` entry pointing at the uploaded banner"
    - "coverage.js gets a 'Marana' area APPENDED to the EXISTING Arizona COVERAGE_STATES block (created by Phase 194, 2 entries today) — NOT a third Arizona block, and NOT an entry in COVERAGE_COUNTIES (where Pima County lives) — with a DB-honest chip (hasContext:true, since stances landed in Plan 03) browsing geo_id 0444270"
    - "The Marana banner is visually distinct from Pima County's Catalina/Pusch-Ridge landscape banner, the Oro Valley CDO Trail Bridge, the Tucson downtown streetscape, and the AZ-state Phoenix skyline — Marana sits by the Tortolita range, NOT the Catalinas (D-03 collision avoidance)"
    - "A real Marana street address, queried against the live candidates-search API, resolves to the seeded Town of Marana Mayor + all 6 at-large Council Members (the phase-goal's resident-facing outcome, end-to-end)"
  artifacts:
    - path: "src/lib/buildingImages.js"
      provides: "CURATED_LOCAL `marana` → uploaded banner URL"
      contains: "marana"
    - path: "src/lib/coverage.js"
      provides: "'Marana' area appended to the EXISTING Arizona COVERAGE_STATES block, browseGovernmentList ['0444270'], hasContext:true"
      contains: "Marana"
  key_links:
    - from: "coverage.js COVERAGE_STATES Arizona → 'Marana'"
      to: "buildingImages.js CURATED_LOCAL marana"
      via: "browse label 'Marana' → representingCity → getBuildingImages substring match"
      pattern: "marana"
    - from: "buildingImages.js marana"
      to: "Storage cities/marana.jpg"
      via: "src URL (state-scoped 'AZ')"
      pattern: "cities/marana.jpg"
---

<objective>
Give the Town of Marana its licensed community banner (BANR-01) and its DB-honest coverage chip. Source a
real Marana photo ONE AT A TIME (no AI, no aerial), process it to the banner spec via
`scripts/banners/process_banner.py` (1700×540 @ 3.15:1), upload to Storage at `cities/marana.jpg` via
`upload_banner.py`, wire it into `src/lib/buildingImages.js` CURATED_LOCAL as a single state-scoped
`marana` entry (single-word, unquoted key), and surface the Town of Marana in `src/lib/coverage.js`.

CRITICAL DELTA (Pitfall 8): the Arizona `COVERAGE_STATES` block ALREADY EXISTS in `coverage.js` today
with TWO entries — `'Tucson'` (Phase 194) and `'Oro Valley'` (Phase 195). This phase APPENDS a third
`'Marana'` area object to that EXISTING block's `areas` array. Do NOT create a second/third
`{ name: 'Arizona', ... }` block (that would be a duplicate-block bug), and do NOT add Marana to
`COVERAGE_COUNTIES` (where Pima County lives — untouched). Likewise `buildingImages.js` is a plain APPEND
of a new `marana` key (single-word → no quotes, mirror the `tucson:` / `henderson:` style, NOT the quoted
multi-word `'oro valley'` style).

CRITICAL (D-03 / Open Question 2 — visual distinctiveness): Marana sits by the TORTOLITA range, NOT the
Catalinas. The banner MUST avoid Catalina-range imagery (which belongs to the Pima County 193 and Oro
Valley 195 banners) — a Catalina shot would visually collide with sibling banners. RESEARCH identified a
licensing-clean fallback (a Dove Mountain golf-course ground shot, Bernard Gagnon, CC BY-SA 3.0, Tortolita
range — no Catalina overlap), but the one-at-a-time sourcing pass should FIRST attempt a direct Flickr CC
search for a stronger "Marana identity" subject (Ed Honea Marana Municipal Complex, Downtown Marana Main
Street, or Heritage River Park) before finalizing — a golf-course fairway may not read as "street-scene
or skyline" as well as a civic/downtown shot. Present options for review; show the Pima Catalinas + Oro
Valley bridge + Tucson streetscape + AZ-state Phoenix banners alongside during review per
`feedback_banner_show_state_shot_on_conflict`.

Purpose: Satisfies SUB-02 / ROADMAP success criteria #4 (licensed banner sourced/processed/uploaded/
wired) and #5 (Marana surfaced with a DB-honest chip). This is the 6th Tucson-metro deep-seed banner and
must stay visually distinct from Pima County (Catalina landscape), Oro Valley (CDO Trail Bridge), Tucson
(downtown streetscape), and the AZ-state Phoenix skyline.
Output: uploaded `cities/marana.jpg` + edited `buildingImages.js` + `coverage.js` in the `essentials`
repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/196-marana-deep-seed/196-RESEARCH.md
@.planning/phases/196-marana-deep-seed/196-PATTERNS.md
@.planning/phases/196-marana-deep-seed/196-CONTEXT.md

<execution_note>
Frontend files (`src/lib/buildingImages.js`, `src/lib/coverage.js`) live in the CURRENT `essentials` repo
(commit here, not `C:/EV-Accounts`). The banner SOURCING + processing + Storage upload are ORCHESTRATOR-RUN
(executor has no network/Storage access): the inline orchestrator sources the photo one-at-a-time, runs
the banner toolchain, and uploads. Per project convention (`feedback_banner_source_one_at_a_time`,
`feedback_banner_prefer_photo_over_gradient`, `feedback_banner_show_state_shot_on_conflict`,
`feedback_deepseed_community_banner`) banner sourcing is sequential, prefers a real photo, and shows the
sibling AZ banners alongside during review because there is a genuine visual-conflict risk. The CDN URL is
deterministic (`cities/marana.jpg`) so the frontend wiring can be authored regardless of upload order.
Depends on Plan 03 — hasContext:true is only honest because Plan 03 seeded ≥1 stance row.
</execution_note>

<interfaces>
- Banner spec: process via `scripts/banners/process_banner.py` (`--url <src> --output <path>
  --vertical-anchor` tuned per shot) → 1700×540 @ 3.15:1, upload via `scripts/banners/upload_banner.py
  --file <path> --dest cities/marana.jpg` (needs SUPABASE_SERVICE_ROLE_KEY from
  `C:/EV-Accounts/backend/.env`). Destination tier is `cities/` (banner TYPE, not government type).
  Storage is no-cache → overwrite = instant refresh.
- CDN URL: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/marana.jpg
- buildingImages.js CURATED_LOCAL: add `marana: { state: 'AZ', src: '<CDN URL>' }` (single-variant —
  `marana` has no same-named-city collision in the covered set, so NOT the array-of-{state,src} form;
  single-word key → NO quotes, mirror `tucson:` / `henderson:`, NOT the quoted `'oro valley'` style) with
  an attribution comment (subject | author | license) above it. getBuildingImages() needs NO code change.
  Storage file is `cities/marana.jpg`; coverage.js label is 'Marana'.
- coverage.js: APPEND one area object to the EXISTING Arizona COVERAGE_STATES block's `areas` array (which
  already holds 'Tucson' + 'Oro Valley'):
  `{ label: 'Marana', browseGovernmentList: ['0444270'], browseStateAbbrev: 'AZ', hasContext: true }`.
  hasContext:true is honest because Plan 03 seeded ≥1 stance row. Do NOT create a second/third Arizona
  block (Pitfall 8); do NOT touch COVERAGE_COUNTIES (Pima County lives there — unrelated). Placement
  within the areas array is cosmetic. browseGovernmentList value is the G4110 geo_id ('0444270').
- Banner candidates (RESEARCH Open Question 2 — visual selection done at execute time): confirmed-licensable
  fallback = `The_Golf_Club_at_Dove_Mountain_(Saguaro)_no_3.jpg` (Wikimedia Commons, CC BY-SA 3.0, Bernard
  Gagnon, 4233×2956, Tortolita range — no Catalina overlap). PREFER first a direct Flickr CC search for the
  Ed Honea Marana Municipal Complex / Downtown Marana Main Street / Heritage River Park (stronger civic
  identity) — a generic WebSearch did not surface these this session, but a Flickr API/browse pass may.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Source + process + upload the Marana banner, one at a time (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-RESEARCH.md (§ Standard Stack banner mechanism; § Open Question 2 — Dove Mountain fallback + the Flickr-first attempt for the Municipal Complex / Downtown Marana / Heritage River Park; § Assumptions Log A5 — golf-course vs street-scene fit)
    - .planning/phases/196-marana-deep-seed/196-CONTEXT.md (D-03 — no AI, no aerial, NO Catalina-range; Tortolita/Dove Mountain/Heritage River Park candidate subjects; show state shot if conflict)
    - .planning/phases/196-marana-deep-seed/196-PATTERNS.md (§ buildingImages.js mapping — the CURATED_LOCAL `marana` single-word-key entry + attribution comment convention; the process_banner.py / upload_banner.py commands + cities/marana.jpg destination)
    - C:/Transparent Motivations/essentials/scripts/banners/process_banner.py (the banner spec 1700×540 @ 3.15:1 + --vertical-anchor)
    - C:/Transparent Motivations/essentials/scripts/banners/upload_banner.py (the Storage upload + --dest convention)
  </read_first>
  <action>ORCHESTRATOR-RUN + blocking human-verify — no executor file action. The orchestrator sources a licensed Marana photo ONE AT A TIME (Flickr-first for a civic/downtown subject, Dove Mountain golf-course fallback), processes it to the banner spec (1700×540 @ 3.15:1), uploads to cities/marana.jpg, and the operator visually approves — with explicit attention to NO Catalina-range imagery (D-03) and distinctiveness from the Pima / Oro Valley / Tucson / AZ-state banners — before wiring. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A licensed community banner for the Town of Marana — the 6th Tucson-metro deep-seed banner. RESEARCH
    identified a licensing-clean fallback (Dove Mountain golf-course ground shot, Bernard Gagnon, CC BY-SA
    3.0, Tortolita range) but flagged that a golf-course fairway may not satisfy "street-scene or skyline"
    as well as a civic/downtown subject, and that a direct Flickr CC search (not generic WebSearch) may
    surface the Ed Honea Marana Municipal Complex / Downtown Marana Main Street / Heritage River Park.
    Human visual QA is required because "real photo, no AI, no aerial, NO Catalina-range, distinct from
    the sibling AZ banners" is a judgment a row-count cannot make.
  </what-built>
  <how-to-verify>
    1. The orchestrator FIRST attempts a direct Flickr CC search for a Marana civic/downtown subject (Ed
       Honea Marana Municipal Complex / Downtown Marana Main Street / Heritage River Park). If none is
       clearly licensed, fall back to the confirmed Dove Mountain golf-course shot (Bernard Gagnon, CC
       BY-SA 3.0). Source ONE candidate at a time (licensed CC/public-domain, real photo, NOT
       AI-generated, NOT aerial/drone, NOT Catalina-range), record subject | author | license, run
       `python scripts/banners/process_banner.py --url <src> --output /tmp/marana.jpg --vertical-anchor
       0.4` (tune anchor per shot), then `python scripts/banners/upload_banner.py --file /tmp/marana.jpg
       --dest cities/marana.jpg` (SUPABASE_SERVICE_ROLE_KEY from `C:/EV-Accounts/backend/.env`).
    2. Operator visually confirms: reads as Marana, a real photo (no AI artifacts, no aerial/drone shot),
       contains NO Catalina-range imagery (D-03), and is visually DISTINCT from the Pima County
       Catalina/Pusch-Ridge banner, the Oro Valley CDO Trail Bridge, the Tucson downtown streetscape, AND
       the AZ-state Phoenix skyline — show all four alongside during review per
       feedback_banner_show_state_shot_on_conflict.
    3. If unusable, too similar to a sibling banner, or Catalina-range, the orchestrator re-sources another
       candidate ONE AT A TIME (prefer a real civic/downtown photo; gradient only if truly nothing usable
       exists, per feedback_banner_prefer_photo_over_gradient).
  </how-to-verify>
  <acceptance_criteria>
    - `curl -sI https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos/cities/marana.jpg` returns HTTP 200.
    - The uploaded image matches the banner spec (process_banner.py output = 1700×540 @ 3.15:1) — PIL/curl-download dimension check.
    - Operator approves: real photo, Marana subject, no AI, no aerial, NO Catalina-range (D-03), distinct from the Pima Catalina/Pusch-Ridge banner AND the Oro Valley CDO bridge AND the Tucson streetscape AND the AZ-state Phoenix skyline.
    - Subject | author | license recorded in 196-04-SUMMARY.md for the attribution comment.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the operator confirms the uploaded banner reads as Marana, is a real non-aerial photo with NO Catalina-range imagery, and is distinct from the Pima County + Oro Valley + Tucson + AZ-state banners — or describe what to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the banner into buildingImages.js + append Marana to the EXISTING Arizona coverage.js block</name>
  <files>src/lib/buildingImages.js, src/lib/coverage.js</files>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-PATTERNS.md (§ buildingImages.js mapping + § coverage.js mapping — the exact CURATED_LOCAL `marana` single-word-unquoted entry + attribution comment, and the APPEND of the 3rd 'Marana' area to the EXISTING Arizona COVERAGE_STATES areas array; the Pitfall 8 warning that the Arizona block already exists with 2 entries and Pima is in COVERAGE_COUNTIES; getBuildingImages needs NO change)
    - C:/Transparent Motivations/essentials/src/lib/buildingImages.js (the CURATED_LOCAL object + the existing single-word `tucson:` entry to mirror the unquoted-key style; getBuildingImages substring/state-scoping logic)
    - C:/Transparent Motivations/essentials/src/lib/coverage.js (the EXISTING Arizona COVERAGE_STATES block with its 'Tucson' + 'Oro Valley' areas — append to its areas array; confirm NO second Arizona block is created; the separate COVERAGE_COUNTIES array holding Pima County — do NOT touch it)
    - .planning/phases/196-marana-deep-seed/196-04-SUMMARY.md (the banner subject | author | license from Task 1 for the attribution comment)
  </read_first>
  <action>
    In `src/lib/buildingImages.js`, add ONE entry to the CURATED_LOCAL object:
    `marana: { state: 'AZ', src: '<CDN URL for cities/marana.jpg>' }` (single-variant, NOT the
    array-of-{state,src} form — `marana` has no same-named-city collision; single-word key → NO quotes,
    mirror the `tucson:` / `henderson:` style, NOT the quoted `'oro valley'` multi-word style), with an
    attribution comment above it (subject | author | license from Task 1). Note in the comment it is
    deliberately distinct from the Pima County Catalina landscape banner, the Oro Valley CDO Trail Bridge,
    the Tucson downtown streetscape, and the AZ-state Phoenix skyline, and that Marana sits by the
    Tortolita range (D-03 — no Catalinas). Make NO change to getBuildingImages() — its substring match +
    state-scoping already handle the label. Storage file is `cities/marana.jpg`.

    In `src/lib/coverage.js`, APPEND one area object to the EXISTING Arizona COVERAGE_STATES block's
    `areas` array (the block that already contains 'Tucson' + 'Oro Valley'):
    `{ label: 'Marana', browseGovernmentList: ['0444270'], browseStateAbbrev: 'AZ', hasContext: true }`.
    Do NOT create a second/third `{ name: 'Arizona', ... }` block (Pitfall 8), do NOT add Marana to
    COVERAGE_COUNTIES (where Pima County lives), and do NOT broaden parseCityFromAddress. hasContext:true
    is honest because Plan 03 seeded ≥1 stance row (this plan depends on Plan 03). Placement within the
    areas array is cosmetic.

    Commit both files to the `essentials` repo via git.
  </action>
  <verify>
    <automated>cd "C:/Transparent Motivations/essentials" && grep -qE "^\s*marana:\s*\{" src/lib/buildingImages.js && grep -qF "cities/marana.jpg" src/lib/buildingImages.js && grep -qF "'Marana'" src/lib/coverage.js && grep -qF "'0444270'" src/lib/coverage.js && [ "$(grep -c "name: 'Arizona'" src/lib/coverage.js)" = "1" ] && node --input-type=module -e "import('./src/lib/coverage.js').then(()=>console.log('coverage-ok')).catch(e=>{console.error(e);process.exit(1)})" && node --input-type=module -e "import('./src/lib/buildingImages.js').then(()=>console.log('bi-ok')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - buildingImages.js CURATED_LOCAL has a single-word unquoted `marana:` key with `state: 'AZ'` and src = the cities/marana.jpg CDN URL, plus an attribution comment (subject | author | license).
    - coverage.js Arizona COVERAGE_STATES block's areas array has a NEW `{ label: 'Marana', browseGovernmentList: ['0444270'], browseStateAbbrev: 'AZ', hasContext: true }` entry.
    - There is EXACTLY ONE `name: 'Arizona'` block in coverage.js (no duplicate/third block created — Pitfall 8).
    - COVERAGE_COUNTIES is NOT modified (Pima County entry untouched); no Marana entry added there.
    - Both files parse cleanly via `node --input-type=module -e "import(...)"` (exit 0).
    - getBuildingImages() and parseCityFromAddress unchanged.
    - `npx vitest run src/lib/buildingImages.test.js` stays green (established non-gap; run as a spot-check).
    - Both files committed to the `essentials` repo.
  </acceptance_criteria>
  <done>Marana banner wired into CURATED_LOCAL (single-word key) and surfaced via a new area appended to the EXISTING Arizona COVERAGE_STATES block with a DB-honest hasContext:true chip; exactly one Arizona block; COVERAGE_COUNTIES untouched; both frontend files parse, buildingImages.test.js green, both committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Verify the resident-facing outcome end-to-end — real Marana address → Mayor + 6 council (ORCHESTRATOR-RUN)</name>
  <read_first>
    - .planning/phases/196-marana-deep-seed/196-01-SUMMARY.md (the 7 seeded officials + their offices/titles to expect back)
    - .planning/phases/196-marana-deep-seed/196-RESEARCH.md (§ Architecture Patterns — "Address routing (NO CODE CHANGE)"; the existing G4110→(LOCAL,LOCAL_EXEC) mapping is expected to cover this with zero code change)
  </read_first>
  <action>ORCHESTRATOR-RUN closing gate — no executor file action. The whole phase goal is "Marana residents can SEE their council with a compass." Every other plan verifies DB structure (row counts, joins) but not the live resident-facing result. The orchestrator POSTs a real Marana street address to the live candidates-search API (POST /candidates/search {query}, full street address required per the search contract) and confirms the response includes the seeded Mayor (LOCAL_EXEC) + all 6 at-large Council Members (LOCAL, all on geo_id 0444270). This exercises the geofence→district→office join that Plan 01 seeded, end-to-end, against the same shared-LOCAL-row shape already live in prod for Torrance/Oro Valley. See &lt;how-to-verify&gt;.</action>
  <what-built>
    A live end-to-end confirmation that the phase goal is actually TRUE for a resident — not just that the
    rows exist. Mirrors Oro Valley (195)'s dedicated live-routing verification: Marana's simple at-large
    shape means every Marana address should return the SAME Mayor + 6 council, which IS the correct
    at-large behavior (no per-ward differentiation to check).
  </what-built>
  <how-to-verify>
    1. The orchestrator POSTs a real Marana street address (e.g. Marana Municipal Complex / Town Hall,
       11555 W Civic Center Dr, Marana, AZ 85653, or another verified in-town street address) to the live
       candidates-search API (accounts-api.empowered.vote, POST /candidates/search {query} — full street
       address required).
    2. Confirm the response includes the Town of Marana government with the Mayor (LOCAL_EXEC) + all 6
       at-large Council Members (LOCAL), matching the roster seeded in Plan 01 (party never displayed —
       antipartisan).
    3. If any council member is missing or an extra/wrong office appears, STOP — this indicates a
       district-attach or geofence-mapping defect from Plan 01 that must be fixed before the phase closes.
  </how-to-verify>
  <acceptance_criteria>
    - A real Marana street address POSTed to the live candidates-search API returns the Town of Marana with the Mayor + exactly 6 at-large Council Members (7 offices total), matching Plan 01's seeded roster.
    - No wrong-jurisdiction or duplicate Marana office appears in the response (section-split / wrong-district-attach would surface here).
    - Result recorded in 196-04-SUMMARY.md (the address used + the offices returned).
  </acceptance_criteria>
  <resume-signal>Type "approved" once a real Marana address returns the Mayor + all 6 Council Members from the live search API — or describe the routing discrepancy found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wikimedia/Flickr (remote) → banner pipeline | Untrusted remote image bytes enter process_banner.py over HTTP |
| banner pipeline → Storage | Processed banner crosses into the production politician_photos bucket |
| frontend edits → live app | coverage.js / buildingImages.js changes ship to Render on push |

## STRIDE Threat Register

Honest scope: a licensed static image + two static frontend appends. Genuine threats are payload-in-image,
license provenance, and a dishonest/mis-placed coverage chip — not injection or access control. No
HIGH-severity threat; none fabricated.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-BAN | Tampering | remote banner image fetch | mitigate | process_banner.py re-encodes to JPEG (strips embedded payloads); operator visual QA confirms a real non-AI non-aerial photo before wiring |
| T-196-LIC | Repudiation | banner license provenance | mitigate | Subject \| author \| license recorded in the SUMMARY + the attribution comment; only CC/public-domain sources used |
| T-196-COVLIE | Tampering | dishonest coverage chip | mitigate | hasContext:true only set because Plan 03 landed ≥1 stance row (depends_on 196-03); node parse-check confirms valid module |
| T-196-COVBLK | Tampering | wrong coverage array (duplicate AZ block / append to COUNTIES) | mitigate | APPEND to the EXISTING Arizona COVERAGE_STATES block (Pitfall 8); grep asserts exactly ONE `name: 'Arizona'` block; COVERAGE_COUNTIES Pima entry untouched |
| T-196-KEY | Information disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key read only from gitignored `.env`; never in a committed frontend file |
| T-196-SC | Tampering | package installs | accept | No new installs — reuses installed PIL/requests banner toolchain; RESEARCH § Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- cities/marana.jpg returns HTTP 200 at the banner spec (1700×540 @ 3.15:1); operator-approved as a real, distinct, non-Catalina Marana photo.
- CURATED_LOCAL `marana` → banner URL; 'Marana' area APPENDED to the EXISTING Arizona COVERAGE_STATES block (exactly one Arizona block); COVERAGE_COUNTIES untouched.
- Both frontend files parse, buildingImages.test.js green, both committed.
- A real Marana address returns the Mayor + all 6 Council Members from the live search API.
</verification>

<success_criteria>
SUB-02 / ROADMAP #4 (licensed banner sourced/processed/uploaded/wired) and #5 (Marana surfaced with a
DB-honest chip appended to the existing Arizona block) are TRUE; a real Marana address resolves to the
seeded Mayor + 6 at-large council members end-to-end.
</success_criteria>

<output>
Create `.planning/phases/196-marana-deep-seed/196-04-SUMMARY.md` when done — include the banner subject |
author | license, the CDN URL, and the end-to-end address-routing result (address used + offices returned).
</output>
