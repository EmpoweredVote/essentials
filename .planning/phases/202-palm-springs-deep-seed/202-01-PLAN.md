---
phase: 202-palm-springs-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-palmsprings-council-boundaries.ts
autonomous: true
requirements: [CV-02]
must_haves:
  truths:
    - "5 Palm Springs City Council district LOCAL geofences (mtfcc='X0022', state='ca') are loaded into essentials.geofence_boundaries, one per district 1-5"
    - "Each geofence geometry is valid WGS84 (SRID 4326) in the Palm-Springs range (lon ~-116.5, lat ~33.8), NOT a native state-plane projection"
    - "The geofence source is the live Palm_Springs_Voting_Districts_2022_(View) FeatureServer (2021 'Map L' boundaries), NOT the stale 2018 ArcGIS Experience app draft"
    - "The 5 geofences gate (pre-flight >=5) the structural migration in Plan 02 — without them Plan 02 must refuse to apply"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-palmsprings-council-boundaries.ts"
      provides: "ArcGIS f=geojson loader for the 5 council districts, parameterized INSERT ON CONFLICT (geo_id, mtfcc) DO NOTHING"
      contains: "X0022"
  key_links:
    - from: "load-palmsprings-council-boundaries.ts"
      to: "services.arcgis.com Palm_Springs_Voting_Districts_2022_(View)/FeatureServer/0"
      via: "f=geojson + outSR=4326 query, JSON.stringify(feature.geometry) pass-through"
      pattern: "outSR=4326"
    - from: "load-palmsprings-council-boundaries.ts"
      to: "essentials.geofence_boundaries"
      via: "parameterized INSERT with ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))"
      pattern: "ON CONFLICT \\(geo_id, mtfcc\\) DO NOTHING"
---

<objective>
Author and run the one-time ETL loader that sources the 5 official Palm Springs City Council district
boundaries from the city GIS-maintained ArcGIS FeatureServer
(`Palm_Springs_Voting_Districts_2022_(View)/FeatureServer/0`, queried with `f=geojson`) and inserts
them as custom LOCAL geofences (`mtfcc='X0022'`, `state='ca'`, geo_id
`palm-springs-ca-council-district-1..5`). These 5 geofences are the gating dependency for true
per-councilmember address routing — the structural migration in Plan 02 refuses to apply until they
exist.

Purpose: Satisfies the geofence half of ROADMAP success criterion #2 (5 council-district X-geofences;
a probe address in each routes to exactly one member) for CV-02. RESEARCH verified this endpoint
returns a GeoJSON FeatureCollection directly (5 valid Polygon features, `DISTRICT` attribute string
"1".."5"), superseding CONTEXT.md's stale 2018 Experience-app pointer.
Output: `C:/EV-Accounts/backend/scripts/load-palmsprings-council-boundaries.ts`, committed to the
`C:/EV-Accounts` repo and run by the orchestrator so 5 X0022 rows exist in production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-palm-springs-deep-seed/202-CONTEXT.md
@.planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md
@.planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md

<execution_note>
`gsd-executor` has NO Supabase MCP / DB / GIS-network access. Task 1 (author the loader) is the only
executor file action. Task 2 is ORCHESTRATOR-RUN: the inline orchestrator runs `npx tsx` against
`C:/EV-Accounts/backend/.env` `DATABASE_URL` and asserts the result. All backend files live in the
SEPARATE `C:/EV-Accounts` repo (`master`→Render) — commit via `git -C "C:/EV-Accounts"`.
</execution_note>

<interfaces>
Key contracts (RESEARCH-confirmed this session + 202-PATTERNS.md loader-pattern block):
- Endpoint (authoritative, verified working): `https://services.arcgis.com/f48yV21HSEYeCYMI/arcgis/rest/services/Palm_Springs_Voting_Districts_2022_%28View%29/FeatureServer/0/query`
  with `where=1%3D1`, `outFields=*`, `returnGeometry=true`, `f=geojson`, `outSR=4326`. Returns a
  GeoJSON FeatureCollection DIRECTLY (no ArcGIS-rings conversion). The URL-encoded parentheses
  `%28View%29` are REQUIRED — the literal `(View)` is part of the REST path segment. `outSR=4326`
  MANDATORY (native SRID is a state-plane projection). Expected feature count = 5.
- `DISTRICT` attribute is a STRING ("1".."5") — parse with `parseInt(String(rawDist ?? ''), 10)` and
  range-reject (1-5) before forming geo_id. A `CouncilName` attribute is also present (Garner D1 /
  Bernstein D2 / deHarte D3 / Soto D4 / Ready D5) — LOG it as a free roster cross-check only; never
  use it to form geo_id.
- Primary analog (copy shape verbatim): `C:/EV-Accounts/backend/scripts/load-riverside-supervisor-boundaries.ts`
  — same `f=geojson` fetch shape, same 5-district loader, `JSON.stringify(feature.geometry)`
  pass-through, `ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON($3),4326))` INSERT, `ST_MakeValid` repair
  fallback, EXPECTED_COUNT shortfall PAUSE.
- Config constants: MTFCC `'X0022'` (next unused after X0021/Riverside — DB-verify unused at execute
  time); STATE_CODE `'ca'` (lowercase — required for LOCAL routing); SOURCE
  `'services.arcgis.com-palm-springs-voting-districts-2022-featureserver0-2026'`; GEO_ID_PREFIX
  `'palm-springs-ca-council-district-'`; EXPECTED_COUNT 5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Palm Springs council-district geofence loader</name>
  <files>C:/EV-Accounts/backend/scripts/load-palmsprings-council-boundaries.ts</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (lines 37-89 — the full loader pattern: config block, f=geojson + %28View%29 URL, DISTRICT string-parse + range-reject, CouncilName cross-check log, verbatim parameterized INSERT + ST_MakeValid guard, EXPECTED_COUNT abort rule)
    - .planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md (Pattern 2 + Code Examples — the confirmed working ArcGIS query; Pitfall 1 stale-map warning; Pitfall 2 DISTRICT-is-a-string)
    - C:/EV-Accounts/backend/scripts/load-riverside-supervisor-boundaries.ts (PRIMARY analog — read in FULL: f=geojson fetch, JSON.stringify(feature.geometry) pass-through, districtMap, parameterized INSERT with ST_Multi(ST_SetSRID(...)), ST_MakeValid fallback, EXPECTED_COUNT shortfall exit — copy this shape verbatim)
  </read_first>
  <action>
    Copy `load-riverside-supervisor-boundaries.ts` and adapt. Set the query URL to the Palm Springs
    endpoint `https://services.arcgis.com/f48yV21HSEYeCYMI/arcgis/rest/services/Palm_Springs_Voting_Districts_2022_%28View%29/FeatureServer/0/query`
    with `where=1%3D1`, `outFields=*`, `returnGeometry=true`, `f=geojson`, and `outSR=4326`. Keep the
    URL-encoded `%28View%29` exactly — it is part of the REST path. `outSR=4326` is MANDATORY (without
    it coordinates return in the native state-plane SRID and store as garbage that never matches an
    address). Keep `f=geojson` — do NOT use `f=json` and do NOT introduce any ring-winding helper
    (this FeatureServer returns GeoJSON directly, RESEARCH-confirmed). Do NOT point at the 2018 ArcGIS
    Experience app (`experience.arcgis.com/experience/1a70cef8...`) — it references a draft/superseded
    map (RESEARCH Pitfall 1).

    Set constants: MTFCC='X0022', STATE_CODE='ca' (lowercase), SOURCE=
    'services.arcgis.com-palm-springs-voting-districts-2022-featureserver0-2026', GEO_ID_PREFIX=
    'palm-springs-ca-council-district-', EXPECTED_COUNT=5.

    District-number extraction: read `feature.properties.DISTRICT` (confirmed field name, STRING
    "1".."5"). Parse defensively with `parseInt(String(rawDist ?? ''), 10)` and reject any value that
    isNaN or is out of range 1-5 BEFORE forming geo_id (skip + warn, never accept silently). Also read
    `feature.properties.CouncilName` and `console.log` it per district as a free cross-check against
    the roster (Garner D1 / Bernstein D2 / deHarte D3 / Soto D4 / Ready D5) — do NOT use CouncilName to
    build geo_id. Build geo_id per feature as `${GEO_ID_PREFIX}${districtNumber}` and name as
    `Palm Springs City Council District ${districtNumber}`.

    Reuse the parameterized INSERT into essentials.geofence_boundaries (columns id, geo_id, mtfcc,
    state, name, geometry, source) with `public.ST_Multi(public.ST_SetSRID(public.ST_GeomFromGeoJSON($3),
    4326))` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING RETURNING public.ST_GeometryType(geometry),
    public.ST_IsValid(geometry)`. All GIS attribute/geometry values MUST be passed as bind params
    ($1 geo_id / $2 name / $3 geomJson via JSON.stringify(feature.geometry) / $4 source) — never
    string-concatenated into SQL (T-202-SQLI). Keep a conditional `ST_MakeValid` re-run guard as
    defensive insurance for any invalid/self-intersecting multipolygon; if `ST_IsValid` still fails
    post-repair, reject that feature rather than loading it.

    Assert EXPECTED_COUNT: if the fetch returns fewer than 5 valid districts, print the shortfall and
    exit non-zero (PAUSE+flag — never silently load a partial set). Print a summary line per district
    (district number, geo_id, geometry type, ST_IsValid, first-coordinate sanity value) so the
    orchestrator can confirm the Palm-Springs WGS84 range in Task 2.

    Do NOT touch essentials.districts or any government/office table — this loader ONLY populates
    geofence_boundaries. The pre-existing whole-city TIGER boundary (geo_id='0655254', mtfcc 'G4110',
    state '06') is untouched.
  </action>
  <verify>
    <automated>F="C:/EV-Accounts/backend/scripts/load-palmsprings-council-boundaries.ts"; test -f "$F" && grep -qF "outSR=4326" "$F" && grep -qF "f=geojson" "$F" && grep -qF "X0022" "$F" && grep -qF "%28View%29" "$F" && grep -qF "ON CONFLICT (geo_id, mtfcc) DO NOTHING" "$F" && grep -qF "palm-springs-ca-council-district-" "$F" && ! grep -qE "arcgisRingsToGeoJson|f=json|1a70cef8|X0021|X0020" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/scripts/load-palmsprings-council-boundaries.ts` exists.
    - Grep confirms `outSR=4326`, `f=geojson`, and `%28View%29` present in the query URL; NO `f=json`, NO ring-winding helper, NO reference to the 2018 Experience app id `1a70cef8`.
    - Grep confirms `X0022` present; NEITHER `X0021` NOR `X0020` appears anywhere.
    - STATE_CODE is lowercase `'ca'` (no uppercase `'CA'` used as the geofence state value).
    - Parameterized INSERT with `ST_GeomFromGeoJSON($3)` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING` present — no string concatenation of GIS data into SQL.
    - geo_id prefix `palm-springs-ca-council-district-` present; `DISTRICT` parsed via parseInt with range-reject 1-5; `CouncilName` logged as cross-check only.
    - Conditional `ST_MakeValid` guard present; EXPECTED_COUNT=5 with a non-zero exit on shortfall.
  </acceptance_criteria>
  <done>Loader authored: fetches 5 districts via f=geojson+outSR=4326 from the confirmed 2022 FeatureServer (GeoJSON pass-through, no rings conversion), inserts 5 X0022/lowercase-ca geofences idempotently with parameterized binds, pauses on shortfall.</done>
</task>

<task type="auto">
  <name>Task 2: Run the loader + assert 5 X0022 geofences (ORCHESTRATOR-RUN)</name>
  <files>N/A — orchestrator runs the loader + read-only DB assertions; no files modified</files>
  <read_first>
    - .planning/phases/202-palm-springs-deep-seed/202-PATTERNS.md (line 58 — X0022 DB-verify-unused note; line 62 — EXPECTED_COUNT=5; lines 412-420 — orchestrator-run vs executor-authored split)
    - .planning/phases/202-palm-springs-deep-seed/202-RESEARCH.md (Pitfall 4 — DB-verify the next X-code before authoring; Open Question 1)
  </read_first>
  <action>
    ORCHESTRATOR-RUN (executor has no DB/network access). First DB-verify X0022 is genuinely unused
    (`SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0022'` = 0 before load); if it
    is claimed by an interim phase (196-198 AZ / 203 Indio may run first), assign the next free X-code,
    update the loader constant, and note the deviation. Then `git -C "C:/EV-Accounts" add` + commit the
    loader. Run `npx tsx backend/scripts/load-palmsprings-council-boundaries.ts` from the
    `C:/EV-Accounts/backend` checkout (uses `.env` `DATABASE_URL`). Confirm the script exits 0 with 5
    rows inserted (or already-present via ON CONFLICT), and that the logged per-district `CouncilName`
    values match Garner D1 / Bernstein D2 / deHarte D3 / Soto D4 / Ready D5. Then assert via
    `psql "$DATABASE_URL" -tAc`: (a) exactly 5 rows in essentials.geofence_boundaries WHERE
    mtfcc='X0022' AND state='ca'; (b) all 5 geo_ids match `palm-springs-ca-council-district-1..5`;
    (c) all 5 geometries valid (ST_IsValid=true) and their centroid longitude is between -116.7 and
    -116.3 and latitude between 33.7 and 34.0 (Palm-Springs WGS84 sanity, confirming outSR=4326
    worked). If any assertion fails, PAUSE and flag — do NOT proceed to Plan 02. Record the 5 geo_id →
    ST_IsValid → centroid → CouncilName cross-check results in 202-01-SUMMARY.md, and record the
    actual X-code used (should be X0022 unless reassigned).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0022' AND state='ca')=5 AND NOT EXISTS (SELECT 1 FROM essentials.geofence_boundaries WHERE mtfcc='X0022' AND NOT public.ST_IsValid(geometry)) AND (SELECT bool_and(public.ST_X(public.ST_Centroid(geometry)) BETWEEN -116.7 AND -116.3 AND public.ST_Y(public.ST_Centroid(geometry)) BETWEEN 33.7 AND 34.0) FROM essentials.geofence_boundaries WHERE mtfcc='X0022')"</automated>
  </verify>
  <acceptance_criteria>
    - X0022 confirmed unused before load (or the actual reassigned code recorded); loader exits 0; 5 X0022 rows present after the run.
    - `psql -tAc` count of X0022/state='ca' geofences = 5.
    - All 5 geo_ids are exactly `palm-springs-ca-council-district-1` through `-5`.
    - All 5 geometries valid; every centroid lon∈(-116.7,-116.3) and lat∈(33.7,34.0) (WGS84, no state-plane garbage).
    - Logged per-district CouncilName matches the roster (Garner/Bernstein/deHarte/Soto/Ready).
    - The combined boolean SELECT returns `t`. Loader committed to `C:/EV-Accounts` via `git -C`.
  </acceptance_criteria>
  <done>5 valid WGS84 council-district geofences (X0022, lowercase ca) are live in production and roster-cross-checked, gating Plan 02's structural migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Palm Springs ArcGIS FeatureServer (remote) → loader | Untrusted GeoJSON geometry + attribute strings enter the loader over HTTP |
| loader → production PostGIS | Converted geometry crosses into live `essentials.geofence_boundaries` via `npx tsx` (service-role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-01 | Tampering | malformed/self-intersecting ArcGIS geometry → INSERT | mitigate | `ST_MakeValid` repair fallback; reject the feature if `ST_IsValid` still fails post-repair (never load an invalid polygon) |
| T-202-SQLI | Tampering | GIS attribute/geometry → INSERT | mitigate | Parameterized binds ($1 geo_id / $2 name / $3 GeoJSON / $4 source) — never string-concatenate GIS response data into SQL |
| T-202-PROJ | Tampering | native state-plane SRID vs WGS84 | mitigate | `outSR=4326` mandatory in the query; Task 2 asserts centroid ∈ Palm-Springs WGS84 range or PAUSES |
| T-202-STALE | Tampering | stale 2018 district map → wrong boundaries | mitigate | Use only the confirmed 2022 `(View)` FeatureServer (2021 Map L); reject the 2018 Experience app; log CouncilName per district as a roster cross-check |
| T-202-SC | Tampering | npm/tsx installs | accept | Zero new package installs this phase — loader reuses already-installed `pg`/`tsx` (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- 5 X0022 / state='ca' geofences present, all valid WGS84 in the Palm-Springs centroid range.
- geo_ids exactly `palm-springs-ca-council-district-1..5`.
- Per-district CouncilName cross-checks the current roster.
- Loader committed to `C:/EV-Accounts`; pre-existing city boundary 0655254 untouched.
</verification>

<success_criteria>
5 valid council-district geofences (X0022, lowercase ca) live in production, gating Plan 02.
</success_criteria>

<output>
Create `.planning/phases/202-palm-springs-deep-seed/202-01-SUMMARY.md` when done (record the 5 geo_id →
ST_IsValid → centroid → CouncilName results and the actual X-code used).
</output>
