---
phase: 74-or-executives-federal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql
autonomous: true
requirements: []
tags: [postgres, migration, supabase, oregon, executives, state-exec]

must_haves:
  truths:
    - "All 5 OR constitutional officers (Kotek, Rayfield, Read, Steiner, Stephenson) are seeded in essentials.politicians with external_ids -4100001..-4100005"
    - "All 5 OR constitutional officers have office rows linked to a STATE_EXEC district (geo_id='41', label='Oregon (Statewide)', state='or')"
    - "All 5 office rows have is_appointed_position=false (all voter-elected)"
    - "Each office is linked to the correct chamber via name subquery against essentials.chambers (Governor, Attorney General, Secretary of State, State Treasurer, Labor Commissioner)"
    - "Migration 223 is idempotent — re-run produces no duplicate politicians, offices, or districts"
    - "office_id is back-filled on all 5 politicians.office_id rows"
    - "Secretary of State row uses Tobias Read (NOT Griffin-Valade)"
    - "Treasurer row uses 'Elizabeth Steiner' (NOT 'Elizabeth Steiner Hayward')"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql"
      provides: "Migration 223: 1 STATE_EXEC district + 5 OR exec politicians + 5 offices + office_id back-fill"
      contains: "STATE_EXEC"
  key_links:
    - from: "essentials.offices (5 rows)"
      to: "essentials.districts (STATE_EXEC, geo_id='41')"
      via: "district_id FK"
      pattern: "district_type = 'STATE_EXEC' AND geo_id = '41'"
    - from: "essentials.offices (5 rows)"
      to: "essentials.chambers (Governor/AG/SoS/Treasurer/Labor Commissioner)"
      via: "chamber_id FK resolved by name subquery"
      pattern: "name = '...' AND government_id = (SELECT id FROM essentials.governments WHERE name = 'State of Oregon')"
    - from: "essentials.politicians.office_id"
      to: "essentials.offices.id"
      via: "UPDATE back-fill scoped to external_id range"
      pattern: "external_id BETWEEN -4100099 AND -4100001"
---

<objective>
Seed Oregon's 5 voter-elected constitutional officers (Governor Tina Kotek, AG Dan Rayfield, SoS Tobias Read, Treasurer Elizabeth Steiner, Labor Commissioner Christina Stephenson) as essentials.politicians, create the shared STATE_EXEC district (geo_id='41', label='Oregon (Statewide)'), and link each politician to a chamber+district via an essentials.offices row.

Purpose: This unblocks Phase 74-03 (executive headshots) and is a prerequisite for any address-lookup view that surfaces OR statewide elected officials. Phase 73 created the 5 OR exec chambers but they currently have no offices.

Output: Migration 223 (applied via Supabase) containing 1 district INSERT, 5 politician INSERTs, 5 office INSERTs, and 1 office_id back-fill UPDATE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-or-executives-federal/74-RESEARCH.md
@.planning/phases/73-or-government-db/73-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks (verify external_id range clear, no duplicate names, STATE_EXEC district absent, chambers present)</name>
  <files>(read-only; no file modifications)</files>
  <read_first>
    - .planning/phases/74-or-executives-federal/74-RESEARCH.md (Officials Roster + Pre-flight section + Assumptions Log)
    - .planning/STATE.md (lines on "politician_images.type must be 'default'", essentials.governments lacks unique on geo_id, slug GENERATED ALWAYS, [GOTCHA] discovery agent creates external_id=NULL rows)
    - .planning/phases/73-or-government-db/73-01-SUMMARY.md (chamber lookup pattern, State of Oregon government UUID)
  </read_first>
  <action>
    Use mcp__supabase-local__execute_sql to run 4 pre-flight queries against the live DB. Block migration writing until ALL 4 pass:

    Query 1 — external_id range clear:
    SELECT external_id, full_name FROM essentials.politicians WHERE external_id BETWEEN -4100099 AND -4100001 ORDER BY external_id;
    Expected: 0 rows. If any row exists, STOP and report findings — range may need adjustment.

    Query 2 — duplicate name pre-check:
    SELECT full_name, external_id FROM essentials.politicians WHERE full_name IN ('Tina Kotek','Dan Rayfield','Tobias Read','Elizabeth Steiner','Christina Stephenson') ORDER BY full_name;
    Expected: 0 rows. If any rows exist, document them in the task report — migration must use UPDATE for those rows instead of INSERT.

    Query 3 — STATE_EXEC district for OR absent (or exactly the row we plan to create):
    SELECT id, geo_id, label, state FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='or';
    Expected: 0 rows OR exactly 1 row with geo_id='41' and label='Oregon (Statewide)'. The migration uses WHERE NOT EXISTS so a pre-existing match is non-fatal but must be documented.

    Query 4 — all 5 OR exec chambers exist under State of Oregon (sanity-check Phase 73 output):
    SELECT name FROM essentials.chambers WHERE government_id = (SELECT id FROM essentials.governments WHERE name='State of Oregon' AND state='OR') AND name IN ('Governor','Attorney General','Secretary of State','State Treasurer','Labor Commissioner') ORDER BY name;
    Expected: 5 rows. If fewer than 5, Phase 73 is incomplete — STOP and surface gap.

    Record all 4 query results verbatim in the task commit message for audit trail. Do NOT proceed to Task 2 unless all 4 are satisfied.
  </action>
  <verify>
    <automated>All 4 pre-flight SQL queries return expected row counts (0, 0, 0-or-1, 5)</automated>
  </verify>
  <acceptance_criteria>
    - Query 1 returns 0 rows (external_id range -4100099..-4100001 is clear)
    - Query 2 returns 0 rows (no name collisions for the 5 exec officers)
    - Query 3 returns 0 rows OR 1 row matching (geo_id='41', label='Oregon (Statewide)', state='or')
    - Query 4 returns exactly 5 rows: Attorney General, Governor, Labor Commissioner, Secretary of State, State Treasurer
    - Each query's result is included in the task commit message
  </acceptance_criteria>
  <done>All 4 pre-flight queries pass; results recorded; ready to write migration 223.</done>
</task>

<task type="auto">
  <name>Task 2: Write and apply migration 223 (1 STATE_EXEC district + 5 OR exec politicians + 5 offices + office_id back-fill)</name>
  <files>C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/222_or_government_chambers.sql (governments subquery pattern, BEGIN/COMMIT structure)
    - C:/EV-Accounts/backend/migrations/170_me_federal_officials.sql (CTE-based INSERT-with-RETURNING + offices pattern, office_id back-fill, WHERE NOT EXISTS on offices)
    - .planning/phases/74-or-executives-federal/74-RESEARCH.md (External ID Scheme, Officials Roster, Migration 223 plan in District Structure section, Gotchas)
    - .planning/STATE.md (essentials.districts column is 'label', is_appointed_position on OFFICE row, slug GENERATED ALWAYS on chambers, essentials.governments lacks unique on geo_id pattern)
  </read_first>
  <action>
    Create C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql using migration 170 as the structural template and migration 222 as the OR-specific pattern reference. The migration must:

    1. Wrap entire body in BEGIN; ... COMMIT;
    2. Include a leading comment block stating: purpose (5 OR constitutional officers + 1 STATE_EXEC district), migration number = 223, idempotency (all guards), is_appointed_position=false on ALL 5 offices (per D-03 from migration 222 comments — all OR statewide officers are voter-elected), SoS is Tobias Read (NOT Griffin-Valade), Treasurer is 'Elizabeth Steiner' (NOT 'Elizabeth Steiner Hayward').

    3. STEP 1 — Pre-flight DO $$ block: assert State of Oregon government row exists exactly once with state='OR' (mirror the pattern in migration 222 lines 42-52).

    4. STEP 2 — Insert STATE_EXEC district. Columns: id (gen_random_uuid), district_type='STATE_EXEC', state='or' (lowercase per ME precedent for STATE_* rows), geo_id='41', label='Oregon (Statewide)', district_id='Oregon (Statewide)', mtfcc=''. Guard with WHERE NOT EXISTS on (district_type='STATE_EXEC' AND state='or' AND geo_id='41').

    5. STEP 3 — For each of the 5 executives, use the CTE pattern from migration 170 (WITH ins_p AS (INSERT...ON CONFLICT (external_id) DO NOTHING RETURNING id) INSERT INTO offices SELECT ... FROM districts d CROSS JOIN ins_p p WHERE ... AND NOT EXISTS ...). Politician columns: id, full_name, first_name, last_name, party, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id. Office columns: id, district_id (FROM the STATE_EXEC district), chamber_id (FROM essentials.chambers WHERE name='X' AND government_id = (SELECT id FROM essentials.governments WHERE name='State of Oregon' AND state='OR')), politician_id, title, representing_state='OR', is_appointed_position=false, is_vacant=false, role_canonical=NULL.

       Use these EXACT values (from 74-RESEARCH.md):

       | external_id | full_name | first_name | last_name | party | chamber name | office title |
       | -4100001 | Tina Kotek | Tina | Kotek | Democrat | Governor | Governor |
       | -4100002 | Dan Rayfield | Dan | Rayfield | Democrat | Attorney General | Attorney General |
       | -4100003 | Tobias Read | Tobias | Read | Democrat | Secretary of State | Secretary of State |
       | -4100004 | Elizabeth Steiner | Elizabeth | Steiner | Democrat | State Treasurer | State Treasurer |
       | -4100005 | Christina Stephenson | Christina | Stephenson | Democrat | Labor Commissioner | Labor Commissioner |

       Office NOT EXISTS guard: use (district_id, chamber_id) — each executive is the sole occupant of their chamber so no senator-style collision concern; but spell out the WHERE NOT EXISTS clause on essentials.offices o WHERE o.district_id = d.id AND o.chamber_id = c.id for safety.

    6. STEP 4 — office_id back-fill: UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -4100099 AND -4100001 AND p.office_id IS NULL;

    7. NEVER include a slug column on chambers (it is GENERATED ALWAYS — but this migration does not INSERT chambers, so this only matters if extending).

    8. Apply the migration via mcp__supabase-local__apply_migration with name='223_or_executive_officials' (no .sql extension on Supabase migration name).
  </action>
  <verify>
    <automated>mcp__supabase-local__apply_migration returns success; immediate post-apply SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -4100005 AND -4100001 returns 5</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql exists with BEGIN/COMMIT wrapper
    - File contains exact substring "Tobias Read" and does NOT contain "Griffin-Valade" anywhere
    - File contains exact substring "Elizabeth Steiner" and does NOT contain "Hayward" anywhere
    - File contains exact substring "is_appointed_position=false" or "is_appointed_position, is_vacant" with constant value false for ALL 5 office INSERTs (no is_appointed_position=true anywhere in this migration)
    - File contains exactly one INSERT INTO essentials.districts statement (the STATE_EXEC district)
    - File contains 5 distinct INSERT INTO essentials.politicians statements with external_ids -4100001 through -4100005
    - Migration applied successfully via mcp__supabase-local__apply_migration
    - SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -4100005 AND -4100001 returns 5
    - SELECT COUNT(*) FROM essentials.offices o JOIN essentials.politicians p ON p.id=o.politician_id WHERE p.external_id BETWEEN -4100005 AND -4100001 returns 5
    - SELECT COUNT(*) FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='or' AND geo_id='41' returns 1
  </acceptance_criteria>
  <done>Migration 223 file written and applied; 5 politicians + 5 offices + 1 STATE_EXEC district present in DB.</done>
</task>

<task type="auto">
  <name>Task 3: Post-apply verification (count gate, smoke test, section-split detector, idempotency check)</name>
  <files>(read-only verification via mcp__supabase-local__execute_sql)</files>
  <read_first>
    - .planning/phases/74-or-executives-federal/74-RESEARCH.md (Validation Architecture section — smoke test SQL is specified there)
    - C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql (the just-applied migration, to confirm content matches)
  </read_first>
  <action>
    Run 4 verification queries via mcp__supabase-local__execute_sql and assert each result matches the expected output. If any assertion fails, surface the failure with the exact SQL output before continuing.

    Verification 1 — full roster smoke test (from 74-RESEARCH.md Validation Architecture):
    SELECT p.external_id, p.full_name, c.name AS chamber, d.district_type, d.geo_id, o.is_appointed_position
    FROM essentials.politicians p
    JOIN essentials.offices o ON o.politician_id = p.id
    JOIN essentials.chambers c ON c.id = o.chamber_id
    JOIN essentials.districts d ON d.id = o.district_id
    WHERE p.external_id BETWEEN -4100005 AND -4100001
    ORDER BY p.external_id DESC;
    Expected: 5 rows; all district_type='STATE_EXEC'; all geo_id='41'; all is_appointed_position=false; chambers ordered: Governor (-4100001), Attorney General (-4100002), Secretary of State (-4100003), State Treasurer (-4100004), Labor Commissioner (-4100005).

    Verification 2 — office_id back-fill confirmed:
    SELECT COUNT(*) AS unfilled FROM essentials.politicians WHERE external_id BETWEEN -4100005 AND -4100001 AND office_id IS NULL;
    Expected: 0.

    Verification 3 — section-split detector (orphaned geofences without districts):
    SELECT gb.geo_id, gb.mtfcc, COUNT(DISTINCT d.id) AS district_count
    FROM essentials.geofence_boundaries gb
    LEFT JOIN essentials.districts d ON d.geo_id = gb.geo_id
    WHERE gb.state = '41'
    GROUP BY gb.geo_id, gb.mtfcc
    HAVING COUNT(DISTINCT d.id) != 1
    ORDER BY gb.geo_id;
    Expected: 0 rows.

    Verification 4 — idempotency check: re-apply migration 223 (mcp__supabase-local__apply_migration with same name will fail or no-op; use mcp__supabase-local__execute_sql to run the file contents again). Then re-run Verification 1 and confirm still exactly 5 rows. If Supabase blocks duplicate migration names, run the raw SQL inside a transaction and confirm INSERT 0 0 / UPDATE 0 across all statements.

    Document all 4 verification results in the task commit message.
  </action>
  <verify>
    <automated>All 4 verification SQL outputs match expected (5 STATE_EXEC rows, 0 unfilled office_ids, 0 section-split rows, idempotent re-run)</automated>
  </verify>
  <acceptance_criteria>
    - Verification 1 returns exactly 5 rows; every row has district_type='STATE_EXEC', geo_id='41', is_appointed_position=false
    - Verification 1 chamber name distribution: 1 Governor, 1 Attorney General, 1 Secretary of State, 1 State Treasurer, 1 Labor Commissioner (verified by chamber column)
    - Verification 1 full_name for external_id=-4100003 is exactly 'Tobias Read' (not 'LaVonne Griffin-Valade')
    - Verification 1 full_name for external_id=-4100004 is exactly 'Elizabeth Steiner' (not containing 'Hayward')
    - Verification 2 returns 0 (all 5 office_id columns back-filled)
    - Verification 3 returns 0 rows (no orphaned OR geofences after migration)
    - Verification 4 idempotency: re-running the migration body produces no new rows; Verification 1 still returns 5
  </acceptance_criteria>
  <done>All 4 verification gates passed; results recorded in commit; Phase 74 Wave 1 contribution (exec side) complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> production DB | Migration 223 is applied to live Supabase Postgres via mcp__supabase-local (which IS the remote/production DB per STATE.md). Any malformed SQL or wrong identifier writes directly to live data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering | essentials.politicians (5 new rows) | mitigate | ON CONFLICT (external_id) DO NOTHING guards prevent overwriting existing rows; pre-flight Task 1 query confirms range is clear before write |
| T-74-02 | Tampering | essentials.districts (1 new STATE_EXEC row) | mitigate | WHERE NOT EXISTS guard on (district_type, state, geo_id) — re-runs do not duplicate |
| T-74-03 | Information Disclosure | Stale SoS data (Griffin-Valade) | mitigate | Migration explicitly seeds 'Tobias Read'; verification Task 3 asserts external_id=-4100003 has full_name='Tobias Read' |
| T-74-04 | Denial of Service | Missing chamber dependency from Phase 73 | mitigate | Task 1 pre-flight Query 4 fails fast if any of the 5 exec chambers missing under State of Oregon |
| T-74-05 | Repudiation | Audit trail for live DB writes | accept | Supabase migration ledger records 223 with timestamp; verification results committed to git via task commit message |
</threat_model>

<verification>
- 5 OR exec politicians visible in essentials.politicians with external_ids -4100005..-4100001
- 5 office rows in essentials.offices linked to STATE_EXEC district (geo_id='41') and the 5 OR exec chambers
- 1 STATE_EXEC district row in essentials.districts (geo_id='41', label='Oregon (Statewide)', state='or')
- is_appointed_position=false on ALL 5 office rows
- All 5 politicians.office_id columns back-filled (none NULL)
- Section-split detector returns 0 rows for state='41'
- Migration 223 file exists at C:/EV-Accounts/backend/migrations/223_or_executive_officials.sql
- Migration 223 is idempotent (re-run produces no new rows)
- SoS is Tobias Read (not Griffin-Valade); Treasurer is Elizabeth Steiner (not Hayward)
</verification>

<success_criteria>
- Phase 74 roadmap criterion #1 partially satisfied: 5 OR constitutional officers seeded (Kotek, Rayfield, Read, Steiner, Stephenson) — corrected per research to use Read instead of Griffin-Valade
- Foundation laid for Phase 74-03 headshot upload (politicians exist with stable politician_id UUIDs)
- Independent of Phase 74-02 (federal) — both can run in parallel as Wave 1
</success_criteria>

<output>
Create `.planning/phases/74-or-executives-federal/74-01-SUMMARY.md` when done. Required SUMMARY fields:
- Migration 223 status (applied successfully)
- 5 politician UUIDs (for Phase 74-03 headshot upload reference)
- STATE_EXEC district UUID and confirmation of geo_id='41'
- Full results of all 4 verification queries (Task 3)
- Confirmation of next available migration number (224, used by Plan 74-02)
</output>
