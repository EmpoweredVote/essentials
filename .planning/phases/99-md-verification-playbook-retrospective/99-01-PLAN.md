---
phase: 99-md-verification-playbook-retrospective
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md
autonomous: true
requirements:
  - UI-01
  - UI-02
  - MD-GEO-01
  - MD-GEO-02
  - MD-GEO-03
  - MD-GEO-04
  - MD-GEO-05
  - MD-GEO-06
  - MD-GOV-01
  - MD-GOV-02
  - MD-GOV-03
  - MD-GOV-04
  - MD-GOV-05
  - MD-GOV-06
  - MD-DEEP-01
  - MD-DEEP-02
  - MD-DEEP-03
  - MD-ELECTIONS-01
  - MD-ELECTIONS-02
  - MD-ELECTIONS-03
  - MD-STANCES-01
  - MD-STANCES-02
  - MD-STANCES-03
  - MD-STANCES-04
  - POST-ELECTION-01
  - POST-ELECTION-02
tags:
  - verification
  - milestone-close
  - maryland

must_haves:
  truths:
    - "All 26 v11.0 requirements have a fresh PASS or DEFER row in 99-01-VERIFICATION.md against production DB"
    - "REQUIREMENTS.md checkboxes show [x] for every requirement whose underlying data is confirmed satisfied (or [DEFER] note for Phase 90 items if 90-03-SUMMARY.md does not yet exist)"
    - "REQUIREMENTS.md Traceability table reads 'Complete' for every row whose checkbox is now [x]"
    - "STATE.md 'Next migration:' line reflects the actual SELECT MAX(version) FROM supabase_migrations.schema_migrations result + 1"
    - "Zero open gaps: every PASS row in 99-01-VERIFICATION.md cites a SELECT COUNT(*) value matching the prior VERIFICATION.md baseline"
  artifacts:
    - path: ".planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md"
      provides: "Final 26-row verification matrix with PASS/DEFER status, count value, baseline source"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated checkboxes + Traceability table for all 26 v11.0 requirements"
      contains: "[x] **MD-ELECTIONS-01**"
    - path: ".planning/STATE.md"
      provides: "Corrected migration counter (293 expected) + last_activity note for Phase 99 Plan 01"
      contains: "Next migration: 293"
  key_links:
    - from: ".planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md"
      to: ".planning/phases/9*/*-VERIFICATION.md"
      via: "baseline-source column cites the prior phase verification file"
      pattern: "Phase 9[1-8]"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md"
      via: "every [x] checkbox change traces to a PASS row in 99-01-VERIFICATION.md"
      pattern: "\\[x\\] \\*\\*(MD-|UI-|POST-)"
---

<objective>
Run the final v11.0 verification sweep: re-confirm all 26 requirements pass against production
Supabase by issuing one lightweight SELECT per requirement, update REQUIREMENTS.md checkboxes and
Traceability table to reflect actual state, and fix the stale STATE.md "Next migration" counter.

Purpose: Phase 99 is the cross-cutting milestone gate. Twenty-two of the 26 requirements were
already verified by their phase VERIFICATION.md files; this plan re-confirms them with a single
count query each (no full re-execution), then updates the documentation artifacts that downstream
reviewers and future planning runs read first. Without this plan, REQUIREMENTS.md would show 9+
stale `[ ]` boxes after v11.0 close and STATE.md would report `Next migration: 278` while the
production DB has migration 292 applied.

Output: A 26-row verification matrix at `99-01-VERIFICATION.md`, updated REQUIREMENTS.md (checkbox
+ Traceability + footer), and corrected STATE.md (Next migration line + last_activity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/99-md-verification-playbook-retrospective/99-RESEARCH.md
@.planning/phases/99-md-verification-playbook-retrospective/99-PATTERNS.md
@.planning/v11.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the 26-requirement verification sweep against production Supabase and write 99-01-VERIFICATION.md</name>
  <files>.planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/99-md-verification-playbook-retrospective/99-RESEARCH.md (Verification Query Strategy table — section "What the Planner Needs to Know" lists exact query type per requirement)
    - .planning/REQUIREMENTS.md (all 26 requirement IDs + current checkbox/traceability state)
    - .planning/phases/91-md-tiger-geofences/91-VERIFICATION.md (baseline counts for MD-GEO-01..06)
    - .planning/phases/92-md-state-government-db/92-VERIFICATION.md (baseline for MD-GOV-01/02)
    - .planning/phases/93-md-legislature-federal-officials/93-VERIFICATION.md (baseline for MD-GOV-03/04/05)
    - .planning/phases/94-md-headshots/94-VERIFICATION.md (baseline for MD-GOV-06; 0-gap query)
    - .planning/phases/95-leonardtown-st-mary-s-county-deep-seed/95-VERIFICATION.md (baseline for MD-DEEP-01/02/03)
    - .planning/phases/96-md-2026-elections-discovery-pipeline-landing/96-VERIFICATION.md (baseline for MD-ELECTIONS-01/02/03)
    - .planning/phases/97-md-compass-stances-executives-senators-wave-1/97-04-SUMMARY.md (baseline for MD-STANCES-01/02)
    - .planning/phases/98-md-compass-stances-house-delegates-wave-2/98-07-SUMMARY.md (baseline for MD-STANCES-03/04)
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-02-SUMMARY.md (baseline for UI-01/02)
    - .planning/phases/90-post-election-follow-up-minicompass-ui/90-03-PLAN.md (status of POST-ELECTION-01/02 — check whether 90-03-SUMMARY.md exists)
  </read_first>
  <action>
    Write `.planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md` with YAML
    front-matter `status: passed` (downgrade to `status: deferred` only if any Phase 90 row is in
    DEFER state) and a 26-row markdown table — one row per requirement ID — using these columns:

    | Requirement | Phase | Query / Reference | Expected | Observed | Baseline source | Status |

    For each row, issue exactly ONE verification action against production via mcp__supabase-local
    (or read the static file artifact for UI / Landing requirements). Use the existing baseline SQL
    from the cited prior VERIFICATION.md — do not invent new queries. Concrete query/reference per
    requirement:

      MD-GEO-01: SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state='24' AND mtfcc='G4110'; — expected 157
      MD-GEO-02: ... WHERE state='24' AND mtfcc='G4020'; — expected 24
      MD-GEO-03: ... WHERE state='24' AND mtfcc='G5210'; — expected 47
      MD-GEO-04: ... WHERE state='24' AND mtfcc='G5220'; — expected 71
      MD-GEO-05: ... WHERE state='24' AND mtfcc='G5200'; — expected 8
      MD-GEO-06: cite 91-VERIFICATION.md 3-address smoke-test PASS (no new query required) — Reference: "Phase 91 smoke-test PASSED"
      MD-GOV-01: SELECT COUNT(*) FROM essentials.chambers WHERE government_id IN (SELECT id FROM essentials.governments WHERE name='State of Maryland'); — expected 5; also confirm WHERE name='State Treasurer' AND is_appointed_position=true returns 1
      MD-GOV-02: SELECT COUNT(*) FROM inform.politician_images pi JOIN essentials.politicians p ON p.inform_politician_id = pi.politician_id WHERE p.external_id BETWEEN -240005 AND -240001 AND pi.type='default'; — expected 5
      MD-GOV-03: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2410047 AND -2410001; — expected 47
      MD-GOV-04: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -2420141 AND -2420001; — expected 141
      MD-GOV-05: SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-400033,-400034) + SELECT COUNT(*) WHERE external_id BETWEEN -2440008 AND -2440001; — expected 2 + 8 = 10
      MD-GOV-06: Re-run the 0-gap query from 94-VERIFICATION.md verbatim (the "non-vacant officials without type='default' image" SELECT; the same query also appears in 94-02-SUMMARY.md if the VERIFICATION.md cross-references); — expected 0 rows
      MD-DEEP-01: SELECT COUNT(*) FROM essentials.governments WHERE name LIKE 'St. Mary%County'; — expected 1; AND SELECT COUNT(*) FROM essentials.chambers WHERE government_id=(that govt id); — expected ≥1
      MD-DEEP-02: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -24037xxxx low and high (see migration 276 range in 95-01-SUMMARY.md); — expected 5
      MD-DEEP-03: SELECT COUNT(*) FROM essentials.governments WHERE name LIKE '%Leonardtown%'; — expected 1; AND count of Leonardtown politicians per 95-VERIFICATION.md (expected 6)
      MD-ELECTIONS-01: SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON r.election_id=e.id WHERE e.state='MD' AND EXTRACT(YEAR FROM e.election_date)=2026 AND e.election_type='general'; — expected 130 per 96-VERIFICATION.md
      MD-ELECTIONS-02: SELECT COUNT(*) FROM essentials.discovery_jurisdictions WHERE state='MD'; — expected 2
      MD-ELECTIONS-03: grep -n "Maryland\|Leonardtown" src/pages/Landing.jsx → assert MD entry present (Reference; no DB query)
      MD-STANCES-01: SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.inform_politician_id = pa.politician_id WHERE p.external_id BETWEEN -240005 AND -240001; — expected 5
      MD-STANCES-02: SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.inform_politician_id = pa.politician_id WHERE p.external_id BETWEEN -2410047 AND -2410001; — expected 47 (or 47 minus documented not-found per 97-04-SUMMARY.md)
      MD-STANCES-03: same shape, external_id BETWEEN -2420141 AND -2420001; — expected ≥140 per 98-07-SUMMARY.md Q-PHASE-1=140
      MD-STANCES-04: cite 98-07-UAT.md "5/6 UI profiles PASS" — Reference (no DB query). Note Brooks NOT-FOUND as INFO per RESEARCH.md Open Question 3 (RESOLVED) — does not block PASS.
      UI-01: cite 90-02-SUMMARY.md (dotRadius=2.5 wired + human-approved). If 90-02-SUMMARY.md not present → DEFER.
      UI-02: cite 90-02-SUMMARY.md (showLabels=false wired + human-approved). If 90-02-SUMMARY.md not present → DEFER.
      POST-ELECTION-01: cite 90-03-SUMMARY.md if exists (migration 272 applied with ME winners). If 90-03-SUMMARY.md missing → status: DEFER with note "Phase 90 Plan 03 not yet executed; verified by reference once SUMMARY exists. Does not block Phase 99 Plan 02 milestone close per RESEARCH.md Pitfall 4."
      POST-ELECTION-02: same — cite 90-03-SUMMARY.md or DEFER.

    For each row, populate Observed with the actual count returned and set Status to PASS if
    Observed equals Expected (or is within the documented not-found tolerance for stance rows), or
    DEFER for any Phase 90 row whose SUMMARY artifact is missing. NEVER mark a row FAIL silently —
    a true FAIL (count mismatch beyond not-found tolerance) must be flagged in front-matter as
    `status: failed` and listed in a `## Gaps` section with the discrepancy and proposed remediation.

    Also include a "Pre-Phase-99-close DB state" section at the top of the file capturing:
      - SELECT MAX(version) AS max_applied FROM supabase_migrations.schema_migrations; — record value
      - SELECT current_timestamp; — record run timestamp
      - Note whether 90-03-SUMMARY.md exists (read directly via file glob)

    Use the EXACT 99-01-VERIFICATION.md filename. Do NOT create 99-VERIFICATION.md (Phase 99 has
    plan-numbered verification files like Phase 91).
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate ".planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md" --schema verification 2>&1 | grep -E '"valid":\s*true' &amp;&amp; grep -c "^| MD-\|^| UI-\|^| POST-" ".planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md" | grep -E "^2[6-9]$|^[3-9][0-9]$"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md` exists
    - Front-matter contains `status: passed` (or `status: deferred` only if UI-01/UI-02/POST-ELECTION-01/POST-ELECTION-02 are in DEFER state)
    - The matrix table contains exactly 26 data rows — one per requirement ID listed in REQUIREMENTS.md v11.0 section
    - Every row has a non-empty Observed value AND a Baseline source citation (either a SELECT result or a `Phase NN-VERIFICATION.md` reference)
    - "Pre-Phase-99-close DB state" section records the actual `SELECT MAX(version)` value (numeric, expected 292)
    - No row has Status="FAIL" — if any FAIL is found, the front-matter `status` is `failed` and a `## Gaps` section lists every failed row with proposed remediation
    - File matches the pattern `^| (MD-|UI-|POST-)[A-Z0-9-]+ \|` for every requirement row (grep verifiable)
  </acceptance_criteria>
  <done>
    26-row verification matrix exists with PASS/DEFER status for every v11.0 requirement; DB state
    snapshot recorded; any gaps escalated to front-matter status and Gaps section. Downstream Plan
    99-02 can read this file to determine whether milestone close is unblocked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes, Traceability table, and footer to match 99-01-VERIFICATION.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — read lines 80-105 of the Traceability table BEFORE editing; per 99-RESEARCH.md the table lists MD-GOV-03/04/05 as "Pending" but the actual file may already show them as "Complete" — verify before flipping)
    - .planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md (output of Task 1 — source of truth for which boxes flip to [x])
    - .planning/phases/99-md-verification-playbook-retrospective/99-PATTERNS.md (section ".planning/REQUIREMENTS.md" lines 73-109 — exact replacement strings)
  </read_first>
  <action>
    **PRE-CHECK (run before any edits):** grep the current state of REQUIREMENTS.md for the
    Traceability table rows MD-GOV-03, MD-GOV-04, MD-GOV-05 and MD-STANCES-01..04. Per
    99-RESEARCH.md these were documented as "Pending" but the actual REQUIREMENTS.md may now read
    "Complete" (lines 89-91 and 99-102 in current state). Concretely:

        grep -nE "^\| MD-GOV-0[345] \|" .planning/REQUIREMENTS.md
        grep -nE "^\| MD-STANCES-0[1234] \|" .planning/REQUIREMENTS.md

    For each row whose current Status column is ALREADY "Complete", do NOT re-edit (idempotent). For
    each row whose Status is "Pending", flip to "Complete" only if the corresponding
    99-01-VERIFICATION.md row Status is PASS. Record both the pre-check observation and the action
    taken in the eventual 99-01-SUMMARY.md.

    Edit `.planning/REQUIREMENTS.md` in three places:

    1. Checkbox list (lines 10-53): For every requirement whose 99-01-VERIFICATION.md row Status is
       PASS, change `- [ ] **REQ-ID**:` to `- [x] **REQ-ID**:`. Do NOT alter requirement text even if
       wording is stale (e.g., MD-ELECTIONS-02 still says "cron_active=true" — research GOTCHA 6
       confirms text is left as-is; deviation is documented elsewhere).

       Concrete edits expected (assuming all 22 non-Phase-90 rows PASS):
       - MD-ELECTIONS-01: [ ] → [x]
       - MD-ELECTIONS-02: [ ] → [x]
       - MD-ELECTIONS-03: [ ] → [x]

       For Phase 90 rows (UI-01, UI-02, POST-ELECTION-01, POST-ELECTION-02): flip to [x] ONLY if the
       corresponding 99-01-VERIFICATION.md row Status is PASS. If any is DEFER, leave that single box
       as `[ ]` and add an inline parenthetical note: ` (deferred — Phase 90 Plan 03 not yet executed)`.

       MD-STANCES-01/02/03/04 and MD-GOV-03/04/05 already show [x] per REQUIREMENTS.md head (lines
       45-48, 26-28) — verify they remain [x] after edit; do not regress.

    2. Traceability table (lines 78-104): For every requirement whose row Status is PASS, change the
       Status column from "Pending" to "Complete" ONLY if the pre-check above observed "Pending".
       Concrete rows that MAY need flipping:
         | MD-ELECTIONS-01 | Phase 96 | Complete |
         | MD-ELECTIONS-02 | Phase 96 | Complete |
         | MD-ELECTIONS-03 | Phase 96 | Complete |
         | MD-GOV-03       | Phase 93 | Complete | (only if pre-check observed "Pending")
         | MD-GOV-04       | Phase 93 | Complete | (only if pre-check observed "Pending")
         | MD-GOV-05       | Phase 93 | Complete | (only if pre-check observed "Pending")
         | UI-01           | Phase 90 | Complete | (only if 99-01-VERIFICATION.md UI-01 row is PASS)
         | UI-02           | Phase 90 | Complete | (only if PASS)
         | POST-ELECTION-01 | Phase 90 | Complete | (only if PASS — else leave "Pending" with note in Coverage block)
         | POST-ELECTION-02 | Phase 90 | Complete | (only if PASS)

       MD-STANCES-01..04 already "Complete" per current file (lines 99-102) — verify, do not regress.

    3. Footer (line 113): Replace `*Last updated: 2026-06-04 after roadmap creation (v11.0 phases
       90-99 mapped)*` with `*Last updated: 2026-06-09 after Phase 99 verification sweep (v11.0 all
       26 requirements confirmed)*` (use today's actual date per system context). If any Phase 90
       row is DEFER, instead use: `*Last updated: 2026-06-09 after Phase 99 verification sweep
       (22/26 confirmed; 4 Phase 90 items deferred pending 90-03 execution)*`.

    Do NOT alter the "Future Requirements (v12.0+)" section or "Out of Scope" section. Do NOT
    introduce new requirement IDs.
  </action>
  <verify>
    <automated>grep -v '^#' .planning/REQUIREMENTS.md | grep -c '^- \[x\] \*\*MD-ELECTIONS-0[123]\*\*' | grep -E "^3$" &amp;&amp; grep -c '| MD-ELECTIONS-0[123] | Phase 96 | Complete |' .planning/REQUIREMENTS.md | grep -E "^3$" &amp;&amp; grep -q "Last updated: 2026-06" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md line for MD-ELECTIONS-01 matches `^- \[x\] \*\*MD-ELECTIONS-01\*\*:` (regex)
    - REQUIREMENTS.md line for MD-ELECTIONS-02 matches `^- \[x\] \*\*MD-ELECTIONS-02\*\*:` (regex)
    - REQUIREMENTS.md line for MD-ELECTIONS-03 matches `^- \[x\] \*\*MD-ELECTIONS-03\*\*:` (regex)
    - Traceability table contains literal substring `| MD-ELECTIONS-01 | Phase 96 | Complete |`
    - Traceability table contains literal substring `| MD-ELECTIONS-02 | Phase 96 | Complete |`
    - Traceability table contains literal substring `| MD-ELECTIONS-03 | Phase 96 | Complete |`
    - MD-GOV-03/04/05 Traceability rows show "Complete" (either pre-existing, or flipped from "Pending" by this task — pre-check observation recorded in 99-01-SUMMARY.md)
    - For each Phase 90 requirement (UI-01, UI-02, POST-ELECTION-01, POST-ELECTION-02): checkbox state matches the corresponding 99-01-VERIFICATION.md Status column (PASS → [x], DEFER → [ ] with inline deferred note)
    - Footer reads `*Last updated: 2026-06-09` (or actual current date) AND mentions "Phase 99 verification sweep"
    - No requirement text was altered — only checkbox state and Traceability Status column
    - File still passes markdown lint (no broken tables — every row has 4 columns: Requirement | Phase | Status)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md accurately reflects v11.0 reality. Every requirement that 99-01-VERIFICATION.md
    marks PASS is checked [x] in the requirement list AND "Complete" in the Traceability table; any
    deferred Phase 90 items carry an explicit deferral note. Footer updated. Pre-check noted whether
    MD-GOV-03/04/05 needed flipping or were already "Complete" (idempotent edit).
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix STATE.md "Next migration" counter and append Phase 99 Plan 01 activity entry</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — line 58 reads "Next migration: 278" — STALE)
    - .planning/phases/99-md-verification-playbook-retrospective/99-01-VERIFICATION.md (the "Pre-Phase-99-close DB state" section records `SELECT MAX(version)` value; expected 292)
    - .planning/phases/99-md-verification-playbook-retrospective/99-RESEARCH.md (section "Current Migration Counter" — explains 292 last applied → 293 next)
  </read_first>
  <action>
    Edit `.planning/STATE.md` in three precise locations:

    1. Line 58 (`- Next migration: 278`): Replace `278` with the value `(max_applied + 1)` derived
       from the `SELECT MAX(version)` recorded in 99-01-VERIFICATION.md. Expected value: `293` (last
       applied is 292 per Phase 98-07-SUMMARY.md; Phase 90 Plan 03 reserves 272 but 272 < 292, so
       next remains 293). If the actual recorded max is different, use `recorded_max + 1` — do not
       hardcode 293 if the DB returned a different value.

    2. Front-matter `last_updated` (line 6): Replace `2026-06-08T03:07:07.654Z` (or current value)
       with the current UTC timestamp at edit time (ISO 8601 with seconds, e.g.,
       `2026-06-09T15:30:00.000Z`).

    3. Front-matter `last_activity` (line 7): Replace with `2026-06-09 -- Phase 99 Plan 01 complete
       — v11.0 verification sweep done; REQUIREMENTS.md cleaned; migration counter corrected`.

    DO NOT yet change:
      - `status:` field (still `ready_to_plan` until Plan 99-02 sets `milestone_complete`)
      - `stopped_at:` (Plan 99-02 will set "v11.0 complete")
      - `progress:` block (Plan 99-02 will update to 100% after milestone close)
      - "Current Position" block (Plan 99-02 territory)
      - "Pending Todos" section — the two ME/CA pending todos stay until Phase 90 Plan 03 actually
        runs; Plan 99-01 only adjusts the migration number, not the todo list.

    Add a single new line under the "Accumulated Context" → "Key Decisions" sub-section (the bullet
    list around lines 84-101):
      `- Phase 99 verification sweep confirmed all 22 non-Phase-90 v11.0 requirements PASS against
       production; Phase 90 items {pass_or_defer_status_summary} (see 99-01-VERIFICATION.md)`
    where `{pass_or_defer_status_summary}` is one of:
      - "also PASS — migration 272 applied" (if 90-03-SUMMARY.md exists and Phase 90 rows PASS)
      - "deferred pending Phase 90 Plan 03 execution" (if 90-03-SUMMARY.md does not exist)

    Do NOT touch any other line in STATE.md. Specifically: do NOT modify the "v11.0 Roadmap Summary"
    table, "Key MD Facts" bullets, "Performance Metrics" table, "Session Continuity" block, or
    "Blockers/Concerns" block.
  </action>
  <verify>
    <automated>grep -E "^- Next migration: 29[0-9]$" .planning/STATE.md &amp;&amp; grep -q "last_activity: 2026-06-.* -- Phase 99 Plan 01 complete" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md line matching `^- Next migration: ` shows a value ≥ 293 (the actual SELECT MAX + 1 result from 99-01-VERIFICATION.md)
    - Front-matter `last_updated:` is a valid ISO 8601 timestamp newer than the prior value
    - Front-matter `last_activity:` literal substring `Phase 99 Plan 01 complete` present
    - A new bullet under "Key Decisions" mentions `99-01-VERIFICATION.md` and explicitly states either "PASS" or "deferred" for the 4 Phase 90 requirements
    - `status:` field is still `ready_to_plan` (NOT yet `milestone_complete` — Plan 99-02 handles that)
    - "Current Position" block (lines ~19-26 in current file) is UNCHANGED
    - "Pending Todos" section is UNCHANGED
    - File parses as valid YAML front-matter (no broken `---` delimiters)
  </acceptance_criteria>
  <done>
    STATE.md migration counter reflects production reality; activity log records Plan 99-01
    completion; status remains pre-close so Plan 99-02 can take over the milestone-close edits
    cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → production Supabase | SELECT-only queries against essentials/inform/supabase_migrations schemas via mcp__supabase-local |
| planner → repo markdown | Direct file writes to `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, and new VERIFICATION.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01-01 | Tampering | REQUIREMENTS.md checkbox edits | mitigate | Task 2 acceptance criteria require grep-verifiable literal strings; only flip [ ] → [x] when 99-01-VERIFICATION.md row Status=PASS; never alter requirement text |
| T-99-01-02 | Information Disclosure | DB count queries | accept | All queries are aggregate COUNT(*); no PII surfaced; queries reuse existing VERIFICATION.md patterns |
| T-99-01-03 | Repudiation | Verification audit trail | mitigate | 99-01-VERIFICATION.md records SELECT MAX(version) + timestamp + per-requirement count; provides auditable trail for milestone close |
| T-99-01-04 | Denial of Service | Production DB SELECT load | accept | 26 COUNT(*) queries against indexed columns; negligible load; identical to prior VERIFICATION.md execution |
| T-99-01-05 | Spoofing | Baseline citation forgery | mitigate | Every PASS row must cite a real, existing file path; acceptance criteria require non-empty Baseline source column |
| T-99-01-06 | Elevation of Privilege | Status downgrade hidden | mitigate | Front-matter `status: failed` mandatory if ANY row FAILs; `## Gaps` section required; never silent-skip a failure |
| T-99-01-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this plan; documentation-only |
</threat_model>

<verification>
- 99-01-VERIFICATION.md exists with 26 rows, front-matter status=passed (or deferred)
- REQUIREMENTS.md MD-ELECTIONS-01/02/03 all show `[x]` checkboxes
- REQUIREMENTS.md Traceability rows for MD-ELECTIONS-01/02/03 read "Complete"
- STATE.md "Next migration:" line shows the actual `SELECT MAX(version) + 1` value
- No FAIL status in any verification row — any FAIL triggers Gap section + escalation, not silent close
</verification>

<success_criteria>
1. `99-01-VERIFICATION.md` is the single source of truth for v11.0 final status; all 26 requirement IDs appear; every row has Status PASS or DEFER (zero unrecorded rows)
2. REQUIREMENTS.md accurately reflects production state — no stale `[ ]` for satisfied requirements; deferred Phase 90 items carry explicit deferral notes
3. STATE.md migration counter correctly points to the next available migration number per production `SELECT MAX(version)`
4. Phase 99 Plan 02 can read 99-01-VERIFICATION.md front-matter `status` field to decide whether to proceed with milestone close (passed) or escalate to user (failed/deferred)
</success_criteria>

<output>
Create `.planning/phases/99-md-verification-playbook-retrospective/99-01-SUMMARY.md` when done,
summarizing: verification matrix outcome (PASS/DEFER counts), REQUIREMENTS.md edits applied,
STATE.md migration counter old→new value, and explicit go/no-go signal for Plan 99-02.
</output>
