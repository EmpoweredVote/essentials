---
phase: 180-city-of-forest-grove-deep-seed
plan: 02
type: execute
wave: 2
depends_on: ["180-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1178_forest_grove_city_council.sql
autonomous: false
requirements: [WASH-06]

must_haves:
  truths:
    - "One 'City of Forest Grove, Oregon, US' government row exists (geo_id 4126200 — confirmed correct)"
    - "One 'City Council' chamber exists under it with official_count=7 (name_formal='Forest Grove City Council')"
    - "Exactly one LOCAL_EXEC district and one LOCAL district exist on geo_id 4126200, state='or' lowercase"
    - "7 offices exist (Mayor + 6 Councilors), each with representing_city='Forest Grove' set INLINE"
    - "Office titles are PLAIN 'Mayor' (LOCAL_EXEC) and 'Councilor' (each of the 6 LOCAL seats) — Tigard convention, NOT Tualatin's numbered 'Council Member (Position N)', NO wards"
    - "ALL 7 politicians carry is_appointed=false + all 7 offices carry is_appointed_position=false (uniform — zero appointed seats, like Tualatin, unlike Tigard's 2 appointed)"
    - "All 7 politicians have office_id back-filled (no NULL office_id)"
    - "Section-split scan returns 0 rows for geo_id 4126200 (via the independent canonical GROUP BY/HAVING query, not the dead same-transaction gate)"
    - "The post-verify block includes an INDEPENDENT geofence-presence assertion for geo_id 4126200 AND mtfcc G4110"
    - "The post-verify block includes the D-14 WR-02 in-file IDENTITY GATE: it asserts exactly 7 politicians on the ext_id block carry the researched full_names (Wenzl/Marshall/Martinez/Valenzuela/Gustafson/Falconer/Schimmel), catching an ON CONFLICT DO UPDATE that silently re-attached a wrong name"
    - "NO ward/X00xx geofences or districts were created (pure at-large model)"
    - "Mariana Valenzuela is one Councilor office (Council President title-on-seat), not a separate office row; her is_appointed=false despite the 2020 appointment mentioned in her bio"
    - "Angel Falconer's pre-2022 Milwaukie, OR history is NOT recorded as Forest Grove tenure; Peter Truax is NOT seeded"
    - "Decision coverage — D-03: ward branch N/A (at-large fired; seat identity on office titles, no duplicate district rows); D-04: no section-split and no empty LOCAL section; D-06: Valenzuela's Council President designation is title-on-seat, no separate office row; D-11: representing_city='Forest Grove' set INLINE (no backfill); D-14 WR-02: in-file identity gate in the structural post-verify"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1178_forest_grove_city_council.sql"
      provides: "Structural seed: government + chamber + 2 districts + 7 offices + office_id backfill + post-verify DO block (independent geofence assertion + section-split + WR-02 identity gate) + ledger row"
      min_lines: 250
      contains: "City of Forest Grove, Oregon, US"
  key_links:
    - from: "offices"
      to: "districts (geo_id 4126200)"
      via: "district_id FK on LOCAL_EXEC + LOCAL rows"
      pattern: "geo_id = '4126200'"
    - from: "politicians.office_id"
      to: "offices.id"
      via: "back-fill UPDATE keyed on external_id IN (-4126201..-4126207)"
      pattern: "office_id = o.id"
---

<objective>
Author the single structural migration that seeds the City of Forest Grove government, City Council
chamber, two citywide districts (LOCAL_EXEC for the Mayor + LOCAL at-large for all 6 Councilors), and
the 7 officials. Forest Grove is a genuine hybrid: use Tualatin mig 1169's district/politician shape
(directly-elected Mayor + shared at-large council, ZERO appointed seats) but Tigard mig 1159's PLAIN
title convention ('Mayor'/'Councilor', NO numbered positions, NO wards). Set
`representing_city='Forest Grove'` INLINE on every office INSERT so the community banner derives
correctly at first browse (no backfill migration). Add the D-14 WR-02 in-file identity gate to the
post-verify block (a NEW template hardening pattern — no prior structural migration has it).

Purpose: Satisfy the roster/structure portion of WASH-06 with an idempotent, self-verifying migration
that uses geo_id 4126200, produces no section-split and no empty LOCAL section, asserts the geofence
actually exists, and asserts the seated names match the researched roster.
Output: `1178_forest_grove_city_council.sql` (registered in schema_migrations), applied by the
orchestrator via psql, with its in-migration DO-block gates passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md
@.planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md

**EXECUTION ARCHITECTURE:** gsd-executor WRITES the `.sql` file to C:/EV-Accounts/backend/migrations/.
It has NO DB access. The INLINE ORCHESTRATOR applies it via `psql "$DATABASE_URL" -f <file>`
(DATABASE_URL from C:/EV-Accounts/backend/.env) and commits it in the C:/EV-Accounts repo
(`git -C "C:/EV-Accounts"` — never cd into it). Use the exact migration number confirmed in
180-01-SUMMARY.md (default 1178; ledger MAX 1169 is a TRAP — on-disk MAX 1177 is authoritative) — if
Wave-0 recorded a different next number, rename the file and update the ledger INSERT.

**VERIFY-GATE HYGIENE:** verify gates grep whole files — keep the literal strings `slug` and
`photo_origin_url` OUT of comments in this file. The ledger INSERT into
supabase_migrations.schema_migrations (version) is a genuine table write (expected), not a comment.
</context>

<interfaces>
<!-- Confirmed schema shapes — executor uses these directly, no exploration needed. -->

essentials.governments  (id, name, type, state, city, geo_id)   -- slug GENERATED; NO geo_id unique constraint; INSERT via WHERE NOT EXISTS on name
essentials.chambers     (id, name, name_formal, government_id, official_count)  -- slug GENERATED ALWAYS — NEVER include in INSERT column list
essentials.districts    (id, district_type, state, geo_id, label, mtfcc)  -- NO name_formal; government_id NULL; state='or' LOWERCASE for LOCAL/LOCAL_EXEC
essentials.politicians  (id, full_name, first_name, last_name, party, is_active, is_appointed, is_vacant, is_incumbent, external_id, office_id)  -- party ALWAYS NULL (antipartisan)
essentials.offices      (id, district_id, chamber_id, politician_id, title, representing_state, representing_city, is_appointed_position, is_vacant, role_canonical)
</interfaces>

<roster>
<!-- Seed exactly these 7 (re-confirmed at Wave-0). ext_id / title / district_type / appointed: -->
| ext_id    | full_name         | first_name | last_name  | title     | district_type | is_appointed |
|-----------|-------------------|------------|------------|-----------|---------------|--------------|
| -4126201  | Malynda Wenzl     | Malynda    | Wenzl      | Mayor     | LOCAL_EXEC    | FALSE (directly elected citywide; elected Mayor Nov 2022, term through Dec 31 2026) |
| -4126202  | Michael Marshall  | Michael    | Marshall   | Councilor | LOCAL         | FALSE (elected Nov 2022, term through Dec 31 2026) |
| -4126203  | Karen Martinez    | Karen      | Martinez   | Councilor | LOCAL         | FALSE (elected Nov 2022, term through Dec 31 2026) |
| -4126204  | Mariana Valenzuela| Mariana    | Valenzuela | Councilor | LOCAL         | FALSE (elected Nov 2022, term through Dec 31 2026; holds Council President title-on-seat; 2020 appointment superseded by the Nov 2022 election) |
| -4126205  | Donna Gustafson   | Donna      | Gustafson  | Councilor | LOCAL         | FALSE (elected Nov 2020, reelected Nov 2024, term through Dec 31 2028) |
| -4126206  | Angel Falconer    | Angel      | Falconer   | Councilor | LOCAL         | FALSE (elected Nov 2024, first FG term, term through Dec 31 2028) |
| -4126207  | Brian Schimmel    | Brian      | Schimmel   | Councilor | LOCAL         | FALSE (elected Nov 2024 — narrow win over Peter Truax; term through Dec 31 2028) |
<!-- Titles use Tigard's PLAIN convention ('Mayor' / 'Councilor') — NOT Tualatin's numbered
     'Council Member (Position N)' and NO ward suffixes. Mayor Wenzl on LOCAL_EXEC; all 6 councilors
     on the shared LOCAL district. ALL 7 use is_appointed=false + is_appointed_position=false (uniform
     Tualatin shape — do NOT import Tigard's appointed-seat block for any seat). party=NULL,
     representing_state='OR', representing_city='Forest Grove' on all 7. Valenzuela's "Council
     President" is a title-on-seat — note ONLY in a migration comment; do NOT create a separate office
     row and do NOT alter her title. There is NO MYAC/Youth seat to seed (MYAC is a standalone
     advisory board). Do NOT seed Peter Truax. Do NOT record Falconer's pre-2022 Milwaukie, OR history
     as Forest Grove tenure. Do NOT seed any 2026-election candidate as an incumbent. -->
</roster>

<tasks>

<task type="auto">
  <name>Task 1: Author the Forest Grove structural migration</name>
  <files>C:/EV-Accounts/backend/migrations/1178_forest_grove_city_council.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1169_tualatin_city_council.sql (the directly-elected-Mayor + shared-at-large + zero-appointed-seats district/politician template — entire file)
    - C:/EV-Accounts/backend/migrations/1159_tigard_city_council.sql lines 134-168 (the PLAIN 'Councilor' title-string convention to overlay — NOT Tualatin's numbered Position strings)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-PATTERNS.md §"1178_forest_grove_city_council.sql (structural migration, CRUD)" (every code block, adapted values — incl. the fully-specified WR-02 identity gate)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-01-SUMMARY.md (confirmed geo_id 4126200, ext_id block, migration number, roster)
    - .planning/phases/180-city-of-forest-grove-deep-seed/180-RESEARCH.md §"Schema Shapes", §"OR Casing Rules", §"Office Title Convention", §"Migration Template Fixes to Carry Forward"
  </read_first>
  <action>
    Wrap everything in a single BEGIN…COMMIT transaction. Structure per Tualatin mig 1169 for the
    directly-elected-Mayor + shared-at-large + zero-appointed-seats shape, overlaying Tigard 1159's
    PLAIN title strings, adapting values:
    (1) Pre-flight hard-abort DO block: RAISE EXCEPTION if a 'City of Forest Grove, Oregon, US'
    government already exists (message references migration 1178).
    (2) Government INSERT … SELECT (name='City of Forest Grove, Oregon, US', type='LOCAL', state='OR'
    UPPERCASE, city='Forest Grove', geo_id='4126200') guarded WHERE NOT EXISTS on name — NEVER insert slug.
    (3) Chamber INSERT (name='City Council', name_formal='Forest Grove City Council', government_id
    subselect, official_count=7) guarded WHERE NOT EXISTS on (name, government_id) — NEVER insert slug.
    (4) TWO district INSERTs, both state='or' LOWERCASE, geo_id='4126200', mtfcc=NULL: one
    district_type='LOCAL_EXEC' label='Forest Grove (Mayor, Citywide)', one district_type='LOCAL'
    label='Forest Grove (At-Large)', each guarded WHERE NOT EXISTS on (geo_id, district_type, state).
    Create NO other districts — no ward/X00xx rows, and NO per-seat/per-position districts (all 6
    councilors share the ONE LOCAL district).
    (5) SEVEN office blocks (the CTE pattern: INSERT politician ON CONFLICT (external_id) DO UPDATE SET
    is_active, then INSERT office CROSS JOIN the correct district guarded by NOT EXISTS on
    (district_id, politician_id)). Use the exact 7 rows in the <roster> table above: Mayor Wenzl on the
    LOCAL_EXEC district (title 'Mayor'); the 6 councilors on the shared LOCAL district (title PLAIN
    'Councilor' for ALL six — do NOT append a position number or ward). party=NULL, role_canonical=NULL,
    representing_state='OR' UPPERCASE, representing_city='Forest Grove' INLINE on every office. UNIFORM
    appointed flags: ALL 7 politicians is_appointed=false AND all 7 offices is_appointed_position=false
    (the Tualatin uniform shape — do NOT import Tigard's is_appointed=true block for any seat). Add a
    comment above Valenzuela's block (-4126204) noting she holds the Council President title-on-seat
    (one office row only, title stays plain 'Councilor') and that her 2020 appointment was superseded
    by the Nov 2022 election so is_appointed=false is correct. Add a comment above Falconer's block
    (-4126206) warning her pre-2022 "council president"/2016/2020 history belongs to Milwaukie, OR (a
    different city) and must NOT be recorded as Forest Grove tenure. Do NOT create a block for Peter
    Truax.
    (6) office_id back-fill UPDATE with an explicit external_id IN (-4126201,-4126202,-4126203,
    -4126204,-4126205,-4126206,-4126207) list, guarded AND p.office_id IS NULL.
    (7) Post-verification DO block with FOUR independent assertions plus the NEW WR-02 identity gate:
    gate gov_count=1; office_count=7 (JOIN districts on geo_id 4126200 + district_type IN
    ('LOCAL','LOCAL_EXEC') + state 'or'); an INDEPENDENT geofence-presence assertion (COUNT(*) FROM
    essentials.geofence_boundaries WHERE geo_id='4126200' AND mtfcc='G4110' must be ≥1); the canonical
    section-split query (GROUP BY o.district_id HAVING COUNT(DISTINCT o.chamber_id) > 1 over offices
    JOIN districts on geo_id 4126200) = 0; office_id NULLs=0; representing_city='Forest Grove' count=7;
    AND THE D-14 WR-02 IDENTITY GATE — SELECT COUNT(*) INTO v_name_count FROM essentials.politicians
    WHERE external_id IN (-4126201..-4126207) AND full_name IN ('Malynda Wenzl','Michael Marshall',
    'Karen Martinez','Mariana Valenzuela','Donna Gustafson','Angel Falconer','Brian Schimmel'), RAISE
    EXCEPTION unless it equals 7 (catches an ON CONFLICT DO UPDATE that re-attached a stale/wrong name
    on re-run — this is a NEW template hardening block, no prior structural migration has it). RAISE
    EXCEPTION on any mismatch; RAISE NOTICE on success listing all gates incl. identity_gate=7.
    (8) Ledger INSERT into supabase_migrations.schema_migrations (version) VALUES ('1178') ON CONFLICT
    DO NOTHING (STRUCTURAL migrations register — contrast with headshot/stance migrations).
    Do NOT place fenced code blocks in this action — the SQL itself lives in the file; the analog
    blocks in 180-PATTERNS.md are the source of truth for exact text.
  </action>
  <verify>
    <automated>MISSING — orchestrator applies `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1178_forest_grove_city_council.sql`; the in-migration post-verify DO block (incl. the WR-02 identity gate) RAISEs on any gate failure (all gates must emit the success NOTICE)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the confirmed migration number path (default 1178_forest_grove_city_council.sql)
    - Single BEGIN…COMMIT; pre-flight abort guard present
    - governments INSERT uses geo_id='4126200', state='OR', WHERE NOT EXISTS on name; no slug column
    - chambers INSERT has official_count=7 and name_formal='Forest Grove City Council'; no slug column
    - Exactly 2 district INSERTs (LOCAL_EXEC + LOCAL), both state='or' lowercase, geo_id='4126200'; NO ward/X00xx and NO per-seat districts
    - 7 office blocks with the exact ext_ids -4126201..-4126207; titles are PLAIN 'Mayor' + 'Councilor' x6 (NO position numbers, NO wards)
    - ALL 7 politicians is_appointed=false AND all 7 offices is_appointed_position=false (no appointed seats)
    - representing_city='Forest Grove' appears on all 7 office INSERTs (inline, no backfill migration)
    - Valenzuela (-4126204) has exactly one office row, plain 'Councilor' title, with a Council-President title-on-seat comment; Falconer block carries the Milwaukie-pitfall comment; no Peter Truax block
    - office_id back-fill UPDATE lists all 7 external_ids
    - Post-verify DO block includes the independent geofence assertion (geo_id='4126200' AND mtfcc='G4110' ≥ 1) + canonical section-split query + the D-14 WR-02 identity gate (full_name IN the 7 researched names, count must = 7); asserts gov=1, offices=7, section-split=0, office_id nulls=0, representing_city=7, identity_gate=7
    - Ledger INSERT VALUES ('1178') present (structural migration registers)
    - Literal strings `slug` and `photo_origin_url` do NOT appear in comments
  </acceptance_criteria>
  <done>Structural migration authored against geo_id 4126200 with plain titles, zero appointed seats, and the WR-02 identity gate; applies cleanly; post-verify DO block emits its success NOTICE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator applies the migration, runs the E2E structural gate, commits</name>
  <what-built>The Forest Grove structural migration. The orchestrator applies it and runs the standalone verification gate.</what-built>
  <how-to-verify>
    1. Apply: `psql "$DATABASE_URL" -f C:/EV-Accounts/backend/migrations/1178_forest_grove_city_council.sql`. The in-migration DO block must emit "Post-verification PASSED". If it RAISEs, the transaction rolls back — fix and re-apply.
    2. Independent E2E gate (run as separate SELECTs):
       a. governments count for name='City of Forest Grove, Oregon, US' = 1.
       b. chambers row exists, official_count=7, name_formal='Forest Grove City Council'.
       c. districts: exactly 1 LOCAL_EXEC + 1 LOCAL for geo_id='4126200' state='or'; ZERO ward/X00xx and ZERO per-seat rows for Forest Grove.
       d. offices count = 7 for the Forest Grove chamber; representing_city='Forest Grove' on all 7; titles are PLAIN 'Mayor' + 'Councilor' x6 (no position numbers, no wards).
       e. Section-split scan returns 0 rows for geo_id 4126200.
       f. All 7 politicians have non-NULL office_id.
       g. ALL 7 have is_appointed=false + is_appointed_position=false; Valenzuela (-4126204) has exactly ONE office row.
       h. Independent geofence check: essentials.geofence_boundaries WHERE geo_id='4126200' AND mtfcc='G4110' returns ≥1.
       i. Identity gate: the 7 politicians on the ext_id block carry exactly the researched names (Wenzl/Marshall/Martinez/Valenzuela/Gustafson/Falconer/Schimmel); NO Peter Truax row exists.
    3. Commit the migration in the EV-Accounts repo: `git -C "C:/EV-Accounts" add backend/migrations/1178_forest_grove_city_council.sql && git -C "C:/EV-Accounts" commit`.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied; "Post-verification PASSED" NOTICE emitted (incl. identity_gate=7)
    - Independent E2E gate a–i all pass
    - Zero ward/X00xx and zero per-seat districts for Forest Grove; exactly 7 offices
    - All 7 uniform is_appointed=false; Valenzuela single office row; plain titles; no Truax row
    - Geofence assertion confirms 4126200 (G4110) present
    - Migration committed in the C:/EV-Accounts repo
  </acceptance_criteria>
  <resume-signal>Type "approved" with the gate results, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod DB | Structural INSERTs applied to production Supabase via psql |
| migration file → schema | Idempotency + post-verify gates (incl. WR-02 identity gate) gate the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-06 | Tampering | Silent politician-identity collision: an ON CONFLICT (external_id) DO UPDATE path re-attaches a stale/wrong full_name to a colliding ext_id on re-run | mitigate | D-14 WR-02 in-file identity gate: post-verify asserts exactly 7 politicians on the ext_id block carry the researched full_names, RAISEs otherwise — in-transaction, not just the out-of-band Wave-0 Probe D |
| T-180-07 | Tampering | Duplicate/partial re-seed on prod | mitigate | Pre-flight hard-abort DO block + WHERE NOT EXISTS on gov/chamber/districts + office guard on (district_id, politician_id); single transaction rolls back on any post-verify failure |
| T-180-08 | Tampering | Dead post-verify gate lets a real section-split ship silently | mitigate | Independent geofence-presence assertion + canonical GROUP BY/HAVING section-split query, not the same-transaction "create-then-check-absence" gate |
| T-180-09 | Spoofing | Wrong seat modeling: seeding Peter Truax or recording Falconer's Milwaukie history as Forest Grove tenure | mitigate | Roster comments flag both pitfalls; identity gate asserts the exact 7 researched names (Schimmel not Truax); no Truax block authored |
| T-180-10 | Elevation of Privilege | Ward/position districts or an appointed-seat mislabel misrepresent the roster | mitigate | Pure at-large enforced (2 citywide districts, plain titles); uniform is_appointed=false; E2E gate 2c/2d/2g assert zero ward rows + exactly 7 offices + no appointed seats |
| T-180-11 | Repudiation | Untracked structural change | mitigate | Migration registered in schema_migrations ledger; committed to EV-Accounts git |
| T-180-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A |
</threat_model>

<verification>
- In-migration post-verify DO block passes (incl. WR-02 identity gate); independent E2E gate a–i
  passes; migration committed.
</verification>

<success_criteria>
- 1 government + 1 chamber (official_count=7) + 2 citywide districts + 7 offices (plain 'Mayor'/'Councilor'
  titles, no appointed seats), all with representing_city='Forest Grove', office_id back-filled,
  section-split=0, geofence 4126200 confirmed present, identity gate confirms the 7 researched names,
  no ward geofences, no Truax.
</success_criteria>

<output>
Create `.planning/phases/180-city-of-forest-grove-deep-seed/180-02-SUMMARY.md` when done, recording
the minted politician UUIDs (by external_id) for plans 03 (headshots) and 04 (stances).
</output>
