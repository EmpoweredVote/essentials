---
phase: 173-nevada-playbook-retrospective-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v18.0-MILESTONE-AUDIT.md
autonomous: true
requirements: [NV-RETRO-01]

must_haves:
  truths:
    - "A reader can see, per NV jurisdiction, the DB-verified gov/chamber/roster-vs-official_count/headshot/stance counts for all 6 metro jurisdictions plus the legislature ride-along"
    - "Each jurisdiction carries a structure-hard / data-soft verdict that distinguishes a structural defect (blocker) from an acceptable documented gap"
    - "The exact set of NV jurisdictions with >=1 seeded compass stance is recorded (drives the D-01 purple-chip decision in Plan 02)"
    - "CCSD is recorded as 0-stance-by-design (deferred school-board compass) — listed/browsable but plain, not a blocker"
    - "All four D-08 known-issues / follow-up items are recorded (legislature stances deferred, Mesquite, browse state-leak bug, 169->173 renumber)"
  artifacts:
    - path: ".planning/v18.0-MILESTONE-AUDIT.md"
      provides: "DB-verified per-jurisdiction audit + verdicts + chip input + four known-issues items"
      contains: "v18.0"
  key_links:
    - from: ".planning/v18.0-MILESTONE-AUDIT.md"
      to: "production DB (mcp__supabase-local)"
      via: "read-only SELECT verification of every per-jurisdiction count"
      pattern: "geo_id"
---

<objective>
Write `.planning/v18.0-MILESTONE-AUDIT.md` — a standalone, DB-verified per-jurisdiction audit of the
v18.0 "Las Vegas & Clark County, NV" milestone (phases 158-168), matching the format/location of the
v17.0/v16.0 audits (D-04).

Purpose: This audit is the source of truth for the milestone close. Its DB-verified per-jurisdiction
stance count drives the D-01 purple-chip reconciliation in Plan 02, and its verdicts gate the close
(structure-hard / data-soft, D-02).

Output: `.planning/v18.0-MILESTONE-AUDIT.md` with a per-jurisdiction count table, per-jurisdiction
verdicts, the recorded set of >=1-stance jurisdictions, and the four D-08 known-issues / follow-up items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/173-nevada-playbook-retrospective-close/173-CONTEXT.md
@.planning/v17.0-MILESTONE-AUDIT.md
</context>

<key_facts>
- `mcp__supabase-local` IS production. ALL queries in this plan are READ-ONLY (SELECT only). No INSERT/UPDATE/DELETE.
- The `gsd-executor` has NO Supabase MCP. DB-verify runs INLINE in main context, or via `psql -f` against a saved `.sql` file (established convention since Phase 159). Specify the SQL; do not assume a tool that the executor lacks.
- Authoritative tables (memory `project_schema_key_tables`): `essentials.governments` (gov rows; geo_id, no unique constraint), `essentials.chambers` (`official_count` lives here, NOT on governments; `slug` is generated), `essentials.offices`, `essentials.politicians`, `essentials.districts` (NO `name_formal` — use `label`; `government_id` is NULL → join via geo_id), `essentials.politician_images` (headshots; `type='default'`), `inform.politician_answers` (stances).
- NV query gotchas (D-03): (a) `districts.state` is lowercase `'nv'` for SLDU/SLDL + city/county district tiers (uppercase `'NV'` only on STATE_EXEC/NATIONAL tiers); (b) `essentials.districts` has no `name_formal`; (c) join districts to offices via geo_id; (d) statewide officials browse via `browse_state_officials=NV`, not geo_id+G5200.
- The 6 metro jurisdictions + geo_ids (CONTEXT.md, verbatim): City of Las Vegas `3240000` · Henderson `3231900` · North Las Vegas `3251800` · Boulder City `3206500` · CCSD `3200060` (mtfcc G5420) · Clark County `32003`.
- ext_id schemes (CONTEXT.md D-07, for cross-checking rosters): Clark County `-3200301..-3200307`; Controller `-3200006`; State Senate `-3203001..-3203021`; Assembly `-3204001..-3204042`; Henderson `-3206001..-3206005`; Boulder City `-3208xxx`; North Las Vegas `-3207xxx`.
- Stance migrations were applied via raw SQL and do NOT register in `supabase_migrations.schema_migrations` — count stance rows from `inform.politician_answers`, NOT from the ledger. On-disk migration counter is authoritative (next = 1115 per ROADMAP Phase 168 scope; confirm at execution).
- Expected stance ballpark from memory (CONFIRM at audit, do not assume): Clark County ~32 · Henderson ~28 · Boulder City ~19 · North Las Vegas (migs 1095-1099) · CCSD = 0 by design (D-01a) · City of Las Vegas = the originally-PARKED phase, real count UNKNOWN (D-01b — verify, do not assume).
- The NV state legislature (21 Senate + 42 Assembly) is seeded + headshots only, NO stances (deferred by design, D-08.1) — it is the "ride-along" surfaced via `browse_state_officials=NV`, not a metro jurisdiction. Audit it for seed/headshot completeness only.
</key_facts>

<tasks>

<task type="auto">
  <name>Task 1: DB-verify per-jurisdiction counts for all 6 NV metro jurisdictions + the legislature ride-along (gotcha-aware, read-only)</name>
  <read_first>
    - .planning/phases/173-nevada-playbook-retrospective-close/173-CONTEXT.md (D-01/D-01a/D-01b chip rules; D-02 verdict bar; D-03 the five required query dimensions + the four baked-in NV query gotchas)
    - .planning/v17.0-MILESTONE-AUDIT.md (the "Per-City DB-Verified Counts" table + "Verified DB Metrics" shape to mirror for NV)
    - .planning/STATE.md (per-phase outcome notes 158-168 — rosters, official_counts, ext_id ranges, headshot/stance counts, mayor types — for cross-checking expected DB results)
    - Memory file `feedback_section_split_check` (split-section detection SQL — run per jurisdiction) at ~/.claude/projects/C--Transparent-Motivations-essentials/memory/feedback_section_split_check.md
  </read_first>
  <action>
    Run READ-ONLY (SELECT) queries against the production DB (inline via `mcp__supabase-local` in main
    context, OR write a `.sql` file and run `psql -f` — the gsd-executor has no Supabase MCP). For
    each of the 6 metro geo_ids (3240000 Las Vegas, 3231900 Henderson, 3251800 North Las Vegas,
    3206500 Boulder City, 3200060 CCSD, 32003 Clark County) capture the five D-03 dimensions:
    (1) the `essentials.governments` row matched by geo_id; (2) its chamber(s) in `essentials.chambers`
    with `official_count` — flag any jurisdiction carrying >1 same-named council/board chamber as a
    structural defect; (3) live roster = count of ACTIVE `essentials.offices` with a linked politician
    on the chamber, compared against the chamber's `official_count`; (4) headshot count from
    `essentials.politician_images` (`type='default'`) for the seated roster; (5) stance count from
    `inform.politician_answers` joined to the seated roster (officials-with->=1 / total rows), NOT from
    the migration ledger.
    Then audit the legislature ride-along separately: confirm 21 State Senators (ext -3203001..-3203021)
    + 42 Assembly members (ext -3204001..-3204042) seeded with offices linked to SLDU/SLDL districts,
    headshot count for the 63, and confirm stance count = 0 (deferred by design).
    Bake in the D-03 NV gotchas explicitly: (a) `districts.state` is lowercase `'nv'` for the
    city/county/SLDU/SLDL tiers — match casing or the join returns 0 rows; (b) `essentials.districts`
    has no `name_formal`, use `label`; (c) join `essentials.districts` to offices via geo_id, never via
    government_id (it is NULL); (d) statewide officials are browsed via `browse_state_officials=NV`.
    Run the per-jurisdiction split-section detection query from `feedback_section_split_check` for each
    of the 6 metro jurisdictions and record the row count (0 = clean). Recommended (not required, D
    discretion): spot-check a handful of NV headshots resolve to the correct person.
    Record raw integers per jurisdiction in a scratch note for Task 2. Pay special attention to City of
    Las Vegas — it was the PARKED phase; verify its real stance count rather than assuming. Do NOT write
    any row to the DB.
  </action>
  <verify>
    <automated>MISSING — verification is the read-only DB result set itself; Task 2 asserts the audit file captures it. Confirm all 6 metro geo_ids were queried, each returned exactly one essentials.governments row, and the 63-legislator seed+headshot+0-stance counts were captured.</automated>
  </verify>
  <acceptance_criteria>
    - All 6 metro geo_ids (3240000, 3231900, 3251800, 3206500, 3200060, 32003) were queried; each returned exactly one `essentials.governments` row.
    - For each metro jurisdiction: chamber count + official_count, active-roster count vs official_count, headshot count, and stance count (pols-with->=1 / total rows) are recorded as concrete integers.
    - City of Las Vegas stance count is verified from the live DB (not assumed from "we ran a stance phase").
    - CCSD stance count is recorded (expected 0 by design) and the legislature ride-along is recorded as 63 seeded / headshot count / 0 stances.
    - The split-section query ran per metro jurisdiction and its row count (0 = clean) is recorded for each.
    - All district joins used lowercase `'nv'` casing and geo_id (no query relied on districts.government_id or name_formal).
    - No write statement (INSERT/UPDATE/DELETE) was issued against the DB.
  </acceptance_criteria>
  <done>
    Raw per-jurisdiction counts (gov, chamber+official_count, roster, headshots, stances, split-section
    rows) captured for all 6 metro jurisdictions + the 63-legislator ride-along, ready to transcribe
    into the audit doc — with the City of Las Vegas (parked-phase) stance count confirmed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .planning/v18.0-MILESTONE-AUDIT.md</name>
  <read_first>
    - .planning/v17.0-MILESTONE-AUDIT.md (format/location precedent — frontmatter shape, Requirements Coverage table, Per-City counts table, Verified DB Metrics, Purple-Chip Input, Known Issues / Deferred sections to mirror)
    - .planning/phases/173-nevada-playbook-retrospective-close/173-CONTEXT.md (D-01 chip rule + D-01a CCSD + D-01b LV; D-02 structure-hard/data-soft verdict rule; D-04 required table dimensions; D-08 the four mandatory known-issues items)
    - .planning/REQUIREMENTS.md (NV-RETRO-01 text + the v18.0 requirement-to-phase mapping for the Requirements Coverage roll-up)
  </read_first>
  <action>
    Create `.planning/v18.0-MILESTONE-AUDIT.md` at the repo-root planning path (NOT
    `.planning/milestones/`), mirroring the v17.0 audit structure. Include: (1) frontmatter with
    `milestone: v18.0`, audited date, status, `verified_against: production DB (mcp__supabase-local)`,
    and key metrics (jurisdictions, active officials, headshots, officials-with-stances, total stance
    rows, split-section defects, jurisdictions-with-purple-chip); (2) a per-jurisdiction DB-verified
    count table with columns Jurisdiction | geo_id | Chamber(s) | Roster (active vs official_count) |
    Headshots | Stances (pols / rows) | Split-sec | Verdict — populated from Task 1's numbers, one row
    per metro jurisdiction plus a row for the legislature ride-along (63 seeded / headshots / 0 stances)
    plus a Total row; (3) a per-jurisdiction structure-hard / data-soft verdict (D-02): missing
    gov/chamber/correct-roster/wrong-mayor-type/wrong-district-structure = BLOCKER; headshot-source 403
    gaps + upscaled low-res sources + deferred-by-design no-stance (CCSD, legislature) = DOCUMENTED
    ACCEPTABLE GAP — phrase each so a reader can tell "incomplete on purpose" from "broken"; explicitly
    note City of Las Vegas structure verified (it was the parked phase); (4) a Purple-Chip Input (D-01)
    section explicitly listing which jurisdictions have >=1 seeded compass stance (chip stays) vs which
    are roster-only / 0-stance (chip drops) — CCSD listed as 0-by-design → plain; (5) a Known Issues /
    Follow-up section recording ALL FOUR D-08 items: (i) NV legislature compass stances — 63 legislators
    seeded + headshots, no stances, deferred to a dedicated follow-up milestone (OR v8.0->v9.0 pattern);
    (ii) Mesquite — Clark County's smallest incorporated city, not seeded, future Clark County wave;
    (iii) browse-government-list state-leak bug — unseeded-city browse leaks stale prior-location
    officials under the wrong state banner (see STATE.md outcome note + `project_browse_government_list_state_leak`),
    deferred fix, not blocking; (iv) the v19.0 detour phase-renumber note — numbers 169-172 occupied by
    the parked v19.0 frontend detour, this phase renumbered 169->173. Tie the whole audit to requirement
    NV-RETRO-01. Use the Write tool (no heredoc).
  </action>
  <verify>
    <automated>test -f .planning/v18.0-MILESTONE-AUDIT.md && grep -q "v18.0" .planning/v18.0-MILESTONE-AUDIT.md && for g in 3240000 3231900 3251800 3206500 3200060 32003; do grep -q "$g" .planning/v18.0-MILESTONE-AUDIT.md || { echo "MISSING geo_id $g"; exit 1; }; done && grep -qi "Mesquite" .planning/v18.0-MILESTONE-AUDIT.md && grep -qi "state-leak\|state leak" .planning/v18.0-MILESTONE-AUDIT.md && grep -qi "169" .planning/v18.0-MILESTONE-AUDIT.md && grep -qiE "legislature.*stance|stances.*deferred|deferred.*stance" .planning/v18.0-MILESTONE-AUDIT.md && grep -qi "NV-RETRO-01" .planning/v18.0-MILESTONE-AUDIT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/v18.0-MILESTONE-AUDIT.md` (root planning dir, not the milestones/ subdir).
    - Frontmatter includes `milestone: v18.0` and `verified_against` referencing the production DB.
    - The per-jurisdiction table contains all 6 metro geo_ids + a legislature ride-along row + a Total row, with the five D-04 dimensions per jurisdiction.
    - Each jurisdiction has an explicit structure-hard/data-soft verdict using BLOCKER vs DOCUMENTED-GAP language; City of Las Vegas structure is explicitly verified.
    - The set of >=1-stance jurisdictions is explicitly listed (the D-01 chip input); CCSD appears as 0-stance-by-design → plain.
    - All four D-08 items are present: legislature stances deferred, Mesquite, browse state-leak bug, the 169->173 renumber.
    - The audit is tied to requirement NV-RETRO-01.
  </acceptance_criteria>
  <done>
    `.planning/v18.0-MILESTONE-AUDIT.md` written with the full per-jurisdiction table, verdicts, chip
    input, and all four known-issues items — ready to drive Plan 02 (surfacing + onboarding) and Plan 03 (close).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB | Read-only SELECTs cross into the live Supabase DB |
| executor → committed planning docs | Audit markdown is committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-01 | Tampering | audit queries against production DB | mitigate | All DB access is SELECT-only; no INSERT/UPDATE/DELETE issued. Verified by acceptance criteria. |
| T-173-02 | Information Disclosure | committed audit markdown | mitigate | Audit records counts/verdicts only — no secrets, credentials, or PII beyond already-public officeholder names. |
| T-173-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (doc + read-only DB only). |

Realistic surface is minimal: read-only DB verification + a single committed markdown doc, no new
inputs/endpoints/auth/installs.
</threat_model>

<verification>
- `.planning/v18.0-MILESTONE-AUDIT.md` exists with all 6 metro geo_ids, the legislature ride-along row, and per-jurisdiction verdicts.
- Every DB interaction this plan performed was read-only.
- All four D-08 known-issues items are recorded.
</verification>

<success_criteria>
- DB-verified per-jurisdiction audit written (criterion 3, audit half) with the >=1-stance jurisdiction set recorded for Plan 02's chip reconciliation.
- Structure-hard/data-soft verdicts present; all four D-08 known-issues/follow-up items documented.
</success_criteria>

<output>
Create `.planning/phases/173-nevada-playbook-retrospective-close/173-01-SUMMARY.md` when done — include
the recorded set of >=1-stance jurisdictions (Plan 02 needs it for the chip reconciliation), the City of
Las Vegas verified stance count, and the audit verdict roll-up.
</output>
