---
phase: 175-washington-county-commission-deep-seed
plan: 03
type: execute
wave: 2
depends_on: ["175-01"]
files_modified:
  - C:/EV-Accounts/backend/migrations/1122_washco_harrington_stances.sql
  - C:/EV-Accounts/backend/migrations/1123_washco_fai_stances.sql
  - C:/EV-Accounts/backend/migrations/1124_washco_treece_stances.sql
  - C:/EV-Accounts/backend/migrations/1125_washco_snider_stances.sql
  - C:/EV-Accounts/backend/migrations/1126_washco_willey_stances.sql
  - src/lib/coverage.js
autonomous: true
requirements: [WASH-01]

must_haves:
  truths:
    - "Evidence-only compass stances render on commissioner profiles — 100% cited, honest blank spokes, zero default values"
    - "Washington County surfaces with the purple hasContext chip in src/lib/coverage.js"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1122_washco_harrington_stances.sql"
      provides: "Audit-only Chair stances: CTE -> JOIN compass_topics (is_live) -> dual INSERT politician_answers + politician_context, ON CONFLICT (politician_id, topic_id) DO UPDATE"
      contains: "is_live = true"
    - path: "src/lib/coverage.js"
      provides: "COVERAGE_COUNTIES entry surfacing Washington County, OR with hasContext chip"
      contains: "Washington County, OR"
  key_links:
    - from: "each stance migration"
      to: "inform.compass_topics (topic_key, is_live=true)"
      via: "JOIN ct ON ct.topic_key = s.topic_key AND ct.is_live = true"
      pattern: "is_live = true"
    - from: "src/lib/coverage.js COVERAGE_COUNTIES"
      to: "Washington County government (geo_id 41067)"
      via: "browseGovernmentList: ['41067']"
      pattern: "browseGovernmentList"
---

<objective>
Research and seed evidence-only compass stances for all 5 seated Washington County commissioners (one research agent at a time), then surface Washington County in src/lib/coverage.js with the purple hasContext chip.

Stances follow the standing rules (D-07): ALL live compass topics per official, evidence-only, 100% cited, honest blank spokes where no public record exists, ZERO default values. Values 1-5 are discrete chair positions (the chair the evidence matches), NOT a polarity scale.

Purpose: Satisfies ROADMAP success criteria #3 (evidence-only stances) and #4 (hasContext chip).
Output: 5 audit-only stance migrations (executor authors from agent-supplied evidence) + 1 coverage.js edit. Stance RESEARCH and DB application are inline-orchestrator steps (one agent at a time per the rate-limit rule; executor has NO DB).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Executor / Orchestrator split + one-agent-at-a-time rule
- Stance RESEARCH runs as **one research agent at a time** (memory: feedback_stance_research_one_at_a_time — parallel burns rate-limit quota with no usable output). The inline orchestrator dispatches Harrington -> Fai -> Treece -> Snider -> Willey sequentially, each researching ALL live compass topics, evidence-only.
- The executor AUTHORS each `.sql` migration from the agent's cited evidence (topic_key, chair value, reasoning, sources). The inline orchestrator applies each via `psql -f` (audit-only — no ledger INSERT) and runs the stance scans.
- The coverage.js edit is a pure file edit (executor) verified by grep in the essentials repo (no DB).
- Commit stance migrations in the EV-Accounts repo via `git -C "C:/EV-Accounts"`; commit coverage.js in the essentials repo.
</execution_context>

<context>
@.planning/phases/175-washington-county-commission-deep-seed/175-PATTERNS.md
@.planning/phases/175-washington-county-commission-deep-seed/175-RESEARCH.md
@.planning/phases/175-washington-county-commission-deep-seed/175-01-SUMMARY.md

<interfaces>
From src/lib/coverage.js (COVERAGE_COUNTIES block, lines 225-241; the Multnomah entry is line 229):

  export const COVERAGE_COUNTIES = [
    ...
    { label: 'Multnomah County', browseGovernmentList: ['41051'], browseStateAbbrev: 'OR' },
    ...
    { label: 'Washington County', browseGovernmentList: ['49053'], browseStateAbbrev: 'UT' },  // line 238 — UT, COLLISION RISK
    ...
    { label: 'Clark County', browseGovernmentList: ['32003'], browseStateAbbrev: 'NV', hasContext: true },
  ];

ALL_COVERAGE_AREAS (line ~258) maps COVERAGE_COUNTIES for typeahead — duplicate labels cause search ambiguity.
</interfaces>

<critical_facts>
- Stance migration files = `1122_washco_harrington_stances.sql` (Chair) through `1126_washco_willey_stances.sql` (D4). All AUDIT-ONLY — NO ledger INSERT. Order: 1122 Harrington/Chair, 1123 Fai/D1, 1124 Treece/D2, 1125 Snider/D3, 1126 Willey/D4.
- Stance shape (analog 1057): CTE `s(topic_key, val, reasoning, sources)` VALUES -> `t` JOIN inform.compass_topics ON topic_key AND is_live=true -> INSERT inform.politician_answers (politician_id, topic_id, value) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value -> INSERT inform.politician_context (politician_id, topic_id, reasoning, sources) ON CONFLICT DO UPDATE. politician_id is the UUID from Plan 01's SUMMARY.
- NEVER hardcode topic UUIDs — resolve LIVE via JOIN on topic_key AND is_live=true. Retired topic_ids must not be used (memory: project_compass_live_topic_ids).
- Evidence rules (NON-NEGOTIABLE): 100% cited; no evidence = NO value (blank spoke); never default to Neutral/Likely; pick the chair the evidence matches (chairs model, not polarity).
- Skip judicial-* topics — these are non-judicial county officials.
- coverage.js label MUST be `'Washington County, OR'` (the bare `'Washington County'` is taken by UT geo_id 49053 at line 238 — collision). Add after the Multnomah entry. Include `hasContext: true`.
- Keep tokens slug / schema_migrations / photo_origin_url out of all .sql comments (grep gates).
- Tailwind hygiene: do not introduce raw Windows backslash paths in committed files (memory: feedback_tailwind_scans_planning_md).
</critical_facts>

<wave_0_dependency>
The inline orchestrator confirms the live topic set BEFORE research: `SELECT id, topic_key FROM inform.compass_topics WHERE is_live = true ORDER BY topic_key;` — this is the canonical topic list each research agent works through. Resolves RESEARCH assumption A6.
</wave_0_dependency>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 per-commissioner stance migrations (1122-1126) from one-at-a-time research</name>
  <files>C:/EV-Accounts/backend/migrations/1122_washco_harrington_stances.sql, C:/EV-Accounts/backend/migrations/1123_washco_fai_stances.sql, C:/EV-Accounts/backend/migrations/1124_washco_treece_stances.sql, C:/EV-Accounts/backend/migrations/1125_washco_snider_stances.sql, C:/EV-Accounts/backend/migrations/1126_washco_willey_stances.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/1057_clark_county_commission_naft_stances.sql (PRIMARY ANALOG — exact CTE -> JOIN compass_topics -> dual INSERT answers+context shape)
    - .planning/phases/175-washington-county-commission-deep-seed/175-PATTERNS.md (the 1120-1124 stances section — full structure + key rules)
    - .planning/phases/175-washington-county-commission-deep-seed/175-RESEARCH.md (SECTION E — stance schema; live-topic resolution)
    - .planning/phases/175-washington-county-commission-deep-seed/175-01-SUMMARY.md (the 5 politician UUIDs)
  </read_first>
  <action>
    For each commissioner IN ORDER (Harrington Chair -> Fai D1 -> Treece D2 -> Snider D3 -> Willey D4), the inline orchestrator dispatches ONE research agent at a time covering ALL live compass topics from the Wave-0 topic list; the executor then authors that commissioner's migration from the agent's cited evidence. Each migration clones the 1057 shape: BEGIN; CTE `s(topic_key, val, reasoning, sources)` with one VALUES row per topic that has cited evidence (omit topics entirely where there is no public record — honest blank spoke, NOT a defaulted value); `t` = `s JOIN inform.compass_topics ct ON ct.topic_key = s.topic_key AND ct.is_live = true`; INSERT inform.politician_answers (politician_id, topic_id, value) SELECT the uuid cast to uuid, topic_id, val FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET value=EXCLUDED.value; INSERT inform.politician_context (politician_id, topic_id, reasoning, sources) SELECT the uuid, topic_id, reasoning, sources FROM t ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning=EXCLUDED.reasoning, sources=EXCLUDED.sources; COMMIT. Use the politician UUID from Plan 01's SUMMARY (matched by external_id: Harrington -410100, Fai -410110, Treece -410111, Snider -410112, Willey -410113). Every value 1-5 is the discrete chair the evidence matches (chairs, not polarity); every row carries at least one source URL in the sources array; skip judicial-* topics. Apply each via psql -f (audit-only — no ledger INSERT). Aim for the 18-21+ depth of prior deep-seeds where evidence supports it; thin records yield honest blanks. Keep the tokens slug / schema_migrations / photo_origin_url out of all comments.
  </action>
  <verify>
    <automated>for f in 1122_washco_harrington 1123_washco_fai 1124_washco_treece 1125_washco_snider 1126_washco_willey; do grep -c "is_live = true" "C:/EV-Accounts/backend/migrations/${f}_stances.sql"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist: 1122_washco_harrington_stances.sql, 1123_washco_fai_stances.sql, 1124_washco_treece_stances.sql, 1125_washco_snider_stances.sql, 1126_washco_willey_stances.sql.
    - Each file JOINs `inform.compass_topics` on `topic_key` AND `is_live = true` (no hardcoded topic UUIDs).
    - Each file does a dual INSERT into `inform.politician_answers` AND `inform.politician_context` with ON CONFLICT (politician_id, topic_id) DO UPDATE.
    - Each VALUES row has a non-empty sources array (100% cited — no uncited rows).
    - No file contains a `judicial` topic_key.
    - No file contains `INSERT INTO supabase_migrations` (audit-only): `grep -v '^--' <file> | grep -c "supabase_migrations"` = 0 for each.
    - DB scan after apply: 0 uncited answers, 0 answers without a paired context row, 0 answers on an inactive/retired topic (see verification).
  </acceptance_criteria>
  <done>5 audit-only stance migrations authored from one-at-a-time evidence-only research and applied; 100% cited; honest blanks; no defaults; no judicial topics; no ledger registration.</done>
</task>

<task type="auto">
  <name>Task 2: Surface Washington County, OR in coverage.js</name>
  <files>src/lib/coverage.js</files>
  <read_first>
    - src/lib/coverage.js (the COVERAGE_COUNTIES block lines 225-241 and ALL_COVERAGE_AREAS at ~258)
    - .planning/phases/175-washington-county-commission-deep-seed/175-PATTERNS.md (the coverage.js edit section — label-collision rule)
  </read_first>
  <action>
    Add ONE entry to the COVERAGE_COUNTIES array immediately after the Multnomah County entry (line 229): an object with label `'Washington County, OR'`, browseGovernmentList `['41067']`, browseStateAbbrev `'OR'`, and hasContext `true`. The label MUST carry the `, OR` suffix to avoid collision with the existing UT `'Washington County'` entry at line 238 — a bare `'Washington County'` would create a duplicate typeahead label. Do not touch any other entry. Do not introduce any raw Windows backslash path anywhere in the file.
  </action>
  <verify>
    <automated>grep -c "Washington County, OR'" "C:/Transparent Motivations/essentials/src/lib/coverage.js"</automated>
  </verify>
  <acceptance_criteria>
    - coverage.js contains exactly one line with `label: 'Washington County, OR'`.
    - That entry has `browseGovernmentList: ['41067']`, `browseStateAbbrev: 'OR'`, and `hasContext: true`.
    - The entry sits immediately after the Multnomah County entry.
    - The existing UT `Washington County` entry (geo_id 49053) is unchanged.
    - `cd "C:/Transparent Motivations/essentials" && node -e "import('./src/lib/coverage.js').then(m=>{const x=m.COVERAGE_COUNTIES.filter(e=>(e.browseGovernmentList||[]).includes('41067'));if(x.length!==1||x[0].label!=='Washington County, OR'||!x[0].hasContext)process.exit(1);console.log('OK')})"` prints OK.
  </acceptance_criteria>
  <done>coverage.js surfaces Washington County, OR with the hasContext purple chip; no label collision; module still imports cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web sources -> research agent -> .sql | Stance evidence (text + source URLs) authored into version-controlled SQL |
| stance .sql -> Postgres | Author-controlled SQL applied by orchestrator (trusted) |
| coverage.js -> frontend bundle | Author-controlled config consumed by the static build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-S1 | Repudiation | Uncited / fabricated stance value | mitigate | 100%-cited rule enforced; DB scan rejects any answer lacking a paired context row with sources; no-evidence -> omit row (honest blank), never default |
| T-175-S2 | Tampering | Stance written against a retired/inactive topic | mitigate | topic_id resolved via JOIN on is_live=true only; DB scan for answers on inactive topics = 0 |
| T-175-S3 | Information Disclosure | Party shown on profile | mitigate | Antipartisan rule: party may be stored but never displayed; stances carry no party field |
| T-175-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced (RESEARCH Package Legitimacy Audit: none) |

No HIGH-severity threats: SQL is parameter-free literal data authored from cited evidence and applied via psql; coverage.js is a static config edit; no user input or auth surface.
</threat_model>

<verification>
Inline-orchestrator gates after each stance migration is applied and after the coverage.js edit:

1. Uncited scan = 0: `SELECT COUNT(*) FROM inform.politician_answers a JOIN essentials.politicians p ON p.id=a.politician_id WHERE p.external_id BETWEEN -410113 AND -410100 AND NOT EXISTS (SELECT 1 FROM inform.politician_context c WHERE c.politician_id=a.politician_id AND c.topic_id=a.topic_id AND array_length(c.sources,1) >= 1);` -> 0.
2. Unpaired scan = 0: every politician_answers row for these 5 politicians has a matching politician_context row (and vice versa).
3. Inactive-topic scan = 0: `SELECT COUNT(*) FROM inform.politician_answers a JOIN essentials.politicians p ON p.id=a.politician_id JOIN inform.compass_topics t ON t.id=a.topic_id WHERE p.external_id BETWEEN -410113 AND -410100 AND t.is_live = false;` -> 0.
3b. No judicial topics: same join filtered to topic_key LIKE 'judicial%' -> 0.
4. Stance presence: each of the 5 politicians has >= 1 answer (Chair likely deepest; Snider/Willey may be thinner — honest blanks acceptable, but each has at least some cited stance or a documented zero-evidence note in the SUMMARY).
5. coverage.js: grep confirms one `Washington County, OR` entry; module imports cleanly via node.
6. (Manual phase-gate) Open the county browse link in the live app and confirm Chair-first ordering + populated compass + the purple chip surfaces.
</verification>

<success_criteria>
- 5 audit-only stance migrations authored one-at-a-time, evidence-only, 100% cited, no defaults, honest blanks, no judicial topics, no ledger registration.
- DB scans: 0 uncited, 0 unpaired, 0 inactive-topic, 0 judicial.
- coverage.js surfaces `Washington County, OR` with the hasContext purple chip (no UT collision).
- Satisfies ROADMAP success criteria #3 (evidence-only stances) and #4 (hasContext chip).
</success_criteria>

<output>
Create `.planning/phases/175-washington-county-commission-deep-seed/175-03-SUMMARY.md` when done. Record per-commissioner stance count + notable honest blanks, the 4 DB scan outputs (all 0), and the coverage.js diff. Note any commissioner with zero usable evidence (documented, not fabricated).
</output>
