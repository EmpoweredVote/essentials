---
phase: 109-ma-tier-2-cities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/351_worcester_government.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-351.ts
autonomous: true
requirements: [MA-TIER2-01]
must_haves:
  truths:
    - "A Worcester address returns a LOCAL section listing Mayor Petty + 10 city councillors"
    - "Mayor Joseph M. Petty is in the LOCAL_EXEC district; the 10 councillors are in the LOCAL district"
    - "All 11 Worcester officials have a non-NULL office_id"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/351_worcester_government.sql"
      provides: "Worcester government + chamber + 2 districts + 11 politicians + 11 offices (per MA-TIER2-01)"
      contains: "City of Worcester, Massachusetts, US"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-351.ts"
      provides: "Apply + smoke-test harness for migration 351"
  key_links:
    - from: "essentials.offices"
      to: "essentials.districts (geo_id=2582000)"
      via: "district_id FK"
      pattern: "geo_id = '2582000'"
    - from: "essentials.politicians (office_id)"
      to: "essentials.offices"
      via: "office_id back-fill"
      pattern: "office_id IS NULL"
---

<objective>
Seed the City of Worcester government so any Worcester address returns a populated LOCAL section with Mayor Joseph M. Petty plus the 10 sitting city councillors (5 at-large + 5 district). Implements MA-TIER2-01.

Purpose: Worcester is Massachusetts' second-largest city. Without a seeded government, Worcester residents see an empty LOCAL section. This is a pure DB-seeding task — Worcester's G4110 geofence (geo_id='2582000') is already loaded from v5.0.
Output: Migration `351_worcester_government.sql` + apply harness `_apply-migration-351.ts`, applied to production via Supabase MCP, with all post-verification gates passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-ma-tier-2-cities/109-RESEARCH.md
@.planning/phases/109-ma-tier-2-cities/109-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 351 — Worcester government</name>
  <files>C:/EV-Accounts/backend/migrations/351_worcester_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/347_boston_government.sql (the exact-match analog: 7-step pattern — header, pre-flight DO blocks, government/chamber/district INSERTs, WITH ins_p politician+office blocks, office_id back-fill, post-verification DO block, ledger INSERT)
    - .planning/phases/109-ma-tier-2-cities/109-PATTERNS.md (lines 25-264: full Worcester migration pattern with concrete SQL)
    - .planning/phases/109-ma-tier-2-cities/109-RESEARCH.md (lines 66-87: Worcester roster; lines 282-353: Pattern 1; lines 515-541: pitfalls)
  </read_first>
  <action>
    Create migration 351 following the 347_boston_government.sql 7-step structure, adapted for Tier 2 (NO per-district geofences — single city geo_id only).

    Step 0 — Header comment block: title "Migration 351: City of Worcester government (MA-TIER2-01)"; restate the CRITICAL rules: slug is GENERATED ALWAYS (omit from chamber INSERT); governments has NO unique constraint on geo_id (use WHERE NOT EXISTS guard); districts.state='ma' (lowercase), governments.state='MA' (uppercase), offices.representing_state='MA' (uppercase); mtfcc=NULL on all LOCAL/LOCAL_EXEC districts; party=NULL (antipartisan); is_appointed=false for all 11 (all popularly elected).

    Step 1 — Pre-flight DO blocks: (a) NOTICE if government 'City of Worcester, Massachusetts, US' already exists; (b) RAISE EXCEPTION if Worcester G4110 geofence (geo_id='2582000', mtfcc='G4110') is absent; (c) RAISE EXCEPTION if external_id range -258200011..-258200001 is not clear.

    Step 2 — Government row: INSERT essentials.governments (gen_random_uuid(), 'City of Worcester, Massachusetts, US', 'LOCAL', 'MA', 'Worcester', '2582000') guarded by WHERE NOT EXISTS on name.

    Step 3 — Chamber row: INSERT essentials.chambers (id, name='City Council', name_formal='Worcester City Council', government_id) — NO slug column — guarded by WHERE NOT EXISTS on (name, government_id).

    Step 4 — Districts: (a) LOCAL_EXEC: gen_random_uuid(), 'LOCAL_EXEC', 'ma', '2582000', 'Worcester (Citywide)', NULL — guarded; (b) LOCAL: gen_random_uuid(), 'LOCAL', 'ma', '2582000', 'Worcester', NULL — guarded.

    Step 5 — 11 politician+office WITH ins_p blocks (party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true; ON CONFLICT (external_id) DO NOTHING RETURNING id; outer office INSERT CROSS JOIN ins_p with NOT EXISTS guard on (district_id, politician_id)):
      - Block 1: Joseph M. Petty, -258200001, title='Mayor', LOCAL_EXEC district (per Open Question 1 / A2: ONE office row in LOCAL_EXEC, title='Mayor' — no separate at-large office row)
      - Block 2: Khrystian E. King, -258200002, title='City Councilor', LOCAL district
      - Block 3: Satya B. Mitra, -258200003, title='City Councilor', LOCAL
      - Block 4: Kathleen M. Toomey, -258200004, title='City Councilor', LOCAL (full_name 'Kathleen M. Toomey' — not 'Kate')
      - Block 5: Morris A. Bergman, -258200005, title='City Councilor', LOCAL
      - Block 6: Gary Rosen, -258200006, title='City Councilor', LOCAL
      - Block 7: Tony Economou, -258200007, title='City Councilor (District 1)', LOCAL
      - Block 8: Robert A. Bilotta, -258200008, title='City Councilor (District 2)', LOCAL
      - Block 9: John P. Fresolo, -258200009, title='City Councilor (District 3)', LOCAL
      - Block 10: Luis A. Ojeda, -258200010, title='City Councilor (District 4)', LOCAL
      - Block 11: Jose A. Rivera, -258200011, title='City Councilor (District 5)', LOCAL
      Set first_name / last_name by splitting full_name (e.g. 'Joseph'/'Petty'). All councillor offices use is_appointed_position=false, is_vacant=false, role_canonical=NULL.

    Step 6 — office_id back-fill: UPDATE essentials.politicians SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -258200011 AND -258200001 AND p.office_id IS NULL.

    Step 7 — Post-verification DO block with gates: 1 government, 1 chamber, 2 districts (LOCAL_EXEC + LOCAL for geo_id='2582000'), 11 politicians, 11 offices, 0 section-split orphans for geo_id='2582000', 0 NULL office_ids. RAISE EXCEPTION on any gate miss; final RAISE NOTICE on pass.

    Step 8 — Ledger: INSERT INTO supabase_migrations.schema_migrations (version) VALUES ('351') ON CONFLICT DO NOTHING.

    Wrap steps 2-8 in BEGIN/COMMIT (pre-flight DO blocks may run outside or inside the transaction matching the 347 analog).

    Do NOT create per-district geofences. Do NOT use 'Robert Sullivan' anywhere (that is Brockton's old mayor). Use exact string '2582000' in every geo_id reference.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/migrations/351_worcester_government.sql','utf8'); const ids=['-258200001','-258200002','-258200003','-258200004','-258200005','-258200006','-258200007','-258200008','-258200009','-258200010','-258200011']; const miss=ids.filter(i=>!s.includes(i)); if(miss.length){console.error('MISSING external_ids:',miss);process.exit(1)} if(!s.includes(\"VALUES ('351')\")&&!s.includes('VALUES (\'351\')')){console.error('missing ledger 351');process.exit(1)} if(/slug/i.test(s.split('chambers')[1]?.slice(0,200)||'')){console.error('slug in chamber insert');process.exit(1)} console.log('OK 11 ids, ledger, no slug')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/migrations/351_worcester_government.sql
    - File contains exact string 'City of Worcester, Massachusetts, US'
    - All 11 external_ids -258200001 through -258200011 appear in the file
    - Petty's office block uses title='Mayor' and links to a LOCAL_EXEC district; no separate at-large office row for Petty
    - District INSERTs use mtfcc=NULL (no 'G4110' literal on any LOCAL/LOCAL_EXEC district row)
    - Chamber INSERT column list does NOT contain 'slug'
    - Ledger INSERT version '351' present
    - String 'Robert Sullivan' does NOT appear anywhere in the file
  </acceptance_criteria>
  <done>Migration 351 SQL file written with all 11 officials, 2 districts, post-verification gates, and ledger entry, following the 347 analog adapted for Tier 2 (no per-district geofences).</done>
</task>

<task type="auto">
  <name>Task 2: Write apply harness _apply-migration-351.ts</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-351.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-347.ts (the apply-harness analog: pg Pool from DATABASE_URL, readFileSync the migration, pool.query, then smoke-test queries with expected-count logging)
    - .planning/phases/109-ma-tier-2-cities/109-PATTERNS.md (lines 611-690: apply-script pattern + per-city adaptation table)
  </read_first>
  <action>
    Create _apply-migration-351.ts by adapting _apply-migration-347.ts. Read and execute migrations/351_worcester_government.sql via a pg Pool (connectionString=process.env['DATABASE_URL'], ssl rejectUnauthorized:false). After pool.query(sql), run 5 smoke tests logging actual-vs-expected:
      1. Worcester government rows where name='City of Worcester, Massachusetts, US' → expect 1
      2. politicians where external_id BETWEEN -258200011 AND -258200001 → expect 11
      3. districts where state='ma' AND geo_id='2582000' AND district_type IN ('LOCAL_EXEC','LOCAL') → expect 2
      4. politicians in that external_id range with office_id IS NULL → expect 0
      5. ledger row version='351' → PRESENT/MISSING
    On error, console.error and process.exit(1); finally pool.end().

    NOTE: This script is the human-runnable fallback. The authoritative apply path in Task 3 is the Supabase MCP execute_sql tool. This harness still exists for parity with Phase 108 and for local smoke testing.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/scripts/_apply-migration-351.ts','utf8'); if(!s.includes('351_worcester_government.sql')){process.exit(1)} if(!s.includes('-258200011')||!s.includes('2582000')){process.exit(1)} console.log('OK apply-351')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at C:/EV-Accounts/backend/scripts/_apply-migration-351.ts
    - References migrations/351_worcester_government.sql
    - Contains the 5 smoke-test queries with expected counts (1, 11, 2, 0, ledger)
    - Reads DATABASE_URL from process.env (no hardcoded credentials)
  </acceptance_criteria>
  <done>Apply harness written for migration 351 with all 5 smoke tests matching Worcester's expected counts.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply migration 351 to production via Supabase MCP</name>
  <files>C:/EV-Accounts/backend/migrations/351_worcester_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/351_worcester_government.sql (the migration to apply — read the full file before executing)
  </read_first>
  <action>
    Apply migration 351 to the production Supabase database using the Supabase MCP tool `mcp__supabase-local__execute_sql` (this project applies migrations directly via the Supabase MCP, NOT via `supabase db push` or the apply .ts harness). Pass the full contents of 351_worcester_government.sql as the query. The migration's own post-verification DO block (Step 7) will RAISE EXCEPTION if any gate fails — if execution succeeds without exception, all gates passed.

    After applying, run the scoped MA section-split query via execute_sql to confirm the orphan count decremented:
      SELECT COUNT(*) AS orphan_count FROM essentials.geofence_boundaries g WHERE g.state='25' AND g.mtfcc='G4110' AND NOT EXISTS (SELECT 1 FROM essentials.districts d WHERE d.geo_id=g.geo_id);
    Expect 55 (was 56 before Phase 109). Worcester's geo_id 2582000 must no longer be an orphan.

    feedback_supabase_local_is_remote: mcp__supabase-local IS the production DB — this write is live.
  </action>
  <verify>
    <automated>MISSING — verified via Supabase MCP execute_sql at runtime: SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -258200011 AND -258200001 returns 11; section-split orphan_count returns 55</automated>
  </verify>
  <acceptance_criteria>
    - mcp__supabase-local__execute_sql applied migration 351 without raising an exception (all Step 7 gates passed)
    - SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -258200011 AND -258200001 returns 11
    - SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -258200011 AND -258200001 AND office_id IS NULL returns 0
    - Scoped MA section-split orphan_count returns 55 (decremented from 56)
    - Ledger row version='351' present in supabase_migrations.schema_migrations
  </acceptance_criteria>
  <done>Migration 351 applied to production; all post-verification gates passed; section-split orphan count is 55.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → production DB | SQL executed via Supabase MCP against live production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering | migration 351 re-run | mitigate | WHERE NOT EXISTS guard on government; ON CONFLICT (external_id) DO NOTHING on politicians; NOT EXISTS guard on (district_id, politician_id) for offices — full migration is idempotent |
| T-109-02 | Information Disclosure | seeded politician records | accept | All records are public elected officials sourced from official city websites; no PII beyond public-office data |
| T-109-03 | Tampering | NULL office_id leaving councillors unroutable | mitigate | Step 6 office_id back-fill + Step 7 gate (0 NULL office_ids) raises exception on failure |
</threat_model>

<verification>
- Migration 351 applied without exception (post-verification DO block gates all pass)
- 11 Worcester politicians present (external_id -258200011..-258200001), 0 with NULL office_id
- MA section-split orphan count decremented 56 → 55
- Worcester government, City Council chamber, LOCAL_EXEC + LOCAL districts all present
</verification>

<success_criteria>
A Worcester address routes (via the existing G4110 geofence at geo_id='2582000') to a LOCAL section listing Mayor Petty + 10 councillors, all with non-NULL office_ids. MA-TIER2-01's Worcester portion is satisfied.
</success_criteria>

<output>
Create `.planning/phases/109-ma-tier-2-cities/109-01-SUMMARY.md` when done
</output>
