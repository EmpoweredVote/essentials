---
phase: 79-or-landing-elections-discovery
plan: 3
type: execute
wave: 3
depends_on: [79-01, 79-02]
files_modified:
  - C:/EV-Accounts/backend/migrations/generate_or_legislative_races.ps1
  - C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql
autonomous: true
requirements: []
tags: [oregon, elections, legislature, races, migration, powershell-generator]

must_haves:
  truths:
    - "essentials.races contains 30 OR State Senate race rows (SD-01 through SD-30), each linked to the correct STATE_UPPER office_id"
    - "essentials.races contains 60 OR State House race rows (HD-01 through HD-60), each linked to the correct STATE_LOWER office_id"
    - "OR State Senate District 1 and OR State House District 1 race rows have DIFFERENT office_ids (district_type disambiguation works)"
    - "All 90 legislative race rows have empty race_candidates"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/generate_or_legislative_races.ps1"
      provides: "PowerShell generator that emits 90 DO blocks for OR legislative scaffold"
      contains: "STATE_UPPER"
    - path: "C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql"
      provides: "90 DO blocks creating 30 Senate + 60 House race rows"
      contains: "OR State Senate District 1"
  key_links:
    - from: "generator script"
      to: "migration 239 SQL file"
      via: "[System.IO.File]::WriteAllLines + UTF8Encoding($false)"
      pattern: "UTF8Encoding"
    - from: "migration 239 race rows"
      to: "essentials.offices via STATE_UPPER/STATE_LOWER districts"
      via: "district_type disambiguation (CRITICAL — geo_id collides)"
      pattern: "district_type = 'STATE_(UPPER|LOWER)'"
---

<objective>
Generate and apply the 90-race OR state legislative scaffold: 30 STATE_UPPER (Senate SD-01..SD-30) + 60 STATE_LOWER (House HD-01..HD-60), all linked to the OR 2026 General election with empty race_candidates.

Purpose: Implements D-06 (full 90-race OR state legislative scaffold). Uses the established ME Phase 55 PowerShell generator pattern (per 79-PATTERNS.md). Mandatory district_type disambiguation (STATE_UPPER vs STATE_LOWER) because SD and HD share the same geo_id pattern '41001'..'41030'/'41060' per RESEARCH.md Pitfall 1 and critical_research_findings #5.

Output:
- Generator script C:/EV-Accounts/backend/migrations/generate_or_legislative_races.ps1
- Generated migration C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql (90 DO blocks)
- 90 new rows in essentials.races (total OR race count rises from 8 to 98)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-or-landing-elections-discovery/79-CONTEXT.md
@.planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md
@.planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md
@.planning/phases/79-or-landing-elections-discovery/79-01-SUMMARY.md
@.planning/phases/79-or-landing-elections-discovery/79-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PowerShell generator generate_or_legislative_races.ps1 (D-06)</name>
  <files>C:/EV-Accounts/backend/migrations/generate_or_legislative_races.ps1</files>
  <read_first>
    - .planning/phases/79-or-landing-elections-discovery/79-PATTERNS.md (section "migrations/generate_or_legislative_races.ps1" — full generator skeleton)
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (sections "PowerShell Generator for 90 OR Legislative Races", "Pitfall 1: STATE_UPPER vs. STATE_LOWER geo_id Collision", "Pitfall 5: BOM in PowerShell-generated SQL")
    - C:/EV-Accounts/backend/migrations/generate_me_legislative_races.ps1 (direct analog from Phase 55 — read structure only)
  </read_first>
  <action>
    Create a PowerShell script at exactly the path `C:/EV-Accounts/backend/migrations/generate_or_legislative_races.ps1`. The script must follow the skeleton in 79-PATTERNS.md "migrations/generate_or_legislative_races.ps1" section. It must initialize an `$output` array starting with header comments (`-- Migration 239: OR 2026 Legislative Race Scaffold`, `-- Generated by generate_or_legislative_races.ps1`, `-- 30 Senate districts + 60 House districts = 90 rows, general election only`). Then loop `for ($n = 1; $n -le 30; $n++)` producing 30 DO blocks for Senate: `$geo_id = "41" + ($n.ToString().PadLeft(3, '0'))` (yields '41001'..'41030'); `$pos = "OR State Senate District $n"`. Each Senate DO block must include the inner SELECT exactly: `(SELECT o.id FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id JOIN essentials.chambers ch ON ch.id = o.chamber_id WHERE d.geo_id = '$geo_id' AND d.district_type = 'STATE_UPPER' AND d.state = 'or' AND ch.name = 'Oregon Senate' LIMIT 1)`. Then loop `for ($n = 1; $n -le 60; $n++)` producing 60 DO blocks for House: same geo_id formula (yields '41001'..'41060'); `$pos = "OR State House District $n"`. Each House DO block uses `district_type = 'STATE_LOWER'` and `ch.name = 'Oregon House of Representatives'`. All 90 INSERTs use `primary_party = NULL` and `seats = 1` and end with `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`. Each DO block first does `SELECT id INTO v_general_id FROM essentials.elections WHERE name = 'OR 2026 General' AND state = 'OR';`. Use the PowerShell here-string format `@" ... "@` with PostgreSQL `$$` escaped as `` `$`$ `` per the skeleton in 79-PATTERNS.md. CRITICAL — output write MUST use: `[System.IO.File]::WriteAllLines((Join-Path $PSScriptRoot '239_or_legislative_races.sql'), $output, [System.Text.UTF8Encoding]::new($false))`. Do NOT use `Out-File -Encoding UTF8` (writes BOM that PostgreSQL rejects per Pitfall 5). Use `$PSScriptRoot` so the script writes its sibling SQL into the migrations directory regardless of cwd. End with `Write-Host "Generated 239_or_legislative_races.sql with $($output.Count) lines"`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='C:/EV-Accounts/backend/migrations/generate_or_legislative_races.ps1';const s=fs.readFileSync(p,'utf8');const must=['for (\$n = 1; \$n -le 30','for (\$n = 1; \$n -le 60','STATE_UPPER','STATE_LOWER','Oregon Senate','Oregon House of Representatives',\"d.state = 'or'\",'UTF8Encoding','WriteAllLines','OR 2026 General','ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING'];for(const t of must){if(!s.includes(t))throw new Error('missing: '+t);}const forbid=['Out-File -Encoding UTF8','cron_active','election_name'];for(const t of forbid){if(s.includes(t))throw new Error('forbidden: '+t);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/generate_or_legislative_races.ps1`
    - File contains both loop headers: `for ($n = 1; $n -le 30` and `for ($n = 1; $n -le 60`
    - File contains literal `STATE_UPPER` and literal `STATE_LOWER` (mandatory disambiguation per Pitfall 1)
    - File contains literal `Oregon Senate` and literal `Oregon House of Representatives`
    - File contains literal `d.state = 'or'` (lowercase, per OR district casing rule)
    - File contains literal `[System.Text.UTF8Encoding]` and `WriteAllLines` (no-BOM emit per Pitfall 5)
    - File does NOT contain `Out-File -Encoding UTF8` (would emit BOM)
    - File does NOT contain `cron_active` or `election_name`
    - File uses `$PSScriptRoot` for output path resolution
    - File contains `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`
  </acceptance_criteria>
  <done>Generator script written with both loops, district_type disambiguation, UTF-8 NoBOM emission, and ON CONFLICT idempotency.</done>
</task>

<task type="auto">
  <name>Task 2: Run generator to produce 239_or_legislative_races.sql</name>
  <files>C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/generate_or_legislative_races.ps1 (the script written in Task 1)
  </read_first>
  <action>
    Execute the generator using PowerShell. From the working directory `C:/EV-Accounts/backend/migrations/`, run: `powershell -ExecutionPolicy Bypass -File generate_or_legislative_races.ps1`. The script writes `239_or_legislative_races.sql` in its sibling directory via $PSScriptRoot. After execution, verify the output file exists at `C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql` and: (1) first 3 bytes are NOT 0xEF 0xBB 0xBF (no BOM); (2) file contains exactly 30 instances of `OR State Senate District ` (note trailing space — to avoid matching District 10 against District 1 etc, count by literal `'OR State Senate District ` followed by digits and apostrophe); (3) file contains exactly 60 instances of `OR State House District `; (4) file contains both literal strings `OR State Senate District 1'` and `OR State Senate District 30'`; (5) file contains both literal strings `OR State House District 1'` and `OR State House District 60'`; (6) file contains literal `district_type = 'STATE_UPPER'` and `district_type = 'STATE_LOWER'`. Do not hand-edit the generated SQL — if any check fails, fix the generator and re-run.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql';const b=fs.readFileSync(p);if(b[0]===0xEF&&b[1]===0xBB&&b[2]===0xBF){console.error('FAIL: BOM');process.exit(1);}const s=b.toString('utf8');const sen=(s.match(/'OR State Senate District \d+'/g)||[]);const hou=(s.match(/'OR State House District \d+'/g)||[]);if(sen.length!==30)throw new Error('expected 30 Senate, got '+sen.length);if(hou.length!==60)throw new Error('expected 60 House, got '+hou.length);if(!s.includes(\"'OR State Senate District 1'\")||!s.includes(\"'OR State Senate District 30'\"))throw new Error('Senate bounds missing');if(!s.includes(\"'OR State House District 1'\")||!s.includes(\"'OR State House District 60'\"))throw new Error('House bounds missing');if(!s.includes(\"district_type = 'STATE_UPPER'\")||!s.includes(\"district_type = 'STATE_LOWER'\"))throw new Error('district_type disambig missing');console.log('OK: '+sen.length+' Senate + '+hou.length+' House');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql`
    - File is UTF-8 without BOM (first 3 bytes NOT 0xEF 0xBB 0xBF)
    - File contains exactly 30 unique position_name literals matching `'OR State Senate District N'` for N in 1..30
    - File contains exactly 60 unique position_name literals matching `'OR State House District N'` for N in 1..60
    - File contains literal `district_type = 'STATE_UPPER'` (at least 30 occurrences expected, in the Senate DO blocks)
    - File contains literal `district_type = 'STATE_LOWER'` (at least 60 occurrences expected, in the House DO blocks)
    - File contains literal `'Oregon Senate'` and literal `'Oregon House of Representatives'` (chamber name disambiguation)
    - File contains literal `'OR 2026 General'` (election name used in subquery)
    - File contains `ON CONFLICT (election_id, position_name) WHERE primary_party IS NULL DO NOTHING`
  </acceptance_criteria>
  <done>Generated SQL file exists, NoBOM, contains 30 Senate + 60 House DO blocks with district_type disambiguation.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 239 to Supabase and verify (D-06)</name>
  <files>C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql (apply only)</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/239_or_legislative_races.sql (the file produced in Task 2)
    - .planning/phases/79-or-landing-elections-discovery/79-RESEARCH.md (section "Validation Architecture — After Plan 03", "Pitfall 1")
  </read_first>
  <action>
    Apply migration 239 to the live Supabase production DB via mcp__supabase-local__execute_sql. The file is large (90 DO blocks) — submit the entire SQL contents in a single execute_sql call. After applying, run these verifications:
    1. Total OR race count: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR';` — expected 98 (8 from Plan 02 + 90 new).
    2. Senate count: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'OR State Senate District %';` — expected 30.
    3. House count: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'OR State House District %';` — expected 60.
    4. office_id non-null: `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND (r.position_name LIKE 'OR State Senate%' OR r.position_name LIKE 'OR State House%') AND r.office_id IS NULL;` — expected 0 (all 90 must have non-null office_id).
    5. CRITICAL district_type disambiguation spot-check (Pitfall 1): `SELECT r.position_name, r.office_id, d.district_type, ch.name FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.chambers ch ON ch.id=o.chamber_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name IN ('OR State Senate District 1', 'OR State House District 1') ORDER BY r.position_name;` — expected 2 rows with DIFFERENT office_ids; Senate District 1 row must have district_type='STATE_UPPER' and chamber 'Oregon Senate'; House District 1 row must have district_type='STATE_LOWER' and chamber 'Oregon House of Representatives'.
    6. Idempotency: re-execute migration 239 SQL — count remains 98.
    7. Section-split check: `SELECT COUNT(*) FROM essentials.geofence_boundaries gb WHERE gb.state='41' AND gb.geo_id NOT IN (SELECT geo_id FROM essentials.districts WHERE state IN ('or','OR','41'));` — expected 0.
    8. race_candidates emptiness: `SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND (r.position_name LIKE 'OR State Senate%' OR r.position_name LIKE 'OR State House%');` — expected 0.
  </action>
  <verify>
    <automated>echo "Manual MCP verification — see acceptance_criteria for explicit queries via mcp__supabase-local__execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR'` returns exactly 98
    - `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'OR State Senate District %'` returns exactly 30
    - `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND r.position_name LIKE 'OR State House District %'` returns exactly 60
    - `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND (r.position_name LIKE 'OR State Senate%' OR r.position_name LIKE 'OR State House%') AND r.office_id IS NULL` returns 0 (no NULL office_ids — would indicate Pitfall 1 disambiguation failure)
    - Disambiguation spot-check: OR State Senate District 1 and OR State House District 1 have DIFFERENT office_ids; Senate row joins to chamber 'Oregon Senate' via STATE_UPPER district; House row joins to chamber 'Oregon House of Representatives' via STATE_LOWER district
    - Re-applying migration 239 a second time produces no error and `SELECT COUNT(*) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR'` still returns 98 (idempotent)
    - Section-split check returns 0
    - `SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.state='OR' AND (r.position_name LIKE 'OR State Senate%' OR r.position_name LIKE 'OR State House%')` returns 0 (D-11)
  </acceptance_criteria>
  <done>Migration 239 applied; 90 new legislative race rows with disambiguated office_ids; total OR race count 98; idempotency confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → live Supabase production DB | Migration 239 applied via mcp__supabase-local__execute_sql (90 DO blocks in one call) |
| Generator script → SQL file | PowerShell writes plain-text SQL with UTF-8 NoBOM encoding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Tampering | generator script | accept | Pure code-gen with deterministic loop bounds; reviewable diff in git history |
| T-79-03-02 | Integrity | district_type disambiguation | mitigate | Pitfall 1 explicitly addressed: `STATE_UPPER` and `STATE_LOWER` literals MANDATORY in disambiguation subqueries; Task 3 verification step 4 fails plan if any office_id IS NULL |
| T-79-03-03 | Integrity | UTF-8 BOM in generated SQL | mitigate | Pitfall 5 explicitly addressed: `[System.Text.UTF8Encoding]::new($false)` required, `Out-File -Encoding UTF8` explicitly forbidden in Task 1 verify |
| T-79-03-04 | Denial of Service | Supabase 90-block batch | accept | Single execute_sql call; idempotent via ON CONFLICT; ME Phase 55 ran 372-block equivalent without issue |

Pure data migration phase. No auth, no API, no user input.
</threat_model>

<verification>
1. Total OR race count = 98
2. Senate count = 30, House count = 60
3. All 90 office_ids non-null
4. district_type disambiguation spot-check: SD-01 and HD-01 have different office_ids and join to different chambers
5. Idempotency: re-apply leaves count at 98
6. Section-split check = 0
7. race_candidates count for legislative races = 0
</verification>

<success_criteria>
- 90 OR legislative race rows exist in essentials.races linked to OR 2026 General
- All 90 have non-null office_id (district_type disambiguation succeeded)
- Generator script is reusable for future re-runs
- Plan 79-04 can build on top (race count rises to 105 after Portland city races)
</success_criteria>

<output>
Create `.planning/phases/79-or-landing-elections-discovery/79-03-SUMMARY.md` documenting:
- Final total OR race count (expected 98)
- Senate/House split (30/60)
- Disambiguation spot-check result (Senate D1 office_id vs House D1 office_id)
- Confirmation of idempotency re-run
- Confirmation of section-split check (0)
- Generator script path + invocation command for future use
- Migration 239 ledger entry confirmation
</output>
