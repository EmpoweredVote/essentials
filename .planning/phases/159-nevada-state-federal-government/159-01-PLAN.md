---
phase: 159-nevada-state-federal-government
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/1050_nv_controller.sql
  - C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql
  - C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py
autonomous: false
requirements: [NV-STATE-01]
user_setup: []

must_haves:
  truths:
    - "Andy Matthews (State Controller) exists under the State of Nevada government as the 6th STATE_EXEC official"
    - "The Controller renders with a 600x750 headshot in the politician_photos bucket"
    - "No politician appears under two different governments (no section-split defect)"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/1050_nv_controller.sql"
      provides: "Structural migration: Controller chamber + STATE_EXEC district + politician + office, idempotent, registers as 1050"
      contains: "INSERT INTO essentials.chambers"
    - path: "C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py"
      provides: "Crop-to-4:5 then resize-to-600x750 Lanczos q90 download+upload pipeline for Andy Matthews"
      contains: "crop_to_4_5"
    - path: "C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql"
      provides: "Audit-only politician_images INSERT for Andy Matthews (external_id -3200006)"
      contains: "essentials.politician_images"
  key_links:
    - from: "essentials.chambers (Controller)"
      to: "essentials.governments (geo_id='32')"
      via: "government_id subquery WHERE geo_id='32'"
      pattern: "geo_id = '32'"
    - from: "essentials.offices (Controller)"
      to: "essentials.politicians (external_id=-3200006)"
      via: "office_id back-fill + politician_id link"
      pattern: "external_id = -3200006"
---

<objective>
Create Nevada State Controller Andy Matthews end-to-end — the only net-new official in this entire phase. The other 5 STATE_EXEC constitutional officers (Governor + Lt. Gov + AG + SoS + Treasurer) already exist in the DB with headshots and MUST NOT be re-created. This plan delivers a new chamber, a new STATE_EXEC district, a new politician row, a new office row (structural migration 1050), and a 600x750 headshot (script + audit-only migration 1052).

Purpose: Satisfies the Controller half of NV-STATE-01 (Success Criterion 1 — all 6 constitutional officers render as STATE_EXEC officials with chambers, offices, and 600x750 headshots).
Output: 1050_nv_controller.sql (structural), _tmp-nv-controller-headshot.py (headshot pipeline), 1052_nv_controller_headshot.sql (audit-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**CRITICAL — executor/orchestrator split (gsd-executor has NO supabase MCP):**
- The executor WRITES the migration .sql files and the headshot Python script to disk and documents the exact SQL/queries. The executor does NOT apply migrations or run DB SELECTs.
- The actual `mcp__supabase-local__apply_migration` (1050), the headshot script run + Storage upload, and the `1052` headshot-row apply are INLINE ORCHESTRATOR steps performed by execute-phase, not by the executor.
- Each task below marks which steps are executor-write vs inline-orchestrator-apply.
</execution_context>

<context>
@.planning/phases/159-nevada-state-federal-government/159-RESEARCH.md
@.planning/phases/159-nevada-state-federal-government/159-PATTERNS.md

<interfaces>
<!-- DB-confirmed facts the executor needs. Do NOT re-query — these are verified 2026-06-23. -->
State of Nevada government: id=9bb67edf-1081-4941-8f7d-2e791a5d28a1, geo_id='32', FIPS=32
Andy Matthews: external_id=-3200006 (next after existing -3200001..-3200005), party='Republican', in office since 2023-01-02
Next structural migration number: 1050 (DB MAX=1049 confirmed; STATE.md's "1048" is STALE)
politician_images columns: (id, politician_id, url, type, photo_license) — NO photo_origin_url column exists
chambers.slug is GENERATED ALWAYS — never include in INSERT column list
districts has NO unique constraint — guard every INSERT with WHERE NOT EXISTS
STATE_EXEC districts use state='NV' UPPERCASE (lowercase silently breaks backend routing — the OR-223a lesson)
CDN base: https://kxsdzaojfaibhuzmclfq.storage.supabase.co/storage/v1/object/public/politician_photos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write structural migration 1050_nv_controller.sql</name>
  <files>C:/EV-Accounts/backend/migrations/1050_nv_controller.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/270_md_state_executives.sql (PRIMARY ANALOG — chamber lookup + CTE politician/office + office_id back-fill; note it includes district_id='' and mtfcc='' columns on the districts INSERT)
    - C:/EV-Accounts/backend/migrations/300_va_government_chambers.sql (chamber INSERT idempotency pattern)
    - .planning/phases/159-nevada-state-federal-government/159-PATTERNS.md (section "1050_nv_controller.sql" — the exact 4-step SQL template + structural-registration block)
    - .planning/phases/159-nevada-state-federal-government/159-RESEARCH.md (Live Database Audit + Common Pitfalls 1, 2, 6)
  </read_first>
  <action>
Write the structural migration following the 4-step template in 159-PATTERNS.md. Open with a header comment block stating: file is 1050_nv_controller.sql, Phase 159 NV-STATE-01, STRUCTURAL (registers in supabase_migrations.schema_migrations), idempotent, State of Nevada government id=9bb67edf geo_id='32', external_id=-3200006, slug is GENERATED ALWAYS never include it, and the DB-verified note that 5 of 6 STATE_EXEC officials already exist. Wrap steps 1-4 in a single BEGIN/COMMIT transaction. Pre-flight: a DO block that RAISE EXCEPTION unless exactly 1 essentials.governments row has geo_id='32'. STEP 1 — INSERT a chamber (columns id, name, name_formal, government_id) with name='Controller', name_formal='Nevada State Controller', government_id from a subquery WHERE geo_id='32', guarded by WHERE NOT EXISTS on (name='Controller', same government_id); never list slug. STEP 2 — INSERT a district (mirror the 270_md column list: id, district_type, state, geo_id, label, district_id, mtfcc) with values STATE_EXEC, 'NV' UPPERCASE, '32', 'Nevada Controller', '' (empty district_id), '' (empty mtfcc), guarded by WHERE NOT EXISTS on (district_type='STATE_EXEC', state='NV', label='Nevada Controller') per Pitfall 1. STEP 3 — a WITH ins_p CTE that INSERTs the politician (id, full_name='Andy Matthews', first_name='Andy', last_name='Matthews', party='Republican', is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, external_id=-3200006) with ON CONFLICT (external_id) DO NOTHING RETURNING id, then INSERT the office (id, district_id, chamber_id, politician_id, title='Controller', representing_state='NV', is_appointed_position=false, is_vacant=false, role_canonical=NULL) selecting from the district CROSS JOIN ins_p where p.id IS NOT NULL and guarded by NOT EXISTS on (district_id, chamber_id). The chamber_id subquery MUST scope to the geo_id='32' government (Pitfall 2 — never the US Federal government). STEP 4 — UPDATE the politician office_id back-fill for external_id=-3200006 where office_id IS NULL. COMMIT. Then OUTSIDE the transaction, INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('1050','nv_controller') ON CONFLICT (version) DO NOTHING. Do NOT apply this migration — that is an inline orchestrator step (Task 2).
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1050_nv_controller.sql" &amp;&amp; grep -q "geo_id = '32'" "C:/EV-Accounts/backend/migrations/1050_nv_controller.sql" &amp;&amp; grep -q "STATE_EXEC" "C:/EV-Accounts/backend/migrations/1050_nv_controller.sql" &amp;&amp; grep -q "\-3200006" "C:/EV-Accounts/backend/migrations/1050_nv_controller.sql" &amp;&amp; grep -q "schema_migrations" "C:/EV-Accounts/backend/migrations/1050_nv_controller.sql" &amp;&amp; ! grep -qi "slug" "C:/EV-Accounts/backend/migrations/1050_nv_controller.sql" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/1050_nv_controller.sql exists.
    - Contains exactly one chamber INSERT (name='Controller'), one district INSERT (STATE_EXEC/'NV'/'Nevada Controller'), one politician CTE (external_id=-3200006), one office INSERT, one office_id UPDATE — all idempotent.
    - state value on the district + office is 'NV' UPPERCASE (no lowercase 'nv').
    - Column list 'slug' does NOT appear anywhere in the file.
    - Every government reference uses the geo_id='32' subquery (no hardcoded UUID, no geo_id='51'/'24').
    - schema_migrations registration line for ('1050','nv_controller') appears OUTSIDE the BEGIN/COMMIT block.
  </acceptance_criteria>
  <done>1050_nv_controller.sql is written to disk and passes all acceptance criteria. Not yet applied.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 1050 inline + write the headshot script</name>
  <files>C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_tmp-va-execs-headshots.py (PRIMARY ANALOG — requests→PIL→Storage PUT pipeline, constants, upload headers)
    - .planning/phases/159-nevada-state-federal-government/159-PATTERNS.md (sections "_tmp-nv-controller-headshot.py" + "Python Headshot Upload Pipeline" — crop_to_4_5 function + constants)
    - ~/.claude/commands/find-headshots.md (find-headshots skill — license-compliant sourcing for Andy Matthews)
    - .planning/phases/159-nevada-state-federal-government/159-RESEARCH.md (Headshot Sources by Official + Open Question 1 — Matthews license)
  </read_first>
  <action>
INLINE ORCHESTRATOR STEP (not the executor): apply C:/EV-Accounts/backend/migrations/1050_nv_controller.sql via mcp__supabase-local__apply_migration. Then run the audit SELECT joining politicians→offices→chambers→governments WHERE geo_id='32' AND district_type='STATE_EXEC' — expect 6 rows (the 5 existing + Andy Matthews). Capture Andy Matthews' new politician UUID via SELECT id FROM essentials.politicians WHERE external_id=-3200006.

EXECUTOR STEP: Write _tmp-nv-controller-headshot.py modeled on _tmp-va-execs-headshots.py with constants BUCKET='politician_photos', CDN_BASE (the politician_photos public URL), TARGET_SIZE=(600,750), JPEG_QUALITY=90. Include a crop_to_4_5(img) function (crop-first, never stretch; eyes ~1/3 from top; crop sides if too wide, crop bottom if too tall) exactly as in 159-PATTERNS.md, then resize to 600x750 with Image.Resampling.LANCZOS. The script must resolve Andy Matthews' politician_id at runtime via DB lookup on external_id=-3200006 (preferred per PATTERNS) OR accept it as a CLI arg. Source resolution: per the find-headshots skill, prefer a Wikimedia Commons cc_by/public_domain portrait; document the chosen source URL and license inline in the script as a SOURCE comment; campaign site andyfornevada.com is fallback with license noted as 'unknown'. Upload to politician_photos/{uuid}-headshot.jpg using the x-upsert PUT headers from the analog. Print a one-line manifest: "SUCCESS: -3200006 Andy Matthews {uuid} -> {cdn_url} [license={license}]". Document (as a comment) that the find-headshots license judgment + the actual upload are confirmed by the orchestrator/operator.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py" &amp;&amp; grep -q "crop_to_4_5" "C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py" &amp;&amp; grep -q "600" "C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py" &amp;&amp; grep -q "750" "C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py" &amp;&amp; grep -q "\-3200006" "C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py" &amp;&amp; grep -q "LANCZOS" "C:/EV-Accounts/backend/scripts/_tmp-nv-controller-headshot.py" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INLINE: migration 1050 applied; STATE_EXEC/geo_id='32' audit returns exactly 6 rows; Andy Matthews politician UUID captured.
    - _tmp-nv-controller-headshot.py exists with a crop_to_4_5 function, TARGET_SIZE (600,750), JPEG_QUALITY=90, Image.Resampling.LANCZOS.
    - Script resolves politician_id via runtime DB lookup on external_id=-3200006 (or CLI arg) — never a hardcoded guess.
    - A SOURCE comment documents the chosen headshot URL + license; Wikimedia/public-domain preferred over campaign site.
    - Upload target is politician_photos/{uuid}-headshot.jpg with x-upsert PUT headers.
  </acceptance_criteria>
  <done>Migration 1050 applied (6 STATE_EXEC rows confirmed inline), Andy Matthews UUID known, and the crop-first headshot script is written to disk.</done>
</task>

<task type="auto">
  <name>Task 3: Write audit-only Controller headshot migration 1052</name>
  <files>C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/315_va_headshots.sql (EXACT ANALOG — politician_images INSERT shape)
    - .planning/phases/159-nevada-state-federal-government/159-PATTERNS.md (section "politician_images INSERT — Column Shape" + Migration Counter Reference)
    - .planning/phases/159-nevada-state-federal-government/159-RESEARCH.md (Pitfall 5 — no photo_origin_url)
  </read_first>
  <action>
Write 1052_nv_controller_headshot.sql as an AUDIT-ONLY migration (header comment states: NOT registered in supabase_migrations.schema_migrations; ledger stays at 1050; applied via execute_sql after the headshot upload). A single idempotent INSERT INTO essentials.politician_images with column list exactly (id, politician_id, url, type, photo_license): id=gen_random_uuid(), politician_id from subquery (SELECT id FROM essentials.politicians WHERE external_id=-3200006), url = the CDN politician_photos/{uuid}-headshot.jpg path (use the same {uuid}-headshot.jpg form — derive the uuid via the subquery in the URL is not possible, so the header comment must instruct the orchestrator to substitute Andy Matthews' actual UUID captured in Task 2 into the url literal before applying), type='default', photo_license set to the license documented by the Task 2 script (public_domain if Wikimedia PD; otherwise the verified license). Guard with WHERE NOT EXISTS on politician_id. Do NOT include photo_origin_url (Pitfall 5 — the column does not exist). Do NOT add a schema_migrations registration line. INLINE ORCHESTRATOR STEP (documented in the header, not executed by the executor): after the Task 2 script uploads the image, substitute the real UUID into the url literal and apply this file via mcp__supabase-local__execute_sql.
  </action>
  <verify>
    <automated>test -f "C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql" &amp;&amp; grep -q "politician_images" "C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql" &amp;&amp; grep -q "\-3200006" "C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql" &amp;&amp; grep -q "'default'" "C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql" &amp;&amp; ! grep -q "photo_origin_url" "C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql" &amp;&amp; ! grep -q "schema_migrations" "C:/EV-Accounts/backend/migrations/1052_nv_controller_headshot.sql" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 1052_nv_controller_headshot.sql exists with exactly one politician_images INSERT for external_id=-3200006.
    - Column list is exactly (id, politician_id, url, type, photo_license) — photo_origin_url absent.
    - type='default'; INSERT guarded by WHERE NOT EXISTS on politician_id.
    - NO schema_migrations registration (audit-only; ledger stays 1050).
    - Header comment instructs the orchestrator to substitute the real Andy Matthews UUID into the url before applying via execute_sql.
    - INLINE: after upload, file applied; SELECT confirms 1 politician_images row with non-null url for external_id=-3200006.
  </acceptance_criteria>
  <done>1052_nv_controller_headshot.sql written; (inline) headshot uploaded and the politician_images row applied so the Controller has a non-null 600x750 headshot URL.</done>
</task>

</tasks>

<verification>
After all tasks (inline orchestrator):
1. STATE_EXEC audit: politicians→offices→chambers→governments WHERE geo_id='32' AND district_type='STATE_EXEC' → 6 rows, all with non-null politician_images.url.
2. Section-split SQL (159-PATTERNS.md "Section-Split Verification SQL") → 0 rows.
3. No duplicate Controller district: SELECT count WHERE district_type='STATE_EXEC' AND state='NV' AND label='Nevada Controller' → 1.
4. supabase_migrations.schema_migrations contains ('1050','nv_controller'); MAX version = 1050 (1052 not registered).
</verification>

<success_criteria>
- Andy Matthews (external_id=-3200006) is the 6th STATE_EXEC official under State of Nevada (geo_id='32') with a chamber, district, office, and office_id back-filled.
- Controller has a 600x750 headshot at politician_photos/{uuid}-headshot.jpg with a documented license.
- Zero section-split defects; zero duplicate Controller districts.
- The 5 pre-existing constitutional officers are untouched.
</success_criteria>

<output>
Create `.planning/phases/159-nevada-state-federal-government/159-01-SUMMARY.md` when done.
</output>
