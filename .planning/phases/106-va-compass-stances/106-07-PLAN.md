---
phase: 106-va-compass-stances
plan: 07
type: execute
wave: 7
depends_on:
  - 106-06
files_modified:
  - C:/EV-Accounts/backend/migrations/338_rief_stances.sql
  - C:/EV-Accounts/backend/migrations/339_harris_stances.sql
  - C:/EV-Accounts/backend/migrations/340_abdalla_stances.sql
  - C:/EV-Accounts/backend/migrations/341_beaty_stances.sql
  - C:/EV-Accounts/backend/migrations/342_carmichael_booz_stances.sql
  - C:/EV-Accounts/backend/migrations/343_kenley_stances.sql
  - C:/EV-Accounts/backend/migrations/344_reyna_stances.sql
  - C:/EV-Accounts/backend/migrations/345_scioscia_stances.sql
  - C:/EV-Accounts/backend/migrations/346_simpson_baird_stances.sql
autonomous: true
requirements:
  - VA-STANCES-03
must_haves:
  truths:
    - "Each ACPS board member with public-record evidence has a stance migration applied"
    - "Members with no public record found within the 5-minute sliding cap (per D-03/D-04) have NO migration row, documented as 'no public record'"
    - "Every applied migration has 100% citation rate (uncited = 0) and paired rows (unpaired = 0)"
    - "ACPS members may only yield education-adjacent findings (school-vouchers, childcare); that is acceptable per D-04"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/338_rief_stances.sql"
      provides: "Stance migration for ACPS Chair Rief (if evidence found)"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "33{8..9}_*_stances.sql / 34{0..6}_*_stances.sql"
      to: "essentials.politicians (external_id BETWEEN -5100090009 AND -5100090001)"
      via: "hardcoded politician_id UUID resolved at plan time"
      pattern: "INSERT INTO inform.politician_answers"
---

<objective>
Research each ACPS school board member's public record on all currently-active compass topics, applying the 5-minute sliding cap per D-03/D-04. Write per-individual stance migrations 338-346 ONLY for members with evidence found within cap. Apply each migration immediately after research per D-06. This closes VA-STANCES-03 and prepares for compass-render verification (Plan 08).

Purpose: Completes VA-STANCES-03 (ACPS board half). Best-effort scope — ACPS members frequently yield only school-vouchers/childcare findings per D-04; that is acceptable.

Output: 0-9 migration files applied (depends on what record each member has); summary documents per-member outcomes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/106-va-compass-stances/106-CONTEXT.md
@.planning/phases/106-va-compass-stances/106-PATTERNS.md
@.planning/phases/106-va-compass-stances/106-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve all 9 ACPS board UUIDs + roster confirmation</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-03, D-04, D-08, D-10)
    - C:/EV-Accounts/backend/migrations/313_acps_school_board.sql (lines 127-400 — full roster + external_ids)
  </read_first>
  <action>
    Run via mcp__supabase-local__execute_sql:
      SELECT external_id, full_name, id
      FROM essentials.politicians
      WHERE external_id BETWEEN -5100090009 AND -5100090001
      ORDER BY external_id DESC;

    Expected 9 rows (confirmed from migration 313 ground truth):
      -5100090001  Michelle Rief (Chair)              -> migration 338
      -5100090002  Christopher Harris (Vice Chair)    -> migration 339
      -5100090003  Abdulahi Abdalla                   -> migration 340
      -5100090004  Tim Beaty                          -> migration 341
      -5100090005  Kelly Carmichael Booz              -> migration 342
      -5100090006  Donna Kenley                       -> migration 343
      -5100090007  Ryan Reyna                         -> migration 344
      -5100090008  Alexander Crider Scioscia          -> migration 345
      -5100090009  Ashley Simpson Baird               -> migration 346

    If counts differ, abort. Save mapping to `.planning/phases/106-va-compass-stances/_tmp-acps-roster.json`.
  </action>
  <verify>
    <automated>
      Via mcp__supabase-local__execute_sql:
        SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -5100090009 AND -5100090001;
      Must return 9.
      Then:
        Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-acps-roster.json"
    </automated>
  </verify>
  <done>
    - All 9 ACPS UUIDs resolved
    - Roster mapping saved
  </done>
</task>

<task type="auto">
  <name>Task 2: For each of 9 ACPS members SEQUENTIALLY (per D-08), research within 5-min cap and apply migration if evidence found</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/106-CONTEXT.md (D-03, D-04, D-08, D-10)
    - .planning/phases/106-va-compass-stances/106-PATTERNS.md ("Full Example")
    - C:/EV-Accounts/backend/migrations/282_md_exec_stances.sql (lines 1-120, 1233-1251)
    - C:/EV-Accounts/backend/migrations/326_spanberger_stances.sql (per-individual analog from this phase)
    - .planning/phases/106-va-compass-stances/_tmp-acps-roster.json
  </read_first>
  <action>
    Process the 9 board members IN ORDER (Rief -> Harris -> Abdalla -> Beaty -> Carmichael Booz -> Kenley -> Reyna -> Scioscia -> Simpson Baird). ONE at a time per D-08 — NEVER launch parallel research agents.

    For each member, follow the SAME sub-protocol used in Plan 06 Task 2:

    SUB-STEP A — Start 5-min timer. Launch ONE web-research subagent searching:
      - acps.k12.va.us school board member pages and meeting minutes
      - alxnow.com / Patch / Alexandria Times coverage
      - 2024 board election campaign sites (Reif/Harris ran citywide 2024)
      Topics LIKELY to yield ACPS-relevant evidence per D-04:
        - school-vouchers (board votes / public statements on voucher programs)
        - childcare (pre-K, daycare programs)
        - religious-freedom (curriculum/library content disputes)
        - civil-rights (equity/anti-discrimination policies)
        - trans-athletes (school athletic eligibility policy votes)
        - public-safety-approach (SRO programs, school safety)
        - judicial-* topics: highly unlikely
      Most non-education topics will yield no evidence — that is normal per D-04.

    SUB-STEP B — Cap rule per D-03: 5-min sliding cap. If a finding comes in during last 3 min, extend by 3 min and re-apply. No record after cap exhausted = declare "no public record" and SKIP migration.

    SUB-STEP C — If evidence found: write migration file `C:/EV-Accounts/backend/migrations/{N}_{lastname-lowercase-with-underscores-for-spaces}_stances.sql` per the mapping. Pattern: lowercase last name, hyphens/spaces converted to underscores (e.g., "Carmichael Booz" -> "carmichael_booz", "Simpson Baird" -> "simpson_baird"). Migration file MUST follow 326 pattern exactly (header, topic UUID reference block, BEGIN, INSERT pairs, COMMIT, verification comment, 100% citation, no defaults).

    SUB-STEP D — Apply immediately per D-06 via mcp__supabase-local__execute_sql; then verify per-member counts (answer_count, unpaired=0, uncited=0).

    SUB-STEP E — Move to next member sequentially.

    Save outcome log to `.planning/phases/106-va-compass-stances/_tmp-acps-outcomes.json` with same shape as Plan 06.
  </action>
  <verify>
    <automated>
      Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-acps-outcomes.json"
      # Via mcp__supabase-local__execute_sql, applied-migration quality gate:
      #   SELECT COUNT(*) FROM (
      #     SELECT p.external_id, COUNT(pa.*) AS n_ans,
      #            COUNT(pa.*) FILTER (WHERE pc.politician_id IS NULL) AS unpaired,
      #            COUNT(pc.*) FILTER (WHERE sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0) AS uncited
      #     FROM essentials.politicians p
      #     LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
      #     LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
      #     WHERE p.external_id BETWEEN -5100090009 AND -5100090001
      #     GROUP BY p.external_id
      #     HAVING COUNT(pa.*) > 0
      #   ) t WHERE unpaired > 0 OR uncited > 0;
      # Must return 0.
    </automated>
  </verify>
  <done>
    - All 9 ACPS members processed sequentially (per D-08)
    - For each: migration applied with paired+cited stances OR "no public record" documented
    - Outcome log saved
    - Zero defective migrations
  </done>
</task>

<task type="auto">
  <name>Task 3: Cleanup + VA-STANCES-03 final summary</name>
  <read_first>
    - .planning/phases/106-va-compass-stances/_tmp-acps-outcomes.json
    - .planning/phases/106-va-compass-stances/106-06-SUMMARY.md (Alexandria council portion)
  </read_first>
  <action>
    Step A: Run final ACPS phase-progress query via mcp__supabase-local__execute_sql:
      SELECT p.external_id, p.full_name, COUNT(pa.*) AS stance_count
      FROM essentials.politicians p
      LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
      WHERE p.external_id BETWEEN -5100090009 AND -5100090001
      GROUP BY p.external_id, p.full_name
      ORDER BY p.external_id DESC;
    Record 9-row result for SUMMARY.

    Step B: Run combined Alexandria+ACPS coverage query for VA-STANCES-03 closure verification:
      SELECT
        SUM(CASE WHEN external_id BETWEEN -5101000007 AND -5101000001 AND stance_count > 0 THEN 1 ELSE 0 END) AS council_with_stances,
        SUM(CASE WHEN external_id BETWEEN -5101000007 AND -5101000001 THEN 1 ELSE 0 END) AS council_total,
        SUM(CASE WHEN external_id BETWEEN -5100090009 AND -5100090001 AND stance_count > 0 THEN 1 ELSE 0 END) AS acps_with_stances,
        SUM(CASE WHEN external_id BETWEEN -5100090009 AND -5100090001 THEN 1 ELSE 0 END) AS acps_total
      FROM (
        SELECT p.external_id, COUNT(pa.*) AS stance_count
        FROM essentials.politicians p
        LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
        WHERE p.external_id BETWEEN -5101000007 AND -5101000001
           OR p.external_id BETWEEN -5100090009 AND -5100090001
        GROUP BY p.external_id
      ) t;
    council_total must = 7; acps_total must = 9. with_stances counts are best-effort.

    Step C: Delete `_tmp-acps-roster.json` and `_tmp-acps-outcomes.json`.
  </action>
  <verify>
    <automated>
      Not Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-acps-roster.json"
      Not Test-Path "C:/Transparent Motivations/essentials/.planning/phases/106-va-compass-stances/_tmp-acps-outcomes.json"
      # VA-STANCES-03 closure SELECT must return council_total=7 AND acps_total=9
    </automated>
  </verify>
  <done>
    - Temp files deleted
    - VA-STANCES-03 closure metrics captured
    - Summary ready
  </done>
</task>

</tasks>

<verification>
- Per-ACPS-member stance counts recorded
- Combined Alexandria+ACPS coverage metrics for VA-STANCES-03 closure
- All applied migrations are paired + cited (zero defective)
- D-03/D-04 sliding cap honored
- D-08 sequential processing honored
</verification>

<success_criteria>
- VA-STANCES-03 fully addressed (best-effort, per ROADMAP success criterion #3)
- All applied migrations satisfy quality gates
- Phase ready to advance to compass-render verification (Plan 08)
</success_criteria>

<output>
Create `.planning/phases/106-va-compass-stances/106-07-SUMMARY.md` documenting:
- 9-row per-ACPS-member result table
- Members with migrations applied (with stance counts and topics)
- Members with "no public record"
- Combined VA-STANCES-03 closure metrics (council + ACPS)
</output>
