---
phase: 115-boston-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/migrations/577_michelle_wu_stances.sql
autonomous: true
requirements:
  - MA-STANCES-05
must_haves:
  truths:
    - "Mayor Michelle Wu (external_id -2507000001) has at least 10 rows in inform.politician_answers — she is the most publicly documented Boston official and the anchor for MA-STANCES-05"
    - "100% citation rate for Wu: every inform.politician_context row has at least 1 URL in sources array (uncited = 0)"
    - "Zero unpaired rows for Wu: every politician_answers row has a matching politician_context row (unpaired = 0)"
    - "No defaulted stances: topics with zero evidence are omitted entirely — no neutral/center values inserted"
    - "city-sanitation (UUID 7687de4f-4d0b-462a-b803-bdfb23b16b42) is included if evidence found — Wu controls DPW and has documented composting/zero-waste initiatives"
    - "Pre-flight confirms 0 pre-existing stance rows for all Boston officials before writing migrations"
    - "Active topic count verified live from inform.compass_topics WHERE is_active=true; baseline is 44 as of 2026-06-12"
  artifacts:
    - path: "C:/EV-Accounts/backend/migrations/577_michelle_wu_stances.sql"
      provides: "Stance migration for Mayor Michelle Wu (LOCAL_EXEC, external_id -2507000001)"
      contains: "BEGIN; ... COMMIT;"
  key_links:
    - from: "577_michelle_wu_stances.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "ON CONFLICT (politician_id, topic_id) DO UPDATE upsert"
      pattern: "INSERT INTO inform.politician_answers / INSERT INTO inform.politician_context"
---

<objective>
Pre-flight verification + evidence-only compass stances for Mayor Michelle Wu (external_id -2507000001, LOCAL_EXEC, City of Boston). Wu has the richest public record of all Boston officials — 13 years in public life (City Councillor At-Large 2014–2021, Mayor since November 2021, re-elected November 2025). Target 10–20 stances covering housing, rent-regulation, climate-change, city-sanitation, local-environment, public-safety-approach, homelessness, transportation-priorities, immigration, taxes, and other topics with documented evidence.

Migration 577 = 577_michelle_wu_stances.sql. Applied immediately after research. This plan also performs the phase-wide pre-flight check to confirm zero pre-existing stances for all 21 Boston officials.

Output: Migration 577 applied; Wu has ≥10 stances; unpaired=0; uncited=0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md
@.planning/phases/115-boston-stances/115-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification — topic count + zero pre-existing Boston stances</name>
  <read_first>
    - .planning/phases/115-boston-stances/115-RESEARCH.md (Pattern 1: UUID resolution, Pattern 3: per-politician verification, Compass Topic Reference, Open Questions section)
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (lines 1–60: authoritative 44-topic UUID reference block — use this, not 111-PATTERNS.md which is missing data-centers)
  </read_first>
  <action>
    Run all three queries via mcp__supabase-local__execute_sql. Complete all three before proceeding to Task 2.

    Q1 — Resolve Mayor Wu UUID + confirm all 21 Boston officials present:
      SELECT external_id, full_name, id
      FROM essentials.politicians
      WHERE external_id IN (
        -2507000001,-2507000002,-2507000003,-2507000004,-2507000005,
        -2507000006,-2507000007,-2507000008,-2507000009,-2507000010,
        -2507000011,-2507000012,-2507000013,-2507000014
      )
      ORDER BY external_id;
    Expected: 14 rows (Mayor Wu + 13 councillors). Record the UUID for external_id=-2507000001 (Mayor Wu) — paste into migration 577 header.

    Also resolve School Committee:
      SELECT external_id, full_name, id
      FROM essentials.politicians
      WHERE external_id IN (
        -2502790001,-2502790002,-2502790003,-2502790004,
        -2502790005,-2502790006,-2502790007
      )
      ORDER BY external_id;
    Expected: 7 rows.

    Q2 — Active compass topics (live count must be verified, baseline=44):
      SELECT slug, id FROM inform.compass_topics WHERE is_active = true ORDER BY slug;
    Record full slug+UUID list. If count differs from 44, capture new topics and include their UUIDs in the migration header for 577. The 416_christopher_flanagan_stances.sql header is the authoritative topic block for 44-topic parity.

    Q3 — Pre-flight: zero pre-existing Boston stances:
      SELECT COUNT(*) AS existing_count
      FROM inform.politician_answers pa
      JOIN essentials.politicians p ON p.id = pa.politician_id
      WHERE p.external_id BETWEEN -2507000014 AND -2507000001
         OR p.external_id BETWEEN -2502790007 AND -2502790001;
    Expected: 0 (no stances exist yet for Phase 115 officials). If non-zero, document the count — ON CONFLICT DO UPDATE handles it correctly but note the baseline.
  </action>
  <verify>
    <automated>
      Q1 must return 14 rows for council+Mayor range (confirm external_id=-2507000001 present).
      Second Q1 must return 7 rows for school committee range.
      Q2 must return at least 44 rows (active topics exist; record actual count).
      Q3 result recorded (0 expected; non-zero documented as upsert baseline).
    </automated>
  </verify>
  <acceptance_criteria>
    - Mayor Wu UUID captured from Q1 result for external_id=-2507000001
    - All 14 council/mayor politicians confirmed present (14 rows from Q1)
    - All 7 school committee members confirmed present (7 rows from second Q1)
    - Active topic count captured from Q2 (baseline 44; any additions appended to migration headers)
    - Q3 result documented (0 or N pre-existing rows noted)
  </acceptance_criteria>
  <done>
    - 21 Boston official UUIDs confirmed in production DB
    - Active topic list captured from inform.compass_topics
    - Pre-existing stance count recorded
  </done>
</task>

<task type="auto">
  <name>Task 2: Mayor Michelle Wu (external_id -2507000001) — migration 577</name>
  <read_first>
    - C:/EV-Accounts/backend/migrations/416_christopher_flanagan_stances.sql (full file: exact SQL format, topic UUID reference block, ON CONFLICT pattern, dollar-quoting for reasoning, sources ARRAY cast)
    - .planning/phases/115-boston-stances/115-RESEARCH.md (Mayor Wu section: expected stance topics, research sources, Pitfalls 3 and 4 — judicial topics and city-sanitation)
    - .planning/phases/111-ma-stances-execs-federal/111-CONTEXT.md (D-01: no defaults; D-08: one at a time; D-10: 100% citation)
  </read_first>
  <action>
    Complete Steps A through D sequentially. Do NOT move to Plan 02 until Step D passes.

    Step A — Research (web search, ONE session, not parallel per D-08):
      Politician: Mayor Michelle Wu, City of Boston (LOCAL_EXEC, external_id -2507000001).
      Primary sources:
        - https://boston.gov/mayor (official statements, policy announcements)
        - https://www.wbur.org/tag/michelle-wu (most comprehensive coverage)
        - https://ballotpedia.org/Michelle_Wu (policy positions, voting record as councillor)
        - https://boston.legistar.com/ (council voting records pre-2021)
        - https://www.bostonglobe.com/tags/michelle-wu/ (Globe coverage)
      Attempt all active compass topics from Task 1 Q2. Priority topics (expect strong evidence):
        housing, rent-regulation, residential-zoning, climate-change, local-environment, city-sanitation,
        public-safety-approach, homelessness, homelessness-response, transportation-priorities,
        immigration, local-immigration, taxes, economic-development, growth-and-development, healthcare, childcare.
      For city-sanitation: Wu has documented composting mandate (2023), zero-waste initiatives, DPW restructuring — this IS a first-tier topic for her.
      For judicial topics: only include if Wu made specific public statements on judicial philosophy (she is a former City Councillor, not a judge or prosecutor; blank spokes expected for most judicial topics).
      Per D-01: omit topics with no public evidence. Do NOT insert neutral defaults.
      For each evidenced topic record: { topic_slug, topic_uuid (from Q2), value (1–5 integer), reasoning (2–5 sentences citing specific votes/actions/statements), sources (1+ full URLs, not bare domains) }.

    Step B — Write migration file:
      File: C:/EV-Accounts/backend/migrations/577_michelle_wu_stances.sql
      Header: "Migration 577: Michelle Wu Stances" with purpose comment (Mayor, City of Boston, LOCAL_EXEC, external_id -2507000001).
      Topic UUID block: copy from 416_christopher_flanagan_stances.sql header (44-topic block); append any new topics found in Task 1 Q2.
      Politician UUID comment: paste Wu's UUID from Task 1 Q1.
      Structure: BEGIN; then per-topic pairs (politician_answers INSERT first, then politician_context INSERT, both ON CONFLICT DO UPDATE); COMMIT;
      Reasoning strings: use $$ dollar-quoting (never single-quote concatenation).
      Sources: ARRAY['url1','url2']::text[]::text[] — at least 1 full URL per topic.
      If no evidence for any topic: write header + BEGIN; -- No evidence found for any active compass topic per D-01; COMMIT;

    Step C — Apply migration:
      Read full content of 577_michelle_wu_stances.sql and pass as query to mcp__supabase-local__execute_sql.

    Step D — Verify (run all three queries):
      SELECT COUNT(*) AS n FROM inform.politician_answers
      WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -2507000001);
      Expected: ≥10 (Wu has rich record; document exact count).

      SELECT COUNT(*) AS unpaired FROM inform.politician_answers pa
      LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
      WHERE pa.politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000001)
        AND pc.politician_id IS NULL;
      Required: 0.

      SELECT COUNT(*) AS uncited FROM inform.politician_context
      WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000001)
        AND (sources IS NULL OR array_length(sources,1) IS NULL OR array_length(sources,1)=0);
      Required: 0.
  </action>
  <verify>
    <automated>
      Test-Path "C:/EV-Accounts/backend/migrations/577_michelle_wu_stances.sql" returns True.
      DB: COUNT(*) for external_id=-2507000001 returns ≥10.
      DB: unpaired=0 for external_id=-2507000001.
      DB: uncited=0 for external_id=-2507000001.
    </automated>
  </verify>
  <acceptance_criteria>
    - File C:/EV-Accounts/backend/migrations/577_michelle_wu_stances.sql exists and contains BEGIN; ... COMMIT;
    - SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id=(SELECT id FROM essentials.politicians WHERE external_id=-2507000001) returns ≥10
    - Unpaired check returns 0 for Wu
    - Uncited check returns 0 for Wu
    - city-sanitation topic (UUID 7687de4f-4d0b-462a-b803-bdfb23b16b42) has a row if Wu evidence was found (Wu's composting mandate is documented public record)
  </acceptance_criteria>
  <done>
    - 577_michelle_wu_stances.sql applied to production
    - Wu has ≥10 stances; unpaired=0; uncited=0
    - Stance count and list of covered topics recorded for SUMMARY
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web research → SQL migration | Scraped reasoning text enters SQL dollar-quoted strings |
| mcp__supabase-local | All SQL executes against remote production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | inform.politician_answers reasoning text | mitigate | Dollar-quoting ($$...$$) for all reasoning strings — never single-quote concatenation |
| T-115-02 | Spoofing | sources array URLs | mitigate | Executor judgment: only real URLs from pages actually consulted; never fabricate |
| T-115-03 | Tampering | politician_id UUID | mitigate | Always resolve UUID via SELECT WHERE external_id=-2507000001; never copy from a prior migration |
</threat_model>

<verification>
After Task 2 Step D:
- 577_michelle_wu_stances.sql exists in C:/EV-Accounts/backend/migrations/
- Wu stance count ≥10 in inform.politician_answers
- Unpaired = 0, uncited = 0 for Wu
</verification>

<success_criteria>
- Pre-flight passed: 21 Boston officials confirmed in DB; active topic count captured; pre-existing stance baseline documented
- Migration 577 applied with ≥10 stances for Mayor Wu
- 100% citation rate (uncited=0) and 0 unpaired rows for Wu
- city-sanitation researched and included if evidence found
- No defaulted stances (D-01 honored)
</success_criteria>

<output>
Create `.planning/phases/115-boston-stances/115-01-SUMMARY.md` documenting:
- Pre-flight results (21 officials confirmed, topic count, pre-existing baseline)
- Wu stance count and list of covered topics
- Stance value table: topic_slug | value | evidence_summary
- unpaired=0, uncited=0 confirmed
- Next migration: 578
</output>
