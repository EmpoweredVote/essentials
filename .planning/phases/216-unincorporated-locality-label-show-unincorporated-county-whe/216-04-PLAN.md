---
phase: 216-unincorporated-locality-label
plan: 04
type: execute
wave: 4
depends_on: ["216-03"]
files_modified: []
autonomous: false
requirements: [LOC-04]
must_haves:
  truths:
    - "essentials frontend locality change is live on Render (main deployed)"
    - "An unincorporated Pima County, AZ search shows banner 'Unincorporated Pima County, AZ' via ADDRESS search"
    - "The same unincorporated point shows the same banner via COORDINATE search"
    - "A Tucson, AZ address still shows the city banner (incorporated/CDP path unchanged)"
    - "A search in an un-loaded state shows today's behavior (no false 'Unincorporated')"
    - "tribal_land badge, county, and browse-mode banners are unregressed"
  artifacts:
    - "216-04-SUMMARY.md with live UAT results (both modes) + operator sign-off"
  key_links:
    - "UAT MUST exercise BOTH address-mode AND coordinate-mode unincorporated fixtures (RESEARCH Pitfall 1 / phase gate)"
---

<objective>
Deploy the 216-03 frontend change to production (essentials, Render via push to `main`) and verify the
"Unincorporated {County}, ST" label end-to-end against live fixtures — in BOTH address and coordinate
search modes — with a live operator checkpoint. `representingCity` has no automated component test, so a
live UAT is the phase gate for LOC-04.

Purpose: Prove the full cross-repo feature works against production, and that the coordinate-mode path
(RESEARCH Pitfall 1) actually renders — not just the address path.
Output: Feature live; LOC-04 verified end-to-end; phase ready for /gsd-verify-work.

**Repo:** this repo (essentials), pushes `main` → Render. This plan writes NO source files (deploy + UAT).
App URL: essentials.empowered.vote.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-CONTEXT.md
@.planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full frontend suite + build, then commit + push to Render (main)</name>
  <read_first>
    - .planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-03-SUMMARY.md
  </read_first>
  <action>
    From the essentials repo root, run the full Vitest suite (`npm test`) and a production build
    (`npm run build`) to confirm no regression. Then commit the 216-03 changes and push to `main` to
    trigger the Render deploy. Guard against the known reset-wipe hazard: before any operation, confirm
    `git log origin/main..HEAD` reflects only the intended local commits (project memory: a prior hard
    reset wiped unpushed v24.0 work). Wait for the Render deploy to go live.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (full frontend Vitest suite) exits 0 — includes localityLabel + bannerProps tests
    - `npm run build` succeeds
    - `git log origin/main..HEAD` after push is empty (all intended commits pushed; no unexpected extras)
    - Render deploy for essentials reports the new commit live at essentials.empowered.vote
  </acceptance_criteria>
  <done>Frontend suite + build green; change deployed to production main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT sign-off — both search modes + controls + regression</name>
  <action>Walk the operator through the five live-verification steps below on essentials.empowered.vote; capture the outcome per fixture/mode; do not close the phase until approved.</action>
  <what-built>The "Unincorporated {County}, ST" locality banner, live on essentials.empowered.vote, rendering for both address and coordinate searches at unincorporated points in place-loaded states.</what-built>
  <how-to-verify>
    1. ADDRESS mode: search an unincorporated Pima County, AZ address (a parcel outside any incorporated
       AZ place). Confirm the city banner reads "Unincorporated Pima County, AZ" — NOT "Tucson" and NOT "Your City".
    2. COORDINATE mode: paste the decimal lat,lng of that same unincorporated point into the combobox.
       Confirm the banner again reads "Unincorporated Pima County, AZ" (this is the RESEARCH Pitfall 1 path
       — the hook is bypassed for coordinate searches; it must still render via coordLocality).
    3. INCORPORATED control: search a Tucson, AZ address — confirm the banner still shows the Tucson city
       banner (place path unchanged, D-04).
    4. UN-LOADED-state control: search an address in a state NOT in the gate list — confirm the banner is
       unchanged from today (no false "Unincorporated").
    5. REGRESSION: confirm the tribal_land badge (on a tribal address), the county display, and browse-mode
       city banners are unchanged.
    (Optional fixtures per CONTEXT: unincorporated LA County, CA; unincorporated Monroe County, IN — IN is
     place-loaded per the DB ground truth, so it is a VALID fixture here.)
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe which fixture/mode failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production frontend | Live deploy + live browser UAT against production essentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-10 | Denial (silent no-op) | coordinate-mode label | high | mitigate | UAT step 2 explicitly exercises coordinate mode (the Pitfall 1 path); approval requires BOTH modes rendering |
| T-216-11 | Integrity | regression to tribal/county/browse banners | medium | mitigate | UAT step 5 regression checks; additive-only change guarded by the retained representing_city hijack guard |
</threat_model>

<verification>
- Full frontend suite + build green pre-deploy
- Live UAT: both modes + incorporated control + un-loaded control + regression checks
- Operator sign-off captured in SUMMARY
</verification>

<success_criteria>
LOC-04 verified end-to-end in production: "Unincorporated {County}, ST" renders for unincorporated points
in place-loaded states via BOTH address and coordinate search, incorporated/CDP and un-loaded-state points
are unchanged, and tribal_land/county/browse banners are unregressed. Phase 216 ready for close.
</success_criteria>

<output>
Create `.planning/phases/216-unincorporated-locality-label-show-unincorporated-county-whe/216-04-SUMMARY.md`.
Record: deployed commit SHA, UAT results per fixture/mode, regression check outcomes, operator approval.
</output>
