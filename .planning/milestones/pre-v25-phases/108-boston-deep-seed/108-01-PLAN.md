---
phase: 108-boston-deep-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/scripts/load-boston-council-boundaries.ts
  - C:/EV-Accounts/backend/migrations/347_boston_government.sql
  - C:/EV-Accounts/backend/scripts/_apply-migration-347.ts
autonomous: true
requirements: [MA-DEEP-01]
must_haves:
  truths:
    - "A Boston citywide address returns Mayor Wu in the LOCAL_EXEC section"
    - "A South Boston (District 2) address returns Edward M. Flynn as its district councillor plus the 4 at-large councillors"
    - "All 13 councillors and the Mayor are seeded under the City of Boston government with correct district links"
    - "9 council district geofences exist in geofence_boundaries with mtfcc='X0013'"
    - "D-01: Phase delivered as the lead plan of the 3-plan structure (city government / school committee / headshots) mirroring Alexandria Phase 103"
    - "D-02: This phase's migrations start at 347 (last v12.0 migration was 346; Phase 107 was verification-only) — Plan 01 writes migration 347"
    - "D-06: Election methods resolved by research — district seats use 'fptp', at-large seats use 'plurality_at_large' (School Committee SC=NULL handled in Plan 02)"
    - "D-08: External ID scheme — Mayor Wu=-2507000001, Councillors=-2507000002 through -2507000014 (geo_id-based scheme)"
    - "D-09: Mayor Wu is LOCAL_EXEC district_type; the 13 City Councillors are LOCAL district_type"
    - "D-10: districts.state='ma' (lowercase) on all LOCAL_EXEC and LOCAL district rows"
    - "D-11: governments.state='MA' (uppercase) and offices.representing_state='MA' (uppercase)"
    - "D-19: Boston government row name is exactly 'City of Boston, Massachusetts, US' (type='LOCAL', state='MA', city='Boston', geo_id='2507000')"
    - "D-20: geofence_boundaries.state='25' (FIPS numeric string for Massachusetts) on the X0013 council-district rows"
  artifacts:
    - path: "C:/EV-Accounts/backend/scripts/load-boston-council-boundaries.ts"
      provides: "ArcGIS loader for 9 Boston council district geofences (X0013)"
      contains: "X0013"
    - path: "C:/EV-Accounts/backend/migrations/347_boston_government.sql"
      provides: "City of Boston government + City Council chamber + 11 districts + 14 politicians + 14 offices"
      contains: "City of Boston, Massachusetts, US"
    - path: "C:/EV-Accounts/backend/scripts/_apply-migration-347.ts"
      provides: "Apply + smoke-test runner for migration 347"
      contains: "347"
  key_links:
    - from: "347_boston_government.sql"
      to: "geofence_boundaries (X0013 rows)"
      via: "pre-flight assertion + per-district LOCAL district rows referencing boston-ma-council-district-{N}"
      pattern: "boston-ma-council-district-"
    - from: "essentials.offices"
      to: "essentials.districts"
      via: "district_id link by geo_id + district_type + state"
      pattern: "district_type = 'LOCAL'"
---

<objective>
Seed the City of Boston government structure: Mayor Wu (LOCAL_EXEC) + 13 City Councillors (4 at-large + 9 single-member district) under geo_id='2507000', with one "City Council" chamber, 11 district rows, 14 politicians, and 14 offices. This requires first loading the 9 council district geofences (mtfcc='X0013') from Boston's ArcGIS FeatureServer.

This is the lead plan of the 3-plan Phase 108 structure (D-01: city government / school committee / headshots, mirroring Alexandria Phase 103) and writes the first migration of the phase (D-02: migration 347, since the last v12.0 migration was 346 and Phase 107 was verification-only).

Purpose: Closes MA-DEEP-01 — a Boston address must return a LOCAL section with Mayor Wu + the correct district councillor + the 4 at-large councillors.
Output: `load-boston-council-boundaries.ts` (new loader), `347_boston_government.sql` (new migration), `_apply-migration-347.ts` (apply runner).

CRITICAL RESEARCH CORRECTION (overrides CONTEXT.md D-07): Boston City Council is NOT all at-large. It is 9 single-member district seats + 4 at-large seats. District councillors must link to per-district geofences. D-12 (mtfcc=NULL) applies ONLY to the LOCAL_EXEC and at-large LOCAL rows; the 9 per-district rows use mtfcc='X0013'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/108-boston-deep-seed/108-CONTEXT.md
@.planning/phases/108-boston-deep-seed/108-RESEARCH.md
@.planning/phases/108-boston-deep-seed/108-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run the Boston council district geofence loader (9 districts, X0013)</name>
  <files>C:/EV-Accounts/backend/scripts/load-boston-council-boundaries.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/load-portland-council-boundaries.ts (EXACT analog — copy header block, config block, DB pool, fetchJson helper, pre-flight, INSERT PostGIS pipeline, verify block, main() wrapper verbatim; adapt only the constants below)
    - .planning/phases/108-boston-deep-seed/108-PATTERNS.md (§ load-boston-council-boundaries.ts — line-referenced extraction of every block to copy)
    - .planning/phases/108-boston-deep-seed/108-RESEARCH.md (§ Boston Council District ArcGIS Source; § Common Pitfalls 5 and 7)
  </read_first>
  <action>
    Create `load-boston-council-boundaries.ts` by copying `load-portland-council-boundaries.ts` structure and adapting these constants: ARCGIS_BASE_URL='https://services.arcgis.com/sFnw0xNflSi8J0uh/arcgis/rest/services/CityCouncilDistricts_2023_5_25/FeatureServer/0/query', MTFCC='X0013', STATE='25' (Massachusetts FIPS numeric string — NOT 'MA' or 'ma'; per D-20), SOURCE='boston_city_council_districts_2023', EXPECTED_COUNT=9, MAX_DISTRICT=9. The district number field in the ArcGIS data is `DISTRICT` (integer 1-9), NOT Portland's field name — parse `props['DISTRICT']` and validate it is an integer in range 1..9. Construct geo_id as `boston-ma-council-district-${distNum}` and name as `District ${distNum}`. Fetch strategy: attempt bulk `where=1%3D1&outFields=DISTRICT,Councilor,LONGNAME&outSR=4326&f=geojson` FIRST; if `geojson.features.length < 9`, fall back to per-DISTRICT loop using `where=DISTRICT%3D${distNum}` (Portland OR had silent truncation — see Pitfall 7). outSR=4326 is REQUIRED (source is Web Mercator WKID 102100/3857). Use the exact INSERT with `public.ST_MakeValid(public.ST_ForcePolygonCCW(public.ST_SetSRID(public.ST_Force2D(public.ST_GeomFromGeoJSON($6)), 4326)))` and `ON CONFLICT (geo_id, mtfcc) DO NOTHING`. Always parameterize distNum and geometry via $N placeholders — never string-interpolate into SQL (V5 input-validation control). Pre-flight: assert mtfcc='X0013' is either unclaimed or contains only `boston-ma-council-district-%` rows. Run `--dry-run` first, then a live run from C:/EV-Accounts/backend. Note: C:/EV-Accounts is NOT a git repo — never run git there.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsx scripts/load-boston-council-boundaries.ts --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - load-boston-council-boundaries.ts contains `const MTFCC          = 'X0013';` (or equivalent assignment of 'X0013')
    - load-boston-council-boundaries.ts contains `const STATE          = '25';` (or equivalent — Massachusetts FIPS per D-20, not 'MA'/'ma')
    - load-boston-council-boundaries.ts contains the literal string `boston-ma-council-district-`
    - load-boston-council-boundaries.ts contains `outSR=4326` in the query URL construction
    - load-boston-council-boundaries.ts contains a per-DISTRICT fallback referencing `where=DISTRICT%3D` for the truncation case
    - load-boston-council-boundaries.ts contains `ST_MakeValid` and `ST_ForcePolygonCCW`
    - After the live run, a query `SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id LIKE 'boston-ma-council-district-%' AND mtfcc='X0013' AND state='25'` returns exactly 9
  </acceptance_criteria>
  <done>9 geofence_boundaries rows exist with geo_id='boston-ma-council-district-1'..'-9', mtfcc='X0013', state='25', each with a valid PostGIS geometry; dry-run reports 9 features fetched.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 347 — Boston government, council chamber, 11 districts, 14 politicians, 14 offices</name>
  <files>C:/EV-Accounts/backend/migrations/347_boston_government.sql</files>
  <read_first>
    - C:/EV-Accounts/backend/migrations/312_alexandria_government.sql (EXACT analog — government INSERT, chamber INSERT, LOCAL_EXEC/LOCAL district INSERTs, politician+office WITH/INSERT block, office_id back-fill, post-verification DO block, ledger INSERT)
    - .planning/phases/108-boston-deep-seed/108-PATTERNS.md (§ 347_boston_government.sql — every block with line references; § Shared Patterns; § Key Boston-Specific Adaptations)
    - .planning/phases/108-boston-deep-seed/108-RESEARCH.md (§ Current Roster (Verified) — exact names + external IDs; § Architecture Patterns Pattern 2; § Common Pitfalls 1, 3, 6)
  </read_first>
  <action>
    Create migration 347 following 312_alexandria_government.sql. Government row: name='City of Boston, Massachusetts, US' (D-19), type='LOCAL', state='MA' (uppercase, D-11), city='Boston', geo_id='2507000', guarded with WHERE NOT EXISTS on name (D-14 — no unique constraint on geo_id). One chamber: name='City Council', name_formal='Boston City Council', election_method matching the council model (D-06 — district seats 'fptp', at-large seats 'plurality_at_large'; set per the chamber/office model documented in RESEARCH.md) — do NOT include slug (D-13 — GENERATED ALWAYS column). Insert 11 district rows: (1) LOCAL_EXEC geo_id='2507000', mtfcc=NULL, state='ma' (D-10), label='Boston (Citywide)'; (2) LOCAL geo_id='2507000', mtfcc=NULL, state='ma', label='Boston (At-Large)'; (3-11) nine LOCAL rows geo_id='boston-ma-council-district-{N}', mtfcc='X0013', state='ma', label='District {N}' for N=1..9. Add a pre-flight DO block that RAISE EXCEPTIONs if fewer than 9 X0013 'boston-ma-council-district-%' rows exist (Pitfall 4 — geofences must be loaded by Task 1 first). Add a pre-flight DO block asserting the Boston G4110 geofence (geo_id='2507000', mtfcc='G4110') is present (D-18 — assert, do NOT re-insert). Add a pre-flight DO block asserting external_id range -2507000014..-2507000001 is clear. Seed 14 politicians via the WITH ins_p / INSERT offices pattern, all with party=NULL (D-15) and is_appointed=false, is_appointed_position=false (D-16 — Mayor + councillors are popularly elected). External IDs and district links (D-08, D-09): Michelle Wu=-2507000001 → LOCAL_EXEC geo_id='2507000', title='Mayor'; four at-large councillors Ruthzee Louijeune=-2507000002, Julia M. Mejia=-2507000003, Erin J. Murphy=-2507000004, Henry Santana=-2507000005 → LOCAL at-large geo_id='2507000', title='City Councillor'; nine district councillors Gabriela Coletta Zapata=-2507000006 (District 1), Edward M. Flynn=-2507000007 (District 2), John FitzGerald=-2507000008 (District 3), Brian Worrell=-2507000009 (District 4), Enrique J. Pepén=-2507000010 (District 5), Benjamin J. Weber=-2507000011 (District 6), Miniard Culpepper=-2507000012 (District 7), Sharon Durkan=-2507000013 (District 8), Liz Breadon=-2507000014 (District 9) → each links to LOCAL geo_id='boston-ma-council-district-{N}', title='City Councillor'. representing_state='MA' (uppercase, D-11). Preserve the é in 'Enrique J. Pepén' as UTF-8 (Pitfall 6 — same precedent as Peña-Melnyk). Liz Breadon's title is 'City Councillor' (not 'President') per Alexandria procedural-title precedent (Assumption A1); role_canonical=NULL. office_id back-fill UPDATE over range -2507000014..-2507000001. Post-verification DO block with gates: (a) 1 government, (b) 1 chamber, (c) 11 districts, (d) 14 politicians, (e) 14 offices, (f) section-split=0, (g) 0 NULL office_id. Final ledger INSERT: version='347' ON CONFLICT DO NOTHING. Do NOT apply via Supabase MCP in this task — Task 3 applies.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('C:/EV-Accounts/backend/migrations/347_boston_government.sql','utf8'); const must=['City of Boston, Massachusetts, US','boston-ma-council-district-9','-2507000014','Enrique J. Pepén','X0013',\"VALUES ('347')\"]; const miss=must.filter(m=>!s.includes(m)); if(miss.length){console.error('MISSING:',miss);process.exit(1)} console.log('all present')"</automated>
  </verify>
  <acceptance_criteria>
    - 347_boston_government.sql contains `City of Boston, Massachusetts, US`
    - 347_boston_government.sql contains `'City Council'` chamber INSERT with NO `slug` in the column list
    - 347_boston_government.sql contains all external IDs `-2507000001` through `-2507000014`
    - 347_boston_government.sql contains the UTF-8 literal `Enrique J. Pepén` (accented é, not stripped)
    - 347_boston_government.sql contains nine `boston-ma-council-district-{N}` geo_id references and exactly one with mtfcc 'X0013' per district row
    - 347_boston_government.sql contains a pre-flight `RAISE EXCEPTION` that fails when fewer than 9 X0013 council-district rows exist
    - 347_boston_government.sql contains a post-verification gate asserting `14` politicians and `14` offices and `11` districts
    - 347_boston_government.sql contains `VALUES ('347')`
  </acceptance_criteria>
  <done>Migration file written with WHERE NOT EXISTS guards, no slug in chamber INSERT, 11 district rows (1 LOCAL_EXEC + 1 at-large LOCAL + 9 per-district LOCAL), 14 politician+office blocks, office_id back-fill, 7-gate post-verification DO block, and ledger entry 347.</done>
</task>

<task type="auto">
  <name>Task 3: Write apply runner and apply migration 347 to production</name>
  <files>C:/EV-Accounts/backend/scripts/_apply-migration-347.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/scripts/_apply-migration-325.ts (EXACT analog — Pool setup, readFileSync of migration, pool.query(sql), smoke-test queries, error handling, pool.end())
    - .planning/phases/108-boston-deep-seed/108-PATTERNS.md (§ _apply-migration-{347|348|349}.ts — full smoke-test code for 347)
    - C:/EV-Accounts/backend/migrations/347_boston_government.sql (the migration this runner executes — read to confirm expected counts)
  </read_first>
  <action>
    Create `_apply-migration-347.ts` copying `_apply-migration-325.ts`. readFileSync `migrations/347_boston_government.sql`, run via `pool.query(sql)`, then run smoke tests and log each with its expected value: (1) Boston government row count (expected 1); (2) politicians in range -2507000014..-2507000001 (expected 14); (3) district count for state='ma' across geo_ids '2507000' + the 9 'boston-ma-council-district-{N}' (expected 11); (4) offices linked to those districts (expected 14); (5) ledger entry '347' PRESENT. Then RUN it from C:/EV-Accounts/backend via `npx tsx scripts/_apply-migration-347.ts`. The migration's own pre-flight will RAISE EXCEPTION if Task 1 geofences are missing — confirm Task 1 completed first. Apply EITHER through this runner OR via Supabase MCP apply_migration — do not double-apply (ON CONFLICT/WHERE NOT EXISTS guards make it idempotent but avoid noise). C:/EV-Accounts is NOT a git repo — never run git there.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsx scripts/_apply-migration-347.ts</automated>
  </verify>
  <acceptance_criteria>
    - _apply-migration-347.ts contains `347_boston_government.sql`
    - _apply-migration-347.ts logs an expected count of 14 for council politicians and 11 for districts
    - Running the apply script prints "Boston government rows: 1", "Boston council politicians: 14", "Boston districts: 11", and "Ledger entry 347: PRESENT"
    - A verification query confirms 0 rows where `office_id IS NULL` for external_id range -2507000014..-2507000001
    - A South Boston address resolution (or a query joining geofence_boundaries 'boston-ma-council-district-2' → districts → offices) returns Edward M. Flynn (external_id -2507000007)
  </acceptance_criteria>
  <done>Migration 347 applied to production; all 5 smoke tests pass with expected values; ledger entry 347 present; office_id back-fill complete (0 NULL).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ArcGIS FeatureServer → loader script | Untrusted external GeoJSON (district number + geometry) crosses into DB writes |
| Loader script → PostgreSQL | District number and geometry flow into SQL INSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | load-boston-council-boundaries.ts (DISTRICT field) | mitigate | Validate `distNum` is an integer in 1..9 via parseInt + range check before constructing geo_id or running any query; never string-interpolate into SQL — use $N parameters only |
| T-108-02 | Tampering | ArcGIS GeoJSON geometry | mitigate | Wrap geometry in `ST_MakeValid(ST_ForcePolygonCCW(ST_SetSRID(ST_Force2D(ST_GeomFromGeoJSON($6)), 4326)))` — same hardened pipeline as Portland OR loader |
| T-108-03 | Denial of Service | bulk ArcGIS fetch silent truncation | accept | Detected and mitigated functionally (per-DISTRICT fallback when features.length<9); no security impact, accept residual risk of source unavailability (GeoJSON direct-download fallback documented in RESEARCH.md) |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — `pg`, `tsx`, `dotenv` already installed (RESEARCH.md Package Legitimacy Audit: N/A). No install tasks in this plan. |
</threat_model>

<verification>
- 9 X0013 council-district geofences present (state='25')
- Boston government row = 1; City Council chamber = 1
- 11 districts (1 LOCAL_EXEC + 1 at-large LOCAL + 9 per-district LOCAL)
- 14 politicians (-2507000001..-2507000014), 14 offices, 0 NULL office_id
- Section-split check returns 0 orphan geofences
- District-2 address → Edward M. Flynn; at-large councillors appear for any Boston address
</verification>

<success_criteria>
- MA-DEEP-01 satisfied: a Boston address returns a LOCAL section with Mayor Wu, the resident's district councillor, and the 4 at-large councillors
- Migration 347 in ledger; all post-verification gates pass
</success_criteria>

<output>
Create `.planning/phases/108-boston-deep-seed/108-01-SUMMARY.md` when done
</output>
</output>
